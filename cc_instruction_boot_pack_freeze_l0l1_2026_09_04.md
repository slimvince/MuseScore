# CC dispatch — FREEZE THE `l0-l1` BOOT PACK: add the third `FROZEN` entry to the pack generator at digests you measure, correct the comment that states the subject is live, scope the docstring's why-a-freeze block to the two freezes it was written for, regenerate the manifest, close — NO ruling taken, NO pack directory written, NO member re-rendered, NO withheld family touched, NO session booted

> **Dispatch (Cowork writing side, 2026-09-04, the session that booted on
> `cowork_handoff_entry_one_hundred_and_one.md`, its first dispatch).** Applies **Ruling 17(a)** of
> `cowork_rulings_2026_08_31_decision_surface_sitting.md` — the standing test for when a boot pack is
> frozen — to a fact that ruling did not yet have: the `l0-l1` deriving session HAS RUN. **Written at a
> verified stop.** The tip was read by this side at both ref files with the file tools and is
> `655b4f774eb3ffb88cbadc4a6bac2acedd814dd5`, `origin/master` equal — the hash the hundred-and-first
> entry states as the tenth batch's end state — and the tenth writing's close section stands in the
> working copy of `cowork_away_returns.md`, read by this side with the file tools. **Nothing is
> running.** *(That the root carries no other live dispatch is the hundred-and-first entry's statement,
> not this side's measurement; this side verified the tip and the close section and nothing else about
> the disk population.)*
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** It is a **narrow batch over one Python file and its
> generated manifest.** **IT DECIDES NOTHING**: it takes no ruling, weighs no position, derives no
> statement about the analysis, opens no outgoing text, touches no row, no register and no open
> question. **IT RENDERS NO PACK MEMBER AND WRITES NOTHING INTO ANY PACK DIRECTORY** — the whole point
> of the act is that one pack directory stops being writable by the tool. **IT DOES NOT TOUCH ANY
> WITHHELD FAMILY**, neither the `CRITERION` table, the `VERDICTS` table nor the `WITHHELD` records:
> what a subject withholds is authored input the user rules, and this batch is about a subject whose
> session has already run. Its whole authored write set is **one new entry in the `FROZEN` table, two
> corrected comments, and the regenerated manifest**, plus the close files.
>
> **★ THE ORDERED FIRST READ.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; rule (a)'s
> `gating_ids` at `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` (DO NOT read
> the whole open-items index). `BUILD_AND_TEST.md` — CONDITIONAL, and this batch MEETS the condition
> (it runs the guard set): read it in full. **(2)** `cowork_handoff.md`, ITS TOPMOST ENTRY, and
> `cowork_handoff_entry_one_hundred_and_one.md` whole — the entry point this batch lands. **(3)**
> `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)** the sitting record's **§3q
> (Ruling 17) WHOLE** — the authority for this batch — and **§3al WHOLE**, which records that the
> `l0-l1` deriving session ran and what its run established about the pack. **(5)**
> `tools/audit/gen_derivation_boot_pack.py`: its module docstring, the `FROZEN` table with the comment
> that closes it, the functions `blob_sha1`, `verify_frozen`, `frozen_block`, `write_all` and
> `check_all`, and the branch of `build` that attaches a frozen subject's block to the manifest.
> **(6) No pack member, no brief, no derivation, no derived specification, no outgoing text and no part
> of the reading file is opened by this batch at all.**
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST, because none of them may be taken as familiar.**
> *A boot pack* — the set of files handed to a fresh Cowork session that derives a specification blind,
> with nothing else mounted; it is rendered into `tools/audit/derivation_boot_pack/<subject>/` by the
> generator named above. *A subject* — one deriving job the generator renders a pack for; there are
> three, `harmony-boundary`, `scoring-model` and `l0-l1`. *SPENT* — the generator's own word for a
> subject whose deriving session has run. *FROZEN* — the table in that generator which pins a spent
> subject's pack at one digest per file, so the tool stops re-rendering it and instead checks it against
> those digests. *A digest* — here, the git blob hash of a file's own bytes, the value
> `git hash-object <file>` prints. *The manifest* — `tools/audit/derivation_boot_pack.json`, the one
> file the generator writes outside the pack directories. *The withheld family* — material deliberately
> kept back from a deriving session so it cannot read off the answer it is chartered to derive; it is
> authored input and **this batch does not touch it**. *A changed passage* — one contiguous run of
> changed lines in a diff (what a diff tool calls a hunk). *The current commit* — the commit named by
> hash, never a bare branch tip.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, **no measurement of the ANALYSIS**, no
> design, no repair of the analysis, **no derivation of any statement**, NO SESSION BOOTED, no document
> archived, moved or deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit
> to any governing document (the two `STATUS.md` / `STATUS_ARCHIVE.md` acts of Task 2 excepted), to the
> reading file, to the derivation, to the derived specification, to the brief, to any pack member, to
> any register entry or register source, or to the suspension file** — **no ruling taken, no position
> weighed, no recommendation anywhere.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (`--check`) and again at the
> end (write mode, committed as the artifact of a real run). Verify every commit at the object by
> explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and any `git diff` over
> the whole working tree time out on this mount and are denied by the armed guard — use the per-path
> explicit-hash forms named in A1; a `git diff` with NO explicit hash is a D-253 breach. `git diff
> --no-index` against a working-tree path is denied by the guard: take every check between two explicit
> blob hashes, writing the working copy into the object store with `git hash-object -w` first, as the
> fifth to ninth batches did. **`gen_guard_state.py` DOES NOT RECOGNISE `--help` and silently selects
> its default WRITE mode** — established by the eighth batch and held by the ninth and tenth; name the
> mode you intend and hash the artifact afterwards. **Do not order or accept a changed-passage count as
> a check**: the property that carries the weight in Task 1 is the enumeration of every deleted line
> against the quoted anchors. **Commit subjects are given below without an apostrophe anywhere.** The
> reserved-word and vocabulary rules bind every line — *score* only the musical score, *key* only
> tonality, *bar* only the metric unit (the prohibiting sense is *exclude*), *note* only a pitch event,
> *instrument* never a script (say *measurement tool*), no numeric grade anywhere.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 17(a), §3q — the authority for this batch, verbatim:** *"THE TWO EXISTING PACKS ARE NOT
  RE-RENDERED. THEY ARE FROZEN, AND `--check` RANGES OVER LIVE SUBJECTS. … `harmony-boundary` and
  `scoring-model` are pinned at their current blobs as SPENT, with that hash STOP; nothing is
  re-rendered and nothing is lost, and `l0-l1` can go green on its own."*
- **The test that ruling states for what makes a subject SPENT, quoted from the generator's own
  docstring where it is written down:** *"A subject whose deriving session HAS RUN is SPENT: its pack
  is no longer an input to be kept current, it is the RECORD OF WHAT THAT SESSION WAS GIVEN."*
- **D-646, the ruled shape, verbatim as the generator quotes it:** *"a generated record that must
  outlive its own writer is frozen at an established snapshot, and the freeze is enforced by a hash
  STOP."*
- **The fact the ruling did not yet have, at the sitting record §3al:** *"★★ THE DETAIL-SPECIFICATION
  PHASE'S FIRST DERIVATION WAS DELIVERED 2026-09-02 and is
  `cowork_blind_derivation_l0_l1_2026_08_31.md`."*
- **What the generator says about `l0-l1` today, quoted verbatim because this batch replaces it:**
  *"`l0-l1` IS ABSENT DELIBERATELY, and its absence is what makes it LIVE: its deriving session has not
  run, so its pack is still an input and must stay current with its sources."* **The second half of
  that sentence is false at the current commit**, and correcting it is half of Task 1.
- **D-674, the filing convention, branch two:** a live governing surface has its BODY corrected and its
  former wording preserved in place (#12). Both comment corrections in Task 1 are branch-two acts: the
  superseded sentence stays at its own site, marked as superseded, and is not deleted.
- **D-672 (the stop rule):** *"A per-entry pass may be stopped at any member boundary, and the stop is
  RECORDED."* The member boundaries of this batch are the tasks. **Within Task 1 the freeze entry, the
  two comment corrections and the manifest regeneration are ONE member and stand or fall together** — a
  `FROZEN` entry whose surrounding comment still says the subject is live states something false about
  itself (#10), and a manifest not regenerated beside the entry leaves `--check` red for a reason this
  batch created.

## Premise ledger

- **FACT — the tip is `655b4f774eb3ffb88cbadc4a6bac2acedd814dd5` and `origin/master` is the SAME
  hash.** Read by this side at `.git/refs/heads/master` and `.git/refs/remotes/origin/master` with the
  file tools, 2026-09-04. **Re-read the refs yourself before Task 0.**
- **FACT — the guard state at the tip, read by this side at the working copy of
  `tools/audit/guard_state.json` (111,688 bytes):** `summary` → **run 76, passing 66, failing 10**. The
  ten failing entries, each read at its own record in that artifact:
  `gen_filing_convention_application.py --check`, `gen_artifact_inventory.py --check` (exit 2),
  `gen_artifact_inventory_surface.py --check`, `gen_test_construction_evidence.py --check`,
  `gen_retirement_caller_check.py --check`, `decisions/apply_soft_discard.py --check` (exit 2),
  `decisions/apply_residue_discard.py --check` (exit 2), `gen_epoch_write_path.py --check`,
  `gen_recognizer_establishment_sort.py --check`, `decisions/gen_cluster_dispositions.py --verify`.
- **FACT — AND IT IS THE ONE THAT MAKES THIS THE CHEAPEST MOMENT TO FREEZE.** In that same artifact
  `tools/audit/gen_derivation_boot_pack.py --check` records **exit 0, verdict PASS**, with stdout
  *"the derivation boot pack re-derives"*, *"harmony-boundary: FROZEN — 7 file(s) at their recorded
  blobs"*, *"scoring-model: FROZEN — 7 file(s) at their recorded blobs"*. **So `l0-l1` still re-renders
  byte-identically from its sources**: unlike the two packs frozen on 2026-08-31, this one is NOT
  stale, and the digests you take are therefore both what the sources render today and what its session
  was given.
- **FACT — the `l0-l1` pack directory holds TEN files**, enumerated by this side at a directory listing
  of `tools/audit/derivation_boot_pack/l0-l1` with their sizes:
  `00_READ_THIS_FIRST.md` (3,853), `01_the_phase_definitions.md` (18,850),
  `02_the_guiding_principles_and_the_conventions.md` (61,585), `03_the_writing_standards.md` (15,159),
  `04_the_dispatch_protocol.md` (104,078), `05_the_ratified_design_intent.md` (297,167),
  `06_the_defect_type_catalog.md` (5,968), `07_the_charter_the_layers_and_the_decisions.md` (29,248),
  `08_the_five_research_extracts.md` (77,287), `09_the_empirical_findings_ledger.md` (51,246). **Every
  one carries the same modification time, 2026-09-01 — before the deriving session ran on 2026-09-02**,
  so nothing has rewritten the directory since it was rendered. **Sizes are not digests: yours are the
  digests, and the count TEN is what your freeze must record, against the SEVEN each existing entry
  records.**
- **FACT — the two existing `FROZEN` entries carry `finding`, `date`, `reason` and `digests`**, and
  `verify_frozen` STOPs on an entry lacking any of the first three. Read at the file by this side. Your
  new entry carries all four, the first three quoted verbatim from this face and the fourth measured.
- **FACT — what `--check` does to a frozen subject, read at `verify_frozen` and `check_all`:** it is
  checked against its RECORDED DIGESTS in BOTH directions — every recorded file present and at its
  digest, and no file in the directory the freeze does not record — and any failure RAISES rather than
  joining the drift list. **And `write_all` skips a frozen subject entirely**, which is what stops the
  tool writing into that directory ever again.
- **FACT — the manifest gains a block and does not lose one**, read at `build`: for a frozen subject it
  sets `rec["★_FROZEN"] = frozen_block(subject)` on that subject's existing record. **So the predicted
  manifest diff is exactly one inserted block under the `l0-l1` subject record and nothing else** —
  which is a prediction for you to prove between two explicit blob hashes, not a figure of record.
- **FACT — the module docstring's WHY-A-FREEZE block is written of the two 2026-08-31 freezes and
  becomes misleading once a third entry exists.** Its ground is that members (2) and (4) of *"both
  spent packs"* no longer re-render because `CLAUDE.md` and `cowork_audit_protocol.md` have grown.
  **That ground does not hold of `l0-l1`, whose check passes today** — which is why edit (c) of Task 1
  scopes that block to the two freezes it was written for, and why `l0-l1`'s own ground is stated in
  its own entry and nowhere else (#6).

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications at your boot:
  **NONE.** This writing side made no edit to any tracked file this session. Untracked and to be landed
  by Task 0: `cowork_handoff_entry_one_hundred_and_one.md` and this dispatch — **two untracked paths.**
  **★ THE BOUND ON THAT DECLARATION, STATED RATHER THAN GLOSSED (#24), AND IT IS WIDER THAN THE TENTH
  BATCH'S:** this side ran **no git object query at all** this session and compared **no** working copy
  against a committed object. What stands behind A1 is the tenth close's own whole-population
  enumeration, this side's own act list since (which contains no write), and the agreement of **six**
  working-copy sizes with the sizes the hundred-and-first entry records — the sitting record
  (536,518), the reading file (1,102,100), `tools/audit/guard_state.json` (111,688),
  `cowork_away_returns.md` (1,656,067), the derived specification (138,737) and the tenth dispatch
  (47,211) — **an agreement with a REPORT, not with an object, and it is named as such.** **Your Task 0 enumeration is what ESTABLISHES A1; this
  declaration is what it is measured against.** **★ AN UNTRACKED-FILE CHECK AT THE ROOT WILL RETURN
  HUNDREDS OF NAMES** — the standing historical `cc_*` population — **and that is not a defect**; no
  dispatch of this line lands it. Any further `cowork_handoff_entry_*.md` or `cowork_rulings_*.md` at
  the root is the writing side's, is NOT landed and is NOT a STOP. A modification at any other tracked
  path, at boot or during the run other than by this batch's own ordered acts, is a STOP-and-report.
  **Check ordered (Task 0, first act, AFTER the ordered first reads):** the whole tracked population
  enumerated with the sanctioned tool `tools/audit/changed_paths.py`; per named untracked path
  `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`.
- **★ ASSUMPTION A1b — THE TEN PACK FILES ARE TRACKED AND UNMODIFIED, AND THIS ONE IS A PRECONDITION OF
  THE ACT RATHER THAN A BACKGROUND FACT.** A freeze pins the bytes on disk as the record of what a
  completed derivation was given, so if any of the ten differs from the current commit, the bytes on
  disk may not be that record. **Check ordered, before any edit:** for each of the ten paths, compare
  the working copy against the current commit's blob between two explicit hashes
  (`git hash-object -w` the working copy, `git rev-parse <tip>:<path>`, then `git diff` between the two
  hashes). **Any difference, and any one of the ten that is untracked, is a STOP-and-report: report it
  and freeze nothing.**
- **ASSUMPTION A2 — the reds at the `--check` run before your first edit.** The ten failing tools named
  above, exactly, and zero STOPs. **`gen_derivation_boot_pack.py --check` is PASSING and must be
  PASSING at your start-state run; if it is not, that is a STOP-and-report and nothing is frozen** —
  a pack that has already drifted must be reported, not pinned at whatever it has drifted to.
  **★ THE REDS THIS BATCH'S OWN ACTS MAY CAUSE ARE NAMED FROM THE TENTH BATCH'S DISPATCH AND ARE NOT
  ESTABLISHED BY THIS SIDE**, which read none of those tools: `gen_session_start_read_size.py --check`
  STALE if regenerated before the LAST edit to `STATUS.md` — regenerate LAST;
  `gen_l0_l1_outgoing_population.py --check` STALE if `STATUS.md`'s recorded-tier hits move under
  Task 2 — regenerate, report that no MEMBER of the population moved, a member moving being a
  STOP-and-report; `gen_artifact_inventory.py --check` and `gen_artifact_inventory_surface.py --check`
  are standing reds — report any difference, repair nothing. **Because that classing is inherited and
  not measured, classify every red you meet by its cause AT THE TOOL and report the class you
  measured**; a red outside this batch's own acts' subjects is a STOP-and-report.
- **ASSUMPTION A3 — the pin-membership regeneration.** `evidence_pin_membership.json` is predicted to
  move by ZERO. **The ground is what this batch does NOT touch rather than a claim about that tool's
  reach, which this side has not established:** this batch edits no ruling record and no register
  source, its whole write set being one `tools/audit/*.py` file, one generated artifact and the Task 2
  close files. **Regenerate, measure against the committed blob before accepting; movement is a
  STOP-and-report.**
- **ASSUMPTION A4 — the guard registry.** No tool added, none removed; population unchanged at **76**;
  failing set the ten named, plus none; zero STOPs throughout. **`gen_derivation_boot_pack.py --check`
  passes at the end state and its stdout carries THREE frozen lines, the new one reading
  `l0-l1: FROZEN — 10 file(s) at their recorded blobs`.**
- **ASSUMPTION A5 — byte-unchanged by this batch, verified at the objects after the last commit (P-3,
  footprint as an act-list):** every file of every pack directory, all three subjects, `l0-l1`'s ten
  included — **this batch renders no member and writes into no pack directory**; the reading file; the
  sitting record; the derivation; the derived specification; the brief; every outgoing text; every
  governing document except `STATUS.md` and `STATUS_ARCHIVE.md`; the suspension file; every register
  source; **every `tools/audit/*.py` except `gen_derivation_boot_pack.py` and Task 2's re-aiming of
  `gen_status_batch_bound.py`**. **In `gen_derivation_boot_pack.py` the diff is exactly the two edits
  Task 1 orders and nothing else. In `derivation_boot_pack.json` it is exactly the inserted `★_FROZEN`
  block under the `l0-l1` record.** Any other difference is a STOP-and-report.

## The declared start state — at the tree you will meet

1. The ten failing tools named in the FACT above — standing reds, none of them this batch's.
2. Zero STOPs; **`gen_derivation_boot_pack.py --check` PASSING**, printing two frozen subjects.
3. The artifact at the tip being the tip's own run, your start-state run is expected to reproduce it
   exactly; a difference is reported with its cause. **Name the mode you run it in** — the tool does
   not recognise `--help` and silently writes.

A failing verdict outside 1–3 is a STOP-and-report, save the A2 exceptions named.

## Findings in this batch

**No finding number is allocated by this dispatch.** A finding met during the run is written up on its
own and surfaced, never absorbed. **An anchor this face quotes that is not found verbatim at the file
is a STOP-and-report for THAT edit, and because Task 1's two edits and its manifest regeneration stand
or fall together, if any one of them cannot be made, Task 1 is not committed at all and the batch
reports and stops.**

## Task 0 — land the hundred-and-first entry and this dispatch, and PUSH

1. **A1's check** (after the ordered first reads), and **A1b's ten-path comparison**, which is a
   precondition: if any of the ten pack files differs from the current commit or is untracked, report
   and stop before Task 1.
2. **Commit exactly these paths and no other:** `cowork_handoff_entry_one_hundred_and_one.md` (new) and
   `cc_instruction_boot_pack_freeze_l0l1_2026_09_04.md` (new) — **two paths.** Subject, exactly:
   `record: the hundred and first entry landed with the boot pack freeze dispatch`
3. **PUSH**; verify at the ref file that `origin/master` equals the new tip.

**Under standing clause P-2 this dispatch is PINNED at Task 0:** record its blob, and prove the blob
unmoved immediately before staging it.

**E0:** the two named paths in one commit; `origin/master` at the commit; A1's whole-tree check
reported with its enumeration; A1b's ten comparisons reported one by one.

## Task 1 — the freeze, in ONE commit

**The order of the two edits and the regeneration is not free, and the ground is stated so it is not
reordered for convenience: the `FROZEN` entry is written FIRST, before the tool is run in any write
mode.** While
`l0-l1` is live, a write run re-renders its directory from today's sources; once the entry exists,
`write_all` skips that directory entirely. Adding the entry first therefore removes the only path by
which this batch could overwrite the record it exists to preserve.

**(a) MEASURE THE TEN DIGESTS.** For each of the ten files of `tools/audit/derivation_boot_pack/l0-l1`,
take `git hash-object <file>` **and** `git hash-object --no-filters <file>`, and **prove the two agree
for every one of the ten** — the same establishment the two existing entries carry, so the recorded
digest is the hash of the bytes on disk and not of a normalized form. **A disagreement on any file is a
STOP-and-report.**

**(b) ADD THE THIRD `FROZEN` ENTRY.** The anchor is the comment that closes the table, quoted here
whole and matched verbatim:

```
    # `l0-l1` IS ABSENT DELIBERATELY, and its absence is what makes it LIVE: its deriving session
    # has not run, so its pack is still an input and must stay current with its sources.
}
```

**Only the two comment lines are replaced** — by the new entry followed by the corrected comment —
**and the `}` line that closes the table is unchanged and stays where it is**, the new entry going
inside the table above it. The replacement is a D-674 branch-two act: the superseded sentence is
preserved at its own site, marked as superseded, and not deleted. The
entry's three authored fields are **quoted verbatim from this face and composed by you not at all**:

- `finding`: *"Its deriving session has RUN: the detail-specification phase's first derivation was
  delivered 2026-09-02 and is `cowork_blind_derivation_l0_l1_2026_08_31.md` (the sitting record,
  §3al). Unlike the two subjects frozen on 2026-08-31, this pack was NOT stale when it was frozen —
  `--check` PASSED at the tip's own guard run — so the digests recorded here are both what the sources
  render today and what its session was given."*
- `date`: *"2026-09-04"*
- `reason`: *"A spent subject's pack is the record of what its session was given rather than an input
  to be kept current (Ruling 17(a); D-646). Left live it would re-render on the first growth of
  `CLAUDE.md` or `cowork_audit_protocol.md` — the addition-only drift that made the other two packs
  stale — and would then either turn `--check` red or, if refreshed, back-date into a completed
  derivation's record rules that did not exist when its session ran (#12). Frozen at the blobs it
  carries, with the hash STOP."*

and the corrected comment reads:

```
    # `l0-l1` IS FROZEN ABOVE, ITS DERIVING SESSION HAVING RUN ON 2026-09-02; its own ground is
    # in its own entry and is not restated here (#6).
    # ★ SUPERSEDED 2026-09-04, THE FORMER WORDING PRESERVED IN PLACE (#12): "`l0-l1` IS ABSENT
    # DELIBERATELY, and its absence is what makes it LIVE: its deriving session has not run, so
    # its pack is still an input and must stay current with its sources."  True when written, and
    # made false by that session's run.
```

**(c) SCOPE THE DOCSTRING'S WHY-A-FREEZE BLOCK TO THE TWO FREEZES IT WAS WRITTEN FOR.** Its opening
sentence is the anchor, matched verbatim:

```
  WHY A FREEZE AND NOT A REFRESH, recorded here so a later reader meets the ground rather than
  the mechanism alone.  Members (2) and (4) of both spent packs no longer re-render, because
```

and becomes:

```
  WHY A FREEZE AND NOT A REFRESH, recorded here so a later reader meets the ground rather than
  the mechanism alone.  ★ WHAT FOLLOWS IS THE GROUND OF THE TWO FREEZES TAKEN 2026-08-31, and it
  is scoped to them: each subject's own ground is in its own entry (#6), and `l0-l1`, frozen
  2026-09-04, was NOT stale when it was frozen.  Members (2) and (4) of the two packs frozen that
  day no longer re-render, because
```

**No other line of the docstring is touched, and no other line of the file outside (b) and (c) and the
inserted entry is touched.**

**(d) REGENERATE THE MANIFEST**, naming the mode you run the tool in, then run `--check` and prove:
exit 0; stdout carrying three frozen lines with `l0-l1: FROZEN — 10 file(s) at their recorded blobs`.
**Prove at the object that no file of any pack directory moved** — for all three subjects, the
directories' files compared against the current commit's blobs — and that the manifest's diff is the
one inserted block. **Any pack-directory file that moved is a STOP-and-report and nothing is
committed.**

**Commit exactly these two paths and no other:** `tools/audit/gen_derivation_boot_pack.py` (modified)
and `tools/audit/derivation_boot_pack.json` (regenerated). Subject, exactly:
`freeze the l0-l1 boot pack at its rendered blobs, its deriving session having run`

**E1:** the ten digests taken twice and agreeing; the entry, the two comment corrections and the
regenerated manifest in one commit; `--check` green with three frozen subjects; every pack directory
proven byte-unchanged; the deleted-line enumeration for `gen_derivation_boot_pack.py` published, every
deleted line inside one of the two quoted anchors and none outside them.

## Task 2 — the close

1. **The `STATUS.md` entries and the forward-bound move, IN THIS ORDER.** **(a) FIRST write this
   batch's own entries** at the top of `STATUS.md`: one pointer entry per task that did work (Tasks 0
   and 1; no figure restated, D-431), **the close entry FIRST and NAMING THIS DISPATCH BY ITS FILE
   NAME, the earlier task's entry below it saying `Same dispatch`**, and **the `Last updated: ` prefix
   moved from the tenth writing's close entry to your newest entry.** The close entry states in one
   sentence that the `l0-l1` boot pack is frozen at the blobs it carries, its deriving session having
   run on 2026-09-02, that no pack member was re-rendered and no pack directory written, and that no
   withheld family was touched. **(b) THEN re-aim `gen_status_batch_bound.py` — ALL FIVE authored
   constants:** `BASE_COMMIT` (this batch's Task 1 commit), `PREVIOUS_BATCH_DISPATCH`
   (`cc_instruction_comparison_l0_l1_tenth_2026_09_04.md`), `ACT_DATE` (`2026-09-04`), `DISPATCH` (this
   dispatch's file name), `TASK` (`Task 2`); **and append to `PREVIOUS_AIMINGS`, kept rather than
   replaced (#12), the aiming you are superseding — read its five current values AT THE TOOL and carry
   them across unchanged, rather than transcribing them from this face, which does not state them.**
   Amend the head comments as the tenth did. **(c) THEN run `gen_status_batch_bound.py --apply`.** **State your prediction from the tool's
   own mechanism BEFORE you run it**, read at the tool, and report the run against it; **if the tool
   STOPs, restore it to its passing aim, re-run to prove it green, report the STOP with the tool's
   message, and move nothing by hand.** **(d) LAST**, after the final edit to `STATUS.md`,
   `gen_session_start_read_size.py` regenerated and its `--check` confirmed green before the commit.
2. The FULL close appended to `cowork_away_returns.md` as a **THE `l0-l1` BOOT PACK FROZEN AT ITS
   RENDERED BLOBS: THE THIRD FROZEN ENTRY, THE CLOSING COMMENT CORRECTED, THE DOCSTRING BLOCK SCOPED,
   NO MEMBER RE-RENDERED** section — **no ordinal is used in that title, this batch not being a writing
   of the L0/L1 comparison line; if that file's own convention requires one, report it rather than
   invent one** — both guard states, every SHA, E0–E2 graded, A1, A1b and A2–A5 graded,
   declared departures, **the ten digests published with the two-form agreement stated per file**, the
   deleted-line enumeration for the generator, and the plan lines (**L2's pack, cut with the §3al.4
   finding applied; then L2, sequenced by Ruling 10**), and **the plan's tell in one sentence: did this
   batch produce anything other than the landed records, the frozen entry, the two corrected comments,
   the regenerated manifest, the Task-2 files and the report? If yes, name it.** The report is
   `cc_report_boot_pack_freeze_l0l1_2026_09_04.md`, which likewise decides nothing.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **The ordered structure
   yields: Task 0, the freeze commit, the close, the end state.**

**E2 — DERIVED from the declared start state plus this batch's footprint (P-3):** at the tree carrying
the close, a fresh full guard run — **population 76**; **zero STOPs**; **the failing set exactly the
ten named, PLUS NONE**, the A2 exceptions handled as A2 says — committed only after the run that
produced it.

## What this batch does NOT do

- **No ruling taken, no position weighed, no verdict on any open question, on the derivation or on the
  deriving session's independence. No session booted. No measurement of the analysis. No derivation of
  any statement about the analysis.**
- **NO PACK MEMBER IS RENDERED AND NOTHING IS WRITTEN INTO ANY PACK DIRECTORY**, `l0-l1`'s ten files
  included. Re-rendering a member, or writing any file into a pack directory, is a STOP-and-report.
- **NO WITHHELD FAMILY IS TOUCHED** — not the `CRITERION` table, not the `VERDICTS` table, not the
  `WITHHELD` records, for any subject. **Nothing about L2's pack is prepared, authored or implied by
  this batch**, which reaches one spent subject only.
- **No `MEMBERS` entry, no `EXTRAS` entry and no span anchor is edited**, for any subject.
- **No edit to the reading file, the sitting record, the derivation, the derived specification, the
  brief, any outgoing text, any governing document other than the two Task 2 orders, the suspension
  file, or any register source. No open-items row created, flipped or discarded. No `D-NNN` allocated**
  — that register cannot accept one and `cowork_register_rule_c_suspension_2026_08_28.md` is the route.
- **No `src/` change, no golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`. No
  guard enrolment. No `tools/audit/*.py` written or edited except `gen_derivation_boot_pack.py`'s two
  ordered edits and Task 2's re-aiming of `gen_status_batch_bound.py` (D-648).**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* **#12** — nothing deleted: both superseded comment sentences are preserved at
   their own sites, and `PREVIOUS_AIMINGS` is appended to. **#10** — the file ceases to state something
   false about itself in the same act that makes it false, which is why the two edits and the
   regeneration are one member.
   **#6** — each subject's ground lives in its own entry; the docstring's shared block is scoped rather
   than duplicated. **#19** — nothing is established by this side: the digests are measured by the
   executing side in the two-form agreement the existing entries carry, and the tool's own hash STOP is
   what enforces them afterwards. **#13** — an anchor not found verbatim is a STOP. **#24** — the bound
   on A1 is stated and named as wider than the tenth batch's, with the direction of its gap given.
   **#17(f)/D-431** — no figure of any pack member is restated; the sizes on this face are a
   directory's own listing, published so the executing side can tell at once whether it is looking at
   the same directory, and the digests are not on this face at all.
2. *Conventions:* American English; no self-invented label — *a boot pack*, *a subject*, *SPENT*,
   *FROZEN*, *a digest*, *the manifest*, *the withheld family*, *a changed passage* and *the current
   commit* are all defined at the head; *measurement tool* never *instrument*; music-theory words in
   their musical sense; no numeric grade anywhere. A changed-passage count is explicitly refused as a
   check.
3. *Figures and premises:* the tip at the two ref files with the file tools; the guard summary, its ten
   failing entries and the pack tool's PASS verdict read by this side at the working copy of
   `tools/audit/guard_state.json`; the ten pack files, their sizes and their modification times at a
   directory listing; Ruling 17(a) and §3al read at the sitting record with the file tools; the
   `FROZEN` table, its closing comment, the docstring block, `blob_sha1`, `verify_frozen`,
   `frozen_block`, `build`, `write_all` and `check_all` read at the generator with the file tools.
   **Declared departures of this writing side, ONE, and it opened no repository file:** a single
   sandbox shell command over six integers quoted from the directory listing, converting recorded
   modification times to calendar dates — which is where the pack directory's *2026-09-01* on this face
   comes from. (*2026-09-02*, the date the deriving session ran, is the sitting record's own word at
   §3al and not a converted figure.) **No git object query was run and no repository file was read by any shell**, which is
   also why A1's bound is as wide as it is stated to be.
4. *File-tools rule:* every repository read through the file tools on bridge-staged snapshots; no
   `git status`, no working-tree diff, no `git log`, no interpreter read of any working-tree file.
5. *Uncertainty:* this side has NOT established that the ten pack files are tracked and unmodified —
   A1b orders that check and makes it a precondition rather than an assumption. This side has NOT
   measured whether any source of members (2) or (4) has grown since 2026-09-01; the start-state PASS
   verdict is read from the tip's own artifact and implies it had not grown as of that run.
