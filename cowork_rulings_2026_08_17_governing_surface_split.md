# User rulings — 2026-08-17 (the governing-surface split sitting)

> **STATUS: RULING RECORD, an interim carrier (D-230).** Taken by the user in conversation on
> 2026-08-17, at the verified return of the fifth preparation batch
> (`cc_instruction_preparation_fifth.md`), whose report and full close were read whole and
> verified at the objects by the writing side before any decision was put (the five commits
> named at the objects, the discard arithmetic re-derived at the data file: 512 live + 165
> retired = 677, the guard artifact read at its values, the full-close reading proved by
> quotation). The surface under ruling is
> `ratification_surfaces/cowork_governing_surface_split_2026_08_16.md`, generated by the batch's
> read-only Task 2. Each decision is presented as a self-contained plain-language surface with
> alternatives, costs named to principles, and a recommendation. **The classification of each
> ruling — decision or exercise — is OWED and not made here**, and no decisions-register entry
> is written.
>
> **This record is appended to as the sitting proceeds** — one section per decision, written in
> the turn the ruling is given, so the record survives the session at every point.

## 1. Ruling — the per-class fates (Alternative A, as proposed on the surface)

Put as Decision 1 of the sitting, the surface's §4 restated self-contained with the measured
shares and the concrete effect (`OPEN_ITEMS.md` about −60%, `CLAUDE.md` about −23% before
Decision 4). **The user's word, verbatim: "agree." RULED — adopted whole:**

- **operative-rule-text STAYS AT SITE**, including every doubt-defaulted span — the ruled
  default (§5(E) of `cowork_rulings_2026_08_16_preparation_return.md`) applied, never a
  stretched judgment.
- **resolved-row, self-declared-historical-or-superseded, preserved-former-wording, and
  defense-and-declined-alternatives ARCHIVE**, each span moved VERBATIM to its archive home
  with a dated pointer left at the site; #12 satisfied by preservation
  elsewhere-with-record, on the register-split precedent.
- **The defense class stays NARROW as the surface states it**: only spans carrying an explicit
  declined-alternative or accepted-cost marker; a rule's PURPOSE is operative and stays at
  site.
- **pointer-entry-of-a-completed-batch is NOT decided by this ruling** — it is Decision 4 of
  this sitting, under `STATUS.md`'s own archive rule and ruling (C).
- **The safeguard binds the executing dispatch:** it READS every span it archives rather than
  trusting its class — the F29 residual risk runs in the archive direction, and the
  read-before-move is the mitigation.
- Alternatives B (keep an archive class at site) and C (rule nothing per class until a finer
  measurement) are DECLINED as rated in the presentation.

*(Further sections follow as the sitting proceeds.)*

## 2. Ruling — the doubt-defaulted share is ACCEPTED as it stands (Alternative A)

Put as Decision 2 of the sitting, self-contained: the doubt-defaulted share is the largest
share of every one of the five files (77.0% of `CLAUDE.md`, 40.1% of `OPEN_ITEMS.md`, 92.8% of
`DECISIONS.md`, 9.3% of `STATUS.md`, 91.6% of `BUILD_AND_TEST.md`), with the reading stated
that a high share is not the same thing in every file — `DECISIONS.md`'s and
`BUILD_AND_TEST.md`'s unplaced text is almost certainly genuinely operative, and the file where
the share plausibly hides more archive material is `CLAUDE.md`. **The user's word, verbatim:
"A." RULED — adopted whole:**

- **The wave executes on today's measurement.** Every doubt-defaulted span STAYS AT SITE under
  the ruled default; no finer measured pass is ordered and none blocks the executing dispatch.
- **Nothing is foreclosed:** staying is the recoverable direction — a later measured pass over
  `CLAUDE.md`'s doubt-defaulted remainder stays available at any time, the continuous-pruning
  rule (§5(D) of the 2026-08-16 record) erodes the share at every future amendment, and the
  phase retrospective is the recorded place to judge, with post-wave numbers in hand, whether
  a finer pass is still worth commissioning.
- Alternatives B (hold the wave for a finer pass first — certain ongoing harm against a
  speculative gain, and finer recognizers err in the archive direction) and C (commission the
  `CLAUDE.md` follow-up today — work whose worth is better judged after the wave lands) are
  DECLINED as rated in the presentation.

## 3. Ruling — the archive destinations: per-file companions on the established pattern (Alternative A)

Put as Decision 3 of the sitting, self-contained, grounded at the tree first: two archive files
exist today (`STATUS_ARCHIVE.md`, `cowork_handoff_archive.md`), both per-file companions; none
exists for the other four governing files. **The user's word, verbatim: "A." RULED — adopted
whole:**

- **One archive companion per governing file:** `CLAUDE_ARCHIVE.md`, `OPEN_ITEMS_ARCHIVE.md`,
  `DECISIONS_ARCHIVE.md` and `BUILD_AND_TEST_ARCHIVE.md`, each CREATED by the executing
  dispatch on the established pattern (a plain file receiving verbatim moves — the shape
  `STATUS_ARCHIVE.md` and `cowork_handoff_archive.md` were each born in, and the shape §5(D)
  of the 2026-08-16 record names for homes that do not yet exist).
- **`STATUS_ARCHIVE.md` continues as `STATUS.md`'s home** — no new destination is invented for
  the one file that already has one.
- **Every dated pointer left at a site names its own companion file**, so the reader re-opening
  a decision goes from the governing file to its companion and nowhere else; each companion
  receives spans in the order they left the parent.
- Alternatives B (one combined archive — mixes five files' histories, departs from the pattern
  both existing archives set) and C (per-class archives — matches no actual readership, since a
  reader re-opens a decision in a document, not a class) are DECLINED as rated in the
  presentation.

## 4. Ruling — `STATUS.md`'s superseded entries: an entry is superseded the moment a later batch's close exists (Alternative A)

Put as Decision 4 of the sitting, self-contained: 129 dated batch entries, 453,664 characters,
89.8% of the file — the largest single block of the five mandatory reads; each entry a POINTER
by design (D-431), its substance living in the batch's report and its full close in
`cowork_away_returns.md`; the file's own archive rule (*a superseded entry moves to
`STATUS_ARCHIVE.md` instead of accumulating*) never defined which entries count as superseded,
which is the open half of [[OI-370]]. The one live session-start readership stated: the branch
rule reads the LATEST batch's pointers only. **The user's word, verbatim: "A." RULED — adopted
whole:**

- **An entry is SUPERSEDED the moment a later batch's close exists.** The site keeps only the
  latest batch's entries.
- **The executing dispatch clears the backlog** — every entry except the newest batch's moves
  verbatim to `STATUS_ARCHIVE.md` under the file's own rule, which ruling (C) of the
  2026-08-16 record already brings into the act — **and [[OI-370]]'s open half is discharged
  at that dispatch through the register's ordinary mechanism.**
- **The bound is maintained forward as an instance of the continuous-pruning rule (§5(D)):**
  every future batch close, in the same act that writes its own entries, moves the
  then-previous batch's entries to the archive. The file stays permanently bounded at roughly
  one batch's worth of entries plus its operative text.
- Alternatives B (keep the current phase's entries — no boot reader, the line moves every
  phase) and C (a per-entry sitting — the entries are mechanically alike, so a walk buys no
  discrimination and costs a cycle) are DECLINED as rated in the presentation.

## 5. The sitting's close

All four decisions of `ratification_surfaces/cowork_governing_surface_split_2026_08_16.md` §6
are ruled. No presentation-form correction fired in this sitting — zero tells under
`cowork_rulings_2026_08_15_session_length.md`, recorded because the count is the instruction's
own measure. The executing dispatch for §§1–4 together is the preparation phase's sixth batch;
this record rides its Task 0. Per §7 of the 2026-08-16 record (fresh sessions every dispatch
cycle until the pruning lands), the writing session that drafted that dispatch closes at the
verified stop after drafting it.

*Provenance: Cowork, 2026-08-17, recorded in the turns the rulings were given, at the verified
return of the fifth preparation batch. The user's verbatim words are marked as such; everything
else is the writing side's operationalization, accepted in sequence. The presentation of each
decision restated the surface's content self-contained in plain language with alternatives,
costs named to principles, and a recommendation; the user ruled A on all four.*
