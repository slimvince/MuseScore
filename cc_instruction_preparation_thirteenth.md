# CC dispatch — the preparation phase's THIRTEENTH batch: the recognizer establishment clause landed, the sort derived and published as data, and two explicit do-nothings

> **Dispatch (Cowork, 2026-08-19).** Executes the three rulings of
> `cowork_rulings_2026_08_19_twelfth_return.md`. **Written at a verified stop; nothing else is running
> and no dispatch was active when this was written.**
>
> **★ THIS IS A SMALL BATCH AND TWO OF THE THREE RULINGS ORDER NOTHING.** Ruling 2 rules that nothing
> is owed for the three UNPLACED members of the write-path enumeration. Ruling 3 rules that the blob's
> one home is the ruling record and no resolution form is added to the membership derivation. **Both
> are do-nothings and both are ordered as such below, so that a later reader meets a decision rather
> than an omission.** The whole executable content of this batch is Ruling 1: one clause, one
> derivation.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional, and explicitly NOT demoted. **(3)** `BUILD_AND_TEST.md` is a
> CONDITIONAL read — **and this batch MEETS the condition**, because it runs the guard set, so read it
> in full. **(4) DO NOT READ THE WHOLE OPEN-ITEMS INDEX.** Rule (a) reads the DERIVED GATING ANSWER
> narrowed to its identity list — `tools/audit/nongating_apparatus_rows.json` →
> `★_the_live_gating_answer` → `gating_ids`. The INDEX remains the AUTHORITATIVE STATUS SURFACE.
>
> **★ AND THE STANDING CLAUSE BINDS THIS ONE: read `cowork_handoff.md`, ITS CURRENT (THIRTY-SECOND)
> BLOCK.** **The findings series stands at F87 when this batch begins** — you allocated F82–F86 at the
> twelfth batch; the writing side allocated **F87** at the return sitting. **Number from F88.**
>
> **Then read, in full:** `cowork_rulings_2026_08_19_twelfth_return.md` (this batch's whole
> authority); **your own `cc_report_preparation_twelfth.md`, its §5 and §10 above all** — §5 because
> Task 2 generalizes what it built, §10 because **F84 is the ground of the clause Task 1 lands**;
> `cowork_rulings_2026_08_19_eleventh_return.md`; and **`cowork_audit_protocol.md`'s dispatch-protocol
> section IN FULL**, because **SEVEN standing clauses now stand there** and Task 1 adds an eighth —
> you must meet all seven before writing it.
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/epoch_write_path.json` (the shape Task 2 generalizes, and the establishment pattern it
> imports); `tools/audit/nongating_apparatus_rows.json` (a subject of Task 2's sort, **read and never
> written**); `tools/audit/evidence_pin_membership.json` (Task 0's subject and a subject of the sort);
> `tools/audit/guard_classification.json` and `tools/audit/guard_state.json`.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no mining, no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, and **no caller flag or
> candidacy acted on**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end.** The
> declared start state is below and is stated **at the tree you will actually meet**. **Verify every
> commit at the object by explicit hash.** **The reserved-word conventions bind all new prose. The
> new-tool rule stands. The E-ordering rule stands, at both grains — your own F79's remedy is the
> route: state at the close that the end state is NOT asserted there, and let the further commit carry
> it.** **F3's reading rule stands** (`gen_cluster_dispositions.py --verify` is the drift authority;
> `reaim_home_anchors.py --check` exits 0 while printing drifted anchors and is not the authority —
> **it fired again in your own last batch**). **Every guard run is taken at a quiet tree.**
>
> **★ THE VOCABULARY RULE OF 2026-08-17 BINDS EVERY LINE THIS BATCH WRITES** — TOWARDS the ultimate
> objective and TOWARDS the guiding principles, never *against* either.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1 — the establishment test:** *"A recognizer over a population is established only by
  both-ways reconciliation against an independently-known population. Where no such population exists,
  the recognizer's output IS the population, no seed set can establish it, and its output stands as a
  LOWER BOUND with its reach declared UNMEASURED — never as a census."* — with its ordered
  derivation: *"a read-only derivation over the recognizers the record leans on … runs regardless of
  this ruling … What is ruled is what its result authorizes: NOTHING."* — with its bound: *"The clause
  binds a recognizer written or touched from here. Existing recognizers' own artifacts are NOT
  retro-fitted with the statement."* — and its forward clause: *"A RECOGNIZER OVER A POPULATION
  STATES, AT ITS OWN ARTIFACT, WHETHER AN INDEPENDENTLY-KNOWN POPULATION EXISTS TO RECONCILE AGAINST —
  AND WHERE NONE DOES, IT PUBLISHES ITS OUTPUT AS A LOWER BOUND WITH ITS REACH DECLARED UNMEASURED."*
- **Ruling 2 — the three UNPLACED members:** *"All three stay UNPLACED, published with the shape the
  derivation can see and the reason it can see no further. No verdict is added, no recognizer is
  widened, and no member is acted on."*
- **Ruling 3 — the blob's one home:** *"The ruling record is the authority for the blob of a separated
  rendering; the membership artifact points at the record and no blob resolution form is added to the
  derivation."*

## Premise ledger

- **FACT** — the twelfth batch is complete and pushed, verified at the objects by the writing side:
  `46d68a146d` → `e3583c2720` (4) → `5f275463b0` (2) → `bd3961c975` (4) → `25cde8bc62` (5) →
  `18127bab01` (2) → `e96d80b550` (6) → `1530162032` (4) → `10921a0cb0` (1) → **`16a172715a`**, every
  parent confirmed and every path count read from the object. **The terminus is `16a172715a` and it
  carries the report alone.**
- **FACT** — `OPEN_ITEMS.md` (`6ae67d8603`) and `tools/audit/nongating_apparatus_rows.json`
  (`5bb43d0b3a`) are **byte-identical** at `9390e2ca2c` and at the terminus, and have been across
  three batches. **Nothing this batch does may move either; a difference in either at any task is a
  STOP-and-report.**
- **FACT, measured by the writing side immediately before `cowork_handoff.md` was edited** — the file
  stood **UNMODIFIED at the terminus**, reproducing the committed blob `bf31a8007e` exactly, at
  **392,730 bytes with ZERO carriage returns**. **So the F57 caveat does not arise for that file, and
  this is measured rather than assumed.**
- **FACT, established by the writing side at its own artifact and the ground of Task 2's first
  expectation** — `gen_nongating_apparatus_rows.py` reconciles **both ways against the parsed INDEX**:
  every gating identity an open row of it, every open row placed as gating or non-gating with none in
  both and none in neither, every gating row carrying a ground, and **a HALT on any row it cannot
  place.** **Task 2 DERIVES this rather than inheriting it from this dispatch.**

- **ASSUMPTION A1 — the working tree, stated by CONTENT and never by a changed-passage count.** The
  tree carries exactly **ONE tracked modification**: `cowork_handoff.md`, whose content is **two parts
  of one act** — the THIRTY-SECOND session-close block inserted, and the THIRTY-FIRST heading carrying
  its entry-point demotion marker.
  **★ ONE PATH IS NAMED AS EXPECTED-UNMODIFIED BECAUSE TASK 0 WILL MOVE IT.**
  `tools/audit/evidence_pin_membership.json` is expected **UNMODIFIED**. **If it IS modified, that is
  accounted for by Task 0 step 2 and is NOT a STOP. A modification at any OTHER tracked path is a
  STOP-and-report.**
  **Untracked and to be landed by Task 0:** `cowork_rulings_2026_08_19_twelfth_return.md` and this
  dispatch.
  **★ AND THE REST OF THE UNTRACKED POPULATION IS NAMED AS THE TREE CARRIES IT, NOT AS A CLASS — WHICH
  IS F82 APPLIED RATHER THAN REPEATED.** The tree also carries newly visible `cc_instruction_*.md`
  files, `cc_*_report.md` and `cc_*_dossier.md` files, and a scratch directory. **This batch touches
  none of them**, and A1's STOP condition is scoped to TRACKED paths.
  **Check ordered (Task 0, first act, blob against blob by explicit hash)**, under the F57 caveat.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  **Check ordered (each task's last step):** any other red is classed by its measured cause, and one
  outside this batch's own acts' subjects is a STOP-and-report. The reds this batch causes and must
  clear: **the membership check, cleared by Task 0 step 2**; **the F15 anchored-quote class**, because
  Task 1 edits `cowork_audit_protocol.md`; and **`gen_session_start_read_size.py --check` at the
  close**, because Task 3 moves `STATUS.md` — **F79's own instance, expected, cleared by regenerating
  the measurement and by nothing else.**

- **ASSUMPTION A3 — the sort's own population is enumerable from the file system.** Every `*.py` under
  `tools/` is walkable, as your own last batch's derivation established at 338 tools. **Check ordered
  (Task 2, first act):** if the walk cannot enumerate, that is a STOP-and-report and nothing is
  published.

## The declared start state — at the tree you will meet, with the RECORD of the run named beside the VERDICTS

**72 guards run, 70 passing, TWO failing, 4 not run, 16 historical, ZERO STOPs**, and *"the guard
classification re-derives"*.

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — **caused by
   this dispatch's own input**: one untracked root-level `cowork_rulings_*.md` now sits there,
   `cowork_rulings_2026_08_19_twelfth_return.md`.

**★ AND THE GUARD REGISTRY'S OWN ARTIFACT DOES NOT RE-DERIVE EITHER, WHICH IS NAMED HERE RATHER THAN
LEFT — F83 APPLIED RATHER THAN REPEATED.** `gen_guard_state.py --check` will open *"STALE vs the run"*,
because the committed registry records the twelfth batch's end state — one failing — while the tree has
two, and **the same input causes both**. **It is not a third failing check**: the registry's own
`not_a_subject_of_this_run` field records why the runner is not a subject of its own run, and its
staleness is therefore not one of the 72 verdicts. **The declared start state is MATCHED when the
verdicts match, and no STOP is owed for the registry's drift.**

**★ THE PREDICTION ABOUT THE MEMBERSHIP DIFFERENCE IS TAKEN FROM BOTH ROUTES.** **Route A** — the count
rises by one from its committed value and one record name is added. **Route B** — the writing side
probed the new ruling record and **no line carries `pinned` together with a backtick-quoted tool name,
so route B is predicted to add NOTHING and no third hunk is expected.** **Both halves are predictions
and not citations — check them at the artifact.**

**A THIRD failing check among the verdicts, or a difference in the membership artifact reaching any
member, route, document, pin constant, state or count, is a STOP-and-report.**

## Task 0 — land the four paths, and clear the membership check in the same act

1. **A1's check first, entirely at content-addressed objects**, under the F57 caveat.
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE its difference against the
   committed blob BEFORE accepting it**, against **both** halves of the prediction. **If any member,
   route, document, pin constant, state or count MOVES, that is a STOP-and-report.** An ADDED derived
   cross-reference whose cause is this batch's own ordered act is not such a movement — that is the
   sharpened bar standing at `cowork_audit_protocol.md` — but it is reported with its cause
   established at the record's own text.
3. **Commit exactly these FOUR paths and no fifth:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_19_twelfth_return.md` (new), `cc_instruction_preparation_thirteenth.md`
   (new). Staged plainly; push; verify at the object.
4. **Re-run `gen_evidence_pin_membership.py --check` at the tree this commit carries: it must PASS.**

**Registered expectation E0:** exactly 4 paths; two modifications matching A1's shape and step 2's
bounded regeneration, two additions; no staging override; the membership check passing at the
resulting tree, so the guard set stands at **72 run, 71 passing, ONE failing** from Task 0 onward.

## Task 1 — Ruling 1's forward clause, at the home the record already makes one, in ONE commit

1. **Write the clause** at `cowork_audit_protocol.md`'s dispatch-protocol section, beside the seven
   already standing there: **a recognizer over a population states, at its own artifact, whether an
   independently-known population exists to reconcile against — and where none does, it publishes its
   output as a LOWER BOUND with its reach declared UNMEASURED, never as a census.**
2. **Record `F84` as its ground and `F87` as the test it rests on**, with both general forms: *a
   recognizer's reach is measured by the instances it FAILS to find, never by the ones it does*, and
   *establishment needs a SECOND, INDEPENDENT enumeration of the same population.*
3. **★ STATE THE CLAUSE'S BOUND AT THE SITE, because a reader will otherwise take it for a sweep: it
   binds a recognizer WRITTEN OR TOUCHED FROM HERE. Existing recognizers' own artifacts are NOT
   retro-fitted** — the sort's artifact carries the per-recognizer verdict so the information has ONE
   home (**#6**), and retro-fitting a population under the standing mechanism freeze would be tool work
   that blocks nothing.
4. **The anchored-quote consequence is the F15 class and is cleared by its ruled treatment** — anchors
   re-aimed PER CITATION from the drift report's own reported start line and **never by an assumed
   uniform shift**; affected derivations regenerated; `gen_cluster_dispositions.py --verify` the drift
   authority. **Any regenerated artifact's own difference is MEASURED before it is accepted**, and
   every changed line must carry a `cowork_audit_protocol.md` coordinate and nothing else — **counted,
   not sampled.**
5. Commit, push, verify at the object.

**Registered expectation E1:** the clause stands at the dispatch-protocol section with its two named
grounds, its two general forms and its bound; no other authoring rule moves; the drift authority
reports every re-aimed anchor correct at its new coordinate.

## Task 2 — Ruling 1's ordered derivation: the recognizer sort, published as data, authorizing nothing

1. **DERIVE the population, never hand-list it.** Walk the tools as your last batch's derivation did.
   For each tool that derives a POPULATION and publishes it, place it by the ruled test: **is there an
   independently-known enumeration of that population to reconcile against, or is the tool's own output
   the population?** Publish, per member: what population it claims to describe, what (if anything)
   enumerates that population independently, whether the tool reconciles against it and in which
   directions, whether it HALTS on a member it cannot place, and the resulting verdict.
2. **★ A MEMBER MAY BE MIXED AND THE SORT MUST BE ABLE TO SAY SO.** A derivation whose POPULATION is
   externally enumerable while its CLASSIFICATION is decided by its own recognizers is established on
   the first and unestablished on the second. **`gen_evidence_pin_membership.py` is the known
   instance** — its population is every root-level `cowork_rulings_*.md`, an external file-system
   enumeration, while which route places a member is decided by recognizers over record text. **A sort
   that cannot express MIXED is a STOP-and-report**, not a verdict forced to one side.
3. **★ THE FIRST EXPECTATION, DERIVED AND NOT INHERITED.** `gen_nongating_apparatus_rows.py` is
   expected to place as ESTABLISHED, its population being the parsed INDEX and its reconciliation
   running both ways with a halt. **DERIVE that at the artifact and at the tool; do not take it from
   this dispatch or from the ruling record.** If it does not so derive, that is a **STOP-and-report**
   and it is the largest thing this batch could return — that answer is what a session reads at boot
   instead of the whole index.
4. **★ AND THE SORT DECLARES ITS OWN STATUS UNDER THE VERY TEST IT APPLIES.** Its population — the
   tools under `tools/` — is externally enumerable, so its membership is checkable; but **which tools
   it classes as recognizers-over-a-population is its own classification, and nothing outside it
   enumerates that.** So **the sort itself publishes as a LOWER BOUND with its reach declared
   UNMEASURED**, at its own artifact, in its own words. **A sort that claims to be a census of
   recognizers is a STOP-and-report.** This is the ruling applied to the act that lands it, and it is
   not optional.
5. **★ THIS TASK AUTHORIZES NOTHING AND FIXES NOTHING.** No recognizer is edited, widened or acted on;
   no artifact of any enumerated member is written. **The published sort is the standing input to a
   later ruled act, never a work list.** A task that edits an enumerated recognizer is a
   STOP-and-report.
6. **If the derivation needs a new measurement tool, the new-tool rule is discharged in the same
   commit** — its authored run-instruction in `gen_guard_state.py` and its authored classification
   verdict in `gen_guard_classification.py` — and **the guard count rises by exactly that one
   invocation, named in the close.** If a cell of the classification cannot be filled because the
   registry predates the tool, **declare it as your own F86 records** rather than leaving it silent.
7. Commit, push, verify at the object.

**Registered expectation E2:** the population is derived and every member placed with its evidence,
MIXED expressible and used where it applies; `gen_nongating_apparatus_rows.py` derived as ESTABLISHED
or STOPped on; **the sort's own status declared under its own test, as a lower bound with UNMEASURED
reach**; no recognizer edited or acted on; any new measurement tool registered in the act that created
it, with the guard count's movement caused and named.

## Task 3 — the close, and the two do-nothings recorded as decisions

1. **★ RECORD THE TWO DO-NOTHINGS AS RULED DECISIONS, in the close and in the report**, so that a
   later reader meets a decision rather than an omission:
   **(a) Ruling 2** — the three UNPLACED members of `tools/audit/epoch_write_path.json` stay UNPLACED;
   no verdict was added, no recognizer widened, no member acted on; the ground recorded is that the
   unplaced set is self-maintaining through its both-ways halt and that widening would be **F84's own
   hazard repeated**.
   **(b) Ruling 3** — no blob resolution form was added to `gen_evidence_pin_membership.py`; the ruling
   record is the authority and the artifact points at it, on the index-plus-detail shape this record
   already uses twice; **nothing is foreclosed.**
2. One `STATUS.md` pointer entry per task, and **in the same act the TWELFTH batch's entries move
   verbatim to `STATUS_ARCHIVE.md`** (`gen_status_batch_bound.py`), both directions proven. **The
   declared `Last updated: ` prefix adjustment is imported, not re-decided (#6).**
3. The FULL close is appended to `cowork_away_returns.md` as a **PREPARATION THIRTEENTH BATCH**
   section: both guard states, every SHA, every registered expectation graded MET or NOT MET with its
   reason, every declared departure, and the carried findings — **F1–F87, surfaced and none rowed; the
   row bar stands whole this batch.** The report is `cc_report_preparation_thirteenth.md`.
4. **★ THE CLOSE DOES NOT ASSERT THE END STATE.** State at its head that the end-state run is taken
   after the commit carrying it exists and that its values land in ONE FURTHER commit. **Do not write a
   row, a cell or a sentence asserting the end state into the close commit** — that is F79's remedy and
   your last batch executed it correctly; do the same.
5. **★ AND NAME THE CHAIN AS FAR AS A SENTENCE CAN NAME IT, DECLARING THE ONE COMMIT IT CANNOT** — the
   shape your last batch's §15 established, which removed a real hazard the eleventh batch left. Import
   it; do not re-invent it.
6. Commit, push, verify at the object.

**Registered expectation E3:** at the tree carrying the close, a fresh full guard run re-derives —
**every run check passing except [[OI-372]]'s tool, zero STOPs** — run and read, never inferred, its
values committed only after the run that produced them, **and no sentence asserting them written
before that run existed**; both do-nothings recorded as decisions with their grounds.

## What this batch does NOT do

- **No recognizer is edited, widened or acted on**, and no enumerated member's artifact is written.
- **No verdict is added to the write-path enumeration** and no UNPLACED member is placed.
- **No blob resolution form is added** to the membership derivation.
- **No pin is taken, restored, moved or re-taken.** The five pinned and two NOT PINNED members are
  untouched, and **no ratification document is opened for writing.**
- **No curated boot list**, no archiving pass, no re-opening of Ruling 2 of the session-start-read
  sitting.
- **No fate on any `CLAUDE.md` span**; `gen_claude_md_finer_archive.py --apply` is not run.
- **No change to rule (a)**; `tools/audit/nongating_apparatus_rows.json` is READ by Task 2 and must be
  **byte-unchanged** at every task.
- **No candidacy acted on, no census re-pin, no caller flag moved.**
- **No F71 sweep of the audit generators** — recorded and unowned.
- **No mining, no empirical findings ledger, no fact-gate admission**, no derivation, design, repair or
  pilot act.
- **No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
  `tools/robust_stop/`, no measurement of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly as found;
  **[[OI-179]] stays OPEN and GATES**; F3 stays surfaced, unfixed and unrowed.
- **`gen_gating_row_sizing.py`'s frozen population is left alone.**
