# EXTRACT — Och, "Minimum Error Rate Training in Statistical Machine Translation" — Task B candidacy row 15, first pass, AT THE OBJECT

> **STATUS: FIRST-PASS EXTRACT, READ WHOLE AT THE OBJECT (2026-09-06).** Written under
> `cowork_reading_pass_commission_2026_08_30.md` §4, whose form the remedial commission
> (`cowork_reading_pass_remedial_commission_2026_08_31.md` §3) binds unchanged.
>
> **★ CORRECTED 2026-09-16 ON THE USER'S RULING OF THAT DATE, FOR ROW 15.** The second extraction's
> cross-check (`reading_pass/extracts_second_pass/och-2003-minimum-error-rate-training-in-statistical-machine-translation.md`
> §9.3, items (a) to (d)) found four places where the paper goes against this file. The user ruled that
> they are corrected at their own sites, the former wording preserved (#12). Each site carries a
> "★ CORRECTED 2026-09-16" note. **No value, no bold cell, no finding and no verdict changed.** The
> paper was re-opened for the cross-check, all eight pages; the ground for each correction is that
> file's §9.3.
>
> **The grade.** All eight pages of the held PDF were read AT THE OBJECT: staged through the bridge
> and read with the file tools as page images. **No relay, no web-fetch read, no prompted
> extraction.** **The page count was established rather than assumed:** a request for page 20 was
> refused by the tool with *"PDF has 8 pages"*. **Pages 6 and 7 — Table 2 and Table 3, which carry
> every value this read verifies — were each read a second time on their own and every transcribed
> cell re-checked against the image.** The document prints **no page numbers**, so every location
> below is given as the paper's own section, table or figure number together with the page of the
> held file counted from the front.
>
> **★ ROW 15 IS GROUP 3's EIGHTH MEMBER** ("the scoring architecture and its fitting",
> `reading_pass/candidacy_upgrades.md` line 177: rows 45, 48, 49, 50, 52, 46, 51, 15, 12, 13, 14, 16,
> 17), after rows 45, 48, 49, 50, 52, 46 and 51.
>
> **★★ ROW 15 HAS A VERIFICATION TARGET, UNLIKE ROW 51, AND THE READ IS A VERIFICATION AGAIN.**
> `FRAMEWORK.md` DP-P (lines 802–806, LIVE) and §S6's DP16 (lines 1874–1877, the sealed first-stage
> draft) each rest a `[FACT]` on this paper, and `reading_pass/population.md` names it at **V13**.
> **V13 IS CONFIRMED AT ITS OWN CELLS, IN BOTH ITS HALVES**: Table 2's BLEU column reads **12.2** at
> the MMI row and **19.6** at the BLEU row on the development corpus, and Table 3's BLEU column reads
> **11.3** and **17.2** on the test corpus, which is V13's own parenthetical.
>
> **★★ THIS READ PRODUCES NO CORRECTED STRUCTURAL CLAIM AND NO STOP OF EITHER CLASS.** What it
> produces is that confirmation, **three precisions on how the ratified figure reads — one of which is
> the one this read most wants carried** — the paper's own two warnings about the method the candidacy
> row admits it for, and data routed below. **Row 18's finding (3), row 47's two corrected structural
> claims, row 45's finding (1) and row 52's corrected structural claim all stay open with the user,
> are applied nowhere, and are neither restated as settled nor built on here.**
>
> **★ AND ITS SUBJECT IS NOT MUSIC AT ALL** — statistical machine translation, Chinese into English,
> on news text. **Every claim carried out of it crosses a domain boundary and says so at the point of
> use.** Row 11 (Sarawagi & Cohen) is the read row of this slice that already had that shape, and its
> extract's wording is followed here: a measured gain of this paper is stated as a
> machine-translation gain **so the formalism paper is not later cited as if it measured a musical
> one**. Nothing about that is called a first.
>
> **Where the record bears on this paper, established by `Grep` of the staged tree and by READING,
> never inherited. What was searched and where, stated as a rule rather than as a tally.** Over the
> staged tree: an IDENTITY sweep — the bare words **"Och"**, **"minimum error rate"**, **"Minimum
> Error Rate"**, **"MERT"**, **"machine translation"**, **"Machine Translation"** and
> **"translation"**. Over `FRAMEWORK.md`, BEFORE the paper was read: a MECHANISM sweep for
> **"combination weight"**, **"fitted to"**, **"fitting"** and **"likelihood"**, and — because the
> record supplies them — a sweep for the four RATIFIED figures **"12.2"**, **"19.6"**, **"11.3"** and
> **"17.2"**, over `FRAMEWORK.md` and then over the staged tree. Then, AFTER the paper was read and
> from figures the record does NOT supply, a FIGURE sweep over the staged tree — **"70.7"**,
> **"69.7"**, **"76.8"**, **"68.0"**, **"68.3"**, **"13.5"**, and the terms **"BLEU"**, **"NIST"**,
> **"mWER"**, **"mPER"**, **"pseudo-reference"** and **"log-linear"** — together with the BARE WORD
> **"2003"** over `FRAMEWORK.md` and then over the staged tree. **Which sweep ran before the paper and
> which after is stated exactly, because the record's own method fact is about what a sweep run after
> reading can reach that one run in advance cannot.** **`FRAMEWORK.md`
> §4.2, the L2 charter, DP-A to DP-Q, A.3, §S4 whole, the entanglement argument and §S6's DP1
> to DP17 were READ WHOLE, not only searched**, on the method fact rows 48 and 52 paid for; **the L3
> charter was read at its OPENING ONLY (lines 426–430) and the rest of it was NOT read**, which is
> what this file's own provenance at its foot has said since it was written.
> *(★ CORRECTED 2026-09-11 on the fact-check over the latest extracts the user asked for: this sentence
> first read "§4.2, the L2 and L3 charters, DP-A to DP-Q, A.3, §S4 whole, the entanglement argument and
> §S6's DP1 to DP17 were READ WHOLE, not only searched" — an overstatement of what was read, contradicted
> by the provenance of the same file, the shape row 14's self-audit names as its defect (2). Nothing in
> this extract rests on the L3 charter, so no finding and no verdict moves.)*
>
> **★ WHAT THE THREE-PART RULE PRODUCED HERE, STATED PER LEG AND DERIVED BY NAMING.** The IDENTITY
> sweep reached **six** of the eight places below — the candidacy row, the slice derivation, the
> bibliography, `population.md` V13, the findings surface's DP-P block, and `FRAMEWORK.md` §S6's DP16,
> which names Och. **It reached NEITHER of the other two**: the findings surface's verification-table
> row, whose cell names no author, and **DP-P, the LIVE design point** — because DP-P names no author,
> no system and no year. **Both were reached by the FIGURE sweep, and DP-P also by the whole reading.**
> That is precisely the mirror-shape lesson rows 48, 49, 50 and 52 recorded, reproduced here on a
> different design point: a live design point's own text can be author-less, and only a figure sweep
> or a reading finds it. **The BARE-WORD "2003" sweep over `FRAMEWORK.md` returned NO MATCH AT ALL**,
> so the trap the progress record named for this row — that "2003" also reaches rows 1, 14 and 18,
> which are 2003 papers — did not arise in that file; over the wider staged tree "2003" occurs only in
> this line's own tracking files and the bibliography, and each was read at its own line this session.
> **The FIGURE sweep returned one false positive, recorded so nobody merges them:** the digits
> *"69.7"* occur inside `CLAUDE.md`'s chorale identifier `bwv169.7@24960` at line 1204, in the Baroque
> case-identity set of **block (C)** — the superseded retrospective, its heading at 1153 and the next at
> 1225, **checked at the heading list and NOT block (A)** — which is a stem-and-tick identifier and not
> a figure of any kind. **No figure of this paper is reported anywhere in the staged tree except at the
> record's own citations of it, which are the FIVE of the eight places below that carry a figure at
> all**: `population.md` V13, the findings surface's DP-P block, the findings surface's
> verification-table row, `FRAMEWORK.md` DP-P and `FRAMEWORK.md` §S6's DP16. **The other three places —
> the candidacy row, the slice derivation and the bibliography — name the paper and carry no figure of
> it.**
>
> **This extract derives no specification statement, amends no document, opens no code and writes no
> open-items row or decisions-register entry.**

## Where the record bears on this paper — eight places, each read at its own line

**In this line's own tracking files** — *places 1–3*:

1. **`reading_pass/candidacy_upgrades.md` line 77**, the candidacy row: *"| 15 | Och, ACL 2003,
   minimum error rate training | ✓ | **ADMITTED** | Its METHOD — fitting combination weights to the
   metric wanted — is the live candidate for DP-P, which §9 names as L2 detail. V13 is its figure;
   the admission is for the method. |"* **ADMITTED outright, not on the doubt default.**
2. **`cowork_l2_task_b_slice_derivation_2026_09_05.md` line 62**, the slice placement: *"| 15 | Och
   2003, minimum error rate training | **L2** | Fitting combination weights to the metric wanted —
   DP-P's live candidate, which §9 names as L2 detail. |"*
3. **`docs/research_papers/BIBLIOGRAPHY.md` line 29**: *"| Och, "Minimum Error Rate Training in
   Statistical Machine Translation," ACL 2003 | https://aclanthology.org/P03-1021.pdf | ✓ | CC (ACL
   Anthology) |"*

**In the reading pass's own record** — *places 4–6*:

4. **`reading_pass/population.md` line 114, verification row V13**, read at the object this session:
   the figure as the framework states it (*"Fitting combination weights by likelihood measured at
   12.2 against 19.6 on the metric actually wanted"*), cited at **DP-P (named though deferred: the
   framework's own defense cites it)**, the primary located as *"Och 2003 …, Table 2, development
   corpus, BLEU column: weights trained by the likelihood-type criterion (MMI) score 12.2 BLEU
   against 19.6 when trained on BLEU itself (test corpus: 11.3 vs 17.2). Domain bound: machine
   translation — the transfer to L2's weight fitting is analogical, which DP-P's deferred status
   already reflects"*, verdict **VERIFIED**.
5. **`cowork_reading_pass_findings_2026_08_31.md` lines 448–453**, the DP-P block, graded
   **ENRICHES; routed to measurement design**, carrying *"V13 VERIFIED at the object"* with both
   pairs and the domain bound.
6. **`cowork_reading_pass_findings_2026_08_31.md` line 617**, the verification table's V13 row,
   **VERIFIED**.

**In `FRAMEWORK.md`** — *places 7–8, and the second of these is the one no identity sweep reached*:

7. **DP-P, lines 802–806, LIVE, `[FACT]`:** *"**DP-P — How are the terms of L2's score combined and
   fitted?** **NOT DECIDED HERE — detail specification and measurement design.** Named because it is
   a real design point rather than an implementation detail: fitting combination weights by
   likelihood was measured at 12.2 against 19.6 on the metric actually wanted, when the weights were
   instead fitted to that metric. [FACT.]"* **It names no author, no system and no year, so the
   identity sweep could not reach it.**
8. **§S6's DP16, lines 1874–1877, the sealed first-stage draft, `[FACT]`:** *"**DP16 — How are the
   terms of L2's score combined and fitted?** **NOT DECIDED HERE — it is detail specification and
   measurement design.** It is named because **Och's result** makes it a real design point rather
   than an implementation detail: fitting combination weights by likelihood measured 12.2 against
   19.6 on the metric actually wanted, when the weights were instead fitted to that metric. [FACT.]"*
   **The sealed text names the author where the live text does not** — the same shape the row 51 read
   recorded for DP-O against §S6's DP15, reached here on a different pair and recorded beside it,
   merged with it nowhere.

**★ A fact about the design point itself, stated because it bounds every finding below.** **DP-P is
NOT A CHOSEN DESIGN POINT.** Its own words are *"NOT DECIDED HERE — detail specification and
measurement design"*, and `population.md`'s own bound on the verification table says so in terms:
*"DP-P is not a chosen point — its figure is included because the framework's §9 carries it as a
`[FACT]` defense."* **So no chosen design point's ground rests on this paper**, and the remedial
commission §5's STOP clause — a CORRECTED verdict on a claim the findings surface used to reach a
SUPPORTS or rival-defused verdict — has no subject here even in principle; the findings surface
grades DP-P **ENRICHES**.

## The verification target, ANSWERED — V13 CONFIRMED at its own cells, in both halves

**Table 2 (page 6 of the held file), *"Effect of different error criteria in training on the
development corpus"*.** Every cell below was read at the image twice. Columns are: error criterion
used in training | mWER [%] | mPER [%] | BLEU [%] | NIST | # words. **Bold marks the best result in
a column; italic marks a result whose difference to the best is NOT statistically significant** —
the caption says so in terms. **★ THE EMPHASIS BELOW IS THE PAPER'S OWN AND NOTHING IS EMPHASISED BY
THIS READER**, on the standing rule that a claim about a table's emphasis is made only where the
emphasis was read as carefully as the values. **Note in particular that NEITHER of the two cells the
record rests on is the bold one in its column on its own side of the comparison: 12.2 and 11.3 are
PLAIN, and the bold BLEU cells are 19.6 and 17.2.**

| error criterion used in training | mWER [%] | mPER [%] | BLEU [%] | NIST | # words |
|---|---|---|---|---|---|
| confidence intervals | +/- 2.4 | +/- 1.8 | +/- 1.2 | +/- 0.2 | – |
| MMI | 70.7 | 55.3 | 12.2 | 5.12 | 10382 |
| mWER | **69.7** | *52.9* | 15.4 | 5.93 | 10914 |
| smoothed-mWER | *69.8* | *53.0* | 15.2 | 5.93 | 10925 |
| mPER | *71.9* | **51.6** | 17.2 | 6.61 | 11671 |
| smoothed-mPER | *71.8* | *51.8* | 17.0 | 6.56 | 11625 |
| BLEU | 76.8 | 54.6 | **19.6** | *6.93* | 13325 |
| NIST | 73.8 | *52.8* | 18.9 | **7.08** | 12722 |

**Table 3 (page 7), *"Effect of different error criteria used in training on the test corpus"*.**

| error criterion used in training | mWER [%] | mPER [%] | BLEU [%] | NIST | # words |
|---|---|---|---|---|---|
| confidence intervals | +/- 2.7 | +/- 1.9 | +/- 0.8 | +/- 0.12 | – |
| MMI | **68.0** | *51.0* | 11.3 | 5.76 | 21933 |
| mWER | *68.3* | *50.2* | 13.5 | 6.28 | 22914 |
| smoothed-mWER | *68.2* | *50.2* | 13.2 | 6.27 | 22902 |
| mPER | *70.2* | *49.8* | 15.2 | *6.71* | 24399 |
| smoothed-mPER | *70.0* | **49.7** | 15.2 | *6.69* | 24198 |
| BLEU | 76.1 | 53.2 | **17.2** | 6.66 | 28002 |
| NIST | 73.3 | *51.5* | *16.4* | **6.80** | 26602 |

*(★ FACT-CHECKED 2026-09-11 at the page images, every cell of both tables and every emphasis mark:
all values and all bold cells hold. ONE emphasis mark is left UNRESOLVED rather than decided: Table
3's mPER row, mWER column, **70.2**, is transcribed above as plain; at the resolution the file tools
render the page, the fact-checking session read it as italic, like the 68.3 and 68.2 above it and the
70.0 below it. The cell bears on no finding — finding (3)'s mWER-column verdict rests on the bold 68.0
alone — and it is put to the user as a cell whose emphasis this record does not establish either way.)*

*(★ CORRECTED 2026-09-16 on the user's ruling, from the second extraction's cross-check, §9.3(a) and (b).
FORMER WORDING OF THE TWO ROWS, PRESERVED (#12): "| mPER | 70.2 | \*49.8\* | 15.2 | \*6.71\* | 24399 |" and
"| smoothed-mPER | \*70.0\* | \*\*49.7\*\* | 15.2 | 6.69 | 24198 |". **70.2** and **6.69** now read italic:
the second extraction read both italic, and the cross-check re-opened page 7 and read both italic. **Both
marks rest on the tool's rendering of the page and are not established beyond it.** The fact-check note
above stands as written; its implication that every mark but 70.2 held is not true of 6.69. Neither
cell bears on any finding in this file.)*

**THE VERDICT, per limb of what the progress record's carried item (a) asked to be verified:**

- **Are the two values 12.2 and 19.6 at the paper's own cells?** **YES** — Table 2, BLEU column, MMI
  row and BLEU row. `[FACT — read at the object, page 6, twice.]`
- **Is the comparison the one the sentence describes — likelihood-fitted weights against
  metric-fitted weights, on the metric wanted?** **YES in its structure.** The MMI criterion is the
  likelihood-type criterion (§2, Eq. 4, the maximum-mutual-information criterion derived from the
  maximum-entropy principle, which the paper calls *"a standard criterion for log-linear models"*);
  the BLEU row is the arm whose training criterion is BLEU itself; the column is BLEU. **A precision
  travels with the YES and is finding (2) below.** `[FACT.]`
- **Is the metric the one the sentence implies?** **YES** — the column is BLEU, and BLEU is one of
  the paper's four automatic evaluation criteria (§3). `[FACT.]`

**Nothing is owed to V13 as a verification.** `population.md`'s row states the table, the corpus, the
column, both pairs and the domain bound, and every part of it is exact at the object.

## Finding (1) — ★ THE PRECISION THIS READ MOST WANTS CARRIED: the pair the framework quotes is measured ON THE CORPUS THE WEIGHTS WERE FITTED ON, and the held-out pair is smaller

**At the object.** Table 2's caption says *"on the development corpus"* and Table 3's says *"on the
test corpus"*. §7 states what the development corpus is for: *"The basic feature functions were
trained using the training corpus. The development corpus was used to optimize the parameters of the
log-linear model. Translation results are reported on the test corpus."* **So Table 2 is the corpus
the combination weights were fitted on, and Table 3 is the held-out corpus.**

**The record's two `[FACT]` sentences quote Table 2's pair — 12.2 against 19.6 — and neither says
which corpus it is.** The held-out pair is **11.3 against 17.2**.

**Derived here, with the sign convention stated (metric-fitted minus likelihood-fitted, BLEU
points):** the gap is **+7.4 on the fitted-on corpus** and **+5.9 on the held-out corpus**. **The
direction is the same and the magnitude is about a fifth smaller held out.**

**★ GRADED A PRECISION AND NOT A CORRECTED STRUCTURAL CLAIM, WITH THE REASONING STATED SO THE USER
CAN OVERRULE IT.** Nothing the record says is false at the object: the values are at the cells, the
comparison is the one described, and the direction the design point needs survives on held-out data.
**The ground on which the other reading could be argued, stated in full so the choice is his:** this
project's own principle **#20** says in terms that *"the headline claim is the held-out figure"* and
that *"a fitted-and-self-measured number is not established (#19)"*, and the framework's own headline
for DP-P is the fitted-on pair. **`population.md` V13 states the corpus correctly and gives the test
pair; the incompleteness, if it is one, is in `FRAMEWORK.md`'s two sentences and not in the pass's
verification record** — which is row 52's shape exactly, and is recorded beside row 52's finding and
**merged with it nowhere**. **It moves no verdict and no value.**

## Finding (2) — the low number's arm carries a confound the authors state twice in their own words, and they say in terms that it could be better

**At the object, §6 (page 5):** *"A major problem in applying the MMI criterion is the fact that the
reference translations need to be part of the provided n-best list. Quite often, none of the given
reference translations is part of the n-best list because the search algorithm performs pruning …
To solve this problem, we define for the MMI training new pseudo-references by selecting from the
n-best list all the sentences which have a minimal number of word errors with respect to any of the
true references. Note that due to this selection approach, the results of the MMI criterion might
be biased toward the mWER criterion. It is a major advantage of the minimum error rate training
that it is not necessary to choose pseudo-references."* `[FACT — quoted at the object.]`

**And §7 (page 6) says it a second time, with the authors' own expectation attached:** *"The MMI
criterion produces significantly worse results on all error rates besides mWER. Note that, due to the
re-definition of the notion of reference translation by using minimum edit distance, the results of
the MMI criterion are biased toward mWER. It can be expected that by using a suitably defined
n-gram precision to define the pseudo-references for MMI instead of using edit distance, it is
possible to obtain better BLEU or NIST scores."* `[FACT — quoted at the object.]`

**And the Conclusions carry the qualification inside the sentence that states the result** (§9, page
7): *"We showed that this approach obtains significantly better results than using the MMI training
criterion (with our method to define pseudo-references) …"* `[FACT — quoted at the object,
the parenthesis the author's own.]`

*(★ CORRECTED 2026-09-16 on the user's ruling, from the second extraction's cross-check, §9.3(d): the
three quotations above printed words in bold that the page prints plain, with no mark that the emphasis
was added. The bold is removed; no quoted word changed. FORMER EMPHASIS, PRESERVED (#12): bold on
"Note that due to this selection approach, the results of the MMI criterion might be biased toward the
mWER criterion." in the §6 quotation; on "It can be expected that by using a suitably defined n-gram
precision to define the pseudo-references for MMI instead of using edit distance, it is possible to
obtain better BLEU or NIST scores." in the §7 quotation; and on "(with our method to define
pseudo-references)" in the §9 quotation.)*

**What this means for the record's ground, stated exactly.** The record's sentence uses the size of
the gap as the reason DP-P is *"a real design point rather than an implementation detail"*. **At the
object the size of that gap is, by the authors' own statement, partly a property of how the
likelihood arm's references had to be constructed, and they expect a differently-constructed
pseudo-reference set to narrow it on BLEU.** **The comparison is still likelihood-fitted against
metric-fitted, and the direction is still the paper's own conclusion** — what is absent from the
record is the bound the authors put on the magnitude.

**★ GRADED A PRECISION ON LIVE RECORDED GROUND AND NOT A CORRECTED STRUCTURAL CLAIM, WITH THE
REASONING STATED SO THE USER CAN OVERRULE IT.** The record's sentence is TRUE at the object in every
part a reader could check: the criterion is a likelihood-type criterion, the values are the cells,
the comparison is the one described. **The ground for the other reading:** the sentence's *work* in
DP-P is done by the magnitude, and the magnitude is bounded by the paper itself. **It is recorded
beside finding (1) and beside row 45's finding (1) and row 52's finding, and merged with none of
them.** **Applied nowhere; no verdict proposed.**

## Finding (3) — ★ the paper's own general rule does not hold on one column of the held-out table, and it fails in favour of the likelihood criterion

**The rule, at the object (§7, page 6):** *"We observe that if we choose a certain error criterion in
training, we obtain in most cases the best results using the same criterion as the evaluation metric
on the test data. The differences can be quite large: If we optimize with respect to word error rate,
the results are mWER=68.3%, which is better than if we optimize with respect to BLEU or NIST and the
difference is statistically significant."* `[FACT — quoted at the object.]` **Note the author's own
words *"in most cases"*, and note that the mWER sentence compares against BLEU and NIST and not
against MMI.**

**Derived here from Tables 2 and 3, with the comparison stated: for each column, is the best-marked
cell the arm trained on that column's own criterion?**

- **On the fitted-on corpus (Table 2) the diagonal is perfect** — mWER 69.7, mPER 51.6, BLEU 19.6 and
  NIST 7.08 are each the bold cell of their own column and each belongs to the arm trained on that
  criterion.
- **On the held-out corpus (Table 3) it holds on two columns outright** — BLEU 17.2 and NIST 6.80.
- **On mPER it holds at the criterion but not at the arm**: the bold cell is **smoothed-mPER 49.7**,
  and the mPER-trained arm reads 49.8 (italic).
- **★ On mWER it FAILS, and the bold cell is the LIKELIHOOD arm** — **MMI 68.0**, ahead of the
  mWER-trained arm's *68.3*, which is italic and so not significantly different from the best.

**What it is and is not.** It is **not** a contradiction of the paper: the author's rule says *"in
most cases"*, and his one large-difference example is carefully scoped to BLEU and NIST. It **is** a
bound on what DP-P's `[FACT]` can be cited for — **on the held-out table the metric-matched arm is
the best on two of four columns, matched-at-the-criterion on a third, and beaten by the likelihood
arm on the fourth.** **Recorded as a bound, not as a result**, and routed to L2's detail
specification and to measurement design. `[FACT — derived at the tables, the comparison stated.]`

## Finding (4) — ★ the paper's own warning about the method the candidacy row admits it for, which the record does not carry

**At the object, §9 (page 8):** *"Of course, the approach presented here places a high demand on the
fidelity of the measure being optimized. It might happen that by directly optimizing an error
measure in the way described above, weaknesses in the measure might be exploited that could yield
better scores without improved translation quality. Hence, this approach poses new challenges for
developers of automatic evaluation criteria."* `[FACT — quoted at the object.]`
*(★ CORRECTED 2026-09-16 on the user's ruling, §9.3(d) of the second extraction: bold removed from inside
the quotation, the page printing it plain; no quoted word changed. FORMER EMPHASIS, PRESERVED (#12): bold
on "It might happen that by directly optimizing an error measure in the way described above, weaknesses
in the measure might be exploited that could yield better scores without improved translation
quality.")*

**Why it is worth carrying to L2's detail specification.** The method row 15 is admitted for is
*fitting the combination weights to the metric wanted*, and the metric this project would fit them to
is the block-(A) robust unit — the class-(b) root-disagree duration. **The paper's own sentence is
that fitting to a measure puts that measure under pressure to be exploited.** This project already
carries the same argument twice on its own side and reached it independently: the **abstain-aware
convention** exists because *"an agreement percentage is abstention-reducible — without the convention
a change that made the system decline more often would raise the percentage without analysing anything
better"*, and **D-606** rules that a metric *"gameable by the change under test … cannot be that
change's own bar"*. **So the paper the record cites FOR the method also states the risk our own
conventions exist against, and the record's DP-P sentence carries the result without the warning.**
**Routed to L2's detail specification, to measurement design, and to the findings surface's DP-P
block. Applied nowhere; no verdict proposed.**

## Finding (5) — the method is established only for a SMALL weight vector, and the authors name the capacity question as open

**At the object.** §6 (page 5): the log-linear model includes **M = 8 different features**. §7 (page
6): *"We observe that the smoothed error count gives almost identical results to the unsmoothed error
count. This might be due to the fact that the number of parameters trained is small and no serious
overfitting occurs using the unsmoothed error count."* §9 (page 7), the first of the two questions the
author says *"should be answered in the future"*: *"How many parameters can be reliably estimated
using unsmoothed minimum error rate criteria using a given development corpus size? We expect that
directly optimizing error rate for many more parameters would lead to serious overfitting problems. Is
it possible to optimize more parameters using the smoothed error rate criterion?"* `[FACT — all three
quoted at the object.]`
*(★ CORRECTED 2026-09-16 on the user's ruling, §9.3(d) of the second extraction: bold removed from inside
the two quotations, the page printing them plain; no quoted word changed. FORMER EMPHASIS, PRESERVED
(#12): bold on "the number of parameters trained is small" and on "How many parameters can be reliably
estimated using unsmoothed minimum error rate criteria using a given development corpus size?")*

**Why it bears.** This is a published precedent for exactly the shape **D-525** already fixes — *"only
a small vector of combination weights is fit discriminatively"* — and it names the quantity **D-271**
answers for us, the capacity budget declared against corpus size. **The development corpus here is 640
sentences (Table 1, page 6) against 8 features.** **Routed to L2's detail specification beside D-525
and D-271, and to measurement design. Applied nowhere.**

## Finding (6) — what the method trades away: a unique global optimum and a gradient

**At the object.** §2 (page 2), of the likelihood criterion: *"The optimization problem under this
criterion has very nice properties: there is one unique global optimum, and there are algorithms (e.g.
gradient descent) that are guaranteed to converge to the global optimum. Yet, the ultimate goal is to
obtain good translation quality on unseen test data … there is no reason to assume that an optimization
of the model parameters using Eq. 4 yields parameters that are optimal with respect to translation
quality."* §4 (page 3), of the error-count criterion: *"It includes an argmax operation (Eq. 6).
Therefore, it is not possible to compute a gradient and we cannot use gradient descent methods to
perform optimization. The objective function has many different local optima. The optimization
algorithm must handle this."* `[FACT — both quoted at the object.]`

**The trade, stated plainly — and the word *convexity* is THIS READER'S, not the paper's, which says
only that the likelihood criterion has one unique global optimum:** a unique optimum and gradient
methods are given up; alignment between what is fitted and what is measured is gained. **A detail specification adopting DP-P's live
candidate adopts that trade, and the record does not state it.** Routed to L2's detail specification.

## Finding (7) — the algorithm itself, which is what could be ADOPTED

**At the object, §5 (pages 3–5).** Along a line in parameter space, $\lambda_1^M + \gamma \cdot d_1^M$,
each candidate translation's score is a line in $\gamma$, so the minimum over candidates,
$f(\gamma;\mathbf{f}) = \min_{\mathbf{e}\in\mathbf{C}}\{t(\mathbf{e},\mathbf{f}) + \gamma\cdot
m(\mathbf{e},\mathbf{f})\}$, **is piecewise linear** (the paper credits Papineni 1999). The exhaustive
representation of that function is computed per sentence as an ordered sequence of interval boundaries
with the change in error count at each; merging the sequences over the corpus gives the complete set of
boundaries, and **the optimal $\gamma$ is found by traversing them while updating an error count**.
*"The new algorithm is much faster and more stable than the grid-based line optimization method."* It
extends to BLEU and NIST *"by accumulating the relevant statistics for computing these scores (n-gram
precision, translation length and reference length)"*. `[FACT — read at the object.]`

**What an L2 detail specification could take.** Our own combination weights are a small vector over
factor scores and each candidate reading's score is linear in them, so the same construction applies
directly; and our own hard stop is a corpus-level accumulated count (a duration), which is the shape
the BLEU/NIST extension handles. **This is the concrete content of the candidacy row's *"its METHOD …
is the live candidate for DP-P"*, and it is stated here so a later specification cites the mechanism and
not the headline figure.** **Domain: machine translation. Nothing here was measured on music.**

## Finding (8) — the candidate list is fitted against, and the paper's own remedy is an outer loop

**At the object, §6 (page 5):** *"Using an n-best approximation, we might face the problem that the
parameters trained are good for the list of n translations used, but yield worse translation results if
these parameters are used in the dynamic programming search. Hence, it is possible that our new search
produces translations with more errors on the training corpus."* The remedy: search with a manually
defined parameter set and compute an n-best list; train on it; **search again with the new parameters,
compute a new n-best list and combine it with the existing one**; retrain; *"This is iterated until the
resulting n-best list does not change."* *"In our experiments, we compute in every iteration about 200
alternative translations. In practice, the algorithm converges after about five to seven iterations. As
a result, error rate cannot increase on the training corpus."* `[FACT — read at the object.]`

**Why it bears on L2.** The L2 charter fixes that **the rule admitting a candidate to the search is part
of this layer's specification and carries its own defense**, because *"a reading the search cannot reach
cannot be found however good the scoring is"*. **This paper is a worked instance of the same dependency
running the other way — the weights are fitted against a candidate set that the weights themselves then
change — with a published remedy and a stated convergence behaviour.** Routed to L2's detail
specification beside the charter's candidate-admission boundary condition and beside **D-098** (the
exact-decode reserve). Applied nowhere.

## Finding (9) — ★ this paper PRINTS UNCERTAINTY AND MARKS SIGNIFICANCE, and the ratified pair is far outside both

**At the object.** Both table captions state the convention: *"Italic numbers refer to results for which
the difference to the best result (indicated in bold) is not statistically significant."* And the text
under Table 1 (page 6): *"For all error rates, we show the maximal occurring 95% confidence interval in
any of the experiments for that column. The confidence intervals are computed using bootstrap resampling
(Press et al., 2002)."* `[FACT — read at the object.]`

**Derived here.** The BLEU column's interval is **±1.2** on the development corpus and **±0.8** on the
test corpus. **The gap the record rests on is 7.4 points on the development corpus and 5.9 on the test
corpus** — several times either interval in both cases. **So the ratified `[FACT]` is safe on the
uncertainty axis, and that is derived rather than assumed (#24).**

**Recorded beside the progress record's item (h) list of this slice's reporting practices, with the rows
named and no comparison asserted:** rows 50, 52 and 46 print no uncertainty anywhere; row 49 prints none
in the table the record rests on; row 48 prints a spread on one block of one table and no significance
test; row 45 prints no spread on any cell but a p-value on each comparison it concludes from; row 51
prints none anywhere on its one table and no significance test. **Row 47's practice is worked in its own
2021 extract and is not restated here.** **This paper prints a confidence interval per column on both
tables AND marks per-cell significance.**

## Identity — title and author match; NO venue, year, copyright, licence or pagination is printed anywhere

**Checked at page 1 before anything was extracted**, against the bibliography's row 29.

- **Title, printed:** *"Minimum Error Rate Training in Statistical Machine Translation"* — **exactly
  the bibliography's title.**
- **Author, printed:** *"Franz Josef Och"*, with the affiliation *"Information Sciences Institute /
  University of Southern California / 4676 Admiralty Way, Suite 1001 / Marina del Rey, CA 90292"* and
  the e-mail *"och@isi.edu"*. **The bibliography names "Och"; the printed given names are its
  expansion.**
- **Venue, year, copyright, licence and page numbers: NOT PRINTED**, on page 1 or anywhere in the eight
  pages. The bibliography's row names **ACL 2003** and tiers it **CC (ACL Anthology)** on the strength of
  its `aclanthology.org` URL. **So whether the held file is the ACL 2003 proceedings text as published is
  not establishable from what is held**, and the CC tier rests on the URL rather than on a printed licence
  line.
- **This is the milder identity shape rows 29, 11 and 18 produced** — the title and author match and the
  venue axis cannot be checked. **The rows are named and no count is asserted.** Routed to the
  bibliography reconciliation, which this extract does not perform.
- **Internal corroboration, recorded as corroboration and not as a venue check:** the Acknowledgements
  (page 8) name *"DARPA-ITO grant 66001-00-1-9814"*, and §7's task is *"the 2002 TIDES Chinese–English
  small data track task"*, both consistent with a 2003 publication; the reference list's latest entries
  are 2002. **Nothing is concluded from that beyond consistency.**

## Claims, labeled

### The problem the paper states

- `[FACT]` §1 (page 1): *"there is a mismatch between the basic assumptions of the used statistical
  approach and the final evaluation criterion used to measure success in a task."* The training is
  usually maximum likelihood or a related criterion; the evaluation is an error measure.
- `[FACT]` §2 (page 2): the model is **log-linear** — $\Pr(\mathbf{e}|\mathbf{f})$ modelled as a
  normalised exponential of $\sum_{m=1}^{M}\lambda_m h_m(\mathbf{e},\mathbf{f})$, with $M$ feature
  functions and one parameter each. **The modeling problem is developing feature functions; the
  training problem is obtaining parameter values.**
- `[FACT]` §2: the standard criterion is **MMI (maximum mutual information)**, Eq. 4 —
  $\hat\lambda_1^M = \arg\max \sum_{s=1}^{S} \log p_{\lambda_1^M}(\mathbf{e}_s|\mathbf{f}_s)$ — derived
  from the maximum-entropy principle. **This is the "likelihood" arm the record's sentence names.**

### The criteria the paper introduces

- `[FACT]` §4 (page 3), Eq. 5: the **unsmoothed error count** —
  $\hat\lambda_1^M = \arg\min \sum_s E(\mathbf{r}_s, \hat{\mathbf{e}}(\mathbf{f}_s;\lambda_1^M))$, over
  a set of $K$ candidate translations per input sentence.
- `[FACT]` §4, Eq. 7: the **smoothed error count**, with a smoothing parameter $\alpha$; as
  $\alpha\to\infty$ it converges to the unsmoothed criterion except at ties. *"the resulting objective
  function might still have local optima, which makes the optimization hard compared to using the
  objective function of Eq. 4 which does not have different local optima."*
- `[FACT]` Figure 1 (page 4): the actual shape of the error count and the smoothed error count for two
  model parameters, computed on the development corpus with 1,600 alternatives per source sentence and
  $\alpha = 3$; *"the unsmoothed error count has many different local optima and is very unstable. The
  smoothed error count is much more stable and has fewer local optima."* **No boundary value or axis tick
  is transcribed from that figure.**
- `[FACT]` §7 (page 6): in the event, *"the smoothed error count gives almost identical results to the
  unsmoothed error count"* — see finding (5) for the author's stated reason.

### The four evaluation criteria (§3, pages 2–3)

`[FACT]` **mWER** (multi-reference word error rate: edit distance to the closest reference);
**mPER** (multi-reference position-independent error rate: the same ignoring word order); **BLEU**
(geometric mean of n-gram precisions times a brevity penalty, $N=4$, citing Papineni et al. 2001);
**NIST** (a weighted n-gram precision times a brevity factor, $N=4$, citing Doddington 2002). *"Both,
NIST and BLEU are accuracy measures, and thus larger values reflect better translation quality. Note
that NIST and BLEU scores are not additive for different sentences, i.e. the score for a document
cannot be obtained by simply summing over scores for individual sentences."*

### The system and the data (§6, §7, Table 1)

`[FACT]` The baseline is the **alignment template approach** (Och & Ney 2002), with a phrase penalty and
special alignment features added: **M = 8 features**. Search is a dynamic-programming beam search, with
n-best candidates extracted by A\* search. `[FACT]` The task is the **2002 TIDES Chinese–English small
data track**, news text; the system has no rule-based components for numbers, dates or names.
`[FACT]` Table 1 (page 6): **Train 5,109 sentences** (89,121 Chinese / 111,251 English words; 3,419 /
4,130 singletons; vocabulary 8,088 / 8,807); **manual lexicon 82,103 entries**; **Dev 640 sentences**
(11,746 / 13,573 words); **Test 878 sentences** (24,323 / 26,489 words).

### What the paper concludes (§9, page 7)

`[FACT]` *"We presented alternative training criteria for log-linear statistical machine translation
models which are directly related to translation quality: an unsmoothed error count and a smoothed error
count on a development corpus. For the unsmoothed error count, we presented a new line optimization
algorithm which can efficiently find the optimal solution along a line. We showed that this approach
obtains significantly better results than using the MMI training criterion (with our method to define
pseudo-references) and that optimizing error rate as part of the training criterion helps to obtain
better error rate on unseen test data."*

`[CONJECTURE — the author's, labelled as his]` §9 (page 7, the Conclusions continuing into the right
column — located at the heading and not at where it was read): *"we expect that actual 'true' translation
quality is improved, as previous work has shown that for some evaluation criteria there is a correlation
with human subjective evaluation of fluency and adequacy … However, the different evaluation criteria
yield quite different results on our Chinese–English translation task and therefore we expect that not
all of them correlate equally well to human translation quality."*

`[CONJECTURE — this reader's, labelled as such and not the paper's]` The paper's argument is
structurally the same as this project's own reason for keeping the robust unit as the fitting objective
(gate block (A), *"the Stage-5 fitting-objective basis"*): fit to the thing that is graded. **Nothing is
carried from that resemblance; it is recorded as a resemblance and no verdict is proposed.**

## Coupling facts (mandatory)

- **What the method ASSUMES about its upstream.** A **log-linear model with a fixed, small set of
  feature functions** whose values are computable per candidate; a **decoder able to produce a candidate
  list (n-best) per input**; a **development corpus with references**; and an **error function E(r,e)
  computable on a candidate against references and summable over sentences** — with the qualification the
  paper states for BLEU and NIST, that they are not additive over sentences, so the algorithm accumulates
  their sufficient statistics instead. **It assumes nothing about music, notation, harmony or time.**
- **What it HANDS downstream.** A fitted parameter vector $\lambda_1^M$ and nothing else. **No
  distribution, no confidence, no rivals, no normaliser** — the criterion optimises a ranking, not a
  probability. **What training on the error count does to the model's reading as a probability is not
  discussed in the eight pages as read.**
  *(★ CORRECTED 2026-09-16 on the user's ruling, §9.3(c) of the second extraction. FORMER WORDING,
  PRESERVED (#12): "and the paper says in terms that the resulting model is no longer a
  maximum-likelihood fit." — met nowhere in the eight pages at the cross-check's opening. That the
  criterion optimises a ranking is this reader's reading, not a sentence of the paper.)*
- **Its own STATED SCOPE and limits.** Small parameter counts (§7, §9); a candidate list that must be
  regrown as the parameters change (§6); an objective with many local optima and no gradient (§4); the
  MMI comparison arm's pseudo-reference confound (§6, §7); the fidelity demand on the measure being
  optimised (§9); and one language pair on one small-data task.
- **Where it sits against L2's charter.** **It is not a method for any question in L2's charter
  sentence.** It decides no tonality, no boundary, no chord-tone assignment and no chord. **It is a
  method for what the charter expressly leaves to the detail specification** — *"how the score over
  candidate readings is formed, what its terms are, what tables they read, and how any weight is
  fitted"* (`FRAMEWORK.md` §5, L2, lines 406–408). **That is the ground on which the candidacy row
  admits it and the slice derivation places it, and this read confirms the placement at the object.**

## What an L2 detail specification could adopt, adapt, or must argue against

1. **ADOPT — the criterion.** Fit the combination weights to the metric that grades the analysis rather
   than by conditional likelihood. **This is DP-P's live candidate and the paper is its primary.** The
   domain crossing is stated at every use: the measured gain is a machine-translation gain.
2. **ADOPT — the algorithm.** Exact line optimization over the piecewise-linear envelope (finding (7)),
   which applies unchanged to a score linear in the weights and to a corpus-level accumulated metric.
3. **ADAPT — the outer loop.** Regrow the candidate set after each fit and iterate to a fixed point
   (finding (8)), which is the published remedy for fitting against a candidate list the weights
   themselves move.
4. **MUST ARGUE AGAINST — the costs.** The loss of a unique global optimum and of a gradient (finding
   (6)); the capacity bound (finding (5)); and the paper's own warning that the optimised measure comes
   under pressure to be exploited (finding (4)), which lands directly on the robust unit.
5. **NOT AVAILABLE FROM THIS PAPER.** Any statement about music; any statement about how many
   combination weights our own decode should carry; any held-out protocol beyond the one it used; and any
   comparison between fitting regimes on a musical corpus. **Rows 10, 11, 19, 5, 26, 30, 45, 48 and 51
   are where this slice's own fitting-regime evidence sits, each at its own extract** (the progress
   record's carried item (e) names them), **and what is taken from any of them is cited to that extract
   and never to a summary of it.**

## The cross-primary check — NOTHING TO RUN ON, established rather than assumed

**Row 51's read had the same outcome and this one reaches it by the same route.** The paper's corpora are
a Chinese–English news translation task; its metrics are mWER, mPER, BLEU and NIST. **A sweep of the
staged tree for its figures and for those metric names returned no read row reporting on any of them**
(the FIGURE sweep named in the banner). **So no figure of this paper is comparable with any figure this
slice already holds, and none is merged.**

## The reference list, read against the population's own rows

**On the method warning row 48's read paid for and rows 49, 50, 52, 46 and 51 applied**, the reference
list (page 8) was read against `docs/research_papers/BIBLIOGRAPHY.md` and the
`reading_pass/extracts/` listing, both read at their own objects this session. **Seventeen works are
cited — derived at the page, six entries in the left column and eleven in the right: Bangalore, Rambox
& Whittaker 2000; Doddington 2002; Duda & Hart 1973; Goodman 1996; Juang, Chou
& Lee 1995; Kumar & Byrne 2002; Nießen, Och, Leusch & Ney 2000; Och & Ney 2002; Och, Tillmann & Ney
1999; Paciorek & Rosenfeld 2000; Papineni, Roukos & Ward 1997; Papineni, Roukos, Ward & Zhu 2001 (BLEU);
Papineni 1999; Press, Teukolsky, Vetterling & Flannery 2002; Schlüter & Ney 2001; Tillmann, Vogel, Ney,
Zubiaga & Sawaf 1997; Ueffing, Och & Ney 2002** *(the last spelled "Uffing" in this file's first
writing; the paper prints "Nicola Ueffing" — corrected 2026-09-11 at the page on the user's
fact-check)*.** **NOT ONE of them is in this project's bibliography and
NOT ONE has an extract** — they are machine-translation, speech-recognition and numerical-methods works
with no member in the candidacy population. **Nothing is carried out of any of them, and none is
fetched.** **This is the opposite outcome to rows 46, 49, 50, 51 and 52, whose reference lists each named
members of the population; the rows are named and nothing is inferred from the difference.**

## Centrality — CENTRAL, and the ground on which NOT CENTRAL could be argued is stated in full

**CENTRAL.** Two grounds. **(i)** A ratified `[FACT]` rests on this paper at two places in
`FRAMEWORK.md` — DP-P's live text and §S6's DP16 — and `population.md` carries it at V13; this read
verifies it at the paper's own cells. **(ii)** Its METHOD is what the candidacy row admits it for and
what the slice derivation places in L2: the fitting regime for the combination weights, which the L2
charter expressly leaves to the detail specification, and which a specification would either adopt (the
criterion and the algorithm) or have to argue against (the capacity bound, the lost convexity, the
metric-exploitation risk). **By the commission's own test — *"any paper whose claims would carry load in
a detail specification or against a design point"* — both halves are satisfied. A SECOND INDEPENDENT
EXTRACTION IS OWED.**

**The ground on which NOT CENTRAL could be argued, stated so the verdict is challengeable here:**
**DP-P is NOT A CHOSEN DESIGN POINT** — its own words are *"NOT DECIDED HERE"* — so no chosen point's
recorded ground rests on the paper, which is the property rows 45, 48, 49, 50 and 52 each had and this
row does not; the figure was already verified by the pass and this read confirms rather than corrects
it; **there is nothing musical in the paper at all**, so everything carried out of it crosses a domain
boundary; and what would be adopted is a general optimisation technique rather than anything about
harmony, which is the argument row 11's extract records for its own formalism paper. **The user may
read either way; the two grounds and their counter-ground are all stated here rather than in a
summary.**

## What this extract does NOT do

It amends no document — not `FRAMEWORK.md`, not `reading_pass/population.md`, not
`cowork_reading_pass_findings_2026_08_31.md`, not `reading_pass/candidacy_upgrades.md`, not the slice
derivation and not the bibliography. It derives no specification statement, opens no code, writes no
open-items row and no decisions-register entry, and takes no ruling. **It proposes no change to DP-P,
to V13 or to any verdict.** **Findings (1) to (9) are all routed, all put to the user and all applied
nowhere** — the earlier statement naming only (1) to (4) was narrower than this document carries and is
corrected here. **Row 18's
finding (3), row 47's two corrected structural claims, row 45's finding (1) and row 52's corrected
structural claim stay open with the user, untouched, neither restated as settled nor built on.** The
ACL 2003 proceedings text at the URL the bibliography names, and every work this paper cites, were not
fetched and are not read; only the held file was.

---

*Provenance: Cowork, 2026-09-06, the nineteenth reading session of L2's slice, after the ordinary
session-start read (`DECISIONS.md` whole, `STATUS.md` whole, and the derived gating
answer's `★_the_live_gating_answer` block with its `gating_ids` list whole; **`CLAUDE.md` was NOT
re-read as a session act — it was met as the harness-delivered project-instructions copy and verified
complete at the staged object, 1,864 lines with its closing self-check section, which is the departure
declared in the hundred-and-thirty-fifth handoff entry**), the
hundred-and-thirty-fourth handoff entry whole, the hundred-and-eighteenth entry whole for its
bridge-fault section, `reading_pass/l2_slice_reading_progress.md` whole, both commissions whole,
`reading_pass/candidacy_upgrades.md` whole, the L2 slice derivation whole,
`docs/research_papers/BIBLIOGRAPHY.md` whole, `reading_pass/population.md` at §3's verification rows V1
to V13, the row 51 extract at its banner and heading list for the form, the row 11 extract at its
domain-bound passage on the progress record's carried item (d), and `FRAMEWORK.md` at §4.2 whole
(237–271), at the **L2 charter whole (386–425)** and at the **OPENING ONLY of the L3 charter
(426–430, that section running to 451 and the rest of it NOT read)**, at the design-points block whole
with DP-A to DP-Q (669–817), at A.3 whole (1133–1155), at §S4 whole (1547–1628), at §S5's entanglement
argument whole (1631–1656) and at §S6's DP1 to DP17 (1783–1882), **every boundary taken from the
file's own heading list rather than from where this side happened to be reading**. Every file was read with the file tools from bridge-staged copies;
no shell read the repository or any staged copy of it. The paper was read as page images with the file
tools, all eight pages, pages 6 and 7 each a second time on their own. No count of this project's own
measurement is restated (D-431).*
