# EXTRACT — Lafferty, McCallum & Pereira, "Conditional Random Fields: Probabilistic Models for Segmenting and Labeling Sequence Data" — Task B candidacy row 12, first pass, AT THE OBJECT

> **STATUS: FIRST-PASS EXTRACT, READ WHOLE AT THE OBJECT (2026-09-06).** Written under
> `cowork_reading_pass_commission_2026_08_30.md` §4, whose form the remedial commission
> (`cowork_reading_pass_remedial_commission_2026_08_31.md` §3) binds unchanged.
>
> **The grade.** All eight pages of the held PDF were read AT THE OBJECT: staged through the bridge and
> read with the file tools as page images. **No relay, no web-fetch read, no prompted extraction.**
> **The page count was ESTABLISHED rather than assumed** — a request for page 20 was refused by the
> tool with *"PDF has 8 pages"*. Pages 8, 2 and 7 were each read a second time on their own, in that
> order — page 8 for the reference list, pages 2 and 7 for the passages this extract quotes at length
> and for Figure 4's cells. **The held document prints no page numbers**; every location below is the
> page's position in the held document (1–8) together with the section, equation, figure or table
> number as printed.
>
> **★ THE SUBJECT IS NOT MUSIC AT ALL.** It is a machine-learning formalism for labelling sequences,
> demonstrated on synthetic data generated from hidden Markov models and on part-of-speech tagging of
> English text. **Nothing musical appears anywhere in the eight pages.** Rows 11 and 15 of this slice
> have the same shape; the rows are named and no count is asserted. **Every claim carried out of this
> paper therefore crosses a domain boundary and says so at the point of use** — the bound row 11's
> extract wrote first (its finding (6): *"so the formalism paper is not later cited as if it measured
> the musical gain"*) and row 15's follows.
>
> **Why this paper and its place in L2's slice.** It is row 12 of `reading_pass/candidacy_upgrades.md`
> (line 74), ADMITTED there **outright and not on the doubt default**, with the reason *"The base
> formalism the two rows above extend; it decides how L2's score is formed and normalised, which §5
> leaves expressly to the detail specification."* `cowork_l2_task_b_slice_derivation_2026_09_05.md` §4
> (line 59) places it in L2's slice on the same ground: *"How L2's score is formed and normalised — the
> detail the charter expressly leaves to the specification."* It is the ninth member of group 3 of the
> proposed reading order (*"the scoring architecture and its fitting"*, `candidacy_upgrades.md` line
> 177). Ruling 1 of `cowork_rulings_2026_09_05_l2_boot_list_sitting.md` keeps the gate: no derivation
> before L2's slice of Task B is read.
>
> **★ THE VERIFICATION TARGET QUESTION IS ANSWERED HERE AND THE ANSWER IS THAT THERE IS NONE.** The
> sweeps and the whole reading are recorded at finding (2) below. **The record bears on this paper in
> exactly THREE places — the candidacy row, the slice derivation and the bibliography — and
> `FRAMEWORK.md` is not one of them.** That is row 51's shape, not row 15's; the two are named and
> nothing is inferred beyond them.
>
> **This extract derives no specification statement, amends no document, opens no code and writes no
> open-items row or decisions-register entry.**

## Identity — a prefix title match, and no venue, year, copyright, licence or page number printed anywhere

**Printed title (page 1):** *"Conditional Random Fields: Probabilistic Models for Segmenting and
Labeling Sequence Data"*. **Printed authors (page 1):** John Lafferty, Andrew McCallum, Fernando
Pereira, each with an e-mail address, and three printed affiliations — WhizBang! Labs–Research, 4616
Henry Street, Pittsburgh, PA 15213 USA; School of Computer Science, Carnegie Mellon University,
Pittsburgh, PA 15213 USA; Department of Computer and Information Science, University of Pennsylvania,
Philadelphia, PA 19104 USA.

`docs/research_papers/BIBLIOGRAPHY.md` line 26 names *"Lafferty, McCallum & Pereira, 'Conditional
Random Fields,' ICML 2001"*, with a `cs.columbia.edu` URL, held ✓, tier **LINK**.

- **The three authors match, in the row's own order.**
- **The title matches as a PREFIX** — the row's title is the printed title's own text up to the colon.
  Rows 11, 45, 46, 47, 48 and 50 also produced prefix matches; the rows are named and no count is
  asserted.
- **★ NO venue, year, conference line, copyright notice, licence line, page number, header or footer is
  printed anywhere in the eight pages.** So **whether the held file is the ICML 2001 proceedings text
  as published is not establishable from what is held**, and the row's LINK tier rests on its URL
  rather than on a printed line. **Row 15's read produced the same printed-nothing shape** (rows 29's,
  11's and 18's are its milder family), **and the two rows differ on the tier**: row 15's row reads CC
  and this one reads LINK. **The tier direction here:** LINK means openly hosted with rights retained,
  and the ABSENCE of a CC line is consistent with it, so **LINK is the correct conservative reading and
  no tier correction is owed** — the direction rows 51 and 52 produced, and the opposite of rows 30's,
  49's and 50's understatements.
- **The year is inferable from the reference list and is not a printed venue line:** the
  latest-dated entry is *Punyakanok, V., & Roth, D. (2001). The use of classifiers in sequential
  inference. NIPS 13. Forthcoming.* (page 8). Recorded as an inference of this reader's, not as a
  printed check that closed.

Routed to the bibliography reconciliation beside rows 28's, 3's, 29's, 10's, 11's, 18's, 49's and
15's findings. No verdict.

**File:** `docs/research_papers/lafferty_mccallum_pereira_2001_icml_conditional_random_fields.pdf`
(**307,737 bytes** at this session's `docs/research_papers/` listing), the one matching name in that
directory.

**Structure, as printed:** eight pages, two columns. Abstract; 1. Introduction; 2. The Label Bias
Problem; 3. Conditional Random Fields; 4. Parameter Estimation for CRFs; 5. Experiments, with 5.1
Modeling label bias, 5.2 Modeling mixed-order sources, 5.3 POS tagging experiments; 6. Further Aspects
of CRFs; 7. Related Work and Conclusions; Acknowledgments; References. Four figures (Figure 1 the
label-bias automaton, Figure 2 the three graphical structures, Figure 3 three scatter plots, Figure 4
a table of per-word error rates), **two numbered equations — (1) at §3 and (2) at §4** — three
footnotes, and **25 references** — derived at the page: seven entries begun in the left column
(Abney; Berger, Della Pietra & Della Pietra; Bottou; Brill; Collins; Collins, Schapire & Singer;
Darroch & Ratcliff) and eighteen in the right (Della Pietra, Della Pietra & Lafferty through Schwartz
& Austin). **The list runs alphabetically and ends at the foot of page 8's right column with Schwartz
& Austin (1993); whether any further entry was cut at the page bottom is not establishable from what
is held**, the document printing no page numbers and no end marker.

## Claims, labeled

### What the model IS, and what it decides

**★ [FACT, p. 1 Abstract; p. 3 §3] A conditional random field is a single globally-normalised
exponential model over the WHOLE label sequence given the whole observation sequence.** The Abstract:
*"We present conditional random fields, a framework for building probabilistic models to segment and
label sequence data. Conditional random fields offer several advantages over hidden Markov models and
stochastic grammars for such tasks, including the ability to relax strong independence assumptions
made in those models. Conditional random fields also avoid a fundamental limitation of maximum entropy
Markov models (MEMMs) and other discriminative Markov models based on directed graphical models, which
can be biased towards states with few successor states."* (p. 1.)

The definition (p. 3 §3): *"Let G = (V, E) be a graph such that Y = (Y_v)_{v∈V}, so that Y is indexed
by the vertices of G. Then (X, Y) is a conditional random field in case, when conditioned on X, the
random variables Y_v obey the Markov property with respect to the graph: p(Y_v | X, Y_w, w ≠ v) =
p(Y_v | X, Y_w, w ∼ v), where w ∼ v means that w and v are neighbors in G."* And immediately after:
*"Thus, a CRF is a random field globally conditioned on the observation X. Throughout the paper we
tacitly assume that the graph G is fixed."*

**★ [FACT, p. 3 §3, eq. 1] THE SCORE FORM — this is the half of the candidacy row's reason that says
"how L2's score is FORMED".** For G a tree (of which a chain is the simplest case), the cliques are the
edges and the vertices, so by the fundamental theorem of random fields (Hammersley & Clifford, 1971)
*"the joint distribution over the label sequence Y given X has the form*

  p_θ(y | x) ∝ exp( Σ_{e∈E, k} λ_k f_k(e, y|_e, x) + Σ_{v∈V, k} μ_k g_k(v, y|_v, x) )  (1)

*where x is a data sequence, y a label sequence, and y|_S is the set of components of y associated with
the vertices in subgraph S."* And, immediately: *"We assume that the features f_k and g_k are given and
fixed. For example, a Boolean vertex feature g_k might be true if the word X_i is upper case and the
tag Y_i is 'proper noun.'"*

**★ [FACT, p. 4 §3] THE NORMALISATION — this is the other half, "how L2's score is NORMALISED", and it
is stated as an explicit contrast with two rival placements of the normaliser.** In matrix form, with
start and stop states added, M_i(y′, y | x) = exp(Λ_i(y′, y | x)) and Λ_i(y′, y | x) = Σ_k λ_k f_k(e_i,
Y|_{e_i} = (y′, y), x) + Σ_k μ_k g_k(v_i, Y|_{v_i} = y, x): *"Then the normalization (partition
function) Z_θ(x) is the (start, stop) entry of the product of these matrices: Z_θ(x) = (M_1(x) M_2(x)
··· M_{n+1}(x))_{start,stop}"*, and p_θ(y | x) = Π_{i=1}^{n+1} M_i(y_{i−1}, y_i | x) / (Π_{i=1}^{n+1}
M_i(x))_{start,stop}. The two rivals are named at the object: against a *joint* model — *"Boltzmann
chain models (Saul & Jordan, 1996; MacKay, 1996) have a similar form but use a single normalization
constant to yield a joint distribution, whereas CRFs use the observation-dependent normalization Z(x)
for conditional distributions"* (p. 3) — and against a *per-decision* model, which is §2 in full below.

**★ [FACT, p. 2 §1] The stated difference from a maximum entropy Markov model is exactly WHERE the
normalisation is applied.** *"The critical difference between CRFs and MEMMs is that a MEMM uses
per-state exponential models for the conditional probabilities of next states given the current state,
while a CRF has a single exponential model for the joint probability of the entire sequence of labels
given the observation sequence. Therefore, the weights of different features at different states can be
traded off against each other."* And, in the same paragraph block: *"We can also think of a CRF as a
finite state model with un-normalized transition probabilities. However, unlike some other weighted
finite-state approaches (LeCun et al., 1998), CRFs assign a well-defined probability distribution over
possible labelings, trained by maximum likelihood or MAP estimation. Furthermore, the loss function is
convex,² guaranteeing convergence to the global optimum."* **Footnote 2, which is the qualification and
is quoted because a later reader must meet it with the claim:** *"In the case of fully observable
states, as we are discussing here; if several states have the same label, the usual local maxima of
Baum-Welch arise."*

**[FACT, p. 3 §3] What the graph must and need not be.** *"X may also have a natural graph structure;
yet in general it is not necessary to assume that X and Y have the same graphical structure, or even
that X has any graphical structure at all. However, in this paper we will be most concerned with
sequences X = (X_1, X_2, …, X_n) and Y = (Y_1, Y_2, …, Y_n)."*

**★ [FACT, p. 3 §3] The HMM-like CRF construction, stated by the paper as a particular case.** *"As a
particular case, we can construct an HMM-like CRF by defining one feature for each state pair (y′, y),
and one feature for each state-observation pair (y, x): f_{y′,y}(⟨u, v⟩, y|_{⟨u,v⟩}, x) = δ(y_u, y′)
δ(y_v, y), g_{y,x}(v, y|_v, x) = δ(y_v, y) δ(x_v, x). The corresponding parameters λ_{y′,y} and μ_{y,x}
play a similar role to the (logarithms of the) usual HMM parameters p(y′ | y) and p(x | y)."* And the
width claim attached to it: *"Although it encompasses HMM-like models, the class of conditional random
fields is much more expressive, because it allows arbitrary dependencies on the observation sequence.
In addition, the features do not need to specify completely a state or observation, so one might expect
that the model can be estimated from less training data. Another attractive property is the convexity of
the loss function; indeed CRFs share all of the convexity properties of general maximum entropy models."*

### §2 — the label bias problem, quoted at length because it is this read's principal finding

**★ [FACT, p. 2 §1 and §2] The defect, its mechanism, and the model classes the paper says are subject
to it.** *"MEMMs and other non-generative finite-state models based on next-state classifiers, such as
discriminative Markov models (Bottou, 1991), share a weakness we call here the label bias problem: the
transitions leaving a given state compete only against each other, rather than against all other
transitions in the model. In probabilistic terms, transition scores are the conditional probabilities of
possible next states given the current state and the observation sequence. This per-state normalization
of transition scores implies a 'conservation of score mass' (Bottou, 1991) whereby all the mass that
arrives at a state must be distributed among the possible successor states. An observation can affect
which destination states get the mass, but not how much total mass to pass on. This causes a bias toward
states with fewer outgoing transitions. In the extreme case, a state with a single outgoing transition
effectively ignores the observation. **In those cases, unlike in HMMs, Viterbi decoding cannot downgrade
a branch based on observations after the branch point**, and models with state-transition structures
that have sparsely connected chains of states are not properly handled. The Markovian assumptions in
MEMMs and similar state-conditional models insulate decisions at one state from future decisions in a
way that does not match the actual dependencies between consecutive states."* (Emphasis this reader's,
marked; the words are the paper's.)

**★ [FACT, p. 2 §2] WHICH model classes the paper names as subject to it — and HMMs are NOT among
them.** *"Classical probabilistic automata (Paz, 1971), discriminative Markov models (Bottou, 1991),
maximum entropy taggers (Ratnaparkhi, 1996), and MEMMs, as well as non-probabilistic sequence tagging
and segmentation models with independently trained next-state classifiers (Punyakanok & Roth, 2001) are
all potential victims of the label bias problem."* **This enumeration is the bound on every use of the
argument** — see finding (4).

**[FACT, p. 2 §2] The worked example.** Figure 1's five-state automaton distinguishes *rib* from *rob*;
on the observation sequence `r i b`, *"Both states 1 and 4 have only one outgoing transition. State 1
has seen this observation often in training, state 4 has almost never seen this observation; but
regardless, state 4 has no choice but to pass all its mass to its single outgoing transition, since it is
not generating the observation, only conditioning on it. Thus, states with a single outgoing transition
effectively ignore their observations. More generally, states with low-entropy next state distributions
will take little notice of observations."*

**[FACT, p. 2–3 §2] The two solutions the paper rejects, and the shape it says a proper solution must
have.** Bottou's two: changing the state-transition structure by determinization — *"but determinization
of weighted finite-state machines is not always possible, and even when possible, it may lead to
combinatorial explosion"* — and starting from a fully-connected model and letting training find the
structure — *"But that would preclude the use of prior structural knowledge that has proven so valuable
in information extraction tasks (Freitag & McCallum, 2000)."* Then: *"Proper solutions require models
that account for whole state sequences at once by letting some transitions 'vote' more strongly than
others depending on the corresponding observations. This implies that score mass will not be conserved,
but instead individual transitions can 'amplify' or 'dampen' the mass they receive."* And the paper's
own claim of novelty: *"To the best of our knowledge, CRFs are the only model class that does this in a
purely probabilistic setting, with guaranteed global maximum likelihood convergence."*

### §4 — how it is fitted

**[FACT, p. 3 §3; pp. 4–5 §4] The objective, and the two algorithms.** The training objective is the
conditional log-likelihood O(θ) = Σ_{i=1}^N log p_θ(y^(i) | x^(i)) ∝ Σ_{x,y} p̃(x, y) log p_θ(y | x)
(p. 3). §4 gives *"two iterative scaling algorithms to find the parameter vector θ that maximizes the
log-likelihood of the training data. Both algorithms are based on the improved iterative scaling (IIS)
algorithm of Della Pietra et al. (1997); the proof technique based on auxiliary functions can be extended
to show convergence of the algorithms for CRFs."* **Algorithm S** introduces a *slack feature* s(x, y) =
S − Σ_i Σ_k f_k(e_i, y|_{e_i}, x) − Σ_i Σ_k g_k(v_i, y|_{v_i}, x), with S *"a constant chosen so that
s(x^(i), y) ≥ 0 for all y and all observation vectors x^(i) in the training set, thus making T(x, y) =
S"*; the feature is *"global"* — *"it does not correspond to any particular edge or vertex"*.
**Algorithm T** *"keeps track of partial T totals"* and accumulates feature expectations into counters
indexed by T(x) = max_y T(x, y), the updates being δλ_k = log β_k and δμ_k = log γ_k where β_k and γ_k are
the unique positive roots of the polynomial equations (2). *"A single iteration of Algorithm S and
Algorithm T has roughly the same time and space complexity as the well known Baum-Welch algorithm for
HMMs."* (p. 5.)

**[FACT, p. 5 §4] Why two algorithms — the cost of Algorithm S's constant.** *"The constant S in Algorithm
S can be quite large, since in practice it is proportional to the length of the longest training
observation sequence. As a result, the algorithm may converge slowly, taking very small steps toward the
maximum in each iteration. If the length of the observations x^(i) and the number of active features
varies greatly, a faster-converging algorithm can be obtained by keeping track of feature totals for each
observation sequence separately."*

**[FACT, p. 5 §4] The forward and backward recursions, and the exact marginal.** α_i(x) = α_{i−1}(x)
M_i(x); β_{n+1}(y | x) = 1 if y = stop else 0, and β_i(x)^T = M_{i+1}(x) β_{i+1}(x). *"The factors
involving the forward and backward vectors in the above equations have the same meaning as for standard
hidden Markov models. For example, p_θ(Y_i = y | x) = α_i(y | x) β_i(y | x) / Z_θ(x) is the marginal
probability of label Y_i = y given that the observation sequence is x."* And: *"This algorithm is closely
related to the algorithm of Darroch and Ratcliff (1972), and MART algorithms used in image
reconstruction."*

### Measured results — corpus, metric and value as the paper states them

**★ [FACT, p. 6 §5.1] The label-bias experiment.** *"We generate data from a simple HMM which encodes a
noisy version of the finite-state network in Figure 1. Each state emits its designated symbol with
probability 29/32 and any of the other symbols with probability 1/32. We train both an MEMM and a CRF
with the same topologies on the data generated by the HMM. The observation features are simply the
identity of the observation symbols. In a typical run using 2,000 training and 500 test samples, trained
to convergence of the iterative scaling algorithm, **the CRF error is 4.6% while the MEMM error is 42%**,
showing that the MEMM fails to discriminate between the two branches."* (**Emphasis this reader's; the
words and both values are the paper's, and the paper emphasises nothing in this sentence.**)

**[FACT, p. 6 §5.2] The mixed-order experiment.** Five labels a–e (|Y| = 5) and 26 observation values A–Z
(|X| = 26); data from a mixed-order HMM with state transitions p_α(y_i | y_{i−1}, y_{i−2}) = α p_2(y_i |
y_{i−1}, y_{i−2}) + (1 − α) p_1(y_i | y_{i−1}) and emissions of the same mixed form, so α = 0 is a standard
first-order HMM. Conditional probability tables constrained sparse (*"at most two nonzero entries, for
each y, y′"*, and *"at most three nonzero entries for each y, x′"*). *"For each randomly generated model, a
sample of 1,000 sequences of length 25 is generated for training and testing."* Training cost, stated:
Algorithm S *"is fairly slow to converge, typically taking approximately 500 iterations for the model to
stabilize. On the 500 MHz Pentium PC used in our experiments, each iteration takes approximately 0.2
seconds. On the same data an MEMM is trained using iterative scaling, which does not require
forward-backward calculations, and is thus more efficient. MEMM training converges more quickly,
stabilizing after approximately 100 iterations."* Figure 3's result, in the caption's and the text's own
words: *"As the data becomes 'more second order,' the error rates of the test models increase. As shown in
the left plot, the CRF typically significantly outperforms the MEMM. The center plot shows that the HMM
outperforms the MEMM. In the right plot, each open square represents a data set with α < ½, and a solid
circle indicates a data set with α ≥ ½. The plot shows that when the data is mostly second order (α ≥ ½),
the discriminatively trained CRF typically outperforms the HMM. These experiments are not designed to
demonstrate the advantages of the additional representational power of CRFs and MEMMs relative to HMMs."*
And in the text: *"The CRF generally outperforms the MEMM, often by a wide margin of 10%–20% relative
error. (The points for very small error rate, with α < 0.01, where the MEMM does better than the CRF, are
suspected to be the result of an insufficient number of training iterations for the CRF.)"*

**★ [FACT, p. 7 §5.3 and Figure 4] The part-of-speech tagging experiment — the only natural-language
measurement.** Penn treebank POS tagging, *"where each word in a given input sentence must be labeled
with one of 45 syntactic tags"*; first-order models trained on 50% of the 1.1 million word corpus; the
caption states the out-of-vocabulary rate as 5.45%. **Figure 4, every cell as printed:**

| model | error | oov error |
|---|---|---|
| HMM | 5.69% | 45.99% |
| MEMM | 6.37% | 54.61% |
| CRF | 5.55% | 48.05% |
| MEMM⁺ | 4.81% | 26.99% |
| CRF⁺ | 4.27% | 23.76% |

⁺ *"Using spelling features"*. **The printed table's three column headings (*model*, *error*, *oov
error*) are set in italic; no DATA cell carries emphasis of any kind, the table states no
best-cell or significance convention, and no cell is emphasised by this reader.** The paper's own
reading: *"The results are consistent with what is observed on synthetic
data: the HMM outperforms the MEMM, as a consequence of the label bias problem, while the CRF
outperforms the HMM."* The second set adds orthographic features — *"whether a spelling begins with a
number or upper case letter, whether it contains a hyphen, and whether it ends in one of the following
suffixes: -ing, -ogy, -ed, -s, -ly, -ion, -tion, -ity, -ies"* — and *"Here we find, as expected, that
both the MEMM and the CRF benefit significantly from the use of these features, with the overall error
rate reduced by around 25%, and the out-of-vocabulary error rate reduced by around 50%."*

**★ [FACT, p. 7 §5.3] The training-cost finding, stated by the authors against their own method.** *"One
usually starts training from the all zero parameter vector, corresponding to the uniform distribution.
However, for these datasets, CRF training with that initialization is much slower than MEMM training.
Fortunately, we can use the optimal MEMM parameter vector as a starting point for training the
corresponding CRF. In Figure 5.3, MEMM⁺ was trained to convergence in around 100 iterations. Its
parameters were then used to initialize the training of CRF⁺, which converged in 1,000 iterations. In
contrast, training of the same CRF from the uniform distribution had not converged even after 2,000
iterations."*

**★ [FACT — recorded as printed and reconciled nowhere] The text refers twice to a *"Figure 5.3"* that
does not exist as a figure number in the eight pages** — the object it describes is the printed **Figure
4**. Row 52's read recorded a finding of the same shape (a §5.1 reference to a "Figure 3" that does not
exist); the rows are named and no count is asserted.

### §6 and §7 — the paper's own further aspects, related work and bound

**[FACT, p. 7 §6] Two further aspects, both named as unexplored here.** *"Conditional random fields can
be trained using the exponential loss objective function used by the AdaBoost algorithm (Freund &
Schapire, 1997)"* — with the parallel update algorithm of Collins et al. (2000) to optimise the
per-sequence exponential loss, requiring a forward-backward algorithm *"along the lines of Algorithm T,
except that each feature requires a separate set of forward and backward accumulators"*. And feature
induction: *"rather than specifying in advance which features of (X, Y) to use, we could start from
feature-generating rules and evaluate the benefit of generated features automatically on data. In
particular, the feature induction algorithms presented in Della Pietra et al. (1997) can be adapted to
fit the dynamic programming techniques of conditional random fields."*

**★ [FACT, p. 7 §7] The claim of what is new, and the published rival finding (7) carries.**
*"As far as we know, the present work is the first to combine the benefits of conditional models with the
global normalization of random field models. Other applications of exponential models in sequence modeling
have either attempted to build generative models (Rosenfeld, 1997), which involve a hard normalization
problem, or adopted local conditional models (Berger et al., 1996; Ratnaparkhi, 1996; McCallum et al.,
2000) that may suffer from label bias."* Then, on locally-trained decision models: *"Non-probabilistic
local decision models have also been widely used in segmentation and tagging (Brill, 1995; Roth, 1998;
Abney et al., 1999). Because of the computational complexity of global training, these models are only
trained to minimize the error of individual label decisions assuming that neighboring labels are correctly
chosen. Label bias would be expected to be a problem here too."* **And the generate-then-rerank rival, with
the paper's own reason for rejecting it:** *"An alternative approach to discriminative modeling of sequence
labeling is to use a permissive generative model, which can only model local dependencies, to produce a
list of candidates, and then use a more global discriminative model to rerank those candidates. This
approach is standard in large-vocabulary speech recognition (Schwartz & Austin, 1993), and has also been
proposed for parsing (Collins, 2000). **However, these methods fail when the correct output is pruned away
in the first pass.**"* (Emphasis this reader's; the words are the paper's.)

**[FACT, p. 8 §7] The nearest rival named, and its stated defect.** *"Closest to our proposal are
gradient-descent methods that adjust the parameters of all the local classifiers to minimize a smooth loss
function (e.g., quadratic loss) combining loss terms for each label. If state dependencies are local, this
can be done efficiently with dynamic programming (LeCun et al., 1998). Such methods should alleviate label
bias. However, their loss function is not convex, so they may get stuck in local minima."*

**★ [FACT, p. 8 §7] The paper's own summary of its properties AND its own stated limitation.**
*"Conditional random fields offer a unique combination of properties: discriminatively trained models for
sequence segmentation and labeling; combination of arbitrary, overlapping and agglomerative observation
features from both the past and future; efficient training and decoding based on dynamic programming; and
parameter estimation guaranteed to find the global optimum. **Their main current limitation is the slow
convergence of the training algorithm relative to MEMMs, let alone to HMMs, for which training on fully
observed data is very efficient.** In future work, we plan to investigate alternative training methods such
as the update methods of Collins et al. (2000) and refinements on using a MEMM as starting point as we did
in some of our experiments. More general tree-structured random fields, feature induction methods, and
further natural data evaluations will also be investigated."* (**Emphasis this reader's on the limitation
sentence; the words are the paper's and the paper emphasises nothing in this passage.**)

**[THEORY, as the paper cites it]** Hammersley & Clifford (1971) for the factorisation over cliques; Della
Pietra, Della Pietra & Lafferty (1997) for improved iterative scaling and for feature induction; Darroch &
Ratcliff (1972) for the related generalised iterative scaling; Bottou (1991) for the conservation of score
mass; McCallum, Freitag & Pereira (2000) for MEMMs; Saul & Jordan (1996) and MacKay (1996) for Boltzmann
chains.

**[CONJECTURE — the authors' own, labelled as such here]** That the features not needing to specify a state
or observation completely means *"one might expect that the model can be estimated from less training
data"* (p. 3) — expressed as an expectation and measured nowhere in the paper. And that the anomalous
low-error points of Figure 3 *"are suspected to be the result of an insufficient number of training
iterations for the CRF"* (p. 6) — the authors' own suspicion, not a measurement.

## Coupling facts (mandatory)

**What the method ASSUMES about its upstream.**
- A discrete observation sequence x = (x_1, …, x_n) and a label variable per position, indexed by the
  vertices of a **fixed** graph G, which the paper takes to be a chain for everything after §3. The
  boundary structure is therefore given, not decided: **there is no segment variable and no segment
  length anywhere in the model.**
- A finite label alphabet Y, **one label per position**.
- **A feature set that is given and fixed** — *"We assume that the features f_k and g_k are given and
  fixed"* (p. 3). The model decides the weights, never which terms exist; feature induction is named at §6
  as something that *could* be adapted, and is not done here.
- Training data as fully labelled sequences D = {(x^(i), y^(i))}, with the **fully-observable-state
  qualification of footnote 2** attached to the convexity claim.
- Features may depend on the whole observation sequence x, from *"both the past and future"* (p. 8), and may
  be arbitrary, overlapping and non-independent — this is the paper's central stated advantage over a
  generative model.

**What it HANDS downstream.**
- One best label sequence by Viterbi over the M_i matrices, and — this is the part a rival single-answer
  model cannot hand on — **a normalised conditional distribution p_θ(y | x) over all label sequences**, with
  Z_θ(x) computable exactly as a matrix product and per-position marginals p_θ(Y_i = y | x) = α_i β_i / Z_θ
  available from the same recursions (p. 5).
- No segmentation object: segments are read off as runs of equal labels; the paper's own word *"segmenting"*
  in its title means labelling every element such that segments emerge, which is exactly what row 11's paper
  says it changes (*"labels are assigned to segments … rather than to individual elements x_i of x"*).

**Its own STATED SCOPE and limits.**
- **Domain:** sequence segmentation and labelling in general; measured on synthetic HMM-generated data and
  on English part-of-speech tagging. **Nothing musical anywhere in the paper.**
- **Expressiveness:** more expressive than HMM-like models *"because it allows arbitrary dependencies on the
  observation sequence"*; the chain case is the one developed, with general tree-structured random fields
  named as future work.
- **Convexity:** guaranteed global optimum **in the case of fully observable states**; footnote 2 states in
  terms that if several states share a label, *"the usual local maxima of Baum-Welch arise"*.
- **Cost:** an iteration of Algorithm S or T is *"roughly the same time and space complexity as the well
  known Baum-Welch algorithm for HMMs"*; the paper's own stated main limitation is **slow convergence** of
  training relative to MEMMs and to HMMs.
- **Coupling:** one label per position, decided over the whole sequence at once; no second label axis, no
  segment variable, no hierarchy.

## What an L2 detail specification could adopt, adapt, or must argue against

- **Adopt (the score form, which is the candidacy row's own reason):** equation (1) — a score over candidate
  readings formed as the exponential of a weighted sum of feature functions over the cliques of a fixed
  graph, with the weights fitted and the features given. This is the published form of *"how the score over
  candidate readings is formed, what its terms are, what tables they read, and how any weight is fitted"*
  that the L2 charter (`FRAMEWORK.md` lines 406–408) leaves to the detail specification. **The paper decides
  the FORM and expressly not the TERMS** — the features are given and fixed — so it answers half of the
  charter's sentence and is silent on the other half.
- **Adopt (the normalisation, and its placement):** the observation-dependent partition function Z_θ(x)
  computed as a matrix product over the sequence, with the exact forward–backward recursions of §4 — so that
  a normalised distribution over whole readings, and per-span marginals, are available rather than only a
  best path. Beside **D-006** (two full candidate lists, no truncation), **D-008** (true probabilities
  deferred) and **DP-K** (rivals with their mass); row 11's finding (10) recorded this datum at the
  segmental extension and it is recorded here at the base formalism, the two named and merged nowhere.
- **Adapt (the HMM-like construction as the published route from a counted table to a fitted score):** §3's
  particular case builds a CRF whose features are exactly the state-pair and state-observation indicators of
  an HMM, so that *"the corresponding parameters λ_{y′,y} and μ_{y,x} play a similar role to the (logarithms
  of the) usual HMM parameters"*. That is the shape **D-525**'s staged fit already has read from the other
  side: the counted-and-frozen tables become the model's features, and what is fitted is the vector of
  weights over them. Routed to L2's detail specification beside **D-525** and DP-P as a published precedent
  for the construction, not as a derivation of ours.
- **Adapt (the convexity property, with its qualification, as a design check):** footnote 2 makes the global
  optimum conditional on fully observable states. L2's state carries tonality, boundary, chord-tone
  assignment and chord together; whether every component of it is observed in the ground truth decides
  whether this property is available at all. Routed to L2's detail specification and to measurement design
  as a check to run, not as an answer.
- **Must argue against — the paper's own named rivals, each with the paper's own ground:** the
  generate-candidates-then-rerank pipeline, rejected because *"these methods fail when the correct output is
  pruned away in the first pass"*; locally normalised conditional models, rejected on label bias; local
  gradient-descent methods over per-label losses, rejected because their loss is not convex; and a fully
  connected structure learned from data, rejected because it *"would preclude the use of prior structural
  knowledge"*.
- **Must argue against — the domain:** nothing in the paper decides a musical question, and every measured
  figure is on synthetic data or English POS tagging. It carries a FORMAL and a MEASURED-ON-TEXT result and
  no musical evidence; the musical instances of this family are rows 10 and 19, and its segmental extension
  is row 11.

## ★ Findings, routed and not applied

**(1) IDENTITY: a prefix title match, three authors matching, and no venue, year, copyright, licence or
page number printed anywhere in the eight pages.** The printed title is *"Conditional Random Fields:
Probabilistic Models for Segmenting and Labeling Sequence Data"* where the bibliography's row 26 names
*"Conditional Random Fields," ICML 2001*; the three authors match in the row's own order; three
affiliations and three e-mail addresses are printed. Whether the held file is the ICML 2001 proceedings
text as published is **not establishable from what is held**. The row's **LINK** tier is the correct
conservative reading and no tier correction is owed, the absence of a CC line being consistent with it.
The year is inferable from the reference list's latest entry (*Punyakanok & Roth, 2001, "NIPS 13.
Forthcoming."*) and is recorded as this reader's inference, not as a printed check that closed. Routed to
the bibliography reconciliation. **No verdict.**

**(2) ★ THERE IS NO VERIFICATION TARGET, AND THE RECORD BEARS ON THIS PAPER IN EXACTLY THREE PLACES —
ROW 51's SHAPE, NOT ROW 15's.** The three are `reading_pass/candidacy_upgrades.md` line 74,
`cowork_l2_task_b_slice_derivation_2026_09_05.md` line 59, and `docs/research_papers/BIBLIOGRAPHY.md`
line 26. **How that was established, stated as the acts that were run rather than as a verdict:** an
IDENTITY sweep of the staged tree for the bare words *"Lafferty"*, *"McCallum"* and *"Pereira"*, and
case-insensitively for *"conditional random field"* and *"CRF"*, which returns **no match of any of them
anywhere in `FRAMEWORK.md`** — that file does not appear in the match list at all — and, in
`reading_pass/population.md` and `cowork_reading_pass_findings_2026_08_31.md`, only the *semi-CRF*
occurrences belonging to rows 10, 11 and 19; a BARE-WORD sweep for *"2001"*, which returns **no match at
all in `FRAMEWORK.md`** and elsewhere reaches row 13 (Ng & Jordan) at the slice derivation, the
bibliography and the candidacy row, Temperley's 2001 book at candidacy row 9 and the bibliography, and —
inside this line's own files — this row's own three places together with several 2001-dated works and one
corpus name that other rows' reads recorded in their not-fetched lists, **each distinguished at its own
line and none of them a citation of this paper**; a FIGURE sweep run
**after** the paper was read, over the staged tree, for *"5.69"*, *"45.99"*, *"6.37"*, *"54.61"*,
*"5.55"*, *"48.05"*, *"4.81"*, *"26.99"*, *"4.27"*, *"23.76"*, *"5.45"*, *"label bias"*, *"MEMM"*,
*"iterative scaling"*, *"partition function"* and *"log-linear"*, which reaches **no citation of this
paper anywhere** (its *"partition function"* hits are the row 11 extract's own, and its *"log-linear"*
hits are row 15's material); and a **WHOLE READING** of `FRAMEWORK.md` §4.2 (237–271), the L2 charter
(386–425), the L3 charter (426–451), the design-points block with DP-A to DP-Q (669–817), A.3
(1133–1155), §S4 whole (1547–1628), §S5's entanglement argument (1631–1656) and §S6 whole
(1777–1885, its DP1 beginning at 1783 and its DP17 ending at 1882) — **every one of those boundaries taken from the file's own heading list** — which reached
nothing further. `reading_pass/population.md`'s verification rows **V1 to V13 were read at the object
this session** and name no figure of this paper; V10 attaches the linear-versus-exponential formal result
to Sarawagi & Cohen (row 11). **So no `[FACT]`, no ratified figure and no characterisation of any kind
rests on this paper**, and the read is not a verification. **One false positive is recorded so nobody
merges them:** the digits *"5.69"* occur inside *"25.69"* in the progress record, which is row 46's own
overall-accuracy cell, distinguished at its line. **No verdict.**

**(3) ★ THE CANDIDACY ROW'S REASON IS CONFIRMED AT THE OBJECT, IN BOTH ITS HALVES, WITH ONE PRECISION ON
WHAT "THE BASE FORMALISM THE TWO ROWS ABOVE EXTEND" MEANS.** The row says the paper *"decides how L2's
score is formed and normalised"*. At the object: equation (1) is the score's FORM — an exponential of a
weighted sum of feature functions over the cliques of a fixed graph — and Z_θ(x), the observation-dependent
partition function computed as a matrix product over the whole sequence, is the NORMALISATION, stated by
the paper against two rival placements by name (a single constant yielding a joint distribution, in
Boltzmann chains; per-state normalisation, in MEMMs). **So the row's reason is exact and the ADMITTED
verdict and the L2 placement stand confirmed at the object rather than merely left standing.** *The
precision:* what rows 10 and 11 extend is **the per-element labelling**. This model assigns one label per
position of a fixed chain and has no segment variable and no segment length at all; segments are read off
as runs of equal labels. Row 11's paper states the change in its own words — labels *"assigned to segments
(i.e., subsequences) of x rather than to individual elements x_i of x"* — and row 10 instantiates that on
music. **The word *"Segmenting"* in this paper's own printed title therefore means labelling every element
so that segments emerge, not deciding a segmentation**, and a later citation must not read it as the
latter. Routed to L2's detail specification beside DP-C and the row 10 and row 11 extracts. **No verdict.**

**(4) ★★ THE ADDITION CANDIDATE THIS READ MOST WANTS CARRIED, AND THE RECORD DOES NOT CARRY IT: THE LABEL
BIAS PROBLEM IS A PUBLISHED, NAMED AND MEASURED ARGUMENT FOR NORMALISING OVER WHOLE READINGS RATHER THAN
AT EACH DECISION — WHICH IS THE ARGUMENT THIS PROJECT REACHES INDEPENDENTLY FROM TWO OTHER DIRECTIONS.**
The paper's mechanism, quoted above in full: per-state normalisation conserves score mass, so *"an
observation can affect which destination states get the mass, but not how much total mass to pass on"*,
and — the sentence that matters here — ***"In those cases, unlike in HMMs, Viterbi decoding cannot
downgrade a branch based on observations after the branch point."*** Its measurement: **CRF error 4.6%
against MEMM error 42%** on the synthetic label-bias data (§5.1), and, on Penn treebank POS tagging, the
MEMM measuring worse than the HMM (6.37% against 5.69%) which the authors attribute *"as a consequence of
the label bias problem"* (§5.3).

**Where this project already stands on the same point, read at the objects this session and named rather
than merged:** the L2 charter's own boundary condition says *"A hard constraint lets one wrong answer
delete the other question's right one"* and, generally, *"a reading-shaped evidence producer is a score and
never a constraint"* (`FRAMEWORK.md` lines 413–420); the charter's **may-not** clause forbids discarding *"a
rival before the whole sequence of spans has been scored"* and states its ground as C45's first reading —
*"a wrong reading can be a local optimum, so a decision taken at a moment on locally-optimal grounds is
unsafe by construction"* (lines 401–404); and the entanglement argument's **C41** says *"where a reading
wrongly carries a root forward, the separating evidence arrives after the moment of the error"*
(lines 1652–1654). **The paper's sentence is that same claim about a model's own normalisation, with a
measured cost attached.** It is therefore an ADDITION CANDIDATE for the ground under the charter's
no-early-discard clause and under DP-C's *"with"*, **stated as a candidate and not as a correction — the
record's existing grounds are untouched and nothing is amended.**

**★ THE BOUND, WHICH IS THE PAPER'S OWN AND IS WHAT KEEPS THIS FROM BEING CARRIED TOO FAR.** The paper
enumerates the model classes it says are subject to the defect — *"Classical probabilistic automata …
discriminative Markov models … maximum entropy taggers … and MEMMs, as well as non-probabilistic sequence
tagging and segmentation models with independently trained next-state classifiers"* — and **HMMs are not
among them**; indeed §5.1 and §5.3 both measure the HMM beating the MEMM. **So this is an argument about
LOCALLY NORMALISED conditional models, and it may not be carried against a model of another shape without
first establishing that shape at the object.** Whether this project's own production arm is of the shape the
argument reaches is **not established here and nothing in this extract asserts it**; establishing it would
mean reading that arm's own specification, which this read did not do. Routed to L2's detail specification,
to the findings surface's DP-C block, and — as a question, not a finding — to whoever next reads the joint
estimator's specification. **No verdict. Applied nowhere.**

**(5) ★ THE CONVEXITY AND GLOBAL-OPTIMUM PROPERTY, WITH ITS OWN QUALIFICATION — AND IT IS THE OTHER END OF
THE QUESTION ROW 15's PAPER ANSWERS.** This paper's fit maximises the conditional log-likelihood; *"the loss
function is convex, guaranteeing convergence to the global optimum"*, qualified at footnote 2 by *"in the
case of fully observable states … if several states have the same label, the usual local maxima of
Baum-Welch arise"*. **Row 15's read established, at its own object, that minimum error rate training trades
exactly this away** — the likelihood criterion's unique global optimum and its gradient, against an
objective with many local optima and no computable gradient — and that its measured payoff on the metric
actually wanted is the pair DP-P's `[FACT]` quotes. **That statement of row 15's content is taken from
`reading_pass/l2_slice_reading_progress.md`'s row 15 paragraph, read whole this session, and NOT from the
row 15 extract, which is the record for it and was not opened here** — a later use should be re-cited to
that extract. **So the two ends of DP-P's question are now both read at their own primaries in this slice,
and the record carries only one of them:** `FRAMEWORK.md` DP-P (lines
802–806) and §S6's DP16 (1874–1877) cite Och's result and say nothing about what the likelihood criterion
buys. Routed to L2's detail specification and to the findings surface's DP-P block beside **D-525**,
**D-271** and row 15's extract; **recorded beside row 15's three precisions and merged with none of them.**
**No verdict.**

**(6) THE HMM-LIKE CRF CONSTRUCTION IS A PUBLISHED ROUTE UNDER WHICH A COUNTED TABLE BECOMES A FEATURE AND
ONLY THE COMBINATION WEIGHTS ARE FITTED — D-525's STAGED FIT AS A SPECIAL CASE OF THE LOG-LINEAR FORM.**
§3's particular case makes each HMM transition and emission probability a feature whose weight is the
logarithm of that probability. **D-525** states the staged fit the other way round: the factor tables are
counted from ground truth and frozen, and only a small vector of combination weights is fitted
discriminatively, with an all-weights-equal ablation arm that must be beaten. **This paper is a published
precedent for the construction that makes those two descriptions one thing**; it is not a derivation of ours
and settles nothing about which tables L2 should carry. Routed to L2's detail specification beside **D-525**.
**No verdict.**

**(7) ★ A 2001 PUBLISHED STATEMENT OF THE L2 CHARTER'S CANDIDATE-ADMISSION BOUNDARY CONDITION, IN THE
PAPER'S OWN WORDS.** §7 names the generate-a-candidate-list-then-rerank design — *"use a permissive
generative model, which can only model local dependencies, to produce a list of candidates, and then use a
more global discriminative model to rerank those candidates"* — as standard in large-vocabulary speech
recognition and proposed for parsing, and rejects it in one sentence: ***"However, these methods fail when
the correct output is pruned away in the first pass."*** The L2 charter's second fixed boundary condition
says the same thing about our own search: *"A rule that decides which readings a span may even consider
bounds every claim the analysis can make about its own ceiling, because a reading the search cannot reach
cannot be found however good the scoring is"* (`FRAMEWORK.md` lines 421–424). Beside **D-098** (the
exact-decode reserve — the declared prune was never adopted). **Row 15's read carried the same dependency
from the other side**, its paper's own remedy being an outer loop that regrows the candidate list and
iterates to a fixed point; **the two are named and merged nowhere.** Routed to L2's detail specification.
**No verdict.**

**(8) THE LENGTH-TERM QUESTION: THIS PAPER ADDS NO SHAPE AND COULD NOT — IT HAS NO SEGMENT AND NO LENGTH
VARIABLE AT ALL.** One label per position of a fixed chain; the graph is fixed before the decode; segments
are runs. It is the **base case** the worked shapes this slice has collected are extensions of. **Those
shapes are listed at `reading_pass/l2_slice_reading_progress.md`'s carried item (g), read whole this
session, and each is recorded in its own row's extract, which is where a later use must cite it** — row
11's Gaussian length feature needing the segmental decode and its exponential one not (read at the row 11
extract this session); row 19's linear self-transition term, row 20's unbounded-history duration model,
row 18's exponential HMM shape, and the no-length-term-at-all of rows 45, 46, 47, 48, 49, 50 and 52 (read
at that carried item and not at those extracts). **The rows are named and no count is asserted.** Routed
as a datum. **No verdict.**

**(9) EXACT POSTERIOR QUANTITIES ARE AVAILABLE AND NOTHING IS PUBLISHED FROM THEM IN THIS PAPER.** Z_θ(x)
is a matrix product and p_θ(Y_i = y | x) = α_i(y|x) β_i(y|x) / Z_θ(x) is stated in terms (p. 5); every
reported result is nonetheless the error rate of a single decoded label sequence, with no rivals and no
confidence published. Beside **D-006**, **D-008** and **DP-K**, and beside row 11's finding (10) — read at
that extract this session — and rows 18's, 19's and 20's instances of the same datum, read at the progress
record's own summaries of them and NOT at their extracts. **The rows are named and no count is asserted.**
Routed to L2's detail specification. **No verdict.**

**(10) ★ THE PAPER PRINTS NO UNCERTAINTY OF ANY KIND ON ANY FIGURE, AND NO SIGNIFICANCE TEST APPEARS
ANYWHERE.** Figure 4's five rows carry no spread; Figure 3 is a scatter of per-data-set error rates with no
interval; the *"10%–20% relative error"* margin is stated in prose. **Figure 4's three column headings are
set in italic and no DATA cell of it carries emphasis of any kind; the table states no best-cell and no
significance convention, and no cell is emphasised by this reader.** The authors themselves attribute one
anomalous region of Figure 3 to *"an insufficient number of training iterations for the CRF"* rather than
to the models, which is a statement about their own protocol rather than a measurement. **The contrast with
row 15 is exact and is why the practice is recorded per row:** row 15's paper prints a bootstrap 95%
confidence interval per column on both its tables and marks per-cell significance — **read at the progress
record's own row 15 paragraph, not at that extract.** Routed to measurement design as the practice to want.
**No verdict.**

**(11) THE CROSS-PRIMARY CHECK HAD NOTHING TO RUN ON, AND THAT WAS ESTABLISHED RATHER THAN ASSUMED.** The
paper's data are synthetic sequences from randomly generated mixed-order HMMs and Penn treebank POS tagging;
its metrics are per-word error rate and out-of-vocabulary error rate. The FIGURE sweep recorded at finding
(2) found **no read row of this slice reporting on any of them**, so no figure of this paper is comparable
with any figure this slice already holds. **The same outcome rows 51's and 15's reads had to record, reached
the same way; the rows are named and no count is asserted.** **No verdict.**

**(12) THE REFERENCE LIST NAMES 25 WORKS AND NOT ONE OF THEM IS IN THIS PROJECT'S BIBLIOGRAPHY OR HAS AN
EXTRACT — ROW 15's OUTCOME REPRODUCED.** Derived at the page: seven entries begun in the left column and
eighteen in the right. They are machine-learning, speech-recognition, natural-language-processing and
numerical-methods works — Abney, Schapire & Singer 1999; Berger, Della Pietra & Della Pietra 1996; Bottou
1991; Brill 1995; Collins 2000; Collins, Schapire & Singer 2000; Darroch & Ratcliff 1972; Della Pietra,
Della Pietra & Lafferty 1997; Durbin, Eddy, Krogh & Mitchison 1998; Freitag & McCallum 2000; Freund &
Schapire 1997; Hammersley & Clifford 1971; LeCun, Bottou, Bengio & Haffner 1998; MacKay 1996; Manning &
Schütze 1999; McCallum, Freitag & Pereira 2000; Mohri 1997; Mohri 2000; Paz 1971; Punyakanok & Roth 2001;
Ratnaparkhi 1996; Rosenfeld 1997; Roth 1998; Saul & Jordan 1996; Schwartz & Austin 1993 — **checked against
`docs/research_papers/BIBLIOGRAPHY.md` (read whole this session) and against the
`reading_pass/extracts/` directory listing (read this session, on the method warning row 48's read paid
for): none is held and none has an extract, and nothing is carried out of any of them.** Recorded as the
result rather than glossed. **No verdict.**

**(13) THE PAPER'S OWN STATED MAIN LIMITATION IS TRAINING COST, AND IT IS STATED AGAINST ITS OWN METHOD.**
*"Their main current limitation is the slow convergence of the training algorithm relative to MEMMs, let
alone to HMMs"* (§7); measured at §5.2 (≈500 iterations at ≈0.2 s each against the MEMM's ≈100) and at §5.3
(CRF⁺ converging in 1,000 iterations from an MEMM⁺ start, and the same CRF from uniform *"had not converged
even after 2,000 iterations"*). A datum beside `CLAUDE.md`'s standing rule that getting the inference right
comes first and runtime speed is traded against it only once slowness has actually turned out to be a
problem — **the rule sequences the concern and does not license ignoring it**, and a fitting method's
convergence cost is a build-time cost rather than a runtime one. Routed as a datum. **No verdict.**

**(14) A DATUM FOR DP-O, WHICH IS UNDERIVED AND OPEN.** The paper states twice that the formalism is not
restricted to chains: *"CRFs also generalize easily to analogues of stochastic context-free grammars that
would be useful in such problems as RNA secondary structure prediction and natural language processing"*
(p. 2), and *"More general tree-structured random fields … will also be investigated"* (§7). **DP-O — whether
the framework commits to a hierarchical reading of harmony — is open and deliberately not foreclosed**, and
`FRAMEWORK.md` records that *"what chord follows what is inside L2's charter, and a grammar is one way to
answer it"* (lines 797–798). **This is a datum that the score form at finding (3) does not itself force a
flat chain**, and nothing more: the paper measures no grammar and this extract carries no argument for or
against DP-O. Routed to L2's detail specification as a datum. **No verdict.**

**(15) THE FEATURES ARE GIVEN AND FIXED, SO THE PAPER ANSWERS HALF OF THE CHARTER'S SENTENCE AND IS SILENT
ON THE OTHER HALF.** The L2 charter leaves to the detail specification *"how the score over candidate
readings is formed, what its terms are, what tables they read, and how any weight is fitted"*. This paper
decides **how it is formed and normalised** and **how a weight is fitted**; **what the terms are** it
explicitly leaves outside the model (*"We assume that the features f_k and g_k are given and fixed"*), naming
feature induction at §6 only as something that could be adapted. Recorded so a later citation does not read
the paper as settling the charter's whole sentence. **No verdict.**

**(16) NO FALSIFIER.** Nothing read contradicts any CHOSEN design point, and no ratified text rests on this
paper at all, so there is no structural claim of the record to correct. **No STOP of the falsifier class and
no STOP of the remedial commission §5 class fires.**

## Centrality

**CENTRAL, on a narrow ground, challengeable at the progress record.** No ratified text rests on this paper
— finding (2) establishes that at the objects — so the load-bearing-claim route to centrality is **absent**,
and the ground is the other one the original commission's §4 gives: *"any paper whose claims would carry load
in a detail specification or against a design point"*. The candidacy row admits it precisely because an L2
detail specification must decide how its score is formed and normalised, and **findings (3), (4), (5), (6),
(7) and (15) are claims such a specification would cite this paper for directly** — the score form and its
global normalisation; the label-bias argument for placing the normaliser over whole readings, with its
measurement; the convexity property that is the other end of DP-P's question from row 15's; the HMM-like
construction under which a counted table becomes a feature; the pruning warning that is the charter's own
candidate-admission condition stated in 2001; and the bound that the paper decides the form and not the
terms. **This is row 11's centrality reasoning applied to row 11's own base formalism**, and it is recorded
as such.

**The ground on which NOT CENTRAL could be argued, stated so the challenge is easy to make:** no `[FACT]`,
no ratified figure and no characterisation of any kind rests on this paper, so unlike rows 10, 11, 18, 19,
45, 48, 49, 50, 52 and 15 this read verifies nothing; **there is nothing musical in the paper at all**, and
every measured figure is on synthetic data or English part-of-speech tagging; what a detail specification
would actually instantiate is the segmental extension (row 11) and its musical instances (rows 10 and 19),
of which this paper is the common ancestor rather than the thing adopted; the record cites it nowhere, which
is row 51's situation and row 51 was graded NOT CENTRAL; and the paper prints no uncertainty on any figure.
**A second independent extraction is therefore OWED on this verdict**, and the verdict itself is
challengeable at `reading_pass/l2_slice_reading_progress.md`.

## What this extract does NOT do

It derives no specification statement. It amends no document — `FRAMEWORK.md`,
`reading_pass/candidacy_upgrades.md`, `cowork_l2_task_b_slice_derivation_2026_09_05.md`,
`reading_pass/population.md`, `docs/research_papers/BIBLIOGRAPHY.md` and
`cowork_reading_pass_findings_2026_08_31.md` are untouched; findings (1) to (16) are routed and written
nowhere else. It takes no position on whether this project's own production arm is of the shape finding (4)'s
argument reaches — that question is stated and left. It does not fetch the ICML 2001 proceedings text at the
`cs.columbia.edu` URL its bibliography row names, nor any of the twenty-five works the reference list names,
nor the Penn treebank data. It opens no code, touches no measurement tool, no corpus, no golden and nothing
under `tools/`. It writes no open-items row and allocates no decisions-register identity. It reads no other
paper and takes no decision about the order of the remaining slice. **Row 18's finding (3), row 47's two
corrected structural claims, row 45's finding (1) and row 52's corrected structural claim stay open with the
user, applied nowhere, and are neither restated as settled nor built on here; row 12 adds none to that
population.**

---

*Provenance: written 2026-09-06 by the Cowork session that booted on
`cowork_handoff_entry_one_hundred_and_thirty_five.md`, on the user's opening instruction naming row 12,
after the ordinary session-start read — `CLAUDE.md` (1,864 lines; met as the harness-delivered
project-instructions copy and verified complete at the staged object by its line count and its closing
lines, which is a departure declared in the handoff entry), `DECISIONS.md` whole (862 lines), `STATUS.md`
whole, and the derived gating answer's `★_the_live_gating_answer` block with its `gating_ids` list whole
(244 open, 219 gating, 25 non-gating). Read for this extract, at the files: the hundred-and-thirty-fifth
handoff entry whole; the hundred-and-eighteenth entry whole for its bridge-fault section;
`reading_pass/l2_slice_reading_progress.md` whole (1,952 lines); `reading_pass/candidacy_upgrades.md`
whole (row 12 at line 74); `cowork_l2_task_b_slice_derivation_2026_09_05.md` whole (row 12 at line 59);
both commissions whole; `docs/research_papers/BIBLIOGRAPHY.md` whole (row 12 at line 26);
`reading_pass/population.md` at §3's verification rows V1 to V13; the row 11 extract whole, for the form
and for the domain bound carried item (d) names; and `FRAMEWORK.md` at §4.2 (237–271), the L2 charter
(386–425), the L3 charter (426–451), the design points DP-A to DP-Q (669–817), A.3 (1133–1155), §S4 whole
(1547–1628), §S5's entanglement argument (1631–1656) and §S6 whole (1777–1885, its DP1 beginning at 1783
and its DP17 ending at 1882) — **every one of
those boundaries taken from that file's own heading list rather than from where this side happened to be
reading**. The sweeps run are enumerated at finding (2). The `reading_pass/extracts/` and
`docs/research_papers/` directory listings were both read, the first on the method warning row 48's read
paid for. The paper itself was read at the object as page images, all eight pages, with pages 2, 7 and 8
each read a second time on their own and every transcribed cell re-checked; the page count was established
by a refused request for page 20. No shell command was run on the repository or on any staged copy of it,
for content or for listings. No figure of this project's own measurement is restated (#17f, D-431); every
value above is the paper's own.*
