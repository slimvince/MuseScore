# EXTRACT — Wu, Nakamura & Yoshii, "A Variational Autoencoder for Joint Chord and Key Estimation from Audio Chromagrams" — Task B candidacy row 51, first pass, AT THE OBJECT

> **STATUS: FIRST-PASS EXTRACT, READ WHOLE AT THE OBJECT (2026-09-06).** Written under
> `cowork_reading_pass_commission_2026_08_30.md` §4, whose form the remedial commission
> (`cowork_reading_pass_remedial_commission_2026_08_31.md` §3) binds unchanged.
>
> **The grade.** All seven pages of the held PDF were read AT THE OBJECT: staged through the bridge
> and read with the file tools as page images. **No relay, no web-fetch read, no prompted
> extraction.** The document prints proceedings page numbers 500–506 and a running header, so every
> location below is given as the printed section, table or figure number together with the
> proceedings page. **The page count was established rather than assumed:** a request for page 20 was
> refused by the tool with *"PDF has 7 pages"*. **Page 5 (Table I) was read a second time on its own
> and every transcribed cell re-checked against the image.**
>
> **★ ROW 51 IS GROUP 3's SEVENTH MEMBER** ("the scoring architecture and its fitting",
> `reading_pass/candidacy_upgrades.md` line 177: rows 45, 48, 49, 50, 52, 46, 51, 15, 12, 13, 14, 16,
> 17), after rows 45, 48, 49, 50, 52 and 46.
>
> **★★ ROW 51 STANDS ON THE DOUBT DEFAULT, SO THERE IS NO VERIFICATION TARGET. THE READ IS THE
> DERIVATION'S OWN QUESTION BEING ANSWERED.** No `[FACT]`, no ratified figure, no characterisation
> and no subject in its own register row. **The question was: does this method bear on a decision
> inside L2's charter at all?** **AT THE OBJECT: YES.** The paper decides tonality and chord
> together over a music signal, which is the first and last limbs of L2's own sentence, so **this is
> NOT a placement finding of row 39's shape**: the ADMITTED verdict and the L2 placement stand
> confirmed rather than merely left standing. **The domain is AUDIO** and travels as a caveat under
> the derivation's consequence (ii).
>
> **★★ THIS READ PRODUCES NO CORRECTED STRUCTURAL CLAIM AND NO STOP OF EITHER CLASS.** What it
> produces is the doubt default's answer, **one addition candidate for DP-A's ground that the record
> does not carry** — a paper of this family that MEASURES head disagreement with a purpose-built
> metric — and data routed below. **Row 18's finding (3), row 47's two corrected structural claims,
> row 45's finding (1) and row 52's corrected structural claim all stay open with the user, are
> applied nowhere, and are neither restated as settled nor built on here.**
>
> **Where the record cites this paper, established by `Grep` of the staged tree BEFORE extracting,
> never inherited. What was searched and where, stated as a rule rather than as a tally.** Over the
> staged tree: an IDENTITY sweep — the bare words **"Nakamura"**, **"Yoshii"**, **"APSIPA"**,
> **"Tsushima"**, **"Itoyama"**, and separately the bare word **"Wu"** and the file-name stem
> **"wu_nakamura"**. Over `FRAMEWORK.md`: the BARE WORD **"2020"**. Over
> `reading_pass/population.md` and `cowork_reading_pass_findings_2026_08_31.md`: **"Nakamura"**,
> **"Yoshii"**, **"APSIPA"**, **"Wu"** — *no match in either file*. Then, AFTER the paper was read, a
> FIGURE sweep over the staged tree for this paper's own figures — **"68.97"**, **"72.51"**,
> **"76.52"**, **"79.08"**, **"74.12"**, **"77.03"**, **"79.69"**, **"79.22"**, **"81.46"**,
> **"81.67"**, **"82.05"**, **"74.94"**, **"78.31"** — and for **"chromagram"**, **"variational
> autoencoder"**, **"VAE"**, **"Isophonics"**, **"uspop"** and **"Robbie Williams"**.
> **`FRAMEWORK.md` §4.2, the L2 and L3 charters, DP-A to DP-Q, A.3, §S4 whole, the entanglement
> argument and §S6's DP1 to DP17 were READ WHOLE, not only searched**, on the method fact rows 48
> and 52 paid for.
>
> **★ WHAT THE THREE-PART RULE PRODUCED HERE, STATED PER LEG RATHER THAN AS A RATIO.** The IDENTITY
> sweep reached all three places. The FIGURE sweep reached **no citation of this paper at all** —
> **no figure of this paper is reported anywhere in the staged tree as this paper's**, which is the
> finding that no `[FACT]` rests on it. **It is stated that way, and not as "no figure of this paper
> occurs anywhere", because the sweep DID return hits**: *"77.03"* occurs twice in `CLAUDE.md` (lines
> 817 and 1077), where it is **this project's own ratified root-agreement baseline** in gate block
> (A), and it is also this paper's Table I **Key-correct cell for the multi-task semi-supervised VAE
> model**. **Two different quantities in two different documents; they are not the same figure and
> must not be read across.** *(This is the same shape as the row 46 read's "66.65" coincidence, and
> it is recorded for the same reason.)* The BARE-WORD "2020" sweep over `FRAMEWORK.md` returned
> exactly one line — 750, *"[Nápoles López, Feisthauer, Levé & Fujinaga 2020 — RELAYED.]"* — which is
> DP-K's second further ground and **not this paper**, read at its own line rather than assumed.
> **★ AND THE TRAP THE PROGRESS RECORD NAMED WAS MET AND RESOLVED AT THE LINE.** Nakamura and Yoshii
> are two of the four authors of candidacy row 25 (Tsushima, Nakamura, Itoyama & Yoshii 2017,
> ADMITTED and NOT READ), and the identity sweep duly reached row 25's places: the bibliography's
> line 39, the slice derivation's line 70, `candidacy_upgrades.md` line 87, and **`FRAMEWORK.md`
> §S6's DP15 at line 1866, *"Tsushima and colleagues find that their tree models are harder to
> optimise…"***. **Each was read at its own line and none of them is this paper.** Recorded beside
> it, because it is a fact about the record rather than about this paper: **the LIVE DP-O carries
> that same Tsushima finding UNATTRIBUTED** (lines 795–797, *"tree models are harder to optimise and
> often perform no better than latent-category sequence models of matched size"*), where the sealed
> DP15 names the authors — so a search on either author name reaches the sealed text and not the live
> one.
>
> **This extract derives no specification statement, amends no document, opens no code and writes no
> open-items row or decisions-register entry.**

## Where the record bears on this paper — three places, each read at its own line

**In this line's own tracking files** — *places 1–2*:

1. **`reading_pass/candidacy_upgrades.md` line 123** — the candidacy row: *"| 51 | Wu, Nakamura &
   Yoshii, APSIPA 2020 | ✓ | **ADMITTED, ON THE DOUBT DEFAULT** | The register names no subject
   beyond the venue; nothing in the record settles what its method does. The default is to read. |"*
2. **`cowork_l2_task_b_slice_derivation_2026_09_05.md` line 86** — the slice placement: *"| 51 | Wu,
   Nakamura & Yoshii 2020 | **L2** (doubt default) | The record names no subject; the lineage section
   places the whole lineage as DP-A evidence. Placed with the lineage on the default; reading
   decides. |"*

**In the bibliography** — *place 3*:

3. **`docs/research_papers/BIBLIOGRAPHY.md` line 70**, in the *"Neural / multi-task lineage"*
   section — *Wu, Nakamura & Yoshii, APSIPA 2020*, URL
   `https://www.apsipa.org/proceedings/2020/pdfs/0000500.pdf`, held ✓, tier **LINK**.

**Where the record does NOT bear on it, established at the objects rather than assumed:**
**`FRAMEWORK.md` names it NOWHERE** — not in the live body, not in Appendix A, not in the sealed
first-stage draft; the identity sweep returned no hit in that file and the whole reading of §4.2, the
L2 and L3 charters, DP-A to DP-Q, A.3, §S4, the entanglement argument and §S6's DP1 to DP17 reached
no passage that could be it. **`reading_pass/population.md` returns no match** on any of the four
identity terms, so no verification row is this paper's. **`cowork_reading_pass_findings_2026_08_31.md`
returns no match** either. **So no `[FACT]`, no ratified figure and no characterisation of any kind
rests on this paper**, and the only load it carries is the candidacy row's own admission of doubt.
**Rows 45, 48, 49, 50, 52 and 46 each had something in the record to check at the object; this row
had nothing, and the sweeps establish that it had nothing rather than the carried items asserting
it. That is a statement about these seven rows and nothing is inferred from it about the rest.**
**The bound:** this is a
claim about the files in this session's staged tree, which the provenance enumerates, and not about
every file in the repository.

## ★ The doubt default's own question, ANSWERED — the method IS a candidate for a decision inside L2's charter

**What had to be answered**, as the progress record's carried item (a) states it: *"what the method
DOES and whether it is a candidate for a decision inside L2's charter at all"*, with row 39's
precedent standing for the answer *no*.

**AT THE OBJECT: the answer is YES, and the paper's own title says so.**

The printed title is **"A Variational Autoencoder for Joint Chord and Key Estimation from Audio
Chromagrams"** (page 1, printed 500). The abstract's own first sentence is *"This paper describes a
deep generative approach to jointly estimating chords and keys from music signals."* §I states the
motivation in the terms L2's charter is written in: *"different musical elements are mutually
dependent… the musical key is closely related to the likelihood that a certain chord is played [18],
and chord transitions are more likely to occur on downbeats [23]"*, and the classification model is
*"a deep classification model that jointly estimates chords and keys from chroma vectors"* (§I).

**Against L2's charter sentence, limb by limb** (`FRAMEWORK.md` §5, L2, lines 388–390):

- *"what is the tonality at each moment"* — **YES**, a key label per frame, 24 classes (major and
  minor keys only).
- *"where does each harmony give way to the next"* — **NO decision of its own.** There is no
  boundary variable, no segment-length term and no segmentation output. Temporal continuity is
  imposed AFTER classification by a Viterbi pass with a hand-set self-transition probability (§III-G).
- *"which sounding notes belong to the harmony and which elaborate it"* — **NO**, and it cannot: the
  input is a chroma vector, not notes.
- *"what chord is read over each span"* — **YES**, a chord label per frame, 73 classes.

**So the method answers the tonality limb and the chord limb of L2's one entangled decision, is
silent on the boundary limb, and cannot reach the chord-tone limb at all.** That is the same
tonality-and-chord-without-segmentation shape group 1 holds, reached from the audio side, and it is
a candidate for the coupling boundary condition the L2 charter fixes. **The ADMITTED verdict and the
L2 placement therefore stand CONFIRMED at the object.** **This is not row 39's outcome and must not
be reported as one.**

## Identity — NO title to match by construction; authors, venue and year all match; the input is AUDIO

**★ THE BIBLIOGRAPHY'S ROW CARRIES NO TITLE AT ALL** — not a short title, not an ellipsis, not a
model's name: *"Wu, Nakamura & Yoshii, APSIPA 2020"*, authors, venue and year and nothing else. **So
the page-1 identity check has nothing to match on the title axis, and that is stated rather than
reported as an identity check that closed.** Rows 49 and 52 each had a row whose *"title"* was a
model's name; **this row has no title at all**, which is a different shape again.

**What page 1 does print, recorded as a fact about the held file:**

- **Title:** *"A Variational Autoencoder for Joint Chord and Key Estimation from Audio Chromagrams"*.
- **Authors:** *Yiming Wu, Eita Nakamura, and Kazuyoshi Yoshii*, Graduate School of Informatics,
  Kyoto University, Kyoto, Japan. **Three authors, in the row's own order** — the row's *"Wu,
  Nakamura & Yoshii"* matches the printed author list exactly.
- **Venue and date:** the running header prints *"Proceedings, APSIPA Annual Summit and Conference
  2020"* and *"7-10 December 2020, Auckland, New Zealand"*; the footer prints
  *"978-988-14768-8-3/20/$31.00 ©2020 APSIPA"* and *"APSIPA-ASC 2020"*. **Venue and year match the
  row.**
- **Pagination:** printed pages **500–506**, which reconciles with the bibliography row's URL
  `.../2020/pdfs/0000500.pdf`.
- **Licence:** **no CC licence line is printed anywhere**; the footer carries an ordinary
  publisher copyright line. **So the row's LINK tier is the correct conservative reading and no tier
  correction is owed** — the same direction as row 52's, and the opposite of rows 30's, 49's and 50's
  understatements.

**NO identity finding**, on the two axes that can be checked. **The title axis cannot be checked at
all, and that is itself recorded** for the bibliography reconciliation, beside rows 28's, 3's, 29's,
10's, 11's and 18's findings — the rows are named and no count is asserted.

**★ THE INPUT IS AUDIO**, established at page 1 and again at §III-A: chroma vectors *"extracted from
the music signal using a pre-trained DNN chroma extractor proposed in [28]"*, a multi-band chroma
vector of **D = 36** dimensions carrying *"the pitch class activations of lower, middle and higher
pitch ranges"*. **There are no notes anywhere in the method**, so the domain travels as a caveat
under the derivation's consequence (ii), exactly as it does for rows 3, 18, 20 and 27.

## Claims, labeled

### The model — what it is, in the paper's own terms

**[FACT — §III-A, page 2]** Three latent variables: a sequence of chord classes
**S** = {s_n}, a sequence of key classes **H** = {h_n}, and a sequence of continuous latent features
**Z** = {z_n}. **s_n ∈ {0,1}^{K_S}** and **h_n ∈ {0,1}^{K_H}** are one-hot discrete variables;
**z_n ∈ R^L** is continuous.

**[FACT — §III-A, page 2]** **The chord vocabulary is K_S = 73**: *"all possible combinations of 12
root notes with 6 types of triad chords (with shorthands maj, min, aug, sus2, sus4), and a non-chord
label"*. *(Derived as a transcription check: 12 × 6 + 1 = 73, the value the paper itself states.
**Recorded as printed and reconciled nowhere: the sentence says SIX types and prints FIVE
shorthands.** Which type is unnamed is not establishable from the held document, and nothing is
carried about it.)*

**[FACT — §III-A, page 2]** **The key vocabulary is K_H = 24**: *"major and minor keys"*. **[FACT —
§III-A]** **L = 64.**

**[FACT — §III-B, page 2, eq. (1)–(4)]** The generative model factorises as
p_θ(**X**,**S**,**H**,**Z**) = p_θ(**X**|**S**,**H**,**Z**) p(**S**) p(**H**) p(**Z**), with
p_θ(**X**|**S**,**H**,**Z**) a product of Bernoullis over the 36 chroma dimensions per frame, and
**the priors on S and H uniform categorical** — *"p(**S**) = ∏ Categorical(s_n | (1/K_S)·1_{K_S})"*
and the same for **H**. **[FACT — §III-B, page 3, eq. (5)]** p(**Z**) is the standard Gaussian.

**★ [FACT — §III-C, page 3, eq. (6)–(8)] THE VARIATIONAL POSTERIOR FACTORISES THE CHORD AND THE KEY
INTO INDEPENDENT PER-FRAME CATEGORICALS.** The paper states the assumption in terms: *"we assume
that the musical classes **S** and **H** and the latent features **Z** are conditionally independent
and the variational posterior can be decomposed as follows"*, giving
q_{α,β}(**S**,**H**,**Z**|**X**) = q_α(**S**,**H**|**X**) q_β(**Z**|**X**), and then
**q_α(**S**,**H**|**X**) = ∏_n Categorical(s_n | [π_α(**X**)_{1..73}]_n) · ∏_n Categorical(h_n |
[π_α(**X**)_{74..97}]_n)** — **one softmax over the 73 chord classes and a separate softmax over the
24 key classes, per frame, with no term coupling them and no transition term of any kind.**

**★ [FACT — Fig. 2, page 3] THE ARCHITECTURE IS ONE SHARED ENCODER WITH TWO HEADS.** Fig. 2(a), the
classification model: **X** → a bi-directional LSTM (α) → layer norm → a **97-dimensional** vector
sequence → **two softmaxes**, π_α(**X**)_{1..73} (73 dims) and π_α(**X**)_{74..97} (24 dims).
*(Derived as a transcription check: 73 + 24 = 97, the dimension the figure prints.)* Fig. 2(b), the
recognition model: **X** → BLSTM (β) → layer norm → 128 dims → μ_β(**X**) (64 dims) and σ²_β(**X**)
(64 dims). Fig. 2(c), the generative model: **S**,**H**,**Z** → BLSTM (θ) → layer norm → 36 dims →
sigmoid → ω_θ(**S**,**H**,**Z**) (36 dims).

**[FACT — §IV-A-1, page 4]** Each of the three models is a **three-layered BLSTM with layer
normalization, 128 hidden units for each layer for each direction**. q_α and q_β take a
36-dimensional vector sequence as input; p_θ takes a **161-dimensional** vector sequence as input.
*(Derived as a transcription check: K_S + K_H + L = 73 + 24 + 64 = 161, the value the paper states
at §III-B.)*

**★ [FACT — §III-G, page 4] THE TEMPORAL MODEL IS A HAND-SET SELF-TRANSITION PRIOR APPLIED AFTER
CLASSIFICATION.** *"Considering the temporal continuity of chords and keys, the optimal paths of
**S** and **H** are estimated from the posterior probabilities by using the Viterbi algorithm with
uniform transition matrices except for the diagonal elements (self-transition probabilities). In this
paper, the self-transition probabilities are set to **0.9 for chords and 0.95 for keys**."*

### The training conditions

**[FACT — §III-D, page 3, eq. (9)–(10)]** Unsupervised training maximises the variational lower
bound of log p_θ(**X**), with the expectation approximated by Monte Carlo with **I = 1** sample,
**S** and **H** sampled by the Gumbel-softmax technique and **Z** by the reparameterisation trick.

**[FACT — §III-E, page 3, eq. (11)–(12)]** Supervised training maximises the conditional
semi-marginalised log-likelihood; because the classification model's parameters α do not appear in
that bound, **a classification term log q_α(**S**,**H**|**X**) is added**, following [14].

**[FACT — §III-F, page 4, eq. (13)]** The proposed regularised training maximises the sum of the two
over an annotated set and an extensive set containing both annotated and non-annotated chroma
vectors, so that *"the generative model p_θ(**X**|**S**,**H**,**Z**) acts as a regularizer on the
classification model q_α(**S**,**H**|**X**) even under the unsupervised condition"*. **[FACT —
§III-F]** A **curriculum** is used: the classification model is trained alone first in the
non-regularised supervised manner, and only then are the three models trained jointly.

### The corpus and the protocol

**[FACT — §IV-A-3, page 4]** **224 songs from the Isophonics dataset [10] and 63 songs from the
Robbie Williams dataset [7]**, both with time-synchronised chord and key annotations. **Excluding
songs including keys other than major and minor keys (the paper's own example is the Mixolydian
scale), 222 annotated songs remain.** **100 songs from RWC-MDB-P-2001 [8] and 185 songs from
uspop2002 [2]**, plus *"the 65 songs from Isophonics and Robbie Williams that were excluded from the
annotated set"*, give **350 non-annotated songs**. *(Derived as a transcription check: 224 + 63 =
287, 287 − 222 = 65, and 100 + 185 + 65 = 350 — every figure the paragraph states reconciles.)*

**★ [FACT — §IV-A-3] THE ANNOTATED SET IS DEFINED BY DISCARDING EVERY SONG WHOSE KEY IS NOT MAJOR OR
MINOR.** The 24-class key space is not a simplification applied to the labels; it is a filter applied
to the corpus, and the songs it removes are then re-used as non-annotated training data.

**[FACT — §IV-A-1, page 4]** Adam, initial learning rate 0.001, **300 epochs**, minibatches of 8
sequences randomly picked from training data, each sequence **431 frames (20 sec)**, with the chroma
vectors and the ground-truth chords and keys *"jointly rotated by a random number for compensating
the imbalance in key classes"*.

**[FACT — §IV-A-3, page 5]** **5-fold cross-validation** on the annotated dataset. Under Supervised
and Supervised VAE, four of the five folds are training data. Under Semi-supervised VAE, the
non-annotated dataset is used as training data in addition.

**[FACT — §IV-A-3, page 5]** The DNN chroma extractor [29] was trained on the **Slakh2100 dataset
[20]**, *"consisting of music signals synthesized from MIDI data"*. Signals at 44.1 kHz, STFT over 84
frequency bins with a Hann window of 4096 points, a shifting interval of 2048 points, a resolution of
one semitone per bin; four log-spectrograms starting from different octaves stacked to a
multi-channel log-spectrogram, then fed to the neural chroma estimator. *(Derived as a transcription
check: 2048 / 44100 = 46.4 ms per frame, and 431 frames × 46.4 ms = 20.0 sec, the duration the paper
states.)*

**[FACT — §IV-A-4, page 5]** Accuracy is *"the weighed overlap rates between the estimated and
ground-truth chord and key classes"*, per song, **calculated with the `mir_eval` library [26]**, and
the overall accuracy is *"the average of the piece-wise accuracies weighed by the song lengths"*.
**[FACT]** Three named key error types are measured beside the correct rate: **perfect 5th, relative
and parallel** — *"These errors are considered more sensitive to the ambiguity of chroma vector."*

**★ [FACT — §IV-A-4, page 5, eq. (14)] THE PAPER PROPOSES A METRIC FOR HEAD DISAGREEMENT AND USES
IT.** *"In order to validate our hypothesis that the multi-task classifier learns the
musically-meaningful relations between chords and keys, we propose a metric to measure the musical
consistency between the estimated chords and keys. Specifically, the musical consistency of each song
was measured by the pitch class overlap between chords and keys"*, Consistency(**S**,**H**) = (1/N)
Σ_n Overlap(s_n, h_n), where **Overlap ∈ {0,1,2,3} because s_n represents a triad chord**. **[FACT]**
Its stated basis: *"The definition of this consistency measure is based on the simple assumption that
a chord is more likely to occur when it shares more notes with the current key. Higher consistency
indicates that the estimated keys and chords are expected to follow musical rules more often."*

### The measured results — Table I, page 5

**Transcribed from the image, every cell re-checked on a second reading of that page alone.** The
caption is *"Estimation accuracy and musical consistency of estimated chords and keys"*. Columns:
**Key (%)** — Correct, Perfect 5th, Relative, Parallel, Other; **Chord (%)** — Correct; and
**Consistency**.

| Row | Key Correct | Perfect 5th | Relative | Parallel | Other | Chord Correct | Consistency |
|---|---|---|---|---|---|---|---|
| Single-task (supervised) | 68.97 | 5.61 | 8.30 | 5.79 | 11.32 | 79.69 | 2.69 |
| Multi-task (supervised) | 72.51 | 4.57 | 7.58 | 4.16 | 11.16 | 79.22 | 2.74 |
| Single-task (supervised VAE) | 76.52 | 3.04 | **6.60** | 3.92 | 9.90 | 81.46 | 2.67 |
| Multi-task (supervised VAE) | **79.08** | **2.36** | 6.93 | **3.48** | **8.13** | **81.46** | **2.74** |
| Single-task (semi-sup. VAE) | 74.12 | 4.47 | 6.17 | 4.93 | 10.29 | 81.67 | 2.68 |
| Multi-task (semi-sup. VAE) | 77.03 | 3.20 | 7.54 | 4.11 | 8.08 | 82.05 | 2.72 |

**★ THE BOLDING IS SCOPED TO THE FIRST FOUR ROWS AND THAT IS RECORDED AS PRINTED, NOT RECONCILED.**
The emphasised cells all sit in the two Supervised-VAE rows, and **three unbolded cells in the
semi-supervised rows are better than a bolded cell in their own column**: Relative 6.17 against the
bolded 6.60, Other 8.08 against the bolded 8.13, and Chord Correct 82.05 and 81.67 against the bolded
81.46. The paper's own §IV-B-1 discusses *"the first four rows"* as the supervised comparison, which
is consistent with the bolding being scoped to that block; **the caption does not say so, and nothing
here reconciles it.**

**[FACT — §IV-A-2, page 4]** The two classification models compared are **Multi-task (proposed)** —
*"The chords **S** and the keys **H** are estimated jointly from **X** by using the unified
multi-task classifier q_α(**S**,**H**|**X**) shown in Fig. 2"* — and **Single-task** — *"The chords
**S** and the keys **H** are estimated separately from **X** by using independent single-task
classifiers q_α(**S**|**X**) and q_α(**H**|**X**) having the same architectures as q_α(**S**,**H**|**X**)
except for the output layers."* **The three training methods are Supervised, Supervised VAE
(proposed) and Semi-supervised VAE (proposed).**

**[FACT — §IV-B-1, page 5]** The authors' own reference point, with their own bound attached: *"the
best-performing method in the audio key detection task of MIREX 2019 achieved 74.94% and 78.31% on
the Isophonics and Robbie Williams datasets, respectively [12]. Although these scores cannot be
directly compared with the scores listed in Table I because different training data were used, our
method can be considered to be comparable to the state-of-the-art method."*

**[FACT — §IV-B-1, page 5]** *"the multi-task learning was proven to improve the musical consistency
between the estimated keys and chords. In contrast, although the VAE-based regularization
significantly improved the estimation accuracy, the consistency was not improved."*

**[FACT — §IV-B-2, page 6]** *"the proposed semi-supervised training positively and negatively
affected the chord and key estimation accuracies, respectively"*, with the authors' stated mechanism:
*"after training with the unknown chroma vectors, the generative model became more vulnerable to the
ambiguity in chroma vectors with respect to key classes."*

**[FACT — §IV-B-1 and §V, pages 5–6]** *"Although the deep generative model apparently did not make
effective use of key information, it was useful for regularizing the key classifier"*, and the
authors' stated possible reason: *"keys are much less informative than chords for reconstructing
chroma vectors; only a single key is often used in a song."*

### Derived here, with the sign convention stated, and read as directions only

**Multi-task minus single-task, within each training method** (the comparison §IV-A-2 says the
experiment is for):

| | Key Correct | Chord Correct | Consistency |
|---|---|---|---|
| Supervised | **+3.54** | **−0.47** | **+0.05** |
| Supervised VAE | **+2.56** | **0.00** | **+0.07** |
| Semi-supervised VAE | **+2.91** | **+0.38** | **+0.04** |

**Read as a direction only, and bounded by the fact that no table in the paper prints uncertainty of
any kind and no significance test appears anywhere (#24).**

**★ THE AUTHORS' OWN SUMMARY SENTENCE ABOUT RELATIVE-KEY ERRORS IS NOT BORNE OUT BY THEIR OWN TABLE
IN ONE OF ITS THREE PAIRS.** §IV-B-1 states *"we found that the multi-task classifier tended to make
more relative key errors than the single-task classifier."* Multi minus single on the Relative
column: **−0.72** (supervised), **+0.33** (supervised VAE), **+1.37** (semi-supervised VAE). **The
sentence holds in two pairs and runs the other way in the supervised pair**, which is the one the
same paragraph opens by discussing. Derived with the sign convention stated, and recorded as a bound
on what that sentence can be cited for — the same shape as row 49's finding about its own summary
sentence.

**A second derived reading of the same paragraph:** *"Comparing the supervised single-task and
multi-task classifiers, we found little improvement in chord estimation, but a large improvement in
key estimation."* **The chord figure does not improve at all in that pair; it falls, 79.69 → 79.22.**
Recorded as printed.

**The authors' "more than 10%" claim, checked:** *"the integration of the multi-task and VAE
strategies improved key estimation by more than 10%, compared to the single-task key classifier"* —
79.08 − 68.97 = **10.11 points**. The claim is a **point** difference, not a relative one (relative
would be 14.7 %); read either way it holds, and the point reading is what the sentence's arithmetic
supports.

## Coupling facts (mandatory)

**What the method ASSUMES about its upstream.**
- A **36-dimensional multi-band chroma vector per frame**, produced by a separately-trained neural
  chroma extractor from a 44.1 kHz audio signal. **No notes, no spelling, no voices, no meter, no
  bar line, no downbeat** — the downbeat is named in §I as a known dependency and is not used.
- **A fixed frame grid of about 46 ms**, derived from the STFT hop and not from the music.
- For TRAINING: time-synchronised chord and key annotations, from published corpora; and, for the
  semi-supervised condition, an additional set of unannotated audio.
- Nothing at test time beyond the chroma vectors: neither model is given boundaries, a chord
  sequence, or a key.

**What it HANDS downstream.**
- **A chord label per frame** out of 73 (12 roots × 6 triad types, plus a non-chord label) and **a
  key label per frame** out of 24 (major and minor only), both after the Viterbi smoothing pass.
- **NO segmentation decision, no boundary variable and no length term** — the only temporal structure
  is the hand-set self-transition probability inside the Viterbi pass, which is a geometric
  (exponential) length shape applied to an already-decided per-frame posterior.
- **No degree, no quality beyond the six triad types, no inversion, no bass, no root as its own
  field, no harmonic rhythm, no cadence, no chord-tone assignment, no figured bass.**
- **No rivals, no confidence and no posterior published** — a full per-frame categorical posterior
  over both label sets exists inside the method and is consumed only by Viterbi; **nothing is
  published from it.** The same shape rows 11, 18, 19, 20, 45, 47 and 50 record at their own
  extracts, beside D-006, D-008 and DP-K; **the rows are named and no count is asserted.**
- **A consistency figure per song** — the pitch-class overlap between the estimated chord and the
  estimated key, which is a published measurement of head agreement and not an analysis output.

**Its own STATED SCOPE and limits.**
- **Domain:** Western popular music audio — Isophonics, Robbie Williams, RWC-MDB-P-2001, uspop2002.
- **Decision scope:** the chord and the key per frame. **Not** where the harmony changes, not which
  notes are chord tones, not the degree, and not any key outside major and minor.
- **Fitting:** 5-fold cross-validation on the annotated set. **The stopping rule is a fixed 300
  epochs, run *"to ensure convergence"*, and not a validation criterion**; **no validation split, no
  hyperparameter search and no test set distinct from the cross-validation folds is described**; the
  self-transition probabilities are hand-set and their derivation is not stated; **no uncertainty is printed on any figure and no
  significance test appears anywhere (#24).**
- **The authors' own bounds:** the MIREX comparison *"cannot be directly compared"*; the generative
  model *"apparently did not make effective use of key information"*; the semi-supervised training
  *"negatively affected"* key accuracy; and the closing sentence of §V calls the work *"a pioneering
  attempt"* whose generative model still needs improving.

## What an L2 detail specification could adopt, adapt, or must argue against

- **★ ADOPT-OR-ARGUE — A MEASURED INSTANCE OF THE COUPLING BOUNDARY CONDITION IN A THIRD SHAPE THAT
  IS NEITHER A COST NOR A VETO.** The L2 charter fixes that *"the coupling between tonality and chord
  is a cost, never a veto"* (`FRAMEWORK.md` lines 413–420). **This paper's coupling is neither.** Its
  variational posterior states the chord and the key to be conditionally independent given the audio
  (eq. 7), its two Viterbi passes carry separate transition matrices, and **the only place the two
  questions meet is the shared encoder's representation.** So the whole coupling is carried by
  parameter sharing, with **no term in any objective that scores a chord against a key**. **The
  charter's own sentence names two shapes** — *"a soft distance inside a scored path, against a hard
  compatibility constraint"* — **and this is a third**, which the charter's ruling that the coupling
  must be a cost and never a veto does not reach, because there is no coupling term to be either.
  The paper measures what the sharing buys: +2.56 to +3.54 points of key accuracy, −0.47 to +0.38 of chord accuracy,
  and +0.04 to +0.07 of its own consistency measure. Routed to L2's detail specification and to the
  findings surface's DP-B block; **applied nowhere.**
- **★ ADDITION CANDIDATE FOR DP-A's GROUND — A PAPER OF THIS FAMILY THAT MEASURES HEAD DISAGREEMENT
  WITH A PURPOSE-BUILT METRIC, WHICH THE RECORD DOES NOT CARRY.** DP-A's live ground (lines 674–677)
  says *"the papers measure what happens when they are allowed to disagree"*, and §S4(a) says the
  same. **Row 45's finding (1) established that the paper DP-A quotes measures nothing about it**, and
  **row 46 established that its own composite is a conjunction which cannot separate a wrong head
  from heads contradicting each other.** **This paper does measure it**, deliberately and with a
  metric built for the purpose (§IV-A-4, eq. 14), and reports that the joint classifier improves it.
  **This does not correct DP-A** — DP-A's attribution is row 45's open finding and is untouched here —
  **and it does not support DP-A's chosen "no" either**: the measurement runs the other way, showing
  that sharing an encoder REDUCES head disagreement rather than showing that dividing the deciding
  produces it. **It is offered as a ground DP-A's defense could cite and currently does not, with its
  domain and its bounds attached.** Routed to the findings surface's DP-A block and to the user;
  **applied nowhere, and no verdict is proposed.**
- **★ MUST ARGUE AGAINST — SEPARATE PER-FRAME HEADS WITH NO JOINT TERM, THE DIVIDED DECIDING IN ITS
  purest measured form.** The classification model is exactly the shape §S4(a) describes for the
  symbolic lineage — one shared encoder, heads decided in one forward pass, nothing enforcing
  consistency — reached independently in the audio domain and with the two degrees of division
  measured against each other. **What a detail specification arguing DP-A's chosen "no" gets from this
  paper is, as at row 46, evidence about SHARING and not about DIVIDING**: the comparison is a shared
  encoder against two independent networks, and **nothing in the paper measures the cost of splitting
  the answer against not splitting it.** **Rows 46 and 51 both have that structure; that is a
  statement about those two rows and nothing is inferred from it about any other.**
- **★ MUST ARGUE AGAINST — THE FIXED FRAME GRID, HERE AN ACOUSTIC ONE.** Every boundary this model
  can imply is a 46 ms STFT hop, against the L1 charter's change-point grid and the L2 charter's
  *"boundaries are a subset of L1's change points"*. Row 46's grid was a notated subdivision; **this
  one is not derived from the music at all.** The slice already holds the fixed-grid shape at rows 18,
  19, 20, 47 and 46 (each read at its own extract); the rows are named and no count is asserted.
- **★ THE LENGTH-TERM QUESTION: THIS IS ROW 50's SHAPE, REACHED INDEPENDENTLY AND APPLIED TO BOTH
  AXES — NOT A NEW ONE, AND NO NEW COUNT IS ASSERTED.** The progress record's carried item (g) lists
  linear/exponential needing no segmental decode, Gaussian needing it, unbounded-history needing an
  approximate decode, and no length term at all, and records **row 50 as adding a first-order
  self-transition bonus decoded by Viterbi AFTER the labelling on the tonality axis.** **This paper's
  is the same mechanism on BOTH axes** — a hand-set self-transition probability inside a post-hoc
  Viterbi pass, **0.9 for chords and 0.95 for keys**, with no derivation stated for either value and
  no experiment measuring their effect. By row 11's §2.3 test it is the shape a segmental decode does
  no work for; it is applied **after the classifier rather than inside any objective**, so it cannot
  influence what the classifier learns. **What is new here is only that both axes carry it and that
  both values are hand-set and undefended**; row 50's own cost was declared negligible with no figure,
  and this paper publishes no figure for its two values either. Routed to L2's detail specification
  beside D-004; **the rows are named and no count is asserted.**
- **A DATUM ON THE LABEL SPACE, BESIDE THE CANDIDATE-ADMISSION BOUNDARY CONDITION.** The chord
  vocabulary carries an explicit **non-chord label**, beside row 18's `X`, row 47's *'others'* and
  *'no-chord'* and row 46's *'other'*; **the rows are named and no count is asserted.**
  **And the key vocabulary is closed to major and minor by discarding the corpus's other songs**,
  which is an admission rule applied to the DATA rather than to the search — the mirror of the
  question the L2 charter reserves to its own specification. Routed to L2's detail specification.
- **A DATUM FOR MEASUREMENT DESIGN — A PUBLISHED CROSS-HEAD CONSISTENCY METRIC WITH ITS OWN STATED
  ASSUMPTION.** Overlap ∈ {0,1,2,3}, defined only because the chord is a triad, and grounded on *"the
  simple assumption that a chord is more likely to occur when it shares more notes with the current
  key"*. **It cannot separate a chord that is wrong from a chord that is right and chromatic**, and
  the paper does not claim it can. Routed to measurement design as a shape, with that bound attached.
- **A DATUM ON THE TRAINING-DATA PROVENANCE.** The chroma extractor the whole system depends on was
  trained on **synthesised audio from MIDI** (Slakh2100), and is then applied to real recordings.
  Recorded as printed; nothing is carried from it.

## ★ Findings, routed and not applied

**(1) ★ THE DOUBT DEFAULT'S QUESTION IS ANSWERED AND THE ANSWER IS THAT THE ROW BELONGS WHERE IT
SITS.** The method decides tonality and chord together over a music signal — the first and last limbs
of L2's charter sentence — so it is a candidate for a decision inside L2's charter and the ADMITTED
verdict and the L2 placement stand **confirmed at the object** rather than merely left standing.
**This is not row 39's outcome.** Two precisions travel with it, and both are about the record's
reasons rather than about its verdicts: the candidacy row's *"nothing in the record settles what its
method does"* is **exactly right** — the sweeps establish that `FRAMEWORK.md`, `population.md` and the
findings surface name it nowhere — and the slice derivation's *"the lineage section places the whole
lineage as DP-A evidence"* is **a placement this paper turns out to fit for a different reason than
the lineage's other members**: it is not a Roman-numeral analyser and predicts no degree, quality or
inversion, so **§S4(a)'s universal does not reach it and nothing here bears on that sentence in
either direction.** Routed to the user; **nothing is amended.**

**(2) ★ AN ADDITION CANDIDATE FOR DP-A's GROUND, STATED AS A CANDIDATE AND NOT AS A CORRECTION.**
The record claims twice — DP-A's live text at lines 674–677 and §S4(a) at lines 1557–1562 — that the
multi-task papers *"measure what happens when they are allowed to disagree"*. Row 45's read
established that the paper DP-A quotes for it measures nothing of the kind; row 46's established that
the lineage's founding paper measures a conjunction, which cannot separate a wrong head from
disagreeing heads. **This paper measures head agreement directly, with a metric proposed for that
purpose, and reports that joint estimation improves it.** **What it does NOT do is support DP-A's
chosen "no"**: the improvement is the joint model's, so the measurement is evidence that a shared
representation reduces disagreement, not evidence that dividing the deciding causes it. **Recorded
BESIDE row 45's finding (1) and row 46's finding, and MERGED WITH NEITHER.** Routed to the findings
surface's DP-A block and to the user; **applied nowhere, and no verdict is proposed.**

**(3) A SECOND AUDIO MEASUREMENT OF THE COST OF SEPARATING TONALITY FROM CHORD, WITH A DIFFERENT SIGN
ON THE CHORD AXIS.** DP-B's recorded ground and §4.2's *"Chord ↔ tonality"* bullet both rest on one
measurement — *"about 2 points of chord accuracy and about 5 points of tonality accuracy over 174
pieces"*, which row 27's read verified at its own object as 1.8 and 4.6. **This paper is a second
audio study of the same comparison**, on 222 songs, and it measures **+2.56 to +3.54 points of key
accuracy and −0.47 to +0.38 points of chord accuracy** for joint over separate. **The tonality side
agrees in direction and is smaller; the chord side does not reproduce — in the supervised pair it runs
the other way.** **No contradiction is asserted between the two studies**: they differ in corpus, in
input representation, in chord vocabulary and in what "joint" means — Rocher's coupling is a term in a
scored path, this one's is parameter sharing with no coupling term at all — **and that difference is
itself the point, because it is the charter's own boundary condition being varied.** Routed to the
findings surface's DP-B block and to L2's detail specification, **recorded beside DP-B's existing
ground and merged with it nowhere**; applied nowhere.

**(4) A PUBLISHED INSTANCE OF SEMI-SUPERVISED TRAINING HELPING ONE AXIS AND HURTING THE OTHER, WITH
THE AUTHORS' OWN MECHANISM.** Adding 350 unannotated songs moved chord accuracy up (+0.59 multi-task,
+0.21 single-task, against the supervised VAE rows) and key accuracy down (−2.05 and −2.40), with the
authors' stated reason that the generative model *"became more vulnerable to the ambiguity in chroma
vectors with respect to key classes"*. Derived with the sign convention stated; read as a direction
only, no uncertainty being printed. **A datum for L2's fitting question (DP-P) and for measurement
design**, beside D-525's staged fit and rows 10's, 11's and 19's fitting findings; routed, applied
nowhere.

**(5) THE AUTHORS' OWN SUMMARY SENTENCE IS NOT BORNE OUT BY THEIR OWN TABLE IN ONE OF THREE PAIRS**
(the relative-key-error claim, derived above), and **one paragraph describes a fall in chord accuracy
as "little improvement"**. Recorded as bounds on what those sentences can be cited for, in the shape
row 49's read used; nothing is carried from either sentence beyond what the table supports.

**(6) A NUMERIC COINCIDENCE RECORDED SO NOBODY MERGES THE TWO.** *"77.03"* is this paper's Table I
Key-correct cell for the multi-task semi-supervised VAE model, and it is also **this project's own
ratified root-agreement baseline** at `CLAUDE.md` lines 817 and 1077. **Two different quantities in
two different documents.** *(The same shape as the row 46 read's "66.65"; recorded for the same
reason and merged with it nowhere.)*

**(7) ★ A SECOND AUTHOR GROUP REPEATS THE CHARACTERISATION OF THE HARMONY TRANSFORMER THAT ROW 47's
READ CORRECTED AT THE OBJECT.** §II, page 2, states: *"The recently proposed harmony transformer [5]
jointly estimates chord sequence and chord transition positions."* Reference [5] is *T. Chen and L.
Su, "Harmony transformer: Incorporating chord segmentation into harmony recognition", ISMIR 2019* —
**candidacy row 47's first paper, read whole at the object by the eleventh session**, whose extract
records that **the training is joint and the deciding is sequential**: the chord-change decision is
committed by a hard threshold at 0.5 and the label decision is then taken conditioned on it. **So a
second, independent author group describes that system the way row 47's read established the system
itself does not work.** **Nothing is carried from this sentence about the Harmony Transformer** — row
47's own object read governs — and **row 47's finding (1) is not restated as settled, applied or
built on here.** Recorded as a datum about how that system is described in the literature, routed to
the findings surface's DP-C block; **applied nowhere.**

**(8) TWO FURTHER DATA FROM §II's RELATED WORK, EACH RECORDED AS PRINTED AND NEITHER FETCHED.**
*(a)* *"A DNN classifier proposed by Mcfee and Bello [22] jointly estimates root notes, bass notes,
and chord tones as well as chord classes"* — **a multi-task chord model carrying a CHORD-TONE head,
in the audio domain**, which is DP-D's excluded rival's shape in a multi-task setting; reference [22]
is *McFee & Bello, "Structured training for large-vocabulary chord recognition", ISMIR 2017*, **which
the bibliography does not hold and for which `reading_pass/extracts/` holds no extract**, both
checked at their own listings this session. *(b)* *"joint estimation of keys and chords is shown to
be beneficial for some musical analysis tasks like music segmentation with proper probabilistic
formulation [25]"* — reference [25] is *Pauwels & Peeters, "Segmenting music through the joint
estimation of keys, chords and structural boundaries", ACM MM 2013, pp. 741–744*, **a published
method that decides tonality, chord AND structural boundary together — all three limbs of L2's
charter sentence that this paper's own method does not reach — and it is neither held nor in the
bibliography**, checked at both objects. **Nothing is carried out of either work**; both are routed
to the bibliography reconciliation and to L2's detail specification as named-but-unread candidates.

**(9) AN INTERNAL INCONSISTENCY IN THE PAPER'S OWN CITATION OF ITS CHROMA EXTRACTOR, RECORDED AS
PRINTED AND RECONCILED NOWHERE.** §III-A (page 2) says the chroma vectors *"are extracted from the
music signal using a pre-trained DNN chroma extractor proposed in [28]"*, where [28] is *Wu & Li,
"Automatic audio chord recognition with MIDI-trained deep feature and BLSTM-CRF sequence decoding
model", IEEE TASLP 27(2):355–366, 2019*. §IV-A-3 (page 5) says *"The DNN-based chroma extractor [29]
used for obtaining **X** was trained with Slakh2100 dataset [20]"*, where [29] is *Wu, Carsault,
Nakamura & Yoshii, arXiv:2005.07091, 2020*. **The paper names two different references for the one
component every figure in it depends on.** Recorded as printed; **the paper reconciles them nowhere
and neither does this extract.** A datum for measurement design about provenance of a load-bearing
front end.

**(10) THE BIBLIOGRAPHY ROW CARRIES NO TITLE, SO A TITLE MATCH IS IMPOSSIBLE BY CONSTRUCTION.** The
printed title is recorded above as a fact about the held file; authors, venue, year and pagination all
match, and the printed copyright line carries **no CC licence**, so the row's LINK tier is correct.
Routed to the bibliography reconciliation as the row's own defect — a register row with no title —
beside the identity findings of rows 28, 3, 29, 10, 11 and 18; the rows are named and no count is
asserted.

## Centrality — NOT CENTRAL, and the ground on which CENTRAL could be argued is stated in full

**NOT CENTRAL.** **No ratified text names this paper, no `[FACT]` rests on it, no figure of it is
reported anywhere in the staged tree, and `population.md`'s thirteen verification rows do not name
it** — all four established at the objects by the sweeps and the whole reading recorded in the banner,
not inherited. Nothing in a BLSTM-over-chroma variational autoencoder would be adopted: the input is
audio, the grid is acoustic, the label space is 73 triads and 24 major/minor keys, and the method
decides no segmentation, no degree, no inversion, no bass and no chord-tone assignment. Every figure
is a single run with no uncertainty of any kind and no significance test.

**The ground on which CENTRAL could be argued, stated in full so the verdict is challengeable
here:** the paper **measures cross-head consistency with a metric built for it**, which is the exact
quantity DP-A's live ground claims the multi-task papers measure and which **rows 45's and 46's reads
established their own papers do not**; a detail specification defending DP-A's chosen "no" would have
to meet it, if only to say that its measurement runs the other way. **Whether any other read row of
this slice carries such a metric is NOT established here** — that would need those rows' own extracts
read, which this session did not do — **so this is a statement about rows 45, 46 and 51 and about no
others.** It is also an instance of a tonality-and-chord coupling carried entirely by parameter
sharing, which is a shape the L2 charter's coupling sentence does not contemplate. **A reader who
takes either of those as load-bearing should read the verdict as CENTRAL and the second extraction as
OWED; the choice is recorded here so it can be made.**

**A SECOND INDEPENDENT EXTRACTION IS THEREFORE NOT OWED** by this verdict, and the verdict is
challengeable at the progress record.

## What this extract does NOT do

It amends no document. It moves no verdict in `reading_pass/candidacy_upgrades.md`, no placement in
the slice derivation, no design point in `FRAMEWORK.md` — live or sealed — no row of
`reading_pass/population.md` and nothing in `cowork_reading_pass_findings_2026_08_31.md`. It writes no
open-items row and no decisions-register entry. It applies, restates as settled, or builds on none of
row 18's finding (3), row 47's two corrected structural claims, row 45's finding (1) or row 52's
corrected structural claim. It lifts no gate: Ruling 1 of
`cowork_rulings_2026_09_05_l2_boot_list_sitting.md` keeps *no derivation before L2's slice of Task B is
read*, and 14 of the 36 rows are still unopened after this row. It fetches nothing: the APSIPA
proceedings deposit at the URL the bibliography names was not re-fetched beyond the held file; the
four audio corpora (Isophonics, Robbie Williams, RWC-MDB-P-2001, uspop2002), the Slakh2100 dataset and
the `mir_eval` library were not obtained; and every work the paper cites — among them **Wu, Carsault,
Nakamura & Yoshii 2020 (its reference [29], the VAE chord-estimation method this paper extends, an
arXiv preprint which is NOT HELD and for which `reading_pass/extracts/` holds no extract, both checked
at their own listings this session)**, **Chen & Su 2019 (its reference [5], which is candidacy row 47
and IS read, at its own extract)**, **Mauch & Dixon 2010 (its reference [21], which is candidacy row
44, ADMITTED and unreadable from here)**, **Krumhansl, *Cognitive Foundations of Musical Pitch*,
Oxford University Press — its reference [18], printed here with the year **2001**, where candidacy
row 9 and the R-8 gap name that book at **1990**; whether the two are the same work in different
printings is NOT establishable from the held document, nothing is carried out of either, and the
divergence is recorded as printed for the bibliography reconciliation**, together with Ba, Kiros & Hinton 2016, Berenzweig et al. 2004,
Bittner et al. 2017, Böck, Davies & Knees 2019, Gershman & Goodman 2014, Di Giorgi et al. 2013, Goto
et al. 2002, Graves, Jaitly & Mohamed 2013, Harte 2010, Jang, Gu & Poole 2017, Jiang, Xia & Carlton
2019, Kingma & Ba 2015, Kingma et al. 2014, Kingma & Welling 2014, Korzeniowski & Widmer 2016 and
2018, Lee & Slaney 2008, Manilow et al. 2019, McFee & Bello 2017, Papadopoulos & Peeters 2011, Pauwels
et al. 2019, Pauwels & Peeters 2013, Raffel et al. 2014, Wu, Chen & Su 2019, and Wu & Li 2019 — **were
not fetched and are not read; only the held file was.**

---

*Provenance: Cowork, 2026-09-06, the eighteenth reading session of Task B's L2 slice, at tip
`911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb` (both ref files read with the file tools). After the
ordinary session-start read (`CLAUDE.md` whole, `DECISIONS.md` whole, `STATUS.md` whole, and the
derived gating answer's `★_the_live_gating_answer` block with its `gating_ids` list whole), the
hundred-and-thirty-third handoff entry whole, the hundred-and-eighteenth entry whole for its
bridge-fault section, `reading_pass/l2_slice_reading_progress.md` whole,
`reading_pass/candidacy_upgrades.md` whole, both commissions whole,
`cowork_l2_task_b_slice_derivation_2026_09_05.md` whole, `docs/research_papers/BIBLIOGRAPHY.md` whole,
the row 46 extract at its heading list, banner, record-footprint, coupling facts, adopt-or-argue,
findings, centrality and closing sections for the form, `FRAMEWORK.md` at §4.2 (237–271), at the L2
and L3 charters (386–430), at the design points DP-A to DP-Q (669–818), at A.3 (1133–1155), at §S4
whole (1547–1626), at §S5's entanglement argument (1631–1656) and at §S6's DP1 to DP17 (1777–1883),
and `reading_pass/population.md` and `cowork_reading_pass_findings_2026_08_31.md` searched for the
identity terms. The `reading_pass/extracts/` and `docs/research_papers/` directory listings were both
read, the first on the method warning row 48's read paid for. Every file was read with the file tools
from bridge-staged copies; no shell read this repository or any staged copy of it. No count of this
project's own measurement is restated (#17f, D-431).*
