# EXTRACT — Ju, Condit-Schultz, Arthur & Fujinaga, "Non-chord Tone Identification Using Deep Neural Networks" — Task B candidacy row 35, first pass, AT THE OBJECT

> **STATUS: FIRST-PASS EXTRACT, READ WHOLE AT THE OBJECT (2026-09-11).** Written under
> `cowork_reading_pass_commission_2026_08_30.md` §4, whose form the remedial commission
> (`cowork_reading_pass_remedial_commission_2026_08_31.md` §3) binds unchanged.
>
> **The grade.** Both pages of the held PDF were read AT THE OBJECT: staged through the bridge and read
> with the file tools as page images, pages 1–2 in one request, then page 2 (Table 1, Table 2, §4 and the
> references) a second time on its own with every transcribed cell re-checked. **The page count was
> established by the first request itself: pages 1–4 were asked for and the tool returned two, which is
> therefore the count.**
>
> **★ FACT-CHECKED WHOLE AGAINST THE PAPER AND THE CITED LINES ON 2026-09-11, AT THE USER'S QUESTION,
> AFTER THE FIRST LANDING — and the check found defects that HAD LANDED, corrected at the sentence and
> named here so the corrected copy can be told from the first:** (i) the banner said an after-read figure
> sweep on the paper's other cells had been run before extracting — it had not; it was run at this
> fact-check, with the result now stated; (ii) the banner listed among the swept files four that were
> staged AFTER the identity sweep and only searched for D-527's figure (`ARCHITECTURE.md`,
> `cowork_joint_estimator_architecture.md`, `decisions/group_A.md`, row 17's extract), and omitted that the
> progress record was excluded from the identity and figure sweeps by the search's own filter; (iii) the
> banner said D-527's home passage was read at lines 146–167 — it was searched, not read; (iv) the *Arthur*
> hits at the bibliography and candidacy rows of row 53 (TAVERN) were omitted from the list of what the
> sweep reached; (v) three citations into the progress record carried line numbers from the copy read at
> boot, which this sitting's own edits then moved; (vi) one inference (a sigmoid output) was written as if
> the paper stated it. Every quotation of the paper was re-read against the page images and every one
> holds; every transcribed cell holds. The check is this side's own and is necessary, not sufficient. **No relay, no web-fetch read, no prompted extraction.** The held document
> prints NO page numbers, so every location below is the printed section, table or figure number, with the
> held file's own page order written as *"page n of 2"* where a location needs one.
>
> **Why this paper and its place in L2's slice.** It is row 35 of `reading_pass/candidacy_upgrades.md`
> (line 97, the file read whole this sitting), **ADMITTED outright** — not on the doubt default — with the
> reason *"**The excluded rival of DP-D**, and V11's primary. The criterion admits a method a design point
> must argue against; DP-D's exclusion ground is about this method's shape."*
> `cowork_l2_task_b_slice_derivation_2026_09_05.md` §4 (line 77, read at the line) places it in L2's
> slice: *"DP-D's excluded rival; DP-D is 'part of L2's one decision'."* It is the first member of group 4
> of the proposed reading order (*"What L2 must publish that nothing in the read set produces"*, rows 35,
> 36, 57, 58 at `candidacy_upgrades.md` line 178); row 36 is L3's slice, so under L2's gate group 4 holds
> rows 35, 57 and 58, and **this read opens group 4.**
>
> **Where the record bears on this paper, established by search and by reading BEFORE extracting, not
> inherited.** An identity sweep with `Grep`, run BEFORE the read, over the repository files staged into
> this session at that moment — `CLAUDE.md`, `STATUS.md`, `DECISIONS.md`, `FRAMEWORK.md`,
> `reading_pass/population.md`, `cowork_reading_pass_findings_2026_08_31.md`,
> `reading_pass/candidacy_upgrades.md`, the L2 slice derivation, `docs/research_papers/BIBLIOGRAPHY.md`,
> the hundred-and-eighteenth, hundred-and-fifty-second and hundred-and-fifty-fourth handoff entries and the
> gating-answer artifact, **with `reading_pass/l2_slice_reading_progress.md` EXCLUDED by the search's own
> filter (it was read whole instead)** — on the bare words *Condit-Schultz*, *Arthur*, *Fujinaga*, every
> hit read at its line; a second sweep over `FRAMEWORK.md`, the findings surface, `population.md` and the
> slice derivation only, on *non-chord tone* / *non-chord-tone* / *nonchord* / *NCT*, *elaboration
> detector*, the author forms *Ju et al*, *Ju,* and *Ju 2017*, and the bare year *2017* (the two-letter
> string *Ju* on its own was NOT swept, the block's trap being exactly that it matches inside ordinary
> words), every hit read at its line; and a sweep on *chord tone* / *chord-tone* over `FRAMEWORK.md`, every
> hit read at its line. **Four files staged LATER were searched only for D-527's figure and this paper's
> authors — `ARCHITECTURE.md` (on *28 %*, *cleaner*, *72.19*, *Ju et al*, *Ju,*, *Condit-Schultz*),
> `cowork_joint_estimator_architecture.md` (on *28 %*, *Ju*, *Condit*, *cleaner*, *non-chord*),
> `decisions/group_A.md` (for D-527 only) and row 17's extract (not searched at all, read whole for its
> form).** **A FIGURE sweep run BEFORE the read on *.72*, *72.19*, *7.68* and *"plausible analytical
> choices"* over the first population above; and a figure sweep on *86.02*, *63.14*, *3.35*, *10.81*, *140
> Bach*, *salami*, *92%* and *8%* run at the fact-check AFTER the first landing — NOT before extracting, as
> this banner first claimed — over everything staged by then, finding only the record's own V11 citations
> and this side's own files (the *8%* pattern's other hits being inside *83.8%* and *68.8%*).** **The
> record bears on this paper at these places and, within the files so searched, no others:** its candidacy row
> (line 97); its slice-derivation row (line 77); its bibliography row (line 49); `reading_pass/population.md`
> V11 (line 112); `FRAMEWORK.md` DP-D (lines 704 and 709–710) and DP4 (lines 1808 and 1814–1815), which
> cite it by figure and by *"the 2017 chord-tone network"* and never by name; `FRAMEWORK.md` §14.2 line
> 1033 (*"A first-running elaboration detector. Excluded at DP-D."* — by mechanism, not by name); and the
> findings surface at line 249 (*"Same direction as V11's F .72 …"*) and line 615 (V11's verification
> row). Every other hit names another paper or this project's own vocabulary: the *Fujinaga* hits are rows
> 17, 36, 48 and 57 at their own rows and `FRAMEWORK.md` line 750's Nápoles López, Feisthauer, Levé &
> Fujinaga 2020; the *Arthur* hits outside V11 are row 53's own rows (TAVERN — `BIBLIOGRAPHY.md` line 77,
> `candidacy_upgrades.md` line 136) and population row 14 — Hu & Arthur 2021, a different paper by one
> shared author (`population.md` lines 75 and 147–150, the findings surface lines 245–249, 552 and 670),
> which is also where the *NCT* hits outside V11 fall; the *2017* hits in the four files it was run over are
> row 25's row, the findings surface's 2017 MIREX remark (line 317), `population.md` line 113 (V12, Koops
> 2017) and `FRAMEWORK.md` line 1808 itself; and every *chord-tone* hit in `FRAMEWORK.md` outside DP-D and
> DP4 is the record's own publication vocabulary or row 52's paper (lines 249 and 1638). **The WHOLE
> READINGS the progress record's block (d‴) names were performed:** DP-D (lines 703–710), §S6's DP4
> (1807–1815) and §S6 whole (1777–1884), §4.2 whole (237–271, the elaboration ↔ chord bullet at 245–252),
> the L2 charter whole including its *Publishes* list (386–425), A.3 whole (1133–1155, its row 2 at 1141),
> §S4 whole including (a) (1547–1627), the entanglement argument (1629–1656), and **D-527 at
> `decisions/group_A.md` lines 601–636; its home passage in `cowork_joint_estimator_architecture.md` was
> SEARCHED (hits at lines 148, 161 and 164) and not read.**
>
> **So row 35 HAS A VERIFICATION TARGET, and it is a CHOSEN design point's exclusion ground** — DP-D
> (chosen: *part of L2's one decision*) excludes this paper's method on the ground *"its own authors report
> F .72 with many 'errors' that are plausible analytical choices, and a first-running detector's mistakes
> are unrecoverable downstream"*; DP4 carries the same ground in the sealed draft; V11 is the pass's
> verification of the figure and the quotation. **This is row 15's shape and stronger** (row 15's `[FACT]`
> defends DP-P, which the progress record's row 15 states is not a chosen point — §9's DP-P itself was
> not read this sitting; this one defends a chosen point). The read is therefore a
> VERIFICATION first, and the remedial commission §5 makes a corrected structural claim here a STOP.
> **The result, stated at the top so it is not missed: the figure and the quotation are CONFIRMED at the
> object; ONE precision on the SCOPE of the quotation is graded a precision and not a correction, with the
> reading under which it would be a correction stated in full at finding (4) so the user can overrule it;
> no STOP fires.**
>
> **This extract derives no specification statement, amends no document, opens no code and writes no
> open-items row or decisions-register entry.**

## Identity — a FULL title match; ONE venue printed of the two the bibliography names; a CC licence printed that the LINK tier understates; the input is SYMBOLIC

Printed on page 1 of 2: title **"NON-CHORD TONE IDENTIFICATION USING DEEP NEURAL NETWORKS"**; authors
**Yaolong Ju, Nathaniel Condit-Schultz, Claire Arthur, Ichiro Fujinaga**, Centre for Interdisciplinary
Research in Music Media and Technology (CIRMMT), Schulich School of Music, McGill University, Montréal,
Canada, e-mail `{yaolong.ju, nathaniel.condit-schultz, claire.arthur, ichiro.fujinaga}@mcgill.ca`. The
foot of page 1's left column prints a Creative Commons icon and: *"© Yaolong Ju, Nathaniel Condit-Schultz,
Claire Arthur, Ichiro Fujinaga. Licensed under a Creative Commons Attribution 4.0 International License (CC
BY 4.0). Attribution: Yaolong Ju, Nathaniel Condit-Schultz, Claire Arthur, Ichiro Fujinaga. "Non-chord
Tone Identification Using Deep Neural Networks", Extended abstracts for the Late-Breaking Demo Session of
the 18th International Society for Music Information Retrieval Conference, Suzhou, China, 2017."*

**The bibliography's row** (`docs/research_papers/BIBLIOGRAPHY.md` line 49, read at the line) names *Ju,
Condit-Schultz, Arthur & Fujinaga, "Non-chord Tone Identification Using Deep Neural Networks,"
ISMIR-LBD/DLfM 2017*, URL `https://simssa.ca/assets/files/Ju2017.pdf`, held ✓, tier **LINK**. **The
candidacy row** (line 97) writes *ISMIR-LBD 2017, non-chord tone identification using deep neural
networks*. The held file is `docs/research_papers/ju_conditschultz_arthur_fujinaga_2017_ismirlbd_nct_dnn.pdf`,
**447,585 bytes** at this session's `docs/research_papers/` listing.

**What matches and what does not, at the object.** The four authors match, in the row's own order. **The
printed title matches the bibliography's row IN FULL and the candidacy row's short title in full** (the
row lowercases it) — not as a prefix; row 14's extract is recorded in the progress record's table as a
full match too, and the rows with prefix matches are named there; no count is asserted. **The year 2017
is printed**, in the attribution line. **Of the TWO venues the bibliography names, ONE is printed:** the
attribution line names the Late-Breaking Demo session of the 18th ISMIR, Suzhou, 2017; **the string
"DLfM" — and any workshop name — is printed NOWHERE in the two pages.** `population.md` V11 carries the
same double venue *"ISMIR-LBD/DLfM"*. So the bibliography's and V11's second venue is unsupported by the
held file; whether the paper also appeared at a DLfM workshop is not establishable from what is held and
is not asserted either way. **No page numbers, no running header and no conference footer beyond the
attribution box are printed.** The attribution box names the session itself, but the URL is an
author-group site (`simssa.ca`), so **whether the held file is byte-identical to the text
the ISMIR late-breaking session hosts is not establishable from what is held** — rows 18's and 28's shape,
as the progress record's block (c‴) anticipated. **The tier direction: a CC BY 4.0 licence IS printed, so
the bibliography's LINK tier UNDERSTATES it** — rows 30's, 49's and 50's direction, and the opposite of
rows 51's, 52's, 12's, 13's, 16's and 17's, whose files printed no licence line at all. Routed to the
bibliography reconciliation as a tier note, the correction itself not made here.

**The carried question of block (c‴) is answered at page 1: the input is SYMBOLIC.** Abstract: *"Trained
and tested on a dataset of 140 Bach chorales, the DNN model was able to identify non-chord tones with
F1-measure of 72.19% using pitch-class, metric information, and a small contextual window around each
input sonority as input features."* §2: *"This project draws on a convenient dataset, Rameau [3],
consisting of 140 Bach chorales with expert harmonic annotations."* §3: the input is a twelve-element
pitch-class presence vector per slice. No audio, no signal processing, no recording appears anywhere in
the paper. **Under block (e‴) no domain bound is owed, and none is stated.** The repertoire, established
at page 1 and at §2, is Bach chorales — this project's own repertoire; **which earlier rows of this slice
share it was not checked, and nothing is asserted about them.**

## What the paper is, in its own structure

Abstract; 1 Introduction; 2 Dataset; 3 Method; 4 Evaluation; 5 Conclusion; 6 References (four entries).
Two figures (Figure 1, the first two measures of BWV 30/6 transposed, with the DNN input and output
vectors for two slices; Figure 2, the first nine measures of BWV 389 with three lines of text under the
staves), two tables (Table 1, the DNN settings; Table 2, the one results column), no numbered equations.
A two-page late-breaking-demo abstract, self-described as *"our preliminary research"* (§1).

## The paper's own claims, labelled

**[FACT — the thesis and the pipeline the authors intend, §1, quoted whole because DP-D's rival is this
sentence.]** *"Non-chord tones are elaborative notes, created by idiomatic step-wise melodic contours, which
do not belong to the local structural harmony. Identifying non-chord tones is essential to many music
analytic tasks, including polyphonic music retrieval [4], harmonization [1], and harmonic analysis [3].
Although the theoretical difficulty and importance of non-chord tone identification are addressed, few
scholars have proposed complete, dedicated non-chord tone identification models."* And: *"Machine learning
of harmonic analysis is difficult due to the large number of chord classes, which require large amounts of
training data to learn. In contrast, the relatively simple task of non-chord tone identification requires
much less training data. Once non-chord tones are identified, harmonic analysis becomes a relatively simple
task, which can be accomplished by a rule-based algorithm."* **So the paper's own stated design is the
first-running detector followed by a rule-based harmonic analysis — the candidate DP-D names as *"an
input, from an elaboration detector running first"* and excludes.**

**[FACT — the dataset and the ground truth's construction, §2.]** *"Harmonic labels in the Rameau dataset
are aligned with the music as salami-slices: a "salami-slice" is formed whenever a new note onset occurs in
any musical voice. To make the tonal relationships between pitch-classes consistent across the dataset, we
also transposed all the chorales into the same key. Non-chord tones can be identified and labeled from each
chord label associated with the slice."* Footnote 1 gives the dataset's location,
`https://github.com/kroger/rameau/tree/master/rameau-deps/genos-corpus`. **Three things this states in
terms:** the slice grid is ONSET-ONLY (a new onset in any voice; a release opens no slice); the chorales
are TRANSPOSED to one key before anything is learned, so a tonality is consumed as given, one per chorale,
and the paper does not say where it came from; and **the non-chord-tone ground truth is DERIVED FROM THE
CHORD LABELS** — a note is a non-chord tone in the ground truth exactly when it is outside the annotated
chord of its slice. Figure 1 shows the same thing: the *Chord* line is the given annotation and the
*Non-chord tone* line is read off it.

**[FACT — the method, §3 and Table 1.]** Input per slice: *"a vector of twelve ones or zeros, representing
which pitch classes (C, C#/Db, D, D#/Eb, etc.) are present (1) or absent (0) in the slice"*, plus *"metric
information about each slice … specifically whether the current slice is on beat (1) or off (0)"*. Output
per slice: *"a similar vector of length four, indicating which, if any, of the four voices contains a
non-chord tone."* Worked example (Figure 1 and §3): the third slice of measure 2 contains D, G, A and B —
input `[0,0,1,0,0,0,0,1,0,1,0,1]`; output `[0,0,1,0]`, *"the pitch (A), which is the third "1" from the
left in the input vector, is a non-chord tone"*. §4: *"one slice adjacent (before and after) to the
current one were added to the input vector, creating a windowed input that allows the model to consider
context"* — Table 2's column heading *PC + B + WS1 (D:42)* is that: three slices of 12 + 1 + 1 = 14, so 42
input dimensions *(derived here from the caption's own key: PC pitch-class, B on/off-beat, WS window
size, D dimension; the paper does not spell the arithmetic out)*. Table 1: network structure *2 hidden
layers, 200 nodes each*; optimizer *ADAM*; loss *binary cross-entropy*; data division *8:1:1
(training:validation:test)*; evaluation metric *precision, recall, F1-measure*; evaluation method *10-fold
cross validation*. *"The experimental settings were determined empirically (shown in Table 1)."* **So the
representation discards spelling, octave, voice identity of the input pitches, duration and the bass;
the output names a VOICE, not a note, and presupposes exactly four voices.**

**[FACT — Table 2, transcribed cell by cell at two readings of page 2 of 2.]** Caption: *"Model
performances with a combination of input features. (PC: pitch-class; B: on/off beat feature; WS: window
size; D: dimension of the feature vector)."* One column, headed *Input features | PC + B + WS1 (D:42)*:

| Row (as printed) | Value (as printed) |
|---|---|
| Precision | 86.02±3.35% |
| Recall | 63.14±10.81% |
| F1-measure | 72.19±7.68% |

*Transcription check, at the page.* The Abstract, §4 (*"the model achieved F1-measure of 72.19%"*) and §5
(*"With an F1-measure of 72.19%"*) all print the F1 value, and all three agree with the cell. **The table
has ONE column, so 72.19 is the paper's headline and its only reported configuration** — the caption's
*"a combination of input features"* promises a comparison the table does not print. **What the "±" is, the
paper does not say**: Table 1 names ten-fold cross-validation and an 8:1:1 division, so the natural reading
is a spread across the ten folds, but whether it is a standard deviation, a standard error or a range is
UNSTATED, and no per-fold value is printed. `population.md` V11 prints *(±7.68)*; `FRAMEWORK.md` DP-D and
DP4 print *F .72* alone.

**[FACT — the class imbalance, §4.]** *"Because of the significant imbalance between the number of chord
tones and non-chord tones (92% and 8%), we report the metrics of precision, recall, and F1-measure."* **A
measured base rate under this ground truth: 8% of voice-slice slots in 140 Bach chorales carry a
non-chord tone.** *Derived here, not printed, and marked so it is not mistaken for the paper's own claim:*
under that imbalance a recall of 63.14% and a precision of 86.02% correspond to about 5.05% of slots
correctly flagged, about 2.95% missed and about 0.82% falsely flagged — **about 3.8% of all voice-slice
slots misclassified**, and about 37% of true non-chord tones missed (1 − recall). Neither of those is
27.81, which is 100 − 72.19. **The complement of an F1-measure is not an error rate of any population in
the paper.** This arithmetic is the reader's and bears on finding (7) only.

**[FACT — the quotation DP-D rests on, §4, quoted with the two sentences before it because they fix what
it is about.]** *"Fig. 2 illustrates the output of the model on a Bach chorale: Chord labels are placed
immediately below the staves, while the next two lines of text indicate non-chord tones in the ground-truth
data and the model's output respectively. As we can see, the model is correct for the first six measures,
with some errors in the rest of the chorale. Experienced music analysts will see that many of the "errors"
in fact represent plausible analytical choices."* Figure 2's caption: *"The first nine measures of BWV 389
"Nun lob, mein Seel, den Herren." The first line of text underneath the score is the original chord
labels. The second line is the non-chord-tone ground truth (note names). The third line is the model's
predicted non-chord tones."* **The sentence V11 quotes verbatim is confirmed word for word at the object.
It is said of the errors in ONE illustrated chorale, BWV 389, measures 1–9; no count of such cases is
given, and the sentence is not attached to Table 2's evaluation.**

**[FACT — §5, the conclusion.]** *"In this demo, a non-chord tone identification model for Bach chorales
using feedforward DNN is proposed. This model is trained and tested using the Rameau dataset. With an
F1-measure of 72.19%, we hope that better performances will be achieved with more higher-quality data.
Thus, we intend to complete the whole Bach chorale dataset with 371 chorales fully annotated with
harmonic/contrapuntal labels. Not only will this dataset help to train and test our model further, it will
be useful for many other music analytical tasks."* Footnote 2 acknowledges SSHRC funding.

**[FACT — uncertainty, #24.]** A "±" value is printed on every one of the three cells and its kind is
unstated (above). No significance test, no comparison and no second configuration is printed, so nothing
in the paper is a difference and nothing is read as one. The practice — a spread printed on every cell but of undefined kind — is recorded
here per row as the progress record's item (h) asks; whether any earlier row's printed spread was likewise
of unstated kind was not checked, and no comparison across rows is asserted.

**[FACT — the references, page 2 of 2, four entries.]** [1] Chuan & Chew, *Computer Music Journal* 35(4),
2011, generating and evaluating musical harmonizations that emulate style; [2] He, Zhang, Ren & Sun, ICCV
2015, delving deep into rectifiers; [3] Kröger, Passos, Sampaio & De Cidra, ICMC 2008, *Rameau: a system
for automatic harmonic analysis*; [4] Pickens, PhD thesis, University of Massachusetts Amherst, 2004,
harmonic modeling for polyphonic music retrieval. **NOT ONE of the four is in this project's bibliography
and NOT ONE has an extract**, both checked at their own listings this sitting (`BIBLIOGRAPHY.md` searched
for *Rameau*, *Kröger*, *Chuan*, *Pickens*, *Kaiming*; `reading_pass/extracts/` listed through the
bridge). **Kröger et al. 2008 is named in rows 45's and 46's not-fetched paragraphs** in the progress record's
not-done section (read there; no line numbers cited, this sitting's own edits having moved them) and is
held nowhere. **Nothing is carried out of any work that is not
held** — in particular nothing about how the Rameau annotations were made, which is the thing finding (5)
most wants and cannot have.

**[CONJECTURE — the authors', §1 and §5.]** That DNNs *"offer an innovative and promising approach"*
(Abstract) and that *"better performances will be achieved with more higher-quality data"* (§5) — stated,
not tested. And the §1 sentence that harmonic analysis *"becomes a relatively simple task, which can be
accomplished by a rule-based algorithm"* once non-chord tones are identified is an assertion the paper
does not build, measure or cite for.

## Cross-primary check (mandatory item (j)) — NOTHING TO RUN ON, established

A figure sweep after the read on *86.02*, *63.14*, *3.35*, *10.81*, *72.19*, *7.68*, *140 Bach*, *Rameau*,
*salami* and the pair *92%* / *8%* found no read row of this slice reporting on this paper's data, its
metric or its figures — only the record's own citations of V11 named above. **Row 52's paper carries a
non-chord-tone head and, as the progress record's row-52 paragraph records of that read (the extract not
opened this sitting), reports no non-chord-tone accuracy of any kind**, so there is no cell of it to set
beside 72.19. **Population row 14 — Hu & Arthur 2021 — is a non-chord-tone classifier on monophonic
melody**, whose figure the findings surface states as *"beats the trivial all-chord-tone baseline by only
about 4–5 points out of sample"* (line 246, RELAYED there); that is accuracy over a baseline on a
different task shape, NOT COMPARABLE with an F1 on four-voice slices, and it is recorded as such rather
than merged. **The paper's dataset — 140 chorales under the Rameau annotation — is not this project's
corpus** (326 chorales under the *When in Rome* / DCML annotation, per `DECISIONS.md`'s terms table), so no
figure here is comparable with any figure this project publishes; **and one numeric coincidence is recorded
so nobody merges it: the paper's 92% chord-tone share is not a figure of this project's, and this
project's root-agreement baseline is a different quantity on a different corpus.**

## Coupling facts (mandatory)

**What the method ASSUMES about its upstream.**
- A score in exactly FOUR voices (the output is one bit per voice), sliced at every new onset in any voice
  — the onset half of L1's partition-point construction with the release half absent.
- **A tonality per chorale, supplied before anything is learned** (*"we also transposed all the chorales
  into the same key"*), its source unstated — an oracle key, never decided by the system and never
  changing within a chorale.
- For training, **a chord label per slice from which the non-chord-tone labels are read off** — the
  detector's target is a function of the chord annotation it is meant to precede.
- Pitch-class presence only: no spelling, no octave, no duration, no bass, no voice identity of the
  pitches; on-beat or off-beat as the one metric fact; one slice of context each side.

**What it HANDS downstream.**
- Per slice, four bits: which voices carry a non-chord tone. **Not per note, not per pitch, and nothing
  for a texture of other than four voices.**
- **No rivals, no confidence, no posterior** is published; the four bits are the whole published output.
  (That a network trained with binary cross-entropy has a graded output before thresholding is this
  reader's inference; the paper says nothing of it.)
- No chord, no tonality, no boundary, no elaboration relation (passing, neighbour, suspension,
  anticipation), no chord-tone assignment for the pitches it does not flag beyond the complement.

**Its own STATED SCOPE and limits.**
- **Domain:** symbolic; Bach chorales; 140 pieces under the Rameau annotation; 2017; *"preliminary"*.
- **Decision scope:** one binary question per voice per slice — non-chord tone or not — under a given
  chord segmentation (the salami slices are the annotation's own grid) and a given key.
- **Fitting:** a feedforward network fitted by binary cross-entropy with ADAM, settings *"determined
  empirically"*, ten-fold cross-validation with an 8:1:1 split — a held-out figure, with a validation
  portion named and its use unstated.
- **Self-assessed:** the authors want *"more higher-quality data"* and intend to annotate all 371 chorales.
- **Coupling:** the paper decides its one question in isolation from every other of L2's four; the
  coupling it has with the chord decision runs the OTHER way from DP-D's rival as a pipeline — the chord
  label is the INPUT to its ground truth, not the output of its method.

## What an L2 detail specification could adopt, adapt, or must argue against

- **Must argue against — and the record already does, at DP-D, on a ground this read confirms and
  sharpens.** The paper is a published, on-repertoire instance of DP-D's first candidate — the one the record
  cites for it — a detector
  run first whose output the harmonic analysis consumes. Its own §1 proposes exactly that pipeline. The
  exclusion ground's first clause — *its own authors report F .72* — is confirmed at Table 2 as the
  paper's one and headline figure. Its second clause — *many "errors" that are plausible analytical
  choices* — is the authors' sentence verbatim, said of one illustrated chorale (finding (4)). Its third
  clause — *a first-running detector's mistakes are unrecoverable downstream* — is the record's own
  reasoning and is stated by the paper nowhere; the paper asserts the opposite intent and does not test
  it. **What this read ADDS to the ground, as a datum a specification would cite this paper for:** the
  detector's ground truth is READ OFF the chord labels (§2), so what it learns to predict is the residue
  of a chord decision already taken by the annotator — a first-running detector trained this way cannot
  supply the chord decision with information that is not already a function of it, which is the
  entanglement §4.2's elaboration ↔ chord bullet states from the other side (the chord label *is* a
  function of which notes are heard as elaboration). Cited to §2 and Figure 1 for the fact; the
  consequence is this reader's and is put to the user beside DP-D's ground, applied nowhere.
- **Must argue against (voice-level output, four voices assumed).** L2's charter publishes the
  chord-tone assignment *per sounding note*; this method publishes it per voice per slice and only for
  four-voice texture. As a rival it does not even produce L2's publication, and a specification carrying
  DP-D's exclusion can say so with the paper's §3 as its ground.
- **Adapt (the melodic and metric covariates D-527 already names).** D-527 conditions each tone's
  emission on chord-independent melodic and metric covariates. This paper's features are a subset of
  that class — on/off-beat and one slice of pitch-class context each side — and its Table 2 measures what
  that subset alone buys for the binary question under a given key and grid. A specification fitting
  D-527's covariate tables would cite this paper as the published floor for pitch-class-and-beat features
  on this repertoire, with the caveat that the paper's target is voice-level and its grid onset-only.
- **A datum for the L1 charter's partition-point construction (V8).** The Rameau grid is onset-only:
  *"a new note onset occurs in any musical voice."* A release opens no slice, so an elaboration whose
  moment begins at a release is invisible on this grid — the class of case C27 names. Recorded beside V8
  as a published instance of the half-construction, routed to L1's detail specification; no verdict.
- **A datum for DP-B and D-526 (oracle key, chords relative to it).** Transposing every chorale to one
  key before learning is D-526's key-relative representation reached by relabelling, with the key supplied
  by hand — row 17's rotated condition on symbolic input, as that extract records it (read this sitting).
  The paper measures nothing against an untransposed condition, so unlike row 17 it supplies no sign.
  Recorded, nothing carried.
- **A datum for the emission design's base rates (D-527) and for measurement design.** 8% of voice-slice
  slots are non-chord tones under this annotation of 140 chorales — a measured prior for the category
  emission, on this repertoire, at this grid, under this ground truth. Beside it: population row 14's
  measured style dependence of non-chord-tone rates, as the findings surface states it at line 670 (that
  paper not opened), merged with nothing.
- **A datum for principle #21 and OI-179 (the ground-truth ceiling).** The authors' own sentence — that
  many of the errors are *"plausible analytical choices"* — is an on-repertoire statement, by a group
  that annotates chorales, that the non-chord-tone label admits more than one defensible reading; the
  record already carries it as such at DP-D. It is UNQUANTIFIED here (one chorale, no count). Recorded
  beside the on-repertoire ambiguity-ceiling statement the progress record's row-52 paragraph records
  that read as finding (that extract not opened; the record calls it *"a second"* and this reader did
  not locate the first), merged with none.
- **Not transferable, recorded so it is not carried (the network).** A two-hidden-layer feedforward
  network over 42 bits is a fitting choice for a binary target; nothing about it is a term, a factor or a
  score of L2's, and the paper claims nothing structural for it.

## ★ Findings, routed and not applied

**(1) IDENTITY: a FULL title match, ONE of the bibliography's TWO venues printed, and a printed CC BY 4.0
licence that the LINK tier understates.** Authors and year match. *DLfM* is printed nowhere; the ISMIR
2017 late-breaking session is. No page numbers. Whether the held author-site file is identical to the
session's own hosting is not establishable from what is held. **Routed to the bibliography
reconciliation as two notes — the unsupported second venue (which `population.md` V11 also carries) and
the tier direction; no verdict, no correction made here.**

**(2) THE CARRIED QUESTION OF BLOCK (c‴) IS ANSWERED: the input is SYMBOLIC, the repertoire is Bach
chorales, and no domain bound is owed.** Nothing is asserted about which earlier rows share the
repertoire. **No verdict moved.**

**(3) ★ THE VERIFICATION: V11's figure and quotation are CONFIRMED at the object, and DP-D's and DP4's
"F .72" is the paper's headline and only configuration.** Table 2: F1-measure 72.19±7.68% — the value V11
prints; §4's sentence — *"Experienced music analysts will see that many of the "errors" in fact represent
plausible analytical choices."* — verbatim as V11 quotes it. **No value moves; no STOP of the falsifier
class.** Two precisions on how the record's figure reads, both graded precisions: **(3a)** the *±7.68*
V11 prints is of UNSTATED kind — the paper names ten-fold cross-validation and nothing else about the
spread — so V11's parenthesis carries a number the paper does not define, and DP-D and DP4, which omit
it, carry a point value with no uncertainty at all (#24); **(3b)** the paper's own F1 is the ONLY
configuration printed, so *"F .72"* is not a cell among several and the record's use of it as the
paper's result is exact.

**(4) ★ THE ONE PRECISION THAT COULD BE READ AS A CORRECTION, graded a PRECISION with the other reading
stated in full so the user can overrule it.** DP-D's and DP4's ground reads *"its own authors report F .72
with many "errors" that are plausible analytical choices"*. At the object the authors report F .72 over
the ten-fold evaluation (Table 2, §4) and, two sentences later and about Figure 2's ONE illustrated
chorale, remark that many of its "errors" are plausible analytical choices. **The sentence is not a
characterisation of the evaluation's errors, and no count of such cases exists in the paper.** *Why this
is graded a precision:* the words are the authors' own and are quoted exactly; the record's sentence does
not say *measured* or *of the evaluation*; and the ground's work — that a first-running detector would
commit a measured error rate upstream, some of whose "errors" are readings a downstream analysis would
have to weigh anyway — is done whether the remark is about one chorale or all of them. *The reading under
which it IS a corrected structural claim, stated so the choice is the user's:* a reader of DP-D takes
*"with many 'errors' that are plausible analytical choices"* as the authors' account of what the 28 points
below F 1.0 consist of; the paper gives no such account, and on that reading the clause attributes to the
authors a claim about their evaluation they did not make. **Put to the user; applied nowhere; DP-D's
verdict and value unmoved either way.** Row 18's finding (3), as the progress record names it, is the
precedent for the write-up shape had it been graded a correction.

**(5) ★ THE DETECTOR's GROUND TRUTH IS DERIVED FROM THE CHORD LABELS — an addition candidate to DP-D's
ground, put to the user.** §2 states it in terms and Figure 1 shows it. So the rival DP-D excludes is,
as published, a model trained to reproduce the residue of a chord decision already taken; run first, it
can hand the chord decision nothing that is not already a function of that decision. This is the
entanglement §4.2 states from the chord side, instantiated on the rival's own construction. **The
inference is this reader's; the fact is the paper's.** Recorded beside DP-D's three grounds and beside
population row 14's circular ground truth as the findings surface states it (line 247–248, RELAYED
there), merged with neither; **an addition candidate, applied nowhere.**

**(6) THE PAPER ADVOCATES THE PIPELINE DP-D EXCLUDES, IN ITS OWN WORDS, AND DOES NOT BUILD OR TEST IT.**
§1's *"Once non-chord tones are identified, harmonic analysis becomes a relatively simple task, which can
be accomplished by a rule-based algorithm"* is the rival's own stated ground for existing. DP-D's
*"unrecoverable downstream"* clause is the record's reasoning, not the paper's, and the paper offers
nothing against it. Recorded so that the clause is not later cited to this paper.

**(7) A NUMERIC COINCIDENCE WITH D-527's "~28 % error rate", RECORDED SO NOBODY MERGES THEM, WITH ITS
PROVENANCE EXPRESSLY NOT ESTABLISHED.** D-527's excluded-alternative clause (`decisions/group_A.md` line
621; its home `cowork_joint_estimator_architecture.md` line 164) reads *"the published cleaners' ~28 %
error rate would be hard-committed upstream"*, and names no source. 100 − 72.19 = 27.81. **Whether that
figure is this paper's F1 complement was NOT established** — neither the home nor `ARCHITECTURE.md` (both
swept this sitting) names a source, and *cleaners* is plural. What IS established at this paper: its F1
complement is not an error rate of any population it measures — under its own 92/8 imbalance about 3.8%
of voice-slice slots are misclassified and about 37% of true non-chord tones missed (derived above,
marked derived). **If D-527's figure does derive from this paper, its clause names the wrong quantity;
if it does not, nothing follows.** A second coincidence is recorded in the same breath so it is not
merged either: the findings surface's line 671 carries *"~28% corpus-wide"* as population row 14's
measured non-chord-tone RATE in ornamented variations — a rate of the music, not an error rate of a
detector, and a third quantity again. Put to the user as a question about a ratified decision's defense;
nothing applied; D-527's rule — no live cleaning stage — rests on the circularity and #12 arguments its
own text carries and is unmoved by the figure either way.

**(8) THE OUTPUT IS PER VOICE, FOR FOUR VOICES.** §3 in terms. As DP-D's rival the method cannot produce
L2's per-sounding-note publication and cannot run on other textures. Recorded beside DP-D as a datum for
the exclusion; no verdict.

**(9) THE GRID IS ONSET-ONLY and THE KEY IS AN ORACLE.** §2 in terms. Two data — for V8 and L1's detail
specification, and for DP-B / D-526 — routed above; nothing carried.

**(10) A MEASURED BASE RATE: 8% non-chord-tone slots under this annotation.** §4. Routed to the emission
design (D-527) and measurement design as a datum; merged with population row 14's rates nowhere.

**(11) UNCERTAINTY PRINTED BUT UNDEFINED; ONE CONFIGURATION; NO COMPARISON.** A "±" on every cell whose
kind the paper does not state; the caption promises a feature combination the table does not print.
Routed to measurement design beside D-097, principle #20 and #24.

**(12) FOUR CITED WORKS, NONE HELD, NONE EXTRACTED — and the one that matters for the ground truth
(Kröger et al. 2008, Rameau) is named in two earlier rows' not-fetched lists.** Nothing is carried out of
it; how the Rameau annotations were made — whose readings the "plausible analytical choices" are
choices against — is not establishable from what is held.

**No falsifier of a chosen design point, so no STOP of the falsifier class; and no corrected structural
claim — the one candidate is graded a precision at finding (4) with the grounds for the other reading
stated, so the user can overrule it.**

## Centrality

**CENTRAL, and a SECOND INDEPENDENT EXTRACTION IS OWED — stated with the ground on which the opposite
verdict could be taken, so the user can flip it.**

*The ground.* The load-bearing-claim route is PRESENT: a CHOSEN design point's exclusion ground — DP-D's,
and DP4's in the sealed draft — rests on this paper's figure and on its sentence, `population.md` V11 is
the pass's verification of both, and this read re-verifies both at the object while adding a precision on
the sentence's scope and an addition candidate to the ground. A detail specification carrying DP-D must
carry this paper as the rival's primary, and findings (4) and (5) are claims it would cite this paper for
directly. This is the route the progress record's table records for rows 45, 48, 49, 50 and 52 among others —
a ratified item rests on the paper — and here the item is the exclusion ground of a chosen point, which
is the case the commission's §5 names.

*The ground on which NOT CENTRAL could be argued, stated in full.* It is a two-page late-breaking-demo
abstract that calls itself preliminary; both items the record rests on it are confirmed rather than
corrected; nothing in it would be adopted — no term, no factor, no score; its one figure carries an
undefined spread and no comparison; its method decides one binary question per voice under a given key and
a given grid, so it does not bear on L2's entangled decision except as the excluded shape; and the record
already states the exclusion in full without needing more from the paper than the figure and the sentence
the pass verified. On that reading the paper is a supplier of one measured figure and one quotation, both
already verified, and the row would be NOT CENTRAL with nothing owed.

*Why CENTRAL is chosen anyway.* The commission's own words make a paper on which a chosen point's ground
rests the case for the second extraction, and finding (4) is exactly the kind of reading — one sentence's
scope against a design point's clause — that a second reader should check independently, since this
reader graded it a precision and a second reader might grade it a correction. Finding (5)'s inference is
this reader's alone and should be re-derived from §2 by someone who has not read this extract. **The
verdict flips to NOT CENTRAL, and the second extraction to not owed, if the user reads findings (4) and (5)
as settled by this extract rather than as claims a second reader must test.**

## What this extract does NOT do

It amends no document. It moves no verdict in `reading_pass/candidacy_upgrades.md`, no placement in the
slice derivation, no design point in `FRAMEWORK.md`, no row of `reading_pass/population.md`, nothing in
`cowork_reading_pass_findings_2026_08_31.md`, no entry of `DECISIONS.md` and no row of
`docs/research_papers/BIBLIOGRAPHY.md` — the two bibliography notes of finding (1) are routed, not made.
It writes no open-items row and no decisions-register entry. It lifts no gate: Ruling 1 of
`cowork_rulings_2026_09_05_l2_boot_list_sitting.md` keeps *no derivation before L2's slice of Task B is
read*, and 7 of the 36 rows are still unopened after this one. It fetches nothing: the ISMIR 2017
late-breaking session's own hosting of this abstract, the Rameau dataset and its GitHub corpus, the four
cited works (Chuan & Chew 2011; He, Zhang, Ren & Sun 2015; Kröger, Passos, Sampaio & De Cidra 2008;
Pickens 2004), and the authors' intended 371-chorale annotation were not fetched and are not read; only
the held file was.

---

*Provenance: Cowork, 2026-09-11, the session booted on `cowork_handoff_entry_one_hundred_and_fifty_four.md`
at tip `0f69cc6b79610c962a8400cdaba3dfc12facfe55` (`.git/refs/heads/master` read with the file tools over
a bridge-staged copy). Read before this extract: the ordinary session-start read (`CLAUDE.md` at its six
ruled session-start spans, delivered whole in the harness's project-instructions block; `DECISIONS.md`
whole; `STATUS.md` whole; the derived gating answer's `gating_ids` list whole), the hundred-and-fifty-fourth
handoff entry whole, the hundred-and-eighteenth entry at its bridge-fault section, the hundred-and-fifty-
second entry at its (vi), `reading_pass/l2_slice_reading_progress.md` WHOLE (1,081 lines at the staged
copy), `reading_pass/candidacy_upgrades.md` whole, the L2 slice derivation at lines 55–94, the bibliography
at lines 40–58, `FRAMEWORK.md` at its heading list and at the sections named in the banner,
`reading_pass/population.md` at lines 96–125, the findings surface at lines 240–253 and 668–672,
`decisions/group_A.md` at lines 601–651, `cowork_joint_estimator_architecture.md` and `ARCHITECTURE.md` by
search only, row 17's
extract whole, and the `docs/research_papers/` and `reading_pass/extracts/` directory listings through the
bridge. The PDF was read as page images in the requests the banner states. Every file was read with the
file tools from bridge-staged copies; no shell touched the repository or any staged copy — the container
shell was used only to create this side's own output directories. No figure of this project's own
measurement is restated (#17f, D-431); every number above is the paper's own, transcribed at the page, or
a quantity derived from such numbers and marked derived.*
