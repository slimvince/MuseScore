# EXTRACT — Chen & Su, "Harmony Transformer: Incorporating Chord Segmentation into Harmony Recognition" — Task B candidacy row 47, FIRST of the row's two papers, first pass, AT THE OBJECT

> **STATUS: FIRST-PASS EXTRACT, READ WHOLE AT THE OBJECT (2026-09-06).** Written under
> `cowork_reading_pass_commission_2026_08_30.md` §4, whose form the remedial commission
> (`cowork_reading_pass_remedial_commission_2026_08_31.md` §3) binds unchanged.
>
> **The grade.** All eight pages of the held PDF were read AT THE OBJECT: staged through the bridge
> and read with the file tools as page images. **No relay, no web-fetch read, no prompted
> extraction.** The held document prints proceedings page numbers 259–266 and a running header, so
> every location below is given as the printed section, table, figure or equation number together
> with the proceedings page.
>
> **★ ONE ROW, TWO PAPERS, AND THIS READER'S DECISION, RECORDED AS THE PROGRESS RECORD ASKS.**
> `reading_pass/candidacy_upgrades.md` row 47 (line 119) carries **two papers in one row** — this
> one and the 2021 TISMIR "Attend to Chords". **The decision is TWO EXTRACTS, one per held file**,
> and the ground is the commission's own §4 sentence: *"Every population paper is read WHOLE. Per
> paper, one extraction file under `reading_pass/extracts/`."* The unit of the §4 form is the
> PAPER, not the register row. Two papers with different corpora, different label structures,
> different segmentation metrics and different findings cannot share one identity check, one set of
> coupling facts or one results section without losing which finding came from which. **The ROW
> stays one row**; its centrality verdict and its second-pass debt are stated once, in this file's
> companion, and pointed at from here (#6). The companion is
> `reading_pass/extracts/chen-su-2021-attend-to-chords-improving-harmonic-analysis-of-symbolic-music.md`.
>
> **Why this paper and its place in L2's slice.** Row 47 is ADMITTED at `candidacy_upgrades.md`
> line 119 because *"The Harmony Transformer decides **segmentation and labelling together** —
> DP-C's own question — and is BACHI's named comparable in the findings surface."*
> `cowork_l2_task_b_slice_derivation_2026_09_05.md` §4 (line 82) places it in L2: *"Segmentation and
> labelling together — DP-C's own question."* It is the LAST member of group 2 of the proposed
> reading order ("segmentation decided with the labelling": rows 10, 11, 19, 20, 18, 4, 47), so
> reading it CLOSES that group.
>
> **Where the record cites this row's papers, established by `Grep` of the staged tree BEFORE
> extracting rather than inherited.** The searches actually run, in order: the phrase set *"Harmony
> Transformer"* / *"Attend to Chords"* / *"Chen and Su"* / *"chen_su"*; then the bare word
> *"Transformer"*; then *"BACHI"*; then the bare word *"Chen"*. **The bare-word searches are what
> found everything, and the reason is the method fact recorded two paragraphs below.** Five places,
> and only five:
> **(1)** `reading_pass/candidacy_upgrades.md` line 119 — the row itself; **(2)**
> `cowork_l2_task_b_slice_derivation_2026_09_05.md` line 82 — the slice placement; **(3)**
> `docs/research_papers/BIBLIOGRAPHY.md` line 66 — **ONE bibliography row carrying BOTH papers**,
> two URLs, held ✓, tier CC; **(4)** `FRAMEWORK.md` line 1553 — §S4(a)'s provenance sentence, which
> names *"Chen and Su 2018"*, i.e. **row 46 and not row 47**; **(5)**
> `cowork_reading_pass_findings_2026_08_31.md` lines 186–187 — *"Harmony Transformer v2 62.1"*,
> inside the DP-C block's BACHI item. **`FRAMEWORK.md`'s live DP-C, at lines 690–701, does not cite
> this paper or its successor at all.**
>
> **What is established about `[FACT]`s, at its own width and no wider.** No `[FACT]` in the record
> NAMES either paper — that is what the searches above establish. Because row 18 is the standing
> proof that the record can carry a paper's figure without its authors' names, the figures were
> checked too: **every figure DP-A's ground cites (lines 671–683: .462→.491, .503→.516, .762→.859)
> and every figure DP-C's ground cites (lines 690–701: 68.8/23.3, 7.6–38.2, 21.3–31.5, the ablation,
> the formal result, 26%) was compared against the two papers' own tables, and none is a figure of
> either paper.** Not checked, and stated rather than glossed: the rest of `FRAMEWORK.md` outside
> those two grounds and the sections this session read.
>
> **The carried item (b) is therefore ANSWERED, with three precisions — see finding (1).** The
> two-word phrase *"Harmony Transformer"* is split across a line break in the findings surface, so a
> search for the phrase alone misses it; the single-word search for *"Transformer"* is what found
> it. **Recorded because it is a method fact about the check, not about the paper**, and it is why
> the citation map above rests on the bare-word searches.
>
> **This extract derives no specification statement, amends no document, opens no code and writes no
> open-items row or decisions-register entry.**

## Identity — the PUBLICATION matches with no finding; ONE mild finding about the held file's completeness; the input is BOTH audio and symbolic

Tsung-Ping Chen and Li Su, Institute of Information Science, Academia Sinica, Taiwan
(`{tearfulcanon, lisu}@iis.sinica.edu.tw`). Printed title, page 259: **"HARMONY TRANSFORMER:
INCORPORATING CHORD SEGMENTATION INTO HARMONY RECOGNITION"**.

**The bibliography's row** (`docs/research_papers/BIBLIOGRAPHY.md` line 66) names *Chen & Su,
"Harmony Transformer," ISMIR 2019; "Attend to Chords," TISMIR 2021*, with the URLs
`https://zenodo.org/records/3527794` and `https://transactions.ismir.net/articles/10.5334/tismir.65`,
held ✓, tier **CC**.

**What matches, at the object.** The authors match. The bibliography's short title *"Harmony
Transformer"* matches the held document's title as a **prefix** — the held title carries the
subtitle *"Incorporating Chord Segmentation into Harmony Recognition"*, which the row abbreviates
away. The venue and year match and are printed twice: the copyright box on page 259 reads *"© Tsung-Ping
Chen and Li Su. Licensed under a Creative Commons Attribution 4.0 International License (CC BY 4.0).
Attribution: Tsung-Ping Chen and Li Su. 'Harmony Transformer: Incorporating Chord Segmentation into
Harmony Recognition', 20th International Society for Music Information Retrieval Conference, Delft,
The Netherlands, 2019."*, and the running header on every subsequent page reads *"Proceedings of the
20th ISMIR Conference, Delft, Netherlands, November 4-8, 2019"*. Proceedings pages 259–266 are
printed. **The tier matches too: CC BY 4.0 is printed on page 1.** **So the PUBLICATION identity
closes with no finding** — title (as a prefix), authors, venue, year, pagination and licence all
printed and all matching, as rows 19, 20 and 30 also closed. **One precision for the bibliography
reconciliation, of the mild kind:** the row carries ONE line for TWO distinct publications, so a
reader who resolves the row to a single held file resolves it wrongly; the row's own two URLs are
what disambiguate it. **And one mild finding about the held FILE rather than about the publication,
stated immediately below the structure list.**

**The input, established at §4.1.** **BOTH domains, in one paper.** *Audio:* the McGill Billboard
dataset, *"which consists of 890 musical pieces sampled from the Billboard chart slots"*, represented
as a *"24-by-T dimensional chromagram"* built by *"combining the 12-D treble chroma and the 12-D bass
chroma"* from the non-negative-least-squares (NNLS) chromagram of the Chordino VAMP plugin.
*Symbolic:* the **BPS-FH** dataset, *"in which the first movements of Beethoven's 32 piano sonatas
are included"*, represented as pianorolls *"with the pitch range from A0 to G♯7 (middle C = C4), where
the duration of each note is measured in term of crotchet beats, and is quantized to 32th note."* So
the domain caveat under the candidacy derivation's consequence (ii) applies to **half** of this
paper's evidence and not to the other half — unlike rows 27, 3, 26, 20 and 18, which are audio
throughout.

**Structure of the held document:** six numbered sections (1 Introduction; 2 Hierarchical Structures
and Chord Recognition; 3 The Harmony Transformer, with §3.1 Basic units, §3.2 Input embedding, §3.3
Encoder: chord segmentation, §3.4 Decoder: segmentation-informed chord recognition; 4 Experiments,
with §4.1 Data (§4.1.1 Audio data, §4.1.2 Symbolic data, §4.1.3 Data augmentation), §4.2 Experimental
trials (§4.2.1 Chord symbol recognition, §4.2.2 Harmonic function recognition), §4.3 Hyperparameters
and training, §4.4 Evaluation results; 5 Conclusion and Future Work; 6 References), two
figures, two tables, fourteen numbered equations. Eight pages, 259–266.

**★ A SECOND MILD IDENTITY FINDING, ABOUT THE HELD FILE'S COMPLETENESS.** The printed reference list
**ends at [41]** (Srivastava et al., dropout), at the foot of page 266, which is the last page of the
held document — read twice at the image. **But §4.3 on page 264 cites "[42]" for label smoothing.**
So the held file's reference list does not carry every entry its own text cites, and **whether the
proceedings text as published carries a [42] is not establishable from what is held.** It changes
nothing above — no claim in this extract rests on reference [42], and label smoothing is transcribed
from §4.3's own sentence rather than from its citation — but it is the kind of fact the bibliography
reconciliation exists to hold. **Routed there. No verdict, and no count of the list is asserted
beyond the number the last printed entry carries.**

**File:** `docs/research_papers/chen_su_2019_ismir_harmony_transformer.pdf` (744,532 bytes at the
listing).

## Claims, labeled

### ★ What the method decides, in what ORDER, and from what — the load-bearing passage

**[FACT — the division of labour, §1 and §3, page 259 and page 261.]** *"For the encoder-decoder
architecture of the Transformer, we assign the chord segmentation task to the encoder, and the chord
recognition task to the decoder. This division allows the chord recognition network to benefit from
the prediction of the chord segmentation."* And §3's opening: *"The encoder of the Harmonic
Transformer performs the chord segmentation task from the input data sequence, and the decoder
performs the chord recognition task from the input data sequence along with the segmentation result
from the encoder."*

**[FACT — the encoder's output is a HARD BINARY DECISION, §3.3, page 262. This is the passage the
whole reading turns on.]** Verbatim: *"To predict chord change, **R**^enc is then fed into a
fully-connected layer followed by a sigmoid activation. That is, the likelihood of chord change at
time t is estimated by the equation: p_t^enc = sigmoid(**w**^T **r**_t^enc) … And the segmentation
loss is calculated with **p**^enc := {p_t^enc}_{t=1}^T during training… **In order to make use of the
chord change prediction for the later part of the chord recognition task, we utilize deterministic
binary neurons (DBNs) to binarize the real-valued chord change probabilities with hard
thresholding.** Accordingly, the final output of the encoder **o**^enc := {o_t^enc}_{t=1}^T is a
sequence of binary chord segmentation prediction, with o_t^enc = 1 at the point of chord change, and
0 otherwise: o_t^enc = 1 if p_t^enc > 0.5 and o_t^enc = 0 if p_t^enc ≤ 0.5."* The paper's own worked
illustration follows: *"For example, for a source sequence of 6 segments, **o**^enc = [1, 0, 0, 1, 1,
0] means that there are three chord regions with the first region containing three segments, the
second containing one segment, and the final containing two segments."*

**[FACT — how the committed segmentation reaches the labelling: REGIONALIZATION, §3.4, page 262.]**
*"the embedded sequence of the decoder **E**^dec is regionalized in line with **o**^enc to generate
**Ē**^dec. Precisely, let **c** := {c_k}_{k=1}^K be the K time steps where chord changes, i.e.,
o_{c_k}^enc = 1, then the regionalization layer in Figure 1 replaces each member **e**_t^dec in
**E**^dec with average pooling"* — equation (8), the mean of the decoder embeddings over the region
[c_k, c_{k+1}). The decoder then takes three inputs: the original embedding **E**^dec, the
regionalized embedding **Ē**^dec, and the encoder's weighted-sum representation **R**^enc (equation
11: **H**_0^dec = **E**^dec + **Ē**^dec + **R**^enc). *"The intuition of adding the regionalized
embeddings **Ē**^dec to the decoder inputs is to guide the model to recognize the sequence with the
segmentation-level, or chord-level information."*

**[FACT — the joint TRAINING, §3.4, page 262, equation 14.]** *"L_total = λ₁BCE(**p**^enc,
**ō**^enc) + λ₂CCE(**O**^dec, **Ô**^dec) … where BCE is the binary crossentropy, CCE is the
categorical crossentropy, and λ₁ and λ₂ are coefficients used for balancing the two cross
entropies."* **Both terms are supervised**: §4.1 states *"chord segmentation labels are further
derived from the chord annotations for the supervised training of the chord segmentation task."*
λ₁ = 3 and λ₂ = 1 (§4.2).

**[FACT — how the gradient crosses the hard threshold, §4.3, page 264.]** *"because the encoder
output contains discrete variables, i.e., the binary chord segmentation predictions **o**^enc, we
utilize a straight-through estimator along with the slope annealing trick to estimate the derivative
gradient during backpropagation."*

**★ AND THE AUTHORS THEMSELVES STATE THE ORDER, IN THE COMPANION PAPER — SO WHAT FOLLOWS RESTS ON
THEIR WORDS AND NOT ONLY ON THIS READER'S READING OF THE MECHANISM.** The 2021 TISMIR paper's §2.2,
summarising this very model: *"the HT estimated chord transitions (or chord boundaries), **and
then** recognized chords via attending to the segmentation-informed sequence."* Read at that paper's
own page 3; the quotation and its treatment are the companion extract's finding (1) and are not
restated here (#6).

**★ WHAT THOSE FIVE PASSAGES ADD UP TO, STATED AS THIS READER'S READING AND LABELLED AS SUCH.** The
chord-change decision is **taken first, by a separately-supervised head, and COMMITTED by a hard
threshold at 0.5**; the label decision is then taken **conditioned on that committed segmentation**,
which enters as region-averaged embeddings. The two are trained end to end and the gradient is
carried across the threshold by an estimator — so the **training** is joint, and the **deciding** is
sequential. **Nothing in the model scores a label against an alternative segmentation, and no
alternative segmentation exists at decode time**: the binarization is not sampled, not marginalised
and not searched. **The ORDER is the authors' own, stated in the companion paper's §2.2 quoted just
above (*"estimated chord transitions … and then recognized chords"*); THIS paper states the division
and the mechanism and lets the order follow from them.** What is labelled as this reader's, and only
this, is the CONSEQUENCE drawn from it — that no label is ever scored against an alternative
segmentation, which is a reading of what the mechanism can and cannot do rather than a sentence
either paper writes. Its consequence for the candidacy row's reason is finding (2).

### The model's other stated properties

**[FACT — the architecture, §3 and §3.1, page 261.]** Encoder-decoder Transformer, *"adopting a
non-autoregressive framework for treating segmentation as the intermediate before the recognition
process"*. Multi-head attention (MHA) and feed-forward network (FFN) units, equations (1)–(3);
residual connections and layer normalization. Absolute sinusoidal positional encoding, equation (6).

**[FACT — the input embedding, §3.2, page 261.]** *"We denote the sequence of frame-level features
(e.g., piano roll or chromagram) with T time steps as **X** := [**x**₁,…,**x**_T]^T… The data
representation entering the Harmony Transformer at time t, as denoted by **X**'_t here, is a segment
of **X** around t: **X**'_t := [**x**_{t−τ},…,**x**_{t+τ}]^T, where the length of each segment is
2τ+1."* Each **X**'_t is flattened through an MHA and FFN unit into a vector **e**_t (equation 5).

**[FACT — the layer weighting, §3.3, page 262, equations 7 and 12.]** *"The hidden states of all
layers are then weighted by the softmax-normalized parameters **α**^enc … The purpose of using the
weighted sum of hidden states from all layers instead of using the output of the last layer is that
different layers tend to encode specific information which individual tasks may rely on to different
extents."* The same is done in the decoder.

**[FACT — the chord-symbol output space, §4.2.1, page 263.]** *"The chord symbol recognition model
has a 26-dimensional output, in which 24 of them represent major and minor triads, 1 represents
'others' for chords other than major or minor triads, and the remaining one represents the 'no-chord'
case."* Metric: *"The weighted chord symbol recall (WCSR) is used as the evaluation metric."*

**[FACT — the harmonic-function output space: FIVE SEPARATE HEADS, §4.2.2, page 263.]** *"We
formulate the harmonic function recognition task as a multi-task learning problem, in which the model
outputs segment-wise predictions of the 5 chord functions: local key of 24 classes, primary degree of
21 classes, secondary degree of 21 classes, chord quality of 10 classes, and chord inversion of 4
classes. We use the classification accuracy to measure the performance of the proposed model."*
**Five heads, one per published field.** This is DP-A's excluded shape, and it is what the record's
§S4(a) sentence describes; see finding (3) and the companion extract, where the same authors abandon
it.

**[FACT — the baselines, §4.2.1, page 263.]** *"We employ a 1-layer bi-directional RNN using LSTM
cells of 512 hidden units (abbreviated as BLSTM) as the baseline for the evaluations of the two
datasets. Additionally, we include the best evaluation result achieved by the ConvNet-HMM model
(denoted as FK here) in [13]"* — a Korzeniowski & Widmer system, *"similar to the Madmom audio chord
recognition framework, which achieves many state-of-the-art scores in the MIREX Audio Chord
Estimation (ACE) campaign."*

**[FACT — the experimental setting, §4.1.1–§4.1.3 and §4.2, page 263.]** *Audio:* each input
sequence *"contains 100 segments (around 23 sec), and is generated through a sliding window of frame
size 21 with hop size 5. Following [13], pieces with id numbers smaller than 1000 are used for
training, and the remaining for testing; also, identical pieces are filtered out. As a result, there
are 5,647 sequences for training and 1,628 sequences for testing."* *Symbolic:* *"The length of each
sequence is 64, and each element in the sequence is a pianoroll segmented with window size of 33 and
hop size of 2… each pianoroll segment is flattened into a vector whose length is 84×33. We use 4-fold
cross-validation for evaluation; the number of sequences of each fold varies from 368 to 585."*
*Augmentation:* *"All the training data are augmented with key modulation and thus expanded to 12
times."* *Hyperparameters:* d = 512, h = 8, L = 2 for both encoder and decoder; λ₁ = 3, λ₂ = 1; Adam,
learning rate 10⁻⁴, β₁ = 0.9, β₂ = 0.98, ε = 10⁻⁹; dropout 0.6; label smoothing 0.1. The **tonal
centroid vector**, *"which models the relationship between chords in a 6-D tonal space"*, is added as
an extra input feature in the variant denoted **HT***.

### The measured results (§4.4, Tables 1 and 2, page 263)

**[FACT — Table 1, transcribed cell by cell and re-read at the page for the fact check.]** Caption:
*"The chord symbol recognition results in term of WCSR score. BLSTM stands for the 1-layer
bidirectional RNN using LSTM cells; both HT and HT* denote the proposed model, except the tonal
centroid vector is added into the input feature of the latter. The F1 scores of the chord
segmentation of HT are shown in the rightmost column."*

| Dataset | BLSTM | FK | HT | HT* | F1 (Seg.) |
|---|---|---|---|---|---|
| Billboard (audio) | 77.03 | 78.90 | 82.68 | **83.00** | 57.15 |
| BPS-FH (symbolic) | 78.87 | – | 83.96 | **84.18** | 66.65 |

**[FACT — Table 2, transcribed cell by cell and re-read at the page.]** Caption: *"The accuracy (in
%) of harmonic function recognition. Note that Degree stands for the accuracy of correctly predicting
both the primary and secondary degrees, and Secondary indicates the accuracy of correctly predicting
the degrees of secondary chords. The segmentation result (with F1 measure) is shown in the bottom
row."* (BPS-FH only — §4.2 states the harmonic function task *"is further applied to the symbolic
dataset"*.)

| Function | BLSTM | HT |
|---|---|---|
| Key | 72.62 | **78.35** |
| Degree | 47.75 | **65.06** |
| Secondary | 48.23 | **68.15** |
| Quality | 53.31 | **74.60** |
| Inversion | 61.59 | **62.13** |
| F1 (Seg.) | – | 67.34 |

*Transcription check, run at the page and recorded because it is what makes the tables safe to
cite.* Four of the paper's own §4.4 sentences cross-check cells: (i) *"For the Billboard dataset, the
performance of the Harmony Transformer outperforms the BLSTM and the FK by 5.65% and 3.78%
respectively"* — 82.68 − 77.03 = 5.65 and 82.68 − 78.90 = 3.78, so the BLSTM, FK and HT Billboard
cells are all confirmed by arithmetic the prose states; (ii) *"For the BPS-FH dataset, our model also
surpasses the BLSTM by 5.09%"* — 83.96 − 78.87 = 5.09, confirming both BPS-FH cells; (iii) *"for
local key, chord degree, secondary chord degree, and chord quality, the HT outperforms the BLSTM
greatly by 5.73%, 17.31%, 19.92%, and 21.29%, respectively"* — 78.35 − 72.62 = 5.73, 65.06 − 47.75 =
17.31, 68.15 − 48.23 = 19.92, 74.60 − 53.31 = 21.29, confirming eight of Table 2's ten cells;
(iv) the Inversion row is the one Table 2 pair the prose does not arithmetically cross-check, and it
is the one pair where the gap is small (62.13 against 61.59); it was read twice at the image and is
recorded with that stated. **The F1 (Seg.) cells are cross-checked only in DIRECTION, by the §4.4
sentence quoted under finding (4); no digit-level prose check exists for them, and that is stated
rather than glossed.** No cell is transcribed that was not read at the image. **Every difference
above is a difference of single runs on one split (Billboard) or of a 4-fold average whose spread is
not reported (BPS-FH): no uncertainty of any kind is stated on any figure in either table (#24).**

**[FACT — the authors' own reading of the segmentation figures, §4.4, page 264.]** *"Nevertheless,
the F1 scores for the segmentation task are not satisfied due to the low recalls (57.10% and 58.69%
for Billboard and BPS-FH). This indicates the challenge to identify the exact time when chord
changes."* **Recorded as printed rather than reconciled:** the recall the prose gives for Billboard,
57.10, and the F1 Table 1 gives for Billboard, 57.15, are different quantities printed one page
apart and nearly equal; both are transcribed as they stand and the near-coincidence is not resolved
here.

**[FACT — the worked example, §4.4 and Figure 2, page 264.]** An excerpt from *"Beethoven's piano
sonata No. 14, Op. 27-2, Mvt. 1, MM. 4-7"*, with three stacked label strips (Label, HT, BLSTM) over
Key / Degree / Quality rows and wrong predictions marked in red. The authors' reading: *"The HT
correctly predicts the chord progression in terms of local key, chord degree, and chord quality,
except that the timing of modulating to E major is slightly later than the ground truth. Notably, the
F minor triad at the second half of measure 6, functioned as a pivot chord in a common chord
modulation, is precisely identified by our model in spite of the key change."* *No value, boundary
time or label is transcribed from this figure beyond the authors' own sentences about it.*

**[CONJECTURE — the authors', §4.4, page 264.]** That the model's advantage over the FK framework
means it *"may compete with the ACE framework of Madmom"* — stated as a possibility, on
*"the same training and evaluation data"*, and not measured against Madmom itself.

**[FACT — §5 Conclusion and Future Work, page 264.]** *"the end-to-end combination of chord
segmentation and chord recognition contributes great benefits to the chord symbol recognition, as
well as to the joint recognition of five harmony functions, a challenging task that relies heavily on
the contextual and structural information in music. Our model has the potential to be further
improved by using the segmentation results to learn the word-level embedding, which has also
witnessed success in natural language processing."*

## Coupling facts (mandatory)

**What the method ASSUMES about its upstream.**
- A frame-level feature sequence on a **fixed grid**: for audio, a 24-dimensional NNLS chroma frame;
  for symbolic, an 84-dimensional pianoroll column quantised to the 32nd note. **Spelling is
  discarded** by both representations. No voices, no meter beyond the quantisation grid, no key
  signature, no tonality.
- For TRAINING: chord annotations, **and chord-segmentation labels derived from them** — the
  segmentation head is supervised, not emergent.
- Nothing at test time beyond the frames: neither condition requires a given chord sequence or given
  boundaries. **In this respect the paper is unlike row 18**, whose headline condition is given the
  label sequence.

**What it HANDS downstream.**
- For the chord-symbol task: one label per frame out of 26 (24 major/minor triads, 'others',
  'no-chord') — equivalently a segmentation into chord regions on the input grid.
- For the harmonic-function task: **five separate segment-wise labels** — local key (24), primary
  degree (21), secondary degree (21), quality (10), inversion (4).
- A binary chord-change sequence from the encoder, published as a segmentation F1.
- **No rivals, no confidence, no posterior.** A softmax over the chord vocabulary exists at every
  time step (equation 13) and nothing is published from it. Fifth instance in the slice, after rows
  11, 19, 20 and 18.
- No chord-tone assignment, no elaboration relation, no segment-length statement.

**Its own STATED SCOPE and limits.**
- **Domain:** popular-music audio (McGill Billboard, 890 pieces) and symbolic classical (BPS-FH,
  Beethoven's 32 first movements). The harmonic-function task is symbolic only.
- **Vocabulary:** 26 chord-symbol classes; the five functional heads above.
- **Decision scope:** where the chord changes, and what the chord is. **Tonality appears only as one
  of the five functional heads (local key, 24 classes) on the symbolic task**, decided in the same
  forward pass and never fed back into the chord decision.
- **Fitting:** the audio split is by piece id with identical pieces filtered; the symbolic evaluation
  is 4-fold cross-validation. **No held-out validation set distinct from the test set is described,
  no stopping rule is stated, and no uncertainty is reported on any figure (#24).**
- **Self-assessed:** *"the F1 scores for the segmentation task are not satisfied due to the low
  recalls"* (§4.4).
- **Coupling:** ONE network, ONE forward pass, TWO supervised losses — and, inside it, the
  chord-change decision committed by a hard threshold before the label decision reads it.

## What an L2 detail specification could adopt, adapt, or must argue against

- **★ Must argue against (the placement of the segmentation decision).** This is a
  **separately-supervised, hard-committed** boundary decision that the labelling then reads. On
  DP-C's own three-way question it is a **"before"** shape with joint training, not a "with" shape:
  no label is ever scored against an alternative segmentation. **It is the nearest thing in the
  slice to the findings surface's rival-shaped BACHI item**, and it goes one step further towards
  "before" than BACHI does — the findings surface records BACHI's boundaries as *"neither committed
  before labelling nor decoded with the labels"*, whereas here they ARE committed, by a threshold,
  and then pooled over. **An L2 detail specification must argue against this shape**, and the
  argument now has a measured cost attached to it in the companion paper (see finding (6) and the
  companion extract's ablation).
- **Adapt (the regionalization idea, stripped of its commitment).** The mechanism by which a
  segmentation informs a labelling here is *average pooling of the frame embeddings inside each
  region*. Read as a design idea rather than as a decode, it is the same content as a segment-level
  feature in a semi-CRF (rows 10, 11, 19): a quantity computed over a candidate span rather than a
  frame. **What differs is that a semi-CRF computes it for every candidate span and scores it, and
  this model computes it once, for the one span set the threshold produced.** A specification
  deciding what its segment-level terms read has a precedent here for the CONTENT and a rival here
  for the COMMITMENT.
- **A datum, not a candidate (the five-head functional output).** The functional task's five
  separate heads are DP-A's excluded shape, decided in one pass on one shared representation — the
  exact configuration §S4(a) describes. It is not a candidate for L2; it is evidence about DP-A, and
  the companion paper is the same authors abandoning it. See finding (3).
- **Must argue against (the fixed frame grid).** Every boundary the model can place is a grid edge —
  a 100 ms-scale audio frame or a 32nd-note pianoroll column — against the L1 charter's change-point
  grid and the L2 charter's *"boundaries are a subset of L1's change points"*. The slice already
  holds this shape at row 19 (an eighth-of-a-beat frame grid), row 18 (a 100 ms grid) and row 20
  (a 0.1 s frame lattice); **what is new here is a grid that is a NOTATED SUBDIVISION** — the 32nd
  note — on symbolic input, which is the grid kind nearest our own and still not the change-point
  grid the L1 charter fixes. *No instance count is asserted beyond the three rows named, which are
  read at their own extracts.*
- **Must argue against (one best path, no rivals).** As above: a softmax exists per time step and
  nothing is published from it.
- **A datum for the candidate-admission boundary condition.** The chord-symbol vocabulary carries an
  explicit **'others'** class *"for chords other than major or minor triads"* and a **'no-chord'**
  class. A second published precedent for an explicit none-of-the-above candidate, after row 18's
  'X'. Routed to L2's detail specification where the admission rule and its defense live.

## ★ Findings, routed and not applied

**(1) THE CARRIED ITEM (b) IS ANSWERED AT THE OBJECTS, AND THE CANDIDACY ROW'S SECOND CLAUSE IS
CONFIRMED WITH THREE PRECISIONS.** The row says the Harmony Transformer *"is BACHI's named
comparable in the findings surface."* **It is:** `cowork_reading_pass_findings_2026_08_31.md` lines
186–187, inside the DP-C block's rival-shaped BACHI item, reads *"it reaches full-chord
macro-accuracy 68.1 against Harmony Transformer v2 62.1, ChordGNN 58.5, AugmentedNet 57.2 and a
rule-based system 28.4."* **The three precisions.** *(i)* The name there is *"Harmony Transformer
**v2**"*, which is **the companion 2021 paper's model**, not this one — that paper's own Note 2
gives its implementation as `Harmony-Transformer-v2`. *(ii)* The naming sits inside a read the
findings surface marks **[RELAYED]**, and the 62.1 figure is **BACHI's paper's report of** the
Harmony Transformer, at second hand; **neither Chen & Su paper is read for it and neither reports
that figure**. *(iii)* **Neither "Chen & Su" nor "Attend to Chords" appears anywhere in the findings
surface**, and `FRAMEWORK.md`'s live DP-C does not cite this row at all. **Routed to the findings
surface's DP-C block as a precision. No verdict moved; the ADMITTED verdict stands.**

**(2) ★ THE CANDIDACY ROW'S FIRST CLAUSE IS IMPRECISE AT THE OBJECT: THE HARMONY TRANSFORMER DECIDES
SEGMENTATION AND LABELLING IN SEQUENCE, TRAINED TOGETHER — NOT TOGETHER.** The row's reason
(`candidacy_upgrades.md` line 119) reads *"The Harmony Transformer decides **segmentation and
labelling together** — DP-C's own question"*, and the slice derivation's line 82 repeats it
verbatim as *"Segmentation and labelling together — DP-C's own question."* At the object the
chord-change decision is taken by a separately-supervised encoder head, **committed by a hard
threshold at 0.5 (§3.3)**, and the label decision is then taken conditioned on that committed
segmentation through region-averaged embeddings (§3.4) — **and the same two authors state that order
in terms in the companion paper**, describing this model as having *"estimated chord transitions (or
chord boundaries), **and then** recognized chords via attending to the segmentation-informed
sequence"* (2021, §2.2). No label is scored against an alternative segmentation, and at decode time
no alternative segmentation exists. **What is joint is the TRAINING** — one total loss, equation (14), with the gradient carried across the threshold by a
straight-through estimator (§4.3) — **not the deciding.**

*What this does NOT do.* It moves no value: every figure above stands. It does not move the row's
**ADMITTED** verdict — under the derivation's own consequence (i), *"have to argue against" admits a
rival*, and a "before" shape with joint training is precisely a rival DP-C must argue against, so the
row is admitted MORE squarely rather than less. It touches no `[FACT]`, because **no ratified text
rests on this paper** (finding (1)(iii)). It does not touch DP-C's verdict or its recorded ground,
which is C27 and four measurements at rows 10, 11, 19 and 18.

*What it DOES do.* It makes row 47's placement in **group 2 of the proposed reading order**
("segmentation decided with the labelling") imprecise on the same shape as **row 8's finding (1)**,
which found that row placed in the wrong group by the order's own definitions. If the group titles
are read strictly, row 47 belongs with the "before" rivals rather than with rows 10, 11 and 19. **The
order is "stated, not ruled" by `candidacy_upgrades.md`'s own words, so nothing is moved: this is
shown to the user and left.** It also makes this row the slice's **closest published analogue to the
findings surface's rival-shaped BACHI item**, which is a fact the DP-C block would want beside that
item.

*Why this is NOT reported as a STOP of the remedial commission §5 class, stated so the judgment is
challengeable.* §5's STOP is *"a CORRECTED structural claim that moves a design-point verdict"* —
and row 18's finding (3) qualified because it corrected the wording of **DP-C's own recorded
ground** in `FRAMEWORK.md`. **This corrects a DERIVATION FILE's reason**, in a document whose own
banner says *"the verdicts below decide only what is READ next; every one of them is challengeable
at its own row"*, and it moves that verdict not at all. It is the same class as the four
rival-shape precisions rows 26, 29, 30 and 5 produced and as row 8's group finding — recorded,
routed, applied nowhere. **The user may grade it otherwise; the facts are above and nothing rests on
the grading.**

**(3) ★ A SECOND CORRECTED STRUCTURAL CLAIM, IN `FRAMEWORK.md` §S4(a)'s SEALED FIRST-STAGE TEXT, AND
ITS DIRECTION SUPPORTS DP-A RATHER THAN WEAKENING IT.** §S4(a) (line 1553) reads: *"**Every system
from Chen and Su 2018 onward predicts key, primary degree, secondary degree, quality and inversion as
separate heads**, and the later ones add root, bass, tonicised key, harmonic rhythm, pitch-class set,
and — in the 2025 work — cadence, phrase, section, pedal, metrical strength and a
chord-tone/elaboration flag."*

*This paper CONFIRMS the sentence.* §4.2.2 states exactly those five heads, with their class counts
(24, 21, 21, 10, 4), and Table 2 reports them one per row. **The 2019 model is the sentence's own
described shape.**

*The companion paper REFUTES the universal.* The 2021 TISMIR paper by the same two authors states in
its §4: *"Chen and Su (2019) predict the components of each RN label individually, while in the
current experiments, **they are combined into a single RN**."* Its Table 3 gives one Roman-numeral
output of vocabulary **5040** beside one key output of vocabulary 42 — **two outputs, not five.** So
*"every system from Chen and Su 2018 onward"* is **false of Chen & Su's own 2021 system**, which the
same bibliography row holds.

*Why this reinforces DP-A rather than damaging it.* §S4(a)'s very next sentence is *"Every later
system adds machinery to undo the separation"*, and it names AugmentedNet, ChordGNN, AnalysisGNN and
RNBert. **Chen & Su 2021 is an instance of exactly that pattern, done by the lineage's own founding
authors, and §S4(a)'s list omits it** — so the passage understates its own evidence while
overstating its universal. The authors' stated reason for combining is DP-A's own argument in their
words: the individual-component form *"makes the components somewhat independent of each other"*.
**Full treatment, with the authors' stated trade-off, is in the companion extract; it is not
restated here (#6).**

*What this does NOT do.* It moves no value, no verdict and no design point: **DP-A's live text
(lines 671–683) does not carry the universal** — it says *"the neural multi-task lineage, read as an
architecture — measured, repeatedly"* — so what is imprecise is the **sealed first-stage text**, on
the same footing as row 27's precision that *"Appendix B's sealed first-stage draft names Catteau by
name where the live charter says 'another'"*. **Nothing is amended.** Routed to the findings
surface's DP-A block and to the user, as row 27's was.

**(4) THE SEGMENTATION IT COMMITS IS POOR, AND THE LABELLING IS GOOD ANYWAY.** Table 1's segmentation
F1 is **57.15** (Billboard) and **66.65** (BPS-FH); Table 2's is **67.34**; the authors attribute the
weakness to low recall — *"57.10% and 58.69% for Billboard and BPS-FH"* — and call it *"the challenge
to identify the exact time when chord changes"*. **In the same runs the chord symbol recall beats
both baselines on both corpora and the five functional accuracies beat the baseline on all five.** So
a committed segmentation that is wrong about a large share of the boundaries still leaves the
labelling better than the segmentation-free baseline. **Read as a datum for DP-C it cuts both ways**,
and both ways are stated: it is evidence that the "before" rival is not fatal at this quality of
boundary, and equally evidence that the labelling is not very sensitive to the boundary decision — in
which case what the segmentation head buys may be a training signal rather than a decode-time
constraint. **The paper measures neither reading; the companion paper's HT-noReg ablation is the
nearest thing to a measurement of it (companion extract, and finding (6) below).** Routed to the
findings surface's DP-C block and to L2's detail specification, with no verdict.

**(5) THE CARRIED ITEM (c) IS ANSWERED: THERE IS NO SEGMENT-LENGTH TERM OF ANY KIND.** The slice
holds three worked shapes for the length term — linear/exponential needing no segmental decode (rows
19 and 20's baseline, row 18's HMM), Gaussian needing it (row 11's example), unbounded-history
needing an approximate decode (row 20's chosen model). **This model has none of them: no duration
distribution, no length feature, no maximum segment length, and no transition cost between adjacent
regions.** Segment length is whatever the sequence of independent per-frame threshold crossings
produces. *This reader's reading, labelled as such and not the paper's:* if those per-frame
chord-change probabilities were read as an independent generative model, the implied length
distribution would be geometric — i.e. row 18's exponential shape, the one a segmental decode does no
work for by row 11's §2.3 test — but **the paper neither states that reading nor treats length as a
modelling choice anywhere**, so nothing stronger is claimed. **The slice's three shapes are unchanged
by this row; what it adds is the fourth possibility, which is to model the length not at all.**
Routed to L2's detail specification and to the findings surface's DP-C block.

**(6) A POINTER, NOT A COPY (#6): THE MEASURED COST OF THE MECHANISM THAT MAKES THE LABELLING SEE THE
SEGMENTATION IS IN THE COMPANION PAPER.** This paper reports no ablation of the regionalization unit.
The 2021 paper does (`HT-noReg`), it reports standard deviations, and the result is small. **The
figures and their reading live in the companion extract and are not restated here.**

**(7) THE 'OTHERS' AND 'NO-CHORD' CLASSES.** §4.2.1's 26-way output carries an explicit
none-of-the-above class and an explicit silence class. Second published precedent in the slice for an
explicit out-of-vocabulary candidate, after row 18's 'X'. Routed to L2's detail specification beside
the candidate-admission clause.

**(8) THE TWO TASKS ARE NOT COMPARABLE ACROSS THE ROW'S TWO PAPERS, AND THE CAUTION IS RECORDED
BEFORE ANYONE TRIES.** This paper reports functional harmony as **five per-component accuracies**
(Table 2) on BPS-FH; the companion reports it as **one whole-Roman-numeral accuracy** (Table 4) on
the same corpus. The two are different quantities over different label spaces, and neither paper
claims otherwise. **No figure of one may be read against a figure of the other.** Recorded so a later
citation of "the Harmony Transformer's functional accuracy" does not silently mix them.

**(9) NO UNCERTAINTY ON ANY FIGURE IN THIS PAPER (#24).** Every cell of Tables 1 and 2 is a single
number: the audio figures are one run on one split, the symbolic figures a 4-fold average whose
spread is not given. **The companion paper reports standard deviations on every cell**, which makes
the pair a within-row instance of the same authors moving from the pattern the slice has recorded
repeatedly to the opposite one. Routed to measurement design beside principle #24.

**(10) TWO IDENTITY POINTS, BOTH MILD, BOTH STATED IN FULL IN THE IDENTITY SECTION AND NOT RESTATED
HERE (#6).** The publication identity closes with no finding; the bibliography carries ONE row for
this paper and its successor, which is a resolution hazard for a later reader; and the held file's
printed reference list ends at [41] while §4.3 cites [42]. **All three routed to the bibliography
reconciliation. No verdict, and nothing in this extract rests on any of them.**

**No falsifier of a chosen design point, so no STOP of the falsifier class. Findings (2) and (3) are
corrected structural claims and are put to the user with this extract; nothing is amended and nothing
is applied.**

## Centrality

**Stated ONCE for the ROW, in the companion extract (#6), because centrality is a property of the
register row and the row is one.** The verdict and the ground on which the opposite could be argued
are there; this file does not restate them.

## What this extract does NOT do

It amends no document. It moves no verdict in `reading_pass/candidacy_upgrades.md`, no placement in
the slice derivation, no design point in `FRAMEWORK.md` — live or sealed — no row of
`reading_pass/population.md` and nothing in `cowork_reading_pass_findings_2026_08_31.md`. It writes
no open-items row and no decisions-register entry. It lifts no gate: Ruling 1 of
`cowork_rulings_2026_09_05_l2_boot_list_sitting.md` keeps *no derivation before L2's slice of Task B
is read*, and 21 of the 36 rows are still unopened after this row. It fetches nothing: the Zenodo
proceedings deposit, the McGill Billboard and BPS-FH data, the Chordino VAMP plugin, the authors' own
GitHub implementation, and every one of the works it cites — Vaswani et al. 2017, Devlin et al. 2019,
Gu et al. 2018, Korzeniowski & Widmer [13] and [14], Bengio et al. 2013, Dong & Yang 2018, Chen & Su
2018 [25], Harte et al. 2006 [20], Burgoyne et al. 2011 [39], Mauch & Dixon 2010 [40] among them —
were not fetched and are not read; only the held file was.

---

*Provenance: Cowork, 2026-09-06, the eleventh reading session of L2's slice, booted on
`cowork_handoff_entry_one_hundred_and_twenty_six.md` at tip
`911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb` (both ref files read with the file tools). Read before
this extract: the ordinary session-start read (`CLAUDE.md` whole, `DECISIONS.md` whole, `STATUS.md`
whole, the derived gating answer with its `gating_ids` list whole), the hundred-and-twenty-sixth
handoff entry whole, the hundred-and-eighteenth entry whole for its bridge-fault section,
`reading_pass/l2_slice_reading_progress.md` whole, `reading_pass/candidacy_upgrades.md` whole, both
commissions whole, the row 18 extract whole for the form, the L2 slice derivation whole, the
bibliography whole, `FRAMEWORK.md` at the L2 charter (lines 386–425), the design points DP-A to DP-K
(lines 669–735) and §S4's opening and §S4(a) (lines 1528–1572), and the findings surface at its DP-A
and DP-C blocks (lines 115–199). The staged tree was searched with `Grep` before extracting, in this
order: the phrase set "Harmony Transformer" / "Attend to Chords" / "Chen and Su" / "chen_su"; the
bare word "Transformer"; "BACHI"; and the bare word "Chen". The held PDF was read as page images, all eight pages, with page 5 (proceedings page 263)
re-read for the fact check. Every file was read with the file tools from bridge-staged copies; no
shell read repository content. No figure of this project's own measurement is restated (#17f,
D-431).*
