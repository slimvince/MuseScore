# EXTRACT — Harasim, Rohrmeier & O'Donnell, "A Generalized Parsing Framework for Generative Models of Harmonic Syntax" — Task B candidacy row 21, first pass, AT THE OBJECT

> **★ CORRECTED 2026-09-19 AT THE SECOND EXTRACT'S CROSS-CHECK, UNDER THE STANDING RULE OF HANDOFF
> ENTRY 198 §7** (the user's ruling of 2026-09-19: where a cross-check finds the first extract departing
> from the page and no value, finding or verdict moves, the site is corrected with the former wording
> preserved, without a new surface). The second extract is
> `reading_pass/extracts_second_pass/harasim-rohrmeier-odonnell-2018-a-generalized-parsing-framework-for-generative-models-of-harmonic-syntax.md`;
> its §9.3 lists the sites, (a) to (n), each marked below at its own place with a "★ CORRECTED" or
> "★ REMARKED" remark outside the quotation. **No printed value, no derived number, no Finding and no
> verdict was changed.** **Three sites inside Findings (4), (8) and (9) were NOT touched and stand with
> the user** — that file's §9.4. *(★ That last sentence was made stale by the ruling below and is left
> standing, #12.)*
>
> **★ AND ON THE USER'S RULING OF 2026-09-19, his words "I agree with recommendation A.", the three
> sites were corrected at their places** with the former wording preserved: Finding (4)'s two
> quotations now carry the page's words; Finding (8)'s third bullet is narrowed to what the pages
> describe and now carries the paper's word "semi-supervised", with "were never fitted" in its fourth
> bullet narrowed to match; Finding (9) reads "no second annotating group and no agreement figure
> reported". **No printed value, derived number or verdict was changed, and Finding (8)'s conclusion —
> intermediate, not established as held-out — stands.** The second extract records the ruling and the
> act at its §9.7.
>
> **STATUS: FIRST-PASS EXTRACT, READ WHOLE AT THE OBJECT (2026-09-12).** Written under
> `cowork_reading_pass_commission_2026_08_30.md` §4, whose form the remedial commission
> (`cowork_reading_pass_remedial_commission_2026_08_31.md` §3) binds unchanged. That remedial
> commission was read at the object this session at its §3, §4 and §5.
>
> **The grade.** All eight pages of the held PDF were read AT THE OBJECT: staged through the bridge
> and read with the file tools as page images, in two requests of four pages each — 1–4 and 5–8 — and
> a third request for pages 9–10 **that returned an out-of-range error naming eight, which is
> therefore the count: eight pages, printed 152–159.** The transcriptions below are of the paper's
> prose, its six figures and its reference list. **The paper contains no table of any kind.** **No
> relay, no web-fetch read, no prompted extraction.** No page was re-opened during the extracting;
> the whole reading of this extract against the paper is the last section of this file and records
> which pages it re-opened.
>
> **★ THE USER's OPENING INSTRUCTION FOR THIS SESSION ORDERS THAT THIS EXTRACT BE READ WHOLE AGAINST
> THE PAPER AND THE CITED LINES BEFORE THE SESSION CLOSES — a reading, not a search — and that what
> it found be written down in a section written AFTER the reading. That section is the last one in
> this file. Until it exists and says so, the reading is OWED and has not been run.**
>
> **Why this paper and its place in L2's slice.** It is row 21 of `reading_pass/candidacy_upgrades.md`
> (line 83, the file read whole this session), **ADMITTED outright** — not on the doubt default; that
> file's own list at line 157 is rows 13, 14, 16, 23, 39, 51 and 58, and the slice derivation's §5
> line 98 gives L2's six as 13, 14, 16, 23, 51 and 58, being "the verdict table's seven minus row 39,
> which is L1's and already read". **Row 21 is on neither list.** The candidacy row's reason is *"The
> parsing framework the hypothesis names; DP-O is open and this is a method for the thing it is open
> about."* `cowork_l2_task_b_slice_derivation_2026_09_05.md` §4 (line 68, the file read whole this
> session) places it in L2's slice: *"DP-O; §9 places what-follows-what inside L2's charter."* That
> placement carries no L3 bearing and no open placement question, unlike rows 34 and 40.
>
> It is the first member of group 6 of the proposed reading order (*"The grammar branch, against
> which DP-O stays open"*, rows 21, 22, 23, 25 and 34 at `candidacy_upgrades.md` line 180); row 22 is
> paywalled and not held (the derivation's §2). **When this row is read, what remains under L2's gate
> is rows 23, 25 and 34.**
>
> **Where the record bears on this paper, established by search and by reading, not inherited — and
> the sweeps ran over TWO DIFFERENT TREES, which is stated because the first writing of this banner
> ran them together and claimed a coverage the first sweeps did not have.**
>
> **(1) THE ORIGINAL IDENTITY AND FIGURE SWEEPS**, run with `Grep` over the repository files staged at
> that point — **named, not counted**: `CLAUDE.md`, `ARCHITECTURE.md`, `FRAMEWORK.md`, `STATUS.md`,
> `DECISIONS.md` and all twenty `decisions/group_*.md` files, `OPEN_ITEMS.md`,
> `docs/research_papers/BIBLIOGRAPHY.md`, `reading_pass/candidacy_upgrades.md`,
> `reading_pass/population.md`, `cowork_l2_task_b_slice_derivation_2026_09_05.md`,
> `cowork_reading_pass_findings_2026_08_31.md`, `reading_pass/l2_slice_reading_progress.md` and its
> companion, and the handoff entries numbered one hundred and eighteen, one hundred and fifty-two and
> one hundred and fifty-eight. **★ THAT TREE CONTAINED NO EXTRACT AT ALL** — the commission and the
> three extracts this side later opened were staged AFTER those sweeps ran, so no sweep result above
> reaches the extracts directory's CONTENTS, and the first writing of this banner listed two of those
> extracts as though they had been covered.
>
> **(2) THE CROSS-PRIMARY SWEEP, RE-RUN AT THE USER'S ORDERED FACT-CHECK OVER A TREE THAT DOES CONTAIN
> THEM.** Every file of `reading_pass/extracts/` was then staged and swept on `45.95`, `39.43`,
> `36.30`, `15.35`, `iRealPro`, `PACFG`, `tree accuracy`, `Harasim`, `O'Donnell`, `1173`, `90.61` and
> `34.52`. **Every hit returned is inside THIS extract and no other.** So the cross-primary result
> below is established over the whole extracts directory and not merely over the staged tree, which is
> a stronger position than the first writing could claim and is recorded as the correction it is.
>
> **The bound that still holds: the staged tree is NOT the repository**, and every claim below about
> where the record does and does not name this paper is bounded by the two lists above and is claimed
> no wider.

---

## Identity — a FULL-TITLE match on both registers; the proceedings running head, the pagination AND a CC BY 4.0 licence line all printed; three authors matching exactly; the input is a CHORD-SYMBOL SEQUENCE, narrower than a score

**Printed title, page 1, in capitals:** *A GENERALIZED PARSING FRAMEWORK FOR GENERATIVE MODELS OF
HARMONIC SYNTAX*. `docs/research_papers/BIBLIOGRAPHY.md` **line 35** carries *"A Generalized Parsing
Framework for Generative Models of Harmonic Syntax," ISMIR 2018*; `candidacy_upgrades.md` **line 83**
carries the same words in lower case. **Both are full titles, neither ends in an ellipsis, and the
printed title matches both word for word.** This is therefore a full-title check on both registers —
the shape (c⁶) said a prefix result would itself be a finding. **There is no prefix result and no
identity finding.** *(That rows 40, 58, 57 and 46 each had a title matching only as a prefix or
abbreviated is **RELAYED from the progress record's item (c⁶)**; this side opened none of those three
extracts and verified none of it. It is the contrast (c⁶) draws, not a finding of this read.)*

**Printed authors and affiliations, page 1:** *Daniel Harasim*^{1,2}, *Martin Rohrmeier*^{1,2},
*Timothy J. O'Donnell*^3 — 1 Digital and Cognitive Musicology Lab, École Polytechnique Fédérale de
Lausanne, Switzerland; 2 Institut für Kunst- und Musikwissenschaft, TU Dresden, Germany; 3 Department
of Linguistics, McGill University, Canada; contact `daniel.harasim@epfl.ch`. **Three authors, in the
order both registers give them, matching exactly.**

**Venue, pagination and licence, all printed.** The page-1 footer reads: *"© Daniel Harasim, Martin
Rohrmeier, Timothy J. O'Donnell. Licensed under a Creative Commons Attribution 4.0 International
License (CC BY 4.0). Attribution: Daniel Harasim, Martin Rohrmeier, Timothy J. O'Donnell. 'A
Generalized Parsing Framework for Generative Models of Harmonic Syntax', 19th International Society
for Music Information Retrieval Conference, Paris, France, 2018."* Pages 2–8 carry the running header
*"Proceedings of the 19th ISMIR Conference, Paris, France, September 23-27, 2018"*. The printed page
numbers run **152 to 159**.

**The tier question (c⁶) sets up, ANSWERED.** The bibliography's tier is **CC** and its URL is the
conference's own site (`ismir2018.ircam.fr`) rather than the ISMIR archive. **A licence line, a
running header and a pagination are ALL printed, and the licence printed is CC BY 4.0** — so the CC
tier AGREES with the printed licence and **errs in neither direction**. *(The three comparison
groups — rows 19, 20, 45, 47, 48, 57 and 58 agreeing; rows 30, 35, 49 and 50 with LINK tiers
understating a printed licence; rows 12, 13, 15, 16, 17, 51, 52 and 40 printing none — are **RELAYED
from the progress record's item (c⁶)** and were verified at no extract by this side. **What this read
establishes is this row's own answer and nothing about the others.**)* **A second thing the
conference-site URL settles:** what it yields is the PROCEEDINGS TEXT, not an author copy — the
running header and the pagination establish that at the object.

**The held file is `docs/research_papers/harasim_rohrmeier_odonnell_2018_ismir_generalized_parsing_harmonic_syntax.pdf`,
1,143,148 bytes** at this session's own staging result. That is the same figure the progress record's
item (c⁶) reports from the previous session's directory listing; the two agree.

**★ THE INPUT, ESTABLISHED AT PAGE 1 AND AGAIN AT §4 AND §6.1 — and the domain bound owed is not the
audio kind but a sharper one.** The paper's terminal symbols are **chord symbols**: *"a set of pairs
describing chords each of which consists of the root of the chord and a string describing the chord
form"* (§4). Its data are lead-sheet chord sequences from a Jazz-standard collection (§6.1). **The
paper never sees a score.** It has no notes, no durations, no spelling, no meter, no voices, no
audio, and no segmentation problem — the chord sequence arrives already segmented and already
labelled. **So no domain bound of the audio kind is owed; the bound that IS owed is that this method
consumes what L2 publishes rather than what L2 consumes**, and every statement below about what it
could be adopted for is written under that bound.

---

## What the paper is, in its own structure

Eight pages. §1 Introduction; §2 Overview of the approach; §3 Abstract Context-Free Grammars (§3.1
Definitions, §3.2 Parsing, §3.3 Inference of Rule Probabilities); §4 A Generative Model of Jazz
Harmony; §5 The Turnaround Problem; §6 Experiments (§6.1 Dataset, §6.2 Tree Accuracy Evaluation, §6.3
Performance Diagnosis using Scale Degree Frequencies); §7 Conclusion and Future Research; §8
Acknowledgements; §9 References.

**Six figures and no tables.** Figure 1, a hierarchical analysis of the A-part of *Afternoon in
Paris*; Figure 2, the parsing algorithm in the parsing-as-deduction framework; Figure 3, parsing the
turnaround of *All of me*; Figure 4, the tree-accuracy bar plot with error bars; Figure 5, predicted
tree accuracy per minibatch update; Figure 6, expected usage of scale degrees. **Across the eight
pages read whole, every quantitative result is in the prose or in Figures 4, 5 and 6** — that reading
is the object this claim is made at.

Three footnotes: the implementation as a publicly available Julia package
(`github.com/dharasim/GeneralizedChartParsing.jl`), and the two corpus URLs.

---

## The paper's own claims, labelled

*(★ REMARKED 2026-09-19, the second extract's §9.3 item (l), under the standing rule of handoff entry
198 §7; no word changed. **Bold type inside a quotation from the paper, in this section and in the
Findings below, is this extract's emphasis.** As the second reader read the pages, the paper's running
text sets none of the quoted words in bold; its own emphasis is italic, on terms it introduces.)*

### The formalism

- **[FACT — §3.1, Definition 1]** A (non-probabilistic) **Abstract Context-free Grammar** (ACFG) is
  `G = (T, C, C₀, Γ)`: a set `T` of terminal symbols, a set `C` of constituent categories, a set
  `C₀ ⊆ C` of start categories, and a set of **partial functions** `Γ := { r | r : C ⇸ (T ∪ C)* }`
  called rewrite rules or rewrite functions. Categories *"are allowed to be of any data type and the
  rules are generalized partial functions"*, so an ACFG *"can therefore take advantage of the
  algebraic structure of categories"* (§2).
- **[FACT — §3.1]** Derivation and language are defined as usual: `α ⟶_r β` when `α = α₁Aα₂` and
  `β = α₁r(A)α₂`, the leftmost category rewritten at each step; `D(α)` is the set of derivations of
  `α`; the language of `G` is the set of terminal sequences having a derivation in `G`.
- **[FACT — §3.1]** *"if `C` is finite, the languages that can be described by ACFGs are exactly the
  languages that can be described by standard context-free grammars"*, and a construction is given:
  divide each rewrite function of domain cardinality `k` into `k` standard rules,
  `R := ⋃_{r∈Γ} { (A, α) ∈ C × (T ∪ C)* | r(A) = α }`. **The expressive power is the same; what
  differs is the parameterisation.** This is the paper's own statement and it bounds every claim made
  for the formalism.
- **[FACT — §3.1, Definition 2]** A **Probabilistic ACFG** (PACFG) is an ACFG where each category
  `A ∈ C` is associated with a random variable `X_A` over rewrite functions `r`, with `P(X_A = r)`
  positive if and only if `r(A)` is defined. `p(d) = Π_{i=1..n} P(X_{A_i} = r_i)` and
  `p(α) = Σ_{d ∈ D(α)} p(d)`.
- **[FACT — §3.1, and this is the load-bearing property]** *"PACFG categories can share the same
  probability distribution over rewrite functions without rewriting to exactly the same right-hand
  sites. This important property allows us to model the structural relations between musical keys. We
  use this property in Section 4 to build a model that abstract chords sequences from their concrete
  scale by defining the probability that a rewrite function is applied to a scale degree
  independently of its key. **The sharing of probability mass between rules additionally reduces the
  number of free parameters of a PACFG model.**"*
- **[FACT — §3.1, the toy illustration]** With `C = {S, A, B}`, `T = {a, b}` and rules
  `S ⟶ A | B`, `A ⟶ A A | a`, `B ⟶ B B | b`, a classical PCFG shares no probability mass between
  `A ⟶ A A` and `B ⟶ B B`, so *"the grammar can learn something about `A ⟶ A A` when it observes
  `B ⟶ B B` and vice versa"* only under the PACFG's meta-rule `x ⟶ x x`.

### Parsing

- **[FACT — §3.2]** Parsing is *"the task of computing the distribution of parse trees conditioned on
  this sequence"*. Rather than converting the grammar to Chomsky normal form — which *"considerably
  blow up the grammar"* — the parser **transforms grammars on the fly during parsing**,
  following reference [18]. *(★ CORRECTED 2026-09-19 at the second extract's cross-check, its §9.3
  item (a), under the standing rule of handoff entry 198 §7. FORMER WORDING, PRESERVED (#12): the
  quotation read "might considerably blow up the grammar". The page prints no "might": "Since grammar
  transformations into Chomsky normal form considerably blow up the grammar".)* Each rule of the form `A ⟶ B₁ … B_k` becomes a set of **states** with a
  **transition function** `tran : S × (T ∪ C) → S` and a **completion function** `comp : S → 2^C`.
  *"the states and the transition function form a search trie where the completion function checks if
  there is a rewrite rule that has a sequence of terminal symbols and categories as its right-hand
  side."* *"the parser can handle any transition and completion functions derived from finite-state
  automata"*.
- **[FACT — §3.2, Figure 2]** The algorithm is stated as **parsing as deduction** (references [3] and
  [29]), with two kinds of atomic formula — **edges** `[s,i,j]` for `s ∈ S` (not yet completed
  constituents) and **constituents** `[A,i,j]` for `A ∈ C` — goal items `[A,1,|α|+1]` for `A ∈ S`,
  axioms `[α_i, i, i+1]`, and three deduction rules: *introduce edge*, *complete edge* and the
  *fundamental rule*.

### Inference

- **[FACT — §3.3]** A **Dirichlet** prior is placed on each category's probability vector,
  `θ⃗_{Γ_A} ~ Dirichlet(α⃗_{Γ_A})`, with **pseudocounts**; the posterior
  `p({θ⃗_{Γ_A}} | D, {α⃗_{Γ_A}}) ∝ p(D | {θ⃗_{Γ_A}}) p({θ⃗_{Γ_A}} | {α⃗_{Γ_A}})` is approximated by
  **variational Bayesian inference** with a **mean-field** approximation
  `q({θ⃗_{Γ_A}} | {ν⃗_{Γ_A}}) = Π_{A∈C} p(θ⃗_{Γ_A} | ν⃗_{Γ_A})`, minimising the KL divergence by
  **coordinate descent**, the optimal update being `ν⃗_{Γ_A} = α⃗_{Γ_A} + E_q[#(r, D)]`.
- **[FACT — §3.3]** The standard coordinate-ascent update needs expected counts over the **whole
  corpus**; the paper instead uses the **stochastic variational Bayes** algorithm of reference [9],
  updating with respect to randomly sampled **minibatches**. *"We make use of this stochastic
  variational Bayes algorithm in the results reported below."*

### The Jazz grammar (§4)

- **[FACT]** The section *"presents a PACFG `G = (T, C, C₀, Γ)` that models the syntax of Jazz harmony
  following the proposal in [24]"* — reference [24] being **Rohrmeier, "Towards a generative syntax of
  tonal harmony", Journal of Mathematics and Music 5(1):35–53, 2011**. *"That work addressed the
  problem of finding a restrictive grammar that describes the full variety of syntactic relations in
  the musical idiom of Jazz-standards."*
- **[FACT]** Terminals `T` are pairs of a chord **root** and a **chord form**, one of: *a major triad,
  a major-seventh chord, a major sixth chord, a dominant-seventh chord, a minor triad, a
  minor-seventh chord, a half-diminished-seventh chord, a diminished seventh-chord, an augmented
  triad, or a suspended chord* — **ten forms**.
- **[FACT]** Categories are **pairs of scale degrees and keys**: `C = Z₇ × K`, where a key is a pitch
  class for its root and a string for its mode, `K = Z₁₂ × {major, min}`. *"Scale degrees are denoted
  by roman numerals from I to VII. All categories with scale degree I are start symbols,
  `C₀ = {I} × K`."* *(★ CORRECTED 2026-09-19, the second extract's §9.3 item (b), same rule. FORMER
  WORDING, PRESERVED (#12): "Scale degrees are denoted by Roman numerals. All categories…" — the page
  prints "roman numerals from I to VII"; the quotation had dropped three words without a mark.)*
- **[FACT]** The rewrite functions, quoted as the paper defines them, for an arbitrary key `k`:
  - *prolongation*: `PROLONG(⟨x,k⟩) = ⟨x,k⟩ ⟨x,k⟩` for `x ∈ Z₇`;
  - *diatonic preparation*: `DIAT-PREP(⟨x,k⟩) = ⟨x + 4 mod 7, k⟩ ⟨x,k⟩` for `x ∈ Z₇ \ {IV}`;
  - *dominant preparation*: `DOM-PREP(⟨x,k⟩) = ⟨V, μ(x,k)⟩ ⟨x,k⟩` for `x ∈ Z₇ \ {I}`, where
    `μ(x,k)` denotes *"the modulation from `k` into the key of scale degree `x`"* (the paper's own
    example: `μ(II,(0,maj)) = (2,min)`, the key of the second scale degree of C major being D minor);
  - *plagal preparation*: `PLAGAL-PREP(⟨I,k⟩) = ⟨IV,k⟩ ⟨I,k⟩`;
  - *modulation*: `MODULATION(⟨x,k⟩) = ⟨I, μ(x,k)⟩`;
  - *mode change*: `MODE-CHANGE(⟨I,(r,m)⟩) = ⟨I,(r,min)⟩` if `m = maj`, `⟨I,(r,maj)⟩` if `m = min`,
    for `r ∈ Z₁₂`, `m ∈ {maj, min}`;
  - *diatonic substitution*: `DIAT-SUBST(⟨x,(r,m)⟩) = ⟨VI,(r,m)⟩` if `x = I, m = maj`;
    `⟨III,(r,m)⟩` if `x = I, m = min`; `⟨IV,(r,m)⟩` if `x = II`; `⟨VII,(r,m)⟩` if `x = V` —
    **the paper states the domain on the following page as `x ∈ {I, II, V}`, `r ∈ Z₁₂`,
    `m ∈ {maj, min}`**;
  - *dominant substitution*: `DOM-SUBSTᵢ(⟨V,(r,m)⟩) = ⟨V,(r + i mod 12, m)⟩` for `r ∈ Z₁₂`,
    `m ∈ {maj, min}`, `i ∈ {3, 6, 9}`.
- **[FACT]** *"Additionally, `Γ` contains appropriate termination rules `C ⇸ T` according to standard
  Jazz harmony theory (e.g. seventh-chord-termination(⟨4,(0,maj)⟩) = G⁷, see [20] for further
  explanation)."* Reference [20] is **Mark Levine, *The jazz theory book*, Sher Music, 1995**.
- **[FACT — and this is the property row 21 is cited for]** *"The distribution of `X_{⟨x,k⟩}` over
  rules rewriting the category `⟨x,k⟩` is defined as a categorical distribution such that
  `P(X_{⟨x,k⟩} = r) = P(X_{⟨x,k'⟩} = r)` for all scale degrees `x`, rules `r`, and keys `k, k'` that
  have the same mode. That is, the probability of `r` rewriting `⟨x,k⟩` does not depend on the root of
  `k` which enables the model to learn the parameters of its probability distributions
  key-independently."*
- **[FACT]** The rules are grouped into **three classes**: prolongation, preparation and substitution.
  *"Preparation rules create categories that for the listener generate the expectation to hear the
  prepared chord. Substitution rules substitute chords for other chords that fulfill an equivalent
  function inside the sequence such as tritone substitutions of dominants in Jazz."*

### The turnaround problem (§5)

- **[FACT]** *"A lead-sheet of a Jazz-standard consists of a melody together with a chord sequence
  describing the fundamental harmonic structure of the piece. The chord sequence is repeated multiple
  times in a performance."* Some lead sheets end in tonic chords; others include harmonic upbeats to
  the first chord of the piece at the end of the sheet, called **turnarounds**. *"The final chord of a
  performances is nevertheless usually a tonic chord."* The worked instance: *All of me* starts with
  `C△` and ends with the turnaround `E♭°⁷ Dm⁷ G⁷`.
- **[FACT]** *"The grammar of Jazz harmony proposed above assumes that pieces end with a tonic chord.
  Therefore, a simple implementation of this grammar would not able to parse lead-sheets that end
  in turnarounds. We solve this problem by cyclic parsing, meaning that we assume that constituents
  can have spans from the end of a piece back to the beginning, see Figure 3."* *(★ CORRECTED
  2026-09-19, the second extract's §9.3 items (c) and (d), same rule. FORMER WORDING, PRESERVED (#12):
  "would not be able to parse" — the page prints "would not able to parse", a slip of the paper's that
  the quotation had silently mended; and the quotation closed on a full stop after "back to the
  beginning", where the page's sentence runs on with ", see Figure 3.")*

### The experiments (§6)

- **[FACT — §6.1]** *"The model is evaluated using the iRealPro dataset of Jazz-standards. This
  dataset consists of **1173 chord sequences** electronically-encoded by the Jazz musician
  community including metadata such as the titles, composers, and keys. The sequences were collected
  and converted into the Humdrum format [10] by Daniel Shanahan and Yuri Broze [28], and are available
  online."* *"The chord forms in the iRealPro dataset include information about ninths and elevenths
  that are not considered in this study."* *(★ CORRECTED 2026-09-19, the second extract's §9.3 item
  (e), same rule. FORMER WORDING, PRESERVED (#12): "1173 chord sequences that are
  electronically-encoded" — the page prints no "that are".)*
- **[FACT — §6.1]** *"The subset of **394** Jazz-standards that consist of at most **40** chords was
  considered to train the models. **34.52% (136)** of these pieces were parsable using the standard
  approach and **90.61% (357)** pieces were parsable using the cyclic parsing approach described
  above. Less then 55% of the considered Jazz-standards therefore end in turnarounds."* *(The final
  sentence is an arithmetic residue; it is derived and stated below rather than repeated as a
  result.)* *(★ CORRECTED 2026-09-19, the second extract's §9.3 item (f), same rule. FORMER WORDING,
  PRESERVED (#12): "using the cyclic parsing approach. Less then 55%" — the page prints "approach
  described above."; two words had been dropped without a mark. No value is touched.)*
- **[FACT — §6.2]** *"We compare four models: (i) the proposed PACFG model that uses a representation
  of rules independent of key, (ii) its PCFG counterpart the rules of which are not independent of
  key, (iii) a baseline of randomly generated trees, and (iv) a right-branching baseline in which all
  constituents split into a constituent on the left and a terminal symbol on the right."*
- **[FACT — §6.2]** *"The models are trained on the **357** cyclic parsable sequences using minibatches
  of **8** sequences. They are evaluated on **13 pieces hand-annotated by the authors**."*
- **[FACT — §6.2, the metric, quoted because the record does not carry it]** *"We report the predicted
  tree accuracy. That is the precision of correctly predicted spans of internal tree nodes. A span of
  a tree node is defined as the start index of its leftmost leaf together with the end index of its
  rightmost leaf."*
- **[FACT — §6.2, Figure 4's caption and the text]** *"Figure 4 shows the means of the tree accuracies
  including **95% confidence intervals as error bars**."* *(★ REMARKED 2026-09-19, the second
  extract's §9.3 item (n), same rule; no word changed: the quoted sentence is §6.2's running text.
  Figure 4's caption, as the second reader read it, is "Tree accuracy plot" and says nothing of the
  error bars.)*
- **[FACT — §6.2, the four figures the record cites, quoted in the paper's own order]** *"The
  right-branching baseline performs at an accuracy level **under 10%**. The random baseline performs
  slightly better at an accuracy level of **15.35%**. Under a uniform prior, both the PACFG and the
  PCFG model perform at an accuracy level of **36.30%** a priori of the data. As opposed to the trained
  PCFG model that only improves its performance by about 3% (in comparison to the uniform prior)
  reaching an accuracy of **39.43%**, the trained PACFG model improves by about 10% (in comparison to
  the uniform prior) reaching an accuracy of **45.95%**."* *(★ REMARKED 2026-09-19, the second
  extract's §9.3 item (g), same rule; no word changed: the page prints no full stop between "15.35%"
  and "Under a uniform prior" — the one inside this quotation is this extract's.)*
- **[FACT — §6.2]** *"The PACFG model was thus able to learn more from the data than the PCFG model.
  Note that since the PCFG model does not abstract the grammar rules from the concrete key wherein
  they are applied, **the number of free parameters of the PCFG model is approximately 12 times higher
  than the number of free parameters of the APCFG model**."* *(The acronym is printed "APCFG" in this
  one sentence; see the residues below.)*
- **[FACT — §6.2]** *"Despite the fact that the PACFG model learns key-independently, it is still much
  simpler than models that produce state-of-the-art parsing results in computational linguistics."*
  Those models use *"larger tree fragments, conditioning on heads and/or adjacent elements in the
  string, state-splitting, and other richer contextual information"*, and the authors *"anticipate that
  the inclusion of similar structures into musical parsing models will lead to similar improvements in
  performance."*
- **[FACT — §6.2, Figure 5 and its caption]** Figure 5 gives the mean predicted tree accuracies per
  minibatch update for PACFG and PCFG; its caption states *"the y-axis displays only values between
  33% and 50%"*. The text: *"this figure is produced using a stochastic algorithm and is therefore
  inherently noisy. We see that the stochasticity of the inference algorithm leads to random jumps of
  the accuracy up to 0.5%. The models appear to do most of their learning in the first 10 minibatches."*
- **[FACT — §6.3, Figure 6]** Figure 6 gives *"the expected frequency of scale-degree use in the whole
  corpus"* by scale degree and mode. *"The scale degrees VI in major and III in minor are more
  frequently used by the model than expected."*
- **[CONJECTURE — §6.3, the authors' own hedges, quoted so the hedge travels]** *"Because these scale
  degrees are substitutions for the first scale degrees and because they enable modulations into the
  relative key (e.g. from C major to A minor and vice versa), **the model may be using them to
  alternate between relative keys**. The prominence of the VII in minor keys is **probably related to**
  the fact that it has a dominant-seventh chord form. **The model may be interpreting** a I in major as
  a III in the relative minor key that is then prepared by the VII in minor."* The worked derivation
  given for the transition `G⁷ C△` is `I_a ⟶ III_a ⟶ VII_a III_a ⟶ G⁷ III_a ⟶ G⁷ C△`. **Three hedged
  sentences and one derivation; no measurement of the alternation itself appears.** *(★ CORRECTED
  2026-09-19, the second extract's §9.3 items (h) and (i), same rule. FORMER WORDING, PRESERVED (#12):
  the quotation ended "as a III in minor." — the page's sentence reads "as a III in the relative minor
  key that is then prepared by the VII in minor."; and the derivation's subscripts were written "α"
  (`Iα ⟶ IIIα ⟶ VIIα IIIα ⟶ G⁷ IIIα ⟶ G⁷ C△`), where the second reader read an italic lower-case
  "a" at two requests of page 6, the key of the sentence before being A minor. Both are a language
  model's reads of a page image. **The same shortened words stand inside Finding (4) below; that site
  is NOT corrected here and stands with the user** — the second extract's §9.4.)* *(★ That last
  sentence was made stale by the user's ruling of 2026-09-19 and is left standing, #12: the site in
  Finding (4) is now corrected there.)*

### The paper's own statements about the field (§1 and §7)

- **[FACT — §1]** *"to the best of our knowledge there is currently no dataset of hierarchically
  analyzed chord sequences by human experts that could serve for the training or the evaluation of
  models of harmonic syntax. As a consequence, there exist no comparisons of models of harmonic syntax
  against expert analyses."* *(★ CORRECTED 2026-09-19, the second extract's §9.3 item (j), same rule.
  FORMER WORDING, PRESERVED (#12): "for the training and the evaluation" — the page prints "or".)*
- **[FACT — §1]** The earlier work's bounds, as the paper states them: applications to *"monophonic
  melodic data [21]"*; *"a corpus of 39 blues chord progressions with a maximum of 24 chords per
  progression [12]"*; *"a dataset of 76 chord progressions (avg. length 40) from Jazz-standards that
  was restricted to subsequences of pieces that did not change key [4]"*. And: *"All these
  earlier approaches assume the knowledge of the key of the pieces a priori."* *(★ CORRECTED
  2026-09-19, the second extract's §9.3 item (k), same rule. FORMER WORDING, PRESERVED (#12):
  "monophonic Schenkerian data [21]" and "restricted to subsequences of pieces that did not end in
  turnarounds [4]". The second reader read "monophonic melodic data [21]" and "did not change key [4]"
  at two requests of page 2. **These two change what the paper is reported to say about earlier work**;
  the second reader found no value, Finding or verdict of this file resting on either, by a search of
  this file for both phrases, which met them at this bullet alone.)*
- **[FACT — §1]** *"At present, there are music databases of simplified Schenkerian analyses [13],
  syntactic analyses of melodies based on the generative theory of tonal music [8], and annotated
  harmonic functions [4]."*
- **[FACT — §7]** *"To the best of our knowledge, this is the first computational approach that
  automatically performs hierarchical analyses of chord sequences and evaluates them on analyses by
  human experts."*
- **[FACT — §7, and it is the load-bearing sentence for the uncertainty question]** *"Our future
  research will in particular focus on expanding the dataset of hand-annotated expert analyses **to
  provide significance tests of the performance comparison of different models**, for example."*
- **[FACT — §7]** Further named future directions: *"unsupervised grammar induction, joint models of
  multiple musical levels of musical structure like harmony and rhythm, and models of musical
  structure that have more complex dependencies than those representable in simple tree structures."*
- **[FACT — footnote 1]** *"The implementation of the algorithms developed in this study are publicly
  available as a package of the Julia programming language [1]"* —
  `https://github.com/dharasim/GeneralizedChartParsing.jl`. *(★ REMARKED 2026-09-19, the second
  extract's §9.3 item (m), same rule; no word changed: the quoted sentence is running text of §1 on
  page 2; footnote 1 is the URL alone.)*

---

## ★ Arithmetic residues and textual residues, DERIVED here, with their cause NOT established

**(1) The turnaround share. The paper's stated bound is smaller than its own two printed percentages
imply, and nothing in the paper explains it.** From §6.1: 394 pieces considered, `136` parsable by the
standard approach, `357` parsable cyclically. The pieces parsable only cyclically are `357 − 136 =
221`, which is `221 / 394 = 56.09%`; equivalently `90.61% − 34.52% = 56.09%`. The paper writes *"Less
then 55% of the considered Jazz-standards therefore end in turnarounds."* **56.09% is more than 55%,
so the stated bound is not the difference of the two printed rates.** Read instead against the
cyclically-parsable set alone the figure is `221 / 357 = 61.90%`, which is further from 55% still.
**No derivation in the paper reaches 55%**, and the cause is not established here. *(The sentence also
prints "Less then" for "less than"; that is a spelling slip and is recorded only so a later search on
the phrase does not miss it.)*

**(2) The two parsability rates themselves CHECK.** `136 / 394 = 34.518%`, which rounds to the printed
`34.52%`; `357 / 394 = 90.609%`, which rounds to the printed `90.61%`. **Both hold.**

**(3) The two "improvement" figures are percentage-POINT differences printed with a percent sign, and
both check as such.** `45.95 − 36.30 = 9.65`, described as *"about 10%"*; `39.43 − 36.30 = 3.13`,
described as *"about 3%"*. **Read as relative improvements they would be 26.6% and 8.6%**, so a reader
taking the paper's "%" as relative would be wrong by a factor of about three. Recorded because the
record's own sentence quotes the endpoint figures and not the improvements, so nothing in the record
is affected — but a later reader going back to the paper meets the ambiguity.

**(4) One acronym is inverted in the sentence that states the parameter ratio.** The model is named
**PACFG** in the abstract, in §3.1 Definition 2, throughout §4, in §6.2's model list and in the
legends of Figures 4 and 5. In the sentence *"the number of free parameters of the PCFG model is
approximately 12 times higher than the number of free parameters of the **APCFG** model"* it is
printed **APCFG**. The paper defines no such object. **Read in context it can only be the PACFG**, but
it is the one sentence carrying the parameter ratio, and the ratio is the paper's own explanation of
why the PACFG learns more.

**(5) Reference [22] prints "Towars a syntax of the Classical cadence"** for Neuwirth & Rohrmeier
2015. Recorded only because a later text search on *"Towards a syntax"* over this reference list would
return nothing.

**(6) Two counts DERIVED at the paper's own definitions and NOT stated by the paper, each with its
bound.** From `C = Z₇ × K` and `K = Z₁₂ × {major, min}`: the category set has `7 × 24 = 168` members
and the start set `C₀ = {I} × K` has `24`. From ten chord forms and twelve roots the enumerated
product of terminals is `120`. **The first two are exact given the definitions; the third is an upper
bound on the enumerated product** — the paper says `T` is *"a set of pairs"* of those kinds and does
not state that the product is full or that every root occurs. **None of the three is the paper's own
figure and none is used as one below.**

---

## ★ THE VERIFICATION — every figure the STAGED TREE attributes to this paper, checked at the paper; NO VALUE MOVED

**THE THREE FIGURES occur at exactly two ratified places in the staged tree** — established by a
FIGURE sweep on `45.95`, `39.43`, `36.30`, `15.35`, `iRealPro`, `PACFG`, `ACFG`, `right-branching` and
`right branching`, **with every hit read at its line**. *(★ Corrected at the user's ordered
fact-check: the first writing of this sentence said the staged tree cites the paper at exactly two
ratified places, which **contradicts Finding (4) of this same file** — `decisions/group_A.md`'s
**D-526** is a third ratified place and it names this paper's first author. What is true of exactly
two places is the FIGURES; the paper is NAMED or reached at more, and the identity sweep is what
found the others.)*

- **`FRAMEWORK.md` §9, DP-O, lines 790–800** — *"For it: a recursive grammar represents something a
  flat sequence cannot — the same surface chords functioning simultaneously as a tonic phrase in one
  tonality and a dominant phrase in another — with tree accuracy 45.95% against 39.43% for a
  non-transposition-tied grammar and under 10% for a right-branching baseline. Against it: … [FACT —
  both.]"*
- **`FRAMEWORK.md` §S6, DP15, lines 1861–1872** — *"Harasim and colleagues show that a recursive
  grammar represents something a flat sequence cannot: the same surface chords functioning
  simultaneously as a tonic phrase in one tonality and a dominant phrase in another, with tree
  accuracy 45.95% against 39.43% for a non-transposition-tied grammar and under 10% for a
  right-branching baseline. [FACT.]"*

**Nowhere else in the staged tree do the figures occur**, except in `l2_slice_reading_progress.md`'s
own block for this row and in the hundred-and-fifty-eighth handoff entry, both of which report the
record's citation rather than ratify it. The strings `iRealPro`, `PACFG` and `ACFG` occur **nowhere at
all** in the staged tree.

### Figure by figure

| The record's words | At the paper | Verdict |
|---|---|---|
| *"tree accuracy 45.95%"* | §6.2: *"the trained PACFG model … reaching an accuracy of 45.95%"* | **VERIFIED** |
| *"against 39.43%"* | §6.2: *"the trained PCFG model … reaching an accuracy of 39.43%"* | **VERIFIED** |
| *"under 10% for a right-branching baseline"* | §6.2: *"The right-branching baseline performs at an accuracy level under 10%"* | **VERIFIED**, and the wording is the paper's own |
| *"for a non-transposition-tied grammar"* | §6.2 model (ii): *"its PCFG counterpart the rules of which are not independent of key"*; §3.1 names the shared-mass property as modelling *"the structural relations between musical keys"*; §4 fixes it as independence of the root of `k` | **VERIFIED as a fair rendering.** Transposition-tying and key-independence of the rule distribution are the same property at this paper |
| *"tree accuracy"* as a term | §6.2: *"We report the predicted tree accuracy"* | **VERIFIED as the paper's own term** |
| (What it is OF — the record does not say) | *"the precision of correctly predicted spans of internal tree nodes"*, a span being the start index of the leftmost leaf with the end index of the rightmost leaf, **on 13 pieces hand-annotated by the authors** | **ESTABLISHED**, and it is not in the record |
| DP15's attribution *"Harasim and colleagues"* | The three named authors | **VERIFIED** |
| The marker placement (b⁶) reports | At **DP-O** the single marker *"[FACT — both.]"* stands after the *Against it* half and covers both halves; at **DP15** each half carries its OWN *"[FACT.]"*, the for-half's marker standing immediately after the three figures | **VERIFIED, and sharpened**: the two homes tag the same sentence differently, and at DP15 the figures carry a marker of their own |
| DP-O naming nobody where DP15 names the authors | The identity sweep on *Harasim*, *O'Donnell* and *Rohrmeier* returns **no hit between FRAMEWORK.md lines 790 and 800**, and a hit at line 1862 | **VERIFIED** |

**No value moved, and no figure is wrong.** Every number the record quotes is at the paper, in the
paper's own words. **The act that establishes this is a comparison of the two quoted sentences and
their four figures against §6.2, and it reaches no further** — no wider collation of the record's
citations was made, and none is claimed.

---

## ★ FINDING (1) — A CORRECTED STRUCTURAL CLAIM CANDIDATE on DP-O's and DP15's live `[FACT]` text: the three figures do not measure the claim they are attached to

**What the record's sentence does.** At both homes the sentence joins, with the word *"with"*, (a) a
representational claim — *"a recursive grammar represents something a flat sequence cannot — the same
surface chords functioning simultaneously as a tonic phrase in one tonality and a dominant phrase in
another"* — and (b) three measured figures. A reader takes the figures as the measurement of the
claim. **They are not.**

**What is the paper's and what is not.**

- The **illustration** is the paper's, in its own words, at §1 on Figure 1's analysis of *Afternoon in
  Paris*: *"Note that the subsequence Cm⁷ F⁷ B♭△ is both a tonic progression in B♭ major and a dominant
  progression in E♭ major. It forms a dominant phrase in A♭ major together with B♭m⁷ and E♭⁷."*
  **That sentence is about one analysis of one A-part. It is not a claim about what a flat sequence
  can or cannot represent, and nothing in the paper measures it.**
- **Across the eight pages read whole**, the phrase **"something a flat sequence cannot"** does not
  appear in any form, and **no flat sequence model appears at all.** The four models compared at §6.2
  are: a PACFG, a PCFG, randomly generated **trees**, and a right-branching **tree** baseline. **All
  four produce trees.** No HMM, no CRF, no n-gram, no latent-category sequence model and no flat
  tagger occurs in those eight pages, which is the object this claim is made at and the only one.
- The **39.43% comparand is another context-free grammar** over the same rule set, differing from the
  winner in one property only: whether the rule distribution is shared across keys. **So the
  45.95-against-39.43 comparison measures transposition-tying — parameter sharing — and not recursion
  against flatness.**
- The **right-branching baseline** is the only comparand that could be read as "flat", and it is a
  degenerate **tree** with no learned grammar at all, sitting below even the random-tree baseline
  (15.35%). *"under 10%"* is the cost of having no model, not the cost of being a sequence model.

**Why this is structural and not cosmetic.** DP-O's own falsifier, immediately below the sentence, is
*"a tree model beating a matched-capacity **sequence** model on this repertoire's ground truth"*. **DP-O
itself sets up a tree-versus-sequence axis, and then puts on its *for* side three figures that lie on
a different axis entirely.** A reader who arrives at the falsifier from the for-side sentence will
take the 45.95/39.43 pair as partial evidence toward it. It is not evidence toward it at all.

**THE OTHER READING, STATED IN FULL so the user can take it.** The word *"with"* can be read as merely
conjoining two things the paper offers — a representational point it illustrates, and a measured
result it reports — without asserting that the second measures the first. On that reading nothing in
the sentence is false: the illustration is the paper's, the figures are the paper's, the attribution
of the 39.43 comparand is accurate, and the sentence claims no causal link. **What would still be
true under that reading is that the sentence supplies no evidence on DP-O's own axis while appearing
to**, which is a case for adding a clause rather than for striking one.

**WHAT IT DOES NOT DO, and this decides the STOP question.** The commission's §5, **read at the object
this session and quoted whole rather than by its branch (D-643)**, makes a STOP of *"A CORRECTED
structural claim **that moves a design-point verdict**; a derived upgrade the environment cannot fetch
or read; anything requiring an act outside §0's licence; and any point at which the derivation cannot
place a paper."* **DP-O's verdict is NONE CHOSEN — underived: open, and this finding leaves it exactly
there** — if anything it makes the openness better founded. **NO STOP FIRES**, and the ground is the
commission's own words.

*(The progress record's item (b⁶) states the STOP condition as "a corrected structural claim on that
ground is a STOP". That is wider than the commission's text, which requires the correction to MOVE a
verdict. Applied literally, (b⁶)'s wording would have fired a STOP the commission does not order. This
is recorded at Finding (3) below.)*

---

## ★ FINDING (2) — DP-O's FALSIFIER is NOT met by this paper, on four independent counts, and (e⁶) asks that this be said either way

**DP-O's falsifier, quoted from `FRAMEWORK.md` line 798–800:** *"a tree model beating a
matched-capacity sequence model on this repertoire's ground truth, measured on the same corpus and the
same axis."* **DP15's is the same sentence at line 1870–1872.**

| The falsifier's condition | At this paper | Met? |
|---|---|---|
| *a tree model* | The PACFG is one | **Identified, not a condition on its own** — the falsifier's subject is a tree model *beating* a matched-capacity sequence model, so this cell names the subject and settles nothing |
| *beating a matched-capacity **sequence model*** | There is **no sequence model in the comparison at all** — the three comparands are a PCFG, random trees and right-branching trees | **No** |
| *matched-capacity* | Explicitly the opposite: *"the number of free parameters of the PCFG model is approximately 12 times higher"* — the winner is the **smaller** model by roughly an order of magnitude | **No**, and the mismatch is stated by the paper itself |
| *on **this repertoire's** ground truth* | Jazz standards from the iRealPro collection, as lead-sheet chord symbols | **No** |
| *measured on the same corpus and the same axis* | Tree-span precision over internal nodes, against 13 pieces annotated by the authors | **No** — neither the corpus nor the axis is this project's |

**ANSWERED: the paper that supplies DP-O's for-side figures does not supply DP-O's falsifier.** That
extends to this row the finding `cowork_reading_pass_findings_2026_08_31.md` §DP-O already records for
the population rows in DP-O's territory — *"Not one supplies the falsifier DP-O names"* — to the one
object that section does not cover, which is the source of DP-O's own cited numbers.

**And a sharper way to put it, because it is the useful half.** The falsifier asks for a comparison
this paper could not have run in 2018 on its own account: §1 states *"there exist no comparisons of
models of harmonic syntax against expert analyses"*, and §7 names expanding the annotated set as
future work. **The absence is a property of the field at the time, not an omission of the paper.**

---

## ★ FINDING (3) — DP-O's figures were NOT verification targets under the commission's own words, and the carried block says the opposite

Established at two objects this session:

- **`cowork_reading_pass_remedial_commission_2026_08_31.md` §4, "What is NOT in scope, stated so it is
  not drifted into":** *"**The underived design points' figures (DP-N, DP-O).** The findings surface
  records that verifying those would be a widening the user must order. **He has not.** Leave the flag
  standing."*
- **`cowork_reading_pass_findings_2026_08_31.md` line 628–630:** *"**DP-O's and DP-N's figures are
  underived points' evidence and were NOT verification targets** under the commission's own words.
  *Widening that is the user's to order.*"*

**Against that, `reading_pass/l2_slice_reading_progress.md` item (b⁶) says "★★ ROW 21 HAS A
VERIFICATION TARGET, AND THIS SIDE ESTABLISHED IT AT THE OBJECT", and the hundred-and-fifty-eighth
handoff entry's headline says "ROW 21 IS NEXT AND — unlike rows 40, 57 and 58 — IT HAS A VERIFICATION
TARGET".** Both name exactly DP-O's and DP15's three figures.

**What is and is not wrong here, stated carefully rather than as a blanket.** Reading row 21 whole is
squarely in scope: Task B orders every admitted row read whole. The figures are in the paper, so a
whole reading meets them whether or not anyone aimed at them, and checking them costs nothing extra.
**What §4 excludes is ordering a verification of DP-O's figures as an act of its own** — a widening.
No such act was ordered here and none was taken as one; the check above is a by-product of the
reading, the flag stays standing, and **no ratified text is amended by anything in this file.**

**What IS wrong is the label.** Calling this row's read a verification with a target, and attaching the
STOP machinery to it, contradicts the commission's §4 and the findings surface's own sentence. A later
session taking (b⁶) at its word would believe DP-O's figures are a commissioned target. **They are
not, and the correction belongs at the record rather than at the paper.** *(That this session ran the
check before establishing §4's exclusion is declared in the handoff entry's departures block, not
hidden here.)*

---

## ★ FINDING (4) — An ADDITION CANDIDATE to D-526's ground: the record's transposition-pooling device, measured, by name, for the first time in this slice

**The identity sweep found a live place the carried block did not name.** `decisions/group_A.md` line
570, **D-526**, *"The joint state's chord axis is SCALE-DEGREE-VALUED — a Roman numeral relative to the
state's own tonic and mode — and the chord symbol is a DERIVED fact published once"*, **LIVE · decided
2026-07-19 · ratified by user**, home `cowork_joint_estimator_architecture.md` §5a. Its verbatim reads,
in part:

> *"The joint state's chord axis is a **Roman numeral — scale degree, quality, inversion — relative to
> the state's tonic and mode** (the Raphael-Stoddard / **Harasim** structure). Consequences, all
> structural: … (b) **transposition invariance**: the chord-transition table pools all keys' evidence
> (twelvefold counts per cell — the decisive capacity device on a 326-piece corpus)"*

**This is a live, user-ratified decision that names Harasim by name and whose consequence (b) rests on
the same PRINCIPLE this paper measures — key-independent parameter tying — IN DIFFERENT MACHINERY.**
*(★ Narrowed at the user's ordered fact-check: the first writing said *"exactly the device this paper
measures"*, which contradicts this finding's own domain bound three paragraphs below. D-526(b) pools
counts across keys in a **chord-transition table inside a decode**; the paper shares a **rule-probability
distribution across keys in a context-free grammar**. Same idea, different object — and the whole
weight of the bound below is that the transfer between them is not established.)*
`decisions/group_A.md` line 669, **D-532** (LIVE · 2026-07-19
· ratified by user), leans on the same device in its own defense — *"The pooling reuses the mechanism
the table design already rests on — **counting the same pattern across transpositions**"*.

**Neither entry carries a measurement of the device itself.** D-526's *Why* argues it from the
excluded alternatives (a root-valued state *"forfeits the transposition pooling"*) and from capacity
on a 326-piece corpus; D-532's argues that the pooling adds no new kind of machinery. **This paper
supplies the measured comparison the record does not hold:** the same grammar with and without
key-independent rule probabilities, `45.95%` against `39.43%` on tree-span precision, with the untied
model carrying **approximately twelve times** the free parameters.

**THE BOUND TRAVELS WITH IT, and it is large.** Jazz lead-sheet chord symbols, not a score; a
context-free grammar's rule probabilities, not a chord-transition table inside a decode; tree-span
precision, not root or key agreement; 13 evaluation pieces annotated by the paper's own authors; and
no significance test (Finding 7). **Nothing here establishes that the gain transfers to a transition
table on 326 chorales, and no such claim is made.**

**★ AND ITS COUNTERWEIGHT, RECORDED WITH IT RATHER THAN LEFT OUT, because an addition candidate that
carries only the favourable half is the defect this record keeps finding.** The same paper diagnoses a
characteristic **failure** of the same device: §6.3 reads Figure 6's over-use of VI in major and III in
minor as the model possibly *"using them to alternate between relative keys"* and *"interpreting a I in
major as a III in the relative minor key that is then prepared by the VII in minor"* — a pathology of
key-independent sharing, since the shared rule set makes the relative pair cheap to swap. *(★ CORRECTED
2026-09-19 ON THE USER'S RULING of that date, "I agree with recommendation A", taken on the three
sites of the second extract's §9.4. FORMER WORDING, PRESERVED (#12): the two quotations read
"alternating between relative keys" and "interpreting a I in major as a III in minor", neither of
which the page prints in that form; "possibly" is added outside the quotation to carry the page's
"may be". The Finding's sense is unchanged.)* **The authors state it three times with hedges — *may be*, *probably*, *may be* — and
measure none of it, so it is CONJECTURE and is labelled so above.** Whoever takes the favourable figure
takes this beside it.

---

## ★ FINDING (5) — An ADDITION CANDIDATE to the ground under L2's candidate-admission boundary condition and under R-5: the measured cost of a too-strict admission rule

**The charter's clause, quoted from `FRAMEWORK.md` §5, L2, lines 421–424:** *"**The rule that admits a
candidate to the search is part of this layer's specification and carries its own defense.** A rule
that decides which readings a span may even consider bounds every claim the analysis can make about
its own ceiling, because a reading the search cannot reach cannot be found however good the scoring
is."* **R-5** (line 920–922) states the same thing as a risk: *"Where such a rule exists without a
recorded basis, no ceiling claim over that search is established."*

**Both state a principle and neither carries a measurement of it.** This paper is a measured instance,
and a stark one. Its grammar assumes pieces end on a tonic chord. Under that assumption **34.52% (136)
of the 394 considered pieces are parsable at all**; with the assumption relaxed by cyclic parsing,
**90.61% (357)** are. **So relaxing that one clause of the admission rule made 56.09 percentage points
of the corpus reachable that had not been** — `90.61 − 34.52`, derived here — **and no amount of
scoring could have recovered any of it, which is the charter's own sentence, measured.** *(The
remaining `9.39` points are unparsable under both approaches; **the paper does not say why**, and that
residue is NOT attributed to the tonic-final clause here.)*

**Bound:** jazz lead sheets, a context-free grammar's coverage, and a binary parsable/not-parsable
outcome rather than an accuracy. **This is an on-paper instance of the principle, not a transfer of a
figure.** `reading_pass/population.md` row 5 (HarmTrace) records the converse half — *"the measured
lesson that unrestricted modulation rules explode the parse space"* — and is **TO FETCH and not
read**, so the two halves of the admission-rule question sit one read and one unread.

---

## ★ FINDING (6) — A RIVAL SHAPE for DP-E that the record's rival list does not hold

**DP-E, quoted from `FRAMEWORK.md` lines 712–714:** *"**CHOSEN: with the chords; a tonality change is
located at a harmonic boundary.** *Rival:* an independent tonality track changing anywhere, which the
output the analysis must produce cannot express."* **Two shapes are recorded: the chosen one and one
rival.**

**This paper's shape is a third.** The key is a component of **every category** (`C = Z₇ × K`), so
every node of the tree — internal nodes included — carries a key. A modulation is the application of a
rewrite function (`MODULATION`, `MODE-CHANGE`, `DOM-PREP` through `μ(x,k)`) **at an internal node**,
and therefore attaches to a **constituent spanning many chords** rather than to a boundary between two
of them. **It is neither "located at a harmonic boundary" nor "an independent track changing
anywhere": it is a tonality attached to a hierarchical constituent.**

**Why it is worth recording rather than dismissing.** DP-E's stated ground for excluding its one rival
is that the required output *cannot express* it. **That ground does not reach this third shape**,
because a key attached to a constituent can be read down to the chords the constituent spans and
written inline exactly as the ground truth writes it. **Whether that makes it a live rival is not
decided here** — it is an addition to the rival record, applied nowhere, and DP-E stays chosen.

---

## ★ FINDING (7) — The uncertainty practice (mandatory item (h)), and it is the one place the record's `[FACT]` is exposed

**What the paper prints.** **No table of any kind**, in eight pages. Its one display of the headline
comparison is **Figure 4**, a bar plot, and the paper states in terms that it *"shows the means of the
tree accuracies including **95% confidence intervals as error bars**"*.

**What the paper does NOT print.** **No numeric confidence interval anywhere in the text**; no standard
error; no sample-size statement beyond *"13 pieces"*; **and no significance test of any comparison.**

**And the authors say so themselves.** §7: *"Our future research will in particular focus on expanding
the dataset of hand-annotated expert analyses **to provide significance tests of the performance
comparison of different models**, for example."* **That is the paper declaring, in its own voice, that
the comparison the record's `[FACT]` rests on is not significance-tested.**

**What this extract does NOT claim.** Item (h) forbids reading a difference off a figure by eye as a
result, and that bar binds here too: **the drawn error bars for the two posterior conditions appear to
overlap, and that observation is recorded as a reading of a plot and is used as no result.** The
load-bearing leg is the authors' own sentence, not the picture.

**Where this row sits in the practice inventory — and the inventory is RELAYED, not verified here.**
The practices recorded for rows 45, 48, 49, 50, 52, 46, 51, 15, 12, 13 and 40 are **taken from the
progress record's item (h), which is a SUMMARY of those rows' extracts and not the extracts
themselves.** Item (e) of that same file says in terms: *"Cite what you take from an extract to that
extract, never to a summary of it."* **This side opened the extract of no row named in that list**, so the
comparison below is relayed and a later session that needs any of it goes to the extract. As item (h)
relays them: row 45, no spread but a p-value per comparison; rows 48, 49, 50, 52, 46 and 51, no spread
and no test; row 15, bootstrap 95% intervals per column on both tables with per-cell significance
marked; row 12, none anywhere; row 13, no table at all and no spread on its only figure; row 40, no
confidence interval but two Fisher's exact tests and raw counts.

**What IS established here, at this paper, is row 21's own practice:** intervals PRINTED but only as
error bars on a plot, never as numbers, with the significance test named by the authors themselves as
work not yet done. **Whether that shape is new to the inventory is not asserted**, because the
inventory it would be new to was not opened.

---

## ★ FINDING (8) — Fit and evaluation (principle #20), stated exactly rather than as a violation or as a clean split

- **No held-out split is declared anywhere in the paper**, and the words *held-out*, *validation*,
  *train/test split* and *cross-validation* do not appear.
- The models are **trained on the 357 cyclic-parsable sequences** and **evaluated on 13 pieces
  hand-annotated by the authors**. **Whether those 13 are among the 357 is not stated.**
- **But as the pages describe the training, the graded labels are not an input to it.** Training as
  §3.3 and §6.2 describe it is over chord sequences — variational inference on rule probabilities
  given `D` — and **the pages describe no tree entering training**. The hand annotations appear in the
  pages as the evaluation target. **The paper's §1 nevertheless calls the training "semi-supervised"
  (*"We train the model in a semi-supervised fashion on a dataset of Jazz-standards"*, printed page
  153) and the pages do not say what the supervised part is.** *(★ CORRECTED 2026-09-19 ON THE USER'S
  RULING of that date, "I agree with recommendation A" — the second extract's §9.4 item 2. FORMER
  WORDING, PRESERVED (#12): "**But the graded labels are unseen by construction.** Training is over
  chord sequences only — variational inference on rule probabilities given `D` — and **no tree is ever
  an input to training**. The hand annotations exist only as the evaluation target." That wording was
  firmer than the pages, and this file did not carry the paper's word "semi-supervised".)*
- **So the position is intermediate and should be reported as such:** the *labels* the accuracy is
  measured against are not described as fitted *(★ same correction; FORMER WORDING, PRESERVED (#12):
  "were never fitted")*, which — if that description is whole — is stronger than a
  fitted-and-self-measured number; the
  *sequences* they are measured on are not declared disjoint from the training set, which is weaker
  than a declared held-out protocol. **Under #20 the headline rates are not established as held-out
  figures, and they are not the fitted-and-self-measured class either.**
- The capacity side is stated for one model only: the PCFG's free parameters are *"approximately 12
  times"* the PACFG's. **No parameter count, no capacity budget and no corpus-size justification is
  given for either.**

---

## ★ FINDING (9) — The ground truth is the authors' own, and the paper says so

The evaluation set is *"13 pieces hand-annotated by the authors"*. **One annotating group, which is the
group that built the model; no second annotating group and no agreement figure reported; n = 13.**
*(★ CORRECTED 2026-09-19 ON THE USER'S RULING of that date, "I agree with recommendation A" — the
second extract's §9.4 item 3. FORMER WORDING, PRESERVED (#12): "no second annotator; no agreement
figure". The page says the pieces were annotated "by the authors", of whom there are three; how many
of them annotated each piece is not stated.)* §7 calls this *"the
first computational approach that automatically performs hierarchical analyses of chord sequences and
evaluates them on analyses by human experts"* — **the human experts being the authors themselves.**

**Recorded, not graded, and applied nowhere.** It is principle #21's shape in another repertoire: a
measured accuracy whose ceiling is an unmeasured annotation. **It supplies nothing to
`OPEN_ITEMS.md` OI-179 or to principle #21's ceiling, which are about Roman-numeral and key annotation
of Baroque/classical symbolic music** — this is hierarchical constituent annotation of jazz lead
sheets, a different object on a different repertoire, and no bridge between them is asserted.

---

## Cross-primary check (mandatory item (j)) — NOTHING TO RUN ON, established; two false-positive families named

The paper's own reported quantities are: `1173`, `394`, `40` chords, `136`, `34.52%`, `357`, `90.61%`,
`8` (minibatch), `13` (evaluation pieces), `<10%`, `15.35%`, `36.30%`, `39.43%`, `45.95%`, `12×`
(parameters), `0.5%` (stochastic jitter), `10` (minibatches), and the `33–50%` axis bound. Its corpus
is **iRealPro**; its metric is **predicted tree accuracy**.

**The sweep terms actually run are these and no others** — `45.95`, `39.43`, `36.30`, `15.35`, `1173`,
`90.61`, `34.52`, `iRealPro`, `PACFG`, `ACFG`, `jazz-standard`, `tree accuracy`, `Afternoon in Paris`,
`All of me` and `turnaround`. **The bare small integers in the list above (8, 13, 10, 40) were NOT
swept**, being too common to carry a result.

**★ AND THE ESTABLISHMENT WAS REPAIRED AT THE USER'S ORDERED FACT-CHECK, BECAUSE THE FIRST ONE DID NOT
REACH THE OBJECT IT CLAIMED.** That first sweep ran over the staged tree, **and the staged tree at that
moment contained NO extract of any row** — so *"no read row reports on any of them"* was not
established by it at all, the read rows' extracts having been nowhere in the swept files. **The
repair: every file of `reading_pass/extracts/` was staged and the sweep re-run** on `45.95`, `39.43`,
`36.30`, `15.35`, `iRealPro`, `PACFG`, `tree accuracy`, `Harasim`, `O'Donnell`, `1173`, `90.61` and
`34.52`. **Every hit it returned lies inside THIS extract and no other.**

**So there is NOTHING TO RUN ON, and it is now established over the whole extracts directory** rather
than over a tree that could not have shown a hit — **the same outcome rows 51, 15, 12, 13 and 40
reached, whose members are named here rather than counted, and whose outcomes are RELAYED from the
progress record's item (j) rather than read at their extracts.**

**Two false-positive families, recorded so nobody merges them:**

1. **`1173`** occurs in the staged tree as a LINE NUMBER — `ARCHITECTURE.md:1173` at
   `decisions/group_C.md:141` and at `decisions/group_D.md:15` and `:29`. It is a home coordinate, not
   a corpus size.
2. **`tree accuracy`** occurs at `FRAMEWORK.md:793` and `:1864` and in `l2_slice_reading_progress.md`.
   **Every one of those is this project quoting THIS paper.** They are the record's own citation, not
   another read row reporting a comparable figure.

---

## Reference-list check (mandatory item (i)) — thirty-two works, EXACTLY ONE in this project's bibliography, NONE with an extract, and two Rohrmeier traps

`reading_pass/extracts/` was listed through the bridge before anything below was called unread, as
item (i) requires.

**The reference list holds thirty-two entries, derived at the pages** — `[1]`–`[14]` in page 158's left
column, `[15]`–`[27]` in its right column, `[28]`–`[32]` on page 159.

**EXACTLY ONE is in `docs/research_papers/BIBLIOGRAPHY.md`:** reference **[24] Martin Rohrmeier,
"Towards a generative syntax of tonal harmony", *Journal of Mathematics and Music*, 5(1):35–53, 2011**
— **bibliography line 36**, and **candidacy row 22**, tier PAYWALL, **not held**. **NOT ONE of the
thirty-two has an extract**, checked at the extracts listing.

**★ AND THE ONE HIT IS THE INTERESTING PART.** Candidacy row 22's recorded reason is *"The grammar
DP-O's *for* side rests on. **Paywalled and not held.**"* §4 of this paper opens: *"This section
presents a PACFG … that models the syntax of Jazz harmony **following the proposal in [24]**."* **So
row 21 is row 22's own authors' implementation, parameterisation and evaluation of row 22's grammar.**
The unheld paper DP-O's for side rests on is reached, at one remove, through the held one — **its rule
classes, its category type and its termination convention are set out in §4 of a CC-licensed paper this
project holds.** Nothing is carried out of row 22 itself and no equation is attributed to it.

**★ TWO ROHRMEIER TRAPS, each a near-miss that a name-match would merge:**

1. Reference **[23] is Rohrmeier, "A generative grammar approach to diatonic harmonic structure",
   *SMC'07*, 2007.** **Candidacy row 23 is Rohrmeier, DCRR-004, 2006, "Towards Modelling Harmonic
   Movement".** **Different year, different venue, different title.** **This paper does not cite
   candidacy row 23.**
2. Reference **[25] is Rohrmeier & Cross, "Tacit tonality: Implicit learning of context-free harmonic
   structure", ESCOM 2009.** **Candidacy row 24 is Rohrmeier & Cross, ICMPC 2008, "Statistical
   Properties of Tonal Harmony in Bach's Chorales".** **Different year, venue and subject.** **This
   paper does not cite candidacy row 24 either.**

**Two further near-misses where an extract exists for a DIFFERENT work by overlapping authors:**

- Reference **[6] de Haas, Magalhães & Wiering, ISMIR 2012** and **[7] de Haas, Rohrmeier & Wiering,
  ISMIR 2009** — the extracts directory holds
  `dehaas-magalhaes-wiering-veltkamp-2013-harmtrace-functional-analysis.md`, which is **neither**: a
  2013 work with a fourth author.
- Reference **[8] Hamanaka, Hirata & Tojo, "Musical Structural Analysis Database Based on Gttm",
  ISMIR 2014** — the extracts directory holds
  `hamanaka-hirata-tojo-2013-computational-music-theory-gttm.md`, a **2013** work with a different
  title. `cowork_reading_pass_findings_2026_08_31.md` §DP-O names that 2013 work as population row 19,
  read whole at the object. **The 2014 database paper is a different object and is not held.**

**Everything else on the list is outside this project's population**, and is named rather than
generalised: Julia and variational-inference works ([1], [2], [9], [11], [32]); parsing and
computational-linguistics works ([3], [14], [17], [18], [29]); Humdrum ([10]); Schenkerian and GTTM
work ([13], [19], [21]); jazz-corpus and jazz-theory work ([4], [5], [12], [20], [26], [27], [28],
[30], [31]); Koops, Magalhães & de Haas FARM 2013 ([16]); Koelsch, Rohrmeier, Torrecuso & Jentschke
PNAS 2013 ([15]); Neuwirth & Rohrmeier 2015 ([22]). **Nothing is carried out of any of them.**

---

## Where this paper sits against the shapes the slice already holds (item (g))

**Row 21 adds NO new shape to the length-term question, and could not.** It carries **no segment
variable, no boundary variable and no length term at all**: its leaves are chords that arrive already
individuated, and a constituent's span is derived from leaf indices after the fact. **It decides
nothing about where one harmony gives way to the next** — that is given to it. Row 12's reason in
another key: it is not a model of segment formation at all.

**Where it DOES add a shape is on the other axis — what follows what.** Against the transition
machinery the slice already holds, it is the first member that answers *what follows what* with a
**recursive constituent structure over the whole sequence** rather than with a Markov or semi-Markov
transition. And it is the first to answer the sparsity problem that machinery has by **parameter
tying** rather than by back-off:

- **`D-533` (LIVE · 2026-07-19 · ratified by user, read at its home this session)** answers sparsity by
  **proportional back-off** — a row's leftover mass divided in proportion to each chord's overall
  frequency.
- **This paper answers it two other ways at once**: **tying the rule distribution across all keys of a
  mode** (§4), and **Dirichlet pseudocounts** in the variational posterior (§3.3).
- **It measures the first and not the second**, and it does not compare either against back-off.

**So three shapes for one problem, and the record holds one.** Recorded as an addition to the rival
record for the transition-table sparsity question; **no candidate against D-533 is raised**, since
tying and back-off are not exclusive.

---

## Coupling facts (mandatory)

- **Assumes upstream:** a **completed, ordered sequence of chord symbols**, each a (root, chord form)
  pair over ten forms, with ninths and elevenths discarded at intake. **No notes, no durations, no
  spelling, no meter, no voices, no key** — the abstract says the model performs *"key finding on the
  fly"*, so the key is not an input. **This is far LESS than this project's L0, and it is not what L1
  publishes either: it is what L2 publishes.**
- **Hands downstream:** a **parse tree** over that chord sequence whose internal nodes are
  `(scale degree, key)` categories — therefore a key at every node and a hierarchical constituent
  structure — together with rule probabilities learned key-independently. **No chord-tone assignment,
  no boundaries over notes, no elaboration relation, no chord symbol it did not receive.** The parser
  computes a distribution over parses and the chart is *"a compact representation of the forest of all
  trees for a given input sequence"*, but **what the evaluation grades is one predicted tree**, and the
  paper publishes no rival set with mass.
- **Stated scope:** *"the syntax of Jazz harmony following the proposal in [24]"*, evaluated on
  Jazz-standard lead sheets. The paper makes no claim about classical repertoire, about scores, or
  about any axis other than tree-span precision.

---

## What an L2 detail specification could adopt, adapt, or must argue against

**Could ADOPT.**

- **Key-independent parameter tying over a categorical rule distribution.** This is D-526's consequence
  (b) and D-532's reused mechanism, here with a measured comparison behind it (Finding 4).
- **Dirichlet pseudocounts with a mean-field variational posterior** as the smoothing route, beside
  D-533's back-off (item (g) above). **Unmeasured in this paper.**

**Could ADAPT.**

- **The `(scale degree, key)` category type.** It is D-526's chord axis under another name, with the
  tonality carried in the state rather than beside it.
- **A transition term over what-follows-what derived from a grammar**, folded into L2's score rather
  than standing as a layer above it. **That is precisely what DP-O's own sentence points at** —
  *"what chord follows what is inside L2's charter, and a grammar is one way to answer it"*.

**Must ARGUE AGAINST, and this is the decisive half.**

- **The input contract.** A grammar over chord symbols **cannot answer three of L2's four questions**.
  L2's sentence asks *"what is the tonality at each moment, where does each harmony give way to the
  next, which sounding notes belong to the harmony and which elaborate it, and what chord is read over
  each span?"* **This method receives the answers to the second and fourth, has no access to the data
  the third needs, and answers only the first — and it answers that only as a by-product of parsing.**
  Adopting it whole would mean stacking it on top of a committed chord stream, which is the pipeline
  shape §S4(e) excludes and which L3's boundary contract forbids from revising L2.
- **The evaluation axis.** Tree-span precision grades the hierarchy, not the reading. **A model can
  score well on it while getting every chord wrong, because the chords are given.** No figure of this
  paper can be compared with any agreement figure this project uses.
- **The measured pathology of the device it recommends** (Finding 4's counterweight): relative-key
  alternation is the authors' own conjectured explanation of a measured over-use, and the cause they
  name is the sharing itself.

---

## Centrality

**CENTRAL.** Two grounds, each established at an object this session:

1. **Ratified `[FACT]` text at two homes rests on this paper and nowhere else.** `FRAMEWORK.md` §9's
   DP-O and §S6's DP15 carry three figures that are this paper's, established by a figure sweep over
   the staged tree with every hit read at its line; DP15 attributes them by author name. **All three
   are re-verified here and none moved** — and the reading found that what the sentence attaches them
   to is not what they measure (Finding 1), which is a claim an L2 detail specification or any future
   hierarchy proposal would carry.
2. **A LIVE, user-ratified decision names this paper's first author and leans on the device it
   measures.** D-526 (2026-07-19) calls the joint state's chord axis *"the Raphael-Stoddard / Harasim
   structure"* and makes transposition pooling *"the decisive capacity device on a 326-piece corpus"*;
   D-532 reuses the same mechanism. **Neither carries a measurement of it. This paper is one.**

**THE GROUND ON WHICH *NOT CENTRAL* COULD BE ARGUED, STATED IN FULL SO THE USER CAN FLIP IT:**

- **DP-O and DP15 are UNDERIVED and OPEN.** No chosen design point rests on this paper, so nothing
  ratified changes if the paper is wrong. The framework's §11 R-6 lists DP-O among the three underived
  points and says none is filled with the most plausible reading.
- **The commission puts these figures out of scope** (Finding 3): §4 excludes verifying DP-N's and
  DP-O's figures as a widening the user has not ordered, and the findings surface states they were
  never verification targets. **On the commission's own terms this paper carries no load the pass is
  obliged to check.**
- **The domain is the furthest from this project's of any row read in this slice.** Not a score at
  all: chord symbols, jazz, thirteen pieces annotated by the model's own builders, one axis that
  measures hierarchy rather than harmony.
- **Its method cannot be adopted for L2's decision as it stands**, for the reason set out above — it
  consumes what L2 publishes.
- **It is not the sole primary for D-526's attribution.** That parenthetical names *"Raphael-Stoddard /
  Harasim"*, and row 1 (Raphael & Stoddard 2003) is read, extracted and CENTRAL already.

**If CENTRAL stands, a second independent extraction is OWED** under the original commission's §4, and
none has been performed.

**★ A PRECISION on D-526's parenthetical, graded as a precision and not as a correction, with the other
reading stated.** D-526 names the chord axis as *"a Roman numeral — **scale degree, quality,
inversion** — relative to the state's tonic and mode (the Raphael-Stoddard / Harasim structure)"*. **At
this paper the category is a two-component pair `(scale degree, key)`.** The chord **form** lives on the
**terminal**, not in the category, and **inversion appears nowhere in the paper at all.** So of the
three components D-526 names, **the Harasim half of the attribution supports the first only.** *The
other reading:* the parenthetical names two sources jointly and may be attributing only the
key-relative-degree idea to Harasim, with quality and inversion coming from Raphael & Stoddard — in
which case it is exact. **Row 1's extract is on disk and was NOT opened by this side, so which reading
holds is not established here.** Applied nowhere.

---

## ★ Findings about this project's own record, found by this row's sweeps, routed and applied nowhere

- **(A) The `Rohrmeier` ambiguity is wider than item (c⁶) states, and `Harasim` is ambiguous too.**
  (c⁶) records a four-way ambiguity — candidacy rows 21, 22, 23 and 24 — and treats *Harasim* as
  distinctive. At the staged tree, **Rohrmeier** also co-authors **candidacy rows 54 and 55** (the
  Annotated Beethoven Corpus and the Annotated Mozart Sonatas), **`population.md` rows 1, 2, 3, 4**
  (the McLeod & Rohrmeier family) and **row 18**, and is named inside other rows' reference-list
  reports (*Hedges & Rohrmeier 2011*, *Lieck & Rohrmeier*). **Harasim** co-authors **candidacy row 54**
  and **`population.md` row 18**. **A bare hit on either name must be read at its line; neither is a
  four-way question.**
- **(B) `population.md` rows 17 AND 18 carry a status cell that is ACCURATE on the axis it names and
  SILENT on the axis that has moved.** *(★ Re-graded at the user's ordered fact-check. The first
  writing said flatly that row 18's cell is STALE, and that over-reached: the cell's own vocabulary is
  about FETCHING, and on that axis it is right.)* **The position, established at four objects.** Both
  rows read *"TO FETCH · **CENTRAL**"* (lines 83 and 84). **Neither work has a file at
  `docs/research_papers/`**, which this side listed — so **on the fetch axis both cells are correct.**
  **But an extract exists for each** —
  `reading_pass/extracts/sapp-2005-visual-hierarchical-key-analysis.md` and
  `…/viaccoz-harasim-moss-rohrmeier-2023-wavescapes.md`, **both opened by this side at the user's
  fact-check**, each dated 2026-08-30 and each bounded by its own banner as read *"via one prompted
  extraction call"* rather than fetched — **and the same file's own line 428 states *"All three are
  now read at a primary."*** So what the two rows do not carry is that the work has been READ and
  extracted; the cell has no field for it. **Whether that is a defect of the cell or of the row's
  vocabulary is NOT settled here.** *(The precedent is row 48's finding that `population.md` row 13's
  status cell is STALE against the record's own extract — **RELAYED from
  `reading_pass/l2_slice_reading_progress_companion.md` line 795, which this side read at its line;
  row 48's own extract was not opened.** That row's cell read TO IDENTIFY-THEN-FETCH and the
  identification HAD happened, which is a stronger case than these two, where only the reading has.)*
- **(C) `cowork_reading_pass_findings_2026_08_31.md` §DP-O states a count that does not match the row
  numbers beneath it.** Line 406 reads *"**Four rows** sit in DP-O's territory"*. The bullets beneath
  it name **five row numbers** — 19, 5, 17, 18 and 6 — **in FOUR bullet points**, rows 17 and 18
  sharing one. **Derived at the list, not taken from another count.** *(★ The alternative reading,
  stated at the user's ordered fact-check because the first writing asserted the defect flatly: the
  author may have counted BULLETS and written *"rows"*. On that reading the arithmetic is sound and
  the word is wrong; on the other the word is right and the count is wrong. **Which it is is not
  established here**, and either way a reader who takes "four" as a count of rows is misled.)*
- **(D) The record holds a jazz collection with hierarchical TREE annotations, and this paper says no
  such dataset existed when it was written.** **D-367** (LIVE · 2026-07-03 · ratified by the user,
  read at its home) names, as its founding counter-example, *"the JHT entered for the harmonic idiom
  study and its hierarchical TREE annotations (a distinct GT layer, lever R-7's footing)"*; **D-497**'s
  preserved former verbatim names *"JHT/McGill-class corpora already inventoried by the idiom study"*.
  **No staged file expands the abbreviation**, and **this side asserts nothing about what it is.**
  Beside it: this paper's §1 states that in 2018 no dataset of hierarchically analysed chord sequences
  by human experts existed, and its §7 names building one as the route to significance-testing exactly
  the comparison DP-O's `[FACT]` rests on. **Whether the project's own JHT is that dataset is NOT
  established here and is a question for a later session.** If it is, the project already holds the
  material this paper's own future-work sentence asks for — **on jazz, which is not this repertoire,
  so it would still not be DP-O's falsifier.**
- **(E) §S4's seven enumerated decompositions contain no hierarchical cut.** `FRAMEWORK.md` §S4 (a)
  through (g), read whole this session, enumerates the candidate decompositions; **a hierarchical or
  grammar-based cut is not among them**, and the question is carried separately at DP-O/DP15 and named
  at §14.2 as *"A hierarchical/grammar layer. Not adopted and not foreclosed"*. **Recorded as an
  observation about where the question lives, not as a defect** — §S4 enumerates cuts of the layer
  decomposition, and DP-O is a question inside L2's charter rather than a cut of it.

---

## Verification targets touched

**No V-numbered target originates here, so far as the staged tree reaches** — established by the
figure sweep, which returns this paper's figures at `FRAMEWORK.md` DP-O and DP15 only and at no
V-numbered item, and by the findings surface's own statement at line 628 that **DP-O's figures are
underived points' evidence and were never verification targets**. *(The V-numbered list V1–V13 is the
findings surface's own; this side did not enumerate it and does not assert its membership.)* The three
figures were nevertheless checked at the paper as a by-product of the whole reading, and **all three
hold** (the verification table above). **No V-numbered target moves.**

---

## What this extract does NOT do

It amends **no** ratified text. It applies **no** finding. It moves **no** design point: **DP-O stays
NONE CHOSEN and open**, and Finding (1) makes its openness better founded rather than moving it. It
raises **no** STOP, on the commission's §5 read at the object. It takes **no** verdict on D-526,
D-532, D-533, DP-E, R-5 or the L2 candidate-admission clause — each finding is an addition candidate or
a precision put to the user. It opens **no** other paper: rows 22, 23, 25 and 34 are untouched, row 1's
extract was not opened, and nothing is carried out of any work this paper cites. It performs **no**
widening of the kind the commission's §4 reserves to the user.

---

## ★ What the whole reading of this extract found — written in the act that ran it

*(How it was run, so a later reader can judge it. The file above was read end to end in two calls,
against the two page-image results of the paper already open in this session and against the lines it
cites — `FRAMEWORK.md` 237–271, 386–493, 669–817, 918–952, 1029–1052, 1133–1155, 1547–1658,
1777–1885 and 1943–1985; `decisions/group_A.md` 548–592 and 667–730; `decisions/group_Q.md` 44–73;
`cowork_reading_pass_remedial_commission_2026_08_31.md` §3, §4 and §5; and
`cowork_reading_pass_findings_2026_08_31.md` 404–451 and 618–631. No page of the PDF was re-opened; the
two page-image results were still whole in this session and were re-read rather than recalled. **An
absolutes sweep was then run as its own act**, with `Grep`, over this file's own writing on *only*,
*never*, *nowhere*, *every*, *all*, *none*, *independent*, *resolved*, *complete*, *exhaustive* and
*always*, and every hit was read at its sentence.)*

**★ The reading found SEVEN things, and every one is about this side's own writing rather than about
the paper. All seven are corrected above at their own sentences.**

1. **★ THREE RELAYS WERE WRITTEN AS THOUGH ESTABLISHED, AND THE THIRD BREAKS A RULE THE PROGRESS
   RECORD STATES IN TERMS.** (i) The claim that rows 40, 58, 57 and 46 had prefix or abbreviated
   titles; (ii) the three tier-agreement groups naming fourteen rows; and (iii) **the whole
   uncertainty-practice inventory for rows 45, 48, 49, 50, 52, 46, 51, 15, 12, 13 and 40**. **All
   three come from the progress record's items (c⁶) and (h) — summaries — and this side opened not one
   of those extracts.** The third is the worse: item (e) of that same file says *"Cite what you take
   from an extract to that extract, never to a summary of it"*, and names it as an error a previous
   session made at four sites. **Each is now marked RELAYED at its own sentence**, and the
   uncertainty section now separates what this read established at this paper from what it relayed.
2. **★ A COUNT TAKEN FROM AN EARLIER TEXT'S COUNT PLUS ONE — the exact move cadence 5 forbids, and
   the user's opening instruction names.** The cross-primary section said the nothing-to-run-on
   outcome was *"reproduced a sixth time in this slice"*, having just named five rows. **That is five
   plus one and nothing else.** **The count is struck and the members are named instead**, and their
   outcomes are marked relayed from item (j).
3. **★ AN OVER-ATTRIBUTION IN FINDING (5), AND IT INFLATED THE FINDING.** The first writing said *"a
   reading of about two-thirds of the corpus was unreachable because of one clause of the admission
   rule"*. **Derived at the paper's own two rates, the clause accounts for `90.61 − 34.52 = 56.09`
   percentage points, not for the full `65.48` unparsable under the standard approach** — the
   remaining `9.39` points are unparsable under BOTH approaches and **the paper does not say why**.
   **The finding now states the 56.09 figure and expressly declines to attribute the residue.**
4. **A cell of the falsifier table that read as a condition satisfied.** *"a tree model | The PACFG is
   one | **Yes**"* sat at the head of a table whose other four cells read **No**, so it read as one of
   five conditions met. **The falsifier's subject is a tree model *beating* a matched-capacity
   sequence model; the first cell identifies the subject and settles nothing.** The cell now says so.
5. **A claim wider than the act that produced it, at the verification.** *"No transcription error"*
   reached the record's citations generally, where the act was a comparison of two quoted sentences
   and their four figures against §6.2. **Narrowed to the act.**
6. **An omission at the object, found by reading the rule list against page 5 again.** `DIAT-SUBST`'s
   stated domain — *"for `x ∈ {I, II, V}`, `r ∈ Z₁₂`, `m ∈ {maj, min}`"*, printed at the top of page 5
   after the formula runs over the page break — **was not in the rendering.** It is now.
7. **The sweep list implied more was swept than was.** Eighteen quantities were listed as the paper's
   own and a sweep was then reported, where fifteen terms were actually run and the bare small
   integers (8, 13, 10, 40) were not. **The section now names the terms run and says which were not.**

**And the absolutes sweep, run as its own act, found FOUR claims wider than the object that produced
them**, each narrowed at its sentence: *"Every quantitative result the paper reports"* and *"does not
appear in the paper in any form"* and *"no flat sequence model appears anywhere in the paper"* — all
three now name **the eight pages read whole** as the object they are made at; and the heading *"every
figure the record cites"* now reads *"every figure the STAGED TREE attributes to this paper"*, because
the sweep ran over the staged tree and the staged tree is not the repository. **The remaining
absolutes were read at their sentences and left standing**: those about the paper have the eight pages
as their object, those about this file's own acts (*applied nowhere*, *none was taken as one*) are
claims about acts this side performed, and those inside quotations are the quoted source's.

**★ What the reading checked and did NOT change.** The three verified figures were compared against
§6.2 a second time and stand: `45.95`, `39.43`, `under 10%`. The DP-O and DP15 quotations were
compared against `FRAMEWORK.md` at their lines a second time and are verbatim, **including the
differing `[FACT]` marker placement**, which was the thing most likely to have been rendered from
memory and was not. The commission's §5 and §4 quotations were compared against that file a second
time and stand. **Findings (1) through (9) and (A) through (E) survive the reading with their grounds
unchanged; what moved is how far three of them claim to reach.**

---

### ★★ A SECOND CHECK, USER-ORDERED AFTER THIS FILE HAD ALREADY LANDED — and it found what the first one passed

*(The user ordered a fact- and source-check of everything this side had written, for completeness,
coherence, correctness and misuse of absolutes. It ran against the LANDED copy, re-staged for the
purpose, and it is written up in the act that ran it. **It found FIVE things, and not one of them
overlaps with what the whole reading found.**)*

1. **★ AN INCOHERENCE BETWEEN TWO SECTIONS OF THIS FILE — the worst of the five.** The verification
   section opened *"The staged tree cites this paper at exactly two ratified places"*, while Finding
   (4) two sections later reports **a third ratified place, D-526, naming this paper's first author**.
   **The file contradicted itself.** What is true of exactly two places is the FIGURES; the paper is
   named at more, and the identity sweep is what found them. Corrected at the sentence.
2. **★ A SWEEP CLAIMED OVER AN OBJECT IT NEVER TOUCHED.** The banner listed the extracts for row 40
   and for `population.md` row 18 among the files the identity and figure sweeps ran over. **Those
   extracts were staged AFTER those sweeps ran.** The consequence is larger than the banner: **the
   swept tree contained no extract at all**, so the cross-primary section's *"no read row reports on
   any of them"* rested on files that could not have carried such a report. **Repaired by doing the
   work rather than narrowing the claim** — every file of `reading_pass/extracts/` was staged and the
   sweep re-run, and every hit lies inside this extract. **The claim is now stronger than it was and
   is established over the whole directory.**
3. **★ A FINDING RE-GRADED DOWNWARD, AND A SECOND ROW ADDED TO IT.** Finding (B) said flatly that
   `population.md` row 18's status cell is STALE. **On the axis the cell names — fetching — it is
   accurate**: `docs/research_papers/` holds no file for that work, which this side had already
   listed and did not read against its own claim. What is unrecorded is the READING, and the cell has
   no field for it. **Re-stated, and extended to row 17**, whose extract this side had declined to
   claim anything about and has now opened. **The row-48 precedent it leans on is marked RELAYED** —
   read at the companion's line 795, not at row 48's extract — **and is a stronger case than either of
   these two**, its cell having also said IDENTIFY where identification had happened.
4. **AN ALTERNATIVE READING OMITTED.** Finding (C) asserted flatly that the findings surface's *"Four
   rows"* is contradicted by its own bullets. The bullets name five row numbers **in four bullet
   points**, rows 17 and 18 sharing one — **so the author may have counted bullets.** Both readings
   are now stated; which holds is not established here.
5. **AN OVER-FIRM EQUATION BETWEEN TWO MACHINERIES.** Finding (4) called D-526(b) *"exactly the device
   this paper measures"*, three paragraphs above its own bound that the transfer is not established.
   **D-526(b) pools counts in a chord-transition table inside a decode; the paper shares a
   rule-probability distribution in a context-free grammar.** Narrowed to the principle they share.

**And two things this check looked at and did NOT change.** The reference-list result was re-derived
against `BIBLIOGRAPHY.md` entry by entry and **EXACTLY ONE of the thirty-two stands**; the two
near-misses were re-checked at the two extracts' own title lines and both are confirmed to be
DIFFERENT works by overlapping authors — the de Haas file is the 2013 HarmTrace article with a fourth
author, and the Hamanaka file is a 2013 chapter on computational music theory, not the 2014 database
paper reference [8] names.

---

**★ And one thing NEITHER reading could check, stated rather than left as a silent hole.** This side's
reading of its own extract is not a second reader. **The two sittings before the hundred-and-fifty-eighth
each had a user-ordered check find what the writer's own reading passed, and that entry's own reading
passed a defect its landing check then caught.** **Necessary, not sufficient** — and the landing check
on this file is a separate act, performed after this section was written and reported in the handoff
entry rather than here.
