# EXTRACT — Sutton & McCallum, "An Introduction to Conditional Random Fields for Relational Learning" — Task B candidacy row 16, first pass, AT THE OBJECT

> **STATUS: FIRST-PASS EXTRACT, READ WHOLE AT THE OBJECT (2026-09-11).** Written under
> `cowork_reading_pass_commission_2026_08_30.md` §4, whose form the remedial commission
> (`cowork_reading_pass_remedial_commission_2026_08_31.md` §3) binds unchanged.
>
> **The grade.** All thirty-five pages of the held PDF were read AT THE OBJECT: staged through the
> bridge and read with the file tools as page images, in requests of at most four pages. **No relay, no
> web-fetch read, no prompted extraction.** **The page count was ESTABLISHED rather than assumed** — a
> request for page 100 was refused by the tool with *"PDF has 35 pages"*. **The held document prints
> its own page numbers 1–35 in its running heads**, so every location below is the printed page
> together with the section, equation, figure or table number as printed; the chapter text runs pp.
> 1–30 and the reference list pp. 31–35.
>
> **★ THE SUBJECT IS NOT MUSIC AT ALL** — a machine-learning tutorial on conditional random fields,
> whose one experiment of its own is information extraction from e-mailed seminar announcements.
> **Nothing musical appears anywhere in the thirty-five pages.** Every claim carried out of it crosses
> a domain boundary and says so at the point of use, in the bound row 11's extract wrote first and was
> read at its own line 379 this session: *"so the formalism paper is not later cited as if it measured
> the musical gain."* Rows 15's, 12's, 13's and 14's extracts follow that wording; the rows are named
> and no count is asserted. *(That those four follow it is this session's reading of rows 15's, 12's
> and 13's extracts whole — fact-checked this same sitting — and of row 14's extract at its line 87,
> found by search and read at the line; it is a reading and not a relay.)*
>
> **★ THIS READ IS NOT A VERIFICATION.** No `[FACT]`, no ratified figure and no characterisation of
> any kind rests on this paper — established at the objects below, not inherited. It is **rows 51's,
> 12's, 13's and 14's shape, not row 15's**. What the read is for is the derivation's own question: the
> candidacy row admits this paper **ON THE DOUBT DEFAULT** with the reason *"A tutorial rather than a
> primary method. Admitted because the record does not settle whether it carries the inference and
> fitting detail the three CRF rows above leave out."* **That question is answered at finding (1), and
> the answer is YES, BY EXPLANATION AND NOT BY MEASUREMENT** — which is the case the progress record's
> item (d′) said to watch for.
>
> **This extract derives no specification statement, amends no document, opens no code and writes no
> open-items row or decisions-register entry.**

---

## The verification target — THERE IS NONE, and this is how that was established

**The record bears on this paper in exactly THREE places:**

| # | Place | Line | What it says |
|---|---|---|---|
| 1 | `reading_pass/candidacy_upgrades.md`, the candidacy row | 78 | *"Sutton & McCallum 2006, an introduction to conditional random fields"*, held ✓, **ADMITTED, ON THE DOUBT DEFAULT** — *"A tutorial rather than a primary method. Admitted because the record does not settle whether it carries the inference and fitting detail the three CRF rows above leave out."* |
| 2 | `cowork_l2_task_b_slice_derivation_2026_09_05.md`, the placement row | 63 | **L2** (doubt default) — *"Admitted for the inference and fitting detail rows 10–12 leave out; the same L2 detail."* |
| 3 | `docs/research_papers/BIBLIOGRAPHY.md` | 30 | *Sutton & McCallum, "An Introduction to Conditional Random Fields for Relational Learning," 2006*, a `people.cs.umass.edu` URL, held ✓, tier **LINK** |

**`FRAMEWORK.md` IS NOT ONE OF THEM.** Nor is `reading_pass/population.md`, nor
`cowork_reading_pass_findings_2026_08_31.md`. **The candidacy row was read at the row and
`candidacy_upgrades.md` was read WHOLE this session**, as its own next-in-order block instructed; so
were the slice derivation whole and the bibliography whole.

**How that was established — the legs NAMED, each with what it actually returned.** The tree searched
is the set of repository files staged into this session — `FRAMEWORK.md`, `reading_pass/population.md`,
`cowork_reading_pass_findings_2026_08_31.md`, `reading_pass/candidacy_upgrades.md`, the L2 slice
derivation, `docs/research_papers/BIBLIOGRAPHY.md`, `reading_pass/l2_slice_reading_progress.md`,
`STATUS.md`, `DECISIONS.md`, the extracts of rows 11, 12 (both files), 13, 14 and 15, and the
hundred-and-eighteenth, hundred-and-thirty-sixth, hundred-and-fifty-first and hundred-and-fifty-second
handoff entries — and it is named so that "the staged tree" is not read as the whole repository.

1. **IDENTITY sweep**, case-insensitive, for the bare words `Sutton`, `McCallum`, `tutorial`,
   `introduction`, `relational learning` and the bare year `2006`. **Over `FRAMEWORK.md` it returned ONE
   hit, the heading "1. Introduction and purpose" at line 112, which is not this paper.** Over
   `population.md` and the findings surface it returned only a 2006-versus-2007 remark about a different
   paper's year (population line 437, findings line 681). Every `McCallum` hit elsewhere was read at its
   line: row 12's own three places and extracts, row 13's acknowledgment of him, and the row-16 tracking
   in the progress record — **none names this paper except the three places above and the progress
   record's own next-in-order block for this row**, which was written by the previous session and is
   this line's tracking, not the record. *The trap the block named — McCallum is an author of row 12 —
   arose exactly as predicted and caught nothing.*
2. **FIGURE and MECHANISM sweep, run AFTER the paper was read**, from what it actually reports:
   `skip-chain`, `skip chain`, `seminars`, `BFGS`, `belief propagation`, `clique template`,
   `within-network`, `3.8 million`, and the values `89.7`, `90.2`, `90.6`, `80.4`, `77.3`, `76.9`,
   `30.2`, `24.7`, `121.6`. **It reached no citation of this paper anywhere.** *Three false-positive
   families are recorded so nobody merges them:* `80.4`, `30.2` and `24.7` occur inside row 11's extract
   as cells and deltas of Sarawagi & Cohen's own tables; `BFGS` and `3.8 million` occur inside row 14's
   extract as Sha & Pereira's own material; each was read at its line and none is this paper.
3. **WHOLE READING** of `FRAMEWORK.md`, every boundary taken from that file's own heading list read
   this session and not from where this side happened to be: **§4.2** (237–271), the **L2 charter**
   (386–425), the **design points DP-A to DP-Q** (669–817), **A.3** (1133–1155), **§S4 whole** with
   (a) to (g) (1547–1628), **§S5's entanglement argument** (1631–1656) and **§S6 whole**, DP1 to DP17
   (1777–1885, DP1 beginning at 1783 and DP17 ending at 1882). **It reached nothing further.** *What
   the whole reading did establish, and it bears on finding (4):* `FRAMEWORK.md`'s two homes for the
   fitting question — DP-P (802–806) and §S6's DP16 (1874–1877) — carry Och's figure and nothing about
   how a conditional-likelihood fit is optimised, regularised, or handled when part of the state is
   unobserved; and nothing anywhere in those sections names a training algorithm, an inference algorithm
   or a confidence-marginal route. **The fitting and inference detail this row was admitted for is absent
   from the framework by name and by mechanism alike.**
4. **`reading_pass/population.md`'s verification rows V1 to V13, read at the object** (lines
   100–121): **none names this paper and none carries a figure of it.** V13, the only DP-P figure, is
   Och's (row 15).

**So: no `[FACT]`, no ratified figure and no characterisation rests on this paper, the read is not a
verification, and NO STOP OF EITHER CLASS FIRES** — no corrected structural claim (there is none to
correct) and no falsifier (DP-P is expressly **not** a chosen design point, `population.md` line 116).

---

## Identity — the title matches the bibliography IN FULL, the candidacy row's title is a prefix, and no venue, year, copyright or licence is printed anywhere

**What is printed on page 1, transcribed as printed:**

- A chapter number and title: **"1 An Introduction to Conditional Random Fields for Relational
  Learning"**.
- **Authors:** *Charles Sutton*, Department of Computer Science, University of Massachusetts, USA,
  `casutton@cs.umass.edu`, `http://www.cs.umass.edu/~casutton`; and *Andrew McCallum*, Department of
  Computer Science, University of Massachusetts, USA, `mccallum@cs.umass.edu`,
  `http://www.cs.umass.edu/~mccallum`.
- Every subsequent page carries a running head: the section title or the chapter title, and a page
  number 1–35.

**The check against the bibliography's row 30, every axis it offers:**

- **Authors: MATCH, in the row's own order.**
- **Title: MATCHES THE BIBLIOGRAPHY's IN FULL** — *"An Introduction to Conditional Random Fields for
  Relational Learning"*. **The CANDIDACY row's title — *"an introduction to conditional random
  fields"* — is a PREFIX of both the printed and the bibliography's title, stopping before *for
  Relational Learning*.** The progress record's item (b′) asked exactly this: three titles to reconcile,
  not two. **Whether the dropped words change what the paper can carry is finding (1)'s subject**, and
  the short answer is that the chapter's first half is the sequence-model tutorial the candidacy row
  describes and its second half is the relational-learning case study the dropped words name.
- **★ NO venue, year, conference line, book title, editor, publisher, copyright notice, licence line
  or DOI is printed anywhere in the thirty-five pages.** The bibliography's row itself names no venue —
  only a year — so on the venue axis there is nothing to check on either side (row 51's shape on a
  different axis, as item (b′) anticipated). The chapter number "1" and the running heads are the only
  typographic evidence of a larger work, and **what work that is, is not printed and is not asserted
  here.**
- **The year is bounded but not established from the document:** the reference list's latest-dated
  entries are 2005 — *Richardson & Domingos, "Markov logic networks", Machine Learning, 2005, "To
  appear"* (p. 34) and *Minka, MSR-TR-2005-144, October 2005* (p. 33) — so the internal evidence gives
  **≥ late 2005** and cannot reach 2006. The bibliography's *2006* is carried from the row and the URL,
  not from the document. *(Row 13's shape, where internal evidence bounded the year below the row's;
  row 12's reference list could date it.)*
- **★ THE TIER: LINK IS THE CORRECT CONSERVATIVE READING AND NO CORRECTION IS OWED.** No licence line
  is printed, and the URL is the second author's own university page — an author copy. This is rows
  51's, 52's, 12's and 13's direction (nothing printed, LINK correct) and not rows 30's, 49's and 50's
  (a printed CC licence the row understated); the rows are named and no count is asserted.

**Routed to the bibliography reconciliation**, beside the identity findings of rows 28, 3, 29, 10, 11,
18, 15, 12, 13 and 14. **Nothing is amended.** *Two second-hand data for that reconciliation, recorded
as relays from this paper's own reference list and NOT as establishment of anything:* it prints *Sarawagi
& Cohen* as **NIPS 17, MIT Press, 2005** where our row 25 says *NIPS 2004*, and *Ng & Jordan* as
**NIPS 14, pages 841–848, MIT Press, 2002** where our row 27 says *NIPS 2001* — the ordinary
conference-year-versus-proceedings-year difference, stated so a later reconciliation knows the two
readings exist and which document each comes from.

**File:** `docs/research_papers/sutton_mccallum_2006_crf_tutorial.pdf` (**414,565 bytes** at this
session's `docs/research_papers/` listing, and at the bridge's staging of it).

**Structure, as printed:** §1.1 Introduction (pp. 1–2); §1.2 Graphical Models — §1.2.1 Definitions,
§1.2.2 Applications (§1.2.2.1 Classification, §1.2.2.2 Sequence Models), §1.2.3 Discriminative and
Generative Models (pp. 2–8); §1.3 Linear-Chain Conditional Random Fields — §1.3.1 From HMMs to CRFs,
§1.3.2 Parameter Estimation, §1.3.3 Inference (pp. 9–15); §1.4 CRFs in General — §1.4.1 Model, §1.4.2
Applications, §1.4.3 Parameter Estimation, §1.4.4 Inference, §1.4.5 Discussion, §1.4.6 Implementation
Concerns (pp. 16–24); §1.5 Skip-Chain CRFs — §1.5.1 Model, §1.5.2 Results, §1.5.3 Related Work (pp.
24–30); §1.6 Conclusion (p. 30); Acknowledgments (p. 30); References (pp. 31–35). Five figures (1.1 to
1.5), three tables (1.1 to 1.3), sixty-one numbered equations (1.1) to (1.61), two footnotes, and
**fifty-nine references — derived at the pages as eleven on p. 31, fourteen on p. 32, thirteen on p.
33, fifteen on p. 34 and six on p. 35.**

---

## Claims, labeled

### What the chapter says it is (§1.1)

**[FACT, p. 2]** *"This chapter is divided into two parts. First, we present a tutorial on current
training and inference techniques for conditional random fields. We discuss the important special case
of linear-chain CRFs, and then we generalize these to arbitrary graphical structures. We include a brief
discussion of techniques for practical CRF implementations. Second, we present an example of applying a
general CRF to a practical relational learning problem. In particular, we discuss the problem of
information extraction … we propose a skip-chain CRF, a model that jointly performs segmentation and
collective labeling of extracted mentions. On a standard problem of extracting speaker names from
seminar announcements, the skip-chain CRF has better performance than a linear-chain CRF."* **So the
paper's own statement of itself is: a tutorial, and one case study.** The candidacy row's word
*tutorial* holds at the object for the first part; the dropped words *for Relational Learning* name the
second.

**[FACT, p. 1]** The opening definition: *"A conditional random field is simply a conditional
distribution p(y|x) with an associated graphical structure."*

### §1.2 — the graphical-model preliminaries, and the generative/discriminative account

**[FACT, p. 3, (1.1)–(1.4)]** An undirected model is p(x, y) = (1/Z) Π_A Ψ_A(x_A, y_A) over factors,
with Z the partition function, *"intractable in general"*; each local function is assumed exponential,
Ψ_A = exp{Σ_k θ_Ak f_Ak}, (1.3), *"for some set of feature functions or sufficient statistics"*; a
directed model factorises over parents, (1.4); *"a generative model is one that directly describes how
the outputs probabilistically 'generate' the inputs."*

**[FACT, pp. 4–5, (1.5)–(1.7)]** Naive Bayes as (1.5) and logistic regression as (1.6), rewritten with
per-class indicator features into (1.7), *"because it mirrors the usual notation for conditional random
fields."* **[FACT, pp. 5–6, (1.8)]** The HMM, motivated by named-entity recognition, with its two
independence assumptions stated.

**[FACT, pp. 6–8, §1.2.3]** The chapter's own account of why discriminative: p(y|x) *"does not include a
model of p(x), which is not needed for classification anyway"*; adding independence assumptions among
inputs *"can hurt performance"* — citing *Caruana and Niculescu-Mizil 2005* that naive Bayes *"performs
worse on average across a range of applications than logistic regression"*; **and the claim that ties
this chapter to row 13, quoted because it is stronger than row 13's own paper states it (p. 7):**
*"Actually, the difference in performance between naive Bayes and logistic regression is due only to the
fact that the first is generative and the second discriminative; the two classifiers are, for discrete
input, identical in all other respects."* And: *"In the terminology of Ng and Jordan [2002], naive Bayes
and logistic regression form a generative-discriminative pair."* **[FACT, p. 8, (1.10)–(1.12)]** Minka's
argument, credited as *"due to Minka [2005]"*: the discriminative model has more freedom to fit *"because
it does not require that θ = θ′"*, at the cost of trading accuracy on p(x) *"which we care less about"*.
*(Row 13's own paper claims that pair's asymptotic ordering and sample-complexity crossing; this chapter
carries none of those results and cites Ng & Jordan for the terminology only — noted so the two are not
merged.)*

### §1.3 — the linear-chain CRF: form, fitting, inference

**[FACT, pp. 9–10, (1.13)–(1.17)]** The HMM rewritten as an exponential model (1.13), compacted with
feature functions (1.14), conditioned to give (1.15), and generalised — *"We simply allow the feature
functions f_k(y_t, y_{t−1}, x_t) to be more general than indicator functions"* — to **Definition 1.1**
(1.16) with Z(x) the instance-specific normaliser (1.17). *"Every HMM can be written in this form."*
**This is the same construction row 12's extract records as the HMM-like CRF, stated here from the HMM
side.**

**★ [FACT, pp. 11–12, §1.3.2 — THE FITTING DETAIL THE CANDIDACY ROW ASKED WHETHER THE PAPER CARRIES.]**
Training data is iid *sequences* — *"we have relaxed the iid assumption within each sequence, but we
still assume that distinct sequences are independent"*. The objective is the conditional log-likelihood
(1.18), (1.20). *"Parameter estimation is typically performed by penalized maximum likelihood."*
Regularisation: the Euclidean-norm penalty with parameter 1/2σ² giving (1.21), *"which can also be
viewed as performing maximum a posteriori estimation of θ, if θ is assigned a Gaussian prior with mean 0
and covariance σ²I"*; *"often the accuracy of the final model does not appear to be sensitive to changes
in σ², even when σ² is varied up to a factor of 10"*; the ℓ1 alternative (an exponential prior, Goodman
2004) *"tends to encourage sparsity"*. The gradient (1.22): empirical feature expectation minus model
feature expectation minus the penalty term — *"at the unregularized maximum likelihood solution, when the
gradient is zero, these two expectations are equal."* **Concavity, stated exactly (p. 12):** *"The
function ℓ(θ) is concave, which follows from the convexity of functions of the form g(x) = log Σ_i exp
x_i … every local optimum is also a global optimum. Adding regularization ensures that ℓ is strictly
concave, which implies that it has exactly one global optimum."* *(Row 12's paper states the same
convexity with its footnote-2 qualification to fully observable states; this chapter's §1.4.3 is where
that qualification's other side is worked — finding (4).)*

**★ [FACT, pp. 12–13 — THE OPTIMISERS, AND THE CHAPTER'S OWN CITATION OF ROW 14 AS THE EVIDENCE.]**
Steepest ascent *"requires too many iterations to be practical"*; Newton's method needs the Hessian,
*"quadratic in the number of parameters. Since practical applications often use tens of thousands or
even millions of parameters, even storing the full Hessian is not practical"*; **quasi-Newton BFGS
(Bertsekas 1999), limited-memory BFGS (Byrd et al. 1994), and conjugate gradient** are the current
techniques, either *"a black-box optimization routine that is a drop-in replacement for vanilla gradient
ascent"*. And: *"When such second-order methods are used, gradient-based optimization is much faster than
the original approaches based on iterative scaling in Lafferty et al. [2001], as shown experimentally by
several authors [Sha and Pereira, 2003, Wallach, 2002, Malouf, 2002, Minka, 2003]."* **That sentence
names row 12's two algorithms as the superseded approach and row 14's paper as one of four experimental
demonstrations; row 14's extract records those measurements at its own object (its L-BFGS, CG and GIS
iteration counts), and this chapter adds no measurement of its own to them.**

**★ [FACT, p. 13 — THE TRAINING COST, STATED AS A FORMULA AND TWO WORKED CASES.]** Forward-backward
costs O(TM²) per instance; training costs **O(TM²NG)**, *"where N is the number of training examples, and
G the number of gradient computations required by the optimization procedure"*; *"if the number of
states is large, or the number of training sequences is very large, then this can become expensive. For
example, on a standard named-entity data set, with 11 labels and 200,000 words of training data, CRF
training finishes in under two hours on current hardware. However, on a part-of-speech tagging data set,
with 45 labels and one million words of training data, CRF training requires over a week."*

**[FACT, pp. 13–15, §1.3.3, (1.24)–(1.37)]** The two inference problems — marginals p(y_t, y_{t−1}|x)
and Z(x) for the gradient; the Viterbi labelling y* = argmax_y p(y|x) — *"both … can be performed
efficiently and exactly by variants of the standard dynamic-programming algorithms for HMMs."* The HMM
forward (1.27)–(1.29), backward (1.30)–(1.32), edge marginal (1.33)–(1.34) and Viterbi (1.35) recursions
are derived, and the CRF is written as (1.36) with factors Ψ_t = exp{Σ_k λ_k f_k} (1.37), so that *"the
forward recursion (1.29), the backward recursion (1.32), and the Viterbi recursion (1.35) can be used
unchanged for linear-chain CRFs. Instead of computing p(x) as in an HMM, in a CRF the forward and
backward recursions compute Z(x)."*

**★ [FACT, p. 15 — THE PER-SPAN MARGINAL, WHICH IS FINDING (5).]** *"A final inference task that is
useful in some applications is to compute a marginal probability p(y_t, y_{t+1}, … y_{t+k}|x) over a
range of nodes. For example, this is useful for measuring the model's confidence in its predicted
labeling over a segment of input. This marginal probability can be computed efficiently using constrained
forward-backward, as described by Culotta and McCallum [2004]."*

### §1.4 — general CRFs, latent variables, approximate inference, and the discussion

**[FACT, pp. 16–17, Definition 1.2, (1.38)–(1.41)]** *"Let G be a factor graph over Y. Then p(y|x) is a
conditional random field if for any fixed x, the distribution p(y|x) factorizes according to G."* Thus
*"every conditional distribution p(y|x) is a CRF for some, perhaps trivial, factor graph."* **Clique
templates** and parameter tying: the factors are partitioned into templates C_p whose parameters are
tied, (1.39)–(1.41); *"in a linear-chain conditional random field, typically one clique template … is
used for the entire network."* Named special cases: dynamic CRFs (multiple labels per time step),
relational Markov networks, Markov logic networks.

**[FACT, p. 17, §1.4.2]** The applications paragraph names row 14 first — *"One of the first
large-scale applications of CRFs was by Sha and Pereira [2003], who matched state-of-the-art performance
on segmenting noun phrases in text"* — and row 11: *"Semi-Markov CRFs [Sarawagi and Cohen, 2005] add
somewhat more flexibility in choosing features, which may be useful for certain tasks in information
extraction and especially bioinformatics."* **That is the chapter's whole statement about the
semi-Markov extension: one sentence, about features, with no mention of segmentation as a decoded
variable.** *(Recorded because DP-C's chosen answer rests on row 11's formalism, and a reader must not
take this chapter as carrying anything about it.)*

**★ [FACT, pp. 18–21, §1.4.3 — THE LATENT-VARIABLE REGIME, WHICH IS FINDING (4).]** For the
fully-observed case the general-CRF likelihood (1.42) and gradient (1.43) *"has many of the same
properties as in the linear-chain case … the function ℓ(θ) is concave."* **Then the other case:**
*"Within-network classification can be viewed as a kind of latent variable problem, in which certain
variables … are not observed in the training data. It is more difficult to train CRFs with latent
variables, because optimizing the likelihood … requires marginalizing out the latent variables. Because
of this difficulty, the original work on CRFs focused on fully-observed training data, but recently there
has been increasing interest in training latent-variable CRFs [Quattoni et al., 2005, McCallum et al.,
2005]."* With latent w, the CRF is (1.44), the objective is the **marginal likelihood** (1.45), computed as
a ratio of two partition functions (1.48) by clamping the observed y; and — the load-bearing sentence —
*"Maximizing ℓ(θ) can be difficult because ℓ is no longer convex in general (intuitively, log-sum-exp is
convex, but the difference of two log-sum-exp functions might not be), so optimization procedures are
typically guaranteed to find only local maxima. Whatever optimization technique is used, the model
parameters must be carefully initialized in order to reach a good local maximum."* Two routes: direct
gradient (1.49)–(1.52), *"the expectation of the fully-observed gradient, where the expectation is taken
over w"*, requiring marginals of both the clamped CRF and the full CRF; and EM (1.53). *"The direct
maximization algorithm and the EM algorithm are strikingly similar … We are unaware of any empirical
comparison of EM to direct optimization for latent-variable CRFs."* And a practical remark: *"In our
experience, conjugate gradient tolerates violations of convexity better than limited-memory BFGS, so it
may be a better choice for latent-variable CRFs."*

**[FACT, p. 21, §1.4.4]** General-graph inference: junction tree exact *"if the graph has small
treewidth"*; otherwise approximate — MCMC *"not been popular"* because inference *"will be invoked
repeatedly, once for each time that the gradient is computed"*; contrastive divergence applied in
vision; **loopy belief propagation** the most popular, *"neither exact nor even guaranteed to converge if
the model is not a tree"*, referring to Yedidia et al. 2004.

**★ [FACT, p. 22, §1.4.5 — THE DISCUSSION, WHICH BEARS ON DP-P TWICE.]** *(i)* *"Although we have
emphasized the view of a CRF as a model of the conditional distribution, one could view it as an
objective function for parameter estimation of joint distributions. As such, it is one objective among
many, including generative likelihood, pseudolikelihood [Besag, 1977], and the maximum-margin objective
[Taskar et al., 2004, Altun et al., 2003]. Another related discriminative technique for structured
models is the averaged perceptron … [Collins, 2002] … **To date, there has been little careful comparison
of these, especially CRFs and max-margin approaches, across different structures and domains.**"*
*(ii)* *"Given this view, it is natural to imagine training directed models by conditional likelihood,
and in fact this is commonly done in the speech community, where it is called maximum mutual information
training. However, it is no easier to maximize the conditional likelihood in a directed model than an
undirected model, because in a directed model the conditional likelihood requires computing log p(x),
which plays the same role as Z(x) in the CRF likelihood."* **Sentence (ii) is finding (3).**

**[FACT, pp. 22–24, §1.4.6]** Implementation concerns, each stated as practice and none measured here:
features of the form f_pk(y_c, x_c) = 1{y_c = ỹ_c} q_pk(x_c) (1.54) — an indicator on the output
configuration times an *observation function* — *"To avoid confusion, we refer to the functions q_pk(x_c)
as observation functions rather than as features"*; *"to match state-of-the-art results on a standard
natural language task, Sha and Pereira [2003] use 3.8 million features"*; **unsupported features** (never
nonzero in training) *"do affect Z(x), so putting a negative weight on them can improve the likelihood by
making wrong answers less likely … including unsupported features typically results in better accuracy"*,
with an ad hoc selection technique; categorical observations converted to binary features; **redundant
node factors beside edge factors** — *"Although one could define the same family of distributions using
only edge factors, the redundant node factors provide a kind of backoff, which is useful when there is
too little data. In language applications, there is always too little data, even when hundreds of
thousands of words are available"*; and log-space forward-backward with the operator a ⊕ b = log(e^a +
e^b) computed as a + log(1 + e^{b−a}) (1.55)–(1.56), *"particularly if we pick the version of the identity
with the smaller exponent."*

### §1.5 — the skip-chain CRF, the chapter's one measurement of its own

**[FACT, pp. 24–26, (1.57)–(1.59)]** A general CRF with two clique templates — the linear chain and skip
edges between *"pairs of identical capitalized words"* — motivated by the Markov assumption's inability to
represent *"the higher-order dependencies that arise when identical words occur throughout a document"*;
*"we must be careful not to include too many skip edges, because this could result in a graph that makes
approximate inference difficult."* **[FACT, p. 27]** Exact inference is intractable on this data:
*"29 of the 485 instances have a maximum clique size of 10 or greater, and 11 have a maximum clique size
of 14 or greater. (The worst instance has a clique with 61 nodes.)"*; inference is loopy belief
propagation with the TRP schedule (Wainwright et al. 2001).

**[FACT, pp. 27–29, Tables 1.2 and 1.3, every cell as printed]** Data: 485 e-mail seminar announcements
(Freitag 1998), fields stime, etime, location, speaker; 5-fold cross-validation with an 80/20 split;
per-token precision, recall and F1 (footnote 2: the per-token metric, and why previous per-document
figures cannot be compared, *"Peshkin and Pfeffer [2003] do use the per-token metric (personal
communication), so our comparison is fair in that respect"*).

| System | stime | etime | location | speaker | overall |
|---|---|---|---|---|---|
| BIEN Peshkin and Pfeffer [2003] | 96.0 | **98.8** | 87.1 | 76.9 | 89.7 |
| Linear-chain CRF | **97.5** | 97.5 | **88.3** | 77.3 | 90.2 |
| Skip-chain CRF | 96.7 | 97.2 | 88.1 | **80.4** | **90.6** |

*(Table 1.2, F1; bold as printed, the best per column; overall is the average of the four fields.)*

| Field | Linear-chain | Skip-chain |
|---|---|---|
| stime | 12.6 | 17 |
| etime | 3.2 | 5.2 |
| location | 6.4 | 0.6 |
| speaker | 30.2 | 4.8 |

*(Table 1.3, inconsistently mislabeled tokens, averaged over 5 folds; no emphasis printed.)*

The text: the skip-chain *"performs much better than all the other systems on the SPEAKER field … On the
other fields, however, the skip-chain CRF does slightly worse (less than 1% absolute F1)"*; the
linear-chain CRF *"mislabels 121.6 true speaker tokens"*, of which the 30.2 inconsistently mislabelled
are *"24.7% of the missed speaker tokens"*; speaker recall 70.0 → 76.8 at precision 86.5 → 85.1; *"On the
LOCATION field … there is no benefit."*

**★ [FACT] THE UNCERTAINTY PRACTICE, RECORDED PER ROW AS THE CADENCE REQUIRES.** **Neither table prints
a spread, an interval or a significance test**; Table 1.3 is *"averaged over 5 folds"* with no spread
shown; the *"less than 1% absolute F1"* differences are called *"slightly worse"* with no test. **Rows
12's and 13's practice, and the exact contrast with row 15**, which prints a bootstrap interval per column
and marks per-cell significance (read at row 15's extract this session). **No difference read off either
table is used anywhere in this extract as a result.**

**[FACT, p. 30, §1.6 — the chapter's own stated limitation.]** *"The main disadvantage of CRFs is the
computational expense of training. Although CRF training is feasible for many real-world problems, the
need to perform inference repeatedly during training becomes a computational burden when there are a
large number of training instances, when the graphical structure is complex, when there are latent
variables, or when the output variables have many outcomes."*

**[THEORY, as the chapter cites it]** Lafferty, McCallum & Pereira 2001 (row 12) for the CRF; Rabiner
1989 for the HMM algorithms; Kschischang, Frey & Loeliger 2001 for factor graphs; Bertsekas 1999 and Byrd,
Nocedal & Schnabel 1994 for BFGS and its limited-memory form; Yedidia, Freeman & Weiss 2004 for loopy
belief propagation; Culotta & McCallum 2004 for constrained forward-backward; Quattoni, Collins & Darrell
2005 and McCallum, Bellare & Pereira 2005 for latent-variable CRFs; Besag 1977, Taskar et al. 2004, Altun
et al. 2003 and Collins 2002 for the rival objectives.

**[CONJECTURE — the authors' own, labelled as such here]** That conjugate gradient *"may be a better
choice for latent-variable CRFs"* — stated as *"in our experience"*, measured nowhere in the chapter.

---

## Coupling facts (mandatory)

**What the method ASSUMES about its upstream.** A **fixed factor graph** (a chain for §1.3, any factor
graph for §1.4, chosen per instance from the input for §1.5); **iid training sequences** with dependence
allowed inside a sequence; **feature functions given and fixed** (feature induction is named at p. 23 by
citation to McCallum 2003 and not done); fully-observed labels for the convex regime, or a declared set of
latent variables for the non-convex one; a discrete label alphabet, one label per node.

**What it HANDS downstream.** As a tutorial: **no model, no score and no term of its own** — it restates
row 12's formalism from the HMM side and supplies the fitting and inference machinery around it: the
regularised objective and its gradient, the optimiser class, the exact recursions, the per-span marginal,
the latent-variable objective with its two optimisation routes, and the approximate-inference options for
loopy graphs. As a case study: one general-CRF design (skip edges over identical capitalised words) and
its per-token F1 on one information-extraction data set. **A specification can instantiate the machinery
from this chapter; it would instantiate the skip-chain design from nothing here, that design being about
repeated proper names in text.**

**Its own STATED SCOPE and limits, each where the chapter states it.**
- Discrete variables only (§1.2.1, *"we discuss only the discrete case in this chapter"*).
- The convexity claim holds for the fully-observed likelihood; **with latent variables the objective is
  non-convex and initialisation decides which local maximum is reached** (§1.4.3).
- Exact inference is available on chains and small-treewidth graphs; otherwise approximate, with loopy
  belief propagation *"neither exact nor even guaranteed to converge"* (§1.4.4).
- Training cost O(TM²NG), *"over a week"* at 45 labels and a million words (§1.3.2), and the stated
  main disadvantage (§1.6).
- **Domain:** text processing throughout; the one measurement is information extraction from e-mail.
  **Nothing musical anywhere in the chapter.**

---

## What an L2 detail specification could adopt, adapt, or must argue against

- **ADOPT — the fitting detail, as machinery beside row 12's form.** The regularised conditional
  log-likelihood (1.21) with a declared prior (Gaussian, or the ℓ1 alternative), its gradient (1.22) as
  empirical-minus-model feature expectations, and a quasi-Newton or conjugate-gradient optimiser in place
  of row 12's iterative scaling — with the evidence for that choice cited to row 14's extract at its
  object and to the three demonstrations this chapter names that are not held (Wallach 2002, Malouf 2002,
  Minka 2003). This is exactly *"how any weight is fitted"* in the L2 charter's sentence (`FRAMEWORK.md`
  406–408), and it is the half of DP-P that Och's figure does not reach: **Och's method (row 15) fits to
  the metric wanted; this chapter and row 12 are the likelihood arm that method is measured against.**
- **ADOPT — the per-span marginal as the published route to a confidence over a stretch** (p. 15,
  constrained forward-backward). Beside **D-006** (two full candidate lists), **D-008** (true
  probabilities deferred), **D-032** (every confidence class-declared and named to its decision) and
  **DP-K** (rivals with their mass). **Its primary — Culotta & McCallum 2004 — is not held**; a
  specification citing this route cites this chapter for the existence of the construction and states
  the gap at the primary, which is what the theory-grounding corollary requires of an unfetched source.
- **ADAPT — the latent-variable machinery, IF any component of L2's state is unobserved in the ground
  truth** (finding (4)). The marginal likelihood (1.45), its gradient (1.52) and the EM alternative (1.53)
  are the published forms; the cost is the loss of the global optimum, stated in terms at p. 20.
- **ADAPT — two implementation practices as design data, not rules:** redundant node factors as a
  backoff *"when there is too little data"* (p. 23), beside **D-271** (the capacity budget) and **D-533**
  (the leftover-share rule for rare continuations); and log-space forward-backward (1.55)–(1.56), beside
  **D-565** (exact ties and a declared total order — the numerical practice does not touch the tie rule).
- **MUST ARGUE AGAINST — the rival objectives §1.4.5 enumerates.** A specification that fits by
  conditional likelihood or to the metric wanted has, by this chapter's own list, left pseudolikelihood,
  the max-margin objective and the averaged perceptron un-argued, and the chapter states that as of its
  writing *"there has been little careful comparison of these"*. **That sentence is dated to the chapter
  and is not a statement about the field now.**
- **MUST ARGUE AGAINST — the domain, and the case study.** Nothing here decides a musical question; the
  skip-chain design is about repeated capitalised words in e-mail and is not a candidate for anything L2
  decides; and the one measurement prints no uncertainty of any kind.

---

## ★ Findings, routed and not applied

### (1) ★★ THE DOUBT DEFAULT'S OWN QUESTION IS ANSWERED — YES, IT CARRIES THE INFERENCE AND FITTING DETAIL, BY EXPLANATION AND NOT BY MEASUREMENT

The candidacy row's question is *"whether it carries the inference and fitting detail the three CRF rows
above leave out."* **At the object it does**: §1.3.2 carries the regularised objective, its gradient, the
concavity argument and the optimiser class with its evidence named; §1.3.3 carries the exact recursions
and the per-span marginal; §1.4.3 carries the latent-variable objective with both optimisation routes;
§1.4.4 the approximate-inference options; §1.4.6 the implementation practices. **Rows 12, 11 and 10 leave
exactly that out** — row 12's extract records its paper's two iterative-scaling algorithms and its own
statement that slow convergence is its main limitation; this chapter is where the replacement is
explained and the replacement's evidence is cited, and one of those citations is row 14, already read.

**What it carries, it carries as a SECONDARY SOURCE.** Every load-bearing sentence above cites a primary:
row 12 for the form; Bertsekas 1999 and Byrd et al. 1994 for the optimisers; row 14, Wallach 2002, Malouf
2002 and Minka 2003 for the evidence that they beat iterative scaling; Culotta & McCallum 2004 for the
per-span marginal; Quattoni et al. 2005 and McCallum et al. 2005 for latent-variable CRFs; Yedidia et al.
2004 for loopy belief propagation. **Of those, rows 12 and 14 are held and read; the others are not held.**
The item (d′) case arose exactly as the progress record said it might: **the chapter's one measurement of
its own (§1.5) is not about the detail the row was admitted for**, so the question is answered by what the
chapter EXPLAINS, and this extract says so.

**★ THE ADMITTED VERDICT AND THE L2 PLACEMENT STAND, with a precision to the reason.** The candidacy row
calls it *"a tutorial rather than a primary method"* — true at the object — and the slice derivation
places it *"for the inference and fitting detail rows 10–12 leave out"* — also true at the object. The
precision: **what the detail is, is a secondary account whose primaries are partly held (rows 12 and 14)
and partly not (the seven works named above)**, so a detail specification citing this chapter for any of
it is citing a source that itself cites another, and the theory-grounding corollary's unfetched-source
rule reaches the not-held primaries. **Routed as a precision to the candidacy row's reason and to the
slice derivation's line 63, and as a not-held-primary list to the bibliography reconciliation. Applied
nowhere; both files stand exactly as they stand.**

### (2) THERE IS NO VERIFICATION TARGET — rows 51's, 12's, 13's and 14's shape

Established at the objects by the legs named at the top of this extract. **The record bears on the paper
in exactly three places and `FRAMEWORK.md` is not one of them.** No `[FACT]`, no ratified figure and no
characterisation rests on it; this read corrects nothing, confirms no ratified value, and had no recorded
structural claim to correct.

### (3) ★ ROW 15's "MMI" AND ROW 12's "CONDITIONAL LIKELIHOOD" ARE ONE CRITERION, AND THIS CHAPTER SAYS SO IN TERMS — A PRECISION FOR HOW DP-P's SENTENCE IS READ

`FRAMEWORK.md` DP-P and §S6's DP16 say *"fitting combination weights by likelihood"*. Row 15's extract
records that the arm behind that phrase is Och's **MMI** criterion, which it calls *"the likelihood-type
criterion"* and which Och derives from the maximum-entropy principle. Row 12's extract records the CRF's
fit as the **conditional log-likelihood**. **This chapter joins them at p. 22:** training a directed model
by conditional likelihood *"is commonly done in the speech community, where it is called maximum mutual
information training"*, and it *"is no easier"* than in a CRF because log p(x) *"plays the same role as
Z(x)"*. **So the likelihood arm Och measured at 12.2 against 19.6 and the fit rows 12 and 10 use are the
same criterion by this chapter's own identification**, and the two halves of DP-P's question — the
criterion, and what fitting to the metric instead buys — are now read at three primaries and one
secondary that names the identity. **A precision on the reading of a recorded sentence, not a correction:
nothing the record says is false.** Routed to L2's detail specification and to the findings surface's DP-P
block, beside row 15's three precisions and row 13's finding (4) and merged with none of them. **Applied
nowhere.**

### (4) ★ THE LATENT-VARIABLE REGIME IS THE OTHER SIDE OF ROW 12's CONVEXITY QUALIFICATION, AND IT IS THE CHECK THAT DECIDES WHICH FITTING REGIME L2 IS EVEN IN

Row 12's extract routed *"the convexity property, with its qualification, as a design check"*: the global
optimum holds *"in the case of fully observable states"*, and *"whether every component of [L2's state] is
observed in the ground truth decides whether this property is available at all."* **This chapter supplies
what that check leads to on its other branch.** If any component of L2's state — the L2 charter publishes the
segmentation, and per span the tonality and the chord, and per sounding note the chord-tone assignment
and, where the reading has one, the elaboration relation (`FRAMEWORK.md` 392–398) — is NOT observed in
the ground truth the weights are fitted on, then the fit is a latent-variable CRF: the objective is the
marginal likelihood (1.45), *"no longer convex in general"*, reached only to a local maximum that depends on
initialisation, by direct gradient (1.52) or EM (1.53), with no published comparison of the two as of the
chapter. **Whether any such component is unobserved in this project's ground truth is NOT established here
and nothing in this extract asserts it either way** — it is a question about what the annotation records,
which is answered at the ground truth and at `ARCHITECTURE.md`'s statement of what the tables are counted
from, neither of which this read opened. **Routed to L2's detail specification and to measurement design
as the question that must be answered before the fitting regime is written down, beside D-525 (the staged
fit), D-271 (the capacity budget), DP-P and row 12's routed check. Applied nowhere; no verdict.**

### (5) THE PER-SPAN MARGINAL IS A PUBLISHED ROUTE TO A CONFIDENCE OVER A STRETCH, AND ITS PRIMARY IS NOT HELD

Page 15 names constrained forward-backward (Culotta & McCallum 2004) as the way to compute
p(y_t … y_{t+k} | x) over a range of nodes, *"for measuring the model's confidence in its predicted
labeling over a segment of input."* **D-006** publishes two full candidate lists, **D-008** defers true
probabilities, **D-032** requires every confidence to be class-declared and named to its decision, and
DP-K publishes rivals with their mass. **This is a published construction for a per-span probability
inside the same normalised model, and the record does not name it.** An ADDITION CANDIDATE for the design
data under those decisions, carried with its bound: **the primary is not held, and this chapter states the
construction's existence and use, not its algorithm.** Routed to L2's detail specification. **Shown, not
ratified.**

### (6) THE TRAINING-COST STATEMENT, AND WHAT IT IS A DATUM FOR

O(TM²NG) with two worked cases, and the stated main disadvantage at §1.6. **M here is the number of
states of the chain** — for L2, the size of the joint state space over tonality, chord and whatever else
the state carries — and the cost is quadratic in it per position. A datum beside `CLAUDE.md`'s standing
rule that getting the inference right comes first and speed is traded against it only once slowness has
turned out to be a problem — **the rule sequences the concern and does not license ignoring it** — and
beside row 12's finding (13), which recorded the same limitation from the primary. **This chapter dates it
and gives it a formula; it measures nothing of ours.** Routed as a datum. **No verdict.**

### (7) THE RIVAL OBJECTIVES ARE ENUMERATED, AND THE ABSENCE OF A CAREFUL COMPARISON IS STATED — DATED TO THE CHAPTER

§1.4.5's list — generative likelihood, conditional likelihood, pseudolikelihood, max-margin, averaged
perceptron — is the set DP-P's question chooses among, and the chapter's *"little careful comparison of
these … across different structures and domains"* is a FACT of absence **as of this chapter's writing,
bounded by its reference list at late 2005, and not a statement about the field now.** Routed to L2's
detail specification and to measurement design as the enumeration a specification must at least name.
**No verdict.**

### (8) THE DOMAIN CROSSING, STATED ONCE AND BINDING ON EVERY CLAIM ABOVE

The chapter's subject is machine learning on text: named-entity recognition and part-of-speech tagging as
running examples, and information extraction from 485 seminar announcements as its one experiment.
**There is nothing musical anywhere in the thirty-five pages.** Row 11's extract, read at its own line 379
this session, writes the bound: *"so the formalism paper is not later cited as if it measured the musical
gain"*. **The same bound binds here: nothing in this chapter measures anything about harmony, and no
figure of it may be cited as evidence about this project's repertoire, its corpus or its accuracy.**

### (9) THE ORDINARY DATA, RECORDED AS PRINTED

- **No uncertainty of any kind on any figure, and no significance test anywhere** (§1.5 above).
- **The cross-primary check RAN ON SOMETHING, and what it found is consistency and not a figure.** This
  chapter cites four held members of the population — rows 12, 11, 13 and 14 — and what it says of each
  agrees with that row's own extract read this session: row 12's iterative scaling as the superseded
  training approach (row 12's own §7 limitation); row 14's 3.8 million features and its matching of
  state-of-the-art noun-phrase chunking (row 14's extract at its lines 118 and 133, read this session);
  row 13's generative–discriminative pair (row 13's extract's §1 quotation); row 11's semi-Markov CRF
  described by its feature flexibility only. **No figure of any of them is reported here, so nothing is
  merged.**
- **★ THE REFERENCE LIST: fifty-nine works, derived at the pages, of which FOUR are in this project's
  bibliography and have extracts — rows 12, 11, 13 and 14 — and fifty-five are not held and have none.**
  The not-held members this extract's findings lean on are named at finding (1). **Nothing is carried out
  of any of them.** *(Rows 15's, 12's and 13's outcome — no member held — does NOT recur here; the rows are
  named and no comparison beyond that is asserted.)*
- **No datum for DP-O, DP-N or any other underived design point** — the chapter's only sentence toward a
  grammar is the §1.4.2 remark that CRFs have been used *"to learn distributions over the derivations of
  a grammar"*, a citation list and nothing more.
- **No printing inconsistency was found.** Every section, equation, figure and table number the text
  refers to exists as printed. *(Rows 12's and 52's shape did not recur.)*

---

## Centrality

**NOT CENTRAL, and the ground on which CENTRAL could be argued is stated in full so the verdict is
challengeable.**

**The ground for NOT CENTRAL, in four parts.**
1. **The load-bearing-claim route is absent and was established, not assumed.** No `[FACT]`, no ratified
   figure and no characterisation rests on this paper — the legs at the top of this extract. **This is
   rows 51's, 12's, 13's and 14's situation; rows 51 and 13 were graded NOT CENTRAL and rows 12 and 14
   CENTRAL, so this part decides nothing alone.**
2. **It is a SECONDARY source, and every claim a specification would cite it for has a primary.** Row 12
   was graded CENTRAL because its formalism IS the family L2's score would instantiate; this chapter
   restates that formalism from the HMM side and adds the machinery around it — and for each piece of
   machinery it names the primary. Under #1 and the theory-grounding corollary a detail specification
   cites the primary it has read, or states the gap; **it does not cite the tutorial in the primary's
   place.** Where the primary is held (rows 12 and 14), the tutorial adds nothing a specification would
   cite; where it is not (Culotta & McCallum 2004, Quattoni et al. 2005, McCallum et al. 2005, the three
   optimiser demonstrations), the tutorial is what a specification would cite ONLY with the gap stated,
   which is part 4's point.
3. **Nothing of its own would be adopted.** Its one design — skip edges between identical capitalised
   words — is about repeated proper names in text, and its one measurement is that design's F1 on one
   information-extraction data set, with no uncertainty printed.
4. **The domain.** Nothing musical anywhere; every figure is text-processing.

**The ground on which CENTRAL could be argued, stated so the user or a later session can overrule this
verdict.** Findings (3), (4) and (5) are claims an L2 detail specification would have to make — the
identity of the likelihood arms, the latent-variable regime if the state is partly unobserved, and the
per-span marginal as a confidence route — **and for (4) and (5) this chapter is a HELD source that states
them where their primaries are not held; whether any OTHER held paper states either was NOT checked, and
no superlative is asserted.** If the user reads either as load-bearing for the specification, the commission's second
route is satisfied and the verdict flips; the reason this side did not take that route is part 2 above —
**the chapter would then be cited for a claim whose primary was not read, which the corollary permits only
as a stated gap, and a source citable only as a stated gap is not central by that fact.** **The judgment is
stated so it can be overruled.**

**Second independent extraction: NOT OWED**, on this verdict. **It flips to owed if the user reads finding
(4) or (5) as load-bearing** — stated here so the choice is his, rows 47's, 51's and 13's treatment.

---

## What this extract does NOT do

It amends nothing. `FRAMEWORK.md`, `reading_pass/candidacy_upgrades.md`,
`cowork_l2_task_b_slice_derivation_2026_09_05.md`, `reading_pass/population.md`,
`docs/research_papers/BIBLIOGRAPHY.md` and `cowork_reading_pass_findings_2026_08_31.md` all stand exactly
as they stand. **No verdict is moved, no value is changed, no row is written and no gate is lifted.**
Finding (1) is a precision to a recorded reason and is applied nowhere; findings (3), (4), (5), (6) and (7)
are routed data and addition candidates, none a correction. **It does not establish which components of
L2's state the ground truth observes** — finding (4) states that question and leaves it. It does not fetch
the book, if any, this chapter appeared in, nor the URL the bibliography names, nor any of the fifty-five
works this chapter cites that are not held.

**Row 18's finding (3), row 47's two corrected structural claims, row 45's finding (1) and row 52's
corrected structural claim all stay open with the user, all applied nowhere, none touched, restated as
settled or built on by this read. Row 16 adds none to that population.**

**Not read, and named so the coverage is not overstated:** anything at the `people.cs.umass.edu` URL
beyond the held file; the seminar-announcements data; and every one of the fifty-five not-held works this
chapter cites. **Only the held file was read.**

---

*Provenance: Cowork, 2026-09-11, the session that booted on
`cowork_handoff_entry_one_hundred_and_fifty_two.md`, at tip `0f69cc6b79610c962a8400cdaba3dfc12facfe55`
(read at `.git/refs/heads/master` with the file tools over a bridge-staged copy). After the ordinary
session-start read — `CLAUDE.md` at its six ruled session-start spans, met as the harness-delivered
project-instructions copy of the whole file (162,259 bytes, equal to the file's size at the root
listing); `DECISIONS.md` whole (862 lines); `STATUS.md` whole; and the derived gating answer's
`gating_ids` list whole — this session performed the user's fact-check over rows 13's, 12's and 15's
extracts (recorded in the progress record) and then read for this extract, at the files: the
hundred-and-fifty-second entry whole, the hundred-and-eighteenth entry at its bridge-fault section, the
hundred-and-thirty-sixth entry at its landing account and its error (c), `reading_pass/
l2_slice_reading_progress.md` at its banner, gate, table, count, next-in-order block and carried items (a)
to (f), `reading_pass/candidacy_upgrades.md` WHOLE (row 16 at line 78), the L2 slice derivation at its
table (row 16 at line 63), `docs/research_papers/BIBLIOGRAPHY.md` at lines 20–33 (row 16 at line 30),
`reading_pass/population.md` at §3's verification rows V1 to V13 and its bound (100–121), the row 11
extract at its line 379, the row 12 extract whole, the row 13 extract whole, the row 15 extract whole, the
row 14 extract at its banner (lines 1–40), at its lines 100–169 and at its line 87, and `FRAMEWORK.md` at its heading list and at §4.2
(237–271), the L2 charter (386–425), the design points DP-A to DP-Q (669–817), A.3 (1133–1155), §S4 whole
(1547–1628), §S5's entanglement argument (1631–1656) and §S6 whole (1777–1885). The sweeps run are
enumerated at the top of this extract with the files they ran over. The paper was read at the object as
page images, all thirty-five pages, in requests of four pages; the page count was established by a refused
request for page 100; no page was read a second time on its own, and every transcribed cell of Tables 1.2
and 1.3 was checked once against the image at transcription. Every repository file was read with the file
tools from bridge-staged copies; no shell read any repository object or any staged copy of it; the shell
acts of this session on its own files were a `mkdir` and `cp` of staged snapshots into the container's
output directory before editing, and an `ls` of that directory. No count of this project's own measurement
is restated (#17f, `D-431`); every value above is the paper's own.*
