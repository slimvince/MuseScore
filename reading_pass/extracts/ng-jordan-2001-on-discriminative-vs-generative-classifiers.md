# EXTRACT — Ng & Jordan, "On Discriminative vs. Generative classifiers: A comparison of logistic regression and naive Bayes" — Task B candidacy row 13, first pass, AT THE OBJECT

> **STATUS: FIRST-PASS EXTRACT, READ WHOLE AT THE OBJECT (2026-09-07).** Written under
> `cowork_reading_pass_commission_2026_08_30.md` §4, whose form the remedial commission
> (`cowork_reading_pass_remedial_commission_2026_08_31.md` §3) binds unchanged.
>
> **The grade.** All eight pages of the held PDF were read AT THE OBJECT: staged through the bridge
> and read with the file tools as page images. **No relay, no web-fetch read, no prompted
> extraction.** **The page count was ESTABLISHED rather than assumed** — a request for page 20 was
> refused by the tool with *"PDF has 8 pages"*. **The held document prints no page numbers**; every
> location below is the page's position in the held document (1–8) together with the section,
> equation, proposition, lemma, theorem, corollary or figure number as printed.
>
> **★ THE SUBJECT IS NOT MUSIC AT ALL** — a machine-learning result about two families of
> classifier, measured on benchmark datasets from the UCI Machine Learning repository. **Every claim
> carried out of this paper crosses a domain boundary and says so at the place it is carried.** The
> wording of that bound is taken from **row 11's own extract, read at its own line 379 this
> session**, and is quoted there rather than here. *(That the same bound was then followed by rows
> 15's and 12's extracts is this slice's progress record's account of those two files, relayed and
> NOT read at either extract this session; it is stated as a relay so it is not mistaken for a
> reading.)*
>
> **★ THIS READ IS NOT A VERIFICATION.** No `[FACT]`, no ratified figure and no characterisation of
> any kind rests on this paper — established at the objects below, not inherited. It is **row 51's
> and row 12's shape, not row 15's**. What the read is for is the derivation's own question: the
> candidacy row admits this paper ON THE DOUBT DEFAULT and says in terms that *"whether that is a
> method for a charter decision or a general result is not settled by the row"*. That question is
> answered at finding (1).

---

## The verification target — THERE IS NONE, and this is how that was established

**The record bears on this paper in exactly THREE places:**

| # | Place | Line | What it says |
|---|---|---|---|
| 1 | `reading_pass/candidacy_upgrades.md`, the candidacy row | 75 | ADMITTED, ON THE DOUBT DEFAULT — *"It bears on which fitting regime L2's score takes (DP-P), and whether that is a method for a charter decision or a general result is not settled by the row. Included by the criterion's own default."* |
| 2 | `cowork_l2_task_b_slice_derivation_2026_09_05.md`, the placement row | 60 | **L2** (doubt default) — *"Bears on DP-P, the fitting regime of L2's score. Admitted on the default; placed on the same ground."* |
| 3 | `docs/research_papers/BIBLIOGRAPHY.md` | 27 | *Ng & Jordan, "On Discriminative vs. Generative Classifiers," NIPS 2001*, a `proceedings.neurips.cc` URL, held ✓, tier **LINK** |

**`FRAMEWORK.md` IS NOT ONE OF THEM.** Nor is `reading_pass/population.md`, nor
`cowork_reading_pass_findings_2026_08_31.md`.

**How that was established. The legs are NAMED rather than counted — an identity sweep, a bare-word
sweep, the identity terms run separately over two further files, a figure-and-mechanism sweep run
after the paper was read, a whole reading, and a reading of `population.md`'s verification rows —
and each is stated with what it actually returned.**

1. **IDENTITY sweep** over the staged tree, for the bare words `Ng`, `Jordan`, `naive Bayes`,
   `naïve Bayes`, `logistic regression`, `discriminativ` and `generativ` (the last two truncated so
   they catch every inflection), each run case-insensitively. **It returned the three places above
   and nothing else in the record** — its other hits are this line's own working files (this slice's
   progress record, the hundred-and-thirty-sixth handoff entry, and the row 12 extract's own
   reference list, where *Saul & Jordan 1996* appears), each distinguished at its own line.
   **`FRAMEWORK.md` returned NO MATCH on any of those terms.**
2. **BARE-WORD sweep** for `2001` over `FRAMEWORK.md`: **no match at all in that file.** (Row 12's
   read established the same absence for the same word; it is re-established here rather than
   inherited.)
3. **The identity terms over `reading_pass/population.md` and over
   `cowork_reading_pass_findings_2026_08_31.md` separately: NO MATCH AT ALL in either file** — not
   on `discriminativ`, not on `generativ`, not on `2001`.
4. **FIGURE and MECHANISM sweep, run AFTER the paper was read**, from what it actually reports:
   `UCI`, `asymptotic`, `sample complexity`, `Vapnik`, `Efron`, `VC dimension`, `conditional
   likelihood`, `joint likelihood`, `hybrid classifier`, `two-regime`, and the four UCI dataset
   names distinctive enough to be searchable (`optdigits`, `ionosphere`, `lymphography`,
   `promoters`). **It reached no citation of this paper anywhere.** *Two false-positive families are
   recorded so nobody merges them:* the three letters `uci` occur inside `reproducible`,
   `reproducing` and `irreducible` throughout `CLAUDE.md`, `DECISIONS.md` and the derived gating
   artifact, and `conditional likelihood` occurs in this slice's progress record as row 10's own
   fitting description and inside the carried-item list — neither is a citation of this paper.
5. **WHOLE READING**, every boundary taken from `FRAMEWORK.md`'s own heading list and not from where
   this side happened to be reading: **§4.2** (237–271), the **L2 charter** (386–425), the **design
   points DP-A to DP-Q** whole (669–817), **A.3** whole (1133–1155), **§S4** whole with §S4(a) to
   §S4(g) (1547–1628), **§S5's entanglement argument** (1631–1656) and **§S6** whole, DP1 to DP17
   (1777–1885). **It reached nothing further.**
6. **`reading_pass/population.md`'s verification rows V1 to V13 read at the object** (§3, lines
   100–121): **none names Ng & Jordan and none carries a discriminative-versus-generative figure.**
   V13 — the only DP-P figure in the table — is Och's, which is candidacy row 15 and is read at its
   own extract.

**★ AND THE TWO PLACES THAT WOULD HAVE CARRIED IT IF ANYTHING DID WERE READ WHOLE, NOT SEARCHED.**
`FRAMEWORK.md` **DP-P** (802–806) and **§S6's DP16** (1874–1877) are the record's two homes for the
question this paper's candidacy row names. **Both cite Och's result and nothing else** — *"fitting
combination weights by likelihood was measured at 12.2 against 19.6 on the metric actually wanted,
when the weights were instead fitted to that metric"* — and neither names an author, a system or a
year for anything further. **This is the reading the carried item (c) demanded and the sweeps could
not have supplied**, because DP-P names nobody: it is row 15's own lesson applied here, and it
returns the same answer from the other direction.

**So: no `[FACT]`, no ratified figure and no characterisation rests on this paper, the read is not a
verification, and NO STOP OF EITHER CLASS FIRES** — neither the remedial commission §5
corrected-structural-claim class (there is no recorded structural claim about this paper to
correct) nor the falsifier class (no chosen design point's ground rests on it; DP-P is expressly
**not** a chosen design point, which the record states at `population.md`'s own bound line 116).

---

## Identity — a prefix title match, and no venue, year, copyright, licence or page number printed anywhere

**What is printed on page 1, transcribed as printed:**

- **Title:** *"On Discriminative vs. Generative classifiers: A comparison of logistic regression and
  naive Bayes"*.
- **Authors:** *Andrew Y. Ng*, Computer Science Division, University of California, Berkeley, CA
  94720; and *Michael I. Jordan*, C.S. Div. & Dept. of Stat., University of California, Berkeley, CA
  94720.

**The check against the bibliography's row 27, both axes:**

- **Authors: MATCH, in the row's own order** — the row writes *"Ng & Jordan"* and the paper prints
  Ng first and Jordan second.
- **Title: a PREFIX MATCH with one capitalisation difference.** The row writes *"On Discriminative
  vs. Generative Classifiers"*, which is the printed title's text up to the colon — **the row stops
  where the printed title's subtitle begins**, exactly the shape rows 11, 12, 45, 46, 47, 48 and 50
  produced. **The one difference beyond the truncation is that the paper prints *classifiers*
  lowercase where the row capitalises it**; recorded as printed and reconciled nowhere.
- **★ NO venue, year, conference line, copyright, licence or page number is printed anywhere in the
  eight pages.** So **whether the held file is the NIPS 2001 proceedings text as published is not
  establishable from what is held**, and the row's **LINK** tier rests on its `proceedings.neurips.cc`
  URL rather than on a printed licence line.

**★ LINK IS THE CORRECT CONSERVATIVE READING AND NO TIER CORRECTION IS OWED** — rows 51's and 52's
direction, and the opposite of rows 30's, 49's and 50's understatements. **This is row 12's
printed-nothing shape with the tier agreeing**, where row 12's row read LINK and row 15's read CC;
the rows are named and no count is asserted.

**★ AND THE YEAR IS LESS RECOVERABLE HERE THAN IT WAS AT ROW 12.** Row 12's held file could be dated
from its reference list, whose latest entry read *"Punyakanok & Roth 2001, NIPS 13. Forthcoming."*
**This paper's reference list carries six works whose latest date is 1999** (Anthony & Bartlett), so
the internal evidence bounds the year only at **≥ 1999** and cannot reach 2001. The bibliography's
*NIPS 2001* is therefore carried from the row and from the URL, not from the document.

**Routed to the bibliography reconciliation**, beside rows 28's, 3's, 29's, 10's, 11's, 18's, 15's
and 12's identity findings. **Nothing is amended.**

---

## Claims, labeled

### What the paper IS, and what it claims

**[FACT]** *(Abstract, page 1, quoted in full because the whole read turns on it.)* **"We compare
discriminative and generative learning as typified by logistic regression and naive Bayes. We show,
contrary to a widely-held belief that discriminative classifiers are almost always to be preferred,
that there can often be two distinct regimes of performance as the training set size is increased,
one in which each algorithm does better. This stems from the observation—which is borne out in
repeated experiments—that while discriminative learning has lower asymptotic error, a generative
classifier may also approach its (higher) asymptotic error much faster."** *(Emphasis nowhere in the
original; the paper emphasises nothing in this passage.)*

**[FACT]** *(§1, page 1.)* The two beliefs the paper sets out to test are named as beliefs, not as
this paper's positions. **(i)** *"There are several compelling reasons for using discriminative
rather than generative classifiers, one of which, succinctly articulated by Vapnik [6], is that
'one should solve the [classification] problem directly and never solve a more general problem as
an intermediate step [such as modeling p(x|y)].' Indeed, leaving aside computational issues and
matters such as handling missing data, the prevailing consensus seems to be that discriminative
classifiers are almost always to be preferred to generative ones."* **(ii)** *"Another piece of
prevailing folk wisdom is that the number of examples needed to fit a model is often roughly linear
in the number of free parameters. This has its theoretical basis in the observation that for 'many'
models, the VC dimension is roughly linear or at most some low-order polynomial in the number of
parameters … and it is known that sample complexity in the discriminative setting is linear in the
VC dimension [6]."*

**[FACT]** *(§1, page 2.)* **The Generative-Discriminative pair, which is the paper's unit of
comparison.** *"A parametric family of probabilistic models p(x, y) can be fit either to optimize
the joint likelihood of the inputs and the labels, or fit to optimize the conditional likelihood
p(y|x), or even fit to minimize the 0-1 training error obtained by thresholding p(y|x) to make
predictions. Given a classifier h_Gen fit according to the first criterion, and a model h_Dis fit
according to either the second or the third criterion (using the same parametric family of models),
we call h_Gen and h_Dis a Generative-Discriminative pair."* Two instances are named: **Normal
Discriminant Analysis and logistic regression** (Gaussian p(x|y), multinomial p(y)); and **the naive
Bayes classifier and logistic regression** for discrete inputs.

**[FACT]** *(§1, page 2 — the paper's own statement of its two results.)* *"we consider the naive
Bayes model (for both discrete and continuous inputs) and its discriminative analog, logistic
regression/linear classification, and show: (a) The generative model does indeed have a higher
asymptotic error (as the number of training examples becomes large) than the discriminative model,
but (b) The generative model may also approach its asymptotic error much faster than the
discriminative model—possibly with a number of training examples that is only logarithmic, rather
than linear, in the number of parameters."*

**[FACT]** *(§2, page 2 — the model, and its two fitting routes for the discriminative member.)*
naive Bayes estimates p(x_i|y) and p(y) by counting with a smoothing constant l — equation (1),
`p̂(x_i = 1|y = b) = (#_S{x_i=1, y=b} + l) / (#_S{y=b} + 2l)` — and classifies on the sign of the
log-odds sum, equation (2). Its discriminative analog is logistic regression, equation (3),
`l_Dis(x) = Σ β_i x_i + θ`, whose parameters *"can be fit either to maximize the conditional
likelihood on the training set … or to minimize 0-1 training error"*.

**[FACT — A SCOPE STATEMENT WORTH CARRYING, §2, page 3.]** *"Insofar as the error metric is 0-1
classification error, we view the latter alternative as being more truly in the 'spirit' of
discriminative learning, though the former is also frequently used as a computationally efficient
approximation to the latter. In this paper, we will largely ignore the difference between these two
versions of discriminative learning and, with some abuse of terminology, will loosely use the term
'logistic regression' to refer to either, though our formal analyses will focus on the latter
method."* **So the paper's own formal results are about fitting to the 0-1 error metric, and the
conditional-likelihood fit is carried along by an explicitly declared elision.**

**[FACT — THE PAPER'S OWN LIMITING CONDITION, §3, page 3.]** *"When D is such that the two classes
are far from linearly separable, neither logistic regression nor naive Bayes can possibly do well,
since both are linear classifiers. Thus, to obtain non-trivial results, it is most interesting to
compare the performance of these algorithms to their asymptotic errors."*

### §3 — the four formal results, each stated as printed

**[FACT] Proposition 1** *(page 3)*: *"Let h_Gen and h_Dis be any generative-discriminative pair of
classifiers, and h_Gen,∞ and h_Dis,∞ be their asymptotic/population versions. Then ε(h_Dis,∞) ≤
ε(h_Gen,∞)."* **Footnote 1 qualifies it:** *"Under a technical assumption (that is true for most
classifiers, including logistic regression) that the family of possible classifiers h_Dis … has
finite VC dimension."* The paper's own gloss: this *"provides a basis for what seems to be the
widely held belief that discriminative classifiers are better than generative ones."*

**[FACT] Proposition 2** *(page 3)*: *"Let h_Dis be logistic regression in n-dimensions. Then with
high probability ε(h_Dis) ≤ ε(h_Dis,∞) + O(√((n/m) log(m/n))). Thus, for ε(h_Dis) ≤ ε(h_Dis,∞) + ε_0
to hold with high probability (here, ε_0 > 0 is some fixed constant), it suffices to pick m =
Ω(n)."* The paper's own gloss: *"the sample complexity of discriminative learning — that is, the
number of examples needed to approach the asymptotic error — is at most on the order of n. Note
that the worst case sample complexity is also lower-bounded by order n [6]."*

**[FACT] Lemma 3** *(page 4)*: with `m = O((1/ε_1²) log(n/δ))` the generative classifier's estimated
parameters are all within ε_1 of their asymptotic values, with probability at least 1 − δ, in both
the discrete and the continuous case. The paper's own gloss, and it is the load-bearing sentence of
the whole paper: *"Thus, with a number of samples that is only logarithmic, rather than linear, in
n, the parameters of the generative classifier h_Gen are uniformly close to their asymptotic values
in h_Gen,∞."*

**[FACT] Theorem 4** *(page 4)*: `ε(h_Gen) ≤ ε(h_Gen,∞) + G(O(√((1/m) log n)))`, under stated
conditions on ρ_0, the class priors and the per-feature conditionals (discrete case) or the
variances (continuous case). **G(τ) is the quantity that must be small for the bound to be
non-trivial**, and the paper says so in terms.

**[FACT] Proposition 5** *(page 5)*: if an Ω(1) fraction of the features are "relevant" —
`|p(x_i = 1|y = T) − p(x_i = 1|y = F)| ≥ γ` for a fixed γ > 0, or `|μ_i|y=T − μ_i|y=F| ≥ γ` in the
continuous case — then `E[l_Gen,∞(x)|y = T] = Ω(n)` and `−E[l_Gen,∞(x)|y = F] = Ω(n)`.

**[FACT] Corollary 6** *(pages 5 and 7 — it opens at the foot of page 5 and its statement closes at
the head of page 7, page 6 carrying Figure 1 alone; this location first read "pages 5–6" and was
corrected 2026-09-11 at the page on the user's fact-check over the latest extracts)*: under its
stated conditions on G(τ), *"for ε(h_Gen) ≤
ε(h_Gen,∞) + ε_0 to hold with high probability, it suffices to pick m = Ω(log n)."* And the
paper's own closing note on it: *"either of these … is a sufficient condition for the asymptotic
sample complexity to be O(log n)."*

**[FACT — A BOUND THE PAPER STATES ABOUT ITS OWN STRONGEST RESULT, page 5.]** The Chebyshev route to
bounding G(τ) *"will frequently be very loose"*; **the exponentially small bound
`G(τ) ≤ exp(−O((α − τ)² n))` holds only *"in the unrealistic case in which the naive Bayes
assumption really holds"*, via the Chernoff bound.** So the headline O(log n) sample complexity is
established at its strongest under an assumption the authors themselves call unrealistic, and the
paper says so at the place it derives it.

### §4 — the experiments

**[FACT]** *(§4, page 7.)* *"To test these predictions, we performed experiments on 15 datasets, 8
with continuous inputs, 7 with discrete inputs, from the UCI Machine Learning repository."*

**[FACT]** *(Figure 1, page 6, caption transcribed as printed.)* *"Results of 15 experiments on
datasets from the UCI Machine Learning repository. Plots are of generalization error vs. m (averaged
over 1000 random train/test splits). Dashed line is logistic regression; solid line is naive
Bayes."* **The fifteen panels are titled** pima (continuous), adult (continuous), boston (predict if
> median price, continuous), optdigits (0's and 1's, continuous), optdigits (2's and 3's,
continuous), ionosphere (continuous), liver disorders (continuous), sonar (continuous), adult
(discrete), promoters (discrete), lymphography (discrete), breast cancer (discrete), lenses (predict
hard vs. soft, discrete), sick (discrete), voting records (discrete).

**[FACT]** *(§4, page 7 — the paper's own verdict on its own experiment.)* *"We find that the
theoretical predictions are borne out surprisingly well. There are a few cases in which logistic
regression's performance did not catch up to that of naive Bayes, but this is observed primarily in
particularly small datasets in which m presumably cannot grow large enough for us to observe the
expected dominance of logistic regression in the large m limit."*

**[FACT]** *(Footnote 2, page 7 — the protocol, transcribed because it is the whole of what the
paper says about how the figure was produced.)* Discrete/continuous hybrids were avoided by using
only the discrete or only the continuous inputs where a dataset had both; *"Train/test splits were
random subject to there being at least one example of each class in the training set, and
continuous-valued inputs were also rescaled to [0, 1] if necessary."* For linearly separable
datasets an MCMC sampler picked a classifier randomly among the separating hyperplanes, *"so the
errors are empirical averages over the separating hyperplanes"*. Normal Discriminant Analysis used
*"the (standard) trick of adding ε to the diagonal of the covariance matrix to ensure
invertibility"*, and **for naive Bayes `l = 1`** — that is, Laplace smoothing, the value equation
(1) names as the traditional one.

**★ [FACT] THE UNCERTAINTY PRACTICE, RECORDED PER ROW AS THE CADENCE REQUIRES.** **The paper
contains NO TABLE of any kind.** Its only reported result is Figure 1's fifteen plots. **Each curve
is an average over 1000 random train/test splits, and NO error bar, NO spread, NO confidence
interval and NO significance test appears anywhere in the eight pages.** **This is row 12's
practice reproduced exactly and the exact contrast with row 15**, which prints a bootstrap 95 %
confidence interval per column on both its tables and marks per-cell significance. **No difference
read off Figure 1 by eye is used anywhere in this extract as a result.**

### §5 — the discussion, Efron, and what the paper declares out of scope

**[FACT]** *(§5, page 7 — the one comparison with prior work, and the paper's own account of why it
differs.)* *"Efron [2] also analyzed logistic regression and Normal Discriminant Analysis (for
continuous inputs), and concluded that the former was only asymptotically very slightly (1/3–1/2
times) less statistically efficient. This is in marked contrast to our results, and one key
difference is that, rather than assuming P(x|y) is Gaussian with a diagonal covariance matrix (as we
did), Efron considered the case where P(x|y) is modeled as Gaussian with a full covariance matrix.
In this setting, the estimated covariance matrix is singular if we have fewer than linear in n
training examples, so it is no surprise that Normal Discriminant Analysis cannot learn much faster
than logistic regression here. A second important difference is that Efron considered only the
special case in which the P(x|y) is truly Gaussian. Such an asymptotic comparison is not very useful
in the general case, since the only possible conclusion, if ε(h_Dis,∞) < ε(h_Gen,∞), is that
logistic regression is the superior algorithm. In contrast, as we saw previously, it is in the
non-asymptotic case that the most interesting 'two-regime' behavior is observed."*

**★ [FACT] REGULARISATION IS DECLARED ORTHOGONAL AND OUT OF SCOPE, IN THE PAPER'S OWN WORDS**
*(§5, pages 7–8)*: *"Practical classification algorithms generally involve some form of
regularization—in particular logistic regression can often be improved upon in practice by
techniques such as shrinking the parameters via an L_1 constraint, imposing a margin constraint in
the separable case, or various forms of averaging. Such regularization techniques can be viewed as
changing the model family, however, and as such they are largely orthogonal to the analysis in this
paper, which is based on examining particularly clear cases of Generative-Discriminative model
pairings."*

**[FACT]** *(§5, page 8 — the paper's own forward-looking sentence.)* *"By developing a clearer
understanding of the conditions under which pure generative and discriminative approaches are most
successful, we should be better able to design hybrid classifiers that enjoy the best properties of
either across a wider range of conditions."*

**[FACT]** *(§5, page 8 — the stated extension, and its bound.)* *"Finally, while our discussion has
focused on naive Bayes and logistic regression, it is straightforward to extend the analyses to
several other models, including generative-discriminative pairs generated by using a
fixed-structure, bounded fan-in Bayesian network model for P(x|y) (of which naive Bayes is a special
case)."* **This is the only sentence in the paper that reaches beyond the one model pair, it is
asserted and not proved, and its reach is a fixed-structure bounded-fan-in Bayesian network — not a
structured sequence model with a decoded segmentation.**

**[FACT]** *(Acknowledgments, page 8.)* *"We thank Andrew McCallum for helpful conversations."* —
recorded because McCallum is an author of candidacy row 12, and this is the only connection between
the two papers printed anywhere in either; **nothing is carried from it.**

---

## Coupling facts (mandatory)

**What the method ASSUMES about its upstream.** A **fixed parametric family** and a training set of
**m independent, identically distributed labelled examples** drawn from a joint distribution over
X × Y. **Binary classification** (`Y = {T, F}`) throughout. Inputs are either **binary**
(`X = {0,1}^n`, *"the generalization offering no difficulties"* by the authors' own statement) or
**real and bounded to [0,1]**. The feature set of size n is **given and fixed**; nothing in the
paper chooses, weights or discovers features. **It assumes nothing whatever about sequences,
structure, segmentation, or dependence between examples.**

**What it HANDS downstream.** **Not a model, not an algorithm, not a score, and not a term.** What
it hands on is a **comparison result about two ways of fitting one family** — an asymptotic ordering
(Proposition 1), two sample-complexity accounts (Proposition 2 and Corollary 6), and one
demonstration on fifteen benchmark datasets that the two orderings cross. **A specification cannot
instantiate anything from this paper; it can only be guided or contradicted by it.** *(The contrast
with row 15 is what makes that concrete: Och's paper hands down an implementable algorithm — an exact
line optimisation with an outer loop that regrows the candidate list — where this paper hands down
nothing implementable. **That description of row 15's algorithm is taken from this slice's progress
record's summary of row 15's extract and NOT from that extract, which was not opened this session**;
row 15's extract is the record for it and a later use is re-cited there.)*

**Its own STATED SCOPE and limits, each at the place the paper states it.**
- Binary classification only; the naive Bayes / logistic regression pair specifically, with Normal
  Discriminant Analysis / logistic regression as the continuous instance.
- Both members are **linear classifiers**, so the comparison is uninformative where the classes are
  far from linearly separable (§3).
- The formal analyses are about fitting to **0-1 training error**; the conditional-likelihood fit is
  carried along under a declared abuse of terminology (§2).
- The strongest form of the sample-complexity result rests on the naive Bayes conditional-
  independence assumption, which the authors themselves call *"unrealistic"* (§3, page 5).
- **Regularisation is declared out of scope** and is said to change the model family (§5).
- The extension beyond the one pair is asserted, not proved, and reaches a fixed-structure
  bounded-fan-in Bayesian network (§5).

---

## What an L2 detail specification could adopt, adapt, or must argue against

**ADOPT: nothing.** There is no mechanism here to take.

**ADAPT: one rule, and it is a rule about choosing, not about computing.** *Where the same family of
scores can be fitted either by counting a joint model or by fitting a conditional one
discriminatively, which is better depends on how much annotated material there is relative to the
number of free parameters — and the two orderings cross.* **`D-525`'s staged fit sits exactly on
that axis**: its factor tables are **counted from ground truth and frozen** (the generative side)
and only a **small vector of combination weights is fitted discriminatively** (the discriminative
side), with an all-weights-equal ablation arm that must be beaten. This paper is the published
general account of the axis that decision sits on — **it is not a defence of `D-525` and does not
mention anything like it**, but a specification defending `D-525` against a fully discriminative
rival is arguing on this paper's ground, and rows 10's and 11's fully discriminative fits are the
rival's instances already read in this slice.

**MUST ARGUE AGAINST: Proposition 1.** The paper's first formal result is that the discriminative
member of a pair has the **lower asymptotic error**. A specification that keeps a counted, frozen
generative component is choosing the member with the higher asymptote, and the honest defence is the
sample-complexity one (Lemma 3, Corollary 6) — not a claim that the counted tables are better in the
limit. **The paper forecloses that second defence explicitly.**

**AND MUST ARGUE AGAINST THE TRANSFER ITSELF, WHICH IS THE HARDER HALF.** Every result above is
stated for **iid examples, a fixed feature set, binary labels and two linear classifiers**. L2's
score is a structured object over a decoded sequence of spans with a segmentation variable. **The
paper establishes nothing about that setting**, its own stated extension reaches only a
fixed-structure bounded-fan-in Bayesian network, and **nothing in this extract asserts the transfer
holds.** A specification citing this paper for L2's fitting regime must establish the transfer at
the object or state that it has not.

---

## ★ Findings, routed and not applied

### (1) ★★ THE DOUBT DEFAULT'S OWN QUESTION IS ANSWERED, AND THE ANSWER IS "A GENERAL RESULT"

The candidacy row admits this paper on the default and states the open question in terms: *"whether
that is a method for a charter decision or a general result is not settled by the row."*

**At the object it is a GENERAL RESULT and not a method.** The paper contains no algorithm, no
model of anything L2 decides, no score, no term and no procedure. Its four formal results are
statements about the asymptotic error and the sample complexity of two classifier families, and its
one experiment is a plot of generalization error against training-set size on fifteen benchmark
datasets. **There is nothing in it a detail specification could implement.**

**★ THE ADMITTED VERDICT AND THE L2 PLACEMENT NONETHELESS STAND, and the ground is the criterion's
own words, not charity.** The commission's criterion admits a paper when a detail specification
*"could adopt, adapt, or have to argue against the way that paper actually does the thing the
charter owns"*, and the derivation's consequence (i) states that *"have to argue against" admits a
rival*. Proposition 1 is something an L2 detail specification defending a counted-table component
**must** argue against, and the two-regime result is the ground on which it would argue. **So the
row is admitted for its ADAPT and ARGUE-AGAINST limbs and not for its ADOPT limb** — which the row's
own reason already half-says, and which is now established rather than presumed.

**★ THIS IS NOT ROW 39's SHAPE and the difference matters.** Row 39 was read and found **not to bear
on L1's cues at all**, on three independent grounds, so the derivation was told it had placed the
row wrongly. **This paper does bear on DP-P's question** — DP-P asks how L2's score terms are
combined and fitted, and this paper is about precisely the choice of fitting criterion for a
parametric family. What is corrected is not the placement but the **kind**: it bears as a result to
be argued with, not as a method to be taken. **Routed as a precision to the candidacy row's reason
and to the slice derivation's line 60. Applied nowhere; both files stand exactly as they stand.**

### (2) THERE IS NO VERIFICATION TARGET — row 51's and row 12's shape

Established at the objects by the sweeps and the readings named — not counted — at the top of this
extract. **The record bears on the paper in exactly three places and `FRAMEWORK.md` is not one of
them.** The consequence is stated so it is not mistaken for a smaller claim: **no `[FACT]`, no
ratified figure and no characterisation of any kind rests on this paper**, so this read corrects
nothing and confirms no ratified value; **there was no recorded structural claim about it to
correct.**

### (3) ★ THE PAPER IS THE PUBLISHED GENERAL ACCOUNT OF THE AXIS `D-525` SITS ON — AN ADDITION CANDIDATE, WITH ITS OWN BOUND

**The record does not carry it.** `D-525` records the staged fit — counted tables frozen, a small
weight vector fitted discriminatively, an all-weights-equal ablation arm — and `population.md` and
`FRAMEWORK.md` carry no published general account of *why* a hybrid of a counted and a fitted
component is a defensible choice rather than a compromise. **This paper supplies one**: the
generative member reaches its asymptote in a number of examples logarithmic in the parameter count
while the discriminative member needs a number linear in it, so **where annotated material is scarce
relative to the parameter count, the counted component is not a concession — it is the member that
has actually converged.**

**★ THE BOUND, WHICH IS THE PAPER'S OWN AND IS CARRIED WITH THE CANDIDATE EVERYWHERE IT GOES:**
- the result is proved for **binary classification with iid examples and a fixed feature set**, and
  its strongest form assumes the naive Bayes conditional independence the authors call
  *"unrealistic"*;
- the paper's own extension sentence reaches a **fixed-structure bounded-fan-in Bayesian network**
  and no further, and is asserted rather than proved;
- **whether L2's score — a structured object over a decoded span sequence — is of a shape the result
  reaches is NOT established here and nothing in this extract asserts it.**

**This is an ADDITION CANDIDATE and not a correction: nothing the record says is false, and no
ground is amended.** Routed to L2's detail specification beside `D-525`, `D-271` (the capacity
budget) and DP-P. **Shown, not ratified.**

### (4) ★ THE THIRD END OF DP-P's QUESTION IS NOW READ, AND THE RECORD CARRIES ONLY THE FIRST

DP-P asks how L2's score terms are combined and fitted. **Three published answers to different halves
of that question are now read at their own primaries inside this slice, and `FRAMEWORK.md` DP-P and
§S6's DP16 carry one of them:**

| The question answered | The primary | Where it is read |
|---|---|---|
| Fit to the metric you actually want, not to likelihood — measured | Och 2003 (row 15) | Row 15's extract. **This is the end the record carries**, at DP-P and DP16, as V13 |
| The conditional fit is **convex, with a global optimum guaranteed** (qualified to fully observable states) | Lafferty, McCallum & Pereira 2001 (row 12) | Row 12's extract. **The record does not carry it** |
| The conditional fit has the **lower asymptote** but needs **more data to reach it**; the two orderings cross | **Ng & Jordan 2001 (this paper)** | this extract. **The record does not carry it** |

**Row 12's own extract states the second of these as the other end of the question row 15's paper
answers.** This finding is recorded **beside** it and **merged with it nowhere** — the two are
different claims about different properties, and no count of "ends" is asserted beyond naming the
three. **Routed beside `D-525`, `D-271` and DP-P. Applied nowhere.**

### (5) THE DOMAIN CROSSING, STATED ONCE AND BINDING ON EVERY CLAIM ABOVE

The paper's subject is **machine learning on fifteen benchmark datasets from the UCI Machine
Learning repository**, named here by Figure 1's own printed panel titles and by nothing else: *pima*,
*adult*, *boston (predict if > median price)*, *optdigits (0's and 1's)*, *optdigits (2's and 3's)*,
*ionosphere*, *liver disorders* and *sonar* on the continuous side, and *adult*, *promoters*,
*lymphography*, *breast cancer*, *lenses (predict hard vs. soft)*, *sick* and *voting records* on the
discrete side. *(Eight and seven, which is the split §4 states; **adult appears on both sides**, which
is footnote 2's stated practice of using only the discrete or only the continuous inputs for a
dataset carrying both — derived from the panel titles and the footnote, and recorded as printed.)*
**There is nothing musical anywhere in the eight pages.**

**★ THE BOUND'S WORDING IS CITED TO THE EXTRACT IT WAS ACTUALLY READ AT, AND THIS SIDE'S FIRST
LANDING OF THIS EXTRACT DID NOT DO THAT** — see the errors note at the foot of this file.
**Row 11's extract, read at its own line 379 this session, writes it:** *"Nothing here is evidence
about music … Routed to the findings surface's DP-C block as a domain-bounded datum, so the
formalism paper is not later cited as if it measured **the** musical gain; no verdict."* **The same
bound binds here, in the same form: nothing in this paper measures anything about harmony, and no
figure of it may be cited as evidence about this project's repertoire, its corpus or its accuracy.**
*(That rows 15's and 12's extracts follow that wording is the progress record's account of those
files, relayed and not read at either extract this session.)*

### (6) ★ AN ERROR OF A PREVIOUS SESSION, FOUND AT THE OBJECT WHILE RUNNING THIS READ'S OWN METHOD WARNING

**Carried item (i) says: before calling anything in the record missing, unidentified or unread, list
`reading_pass/extracts/` and look.** That listing was made for this read, to check whether any of
this paper's six cited works has an extract. **It found something else: the extracts directory holds
TWO row 12 extracts.**

- `lafferty-mccallum-pereira-2001-conditional-random-fields-for-segmenting-and-labeling-sequence-data.md`
  — **63,127 bytes**, which is the size the hundred-and-thirty-sixth handoff entry records for row
  12's extract and the file the record points at.
- `lafferty-mccallum-pereira-2001-conditional-random-fields-for-segmenting-and-labeling-sequence-data-1.md`
  — **62,394 bytes**, **which is exactly the size that entry records for the extract's SECOND
  landing**, the one it says was *"guarded by the landed copy's own `expectedMtimeMs`"*.

**What is established at the object, and no more.** The second file's first fourteen lines were read:
its banner is the row 12 first-pass extract's own banner, word for word — *"EXTRACT — Lafferty,
McCallum & Pereira … Task B candidacy row 12, first pass, AT THE OBJECT"* — so it is **a superseded
copy of the same extract**, not a second independent extraction of the kind row 47's row carries.
**The hundred-and-thirty-sixth entry's untracked-file list does not name it**: that list accounts for
row 47 having two extracts and says *"the rows are named and no file count is asserted"*, and it
names one file for row 12.

**What is NOT established here, and is not asserted.** Whether the `-1` suffix was chosen by this
line's own side or added by the bridge; whether the guarded re-landing the entry describes therefore
did what the entry says it did; and what the second file contains beyond its banner — **only its
first fourteen lines were read.** By that entry's own account the 62,394-byte version still carries
its error (c) uncorrected, since (c) was what the third landing fixed; **that is the entry's
account and not a reading of the file.**

*(★ ESTABLISHED AT CONTENT 2026-09-11, on the user's fact-check over the latest extracts, and
recorded here because this is where the question was left open. Both files were searched with the file
tools for the two marks the hundred-and-thirty-sixth entry's error (c) names. The UNSUFFIXED 63,127-byte
file carries the correction — its banner and its provenance both read "8, 2 and 7", and its provenance
declares the `jq` act; the `-1` 62,394-byte file does NOT — its provenance still reads "2, 7 and 8" and
names no `jq`. So the unsuffixed file is the third landing and the current extract, and the `-1` file is
the superseded second landing, exactly as the sizes said. What the modification times say is the
OPPOSITE — the `-1` file's is about ten minutes LATER than the unsuffixed file's at the directory
listing — so modification time is not landing order here, one more instance of the hundred-and-
fifty-second entry's rule that a moved modification time is not evidence of a landing. Whether the
`-1` suffix was chosen by that side or added by the bridge is still not established. Nothing is deleted,
moved or renamed; the directory stands as it stands.)*

**Why it matters enough to record rather than tidy away.** A later reader listing that directory
meets two files with the same paper's name and no statement anywhere of which is current — and row
47's genuine two-extract case is the precedent that would make the wrong reading plausible.
**Nothing is deleted, moved or amended by this extract; the finding is put to the user with the
extracts directory left exactly as it is.**

### (7) THE ORDINARY DATA, RECORDED AS PRINTED

- **No uncertainty of any kind on any figure, and no significance test anywhere.** The paper prints
  **no table at all**; Figure 1 averages over 1000 random train/test splits and shows no spread.
  **The exact contrast with row 15 and the same practice as row 12.**
- **The cross-primary check had NOTHING TO RUN ON, and that was established rather than assumed.**
  Its data are fifteen UCI benchmark datasets and its metric is generalization error against
  training-set size; a sweep of the staged tree for the distinctive dataset names and for the
  paper's own vocabulary found no read row of this slice reporting on any of them.
- **★ ITS SIX CITED WORKS — derived at the page, all six printed in one column — are statistics,
  learning-theory and pattern-recognition works, and NOT ONE is in this project's bibliography and
  NOT ONE has an extract**, both checked at their own listings this session. They are: **[1]**
  Anthony & Bartlett, *Neural Network Learning: Theoretical Foundations*, Cambridge University
  Press, 1999; **[2]** Efron, "The efficiency of logistic regression compared to Normal Discriminant
  Analysis", *J. Amer. Statist. Assoc.* 70:892–898, 1975; **[3]** Goldberg & Jerrum, "Bounding the
  VC dimension of concept classes parameterized by real numbers", *Machine Learning* 18:131–148,
  1995; **[4]** McLachlan, *Discriminant Analysis and Statistical Pattern Recognition*, Wiley, 1992;
  **[5]** Rubinstein & Hastie, "Discriminative vs. informative learning", *Proc. Third International
  Conference on Knowledge Discovery and Data Mining*, pages 49–53, AAAI Press, 1997; **[6]** Vapnik,
  *Statistical Learning Theory*, John Wiley & Sons, 1998. **Nothing is carried out of any of them.**
  **Rows 15's and 12's outcome reproduced** — six here, derived at the page this session, against
  row 15's seventeen and row 12's twenty-five, **which are those rows' own recorded derivations
  relayed from the progress record and are NOT re-derived here**; the rows are named and no
  comparison beyond the three counts is asserted.
- **No datum for DP-O, DP-N or any other underived design point** — the paper's subject reaches
  none of them.
- **No printing inconsistency was found.** Every section, proposition, lemma, theorem, corollary and
  figure number the text refers to exists as printed. *(Rows 12's and 52's shape did not recur
  here.)*

---

## Centrality

**NOT CENTRAL, and the ground on which CENTRAL could be argued is stated in full so the verdict is
challengeable.**

**The ground for NOT CENTRAL, in four parts.**
1. **The load-bearing-claim route is absent and was established, not assumed.** No `[FACT]`, no
   ratified figure and no characterisation rests on this paper — the sweeps and the whole reading
   above. **This is row 51's situation exactly, and row 51 was graded NOT CENTRAL.**
2. **Nothing in the paper would be adopted.** It contains no algorithm, no model, no score and no
   term. **Row 12 was graded CENTRAL on the commission's second route, and that is read at row 12's
   own extract, lines 675–676, this session:** *"findings (3), (4), (5), (6), (7) and (15) are claims
   such a specification would cite this paper for directly"* — the score's own form and its global
   normalisation among them. *(Row 15 was graded CENTRAL partly for its method; that is the progress
   record's account of row 15's extract, relayed and not read at that extract this session.)*
   **This paper offers a specification nothing to cite about L2's score itself.**
3. **Its model pair is not L2's model, and the transfer is exactly what it does not establish.**
   Binary classification, iid examples, a fixed feature set, two linear classifiers. L2's score is a
   structured object over a decoded span sequence with a segmentation variable. The paper's own
   extension sentence reaches a fixed-structure bounded-fan-in Bayesian network and is asserted
   rather than proved.
4. **Every figure is on UCI benchmark data with no uncertainty of any kind**, so nothing in it could
   carry a quantitative load anywhere.

**The ground on which CENTRAL could be argued, stated so the user or a later session can overrule
this verdict.** `D-525` is a live, ratified decision about this project's own fitting regime, and it
is precisely a hybrid of the two members this paper compares. A detail specification defending
`D-525` against the fully discriminative rival that rows 10 and 11 supply is arguing on this paper's
axis, and finding (3) is the published general account of that axis — which is a claim that *would*
carry load in a detail specification, the commission's own second route to centrality and the exact
route row 12 was graded CENTRAL on. **The reason this side did not take that route is the third part
above: row 12's formalism IS the family L2's score would instantiate, where this paper's family is
not, so the load row 12's claims carry is direct and the load this paper's claims carry passes
through a transfer the paper does not establish.** **The judgment is stated so it can be
overruled.**

**Second independent extraction: NOT OWED**, on this verdict. **It flips to owed if the user reads
finding (3) as load-bearing** — stated here so the choice is his, row 47's and row 51's treatment.

---

## What this extract does NOT do

It amends nothing. `FRAMEWORK.md`, `reading_pass/candidacy_upgrades.md`,
`cowork_l2_task_b_slice_derivation_2026_09_05.md`, `reading_pass/population.md`,
`docs/research_papers/BIBLIOGRAPHY.md` and `cowork_reading_pass_findings_2026_08_31.md` all stand
exactly as they stand. **No verdict is moved, no value is changed, no row is written and no gate is
lifted.** Finding (1) is a precision to a recorded reason and is applied nowhere; finding (3) is an
ADDITION CANDIDATE and not a correction; finding (6) is a report about a file on disk and **nothing
in the extracts directory is deleted, renamed or moved by it.**

**Row 18's finding (3), row 47's two corrected structural claims, row 45's finding (1) and row 52's
corrected structural claim all stay open with the user, all applied nowhere, none touched, restated
as settled or built on by this read. Row 13 adds none to that population.**

**Not read, and named so the coverage is not overstated:** the NIPS 2001 proceedings text at the
`proceedings.neurips.cc` URL the bibliography row names; the fifteen UCI datasets themselves; and
every one of the six works this paper cites — Anthony & Bartlett 1999, Efron 1975, Goldberg & Jerrum
1995, McLachlan 1992, Rubinstein & Hastie 1997 and Vapnik 1998 — **none of which is held and none of
which has an extract**, both checked at their own listings. **Only the held file was read.**

---

## ★ Errors this side's own read-back of the landed copy found, and corrected in a guarded re-landing

**This extract was landed, staged back and read whole at the object without being asked — the act
cadence step 4 prescribes. That read-back found TWO errors, both of which had reached disk, and both
are of the family the degradation rule names. They are NAMED and no total is asserted across them.**

**(a) THE BANNER OF THE VERIFICATION-TARGET SECTION SAID *"all four legs run"* WHERE THE ENUMERATION
DIRECTLY BENEATH IT LISTS SIX ITEMS, AND FINDING (2) SAID *"four sweeps and a whole reading"*.** A
count of this side's own acts, asserted from the shape of the sentence rather than derived against
the list it introduces, and disagreeing with that list six lines below — **which is a statement of
this side's own disagreeing with another statement of its own in the same file, the shape the
hundred-and-thirty-sixth entry's errors (a) and (c) both have.** Corrected by NAMING the legs and
asserting no count, which is the record's own rule: put no number on this side's own acts at all.

**(b) FOUR SITES CITED ANOTHER EXTRACT'S CONTENT TO THAT EXTRACT WHERE IT HAD ACTUALLY BEEN TAKEN
FROM THIS SLICE'S PROGRESS RECORD'S SUMMARY OF IT** — the banner's row-11/15/12 lineage sentence,
finding (5)'s quotation of row 11's domain bound, the coupling section's description of row 15's
algorithm, and the centrality section's *"six of its findings"*. **This is the degradation rule's
own first named tell — citing a summary instead of the source — and the progress record's carried
item (e) states it in terms:** *"Cite what you take from an extract to that extract, never to a
summary of it."* **It was not notional: the quotation carried at second hand read *"as if it
measured **a** musical gain"* where row 11's extract's own line 379 reads *"as if it measured **the**
musical gain"*.** Corrected by reading the two passages at their own objects — row 11's extract line
379 and row 12's extract lines 675–676, both read this session — and, at the two sites where the
source was not opened, by saying at the site that the statement is the progress record's relay and
not a reading. **Counted as ONE**, being one shape at four sites corrected in one act; the judgment
is stated so it can be overruled.

**Both reached disk, so this side's own drafting check let both through, and its own read-back of the
landed copy found both.** **A read-back by the side that wrote the file is necessary and it is not
sufficient** — the hundred-and-thirty-sixth entry's own error (j) is the standing evidence for that,
and nothing here claims this side's passes were enough.

---

*Provenance: Cowork, 2026-09-07, the twenty-first reading session of L2's slice, at tip
`911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb` (read at `.git/HEAD` → `.git/refs/heads/master`, both
with the file tools). After the ordinary session-start read — `CLAUDE.md` whole (1,864 lines, read
at the staged object, NOT as a harness-delivered block: this session's project-instructions block
carries no `CLAUDE.md` text, so the departure the nineteenth and twentieth sessions declared was not
available and was not taken), `DECISIONS.md` whole (862 lines), `STATUS.md` whole, and the derived
gating answer's `★_the_live_gating_answer` block with its `gating_ids` list whole (244 open, 219
gating, 25 non-gating) — the hundred-and-thirty-sixth handoff entry whole (747 lines, 73,175 bytes at
the root listing), the hundred-and-eighteenth entry whole for its bridge-fault section,
`reading_pass/l2_slice_reading_progress.md` whole (2,159 lines), `reading_pass/candidacy_upgrades.md`
whole, both commissions whole, the L2 slice derivation whole, `docs/research_papers/BIBLIOGRAPHY.md`
whole, `reading_pass/population.md` at §3's verification rows V1 to V13, the row 12 extract at its
heading list and, in the guarded re-landing, at its centrality passage (lines 675–676), the row 11
extract at its domain-bound passage (line 379, staged for that purpose), and `FRAMEWORK.md` at §4.2
(237–271), the L2 charter (386–425), the design points DP-A to DP-Q
(669–817), A.3 (1133–1155), §S4 whole (1547–1628), §S5's entanglement argument (1631–1656) and §S6
whole (1777–1885) — **every one of those boundaries taken from that file's own heading list rather
than from where this side happened to be reading**. The staged tree was searched with `Grep` before
extracting for an IDENTITY sweep (`Ng`, `Jordan`, `naive Bayes`, `naïve Bayes`, `logistic
regression`, `discriminativ`, `generativ`, each case-insensitive) and over `FRAMEWORK.md` for the
BARE WORD `2001`, which returned no match at all in that file; and after the paper was read, a FIGURE
and MECHANISM sweep (`UCI`, `asymptotic`, `sample complexity`, `Vapnik`, `Efron`, `VC dimension`,
`conditional likelihood`, `joint likelihood`, `hybrid classifier`, `two-regime`, `optdigits`,
`ionosphere`, `lymphography`, `promoters`) and a reference-list sweep (`Anthony`, `Bartlett`, `Efron`,
`Goldberg`, `Jerrum`, `McLachlan`, `Rubinstein`, `Hastie`, `Vapnik`), which returned **no match
anywhere in the staged tree**. **How the three places were reached: all three fell to the identity
sweep; the bare-word, figure and whole-reading legs each reached NOTHING FURTHER.** Two false-positive
families are recorded at the top of this extract. The `reading_pass/extracts/` and
`docs/research_papers/` directory listings were both read, the first on the method warning row 48's
read paid for — and it is what produced finding (6). Every repository file was read with the file
tools from bridge-staged copies; no shell read any repository object or any staged copy, the declared
shell acts of this session being a `mkdir` and a `cp` in the container on this side's own files only.
No count of this project's own measurement is restated (#17f, `D-431`).*
