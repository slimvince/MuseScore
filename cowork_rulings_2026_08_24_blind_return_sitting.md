# Rulings — the blind-return sitting, 2026-08-24

> **STATUS: RULING RECORD.** Cowork, 2026-08-24 (the forty-fifth Cowork session, its fourth
> sitting). An interim carrier under the standing clause that a sitting record is written in the
> turn its ruling is given and lands in git at the next dispatch's Task 0.
>
> **Taken at branch tip `3a32d1e708`** (parent `e628334eeb`; both refs at the tip), read with
> `git for-each-ref` at the explicit hash on the user's machine, **after the blind deriving
> session (a fresh Cowork session, per Ruling 1 of
> `cowork_rulings_2026_08_22_deriving_side_sitting.md`) reported its output delivered; nothing
> was running and no commit was added.** The objects ruled on are the blind output's existence
> record (§1), the staged-pair enumeration of the deriving-side sitting's Ruling 3, and the three
> corpora files named in §2 — read at their own text on the user's machine.

---

## 0. The blind output — RECEIVED, verified at the object as a FILE, and deliberately UNREAD

`cowork_blind_derivation_harmony_boundary_2026_08_23.md` exists at the repository root: 89,497
bytes, sha256 `86b194280c9ab24fdb330cdf6fb34ab5a7cc70fe19b5ea25db7d7e0875ab0407` (matching the
figure the deriving session reported), untracked, its first line the title and its banner *DRAFT —
BLIND DERIVATION, NOT COMPARED, NOT RATIFIED*. **This session read the title and banner and
nothing more, by design:** the writing side is oracle-aware — it ruled the leak sittings and has
quoted the withheld answer — so the output's content is the comparison session's to read first.
No verdict of any kind is stated or implied here. The deriving session reported, beside the
delivery: the stop-on-meeting clause never fired (the positive statement is in its independence
record, with three disclosures); 26 statements, 10 open questions, a sizing record with measured
timestamps (a first draft with hand-typed times caught and corrected from the log by that session
itself). Those are the deriving session's statements, relayed as such and verified here only at
the file's existence and hash.

## 1. Ruling 1 — Open question 9 is answered A: the third chorale's human analysis is *When in Rome* folder 134; the pairing is by CONTENT, and the deriving-side enumeration's number-pairing is corrected of record (the user's word: "A")

**The facts ruled on, at the objects (each read at the file's own text on 2026-08-24).**
- The staged score `tools/dcml/bach_chorales/MS3/137 Du, o schönes Weltgebäude.mscx` carries its
  own title text `(BWV 301; R 071)`, a ONE-flat key signature, 4/4, and 14 bars (28 `Measure`
  elements over two staves).
- `tools/dcml/when_in_rome/Corpus/Early_Choral/Bach,_Johann_Sebastian/Chorales/137/analysis.txt`
  is a DIFFERENT chorale: *Wer Gott vertraut, hat wohl gebaut* (Tymoczko). The two corpora number
  by different schemes.
- *When in Rome* holds TWO folders titled *Du, o schönes Weltgebäude*: `087` (g minor, a stated
  two-flat signature, 13 bars, with `analysis_BCMH.txt`) and `134` (d minor — one flat — 4/4,
  14 bars, no BCMH file). **Folder `134` matches the staged score on every measured axis (title,
  signature, meter, bar count); `087` on none.**
- The pairs for chorales 001 and 003 agree title-to-title (BWV 269 *Aus meines Herzens Grunde*;
  BWV 153 *Ach Gott, vom Himmel sieh' darein*) and stand as staged.
- The deriving session aligned nothing to the mismatched pair and raised it as its Open
  question 9 — the finding is the deriving session's; the verification and the 134 identification
  are this session's.

**Ruled.** The third chorale's human analysis is
`tools/dcml/when_in_rome/Corpus/Early_Choral/Bach,_Johann_Sebastian/Chorales/134/analysis.txt`.
The comparison's annotated-score set is: 001 (`analysis.txt` + `analysis_BCMH.txt`), 003
(`analysis.txt` + `analysis_BCMH.txt`), and the BWV 301 score with folder 134's `analysis.txt`
(no BCMH exists for it — as none was claimed before). **The fact (not the rule) of the
deriving-side sitting's Ruling 3 enumeration is corrected of record:** its pairing of the third
chorale to folder `137` was taken by matching numbers, and for this chorale the number lied; the
rule — the repository's own human-annotation files, staged by name — stands and is what this
correction applies.

**The alternatives declined.** B — drop the third chorale, compare on two: declined; it shrinks a
sample the deriving session itself flagged as narrow, for a mismatch resolved at the objects.
C — keep folder 137: not defensible; declined of record.

## 2. What this sitting orders, and what it does NOT do

**Ordered:** one dispatch — Task 0 lands the blind output AS DELIVERED (byte-identical; a defect
found in it is a STOP, never a fix), this record, the modified handoff and the dispatch itself,
regenerates the evidence-pin membership, and PUSHES, with a commit subject that states no verdict
and names no comparison (Ruling 1 of the deriving-side sitting); Task 1 closes. **After it lands,
the next act is the COMPARISON SURFACE — designed by the writing side, ruled by the user — and
then the comparison dispatch.**

**Not done:** no comparison is performed or prepared in substance here; the blind output is not
read beyond its banner by this side; no oracle document is opened; no boot-list member, register
entry, sort artifact or register source is touched; no finding number is allocated (the series
stands at F88); no open-items row is created, flipped or discarded — [[OI-179]] stays OPEN and
GATES, [[OI-372]] and [[OI-374]] stand as found. The pack and the generator are untouched — the
re-pairing is an input list for the COMPARISON, not a pack matter; the blind derivation is done
and nothing about its inputs is re-run.

---

*Provenance: Cowork, 2026-08-24, the forty-fifth session's fourth sitting, at tip `3a32d1e708`.
The corpora facts were read at the files' own text with `grep`/`sed`/`python3` on the user's
machine (working-tree reads of data files outside the governed document set, declared in the
forty-ninth handover entry); the blind output verified at name, size, hash and banner only. The
user's word: "A".*
