# Consumer-build addendum — the D5 consistency-test ruling (Cowork, 2026-07-02)

> Your STOP was correct on all three counts, and the D5 premise error was **Cowork's** ("built from the same
> grammar" — asserted, not verified; the §5.0 set descends from the old scoring-bonus signals, not a complete
> functional grammar). The test proved its worth pre-birth. Ruling: **A now, B routed properly, C rejected.**

1. **Task 1: PROCEED in parallel** (unaffected, as you analysed).
2. **Task 2.1, final form (Option A, scoped honestly — not tagged around):** the test asserts the TRUE containment:
   every adjacent pair of every entry is EITHER licensed by `isLicensedProgression` OR on the **explicit,
   enumerated known-gap list** — exactly the 6 entries/12 motions you found, each row carrying delta + the verdict
   "musically correct; §5.0 grammar gap (L5-owned; L5 spec §15-12)". **Any 7th failure turns the suite red.** The
   test's comment block states both readings verbatim (mis-encoded vs grammar gap) and that these 6 are ruled
   grammar gaps on 2026-07-02. This is a pin plus a tripwire, not a silent log.
3. **Option B is routed to its proper layer, NOT this build:** Cowork writes the L5 §5.0 grammar-completion
   amendment (license ascending-fifth/plagal motion, descending-second/Phrygian step, the diatonic
   diminished-fifth link — algorithmic completion per theory, not tuning) for user ratification; it lands as its
   own small dormant L5 increment with its own tests, after which the Task-2.1 known-gap list shrinks to empty and
   the test tightens to the clean assert. Do not touch `functionprogression` this run.
4. **Option C rejected:** the entries are musically correct; data is never re-encoded to fit code.
5. **D5's premise text in the design doc is corrected by Cowork** (same day) to the measured state.

All other terms unchanged. Report the Task-2.1 test's final text verbatim in your report.
