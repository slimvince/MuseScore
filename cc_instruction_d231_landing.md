# CC dispatch — land the ruled D-231 rephrasing, with the six affected artifacts regenerated under the user's ruling permitting that named act

> **Dispatch (Cowork, 2026-08-15).** Executes §4 and §9 of
> `cowork_rulings_2026_08_15_phase_definition_sitting.md`. §9 is the user's ruling, taken at the
> previous batch's return, permitting the named act your previous STOP correctly refused to take
> on its own judgment: the four non-anchor artifacts regenerated alongside the two anchor
> remaps, in the same commit as the `CLAUDE.md` edit. Written at a verified STOP; nothing else
> is running.
> **Read first (every session):** `C:\s\MS\CLAUDE.md`, `C:\s\MS\STATUS.md` (header only),
> `C:\s\MS\BUILD_AND_TEST.md`. **Then read, in full:** the sitting ruling record named above
> (its §4 and §9 especially); your own `cc_report_phase_rulings_landing.md` and the batch's
> close (your measured six-red diagnosis is this dispatch's premise); and
> `cc_instruction_phase_rulings_landing.md` Task 1 step 2, which carries the ordered block's
> VERBATIM text — **this dispatch points at that text and does not restate it (#6); insert it
> from there, character for character.**
> The standing bars bind this batch whole: **no `src/` edit, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the
> analysis, no design, no repair, no derivation beyond the six named regenerations, no
> decisions-register entry** (the filtering ruling stands), **no open-items row created,
> flipped or discarded, no preparation-phase act, no file landed beyond what Task 1 names.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the
> end; record both states.** The expected start state: 48 run, 47 passing, ONE failing
> ([[OI-372]]'s tool), zero STOPs, no stale report. **A different start state is a
> STOP-and-report.** Verify every commit after the fact at the object and record the SHAs in
> the close. The reserved-word conventions and the ruled vocabulary replacements bind all new
> prose. **Reading rule from your own finding F3: `reaim_home_anchors.py --check`'s exit code
> is not evidence — the drift authority is `gen_cluster_dispositions.py --verify`'s report.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Sitting record §4:** the D-231 Conventions entry is rewritten in place — the superseding
  block inserted, former wording preserved (#12), the truth half replaced by the evidence rule.
- **Sitting record §9:** *"The four non-anchor artifacts are REGENERATED BY THEIR OWN
  GENERATORS alongside the two anchor remaps, in the SAME commit as the ruled `CLAUDE.md`
  edit. Nothing is hand-edited."* And its travelling bound: *"each regenerated artifact is
  diffed against its committed blob at an explicit hash before the commit; movement in any
  verdict, gate, cut or population field is a STOP."*

## Premise ledger

- **FACT** — the working tree is clean and the ordered block's verbatim text is committed
  inside `cc_instruction_phase_rulings_landing.md` (your Task 0, commit `1c52098b64`; the
  revert verified at the object, `61ae1b484c` both sides — your report §2.f, independently
  re-verified by the writing side at the staged snapshot).
- **FACT** — the six affected checks and the measured shape of each difference are your own
  report's §2.c, established by regeneration and object-level diff.
- **ASSUMPTION A1** — re-making the identical insertion turns red exactly the six checks
  measured, for the causes measured, and no seventh. **Check ordered: run the full guard set
  after the edit; a seventh new red, or one of the six red for a DIFFERENT cause than measured,
  is a STOP-and-report — the §9 authorization does not cover it.**
- **ASSUMPTION A2** — each of the four non-anchor regenerations changes ONLY the measured
  fields: `clause_opening` (completion inventory), the same field inherited (finish line), one
  `mentions_in_ratified_surfaces` entry (delegation bar), one live-list entry (reads5 repack).
  **Check ordered per §9's bound: diff each regenerated artifact against its committed blob by
  explicit hash BEFORE the commit; any movement in a verdict, gate, cut or population field is
  a STOP-and-report.**
- **ASSUMPTION A3** — `cowork_rulings_2026_08_15_phase_definition_sitting.md` differs from its
  committed blob ONLY by the appended §9 (the authorizing ruling) and §9's insertion point
  before the provenance line; `cc_instruction_d231_landing.md` is untracked. **Check ordered
  (Task 0): diff the ruling record against its committed blob at an explicit hash; any
  difference outside that appended section is a STOP-and-report.**

## Task 0 — land the authorizing ruling and this dispatch

1. **A3's check first.**
2. Commit, in ONE commit whose message names its own act, exactly these TWO paths and no third:
   `cowork_rulings_2026_08_15_phase_definition_sitting.md` (modified — §9 lands) and
   `cc_instruction_d231_landing.md` (new, staged plainly). Push. Verify at the object.

**Registered expectation E0:** `git diff-tree` on the Task 0 commit lists exactly 2 paths, no
staging override of any kind.

## Task 1 — the edit and the six regenerations, ONE commit

1. Insert the block VERBATIM from `cc_instruction_phase_rulings_landing.md` Task 1 step 2, at
   the ordered position (immediately after the D-231 entry's opening bold heading sentence,
   before `Three phases, strictly ordered.`), wrapped at the bullet's continuation indent
   exactly as your previous run wrapped it. Nothing else in `CLAUDE.md` moves.
2. Run the full guard set. **A1's check applies.**
3. Remap the two anchor-drift checks per the drift report's own per-citation practice
   (`gen_cluster_dispositions.py --verify`'s report is the authority), touching only what that
   practice touches.
4. Regenerate the four non-anchor artifacts BY THEIR OWN GENERATORS. **A2's check applies to
   each, before the commit.**
5. Regenerate `guard_state.json` (and `guard_classification.json` if it moves — expected
   byte-unchanged, no verdict moving) by their own generators; confirm each re-derives on a
   second run.
6. Run the full guard set again at the edited tree: **expected 48 run, 47 passing, ONE failing
   ([[OI-372]]'s tool), zero STOPs.** Any other state is a STOP-and-report.
7. Commit as ONE commit naming its own act and citing sitting record §4 and §9. **The commit
   carries: `CLAUDE.md`; the files the anchor-remap practice touches (named in the close, each
   with its reason); the four regenerated artifacts; the regenerated guard state. A path
   outside that set is a STOP before committing.** Push. Verify at the object.

**Registered expectation E1** *(the shape your own measurement established — no zero-deletion
limb this time, per your finding F1)*: the `CLAUDE.md` diff is ONE hunk, 24 insertions and 1
deletion, the removed line's words preserved verbatim across the added lines; afterwards the
truth-half sentence occurs in the file exactly once, inside the preserved text, with the
superseding block above it.

**Registered expectation E2:** every regenerated artifact's committed diff touches only the
fields A2 names — zero movement in any verdict, gate, cut or population field — proven at the
objects and quoted per artifact in the close.

## Task 2 — the close

One `STATUS.md` pointer entry and nothing else in that file. The FULL close appended to
`cowork_away_returns.md` — both guard-set states, every SHA, every expectation graded, every
problem declared. The report file `cc_report_d231_landing.md`, an ordinary tracked file.
Commit, push, verify at the object.

**Registered expectation E3:** at the tree carrying the close (one commit past Task 1), a
fresh `gen_guard_state.py --check` prints "the guard state re-derives" with 48 run, 47
passing, one failing ([[OI-372]]'s tool), zero STOPs — demonstrated by running it and reading
what it prints, across the commit boundary.

## What this batch does NOT do

- **No edit to any guard, generator or tool** — the anchor remap's per-citation practice and
  the named regenerations are the whole of what moves outside `CLAUDE.md`. `reaim_home_anchors.py`'s
  F3 defect is NOT fixed here (surfaced, awaiting disposition).
- **No preparation-phase act, no mining, no pruning, no caller-check, no landing of the 284
  newly visible or remaining ignored files.**
- **No open-items row touched; no decisions-register entry written.**
- **No `src/` edit, no golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`,
  no measurement of the analysis, no design, no repair, no pilot.**
- **[[OI-372]] and [[OI-374]] stay exactly as found. [[OI-179]] stays OPEN and GATES.**
