# Cowork independent audit — jointkeydecision (the SYNTHESIS layer, dormant) — to reconcile with CC

> Second-opinion pass from deep prior source verification (J-key-i/ii/iii) + the committed HEAD state
> (the dormant commit restored the J-key-i strong-signature-backbone). Correctness vs DCML key.

## Responsibility
`decideJointKey` — the **constrained-joint KEY decision** (dormant, flag-OFF): combine the SOFT evidence
(the scorer's per-region candidates + the cadence anchor + modulation spans + bass-is-root + declared hint)
over a **global key-path Viterbi** (+ a scoped joint on the coupled core), with the key candidates pinned to
the signature **home pair**. Output: per-region (tonic, mode). **This is the SYNTHESIS layer the other five
key-axis layers point to** — it is meant to resolve what none of them can alone.

## Correctness (the decision quality, vs DCML)
1. **[the WIN — correct] The SOFT broad-evidence re-rank beats production** +3.80/+3.50/+12.24 pp; S2
   −140/−96/−1706 (J-key-i, Cowork-verified). Integrating note + cadence + global evidence is the right move.
2. **[correctness · key-axis · UNSAFE] The home-fifths HARD pin pins WRONG ~17% (56 stems)** — partial/modal
   signatures whose fifths ≠ the DCML key are structurally unrepresentable (J-key-i §6a). The one hard
   constraint on the key axis fails the safety gate.
3. **[correctness · key-axis · inherits-cadence-wall] Non-chorale regressions** — the soft decision inherits
   the cadence anchor's I→IV/I→V over-detection → over-commits to subdominant/dominant on non-chorale (the
   J-key-iii snapshot regressions; the bridge-anchor windowing).
4. **[design finding] The scoped JOINT is INERT** on the key axis (+0.04/−0.14/−0.06 pp) — the chord×key
   coupling does not move the decision. **The win is the SOFT re-rank, NOT the joint search** → confirms the
   META-PRINCIPLE (precision lives in evidence breadth + calibration, not search).

## Completeness gaps
5. **[completeness · key-axis] Home-pair lattice** excludes non-signature keys (partial-sig unrepresentable;
   the demotion to soft was measured NOT separably fixable — J-key-ii/redux).
6. **[completeness · key-axis] Inherits the cadence/modulation imprecision + the scorer's relative-pair limit**
   — its soft inputs are the very layers audited above.

## ★ Phase-2 synthesis — this layer IS where the obligations converge
The joint decision is the combination point, so its correctness = the synthesis of the inherited gaps:
- **upstream root:** cadence precision (anchor/modulation feed it the I→IV/I→V noise) → fix UPSTREAM;
- **the note side:** the scorer's relative-pair limit → needs the global/cadential evidence the joint supplies;
- **the home pin:** the signature hard-pin is unsafe but the soft demotion isn't separably safe → calibration /
  a confident-contradiction signal (the unbuilt cadence-vote-for-home), or a learned emission;
- **the mechanism:** the joint SEARCH is inert; the SOFT re-rank is the lever → invest in soft evidence
  quality + calibration, not the lattice/search.

## Reconciliation targets (for CC)
- Confirm the soft win + the inert joint increment + the ~17% home-fifths unsafe rate (all prior-verified).
- Agree this is the convergence layer: the three obligations (cadence precision / joint combination / soft
  calibration) all terminate here.
