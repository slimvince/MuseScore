# CC dispatch — the preparation phase's NINTH batch: the session-start read cut at its ruled route — the live gating answer derived and established, the archiving pass run under the widened pointer constraint, and `BUILD_AND_TEST.md` demoted to a conditional read

> **Dispatch (Cowork, 2026-08-17).** Executes the rulings of
> `cowork_rulings_2026_08_17_session_start_read_sitting.md` (Rulings 1–3; Ruling 4 orders no act)
> and the two acts `cowork_rulings_2026_08_17_eighth_return.md` leaves for its successor (Ruling
> 1's standing constraint, and F49's correction at its generator), together with the F51
> correction at its three remaining sites. **Written at a verified stop; nothing else is
> running and no dispatch was active when this was written.** The empirical findings ledger, the
> fact-gate, the curated boot list, the mining, the landing of the newly visible instruction
> files, the whole-document archiving wave, and every act on any candidacy a regenerated census
> may flip are NOT in this batch.
>
> **Read first (every session):** `C:\s\MS\CLAUDE.md`, `C:\s\MS\STATUS.md` and
> `C:\s\MS\BUILD_AND_TEST.md` as they now stand — `BUILD_AND_TEST.md` unconditionally for THIS
> batch, because this batch runs measurement tools whose commands live there and because its own
> Ruling 3 does not bind until this batch performs it. An archive companion is reached only when
> a decision is re-opened. **Then read, in full:**
> `cowork_rulings_2026_08_17_session_start_read_sitting.md` (this batch's whole authority);
> `cowork_rulings_2026_08_17_eighth_return.md`, its §6 above all — the correction note and F51;
> `cowork_rulings_2026_08_16_preparation_return.md` §§5–6 (the archivability test §5(E), the
> continuous-pruning rule §5(D), and the STANDING-check treatment this batch's regenerations run
> under); `cowork_rulings_2026_08_17_callers_sitting.md` §4 (what a naming must be to HOLD a
> candidate, which is what Task 4 moves); your own `cc_report_preparation_eighth.md` §§2–4 and §9.
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/guard_classification.json` (Task 1's frozen-or-live split),
> `tools/audit/nongating_apparatus_rows.json` and `tools/audit/gating_row_sizing.json` (Task 1's
> two candidate answers), `tools/audit/phase1_completion_inventory.json` (the frozen enumeration
> Task 1 measures against and does not use), `tools/audit/retirement_caller_check.json` (Task 4's
> subject).
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis,
> no design, no repair, no derivation of any specification, no mining, no document archived,
> moved or deleted AS A FILE, **no open-items row created, flipped or discarded**, and **no
> caller flag or candidacy acted on** — a census crossing confers CANDIDACY only.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end;
> record both states.** The expected start state, which is the eighth batch's proven end state:
> **68 run, 67 passing, ONE failing — `gen_filing_convention_application.py --check` ([[OI-372]])
> — 4 not run, 16 historical, zero STOPs.** **A different start state is a STOP-and-report.**
> Verify every commit at the object by explicit hash and record the SHAs in the close. **The
> reserved-word conventions bind all new prose. The new-tool rule stands. The E-ordering rule
> stands. F3's reading rule stands** (`gen_cluster_dispositions.py --verify` is the drift
> authority; `reaim_home_anchors.py --check` exits 0 while printing drifted anchors and is not
> the authority). **Every guard run is taken at a quiet tree.**
>
> **★ A VOCABULARY RULE RULED 2026-08-17 BINDS EVERY LINE THIS BATCH WRITES (eighth-return §3).**
> A surface rates an alternative **TOWARDS the ultimate objective** and **TOWARDS the guiding
> principles** — never *against* either, because *Against:* is already the heading for the case
> OPPOSING an option and one token may not carry two contrary senses in adjacent context. The
> general form binds beyond that word: a term carrying opposite senses in adjacent context is
> fixed AT THE WORD, never left to the reader.

## Ruling ledger (quoted at the records, not paraphrased)

- **Session-start-read §1, Ruling 1:** *"A session no longer reads the whole index at session
  start. It reads the derived gating answer, and opens the INDEX when it needs a row."* —
  *"★ THE #19 PRECONDITION IS PART OF THE RULING AND NOT A FOLLOW-UP. The artifact a session
  reads instead must be POSITIVELY ESTABLISHED before anything relies on it — not merely
  unfalsified. Until it is established, the current read stands unchanged."* — *"it may NOT
  assume a live derivation exists. It DERIVES what the live gating answer is — at the live
  artifacts, under the six phases that superseded the three-phase structure, not at the frozen
  phase-1 cut — establishes it under #19, and STOPS to the user if no live derivation answers
  rule (b)'s question, rather than substituting a frozen record or authoring a new judgment."*
- **Session-start-read §2, Ruling 2:** *"The archivability test (§5(E) of the 2026-08-16 record)
  is applied to what has accumulated since the split — the resolved-opening rows still in
  `OPEN_ITEMS.md`, `DECISIONS.md` exhaustively, and any span in the five that the test places —
  riding whatever dispatch executes Ruling 1."* — *"A modest yield is the expectation, not a
  transformative one, and a pass that returns little is this ruling working rather than
  failing."*
- **Session-start-read §3, Ruling 3:** *"`BUILD_AND_TEST.md` is demoted from an unconditional
  session-start read to a CONDITIONAL one: mandatory for a session that builds, tests, or runs a
  tool whose command lives there."* — and for the half that binds harder: *"a condition would
  replace a mechanical bind with a judgment each session makes about its own work BEFORE reading
  the thing that would tell it whether the judgment is right. That is the never-work-from-memory
  rule's own failure shape and #19's silent-failure direction."*
- **Session-start-read §4, Ruling 4 (orders no act, but names this batch's mechanical
  consequence):** *"the callers sitting ruled that a mandatory-read or boot listing HOLDS a
  retirement candidate, so changing the boot list moves what holds candidates and the archiving
  wave's inputs move with it."*
- **Eighth-return §1, the standing constraint:** *"a span whose archive classification derives
  from text inside an archive pointer is NOT archivable, wherever in the span the pointer sits."*
  — *"The finer pass's own structural guard fires only when a span BEGINS with a pointer; that
  guard is hereby insufficient as written."*
- **Eighth-return §4, F49:** *"The remedy is a one-sentence qualification of `what_this_is`, at
  the generator, in the next dispatch."*
- **Eighth-return §6, F51:** *"The three other sites carrying the widened sentence — the
  seventh-return record §3 and the two handover blocks — need the same correction, as recorded
  acts, in the next dispatch."*

## Premise ledger

*(Every FACT pins content at content-addressed objects. No premise names a terminus commit, and
no premise asserts a changed-passage count — the F25 lesson.)*

- **FACT** — the eighth batch is complete, pushed, and verified at the objects by the writing
  side: the chain and every path count to the digit, and the E-ordering terminus the batch could
  not name. Its proven end-state guard shape is the start state declared above.
- **FACT** — the two ruling records this batch executes exist on disk untracked, written by the
  writing side on 2026-08-17.
- **FACT, established at the objects by the writing side and RE-ESTABLISHED by Task 1 rather
  than trusted from here** — in `tools/audit/guard_classification.json`,
  `gen_phase1_finish_line.py` and `gen_phase1_completion_inventory.py` both carry the verdict
  `records-a-point-in-time-measurement`, are OUT of the live guard list, and stand HISTORICAL;
  `gen_nongating_apparatus_rows.py` and `gen_gating_row_sizing.py` both carry
  `re-derives-a-live-invariant` and both PASS.
- **ASSUMPTION A1** — the working tree carries exactly ONE tracked modification,
  `cowork_handoff.md`, whose content is: **THREE session-close blocks inserted — the
  TWENTY-FIFTH** (uncommitted since it was written during the eighth batch's flight and
  deliberately declared rather than landed by that batch), **the TWENTY-SIXTH, and the
  TWENTY-SEVENTH** (written at the close of the session that wrote this dispatch) — **and the
  TWENTY-SIXTH, TWENTY-FIFTH and TWENTY-FOURTH headings each carrying an entry-point demotion
  marker.** Untracked: `cowork_rulings_2026_08_17_eighth_return.md`,
  `cowork_rulings_2026_08_17_session_start_read_sitting.md`, and this dispatch. **Check ordered
  (Task 0a, first act, blob against blob by explicit hash):** any tracked difference whose
  CONTENT falls outside that shape is a STOP-and-report.
  **★ WHY A1 NAMES THREE BLOCKS AND NOT TWO, recorded so the shape is checkable rather than
  puzzling:** the twenty-seventh block and this sentence were written in the SAME act. A handover
  block written after a dispatch has already stated its premise about the working tree is how a
  false premise reaches the executing side — the A1 premise error's own shape — so the block and
  this assumption were amended together rather than in sequence.
  **★ ONE DECLARED CAVEAT ON THAT CHECK, MEASURED BY THE WRITING SIDE AND STATED SO IT CANNOT
  RAISE A FALSE STOP.** The tree stores its text files in BOTH line-ending conventions. The
  repository marks them `text=auto` and git normalises on check-in, so git reports no
  modification — but a raw byte copy of such a file does not hash equal to its blob. Measured over
  the governing and high-traffic files at the repository root: **most hash equal directly, and a
  comparable number differ from their blobs by EXACTLY their own line count** — one carriage
  return per line and nothing else — **including `DECISIONS.md`, which sits among four mandatory
  reads that hash equal without adjustment.** **Compare through git, never through a raw byte
  copy**, and where a byte-level comparison is used at all, normalise line endings before it
  decides anything. **A file differing from its blob by exactly its line count is NOT a
  modification and is not a STOP.**
- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  **Check ordered (each task's last step):** any other red is classed by its measured cause, and
  one outside this batch's own acts' subjects is a STOP-and-report. The reds this batch itself
  causes and must clear by their own ruled treatments: the retirement caller census, whose holds
  move when the mandatory-read listing changes (Task 4, the Ruling-4 consequence quoted above);
  any artifact whose subject is a span the Task 3 archiving pass moves (the F35 class); and the
  decisions register's rendered views if Task 0b's F49 correction reaches the data file.
- **ASSUMPTION A3** — the live-or-frozen split stated as FACT above still holds at this batch's
  base commit. **Check ordered (Task 1, first act, at the objects):** a different split is a
  STOP-and-report, because Ruling 1's whole first task rests on it.
- **ASSUMPTION A4** — every span the Task 3 archiving pass proposes reads, whole, as its class
  says. **Check ordered (read-before-move, binding every span):** a span that does not read as
  its class assumes STAYS AT SITE and is flagged; the doubt default decides every span the test
  cannot place; a forced move is never available.

## Task 0a — land the two ruling records, the handover block and this dispatch

The records must be tracked before anything corrects them, which is why Task 0's seven subjects
land in two commits rather than one. This is the first.

1. **A1's check first, entirely at content-addressed objects**, and under A1's declared
   line-ending caveat.
2. Commit exactly these FOUR paths and no fifth: `cowork_handoff.md` (modified),
   `cowork_rulings_2026_08_17_eighth_return.md` (new),
   `cowork_rulings_2026_08_17_session_start_read_sitting.md` (new),
   `cc_instruction_preparation_ninth.md` (new). Staged plainly; push; verify at the object.

**Registered expectation E0a:** exactly 4 paths at the enumeration; one modification whose
content matches A1's stated shape, three additions; no staging override.

## Task 0b — the three recorded corrections, in ONE commit

Each is a correction of record: the corrected wording is PRESERVED (#12), never deleted, and
each carries its date and the finding it answers.

1. **F49 — the retired-entries block's opening sentence.** Its generator is
   `tools/audit/decisions/apply_soft_discard.py`, in the `what_this_is` string of the retired-block
   assignment; the rendered value stands in `tools/audit/decisions/backbone_decisions.json`. The
   sentence attributes the whole block to one ruling — *"Entries SOFT-DISCARDED from the live
   record by the user's ruling of 2026-08-16 §3(A)"* — while the block now also holds the entries
   the 2026-08-17 residue sitting retired. The block's correcting field
   (`★_this_block_now_holds_more_than_one_retirement`) enumerates `retired_on`, `retired_by` and
   `the_retiring_authority` by name and does not name `what_this_is`, which is the prose a reader
   meets first. **The remedy is the ruled one and no more: ONE qualifying sentence, written at
   the generator and rendered into the data file in the same act**, saying that the block holds
   more than one retirement and pointing at the `acts` list where each act records itself. **No
   verdict, population, arithmetic or entry identity moves.** The generator's own `--check` must
   pass at the end of the act; a disagreement between the generator's literal and the data file's
   value is a STOP-and-report, not an edit to whichever is convenient.
2. **F51 — the three remaining sites of the widened sentence.** The curated boot list's ruled
   definition is the implementation-free read list an implementation-blind session boots from,
   **for those sessions only**; one unchecked sentence widened it to the session-start read
   generally, and Ruling 4 of the session-start-read sitting has since confirmed the narrow
   definition. Correct, at each site, as a recorded act with the former wording preserved:
   (a) `cowork_rulings_2026_08_17_seventh_return.md` §3, the sentence beginning *"The standing
   route to a materially smaller session-start read is the CURATED BOOT LIST"*; (b) the
   TWENTY-FOURTH block of `cowork_handoff.md`; (c) the TWENTY-FIFTH block of `cowork_handoff.md`.
   Each correction names F51, names the ruled definition, and states that Rulings 1–3 of the
   session-start-read sitting are the route for ordinary sessions. **A ruling record is corrected
   by appending a dated correction that quotes what it replaces, never by editing the ruled text
   in place.**
3. **The eighth-return Ruling 1 standing constraint, written where a future pass meets it.** The
   constraint is quoted in the ruling ledger above. Its site is
   `tools/audit/gen_claude_md_finer_spans.py`, whose archive-pointer recognizer is anchored to
   the start of a span and therefore fires only when a span BEGINS with a pointer. **Write the
   constraint at that recognizer AND make the guard answer it: a span whose archive
   classification derives from text inside an archive pointer is not archivable wherever in the
   span the pointer sits.** The widening errs only in the recoverable direction — a span it
   declines to place falls to the doubt default and stays at site. **It is a recognizer change
   and it moves no fate:** `gen_claude_md_finer_archive.py --apply` is NOT run by this batch or
   any other without a fresh ruling, and `CLAUDE.md` is not touched by this task.
4. Commit, push, verify at the object.

**Registered expectation E0b:** the decisions register's own check passes with the generator and
the data file agreeing; each of the three F51 sites carries a dated correction with its former wording
preserved beside it; the widened recognizer re-derives its artifact and every span it now
declines is marked doubt-defaulted rather than dropped; **no verdict, no fate, no population and
no row moves anywhere in this commit.**

## Task 1 — DERIVE and ESTABLISH the live gating answer, in ONE commit

This is Ruling 1's first task and the ruling names it as such. **Nothing in Task 2 may run until
this task's establishment either succeeds or STOPS.**

1. **Re-establish A3 at the objects**, at this batch's base commit, reading
   `tools/audit/guard_classification.json` by explicit hash: which of the candidate derivations
   are HISTORICAL and frozen, and which are live and passing. **A different split from the one
   the premise ledger states is a STOP-and-report.**
2. **Derive what the live gating answer actually IS, and state the test in the ruling's own
   terms.** Rule (b) of the open-items register is *a stage may not open while a register item
   gating it is open*, so the answer a session reads must let that session tell **which rows
   gate** — a count alone does not answer it. Establish, at the artifacts and not from their
   headers: **(i)** whether any LIVE derivation publishes the gating rows BY IDENTITY, derived
   from the INDEX as it stands today; **(ii)** where each candidate's population comes from,
   followed link by link to its source. **The known shape, to be confirmed or refuted rather than
   assumed:** `gen_nongating_apparatus_rows.py` parses `OPEN_ITEMS.md` on every run and publishes
   the non-gating side by identity and the gating side by count and by ground, while
   `gen_gating_row_sizing.py` — live as a check — draws its population from
   `tools/audit/phase1_completion_inventory.json`, whose generator is frozen HISTORICAL because
   its subject is the superseded three-phase structure. **If that shape is confirmed, the one
   artifact enumerating gating rows by identity draws that enumeration from a superseded record,
   which is F22's ossification reaching the answer this ruling needs.**
3. **The bounded act this task is permitted, and its limits.** Where a live derivation already
   determines the gating verdict for every open row but does not PUBLISH the gating side by
   identity, **the permitted act is to publish what that derivation already computes** — the
   gating identities being the complement of the non-gating verdicts over the open rows the tool
   already parses. **That is a publication, not a judgment.** **STOP-and-report instead of
   proceeding if** any of the following holds: the complement cannot be computed without a new
   authored verdict; the tool would have to import a frozen artifact to complete it; or the act
   would require widening the ruled criterion, the ruled exemption or the ruled default in any
   way. **Under no circumstance substitute a frozen record or author a new judgment** — the
   ruling forbids both by name.
4. **Establish the result under #19 — POSITIVELY, never merely unfalsified.** The established
   answer must reconcile **in both directions** against the INDEX itself: every gating identity
   located at an open row of `OPEN_ITEMS.md`, every open row accounted for as gating or
   non-gating with its ground, and **a row the derivation cannot place halts the run.** The
   establishment is recorded with the artifact — what was derived, from what, and what would
   falsify it — so a later session can challenge the derivation rather than rediscover the
   question.
5. **Measure the frozen enumeration against the live one, publish the difference, and act on
   none of it.** Compare the derived gating set against
   `tools/audit/phase1_completion_inventory.json → the_gating_split.gates.ids` and publish every
   difference with the side it falls on. **This is the measurement of what the ossification has
   been hiding and it is evidence for the phase's retrospective, not an act:** no row is flipped,
   created or discarded by it, and `gen_gating_row_sizing.py`'s own population is left exactly as
   it stands, because changing it is a separate act over a frozen artifact and is not ruled here.
6. **The read-regime change, and ONLY if step 4 established.** Amend rule (a) of the open-items
   register section of `CLAUDE.md` so a session reads the established derived gating answer at
   session start and **opens the INDEX when it needs a row**. **Rule (a)'s authority is
   untouched: the INDEX remains the authoritative status surface, and the substitute is a route
   to one question it answers, never a second home for status (#6).** The superseded wording moves
   to `CLAUDE_ARCHIVE.md` in the same act under the continuous-pruning rule (§5(D) of the
   2026-08-16 record), a compact dated pointer left at the site. **If step 4 did not establish,
   this step does not happen and the current read stands unchanged** — that is the ruling's own
   instruction, not a fallback.
7. Commit, push, verify at the object.

**Registered expectation E1:** the live-or-frozen split re-derives as stated; the gating answer
is derived at the INDEX as it stands today and reconciles in both directions with a halt on any
unplaceable row; the frozen-against-live difference is published and acted on in no direction;
`CLAUDE.md`'s rule (a) is amended **if and only if** the establishment succeeded; **no row is
created, flipped or discarded and [[OI-179]] stays OPEN and GATES.**

## Task 2 — the archiving pass the ruled test already authorises, in ONE commit

Ruling 2's pass, taken in the order the sitting's own §5 binds — after Ruling 1's establishment
and before Ruling 3's demotion. It runs AFTER Task 0b, so the widened archive-pointer constraint
binds it.

1. **The subjects, exactly as the ruling names them:** the resolved-opening rows still carried in
   `OPEN_ITEMS.md`; `DECISIONS.md` exhaustively; and any span in the five governing files that
   the archivability test (§5(E) of the 2026-08-16 record) places. **No new mechanism is ruled
   and none is needed** — the test is standing, the machinery is built and proven byte-faithful,
   and the destinations are the ruled per-file companions.
2. **The safeguards bind every span, and each is the ruling's own:** the read-before-move
   safeguard (A4) — every span READ WHOLE before it moves; **the doubt default — a span the test
   cannot place STAYS AT SITE and is marked**; the eighth-return standing constraint — a span
   whose archive class derives from text inside an archive pointer is not archivable wherever the
   pointer sits; byte-faithful both-directions reconciliation; a compact dated pointer at every
   site naming its own companion; anchors re-aimed per citation from the drift report.
3. **The four spans REFUSED at the seventh-return sitting, and the two ruled spans the eighth
   batch's safeguard refused, are not re-proposed by this pass.** Their refusals are readings
   performed and recorded; a pass that proposes any of them again must answer the recorded reason
   rather than rediscover it, and this batch does not answer them.
4. **The expectation is recorded honestly, because it was stated in the surface and accepted with
   the ruling:** the last two passes both under-delivered against their predictions. **A modest
   yield is the expectation, and a pass that returns little is this ruling working rather than
   failing.** Do not stretch a judgment to reach a verdict, and do not report a small yield as a
   shortfall.
5. Affected artifacts regenerate under the ordinary enumerated-movement bound; commit, push,
   verify at the object.

**Registered expectation E2:** every moved span is byte-present in its companion exactly once and
absent from its parent, and every span left at site is byte-present at site exactly once — **both
directions proven, the second being the load-bearing one**; every doubt-defaulted span is marked;
every re-aimed anchor verified at its new coordinate; **no verdict, gate, cut or population
moves, and no document is archived AS A FILE.**

## Task 3 — `BUILD_AND_TEST.md` demoted to a CONDITIONAL read, `DECISIONS.md` explicitly not, in ONE commit

1. **The demotion, written where a session meets it.** In `CLAUDE.md`'s build-and-test section —
   the block that today reads *"Always read these three files at the start of every session"* —
   `BUILD_AND_TEST.md` becomes a CONDITIONAL read on the twice-ruled pattern (the
   `docs/scoring_model.md` conditional; Ruling 64's conditional read): **mandatory for a session
   that builds, tests, or runs a measurement tool whose command lives there.** The condition is
   written in those terms and no others; the superseded wording moves to `CLAUDE_ARCHIVE.md`
   under the continuous-pruning rule with a dated pointer at the site.
2. **`DECISIONS.md` is explicitly NOT demoted, and the ground is recorded at the site**, because
   a later ruling may revisit it and must have the ground to answer: rule (a) rests on the stated
   premise that rulings bind mechanically only if every session reads them, and a condition would
   replace a mechanical bind with a judgment each session makes about its own work before reading
   the thing that would tell it whether the judgment is right. **Nothing about `DECISIONS.md`'s
   read is edited** beyond recording that it was considered and ruled out.
3. **This batch's own reading is unaffected** — the read-first block above orders
   `BUILD_AND_TEST.md` read unconditionally for this batch, and this task does not
   retroactively change what any session has already read.
4. Commit, push, verify at the object.

**Registered expectation E3:** the condition is stated in the ruled words at the site a session
meets; the superseded wording is preserved in the companion with its pointer; `DECISIONS.md`'s
session-start read is unchanged and its ground recorded; no other read regime moves.

## Task 4 — the retirement caller census regenerated for the moved read regime, in ONE commit

Ruling 4's named mechanical consequence, and the reason it is a task rather than a note: **a
mandatory-read or boot listing HOLDS a retirement candidate**, so Tasks 1 and 3 have both moved
what holds candidates — Task 1 at the open-items register's rule (a), Task 3 at the build-and-test
listing — and Task 2 has moved namings by archiving spans that carried them (the F35 class).

1. Regenerate `gen_retirement_caller_check.py` at **Task 3's commit** (the content-addressed pin,
   `--at` that commit), with the ruled kinds of the callers sitting unchanged.
2. **Every movement against the previous committed reading is enumerated and classed**, with the
   read-regime change carried as its own named cause beside the existing classes. **A movement
   fitting no class is a STOP-and-report** — never a fourth class taken silently; if a new class
   is genuinely needed it is DERIVED on the same shape as the others, tested against the census's
   own published fields, and declared in the report and the close.
3. **A verdict that flips toward PASSES-THE-CHECK confers CANDIDACY ONLY.** No candidacy is acted
   on, no file is archived, and every ruled condition on the archiving wave (mined-first;
   members-seen-by-the-user-first for the stray root files) stands untouched.
4. **The KIND-UNDERIVABLE population returns to the user as published data** — a caller whose
   kind the derivation cannot place still STOPS to the user and is never guessed.
5. Commit, push, verify at the object.

**Registered expectation E4:** the check PASSES at the new pin; every difference is classed with
zero unclassed; every set-aside naming stays published; any verdict flip is enumerated and
confers candidacy only.

## Task 5 — the close

One `STATUS.md` pointer entry per task — and, per the standing Ruling-4 bound of the
governing-surface split record, **the same act moves the EIGHTH batch's entries verbatim to
`STATUS_ARCHIVE.md`** (`gen_status_batch_bound.py`), both directions proven. **The declared
`Last updated: ` prefix adjustment F48 names is imported, not re-decided (#6); an entry needing a
second adjustment still halts.**

The FULL close is appended to `cowork_away_returns.md` as a PREPARATION NINTH BATCH section: both
guard states, every SHA, every registered expectation graded MET or NOT MET with its reason,
every A4 flag, every declared departure, and the carried findings restated for the phase's
retrospective — **F1–F51, the E3 ordering defect and the A1 premise error, surfaced and none
rowed; the row bar stands whole this batch.** The report is `cc_report_preparation_ninth.md`, an
ordinary tracked file. Commit, push, verify at the object.

**★ THE E-ORDERING RULE, AND THE SMALLER-GRAIN INSTANCE F48's SIBLING DECLARED.** The end-state
guard run is taken AFTER the Task 5 commit exists, and its output with the final SHAs lands in
ONE FURTHER commit. **A batch-level summary sentence written in the same commit as the work it
summarizes is the same defect at a smaller grain** — the eighth batch declared one. Write no
`STATUS.md` entry and no close sentence asserting the end state before the run that produces it
exists; if one is unavoidable, declare it in the close rather than leave it to be noticed.

**Registered expectation E5:** at the tree carrying the close, a fresh full guard run re-derives
— **every run check passing except [[OI-372]]'s tool, zero STOPs** — run and read, never
inferred, its values committed only after the run that produced them.

## What this batch does NOT do

- **No curated boot list.** It stays at its ruled definition — the implementation-free read list
  an implementation-blind session boots from, for those sessions only — and remains a live,
  unblocked preparation output awaiting its own drafting and sitting. Ruling 4 orders no act.
- **No fate on any `CLAUDE.md` span ruled at the eighth-return sitting.** Both stay at site, that
  question is CLOSED, and `gen_claude_md_finer_archive.py --apply` is not run.
- **No candidacy acted on, no document archived, moved or deleted AS A FILE, no mining, no
  empirical findings ledger, no fact-gate admission**, no completion claim about the superseded
  phase-1 program, no derivation, design, repair or pilot act.
- **No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
  `tools/robust_stop/`, no measurement of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly as
  found; **[[OI-179]] stays OPEN and GATES**; F3 stays surfaced, unfixed and unrowed.
- **`gen_gating_row_sizing.py`'s frozen population is measured and left alone.** Regenerating it
  against a live population is a separate act over a frozen artifact and is not ruled here.
