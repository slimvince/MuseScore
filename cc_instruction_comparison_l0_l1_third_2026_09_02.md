# CC dispatch — the L0/L1 COMPARISON, THIRD WRITING: resume the tabulation at position 8 of 29, finish the population, and write the whole-population sections — proposals only, NO decision taken, NO session booted

> **Dispatch (Cowork writing side, 2026-09-02, the session that booted on
> `cowork_handoff_entry_eighty_nine.md`, its third dispatch).** Executes **Rulings 32 and 33** (§3am
> and §3an of `cowork_rulings_2026_08_31_decision_surface_sitting.md`) and, through them, Rulings 1 to 4
> and §5 of `cowork_rulings_2026_08_24_comparison_design_sitting.md` and the disposition discipline of
> `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §0 and §3.4. **Written at a
> verified stop: the second writing's batch returned with seven of twenty-nine documents tabulated and
> a recorded stop at the boundary between positions 7 and 8; its twelve commits are verified at the
> objects and pushed; nothing is running.**
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** It is the CONTINUATION of the comparison the second
> writing opened, under the same rules, resuming where the reading file's own §0 says it stopped.
> Task 0 lands the ninety-first entry and this dispatch, and PUSHES. Task 1 tabulates positions 8 to
> 29 of the population in the artifact's order, one document per commit, under exactly the rules the
> second writing gave (repeated below so this instruction is self-sufficient), and then — once and
> only once every document is done — writes the three sections the reading file marks NOT YET
> WRITTEN and completes the gathers over the whole population. Task 2 closes. **THIS BATCH DECIDES
> NOTHING: every disposition is a PROPOSAL the user ratifies later; every difference is put as a
> proposal, never applied. It BOOTS NO SESSION, BUILDS AND RUNS NO MEASUREMENT OF THE ANALYSIS,
> DERIVES NO SPECIFICATION STATEMENT, and EDITS NOTHING BUT ITS OWN DELIVERABLES.** Your framing is
> NEUTRAL throughout: you tabulate; you neither refute nor confirm.
>
> **★ THE WRITING SIDE IS PUTTING DISPOSITION SURFACES TO THE USER WHILE THIS BATCH RUNS, over the
> rows of documents 1 to 7.** That is the standing cadence, not a collision, and two things follow for
> you. **(1)** `cowork_rulings_2026_08_31_decision_surface_sitting.md` may be MODIFIED on disk at any
> point during your run, by the writing side, with new rulings; **you do not read it beyond §3am and
> §3an, you do not land it, and its modification is NOT a STOP** — A1 below names it. **(2)** Nothing
> the user rules on documents 1 to 7 changes a row you write for documents 8 to 29: you tabulate
> against the derivation as it stands (blob `9b102c57…`, unchanged), and a ruling on a family of
> rows is applied later by a separate act. **You do not read any ruling taken during your run, and
> you do not look for one.**
>
> **★ THE ORDERED FIRST READ COMES BEFORE YOUR SESSION-START READ, BY RULING, AND THE DEPARTURE IS
> OF RECORD (Ruling 2 of the comparison-design sitting, applied by Ruling 32).** Your VERY FIRST
> read this session is `cowork_blind_derivation_l0_l1_2026_08_31.md` — **§5 first, then §6, then
> §7, then the whole file** — before `CLAUDE.md`, before anything. *Why:* the ordinary
> session-start read includes `CLAUDE.md`, whose Conventions section states in words how slice
> identity currently works (the derivation's own §6.2 records those lines as passages it met), and
> the ruling orders that you understand what the blind session claimed before the current text's
> frame exists to map it onto. The read SET of the session-start bar is unchanged; its ORDER is
> changed by the ruling. **Your SECOND read is the reading file as it stands,
> `ratification_surfaces/cowork_comparison_l0_l1_reading.md`, WHOLE** — §0 to §16, every row of
> documents 1 to 7 — because your rows must use the same unit, the same vocabulary and the same row
> shape, and because the families of difference those rows already carry (grace notes; the clip and
> the published domain; what a slice carries; the enlargement contract; eligibility; the no-horizon
> rule; the metric strength class) will be met again in documents 8 to 29 and must be named as the
> same family when they are. Then: **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full;
> `BUILD_AND_TEST.md` — CONDITIONAL, and this batch MEETS the condition (it runs the guard set):
> read it in full; rule (a)'s `gating_ids` at `tools/audit/nongating_apparatus_rows.json` →
> `★_the_live_gating_answer` (DO NOT read the whole open-items index). **(2)** `cowork_handoff.md`,
> ITS TOPMOST ENTRY, AND `cowork_handoff_entry_ninety_one.md` whole — the current entry point,
> untracked at the root. **(3)** `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL —
> every `###` section carrying the standing-clause marker binds this batch; the marker is the
> population, and no number is written here. **(4)**
> `cowork_rulings_2026_08_31_decision_surface_sitting.md` §3am and §3an ONLY — this batch's
> authority. **(5)** `cowork_rulings_2026_08_24_comparison_design_sitting.md` whole. **(6)**
> `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §0 (the referents) and §3.4.
> **(7)** `cowork_blind_session_brief_l0_l1.md` §2 (the seven faces), §4 (the six-field form) and
> §7. **(8)** `tools/audit/derivation_boot_pack.json` → `subjects` → `l0-l1` →
> `the_members_as_rendered` (members 7 and 8) and → `LEAKS`; `THE_WITHHELD_FAMILY` for this subject
> is EMPTY BY RULING, so no D-450/D-575 caveat applies to any row. **(9)**
> `cc_report_comparison_l0_l1_second_2026_09_02.md` whole and the close it points at in
> `cowork_away_returns.md` (*THE L0/L1 COMPARISON TABULATED (SECOND WRITING), NOTHING DECIDED* and
> its end-state subsection). **(10)** `tools/audit/l0_l1_outgoing_population.json` →
> `the_population_in_comparison_order` — positions 8 to 29 are your work list, in that order. **(11)**
> The outgoing texts are opened AS TASK 1 ORDERS, one at a time, not read whole up front.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The derivation* — the blind output: 54
> statements (S-1 to S-54, in the six-field form) and 17 open questions (OQ-1 to OQ-17), written by
> an implementation-blind session that read the brief, the ten-member boot pack, four staged scores
> and fetched research, and nothing else of this project — **relayed from its own §5; you count
> them yourself and your counts are the figures of record.** *The outgoing text* — the current
> specifications describing what the new L1 charter now covers; under the phase definition it is a
> WITNESS read after the derivation, evidence about the present text and never authority over a
> derived statement. *The outgoing population* — the set of passages of the outgoing text this
> comparison covers, fixed by Rulings 32 and 33 and published at the population artifact; positions 8 to
> 29 are this batch's. *An outgoing statement* — one atomic
> claim of the outgoing text about what the analysis does, must do or may assume for L0's or L1's
> subject: one sentence or one bullet, never a paragraph. *A disposition* — one of the five fates
> the phase definition names for an outgoing statement: **adopted** (its content is carried by a
> derived statement, or is proposed for adoption), **relocated** (its content belongs to another
> charter — L2, L3, the voice-leading axis, or the measurement design — and goes to the
> cross-layer transfer list), **quarantined** (it states how the implementation currently works, or
> a measured property of it, and becomes an audit question), **discarded** (under principle #10's
> worth test, with finding, date and reason), **historical** (it records an event, a build state,
> a superseded plan or a status, not a rule). *The transfer list* — the parking list for content
> found in the wrong charter; it exists so nothing is silently dropped. *The reading file* — your
> deliverable, `ratification_surfaces/cowork_comparison_l0_l1_reading.md`.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, **no measurement of the ANALYSIS
> built, designed, scoped or run** (the comparison is TEXTUAL), no design, no repair, **no
> derivation of any specification statement**, NO SESSION BOOTED, no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit to any
> governing document, to the derivation, to the brief, to the pack or its generator, to any register
> entry or register source, to any outgoing text**, **no disposition APPLIED anywhere** — the transfer
> list is written inside the reading file and nowhere else — **no verdict word attached to the
> derivation's independence record** (relay its contents as facts), **no recommendation about the
> derivation, the method or the split of L0 from L1 anywhere** — not in the reading file, not in the
> report, not in a commit message.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (CHECK mode) and again
> at the end (write mode, committed as the artifact of a real run). Verify every commit at the
> object by explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and
> `git diff` over the working tree time out on this mount and are denied by the armed guard — use
> the per-path explicit-hash forms named in A1. If staging is refused by a stale zero-length
> `index.lock` with no git process running, establish the cause (D-669), remove it, report the act.
> Plain single-quoted `git commit -m '…'` subjects. The reserved-word and vocabulary rules bind
> every line — *score* only the musical score, *key* only tonality, *bar* only the metric unit (the
> prohibiting sense is *exclude*), no numeric grade anywhere: the vocabulary in §T2 is the whole of
> the verdict language.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 33, §3an (the population):** *"a term-search hit enters the outgoing population only if its
  file is a member of the specification document set. The population is therefore Ruling 32's eleven
  named members plus every hit file in that set."* The artifact's ordered list is that population.
- **Ruling 3 of the comparison-design sitting (the vocabulary's form):** closed, two axes, words not
  numbers, fixed before grading; every row carries both texts' own words; *"a verdict the comparer
  cannot defend in one sentence at the texts is delivered UNGRADED, with what was read."*
- **Ruling 4 of the same sitting:** *"The comparison tabulates; the method ruling comes to the user
  afterwards as a Cowork-written decision surface over the tabulation."*
- **The phase definition §0, the disposition discipline:** *"every statement of an outgoing text
  reaches ONE recorded disposition — adopted / relocated (to the cross-layer transfer list) /
  quarantined (as an audit question) / discarded (under the worth test, with finding, date, reason) /
  historical — with completeness checked by arithmetic."* §3.4: *"a silent rewrite where a proposal
  per difference is owed; a disposition class invented beyond the five"* — both NOT ALLOWED.
- **D-672 (the standing clause the second writing stopped under):** *"A per-entry pass may be stopped
  at any member boundary, and the stop is RECORDED — what was done, what was not, and that the
  remainder is untouched rather than partly worked."* This batch resumes at that recorded boundary.

## Premise ledger

- **FACT** — the local branch tip is `4cae08c06a8b637f63cd09ee86421284ba0f79d4` (parent
  `2dd7131f8c02f315b0bb5b304a5cef5f027c37b7`), the second writing's end-state commit; `origin/master`
  is the SAME hash. Read by the writing side at the two ref files with the file tools, 2026-09-02,
  after the batch returned. **Nothing is unpushed. Re-read the refs yourself before Task 0.**
- **FACT** — at the tip, `tools/audit/guard_state.json` → `summary` records **run 76**, passing 66,
  **failing 10**, not_run 4, historical 16, **no unclassified candidate** — and it was WRITTEN BY
  THAT COMMIT (its path list is `cowork_away_returns.md`, `tools/audit/guard_state.json` and
  `tools/audit/l0_l1_outgoing_population.json`). The ten failing tools:
  `gen_filing_convention_application.py --check`, `gen_artifact_inventory.py --check`,
  `gen_artifact_inventory_surface.py --check`, `gen_test_construction_evidence.py --check`,
  `gen_retirement_caller_check.py --check`, `decisions/apply_soft_discard.py --check`,
  `decisions/apply_residue_discard.py --check`, `gen_epoch_write_path.py --check`,
  `gen_recognizer_establishment_sort.py --check`, `decisions/gen_cluster_dispositions.py --verify`.
  **Re-read the artifact yourself at the object `4cae08c0…:tools/audit/guard_state.json`.**
- **FACT** — the reading file `ratification_surfaces/cowork_comparison_l0_l1_reading.md` is TRACKED
  at the tip and its §0 records positions 1 to 7 DONE and 8 to 29 UNTOUCHED, resuming at position 8;
  its §13 records 302 statements placed over the seven, arithmetic closing. Read at the file by the
  writing side. **Its counts are its own; none is restated as a figure of record here.**
- **FACT** — the population at `tools/audit/l0_l1_outgoing_population.json` →
  `the_population_in_comparison_order` has 29 entries; positions 8 to 29 are, in order:
  `ARCHITECTURE.md` (the section *"#### Layer 5 — the function/cadence layer"*),
  `cowork_layer5_function_design.md`, `cowork_phrase_boundary_design.md`,
  `cowork_joint_estimator_factorization.md`, `cowork_evidence_inventory.md`,
  `cowork_layer6_grouping_design.md`, `cowork_target_architecture.md`,
  `cowork_layer3_keymode_design.md`, `cowork_score_census.md`, `docs/scoring_model.md`,
  `cowork_bounded_context_design.md`, `cowork_voiceleading_axis_design.md`,
  `docs/llm_integration.md`, `cowork_joint_estimator_architecture.md`,
  `cowork_layer4_chordsymbol_design.md`, `cowork_prefit_gates.md`,
  `cowork_layer5_engagement_design.md`, `cowork_notation_adoption_increment.md`,
  `cowork_idiom_entry_mapping.md`, `cowork_notation_output_contract.md`,
  `cowork_progression_schema_dictionary.md`, `cowork_stage5_fitter_design.md`. Read at the committed
  object by the writing side; **the artifact is the list of record, and a disagreement between this
  ledger and the artifact is resolved in the artifact's favour and reported.**
- **FACT** — at the tip: `cowork_rulings_2026_08_31_decision_surface_sitting.md` is TRACKED (blob
  `31163be6…`) and, at the moment of writing, IDENTICAL on disk to that blob; `cowork_handoff.md` is
  TRACKED (blob `e6457cf3…`); `cowork_handoff_entry_ninety_one.md` and this dispatch
  (`cc_instruction_comparison_l0_l1_third_2026_09_02.md`) are ABSENT from the tip.

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications at your boot:
  **none expected**, with ONE named exception that is NOT a STOP: **(i)**
  `cowork_rulings_2026_08_31_decision_surface_sitting.md` MAY be modified on disk at any time during
  this batch by the writing side, additions only, below §3an — **not landed by this batch, not read
  beyond §3an, its modification reported in the close and graded as the cadence working.** A
  modification at any OTHER tracked path is a STOP-and-report. Untracked and to be landed by Task 0:
  `cowork_handoff_entry_ninety_one.md` and this dispatch — **two untracked paths.** A further
  untracked `cowork_handoff_entry_*.md` or `cowork_rulings_*.md` appearing at the root during the run
  is the writing side's and is NOT landed and NOT a STOP. The rest of the untracked population is
  outside A1's STOP. **Check ordered (Task 0, first act — AFTER the ordered first reads):** per named
  tracked path `git diff --stat 4cae08c06a8b637f63cd09ee86421284ba0f79d4 -- <path>; echo "exit:$?"`
  with any changed passages written to a scratch path outside the repository and read with the file
  tools; per named untracked path `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`;
  and the whole tracked population enumerated with the sanctioned enumeration tool.

- **ASSUMPTION A2 — the reds at the CHECK-mode run before your first edit.** The ten failing tools
  named above, exactly, and zero STOPs. The reds this batch's own inputs may cause:
  `gen_evidence_pin_membership.py --check` STALE if its derivation counts a modified sitting record
  on disk (predict PASS at boot; if the writing side modifies the record mid-run, a later STALE on
  that one check is the named exception's own consequence — report it, do not repair it, and it is
  not a STOP); `gen_l0_l1_outgoing_population.py --check` STALE if `STATUS.md`'s recorded-tier hits
  move under Task 2's forward-bound move (the second writing's end state recorded exactly this: a
  recorded-tier `tie` hit inside the word *TIERS*) — **if it goes STALE at the end state, regenerate
  it, measure the difference as the second writing did, commit it with the end state, and report
  that no member of the population moved.** A red outside that set is classed by its measured cause;
  one outside this batch's own acts' subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration.** `ruling_records_read` moves by ZERO at Task 0.
  Measure before accepting; movement beyond is a STOP-and-report; a mid-run movement caused by the
  writing side's record edits is reported under A1's exception.

- **ASSUMPTION A4 — the guard registry.** No tool added; population unchanged at **76**; failing set
  the ten named, less any a Task-2 ordered act clears, plus none; zero STOPs throughout.

- **ASSUMPTION A5 — byte-unchanged by this batch, verified at the objects after the last commit:**
  the derivation (blob `9b102c57…`), the brief, the pack directory and `derivation_boot_pack.json`,
  every outgoing text in the population, `tools/audit/gen_l0_l1_outgoing_population.py`, the
  population artifact EXCEPT the A2 regeneration case, and every governing document EXCEPT
  `STATUS.md` and `STATUS_ARCHIVE.md` (Task 2's own orders). **The rows of documents 1 to 7 in the
  reading file are byte-unchanged by this batch** — you append; you do not revise a placed row. Any
  other difference is a STOP-and-report.

## The declared start state — at the tree you will meet

1. The ten failing tools named in the FACT above — standing reds, none of them this batch's.
2. Zero STOPs; `gen_evidence_pin_membership.py --check` and `gen_l0_l1_outgoing_population.py --check`
   PASS predicted, **measured** (A2).

A failing verdict outside 1–2 is a STOP-and-report, save the two A2 exceptions named.

## Findings in this batch

**No finding number is allocated by this dispatch; the series stands where the topmost entry of
`cowork_handoff.md` and the ninety-first entry file say it stands — read both.** Ruling 9's routing
binds. **What you find IN the derivation or IN the outgoing text is not a finding of this batch — it
is the tabulation's content.** A defect in the comparison APPARATUS (a population member absent at its
path, an outgoing text you cannot parse into statements, a row of documents 1 to 7 whose vocabulary
you cannot reuse) is a STOP-and-report, not a thing to fix.

## Task 0 — land the entry and this dispatch, and PUSH

1. **A1's check** (after the ordered first reads).
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob at the tip BEFORE
   accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_handoff_entry_ninety_one.md` (new),
   `cc_instruction_comparison_l0_l1_third_2026_09_02.md` (new), and
   `tools/audit/evidence_pin_membership.json` (regenerated, only if it moved) — **two or three
   paths.** Subject, exactly:
   `record: the ninety-first entry and the third comparison dispatch landed; the comparison resumes at position 8`.
4. **PUSH**; verify at the ref file that `origin/master` equals the new tip.

**E0:** the named paths in one commit; `origin/master` at the commit; A3 measured; A1's whole-tree
check reported with its enumeration.

## Task 1 — the tabulation, positions 8 to 29, ONE commit PER DOCUMENT, then the whole-population sections

**The unit, unchanged from the second writing (Ruling 8: the grain of a unit is the decision).** An
OUTGOING STATEMENT is one sentence or one bullet of the outgoing text that states what the analysis
does, must do, may assume, or must not do, about L0's subject (what the notated record supplies, what
may be assumed of it, what happens when it does not supply it) or L1's subject (the brief's seven
faces (a)–(g): note eligibility, ties, the change-point set and slices, metric strength, notated
boundary evidence, the local cadence cues, the form of what is published). A sentence that is none of
those is NOT an outgoing statement. **A paragraph is never a unit.** Where one sentence carries two
claims, it is two statements, marked (i) and (ii).

**★ THE SCOPE OF A DOCUMENT ADMITTED BY THE SEARCH, stated because positions 12 to 29 differ in kind
from positions 1 to 11.** Positions 8 to 11 are Ruling 32's item 3 — documents the ruling named because
they describe an L1 output — and are read like documents 1 to 7: every passage that speaks to L0's or
L1's subject is tabulated, and the rest of the document is COUNTED under *not a statement* by
paragraph, not listed sentence by sentence. Positions 12 to 29 are documents the term search admitted;
several are long and speak to L0's or L1's subject in a few places only. For these: **read the whole
document once** (never only the hit lines — a hit locates, it does not bound); tabulate every passage
that speaks to L0's or L1's subject; and record the rest as *not a statement* by SECTION (heading and
paragraph count), so the arithmetic closes at the document without a sentence-by-sentence listing of
material outside the subject. **The manifest header of such a document states both counts** — outgoing
statements, and sections counted as not-statements — so a reader can see how much of the document the
comparison reached.

**(a) Per outgoing document, in the population's order, establish the population at the document
itself:** count its outgoing statements; the count goes in that document's manifest header in the
reading file and nowhere else.

**(b) Per outgoing statement:** quote it verbatim with its location (document and heading; a line
number only as a locator beside the heading, never as the anchor); name the derived statement or
statements (by S-number) that speak to it, or state that none does; on the **CURRENT-TEXT AXIS**
say, for each named derived statement, exactly one of **AGREES / DIFFERS / THE DERIVATION IS
SILENT** — evidence about the present text, never a verdict on the derivation; then the
**PROPOSED DISPOSITION**, exactly one of:

- **ADOPTED — carried**: a derived statement carries its content in substance (name it).
- **ADOPTED — proposed**: no derived statement carries it, it is not implementation-describing,
  not another charter's, not historical, and the worth test does not discard it. **This is a
  PROPOSAL for the user: the outgoing content would be added to the derived specification.** Write
  the proposal in one sentence beside the row. Never write it into the derivation.
- **RELOCATED**: its content belongs to another charter. Name the charter (L2, L3, the
  voice-leading axis, the measurement design) by `FRAMEWORK.md` §5's own words, and append the
  statement to the TRANSFER LIST section of the reading file.
- **QUARANTINED**: it states how the implementation currently works, or a measured property of it.
  Write the audit question in one sentence beside the row. (The derivation's §6.2 lists such
  passages it met inside the pack; where an outgoing statement is one of those, say so.)
- **DISCARDED**: under principle #10's worth test, with the finding, the date (2026-09-02) and the
  reason in the row — all three, or the disposition does not reach the row (D-677's shape).
- **HISTORICAL**: an event, a build state, a superseded plan, a status.
- **UNPLACED**: you cannot defend one disposition in one sentence at the two texts. Say what you
  read. **This is the honest default and is not a sixth disposition** — it is a row the user
  places.

**Where a derived statement DIFFERS from an outgoing statement**, the row carries, beside the
disposition, the DIFFERENCE stated in one sentence in both texts' own words, and NOTHING that
chooses between them: that choice is the user's, put later by the writing side as a surface over
your rows.

**(c) The derived side, once, after every document is done:** one row per derived statement S-1 to
S-54, in the derivation's order — the statement's own words (its first field), its source class,
and the outgoing statements that speak to it (by document and row) with the AGREES / DIFFERS /
SILENT verdict each carried, or **THE OUTGOING TEXT IS SILENT** where none does. **This row set is
the same matrix as (b) read from the other side; every AGREES or DIFFERS in (b) appears here and
vice versa — check it by arithmetic and state the check's result.**

**(d) The open questions OQ-1 to OQ-17:** listed, ungraded, each with one sentence on what the
outgoing text says about it, with location, or that it says nothing.

**(e) The derivation's §7, its five points:** each with one sentence on what the outgoing text
says about it (with location) or that it says nothing — the two structural points (notated versus
unfolded order as its own face; marks that fall where no change point is) named as such, as
§3al.4 records them. **No verdict on whether §7 is right.**

**(f) The independence record (§6 of the derivation):** ALREADY WRITTEN at §14 of the reading file by
the second writing; **do not rewrite it.** Where a row of documents 8 to 29 is one of the passages §6.2
records the deriving session having SEEN, the row says so, as the rule below states. The rule as the
second writing gave it: relayed in its own section — what arrived
and how much was read, the stop-on-meeting record by file and location, the two elements not read,
the fetched sources, the primaries not re-read — **with no verdict word attached.** Where an
outgoing statement of this comparison is one of the passages §6.2 records the deriving session
having SEEN (in `02_the_guiding_principles…` lines 323–335 and 426–428 as the pack renders them,
or in `05_the_ratified_design_intent` entries D-023, D-024, D-033, D-057, D-207, D-221, D-224,
D-229), the row says so — that is a fact the user reads the AGREES verdict against.

**(g) The reading file** — `ratification_surfaces/cowork_comparison_l0_l1_reading.md`, WHICH EXISTS:
you append the new documents' sections under §6 in the population's order (§6.8 onward), extend the
per-document gathers, and touch no placed row. Its shape, as the second writing fixed it and you keep it: in the
standing ratification-surface shape and following the pilot's file section for section: the
subject re-explained from scratch for a reader who knows music and not this project; the words
used, explained first; the population, named to the Task-1 artifact; the unit, stated; the
vocabulary and that it is closed; then the tabulation per outgoing document in the population's
order, each document opening with its manifest header; then the derived-side rows; then the open
questions; then §7; then the TRANSFER LIST (every RELOCATED row gathered, by target charter); then
the AUDIT QUESTIONS (every QUARANTINED row gathered); then the PROPOSALS (every ADOPTED — proposed
row and every DIFFERS row gathered, each in one sentence, with nothing chosen); then the
distribution of dispositions and of current-text verdicts BY COUNT AT THE FILE'S OWN ROWS, per
document and in total, with the arithmetic check that every outgoing statement carries exactly
one disposition and every derived statement exactly one row; then *what the user is asked to rule*
— NOTHING in this file; the surface follows from the writing side — and *what this file does not
do* (it decides nothing; it establishes nothing; it applies no disposition; every row is
re-placeable at the quoted texts). **Neutral framing throughout.**


**(h) Families of difference, named as such.** Where a row of documents 8 to 29 carries a difference
already carried by a row of documents 1 to 7 — the seven families the reading file's §12 gathers, and
any new one you meet — the row NAMES the family in the words §12 already uses and cites the earlier
row, so that the user's ruling on a family can later be applied across every row that carries it
without a second reading. **A family met for the first time is named as new, as §12 does for the
metric strength class at document 6.** The cadence cues, the notated boundary evidence and the metric
strength class are expected at positions 8 to 11; a difference on any of them is stated with the
same care as the grace-note difference was.

**(i) Batching, ruled (D-672).** One outgoing document at a time, in the population's order,
**committed after each document** with the subject
`comparison L0/L1: <document name> tabulated — <n> outgoing statements placed, proposals only`
where `<n>` is that document's count from its manifest header. Push after each commit. **If your
context is degrading — the tells: citing a summary instead of the source, a count never derived, a
defined term glossed — STOP AT THE NEXT DOCUMENT BOUNDARY**, commit what is done, update §0's table
and the resume position, and record in your report which documents are done, which are not, and
that the remainder is UNTOUCHED. **A partly worked document is never committed.** The next dispatch
resumes at the recorded boundary. **§0's table is updated at every document commit**, so the file
always states its own progress.

**(j) ONLY once every one of the 29 documents is DONE — the whole-population sections, ONE
commit.** Replace each NOT YET WRITTEN marker with the section it stands for: §7, one row per
derived statement S-1 to S-54 in the derivation's order — the statement's own words (its first
field), its source class, and every outgoing statement that speaks to it (by document and row) with
the AGREES / DIFFERS / SILENT verdict each carried, or THE OUTGOING TEXT IS SILENT where none does —
with the arithmetic check that every AGREES and DIFFERS in the per-document rows appears here and
vice versa, its result stated; §8, the seventeen open questions, each with one sentence on what the
outgoing text says about it (with location) or that it says nothing; §9, the derivation's §7, its five
points, each with one sentence on what the outgoing text says (with location) or that it says nothing,
the two structural points named as §3al.4 records them, and no verdict on whether §7 is right; §10
to §12, the gathers completed over the whole population, families named; §13, the distribution over
the whole population, per document and in total, with the arithmetic that every outgoing statement
carries exactly one disposition and every derived statement exactly one row. Update the banner's
incompleteness notice and §0 to state that the tabulation is COMPLETE. Subject:
`comparison L0/L1: the population complete — the derived-side rows, the open questions, §7 and the gathers written over all 29 documents, proposals only`.
Push; verify at the ref file.

**E1:** for every document done: its manifest count(s); every outgoing statement with exactly one
disposition or UNPLACED with what was read; every DIFFERS with its one-sentence difference and nothing
chosen, its family named; the per-document gathers present; documents 1 to 7 byte-unchanged. **And if
all 29 are done:** §7, §8, §9 written, §10–§13 completed, the two arithmetic checks stated, the
banner and §0 updated. No recommendation anywhere.

## Task 2 — the close

1. One `STATUS.md` pointer entry per task that did work (no distribution restated — the pointer names
   the reading file); the previous batch's entries move through `gen_status_batch_bound.py --apply`
   (D-648, licensed in terms; **re-aim ALL FOUR of its authored fields**, the second writing having
   found the first writing moved only two); `gen_session_start_read_size.py` regenerated.
2. The FULL close appended to `cowork_away_returns.md` as a **THE L0/L1 COMPARISON TABULATED (THIRD
   WRITING), NOTHING DECIDED** section — both guard states, every SHA, E0–E2 graded, A1–A5 graded,
   declared departures (the sitting record's mid-run modification, if any, reported here under A1's
   exception), quarantined questions (the reading file's section pointed at, not restated), the plan
   lines (**the writing side's disposition surfaces, one decision per turn, over the whole
   population once it is complete — or over documents 1 to N if this batch stopped; the L0/L1 split
   at ratification; the pack finding of §3al.4 to L2's pack; then L2, sequenced by Ruling 10**), and
   **the plan's tell in one sentence: did this batch produce anything other than the landed records,
   the reading file and the report? If yes, name it.** The report is
   `cc_report_comparison_l0_l1_third_2026_09_02.md` — which likewise decides nothing; where the report
   would name hashes it cannot contain, point at the git log.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **The ordered structure
   yields: Task 0, one commit per outgoing document, the whole-population commit (if reached), the
   close, the end state.**

**E2 — DERIVED from the declared start state plus this batch's footprint (P-3):** at the tree
carrying the close, a fresh full guard run — **population 76**; **zero STOPs**; **the failing set
exactly the ten named, LESS any this batch's ordered acts clear, PLUS NONE**, the two A2 exceptions
handled as A2 says — committed only after the run that produced it.

## What this batch does NOT do

- **No decision on any disposition, any difference, the derivation, the method, or the L0/L1 split.
  No verdict on the deriving session's independence. No session booted. No measurement built or run.
  No row of documents 1 to 7 revised.**
- **No edit to the derivation, the brief, the pack, its generator, its artifact (save A2's
  regeneration), the population tool, any outgoing text, any governing document other than the two
  Task 2 orders, any register source, or the sitting record.** The reading file's new rows and
  sections and the report are the whole authored write set.
- **No finding number. No open-items row created, flipped or discarded EXCEPT an apparatus-finding
  row under rule (c). No `src/` change, no golden, no test, nothing under `tools/corpus/` or
  `tools/robust_stop/`. No guard enrolment.**
- **No disposition of any residue file.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #19 — the reading file establishes nothing; every row re-placeable at
   quoted texts; #6 — the rules of the second writing are repeated verbatim rather than varied, so
   one comparison has one unit and one vocabulary; the population list of record is the artifact;
   #12 — documents 1 to 7 are appended to, never revised; the sitting record's mid-run modification
   is named and admitted rather than forbidden and then met; #13 — the concurrent cadence is stated
   at the head so a modified record is not a surprise; #17(f)/D-431 — counts live at the reading
   file's own rows; #24 — the two axes are kept apart. Conforms.
2. *Conventions:* American English; no self-invented labels — *family* is the reading file's own
   word for a difference met in several rows and is defined at (h); music-theory words in their
   musical sense; *exclude* for the prohibiting sense; towards, never against.
3. *Figures and premises:* the tip and its parent read at the ref files after the batch; the guard
   summary read at the object the end-state commit itself wrote and its path list checked by
   `git show --stat` at the explicit hash; the population's positions 8 to 29 read at the committed
   artifact by explicit hash; the sitting record's on-disk identity with its blob established by
   sha256; the reading file's §0, §12 and §13 read at the file.
4. *File-tools rule:* every repository read through the file tools on bridge-staged snapshots; git
   object queries by explicit hash only; no `git status`, no working-tree diff.
5. *Uncertainty:* whether this batch reaches all 29 documents is unknown and the stop rule covers it;
   the two A2 exceptions are named with their measured precedent.
6. *Re-read from disk before release:* performed over the committed file after it is written to the
   root, hash-compared with the written copy, recorded in the ninety-second entry.

---

*Provenance: Cowork writing side, 2026-09-02, the session that booted on
`cowork_handoff_entry_eighty_nine.md`, at tip `4cae08c0…` with `origin/master` equal, under Rulings 32
and 33 of `cowork_rulings_2026_08_31_decision_surface_sitting.md`, applying Rulings 1–4 and §5 of
`cowork_rulings_2026_08_24_comparison_design_sitting.md` and the disposition discipline of the phase
definition. TOWARDS the ultimate objective and TOWARDS the guiding principles.*
