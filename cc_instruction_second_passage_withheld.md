# CC dispatch — land the second-leak sitting; withhold the SECOND member (2) passage (the founding-instances sentence); re-render the pack; boot NO session

> **Dispatch (Cowork, 2026-08-23, the forty-fifth Cowork session).** Executes Ruling 1 of
> `cowork_rulings_2026_08_23_member_two_second_leak_sitting.md`. **Written at a verified stop: the
> withheld-family correction batch returned complete (`6e29b06a49` → `72534e5da9` → `1db5cf1e90` →
> `972ba18005`), was verified at the objects by the writing side, and nothing is running.** The tip
> was read by the writing side with `git show -s` and `git for-each-ref` at the explicit hash
> `972ba18005` and at nothing else.
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** Task 0 lands the sitting record, the modified
> handoff and this dispatch, and PUSHES. Task 1 adds ONE entry to the generator's authored
> `withheld_passages` list, makes the read-me's withholding sentence true of the count it describes
> (the one licensed rendering edit), re-renders the pack, and updates the reading file's LIST FIVE —
> one commit. Task 2 closes. **THIS BATCH BOOTS NO SESSION FROM THE PACK, DERIVES NOTHING, AND
> OPENS NO PILOT ACT.** When it lands, the pack at
> `tools/audit/derivation_boot_pack/harmony-boundary/` is the one the blind session boots from;
> that session is a FRESH COWORK SESSION (Ruling 1 of
> `cowork_rulings_2026_08_22_deriving_side_sitting.md`), not you.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The pilot* — the phase that proves the
> derivation method before it is trusted. *The boot pack* — the rendered, self-contained directory
> an implementation-blind deriving session reads at boot. *The withheld family* — the register
> identities, one document and the passages withheld from the pack for one subject; an AUTHORED
> input the user rules. *The authored table* — the `WITHHELD` and verdict tables inside
> `tools/audit/gen_derivation_boot_pack.py`; inputs to the generator, corrected by hand under a
> ruling record. *The founding-instances sentence* — the sentence Ruling 1 withholds, quoted in
> the ruling ledger below. *The reading file* —
> `ratification_surfaces/cowork_withheld_family_harmony_boundary_reading.md`, written for the
> user and not generated.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional. **(3)** `BUILD_AND_TEST.md` — CONDITIONAL, and this batch MEETS
> the condition (it runs the guard set and a generator): read it in full. **(4) DO NOT READ THE
> WHOLE OPEN-ITEMS INDEX.** Rule (a) reads `tools/audit/nongating_apparatus_rows.json` →
> `★_the_live_gating_answer` → `gating_ids`.
>
> **★ AND THE STANDING CLAUSE BINDS THIS ONE: read `cowork_handoff.md`, ITS CURRENT (FORTY-SIXTH)
> BLOCK.** The findings series stands at F88 and this batch allocates NO finding number.
>
> **★ AND READ THE STANDING CLAUSES AT THE FILE, NOT AT A COUNT.** `cowork_audit_protocol.md`'s
> dispatch-protocol section IN FULL before Task 1 writes anything.
>
> **Then read, in full and in this order:**
> `cowork_rulings_2026_08_23_member_two_second_leak_sitting.md` (this batch's authority — both
> sections); `cowork_rulings_2026_08_22_member_two_leak_sitting.md` and Ruling 5 of
> `cowork_rulings_2026_08_22_withheld_family_sitting.md` (the two passage rulings this one stands
> beside); `cc_report_withheld_family_correction.md` (your predecessor's report, whole); the
> reading file at the tip, whole; the module docstring, the authored tables,
> `withhold_passage()` and the read-me template of `tools/audit/gen_derivation_boot_pack.py`
> (read the whole file — you edit it).
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/derivation_boot_pack.json` (the manifest; rewritten by the generator);
> `tools/audit/decisions/backbone_decisions.json` and `tools/audit/rulings_sort_classification.json`
> (**read and never written**); `tools/audit/evidence_pin_membership.json` (Task 0's subject);
> `tools/audit/guard_classification.json`, `tools/audit/guard_state.json`,
> `tools/audit/epoch_write_path.json`, `tools/audit/recognizer_establishment_sort.json` (A4's
> subjects).
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS built,
> designed, scoped or run, no design, no repair, **no derivation of any specification statement, no
> held-out derivation, NO SESSION BOOTED FROM THE PACK**, no document archived, moved or deleted AS
> A FILE, **no open-items row created, flipped or discarded**, **no edit to `ARCHITECTURE.md`,
> `CLAUDE.md`, `DEFECT_TYPES.md`, `cowork_design_doc_template.md`, `cowork_audit_protocol.md`, the
> phase-definition surface, `DECISIONS.md` or any register source**, **no change to the generator
> OTHER THAN the one authored-table addition and the one licensed read-me rendering edit** (no
> change to the criterion, the leak check, `withhold_passage()`, the marker or any STOP), **no
> re-grading of any verdict** (the sixteen IN, fifty-nine OUT and both existing withholdings stand
> byte for byte at the manifest except the fields the re-render itself derives).
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end —
> in CHECK mode (`--check`; your predecessor's pre-edit run passed `--help`, which the runner does
> not parse, and wrote instead of comparing — do not repeat it). Verify every commit at the object
> by explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and `git diff`
> over the working tree time out on this mount and are denied by the armed guard — use the
> per-path explicit-hash forms named in A1. If staging is refused by a stale zero-length
> `index.lock` with no git process running, establish the cause as your predecessors did (D-669)
> and remove it; report the act. Resolve every commit hash at the object before writing it
> anywhere. The reserved-word and vocabulary rules bind every line this batch writes** (music-theory
> words in their musical sense; *measurement tool*, never the reserved word; *the decisions
> register* in full; TOWARDS, never *against*).

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1 of the second-leak sitting (2026-08-23):** *"the generator's authored
  `withheld_passages` list gains a SECOND entry: `CLAUDE.md`, member 2, scope anchor
  `**EVERY DESIGN DECISION CARRIES ITS DEFENSE AT ITS HOME`, opening 'Founding instances of the
  gap:' and closing 'each recorded with no derivation.' — located by its own text and never by
  line number, in the D-677 shape with finding, date (2026-08-23) and reason. The rendered member
  (2) omits the sentence and marks the omission in place; the bullet's principle … stays whole
  above the cut. `CLAUDE.md` itself is untouched. The reading file's LIST FIVE lists both
  passages."* And, of the read-me: *"the read-me's withholding sentence made true of the count it
  describes (its rendering currently hardcodes 'one passage' — the one licensed rendering edit)."*
- **The sentence withheld, quoted at `972ba18005:CLAUDE.md` (wrapped):** *"Founding instances of
  the gap: the decode segment cap's value (4), the legacy 16-beats-back/8-forward window, the
  boundary-tick-belongs-to-the-segment-it-starts convention — each recorded with no derivation."*
- **The standing clause on authoring:** the family is authored and RULED; this batch applies a
  ruled withholding and clears nothing further.

## Premise ledger

- **FACT** — the branch tip is `972ba18005` (parent `1db5cf1e90`), subject opening *"end state:
  the guard set run at the tree the close left — every check passing except OI-372's tool, ZERO
  STOPs, the guard population UNMOVED"*; both refs at it. Read by the writing side at the explicit
  hash on 2026-08-23. **Nothing is unpushed.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 75, passing 74, failing 1
  (`tools/audit/gen_filing_convention_application.py --check`, [[OI-372]]'s tool), not_run 4,
  historical 16. **Read it again yourself at `git show 972ba18005:tools/audit/guard_state.json`.**
- **FACT** — at the tip, `derivation_boot_pack.json` → `subjects.harmony-boundary.counted` reads
  `design_intent_class 244, candidates 75, verdicts IN 16 / OUT 59 / UNPLACED 0,
  withheld_identities_authored 16, withheld_identities_derived 17, withheld_identities_total 33,
  withheld_documents 1, withheld_passages 1, leaks 3, design_intent_entries_rendered 208,
  defect_type_rows_rendered 25, files_in_the_pack 7`. **Re-read it; these are the BEFORE figures
  A5 is measured against.**
- **FACT, measured by the writing side over whitespace-normalized text (the generator's own
  form):** at `git show 972ba18005:CLAUDE.md`, `Founding instances of the gap:` occurs exactly
  ONCE, `each recorded with no derivation.` occurs exactly ONCE, and the scope bullet's opening
  `- **EVERY DESIGN DECISION CARRIES ITS DEFENSE AT ITS HOME` occurs exactly ONCE (the bullet
  opens at line 1574; the sentence wraps across lines 1585–1586 — the line numbers are for your
  corroboration only and are never written into the tool). **The generator halts if an anchor does
  not resolve exactly once inside its scope; a halt is a STOP-and-report, not a thing to fix
  around.**
- **FACT** — at `git show 972ba18005:tools/audit/gen_derivation_boot_pack.py`, line 1144's read-me
  template carries the hardcoded sentence `* one passage inside \`{MEMBERS[1]['filename']}\`,
  marked in place where it was removed.`; the authored `withheld_passages` list carries ONE entry
  (the widened founding-instance passage, `opens: "**Founding instance:** on 2026-07-28 Cowork
  reasoned"`); `withhold_passage()` cuts one span per entry within its scope bullet and replaces
  it with the marker.
- **FACT, measured by the writing side:** `cowork_rulings_2026_08_23_member_two_second_leak_sitting.md`
  is on disk and absent from the tip; it does not carry the word `pinned` on any line (Grep by the
  writing side over the container copy).

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications: **(i)**
  `cowork_handoff.md` — ONE block (the forty-sixth) inserted above the committed content, the
  forty-fifth heading marked superseded-as-entry-point, otherwise additions only. **A modification
  at any OTHER tracked path is a STOP-and-report.** Untracked and to be landed by Task 0:
  `cowork_rulings_2026_08_23_member_two_second_leak_sitting.md` and this dispatch
  (`cc_instruction_second_passage_withheld.md`) — **two untracked paths; a named path the tree
  does not carry is a STOP-and-report.** The rest of the untracked population is NOT landed and is
  outside A1's STOP. **Check ordered (Task 0, first act):** per named tracked path
  `git diff --stat 972ba18005 -- <path>; echo "exit:$?"`; per named untracked path
  `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`; and the whole tracked
  population enumerated with the sanctioned enumeration tool as your predecessors did.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  The reds this batch causes and must clear: **the membership check, cleared by Task 0 step 2**;
  **the boot-pack generator's own `--check`, red between the table edit and the re-render, inside
  the SAME commit**; **`gen_session_start_read_size.py --check` at the close** (Task 2 moves
  `STATUS.md` entries, cleared by regenerating); **`gen_guard_classification.py --check` red
  between any write-mode guard run that records the membership check failing and the run after
  Task 0 clears it** (your predecessor's §3b shape — avoid causing it by running in check mode,
  and if caused, class it by its measured cause). Any other red is classed by its measured cause,
  and one outside this batch's own acts' subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration, from EVERY route.** **Route A** — the
  ruling-record count rises by exactly ONE (50 → 51) and ONE name is added
  (`cowork_rulings_2026_08_23_member_two_second_leak_sitting.md`). **Route B** — the record
  carries the word `pinned` on no line, so route B adds NOTHING. **Route C** — no tool is added.
  Measure the diff against the committed blob at `972ba18005` BEFORE accepting it; a further
  additive derived cross-reference caused by the landed record's own naming of other documents is
  reported rather than absorbed; movement beyond these is a STOP-and-report.

- **ASSUMPTION A4 — the guard registry.** This batch ADDS NO TOOL. `guard_state.json`: the
  population UNCHANGED at run 75; the failing set unchanged at [[OI-372]]'s tool.
  `guard_classification.json`, `epoch_write_path.json`, `recognizer_establishment_sort.json`:
  **UNCHANGED except** any run-stamp field. A member entering or leaving any of them is a
  STOP-and-report.

- **ASSUMPTION A5 — the manifest after Task 1, from EVERY field of `counted`.**
  `design_intent_class` 244; `candidates` 75; `verdicts` IN 16 / OUT 59 / UNPLACED 0;
  `withheld_identities_authored` 16; `withheld_identities_derived` 17;
  `withheld_identities_total` 33; `withheld_documents` 1; **`withheld_passages` 2**; `leaks` 3;
  `design_intent_entries_rendered` 208; `files_in_the_pack` 7 — **everything unmoved except the
  passage count.** At the pack files: **the rendered member (2) carries the marker exactly TWICE**;
  it carries NONE of: `Founding instances of the gap:`, `boundary-tick-belongs`, `decode segment
  cap`, `16-beats-back`; the bullet's own statement (`EVERY DESIGN DECISION CARRIES ITS DEFENSE AT
  ITS HOME` through `never filled in retroactively from memory` and its parenthesis) stays whole
  above the second marker; the first cut and its marker are unchanged. **The read-me's withholding
  sentence states the passage count derived from the applied passages** (singular wording at one,
  plural at more — so it stays true for every future subject) **and the read-me changes in no
  other way. Members (1), (3), (4), (5), (6) are byte-unchanged against `972ba18005`.** Only four
  files move against that tip in this task's subject area: the generator, the manifest, member (2)
  and the read-me — plus the reading file. Any movement outside this is a STOP-and-report.

## The declared start state — at the tree you will meet

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — caused
   by this dispatch's own input, the ONE untracked root-level `cowork_rulings_*.md` record.

**`gen_guard_state.py --check` will open "STALE vs the run" for the same cause (F83); it is not a
third failing check.** A THIRD failing verdict is a STOP-and-report.

## Findings in this batch

**No finding number is allocated; the series stands at F88.** Ruling 9's routing (2026-08-21)
binds as before: a finding bearing on the analysis is SURFACED under "Quarantined audit
questions"; an apparatus finding gets its row under rule (c); a finding bearing on neither is
DISCARDED with finding, date and reason. **A further paraphrase-shaped leak you happen to MEET in
the spans the generator renders is SURFACED to the user in your report with its verbatim and its
location — not cut, not ruled, not chased by a sweep** (the sitting declines the sweep in terms).

## Task 0 — land the sitting record, clear the membership check in the same act, and PUSH

1. **A1's check first, entirely at content-addressed objects.**
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE its difference against the
   committed blob BEFORE accepting it**, against A3: `python tools/audit/gen_evidence_pin_membership.py;
   echo "exit:$?"` then `git diff --stat 972ba18005 -- tools/audit/evidence_pin_membership.json;
   echo "exit:$?"`, the changed passages written to an absolute scratch path outside the repository
   and read with the file tools.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_23_member_two_second_leak_sitting.md`, and
   `cc_instruction_second_passage_withheld.md` (new) — **four paths.** Subject, exactly:
   `record: the 2026-08-23 second member-two leak ruling landed; evidence-pin membership regenerated`.
   Staged by explicit path, never by `git add -A` or `git add .`.
4. **PUSH**; verify at the object that `origin/master` names your Task 0 commit.
5. **Re-run `gen_evidence_pin_membership.py --check`: it must PASS.**

**Registered expectation E0:** exactly the four named paths; `ruling_records_read` 51 at the
committed artifact; the membership check passing; `origin/master` at the Task 0 commit.

## Task 1 — the one addition, the licensed read-me edit, the re-render, the reading file — ONE commit

**(a) The authored `withheld_passages` list gains ONE entry and loses nothing:**
`file: "CLAUDE.md"`, `member: 2`,
`scope_anchor: "**EVERY DESIGN DECISION CARRIES ITS DEFENSE AT ITS HOME"`,
`opens: "Founding instances of the gap:"`, `closes: "each recorded with no derivation."`,
`finding` (*"Ruling 1 of `cowork_rulings_2026_08_23_member_two_second_leak_sitting.md`: the
sentence names, from its own words, a boundary-membership convention — a tick at a boundary
belongs to the segment it starts — adjacent to the oracle's ranking of actual sounding notes and
to withheld D-317's subject, and names the decode segment cap's value beside it; it stands in
member (2), rendered whole, outside the ruled leak-check scope and outside both passages
previously ruled."*), `date: "2026-08-23"`, `reason` (*"The disclosure is content, not existence —
a deriving session learns the convention from its name alone; withholding it as an authored
passage is the mechanism twice ruled for this shape, and the bullet's principle stays whole above
the cut."*). **The existing entry is untouched.**

**(b) The one licensed read-me rendering edit:** the template sentence that hardcodes
*"one passage inside …"* derives its number from the count of passages applied for the subject —
singular wording at one, plural at more — and states nothing else new. **No other rendering
string, and nothing else in the generator, changes.**

**(c) Run the generator; run `--check`.** Its STOPs are the check: an anchor not resolving exactly
once inside its ruled scope halts it, and a halt is a STOP-and-report.

**(d) A5, measured at the manifest and at the pack files, every item.**

**(e) The reading file's LIST FIVE** becomes *the withheld PASSAGES* (two entries): the existing
entry stands with its widened anchors; the second entry states where (the defense-at-its-home
bullet), what (the founding-instances sentence, its anchors), why (the finding of (a), in the
reading file's own words), and how it was applied (cut and marked in place, `CLAUDE.md`
untouched); the banner's ruling line gains this sitting's record name. **Nothing else in the file
changes.**

**(f) Run the FULL guard set. A2 and A4 must hold.** Commit the generator, the manifest, member
(2), the read-me, the reading file, and the guard artifacts the run rewrote, with the subject
`the second member-two passage withheld — the founding-instances sentence cut from the rendered pack; the read-me counts its passages; no session booted`.
Push; verify at the object.

**Registered expectation E1:** the authored list carries exactly the two passages; A5 held field
by field; `--check` green; member (2) with two markers and none of the four strings; the read-me's
sentence true at two and derivable at any count; the reading file's LIST FIVE at two entries;
**`CLAUDE.md`, `ARCHITECTURE.md`, `DEFECT_TYPES.md`, `cowork_design_doc_template.md`,
`cowork_audit_protocol.md`, the phase-definition surface, `DECISIONS.md` and every register source
byte-unchanged**; the seventy-five verdict rows byte-unchanged at the manifest against
`git show 972ba18005:tools/audit/derivation_boot_pack.json`; no session booted.

## Task 2 — the close

1. One `STATUS.md` pointer entry per task that did work; the previous batch's entries move
   verbatim to `STATUS_ARCHIVE.md` through `gen_status_batch_bound.py --apply` at its three
   declared authored inputs (authored-input maintenance, D-648 — licensed here in terms);
   `gen_session_start_read_size.py` regenerated (A2).
2. The FULL close appended to `cowork_away_returns.md` as a **THE SECOND PASSAGE WITHHELD: THE
   PACK AT ITS RULED STATE** section — both guard states, every SHA, E0–E2 graded, A3–A5 graded at
   the artifacts, the declared departures, the quarantined questions (or none new), the plan lines
   (**the blind derivation by a FRESH COWORK SESSION, unblocked — nothing further stands between**;
   the comparison after it; the sizing pilot after the method is ruled; the ledger before the
   framework phase), and **the plan's tell in one sentence: did this batch produce anything other
   than the one authored addition, the one licensed read-me edit, the re-rendered pack and
   manifest, the reading-file LIST FIVE update and the report? If yes, name it.** The report is
   `cc_report_second_passage_withheld.md`.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run.

**Registered expectation E2:** at the tree carrying the close, a fresh full guard run — **every
run check passing except [[OI-372]]'s tool, zero STOPs, the guard population at run 75** — run and
read, committed only after the run that produced them.

## What this batch does NOT do

- **No session booted. No derivation. No comparison. No oracle opened for the purpose of the test.**
- **No change to the generator beyond (a) and (b); no re-grading of any verdict; no paraphrase
  sweep** (declined in terms by the sitting).
- **No edit to any boot-list member at its home; no register entry, sort artifact or register
  source touched.**
- **No finding number allocated.** [[OI-179]] stays OPEN and GATES; [[OI-372]], [[OI-374]] stand.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule (c).**
- **No `src/` change, no golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #19 — a ruled withholding applied, nothing wider; the member-two bound
   (no completeness claim over member (2)) restated, not weakened; #6 — one authored list, the
   pack derived from it; #12 — the sentence is cut from the pack and not from its home, the
   existing passage untouched, nothing re-graded; #17(f)/D-431 — every BEFORE figure cited at the
   artifact and ordered re-read, every AFTER figure predicted field by field; #24 — no comparison
   asserted. Conforms.
2. *Conventions:* American English; no self-invented labels; music-theory words in their musical
   sense; towards, never against.
3. *Figures and premises:* every quantity named to its artifact; the ruling quoted at its record;
   the anchor-uniqueness counts taken by the writing side over whitespace-normalized text and
   ordered re-located by the tool itself.
4. *File-tools rule:* the writing side's own departures are in the forty-sixth handover block.
5. *Uncertainty:* every A5 field is predicted; none is left to measure-and-report this time, and
   any deviation is a STOP rather than a figure to absorb.
6. *Re-read from disk before release:* performed over the delivered file, hash-verified after
   re-staging (F74), recorded in the forty-sixth handover block.

---

*Provenance: Cowork, 2026-08-23, the forty-fifth session, written at branch tip `972ba18005`
under Ruling 1 of `cowork_rulings_2026_08_23_member_two_second_leak_sitting.md`. TOWARDS the
ultimate objective and TOWARDS the guiding principles.*
