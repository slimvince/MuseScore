# CC dispatch — READ WAVE 5: ratify, home the build-it-right rule, write three delegations, re-pack the regime

> **Status: ACTIVE DISPATCH, written 2026-08-04 (Cowork).** Read IN FULL.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_reads_5.md`.
>
> **★ NO FIGURES (D-431) AND NO STATE NOT READ (#17a).** No `src/`, no goldens, no corpus, no
> behaviour change, no fix to inference, no design. Phase 1 under D-231. **The freeze holds** — no tool
> built or repaired; a tool defect found is rowed and left. Task 5 modifies the regime generator's
> **packing**, which is executing a ruling about how reading is scheduled, not repairing a tool.

## 0a. THE RULING LEDGER

- **R1 — RULED by the user, 2026-08-04.** **D-547…D-583 are ratified as drafted**, each keeping the
  status the record states — including the four entered **SUPERSEDED IN FACT** (D-571, D-572, D-575,
  D-579), which stay superseded rather than being entered LIVE and rowed for correction later.
- **R2 — RULED, same act.** The **build-it-right-before-tune-precision rule is homed at `CLAUDE.md`
  principle #8**, widened to the fuller statement D-557 carries — *the refactoring, the architectural
  design and the algorithmic completion*, not only "all methods and algorithms implemented in their
  correct layer". `cowork_l1l3_stabilization_plan.md` **points at it** and does not restate it (#6).
- **R3 — RULED, same act.** D-576's caveat — that the corpus root-agreement measurement **understates**
  the real-world impact of a wrong key, because root and bass are largely key-independent — is
  **recorded beside the figures it qualifies**, in `CLAUDE.md` gate block (A).
- **R4 — RULED, same act.** **Three** of OI-327's drafted delegations are written:
  `cowork_layer1_note_model_design.md`, `cowork_layer2_slicing_design.md`,
  `cowork_phrase_boundary_design.md`. **The other two are NOT written** — the Layer-6 grouping design
  and the Layer-1 tone-collection design each carry a question a delegation would answer by fiat, and
  those go back to the user.
- **R5 — RULED, same act.** The reading regime is **re-packed so a wave spans length terciles**, and
  the predicted bands are **re-registered for the re-packed waves before any of them is read**.

**None of R1–R5 authorizes a fix, a design, or an inference change.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — read at the object by Cowork this session:**

- **F1.** D-557 reads *"Build-it-right comes BEFORE tune-precision, strictly — no inference-problem
  fixing anywhere until the refactoring, the architectural design and the algorithmic completion are
  done"*, LIVE, homed at `cowork_l1l3_stabilization_plan.md` — the `DECISIONS.md` index row.
- **F2.** OI-327's five documents and the situation of each — `open_items/OI-327.md:28-50`. The
  Layer-1 note-model case closes a **measured asymmetry**: the Layer-3, Layer-4 and Layer-5 sections
  each carry an explicit delegation pointer and the Layer-1 section carries none.
- **F3.** The Layer-6 case sits against **D-266**, which prohibits Layer 6 until the bounded-context
  design is coded and regression-tested for Layers 1–5; and **D-433** holds that a shelved section can
  be a home, so shelving does not settle it.
- **F4.** The tone-collection design is a **sign-off surface with unticked checkboxes** whose designed
  role was **absorbed by the note model**.

**ASSUMPTION — each checked before the act it licenses:**

- **A1.** That `CLAUDE.md` principle **#8** currently reads as Cowork believes and is still numbered 8.
  The file has been edited many times since Cowork last read that line. → **Task 2.1**.
- **A2.** That the regime **packs waves by estimated token cost, and that the estimate is built from
  length** — the diagnosis R5 rests on. Cowork has it from a session report, not from the generator.
  → **Task 5.1**; **if it is false, STOP** — R5's whole premise is that the packer and the proxy are
  the same quantity.

## 1. Task 1 — Ratify (R1)

Ratify **D-547…D-583** as drafted, each keeping the record's own status. Record per the register's
existing entry-ratification convention; if that convention wants a form Cowork has not anticipated,
follow the convention and report the difference.

## 2. Task 2 — Home the build-it-right rule (R2)

**2.1** Check A1: read principle #8 at its own text and report it verbatim with its number.

**2.2** Widen it to D-557's fuller statement, in `CLAUDE.md`'s own voice, **preserving the former
wording in the principle's provenance or a dated note (#12)**. The widened rule names all three:
the refactoring, the architectural design, the algorithmic completion.

**2.3** Make `cowork_l1l3_stabilization_plan.md` **point at it** rather than restate it, and re-take
D-557's verbatim and home accordingly, former preserved in provenance.

**2.4** Record the reason at the principle, briefly: the fuller statement was the one being applied in
practice while the governing document carried a narrower one — so a session reading only `CLAUDE.md`
would have applied the wrong width.

## 3. Task 3 — Record D-576's caveat beside the figures (R3)

Write it into `CLAUDE.md` gate block (A), beside the root-agreement figures it qualifies: the
measurement **understates** the real-world impact of a wrong key, because root and bass are largely
key-independent. Cite D-576. This is the same act phase 1j performed for the grading conventions — a
figure and the thing that bounds its meaning belong together.

**Do not restate any figure** (D-431); the caveat qualifies figures already in the block.

## 4. Task 4 — Write three delegations, and put two questions back (R4)

**4.1** Write the three delegations from the drafted wordings in the write list —
`cowork_layer1_note_model_design.md`, `cowork_layer2_slicing_design.md`,
`cowork_phrase_boundary_design.md` — in the form `ARCHITECTURE.md` already uses for its delegated
layers.

**Report each wording verbatim in the report**, so the user sees exactly what was written on their
ruling and can correct it; each is one line and revertible.

**4.2** The other two are **not written**. Record the question each carries, stated and not decided:

- **Layer 6** — does the record mean to delegate a contract home for a layer **D-266 prohibits** until
  the bounded-context design is coded and regression-tested? D-433 makes shelving irrelevant to the
  question, so nothing settles it mechanically.
- **Tone collection** — the document is a sign-off surface with unticked checkboxes whose role was
  absorbed by the note model, so **whether it should be a contract home at all** is one of the things
  a delegation would decide. Writing one would answer it by fiat.

Keep both on OI-327 with the question stated for the user.

## 5. Task 5 — Re-pack the regime (R5)

**5.1 Check A2 at the generator**: does the packer use an estimate built from length? **If it does
not, STOP and report** — R5 rests on the packer and the proxy being the same quantity, and if they
are not, the diagnosis is wrong and the re-pack is unwarranted.

**5.2** Re-pack so that **a wave spans length terciles** rather than falling inside one. The same
documents get read; only the grouping changes.

**5.3 Re-register the predicted bands for the re-packed waves BEFORE any of them is read** (#17b).
The existing bands were registered against the old ordering and may not be reused — reusing them
would grade re-packed waves against predictions made for a different grouping.

**5.4** Record, in the regime artifact, **why** the re-pack happened: the proxy could not be tested
because the quantity being tested and the quantity doing the packing were the same. That is the
finding, and it should outlive the fix.

## 6. Task 6 — Read

Read in the **re-packed** order, in full, for as much capacity as Tasks 1–5 leave. Per document as in
waves 1–4: entries at the record's own status; yield against the **newly registered** band, in this
wave's own artifact; OI-232 / OI-274 / OI-276 / OI-279 / OI-315 class findings **rowed, not
corrected**; findings-recording homes noted for OI-296.

**Do not write yields into the regime artifact** — OI-316.

## 7. Task 7 — Close

Guards at the committed tree with the list derived by `gen_guard_state.py`; report the six
pre-existing failures as moved or unmoved and **fix none**. Re-aim any anchor this wave's own edits
drift, per citation. Verify what is committed through `tools/audit/changed_paths.py`. Run
`tools/audit/process_check.py` over this dispatch.

`STATUS.md` gains one POINTER entry. Update OI-207 with the new read count and measured yield.

## 8. Accepted outcomes

**A1 or A2 coming back different is reportable, and A2 is a STOP.** **Task 4.2 leaving two documents
un-delegated is the intended outcome**, not an incomplete one. **The first re-packed wave's bands
being badly missed is a result** — that is the first real test the proxy has had.

## 9. Self-check (D-434) — run by Cowork before release

- **Ruling ledger.** Five rulings written out verbatim, including which two delegations are withheld
  and why.
- **#17(a).** Four facts read at the objects. Two assumptions — A1 because `CLAUDE.md` has moved
  repeatedly, A2 because the packing diagnosis came from a session report and R5 rests entirely on it.
- **Principles.** #6 — R2 puts one rule in one home and points from the other. #12 — every former
  wording preserved. #17(b) — bands re-registered before reading, never reused. #19/#20 — the re-pack
  exists so the proxy can be falsified at all. Rule (g) — two delegations withheld because writing
  them would decide a question by fiat.
- **Scope.** Task 5 changes the packing, not a tool's correctness; the freeze on repairs is untouched.
