# CC report — the L0/L1 FAMILY PLACEMENTS, seventh writing: the item (d) rows placed under the rulings that reach them, counted at §13b, S-48's anchor named

> **This report decides nothing.** It records what was placed, what was measured, what was declared
> rather than asserted, and the places where a hit the mechanical search returned is not the family's
> subject. **Every placement it refers to is either a ruling's own words transcribed to a row (kind A)
> or the writing side's reading of a ruling that reaches the row's family without naming its case
> (kind B), marked as such at the row and overrulable at the user's word.** No ruling was taken, no
> position was weighed, no proposed disposition was edited or deleted, no verdict was attached to the
> derivation or to the deriving session's independence, and no register and no suspension file was
> written. Where this report would name a hash it cannot contain — its own commit, and the end-state
> commit that follows — it points at the git log. **No count of the reading file other than §13b's own
> is restated here** (**D-431**, #17f).

**Dispatch:** `cc_instruction_comparison_l0_l1_seventh_2026_09_03.md`, pinned at Task 0 to blob
`f2a9667589e7490dcdd0933780e12b2992ba228e` and proven unmoved immediately before staging.
**Full close:** `cowork_away_returns.md`, the section **THE L0/L1 FAMILY PLACEMENTS (SEVENTH WRITING)
…**, which carries the commit table, both guard states, E0–E2, A1–A5, the departures and the plan
lines. This report does not restate them; it carries the per-row table and the object-level checks the
dispatch orders here rather than in the close.

---

## 1. What was done

**Fifty-one rows were placed and one statement was corrected.**

- **Task 0** landed `cowork_handoff_entry_ninety_seven.md` and this dispatch — **two paths**, because
  A3 was measured first and the membership artifact had not moved.
- **Task 1** added, at the foot of each of **51** family-hit rows of documents 8 to 29,
  **one placement line** under the ruling that reaches it; added the one §10 relocation item Row
  10.31's placement carries; added the §13a foot note; and wrote the new **§13b**, whose every figure
  was counted at the file after the lines were added.
- **Task 2** applied to `cowork_derived_specification_l0_l1_2026_09_03.md` the one consequential
  correction the dispatch authors: **S-48's anchor named as Ruling 42 orders**, the one statement
  Ruling 74's item (1) listed neither, with the former wording preserved at its site.
- **Task 3** closed: the four `STATUS.md` pointer entries, the forward-bound move, the regenerated
  read-size artifact, the close section and this report.

**Nothing else was touched.** The derivation is byte-unchanged, banner and body; the sitting record is
byte-unchanged; no outgoing text was opened; no register, no suspension file, no open-items row.

## 2. The per-row table — every row placed, its kind, its disposition word and its ruling

*Read in the reading file's own row order. **Kind (A)** — the ruling's paragraph names the row's case
in terms. **Kind (B)** — the ruling reaches the row's family, its paragraph does not name the row's
case, and the placement is the writing side's reading, marked at the row as overrulable.*

| Row | Kind | Disposition placed | Ruling |
|---|---|---|---|
| 8.7 | A | ADOPTED — carried | 48 (§3bc) |
| 10.7 | A | ADOPTED — proposed | 35 (§3ap) |
| 10.5 | B | ADOPTED — carried in substance | 46 (§3ba) |
| 10.11 | B | ADOPTED — carried in substance | 35 (§3ap) |
| 10.16 | A | ADOPTED — carried | 35 (§3ap) |
| 10.25 | A | ADOPTED — carried | 35 (§3ap) |
| 10.30 | A | ADOPTED — carried | 48 (§3bc) |
| 10.31 | B | RELOCATED — to L3, *the read-off facts* | 48 (§3bc) |
| 10.44 | A | RELOCATED — to *the measurement of the analysis* | 48 (§3bc) |
| 10.51 | A | QUARANTINED | 35 (§3ap) |
| 13.3 | B | ADOPTED — proposed | 48 (§3bc) |
| 13.13 | A | ADOPTED — carried | 48 (§3bc) |
| 14.14 | B | ADOPTED — carried in substance | 46 (§3ba) |
| 14.21 | A | ADOPTED — carried | 34 (§3ao) |
| 15.8 | B | ADOPTED — carried in substance | 46 (§3ba) |
| 18.3 (i) | B | ADOPTED — carried in substance | 46 (§3ba) |
| 18.4 | B | ADOPTED — carried in substance | 46 (§3ba) |
| 18.5 | B | ADOPTED — carried in substance | 46 (§3ba) |
| 18.6 | A | ADOPTED — proposed | 46 (§3ba) |
| 18.7 | A | ADOPTED — carried | 46 (§3ba) |
| 18.8 | A | ADOPTED — proposed | 46 (§3ba) |
| 18.9 | A | ADOPTED — proposed | 46 (§3ba) |
| 18.10 | A | ADOPTED — carried in substance | 46 (§3ba) |
| 18.11 | A | ADOPTED — carried in substance | 46 (§3ba) |
| 18.13 | A | ADOPTED — carried in substance | 46 (§3ba) |
| 18.14 (i) | A | ADOPTED — carried in substance | 46 (§3ba) |
| 18.14 (ii) | A | ADOPTED — carried | 46 (§3ba) |
| 18.16 | A | ADOPTED — proposed | 46 (§3ba) |
| 18.19 | A | ADOPTED — proposed | 46 (§3ba) |
| 18.20 | A | ADOPTED — carried in substance | 46 (§3ba) |
| 18.21 | A | ADOPTED — proposed | 46 (§3ba) |
| 18.22 | A | ADOPTED — proposed | 46 (§3ba) |
| 18.23 | A | ADOPTED — proposed | 46 (§3ba) |
| 18.24 | B | ADOPTED — carried in substance | 46 (§3ba) |
| 18.26 | A | ADOPTED — proposed | 46 (§3ba) |
| 18.28 | A | ADOPTED — carried | 46 (§3ba) |
| 18.29 | A | ADOPTED — proposed | 46 (§3ba) |
| 18.30 | B | ADOPTED — carried in substance | 46 (§3ba) |
| 18.31 | A | RELOCATED — to L2 | 46 (§3ba) |
| 18.32 | A | RELOCATED — to L2 | 46 (§3ba) |
| 18.33 | A | RELOCATED — to L2 | 46 (§3ba) |
| 18.34 | A | RELOCATED — to L2 | 46 (§3ba) |
| 18.35 | A | QUARANTINED | 46 (§3ba) |
| 18.37 | A | RELOCATED — to L3 | 46 (§3ba) |
| 18.39 | A | QUARANTINED | 46 (§3ba) |
| 18.40 | B | ADOPTED — carried in substance | 46 (§3ba) |
| 18.41 | B | ADOPTED — carried in substance | 46 (§3ba) |
| 18.43 | A | HISTORICAL | 46 (§3ba) |
| 19.10 | A | ADOPTED — carried in substance | 46 (§3ba) |
| 19.22 | A | QUARANTINED | 35 (§3ap) |
| 21.6 | A | RELOCATED — to L2 | 47 (§3bb) |

**The table's own arithmetic, and it is the reading file's §13b that is the home of the count.** 51
rows, 51 lines, kind (A) 38 and kind (B) 13; by ruling 34 → 1, 35 → 6, 46 → 37, 47 → 1, 48 → 6. The
figures were **derived at the placement lines of the committed file**, by reading the file's own blob
at an explicit hash, and they agree with §13b line for line.

### 2.1 The two rows Ruling 46's clauses reach through Ruling 41

**Rows 18.4 and 18.30 are placed in Ruling 46's family and their ground is Ruling 41's words with
Ruling 39's**, which is how the dispatch's face states them — *"no selection tag is added"* and L1
*"owns no selection semantics"*. Their placement lines therefore name Ruling 46 as the family ruling
and quote Ruling 41 and Ruling 39 as the ground, and §13b counts them under Ruling 46. **Recorded so
a reader meets the fact rather than inferring it from the by-ruling table.**

## 3. The placements not made — and the three hits the rulings do not reach

**No placement was left unmade.** Every row heading the dispatch's face names was found **verbatim**
at the reading file, and every named row's `*PROPOSED DISPOSITION.*` paragraph read as the face states
it, so the *placements the batch could not make* list is **EMPTY**.

**Three term hits took no line, exactly as the dispatch's face orders**, and each keeps the
disposition it already carried, untouched:

| Row | What the search matched | Why the ruling does not reach it |
|---|---|---|
| **22.11** | *"extends across contiguous neighbouring slices"* (Ruling 46's term *extend*) | The sentence is a **chord-window** rule — how far a chord reading runs across neighbouring slices — not the enlargement of a working span. Its RELOCATED — to L2 disposition stands. |
| **19.20** | the word *profile* (Ruling 48's term) | The profile there is the **voice-leading axis's motion profile** and a declared reduction's provenance, not notated boundary evidence. Its ADOPTED — carried disposition stands. |
| **19.21** | the word *profile* (Ruling 48's term) | The same: the per-sample **motion-event series** of the voice-leading axis. Its ADOPTED — carried disposition stands. |

**This is the fifth writing's own treatment of Row 28.12 applied again** — a term hit whose subject is
not the family's is recorded as a hit the ruling does not reach, and nothing is written for it.

**Row 4.3 is placed by no line and was not touched.** The fifth writing reported that Ruling 62 item
4's citation of it and the row's own content do not meet; it stands at ADOPTED — proposed for its own
surface, and §13b says so.

## 4. The object-level check of Task 1, measured rather than asserted

The edited reading file was written into the object store with `git hash-object -w` (blob
`f83b30d3f15959e186777558d14407d0335b055f`) and diffed **between two explicit blob hashes** against the
tip blob `71b6bcdbb4f464d66614b34111fe0b6ee3384beb`.

- **285 insertions and ZERO deletions.** The diff is insertions only, which is what A5 requires of this
  file.
- **53 hunks**, and every one is accounted for: **51** placement lines, **one** for the §10 relocation
  item, and **one** carrying the §13a foot note together with the whole of §13b. A hunk none of those
  accounted for would have been a STOP and there was none.
- **38 `*RULED DISPOSITION … — FAMILY PLACEMENT` opening lines and 13 `*PLACED — FAMILY, AUTHORED BY
  THE WRITING SIDE` opening lines** — 51 in total, equal to the rows the face places, none skipped.
- **No `**Row` heading was added or removed; no `*PROPOSED DISPOSITION`, `*Statement.`, `*Outgoing
  statement.`, `*Derived.` or `*Current-text axis.` line was added or removed** — enumerated over
  every added line of the diff, and zero deletions makes the removal half true by construction.
- **§13's two tables are byte-identical.** They sit at old lines 15713–15736; the nearest hunk positions
  are old line 14888 (the §10 item) and old line 15823 (the §13a foot note and §13b), so no hunk falls
  inside them.

## 5. The object-level check of Task 2

The corrected successor was hashed the same way (blob
`61ca4bb7b0590ad65c33d52b9969501eb4ce4219`) and diffed against the tip blob
`9fb4f74f5f4103a0de59c6f1a6b243438135e69a`.

- **Exactly two hunks**, and they are the two the dispatch orders: the statement's last line, and the
  added `- *Reworded under Ruling 42 …*` marker line.
- **All 54 statements present exactly once; all 19 open questions present exactly once** — enumerated
  at the new blob's own `**S-n.` and `- **OQ-n` openings.
- **Zero deleted `*Beside`, `*Amended by`, `*Ruled`, `*Reworded` or `*Completed` lines.** The one
  deletion in the whole diff is the statement line the correction replaces, whose former wording is
  preserved verbatim inside the added marker.

## 6. What was measured, not assumed

- **The declared start state held exactly.** A full guard run in CHECK mode before the first edit:
  **76 run, 10 failing, 4 not run, 16 historical, zero STOPs** — the ten failing being the ten the
  dispatch's own FACT names, member for member, with no eleventh red. The run reproduced the tip's own
  artifact exactly, `--check` exiting 0.
- **A1 was checked over the whole tracked population** with the sanctioned enumeration tool:
  **zero** tracked modifications at boot, which is what A1 declares, and both named untracked paths
  confirmed present.
- **A3 was measured before being accepted:** the membership artifact regenerated and its blob hash
  compared against the committed one — **byte-identical** (`fb3a00be…` both sides) — so it was **not**
  committed and Task 0 carried **two** paths rather than three.
- **The counts of §13b were measured at the committed file**, by reading its blob at an explicit hash
  and summing the placement lines, never taken from the dispatch's face.
- **The forward bound was measured, not assumed:** re-aimed at all five authored inputs with
  `PREVIOUS_AIMINGS` appended to rather than replaced, run, and the dispatch's prediction held exactly
  — **the sixth writing's three entries moved, the second writing's two stayed, no STOP** — with the
  tool's own reconciliation green in both limbs and `--check` passing after the act.
- **A2's two watched exceptions did not fire.** `gen_l0_l1_outgoing_population.py --check` still
  re-derives after the reading file and `STATUS.md` moved, and `gen_session_start_read_size.py --check`
  is green, regenerated after the last edit to `STATUS.md`.
- **Every commit was verified at the object** by `git show --stat` at its explicit hash, and
  `origin/master` read at its ref file with the file tools after each push, never from a push's own
  output.

## 7. What this batch did not do

No ruling taken, no position weighed, no verdict on any open question, on the derivation, or on the
deriving session's independence. No session booted. No measurement built or run. No derivation of any
statement. No edit to the derivation (banner or body), the sitting record, the brief, the pack, its
generator, its artifact, the population tool, any outgoing text, the suspension file, any register
source, or any governing document other than the two Task 3 orders. In the reading file: no
`**Row` heading, statement, derived line, axis line or `*PROPOSED DISPOSITION` paragraph edited, no
existing `*RULED DISPOSITION` line edited, §13's tables and §13a's figures untouched. No `src/`
change, no golden, no test changed, moved or run, no build, nothing under `tools/corpus/` or
`tools/robust_stop/`. No open-items row created, flipped or discarded; no register entry and no
identifier allocated; no guard enrolment; no tool added. **No recommendation appears anywhere** — not
in the reading file, not in the successor, not in the close, not in this report, not in a commit
message.

The one `tools/audit/*.py` edit is the re-aiming of `gen_status_batch_bound.py`'s five authored
constants, its `PREVIOUS_AIMINGS` list and its two describing head comments, which Task 3 orders in
terms and which **D-648** classes as authored-input maintenance.

## 8. Declared departures

1. **The dispatch was read from the working tree before it was pinned** — P-2's own fallback route.
   Pinned at Task 0, blob proven unmoved before staging.
2. **Diff output and guard output were written to scratch paths outside the repository and read with
   the file tools.** Every repository read went through the file tools; the only shell reads were git
   object queries by explicit hash (`git show <hash>:path`, `git rev-parse <hash>:path`,
   `git hash-object -w`, and `git diff` between two explicit hashes).
3. **★ The ordered correction text of Task 2 was WRAPPED to the file's own line width.** The dispatch
   fixes the words of S-48's new last clause and of the marker line; the successor wraps its prose at
   about a hundred columns, and each ordered text was wrapped to it. **No word was added, dropped or
   altered.** This is the sixth writing's declared departure 4, met again.
4. **★ Task 2's commit subject was first written without the apostrophe in *S-48's*, and the commit was
   amended before it was pushed.** The dispatch orders plain single-quoted `git commit -m '…'` subjects
   **and** an ordered subject containing an apostrophe; a single-quoted shell string cannot carry one.
   The first attempt dropped it, and the amend restored the ordered subject **verbatim** by feeding the
   message on standard input. **Declared because the ordered text is a text and the first commit did
   not carry it exactly**; nothing but the message changed, and the amended commit is the one pushed.
5. **★ One added line of §13b was reworded before the commit, and the reason is the check itself.** The
   paragraph about Row 4.3 first opened `**Row 4.3 is placed by no line here.**`, which the ordered
   check *no `**Row` heading added* would have flagged on its shape although it is prose. It was
   reworded to `**No line here places Row 4.3.**` before the object-level check was taken and before
   the commit. **Declared because the committed state is right and the intermediate state read wrong to
   a mechanical test.**

## 9. Where the hashes are

The commit table — the tree met, Task 0, Task 1, Task 2 and the close — is in the close section of
`cowork_away_returns.md`, each by explicit hash, each pushed and each verified equal at
`.git/refs/remotes/origin/master` **with the file tools**. **This report's own commit and the end-state
commit that follows it are in the git log**, which is where a document cannot name its own identifier.
**The close does not assert the end state**; the one further commit carries it.
