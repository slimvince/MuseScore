## COWORK SESSION CLOSE (HUNDRED AND TWENTY-THIRD ENTRY, 2026-09-06 — THE CONTINUING LINE) — ★★ **THE SESSION THAT BOOTED ON THE HUNDRED-AND-TWENTY-SECOND ENTRY READ ROW 11 WHOLE AT THE OBJECT. ELEVEN OF 36 ARE NOW READ — rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10 and 11 — GROUP 1 IS CLOSED AND GROUP 2 IS OPEN WITH TWO READ. The extract `reading_pass/extracts/sarawagi-cohen-2004-semi-markov-conditional-random-fields.md` (36,476 bytes) and the rewritten progress record `reading_pass/l2_slice_reading_progress.md` (43,919 bytes) are on disk, both proved at a listing, BOTH ON THE FIRST COMMIT — the progress record on a STAGED path, guarded by the staged copy's `expectedMtimeMs`, and it landed RIGHT.** ★ **ROW 11 IS CENTRAL ON A NARROW GROUND AND OWES A SECOND EXTRACTION. An IDENTITY finding of row 29's milder shape (title a prefix match; no venue, year or licence printed). DP-C's linear-versus-exponential [FACT] is RE-VERIFIED at the object with three wording precisions. The question row 10 carried is ANSWERED: the original semi-CRF's segment features see the previous label. No falsifier; no STOP.** ★ **THE NEXT ACT IS THE NEXT BOUNDED READING SESSION — row 19 next (Harana), then rows 20, 18, 47.** ★ **NOTHING RENDERS: Ruling 1's gate stands — 25 of 36 not opened.** ★ **THIS ENTRY'S OWN FIRST LANDING WAS STALE — ON A NEVER-STAGED PATH, THE FIRST TIME THE LINE HAS SEEN THAT SHAPE THERE; it was caught at the user's fact-check instruction, corrected and re-landed guarded. Read the bridge-fault section below: a never-staged path is NOT the safe case.** ★ **NO BATCH IS OUT. THE TIP IS UNMOVED AT `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, read at both ref files.** ★ **THE GUARD SET STANDS AT TWELVE FAILING (relayed) AND `STATUS.md`'s TOPMOST ENTRY SAYS ELEVEN (read at the object this session) — both known, both ruled (Rulings 8 and 9, relayed as the hundred-and-twenty-second entry relays them), untouched; nothing ran.** THE CURRENT ENTRY POINT; the hundred-and-twenty-second entry is superseded as entry point and stands otherwise — including its bridge-fault section, its running total and its declared departures, none of which is restated here except where this session adds to it.

You start clueless. Read this block first, then perform the ordinary session-start read: `CLAUDE.md`
whole, `DECISIONS.md` whole, `STATUS.md`, and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`). **A
single-file opening instruction is not an exemption from that read** (P-1). **The eighty-second
through hundred-and-twenty-second entries remain binding except where this entry names a change**, and
every standing bar of the earlier blocks remains binding.

**★ THE PRACTICAL FACTS of the hundred-and-sixteenth, hundred-and-eighteenth, hundred-and-twentieth and
hundred-and-twenty-second entries are unchanged and are not restated here (#6); read them there.** This
session confirms one of them by doing it right: the bridge's directory listing of the repository ROOT
exceeds the tool-result cap and is saved by the harness to a file; **this session consulted that saved
listing with `Grep` only** (three times: to prove the previous entry's landing, to locate five root
files, and to find that `candidacy_upgrades.md` is not at the root), and listed `reading_pass/`,
`reading_pass/extracts/` and `docs/research_papers/` directly as subdirectories. No Python and no shell
touched a listing. **`candidacy_upgrades.md` lives in `reading_pass/`, not at the root.**

**On form.** Plain ordinary English with the user. **NEVER use the multiple-choice widget with him.**
A decision surface is fact-based and checked at the objects, every position weighed towards the
ultimate objective and towards the guiding principles, the recommendation with the reason and the
strongest argument against it; when the facts collapse the alternatives to one, say so; before putting
a question, check whether the record already answers it. **He asks that a dispatch be sourced and
fact-checked at the objects before he runs it, and that a deliverable he is about to rule on be
fact-checked as a turn of its own before any question** — do both unasked. **A read put to him by name
is shown, not ratified; if he says nothing it stands and the next act proceeds.** Every self-invented
word is explained in plain words the first time or not used.

### ★★ THE BRIDGE FAULT — READ THE HUNDRED-AND-EIGHTEENTH ENTRY'S SECTION BEFORE YOU WRITE ANYTHING

It is not restated here (#6). **★★ THIS SESSION SAW THE STALE SHAPE — AND ON A NEVER-STAGED PATH,
WHICH THE LINE HAD NOT SEEN BEFORE.** Both of its reading files landed the right bytes on the first
attempt, in ONE `device_commit_files` call carrying both: the extract (never-staged path, 36,476 at the
listing, equal to the container source) and the progress record (staged path, guarded by the staged
copy's `expectedMtimeMs` 1788676692169, 43,919 at the listing, equal to the container source). **But
this entry's own FIRST landing was STALE:** the call returned `written`, nothing rejected, and the file
at the root listing was 26,386 bytes — the version of this entry BEFORE its last `Edit` — where the
container source was 26,453; the landed copy, staged back and read whole, carried the pre-edit
wording at its departures item (ii). **The path had never been staged.** Until this session the line
had recorded the stale shape twice, both on staged paths (the hundred-and-eighteenth entry's section);
a never-staged path is therefore NOT the safe case, and the operating rule applies to every commit
without exception. The entry was corrected in the container and RE-LANDED guarded by the landed copy's
`expectedMtimeMs` 1788678880958, to be proved at the listing after the call. **The operating rule was
followed for the two reading files and was BROKEN for this entry's first landing**: the root listing
was taken and the size read, but it was compared against a `stat` taken BEFORE the last edit, so the
comparison proved nothing and the claim "equal to the container source" was asserted, not checked —
recorded under Errors. **The rule, sharpened by this instance: take the `stat` of the container
source IMMEDIATELY BEFORE the commit call, and compare the listing's size against THAT.** **The running
total, in the hundred-and-twenty-second entry's two-reading form:** that entry's totals were
*thirty-seven across nine sessions, thirty never-staged and seven staged*, or *thirty-six, twenty-nine
and seven*. This session adds two proven first-commit landings (the extract, never-staged; the progress
record, staged), and proves the hundred-and-twenty-second entry's SECOND landing — the one that entry
records as on a by-then-staged path, guarded, and leaves to the next session — at 29,428 at the root
listing, read by `Grep` on the saved listing, the size the user gave as its proof (its FIRST landing,
26,335, that entry's own session proved and the line counts in neither total): **forty across ten
sessions, thirty-one never-staged and nine staged; or thirty-nine, thirty and nine.** **Beside either
total now stand THREE proven STALE first-commit landings — two on staged paths** (the
hundred-and-seventeenth and hundred-and-twenty-first entries' sessions) **and ONE on a never-staged path
(this entry, this session).** This entry's second landing is proved by the next session, by its size at
the root listing and a whole read.

### What this session was

The session that booted on `cowork_handoff_entry_one_hundred_and_twenty_two.md`, on the user's opening
instruction to read that entry with the file tools, take its proof of landing as 29,428 bytes at the
root listing, and read what row 11 IS at `candidacy_upgrades.md`'s row 11 before staging anything. It
did both first and reported them to the user in plain text, then the full session-start read (the tip
at both ref files; `CLAUDE.md` whole, 1,864 lines; `DECISIONS.md` whole, 862 lines; `STATUS.md` whole;
the gating answer — 244 open, 219 gating, 25 non-gating, and the `gating_ids` list whole; the
hundred-and-eighteenth entry's bridge-fault section), then the reading set the hundred-and-twenty-second
entry's cadence step 3 names (the progress record whole; `candidacy_upgrades.md` whole; both
commissions whole; the row 10 extract at its form-bearing sections — opening, identity, model,
coupling, adopt-or-argue, findings, centrality, provenance; `population.md`'s V10 row; `FRAMEWORK.md`'s
DP-C and §14.1 passages) and beside it the slice derivation's row 11 line, the bibliography's row 11
line and the findings surface's DP-C block, then one thing:

**Read row 11 whole at the object and extracted it.** Sarawagi & Cohen, "Semi-Markov Conditional Random
Fields for Information Extraction", the held file `docs/research_papers/sarawagi_cohen_2004_nips_semi_markov_crf.pdf`
(87,343 bytes), eight pages, staged through the bridge by the one matching file name in the
`docs/research_papers/` listing and read as page images with the file tools; the printed title, authors
and (absent) venue were checked against the bibliography's row (line 25) on page 1 before anything was
extracted — **authors match; the title matches the row's as a prefix and adds "for Information
Extraction"; the held document prints no venue, year, copyright or licence line — an IDENTITY finding
of row 29's milder shape, routed to the bibliography reconciliation.** The record's own statements about
the paper were read at their lines first: `candidacy_upgrades.md` row 11 (line 73); the slice
derivation's row 11 (line 58); the bibliography's row; `FRAMEWORK.md` DP-C (lines 690–701) and §14.1
(lines 1008–1010), located by a `Grep` of the staged tree; the findings surface's DP-C block; and
`population.md` §3's V10 row (line 111). **The extract is the record; what it produced, in one line
each, is in the progress record and is not restated here (#6). No falsifier; no STOP.** The paper is
**CENTRAL on a narrow ground** by the extract's own reasoning — DP-C's "formal result" [FACT] rests on
it and is re-verified at the object, and three of its claims would be cited directly by an L2 detail
specification — and **a second independent extraction is OWED**; the extract states the ground on which
NOT CENTRAL could be argued (no musical content; the one figure already verified by the pass), and the
verdict is challengeable at the progress record.

**Two things stated here because the next reader meets row 19 through them.** (a) The question row 10
carried is answered at the object: in the original formalism every segment feature may depend on the
previous segment's label (equation 2, page 2); row 10's "weak" form is that paper's speed restriction.
The reader of row 19 should note which form Harana uses. (b) V10's "largest ablation contributor"
figure is cited from row 19 (`population.md` line 111: Harana §5.3 and Table 2) — re-verify it at the
object as a matter of course.

**Why the session stopped after one paper.** The full boot read plus one eight-page paper with a
thirty-six-kilobyte extract, plus the two opening reads and their report, is at the bound the previous
sessions found; the stop is at a member boundary, by choice, under the commissions' cadence rule, with
everything landed and proved.

### ★ THE STATE YOU INHERIT

**What in this section was read at an object this session and what was relayed, stated once:** the
tip (both ref files), `CLAUDE.md`, `DECISIONS.md`, `STATUS.md`, the gating answer, the progress record,
`candidacy_upgrades.md`, both commissions, the row 10 extract at the sections named above, the slice
derivation's and the bibliography's row 11 lines, `FRAMEWORK.md` at DP-C and §14.1, the findings
surface's DP-C block, and `population.md` §3 were READ. The verdict counts, the guard-set figure, the
untracked-file list, the continuous-pruning remark and the "waiting on him" list are RELAYED from the
hundred-and-twenty-second entry, which relays them from earlier entries; this session did not open
those earlier entries beyond the hundred-and-eighteenth's opening and bridge-fault section.

- **The tip** is `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, `origin/master` equal, read at both ref
  files with the file tools (`.git/HEAD` → `refs/heads/master`); nothing ran since. **No batch is out.**
- **`VERDICTS["l2"]`** 111 IN / 133 OUT / 0 UNPLACED (relayed; not re-verified). **`WITHHELD`, `EXTRAS`,
  `FROZEN`** carry no `l2` key (relayed; `STATUS.md`'s topmost entry, read this session, says the same
  of the writeback batch).
- **The reading file** `ratification_surfaces/cowork_withheld_family_l2_reading.md` stands RULED
  (`STATUS.md`'s topmost entry, read this session).
- **Task B's L2 slice: 11 of 36 read** (rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10 and 11); **group 1
  closed, group 2 open with rows 10 and 11 read.** The progress record is
  `reading_pass/l2_slice_reading_progress.md` (43,919 bytes) — read it whole before opening any paper;
  it names the next member, carries TWO things from row 11 to row 19, and states that its row numbers
  are `candidacy_upgrades.md`'s and that `object_reads/` uses `population.md`'s numbering.
- **Untracked files of this line at writing, none landed by this side:** this entry; the
  hundred-and-twenty-second through hundred-and-ninth entries; the surface
  `cowork_l2_boot_list_surface_2026_09_05.md`; the sitting record
  `cowork_rulings_2026_09_05_l2_boot_list_sitting.md`; the two derivations of the hundred-and-eleventh
  entry; the read file `cowork_l2_score_set_read_2026_09_05.md`; the eleven extracts of rows 27, 28, 1,
  8, 3, 26, 29, 30, 5, 10 and 11 and the progress record. The standing untracked historical `cc_*` root
  population is pre-existing. This side ran no enumeration.
- **The gating answer:** 244 open, 219 gating, 25 non-gating at the session-start read; nothing ran.
- **The guard set:** 77 run, 12 failing (relayed; not re-run). **`STATUS.md`'s topmost entry says
  "ELEVEN FAILING"** (read at the object this session; Ruling 9: corrected in place by the next dispatch
  that touches `STATUS.md`, former wording preserved). **The continuous-pruning bound is unmaintained** —
  standing since the hundred-and-sixth entry (relayed).
- **The plan stands at:** … → L2's boot list RULED (nine rulings) → reads 1, 2 and 3 of §11 DONE →
  **Task B's L2 slice (36 papers) read whole at the object — 11 DONE, 25 TO GO, group 1 closed, group 2
  open** — in bounded sessions closing at member boundaries, under the remedial commission §3 and the
  original commission's §4 form, central papers owing a second extraction, in the verdict table's
  proposed order ("stated, not ruled") → **the pack-build dispatch** when next (D-250) → the brief → the
  blind deriving session → L2's comparison reading. **Beside it, the L0/L1 comparison reading stands at
  7 of 29 documents tabulated** (relayed).

### ★★ THE GATE, RESTATED

No derivation may begin before L2's slice of Task B is in (eighty-seventh entry; Ruling 10 of the
2026-08-31 sitting; Ruling 1 of the 2026-09-05 sitting). **The slice is 36 papers, all held. Eleven are
read.** The gate is not lifted and cannot be until the other 25 are read, flagged unreadable, or
stop-reported.

### ★ WHAT COMES NEXT

**The next act is the next bounded reading session** — open `reading_pass/l2_slice_reading_progress.md`,
the remedial commission's §3 and the original commission's §4 form before reading anything; take the
next row's paper from `candidacy_upgrades.md`'s own row (never from a file name elsewhere), find its
held file in the `docs/research_papers/` listing, stage it by name, check its printed title, authors
and version against the bibliography's row on page 1, read it as page images, extract in the §4 form,
rewrite the progress record whole (a file this line wrote, 43,919 bytes — read it whole first), close
at a member boundary. **Row 19 is next** — this session did not look up what row 19 is beyond its table
line in `candidacy_upgrades.md` (line 81: Yang, Cwitkowitz & Duan, ISMIR 2023, harmonic analysis with
neural semi-CRF, Harana, held, "The neural semi-CRF; DP-C's largest-ablation-contributor evidence, and
a current candidate architecture for L2") and its slice-derivation line (line 66); the next session
reads both rows again and the bibliography's row (line 33), and finds the held file in the listing (the
one whose name begins `yang_cwitkowitz_duan_2023`, 318,293 bytes at this session's listing). **Row 19 is
cited by the record at DP-C's "single largest contributor in a third system's ablation" [FACT]
(`FRAMEWORK.md` lines 696–697) and verified by the pass at `population.md`'s V10 row (line 111, "§5.3 +
Table 2")** — read those before extracting, and check the figure at the object as a matter of course.
**The two things carried from row 11 to row 19 are in the progress record's "Next" section.** The
pack-build dispatch is NOT next (D-250).

**Routed in the row 11 extract and the USER's to act on or to leave:** the identity finding (title a
prefix match; no venue, year or licence printed), for the bibliography reconciliation; the three
wording precisions to DP-C's "formal result" [FACT] (upper bound, not equality; "much of the power" in
the paper's own words; feature-set dependence), for the findings surface's V10 row and DP-C block; the
answer to row 10's question (previous-label access in the original formalism), for L2's detail
specification beside DP-C; the factorization test as a design check on score terms, for L2's detail
specification and beside row 20; the cap-from-observed-lengths precedent, beside D-004; the text-domain
bound on the paper's measurements, for the DP-C block; the fully discriminative held-out fit, beside
D-525 and DP-P and for measurement design; the paper's own intractable rival, for L2's detail
specification; the one-label-per-segment datum, beside D-004 and D-526; and the exact-posterior datum,
beside D-006, D-008 and DP-K. Together with the earlier extracts' routed findings, none is a STOP and
none needs a ruling before row 19 is read. **Shown, not ratified.**

### The user-owed list

**Closed this session by this side, and not owed to him:** the eleventh reading session (row 11).

**Owed to him by this side:** nothing. Every finding of the extract is ROUTED in the extract itself and
applied nowhere.

**Waiting on him, CARRIED from the hundred-and-tenth entry as later entries relay it (this session read
only the hundred-and-twenty-second and the hundred-and-eighteenth's opening and bridge-fault section),
still open, unverified this session:** **(a)** whether the pilot's derived specification still carries
its independence claim after Ruling 85; **(b)** which branch of the filing convention (D-674) governs a
ruling record; **(c)** the close test's wording (close section and end-state artifact in one commit
versus one apart); **(d)** re-aiming the `expected_line` values in `gen_phase3_gate_partition.py`;
**(e)** whether the report template should carry the close-tip marker by design; **(f)** the fifth,
sixth and seventh guard observations not yet in OI-378's detail file.

**Shown to him and standing unless challenged:** the score-set read of §11 (hundred-and-twelfth
entry); the row 27, 28, 1, 8, 3, 26, 29, 30, 5, 10 and 11 reads. His opening instruction this session
named row 11 as the next act, which is the next act proceeding, not a ratification of any read.

**Standing, unchanged and still the user's:** the list the hundred-and-ninth entry carries under this
heading, as later entries relay it — neither the hundred-and-ninth nor the hundred-and-tenth entry was
read this session. **The eleven quarantined audit questions stay reserved to the AUDIT.** **Not acted
on:** the guard-runner parallelisation suggestion; the rendering tool's "1 entries" pluralisation
defect. The hundred-and-twenty-second entry's observation about `FRAMEWORK.md`'s sealed first-stage
text at lines 1685–1686 (the former V4 wording) stands as that entry states it; this session did not
read that passage.

### Errors

**Counted on this side this session, of the family the degradation rule names: ONE, and it reached
the user's chat and a landed file.** The landing proof of this entry's first commit compared the root
listing's size (26,386) against a `stat` of the container source taken BEFORE the entry's last edit,
and reported "equal to the container source" — an equality asserted without having been checked, of
exactly the family the rule names (a claim about an unexamined thing). The landed file was in fact the
pre-edit version (the bridge-fault section above), so the proof was false and the first landed copy
carried a departures count ("Two `stat` commands") that was already false when it landed. Caught at the
user's instruction to fact-check the handover, at the landed object; corrected here and re-landed. The
session total is ONE; this close is the handover in any case, at a member boundary with nothing
half-done. **One near-miss, recorded because the rule counts the tell and not the landing:** the
extract's first draft stated the semi-CRF's gain over the better CRF baseline as "1.3 to 5.6 F1
points"; the fact check against Table 1 (page 6) derived the five differences — 4.8, 5.3, 1.3, 2.7 and
5.4 — and the range is 1.3 to 5.4. An upper figure asserted without having been derived, of exactly the
family the rule names; caught at the extract's own fact check before the file was committed, and
corrected there. It was caught at the object before landing, so it is not counted; it is recorded so
the next reader knows the shape recurred. The extract's fact check was run against the page images
before landing — every Table 1 and Table 2 cell compared with the extract's transcription, every
quotation of §2.1–§2.3 and §3.3 compared at the page, the section, equation, table, figure and footnote
counts confirmed at the pages — and no claim rests on a summary of an unread extract (the row 10
extract, cited for the carried question and the form, was read at the sections named above; its
tabular body, lines 131–394, was NOT read).

**Errors of previous sessions found this session:** none. The hundred-and-twenty-second entry's report
of the row 11 held file's size (87,343 bytes) and name prefix is CONFIRMED at this session's listing;
its statement that `candidacy_upgrades.md` row 11 is at line 73 and the slice derivation's at line 58
is CONFIRMED at the files.

**Declared departures of this side, this session.** (i) **A shell `cp` of the progress record's STAGED
copy into the work directory before its rewrite** — the hundred-and-twentieth and
hundred-and-twenty-second entries' declared departure, repeated here because no work-directory copy of
the file existed in this session and re-typing 38,325 bytes from tool output is the transcription risk
the record forbids; the copy was byte-exact (38,325 at `stat`), then edited with the `Edit` tool in six
places (the session note; the table row and counts; the "what row 11 produced" paragraph and the
"Next" section; the not-done paragraph; the provenance's session count; the provenance's seventh-session
sentence). (ii) **Five `stat` commands (two on the reading files, three on this entry — before its first
landing, at the fact check that found the stale landing, and immediately before its second landing),
one `mkdir`, one `cp` of the two reading files into the outputs directory, and one `grep -c` on this
entry's container copy** at the fact check — all in the container on this side's OWN output files,
declared because they are shell commands in the sandbox; none touched repository content beyond the
staged copy named in (i). (iii) **Three `device_commit_files` calls:** the first carrying two files —
the extract (never-staged) and the progress record (staged, guarded) — both landed right, both proved
at listings; the second carrying this entry as first written, which landed STALE (26,386 at the root
listing against 26,453 in the container, the pre-edit version); the third carrying this entry corrected
and RE-LANDED on the by-then-staged path, guarded by the landed copy's `expectedMtimeMs`
1788678880958, to be proved at the listing after the call by `Grep` on the saved root listing against
a `stat` taken immediately before the call. (iiia) **This entry was re-staged and read back WHOLE after
its first landing, fact-checked against the sources in this session's context on the user's
instruction, amended in three places, and re-landed.** The amendments: the bridge-fault section (the
stale landing, the false proof, the sharpened rule, and the running total's account of the
hundred-and-twenty-second entry's landing, which had called its SECOND landing its first); the Errors
count (none → one, the false proof); and this departures list (the shell-command counts and the
commit-call count). (iv) **`Grep` through
the file tools** on: the saved root listing (three times, as stated in the practical facts above); the
gating answer for its keys; the hundred-and-eighteenth entry for its bridge-fault heading; the staged
tree for "Sarawagi", "semi-Markov" and "semi-CRF"; and the rewritten progress record for stale phrases
before landing. (v) No subagent was used. (vi) One paper was opened, whole, eight pages; page 6 and
page 3 were re-read once each for the fact check. (vii) The handoff entries before the
hundred-and-twenty-second were NOT read except the hundred-and-eighteenth entry's opening and
bridge-fault section; the sitting record, the hundred-and-eleventh entry's derivations, the extracts of
rows 27, 28, 1, 8, 3, 26, 29, 30 and 5, the row 10 extract's tabular body, the V4 surface, and
`FRAMEWORK.md` outside DP-B's close, DP-C and §14.1 were NOT read. (viii) The user connected no new
folder. (ix) `SendUserMessage` was used twice to put the two opening reads and then the row 11 read to
the user in plain text; no multiple-choice widget was used. (x) One directory listing of the
repository root was requested before this entry; it exceeded the cap and was saved, and the saved file
was consulted by `Grep` only (three times); a second root listing is taken for this entry's landing
proof and consulted the same way, a fourth `Grep`.

**What went right and is worth copying:** the two opening reads were done and reported before the boot
so the user saw them at once; `candidacy_upgrades.md` was found by listing its subdirectory rather than
by searching the saved root listing with a shell; the paper's page 1 was checked against the
bibliography's row before extracting and the identity finding was taken there; the record's citations of
the paper were located by `Grep` and every one read at its lines; the fact check re-read the two pages
the values come from and caught the one underived figure before landing; both landings were proved at
listings in one pass; and the handover was written at a member boundary with nothing half-done.

### The cadence for your session

0. **Boot as above, in full.** This entry file is UNTRACKED and you never land it yourself; a later
   dispatch's Task 0 does. **If your first file read fails, stage the file through the bridge and read
   the staged copy; only if staging fails, ask the user to add the folder.** **List `docs/research_papers/`
   and `reading_pass/` as subdirectories; if you must consult the saved root listing, `Grep` it.
   `candidacy_upgrades.md` is in `reading_pass/`.**
1. **Re-read the tip at the ref files with the file tools.** It should be `911f5f7cda…` at both. A
   moved tip means something ran that this entry does not know of — read `cowork_away_returns.md`'s
   last section before anything else.
2. **BEFORE YOU WRITE ANYTHING, READ THE HUNDRED-AND-EIGHTEENTH ENTRY'S "THE BRIDGE FAULT" SECTION.**
   Prove this entry landed: a successful whole read of it at the object is the proof; record its size
   from the root listing in your own entry.
3. **Read `reading_pass/l2_slice_reading_progress.md` whole**, then the remedial commission's §3 and
   the original commission's §4 form, then one of the eleven extracts for the form (the row 10 extract
   is the largest; the row 11 extract is the most recent). **The next act is row 19 — read what row 19
   IS at `candidacy_upgrades.md`'s row 19 before anything else, and read that file whole while you are
   there; read `population.md`'s V10 row and `FRAMEWORK.md`'s DP-C passage too, because the record cites
   row 19 for the largest-ablation-contributor [FACT].**
4. **Verify at the object, not at a report; sweep the whole document when you correct a claim; run the
   fact check twice and search for the corrected text on the second pass. Locate a section reference at
   the heading list, never from where the passage was found; locate a ROW at its table, never from a
   file name. Check the paper's printed title, authors and version against the bibliography's row
   before extracting. Do not transcribe a table cell you cannot read with certainty at the image. Do
   not cite another extract's content from a summary of it — read the extract or cite nothing. Derive
   every range and count you state from the cells, and check it once more at the fact check. Take
   the `stat` of the container source IMMEDIATELY BEFORE every `device_commit_files`, and after it
   LIST the directory and compare the listing's size against THAT stat; a size that matches an older
   stat, or a new modification time at the old size, is the stale shape — it happens on never-staged
   paths too — stage the landed copy back, read it, re-commit guarded by its `expectedMtimeMs`.**
5. **A read put to him is shown, not ratified. Do not ask him to confirm it; do not gate the next act
   on his word.**
6. **Close at a member boundary with nothing half-done**, and write the next entry before you close.
