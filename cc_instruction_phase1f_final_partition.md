# CC instruction — Phase 1f: the FINAL enumeration partition (the STATUS_ARCHIVE remainder + the design-document clusters), with the OI-271 licence-class verification riding

> **Read first (every session):** `C:\s\MS\CLAUDE.md` IN FULL (the three-phase rule D-231 — this
> is phase 1's LAST partition; the decisions-register section; the conventions).
> `C:\s\MS\DECISIONS.md` (the INDEX — 299 user-ratified entries; full entries under
> `decisions/group_*.md`). `C:\s\MS\STATUS.md`. `C:\s\MS\OPEN_ITEMS.md` (INDEX) with detail files
> **OI-207** (closure is proposed in THIS report if the arithmetic completes its scope),
> **OI-271** (the riding verification below), **OI-268/OI-272** (evidence duties continue),
> **OI-270** (the D-282…D-285 flag duty continues). `C:\s\MS\cowork_design_doc_template.md`.
> The disposition machinery under `tools/audit/decisions/`.
>
> **Current state:** the local branch is TWO commits ahead of origin (`0c6ed5f791`,
> `dcaa008681` — the OI-271 ruling and the archive-queue ratification). **FIRST act:
> `git push origin master`; then verify HEAD `dcaa008681`; mismatch = STOP.** Last act: push.
>
> **Hard stops:** unchanged from phase 1e — origin only; no `src/` change; no golden,
> `tools/corpus/` or `tools/robust_stop/` movement; no fix, no design, no inference change; no
> `ARCHITECTURE.md` edits (documentation-gap ROWS, never spec edits — the homing wave follows
> the user's OI-268/OI-272 rulings); a NEW decision is NEVER self-ratified (the RATIFICATION
> QUEUE is the user's); resolving what the record leaves open is a STOP on that item; a surprise
> is a STOP (#13); VS Code bash rules; a measured feasibility stop is an accepted outcome.

**Dispatch author:** Cowork, 2026-08-02, at the user's delegation of the sequencing choice
(final partition before phase 2 — D-231's phase order; the archives' measured yield).

## Task 0 — push, then the OI-271 licence-class verification (riding act, read-only)

The user ruled (OI-271, reading 1): the fitting-pool licence constraint (D-292) BINDS. The
remaining action is FACTUAL: **establish which licence class the 326-chorale fitting ground
truth actually falls in.** The census's own class list (`cowork_score_census.md:239-248`) places
the *When in Rome* analyses (CC-BY-SA) in the SHIPPABLE pool and the DCML corpora in the NC
class; the record names the source "WiR/DCML" interchangeably. Trace at the objects: where
`dcml_parser.load_wir_regions` actually reads from; the provenance of those committed annotation
files (licence files, READMEs, the census's own per-corpus classification, the upstream
repository they were ingested from — the OI-142 transposed-editions history names the lineage).
**Record the established class with its evidence at D-292's provenance and flip OI-271** (with a
dated note) if the evidence is unambiguous; if it is genuinely ambiguous at the objects, report
the ambiguity with what was found — never guess. If NC-class is established, the consequence is
already recorded (development/validation only; the ship-intended fit at the OI-38 event) — no
further action here.

## Task 1 — the STATUS_ARCHIVE remainder, read in full

`STATUS_ARCHIVE.md` lines 119–300 (the dated entries 2026-07-11 back to ≈2026-06-10, ≈73k
tokens) and 930–3,861 (the older historical sections, ≈15k) — the measured remainder phase 1e
declared. Same discipline: decisions extracted verbatim by line range, status from the record,
register entries, queue for the user; supersession chains entered, never skipped; the three
standing flag duties (D-282…D-285 refinements; OI-268/OI-272 conduct evidence; measurement-tools
findings fenced in their own report section). Once read, the 99 UNREAD-marked clusters get real
dispositions and `STATUS_ARCHIVE.md` may receive a bulk rule licensed by the read.

## Task 2 — the 1,084 design-document clusters

The remaining `cowork_*` and `docs/` documents never read in full, prioritized by unresolved
cluster count per file (the manifest's partition is the map). Documents read IN FULL, never
statements in isolation. Same entry/queue/row discipline. If capacity ends first: the remainder
measured per file and declared — but this being the FINAL partition, prefer a second session of
this same dispatch over a declared remainder; phase 1 should end with the unread population at
ZERO or with an explicit user-accepted exclusion list.

## Task 3 — dispositions, guards, close — and the phase-1 completion statement

Every cluster read reaches a final disposition; the manifest regenerates; the guards all pass at
the final tree (register `--check`, `--verify` with the anchor-remap discipline, dispositions
`--check`, the open-items living check). Rows for gaps and conflicts. Dated notes on OI-207 and
OI-268/OI-272 (evidence summaries for the user's rulings). `STATUS.md` pointer at the TOP.
**The report's final section is the PHASE-1 COMPLETION STATEMENT:** what was read in full
(everything, or the explicit exclusion list), the final unresolved count with its per-surface
stated reasons, the total register size, and — if the arithmetic supports it — **the proposal
that OI-207 close**, put to the user. Commits per change-class; push origin.

## Report

Read-in-full confirmations; the OI-271 verification's evidence and outcome; the new-decision
count and RATIFICATION QUEUE; the flag sections (D-282…D-285; OI-268/OI-272 evidence;
measurement tools); the final disposition arithmetic; the completion statement; guard results;
anomalies each diagnosed. Standing self-check before reporting.
