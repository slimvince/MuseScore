# CC dispatch — the preparation phase's TWELFTH batch: the two separated renderings closed by NAMING THE OBJECT, three standing clauses landed at one home, the write-path asymmetry enumerated as data, and two statements corrected of record

> **Dispatch (Cowork, 2026-08-19).** Executes the four rulings of
> `cowork_rulings_2026_08_19_eleventh_return.md`. **Written at a verified stop; nothing else is
> running and no dispatch was active when this was written.** The curated boot list, the empirical
> findings ledger, the fact-gate, the mining, the landing of the newly visible instruction files, the
> whole-document archiving wave, the F71 sweep, and every act on any candidacy are NOT in this batch.
>
> **★ THIS BATCH TAKES NO PIN, SWEEPS NO TOOL, AND REGENERATES NO RATIFICATION DOCUMENT.** Every act
> below is a derivation, an appended correction, or a clause written at a home the record already
> makes one. **The two questions your own report returned at its §11 on the unappliable pins are
> ANSWERED by Ruling 1 and are not re-opened**: neither member is restored, neither is pinned at a
> later commit, and neither document is touched.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional, and explicitly NOT demoted. **(3)** `BUILD_AND_TEST.md` is a
> CONDITIONAL read — **and this batch MEETS the condition**, because it runs the guard set, whose
> commands live there, so read it in full. **(4) DO NOT READ THE WHOLE OPEN-ITEMS INDEX.** Rule (a)
> reads the DERIVED GATING ANSWER narrowed to its identity list —
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids` — and the
> grounds stand at `the_gating_rows` in the same file and are opened when a verdict is CHALLENGED.
> The INDEX remains the AUTHORITATIVE STATUS SURFACE; you open it when you need a row.
>
> **★ AND THE STANDING CLAUSE BINDS THIS ONE: read `cowork_handoff.md`, ITS CURRENT (THIRTY-FIRST)
> BLOCK.** The clause lives at `cowork_audit_protocol.md`'s dispatch-protocol section, where your own
> last batch landed four more beside it. **The findings series stands at F81 when this batch begins**
> — you allocated F75–F79 at the amended eleventh batch; the writing side allocated **F80** and
> **F81** at the return sitting, and the thirty-first block is where it announced them. **Number from
> F82.**
>
> **Then read, in full:** `cowork_rulings_2026_08_19_eleventh_return.md` (this batch's whole
> authority); **your own `cc_report_preparation_eleventh_amended.md`, its §3 and §11 above all** —
> §3 because Task 1 completes what it STOPPED on, §11 because Ruling 1 answers it; the **two ruling
> records Task 1 corrects**; `cowork_rulings_2026_08_18_eleventh_stop.md` and
> `cowork_rulings_2026_08_18_tenth_return.md` (the rulings this arc is completing, **NOT re-opened**);
> and `cowork_audit_protocol.md`'s **dispatch-protocol section in full**, because Task 2 adds to it
> and you must meet the four clauses already standing there before writing three more.
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/evidence_pin_membership.json` (Task 1's subject); `tools/audit/guard_classification.json`
> and `tools/audit/guard_state.json` (the start and end states, and Task 3's population);
> `tools/audit/session_start_read_size.json` and `tools/audit/nongating_apparatus_rows.json` (Task 4
> reads the first and neither is written by this batch).
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no mining, no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, and **no caller flag or
> candidacy acted on**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end;
> record both states.** The declared start state is at §"The declared start state" below and is
> stated **at the tree you will actually meet**. **A start state differing from the one declared
> there, in any way its own paragraph does not account for, is a STOP-and-report.** Verify every
> commit at the object by explicit hash and record the SHAs in the close. **The reserved-word
> conventions bind all new prose. The new-tool rule stands. The E-ordering rule stands, at both
> grains — and your own F79 is the reason it is named again here: the run may be taken late, but a
> SENTENCE about the run written early is the same defect.** **F3's reading rule stands**
> (`gen_cluster_dispositions.py --verify` is the drift authority). **Every guard run is taken at a
> quiet tree.**
>
> **★ THE VOCABULARY RULE OF 2026-08-17 BINDS EVERY LINE THIS BATCH WRITES.** A surface rates an
> alternative **TOWARDS the ultimate objective** and **TOWARDS the guiding principles** — never
> *against* either.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1 — the separated renderings:** *"THE PIN WAS ONLY EVER A MEANS. ITS END IS THAT A LATER
  READER CAN OBTAIN THE RENDERING THE USER RULED FROM — AND GIT ALREADY PROVIDES THAT END, EXACTLY AND
  SELF-VERIFYINGLY, PROVIDED THE RECORD NAMES THE OBJECT. … the ruling record names the BLOB of the
  ruled rendering, beside the commit it already names, and the member is recorded NOT PINNED with its
  measured reason."* — with its bound: *"THE FIVE MEMBERS ALREADY PINNED STAY PINNED AND ARE NOT
  RE-TAKEN."* — and its forward clause: *"WHERE A GENERATED DOCUMENT'S COMMITTED CONTENT HAS SEPARATED
  FROM THE RENDERING A RULING WAS TAKEN OVER, THE RULING RECORD NAMES THE BLOB OF THE RULED RENDERING
  AND THE MEMBER IS RECORDED NOT PINNED WITH ITS REASON."*
- **Ruling 2 — the bookkeeping bar:** *"a check on a derived artifact's difference bars the MOVEMENT
  of an existing value, and does not bar the ADDITION of a derived cross-reference whose cause is the
  act the dispatch itself orders."* — with its second clause: *"WHERE A DERIVATION REACHES ITS INPUTS
  BY MORE THAN ONE ROUTE, A PREDICTION ABOUT ITS OUTPUT IS TAKEN FROM EVERY ROUTE."*
- **Ruling 3 — the write-path enumeration:** *"The enumeration is published as data. Each pass is
  corrected only when a ruled act needs its write path, and the published enumeration is the standing
  input to that act rather than a work list."*
- **Ruling 4 — the two corrections:** *"A DATED CORRECTION IS APPENDED AT EACH SITE, NOTHING
  REWRITTEN, THE FORMER WORDING QUOTED AND PRESERVED (#12), each naming what it corrects and the
  establishment behind it."*

## Premise ledger

*(Every FACT pins content at content-addressed objects or at a per-path git history query. No premise
asserts a changed-passage count — the F25 lesson.)*

- **FACT** — the amended eleventh batch is complete and pushed, verified at the objects by the
  writing side: `9390e2ca2c` → `a14aff1d5f` (8) → `9a78ed2fea` (10) → `6fe84208c0` (4) →
  `65fbef6df4` (6) → `3a37c5d069` (4) → `65cd91f531` (6) → `8e76921cd8` (1) → `47b5da5678` (2) →
  **`46d68a146d`**, every parent confirmed and every path count read from the object. **The terminus
  is `46d68a146d`, which no close and no report names** — it carries the report alone.
- **FACT** — `OPEN_ITEMS.md` (`6ae67d8603`) and `tools/audit/nongating_apparatus_rows.json`
  (`5bb43d0b3a`) are **byte-identical** at `9390e2ca2c` and at the terminus. **Nothing this batch does
  may move either; a difference in either at any task is a STOP-and-report.**
- **FACT, established by the writing side at the git objects and Ruling 1's own subject** — the
  artifact-inventory ratification document stands at **`d04aa5726b`** at the selected commit
  `b1d48d6c87`, at `9f76701ed4` at `9390e2ca2c`, and at `aa6157eb06` at the terminus. **The ruled
  rendering is obtainable at the object and is the thing Task 1 names.**
- **FACT, established by the writing side at the objects** — `git ls-tree 53e552296f` returns nothing
  for `cowork_rulings_2026_08_17_rulings_sort_sitting.md`, and the record is present at `570f2b63b1`.
  **The rulings-sort member's pin is not constructible and this batch does not attempt it.**
- **FACT, measured by the writing side immediately before `cowork_handoff.md` was edited** — the file
  stood **UNMODIFIED at the terminus**, its content reproducing the committed blob `cbed24a27d`
  exactly, at **378,581 bytes with ZERO carriage returns**, sha256
  `3786fc6a33c5c999d37d03fc332c2c8538aa8026d3a53c91ce11f839571cbe96`. **So the F57 caveat does not
  arise for that file, and this is measured rather than assumed.**

- **ASSUMPTION A1 — the working tree, stated by CONTENT and never by a changed-passage count.** The
  tree carries exactly **ONE tracked modification**: `cowork_handoff.md`, whose content is **two parts
  of one act** — the THIRTY-FIRST session-close block inserted, and the THIRTIETH heading carrying its
  entry-point demotion marker.
  **★ ONE PATH IS NAMED AS EXPECTED-UNMODIFIED BECAUSE TASK 0 WILL MOVE IT.**
  `tools/audit/evidence_pin_membership.json` is expected **UNMODIFIED** at its committed blob: the
  writing side did not run its generator. **If it IS modified, that is accounted for by Task 0 step 2
  and is NOT a STOP. A modification at any OTHER tracked path is a STOP-and-report.**
  **Untracked and to be landed by Task 0:** `cowork_rulings_2026_08_19_eleventh_return.md` and this
  dispatch. **Every other untracked path is the known, declared, unlanded population** — the
  newly visible `cc_instruction_*.md` files, which this batch does not touch.
  **Check ordered (Task 0, first act, blob against blob by explicit hash):** any tracked difference
  whose CONTENT falls outside that shape is a STOP-and-report, under the F57 caveat.
  **★ WHY THE THIRTY-FIRST BLOCK AND THIS ASSUMPTION WERE WRITTEN IN THE SAME ACT:** a handover block
  written after a dispatch has already stated its premise about the working tree is how a false
  premise reaches the executing side.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  **Check ordered (each task's last step):** any other red is classed by its measured cause, and one
  outside this batch's own acts' subjects is a STOP-and-report. The reds this batch itself causes and
  must clear by their ruled treatments: **the membership check, cleared by Task 0 step 2**; **the F15
  anchored-quote class**, because Tasks 1, 2 and 4 edit `cowork_audit_protocol.md`, two ruling records
  and the report; and **`gen_session_start_read_size.py --check` at the close**, because Task 5 moves
  `STATUS.md`, which is a member of the read that tool measures — **this is F79's own instance,
  expected, and cleared by regenerating the measurement and by nothing else.**

- **ASSUMPTION A3 — for each of the two separated members, the ruled rendering is obtainable as a git
  object at the commit its ruling record names.** **Check ordered (Task 1, first act):** obtain each
  blob at the object; a member whose object cannot be obtained is a **STOP-and-report** and is left
  exactly as it stands. The writing side established this for the artifact-inventory member (the FACT
  above) and **did not** establish it for the rulings-sort member, whose blob is derived at the act.

## The declared start state — stated at the tree you will meet

**71 guards run, 69 passing, TWO failing, 4 not run, 16 historical, ZERO STOPs**, and *"the guard
classification re-derives"*.

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing red,
   untouched by this batch and expected.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — **caused
   by this dispatch's own input**: the tool derives its ruling-record population from the file system,
   and **ONE untracked root-level `cowork_rulings_*.md`** now sits there,
   `cowork_rulings_2026_08_19_eleventh_return.md`.

**★ AND THE PREDICTION ABOUT THAT DIFFERENCE IS TAKEN FROM BOTH OF THE DERIVATION'S ROUTES, WHICH IS
RULING 2's SECOND CLAUSE APPLIED TO THE FIRST DISPATCH WRITTEN AFTER IT WAS RULED.** **Route A**, the
ruling-record population: the count rises by one from its committed value and one record name is
added. **Route B**, the whole-text scan for a tool named as PINNED: **the writing side probed the new
record and NO line carries `pinned` together with a backtick-quoted tool name, so route B is predicted
to add NOTHING and no third hunk is expected.** **Both halves are predictions and not citations —
check them at the artifact**, and a difference either half does not account for is a STOP-and-report.

**A THIRD failing check, or a difference in the membership artifact reaching any member, route,
document, pin constant, state or count, is a STOP-and-report.** Per Ruling 2, an ADDED derived
cross-reference whose cause is this batch's own ordered act is not such a movement — but it is
reported, with its cause established at the record's own text.

## Task 0 — land the four paths, and clear the membership check in the same act

1. **A1's check first, entirely at content-addressed objects**, under the F57 caveat.
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE its difference against the
   committed blob BEFORE accepting it**, blob against blob by explicit hash, against **both** halves
   of the prediction above. **If any member, route, document, pin constant, state or count MOVES, that
   is a STOP-and-report.**
3. **Commit exactly these FOUR paths and no fifth:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_19_eleventh_return.md` (new), `cc_instruction_preparation_twelfth.md`
   (new). Staged plainly; push; verify at the object.
4. **Re-run `gen_evidence_pin_membership.py --check` at the tree this commit carries: it must PASS.**

**Registered expectation E0:** exactly 4 paths; two modifications matching A1's shape and step 2's
bounded regeneration, two additions; no staging override; the membership check passing at the
resulting tree, so the guard set stands at **71 run, 70 passing, ONE failing** from Task 0 onward.

## Task 1 — Ruling 1: the two separated members closed by NAMING THE OBJECT, in ONE commit

1. **A3's check first, per member, at the git objects.** For each of the two members recorded NOT
   PINNED, obtain **the blob of its ratification document at the commit its ruling record names**.
   **Publish, per member: the commit, the blob, and the command that obtains it**, so a later reader
   fetches the object rather than trusting a hash. A member whose object cannot be obtained is a
   **STOP-and-report** and is left exactly as it stands.
2. **Write the blob as a DATED CORRECTION APPENDED to each of the two ruling records** — beside the
   correction that batch already appended, **never rewriting the ruled text**. Each names the blob,
   the commit, the derivation that produced both, **and the measured reason the pin is not applied**,
   with Ruling 1 of `cowork_rulings_2026_08_19_eleventh_return.md` as its authority. **Cite that
   ruling, and Ruling 1 of the tenth-return sitting as amended, and nothing else** — in particular,
   do not cite D-230 for anything.
3. **The two members are ALREADY recorded NOT PINNED at
   `tools/audit/evidence_pin_membership.json` and that state is not re-taken.** What this step
   settles is only whether the derivation also picks the BLOB up: **it may, if and only if it DERIVES
   the blob from the ruling record's own text, by the same structural route it already uses for the
   commit.** **A blob authored directly at the artifact is forbidden (#6, D-431).** If the derivation
   cannot reach it, **say so and leave the artifact as it stands** — that is data returned, not a
   defect to fix here, and the ruling record remains the authority either way.
4. **★ NOTHING IS PINNED, RESTORED, REGENERATED OR REVERTED BY THIS TASK.** The five members already
   pinned are untouched. **No ratification document is opened for writing.** A task that ends with any
   document under `ratification_surfaces/` differing from its committed bytes is a STOP-and-report.
5. **The anchored-quote consequence is the F15 class and is cleared by its ruled treatment** — anchors
   re-aimed PER CITATION from the drift report, affected derivations regenerated under the ordinary
   enumerated-movement bound, `gen_cluster_dispositions.py --verify` the drift authority. **Any
   regenerated surface's own difference is MEASURED before it is accepted.**
6. Commit, push, verify at the object.

**Registered expectation E1:** both members' ruled renderings are named by blob with the command that
obtains them, each written as an appended correction with nothing rewritten and neither citing D-230;
each member's NOT PINNED state carries its measured reason; no pin is taken and no ratification
document differs from its committed bytes; `OPEN_ITEMS.md` byte-unchanged.

## Task 2 — the three standing clauses, at ONE home, in ONE commit

**The consolidation is the dispatch's choice and its ground is stated:** all three land in
`cowork_audit_protocol.md`'s dispatch-protocol section, in one edit, which clears the F15
anchored-quote class **once instead of three times** — the shape your own last batch used for four
clauses and which worked.

1. **Ruling 1's forward clause:** *where a generated document's committed content has separated from
   the rendering a ruling was taken over, the ruling record names the blob of the ruled rendering and
   the member is recorded NOT PINNED with its reason.* Record **F76** as its ground, with the general
   form: *the pin protects TWO things that are normally one rendering — the evidence of what was PUT
   and the record of what was RULED — and once a post-ruling regeneration has separated them, no
   single commit holds both.*
2. **Ruling 2's sharpened bar:** *a check on a derived artifact's difference bars the MOVEMENT of an
   existing value and does not bar the ADDITION of a derived cross-reference whose cause is the act
   the dispatch itself orders.* Record its ground at the measured episode — the ordered check's stated
   purpose is that no verdict moves, and it was met in full while the strict reading would have
   returned a second consecutive batch over an additive cross-reference.
3. **Ruling 2's second clause:** *where a derivation reaches its inputs by more than one route, a
   prediction about its output is taken from every route.* Record **F75** as its ground, and **state
   that it stands beside the declared-start-state clause because it is the second half of the same
   lesson.**
4. **The anchored-quote consequence is cleared once, for all three.**
5. Commit, push, verify at the object.

**Registered expectation E2:** three clauses stand at the dispatch-protocol section, each with its
named ground and its general form; no other authoring rule moves; the drift authority reports every
re-aimed anchor correct at its new coordinate.

## Task 3 — Ruling 3: the write-path asymmetry enumerated, published as data, authorizing nothing

1. **DERIVE the population, never hand-list it.** Enumerate the passes whose `--check` re-derives at a
   commit their committed artifact RECORDS — the epoch-pinned kind — and report, per member, whether
   its WRITE path resolves the same commit or resolves `HEAD`. **A member whose shape the derivation
   cannot place is published as UNPLACED and STOPs to the user; it is never guessed.**
2. **Publish it as data at its own artifact**, with the derivation's own bound stated: what it reads,
   what it cannot see, and that a bare run of an asymmetric member may STOP rather than merely differ
   — which is F77's measured instance.
3. **★ THIS TASK AUTHORIZES NOTHING AND FIXES NOTHING.** No write path is corrected, no tool is
   edited beyond whatever the new derivation itself requires, and no member is acted on. **The
   published enumeration is the standing input to a later ruled act, never a work list.** A task that
   corrects a write path is a STOP-and-report.
4. **If the derivation needs a new measurement tool, the new-tool rule is discharged in the same
   commit** — its authored run-instruction in `gen_guard_state.py` and its authored classification
   verdict in `gen_guard_classification.py` — and **the guard count rises by exactly that one
   invocation, named in the close.** If it does not need one, say so; the freeze prefers no tool.
5. Commit, push, verify at the object.

**Registered expectation E3:** the population is derived and published with its own bound, every
member placed or STOPped on; no write path is corrected and no member acted on; any new measurement
tool is registered in the act that created it and the guard count's movement is caused and named.

## Task 4 — Ruling 4: the two corrections of record, in ONE commit

1. **A dated correction appended at `cc_report_preparation_eleventh_amended.md` §3.c**, the former
   wording **quoted and preserved (#12)** and the section's own text **not rewritten**. It states:
   the *"at HEAD"* column was measured at the tree Task 1 met **before its own edits**; the generator
   stands at `9a98c96729` at `b1d48d6c87` and at `9390e2ca2c`, and at a different object from
   `9a78ed2fea` — Task 1's own commit — onward, that task having written Ruling 1b's split into it,
   and different again after Task 3 wrote the banner. **Every one of those objects is stated BY
   CITATION to the git object and never transcribed from this dispatch** (D-431). **The conclusion is
   stated to SURVIVE and to be strengthened; only the inference is corrected**, and the correction
   says in terms that a render today would produce neither the ruled rendering nor the committed
   document.
2. **A dated correction appended at both sites carrying the count** — the report's opening and the
   close's own statement — with the former wording quoted and preserved: **there are SIX task commits
   and SIX `STATUS.md` pointer entries**, Tasks 0 through 5, the close being a task commit with its
   own entry. **The corrected count is stated by citation to the enumeration** and not asserted.
3. **Neither site's own text is rewritten**; a report and a close are both corrected by appending.
4. **The anchored-quote consequence is cleared exactly as in Task 1.**
5. Commit, push, verify at the object.

**Registered expectation E4:** both corrections stand as appended dated notes with the former wording
quoted and preserved and nothing rewritten; every object named enters by citation; the conclusion of
§3.c is stated to survive and only its inference is corrected.

## Task 5 — the close

One `STATUS.md` pointer entry per task — and, per the standing Ruling-4 bound of the
governing-surface split record, **the same act moves the ELEVENTH batch's entries verbatim to
`STATUS_ARCHIVE.md`** (`gen_status_batch_bound.py`), both directions proven. **The declared
`Last updated: ` prefix adjustment is imported, not re-decided (#6).**

The FULL close is appended to `cowork_away_returns.md` as a **PREPARATION TWELFTH BATCH** section:
both guard states, every SHA, every registered expectation graded MET or NOT MET with its reason,
every declared departure, and the carried findings restated — **F1–F81, surfaced and none rowed; the
row bar stands whole this batch.** The report is `cc_report_preparation_twelfth.md`. Commit, push,
verify at the object.

**★ THE E-ORDERING RULE, AT BOTH GRAINS, AND YOUR OWN F79 IS WHY IT IS SPELLED OUT.** The end-state
guard run is taken AFTER the Task 5 commit exists, and its output with the final SHAs lands in ONE
FURTHER commit. **Do not write a row, a table cell or a sentence asserting the end state into the
close commit.** The tenth batch's route is the one to take: state at the close that the end state is
NOT asserted there and that it lands in the further commit. **Expect `gen_session_start_read_size.py
--check` to go red on the first end-state run**, because Task 5 moves `STATUS.md`, a member of the
read that tool measures; it is cleared by regenerating the measurement and by nothing else, and the
sequence is recorded rather than the summary.

**Registered expectation E5:** at the tree carrying the close, a fresh full guard run re-derives —
**every run check passing except [[OI-372]]'s tool, zero STOPs** — run and read, never inferred, its
values committed only after the run that produced them, and **no sentence asserting them written
before that run existed.**

## What this batch does NOT do

- **No pin is taken, restored, moved or re-taken.** The five pinned members are untouched and the two
  NOT PINNED members stay NOT PINNED.
- **No ratification document is regenerated, restored or opened for writing.**
- **No write path is corrected and no tool is swept** — Task 3 publishes and authorizes nothing.
- **No curated boot list**, no archiving pass, no re-opening of Ruling 2 of the session-start-read
  sitting.
- **No fate on any `CLAUDE.md` span** ruled at the eighth-return sitting;
  `gen_claude_md_finer_archive.py --apply` is not run.
- **No change to rule (a)**; `tools/audit/nongating_apparatus_rows.json` byte-unchanged at every task.
- **No candidacy acted on, no census re-pin, no caller flag moved.**
- **No F71 sweep of the audit generators** — recorded and unowned.
- **No mining, no empirical findings ledger, no fact-gate admission**, no derivation, design, repair
  or pilot act.
- **No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
  `tools/robust_stop/`, no measurement of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly as
  found; **[[OI-179]] stays OPEN and GATES**; F3 stays surfaced, unfixed and unrowed at fourteen times
  surfaced.
- **`gen_gating_row_sizing.py`'s frozen population is left alone.**
