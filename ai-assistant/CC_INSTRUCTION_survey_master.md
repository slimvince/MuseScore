# CC Instruction — State Survey (master worktree)

## This instruction is for the master worktree (`C:\s\MS\`)

## No changes. Report only.

List the contents of `C:\s\MS\ai-assistant\` — every file, with size and last-modified date.

Then report the contents of your memory files under
`C:\Users\vince\.claude\projects\c--s-MS\memory\` — list every file and paste the
full content of any file that mentions "ai-assistant", "extension", "Main.qml",
"ScoreAccess", "ToolSchemas", "Dispatch", or "canonical".

That is all. No edits, no deletions, no deploys.
