## COWORK SESSION CLOSE (HUNDRED AND TWENTY-FOURTH ENTRY, 2026-09-06 — THE CONTINUING LINE) — ★★ **THE SESSION THAT BOOTED ON THE HUNDRED-AND-TWENTY-THIRD ENTRY READ ROW 19 WHOLE AT THE OBJECT. TWELVE OF 36 ARE NOW READ — rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11 and 19 — GROUP 1 IS CLOSED AND GROUP 2 IS OPEN WITH THREE READ. The extract `reading_pass/extracts/yang-cwitkowitz-duan-2023-harmonic-analysis-with-neural-semi-crf.md` (44,701 bytes) and the rewritten progress record `reading_pass/l2_slice_reading_progress.md` (50,725 bytes) are on disk, both proved at a listing against a `stat` taken immediately before the commit, BOTH ON THE FIRST COMMIT — the progress record on a STAGED path, guarded by the staged copy's `expectedMtimeMs`.** ★ **ROW 19 IS CENTRAL AND OWES A SECOND EXTRACTION. NO identity finding — title, authors, venue, year and CC BY 4.0 licence all match the bibliography's row at page 1. DP-C's "largest ablation contributor" [FACT] is RE-VERIFIED at the object with its magnitudes recorded. Both questions row 11 carried are ANSWERED: Harana uses the WEAK semi-CRF form, and the ablation figure verifies on all six columns. No falsifier; no STOP.** ★ **THE NEXT ACT IS THE NEXT BOUNDED READING SESSION — row 20 next (Korzeniowski & Widmer), then rows 18, 47.** ★ **NOTHING RENDERS: Ruling 1's gate stands — 24 of 36 not opened.** ★ **THIS ENTRY'S SECOND AND THIRD LANDINGS WERE BOTH STALE — ON A STAGED, GUARDED PATH, each carrying the version one commit behind: the line's fourth and fifth proven stale landings, and a NEW OBSERVATION about re-used container paths. Read the bridge-fault section below before you commit anything.** ★ **NO BATCH IS OUT. THE TIP IS UNMOVED AT `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, read at both ref files.** ★ **THE GUARD SET STANDS AT TWELVE FAILING (relayed) AND `STATUS.md`'s TOPMOST ENTRY SAYS ELEVEN (read at the object this session) — both known, both ruled (Rulings 8 and 9, relayed as the hundred-and-twenty-third entry relays them), untouched; nothing ran.** THE CURRENT ENTRY POINT; the hundred-and-twenty-third entry is superseded as entry point and stands otherwise — including its bridge-fault section, its running total and its declared departures, none of which is restated here except where this session adds to it.

You start clueless. Read this block first, then perform the ordinary session-start read: `CLAUDE.md`
whole, `DECISIONS.md` whole, `STATUS.md`, and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`). **A
single-file opening instruction is not an exemption from that read** (P-1). **The eighty-second
through hundred-and-twenty-third entries remain binding except where this entry names a change**, and
every standing bar of the earlier blocks remains binding.

**★ THE PRACTICAL FACTS of the hundred-and-sixteenth, hundred-and-eighteenth, hundred-and-twentieth,
hundred-and-twenty-second and hundred-and-twenty-third entries are unchanged and are not restated here
(#6); read them there.** This session confirms them by doing them: the file tools do not read a device
path directly (the first `Read` of the entry failed on both the device path and the mount path, as the
hundred-and-eighteenth entry records), so every repository file was staged through the bridge and read
at its staged copy; the root listing exceeded the tool-result cap and was saved by the harness, and
**this session consulted the saved listing with `Grep` only** (twice: to prove the previous entry's
landing at 29,994 bytes, and to locate root files by name — five matches, four of them not yet
staged); `reading_pass/`,
`reading_pass/extracts/` and `docs/research_papers/` were listed directly as subdirectories.
**`candidacy_upgrades.md` lives in `reading_pass/`, not at the root.**

**On form.** Plain ordinary English with the user. **NEVER use the multiple-choice widget with him.**
A decision surface is fact-based and checked at the objects, every position weighed towards the
ultimate objective and towards the guiding principles, the recommendation with the reason and the
strongest argument against it; when the facts collapse the alternatives to one, say so; before putting
a question, check whether the record already answers it. **He asks that a dispatch be sourced and
fact-checked at the objects before he runs it, and that a deliverable he is about to rule on be
fact-checked as a turn of its own before any question** — do both unasked. **A read put to him by name
is shown, not ratified; if he says nothing it stands and the next act proceeds.** Every self-invented
word is explained in plain words the first time or not used.

### ★★ THE BRIDGE FAULT — READ THE HUNDRED-AND-EIGHTEENTH ENTRY'S SECTION BEFORE YOU WRITE ANYTHING

It is not restated here (#6). **★★ THIS SESSION SAW THE STALE SHAPE, ON THIS ENTRY'S SECOND
(CORRECTED) LANDING, ON A STAGED PATH, GUARDED.** The call returned `written`, nothing rejected, and the
root listing showed a NEW modification time (1788680443010) at the OLD size (29,487) where the
container source was 32,094; the landed copy was staged back and its Errors count still read "NONE" —
the pre-correction text. **The guard did not prevent it: `expectedMtimeMs` protects against the user's
edit, not against the bridge writing nothing.** With the third landing below, these are the line's FOURTH and FIFTH proven stale landings and the
THIRD and FOURTH on a staged path (the hundred-and-seventeenth, hundred-and-twenty-first and this entry's sessions;
the hundred-and-twenty-third's was on a never-staged path). The entry was re-committed guarded by the
landed copy's new `expectedMtimeMs` 1788680443010 — **and that THIRD landing was STALE too:** 32,094 at
the root listing (new modification time 1788680498505) where the container source was 33,766. **AN
OBSERVATION, STATED WITH NO CAUSE ASSERTED (D-641, D-658):** 32,094 is exactly the size the container's
outputs path held at the PREVIOUS commit, and 29,487 before it was exactly the size that path held at
the commit before that — so across three commits from ONE unchanged container path, each landing
carried the bytes that path held at the commit before it, and only the first (when the path was new)
landed the current bytes. Two data points of that shape are not a cause; they are recorded so the next
session can test the one cheap hypothesis they suggest — that a re-used outputs path is served stale —
which this session tested once, immediately, by re-landing from a FRESH container path
(`outputs/relanding/…`), guarded by the stale copy's `expectedMtimeMs` 1788680498505, to be proved at
the root listing against a `stat` taken immediately before that call. **Whatever the outcome, the
operating rule is unchanged: prove every write at the listing against the immediately-preceding
`stat`, and read the landed copy back when the sizes differ.** Both of its reading files landed the
right bytes on the first attempt, in ONE `device_commit_files` call carrying both: the extract
(never-staged path, 44,701 at the listing, equal to the `stat` taken immediately before the call) and
the progress record (staged path, guarded by the staged copy's `expectedMtimeMs` 1788678721503, 50,725
at the listing, equal to the same `stat`). **The sharpened rule of the hundred-and-twenty-third entry
was followed for both:** the `stat` of the container source was taken in the same shell call that
copied the files to the outputs directory, immediately before the commit, and the listing's sizes were
compared against THAT stat and no earlier one. **This entry's own FIRST landing followed the same
rule and landed RIGHT** — 29,487 at the root listing against a `stat` of 29,487 taken immediately
before the call, and a whole read of the staged-back copy at the user's fact-check instruction; the
corrections that read produced are in the entry's own account of itself (Errors below), not in what
landed versus what was sent. Its SECOND landing, corrected and guarded, is proved by the next session,
by its size at the root listing and a whole read.

**The running total, in the hundred-and-twenty-third entry's two-reading form:** that entry's totals
were *forty across ten sessions, thirty-one never-staged and nine staged*, or *thirty-nine, thirty and
nine*. This session adds two proven first-commit landings (the extract, never-staged; the progress
record, staged), and proves the hundred-and-twenty-third entry's SECOND landing — the one that entry
records as re-landed on its by-then-staged path, guarded by `expectedMtimeMs` 1788678880958, and leaves
to the next session — at 29,994 at the root listing, read by `Grep` on the saved listing and by a whole
read, the size the user gave as its proof (its FIRST landing, 26,386, was STALE and is counted below,
not here), and proves its own FIRST landing (never-staged, right, above): **forty-four across eleven
sessions, thirty-three never-staged and eleven staged; or forty-three, thirty-two and eleven.** **Beside either total now stand FIVE proven STALE landings — four on staged paths and one on a
never-staged path** — this session adding two (this entry's second and third landings, above). *A precision to the
line's own phrase:* the earlier three are recorded as stale FIRST-commit landings; this one is a stale
SECOND landing on a path staged minutes earlier, so the stale shape is not confined to first commits
either.

### What this session was

The session that booted on `cowork_handoff_entry_one_hundred_and_twenty_three.md`, on the user's
opening instruction to read that entry with the file tools, take its proof of landing as 29,994 bytes
at the root listing, and read what row 19 IS at `candidacy_upgrades.md`'s row 19 before staging
anything. It did both first and reported them to the user in plain text, then on his "go ahead" the
full session-start read (the tip at both ref files; `CLAUDE.md` whole, loaded by the harness;
`DECISIONS.md` whole, 862 lines; `STATUS.md` whole; the gating answer — 244 open, 219 gating, 25
non-gating, and the `gating_ids` list whole; the hundred-and-eighteenth entry's bridge-fault section),
then the reading set the hundred-and-twenty-third entry's cadence step 3 names (the progress record
whole; `candidacy_upgrades.md` whole; both commissions whole; the row 11 extract whole, for the form
and for the two carried questions; the slice derivation's row 19 line; the bibliography's row 19 line;
`FRAMEWORK.md` at the L2 charter, DP-A, DP-B and DP-C, DP-E to DP-H, DP-P and §14.1; the findings
surface's DP-C block; `population.md` §1–§3a with the V10 row), then one thing:

**Read row 19 whole at the object and extracted it.** Yang, Cwitkowitz & Duan, "Harmonic Analysis with
Neural Semi-CRF" (Harana), the held file
`docs/research_papers/yang_cwitkowitz_duan_2023_ismir_harana_neural_semi_crf.pdf` (318,293 bytes), eight
printed pages (676–683), staged through the bridge by the one matching file name in the
`docs/research_papers/` listing and read as page images with the file tools; the printed title,
authors, venue, year and licence were checked against the bibliography's row (line 33) on page 1 before
anything was extracted — **all match; NO identity finding**, the first paper of group 2 with none. The
record's own statements about the paper were read at their lines first: `candidacy_upgrades.md` row 19
(line 81); the slice derivation's row 19 (line 66); the bibliography's row; `FRAMEWORK.md` DP-C (lines
690–701) and §14.1 (lines 1008–1010), located by a `Grep` of the staged tree; the findings surface's
DP-C block; and `population.md` §3's V10 row (line 111). **The extract is the record; what it produced,
in one line each, is in the progress record and is not restated here (#6). No falsifier; no STOP.** The
paper is **CENTRAL** by the extract's own reasoning — DP-C's ablation [FACT] rests on it and is
re-verified at the object with its magnitudes; it is the candidacy row's "current candidate architecture
for L2" and the only paper in the slice that builds DP-C's chosen machinery with a learned score on
symbolic classical music; five of its findings would be cited directly by an L2 detail specification —
and **a second independent extraction is OWED**; the extract states the ground on which NOT CENTRAL
could be argued, and the verdict is challengeable at the progress record.

**Two things stated here because the next reader meets row 20 through them.** (a) Row 11's §2.3
factorization test — a segment-length term needs the segmental decode if and only if it is not a sum of
per-frame terms — now has a worked example on each side (row 11's Gaussian prior needs it; row 19's
linear self-transition term does not), and row 20's duration model is the next candidate to be classed
by it. (b) Nothing in the bibliography's row 34 or the candidacy row says whether row 20's input is
audio or symbolic; the reader establishes that at page 1 with the identity check.

**Why the session stopped after one paper.** The full boot read plus one eight-page paper with a
forty-four-kilobyte extract, plus the two opening reads and their report, is at the bound the previous
sessions found; the stop is at a member boundary, by choice, under the commissions' cadence rule, with
everything landed and proved.

### ★ THE STATE YOU INHERIT

**What in this section was read at an object this session and what was relayed, stated once:** the
tip (both ref files), `CLAUDE.md`, `DECISIONS.md`, `STATUS.md`, the gating answer, the progress record,
`candidacy_upgrades.md`, both commissions, the row 11 extract whole, the slice derivation's and the
bibliography's row 19 lines, `FRAMEWORK.md` at the sections named above, the findings surface's DP-C
block, and `population.md` §1–§3a were READ. The verdict counts, the guard-set figure, the
untracked-file list, the continuous-pruning remark and the "waiting on him" list are RELAYED from the
hundred-and-twenty-third entry, which relays them from earlier entries; this session did not open
those earlier entries beyond the hundred-and-eighteenth's opening and bridge-fault section.

- **The tip** is `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, `origin/master` equal, read at both ref
  files with the file tools (`.git/HEAD` → `refs/heads/master`); nothing ran since. **No batch is out.**
- **`VERDICTS["l2"]`** 111 IN / 133 OUT / 0 UNPLACED (relayed; not re-verified). **`WITHHELD`, `EXTRAS`,
  `FROZEN`** carry no `l2` key (relayed; `STATUS.md`'s topmost entry, read this session, says the same
  of the writeback batch).
- **The reading file** `ratification_surfaces/cowork_withheld_family_l2_reading.md` stands RULED
  (`STATUS.md`'s topmost entry, read this session).
- **Task B's L2 slice: 12 of 36 read** (rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11 and 19); **group 1
  closed, group 2 open with rows 10, 11 and 19 read.** The progress record is
  `reading_pass/l2_slice_reading_progress.md` (50,725 bytes) — read it whole before opening any paper;
  it names the next member, carries TWO things from rows 11 and 19 to row 20, and states that its row
  numbers are `candidacy_upgrades.md`'s and that `object_reads/` uses `population.md`'s numbering.
- **Untracked files of this line at writing, none landed by this side:** this entry; the
  hundred-and-twenty-third through hundred-and-ninth entries; the surface
  `cowork_l2_boot_list_surface_2026_09_05.md`; the sitting record
  `cowork_rulings_2026_09_05_l2_boot_list_sitting.md`; the two derivations of the hundred-and-eleventh
  entry; the read file `cowork_l2_score_set_read_2026_09_05.md`; the twelve extracts of rows 27, 28, 1,
  8, 3, 26, 29, 30, 5, 10, 11 and 19 and the progress record. The standing untracked historical `cc_*`
  root population is pre-existing. This side ran no enumeration.
- **The gating answer:** 244 open, 219 gating, 25 non-gating at the session-start read; nothing ran.
- **The guard set:** 77 run, 12 failing (relayed; not re-run). **`STATUS.md`'s topmost entry says
  "ELEVEN FAILING"** (read at the object this session; Ruling 9: corrected in place by the next dispatch
  that touches `STATUS.md`, former wording preserved). **The continuous-pruning bound is unmaintained** —
  standing since the hundred-and-sixth entry (relayed).
- **The plan stands at:** … → L2's boot list RULED (nine rulings) → reads 1, 2 and 3 of §11 DONE →
  **Task B's L2 slice (36 papers) read whole at the object — 12 DONE, 24 TO GO, group 1 closed, group 2
  open** — in bounded sessions closing at member boundaries, under the remedial commission §3 and the
  original commission's §4 form, central papers owing a second extraction, in the verdict table's
  proposed order ("stated, not ruled") → **the pack-build dispatch** when next (D-250) → the brief → the
  blind deriving session → L2's comparison reading. **Beside it, the L0/L1 comparison reading stands at
  7 of 29 documents tabulated** (relayed).

### ★★ THE GATE, RESTATED

No derivation may begin before L2's slice of Task B is in (eighty-seventh entry; Ruling 10 of the
2026-08-31 sitting; Ruling 1 of the 2026-09-05 sitting). **The slice is 36 papers, all held. Twelve are
read.** The gate is not lifted and cannot be until the other 24 are read, flagged unreadable, or
stop-reported.

### ★ WHAT COMES NEXT

**The next act is the next bounded reading session** — open `reading_pass/l2_slice_reading_progress.md`,
the remedial commission's §3 and the original commission's §4 form before reading anything; take the
next row's paper from `candidacy_upgrades.md`'s own row (never from a file name elsewhere), find its
held file in the `docs/research_papers/` listing, stage it by name, check its printed title, authors and
version against the bibliography's row on page 1, read it as page images, extract in the §4 form,
rewrite the progress record whole (a file this line wrote, 50,725 bytes — read it whole first), close
at a member boundary. **Row 20 is next** — this session read its table line in `candidacy_upgrades.md`
(line 82: Korzeniowski & Widmer, ISMIR 2018, combining duration and harmonic language models, held, "A
segment-duration model beside a chord language model — directly a candidate for how L2 scores segment
length, which the charter leaves to detail") and its bibliography row (line 34: "Improved Chord
Recognition by Combining Duration and Harmonic Language Models", ISMIR 2018, tier CC); the next session
reads both rows again and the slice derivation's row 20 line, and finds the held file in the listing
(the one whose name begins `korzeniowski_widmer_2018`, 384,606 bytes at this session's listing). **`population.md`
§3's verification table, read whole this session, names no Korzeniowski primary; `FRAMEWORK.md` and
the findings surface were NOT searched for the name** — the reader establishes whether the record
cites row 20 anywhere with a `Grep` of the staged tree for the authors' names before extracting, as
row 26's session did. **The two things
carried from rows 11 and 19 to row 20 are in the progress record's "Next" section.** The pack-build
dispatch is NOT next (D-250).

**Routed in the row 19 extract and the USER's to act on or to leave:** the bibliography datum (this
paper cites Masada & Bunescu's ISMIR 2017 conference form, unheld), for the bibliography
reconciliation; the four precisions to DP-C's ablation [FACT] (small magnitudes; the "no semi-CRF"
variant's decode unstated; the "no absence score" variant ahead on two columns; single runs with no
uncertainty), for the findings surface's V10 row and DP-C block; the weak-form answer to row 11's
question, for L2's detail specification beside DP-C; the counted-and-frozen transition table beside a
fitted score, beside D-525 and DP-P; the linear self-transition term as a worked example of row 11's
factorization test, beside row 20; the paper's stated argument against a product label, for the
findings surface's DP-A and DP-E blocks and the admission rule; the rule-based semi-CRF below a plain
CRNN, for the DP-C block beside D-531; the fixed frame grid and measure-aligned windows, for the L1 and
L2 detail specifications beside D-030/D-031; spelling discarded and bass carried, beside DP-F and D-449;
the pre-decode chord-tone estimate, for the DP-D block; the fit findings and the segmentation-quality
convention, for measurement design; the quadratic cost statement, beside D-004; the unpublished
posterior, beside D-006, D-008 and DP-K. Together with the earlier extracts' routed findings, none is a
STOP and none needs a ruling before row 20 is read. **Shown, not ratified.**

### The user-owed list

**Closed this session by this side, and not owed to him:** the twelfth reading session (row 19).

**Owed to him by this side:** nothing. Every finding of the extract is ROUTED in the extract itself and
applied nowhere.

**Waiting on him, CARRIED from the hundred-and-tenth entry as later entries relay it (this session read
only the hundred-and-twenty-third and the hundred-and-eighteenth's opening and bridge-fault section),
still open, unverified this session:** **(a)** whether the pilot's derived specification still carries
its independence claim after Ruling 85; **(b)** which branch of the filing convention (D-674) governs a
ruling record; **(c)** the close test's wording (close section and end-state artifact in one commit
versus one apart); **(d)** re-aiming the `expected_line` values in `gen_phase3_gate_partition.py`;
**(e)** whether the report template should carry the close-tip marker by design; **(f)** the fifth,
sixth and seventh guard observations not yet in OI-378's detail file.

**Shown to him and standing unless challenged:** the score-set read of §11 (hundred-and-twelfth
entry); the row 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11 and 19 reads. His opening instruction this
session named row 19 as the next act and his "go ahead" released the session-start read and the paper,
which is the next act proceeding, not a ratification of any read.

**Standing, unchanged and still the user's:** the list the hundred-and-ninth entry carries under this
heading, as later entries relay it — neither the hundred-and-ninth nor the hundred-and-tenth entry was
read this session. **The eleven quarantined audit questions stay reserved to the AUDIT.** **Not acted
on:** the guard-runner parallelisation suggestion; the rendering tool's "1 entries" pluralisation
defect. The hundred-and-twenty-second entry's observation about `FRAMEWORK.md`'s sealed first-stage
text at lines 1685–1686 (the former V4 wording) stands as that entry states it; this session did not
read that passage.

### Errors

**Counted on this side this session, of the family the degradation rule names: FOUR, all in this
entry's FIRST landed copy and none in the extract or the progress record, found at the user's
instruction to fact-check the handover and corrected here before the re-landing.** They were: a count
of `Grep` matches asserted without being derived ("four root files"; five matched); a claim about an
unexamined thing ("the record does not cite row 20 at any [FACT] this session found" — only
`population.md`'s verification table had been read, and nothing had been searched); a second underived
count ("two `cp`"; three); and a gloss ("every quotation … compared at the page" where the page-1
quotations were compared at the first read only). Four is past the rule's two; this close IS the
handover, at a member boundary with nothing half-done, and the four are in the entry's account of
itself, not in the reading files — checked by re-reading both at their landed copies' sizes against
the pre-commit `stat` and by the extract's own two fact-check passes. **Three near-misses, recorded
because the rule counts the tell and not the landing:** (i) the extract's first draft said the semi-CRF removal was the largest drop "on every
column but Over Seg (where all three drops are within 0.007)" — derived wrongly from the cells; the
fact check against Table 2 (p. 681) found the other two removals are small GAINS on that column, so the
semi-CRF removal is the largest loss on all six, and the sentence was corrected; (ii) the same draft's
difference table was labelled "full model minus variant" while its signs were variant minus full — a
label asserted against its own arithmetic, corrected at the same check; (iii) the progress record's
first draft of the "Next" section called row 20 "an AUDIO paper by its title", which neither the
bibliography's row nor the candidacy row supports — a claim about an unexamined thing, of exactly the
family the rule names, caught at the fact check against the bibliography before landing and replaced
by the statement that the reader establishes it at page 1. All three were caught at the object before
landing, so none is counted; they are recorded so the next reader knows the shapes recurred, and the
third is the shape the hundred-and-twenty-third entry's own near-miss had (an assertion ahead of its
derivation). The extract's fact check was run twice against the page images before landing — every
cell of Tables 1 to 4 compared with the extract's transcription, every quotation of §3.1–§3.6, §4.1–§4.4,
§5.1–§5.3 and §6 compared at the page on the re-read (pages 677–681), the Abstract and §1 quotations
(page 676) compared at the first read only, the section, equation, figure, table and footnote counts
confirmed at the pages (the section count corrected from six to eight at the first pass), and the
corrected text searched for on the second pass.

**One departure from the user's opening instruction, reported to him in the first turn:** the
instruction said to read row 19 "before staging anything", and the file tools cannot read a device
path, so the entry and `candidacy_upgrades.md` were STAGED in order to be read at all, and in the same
call `STATUS.md`, `DECISIONS.md` and the gating answer for the session-start read. The paper was not
staged until after row 19 had been read and reported. He said "go ahead".

**Errors of previous sessions found this session:** none. The hundred-and-twenty-third entry's report
of the row 19 held file's size (318,293 bytes) and name prefix is CONFIRMED at this session's listing;
its statement that `candidacy_upgrades.md` row 19 is at line 81 and the slice derivation's at line 66
is CONFIRMED at the files; its running total is carried forward as it states it.

**Declared departures of this side, this session.** (i) **A shell `cp` of the progress record's STAGED
copy into the work directory before its rewrite** — the hundred-and-twentieth, hundred-and-twenty-second
and hundred-and-twenty-third entries' declared departure, repeated here for the same reason (no
work-directory copy existed and re-typing 43,919 bytes from tool output is the transcription risk the
record forbids); the copy was byte-exact (43,919 at `stat`), then edited with the `Edit` tool in seven
places (the session note; the table row and counts; the "what row 19 produced" paragraph and the
"Next" section; the "Next" section's row 20 clause, corrected; the not-done paragraph; the provenance's
session count; the provenance's eighth-session sentence). (ii) **Three `stat` commands (one on the
progress record's copy and the extract in the work directory; one on both reading files in the outputs
directory immediately before their commit; one on this entry immediately before its commit), two
`mkdir`, and three `cp` of this side's own output files into the outputs directory (the two reading
files, then this entry)** — all in the
container on this side's OWN output files, declared because they are shell commands in the sandbox;
none touched repository content beyond the staged copy named in (i). (iii) **Five `device_commit_files`
calls:** the first carrying two files — the extract (never-staged) and the progress record (staged,
guarded) — both landed right, both proved at listings; the second carrying this entry as first written
on a never-staged path, which landed RIGHT (29,487 at the root listing, equal to the `stat` taken
immediately before it — the first time this line has proved a never-staged entry landing right on the
first attempt); the third carrying this entry corrected and RE-LANDED on the by-then-staged path,
guarded by the landed copy's `expectedMtimeMs` 1788680308181, which landed STALE (the bridge-fault
section above); the fourth carrying the same corrected entry, with this record of the stale landing
added, guarded by the stale copy's `expectedMtimeMs` 1788680443010, which landed STALE as well (the previous
version's 32,094 bytes); the fifth carrying the entry with the second stale landing recorded, from a
FRESH container path, guarded by `expectedMtimeMs` 1788680498505, to be proved at the root listing after
the call by `Grep` on the saved listing against a `stat` taken immediately before it. (iiia) **This
entry was re-staged and read back WHOLE after its first landing, fact-checked against the sources in
this session's context on the user's instruction, amended in six places, and re-landed.** The
amendments: the four corrections the Errors count above names, this departures list ((ii)'s `cp`
count, (iii) and (x)), and the Errors count itself (none → four). A fourth, fifth and sixth `stat` on this entry
precede the three re-landings, so (ii)'s count of three becomes six, its `cp` count of three becomes
six, and one more `mkdir` (the fresh path) makes three. (iv) **`Grep` through the file tools** on: the saved
root listing (twice, as stated in the practical facts above); the gating answer for its keys; the
hundred-and-eighteenth entry for its bridge-fault heading; the staged tree for "Harana", "Cwitkowitz",
"neural semi-CRF" and "ablation contributor"; `population.md` for "Harana"; `FRAMEWORK.md` for its
heading list; and the extract and the rewritten progress record for stale or corrected phrases before
landing. (v) No subagent was used. (vi) One paper was opened, whole, eight pages; pages 5–6 and pages
2–4 were re-read once each for the fact check. (vii) The handoff entries before the
hundred-and-twenty-third were NOT read except the hundred-and-eighteenth entry's opening and
bridge-fault section; the sitting record, the hundred-and-eleventh entry's derivations, the extracts of
rows 27, 28, 1, 8, 3, 26, 29, 30, 5 and 10, the V4 surface, and `FRAMEWORK.md` outside the sections named
above were NOT read. (viii) The user connected no new folder. (ix) `SendUserMessage` was used once to
put the row 19 read to the user in plain text; the two opening reads were reported as the first turn's
final response; no multiple-choice widget was used. (x) One directory listing of the repository root
was requested at the start of the session; it exceeded the cap and was saved, and the saved file was
consulted by `Grep` only (twice); a second root listing was taken for this entry's first landing proof
and consulted the same way, a third `Grep`; a third root listing was taken for the second landing and found it
STALE, a fourth `Grep`; a fourth root listing found the third landing STALE, a fifth `Grep`; a fifth root listing is taken
for the fourth landing's proof, a sixth `Grep`. (xi) The task-list widget was used to track the session's six
steps; it asked the user nothing.

**What went right and is worth copying:** the two opening reads were done and reported before the boot
so the user saw them at once, and the departure from "before staging anything" was stated in the same
turn; `candidacy_upgrades.md`, `reading_pass/extracts/` and `docs/research_papers/` were listed as
subdirectories rather than searched in the saved root listing with a shell; the paper's page 1 was
checked against the bibliography's row before extracting; the record's citations of the paper were
located by `Grep` and every one read at its lines; the row 11 extract was read WHOLE for the form, so
both carried questions were met at their source and not from a summary; the fact check re-read the
pages the values come from and caught two derivation errors and one over-assertion before landing; both
landings were proved at listings against a `stat` taken in the same shell call as the copy, immediately
before the commit; and the handover was written at a member boundary with nothing half-done.

### The cadence for your session

0. **Boot as above, in full.** This entry file is UNTRACKED and you never land it yourself; a later
   dispatch's Task 0 does. **If your first file read fails, stage the file through the bridge and read
   the staged copy; only if staging fails, ask the user to add the folder.** **List `docs/research_papers/`
   and `reading_pass/` as subdirectories; if you must consult the saved root listing, `Grep` it.
   `candidacy_upgrades.md` is in `reading_pass/`.**
1. **Re-read the tip at the ref files with the file tools.** It should be `911f5f7cda…` at both. A
   moved tip means something ran that this entry does not know of — read `cowork_away_returns.md`'s
   last section before anything else.
2. **BEFORE YOU WRITE ANYTHING, READ THE HUNDRED-AND-EIGHTEENTH ENTRY'S "THE BRIDGE FAULT" SECTION.**
   Prove this entry landed: a successful whole read of it at the object is the proof; record its size
   from the root listing in your own entry.
3. **Read `reading_pass/l2_slice_reading_progress.md` whole**, then the remedial commission's §3 and
   the original commission's §4 form, then one of the twelve extracts for the form (the row 10 extract
   is the largest; the row 19 extract is the most recent). **The next act is row 20 — read what row 20
   IS at `candidacy_upgrades.md`'s row 20 before anything else, and read that file whole while you are
   there; `Grep` the staged tree for the authors' names, because this session found no [FACT] resting
   on row 20 and the next reader confirms that rather than inherits it.**
4. **Verify at the object, not at a report; sweep the whole document when you correct a claim; run the
   fact check twice and search for the corrected text on the second pass. Locate a section reference at
   the heading list, never from where the passage was found; locate a ROW at its table, never from a
   file name. Check the paper's printed title, authors and version against the bibliography's row
   before extracting. Do not transcribe a table cell you cannot read with certainty at the image. Do
   not cite another extract's content from a summary of it — read the extract or cite nothing. Derive
   every range, count and difference you state from the cells, state the sign convention with the
   numbers, and check it once more at the fact check. Take the `stat` of the container source
   IMMEDIATELY BEFORE every `device_commit_files`, in the same shell call as the copy where possible,
   and after it LIST the directory and compare the listing's size against THAT stat; a size that
   matches an older stat, or a new modification time at the old size, is the stale shape — it happens
   on never-staged paths too — stage the landed copy back, read it, re-commit guarded by its
   `expectedMtimeMs`.**
5. **A read put to him is shown, not ratified. Do not ask him to confirm it; do not gate the next act
   on his word.**
6. **Close at a member boundary with nothing half-done**, and write the next entry before you close.
