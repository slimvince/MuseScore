# CC dispatch — `STATUS.md`'s forward bound restored BY ITS OWN MECHANISM: the backlog cleared in six late moves of `gen_status_batch_bound.py`

> **Dispatch (Cowork, 2026-09-07). This SUPERSEDES `cc_instruction_status_forward_bound_2026_09_07.md`,
> which is withdrawn.** That dispatch rested on a premise the executing side established FALSE at the
> object and correctly STOPped on: it asserted that Ruling 4's forward clause has no mechanism behind
> it, and ordered a hand-authored archive act on that ground. The mechanism exists, it is
> guard-enrolled, and it is byte-faithful. The withdrawn dispatch and the stop report
> `cc_report_status_forward_bound_2026_09_07.md` are landed by Task 0 of this one so the refutation is
> on the record and not only in a chat (#12).
>
> **★ THE ONE ACT THAT NEEDED A RULING HAS IT. The user ruled it PERMITTED on 2026-09-07 — see "The
> ratified act" immediately below. This dispatch is RELEASED.**
>
> **Written at a verified stop; nothing is running.** The tip at writing is
> `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, read at `.git/refs/heads/master` and at the branch's own
> reflog, agreeing. The working tree carries no tracked modification (relayed from the withdrawn
> dispatch's own Task 0 run, and not re-measured by the writing side — A1 orders it re-measured).
>
> **NOT in this batch, and none of it is licensed by anything here:** the row-by-row extraction (PAUSED
> by the user, 2026-09-07); the pruning pass (PAUSED, same order); any cut to `CLAUDE.md`; anything
> about `DECISIONS.md`'s preamble or the boot's mandated membership; any repair of the forward clause's
> MISSING TRIGGER, which is a question standing with the user; and any move of the two 2026-09-02
> entries, which is a separate question standing with the user and is treated at Task 1 clause 8.
>
> **Read first, in full — the ordinary session-start read (`D-230`, P-1).** `CLAUDE.md`, `DECISIONS.md`,
> `STATUS.md`, and the derived gating answer (`tools/audit/nongating_apparatus_rows.json` →
> `★_the_live_gating_answer` → `gating_ids`). **A single-file opening instruction is not an exemption
> from that read.** **Then read, in full:** `tools/audit/gen_status_batch_bound.py` — the whole file,
> including its module docstring, its authored fields and every row of `PREVIOUS_AIMINGS`, because this
> batch re-aims it seven times and the shape of a re-aiming is stated there and nowhere else;
> `cowork_rulings_2026_08_17_governing_surface_split.md` (Ruling 4 is the whole authority — open it at
> its own object, not at any relay of it, `D-643`); and `STATUS_ARCHIVE.md` at its tail, for the shape
> an append lands beside.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or run,
> no build, no measurement of the analysis, nothing under `tools/corpus/` or `tools/robust_stop/`, no
> design, no derivation of any specification, no document archived or deleted as a file, no
> decisions-register entry and no `D-NNN` allocated, and no edit to `CLAUDE.md`, `DECISIONS.md`,
> `ARCHITECTURE.md`, `FRAMEWORK.md`, any ruling record, or any governing document other than
> `STATUS.md` and `STATUS_ARCHIVE.md`. **The ONE tool-source edit permitted is the ratified act at
> Task 1 clause 0 and nothing else.** **Open-items rows: Task 2 orders exactly ONE row created; no other row
> is created, flipped or discarded.**
>
> **NO ENTRY IS RETYPED, EVER.** Every move in this batch goes through
> `gen_status_batch_bound.py --apply`, which reads each entry from a git object and matches it verbatim
> in the live file. A hand copy of an entry is a STOP, not a fallback: the entries are single lines of
> several thousand characters, and the tool's own docstring states that retyping one is the
> transcription the record forbids and that a non-byte-faithful move is what #12 exists against.
>
> **Run the FULL guard set BEFORE the first edit and again at the end; record both states.** Verify
> every commit at the object and record the SHAs in the close. The reserved-word conventions and the
> ruled vocabulary replacements bind all new prose. **Commit and push per task.**

## ★ The ratified act — Task 1 clause 0

**USER RULING, 2026-09-07: PERMITTED. The user's word was "yes".** The question was put to him in
plain terms and not in this record's vocabulary: that the script's fixed introductory sentence would
be false of a late cleanup move; that the remedy is one setting choosing between two sentences, with
the ordinary one required to render exactly as it renders today; and that the ruling was asked for
because the edit touches a guard-enrolled tool and reverses this dispatch's own bar. **The
alternative he was given, and did not take, was that the backlog is not cleared this way at all.**
Recorded here because a session may not permit itself a tool-source edit.

`ARCHIVE_HEADER` in `gen_status_batch_bound.py` renders, for every move, the sentence *"…moved
verbatim out of `STATUS.md` by `<dispatch>` <task> **in the same act that wrote this batch's own
entries**"*, and names the moved entries as *"the PREVIOUS batch's"*. **Both clauses are false of a
late catch-up move**: five of the six batches this dispatch clears are not the previous batch relative
to this one, and the six moves happen at Task 1, not in the act that writes this batch's own entries.
Landing six archive blocks whose headers misdescribe their own act is forbidden by #10, and the record
already names that exact defect once — the tool's own comment at `BASE_COMMIT` records the 2026-09-02
incomplete re-aiming as having produced *"a header that misattributes its own act"*.

**The ratified act is therefore a source edit to that one constant**, specified at Task 1 clause 0. It
adds one authored field and renders one of two sentences. **It changes no derivation, no membership
rule and no STOP**, and the ordinary sentence is required to render byte-identical to the constant as
it stands today — which Task 1 clause 0 orders PROVEN before any move runs.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 4, the state rule:** *"An entry is SUPERSEDED the moment a later batch's close exists. The
  site keeps only the latest batch's entries."*
- **Ruling 4, the forward clause:** *"The bound is maintained forward as an instance of the
  continuous-pruning rule (§5(D)): every future batch close, in the same act that writes its own
  entries, moves the then-previous batch's entries to the archive. The file stays permanently bounded
  at roughly one batch's worth of entries plus its operative text."*
- **Ruling 3, the destination:** *"`STATUS_ARCHIVE.md` continues as `STATUS.md`'s home."*
- **Ruling 1, the safeguard that binds every archive act:** *"the executing dispatch READS every span
  it archives rather than trusting its class."*

## Premise ledger

*(Every FACT below was established at the object named beside it by the writing side on 2026-09-07,
after the withdrawn dispatch's premise was refuted. No premise asserts a count of this side's own
acts; where a number appears it is derived at the object named.)*

- **FACT — the mechanism exists and is this tool.** `tools/audit/gen_status_batch_bound.py`'s module
  docstring states that the backlog was cleared by `gen_governing_surface_split.py` and that *"this
  tool is the FORWARD half"* the same ruling installs, quoting the forward clause. Established at the
  tool's source, read whole.
- **FACT — it is guard-enrolled, by the act the withdrawn dispatch named as its own precedent.**
  `tools/audit/gen_guard_state.py` carries the row
  `("tools/audit/gen_status_batch_bound.py", ["--check"], …)` under a comment naming
  `cc_instruction_preparation_sixth.md` Task 2. Established at that file's source.
- **FACT — it never authors entry text.** `moved_entries()` reads `STATUS.md` from the git object at
  `BASE_COMMIT`, derives membership from the dispatch name each entry carries plus the run of dated
  entries below it whose own text says `Same dispatch`, and `apply_move()` refuses to proceed unless
  each moved entry occurs exactly once in the live file and is absent from the archive. Established at
  the tool's source.
- **FACT — the mechanism has no trigger, which is the true and narrower defect.** `BASE_COMMIT`,
  `PREVIOUS_BATCH_DISPATCH`, `ACT_DATE`, `DISPATCH`, `TASK` and the appended `PREVIOUS_AIMINGS` row are
  authored constants, re-aimed by hand at every close. `--check` compares the committed artifact
  against `build()`, which reconciles only the aiming currently authored. **A close that never re-aims
  the tool leaves the guard GREEN and the bound UNMET.** Established at the tool's source at `main()`,
  `build()` and the authored fields. *(Observation carrying no verdict: the tool's own comments
  enumerate its authored inputs as three in one place, four in another and five in a third. This
  dispatch NAMES the fields rather than counting them, which is also the safeguard against the one
  recorded incomplete re-aiming repeating.)*
- **FACT — the omission is SIX consecutive batch closes, not seven.** Read at the branch's reflog over
  the range from the last performed move to the tip: the closes of `cc_instruction_l2_keyword_count_2026_09_04.md`,
  `cc_instruction_l2_criterion_write_2026_09_04.md`, `cc_instruction_l2_candidate_list_2026_09_05.md`,
  `cc_instruction_l2_verdict_pass_2026_09_05.md`, `cc_instruction_l2_reading_file_2026_09_05.md` and
  `cc_instruction_l2_ruling_writeback_2026_09_05.md`. The last performed move was the
  `cc_instruction_boot_pack_freeze_l0l1_2026_09_04.md` close, which is also the tool's live aiming.
- **FACT — the nine backlogged entries split two ways.** SEVEN entries, belonging to the six batches
  above, are reachable by the mechanism, each of the six dispatch names occurring exactly once in
  `STATUS.md`. TWO — the 2026-09-02 pair — name no dispatch at all and are unreachable at any aiming;
  that is a declared standing state recorded in `PREVIOUS_AIMINGS`, in the row for the third L0/L1
  comparison writing. Established at `STATUS.md` and at the tool's source. **Neither of the two other
  archive tools the record names reaches them either** — `gen_status_residue_move.py` and
  `gen_governing_surface_split.py`, each pinned to a decomposition measured in August 2026, before
  those entries existed; established at both tools' sources. *(Scope, stated so it is not read wider
  than it was checked: this is a statement about those two tools and this one, not a claim that no
  mechanism anywhere could reach them.)*
- **ASSUMPTION A1** — the working tree carries no tracked modification, and this dispatch, the
  withdrawn dispatch and the stop report are among the untracked root files. **Check ordered (Task 0,
  first act, over the whole tracked population with the sanctioned enumeration tool):** any tracked
  difference is a STOP-and-report.
- **ASSUMPTION A2 — the guard set's start state is 77 run, 12 failing.** This is RELAYED from the
  withdrawn dispatch's own Task 0 run and corroborated at an independent object, the tip's commit
  subject, which records the guard set *"at twelve"*. It is not re-measured by the writing side.
  **Check ordered (Task 0, before any edit):** run the full guard set and record what it reports. A
  start state matching neither 12 nor the superseded 11 is a STOP-and-report. **Nothing is adjusted to
  reach a number.**
- **ASSUMPTION A3** — every one of the seven entries READS, on inspection, as a completed batch's dated
  pointer entry superseded by a later batch's close (Ruling 1's safeguard). **An entry that does not is
  LEFT AT SITE, flagged, and enumerated in the close** — the doubt default, not a batch STOP; the act
  proceeds over the remainder. Reading an entry before it moves is ordered at Task 1 clause 2 and is
  not discharged by the tool's byte-faithfulness.
- **ASSUMPTION A4** — at the Task 0 commit, `STATUS.md` is byte-identical to the live file and carries
  ten dated entries. **Check ordered (Task 1 clause 1):** compare the git object at the Task 0 commit
  against the live file; any difference is a STOP.

## Task 0 — land the record, and measure the start state

1. **A1's check first**, over the whole tracked population with the sanctioned enumeration tool.
2. **A2's check:** run the full guard set and read it. Record the state; do not act on it.
3. **Commit, in ONE commit of three paths:** this dispatch; the withdrawn
   `cc_instruction_status_forward_bound_2026_09_07.md`; and the stop report
   `cc_report_status_forward_bound_2026_09_07.md`. Land this line's current handoff entry too if it is
   not yet landed. Push; verify at the object. **Record this commit's SHA — every catch-up move at
   Task 1 uses it as `BASE_COMMIT`.**

**Registered expectation E0:** A1 holds as stated; the guard state is recorded as measured and is 12,
or is reported as matching neither reading.

## Task 1 — the seven entries archived by the mechanism, in six late moves

### Clause 0 — the ratified source edit (user ruling, 2026-09-07)

In `tools/audit/gen_status_batch_bound.py`, add ONE authored field immediately after `TASK`, beside the
other authored fields:

```python
# ★ WHAT KIND OF MOVE THIS AIMING PERFORMS. Two values and no others.
#   "ordinary"  — the move Ruling 4's forward clause describes: the then-previous batch's entries,
#                 moved in the same act that writes this batch's own entries.
#   "catch-up"  — a LATE move: the close that owed this move omitted it, and this act performs it
#                 now. The then-previous-batch language is false of such a move, so the header says
#                 what actually happened instead (#10).
MOVE_KIND = "ordinary"
```

and replace the header constant, at the place it already stands, with:

```python
HEADER_ORDINARY = (
    f"The entries below are the PREVIOUS batch's (`{PREVIOUS_BATCH_DISPATCH}`), moved verbatim out "
    f"of `STATUS.md` by `{DISPATCH}` {TASK} in the same act that wrote this batch's own entries"
)
HEADER_CATCH_UP = (
    f"The entries below belong to the batch `{PREVIOUS_BATCH_DISPATCH}`, moved verbatim out of "
    f"`STATUS.md` by `{DISPATCH}` {TASK} as a LATE move: the close that owed this move omitted it, "
    f"and this act performs it now"
)
# A MOVE_KIND that is neither key raises KeyError HERE, at import, before any move can run. The
# lookup is deliberate and is not replaced by a conditional expression: a conditional would render
# the catch-up sentence for a typo, which is the silent-failure direction #19 exists against. It is
# also not a `Stop`, because `class Stop` is defined BELOW this constant and is not in scope here.
ARCHIVE_HEADER = (
    f"> **★ RULING 4's FORWARD BOUND, {ACT_DATE}.** "
    f"{ {'ordinary': HEADER_ORDINARY, 'catch-up': HEADER_CATCH_UP}[MOVE_KIND] } — Ruling 4 of "
    f"`{RULINGS}`: *an entry is SUPERSEDED the moment a later batch's close exists, and the site "
    f"keeps only the latest batch's entries.* Nothing was edited in transit; the reconciliation is "
    f"re-derived by `tools/audit/gen_status_batch_bound.py --check`.\n\n"
)
```

Also add `"the_kind_of_move": MOVE_KIND` to the dictionary `build()` returns, immediately after
`"the_then_previous_batch"`, so the artifact records which kind of move it reconciles.

**PROVE the ordinary branch is byte-unchanged before any move runs.** With `MOVE_KIND = "ordinary"`
and the fields at their current aiming, render `ARCHIVE_HEADER` and compare it character for character
against the constant as it stands at the Task 0 commit's git object. **A difference of any kind is a
STOP.** Record the comparison in the report.

**What this clause does NOT do:** it changes no derivation, no membership rule, no STOP condition and
no value in the artifact other than the added field. It is not a mechanism change under `D-657`: the
population of moves it reaches is every future move, and it leaves every ordinary one rendering exactly
what it renders today, which is the proof required above.

**The ruling permits this edit and nothing beyond it.** No move is performed by hand as a substitute
for the tool, and no header is edited after the tool writes it. If the byte-identity proof above
fails, Task 1 STOPs and the edit is reverted — the ruling licenses the change described here, not
whatever the change turns out to be.

### Clauses 1 to 10 — the moves

1. **A4's check.** `git show <Task-0-SHA>:STATUS.md` against the live file. Any difference is a STOP.
2. **Read each of the seven entries before it moves** (Ruling 1's safeguard, A3). Record one reading
   verdict per entry in the report. An entry that does not read as a completed batch's dated record
   stays at site and is flagged.
3. **Six moves, in this order — oldest omission first, so the archive reads in the order the moves
   would have happened had each close performed its own.** For each move, re-aim these fields and no
   others, then run `python tools/audit/gen_status_batch_bound.py --apply`:

   | # | `PREVIOUS_BATCH_DISPATCH` | entries expected |
   |---|---|---|
   | 1 | `cc_instruction_boot_pack_freeze_l0l1_2026_09_04.md` | **2** — the entry naming it, plus the entry below it saying `Same dispatch` |
   | 2 | `cc_instruction_l2_keyword_count_2026_09_04.md` | 1 |
   | 3 | `cc_instruction_l2_criterion_write_2026_09_04.md` | 1 |
   | 4 | `cc_instruction_l2_candidate_list_2026_09_05.md` | 1 |
   | 5 | `cc_instruction_l2_verdict_pass_2026_09_05.md` | 1 |
   | 6 | `cc_instruction_l2_reading_file_2026_09_05.md` | 1 |

   For every one of the six: `BASE_COMMIT` = the Task 0 commit SHA; `ACT_DATE` = the day the move
   actually runs; `DISPATCH` = this dispatch; `TASK` = `"Task 1"`; `MOVE_KIND` = `"catch-up"`; and ONE
   row appended to `PREVIOUS_AIMINGS` naming the executing act, the base commit, the then-previous
   batch and the kind. **Every one of those fields moves at every move. A move that leaves any of them
   naming a previous aiming is the recorded 2026-09-02 defect repeating and is a STOP.**
4. **A moved count that differs from the table is a STOP**, not an adjustment. So is any `Stop` the
   tool raises.
5. **The one declared prefix adjustment is expected NOT to fire on any of the seven**, because the
   `Last updated:` prefix sits on the entry that stays. Report
   `the_one_declared_adjustment_applied` for every moved entry; **a true value anywhere is a STOP**,
   because it would mean the file is not shaped as this dispatch reads it.
6. **Run the FULL guard set at the edited tree, before the Task 1 commit.** A guard that HALTS on the
   new shape of `STATUS.md` is a STOP, not an improvised tool edit; a guard that merely goes RED is
   reported with its cause established at the object, not adjusted. Note beside it, as a fact about
   what the guards prove: `gen_status_batch_bound.py --check` reconciles only the LAST of the six
   moves, so it is not the proof that the backlog cleared — E1's own arithmetic is.
7. **ONE dated pointer** left in `STATUS.md`'s body naming this clearing act and the standing bound —
   one pointer for the whole catch-up, on the precedent shape, not one per move.
8. **The two 2026-09-02 entries STAY, and the close says so in terms.** They name no dispatch, so no
   aiming of this tool can identify them, and neither `gen_status_residue_move.py` nor
   `gen_governing_surface_split.py` reaches them either. **They are not moved by hand and they are not
   moved at all in this batch.** Enumerate them in the close with the tool's own recorded reason.
   Whether they ever move is a question standing with the user and is not this batch's.
9. **Run the standing self-check on the tool-source edit before reporting it** — read the actual diff
   of `gen_status_batch_bound.py` and check it against the guiding principles, the conventions and
   `DEFECT_TYPES.md`, as `CLAUDE.md`'s self-check section requires of every coding exercise. Record its
   output.
10. ONE commit for the whole of Task 1, push, verify at the object.

**Registered expectation E1:** seven entries moved; each byte-present in `STATUS_ARCHIVE.md` exactly
once and byte-absent from `STATUS.md`; moved plus stayed accounts for the Task 0 commit's `STATUS.md`
to the character, modulo the one added pointer; **`STATUS.md` ends Task 1 carrying THREE dated entries
— the ruling-write-back batch's, and the two 2026-09-02 entries** (not one: the withdrawn dispatch said
one and was wrong, because it treated the unreachable pair as movable); A3's left-at-site flags
enumerated, zero being a valid count; the ordinary-branch byte-identity proof recorded.

## Task 2 — the row, worded to what is established

1. **ONE open-items row created, and it is the only row act permitted.** Its subject, stated so that
   every clause is established at an object this dispatch names:

   > **Ruling 4's forward bound has a mechanism and the mechanism has no trigger.**
   > `tools/audit/gen_status_batch_bound.py` performs the move byte-faithfully, but it is re-aimed by
   > hand at its authored fields — `BASE_COMMIT`, `PREVIOUS_BATCH_DISPATCH`, `ACT_DATE`, `DISPATCH`,
   > `TASK` and an appended `PREVIOUS_AIMINGS` row — at every batch close, and its `--check` guard
   > reconciles only the aiming currently authored. A close that never re-aims it leaves the guard
   > green and the bound unmet. That happened at six consecutive closes, and the backlog was cleared
   > late by `cc_instruction_status_forward_bound_second_2026_09_07.md` Task 1.

   **The row asserts no cause beyond what is read at the tool's source, proposes NO REMEDY, carries no
   gating verdict and allocates no finding number.** Its detail file is created in the same commit
   (register rule (c)), and relays the six omitting closes by the dispatch each names, never by line
   number (`D-307`).
2. **The derived gating answer is REGENERATED, never hand-edited**, the open-row population having
   grown by one — the settled treatment at the batch that created OI-378. Its check goes FAIL-then-PASS;
   the index lint and the split reconciliation both run across it.
3. ONE commit, push, verify at the object.

**Registered expectation E2:** the row states only what this dispatch's premise ledger establishes at
objects; the regenerated gating answer's check passes and the new row falls in the ruled default cut.

## Task 3 — the close, which performs the forward clause on itself

One `STATUS.md` pointer entry per task — **and, per Ruling 4's forward clause, the SAME act moves the
ruling-write-back batch's entry to `STATUS_ARCHIVE.md`**. That is a SEVENTH re-aiming and it is an
`"ordinary"` move, not a catch-up: `PREVIOUS_BATCH_DISPATCH` = `cc_instruction_l2_ruling_writeback_2026_09_05.md`,
`BASE_COMMIT` = this batch's last task commit pushed before the close began, `DISPATCH` = this
dispatch, `TASK` = `"Task 3"`, `MOVE_KIND` = `"ordinary"`, one `PREVIOUS_AIMINGS` row appended.

**★ THE ORDER INSIDE TASK 3 IS FIXED AND IS NOT A PREFERENCE. Write this batch's own dated entries
into `STATUS.md` FIRST, then run `--apply`.** The reason is mechanical: `PREFIX_ADJUSTMENT` is the
string `Last updated: `, `moved_entries()` strips it from the text read at the base commit, and
`apply_move()` then requires that stripped text to occur exactly once in the LIVE file. The
ruling-write-back entry still carries that prefix until this batch's own entries are written above it
and the prefix moves to the newest of those. Running `--apply` before writing them leaves the prefix
where it is, the stripped text matches nothing, and the tool STOPs. **The declared prefix adjustment IS
therefore expected to fire on this move, and a report of `false` here is a STOP.**

The report `cc_report_status_forward_bound_second_2026_09_07.md`: both guard states, every SHA, every
expectation graded, the per-file arithmetic to the character, one reading verdict per entry, the
ordinary-branch byte-identity proof, every A3 flag, and every declared departure reported rather than
absorbed. **If `STATUS.md`'s size change stales the read-size measurement, regenerate it in the
end-state commit** — the settled practice of the preceding batches — and say so.

**Registered expectation E3:** at the tree carrying the close, a fresh full guard run re-derives, run
and read rather than inferred, its values committed only after the run; the failing count is reported
as measured against A2's recorded start state, with any addition traced to this batch's own ordered
acts; and `STATUS.md` ends holding this batch's own entries plus the two 2026-09-02 entries and nothing
else.

## What this batch does NOT do

- **It does not repair the missing trigger.** It clears the backlog by the mechanism, rows the true
  defect, and leaves the repair to the user. A seventh omission is not prevented by anything here.
- **It does not move the two 2026-09-02 entries, by hand or otherwise**, and takes no position on
  whether they should ever move.
- **It retypes no entry and hand-copies no entry.** Every move is the tool's.
- **It changes no derivation, membership rule or STOP in the tool.** The one permitted source edit is
  the held header act, whose ordinary branch must be proven byte-unchanged.
- **It touches nothing about the token question:** no re-measurement of the boot, no cut to any boot
  file, no change to the session-start read.
- **It opens no paper, no extract, no progress record and no companion; it runs no sweep; it moves no
  verdict; and it lifts no gate.** The reading and the pruning stay paused.
