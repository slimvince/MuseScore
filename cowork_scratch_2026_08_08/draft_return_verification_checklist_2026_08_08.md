# DRAFT — the return-STOP verification checklist for the away batch

> **COWORK SCRATCH, 2026-08-08 — EXECUTED 2026-08-09 for the completed scope.** The away
> batch stopped at a clean boundary after Tasks 0–2; the batch-wide checks and the T0/T1/T2
> per-task checks below were executed at that HEAD (returns file end to end; STATUS pointer
> entries confirmed; commits `82ebfd68d9` / `d286aaf32e` / `dfbf3ab824` shown by explicit
> hash and each an ancestor of `origin/master`; archives untouched in both homing commits;
> the T1 establishment artifact read against every claim; the class-2 population at exactly
> the three held entries; guard summary at three standing failures). **T3–T8's checks are
> UNEXECUTED — those tasks were not started.** One wording defect found and reported (the
> returns-file Task-2 heading's "nine of the eleven"; the state is eight of the eleven plus
> D-601 = nine homings). Built from the away plan + dispatch, the audit protocol's dispatch
> rules, and the standing verify-at-objects discipline. File tools only; git object queries
> by explicit hash.

## Batch-wide, first

1. Read `cowork_away_returns.md` END TO END before anything else (holds, surfacings, STOP
   notes, per-task completion notes, the batch summary).
2. `STATUS.md` pointer entries: one per completed task, each naming the dispatch by exact name.
3. Commit chain: one commit per completed task, each provenance-stamped; verify by
   `git show --stat <hash>` per reported hash; push state to `origin` only (report claims vs
   `git merge-base --is-ancestor` checks); on any push failure: reported verbatim, nothing
   worked around.
4. Live guard battery at HEAD: register `--check`, disposition `--verify` (all verbatims at
   zero drift), `reaim_home_anchors.py --check`, `open_items_split_check.py`, `--producible`,
   `gen_guard_state` summary vs the last report — failing set explained item by item.
5. Every dispatch/report self-check section present (its absence is a process-check failure —
   the writing-side rule).
6. D-431 sweep of the reports: no transcribed figures; counts derived from state not lists.

## Per task

**T0 (OI-305/OI-350):** the phase-1q artifact epoch-declared, its snapshot untouched
(hash-compare); apply run; `gen_home_classification --check` green; the six generators run;
route (i)'s eleven entries derived contract-home; the four stale artifacts regenerated and
their movement accounted (27 re-homes + D-472 at Layer 6 + route (i) closed); OI-305/OI-350
flipped; OI-319 scope note. A1 field-diff claim spot-checked.
**T1 (guard family):** corpus extended FIRST (order visible in the artifact); blindness
measured at unwidened guard AND live hook (OI-351's cause established); five design parts
each present; both rates on the extended corpus; false-deny condition; POSIX verdicts unmoved
(A5); OI-348 flip; OI-300 retirement or named residuals; OI-351 flip-or-held. Re-run
`--establish --check` live myself.
**T2 (archive eleven + D-601):** per-entry homes read at their sections; archives byte-
untouched (#12 — spot-check two); D-601 in the confidence contract's frame table, the one-edit
scope held (that file's diff is ONE region); register fields preserved-former.
**T3 (defense gaps):** per-entry — defense from the record WITH citation, or "derivation not
recorded"; spot-read 5 entries' cited sources to confirm the defense is really there (the
never-from-memory rule is the thing to verify); helds have reasons.
**T4 (reach derivation):** ONE generated artifact, per-row reasons, rows moved where it puts
them; no hand verdicts.
**T5 (17 re-homes):** as T2's discipline; the D-469/D-625-style STOPs honored where fired.
**T6 (gating rows):** each flip's correction read at the document; helds have reasons; the
TRUE-half population re-derived at HEAD.
**T7 (probe):** A4 — the task's diff empty except the probe artifact; the finding's citations
read at the code; if NOT-SATISFIED: it is analysis-bearing — goes on the return surface as a
#13 item, nothing built on it.
**T8 (marks):** §6 marks each naming a validating corpus; OI-346 flip-or-remainder.

## Then, the ⟲ re-derivations for my scratch drafts

Finish line counted (all items); the disposition residual (now current post-adoption — the
figure the register's template fills); the phase-3 gate partition verdicts (any widening = #13
STOP by its own terms); the family rows' dated notes (T7's probe may have annotated OI-349);
the OI-245…OI-257 census rows' states (channel-7 table row 10); OI-207's channel-9 residual
face.

## Then the landing and the surface

Land the seven scratch drafts into the repository at the STOP (as cowork_* documents with
their DRAFT banners adjusted to reading-surface status); write the consolidated return
surface: batch outcome, holds needing rulings, the probe finding, the phase-2 program with
its five decisions, the OI-179 commissioning, and the finish line's remaining distance to the
completion statement.
