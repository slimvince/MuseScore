# DRAFT — grounding digest: the audit protocol and the arc plan, read in full 2026-08-08

> **COWORK READING SURFACE — landed at the return STOP 2026-08-09; a reading aid, NOT ratified.** Both documents read in full this
> session (`cowork_audit_protocol.md` through its dispatch-protocol rules;
> `cowork_engage_arc_plan.md` all 186 lines). This digest is a reading aid, never a source —
> cite the documents, not this.

## The audit protocol — what binds every audit and every dispatch

**P1–P10:** machine-generated total inventory, never search (P1); closed verdict set, "no
issue" is a recorded claim (P2); spec→code negative-space direction mandatory (P3); behavioral
fire-rate characterization per branch (P4); blinded second pass, adversarial, withheld
findings NEVER in a mandatory session-start read (P5/OI-222); the audit's own measured
residual-error rate — completeness is a number or the audit certifies nothing (P6); the defect
catalog with mechanical signatures, every new type gets an entry same-commit (P7); blind pass
FIRST, signature sweep second (P8); retiring code gets NO audit, only the #12 check at
deletion — and this does NOT shield the joint estimator (P9); audits are owed per layer when
its pieces are in place (P10).

**The dispatch protocol (the section that binds our working rhythm):**
- Cowork writes instructions, CC executes, never the reverse; Cowork never edits `src/`,
  builds, or spawns build agents (two breakages on record).
- **Dispatches written only when next; a parked instruction carries a `⏸ PARKED` banner and is
  REVALIDATED against then-current STATUS.md and HEAD with a dated dispatch note before
  execution.** (The away plan file conforms by its PLAN re-banner; the executing dispatch is
  written at the STOP.)
- No mid-flight steering, ever; instructions are self-sufficient; the only mid-run channel is
  the executing session's own STOP question.
- **D-431** with its five measured instances: figures AND premises by citation to primary
  sources; binds dispatches and reports both.
- Counts of outstanding work DERIVED from state at HEAD, never from a list of asks (the
  write-list instance; OI-335).
- The writing side runs the standing self-check before release; its five-item checklist
  (principles; conventions incl. music-word discipline; figures/premises; file-tools rule;
  #24 uncertainty on comparisons); absence is a process-check failure.
- **D-436**: a mechanism is judged on three measured conditions (automatic, measured
  detection, ~zero false-positive); failure is REPORTED, keep-or-remove is the user's.
- **D-641** sort + the #19 exception; **D-642** (C1 reaches live content; superseded →
  successor); **D-643** (full-quote); **D-644** (removal shape); **D-645** (homing licence).

## The arc plan — the ratified order of work to the precision phase

Stages: 1 pre-L5 refactoring (DELIVERED); 2 L5 engagement design (COMPLETE — carry contract,
selection-by-joint-consistency, pedal as reader over the carry, F-B demoted to annotation);
3 algorithmic completion / E4 behind the ENTRY GATE EG-1…EG-7 (Tier-1 defusal prerequisite;
go/no-go under full #17/#19; pedal hard-gated on owed-P1; confidence-scale ledger before any
θ/kBoundary fit; param-manifest extension; Jazz validation honesty; dependency-ordered audit
certification — all still-open rows OI-1…OI-7); 4 the R9 file split LAST; 5 precision (#4),
moratorium lifts only then.

**MEASURE-BEFORE-BUILD** is the funnel's middle stage: desk-simulate → read-only probe →
build; precision claims measured before built; byte-identical refactors exempt (byte-identity
IS the prediction). The joint key↔chord re-ranking SHELVING is LEGACY-scoped by D-278's
annotation — it does NOT bear on the joint estimator (different mechanism class). Beside the
gate: D-539's structural/fitted/ceiling decomposition (what the measurement is OF) and
D-568's two-axes medicine (chord hand-buildable competition rules; key soft-evidence quality +
calibration; NEITHER a wider search) — both confirmed homed in place 2026-08-07, both
work-programme statements for Stage 5.

**Return-day relevance:** the away batch's verification checklist comes straight from the
dispatch protocol (self-check sections present per task; D-431 compliance; counts derived not
listed; the PARKED rule if any instruction file sat idle); and the phase-2/phase-3 surfaces I
drafted must respect EG-1…EG-7 as the STILL-STANDING Stage-3 entry gate — phase 1-2-3 (D-231)
sequences knowledge work, but the arc plan's gates sequence the BUILD, and both hold at once.
