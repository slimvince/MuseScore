# DRAFT — the phase-2 program: the decision surface for the user's return

> **STATUS: COWORK READING SURFACE — landed at the return STOP 2026-08-09, ⟲ markers
> re-derived at that HEAD. NOT ratified, NOT a dispatch.** Purpose: when phase 1's finish line
> closes, phase 2 ("issue-finding is EXHAUSTED with measured coverage") opens, and its shape
> needs your rulings. This drafts that surface so it costs zero cycles then.
>
> Sources: `CLAUDE.md`'s phase-2 clause (D-231); `cowork_oi200_perspective_inventory.md` §4
> (the ONE ratified home of the channel enumeration, D-439, read this session) and its
> 2026-08-03 reach ruling; the phase-3 gate partition ⟲ (regenerable; re-derive at the STOP).

## What phase 2 IS, by its clause

The remaining audit partitions and the blind second pass with its seeded error rate, PLUS the
enumerated discovery channels — each search reporting its DETECTION POWER, ending in the
BOUNDED TRUST STATEMENT: every channel enumerated, every miss rate measured, every finding
rowed. (#19 applied to the search itself: a search that cannot state what it would have missed
establishes nothing.)

## The channel set the clause reaches (ruled 2026-08-03)

IN: channels 1 (population variation), 2 (oracle multiplication), 3 (invariant/metamorphic),
5 (residual decomposition), 6 (public-research concept comparison), 7 (requirement-side
enumeration), 9 (history mining — completing the OI-207 adjudication. ⟲ re-derived 2026-08-09: the READS
face is CLOSED — the owed set is empty, 102 documents read in full — and what the channel
still owes is the RESIDUAL face alone: 2,637 unresolved clusters, publishable as current
because the adopted disposition layer's backbone and the register are equal at 645; the
values live at `tools/audit/decisions/disposition_manifest.json` →
`disposition_counts.unresolved` and `DECISIONS.md`'s "The remainder, measured").
ALREADY REACHED by the clause's own words: 4 (prediction-first — an obligation riding every
probe), 8 (fresh-reader passes — the OI-199 partitions and pass 2 with seeded error rate).
NOT a discovery channel: 10 (defect-signature sweeps — inventory/regression; every finding
from 1–9 asks "new defect TYPE?" and feeds the catalog).

## The rulings phase 2 will need from you (the actual surface, to finalize at the STOP)

1. **Order.** A cost/yield-motivated default to propose: 3 (invariants — cheapest, mechanical,
   each HOLD becomes a permanent regression guard; transposition/octave/time-stretch/part-order/
   no-information-loss) → 7 (requirement-side enumeration — cheap reading, historically high
   yield: OI-209's shape) → 9 (history-mining completion) → 5 (residual decomposition — NOTE:
   its own text bounds it by the OI-179 ceiling; see ruling 3) → 6 (concept inventory — bounded
   literature pass under the theory-grounding corollary's fetch rules) → 1 (population
   variation — needs corpus acquisition; research-tier on entry, gate unmoved) → 2 (oracle
   multiplication — external systems; adoption questions none). The already-scheduled OI-199
   partitions (channel 8) interleave as CC capacity allows.
2. **Scope per channel** — particularly channel 1's population list (the OI-38 late-romantic
   expansion; the notation-feature census pieces; the synthetic extreme textures) and channel
   2's external-system list. Each probe carries written bands (#17b) before measuring;
   surprises are the intended product here (explorational scope), but each is still rowed.
3. **OI-179 — the ground-truth ceiling measurement.** #21's ★ clause: it CANNOT be cited from
   the literature; measuring it here is the only route, and channel 5's decomposition and every
   "irreducible residual" claim are bounded by it. It is its own commissioning decision
   (annotator time, corpus slice, per-axis agreement design) and arguably phase 2's critical
   path. A surface for HOW to measure it is not yet drafted — flagged as the one large
   unprepared piece.
4. **Detection-power reporting** — the form each channel's miss-rate statement takes (seeded
   errors where seeding is possible; stated structural limits where not), so §5's arithmetic
   has inputs and the bounded trust statement can be written at the end.
5. **The struck-versus-sounding gate interaction.** The phase-3 qualification: the family
   design waits only on phase-2 items whose search space could contain a family fact —
   admission or what-the-model-reads. Channels 1 and 3 plausibly can (new populations, new
   invariance violations); the partition's per-item verdicts are the record — ⟲ re-derived
   2026-08-09: 17 items, 14 GATING, 3 NON-GATING, no classified item has run, no widening;
   any future widening is a #13 STOP by the partition's own terms.

## What phase 2 explicitly does NOT open

No fix design (that is phase 3, ONE prioritized plan over the complete list); no inference
change; the estimator funnel stays shut. Findings are rowed, not acted on.
