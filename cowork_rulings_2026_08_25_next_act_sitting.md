# Rulings — the next-act sitting, 2026-08-25 — THE NEXT ACT IS THE RE-RUN, AND THIS SESSION CLOSES

> **STATUS: RULING RECORD.** Cowork, 2026-08-25 (the forty-fifth Cowork session, its sixteenth and
> **final** sitting). An interim carrier under the standing clause that a sitting record is written
> in the turn its ruling is given and lands in git at the next dispatch's Task 0.
>
> **Taken at branch tip `0f18b358bc6a8da5ec6064760d675129e64d8f3b`** — the cascade-sweep batch's
> second commit — **after that batch's return was verified at the objects by this session.**
>
> **★ THIS RECORD ORDERS NO DISPATCH.** Nothing in it is a repository act. **It closes the session.**

---

## 0. The return, verified at the objects

**Verified, not relayed.** `428b44143db6e3eeb6f052ad2216cfd63bd01e9a` carries **exactly nine**
paths; `0f18b358bc6a8da5ec6064760d675129e64d8f3b` carries **exactly three**, its parent the first.
**Both writing-side files are byte-identical at the committed blobs.** The `OI-377` row splits into
**six** cells and occurs **once**. The final guard summary reads **`{run 75, passing 74, failing 1}`**
— **the standing red alone.** `gating_ids` carries **218** identities including both `OI-376` and
`OI-377`; `non_gating_rows` is unchanged at **25**. **No `.py` path appears in either commit**, and
`216` still stands at lines 34, 140 and 298 of `gen_session_start_read_size.py` — **deliberately not
updated, as ruled.**

**★ THE SWEEP ANSWERED THE QUESTION IT WAS BUILT TO ANSWER, AND THE ANSWER EXCEEDS THE LEAD.**
Opening ONE register row turned **three** artifacts red — all in the **same round** — and the chain
had a member the lead did not name: **`tools/audit/evidence_pin_membership.json`**. **None of the ten
generators named in the lead went red.** Then **two further artifacts moved without ever appearing in
a failing set**, because two guard-set members run in **living mode** and write on every run:
`tools/open_items_split_check.py` rewrites `open_items/register_check.json`, and
`gen_guard_state.py` writes its own artifact.

**★ THE MEASURED BLAST RADIUS OF ONE REGISTER ROW IS FIVE ARTIFACTS, AND TWO OF THEM ARE INVISIBLE
TO ANY INSTRUCTION PHRASED AS "REGENERATE ONLY WHAT GOES RED."** It cost ONE batch to learn instead
of five. **The consequence is standing and belongs to every later sitting: Ruling 9 routes an
apparatus finding to a row, and #10's worth test decides which findings get there at all — THAT TEST
IS NO LONGER FREE, and it should be applied knowing the price.**

**The batch's other findings, judged.** Its refusal to open a row for the shell-dependent captured
text was **right** — [[OI-374]] already owns that subject. Its decision to commit the two
living-mode artifacts under Task 4's *"every artifact the sweep regenerated"* clause, and to name
why, was **right**. **Nothing in its account was found wrong at the objects.**

## 1. Ruling 1 — the next act is THE RE-RUN of the held-out test (Alternative A)

**Ruled.** The next act is the **re-run**, which is **the user's to open**: a fresh Cowork session on
a configuration chosen to be clean, on the **same unit (harmony-boundary)**, with the ruled brief,
the ruled pack, the ruled withheld family and the oracle **unchanged**, and with the one added bound
of Ruling 2 of `cowork_rulings_2026_08_25_method_voiding_sitting.md` — **before it reads anything it
reports what stands in its context about this repository, and STOPS if `CLAUDE.md` or any repository
file arrived.**

**Why, toward the objective:** it is **the only act that moves the method from UNTESTED toward
established or refuted**, and every remaining phase opens on that; and it buys the **paired
contaminated/clean derivation of one subject**, obtainable no other way and lost if the pack or
brief ever drift. **Counting against it:** if the clean boot does not hold there is no derivation —
but the session stops at its first act, before deriving, so almost nothing is spent. **Toward the
principles:** just-in-time; **not self-generated** — a re-open, not a re-design; and it discharges
**#19** by replacing *probably held* with a test.

**★ THE LEDGER (Alternative B) IS AVAILABLE IN PARALLEL AND IS NOT A RIVAL.** The re-run is a Cowork
session the user opens; the ledger is a CC batch. **They contend for nothing, and the re-run needs
nothing from this side while it runs.** The ledger remains **owed before the framework phase**
(Ruling 8 of 2026-08-21, a declared hole hand-applied per admission) whatever the method's fate.

**★ ALTERNATIVE C REMAINS OWED AND COMPETES WITH NEITHER** — whether the format test **(b)**, which
judges the FORM of a six-field statement rather than its content, survives the contamination. **It
unholds nothing either way**, and it is taken up whenever the user calls for it.

**Alternative D — hold — declined**, and the ground is recorded because it is the trap: **a plan can
pass every process rule while going nowhere.** The method staying untested indefinitely is the one
state the objective cannot tolerate.

## 2. Ruling 2 — the two riders are ROUTED, not surfaced, with a NAMED CARRIER

**★ A SURFACE THIS SIDE PUT AND THEN WITHDREW, RECORDED BECAUSE THE ERROR IS INSTRUCTIVE.** This
side built a four-alternative decision surface around what to do with the batch's two small findings
— a mis-citation in a narrative file and an observation belonging to an already-open row. **That was
reactive to the latest return with no reference to the larger plan, it rated nothing against the
ultimate objective or the guiding principles, and by making two line-edits into a governance event
it self-generated work for both sides.** The user rejected it. **It is withdrawn; these are routing,
not decisions.**

**Ruled, and carried as riders on whichever dispatch comes next, at its Task 0:**

- **(a)** `open_items/OI-376.md` line 99 still reads *"([[OI-319]], [[OI-336]], **D-436**)"* — the
  same sentence the INDEX row was corrected away from at `744ed4a708`. **Corrected in place to
  D-438, with an inline note**, exactly as the INDEX row was. *This side corrected the INDEX and
  never considered the detail file carrying the same words; the oversight is this side's.*
- **(b)** `open_items/OI-374.md` gains the cascade-sweep batch's observation: **the variable is not
  only the interpreter's output encoding but the LAUNCHING SHELL**, and it can move the committed
  bytes of a guard artifact **between two commits of the same batch**. No verdict moved; both runs
  reported `OVERALL PASS`; encoding touches captured text and never an exit code. **The row's status
  is NOT flipped and no new row is opened** — [[OI-374]] already owns the subject.

**Neither touches the INDEX, so neither cascades.** **The carrier is a REGISTERED EXPECTATION of the
next dispatch, not a note** — this project's deferred list has a poor record of draining, and a rider
without a named carrier joins it.

## 3. Ruling 3 — this session CLOSES, on nine counted signals

**Ruled, on the user's question and this side's own tally.** **Nine distinct signals**, and **the
shape matters more than the count**:

**Content — each caught by an object or by CC:** (1) **A1 asserted rather than measured, three
times**, the third after the rule ordering it measured was already standing; (2) **a ruling citation
put to the user from memory and wrong in both halves** ("Ruling 9(b)", "F89"), against the founding
never-work-from-memory rule; (3) **a dispatch that ordered a command this repository's own guard
forbids** (`git status`, D-253); (4) **a self-referential hash** written into the instruction
carrying it — a logical impossibility; (5) **A5 half-reasoned**; (6) **the F1 oversight**.

**Form — which nothing but the user catches:** (7) **the decision-surface rule violated twice**, the
second time in §2 above, on a rule the user had personally sharpened; (8) **"against" used
ambiguously** in the very surface written to repair (7); (9) **a stray CJK character** in a ruling
record authored by this side — a token-level artifact, a different class of signal from the rest.

**★ THE DIAGNOSIS, STATED SO THE SUCCESSOR INHERITS IT RATHER THAN REPEATING IT: the errors have
MIGRATED FROM CONTENT, WHERE THE OBJECTS CATCH THEM, TO FORM, WHERE NOTHING CATCHES THEM BUT THE
USER — and the governance form is the LAST thing that should degrade, because it is what catches
everything else.** Signals 7, 8 and 9 all fall in the last few cycles. Add one context compaction
already behind this session and sixteen sittings within it.

**The timing is clean and that is part of the ruling:** the ruling is given, **everything is
landed**, the guard set stands at its one standing red, **nothing is dispatched and no batch is in
flight**, and the act ruled at §1 is a session the user opens that needs nothing from this side.
**A handoff here carries no work in progress.**

## 4. What is HELD, and what this sitting does NOT do

**HELD:** the method ruling stands **VOIDED** and the method is **UNTESTED**; the framework phase and
the detail-specification phase do not open; the tests batch (a)+(b) stays held.

**Not done:** no dispatch is written and none is owed; no session is booted by this side; no
derivation, no comparison, no oracle opened; no blind output read beyond its bounded receipt; **no
pack, generator, manifest, withheld family, brief or governing document is touched — `CLAUDE.md`
above all**; **no tool source is edited**, the `216` included; **[[OI-372]] and its tool are not
touched and are NEVER regenerated — it is a DECISION red**; no register row is created, flipped,
closed or re-worded, and **neither rider of §2 is applied by this side**; no finding number is
allocated (**Ruling 9 opens no series**); [[OI-179]] stays OPEN and GATES; [[OI-374]], [[OI-376]] and
[[OI-377]] stand as found. **The three deferred apparatus items stay deferred.** **The large
pre-existing untracked population is not re-litigated and nothing of it is committed.**

---

*Provenance: Cowork, 2026-08-25, the forty-fifth session's sixteenth and final sitting, at tip
`0f18b358`. The return was verified at content-addressed git objects — both commits and their path
lists, both landing blobs' hashes, the new row's cell count and occurrence, the final guard summary,
the `gating_ids` length and membership, the absence of any `.py` path in either commit, and the
three `216` sites unchanged. §2's withdrawn surface and §3's tally are this side's own account of
its own errors and are labelled as such. The user's words: "Agree on your recommendations" — against
this side's stated choices A, with B available in parallel and C owed — and "yes, do the close here
and handoff for next session".*
