# CC dispatch — `STATUS.md`'s forward bound restored: the nine superseded batch entries archived under Ruling 4

> **Dispatch (Cowork, 2026-09-07).** Executes the FORWARD-MAINTENANCE clause of Ruling 4 of
> `cowork_rulings_2026_08_17_governing_surface_split.md`, which the last seven batch closes did
> not perform. **Written at a verified stop; nothing else is running.** The tip at writing is
> `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, read at `.git/refs/heads/master` and at the
> branch's own reflog, agreeing; no batch is out.
>
> **NOT in this batch, and none of it is licensed by anything here:** the row-by-row extraction
> (PAUSED by the user, 2026-09-07); the pruning pass (PAUSED, same order); the step-1 re-run of
> `gen_governing_surface_spans.py`; any cut to `CLAUDE.md`; anything about `DECISIONS.md`'s
> preamble or the boot's mandated membership; any change to the mechanism behind Ruling 4's
> forward clause; and any decisions-register write.
>
> **Read first, in full — the ordinary session-start read (`D-230`, P-1).** `CLAUDE.md`,
> `DECISIONS.md`, `STATUS.md`, and the derived gating answer
> (`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`).
> **A single-file opening instruction is not an exemption from that read.** **Then read, in
> full:** `cowork_rulings_2026_08_17_governing_surface_split.md` (Ruling 4 is the whole authority
> for Task 1; open it at its own object, not at any relay of it — `D-643`);
> `cc_instruction_preparation_sixth.md`, Task 1 clause 3 and Task 2 (the executing shape this
> dispatch repeats); `STATUS.md`'s own banner rule and its two dated archive stubs; and
> `STATUS_ARCHIVE.md` at its head, for the shape a move appends to.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved
> or run, no build, no measurement of the analysis, nothing under `tools/corpus/` or
> `tools/robust_stop/`, no design, no repair, no derivation of any specification, no document
> archived or deleted as a file, no decisions-register entry and no `D-NNN` allocated, and no
> edit to `CLAUDE.md`, `DECISIONS.md`, `ARCHITECTURE.md`, `FRAMEWORK.md`, any ruling record or
> any governing document other than `STATUS.md` and `STATUS_ARCHIVE.md`. **Open-items rows:
> clause 6 of Task 1 orders exactly ONE row created; no other row is created, flipped or
> discarded.**
>
> **Run the FULL guard set BEFORE the first edit and again at the end; record both states.**
> **The expected start state is stated as an ASSUMPTION, not as a fact — see A2, which names a
> disagreement between two objects and orders it settled at the tree before anything is edited.**
> Verify every commit at the object and record the SHAs in the close. The reserved-word
> conventions and the ruled vocabulary replacements bind all new prose. The new-tool rule stands.
> **Commit and push per task.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 4, the state rule:** *"An entry is SUPERSEDED the moment a later batch's close
  exists. The site keeps only the latest batch's entries."*
- **Ruling 4, the forward clause — the half this batch executes:** *"The bound is maintained
  forward as an instance of the continuous-pruning rule (§5(D)): every future batch close, in
  the same act that writes its own entries, moves the then-previous batch's entries to the
  archive. The file stays permanently bounded at roughly one batch's worth of entries plus its
  operative text."*
- **Ruling 3, the destination:** *"`STATUS_ARCHIVE.md` continues as `STATUS.md`'s home"* — no
  new destination is invented for the one file that already has one.
- **Ruling 1, the safeguard that binds every archive act:** *"the executing dispatch READS every
  span it archives rather than trusting its class."*
- **The pointer shape, at the precedent act:** `cc_instruction_preparation_sixth.md` Task 1
  clause 3 ordered *"one dated pointer left in the body noting the clearing act and the new
  standing bound (not one pointer per entry — the file's own rule already names the archive)"*.
  `STATUS.md`'s own standing stub records the same reading. **This batch follows it.**

## Premise ledger

*(No premise asserts a count of this side's own acts. Every FACT below was established at the
object named beside it by the writing side before this dispatch was released.)*

- **FACT** — `STATUS.md` carries TEN dated batch entries. The newest belongs to
  `cc_instruction_l2_ruling_writeback_2026_09_05.md` and also carries the file's *"Last
  updated:"* prefix. The other nine belong to seven earlier batches, enumerated at Task 1
  clause 2. A later batch's close exists for every one of the nine, so all nine are SUPERSEDED
  under Ruling 4's state rule. Established by reading `STATUS.md` whole, 2026-09-07.
- **FACT** — `tools/audit/gen_governing_surface_split.py` CANNOT perform this act and must not be
  invoked to attempt it. Its `load_spans()` raises `Stop` unless the span decomposition's
  `measured_at_commit` equals the commit the tool pins; that decomposition was measured before
  any of these nine entries existed, and the tool's archive population is keyed to
  `(file, first_line, last_line)` inside those pinned coordinates with an authored verdict
  required per span. It is a one-shot for the 2026-08-17 wave and its own guard keeps it one.
  Established at the tool's source, 2026-09-07. **The act ordered here is therefore a
  hand-authored, read-before-move archive act — which Ruling 1's safeguard requires in any
  case.**
- **FACT** — Ruling 4's forward clause has no mechanism behind it. No check enforces it, and the
  one tool that performed the original clearance cannot, per the FACT above. It has now been
  omitted at seven consecutive batch closes. **This dispatch clears the backlog and does NOT
  repair the mechanism; the repair is a question standing with the user.**
- **ASSUMPTION A1** — the working tree carries no tracked modification, and this dispatch is
  among the untracked root files of this line. **Check ordered (Task 0, first act, over the
  whole tracked population with the sanctioned enumeration tool):** any tracked difference is a
  STOP-and-report.
- **ASSUMPTION A2 — the guard set's start state, with a DISAGREEMENT the writing side found and
  did not resolve.** `STATUS.md`'s newest entry records *77 guards run, eleven failing*. The
  commit subject at the tip records the guard set *"at twelve with both additions traced to this
  batch's own ordered acts"*. The entry was written at the earlier of the two commits and the
  end-state commit came after it, so twelve is the likelier live figure — **but this is not
  established and is not asserted.** **Check ordered (Task 0, before any edit):** run the full
  guard set and record what it actually reports. A start state matching neither reading is a
  STOP-and-report. **Nothing is adjusted to reach a number.**
- **ASSUMPTION A3** — every one of the nine entries READS, on inspection, as a completed batch's
  dated pointer entry, superseded by a later batch's close (Ruling 1's safeguard). **An entry
  that does not is LEFT AT SITE, flagged, and enumerated in the close** — the doubt default, not
  a batch STOP; the act proceeds over the remainder.
- **ASSUMPTION A4** — `STATUS_ARCHIVE.md` exists and receives verbatim appends in the order
  entries leave the parent. **Check ordered:** read it at its head before appending; a shape
  that does not match the precedent is a STOP, not an improvised header.

## Task 0 — land this dispatch, and measure the start state

1. **A1's check first**, over the whole tracked population with the sanctioned enumeration tool.
2. **A2's check:** the full guard set, run and read. Record the state; do not act on it.
3. Commit this dispatch and this line's current handoff entry if it is not yet landed. Push;
   verify at the object.

**Registered expectation E0:** A1 holds as stated; the guard state is recorded as measured and
matches one of A2's two readings or is reported as matching neither.

## Task 1 — the nine archived, in ONE commit

1. **Read each of the nine before moving it** (Ruling 1's safeguard, A3).
2. **Move these nine entries VERBATIM to `STATUS_ARCHIVE.md`, in parent order.** Identified by
   the dispatch each records, never by line number (`D-307`):
   - `cc_instruction_l2_reading_file_2026_09_05.md`
   - `cc_instruction_l2_verdict_pass_2026_09_05.md`
   - `cc_instruction_l2_candidate_list_2026_09_05.md`
   - `cc_instruction_l2_criterion_write_2026_09_04.md`
   - `cc_instruction_l2_keyword_count_2026_09_04.md`
   - `cc_instruction_boot_pack_freeze_l0l1_2026_09_04.md` — **its Task 1 entry AND its Task 0
     entry, two entries of one batch, both moving together**
   - the 2026-09-02 pair — the entry opening *"THE L0/L1 COMPARISON IS OPEN"* and the entry
     opening *"Same dispatch, Tasks 0 and 1"*, **two entries of one batch, both moving together**
3. **The entry that STAYS** is `cc_instruction_l2_ruling_writeback_2026_09_05.md`'s, with its
   *"Last updated:"* prefix untouched. Nothing else in the parent changes on the same lines.
4. **ONE dated pointer** left in the body naming this clearing act and the standing bound, on the
   precedent shape — not one pointer per entry.
5. **The parsing tool the pinned reader inventory names for `STATUS.md` runs at the edited tree.**
   A parser halting on the new shape is a STOP, not an improvised tool edit.
6. **ONE open-items row created, and it is the only row act permitted:** the forward clause's
   lapse — that Ruling 4's forward maintenance has no mechanism and was omitted at seven
   consecutive batch closes. **Its observations are RELAYED from the objects this dispatch's
   premise ledger names. It asserts NO CAUSE, proposes NO REMEDY, carries no gating verdict and
   allocates no finding number.** The row's detail file is created in the same commit (register
   rule (c)).
7. **The derived gating answer is REGENERATED, never hand-edited**, because the open-row
   population grows by one — the settled treatment at the batch that created OI-378. Its check
   goes FAIL-then-PASS; the index lint and the split reconciliation both run across it.
8. ONE commit, push, verify at the object.

**Registered expectation E1:** moved + stayed accounts for the pre-act committed blob to the
character, modulo the one added pointer; every moved entry byte-present in
`STATUS_ARCHIVE.md` exactly once and byte-absent from `STATUS.md`; the file ends carrying ONE
dated batch entry; A3's left-at-site flags enumerated (zero is a valid count); the regenerated
gating answer's check passes and the new row falls in the ruled default cut.

## Task 2 — the close, which performs the forward clause on itself

One `STATUS.md` pointer entry per task — **and, per Ruling 4's forward clause, the SAME act moves
the ruling-write-back batch's entry to `STATUS_ARCHIVE.md`**, so the file ends holding this
batch's entries only. That is the clause this dispatch exists to restore, and a close that
omits it reproduces the defect the batch was written to clear.

The report `cc_report_status_forward_bound_2026_09_07.md`: both guard states, every SHA, every
expectation graded, the per-file arithmetic to the character, every A3 flag, and every declared
departure reported rather than absorbed. **If `STATUS.md`'s size change stales the read-size
measurement, regenerate it in the end-state commit** — the settled practice of the four
preceding batches — and say so.

**Registered expectation E2:** at the tree carrying the close, a fresh full guard run
re-derives, run and read rather than inferred, its values committed only after the run; the
guard set's failing count is reported as measured against A2's recorded start state, with any
addition traced to this batch's own ordered acts.

## What this batch does NOT do

- **It does not repair the forward clause's missing mechanism.** It clears the backlog, rows the
  lapse, and leaves the repair to the user. A tenth omission is not prevented by anything here.
- **It moves no span and no entry other than the ten Task 1 and Task 2 name.** Every operative
  span of `STATUS.md` — its banner, its archive stubs, its navigational paragraph — stays at
  site.
- **It does not invoke `gen_governing_surface_split.py --apply`,** for the reason the premise
  ledger establishes at that tool's source.
- **It touches nothing about the token question beyond this one act:** no re-measurement of the
  boot, no cut to any other boot file, no change to the session-start read.
- **It opens no paper, no extract, no progress record and no companion; it runs no sweep; it
  moves no verdict; and it lifts no gate.** The reading and the pruning stay paused.
