# Ten rulings of 2026-08-09, taken at the return STOP

> **STATUS: RATIFIED RULING RECORD, written 2026-08-09 (Cowork).** Ruled by the user ("agree
> with recommendations") after the full decision surface — each item with its alternatives,
> the pros and cons naming their principles, and the effect on the ultimate objective
> (maximum-precision inference, #4) — was delivered as user-visible text, with the batch's
> Tasks 0–2 verified at the objects first. Interim carrier until the applying dispatch records
> them (D-230). The away batch's Tasks 3–8 were NOT performed and remain owed; the applying
> dispatch is their continuation.
>
> **None of these rulings authorizes a fix to the analysis, a design, or an inference change.
> Phase 1 (D-231) remains open; #8's full three-clause gate (refactoring + architectural
> design + algorithmic completion) stands untouched.**

## Ruling 1 — `phase1q_reclassification.json` is REMOVED

The live file is deleted. The phase-1q record survives at its established snapshot (hash-STOP
guarded); the present classes live at `home_classification.json`; every committed state of
the removed file survives in git history and its drift is recorded in the batch's divergence
measurement artifact — so #12 is satisfied by the recomputable-collapse clause, and #6 gets
one home per concern. The removal is recorded where the file was named (the D-644 shape:
state the current arrangement, record the removal as closed). *Pre-act check:* the named
commits carrying its states are reachable as git objects; a missing object is a STOP.
*Excluded alternatives, recorded:* restoring from the snapshot (a second, unguarded copy of
the record — the OI-305 divergence class reopened, #6); renaming (an orphan generated
artifact with no writer and no consumer — undeclared dormancy).

## Ruling 2 — OI-319 FLIPS; its remaining fragment gets its own row

OI-319 is flipped RESOLVED with provenance (the mechanism is discharged; the OI-283
never-marked-discharged shape ends). The one remaining fragment of different subject —
`gen_guard_classification.py` carries a re-derivation mode no guard list runs — gets its own
open-items register row in the same commit (rule (c); tracking an owed item in prose only is
a #10 violation).

## Ruling 3 — the `CLAUDE.md` D-253 doc-sync correction is LICENSED (one edit)

The clause stating "No guard watches either surface" is corrected as a NARROWING: the
surfaces are watched where the hook runs, and the Cowork sandbox is not that place. The
clause's #19 point — a guard's silence on an unwatched execution surface is not compliance —
survives verbatim. One edit, this act alone; not a homing act, hence this licence.

## Ruling 4 — the delegation-bar generator: pointer re-aim AND retired-block maintenance, both LICENSED

(a) Both artifact-prose namings of the removed phase-1q file re-aim at
`home_classification.json`. (b) The generator's authored judgments for documents that are
nobody's home any more move WHOLE into its retired block with their reasons (#12) — the same
authored-input maintenance the away batch performed three times under the tools' own STOPs,
not a mechanism change under D-436 — which is what clears the delegation-bar guard's
standing failure.

## Ruling 5 — D-286 is homed by the D-472 PATTERN; no conformance verdict is taken

The whole-score interactive-analysis SHELVING is written into the owning specification — the
section that owns the analysis-extent question (the effort-control home `CLAUDE.md` names) —
AS a shelving, with the later build's contradiction stated beside it and the conformance and
extent questions pointed at their existing rows (OI-206, OI-210). Both facts visible, nothing
false written (#10, #12), no premature judgment (D-231; the make-it-work-first sequencing
keeps the extent question last). *Pre-act check (the register's rule (h) kind half):* the
target section STATES RULES; a findings-recording section is a STOP back to the user.

## Ruling 6 — D-289 is ruled SUPERSEDED (the D-642 route), conditional on successor coverage

Its obligation moves to its named successors and is discharged where they are homed. *The
condition:* the successors carry ALL of its live content — checked at their homes before the
status is recorded; a content residue no successor carries is a STOP back to the user (#12).
*Excluded alternative, recorded:* homing it as doctrine, which duplicates its successors'
rules (#6) — the reason it sat in the no-home class.

## Ruling 7 — D-291 is SPLIT: the build half homed at its layer, the measurement half ruled recorded-twice

Two decisions, two owners (#7). The build decision (the tonicization labeller left unwired)
homes at the owning layer's specification. The measurement half is ruled ONE DECISION
RECORDED TWICE, the gate-block-(A) convention of the same date standing as its single home
(#6), the decisions register recording the identity. *The condition:* the two texts are the
same binding statement — compared verbatim before the act; any binding difference is a STOP
back to the user.

## Ruling 8 — the owed `CLAUDE.md` rule-triage entries are AUTHORED BY THE SESSION, reviewed at the queue

The continuation session authors the owed triage entries per the triage tool's own criteria,
each verdict with its reason, delivered for the user's review as a ratification-queue reading
file (the pattern ruled for the non-gating generator's authored verdicts). *Pre-act check:*
the tool's own contract is read first; if its criteria demand an author the session cannot
be, that is a STOP back to the user.

## Ruling 9 — the OI-349 probe RUNS NOW (the timing ruling the row reserved to the user)

Read-only, in the continuation, early: establish what the record arm supplies to the key-area
grouping and compare it against the stated stabilization precondition. The finding goes to
the returns file with citations whichever way it lands; a NOT-SATISFIED finding is a #13
surface and nothing is proposed for it beyond the row. Its diff is empty but the probe's own
named artifact.

## Ruling 10 — OI-179: commissioned in its two zero-session-cost halves; the measurement opens WITH phase 2

Recorded on OI-179's row: (a) the user performs the PeARL laboratory contact (the one act
only the user can perform; longest lead; outside any session's scope) — **PERFORMED
2026-08-09: a public GitHub issue on the BCMH repository asking annotator count and
background, validation/review procedure, unpublished duplicate or superseded readings, and
errata/citation. A reply is recorded on the row when it arrives; silence after a reasonable
wait is recorded as the route EXHAUSTED, making D-475's declared status final rather than
provisional (#19 closed by exhaustion)**; (b) the ceiling
measurement is COMMISSIONED and opens WITH phase 2, desk simulation first (#17c), per the
landed design surface (`cowork_scratch_2026_08_08/draft_oi179_ceiling_measurement_surface_2026_08_08.md`).
No measurement tool is built before phase 2 opens — the finish-line scope rule stands, and
this ruling is the user ruling that scope rule requires for the addition when phase 2 opens.
