# OPEN ITEMS REGISTER — the ONE home for every discovered-but-unresolved issue

> **Created 2026-07-10 (session 36), user-directed** after a full-repo sweep found 91 open items
> scattered across 12 tracking surfaces with 11 status contradictions. **Standing rule (CLAUDE.md
> "Open-items register" section):** read this file at session start; a stage may not open while a
> register item gating it is open; every new discovery gets a row in the same commit that records
> it; every resolution flips its row with provenance. Detail lives at the cited source — rows here
> are pointers, not restatements. IDs are stable; do not renumber.

## A. STAGE-3 ENTRY GATE — blocks E4/L5 engagement (from `cowork_engage_arc_plan.md`)

| ID | item | source | status |
|---|---|---|---|
| OI-1 | EG-1: T1-1 resolver progression-first re-ordering (arc-#9 design) must land pre-production | arc plan; premise-debt T1-1; `functionresolver.cpp:221-246` | OPEN — prerequisite |
| OI-2 | EG-1: T1-2 F-B override demotion (arc-#11 design; `attemptFineGrainOverride` unconditional, −756) | arc plan; premise-debt T1-2; `functionresolver.cpp:529-531` | OPEN — prerequisite |
| OI-3 | EG-2: rebuilt-vs-legacy go/no-go | `cc_eg2_probe_report.md` | PROBED — P1 not supported as measured (abstention artifact); honest re-measure blocked on OI-1/OI-2; decision with user |
| OI-4 | EG-3: pedal reader HARD-GATED on owed-P1 over an established pedal-dense corpus (premise currently unfavorable 0.20/0.50/0.20, n=2–5) | arc plan; L5-engage §9.2 #3; owed-P1 | OPEN — gated |
| OI-5 | EG-4: confidence-scale commensurability (T1-3/S19/F-1/D-FS) owes #17 ledger + desk sim before any θ/kBoundary fit; combinedBoundary calibration already FAILED (D-8) | arc plan; premise-debt T1-3 | OPEN |
| OI-6 | EG-5: extend `tools/param_manifest.json` to L1/L2 constants + live-L3 hysteresis margins before Stage 5 closes (T3-1) | arc plan; premise-debt T3-1 | OPEN |
| OI-7 | EG-6: Jazz validation status — establish jazz GT corpus or de-scope Jazz correctness claims (T3-2) | arc plan; premise-debt T3-2 | OPEN |

## B. Owned by Stage-3 / E4 (build + design decisions at the engage step)

| ID | item | source | status |
|---|---|---|---|
| OI-8 | FQ-4 anchor: decoder carry replaces `results` (cap→append + pedal clobber + Iter 86/91 die by construction) | audit §3; §9.2 #1 | OPEN |
| OI-9 | Distinct-root-preserving carry (topK caps voicings, ≥3rd root not guaranteed; exclusion tail #12) | L5-engage §2.3; §9.2 #2 | OPEN |
| OI-10 | FQ-2 quality-from-key single owner (S12/S14/S15/S16/S17) — decided WITH §6-block dissolution | audit; §9.2 #5 | OPEN (owner named-not-chosen — contradiction G) |
| OI-11 | FQ-1 different-root primitive — retires WITH the decoder (reassigned E4; audit §3 table stale) | audit §3.1 | OPEN |
| OI-12 | FQ-3 `findTemporalContext` ownership move (reassigned E4; audit §3 table stale) | audit §3.1 | OPEN |
| OI-13 | FQ-8 owed migrations: two-segmenters, two-pitch-context, tpc-reader fold, `function/` rename (S18) | audit; §9.2 #7 | OPEN |
| OI-14 | PC-1 E4 design question: symmetric-set detection from pc set vs land C2/G5 dim7 type vs (c) generalize spelling channel (OI-15) | `cowork_eg1_premise_checks.md` | OPEN — design decision at E4, each option owes #17 ledger |
| OI-15 | ★ Spelling as a FIRST-CLASS evidence primitive (user-raised 2026-07-10): tpc facts are L1-carried but consumed only by the pin; O1 measured ~60% of Baroque residual spelling-resolvable; arc-#9 channel #2. Design where the general spelling channel lives (#6/#7) | PC-1 amendment; O1; arc #9 | OPEN — E4/L5 design input |
| OI-16 | T1-4 decoder rotation assumes key prior correct (diatonic terms only key input; spelling-pin sole guard) + two chord-equality relations (`sameChordVoicing`/`sameChordSymbol`) latent inconsistency | premise-debt T1-4 | OPEN — surfaces at E4 |
| OI-17 | PC-3 Xsus fifth mis-rooting (43% of EG-2 new errors) — hypothesis UNCHECKED (bass-on-fifth + sus template); checkable at bwv87.7@9600 cube | PC-3; cc_eg2 §2.4 | OPEN — L4 scorer owner; check at E4 design, fix per #8 timing |
| OI-18 | L4 temporal-extension trigger specified NOT coded (silently truncates); whole §2.15 extension cluster exercised nowhere; unified orchestration contract owed | roadmap §2.15 | OPEN — at engage |
| OI-19 | S18 `function/` dir rename (rides R7) | audit UNCLEAR-4 | OPEN |
| OI-20 | Jazz +1 interim class-(a) case — retires when Layer 4 pins rotation/center | CLAUDE.md two-tier block | OPEN — interim |
| OI-21 | owed-P2 confirmation-gap reproduction (carry margin vs pass2 sigmoid) | L5-engage §8 | OPEN — post-E4 |
| OI-22 | owed-FB1: F-B annotate must move class-(b) duration favorably at its build event | L5-engage §8 | OPEN — at F-B build |
| OI-84 | ★ FULL-COVERAGE AUDIT PLAN (corrected 2026-07-10 — the earlier "audit-at-touch" form was rejected by the user as risky: touching 1 % would audit 1 %, and new work would build on unknown problems, itself a #18 violation at architecture scale). The plan: (a) PARTITION the module by the retirement map R1–R9 — code that RETIRES at E4 gets NO audit, only the #12 interpretation-check at deletion (A1 verdict); (b) the SURVIVING stack is audited EXHAUSTIVELY per layer in DEPENDENCY ORDER — L1 → L2 → L3 → L4 (decoder + surviving scorer core) → L5 + the instruments — each a read-only session whose ledger output feeds the corresponding E4 step's #17 ledger; (c) enforcement = arc-plan EG-7: an E4 step may not open until every layer it DEPENDS ON (not merely touches) is audit-certified. Coverage of the end-state architecture: 100 % before it carries new load. FIRST ITEM: the L1/L2 certification audit (also the least-covered layers to date) | session-36 user challenge ×2 | OPEN — EG-7; **L1/L2 CERTIFIED by the user, 2026-07-11**, on pass 1 + pass 2 + the OI-89 fully-blind re-run (`cc_l1l2_audit_blind_rerun_report.md`; zero correctness defects found by either reader; residual findings tracked at OI-86/OI-87). **The next layer in dependency order: the L3 audit** (key/mode inference). **★ L3 PASS-1 DONE 2026-07-11** (blind enumerative, P1–P4; `cc_l3_audit_pass1_report.md`; freeze `61dabd86d1`): 22 deep files (15 L3 + 7 L3-MIXED) → **1943 dispositioned rows** (every one verdicted), decoder fire-rate **352/352** (`c50002fee1`; 11.43 % uncertain slices, 3.94 % region key-change, 275/352 modulating). 8 findings (OI-90…OI-95 + existing OI-75), **NONE a correctness bug; the L3 spine reproduces its documented design.** **Certification NOT granted by pass 1** — pass 2 (the DT signature sweep, fresh session) + the P6 error-rate are owed. **★ L3 PASS-2 DONE 2026-07-11** (blind second reading P5 + catalog sweep P8 + error rate P6; `cc_l3_audit_pass2_report.md`; freeze `1021e81e44` = the blinding boundary): 116-row reading (seed 20260714) + 40-row error-rate (seed 20260715), all verdicts frozen BEFORE any withheld file opened (fully blind, the OI-89/DT-20 lesson applied — `OPEN_ITEMS.md` read deferred to Task 2). Result: **error rate 0/40 substantive (100 % substantive concordance on the error sample, 99.1 % on the reading); every disagreement diagnosed** — ONE substantive miss (`extraToneScore` dead local field, OI-96, caught by the second reading, contradicting pass-1 §2's "zero DEAD"), all others verdict-axis (this reader's coarser 4-label vocabulary vs pass-1's full rubric). The whole-layer DT sweep (`gen_signature_sweep.py --layer l3`, all 1943 rows) found **no untracked correctness defect** — DT-2 122 reproduces OI-91, DT-19 18 reproduces OI-93, and 4 new hygiene/layering/doc findings (OI-96 DT-5, OI-97 DT-3, OI-98 DT-16, OI-99 DT-12). **★ L3 spine certification PROPOSED (report §6)**, then established by the OI-100 fine-label re-derivation (`cc_l3_audit_pass2_relabel_report.md`: prose fully carries the code + derived-facts axes; constants-axis gaps named, bounded, and DT-2-sweep-covered; the one uncovered bound verified inert at code). → **✅ L3 CERTIFIED 2026-07-11** (the user delegated the decision to the principle-derived recommendation). **The next layer in dependency order: the L4 audit (decoder + surviving scorer core)** — the largest layer; its pass-1 instruction is Cowork's to draft. **★ L4 PASS-1 TASK-1 DONE + FEASIBILITY STOP 2026-07-11** (blind inventory only; `cc_l4_audit_pass1_report.md`; freeze `88befa3055`): 10 deep files → **2121 rows (~1880 in-scope)**, three populations verified at the code + call sites (surviving scorer-core 7 / dormant decoder 2 / mixed 1 / retires 2 file-level / deferred-not-L4 11). The every-row disposition + the P3 contract check + P4 fire rates are **INFEASIBLE in one session** (Task 1.4) → the deep audit is **partitioned into ~3 sequential sessions + the pass-2 sweep** (proposed, **OI-102**). Task-1 findings: file-table mis-tags **OI-101** (DT-21, incl. `chordanalyzer.cpp` whole-file RETIRES corrected to the surviving scorer core — a would-be missed file); the `sparsechordrefinement` L4/L5 boundary + `chordpathdecoder` retire questions carried to OI-102. **L4 certification NOT proposed — the deep work is owed.** **★ L4-2a (DECODER) PASS-1 DISPOSITIONS DONE 2026-07-11** (`cc_l4_audit_pass1_decoder_report.md`; freeze `dba57ce570` = the blinding boundary): the 311 `chordslicedecoder.{h,cpp}` rows **all verdicted** (43 fn / 8 include / 61 field / 72 literal / 127 branch → 43 SURVIVES-fn, 30 PUBLISHED / 28 SURVIVES / 2 TRAPPED / 1 SILOED fields, 63 ESTABLISHED / 9 UNFIT literals, 127 SURVIVES branches, 8 SURVIVES includes); the P3 contract-direction check located every ARCHITECTURE §L4 expectation in the code (no defect absence); P4 behavioral characterization on 3 routes (test suite 67/67 `Composing_DecodeChord`; `--decode-chords` + `--dump-fullspine` over `c50002fee1`, 29080 slices — Commit 34.4 % / Inherit 3.0 % / Abstain 62.6 %). **NO correctness defect — clean dormant-but-surviving code whose output surface carries the engagement's needed facts.** Findings **OI-103** (manifest-gap, DT-2), **OI-104** (G6 SymmetricRotation never-fires + reserved NoteMembership, DT-7/DT-5), **OI-105** (doc/naming, DT-12) — all existing TYPES; existing rows confirmed OI-9/OI-16/OI-18/OI-28/OI-72/OI-73/OI-82. **★ L4 PASS 1 COMPLETE (L4-2a/2b/2c) + L4 PASS 2 DONE 2026-07-12** (`cc_l4_audit_pass2_report.md`; freeze `d716ac1ca8`): fully-blind second reading + error rate **0/40 substantive** at full P2 vocabulary + whole-layer 21-DT sweep — **no untracked correctness defect**, one new benign dead-field OI-115 (DT-5, L4 twin of OI-96). **★ L4 SPINE CERTIFICATION PROPOSED** → **✅ L4 CERTIFIED 2026-07-11** (the user delegated the decision to the principle-derived recommendation: both passes complete at full vocabulary resolution, blinding held, error rate 0/40 substantive blind-first, disagreements diagnosed to the known constants boundary, sweep clean; companion decision — the oracle fire-count instrumentation REVERTS, OI-110). **The LAST layer in dependency order: L5 + the instruments — its pass-1 instruction is Cowork's to draft. When it completes, the OI-84 certification plan is DONE and the held OI-43 discussion (mode/key + chord inference — where and how) opens.** **★ L5 PASS-1 TASK-1 DONE + FEASIBILITY STOP 2026-07-12** (blind inventory only; `cc_l5_audit_pass1_report.md`; Task-0 revert `940632ecd1` closing OI-110; freeze `0382c3275e`): `gen_inventory.py --layer l5` extended (a SECOND scope root — the `tools/` instruments — + a Python `ast` extractor beside the C++ scan; prior-layer byte-identity re-proven) → **351 files in scope → 34 deep files / 3,372 rows**, populations verified at the code (dormant resolver 20 / Python instruments 13 / harness 1 / retires+mixed 5 file-level / deferred 4). Deep dispositions + P3 + P4 **INFEASIBLE in one session** → **partitioned into ~3 sessions + the pass-2 sweep** (**OI-116**). Task-1 findings: the `harmonicfunctionlayer` L4-legacy-competition mis-tag (**OI-117**, DT-21) + the no-live-surviving-L5 structural observation. **L5 certification NOT proposed — the deep work is owed.** **★ L5 PASS-1 COMPLETE across ALL populations 2026-07-12 (partition-1 dormant resolver `e346f788d4` + partition-2a regression-stop core `3d7d1cb290` + partition-2b grading+fitting `b426616ba2` + partition-3 the shared harness `708d0c3708`; OI-116).** The whole-scope pass-2 signature sweep + the P6 error rate (the SECOND pass) decide the certification proposal — L5 certification still NOT proposed. When L5's two passes complete, the OI-84 dependency-ordered plan is DONE and the held OI-43 discussion opens. **★ L5 PASS 2 DONE 2026-07-12 (`cc_l5_audit_pass2_report.md`, detail at OI-116) → ✅ L5 + INSTRUMENTS CERTIFIED by the user 2026-07-12. ★ THE OI-84 PLAN IS COMPLETE:** every surviving layer (L1/L2, L3, L4, L5) and the measurement chain audited on TWO passes each, all five certifications user-granted on measured records (error rates 0/40 blind-first at L3/L4/L5; the L1/L2 fully-blind re-run). EG-7's certification chain is SATISFIED; the remaining Stage-3 entry-gate items are OI-1…OI-7; the priority instrument-hardening item is OI-140. **This row CLOSES. The OI-43 hold LIFTS — the discussion (mode/key + chord inference — where and how) is OPEN.** |
| OI-86 | L1/L2 PASS-1 layering findings (#7/#6): upward cross-layer deps — `metricweights.h:42`→`key/keymodeanalyzer.h` (L1.5→L3), `regiontonecollector.cpp:37` + `regiontoneprimitives.cpp:37/38`→`chord/` (L1.5→L4; the audit-Q2 back-edge is killed in the HEADER only, live in the .cpp); mixed-layer grab-bags (engravingbridge + metricweights host L2-legacy/L3/L4 code); two metric-weight tables (`regionMetricWeightForBeatType` vs `beatTypeToWeight`, #6 dup). Much rides existing retirements R4/R5/R6 + FQ-8/OI-13. Promoted DT-19. **PASS-2 refinements (`cc_l1l2_audit_pass2_report.md` §3/§5): DT-19 4 upward includes reproduced mechanically; `SpanWindowWeights` `decayRate=0.7`/`lookaheadWeight=0.5` are DT-3 value-copies of `scoreharvest::DECAY_RATE`/`LOOKAHEAD_WEIGHT` (a refit silently diverges); DT-16 4 raw-DOM note re-readers (`collectPitchContext`, `detectOnset/BassSubBoundaries`, `findTemporalContext`) — the mechanical reason those files are mixed-layer; all ride R5/R6/E4.** **Blind-re-run refinement (2026-07-11, `cc_l1l2_audit_blind_rerun_report.md` §5): the `regiontonecollector.cpp:37`→`analysisutils.h` upward include is not merely upward but UNUSED — none of its 6 exported symbols is referenced in that TU (grep-verified) — so it is removable outright (zero behavior change), which alone would delete that specific L1.5→L4 back-edge. Needs a build-confirm; removal itself waits for the E4 #8 timing.** | `cc_l1l2_audit_pass1_report.md` §3a/§3b; `cc_l1l2_audit_pass2_report.md` §3/§5; `cc_l1l2_audit_blind_rerun_report.md` §5; DT-19 | OPEN — dissolves at E4 retirements; re-verify at each |
| OI-88 | L1/L2 PASS-2 doc-precision findings (DT-12/#10, minor, non-correctness): (a) **stale file:line anchor** `slicer.h:68` cites `regionanalyzer.cpp:579` for `KeyModeSequenceDecoder::decode` — actual call at `:634`/`:705` (line 579 drifted to a Layer-1 comment); (b) `note_model.h:157-163` docstring "**no layer calls [`extend`] yet**" is contradicted by 3 gated production call sites (`regionanalyzer.cpp:702` behind `ReachBackOptions::enabled`=false; `chordslicedecoder.cpp:1387/1393` behind `enableEdgeExtension`; `textureclassifier.cpp:183/187`) — the dormancy SUBSTANCE holds (all gated off), only the wording is inaccurate. One-line doc fixes; do not touch code (#8). | `cc_l1l2_audit_pass2_report.md` §5-A/§5-B; `cc_l1l2_audit_blind_rerun_report.md` §4; DT-12 | ✅ RESOLVED 2026-07-11 (`f76e8b65c8`, comment-only, no behavior change): (a) `slicer.h` anchor rewritten symbol-led + cites both real call sites `:634`/`:705`; (b) `note_model.h` reworded to "dormant in SUBSTANCE, callers gated off on the production path" citing the 3 gated sites. Both re-verified at the code; the blind re-run independently re-found (b). |
| OI-90 | L3 PASS-1 file-table mis-tags (DT-21): the L1/L2 file-table tagged `decode/chordpathdecoder.h` "L3 key-mode decoder scaffolding" — it is the beam-1 **CHORD**-path decoder (Layer 4; includes `chord/chordanalyzer.h`, threads `ChordAnalysisResult`/`ChordIdentity` via `advanceTemporalContext`); `region/sparsechordrefinement.{h,cpp}` similarly re-tagged L4 (chord-quality refinement consuming the L3 key as a prior). Corrected in `gen_inventory.py --layer l3`; the L1/L2 `tools/audit/l1l2/file_table.csv` reason strings are now stale. Promoted DT-21. | `cc_l3_audit_pass1_report.md` §8; `tools/audit/l3/file_table.csv`; DT-21 | OPEN — correct the L1/L2 file-table reason at its next touch (comment/data-only; #8 no code) |
| OI-92 | L3 PASS-1 duplicated table (DT-3, #6): `jointkeydecision.cpp:73-88` `kJkdTemplates` (14-entry tertian chord vocabulary) is comment-declared "IDENTICAL to `tools/cc_joint_residual_probe.py` TEMPLATES" but NOT sync-test-guarded (unlike the `ModePriorPreset`↔`KeyModeAnalyzerPreferences` mode-prior dup, which `modepriorpresets_tests` guards) — a C++↔Python value-copy that silently diverges if either drifts. Diagnostic-only, gated OFF. | `cc_l3_audit_pass1_report.md` §5 OI-92; DT-3; OI-63 (mode-prior single-sourcing sibling) | OPEN — add a sync test or single-source at the joint-key wiring touch (#8) |
| OI-93 | L3 PASS-1 cross-layer deps (DT-19, #7): (a) `section/cadencekeyanchor.h:50` + `section/jointkeydecision.h:85` `#include chord/chordanalyzer.h` (a HEAVY L4 header) solely for `ChordQuality`, which lives in the dependency-free types leaf `analysistypes.h:120` — an avoidable header back-edge the types-leaf refactor removed for `keymodeanalyzer.h`/`regiontonecollector.h` but LEFT here (one-line include swap to the leaf); (b) 5 core-L3 `.cpp` include `chord/analysisutils.h` for `normalizePc`/`diatonicMaskFromFifths` — dependency-free pc primitives siloed under `chord/` (layering smell, light). | `cc_l3_audit_pass1_report.md` §5; `tools/audit/l3/pass1_dispositions.csv` (2 BACK-EDGE + 5 BACK-EDGE-NOTE); DT-19; OI-86 (L1/L2 sibling) | OPEN — fix (a) at the next section-header touch (#8); (b) rides a shared-pitch-utils leaf decision |
| OI-94 | L3 PASS-1 contract deferrals (P3 negative space; documented, not defects): (a) a mid-piece NOTATED key-signature CHANGE is not re-anchored — the decoder reads one signature at startTick and tracks a notated key change only via the note-driven change cost (`regionanalyzer.cpp:626-628`, "a deferred refinement"); (b) the A-3 dominant-implication key-evidence channel (L3 §15) is named but NOT built (= OI-68). | `cc_l3_audit_pass1_report.md` §3/§5 OI-94; OI-68 (A-3 capability track) | OPEN — (a) a deferred L3 refinement; (b) tracked at OI-68 |
| OI-96 | L3 PASS-2 dead local field (DT-5, #6/#12, low): `keymodeanalyzer.cpp` `CandidateEvaluation::extraToneScore` is declared (`:217`) and assigned `= 0.0` (`:588`, comment "folded into triadScore") but **read NOWHERE** (grep-confirmed; the mechanical `DT-5_local_dead_field` sweep found exactly this one field, 2 occurrences / 2 writes / 0 reads) — a vestigial/dead local field (waste). Caught by the fully-blind second reading; **contradicts pass-1 report §2's "zero DEAD constants and zero SILOED/TRAPPED facts"** (a LOCAL struct field escapes both the cross-layer fields inventory and pass-1's literal constant-classifier, which templated `:588` ESTABLISHED without a consumer-check). No runtime effect. | `cc_l3_audit_pass2_report.md` §2/§4; `tools/audit/l3/sweep_results.json` (DT-5_local_dead_field); DT-5 | OPEN — remove the field + its write at the next `keymodeanalyzer.cpp` touch (#8 timing; a comment/dead-code cleanup, no behavior change) |
| OI-97 | L3 PASS-2 value-copied constant (DT-3, #6, low): `analysistypes.h:788` `relativeKeyHysteresisMargin = 2.0` duplicates `hysteresisMargin = 2.0` by comment ("[empirical, = hysteresisMargin by default]"), not by reference — a documented SOFT-coupling (two conceptually-distinct hysteresis knobs sharing a default). Refitting `hysteresisMargin` alone leaves `relativeKeyHysteresisMargin` at 2.0. Distinct from OI-63 (mode-prior) and OI-92 (`kJkdTemplates`). Low severity; the decoder's copy IS single-sourced (`keymodesequence.h` reads `kDefault…relativeKeyHysteresisMargin` by reference). | `cc_l3_audit_pass2_report.md` §4; `tools/audit/l3/sweep_results.json` (DT-3); DT-3 | OPEN — the Stage-5 fitter must treat the two as independent (or single-source) when OI-91 lands them in the manifest |
| OI-98 | L3 PASS-2 raw-DOM read outside L1 (DT-16, #7, low-med): `keyresolver.cpp:107` `partialSignatureCorrection` walks the engraving DOM directly (`s->cr(...)` / `toChord(cr)->notes()` / `n->ppitch()` / `n->play()` / `n->visible()`, `:160-170`) to build the signature-scoped duration-weighted pitch histogram, bypassing the L1 NoteModel — the L3 sibling of the L1/L2 DT-16 readers (OI-86). NOT flagged by pass 1. Functionally correct; the histogram needs a per-segment `keySigEvent().concertKey()` filter the NoteModel does not expose, so the read may be irreducible without extending L1. `resolveKeySignatureContext` (its caller) is KEEP-conscious past the R5 resolver shrink (pass-1 §7), so this read persists. | `cc_l3_audit_pass2_report.md` §4; `tools/audit/l3/sweep_results.json` (DT-16); DT-16; OI-86 (L1/L2 sibling) | OPEN — re-home behind an L1 primitive (signature-scoped pc histogram) at the R5/E4 resolver-shrink, or accept as an irreducible signature read with a note |
| OI-99 | L3 PASS-2 dangling doc-reference (DT-12, #10, low): `regionanalyzer.cpp:534` and `:815` comments cite `cowork_phase5c_step4_report.md` (the J-key-iii §6 byte-identity proof), which **does not exist** in the repo (not tracked, not gitignored). Two occurrences in production source comments. Doc-sync only, no code effect. | `cc_l3_audit_pass2_report.md` §4; `tools/audit/l3/sweep_results.json` (DT-12); DT-12 | OPEN — re-point to the extant provenance doc or drop the citation at the next `regionanalyzer.cpp` touch (comment-only, #8) |
| OI-101 | L4 PASS-1 file-table mis-tags (DT-21, sibling of OI-90): re-verified at the code + call sites, the inherited chord/decode tags were corrected — (a) `chord/chordanalyzer.cpp` was tagged whole-file **RETIRES**, but it is the **surviving vertical scoring ORACLE** the dormant decoder REUSES (`chordslicedecoder.cpp:453`; live at `regionanalyzer.cpp:987`) — only the competition (`function/`) + Gates A–L retire (R1), and R9 file-SPLITS it (does not delete); inheriting RETIRES would have deep-audited it at FILE-LEVEL only and **MISSED the surviving scorer core** (the DT-21 miss-a-file failure) → corrected to `L4-SCORER`; (b) `chord/chordvoicing.cpp` is a notation voicing/arrangement helper, NOT L4 → `DEFERRED`; (c) `vocabulary/*` "(L4)" + `voiceleading/*` "(L4/L5)" are NOT L4 (an L5-consumer catalog + the voice-leading axis-2) → `DEFERRED`. Corrected in `gen_inventory.py --layer l4`; the L1/L2 file-table reason strings for these files are now stale (as with OI-90). | `cc_l4_audit_pass1_report.md` §4 F-1; `tools/audit/l4/file_table.csv`; DT-21; OI-90 (L3 sibling) | OPEN — correct the L1/L2 file-table reasons at their next touch (comment/data-only; #8 no code) |
| OI-102 | L4 PASS-1 **feasibility stop + partition** (Task 1.4): the machine inventory is 10 deep files → **2121 rows (~1880 in-scope)**, dominated by dense scorer/formatter logic (`chordanalyzer.cpp` 545 + `chordsymbolformatter.cpp` 505 + `chordanalyzer.h` 241; decoder 311) — an every-row disposition + the P3 contract-direction check + P4 fire-rate instrumentation are **INFEASIBLE to complete rigorously in one session**, so Tasks 2–3 were NOT attempted (no silent sampling — the protocol's totality is the point). **Owed:** the deep pass-1 dispositions partitioned into ~3 sequential sessions (proposed **L4-2a** decoder `chordslicedecoder.*` ~311 / **L4-2b** oracle `chordanalyzer.cpp`+`.h`+`analysisutils.h` ~855 / **L4-2c** `chordsymbolformatter.cpp`+`chordpathdecoder.h`+`sparsechordrefinement.*`+the L4 rows of `analysistypes.h` ~717) + the whole-layer pass-2 signature sweep. **Two live in-scope population/boundary questions carried here** (resolve when their session opens): (i) `region/sparsechordrefinement.*` sits on the L4/L5 boundary — it overwrites `identity.quality` from the resolved key POST-commit (= the DT-4 / OI-10 quality-from-key-single-owner / OI-29 §6-dissolution substance); which layer audits it is open; (ii) `decode/chordpathdecoder.h` (live beam-1 commit chain) — retires with the legacy path vs survives as Stage-3.2 wider-beam scaffolding is open. **L4 certification NOT proposed.** **★ L4-2a (decoder) pass-1 dispositions DONE 2026-07-11** (`cc_l4_audit_pass1_decoder_report.md`; freeze `dba57ce570`): 311 decoder rows verdicted + P3 contract check + P4 fire rates (3 routes); findings OI-103/OI-104/OI-105. **★ L4-2b (oracle) pass-1 dispositions DONE 2026-07-11** (`cc_l4_audit_pass1_oracle_report.md`; freeze `1a11cf7210`): all **855** rows of `chordanalyzer.cpp`+`.h`+`analysisutils.h` verdicted (490 ESTABLISHED constant / 268 SURVIVES code / 83 PUBLISHED / 9 THEORY + 5 FACT premise; 24 flagged); P3 contract check — template contract IN SYNC (kTemplateCount=17 = templates array + static_assert + kTemplateIntervals + scoring_model.md §2), oracle genuinely vertical per §11; P4 — output-observable (all 8 qualities fire, Aug/Power at floor, 36.7 % slash, 1.1 % sparse) + a default-OFF fire-count instrument over **122,047 analyzeChord invocations** (byte-identical 352/352, suites green). **NO correctness defect.** Findings all existing TYPES: **OI-106** (constant/manifest-publication gaps, DT-2/DT-3, oracle twin of OI-103), **OI-107** (ARCHITECTURE.md oracle doc drifts, DT-12/#10), **OI-108** (augmented-root correction never-fires 0/122047 + display/voicing decls in the analysis header, DT-7/DT-19), **OI-109** (orphaned `// BUG-10` marker + the P5-over-diminished hard-contradiction question declared to Cowork, DT-12/#10). **★ L4-2c (satellites) pass-1 dispositions DONE 2026-07-11** (`cc_l4_audit_pass1_satellites_report.md`; freeze `10495a6bca`): all **699** in-scope rows of `chordsymbolformatter.cpp` (505) + `chordpathdecoder.h` (16) + `sparsechordrefinement.{cpp,h}` (66) + the L4-type rows of `analysistypes.h` (112, lines <546; L3 types excluded) verdicted (332 SURVIVES code / 305 ESTABLISHED constant / 44 PUBLISHED / 15 ASSUMPTION premise / 3 DEAD-reserved); P3 formatter-coverage — every committed identity renders (100 % non-empty symbol + Roman on all 3 presets; the only gap is Nashville chromatic-root `"?"`); P4 fire rates from the batch outputs themselves (formatter branches) + reading/test (decoder: `commit` per region byte-identical, path members zero-consumer inert) + corpus bound (sparse refinement: call 100 %, quality-change confined to ≤2-PC regions ≤125/corpus). **NO correctness defect.** Findings, all existing TYPES: **OI-111** formatter duplicated music-theory tables (DT-3), **OI-112** formatter/ARCHITECTURE doc drifts (DT-12, sibling of OI-107), **OI-113** Nashville chromatic-root coverage gap (DT-17), **OI-114** spelling-normalization premise provenance (DT-11). **Boundary questions (i)/(ii) FACTS gathered (not decided):** (i) the sparse refinement overwrites `identity.quality` from the resolved key POST-commit (`regionanalyzer.cpp:1003-1006`), `applyTonicPriorToSparseChord` even overrides a committed non-Unknown thin quality — enriches OI-10/OI-29/DT-4; (ii) `chordpathdecoder` is used by `regionanalyzer.cpp` only, `commit()` re-expresses the retiring `advanceTemporalContext` while its forward members are inert Stage-6/wider-beam staging — coupled to the retiring path, shaped for the surviving consumer. The ChordAnalyzerPreferences scoring defaults ARE in `param_manifest.json` (no new manifest gap for this scope, unlike OI-103/OI-106). **The whole-layer pass-2 sweep still owed; L4 certification NOT proposed.** | `cc_l4_audit_pass1_report.md` §3/§4/§7 + `cc_l4_audit_pass1_decoder_report.md` + `cc_l4_audit_pass1_oracle_report.md` + `cc_l4_audit_pass1_satellites_report.md`; `tools/audit/l4/manifest.json`; OI-84; OI-10/OI-29; DT-4 | OPEN — **★ L4 PASS 2 DONE 2026-07-12** (`cc_l4_audit_pass2_report.md`; freeze `d716ac1ca8` = the blinding boundary): blind second reading (121 rows, seed 20260801) + error-rate (40 rows, seed 20260802), all verdicts frozen BEFORE any withheld file opened. **Error rate 0/40 substantive (0.0 %); reading 119/121 concordant; every disagreement diagnosed verdict-axis (0 substantive misses, no class re-opened).** Whole-layer catalog sweep (`gen_signature_sweep.py --layer l4`, all 21 DTs, 2121 rows): **NO untracked correctness defect** — DT-2 reproduces OI-106/OI-103/OI-91, DT-3 reproduces OI-97, DT-5 the dormant decoder, DT-19 the R1/R7 chord→function coupling, DT-16=0, the one DT-12 hit a verified false positive; ONE NEW item **OI-115** (dead `ExtensionFlags::hasNinth` field, DT-5, benign — the L4 twin of OI-96). OI-110 facts + recommendation gathered. **★ L4 SPINE CERTIFICATION PROPOSED (report §6) — proposed, awaiting the user's decision; NOT self-granted, OI-84/EG-7 left OPEN.** The three pass-1 dispositions + the pass-2 sweep are all DONE |
| OI-103 | L4 PASS-1 (L4-2a decoder) constant manifest-gap (DT-2; the **L4 twin of OI-87/OI-91**, feeds OI-6/EG-5): of the ~13 `ChordSliceDecoderPreferences` tunable settings, only `sufficiencyChordTones` (`chordslicedecoder.h:263`) is in `tools/param_manifest.json` (group G11, frozen/dormant). Absent: `topK`, `contextSlices`, `maxContextSlices`, `minHarmonyPcs`, `minDistinctPcs`, `membershipSalienceThreshold`, `membershipReferenceDurationQn`, `membershipPenaltyWeight`, `stepwiseGapToleranceTicks`, `maxEdgeExtendSteps`, `edgeExtendIncrementSlices` (`uncertaintyMargin` is separately OI-28). Dormant (d=0 by construction), fit when the decoder engages — anticipated by the parent report's "OI-91-class" note for the decoder session. | `cc_l4_audit_pass1_decoder_report.md` §4.1/§7.1; `tools/audit/l4/pass1_dispositions_decoder.csv` (9 UNFIT literals); OI-87/OI-91/OI-28/OI-6/EG-5/OI-23/OI-25; DT-2 | OPEN — Stage-5/EG-5 manifest extension |
| OI-104 | L4 PASS-1 (L4-2a decoder) G6 open-question partially-dormant/reserved paths (DT-7 never-fires + declared-dormant reserved field): (a) `AmbiguityKind::SymmetricRotation` (`chordslicedecoder.cpp:967`) fires **0/29080** on the pinned corpus AND has **no dedicated behavior test** — it triggers only on two competing *augmented* rotations at an abstain; the dim7 case awaits the deferred G5 four-note type (relates OI-14); (b) `OpenQuestion::NoteMembership` + `OpenQuestionLabel::contestedPc` (`chordslicedecoder.h:423`) reserved, never populated this increment (always -1) — declared dormancy per the header. Not defects (documented); tracked so they are exercised/verified when G5 / the NoteMembership increment land. | `cc_l4_audit_pass1_decoder_report.md` §4.2/§4.3/§5; `tools/audit/l4/pass1_dispositions_decoder.csv` (branch cpp:966 + field h:423 SILOED); OI-14/OI-15; DT-7/DT-5 | OPEN — verify at the G5 / NoteMembership build (#8) |
| OI-105 | L4 PASS-1 (L4-2a decoder) doc/naming precision (low, non-correctness): (a) `isSemitoneStep` (`chordslicedecoder.cpp:242-246`) returns true for a whole tone too (`|Δpitch| ∈ {1,2}`) — a "melodic step (≤ major 2nd)" test, comment-accurate but name-imprecise (no clean DT match; **below the type-promotion bar — recorded, not promoted**); (b) `tools/param_manifest.json` G11/D9 consuming-path note states the decoder is referenced "ONLY by the dormant L5 function headers … NOT by batch_analyze" but `batch_analyze.cpp` DOES call `ChordSliceDecoder::decode` on `--decode-chords`/`--dump-fullspine` (DT-12). Both comment/data-only. | `cc_l4_audit_pass1_decoder_report.md` §4.5/§4.6/§7.3; DT-12 | OPEN — fix at the next `chordslicedecoder.cpp` / `param_manifest.json` touch (#8) |
| OI-106 | L4 PASS-1 (L4-2b oracle) constant/manifest-publication gaps (DT-2/DT-3; the ORACLE twin of OI-103; feeds OI-6/EG-5, relates OI-23): (a) **three override-registered scoring constants are absent from `tools/param_manifest.json`** — `kWCompletePresenceThreshold` (`chordanalyzer.cpp:142`), `kComplexityEvidenceFloor` (`:143`), `kAugThinEvidenceFactor` (`:144`) are `registerDouble`-registered (`:178-181`) yet not enumerated (sibling `kExtensionThreshold` IS), so the Stage-5 fitter reading the manifest is blind to them (two multiplicatively shape the score); (b) a cluster of **inline extension/presence thresholds is hand-set, unregistered, undocumented** — `0.3` (`:355`,`:375`), `0.2` (`:376`,`:1003`), `0.1` (`:1081`), repeated `0.05` (`:692`/`:704`/`:726`/`:944`/`:1190`/`:1248`) in `detectExtensions`/`structuralPenalties`/`buildChordResult`/`analyzeChord`; the `0.05` presence bar DT-3-duplicates the named `kBassSupportPresenceThreshold`/`kWCompletePresenceThreshold`, and none is documented in `docs/scoring_model.md` §4 (P3 negative-space). | `cc_l4_audit_pass1_oracle_report.md` §1/§2; `tools/audit/l4/pass1_dispositions_oracle.csv` (14 in_param_manifest=no); DT-2/DT-3; OI-103/OI-87/OI-91/OI-6/EG-5/OI-23 | OPEN — Stage-5/EG-5 manifest extension + register/document the inline bars (#8 timing) |
| OI-107 | L4 PASS-1 (L4-2b oracle) `ARCHITECTURE.md` doc drifts vs the code (DT-12/#10, non-correctness; `docs/scoring_model.md`, the authoritative oracle contract per CLAUDE.md, IS in sync — the drift is confined to ARCHITECTURE.md): (a) **`bassNoteRootBonus` documented 0.65** (§4.1 line 1289, §4.1b line 1373) but the code default is **0.70** (`analysistypes.h:177`); (b) the **`Extension` enum bit order** (§4.1 lines 1227-1244) has bits 4/5, 9/10, 14/15 *swapped* vs `chordanalyzer.h:213-230` (in-memory only, no functional impact); (c) **§4.4 mis-locates AnalysisUtils** (`analysis/analysisutils.h` vs actual `analysis/chord/analysisutils.h`) and under-lists it (omits `diatonicMaskFromFifths`/`collectionMask`/`pcInMask`); (d) **§4.1 broadly predates the refactor-1 split** (Gates A–L described as in `chordanalyzer.cpp`; Gate K/M + iteration BIR baselines 21/128 + test counts 407/407 stale). | `cc_l4_audit_pass1_oracle_report.md` §1/§2; ARCHITECTURE.md §4.1/§4.1b/§4.4; DT-12 | OPEN — re-sync ARCHITECTURE.md §4.1/§4.4 to the code at the next doc touch (comment/doc-only, #8/#10) |
| OI-108 | L4 PASS-1 (L4-2b oracle) never-fires + layering notes (DT-7/DT-19, both benign/known): (a) the **augmented-root correction** in `buildChordResult` (`chordanalyzer.cpp:960`, bass-fallback when the aug winner has NO TPC data) fires **0 / 122,047** analyzeChord invocations on the pinned Baroque corpus — its designed population (aug winner + TPC-absent input) is empty in a fully-spelled corpus; NOT dead code (it serves status-bar/synthetic/catalog TPC-absent paths, not fire-measured this pass); (b) **display/voicing declarations live in the analysis contract header** — the `ChordSymbolFormatter` namespace (`chordanalyzer.h:797-835`) + the voicing helpers (`:839-862`) sit in a display-agnostic analysis header (ARCHITECTURE §2.3), already named as backlog tech-debt in ARCHITECTURE §4.1i. | `cc_l4_audit_pass1_oracle_report.md` §1/§3; `tools/audit/l4/pass1_oracle_firecount.json`; DT-7/DT-19; OI-104 (decoder never-fires sibling); ARCHITECTURE §4.1i | OPEN — verify (a) is exercised at a TPC-absent-path fire pass; move (b) to its own TU at the R9 split / #8 timing |
| OI-109 | L4 PASS-1 (L4-2b oracle) orphaned in-code bug marker + a declared inference question (DT-12/#10): `categorizeExtraNote` (`chordanalyzer.cpp:216`) carries `if (rel == 7) return Contradiction; // P5 → not diminished (requires ♭5) // BUG-10` — the **`BUG-10` label resolves to no `OPEN_ITEMS.md`/`DEFECT_TYPES.md`/doc row** (a dangling reference + a #10 register-completeness gap: an issue tracked only in a source comment). **DECLARED to Cowork (not resolved here — auditor, not amender):** the underlying scoring question is whether treating a sounding perfect fifth over a Diminished template as a *hard* Contradiction (−`kContradictionPenalty` 0.75) is correct for every sonority the marker was flagging — an inference-affecting question for the owning layer, not investigated this read-only pass. | `cc_l4_audit_pass1_oracle_report.md` §1; `chordanalyzer.cpp:216`; DT-12 | OPEN — ADJUDICATED (Cowork, 2026-07-11): THIS row is now the one home for the question; the orphaned `BUG-10` label is re-pointed to `OI-109` at the file's next authorized touch (comment-only); the P5-over-diminished scoring question is PARKED to the precision phase (#8 — inference-affecting, owned by the oracle's scoring model, to be examined with a #17 ledger when constants are fit) |
| OI-110 | L4-audit instrumentation lifecycle (Cowork-opened 2026-07-11, from verifying `55829ebe15`): the oracle fire-count instrument is **127 default-OFF lines inside `src/composing/analysis/chord/chordanalyzer.cpp`** (+ a 6-line gated flush in `tools/batch_analyze.cpp`) — authorized by the audit route, byte-identity proven for gated and ungated builds, all suites green. UNDECIDED: **keep** (a permanent, established fire-rate diagnostic — useful for the L4 second pass, the future L5 audit, and the Stage-5 fitter's retained-rule liveness checks OI-36) **or revert** when the layer-4 audit closes (#6/#7 cleanliness — audit scaffolding does not belong in the oracle indefinitely). Decide at the layer-4 second pass's end, with the user. **★ FACTS GATHERED at the L4 second pass (`cc_l4_audit_pass2_report.md` §4), decision STILL the user's:** extent re-confirmed `git diff --numstat 7f57aad4b5 HEAD` = **127/6 lines, zero deletions** (`OracleFireCounters` ~22 counters + ~35 `fbump` sites + dump/flush + atexit in `chordanalyzer.cpp`; fwd-decl `:130` + gated flush `:4620` in `batch_analyze.cpp`). **Measures** per-branch/term FIRE COUNTS of the oracle's documented mechanisms (the P4 "what does it DO" for the live scorer) — nothing else counts internal scoring-term fires (batch output shows only the winning chord). **Re-run** cheap (`MU_ORACLE_FIRECOUNT=<file>` + a corpus `batch_analyze` pass, one appended JSON line; revert-then-re-add is a mechanical byte-identity-proven edit). **Keep cost** 127 default-OFF lines in the oracle TU (#6/#7) + a real R9-split maintenance burden (the `fbump` sites thread through the scoring helpers). **Reuse** plausible: OI-36 (Stage-5 retained-rule liveness) + the L5 audit P4 both want the same fire-count pattern; this L4 second pass did NOT need to re-run it (frozen pass-1 table + code reading sufficed). **CC recommendation (decision remains the user's): REVERT at the L4 audit close on #6/#7 cleanliness (cheaply re-addable), OR — if the fire-rate diagnostic is wanted for the imminent L5 audit / OI-36 — defer the revert to the R9 split and give it a clean diagnostic-TU home rather than leaving audit scaffolding in the live oracle indefinitely.** | Cowork verification of `55829ebe15`; `cc_l4_audit_pass1_oracle_report.md` §3; `cc_l4_audit_pass2_report.md` §4; OI-36 | ✅ DECIDED 2026-07-11 (user-delegated, principle-derived — #7: audit counters are not scoring; keep-until-R9 would be an interim state hanging on the plan's last event; the instrument's commit stays in history so re-adding at Stage 5/OI-36 is one cherry-pick): **REVERT `55829ebe15` at the next dispatch** as its own clean commit, both suites re-proven green (revert of default-OFF code — byte-identity expected trivially). Row closes when the revert commit lands. **✅ CLOSED 2026-07-12 — reverted at `940632ecd1`** (`git revert --no-edit 55829ebe15`, the L5 pass-1 Task-0; 236 deletions across **4** files — chordanalyzer.cpp 127 + batch_analyze.cpp 6 + the two `tools/audit/l4/pass1_oracle_firecount_{run,agg}.py` driver scripts the SAME commit had bundled, which reverting removes with it); build green, composing 1101/1101 + notation + pipeline-snapshot suites all pass. The instrument stays in history at `55829ebe15` for a one-cherry-pick re-add at Stage 5 / OI-36. (Note: the L5-audit-instruction's post-hoc "exactly those two files" check undercounted — the revert touched 4, the extra two being the instrument's own audit driver scripts; `cc_l5_audit_pass1_report.md` §1.) |

| OI-111 | L4 PASS-1 (L4-2c satellites) formatter internal duplicated music-theory tables (DT-3/#6; sibling of OI-92/OI-97): `chord/chordsymbolformatter.cpp` re-stores the same music-theory constant tables in multiple places — (a) the 4 note-name arrays (SHARP/FLAT + German) verbatim in `csfPitchClassName` (`:46-58`) and `csfPitchClassNameFromTpc` (`:88-99`), **already flagged in `ARCHITECTURE.md`** (latent mis-spelling risk, proposed shared helper); (b) the 7-mode diatonic scale-interval table **triplicated** — `csfChromaticRoman` `SCALES` (`:457-465`) + `csfTonicizationScales` (`:788-796`) + the shared key-layer primitive `keyModeScaleIntervals` the sparse refinement already uses; (c) the 21-entry KeySigMode→diatonic-parent map byte-identical in `csfTonicizationParent` (`:799-803`) and the local `CHR_DIATONIC_PARENT` (`:840-844`); (d) the UPPER/LOWER Roman-numeral arrays ×3 (`:466-467`/`:492-493`/`:937-938`). ESTABLISHED values; the duplication is the #6 concern. | `cc_l4_audit_pass1_satellites_report.md` §4; `tools/audit/l4/pass1_dispositions_satellites.csv`; DT-3; OI-92/OI-97 | OPEN — fold into a shared music-theory-table primitive at the formatter's next touch / the shared-primitive decision (#8) |
| OI-112 | L4 PASS-1 (L4-2c satellites) formatter/`ARCHITECTURE.md` doc drifts (DT-12/#10, non-correctness; sibling of OI-107 — `docs/scoring_model.md` IS in sync): (a) §4.3 "File: `chordanalyzer.h`" + the "move `ChordSymbolFormatter` to its own `chordsymbolformatter.{h,cpp}` pair" fix are stale — refactor-1 split the IMPLEMENTATION to `chordsymbolformatter.cpp` while intentionally keeping the DECLARATIONS in `chordanalyzer.h` (the header-location substance is OI-108(b)/§4.1i; no `chordsymbolformatter.h` exists); (b) §4.3 "extensions beyond the 7th (9th, 11th, 13th) are not yet emitted" is contradicted — `csfDiatonicRoman` emits 9/11/13 levels + `(add9/11/13)`, ~129/80/129 per preset in the corpus (verified at code+data, #15); (c) §5.11 aug6 label preset-gating "Standard/Baroque only" is ASSERTED but the formatter DEFERS it (`:882-883` comment "no preset context") — the corpus Jazz-0 aug6 is an upstream-analysis coincidence (`SharpThirteenth` not set), not the documented gate. | `cc_l4_audit_pass1_satellites_report.md` §3/§4; ARCHITECTURE.md §4.3/§5.11; DT-12; OI-107/OI-108 | OPEN — re-sync ARCHITECTURE.md §4.3/§5.11 to the code at the next doc touch (comment/doc-only, #8/#10) |
| OI-113 | L4 PASS-1 (L4-2c satellites) `formatNashvilleNumber` contract-coverage gap (DT-17/P3, display-only Nashville, user-reachable): a non-diatonic (chromatic) root renders as `"?"` (`chordsymbolformatter.cpp:1029-1032`, "refine as needed") and `nashvilleBassSuffix` maps ANY bass by a crude `(bassDegree % 7) + 1` (`:1013-1020`), which mislabels a chromatic bass degree. LIVE via the notation bridge / imploder / context-menu (opt-in Nashville option; `notationcomposingbridge.cpp:493/812/945`, `notationimplodebridge.cpp:1173`), so the gap is user-reachable, not test-only. Not every committed identity has a defined Nashville rendering (chromatic roots ~1 % of corpus regions). | `cc_l4_audit_pass1_satellites_report.md` §3/§4; `tools/audit/l4/pass1_dispositions_satellites.csv`; DT-17 | OPEN — complete the chromatic Nashville degree/bass rendering at the next `chordsymbolformatter.cpp` touch (#8; display-layer, not inference) |
| OI-114 | L4 PASS-1 (L4-2c satellites) enharmonic-spelling normalization premise provenance (DT-11/#17f, display-only, low): the `pc=3`/`pc=8`/`pc=10` + `Cb`/`Fb` spelling-normalization branches in `csfPitchClassNameFromTpc` (`chordsymbolformatter.cpp:108-193`) are notation-convention ASSUMPTIONS justified in comments by **hand-transcribed corpus-survey counts** ("533 sharp-authored pc=8", "155/277", "95/256"; Iter 78/84/89) that are NOT generated artifacts (#17f). Display-layer only — does not affect chord identity/inference — but load-bearing for display spelling correctness. | `cc_l4_audit_pass1_satellites_report.md` §4; `chordsymbolformatter.cpp:108-193`; DT-11 | OPEN — back the thresholds with a regenerable artifact or accept-with-note at the next formatter/spelling touch (#8) |
| OI-115 | L4 PASS-2 dead local field (DT-5, #6/#12, low — the L4 twin of OI-96): the anonymous-namespace struct `ExtensionFlags` in `chord/chordanalyzer.cpp` declares `bool hasNinth` (`:348`) and assigns it once (`f.hasNinth = f.hasNinthNatural \|\| f.hasNinthFlat \|\| f.hasNinthSharp;`, `:408`) but the field is **read NOWHERE** (grep-confirmed: decl + 1 write in the TU, 0 reads, 0 occurrences elsewhere in `src/composing`; `extensionBits` maps the three individual ninth flags, and "has any ninth" is served elsewhere by the free `hasAnyNinth()`). A dead/vestigial LOCAL field (waste), **no runtime effect**. Caught by the pass-2 `DT-5_local_dead` whole-layer sweep; it escaped pass 1 exactly as OI-96 did — a local struct field is not in the cross-layer `l4_fields.csv` inventory (0 rows) and not in `pass1_dispositions_oracle.csv` (0 rows), so the oracle pass could not disposition it. | `cc_l4_audit_pass2_report.md` §3.3; `tools/audit/l4/sweep_results.json` (DT-5_local_dead_field); DT-5; OI-96 (L3 sibling) | OPEN — remove the field + its write at the next `chordanalyzer.cpp` touch (#8 timing; dead-code cleanup, no behavior change) |
| OI-116 | L5 (function) + instruments PASS-1 **feasibility stop + partition** (Task 1.4, the OI-102 analog): the machine inventory (`gen_inventory.py --layer l5`, HEAD `940632ecd1`, corpus `c50002fee1`) is **351 tracked files in scope** (216 `src/composing` + 135 `tools/` instruments domain) → **34 deep-audited files / 3,372 deep rows** (funcs 326 · literals 772 · branches 1,685 · fields 307 · decls 28 · crosslayer 131 · io 123). Populations verified at the code + call sites: **L5-DORMANT** 20 files / 815 rows (the dormant-but-surviving resolver — `function/` minus `harmonicfunctionlayer`, + `progression/progressionrecognizer`); **INSTRUMENT** 13 files / 1,687 rows (the Python measurement chain = the import closure of the regression stops + corpus gen + GT parse + fitting); **INSTRUMENT-HARNESS** 1 file / 870 rows (`batch_analyze.cpp`); plus file-level-only **L5-RETIRES** 3 + **L5-MIXED** 2 + **DEFERRED** 4 (L3-audited substrate / L6) + INSTRUMENT-MANIFEST 1 + INSTRUMENT-TEST 5 + NON-INSTRUMENT 115. The every-row disposition + P3 contract check + P4 characterization are **INFEASIBLE in one session** (L4 needed 3 partition-sessions for ~2,121 rows) → Tasks 2–3 NOT attempted (no silent sampling — protocol totality is the point). **Owed:** the deep pass-1 dispositions partitioned into ~3 sequential sessions (**partition 1** dormant resolver `L5-DORMANT` 815 — attend to what each module reads from the L4/L3 carry + emits; **partition 2** Python instruments `INSTRUMENT` 1,687, establishment form of P2 [#19] — may need a two-way sub-split, regression-stop core vs grading+fitting; **partition 3** the harness `batch_analyze.cpp` 870) + the whole-scope pass-2 signature sweep. **Deferred establishment check owed to partition 1/2:** whether `param_manifest.json` covers the L5 dormant-resolver numeric literals (~183) or they are firewall-deferred per the design's "build it right, do not tune it" (DT-2). **L5 certification NOT proposed — the deep work is owed.** Machine-readable partition: `tools/audit/l5/pass1_partition.json`. **★ PARTITION 1 (DORMANT RESOLVER) DEEP DISPOSITIONS DONE 2026-07-12** (`cc_l5_audit_pass1_resolver_report.md`; freeze `e346f788d4` = the blinding boundary): all **815** `L5-DORMANT` rows verdicted (SURVIVES 396 / PUBLISHED 225 / ESTABLISHED 149 / UNFIT 34 / ASSUMPTION 8 / FACT 3; no DEAD/DUPLICATED, no backward include); P3 contract check BOTH directions; P4 via the test suites (137 tests, all pass; **no `src/` production consumer** — dormant confirmed). **The deferred F-L5-3 param_manifest establishment check is ANSWERED → OI-120** (firewall-deferred; only the θ pair registered). Findings: **OI-118/OI-119** (DT-22 NEW type — signed-rule divergences: modulation confirms on all cadence types; half omits the seventh down-weight), **OI-120** (DT-2 manifest gap), **OI-121** (DT-12 design-doc stale vs the landed §15-12 grammar), **OI-122** (documented/declared items). **★ PARTITION 2a (regression-stop-CORE instruments) DEEP DISPOSITIONS DONE 2026-07-12** (`cc_l5_audit_pass1_instruments_report.md`; freeze `3d7d1cb290` = the blinding boundary; post-freeze `a039fd87df`): per Task-0.3 the 1,687-row INSTRUMENT population was **SPLIT (user-ratified)** into **regression-stop core (7 files / 954 rows — DONE)** + **grading+fitting (6 files / 733 rows — partition 2b, OWED)**. All **954** core rows verdicted (via the reproducible generator `tools/audit/l5/gen_instruments_core_dispositions.py`) **+ 7 synthetic auditor rows** for negative-space findings the ast inventory did not surface → **SURVIVES 866 / FACT 72 / RETIRES 11 / ESTABLISHED 3 / UNFIT 5 / PUBLISHED 4**. P3 contract check vs `CLAUDE.md` gate blocks + `REPRODUCIBILITY`: most guarantees enforced; **two prose-guarantees enforced by nothing → OI-124**. **P4 — the regression-stop pair REPRODUCES BYTE-IDENTICALLY + PASSES clean at HEAD** (`a8` self-validation grid==oracle on 326×3; `robust_stop_diff` OVERALL PASS, +0/-0 run-diff, class-(b) Δ+0 all presets; `characterise_bir_false` `validate_corpus_dir` PASS 352/352 + BIR 52/24/52; `compare_rn` grid reproduced) — **NO highest-rank stop-and-report finding.** 18 findings → **OI-123** (DT-23 NEW — silent-failure paths) / **OI-124** (DT-2 — the two first-rank contract/establishment gaps) / **OI-125** (DT-2 — hand-set measurement tolerances) / **OI-126** (DT-5+DT-3 — dead+duplicated instrument code) / **OI-127** (documented/minor + audit-tool notes). **★ PARTITION 2b (GRADING+FITTING instruments) DEEP DISPOSITIONS DONE 2026-07-12** (`cc_l5_audit_pass1_grading_fitting_report.md`; freeze `b426616ba2` = the blinding boundary): all **733** rows of the 6 grading/fitting files (`analyze_inversion_errors` 122 / `music21_batch` 94 / `oracle_root_metric` 94 / `calibration_fit` 78 / `c1_reliability` 100 / `stage5_fit_driver` 245) verdicted via the reproducible generator `tools/audit/l5/gen_grading_fitting_dispositions.py` → **SURVIVES 505 / ESTABLISHED 214 / UNFIT 13 / DEAD 1** (55 flagged); no audit-tooling rows in scope (all judged blind, no post-freeze section). **P4 (Task-2) reproduce — all 6 ran READ-ONLY (scratch), working tree unchanged:** `stage5_fit_driver fixture` **PASS** (63.36/62.37/63.25 MATCH, batch **52/24/52**); `calibration_fit` maps **byte-identical** to the committed `tools/calibration_maps/`; `c1_reliability` clean (observed median tonicVote 3.5 corroborates `SQUASH_K_CADENCE`); `oracle_root_metric`/`analyze_inversion_errors`/`music21_batch --single` clean. **P3 contract check** vs `CLAUDE.md`/`BUILD_AND_TEST`/`REPRODUCIBILITY`: 3 documented guarantees enforced by nothing (music21 version pin, HEAD-staleness, `.music21.json` producer). **6 findings + ONE NEW type DT-24** (destructive default output path): **OI-128** (DT-23 silent-failure, grading+fitting sibling of OI-123) / **OI-129** (DT-2 the grading chain skips `validate_corpus_dir` entirely, ties OI-35) / **OI-130** (DT-24 NEW + DT-2 destructive default outputs + unenforced music21 pin, producing-side complement of OI-124) / **OI-131** (DT-5+DT-3 fit-manifest consumed-by-nothing + `PARAMS` triple-representation) / **OI-132** (DT-6+DT-3+DT-5 two key-parser paths + value-copies + dead `lt_2`) / **OI-133** (DT-11+DT-12+DT-2 stale docstring figures + dangling anchors + hand-set tolerances). **NO highest-rank stop-and-report finding** (the fixture reproduces clean). **★ PARTITION 3 (the SHARED HARNESS `batch_analyze.cpp`) DEEP DISPOSITIONS DONE 2026-07-12** (`cc_l5_audit_pass1_harness_report.md`; freeze `708d0c3708` = the blinding boundary): all **870** rows (51 fn / 188 literal / 562 branch / 69 crosslayer) verdicted via the reproducible generator `tools/audit/l5/gen_harness_dispositions.py` → **SURVIVES 682 / ESTABLISHED 188** (29 flagged / 6 row-anchored findings). **★ P4 (Task-2) — THE REPRODUCE-CHECK PASSES BYTE-IDENTICAL: a scratch regen of all 3 presets (`run_bach_preset.py` → scratch, corpus read-only) matched the committed `tools/corpus/{baroque,jazz,default}` per-score sha256 EXACTLY, 352/352 each** (the current HEAD binary, rebuilt, size 50006016 vs the corpus's 49971712, reproduces the corpus bit-for-bit — the harness established end-to-end); harness regression test `test_batch_analyze_regressions.py` PASS; probes confirmed strict preset validation, append-flag byte-identity, and the CRLF/LF + exit-code observations. **P3 contract BOTH directions:** every `printHelp()`/`BUILD_AND_TEST.md` flag delivered; **six flags parsed but absent from help** (code→doc). **NO highest-rank stop-and-report finding.** 6 row-anchored findings + 2 doc-adjacent → **OI-135** (DT-3 value-copied harness constants — 21 hand-copied "Default" mode priors + hard-coded onsetBoundaryThreshold 0.25) / **OI-136** (NEW type **DT-25** — undocumented harness measurement modes `--reachback-ab` + 5 `--key-in-*`) / **OI-137** (establishment/reproducibility, #16 — standard-output CRLF vs diagnostic LF + the force-exit-vs-normal-return exit-path asymmetry, both latent) / **OI-138** (DT-12/audit-tooling — stale param_manifest line-refs + broken `--preset jazz` lowercase example + inventory over-capture). **★ THIS COMPLETES THE L5 PASS-1 ACROSS ALL POPULATIONS (partitions 1+2a+2b+3); the whole-scope pass-2 signature sweep + the P6 error rate are the SECOND pass, which decides the certification proposal — L5 certification NOT proposed here.** | `cc_l5_audit_pass1_report.md` §2/§5 + `cc_l5_audit_pass1_resolver_report.md` + `cc_l5_audit_pass1_instruments_report.md` + `cc_l5_audit_pass1_grading_fitting_report.md` + `cc_l5_audit_pass1_harness_report.md`; `tools/audit/l5/manifest.json` + `pass1_partition.json` + `pass1_dispositions_resolver.{csv,json}` + `pass1_dispositions_instruments_core.{csv,json}` + `pass1_dispositions_grading_fitting.{csv,json}` + `pass1_dispositions_harness.{csv,json}`; freeze `0382c3275e` / partition-1 `e346f788d4` / partition-2a `3d7d1cb290` / partition-2b `b426616ba2` / partition-3 `708d0c3708`; OI-84; OI-102 (L4 analog) | OPEN — EG-7; **partitions 1 + 2a + 2b + 3 ALL DONE — the L5 first pass is COMPLETE**. **★ PASS 2 DONE 2026-07-12** (`cc_l5_audit_pass2_report.md`; freeze `20fbc8142d` = the blinding boundary): blind second reading (147 rows, seed 20260901, four-population×kind stratified, all 34 files) + error-rate (40 rows, seed 20260902, uniform over 3372), ALL verdicts frozen BEFORE any withheld file opened (full P2 vocabulary — the OI-100 lesson; `OPEN_ITEMS.md` read deferred to Task 3). **Error rate 0/40 = 0.0 % substantive; every disagreement diagnosed** (verdict-axis field SURVIVES↔PUBLISHED, or tracked hygiene findings the P8 sweep re-finds [OI-135b/OI-136], or the two second-pass finds OI-139/OI-140). Whole-scope 25-DT sweep (`gen_signature_sweep.py --layer l5`, all 3,372 rows, `sweep_results.*`): **NO untracked correctness defect** — DT-2(25)=OI-120 (and mechanically CORRECTS it → OI-139), DT-3(2 real)=OI-135, DT-5(7)=the declared dormancy, DT-23(40)⊇OI-123/OI-128 + the new OI-140, DT-24(1 real)=OI-130, DT-25(6)=OI-136; two NEW rows OI-139 (OI-120 correction + manifest site-drift, DT-12) + OI-140 (WiR-coverage hard-stop gap, DT-23/DT-2). **★ L5 + INSTRUMENTS SPINE CERTIFICATION PROPOSED (report §5)** → **✅ CERTIFIED by the user, 2026-07-12** (on the checked record: both passes at full vocabulary, blinding held, error rate 0/40 blind-first, disagreements diagnosed, sweep clean; residual gaps named/tracked/non-correctness — the OI-140 hard-stop hardening noted as the priority instrument item). **THE OI-84 DEPENDENCY-ORDERED CERTIFICATION PLAN IS COMPLETE** — this row closes; the held OI-43 discussion (mode/key + chord inference — where and how) OPENS |
| OI-117 | L5 PASS-1 file-table mis-tag (**DT-21**, a new instance in the same family as OI-101's chordpathdecoder/sparsechordrefinement): the base file-table tags the whole `analysis/function/` dir "function layer (L5) — deferred to the L5 audit", but **`function/harmonicfunctionlayer.{h,cpp}` is NOT surviving Layer-5 — it is the LEGACY Layer-4 chord-COMPETITION pipeline** (verified at `harmonicfunctionlayer.h:23-53`: *"Competition pipeline — the SINGLE owner of winner selection"*; `applyHarmonicFunction` applies the progression signals, runs the per-bass competition, selects the winner, LIVE via `chordanalyzer`/`regionanalyzer`; its functional labeling is *"E4 (planned)"*, never built). It is on the retirement map as **R1** (legacy competition) + **R7** (rename), so this audit corrects it to `L5-RETIRES` (file-level interpretation-check note only — the #12 note on deletion: it is the SOLE owner of winner selection + progression-signal application, physically retiring joint with the L4 decoder engagement). **Companion structural observation (not a defect):** Layer 5 has **no pure LIVE-and-surviving population** — the only LIVE L5 production code is the key-dependent (circular) `detectCadences`/`detectPivotChords` in `sectioncadencedetection.cpp`+`sectionanalyzer` (reached from `notationcomposingbridge.cpp`), which is the **R2** retirement target (L6 grouping is its rebuild); the surviving function machinery is 100% dormant. This is the expected engagement shape, recorded because the instruction's three-population frame presumed a (c) live-surviving slice that for L5 is empty. | `cc_l5_audit_pass1_report.md` §3 (F-L5-1/F-L5-2); `harmonicfunctionlayer.h:23-53`; roadmap R1/R2/R7 (`docs/implementation_roadmap.md:138-145`); DT-21; OI-101 (L4 sibling) | OPEN — EG-7; the `harmonicfunctionlayer` R1/R7 retirement + the R2 cadence-detector retirement carry the #12 interpretation-check notes (§4); no fix (auditor, not amender) |

| OI-118 | L5 PASS-1 (partition-1 dormant resolver) **signed-rule divergence (DT-22, NEW type)**: `functionmodulation.cpp:52-60` `decideTonicizationVsModulation` counts **ANY** `FunctionalCadence` whose tonic+mode match the span (arrival-in-span) as confirming a modulation — it never checks `c.type`, though §5.3(a)/§5.4 **and the file's own header comment** restrict confirmation to an **authentic or half** cadence. A deceptive/plagal/evaded cadence with a matching tonic would wrongly confirm a key change (a deceptive cadence by definition DENIES the tonic arrival). DORMANT (no `src/` consumer; production byte-identical); a real contract divergence to fix before the modulation path engages. Untested — every `FunctionModulation` test uses `PerfectAuthentic`. | `cc_l5_audit_pass1_resolver_report.md` §3a(1)/§5; `tools/audit/l5/pass1_dispositions_resolver.csv`; DT-22; `cowork_layer5_function_design.md` §5.3/§5.4 | OPEN — add a `c.type ∈ {authentic,half}` gate at the modulation-arbiter engage build (#8; auditor did not amend) |
| OI-119 | L5 PASS-1 (partition-1) **signed-rule divergence (DT-22)**: `functioncadence.cpp:387` `tryHalf` sets `genuineDominant=false` UNCONDITIONALLY, so a seventh (or inverted) half cadence votes EQUAL to a plain-triad half — §5.2 fixes the DIRECTION that a seventh "weakens the reading … down-weighted, not excluded"; the down-weight term is absent (the code comment "a seventh WEAKENS a half (not credited)" describes *neutral*, not weakening). The direction — not just the magnitude — is a §5.2 rule, so its absence is a spec divergence, not deferred firewall tuning. DORMANT; minor. Unimplemented ⇒ untested. | `cc_l5_audit_pass1_resolver_report.md` §3a(2)/§5; DT-22; `cowork_layer5_function_design.md` §5.2 | OPEN — add the half seventh/inversion down-weight term at the cadence engage build (#8) |
| OI-120 | L5 PASS-1 (partition-1) constant manifest gap (DT-2; the **L5 twin of OI-87/OI-91/OI-103/OI-106**; **answers OI-116 / F-L5-3's deferred `param_manifest.json` establishment check**; converges OI-25): of the dormant resolver's 34 firewall seeds, ONLY the `forwardoverride` θ pair (`baseBar`/`confidenceScale`, forwardoverride.h:81-82, group G8) is in `tools/param_manifest.json`. **NOT registered:** the 28 named precision-phase param defaults (`FunctionCadenceParams` wBase/wBassFiveToOne/wLeadingTone/wSeventh/wMetric/**wPhraseBoundary=2.0**/wFinalBar + discountHalf/Plagal/Evaded=0.5; `ModulationParams` baseChangeCost/wDuration/wCadentialWeight/**wSpelling=0.5**; `FunctionOutputParams` wCadenceVote/wLicensedFit/wNextBestMargin/kBoundary; `FunctionResolverParams` wLicensedOut/In/wCadentialFit/**decidingMargin=0.5**; `RecognitionParams` blendRate/admissionThreshold/modeCueFactor/chordsOnlyFactor) **plus** the 6 inline §7 function-confidence magnitudes (`functionresolver.cpp:210/225/232/246/322/338` = 0.25/0.5/1.0) — exactly the OI-25 "L5 firewall 1.0s, θ, inline 1.0/0.5/0.25" set. All are "NOT tuned" firewall seeds, dormant (d=0), load-bearing at engage. **★ CORRECTED by pass 2 (2026-07-12, OI-139): `wLicensedOut`/`wLicensedIn`/`wCadentialFit`/`decidingMargin` AND `maxForwardExtendSlices` ARE registered (`param_manifest.json` G8) — this row wrongly listed the four resolver plausibility weights as unregistered and claimed "ONLY the θ pair" is registered. The true gap is the cadence/modulation/output/recognition seeds (the DT-2 sweep's 25 hits confirm exactly that set); the 5 resolver params are registered-but-with-stale-site-lines (DT-12, OI-139).** | `cc_l5_audit_pass1_resolver_report.md` §1/§3c(8); `cc_l5_audit_pass2_report.md` §4; `tools/audit/l5/pass1_dispositions_resolver.csv` (34 UNFIT); OI-116 (F-L5-3); OI-139 (correction); OI-25/OI-6/EG-5; OI-87/OI-91/OI-103/OI-106; DT-2 | OPEN — Stage-5/EG-5 manifest extension (register the L5 resolver seeds when the resolver-output objective is defined, OI-26); the specific membership corrected per OI-139 |
| OI-121 | L5 PASS-1 (partition-1) doc-sync (DT-12/#10): `cowork_layer5_function_design.md` §5.0 + §15-12 are **STALE vs code** — they read "in force in this spec, **not yet in code** … the code implements the **pre-amendment set** … the code increment is **pending**", but the §15-12 grammar completion **LANDED at commit `2e9a22557e`** (`feat(L5): §15-12 grammar completion — license ascending fifth, descending second, diatonic diminished fifth`): `functionprogression.{h,cpp}` implement `isAscendingFifth`/`isDescendingSecond`/`isDiatonicDiminishedFifth`, OR'd into `isLicensedProgression`, with the D5 consistency test in its tightened "every catalog pair licensed" state (`ProgressionRecognizerD5Consistency.EveryCatalogPairIsLicensed` PASSES). Handoff line 1389 likewise future-tenses the already-landed increment. Cowork-owned docs → surfaced, not fixed (auditor, not amender). | `cc_l5_audit_pass1_resolver_report.md` §3a(3); `cowork_layer5_function_design.md` §5.0/§15-12; `cowork_handoff.md:1389`; commit `2e9a22557e`; DT-12 | OPEN — Cowork re-words §5.0/§15-12 (+ handoff 1389) to "landed at `2e9a22557e`" at the next design-doc touch (#10) |
| OI-122 | L5 PASS-1 (partition-1) documented/declared spec-fidelity items (confirmed at code; no correctness bug; all DORMANT): (a) **Δ0 augmented-resolution exclusion** — `functionprogression.cpp:120-122` `isAppliedResolution` deliberately drops the augmented→major/minor same-root (Δ0) `resolutionEdgeBonus` edge (§5.0 licenses root MOTION); the code flags this "to Cowork" but no adjudication row existed → **Cowork to ratify or restore**; (b) **aug6 ♭6̂-root L4 input assumption** (DT-1-adjacent) — `functionrelationallabel.cpp:118` `tryAugmentedSixth` fires only when `rootPc==tonic+8`, assuming L4 roots a (root-ambiguous) aug6 sonority on the lowered submediant — an L4→L5 input-contract assumption to validate at engage; (c) **minor-key modal-mixture ROLE gap** (DT-17-adjacent) — `functionrelationallabel.cpp:196` `tryModalMixture` tags the role only for a chromatic root or an Ionian quality-altered degree; a quality-altered diatonic-root borrowing in a MINOR key gets no role (label STRING always correct); declared to Cowork; (d) **decision-context stop-(ii) subsumed** — `functionresolver.cpp` `isCutDecision` implements stops (i)+(iii) and folds the punctuation-boundary stop (ii) into (i) for the dormant resolver ("a standalone L1.5 boundary tick is an engage input"); (e) **`combinedBoundary` k>0 guard** — `functionoutput.cpp:131-132` has no guard for `kBoundary<=0` (0/0→NaN); default 1.0 safe, dormant. | `cc_l5_audit_pass1_resolver_report.md` §3b/§3c(9); `tools/audit/l5/pass1_dispositions_resolver.csv`; DT-1/DT-17 | OPEN — (a) Cowork adjudication; (b)/(c)/(d)/(e) verify/complete at the respective engage build (#8; auditor did not amend) |

| OI-145 | ★★ THE KEY-LAYER READINESS GATE (user-directed 2026-07-12): **every open issue that directly or indirectly impacts the key layer must be FIXED before the key layer is built.** The user's rationale, recorded: building on a substrate with identified issues is waste; and fixing may itself surface new issues or unpondered knowledge useful downstream — both outcomes serve the key layer. The dependency closure, enumerated from the register 2026-07-12 (~37 rows), in TWO WAVES + the design-resolved set: **WAVE 1 — the measurement chain** (every key-work probe/fit is graded through it): OI-140, OI-124, OI-129, OI-132, OI-33 (the five blocking), plus OI-123, OI-125, OI-126, OI-127, OI-128, OI-130, OI-133, OI-135, OI-136, OI-137, OI-138, OI-139, OI-35, OI-144 (scoped: graded surfaces only). **WAVE 2 — the substrate + the key layer's own hygiene** (`src/` structural, byte-identical expected): OI-86 (the input-path raw-DOM readers + two metric-weight tables + SpanWindowWeights value-copies + back-edges), OI-13 (the two-pitch-context collapse; two-segmenters as scoped), OI-87 (the L1/L2 constants into the manifest), OI-79, OI-63, OI-92, OI-93, OI-96, OI-98, OI-99, OI-90/OI-101 (file-table reasons), OI-135(the harness mode-prior single-sourcing rides wave 1). **THE CADENCE-ASSET PAIR** (before its votes feed anything): OI-118, OI-119 (+ the OI-122(b)/(e) validations at consumption). **DESIGN-RESOLVED (not blockers — the key-layer work itself fixes them):** OI-75, OI-81, OI-94, OI-78, OI-15, OI-91, OI-97. **Corpus-scope bounds (declared, not gating):** OI-7, OI-38/OI-39. **WAVE 3 — the evidence publication (added at the user's direction 2026-07-12, from `cowork_evidence_inventory.md`):** the evidence facts the key-layer design needs are PUBLISHED by their owning layers before the key layer consumes them (the trapped/dropped ones are #12 information-loss instances: OI-72/OI-73/OI-74/OI-75/OI-81/OI-82; the never-read ones join them: fermatas + phrase facts, bass-motion skeletons, notated accidentals, harmonic rhythm, boundary strength, the collection/tonic split, OI-80 annotations); the facts with NO known consumer yet are published anyway under the CLAUDE.md corollary amendment (the smörgåsbord rule — establishment status on the surface, unvalidated facts never under load). RULE (the register's own gating rule applied): **no key-layer build dispatch opens while a WAVE-1/WAVE-2/WAVE-3/cadence-pair row is open**, unless the user explicitly re-scopes a row out. The design conversation proceeds meanwhile (read-only); the user reads `cowork_key_layer_design_opening.md` WITH THE STATED CAVEAT that fixing may change it. Discovery protocol per fix: any grading digit that moves, any poisoned artifact found, any new issue → its own register row + STOP-and-report, never silently absorbed. | user directive 2026-07-12; the register closure walk (Cowork, this session); `cowork_key_layer_design_opening.md` (the caveat); `cowork_evidence_inventory.md` (wave 3's menu) | OPEN — **★ WAVE 1 SUBSTANTIALLY DELIVERED 2026-07-12** (`cc_measurement_chain_hardening_report.md`; 14 fix commits + fold, `f079a78f6c`→`ad03c56dea`; NO grading digit moved — the establishment battery byte-identical after every fix): CLOSED OI-140, OI-124 (0 poisoned GT of 1056 scanned), OI-129, OI-33, OI-123, OI-128, OI-130, OI-126, OI-35 + the doc-precision set OI-133/OI-138/OI-139 (incl. the fixture's stale hardcoded baseline, corrected — the measurement never changed). **★ THE TWO FIGURE-MOVING REMAINDERS CLOSED 2026-07-13** at the combined re-baseline event (`cc_key_grading_and_calibration_rebaseline_report.md`; O-12 snapshot `23e21da8ea` → landing commits `800f1a12bf` (OI-132) + `b3511fd28a` (OI-144), user-ratified 2026-07-13): **OI-132 CLOSED** — the five dominant-family exotic modes reduce to their PARENT COLLECTION's minor key in the ONE shared reduction, the second parser folded onto it; the KEY columns move (+0.13…+0.49 pp, exactly as the probe predicted) and the ROOT axis is byte-identical (+0/−0 runs, class-(b) Δ+0). **OI-144 CLOSED** — the graded surfaces routed onto the corrected ground truth and the four calibration maps refit; a8 byte-identical; the full corpus regen proves **0 `.ours.json` differ** (the maps are read by two measurement instruments only — the dispatch's "consumed at analysis time" premise was REFUTED at the code and recorded). Two leftovers filed: **OI-150** (the Dor♭2 key-parse abstain) + **OI-151** (the probe's destructive default output path). **WAVE-1 REMAINDER IS NOW THE HARNESS GROUP ONLY:** OI-135/OI-136/OI-137 (one build + corpus-regen-proof dispatch; the mode-prior single-source needs the CLI to read the composing Settings defaults); plus the non-figure-moving OI-125/OI-133 tolerance re-derivations (#19, scheduled later) and OI-127. Waves 2–3 follow; the gate lifts when all listed rows close |
| OI-146 | THE EVIDENCE INVENTORY + LAYER-SPECIFICATION SYNC (user-directed 2026-07-12): `cowork_evidence_inventory.md` is the standing catalog of every hint each layer finds or could find (per layer, with statuses, the key-layer shopping list, and the circularity map — all five alleged circles broken). TWO OBLIGATIONS TRACKED HERE: (a) the inventory is a LIVE document — new evidence kinds discovered in any future work are added to it in the same commit (the OPEN_ITEMS rule applied to evidence kinds); (b) **the `ARCHITECTURE.md` layer specifications are updated from it as evidence facts are adopted** — when the key-layer work completes, each layer's spec section must state its published-facts contract, and THAT becomes the place to look (the user's direction: "when we are done, that is the place to look"). The entry chain for never forgetting any of this: `cowork_handoff.md` (the one entry document) → its standing key-layer work-package block → this row + OI-145. The CLAUDE.md fact-publication corollary carries the user's 2026-07-12 amendment (publish evidence broadly, establishment status on the surface). | user direction 2026-07-12; `cowork_evidence_inventory.md`; CLAUDE.md corollary amendment; OI-145 wave 3 | OPEN — live obligations; the ARCHITECTURE.md sync lands with the key-layer completion |
| OI-147 | ★ DECLARED INFERENCE FINDING (Layer 3; CC surfaced it read-only, does NOT fix — "declare inference problems to Cowork"): the mode-grading adjudication probe's desk-check found a sub-population inside the dominant-family conflict modes where **the engine emits an exotic dominant-family scale label on a span the annotator reads as a plain diatonic chord** — a Lydian-augmented label over an applied leading-tone chord (`bwv282`@7680, our "CLyd+" vs annotator `viio7/ii` in G major), an Aeolian-dominant label over a tonic minor (`bwv382`@22560, our "DMix♭6" vs annotator D-minor tonic), an altered-scale label over a plain V6 (`bwv145.5`@11520, our "D#alt" vs annotator A:V6). These are the "neither rule matches" cells (~33 % of the disagreeing duration on the local column); no grading rule can rescue them because the MODE EMISSION itself is the mismatch. It does NOT change the OI-132 conclusion (Rule B remains best for the genuine dominant-tonicization cases) but bounds how much of the population any grading rule reaches. A Layer-3 mode-decoder precision question (the decoder occasionally prefers an exotic synthetic mode over a plain major/minor reading), inference-affecting — for Cowork to place (relates the OI-43 mode/key inference discussion; distinct from OI-94 which is contract negative-space). **★ A LIVE IN-SUITE INSTANCE FOUND (backlog triage, 2026-07-13, OI-148):** the skipped notation test `PopulateChordTrackEmitsCadenceMarkersOnCorelli` mis-keys the Corelli C-minor ending as **G Phrygian-dominant** (one of the five OI-132 dominant-family modes), under which `Cm→G` reads `iv→I` instead of `i→V` and a spurious plagal cadence marker is emitted at m38 b1 — so OI-147 is no longer only a corpus statistic; it has a reproducible, user-visible, DCML-checked test case. | `cc_mode_grading_adjudication_probe_report.md` §6; `tools/reports/mode_grading_adjudication_probe.json` (population_cells, the `neither` verdicts); `cc_backlog_triage_report.md` §2; `notationimplode_tests.cpp:1708`; OI-132; OI-148; OI-43 | OPEN — DECLARED to Cowork; a Layer-3 inference observation, no fix in this read-only task |
| OI-55 | Ninth-detection gap — melody/harmony conflation (adopted out of the §G submission-era backlog by the backlog triage, 2026-07-13; **STILL REAL, verified at code**). The LIVE ninth verdict is a bare pitch-class weight cutoff — `chordanalyzer.cpp:302-314` (`f.hasNinthNatural = w(2) > extThreshold` etc.; `kExtensionThreshold=0.20` at `:129`) over ONE 12-bin pitch-class histogram, blunted further by a floor (`:1081` `pcWeight[pc] += std::max(0.1, t.weight)` — every sounding tone contributes ≥0.1 however fleeting). **Nothing in that decision knows which voice, staff, or line a note came from**: no non-chord-tone verdict, no membership test, no melody/harmony separation on the production path (the one passing-tone guard, `bassPassingToneMinWeightFraction`, applies ONLY to bass-pitch selection). Live tests confirm the verdict is a tunable cutoff, not a musical judgement — the SAME voicing flips the ninth on/off purely by moving the threshold (`chordanalyzer_tests.cpp:2218` Jazz 0.12 detected vs `:2243` Standard 0.20 not-detected). **The discriminator EXISTS but is DORMANT:** `ChordSliceDecoder::classifyMembership` (`chordslicedecoder.cpp:794-855`) returns `chordTonePcs`/`nonChordTonePcs` through a stepwise-embellishment ladder, and its own comment names this exact bug — *"the passing D heard as Cadd9's ninth"* (`:825`); it has a dedicated test on the dormant path (`decode_chord_tests.cpp:658` `Memb_G2_CVsCadd9Discriminator`). | `cc_backlog_triage_report.md` §1; `chordanalyzer.cpp:302-314/1081`; `chordslicedecoder.cpp:794-855`; `decode_chord_tests.cpp:658`; STATUS post-sub #11 (the origin) | OPEN — **ASSIGNED: the L4 decoder engagement (E4) + the NCT-filter L4 lever (OI-68)**; the membership verdict's publication is OI-73. **Gate: E4 — do NOT fix earlier (#8).** The fix is the membership verdict, not a threshold re-tune |
| OI-148 | ★ THE FOUR KEY-EMISSION xfail NOTATION TESTS — the key layer's ready-made acceptance tests (backlog triage, 2026-07-13; **measured by building and running the suites at HEAD**: `notation_tests` = 53 pass, **4 SKIPPED**). The four — `Notation_ImplodeTests.MozartK279OpeningPrefersCMajorOverFLydian` (`notationimplode_tests.cpp:731`), `Notation_ImplodeTests.PopulateChordTrackEmitsCadenceMarkersOnCorelli` (`:1708`), `NotationInteractionHarmonyPinning.BehaviorSnapshot_RomanNumeral` (`notationinteraction_harmony_pinning_tests.cpp:211`), `BehaviorSnapshot_Nashville` (`:265`) — are `GTEST_SKIP()` xfails charged to ONE root cause: the key-emission regression from `a6b08af3fe` (L3 decoder wiring). **The mechanism is the key layer's own, verified at the code:** the `characteristicPitch` and `trueLeadingTone` scorer terms are HARD-GATED on a `> 0.1` window weight — `keymodeanalyzer.cpp:339-354` (below the cliff the candidate takes the *penalty*, not a reduced boost) and `:374` (`(ltWeight > 0.1) ? trueLeadingToneBoost : 0.0`); C major's leading tone B♮ carries weight **0.093** in the 4-beat decoder window at the K279 opening, just under the gate, so C is denied its anchors and the opening flips to F. That is exactly the emission-model-plus-window defect the OI-141 mechanism report pinned ("the largest loss point, 2/3 traces") and that the design opening's **decision 2** (the emission model — leading-tone term, window treatment) exists to fix. **One of them is a live in-suite instance of OI-147** (the Corelli case mis-keys C minor as G Phrygian-dominant → a spurious plagal marker at m38 b1). **★ THEIR SCHEDULED FIX DOES NOT EXIST:** all four skip messages say "Fix scheduled: L1/L3 stabilization plan Phase 4c", but `cowork_l1l3_stabilization_plan.md` has **no Phase 4c** (its Phase 4 is the tpc spelling capability; STATUS.md's "Phase 4c" is the already-landed `analyzeSection` refactor move `8598cbd245` — a different thing) — so until now these four xfails had **no owner in the register**. The tests' own skip messages carry the standing instruction "**Do NOT refresh the goldens / the expectation** to the wrong reading" — the current expectations are the DCML-correct ones and must be met, not blessed. | `cc_backlog_triage_report.md` §2; `keymodeanalyzer.cpp:339-354/374`; the four skip messages; `cc_keyregression_diagnosis_report.md`; `cc_foundation_stage3b_report.md`; OI-141/OI-145/OI-147; OI-59 (the row whose triage surfaced them) | OPEN — **ASSIGNED: the key-layer build (OI-141 mechanism / OI-145 readiness gate; the emission model + window = design-opening decision 2)**. They are its acceptance tests: four user-visible, in-suite, DCML-checked cases that must go GREEN (never be re-blessed). Re-point the dangling "Phase 4c" schedule anchor at this row at the tests' next authorized touch (comment-only, #8) |
| OI-149 | German-flat-bass slash DROPPED — a surfaced defect tracked ONLY in a source comment (backlog triage, 2026-07-13; the **OI-109 pattern** = a #10 register-completeness gap). `csfIsValidBassNoteName` rejects a German-spelled flat bass name ("Ces"/"Fes" — the `e` is not `#`/`b`), so **the slash is dropped and the bass is LOST from the symbol**: a C-major chord over a C♭ bass in German spelling renders `"C"`, not `"C/Ces"`. Both the current (wrong) behavior and the correct oracle are already checked in: `chordsymbolformatter_branch_tests.cpp:300` `RegressionGuard_GermanFlatBass_SlashDropped` (enabled, pins today's output) and `:318` `DISABLED_GermanFlatBass_ShouldKeepSlash` (the correct oracle, disabled — *"Disabled until the bass-name validator accepts German note names; flagged for Cowork"*) — but **no register row existed**, so the "flagged for Cowork" never reached the one register. User-visible display defect (German locale); DISPLAY-layer only, does not affect chord identity or inference. Sibling of OI-113 (Nashville chromatic-root `"?"`) and OI-114 — the formatter's coverage-gap family. | `cc_backlog_triage_report.md` §6; `chordsymbolformatter_branch_tests.cpp:295-324`; `chordsymbolformatter.cpp` (`csfIsValidBassNoteName`); OI-113/OI-114; OI-109 (the same "tracked only in a source comment" pattern) | OPEN — teach the bass-name validator the German note names, then ENABLE the disabled correct-oracle test, at the next `chordsymbolformatter.cpp` touch (#8; display-layer, not inference) |
| OI-161 | ★ TWO RATIFIED POSITIONS CONTRADICT EACH OTHER ON THE CADENCE→KEY CHANNEL (whole-graph fact-dependency audit, 2026-07-13). `cowork_target_architecture.md` §2 (user-ratified; the corrections recorded 2026-06-21) states the L3 contract as *"key/mode needs only the notes … **NOT** chord symbols, functions, or **cadence detection**"* and records that an earlier draft which *"imported cadence detection into key/mode"* was **wrong** — pointing cadence-based key refinement at **the gated Stage-5 joint step**, which is now **SHELVED on both axes** (OI-43/OI-44). `cowork_key_layer_design_opening.md` **decision 3(b)** proposes precisely the rejected thing (a cadence fact consumable by the key decode) and calls it *"the largest single precision opportunity in this document"*. Both positions are ratified; they cannot both stand. The audit's §5.2 proposal (an L1.5 bass/voice-leading tonic-evidence primitive, below the chord layer) is ONE way to satisfy both — it is not "cadence detection" in the sense the ruling forbade, and it is not the shelved joint step — but the design pass must say so explicitly and amend the ruling's wording, or the contradiction persists. | `cc_fact_dependency_audit_report.md` §2/§5.2; `cowork_target_architecture.md` §2 sub-point 1; `cowork_key_layer_design_opening.md` decision 3(b); OI-43/OI-44 | OPEN — **the design pass must resolve it explicitly** (a user ratification, not a CC fix) |
| OI-162 | The phrase-boundary punctuation cues are **BUILT, not unread** (whole-graph fact-dependency audit, 2026-07-13). `cowork_evidence_inventory.md` §1 statuses fermatas as *"UNDISCUSSED as key evidence; not in the note model's 11 documented facts. Cheap to read; enormous leverage"* and double barlines as *"UNDISCUSSED"*. At the code they are **already read**, by the Layer-1.5 phrase-boundary view: fermatas `phraseboundaryview.cpp:113` (`a->isFermata()`), breath/caesura `:120-123`, mid-score key-signature change `:137-142`, DOUBLE/END/repeat barlines `:163-165`, ritardando-family spanners `:183-189`, all-voice-rest spans ≥ `minSilenceTicks` `:200` (threshold **240**, an OI-87 unfit constant). The view is **DORMANT on production** — its only `src/` caller is `regionanalyzer.cpp:431`, inside `applyJointKeyWiring`, gated at `:1472` on `jointKeyWiringEnabled()` (default OFF, `jointkeydecision.cpp:144-145`). **The gap is wiring + publication, not detection.** Inventory correction owed under OI-146(a). | `cc_fact_dependency_audit_report.md` §3.1/§3.8; `phraseboundaryview.cpp:98-200`; OI-146(a); OI-87 | OPEN — correct the inventory at its next authorized touch (doc-only); the wiring itself is key-layer work |
| OI-163 | **No spelling channel into the Layer-3 emission.** The emission's entire evidence surface is `PitchContext` = `{pitch, durationWeight, beatWeight, isBass}` (`analysistypes.h:930-935`) — it carries **no `tpc`**, though L1 has carried spelled pitch since the note model (`note_model.h:82`). So `cowork_key_layer_design_opening.md` **decision 2(a)** (Temperley spelling-aware profiles — *"the single best-grounded emission improvement"*) has **no input path**: the fact exists at L1 and the consumer exists at L3, and nothing connects them. Strictly FORWARD (L1→L3) — not a cycle, a missing field. Relates OI-15 (spelling as a first-class evidence primitive). | `cc_fact_dependency_audit_report.md` §3.5/§5.1; `analysistypes.h:930-935`; `note_model.h:82`; OI-15 | OPEN — a build item at the key layer (#8 timing: with decision 2(a)) |
| OI-164 | **L5→L6 seam gaps** (whole-graph fact-dependency audit, 2026-07-13; both dormant, neither a cycle): (a) there is **no code edge** from `functionoutput` to `groupinglayer` — `assembleGrouping` takes hand-built `GroupingUnit`s, not `FunctionLayerOutput`; the mapping exists only as a comment (`groupinglayer.h:41-47`) and in `tools/batch_analyze.cpp`; (b) **`GroupingUnit::keyConfidence` has no producer** (`groupinglayer.h:106-109`) — `FunctionConfidence` (`functionoutput.h:85-99`) has no key-confidence field, so a declared consumer input is dangling. Both surface when L5/L6 engage. | `cc_fact_dependency_audit_report.md` §3.7; `groupinglayer.h:41-47/106-109`; `functionoutput.h:85-99` | OPEN — at the L5/L6 engagement |
| OI-165 | ★ **`findTemporalContext` — a LIVE Layer-1.5 → Layer-4 fact cycle with NO retirement row.** `regiontoneprimitives.cpp:451-592`: a Layer-1.5 note-view primitive **instantiates a chord analyzer** (`:465`) and runs full `analyzeChord` on the previous and next segments (`:507-509`, `:565-567`), writing `previousRootPc/Quality/BassPc` + `nextRootPc/BassPc` into a `ChordTemporalContext`. LIVE — it seeds every Pass-1 decoder at `regionanalyzer.cpp:918-919`. It also consumes L3 (`keyFifths`, `keyMode`). **Critically it passes `kDefaultChordAnalyzerPreferences`, whose `scoringPhase` default is `Final`** (`analysistypes.h:386`) — so unlike `greedyExpandSegmentation` it runs chord analysis with **every progression signal active**, and the `ARCHITECTURE.md` §2.14 `ScoringPhase::Segmentation` mitigation **does not cover it**. It is on **no** retirement row (R1–R10); OI-12 tracks it only as an "ownership move", which does not name it as a cycle. **Companion doc-precision item (#10, same file family, no code effect):** `regionanalyzer.h:36` claims `absorbShortRegions` carries "the Iter 78 Fix A `sharesPrevRoot` guard" — it has **no root check** (`regionanalyzer.cpp:239-256` is purely duration-based); the root-aware behavior lives in the separate `coalesceShortSameRootRuns`. | `cc_fact_dependency_audit_report.md` §4.1 (C11) / §6.6b; `regiontoneprimitives.cpp:451-592`; `regionanalyzer.cpp:918-919`; `analysistypes.h:386`; OI-12 (the partial tracking) | OPEN — **needs a retirement decision at E4** (it is not currently scheduled to die); the doc-precision half is a comment-only fix at the next touch |
| OI-166 | ★★ **THE KEY-AGNOSTIC CADENCE PRE-SCAN IS NOT BUILT — the design opening's identification is REFUTED at the code.** `cowork_key_layer_design_opening.md` §1 proposes that the dormant, certified `functioncadence.cpp` *"appears to BE"* the June dossier's called-for pre-scan, and its §5 flags the identification as *"argued from its shape, not yet verified at code"*. **Verified; it fails.** The detector IS key-agnostic (FACT — `detectFunctionalCadences(events, params)`, `functioncadence.h:260-262`, takes no key), but it is **CHORD-DERIVED**: the tonic hypothesis is read off the **Layer-4 committed chord root** — `tryAuthentic` `const int tonicPc = arr.rootPc;` (`functioncadence.cpp:229`), `tryPlagal` `:409`, and `tryDeceptive`/`tryHalf`/`tryEvaded` `norm(root ± 7)` at `:301`/`:358`/`:450` — and every entry gate reads `.quality`. The June dossier specified a **bass-driven, chord-free, tonic-ENUMERATING** pre-scan running *before* `analyzeKeyMode` (`cc_cadence_key_investigation_dossier.md:203-209`: *"root motion (descending-fifth **bass**) and leading-tone resolution; **for each candidate tonic accumulate** a cadential weight"*). Different units. **⇒ `cowork_evidence_inventory.md` §8 circle 2's break ("BROKEN BY THE KEY-AGNOSTIC FORM … what remains is plumbing") tests the wrong axis: key-agnostic ≠ layer-forward.** L3 consuming this vote is L3←L5←L4←L3, a real cycle. **A FORWARD BREAK IS REALIZABLE** — the four chord-free predicates already exist (`eventHasPc` `:38`, `voiceMovesFromTo` `:52`, `dominantTritonePresent` `:72`, `leadingToneResolves` `:117`, all reading ONLY per-voice notes + a candidate tonic) — so the pre-scan is buildable as an **L1.5 derived view** consumed strictly forward by L3, with L5 keeping the cadence typology + modulation confirmation. Trade-offs (the #6 duplication risk; the measured-unknown precision cost of the coarser chord-free vote; the already-sanctioned forward-override alternative) at report §5.2. | `cc_fact_dependency_audit_report.md` §0/§5.2/§6; `functioncadence.cpp:38-75/117-124/229/301/358/409/450`; `functioncadence.h:82-84/260-262`; `cc_cadence_key_investigation_dossier.md:203-209`; `cowork_evidence_inventory.md` §8 circle 2; OI-161 | OPEN — **a STOP before any cadence→key build; the layer placement is the design pass's to ratify.** The design opening's §4(2) cadence-vote probe must be re-scoped to the CHORD-FREE predicate set (measuring the built L5 detector would measure the wrong unit) |
| OI-167 | ★ **THE COLLECTION/TONIC SPLIT HOLDS — CONDITIONALLY.** `cowork_evidence_inventory.md` §8 circle 3's break (the chord layer needs the COLLECTION, not the TONIC) is **upgraded THEORY→FACT for the REBUILT L4**: `ChordSliceDecoder::decideSlice` takes **no key at all** (`chordslicedecoder.h:599-604`); the decoder's only key use is one `analyzeChord` call with `gateCtxOut=nullptr` (`chordslicedecoder.cpp:452-454`), so it **never runs the post-scoring gates**; and its two key-consuming terms — `dim7CharacteristicBonus` (`chordanalyzer.cpp:574-578`) and `diatonicRootContribution` (`:896-907`) — are **pure collection-membership tests** (the constructed pc-set is provably identical for all 7 diatonic modes of one signature, so the oracle cannot distinguish relative major from relative minor). Corroborated by corpus measurement (chord-flip-under-GT fires on 7/8/6 regions = 0.30–0.37 %, `cc_mode_key_chord_probe_report.md`). **BUT two TONIC-dependent sites are LIVE and are NOT in the decoder:** (a) **Gate G-E** (`postscoringgates.cpp:379-385` — the ii/iii/vii *degree* test; firing swaps the winner to a HalfDiminished root, i.e. changes the committed chord; preset-gated `preferMinorOverMajorAdd6`, ON for Baroque) — rides **R1** ("Gates A–L"); G-E is a Gate-G sub-rule so the letter range covers it, but R1's text names letters, not sub-rules — **confirm at E4**; (b) the **Aeolian lone-tonic/dominant guard** (`sparsechordrefinement.cpp:154-159`: a single sounding A under A-Aeolian stays `Unknown`; the same A under C-Ionian — identical collection — hardens to A-minor) — and **`sparsechordrefinement`'s retirement is UNDECIDED** (OI-102 carries "the `sparsechordrefinement` L4/L5 boundary + retire question"; OI-90 re-tagged the file L4). **If that guard survives the engagement, L4 is NOT tonic-independent and every design resting on the collection/tonic split loses its premise (#18).** | `cc_fact_dependency_audit_report.md` §3.6/§5.3/§6; `chordslicedecoder.h:599-604`; `chordslicedecoder.cpp:452-454`; `postscoringgates.cpp:379-385`; `sparsechordrefinement.cpp:154-159`; `cc_mode_key_chord_probe_report.md`; OI-90/OI-102 | OPEN — **a checkable gate the design pass must clear before the collection/tonic split carries load**; resolve with OI-102's `sparsechordrefinement` disposition |

## C. Owned by Stage-5 (precision phase — do NOT fix earlier, #8)

| ID | item | source | status |
|---|---|---|---|
| OI-23 | Tier-2 Class-B mass: ~30 live constants hand-set pre-2026-06-13 against the broken batch gate; only kWStepIn ever robust-unit-fit — retires as the fitter RUNS | premise-debt T2; param_manifest | OPEN |
| OI-24 | O-17: recover the 53 lost F-B corrections via a correctness-correlated signal | fitter O-17; fb §4.4 | OPEN |
| OI-25 | T3-3 dormant placeholder constants (L5 firewall 1.0s, θ, inline 1.0/0.5/0.25, §15-13 null) — load-bearing at engage, fit at Stage 5 | premise-debt T3-3 | OPEN |
| OI-26 | O-13/§15-13 family-4 weight: size-viable but not runnable until L5 engages (objective gap) | fitter O-13 | OPEN — blocked |
| OI-27 | Uncalibratable confidences: L5 mid-range inversion (non-monotone), tonicVote (anti-monotone), L1.5 spread — maps deferred D-8, fixes belong to owning layers | fitter §11 | OPEN |
| OI-28 | `uncertaintyMargin` 0.5 never-fit seed governs decoder abstention (18% duration) — fit at Stage 5; behavior characterized at PC-2 | PC-2 | OPEN |
| OI-29 | §6-block dissolution (OWED #2) incl. S14/S16 quality-from-key gates | roadmap; audit | OPEN |
| OI-30 | Stage 3.3 oracle temporal-signal migration + Gate R basisDep proxy revisit | roadmap | OPEN |
| OI-31 | Stage 6.3 convention-gap buckets (Maj→Dom7 implied sevenths etc.) | roadmap | OPEN |
| OI-32 | O-2 class-(a) weighting revisit trigger (conditional) | fitter O-2 | OPEN — trigger |
| OI-87 | L1/L2 PASS-1 constant manifest-gap list (feeds EG-5/OI-6, DT-2): 16 hand-set inference-affecting constants NOT in `param_manifest.json` — beat-weight table 1.0/0.85/0.75/0.5 (`metricweights.cpp:77-82`), sliding-window LOOKBACK_BEATS 16 / LOOKAHEAD_BEATS 8 / DECAY_RATE 0.7 / LOOKAHEAD_WEIGHT 0.5 (`metricweights.h:57-60`), weightedPcView repetition-boost 0.3 + cross-voice 1.5 (`regiontonecollector.cpp:297/312`), phrase k 1.0 / minSilenceTicks 240 / coincidenceWeight 0.0 (`phraseboundaryview.h`) — [wGap/wInterOnset/wPitch/spikeCeilingFactor ARE in the manifest]. | `cc_l1l2_audit_pass1_report.md` §3c; `tools/audit/l1l2/pass1_dispositions.csv`; OI-6/EG-5; DT-2 | OPEN — Stage-5/EG-5 manifest extension |
| OI-91 | L3 PASS-1 constant manifest-gap (DT-2; the **L3 twin of OI-87**, feeds OI-6/EG-5): the ENTIRE L3 key/mode emission-scoring surface is absent from `tools/param_manifest.json` — the ~60 `KeyModeAnalyzerPreferences` weights/bonuses/penalties (`analysistypes.h`), the 21×5 `ModePriorPreset` mode priors (`modepriorpresets.cpp`), the `KeyModeSequencePreferences` change-cost/window settings, the cadence-anchor `kWeight*` (1.0/2.0/1.0/1.0), local-modulation `kEstablishmentMinChords`=5 / `kPitchTolerance`=2, the partial-sig `kPervasiveFraction`=0.03 / `kDominanceRatio`=2.0, and `JointKeyWeights` — ALL code-labeled "[empirical — Stage-5 fits]". The manifest (Stage-5 fitter Phase-0 inventory; production scope = "live rebuilt L1–L3") lists only the section annotate gate `kAnnotateKeyConfidenceThreshold` (group G10); **0 params sited in key/mode files**. [Cross-check whether `cowork_stage5_fitter_design.md` deliberately defers L3 key-emission fitting; if not, a coverage gap.] | `cc_l3_audit_pass1_report.md` §5 OI-91; `tools/audit/l3/pass1_dispositions.csv` (387 UNFIT); OI-6/EG-5/OI-87/OI-23/OI-25; DT-2 | OPEN — Stage-5/EG-5 manifest extension |

## D. Instrument / measurement layer

| ID | item | source | status |
|---|---|---|---|
| OI-33 | ★ Abstain-aware robust-stop convention OWED before any abstaining path is adoption-gated (metric is abstention-reducible; probe grader = prototype; pinned a8 untouched) | PC-2; cc_eg2 §4.5 | ✅ RESOLVED 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): convention WRITTEN + mechanically ENFORCED. Convention (a8 docstring + robust_stop `manifest.abstain_convention_note`): the ROOT respect (the governing hard stop) counts an abstained cell — OUR region with no `root_pc` — as a DISAGREEMENT, so root is NOT abstention-reducible; the KEY-agree denominator (agree+disagree) EXCLUDES abstained (`keyfail`) cells, so key-agree IS abstention-reducible. Enforcement: a8 publishes `b_key_fail` (the abstain duration) + manifest `key_parse_fail_pct`, and `robust_stop_diff` now reports the key-ABSTAIN coverage BESIDE key-agree per preset and FLAGS a candidate abstain above the reference (compared at the manifest's 4-dp precision — proven: no flag on the clean run, the flag fires on a simulated abstention rise 0.09→6.03 %). Informational reporting only — the pinned a8 root metric and the diff exit code are untouched; battery byte-identical. |
| OI-34 | O-12 corpus git-tracking decision (user call). **★ Carries a deferred obligation, filed 2026-07-13 (the user's defer ruling on the harness group): THE COMMITTED CORPUS IS PLATFORM-DEPENDENT.** All 1056 `.ours.json` are CRLF *only because regeneration runs on Windows* with `QIODevice::Text` (`batch_analyze.cpp`); the same code on Linux writes LF and every `corpus_manifest.json` fingerprint moves (#16). OI-137(a) ESTABLISHED this at the data (all 1056 strictly CRLF; all 1056 would change sha256 if the standard path were opened binary) and deliberately changed nothing — dropping Text mode is a full-corpus RE-BASELINE, not a cleanup. A byte-identical fix exists (open binary and write the CRLF explicitly: reproduces the committed bytes exactly while removing the OS dependence) and was OFFERED, not taken. It belongs to THIS decision: whether the corpus stays git-tracked is what determines whether its line-ending discipline must be OS-independent at all. | fitter O-12; OI-137(a) (`cc_harness_group_report.md`); `cc_oi155_report.md` §6 | OPEN — user |
| OI-35 | O-18 stale fs_* manifests process fix (validate or re-manifest E0 dirs; EG-2 used fresh stamped dumps — generalize) | fitter O-18 | ✅ RESOLVED (read-site validation half) 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): the generalized "validate-or-re-manifest at the read site" rule is applied at the km/fs dump-substrate read (`calibration_fit`) — the substrate's per-carrier stem COVERAGE is checked against the validated corpus and a partial/stale/missing substrate is SURFACED loudly (silent on the full substrate today, so byte-identical). Written down + mechanically enforced at the read site. RESIDUAL: a full manifest-based validate (fingerprints) needs the km/fs PRODUCER to stamp a manifest — the same producer-side change as OI-129's km/fs half; tracked there. |
| OI-36 | O-10 retained-rule liveness counts at every adoption sandwich | fitter O-10 | STANDING |
| OI-37 | CLASS_A_INVESTIGATE advisory: rebuilt-arm dim7 churn +46320 > 9600 tripped read-only (cc_eg2 §2.6) — investigate at E4 design | CLAUDE.md; cc_eg2 | OPEN advisory |
| OI-38 | O-5/Wave-3 corpus onboarding (jazz/pop GT tiers; pedal-dense corpus for OI-4; VL-D census gap). **USER DIRECTIVE (2026-07-12, at the OI-134 debris comparison): at the proper stage, use as many scores/corpora as possible — explicitly including the 53 extra Bach chorale stems the `docs/score_inventory.md` §C3 filter currently excludes, the 22 Beethoven movements, `bwv846`, and whatever other available scores are not yet used.** Timing stays #8/#9-governed (corpus changes re-baseline the regression stops and need GT coverage per score — an onboarding event, not a drive-by); the directive makes the expansion MANDATORY at that event, not optional. **★ Enriched from the OI-56 decision (user, 2026-07-13): the event's method for repertoire WITHOUT formal ground truth = a HUMAN acts as ground truth, deciding by any method including the `llm-triage` judge as guidance (the judge lands here as that guidance instrument, never a grader). The event's target breadth, named by the user: jazz, pop, Schönberg/Wagner/R. Strauss/the Monday Group/impressionism — late-romantic chromaticism through atonality; the TIMING of that breadth is an open scheduling question the user has put up for debate.** Also absorbed here: the 23 unregistered extra scores incl. all 20 `hiromi/` (the OI-57 triage finding). | fitter O-5; roadmap; OI-134 comparison (`cc_l5_audit_pass1_harness_report.md`); user 2026-07-12/13; OI-56 (closed into this event); OI-57 | OPEN — expansion mandated at the corpus-onboarding event; the when-question with the user |
| OI-39 | Overfitting risk: single-composer 326-score split; idiom coverage 1/5 (A-7 marks) | fitter §11 | OPEN debt |
| OI-40 | R-11 conformal / R-12 self-consistency levers (deferred dispositions) | fitter §15 | OPEN — deferred |
| OI-134 | Debris directory `tools/tools/corpus/` discovered on disk (Cowork, 2026-07-12, while verifying the grading+fitting instruments session): **23,613+ untracked files** (git-tracked count 0) with mangled absolute-path filenames (`C__Users_vince_AppData_Local_Python_..._music21_corpus_bach_bwv*.mxl.xml`) — physical evidence that the OI-130 destructive-default-output-path hazard (DT-24) has ALREADY FIRED historically: a batch run executed from inside `tools/` with a relative default output path nested a second `tools/corpus` tree. Not referenced by any register row or report before this one. No repo damage (untracked); disk waste + a contamination trap for path-globbing tools. Cleanup = deleting the directory — a user-authorized deletion, not done unilaterally; the underlying default-path fix is OI-130. | Cowork verification session 2026-07-12; OI-130; DT-24 | OPEN — deletion HELD (user question 2026-07-12: debris, or scores we should have been using?): the files are converted copies of the music21 package's own Bach corpus (canonical source = the Python package, so deletion loses nothing unique), BUT before deleting, the harness session runs the mechanical check — diff the debris stem set against the corpus registry + `docs/score_inventory.md`'s documented exclusions; report any stems present in the debris but absent from the 352 and whether their exclusion is documented or accidental; deletion + any corpus-onboarding implication (relates OI-38/OI-39) decided by the user on that comparison. **★ COMPARISON DONE 2026-07-12 (harness session, `cc_l5_audit_pass1_harness_report.md` §9, READ-ONLY, nothing deleted):** the debris `tools/tools/corpus/` is a strict SUPERSET of the project corpus — **all 352 committed stems present**, plus **53 extra Bach chorale stems** + 22 Beethoven string-quartet movements + `bwv846` (WTC prelude). The mangled filenames literally encode the music21 package path (`…_music21_corpus_bach_bwv<stem>`), so nothing unique is lost by deletion (canonical source = the package). **Every exclusion is documented at the RULE level** in `docs/score_inventory.md` §"Chorale-selection provenance (audit C3)": the `_is_bach_chorale` / `410 → 353` filter (has bwv, **not a variant suffix**, **not a non-chorale BWV**, exactly 4 SATB parts) — spot-checks confirm the 53 extras are variant-suffix (`bwv248.12-2`, `bwv36.4-2`, …) or non-chorale (`bwv846`) exclusions; the Beethoven set is a different repertoire. **DEBRIS, not scores we should have been using** — the extras are deliberately filtered out, not accidentally omitted; expansion is a Stage-5 decision (§C3 + OI-38/OI-39). **Deletion + onboarding remain the user's call; nothing deleted.** → **✅ USER DECIDED 2026-07-12: DELETE the debris.** **✅ CLOSED 2026-07-12** — deleted at the L5 pass-2 Task-0 (`rm -rf tools/tools`; untracked = 0 git-tracked files, so a filesystem deletion with no commit of its own; `tools/tools/` confirmed gone afterward, `git status --short` unchanged by the deletion — `cc_l5_audit_pass2_report.md` §0). **The onboarding half became a standing directive at OI-38** (use as many scores/corpora as possible at the corpus-onboarding event — the 53 extras, the Beethoven set, `bwv846`, and anything else unused). |
| OI-85 | L1/L2-audit Task-0 git-state incident: plumbing commits `eb624d442d`/`7123c7cb55` left the object DB + ref AHEAD of the working tree + main index (staged reversal of the session-36 doc arc + unmaterialized disk on 4 docs); resolved READ-ONLY via `git reset` (unstage) + `git restore` (materialize) with user authorization — nothing discarded (all files == HEAD). Plus a CONCURRENT-EDIT HAZARD: the working tree is live-edited by Cowork (the OI-43 edit; ref moves) — CC staged only its own files and appended (never rewrote) shared docs. Promoted DT-18. | `cc_l1l2_audit_pass1_report.md` §0; DT-18 | OPEN — convention owed (a plumbing commit ends with a main-index refresh + a disk-vs-HEAD verify) |
| OI-89 | L1/L2 pass-2 second reading was only PARTIALLY blind: the pass-2 instruction (Cowork-authored) required `STATUS.md` at Task 0 while `STATUS.md` carried pass 1's headline verdict ("spine is sound") — declared by CC in `cc_l1l2_audit_pass2_report.md` §0. Consequence (user decision 2026-07-11, principle-derived: the audit protocol's second-reading step requires full blinding; #19 forbids trusting a known-defective instrument because its defect "probably didn't matter"): **L1/L2 certification WITHHELD**; remedy = a fresh-session re-run of the blinded second reading + the error-rate measurement under a corrected instruction (`cc_instruction_l1_l2_audit_blind_rerun.md`: STATUS.md/OPEN_ITEMS.md/DEFECT_TYPES.md/handoff/both pass reports all withheld until the blind verdicts are frozen; new seeds; the 40 error-rate rows judged blind FIRST, then compared). Instruction-design failure promoted DT-20. | `cc_l1l2_audit_pass2_report.md` §0; `cc_l1l2_audit_blind_rerun_report.md`; DT-20; Cowork session 2026-07-11 | ✅ RE-RUN DONE 2026-07-11, remedy delivered: fully-blind reading (111 rows, seed 20260712) + error-rate (40 rows, seed 20260713), ALL verdicts frozen at `fbcb59c8d7` before any withheld file opened (strictly stronger blinding than pass 2 — STATUS.md not read until after the freeze). Result: reading 94/111 issue-agree, error-rate 35/40; **every disagreement is verdict-axis (retirement-map / DT-2 constants / DT-19 includes), ZERO correctness misses**; the leak moved the agreement NUMBERS (reading flag rate 23.6 %→1.8 %, error rate 0/40→12.5 %) but NOT the substance — pass 1/2's "spine sound, findings tracked" reproduced independently. **L1/L2 spine certification PROPOSED (report §5)** → **✅ CERTIFICATION GRANTED by the user, 2026-07-11, on the fully-blind re-run — row closed.** |
| OI-100 | L3 pass-2 second reading DEVIATED from the protocol P2 rubric: it judged with a coarse four-label vocabulary (ESTABLISHED / SURVIVES / PUBLISHED / DEAD) instead of the full fine verdict set — declared and crosstabbed by CC (`cc_l3_audit_pass2_report.md` §1–§2), which argued "the per-row prose carries the finer distinctions." User-directed remedy (2026-07-11, principle-derived: that claim is checkable-but-unchecked, forbidden as a load-bearer by #18; the blinding itself HELD and the prose was frozen pre-unblind at `1021e81e44`, so re-measurement is possible without a re-run): **re-derive the fine labels from the FROZEN blind prose ONLY** (no code re-reading, no manifest consult — if the prose does not decide a row, it is marked unresolvable-from-prose, which is itself the measurement), blind to pass-1's labels until the re-derivation is frozen; then crosstab and put the certification decision back to the user. Instruction: `cc_instruction_l3_audit_pass2_relabel.md`. | `cc_l3_audit_pass2_report.md` §1–§2; `cc_l3_audit_pass2_relabel_report.md`; `tools/audit/l3/pass2_fine_relabel_crosstab_*`; CLAUDE.md #18/#19; Cowork session 2026-07-11 | OPEN — **✅ RE-DERIVATION DONE 2026-07-11** (`cc_l3_audit_pass2_relabel_report.md`; freeze `30194061d1`; artifacts `tools/audit/l3/pass2_fine_relabel_*`). **MEASURED — the prose carries the fine label WITHOUT LOSS on the code axis (56/56 SURVIVES) and the derived-facts axis (16/16 PUBLISHED); ALL divergence is on the constants axis.** The pass-2 claim "the prose carries the finer distinctions" is TRUE on those two axes and **OVERSTATED on constants**: the prose recovers UNFIT for only the 2 fitted magnitudes it flags "provisional", leaving **22 fitted magnitudes UNRESOLVABLE-FROM-PROSE** (the coarse ESTABLISHED collapsed the ESTABLISHED-vs-UNFIT split and "empirical/in-bounds" does not restore the UNFIT verdict), and it **over-establishes ~20 `ParameterBoundsMap` range-endpoint / inline / inert-zero literals** that pass 1 flags more conservatively (UNFIT/DEFERRED). Crosstab (156): **113 CONCORDANT / 21 GENUINE-DISAGREEMENT / 22 UNRESOLVABLE** — the 21 disagreements = **1 real defect** (`extraToneScore` dead field, **OI-96**, independently reproduced from the prose) + **20 benign constants-classification differences** (no code defect; pass 1's more-conservative UNFIT/DEFERRED is the one to rely on). **21/22 unresolvable constants are covered by the DT-2 manifest sweep (OI-91)**; the 1 uncovered = `ReachBackOptions::maxReachSteps=8` (dormant/default-OFF, inert on production; the DT-2 struct-name patterns miss it, and pass 1's constant-classifier calls bound-endpoints "emission defaults" — both OI-95-class tooling-precision notes, zero conclusion impact, EG-5/OI-91 manifest extension should sweep up dormant-config bounds). **L3 certification proposal STILL STANDS, weakened only by these named+bounded+covered gaps.** → **✅ L3 CERTIFICATION GRANTED 2026-07-11 — the user delegated the decision to the principle-derived recommendation (every protocol condition met on checked fact; Cowork verified the inert-gap and dead-field claims at the code). Row closed.** |
| OI-95 | L3-audit tooling debt: (a) **#6** — `tools/audit/l3/gen_l3_dispositions.py` is a SECOND disposition generator alongside the L1/L2 `tools/audit/gen_dispositions.py` (blinding forbade extending the withheld original during pass 1); unify into ONE layer-selected generator the way `gen_inventory.py --layer` was, now that blinding is lifted; (b) the committed `tools/audit/l1l2/{l1l2_functions.csv,l1l2_decls.csv,inventory.json}` are LINE-STALE vs current `note_model.h` (rows shifted +4; substance byte-identical — proven at the L3 inventory build) — a re-gen restamp owed at a convenient L1/L2 touch. | `cc_l3_audit_pass1_report.md` §5 OI-95 + §1; `cc_l3_audit_pass2_report.md` §4a; #6 | OPEN — housekeeping; (a) unify generators [**the SWEEP-tool part IS discharged (L3 pass 2): `gen_signature_sweep.py` now takes `--layer` — one instrument; `--layer l1l2` reproduces the L1/L2 counts identically. The DISPOSITION generator `gen_l3_dispositions.py`↔`gen_dispositions.py` unification remains OPEN**], (b) restamp l1l2 artifacts |
| OI-123 | L5 PASS-1 (partition-2a instruments) **silent-failure paths (DT-23, NEW type)**: broad/bare `except` clauses (and one error→wrong-bucket fold) in the measurement chain that discard or mis-attribute data with no skip-counter/surfacing — the family the corrected `dcml_parser` P0 fix exists to prevent: `dcml_parser._compute_root_pc:189` `except Exception: return None` + `_resolve_dcml_key:451` `except Exception: return globalkey` (a systematic numeral/key mis-parse silently reduces GT root coverage — region reads as "no GT root"); `compare_rn.score_piece:514` / `grid_score_piece_tsv:655` bare `except: return None` (a corrupt ours.json/tsv drops the WHOLE piece from the aggregate, no counter — vs `dcml_parser`'s `skipped`-collector); `a8.measure_preset:290` corrupt `.ours.json` folded into the `no_wir` count (conflates parse-failure with no-annotation). The dead `parse_dcml_file:108` still carries the pre-P0 bare-except (dormant, OI-126). Latent TODAY (the runs pass clean, the failure population is empty) but a real silent-shrink risk for the gate denominators. | `cc_l5_audit_pass1_instruments_report.md` §3 (F3/F4/F10/F12); DT-23 (founding = the P0 58.9 % drop) | ✅ RESOLVED 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): the regression-stop-core broad catches are NARROWED to the load/parse types + SURFACED. `dcml_parser._compute_root_pc`/`_resolve_dcml_key` bare `except Exception` narrowed to `(AttributeError,IndexError,KeyError,TypeError,ValueError)` so a systematic parser bug PROPAGATES (no longer silently returns None/globalkey). `compare_rn.score_piece`/`grid_score_piece_tsv` whole-piece drops narrowed + a named stderr surface (a corrupt piece can no longer silently shrink the corpus with no signal). **The `no_wir` mis-classification is CLOSED:** `a8_rebaseline_measure` narrows the `.ours.json`/`.music21.json` load excepts and counts a corrupt `.ours.json` in its OWN named bucket (`coverage.ours_load_fail`/`ours_load_fail_stems` + `empty_ours` + `m21_load_fail`), never folded into `no_wir`, with a loud stderr surface. Battery byte-identical (all new counters 0 on the clean corpus; a8 root/class-(b)/calib/validate/BIR 54/24/54 unchanged). |
| OI-124 | L5 PASS-1 (partition-2a) **first-rank establishment/contract gaps (DT-2, #19; the P3 "prose guarantee enforced by nothing")**: (a) `characterise_bir_false.validate_corpus_dir:50` + `run_bach_preset._write_manifest:77` fingerprint (sha256) ONLY `.ours.json`, NOT the paired `.music21.json` GROUND TRUTH (`music21_version` is copy-through informational, explicitly "NOT enforced") — so `REPRODUCIBILITY.md` C2's "the `.music21.json` are canonical as-committed; regenerating is a deliberate re-baseline" is enforced by NOTHING; a stale/foreign/version-mismatched GT export (read by a8's variant-(a) genuine filter + the BIR gate) passes the contamination guard undetected; (b) `robust_stop_diff.ref_class_durs:90` reads the reference class-(b) baseline from the RE-KEYED `manifest.json` (the separate R10-a assembly) not the sibling `summary.json` (a8's direct output, identical schema to the candidate), and never cross-checks the manifest figure against the run-enumeration it ships beside — a partial re-baseline of one artifact but not the other compares against a stale baseline silently (byte-consistent TODAY: manifest `class_b_root_disagree_dur` == a8 `summary.json` `b_cls_b_dur`, all 3 presets). | `cc_l5_audit_pass1_instruments_report.md` §3 (F14/F15/F16) + §5 (U1/U2); `REPRODUCIBILITY.md` C2; DT-2; OI-35/OI-34 | ✅ RESOLVED 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): (a) `run_bach_preset._write_manifest` now fingerprints the paired `.music21.json` GROUND TRUTH (music21_size/music21_sha256) beside the `.ours.json`; `validate_corpus_dir` checks it (backward-compatible — a manifest predating the stamp skips the check); the 3 local corpus manifests re-stamped with the GT fingerprints (provenance-preserving surgical re-manifest — git_hash c50002fee1 kept, `.ours.json`/`.music21.json` untouched); tamper-detection proven (a 1-byte GT edit → CorpusValidationError). A mandated poisoned-GT scan of all 1056 committed `.music21.json` found **0 error-field / 0 zero-region / 0 unparseable** — no OI-123/OI-128-shape poison present (no discovery). (b) `robust_stop_diff` now cross-checks the reference manifest's `class_b_root_disagree_dur` against the sibling `summary.json` b_cls_b_dur (catches a partial re-baseline → FAIL) and surfaces WiR-source drift; the reference is internally consistent (all 3 presets manifest==summary). Also added: a8 publishes `coverage.wir_source_sha256` (the WiR SOURCE identity, offset-file pattern, paired with the committed offsets_file_sha256), re-stamped into the reference manifest. NO grading figure moved — battery byte-identical (root/class-(b)/calib/validate unchanged). |
| OI-125 | L5 PASS-1 (partition-2a) **hand-set measurement tolerances (DT-2, #19)**: two measurement-decision constants in `compare_analyses.py` are hand-set with documented rationale but NO independent derivation/oracle establishment — (a) the `0.5` region-alignment overlap tolerance (lenient-OR ≥50% of EITHER duration; `:216`/`:516`/`:549`), governing the batch-stop + secondary-metric + oracle-root-metric alignment (NOT the robust grid, which unions boundaries and needs no overlap threshold); (b) the `4 * tpb` hardcoded 4-beats-per-measure assumption in `_dcml_tick_for`'s extrapolation-beyond-anchors fallback (`:449`/`:453`) — a silent approximation for non-4/4 meters on the WiR/rntxt path (which lacks `abs_tick`). The `CLASS_A_INVESTIGATE_TICKS=9600` advisory is separately ESTABLISHED-advisory (relates OI-37). | `cc_l5_audit_pass1_instruments_report.md` §3 (F5/F6); DT-2; OI-37 | ✅ RESOLVED (single-home + note half) 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): the hand-set comparator tolerances are CENTRALIZED into one named, commented constants block in `compare_analyses.py` with provenance — `ALIGN_OVERLAP_FRACTION`=0.5 (the region-overlap ≥50%-of-EITHER-duration tolerance; both alignment sites now reference it), `ALIGN_BEAT_DISTANCE_TOL`=0.5 (the measure-anchored beat-distance tolerance — split out as its own named constant, semantically distinct from the overlap fraction), `EXTRAPOLATION_BEATS_PER_MEASURE`=4 (the silent 4/4 assumption in `_dcml_tick_for`'s beyond-anchors extrapolation). Each carries a `[hand-set; re-derivation flagged]` note and the standing statement that these are grading conventions (NOT fittable scorer constants → NOT param_manifest rows). Byte-identical (same values; battery + BIR 54 unchanged). RESIDUAL: the actual re-derivation / oracle-establishment of the values (#19) is later work, flagged per constant. **ESTABLISHMENT 2026-07-13** (OI-145 wave-1 instrument-hygiene sweep, `cc_instrument_hygiene_sweep_report.md`, #19) — **two of the three are ESTABLISHED as DERIVED, not hand-set; the third is measured correct but its RULE stays an assumption.** **`ALIGN_OVERLAP_FRACTION` = 0.5 — ESTABLISHED (a definition, nothing to re-derive):** two spans name the same harmonic event iff one of them spends the MAJORITY of its life inside the other, and 0.5 is the majority boundary — the only value for which "majority" means majority. Two properties fall out, and both are the behavior we want: with our region covered by exactly TWO ground-truth regions the >=0.5-of-OURS leg always succeeds BY PIGEONHOLE (two parts of a whole cannot both be under half), so the pair never silently drops; and the OR (either duration, not both) is what keeps a segmentation OVER-GRAB aligned and therefore GRADED as an error, where requiring both halves would let over-grabbed regions vanish from the denominator — i.e. flatter the score. **`ALIGN_BEAT_DISTANCE_TOL` = 0.5 — ESTABLISHED:** it is half the spacing of a 1.0-beat grid, i.e. the nearest-neighbour (Voronoi) radius — the unique tolerance for which every on-grid beat matches its NEAREST beat and nothing else (larger admits a non-nearest beat; smaller leaves a dead band). **`EXTRAPOLATION_BEATS_PER_MEASURE` = 4 — NOT established; the value is right, the rule is not.** MEASURED over the committed corpus: the extrapolation branch is **LOAD-BEARING, not inert — it fires 162 times across 15 stems** (overwhelmingly the pickup measure, which DCML numbers 0 while our regions start at 1), and on **every one of those 15 stems the measure length DERIVED from the piece's own anchors is exactly 4.0 beats** — so the constant is correct wherever it currently fires and no committed figure rests on luck. But it hard-codes 4/4 and is simply WRONG for any other meter (a 3/4 pickup at beat 3 is placed a full beat early), and the corpus that would break it is one we plan to add (OI-38/OI-39). Documented in full at the constant. **★ THE EXTRAPOLATION HALF IS NOW RESOLVED 2026-07-13** (the user's ruling: apply the derivation now — `cc_wave1_finalize_report.md`): `_dcml_tick_for` extrapolates beyond the outermost anchor using the measure length DERIVED from each stem's own anchors (the new `_derive_ticks_per_measure` — the median tick distance per measure over consecutive anchored measures, robust to the pickup and to a truncated final measure), the same quantity the INTERPOLATION branch already derives from the two anchors it sits between. The shared graded resolver is therefore meter-correct for the non-4/4 repertoire we plan to add (OI-38/OI-39), and the silent 4/4 assumption is gone from every firing. **PROVEN BYTE-IDENTICAL, and the prediction was written first (#17b):** re-measured before the edit, the branch fires **162 times across 15 stems** and the derived measure length is **exactly 4.0 beats on every firing** ⇒ **0 of 162 resolved ticks change**; the establishment battery afterwards reproduces every governing surface to the digit (a8_diff +0/−0 on all three presets, class-(b) duration Δ+0, coverage 326/326/326, calib 4/4 sha256-identical, validate 3/3, and all 159 leaf fields of the a8 summary — root/RN/key-home/key-local included — unmoved). `EXTRAPOLATION_BEATS_PER_MEASURE` survives ONLY as the last resort for a stem too thinly anchored to derive a measure length (fewer than two anchored measures): **measured 0 of 352 stems on any preset**, so it never fires today; it is kept so such a stem still resolves its onset rather than silently dropping the ground-truth row from the denominator (#12). **The measurement is REPRODUCIBLE, not hand-transcribed (#17f):** `tools/cc_oi125_extrapolation_probe.py` (committed, read-only, writes nothing) reprints all five figures on demand. **The behavior is PINNED by 7 new tests** (`TestDcmlTickExtrapolation` in `tools/tests/test_metric_primitives_l0l1.py`, suite 127 → 134): the derivation; its robustness to a short pickup and a truncated final measure; the `None` when fewer than two measures are anchored; **a 3/4 stem's pickup — the exact case the superseded constant placed a full beat early, now placed correctly** (so the meter-correctness claim is TESTED, not asserted); the 4/4 agreement with the old constant (the corpus byte-identity in unit form); and that the interpolation branch is untouched. **THE ROW STAYS OPEN ON ONE NAMED REMAINDER, shared with OI-133(c):** the two genuinely load-bearing `calibration_fit` gates that are still hand-set and NOT established (#19) — the **min-cell 50/20** fit-at-all floor and the **`NEAR_LOGISTIC_TOL` 0.05** Platt-vs-isotonic tie-break — each carrying the concrete experiment the hygiene sweep named. Neither is a desk exercise; they are scheduled at the calibration / Stage-5 work, not here. |
| OI-126 | L5 PASS-1 (partition-2a) **dead + duplicated instrument code (DT-5 + DT-3, #6/#12)**: (a) `dcml_parser.parse_dcml_file:76` is a SUPERSEDED DCML-TSV parser with NO live consumer (referenced only in its own docstring example; superseded by `parse_abc_harmonies_file`, still carrying the pre-P0 bare-except) and `find_dcml_file:475` has ZERO references anywhere — both DEAD (DT-5, the function-level analog of OI-96/OI-115's dead fields); (b) the note-name→pitch-class mapping is DUPLICATED across 3 sites — `dcml_parser._NOTE_TO_PC:124`, `compare_rn._KB_NOTE_DCML:222`, `_KB_NOTE_OURS:223` (the `_KB_` pair a declared verbatim port of `key_confound.py`) — one concern, three copies (DT-3, sibling of OI-92/OI-97/OI-111). | `cc_l5_audit_pass1_instruments_report.md` §3 (F1/F2/F9); DT-5; DT-3; OI-96/OI-115; OI-92/OI-97/OI-111 | ✅ RESOLVED 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): (a) both DEAD functions DELETED — `dcml_parser.parse_dcml_file` (superseded by `parse_abc_harmonies_file`, no live consumer; verified via repo-wide grep = only self/docstring/audit refs; its dormant pre-P0 silent-drop goes with it) and `find_dcml_file` (zero references anywhere); the stale module-docstring usage example re-pointed to `parse_abc_harmonies_file`. (b) the note-name→pitch-class map single-sourced — `compare_rn._KB_NOTE_OURS = dcml_parser._NOTE_TO_PC` and `_KB_NOTE_DCML` a lowercase-keyed view of it (was 3 verbatim copies). Byte-identical: the maps produce identical values, battery unchanged (a8 key parsers consume `_KB_NOTE_*`). |
| OI-127 | L5 PASS-1 (partition-2a) documented/minor + audit-tooling notes (no correctness bug): (a) `compare_analyses._QUALITY_NORMALISE:128` — an unmapped quality string passes through unnormalised (potential false quality (dis)agreement; completeness unproven vs both producers' vocabularies; DT-2-mild, F7); (b) `compare_analyses._load_region:99` `root_pc` default `-1` sentinel — two regions both missing a root compare equal in the two-way `_roots_match` → a false chord agreement (narrow correctness edge, no clean DT — recorded not promoted, F8); (c) `compare_rn.normalise_rn:120` strips ALL parenthetical figures on both sides — documented leniency that can mask an extension difference in the exact/partial split (F11); (d) `a8.cell_class:97` — the class-(a)/(b) split (the hard-stop-governing quantity) inherits the RN-bucket self-validation transitively but has NO independent second implementation cross-checking the class split (DT-2-establishment note, F13); (e) `gen_inventory.py` mis-resolves `run_bach_preset.py:198 import platform` (stdlib) to `tools/platform.py`, tagged `instrument` — a crosslayer-extraction false positive in the audit's OWN tool (DT-21-family). **Also (#6):** this session's committed disposition generator `gen_instruments_core_dispositions.py` is another instance of **OI-95(a)**'s disposition-generator proliferation (each partition commits its own — the established per-partition reproducibility pattern, adding to the "unify into one layer-selected generator" debt). | `cc_l5_audit_pass1_instruments_report.md` §3 (F7/F8/F11/F13) + §10; DT-2/DT-21; OI-95(a) | OPEN — address each at its file's next touch (#8); (e) correct the `gen_inventory` crosslayer resolution / unify the disposition generators under OI-95(a) **PARTIAL 2026-07-13** (OI-145 wave-1 instrument-hygiene sweep, `cc_instrument_hygiene_sweep_report.md`) — **(a), (b) and (e) CLOSED, all three proven byte-identical by MEASUREMENT before the edit, not by assertion.** **(b) the `-1` root sentinel:** `_load_region` defaults a missing root to `-1`, NOT to the `None` the OI-52 abstain convention is written in, so two rootless regions compared EQUAL and scored a chord AGREEMENT — a false agreement that FLATTERS the metric. `_roots_match` now settles an unresolved root on EITHER side the way `compare_rn.classify_pair` settles it: not an agreement. Byte-identical — measured over the committed corpus (352 stems x 3 presets, 33,296 aligned pairs, the same denominator `roots_agree` cites) an unresolved root occurs on NEITHER side, **0 times**, so the guard fires on no real pair; it makes the false agreement STRUCTURALLY impossible instead of merely empirically absent (#19). **(a) `_QUALITY_NORMALISE` completeness — ESTABLISHED by measuring BOTH producers' vocabularies:** ours emits exactly the 8 strings `qualityToString()` can return (all mapped) and music21 emits 5, of which `Unknown` (5,856 corpus regions) is the no-quality marker and is deliberately NOT mapped. A pin test now parses `qualityToString()` and asserts every string it can emit is a map key, so a NEW `ChordQuality` cannot land and pass through unnormalised; and `_quality_matches` now refuses the both-`Unknown` pair (two abstains are not a match — the sibling of the root sentinel; byte-identical, OUR side never emits `Unknown`). **(e) the `gen_inventory` crosslayer false positive — CLOSED at the CLASS, not the instance:** the hand-listed `_PY_STDLIB_MODS` silently mis-resolved every stdlib module it forgot (`import platform` -> a nonexistent `tools/platform.py`, tagged an instrument dependency). The stdlib set is now ASKED OF THE INTERPRETER (`sys.stdlib_module_names`) and an edge is only claimed when the resolved file ACTUALLY EXISTS. Verified by regenerating the L5 inventory to scratch: the phantom `platform` row is gone and **no legitimate edge is dropped** (the committed audit CSVs are frozen pass-1 evidence and were deliberately NOT regenerated). **STILL OPEN: (c)** `normalise_rn` parenthetical-stripping leniency and **(d)** the `a8.cell_class` split having no independent second implementation; plus the OI-95(a) disposition-generator unification. |
| OI-128 | L5 PASS-1 (partition-2b grading+fitting) **silent-failure paths (DT-23, the grading+fitting sibling of OI-123)**: 15 broad `except` swallows drop a score/region/value with NO skip-counter/surfacing — `analyze_inversion_errors.py:149` (WiR parse `except: pass`), `oracle_root_metric.py:159/169` (load→continue / wir→[]), `c1_reliability.py:169/187/196/237/246/292`, `calibration_fit.py:57/77/86/122/131`. **Most consequential: `music21_batch.analyze_chorale:199` — a `chordify()` failure returns 0 regions + an `error` field that IS SILENTLY WRITTEN to `{stem}.music21.json`**, so that score contributes an empty ground truth (removed from the oracle denominator) with no stderr. Latent today (runs clean, failure population empty); a real silent-shrink risk for the GT denominators. | `cc_l5_audit_pass1_grading_fitting_report.md` §5; `tools/audit/l5/pass1_dispositions_grading_fitting.csv` (15 silent-failure-swallow); DT-23; OI-123 | ✅ RESOLVED 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): **the most consequential fold — the chordify-failure FAKE-GT write — is CLOSED:** `music21_batch.process_chorale` now detects the `error` field a failed `chordify()` produces and writes a FAILURE RECORD (`{stem}.music21.FAILED.json` + a loud stderr) and returns None — NO `{stem}.music21.json` GT is written, so a failed GT production can no longer masquerade as a valid 0-region ground truth that silently drops the score from the denominator. The grading/fitting load excepts are narrowed to `(OSError,ValueError,KeyError,TypeError)` + surfaced: `oracle_root_metric.load_dir` (whole-stem drop named), `analyze_inversion_errors` (already counts skipped_no_regions), `c1_reliability` (6 load excepts) + `calibration_fit` (5) narrowed. Byte-identical on the clean corpus (no failure population; the fake-GT scan in OI-124 found 0 poisoned GT; battery + BIR 54/24/54 unchanged; calib maps sha256-identical). |
| OI-129 | L5 PASS-1 (partition-2b) **grading/fitting chain skips the anti-contamination guard ENTIRELY (DT-2, worse than OI-124; ties OI-35)**: `calibration_fit` (`collect_l3:65`/`collect_l4:108`) and `c1_reliability` (`measure_l3:177`/`measure_l4_l5:223`) read BOTH the corpus (`tools/corpus/{preset}`) and the km/fs substrate WITHOUT `validate_corpus_dir` — the guard the metric tools enforce (whose `.music21.json` blind spot is OI-124). Worse than OI-124: the km/fs dump substrate has NO manifest at all (a bare dir under `C:/tmp/c1`), so a stale/foreign/preset-crossed substrate feeds the reliability curves + the committed calibration maps undetected. The concrete grading-chain instance of OI-35 (the O-18 "validate or re-manifest the fs_ dirs" process fix). | `cc_l5_audit_pass1_grading_fitting_report.md` §4/§6; DT-2; OI-124/OI-35 | ✅ RESOLVED (validate_corpus_dir half) 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): `calibration_fit` (per carrier, before `collect_l3`/`collect_l4`) and `c1_reliability` (per preset, before `measure_l3`/`measure_l4_l5`) now call `characterise_bir_false.validate_corpus_dir` on `tools/corpus/{preset}` before reading it — the same guard the metric tools enforce (now including the OI-124 `.music21.json` GT fingerprint) — so a stale/foreign/preset-crossed corpus can no longer silently poison the committed calibration maps or the reliability curves. Byte-identical: the 4 calibration maps reproduce sha256-identical, c1 runs clean. RESIDUAL (tracked, not closed): the km/fs dump substrate under `C:/tmp/c1` is still a bare manifest-less scratch dir — manifesting it needs a producer-side stamp (a deeper fitting-tool touch, #8); the corpus-read guard is the mandated OI-145-wave-1 half. |
| OI-130 | L5 PASS-1 (partition-2b) **destructive default output paths + unenforced music21 pin (NEW type DT-24 + DT-2; the PRODUCING-side complement of OI-124)**: `music21_batch` default `--output tools/corpus` (`main:515`→`process_chorale:439`) OVERWRITES the committed canonical corpus; `calibration_fit` default `--out-dir tools/calibration_maps` overwrites the committed maps; `stage5_fit_driver split` overwrites the committed `tools/stage5_split_registry.json`. Combined with the unenforced music21 v9.9.1 pin (`music21_batch` checks no version; `run_bach_preset` records it "informational — not validated", REPRODUCIBILITY C2), a no-arg `music21_batch` run silently RE-BASELINES the exact `.music21.json` GT that OI-124 showed is unfingerprinted — nothing stops it at produce time, nothing detects it at consume time. (The `fit`/`audit` appends to `tools/fit_ledgers` are BY DESIGN, O-8 — not flagged.) | `cc_l5_audit_pass1_grading_fitting_report.md` §5; `tools/audit/l5/pass1_dispositions_grading_fitting.csv` (io-write-committed); DT-24 (NEW); DT-2; OI-124/OI-34 | ✅ RESOLVED 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): the DT-24 family is closed at the PRODUCING side — every instrument now defaults its output to SCRATCH, so a bare run can never overwrite a committed artifact: `music21_batch --output` → scratch (was `tools/corpus`), `calibration_fit --out-dir` → scratch (was `tools/calibration_maps`), `stage5_fit_driver split --out` → scratch (was the committed `tools/stage5_split_registry.json`). Re-baselining a committed artifact must now name it EXPLICITLY (as REPRODUCIBILITY.md documents). The music21 v9.9.1 pin is ENFORCED at run start where GT is produced: `music21_batch` refuses (exit 2) if `music21.__version__` != the pinned `MUSIC21_PIN` unless `--allow-version-mismatch` is passed (proven: a simulated 9.0.0 blocks; the installed 9.9.1 passes). Byte-identical: `--single` works, the battery passes explicit-path scratch out-dirs, calib maps sha256-identical. |
| OI-131 | L5 PASS-1 (partition-2b) **fit-manifest integrity — validated by nothing, consumed by no code + triple-represented (DT-5 + DT-3; answers the instruction's fit-manifest-integrity question)**: `tools/param_manifest.json` is read by NO code (grep of `tools/*.py` + `src/composing` clean) — the Stage-5 fitter that would consume it (`stage5_fit_driver`) carries its OWN copy, the `PARAMS` dict (`:65-147`, 49 params), so the fit surface is TRIPLE-represented (C++ source = truth / `param_manifest.json` / `PARAMS`), one concern three paths (#6/DT-3). The ONLY cross-check is the driver `fixture` aggregate-root reproduce test, which catches a wrong `PARAMS` value ONLY for params that materially move root-% — the many zero-sensitivity params (per `param_manifest.json`) can drift undetected. DISTINCT from OI-91/OI-120/OI-103/OI-106 (params MISSING from the manifest); this is the manifest's OWN consumption + integrity (a published fact whose only consumer, the fitter, bypasses it). | `cc_l5_audit_pass1_grading_fitting_report.md` §5; DT-5; DT-3; OI-91/OI-120/OI-6/EG-5 | OPEN — single-source the fit surface (generate `PARAMS`/the manifest from the C++ source, or the manifest from `PARAMS`) at the Stage-5/EG-5 manifest work (#8) |
| OI-132 | L5 PASS-1 (partition-2b) **duplicated derivation + value-copied constants + dead code (DT-6 + DT-3 + DT-5, sibling of OI-126)**: (a) DT-6 — TWO key-parsing paths for one concern: `oracle_root_metric.parse_our_key/parse_dcml_key/parse_m21_key` (`:102/119/128`, its KEY tiers) vs `compare_rn._our_key_tonic/_dcml_key_tonic` (`:273/260`, the robust-unit key-agree) — two implementations of "key string→(tonic,mode)"; a divergence makes the two instruments disagree on key correctness (oracle_root_metric's docstring calls its parsers a verbatim port of a diagnostic, not the compare_rn path). (b) DT-3 — value-copied constants: `analyze_inversion_errors.INVERSION_SUSPICION_MARGIN=0.70` (`:322`) copies the production pref `inversionSuspicionMargin` (silently stale if the pref moves); `music21_batch.TICKS_PER_QUARTER=480` (`:71`) copies MuseScore `Constants::DIVISION`. (c) DT-5 — `analyze_inversion_errors:214` `lt_2` computed, never used (dead). | `cc_l5_audit_pass1_grading_fitting_report.md` §5; `tools/audit/l5/pass1_dispositions_grading_fitting.csv`; DT-6/DT-3/DT-5; OI-126 | 🔶 PARTIAL + ★ DISCOVERY D2 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): (c) DONE — dead `lt_2` DELETED (byte-identical; it was computed at `analyze_inversion_errors:214` and read nowhere). (a) DT-6 key-parser fold is a **DISCOVERY, NOT byte-identical** — A/B measured on all 3 presets: `oracle_root_metric.parse_dcml_key` IS byte-identical to `compare_rn._dcml_key_tonic`, BUT `parse_our_key` is NOT interchangeable with `_our_key_tonic` — the two embed DIFFERENT music-theory readings of the dominant-family exotic modes (PhrygDom + alt: MAJOR in parse_our_key vs minor in _our_key_tonic; Lydb7: None vs major). Swapping in the shared substrate MOVES oracle_root_metric's KEY-tier split (**jazz +1 / default +13 records** shuffling KEY-HARD/KEY-TONICIZATION/AMBIGUOUS; the charged/floor ROOT sets — the primary 3878/4084/3910 metric — are UNCHANGED on all 3 presets). Because `compare_rn` is ALSO the substrate the RATIFIED a8 key-agree column (71.29/67.49/70.52) uses, folding the other direction would move the ratified column instead — **neither fold direction is byte-identical**. The fold needs a music-theory adjudication (which mode reading is authoritative for the dominant family) + a coordinated re-baseline; NOT done silently — a prominent note is added at `parse_our_key`. (b) DT-3 value-copies (`INVERSION_SUSPICION_MARGIN`=0.70 copies the C++ pref, `TICKS_PER_QUARTER`=480 copies MuseScore `Constants::DIVISION`) are cross-language copies that cannot be Python-single-sourced — documented RESIDUAL. **[SUPERSEDED — see the DT-3 close at the end of this row]** **OI-132 STAYS OPEN on the DT-6 adjudication + DT-3.** **★ THE MODE-GRADING ADJUDICATION PROBE DELIVERED 2026-07-12** (`cc_mode_grading_adjudication_probe_report.md`; `tools/mode_grading_adjudication_probe.py` + `tools/reports/mode_grading_adjudication_probe.json`; READ-ONLY, no graded-pipeline change — the two candidate rules computed side by side): the disagreeing population is the 5 dominant-family conflict modes (PhrygDom/alt/Lydb7/Lyd+/Mixb6), **0.509 / 0.706 / 0.480 %** of graded duration (baroque/jazz/default; all under 1 %). Establishment PASS: the baseline column reproduces the committed a8 `summary.json` counters exactly, and with the population excluded both rules == baseline exactly. **The evidence supports RULE B (parent collection):** on the LOCAL column Rule B matches the DCML annotators on **67 %** of the disagreeing duration (per mode 54–100 %; PhrygDom 72 %), **Rule A matches 0 %**; Rule B raises key-agreement **+0.13…+0.49 pp** per column (Rule A ≈ 0) and fixes the Lydb7/Lyd+/Mixb6 KEYFAIL artifact. The ONE Rule-A win is the HOME column of Lydian dominant (67 % vs 22 %, ~2 880 ticks). Expectations: (1) MET, (2) PhrygDom-≥60 % MET / "altered-Lydian-dom lean the other way" largely FAILED (Rule A is 0 % on local for every mode; only Lydb7-on-home leans A), (3) MET (max \|A−B\|=0.486 pp < 0.5). Consolidating to Rule B is a re-baseline event (O-12 snapshot + ratification + robust_stop re-stamp), NOT byte-identical — as D2 stated. **RULING PENDING WITH THE USER; a Layer-3 inference finding surfaced → OI-147 (same commit).** **✅ RULED by the user, 2026-07-12: PARENT COLLECTION, uniformly, all five modes.** The consolidation executes at the combined re-baseline event (together with the OI-144 calibration-map refit): one shared reduction in the one substrate, both figure moves under one ritual (O-12 snapshot, explained diff, ratification pause). Row closes when that event lands. **The user's companion direction — "with the Baroque preset, only ordinary major and minor high-priority" — is recorded at the design opening's state-space decision (decision 6), NOT hand-applied now:** the mode-INVENTORY decision precedes any prior adjustment, priors are then FIT (not hand-set again) at the fitting stage against the local column, per the readiness-gate sequencing; the Default preset's inventory/priors are deferred pending the corpus-expansion evidence (OI-38/OI-39 — no ground truth beyond Bach exists to establish them against). **✅ RESOLVED (DT-6 half) 2026-07-13 — the consolidation LANDED (`800f1a12bf`) and is USER-RATIFIED** (`cc_key_grading_and_calibration_rebaseline_report.md`): the parent-collection reduction is implemented ONCE, in the shared substrate `compare_rn._our_key_tonic` (`_KB_PARENT_COLLECTION_MODES` — PhrygDom −7, Mix♭6 −7, Lyd♭7 −5, Lyd+ −3, alt +1, all to MINOR), and `oracle_root_metric`'s second parser is folded onto it (its divergent copy DELETED; `parse_our_key`/`parse_dcml_key` are now thin adapters). **MEASURED, and every figure landed on the probe's written prediction to the digit:** key-agree HOME 71.29→**71.4182** / 67.49→**67.8274** / 70.52→**70.6514**, LOCAL 65.72→**65.9900** / 62.49→**62.9805** / 65.39→**65.7093** (Baroque/Jazz/Default); key-abstain 7680/10800/33120 → **0/4080/2400** ticks. **The ROOT axis is byte-identical:** run-diff **+0/−0** on all presets, class-(b) hard-stop duration **Δ+0**, root/RN columns unchanged (66.04/64.98/65.93 · 46.33/44.10/46.23), WiR coverage 326/326/326 — the hard stop PASSES. **Establishment:** the probe's own harness reports `ruleB Δ+0.0000` against the new baseline (the implementation IS Rule B) and `excl-population ruleA==ruleB==baseline: OK` on all three presets (**nothing outside the five modes moved**); a8's counters reproduce the probe's cell-by-cell durations exactly. `oracle_root_metric`'s charged/floor ROOT sets are SET-IDENTICAL before/after (3878/4084/3910 charged); only its KEY tiers shuffle (5/9/11 identities, all OUT of the key-error tiers). Suites green (composing 1101/1101; notation 53+4 skipped; pipeline-snapshot 11+1 skipped — no golden refresh owed). Reference re-baselined + manifest re-stamped programmatically from the a8 summary (#17f); outgoing reference snapshotted first at `tools/robust_stop/snapshot_2026-07-13_pre_oi132_oi144/` (O-12). **RESIDUAL (b) DT-3** (the two cross-language value-copies `INVERSION_SUSPICION_MARGIN` / `TICKS_PER_QUARTER`) stays as documented — it cannot be Python-single-sourced. **A leftover surfaced and filed: OI-150** (the Dor♭2 key-parse abstain the shared regex still rejects — deferred by the user, with the parent-collection reduction target recorded). **✅ RESOLVED (DT-3 half — the LAST open half) 2026-07-13** (OI-145 wave-1 instrument-hygiene sweep, `cc_instrument_hygiene_sweep_report.md`): the two cross-language value copies cannot be single-sourced from Python, so they are **mechanically PINNED instead** — `tools/tests/test_cross_language_constants.py` (NEW) PARSES the producing C++ declaration and asserts the Python copy equals it, the same producer-parsing pattern the OI-155 vocabulary test and the OI-135 sync test use: `analyze_inversion_errors.INVERSION_SUSPICION_MARGIN` (0.70) vs the `inversionSuspicionMargin` default in `src/composing/analysis/types/analysistypes.h`, and `music21_batch.TICKS_PER_QUARTER` (480) vs `Constants::DIVISION` in `src/engraving/types/constants.h`. A drift in either production value now turns the guard RED instead of silently mis-attributing every inversion blocker / mis-scaling every music21 tick alignment. `INVERSION_SUSPICION_MARGIN` was hoisted from inside `main()` to module scope so the guard can see it (same value, same single use — byte-identical). Battery byte-identical (a8_diff +0/−0 ×3, calib 4/4, validate 3/3); 127 Python tests green. **OI-132 IS NOW FULLY CLOSED** (DT-6 at the ruling, DT-5 earlier, DT-3 here).|
| OI-133 | L5 PASS-1 (partition-2b) **stale docstring figures + dangling anchors + hand-set grading tolerances (DT-11 + DT-12 + DT-2, sibling of OI-125/OI-127; documented/minor)**: (a) DT-11 — `oracle_root_metric` docstring (`:61-62`) cites per-record charged 3882/4083/3914 & deduped 3862/4065/3894, but at HEAD the tool produces **3878/4084/3910 & 3858/4066/3890** (the −4/+1/−4 L3-wiring-delta drift) — hand-transcribed measurement figures, not a generated artifact; (b) DT-12 — `stage5_fit_driver` docstring (`:12-13`) describes the constraint as "no NEW class-(b) batch-stop case (vs 53/24/53)" but the CODE uses the a8 robust-unit mapping (correct, CLAUDE.md block A, which SUPERSEDED the batch stop) and the live gate is **52/24/52** (the fixture prints batch=52/24/52) — prose lags on both unit and integer; `analyze_inversion_errors:321` comment cites "chordanalyzer.cpp ~1916-1923 (seventh-exemption guard)" but the file is 1610 lines (dangling anchor); `:104` empty-input error prints "No .ours.json files in tools/reports/corpus/" but reads `corpus_dir` (wrong path); (c) DT-2 — 13 UNFIT hand-set grading tolerances with no cited provenance: beat window ±0.26 (`analyze_inversion_errors:273-276`), noteCount≥3 (`:260/289/292`), margin≥2.0 "likely correct" (`:215`), near_logistic 0.05 (`calibration_fit:219`), min-cell 50/20 (`:199`), FloatingKey ±4 (`music21_batch:193-194`) — grading conventions, NOT `param_manifest` rows (sibling of OI-125). | `cc_l5_audit_pass1_grading_fitting_report.md` §3/§5; `tools/audit/l5/pass1_dispositions_grading_fitting.csv`; DT-11/DT-12/DT-2; OI-125/OI-127 | ✅ RESOLVED (figures + anchors) + ★ DISCOVERY D1 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): **★ D1 — the OI-142/OI-143 re-baseline left the stage5 fixture's self-check STALE:** `stage5_fit_driver`'s hardcoded `RATIFIED` root baseline (63.36/62.37/63.25) was not updated at the adoption (`d9b52ba969`), so at HEAD the fixture DETERMINISTICALLY reproduced the correct user-ratified 66.04/64.98/65.93 yet self-reported MISMATCH/FIXTURE:FAIL. Corrected to the ratified reference (the a8 measurement never changed — a stale-figure sync, NO grading digit moved); the fixture now self-reports MATCH/PASS (verified). The docstring constraint prose (unit + integer, "vs 53/24/53" batch stop) re-worded to the a8-robust-unit framing (batch diagnostic now 54/24/54, superseded). (a) `oracle_root_metric` docstring hand-transcribed figures (3882/… drift) REPLACED with a pointer to the tool's own regenerable output (#17f). analyze_inversion_errors: the dangling `chordanalyzer.cpp ~1916-1923` anchor made symbol-led, and the empty-input error now prints the dir actually read (was a hardcoded wrong path). (c) the 13 scattered hand-set grading tolerances → flagged for re-derivation (RESIDUAL, like OI-125). Byte-identical: battery + fixture PASS. **ESTABLISHMENT 2026-07-13** (OI-145 wave-1 instrument-hygiene sweep, `cc_instrument_hygiene_sweep_report.md`, #19) — **the (c) half splits three ways, and the audit's own classification of it was too broad.** **(i) NOT grading tolerances at all — no derivation owed (the majority):** the +/-0.26 beat window, the `noteCount>=3` grouping and the margin cut-points (0.25/0.5/1.0/1.5/2.0, incl. the "likely correct" >=2.0 label) in `analyze_inversion_errors` are **HISTOGRAM BUCKET EDGES and REPORT LABELS in a printed diagnostic** — they classify nothing and NO figure is graded through them. Established as such and documented at the sites; the +/-0.26 radius is now the named `BEAT_BUCKET_RADIUS` and its just-over-a-sixteenth width is explained (it pulls the sixteenths immediately adjacent to a beat into that beat's bucket, which is the question the diagnostic asks). **(ii) GENUINELY load-bearing, and left OWED rather than justified after the fact:** `calibration_fit`'s min-cell floors (50 fitting / 20 held-out — they decide whether a calibration row is fitted AT ALL) and `NEAR_LOGISTIC_TOL` (0.05 — it decides whether a row ships a Platt curve or an isotonic one). Both are now NAMED constants carrying an explicit `[hand-set; NOT established]` note plus **the concrete experiment each would take** (min-cell: sweep the counts and find the floor where the fitted curve stops moving under resampling; near-logistic: show held-out ECE is insensitive to the tie-break across the band where the two curves agree). Neither is a desk exercise, so neither was forced. **(iii) `music21_batch` "FloatingKey +/-4" — NOT A TOLERANCE. It is unreachable dead configuration, and establishing it surfaced a Class-B instrument failure -> OI-158.** Byte-identical: battery a8_diff +0/-0 x3, calib 4/4, validate 3/3. **OPEN — narrowed to the two `calibration_fit` gates** (min-cell 50/20 + near-logistic 0.05), each with the experiment above as its gate. The diagnostic bucket edges are CLOSED (established as not-tolerances); the FloatingKey item moves to OI-158. |

| OI-135 | L5 PASS-1 (partition-3 harness) **value-copied inference-affecting constants (DT-3, #6/#17f; harness sibling of OI-63/OI-92/OI-97/OI-126/OI-132)**: (a) `batch_analyze.cpp:196-216` `applyPreset()`'s **"Default" branch hand-copies the 21 app mode-prior defaults** from `composingconfiguration.cpp` (init() MODE_PRIOR_* setDefaultValue), sync enforced ONLY by a code comment ("KEEP IN SYNC") — no mechanical check — and the values are NOT in `tools/param_manifest.json` (chord-scoring scoped); if the app config drifts, the "Default" preset silently mis-measures the live out-of-box config (the config OI-63's mode-prior single-sourcing would fix; the NAMED presets read the single-source `modePriorPresets()`, correct). (b) `batch_analyze.cpp:597` (also `2555`/`3718`) hard-codes `onsetBoundaryThreshold = 0.25`, duplicating the `IComposingAnalysisConfiguration` default the user-facing bridge reads (fallback 0.25) — documented latent divergence (roadmap 0.6): coincide today, no mechanical sync, so a config-default change makes batch stop measuring the user pipeline; not in `param_manifest.json` (a Layer-2 segmentation constant). Both production byte-identical (reproduce-check PASS). | `cc_l5_audit_pass1_harness_report.md` §3.1/§3.2; `tools/audit/l5/pass1_dispositions_harness.csv` (default-preset-mode-priors-hand-copied ×21 + onset-boundary-threshold-hardcoded ×3); DT-3; OI-63/OI-87/OI-91; roadmap 0.6 | ✅ RESOLVED 2026-07-13 (the harness group, `cc_harness_group_report.md`) — SINGLE-SOURCED, not sync-tested: the true #6 fix was available because `composing_analysis` is the one library both the app and `batch_analyze` link (the same reason `modePriorPresets()` was moved there). (a) the 21 app defaults now live ONCE, in `mu::composing::modePriorAppDefaults()` (`analysis/key/modepriorpresets.{h,cpp}`), read by BOTH `ComposingConfiguration::init()` (which registers them as the MODE_PRIOR_* settings defaults) and `batch_analyze`'s "Default" branch — the hand-copied literal block and its "KEEP IN SYNC" comment are gone, and `applyPreset()`'s second copy of the 21-line assignment block folded into one `applyModePriors()` helper. Value-identity was PROVEN BEFORE the edit (a machine diff of the two literal blocks: 21/21 identical, zero value differences). (b) `analysis::kDefaultOnsetBoundaryThreshold` (the dependency-free leaf `analysis/types/analysistypes.h`) is now the ONE source, read by `AnalyzeRegionsOptions::onsetBoundaryThreshold`, by `ComposingConfiguration::init()`'s ONSET_BOUNDARY_THRESHOLD registration, and by the harness's 3 sites — so a change to the config default now moves the harness WITH it, closing the roadmap-0.6 divergence (batch silently measuring a pipeline nobody runs). **BYTE-IDENTICAL, PROVEN AT THE FULL SURFACE:** the recompiled harness regenerated all 352×3 `.ours.json` **sha256-identical to the committed corpus (0 differ)**; battery PASS (a8_diff +0/−0 all presets, class-(b) Δ+0, calib 4/4, validate 3/3); suites green (composing 1103/1103 — the 2 new contract tests; notation 53+4 skipped; pipeline-snapshot 11+1 skipped, NO golden refresh; batch_analyze regressions passed). Two contract tests added establishing the documented claim that the app defaults are NOT "Standard" (they diverge on exactly 11 of the 21 modes, agree on the other 10) and are not a sixth named preset. **RESIDUAL (named, not silently closed):** `notationharmonicrhythmbridge.cpp:85` still carries a 4th literal `0.25` as its `cfg == nullptr` fallback — a one-line change to read the same constant, NOT made here because that file is outside this dispatch's authorized edit scope. |
| OI-136 | L5 PASS-1 (partition-3 harness) **undocumented measurement modes on the shared instrument (NEW type DT-25)**: `batch_analyze.cpp` parses `--reachback-ab` (`:3982`) + the five `--key-in-neither/-keysig-only/-candidate-only/-both/-leading-tone` emission-weight flags (`:4093`) in `main()`, but `printHelp()` lists NEITHER and `BUILD_AND_TEST.md` defers to `--help` — documented only in in-code comments. A runnable measurement path on the shared harness its own contract does not surface (the reverse of DT-17). Both are default-off diagnostic-only paths (`--reachback-ab` returns before analyzeScore, HELD for L3 reach-back activation; `--key-in-*` mutate a LOCAL keyPrefs copy inside `--decode-keymode` only), so production is byte-identical — contract-completeness, not behavior. | `cc_l5_audit_pass1_harness_report.md` §3.3/§4/§5; `tools/audit/l5/pass1_dispositions_harness.csv` (flags-absent-from-help); DT-25 (NEW); OI-105(b) | ✅ RESOLVED 2026-07-13 (the harness group, `cc_harness_group_report.md`): all six flags added to `printHelp()` — `--reachback-ab` (the L3 reach-back A/B, stated as read-only, returning before analyzeScore, and as NOT the activation ratification) and the five `--key-in-neither/-keysig-only/-candidate-only/-both/-leading-tone` emission-weight overrides (grouped with the other bounded-sweep flags, stating that they apply to a COPY of the key prefs inside `--decode-keymode` only, so the corpus stays byte-identical). Verified at the built binary's `--help`. Doc/contract-only: no parsing, no default, no measurement path changed — the corpus regen is 352×3 sha256-identical and the harness regression test passes. |
| OI-137 | L5 PASS-1 (partition-3 harness) **output-discipline + exit-path robustness (establishment/reproducibility #16, both LATENT — not defects today)**: (a) the standard output file is opened `QIODevice::Text` (`batch_analyze.cpp:4593`) → on Windows the committed `.ours.json` corpus is written **CRLF**, while every diagnostic path writes `std::ofstream(std::ios::binary)` (**LF**) — two line-ending disciplines in one instrument; corpus byte-identity (reproduce-check PASS) holds only while regeneration stays on Windows with Text mode (a foreign-OS regen would silently differ; compounds the O-12/OI-34 corpus git-tracking decision); (b) the standard path FORCE-EXITS (`::TerminateProcess`/`std::_Exit`, `:4615-4621`, bypassing static destructors to dodge a Qt-TLS hang) while the ~10 diagnostic early-return paths return NORMALLY through that same destructor sequence — an exit-discipline asymmetry; whether the diagnostic returns are exposed to the "some runs" hang is UNESTABLISHED (NOT observed — `--validate-slices`/`--decode-chords` exited 0 cleanly this session, but `--validate-slices` returns a MEANINGFUL 0/2 a crash could mask). | `cc_l5_audit_pass1_harness_report.md` §3.4/§3.5/§6; `tools/audit/l5/pass1_dispositions_harness.csv` (standard-output-crlf-vs-diagnostic-lf + exit-path-asymmetry); #16; OI-34 | ✅ ESTABLISHED-AND-DOCUMENTED 2026-07-13, NO CODE CHANGE (the harness group, `cc_harness_group_report.md`) — both halves were measured, and the measurement says do not touch either. **(a) line endings — the prediction was confirmed at the data, then declined:** all **1056** committed `.ours.json` (352 stems × 3 presets) are strictly CRLF (no bare LF, no bare CR), and **all 1056 would change sha256** if the standard path were opened binary — so dropping `QIODevice::Text` is a full-corpus RE-BASELINE, not a cleanup, and is out of scope for a byte-identical dispatch. The diagnostic writers were NOT "aligned" to it either: they already use `std::ofstream(std::ios::binary)`, which is the OS-INDEPENDENT discipline, so aligning them to the standard path would SPREAD the platform dependence rather than remove it (the correct alignment direction is the other one, and that is the re-baseline). Both facts are now in the code at the open site. **The live hazard, stated:** corpus byte-identity holds only while regeneration stays on Windows — the same code on Linux writes LF and every `corpus_manifest.json` fingerprint moves (#16; compounds OI-34). A byte-identical fix EXISTS and is offered to the user, not taken unilaterally: open binary and write the CRLF explicitly, which reproduces the committed bytes exactly (provable by the regen gate) while removing the OS dependence. **(b) exit-path asymmetry — established as LOAD-BEARING in both directions, not an oversight:** the standard path may force-exit because it has already flushed and closed everything by hand (`cout.flush()`+`fflush(stdout)`, or `outFile.flush()`+`close()`), so skipping the destructors cannot truncate it, and every error return happens BEFORE it (verified at the binary: unreadable score → exit 1, so the force-exit masks no failure code). The ~10 diagnostic paths may NOT adopt it: they rely on `std::ofstream`'s destructor at scope exit to write their output, which `_Exit`/`TerminateProcess` would SKIP — truncating it — and `--validate-slices` returns a MEANINGFUL 0/2 that a force-exit(0) would swallow. So unification is unsafe in either direction; the reason is now recorded at the force-exit. Verified side by side at the built binary on one score: standard → exit 0, 1704 CRLF / 0 LF; `--validate-slices` → exit 0, 0 CRLF / 17 LF, output complete. Whether the diagnostic returns are exposed to the same "some runs" Qt-TLS hang stays UNESTABLISHED (never observed); the code now states that if one is ever seen to hang, the fix is an explicit flush + a force-exit carrying that path's OWN exit code — never a bare force-exit(0). **The row closes as a #16 establishment item; the deferred corpus line-ending normalization is the named future obligation above (user's call).** |
| OI-138 | L5 PASS-1 (partition-3 harness) **doc-sync + audit-tooling fidelity (DT-12/#10, low; siblings of OI-105/OI-107/OI-112 + OI-95/OI-127(e))**: (a) `tools/param_manifest.json` cites the `batch_analyze.cpp` preset-builder at `~3843-3898` but at HEAD the builder is at `~4220-4257` — stale line refs (manifest stamped to the older head `459c92c46d`; the VALUES it records are correct); (b) `BUILD_AND_TEST.md:24`/`:342` give a lowercase `--preset jazz` example, but `applyPreset()` matches CASE-SENSITIVELY, so it errors — confirmed behaviorally (`--preset jazz` → exit 1, "unknown preset 'jazz'"); (c) the machine inventory carries a spurious `locateMeasureByTick` function row (`l5_functions.csv` 748-749) that is actually a CALL site in `convertNotationRegion`, from the over-capture-biased C++ scanner (bias disclosed in `manifest.json`; sibling of OI-127(e)); (d) `l5_io.csv`/`l5_fields.csv`/`l5_decls.csv` carry ZERO harness rows (the C++ extractor emits no io/fields/decls) — the harness's reads/writes surface was audited from source + branch/literal rows, not an io table. | `cc_l5_audit_pass1_harness_report.md` §3.6/§3.7/§3.8/§0; `tools/param_manifest.json`; `BUILD_AND_TEST.md:24/342`; `tools/audit/l5/manifest.json`; DT-12; OI-105/OI-107/OI-112; OI-95/OI-127(e) | ✅ RESOLVED (a)/(b) 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): (a) the `param_manifest.json` batch_analyze preset-builder line-refs (stale `~3843-3898` + the six per-field `@38NN` anchors) made SYMBOL-LED (cite `main()`'s per-preset ChordAnalyzerPreferences builder, values authoritative) — per the OI-88 precedent, dropping drift-prone line numbers rather than re-stamping numbers that will drift again. (b) the broken lowercase `--preset jazz` example in `BUILD_AND_TEST.md:24` fixed to `--preset Jazz` (applyPreset matches case-sensitively). (c)/(d) inventory over-capture remains folded under OI-95. Doc/data-only; JSON re-validated. |
| OI-139 | L5 PASS-2 (second reading) **OI-120 factual correction + `param_manifest.json` `functionresolver.h` site-line drift (DT-12, sibling of OI-138a)**: (a) at HEAD the manifest **registers** `wLicensedOut`/`wLicensedIn`/`wCadentialFit`/`decidingMargin` (G8, `§15-13`) + `maxForwardExtendSlices` (G8, `abstention`) — all `functionresolver.h` params — so **OI-120's "ONLY the `forwardoverride` θ pair is registered / these four NOT registered" is wrong**; the manifest predates the audit (last committed `c50002fee1`), so OI-120 erred, not the manifest. OI-120's TRUE gap is the cadence/modulation/output/recognition seeds (the `--layer l5` DT-2 sweep's 25 hits enumerate exactly that set; the 5 resolver params are correctly ABSENT from the hits). (b) the manifest's `site` lines for those 5 are **stale by ~14** — cites `functionresolver.h:197/198/199/200/258`, actual decls `211/212/213/214/272` (line 197 is now an unrelated `bothLicensed` field); mechanically confirmed by the new `DT-12_manifest_site_drift` sweep rule (5 hits). Doc-sync only, no code effect. | `cc_l5_audit_pass2_report.md` §2/§4; `tools/audit/l5/sweep_results.json` (DT-12_manifest_site_drift); `tools/param_manifest.json` (G8); DT-12; OI-120 (corrected); OI-138a (batch_analyze sibling) | ✅ RESOLVED 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): (a) OI-120's membership correction is already recorded in the OI-120 row ("★ CORRECTED by pass 2"). (b) the manifest's stale `functionresolver.h` site lines RESTAMPED to the current decls — `wLicensedOut` 197→211, `wLicensedIn` 198→212, `wCadentialFit` 199→213, `decidingMargin` 200→214, `maxForwardExtendSlices` 258→272 (verified against `functionresolver.h`). Doc/data-only; JSON re-validated; values unchanged. |
| OI-140 | L5 PASS-2 (second reading) **WiR-coverage → the governing hard stop can pass silently (DT-23 + DT-2; second-pass find pass 1 did not flag; cross-refs OI-123/OI-124)**: `a8_rebaseline_measure.py:307-310` (the GOVERNING variant-(b) measurement) wraps `dcml.parse_rntxt_file` in a broad `except Exception: wir_regions=[]` then `if not wir_regions: continue`, so a WiR-parse failure silently EXCLUDES that piece from the class-(b) measurement; `robust_stop_diff.py` never reconciles WiR coverage between candidate and reference (grep: 0 coverage/`wir` refs). A systematic WiR-parser breakage in a candidate run would drop pieces → LOWER the class-(b) root-disagree duration → PASS the automated non-increase hard stop. Mitigated only human-in-the-loop: the mandatory explained per-run set-diff (CLAUDE.md block A) would list the removed runs, but the AUTOMATED gate would not fail. `characterise_bir_false.py:149-153` carries the identical diagnostic-side `except Exception: pass` (pass 1 dispositioned that exact row a clean control-flow guard — the second-reading miss). LATENT today (runs reproduce byte-identical, failure population empty) — the same character as OI-123/OI-124. The DT-23 sweep enumerates 40 broad-except sites overall (⊇ OI-123/OI-128's ~21) — those two rows' enumerations should be broadened to the full `sweep_results.json` list at their next touch. | `cc_l5_audit_pass2_report.md` §2/§3/§4; `tools/audit/l5/sweep_results.json` (DT-23_silent_failure, 40); `a8_rebaseline_measure.py:307`; `robust_stop_diff.py`; `characterise_bir_false.py:149`; DT-23; DT-2; OI-123/OI-124/OI-128 | ✅ RESOLVED 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): the GOVERNING a8 instrument now distinguishes a MISSING WiR annotation (`find_wir_file` None → `wir_no_annotation`, a legitimate exclusion) from a WiR PARSE FAILURE (present file that throws or yields 0 regions → NAMED per-stem in the summary `coverage.wir_parse_fail_stems` + a loud stderr surfacing), and `robust_stop_diff` now RECONCILES the candidate `wir_covered` against the committed reference and FAILS loudly (exit 1, `COVERAGE SHRUNK`) on any coverage shrink — so a systematic WiR-parser breakage can no longer pass the class-(b) non-increase gate by dropping pieces. Reference manifest re-stamped with `coverage.wir_no_annotation`/`wir_parse_fail` (metadata-only; 0 parse failures; NO grading figure moved — battery byte-identical). The `characterise_bir_false.py:149` diagnostic-side WiR except (batch diagnostic, not the hard stop) is narrowed under OI-123. Guard established by a simulated coverage shrink (321<326 → FAIL). |
| OI-152 | *(RENUMBERED from OI-150 by Cowork, 2026-07-13 — the combined-event session filed this row as OI-150 while the backlog-triage session had already used OI-150 for the BUILD_AND_TEST stale-baselines row; the older row keeps the number; `cc_key_grading_and_calibration_rebaseline_report.md`'s "OI-150" references mean THIS row. The collision itself is OI-153.)* THE REMAINING KEY-PARSE ABSTAIN — the shared graded key reduction (`compare_rn._our_key_tonic`) still ABSTAINS on any emitted mode name whose suffix contains an accidental or a digit, because the fallback mode regex is `[A-Za-z]+` (`_KB_OURS_KEY_RE`). At HEAD exactly one such mode survives in the corpus: **Dorian ♭2** (`Dor♭2`), costing a key-parse abstain of **4 080 ticks Jazz / 2 400 ticks Default / 0 Baroque** (0.049 % / 0.029 % of graded duration). Surfaced at the OI-132 consolidation, 2026-07-13, which rescued the other four abstaining exotic modes (Lyd♭7 / Lyd+ / Mix♭6 were KEYFAILs of the same regex) but deliberately left this one, because fixing it moves the key figures OUTSIDE the five modes the user ruled on. **The fix, when it lands at the parser's next touch, extends the PARENT-COLLECTION principle (the user's OI-132 ruling) to any remaining exotic mode name the parser learns to read — with its own small measured ratification, since it moves figures outside the five ruled modes.** ★ **The reduction target, stated exactly (user correction, 2026-07-13):** Dorian ♭2 is the **2nd mode of the melodic minor** scale — D Dorian ♭2 is the notes of **C melodic minor** — so it grades as **C minor: the PARENT tonic (emitted − 2 semitones), minor**. It does **NOT** grade as D minor. Same-tonic-minor is exactly the unprincipled reduction the old code improvised and that the OI-132 ruling retired; recording the wrong target here would let that misunderstanding resurface. **Boundary (declared, NOT decided here):** the plain church modes (`Dor`, `Phryg`) keep their same-tonic-minor reduction under a separate standing declared choice documented at `_KB_MINOR_MODES` in `compare_rn.py` *(anchor updated at OI-155, 2026-07-13: the prefix-rule constant `_KB_MAJOR_MODE_PREFIXES` this row originally cited is gone — the classification is now set-membership in the producer's emitted vocabulary, and the declared choice is documented in the comment block above those two sets)*; whether that choice should be revisited under the same parent-collection principle is a question for Cowork/the user, not settled by this row. | `cc_key_grading_and_calibration_rebaseline_report.md`; `compare_rn._KB_OURS_KEY_RE` / `_KB_PARENT_COLLECTION_MODES`; OI-132 (the ruling) | OPEN — deferred by the user 2026-07-13 ("land what's ratified; file the leftover"). Gate: the parser's next touch |
| OI-153 | REGISTER ID COLLISION (Cowork-opened 2026-07-13, found verifying the combined re-baseline): two near-concurrent CC sessions each took "the next free OI number" — the backlog triage filed OI-150 (BUILD_AND_TEST stale baselines) and the combined-event session later filed a DIFFERENT row as OI-150 (the Dorian-♭2 abstain, now renumbered OI-152). The register's ID-stability rule has NO mechanical guard: nothing detects a duplicate `| OI-N |` row. FIX OWED (trivially scriptable): a duplicate-ID check added to the establishment battery / register lint, run at every fold — plus the standing habit that a session filing rows STATES the numbers it takes in its report (both sessions did; the collision was visible, just unchecked). | Cowork verification 2026-07-13; OI-152 (the renumbered row); the triage + re-baseline folds | ✅ RESOLVED 2026-07-13 (the harness group, `cc_harness_group_report.md`): `tools/audit/register_lint.py` collects the ID of every register ROW (the FIRST cell of a table row — an OI-N inside another row's prose is a cross-reference, not a row, and is ignored) and exits non-zero on any ID that heads more than one row. Wired as gate 0 (`register`) of `tools/audit/hardening_battery.py`, so it runs at EVERY fold — the point of the row. ESTABLISHED both ways (#19), not merely unfalsified: PASS on the register at HEAD (154 unique row IDs) and FAIL, naming `OI-52 heads 2 rows (lines 140, 234)`, on a synthetic register with one duplicated row ID. |
| OI-154 | EXPLAINABILITY as a future end-user feature (user-declared 2026-07-13, recorded at `cowork_evidence_inventory.md` §8b): the end user may ask HOW a mode/chord/function was inferred; if the evidence trail behind every inference is PUBLISHED (which pitch classes drove the key, which cadence vote confirmed, the winner's margin, why an abstain), then "show me why" is a late-bound DISPLAY consumer of already-published facts — no new analysis. Much of the substrate exists as internal diagnostics (the chord-diagnosis replay, the dormant open marks/ambiguity kinds, ranked-candidates-plus-margins); the gap is publication = OI-145 wave 3's job. HELD long-horizon like the intonation feature (OI-62), both declared consumers strengthening the publish-evidence-broadly rule. | user 2026-07-13; `cowork_evidence_inventory.md` §8b; OI-62 (sibling consumer); OI-145 wave 3 | OPEN — held; revisit when wave 3 publishes the evidence surfaces |
| OI-155 | ★ DISCOVERY (the harness group, 2026-07-13) — **THE OI-132 CONSOLIDATION LEFT TWO REGRESSION TESTS RED, AND ONE OF THEM HIDES A GRADING-SEMANTICS QUESTION (#11 + a declared inference finding).** Found while establishing OI-52 (running the Python metric suites); **PRE-EXISTING, not caused by that work** — verified by re-running the two assertions against HEAD's files with this session's changes absent, where both already failed. The parent-collection consolidation (`800f1a12bf`, user-ratified 2026-07-13) changed the shared key reduction but did not update the tests that pin it, so `tools/tests/` has been red since it landed: (a) `test_metric_primitives_l0l1.test_our_key_tonic_mode_qualified_normalization` expects `crn._our_key_tonic("EPhrygDom") == (4, False)` (E minor — the SUPERSEDED same-tonic reading); at HEAD it returns `(9, False)` (A minor — E Phrygian dominant is the dominant of A, so the parent collection IS A minor). **The code is right and the test is stale; its correct new expectation is derivable directly from the user's ruling.** (b) `test_oracle_root_metric.test_parse_our_key` expects `orm.parse_our_key("Cweird") == (0, None)` (abstain on an unknown mode); at HEAD it returns `(0, 'minor')`. **This one is NOT merely a stale figure — it is a semantic change the fold introduced and nobody has ruled on:** `parse_our_key` is now a thin adapter over the shared reduction, whose `_mode_is_major()` is `mode.lower()[:3] in _KB_MAJOR_MODE_PREFIXES`, so ANY unrecognized mode suffix falls through to **minor** rather than abstaining. An unparseable mode now grades as a definite minor key instead of a keyfail — which cuts against the abstain convention (OI-33: an abstain must not be silently converted into a confident reading) and is the same family as OI-152 (the Dor♭2 key-parse abstain). It was inside the ratified measurement (the OI-132 report records oracle_root_metric's key tiers shuffling by 5/9/11 identities, all outside the key-error tiers), so no committed figure is wrong — but the RULE was never stated. **NOT fixed here, deliberately:** (a) is a one-line test update but belongs with (b); (b) is a grading-semantics decision for Cowork/the user, and changing a red regression test's expectation to match current code is exactly how a defect gets laundered. **DECLARED, per the standing rule that CC declares inference/grading findings rather than deciding them.** | this session's OI-52 establishment; `tools/tests/test_metric_primitives_l0l1.py:361`; `tools/tests/test_oracle_root_metric.py:160`; `compare_rn._mode_is_major` / `_KB_MAJOR_MODE_PREFIXES`; OI-132 (the fold that moved them); OI-33 (the abstain convention); OI-152 (sibling) | ✅ RESOLVED 2026-07-13 (`cc_oi155_report.md`) — **the user ruled: RESTORE ABSTAIN-ON-TRULY-UNKNOWN.** (b) is fixed in the CODE, not in the test: `_mode_is_major` (the two-valued prefix rule, no unknown state) is replaced by the three-valued `_mode_class` — a set-membership test against **the producer's own emitted vocabulary**, read from `keymodeformatting.cpp` `keyModeSuffix()` (the 21 spellings) partitioned by `keymodeanalyzer.h` `keyModeIsMajor()` (the producer's own major-third/minor-third split). A suffix in neither set and not one of the five OI-132 exotics ABSTAINS on the mode axis; `compare_rn._our_key_ident` is the ONE abstain decision every graded site now routes through (the OI-52 pattern), so an abstain is a `keyfail` — excluded from the key-agree denominator, published as abstain coverage, flagged by `robust_stop_diff` if it rises — and is never completed into a confident reading. (a) is the stale figure it was: `EPhrygDom` → `(9, False)` = A minor, the parent collection. **Zero grade movement, proven:** every a8 output byte-identical on all three presets (summary + all run/cell enumerations + mapping), battery PASS (a8_diff +0/−0, class-(b) Δ+0, calib 4/4, validate 3/3) — because all 14 mode suffixes the corpus actually emits are in the vocabulary, so the abstain path fires on no real cell. Both named tests green by the code; a new test PARSES the two C++ producer sources and pins the grader's table complete-and-faithful to them, so a 22nd emitted mode goes red instead of grading silently. Also restored: the unicode-accidental tonic normalization the same fold had dropped (`F♯maj`, a second hidden red assertion in the same test) |
| OI-151 | DESTRUCTIVE DEFAULT OUTPUT PATH on the mode-grading adjudication probe (DT-24, sibling of OI-130): `tools/mode_grading_adjudication_probe.py` writes its results to `tools/reports/mode_grading_adjudication_probe.json` **by default, with no `--out` flag** — and that file is a COMMITTED artifact (force-added as the evidence the user's OI-132 ruling rests on). Re-running the probe silently overwrites the committed evidence; the figures in `cc_mode_grading_adjudication_probe_report.md` cite it, so an overwrite is a doc-sync break (#10) as well as an evidence loss (#12). DISCOVERED 2026-07-13 by exactly that: a post-consolidation self-check re-run of the probe overwrote the file (the new content is legitimate — with the ruling landed, the probe's `baseline` column IS Rule B and its `ruleB` Δ reads +0.0000 — but it is NOT the pre-ruling evidence). The committed file was restored (`git checkout`) and the self-check output kept in the session's scratch instead. The probe post-dates OI-130, which closed this defect type at the other instruments' producing side — so this is a fresh instance, not a leftover. Fix: default `--out` to scratch, as `calibration_fit` / `music21_batch` / `stage5_fit_driver` already do; writing the committed evidence path must be explicit. | `cc_key_grading_and_calibration_rebaseline_report.md` (self-check); `tools/mode_grading_adjudication_probe.py`; DT-24; OI-130 | ✅ RESOLVED 2026-07-13 (OI-145 wave-1 instrument-hygiene sweep, `cc_instrument_hygiene_sweep_report.md`): `--out` now defaults to a SCRATCH path and the committed evidence path is written only when named explicitly; the docstring's false claim that the default target is "gitignored/regenerable" is corrected to say what the file actually is (the record the user's OI-132 ruling was made on). NO measurement code touched — a pure output-path fix. **VERIFIED by running the probe with default arguments: `ESTABLISHMENT PASS`, output written to scratch, and `git status tools/reports/` clean — the committed artifact is untouched by a bare re-run.** ★ The SAME defect was found at a second instrument during the sweep and fixed in the OI-157 commit: `measure_joint_probe.py` defaulted to `tools/reports/joint_probe_measure.json`, also committed evidence; it too now defaults to scratch. |
| OI-52 | S20 — the metric's core comparison written out four times (adopted out of §F by the backlog triage, 2026-07-13). "Does our root equal the ground-truth root" is a bare `==` re-implemented at every graded site — `a8_rebaseline_measure.py:148` (the GOVERNING hard stop), `compare_analyses.py:243-244` (`_roots_match`), `compare_rn.py:352/370/474` (the robust-unit grid) — the derivation behind it is single-owned; only the one-line comparison repeats. **The row's old "trivial; likely not worth it — decide and close either way" status was STALE:** the A6 verdict — *"add the one shared helper at the next instrument touch; close the row then. Not worth its own commit; not acceptable to leave forever"* — was **already user-ratified 2026-07-10** under OI-42 (`cowork_adjudication_dossier.md:78-84`); the register row was never updated. **And the risk A6 named has since FIRED in the sibling family:** OI-132's discovery D2 found the two OURS *key*-parsers embedding different music-theory readings, so folding them moves a graded figure either way. The root comparison carries the same latent nuance today — the abstain convention (OI-33) had to be written into PROSE precisely because "what does a missing root mean" must be decided identically at every `==` site (`a8_rebaseline_measure.py:34` states it; the other sites each re-implement the comparison). A convention enforced by prose across four copies is the construction that produced D2. | `cc_backlog_triage_report.md` §3; `cowork_adjudication_dossier.md:78-84` (A6, ratified at OI-42); OI-132 (D2, the materialized risk); OI-33; audit UNCLEAR-6 (the origin) | ✅ RESOLVED 2026-07-13 (the harness group, `cc_harness_group_report.md`): the shared helper is `compare_analyses.roots_agree(ours_root_pc, gt_root_pc)`, and **every** OURS-vs-ground-truth root comparison routes through it — `a8_rebaseline_measure:148` (the GOVERNING hard stop), `compare_analyses._roots_match`, and `compare_rn`'s three (`classify_pair`'s root-only fallback, its `root_match`, and the `root_agree` denominator). **A complete enumeration found THREE MORE the row had not listed** — `compare_analyses.three_way_classify` (the three-way BIR metric's ours-vs-DCML leg), `dcml_direct` and `dcml_anchored` — and they were folded too, since closing the row while leaving three more copies of the very comparison it is about would have been a false close. `three_way_classify`'s music21-vs-DCML leg deliberately does NOT route through it: that is corroborator-vs-GT, not ours-vs-GT, and the helper's convention is about OUR abstain. The helper's substance is the ABSTAIN CONVENTION (OI-33), now encoded once instead of carried by prose across the sites: OURS abstained vs a resolved GT root is a DISAGREEMENT (so the root metric stays non-abstention-reducible), while a GT root that did not resolve is a SCOREABILITY question each caller settles BEFORE calling (`classify_pair` drops the pair; the `root_agree` denominator guards `is not None`) — those guards deliberately stay at their call sites, because they decide the denominator, not the equality. **The D2 risk was checked, not assumed:** `compare_analyses.classify` is the one site with no GT-None guard, where `None == None` → True would read a both-abstained pair as a root match. MEASURED over the committed corpus — **0 of 33,296 aligned pairs (all 3 presets) carry a None root on either leg** — so the divergence is latent-but-unreachable and the sites agree in practice as well as in intent. Unlike the KEY parsers (OI-132's D2, where folding moved a graded figure), this fold is value-identical BY CONSTRUCTION, and that was PROVEN EXHAUSTIVELY rather than sampled: `roots_agree(a,b) == (a == b)` on all 169 (a,b) over the full domain {None, 0..11}², and `three_way_classify` old-vs-new agrees on all 2,197 (ours, m21, dcml) triples. **PROVEN INERT on the governing metric independently of the C++ work** (the Python fold was gated against the committed corpus before the harness was rebuilt): a8_diff **+0/−0 on all presets**, class-(b) hard-stop duration **Δ+0**, calib 4/4 byte-identical, validate 3/3; the batch diagnostic reproduces 54/24/54. Landed folded into the harness-group tooling touch, as the A6 verdict allowed. |
| OI-156 | **The user-facing bridge's 4th hard-coded `0.25` onset-boundary literal (DT-3, #6 — the residual of OI-135(b), which is CLOSED).** `src/notation/internal/notationharmonicrhythmbridge.cpp:85` reads `(cfg != nullptr) ? cfg->onsetBoundaryThreshold() : 0.25` — the null-config fallback is a value-copy of `analysis::kDefaultOnsetBoundaryThreshold` (`src/composing/analysis/types/analysistypes.h:94`), which OI-135(b) established as the ONE source that the configuration registration and the harness's three sites now read. The bridge is the last site holding a literal, and it lives in `src/notation` — outside the harness dispatch's edit scope, so it was OFFERED and deliberately not taken. Coincides today (0.25 == 0.25) and nothing is mis-measured; but there is no mechanical sync, so a change to the config default would silently leave the bridge's no-config path behind. | `cc_harness_group_report.md` (offered, not taken); `cc_oi155_report.md` §6; OI-135(b) (the closed parent); DT-3 | OPEN — deferred by the user 2026-07-13. **Gate: the next `src/notation` config-unification touch** — the bridge reads `analysis::kDefaultOnsetBoundaryThreshold` instead of the literal. Proper-layer dedup, done at that touch, not before |
| OI-157 | ★ DISCOVERY (CC, 2026-07-13, at the OI-155 consumer audit) — **a THIRD copy of the mode classification, in `tools/measure_joint_probe.py`, went STALE at the OI-132 consolidation (DT-3/DT-6 + DT-11; sibling of OI-132's D2).** `_MAJOR_MODE_IDX = {0,3,4,9,10,11,16,19}` (`:86`) classifies a carried/argmax key from its `KeySigMode` ENUM INDEX, and its comment states the table is "derived from ... `compare_rn._mode_is_major`" and "verified faithful per row against `crn._our_key_tonic`". **Both claims are false at HEAD:** (a) `_mode_is_major` no longer exists — OI-155 replaced it with the three-valued `_mode_class` over the producer's emitted vocabulary; (b) the table still encodes the SUPERSEDED same-tonic prefix rule, under which `PhrygianDominant` (enum index 18, absent from the set) graded minor **at its own tonic** — whereas the shared reduction has, since OI-132, reduced the five dominant-family exotics to their PARENT COLLECTION, i.e. **a different tonic**, which an (index → is_major) table cannot express at all. The probe's own faithfulness self-check (`:307-314`, comparing its ident against `crn._our_key_tonic`) is the mechanism that would surface this at its next run. **Not a governing surface** (a read-only joint-key probe; not in the establishment battery) and it moved NO committed figure — the OI-155 fix is byte-identical on every graded output. Not fixed here: out of the OI-155 dispatch's edit scope, and the probe's outputs are frozen report evidence. | this session's OI-155 consumer audit (`cc_oi155_report.md` §4); `tools/measure_joint_probe.py:83-99` + `:307-314`; OI-132 (the fold that moved the shared reduction); DT-3/DT-6/DT-11 | ✅ RESOLVED 2026-07-13 (OI-145 wave-1 instrument-hygiene sweep, `cc_instrument_hygiene_sweep_report.md`): **the table is DELETED and the probe grades a carried key through the ONE shared reduction.** The probe could not simply read `ourKeyStr`, because the carried ALTERNATIVES carry no key string — only `(tonicPc, KeySigMode int)` — so the integer is first resolved to the producer's OWN emitted suffix and the key string the producer would print is composed from it, then graded by `crn._our_key_ident`. The suffix comes from **`tools/producer_key_modes.py` (NEW — the ONE reader of the producer's KeySigMode vocabulary)**, which parses `keyModeSuffix()` + the `KeySigMode` declaration order + `keyModeIsMajor()`; the two copies of those parsers that `test_metric_primitives_l0l1.py` held are folded onto it (#6). So the OI-132 parent-collection ruling and the OI-155 abstain rule now reach this probe with no second edit, and its consumer `classify_key_disagreement.py` (which imports `_key_ident`) is fixed with it. **ESTABLISHED at full-corpus scale:** the probe's own faithfulness self-check (renamed `key_ident_selfcheck` — the old name described a table that no longer exists) reports **0 mismatches on 6409 / 6311 / 6413 committed regions**, all three presets — the composed identity equals the identity of the key string batch_analyze actually emitted, on every region. Battery byte-identical (a8_diff +0/−0 ×3, calib 4/4, validate 3/3): the probe feeds nothing graded. **The probe's own figures DO move, and were ATTRIBUTED by an A/B re-run of the pre-fold code at HEAD** (11 of the 21 producer modes graded differently under the old table — the 5 exotics whose tonic MOVES, and 6 whose accidental/digit suffix the shared reduction abstains on per OI-152): of the shift vs the committed evidence, **the OI-142 ground-truth correction accounts for the bulk** (key-disagree −196/−187/−191) and **this fold for a small remainder** (−11/−20/−8; abstains +0/+2/+1; menu-containment −4/−10/+0). **`chord-flip-under-GT` — the number the OI-43/OI-44 shelve ruling rests on — is BYTE-IDENTICAL (7/8/6) under the fold**, so the ruling is untouched. The probe's committed artifact was NOT overwritten (its default `--out` is now scratch — the OI-151 defect at this file, fixed in the same commit). The staleness of the committed probe evidence is OI-159. |
| OI-158 | ★ DISCOVERY (CC, 2026-07-13, while ESTABLISHING the OI-133(c) "FloatingKey ±4" tolerance) — **the music21 corroborator's local-key path HAS NEVER RUN, and the "±4" is not a tolerance but unreachable configuration on an object that is never constructed.** `music21_batch.py` does `m21_floatingKey.FloatingKey()`, but `music21.analysis.floatingKey` exports no such name (v9.9.1 — the class is **`KeyAnalyzer`**). The constructor raises `AttributeError`, a bare `except Exception` swallows it, `fk_analyzer` stays `None`, and `local_key` silently falls back to the GLOBAL key for every region — which then feeds `romanNumeral` and the emitted `key` field. **Proven at the artifact, not inferred: all 28,914 regions of the committed Baroque corroborator have `key == keyGlobal`,** on every stem. So the `.music21.json` Roman numerals were computed against the global key throughout, never a local one — a **Class-B (#19) instrument-establishment failure** (the instrument does not do what it says) plus a silent-degradation defect (a bare `except` masking an API mismatch) and a #10 doc-sync break (the docstring advertised a `keyLocal` field that is never written; corrected). **NO governing figure is affected:** the ratified robust unit is DCML-only and never reads music21, and the BIR gate reads music21's root + quality, not its key or RN; the blast radius is the `full_agree` / `chord_agree_rn_differs` / `chord_agree_key_differs` sub-split in `compare_analyses.classify`. **NOT FIXED — deliberately:** constructing `KeyAnalyzer` would give the corroborator genuinely local keys and RNs, changing the committed `.music21.json` — a ground-truth-corroborator re-baseline under the user's ratification, not a hygiene edit. Recorded at the site with the evidence. | `cc_instrument_hygiene_sweep_report.md`; `tools/music21_batch.py` (the `fk_analyzer` block); OI-133(c) (the tolerance whose establishment surfaced it); DT-24-family (silent degradation) | 🔶 **DEAD-CODE HALF RESOLVED 2026-07-13** (the user's ruling: delete the dead machinery, do NOT activate `KeyAnalyzer`, do NOT foreclose the question — `cc_wave1_finalize_report.md`). The `_HAS_FLOATING_KEY` import/flag, the `fk_analyzer` construction (the `FloatingKey()` call + the unreachable `numFlats`/`numSharps` ±4), and the `local_key` branch are GONE from `music21_batch.py`; the corroborator now reads plainly at the global key, and the docstring says so. **ESTABLISHED at the artifact, not asserted: the corroborator regenerates BYTE-IDENTICAL without the block** — a full 352-stem regen to scratch is sha256-identical to every committed copy (352/352 against the flat `tools/corpus` set AND against each of the three per-preset dirs; 0 differ, 0 missing), which is the proof the removed code never once affected output. Establishment battery byte-identical (a8_diff +0/−0 ×3, calib 4/4, validate 3/3; music21's key feeds no governing figure). The two frozen L5 disposition rows for the ±4 (`gen_grading_fitting_dispositions.py`) are left verbatim — they are stamped audit evidence and their artifacts must keep reproducing — but they now carry a comment recording that OI-158 refuted the "feeds oracle_root_metric's KEY tiers" claim in their note. **EVIDENCE HALF STILL OPEN:** should the key layer consume a music21 `KeyAnalyzer` LOCAL key as an (unvalidated, non-ground-truth) second opinion? Filed in `cowork_evidence_inventory.md` §8c (OI-146) — **gate: the key-layer design conversation**; if ever adopted it enters as an explicitly-unvalidated field, never under load until established (#19), and its adoption is a corroborator re-baseline (#16), not a refresh |
| OI-159 | ★ DISCOVERY (CC, 2026-07-13, at the OI-157 fold's attribution run) — **the OI-142 ground-truth correction silently STALED the committed OI-43/OI-44 joint-probe evidence, and nothing re-ran it.** `tools/reports/mode_key_chord_probe.json` is stamped `git 243cfd2165`, which PRE-DATES the OI-142 transposition correction to `dcml_parser.load_wir_regions`. Re-running the probe at HEAD (pre-fold code, isolating OI-142) moves its key-axis figures materially: key-disagree regions **−196 / −187 / −191** and menu-containment **+24 / +19 / +25** (Baroque/Jazz/Default). The OI-142 row enumerated the graded surfaces it re-based but not this read-only consumer, so the evidence and the report citing it (`cc_mode_key_chord_probe_report.md`) drifted from the instrument (#10, #16). **The OI-43/OI-44 CONCLUSION IS UNCHANGED under the corrected instrument, measured, not assumed:** P1 (chord-flip-under-GT) is **byte-identical at 7 / 8 / 6 regions** — the coupling is still inert — and P3 (menu-containment) rises from 62–67 % to **68.7–75.6 %**, still below the 80 % bar the prediction set, so it is still NOT MET. The shelve ruling stands; only the recorded FIGURES are stale, and the "menu-widening signal" is weaker than recorded. Committed evidence deliberately NOT overwritten (that is the OI-151 discipline). | `cc_instrument_hygiene_sweep_report.md` (the three-way attribution table); `tools/reports/mode_key_chord_probe.json` (stamped `243cfd2165`); OI-142 (the correction); OI-43/OI-44 (the ruling the evidence backs) | ✅ RESOLVED 2026-07-13 (`cc_wave1_finalize_report.md`): **the committed evidence is REFRESHED from a run at HEAD, and the shelve ruling is RE-CONFIRMED.** The outgoing artifact was snapshotted FIRST (O-12) at `tools/reports/snapshot_2026-07-13_pre_oi159/` with a `SNAPSHOT_NOTE.md` recording what it preserved and why it was superseded; `tools/reports/mode_key_chord_probe.json` then took the HEAD run (stamped `6725329381`, corpus `c50002fee1` — unchanged), and `cc_mode_key_chord_probe_report.md` carries the refreshed figures with the superseded ones kept beside them (#12) and the drift attributed. **The measured refresh:** key-disagree regions **1982/2143/2019 → 1775/1936/1820** (**−207/−207/−199**, of which the hygiene sweep's A/B attributed −196/−187/−191 to the OI-142 ground-truth correction and −11/−20/−8 to the OI-157 fold); menu-containment **66.7/61.6/64.1 % → 75.6/68.7/72.5 %** — **still below its 80 % bar, so P3 is STILL NOT MET**; **chord-flip-under-GT BYTE-IDENTICAL at 7/8/6 regions** (coupled 3/4/2, durations 9600/11760/8160 unchanged) — the mechanism the ruling rests on is still inert. **The OI-43/OI-44 shelve ruling therefore stands, unchanged; only the figures were stale, and the "menu-widening signal" is weaker than recorded** (the menu misses the GT key in ~¼ of key-disagree regions, not ~⅓). No governing surface is involved: the establishment battery is byte-identical across this fix. **A SIDE PROOF fell out of the refresh:** the probe was run twice, before and after the OI-125 resolver fix in the same session, and the two runs are identical in all 177 measurement fields (only `git_hash` differs) — an independent byte-identity check on OI-125 through this probe's grading path. **★ ONE DISCOVERY, DECLARED NOT ABSORBED — OI-160:** the SIBLING evidence artifact (`tools/reports/joint_probe_measure.json`, the arc-12 chord-axis go/no-go) is stale from the SAME OI-142 cause and was NOT refreshed here — that is a different ruling's evidence and not this dispatch's to rewrite. **★ SUPERSEDED SAME DAY by the OI-160 collapse (`cc_oi160_report.md`): `mode_key_chord_probe.json` — the file this row refreshed — is now RETIRED, and its content lives on in the ONE canonical artifact `tools/reports/joint_probe_measure.json` (both axes), reproduced there to the digit (the collapse run differs from this row's run in exactly one of 181 leaf fields: `git_hash`). The paths named in this row are historical; the retired file is preserved at `tools/reports/snapshot_2026-07-13_pre_oi159/` and `.../snapshot_2026-07-13_pre_oi160/`.** |
| OI-57 | Corpus QA systematic pass — **RESCOPED at the backlog triage (2026-07-13); half superseded, half real and MEASURED.** **SUPERSEDED half (the ground-truth corpora):** the manifest discipline that did not exist in the 84-score-registry era now does — `run_bach_preset._write_manifest` fingerprints every `.ours.json` AND (since OI-124) the paired `.music21.json` ground truth; `characterise_bir_false.validate_corpus_dir` refuses a stale/foreign/incomplete/preset-crossed dir; `tools/tests/test_snapshot_sources.py` pins the source bytes against `snapshot_sources_manifest.json`; the music21 v9.9.1 pin is enforced at produce time (OI-130). **STILL REAL half (the extra-scores registry — exactly what the item was about), counted at the data:** `tools/extra_scores_registry.json` holds **140 entries** (jazz 104 + piazzolla 6 + steelydan 23 + snarkypuppy 7; `_updated: 2026-04-24`) against **163 score files on disk** under `tools/extra scores/` → **23 unregistered**, including **ALL 20 `hiromi/` scores** that `docs/score_inventory.md:33` designates the LLM-triage corpus, plus 3 duplicate `(1).mscz` files. **No tool validates this registry** (grep: no validator among the three files that reference it). `docs/llm_triage_design.md:17` still says "~125 scores" — stale against the actual 140. All 140 entries carry `ground_truth: false`, so nothing here can be graded — the gap is registry ACCURACY, not measurement. | `cc_backlog_triage_report.md` §1; `tools/extra_scores_registry.json`; `docs/score_inventory.md:33`; `docs/llm_triage_design.md:17`; STATUS post-sub #13 (the origin); OI-38/OI-39 | OPEN — **ASSIGNED: the corpus-onboarding event (OI-38)**, where the user's standing "use as many scores/corpora as possible" directive lands — the registry must be accurate and mechanically validated THERE, before any of those scores is used. Gate: the onboarding event (#8/#9) |

| OI-160 | ★ DISCOVERY (CC, 2026-07-13, at the OI-159 refresh run) — **the OI-142 ground-truth correction ALSO staled the SIBLING joint-probe artifact, `tools/reports/joint_probe_measure.json` (the arc-12 chord-axis go/no-go), and the report that cites it.** OI-159 named only the OI-43 key-axis evidence; this is the same instrument's OTHER committed artifact, stamped `git fa0a881aa4` and backing a DIFFERENT ruling (the arc-12 "joint step SHELVED on the chord axis" decision, `cc_engage_stage3_joint_measure_report.md`). Measured at HEAD in the OI-159 run: the **fire-rate is byte-identical** (99 / 95 / 89 flip regions; which chords flip is a property of our analyzer, not of the ground truth), but **whether a flip CORRECTS or HARMS is graded against the ground truth, so the corr/harm split re-sorts**: `benefit_top_alt_flip` corr/harm **37/28 → 38/29** (Baroque), **36/33 → 39/33** (Jazz), **35/25 → 36/26** (Default), i.e. **net (corr−harm) +9 / +3 / +10 → +9 / +6 / +10**. `cc_engage_stage3_joint_measure_report.md` cites the stale corr/harm figures in its go/no-go table. **The arc-12 CONCLUSION is not visibly threatened** — a net of +6 to +10 regions out of ~6200 scored per preset is still the "nearly a coin-flip, ~+0.1 pp" reading the no-go rests on — but that is a RULING, and this session was not dispatched to rewrite a ratified decision record. **NOT FIXED: no governing figure is involved** (this artifact feeds no gate); what is owed is (a) refreshing the artifact + the report's figures from a HEAD run, and (b) a decision on the #6 duplication behind this: **two committed artifacts are produced by ONE instrument** (`measure_joint_probe.py`), the older one a strict subset of the newer's blocks — the newer `mode_key_chord_probe.json` already contains the arc-12 chord-axis blocks, so `joint_probe_measure.json` may simply be superseded rather than refreshed. | this session's OI-159 refresh run (`cc_wave1_finalize_report.md`); `tools/reports/joint_probe_measure.json` (stamped `fa0a881aa4`); `cc_engage_stage3_joint_measure_report.md` §2.1 (the stale table); OI-142 (the correction); OI-159 (the sibling row, same cause) | ✅ RESOLVED 2026-07-13 (`cc_oi160_report.md`) — **the user ruled option A: collapse to ONE artifact. Both halves done; BOTH rulings re-confirmed, neither reopened.** **(1) The subset premise PROVEN AT THE FILES, not assumed:** every field of `joint_probe_measure.json` is present in the OI-43 run (**0 missing**), which adds **exactly** the `key_axis_desksim` subtree (**93 paths**) and nothing else — a strict superset, so there was never a second concern, only a second file. **(2) The collapse:** one canonical artifact, **`tools/reports/joint_probe_measure.json`** — the instrument's own natural committed target (the one path named in its `--out` help, `measure_joint_probe.py:470-477`, and the name derived from the instrument's) — refreshed by a full both-axes run at HEAD; the separately-named `mode_key_chord_probe.json` is **RETIRED** (#6). **O-12 FIRST:** both outgoing artifacts snapshotted at `tools/reports/snapshot_2026-07-13_pre_oi160/` with a `SNAPSHOT_NOTE.md`. **(3) The arc-12 chord-axis NO-GO STANDS:** net (corr−harm) **+9/+3/+10 → +9/+6/+10** = **+0.14/+0.10/+0.16 pp** of ~6,200 scored regions — far below the **+0.3 pp** written shelve floor; **and its SHARPEST evidence is BYTE-IDENTICAL** — the coupled minority (the only population the joint step is theory-scoped to) reads **0 / +5 / −2**, every cell unmoved, because none of the three re-sorted flips fell in it. **(4) The OI-43/OI-44 SHELVE STANDS** (re-confirmed at OI-159, reproduced here to the digit): menu-containment **75.6/68.7/72.5 %** still under the 80 % bar; chord-flip-under-GT **7/8/6**, coupling still inert. **(5) THE CAUSAL CLAIM HELD EXACTLY:** of the 109 fields the retired subset shares with the refreshed run, the **only** ones that moved are the ground-truth-graded `benefit_*` fields — **not one** fire-rate, beam-width, pedal, or region-count field. The OI-142 correction moved the GROUND TRUTH; our analyzer never moved. Stronger still: the refreshed artifact differs from the OI-159 run in **exactly one of 181 leaf fields — `git_hash`**. **No governing surface touched:** establishment battery byte-identical (a8_diff +0/−0 all presets, class-(b) Δ+0, calib 4/4, validate 3/3). **Doc-sync tail fixed in passing:** the OI-141 row and `cowork_key_mode_inference_diagnosis.md` still carried OI-159's *superseded* menu-containment (66.7/61.6/64.1 %) — both now carry the corrected figure with the superseded one beside it (#12). |

## E. User adjudication / ratification pending

| ID | item | source | status |
|---|---|---|---|
| OI-41 | #17(c) sharpening: "establish the mechanism FIRES (control flow) before tracing arithmetic" | PC doc §"hands" | ✅ RATIFIED 2026-07-10 — folded into CLAUDE.md #17(c) |
| OI-42 | Audit §5 UNCLEAR rows 1–7 — plain-language explanations + PRINCIPLE-DERIVED verdicts in `cowork_adjudication_dossier.md` Part A | adjudication dossier | ✅ RATIFIED 2026-07-10 — six were pure rule applications; the one genuine acceptance: A3, tolerating the quality-overwrite #12 violation until E4 (kept visible via OI-10) |
| OI-43 | Reopen the joint (key,chord) ranking question (user, handoff — there called "ponder-point 1"). Sharpened 2026-07-11 (Cowork): the arc-#12 probe decided the KEY FIRST (decode under argmax key, re-decode under carried alternatives) and graded only ROOT flips — the framing the research grounding §3 says to escape (Raphael & Stoddard single (tonic,mode,chord) state; Wu & Yoshii: sequential coupling = the pipeline to escape; "carry a beam of (key,chord) hypotheses, not commit to key first"). What the probe legitimately bounds: chord-axis benefit over the carried-key menu (oracle ceiling +0.6 pp; carried alts are collection siblings — that fact survives any framing). What it does NOT test: (1) the KEY AXIS — chord evidence re-ranking the key (key-agree ~68/64/67 % has more headroom than root-agree ~63 %); (2) a key menu wider than the carried `keyAlternatives`. NEXT ACTION when hold lifts: draft a read-only re-measure scoping under #17 — written quantitative prediction recorded first, key axis primary. **★ DELIVERED 2026-07-12: `cowork_mode_key_chord_inference_discussion.md`** — the Premise-Gate opening (ledger with facts/theory/assumptions, three written quantitative predictions recorded before measurement, the three candidate placements against the layer rules, the desk-simulate → probe → build funnel with instrument establishment first). The discussion is OPEN with the user. **★ PROBE DELIVERED 2026-07-12 (`cc_mode_key_chord_probe_report.md`; `tools/reports/joint_probe_measure.json` — the canonical joint-probe artifact since OI-160; it was the separately-named `mode_key_chord_probe.json` until that collapse):** the Premise-Gate desk-simulation FIRED THE HARD GATE — the joint chord→key coupling the user's question turns on does NOT fire, because the carried key alternatives are diatonic-collection siblings (relative maj/min) under which the chord is **key-invariant**: chord-flip-under-GT on **0/6** hand-traced cases and **0.30–0.37 %** of key-disagree regions corpus-wide (×3 presets). Predictions answered: **P1 NOT MET** (chord-coupling key-flip ceiling ≤ +0.16/+0.19/+0.14 pp even if every flip were correct — below the +0.3 pp shelve floor); **P3 NOT MET** (menu-containment **62–67 %**, not ≥80 % — a menu-widening signal); **P2 NOT EVALUABLE** (mechanism inert + carried alternatives carry no per-alt confidence — `keyConf` populated on 0.01 %, the OI-75/OI-81 discarded runner-up closeness). Per the gate, the key-agreement ceiling/floor grader was **NOT built**; the extension is the additive read-only fire-rate diagnostic in `measure_joint_probe.py` (enum-table self-check 0 mismatch; a8 reference reproduces the ratified column 68.13/64.43/67.50). | handoff; `cowork_functional_analysis_research_grounding.md` §3; `cowork_mode_key_chord_inference_discussion.md`; `cc_mode_key_chord_probe_report.md`; Cowork sessions 2026-07-11/12 | ✅ SETTLED 2026-07-12: the probe delivered the numbers; the user declared the joint step SHELVED on both axes (OI-44, closed) and redirected the key-axis headroom to the key layer — **the measured successor question is OI-141 (the key-menu investigation: width + ranking)**. Row CLOSED with its answer: the most likely chord IS, on this corpus and menu, found under the most likely collection — the coupling the question hypothesized has no signal to carry; the wrongness the question sensed lives in the key layer's own menu and its unranked, unconsumed alternatives. |
| OI-44 | Joint step B1–B4 status: design DELIVERED / build SHELVED / framing REOPENED (contradiction B) — needs one declared status. **★ 2026-07-12 — the OI-43 probe now supports a single status: design DELIVERED / build SHELVED ON BOTH AXES** (chord axis arc-12 net +9/+3/+10; key axis this probe — chord→key coupling inert at 0.30–0.37 % fire-rate, ≤ +0.16 pp ceiling). The reopened framing does NOT revive the joint step; it redirects the key-axis headroom to the key layer (runner-up ranking, OI-75/OI-81) and to menu width (P3 62–67 %), not to chord↔key coupling. The final one-word status is the user's to declare. | fitter O-4; §9.2 #6; handoff; `cc_mode_key_chord_probe_report.md` | ✅ DECLARED by the user, 2026-07-12: **design DELIVERED / build SHELVED — on both measured axes** (chord axis arc-12; key axis the OI-43 probe: coupling inert, ceiling below the written shelve floor). Contradiction B is resolved; the design document stays the record of the architecture should evidence ever change. Row CLOSED. |
| OI-141 | ★ THE KEY-MENU INVESTIGATION (user-directed 2026-07-12, the OI-43 discussion's outcome — the measured successor to the joint-step question): the key-axis headroom (~⅓ of graded duration key-disagrees) is a KEY-LAYER menu-and-ranking question, in two measured halves: (a) **menu width** — the ground-truth key is ABSENT from the carried `keyAlternatives` in **~¼** of key-disagree regions (menu-containment **75.6/68.7/72.5 %** per preset, `tools/reports/joint_probe_measure.json` — *figures refreshed at OI-159/OI-160 under the OI-142-corrected ground truth; they supersede **66.7/61.6/64.1 %**, i.e. ~⅓, which was graded against the pre-correction ground truth (#12, kept beside). The menu-widening signal is real but **weaker** than originally recorded; still below the 80 % bar, so the reading is unchanged. The artifact is the canonical joint-probe one since OI-160 — formerly the separately-named `mode_key_chord_probe.json`, now retired, #6*) — WHY? (beam too narrow / hysteresis / mode confusion / enharmonics — unknown, to be measured, not assumed); (b) **menu ranking** — where the true key IS carried (~⅔), nothing ranks or consumes it: the per-alternative confidence is unpopulated (2/25,864) and the runner-up closeness is discarded (OI-75/OI-81). Next action: a read-only investigation under the Premise Gate — written quantitative predictions per hypothesis BEFORE the probe; desk-simulate the absent-key cases first. Any eventual fix is key-layer (L3) work at its #8-proper stage under the certified-layer discipline. | user decision 2026-07-12; `cc_mode_key_chord_probe_report.md`; `tools/reports/mode_key_chord_probe.json`; OI-75/OI-81; OI-43/OI-44 | OPEN — **REFRAMED by the user 2026-07-12: "understand why our key/mode inference does not work."** The user's directions, recorded: (1) the key/mode inference is measurably not good enough (true key absent from `keyAlternatives` in ~⅓ of disagreeing regions); (2) HYPOTHESIS to test, not assume: key/mode inference may NEED chord-derived evidence — refined by the probe's fact that chord ROOTS cannot split relative keys (measured inert), so the plausible channels are FUNCTIONAL: cadences, leading-tone accidentals, progressions (note: the built-but-gated chord→key coupling term in `decideJointKey` and the certified dormant L5 cadence/modulation machinery already point this way); (3) the isolated key/mode check EXISTS (the ratified key-agree column vs DCML ground truth) — what is missing is the DIAGNOSIS: break the failing ~⅓ of duration down by cause (relative-key confusion / parallel-mode confusion / tonicization-vs-modulation boundary / segmentation artifact / absent-from-candidates vs present-but-outranked), read-only from existing dumps vs GT; (4) the present-but-outranked half is likely solvable per published methods — targeted (#2) research: relative-key/mode disambiguation (cadence- and leading-tone-aware key finding). Next: Cowork drafts the Premise-Gate opening (predictions per cause class, written first), then the read-only diagnostic dispatch. **★ DIAGNOSIS DELIVERED 2026-07-12 (`cc_key_mode_inference_diagnosis_report.md`; `tools/reports/key_mode_inference_diagnosis.json`; classifier `tools/classify_key_disagreement.py`, reconciled EXACTLY to the ratified key column 68.13/64.43/67.50 on all 3 presets, 100 % probe-join, coverage 326/352).** Measured breakdown of the failing mass (Baroque/Jazz/Default): **~½ is NOT a genuine inference error** — the **tonicization/modulation label-gap (our region key == the DCML LOCAL key, disagree only vs the global-key grading) DOMINATES at 43.1/37.8/42.1 %** (the metric penalizing correct modulation-following), plus **corpus transposition mismatch 12.4/11.1/12.1 %** (12 pieces transposed vs the WiR edition → **new row OI-142**; clean-corpus key-agree rises to 70.9/67.1/70.3 %). Of the GENUINE errors, **wrong-key-area dominates** (dominant/subdominant-of-local 21.0/27.7/20.4 % + distant 17.8/21.3/18.1 %) **ahead of relative-key confusion (16.2/11.2/16.4 %)** and parallel (~1.7 %) — pointing at anchoring/beam/hysteresis (OI-94/OI-91/OI-97), not primarily relative-sibling disambiguation. **Present-but-outranked 77.8/76.9/77.2 %** (GT global key carried but outranked; above the written 55–70 %). **Leading-tone (chord-hints) test: present in 56.7/53.7/58.6 % of relative-confusion duration (local-anchored), 32.0/32.7/33.6 % (global) — below the written ≥ 60 %:** within-region evidence exists for a slim majority of the (minority) relative cases but is not the decisive lever. **6 of 8 written predictions FAILED** (a diagnosis-worthy incompleteness, #17/#3). The what-next research-target selection stays the user's (#8/#14). **★ USER DIRECTION 2026-07-12: (a) solve the DRIFT — a targeted (#2) search of published algorithms, software, and research on how key-tracking systems avoid wrong-key-area drifting (beam management, transition/change penalties, anchoring); Cowork runs it and delivers a grounding document; (b) leading-tone/cadence work CONFIRMED as the SECOND lever** (the user asked whether it helps mode/key inference — answer: yes, three ways: the raised leading tone is the relative-key AND parallel-mode discriminator, measured present-but-unused in ~57 % of the failing relative class; cadences are the published strongest key-establishment signal; the certified dormant L5 cadence/modulation machinery is the already-built channel, with OI-118/OI-119 to reconcile at engage). The research runs in parallel with the OI-142/OI-143 re-baseline event. **★ 2026-07-12: the OI-142/OI-143 re-baseline LANDED (adoption `d9b52ba969`) — the drift research now grades against HONEST columns:** the ~12 % corpus-transposition artifact is corrected (root-agree 63.36/62.37/63.25 → 66.04/64.98/65.93, key-agree(home) 68.13/64.43/67.50 → 71.29/67.49/70.52), and a key-agree-LOCAL column now sits beside HOME (65.72/62.49/65.39). The re-baseline surfaced a drift-relevant fact: key-agree LOCAL < HOME — our analyzer UNDER-follows DCML local modulations (stays in the tonal home), consistent with the diagnosis's wrong-key-area-drift dominance. **★ GROUNDING DELIVERED 2026-07-12: `cowork_key_drift_research_grounding.md`** — Temperley 2002 read and verified in-paper (full-lattice dynamic programming over all 24 keys + ONE tuned change penalty; "key has a kind of inertia"; his 2002 error taxonomy already contains our tonicization-vs-modulation class; a spelling-aware variant scored +3.6 points — the OI-15 channel corroborated at source); the repository's own `contrapunctus_findings.md` mined FIRST this time (never-learn-keys; long phrase-aligned key spans beat per-beat accuracy — every short spurious segment poisons the chords around it; their largest single win came from phrase-boundary-aligned key detection; our phrase-boundary view exists dormant); Feisthauer (diatonic-set + dominant-to-tonic evidence + DP tonal plan) and the local-key-estimation field at abstract level (HAL blocks automated fetching — flagged honestly). Six grounded implications, the FIRST a checkable premise owed before any design: whether our layer-3 decode is already full-lattice with only the CARRY pruned, or the search itself prunes — the hand-traced beam-drop's true mechanism, to pin at the code. The design conversation (what the key layer consumes, how it decides, which constants get fit against the LOCAL column) is the user's to open on this grounding. **★ MECHANISM PINNED AT THE CODE 2026-07-12 (`cc_l3_key_decode_mechanism_report.md`; READ-ONLY, no `src`/constant/golden touched; HEAD `cfcb5cceea`, corpus `c50002fee1`).** The grounding's FIRST checkable premise is answered: **our decoder is NOT full-lattice.** The per-slice emission scores all 12×21=252 states (no prune; `keymodeanalyzer.cpp:571-605`), but the whole-sequence Viterbi runs over a PRUNED lattice = the global union of each slice's emission TOP-8 (`topK=8`; `keymodesequence.cpp:140,156-159`) — measured 26–31 states (~12 % of 252) on the traced pieces. So a key never top-8 at ANY slice is ABSENT from the search (cannot be decoded/carried/recovered); the Viterbi WITHIN that set is exhaustive+global with the change costs (`:266-321,231-240`), no beam inside it. **Cowork's 3 predictions: P1 MET (emission scores all 252), P2 FAILED (there IS a search-level prune — the top-8 union, not full-lattice), P3 FAILED/refined (NO greedy or hysteresis key-commit — the greedy step is SEGMENTATION `regionanalyzer.cpp:870` not key; the region key is a deterministic duration-majority reduction of the global Viterbi `:737-843`; the only post-decode key override `applyJointKeyWiring` is env-gated OFF `:1472`; the carry truncation is downstream+secondary).** Change costs are 3 hand-set `[empirical]` constants NOT in `param_manifest.json` (`changeBaseCost=hysteresisMargin=2.0`, `changePerFifthStep=keySignatureDistancePenalty=0.60`, `relativePairExtraCost=relativeKeyHysteresisMargin=2.0`; `analysistypes.h:782/704/788`; OI-91/OI-97); emission window `windowBeats=4.0` (deliberately small). Anchoring is one signature+declared-mode read at startTick applied to every slice — mid-piece notated change never re-anchored (OI-94), no cadence/dominant channel in the decode (OI-68). **Re-traced 3 genuine errors at the DECODER's own numbers → 3 distinct mechanisms, NONE a carried-list "beam drop": (A) `bwv369@10080` LOCAL-EMISSION failure** — true e minor is a lattice state but the tight-window emission ranks wrong G major #1 (35.56) vs e minor #7 (29.11); the resolver's WIDER window ranks e minor #1 (implicating `windowBeats`); **(B) `bwv226.2@36960` EMISSION-MODEL failure** — true G major is ABSENT from the 27-state lattice (never top-8; resolver-window rank #116, gap 29.39) → the one true search-level absence, caused by the emission model not the beam; **(C) `bwv110.7@14400` CHANGE-COST over-smoothing** — true B minor IS the local emission #1 (27.96, near-tie spread 0.09) but the whole-sequence Viterbi commits to the cadential dominant F♯ minor (a fifth off), no dominant/cadence channel to hold B. **Corrects the diagnosis report's "drops off the beam" phrasing** (none is a carry beam-drop). **WHERE drift/stickiness live (mechanism only): the emission scoring model + its ±4-beat window (largest loss point, 2/3 traces) and the single unfit change cost (stickiness, 1/3); the top-8 prune + carry cap are downstream/secondary.** Design decides/builds nothing — the user's to open. **★ THE DESIGN-CONVERSATION OPENING DELIVERED 2026-07-12 (`cowork_key_layer_design_opening.md`), under the user's stated frame: MAXIMUM PRECISION governs (#4) — complexity, code volume, architecture redesign, and long analysis times are acceptable; the `ARCHITECTURE.md` §2.14 effort preset (quick/normal/ambitious; defined, not yet implemented) is the sanctioned latency valve; the ratified forward-only frame holds, with the cadence-confirmed key override already sanctioned as a forward recompute.** Seven design decisions with grounded options and written expectations: (1) retire the top-8 prune (full 252-state lattice — enabling, near-free); (2) the emission model — spelling-aware profiles (Temperley +3.6 verified; OI-15), leading-tone term, window treatment incl. multi-scale, input-weighting check, profiles to fit; (3) the transition model — phrase-boundary-modulated change costs (dormant phrase view), the cadence→key channel (the re-discovered June dossier `cc_cadence_key_investigation_dossier.md`: ≈91 % of that era's mode-absent floor addressable; the called-for key-agnostic tonic-voting pre-scan appears since BUILT as the dormant L5 cadence machinery — flagged to verify), proximity-costs caution, tonicization modeling; (4) anchoring (OI-94 re-anchor + OI-78 declared-mode prior); (5) the output surface (publish ranked alternatives + margins, OI-75/OI-81); (6) the 21-mode state-space inventory revisit; (7) the structural fit (forward-only holds; any genuine back-edge need returns as an open layer-redesign proposal under #7). Three cheapest read-only deciders recommended first: the offline re-decode probe (full-lattice + window variants from existing emission dumps), the cadence-vote coverage probe (also verifies the dossier claim), the spelling-profile desk simulation. **The design discussion with the user is OPEN.** |
| OI-142 | ★ CORPUS INTEGRITY (#9/#19) — **12 of 326 WiR-covered Bach chorales are TRANSPOSED relative to their When-in-Rome reference edition** (discovered in the OI-141 diagnosis, 2026-07-12). Signature: a constant whole-piece root offset (our chord root − DCML root, mod 12) covering 81–100 % of aligned chords — the same piece, shifted; confirmed by the notated key signature matching OUR reading, not DCML's (e.g. `bwv115.6` notated 1 sharp = G major, DCML global Eb, offset +4). Our reading is correct FOR OUR SCORE; the reference edition is in another key. Each is **100 % key-disagree AND 100 % root-disagree by construction** (`fail_dur == scored` for all 12) — pure ground-truth misalignment, NOT inference error. The 12 (offset from WiR global): `bwv126.6`(+2) `bwv267`(+11) `bwv180.7`(+2) `bwv30.6`(+2) `bwv184.5`(+7) `bwv145.5`(+2) `bwv39.7`(+3) `bwv177.5`(+3) `bwv244.62`(+10) `bwv73.5`(+3) `bwv148.6`(+1) `bwv115.6`(+4). They are **12.4/11.1/12.1 % of the key-disagree failing mass** (Baroque/Jazz/Default); removing them raises key-agree to 70.9/67.1/70.3 % (from 68.13/64.43/67.50). Because they are ALSO 100 % ROOT-disagree, they equally contaminate the **root-agreement column — the governing hard regression stop (63.36/62.37/63.25 %)** — and the class-(b) root-fail mass, a cross-cutting concern for the whole measurement substrate. Detection is a stated reproducible rule (`classify_key_disagreement._piece_transposition`, modal nonzero offset ≥ 70 % coverage). | `cc_key_mode_inference_diagnosis_report.md` §5; `tools/reports/key_mode_inference_diagnosis.json` (`transposition_contamination`); `tools/classify_key_disagreement.py`; OI-141; #9/#19 | ✅ DECIDED by the user, 2026-07-12: **ARITHMETIC CORRECTION** — the grading applies each piece's documented constant offset to the ground truth (per-stem offsets committed with provenance and per-stem verification; no score file and no ground-truth file edited; coverage stays 326). Executes as ONE re-baseline event together with the OI-143 dual-key grading decision, under the full ritual: outgoing reference snapshotted first (O-12), per-run explained diff (removals expected concentrated on the 12 stems), manifest re-stamped, the NEW baselines proposed by CC and RATIFIED by the user before the reference commit. Physical re-transposition of the 12 editions remains available later at the OI-38 corpus-expansion event. **✅ RESOLVED 2026-07-12 (adoption `d9b52ba969`, user-ratified before the reference landed): the arithmetic correction landed — 12 offsets committed in `tools/robust_stop/corpus_transposition_offsets.json` + independently re-verified per-preset (modal offset identical across presets; all ≥ 0.70 coverage), applied at the shared substrate `dcml_parser.load_wir_regions`. Run-diff confined to the 12 corrected stems (other 314 byte-identical, proven per preset); class-(b) root-disagree duration DECREASED on all presets (hard stop PASS); root-agree 63.36/62.37/63.25 → 66.04/64.98/65.93; key-agree(home) 68.13/64.43/67.50 → 71.29/67.49/70.52. O-12 snapshot `bd9e9c1ab2`. Report `cc_key_grading_rebaseline_report.md`.** |
| OI-143 | THE KEY-GRADING CONVENTION (user-ratified decision, 2026-07-12, from the OI-141 diagnosis): the ratified key-agreement column graded only against the DCML GLOBAL (home) key, counting correct modulation-following as failure (~43/38/42 % of the failing mass — the largest single class). DECISION: **grade against BOTH — a local-key column beside the home-key column**, both carried everywhere the key column appears (the a8 instrument, the robust-stop summary, the diagnosis classifier). Nothing is lost (both views kept); the headline distortion ends. Executes in the same re-baseline event as OI-142, same ritual, new baselines user-ratified before the reference commit. | user decision 2026-07-12; `cc_key_mode_inference_diagnosis_report.md` §1–§2; OI-142; CLAUDE.md gate blocks (the ratified baselines to be re-stamped) | **✅ RESOLVED 2026-07-12 (adoption `d9b52ba969`, user-ratified): the dual column landed everywhere the key column appears — the a8 summary (`b_key_agree_local` etc. + `key_agree_pct_local` in manifest), the robust_stop_diff report, the probe, the classifier. New key-agree LOCAL 65.72/62.49/65.39 (Baroque/Jazz/Default), reported BESIDE key-agree HOME 71.29/67.49/70.52. FINDING (surfaced): local is LOWER than home — our analyzer tracks the tonal home more faithfully than DCML's shifting local key; both views kept (nothing lost). Report `cc_key_grading_rebaseline_report.md`.** |
| OI-144 | WiR ground-truth loading has TWO views since the OI-142 correction (`d9b52ba969`): the four GRADED consumers (a8_rebaseline_measure, characterise_bir_false, measure_joint_probe, classify_key_disagreement) load via the corrected shared substrate `dcml_parser.load_wir_regions`, but ~50 historical/secondary WiR-reading scripts (`oracle_root_metric`, `compare_rn --wir-bach` mode, the `cc_*`/`diag_*`/`iter*` one-offs) still call `find_wir_file`+`parse_rntxt_file` directly (RAW, uncorrected). Scoped by the instruction (only the four graded surfaces routed); the raw view is correct for those secondary/diagnostic scripts. But any that later become a GRADED surface would be inconsistent with a8. The correction logic is single-sourced in `load_wir_regions` (opt-in is one call). | `cc_key_grading_rebaseline_report.md` (surprises §3); `d9b52ba969`; OI-142 | OPEN — **★ DISCOVERY D3 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): three of the "secondary" WiR consumers ARE graded surfaces and are on the RAW (uncorrected) parse** — `c1_reliability._load_wir` + (via it) `calibration_fit` feed the COMMITTED `tools/calibration_maps/*.json` + the reliability curves; `oracle_root_metric.load_dir` feeds the charged/floor root figures. MEASURED: patching `_load_wir` to `load_wir_regions` and re-running `calibration_fit` MOVES ALL 4 committed maps (all DIFFER) — the committed maps are fit on pre-OI-142 WiR (the 12 transposed pieces graded wrong). Routing them is the correct fix but MOVES committed artifacts → a RE-BASELINE needing the full ritual (O-12 snapshot + user ratification), NOT a byte-identical hygiene edit. NOT done this session; a prominent uncorrected-WiR caveat note added at each raw read site (`c1_reliability`/`oracle_root_metric`); SURFACED for the user's decision. The purely-diagnostic one-off scripts remain correct on the raw view. **Routing the graded surfaces waits on the user-ratified re-baseline.** **✅ RESOLVED 2026-07-13 — the routing + refit LANDED (`b3511fd28a`) and are USER-RATIFIED** (`cc_key_grading_and_calibration_rebaseline_report.md`): the two graded surfaces now read the ground truth through the ONE corrected substrate — `c1_reliability._load_wir` → `dcml.load_wir_regions` (and through it `calibration_fit`), and `oracle_root_metric.load_dir` likewise. **All four committed `tools/calibration_maps/*.json` were REFIT on the corrected ground truth and re-committed** (outgoing maps snapshotted first at `tools/calibration_maps/snapshot_2026-07-13_pre_oi132_oi144/`, O-12). **Attribution, measured separately:** the OI-132 mode-grading consolidation alone moves only the two L3 key-margin maps (max pointwise change in the fitted probability 0.008/0.009); the WiR routing moves all four (L3 0.093/0.089, L4 0.041/0.041). Held-out correctness RISES on every map (L3 0.679→0.685 / 0.673→0.679; L4 0.473→0.480 / 0.472→0.479) — the maps are now fit against correct labels; the held-out calibration error rises slightly with it (e.g. L3 Baroque 0.0262→0.0373), reported, not hidden. `oracle_root_metric`'s movement is confined to **exactly the 12 transposed stems on every preset** (set-verified against `corpus_transposition_offsets.json`): 625 events per preset LEAVE the unusable floor (music21 and DCML now agree there), ~445 proving correct and ~180 surfacing as genuine root errors that were previously invisible — a coverage gain, not a regression. **a8 / the robust-stop reference is BYTE-IDENTICAL** (it already read the corrected substrate), so this correction moves no governing figure. **★ A PREMISE OF THE DISPATCH WAS REFUTED AT THE CODE:** the instruction stated the calibration maps are "consumed at analysis time", making the refit a production-behavior change. They are NOT — the four maps are read by exactly two measurement instruments (`conformal_check.py`, `theta_fit.py`, both declaring no behavior change); no C++/CMake/resource file references them, and `tools/batch_analyze.cpp` reads only the score and its CLI parameters. **Established empirically as well as at the code:** the full corpus regeneration (352 scores × 3 presets, `hardening_battery --corpus-regen`) is byte-identical — **0 `.ours.json` differ on every preset**. The refit is a measurement-artifact re-baseline; no production region moved and no golden refresh was owed. |
| OI-56 | ★ AWAITING THE USER — **do we want a music-theory judge that works WITHOUT ground truth?** (adopted out of the §G submission-era backlog by the backlog triage, 2026-07-13.) **Checked:** `auto_review.py` exists nowhere in the repo; it was DESIGNED at `ARCHITECTURE.md:5717-5770` in three modes. **Mode 3** (score against known ground truth) is now massively over-served by the measurement chain (compare_analyses / compare_rn / characterise_bir_false / a8 / robust_stop_diff) — obsolete either way. **Modes 1/2** (judge on music-theory grounds alone, *"without corpus ground truth dependency… for repertoire not covered by DCML/WiR/Bach chorale annotations"*, `:5721-5725`) are the item's actual reason for existing and have **NO code on master** (zero `anthropic`/`openai`/`llm judge` hits in `tools/`+`src/`; `docs/llm_triage_design.md:3` — "Discussion only. No implementation committed."). **An implementation DOES exist on the unmerged `llm-triage` branch** (`git merge-base --is-ancestor llm-triage master` → 1; last commit 2026-05-14; `tools/llm_triage/` present there; `ARCHITECTURE.md:5744-5752` warns it "needs periodic re-merging"). **Why this is the user's call, not a rule application:** an LLM judge is NOT ground truth (#9; the standing "music21 is not ground truth" convention), so it could never GRADE us — at most it could TRIAGE (point a human at the scores most likely wrong). Whether that is worth having, and worth maintaining a branch for, is a scope judgement. **THE OPTIONS: (a) LAND IT, RESCOPED** — merge the `llm-triage` judge restricted to Modes 1/2, explicitly a triage instrument that never produces a graded number (cost: merging a two-month-old branch + maintenance; payoff: the first quality signal we would ever have on the ~163 unannotated scores, which matters more once OI-38 lands); **(b) DEFER to the corpus-onboarding event (OI-38)** — keep the row, decide when non-annotated repertoire is actually onboarded (costs nothing; the branch rots further); **(c) DROP IT** — declare the ground-truth chain sufficient and close (honest and cheap, but we accept never knowing how good the analysis is outside the annotated corpora). | `cc_backlog_triage_report.md` §4; `ARCHITECTURE.md:5717-5770`; `docs/llm_triage_design.md:3`; the `llm-triage` branch; STATUS post-sub #12 (the origin); OI-38/OI-57 | ✅ DECIDED by the user, 2026-07-13: **a HUMAN acts as ground truth where no formal ground truth exists** — the human decides by any method they choose, INCLUDING using the triage judge as guidance. So the judge lands eventually as a guidance instrument for the human-as-ground-truth workflow (never a grader, never a graded number); the branch is preserved until then. **WHEN is tied to the corpus-onboarding event (OI-38) and the user has put the timing itself up for debate** — "when must we do a lot of jazz, pop, Schönberg/Wagner/R. Strauss/the Monday Group/impressionism etc" — recorded as an open scheduling question AT OI-38. Row closes into OI-38's event scope. |
| OI-62 | ★ AWAITING THE USER — **is the intonation/tuning feature still a goal?** (adopted out of the §G submission-era backlog by the backlog triage, 2026-07-13.) **Checked at code — all six documented-not-implemented items are STILL missing:** `ARCHITECTURE.md` §11.3 specifies them and `STATUS.md:2106-2115` lists them — (1) voice-role-weighted centering (§11.3b: `minimizeTuningDeviation` uses an equal arithmetic mean; no voice-role type exists anywhere in `src/`); (2) duration-based susceptibility budget (§11.3c: `computeSusceptibility`, `notationtuningbridge.cpp:510-520`, returns `Free` for EVERY non-anchor note — its own comment: *"Duration-based and context-based classification is a future addition… All non-anchor notes are Free for now"*); (3) sustained fifth/octave protection (§11.3e step 2); (4) per-note susceptibility clamping (§11.3e step 5); (5) tuning-session drift state (§11.3d: `TuningSessionState` specified, absent from `src/`); (6) FreeDrift reset marker (§11.3f). **+ RE-FILED HERE from OI-58 (the old Known-Gaps "tie re-split", which was mis-filed under analysis):** one non-partial tie on ANY note skips the WHOLE chord for split-and-retune (`notationtuningbridge.cpp:1218`) — an INTONATION limitation; the analyzer itself has no tie defect (`note_model.cpp:45-46/57-60/181-192` — tie continuations skipped, exactly one onset per tied group). **Why this is the user's call:** every item is real, checked and unbuilt — but NOTHING in the current programme touches them. The whole arc (key layer, engage arc, certification audits) is harmonic ANALYSIS; no register row gates on tuning. So this is not a sequencing question but a scope one. **THE OPTIONS: (a) KEEP IT HELD** — confirm the six as a deliberate long-horizon hold (§H), revisited at a natural pause in the analysis work (they stay visible, never forgotten); **(b) SCHEDULE IT** — give the six an owner and a stage, competing for time with the key layer; **(c) CLOSE AS OUT OF SCOPE** — declare the tuning feature complete-as-shipped and drop the six from the register (the §11.3 specification stays in `ARCHITECTURE.md` as the record of what was designed). | `cc_backlog_triage_report.md` §4; `ARCHITECTURE.md` §11.3a–g; `STATUS.md:2106-2115`; `notationtuningbridge.cpp:510-520/1218`; memories `backlog_drift_reset.md`/`backlog_tuning_preference.md`; STATUS Known Gaps (the origin) | ✅ DECIDED by the user, 2026-07-13: **KEEP HELD — intonation IS a future feature** (the six §11.3 items + the tie limitation stay as a deliberate long-horizon hold, revisited at a natural pause in the analysis work). **AND the user stated the dependency that makes the hold strategic, recorded here and in the evidence inventory: the intonation feature will CONSUME the analysis facts** — knowing mode/chord/chord-function/progression enables just-intonation tuning decisions, especially in the TIME dimension (stay in tune over time vs allow drift) — i.e., intonation is a declared FUTURE CONSUMER of the published analysis surfaces, a concrete instance of the publish-evidence-broadly rationale. |

## F. Doc-sync debt (#10) — annotate, don't rewrite history

| ID | item | source | status |
|---|---|---|---|
| OI-45 | scoring_model §4/§6 stale anchors + kHalfDimFirstInversionBonus missing from §6 | premise-debt T3-4 | OPEN — fix at next scoring_model touch |
| OI-46 | Audit §3/§4 tables contradict §3.1 build-status on FQ-1/FQ-3 stage (contradiction A) | audit | OPEN — annotate tables |
| OI-47 | STATUS.md submission-era sections (Current State BIR 25/16, Post-submission priorities, Known Gaps, Future Considerations, cowork_handoff.md dangling ref) contradict governing docs (contradiction C). **Re-verified at HEAD (backlog triage, 2026-07-13):** all four sections still present and still contradicting — `STATUS.md:570` (Current State; BIR 25/16 at `:585`), `:2302` (Known Gaps), `:3721` (Post-submission priorities), `:3730` (Future Architectural Considerations). Their figures are superseded by the governing robust-unit stop (CLAUDE.md block A). | STATUS.md; `cc_backlog_triage_report.md` §1 | OPEN — **the TRIAGE half is ✅ DISCHARGED** (2026-07-13: every §G submission-era row is now verdicted — SUPERSEDED / ASSIGNED / USER-DECISION; the §G rows the four sections spawned are OI-53…OI-62). **The BANNER half remains ASSIGNED:** mark the four sections historical (annotate, don't rewrite — this section's own rule) at the next STATUS.md doc-sync touch |
| OI-48 | `backlog_chord_track_flag.md` referenced by two live docs, file DOES NOT EXIST (contradiction J). **RESOLVED AS A FACT (backlog triage, 2026-07-13): the file EXISTS — it is a Claude Code MEMORY** (`~/.claude/projects/c--s-MS/memory/backlog_chord_track_flag.md`, 1017 bytes), never a repo file, so **nothing is lost**; what dangles is the repo-internal reference. Referenced from `STATUS.md:3744` (the Future-Architectural-Considerations list) and cited as the canonical DT-12 example at `DEFECT_TYPES.md:24` (self-referential — resolving the reference makes that example stale, note it there). **The SUBSTANCE is live and real at the code:** `regiontonecollector.h:73` `isChordTrackStaff()` still detects the chord track by staff NAME; the Part-level-flag replacement is unbuilt (also listed at OI-61). | STATUS refs; `cc_backlog_triage_report.md` §1; `regiontonecollector.h:73`; OI-61 | OPEN — **ASSIGNED (doc-sync):** re-point `STATUS.md:3744` at this register row (the ONE home) and note the `DEFECT_TYPES.md:24` example is spent, at the next STATUS.md touch. The code substance rides OI-61 |
| OI-49 | D-L3a "closed" (fitter) vs "remains" (roadmap G1) (contradiction K) | fitter §4.5; roadmap | OPEN — reconcile |
| OI-50 | FQ-2 "decided at Stage 2" (arc plan) vs "enumerated-not-resolved" (L5-engage) (contradiction G) | arc plan; L5-engage | OPEN — annotate arc plan |
| OI-51 | Foreign uncommitted `cowork_joint_key_chord_design.md` SHELVED-banner edit sitting in the working tree since session 35 | cc_eg2 §0 | OPEN — fold or discard |
| OI-150 | `BUILD_AND_TEST.md` stale test baselines (backlog triage, 2026-07-13; DT-12/#10 — found by RUNNING the suites at HEAD): `:103` states composing "**Current baseline: 974/974** passing, 2 disabled" — measured at HEAD **1101/1101 passing, 2 disabled**; `:114` states notation "**Current baseline: 53/53** passing" — measured at HEAD **53 passing and 4 SKIPPED**. The notation line is the harmful one: it reports a clean suite and **hides the four key-emission xfails** (OI-148), so a reader checking "do the tests pass" is told yes and never learns four user-visible DCML-checked cases are failing by design. Both lines carry a "(verify with CC — count changes as tests are added)" caveat that was never exercised. | `cc_backlog_triage_report.md` §6; `BUILD_AND_TEST.md:103/114`; measured suite output at HEAD `3966502265`; OI-148; DT-12 | OPEN — re-stamp both baselines AND make the notation line name the 4 xfails (a passing count that conceals xfails is worse than no count) at the next `BUILD_AND_TEST.md` touch (doc-only, #10) |

## G. Submission-era backlog — ✅ TRIAGED 2026-07-13 (`cc_backlog_triage_report.md`); rows either CLOSED here or ADOPTED into A–F/H

> **The triage mandate is DISCHARGED.** Every row below carried the plan "triage: verify this is still
> real, then assign or supersede"; every one now carries a checked verdict. **Adopted out of this
> section** (each moved to its proper section with owner/stage/gate): **OI-55** → §B (E4 decoder
> engagement), **OI-56** → §E (user decision), **OI-57** → §D (corpus-onboarding event), **OI-58** → §H
> (rescoped to its 3 surviving sub-items), **OI-61** → §H, **OI-62** → §E (user decision). **Closed
> below:** OI-53, OI-54, OI-59, OI-60. **OI-63** was never a triage row (already assigned — OI-145 wave 2).

| ID | item | source | status |
|---|---|---|---|
| OI-53 | Tonicization classifier (V/V, V/ii) wired, not implemented | STATUS post-sub #9 | ✅ **SUPERSEDED 2026-07-13** (backlog triage; verified at the code, not at assertion) — **the claim is refuted both ways.** A tonicization classifier IS implemented AND IS live: `chordsymbolformatter.cpp:901-940` emits `V7/x` and `vii°/x` (fires on `nextRootPc >= 0`, excludes the tonic so plain V→I stays "V7"), fed **unconditionally** by `backfillNextRootPc` (`regionanalyzer.cpp:284-290`, called at `:1465`) and user-reachable via `formatRomanNumeral` (`notationcomposingbridge.cpp:492/809/944/1159`; `notationcontextmenumodel.cpp:87`). A RICHER version — applied/secondary labeling with the chromatic-leading-tone guard, plus the tonicization-vs-modulation arbiter — is implemented and **dormant** in the certified L5 machinery (`functionrelationallabel.cpp:215` `emitAppliedLabel`, `functionmodulation.cpp:26` `decideTonicizationVsModulation`, `tonicizationlabeler.cpp:64`), engaging at E4. **Carried forward (#12), not dropped:** the LIVE labeler's known limits — only dom7 and dim/half-dim trigger (a plain major-triad V/x is dropped, stated at `functionrelationallabel.cpp:229`) and the inline path has no chromatic-leading-tone guard — are **subsumed by the dormant L5 labeler at its engage** (that is the fix; do not patch the inline path, #8). Also stale: `ARCHITECTURE.md` doc-version 3.25's "tonicization and augmented sixth labels deferred (no classifier implemented)" → fold into OI-107/OI-112. |
| OI-54 | Pedal-point calibration needs corpus evidence (= OI-4/OI-38 pedal-dense corpus) | STATUS post-sub #10 | ✅ **SUPERSEDED — merge re-confirmed 2026-07-13** (both absorbing rows OPEN and both still carry the pedal-dense corpus). Stays listed until OI-4/OI-38 close, per this register's own MERGED convention. *(The related piano-sustain-pedal DECAY gap — a different thing from the harmonic pedal point — is OI-58 sub-item 5, §H.)* |
| OI-59 | Corelli notation regressions (Gm vs G m1b3 etc.) + 4 deferred notation tests + chopin_bi105 segmentation cascade | STATUS known regressions | ✅ **SUPERSEDED 2026-07-13 — all three claims dead (built + ran the suites at HEAD).** **(a) Corelli "Gm vs G m1 b3": FIXED** — the expectation was reverted to `"G"` exactly as `STATUS.md:646` prescribed (`notationimplode_tests.cpp:1165`; the sibling m10 b3 correctly KEEPS "Gm" at `:1173`); both formerly-failing Corelli implode tests are active and passing. **(b) chopin_bi105 cascade: NEVER TRIGGERED** — it was only ever an indirect consequence of *landing* the deferred key-confidence-gated dominant-quality fix, which was never landed; the golden `chopin_bi105_op30_2.json` shows tick 4800 as ONE unsplit F♯m region (no Bm at the head of `[4800,6240)`). **#12 carry — the latent fragility is NOT dropped:** re-attempting that specific fix re-triggers the cascade → recorded as a standing caution at **OI-70**. **(c) "4 deferred notation tests": the COUNT still holds but the TESTS are a different set with a newer root cause** (measured: 53 pass + **4 SKIPPED**) — all four are `a6b08af3fe` key-emission xfails → **NEW ROW OI-148** (they had no owner in the register). |
| OI-60 | Blocking/needs-fix trio: chord-symbols-as-input context-menu path; declaredMode soft-boost; implode chord-track gaps | STATUS next-session | ✅ **SUPERSEDED 2026-07-13 — all three fixed (verified at code).** **(a) chord-symbols-as-input: FIXED** — the symbol-reading path was DELETED wholesale (`02e3733afb`); `forceClassicalPath`/`scoreHasChordSymbols`/`analyzeHarmonicRhythmJazz` have **zero hits in `src/`** (`ARCHITECTURE.md:3454-3459` — "no Jazz path and no symbol-reading gate"). *The row's polarity was misleading:* reading symbols was the BUG to remove, not a wanted feature; consuming them as INPUT EVIDENCE is a distinct FUTURE item, already carried at **OI-80**. **(b) declaredMode soft-boost: FIXED** — the hard override is gone (`keyresolver.h:43-45`); it is now a graded additive prior (`keymodeanalyzer.cpp:594-603`; `declaredModePenalty=1.0`, fit-bounds {0,15}, droppable via `ignoreDeclaredMode`), regression-covered (`decode_keymode_tests.cpp:379-405`). The *remaining* declaredMode concern (siloed to the key path) is **OI-78**, untouched. **(c) implode chord-track gaps: both named mechanisms VERIFIED PRESENT** — the repeated-chord-suppression escape hatch (`notationimplodebridge.cpp:646-679`, `kSameChordReannotationGap` at `:661`) and the head-gap AND tail-gap synthesis safety nets, which survived the move into `harmonicsegmenter.cpp:799-896`. **Residue stated honestly (#12):** the original "Oak and the Lark" fixture is GONE from the repo, so that specific three-symptom report is not re-testable; implode gap-filling has no dedicated fixture beyond Corelli — a score-acquisition matter that rides OI-38. |
| OI-63 | S7 full mode-prior single-sourcing (Stage-1 leftover, dependency-profile call) | audit; arc plan | OPEN — **not a triage row:** already ASSIGNED (OI-145 wave 2 — the substrate hygiene set); the harness half of the same concern rides OI-135 |

## H. Long-horizon roadmap (held deliberately; listed so they are never "forgotten")

| ID | item | source | status |
|---|---|---|---|
| OI-64 | Engage plan E1–E5 (wire → A/B → default-ON → retirements → seal); gates G1–G6 (G2 not met at E0) | roadmap | OPEN — the arc itself. **Hold confirmed 2026-07-13** (backlog triage): trigger/owner accurate, now gated by TWO things — the Stage-3 entry gate (OI-1…OI-7) **and** the key-layer readiness gate (OI-145) |
| OI-65 | Retirement map R1–R9 (R9 = chordanalyzer.cpp split, LAST) | roadmap | OPEN. **Hold confirmed 2026-07-13:** trigger (E4) and owner still accurate |
| OI-66 | Recognition consumer (encyclopedia → L5 prior) designed-not-built | roadmap | OPEN — deferred. **Hold confirmed 2026-07-13:** trigger (the recognition-consumer build; roadmap A-6) still accurate |
| OI-67 | Style-clustering / idiom auto-detection / mixtures | fitter A-8ask; `idiom_discovery/` (commits `d9b2020623`, `95374ef16a`, `2a3c767dae`) | OPEN — deferred. **Hold confirmed for the BUILD 2026-07-13, but the row's source was UNDERSTATED:** a cross-tradition harmonic-idiom study has since **LANDED** — `idiom_discovery/` (5 idioms + the voice-leading axis + orthogonality; `research(idiom-discovery)` `d9b2020623`, `feat(idiom-discovery)` `95374ef16a`, `feat(tools) --dump-vl` `2a3c767dae`). The evidence base for OI-39's "idiom coverage 1/5" now EXISTS; the auto-detection BUILD stays deferred to Stage 5+ |
| OI-68 | Capability tracks A-3/A-4/A-5 + NCT L4 lever + voice-leading axis | roadmap (`docs/implementation_roadmap.md:233`) | **★ PARTLY PROMOTED 2026-07-13 (backlog triage — the A-3 trigger has QUIETLY ALREADY FIRED): A-3 → STILL REAL — ASSIGNED to the key-layer build.** A-3 = "**dominant-implication key evidence in the L3 emission** (L3 §15)". The key-layer work now names exactly this channel: the OI-141 mechanism report found "**no cadence/dominant channel in the decode** (OI-68)"; the design opening's **decision 3** IS the cadence→key channel; and **the user CONFIRMED leading-tone/cadence work as the SECOND lever** (recorded at OI-141). So A-3 is no longer a deferred capability track — it is an active key-layer design input (owner: OI-141/OI-145; the dormant certified L5 cadence machinery is the already-built channel, with OI-118/OI-119 to reconcile at engage). **The rest stay HELD:** A-4/A-5 (L5 §15) and the voice-leading axis — though the **NCT-filter L4 lever** now has a named consumer, **OI-55** (the ninth melody/harmony conflation) |
| OI-69 | Joint segmentation (past Stage 5) | roadmap | OPEN — deferred. **Hold confirmed 2026-07-13:** trigger (past Stage 5) still accurate |
| OI-70 | B3 dim7-template dead end + rootContinuity sparse-gate dead end + Gate-A enharmonic constraint (standing cautions §8). **★ + ONE CAUTION ADDED 2026-07-13 (the #12 carry out of OI-59's closure): re-attempting the key-confidence-gated dominant-quality fix** (promote a thirdless Aeolian-degree-4 chord from natural-minor v to common-practice V) **re-triggers the Chopin op30-2 segmentation cascade** — a 1-PC thin dominant at `BI105-2op30-2` tick 23040 splits the unrelated `[4800, 6240)` F♯m region into Bm + F♯m, DCML-incorrect at the head of the split. The fix was never landed, so the cascade has never fired (the golden shows one unsplit F♯m region); the fragility is LATENT and returns only if that specific fix is re-attempted. Its original motivation (the Corelli m1 b3 "Gm") was resolved by a different route, so the trigger is no longer needed. | scoring_model §8; `cc_backlog_triage_report.md` §2; `STATUS.md:636-646` (the original diagnosis) | STANDING constraints |
| OI-71 | Roadmap 0.1 doc pass (stale explorationMode refs; untracked audit doc) | roadmap 0.1 | ✅ **SUPERSEDED / DONE 2026-07-13 (backlog triage — a row left open for work finished five weeks earlier).** All three claims discharged, verified: (a) `docs/layer_architecture_audit.md` **IS tracked** — committed at **`7bc1609159`** (2026-06-10, *"docs: consolidated roadmap + architecture-review updates (Stage 0.1)"*, 429 lines added); (b) `explorationMode` occurs **0 times** in that doc; (c) the 3 remaining occurrences in `ARCHITECTURE.md` (`:1564/:1570/:1856`) are **accurate historical notes** explaining the rename to `ScoringPhase` ("Iter 94 as `explorationMode`; reworked to the enum in `e7d4ba2b1a`"), NOT "as-live" references — so the "docs grep clean" acceptance is met. The roadmap itself already marks **Stage 0 COMPLETE** citing that very commit (`docs/implementation_roadmap.md:271`). Row closes. |
| OI-58 | Known-Gaps survivors (adopted out of §G by the backlog triage, 2026-07-13 — rescoped to the **3 sub-claims that survived checking**; each of the six was checked at the code). **✅ CLOSED sub-claims, recorded here so nothing is lost (#12): (4) MusicXML sus export bug — SUPERSEDED/FIXED** in this fork at `exportmusicxml.cpp:9040-9042` (commit `70e679e819`; upstream still carries the index-assign-into-an-empty-String bug that makes the degree-text guard never fire) — **residue carried: no C9sus2 export regression test locks it**. **(6) Rampageswing walking bass — SUPERSEDED, the description is REFUTED:** it is a 36-file **horn-only** big-band corpus (`tools/corpus_rampageswing_full/`), and a horn-only chart has **no bass part at all**, so walking-bass passing tones cannot be diluting the root; the later `--inject-written-root` experiment raised agreement **39.8 % → 98.3 %** (`ARCHITECTURE.md:2036/2132`, `STATUS.md:1494-1500`), diagnosing an **absent** root/bass — the opposite failure. **(2) Tie re-split — MIS-FILED, re-filed at OI-62:** it is an INTONATION limitation (`notationtuningbridge.cpp:1218` — one non-partial tie on ANY note skips the WHOLE chord for split-and-retune); the analyzer has no tie defect (`note_model.cpp:45-46/57-60/181-192`). **THE THREE THAT SURVIVE: (1) Tone weight — RESTATED, the original wording was inaccurate:** `weight` (`analysistypes.h:137`) IS populated on the region path (`regiontonecollector.cpp:384` — duration-fraction × beat weight, + repetition/cross-voice/pedal-tail terms) but is left at the **1.0 default on the single-tick `buildTones` path** (`regiontoneprimitives.cpp:112-120`, used by the status-bar / analyze-at-tick bridge); and **nothing anywhere populates it from dynamics or velocity** — `NoteEvent` carries no velocity field at all (`note_model.cpp:51-66`). True gap: *duration × metric only, never loudness; and one path defaults to 1.0.* **Owner: the E4 tone-surface / fact-publication design** (with OI-82 per-note metric salience, OI-74 voice identity); detail home = memory `backlog_tone_weight.md`. **(3) Cadence labels are hardcoded English:** raw literals `"PAC"`/`"PC"`/`"DC"`/`"HC"` at `sectioncadencedetection.cpp:104/106/110/137`, written verbatim to the score at `notationcomposingbridge.cpp:1244`; **zero** `qtrc`/`mtrc`/`TranslatableString`/`tr(` anywhere in `src/composing/`. **Owner: a product/i18n pass** (memory `backlog_cadence_i18n.md`) — held, gates nothing. **(5) Piano-sustain-pedal DECAY model absent:** pedal tails ARE carried but with a **flat** discount (`regiontonecollector.cpp:316-341`; `pedalTailWeightMultiplier = 0.3`, `analysistypes.h:325`) — no decay term, although the codebase HAS a decay primitive (`shv::timeDecay`) used only for span windows. Distinct from the harmonic pedal POINT (built; `chordpostpasses.cpp:190-275`). **Owner: the pedal-calibration family (OI-4/OI-54), which needs the pedal-dense corpus (OI-38).** | STATUS Known Gaps; `cc_backlog_triage_report.md` §2; the code citations above | OPEN — held; the three owners named above. None gates any stage |
| OI-61 | Future Architectural Considerations (adopted out of §G by the backlog triage, 2026-07-13; the six items at `STATUS.md:3730-3745`). **Hold confirmed as a list**, with **two dedupes (#6 — one home per concern): (i) "Voice role information in `HarmonicRegion`" is the substance of the ACTIVE row OI-74** (voice/staff identity dropped at `ChordAnalysisTone` — the shared tone surface is voice-blind), owned by the E4 fact-publication/surface design and OI-145 wave 3 — **and its stated trigger ("revisit when sophisticated tuning algorithm is implemented") is now WRONG**: the trigger fired by a different route entirely (the evidence-publication design), so the item is promoted THERE, not here; **(ii) "`isChordTrackStaff()` → Part-level flag" is OI-48's substance** (still name-based at `regiontonecollector.h:73`). The remaining four (bridge-file reorganization by musical concept; instance-based vs static analyzers; the `HarmonicRegion` include-pair friction; the "chord track" → "chord staff" rename, ~31 occurrences) are genuine long-horizon holds with accurate triggers. | STATUS FAC (`STATUS.md:3730-3745`); `cc_backlog_triage_report.md` §5; OI-74; OI-48 | OPEN — long-horizon hold (4 items); 2 items deduped into OI-74 / OI-48 |

## I. Siloed analyzed facts (fact-publication sweep 2026-07-10 — `cowork_siloed_facts_audit.md`)

One root cause, one design owner: the shared cross-layer surfaces are voice-blind,
spelling-blind, membership-blind — resolved by the **E4 carry/surface design** (which facts the
shared surfaces publish), not per-site patches. OI-15 (spelling) is the headline instance.

| ID | item | source | status |
|---|---|---|---|
| OI-72 | `StepwiseSignals` (per-note suspension/step/leap, per voice) TRAPPED in decoder membership — the voice-leading evidence L5 selection + pedal need | siloed audit #1 | OPEN — E4 surface design |
| OI-73 | Membership verdict (`chordTonePcs`/`nonChordTonePcs`) dies at the L4→L5 boundary — `FunctionSlice` never copies it | siloed audit #2 | OPEN — E4 surface design |
| OI-74 | Voice/staff identity dropped at `ChordAnalysisTone` — the shared tone surface is VOICE-BLIND (structural root of OI-72/73; pedal "upper-voice" has no channel) | siloed audit #17 | OPEN — E4 surface design |
| OI-75 | `keyAlternatives`/`keyConfidence`: ZERO production consumers (diagnostics only) — the L5 carry contract's key inputs sit unread. **L3 PASS-1 re-confirmed independently (2026-07-11, OI-75):** both are PUBLISHED-but-unconsumed (declared dormancy, consumer named = L5 modulation recompute); further, the production 0.8 KeyArea/cadence gate reads the emission SIGMOID (`keyModeResult.normalizedConfidence`), NOT the better-calibrated boundary MARGIN (`keyConfidence`, code-noted 2.8–3.1× better calibrated) — the deliberate D-L3a stance (relates OI-49 D-L3a "closed"-vs-"remains" contradiction). | siloed audit #4/#5; `cc_l3_audit_pass1_report.md` §5 OI-75; OI-49 | OPEN — consumed at L5 engage |
| OI-76 | Cadence detections transient (recomputed per emit path, never stored on regions); L5 cadence channel blocked; production detector circular by design | siloed audit #7 | OPEN — E4 |
| OI-77 | Bass-chord-tone/inversion VERDICT recomputed at ~60 call sites (facts shared, derivation duplicated) — channel #1 has no published form | siloed audit #11 | OPEN — E4 primitive |
| OI-78 | `declaredMode` siloed to the key path; chord diatonic bonus reads only the inferred key | siloed audit #12 | OPEN — design input |
| OI-79 | Duplicated constants: pedal sigmoid inlined (2.0/1.5) vs prefs; emission sigmoid written in two files (S10) | siloed audit #16 | OPEN — fold at next touch |
| OI-80 | Score-annotation input facts (chord symbols/RN/Nashville) recognized as TODO flags, never read | siloed audit #14 | OPEN — long-horizon |
| OI-81 | Key runner-up (closeness of the 2nd-best key) folded into a confidence number and discarded in-analyzer; only the batch tool serializes it — the L5 key-consistency channel wants it | siloed audit #6; dossier B6 | OPEN — E4 fact-publication design |
| OI-82 | Per-note beat weight / fine metric salience decoder-private (region-level IS published) | siloed audit #10; dossier B10 | OPEN — E4 fact-publication design (with OI-72/73/74) |
| OI-83 | Dispositions declared OK-BY-DESIGN, kept visible: `bothLicensed` (Stage-5 fitter telemetry) and the fan-out SUMMARY (diagnostic); the LOST uncapped readings behind the summary are the OI-9 distinct-root carry | siloed audit #8/#9; dossier B8/B9 | DECLARED — no action; the loss itself is OI-9 |

**Coverage note (user-challenged 2026-07-10): all 17 siloed-facts findings are now mapped** —
1→OI-72, 2→OI-73, 3→OI-15, 4/5→OI-75, 6→OI-81, 7→OI-76, 8→OI-83, 9→OI-9(+OI-83), 10→OI-82,
11→OI-77, 12→OI-78, 13→OI-4, 14→OI-80, 15→OI-11, 16→OI-79, 17→OI-74. Items 1/2/3/10/17 are ONE
design decision (the E4 fact-publication/surface design — dossier Part B); the proposed
"publish-once, consumers-read" corollary awaits ratification (dossier, end of Part B).

*Sweep provenance: session-36 full-repo sweep (12 surfaces, 91 raw items, 11 contradictions) —
the raw sweep detail with file:line citations is preserved in the session record; rows above
consolidate duplicates (e.g. pedal appears once as OI-4 with merged sources). Items marked
MERGED/superseded stay listed until their absorbing row closes.*
