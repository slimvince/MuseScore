# OPEN ITEMS REGISTER — the ONE home for every discovered-but-unresolved issue

> **Created 2026-07-10 (session 36), user-directed** after a full-repo sweep found 91 open items
> scattered across 12 tracking surfaces with 11 status contradictions. **Split into a lean INDEX +
> per-item detail files 2026-07-26 (user-ratified option 1):** this file is the complete INDEX and
> the **authoritative status surface**; every item's full original row (text + source + status)
> lives verbatim in `open_items/OI-<n>.md`, which carries narrative and provenance only and never a
> status of its own. **Standing rule (CLAUDE.md "Open-items register" section):** read this INDEX at
> session start; a stage may not open while a register item gating it is open; every new discovery
> gets an index row AND its detail file in the same commit that records it; every resolution flips
> its INDEX row with provenance (the detail file gains a dated resolution note, never a status of
> record). Detail lives in `open_items/`; rows here are pointers, not restatements. IDs are stable;
> do not renumber.
>
> **★ EVERY STATUS CELL BEGINS WITH ONE CANONICAL TOKEN** — the resolved mark at the head of the
> cell, or one of the open-state words. A row's state is read from that token and never by searching
> its prose, so a cell may name any other row's resolution freely, and a resolution spelled only in
> words is not a state. **This line is a POINTER, not the rule's home:** the vocabulary and the check
> are `tools/audit/index_status_lint.py`, which STOPs on a non-canonical opening, and the ONE index
> parser STOPs on a row that does not split into six cells. Ruled by the user 2026-08-09 (Ruling 33
> of `cowork_rulings_2026_08_09_fifth_stop.md`) over the family [[OI-356]] / [[OI-361]] / [[OI-362]].

> **★★ GOVERNING ARCHITECTURE DECISION (user-ratified 2026-07-17) — read `cowork_joint_estimator_architecture.md`.**
> The key/mode/chord estimator is **JOINT (option A)** — one probabilistic decode over `(tonic, mode, chord)`
> with segmentation as a modeled (semi-Markov) variable and every enumerated clue as a theory-grounded factor;
> NOT a feed-forward pipeline, NOT a separable "key layer." Theory basis: `cowork_key_chord_joint_inference_grounding.md`.
> This **reframes many rows below as A-factors or superseded** (architecture doc §3): OI-166 → the L1.5 cadence
> factor; OI-168/OI-170 → the signature-mask collection emission term; OI-174/OI-147 → A's mode-vocabulary
> question; OI-175 → superseded by the joint decode; OI-23/OI-91/OI-97 (hand-set constants) → retire into A's
> fit-once factors; OI-145 wave-1 (the measurement chain) is RETAINED. **Immediate next action:** the
> term-level theory-grounding audit (READ-ONLY, the #17 funnel's first stage — enumerate every factor, derive
> each form from established theory #1/#2, triage keep/fix/drop). The estimator funnel stays SHUT until the
> forms are pinned and ratified; forms from theory, values fit ONCE against GT (#19), never per-case tuned
> (#8/DT-2). Full arc: `cowork_handoff.md` 2026-07-17 entry.


## A. STAGE-3 ENTRY GATE — blocks E4/L5 engagement (from `cowork_engage_arc_plan.md`)

| ID | name | description | layer / gate | status | detail |
|---|---|---|---|---|---|
| OI-1 | EG-1: T1-1 resolver progression-first re-ordering | The arc-#9 resolver progression-first re-ordering must land before production. | Stage-3 entry gate | OPEN — prerequisite | [detail](open_items/OI-1.md) |
| OI-2 | EG-1: T1-2 F-B override demotion | Demote the unconditional attemptFineGrainOverride (arc-#11 design, −756). The redesign that discharges this row is now registered: **D-492** recommends demoting it to an annotation carrying the earlier reading unchanged, floored by disabling it; **D-490** and **D-491** are the falsification and the refuted repair behind that recommendation, **D-493** the un-computable long-run successor. | Stage-3 entry gate | OPEN — prerequisite. **★ THIS ROW IS WHERE THE PHASE-3 FIX PLAN PICKS THE F-B REDESIGN UP (recorded 2026-08-04, CC, READ WAVE 3, dispatch `cc_instruction_reads_3.md` Task 4.1, on the user's ruling R4).** D-492's recommendation is an **INPUT** to the ONE prioritized fix plan **D-231**'s phase 3 assembles — **not an authorization**, and it is NOT acted on: D-231 forbids fix design before phases 1 and 2 close, and D-490's falsification changes the evidence, not the sequencing. **★ AND THE ARM IS CHECKED RATHER THAN ASSUMED (Task 4.2, read at the code at HEAD):** the measured-harmful mechanism is **NOT running today, on any arm** — `attemptFineGrainOverride` has one caller, `resolveCarriedReadings`, whose only non-test caller in the tree is inside `runFullSpine`, reached only from the `--dump-fullspine` block, which returns before `analyzeScore`/`analyzeRegions`; the full citations are on **D-492**. So it is dormant behind a return-early diagnostic dump flag and the test suites — **narrower than "legacy"**: plain flag-less `batch_analyze`, the reachability path [[OI-289]] established for the legacy arm generally, does not reach it either. **No STOP.** | [detail](open_items/OI-2.md) |
| OI-3 | EG-2: rebuilt-vs-legacy go/no-go | The rebuilt-vs-legacy decoder go/no-go decision. | Stage-3 entry gate | PROBED — P1 not supported as measured (abstention artifact); honest re-measure blocked on OI-1/OI-2; decision with user | [detail](open_items/OI-3.md) |
| OI-4 | EG-3: pedal reader hard-gated on owed-P1 | The pedal reader is hard-gated on the owed P1 over an established pedal-dense corpus (premise currently unfavorable 0.20/0.50/0.20, n=2–5). | Stage-3 entry gate / L5 | OPEN — gated | [detail](open_items/OI-4.md) |
| OI-5 | EG-4: confidence-scale commensurability | Confidence-scale commensurability (T1-3/S19/F-1/D-FS) owes a #17 ledger + desk sim before any θ/kBoundary fit; combinedBoundary calibration already failed (D-8). | Stage-3 entry gate | OPEN | [detail](open_items/OI-5.md) |
| OI-6 | EG-5: extend param_manifest to L1/L2 + live-L3 constants | Extend tools/param_manifest.json to L1/L2 constants + live-L3 hysteresis margins before Stage 5 closes (T3-1). | Stage-3 entry gate | OPEN | [detail](open_items/OI-6.md) |
| OI-7 | EG-6: Jazz validation status | Establish a jazz GT corpus or de-scope Jazz correctness claims (T3-2). | Stage-3 entry gate | OPEN | [detail](open_items/OI-7.md) |

## B. Owned by Stage-3 / E4 (build + design decisions at the engage step)

| ID | name | description | layer / gate | status | detail |
|---|---|---|---|---|---|
| OI-8 | FQ-4 anchor: decoder carry replaces results | The decoder carry replaces the results cap→append (pedal clobber + Iter 86/91 die by construction). | Stage-3 / E4 | OPEN | [detail](open_items/OI-8.md) |
| OI-9 | Distinct-root-preserving carry | topK caps voicings so a ≥3rd root is not guaranteed (exclusion tail, #12). | Stage-3 / E4 | OPEN | [detail](open_items/OI-9.md) |
| OI-10 | FQ-2 quality-from-key single owner | A single owner for quality-from-key (S12/S14/S15/S16/S17), decided with the §6-block dissolution. | Stage-3 / E4 | OPEN (owner named-not-chosen — contradiction G) | [detail](open_items/OI-10.md) |
| OI-11 | FQ-1 different-root primitive | Retires with the decoder (reassigned E4; audit §3 table stale). | Stage-3 / E4 | OPEN | [detail](open_items/OI-11.md) |
| OI-12 | FQ-3 findTemporalContext ownership move | Reassigned E4 (audit §3 table stale). | Stage-3 / E4 | OPEN | [detail](open_items/OI-12.md) |
| OI-13 | FQ-8 owed migrations | Two-segmenters, two-pitch-context, tpc-reader fold, function/ rename (S18). | Stage-3 / E4 | OPEN | [detail](open_items/OI-13.md) |
| OI-14 | PC-1 E4 symmetric-set detection design question | Symmetric-set detection from the pc set vs land a C2/G5 dim7 type vs generalize the spelling channel (OI-15). | Stage-3 / E4 | OPEN — design decision at E4, each option owes #17 ledger | [detail](open_items/OI-14.md) |
| OI-15 | Spelling as a first-class evidence primitive | User-raised — tpc facts are L1-carried but consumed only by the pin; O1 measured ~60% of Baroque residual spelling-resolvable; design where the general spelling channel lives (#6/#7). | E4 / L5 | OPEN — E4/L5 design input | [detail](open_items/OI-15.md) |
| OI-16 | T1-4 decoder rotation assumes the key prior correct | The decoder rotation assumes the key prior is correct (diatonic terms are its only key input, spelling-pin the sole guard) + a latent sameChordVoicing/sameChordSymbol inconsistency. | Stage-3 / E4 | OPEN — surfaces at E4 | [detail](open_items/OI-16.md) |
| OI-17 | PC-3 Xsus fifth mis-rooting | Xsus fifth mis-rooting (43% of EG-2 new errors); hypothesis unchecked (bass-on-fifth + sus template), checkable at bwv87.7@9600. | L4 scorer | OPEN — L4 scorer owner; check at E4 design, fix per #8 timing | [detail](open_items/OI-17.md) |
| OI-18 | L4 temporal-extension trigger specified not coded | The trigger is specified not coded (silently truncates); the whole §2.15 extension cluster is exercised nowhere; a unified orchestration contract is owed. | L4 / engage | OPEN — at engage | [detail](open_items/OI-18.md) |
| OI-19 | S18 function/ dir rename | Rides R7. | Stage-3 / E4 | OPEN | [detail](open_items/OI-19.md) |
| OI-20 | Jazz +1 interim class-(a) case | Retires when Layer 4 pins the rotation/center. | L4 | OPEN — interim | [detail](open_items/OI-20.md) |
| OI-21 | owed-P2 confirmation-gap reproduction | Carry margin vs pass-2 sigmoid. | Stage-3 / E4 | OPEN — post-E4 | [detail](open_items/OI-21.md) |
| OI-22 | owed-FB1 F-B annotate class-(b) movement | The F-B annotate must move class-(b) duration favorably at its build event. | Stage-3 / E4 | OPEN — at F-B build | [detail](open_items/OI-22.md) |
| OI-84 | Full-coverage audit plan (EG-7) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | EG-7 / all layers | ✅ COMPLETE 2026-07-12 — every surviving layer (L1/L2, L3, L4, L5+instruments) certified on two passes each; … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-84.md) |
| OI-86 | L1/L2 pass-1 layering findings | Upward cross-layer deps + mixed-layer grab-bags + two metric-weight tables + value-copied SpanWindowWeights (DT-19); much rides existing retirements. | L1/L2 | OPEN — dissolves at E4 retirements; re-verify at each | [detail](open_items/OI-86.md) |
| OI-88 | L1/L2 pass-2 doc-precision findings | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | L1/L2 doc | ✅ RESOLVED 2026-07-11 (f76e8b65c8, comment-only, no behavior change) → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-88.md) |
| OI-90 | L3 pass-1 file-table mis-tags | chordpathdecoder/sparsechordrefinement are L4 not L3 (DT-21); the L1/L2 file-table reason strings are now stale. | L3 / audit | OPEN — correct the L1/L2 file-table reason at its next touch (comment/data-only) | [detail](open_items/OI-90.md) |
| OI-92 | L3 pass-1 duplicated table | kJkdTemplates is comment-declared identical to a Python table but not sync-test-guarded (DT-3). | L3 | OPEN — add a sync test or single-source at the joint-key wiring touch | [detail](open_items/OI-92.md) |
| OI-93 | L3 pass-1 cross-layer deps | Section headers include the heavy chord/chordanalyzer.h just for ChordQuality; 5 core cpp include chord/analysisutils.h for pc primitives (DT-19). | L3 | OPEN — fix (a) at the next section-header touch; (b) rides a shared-pitch-utils leaf decision | [detail](open_items/OI-93.md) |
| OI-94 | L3 pass-1 contract deferrals | A mid-piece notated key-signature change is not re-anchored; the A-3 dominant-implication channel is named but not built (= OI-68). | L3 | OPEN — (a) a deferred L3 refinement; (b) tracked at OI-68 | [detail](open_items/OI-94.md) |
| OI-96 | L3 pass-2 dead local field | extraToneScore declared/assigned but read nowhere (DT-5); contradicts pass-1's "zero DEAD". | L3 | OPEN — remove the field + its write at the next keymodeanalyzer.cpp touch | [detail](open_items/OI-96.md) |
| OI-97 | L3 pass-2 value-copied constant | relativeKeyHysteresisMargin=2.0 duplicates hysteresisMargin by comment, not reference (DT-3). | L3 | OPEN — the Stage-5 fitter treats the two as independent (or single-sources) when OI-91 lands them in the manifest | [detail](open_items/OI-97.md) |
| OI-98 | L3 pass-2 raw-DOM read outside L1 | partialSignatureCorrection walks the engraving DOM directly, bypassing the L1 NoteModel (DT-16). | L3 | OPEN — re-home behind an L1 primitive at the R5/E4 resolver-shrink, or accept as an irreducible signature read with a note | [detail](open_items/OI-98.md) |
| OI-99 | L3 pass-2 dangling doc-reference | Two production comments cite cowork_phase5c_step4_report.md, which does not exist (DT-12). | L3 doc | OPEN — re-point to the extant provenance doc or drop the citation at the next regionanalyzer.cpp touch | [detail](open_items/OI-99.md) |
| OI-101 | L4 pass-1 file-table mis-tags | chordanalyzer.cpp is the surviving scorer oracle not whole-file RETIRES; chordvoicing/vocabulary/voiceleading re-tagged (DT-21, sibling of OI-90). | L4 / audit | OPEN — correct the L1/L2 file-table reasons at their next touch (comment/data-only) | [detail](open_items/OI-101.md) |
| OI-102 | L4 pass-1 feasibility stop + partition | The L4 deep audit is partitioned into decoder/oracle/satellites sessions + a pass-2 sweep; two boundary questions carried (sparsechordrefinement L4/L5, chordpathdecoder retire). | L4 / audit | OPEN — L4 pass-2 done 2026-07-12 (error 0/40 substantive); L4 spine certification PROPOSED, awaiting the user; all pass-1 dispositions + the pass-2 sweep done | [detail](open_items/OI-102.md) |
| OI-103 | L4 decoder constant manifest-gap | 12 ChordSliceDecoderPreferences settings are absent from param_manifest (DT-2, the L4 twin of OI-87/OI-91). | L4 / Stage-5 | OPEN — Stage-5/EG-5 manifest extension | [detail](open_items/OI-103.md) |
| OI-104 | L4 decoder never-fires + reserved paths | SymmetricRotation fires 0/29080 + the reserved NoteMembership open-question is never populated (DT-7/DT-5). | L4 | OPEN — verify at the G5 / NoteMembership build | [detail](open_items/OI-104.md) |
| OI-105 | L4 decoder doc/naming precision | isSemitoneStep returns true for a whole tone too + a param_manifest consuming-path note wrongly says the decoder is not called by batch_analyze (DT-12). | L4 doc | OPEN — fix at the next chordslicedecoder.cpp / param_manifest.json touch | [detail](open_items/OI-105.md) |
| OI-106 | L4 oracle constant/manifest-publication gaps | Three registered scoring constants absent from the manifest + a cluster of undocumented inline extension/presence thresholds (DT-2/DT-3, the oracle twin of OI-103). | L4 / Stage-5 | OPEN — Stage-5/EG-5 manifest extension + register/document the inline bars | [detail](open_items/OI-106.md) |
| OI-107 | L4 oracle ARCHITECTURE.md doc drifts | bassNoteRootBonus 0.65 vs 0.70, Extension bit order, AnalysisUtils location, and pre-refactor Gate descriptions all stale (DT-12); scoring_model.md is in sync. | ARCHITECTURE doc | OPEN — PARTIALLY DISCHARGED 2026-08-02 (CC, the phase-1 truth-sync `ab336f43b5`): (a) bassNoteRootBonus corrected 0.65→0.70 at both sites, (b) the Extension bit order corrected (bits 4/5, 9/10, 14/15) and ChordIdentity's four missing fields added, (c) §4.4's path corrected to `analysis/chord/analysisutils.h` with the three missing functions listed, and the first half of (d) — the gates re-homed to `postscoringgates.cpp` and **Gate K recorded RETIRED** (Stage 5, 2026-07-05), which the section still listed as a live calibrated threshold. **Still owed:** the rest of (d) — §4.1h's iteration-era BIR baselines and test counts are stale as current state. **NEWLY FOUND, owed:** `CLAUDE.md`'s own gate-threshold policy still lists the retired Gate K (0.20); correcting a governing document was outside that pass's scope. **★ THAT SECOND HALF IS DISCHARGED, AND THE ROW WAS STALE ABOUT IT — read at `CLAUDE.md` 2026-08-11 (CC, `cc_instruction_return_continuation_11.md` Task 1), not recalled:** the gate-threshold policy does NOT list Gate K as a live calibrated threshold; it names it as RETIRED with the ruling that retired it — the user's of 2026-08-02, [[OI-260]] — which is exactly what this clause asked for, performed by a different act two days after the clause was written. **So what remains owed on this row is the FIRST half only:** `ARCHITECTURE.md` §4.1h's iteration-era BIR baselines and test counts, stated as current state. **Recorded rather than flipped**, because that half is not done; and recorded rather than silently dropped, because a row asserting an owed act that has already been performed sends the next session to do it twice | [detail](open_items/OI-107.md) |
| OI-108 | L4 oracle never-fires + layering notes | The augmented-root correction fires 0/122047 (TPC-full corpus); display/voicing decls live in the analysis contract header (DT-7/DT-19, both benign). | L4 | OPEN — verify (a) at a TPC-absent-path fire pass; move (b) to its own TU at the R9 split | [detail](open_items/OI-108.md) |
| OI-109 | L4 oracle orphaned BUG-10 marker + P5-over-diminished question | The BUG-10 comment marker resolves to no register/doc row; the P5-over-diminished hard-Contradiction scoring question is declared to Cowork (DT-12). | L4 | OPEN — ADJUDICATED (Cowork 2026-07-11): this row is the home; re-point BUG-10 to OI-109 at the file's next touch; the scoring question parked to the precision phase | [detail](open_items/OI-109.md) |
| OI-110 | L4-audit instrumentation lifecycle | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | L4 / audit | ✅ CLOSED 2026-07-12 — reverted at 940632ecd1 (user-delegated, principle-derived); kept in history for a … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-110.md) |
| OI-111 | L4 formatter duplicated music-theory tables | Note-name arrays, diatonic scale-interval tables, the parent-map, and Roman-numeral arrays re-stored in multiple formatter places (DT-3, sibling of OI-92/OI-97). | L4 formatter | OPEN — fold into a shared music-theory-table primitive at the formatter's next touch | [detail](open_items/OI-111.md) |
| OI-112 | L4 formatter/ARCHITECTURE doc drifts | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | ARCHITECTURE doc | ✅ RESOLVED 2026-08-02 (CC, the phase-1 truth-sync `ab336f43b5`) — all three: (a) §4.3's file line corrected … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-112.md) |
| OI-113 | L4 formatter Nashville chromatic-root coverage gap | A non-diatonic root renders "?" and nashvilleBassSuffix mislabels a chromatic bass; user-reachable via the notation bridge (DT-17). | L4 formatter (display) | OPEN — complete the chromatic Nashville degree/bass rendering at the next chordsymbolformatter.cpp touch (display-layer, not inference) | [detail](open_items/OI-113.md) |
| OI-114 | L4 formatter enharmonic-normalization premise provenance | The spelling-normalization branches are justified by hand-transcribed corpus counts, not generated artifacts (DT-11/#17f); display-only but load-bearing for display spelling. | L4 formatter (display) | OPEN — back the thresholds with a regenerable artifact or accept-with-note at the next formatter/spelling touch | [detail](open_items/OI-114.md) |
| OI-115 | L4 pass-2 dead local field | ExtensionFlags::hasNinth assigned once, read nowhere (DT-5, the L4 twin of OI-96). | L4 | OPEN — remove the field + its write at the next chordanalyzer.cpp touch | [detail](open_items/OI-115.md) |
| OI-116 | L5 + instruments pass-1 feasibility stop + partition | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | L5 / audit | ✅ CERTIFIED by the user 2026-07-12 — all partitions + pass 2 done (error 0/40 substantive, sweep clean); the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-116.md) |
| OI-117 | L5 pass-1 file-table mis-tag | harmonicfunctionlayer is the legacy L4 competition (R1/R7), not surviving L5 (DT-21); L5 has no pure live-surviving population. | L5 / audit | OPEN — EG-7; the R1/R7 + R2 retirements carry the #12 interpretation-check notes; no fix (auditor, not amender) | [detail](open_items/OI-117.md) |
| OI-118 | L5 signed-rule divergence (modulation) | decideTonicizationVsModulation counts ANY matching-tonic cadence, not just authentic/half, as confirming a modulation (DT-22, new type). | L5 | OPEN — add a c.type ∈ {authentic,half} gate at the modulation-arbiter engage build | [detail](open_items/OI-118.md) |
| OI-119 | L5 signed-rule divergence (half cadence) | tryHalf sets genuineDominant=false unconditionally, so the §5.2 seventh/inversion down-weight direction is absent (DT-22). | L5 | OPEN — add the half seventh/inversion down-weight term at the cadence engage build | [detail](open_items/OI-119.md) |
| OI-120 | L5 constant manifest gap | The dormant resolver's cadence/modulation/output/recognition seeds are unregistered (DT-2, the L5 twin of OI-87/91/103/106); membership corrected by OI-139. | L5 / Stage-5 | OPEN — Stage-5/EG-5 manifest extension (register the L5 resolver seeds when the objective is defined); the specific membership corrected per OI-139 | [detail](open_items/OI-120.md) |
| OI-121 | L5 doc-sync (§15-12 grammar) | The design doc §5.0/§15-12 future-tenses the §15-12 grammar completion that already landed at 2e9a22557e (DT-12). | L5 doc (Cowork) | OPEN — Cowork re-words §5.0/§15-12 + handoff 1389 to "landed at 2e9a22557e" at the next design-doc touch | [detail](open_items/OI-121.md) |
| OI-122 | L5 documented/declared spec-fidelity items | Five confirmed-at-code dormant items (Δ0 aug-resolution exclusion, aug6 root assumption, minor modal-mixture role gap, decision-context stop-ii fold, combinedBoundary k>0 guard). | L5 | OPEN — (a) Cowork adjudication; (b)/(c)/(d)/(e) verify/complete at the respective engage build | [detail](open_items/OI-122.md) |
| OI-145 | The key-layer readiness gate | Every open issue directly or indirectly impacting the key layer must be fixed before the key layer is built (waves 1–3 + the cadence-asset pair enumerated). | key-layer gate | OPEN — WAVE 1 substantially delivered 2026-07-12/13 (no grading digit moved); remainder = the harness group + tolerance re-derivations; waves 2–3 follow, the gate lifts when all listed rows close | [detail](open_items/OI-145.md) |
| OI-146 | The evidence inventory + layer-specification sync | cowork_evidence_inventory.md is the live catalog of every hint each layer finds; the ARCHITECTURE.md layer specs update from it as evidence facts are adopted. | key-layer / doc | OPEN — live obligations; the ARCHITECTURE.md sync lands with the key-layer completion | [detail](open_items/OI-146.md) |
| OI-147 | Declared inference finding — L3 exotic-mode emission | The engine emits an exotic dominant-family scale label on spans the annotator reads as a plain diatonic chord (~33% of disagreeing local duration); a live in-suite instance found (OI-148). | L3 (declared to Cowork) | OPEN — DECLARED to Cowork; a Layer-3 inference observation, no fix in a read-only task | [detail](open_items/OI-147.md) |
| OI-55 | Ninth-detection gap — melody/harmony conflation | The live ninth verdict is a bare pitch-class weight cutoff that knows no voice/staff; the dormant classifyMembership discriminator (which names this exact bug) exists on the decoder path. | L4 decoder (E4) | OPEN — ASSIGNED: the L4 decoder engagement (E4) + the NCT-filter lever (OI-68); do NOT fix earlier (#8) — the fix is the membership verdict, not a threshold re-tune | [detail](open_items/OI-55.md) |
| OI-148 | The four key-emission xfail notation tests | Four GTEST_SKIP xfails charged to the a6b08af3fe key-emission regression — the key layer's ready-made DCML-checked acceptance tests, with a dangling "Phase 4c" schedule. | key-layer | OPEN — ASSIGNED: the key-layer build (they must go GREEN, never be re-blessed); re-point the "Phase 4c" anchor at this row | [detail](open_items/OI-148.md) |
| OI-149 | German-flat-bass slash dropped | csfIsValidBassNoteName rejects German flat bass names ("Ces"/"Fes") so the slash is dropped and the bass is lost (OI-109 pattern; a DISABLED correct-oracle test present); display-only. | L4 formatter (display) | OPEN — teach the bass-name validator the German note names, then ENABLE the disabled correct-oracle test, at the next chordsymbolformatter.cpp touch | [detail](open_items/OI-149.md) |
| OI-161 | Two ratified positions contradict on the cadence→key channel | target_architecture §2 forbids cadence detection in key/mode; the design-opening decision 3(b) proposes exactly it — both ratified, they cannot both stand. | key-layer / design | OPEN — the design pass must resolve it explicitly (a user ratification, not a CC fix) | [detail](open_items/OI-161.md) |
| OI-162 | Phrase-boundary punctuation cues are built, not unread | The Layer-1.5 phrase-boundary view already reads fermatas/barlines/silences but is dormant (gated OFF); the gap is wiring + publication, not detection. | L1.5 / key-layer | OPEN — correct the inventory at its next touch (doc-only); the wiring itself is key-layer work | [detail](open_items/OI-162.md) |
| OI-163 | No spelling channel into the L3 emission | PitchContext carries no tpc, so the design-opening decision 2(a) (spelling-aware profiles) has no input path — a missing forward field (L1→L3), not a cycle. | L3 / key-layer | OPEN — a build item at the key layer (#8 timing: with decision 2(a)) | [detail](open_items/OI-163.md) |
| OI-164 | L5→L6 seam gaps | There is no code edge functionoutput→groupinglayer, and GroupingUnit::keyConfidence has no producer; both dormant, neither a cycle. | L5 / L6 | OPEN — at the L5/L6 engagement | [detail](open_items/OI-164.md) |
| OI-165 | findTemporalContext — a live L1.5→L4 fact cycle | A Layer-1.5 primitive instantiates a chord analyzer and runs full analyzeChord (all progression signals active) on neighbor segments; on no retirement row (OI-12 tracks it only as an ownership move). | L1.5 / L4 (E4) | OPEN — needs a retirement decision at E4 (not currently scheduled to die); the doc-precision half is a comment-only fix | [detail](open_items/OI-165.md) |
| OI-166 | The key-agnostic cadence pre-scan is not built | functioncadence is key-agnostic but chord-derived (reads the L4 root/quality), not the bass-driven tonic-enumerating pre-scan the dossier specified; a forward L1.5 break is realizable. | L5 / L1.5 (key-layer) | OPEN — a STOP before any cadence→key build; the layer placement is the design pass's to ratify | [detail](open_items/OI-166.md) |
| OI-167 | The collection/tonic split holds — conditionally | Two named tonic-dependent L4 sites (Gate G-E, the Aeolian guard); site (b) is measured DEAD (0/0/0, body unexercised). | L4 | OPEN — the row's conclusion is now REFUTED (the split does not hold); cannot close until OI-170/OI-171/OI-172 resolve | [detail](open_items/OI-167.md) |
| OI-168 | The collection/tonic split breaks inside the engaged decoder | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | L4 | ✅ FIXED + RE-BASELINED 2026-07-14 — the signature-mask form (Jazz one chord flip, class-(b) −480, … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-168.md) |
| OI-169 | structuralPenalties silently ignores its extThreshold parameter | A preset knob wired to a scoring term but never read (a dead parameter); a second instance in formatNashvilleNumber's keySignatureFifths. | L4 | OPEN — CC-discovered 2026-07-14; needs disposition (a) delete the dead parameter or (b) make the branches read it (a scoring change) | [detail](open_items/OI-169.md) |
| OI-170 | The OI-168 defect survives at three more sites — L4 is NOT tonic-independent | diatonicToKey + Gate I + Gate L carry the identical tonic-through-collection defect on the live region path; a whole-tree sweep also found live class-(b) genuine-tonic sites (DT-1, Class-A #18). | L4 | OPEN — the STOP stands; the three class-(a) sites are MEASURED metric-neutral (0 chord flips), ready as a user-ratified correctness commit; the degree-decides-identity design question is exposed | [detail](open_items/OI-170.md) |
| OI-171 | The OI-167 Aeolian-guard re-home is not clean | refineSparseChordQualityFromKeyContext has three call sites on the commit path, so "zero behavior change, runs post-commit" is false; a move would be a structural behavior change. | L4 (design) | OPEN — CC-discovered 2026-07-14; OI-167 cannot close until this disposition is made | [detail](open_items/OI-171.md) |
| OI-172 | applyTonicPriorToSparseChord decides committed quality from a scale degree | A live tonic-dependent identity write on the commit path (172/183/172 overwrites) that no row named; a class-(b) genuine-tonic site the signature-mask cannot replace. | L4 (design) | OPEN — CC-discovered 2026-07-14; needs a design-pass disposition alongside OI-170's Gate G-E question and OI-171 | [detail](open_items/OI-172.md) |
| OI-173 | Four inequivalent definitions of diatonicToKey | Four diatonicToKey predicates, two degree definitions, three copies of the parent table; "single-source the published fact" is not implementable as stated because most consumers re-derive for a different key (#6/#12). | L4 / L5 | OPEN — CC-discovered 2026-07-14; the collection question unifies on the signature mask (OI-170's fix), the degree question separately — they must not be folded together | [detail](open_items/OI-173.md) |
| OI-174 | The key layer emits Altered on material with no altered tone | 22 of 23 Jazz Altered regions are entirely diatonic to their own notated signature — a key-layer inference failure, not a chord-layer one (declared, not fixed). | L3 key-layer | OPEN — CC-measured 2026-07-14; a key-layer item for the key-layer design pass | [detail](open_items/OI-174.md) |
| OI-175 | The segmenter decides a committed root and quality from the tonic | The Iter-74 head-gap tonic prior in greedyExpandSegmentation overwrites root/bass/quality from the tonic — the first live genuine-tonic chord-deciding site outside Layer 4 (DT-26 founding instance). | L2 segmenter | MIXED — SUPERSEDED-IN-FACT on the batch/corpus surface 2026-07-26 (OI-178 — the joint decode never runs greedyExpandSegmentation); remains LIVE on the legacy notation path, retires with the OI-180 notation increment | [detail](open_items/OI-175.md) |
| OI-180 | Sanctioned dual path for building the joint estimator | A built beside the certified stack temporarily violates #6; declare the parallel decode + side-by-side grading + the retirement map so the transition is pre-ratified. | joint-estimator / structure | OPEN — PROTOCOL RATIFIED 2026-07-19; the FORWARD EXIT is now EXECUTED ON BOTH SURFACES — the notation switch (2026-07-27, default `useJointNotationRecord` ON) adopted the record path in-app, so the six-item retirement map is now FULLY LIVE (the legacy notation path compiled+dormant behind an explicit flag-false, awaiting deletion); sanction stays in force until the map completes | [detail](open_items/OI-180.md) |
| OI-181 | Desk-sim form for the joint decode | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | joint-estimator | ✅ RESOLVED 2026-07-19 — the forms were declared in the factorization doc §6 and the stage ran the same day … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-181.md) |
| OI-182 | Notation-bridge decision constants on no audited surface | kTentativeKeyExposureThreshold/kAssertiveKeyExposureThreshold are hand-set key-exposure buckets in the imploder, in no manifest/audit scope/row (DT-26); consumption-side, not an inference STOP. | notation bridge | OPEN — EXECUTED AT THE RECORD SURFACE 2026-07-27 (seams-2 P4, §4.1) — the exposure/annotation gate VALUES are declared PRESENTATION constants at their emitters: record-arm kAssertiveKeyExposureGap=1.055757 (P2b) + kTentativeKeyExposureGap=0.975911 (P4 Task 1), both in sectionrecordadapter.cpp from exposure_constants.json; the 0.35 mode-suffix gate RETIRED on the record path (C1, g₀.₃₅=0.649691 unwired by design, kept in the artifact); kSameChordReannotationGap (960) re-homed unchanged; the legacy 0.5/0.8 literals now legacy-arm-only (sectionanalyzer.cpp::legacyKeyExposureBucket). Retires fully with the legacy path at the OI-180 map. | [detail](open_items/OI-182.md) |
| OI-183 | scoring_model.md symbol-level coverage gap | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | scoring_model doc | ✅ RESOLVED — 2026-08-14 (CC, `cc_instruction_scoring_model_pass.md` Task 2, commit `b366d44947`). The twelve … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-183.md) |

## C. Owned by Stage-5 (precision phase — do NOT fix earlier, #8)

| ID | name | description | layer / gate | status | detail |
|---|---|---|---|---|---|
| OI-176 | Held-out evaluation for the joint-estimator fit event | A's values are fit once against DCML GT and graded on the same GT — declare the fit/held-out split (or k-fold over the 326 pieces) BEFORE fitting; the headline claim is the held-out figure (#19/#16/#20). | Stage-5 / joint-estimator | OPEN — PROTOCOL RATIFIED 2026-07-19 (5-fold CV grouped by WiR file, all fitted objects train-fold-only, headline = pooled CV + piece-bootstrap CI) — pending the fit event | [detail](open_items/OI-176.md) |
| OI-177 | Capacity budget for the joint-estimator fit | Parameter count + regularization declared and justified against corpus size (352 stems, one composer) before fitting; fitted values stay Bach-chorale values, generalization de-scoped. | Stage-5 / joint-estimator | OPEN — PROTOCOL RATIFIED 2026-07-19 (pre-fit parameter-inventory, cell own-MLE iff count ≥ 20, free params ≤ tokens/10, ≤ 12 weights) — pending the fit event | [detail](open_items/OI-177.md) |
| OI-23 | Tier-2 Class-B mass — ~30 hand-set constants | ~30 live constants hand-set pre-2026-06-13 against the broken batch gate (only kWStepIn ever robust-unit-fit); measured live chord-surface manifest rows = 57, of which 1 fit-adopted. | Stage-5 | OPEN | [detail](open_items/OI-23.md) |
| OI-24 | O-17 recover the 53 lost F-B corrections | Recover the 53 lost F-B corrections via a correctness-correlated signal. | Stage-5 | OPEN | [detail](open_items/OI-24.md) |
| OI-25 | T3-3 dormant placeholder constants | L5 firewall 1.0s, θ, inline 1.0/0.5/0.25, §15-13 null — load-bearing at engage, fit at Stage 5. | Stage-5 | OPEN | [detail](open_items/OI-25.md) |
| OI-26 | O-13/§15-13 family-4 weight | Size-viable but not runnable until L5 engages (an objective gap). | Stage-5 | OPEN — blocked | [detail](open_items/OI-26.md) |
| OI-27 | Uncalibratable confidences | L5 mid-range inversion (non-monotone), tonicVote (anti-monotone), L1.5 spread — maps deferred D-8, fixes belong to the owning layers. | Stage-5 | OPEN | [detail](open_items/OI-27.md) |
| OI-28 | uncertaintyMargin 0.5 never-fit seed | Governs decoder abstention (18% duration) — fit at Stage 5; behavior characterized at PC-2. | Stage-5 | OPEN | [detail](open_items/OI-28.md) |
| OI-29 | §6-block dissolution (OWED #2) | Includes the S14/S16 quality-from-key gates. | Stage-5 | OPEN | [detail](open_items/OI-29.md) |
| OI-30 | Stage 3.3 oracle temporal-signal migration | + a Gate R basisDep proxy revisit. | Stage-5 | OPEN | [detail](open_items/OI-30.md) |
| OI-31 | Stage 6.3 convention-gap buckets | Maj→Dom7 implied sevenths etc. | Stage-5 | OPEN | [detail](open_items/OI-31.md) |
| OI-32 | O-2 class-(a) weighting revisit trigger | Conditional. | Stage-5 | OPEN — trigger | [detail](open_items/OI-32.md) |
| OI-87 | L1/L2 pass-1 constant manifest-gap list | 16 hand-set inference-affecting L1/L2 constants NOT in param_manifest (beat-weight table, sliding-window params, repetition/cross-voice boosts, phrase constants) (DT-2). | Stage-5 / EG-5 | OPEN — Stage-5/EG-5 manifest extension | [detail](open_items/OI-87.md) |
| OI-91 | L3 pass-1 constant manifest-gap | The entire L3 key/mode emission-scoring surface (~60 weights, 21×5 mode priors, sequence/cadence/modulation constants) is absent from param_manifest — the L3 twin of OI-87 (DT-2). | Stage-5 / EG-5 | OPEN — Stage-5/EG-5 manifest extension | [detail](open_items/OI-91.md) |

## D. Instrument / measurement layer

| ID | name | description | layer / gate | status | detail |
|---|---|---|---|---|---|
| OI-178 | Architecture-adoption protocol for the robust stop | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | measurement / adoption gate | ✅ RESOLVED — PROTOCOL RATIFIED 2026-07-19; ✅ EXECUTED 2026-07-26 — the joint estimator is the PRODUCTION … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-178.md) |
| OI-186 | compare_rn.extract_quality coarse on WiR slash notation | The half-dim sigil /o and slashed seventh figures read as the wrong quality; the count inventory is not corrupted, but fit-time normalization must not read quality through extract_quality. | instrument / fit-event | OPEN — CC-found 2026-07-19; (a) binds the fit-event normalization design, (b) a read-only instrument assessment, separate | [detail](open_items/OI-186.md) |
| OI-192 | The adoption's genuine-new class-(b) subset | ~1,470 runs / ~670k ticks per preset where A is root-wrong and the old system was root-right — 70% local-key-correct, dominated by fifth-apart functional root substitutions; the cost side of the −33% net trade. | measurement / refinement | OPEN — post-adoption refinement agenda; fix at its #8-correct stage under the standing measured loop, never as a knob-turn | [detail](open_items/OI-192.md) |
| OI-193 | The notation increment's posterior completion | The uncertainty contract is the FULL posterior (forward-backward marginals, status-marked as model probabilities), with the local slice the first delivered step; the marginal oracle must be positively established before publication. | notation increment | OPEN — a named step of the notation-layer adoption increment | [detail](open_items/OI-193.md) |
| OI-194 | The ornament-label publication | Post-decode non-chord-tone labels from the emission's categories, published with establishment status; its own increment after the switch, including the voice-independent pedal-point class superseding the legacy bass-only fact. | notation increment | OPEN — its own increment after the notation switch; the handoff dual-path/visibility line carries it until delivered | [detail](open_items/OI-194.md) |
| OI-195 | The embedded-table generator embeds CRLF, not LF blobs | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | codegen / establishment | ✅ RESOLVED 2026-07-26 (4565830c82) — the generator canonicalizes each input to LF before embedding AND … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-195.md) |
| OI-196 | Module-local (tonic,mode)→fifths mapping duplicate | jointprimitives::keySignatureFifths is implemented module-local to keep the joint module's L1-only isolation, duplicating the legacy keySignatureFifthsForKey; a declared interim #6 duplicate. | joint module | OPEN — a bounded interim duplicate; retires at OI-180 map item 2 (the legacy key-analyzer retirement) | [detail](open_items/OI-196.md) |
| OI-197 | C++ spelling derivation establishment is partial | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | joint module / establishment | ✅ RESOLVED 2026-07-27 — --joint-spelling-parity + gen_spelling_establishment --cpp-parity: ZERO divergences … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-197.md) |
| OI-198 | The register-driven implementation review | Sweep the full register + DEFECT_TYPES.md catalog against the post-retirement-map implementation (total unification proven, test-data reliability, complete green regression coverage); new issue types get rows. | review (user-directed) | OPEN — scheduled post-map, pre-refinement | [detail](open_items/OI-198.md) |
| OI-199 | The comprehensive code review | Apply the certified two-pass blind audit pattern to everything the existing certifications predate — the joint module, the record path + seams, the codegen machinery, the new instruments. | review (user-directed) | OPEN — **PASS 1 (blind enumerative) DELIVERED 2026-07-28** (`cc_oi199_pass1_report.md`, `tools/audit/oi199/`): the 4-area inventory (6,375 deep rows / 67 files) + the feasibility STOP + the partition (1: joint DONE, 2: instruments, 3: record-seams); **area (a) the joint module fully dispositioned (1,069 rows: SURVIVES 252 / NO-ISSUE 367 / ESTABLISHED 336 / UNFIT 2 / PUBLISHED 64 / FORWARD-OK 47 / BACK-EDGE-NOTE 1)**, P3 two-sided + P4 78/78 tests; findings OI-216/217/220/221 + the process rows OI-218/219/222/223. **The blinding was COMPROMISED at the source (OI-222).** Certification NOT granted; pass 2 (fresh session, seeded error rate, whole-scope DT sweep) + partitions 2–3 owed | [detail](open_items/OI-199.md) |
| OI-200 | The architecture step-back | A whole-system architecture/layer review against maximum-precision inference on the clean post-map stack, in both empirical (vs measured ceilings) and theoretical (vs the public state of the art) halves; prioritizes the refinement agenda. | review (user-directed) | OPEN — scheduled after OI-198/OI-199; consumes OI-179 | [detail](open_items/OI-200.md) |
| OI-201 | Aug-sixth display chord symbol renders as a plain major triad | On the record arm the DISPLAY chord symbol for an augmented-sixth class renders as the coarse-quality root major-triad symbol (e.g. "Ab") — the fitted vocabulary collapsed the aug-sixth family to Italian pitch content / the coarse Major quality, so the class carries no aug-sixth character; the It/Ger/Fr distinction lives only in the Roman numeral (It+6/Ger+6/Fr+6). Faithful to the published facts, not an inference bug. | notation / presentation | OPEN — CC-found 2026-07-27 at the P-strings display-symbol build; a presentation-completeness gap declared to Cowork; catalogue at the P6 dual-arm comparison, resolve at its #8-correct stage (vocabulary or a presentation-side aug-sixth read), never a knob-turn | [detail](open_items/OI-201.md) |
| OI-207 | ★★ THE DECISION-CONFORMANCE AUDIT (user-directed 2026-07-27, at the 3.1b contradiction): is anything else implemented in DIRECT opposition to a recorded decision? | Read-only, priority: enumerate every recorded ruling/shelving/falsification/dead-end across the archives and design docs (the "SHELVED with evidence" / "FALSIFIED" / "rejected" / dead-end classes: `cowork_handoff_archive.md`, `STATUS_ARCHIVE.md`, `docs/scoring_model.md` §8, `docs/p3_granularity_ab_3_1b.md`, `DEFECT_TYPES.md`, the cowork design docs' excluded-alternatives records) and check each against the CURRENT implementation for direct opposition — the 3.1b/whole-score class (a decision whose evidence lived outside the session-start read and was contradicted by a later build). Every hit gets its own row. Trigger case: the seams dispatches' whole-score interactive producer vs the 3.1b shelving (OI-206's dated correction) | repo-wide / process | OPEN — **THE ADJUDICATION RAN 2026-08-01** (read-only; no fix, no design, no `src/` change). Deliverable: **`DECISIONS.md`** (115 decisions, generated) + `tools/audit/decisions/cluster_dispositions.*` + `gen_cluster_dispositions.py`. Method specification-first: the `ARCHITECTURE.md` per-layer specs read in full, the harvest used as index + backstop. **Established (#19): 115/115 backbone verbatim quotes confirmed at their cited file:line** (`--verify`), including all three known signature-net misses. **Coverage: 14,460/14,460 clusters dispositioned, 15,224/15,224 occurrences** (`--check` PASS) — restates 2,603 / not-a-decision 3,629 / boilerplate 74 / no-spec-home 1,780 / **unresolved 6,374**. Nine findings rowed: [[OI-231]]…[[OI-239]]; dated notes on [[OI-210]]/[[OI-212]]/[[OI-228]] naming the decisions they bear on. **★ CORRECTED AND COMPLETED 2026-08-01** (the ratification review's findings + the scope gap): 17 mis-aimed provenance cross-references swept and re-aimed (cause: forward-guessed before the rows existed, now mechanically guarded), stale anchors fixed, three restatements reworded, D-103 given its ratified successor (D-207), the STATUS entry order restored; a **rationale ("Why.") field** added to every entry (183 of 228 populated, 45 read "derivation not recorded"); and the register COMPLETED **115 → 228** by reading `ARCHITECTURE.md` §1 and §6–§19 + appendices, `CLAUDE.md` in full, the rulings living only in open-item rows and handoff blocks, and the standing decision-bearing surfaces. `--verify` 228/228 quotes, 222/222 anchors, all cross-references resolving; `--check` PASS. **Three new findings: [[OI-240]] / [[OI-241]] / [[OI-242]]**; [[OI-232]] extended five → eight. **STAYS OPEN: the pass is NOT exhaustive** — 44 % of clusters could not be classified mechanically, and that residual mixes real rulings with narrative; a second pass over it is owed. **★ REGISTER CONTENT RATIFIED by the user 2026-08-02** (all 228 entries — the 115 with review corrections applied, the 113 completion entries read via `ratification_surfaces/cowork_decisions_pending_ratification.md`); the residual second pass is this row's remaining scope. **★ CORRECTION OF RECORD 2026-08-03 (CC, phase 1m, Task 6): the coverage figure is 36 design documents read in full and 66 owed, NOT 40 and 62.** Derived mechanically and asserted by the generator (36 + 41 exclusions + 66 = 143 exactly). The phase-1j note stated "38 … (32 at phase 1h, plus phase 1i's one, plus phase 1j's one)", which is 34, and the +4 was carried forward unchecked to 39 and 40 — each wave taking the previous total as given instead of re-deriving it. **Yield measured over the 36:** 157 entries, mean 4.4, median 3.5, max 17, **12 documents yielded ZERO** and 18 of 36 carry 90 % of the total. **The remaining 66 are ≈383 k tokens.** A yield PROXY was measured (rank correlation with length 0.745, cluster count 0.628, named-in-a-ratified-surface 0.530) and is **explicitly NOT established** — fitted and self-measured, with two counter-examples inside the measured set (`docs/beam_widening_design.md`, 558 lines / 37 clusters / **0** entries; `cowork_stage5_fitter_design.md`, the surface's largest at 1,523 lines / 53 clusters / **3**). **The proposal is therefore: read all 66, carry no tail** — a bound resting only on that proxy is not a bound, and the validation that would change the answer (five reads from the predicted-low band against a prediction registered in advance) is written down but not run. **This wave read NO document, by the dispatch's design** | [detail](open_items/OI-207.md) |
| OI-208 | THE DECISIONS REGISTER (structural prevention, proposed at OI-207's rowing): rulings need the same ONE-home treatment issues got | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | docs / process (#6 applied to decisions) | ✅ RESOLVED — **SHAPE RATIFIED by the user 2026-07-28: the register records WHAT WAS DECIDED and its status … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-208.md) |
| OI-206 | ★ IN-APP INTERACTIVE REGRESSION POST-SWITCH (USER-REPORTED): selection highlight never renders; spacebar playback dead; selection STATE works | User-reported 2026-07-27 running the switched build: selecting anything renders NO visual highlight, though the status bar shows the selection and annotate-selection acts on it correctly (selection state + annotation work); spacebar does not start/stop playback. TWO candidate mechanisms, BOTH HYPOTHESES until diagnosed: (a) the OI-203 latency escalated to functional breakage — the note-seam funnel runs a whole-score decode SYNCHRONOUSLY on the selection-changed/status-bar path, starving UI paint and input dispatch; (b) an exception escaping the record-arm funnel mid-handler, aborting the selection-visual chain. Diagnosis FIRST (investigate-by-default): reproduce, profile the selection-changed path, exception-trap the funnel — no fix design before the mechanism is measured. The designed mitigation exists: explicit `useJointNotationRecord=false` (the dormant legacy arm — the revert escape preserved for exactly this) | notation / the record-arm interactive path | OPEN — the READ-ONLY INVESTIGATION DELIVERED 2026-07-28 (dispatch `cc_instruction_oi206_investigation.md`): the call-path confirmation (`tools/notation_seams/callpath_facts.json` — one synchronous whole-score `produceNotationRecord` per single-note selection, the note-seam status bar the sole per-selection payer, field pattern fully reproduced → hypothesis (a) CONFIRMED, (b) dead) + the windowed-vs-whole-piece study (`tools/joint_estimator/window_study.json` — readings stabilize in a bounded ≈4–8-measure window at ~zero accuracy cost on the chorale envelope); THE FIX is Cowork's next step (the decision surface: keyed cache / off-thread / bounded grow-until-stable, rated on these numbers + the 3.1b evidence + the Tristan review). Still blocks OI-180 map (the legacy escape survives until the fix resolves this) | [detail](open_items/OI-206.md) |
| OI-209 | ★ THE LARGE-SCORE REQUIREMENT: very large scores (a Tristan act, a symphony) must be handled | User-directed 2026-07-28, in the user's words: very large scores MUST be handled and are expected to be a MORE COMMON use case than our corpora. A STANDING DESIGN REQUIREMENT (not a defect) — every subsequent inference/notation design is judged against it. Records the collision: the joint estimator's ratified tractability envelope is CHORALE scale (60–150 events, exact decode, documented-pruning reserve OI-188), the fitted corpus is 326 Bach chorales by one composer (OI-39), and OI-38 already mandates late-romantic→atonal expansion. The effort control is ONE setting with several dials that must bound TEMPORALLY too; too early to implement until we know factually which parts must be switchable — which this dispatch's measurement establishes. User #17(b) prediction verbatim: "always read the entire score will VERY likely not survive (maybe only under some effort setting = EXTREME)." | requirement / all layers | OPEN — a standing design requirement; consumed by the analysis-extent decision surface (OI-210), the corpus onboarding (OI-38), and the architecture step-back (OI-200) | [detail](open_items/OI-209.md) |
| OI-210 | ★ THE ANALYSIS-EXTENT QUESTION IS OPEN, and the implementation contradicts the last recorded ruling | What temporal EXTENT the analyzer reads per query, and how OFTEN it re-reads it — two independent axes (extent: whole-piece / bounded-window / grow-until-stable / enclosing-unit / viewport; frequency: per-query / once-per-edit / once-then-incrementally-patched). Recorded history: ratified as whole-piece (Q1) → OVERTURNED and SHELVED WITH EVIDENCE at Stage 3.1b 2026-06-12 (window beat whole-score, `docs/p3_granularity_ab_3_1b.md`) → explicitly PARKED pending the granularity-robust metric, which has existed since 2026-07-06 → now implemented as whole-piece by dispatch specification (OI-212), NO ruling. The candidate designs + the measurement that discriminates them: `cc_instruction_analysis_cost_profile.md` §A. | notation / inference (design) | OPEN — priority; the analysis-extent decision surface is Cowork's next step (§A candidates rated on the cost profile + 3.1b evidence + Tristan review); cross-ref OI-207, OI-208, OI-18 | [detail](open_items/OI-210.md) |
| OI-211 | The parked crash: making a hidden piano part visible in a 60-bar arrangement on the switched build | User crashed MuseScore making a hidden piano part visible in a sixty-bar arrangement on the switched build (useJointNotationRecord ON); a second attempt did NOT reproduce. User-directed: PARKED pending recurrence, no work now. The decisive cheap test for when it recurs: reproduce with `useJointNotationRecord = false` (the dormant legacy arm) — separating a MuseScore part-visibility fault from anything of ours. Sub-question raised: whether hidden staves feed the analysis input at all (the OI-204/OI-212 input-scoping class). | notation / crash (observed-once) | OPEN — observed once, not reproduced; parked pending recurrence | [detail](open_items/OI-211.md) |
| OI-212 | The record producer analyzes the WHOLE SCORE regardless of the requested span | `produceNotationRecord(score, stem, excludeStaves)` takes NO tick range; all four record-arm seams (annotation emit `notationcomposingbridge.cpp:1495`, implode `:1420`, tuning `:777`, note-seam `:732`) analyze the entire score and narrow by view AFTERWARD, while every legacy arm passed the actual span into `analyzeHarmonicRhythm` (+~8-bar RN lookahead). Both a cost multiplier on every seam AND a silent analysis-INPUT-SCOPE change made without a ruling; the contract's span seam specifies the opposite. The layer-correct fix (if the extent ruling calls for it) is a span input at the producer/adapter (input-scoping, NOT inference — the OI-204 pattern for the temporal axis). | composing / the record producer's input surface | OPEN — the cost side quantified by the analysis-cost phase profile; the scope-change side is an OI-207 decision-conformance item; resolves with the OI-210 extent ruling | [detail](open_items/OI-212.md) |
| OI-213 | The addAnalyzedHarmonyToSelection per-note produce multiplier | `addAnalyzedHarmonyToSelection` (`notationinteraction.cpp:8256`) calls the note-seam funnel once PER SELECTED NOTE (`:8311`, a loop over byStaffTick), so an N-note selection costs N whole-score `produceNotationRecord` decodes (OI-212) per single command — compounding the OI-206/OI-203 latency on an explicit command. A keyed (score, change-token) record cache collapses the N to 1; a named input to the OI-206 fix surface. Correctness unaffected (the N decodes are identical). | notation / the record-arm interactive path | OPEN — confirmed at the code (callpath_facts.json q4); resolves with the OI-203 record cache | [detail](open_items/OI-213.md) |
| OI-214 | The embedded tables are stored as JSON text and parsed per call (a decision record with an undeclared cost) | `jointembeddedartifacts.h`: the artifacts are embedded VERBATIM (JSON bytes, not a parsed-structure codegen) and parsed through the SAME parser as the filesystem path; `jointnotationproducer.cpp:37-40`: loaded FRESH per call (a shared/cached loader is a later, measured concern). Ratified Decision D1 said "compiled-in constant data" without settling the form; the verbatim-bytes-plus-shared-parser form was chosen on carried-forward-establishment (#19) + one-parse-path (#6) grounds, and its runtime cost was never declared or measured. A DECISION RECORD with an undeclared cost, not a performance defect; the phase-2 measurement answers whether it matters. | joint module / codegen | OPEN — the per-call parse cost is measured as phase 2 (`large_score_decode_profile.json`); if immaterial the decision stands with its cost now declared, if material at scale it feeds the OI-203 cache design | [detail](open_items/OI-214.md) |
| OI-215 | ★ THE JOINT DECODER RETURNS AN EMPTY ANALYSIS (0 segments) on most orchestral scores — a robustness cliff (a STOP, #13) | CC-found 2026-07-28 at the analysis-cost decode profile: butterworth + holst_mercury decode to `complete=false`, 0 committed segments — the decode runs 10-14s then returns NOTHING. Mechanism (code-PROVEN, a theorem): the semi-Markov DP needs every event covered by a ≤segCap(=4) segment, and a candidate is skipped unless ≥2 of its members are in the segment's ONSET-pc union (jointdecoder.cpp:444-445/773); an event whose every ≤segCap covering window has <2 distinct onset pcs is UNCOVERABLE → V[N] empty → 0 segments (jointdecoder.cpp:838). Chorales (the fit corpus, OI-39) never hit it (always ≥2 onset pcs/window); orchestral solo-over-sustain / monophonic passages do. MEASURED (`large_score_profile_counts.json` uncoverableEvents): **13 of 23 large scores** have uncoverable>0 → guaranteed empty decode — EVERY symphony in the set (Beethoven 7/9, Holst Planets, Haydn 6/8, Dvořák, Tchaikovsky, Gluck, Schubert quartet). Not a cost issue — a correctness/robustness limitation that bears on OI-209/OI-210. | joint decoder / robustness (declared to Cowork) | OPEN — a STOP surfaced not built around; NOT fixed here (a read-only measurement; any fix is an inference/layer design decision at its #8-correct stage, never a knob-turn) | [detail](open_items/OI-215.md) |
| OI-216 | The C++ joint decode is std::string-keyed in its DP hot loop, and appears slower than the pure-Python reference | Sealed §S finding S2. Chorale (80 events) C++ decode 5,650 ms vs Python (memos off) 3,415 ms at 77.7 events; build RelWithDebInfo /O2. The DP hot loop (`jointdecoder.cpp:732-836`) builds a `std::string` per candidate per window per boundary (stateEnc/keyEnc/to_string cache keys, StateEntry.backEnc, unordered_map<string> everywhere) — heap alloc + string hash in the innermost loop. Cost-structural, not a correctness matter (decode is probe_decoder-parity). Direction (C++ slower) is Cowork's timing; the string-state cause was CC-confirmed at the code (OI-199 pass 1). | joint decoder / cost (declared to Cowork) | OPEN — CC-confirmed at OI-199 pass 1; a performance-design decision at its #8-correct stage, OI-199's own subject matter, never a knob-turn | [detail](open_items/OI-216.md) |
| OI-217 | buildAdapterFacts is near-quadratic in events (repeated full note scans) | Sealed §S finding S3. `buildAdapterFacts` fits `events^1.80` (CI 1.65–2.09, R²=0.965), 16.8 s on the largest score before the decode. Mechanism CC-confirmed: the event-lattice build (`jointfactadapter.cpp:465-491`) scans all notes per boundary → O(events·notes); `melodic` (`:279`) scans the part list per note → O(part²). A sweep-line/interval index is linear. Cost/algorithm, not correctness (byte-parity facts). | joint fact adapter / cost (declared to Cowork) | OPEN — CC-confirmed at OI-199 pass 1; a performance-design decision at its #8-correct stage, OI-199's own subject matter, never a knob-turn | [detail](open_items/OI-217.md) |
| OI-218 | Correction of record: incremental patching of the joint decode is UNMEASURED, not refuted | Sealed §S finding S4. CC's analysis-cost conclusion "the coupled DP dominates … incremental patching saves ≤40%" does NOT follow — the 40.5/59.5 split is a cold whole-piece decode's cost share, not proof an edit forces full recomputation; whether the recursion admits a bounded local re-solve is unmeasured. Second caveat: the split is Python-derived, a cost fraction does not transfer to the C++ path (OI-216). | measurement / a correction of record | OPEN — record only; the once-then-patched frequency axis of OI-210 remains genuinely open; re-measure a bounded neighbourhood re-solve on the C++ path | [detail](open_items/OI-218.md) |
| OI-219 | Process: #17(b) pre-registered prediction bands were not registered before measuring (analysis-cost dispatch) | Sealed §S finding S5. The analysis-cost dispatch required pre-registered prediction bands (#17b) in two places; none of CC's own were registered before the measuring run. CC surfaced it honestly; the measurements stand, but the guard against motivated interpretation was lost — OI-218 is an instance of exactly the error the guard exists to catch. Standing reminder: register the bands in the artifact BEFORE the measuring run. | process / methodology | OPEN — record only; a standing reminder for every future dispatch (CC and Cowork alike) | [detail](open_items/OI-219.md) |
| OI-220 | The joint module's headers uniformly assert a DORMANCY the code has outgrown (a #10 doc-sync lag post-switch) | OI-199 pass-1 finding (area a). Six+ joint headers (`jointdecoder.h:43`, `labelclass.h:39`, `jointadapter.h:41`, `jointtables.h:44`, `jointweights.h`, `jointnotationrecord.h:48`) say "DORMANT — no production consumer" while the module IS the production notation analysis (flag default Val(true) at `composingconfiguration.cpp:178`; the three bridges call produceNotationRecord). A reader would wrongly believe the inference module is inert. The record-arm branch-site "default OFF" comments are the same lag on the area-(b) side. | joint module / #10 doc-sync (DT-12) | OPEN — CC-found OI-199 pass 1; a comment sweep at next touch or its own doc commit; NOT fixed here (read-only) | [detail](open_items/OI-220.md) |
| OI-221 | Two published joint-record facts have no consumer (§3.4 modal reading + §3.2 augSixthSubType; declared dormancy) | OI-199 pass-1 finding (area a; fact-publication corollary). `NotationRecord::modalReading` (+ ModalKeyRun/Degree/Inflection, 10 fields) and `RecordSegment::augSixthSubType` are on the published surface but consumed by nobody outside joint/ (grep-verified); every other published derived fact IS consumed. Declared dormancy (named future consumer = the presentation layer, contract §3.2/§3.4), not waste — flagged so the future consumer is tracked (augSixth relates to OI-201). | joint module / DT-5 (declared-dormancy) | OPEN — CC-found OI-199 pass 1; tracking so the named future consumer lands; nothing to fix (declared dormancy) | [detail](open_items/OI-221.md) |
| OI-222 | The OI-199 pass-1 blinding was DEFEATED at the source (STATUS.md + the inline §S both leaked the sealed findings) — DT-20 | OI-199 pass-1 PROCESS finding. The pass seals S1/S2/S3 until post-freeze, but (1) the mandatory `STATUS.md` session-start read carries the full S1/S2/S3 text (the riding Cowork LATEST entry — the OI-89 founding shape), and (2) the dispatch delivered §S inline. So the reconciliation cannot claim knowledge-free discovery; it reports instead whether the audit's ARTIFACTS point at each mechanism on their merits (they do). Remedy (OI-89 generalized): cross-check every required read + the dispatch body against every withholding requirement; keep §S in a separate post-freeze artifact; do not headline the withheld findings in a mandatory blind-pass read. | process / DT-20 | OPEN — record only; the OI-199 pass-1 work stands, only the "was it blind" claim is compromised; feeds OI-208 (process hygiene) | [detail](open_items/OI-222.md) |
| OI-223 | The committed prior-layer audit inventories (l1l2/l3/l4/l5) are commit-stale vs HEAD (OI-95 class, one level up) | OI-199 pass-1 finding. The committed `tools/audit/{l1l2,l3,l4,l5}/` deep-CSVs were frozen at their audit commits; HEAD `5135764ed7` is far past them (the joint arc + the switch landed between), so regenerating differs by SOURCE DRIFT (proven NOT the OI-199 tooling change — the edited tool reproduces a minimal joint-rule-only tool byte-for-byte on all four layers; the extraction engine was untouched). The frozen inventories remain valid as frozen references; a session needing a current L1–L5 inventory must regenerate, not read the CSVs. | housekeeping / DT-12 (OI-95 class) | OPEN — CC-found OI-199 pass 1; NOT re-baselined here (out of OI-199 scope; each layer owns its inventory) | [detail](open_items/OI-223.md) |
| OI-224 | OI-199 pass 1 under-delivered two arms against the L4 standard (P4 corpus routes; P3's second side) | OI-199-pass-2 method finding. Pass 1 dispositioned the joint module's 1,069 rows correctly but two arms fell short of the certified L4 precedent (`cc_l4_audit_pass1_decoder_report.md`): P4 ran a passing test suite + a code-read fire structure instead of real corpus routes with per-branch fire counts; and P3's second side reported only stale dormancy headers ([[OI-220]]) and MISSED that candidate admission has no ratified basis ([[OI-226]]). The remedy is this dispatch (`cc_instruction_oi199_joint_p3_p4.md`), which completed both arms to the L4 standard. The pass-1 dispositions STAND ([[OI-225]]). | review (user-directed) | OPEN — CC-found 2026-07-28; the two arms COMPLETED by this dispatch (`cc_oi199_joint_p3_p4_report.md`); certification still owed (pass 2 blind reading + error rate + partitions) | [detail](open_items/OI-224.md) |
| OI-225 | Correction of record: the OI-199 pass-1 dispositions were NOT "generated, not made"/void; the verdict-entropy guard is withdrawn | OI-199-pass-2 correction. Cowork initially faulted the 1,069 dispositions as generated-therefore-void and proposed a verdict-entropy guard. Measured against the certified L4 pass (nine (kind,verdict,reason) combinations over 311 rows, all 127 branches on ONE verdict) vs CC's ten over 1,069, CC's pass has MORE verdict diversity than the precedent the user certified; generated dispositions with hand-curated exceptions ARE the ratified method. The claim was WRONG and is withdrawn; the guard (calibrated against nothing) is withdrawn. CC's inventory/partition/feasibility-stop/fields/rows STAND. | review (user-directed) | OPEN — record only (user's ruling 2026-07-28); no action — the committed pass-1 dispositions are not discredited | [detail](open_items/OI-225.md) |
| OI-226 | Candidate admission (root-present / member-overlap / NCT-budget gates + the top-K key prune) entered production with NO ratified basis | OI-199-pass-2 P3-second-side finding. `candidateStates` admits states through three gates (`jointdecoder.cpp:438/444-445/448`) + `candidateKeys` prunes to the top-6 by onset overlap (`kKeyPruneTopK=6`); the ratified decode plan (`cowork_joint_estimator_factorization.md` §5) specifies the FULL "24 keys × the degree vocabulary" state space and reserves only a DIFFERENT (circle-of-fifths fitted-mass) prune. The gates are described nowhere in §5/§4/prefit-gates/contract; they entered via the `probe_decoder` port with inline comments only; the desk-sim (§6a) merely truncated the candidate set as a hand-trace convenience. [[OI-188]] measures their COST (GT reachability ~72 %, 14,257 spans force-added) but never their FORM. Bears directly on the OI-215 fix (the fix must give admission a ratified basis, covering both gates [[OI-215]]/[[OI-227]]) and is [[OI-207]] subject matter. | joint decoder / decision-conformance (declared to Cowork) | OPEN — CC-found 2026-07-28; a Premise-Gate decision-surface item (never a knob-turn); feeds [[OI-207]] as a pre-located instance | [detail](open_items/OI-226.md) |
| OI-227 | ★ THE EMPTY-DECODE CLIFF HAS A SIBLING: the FIT / NCT-budget admission gate empties the decode on DENSE (chromatic-tutti) events — OI-215 is NOT alone (a STOP, #13) | OI-199-pass-2 Task-1 answer to "is OI-215 alone": NO. `scanCoverage` over the 23 committed large scores (`joint_firecount_large.json`, 172,611 events) finds 312 uncoverable events = **291 (93.3 %) OI-215 member-overlap (gate 2, SPARSE <2-onset-pc windows) + 21 (6.7 %) this SIBLING — the FIT gate (gate 3, `:448`, `(nOnset−present)>max(1,j−i)`) on DENSE events** (holst_planets 14, tchaikovsky 5, beethoven_sym9 ×2); 0 root-only, 0 rich-member-overlap. Opposite ends of the onset-density spectrum, DIFFERENT gates. Corollary: OI-215's `<2-onset-pc` proxy scan UNDERCOUNTS (true 312, not 291 — it is blind to the FIT-gate sibling). The OI-215 fix must cover BOTH gates (#3/#6/#7). | joint decoder / robustness (declared to Cowork) | OPEN — CC-found 2026-07-28; a STOP surfaced not built around; NOT fixed here (the decision surface is Cowork's, under the Premise Gate); the fix covers the whole family with [[OI-215]] [[OI-226]] | [detail](open_items/OI-227.md) |
| OI-228 | ★ The pitch/spelling emission reads STRUCK (onset) tones, not SOUNDING tones — a departure from the ratified per-tone emission + the sounding-note-set layer decisions (declared to Cowork) | Cowork-found at OI-199 pass 2 (the user's challenge); CC-confirmed at the code 2026-07-28. The ratified factorization emits `P_emit(tone \| k,c)` PER TONE with the bass factor on each event's SOUNDING bass (+ a tied-over-preparation covariate); `ARCHITECTURE.md:1045-1053` decides slice identity IS the eligible SOUNDING-note set (releases are boundaries; non-boundary notes ride along, passed through, not dropped) and `:3134-3141` ranks actual sounding notes the STRONGEST evidence — but the decode walks `notesByEvent` = "note indices whose ONSET is in event e" (`jointdecoder.h:67`; emission summed over it `jointdecoder.cpp:298-301`; bass from it `:117`/`:369`), so a note already sounding contributes nothing (consulted only for the missing-tone penalty). The slicer manufactures release-boundary windows with zero struck pitch classes (428 in the fire-count histogram) at which the emission sees no pitch evidence. Caveats carried honestly: (a) refit constraint — SHARPENED by CC, the factor-presence table's own provenance declares OVERLAP membership PRIMARY / onset a cross-check (`jointembeddedartifacts.cpp:118`), so the decode already reads an overlap-fitted table through an onset lens; (b) consequence unmeasured — admission fails loudly and was found, the emission miscounts SILENTLY, an accuracy question everywhere. The emission face of the same family as [[OI-215]]/[[OI-226]]/[[OI-227]] (struck where the design says sounding); the fix is ONE design over the whole family (#6/#7), never per-symptom. | joint decoder / decision-conformance | OPEN — priority; CC-declared to Cowork 2026-07-28; NOT fixed here (read-only; the fix is a Premise-Gate decision surface at its #8-correct stage); the [[OI-215]] fix surface is DEFERRED until the family is enumerated; feeds [[OI-207]] | [detail](open_items/OI-228.md) |
| OI-365 | ★ **Whether the live competition design owes the two carved-out gates' CONCERNS is a phase-3 fix-plan input — recorded, not decided** | Created 2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 0) by the register rule (c) check the user's **Ruling 34** of `cowork_rulings_2026_08_09_fifth_stop.md` requires. That ruling settles the conflict the Ruling-27 fact-gathering pass located between **D-580** and the retirement map's first entry, and settles it as **reading (ii): different acts, both records true.** D-580's carve-out is about a DISSOLUTION into the competition that never ran; the map's R1 performs a DELETION of the legacy path; **both stand, R1 is NOT qualified, and the carve-out is recorded as scoped to the unrun dissolution.** The excluded reading is recorded (#12) — that R1 overlooked the carve-out, which would qualify a ratified map on an assumption about intent. **What survives the ruling is a different question, and it is this row:** the carve-out rested on a MEASURED property — the two gates refine the reading from the notes alone and compensate for nothing — and that property does not disappear when the path carrying them is deleted, so whether the live design owes what they were FOR (an enharmonic reading between a major-triad-with-added-sixth and a first-inversion minor seventh; a completeness reading on an inverted dominant seventh, both decided from the sonority alone) is an INPUT to the ONE prioritized fix plan **D-231** assembles. **NOT in doubt and NOT claimed:** no defect and no regression — whether the live estimator already expresses either reading as a factor is UNSETTLED in the record, and absence of a legacy construct's NAME in the joint module is not absence of the behaviour; D-580 itself, whose verbatim, home, status and mark are untouched; and the retirement map, which no session may qualify. Every located fact is at `ratification_surfaces/cowork_d580_transfer_fact_gathering_2026_08_09.md` and none is restated here (**D-431**) | joint estimator / phase-3 fix-plan input | OPEN — rowed 2026-08-09 (CC), and **rowed is all that is done to it**: **D-231** forbids fix design before phases 1 and 2 close and #8's three-clause gate stands, so no fix, design or inference change is authorized or proposed. **GATES** — its subject is what the live inference design must cover, which bears on the analysis, which is the test the non-gating declaration states, answered YES; what it gates is the fix plan's completeness, not any measurement. Cross-ref [[OI-226]] (the family whose scope **D-429**'s carrying ruling names, and which the gate layer is not a member of), [[OI-354]] (the establishment whose eleventh verdict this cell is), [[OI-2]] (the precedent for carrying a phase-3 fix-plan input on a row) | [detail](open_items/OI-365.md) |
| OI-277 | ★ The emission is NOTE-COUNT-weighted: an octave doubling moves the committed reading on 13.2 % of segments and re-cuts the segmentation on 9 of 12 pieces — the third face of the what-does-the-emission-read family | Cowork-found 2026-08-02 by a read-only invariance probe (candidate finding A-1, user-reviewed before rowing); rowed by CC 2026-08-02. The emission and spelling factors are summed PER NOTE RECORD (`probe_decoder.py:747-753`) and the fitted table was COUNTED per note the same way (`gen_note_tables.py:385-410`), so fit and decode AGREE — this is a MODEL PROPERTY BY TRAINED DESIGN, **not** the [[OI-228]] fit/decode-mismatch class and not a decoder defect. Measured: doubling one upper voice an octave up (pitch classes, bass and event lattice all unchanged) moved 13.2 % of committed segments (354/408 survived), re-cut boundary structure on 9 of 12 pieces (the registered surprise — the prediction said ≥9 would be STABLE), and lowered every total score; `bass`/`missing_tone`/`boundary` deltas are exactly 0.0, only `emission` and `spelling` move. Diagnosed classes: quality inflation (`bwv2.6@5760`, `V\|Maj`→`V\|Dom7`, margin +0.483→+0.959), a KEY relabel decided by the SPELLING factor alone with identical emission deltas (`bwv153.1@18000`, E major→A minor), and wholesale re-segmentation. **Why it is rowed:** it is the third face of the input-representation question ([[OI-228]] struck-vs-sounding, [[OI-243]]/[[OI-244]] transposition, [[OI-246]] concert-vs-written) — notes versus pitch classes — and the standing one-fix-per-family rule forbids designing over part of it. A pc-level or voice-deduplicated emission is a refit-requiring MODEL change, never a knob-turn. Bears on [[OI-209]]: doubling is the norm in orchestral writing and absent from the four-part chorale fit corpus, so 13.2 % is an in-envelope figure. **Establishment (#19): agent-run apparatus**, self-established against the committed parity reference (PASS 12/12 vs `decode_parity_ref.json`) with bands registered before measuring; a **repo-side re-run is OWED before any design puts these figures under load** (the [[OI-243]] condition), and the probe's harness scripts are NOT among the committed artifacts (#16 gap, recorded in the detail file). Artifacts `tools/joint_estimator/invariance_probes_2026_08_02/` | joint decoder / model design (declared to Cowork) | OPEN — a FAMILY-DESIGN INPUT for the phase-3 plan, taken with [[OI-228]]/[[OI-243]]/[[OI-246]]; not a defect, nothing fixed here | [detail](open_items/OI-277.md) |
| OI-278 | A declared adjudication was never recorded as made — and a ratified register entry still offers the unification the code measurement says does not exist | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / a declared adjudication with no recorded outcome | ✅ RULED by the user 2026-08-02 (option (a)) — **FQ-1 LAPSES WITH THE LEGACY PATH**: the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-278.md) |
| OI-229 | Music-theory terminology collision — the reserved-word convention is now STANDING; the existing tree is not renamed unilaterally | User-directed 2026-07-28, generalizing the "instrument" case: any term coinciding even slightly with music theory is reserved for its music-theory meaning (an ambiguous domain vocabulary makes every document harder to read and every specification easier to misapply). Now in `CLAUDE.md` Conventions (a riding edit) and BINDING on new writing; the disambiguation convention = the bare word always carries the musical meaning, every non-musical use explicitly qualified. The existing tree is NOT renamed unilaterally — a scoped, ratified work item (some names carry correspondence to the published research the design is grounded in, #1/#2, so it is a decision surface, not a sweep). Starting collision inventory recorded (instrument, score, key, measure, stem, note, mode, tie, dynamic, register, beat, scale, figure, interval, resolution, sharpen, flat, root, part, rest); the two STRUCTURAL cases (real renaming cost) are SCORE (numerical vs musical) and MEASURE (to gauge vs the bar). | docs / convention | OPEN — the convention is LIVE for new writing (CLAUDE.md); the tree-wide cleanup is now SCOPED by the user's **Rulings 30 and 31** of 2026-08-09 (`cowork_rulings_2026_08_09_fifth_stop.md`), recorded here 2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 0). **NO TREE-WIDE RENAME.** The order is fixed: the scanner's inventory FIRST (Ruling 31), then the user rules PER-WORD BATCHES, governing surfaces first — `CLAUDE.md`, `ARCHITECTURE.md`, the signed specifications — with code identifiers and research-tied names each a named LATER decision. **RESEARCH-TIED NAMES ARE NOT RENAMED, by the user's own two-tier rule:** (i) at the INTRODUCTION SITE, where the public research is actually discussed and which is expected to be one or very few places, the collision is explained and our decided synonym stated; (ii) EVERY SUBSEQUENT USE of the research term outside our vocabulary carries a compact inline annotation referencing the research — so an unannotated repeat use is a flag, while the term at its introduction site with the conflict-and-synonym statement is conformant. **Ruling 31 answers how the inventory's completeness is known: it is NOT known, and it becomes checkable BY DERIVATION** — the candidate population is the project's own musical vocabulary intersected with the words the documentation uses in non-musical contexts, this row's twenty-word inventory being the SEED verdicts, every derived candidate carrying an authored verdict and **an unclassified candidate being a STOP** (the guard-population pattern; #19 — complete means complete relative to a named derivation, re-derived as the tree grows). The full-tree run is ADVISORY and feeds Ruling 30's batches; adoption as a diff-time check on new text only on measured clean separation (**D-473** governs a failure). **The row stays OPEN because the cleanup itself is not done** — what is settled is its scope and its order. **★ THE SCANNER IS BUILT AND RUN 2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 2), AND ITS RESULT IS THAT NEITHER DERIVATION IS BOTH SOUND AND BOUNDED — REPORTED, NOT TUNED.** Two readings of the musical surface were computed and both are published: the SHARP one (every `enum` in the analysis code, crossed with the two music-theory documents — where a program writes down the categories its domain has) **is not SOUND, missing seed words this row's own inventory names**, one of them a collision the standing self-check has caught in this project's prose more than once; the BROAD one (every word inside any identifier, same crossing) reaches all but one seed word and **is not BOUNDED**, proposing a population no session can rule on. **One seed word is missed by BOTH**, so the record is not a subset of the derivation either. **The completeness question is therefore ANSWERED rather than closed:** the inventory is not complete relative to either derivation, the derivation is not complete relative to the record, and *complete* now means complete relative to a NAMED derivation whose measured miss rate against the seed is part of its name. **NO CHECK IS ADOPTED** — Ruling 31 permits a diff-time check only on measured clean separation, and there is none to measure at the level a check would fire: the population says which WORDS deserve a ruling, not which USE is the non-musical sense, which is the judgment Ruling 32 closed the neighbouring limb over; a check built on it would fire on every legitimate musical use of every word in it (**D-473**; **D-436**'s third condition). **Nothing was tuned to improve the measurement** (DT-2). **The external glossary leg is ABSENT and said so, never silently substituted:** this repository vendors no music-theory glossary and importing one would put an unestablished list under load (#19). The per-candidate verdicts are **NOT authored** — that is a named act of its own over a population the artifact counts, and the tool STOPs while any candidate is unclassified, its advisory inventory written anyway so the STOP is a headline rather than a silent nothing. Every value is at `tools/audit/reserved_word_scanner.json` (**D-431**); the reading surface for the user's batch rulings is `ratification_surfaces/cowork_reserved_word_inventory_2026_08_09.md`. **★ RULED 2026-08-09 — the user's Ruling 37 of `cowork_rulings_2026_08_09_sixth_stop.md`, recorded here by `cc_instruction_return_continuation_6.md` Task 0: the scanner outcome is READING 3 — THE COMPLETENESS QUESTION IS ANSWERED AND RECORDED, NOT CLOSED.** The reading surface's own third option is the one taken: the inventory stays at the user's twenty, the convention stays LIVE for new writing, and the answer — *the inventory is incomplete by a measured amount relative to named derivations that are themselves incomplete against the record* — is RECORDED here and in the scanner artifact's closing state as **the standing statement of what "complete" means for this project: complete relative to a NAMED DERIVATION, re-derived as the tree grows, with the derivation's measured miss rate against the seed part of its name.** That statement is now register entry **D-661**, homed at `cowork_audit_protocol.md` beside **D-436**. **The working enforcement is the PER-WAVE SELF-CHECK and not a mechanism** — four consecutive waves of caught collisions in the catching wave's own new prose are its establishment (#19) — and **NO VERDICT-AUTHORING PASS RUNS over the derived population**. **★ ONE BOUNDED OPTION IS HELD AND EXPLICITLY NOT SCHEDULED:** a batch over the SEED TWENTY's bare uses in the governing surfaces — the one subset sound by construction, since the seed words are the user's own inventory and need no derivation to justify them — as a later LEFTOVER-CAPACITY item. It is named so it is not rediscovered and not started. *Excluded alternatives, recorded at the ruling:* authoring verdicts over either derived population, which is unsound over the sharp one and unbounded over the broad one — confidence is not what #19 licenses; and per-word batches over the same population, which is the same defect at a smaller bill. **The row STAYS OPEN because the cleanup itself is still not done**, and what Ruling 37 settles is the completeness question rather than the cleanup | [detail](open_items/OI-229.md) |
| OI-230 | The writing-standards conformance question — DEFERRED by the user to the Friday discussion | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | docs / process | ✅ RESOLVED 2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 0) — **the deferral ends: all three … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-230.md) |
| OI-231 | ★ NON-CONFORMANCE: the production notation path publishes an UNBOUNDED score gap (nats) in a field whose declared contract is [0,1] — against the ratified cross-layer confidence contract | The confidence contract is RATIFIED (user 2026-07-02, `cowork_confidence_contract.md:3`) and its rule U2 (`:39-40`) requires every confidence crossing a layer boundary to be `[0,1]`, class-declared, decision-named; U4 forbids silent re-interpretation downstream. The record arm writes the raw §3.3 key-axis gap straight into `KeyModeAnalysisResult::normalizedConfidence` (`sectionrecordadapter.cpp:293`), a field whose own type header (`keymodeanalyzer.h:111`) and the canonical architecture document (`ARCHITECTURE.md:2224`) both declare `[0,1]`, and carries it across the module boundary as `NoteHarmonicContext::keyConfidence`. NOT a live defect — the one in-tree consumer reads the STORED `hasAssertiveExposure` flag and never re-thresholds (`sectioncadencedetection.cpp:76-89`, the P2a unification) — but a ratified contract is contradicted with no recorded amendment, and a future consumer written against the declared range would be wrong on the production arm and right on the legacy one. Violates `DECISIONS.md` D-032; the departing design is D-019 | joint decoder / notation record / decision-conformance | OPEN — CC-found at the [[OI-207]] adjudication 2026-08-01; a Premise-Gate decision surface (squash at the boundary, OR amend the contract and rename the field + correct its two declared-range statements), never a knob-turn; NOT fixed here (read-only) | [detail](open_items/OI-231.md) |
| OI-232 | The canonical architecture document carries five stale or self-contradicting statements about WHICH CODE IS RUNNING | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | ARCHITECTURE doc / #10 (DT-12) | ✅ RESOLVED 2026-08-02 (CC, the phase-1 truth-sync `ab336f43b5`) — all EIGHT corrected, each dated and citing … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-232.md) |
| OI-233 | NON-CONFORMANCE: the ratified ban on the bare word "region" was recorded EXECUTED "through every layer spec", but the canonical layer specifications still use it 216 times | `ARCHITECTURE.md:786-787` bans "region" unqualified as ambiguous and `:765-768` records the family rename CONFIRMED (user, 2026-07-02) and EXECUTED 2026-07-03 "propagated through every layer spec + the dictionary". `ARCHITECTURE.md` — THE layer specification — contains 216 occurrences including four section headings (`:1223`, `:1722`, `:5416`, `:5720`) and the whole Layer-3 intra-region rule prose. Does NOT propose a tree-wide rename ([[OI-229]] rules that out unilaterally); records that an execution claim in the record is false, so either the claim is corrected to name what it actually covered or the canonical document is brought into line. Violates `DECISIONS.md` D-028 | ARCHITECTURE doc / #10 | OPEN — PARTIALLY DISCHARGED 2026-08-02 (CC, the phase-1 truth-sync `ab336f43b5`): the FALSE EXECUTION CLAIM is corrected — §2.15 now records the 2026-07-03 rename as covering the Cowork design documents and the schema dictionary, names this document as not covered, and cites the four section headings; the ban itself stands for all new text. **The rename pass over this document is NOT done** and stays owed here, still sequenced against [[OI-229]] (b)/(c) so ONE ratified terminology decision governs both | [detail](open_items/OI-233.md) |
| OI-234 | ★ DECISION CONFLICT: two ratified positions on the inference mechanism cannot both stand — forward-only (2026-06-29, which measured the global joint decode INERT) and the joint estimator (2026-07-17, which IS one) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | user adjudication / architecture | ✅ RULED by the user 2026-08-02 — **READING 3**: the 2026-06-29 finding STANDS FOR WHAT IT TESTED … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-234.md) |
| OI-258 | The decided no-key-abstention rule (D-114) and the decided calibrated-abstention principle (D-090) are in recorded tension — stated at one home, settled nowhere | The 2026-08-02 homing act wrote D-114 into the joint estimator's standing rules with the D-090 tension NAMED and deliberately unsettled (resolving-by-drafting was forbidden). Both ratified, same output surface, different answers. Reconciliation is a user ruling at phase 3: exempt the key axis from D-090 with a stated reason, or specify calibrated key-abstention as future completion with D-114 the interim | E — user adjudication / joint output contract | OPEN — phase-3 input (D-231); cross-ref [[OI-234]]'s pattern | [detail](open_items/OI-258.md) |
| OI-259 | The engage-era agenda (Stage-3 entry gate, E4 rows, R1–R9, E1–E5) was overtaken by the joint estimator; no recorded plan states those rows' fate | OI-1…OI-22, OI-64, OI-65 presuppose engaging the rebuilt legacy decoder with Layer 5 — the architecture D-001 replaced. Some concerns transfer (OI-15 spelling, OI-55 membership), some are superseded-in-fact, none re-dispositioned. Phase 3 gives every engage-era row ONE fate: transfers-to / superseded (D-entry named) / stands | E — user adjudication / architecture planning | OPEN — phase-3 input (D-231); surfaced by the phase-1 truth-sync | [detail](open_items/OI-259.md) |
| OI-261 | The ratified tick-local defer contract (D-053) now governs an unreachable path | D-053 kept the older per-stretch resolver on the tick-local path; the OI-238 truth-sync established that fallback is unreachable from the production funnel (survives only as a snapshot-test direct call). Disposition at phase 3: retire with the path (OI-180 scope) or re-scope to the test surface | E — user adjudication / dormant contract | OPEN — phase-3 input (D-231) | [detail](open_items/OI-261.md) |
| OI-262 | What the §10.0 inference-demo view should step through is undecided | §10's demo material predates the joint estimator; the truth-sync could only mark the question open. A held product/design question | E — user adjudication / visualization (held) | OPEN — low; phase 3 or later | [detail](open_items/OI-262.md) |
| OI-263 | Whether §5.11's augmented-sixth preset gate should exist at all | Labels gated to two presets while inference is preset-independent (D-003) and the aug-sixth display collapses on the record arm ([[OI-201]]). A presentation-rule decision, taken with [[OI-201]] | E — user adjudication / presentation | OPEN — phase-3 input | [detail](open_items/OI-263.md) |
| OI-264 | The right-click chord anchor (notes().front()) has no recorded defense — is it the right rule? | Recorded at the owning specification 2026-08-02 with "derivation not recorded"; the list order is an engraving-model order, not a musical one. What should anchor a chord right-click is a phase-3 presentation-seam decision | E — user adjudication / presentation seam | OPEN — phase-3 input; cross-ref [[OI-257]], OI-256 | [detail](open_items/OI-264.md) |
| OI-271 | ★ CONFLICT: the ratified fitting-pool licence constraint says a shipped fitted value may be fitted only on freely-licensed music; the joint estimator's tables and weights are fitted on the DCML annotations that same constraint classifies as validation-only | `cowork_handoff_archive.md:2478-2481` records, **user-ratified 2026-07-04 and marked BINDING on the fitter design** (register entry **D-292**): "ship-intended weights fit only on the PD/CC0/CC-BY pool; NC-class (all DCML, MCMA, Essen…) + no-license sources = validation/QA only". `CLAUDE.md` gate block (A) records that production inference on both surfaces decodes at the committed all-326 tables + the direct-metric SELECTED weight vector — fitted against the *When in Rome* / DCML analyses of the 326 Bach chorales, the corpus the constraint names in its own non-commercial class. Nothing this pass read amends, scopes or withdraws the constraint; the later ratified fitting protocols (D-270 held-out evaluation, D-271 capacity budget, both 2026-07-19) settle how the fit is evaluated and how large it may be, and say nothing about which corpora a shipped value may be fitted on. Three readings are possible (the constraint still binds / superseded in fact and never written down / scoped narrower and never written down) and the record does not say which. The product is unshipped, so nothing is distributed today under a licence it may not carry — which is what makes this settleable deliberately rather than urgently. Bears on D-118 (GPL v3) and D-117 (the intent to contribute officially) | E — user adjudication / licensing | OPEN — ★ RULED by the user 2026-08-02 — **READING 1: THE CONSTRAINT BINDS, reaffirmed as written** (D-292 carries the ruling; D-005 cross-referenced). Cowork verification at the source sharpened the consequence: the census's own class list (`cowork_score_census.md:239-248`) places the **WiR analyses (CC-BY-SA) in the SHIPPABLE pool** and the DCML corpora in the NC class, while the record names the 326-chorale ground truth "WiR/DCML" interchangeably — so the REMAINING ACTION is a verification, not a refit decision: **establish at the objects which licence class the fitted tables' annotation source falls in.** If NC-class → the current fitted values are development/validation only and ship-intended values are fitted on the licensed pool at the corpus-onboarding event ([[OI-38]], where licensing and precision converge); if CC-BY-SA → the fit conforms and the establishment is recorded. **★ THE VERIFICATION RAN 2026-08-02** (CC, the phase-1f final-partition wave; evidence recorded at **D-292**'s provenance and in the dated note): the fit's ground truth is traced end to end to `tools/dcml/when_in_rome/Corpus/Early_Choral/…/analysis.txt` via `dcml_parser.load_wir_regions`, and four things are established — (1) it is **NOT a DCML-lab corpus**, so the NC class the constraint names does not reach it (the DCML corpora are sibling directories each carrying its own CC BY-NC-SA 4.0 licence file; `when_in_rome` carries none), **the NC reading is REFUTED**; (2) **nothing under `tools/dcml/` is committed** (`.gitignore:24`; `git ls-files` returns zero), so the fork distributes none of this material; (3) When-in-Rome's blanket CC-BY-SA grant covers "new content" and conversions, and its own README lists the Bach chorales under "Corpora originating elsewhere" as unconverted Tymoczko-origin RomanText — **neither branch of the grant**; (4) **no analysis file names a licence or a source** (0 of 371 carry a `Source:` header, against the README's own claim). So the class is not NC and not unambiguously CC-BY-SA. **STAYS OPEN on the narrowed question** — under what licence did the Tymoczko-origin analyses reach When in Rome — which is answerable only by the WiR maintainers or the TAOM supplementary's terms, not from anything on this disk. No guess entered; no refit decision follows today | [detail](open_items/OI-271.md) |
| OI-267 | D-248's revisit must be PLANNED: tonicization labels are deferred, but for maximum-precision inference they may be needed | User-directed 2026-08-02 at the residual-queue ratification. The ground truth annotates applied chords (V/V), so never producing them costs Roman-numeral agreement wherever the annotator wrote one. The planning must settle (1) the OI-53 tension — a LIVE classifier emitting V7/x was found 2026-07-13 while D-248's home says not implemented: which statement describes which surface; and (2) the measured stake — a read-only decomposition of the Roman-numeral residual attributable to absent applied labels (the [[OI-192]] fifth-substitution family the suspected neighbour), which decides the revisit's priority | E — user-directed planning / phase-3 input | OPEN — **BOTH owed items ANSWERED 2026-08-02** (CC, the phase-1i delivery acts, on Cowork's user-reviewed probe; dated note): (1) the OI-53 tension — the PRODUCTION joint renderer DOES emit applied labels (`jointrender.h:62-63`; 8.62 % of scored duration carries one, 50,280 ticks EXACT against applied ground truth), so **D-248's recorded scope is CORRECTED in the same commit** to the LEGACY `ChordFunction` structure it actually describes; (2) the measured stake is SMALL — hard ceiling **+1.62 points** on RN-agree (64.12 → at most 65.74), pure right-chord-wrong-name slice **+0.38**, and the suspected [[OI-192]] fifth-substitution kinship **REFUTED both ways** (applied cells are 4.12 % of the fifth family; the fifth shape explains 29.38 % of applied root errors). All four registered bands came in BELOW prediction. Enters the phase-3 plan at **LOW** priority | [detail](open_items/OI-267.md) |
| OI-235 | NON-CONFORMANCE: §4.1's "keep the vertical/contextual boundary clean" prohibition is contradicted by the contextual scoring the same section specifies | `ARCHITECTURE.md:1877-1879` forbids adding heuristics to `RuleBasedChordAnalyzer` "that embed contextual assumptions"; `:1631-1632` and `:1772-1779` specify exactly that (contextual bonuses "using information from neighbouring chords"), and the §4.1g gate table plus the §4.1d bonus table list a dozen of them, each conditioned on a neighbouring chord's root or bass. Not a claim that the bonuses are wrong (Iter 46 alone moved BIR=false 177→128); the finding is that a prohibition and its violation sit in the SAME specification, both stated as current, so the specification cannot decide whether a NEW contextual bonus is admissible. `ARCHITECTURE.md:657-659` already names the mechanism ("accumulating gates are a warning sign"). Violates `DECISIONS.md` D-060 | L4 / decision-conformance | OPEN — CC-found at the [[OI-207]] adjudication 2026-08-01; the legacy scorer is dormant and retires at the [[OI-180]] map, but the DECISION (where the vertical/contextual boundary sits) is not retired by deleting the code that broke it — the joint estimator draws that boundary too, and nothing in the record says where | [detail](open_items/OI-235.md) |
| OI-236 | §4.1b's safety constraint ("bonuses never fire for Diminished, HalfDiminished, Augmented, or Suspended") is superseded by Iter 46 in the same document, unannotated | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | ARCHITECTURE doc / #10 (DT-12) | ✅ RESOLVED 2026-08-02 (CC, the phase-1 truth-sync `ab336f43b5`) — §4.1b now carries the supersession marker … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-236.md) |
| OI-237 | ★ DOCUMENTATION GAP ([[OI-208]] ruling 2): sixteen load-bearing decisions have no layer-specification home; SEVEN of them govern a layer's behaviour and are unfindable from that layer's section | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | docs / process | ✅ RESOLVED 2026-08-02 (CC, the phase-1 specification-completion pass `f833a2d2a9`) — all EIGHT gap decisions … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-237.md) |
| OI-238 | §5.13's analyze-at-tick path table predates the record arm: it asserts "no path-selection flag" while a flag selects between two arms, and its entry-point list describes the arm that is not running | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | ARCHITECTURE doc / #10 (DT-12) | ✅ RESOLVED 2026-08-02 (CC, the phase-1 truth-sync `ab336f43b5`) — §5.13 rebuilt at HEAD: the "no … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-238.md) |
| OI-239 | The joint fact adapter re-implements the Layer-2 boundary rule with three behavioural differences, and no layer specification records that it does | `jointfactadapter.cpp:456-490` builds its own event lattice. The BOUNDARY RULE ITSELF AGREES with Layer 2 (every onset and every offset — worth recording, the segmentation unit conforms). Three properties do not: eligibility (Layer 2 reads Layer 1's flags and forbids re-deciding, `ARCHITECTURE.md:1047-1049`; the adapter has its own inclusion rules and no specification says whether the sets match), silence (Layer 2 emits an explicit EMPTY slice, `:1063-1065`; the adapter skips it — `jointfactadapter.cpp:479-481`, stated only in a code comment), and loaded-span clipping (`:1055-1070` and its re-slice equivalence have no counterpart — the mechanism side of [[OI-212]]). Documentation half is the sharper half: `ARCHITECTURE.md:15` names the event lattice and never states its rule, while the Layer-2 section states a rule for a slicer the production decode does not call. Separable from [[OI-228]] (which is about the note set WITHIN an event, not who computes the events) | joint module / #6 + documentation gap | OPEN — CC-found at the [[OI-207]] adjudication 2026-08-01; two separable acts — record the lattice rule in a layer specification ([[OI-237]] Kind-1 pattern), and decide whether the two boundary computations unify, the latter belonging WITH the [[OI-215]]/[[OI-226]]/[[OI-227]]/[[OI-228]] family design, not before it | [detail](open_items/OI-239.md) |
| OI-240 | ★ DOCUMENTATION GAP ([[OI-208]] ruling 2, one level out from [[OI-237]]): twelve standing decisions are recorded ONLY on a tracking surface — an open-item row or a session handoff block — which is not a home | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | docs / process | ✅ RESOLVED 2026-08-02 (CC, the phase-1 specification-completion pass `f833a2d2a9`, in ONE pass with … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-240.md) |
| OI-241 | ★ A MISSING RULING (the user's to make): no GENERAL rule states which existing MuseScore code our code may depend on — only scoped forms exist | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | E — user adjudication / architecture | ✅ RULED by the user 2026-08-02 — the MuseScore-dependency rule ADOPTED as proposed: analysis library free of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-241.md) |
| OI-242 | NON-CONFORMANCE / #10: `ARCHITECTURE.md` §12.1a's recorded reason for the harmonic-display preference is false at HEAD — the note-seam analysis costs seconds, not "well under 1ms" | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | ARCHITECTURE doc / #10 (DT-12) | ✅ RESOLVED 2026-08-02 (CC, the phase-1 truth-sync `ab336f43b5`) — §12.1a states the measured truth on both … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-242.md) |
| OI-243 | ★ The decode is NOT transposition-equivariant: the spelling factor anchors each key at a canonical per-tonic spelling, so a respelled edition moves content scores AND segmentation | Exploratory invariant probe (perspective-inventory channel 3, 2026-08-02): 12 pieces established 12/12 against `decode_parity_ref.json`, predictions registered first (≥99 % equivalence expected under +2/−3/+6 semitone uniform respellings) — measured **811/1224 segments (66.26 %)**, boundaries moved in 27/36 conditions, three near-collapses at +6 (worst 0/32); six bit-exact conditions prove the apparatus. Mechanism Cowork-verified at source: `probe_decoder.py:743` anchors `key_lof = _PC_TO_FIFTHS[tonic%12]` and the fitted note table bins relative to it, so a uniform respell re-bins a tonality's diatonic tones as pooled chromatic (396 of 413 violations); the signature fold is invariant (`:406-412`, exonerated). Artifacts `tools/joint_estimator/transposition_probe_2026_08_02/`. The REPRESENTATION face of the [[OI-215]]/[[OI-226]]/[[OI-227]]/[[OI-228]] family | joint decoder / emission spelling factor | OPEN — Cowork-found 2026-08-02; any fix is Premise-Gate family design, never per-symptom. **★ THE OWED REPOSITORY-SIDE RE-RUN IS DONE 2026-08-03 (CC, phase 1o, Task 4) AND THE FINDING IS ESTABLISHED — WITH A NARROWER CLAIM THE ROW DID NOT MAKE.** The committed apparatus was driven UNCHANGED (its two absolute sandbox paths re-pointed, its three measurement constants asserted equal to the committed values first); the establishment phase was run BEFORE anything else and PASSED, and predictions were registered before measuring (#17b). **Every one of the 36 conditions reproduces the committed run condition for condition and violation identity for violation identity**; the only difference anywhere is two total scores at the last representable places of a double, which was the named risk and which moved no segment. **The claim is now established AND SHARPENED:** the §4.4 separation the original run did not compute — how much of the non-equivariance an engraver's spelling choice could account for — is generated at `tools/joint_estimator/transposition_probe_rerun_2026_08_03/rerun_analysis.json` → `B_separation_the_dispatch_asked_for` under classes registered BEFORE they were computed (boundary movement is never defensible; a pruned-away expected reading is the [[OI-244]] prune, not a spelling judgment; only the tritone shift was ever enharmonically ambiguous). **The defensible share is a small minority and the boundary-movement share is the overwhelming majority** — so the caveat this row carries does NOT blunt its finding, and the row's establishment status moves UNESTABLISHED → **ESTABLISHED**. Artifacts + method: that directory's `README.md`, `predictions.md`, `rerun_analysis.json` (no figure transcribed, D-431) | [detail](open_items/OI-243.md) |
| OI-244 | The key-prune tie-break ranks by ABSOLUTE tonic pitch class — not transposition-covariant; the expected shifted key can fall out of the kept candidates | `probe_decoder.py:1039-1052` (Cowork-verified): ties among equal-fit keys break on `(−fit, absolute tonic pc, mode)` before the top-K keep, so under transposition the tie order permutes and the correctly-shifted key can be pruned — 5 of the probe's 17 label-only violations diagnosed to exactly this. Compounds [[OI-226]]: the admission prune that entered production with NO ratified basis is also measured representation-non-covariant; whatever basis the family design gives admission must state its invariance properties | joint decoder / candidate admission | OPEN — Cowork-found 2026-08-02; a family-design input, never a knob-turn. **★ THE ESTABLISHMENT CAVEAT IS DISCHARGED 2026-08-03 (CC, phase 1o, Task 4), ON THE SAME RE-RUN AS [[OI-243]] AND ON EVIDENCE OF THIS ROW'S OWN.** The re-run reproduces every condition and every violation identity, so the label-only violations this row is about reproduce individually and not merely in aggregate. **Two things the re-run adds that this row did not have.** (1) The prune-diagnosed violations are now counted MECHANICALLY rather than read off a prose diagnosis: the class is defined as a label-only violation whose expected shifted state was NOT in the candidate set at all — `expected_state_in_candidates` false in the raw comparison — and the count is a field of `rerun_analysis.json` → `B_separation_the_dispatch_asked_for.totals.LBL_PRUNE`, with the member cells at `.members`. It matches the original run's prose diagnosis. (2) The separation puts this row on the correct side of the line the family design needs: **a reading the prune removed from the candidate list is NOT defensible as an enharmonic frame difference**, because the decode never weighed it — which is why the class is scored apart from the tritone-ambiguity class rather than with it. Establishment status UNESTABLISHED → **ESTABLISHED**; artifacts at `tools/joint_estimator/transposition_probe_rerun_2026_08_03/` | [detail](open_items/OI-244.md) |
| OI-245 | Three different eligibility rules exist for ONE Layer-1 note surface; the adapter's grace-note drop contradicts the ratified Layer-2 boundary rule | The joint fact adapter admits everything except grace/zero-duration/excluded-staff notes (`jointfactadapter.cpp:393-399`, the eligibility flags never read); the Layer-2 slicer applies the three flags and KEEPS grace notes (the ratified rule: a grace note opens a boundary by its span, `ARCHITECTURE.md:1112-1116`, `slicer.cpp:40-48`); the legacy views apply the flags AND drop grace (`regiontoneprimitives.cpp:54-57`/`:232-235`). One question, three answers (#6); the adapter's variant is deliberate (fitting-extraction parity, `jointfactadapter.h:65`) but specified nowhere | joint module / layer boundaries (#6) | OPEN — Cowork-found 2026-08-02 (notation-feature census); resolves with [[OI-239]]'s input-surface specification act + the family design | [detail](open_items/OI-245.md) |
| OI-246 | Concert and written pitch are MIXED inside one record on transposing instruments: pitch class sounding, spelling written, signature prior concert | `note_model.cpp:93` (pc from sounding `ppitch()`) vs `:94` + `note.cpp:826-829` (line-of-fifths from the WRITTEN tpc when the concert-pitch style is off) vs `jointfactadapter.cpp:360` (concert signature) — the spelling factor and key prior are fed contradictory evidence on any transposing staff, and [[OI-243]] establishes the spelling factor is exactly where representation errors move committed readings. No layer statement records which representation the spelling field must use. Unexercised by the chorale corpus | joint module / input representation | OPEN — Cowork-found 2026-08-02; a first-class input-representation decision for the family design's input-surface specification | [detail](open_items/OI-246.md) |
| OI-247 | The key-signature prior is read from staff 0 at tick 0 ONLY: mid-score key changes never enter it; staff 0 is read with no eligibility or exclusion check | `jointfactadapter.cpp:358-367` — one staff, one moment: a mid-score notated key change never reaches the prior (the joint-module sibling of OI-94(a)), and staff 0 could be the program's own chord track or a drum staff (no `excludeStaves`, no eligibility read) | joint module / input surface | OPEN — Cowork-found 2026-08-02; input-surface half with the family design's specification, mid-score half an inference completion at its #8-correct stage | [detail](open_items/OI-247.md) |
| OI-248 | The adapter's meter arithmetic is wrong off common time — and the beat-strength class it feeds is a FITTED covariate of the boundary factor | `jointfactadapter.cpp:352` integer-divides the bar length (3/8→1, 7/8→3, 6/16→1); compound meter recognized only at denominator 8 (`:119-122`); the mid-strong class can only fire in 4-quarter bars (`:79`). Boundary evidence is systematically mis-classified in those meters; the chorale fit corpus never sees it | joint module / metrical facts | OPEN — Cowork-found 2026-08-02; inference-affecting, #8-correct stage, with a refit caveat (the covariate's fitted values assumed the current classification); shares a design surface with [[OI-249]] | [detail](open_items/OI-248.md) |
| OI-249 | The adapter's reconstructed metrical grid assumes a constant nominal bar length and detects only a leading pickup — mid-score irregularity drifts it permanently | `jointfactadapter.cpp:163-184`; acknowledged in-code only as "pathological xml" (`:126-129`), which mid-score meter changes are not. Downstream of the drift: the beat-strength covariate ([[OI-248]]), the bar assignment that deletes anacrusis notes (the [[OI-228]] dated note), fermata/boundary alignment | joint module / metrical facts | OPEN — Cowork-found 2026-08-02; one design surface with [[OI-248]] (what metrical facts the adapter derives, from what, specified where) | [detail](open_items/OI-249.md) |
| OI-250 | Repeats, voltas and jumps are NOT unfolded, and no recorded rule says which timeline the analysis reads | Zero repeat-list references under `src/composing`; the timeline is raw notated tick order everywhere. Volta-1-end and volta-2-start read as adjacent by the transition machinery and the cadence-approach window — a progression never heard is scored as if it were. Notated order may be the RIGHT answer for a notation-facing analysis, but nothing says so — an unstated-decision finding of the [[OI-226]] class | joint module + L1 / timeline semantics | OPEN — Cowork-found 2026-08-02; decide once, record at the owning layer, state the factor consequences | [detail](open_items/OI-250.md) |
| OI-251 | A two-note tremolo is analyzed as two sequential half-length chords, not one sustained sonority | `chord.cpp:540-571` halves each written chord's duration; no analysis-side tremolo handling exists — one sustained dyad becomes two consecutive shorter events with different bass picks possible per half. Orchestral/romantic textures use it constantly; chorales never | joint module + L1 / feature handling | OPEN — Cowork-found 2026-08-02; low; a feature-handling rule at the input surface, #8-correct stage | [detail](open_items/OI-251.md) |
| OI-252 | Bar-repeat signs contribute ZERO notes — the repeated bars' music is silently absent from the harmonic evidence (#12) | `MeasureRepeat` is a Rest subtype (`measurerepeat.h:33`) and the L1 walk takes chords only (`note_model.cpp:227`): the music sounds, the evidence is silent, nothing marks the gap | L1 / feature handling (#12) | OPEN — Cowork-found 2026-08-02; a Layer-1 completion (resolve the repeated content, or flag the bar unread-not-silent) at its #8-correct stage | [detail](open_items/OI-252.md) |
| OI-253 | Fermata detection is track-exact: a fermata on one voice does not flag a simultaneous chord in another voice — and the flag feeds TWO fitted factors | `note_model.cpp:81` requires `a->track() == chord->track()`; on two-voices-per-staff SATB engraving up to three of four voices' simultaneous chords carry no flag — the FIT CORPUS ITSELF is affected (not an out-of-envelope finding). The flag enters the boundary factor and the cadence factor (both fitted); whether the fitted values absorbed the track-exactness is the [[OI-228]]-class refit caveat | L1 / fermata fact | OPEN — Cowork-found 2026-08-02; small, fitted-covariate-affecting | [detail](open_items/OI-253.md) |
| OI-254 | NON-CONFORMANCE: the status-bar harmonic annotation is UNLOCALIZED and outside the accessibility tree — against the ratified §12.1 localization + accessibility statements (D-118/D-119) | Raw string concatenation with no translation call (`notationcomposingbridge.cpp:835-844`); plain-text append to accessibility info (`notationaccessibility.cpp:203-207`), never in `screenReaderInfo`. `ARCHITECTURE.md:5854-5858` mandates MuseScore Qt localization (English + Swedish for ALL new strings) and MuseScore accessibility patterns; the user re-stated the requirement 2026-08-01 | notation / presentation / decision-conformance | OPEN — Cowork-found 2026-08-02 (requirement-side review); a presentation-layer completion at its #8-correct stage (no inference change anywhere) | [detail](open_items/OI-254.md) |
| OI-255 | `addAnalyzedHarmonyToSelection` is UNREACHABLE from the UI — implemented, interfaced, mocked, snapshot-tested, registered to no action, menu or shortcut | Repo-wide: no caller (`notationinteraction.cpp:8256` + interface + mocks + replication tests exist; the three annotate action codes route to `addHarmonicAnnotationsToSelection`, `notationactioncontroller.cpp:378-386`). Needs ONE disposition: dead code at the seam (#6/DT-5) or an unshipped feature (declared dormancy, consumer named). RE-SCOPES [[OI-213]]: its N-produce multiplier concerns a command no user can invoke — the cost fact stands, the exposure does not | notation / interaction seam | OPEN — Cowork-found 2026-08-02; disposition owed | [detail](open_items/OI-255.md) |
| OI-256 | Record-arm test-coverage gaps against #11, enumerated — the production interactive path's seams are largely untested; every large-score test is a DISABLED measurement, not a gate | Six checked gaps: `NotationAccessibility` untested (the OI-206 mechanism site); the context-menu model untested; `addAnalyzedHarmony` + "Tune as" untested; the decode-cache suite pins the LEGACY arm only (fixture never sets the flag — the production path has no cache-semantics coverage); all large-score tests `DISABLED_` with the enabled golden corpus capped at 16 measures (nothing gates the [[OI-209]] repertoire class); the golden suite is ARM-MIXED (`implode`/`keyAreas` built by direct legacy calls, three others flag-ON — `pipeline_snapshot_tests.cpp:768-773`/`:938`) | tests / #11 | OPEN — Cowork-found 2026-08-02; the INPUT LIST for [[OI-199]] partition 3 + the OI-203 cache increment's test plan; the arm-mixed golden question may warrant an earlier act | [detail](open_items/OI-256.md) |
| OI-205 | STATUS.md and ARCHITECTURE.md have grown too large (render + human/LLM context capacity) | USER-DIRECTED 2026-07-27, two halves on different clocks: (a) STATUS.md — the mechanical archive sweep (the established `STATUS_ARCHIVE.md` pattern: superseded dated entries move verbatim; the lean file keeps the current entries + active next-action + baseline pointers) — rides the switch's doc-sync or immediately after; (b) ARCHITECTURE.md — a DESIGN question, not a sweep: sufficiently complete and comprehensive without exceeding what a human or LLM can hold — candidate shape the register split's index+satellite pattern (a lean top-level map + per-layer/per-surface satellite docs), decided as its own Cowork-drafted decision surface. Timing for (b) derived from the ratified Stage-3.5 file-split lesson (restructure ONCE, after the boundaries stabilize): AFTER the OI-180 retirement map's deletions settle, BEFORE the OI-198/199/200 reviews consume it | docs / #10 | OPEN — half (a) the STATUS.md archive sweep DONE at the notation switch 2026-07-27 (this commit — superseded dated entries moved verbatim to STATUS_ARCHIVE.md, byte-reconciled); half (b) the ARCHITECTURE.md restructure OPEN — post-map, pre-reviews, its own Cowork-drafted decision surface | [detail](open_items/OI-205.md) |
| OI-203 | Record-arm note-seam latency: whole-score decode per interactive query (record cache deferred) | The record arm's note seam runs `produceNotationRecord` (a whole-score decode, ~seconds-scale on large scores) on EVERY interactive query (status bar fires per note selection), bypassing the legacy bounded-window decode cache — a genuine interactive-latency regression on the flag-ON path, declared at the note-seam build, deferred not absorbed. The remedy is a measured, keyed record cache ((score, change-token) — simpler than the legacy window cache), built as its own later increment under #17's measure-before-build, after the switch. | notation / presentation performance | OPEN — Cowork-rowed 2026-07-27; POST-SWITCH the whole-score-decode-per-query latency is now on the DEFAULT path, priority RISES; the latency MEASURED (`noteseam_latency.json`, marginals Task 1) + the decode cost-vs-span SHAPE MEASURED (`window_study.json` cost_curve, OI-206 Task 2, 2026-07-28); still measured-then-built — the keyed (score, change-token) record-cache DESIGN is Cowork's next step (the OI-206 fix decision surface), correctness unaffected, product unshipped | [detail](open_items/OI-203.md) |
| OI-204 | The record producer lacks analysis-input staff exclusion (`excludeStaves`) — chord-track self-feedback risk | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | composing / the record producer's input surface | ✅ RESOLVED 2026-07-27 (seams-2 P6 Task 1) — `buildAdapterFacts`/`produceNotationRecord` gain an … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-204.md) |
| OI-202 | Register split-check false-fails on every post-split addition | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / register tooling | ✅ RESOLVED 2026-07-27 (seams-2 note-seam Task 1) — added a default LIVING mode (register_check.json: … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-202.md) |
| OI-184 | WiR anacrusis beat-alignment convention unestablished | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | measurement / establishment | ✅ RESOLVED (ESTABLISHED) 2026-07-19 — the (measure,beat)→tick convention is correct at measure bodies; the m0 … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-184.md) |
| OI-187 | The weight fit's band miss | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | joint-estimator / fit event | ✅ ANSWERED 2026-07-20 by the ratified direct-metric fallback (beats the likelihood arm; mechanism explained); … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-187.md) |
| OI-188 | The decode prune cannot reach the ground truth | The GT state was force-added on 14,257 spans (~72% reachability), and the declared prune's cost is worse at the fitted weights — bounding every ceiling claim on this decoder. | joint-estimator / fit event | OPEN — ★ OPEN — bounds every ceiling claim; re-measured at the direct-metric weights it is materially less prune-sensitive; the exact-decode reserve is the declared remedy (never budgeted at fitted weights) | [detail](open_items/OI-188.md) |
| OI-189 | 42% of GT chord transitions go through the leftover rule | The ratified option-2a leftover apportionment carries nearly half the transition evidence at the fit event, ratified from a single sensitive-cell probe — an unquantified sensitivity. | joint-estimator / fit event | OPEN — quantify the fit's sensitivity to the leftover variant (machinery supports leftover_mode="even") before the rule is relied on | [detail](open_items/OI-189.md) |
| OI-190 | The cadence factor's leading-tone feature is indistinguishable from zero | The fitted weight is 0.007±0.029 (premise P8 as predicted); the direct-metric search inverts the ranking (tritone_pair pinned to 0, leading_tone carries load) — a form question, not a value tweak. | joint-estimator / fit event | OPEN — SHARPENED 2026-07-20; two objectives disagree completely about which cadence feature matters; Cowork's call now covers the whole cadence feature set | [detail](open_items/OI-190.md) |
| OI-191 | The direct-metric search is rugged and under-determined | The 21 restarts converge to 21 different optima (held-out key-local varies 3.38–5.42 pp within one fold), so "best of 21" is itself an unbudgeted fitting act the OI-177 capacity budget never counted. | joint-estimator / methodology | OPEN — ★ OPEN — a methodology item; it does not invalidate the held-out figures but bounds how much of the gain is restart selection; options named, not actioned | [detail](open_items/OI-191.md) |
| OI-185 | bwv352 m1 b4 bass note unverified | Decides the C2 share-tone posterior (Am6↔F♯ø7); a one-note read at the kern source settles it; the BCMH copy is not currently on disk. | measurement / verification | OPEN — low; a one-note verification when the kern copy is next available | [detail](open_items/OI-185.md) |
| OI-179 | Ground-truth ceiling unmeasured — and measuring it here is the ONLY route, because the field has published nothing to cite | DCML annotator agreement (per axis) bounds attainable precision; without it structural error and annotator disagreement are indistinguishable; the field has published no such figure (**D-474**, a FACT-of-absence, ratified 2026-08-04); 87 dual-annotated (BCMH) stems exist — BCMH independent-in-origin but **unestablished as an instrument** (**D-475**), and a consumer may not put it under load while that stands (#19). | measurement / establishment (#21) | OPEN — **OPEN — NOT "a measurement not yet built" among others: it is the only available route to the quantity principle #21 demands** (re-stated 2026-08-04, CC, READ WAVE 3, dispatch `cc_instruction_reads_3.md` Tasks 2.3/3, on the user's ruling; the consequence is recorded at the principle itself, `CLAUDE.md` #21). Citation is unavailable — **D-474** measured the absence by search, corpus by corpus — so the choice is not between citing and measuring but between measuring and having no ceiling at all. The in-repo census and the literature half are DONE. **The two establishment routes D-475 names are now BOTH EXHAUSTED, and neither is still owed from the user:** the BCMH zip was downloaded by the user and read at the files 2026-07-19 (no annotator record in any of them — the only editor records are the KernScores note-encoding provenance), and the JEP:HPP 2023 Method section was read from a user-supplied PDF the same day (it names no annotator and no validation procedure; its corpus citation is an unpublished 2019 presentation). **What remains is one act only the user can perform: contacting the PeARL laboratory** for annotator count, identity and validation. **★ THAT ACT WAS PERFORMED AND IT HAS BEEN ANSWERED, so the sentence immediately above is SUPERSEDED and is preserved rather than removed (#12): the laboratory replied on 2026-08-11 and annotator count, identity and validation are no longer UNKNOWN** — ONE annotator, named, PhD in musicology, weekly review with the corpus author and **no independent annotation**, no duplicate readings. The reply is recorded in substance on the detail file, its consequence at `CLAUDE.md` principle #21, and **D-475**'s first ground discharged at the decisions register with its other two carried; none is restated here (#6). **What remains from the user is a DIFFERENT act — the ERRATA limb, which the reply did not address** — **★ AND THAT ACT IS NOW PERFORMED TOO, so the sentence immediately before this one is SUPERSEDED and is preserved rather than removed (#12): the user sent the errata re-ask on 2026-08-11, so the limb is RE-ASKED and AWAITING REPLY, with principle #21's clause (a) reasonable-wait clock running from that date. It is NOT answered and NOT closed; what is awaited is a reply and no longer an act. The re-ask carried two further questions bearing on D-475's two surviving defects, recorded with the #19 clause that an answer to either is an INPUT to the desk simulation and never a substitute for reading the original files — on the detail file, and not restated here (#6). Corrected 2026-08-12, dispatch `cc_instruction_pack_section7_and_errata_state.md` Task 2, declared as one act beyond that dispatch's three named surfaces; the correction sits after the canonical opening, where rule (f) makes it inert, so no state moved** — and one new obligation: **this row does not close until the measurement's result has been sent to the laboratory**, which the reply asked for and the detail file states with the caution that what would be sent is agreement between two annotation traditions and is **not** an error rate for their corpus. **Two defects survive establishment and bind any measurement design** — the annotations sit on a homorhythmic REDUCTION (a unit mismatch with full-texture grading), and they reached the repository through a machine translation into rntxt whose noise would appear inside any measured disagreement; the local originals let the second be cross-checked or removed by grading against BCMH's own `**harm` spines. **★ COMMISSIONED IN ITS TWO ZERO-SESSION-COST HALVES 2026-08-09 (CC, `cc_instruction_return_continuation.md` Task 0, recording the user's **Ruling 10 of 2026-08-09**, `cowork_rulings_2026_08_09_return.md`); THE ROW STAYS OPEN AND THE GATE IS UNCHANGED.** **(a)** The PeARL laboratory contact — the one act named above as only the user's — is the user's, and is the longest-lead item because it is outside any session's scope; nothing else waits on it, and no session may substitute for it. **(b)** The ceiling MEASUREMENT is COMMISSIONED and **opens WITH phase 2, desk simulation first (#17c)**, per the landed design surface `cowork_scratch_2026_08_08/draft_oi179_ceiling_measurement_surface_2026_08_08.md`. **What the commissioning does NOT do, stated so the row is not read as further along than it is:** **no measurement tool is built before phase 2 opens** — the finish-line scope rule stands, and this ruling is precisely the user ruling that scope rule requires before the measurement may be added when phase 2 opens; no measurement is taken; no ceiling figure exists or may be quoted; the two surviving defects above (the homorhythmic-reduction unit mismatch and the rntxt machine-translation noise) still bind any design and are not discharged by being scheduled; and **D-475**'s establishment bar on BCMH is untouched, so a consumer still may not put it under load (#19). **GATES** — an establishment obligation (#19), which D-438's own starred clause makes gating whatever its subject, and commissioning a measurement is not performing one | [detail](open_items/OI-179.md) |
| OI-33 | Abstain-aware robust-stop convention | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | measurement | ✅ RESOLVED 2026-07-12 (wave-1) — convention written + mechanically enforced (root counts an abstain as … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-33.md) |
| OI-34 | O-12 corpus git-tracking decision | A user call; carries the deferred obligation that the committed corpus is platform-dependent (CRLF only because regeneration runs on Windows; a byte-identical OS-independent fix exists, offered not taken). | measurement / user | OPEN — user | [detail](open_items/OI-34.md) |
| OI-35 | O-18 stale fs_* manifests process fix | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | measurement | ✅ RESOLVED (read-site validation half) 2026-07-12 — validate-or-re-manifest at the km/fs read … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-35.md) |
| OI-36 | O-10 retained-rule liveness counts | At every adoption sandwich. | measurement / standing | STANDING | [detail](open_items/OI-36.md) |
| OI-37 | CLASS_A_INVESTIGATE advisory tripped | The rebuilt-arm dim7 churn +46320 > 9600 tripped the advisory read-only — investigate at E4 design. | measurement / advisory | OPEN advisory | [detail](open_items/OI-37.md) |
| OI-38 | O-5/Wave-3 corpus onboarding | Jazz/pop GT tiers + a pedal-dense corpus + the 53 excluded Bach stems, 22 Beethoven movements, bwv846; a human acts as ground truth where no formal GT exists; the target breadth is late-romantic through atonal; timing stays #8/#9-governed. | corpus onboarding | OPEN — the expansion is mandated at the corpus-onboarding event; the when-question is with the user | [detail](open_items/OI-38.md) |
| OI-39 | Overfitting risk — single-composer split | A single-composer 326-score split; idiom coverage 1/5 (A-7 marks). | corpus / debt | OPEN debt | [detail](open_items/OI-39.md) |
| OI-40 | R-11 conformal / R-12 self-consistency levers | Deferred dispositions. | measurement / deferred | OPEN — deferred | [detail](open_items/OI-40.md) |
| OI-134 | Debris directory tools/tools/corpus/ | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | measurement / cleanup | ✅ CLOSED 2026-07-12 — comparison done (debris, deliberately-filtered extras); user decided DELETE; deleted … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-134.md) |
| OI-85 | L1/L2-audit Task-0 git-state incident | Plumbing commits left the object DB ahead of the working tree; resolved read-only via reset+restore (nothing discarded); plus a concurrent-edit hazard with Cowork. Promoted DT-18. | harness / convention | OPEN — a convention owed (a plumbing commit ends with a main-index refresh + a disk-vs-HEAD verify) | [detail](open_items/OI-85.md) |
| OI-89 | L1/L2 pass-2 second reading only partially blind | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | audit / L1-L2 | ✅ RE-RUN DONE 2026-07-11 — fully-blind reading + error rate, zero correctness misses; L1/L2 certification … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-89.md) |
| OI-100 | L3 pass-2 second reading deviated from the P2 rubric | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | audit / L3 | ✅ RE-DERIVATION DONE 2026-07-11 — the prose carries the fine label on the code+facts axes, overstated on … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-100.md) |
| OI-95 | L3-audit tooling debt | A second disposition generator alongside the L1/L2 one + line-stale committed L1/L2 audit CSVs (#6/housekeeping). | audit tooling | OPEN — housekeeping; the SWEEP-tool half discharged (gen_signature_sweep --layer); the disposition-generator unification + the l1l2 restamp remain | [detail](open_items/OI-95.md) |
| OI-123 | L5 instruments silent-failure paths (DT-23) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED 2026-07-12 (wave-1) — broad catches narrowed to load/parse types + surfaced; the no_wir … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-123.md) |
| OI-124 | L5 first-rank establishment/contract gaps (DT-2) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED 2026-07-12 (wave-1) — GT fingerprinted + checked; robust_stop_diff cross-checks manifest vs … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-124.md) |
| OI-125 | L5 hand-set measurement tolerances (DT-2) | compare_analyses' 0.5 overlap tolerance + the 4-beats-per-measure extrapolation assumption hand-set with no independent derivation (#19). | instrument / measurement | OPEN — centralized + two established as derived; the extrapolation half resolved 2026-07-13 (meter-correct derivation, byte-identical, tested); OPEN on the two calibration_fit gates (min-cell 50/20 + near-logistic 0.05), each with its named experiment | [detail](open_items/OI-125.md) |
| OI-126 | L5 dead + duplicated instrument code (DT-5+DT-3) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED 2026-07-12 (wave-1) — both dead functions deleted; the map single-sourced; byte-identical → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-126.md) |
| OI-127 | L5 documented/minor + audit-tooling notes | Five minor instrument notes (unmapped-quality passthrough, a -1 root sentinel false-agreement, RN parenthetical leniency, no independent class-split check, a gen_inventory stdlib false positive). | instrument / measurement | OPEN — PARTIAL 2026-07-13: (a)/(b)/(e) closed byte-identical (measured); (c) the normalise_rn leniency + (d) the a8 class-split second implementation + the disposition-generator unification remain | [detail](open_items/OI-127.md) |
| OI-128 | L5 grading+fitting silent-failure paths (DT-23) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED 2026-07-12 (wave-1) — the fake-GT write closed (FAILED.json + stderr, no GT written); load excepts … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-128.md) |
| OI-129 | Grading/fitting chain skips the anti-contamination guard | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED (validate_corpus_dir half) 2026-07-12 (wave-1) — both now validate the corpus before reading; … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-129.md) |
| OI-130 | Destructive default output paths + unenforced music21 pin (DT-24) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED 2026-07-12 (wave-1) — every instrument defaults output to scratch; the music21 v9.9.1 pin is … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-130.md) |
| OI-131 | Fit-manifest validated by nothing, consumed by no code + triple-represented | param_manifest.json is read by no code; the fitter carries its own PARAMS copy, so the fit surface is triple-represented (C++ / manifest / PARAMS) (DT-5+DT-3). | instrument / Stage-5 | OPEN — single-source the fit surface at the Stage-5/EG-5 manifest work | [detail](open_items/OI-131.md) |
| OI-132 | L5 grading+fitting duplicated key-parsing + value-copies + dead code (DT-6+DT-3+DT-5) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ FULLY CLOSED 2026-07-13 — dead code deleted; the parent-collection reduction ruled by the user + landed … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-132.md) |
| OI-133 | L5 stale docstring figures + dangling anchors + hand-set tolerances (DT-11+DT-12+DT-2) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED (figures + anchors + the D1 stale-fixture); the tolerance half narrowed — most are diagnostic … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-133.md) |
| OI-135 | L5 harness value-copied inference-affecting constants (DT-3) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / harness | ✅ RESOLVED 2026-07-13 — single-sourced via modePriorAppDefaults() + kDefaultOnsetBoundaryThreshold; the regen … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-135.md) |
| OI-136 | Undocumented harness measurement modes (DT-25) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / harness | ✅ RESOLVED 2026-07-13 — all six flags added to printHelp(); doc/contract-only, the corpus regen is … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-136.md) |
| OI-137 | Harness output-discipline + exit-path robustness (#16) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / harness | ✅ ESTABLISHED-AND-DOCUMENTED 2026-07-13, NO CODE CHANGE — measured (all 1056 strictly CRLF; the force-exit … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-137.md) |
| OI-138 | Harness doc-sync + audit-tooling fidelity (DT-12) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / harness doc | ✅ RESOLVED (a)/(b) 2026-07-12 — the manifest line-refs made symbol-led + the example fixed to --preset Jazz; … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-138.md) |
| OI-139 | OI-120 factual correction + manifest site-line drift (DT-12) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement doc | ✅ RESOLVED 2026-07-12 — OI-120's membership correction recorded; the manifest functionresolver.h site lines … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-139.md) |
| OI-140 | WiR-coverage → the hard stop can pass silently (DT-23+DT-2) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED 2026-07-12 (wave-1) — a8 distinguishes missing-annotation from parse-failure (named + stderr); … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-140.md) |
| OI-152 | The remaining key-parse abstain (Dorian ♭2) | The shared graded key reduction still abstains on any mode suffix with an accidental/digit; at HEAD exactly Dor♭2 survives (4080 Jazz / 2400 Default ticks); it reduces to the PARENT tonic minor (C minor), NOT D minor. | instrument / measurement | OPEN — deferred by the user 2026-07-13 ("land what's ratified; file the leftover"); gate: the parser's next touch | [detail](open_items/OI-152.md) |
| OI-153 | Register ID collision | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | register / harness | ✅ RESOLVED 2026-07-13 — register_lint.py detects duplicate row IDs, wired as gate 0 of the hardening battery; … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-153.md) |
| OI-154 | Explainability as a future end-user feature | If the evidence trail behind every inference is published, "show me why" is a late-bound display consumer of already-published facts; the gap is publication (OI-145 wave 3). | held / long-horizon | OPEN — held; revisit when wave 3 publishes the evidence surfaces | [detail](open_items/OI-154.md) |
| OI-155 | The OI-132 consolidation left two regression tests red | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | measurement / #11 | ✅ RESOLVED 2026-07-13 — the user ruled RESTORE ABSTAIN-ON-TRULY-UNKNOWN; fixed in code (_mode_class over the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-155.md) |
| OI-151 | Destructive default output path on the mode-grading probe (DT-24) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED 2026-07-13 — --out defaults to scratch; verified a bare re-run leaves the committed artifact … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-151.md) |
| OI-52 | S20 — the metric's core comparison written out four times | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / #6 | ✅ RESOLVED 2026-07-13 — one shared roots_agree() helper (three MORE copies found + folded); value-identity … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-52.md) |
| OI-156 | The 4th hard-coded 0.25 onset-boundary literal (DT-3) | notationharmonicrhythmbridge.cpp's null-config fallback is a value-copy of kDefaultOnsetBoundaryThreshold — the residual of OI-135(b); in src/notation, outside the harness dispatch's scope. | notation bridge / #6 | OPEN — deferred by the user 2026-07-13; gate: the next src/notation config-unification touch (the bridge reads the constant instead of the literal) | [detail](open_items/OI-156.md) |
| OI-157 | A third copy of the mode classification went stale (DT-3/DT-6+DT-11) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED 2026-07-13 — the table deleted, the probe grades through the ONE shared reduction (new … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-157.md) |
| OI-158 | The music21 corroborator's local-key path has never run | FloatingKey() raises (the v9.9.1 class is KeyAnalyzer); a bare except swallows it and local_key silently falls back to the global key on every region — a Class-B instrument-establishment failure; no governing figure affected. | instrument / establishment (#19) | OPEN — 🔶 DEAD-CODE HALF RESOLVED 2026-07-13 (user ruled: delete the dead machinery, do NOT activate KeyAnalyzer; regen byte-identical); the evidence half (adopt a KeyAnalyzer local key as a second opinion?) is OPEN at the key-layer design conversation | [detail](open_items/OI-158.md) |
| OI-159 | OI-142 silently staled the committed OI-43/OI-44 joint-probe evidence | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED 2026-07-13 — evidence refreshed from a HEAD run (O-12 snapshot first), shelve ruling re-confirmed; … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-159.md) |
| OI-57 | Corpus QA systematic pass | Half superseded (the GT-corpus manifest discipline now exists); half real — the extra-scores registry (140 entries) is stale/unvalidated against 163 files on disk (23 unregistered incl. all 20 hiromi/). | corpus / registry | OPEN — ASSIGNED: the corpus-onboarding event (OI-38); the registry must be accurate + mechanically validated there before any of those scores is used | [detail](open_items/OI-57.md) |
| OI-160 | OI-142 also staled the sibling joint-probe artifact | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | instrument / measurement | ✅ RESOLVED 2026-07-13 — the user ruled option A (collapse to ONE artifact); the arc-12 no-go + the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-160.md) |

## E. User adjudication / ratification pending

| ID | name | description | layer / gate | status | detail |
|---|---|---|---|---|---|
| OI-41 | #17(c) sharpening (mechanism-fires before arithmetic) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | user adjudication | ✅ RATIFIED 2026-07-10 — folded into CLAUDE.md #17(c) → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-41.md) |
| OI-42 | Audit §5 UNCLEAR rows 1–7 | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | user adjudication | ✅ RATIFIED 2026-07-10 — six were pure rule applications; the one genuine acceptance is A3 (tolerating the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-42.md) |
| OI-43 | Reopen the joint (key,chord) ranking question | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | user adjudication | ✅ SETTLED 2026-07-12 — the joint step is SHELVED on both axes; the key-axis headroom is redirected to the key … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-43.md) |
| OI-44 | Joint step B1–B4 status | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | user adjudication | ✅ DECLARED by the user 2026-07-12 — design DELIVERED / build SHELVED on both measured axes; contradiction B … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-44.md) |
| OI-141 | The key-menu investigation | The key-axis headroom is a key-layer menu-and-ranking question (menu width + ranking); reframed to "understand why our key/mode inference does not work", diagnosed and grounded, mechanism pinned at the code, design opening delivered. | key-layer (L3) | OPEN — REFRAMED 2026-07-12; the diagnosis + drift-research grounding + mechanism-at-code + design-opening are all delivered; the design conversation is OPEN with the user | [detail](open_items/OI-141.md) |
| OI-142 | Corpus integrity — 12 transposed chorales | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | corpus / measurement (#9/#19) | ✅ RESOLVED 2026-07-12 (adoption d9b52ba969) — an arithmetic offset correction at load_wir_regions; run-diff … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-142.md) |
| OI-143 | The key-grading convention (home + local) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | measurement | ✅ RESOLVED 2026-07-12 (adoption d9b52ba969) — the dual column landed; local < home (the analyzer tracks the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-143.md) |
| OI-144 | WiR ground-truth loading has two views | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | measurement | ✅ RESOLVED 2026-07-13 (b3511fd28a, user-ratified) — graded surfaces routed + the 4 calibration maps refit; a8 … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-144.md) |
| OI-56 | A music-theory judge without ground truth? | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | user adjudication | ✅ DECIDED by the user 2026-07-13 — a HUMAN acts as ground truth (the judge as guidance, never a grader); the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-56.md) |
| OI-62 | Is the intonation/tuning feature still a goal? | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | user adjudication / long-horizon | ✅ DECIDED by the user 2026-07-13 — KEEP HELD (intonation is a future feature and a declared future CONSUMER … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-62.md) |

## F. Doc-sync debt (#10) — annotate, don't rewrite history

| ID | name | description | layer / gate | status | detail |
|---|---|---|---|---|---|
| OI-45 | scoring_model §4/§6 stale anchors | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync | ✅ RESOLVED — 2026-08-14 (CC, `cc_instruction_scoring_model_pass.md` Task 2; the document pass is commit … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-45.md) |
| OI-46 | Audit §3/§4 tables contradict §3.1 build-status | On the FQ-1/FQ-3 stage (contradiction A). | doc-sync | OPEN — annotate the tables | [detail](open_items/OI-46.md) |
| OI-47 | STATUS.md submission-era sections contradict governing docs | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_status_touch_and_oi141_premise_repin.md` Task 2) — **BOTH halves … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-47.md) |
| OI-48 | backlog_chord_track_flag.md dangling reference | Referenced by two live docs; the file is a CC memory (nothing lost); the code substance (name-based chord-track detection) is unbuilt (contradiction J). | doc-sync | OPEN — ASSIGNED (doc-sync): re-point STATUS.md:3744 + note the DEFECT_TYPES.md example is spent; the code substance rides OI-61 | [detail](open_items/OI-48.md) |
| OI-49 | D-L3a "closed" vs "remains" | Fitter §4.5 vs roadmap G1 (contradiction K). | doc-sync | OPEN — reconcile | [detail](open_items/OI-49.md) |
| OI-50 | FQ-2 "decided at Stage 2" vs "enumerated-not-resolved" | Arc plan vs L5-engage (contradiction G). | doc-sync | OPEN — annotate the arc plan | [detail](open_items/OI-50.md) |
| OI-51 | Foreign uncommitted SHELVED-banner edit | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync | ✅ RESOLVED 2026-08-03 (CC, phase 1p, Task 3) — **the premise is REFUTED: the edit is IN the record.** The … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-51.md) |
| OI-257 | §12.1b documents 2 of the 9 registered harmonic-analysis actions; the right-click chord anchor is an undocumented representative-note choice | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / #10 (DT-12) | ✅ RESOLVED 2026-08-02 (CC, the phase-1 truth-sync `ab336f43b5`) — §12.1b completed from two actions to all … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-257.md) |
| OI-260 | CLAUDE.md's gate-threshold policy still lists the retired Gate K (0.20) | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / governing document | ✅ RESOLVED 2026-08-02 — the Gate K line corrected in `CLAUDE.md`'s gate policy (user-ratified; rode the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-260.md) |
| OI-265 | The Layer-4 as-built body still says production chord analysis runs the legacy path | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / #10 (DT-12) | ✅ RESOLVED 2026-08-02 (CC, the phase-1d enumeration wave) — the Layer-4 correction block now carries the same … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-265.md) |
| OI-266 | The session handoff's working-protocol standing rules have no specification home | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / documentation gap | ✅ RESOLVED 2026-08-02 (CC, the phase-1d enumeration wave, after the user ratified D-232…D-254 at … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-266.md) |
| OI-268 | ★ Twenty-two standing decisions are recorded in a ratified contract or cross-layer design document, not in a layer specification | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / documentation gap | ✅ RULED by the user 2026-08-02 — **the FIFTH HOME CASE exists**: a ratified contract document the owning … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-268.md) |
| OI-269 | CONFLICT: a standing process lesson says commit pipeline-affecting changes immediately; the governing document says commit only when asked | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / #10 | ✅ RESOLVED 2026-08-02 (CC, the phase-1e second-partition wave, executing the user's ruling of the same date) … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-269.md) |
| OI-270 | Four cross-cutting "meta-findings to institutionalize" whose ratification the record does not state — the wave's one recorded REMAINDER | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | documentation gap | ✅ RULED by the user 2026-08-02 — SPLIT INTO FOUR and each ruled SUPERSEDED-BY its named later ratified … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-270.md) |
| OI-272 | Fourteen standing decisions are recorded ONLY in the session-handoff ARCHIVE — a surface two governing documents declare outside the session-start read | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / documentation gap | ✅ RESOLVED — ★ RULED by the user 2026-08-02 — the PER-KIND scheme: live constraints/policies homed into the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-272.md) |
| OI-273 | ★ NON-CONFORMANCE: a THIRD edit to MuseScore's own code is live at HEAD, absent from `CLAUDE.md`'s local-patches section, with no distribution disposition | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / decision-conformance | ✅ RULED by the user 2026-08-02 (option (i)) — the patch is RECORDED in `CLAUDE.md`'s local-patches section … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-273.md) |
| OI-274 | `docs/scoring_model.md` describes the legacy scorer as WHAT RUNS, in a document `CLAUDE.md` makes a mandatory session-start read | §1 opens "`RuleBasedChordAnalyzer::analyzeChord` **is** a bottom-up vertical-sonority scorer" (`:32-34`) and the whole document keeps that present tense, while that scorer is dormant on BOTH production surfaces (the joint estimator since 2026-07-26 batch/**D-005** and 2026-07-27 notation/**D-010**). The [[OI-232]]/[[OI-265]] class one document over — and both of those were resolved by adding a SCOPING SENTENCE, which this document has none of. It matters more here because `CLAUDE.md` directs every scoring session to read it and no other mandated read corrects it. Two smaller instances found in the same read: the footer still says "Last updated: 2026-06-12" while the body carries the Stage-3.3 migration, the 2026-07-05 gate retirements, the 2026-07-14 OI-168 fix and the 2026-07-16 OI-170 measurement; and `docs/key_path_design.md:3-6` + `docs/precision_metric_design.md:3-5` both banner themselves "DRAFT — UNCOMMITTED" (the former adding "`git commit` NOT") while being tracked files at HEAD. NOT in doubt: the scoring content — §8's twelve constraints are all registered (D-214…D-224, D-061) and this read corrected none of them | doc-sync / #10 (DT-12) | OPEN — **its FIRST half and both smaller instances are DONE; the row stays open on its SECOND half, which is the user's and was not performed** (2026-08-14, CC, `cc_instruction_scoring_model_pass.md` Tasks 2–3; the document pass is commit `b366d44947`). **Done:** the §1 SCOPING SENTENCE, the [[OI-232]]/[[OI-265]] form — everything from §1 on remains accurate for the legacy vertical scorer and is NOT a description of what produces a committed chord today, with §8's constraints binding regardless of that dormancy; the FOOTER re-stamped, naming at the dates the body itself gives them every act the stale stamp omitted; and BOTH banners corrected, `docs/key_path_design.md` (banner and its closing line, which repeated the claim) and `docs/precision_metric_design.md`, each with its former wording preserved (#12) and its trackedness established at the git object by explicit hash rather than asserted. **NOT done, and the reason this row does not flip:** whether `CLAUDE.md`'s mandatory-read instruction should also name the joint estimator's own specification is a GOVERNING-DOCUMENT question and therefore the user's. *(A pointer to that specification was added inside `docs/scoring_model.md` §1 and is marked in the document as a pointer that does not amend `CLAUDE.md`.)* **The banner correction is deliberately MINIMAL and does not close [[OI-317]]**, whose own cell reserves the filing decision behind what the banner should become | [detail](open_items/OI-274.md) |
| OI-275 | ★ CONFLICT: the SIGNED Layer-4 specification makes COMPLETE candidate listing the governing design decision; the production decoder's candidate admission prunes, and empties the analysis outright on most orchestral scores | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | E — user adjudication / decision-conformance | ✅ RULED by the user 2026-08-02 — **READING 1-WITH-TRANSFER** (the D-266 pattern): D-329's PRINCIPLE binds the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-275.md) |
| OI-276 | Three LIVE-SPEC design documents state as CURRENT something false at HEAD — and one names a REGRESSION GATE that no longer exists | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / #10 (DT-12) | ✅ RESOLVED — 2026-08-11 (CC, `cc_instruction_return_continuation_11.md` Task 1, the session-executable gating … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-276.md) |
| OI-279 | ★ `ARCHITECTURE.md` §6.7 states the RETIRED genre style taxonomy as the canonical one, and two register entries quote that stale text | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / #10 (DT-12) | ✅ RESOLVED 2026-08-03 (CC, phase 1k, executing the user's ruling of the same date) — the user was presented … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-279.md) |
| OI-280 | `cowork_progression_schema_dictionary.md` uses the label "D5" for two different decisions | The document names two unrelated rulings "D5": the **dependency-map ownership** ruling at §1 (`:41`, and pointed at from the terms table `:19`) — this catalog owns the named progressions and substitutions, the function layer owns the pairwise licensing grammar — entered as **D-406**; and the **harmonic-scope** component decision at §7 (`:261-263`), the fifth of that section's own D1…D5 series — entered as **D-408**. The two come from different private numbering series (`cowork_progression_schema_design.md` §6's, and this document's own §7's) and nothing in the text distinguishes them, so a reader meeting "D5" and looking it up under "Component decisions" finds the wrong ruling. It offends `CLAUDE.md`'s no-self-invented-labels convention. Recorded until now only inside D-406's provenance field, which is a status surface for one decision and not a home for a document's defect. NOT in doubt: both decisions' content, ratified by the user 2026-08-03 | doc-sync / documentation gap | OPEN — CC-found 2026-08-02 (phase 1j); rowed 2026-08-03 by the user's direction (phase 1k, ruling R5(a)). A documentation act at the dictionary's next touch — name each label's series, or drop the private labels for plain words. Best landed together with any [[OI-281]] banner edit so the file is touched once | [detail](open_items/OI-280.md) |
| OI-281 | ★ Does an `ARCHITECTURE.md` delegation pointer confer contract-home status on a document that self-declares "v1 draft"? | [[OI-268]]'s fifth home case requires **ratified-or-signed + a status banner + the delegation pointer**, the ratification being the user's directly **or transitive** through a user-ratified delegating surface, guarded by "an assistant's stamp alone never confers contract-home status" (`CLAUDE.md` rule (g)). `cowork_progression_schema_dictionary.md` satisfies the delegation half twice over (`ARCHITECTURE.md:4589-4591` §7 names it as the component's own spec; §6.7 `:4579-4580` points at it for the taxonomy) and fails the ratification half in a way no other candidate showed — its banner reads "component spec, **v1 draft** (2026-06-29)" (`:3`), and a draft names no ratifier at all. **Nine entries (D-406…D-414) carry the `⚠gap` flag for exactly this**, and their 2026-08-03 ratification as *rules* does not decide the document's home class. Two readings, neither chosen: **(i)** transitive authority reaches it — the delegating surface is user-ratified, so the draft banner is a stale self-description, and nine flags close at their cause (#6); **(ii)** it does not — a self-declared draft names nobody for the delegation to carry across, and a pointer establishes where detail lives, not that it was decided. Phase 1i's own precedent cuts both ways: `cowork_structural_integrity_audit.md` was EXCLUDED on the second half of the criterion though its transitive ratification was genuine, while `cowork_layer5_engagement_design.md` was ADMITTED on transitive authority with only "DESIGN (CC, 2026-07-07)" as its banner. **Scope, stated honestly: the answer applies to every draft-bannered document `ARCHITECTURE.md` points at, not to this one** — which is why it is a user ruling and not a homing act. Sibling of [[OI-268]]'s other open question (whether a COWORK ratification counts — **21** entries: `docs/decoder_design.md` 4 + `cowork_score_census.md` 17); the two should be ruled together and must answer consistently | E — user adjudication / documentation gap | OPEN — CC-found 2026-08-02 (phase 1j, inside D-406…D-414's provenance); rowed 2026-08-03 by the user's direction (phase 1k, ruling R5(b)). **Deliberately NOT resolved by editing the dictionary's banner** — that would decide the question by fiat, and for one document only. **★ THE USER RULED THE CRITERION 2026-08-03 — the DELEGATION-SPECIFICITY criterion (option iii), over this row and [[OI-268]]'s sibling together — and phase 1l MEASURED it without applying it** (the dated measurement note on the detail file; no entry's home class changed, as the ruling did not authorize re-classification before the cost was known). Population **131** entries / 21 documents. **36 move on the clean cases — 26 IN** (`cowork_score_census.md` 17, `cowork_progression_schema_dictionary.md` 9) **and 10 OUT** (`cowork_architecture_reassessment.md` 4 and `docs/iteration_path1_summary.md` 2, named in NO user-ratified surface; `docs/redesign_plan.md` 4, cited three times but twice from blocks `ARCHITECTURE.md` itself banners SUPERSEDED). **Both precedents land where phase 1i had them** — so no #13 STOP — but `cowork_structural_integrity_audit.md` is now excluded on the JUDGMENT clause alone ("stable enough to be cited"; its §5 is headed "UNCLEAR rows for user adjudication"), having PASSED by-name-for-a-stated-concern at `cowork_engage_arc_plan.md:4`. **33 entries are genuinely AMBIGUOUS** — more than the clean net movement of +16, so the criterion does not determine the direction of its own effect — and they reduce to TWO general follow-on questions: does "Full spec: `X`" delegate or only cite (26 entries), and is a SHELVED document stable enough to be cited (9). **And the criterion does not ANSWER [[OI-268]]'s sibling question, it makes it MOOT** — it never asks who ratified the document, only who delegates to it; the 21-entry Cowork-ratification set splits 17/4 on grounds unrelated to Cowork ratification. **★ THE USER REPLACED THE THIRD CLAUSE WITH A KIND TEST 2026-08-03, and phase 1m MEASURED IT — again without applying it** (second dated note on the detail file; artifact `tools/audit/decisions/phase1m_measurements.json`). Population now **134** entries / 22 documents. **ADMIT 65 · EXCLUDE 40 · AMBIGUOUS 29**; 16 move in (dictionary 9, joint-key-chord 4, arc plan 3), 10 move out (reassessment 4, redesign plan 4, iteration summary 2), **net +6** against the old clause's +16. **Both precedents land right — no #13 STOP** — and the audit is now excluded on KIND, from its own words, rather than on a judgment about settledness: **clause (c) as a judgment is gone from the test entirely.** **Cowork's advance prediction is CONFIRMED**: the census's 17 move OUT against phase 1l's measurement. **But ambiguity falls only 33 → 29**, because the dominant source was never clause (c) — 26 of the 29 turn on the untouched clause-(b) question (does "Full spec: `X`" delegate or only cite), so the criterion STILL does not determine the direction of its own effect (+16 if all ambiguous admit, −20 if all exclude). **One correction of record:** phase 1l recorded `cowork_notation_adoption_increment.md` as named in NONE of the three surfaces; it is named in two (`ARCHITECTURE.md:4660`, `CLAUDE.md:128`), and its 3 entries join the ambiguous set. **One structural finding the ruling does not answer:** kind is a property of the DOCUMENT while a delegation points at a SECTION — `ARCHITECTURE.md:317` delegates to the census's §8c, a rule-stating block inside a findings-kind document, and that decides the largest single population (17) | [detail](open_items/OI-281.md) |
| OI-282 | `cowork_style_clustering_plan.md` still presents the CLUSTERS half as future work, a day after it was delivered and ratified | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / #10 (DT-12) | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Task 3) — **the act the row itself … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-282.md) |
| OI-283 | The decisions register's coverage claim states a stale `ARCHITECTURE.md` line count, and nothing keeps it current | `DECISIONS.md`'s "What was read, and what was not" — the register's own bounded-trust statement, the thing `DEFECT_TYPES.md` DT-26 requires so a sweep complete inside its own file set is not read as complete about the whole question — says "ARCHITECTURE.md IN FULL (**all 6,523 lines** … ). Lines 1-3981 were read by the 2026-08-01 adjudication". The file is **7,045 lines at HEAD** and 7,089 after the phase-1k §6.7 restatement; the figure was already 522 lines stale before this session, the phase-1i and phase-1j homing acts having written sixteen decisions into the document without re-stamping the sentence. The sentence lives in the backbone DATA (`backbone_decisions.json`, `header.scope.read_in_full`) so it renders as a generated figure on every regeneration — but it is **hand-typed and carried forward**, the shape #17(f) forbids. Two unstated narrowings ride with it: most of the growth is text the homing passes wrote out of already-registered decisions (likely harmless, but the paragraph does not make that argument, so a reader cannot tell it from unread growth), and the cited line ranges are themselves stale. NOT in doubt: the per-entry anchoring, mechanically checked and passing at 414/414 quotes and 408/408 anchors | doc-sync / #10, #17(f) | OPEN — CC-found 2026-08-03 (phase 1k, on the standing self-check of its own diff). Two acts at the register's next touch: stamp the claim to a COMMIT rather than a line count (#16 applied to a coverage claim), or have `gen_decisions_register.py` compute it (#17(f), which removes the failure mode). Not executed at phase 1k — rewording what the register claims about itself belongs with the phase-1 completion statement, which rests on this very paragraph | [detail](open_items/OI-283.md) |
| OI-284 | `docs/implementation_roadmap.md`, the SINGLE TRACKER, states the dormant-spine world as current — the layer table, the engage plan, and the superseded batch gate | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / #10 (DT-12) | ✅ RESOLVED 2026-08-03 (CC, phase 1l, Task 4, executing the user's ruling of the same date) — the question … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-284.md) |
| OI-285 | The eight ratification-surface documents are UNCOMMITTED, and the register cites two of them as the provenance of ratified decisions | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / #12 | ✅ RESOLVED — **THE COMMIT HALF ONLY** — 2026-08-03 (CC, phase 1l, Task 5, executing the user's ruling of the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-285.md) |
| OI-286 | ★ The record says the `chordanalyzer.cpp` file split is OWED and parked; it was delivered on 2026-06-17, and one of the units it produced is LIVE production code | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / decision-conformance | ✅ RESOLVED 2026-08-03 (CC, phase 1m, Tasks 1–2, executing the user's ruling of the same date) — all three … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-286.md) |
| OI-287 | The ratification surfaces and the dispatches have no directory of their own, and moving them re-aims cited paths | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / repository organization | ✅ RESOLVED 2026-08-03 (CC, phase 1u, Task 4, executing the user's ruling of the same date — **option 4A, a … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-287.md) |
| OI-288 | How live are the P4 fallback and the [[OI-165]] Layer-1.5 primitive, on real repertoire? | A fire-rate question of the [[OI-199]] P4 kind, bearing on what the retirement map deletes. **The row's framing was CORRECTED against the code before it was written:** the phase-1m dispatch held the P4 fallback reachable on the LIVE notation arm, and it is not — `analyzeHarmonicContextAtTick` returns the record view at `notationcomposingbridge.cpp:737` whenever `useJointNotationRecord()` is true, which is the default (`composingconfiguration.cpp:178`), and the regional path (`:743`) and the P4 fallback (`:753`) sit BELOW that return; the other caller (`:789`) is inside a function `:760` declares "NOT the production path". So the only figure in the record — "Fires 0/2231 on the perf corpus" (`:750-752`) — measures how often P4 fires GIVEN the legacy arm ran, and is not a measurement about the live arm at all. Two things had been conflated: whether the branch is reachable (control flow, now answered) and how often it fires when reached (repertoire, still open). **What is still worth measuring: (a)** the fire rate WITHIN the legacy arm on the 23 committed large scores — [[OI-215]]'s lesson is that a branch measured dead on the fit population can be live on the requirement's repertoire, and a zero count on one corpus is not an establishment (#19); **(b) the sharper half — is [[OI-165]]'s own "not currently scheduled to die" still true?** Both production call sites of `findTemporalContext` (`regionanalyzer.cpp:921`, `notationcomposingbridge.cpp:651`) are on the legacy arm, and what schedules the legacy path for deletion is a **general statement** — [[OI-180]]'s note of 2026-07-27 and `CLAUDE.md` gate block (A) — **not a numbered retirement item**. ★ **CORRECTED 2026-08-03 (CC, phase 1o, Task 4.2), READ AT THE MAPS' OWN TEXT:** this row first said the call sites sit on an arm "which R1/R5/R6 delete (**D-418**)", and those three rows do not say that — `docs/implementation_roadmap.md:180-187` has R1 name the legacy chord competition and Gates A–L, R5 name `resolveKeyAndModeRanked`+`collectPitchContext`, R6 name the segment-first spine, and **no row of R1–R10 names** `findTemporalContext`, `regiontoneprimitives.cpp`, `analyzeRegions`, `analyzeHarmonicRhythm` or `analyzeHarmonicContextLocallyAtTick`; neither does the six-item map at `cowork_prefit_gates.md:173-186`. The conclusion the row drew is unchanged — the route to it was wrong, and a reader following the citation would have found nothing there. The detail file recorded this correction at phase 1n; **the INDEX row, which is the authoritative status surface, carried the false route for two further waves**, which is why the correction is made here and not only there. (b) matters beyond bookkeeping: **D-428** classifies the iteration-API renames as debt on *partly-live* code precisely because `regiontoneprimitives.cpp` is held to survive. ★ **AND (b) IS ANSWERED** (CC, phase 1n; the answer lives at [[OI-165]], not duplicated here): the phrase is accurate about **scheduling** and was being read as a claim about **survival**; no consumer for the primitive is named anywhere in the record, and six false-negative paths are enumerated there (#17e). **Only half (a) — the fire rate within the legacy arm, on real repertoire — remains open on this row.** NOT in doubt: the three call facts the dispatch cites | E — measurement owed / L1.5, L4 | OPEN — rowed 2026-08-03 (CC, phase 1m, Task 3) by the dispatch's direction, **not scheduled here**; it enters phase 2's program | [detail](open_items/OI-288.md) |
| OI-289 | The 80 LEGACY-marked register entries were SWEPT, never verified — and two of them were wrong | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | E — verification owed / decisions register | ✅ VERIFIED 2026-08-03 (CC, phase 1w) — the full re-verification is DELIVERED as the generated artifact … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-289.md) |
| OI-290 | `cowork_score_census.md` §8c is a rule-stating section that also holds a findings table, and most entries admitted through it are rows of that table | The section-level home criterion (**D-430**) admits a SECTION that states rules; `ARCHITECTURE.md:319` delegates the licence-pool constraint to §8c by name, and §8c does state three binding rules — but it also carries the needs-vector TABLE, whose rows record findings, and most of the entries the criterion admitted are rows of it. Counts generated, never transcribed: `tools/audit/decisions/phase1p_delegation_bar.json` → `census_8c_mixed_section`. **One correction of record rides with it:** [[OI-281]]'s phase-1n note §4 and the phase-1n `STATUS.md` entry both say NINE of the eleven are table rows; derived from the file the count is one fewer, the over-counted entry being the section's opening question-and-answer above the table. NOT in doubt: that §8c states three binding rules, that the delegation names it, and that the eleven are correctly homed there | doc-sync / #10 | OPEN — rowed 2026-08-03 (CC, phase 1p, Task 1) on the user's ruling of the same date (W1, option 3C): the eleven admitted entries **STAND** on the section mechanism, and the finding is rowed rather than smoothed over. **The remedy is at the DOCUMENT, not at the classification** — split §8c so the rules and the findings are not one block, at the census's next touch, in the [[OI-269]]/[[OI-282]] shape (preserve and re-section, never rewrite). Not executed here: the dispatch licenses the register, `CLAUDE.md`, `cowork_audit_protocol.md` and the rows, and re-sectioning a user-ratified corpus document is none of those. The narrower *"scoped to the delegated concern"* reading is **recorded and NOT adopted**, on precedent rather than preference — through three waves "for a stated concern" has tested DELEGATION QUALITY and never been a per-decision scope limit (`ARCHITECTURE.md:1331` delegates "this layer" and admits all sixteen of that document's entries) | [detail](open_items/OI-290.md) |
| OI-291 | ★ STOP: the register's `contract-home` class was never reached by any ratified criterion, and the delegation bar excludes documents it currently admits | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | E — user adjudication / decisions register | ✅ RESOLVED 2026-08-03 (CC, phase 1q, Task 1, executing the user's ruling of the same date — **option A3: ONE … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-291.md) |
| OI-292 | Ten `CLAUDE.md` rules could be enforced by a mechanism and are not; the strongest is that no check confirms the four local patches are still present | Every rule `CLAUDE.md` carries, classified MECHANISM-EXISTS / MECHANISABLE-AND-NOT / KNOWLEDGE, over a population DERIVED from the register (every entry homed in `CLAUDE.md`; the register enumerated the document in full — protocol P1) rather than searched for. Artifact `tools/audit/claude_md_rule_triage.json` from `tools/audit/claude_md_rule_triage.py`; counts at `totals` and `the_defect_set`, nothing transcribed (**D-431**). Five families in the defect set, and the first is this wave's own: **★ the file-tools rule (D-253) itself, BUILT AND NOT ARMED** — `tools/audit/shell_read_guard.py` is committed and established (its `--establish` artifact carries the deny rate on the forbidden forms and the FALSE-deny rate on this session's real commands), but arming it means one block in `.claude/settings.json`, which `.gitignore:112` puts OUTSIDE the record as "Claude Code local session state", and **a live control existing only in an untracked file is the [[OI-285]] class** — so it was left unarmed rather than switched on invisibly, the arming block written out verbatim in the tool's own docstring; **the four local patches** (**D-197**/**D-198**/**D-199**/**D-316**), each stated as do-not-revert *against a dependency update* — the silent failure a check catches and a reader does not, with no false-positive mode; **two same-commit rules** stated as conditions on a diff (**D-192**, whose named arithmetic staleness check — the scoring document's template count against `kTemplateCount` — nothing performs; **D-230**); **two prose conventions** (**D-113** the reserved music-theory vocabulary, whose collided words the rule itself enumerates and whose convention is mechanical; **D-194**, weak); **two measurement rules** (**D-187** uncertainty on every comparison; **D-308** the research-tier-on-entry rule the corpus manifest could carry as a tier). NOT in doubt: the population, derived; the classes, each carrying its mechanism or its ground | process / mechanisation debt | OPEN — rowed 2026-08-03 (CC, phase 1p, Task 5, Mechanism 4) by the dispatch's own instruction. **The triage is a PROPOSAL and nothing was executed** — retiring ratified text is a change to what the governing document says and is the user's; the tool does not edit `CLAUDE.md`. Each member needs an ESTABLISHED form before it is built (#19): a measured detection rate against real violations and a measured false-positive rate on legitimate work, because a guard that fires on legitimate work gets switched off. **The honest answer to the dispatch's own discipline, recorded rather than glossed** (`what_was_executed`): neither mechanism built this wave retires any prose — the process check makes **D-431**/**D-434** checkable without replacing their statements, and the shell-read guard enforces part of **D-253** while the rule's scope, exemption and measured defense must stay readable. They remove failure modes; they do not shrink the governing document, and whether that is reason enough to keep them is the user's. **★ UPDATED 2026-08-03 (CC, phase 1q, Tasks 3–5) — the criterion that judged them has been WITHDRAWN by the user and replaced, and two of the ten are now built.** (1) The prose-retirement test is withdrawn as an unvalidated structural proxy (#17d) and replaced by three measured conditions — runs automatically, measured detection rate, measured false-positive rate at or near zero — homed at `cowork_audit_protocol.md`'s dispatch-protocol section, register entry **D-436**; both phase-1p mechanisms are KEPT under it, and their failing the withdrawn test is no longer a defect. (2) **The local-patches check is BUILT and ESTABLISHED**: `tools/audit/local_patches_check.py`, its patch list DERIVED from `CLAUDE.md`'s own section so a fourth patch recorded there is covered without a code change, with a mandatory retirement path (a `SUPERSEDED UPSTREAM` line that must name the upstream commit or release, or it is a STOP) now stated in that section's preamble; established at `tools/audit/local_patches_establishment.json`, which measures detection against deliberately unpatched content — synthetic mutations of every marker AND the real pre-patch object where `CLAUDE.md` records the commit. **One correction of record rides with it: `CLAUDE.md`'s section carries THREE patch subsections, not four** — the four this row names are four REGISTER ENTRIES (D-197 being a distribution disposition on D-199's patch, not a patch of its own), and the check derives its list from the document. (3) **The armed-guard check is BUILT and reports NOT ARMED**: `tools/audit/guard_armed_check.py` verifies the shell-read guard is declared as a `PreToolUse` hook in the local settings; the CONTROL is thereby in the record while the machine-specific settings file stays out of it, which is the answer to this row's own [[OI-285]] objection. Its failing state is the expected delivery state and is recorded as such in `STATUS.md`; **the arming remains owed and this row carries it**. **★ ARMED 2026-08-03 (the user's act) — and the row does NOT close clean, because the control is ONE-SIDED.** `tools/audit/guard_armed_check.py` now reports ARMED: `.claude/settings.json` declares a `PreToolUse` hook running the guard. Four coverage limits stand and are stated rather than glossed (#19 — a control believed to be in place and not in place is worse than none): **(i)** the matcher names the CC-side shell tools, so the guard binds the EXECUTING side and NOT the writing side, whose shell tool carries a different name and does not match — **the file-tools rule must not be described as enforced, only as enforced on one side**; **(ii)** the check verifies the hook is DECLARED, not that it FIRED, and the settings watcher only reloads directories that held a settings file at session start; **(iii)** the guard covers the TEXT-UTILITY half only — the branch-tip/index half (`git status`, `git log`, `git rev-parse HEAD`) is deliberately unenforced, as the guard's own docstring states, and **that gap was demonstrated live this wave: a `git status --porcelain` issued by the executing side was NOT denied**, which is the guard behaving as designed and the rule being broken anyway; **(iv)** the configuration lives in a gitignored file, which is why the control is in the record as the CHECK and not as the settings. **A FALSE DENY was also measured live**, and it is not in the establishment corpus: `grep -c "^  D-" /tmp/reaim_1r.txt` was denied because the command is split on whitespace, so the quoted pattern's second word `D-"` is read as a repository path. The guard's SANCTIONED set contains no quoted-pattern-with-spaces case, so its measured false-deny rate does not cover this shape — the arming is recorded, the rate is not claimed to bound it. **★ TWO OF THOSE LIMITS ARE CLOSED 2026-08-03 (CC, phase 1s, Task 1, on the user's ruling Y1), and the two that remain are unchanged.** Limit (iii): `git status` is now DENIED. It became separable from `git log` and `git rev-parse HEAD` the moment a sanctioned way existed to answer the question it was being used for — `tools/audit/changed_paths.py`, built this wave — so denying the raw command no longer fires on legitimate work, which was the whole ground for leaving it unblocked; a command naming the enumeration tool is exempt, so the guard cannot deny the thing that makes it affordable. `git log` and `git rev-parse HEAD` stay unblocked, and the reasoning that admits `git status` does not reach them. The measured FALSE DENY is FIXED: segments are tokenized with `shlex` in NON-posix mode (posix mode would eat the backslashes in a Windows path), so a quoted argument stays one token; **re-established, and both figures published either way under D-436** — `tools/audit/shell_read_guard_establishment.json` → `changes_2026_08_03_phase_1s.the_tokenizer_fix`, whose `false_denies_on_the_sanctioned_set` and `detection_on_the_forbidden_set` carry the before-and-after. The dispatch's revert condition (a rise in false denies elsewhere) is **not met**, so the fix stands. Limits (i) and (ii) — one side only, and declared-not-fired — are **untouched, and the rule must still not be described as enforced on the writing side**. **★ One BYPASS in that same change, found by the standing self-check and fixed in the same wave:** the enumeration exemption was written over the WHOLE command, so `cat CLAUDE.md; python tools/audit/changed_paths.py` would have passed — the exemption built to make the `git status` denial affordable would have opened a hole wider than the denial closed. It is now judged PER SEGMENT, and both bypass shapes are in the guard's FORBIDDEN corpus so the published deny rate measures them. **★ And a SECOND false deny, measured live by the new rule blocking its own commit:** the `git status` denial was first written as a pattern search over the whole command, so a `git commit` whose MESSAGE quotes the rule was denied — a commit message, a heredoc body and a quoted argument are all text. Fixed by testing the SUBCOMMAND'S POSITION (after the segment's leading `git`, past `-C <dir>` and any options, the next word must be `status`), which is the shape the text-utility test already uses rather than an exception; three prose-quoting forms are now in the SANCTIONED corpus. Both corrections are the same lesson at two scales, and both were found by RUNNING the guard, not by reasoning about it. **★ THE ARTIFACT THIS ROW POINTS AT IS WRITTEN AGAIN 2026-08-09 (CC, `cc_instruction_return_continuation.md` Task 3, on the user's Ruling 8 of 2026-08-09), AFTER A PERIOD IN WHICH IT COULD NOT BE.** The triage's population is DERIVED — every register entry homed in `CLAUDE.md` — and its class is AUTHORED, so every homing wave that writes a decision into that document adds a member and cannot supply its own verdict; fifteen had accumulated and the tool had been STOPPING, which means writing nothing. The fifteen are now authored per the tool's own criteria and delivered for the user's review at `ratification_surfaces/cowork_rule_triage_entries_2026_08_09.md`; **none of them is in the defect class**, so this act adds nothing to it. **But the regenerated set is NOT the committed one:** it gains **D-546**, whose OWED classification was authored in the tool's source before this session and was never written out because a tool that stops writes no artifact — so the COMMITTED artifact has been describing a defect set one member short for as long as the check has been failing. Checked at the objects, not assumed. Every count stays in the artifact and none is restated here (**D-431**); this row's title figure is the one it was rowed at and the movement since is in the text above | [detail](open_items/OI-292.md) |
| OI-293 | The WRITE LIST — six homes the record means to keep whose delegation the bar does not admit, or whose delegation names too few sections | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | E — user adjudication / decisions register | ✅ RESOLVED 2026-08-03 (CC, phase 1r, Tasks 1 and 4). **The user WROTE all six delegations**, and the pass was … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-293.md) |
| OI-294 | Writing the delegations falsified the `CLAUDE.md` rules that asked for them — rule (j) still says no delegation exists, and rule (i)'s defense cites two line numbers that have moved | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / #10 | ✅ RESOLVED 2026-08-03 (CC, phase 1s, Task 2, executing the user's ruling of the same date — **Y2, option 1B: … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-294.md) |
| OI-295 | A user-RATIFIED rule is left with no home because its section is an open-items list — the [[OI-290]] shape, in a signed layer specification | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / decisions register | ✅ RESOLVED 2026-08-03 (CC, phase 1s, Task 3, executing the user's ruling of the same date — **Y3, option 2B: … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-295.md) |
| OI-296 | Sweep for ratified rules parked in tracking sections — D-341 is unlikely to be the only one | [[OI-295]] found ONE user-ratified rule homed in a design document's open-items list and moved it to the section it amends; nothing about how it got there was special to that document. A design document accumulates its open-items section as it is built, a ruling arrives against an item in that list, and the ruling is recorded where the ITEM sits rather than where the RULE belongs — a habit, not an accident. [[OI-290]] is the mirror shape at a second document (a rule-stating section that also holds a findings table), and both are the same underlying fact: **the section is the unit of the home criterion, and these documents were not written in sections that separate rules from findings.** **The sweep is the part the classification CANNOT see**, which is why it needs its own act: the pass reaches only sections that hold a register entry, in the 22 documents that are somebody's home, so it cannot see a ratified rule in a tracking section that no entry records, or in a document outside the home population, or a MIXED section admitted as rule-stating whose findings rows then ride along (the [[OI-290]] direction — both directions are the same defect and only one of them raises a `gap`). What the pass CAN see is already reported and already rowed elsewhere: after the OI-295 move exactly two sections judged RECORDS FINDINGS still hold entries (`tools/audit/decisions/home_classification.json` → `by_document`, `states_rules` false; re-aimed 2026-08-09 at the removal of the file this formerly named, under the user's Ruling 1), both named at `not_write_list_cases` — one decision homed in a status banner, three in a build-status record. NOT in doubt: that D-341 was one instance and is fixed; that OI-290 is a second, unfixed; that neither was found by looking for the class | doc-sync / decisions register | OPEN — rowed 2026-08-03 (CC, phase 1s, Task 3) by the dispatch's own instruction (*row the sibling sweep, do not run it here*). **A mechanical form is available and is what the sweep should be, not a re-read:** the signal that found D-341 is a section whose heading names open items, deferred refinements, status or risks, holding a paragraph that carries a ratification marker (`★ RATIFIED`, `user-ratified`, `DECIDED`) or an amendment verb (*"the enumeration is amended"*, *"the set now also includes"*). Under **D-436** that owes a measured detection rate against the two instances already known and a measured false-positive rate on legitimate tracking prose before it can be trusted. Class 1 of the sweep depends on [[OI-207]]'s reading coverage, which is not complete | [detail](open_items/OI-296.md) |
| OI-297 | `tools/audit/process_check.py` truncates its own findings list on a Windows console — it crashes mid-print and exits non-zero after writing a complete artifact | CC-found 2026-08-03 running the check over the phase-1o dispatch. The tool writes its JSON artifact first and then prints the findings; the print crashes with `UnicodeEncodeError` on the first finding whose quoted sentence carries a character outside the console's `cp1252` encoding (here `→`, in the dispatch's own text), so the run ends with a traceback after listing a PREFIX of the findings. **Why it is more than cosmetic:** the visible symptom is a truncated list plus a traceback, and a reader who takes the printed lines for the whole result under-counts the findings — the same shape as a search that reports fewer misses than it made, which is what #19 exists against. **What is NOT at risk:** the artifact is written before the print and is complete, and the artifact is what a report cites (D-431), so no recorded figure is affected; the establishment artifact regenerated byte-identically this session. The fix is one `errors='replace'` stream wrapper or printing through the artifact, and it needs its own establishment under **D-436** like every other member of the mechanism set | process / audit tooling | OPEN — CC-found 2026-08-03 (phase 1o, Task 5) on the standing self-check of this session's own guard run; NOT fixed here (the dispatch licenses no tooling change beyond what its tasks build). Cross-ref [[OI-292]], which carries the mechanism set this tool belongs to. **★ CLASSIFIED 2026-08-03 (CC, phase 1u, Task 5) on the user's direction: a KNOWN LIMIT — a print-encoding matter, NOT a check defect.** What the check finds is unaffected (the artifact is written first and is complete; a report cites the artifact, **D-431**), so no recorded figure is wrong and the tool's measured rates are not in question — it is not a mechanism failing one of D-436's conditions. **And the class is WIDER than this tool, measured this wave:** the same `cp1252` abort was hit twice more on ordinary inspection commands over repository prose, so it reaches any `tools/audit/` tool that prints repository text to a Windows console. The two tools built this wave print **ASCII only** and say so in their source, so the class does not grow while the row is open; the fix belongs across the set, not to this one tool | [detail](open_items/OI-297.md) |
| OI-298 | `CLAUDE.md`'s phase-2 clause leans on an UNRATIFIED DRAFT for the enumeration it requires | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / governance | ✅ RESOLVED — ★ RESOLVED 2026-08-03 (CC, phase 1v) — the user ruled **option (C) with option (B)'s correction … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-298.md) |
| OI-299 | The open-items INDEX rows are paragraph-length narratives duplicating their detail files, and a correction reached one and not the other | `CLAUDE.md`'s register section says *"rows here are pointers, not restatements."* They are not: an INDEX row routinely runs to several hundred words and restates its detail file's narrative and evidence. **The measured cost:** the phase-1n correction to [[OI-288]] reached the DETAIL FILE only, and the INDEX row — the surface this register's own rule makes *"the authoritative status surface"* — carried the false citation through five further waves. Two copies of one narrative drift the moment either is corrected, and the index copy is what a session reads at session start. **The redesign:** an INDEX row carries status, subject and a pointer; the narrative lives once, in the detail file (the status of record stays in the INDEX — that is the split's whole point and is not what changes). **★ A PREMISE OF THE DISPATCH THAT ORDERED THIS ROW DOES NOT HOLD, and the row states it rather than carrying it:** the dispatch held that both halves disturb `tools/open_items_split_check.py`'s byte-verbatim guarantee; read at the tool, that guarantee is on the DETAIL files (`:184-188`, comparing `read_detail_row` against the pre-split row from `git show cb246a7580:OPEN_ITEMS.md`) and the INDEX is read for **IDs only** (`read_index_ids`, `:104-112`) — so shortening index rows disturbs it not at all, and the **split half** is the one that genuinely breaks the bijection. Two real constraints survive the correction: a detail file must hold exactly one row-shaped line (`:125-128`), and a shortened index row must still begin `\| OI-<n> \|`. NOT in doubt: the detail files keep each row as written, so #12 is satisfied by that copy whatever the index row becomes | doc-sync / #6 | OPEN — rowed 2026-08-03 (CC, phase 1u, Task 3) on the user's ruling AA3 (option 3D). **ONE ACT with the deferred resolved-row split** — both reshape the same surface and owe ONE establishment run, which is the corrected reason for pairing them, not that both break the same check. Scope: the index-row shape, the resolved-row split, the split check extended to cover both, and its establishment under **D-436** (measured detection against deliberately broken index rows, measured false positives on legitimate ones) before the reshaped index is trusted. An **apparatus row under D-438** — it blocks nothing; **the establishment run it ends in DOES gate**, per that declaration's own establishment clause | [detail](open_items/OI-299.md) |
| OI-300 | The shell-read guard owes a false-deny establishment run over five shapes its corpora do not contain | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | process / mechanisation debt (#19) | ✅ RESOLVED 2026-08-08 (CC, `cc_instruction_away_execution.md` Task 1) — **the owed run was performed, and all … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-300.md) |
| OI-301 | `tools/open_items_split_check.py --split-baseline` overwrites the frozen split reconciliation with a failing report, and nothing warns | The tool's LIVING mode is the standing register-health check and is valid any time; `--split-baseline` is the HISTORICAL reconciliation of the 2026-07-26 split against commit `cb246a7580`. Every item opened since the split is by construction an addition relative to that baseline, so the historical mode now ALWAYS fails — and it writes its failing report over `open_items/split_reconciliation.json`, the committed artifact `CLAUDE.md`'s open-items-register section names as the byte-level split reconciliation. Triggered here during a routine guard sweep: `OVERALL FAIL` over every post-baseline item, replacing the frozen PASS artifact, restored from HEAD in the same session. **Why it is more than a misuse:** the frozen record is the casualty and it exists to document that the split lost and added nothing (**#12**); nothing warns that the mode is meaningful only at the split commit, and nothing refuses to overwrite a passing artifact with a failing one; and the failure READS like the register has broken, so a session that committed it would leave a false alarm no later reader could distinguish from a real one. **The fix:** exclude post-baseline items by construction (living mode already computes them as `post_baseline_ids`) or refuse to run outside the split commit — and either way write only on a pass, or to a distinct path. NOT in doubt: LIVING mode, which passes — bijection holds, no detail file carries a status of its own, every original item byte-verbatim; and the split itself, which reconciled correctly when performed | process / audit tooling | OPEN — CC-found 2026-08-03 (phase 1u, Task 6) by triggering it, and caught by the standing self-check on the session's own changed-path list; the frozen artifact is restored and clean at HEAD. NOT fixed here (the dispatch licenses no tooling change beyond what its tasks build), and under **D-436** as amended the fix owes a measured detection rate — it must still catch a genuine lost or altered original row — before it is trusted. An **apparatus row under D-438**: the subject is a tracking-surface check's reporting behaviour, so it blocks nothing; the establishment run its fix ends in gates on its own account | [detail](open_items/OI-301.md) |
| OI-302 | The LEGACY mark has a THIRD meaning the register cannot express — the subject is dormant and the prohibition is LIVE | Found by the [[OI-289]] verification (CC, phase 1w, 2026-08-03; artifact `tools/audit/decisions/phase1w_legacy_verification.json`). The mark says what a decision is ABOUT. Measured over the whole marked set, that leaves three kinds, not two: about live code, about dead code, and **about dead code while a LIVE specification section still enforces the decision as a standing do-not-retry** (`ARCHITECTURE.md:306`, `:1344`, `:1420`; `docs/redesign_plan.md:4` says it in its own words — *"LIVE prohibitions about the legacy chord path"*). That third kind is the largest transfer class in the set and the register has no field for it, so a reader meeting the mark alone is told the subject is dormant and not told the rule still binds. **Three things ride on this row and none is a session's to settle.** (a) Does the register gain a field, or is the fifth-home-case pointer enough? (b) **D-423** says its three prohibitions hold *"for the whole programme"* while being LEGACY-marked, and one of the three is the rule **D-429**'s transfer already carries to the live design — so either the programme-wide claim is an unruled transfer or the wording overstates it. (c) The remaining undetermined transfers, each with its stated reason in the artifact — chiefly **D-061** (the never-widen rule is also a standing `CLAUDE.md` instruction), **D-068**, **D-321**/**D-322**, **D-325**/**D-326**, **D-344**, **D-358**, **D-380**. **Plus the one candidate wrong mark, reported and NOT corrected: D-055** — the twenty-one mode-prior preferences are registered unconditionally in the module configuration (production code) while their consumer is the legacy key scorer; whether the settings are themselves legacy-scoped is a scoping judgment, not a code fact | E — user adjudication / decisions register | OPEN — rowed 2026-08-03 (CC, phase 1w) as the residue of [[OI-289]]'s verification. **GATES, under D-438's own default.** Half (a) is apparatus — the register's field structure. Halves (b) and (c) are not: whether a prohibition binds the live design is a ruling about what the family design may do, and D-055's half is a factual question about production code. A row whose subject its own text does not settle GATES, and this one holds both kinds | [detail](open_items/OI-302.md) |
| OI-303 | Six comments in `src/` and `tools/` still describe the production record arm as "default OFF", or the record section adapter as having no caller | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_13.md` Task 2) — **all six claims verified … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-303.md) |
| OI-304 | Two dated annotations still assert the opposite of **D-428** as corrected one wave later | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Task 3) — **the act the row itself … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-304.md) |
| OI-305 | `gen_home_classification.py --check` reports STALE at the committed tree — four `home_section` fields do not re-derive and `phase1q_reclassification.json` does not reproduce | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer | ✅ RESOLVED 2026-08-08 (CC, `cc_instruction_away_execution.md` Task 0) on the user's **Ruling 1 of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-305.md) |
| OI-306 | A THIRD over-breadth class of `tools/audit/process_check.py`, measured and not yet in its published corpus | Found by running the check over the phase-1w dispatch (CC, 2026-08-03). Its "uncited code claim" rule fires on the words *reachable*, *dormant* and *unreachable* where the dispatch is **defining the vocabulary of a test** or **quoting the row it comes from**, not asserting anything about the code — and the facts that DO cite the code sit two lines above. This is the same shape as the two classes already published at `tools/audit/process_check_establishment.json` → `known_over_breadth_do_not_tune_it_away`: the check has no model of a QUOTED or DEFINED term, as it has none of a section reference. **It must not be closed by special-casing the three words** — that is tuning to a specimen, which that artifact forbids in terms and for the reason it gives. What is owed is the same treatment the second class got: the specimen enters the CONTROL corpus so the published false-positive rate RISES visibly and the class is a known property rather than a defect designed around | D — instrument / measurement layer | OPEN — rowed 2026-08-03 (CC, phase 1w). NOT executed here: extending a mechanism's corpus re-measures both of its published rates, which is a mechanism act under **D-436** as amended — report, do not execute. **GATES by default** — the non-gating declaration's derived set is generated from an over-inclusive first cut over each row's subject column, this row's subject is not in that vocabulary, and a hand-added verdict for a row the cut never proposed is exactly the hand-listing `CLAUDE.md` forbids. If it belongs in the apparatus class, the cut is what should reach it | [detail](open_items/OI-306.md) |
| OI-307 | The documented corpus regeneration produces LEGACY output into the PRODUCTION corpus directory, and the manifest cannot tell the two arms apart | Three facts, each read at the object (CC, phase 1x, 2026-08-03). (1) `--joint-inference` is opt-in: `jointInferenceDir` is initialised empty at `tools/batch_analyze.cpp:4917` and the joint path is entered only at `:5590` under `if (!jointInferenceDir.empty())`, so **without the flag `batch_analyze` runs the legacy pipeline**. (2) `BUILD_AND_TEST.md` §2 and `CLAUDE.md` gate block (C) both regenerate **without** the flag into `tools/corpus/<preset>` — the directory `tools/a8_rebaseline_measure.py` reads for the block-(A) hard stop (`:290-294`) — and `run_bach_preset.py` **clean-slates** its output directory first. (3) `corpus_manifest.json` records the binary, the fingerprints and `git_hash` (today `d615152c51`, the OI-178 adoption) and **no field naming the inference arm**, so `validate_corpus_dir` — which checks fingerprints against the manifest written beside them — validates a swapped-arm corpus cleanly. Together: following the documented command replaces the joint-estimator corpus with legacy output where the hard stop reads it, and nothing detects it. **The phase-1x dispatch's assumption A3 comes back PARTLY REFUTED:** block (C)'s flag omission is RIGHT (its `52/24/52` sets are legacy-pipeline figures), the DESTINATION is not. NOT in doubt: the corpus on disk today is the joint one | D — instrument / measurement layer | OPEN — **OPEN — the MANIFEST half is CLOSED 2026-08-03 (CC, phase 1y); the DIRECTORY half moves to [[OI-312]] and the row stays open until that is ruled.** The manifest is at schema 2 and carries `inference_arm` with both its sources; `validate_corpus_dir` gains `expect_arm` and refuses a mismatch; `a8_rebaseline_measure.py` declares `joint`; a mixed-arm directory is refused. The **D-436** obligation is discharged by measurement, both directions plus a negative control, at `tools/audit/corpus_arm_establishment.json`. **A1 is ESTABLISHED at the object and there is NO #13 STOP** — the arm was readable all along (`tools/batch_analyze.cpp:4695` stamps `"analysisPath": "joint"`, the standard writer at `:1448` stamps `"batch"`), all three gate corpora are unanimously joint, and 47 of 50 corpus directories are now back-stamped with that evidence (`tools/audit/corpus_arm_backstamp.json`). **One dispatch premise did NOT hold:** `.gitignore:26` ignores `/tools/corpus/`, so there is no *committed* corpus and no commit to cite — the manifest is the whole of its provenance. The transition is declared and bounded (#23), retirement condition at `characterise_bir_false.ARM_UNKNOWN_IS_FATAL`. **GATES** — the subject is an instrument a measurement depends on, which D-438's own test makes gating | [detail](open_items/OI-307.md) |
| OI-308 | `--joint-inference` defaults OFF while the joint estimator is the production inference layer — a candidate default flip, for phase 3 | Gate block (A) records that the joint estimator **IS** the production inference layer on the batch/corpus surface and measures every baseline through it; on the notation surface the equivalent switch was made by flipping a default (`useJointNotationRecord` ON since 2026-07-27). On the batch surface the default did not move, so the production layer is reached only by a caller who remembers a flag and the dormant pipeline is what a caller gets. **For:** it is the same act already ratified on the other surface; every omission [[OI-307]] found is an instance of it and correcting each site treats instances rather than the cause; a default that must be remembered will be forgotten and the failure is silent. **Against:** it is a behaviour change, which **D-231** forbids in phase 1; it is not one commit's worth of nothing — the default governs what `test_batch_analyze_regressions.py` pins and what the retired batch diagnostic measures, so flipping it makes the legacy figures unreachable without a second opt-out flag, a design decision riding on the first; and #14 makes a behaviour change a user-ratified revertible commit, which this is not | D — instrument / measurement layer | OPEN — rowed 2026-08-03 (CC, phase 1x, **Task 4.3, which names this as the behaviour change it refuses to make**). Deliberately NOT taken; the wave corrected the documentation instead. For **phase 3**, where it belongs beside the [[OI-180]] retirement map — deleting the legacy arm and flipping the default are two ways of reaching one end state and should be designed together, not in sequence by accident. **GATES** — its subject is what the analysis runs, not apparatus | [detail](open_items/OI-308.md) |
| OI-309 | Four generated artifacts in the decisions chain do not re-derive at the committed tree | Found by running EVERY guard with the visibility fix in place (CC, phase 1x, 2026-08-03), deriving the guard list from what exists rather than from a dispatch. `gen_phase1p_delegation_bar.py`, `gen_phase1n_reading_regime.py`, `gen_phase1m_measurements.py` and `gen_phase1g_triage.py` each report drift; complete output at `tools/audit/guard_state.json` → `runs`, nothing restated (**D-431**). **PRE-EXISTING, and how that was established:** none of the four reads a `.py` file's content or line numbers, and none reads `OPEN_ITEMS.md`, `open_items/` or `STATUS.md` as data — so neither this wave's edits nor phase 1w's uncommitted work can have moved them; the changed-path lists were taken through `tools/audit/changed_paths.py`. **WHICH wave introduced each is NOT established** — that needs a bisect this wave did not do, and unknown is not the same as recent. **Why unnoticed:** [[OI-305]]'s finding widened — the guard list lived in prose, one wave at a time, and prose notices neither an addition nor an omission; and until [[OI-297]]'s fix a check could exit non-zero having printed a clean summary and none of what it found. NOT in doubt: the register's own two guards PASS — the rendered register matches its data and every entry's quote is at its cited home | D — instrument / measurement layer | OPEN — rowed 2026-08-03 (CC, phase 1x, Task 2), which forbids fixing what it finds. Each needs the diagnosis [[OI-305]] asks for and does not answer: **stale** (regenerate) or **historical** (frozen, and regenerating destroys the finding — the [[OI-301]] hazard). `gen_phase1p_delegation_bar.py` is the clearest candidate for the second reading — its own docstring says it *"IS A PRE-APPLY RECORD, AND IT STAYS ONE"* and it reads frozen fields precisely so a re-run cannot overwrite the finding **D-432** cites. An artifact meant not to reproduce needs a mode that says so. **GATES** — a generated surface failing its own reproduce check is an establishment matter (#19) | [detail](open_items/OI-309.md) |
| OI-310 | Three ratified `CLAUDE.md` rules carry no authored mechanisation triage, and the check that says so was not being run | `tools/audit/claude_md_rule_triage.py --check` exits non-zero: **D-437, D-438 and D-439 have no authored triage** (CC, phase 1x, 2026-08-03; output at `tools/audit/guard_state.json` → `runs`). The tool classifies every `CLAUDE.md` rule MECHANISM-EXISTS / MECHANISABLE-AND-NOT / KNOWLEDGE and refuses a clean run while one is unclassified. **Which wave introduced it IS establishable here:** `STATUS.md` records phase 1o taking the register 436 → 438 (D-437, D-438) and phase 1v taking it 438 → 439, so the check has been failing since D-437 was entered and the table was extended in neither commit. That is the ordinary shape — the register's same-commit rule (c) covers the register's own files, and this triage is a separate authored table in a separate tool, so entering a decision prompts nobody to extend it. NOT in doubt: the three entries are ratified, the check behaves as designed, and nothing about the failure bears on any measurement | D — instrument / measurement layer | OPEN — rowed 2026-08-03 (CC, phase 1x, Task 2), which forbids fixing what it finds. Beyond that the verdicts are **authored judgments** about whether a rule can be mechanised at all, and all three are governance rules (the non-gating declaration, the phase-3 partition's authority, the channel enumeration) — exactly what the tool refuses to guess, and a session should not guess it either. Now in the derived population of `tools/audit/gen_guard_state.py`, so a wave that skips it shows it skipped rather than absent. **GATES by default, on the [[OI-306]] precedent** — the row reads like apparatus (a record of what could be mechanised, an input to nothing), but the non-gating set is DERIVED from an over-inclusive first cut over the subject column and that cut did not propose this row; a hand-added verdict for a row the cut never reached is the hand-listing `CLAUDE.md` forbids. If it belongs in the apparatus class, the cut is what should reach it | [detail](open_items/OI-310.md) |
| OI-311 | `gen_ratification_surface_set.py` has no verify-only mode, so it cannot be run as a guard without overwriting its own committed artifact | Found assembling the derived guard list (CC, phase 1x, 2026-08-03). Every other generator in the audit set carries a `--check`/`--verify` that re-derives, diffs and writes nothing; this one has only a write mode. **Why that makes it unrunnable rather than merely inconvenient:** the artifact's `census_scope.files_scanned` counts files in the tree, so **any wave that adds a file changes it by construction** — phase 1w recorded exactly that, one field moved — and running it here would have folded this session's new files into phase 1w's **uncommitted** record before that record was committed. Second instance of the [[OI-301]] shape: a mode whose only way of answering a question destroys the record it is asked about. Recorded as NOT RUN with this reason at `tools/audit/guard_state.json` → `not_run`. NOT in doubt: the generator itself is correct and its derivation established — phase 1w drove it and reconciled the one moved field against the committed object by hash | D — instrument / measurement layer | OPEN — rowed 2026-08-03 (CC, phase 1x, Task 2). The fix is the `--check` its siblings already have (`gen_phase3_gate_partition.py`, `gen_nongating_apparatus_rows.py`), plus **one judgment**: a census whose scope is "the tree" cannot be byte-stable across waves, so the file-count field is either excluded from the comparison and reported separately, or reported as expected movement rather than drift. That decision is why this is rowed and not done in passing. **GATES by default, on the [[OI-306]] precedent** — the row reads like apparatus (a tracking artifact's verification mode), but the derived first cut did not propose it and a hand-added verdict is the hand-listing `CLAUDE.md` forbids; if it belongs in the apparatus class the cut is what should reach it. Independently, the establishment its fix ends in gates on its own account | [detail](open_items/OI-311.md) |
| OI-312 | Gate block (C)'s documented procedure clean-slates the block-(A) hard stop's corpus with legacy output, and redirecting it is the user's ruling | The residue [[OI-307]] leaves behind once the manifest half is closed (CC, phase 1y, Task 4.3, 2026-08-03). `CLAUDE.md` gate block (C) gives four commands, read at `CLAUDE.md:635-640`: two `run_bach_preset.py --output-dir tools/corpus/{baroque,jazz}` regens with **no** `--joint-inference`, each followed by a `characterise_bir_false.py` measurement. **Its flag omission is RIGHT** — the `52/24/52` case-identity sets those commands reproduce are LEGACY-pipeline figures, and the joint arm would measure a different system. **Its DESTINATION is what is owed:** `run_bach_preset.py` clean-slates its `--output-dir` (`:374-378`, skipped only by `--resume`/`--skip-cpp`), and `tools/corpus/<preset>` is the directory `tools/a8_rebaseline_measure.py` reads for the block-(A) hard stop. So following a documented, historically-correct procedure destroys the production corpus. **What phase 1y changed and what it did not:** the consequence is now VISIBLE and REFUSED — the regen stamps `inference_arm: legacy` and the block-(A) instrument declares `joint`, so the swapped corpus is detected rather than measured — but the corpus is still destroyed, and re-creating it is a full regen. **The remedy is one word (`tools/corpus/batch_diag/<preset>`) and it is NOT a session's to make:** block (C) is a user-ratified surface, and changing a procedure it states is a ruling (D-249). NOT in doubt: the omission, the destination's identity, and that the diagnostic is retired-but-kept | D — instrument / measurement layer | OPEN — rowed 2026-08-03 (CC, phase 1y, Task 4.3), which names the redirect as the thing it refuses to do. `BUILD_AND_TEST.md` §2 carries the same two pairs and now warns at both sites without redirecting either. Cross-ref [[OI-307]] (the manifest half, closed here), [[OI-308]] (the flag's default, phase 3), [[OI-313]]. **GATES** — the subject is an instrument a measurement depends on, which D-438's own test makes gating | [detail](open_items/OI-312.md) |
| OI-313 | `hardening_battery.py` gate 5 regenerates on the legacy arm and byte-compares against the joint corpus, so it cannot pass | Found tracing the `--joint-inference` omission to every regeneration site (CC, phase 1y, Task 2.1, 2026-08-03). `tools/audit/hardening_battery.py:173-179` runs `run_bach_preset.py --preset <P> --output-dir <scratch>` with **no** `--joint-inference`, then compares each produced `.ours.json` sha256 against `tools/corpus/<preset>/`. The committed corpus is the JOINT arm (established at the object, `tools/audit/corpus_arm_backstamp.json`); the regen is the LEGACY arm; the two pipelines produce different output by design. Its pass condition — `total_bad == 0` — is therefore unreachable, and its failure would read as "batch_analyze.cpp has regressed" rather than "this gate is comparing two different systems". **Why it went unnoticed:** the gate is opt-in behind `--corpus-regen` and is described as expensive, so nothing has run it since the OI-178 adoption made the destination corpus joint-arm. **NOT the same defect as [[OI-307]]:** it writes to a scratch dir, so it destroys nothing — what is broken is the comparison, not the corpus. NOT in doubt: the other four gates in that battery, which do not regenerate | D — instrument / measurement layer | OPEN — rowed 2026-08-03 (CC, phase 1y). NOT fixed here: the dispatch's Task 2.1 says a measurement reading the corpus outside the validator is a finding to report, and the fix is a judgment — pass the flag (compare joint against joint) or keep the legacy regen and compare against a legacy reference, which are different questions about `batch_analyze.cpp`. Under **D-436** as amended the fix owes a measured detection either way. **GATES** — the subject is an instrument a measurement depends on | [detail](open_items/OI-313.md) |
| OI-314 | Four tools read the production corpus, or its manifest, without the contamination guard every other reader routes through | The second hole the phase-1y dispatch predicted at Task 2.1 and asked to be reported rather than fixed (CC, 2026-08-03). `characterise_bir_false.validate_corpus_dir` (`tools/characterise_bir_false.py:50`) is the one function that enforces the manifest, and the block-(A) instrument, the batch diagnostic, `analyze_inversion_errors`, `c1_reliability`, `calibration_fit`, `cc_decomp_measure`, `cc_eg2_probe`, `oracle_root_metric`, `mode_grading_adjudication_probe`, `gen_wir_alignment_probe`, `hardening_battery` and three throwaway round measures all call it. **Four do not.** Reading `.ours.json` CONTENT: `tools/audit/l4/pass1_satellites_firerate.py:36-38` globs every file in `tools/corpus/<preset>` and reads the manifest for its `git_hash` alone; `tools/classify_key_disagreement.py:509-514` opens `<preset>/<stem>.ours.json` directly. Copying the manifest's `git_hash` into a report as provenance without validating what it describes: `tools/measure_fanout.py:162-164`, `tools/measure_joint_probe.py:507-509`. **Why the second pair is not merely tidiness:** stamping a corpus hash into a report is a provenance claim about a directory the tool never checked, which is exactly the "recorded but unestablished" shape #19 exists for. NOT in doubt: the guard itself, whose own tests pass (`tools/tests/test_metric_scripts.py`), and the fact that all four now also miss the arm check added on 2026-08-03 | D — instrument / measurement layer | OPEN — rowed 2026-08-03 (CC, phase 1y, Task 2.1), whose accepted-outcomes section names this as a second hole to be rowed and not fixed here. The two content readers are audit-pass artifacts of named earlier arcs, so whether they are re-run at all is part of the fix decision. Cross-ref [[OI-129]], [[OI-124]] (the guard's own establishment history). **GATES** — an instrument a measurement depends on, and independently an establishment matter (#19) for the provenance half | [detail](open_items/OI-314.md) |
| OI-315 | The piece-start shortcut was removed from the code on 2026-06-14, is still specified in the present tense, is registered LIVE as **D-058**, and is contradicted twice inside one design document | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) | ✅ RESOLVED 2026-08-13 (CC, dispatch `cc_instruction_oi315_class_licence.md`, Tasks 1–3) — **BOTH HALVES' … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-315.md) |
| OI-316 | The reading regime’s registered predictions cannot survive their own update protocol — the generator refits the bands on the documents being graded | Found on the first attempt to follow the protocol (CC, READ WAVE 1, 2026-08-04). `phase1n_reading_regime.json` → `bands.registration` tells each read wave to write its actual yield into `actual_yield_when_read` on the owed row it read. That field is in a GENERATED artifact, so the only lawful writer is `gen_phase1n_reading_regime.py` — and read at the generator, recording a wave’s reads means adding it to `gen_phase1m_measurements.READ_WAVES`, after which the document leaves `owed_rows` for `read_rows`, **the bands are recomputed as terciles of the READ set that now contains it**, and the emitted `actual_yield_when_read` is `None` unconditionally. So following the protocol moves the document out of the row holding its prediction, refits the prediction on the data being graded (**#20**), and loses the registered value (**#12**). **Why it is more than bookkeeping:** the registration is the whole basis on which a tail could ever be bounded — `no_tail_is_bounded` says so — and the proxy is declared unvalidated (#17d) precisely because it is fitted and self-measured. A registration that silently refits each wave would leave a proxy that always looks calibrated and never was tested. **One measured fact bearing on the fix:** every registered band has a minimum of 0, so `inside_the_registered_band` is close to unfalsifiable — wave 1 passed it 6/6 while landing 4 of 6 BELOW the point prediction | D — instrument / measurement layer | OPEN — rowed 2026-08-04 (CC, READ WAVE 1, Task 3). The wave left `READ_WAVES` and the regime artifact **untouched** and recorded its reads and yields in `tools/audit/decisions/reads1_yield.json` (`gen_reads1_yield.py`), which reads each registered band off the regime artifact rather than recomputing it, and re-derives. So **the read count moves there and in [[OI-207]] and NOT in the regime artifact** — a divergence a later reader must not mistake for a stalled wave. Owed, and not a session’s to choose: freeze the bands at registration (the epoch pattern `gen_home_classification.py` and `gen_phase1p_delegation_bar.py` already use), or make the regime read-only with the grading beside it, or split partition from prediction. **GATES** — an instrument a measurement depends on, and independently an establishment matter (#19/#20) | [detail](open_items/OI-316.md) |
| OI-317 | `docs/key_path_design.md`’s banner says the document is UNCOMMITTED and under a commit hold; it is committed at HEAD and names no ratification addendum | Found reading it in full (CC, READ WAVE 1, 2026-08-04). The banner reads *“**DRAFT — UNCOMMITTED** … Ratification-gated: this document is **HELD** (`git add` permitted, `git commit` NOT) until a ratification addendum lands”*, and the closing line repeats it. The file is tracked and committed — established from the session-start working-tree status, which enumerates every untracked path and does not list it. No addendum is named anywhere, and its four §10 open questions carry recommendations and no answers. **Why it is worth a row:** the banner is a standing INSTRUCTION to a future session not to commit the file, and a stale standing rule is worse than none; and the same document’s self-contradiction about the piece-start shortcut ([[OI-315]]) shows it was left as its banner describes and then overtaken — the banner is the marker that would have said so, and it says the opposite. NOT in doubt: its substantive §3 finding, corroborated three independent ways already (**D-283**, **D-284**, **D-289**, and `docs/beam_widening_design.md` §3.3 from a per-cell path walk), which is why the document yielded no register entry | F — doc-sync debt (#10) | OPEN — **its FALSE STATEMENTS ARE STRUCK; the FILING DECISION this cell reserves is untaken, so the row does not close** (2026-08-14, CC, `cc_instruction_scoring_model_pass.md` Task 3, acting on [[OI-274]]'s banner half and not on this row, which has LAPSED and was owed nothing). The banner's "UNCOMMITTED" and its `git commit` NOT hold are struck, and so is the closing line that repeated them — correcting the banner alone would have left the falsity in the document. Trackedness established at the git object by explicit hash, not asserted; the former wordings stand in place (#12); "Awaiting ratification" is replaced by the fact behind it — no addendum is named in the document — and NOT by any claim that ratification has or has not happened. **What is untouched and is why the row stays open:** whether the document is retained as a superseded design record or re-bannered is a filing decision, reserved by this cell and not taken; the DRAFT marking stands; the §10 open questions still carry recommendations and no answers. *(The lapse under **D-676** is unaffected: this row still stays open, still gates nothing and is still not owed. What happened is that an act authorized by another row removed part of this one's subject.)* An **apparatus row under D-438** — the subject is a status banner, which that declaration’s own line names as apparatus; the substantive half of the same document’s staleness is [[OI-315]], which gates | [detail](open_items/OI-317.md) |
| OI-318 | Two label defects in the canonical architecture document: the Layer-6 section still calls the grouping unit a “phrase”, and §19.4 is used twice | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Task 3) — **both items corrected, and … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-318.md) |
| OI-319 | Every read wave now widens `gen_home_classification.py --check`’s stale report, because a new gap-homed entry cannot be given its `home_section` while [[OI-305]] is open | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer | ✅ RESOLVED 2026-08-09 (CC, `cc_instruction_return_continuation.md` Task 0) on the user's **Ruling 2 of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-319.md) |
| OI-320 | `docs/score_inventory.md` — the document `CLAUDE.md` sends every score-touching task to FIRST — states the SUPERSEDED batch case-identity gate as the current gate, three times, and tells the reader the granularity-robust metric does not exist yet | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Task 2) on the user's **Ruling 62 of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-320.md) |
| OI-321 | `docs/unified_analysis_pipeline.md` states three things as current that are false at HEAD, including a phase it calls deferred that the code says was executed | Found reading it in full (CC, READ WAVE 2, 2026-08-04). **(1)** Its header says *"Phase 4c (`analyzeSection` move to composing module) deferred — gated on consumer need"*; `src/notation/internal/notationcomposingbridgehelpers.cpp:29-33` says in its own comment *"Stage 2.1 (Phase 4c) moved the section-level ANALYSIS layer out of this file and into the composing module"*, and `src/composing/analysis/section/sectionanalyzer.h:26-30` says the same from the other side. The move HAPPENED. **(2)** The architecture section gives the pipeline's location as *"`src/composing/analysis/pipeline/` (new directory)"*; there is no such directory at HEAD — the entry point lives in `src/composing/analysis/section/`. **(3)** Divergence C is described in the present tense as *"annotation (P2) applies `minimumDisplayDurationBeats = 0.5` … implode (P1) and tick-regional (P3) do not"*; at HEAD the implode path applies it — `src/notation/internal/notationimplodebridge.cpp:847-849` reads the preference and `:869` computes the test, which gates the chord symbol (`:1167`), the function notation (`:1185`) and the non-diatonic marker (`:1231`). The document's own header calls the refactor structurally complete, so a reader takes its body as as-built | F — doc-sync debt (#10) | OPEN — rowed 2026-08-04 (CC, READ WAVE 2). **★ CORRECTED IN PLACE 2026-08-13 (CC, `cc_instruction_oi315_licence_and_cells.md` Task 2), AFTER THE CANONICAL OPENING WHERE RULE (f) MAKES IT INERT, SO NO STATE MOVED — ITEMS (1) AND (2) *ARE* CORRECTED AT HEAD:** the header now says the phase was **EXECUTED in Stage 2.1**, and the architecture section's location sentence names the directory the code actually has, each with its former wording preserved beside it (#12) and the establishment carried in one correction block rather than repeated (#6). Landed at commit `cfa3775a2a` (`cc_instruction_false_statements_pass.md`), read here as a git object by explicit hash and then at the document itself. **FORMER WORDING, PRESERVED (#12):** *"NOT corrected."* — true when written on 2026-08-04 and false at HEAD from that commit onward, so a session choosing what to work next read this cell and was told nothing had been done. **The row stays OPEN on item (3) alone, which is untouched and is a user ruling** — the document now says so where a reader meets the two corrections. Item (3) additionally raises a question this row does not answer and a session may not: the header records divergence C as **PARKED** and **D-471** (entered by this wave) is its undischarged decision rule — so whether the implode path applying the gate is the rule being discharged, or a divergence that closed by drift, is a matter for whoever rules on C. **GATES** under D-438, the subject being statements about the analysis's build state. **★ THIS ROW WAS BEING DROPPED BY EVERY DERIVATION OVER THE INDEX UNTIL 2026-08-09** — it carried SEVEN cells, the layer/gate column written twice with the gating clause in the second, so the ONE index parser skipped it silently and it was in no population at all ([[OI-362]]). The duplicate column is merged here, its whole text preserved in this cell (#12), by the normalization the user's **Ruling 33** ordered; the row's own state is unchanged and it is OPEN, which is what its text always said | [detail](open_items/OI-321.md) |
| OI-322 | `docs/symbol_input_audit.md` describes throughout, in the present tense, code its own closing line says was deleted — a reader who stops before the last line is told the opposite of HEAD | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Task 2) on the user's **Ruling 62 of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-322.md) |
| OI-323 | The shell read guard let a forbidden working-tree read THROUGH — a false NEGATIVE, the opposite direction from [[OI-300]]'s four false-deny shapes and the more serious one | Found by the standing self-check on this session's own conduct (CC, READ WAVE 2, 2026-08-04). Two commands, minutes apart, both `grep -n` over paths under `src/`: the first — `grep -n "changePointSlices\|KeyModeSequenceDecoder" src/composing/analysis/region/regionanalyzer.cpp` — **ran**, and returned the file's content; the second — the same tool with a pattern carrying no escape and **three** path arguments — was **DENIED** with the correct message. Both are the read **D-253** forbids (working-tree content goes through the file tools; shell access is for git object queries by explicit hash), and the session should not have issued either; that half is recorded in this wave's report as its own conduct finding. What is rowed here is the guard: it denied one and not the other, so its detection rate on the shape it exists to catch is **not 100 %** and is unmeasured. Which difference decided it — the escaped alternation `\|` in the pattern, the single-versus-multiple path arguments, or something else — is **NOT ESTABLISHED** here and is deliberately not guessed | D — instrument / measurement layer | OPEN — rowed 2026-08-04 (CC, READ WAVE 2). NOT fixed, and the freeze is not the only reason: a change to a guard that makes it deny MORE owes a measured false-deny rate exactly as [[OI-300]]'s pending change owes a measured detection rate, and both are the same establishment run. **Fold into [[OI-300]]'s establishment run rather than running a second one** — that row's corpora already exist and this is the missing half of them (they measure false denies; this measures misses). **GATES — an establishment obligation (#19), which D-438's own starred clause makes gating whatever its subject** | [detail](open_items/OI-323.md) |
| OI-324 | **D-057**'s binding scope is now unstated: the priority-of-evidence table's home section was scoped to the LEGACY key path, while the open item that rests on it is about the PRODUCTION emission | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) / E — user adjudication | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Task 0) on the user's **Ruling 63 of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-324.md) |
| OI-325 | **D-501 conformance is unchecked at HEAD: does anything read a written chord symbol as input that influences what the analyzer computes?** | Rowed 2026-08-04 (CC, READ WAVE 3, dispatch `cc_instruction_reads_3.md` Task 5, on the user's ruling R5) at the ratification of **D-501** — the user's operating principle that a chord symbol printed in a score is an instruction the user wrote, never a result: production paths may not read one as input at all, and a measurement tool may read one **only** as a comparison or ground-truth label. **The decision is registered; whether the code obeys it is not** — that split is `DECISIONS.md`'s own, and this is the row the register points at. **What is known and does NOT answer it:** **D-066** and **D-305** carry the production ban and **D-067** records the category-C resolution — the tool-side symbol paths were deleted — and [[OI-322]] establishes at HEAD that the five symbols that audit named return no match anywhere in the tree. That is an audit of the sites ONE 2026-era audit enumerated, at the names it happened to use; it is not a sweep of the question, and D-501's tool clause (comparison label yes, input no) has never been checked as a class. **Why it gates rather than sitting with the apparatus rows:** under **D-438**'s own test the subject bears on the analysis's INPUTS — the strongest form the test has — so it gates. A symbol read as input makes the analysis agree with the user rather than with the music, and inside a measurement tool it destroys the measurement outright, because the tool would be comparing the annotation with itself; that is a #19/#20 defect, not a tidiness one. **NOT in doubt:** D-501 itself, and the deletions [[OI-322]] verified | conformance check over the analysis's inputs — belongs to **phase 2**'s issue-finding program (D-231) | OPEN — rowed 2026-08-04 (CC, READ WAVE 3). **ROWED, NOT RUN** — the dispatch says so in terms, and the reason is the sequencing: the check belongs to phase 2, whose searches must each report their detection power, and a search run here would enter the coverage claim without one. What the check owes when it runs: a stated search space (production `src/` and every tool that reads a score), a stated criterion for "influences what the analyzer computes" that separates an input from a label, and its own miss rate. **GATES** under D-438 — the subject bears on the analysis's inputs | [detail](open_items/OI-325.md) |
| OI-326 | Whether `ARCHITECTURE.md`'s doc-governance hierarchy clause is itself a DELEGATION under **D-432** is unsettled — and it decides a whole CLASS of per-component design documents at once | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | E — user adjudication / register criteria | ✅ RESOLVED — **RESOLVED 2026-08-04 (CC, READ WAVE 4) — the user RULED, and the ruling was MEASURED BEFORE IT … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-326.md) |
| OI-327 | ✅ **RESOLVED** — all five drafted delegations are disposed: four written into `ARCHITECTURE.md`, one ruled NOT a delegation target | Rowed 2026-08-04 (CC, READ WAVE 4) on the user's ruling **R2** at [[OI-326]]: anything R1 leaves out is decided through the OI-293 write list, a delegation the user writes settling a document without touching the bar. The five were added to the list where it lives (`tools/audit/decisions/backbone_decisions.json` → `section_home_criterion.write_list`), each with a drafted delegation in the wording `ARCHITECTURE.md` already uses. **Why a row of its own rather than a re-opening:** [[OI-293]] is RESOLVED for its own population — the six documents phase 1q reported losing entries, all six of whose delegations the user wrote on 2026-08-03 — and a resolved row silently acquiring new open work is the status contradiction this register exists against. **The five:** `cowork_layer1_note_model_design.md`, `cowork_layer2_slicing_design.md`, `cowork_layer6_grouping_design.md` and `cowork_layer1_tone_collection_design.md` (the glob `cowork_layer*_design.md` matches each and, under **D-546**, confers nothing — two are named in no ratified surface at all, one by a bare citation inside a *"See …"* list, one by the *"Full spec:"* shape that is the bar's own worked example of the excluded form); and `cowork_phrase_boundary_design.md` (the PROSE-REFERENCE case, which R1's words reach in neither direction). **★ The fourth arrived INSIDE the ruling's own wave** — read wave 4 made the tone-collection design a home in the same commit that applied R1, so the R5 measurement run before the ruling did not carry it and the glob-matched home count moved by one during the wave; recorded as this wave's own effect rather than absorbed. **Three questions the drafts state and do not answer, each the user's:** the Layer-6 document is a contract for a layer **D-266** currently PROHIBITS, and nothing about that settles it mechanically (**D-433** — a shelved section can be a home); the phrase-boundary reference has more than one candidate document, with no `ARCHITECTURE.md` section owning the primitive it names, so siting the clause is an architectural act rather than a wording repair; and the tone-collection design is a SIGN-OFF surface with its checkboxes unticked whose role the note model absorbed, so whether it is meant to be a home at all is one of the things a delegation would answer. NOT in doubt: the ruling itself, that nothing moved on it, and that no delegation was written into `ARCHITECTURE.md` (rule (g)'s guard) | E — user adjudication / decisions register | OPEN, NARROWED FIVE → TWO — rowed 2026-08-04 (CC, READ WAVE 4); **the user RULED on all five 2026-08-04 (READ WAVE 5, ruling R4) and THREE ARE WRITTEN INTO `ARCHITECTURE.md`**: `cowork_layer1_note_model_design.md` (Layer-1 section, D-517…D-520 — the measured asymmetry with its three delegated siblings is closed), `cowork_layer2_slicing_design.md` (Layer-2 section, D-540…D-541, supplementing the *"See …"* citation rather than replacing it) and `cowork_phrase_boundary_design.md` (Layer-1 section, D-476…D-485). **The phrase-boundary write closed BOTH questions this row carried, exactly as the row predicted** — naming the file by filename settles which document, and the siting is stated inside the clause: `ARCHITECTURE.md` defines L1.5 as a derived view over the L1 note model and carries no section of its own for it, while the only other place the primitive is named is the Layer-6 section, which consumes rather than owns it and which **D-266** prohibits. **THE OTHER TWO ARE DELIBERATELY NOT WRITTEN, which is the ruling's intended outcome and not an incomplete execution of it**: `cowork_layer6_grouping_design.md` (would be a contract home for a layer D-266 PROHIBITS, and **D-433** means shelving settles nothing mechanically) and `cowork_layer1_tone_collection_design.md` (a sign-off surface with unticked checkboxes whose role the note model absorbed, so whether it should be a contract home AT ALL is one of the things a delegation would decide). Their nine and two entries stay `gap`; each write-list row carries its `disposition_2026_08_04`. **Nothing was promoted:** no entry's home class moved, `gen_home_classification.py`'s apply mode stays UNRUN under [[OI-305]]/[[OI-319]], and **D-432**/**D-430**/**D-546** are untouched — a delegation the user writes settles a document without touching the bar. **★ RESOLVED 2026-08-04 (CC, READ WAVE 6) — THE USER RULED ON BOTH WITHHELD DOCUMENTS, AND THE TWO ANSWERS ARE DIFFERENT KINDS, WHICH IS WHY THE ROW CLOSES RATHER THAN NARROWING AGAIN.** **`cowork_layer6_grouping_design.md` — DELEGATION WRITTEN (ruling R5).** The clause is in `ARCHITECTURE.md`'s Layer-6 section, beside the *"Full spec:"* line it supplements, with the entry range D-454…D-462 and the AS-BUILT (2026-07-02) banner state taken from the document's own line 3. The question the row carried is ANSWERED and its ground is stated inside the clause: **D-611** establishes the dormant path and its staged scaffolding as **deferred-engagement, not dead code** — their wire-or-remove verdict belongs to a named later step and removing them is a STOP — so deferred work is a state the record keeps decisions for, and a contract home for it is the same posture rather than a new one; **D-266**'s prohibition on BUILDING the layer is untouched and the clause says so in terms. **`cowork_layer1_tone_collection_design.md` — NOT DELEGATED, RULED (ruling R6), not deferred again.** The concern the document designs — collecting the tones a stretch of music sounds — was **ABSORBED BY THE NOTE MODEL**, so a delegation would create a SECOND home for a concern that already has one, which **#6** forbids; the answer is therefore decided by the principles rather than open. What was recorded instead: the absorption and the document's HISTORICAL status, on the document's own banner, and the same disposition in the write-list row. Its two entries stay `gap`, which is the correct class for a decision whose concern is homed elsewhere and whose recording surface is historical. **The delegation-bar grades moved with the acts and are recorded, not absorbed:** `cowork_layer6_grouping_design.md` moves BARE_CITATION → CLAUSE with its former grade preserved (#12), anchored on the delegation's own SENTENCE rather than the bare filename — the [[OI-330]] lesson applied at the write instead of after it — and the tone-collection row now records NOT_NAMED as a RULING rather than as an unanswered question. **An apparatus row under D-438** — the subject is where a decision is recorded and which surface delegates to which document, the register's own filing apparatus; it bears on no analysis, input or measurement tool, carries no establishment obligation, and gates nothing | [detail](open_items/OI-327.md) |
| OI-328 | The reading regime's ORDERING KEY is contaminated by the project's own recording acts — writing a delegation moves a proxy the schedule is fitted on, and re-running the generator now FLIPS the key | Found by a freeze check written for the [[OI-327]] re-pack, firing on its first run (CC, READ WAVE 5, 2026-08-04); measured, not inferred, at `tools/audit/decisions/reads5_repack.json` → `why_the_regime_artifact_is_read_and_not_regenerated`, with no figure restated here (**D-431**). The regime orders the 66 owed documents by **whichever of four candidate proxies correlates best with yield on the 36 already-read documents**, and one of the four is **`named_in_a_user_ratified_surface_count`** — how many lines of `ARCHITECTURE.md`, `CLAUDE.md` and `cowork_engage_arc_plan.md` name the document. **Writing a delegation into one of those surfaces increments that count.** The user wrote six on 2026-08-03 ([[OI-293]]) and three more on 2026-08-04 ([[OI-327]]), moving the naming count of three READ documents — the very documents the correlation is fitted on. Recomputed today, that proxy **overtakes document length and the ordering key flips**, so a re-run would re-order every owed document and every wave with them. **Why it is a defect and not a curiosity:** the register's own homing work EDITS A FEATURE THE PROXY IS GRADED ON, for documents whose outcome is already known — the fit/evaluation separation **#20** forbids, arriving by a route nobody designed, with nothing in the regime warning that one of its proxies is writable by the project. **And the margin is inside the uncertainty the regime artifact itself records**, so under **#24** the flip is not even a claim that one proxy is better — it is a coin landing the other way, and the ordering key is read off that coin. The regime's own `ordering_decision` already says the top two are indistinguishable at 95 % and that length wins by a **stated tie-break, not by its correlation**; this row is what that sentence costs on a re-run | D — instrument / measurement layer (the reading programme's own apparatus) | OPEN — rowed 2026-08-04 (CC, READ WAVE 5). **NOT fixed, and the freeze is not the only reason:** the remedy is [[OI-316]]'s and is not a session's to choose. **What this wave DID do:** the regime artifact was **not regenerated** — ruling R5's re-pack is applied to the **registered rows read verbatim** by `tools/audit/decisions/gen_reads5_repack.py`, which imports the packing rule from the regime generator rather than copying it (#6), and recomputes no band, point, tercile cut or reading order; and the regime generator now **STOPS loudly** rather than silently re-deriving, its freeze check comparing each completed wave against the documents that wave's own yield artifact records reading. `gen_phase1n_reading_regime.py --check` **was already one of the six pre-existing guard failures** before this wave, so the verdict is unmoved — what changed is that the failure now says why. **This is a SECOND, SHARPER INSTANCE of [[OI-316]]**: that row found the update PROTOCOL could not be followed without refitting; this finds that **no re-run at all is safe**, for a different cause — ordinary correct work passing over the proxy's own inputs. OI-316's three candidate remedies are unchanged and each would fix this too. **GATES** — an instrument a measurement depends on, and independently an establishment matter (#19/#20), the same grounds [[OI-316]] gates on | [detail](open_items/OI-328.md) |
| OI-329 | ✅ **RESOLVED** — D-172 survives as principle #8's entry; D-557 is superseded into it with its former home and text preserved | Created 2026-08-04 by this wave's own act (CC, READ WAVE 5, Task 2) and found by the standing self-check. The user ruled (R2) that the build-it-right-before-tune-precision rule is homed at principle #8 in the fuller form **D-557** carries, with `cowork_l1l3_stabilization_plan.md` pointing at it and not restating it. What the ruling did not have in view, and the dispatch's premise ledger did not carry, is that **principle #8 already had a register entry — D-172** — whose verbatim was that principle. After the act both entries are live, both are homed at `CLAUDE.md:23-26`, and both carry the same re-taken verbatim: the duplication **#6** forbids, inside the register itself. **NOT in doubt:** the rule, its width and its home — principle #8 states it, the plan points at it, both entries verify at the cited lines, and no reader is misled about the RULE. The defect is that the register holds two rows where it should hold one, so a conformance pass counting entries counts this rule twice | E — user adjudication / decisions register | OPEN — rowed 2026-08-04 (CC, READ WAVE 5). **NOT resolved here, for two reasons each sufficient.** (1) **The statuses were ratified in this same act:** ruling R1 ratified D-547…D-583 as drafted *keeping the status the record states* — D-557 among them, live — and changing a status the user has just ratified, in the same commit, without a ruling, is what a session may not do. (2) **Which entry survives is a filing decision with more than one defensible answer, and it is the user's**: D-557 becomes `superseded-by` D-172 (the register's own vocabulary, accurate on its face, but supersedes an entry ratified live a moment earlier); or D-172 becomes the pointer and D-557 the entry (D-557 carries the richer provenance — the 2026-06-25 user ratification and the plan's own defense — while D-172's `rationale` reads *derivation not recorded*); or the two are merged carrying both provenances. **★ RESOLVED 2026-08-04 (CC, READ WAVE 6) — the user ruled (R3): D-172 SURVIVES as principle #8's entry and D-557 is recorded `superseded-by D-172`, its former home (`cowork_l1l3_stabilization_plan.md:14-22`) and its former verbatim preserved in its own provenance (#12).** The ruling also required the thing the register would otherwise have got wrong, and it is done: **the 2026-06-25 user-ratification of the fuller three-clause form is carried into D-172's provenance**, verified at this entry's own home rather than taken from the dispatch — `CLAUDE.md:30-32` states that the plan *"has carried the fuller three-clause form — refactoring + architectural design + algorithmic completion — user-ratified 2026-06-25"*. Without it the record would have read as if the widened rule were decided on 2026-08-04, five weeks after it was ratified. D-172's `date` field stays *not stated*, because it dates THE PRINCIPLE, which `CLAUDE.md` records as part of the user's undated standing list 1-11; the 2026-06-25 ratification is of the widened WIDTH and is recorded where the record can carry it without asserting a date the principle does not have. **The supersession is a FILING act between two entries of one rule, not a supersession of the rule by a later ruling** — D-557's own provenance says so in terms, and the rule is in force at D-172 at the full three-clause width. `CLAUDE.md` #8 now records the resolution beside the widening that caused it | [detail](open_items/OI-329.md) |
| OI-330 | Writing the Layer-2 delegation made a wave-4 measurement tool's anchor AMBIGUOUS — `gen_reads4_oi326_application.py --check` fails, and neither available repair is admissible | Found at this wave's guard run and caused by its own act (CC, READ WAVE 5, 2026-08-04). The tool locates each quote it cites by a **unique anchor string**, so the artifact carries the file's own current line rather than a transcribed number. One anchor is the bare filename `` `cowork_layer2_slicing_design.md` ``, which until this wave appeared ONCE in `ARCHITECTURE.md` — inside the *"See …"* citation the wave-4 measurement graded. Ruling **R4** then had this session write the Layer-2 delegation into the same section, naming the same file again, so the anchor now matches two lines and the tool stops. **This is the ONE guard failure this wave added to the six standing ones.** **Why neither repair was taken:** re-pointing the anchor would edit a HISTORICAL measurement's tool to cite a line that did not exist when the measurement was made; and re-deriving the artifact would restate wave 4's measurement against a population this wave's three delegation writes CHANGED — the [[OI-301]]/[[OI-305]] shape, a historical artifact destroyed by the regeneration meant to maintain it. The freeze independently forbids repairing a tool that does not block a read. **NOT affected:** the artifact on disk stands as the record of what was measured before the ruling was applied; the grade this wave's write produces is separately recorded and current in `gen_phase1p_delegation_bar.py`'s FORMS table, with the former grade preserved and its `--check` passing; nothing about the ruling, the bar or the three delegations is in doubt. **The general lesson, stated so it outlives the row:** a tool that anchors on a BARE FILENAME is fragile against the project's own correct work — every delegation the user writes names a file some tool may already anchor on — and the durable form is to anchor on the SENTENCE, which is what the delegation-bar tool's anchor for this document was moved to and what `gen_live_prohibition_pointers.py` argues for in its own docstring. Changing the anchoring convention is not a session's to do | D — instrument / measurement layer | OPEN — rowed 2026-08-04 (CC, READ WAVE 5). **NOT fixed**, per the freeze and per the two inadmissible repairs above. **GATES** — the subject is whether a recorded measurement can still be reproduced, which is reproducibility (#16) and establishment (#19); D-438's own starred clause makes an establishment obligation gate whatever its subject | [detail](open_items/OI-330.md) |
| OI-331 | **A LIVE, USER-RATIFIED entry names a convergence proxy the build MEASURED FALSE and dropped** — **D-261**'s worked example versus **D-622** | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) / E — user adjudication | ✅ **RESOLVED 2026-08-07 (CC), by the user's ruling R3** — dispatch `cc_instruction_five_rulings.md` §0a, … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-331.md) |
| OI-332 | Three of read wave 6's documents misdescribe their OWN state — a *"no code"* banner over a built design, stale as-built code anchors, and a design whose approach was later falsified with no note saying so | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Tasks 2 and 3) — **all three items … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-332.md) |
| OI-333 | **The cluster-disposition layer CANNOT BE REGENERATED at HEAD** — six register patterns are invalid regular expressions, and the guard that watches that tool is structurally incapable of seeing it | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer | ✅ **RESOLVED 2026-08-07 (CC), by the user's ruling R4** — dispatch `cc_instruction_five_rulings.md` §0a: one … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-333.md) |
| OI-334 | **The decisions register's own published account of itself is FALSE at HEAD in three places** — and one of the three is a figure its own computed table contradicts | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) | ✅ **RESOLVED 2026-08-04 (CC), on the user's ruling R3** — dispatch … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-334.md) |
| OI-335 | **A count of OUTSTANDING work was the MEMBERSHIP of a list of asks** — inside the artifact a phase-1 completion statement would rest on; and the per-row state was an authored field a later wave superseded by appending a second one beside it | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer | ✅ **RESOLVED 2026-08-04 (CC), same wave** — and the remedy deliberately is **neither** of the two the … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-335.md) |
| OI-336 | **Ruling R1 moves [[OI-332]]'s own gate verdict** — the row classed itself apparatus on ONE HALF of D-438's line | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | E — user adjudication / register criteria | ✅ **RESOLVED 2026-08-07 (CC), by the user's ruling R5** — dispatch `cc_instruction_five_rulings.md` §0a, … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-336.md) |
| OI-337 | **The uncommitted backlog is SIX waves, not the three ruling R4 names** — and the waves cannot be committed separately | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) | ✅ **RESOLVED 2026-08-04 (CC), on the user's ruling R1** — dispatch … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-337.md) |
| OI-338 | **The phase-1 completion inventory publishes as an OPEN QUESTION one the user has since ruled** — its derived half moved with HEAD and its authored half did not, so `--check` passes over a self-contradiction | Found re-reading D-231's clause at the file as the finish-line dispatch ordered (CC, 2026-08-04, `cc_instruction_commit_and_finish_line.md` Task 2.1, whose fourth bullet says in terms *"read the clause again rather than reusing the earlier reading of it"* — this is what that instruction was for). `tools/audit/phase1_completion_inventory.json` carries `the_question_the_two_halves_do_not_settle`, whose `who_settles_it` reads *"the user. It is reported, not decided."*, and an authored criterion C5 saying *"The question is REPORTED, not settled"*. **The user ruled it the next wave, as D-639** — the doc-sync half reaches a document's account of itself only where that account changes how the document's analysis content is read, with three worked examples and an explicit fallback. **The two halves of the same file now disagree:** its clause quote is DERIVED and therefore contains D-639's text at HEAD, while its criteria are an authored Python constant that did not move. So `gen_phase1_completion_inventory.py --check` PASSES — the artifact does match a fresh derivation — while the artifact contradicts itself internally. This is the [[OI-332]]/[[OI-334]] shape a third time: a computed half that is current beside an authored half that is not, in a file whose `--check` cannot tell them apart. **NOT in doubt:** every population, identifier and count the inventory derives — those all move with HEAD and re-derive byte-identically; and the corrected reading is on the record, carried at `tools/audit/phase1_finish_line.json` → `the_re_reading_of_the_clause`, which is where the dispatch ordered it written | F — doc-sync debt (#10) | OPEN — rowed 2026-08-04 (CC). **NOT fixed**, and not merely because the dispatch's header forbids fixing what a session finds: the artifact is also the RECORD of the wave that produced it, so overwriting its authored reading in place is a #12 question rather than a typo, and the same authored-constant hazard would remain for the next ruling. **Rowed and left under D-641**, homed the same day — the subject is an authored prose field inside an audit artifact; it bears on no analysis, no analysis input, and no instrument any measurement of the analysis depends on, and it carries no establishment obligation (#19), so it is neither surfaced nor given a wave. The verdict is recorded here as the row's own reading; the non-gating set itself is DERIVED and never hand-listed (`tools/audit/nongating_apparatus_rows.json`) | [detail](open_items/OI-338.md) |
| OI-339 | **A guard failed once and passed on every later run with no intervening write** — cause not established, and the record does not name the guard | Observed at the census-delegation wave (CC, 2026-08-04) and rowed at the finish-line wave on the user's ruling **R2**. `STATUS.md`'s own census entry states it: *"a guard failed once and passed on every later run with no intervening write, and its `--check` path depends only on two files nothing in the guard list writes — cause NOT established, and no fix attempted under the freeze."* The inputs could not have changed between the failing run and the passing ones by any act inside that wave, which is what makes the transition unexplained rather than merely unnoticed. **★ THE ROW CANNOT NAME THE GUARD AND SAYS SO RATHER THAN FILLING IT IN:** the observation survives only in that one sentence — `tools/audit/guard_state.json` records each guard's FINAL verdict for the run that wrote it, so a guard that failed transiently and then passed leaves no trace in it, and a repository-wide search for the observation's own phrasing returns that sentence and nothing else. Identifying the guard is therefore the FIRST act of whoever works this row, and it is not attempted here: a wrongly-named guard reads as established and sends the next session to the wrong tool. **One partially-matching candidate is recorded at low confidence (#12) and explicitly is NOT an identification** — `tools/audit/guard_armed_check.py` reads settings files nothing in the guard list writes, but its own output records **three**, where the record says two. **Why it is kept although it currently passes:** a guard whose verdict moves without its inputs moving is a guard whose PASS carries less than a reader takes it to carry — the [[OI-333]] shape from a different and, unlike that one, *unexplained* cause. **NOT in doubt:** no analysis behaviour, no measurement, no register entry, no status; the guard has passed at every run since. **★ This row also records a false statement in the preceding dispatch:** `cc_instruction_commit_and_finish_line.md` Task 4 lists *"the intermittent guard"* among items it calls *"rowed, left, and not surfaced"* — **no row existed**; it was left and not surfaced, but never rowed, so it was carried as tracked when nothing tracked it | D — instrument / measurement layer (the audit's own guard apparatus) | OPEN — rowed 2026-08-04 (CC), and **rowed is all that is done to it**. **Apparatus under D-641** on the user's ruling R2, taken with the row: the subject is the audit's own guard apparatus and bears on no analysis, no analysis input and no instrument any measurement of the analysis depends on — so it is **rowed and left, with no wave, no dispatch and no surface**. **★ THAT IS D-641's TEST AND NOT D-438's, AND THE TWO DO NOT AGREE HERE — STATED RATHER THAN RECONCILED.** D-641 decides what a finding is OWED; D-438 decides what a STAGE WAITS ON; **D-639 says in terms they are different tests with different subjects and neither overrides the other.** Under D-438 this row **GATES BY THE DECLARATION'S OWN DEFAULT**: its subject column is the instrument/measurement layer, which the over-inclusive first cut does not propose as an apparatus candidate, so `tools/audit/gen_nongating_apparatus_rows.py` never reaches it — verified by running it, which produced no STOP for a missing verdict. **No gating verdict is hand-added here**, which `CLAUDE.md` forbids in terms and [[OI-319]]/[[OI-336]] record the reason for; moving it is the user's. **The #19 question was put rather than assumed:** D-641's exception is that an establishment obligation always gates whatever its subject, and the user ruled this apparatus with that clause in force and in the same act that restated it, so the D-641 classification is the ruling's and not this row's inference from it | [detail](open_items/OI-339.md) |
| OI-340 | ★ **Whether criterion C1 reaches SUPERSEDED register entries is UNRULED** — the per-kind scheme said to settle it does not carry that kind, and read at its own text it routes those entries the OPPOSITE way | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync / documentation gap | ✅ **RESOLVED 2026-08-04 (CC), by the user's ruling** — dispatch `cc_instruction_c1_ruling_and_item1c.md` §0a, … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-340.md) |
| OI-341 | **A guard dates its line number as an observation and then requires it to equal today's** — so it reports its whole class stale on any insertion above the sections it points at | Found reading the tool in full to classify it under ruling **R4** (CC, 2026-08-04, `cc_instruction_finish_line_item1b.md` Task 2.1). `tools/audit/decisions/gen_live_prohibition_pointers.py`'s own docstring states the design and its reason — a line number in prose is not a register anchor, `reaim_home_anchors.py` maintains the `home` anchors and **nothing maintains this one**, so the pointer carries the section's own words and *"dates the line number as an observation rather than asserting it as current."* **Its `--check` does not honour that:** it re-derives the whole backbone serialization, freshly locating each section's line, and compares the file as a whole — so the dated observation is in fact required to equal today's. Measured at the object: the tool reports its three sections at `ARCHITECTURE.md:1495`, `:1389`, `:306` and then *"13 entry(ies) do not carry the pointer as derived"* — **all thirteen at once**, the signature of a whole-file re-serialization difference rather than thirteen defects. The insertions that moved those sections are the preceding wave's homing acts. **NOT in doubt: its classification** — under R4 it RE-DERIVES A LIVE INVARIANT (class membership derived from `phase1w_legacy_verification.json`, each section located by its own quoted words) and **stays in the live guard list**, re-checked per tool and confirmed. **In doubt:** whether the remedy is to re-run the apply mode (a register write) or to stop comparing the number at all so the docstring's design is what the check enforces — the second looks right and is deliberately NOT done under the dispatch's *repair none* | D — instrument / measurement layer (the audit's own guard apparatus) | OPEN — **apparatus under D-641: rowed and left, no wave, no dispatch, no surface.** The subject is a pointer inside the register's provenance field; the prohibitions it points AT are published once in the specification (#6) and are untouched. **Under D-438 it gates by the declaration's own default** (subject column = the instrument/measurement layer, which the over-inclusive first cut does not propose as a candidate); **no gating verdict is hand-added here** | [detail](open_items/OI-341.md) |
| OI-343 | ★ **The shell-read guard misses any command whose quoted pattern contains a pipe** — it splits the command into pipeline segments BEFORE tokenizing, so a `\|` inside a grep pattern shreds the command and the segment holding the repository path no longer starts with the utility | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the audit's own guard apparatus) | ✅ **RESOLVED 2026-08-04 (CC), by the user's ruling R1** — dispatch `cc_instruction_guard_fix_and_item1d.md`, … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-343.md) |
| OI-342 | **An audit artifact's "owner is unambiguous" column is carrying "outside this dispatch's edit surface"** — for which the same artifact already has a separate field | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (an audit artifact's authored field) | ✅ **RESOLVED 2026-08-08 (CC), by the user's ruling R2 of 2026-08-07** — ruling 2 of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-342.md) |
| OI-344 | **A LIVE guard carries a DATED recomputation of a proxy figure taken off the cluster-disposition layer, so refreshing that layer makes it fail** — `gen_reads5_repack.py --check` | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer | ✅ **RESOLVED 2026-08-07 (CC), by the user's ruling R3** — dispatch … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-344.md) |
| OI-345 | ★ **The shell-read guard is armed on the PowerShell tool and its utility set contains no PowerShell reading verb** — so `Get-Content`, `Select-String` and `Get-ChildItem` aimed at a repository path are ALLOWED where `cat`, `grep` and `ls` are denied | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the audit's own guard apparatus) | ✅ **RESOLVED 2026-08-08 (CC), by the user's ruling R1 of 2026-08-07** — ruling 1 of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-345.md) |
| OI-346 | **The empirically-unvalidated mark is SPECIFIED but not APPLIED** — the Jazz preset constants and the ground-truth-less idioms carry no mark and name no validating corpus | Created 2026-08-07 (CC, `cc_instruction_owner_rulings_homing.md`) by the register rule (c) check the homing of **D-497** requires. That amendment has two halves. **The RULE half is now discharged:** a style constant or an idiom no ground truth has calibrated carries the mark, the corpus that would validate it is named beside it, and the mark is maintained until an established corpus measures the value — written into `ARCHITECTURE.md` §6 beside the preset constants on the user's ruling. **The APPLICATION half is what this row tracks** and is not done: putting the mark on the Jazz preset constants and on the §6.7 idioms for which no gate-grade ground truth exists, constant by constant, each with its validating corpus named. **★ [[OI-7]] WAS CHECKED AS THE CANDIDATE AND IS NOT THE SAME OBLIGATION:** that row asks for a jazz ground-truth corpus to be ESTABLISHED or the Jazz correctness claims DE-SCOPED, and it gates the Stage-3 entry gate; this asks for the ABSENCE of that evidence to be DECLARED where a reader meets the values, and gates nothing. Either could be discharged without the other, so folding this into that row would make a closed row assert something it never checked. No other open row names the mark's application. **NOT in doubt:** no analysis behaviour, no measurement, no baseline — what changes is what a reader of those constants is told about how far they have been established | doc-sync / verifiability apparatus | OPEN — **★ THE JAZZ HALF IS APPLIED 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Task 1, a DEDICATED task after six continuations recorded that leftover capacity never reaches this row); THE IDIOM HALF IS HELD, and the row stays open for that half alone.** **What was marked, each with the SAME validation path named beside it** — jazz ground truth carrying written-out bass and piano voicings, converted and score-aligned: the **six Jazz mode-prior overrides** at `ARCHITECTURE.md` §4.6's preset table, and the **Jazz chord-scoring constants** (the extension threshold, and the reduced individual inversion bonuses that are where the preset's inversion behaviour actually comes from) beside the `ChordAnalyzerPreferences` structure. **The establishment was performed inside the act rather than assumed (#19):** §4.1c records the standing consequence that jazz accuracy is **not measurable on the corpora held at all**, measured by the bass-injection experiment, and `cowork_score_census.md` §5 defers the jazz fit to the jazz-ground-truth conversion because a fit has nothing to be evaluated against (#20). **One thing a reader could mistake for validation is named at the mark itself:** the Jazz BIR regression check runs the Jazz preset over the **Bach chorale** corpus — a regression surface, not jazz ground truth. **★ WHY THE IDIOM HALF IS HELD RATHER THAN GUESSED, which is a result and not a shortfall.** The record establishes that only the **classical common-practice** idiom is covered by held annotated music, but **no surface maps the five §6.7 idiom names onto a per-idiom ground-truth verdict**, and the one mapping §6.7 supplies — Baroque/galant/Classical share Chromatic-functional — is complicated by the discovery study's own finding that the gate corpus's chorales **form no distinct chord-idiom and scatter across clusters**. Marking under those conditions is marking by assertion, which #19 forbids in BOTH directions: a mark on a validated idiom states something false, and an exemption of an unvalidated one states something worse. **§6.6's own "not applied at HEAD" clause is corrected in the same act, its former wording preserved (#12).** **FORMER STATUS, PRESERVED (#12):** "OPEN — rowed 2026-08-07 (CC), and **rowed is all that is done to it** under the wave's freeze. **Apparatus under D-641:** the subject is the project's own verifiability marking on a constant table and bears on no analysis, no analysis input and no instrument any measurement of the analysis depends on. **No gating verdict is hand-added here** — a non-gating verdict is derived from a cut and never hand-added ([[OI-319]], [[OI-336]]); under D-438 the row's own subject column is what a later derivation will read" | [detail](open_items/OI-346.md) |
| OI-347 | **The two Layer-1.5 primitives are sited in different sections of the canonical architecture document**, and only one of the two sitings states its reason | Found 2026-08-07 (CC, `cc_instruction_owner_rulings_homing.md`) discharging the STOP the homing of **D-625** carries — *STOP if the primitive is specified elsewhere*. `ARCHITECTURE.md` §3.3's terminology note names exactly two L1.5 primitives, *"the shared `lineOfFifths` spelling interpreter, the phrase-boundary primitive."* The **phrase-boundary** primitive's contract pointer sits at **Layer 1** and states its reason in full — the document defines L1.5 as the thin derived-view layer over the L1 note model and carries no section of its own for it, so the layer the primitive is a view OVER is the nearest owning section. The **spelling** primitive is named in three places and **specified in none**: the §3.3 terminology note lists it, the **Layer 4** section names it as the interpreter the spelling-pin reads through, and Layer 1's own *derived views over the model* list omits it. **★ THE STOP DID NOT FIRE, AND THAT IS WHY THIS IS A ROW AND NOT A HELD ENTRY:** no section specifies the primitive, so **D-625** was homed at Layer 4 as ruled, with §5.14 pointing. What the check surfaced is the adjacent fact — the sibling's siting reason, applied to this one, points at Layer 1 instead — which is a question about where a primitive's own specification should live and not the question the ruling answered. **NOT in doubt:** the primitive, its behaviour, and the rule D-625 records, which is published once at Layer 4. **In doubt:** whether the two should be sited alike and where; and whether Layer 1's derived-views list is incomplete for omitting one of the two views §3.3 names | doc-sync / documentation gap | OPEN — rowed 2026-08-07 (CC), **rowed and left**: re-siting a specification is not a homing act and the wave's freeze is not relaxed. **Apparatus under D-641** — the subject is where a specification paragraph sits, and the rule it would move is published and unchanged. **No gating verdict is hand-added here** | [detail](open_items/OI-347.md) |
| OI-348 | ★ **The shell-read guard classifies only the FIRST token of a segment, so a working-tree read carried INSIDE another command's quoted argument is admitted** — `powershell.exe -Command "…"` and `python -c "…"` both pass | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the audit's own guard apparatus) | ✅ RESOLVED 2026-08-08 (CC, `cc_instruction_away_execution.md` Task 1) by the user's guard-family ruling of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-348.md) |
| OI-349 | ★ **One decision's two ends sit on different arms: the key-area grouping rule runs on the PRODUCTION record arm while the stabilization it names as its precondition is legacy-arm-only** — does the record arm satisfy that precondition by other means, or is the live path grouping un-stabilized regions? | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | B — the analysis (what the production notation path computes when it groups key areas) | ✅ RESOLVED — **RESOLVED 2026-08-09** (CC, `cc_instruction_return_continuation_2.md` Task 0, on the user's … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-349.md) |
| OI-350 | ★ **A delegation the user writes lands its entries in class C and STOPS four generators** — the finish-line machinery classifies from a stored field whose only writer is the classifier apply that [[OI-305]] holds | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the register's own classification machinery) | ✅ RESOLVED 2026-08-08 (CC, `cc_instruction_away_execution.md` Task 0), by the ruling this row was blocked … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-350.md) |
| OI-351 | ★ **`ls` aimed at a repository path was ADMITTED by the live guard on 2026-08-08**, one day after the widening whose resolution note records that shape as now denied | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the audit's own guard apparatus) | ✅ RESOLVED 2026-08-08 (CC, `cc_instruction_away_execution.md` Task 1), inside the one family design as Ruling … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-351.md) |
| OI-352 | ★ **The guard classification's own `--check` is run by no guard list** — the one check that would catch a drifted classification is exempt from the runner by construction, and its running depends on a session remembering | Separated out of [[OI-319]] on 2026-08-09 (CC, `cc_instruction_return_continuation.md` Task 0) on the user's **Ruling 2 of 2026-08-09**; the fact itself was recorded on that row's 2026-08-04 dated entry and rowed nowhere, which is the prose-only tracking #10 forbids. **Read at the two tools, not recalled.** `tools/audit/gen_guard_state.py` derives its candidate population from every `*.py` under `tools/audit/` carrying a `--check`, `--verify` or `--establish` mode, and then removes two files by name in `NOT_A_SUBJECT` — itself, and `tools/audit/gen_guard_classification.py`. **The exemption is correct and is not what this row disputes:** the classification READS the artifact the guard state WRITES, so running it inside that run would classify the previous run's state and then change the artifact it had just read, with no fixed point; both files state that reason in their own words, and the classification's own STOP additionally requires it to run AFTER the state. **What the exemption leaves is the gap:** `gen_guard_classification.py --check` is therefore in no derived list and in no authored list, so nothing runs it as part of any guard sweep, and whether the committed `guard_classification.json` still re-derives is established only when a session runs it by hand. Its three STOPs — a tool with no authored verdict, a verdict naming a tool the state does not carry, and a disagreement with `gen_guard_state.HISTORICAL` in either direction — are the mechanism that keeps the verdict table and its invocation consequence from drifting apart (#6), and they only fire when something runs it. **NOT in doubt:** the classification's verdicts, the guard state's own runs, and the recursion exemption's ground — none of which this row questions; and nothing about the analysis, its inputs, or any measurement of it | D — instrument / measurement layer (the audit's own guard apparatus) | OPEN — rowed 2026-08-09 (CC), and **rowed is all that is done to it**: the remedy is a change to how the guard sweep is composed — a second runner stage, a wrapper that runs the two in the required order, or an authored entry with a declared ordering — and choosing among those is a mechanism change **D-436** reserves to the user. **Apparatus under D-641**, and no gating verdict is hand-added here: the subject is this project's own guard runner, it bears on no analysis, no analysis input and no measurement tool any measurement of the analysis depends on, and a non-gating verdict is DERIVED from the cut rather than written into a row ([[OI-319]], [[OI-336]]). **This row is not an establishment obligation on any published rate** — the classification records verdicts about which guards re-derive, not a measured rate — so the clause that always gates is not engaged | [detail](open_items/OI-352.md) |
| OI-353 | ★ **Six comments in `src/` say the production notation record path is "default OFF"; the configuration sets its default to TRUE** — a reader of any of them learns which analysis arm ships and learns it wrong | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10), in the code | ✅ RESOLVED 2026-08-09 (CC, `cc_instruction_return_continuation_8.md` Task 4) on the user's **Ruling 16** of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-353.md) |
| OI-354 | ★ **The legacy-mark verification's population has GROWN since it was declared verified, and a drifted citation hid that** — register entries now carry the legacy mark for which [[OI-289]]'s ✅ VERIFIED artifact holds no verdict | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | E — verification owed / decisions register | ✅ RESOLVED 2026-08-09 (CC, `cc_instruction_return_continuation_4.md` Task 0) on the user's **Ruling 23** of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-354.md) |
| OI-355 | ★ **The shell-read guard denies every `sed` and `awk`, wherever they are aimed** — the script argument is treated as a path candidate, so a read of a file OUTSIDE the tree is refused and the denial names the script as though it were a repository path | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the audit's own guard apparatus) | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_10.md` Task 4) on the user's **Ruling 50** of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-355.md) |
| OI-356 | ★ **A row is judged OPEN or RESOLVED by searching its whole status cell for the resolved mark, so a status cell that merely MENTIONS another row's ✅ makes its own row read as resolved to every derivation over the index** | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the open-items index's own row parser) | ✅ RESOLVED 2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 1) by the user's **Ruling 33** of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-356.md) |
| OI-357 | ★ **The Baroque partial-signature correction lives in the LEGACY key resolver, which the production arm does not run — and whether the joint estimator handles that notation practice AT ALL is unestablished in both directions** | Found 2026-08-09 (CC, `cc_instruction_return_continuation_3.md` Task 1) while performing the citation-transfer half of the [[OI-354]] establishment on **D-575**, under the user's Ruling 18. **Read at the record and at the code, not recalled.** Baroque scores are frequently notated with one accidental fewer than the modern practice, so the sounding key sits one step to the sharp side of anything a signature-faithful reading could name; the adopted handling DETECTS that situation — the flattened sixth degree pervasive across the sounding weight and dominating its natural form — and reinterprets the written signature one step for the whole resolution. **That machinery is in `src/composing/analysis/key/keyresolver.{h,cpp}` and the legacy types beside it, and nowhere else**: the joint module carries no construct of that name, its only matches on the search terms being a phrase-split remark and a partial-record remark, both read and neither related. The production arm does not reach the legacy resolver — the record arm's section derivation calls neither `resolveKeyAndModeRanked` nor the legacy key layer, and `--joint-inference` decodes through the joint module alone. **D-575's own home text says the question is open, in terms:** *"Whether the joint estimator handles the convention AT ALL is NOT settled by this entry and is not asserted here."* **★ WHAT IS DELIBERATELY NOT CLAIMED, because the distinction is the whole row:** absence of the machinery is NOT absence of the handling. The live arm takes the signature and declared mode as a **weak fitted soft prior** with no conditional gate anywhere (**D-528**) rather than as a hard constraint, so a key one step to the sharp side is reachable there without any explicit correction. **This row asserts no defect and no regression** — it asserts that the question is unestablished, which is the state #19 refuses to treat as established. **★ AND ONE THING SUBSTANTIALLY REDUCES ITS SIZE, STATED SO IT IS NOT READ AS LARGER THAN IT IS:** the production arm's key agreement on exactly this repertoire IS measured and published in `CLAUDE.md` gate block (A), so this is not an unmeasured failure behind a green gate — any effect already sits inside a published value. What is missing is whether a named, corpus-wide notation practice is handled on the arm that ships, which bears on how that value should be read. **NOT in doubt:** D-575 itself, its ⚠ LEGACY mark, its superseded-in-fact status and its home, none of which this row reopens; the legacy fix, which landed and was verified on its anchor case; and every published measurement, none of which moves | B — the analysis (what the production key arm does with a Baroque notation practice the legacy arm detected and corrected) | OPEN — rowed 2026-08-09 (CC), and **rowed is all that is done to it**: what is owed FIRST is a read-only establishment — does the production arm read these scores correctly, and by what means — and no fix, design or inference change is authorized by this row (**D-231**; #8's three-clause gate stands). **GATES** — its subject bears on the analysis, which is the test `CLAUDE.md`'s non-gating declaration states, answered YES, so the apparatus declaration does not reach it; and it is independently an establishment obligation (#19), which always gates whatever its subject. **Why it is rowed rather than left in D-575's home text:** the record states the gap only as a scope disclaimer inside one entry's home, and tracking an owed item in prose alone is the #10 violation this register exists against — the [[OI-283]] shape. **★ THE EARLY, BOUNDED, READ-ONLY ESTABLISHMENT ORDERED BY THE USER'S RULING 26 OF 2026-08-09 HAS RUN (CC, `cc_instruction_return_continuation_4.md` Task 3), AND IT ANSWERS THE QUESTION IN ONE DIRECTION ONLY — SO THIS ROW STAYS OPEN AND WHAT REMAINS IS NAMED.** **The population was DERIVED, never sampled:** the evidence document's own stated method — notated key signature against the published annotated key, read from the corpus metadata — was applied mechanically to every piece in that file rather than its six-row table being transcribed, and it yields a population many times larger than that table shows. **★ WHAT THE COMPARISON CANNOT SEE, measured rather than assumed and stated before any value in it is read:** every committed output directory for this repertoire predates the date the joint estimator became the production inference layer, none carries a corpus manifest, and none carries any field naming an inference arm — **so the committed outputs are the LEGACY arm's, and this comparison cannot answer whether the production arm reads these scores correctly.** That half of the row is untouched and stays open. **★ WHAT IT DOES ESTABLISH IS NOT WHAT WAS EXPECTED, and it is rowed at [[OI-363]]:** the legacy correction — the handling this row's own premise treats as the thing the production arm lacks — **does not resolve the derived population at the committed outputs.** A large minority of the members is NOT read with the annotated tonic; the large majority of the disagreements land on a home of the NOTATED signature, which is the evidence document's own diagnostic for the lock the correction exists to escape; and the shortfall is concentrated in the major-key half of the population. *(★ THE FIRST CLAUSE WAS CORRECTED 2026-08-09 by `cc_instruction_return_continuation_7.md` Task 2. THE FORMER WORDING, PRESERVED (#12): "Fewer than half of the members are read with the annotated tonic." It was INVERTED against the artifact it cites — at the column it names the agreeing members outnumber the disagreeing ones, by a narrow margin. The other two clauses are true and are not disturbed, the finding stands, and the user's Ruling 35(c) is not disturbed: it ruled the FORM of a direction-without-value statement acceptable, and a ruling about form cannot make a direction true. Account at `cowork_away_returns.md` §2.11.)* Every value is at `tools/audit/oi357_partial_signature_establishment.json` and none is restated here (**D-431**). **This changes what the row's remaining question is worth asking about**, and it changes nothing about the analysis: no fix, no design, no inference change, and no measurement of the production arm was taken. **★ THE PRODUCTION-ARM RUN THE USER'S RULING 35(b) LICENSED HAS NOW RUN (CC, `cc_instruction_return_continuation_7.md` Task 2), AND THIS ROW'S FIRST QUESTION IS ANSWERED WHILE THE ROW STAYS OPEN FOR ITS SECOND.** The arm that ships was run over the whole derived population with `--joint-inference`, its effect established at the outputs' own `analysisPath` field rather than trusted from the command line; outputs went to `scratch_artifacts/` and nowhere else, nothing was promoted, no golden or gate moved, and the run record carries the #16 stamps. **ANSWERED: the production arm reads the LARGE MAJORITY of the derived population with the annotated tonic, its disagreements are a small minority, and it carries no partial-signature correction at all** — which is exactly the "NOT claimed" half this row already stated, moved from conjecture to measurement. **★ AND ONE THING HAD TO BE BUILT BEFORE ANY OF IT COULD BE COMPARED:** the production arm's batch output publishes NO top-level whole-run key field, only a key per region, so the comparison this row was waiting for could not have been made on the field it names — run unchanged it would have scored every piece a disagreement and reported a surface artifact as a catastrophe. The two arms share one surface, the per-region key, which is reduced to a whole-run reading by a duration-weighted vote computed identically for both arms, with its faithfulness against the published field measured rather than assumed. **★ A SAME-COMMIT CONTROL WAS ALSO RUN AND IT CHANGES WHAT MAY BE CONCLUDED:** the committed outputs are two months older and used a different preset, so the legacy arm was re-run at this commit under the same preset — the two arms are then CLOSE, with the production arm modestly ahead, and **the large difference is between either HEAD run and the June outputs, which is not attributable to the arm.** That control run is a REPORTED WIDENING of Ruling 35(b)'s letter (**D-654**), with its ground stated at `cowork_away_returns.md`'s Task 2 log. **STILL OPEN: the second half of this row's own question — *by what means* — on which this pass counts and does not explain; and the per-case reading, which it does not make.** Every value is at `tools/audit/oi357_production_arm_comparison.json`, `tools/audit/oi357_legacy_arm_same_commit_control.json` and the two run records, and none is restated here (**D-431**). **Nothing is proposed** — no fix, no design, no inference change. **★ THE USER HAS NOW RULED ON THAT PASS — Ruling 43 of `cowork_rulings_2026_08_09_eighth_stop.md`, recorded here 2026-08-09 by `cc_instruction_return_continuation_8.md` Task 0 — AND THE ROW STAYS OPEN, WHICH IS A READING OF THE ROW'S OWN TERMS AND IS STATED SO IT CAN BE OVERRIDDEN IN ONE ACT.** Three things are ratified. **(i) The same-commit legacy control is ACCEPTED**, on the reported-widening ground (**D-654**), because it replaced a false large-arm-effect conclusion with a controlled one — most of the apparent gap is NOT the arm. **(ii) The inversion corrections applied within the previous session's authority are ratified as CORRECTIONS OF RECORD**, and Ruling 35(c) is undisturbed: it ruled a FORM acceptable and never made a direction true. **(iii) The substantive outcome is recorded: this row's question is ANSWERED — the production arm handles the partial-signature repertoire AT LEAST AS WELL as the legacy arm's explicit correction, established by controlled comparison with both artifacts re-deriving byte-identically.** **WHY THE ROW IS NOT FLIPPED:** the applying dispatch conditions a flip on the question reading answered *in both directions at the row's own terms*, and this row's own terms name TWO owed things — *does the production arm read these scores correctly* (answered) and *by what means* (not answered, and this pass counts rather than explains) — beside the per-case reading it also names and does not make. **So what the ruling answers is recorded and what remains is named, rather than the row being closed over a half it still carries** ([[OI-283]]'s shape). A user who reads the ruling's *ANSWERED* as covering the whole row can flip it in one act; nothing else would move with it. The ruling's own record of its acceptance and its bounds is at the comparison artifacts' closing state, written into their generator so it re-derives (**D-431**). Cross-ref [[OI-354]] (the establishment whose transfer half found it), [[OI-363]] (what the bounded establishment measured), [[OI-315]] (the sibling legacy key-path staleness, whose open half names the same source document) | [detail](open_items/OI-357.md) |
| OI-358 | ★ **Five consecutive register entries quote a DIFFERENT rule than their own title, plain restatement and defense describe — one quotes a section heading — and BOTH guards pass over it, because each verifies that the verbatim sits at the cited line and neither asks whether it is the rule the entry is about** | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | E — verification owed / decisions register | ✅ RESOLVED 2026-08-09 (CC, `cc_instruction_return_continuation_4.md` Task 1) on the user's **Ruling 24** of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-358.md) |
| OI-360 | ★ **The decisions register still has NO mechanism that could catch a quote belonging to a different rule than the entry it sits in — and the one candidate has now been MEASURED and does not separate** | Measured 2026-08-09 (CC, `cc_instruction_return_continuation_4.md` Task 1) on the user's **Ruling 24(b)** of `cowork_rulings_2026_08_09_fourth_stop.md`, which ordered the detection method [[OI-358]] stated to be built corpus-first and **adopted as a guard only on measured clean separation**. It was built, the five known-bad quotes were recorded as its labelled corpus before the mechanism was measured, and **the separation is not clean**: a substantial population of legitimate entries sits at or below the worst known-bad value, so no threshold divides the two and a guard built on it would deny legitimate work. **★ AND THE SHARPEST RESULT IS ABOUT THE SIGNAL'S CEILING RATHER THAN ITS THRESHOLD:** after the repair, four of the five entries rise and **one stays at the floor** — its correct bullet states the rule in code names while its title paraphrases them, which is exactly the blindness the tool declares about itself in advance. So the method [[OI-358]] identified is not merely un-thresholded; it is measurably unable to see one of the five cases it was derived from. **What remains open is [[OI-358]]'s SECOND gating ground, which the repair does not touch:** the register's own quote check and the disposition verifier are **both satisfied by a corrupted pair**, because once the quote IS the text at the drifted line the two agree with each other permanently — so their green verdicts do not bound what they appear to bound, which is the shape #19 refuses. Every value is at `tools/audit/decisions/verbatim_subject_consistency.json` and none is restated here (**D-431**). **NOT in doubt:** the five repaired entries, whose quotes now sit in the bullets their titles and defenses describe, verified at the home with zero anchor drift; the advisory artifact itself, which is correct about what it can and cannot see; and nothing about the analysis, any measurement of it, any golden or anything in `tools/robust_stop/` | E — verification owed / decisions register | OPEN — rowed 2026-08-09 (CC), and **rowed is all that is done to it**: a second candidate signal is a MECHANISM design, which **D-436** reserves to the user, and the guard-family order this record fixed puts the corpus before the clause — the corpus for this class already exists and is committed, so a later act starts from it rather than from a blank page. **Nothing here proposes a design**, and the advisory artifact is explicitly NOT to be tuned until it separates: tuning a signal against the five cases that motivated it is fitting to the motivating cases, which `DEFECT_TYPES.md` DT-2 forbids and which is the reason the adoption condition was ruled in advance. **GATES** — it is an establishment obligation (#19), which always gates whatever its subject: two green verdicts that cannot fail on this defect are exactly a thing trusted because nothing contradicted it, where what would contradict it cannot. Cross-ref [[OI-358]] (the row whose repair half is discharged and whose detection half this carries), [[OI-354]] (a verification whose population grew unseen), [[OI-292]] (where a mechanism owed on a governing rule is tracked) | [detail](open_items/OI-360.md) |
| OI-359 | ★ **Nothing checks that a measurement reported over several repertoires is broken down BY repertoire** — the per-corpus half of a ratified measurement convention has no mechanism, and the cut that does exist is by PRESET, which is a different question | Re-classed 2026-08-09 (CC, `cc_instruction_return_continuation_4.md` Task 0) on the user's **Ruling 22** of `cowork_rulings_2026_08_09_fourth_stop.md`, which ratifies the fifteen owed `CLAUDE.md` rule-triage verdicts as authored with **one movement**: **D-486**'s uncovered half moves from MECHANISM-EXISTS (partial) into the defect class, MECHANISABLE-AND-NOT. **The two halves of D-486 come apart, and that is not in dispute.** The COVERAGE DENOMINATOR half is mechanical where it matters — gate block (A)'s figures are regenerated from the measurement's own summary by `tools/robust_stop_restamp.py`, which is established by reproducing the outgoing manifest exactly, so a published value carries the block it was computed from and cannot be stated at a denominator the run did not have. The PER-CORPUS half is not mechanised at all: the gate corpora are partitioned per PRESET, which answers a different question, and nothing checks that a value spanning several repertoires is broken down by them. `process_check.py` carries only the adjacent shape — it flags a bare quantity, not an aggregate that hides which corpus moved. **THE RULING'S GROUND for treating the gap as a defect rather than an absence:** the convention's own stated reason is that a single aggregate can hide which corpus moved, which bears on measurement integrity (#9, #24) — so a half of it left uncovered is a defect in the mechanisation, not merely a rule nobody automated. **NOT in doubt:** the convention **D-486** itself, which is unchanged and binding; the denominator half's mechanism, which is established and is not questioned; every published value, none of which moves; and the fourteen other triage verdicts, which the same ruling confirms as authored | D — instrument / measurement layer (the mechanisation triage over the governing document's own rules) | OPEN — re-classed 2026-08-09 (CC), and **the mechanism is deliberately NOT built by the ruling that created this row**: it joins the **D-436** mechanism backlog like every other member of the defect class, and what a member of that class is owed is a user ruling on whether to build it, not a session's initiative. **GATES** — its subject is a measurement convention's enforcement, which is an instrument a measurement of the analysis depends on, and that is the test `CLAUDE.md`'s non-gating declaration states, answered YES. The defect class as a whole is tracked at [[OI-292]]; this row exists because the re-class is a newly recorded issue and rule (c) requires its own index row and detail file in the commit that records it. Cross-ref [[OI-292]] (the defect class), [[OI-179]] (the other open obligation on the measurement side of the same principles) | [detail](open_items/OI-359.md) |
| OI-361 | ★ **A row whose status cell states its resolution IN WORDS, with no ✅, reads OPEN to every derivation over the index** — the same parser's blind spot in the opposite direction from [[OI-356]]'s | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the open-items index's own row parser) | ✅ RESOLVED 2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 1) by the user's **Ruling 33** of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-361.md) |
| OI-362 | ★ **The ONE index parser SILENTLY DROPS any row that does not split into the expected number of cells — one row is being dropped at HEAD, and a dropped row is neither open nor resolved but ABSENT from every derivation** | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the open-items index's own row parser) | ✅ RESOLVED 2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 1) by the user's **Ruling 33** of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-362.md) |
| OI-363 | ★ **The Baroque partial-signature correction is recorded as RESOLVED on one anchor case, and over the DERIVED population at committed outputs it leaves a large minority of the tonics unread and locked to the notated signature — with the shortfall concentrated in the major-key half** | Measured 2026-08-09 (CC, `cc_instruction_return_continuation_4.md` Task 3) by the early, bounded, read-only establishment the user's **Ruling 26** ordered for [[OI-357]]. **Read-only over COMMITTED outputs: no corpus was regenerated, no measurement tool of the analysis was built or run, and nothing about the analysis was changed.** **The population was DERIVED, never sampled** — `docs/key_detection_baroque_partial_signature.md` §3 states its own method (notated `KeySig` against DCML `annotated_key`, from the corpus metadata), and that METHOD was applied mechanically to every piece in that file rather than its six-row table being transcribed, so the population cannot be narrower than the record's own rule makes it. It is many times the size of that table. **★ WHAT WAS MEASURED.** At the most recent committed run for this repertoire — dated ten days AFTER the correction landed, so it reflects it — a large minority of the population is NOT read with the annotated tonic. *(★ CORRECTED 2026-08-09 by `cc_instruction_return_continuation_7.md` Task 2. THE FORMER WORDING, PRESERVED (#12): "fewer than half the population is read with the annotated tonic", which also stood in this row's title and in [[OI-357]]'s row. It was INVERTED against the artifact it cites — at the column it names the agreeing pieces outnumber the disagreeing ones, by a narrow margin. Nothing else in this row is withdrawn, and the user's Ruling 35(c) is not disturbed: it ruled the FORM of a direction-without-value statement acceptable, and a ruling about form cannot make a direction true. Account at `cowork_away_returns.md` §2.11.)* **The large majority of the disagreements land on a home of the NOTATED signature**, which is the evidence document's own diagnostic for the signature lock the correction exists to escape, and both homes are computed here from the notated signature alone, so that column is derived and not a judgment. **The split by the annotated key's mode is the sharpest cut:** the minor-key half fares markedly better than the major-key half, and the correction's recorded detector keys on a single MINOR-key signal — the flattened sixth degree pervasive across the sounding weight and dominating its natural form. **THE CUT IS REPORTED AND THE CAUSAL CLAIM IS NOT MADE:** this pass counts, it does not explain, and no fix, design or inference change is proposed or authorized (**D-231**; #8's three-clause gate stands). **★ THE RECORD'S OWN ACCOUNT READS WIDER THAN THE MEASUREMENT SUPPORTS**, which is the second half of the finding: that document's banner declares the weakness RESOLVED, and what it verifies is ONE anchor case. A reader meets *resolved* and would not learn that a large minority of the population it belongs to is not. Every value is at `tools/audit/oi357_partial_signature_establishment.json` and none is restated here (**D-431**). **NOT in doubt:** the anchor case itself, which the committed output reads with the annotated tonic exactly as the record says; the correction, which landed and does what its own description says; the ground truth, which is the published human annotation and nothing else; and every published measurement of the gate corpus, none of which this touches — this repertoire is not the gate corpus | B — the analysis (what the legacy key arm does with a Baroque notation practice it has a dedicated correction for) | OPEN — rowed 2026-08-09 (CC), and **rowed is all that is done to it.** **What is owed FIRST is not a fix but a reading**: whether each disagreement is a defect, a defensible modal reading the major/minor ground truth cannot represent (which `CLAUDE.md` gate block (A) makes a GROUND-TRUTH LIMITATION and not a defect to optimize away), or a global-versus-local key comparison artifact. That reading is per case and this pass makes none of it. **GATES** — its subject bears on the analysis, which is the test the non-gating declaration states, answered YES. **★ AND ITS SUBJECT IS THE LEGACY ARM, WHICH BOUNDS WHAT IT CAN COST:** the arm that ships does not run this resolver, so nothing here is a claim about production behaviour, and [[OI-357]] — not this row — carries the production question. **The doc-sync half is one edit for a session whose authority reaches `docs/`:** the banner should say what was verified rather than that the weakness is resolved. **★ THAT EDIT IS PERFORMED 2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 0) on the user's Ruling 35(a) of `cowork_rulings_2026_08_09_fifth_stop.md`, and the row stays OPEN because the edit was never what it gates on.** The banner of `docs/key_detection_baroque_partial_signature.md` now scopes its RESOLVED to the ANCHOR CASE it verified and carries a second, marked block stating what that word does not cover, pointing here; nothing above it is withdrawn, because both halves are true as written and the defect was that a reader met *resolved* and would not learn the population it belongs to is mostly not (C5). **What still gates is the per-case reading** — defect, ground-truth limitation, or global-versus-local comparison artifact — and this pass makes none of it. **★ AND THE ROW'S OWN WORDING IS RULED ACCEPTABLE, so it is not re-edited (Ruling 35(c)):** *fewer than half*, *the large majority* and the like are DIRECTION-WITHOUT-VALUE statements with the artifact cited beside them, which is what **D-431** asks for — a direction a reader can act on, with every value at `tools/audit/oi357_partial_signature_establishment.json` and none transcribed. **★ THE PRODUCTION-ARM COLUMN THIS ROW WAS OWED IS ADDED 2026-08-09 (CC, `cc_instruction_return_continuation_7.md` Task 2), TOGETHER WITH A SAME-COMMIT LEGACY CONTROL, AND THE CONTROL QUALIFIES THIS ROW RATHER THAN OVERTURNING IT.** On the PUBLISHED whole-run field this row measured, the legacy arm at HEAD is close to the committed outputs, **so this row's finding stands on its own quantity.** On a DERIVED shared surface — the per-region keys reduced to a whole-run reading by a duration-weighted vote, built because the production arm publishes no whole-run field at all and computed identically for both arms — the legacy arm at HEAD is markedly better than the June outputs, so **a reader must not carry the committed-outputs picture forward as a statement about the legacy arm as it stands.** The production arm, which carries no partial-signature correction, reads the large majority of the same population with the annotated tonic and is modestly ahead of the same-commit legacy arm. **One observation is recorded and explicitly NOT diagnosed:** how often the derived whole-run reading equals the legacy arm's own published field is much lower at HEAD than in the June outputs, which is a fact about two of that arm's output fields and not about correctness. Every value is at `tools/audit/oi357_production_arm_comparison.json` and `tools/audit/oi357_legacy_arm_same_commit_control.json` (**D-431**). **Nothing is proposed** — no fix, no design, no inference change, and the per-case reading this row gates on is still not made. **★ THE INVERSION CORRECTIONS ARE RATIFIED AND THE LAST SURVIVING INSTANCE IS NOW CORRECTED — the user's Rulings 41 and 43 of `cowork_rulings_2026_08_09_eighth_stop.md`, applied 2026-08-09 by `cc_instruction_return_continuation_8.md` Task 0.** Ruling 43 ratifies the corrections the previous session applied within its own authority as CORRECTIONS OF RECORD, and confirms that **Ruling 35(c) is undisturbed** — the form it ruled acceptable is still acceptable, and a ruling about form was never capable of making a direction true. Ruling 41 licenses the ONE edit that session could not reach: `docs/key_detection_baroque_partial_signature.md`'s marked block said the correction *"reads fewer than half of that population with the annotated tonic"*, and it now says *slightly more than half*, with the former wording preserved in place (#12) and **everything else in that block untouched** — what *resolved* covers, the derived population, the notated-signature diagnostic, the disagreement-total clause and the two bounds are all true as written. **The row STAYS OPEN, and for the reason it has always stayed open:** what it gates on is the per-case reading — defect, ground-truth limitation, or global-versus-local comparison artifact — which no pass has made. Cross-ref [[OI-357]] (the row whose bounded establishment measured this), [[OI-315]] (the sibling legacy key-path staleness naming the same source document) | [detail](open_items/OI-363.md) |
| OI-364 | ★ **The MECHANICAL half of the writing-standards conformance check is ruled IN SCOPE and nothing implements it** — status-banner presence, terms-table presence on a new specification, and section structure against the kind list | Created 2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 0) by the register rule (c) check the user's **Rulings 28, 29 and 32** of `cowork_rulings_2026_08_09_fifth_stop.md` require. Those rulings settle the conformance question [[OI-230]] carried, by SPLITTING it. **Ruling 32 closes the deep limb:** predicate qualification is ruled KNOWLEDGE — no mechanical test can decide whether a two-place word names its argument without understanding the sentence, and a noisy checker gets switched off, which is worse than none (**D-473**) — so prose quality and predicate discipline stay the writer's method in `cowork_design_doc_template.md`, the per-session self-check (**D-434**) and the reading passes. **Nothing is owed on that half.** **Ruling 29 opens a mechanical one:** documents ARE in the conformance enumeration for a named subset — status-banner presence, terms-table presence on a NEW specification, and section structure judged per Ruling 28's kind rule. **Ruling 28 supplies what the third needs to be checkable at all:** the KIND LIST, now enumerated in `cowork_design_doc_template.md`, where a document of an unlisted kind is a **STOP** and never a silent pass. All three read a document's own STRUCTURE rather than its prose, which is why they fall on the mechanisable side of the line the deep half fails; the rule-triage entry **D-193** is moved into the defect class on this subset with its former KNOWLEDGE class and ground preserved beside it (#12). **NOT owed, so the row is not read as larger than it is:** no conformance VERDICT over the existing tree — Ruling 28 decides which KINDS the structure binds, not which documents conform; no document is judged, re-shaped or re-bannered; and no prose is retired, which under **D-436** is no longer a defect | process / mechanisation debt | OPEN — rowed 2026-08-09 (CC), and **rowed is all that is done to it**. **The mechanism is deliberately NOT built by the rulings that created this row**: it joins the **D-436** mechanism backlog like every other member of the defect class, and what a member of that class is owed is a measured establishment before it is trusted (#19) — a detection rate against documents known to be missing a banner, a terms table or a listed kind, and a false-positive rate at or near zero on legitimate documents — then a user ruling on whether to build it. Nothing here proposes a design for the check and no session may write one under this row. **No gating verdict is hand-added here** — a non-gating verdict is derived from a cut and never hand-written ([[OI-319]], [[OI-336]]). Cross-ref [[OI-230]] (the row whose three questions these rulings answer), [[OI-229]] (the sibling standard, whose mechanical form is licensed as a scanner by Ruling 31), [[OI-292]] (the defect class this entry now belongs to), [[OI-359]] (the other member added to the same backlog by the rulings of 2026-08-09) | [detail](open_items/OI-364.md) |
| OI-366 | ★ **The shell-read guard's establishment check reports STALE intermittently, while the artifact it checks re-derives BYTE-IDENTICALLY — so a published deny rate and false-deny rate are not reproducible on demand** | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the audit's own guard apparatus) | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_10.md` Task 4) on the user's **Ruling 50** of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-366.md) |
| OI-367 | ★ **A FOURTH row whose status cell states a closure in words reads OPEN to every derivation — and the pass that enumerated that family missed it, because the family was derived by a pattern whose reach was never measured** | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the open-items index's own status apparatus) | ✅ RESOLVED — 2026-08-11 (CC, `cc_instruction_return_continuation_11.md` Task 0) on the user's **Ruling 53** … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-367.md) |
| OI-368 | ★ **A derived enumeration reports its class EMPTY at HEAD while an instance of that class stands — the same claim, in the same file, at a block the pattern does not reach** | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (a derived enumeration's unmeasured reach) | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_13.md` Task 0) on the user's **Ruling 59** of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-368.md) |
| OI-369 | ★ **The finish line's LAST homing item counts one entry, and the user has already ruled that entry SUPERSEDED with no home owed — the subtraction its sibling item performs by machinery, this one does not perform at all** | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | F — doc-sync debt (#10) at a DERIVED surface: a completion map states an obligation the record has already dispositioned | ✅ RESOLVED 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Task 0) on the user's **Ruling 61 of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-369.md) |
| OI-150 | BUILD_AND_TEST.md stale test baselines | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync | ✅ RESOLVED 2026-08-13 (CC, dispatch `cc_instruction_oi150_baselines.md`, Task 2) — **BOTH CLOSING CONDITIONS … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-150.md) |
| OI-370 | A mandatory session-start read cannot be performed — `STATUS.md` has accumulated past the point where the file tools can read it | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | doc-sync | ✅ RESOLVED 2026-08-17 (CC, `cc_instruction_preparation_sixth.md` Task 1, executing **Ruling 4** of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-370.md) |
| OI-371 | ★ **A set of comment blocks was HELD pending a call-graph answer, that answer is now established, and the row recording the hold is RESOLVED** — so a false-at-HEAD statement about which decoder ships has no live tracking | Found 2026-08-11 (CC, `cc_instruction_row_landing_and_oi141_arm_check.md` Task 2) while performing the licensed read-only establishment of the shipping arm's key path at the call graph — the exact input those verdicts were waiting on. `tools/audit/arm_comment_sweep.json` grades a group of blocks **HELD** with the recorded reason that settling them needs the CALL GRAPH, under Ruling 16's clause that *a sibling whose falsity is not mechanical is HELD*; the population is at that artifact and none of it is restated here (**D-431**). The trace establishes that `produceNotationRecord` calls `decodePiece` and that both production surfaces reach it, so `jointdecoder.h`'s *"DORMANT (no production consumer)"* is false at HEAD. [[OI-353]], which recorded the hold, is RESOLVED, so nothing open carries the set. **Nothing is corrected here** — the dispatch admits no `src/` edit, and a re-grade must run through the sweep rather than around it, as [[OI-368]]'s act (a) records for the neighbouring site. **No gating verdict, and no count in either direction** — both are derivations, never hand-added. **NOT in doubt:** no behaviour, no measured value, no golden, no corpus of scores, nothing in `tools/robust_stop/`; Ruling 16 declined to guess and was right to | F — doc-sync debt (#10), in the code | OPEN | [detail](open_items/OI-371.md) |
| OI-372 | ★ **The filing-convention application STOPs before it classifies anything — its authored verdict table names a document its own derivation does not carry, and the tool has no retired block to move that verdict into** | Rowed 2026-08-12 (CC, `cc_instruction_resume_lapse_records.md` Task 1) on the user's **Ruling 67**, which released the halt the previous batch declared over this same tool and directed that the defect be rowed rather than repaired. **Verified at the objects before rowing, and not carried from either record.** `tools/audit/gen_filing_convention_application.py --check` exits non-zero on its third STOP — *"authored verdicts for documents the derivation does not carry: STATUS_ARCHIVE.md -- the tree has moved under the table"* — and because that STOP is raised before anything is written, **the tool produces nothing at all**: its candidate population, its per-candidate verdicts and its soundness verdict against its own seeds are all unavailable while it stands, not merely the one row the message names. Its `--derive-only` mode lists the candidates the derivation currently carries and the named document is not among them. **The cause is established and was recorded one batch earlier**, at `cowork_away_returns.md` §1.21 and then independently at §1.22: the S1 signature scans a fixed count of a document's last non-blank lines for a line carrying both a fate word and a status-or-commit marker, and that document's tail now carries an ordinary dated entry, so it is not a derived candidate while its authored `CONFORMANT` verdict still names it. Nothing about the bearing cut touches that derivation, which is what Ruling 67 turns on. **★ WHAT MAKES IT A ROW RATHER THAN AN ACT, and this half is this session's own reading:** #12 asks that the verdict be moved out WHOLE with the reason it left, and **this tool has nowhere to move it to** — its two sibling tools each carry a retired block for exactly this case (`gen_nongating_apparatus_rows.py`'s `RETIRED_VERDICTS` and `gen_gating_row_sizing.py`'s `RETIRED_SIZINGS`, both on the same recorded ground), and this one carries none, so discharging the row means ADDING one, which is a change to a mechanism's structure that **D-436** reserves. **NOT in doubt:** the user's Ruling 62 and its filing rule, homed at `cowork_design_doc_template.md` and not restated here (#6); the other authored verdicts in the same table, about which this says nothing; and nothing about the analysis, no measured value, no golden, no corpus of scores, nothing under `tools/robust_stop/` | D — instrument / measurement layer (an audit tool's authored verdict table, and the retired block it does not have) | OPEN — rowed 2026-08-12 (CC), and **rowed is all that is done to it**: the dispatch that ordered the rowing admits no mechanism change, and both closing acts are mechanism acts **D-436** reserves — **(a)** a retired block with the verdict moved into it whole, the shape both sibling tools already use, which is what `cowork_away_returns.md` §1.22 names in one line as the act that would clear the guard; or **(b)** a ruling on the signature itself, whose window is a fixed line count, so a document being long enough removes it from the population whatever its banner says. **No gating verdict is hand-added here** — a verdict is derived from a cut and never hand-written ([[OI-319]], [[OI-336]]). Cross-ref [[OI-373]] (the sibling finding from the same opening guard run), [[OI-47]] (whose closure moved the population that exposed this), [[OI-370]] (the archive pass that changed the document's tail). **★ DISCARDED 2026-08-12 (CC, `cc_instruction_worth_test.md` Task 2) under the user's Ruling 68 of 2026-08-11 — the worth test at `CLAUDE.md` principle #10, register entry D-174. THIS IS NOT A RESOLUTION AND THE STATE ABOVE IS UNMOVED:** the row stays OPEN, nothing about the defect has been done, and this clause is inert to every derivation because a row's state is its opening token and nothing else (rule (f)). What ends is the OBLIGATION — no gate, no capacity. **The four grounds, each answered at the objects and written in full in the detail file:** no establishment obligation is carried, because the tool's artifact publishes its own bound and its own NOT SOUND soundness verdict and **D-673** closed its detection measurement at this very tool; no analysis decision consumes the enumeration, which that tool's own text states in the words D-673's test requires; limb (a) fails because nothing is built from a document-filing enumeration; and limb (b) fails at its first link, because the derivation's candidate set at this tree — read by running its own `--derive-only` mode — carries no specification at all. **Re-open it by challenging a recorded ground, never by rediscovering the issue**, and an establishment obligation re-opens it by the carve-out alone | [detail](open_items/OI-372.md) |
| OI-373 | ★ **A guard joined the derived guard-candidate population with no authored invocation, so the guard-set runner cannot exit clean — and the batch record reports the set's run counts without it** | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (the guard set's own membership table) | ✅ RESOLVED 2026-08-15 (CC, `cc_instruction_batch_return_rulings.md` Task 2) on the user's ruling of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-373.md) |
| OI-374 | ★ **Four guards print through a stream the shared encoding module does not reconfigure, so the guard runner's captured output follows the ENVIRONMENT rather than the tree — and the artifact's own ground for trusting the run is not true of them** | Found 2026-08-12 (CC, `cc_instruction_resume_lapse_records.md` Task 2) while verifying that task's own movement claim AT THE OBJECTS, which is what put the difference in front of a reader: the two commits diffed as git objects carried one line nobody was looking for, the guard runner's captured output for `tools/open_items_split_check.py` changing from a replacement character to an em dash with the tool unedited between the runs. **THE MECHANISM, read at the two tools.** `tools/audit/output_encoding.py` reconfigures the CALLING process's own standard streams and sets no environment variable, so a child inherits nothing from it; `tools/audit/gen_guard_state.py` runs each guard as a child and decodes its output as UTF-8 with `errors="replace"`. A child that does not reconfigure its own stream therefore encodes with whatever the environment gives it. **THE DEMONSTRATION, run both ways rather than argued:** the bijection check was run twice at this tree, unedited, with only the interpreter's output encoding changed — under the locale codepage its em dash arrives as a replacement character, under UTF-8 as an em dash — and **neither run changed the verdict**, both being `OVERALL PASS`, which is why this can sit in a guard artifact unnoticed. **THE POPULATION IS DERIVED, not listed:** every `*.py` under `tools/` was searched for a call to the shared module and the result compared against the runner's own invocation table; **four members of the run set do not call it** — the open-items bijection check and the notation-seam call-path check, both authored by name from outside `tools/audit/`, and two inside it, the decisions-register check and the finish-line route check. **Whether the other three print a character outside ASCII was NOT tested.** **★ AND THE ARTIFACT'S OWN GROUND IS WHAT MAKES IT MORE THAN AN ODDITY:** `tools/audit/guard_state.json` carries a field whose whole job is to say why its run can be trusted — *"Every guard here routes its printing through tools/audit/output_encoding.py, so a findings list can no longer be truncated by a character the console cannot encode"* — and that sentence is not true of those four, for the reason the sentence itself gives ([[OI-305]], where a check exited non-zero having printed a clean-looking summary and none of what it found). **NOT claimed:** that any verdict moved, since encoding touches captured text and never an exit code; and **the truncation failure mode is NOT demonstrated here** — an em dash is inside the locale codepage, so it degrades silently rather than raising, and whether any of the four would truncate on a character outside it is not tested and not asserted. **NOT in doubt:** every guard's pass or fail; the shared module, which does what it says for the processes that call it; and nothing about the analysis, no measured value, no golden, no corpus of scores, nothing under `tools/robust_stop/` | D — instrument / measurement layer (the guard runner's captured output, and the encoding claim its own artifact makes about every member) | OPEN — rowed 2026-08-12 (CC) under **D-641**, whose whole demand for a finding of this subject is the row: **rowed and left**. **Nothing was changed** — three of the four tools are outside this batch's subject, and adding a call to a shared module is a mechanism change **D-436** reserves, the same ground on which [[OI-373]] authors no invocation. **The two closing acts are stated so neither is rediscovered: (a)** the four route their printing through the shared module, after which the artifact's sentence is true of every member and the captured text stops following the environment; or **(b)** the sentence is bounded to the members it covers, which is the honest smaller act and leaves the exposure where a reader can see it. **Which is done first is not decided here**, and correcting the sentence alone would leave the exposure it describes. **No gating verdict is hand-added here** ([[OI-319]], [[OI-336]]). Cross-ref [[OI-373]] (the row that recorded the condition and left the cause open), [[OI-305]] (the finding the shared module was built against), [[OI-366]] (an establishment check reporting a difference no content change explains — the same shape at a neighbouring tool). **★ THE WORTH TEST WAS APPLIED 2026-08-12 (CC, `cc_instruction_worth_test.md` Task 2) under the user's Ruling 68 of 2026-08-11 AND THIS ROW IS NOT DISCARDED — it stays open and STAYS OWED.** **The #19 carve-out decides it before the two limbs are reached:** this row carries an establishment obligation, and the ruling never discards one whatever its subject. The ground is at the object — `tools/audit/guard_state.json` states why its run may be trusted, and that statement is untrue of four members of the run set, so a gate is trusted on a ground that does not hold, which is the shape **D-647** already classes as #19. **And limb (b) is met independently at ONE member, so the verdict does not rest on the carve-out alone:** `tools/notation_seams/gen_callpath_facts.py --check` is a code-against-record comparability check whose findings go to `sys.stderr`, which the shared module does not reconfigure, and whose failure text is composed from source-file content rather than from ASCII literals — so the findings list is not bounded to ASCII by construction, which is the path [[OI-305]] measured. **NOT CLAIMED:** that any current anchor's failure text is non-ASCII, that a truncation has occurred, or that any verdict ever moved; and the other two of the four were not examined. **Nothing is fixed here** and which closing act comes first is still not decided | [detail](open_items/OI-374.md) |
| OI-375 | ★ **The consequence sentence written into principle #21 carries an UNQUALIFIED argument — "this repertoire" — and the two widths the record already uses for that phrase give it opposite truth values; under the wider one, two members of its own enumerated set hold the material it says is not obtainable** | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | D — instrument / measurement layer (where the ground-truth ceiling principle #21 demands may be obtained) | ✅ RESOLVED 2026-08-12 (CC, `cc_instruction_oi375_width_correction.md` Task 1) on the user's ruling of … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-375.md) |

## G. Submission-era backlog — ✅ TRIAGED 2026-07-13 (`cc_backlog_triage_report.md`); rows either CLOSED here or ADOPTED into A–F/H

| ID | name | description | layer / gate | status | detail |
|---|---|---|---|---|---|
| OI-53 | Tonicization classifier wired, not implemented | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | submission backlog / L5 | ✅ SUPERSEDED 2026-07-13 (verified at code) — live + dormant classifiers exist; the live labeler's limits are … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-53.md) |
| OI-54 | Pedal-point calibration needs corpus evidence | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | submission backlog | ✅ SUPERSEDED — merged into OI-4/OI-38; stays listed until they close → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-54.md) |
| OI-59 | Corelli regressions + 4 deferred tests + chopin cascade | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | submission backlog | ✅ SUPERSEDED 2026-07-13 (built + ran the suites); the #12 carry (a latent cascade fragility) recorded at OI-70 → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-59.md) |
| OI-60 | Blocking/needs-fix trio | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | submission backlog | ✅ SUPERSEDED 2026-07-13 — all three fixed; residue (missing fixtures) rides OI-38 → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-60.md) |
| OI-63 | S7 full mode-prior single-sourcing | A Stage-1 leftover (a dependency-profile call). | substrate hygiene | OPEN — not a triage row; already ASSIGNED (OI-145 wave 2); the harness half rides OI-135 | [detail](open_items/OI-63.md) |

## H. Long-horizon roadmap (held deliberately; listed so they are never "forgotten")

| ID | name | description | layer / gate | status | detail |
|---|---|---|---|---|---|
| OI-64 | Engage plan E1–E5 + gates G1–G6 | The engage arc (wire → A/B → default-ON → retirements → seal); gates G1–G6 (G2 not met at E0). | long-horizon roadmap | OPEN — the arc itself; hold confirmed 2026-07-13, now gated by the Stage-3 entry gate + the key-layer readiness gate | [detail](open_items/OI-64.md) |
| OI-65 | Retirement map R1–R9 | R9 = the chordanalyzer.cpp split, LAST. | long-horizon roadmap | OPEN — hold confirmed 2026-07-13; trigger (E4) + owner still accurate | [detail](open_items/OI-65.md) |
| OI-66 | Recognition consumer (encyclopedia → L5 prior) | Designed-not-built. | long-horizon roadmap | OPEN — deferred; hold confirmed 2026-07-13 | [detail](open_items/OI-66.md) |
| OI-67 | Style-clustering / idiom auto-detection | The cross-tradition harmonic-idiom study has landed in idiom_discovery/; the auto-detection BUILD stays deferred. | long-horizon roadmap | OPEN — deferred; the BUILD hold confirmed 2026-07-13, but the idiom study has landed (the row's source was understated) | [detail](open_items/OI-67.md) |
| OI-68 | Capability tracks A-3/A-4/A-5 + NCT lever + VL axis | A-3 (dominant-implication key evidence) partly promoted to the key-layer build; A-4/A-5 + the voice-leading axis stay held; the NCT-filter lever now has a consumer (OI-55). | long-horizon roadmap / key-layer | OPEN — ★ PARTLY PROMOTED 2026-07-13 — A-3 ASSIGNED to the key-layer build; the rest stay HELD | [detail](open_items/OI-68.md) |
| OI-69 | Joint segmentation (past Stage 5) | Deferred. | long-horizon roadmap | OPEN — deferred; hold confirmed 2026-07-13 | [detail](open_items/OI-69.md) |
| OI-70 | Standing cautions §8 + the Chopin cascade | The B3 dim7-template + rootContinuity sparse-gate dead ends + the Gate-A enharmonic constraint; + re-attempting the key-confidence-gated dominant-quality fix re-triggers the Chopin op30-2 segmentation cascade (latent). | scoring model / standing | STANDING constraints | [detail](open_items/OI-70.md) |
| OI-71 | Roadmap 0.1 doc pass | → ARCHIVED 2026-08-17: the full row is in `OPEN_ITEMS_ARCHIVE.md` | roadmap doc | ✅ SUPERSEDED / DONE 2026-07-13 — layer_architecture_audit.md is tracked (7bc1609159), 0 explorationMode in … → full status in `OPEN_ITEMS_ARCHIVE.md` | [detail](open_items/OI-71.md) |
| OI-58 | Known-Gaps survivors (3 sub-claims) | Tone weight (duration×metric only, one path defaults to 1.0), hardcoded-English cadence labels, an absent piano-sustain-pedal decay model — the three that survive checking; three others closed/re-filed. | held / various owners | OPEN — held; the three owners named (the E4 tone-surface, an i18n pass, the pedal-calibration family); none gates any stage | [detail](open_items/OI-58.md) |
| OI-61 | Future Architectural Considerations (4 held items) | Bridge-file reorganization, instance-vs-static analyzers, HarmonicRegion include-pair friction, the "chord track"→"chord staff" rename; two items deduped into OI-74/OI-48. | long-horizon roadmap | OPEN — a long-horizon hold (4 items); 2 items deduped into OI-74 / OI-48 | [detail](open_items/OI-61.md) |

## I. Siloed analyzed facts (fact-publication sweep 2026-07-10 — `cowork_siloed_facts_audit.md`)

One root cause, one design owner: the shared cross-layer surfaces are voice-blind,
spelling-blind, membership-blind — resolved by the **E4 carry/surface design** (which facts the
shared surfaces publish), not per-site patches. OI-15 (spelling) is the headline instance.

| ID | name | description | layer / gate | status | detail |
|---|---|---|---|---|---|
| OI-72 | StepwiseSignals trapped in decoder membership | Per-note suspension/step/leap (per voice) trapped in decoder membership — the voice-leading evidence L5 selection + pedal need. | siloed facts / E4 surface | OPEN — E4 surface design | [detail](open_items/OI-72.md) |
| OI-73 | Membership verdict dies at the L4→L5 boundary | chordTonePcs/nonChordTonePcs are never copied onto FunctionSlice. | siloed facts / E4 surface | OPEN — E4 surface design | [detail](open_items/OI-73.md) |
| OI-74 | Voice/staff identity dropped at ChordAnalysisTone | The shared tone surface is voice-blind (the structural root of OI-72/73; pedal "upper-voice" has no channel). | siloed facts / E4 surface | OPEN — E4 surface design | [detail](open_items/OI-74.md) |
| OI-75 | keyAlternatives/keyConfidence have zero production consumers | Published-but-unconsumed (consumer named = L5 modulation recompute); the 0.8 gate reads the emission sigmoid, not the better-calibrated boundary margin. | siloed facts / L3→L5 | OPEN — consumed at L5 engage | [detail](open_items/OI-75.md) |
| OI-76 | Cadence detections transient | Recomputed per emit path, never stored on regions; the L5 cadence channel is blocked; the production detector is circular by design. | siloed facts / E4 | OPEN — E4 | [detail](open_items/OI-76.md) |
| OI-77 | Bass-chord-tone/inversion verdict recomputed at ~60 sites | Facts shared, derivation duplicated; channel #1 has no published form. | siloed facts / E4 primitive | OPEN — E4 primitive | [detail](open_items/OI-77.md) |
| OI-78 | declaredMode siloed to the key path | The chord diatonic bonus reads only the inferred key. | siloed facts / design input | OPEN — design input | [detail](open_items/OI-78.md) |
| OI-79 | Duplicated constants (pedal + emission sigmoid) | The pedal sigmoid inlined (2.0/1.5) vs prefs; the emission sigmoid written in two files (S10). | siloed facts / fold at touch | OPEN — fold at next touch | [detail](open_items/OI-79.md) |
| OI-80 | Score-annotation input facts never read | Chord symbols/RN/Nashville recognized as TODO flags, never read. | siloed facts / long-horizon | OPEN — long-horizon | [detail](open_items/OI-80.md) |
| OI-81 | Key runner-up closeness discarded in-analyzer | Folded into a confidence number and discarded in-analyzer; only the batch tool serializes it — the L5 key-consistency channel wants it. | siloed facts / E4 fact-publication | OPEN — E4 fact-publication design | [detail](open_items/OI-81.md) |
| OI-82 | Per-note beat weight / fine metric salience decoder-private | The region-level metric salience is published; the per-note weight is decoder-private. | siloed facts / E4 fact-publication | OPEN — E4 fact-publication design (with OI-72/73/74) | [detail](open_items/OI-82.md) |
| OI-83 | Dispositions declared OK-by-design, kept visible | bothLicensed (Stage-5 fitter telemetry) + the fan-out SUMMARY; the lost uncapped readings behind the summary are the OI-9 distinct-root carry. | siloed facts / declared | DECLARED — no action; the loss itself is OI-9 | [detail](open_items/OI-83.md) |

**Coverage note (user-challenged 2026-07-10): all 17 siloed-facts findings are now mapped** —
1→OI-72, 2→OI-73, 3→OI-15, 4/5→OI-75, 6→OI-81, 7→OI-76, 8→OI-83, 9→OI-9(+OI-83), 10→OI-82,
11→OI-77, 12→OI-78, 13→OI-4, 14→OI-80, 15→OI-11, 16→OI-79, 17→OI-74. Items 1/2/3/10/17 are ONE
design decision (the E4 fact-publication/surface design — dossier Part B); the proposed
"publish-once, consumers-read" corollary awaits ratification (dossier, end of Part B).

*Sweep provenance: session-36 full-repo sweep (12 surfaces, 91 raw items, 11 contradictions) —
the raw sweep detail with file:line citations is preserved in the session record; rows above
consolidate duplicates (e.g. pedal appears once as OI-4 with merged sources). Items marked
MERGED/superseded stay listed until their absorbing row closes.*
