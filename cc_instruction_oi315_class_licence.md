# CC dispatch — the second `src/` licence, bounded by an enumerated class, so OI-315 can close

> **Status: ACTIVE DISPATCH, written 2026-08-13 (Cowork), at a verified STOP** — the previous batch
> completed and Cowork verified it at the objects by explicit hash: the licensed commit touched
> **one path**, `src/composing/tests/regionanalysis_tests.cpp`, and an independent filter over the
> commit object found **no changed line under `src/` that is not a comment**. A2's guard reproduces.
> Nothing is running.
>
> **★ A SECOND COWORK D-253 DECLARATION, for this batch's close.** After the last close was
> written, Cowork made **two shell reads of a repository file** — `head -4` and `head -2` on
> `cc_instruction_oi315_licence_and_cells.md` — **at the user's explicit direction**, to test whether
> a guard he had attempted was in place, on a file Cowork itself authored so that nothing could rest
> on the result. It was not in place; the reads succeeded. **Record them in the close as Cowork's,
> beside the branch-tip lapse already recorded. Not rowed.**
>
> **Read IN FULL, and read FIRST:** `open_items/OI-315.md` END TO END including both halves;
> **Ruling 16**'s held-sibling clause, whose shape this dispatch imports; the previous batch's close.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_oi315_class_licence.md`. Acts dated from
> the clock; **no positional count anywhere**; cite rulings by number, not by date.
>
> **★ Standing rules as adopted, with ONE narrow exception at §0a.** D-253 in every dialect —
> **including that the comment-only check is run at the COMMIT OBJECT, never at a working-tree
> diff**, which is how the previous batch correctly did it. NO TRANSCRIBED VALUES (D-431).
> Hold-don't-guess. **No golden, no test changed or skipped, no corpus of scores, nothing under
> `tools/corpus/` or `tools/robust_stop/`, no measurement, no design, no behaviour change.** D-231
> and #8 stand. **Phase 1's completion statement is not written, not drafted and not partially
> written.** Commit and push per task boundary; `origin` only.

## 0a. THE LICENCE — bounded by a class that is enumerated before it is spent

**The user ratified a SECOND `src/` licence**, on a surface with three options and their objective
ratings, and **named it deliberately as the second in a day.** Its ground: the first licence was
correctly narrow and its siblings are the row's own subject, still true at HEAD inside `src/`;
ruling them outside the row would be the false-completion shape, and waiting for a freeze with no
stated lift condition would gate OI-315 indefinitely on comments.

**THE LICENCE REACHES:** every comment under `src/` **stating the removed piece-start shortcut as
live**, as enumerated and graded by Task 1 — **and nothing else in `src/`, ever, under this
dispatch.**

**IT DOES NOT REACH:** any code line; any comment about a different subject; any comment whose
falsity is not mechanical; and any statement whose falsity is a **homing** question rather than a
falsity in place (#7), which STOPs.

## 0b. THE PREMISE LEDGER (#17a)

**FACT — verified by Cowork at the objects.** The previous batch's three commits and their changed
paths by explicit hash; the comment-only property of the licensed commit, reproduced independently.

**FACT — established by the false-statements pass at the code and NOT re-established here:** the
piece-start shortcut is removed at HEAD, read whole at `resolveKeyAndModeRanked`.

**ASSUMPTION — each checked BEFORE the act resting on it; a refutation is a STOP.**

- **A1.** The class is **enumerated at the code**, not from any row's list. *Check: every candidate
  located at its file; report any the row does not name and any it names that no longer carries the
  statement.*
- **A2.** **Ruling 16's clause governs each member: a sibling whose falsity is NOT MECHANICAL is
  HELD, named, and not edited.** *Check: each member is graded against the established code fact,
  and the grading is stated per member. Mechanical means the comment plainly asserts the shortcut
  as live and the code plainly does not carry it.*
- **A3.** The diff is comment-only, **verified mechanically at the COMMIT OBJECT** (D-253). *Check:
  run after the commit and before the push, as the previous batch did. **If it cannot be made
  comment-only, STOP.***
- **A4.** A flip happens only where the row's closing conditions are met **across both halves**,
  since rule (f) gives a row one state. *Check: state which condition each half met; if the held
  set is non-empty the row stays open and the report says so.*

## 0c. THE TASKS, IN ORDER

**Task 1 — enumerate and grade the class. FIRST, read-only, no commit.** It is D-671's class — a
derivation over a population, published whole or not at all — so it goes first with nothing in front
of it. Enumerate every candidate comment under `src/`; grade each under A2; **report the corrected
set and the held set separately, each member named with its grading.** No edit in this task.

**Task 2 — spend the licence on the graded-mechanical set only (A3).** Correct those members,
**former wording preserved in place (#12)**, putting each correction where the statement stood.
Verify comment-only at the commit object, then push. **The held set is not touched.**

**Task 3 — OI-315's state (A4).** Flip **only** if both halves' conditions are met; otherwise
correct the cell to what is true at HEAD, record the acts in the detail file, and name what remains.
Commit and push.

**Task 4 — the close.** One `STATUS.md` pointer entry per task, nothing else in that file. Append
the close to `cowork_away_returns.md`, **including Cowork's two declared shell reads per the
banner**. Report at the objects, with commit hashes.

## 0d. WHAT IS DELIBERATELY NOT DONE

**No third licence, and no widening of this one** — a member outside the enumerated class is
reported, never corrected. No test is changed, skipped or re-blessed; a comment inside a test file
is still only a comment. No sweep for other stale-comment classes. The `docs/scoring_model.md` pass
with OI-274's banner half is ratified for later; OI-207 is last and alone; OI-321 stays open on its
parked item, which is a user ruling. CC's two standing findings remain held. OI-179 stays OPEN and
GATES.

## 0e. STOP RULES

Halt with a STOP in `cowork_away_returns.md` if: the enumeration reaches a file or a subject the
class's plain reading does not cover; a member's falsity is not mechanical **and** correcting it
would still be needed to close the row; the comment-only verification does not pass; a falsity turns
out to be a homing question; a cell cannot be brought to truth without asserting more than the
commits support; or a guard goes red for a cause that is neither this dispatch's own edits nor
already recorded.

---

*Provenance: Cowork, 2026-08-13. Ratified on a surface carrying three options, each pro and con
naming its principle and each rated against the ultimate objective, with the second-licence-in-a-day
cost named before the ruling. Self-check run before release (D-434).*
