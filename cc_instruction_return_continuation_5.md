# CC dispatch — the fifth return continuation: Rulings 28–35 applied, then the remaining tail through the batch close

> **Status: ACTIVE DISPATCH, written 2026-08-09 (Cowork), at a verified STOP** — the fourth
> continuation closed clean after its Tasks 0–4; all six commits verified ancestors of
> `origin/master`; the guard set verified at ZERO failing; both negative results
> (Ruling 24's comparison staying advisory by its own measurement, Ruling 25's remedy
> refuted by its own both-ways condition) verified as reported. Read IN FULL, and read
> FIRST, in this order: **`cowork_rulings_2026_08_09_fifth_stop.md`** (Rulings 28–35, each
> read WHOLE, D-643), the four earlier 2026-08-09 ruling records,
> **`cowork_away_returns.md`** END TO END (§1.10's measurement table is Ruling 33's input;
> the Task-5 reconnaissance note is Ruling 16's enumeration head start), and
> **`cc_instruction_return_continuation_4.md`** (whose Task 5–6 tail this dispatch resumes;
> its adopted frames govern except where Rulings 28–35 and the task list below differ).
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_return_continuation_5.md`.
>
> **★ All standing rules as adopted: hold-don't-guess into the returns file; D-641
> surfacing; #19 always surfaced; D-253 in every dialect and mechanism; NO TRANSCRIBED
> VALUES (D-431); read-only on the analysis — no behaviour change, no golden, no corpus of
> scores, no `tools/corpus/` or `tools/robust_stop/` movement; no fix to inference, no
> design. TWO licensed acts stand outside the read-only default and each names its ruling:
> Ruling 16's ONE comment-only `src/` commit, and Ruling 35(b)'s bounded explorational run
> to a scratch artifact. Homings in the owning section's own voice (#7, rule (e)); D-230
> same-commit; commit and push per task boundary; `origin` only, NEVER `upstream`. Phase
> 1's completion statement is not written, drafted, or partially written by this batch.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork at this STOP, each at the object named:** the six
fourth-continuation commits and their push state; `tools/audit/guard_state.json` →
`summary` at zero failing; the §1.10 both-ways table
(`tools/audit/oi356_parser_correction.json`) with its three movers; the OI-363 measurement
artifact (`tools/audit/oi357_partial_signature_establishment.json`); the D-580 surface's F8
conflict block; the Task-5 reconnaissance note on Ruling 16's family size; the OI-354
verdict set applied and its guard green.

**ASSUMPTION — checked before the act it licenses, STOP into the returns file if refuted:**

- **A1.** Ruling 33's normalization: the both-ways table shows every row's state unmoved
  EXCEPT the three defect rows, whose corrected states are named in advance (the two
  in-words-resolved rows read RESOLVED; the silently-dropped row is parsed and reports its
  true state); any other movement is a STOP. The lint and the malformed-row STOP are added
  in the same act; the canonical token vocabulary is derived from the states the INDEX
  actually uses, not invented.
- **A2.** Ruling 31's scanner: the musical vocabulary is derived from in-repo sources (the
  specifications' domain terms, the analysis code's domain identifiers); an external
  glossary leg is optional and its absence is stated, never silently substituted. The seed
  inventory is `CLAUDE.md`'s twenty. Separation is measured before anything guards; failure
  to separate leaves an advisory artifact that says so.
- **A3.** Ruling 35(b)'s run: outputs land under `scratch_artifacts/` (untracked) or a
  named `tools/audit/` artifact — NOWHERE under `tools/corpus/`; the comparison is the same
  tool as the committed-outputs pass; corpus-hash and tool-commit stamped (#16);
  predictions are NOT required (explorational scope, #5) but every surprise is rowed.
- **A4.** Ruling 35(a)'s banner edit is ONE region in the evidence document, scope stated,
  pointing at OI-363.
- **A5.** Ruling 28's kind list lands in `cowork_design_doc_template.md` in that document's
  own voice — that one edit is licensed by the ruling; the conformance check reading it
  STOPs on an unlisted kind.
- **A6.** Every population derived at task start from the artifact that owns it; Ruling
  16's family from the sweep, never from the six known sites or the reconnaissance prose.

## The tasks, in order

**Task 0 — the rulings recorded and the small licensed edits (Rulings 28, 29, 30, 32, 34,
35a, 35c).** The kind list into the template (A5); Ruling 29 recorded at the conformance
machinery; Rulings 30/31/32's dispositions recorded on OI-229/OI-230's rows (OI-230 flips
if its three questions are now all ruled — derive, don't assert); Ruling 34 recorded on the
D-580 transfer cell, the fix-plan-input row created for the residual (rule (c)), the
retirement map untouched; Ruling 35(a)'s banner correction; Ruling 35(c) recorded on
OI-363's row. The registration queue extended by the same derivation over Rulings 24–35.

**Task 1 — Ruling 33 executed: the canonical status discipline.** A1's both-ways gate; the
normalization, the lint, the malformed-row STOP; OI-356, OI-361, OI-362 flip on it;
everything downstream of the index regenerated and reconciled.

**Task 2 — Ruling 31 executed: the scanner, corpus-first.** The derived candidate
population with authored verdicts over it (A2), the STOP on unclassified candidates armed;
the full-tree advisory inventory generated (it is Ruling 30's batch input, and its
research-term class implements Ruling 30's two-tier test); separation measured; diff-time
adoption only on clean separation, otherwise advisory-and-says-so. Deliver the inventory's
summary face as a reading file for the user's batch rulings.

**Task 3 — Ruling 35(b) executed: the production-arm bounded run.** A3 throughout; the
comparison artifact beside the legacy one; OI-357 answered in both directions and flipped
or explicitly held with what remains; OI-363 gains the production-arm column; findings
rowed, nothing proposed.

**Task 4 — the standing tail:** the reach-verdict derivation; the section-unreached and
findings-not-rules re-homes; the session-executable gating rows; Ruling 16's sibling sweep
(A6) and its ONE comment-only `src/` commit, diff verified comment-only, OI-353 flipping on
it.

**Task 5 — OI-346's marks (leftover capacity), then final close:** batch summary appended
to the returns file — tasks completed, holds, surfacings, the finish line's end state
derived fresh, and the remaining-distance triage the user asked for (each remaining gating
row sized session-small / session-large / needs-ruling / real-work, AUTHORED and labeled as
sizing, no population carried) — last commit and push.

**Edit authority:** the fourth continuation's list, PLUS `cowork_design_doc_template.md`
(A5's one edit), the evidence document of Ruling 35(a) (one region), the new scanner tool
and its artifacts, the index normalization (Ruling 33's acts), and the Ruling 35(b) run's
scratch artifact. **Anything else: hold.**

## Accepted outcomes

Holds are results; a STOP on any assumption returns to the user with evidence; the scanner
failing separation is a RESULT; OI-357 answered partially is a result; a capacity stop at
any clean task boundary is accepted and recorded as every previous continuation recorded
theirs.

## Self-check (D-434) — run by Cowork before release

Written at a verified STOP with nothing running; every ruling applied is on disk in a
ratified record and ordered read whole (D-643); populations derived at task start; the
ruled acts are the only exceptions to the read-only default, each naming its ruling; the
two run-licences' bounds carried into their tasks' own text; the push hard stop carried;
the completion statement barred; no value restated (D-431); reserved-word discipline
checked over this file's own text.
