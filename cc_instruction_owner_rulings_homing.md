# CC dispatch — apply the forty-eight owner rulings of 2026-08-07 (the homing wave)

> **Status: ACTIVE DISPATCH, written 2026-08-07 (Cowork).** Read IN FULL — and read
> **`cowork_owner_rulings_2026_08_07.md` IN FULL FIRST**: it is the ratified ruling record this
> dispatch applies, entry by entry, and nothing in its table is restated here (#6; D-643 is
> satisfied by reading the record whole, not by excerpting it).
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_owner_rulings_homing.md`.
>
> **★ THE FREEZE IS NOT RELAXED.** No mechanism changes this wave. Everything found is rowed and
> left, except a finding whose subject bears on the analysis, its inputs, or a measurement tool a
> measurement depends on — SURFACED, not acted on (D-641). An establishment obligation (#19)
> always gates and is always surfaced. **OI-345 is NOT worked here** — its fix awaits the user's
> ruling.
>
> **★ NO FIGURES (D-431) AND NO STATE NOT READ (#17a).** No `src/` change, no goldens, no corpus,
> no `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to inference, no
> design. Phase 1 under D-231. **Never bash to read working-tree files (D-253) — and note
> OI-345: the rule covers PowerShell text utilities too; the guard's current blindness to them
> is not a licence.**
>
> **★ Same-commit rule (D-230)** for every register movement. **Commit only when the user asks** —
> leave the tree uncommitted at close and record the worktree enumeration.
>
> **★ Item 2 of the finish line is not started. Phase 1's completion statement is not written,
> drafted, or partially written.**

## 0a. THE RULING LEDGER

**The forty-eight rulings are the table of `cowork_owner_rulings_2026_08_07.md`, ratified by the
user on 2026-08-07 across four decision surfaces**, together with its five family rules and its
authorization notes (the joint-estimator section's creation; the one-edit authorization for
`cowork_engage_arc_plan.md` covering D-568, as it covered D-539). That file is the ruling of
record; where this dispatch and that file could be read to disagree, that file governs and the
disagreement is a STOP.

**Edit authority for this wave, complete list:** `ARCHITECTURE.md` (standing authorization);
`docs/scoring_model.md`, `CLAUDE.md`, `BUILD_AND_TEST.md` (the D-645 licence, homing acts only);
`cowork_engage_arc_plan.md` (the D-568 one-edit authorization); the register data and its
generated files; `OPEN_ITEMS.md` + a detail file where D-497's row check requires one. Nothing
else.

**None of the rulings authorizes a fix to the analysis, a design, or an inference change.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork this session:**

- **F1.** The ruling record itself, written by Cowork from the four ratified surfaces:
  `cowork_owner_rulings_2026_08_07.md`.
- **F2.** The population it covers is exactly the blocker partition's two remaining classes at
  the time of ruling (forty owner-not-determinate + eight no-blocker):
  `tools/audit/decisions/item1_rehome_blocker.json`.
- **F3.** The named STOPs and arm checks are IN the table (D-469, D-625; D-472, D-616) — they are
  rulings' own conditions, not this dispatch's additions.

**ASSUMPTION — each checked BEFORE the act it licenses, with a STOP if refuted:**

- **A1 (per entry).** The destination section exists and STATES RULES (D-430's kind half). →
  checked per entry before writing; an entry whose destination fails the check is NOT homed —
  STOP for that entry, report it with what was found, home the rest. The two entries whose table
  rows already carry this STOP by name (D-469, D-625) follow their rows.
- **A2 (per licensed file).** Every edit of `CLAUDE.md`, `docs/scoring_model.md` and
  `BUILD_AND_TEST.md` is adds-only — no existing line modified or deleted. → checked at each
  file's diff against the committed git object; a violation is reverted before close.
- **A3.** `docs/scoring_model.md`'s staleness check (§2 template count against
  `chordanalyzer.cpp`) is untouched. → run after the edits; a trip is a STOP.
- **A4 (the section creation).** The joint-estimator section is created by the MINIMAL
  structural act: prefer converting the existing standing-rules block into a headed section —
  a heading added, NO existing text moved or reworded — over moving text. → if conversion
  without movement is impossible, STOP and report what the section would require; do not move
  specification text under a homing licence.
- **A5 (the arm checks).** D-472's smoothing pass and D-616's two named mechanisms are checked
  at the code for which arm reaches them, per their table rows, BEFORE the home text is written
  — the LEGACY mark follows the check's result, never the entry's age.

## 1. Task 1 — The joint-estimator section

Discharge A4; create the section; home D-449, D-450, D-532, D-533, D-534 into it per their rows.
This runs FIRST because five rulings depend on the section existing.

## 2. Task 2 — The remaining forty-three, grouped by destination

Apply the table: the `docs/scoring_model.md` group; the `ARCHITECTURE.md` section groups (§3.3,
§7, §6.7, §5.3, §11.5, §2.2, §6, Layers 3/4/5/6 — D-494 SPLIT per its row, D-495 whole); the
`CLAUDE.md` gate block (A) pair; the arc-plan entry (D-568); D-474's field re-aim; D-497's row
check (create the index row + detail file in the same commit if none tracks the application —
register rule (c)). Discharge A1 per entry, A2 and A3 at close of the group edits, A5 where the
rows require it. Every homing in the destination's own voice, defense from the record or
"derivation not recorded", former home/class/verbatim preserved (#12), provenance in register
fields only — never in specification text (the OI-328 contamination shape).

## 3. Task 3 — Close

Regenerate everything that follows the register — the blocker artifact (its remaining classes
should EMPTY except for per-entry STOPs, and the artifact must say which), the routes artifact,
the finish line (its STOP accounts for every entry that leaves item 1). Re-aim drifted anchors
per citation — expect many; per citation, never a uniform shift. Guards at the tree with the
derived list; **fix nothing**. Record the worktree enumeration to a named artifact. Run
`tools/audit/process_check.py` over this dispatch. `STATUS.md` gains one POINTER entry. Leave
the tree uncommitted.

## 4. Accepted outcomes

**Per-entry STOPs are results** — an entry held back with its reason is the mechanism working,
and the count of homed-versus-held is reported in the artifacts, not predicted here. **A4
finding conversion impossible is a STOP for the five section entries and only them.** **A5
finding an arm mixed (one end live, one legacy) is reported with the citation and the entry held
for the user.** **The finish line's STOP firing on an unaccounted entry is a STOP.**

## 5. Self-check (D-434) — run by Cowork before release

- **Ruling ledger:** points at the ruling record read whole; the authority list is complete and
  closed; disagreement between dispatch and record is a STOP in the record's favour.
- **#17(a):** three facts; five assumptions, each with an ordered check and a STOP, A1 applied
  per entry.
- **Principles:** #6 at the no-restatement structure and the family rules; #7 at every
  destination; #12 at every preservation; #22 carried into D-471's homing per its row; D-431 —
  no figure here; D-436/D-641 at the OI-345 exclusion.
- **Sequencing:** the section creation first; the licensed files' adds-only checks at their
  diffs; OI-345 untouched.
