# CC dispatch — the preparation phase's SECOND batch: the sitting's rulings landed, the caller-check RE-RUN under the ruled reading, the deciding-act RECOVERY PASS over the 266, and the RULINGS SORT proposed over the ratified 411 — derivations and record only; nothing discarded, nothing archived

> **Dispatch (Cowork, 2026-08-16).** Executes the rulings of
> `cowork_rulings_2026_08_16_preparation_return.md` (§1 the caller-check ruling, §2 the filter's
> keep-side ratification with its ordered recovery pass and sort) — the second dispatch of the
> PREPARATION phase under its ruled definition
> (`ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §3.1 and §8 wave 1).
> Written at a verified STOP; nothing else is running. **The archiving wave, the empirical
> findings ledger, the fact-gate, the curated boot list, the mining and the landing of the newly
> visible files are NOT in this batch; no soft-discard is executed by it.**
> **Read first (every session):** `C:\s\MS\CLAUDE.md`, `C:\s\MS\STATUS.md` (header only),
> `C:\s\MS\BUILD_AND_TEST.md`. **Then read, in full:**
> `cowork_rulings_2026_08_16_preparation_return.md` (this batch's whole authority);
> your own `cc_report_preparation_opening.md` §3–§4 (the two artifacts this batch extends);
> `cowork_rulings_2026_08_15_batch_return.md` §5 (the presentation rule every generated surface
> obeys); and `cowork_rulings_2026_08_13_eighteenth_stop.md` §2 Ruling 8 (the filter's authority,
> quoted in the ruling record).
> The standing bars bind this batch whole: **no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no decisions-register entry written or
> edited, no register file edited** (the filtering ruling stands over the 266 non-keep entries),
> **no open-items row created, flipped or discarded, no soft-discard executed, no archiving, no
> file moved or deleted, no mining, no landing of the 284 newly visible instruction files or the
> remaining ignored files.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end;
> record both states.** The expected start state: **50 run, 49 passing, ONE failing**
> ([[OI-372]]'s tool, `gen_filing_convention_application.py --check`), zero STOPs, no stale
> report. **A different start state is a STOP-and-report.** Verify every commit after the fact at
> the object and record the SHAs in the close. The reserved-word conventions and the ruled
> vocabulary replacements bind all new prose. **F3's reading rule stands:**
> `reaim_home_anchors.py --check`'s exit code is not evidence; the drift authority is
> `gen_cluster_dispositions.py --verify`'s report. **The new-tool rule stands:** each tool this
> batch adds with a `--check` mode lands WITH its authored run-instruction and its authored
> classification verdict in the SAME commit that adds it. **The E3-ordering rule stands:** no
> graded value is committed before the run that produced it.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling record §1:** *"A naming from a tree-enumerating record does not hold a candidate."*
  The check is re-run under a **derived caller-kind classification** — *"never a naming-breadth
  threshold... and never a hand-typed exemption list"*; *"a caller whose kind cannot be derived
  STOPS to the user rather than being guessed"*; cross-flagged namings *"set aside into their own
  field"*; surviving holders published **by kind**, the prose-citation question *"DEFERRED to
  that evidence"*; *"Nothing is archived by this ruling."*
- **Ruling record §2:** the 411 keep verdicts *"RATIFIED as the live record's confirmed side"*;
  the recovery pass over *"ALL 266 non-keep entries (whole population, D-671)"*, publishing
  *"ACT-FOUND (quoted, located) or NOTHING-FOUND"*; the sort over the ratified 411 as *"a
  PROPOSED classification onto a ruling surface, nothing executed by it"*; and the standing
  clause: a soft-discard record is *"a PROVENANCE verdict and not a judgment on soundness or
  usefulness."*

## Premise ledger

- **FACT** — the previous batch is complete and pushed through `5c384d8966` (verified by the
  writing side at the objects: path sets, numstats, the register's byte-identity, both
  artifacts' populations recounted, the committed guard artifact's summary read at the object).
- **FACT** — the two artifacts this batch extends are the committed
  `tools/audit/decisions_filter_classification.json` (677 entries, 411/182/84) and
  `tools/audit/retirement_caller_check.json` (nine candidacies, all HELD-BY-CALLERS, measured at
  `0a2cc3f86a`), each read by the writing side at the staged snapshot before this dispatch was
  written.
- **ASSUMPTION A1** — the working tree carries NO tracked modification, with
  `cowork_rulings_2026_08_16_preparation_return.md` and `cc_instruction_preparation_second.md`
  untracked. **Check ordered (Task 0, first act):** the sanctioned enumeration; any tracked
  modification anywhere is a STOP-and-report.
- **ASSUMPTION A2** — a caller's KIND is derivable from the record's own machinery: a generated
  file names its generator (the `generated_by`/`generated_for` fields, the guard mechanism's
  generator-to-artifact pairs), and whether that generator enumerates the tracked tree is
  readable at the generator's own source (its use of the tracked-file enumeration). **Check
  ordered (Task 1):** the kind classification is derived from those sources and published per
  caller with its evidence; a caller whose kind the derivation cannot establish joins a
  KIND-UNDERIVABLE list that RETURNS TO THE USER on the surface — never guessed, never a silent
  default; if the list exceeds a workable size that is a finding, not a licence to guess.
- **ASSUMPTION A3** — each of the 266 non-keep entries carries followable citations in its own
  recorded text (a status source naming rows, records or sections; a home). **Check ordered
  (Task 2):** per entry the pass records WHAT it followed and WHERE it searched; an entry whose
  citations resolve to nothing on disk is recorded as CITATIONS-UNRESOLVED (with the dangling
  text quoted) — a per-entry result, not a halt; a malformed entry the walk cannot read at all
  is a STOP.

## Task 0 — land the sitting's ruling record and this dispatch

1. **A1's check first.**
2. Commit, in ONE commit whose message names its own act, exactly these TWO paths and no third:
   `cowork_rulings_2026_08_16_preparation_return.md` (new) and
   `cc_instruction_preparation_second.md` (new, staged plainly). Push. Verify at the object.

**Registered expectation E0:** `git diff-tree` on the Task 0 commit lists exactly 2 paths, both
additions; no staging override of any kind.

## Task 1 — the caller-check RE-RUN under the ruled reading (§1)

1. Extend `tools/audit/gen_retirement_caller_check.py`: classify every caller by KIND, derived
   per A2 — **ENUMERATOR** (a generated artifact, or a surface rendered from one, whose
   generator enumerates the tracked tree), or a non-enumerating caller. Enumerator namings are
   **published as data and hold nothing**. Namings from fellow flagged files (cross-flagged) are
   **set aside into their own field**, like same-class namings — nothing dropped (#12).
2. Re-derive every candidacy's verdict under that reading. For every member still held, publish
   each surviving holder **BY KIND**: a tool reading the file by name / a mandatory-read or boot
   listing / a prose citation — with the evidence line quoted. The prose-citation question is
   NOT decided; the artifact states it as the deferred question, per §1.
3. The KIND-UNDERIVABLE list (A2) is published on the artifact and the surface, returning to
   the user; those callers' namings HOLD in the meantime (the conservative direction).
4. Regenerate `tools/audit/retirement_caller_check.json` by its own generator; the artifact
   names the ruling record §1 as the reading's authority and the commit whose tree was measured.
   **NO file is moved, archived or deleted; a PASSES verdict confers nothing** — the wave's own
   dispatch executes it later, conditions intact.

**Registered expectation E1:** every candidacy re-verdicted under the ruled reading over the
same derived population; every set-aside naming still present in its own field; every surviving
holder carries a kind with quoted evidence or sits on the KIND-UNDERIVABLE list; `--check`
re-derives; no path outside the tool, its artifact and the guard-mechanism records in the
commit.

## Task 2 — the DECIDING-ACT RECOVERY PASS over all 266 non-keep entries (§2)

1. Build `tools/audit/gen_deciding_act_recovery.py`. For EVERY entry the filter classified
   NO-DECIDING-ACT-FOUND or EVIDENCE-AMBIGUOUS (the population imported from the committed
   filter artifact, never restated — #6): follow the entry's OWN cited sources — the rows,
   records and sections its recorded status source and home name — and search those documents,
   plus the dated ruling records where the entry carries a date, for a USER ACT naming the
   entry's subject (a ruling, a ratification, a recorded user direction). Read cited open-items
   rows at their detail files; follow one level of citation (the sources the entry names, and
   the ruling record a found date names), not the whole record — the bound is recorded in the
   artifact.
2. Publish `tools/audit/deciding_act_recovery.json`: per entry, **ACT-FOUND** (the act QUOTED,
   its document and location named, and what it decides in one sentence) or **NOTHING-FOUND**
   (with what was followed and where the search looked), or **CITATIONS-UNRESOLVED** (A3). **The
   pass proposes nothing and discards nothing** — it recovers evidence for the user's ruling.
3. Generate the ruling surface
   `ratification_surfaces/cowork_deciding_act_recovery_surface_2026_08_16.md` from the artifact:
   referents re-explained from scratch (the §5 presentation rule); the ACT-FOUND members with
   their quoted acts; the NOTHING-FOUND members grouped by evidence shape, each with its plain
   restatement carried over from the filter artifact; the banner stating **NOTHING HERE IS RULED
   AND NOTHING HAS BEEN DISCARDED**, and the §2 clause verbatim: an eventual soft-discard is a
   provenance verdict, not a judgment on soundness or usefulness.
4. **The new-tool rule applies** — authored run-instruction and classification verdict in the
   same commit. The register's files stay byte-unchanged (hash-checked, as the previous batch
   proved its own).

**Registered expectation E2:** the artifact covers all 266 (its own counted fields; none
restated here, D-431), each entry in exactly one of the three result classes with its evidence;
the register's rendered and data files byte-identical to their committed blobs; `--check`
re-derives.

## Task 3 — the RULINGS SORT, proposed over the ratified 411 (§2)

1. Build `tools/audit/gen_rulings_sort.py` over exactly the 411 DECIDING-ACT-NAMED entries (the
   population imported from the committed filter artifact — a subset published under a scope
   that NAMES its members, D-671's second half, the ratified keep side). Per entry, propose ONE
   class from the ruled vocabulary (surface §3.1 output (b)): **DESIGN-INTENT** (a decision
   about what the analysis is or does — seed material for the framework phase) or
   **IMPLEMENTATION-MANAGEMENT** (a decision about process, apparatus, workflow, record-keeping
   or sequencing — not design input). The classification rule and its recognizers are AUTHORED
   and published in the artifact beside the verdicts; the evidence per entry is the entry's own
   quoted text; an entry the rule cannot place is proposed **NEEDS-THE-USER**, never guessed.
2. Publish `tools/audit/rulings_sort_classification.json` and generate
   `ratification_surfaces/cowork_rulings_sort_surface_2026_08_16.md` from it — self-contained,
   every NEEDS-THE-USER and every DESIGN-INTENT member listed with its restatement (the
   design-intent side is the payload: it becomes the framework's seed list, so the user must see
   exactly what is proposed to seed). **Nothing is executed; the sort is proposed and awaits the
   user.**
3. **The new-tool rule applies.**

**Registered expectation E3:** the artifact classifies exactly the 411, each in one proposed
class with evidence; the scope block names its population as the ratified keep side of the
filter artifact by citation; `--check` re-derives.

## Task 4 — the close

One `STATUS.md` pointer entry per task and nothing else in that file. The FULL close appended to
`cowork_away_returns.md` (a PREPARATION SECOND BATCH section) — both guard states, every SHA,
every expectation graded, every problem declared, the carried findings restated for the phase's
retrospective (F1–F7, the E3 ordering defect, the A1 premise error — surfaced, none rowed; rows
stay barred). The report file `cc_report_preparation_second.md`, an ordinary tracked file.
Commit, push, verify at the object. **The E3-ordering rule:** the end-state guard run
demonstrated across the commit boundary is taken AFTER the Task 4 commit exists, its output and
the final SHAs landing in ONE FURTHER commit.

**Registered expectation E4:** the end guard state shows every check passing except
`gen_filing_convention_application.py --check` ([[OI-372]]), zero STOPs, this batch's new checks
inside the classified population under authored verdicts, the runner and
`gen_guard_classification.py` both printing their re-derives lines. **Any other failing check is
a STOP-and-report** — no sanctioned red class exists in this batch.

**Registered expectation E5:** at the tree carrying the close, a fresh full guard run re-derives
with the same state — run and read, not inferred, its values committed only after the run.

## What this batch does NOT do

- **No soft-discard executed, no register entry written, edited or retired; no register file
  changes in any commit.** The filtering ruling stands over the 266; the recovery surface and
  the sort surface await the user.
- **No archiving, no file moved or deleted, no mining, no landing** — every retirement flag
  stays a candidacy; re-derived PASSES verdicts confer nothing; the ruled conditions
  (mined-first; members-seen-by-the-user-first) stand.
- **No empirical findings ledger, no fact-gate admission, no curated boot list** — they follow
  once the recovery return is ruled; the boot list is the writing side's draft.
- **No derivation of any specification, no design, no repair, no pilot act, no `src/` edit, no
  golden, no test changed, moved or run, nothing under `tools/corpus/` or `tools/robust_stop/`,
  no measurement of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly
  as found; [[OI-179]] stays OPEN and GATES; `reaim_home_anchors.py`'s F3 defect stays
  surfaced, unfixed and unrowed (both bars stand).
