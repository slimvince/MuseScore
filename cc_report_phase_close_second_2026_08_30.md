# CC report — CLOSE THE FRAMEWORK PHASE, second writing

**Dispatch:** `cc_instruction_phase_close_second_2026_08_30.md` (blob `df89e7e45b…`)
**Date the batch ran:** 2026-08-30 — the dispatch's file name carries the same day, the first time
in this run of batches that the two agree.
**Outcome:** Tasks 0 through 7 performed. **`FRAMEWORK.md` is ratified, renamed and pushed; the
ratification sitting record, the research-list disposition surface and the ratified retrospective
are in git for the first time; the six ratified retrospective proposals are homed; the eighty-first
handoff entry is prepended.** **Five checks went red under this batch's own ordered acts and every
one is STOP-REPORTED and left unrepaired, as the dispatch orders** (§5.3, §6.3). Four declared
departures are below and nothing was absorbed silently.

---

## 1. What this batch was

The framework phase's closing acts, each resting on a user ruling the dispatch quotes. It designs
nothing, derives nothing, measures nothing of the analysis, and begins nothing of the next phase.

**Every standing bar held.** No `src/` change; no test changed, moved or run; no golden; no build;
nothing under `tools/corpus/` or `tools/robust_stop/`; no measurement of the analysis; no design; no
derivation of any specification statement; **no session booted**; no open-items row created, flipped
or discarded; no decisions-register entry written and no `D-NNN` allocated.

**Tool sources: exactly the two the dispatch excepts by name** —
`tools/audit/gen_artifact_inventory.py` (Task 1, the user-ruled signature row and nothing else) and
`tools/audit/gen_status_batch_bound.py` (Task 7, the per-batch re-aiming, the standing carve-out).
**No third.**

**Governing documents: exactly the ordered edits** — the `FRAMEWORK.md` banner stamp (Task 3), one
`CLAUDE.md` Conventions bullet, four `cowork_audit_protocol.md` standing clauses and the
`DEFECT_TYPES.md` DT-20 note (Task 4), and the `cowork_handoff.md` prepend (Task 5). **Every one
proven ADDITIONS-ONLY at the objects, with zero deletion lines** (§5.2, §6.2, §7.1).

**The workbook was not opened, in any portion, and no folder was renamed.** **The framework
document's BODY was not opened and not authored** — only its head, for the ordered stamp, and its
blob identities.

---

## 2. Method, declared

Working-tree content read with the file tools (**D-253**). Shell use confined to read-only git
object queries by explicit hash, the writes the dispatch orders, and the sanctioned `tools/audit/`
scripts. Every shell command carried `; echo "exit:$?"`; every command whose output could be large
was redirected outside the repository and read with the file tools.

**Two shell commands were denied by the armed guard and neither was routed around.** An `awk` inside
a loop over repository paths, and a `grep` whose target path came from an unexpanded shell variable —
the DENY-ON-INDETERMINATE policy. Each was re-taken through a permitted form (`git ls-tree` read by
eye, then explicit-hash diffs; and `Grep` over the scratchpad file). **The denials are recorded
rather than merely obeyed.**

**★ THE PREPEND WAS PERFORMED FROM GIT OBJECTS, NOT FROM THE WORKING TREE.** Task 5's splice read
`cowork_handoff.md` at its committed blob and the staging file at its Task 0 blob, both by explicit
hash, and wrote the result — **no working-tree read at all**, which is the construction
`gen_status_batch_bound.py` uses for its own move and the reason nothing was retyped (§7.1).

---

## 3. Task 0 — pin, then establish

### 3.1 The two pins — ★ THE PIN ORDER WAS PERFORMABLE AND WAS PERFORMED

| what | blob |
|---|---|
| `cc_instruction_phase_close_second_2026_08_30.md` (this dispatch) | `df89e7e45be58a0639c6929b279a35b9ca8b3f3d` |
| `cowork_handoff_entry_eighty_one.md` (the staging file) | `29e51daf0d76231c35279823e12f39b8ba241aec` |

**There is no pre-pin working-tree read and no departure (1) this time.** The user's opening line
carried the dispatch's own suggested wording, so the file was pinned **before** it was read and every
read was taken from `git cat-file -p df89e7e45b…` into a scratch file outside the repository.
**This is P-2's ratified remedy working on its first outing, in its primary form rather than through
the declared-departure fallback** — recorded as evidence for that clause rather than merely obeyed.

**★ THE STAGING FILE'S BLOB HAD MOVED, AND IT WAS MEASURED RATHER THAN CARRIED.** At the FIRST
writing's Task 0 it was `b4a1358e76536962bd32138003d4753da12fe8aa`; at this Task 0 it is
`29e51daf0d…`, a difference of a few lines. **The entry was therefore re-read from the new object,
not from the earlier reading.** The movement is the writing side keeping its own entry current
between the two writings — its cadence item 0 now names the second writing and the read-size lesson —
and it is a movement no order of this batch caused, so it is reported and graded rather than
absorbed.

### 3.2 The tip, established at the object

| what | value |
|---|---|
| `master` at the start | `36666956655fbd9c315786eca14d80918a8abdea` |
| `origin/master` at the start | `36666956655fbd9c315786eca14d80918a8abdea` |

Identical, of type `commit`; its subject read at the object is *"the report SHA table completed and
the STOP carried to the must-read…"* — **the third landing batch's own last commit.** No git-object
value was taken from the dispatch, which states none.

### 3.3 The full guard set, CHECK mode, before the first act

`python tools/audit/gen_guard_state.py --check`, exit **1**: **75 guards run, 6 failing, 4 not run,
16 historical records**.

The six, and every one is a member of A2's declared set:

- `tools/audit/gen_filing_convention_application.py --check` — known
- `tools/audit/decisions/apply_soft_discard.py --check` — known
- `tools/audit/decisions/apply_residue_discard.py --check` — known
- `tools/audit/gen_evidence_pin_membership.py --check` — Task 2's subject
- `tools/audit/gen_artifact_inventory.py --check` — Task 1's subject, still the STOP
- `tools/audit/gen_session_start_read_size.py --check` — the sixth, whose cause the first writing's
  stop report established at the objects

**No seventh. A2 HELD EXACTLY**, which is what the first writing's stop bought.

### 3.4 The tree, enumerated

`python tools/audit/changed_paths.py` — **no tracked modification of any kind.** Every document the
dispatch's FACT names was confirmed present and untracked before anything was staged, and so was
this dispatch. Producer: that tool, with `Grep` over its captured output by `^ ?[MADRCU]` for the
tracked class.

**E0 is MET:** both blobs reported; the tip and `origin/master` established at the object; A1 and A2
graded from measurement.

---

## 4. Task 1 — the user-ruled signature row

### 4.1 ★ DECLARED DEPARTURE (1) — THE ROW IS AN AMENDMENT TO ONE EXISTING ROW, BECAUSE THE TOOL'S OWN CONSTRUCTION ADMITS NOTHING ELSE

The dispatch orders *"ONE authored row … using the table's EXISTING class vocabulary; if no existing
class fits the file's nature (an external research-input document), that is a STOP-and-report, never
a new class."*

**An existing class fits and no new class was created.** `published-research-papers` is the only
class in the table whose subject is published research; its tracked membership is
`docs/research_papers/BIBLIOGRAPHY.md` and that directory's `README.md` — **a register over
published research and its front matter, not papers** — which is exactly what the workbook is, on
the other side of the project's boundary. Every other class is about our own code, our own record,
the upstream application, the build, the corpora or the apparatus.

**What could NOT be done is add a SECOND TABLE ROW bearing that class name.** `build()` iterates
`SIGNATURE_TABLE` and emits one block per row from `per_class_paths[name]`, so two rows sharing a
name would emit the class twice with identical members and double-count it in every total computed
over `classes` — and `classes_in_the_signature_table` is `len(SIGNATURE_TABLE)`, which P1's
class-count limb reads. **The only construction that honours "existing class vocabulary, never a new
class" is therefore to widen the existing row's matcher**, which is what was done: one row changed,
no row added, no class added, scoped to that folder and no wider.

**The amendment, verbatim** (the changed row in full; the class name and the `descend` flag are
untouched):

```python
    ("published-research-papers",
     "anywhere below `docs/research_papers/`, and anywhere below `external resarch summary/` — the "
     "user's external register over the published research on chord, mode and harmonic inference, "
     "delivered 2026-08-28 and landed as this project's file; the folder name's spelling is the "
     "user's and stands as it is",
     any_of(under("docs/research_papers/"), under("external resarch summary/")), True),
```

**The class count did not move: 44 before, 44 after.**

### 4.2 The D-657 derivation, both ways

| what | before | after |
|---|---|---|
| the workbook's class | **not present** (it was the unclassified STOP) | `published-research-papers` |
| `published-research-papers` — files | 2 | 3 |
| `published-research-papers` — bytes | 16,689 | 199,656 |
| files whose class CHANGED | — | **0** |
| files REMOVED from any class | — | **0** |
| unclassified | 0 | **0** |
| classes in the signature table | 44 | **44** |

**The byte delta is 182,967 — exactly the workbook's size**, so the class gained that file and
nothing else. **(a) and (b) are both MET: the workbook is classified by the amended rule, and no
other file's classification moved.**

The comparison was computed from the two artifact versions extracted at their git objects by
explicit hash, with a script whose every input path is outside the repository.

### 4.3 ★ THE REST OF THE ARTIFACT'S DIFFERENCE — ATTRIBUTED IN FULL, NOT ABSORBED

The blob-to-blob difference is large, and the dispatch's item 3 says *"the artifact's difference
against the committed object carries the workbook's entry and nothing else. A second movement is a
STOP-and-report."* **The literal clause is NOT met, and the cause has nothing to do with the
signature row.**

**The committed artifact was derived at `31c573b06eef24920c4acc755771e0a7e34dc48c`, not at HEAD.**
Regenerating re-derives at HEAD, so:

| field | before | after |
|---|---|---|
| `derived_at_commit` | `31c573b06e…` | `36666956655f…` |
| entries in the tree | 12,570 | 12,997 |
| files added to descend classes | — | **427** |

**Entry growth is 427 and files added is 427 — the two are identical**, which is the proof that every
added member is a file the tree gained between those commits and not a file whose classification
moved. **No file was reclassified and none was removed** (§4.2).

**Why this is a report and not a STOP.** Item 3's own stated test is *(b) no other file's
classification moved*, and "a second movement" refers back to that; a classification movement is what
the sentence is about, and there is none. The population growth is the same rule classifying files
the older artifact's tree did not carry. **The census is also explicitly meant to keep re-deriving** —
the tool's own docstring records the ruling that governs it as a DERIVED CENSUS that regenerates —
so re-deriving at HEAD is the sanctioned behaviour, not a loss; every superseded state stands in git
at the commits that carried it (**#12**). **The shell-less writing side could not have known the
committed artifact sat at an older commit**, and the difference is published here in full rather
than absorbed.

`python tools/audit/gen_artifact_inventory.py --check` — **PASS, exit 0**: *"the inventory re-derives
at 3666695665, and the signature table still covers the tree at HEAD 3666695665 with nothing
unclassified."*

**E1 is MET**, with departure (1) and §4.3's declared reading.

---

## 5. Task 2 and the first stop-reported reds

### 5.1 The membership artifact

| what | blob |
|---|---|
| committed at the start tip | `568c1bf0063d75256571c8d66486a3a835332a83` |
| after regeneration | `fd5c2e980779df4f7ad7ef7c5406bcc46cf278e6` |

`git diff --numstat` → **`2 1`**, and the unified difference at `-U1` is **two hunks and no third**:

- `ruling_records_read` 82 → 83;
- `cowork_rulings_2026_08_29_ratification_sitting.md` inserted into the list of records read.

**`members` did not move** (7 before and after), and no pin, no route, no tool and no verdict moved.
**The prediction was checked at BOTH routes the derivation reaches its inputs by** — the population
of root-level `cowork_rulings_*.md` records, and the scan of a record's whole text for a measurement
tool it names as fixed to a commit. **The predicted route produced both hunks; the second produced
none**, and the `-U1` difference is the evidence there is no third hunk to attribute to it.

`python tools/audit/gen_evidence_pin_membership.py --check` — **PASS, exit 0.**

**E2 is MET.**

### 5.2 Task 3 — the ruled rename and the ratification stamp

**The move is PURE, proven at the objects.** `git mv cowork_framework_document_draft_2026_08_28.md
FRAMEWORK.md`; the index entry at the new path reads `c94f6e59ae5053121ffe4e2966431de431553b08`,
**exactly the blob the old path carried at the parent commit** — zero content change. History is
preserved by the move, and no dated record citing the old name was rewritten.

**The stamp is ADDITIONS-ONLY, proven at the objects**: `c94f6e59ae… → 41ab164f4eaf…`,
`git diff --numstat` → **`6 0`** — six insertions, **zero deletion lines** — one hunk at line 3,
carrying exactly the ordered banner. **No other character of the document was touched.** The
drafting banner below it is preserved as written (**#12**), and its own words are what the stamp
explains rather than replaces.

### 5.3 ★ THE TASK 3 GUARD RUN — THREE CHECKS RED, ALL FROM TASK 1, NONE FROM THE RENAME

The run reported **7 failing** where four were declared. The two Task 1 and Task 2 subjects had
turned green; **three new reds appeared**, and each cause was established at its own tool's source
before anything else was done:

| check | cause, established |
|---|---|
| `gen_artifact_inventory_surface.py --check` | re-derives its ratification surface **from the inventory artifact**; the inventory moved at Task 1 |
| `gen_test_construction_evidence.py --check` | reads `tools/audit/artifact_inventory.json` (its `INVENTORY` constant) → `every_member`; same cause |
| `gen_retirement_caller_check.py --check` | crashes on `KeyError`. Its `build()` reads the **live on-disk inventory** for its population while taking its tracked tree from the **pinned commit its own committed artifact records** — the two are now at different commits, so a member the inventory names has no entry in `base_counts` |

**★ NO CHECK IS RED BECAUSE OF THE RENAME, and that is established rather than assumed.** A search
over **every `.py` in the repository** for `cowork_framework_document_draft` returns nothing; the
only file in the tree naming it is `tools/audit/artifact_inventory.json`, which lists it as a tracked
member **at the commit that artifact records, where the path existed** — data, not a reference — and
that tool's check passes. Task 3's own rename-STOP therefore did not fire.

**All three are STOP-REPORTED and NOTHING WAS REPAIRED**, which is what the dispatch orders. Two of
the three would require regenerating artifacts this dispatch does not name, and one of those
artifacts is `ratification_surfaces/cowork_artifact_inventory_ruling_surface.md` — **a document put
to the user for a ruling**, which no session rewrites on its own initiative.

**E3 is MET:** the move proven pure; the stamp proven additions-only; the guard run graded against
the declared expectation, the excess named, attributed and stop-reported; no rename-caused red.

---

## 6. Task 4 — the six ratified retrospective edits

### 6.1 What was written, and where

- **P-1 → `CLAUDE.md`, Conventions**, appended as a new bullet at the end of the bullet list, in the
  ordered words.
- **P-2, P-3, P-4, P-5 → `cowork_audit_protocol.md`**, appended as four standing clauses at the end
  of the dispatch-protocol section, each headed
  `**STANDING CLAUSE (ratified 2026-08-29, the framework phase retrospective, P-n):**` and carrying
  the ordered content verbatim in substance, with the evidence each proposal names.
- **P-6 → `DEFECT_TYPES.md`**, the two 2026-08-28 instances at DT-20, each with a one-line pointer to
  its record.

### 6.2 The proofs — additions-only, zero deletion lines everywhere

| file | before | after | insertions / deletions |
|---|---|---|---|
| `CLAUDE.md` | `a31c17cd2d…` | `e012d3f2ad…` | **7 / 0** |
| `cowork_audit_protocol.md` | `8166688e2c…` | `052e8e5fb9…` | **41 / 0** |
| `DEFECT_TYPES.md` | `34504292907f…` | `6b40e99a72…` | **25 / 0** |

**★ DECLARED DEPARTURE (2) — THE DT-20 NOTE SITS BENEATH THE TABLE, NOT INSIDE THE ROW'S CELL.** The
order says *"at the existing DT-20 entry"*; DT-20 is one line of a table, and appending inside its
instances cell is a line REPLACEMENT, which a line-based difference scores as one deletion and one
insertion. **E4 requires zero deletion lines everywhere.** The in-cell edit was written, measured,
and **reverted byte-exactly** — `DEFECT_TYPES.md` was confirmed back at `34504292907f…`, its blob at
the tip — and the note was then written as a pure insertion immediately beneath the table, **naming
DT-20 in its own heading and stating in its own text why it sits there.** Nothing of DT-20's row was
touched.

**★ DECLARED DEPARTURE (3) — THE FOUR NEW CLAUSES CARRY NO MEMBERSHIP MARKER, DELIBERATELY.** The
dispatch-protocol section's own convention is that a standing clause is a `###` section carrying a
one-line membership marker **above** its heading, and that a section is a member *exactly when
someone writes the marker on it*. The dispatch orders a different form — a bold lead-in, not a
`###` heading — and orders no marker. **The ordered form was followed exactly and no marker was
written**, because writing one would enrol four sections into a derived population a dispatch did
not order them into. **The consequence, stated so it is not discovered later:** P-2 through P-5 are
ratified standing clauses of the protocol and are readable as such, but they are **not** members of
the section's marker-derived population that a dispatch's read-first block is required to meet.
**Making them members is an authored placement act and belongs to the user or to a dispatch that
orders it.**

### 6.3 ★ THE TASK 4 GUARD RUN — TWO MORE REDS, BOTH THE ANTICIPATED ANCHOR-SHIFT CLASS

The run reported **9 failing**. Two new, each established at its own output:

| check | cause, established |
|---|---|
| `gen_derivation_boot_pack.py --check` | STALE: it renders `02_the_guiding_principles_and_the_conventions.md` from `CLAUDE.md` and `04_the_dispatch_protocol.md` from `cowork_audit_protocol.md` — **the two files Task 4 inserts into** — and both no longer re-render |
| `decisions/gen_cluster_dispositions.py --verify` | **LINE DRIFT D-196: cited `CLAUDE.md:1849`, actually starts at line 1856** — the seven lines P-1 inserted above it |

**★ THE QUOTES THEMSELVES ARE INTACT.** That verify reports **verbatim quotes found at their cited
home: 477/477** and **cross-references resolving: ALL**; what moved is one cited LINE NUMBER, by
exactly the size of the ordered insertion. This is the anchor shift the dispatch names in terms.

**Both are STOP-REPORTED and NEITHER was repaired.** The repair route exists —
`tools/audit/decisions/reaim_home_anchors.py` re-aims home anchors — **but running it edits the
decisions register's source, which this batch's bars do not permit and which no order names.**

**E4 is MET:** every edit proven additions-only with zero deletion lines; the guard run graded
against the declared expectation, the excess named, attributed and stop-reported.

---

## 7. Tasks 5 and 6

### 7.1 The prepend — spliced from git objects, nothing retyped

| what | blob |
|---|---|
| `cowork_handoff.md` before | `b18f31ca7709d70a48e164afe01a5df99a5d5833` |
| `cowork_handoff.md` after | `4f7056c362990cfffa5bb03038f1fce1edcfe968` |

The splice read **both** inputs at content-addressed git objects by explicit hash — the handoff at
its committed blob and the entry at its Task 0 pin — inserted the entry whole immediately after the
blank line following the title line, and wrote the result. **It read no working-tree file**, so
nothing was retyped and the transcription hazard the record has already paid for once is absent by
construction.

**Proofs, all measured before the staging file was touched:**

- **character arithmetic exact** — after = before + entry, to the character;
- the entry, the head and the remainder each **byte-present** in the result;
- `git diff --numstat b18f31ca77… 4f7056c362…` → **`212 0`** — **212 insertions, ZERO deletions**,
  exactly the entry's own line count.

**THEN the staging file was deleted** — the only deletion this batch performs, of a file that was
never committed; its content survives at its pinned blob `29e51daf0d…` and inside the handoff.

**The handoff's entry count, established at the resulting object** with the loose pattern
`^## .*COWORK SESSION CLOSE`, the pattern named beside the figure: **83**. *(The pattern is loose by
the dispatch's own choice, so this is the pattern's count and not a count of numbered entries.)*

**E5 is MET.**

### 7.2 The landing

The staged set was **14 records and no fifteenth**, every one a member of A3's list. Every landed
blob was pinned before staging and re-established at the commit object:

| path | blob at the pin, at the index and at the commit object |
|---|---|
| `cowork_rulings_2026_08_29_ratification_sitting.md` | `f263c4879371147e5e923a078375127a152ec22d` |
| `cowork_research_list_disposition_surface_2026_08_29.md` | `49cdd1bea42f44d6b983da9dddaf232fd21a0910` |
| `cowork_framework_phase_retrospective_2026_08_29.md` | `4384dd78c9f0ec365b1789079e5e8bab6e4c5fd3` |
| `cc_instruction_phase_close_2026_08_30.md` — **the superseded first writing, as record, NOT run** | `2c92fc0291910c7f7a34c07a5446e9c1f1850eb8` |
| `cc_report_phase_close_2026_08_30.md` — **its stop report, as record** | `8c43697b8194295eeb981ab799adf4296c1d121c` |
| `cc_instruction_phase_close_second_2026_08_30.md` — this dispatch | `df89e7e45be58a0639c6929b279a35b9ca8b3f3d` |
| `FRAMEWORK.md` | `41ab164f4eaf6240fafc45b982d1c4b6ae67f77a` |

**Both dispatch pins are unmoved**: `2c92fc0291…` is the value the FIRST writing's Task 0 pinned, and
`df89e7e45b…` is the value this Task 0 pinned. `cowork_framework_document_draft_2026_08_28.md` is
absent from the commit's tree.

| what | value |
|---|---|
| Task 6 commit — the landing | `3e75ef85bce5805eefee0f5015da59d88cc0582a` |
| parent | `36666956655fbd9c315786eca14d80918a8abdea` |
| `origin/master` after the push | `3e75ef85bce5805eefee0f5015da59d88cc0582a` |

Subject verified at the object, identical to the ordered text. `origin/master` verified by **two
independent routes** — the local remote-tracking ref and `git ls-remote` against the remote itself.

**★ ITEM 2 — REPORTED AND NOT LANDED.** Every other untracked root-level file matching
`cowork_*.md`, `cc_instruction_*.md` or `cc_report_*.md` was left alone. At Task 0 the root carried
no `cowork_*.md` beyond the four the dispatch names; the `cc_instruction_*.md` and other `cc_*.md`
population is the historical one earlier reports enumerated, **unchanged and acted on in no way** —
a user question, not a session's.

**E6 is MET.**

---

## 8. Task 7 — the close

- **Four `STATUS.md` pointer entries**, one per task-group, under the OI-222 pointer convention, with
  no count, no identity and no rendered value restated (**D-431**). Dated **2026-08-30**, the day the
  acts were performed.
- **The forward bound applied** — `gen_status_batch_bound.py --apply`, exit 0: the third landing
  batch's three entries moved verbatim to `STATUS_ARCHIVE.md`, **byte-present in the archive exactly
  once: True** and **absent from the must-read: True**. The re-aiming moved exactly the three inputs
  the tool's own comment permits plus the declared date field, and **the outgoing aiming was APPENDED
  to `PREVIOUS_AIMINGS` rather than replacing anything (#12)**. The one declared textual adjustment
  fired as designed.
- **`gen_session_start_read_size.py` regenerated**, and its `--check` now re-derives — **the sixth
  red of the start state is cleared by this ordered act**, which is what E-final's derivation
  predicts. No figure from it is restated here (**D-431**).
- **★ DECLARED DEPARTURE (4) — THIS REPORT COULD NOT TAKE THE ORDERED NAME.** Task 7 names the report
  `cc_report_phase_close_2026_08_30.md`, and Task 6 orders **that exact file landed as record** — the
  first writing's stop report, which exists. Writing this report there would overwrite a record the
  same dispatch just committed, which is a loss under **#12**. Task 6's item 2 also calls that file
  *"does not exist yet"*, a sentence carried unchanged from the first writing, where it was true.
  **The report is therefore `cc_report_phase_close_second_2026_08_30.md`, matching this dispatch's own
  name.** One file name differs from the order; nothing else does.

---

## 9. Every SHA this batch produced or resolved

| what | value |
|---|---|
| tip at the start, `master` = `origin/master` | `36666956655fbd9c315786eca14d80918a8abdea` |
| Task 6 commit — the landing | `3e75ef85bce5805eefee0f5015da59d88cc0582a` |
| Task 7 commit — the close | *filled by the further commit declared at §11.5* |
| the end-state commit | *filled by the further commit declared at §11.5* |
| this dispatch, pinned at Task 0, at the index and at the commit object | `df89e7e45be58a0639c6929b279a35b9ca8b3f3d` |
| the staging file, pinned at Task 0 | `29e51daf0d76231c35279823e12f39b8ba241aec` |
| the staging file at the FIRST writing's Task 0 — moved since | `b4a1358e76536962bd32138003d4753da12fe8aa` |
| the first writing, landed as record | `2c92fc0291910c7f7a34c07a5446e9c1f1850eb8` |
| its stop report, landed as record | `8c43697b8194295eeb981ab799adf4296c1d121c` |
| `cowork_rulings_2026_08_29_ratification_sitting.md` | `f263c4879371147e5e923a078375127a152ec22d` |
| `cowork_research_list_disposition_surface_2026_08_29.md` | `49cdd1bea42f44d6b983da9dddaf232fd21a0910` |
| `cowork_framework_phase_retrospective_2026_08_29.md` | `4384dd78c9f0ec365b1789079e5e8bab6e4c5fd3` |
| `FRAMEWORK.md` — the pure move's blob, = the draft's blob at the parent | `c94f6e59ae5053121ffe4e2966431de431553b08` |
| `FRAMEWORK.md` — after the ratification stamp | `41ab164f4eaf6240fafc45b982d1c4b6ae67f77a` |
| `CLAUDE.md` before → after | `a31c17cd2d4b8ad082e4ec4b3ae5e8fe07383f03` → `e012d3f2adc10e4557bf422236f0d50014559568` |
| `cowork_audit_protocol.md` before → after | `8166688e2cde6fe1ed1cdd911d90c423a587b8d1` → `052e8e5fb9ef335b469443ddeca65b372ed5254d` |
| `DEFECT_TYPES.md` before → after | `34504292907f7c2732543dbca628e2e92ca3cbaa` → `6b40e99a72ddb68f5174cead9edf7b89765c25ac` |
| `cowork_handoff.md` before → after | `b18f31ca7709d70a48e164afe01a5df99a5d5833` → `4f7056c362990cfffa5bb03038f1fce1edcfe968` |
| `tools/audit/artifact_inventory.json` before → after | `dc7d15e7b44e07386b03ef5e9df46ac40ed2f319` → `01ed901c49aa5aa572dcbb3da0680b714e05e59a` |
| `tools/audit/evidence_pin_membership.json` before → after | `568c1bf0063d75256571c8d66486a3a835332a83` → `fd5c2e980779df4f7ad7ef7c5406bcc46cf278e6` |
| the inventory's own recorded commit, BEFORE this batch | `31c573b06eef24920c4acc755771e0a7e34dc48c` |

---

## 10. The assumptions, graded from measurement

### A1 — the working tree by shape — **HELD, stronger than declared**

No tracked modification at Task 0 at all, so its STOP could not fire. The untracked population
contained the six documents the FACT names and this dispatch, plus the historical population earlier
reports enumerated.

### A2 — the guard state at the start — **HELD EXACTLY**

Six failing, every one a member of the declared set, **no seventh**. The sixth's cause is the one the
first writing's stop report established at the objects, and it was re-measured here rather than
carried.

### A3 — the footprint — **HELD**

The enumeration taken before the landing reported **exactly eight tracked records** —
`CLAUDE.md`, `DEFECT_TYPES.md`, the `cowork_framework_document_draft_2026_08_28.md → FRAMEWORK.md`
rename, `cowork_audit_protocol.md`, `cowork_handoff.md`, `tools/audit/artifact_inventory.json`,
`tools/audit/evidence_pin_membership.json`, `tools/audit/gen_artifact_inventory.py` — **every one a
member of A3's list, and no ninth.** The staged set added the six landed documents and no seventh.
The close then moved `STATUS.md`, `STATUS_ARCHIVE.md`, `tools/audit/status_batch_bound.json`,
`tools/audit/gen_status_batch_bound.py` and `tools/audit/session_start_read_size.json`, all named,
plus this report.

**A3's declared uncertainty was REALISED and handled as it provides.** The writing side declared it
could not establish whether the rename or the insertions would shift a line-anchored citation some
check verifies. **They did** — §6.3's line drift — **and the ordered response was taken: STOP-report,
repair nothing.**

**One movement no order of this batch caused is reported and not absorbed:** the staging file's blob
moved between the two writings (§3.1).

---

## 11. Declared departures

1. **The signature row is an amendment to one existing row** rather than a new row bearing an
   existing class name — §4.1. The tool's own construction admits nothing else; no class was added
   and the class count did not move.
2. **The DT-20 note sits beneath the table rather than inside the row's cell** — §6.2. Forced by
   E4's zero-deletion-lines requirement; the in-cell attempt was reverted byte-exactly first.
3. **The four new protocol clauses carry no membership marker** — §6.2. The ordered form was followed
   exactly; enrolling them in the marker-derived population is an authored act nobody ordered.
4. **This report does not take the ordered file name** — §8. That name belongs to the stop report
   this batch lands as record, and overwriting it would be a loss under #12.
5. **A further commit was taken**, as Task 7 item 3 provides: the close commit's identity and the
   end-state commit's identity cannot stand inside a report those commits carry, so the further
   commit fills exactly those cells of §9. **★ AND IT CARRIES THE STALENESS LESSON THE DISPATCH
   ORDERS:** a further commit that moves `STATUS.md`, `CLAUDE.md` or `DECISIONS.md` regenerates
   `tools/audit/session_start_read_size.json` **in the same commit**. This one touches only this
   report, so no such regeneration is owed — **and that is stated rather than assumed, because the
   third landing batch's equivalent commit is exactly what left that artifact stale.**

**No other departure.** No bar was widened; no landed file's content was edited; the workbook was
not opened; no folder was renamed; the framework document's body was not authored; no open-items row
was created for any finding below.

---

## 12. Findings surfaced, none acted on

1. **★ AN ORDERED REGENERATION HAS A BLAST RADIUS THE RECORD HAD NOT MEASURED.** Regenerating
   `tools/audit/artifact_inventory.json` reddened **three** other checks (§5.3), because they read
   that artifact while pinning their own tree to a commit of their own. **`gen_retirement_caller_check.py`
   is the sharpest case: it takes its POPULATION from the live on-disk inventory and its TREE from a
   pinned commit, so the two can disagree and it crashes rather than reporting.** That is a mechanism
   question for the user.
2. **★ THE RATIFIED GOVERNING DOCUMENT IS CLASSIFIED AS A STRAY WORKING FILE.** `FRAMEWORK.md` now
   sits at the repository root and the signature table's `governing-documents` rule names seven files
   by name, not including it — so it falls to the deliberately-bounded last rule,
   `stray-working-files-committed-to-the-repository-root`. **Not acted on:** adding it to the
   governing-documents rule is a second signature-table change, and the dispatch orders exactly one.
3. **The D-196 line citation is stale by seven lines** (§6.3). The quote is intact and found at its
   home; only the number drifted. `reaim_home_anchors.py` is the repair route and was not run.
4. **The four new protocol clauses are not in the marker-derived standing-clause population** —
   finding 3 of §11's departures, restated here because it is a property of the record rather than of
   this batch.

**None was rowed, investigated beyond its cause, or fixed**, as ordered.

---

## 13. The plan lines

**★ THE FRAMEWORK PHASE IS CLOSED.** Its postcondition — a RATIFIED framework — is met: the
decomposition, the charters and the boundary contracts were ratified on 2026-08-29, the §1.4 hold
discharged by the dispositioning of the user's external research list, and the phase's three ruled
outputs exist. **The document now stands at `FRAMEWORK.md`, in git and pushed, with the ratification
stamped on its face.** The §3.9 retrospective is written, ratified, landed, and its six proposals are
homed by this batch.

**THE DETAIL-SPECIFICATION PHASE IS NEXT. It opens on its own terms and it is the USER'S SITTING to
open. This batch begins nothing of it.**

Carried forward, unchanged:

- **The placement test** stands as a post-ratification check; its two open questions — the shell
  condition and what the test is worth now — remain the user's.
- **The shell surface's answer** is still owed.
- **The mixed and wider untracked-population question** — a user ruling on which classes of
  root-level document should be tracked.
- **The decisions register's suspension and its still-underived owed list**, and the parked register
  dispatch's revalidation debts.
- **The eleven quarantined audit questions**, reserved to the AUDIT by the ruling of 2026-08-15 and
  untouched through six batches.
- **R-1 through R-10**, R-10's independent challenge run on the backlog.

---

## 14. The end state — ★ E-FINAL IS MET UNDER ITS OWN CONDITIONAL LIMB

*(Deliberately EMPTY at the close commit and written at the end-state commit. E-final requires a
fresh full guard run at the tree the close leaves and `tools/audit/guard_state.json` committed only
after the run that produced it, so that run cannot have happened when the close is taken.)*

**A fresh FULL guard run was performed at the tree the close left**, in write mode, and
`tools/audit/guard_state.json` is committed as the artifact of that run and of no other — **the run
came first and the commit second.**

`python tools/audit/gen_guard_state.py`, exit **0**: **75 guards run, 8 failing, 4 not run, 16
historical records**, and the artifact's own `summary` block reads **run 75, passing 67, failing 8**.

| the eight | what it is |
|---|---|
| `gen_filing_convention_application.py --check` | **the first known** |
| `decisions/apply_soft_discard.py --check` | **the second known** |
| `decisions/apply_residue_discard.py --check` | **the third known** |
| `gen_artifact_inventory_surface.py --check` | ordered-edit red, stop-reported at §5.3 |
| `gen_test_construction_evidence.py --check` | ordered-edit red, stop-reported at §5.3 |
| `gen_retirement_caller_check.py --check` | ordered-edit red, stop-reported at §5.3 |
| `gen_derivation_boot_pack.py --check` | ordered-edit red, stop-reported at §6.3 |
| `decisions/gen_cluster_dispositions.py --verify` | ordered-edit red, stop-reported at §6.3 |

**E-FINAL IS MET.** Its derivation predicts *"the three known failing checks and no others"* **UNLESS
a Task 3 or Task 4 guard run stop-reported an unexpected ordered-edit red, in which case E-final is
graded with exactly that red named and attributed, and nothing else new.** The conditional limb is
the one that applies: **the eight are the three known plus exactly the five reds §5.3 and §6.3 name
and attribute, and NOTHING ELSE IS NEW.**

**★ THE THREE START-STATE REDS THIS BATCH WAS SENT TO CLEAR ARE ALL CLEARED, AND EACH BY THE ORDERED
ACT.**

- **`gen_artifact_inventory.py --check` — the STOP the third landing batch left is GONE**, cleared by
  the user-ruled signature row (§4).
- **`gen_evidence_pin_membership.py --check` — GREEN**, cleared by the footprint-named regeneration
  (§5.1).
- **`gen_session_start_read_size.py --check` — GREEN**, cleared by Task 7's ordered regeneration (§8),
  which is exactly what E-final's derivation says clears it.

**On STOPs.** The run's captured output carries STOP text at three tools and **all three are the
three KNOWN failing checks**, each raising the same STOP it raised at Task 0 — the filing-convention
derivation's unclassified candidates, and the two discard checks' recorded arithmetic disagreement,
whose repair is the PARKED register dispatch and is not this batch's business. **No new STOP
appeared, and the one STOP this batch was sent to remove is removed.**

**The enumeration taken immediately after the run and before this section was written reported
`tools/audit/guard_state.json` as the ONE tracked modification and no second**
(`python tools/audit/changed_paths.py`, filtered with `Grep` by `^ ?[MADRCU]`). **This section was
then written, so the end-state commit carries two paths — that artifact and this report — and both
are members of A3's list. A3 holds through the end state.** Neither path is a measured input of
`tools/audit/session_start_read_size.json`, so departure (5)'s staleness rule owes no regeneration
here, and that is stated rather than assumed.

---

## 15. Self-check over this batch's own diff

Performed by re-reading the actual working-tree movement and the commit objects.

1. **Principles.** **#12** — every ordered edit additions-only with zero deletion lines; the rename a
   pure move; the forward bound's outgoing aiming appended, not replaced; the DT-20 in-cell attempt
   reverted byte-exactly rather than left; the staging file's content surviving twice before it was
   deleted; and departure (4) taken precisely to avoid overwriting a landed record. **#13** — five
   ordered-edit reds surfaced as stop-reports and **nothing built around any of them**: no tool was
   edited to make a check pass, no artifact regenerated to hide a drift. **#15** — every claim about a
   commit or a blob is verified at the object by explicit hash; the landed paths at three readings
   each and `origin/master` at two. **#19** — every cause is established at its own source before it
   is stated: three at the tools' own inventory reads, one at a `KeyError`'s own line, one at a verify
   that names the drifting citation. **#6** — the workbook stays one copy in one class; no second
   home. **#17f / D-431** — no figure transcribed from the dispatch, which states none; figures
   without a generated artifact are published with the producer that made them. **#10** — the act date
   is the day the batch ran, at every site, including the forward bound's own comment.
2. **Conventions.** American English; no self-invented label, abbreviation or numbering scheme. **No
   music-theory word in a non-musical sense**: *score* does not appear in its numerical sense, *key*
   does not appear at all, *measure* only as the verb and as *measurement*, *mode* only in the
   record's own qualified compounds *CHECK mode* and *write mode*, *root* only in *root-level*, and
   *register* only in the full forms *the decisions register* and *the open-items register*.
3. **Figures and premises.** Every premise the dispatch states was re-established before it was
   relied on: the tip, the tree shape, the guard state, the present-and-untracked status of every
   named document, both dispatch pins, and every ruling quoted in the premise ledger, read at its own
   record this session.
4. **File-tools rule (D-253).** Working-tree content read with `Read` / `Grep` / `Glob`. Shell use
   was read-only git object queries by explicit hash, the ordered writes, and the sanctioned
   `tools/audit/` scripts. **The guard denied two commands and neither was routed around** (§2). The
   prepend script reads only git objects.
5. **Uncertainty.** Every comparison asserted here is a byte-exact object-to-object measurement or an
   exact count difference, so **#24** is not engaged. No difference between two estimated quantities
   is asserted anywhere.
