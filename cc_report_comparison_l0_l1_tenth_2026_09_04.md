# CC report — the AUDIT QUESTIONS FOR THE FOUR PER-NOTE FIELD-LIST ROWS, tenth writing: Rows 3.41, 3.42 and 3.48 recorded as carried by question 2, Row 14.9 carrying the question Ruling 80 wrote for it

> **This report decides nothing.** It records what was replaced, what was measured at the objects, and
> what was deliberately not touched. **The placement is a ruling APPLIED, not a judgment made**:
> Ruling 80 (§3ci) settles which questions the audit is handed at these four rows, and this batch
> carried its words. No ruling was taken, no position was weighed, no row block was touched, no
> proposed disposition and no ruled line was edited or deleted anywhere, no numbered item of §11's
> standing list 1 to 82 was edited, deleted, renumbered or reordered, §10 and §12 were not edited at
> all, **no audit question was authored by this batch**, no verdict was attached to the derivation or
> to the deriving session's independence, and no decisions register and no suspension file was written.
> Where this report would name a hash it cannot contain — its own commit, and the end-state commit that
> follows — it points at the git log. **No count of the reading file is restated here** (**D-431**,
> #17f); the counts below are this batch's own measurements at the objects, each named with how it was
> taken.

**Dispatch:** `cc_instruction_comparison_l0_l1_tenth_2026_09_04.md`, pinned at Task 0 to blob
`d0684269d93d169f81d71a33d790d2a4f932f3b6` and proven unmoved immediately before staging.
**Full close:** `cowork_away_returns.md`, the section **THE AUDIT QUESTIONS FOR THE FOUR PER-NOTE
FIELD-LIST ROWS (TENTH WRITING) …**, which carries the commit table, both guard states, E0–E2, A1–A5,
the departures and the plan lines. This report does not restate them; it carries the five replacements
with their anchors, the deleted-line enumeration, and the object-level check of Task 1.

---

## 1. What was done

**Five sentences were replaced inside one subsection of §11. Nothing else in the file moved.**

- **Task 0** landed `cowork_rulings_2026_08_31_decision_surface_sitting.md` (Ruling 80, additions
  only), `cowork_handoff_entry_one_hundred.md` and this dispatch — **three paths**, because A3 was
  measured first and the membership artifact had not moved.
- **Task 1** made the five ordered sentence replacements in one commit: the subsection's preamble
  clause, and the entries for Rows 3.41, 3.42, 3.48 and 14.9.
- **Task 2** closed: the three `STATUS.md` pointer entries, the forward-bound move, the regenerated
  read-size artifact, the close section and this report.

**Nothing else was touched.** The derivation is byte-unchanged, banner and body; the derived
specification is byte-unchanged and was never opened; no outgoing text was opened; §10 and §12 are
byte-unchanged; no decisions register, no suspension file, no open-items row.

## 2. What Ruling 80 placed, and that this batch composed nothing

**Rows 3.41, 3.42 and 3.48** are recorded as carried by §11's **standing numbered question 2**, Row
1.3's, in the form §11's own opening convention already carries — *"several questions carry more than
one row where the same question is met a second, third or fourth time and the row says so."* Each of
the three entries now names which time it is, and gives the ruling's own reason for the row belonging
to that question.

**Row 14.9** carries **one further audit question of its own**, written by Ruling 80 and quoted from
it: does the arm that ships carry the tie facts the target list of `cowork_target_architecture.md` §2
has named all along, and if it does not, does any consumer depend on their absence? The entry states
why it is distinct from question 2 — question 2 asks whether the facts S-3 requires reach any consumer
in the code, and this asks after a fact the architecture itself already required and the build does not
carry.

**NO AUDIT QUESTION WAS AUTHORED, PARAPHRASED, EXTENDED OR MERGED BY THIS BATCH.** The one new question
is Ruling 80's, quoted; the three placements are Ruling 80's, quoted. **Question 2 itself is pointed at
and not copied**: it gains no cross-reference and no appended clause, which the ordered numbered-item
comparison below proves rather than asserts.

## 3. The preamble clause, amended with its former wording preserved

Ruling 80's ordered consequence is that the subsection's preamble stops being true of itself the moment
Row 14.9's entry carries a question, so the clause is corrected **in the same act**. Its former
wording — *"Where no audit question is written at the row, the entry says so and composes none."* —
**stands verbatim at the site (#12)**, inside the amendment that replaces it. The amended clause records
**both** limbs the ruling makes, because either limb alone would leave it false of some entry: where the
ruling that quarantined a row wrote no question, the entry carries either the question the placing
ruling writes, naming that ruling, or the standing numbered question above that the placing ruling
records as carrying the row. **No other sentence of the preamble was touched, and the count of entries
in the subsection did not move.**

## 4. The five anchors, found verbatim and proven unique

Every anchor the dispatch quotes was found **verbatim** at the file, matched on its whole quoted block
rather than on a fragment, and each proven unique **before any edit**:

| Anchor | Lines quoted | Occurrences at the file |
|---|---|---|
| the preamble's last two lines, ending `none.*` | 2 | 1 |
| Row 3.41's entry | 5 | 1 |
| Row 3.42's entry | 4 | 1 |
| Row 3.48's entry | 4 | 1 |
| Row 14.9's entry | 6 | 1 |

The entry-opening fragment `- Row N.M — **no audit question` occurs **exactly four times** in the whole
file — once each at Rows 3.41, 3.42, 3.48 and 14.9 — so **the dispatch's own STOP for a fifth such
entry did not fire**, and the four are exactly the four it names. The line `none.*` standing alone
occurs **exactly once**. The five replacements, which the dispatch says stand or fall together, **all
stand**; no placement was left unmade.

## 5. The deleted-line enumeration, published

The diff between the two blobs is **31 insertions and 21 deletions**. Every one of the twenty-one
deleted lines belongs to one of the five anchors, and the arithmetic closes:

| Anchor | Deleted lines | Added lines in its replacement |
|---|---|---|
| the preamble's last clause | 2 | 5 |
| Row 3.41's entry | 5 | 6 |
| Row 3.42's entry | 4 | 5 |
| Row 3.48's entry | 4 | 5 |
| Row 14.9's entry | 6 | 10 |
| **total** | **21** | **31** |

**2 + 5 + 4 + 4 + 6 = 21**, which is the whole deletion count, so **no deleted line falls outside the
five anchors anywhere in the file**. **5 + 6 + 5 + 5 + 10 = 31**, which is the whole insertion count,
so no added line falls outside the five replacements either.

**No count of changed passages is offered as a check.** The diff carries two changed passages; the
ninth batch's own close established that two nearby edits merge into one, so the property that carries
the weight here is the enumeration above and not a passage count.

## 6. The object-level check of Task 1, measured rather than asserted

The edited reading file was written into the object store with `git hash-object -w` (blob
`f3edcfe810b96ec6bfde07af2ba213564c59add4`) and diffed **between two explicit blob hashes** against the
base blob `a025c686cb652ad871bf12b7cec140023ce92609`. Both blobs were then materialised to scratch
paths outside the repository with `git cat-file blob <hash>`, and every structural comparison was taken
over those two content-addressed copies.

1. **Every deleted line is one of the five anchor blocks** — §5 above, enumerated and matched in full.
2. **No `###` heading added or removed** — 264 on each side; no `##` heading either — 283 on each side.
3. **No `**Row` heading added or removed, proven rather than eyeballed**: the strict row-heading line
   set was extracted from both blobs, sorted and diffed — **817 on each side, empty both ways**. **No
   `*PROPOSED DISPOSITION` or `*RULED DISPOSITION` line touched** — 757 and 249, identical on both
   sides.
4. **No numbered item's leading number changed**: every numbered-item opening line was extracted from
   both blobs **in file order** and compared — **300 on each side, the ordered comparison empty**,
   which is stronger than a set comparison because a permutation would still show.
5. **§13's tables, §13a's tables and figures, and §13b's three tables byte-identical**, proven over the
   whole file rather than a line range: every table row extracted from both blobs, sorted and compared
   — **901 on each side, the two sets diff empty**.
6. **The subsection's bullet count is nine on both sides** — Rows 2.16, 2.17, 3.5, 3.41, 3.42, 3.48,
   4.3, 14.9 and 17.13 — and the whole-file count of lines opening `- Row ` is 191 on each side, so no
   bullet was added or removed anywhere.

**A difference none of those accounts for would have been a STOP; there is none.** The five entries of
the subsection this batch did not touch — Rows 2.16, 2.17, 3.5, 4.3 and 17.13 — are byte-unchanged,
which the deleted-line enumeration proves rather than asserts.

## 7. The five row blocks of §6, read to check this dispatch's quotations

The dispatch's replacement text makes four claims about the rows. Each was checked at that row's own
block in §6, which was **read and not edited**:

- **Row 1.3** carries §11's question 2 as its own audit question, in the row's own words, and its ruled
  line is Ruling 62 (§3bq, item 4).
- **Row 3.41**'s current-text axis line reads *"the same difference as Row 1.3, stated there"* — the
  quotation the replacement makes.
- **Row 3.42** enumerates the eleven fields and records as absent exactly the five facts question 2
  asks about, adding `staff-eligible`, which S-3 does not name.
- **Row 3.48** is the time clause of that same list — *"All times are absolute time-positions within
  the piece"* — and what it lacks is the bar index and the within-bar offset, which is one of question
  2's own five facts.
- **Row 14.9** quotes `cowork_target_architecture.md` §2's target list of twelve fields and records
  that it carries `ties`, which the as-built eleven do not — the fact Ruling 80's new question is
  about.

**All four quotations hold.** Nothing in §6 was edited; reading is not editing.

## 8. What was measured, not assumed

- **The declared start state held exactly.** A full guard run **in `--check` mode, which the dispatch
  orders**, before the first edit: **76 run, 10 failing, 4 not run, 16 historical, zero STOPs** — the
  ten failing being the ten the dispatch's own FACT names, member for member, with no eleventh red.
  **The mode was named at the tool rather than probed for**, and the artifact was hashed afterwards
  rather than assumed unmoved: `guard_state.json` came back at
  `497e802bac1bcc1715e7f87a80e5854d625eb098`, the very blob the dispatch names, so the check mode wrote
  nothing.
- **A1 was checked over the whole tracked population** with the sanctioned enumeration tool: **exactly
  one** tracked modification at boot — the sitting record — which is what A1 declares, and both named
  untracked paths present. The large standing untracked root population was met exactly as A1 names it
  in advance and none of it was landed.
- **The sitting record's additions-only property was proven at the object**, between two explicit blob
  hashes: one changed passage, **165 insertions, ZERO deletions**, every added line inside the new
  `## 3ci.` section, the trailing context the `## 4.` heading. The Task 0 commit's own stat confirms it
  independently — three paths, 1,062 insertions, zero deletions.
- **A3 was measured before being accepted:** the membership artifact regenerated and its blob hash
  compared against the committed one — **byte-identical** (`fb3a00be…` both sides) — so it was **not**
  committed and Task 0 carried **three** paths rather than four.
- **The forward bound was measured, not assumed:** re-aimed at all five authored inputs with
  `PREVIOUS_AIMINGS` appended to rather than replaced, run, and the dispatch's prediction held exactly
  — **the ninth writing's three entries moved, the second writing's two stayed, no STOP** — with the
  tool's own reconciliation green in both limbs and `--check` passing after the act.
- **A2's watched exceptions did not fire.** `gen_l0_l1_outgoing_population.py --check` still re-derives
  after the reading file and `STATUS.md` moved — no member of the population moved and the artifact
  needed no regeneration — and `gen_session_start_read_size.py --check` is green, regenerated after the
  last edit to `STATUS.md`.
- **Every commit was verified at the object** by `git show --stat` at its explicit hash, and
  `origin/master` read at its ref file with the file tools after each push, never from a push's own
  output. The Task 1 commit's own blob for the reading file was read back at
  `git rev-parse <commit>:<path>` and is the blob the check above was taken over.

## 9. What this batch did not do

No ruling taken, no position weighed, no verdict on any open question, on the derivation, or on the
deriving session's independence. No session booted. No measurement of the analysis built or run. No
derivation of any statement about the analysis. No edit to the derivation (banner or body), the derived
specification, the brief, the pack, its generator, its artifact, the population tool, any outgoing
text, the suspension file, any register source, or any governing document other than the two Task 2
orders. In the reading file: **no row block touched**; **§10 and §12 not edited at all**; no numbered
item of §11's list 1 to 82 edited, deleted, renumbered or reordered; no proposed disposition and no
ruled line deleted anywhere; §13's tables, §13a's figures and §13b's three tables untouched; no entry
added or removed from the subsection. **No audit question authored, paraphrased, extended or merged.**
No `src/` change, no golden, no test changed, moved or run, no build, nothing under `tools/corpus/` or
`tools/robust_stop/`. No open-items row created, flipped or discarded; no register entry and no
identifier allocated; no guard enrolment; no tool added. **No recommendation appears anywhere** — not
in the reading file, not in the close, not in this report, not in a commit message.

The one `tools/audit/*.py` edit is the re-aiming of `gen_status_batch_bound.py`'s five authored
constants, its `PREVIOUS_AIMINGS` list and its head comments, which Task 2 orders in terms and which
**D-648** classes as authored-input maintenance.

## 10. Declared departures

1. **The dispatch was read from the working tree before it was pinned** — P-2's own fallback route.
   Pinned at Task 0, blob proven unmoved before staging.
2. **Diff, guard and check output was written to scratch paths outside the repository and read with the
   file tools.** Every repository read went through the file tools; the only shell reads were git object
   queries by explicit hash, some piped to `grep`, `sort`, `diff` and `wc` to count and compare
   object-level line sets over scratch copies.
3. **★ The two blobs were materialised to scratch with `git cat-file blob <hash>` and compared there.**
   Both sides of every structural comparison were taken over those two content-addressed copies. It is
   a read by explicit hash on both sides, which is what makes the comparison self-verifying; declared
   because the reads that followed were ordinary text utilities over those copies.
4. **★ Three commands were denied by the armed shell-read guard and re-taken.** The first named a
   scratch file by a relative path after a `cd`, the second and third through a shell variable; the
   guard refused on its deny-on-indeterminate policy (**D-647**). All three were re-taken with fully
   literal absolute scratch paths. **Recorded because the guard firing is the mechanism working.**
5. **★ One `awk` invocation over the two scratch copies returned an empty count and was abandoned
   rather than debugged.** Its subject — the subsection's bullet count — was then established directly
   at the file with the Read tool, and whole-file by the `- Row ` count over both blobs. It read nothing
   inside the repository and wrote nothing; declared because it is an invocation this batch made.

## 11. Where the hashes are

The commit table — the tree met, Task 0, Task 1 and the close — is in the close section of
`cowork_away_returns.md`, each by explicit hash, each pushed and each verified equal at
`.git/refs/remotes/origin/master` **with the file tools**. **This report's own commit and the end-state
commit that follows it are in the git log**, which is where a document cannot name its own identifier.
**The close does not assert the end state**; the one further commit carries it.
