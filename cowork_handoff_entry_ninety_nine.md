## COWORK SESSION CLOSE (NINETY-NINTH ENTRY, 2026-09-04 — THE CONTINUING LINE) — ★★ **THE SEVENTH AND EIGHTH BATCHES ARE BOTH VERIFIED AT THE OBJECTS. ROW 4.3's SURFACE WAS PUT, THE USER RULED IT, RULING 78 (§3cg) WAS TAKEN AND LANDED, AND THE PLACEMENT IS MADE: ROW 4.3 IS QUARANTINED AT THE READING FILE, THE §13b PARAGRAPH IT FALSIFIED IS CORRECTED WITH ITS FORMER WORDING PRESERVED, AND §13a CARRIES ITS FOOT NOTE. WITH IT, EVERY ROW OF RULING 62's ITEM-4 SET CARRIES A PLACEMENT.** ★ **THE TIP IS `28c465e9c3467f4120bc658fb3ab10bc7489e17d`; `origin/master` EQUAL; NOTHING IS IN FLIGHT; THE TRACKED TREE IS CLEAN; `cowork_handoff_entry_ninety_nine.md` IS THE ONLY UNTRACKED FILE OF THIS WORKING LINE AND THE NEXT DISPATCH'S TASK 0 LANDS IT — NEVER YOU. THE ROOT ALSO CARRIES A LARGE STANDING POPULATION OF UNTRACKED HISTORICAL `cc_*` DISPATCHES AND REPORTS, WHICH IS PRE-EXISTING, IS NOT THIS LINE'S BUSINESS, AND WHICH NO DISPATCH OF THIS LINE LANDS.** THE CURRENT ENTRY POINT; the ninety-eighth entry is superseded as entry point and stands otherwise.

You start clueless. Read this block first, then perform the ordinary session-start read: `CLAUDE.md`
whole, `DECISIONS.md` whole, `STATUS.md`, and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`). **A
single-file opening instruction is not an exemption from that read** (P-1). **The eighty-second
through ninety-eighth entries remain binding except where this entry names a change**, and every
standing bar of the earlier blocks remains binding.

**On form.** Short. In conversation with the user, plain ordinary English — he ruled on 2026-08-28
that the record style stays in the records. **NEVER use the multiple-choice question widget with him**
— he ruled on 2026-09-04, after one was used, that decision surfaces and choice questions go as plain
text in the message, *"fact-checked/based, with alternatives weighed against ultimate objective and
guiding principles, in uncompressed plain language"*. A decision surface is fact-based and checked at
the objects, every position weighed towards the ultimate objective and towards the guiding principles,
one decision per turn, the choice question in the LATER turn (D-249, Ruling 1, Ruling 21); **when the
facts have collapsed the alternatives to one, say so plainly rather than presenting a menu** (his
2026-08-28 bar). **A "go on" is never a ratification** — this session met one immediately before the
choice question and did not read it as the answer. **Check at the object before you write a fact;
source-check a dispatch at the objects before releasing it — and NEVER take a fact about a tool from a
previous dispatch's statement of it.** Read a staged repository file with the file tools, never with a
shell and never with an interpreter in the sandbox. **Read Claude Code's close and report WHOLE and
verify at the objects before believing any of it** — this session did, twice, and both times what it
found was worth the reading.

### What this session was

The session that booted on `cowork_handoff_entry_ninety_eight.md`, performed the full session-start
read with the file tools (`CLAUDE.md` all 1,864 lines, `DECISIONS.md` all 862 lines, `STATUS.md`, the
gating answer's field at the file), and then did four things.

**It verified the seventh batch at the objects** before writing a word: both ref files; the chain
`c09f3fa9ea… → 3b30aadb3e… → 507e2fbb10… → cbb059bc19… → 56dcec1434… → 7a5f307563…` at
`git show --stat` by explicit hash; the reading file's Task 1 commit read as **285 insertions and zero
deletions**; the successor's Task 2 diff read whole as exactly two changed passages; §13b's three
tables checked against the placement lines **counted at the file by this side** (38 kind A, 13 kind B,
51). **It put Row 4.3's decision surface** as user-visible text, answered the user's question about
what the placement costs, put the choice question in a later turn as plain text, and **took Ruling
78** on his words *"I agree with your recommendation."* **It wrote the ruling into the sitting record
and wrote the eighth dispatch**, then fact-checked the dispatch at the objects on the user's own
instruction and corrected four things in it. **It then verified the eighth batch at the objects**: the
chain `7a5f307563… → e10acd807d… → 0e6649fd95… → cf268270cb… → 28c465e9c3…` by explicit hash; the
reading file's diff taken between the two explicit blob hashes and read — **three changed passages at
old lines 2470, 16046 and 16102, twenty insertions, exactly three deletions, and the three deleted
lines the §13b paragraph the replacement carries verbatim inside itself**; the placement line, the
§13b replacement and the §13a note each read at the file; the end-state guard summary read at the
object. It did not compact.

### ★ THE STATE YOU INHERIT

- **The tip is `28c465e9c3467f4120bc658fb3ab10bc7489e17d`**, `origin/master` equal, both read at the
  ref files with the file tools. **Re-verify before relying on it.**
- **The tracked tree is clean.** `cowork_handoff_entry_ninety_nine.md` is the only untracked file **of
  this working line**, and the next dispatch's Task 0 lands it. **★ THE ROOT ALSO CARRIES A LARGE
  STANDING POPULATION OF UNTRACKED HISTORICAL `cc_instruction_*` AND `cc_*_report` FILES — MEASURED,
  NOT ASSUMED**, by `git ls-files --others --exclude-standard --directory` filtered to root-level names.
  It is pre-existing, no dispatch of this line lands it, and **an untracked-file check at the root will
  return hundreds of names**: do not read that as a defect, and do not read this entry as saying it is
  alone there. Every A1 this line writes names its untracked paths BY NAME for exactly this reason.
- **★ HOW TO TELL A RUNNING BATCH FROM A FINISHED ONE, stated because the ninety-eighth entry did not
  and a booting session needs it.** A batch commits and pushes per task, so a tip that has moved is not
  by itself a finished batch. **The batch is FINISHED only when `cowork_away_returns.md` carries its
  close section AND one further commit after it carries the end-state guard artifact** — the close
  never asserts the end state. A moved tip with no close section for that writing means the batch is
  **mid-flight**: write nothing into the tree, write no dispatch (D-251, P-5), tell the user, and wait.
- **The eighth batch, verified: what it did.** Task 0 (`e10acd807d…`) landed the sitting record's
  Ruling 78 — **108 insertions, zero deletions, one changed passage, all inside the new `## 3cg.`
  section** — with the ninety-eighth entry and the dispatch, three paths, the membership artifact
  measured byte-identical first. Task 1 (`0e6649fd95…`) made the three ordered edits at the reading
  file, one path. The close is `cf268270cb…`; the end state is `28c465e9c3…`, whose artifact reads
  **76 run / 66 passing / 10 failing / 4 not run / 16 historical**, the ten failing the same ten by
  name, no eleventh red. The forward bound ran and its prediction held: the seventh writing's four
  `STATUS.md` entries moved, the second writing's two stayed.
- **Row 4.3 is QUARANTINED**, its line at the foot of the row carrying Ruling 62's own words as the
  ground and the audit question in one sentence as §5 requires. Its proposed disposition stands beside
  it, unedited. **With it, every row of Ruling 62's item-4 set — 1.3, 3.31, 3.41, 3.42, 3.48, 14.9 and
  4.3 — carries a placement.**
- **The plan stands at:** phase OPEN → derivation LANDED → comparison COMPLETE → dispositions: Rulings
  34–63 → application act RAN (fifth) → ratification act TAKEN: Rulings 64–77 → ratification edits
  APPLIED, the specification RATIFIED (sixth) → the item (d) family placements MADE (seventh) → **Row
  4.3 RULED AND PLACED (eighth)** → **the pack finding of §3al.4 to L2's pack** → **L2, sequenced by
  Ruling 10**.
- **Nothing is owed by this session to any batch.** One ruling was taken (78) and is landed; one
  decision surface was put and is answered; one dispatch was written and has run.

### Facts established this session that the next dispatch needs (each checked at its object)

- **★ `gen_guard_state.py` DOES NOT RECOGNISE `--help`, AND SILENTLY SELECTS ITS DEFAULT WRITE MODE.**
  The eighth batch's start-state run was taken that way and is declared at its close as departure 3.
  It cost nothing here — the artifact it wrote hashed byte-identical to the committed blob at the tip,
  `5ede2469…`, so no tracked path moved — **but a session probing that tool with `--help` WRITES the
  guard artifact instead of being told the flag is unknown.** Any dispatch ordering a `--check`
  start-state run should say so at the tool, and any session that runs it must hash the artifact
  afterwards rather than assume nothing moved.
- **★ THE SEVENTH DISPATCH's STATEMENT OF `gen_evidence_pin_membership.py` IS INCOMPLETE, AND THE
  CORRECTED FORM IS ON THE EIGHTH DISPATCH's FACE.** That dispatch says the tool reads, of a ruling
  record, *"only its LEADING BLOCKQUOTE and its paragraphs opening `*Provenance:`"*. Read at the tool's
  own source: **route A** reads exactly those two blocks, but **route B reads EVERY LINE of the
  record** — for each line containing `PINNED` in any case it resolves any tool name on that line, and
  a resolved tool becomes a member. **So a ruling-record edit moves the membership artifact if it adds
  a line carrying the word *pinned*, wherever in the file it sits.** Ruling 78's section carries none,
  which is why A3 predicted zero and the measurement agreed.
- **★ THE READING FILE's §11 AND §12 ARE STALE AGAINST THE RULED DISPOSITIONS, AND THIS IS NOT PURE
  APPARATUS. IT IS THE ONE ACT OWED ON THIS FILE.** Both open *"★ COMPLETE OVER THE POPULATION"* and
  were derived from the PROPOSED dispositions. Rows **3.41**, **3.42**, **3.48** and **14.9** were
  quarantined at their rows by the fifth writing and are not in §11; **Row 4.3 is now the fifth.**
  **The audit reads §11**, so a quarantined row whose question is not there is a question the audit
  does not meet. **What closes it is ONE re-derivation of both sections against the ruled
  dispositions**, as its own scoped act; the eighth dispatch made attempting half of it a STOP, and
  neither half was attempted.
- **`tools/audit/changed_paths.py` CANNOT BE RUN FROM THE COWORK SIDE.** It exceeds the bridge's
  command window (measured twice, the second time with a 170-second inner limit the bridge still cut at
  120). **So the whole-population A1 check is Claude Code's alone**, and the writing side declares A1
  by content with its bound stated (#24). The eighth dispatch's bound was the seventh close's own
  enumeration, this side's act-list, and a per-path hash comparison against the tip for the eight paths
  the batch depended on; CC's own enumeration then established it exactly.
- **★ ONE LINE OF THE LOOSE `**Row` SHAPE NOW STANDS IN §13b, AND IT IS INERT — MEASURED, NOT ARGUED.**
  The §13b replacement the eighth dispatch ordered verbatim opens `**Row 4.3 is PLACED, …`. It is prose
  inside §13b, carries no em dash, and stands in no row block, so the strict row-heading set is
  unchanged, which CC proved both sides. **This side then checked whether any tool matches the loose
  shape: `git grep -F '**Row'` over `tools/*.py` at the tip returns nothing.** No check in the tree
  trips on it. It is recorded so that a future check written on the loose shape is written knowing this
  line exists.
- **Ruling 62's closing phrase mis-states Row 4.3's prior disposition** — *"as the reading file already
  has 1.3 and 4.3"* is true of 1.3 and untrue of 4.3. Read the other way, as naming which rows are in
  scope, it is sound, and **nothing turned on which reading governs.** **D-674 answers what to do
  without a ruling** — a document its two branches do not decide is REPORTED, not bannered or rewritten
  by stretch. Reported at Ruling 78 and left standing; the eighth batch left it standing too.
- **A difference Claude Code reported in chat and NOT in its landed close or report (seventh batch).**
  Its close message named the seventh dispatch's read list saying *"§10 (the two `★ ADDED BY THE
  APPLICATION ACT` subsections)"* against a file carrying one subsection with that heading and a second
  headed `### ★ Flagged to the L2 surface, not relocated, not decided`. Both halves verified at their
  objects; **CC's handling was right** and the item is apparatus, earning no row. What is worth
  carrying is the shape: **a difference reported only in chat is one the next session does not meet.**

### The user-owed list

**Nothing is waiting on him to start.** No dispatch is on disk and none is running.

**Next in the plan:** the pack finding of §3al.4 goes to L2's pack; **then L2**, sequenced by Ruling
10, with the three L2 flags (Rulings 48, 57, 60) waiting at the reading file's §10 and OQ-1's remainder
at L2's surface (Ruling 70).

**NEW and owed, not blocking:** the ONE re-derivation of the reading file's §11 and §12 against the
ruled dispositions, over the five rows named above.

**Standing, unchanged from the ninety-eighth entry and still the user's:** the two apparatus findings
of the fourth batch; the register repair's STOP leaving the suspension list empty since 2026-08-28
while every ruling since names it as the route (**Ruling 78 among them**); the §3 leak repair; the
rename of `00_READ_THIS_FIRST.md`; the plainness repair; `.gitattributes` for committed `.mscx`
exemplars; the merged D-674 question; the three routed `FRAMEWORK.md` findings; the corrections-batch
scope; the commit-trailer form; the two candidate bounds; the two research-paper binaries; the shell
surface's answer; marker enrolment of P-2..P-5; the artifact-inventory signature-table amendment (the
standing STOP). **The eleven quarantined audit questions stay reserved to the AUDIT**, with the reading
file's §11 and the rows the rulings quarantined.

**Owed as work, none of it blocking:** unchanged from the ninety-eighth entry, less Row 4.3, which the
eighth batch placed.

### Errors

**THREE COUNTED, all three this side's. Two draft defects caught by this side's own pre-release check
and declared, not counted. Five declared departures.** **★ THE STANDING DEGRADATION RULE IS ENGAGED:
three of one family — an assertion made without going to the object — is past the two the rule names,
and the recommendation it orders was made to the user at this verified stop.**

**COUNTED ERROR 1 — a FACT taken from another dispatch's summary instead of from the source, on a face
this side had already called READY.** The eighth dispatch's premise ledger stated the reading rule of
`gen_evidence_pin_membership.py` in the seventh dispatch's words, which are incomplete: route B reads
every line, not the two structural blocks. **It was found only because the user then instructed *"make
sure the cc dispatch is fact checked"*** — this side's own release check had passed over it. **This is
the degradation tell the standing rule names first: citing a summary instead of the source.** The
corrected form is a FACT above so no later dispatch inherits the error. *Recorded also because the
conclusion did not change — A3 still predicted zero — and "it happened to be right" is the argument the
never-work-from-memory rule already rejects.*

**COUNTED ERROR 2 — the dispatch ordered text whose shape its own ordered check would flag, after this
side had read the batch that met the same shape and reworded around it.** The §13b replacement it
orders verbatim opens `**Row 4.3 is PLACED, …`; the seventh writing's declared departure 5 records that
batch rewording exactly this shape before its own commit, and this side had read that departure whole.
**CC reported it rather than smoothing it over and wrote the ordered text as ordered**, which is the
right handling of a dispatch's own words. It is inert, as the measurement above shows, but the defect
is the dispatch's internal tension between its ordered text and its ordered check, and that is counted.

**COUNTED ERROR 3 — a property of the whole root population asserted in THIS ENTRY without being
measured.** The first version of this entry stated that this file was the only untracked file at the
root. It is not: the root carries a large standing population of untracked historical `cc_*` dispatches
and reports. The claim was written from what the eighth batch's close reported about the paths ITS A1
named — which is a statement about named paths, not about the population — and this side generalised
it. **It was found only when this side went to the object before handing over**, and the entry is
corrected above. **Same family as counted error 1: an assertion about something unexamined, from a
statement that did not cover it.**

**Declared, not counted — two defects in the unreleased draft, caught by this side's own check.** The
dispatch first ordered Claude Code to locate a row with `grep -n` over a working-tree file, which is
the read D-253 forbids and which the armed guard would have denied; it now orders the file tools. And a
count of the reading file was transcribed onto its face while the same document's self-check claimed no
count was restated there; the replacement now names no figure.

**Declared departures of this side.** **(i)** `git show --stat`, `git rev-parse`, `git cat-file -s`,
`git diff` between two explicit blob hashes and `git show <hash>:path` piped to `grep`, `sed` and a
python parser — git object queries by explicit hash with their output filtered, the ninety-fifth
entry's declared form. **(ii)** two python read-modify-writes on the user's machine — one to splice the
Ruling 78 section into the sitting record, one to apply the dispatch's pre-release corrections — each
anchor proven unique before the write and each result read back through the file tools; **a write, not
a read for a fact.** **(iii)** `git hash-object` on eight working-tree paths, compared against their
blobs at the tip — it returns a hash and no content. **(iv)** one `git grep -F` at an explicit commit
over `tools/*.py`, to establish that no tool matches the loose `**Row` shape — content-addressed and
read-only, but not among D-253's named forms, so declared. **(v)** one
`git ls-files --others --exclude-standard --directory` filtered to root-level names, to measure the
untracked population before handing over — it returns NAMES and no file content, and it is the form
this project's own dispatches order, declared on the ninety-eighth entry's precedent. **No working-tree
file was read for its CONTENT by any shell.**

**What went right and is worth copying:** both CC closes were verified whole at the objects before a
word was written, and §13b's count was re-derived by counting the placement lines at the file rather
than read off the table; the surface for Row 4.3 was built by reading all seven of Ruling 62's rows
rather than the one that was stopped on, which is what collapsed the alternatives to one; the split
shape was considered and excluded on the ruling's own declining of it at Row 14.9 rather than offered
as manufactured optionality; the dispatch's fact-check was taken at the tool's source, which is what
found the first counted error; and the loose-shape line was settled by a measurement over the tool
population rather than left as a worry.

### The cadence for your session

0. **Boot as above, in full.** This entry file is UNTRACKED and you never land it yourself.
1. **Re-verify the tip at the ref files with the file tools**; expect `28c465e9c3…`. **If a batch is
   running — a moved tip with no close section for that writing — write nothing into the tree, write no
   dispatch (D-251, P-5), tell the user, and wait.**
2. **If nothing is running, the next act is the plan's:** the pack finding of §3al.4 to L2's pack, then
   L2 sequenced by Ruling 10. **The §11/§12 re-derivation is owed and is its own scoped act** — put it
   to the user as its own surface if you mean to take it, and do not fold it into an L2 dispatch.
3. **Nothing is owed to a running batch and nothing is half-done.** This session closed at a verified
   stop with the tracked tree clean.
4. **Verify at the object, not at a report** — including this entry, and including any FACT a previous
   dispatch states about a tool.
5. **Close at a member boundary with nothing half-done**, and write the next entry before you close.
