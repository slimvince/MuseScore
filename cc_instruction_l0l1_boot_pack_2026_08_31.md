# The L0+L1 boot pack — extend the generator, render the pack (dispatch, 2026-08-31)

> **STATUS: WRITTEN, NOT STARTED.** Written by the Cowork writing side on 2026-08-31, executing
> **Rulings 11, 12, 15 and 16** of `cowork_rulings_2026_08_31_decision_surface_sitting.md`. **The user
> opens it with the CC run of his choice** (the 2026-08-26 role ruling).
>
> **★ THE WRITING SIDE'S RESTRAINT, DECLARED ON THIS DISPATCH'S FACE.** From the moment this file is
> handed over, the writing side does not touch it, and does not touch any file named in §0's read-first
> block, while the batch runs.
>
> **This batch renders a boot pack. It boots no deriving session, stages no score to any session, writes
> no brief, and derives nothing.**

## 0. Boot — read before any other act

You start clueless. **A single-file opening instruction is not an exemption from the standing
conventions** (`CLAUDE.md` Conventions, ratified 2026-08-29, P-1). In order:

1. The ordinary session-start read: `CLAUDE.md` whole, `DECISIONS.md` whole, `STATUS.md`, and the
   derived gating answer (`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` →
   `gating_ids`).
2. `BUILD_AND_TEST.md` — **conditionally mandatory and the condition is MET**: this batch runs a Python
   tool.
3. `cowork_rulings_2026_08_31_decision_surface_sitting.md` **§3k, §3l, §3o and §3p** — Rulings 11, 12,
   15 and 16 — **whole, not their headings.**
4. This document, whole.

**The bash rules of `CLAUDE.md`'s VS Code section bind every command** and are not restated here (#6).

## 1. Why this batch exists

**Ruling 10** made **L0+L1** the detail-specification phase's first deriving subject, and its blind
deriving session reads one boot pack. **That pack does not exist**, and **Ruling 11** requires it to
carry three things the generator's ruled member list does not hold: a **leak-filtered `FRAMEWORK.md`
§5-and-§9**, the **five reading-pass extracts with their conclusion sections cut**, and — by **Ruling
12**'s correction of record — the **empirical findings ledger**.

**Ruling 14 first put those outside the generator; its own overturn condition fired** at three findings
in `tools/audit/gen_derivation_boot_pack.py`, the decisive one being that `check_all` compares the
directory listing against the generated set, so **any file written into a pack directory outside the
generator makes that pack permanently STALE**. **Ruling 16 therefore extends the generator**, additively
and per-subject.

## 2. Task 0 — pin

Pin this file and every file §0 names to git blobs with `git hash-object -w`, record the hashes, and
**take every later read from `git cat-file blob <hash>`**. A read disagreeing with its pin is a **STOP**.

## 3. Task 1 — establish the generator at the objects, read-only

**Nothing about the generator is taken from this dispatch on trust.** Establish and report, at the file:
`MEMBERS` and its members in order; the three authored subject tables `WITHHELD`, `CRITERION` and
`VERDICTS`, and which of them `build_subject` STOPs on; what `write_all` writes; what `check_all`
compares; and what `render_read_me` states about the pack's file list. **Report each with its symbol.**

**Establish the `scoring-model` precedent specifically:** Ruling 16 rests on it, and this side has it
only as a relayed claim. **Is `scoring-model` rendered with an EMPTY withheld family — empty `WITHHELD`
sub-tables, an empty `CRITERION`, an empty `VERDICTS` table?** If it is not, **STOP**: the precedent
Ruling 16 leans on does not exist as described, and the subject tables for `l0-l1` must then be put to
the user rather than filled by this batch.

## 4. Task 2 — the prediction, written before the act

**#17(b): the prediction is recorded before the measurement, not after.** State it in the artifact and
in the report **before Task 3 is begun**:

> **The extension is additive and per-subject. The ruled six members stay in every pack, unchanged and
> in their ruled order. For the two existing subjects the extras list is EMPTY, so
> `tools/audit/derivation_boot_pack/harmony-boundary/`, `…/scoring-model/` and
> `tools/audit/derivation_boot_pack.json` MUST re-render BYTE-IDENTICAL.**

**A difference anywhere in either existing pack is a STOP**, not a thing to explain afterwards. The
manifest is the one admitted exception and only in one direction: it gains the `l0-l1` subject, and
**nothing of the two existing subjects' entries may move**.

## 5. Task 3 — extend the generator

**Additively and per-subject.** A subject may carry **EXTRAS after the ruled six**; the ruled six are
**not removed, not reordered and not renamed for any subject**. **`MEMBERS` keeps its ruled content**
(user-ruled 2026-08-22).

**One prose defect the extension itself causes, and must therefore fix:** `render_read_me`'s heading is
*"The six files of this pack, in order"*. **The count becomes DERIVED from the actual member count for
that subject.** Nothing else of the read-me's wording changes — in particular its boundary clause and
its stop-and-record clause are untouched.

**`check_all` must cover the extras exactly as it covers the six**, so the pack stays re-derivable in
one direction and complete in the other.

## 6. Task 4 — add the `l0-l1` subject

A subject is a key in the three authored tables. **Add `l0-l1` with an EMPTY withheld family**, on the
`scoring-model` precedent Task 1 established. **The subject in plain words**, for the read-me:

> **L0, the input contract — what a notated record must supply, what may be assumed of it, and what
> happens when a real score does not supply it — and L1, which finds the moments at which a harmony may
> begin and publishes what the notation says at each, deciding nothing about the music.**

## 7. Task 5 — the three extras, each verified in BOTH directions

**(a) The filtered `FRAMEWORK.md` §5-and-§9 member.** The member carries **§5 whole and §9 whole and
nothing else** — **Appendix A never enters**, and no other section does.

**Two passages are removed, addressed by ANCHOR TEXT and never by line number** (the generator's own
convention for spans):

- in **DP-N** — the anchor `Stage two established that this project's own layer specifications`
- in **DP-Q** — the anchor `Stage two found the same question open in this project's own record`

**Each anchor must match EXACTLY ONCE inside the member's source text; zero or more than one is a
STOP.** The removal runs from the opening of the parenthetical the anchor sits in to its close.

**★ AND A SWEEP, BECAUSE TWO PASSAGES FOUND BY ONE READER IS NOT AN ESTABLISHMENT (#19).** The two above
are the writing side's own reading of §5 and §9. **Sweep the member's source for any FURTHER passage
that describes what this project currently has** — statements about this project's own specifications,
its record, its documents or its code, as against statements about the framework's own drafting history,
which are NOT in scope. **Report every candidate. STOP on any candidate not on the list above** and do
not remove it: widening the filter is the user's act, not this batch's.

**Verified both ways:** the two anchors' passages are **absent** from the rendered member, **and nothing
else was removed** — the rendered member must equal the source with exactly those two spans deleted.

**(b) The five reading-pass extracts, with their conclusion sections cut.** The five are the L1 slice of
`reading_pass/candidacy_upgrades.md`, named there in its reading-progress table:
`reading_pass/extracts/pardo-birmingham-2002-algorithms-for-chordal-analysis.md`,
`…/temperley-sleator-1999-modeling-meter-and-harmony.md`,
`…/bigo-feisthauer-giraud-leve-2018-relevance-of-musical-features-for-cadence-detection.md`,
`…/karystinaios-widmer-2022-cadence-detection-graph-neural-networks.md`,
`…/sears-pearce-caplin-mcadams-2018-simulating-expectations-for-tonal-cadences.md`.

**The cut is the section whose heading contains `detail specification could adopt, adapt, or must argue
against`**, from that heading to the next `## ` heading. **Absent from any of the five, or not
terminating at a `## `, is a STOP.** **Verified both ways:** the section is gone, and the rest of each
extract is byte-identical to its source.

**★ AND ONE THING TO REPORT AND NOT CUT.** Each extract also carries a section of **findings routed to
the user** — this side's conclusions rather than the papers' content. **Ruling 11 Decision 2 cut ONE
named section and this batch does not widen it.** **Report what those sections contain** so the user can
see what the deriving session will inherit, and **cut nothing beyond the named heading.**

**(c) The ledger.** `EMPIRICAL_FINDINGS_LEDGER.md`, **whole and unfiltered**, by Ruling 12's correction
of record and the phase definition's own naming of the ledger among this phase's inputs.

## 8. Task 6 — render, and prove the prediction

Render all subjects. Then: **`--check` green**; **both existing pack directories and their files
byte-identical to their pre-batch state**, proven by hash and reported per file; and the manifest
changed **only** by the addition of `l0-l1`. **Any of the three failing is a STOP.**

## 9. Task 7 — the Corelli claim, checked at the file

**Ruling 15 requires each staged score's claim checked at the file before it is staged.** Four are
proposed; three are established and one is not. **At `tools/dcml/corelli/MS3/op01n08a.mscx`, establish
whether the piece exhibits STAGGERED ENTRIES — parts whose onsets and releases do not coincide, so that
releases fall away from onsets.** Report what you find with what you read. **Stage nothing**; staging is
the writing side's act, and this task only supplies the fact it rests on.

## 10. STOPs — surface, never absorb

An anchor matching zero times or more than once; a sweep candidate not on the list; a cut heading absent
or not terminating; either existing pack or the manifest not byte-identical; `--check` not green; any
ruled member removed, reordered or renamed; the `scoring-model` precedent not as Task 1 expects; a read
disagreeing with its pin; any act outside §11.

**Each STOP is written up and reported. This batch repairs nothing it finds and widens no filter.**

## 11. The footprint — enumerated from this batch's own orders

**Created:** `tools/audit/derivation_boot_pack/l0-l1/` and its files; this batch's report.

**Edited:** `tools/audit/gen_derivation_boot_pack.py` — the per-subject extras dimension, the derived
read-me count, the `l0-l1` subject tables, and the extras' inclusion in `check_all`. **This is a tool
source and it is named here deliberately**, so no bar of this dispatch reads as forbidding it. Also
regenerated: `tools/audit/derivation_boot_pack.json`. **No other existing tool source is edited except
the forward bound's own per-batch re-aiming, `gen_status_batch_bound.py --apply`, EXCEPTED BY NAME**
under Ruling 5 of `cowork_rulings_2026_08_26_amendment_landing_sitting.md`.

**Read and never written:** `FRAMEWORK.md`, the five extracts, `EMPIRICAL_FINDINGS_LEDGER.md`,
`tools/dcml/corelli/MS3/op01n08a.mscx`. **`FRAMEWORK.md` IS NOT AMENDED** — the filtering governs what a
pack carries, never what that document says.

**Not done at all:** no build, no test, no golden, no measurement of the analysis, nothing under
`tools/corpus/` or `tools/robust_stop/`, nothing under `src/` or `docs/`; no governing document amended;
**no open-items row created, flipped or discarded**; **no decisions-register entry and no `D-NNN`** —
that register cannot accept one and `cowork_register_rule_c_suspension_2026_08_28.md` is the route; the
workbook not opened; no score staged; no brief written; no session booted.

**The tracked-modification assumption, as a SHAPE with a STOP, and with the KNOWN exception named so this
bar does not break on an act the writing side has already taken.** This batch expects the working tree to
carry untracked Cowork material, **and expects `cowork_rulings_2026_08_31_decision_surface_sitting.md` to
be tracked-and-modified** — that file is the writing side's landing of the very rulings this dispatch
executes, it is addition-only, and it is **not** a STOP. **Any OTHER tracked modification this batch did
not make is a STOP.**

## 12. Done

Done when: the generator carries a per-subject extras dimension with the ruled six intact; `l0-l1` exists
in the three subject tables; the pack is rendered with its six ruled members and its three extras; the
two filters are verified in both directions; the sweep is reported; both existing packs and the manifest
are proven byte-identical but for the manifest's new subject; `--check` is green; Task 7's finding is
reported; and every STOP met is written up. **Then close on the ruled stop form.**

**The standing self-check runs before the report is written** — the actual diff on disk of every touched
file, against the guiding principles, the conventions and `DEFECT_TYPES.md`.

---

*Provenance: written by the Cowork writing side, 2026-08-31, executing Rulings 11, 12, 15 and 16 of
`cowork_rulings_2026_08_31_decision_surface_sitting.md`. Read at the files by this side before writing:
`FRAMEWORK.md` §5 and §9 whole — which is where the two anchor passages were found and why Task 5(a)
orders a sweep rather than trusting that finding; `tools/audit/gen_derivation_boot_pack.py` at
`render_read_me`, `write_all` and `check_all`, which is where Ruling 14's overturn condition was
established; `docs/score_inventory.md`; `cowork_blind_session_brief_scoring_model.md` whole. **The
`scoring-model` empty-withheld precedent is a RELAYED claim and is ordered established at the objects
rather than restated as fact** (Task 1). No shell command was run on the repository by the writing side.
No figure of this project's own measurement is restated (#17f, D-431).*
