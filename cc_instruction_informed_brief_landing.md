# CC dispatch — land the seven brief-points rulings, the revised informed brief and the two ratification-surface banners; NO tool edit; boot NO session

> **Dispatch (Cowork, 2026-08-28).** Lands the writing side's work following
> `cowork_rulings_2026_08_28_informed_brief_points_sitting.md`. **Nothing is running and nothing else
> is dispatched.**
>
> **★ WHAT THIS BATCH IS.** Task 0 lands four tracked modifications and two new files, regenerates
> the evidence-pin membership, commits once and pushes. Task 1 closes. **That is the whole of it.**
>
> **★ WHAT THIS BATCH IS NOT. It edits nothing itself.** Every file it commits was written by the
> writing side and is landed as it stands. **No tool source is touched, no generator is edited, no
> pack is rendered, nothing is withheld, no session is booted, nothing is derived, compared or
> placed, and no framework text is authored.** If any ordered path's content looks wrong to you,
> **that is a STOP-and-report, not an edit to make.**
>
> **★ THE ASSUMPTION THAT HAS CHANGED SINCE EVERY PREVIOUS DISPATCH, AND IT WILL STOP YOU IF YOU
> CARRY THE OLD ONE.** Every dispatch before this one declared **exactly one** tracked modification,
> `cowork_handoff.md`. **This one declares FOUR.** A1 below states them by path and by shape. **A
> modification at any path A1 does not name is a STOP-and-report.**
>
> **★ TWO OF THE FOUR ARE UNDER `ratification_surfaces/`, AND THAT IS RULED, NOT A LAPSE.** The
> previous dispatch barred itself from editing a ratified surface on the ground that it is not a
> batch's call. **The user has since made the call** —
> `cowork_rulings_2026_08_28_informed_brief_points_sitting.md`, Rulings 6 and 7 — and the writing side
> made both edits. **You land them; you do not author, extend or tidy them.**
>
> **★ THE ONE TIMING CONSTRAINT.** Task 0 lands `cowork_handoff.md`. **This dispatch asserts no
> number of handoff entries** and orders the count established at the object by a blob-to-blob diff,
> reported. **`cowork_handoff.md` must not be edited between A1's check and Task 0's commit** — the
> writing side does not touch it while this batch runs.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The framework document* — the product of the
> framework phase: the layer decomposition, each layer's charter, and the boundary contracts between
> them. *The informed session* — the session that will author it; under the 2026-08-28 ruling it may
> read this project's code and specifications as design input. *The two-stage order* — that session
> derives from outside this project and records that draft before opening any of ours, then reads ours
> and revises on the record. *The first-stage draft* — what it writes before opening ours.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md` —
> CONDITIONAL, and this batch does NOT meet the condition: rule (a)'s `gating_ids` only. **(2)**
> `cowork_handoff.md`, ITS CURRENT ENTRY (the seventy-seventh) and the entry below it. **(3)**
> `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)** The record this batch lands,
> whole: `cowork_rulings_2026_08_28_informed_brief_points_sitting.md`. **You do NOT open:**
> `ARCHITECTURE.md`; any of the three sealed placement-sample files; either pack directory's contents.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS, no
> design, no repair, no derivation of any specification statement, NO SESSION BOOTED, no document
> archived, moved or deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit
> to any governing document, any register entry or any register source**, and **no edit to any tool
> source** — this batch changes no tool at all.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first act (CHECK mode) and again at
> the end (write mode, committed as the artifact of a real run). Verify every commit at the object by
> explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and a working-tree
> `git diff` are denied by the armed guard — use the per-path explicit-hash forms and
> `tools/audit/changed_paths.py`. Plain single-quoted `git commit -m '…'` subjects.**

## Ruling ledger (quoted at the record, not paraphrased)

- **`cowork_rulings_2026_08_28_informed_brief_points_sitting.md`, Ruling 1 (P1):** the session
  *"**first** derives from the outside sources … and **records that draft**"*, and *"**only then**
  does it open this project's own material"*, recording each change and its reason. **It does not
  discharge the #18 exposure and that record says so.**
- **Rulings 2–5 (P4, P5, P6, P7):** a dated draft name renamed at ratification; a minimal sizing
  record declared NOT A BUDGET; the same three score-and-analysis pairs by name as exemplars; the
  code-site fill-in stays with a side other than the author. **Ruling 5 is declared to rest on a
  preference and not on a prior ruling.**
- **Rulings 6 and 7 (B1, B2):** the two ratification-surface banners, **appended, with no ruled text
  edited.**
- **The user's words for all seven: *"I agree with all recommendations."***

## Premise ledger

**★ THE WRITING SIDE HAS NO SHELL AND ASSERTS NO GIT-OBJECT VALUE.** Everything below was read with
the file tools at the working tree on 2026-08-28. **No commit, blob or tree value is stated anywhere
in this dispatch and none may be taken from it.** The tip was read as a file at
`.git/refs/heads/master` and is `8798d6049e2e237efd4d8bffd5b7f7f904815493`; **establish it, and
everything else, at the objects yourself.**

- **FACT** — `tools/audit/guard_state.json` → `summary` records run **75**, passing **72**, failing
  **3**: `gen_filing_convention_application.py --check`, `decisions/apply_soft_discard.py --check`,
  `decisions/apply_residue_discard.py --check`. **`gen_derivation_boot_pack.py --check` is GREEN** and
  must stay green — this batch touches no tool.
- **FACT** — `cowork_rulings_2026_08_28_informed_brief_points_sitting.md` and this dispatch are on
  disk and **untracked**.
- **FACT** — the four tracked files A1 names carry the writing side's edits already; **nothing in
  them is owed to this batch.**

- **ASSUMPTION A1 — the working tree, stated by PATH and by SHAPE.** Exactly **four** tracked
  modifications:
  1. **`cowork_handoff.md`** — additions-only and **prepended** above the committed content; the
     new-entry count established at the object and reported.
  2. **`ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md`** — **additions-only**, a
     single hunk inside the opening block quote, **zero deletions**. *(An appended banner; §3.3's
     clause text is untouched and a deletion anywhere in this file is a STOP.)*
  3. **`ratification_surfaces/cowork_withheld_family_framework_reading.md`** — **additions-only**, a
     single hunk at the head, **zero deletions**. *(Same shape, same STOP.)*
  4. **`cowork_informed_session_brief_framework.md`** — **a revision carrying both additions and
     deletions**, which the other three do not; its §3, §5, §6, §7 and provenance were rewritten
     against the rulings. **This is the one path where deletions are expected.**

  **Untracked and to be landed: the ruling record and this dispatch. A modification at any other
  tracked path is a STOP-and-report**, and the rest of the untracked population is NOT landed and is
  outside A1's STOP.
- **ASSUMPTION A2 — the guard state.** Unchanged at **75 / 72 / 3** throughout, except that
  `gen_evidence_pin_membership.py --check` is red between the untracked landing and its own
  regeneration inside Task 0. **Any other red is a STOP-and-report** — this batch edits no tool and
  should move no derived artifact but that one.
- **ASSUMPTION A3 — the boot pack is untouched.** `tools/audit/derivation_boot_pack.json` and all
  fourteen pack files are **byte-identical** to their committed blobs at the end, **and no tool is
  run that would rewrite them.** Prove the manifest by hash; do **not** run the generator.
- **ASSUMPTION A4 — no ratification-surface tool moves.** `gen_ratification_surface_set.py` and
  `reaim_ratification_surface_paths.py` derive over that directory. **Two files there change content
  but neither is added nor removed and no path moves**, so both checks are predicted to stay green.
  **If either goes red, MEASURE the cause and report it; do not re-aim anything** — a re-aiming under
  this batch's fence would be a tool edit this dispatch forbids.

## The declared start state

Three failing guard checks, the three known, each for its own recorded cause; plus
`gen_evidence_pin_membership.py --check` STALE, caused by this dispatch's own untracked ruling record.
**A further failing verdict is a STOP-and-report.**

## Findings in this batch

**No finding number is allocated.** **One item is carried into this batch's report and is NOT acted
on:** the writing side's finding that §3.3 of `cc_report_framework_arrangement_landing.md` explains a
count discrepancy by asserting that `cowork_handoff_archive.md` carries the moved entries, when the
measured cause is that report's own stricter counting pattern — the strict form returns 12 where the
loose form returns 78 in the same file. **The figures are unaffected and the +2 stands by two routes.
Report it as carried; correct nothing at that file** — a landed report is a record of what a batch
said, and amending it is the user's call.

## Task 0 — land the six paths and PUSH

1. **A1's check first, entirely at content-addressed objects.** Enumerate with
   `tools/audit/changed_paths.py`; confirm the four tracked modifications and **no fifth**; prove
   paths 1–3 additions-only at blob-to-blob diffs with **zero deletion lines**, and path 1
   additionally **prepended**; report path 4's `--numstat` without a shape constraint.
2. **The handoff's new-entry count ESTABLISHED at the object** and reported. **Count with the pattern
   `^## .*COWORK SESSION CLOSE`** — the loose form the earlier batches used — **and, if you also
   report a strict-pattern figure, say which pattern produced which number.** *(This instruction
   exists because the previous batch's report reconciled the two by an explanation it had not
   measured; see Findings.)*
3. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE the diff against the committed
   blob BEFORE accepting it.** Movement beyond the one landing ruling record's own addition is
   reported rather than absorbed.
4. **Commit exactly these paths and no other:** `cowork_handoff.md` (M),
   `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` (M),
   `ratification_surfaces/cowork_withheld_family_framework_reading.md` (M),
   `cowork_informed_session_brief_framework.md` (M),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_28_informed_brief_points_sitting.md` (A) and
   `cc_instruction_informed_brief_landing.md` (A). Subject, exactly:
   `record: the informed framework brief is ruled and revised — the two-stage derivation order, the seven brief-points rulings, and the two ratification-surface banners landed`.
5. **PUSH**; verify at the object. **`gen_evidence_pin_membership.py --check` must PASS.**

**E0:** the seven committed paths and no others at the object; the established new-entry count with
its pattern named; paths 1–3 proven additions-only; the membership check passing; `origin/master` at
the commit.

## Task 1 — the close

1. One `STATUS.md` pointer entry for Task 0 and one for the close, per the OI-222 pointer convention —
   no count, no identity and no rendered value restated (**D-431**); the previous batch's entries
   moved through `gen_status_batch_bound.py --apply`; `gen_session_start_read_size.py` regenerated.
2. The report is `cc_report_informed_brief_landing.md`: both guard states, every SHA, E0 graded,
   A1–A4 graded, declared departures, the established handoff-entry count **with its pattern named**,
   the carried finding above, and the plan lines — **the framework document is authored next, in a
   session that has read none of this; the independent challenge run, the sealed samples' disposition,
   the first-stage draft's home and the decisions-register blocker are all unruled.**
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state guard run.

**E1:** at the tree carrying the close, a fresh full guard run — **the three known failing checks and
no others, zero STOPs** — committed only after the run that produced it.

## What this batch does NOT do

- **It edits no file's content.** Every landed path is the writing side's text, landed as it stands.
- **No tool source is touched and no generator is run** except the evidence-pin membership
  regeneration Task 0 orders and the guard set. **The boot pack is not re-rendered.**
- **No pack, nothing withheld, no session booted, no derivation, no comparison, no placement test, no
  framework text.**
- **No edit to any governing document, register entry or register source.** `ARCHITECTURE.md` not
  opened; none of the three sealed placement-sample files opened.
- **No finding number.** [[OI-179]] OPEN and GATES.
- **It writes no decisions-register entry.** Seven ratifications are owed one and the register cannot
  accept them; the route is **D-652**. **This is the seventh consecutive act shaped around that
  blocker, which has still never been put to the user** — recorded, not hidden, and not this batch's
  business.

## The writing side's self-check over this dispatch

1. *Principles touched:* **#12** — both banners append and delete nothing, and A1 makes a deletion in
   either a STOP rather than a matter of judgment; the superseded brief and the withheld-family
   reading file are kept; the previous batch's false explanation is **carried and named rather than
   corrected at its file**, because overwriting a landed report is a different act and the user's.
   **#6** — the rulings have one home, the record, and every other file points at it. **#13** — the
   changed A1 count is stated at the head as the thing that will stop a batch carrying the old
   assumption, rather than left to fire silently. **#17f / D-431** — no count and no git-object value
   asserted anywhere; the entry count is ordered measured **and its pattern named**, which is the
   direct lesson of the previous batch's §3.3. Conforms.
2. *Conventions:* American English; no self-invented labels; no music-theory word arises in this
   batch's subject matter.
3. *Figures and premises:* the guard summary and the four tracked paths were read at the files by the
   writing side on 2026-08-28 and are ordered re-established at the objects.
4. *File-tools rule:* the writing side read only through the file tools and ran **no shell command of
   any kind** — the user barred it for this session — **so it verified no git object and this dispatch
   relays none.**
5. *Uncertainty:* the measured-not-predicted items are the handoff-entry count, the four paths' diff
   shapes, and the evidence-pin membership movement. **A4's prediction that the two
   ratification-surface checks stay green is a PREDICTION and is registered as one**; if either goes
   red the cause is measured and reported, and nothing is re-aimed to force it.
