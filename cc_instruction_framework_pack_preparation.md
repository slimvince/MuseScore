# CC dispatch — land the 2026-08-27 ruling record and the framework brief; author the `framework` subject and ENUMERATE its withheld candidates; RENDER NO PACK; boot NO session

> **Dispatch (Cowork, 2026-08-27, the fifty-fourth session).** Executes the writing side's owed
> preparation for the framework phase's authoring act, which is ruled to a fresh Cowork session
> (Ruling 1 of `cowork_rulings_2026_08_26_framework_opening_sitting.md`) and cannot open until a
> boot pack exists for the subject. **Nothing is running and nothing else is dispatched.**
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** Task 0 lands the untracked writing-side files and
> the modified handoff, and PUSHES. Task 1 authors the `framework` subject into the boot-pack
> generator's AUTHORED tables **with an EMPTY withheld family**, runs the candidate enumeration,
> enumerates the member-(2) passage candidates, and writes BOTH candidate lists as a reading file
> for the user to rule — ONE commit. Task 2 closes. **THIS BATCH RENDERS NO PACK DIRECTORY FOR THIS
> SUBJECT, WITHHOLDS NOTHING, BOOTS NO SESSION, DERIVES NOTHING AND COMPARES NOTHING** — and it
> touches nothing of the `harmony-boundary` or `scoring-model` subjects: their manifest blocks,
> pack directories, families and reading files are byte-unchanged or the batch STOPs.
>
> **★ WHY NO PACK IS RENDERED, STATED SO IT IS NOT READ AS TIMIDITY.** A rendered pack is a bootable
> pack. For this subject the withheld set is not yet ruled, and a pack rendered with an empty family
> would hand a deriving session the very thing it must derive. **The enumeration is the deliverable;
> the render is a later batch, after the user rules.** If the generator's shape cannot enumerate
> without writing its pack directory, **that is a STOP-and-report with the shape described** — never
> a render taken anyway, and never a wider rewrite of the tool.
>
> **★ THE ONE TIMING CONSTRAINT, AND IT IS NOT A PRECONDITION ON OPENING.** Task 0 lands
> `cowork_handoff.md`. **Whether the writing side's entry for the session that wrote this dispatch is
> in that file is NOT a precondition** — if it is there it lands here, and if it is not it lands at
> the next dispatch, which is this record's ordinary state. A1 holds either way: additions-only and
> prepended, with the entry count established at the object and asserted nowhere. **What IS binding:
> `cowork_handoff.md` must not be edited between A1's check and Task 0's commit** — the writing side
> does not touch it while this batch runs, so what you verify is what you commit.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The framework document* — the product of the
> framework phase: the layer decomposition, each layer's charter, and the boundary contracts
> between layers. *A deriving session* — one that writes what the analysis SHOULD do without reading
> what this project's code or specifications say it DOES. *The boot pack* — the rendered directory
> such a session reads; six members plus a read-me. *The withheld family* — the per-subject authored
> cuts: withheld identities, withheld documents, and withheld passages inside member (2). *The leak
> check* — the standing per-entry test that keeps an entry whose rendered fields carry a `docs/` or
> `src/` path or the string `ARCHITECTURE.md` OUT of the generated member, listed instead. *The
> candidate criterion* — the per-subject authored pattern that proposes candidates for withholding;
> **it proposes, the user rules.** *The authored tables* — the per-subject structures inside
> `tools/audit/gen_derivation_boot_pack.py`.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md` —
> CONDITIONAL, and this batch MEETS the condition: read it in full; rule (a)'s `gating_ids` only.
> **(2)** `cowork_handoff.md`, ITS CURRENT ENTRY, and the entry below it. **(3)**
> `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)** The two records this batch
> lands, whole: `cowork_rulings_2026_08_27_framework_authoring_sitting.md` (this batch's occasion)
> and `cowork_blind_session_brief_framework.md` (the object the enumeration serves; its §7 (P2) is
> the point this batch feeds). **(5)** The whole of `tools/audit/gen_derivation_boot_pack.py` — you
> edit its authored tables and nothing else. **(6)**
> `ratification_surfaces/cowork_withheld_family_harmony_boundary_reading.md` — the shape your
> reading file takes. **You do NOT open:** `ARCHITECTURE.md` beyond what the tool's own mechanics
> touch; any of the three sealed placement-sample files; either existing pack directory except to
> prove it byte-unchanged.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS built,
> designed, scoped or run, no design, no repair, no derivation of any specification statement, NO
> SESSION BOOTED, no document archived, moved or deleted AS A FILE, **no open-items row created,
> flipped or discarded**, **no edit to any governing document, any register entry or any register
> source**, **no change to the generator other than the authored `framework` subject entry — the
> criterion machinery, the leak check's tests, `withhold_passage()`, the marker and every STOP are
> untouched**, **no edit to any `harmony-boundary` or `scoring-model` authored entry, rendered file
> or manifest record.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (CHECK mode) and again at
> the end (write mode, committed as the artifact of a real run). Verify every commit at the object
> by explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and `git diff` over
> the working tree are denied by the armed guard — use the per-path explicit-hash forms. Plain
> single-quoted `git commit -m '…'` subjects. The reserved-word and vocabulary rules bind every line
> this batch writes.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1 of `cowork_rulings_2026_08_26_framework_opening_sitting.md`:** the framework document's
  author *"is a **fresh** Cowork session … It holds the blindness barrier for the whole of its work:
  no source file, no measurement output, no document derived from the code."*
- **Ruling 2 of `cowork_rulings_2026_08_21_successor_plan_sitting.md`:** the unit list is derived
  from four sources, the fourth being *"the user-ratified ten-factor model of 2026-07-19
  (`cowork_joint_estimator_factorization.md`), **admissible to a blind session because it is a
  user-ratified ruling**"*, and the grain question — *"whether the resulting units are factors of
  the model, decisions the analysis makes about the music, or a reconciliation of the two"* — is
  *"the framework phase's first ratified finding, put to the user then with the evidence."*
- **`ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §3.3, constraints:**
  *"**NOT ALLOWED:** implementation-derived material as design input; evidence treated as the
  decision."*
- **Ruling 1 of `cowork_rulings_2026_08_22_member_two_leak_sitting.md`** and **Ruling 1 of
  `cowork_rulings_2026_08_23_member_two_second_leak_sitting.md`:** two passages of member (2) were
  withheld, **each scoped to the harmony-boundary subject**, and both lay **outside the leak check's
  scope, which is members (5) and (6)**.
- **The 2026-08-27 ruling this batch lands:** the framework document is authored now, and
  ratification of its decomposition is HELD until the user's incoming external list is dispositioned
  against it.

## Premise ledger

**★ THE WRITING SIDE HAS NO SHELL AND ASSERTS NO GIT-OBJECT VALUE.** Every FACT below was read with
the file tools at the working tree on 2026-08-27; **no commit, blob or tree value is stated
anywhere in this dispatch, and none may be taken from it.** The tip was read as a file at
`.git/refs/heads/master` and is `acedffc66d8c40f17d5fe6dbb73ca1ac90129997`; **establish it, and
everything else, at the objects yourself.**

- **FACT** — `tools/audit/derivation_boot_pack.json` carries **TWO** subjects, `harmony-boundary`
  and `scoring-model`, and no third. Its `harmony-boundary.counted` reads `design_intent_class` 244,
  `candidates` 75, `withheld_identities_authored` 16, `withheld_identities_derived` 17,
  `withheld_identities_total` 33, `withheld_documents` 1, `withheld_passages` 2, `leaks` 3,
  `design_intent_entries_rendered` 208, `files_in_the_pack` 7. Its `scoring-model.counted` reads
  `design_intent_class` 244, every `withheld_*` field 0, `leaks` 3,
  `design_intent_entries_rendered` 241, `files_in_the_pack` 7. **Both blocks are predicted
  BYTE-UNCHANGED after this batch.**
- **FACT** — `tools/audit/derivation_boot_pack/` holds exactly two directories, `harmony-boundary/`
  and `scoring-model/`, each of seven files. **There is no `framework/` directory.**
- **FACT** — `tools/audit/guard_state.json` → `summary` records run **75**, passing **72**, failing
  **3** — `tools/audit/gen_filing_convention_application.py --check`,
  `tools/audit/decisions/apply_soft_discard.py --check`,
  `tools/audit/decisions/apply_residue_discard.py --check` — not_run 4, historical 16. **Re-read it
  yourself at the object.**
- **FACT** — `cowork_rulings_2026_08_27_framework_authoring_sitting.md` and
  `cowork_blind_session_brief_framework.md` are on disk and untracked at the tree the writing side
  read.

- **ASSUMPTION A1 — the working tree, stated by CONTENT and not by count.** Tracked modification:
  `cowork_handoff.md`, additions-only and prepended above the committed content. **THIS DISPATCH
  DELIBERATELY ASSERTS NO NUMBER OF NEW HANDOFF ENTRIES** — the last several dispatches that stated
  one were wrong — **and orders the count ESTABLISHED AT THE OBJECT** by a blob-to-blob diff, whose
  result is reported. **A modification at any OTHER tracked path is a STOP-and-report.** Untracked
  and to be landed by Task 0: the two files named in the last FACT, and this dispatch. The rest of
  the untracked population is NOT landed and is outside A1's STOP.
- **ASSUMPTION A2 — the guard state after all tasks.** The three failing checks above still fail for
  their own causes; the boot-pack generator's own `--check` is red between the table edit and the
  enumeration, inside Task 1's SAME commit; the membership check is red between Task 0's untracked
  landings and its own regeneration. **Any other red is classed by its measured cause; one outside
  this batch's own acts' subjects is a STOP-and-report.**
- **ASSUMPTION A3 — the manifest after Task 1.** It gains ONE subject block, `framework`, carrying
  the subject line, the criterion, the declared bound, and a `counted` block whose `candidates` and
  `leaks` are **MEASURED, never predicted** (#17f, **D-431**), and whose every `withheld_*` field is
  **0 by this dispatch's own instruction**. **No `files_in_the_pack` value is predicted, because no
  pack is rendered.** The two existing subject blocks are byte-unchanged.
- **ASSUMPTION A4 — the guard registry.** No tool is added: the generator gains a subject, not an
  invocation. **If its registered invocation is per-subject and the registry therefore moves, that
  is a STOP-and-report, not an edit to make.**

## The declared start state — at the tree you will meet

1. Three failing guard checks, named in the premise ledger, each for its own recorded cause.
2. `tools/audit/gen_evidence_pin_membership.py --check` STALE — caused by this dispatch's own
   untracked ruling record.

**A further failing verdict is a STOP-and-report.**

## Findings in this batch

**No finding number is allocated.** A leak-shaped passage you MEET in a span you touch is SURFACED
with its verbatim and its location — **not cut, not ruled.** A tool shape this dispatch's
instruction cannot reach without a wider change is a STOP-and-report with the shape described.

## Task 0 — land the two records and this dispatch, clear the membership check, and PUSH

1. **A1's check first, entirely at content-addressed objects**, including the handoff's
   additions-only property proven at a blob-to-blob diff and **the new-entry count established
   there and reported.**
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE the diff against the
   committed blob BEFORE accepting it.** Movement beyond the ruling record's own addition is
   reported rather than absorbed.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_27_framework_authoring_sitting.md`,
   `cowork_blind_session_brief_framework.md`, and `cc_instruction_framework_pack_preparation.md`
   (new). Subject, exactly:
   `record: the framework document is authored now with ratification held — the 2026-08-27 ruling and the framework brief landed; evidence-pin membership regenerated`.
4. **PUSH**; verify at the object.
5. **`gen_evidence_pin_membership.py --check` must PASS.**

**E0:** the committed paths; the established new-entry count; the check passing; `origin/master` at
the commit.

## Task 1 — the `framework` subject authored, its candidates ENUMERATED, the reading file delivered — ONE commit

**(a) Author the `framework` entry** in the generator's AUTHORED tables, in the shape the two
existing subjects use:

- **subject name** `framework`;
- **the subject in plain words:** *"How the analysis should be divided into layers — what question
  each layer answers, on what evidence, what it publishes, and what may cross between them."*;
- **what the family protects** — **not an oracle**: this subject is not held out against a ruled
  answer, and there is none to protect. What the family exists to keep out is **implementation-derived
  material as design input**, which is the ruled NOT ALLOWED clause quoted above. **State that in the
  field rather than naming an oracle span, and if the tool's shape requires an oracle span, that is a
  STOP-and-report.**
- **the withheld family: EMPTY** — no identities, no documents, no passages. **Nothing is withheld
  by this batch.**

**(b) The candidate criterion, authored by the writing side and given here in full.** A candidate is
every DESIGN-INTENT entry meeting ANY of:

- its `group` is one of **A** (the estimator architecture), **C** (cross-cutting analysis
  contracts), **D**, **E**, **F**, **G**, **H** (the layers) or **I** (module boundaries and code
  structure);
- any of `title`, `verbatim`, `plain`, `patterns` contains one of the keywords: `layer`, `layers`,
  `decomposition`, `charter`, `boundary contract`, `publishes`, `consumes`, `one path per concern`,
  `span typology`, `the record`, `seam`, `adapter`, `pipeline`, `stage`, `upstream`, `downstream`,
  `note model`, `slicer`, `slice`, `key and mode`, `chord identity`, `function layer`, `grouping
  layer`, `derived view`, `emission`, `decode`, `joint estimator`, `state space`, `factor`,
  `forward-only`, `backward edge`, `re-derive`, `recompute`.

**★ THE STANDING EXCLUSION, which is a ruling and not a preference:** an entry whose home is
`cowork_joint_estimator_factorization.md` — the user-ratified ten-factor model — **is NEVER a
candidate**, because Ruling 2 of the successor-plan sitting admits it to a blind session by name.
**Apply the exclusion after the criterion and report how many it removed.**

**★ THE BOUND ON THE CRITERION, to be written into the manifest field the other subjects use.** *The
criterion is a pattern match over the design-intent register's own text. **ITS REACH IS UNMEASURED**
(#19): an entry that bears on this subject in words none of the criterion's terms carry would not
appear, and an empty match would be evidence of nothing. The bound is stated rather than a detection
measurement being owed, under **D-673**, and that clause's test is met: **NO ANALYSIS DECISION
CONSUMES THIS ENUMERATION** — the user rules it at the reading file this dispatch delivers.*

**(c) Run the enumeration and NOT the render.** Produce the candidate list and the leak list for
this subject. **Write no directory under `tools/audit/derivation_boot_pack/`.** If the tool cannot
enumerate without rendering, **STOP and report the shape**; do not render.

**(d) Enumerate the member-(2) PASSAGE candidates separately, and cut nothing.** Member (2) is
`CLAUDE.md` rendered whole, and the record carries two occasions on which it leaked the held-out
answer for a different subject — **both outside the leak check's scope, which is members (5) and
(6).** For THIS subject the answer is the decomposition itself. Enumerate, from `CLAUDE.md` as it
stands at the tree, every span that states any of: **what the layers are or what one is responsible
for; a ranking of evidence; a convention about where a boundary or a segment falls; the arrangement
of the record and the two arms; or a named layer's input or output.** Report each with its
**verbatim**, its anchor text, and the section it sits in. **The two passages already withheld for
`harmony-boundary` are re-tested here and reported with the rest; their existing withholding is
scoped to that subject and is neither carried across nor removed.** **This enumeration is authored
in its signature and its reach is unmeasured; say so on the artifact in the same words as (b)'s
bound.**

**(e) Write the reading file** `ratification_surfaces/cowork_withheld_family_framework_reading.md`,
in the shape `cowork_withheld_family_harmony_boundary_reading.md` takes: what the family is; what
the leak check is and its scope; the design-intent candidate list, each entry with its id, title,
the criterion limb that matched and the matched string; the member-(2) passage candidate list, each
with its verbatim and anchor; the standing exclusion and what it removed; the declared bound on both
enumerations; **the statement that NOTHING is withheld yet and that the pack is deliberately not
rendered**; and what the file does not do. **It asks the user for one verdict per candidate and
recommends none** — where the record does not settle the question, the surface that returns it
gathers facts and makes no recommendation (**D-658**).

**(f) Run the FULL guard set.** A2, A3, A4 must hold. Commit the generator, the manifest and the
reading file, subject:
`the framework subject authored and its withheld candidates ENUMERATED — nothing withheld, no pack rendered, no session booted`.
Push; verify at the object.

**E1:** the manifest's new subject block at A3, with `candidates` and `leaks` measured; the two
existing subject blocks and both pack directories byte-unchanged; **no `framework/` directory in
existence**; the reading file whole in the ruled shape; both declared bounds on its face.

## Task 2 — the close

1. One `STATUS.md` pointer entry per task that did work, per the OI-222 pointer convention — no
   count, no identity and no rendered value restated (**D-431**); the previous batch's entries moved
   through `gen_status_batch_bound.py --apply`; `gen_session_start_read_size.py` regenerated.
2. The report is `cc_report_framework_pack_preparation.md`: both guard states, every SHA, E0–E1
   graded, A1–A4 graded, declared departures, the established handoff-entry count, and the plan
   lines — **the user rules the two candidate lists; the render is a later batch; the brief is
   finalised against the rendered pack; then the fresh Cowork session authors the framework
   document.**
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state guard run.

**E2:** at the tree carrying the close, a fresh full guard run — the three known failing checks and
no others, zero STOPs — committed only after the run that produced it.

## What this batch does NOT do

- **No pack rendered for this subject. Nothing withheld. No session booted. No derivation, no
  comparison, no placement test.**
- **Nothing of the `harmony-boundary` or `scoring-model` subjects touched** — manifest blocks, pack
  directories, families and reading files byte-unchanged, or STOP.
- **No edit to the generator beyond the authored `framework` entry**; no edit to any governing
  document, register entry or register source; **`ARCHITECTURE.md` not opened**; none of the three
  sealed placement-sample files opened, not in any portion.
- **No finding number.** [[OI-179]] OPEN and GATES.
- **No open-items row created, flipped or discarded** except an apparatus-finding row under rule
  (c). **No `src/` change, no golden, no test, nothing under `tools/corpus/` or
  `tools/robust_stop/`.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* **#19** — nothing is withheld without a ruled reason, so this batch
   withholds nothing and only enumerates; **#13** — a tool shape the instruction cannot reach is a
   STOP surfaced, never a rewrite; **#6** — one generator, one authored home, the third subject
   derived from it; **#12** — the two existing subjects byte-unchanged, the harmony-boundary
   passages neither carried across nor removed, the candidate lists delivered rather than absorbed;
   **#17f / D-431** — the candidate and leak counts measured at the artifact and never predicted
   into prose, and no git-object value stated anywhere; **D-658** — the reading file recommends
   nothing. Conforms.
2. *Conventions:* American English; no self-invented labels; music-theory words in their musical
   sense — *layer* and *slice* appear as keywords of the criterion, which is a quotation of the
   register's own vocabulary and not a new coinage.
3. *Figures and premises:* the manifest blocks, the pack directory listing and the guard summary
   were read at the files by the writing side on 2026-08-27 and are ordered re-read; the candidate
   and leak counts are explicitly measured-not-predicted; **the handoff's new-entry count is
   deliberately not asserted.**
4. *File-tools rule:* the writing side read only through the file tools and ran no shell command
   against the repository; **it therefore verified no git object, and this dispatch relays none.**
5. *Uncertainty:* the measured-not-predicted items are the candidate count, the leak count, the
   standing exclusion's removal count, the member-(2) passage enumeration, and the handoff-entry
   count. The criterion of (b) and the signature of (d) are **authored**, their reach unmeasured and
   declared as such on the artifact.
