# OI-220 — The joint module's headers uniformly assert a DORMANCY the code has outgrown (a #10 doc-sync lag post-switch)

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — the joint-module review (OI-199 pass-1 finding, area a)**

**The finding (CC, OI-199 pass 1, verified at the code).** The joint module was built DORMANT (default-
OFF, ahead of the notation switch), and its file/header comments say so — uniformly. But since **THE
SWITCH** (`useJointNotationRecord` default `Val(true)`, `composingconfiguration.cpp:178`, user-ratified
2026-07-27) the module IS the production notation analysis: the three notation bridges call
`joint::produceNotationRecord(score, …)` behind the flag (`notationcomposingbridge.cpp:732/1495`,
`notationimplodebridge.cpp:1420`, `notationtuningbridge.cpp:777`). The dormancy comments were not swept
at the switch. Stale sites found:
- `jointdecoder.h:43` — "DORMANT (no production consumer)."
- `labelclass.h:39` — "This module is DORMANT — no production path reads it."
- `jointadapter.h:41` — "DORMANT (no production consumer)."
- `jointtables.h:44-45` — "This module is DORMANT — no production path reads it."
- `jointweights.h:37` — "the weights are applied at the factor call sites in the decoder (a later
  commit); this component only holds and names them."
- `jointnotationrecord.h:48-50` — "DORMANT (declared dormancy) … Nothing in src/ reads it yet."
  (grep confirms `sectionrecordadapter` reads the record — it IS consumed.)

**Why it matters.** #10 (documentation always in sync with code) — but more than cosmetic: #19
(establishment) and the fact-publication corollary treat "declared dormancy with a named future
consumer" differently from "published + consumed." A reader trusting these comments would wrongly
believe the whole inference module is inert, when it is on the production path on both surfaces. The
correct comment is the one `composingconfiguration.cpp:178` already carries ("default TRUE since the
user-ratified switch … the joint estimator's notation record IS the production notation analysis").

**Scope note.** The sibling stale comments on the RECORD-ARM branch sites in the notation bridges
("Record arm (default OFF, useJointNotationRecord)" at `notationcomposingbridge.cpp:719`,
`notationimplodebridge.cpp:1408`, `notationtuningbridge.cpp:765`) are the same lag on the area-(b)
record-seams side — they are the record-seams deep pass's finding; noted here for the whole picture.

**NOT fixed here** (OI-199 read-only; a comment sweep is a #10 fix at the next touch, or its own doc
commit). Related: [[OI-199]] (the review this is a pass-1 finding of), [[OI-180]] (the retirement map
whose completion finalizes the migration language). DT-12 / #10.

*Provenance: CC-found at OI-199 pass 1 (2026-07-28, `cc_oi199_pass1_report.md` §4/§6 F-OI199-1). Rowed
at the pass-1 unblind commit.*
