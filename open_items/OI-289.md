# OI-289 — The 80 LEGACY-marked register entries were swept, never verified; two of them were wrong

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section E — verification owed, phase 2. Rowed 2026-08-03 (CC, phase 1m, Task 4) by the user's
ruling of the same date, which weakened the marker's wording now and put the re-verification here.**

## What the mark used to claim, and why the claim is gone

The LEGACY marker is generated text, one fixed sentence rendered on every marked entry
(`tools/audit/decisions/gen_decisions_register.py`, `LEGACY_MARK`). Until 2026-08-03 it ended:

> *…this decision's subject is the dormant pipeline awaiting deletion at the retirement map; **it
> has no effect on the live solution**.*

That last clause is a claim about the LIVE system, and the marking pass never checked it — the set
was drawn by asking what each decision is ABOUT, which is a different question. **It failed twice,
both times on evidence found later:**

- **D-329** (phase 1i) — the complete-candidate-listing decision. Its subject is the legacy scorer,
  but the user's [[OI-275]] ruling carried its PRINCIPLE across to the live family design, so "no
  effect on the live solution" was false of it. The remedy then was a second marker variant (the
  transfer mark), which fixed the instance and left the clause standing on the other entries.
- **D-311** (this wave) — the chord-analyzer file-split sequencing. Its subject is
  `chordanalyzer.cpp`, which `STATUS_ARCHIVE.md:114` records as a file that *"SURVIVES the
  engagement; R9 splits `chordanalyzer.cpp`, does not delete it"*, and one of the five translation
  units the split produced, `chordsymbolformatter.cpp`, is run by the record arm at
  `notationimplodebridge.cpp:1170`. Neither the old clause NOR the weakened sentence is true of it.

**The user ruled the clause removed rather than re-argued** (option C3 of the phase-1m dispatch): a
swept population with two demonstrated errors is not established (#19), and a mark that asserts more
than the pass behind it checked is a claim under load with nothing holding it. The mark now states
only what the decision is ABOUT. Executed in the GENERATOR, so one change covers every marked entry
and the wording cannot drift between them (#17f).

## What this row carries

**The full re-verification of the marked set against a live-reachability test.** As of the weakening
the set is **80 entries** (81 before D-311's mark was removed on the established evidence above —
which is a correction of a demonstrated error, not a sweep, and is the only per-entry change made
without the verification this row calls for).

*The 80 is counted from the data, and no figure in the record matches it.* `STATUS.md`'s phase-1i
entry says **75**; the register's own ratified preamble says **79**
(`DECISIONS.md`, the eighth ratification event); the phase-1m dispatch says **75**. The 75/79 pair
is an internal inconsistency dating from phase 1i itself; the growth from there is the marked
entries among D-406…D-426. Sizing this row's work from any of the three would under-count it, so the
count is stated here as derived and a reader should re-derive rather than quote — the same failure
shape [[OI-283]] rows for the register's coverage claim. The test is the one the D-311 and D-329 failures show
is needed: for each marked entry, is its subject reachable on either production surface at HEAD, and
does any ruling carry its principle across to the live design?

**Note that the phase-1m finding on the P4 fallback may bear on a whole CLASS of them, not just on
D-311.** [[OI-288]] establishes that the legacy notation arm's reachability is decided by one runtime
flag whose default is `true` (`composingconfiguration.cpp:178`) and by four branch points, not by
compilation — so "dormant" is a statement about a default, with an enumerated false-negative path
(an explicit flag flip, `batch_analyze` without `--joint-inference`, the test suites, and the
declared non-production uncached path). Any per-entry verdict has to say which of those it means.

## What is NOT in doubt

The marking convention itself (user-ratified 2026-08-02, `CLAUDE.md` decisions-register rule (f)) and
its purpose: a reader must never mistake a ruling about soon-deleted code for one about the live
solution. Nothing in this row weakens that. What it withdraws is an unchecked assertion that rode
along with it.

*Provenance: rowed 2026-08-03 (CC, phase 1m, Task 4) on the user's ruling of the same date. The two
failure instances are established — D-329 at the [[OI-275]] ruling, D-311 at the objects and the code
this session. Cross-ref [[OI-275]] (the D-329 transfer), [[OI-286]] (the D-311 finding), [[OI-288]]
(the reachability class), [[OI-180]] (the retirement map the mark names), [[OI-208]].*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

---

## Dated note — 2026-08-03 (CC, phase 1w): the verification is DONE, and the number this row states is stale by one

**Dispatch `cc_instruction_phase1w_legacy_mark_verification.md`. The whole verification is the
generated artifact `tools/audit/decisions/phase1w_legacy_verification.json`; no count, verdict or
citation is restated here (D-431).** Every figure below is a field of that artifact, and every
citation it rests on is located at the object by the generator, which stops rather than emitting a
citation that has moved.

**The set size.** Derived from `backbone_decisions.json`, exactly as this row instructs. The row's
**80 was TRUE when it was written and has been stale by one since the very next wave** — established
by read-only git object queries walking parents from this session's own commit report: D-428 gained
the mark at phase 1n when its classification was corrected against the OI-165 premise. So this row is
not wrong about its method; it is stale about its number, which is the failure shape it names. **Five
recorded figures now disagree with the derived count, not three** — the two the row lists, the
register preamble it names, and both of the 75s, one of which is in the *live* `STATUS.md` entries
rather than the archive. All five are located in the artifact; **none is corrected here**, because
correcting a surface is action and this wave gathers.

**What the verification found, in the order that matters.**

1. **No mark is established wrong, so none was corrected.** On the test the *surviving* mark actually
   makes — what the decision is ABOUT — all 81 hold. Task 4's convention-in-question clause does not
   fire. The one candidate is **D-055**: the twenty-one mode-prior *preferences* are registered
   unconditionally in the module configuration, which is production code, while the *consumer* that
   reads them is the legacy key scorer. Whether the settings are themselves legacy-scoped is a scoping
   judgment and not a code fact, so the mark stands and the question is carried on [[OI-302]].

2. **★ "Dormant" is much weaker than it sounds, and this is the finding the family design needs.**
   **No** entry earned "unreachable at any setting" on the notation flag, and **none** earned
   "unreachable only while the flag holds its default". Every marked subject that has code at HEAD is
   reached by something — plain `batch_analyze` (the `--joint-inference` flag defaults **OFF**, so the
   corpus instrument runs the legacy pipeline unless told otherwise), a `batch_analyze` diagnostic
   dump flag, or the test suites. Changing any of these subjects still breaks tests and still moves
   what an untagged `batch_analyze` run produces. *Dormant* means "not what the product runs", never
   "nothing runs it".

3. **★ The transfer half found a class the prediction did not contain, and it is the largest one.**
   The registered prediction expected the two known ruling-transfers and a handful of entries whose
   own text assigns live work; both bands came back right. What it did not predict: a much larger set
   of entries that a **LIVE specification section restates as standing do-not-retry prohibitions**
   (`ARCHITECTURE.md:306`, `:1344`, `:1420`), with `docs/redesign_plan.md:4` saying the distinction in
   its own words — *"LIVE prohibitions about the legacy chord path"*. **The mark is right about the
   subject and the entry still binds what a future designer may attempt.** That is a third thing a
   mark can be, beside "about live code" and "about dead code", and the register has no field for it
   — [[OI-302]].

4. **Two enumerations in this row are short.** (a) The four false-negative paths omit
   **`batch_analyze`'s diagnostic dump flags**, which reach code neither production surface reaches
   and plain `batch_analyze` does not either — the Layer-4 slice decoder and the whole Layer-5
   function module. `ARCHITECTURE.md:1339` already names that path for Layer 3; this row's list does
   not. (b) Reachability is not decided by `useJointNotationRecord` alone: **`jointKeyWiringEnabled()`
   is a second runtime flag, default OFF**, gating the built half of the shelved joint key-and-chord
   step inside the legacy arm.

5. **Three marked entries describe an arrangement the live system does not have** — the style preset
   entering at the key/mode layer (production inference is preset-independent, so the live arm has no
   preset entry point at all), the key search being its own decoder (the live estimator decides key
   and chord in one decode), and the signature read living in one shared function (the joint fact
   adapter does its own). **None is a wrong mark. All three are why the mark exists.**

6. **Nothing in the code cites a decision.** Not one occurrence of a marked ID appears in any `.cpp`
   or `.h` file, so the transfer half could only ever be searched in prose — which bounds what a
   "none-found" verdict is worth, and the artifact says so where the search method is described.

**Two side findings met while tracing the production paths, both reported and neither fixed here:**
six comments in `src/` and `tools/` still call the record arm *"default OFF"* or the record section
adapter uncalled ([[OI-303]] — a `src/` change, which this wave forbids); and two dated annotations
still assert the opposite of **D-428** as corrected at phase 1n ([[OI-304]]).

*Provenance: CC, phase 1w, 2026-08-03. Artifact
`tools/audit/decisions/phase1w_legacy_verification.json`, generated by
`tools/audit/decisions/gen_phase1w_legacy_verification.py` (reproduces byte-identically). Residues on
[[OI-302]], [[OI-303]], [[OI-304]].*
