# OI-195 — The embedded-table generator embeds CRLF, not LF blobs

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-195 | The embedded-table generator embeds the CRLF WORKING-TREE form, not the git-canonical LF blobs (Cowork-found 2026-07-26 verifying the D1 codegen commit `83fbb9e661` at the objects): `tables_all.json` blob = 114,787 bytes LF vs 118,460 embedded (the `text=auto` CRLF checkout); the generated `jointembeddedartifacts.cpp` carries literal `\r\n` escapes; the recorded sha256 values and the drift-guard test are therefore CHECKOUT-CONFIGURATION-DEPENDENT (the OI-34 platform-dependence class, now in a `src/` generated file + a permanent establishment test — on an LF checkout the regeneration differs byte-for-byte and the guard fails, #16). NO inference impact: JSON parsing is line-ending-insensitive; the regen byte-identity + robust-stop (+0/−0) establishment stands. Fix: normalize inputs to the git-canonical LF form at the generator (values unchanged), regenerate, normalize the drift guard's file side identically | Cowork verification of `83fbb9e661`; `.gitattributes` `* text=auto`; OI-34 | ✅ RESOLVED 2026-07-26 (Task-1 commit `4565830c82` of `cc_instruction_posterior_slice.md`; supersedes the CRLF embedding introduced at `83fbb9e661`). `gen_embedded_tables.py` now `canonicalize_lf`-normalizes each input artifact's bytes (CRLF→LF) BEFORE embedding AND hashing, states the canonical form in the generated header, and the drift guard normalizes the checked-out file's line endings to LF before every byte/hash comparison — so both the recorded sha256 and the guard establish against the committed git-blob (LF) OBJECT content, checkout-independent (#16/#19). Values untouched (parse-structure byte-equal CRLF-vs-LF on all five artifacts, verified); `tables_all.json` embedded 118,460→114,787 bytes = the committed blob, sha256 `db905d…`→`4480b4…`. Re-established: drift guard 6/6 green, `composing_tests` 1152, 4 covered stems byte-identical to the committed corpus |
