# OI-356 — The open/resolved test searches the whole status cell for the resolved mark, so a cell that mentions another row's mark reads as resolved

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — instrument / measurement layer (the open-items index's own row parser). Rowed
2026-08-09 (CC, `cc_instruction_return_continuation_3.md`, Task 0).**

## How it was observed

Task 0 wrote the user's Ruling 18 onto [[OI-354]]'s index row. Among the things that ruling settles
is that [[OI-289]]'s completed-verification status is **not** withdrawn, and the sentence recording
it named that status with its own mark. The row stayed OPEN and its text said so in words.

The task-boundary guard run then reported **five** derivations failing where the committed state
carried one, and their messages named OI-354: *"authored verdicts for rows that are not open
first-cut candidates (the register moved under the table)"*, and, in the four that import it,
*"the committed apparatus declaration carries verdicts for rows the INDEX no longer carries open"*.

**It was diagnosed at the tool and then confirmed in both directions**, rather than being inferred
from the message: removing the mark from the status cell — with the sentence's meaning unchanged,
carried in words instead — makes all five pass.

## The cause, read at the tool

`tools/audit/gen_nongating_apparatus_rows.parse_rows` splits an index row into its six cells and
computes the row's open state as **the resolved mark does not occur anywhere in the status cell**.

That function is **the ONE index parser** (#6). `gen_phase1_completion_inventory.py` imports it by
name — its own comment says *"#6 - one parser"* — and the finish-line machinery follows from there.
So the reading is not one tool's: it is the reading every derivation over the register uses.

## The two halves, and only one of them is loud

1. **LOUD — a row that carries an authored apparatus verdict.** The declaration's own STOP fires and
   names the row, and the tools that import the parser stop with it. **That is the guard working**,
   and it is what happened here: nothing was written on a wrong reading, because the run refused to
   produce an artifact at all.
2. **SILENT — a row that carries no authored verdict.** Nothing cross-checks it. The row leaves the
   open population, and the open-row count, the TRUE-half cuts and the finish line's populations all
   move with it, with no stop anywhere. A reader of those artifacts would see a smaller open set and
   no indication that anything had been mis-read.

The silent half is why this is rowed rather than left as a session remark beside the previous
continuations' two JSON traps.

## What is NOT in doubt

- **No analysis, no analysis input, no corpus of scores, no measurement of the analysis**, no golden
  and nothing in `tools/robust_stop/`.
- **No row's actual status.** A row's status is what its own words say; what this concerns is how a
  derivation reads them.
- **No verdict in the apparatus declaration.** None is reopened, and the observed instance produced
  no artifact.
- **The parser's test is not asserted to be wrong for the case it was written for.** A resolved row's
  status cell does begin with the mark, and the test answers correctly for every row that does not
  discuss another row's status.

## Why nothing was changed

Changing how the index is parsed is a mechanism change **D-436** reserves to the user, and there is
more than one defensible remedy — anchor the test to the START of the status cell, take the status
from a dedicated position rather than by searching prose, or forbid the glyph in status prose and
check for it. Choosing among them is a design call, not a repair.

**The working convention adopted meanwhile, stated so the next session does not rediscover it:**
inside a status cell, name another row's resolved status **in words**, never with the glyph. This
row's own status cell follows it.

## Why it carries the declaration's default

The over-inclusive first cut does not reach this row's subject column, so no authored verdict is
written for it and it carries the default, which is that it GATES — the same handling as
[[OI-352]]'s. **No gating verdict is hand-added**: a non-gating verdict is derived from a cut and
never hand-written ([[OI-319]], [[OI-336]]).

**It is not an establishment obligation on a published rate.** The parser produces populations for
tracking artifacts, not a measured value about the analysis, so the clause that always gates
whatever its subject is not engaged here.

## ★ RESOLVED 2026-08-09 — the canonical status discipline, and what it changed about this row's own account

**The user's Ruling 33** of `cowork_rulings_2026_08_09_fifth_stop.md` remedies this row and its two
siblings ONCE, at the cause rather than per symptom (#6): **every INDEX status cell begins with one
canonical token** — the resolved mark at the head of the cell, or one of six open-state words — so a
row's state is read from its opening and never by searching its prose. A mention of another row's
mark anywhere in a cell is now **inert**, which closes this row's defect by construction. Applied
2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 1).

**The remedy this row proposed as its first shape is NOT what was adopted, and the difference
matters.** Anchoring the existing test to the first character was ruled and then refuted by its own
both-ways condition — it would have marked genuinely closed rows open. What makes the leading token
readable is that every cell now HAS one, which is the normalization; the parser change alone was
never enough.

**Three things guard it from here:** the ONE parser reads the state through the canonical token and
**STOPS** on a row that does not split; a standing **lint** (`tools/audit/index_status_lint.py`)
reports every non-canonical opening; and the vocabulary lives in ONE place, which both of them read
(#6). Every value of the pass is at `tools/audit/index_status_normalization.json` (**D-431**).

**★ THE WORKING CONVENTION ABOVE IS RETIRED, and that is said plainly rather than left to be
inferred.** Naming another row's resolved status in words was the only thing preventing the defect
while the parser searched prose; nothing depends on it now. It costs nothing to keep writing that
way, and no derivation is affected either way.

**★ AND THE PASS FOUND A FOURTH INSTANCE OF THIS FAMILY, IN A DIRECTION NOBODY HAD LOOKED.** A row
whose cell OPENS with a stale `OPEN —` while its own closing words say it CLOSES: the opening and
the prose disagreed about that row's own state, and the rule at HEAD happened to read it correctly
while the leading-token rule would not have. It is [[OI-208]], its reading is authored in the
artifact, its state did not move — and it **refutes** the previous continuation's reading of that
same row, which had called it the one row the refuted remedy improved. That correction makes Ruling
25's remedy worse than it was recorded to be, not better: it would have marked three genuinely
closed rows open and improved none.

*Provenance: observed and diagnosed 2026-08-09 by CC under `cc_instruction_return_continuation_3.md`
Task 0, at the standing self-check, by reading the derived guard run rather than trusting the edit —
the same method that caught the duplicate-key trap the previous continuation recorded. Cross-ref
[[OI-354]] (the row the instance was observed on), [[OI-352]] (the same apparatus one construct out),
[[OI-292]] and [[OI-297]] (the audit apparatus's own enforcement and rendering rows).*

## Dated note — 2026-08-09 (CC, `cc_instruction_return_continuation_4.md` Task 2): the user's Ruling 25 was measured, and its remedy is REFUTED

**Ruling 25 ordered the resolved-detection anchored to the status cell's own LEADING TOKEN**, with
the condition that makes it safe: every index row decided both ways, and **only rows the defect's
shape names may move.** The dispatch's assumption A3 states that shape in words — *rows whose status
cell mentions another row's resolution.*

**The table was built and every row decided both ways before anything moved**
(`tools/audit/oi356_parser_correction.json`), by a generator that implements BOTH rules itself
rather than reporting a diff of the result — which is the only construction that can show a
movement rather than assert one.

**★ THREE ROWS MOVE, AND NOT ONE OF THEM MENTIONS ANOTHER ROW'S RESOLUTION.** Read at the rows, with
the text around each mark generated rather than transcribed:

- **Two state THEIR OWN resolution somewhere other than the first character** — one because a
  ratification date precedes the mark, the other because the mark sits mid-cell on the delivered
  half of that row's own work, in a cell ending *"Row CLOSED"*. **Both are genuinely resolved, the
  rule as it stands reads them correctly, and the corrected rule would mark them open.**
- **One is genuinely OPEN**, its cell beginning with the word *OPEN* and the mark appearing far into
  it on a delivered sub-result. **That is the single row the correction improves.**

**So the remedy fixes one row and breaks two. The parser is left EXACTLY as it stands.**

**★ AND THE FOUNDING INSTANCE NO LONGER EXISTS AT THIS TREE**, which is why the movers are all of a
different kind: the working convention this row adopted — *name another row's resolved status in
words, never with the glyph* — was applied to the cell that caused it. **The mechanism is untouched
and the convention is now the only thing preventing recurrence**, which is precisely why this row
stays OPEN.

**Nothing in this row is withdrawn.** The finding stands whole. What is refuted is one proposed
remedy, and the refutation is a measurement rather than an argument.

**What a later act has to choose among**, none of it decided here and all of it a mechanism change
**D-436** reserves to the user: recognise a resolution token anywhere in a bounded OPENING rather
than at the first character; take the state from a dedicated position rather than by search; or
forbid the glyph in prose and check for it. **The last of the three is a convention rather than a
mechanism**, and it would also answer [[OI-361]].

**★ AND THE BOTH-WAYS PASS TURNED UP TWO FURTHER DEFECTS IN THE SAME PARSER, rowed rather than
folded in here:** [[OI-361]], a resolution spelled in words reading OPEN to every derivation; and
[[OI-362]], a malformed row dropped silently so that it is neither open nor resolved but absent. The
three together are the enumerated family, which is what lets one remedy be designed over all of
them instead of whichever is currently visible.

*Resolution belongs in the INDEX row; dated notes may be appended here.*
