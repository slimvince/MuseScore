# OI-171 — The OI-167 Aeolian-guard re-home is not clean

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-171 | **The OI-167 Aeolian-guard RE-HOME is NOT CLEAN — the disposition's premise is wrong at the code (CC-discovered 2026-07-14, attempting the re-home).** `cc_oi167_collection_tonic_report.md` §4.2 proposes re-homing `refineSparseChordQualityFromKeyContext` to the presentation/L5 surface and calls it "**Zero behavior change** (it already runs post-commit)". **That premise is FALSE.** The function has FOUR call sites, and three of them are **inside the region analyzer's commit path, BEFORE the commit**: `regionanalyzer.cpp:1015` mutates `chosenResult` and `decoder.commit(chosenResult.identity, gateCtx)` runs at `:1026` — so the refinement's quality overwrite reaches the **committed chord identity** and the emitted `.ours.json`. Same shape at `:1233` (pass-2) and `:1423` (pass-2b). Only the 4th site (`sectionanalyzer.cpp:158`) plus the notation bridge are presentation-side. **Moving the file to a presentation home therefore REMOVES it from the analysis commit path — a structural behavior change, not a re-home.** It is *empirically* inert on this corpus only because the body never executes (OI-167: 0/0/0 fires, and the function is reached with an `Unknown`-quality chord **0** times — `analyzeChord` never hands the region path an unqualified chord), which is a corpus fact, not a structural guarantee. **CC STOPPED rather than force the cross-layer move** — the dispatch's own escape clause ("If the re-home target is not clean, STOP and report rather than forcing a cross-layer move"). **The disposition goes to the design pass**, which must now choose between: (a) DELETE the three `regionanalyzer` call sites (byte-identical on this corpus; needs the robust-stop diff and an argument that an `Unknown`-quality region cannot arise), then re-home the remainder; (b) re-home only the presentation call sites and leave L4's; (c) retire the function outright (OI-102's carried question). **The OI-167 row's "RE-HOME (preferred) … zero behavior change" recommendation must be corrected before it is acted on.** | `cc_oi168_fix_report.md` §6; `regionanalyzer.cpp:1015/1026`, `:1233`, `:1423`; `sectionanalyzer.cpp:158`; `sparsechordrefinement.cpp:122-180`; `cc_oi167_collection_tonic_report.md` §4.2; OI-102; OI-167 | OPEN — CC-discovered 2026-07-14; **OI-167 cannot close until this disposition is made** |
