# OI-196 — Module-local (tonic,mode)→fifths mapping duplicate

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-196 | Module-local (tonic, mode)→signature-fifths mapping is a declared interim duplicate (#6/#23; CC-found at the record assembly `e064f08298`): the existing `keymodeanalyzer::keySignatureFifthsForKey` depends on the legacy key analyzer's internals (`resolveToFifths`/`keyModeIndex`/`KeySigMode`), so reusing it would breach the joint module's L1-only isolation (OI-180 §1); `jointprimitives::keySignatureFifths` implemented module-local with a comment naming the duplicate. UNIFIES at the legacy key-analyzer retirement (OI-180 map item 2) — one mapping survives | CC report 2026-07-26 (`cc_instruction_notation_record_assembly.md`); `jointprimitives` | OPEN — bounded interim duplicate; retires at map item 2 |
