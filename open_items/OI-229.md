# OI-229 — Music-theory terminology collision — the reserved-word convention is now standing; the existing tree is not renamed unilaterally

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — docs / convention (user-directed 2026-07-28).**

Rowed by the decision-harvest dispatch (`cc_instruction_decision_harvest.md`) Task 0, recording a
standing convention the user directed so it lives in the register rather than only in
conversation.

## The convention

Generalizing the earlier "instrument" case (that word means a violin, not a measurement script):
**any term that coincides even slightly with music theory is reserved for its music-theory
meaning.** The rationale, in the user's framing: this is a music-analysis system, and an
ambiguous domain vocabulary makes every document harder to read and every specification easier to
misapply. The rule is now in `CLAUDE.md` Conventions (a riding Cowork edit committed with this
row) and **binds new writing immediately**. The disambiguation convention is one rule covering
every case: **the bare word always carries the musical meaning; every non-musical use is
explicitly qualified** (e.g. *candidate score* / *content score* for a number, bare *score* for
the music; *map key* / *cache key* for a lookup handle, bare *key* for tonality; *measurement* /
*to gauge* for the gauging sense, bare *measure* for the bar).

## What is NOT decided here

The existing tree is **NOT renamed unilaterally.** That pass is its own scoped, ratified work
item, because some names carry correspondence to the published research the design is grounded in
(#1/#2), which makes the rename a **decision surface** rather than a mechanical sweep. What binds
now is: no NEW collision is introduced, and anything written for the user avoids the collided
sense entirely.

## The starting inventory of known collisions (from the CLAUDE.md entry)

*instrument*, *score* (numerical vs musical), *key* (map key vs tonality), *measure* (to gauge vs
the bar), *stem* (file-name stem vs note stem), *note* (annotation vs pitch event), *mode*
(operating mode vs musical mode), *tie* (tie-break vs notated tie), *dynamic* (dynamic
programming vs dynamics), *register* (the open-items register vs pitch register), *beat* (to
defeat vs the pulse), *scale* (to grow with vs the collection), *figure* (a reported value vs
figuration), *interval* (uncertainty range vs pitch interval), *resolution* (level of detail vs
harmonic resolution), *sharpen* (to refine vs to raise a pitch), *flat* (featureless vs the
accidental), *root* (underlying cause vs chord root), *part* (portion vs musical part), *rest*
(remainder vs silence).

## The two structural cases (not careless — real renaming cost)

**score** (a candidate's numerical score versus the music) and **measure** (to gauge versus the
bar) are pervasive, are used in this project in **both** senses already, and both carry a real
cost to renaming. They are flagged so the eventual scoped cleanup treats them deliberately rather
than mechanically.

*Provenance:* user 2026-07-28; the `CLAUDE.md` Conventions riding edit (the disambiguation
convention + the collision inventory); the dispatch's Task 0.

## 2026-08-09 — the user's Ruling 37: the completeness question is ANSWERED and recorded, not closed

Recorded by `cc_instruction_return_continuation_6.md` Task 0 from
`cowork_rulings_2026_08_09_sixth_stop.md`, Ruling 37, read whole (D-643). **The status of record is
the INDEX row and this note is narrative** (register rule (d)).

**What was ruled.** The scanner licensed by Ruling 31 and run at the previous continuation produced
a negative result — neither derivation of the project's musical vocabulary is both SOUND and
BOUNDED — and three readings of that result were put to the user. **Reading 3 is ruled:** the
inventory stays at the user's twenty; the convention stays LIVE for new writing; and the answer is
RECORDED rather than the question being closed.

**The recorded answer, which is now a standing statement rather than one pass's finding.** The
inventory is incomplete by a measured amount relative to named derivations that are themselves
incomplete against the record — so *complete* means **complete relative to a NAMED DERIVATION,
re-derived as the tree grows, with the derivation's measured miss rate against the seed part of its
name**. It is register entry **D-661**, homed at `cowork_audit_protocol.md` beside **D-436**, and
the scanner artifact carries the same answer in its own closing state.

**What the ruling does NOT do, stated so the row is not read as further along than it is.** It runs
no verdict-authoring pass over the derived population; it adopts no check; it renames nothing and
schedules no batch; and it does not close this row, whose subject is the cleanup rather than the
completeness question.

**The one bounded option HELD and explicitly not scheduled.** A batch over the SEED TWENTY's bare
uses in the governing surfaces — the one subset sound by construction, the seed being the user's own
inventory — as a later leftover-capacity item. Named here so it is not rediscovered.

**Excluded alternatives, recorded (#12).** Authoring verdicts over either derived population: over
the sharp one it is unsound, because the derivation misses seed words the record already holds; over
the broad one it is unbounded. Confidence in a verdict is not what #19 licenses. And per-word batches
over the same population, which is the same defect at a smaller bill.

**Where the working enforcement actually is.** The per-wave standing self-check (D-434), whose
establishment is four consecutive waves each catching a reserved-word collision in its own new prose
— recorded as data in the scanner's labelled corpus rather than read live, because a corpus read
from the tree empties itself the moment the defect is corrected.

*Resolution belongs in the INDEX row; dated notes may be appended here.*

## ★ LAPSE RECORD — 2026-08-12 (CC, `cc_instruction_resume_lapse_records.md` Task 3)

**What has happened to this row.** Under the user's **Ruling 66** of 2026-08-11
(`cowork_rulings_2026_08_11_fifteenth_stop.md`; register entry **D-676**, homed at `CLAUDE.md`'s
non-gating declaration) this row **STAYS OPEN, STOPS GATING and STOPS BEING OWED** — it no longer
draws leftover capacity. **This is not a resolution.** No status cell moves, the row does not close,
the open-row count is unchanged, and nothing about the issue recorded above has been done. What
lapses is the obligation, not the row.

**The derivation that graded it — which is what makes this record challengeable rather than final.**

- `tools/audit/nongating_apparatus_rows.json` — the apparatus cut, which classes this row
  NON-GATING on **D-438**'s criterion on the ground *tracking/documentation apparatus, the user's
  own phrase*, with its per-row reason recorded there.
- `tools/audit/decisions/true_half_reach_rows.json` — **D-639**'s test, which put this row OUTSIDE
  the doc-sync half, so it is owed nothing further on phase 1's TRUE half either.

**How to re-open it.** Challenge a named derivation rather than rediscovering the issue: if the
apparatus cut's verdict for this row is wrong, or D-639's test reaches it after all, the row returns
inside the gate and back into what is owed. **An establishment obligation (#19) never lapses,
whatever its subject** — that clause of the non-gating declaration is untouched by Ruling 66, and the
cut cannot place a row outside the gate whose recorded ground names it, the carve-out being encoded
in the cut rather than remembered.

**Where the population lives.** `tools/audit/phase1_finish_line.json` →
`★_the_bearing_cut.the_lapse_population`, derived on every run from the apparatus declaration's
NON-GATING set and never hand-listed; no count is restated here (**D-431**). A row with no named
grading does not lapse — it halts that derivation instead.
