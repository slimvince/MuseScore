# OI-256 — Record-arm test-coverage gaps against #11, enumerated: the production interactive path's seams are largely untested, and every large-score test is a disabled measurement, not a gate

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — tests / #11 (feeds the [[OI-199]] record-seams partition — an input to it, not a
duplicate of it).**

The enumerated gaps, each checked rather than assumed (repository-wide searches):

1. `NotationAccessibility` — no test anywhere references it, though its selection-changed path is
   the OI-206 mechanism site.
2. The context-menu model (`NotationContextMenuModel`) — no test; `src/notationscene` has no tests
   directory.
3. The single-write `addAnalyzedHarmony` (`notationinteraction.cpp:8210`) and the per-note
   "Tune as" (`addAnalyzedTuning` `:8822`) — no test.
4. The record arm has NO cache/incremental-recompute coverage: the decode-cache suite
   (`notationdecodecache_tests.cpp`) exercises the LEGACY arm only — its fixture never sets
   `useJointNotationRecord`, so the production default path has no behavioral coverage of
   edit-invalidate/navigation-warm semantics (and no cache to test — [[OI-203]] — which is
   itself the point: when the cache lands, this row is its test obligation).
5. Every large-score test is `DISABLED_` (LargeScoreCounts, LargeScoreDecodeProfile,
   NoteSeamLatency, the 3.1b sweeps — measurement tools, not gates), and the enabled golden
   corpus caps analysis at 16 measures — so nothing gates the repertoire class the OI-209
   requirement names.
6. The golden suite is arm-mixed: two snapshot keys (`implode`, `keyAreas`) are built by direct
   LEGACY calls while three go through the flag-ON seams (`pipeline_snapshot_tests.cpp:768-773`,
   `:938`) — the goldens do not uniformly pin the production arm.

Disposition: this row is the INPUT LIST for the [[OI-199]] partition-3 (record path/seams) deep
pass and for the OI-203 cache increment's test plan; the arm-mixed golden question may warrant
its own act earlier (it bears on what the switch reconciliation actually pinned). Nothing fixed
here.

*Provenance: Cowork-found 2026-08-02, the requirement-side review
(`cowork_candidate_open_items_2026_08_02.md` C-5); user reviewed 2026-08-02.*
