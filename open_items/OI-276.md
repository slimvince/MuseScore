# OI-276 — three LIVE-SPEC design documents state as CURRENT something that is false at HEAD, and one of them names a regression gate that no longer exists

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10) / DT-12 (stale which-code-is-running statement). Found by the
phase-1h continuation wave, 2026-08-02, reading four LIVE-SPEC design documents IN FULL. This is
the [[OI-232]] / [[OI-265]] / [[OI-274]] class over the DESIGN-DOCUMENT surface. Both sides are
quoted in each instance; nothing is fixed here.**

## Why a row of its own rather than a note on [[OI-274]]

[[OI-274]] is scoped to `docs/scoring_model.md` — a document `CLAUDE.md` makes a mandatory
session-start read — plus two banner instances found in the same read. These three are different
documents, found by a different wave, and the third instance is sharper than a stale tense: it is
a **signed specification stating an acceptance criterion that no longer exists**, which a build
executed against that specification would try to satisfy. The class is the same; the population
and the severity are not, and the phase-1g triage classifies all three as LIVE-SPEC — documents
the reading set treats as standing force.

## Instance 1 — the SIGNED Layer-3 specification says its decoder is the live path

`cowork_layer3_keymode_design.md:3-4`, in the status banner:

> **Status: SIGNED (user, 2026-06-22)** … **WIRED — AS-BUILT, Step 1 (2026-06-22): the decoder is
> the live key/mode path.**

False at HEAD. The joint estimator is the production inference layer on the batch and corpus
surface since 2026-07-26 (**D-005**) and on the notation surface since 2026-07-27 (**D-010**);
[[OI-232]]'s resolution records that `ARCHITECTURE.md`'s Layer-3 build tag was re-tagged
**Built+Dormant** with *all four production call sites verified in the flag's false branch*. The
canonical document was corrected; this one was not, and it carries no scoping sentence of the
kind [[OI-232]] and [[OI-265]] were resolved with.

The whole document is written in that tense — "the decoder is the live key/mode path", "the
resolver + `collectPitchContext` remain only as the diagnostic/grading baseline" — so a reader
sent here for how the tonality layer works meets a description of a dormant path presented as
production.

## Instance 2 — a shelved design still instructs the next session to revisit a question that was answered

`cowork_joint_key_chord_design.md:9-12`, in the shelving banner:

> **★ BUT SEE `COWORK_HANDOFF.md` PONDER-POINT 1 (next session):** this probe tested a
> *key-first-then-chord* framing; the user's open question is whether the correct architecture is
> the **full joint (key, chord) space ranked together** (the top chord may come from a non-top
> key) — which was NOT measured. Revisit the shelving under that framing, on measured fact.

The revisit **ran and was settled**. `OPEN_ITEMS.md` records [[OI-43]] — "Reopen the joint
(key,chord) ranking question … (handoff 'ponder-point 1')" — as **✅ SETTLED 2026-07-12: the joint
step is SHELVED on both axes; the key-axis headroom is redirected to the key layer (the successor
is [[OI-141]]); row CLOSED**, and [[OI-44]] as **✅ DECLARED by the user 2026-07-12 — design
DELIVERED / build SHELVED on both measured axes**. The document carries no note of either.

The cost is a live instruction with no discharge: a reader taking the banner at its word opens a
question the record closed three weeks ago, and the pointer it follows (`COWORK_HANDOFF.md`
ponder-point 1) is a tracking surface, not the answer.

## Instance 3 — ★ the SIGNED voice-leading specification's acceptance criterion is a RETIRED gate

`cowork_voiceleading_axis_design.md` states the regression discipline its build must satisfy, in
five places, as the batch case-identity sets **53 / 24 / 53**:

- `:4` — "gate 53/24/53 case-identity sets byte-identical on all three presets" (the as-built proof)
- `:29` — "the corpus gate (Baroque 53 / Jazz 24 / Default 53), by construction"
- `:78` — "the frozen Bach corpus's BIR case-identity sets (Baroque 53 / Jazz 24 / Default 53)
  reproduce exactly after a change (CLAUDE.md; STATUS.md)" — cited to the governing documents
- `:209` — "the corpus gate (53/24/53 case-identity sets) is untouched by construction"
- `:561` — "the corpus gate (53/24/53 case-identity sets) reproduced byte-identically as the
  no-contamination proof"

**Two things have moved since, and `CLAUDE.md` records both.** The batch stop was re-baselined to
**52 / 24 / 52** at the 2.2e adoption on 2026-07-05 (block (C): removal-only `{bwv244.32@5760}`),
and then **SUPERSEDED IN WHOLE** at R10-b on 2026-07-06 by the granularity-robust union-of-boundaries
unit (block (A), register entry **D-115**), with block (C) retained as *"historical reference only"*
and the reason stated: the batch gate under-counted the true per-onset root error by 15 to 56 times.

The document was written on 2026-07-03 and was correct then — this is drift, not an error of
authorship. But it is the sharpest instance of the class found so far, because the stale statement
is not a description, it is an **acceptance criterion**: a build executed against this
specification would go looking for a gate the project retired, and `:78` sends the reader to
`CLAUDE.md` and `STATUS.md` as its authority, where the number is no longer to be found.

## What is NOT in doubt

None of the three documents' design content is questioned by this row. The Layer-3 specification's
rules, the shelved step's architecture, and the voice-leading axis's measured footing were all read
in full by this wave and produced 16, 4 and 13 register entries respectively (D-343…D-358,
D-376…D-379, D-388…D-400) — every one entered because the content is standing, not because it is
suspect. What is wrong in each case is the frame: what runs, what is still open, and which gate
applies.

## Disposition

For a later doc-sync act, at each document's next touch, and each is one edit:

1. the [[OI-232]]/[[OI-265]] scoping sentence in the Layer-3 banner ("the description below remains
   accurate for the dormant decoder … it is not a description of what runs");
2. a dated discharge note in the joint key-and-chord banner, naming [[OI-43]]'s settlement and
   [[OI-44]]'s declaration;
3. the voice-leading document's five gate citations re-pointed at the governing stop — `CLAUDE.md`
   block (A), the class-(b) root-disagree duration non-increase (**D-115**) — with the historical
   53/24/53 preserved as what the 2026-07-03 build was actually proven against (#12: the as-built
   proof stands; it is the *criterion for future work* that has moved).

**No edit is made here** — this pass is read-only on every specification. Whether a sweep for the
same class over the remaining 75 unread LIVE-SPEC documents should ride the reads, or run as its
own pass once they are read, is a scoping question for the user.

*Provenance: CC-found 2026-08-02 at `cc_instruction_phase1h_continue_live_spec.md`, Task 1,
reading `cowork_layer3_keymode_design.md`, `cowork_joint_key_chord_design.md` and
`cowork_voiceleading_axis_design.md` in full. Cross-ref [[OI-232]] (eight `ARCHITECTURE.md`
instances, resolved), [[OI-265]] (the ninth, resolved), [[OI-274]] (the tenth, open — the
mandatory-read scoring document), [[OI-207]] (the audit this serves).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
