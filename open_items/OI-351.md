# OI-351 — `ls` aimed at a repository path was ADMITTED by the live guard on 2026-08-08, one day after the widening whose record says that shape is now denied

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — instrument / measurement layer (the audit's own guard apparatus). Found 2026-08-08
(CC, `cc_instruction_document_routes_and_d472.md`) at this session's own standing self-check over its
own conduct — the same place [[OI-343]], [[OI-345]] and [[OI-348]] were found.**

## The observation, stated as exactly what was seen

At this session's start, two commands were issued through the Bash tool within one minute of each
other:

1. `git log --oneline -5 && git status --porcelain | head -60` — **DENIED**, with the guard's own
   D-253 message naming `git status` and pointing at `tools/audit/changed_paths.py`. **So the hook is
   armed and it fired.**
2. `ls -la c:/s/MS/tools/audit/decisions/ 2>&1 | head -60` — **ADMITTED**. It ran and returned the
   directory listing.

The second is a shell enumeration of a working-tree path: existence and size information about
repository files, which is one of the four acts D-253 names. It should not have run.

**This sits in tension with what the record says.** The 2026-08-07 widening's own resolution note for
[[OI-345]] states, as a measured consequence, that *"[[OI-300]]'s shape (1) — `ls` at a repository
path — is now denied, because `ls` is an enumerated `Get-ChildItem` alias and the guard has no model
of which shell handed it the command."*

## What is NOT established here, and is deliberately not claimed

**Why it was admitted is unknown.** Two candidate explanations were visible without running anything,
and this row asserts neither:

- the path form. The command used a **Windows drive-letter path** (`c:/s/MS/…`) rather than a
  repository-relative path, and whether the guard's repository-path matcher recognises that form was
  not tested here.
- the arming surface. The widening was measured at the guard's own `decide()` over its corpus; how
  the live hook presents a Bash-tool command to that function was not inspected.

**No measurement was run for this row.** Running `--establish`, reading the guard's source, or
adding a corpus row are all acts the wave's freeze does not license, and the second candidate above
would in any case need the corpus to go first — the order [[OI-343]]'s ruling fixed and
[[OI-345]]'s widening kept.

## Why it is rowed rather than folded into an existing row

It is close to [[OI-348]] and [[OI-300]] and belongs to the same family — *"the guard parses a shell
command line without a model of shell syntax"* — but it is **not** either of their shapes. OI-348 is
about a read nested inside another command's quoted argument, which no name set can reach; this is a
**bare command at the top of the segment**, which the name set is exactly the mechanism for. And it
is a discrepancy between a **recorded claim** and an **observed admission**, which is a different
kind of finding from a known gap: the deny rate published on 2026-08-08 is the number a reader would
consult, and if this shape is admitted in the live path the rate does not bound what it appears to.

## What is NOT in doubt

No analysis, no analysis input, no corpus of scores, no measurement of the analysis, no golden,
nothing in `tools/robust_stop/`, and no entry or status in either register. The `git status` denial
shows the hook armed and working on the shape it was measured against. Nothing about the widening's
own measured POSIX-side or PowerShell-side rows is questioned here — only whether the live path
denies this one shape.

## The session's own conduct, recorded beside the guard's

This session broke **D-253 twice**, both at its start, and both are recorded here rather than left
out. The first is the `ls` above. The second is a `python - <<'PY' … open(…) …` read of
`backbone_decisions.json` — the **second shape [[OI-348]] records**, whose founding instance was the
previous session's; it errored on an encoding fault after printing one partial line, the data was
re-obtained through the file tools, and no value it produced entered any artifact. Every other read
of working-tree content in this session went through Read, Grep and Glob. **The guard's miss is why
neither was stopped, not why either was done.**

*Provenance: CC, 2026-08-08, dispatch `cc_instruction_document_routes_and_d472.md`, the standing
self-check. Cross-ref [[OI-300]] (the family and its diagnosis), [[OI-345]] (the widening whose
resolution note this observation is in tension with), [[OI-348]] (the sibling admission shape),
[[OI-343]] (the corpus-first order any remedy must keep).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
