# OI-162 — Phrase-boundary punctuation cues are built, not unread

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-162 | The phrase-boundary punctuation cues are **BUILT, not unread** (whole-graph fact-dependency audit, 2026-07-13). `cowork_evidence_inventory.md` §1 statuses fermatas as *"UNDISCUSSED as key evidence; not in the note model's 11 documented facts. Cheap to read; enormous leverage"* and double barlines as *"UNDISCUSSED"*. At the code they are **already read**, by the Layer-1.5 phrase-boundary view: fermatas `phraseboundaryview.cpp:113` (`a->isFermata()`), breath/caesura `:120-123`, mid-score key-signature change `:137-142`, DOUBLE/END/repeat barlines `:163-165`, ritardando-family spanners `:183-189`, all-voice-rest spans ≥ `minSilenceTicks` `:200` (threshold **240**, an OI-87 unfit constant). The view is **DORMANT on production** — its only `src/` caller is `regionanalyzer.cpp:431`, inside `applyJointKeyWiring`, gated at `:1472` on `jointKeyWiringEnabled()` (default OFF, `jointkeydecision.cpp:144-145`). **The gap is wiring + publication, not detection.** Inventory correction owed under OI-146(a). | `cc_fact_dependency_audit_report.md` §3.1/§3.8; `phraseboundaryview.cpp:98-200`; OI-146(a); OI-87 | OPEN — correct the inventory at its next authorized touch (doc-only); the wiring itself is key-layer work |
