# OI-320 — `docs/score_inventory.md` states the superseded batch gate as the current gate, in the document `CLAUDE.md` makes a mandatory first read

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10). Found 2026-08-04 (CC, READ WAVE 2,
`cc_instruction_reads_2.md`) reading `docs/score_inventory.md` in full — the wave's reading order 11.**

## Why this document and not another

`CLAUDE.md:811` makes this document a mandatory first read: *"For any task involving scores
(validation, snapshot tests, manual QA, LLM-triage, qualitative review), read
`docs/score_inventory.md` first."* So whatever it says about the gate is what a session starting a
score-touching task is pointed at before it reads anything else.

## The four statements, each read at the object

1. **The quick-pick table's "Measure the BIR gate" row** (`docs/score_inventory.md:27`) gives the
   acceptance figures as *"(Baroque 53, Jazz 24, Default 53 — re-baselined 2026-06-13 + L3-wiring
   delta 2026-06-26; see CLAUDE.md)"*.
2. **The per-preset measurement-dir section** (`:166-167`) repeats them in the same form.
3. **The WiR-coverage section** (`:197`, `:206-208`) repeats them a third time, calls them *"The
   headline gate"*, and then says *"A granularity-robust metric over the full annotated set is
   **roadmap 5.2** — until then, read 'Baroque 53 / Jazz 24 / Default 53' as candidate cases …"*.
4. **The in-tree fixtures row** (`:25`) states *"Wired into `composing_tests` (498/498)"*.

## What is true at HEAD

`CLAUDE.md` gate block (C) records the batch case-identity gate as re-baselined to **52/24/52** at the
2.2e adoption on 2026-07-05, and then **superseded in whole** at R10-b on 2026-07-06 — the block is
headed *"RETROSPECTIVE — the batch 52/24/52 stop (superseded at R10-b, 2026-07-06 — historical
reference)"* and says of the batch form that it *"remains a runnable per-region diagnostic … it is no
longer the regression gate."* Block (A) carries the governing stop and reports its coverage as
326/352. So statement 3's *"until then"* describes a state that ended thirteen months' worth of
waves ago in project time and on 2026-07-06 in calendar time.

Statement 4 is a figure of the same vintage and is not separately established here — it is recorded
as the fourth site rather than measured, because this wave neither built nor ran a suite ([[OI-150]]
is the standing row for stale suite baselines and its own trigger discipline applies).

## Why it is the OI-276 class and where it is sharper

[[OI-276]] recorded three documents stating as current something false at HEAD, the third being a
SIGNED specification naming its acceptance criterion five times as the batch sets. This is the same
defect in a fourth document. It is sharper in one respect only: the other three are design documents
a reader consults deliberately, and this one is the document the governing instructions send every
score-touching session to FIRST.

## Why it is not corrected here

Two reasons, and the second is the binding one. The wave reads and rows. And block (C) deliberately
KEEPS the batch form as a runnable diagnostic under its own commands, so a correction must decide,
per site, whether the sentence becomes "the diagnostic" or "the block-(A) unit" — which is a
presentation decision about a user-ratified gate, and [[OI-312]] is the standing instance of the rule
that redirecting what a user-ratified surface states is a ruling rather than a session's edit.

*Provenance: CC, READ WAVE 2, 2026-08-04, dispatch `cc_instruction_reads_2.md`, Task 1.2. Cross-ref
[[OI-276]], [[OI-232]], [[OI-312]], [[OI-150]], and register entries **D-487**, **D-488**, **D-489**,
which this same read entered from the document's hard rules and licence finding.*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

---

**Dated note — 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Task 2), on the user's
Ruling 62 of `cowork_rulings_2026_08_11_fourteenth_stop.md`. The status of record is the INDEX row.**

**The filing decision this row said a session could not take was taken by the user, and it is BRANCH
TWO.** `docs/score_inventory.md` is a **live governing surface** — the document `CLAUDE.md` sends
every score-touching task to FIRST — so its job is to be true now and its BODY is corrected, not
bannered. That is the convention's own split by document kind, and this document is the instance the
ruling names for the second branch.

**All four sites, each corrected by POINTING at the block that owns it rather than by restating
anything (#6, D-431).**

1. **The quick-pick row is SPLIT into two rows.** One for the **governing** hard stop — block (A)'s
   granularity-robust unit, with its own two commands — and one for the **retired diagnostic**, the
   batch characterisation of block (C). The split is the record's own: block (C) keeps the batch
   form runnable and says so.
2. **The per-preset-dir section** now says in terms that the check is a diagnostic and not the gate,
   and records **why it is kept** — the governing instrument imports its corpus-integrity mechanism,
   so the guard cannot bit-rot into uselessness.
3. **The WiR-coverage section** gains a leading correction naming which gate it is about, and its
   inline acceptance figures are removed in favour of the block that owns them.
4. **The "roadmap 5.2" sentence is replaced by a pointer to block (A)** — and it was the costliest
   of the four, because it told a session on its first read that the governing stop did not exist
   yet. It has governed since 2026-07-06.

**Every former wording is preserved at the correction (#12), and what survives it is named rather
than dropped:** the batch count was never an absolute quality figure, most of it is legitimate
ambiguity, and the pitch-class-root-resolvable qualifier is real. All three are carried by blocks (B)
and (C), which this document now points at instead of keeping a second copy.

**The test-count site is corrected the same way** — it points at `BUILD_AND_TEST.md` rather than
carrying a suite count of its own vintage, which removes the failure rather than refreshing it
(#17f).
