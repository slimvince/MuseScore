# OI-374 — Four guards print through a stream the shared encoding module does not reconfigure, so the guard runner's captured output follows the environment rather than the tree — and the artifact's own ground for trusting the run is not true of them

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — instrument / measurement layer. Found 2026-08-12 (CC,
`cc_instruction_resume_lapse_records.md` Task 2) while verifying that task's own claim at the
objects, which is what put the difference in front of a reader.**

## How it was found

Task 2 claimed that the only movement in its regenerated artifacts was the open-row count. Verifying
that claim meant diffing the two commits as git objects rather than describing the change — and the
diff carried one line nobody was looking for: the guard runner's captured output for
`tools/open_items_split_check.py` changed from `OVERALL PASS <replacement character> bijection
holds` to `OVERALL PASS — bijection holds`, with the tool unedited between the two runs.

**This is the residue of the condition [[OI-373]] recorded and explicitly did not diagnose.** That
row states that the committed guard artifact did not re-derive at this tree while its verdict set
was identical, and says in terms that which tool's text moved was not established. This is that
tool, and this is the cause.

## What is established, at which object

**The mechanism, read at the two tools.** `tools/audit/output_encoding.py` reconfigures **the
calling process's own** standard streams; it sets no environment variable, so a child process
launched by the guard runner inherits nothing from it. `tools/audit/gen_guard_state.py` runs each
guard as a child and decodes its output as UTF-8 with `errors="replace"`. A child that does not
reconfigure its own stream therefore encodes with whatever the environment gives it, and any byte
that is not valid UTF-8 reaches the artifact as a replacement character.

**The demonstration, run both ways rather than argued.** `tools/open_items_split_check.py` was run
twice at this tree, unedited, with only the interpreter's output encoding changed between the runs.
Under the locale codepage its em dash arrives as a replacement character; under UTF-8 it arrives as
an em dash. **Neither run changed the verdict** — both are `OVERALL PASS` — which is why this can
sit in a guard artifact unnoticed: what moves is the captured text and never the exit code.

**The population, derived rather than listed.** Every `*.py` under `tools/` was searched for a call
to the shared module, and the result was compared against the guard runner's own invocation table.
**Four members of the run set do not call it:** the open-items bijection check and the notation-seam
call-path check — both authored by name from outside `tools/audit/` — and two inside it, the
decisions-register check and the finish-line route check. Whether each of the other three prints a
character outside ASCII was NOT tested; only the first is demonstrated above.

**The artifact's own ground, read at the artifact.** `tools/audit/guard_state.json` carries a field
whose whole job is to say why its run can be trusted: *"Every guard here routes its printing through
tools/audit/output_encoding.py, so a findings list can no longer be truncated by a character the
console cannot encode."* **That sentence is not true of the four members above**, and the reason it
matters is the one the sentence itself gives — the module exists because a check once exited
non-zero having printed a clean-looking summary and none of what it found ([[OI-305]]).

## Why it is a row rather than an act

**The remedy is a change to guards this dispatch may not touch.** Three of the four are outside
`tools/audit/` or outside this batch's subject entirely, and adding a call to a shared module is a
mechanism change **D-436** reserves — the same reason [[OI-373]] authors no invocation. Nothing here
proposes a design.

**And the artifact's own sentence is the sharper half.** Correcting the four tools would make the
sentence true; correcting the sentence alone would leave the exposure it describes. Which of those
is done first is not decided here.

## What is NOT claimed

**No verdict moved, and none is claimed to have moved.** Every guard's pass or fail is unaffected by
its output's encoding; what this row is about is the captured TEXT and the artifact's reproducibility.

**The failure mode is NOT demonstrated on this tool.** What the module was built against is a
`UnicodeEncodeError` that truncates a findings list. An em dash is inside the locale codepage here,
so it degrades silently rather than raising. Whether any of the four would truncate on a character
outside that codepage is **not tested and not asserted** — the row records the missing coverage, not
an observed truncation.

**No gating verdict.** Whether this row gates is derived from a cut and is never hand-added
([[OI-336]], **D-436**).

**Nothing about the analysis.** No behaviour, no measured value, no golden, no corpus of scores,
nothing under `tools/robust_stop/`. The subject is the audit's own guard apparatus and one field of
its own record.

## The closing acts, neither of them this session's

**(a) The four guards route their printing through the shared module**, after which the artifact's
sentence is true of every member and the captured text stops following the environment.

**(b) Or the sentence is bounded to the members it covers**, which is the honest smaller act and
leaves the exposure standing where a reader can see it.

*Provenance: CC, 2026-08-12, dispatch `cc_instruction_resume_lapse_records.md` Task 2, at the
object-level verification of that task's own movement claim. Rowed under **D-641**: an apparatus
finding is rowed and left. Cross-ref [[OI-373]] (the row that recorded the condition and left the
cause open), [[OI-305]] (the finding the shared module was built against), [[OI-366]] (an
establishment check reporting a difference no content change explains — the same shape at a
neighbouring tool).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

## ★ WORTH-TEST RECORD — 2026-08-12 (CC, `cc_instruction_worth_test.md` Task 2): NOT DISCARDED

**What has happened to this row.** The user's **Ruling 68** of 2026-08-11
(`cowork_rulings_2026_08_11_sixteenth_stop.md`; the worth test now homed at `CLAUDE.md` principle
#10, register entry **D-174**) was applied to this row and the verdict is **WORTH FIXING**. The row
**stays open and stays owed**; its two closing acts stand exactly as stated above, and **nothing is
fixed here** — which of the two is done first is still not this act's to decide.

**The #19 check, taken FIRST — and it decides the row before the two limbs are reached.** **This row
carries an establishment obligation, so Ruling 68's first carve-out applies: it is never discarded,
whatever the rest of the test would say.** The ground is read at the object.
`tools/audit/guard_state.json` carries a field whose whole job is to state why its run can be
trusted, `why_this_run_can_be_trusted_where_earlier_ones_could_not`: *"Every guard here routes its
printing through tools/audit/output_encoding.py, so a findings list can no longer be truncated by a
character the console cannot encode."* **That is not true of four members of the run set** — the
population this row derives. A gate trusted on a ground that does not hold is trusted while merely
unfalsified, which is what #19 refuses; and the record already classes this exact shape as an #19
matter at **D-647**, where a guard's published rate must publish the ceiling the guard cannot see,
because *"a session that does not know the ceiling reads the published rate as covering more than it
does."*

**Limb (b) is met independently, at ONE member, so the verdict does not rest on the carve-out
alone.** `tools/notation_seams/gen_callpath_facts.py --check` is a **code-against-record comparability
check** — the guard set's own description of it is *"the notation-seam call-path anchors are still
where the artifact says"*. Three facts about it were read at the code this session: its findings are
written to **`sys.stderr`**, one line per failure under the heading *"ANCHOR VERIFICATION FAILED (a
citation no longer matches the source — a finding, #13)"*; the tool **does not call the shared
encoding module**; and each failure's text is **composed from source-file content**, not from ASCII
literals in the tool, so the findings list is **not bounded to ASCII by construction**. That is the
path OI-305 measured: a check exiting non-zero having printed a clean-looking summary and none of
what it found.

**NOT CLAIMED, and the boundary matters more than the finding.** It is **not** claimed that any
current anchor's failure text carries a character outside ASCII, **not** claimed that a truncation
has occurred, and **not** claimed that any verdict has ever moved — an encoding difference moves
captured text and never an exit code, and the parent decodes with `errors="replace"` so it never
raises either. What is established is the PATH, not an instance of it. The other two members of the
four were not examined here.

**The consumer, named for completeness though the carve-out does not turn on it.** **No analysis
decision consumes the guard runner's captured text**; its readers are
`tools/audit/gen_guard_classification.py` and sessions reporting the guard set's state. Under
**D-673** that would bound an enumeration's reach — but it does not reach an artifact's own
statement of why it may be trusted, which is what #19 governs.

**What this record does NOT do.** It authorizes no fix, no design and no inference change. It routes
no printing through the shared module, bounds no sentence, and takes no verdict on which of the two
closing acts comes first. It moves no status and closes no row. And it says nothing about [[OI-372]]
or [[OI-373]], both of which are different subjects and were tested separately.

## ★ THE CASCADE-SWEEP BATCH'S OBSERVATION — 2026-08-25: the variable is the LAUNCHING SHELL, and it moved a committed artifact between two commits of the same batch

**What widens.** The demonstration recorded above changed *the interpreter's output encoding* between
two runs. The cascade-sweep batch of 2026-08-25 (`cc_instruction_cascade_sweep.md`) observed that the
variable is **not only the interpreter's output encoding but the LAUNCHING SHELL** — the shell a guard
run is started from is enough on its own to move which bytes reach the artifact, because a child that
does not reconfigure its own stream encodes with whatever that environment gives it. Nothing in the
mechanism read at the two tools above changes; what changes is the size of the population of ways to
provoke it, which was previously stated only as an interpreter setting.

**And it can move a guard artifact's committed bytes BETWEEN TWO COMMITS OF THE SAME BATCH.** That is
the sharper half, because the earlier demonstration was two runs at one tree and this is two committed
states of the record. Measured at the objects for this note, not relayed: `git diff 428b44143d
0f18b358bc -- tools/audit/guard_state.json` is a single hunk of one changed line — the captured
`stdout` of `tools/open_items_split_check.py`, whose `OVERALL PASS <replacement character> bijection
holds` at the earlier commit is `OVERALL PASS — bijection holds` at the later one. It is the same tool
and the same character as the founding instance at the head of this row, arriving the other way round;
the cascade-sweep batch reports the cause as its own final run being launched from a different shell.

**NO VERDICT MOVED, and that is recorded with the observation rather than left to be inferred.** Both
runs reported `OVERALL PASS`, and encoding touches captured text and never an exit code — which is the
property already established above and is why this can sit in a guard artifact unnoticed. No guard's
pass or fail is affected, and no summary figure moves.

**What this note does NOT do.** It flips no status, adds no verdict, opens no row and proposes no
remedy; the two closing acts stand exactly as stated above and neither is taken. It changes nothing in
the mechanism, the derived four-member population or the artifact's own sentence. It touches no tool
source. Nothing about the analysis — no behaviour, no measured value, no golden, no corpus of scores,
nothing under `tools/robust_stop/`.

*Provenance: observation from `cc_instruction_cascade_sweep.md`'s batch, 2026-08-25, relayed through
its commit record at `0f18b358bc`; the artifact difference re-measured at the git objects by CC,
2026-08-25, dispatch `cc_instruction_ledger_harvest.md` Task 0(b), which is the act that appended this
note. [[OI-374]] already owns this subject, so no new row is opened.*
