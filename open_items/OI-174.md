# OI-174 — The key layer emits Altered on material with no altered tone

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-174 | **The key layer emits an `Altered` local key on material with NO altered tone — 22 of the 23 Jazz `Altered` regions are entirely diatonic to their own notated signature (CC-measured 2026-07-14; an INFERENCE problem, declared not fixed).** Measured at the objects, not inferred: with the OI-170 signature-mask variant on, the `diatonicToKey` flag flips `false → true` on **22 of the 23** regions whose local key is `Altered` — and that flag is true exactly when **every** sounding pitch class (weight > 0.2) lies inside the key signature's own diatonic collection. The regions' committed chords are plain diatonic triads and sevenths (`bwv135.6@21600 A`, `@23520 G` under `C♯alt` — i.e. the C-major signature; `bwv64.8@21600 Em`, `@22560 B` under `G♯alt`; full list in the report). **An "altered" mode over material containing no altered tone is a key-layer inference failure**, not a chord-layer one — and it is the reason the OI-170 flag-flip count (22) came in above the predicted range (≤18). Chord-side it is now harmless: the OI-170 fix makes the chord layer read the SIGNATURE's collection whatever mode the key layer emits, so a wrong mode can no longer corrupt a collection test. **NOT FIXED — declared to Cowork per the standing rule (no inference-problem-driven coding).** Related: the same 24-ish Altered population is what OI-168 measured; the mode-grading consolidation (`cc_mode_grading_adjudication_probe_report.md`) already found the dominant-family exotic modes need parent-collection reading on the GRADING side — this is the same phenomenon on the EMISSION side. | `cc_oi170_measure_report.md` §5.2/§6/§7; the generated artifact `cc_oi170_measurements.txt` (§flips — 22 flag flips, all `false → true`); `keymodeanalyzer.cpp:70-71` (all 21 modes active); OI-168; OI-170 | OPEN — CC-measured 2026-07-14; a **key-layer** item for the key-layer design pass (the key funnel is shut until the corrected layer assignment is ratified) |
