# OI-168 — The collection/tonic split breaks inside the engaged decoder

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-168 | ★★ **THE COLLECTION/TONIC SPLIT BREAKS *INSIDE* THE ENGAGED DECODER — at the site OI-167 clears (DT-1, Class-A #18; DT-7 rider).** OI-167 upgrades THEORY→FACT on the claim that `analyzeChord`'s two key-consuming terms are *"pure collection-membership tests (the constructed pc-set is provably identical for **all 7 diatonic modes** of one signature)"*. **True as written, insufficient as used:** `analyzeChord` accepts **all 21** `KeySigMode` values (*"All 21 modes are active"*, `keymodeanalyzer.cpp:70-71`) and the key layer **emits non-diatonic ones**. Both terms test membership in **S = { (keyTonicPc + scale[i]) mod 12 }**, where `keyTonicPc = ionianTonicPcFromFifths(fifths) + keyModeTonicOffset(M)` and `scale` = the intervals of `DIATONIC_PARENT_INDEX[M]` (`chordanalyzer.cpp:1329-1342`; terms at `:574-578` + `:901-905`, called `:1403`/`:1406`). **Derivation: S = the signature's collection TRANSPOSED by δ = keyModeTonicOffset(M) − keyModeTonicOffset(parent(M))** — the tonic cancels **iff δ=0**. δ=0 for **19** modes ✅; **δ=+1 for `Altered` (offset 1, parent Ionian offset 0) and `AlteredDomBB7` (offset 8, parent Mixolydian offset 7)** ❌ — for these two, S is the collection **transposed up a semitone** (C-Altered ⇒ S = the C-major collection, while the signature's collection is B major: **2 of 7 pcs shared**). Structurally unsatisfiable: their tonic is not a member of their parent collection, so **no** parent index fixes it. **This contradicts the code's OWN comment** (*"mapped to their diatonic key-signature parent **so that** diatonic-root bonus and scale-membership scoring **stay correct**"*, `:1333-1335`) — a load-bearing causal claim about our own system, checkable and unchecked (**DT-1**). **LIVE: `Altered` is emitted on 24 Jazz regions** (0 Baroque/Default; `AlteredDomBB7` 0-firing everywhere — **DT-7**), and the emitted region key IS what reaches the scorer (`localKeyMode = localKey.mode`, `regionanalyzer.cpp:955-957` → `analyzeChord` `:987-988`). **The ENGAGED `ChordSliceDecoder` calls the same `analyzeChord`** (`chordslicedecoder.cpp:452-454`) ⇒ **its candidate scores are tonic-dependent on a live population, so the collection/tonic split is FALSE for the engaged L4 today.** *(OI-167's two named sites are separately CONFIRMED harmless — Gate G-E and the Aeolian guard are both unreachable from the decoder, and the guard has 0 fire sites on all 3 corpora.)* **Proposed fix (restores the split as STRUCTURAL, not algebraic):** replace both membership loops with `pcInMask(diatonicMaskFromFifths(keySignatureFifths), pc)` — the repo's existing *"Key-agnostic: depends ONLY on the notated signature, never a resolved mode"* primitive (`analysisutils.h:82-94`); **byte-identical for the 19 δ=0 modes**, corrects the 2, and the terms then take **no tonic at all** so the property cannot silently lapse again. **Behavior change on Jazz (≤24 regions) ⇒ robust-stop diff + class-(b) duration non-increase + explained run-level set-diff + user ratification (#14). Whether the corrupted bonus flips a winner is UNMEASURED** (needs a build). **★ MEASURED 2026-07-14 (`cc_oi168_magnitude_report.md`; default-OFF instrumentation + a default-OFF signature-mask A/B, the OI-110 pattern; OFF path byte-identical on 352×3, all three suites green, no golden refreshed, `tools/robust_stop`/`tools/corpus` untouched):** the fix path is a **CORRECTNESS RE-BASELINE**, and the smallest possible one. **Baroque + Default are BYTE-IDENTICAL under the variant** (352/352 each — the δ=0 derivation verified at runtime, `…MembershipDiffers` = 0). **Jazz: 9 files change, exactly ONE committed chord flips** — `bwv145.5@12960` (local key `D#alt`): `Ebm` (root pc 3) → **`B/Eb` (root pc 11)**. The current reading is a chord the notes do not contain (sounding D♯–F♯–B, tpc 23/20/19 = a B-major triad in first inversion; E♭ minor needs B♭). **Both oracles back the variant:** music21 root 11, and the robust-stop enumeration already lists this region as a class-(b) root FAILURE (`bwv145.5@12960 our_root=3 -> dcml_root=11 dur=480 cls=b`). Robust-stop diff on the variant corpus: **removal-only, one run, class-(b) duration −480 on Jazz, +0/+0 Baroque/Default, zero additions, OVERALL PASS.** The other 22 Altered regions move their SCORE (+0.30, the withheld `diatonicRootBonus`) without moving the winner — the defect is mostly latent, but not entirely. **Two register corrections the measurement forces:** (1) **the `ChordSliceDecoder` is NOT on the production path** — `decoderWindowCalls` = 0 on all three corpora; it is reached only from `--decode-chords`/`--dump-fullspine`/`--dump-joint-probe` (`batch_analyze.cpp:3121` calls it "the dormant per-slice decoder"), so the defect is live **TODAY THROUGH THE REGION PATH** (`regionanalyzer.cpp`'s committing `analyzeChord` call) and the decoder will INHERIT it when engaged — the substance is unchanged and if anything stronger, only the word "engaged" is wrong; (2) **the scorer's exposure is ~2× the output surface** — the committing call runs **49** times under `Altered` on Jazz (10 scores) while only **24** Altered regions survive to `.ours.json` (9 scores), because the region analyzer scores each region on more than one attempt. `AlteredDomBB7` 0-firing CONFIRMED at the counter. **Next step (separate, user-ratified commit):** promote the signature-mask form (delete `pcInKeyCollection`'s committed branch), O-12-snapshot + re-baseline `tools/robust_stop`, explained run-diff = the one run above. | `cc_oi167_collection_tonic_report.md` §3/§4.3; `chordanalyzer.cpp:1329-1342/574-578/901-905/1333-1335/1403/1406`; `keymodeanalyzer.h:81-103`; `keymodeanalyzer.cpp:70-71`; `analysistypes.h:565-592`; `keymode_branch_tests.cpp:153`; `analysisutils.h:82-94`; `chordslicedecoder.cpp:452-454`; OI-167 | ✅ **FIXED + RE-BASELINED 2026-07-14** (`cc_oi168_fix_report.md`; dispatch `cc_instruction_oi168_fix.md`, user-ratified 2026-07-13). Both terms now test `pcInMask(diatonicMaskFromFifths(keySignatureFifths), pc)` and take **no tonic and no mode scale** (the dead `keyTonicPc`/`scale` parameters are gone, so the property is structural). Measured at the objects, reproducing the A/B exactly: **Baroque + Default BYTE-IDENTICAL (352/352 each)**; **Jazz 9 files, exactly ONE committed chord flips** (`bwv145.5@12960` `Ebm`→`B/Eb`, to the DCML ground-truth root); **run-level set-diff REMOVAL-ONLY, one run, zero additions**; class-(b) duration **Jazz −480 / Baroque +0 / Default +0**; `robust_stop_diff.py` **OVERALL PASS**. Reference re-stamped (O-12 snapshot `snapshot_2026-07-13_pre_oi168/`); all three suites green with **no golden refreshed**. **⚠ THIS DID NOT MAKE L4 TONIC-INDEPENDENT — see OI-170**: three further sites (`diatonicToKey`, Gate I, Gate L) carry the identical defect and are still open. The collection/tonic split still does NOT hold. |
