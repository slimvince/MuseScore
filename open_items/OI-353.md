# OI-353 — Six comments in `src/` say the production notation record path is "default OFF"; the configuration sets its default to TRUE

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10), in the code. Found 2026-08-09 (CC,
`cc_instruction_return_continuation.md` Task 1) while establishing which arm ships, which is step 1
of the [[OI-349]] probe.**

## What was read

`src/composing/composingconfiguration.cpp` sets the default value of the
`composing/useJointNotationRecord` setting to **true**, and the comment immediately above the call
says so in terms: *"default TRUE since the user-ratified switch (2026-07-27): the joint estimator's
notation record IS the production notation analysis. Explicit false selects the LEGACY notation
path, still compiled and dormant."* That agrees with `CLAUDE.md` gate block (A)'s staged-scope
paragraph, which records the notation switch and its ratification.

**Six other sites describe the same flag as default OFF.** They are enumerated with their located
lines at `tools/audit/oi349_record_arm_precondition_probe.json` →
`incidental_finding_kept_apart.the_sites_saying_otherwise`, derived by search on each run rather
than hand-listed, so the set cannot quietly go stale in either direction. In prose, they sit at:
the span-annotation emitter's record branch, the note-seam record arm, the implode bridge's record
branch, the tuning bridge's record branch, the record adapter's own header, and the composing
analysis build file's module comment.

## Why it is rowed

These are the comments a session reads when it wants to know **which arm produces the in-app
analysis**, and they say the dormant one does. The never-work-from-memory rule sends a session to
the code for exactly this question, and the code answers it wrongly in six places and correctly in
one.

It is [[OI-232]]'s class one surface over. That row found five stale build-state statements in
`ARCHITECTURE.md` and diagnosed the cause as an adoption doc-syncing the blocks it ADDED rather
than the older blocks it FALSIFIED. OI-232 is resolved on the document half; the same adoption left
the same debt in the code, and nothing looked there.

## What is NOT in doubt

- **The behaviour.** No comment is executable. The default is read from the configuration, and the
  record arm is what runs.
- **The switch itself**, which is user-ratified (2026-07-27) and recorded in `CLAUDE.md` gate
  block (A).
- **The [[OI-349]] finding**, which rests on the configuration and on the decoder, not on any of
  these comments.

## What is owed, and why this batch did not do it

The correction is mechanical: each comment states the current default, and where the surrounding
sentence turns on it — *"Flag OFF -> the legacy path below runs byte-identically"* reads very
differently once the flag is on by default — the sentence follows. **Every site is a `src/` file**,
and the batch that found it is read-only on the analysis and admits no `src/` change. One edit for
a session whose authority reaches `src/`.

*Provenance: CC, 2026-08-09, dispatch `cc_instruction_return_continuation.md`, Task 1, the [[OI-349]]
read-only probe under the user's Ruling 9 of 2026-08-09. Cross-ref [[OI-232]] (the same class in
`ARCHITECTURE.md`, resolved), [[OI-349]] (the probe that met it), [[OI-180]] (the retirement map at
which the flag and the legacy branch both go).*

## Dated note — 2026-08-09 (CC, `cc_instruction_return_continuation_8.md` Task 4): Ruling 16 executed, sweep first, and the family is materially larger than this row

**The ruling's order was kept and it is the reason the outcome is worth reading.** Ruling 16 refused
a per-symptom fix and required a read-only SWEEP first, so the family would be enumerated against
the configuration facts rather than read off this row's six sites.

**The sweep is `tools/audit/gen_arm_comment_sweep.py` → `arm_comment_sweep.json`.** Its candidate
population is DERIVED, not listed: every comment BLOCK under `src/` that makes a dormancy,
no-consumer or flag-default claim AND names the joint module or the notation record. **The unit is
a BLOCK rather than a line**, because the build-file entries put the state claim and its subject on
different lines of one block — a per-line scan would have missed precisely the group the fourth
continuation's reconnaissance reported as largest. Each candidate is graded against **two
configuration facts, read at the code by anchor string on every run and never recalled**: the flag's
default value, and whether any non-test `src/` translation unit outside the joint module references
a joint or record symbol.

**★ THE RULING'S PREDICTION IS CONFIRMED AT THE OBJECTS.** Its own words were that this row's six
sites *"are the found members, not the family"*. They are: the false set reaches beyond the
default-OFF claim, and its **largest group declares the JOINT MODULE'S OWN dormancy** — comments
written before the notation switch, describing a state that switch ended — in the build file, in
three joint headers, in the record adapter's header, and in three test-file headers whose closing
sentence makes a claim about `src/`.

**The comment-only commit corrected every FALSE-AT-HEAD block**, this row's six among them. Each
correction changes only the false claim; every accurate sentence in every block stands. **The diff
was verified comment-only MECHANICALLY** — every changed line under `src/` begins with a comment
marker — so no behaviour can move, which is what the ruling's #14/#15 clause asks for.

**★ WHAT WAS DELIBERATELY NOT EDITED, and it is the ruling's own clause rather than a shortfall.**
Ten blocks are **HELD**. Their claim is that a joint-INTERNAL module — the adapter, the decoder, the
tables loader, the class value type, the fact adapter — has no production consumer. No outside
`src/` file references those symbols, so the derived facts do not refute them literally; whether
they are nonetheless ON the live path is a CALL-GRAPH question, which is a judgment about how the
decode is composed rather than a file-level fact. *A sibling whose falsity is not mechanical is
HELD, not edited.* Each is named in the artifact with that reason.

**★ THE SWEEP'S TWO STOPS BOTH FIRED DURING THIS ACT, which is why it is a mechanism and not a
list.** An unclassified candidate halts the run — so a sibling added by a later commit cannot enter
the population silently — and it fired first, over the whole derived population. A verdict for a
block the scan no longer finds halts it too, and that one fired *after* the corrections, which is
what forced the corrections to be RECORDED with their former wording (#12) instead of the stale
verdicts being quietly deleted.

**No value is restated here (D-431)** — every count lives in the artifact.

*Resolution belongs in the INDEX row; dated notes may be appended here.*
