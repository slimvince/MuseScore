# OI-211 — The parked crash: making a hidden piano part visible in a 60-bar arrangement on the switched build

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — the analysis-cost / notation-surface cluster (observed-once, unreproduced)**

**The report (user, 2026-07-27, running the switched build).** The user crashed MuseScore while making
a hidden piano part visible in a sixty-bar arrangement on the switched build (`useJointNotationRecord`
default ON). A **second attempt did not reproduce** the crash.

**User-directed disposition: PARKED pending recurrence.** No work now — observed once, not reproduced,
so there is no mechanism to diagnose yet. The row exists so the observation is not forgotten if it
recurs (register rule (c)/(e)).

**The decisive cheap test recorded for when it recurs.** Reproduce with `useJointNotationRecord = false`
(the dormant legacy arm, the deliberately-preserved revert escape). If the crash reproduces on the
legacy arm too, it is a MuseScore part-visibility fault independent of the joint record path; if it
reproduces only on the record arm, it implicates the record path — separating the two before any
diagnosis spends effort.

**The sub-question it raised.** Whether **hidden staves feed the analysis input at all.** The record
producer decodes the whole score with no staff exclusion beyond the chord-track set ([[OI-212]],
[[OI-204]]); whether a hidden/invisible staff is in or out of the L1 fact surface is unestablished, and
a visibility toggle that changes the analysis input would be an input-scoping question (the [[OI-204]]
class) as well as a possible crash trigger. Related interactive-path regression: [[OI-206]].

*Provenance: user field report 2026-07-27 on build `2a81af273e`; rowed by CC at the analysis-cost
dispatch's Task 0, 2026-07-28.*
