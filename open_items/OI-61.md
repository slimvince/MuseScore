# OI-61 — Future Architectural Considerations (4 held items)

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section H — Long-horizon roadmap (held deliberately; listed so they are never "forgotten")**

| OI-61 | Future Architectural Considerations (adopted out of §G by the backlog triage, 2026-07-13; the six items at `STATUS.md:3730-3745`). **Hold confirmed as a list**, with **two dedupes (#6 — one home per concern): (i) "Voice role information in `HarmonicRegion`" is the substance of the ACTIVE row OI-74** (voice/staff identity dropped at `ChordAnalysisTone` — the shared tone surface is voice-blind), owned by the E4 fact-publication/surface design and OI-145 wave 3 — **and its stated trigger ("revisit when sophisticated tuning algorithm is implemented") is now WRONG**: the trigger fired by a different route entirely (the evidence-publication design), so the item is promoted THERE, not here; **(ii) "`isChordTrackStaff()` → Part-level flag" is OI-48's substance** (still name-based at `regiontonecollector.h:73`). The remaining four (bridge-file reorganization by musical concept; instance-based vs static analyzers; the `HarmonicRegion` include-pair friction; the "chord track" → "chord staff" rename, ~31 occurrences) are genuine long-horizon holds with accurate triggers. | STATUS FAC (`STATUS.md:3730-3745`); `cc_backlog_triage_report.md` §5; OI-74; OI-48 | OPEN — long-horizon hold (4 items); 2 items deduped into OI-74 / OI-48 |
