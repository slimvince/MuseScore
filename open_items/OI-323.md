# OI-323 — the shell read guard admitted a forbidden working-tree read: a measured false NEGATIVE

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — instrument / measurement layer. Found 2026-08-04 (CC, READ WAVE 2,
`cc_instruction_reads_2.md`) by the standing self-check on the session's own conduct.**

## The two commands

Both were issued by this session, minutes apart, while checking register entries **D-042** and
**D-057** against the code (dispatch Task 2.2). Both are reads of working-tree content through a
shell text utility, which **D-253** forbids.

| | command | outcome |
|---|---|---|
| first | `grep -n "changePointSlices\|KeyModeSequenceDecoder" src/composing/analysis/region/regionanalyzer.cpp` | **RAN** — returned four matching lines of the file |
| second | `grep -n "resolveKeyAndMode" src/notation/internal/notationcomposingbridgehelpers.cpp src/notation/internal/notationcomposingbridge.cpp src/composing/analysis/key/keyresolver.h` | **DENIED**, with the correct message naming D-253 |

## What is rowed, and what is not

**Not rowed here — the session's own conduct.** Issuing either command was a violation of the
standing rule; the session should have used the Grep tool for both, and did so for every subsequent
read. That half is reported in this wave's own report as a conduct finding against the standing
self-check, not as an instrument defect.

**Rowed here — the guard.** It denied one and admitted the other. So its detection rate on the shape
it exists to catch is **not 100 %**, and the true rate is **unmeasured**.

## What is NOT established, and is deliberately not guessed

Which difference between the two commands decided the outcome. The candidates visible in the pair are
the escaped alternation `\|` inside the pattern argument, and one path argument versus three. There
may be others. This row records the two commands verbatim so the difference can be established by
measurement rather than by reading the guard's source and reasoning about it — which is how
[[OI-300]]'s four false-deny shapes were each established, one measured command at a time.

## Why it is not fixed here, and where it belongs

The freeze is not the only reason. A change that makes the guard deny MORE owes a measured
false-deny rate exactly as [[OI-300]]'s pending change owes a measured detection rate — the two are
the same establishment run over the same corpora, one measuring misses and the other measuring false
denies. [[OI-300]] already carries that run as an establishment obligation, its corpora already
exist, and this row should be **folded into it** rather than triggering a second run.

The direction matters and is stated so a later reader does not file this with [[OI-300]]'s four
shapes: those are FALSE DENIES (the guard blocking something lawful), an inconvenience. This is a
MISS (the guard admitting something forbidden), which is the failure the guard exists to prevent.

*Provenance: CC, READ WAVE 2, 2026-08-04, dispatch `cc_instruction_reads_2.md`, the standing
self-check. Cross-ref [[OI-300]] (fold into its establishment run), register entry **D-253** (the rule
violated).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
