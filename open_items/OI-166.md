# OI-166 — The key-agnostic cadence pre-scan is not built

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-166 | ★★ **THE KEY-AGNOSTIC CADENCE PRE-SCAN IS NOT BUILT — the design opening's identification is REFUTED at the code.** `cowork_key_layer_design_opening.md` §1 proposes that the dormant, certified `functioncadence.cpp` *"appears to BE"* the June dossier's called-for pre-scan, and its §5 flags the identification as *"argued from its shape, not yet verified at code"*. **Verified; it fails.** The detector IS key-agnostic (FACT — `detectFunctionalCadences(events, params)`, `functioncadence.h:260-262`, takes no key), but it is **CHORD-DERIVED**: the tonic hypothesis is read off the **Layer-4 committed chord root** — `tryAuthentic` `const int tonicPc = arr.rootPc;` (`functioncadence.cpp:229`), `tryPlagal` `:409`, and `tryDeceptive`/`tryHalf`/`tryEvaded` `norm(root ± 7)` at `:301`/`:358`/`:450` — and every entry gate reads `.quality`. The June dossier specified a **bass-driven, chord-free, tonic-ENUMERATING** pre-scan running *before* `analyzeKeyMode` (`cc_cadence_key_investigation_dossier.md:203-209`: *"root motion (descending-fifth **bass**) and leading-tone resolution; **for each candidate tonic accumulate** a cadential weight"*). Different units. **⇒ `cowork_evidence_inventory.md` §8 circle 2's break ("BROKEN BY THE KEY-AGNOSTIC FORM … what remains is plumbing") tests the wrong axis: key-agnostic ≠ layer-forward.** L3 consuming this vote is L3←L5←L4←L3, a real cycle. **A FORWARD BREAK IS REALIZABLE** — the four chord-free predicates already exist (`eventHasPc` `:38`, `voiceMovesFromTo` `:52`, `dominantTritonePresent` `:72`, `leadingToneResolves` `:117`, all reading ONLY per-voice notes + a candidate tonic) — so the pre-scan is buildable as an **L1.5 derived view** consumed strictly forward by L3, with L5 keeping the cadence typology + modulation confirmation. Trade-offs (the #6 duplication risk; the measured-unknown precision cost of the coarser chord-free vote; the already-sanctioned forward-override alternative) at report §5.2. | `cc_fact_dependency_audit_report.md` §0/§5.2/§6; `functioncadence.cpp:38-75/117-124/229/301/358/409/450`; `functioncadence.h:82-84/260-262`; `cc_cadence_key_investigation_dossier.md:203-209`; `cowork_evidence_inventory.md` §8 circle 2; OI-161 | OPEN — **a STOP before any cadence→key build; the layer placement is the design pass's to ratify.** The design opening's §4(2) cadence-vote probe must be re-scoped to the CHORD-FREE predicate set (measuring the built L5 detector would measure the wrong unit) |
