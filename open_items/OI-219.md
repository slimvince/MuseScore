# OI-219 — Process: pre-registered #17(b) prediction bands were not registered before measuring (analysis-cost dispatch)

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section — process / methodology (a declared #17(b) shortfall; sealed §S finding S5)**

**The finding (declared by CC at the analysis-cost dispatch, restated in §S).** The analysis-cost
dispatch required pre-registered prediction bands per task (#17(b): a written quantitative prediction —
fire-rate, magnitude, direction, population — recorded BEFORE measuring) in two places; **none of CC's
own bands were registered before the measuring run.** CC surfaced this honestly. The measurements
stand; what was lost is the guard against motivated interpretation — and [[OI-218]] (the "incremental
patching saves ≤ 40 %" conclusion that does not follow from a cold-decode cost share) is an instance of
exactly the error that guard exists to catch.

**The standing reminder (for every future dispatch).** Register the #17(b) prediction bands in the
artifact (the `manifest.json` / a `predictions` block) **before** the measuring run — no prediction, no
build (#17(b), literally). A band registered after seeing the number is not a prediction. This applies
to CC and Cowork dispatches alike.

**Record only** — a process finding, not a code defect; no build. Related: the Premise Gate (#17), the
constrained-optimum ledger corollary; [[OI-218]] (the motivated-interpretation instance it would have
caught); [[OI-222]] (the sibling process finding — the blinding failure — from the SAME OI-199 pass).

*Provenance: CC-declared at the analysis-cost dispatch (2026-07-28); restated as §S S5. Recorded at the
OI-199 pass-1 unblind commit (record-only, not a reconciliation subject).*
