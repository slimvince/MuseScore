# OI-342 — The item-1 route table's "owner is unambiguous" column carries "outside this dispatch's edit surface" for part of its population, and the artifact already has a separate field for that

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — instrument / measurement layer (an audit artifact's authored field). Found 2026-08-04
(CC, `cc_instruction_finish_line_item1b.md`, Task 3) while deriving how much of item 1's re-home
class is executable.**

## The finding

`tools/audit/decisions/finish_line_item1_routes.json` carries two authored fields that answer
different questions:

- `owner_is_unambiguous` — is the owning specification determinate?
- `executable_under_this_dispatch` — does that owner lie inside the edit surface the dispatch
  authorizes?

The artifact states the second's purpose in its own words: a small executed set is *"a SCOPE BOUND,
not a verdict that the remaining entries have no owner."* **For part of the population the first
field is nevertheless carrying the second's answer.** Ten open rows have `owner_is_unambiguous:
false` whose recorded reason names **only** the edit surface and no ambiguity at all — for example
D-604, whose reason reads *"That is a GRADING convention and belongs beside the four the gate block
already states"*, and D-602, D-486, D-603, D-564, D-582, D-451, D-473: each names a determinate site
in `CLAUDE.md` and then says it is *"outside this dispatch's edit surface"*.

**Not every member of that ten is the same case, and the row says so rather than proposing a
number.** Three have a genuinely soft owner independent of the edit surface — D-539 (*"the
principles"*), D-538 (*"the project's standing rules"*) and D-281, which names two candidate homes
(`CLAUDE.md` gate block (C) and `BUILD_AND_TEST.md`). So the corrected count is a judgment, and this
row deliberately does not make it: the dispatch that found this instructed *do not guess*, and
re-authoring another wave's judgment table is not a reading act.

## Why it matters

The finish line is **the scope** — additions to it are a user ruling by its own terms — and this
column is what a reader consults to see how much of item 1 is closeable. Read at face value it says
**zero** entries of the re-home class have a determinate owner left, which is true only of the
`ARCHITECTURE.md` surface the preceding dispatch could edit. A dispatch whose edit surface includes
`CLAUDE.md` would find work here that the column currently hides, and the artifact's own separate
field is what was supposed to keep those two facts apart.

## What is NOT in doubt

The population, every entry identifier and every count in that artifact are DERIVED and re-derive at
HEAD — `gen_finish_line_item1_routes.py --check` passes. What this row concerns is one AUTHORED
column, marked authored on every row exactly so that a reader does not take it for a measured
quantity (**D-431**). No route is wrong as a route; no entry's home moved; nothing about the analysis
is touched.

## Classification

**Apparatus under D-641, and therefore rowed and left — no wave, no dispatch, no surface.** The
subject is an authored field in the audit's own artifact. It bears on no analysis, no analysis input,
and no instrument any measurement of the analysis depends on.

**Under D-438 this row gates by the declaration's own default**, its subject column being the
instrument/measurement layer, which the over-inclusive first cut does not propose as an apparatus
candidate. **No gating verdict is hand-added here**, for the reason [[OI-319]]'s provenance records.

*Provenance: CC-found 2026-08-04 at `cc_instruction_finish_line_item1b.md` Task 3, deriving the
remaining executable population from the artifact rather than reading its stored counts. Cross-ref
[[OI-335]] (an authored field inside the artifact a completion statement rests on — the same shape at
a different field), [[OI-338]] (an authored half contradicting a derived half in one file),
[[OI-340]] (the ruling that holds the NO-HOME class of the same item).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

---

## Dated note — 2026-08-04 (CC, `cc_instruction_guard_fix_and_item1d.md`, Task 4): the finding is CONFIRMED and generalized by a derivation, and one name in this row's own enumeration is corrected

**The row's ten is right and one of the names beside it is not.** Cut at HEAD by the exact phrase
those reasons contain, the rows whose recorded reason carries *"outside this dispatch's edit
surface"* number **ten**, exactly as this row says. **`D-604` is not one of them.** Its recorded
reason ends at *"belongs beside the four the gate block already states"* and never names the edit
surface, so the section above quotes it correctly and then places it in the wrong set. With D-604
removed, the seven the section names — D-602, D-486, D-603, D-564, D-582, D-451, D-473 — plus the
three it separately identifies as genuinely soft owners — D-539, D-538, D-281 — are the ten. The
arithmetic the row could not close (eight named plus three is eleven, not ten) closes on the
correction. *Corrected rather than left because a row whose subject is a mis-carried authored field
must not itself mis-carry one; the former enumeration is preserved above, unedited (#12).*

**And the finding is larger than this row could see from ten rows.** Asked of the WHOLE open
re-home class rather than of the rows where the column looked wrong, the blocker partitions three
ways, and the licensing case is the minority. Every count, every identifier and the per-row evidence
are generated at `tools/audit/decisions/item1_rehome_blocker.json`, and none is restated here
(**D-431**). What the artifact establishes in words:

- the majority of the class is blocked by an owner the record itself calls **not determinate**, not
  by any edit surface;
- **half the class names only `ARCHITECTURE.md`** — a file the standing authorization already
  covers — and is still not executable, which is the sharpest available evidence that licensing is
  not the binding constraint: for those entries there is nothing to widen;
- a residual carries **no blocker statement at all** and is published verbatim with **no verdict
  authored**, `D-531`'s near-miss wording named explicitly so the declined judgment is visible.
  Assembling a phrase list until the residual emptied would be the *"stable enough to be cited"*
  failure this row refused to commit, and it is refused again.

**The corrected count this row declined to propose is still not proposed.** What the derivation
produces is a per-row BLOCKER, derived from each row's own recorded reason; it does not re-author
the `owner_is_unambiguous` column, and the column is unchanged. **The row stays open**: its subject
is that one authored field carries another field's answer, and that is still true at HEAD.
