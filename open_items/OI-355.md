# OI-355 — The shell-read guard denies every `sed` and `awk`, wherever they are aimed

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — instrument / measurement layer (the audit's own guard apparatus). Rowed 2026-08-09
(CC, `cc_instruction_return_continuation_2.md`, Task 0).**

## How it was observed

A read of a file in the session scratchpad — a directory **outside the repository** — was denied,
with the reason naming `sed`'s own range script as though it were a repository path:

> `sed` is aimed at a path inside this repository ('40,60p').

**It was reproduced once, deliberately, before being claimed** — a different script, a different
scratchpad file, the same denial. One observation of a guard verdict establishes nothing; two on
different inputs establish the shape.

## The cause, read at the tool

For a POSIX text utility the guard's path candidates are **every non-option token** after the
utility name. Four utilities carry a correction — `grep`, `egrep`, `fgrep`, `rg` — which drops the
FIRST non-option token because it is the search pattern rather than a path.

`sed` and `awk` are in the same utility set and take a **script or program in exactly that
position**, and they carry no such correction. So the script becomes a path candidate. A bare
script such as `'1,2p'` has no drive letter and no leading separator, so it resolves against the
working directory — the repository root — and reads as inside the tree. The command is denied on
that token, before the real file argument is considered at all.

## Two errors, and the second is the one that bites

1. **A non-path token is classified as a path.** On its own this would be harmless where the real
   target is also inside the tree.
2. **The denial is reached without regard to where the command's actual target is.** That is what
   makes it a false denial: the command was aimed outside the repository, where D-253 does not
   reach, and it was refused anyway.

## What this does NOT do

**It never admits a forbidden read.** The whole failure is on the deny side. A `sed` aimed at a
repository path is still denied — correctly, though for the wrong reason, which is worth saying
because a right verdict from a wrong cause is not evidence the cause is sound.

## Why it GATES

By the clause that overrides the documentation test, not by that test. The guard's **published
false-deny rate is an established value** (#19), measured over the establishment corpus. This
shape is not in that corpus, so the rate is a rate over a population that does not contain the
defect. An establishment obligation always gates, whatever its subject — including the tracking and
audit apparatus itself.

There is a second, practical reason the record already states in its own words: a guard that fires
on legitimate work gets switched off, and that is worse than no guard.

## What is NOT in doubt

- **The rule** (**D-253**) and the guard's arming as a hook in this directory.
- **The case fix of 2026-08-08** (`OPEN_ITEMS.md` OI-351) and the guard-family clauses built with
  it. None is questioned here.
- **Every verdict the establishment corpus already carries.** No corpus row is disputed; the point
  is that the corpus does not contain this shape.

## Why nothing was changed

Changing the guard's decision function is a mechanism change **D-436** reserves to the user. The
guard-family ruling of 2026-08-08 also fixed the ORDER for this family — **the corpus row goes in
before the clause**, so that the blindness is measured at the unwidened guard rather than at one
already changed to see it. That order is the ruling's and is not a session's to reorder.

**Meanwhile nothing is worked around.** The reads this denial refused were performed with the file
tools, which is what the rule says to do anyway.

*Provenance: observed and reproduced 2026-08-09 by CC under
`cc_instruction_return_continuation_2.md`, Task 0. Cross-ref [[OI-300]] (the earlier false-deny
shapes and the deny-on-indeterminate ruling), [[OI-345]] (the PowerShell dialect widening),
[[OI-348]] and [[OI-351]] (the guard family and the case fix).*

## Dated note — 2026-08-09 (CC, `cc_instruction_return_continuation_3.md` Task 0): the user's Ruling 19

**The disposition is ruled: this row RIDES the next guard maintenance act, and no dispatch is written
for it.** Ruling 19 of `cowork_rulings_2026_08_09_third_stop.md`.

**What that means concretely, and it is the order this file's own "why nothing was changed" section
already names as the ruling's rather than a session's.** When the next act on this guard family
happens, the `sed`/`awk` over-deny goes into it — **corpus rows FIRST** (the failing shapes, and
their out-of-tree controls beside them, so the blindness is measured at the UNWIDENED guard rather
than at one already changed to see it), **the clause second**, both rates re-measured on the same
extended corpus, and the revert condition governing. That is #6 — one fix designed once over the
family, never per symptom — and #19: the published false-deny rate then reports this shape instead of
being a rate over a population that does not contain it.

**And what it means for the meantime, stated as a decision rather than left as neglect: the deny-side
failure is TOLERATED.** The grounds are the ones this file already establishes — it admits nothing
forbidden, the whole failure is on the deny side, and the reads it refuses are performed with the file
tools, which is what **D-253** prescribes anyway.

**Nothing about the row's own content moves.** The cause stands as read at the tool, the two errors
stand as stated, and the row stays OPEN and GATING.

*Resolution belongs in the INDEX row; dated notes may be appended here.*

## Dated note — 2026-08-11 (CC, `cc_instruction_return_continuation_10.md` Task 4): the act ran, in the order this file already said was the ruling's

**The user's Ruling 50** of `cowork_rulings_2026_08_11_tenth_stop.md` licensed ONE family
maintenance act, and this row rode it under Ruling 19 as that ruling directed. **The order was the
family's own and was not reordered.**

1. **The corpus rows went in FIRST**, before one line of the mechanism moved: the failing shapes
   with their quoting variants, aimed OUTSIDE the tree; and, in the other direction, the same
   utilities aimed INSIDE it, which must stay denied on their real target rather than on their
   script. That is what makes the blindness measured at the UNWIDENED guard rather than at one
   already changed to see it.
2. **The clause second.** `sed`, `awk` and their siblings drop the first non-option token — the
   same correction `grep`, `egrep`, `fgrep` and `rg` already carried, one utility class further
   out. What is dropped is ONE token in the script's position; every remaining token is still
   tested, which is why the clause opens no hole.
3. **Both rates re-measured on the SAME extended corpus**, with the clause off and on, so the
   published delta is the CLAUSE's and not the corpus rows'.

**The revert condition is NOT met: false denials FALL**, and the ones that remain are the two the
deny-on-indeterminate policy accepts on purpose. Detection is unmoved, the clause being deny-side by
construction. **Not one verdict moved among the rows the corpus already held** — the both-ways
discipline discharged by measurement rather than asserted.

**★ A NEW MISS IS REPORTED RATHER THAN TUNED AWAY, and it is worth a reader's attention because the
row that added it is the row that found it.** One forbidden row added by this act is still missed:
a separator character inside a QUOTED OPTION — `awk -F'|' … <repository path>` — whose tokens the
lexer splits at the separator, so the segment carrying the repository path has no utility at its
head. **Diagnosed at the decision function and not reasoned from the source**, and the same command
with a comma delimiter is denied correctly, which is what establishes that **this is not a defect of
the new clause and the clause does not reach it.** It is the 2026-08-04 segmentation class one
quoting shape further out; repairing it is a further mechanism change Ruling 50 does not license.
**The row stays in the forbidden corpus** — removing a row because the guard misses it is a corpus
chosen to make a guard look clean, which measures nothing (#19).

Every value is at `tools/audit/shell_read_guard_establishment.json` and none is restated here
(**D-431**).
