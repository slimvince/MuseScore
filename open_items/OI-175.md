# OI-175 — The segmenter decides a committed root and quality from the tonic

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-175 | ★ **THE SEGMENTER DECIDES A COMMITTED ROOT AND QUALITY FROM THE TONIC — the third live chord-deciding genuine-tonic site, and the FIRST outside Layer 4; on no register row (CC-discovered 2026-07-16 by the OI-170 sweep's SCOPE CHECK).** (The two inside L4 are Gate G-E and `applyTonicPriorToSparseChord`/OI-172.) `harmonicsegmenter.cpp:849-852`, the **Iter-74 Fix-B head-gap tonic prior**, inside `greedyExpandSegmentation` — which `regionanalyzer.cpp` calls on the **live region path**. It builds the mode tonic exactly as every OI-168/OI-170 site does (`ionianTonicPcFromFifths(globalKeyFifths) + keyModeTonicOffset(globalKeyMode)`), then, when a synthesized head region's winner is **not** tonic-rooted and its score margin is thin (`< kHeadGapTonicPreferenceMargin = 0.4`), **overwrites `headRegion.rootPitchClass`, `bassPitchClass` and `quality`** — either with a tonic-rooted alternative from `headCands`, or, failing that, with the tonic pc and the modal triad quality outright. **Classification: (b) genuine tonic-use** — it asks *"is this root **the tonic**?"*, which no collection can answer, so the OI-170 signature-mask primitive **cannot** replace it; it is **not** a δ-bug. **Fire count NOT MEASURED** — the key-collection probe instruments L4 only, and extending it to Layer 2 was outside the OI-170 dispatch's scope; the site's liveness is established structurally (it is on the call path), its magnitude is not. **Why it matters:** (1) it means "make **L4** tonic-independent" would **not** make the *committed chord* tonic-independent — a tonic decides chord identity in the **segmenter** too, so the design question is whether tonic-independence is a property of L4 or of the whole chord-committing path; (2) it consumes the **global** key mode, so it **composes with OI-174** — where the key layer emits an `Altered` mode on diatonic material, this prior would prefer a tonic a semitone away from the real one; (3) **methodological**: it is the fourth consecutive time a site-by-site or scoped audit missed a site, and it was caught **only** because the sweep was re-run tree-wide and the out-of-L4 remainder read file by file (`cc_oi170_measurements.txt` §SWEEP 1b/1c) — promoted to the catalog as **DT-26** (scope-assumed enumeration) in the same commit, per the standing rule. **NOT FIXED, NOT BUILT AROUND** — declared per the standing rule (no inference-problem-driven coding; #13 surface-a-surprise-as-a-STOP). | `cc_oi170_measure_report.md` §2 (the scope check), §3 (classification), §7 item 3b; the generated artifact `cc_oi170_measurements.txt` §SWEEP 1b/1c; `DEFECT_TYPES.md` **DT-26** (this is its founding instance); `harmonicsegmenter.cpp:849-852`; `regionanalyzer.cpp` (the `greedyExpandSegmentation` call) | **SUPERSEDED-IN-FACT ON THE BATCH/CORPUS SURFACE 2026-07-26 (the OI-178 adoption, this commit):** `batch_analyze --joint-inference` produces the committed corpus/reference from the joint decode, which never runs `greedyExpandSegmentation` — so the head-gap tonic prior no longer decides the committed batch/corpus output. It **REMAINS LIVE on the legacy NOTATION path** (staged scope — the notation layer stays legacy) and retires with the OI-180 retirement map's notation increment (map item 1: the L2 segmenter's filter cascade + head-gap prior → the modeled semi-Markov segmentation). *Prior:* OPEN — CC-discovered 2026-07-16; a **design-pass** item (layer assignment: which layer may let a tonic decide a chord?). Magnitude un-measured — an instrumentation step, if Cowork wants it, is Layer-2-scoped and separate |
