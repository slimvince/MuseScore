# OI-178 — Architecture-adoption protocol for the robust stop

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-178 | Architecture-adoption protocol for the robust stop: the class-(b) per-preset non-increase is a ratchet built for incremental change; A's first decode will move runs in both directions, and the stop as written ("any preset increasing ⟹ FAIL") could block a globally better architecture. Write the adoption-event variant (aggregate criterion + the mandatory explained per-run diff + O-12 snapshot + user ratification) BEFORE A's first measured decode — not negotiated on a live diff | `cowork_joint_estimator_architecture.md` §7 item 3; CLAUDE.md block (A) | PROTOCOL RATIFIED 2026-07-19 (`cowork_prefit_gates.md`), written before any live diff (#22): A commits MAP (no abstention; OI-33 flag must read 0), #17b asymmetric prediction recorded pre-measurement, O-12 snapshot, PASS = key columns beat baseline beyond piece-bootstrap CI on every preset + root/RN non-degrading + class-(b) duration NET DECREASE per preset with every added run enumerated/classified and every added class-(b) run individually diagnosed (undiagnosable ⟹ STOP) + one revertible user-ratified adoption commit re-baselining `tools/robust_stop/` — ✅ **EXECUTED 2026-07-26 (this adoption commit; user-ratified option 1 — STAGED SCOPE)**. The joint estimator is the PRODUCTION inference layer on the **batch/corpus surface** (`batch_analyze --joint-inference <dir>` → the L1 fact adapter → §5 decoder at the committed all-326 tables + direct-metric SELECTED weights; `run_bach_preset.py --joint-inference` regenerates the corpus through it), **preset-independent**. `tools/robust_stop/` re-baselined (root 77.03 / RN 64.12 / key-home 56.14 / key-local 78.42 %, class-(b) 1,817,280 ticks per preset; `robust_stop_diff` OVERALL PASS, −33.0/−34.7/−33.1 %; O-12 snapshot `snapshot_2026-07-26_pre_oi178_adoption/`; superseded OI-168 columns preserved in `reproduce_status.superseded_oi168`). **Establishment (#19):** the regenerated corpus decode is field-exact vs `tools/joint_estimator/adoption_decode.json` on all 326 covered pieces (all 3 preset dirs), and the a8 columns reproduce the ratified adoption record exactly. Legacy units all pass unmodified (composing 1146 / notation 53 / pipeline-snapshot 11 — no golden refresh; the notation path is untouched). **The in-app NOTATION layer stays on the legacy analysis (declared migration state, #23); the retirement map (OI-180) is now the live agenda.** Report `cc_adoption_measurement_report.md`; record `tools/joint_estimator/adoption_record.json`; the genuine-new cost side is OI-192 |
