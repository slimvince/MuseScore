# OI-199 — The comprehensive code review

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-199 | ★ USER-DIRECTED (2026-07-26): the comprehensive CODE REVIEW — the certified two-pass blind audit pattern (the L1–L5 precedent: blind enumerative pass 1, blind second reading + error rate + DT signature sweep pass 2, user-granted certification) applied to everything the existing certifications predate: the joint module, the record path + seams, the embedded-tables/codegen machinery, the new instruments. Shares the DT-sweep machinery with OI-198 (the register review); this row is the full per-layer blind treatment. TIMING: after the switch + map + completions, on the surviving end-state stack | user directive 2026-07-26; OI-84/OI-102/OI-116 precedent | OPEN — scheduled post-map, pre-refinement |

---

**PASS 1 (blind enumerative) DELIVERED — 2026-07-28 (CC; `cc_oi199_pass1_report.md`; freeze `0ea3d31204`).**
The four-area machine inventory via the ONE extended instrument (`gen_inventory.py --layer oi199`,
proven inert for the prior layers): **6,375 deep rows across 67 files** — (a) joint 1,069 / (b)
record-seams 818 (record-arm subset ~600; the per-row arm split is the area-(b) deep pass) / (c) codegen
~51 (+ the 180 KB generated-data population, established by its drift guard, not row-dispositioned) /
(d) instruments ~4,479. The **feasibility STOP fired** (expected). Partition proposed, honouring the
ordering rules: **1 = the joint module (DONE this session), 2 = the new instruments (Cowork's amendment —
every steering figure came from them, #19; needs a 2–3-way sub-split), 3 = the record path/seams.**
**Area (a) fully dispositioned: 1,069 rows** at the L3/L4/L5 vocabulary — no correctness defect; a
clean `probe_decoder`-parity port, production-surviving on both surfaces. Positive finding: the source
is **constant-clean** (336/338 literals ESTABLISHED, 2 UNFIT hyperparameters; fitted values externalized
to the generated artifacts). Findings: OI-220 (stale dormancy comments), OI-221 (two unconsumed
published record facts), and the cost/robustness siblings OI-215/216/217. **The blinding was
COMPROMISED at the source — OI-222 (DT-20): `STATUS.md` (a mandatory read) + the inline §S both leaked
S1/S2/S3 before Task 1.** Certification is NOT granted by pass 1 (it needs pass 2 — a fresh-session
blind second reading, a seeded error rate at full vocabulary, the whole-scope DT-signature sweep — plus
the partition-2/3 deep passes). Cross-refs: [[OI-84]]/[[OI-102]]/[[OI-116]] (the audit-plan precedent),
[[OI-198]] (the register-driven review sharing the DT-sweep machinery), [[OI-200]] (the architecture
step-back that consumes this).
