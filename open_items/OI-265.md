# OI-265 — The Layer-4 as-built body still says production chord analysis runs the legacy path

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10), DT-12. Found by the [[OI-207]] residual second pass, 2026-08-02.
The same class as [[OI-232]], which is RESOLVED for the eight statements it enumerated; this is a
ninth, inside a block that pass corrected but did not finish.**

## What is false

`ARCHITECTURE.md:1336-1337`, in the Layer-4 as-built body, verbatim:

> Production chord
> analysis still runs the **legacy** `analyzeChord` + post-scoring gates (§4.1)

At HEAD the joint estimator is the production inference layer on both surfaces — the batch/corpus
surface since the OI-178 adoption (2026-07-26) and the in-app notation surface since the notation
switch (2026-07-27), where `useJointNotationRecord` defaults to `true`
(`composingconfiguration.cpp:178`). The legacy `analyzeChord` path is compiled and dormant.

## Why the truth-sync did not catch it

The phase-1 truth-sync (commit `ab336f43b5`) added a dated correction above this paragraph, at
`ARCHITECTURE.md:1325-1332`. That correction is about a different clause — the "engages with L5"
production-switch plan — and it says so: it corrects the heading's plan clause and records the
build state as unchanged and correct. It carries no statement about the paragraph beneath it.

The **Layer-3** correction of the same commit does carry one. At `ARCHITECTURE.md:1268-1269`:

> **The description below remains accurate for the dormant pipeline** and is retained as
> the record of what that pipeline does; it is no longer a description of what runs.

That sentence is what makes the Layer-3 body safe to read: every "production" in it is scoped to
the pipeline the section describes. The Layer-4 correction has no such sentence, so its body's one
"production" sentence still reads as a statement about what runs today.

## Disposition

One sentence, at the next `ARCHITECTURE.md` touch, with [[OI-107]] / [[OI-112]] / [[OI-233]] /
[[OI-236]] / [[OI-238]] and the other corrections to the same document: give the Layer-4 correction
the same scoping sentence the Layer-3 correction carries, or re-word the paragraph so its
"production" is the legacy pipeline's own frame. **No code change, no decision change.** Nothing is
edited here — the residual second pass was read-only on the specifications by dispatch, and
rewriting a layer specification is a separate act ([[OI-208]] ruling 2).

*Provenance: CC-found 2026-08-02 at `cc_instruction_oi207_residual_pass.md`, reading the
ARCHITECTURE.md residual against HEAD. Cross-ref [[OI-232]] (the eight enumerated statements, now
resolved), [[OI-180]] (the retirement map that will delete the path this paragraph describes).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

---

**Dated note — 2026-08-02 (CC, the phase-1d enumeration wave, `cc_instruction_phase1d_enumeration_wave.md`
Task 0): the correction is made, and it does both halves rather than only the one this row asked for.**

The Layer-4 dated correction block now carries a second paragraph. It gives the section the scoping
sentence the Layer-3 correction carries — *the description below remains accurate for the dormant
decoder … it is not a description of what runs* — and then, because the Layer-4 case differs from
Layer 3's in a way this row's analysis identified, it goes one step further: the offending sentence is
about a DIFFERENT path (the legacy `analyzeChord` pipeline, not the decoder the section describes), so
a bare scoping sentence would not have reached it. The correction therefore quotes that sentence,
states that it was true when written and is false at HEAD, and records what is true instead — the
joint estimator produces the committed chord reading on the batch/corpus surface since the OI-178
adoption (2026-07-26) and on the in-app notation surface since the notation switch (2026-07-27), where
`useJointNotationRecord` defaults to `true`. That default was **verified at the code this session**,
not carried from memory: `composingconfiguration.cpp:178`, whose own comment names the 2026-07-27
ratification and the OI-180 retirement.

No code change, no decision change, and nothing removed — the paragraph the row names is left standing
and re-framed, which is what [[OI-232]]'s other eight corrections did.
