# OI-319 — Every read wave now widens `gen_home_classification.py --check`'s stale report, because a new gap-homed entry cannot be given its `home_section` while OI-305 is open

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — instrument / measurement layer. Found and CAUSED 2026-08-04 (CC, READ WAVE 1,
`cc_instruction_reads_1.md`), recorded here because the change is this wave's own.**

## What moved, and why it is this wave's doing

`gen_home_classification.py --check` reported **4** stale classification fields at the committed tree
(D-321…D-324, the [[OI-305]] finding). After this wave entered its register entries it reports **29**:
the same four, plus **25 new gap-homed entries that carry no `home_section` at all**.

Those 25 are not drift. The four are a *recorded* value that no longer re-derives; the 25 are a field
that has never been written, because the only writer is `gen_home_classification.py` in apply mode.

## Why the apply was deliberately not run

[[OI-305]] states the reason in its own words, and it is a warning against exactly this act:

> *"If the artifact is historical, what is owed is the epoch treatment the classifier already uses
> for its class fields, not a regeneration that would overwrite the record of what phase 1q found —
> the [[OI-301]] hazard."*

Running the apply would have written `phase1q_reclassification.json` from today's inputs, discharging
[[OI-305]]'s first half as a side effect and destroying the evidence its second half asks a diagnosis
about. A read wave has no licence to resolve a rowed finding by regenerating over it, and the row
asks for a ruling rather than a fix.

## What the delegation grades this wave DID author, and why that is different

Five documents became somebody's home for the first time, and
`gen_phase1p_delegation_bar.FORMS` **stops** on a home document with no authored grade — by design,
so the grade cannot be skipped. The five grades were made by reading the located line in place
(`docs/llm_integration.md`, `cowork_layer6_grouping_design.md` and `cowork_target_architecture.md` all
graded **bare-appended-citation**; `cowork_factorization_desk_simulation.md` and
`docs/layer_architecture_audit.md` are **named in none of the three ratified surfaces**), all five
therefore EXCLUDE, and no section-kind judgment is reached. Supplying an input a tool refuses to run
without is not the same act as regenerating an artifact another row is protecting.

## What is owed

The 25 fields are populated by whatever ruling [[OI-305]] receives — one apply run, once the phase-1q
artifact's status (stale or historical) is settled. Until then **each read wave adds to the count**,
and a later reader must not read a growing stale number as a growing problem: the number is the wave
count times the entries it homed, and it clears in one run.

*Provenance: CC, READ WAVE 1, 2026-08-04, dispatch `cc_instruction_reads_1.md`, Tasks 1 and 4.
**GATES by default, on the [[OI-306]] precedent** — the row reads like apparatus (a section-pointer
field, which D-438's own line names as apparatus), but the non-gating set is DERIVED from an
over-inclusive first cut over each row's subject column and that cut did not propose this row; a
hand-added verdict for a row the cut never reached is exactly the hand-listing `CLAUDE.md` forbids.
If it belongs in the apparatus class, the cut is what should reach it. Independently, the
establishment half riding on it is [[OI-305]]'s and gates there. Cross-ref [[OI-305]], [[OI-301]],
[[OI-291]], [[OI-207]].*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

---

## 2026-08-04 (CC, phase 1z) — the count is larger than this row states, and phase 1z added none of it

Running the check at the tree phase 1z commits, the stale population is **enumerated by identity** in
`tools/audit/guard_state.json` → `runs` (nothing restated here, D-431). Read against this row's own
narrative, two things follow and both matter to whoever takes [[OI-305]]'s ruling:

1. **The population is wider than the "4 + 25" this row describes.** Besides D-321…D-324 and the read
   wave's own gap-homed entries, it includes entries from earlier waves (the phase-1j and phase-1m/1n
   ranges) that carry no `home_section` either. So the growth is not one wave's arithmetic — it has
   been accruing across waves, and the row's *"the number is the wave count times the entries it
   homed"* understates it. **The mechanism this row identifies is right**; its extent was measured
   only at the wave that noticed it.
2. **Phase 1z added nothing to it.** That wave changed one entry's home (**D-058**, re-aimed within the
   same document at its supersession) and entered one new entry (**D-468**); neither appears in the
   stale list, and the register home anchors it re-aimed keep each entry pointing at the same text,
   so no derived section moved. Checked by identity against the enumeration, not inferred.

Nothing is fixed here and the apply is still not run — the [[OI-305]] hazard this row was opened to
respect is unchanged.
---

**Dated note — 2026-08-04 (CC, READ WAVE 2, `cc_instruction_reads_2.md`): the second wave, and the
count rises again — by this wave's doing, exactly as this row predicts.**

READ WAVE 2 entered 33 register entries, 30 of them gap-homed, and the apply mode of
`gen_home_classification.py` was again **NOT run**, on this row's own reasoning and [[OI-305]]'s. So
`--check`'s stale report grows by those 30. It is the same fact this row already states — a field
whose only writer is the apply mode is absent, not drifted — and it is recorded here rather than as a
new row because the row exists for exactly this.

**What the wave DID author, the same act as wave 1's and no more:** seven documents became somebody's
home for the first time, and `gen_phase1p_delegation_bar.FORMS` STOPS on a home document with no
grade. All seven are graded **NOT_NAMED**, which the tool re-verifies mechanically against the three
ratified surfaces, so no reading judgment was made and no section-kind judgment reached. The wave's
eighth home document, `docs/score_inventory.md`, is not in that table at all: its three entries are
process rules, so it does not enter the artifact's population — which is why the one naming that
would have needed a judgment (`CLAUDE.md`'s mandatory-read instruction) was not reached.

---

**Dated note — 2026-08-04 (CC, READ WAVE 3, `cc_instruction_reads_3.md`): the third wave, the count
rises again by this wave's doing, and this wave adds a SECOND kind of pending classification the apply
run will have to settle.**

READ WAVE 3 entered 44 register entries, all of them gap-homed, and the apply mode of
`gen_home_classification.py` was again **NOT run**, on this row's own reasoning and [[OI-305]]'s. So
`--check`'s stale report grows by those 44. Same fact this row already states — a field whose only
writer is the apply mode is absent, not drifted.

**What is new, and why it is recorded here rather than as another row.** Waves 1 and 2 graded every
newly-named home document NOT_NAMED, which `gen_phase1p_delegation_bar.py` re-verifies mechanically —
so no reading judgment was pending when the apply eventually runs. This wave could not: of its eleven
new home documents, **four are named in a ratified surface**, three of them in a form that arguably
ADMITS. Those grades are authored in `FORMS` with the naming each was made on, and the question they
turn on is rowed at [[OI-326]].

The consequence for this row: when the apply finally runs, it will not merely fill 4 + 25 + 30 + 44
empty `home_section` fields — for three documents it will also **apply an admitting grade**, which
moves their entries from `gap` to `contract-home`. **This wave deliberately did not pre-empt that**:
every wave-3 entry is entered `gap`, the class the record already carries, so the apply run stays the
one place the classification changes and no entry is promoted on a session's reading. A later reader
should not take the wave-3 entries' `gap` class as the wave's verdict on the delegation question — it
is the wave declining to answer it.

---

## 2026-08-08 (CC, `cc_instruction_away_execution.md` Task 0) — the apply has run; what this row's mechanism owed is discharged, and the row is NOT flipped here

**The dispatch orders the remaining scope reported, not the status moved**, so this entry reports
and the INDEX row carries the report. What follows is the whole of it.

**The mechanism is discharged.** [[OI-305]] received the user's Ruling 1 of 2026-08-08, the epoch
treatment was applied, and the classifier's applying run ran at HEAD. Every `home_section` field
in the home population is written, `gen_home_classification.py --check` is green, and the
prediction this row made about the apply run holds: the entries whose documents gained an admitting
delegation moved from `gap` to `contract-home` at the apply and nowhere else. No later read wave
can widen the stale report by this cause, because the cause was a field with no writer and the
writer has run.

**What is left attached to this row is a DIFFERENT subject, and it is the one its own 2026-08-04
entry records rather than rows:** `tools/audit/gen_guard_classification.py` carries a re-derivation
mode that no guard list runs, excluded by name from the derived population because it reads the
artifact `gen_guard_state.py` writes. That entry already states the remedy — one line in a future
dispatch, run after the guard state, which is the order its own STOP requires anyway — and states
what bounds it. Nothing here changes either.

**Two accuracy corrections to this row's own narrative, from the same act, recorded because a
later reader would otherwise take the counts literally.** The *"4 + 25"* arithmetic understates the
population, which the phase-1z entry above already says. More consequentially, **the check has been
STOPPING since 2026-08-07** — on an authored judgment for a document that is nobody's home, a STOP
that fires before any entry is classified — so every count quoted in this row describes a run older
than that date, and the growth this row tracks stopped being observable at all when the STOP
started firing. The retirement that clears it was completed at Task 0; the finding is surfaced in
`cowork_away_returns.md`.
