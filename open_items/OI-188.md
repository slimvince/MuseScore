# OI-188 — The decode prune cannot reach the ground truth

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-188 | ★ **The decode prune cannot reach the ground truth on a large minority of ground-truth-defined spans, and the fitted weights make the prune cost WORSE.** CC-measured 2026-07-20 at the fit event. (a) Training-lattice augmentation required to make the GT path representable at all: the GT KEY widened in on **3 135** spans and the GT STATE force-added on **14 257** spans (the decode-time key-fit prune + root-present/member-overlap filters exclude them); a 40-piece pre-probe put GT reachability at ~72 %. (b) The declared prune's cost, re-measured AT THE FITTED WEIGHTS on the fixed 12-piece sample: tight(seg_cap 4, topK 6) → loose(seg_cap 6, topK 12) gains key-local **+7.04 pp**, key-home **+9.31 pp**, root +1.14 pp, RN +3.19 pp — against the step-1 identity-weight measurement of +4.8 / +0.9. So the fitted model is hurt by the prune roughly 1.5× as much as the generative baseline is, and a loosened decode would put fitted key-local near 77 (inside the missed band). The headline is a conservative lower bound, but the bound is no longer small, and the prune interacts with the fitted weights rather than being neutral to them | `tools/joint_estimator/weight_fit.json` (construction block); `fit_grading.json` prune_cost_at_fitted_weights; step-1 `probe_prune_loss.json` | ★ OPEN — bounds every ceiling claim on this decoder and must be read together with OI-187 before any adoption reading (OI-178). The exact-decode reserve (§5) is the declared remedy; its cost was never budgeted at fitted weights. **Re-measured 2026-07-20 at the direct-metric selected weights on the same fixed 12-piece sample: key-local +2.15 pp, key-home +3.22 pp, RN +1.19 pp, root −0.58 pp** — materially LESS prune-sensitive than the likelihood weights (+7.04 / +9.31) and, on key-local, less than identity (+4.8). The direct-search decode is therefore closer to its own unpruned optimum than either prior arm, which weakens (but does not remove) the conservative-lower-bound caveat on its headline. The negative root entry — loosening the prune makes root slightly WORSE — is unexplained and is itself a small anomaly to carry |
