# OI-317 — `docs/key_path_design.md`'s banner says the document is UNCOMMITTED and under a commit hold; it is committed at HEAD and no ratification addendum is named

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10). Found 2026-08-04 (CC, READ WAVE 1, `cc_instruction_reads_1.md`)
reading the document in full.**

## What the banner says

`docs/key_path_design.md:3-6`:

> *"**DRAFT — UNCOMMITTED. Design only; no production code, no behavior change.**
> Base commit `f8c6b3932a` … Ratification-gated: this document is **HELD** (`git add` permitted,
> `git commit` NOT) until a ratification addendum lands."*

and its closing line repeats it: *"the only repo write is this draft (HELD, uncommitted) … Awaiting
ratification."*

## What is true

The file is tracked and committed. Established from the session-start working-tree status, which
enumerates every untracked path in the repository and does not list this one; it is also in the
design-document surface the phase-1 triage and the reading regime derive from the tree.

No ratification addendum is named anywhere in the document, and the document's four open questions
(§10 OQ-1…OQ-4) are recorded with recommendations and no answers.

## Why it is worth a row and not a silent fix

Two reasons, neither of them tidiness. First, the banner is an **instruction to a future session** —
it tells whoever opens the file not to commit it, which is a standing rule of the "Held for Cowork"
kind, and a stale standing rule is worse than none. Second, the same document's own §1/§2.1/§5
disagreement about the piece-start shortcut ([[OI-315]]) shows this file was left in the state its
banner describes and then overtaken; the banner is the marker that would have told a reader so, and
it says the opposite.

## What is NOT in doubt

The document's substantive §3 finding — that a path-with-transition-penalty fixes roughly a tenth of
the target error class and that the bulk is a consistently wrong emission a stickiness penalty
entrenches rather than fixes — is corroborated three independent ways in the record already
(**D-283**, **D-284**, **D-289**, and `docs/beam_widening_design.md` §3.3 reaching it from a per-cell
path walk). Nothing here questions it; that is why the document yielded no register entry.

*Provenance: CC, READ WAVE 1, 2026-08-04, dispatch `cc_instruction_reads_1.md`, Task 1. An
**apparatus row under D-438** — the subject is a document's status banner, which that declaration's
own line names as apparatus. Cross-ref [[OI-315]] (the substantive half of the same document's
staleness, which gates), [[OI-276]], [[OI-207]].*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

## ★ LAPSE RECORD — 2026-08-12 (CC, `cc_instruction_resume_lapse_records.md` Task 3)

**What has happened to this row.** Under the user's **Ruling 66** of 2026-08-11
(`cowork_rulings_2026_08_11_fifteenth_stop.md`; register entry **D-676**, homed at `CLAUDE.md`'s
non-gating declaration) this row **STAYS OPEN, STOPS GATING and STOPS BEING OWED** — it no longer
draws leftover capacity. **This is not a resolution.** No status cell moves, the row does not close,
the open-row count is unchanged, and nothing about the issue recorded above has been done. What
lapses is the obligation, not the row.

**The derivation that graded it — which is what makes this record challengeable rather than final.**

- `tools/audit/nongating_apparatus_rows.json` — the apparatus cut, which classes this row
  NON-GATING on **D-438**'s criterion on the ground *tracking/documentation apparatus, the user's
  own phrase*, with its per-row reason recorded there.
- `tools/audit/decisions/true_half_reach_rows.json` — **D-639**'s test, which put this row OUTSIDE
  the doc-sync half, so it is owed nothing further on phase 1's TRUE half either.

**What the lapse does NOT reach, because this row's own verdict keeps it apart.** The substantive
half of the same document's staleness is a SEPARATE row, which gates and is untouched here. Only
this row's banner half lapses.

**How to re-open it.** Challenge a named derivation rather than rediscovering the issue: if the
apparatus cut's verdict for this row is wrong, or D-639's test reaches it after all, the row returns
inside the gate and back into what is owed. **An establishment obligation (#19) never lapses,
whatever its subject.**

**Where the population lives.** `tools/audit/phase1_finish_line.json` →
`★_the_bearing_cut.the_lapse_population`, derived on every run from the apparatus declaration's
NON-GATING set and never hand-listed; no count is restated here (**D-431**). A row with no named
grading does not lapse — it halts that derivation instead.
