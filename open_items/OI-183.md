# OI-183 — scoring_model.md symbol-level coverage gap

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-183 | `docs/scoring_model.md` symbol-level coverage gap (doc-sync #10, low; the named-constant sibling of OI-106(b)'s inline-bar finding; CC-measured 2026-07-18, mechanically, by `tools/term_inventory/gen_term_inventory.py`): of the 32 L4 override-registered scoring constants (`chordanalyzer.cpp` 29 + `postscoringgates.cpp` 3), **12 have no by-name mention** in the doc: `kSus4FlatThirdFactor`, `kSus4SharpThirdFactor`, `kSus4StructuralFourthThreshold`, `kSeventhThreshold`, `kExtensionThreshold`, `kBassSupportPresenceThreshold`, `kTemplateToneWeightCap`, `kExtraNoteWeightCap`, `kExtensionFactorFlat13`, `kExtensionFactorDefault`, `kComplexityEvidenceFloor`, `kAugThinEvidenceFactor`. Several are covered by combined table cells or prose values (the `kExtensionFactor7th / Flat13 / Default` cell; "P4 above 0.50"; the caps prose; the §3 cf/af descriptions), so the substantive gap is smaller than 12 — but the doc cannot currently be grepped by symbol for these terms, which is exactly how the staleness check works. (`kAnnotateKeyConfidenceThreshold`, also flagged by the mechanical check, is an L3-section constant outside this doc's scope — not a gap.) No doc edit made under the read-only dispatch | `cc_term_inventory_report.md` §1a; `tools/term_inventory/term_inventory_summary.json` (registered_constants_doc_mention_check); OI-106(b) | OPEN — a small doc pass at the next scoring_model.md touch; no code effect |

## ★ DATED NOTE — 2026-08-14 (CC, `cc_instruction_scoring_model_pass.md` Task 2; document pass `b366d44947`)

*Status of record is the INDEX row, which this note does not carry.*

**The act.** The twelve constants get a by-name table in `docs/scoring_model.md` §4, each with the
site it acts at and — where the document already describes its effect without naming it — the cell
or prose that covers it. **That second column is this row's own stated alternative, and it was taken
as an equal outcome rather than a lesser one**, because the row is right that several of the twelve
were never a substantive gap: they were an ungreppable one, which is what defeats the staleness
check the row names.

**The population was re-enumerated at the code and the document, not taken from this row's list**
(the dispatch's A2). **The twelve reproduce exactly at HEAD.** Every name this row lists is
registered, and none of them appears anywhere in the document; no thirteenth was found.

**The row's population STATEMENT does not reproduce, and the discrepancy is recorded rather than
smoothed over.** `chordanalyzer.cpp` registers one fewer name at HEAD than this row gives it, so the
two-file total is one short of the row's figure. This note does not speculate about the difference:
the row's figure was measured 2026-07-18 by `tools/term_inventory/gen_term_inventory.py` and what is
established here is only the count at HEAD, read at the `registerDouble` calls.

**And the row's population EXCLUDES a set that belongs to the same mechanism.**
`harmonicfunctionlayer.cpp` registers `kWSeq`, `kWDim`, `kWStepIn`, `kWStepOut` and `kStepBudget`
through the same registry. **None of them is a gap** — the document names every one, in §4's
`w_seq` / `w_dim` / `w_stepIn` / `w_stepOut` sections — so the outcome is unchanged; but a reader
taking this row's population for the whole registered surface would be reading a short list.

**No value is transcribed into the new table, deliberately.** A hand-typed constant is a future
staleness site of exactly the class [[OI-45]] exists against, and each value has one home already:
the literal beside the constant in its owning translation unit, which the override registry names
and `--param-override` reads. Where the document already carries a value in prose — "without P4
above 0.50" for `kSus4StructuralFourthThreshold`, §3's complexity-factor bullet for
`kComplexityEvidenceFloor` — the table points at that prose and the prose stays the citation.

**No code effect, and no `src/` edit.**
