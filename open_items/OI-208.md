# OI-208 — The decisions register: rulings need the same one-home treatment issues got

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — docs / process (#6 applied to decisions)**

Proposed by Cowork 2026-07-27 at OI-207's rowing; user ratification of the shape owed.

**The root cause it fixes:** the 3.1b contradiction happened structurally, not carelessly-once:
(1) the 2026-07-18 doc split moved decision history into `STATUS_ARCHIVE.md` /
`cowork_handoff_archive.md`, both explicitly OUTSIDE the session-start read ("reference-only");
(2) the project has a register for ISSUES (`OPEN_ITEMS.md` — created because 91 items were
scattered over 12 surfaces) but NO register for DECISIONS — a ratification, a
shelving-with-evidence, or a falsification has no living surface a later dispatch is forced to
consult; it binds only as long as someone remembers it. The constrained-optimum ledger
corollary records alternatives per decision, but inside per-decision documents that also age
into archives.

**The proposal (the OPEN_ITEMS pattern applied to rulings):** one lean DECISIONS index — per
row: the decision (plain words), date, who ratified, STATUS (live / superseded-by /
shelved-with-evidence / falsified), the evidence pointer, the detail link — with per-decision
detail files where the row's pointer does not suffice; populated initially from OI-207's
enumeration; added to the session-start read (CLAUDE.md); and rule (c)'s analogue: a new
ratification/shelving/falsification gets its index row IN the commit that records it. A
dispatch that touches a decision's subject must cite its row — the mechanical habit that would
have surfaced 3.1b before the seams dispatches specified whole-score.

**Bound:** the index stays lean (the register-split lesson — descriptions 1–2 sentences,
narrative in detail files/the cited documents); the living-mode coherence check pattern
(`open_items_split_check.py`) extends to it if warranted.

---

**Dated note — 2026-07-28 (CC, the decision-harvest dispatch): the population INPUT is delivered in
this register's field shape.** The harvest (`tools/audit/decisions/decision_candidates.json`/`.csv`)
emits each candidate in exactly the shape this register would consume, so the later pass populates a
register rather than writing prose: **id** (`DH-…`) · **decision_text** (the author's own words,
verbatim) · **date** · **ratified_by** · **status** (LEFT EMPTY — this register's live /
superseded-by / shelved-with-evidence / falsified) · **conformance** (LEFT EMPTY) · **evidence_pointer**
(commit hashes, document names, `OI-…` refs, artifact paths auto-extracted) · **source_occurrences**
(this occurrence + every linked repetition; repetitions are LINKED, never dropped — repetition is
evidence of standing). The `manifest.json` is stamped to the corpus git hash + the instrument blob sha
(#16). When the user ratifies this register's shape, the initial rows are a mechanical projection of the
`status`/`conformance`-empty candidates the OI-207 adjudication has filled.

**★ SHAPE RATIFIED (user, 2026-07-28) — three rulings.**

1. **The register holds WHAT WAS DECIDED, and its status. Nothing else.** The proposed
   `conformance` field is REMOVED. Rationale (the user's, recorded): a decision's status changes
   only when someone rules again; whether the code obeys it changes every time the code moves.
   Holding both in one row produces a register that silently goes stale — the exact failure the
   issue register was created to end. **Non-conformance is tracked in `OPEN_ITEMS.md` as ordinary
   rows**, each pointing back at the decision it violates. One home per concern, each with its own
   clock (#6).
2. **A decision belongs, wherever possible, in the OWNING LAYER'S SPECIFICATION** — that layer's
   section of `ARCHITECTURE.md` — and the register is the **index and pointer**, never a
   substitute home. This follows the standing rule that the layer specification is the primary
   place decisions about how a layer should work are recorded (`CLAUDE.md` Conventions,
   2026-07-28). **Consequence for the adjudication:** a decision whose proper home is a layer
   specification and which is not recorded there is a **documentation gap → an `OPEN_ITEMS.md`
   row**, distinct from a non-conformance row. The register entry then points at the specification
   section, not at a scattered restatement.
3. **The register is CLEAN — one entry per decision, not one per occurrence.** The harvest's
   15,224 candidates are occurrences; roughly 57 % are Claude Code session reports restating
   rulings made elsewhere. Consolidation to the distinct decisions, each with its authoritative
   home identified and its repetitions linked as evidence of standing, is the adjudication's first
   task and the precondition for the register being readable at all.

---

**Dated note — 2026-07-28 (CC, the decision-clustering dispatch): the clustering LAYER exists, and it
carries a STOP for ruling 3.** `tools/audit/decisions/gen_decision_clusters.py` (a sibling of the
harvest, one tool per concern — #6) emits `decision_clusters.json`/`.csv` + `cluster_manifest.json` as a
layer OVER `decision_candidates.json`, which it never writes. Signal 1 (near-identical text: Jaccard on
word 3-gram shingles, threshold **0.80**, statements under 8 tokens matched by exact text only) FORMS the
clusters; signals 2 (shared evidence pointer) and 3 (same date + ratifier) are emitted as links BETWEEN
clusters and merged nothing, per their declared weaker/weakest strength. **Non-destructive, proven:**
all 15,224 candidates present, each in exactly one cluster, input sha256 unchanged
(`gen_decision_clusters.py --check`). **Established (#19), exactly:** `--check` plus `--establish`
(`cluster_establishment.json`) brute-forces the EXACT similarity of every pair of the 13,894
similarity-eligible units sharing a shingle — **93 exact pairs at/above 0.70, 93 found, 0 missed, 0
spurious; recall and precision 1.0000.**

**★ THE STOP (#13) — ruling 3's premise does not hold at the harvest's unit granularity.** The ratified
shape assumes the 15,224 occurrences consolidate to a readable number of distinct decisions. They do not
consolidate MECHANICALLY: **the clustering yields 14,460 clusters — 14,274 of them singletons.** The
cause is diagnosed, not guessed: **this repository restates decisions by PARAPHRASE, not by copying**, so
whole-unit wording similarity cannot reach them. Corpus-wide there are only **93** near-identical pairs at
all, almost all repeated layout or dispatch instruction text; the threshold is not the cause (the whole
0.70–0.95 sweep spans 79 clusters in 14,460). The worked counter-example is this project's own leading
case: **46 candidate statements about the Stage-3.1b shelving fall into 46 clusters — every one a
singleton** (`worked_counter_example` in the artifacts). **Consequence for the adjudication: the cluster
count is NOT the decision count; it is the count of distinctly-worded statements.** Ruling 3's
consolidation therefore remains judgment work in full — it was not reduced by this pass. What the pass
DOES deliver: a labelled **boilerplate bucket** (74 clusters / 373 occurrences — recurring headings and
table header rows, and the standing push/stop/read-only dispatch instruction lines; KEPT, not deleted, so
the judgment is confirmable a class at a time), **6 clusters flagged internally divergent** (5 outside the
bucket) left unresolved by design, 1,876 evidence-pointer link groups and 52 date+ratifier link groups,
and a proposed representative per cluster chosen by SOURCE AUTHORITY alone. **No adjudication was done:
status, conformance and supersession are untouched.** The user-facing worked preview of ruling 1/2/3's
entry shape is `tools/audit/decisions/cluster_preview.md` — **awaiting the user's read**. Not worked
around: changing the unit (sentence-level splitting) or matching by meaning are method changes with their
own risks and were not in this dispatch's scope.

*Resolution belongs in the INDEX row; dated notes may be appended here.*

---

**★ Dated note — 2026-08-01 (CC, the OI-207 adjudication dispatch): THE REGISTER EXISTS —
`DECISIONS.md`, in the ratified shape.** **115 decisions**, grouped by subject, each carrying the
decision **verbatim**, a one-or-two-sentence **plain restatement** beneath it, its **status**, its
**home** as `file:line`, and its **provenance**. **Ruling 1 honoured:** there is no conformance field
— non-conformance is rowed in `OPEN_ITEMS.md` ([[OI-231]], [[OI-233]], [[OI-235]] and the doc-sync
rows), each pointing back at the decision it violates. **Ruling 2 honoured:** every entry names where
the decision is actually recorded, and the 16 whose home is not a layer specification are marked as
such in the document and rowed as a documentation gap at [[OI-237]]. **Ruling 3 — the honest
outcome:** consolidation to distinct decisions was done by **reading the layer specifications in
full**, not by consolidating the harvest, because the clustering STOP recorded in the note above
holds exactly as reported. The register's 115 entries are not a consolidation of 14,460 clusters;
they are the decisions the specifications state, with the clusters serving as the searchable index
and the backstop.

**The register is a GENERATED file** (`tools/audit/decisions/gen_decisions_register.py` reading
`backbone_decisions.json` + `cluster_dispositions.json`), so every number in it is computed and it
cannot drift from its data (#17f/#10); `--check` reports whether the committed file still matches.
**Establishment (#19):** all 115 verbatim quotes are mechanically confirmed to exist at their cited
`file:line` (`gen_cluster_dispositions.py --verify`, 115/115).

**The standing rule this register needs, and does not yet have:** rule (c)'s analogue — a new
ratification, shelving or falsification gets its entry in the same commit that records it — is
stated in `DECISIONS.md`'s own provenance section but is **not yet in `CLAUDE.md`'s session-start
read**, and the register is not yet listed there. Adding it is a `CLAUDE.md` edit, which is a
governing-document change and therefore the user's to ratify; it is not made here. Until it is made,
the register is a snapshot rather than a living surface — the same weakness this row was opened to
end.

---

**★ Dated note — 2026-08-01 (CC, the register correction and completion pass): the register is
corrected and complete over its declared scope — 228 decisions.** It grew from 115 by reading what
the adjudication had not: `ARCHITECTURE.md` §1 and §6–§19 plus both appendices (the adjudication's
own citations show it covered the preamble, §2, §3, §4 and §5 and nothing else), `CLAUDE.md` in
full, the rulings that live only in an open-item row or a session handoff block, and the standing
decision-bearing surfaces. Commits `80ad92f9d3` → `243c7f679e` → `3478e2ff61` → this one. Full
method and figures: the [[OI-207]] dated note of this date.

**Ruling 1 still honoured.** No conformance field. The three findings this pass produced are
ordinary `OPEN_ITEMS.md` rows — [[OI-240]], [[OI-241]], [[OI-242]] — each pointing back at the
decision it bears on.

**Ruling 2 sharpened, not weakened.** `home_is_layer_spec = false` was rendering ONE warning for
four different situations, including for `CLAUDE.md` conventions [[OI-237]] itself says are
correctly homed. Each entry now carries which case it is, and the register counts them separately:
documentation gap 8 · recorded only on a tracking surface 12 · project-wide convention correctly
homed 31 · decision about the process correctly homed 16. The **twelve** are the new finding —
decisions whose only record is an open-item row or a handoff block, which are places for tracking
work, not homes for standing decisions ([[OI-240]]).

**Ruling 3 unchanged.** Consolidation was again done by reading, not by consolidating the harvest.
**The cluster dispositions were NOT redone** — the dispatch directed it, and the Task-3 coverage
guarantee (14,460/14,460 clusters, 15,224/15,224 occurrences) stands exactly as recorded above.
**The refinement this creates is named, not silently taken:** the 113 new backbone entries would
make some of the 6,374 `unresolved` clusters classifiable under bulk rules BR-3/BR-4, because those
rules match a cluster against the backbone. Re-running `gen_cluster_dispositions.py` against the
enlarged backbone is the mechanical way to take it, and doing so re-baselines every number the
disposition manifest records — so it is deferred and recorded here rather than performed inside a
pass whose scope excluded it. `disposition_manifest.json` is a generated artifact and was not
hand-edited (#17f).

**The new entry field.** Every entry carries a **Why.** — the research citation, the measurement, or
the constraint the record gives, cited to file:line, or the words **derivation not recorded**
(183 populated, 45 not). This serves the `CLAUDE.md` Conventions entry of 2026-08-01, which is
itself registered as D-195.

**Establishment (#19).** Every verbatim quote is EXTRACTED from its source by line range rather than
retyped. `gen_cluster_dispositions.py --verify`: 228/228 quotes found at their cited home, 222/222
line anchors correct, **all `D-…` and `OI-…` cross-references resolving** — a new guard, added
because the correction pass found 17 references pointing at the wrong row after being forward-guessed
before the rows existed. It caught two live instances during this very pass.

**The LIVING-SURFACE half is STILL OWED and is still the user's.** The register is not in
`CLAUDE.md`'s session-start read, and rule (c)'s analogue — a new ratification, shelving or
falsification gets its entry in the same commit that records it — is stated in `DECISIONS.md`'s own
provenance section but is not a standing rule there. Both are governing-document edits. Until they
are made the register remains a snapshot rather than the living surface this row was opened to
create, and every pass over it is a re-derivation rather than a read.

---

**Dated note — 2026-08-02 (Cowork): the register's CONTENT IS RATIFIED, and the register is
re-emitted in the index-plus-detail shape.** The user ratified all 228 entries: the 115
originally-enumerated (with the user's own review corrections applied — the D-033/D-050/D-100
restatement rewordings, the D-103 → D-207 supersession, the derivation-not-recorded flags) and
the 113 completion-pass entries, read via the generated review aid
`ratification_surfaces/cowork_decisions_pending_ratification.md` (complete entries, rendered by the register
generator's own entry renderer). Same day, user-directed at the ratification review: the one-file
register had outgrown rendering, so the generator now emits the open-items register's ratified
index-plus-detail shape — `DECISIONS.md` the lean index (one table row per decision), full
entries in one generated file per group under `decisions/`, `--check` verifying all 21 emitted
files. The register-level ratification does not overwrite per-entry provenance ("ratifier not
stated" still describes the original record of that decision); the ratification line lives in the
index preamble. **Remaining open on this row: the living-surface half only** (the `CLAUDE.md`
session-start read + rule (c)'s analogue as a standing rule — the user's governing-document
edits).

---

**Dated note — 2026-08-02 (CC, the [[OI-207]] residual second pass): the register is 254 entries,
and its same-commit rule was honored again.** Twenty-three decisions the register did not carry were
found in the BR-8 residual and entered as data in the commit that records the finding (`249fc81b6c`
— rule (c)): seventeen homed in `ARCHITECTURE.md` (D-232…D-248) and six recorded only in
`cowork_handoff.md` blocks (D-249…D-254, rowed [[OI-266]]). **They are entered, not ratified** —
this pass may not ratify, so they carry the record's own status and go to the user in the pass
report's ratification queue.

**Ruling 2 sharpened by a measurement, not weakened.** The six handoff rules are entered
`nonspec_kind: "unhomed"`, which moves the register's *recorded only on a tracking surface* count
from **0 back to 6**. That zero came from the phase-1 homing acts and was true of the 231 entries
the register then held; it was not true of the repository. The count is only as strong as the
enumeration behind it, and this pass's own residual partition names 1,533 clusters in surfaces
never enumerated for the backbone at all.

**Ruling 3 unchanged.** Consolidation was again done by reading, not by consolidating the harvest.

**Establishment (#19), at 254 entries.** Every verbatim quote extracted from its home by line range,
never retyped. `gen_cluster_dispositions.py --verify`: 254/254 quotes found at their cited home,
249/249 line anchors correct, all `D-…`/`OI-…` cross-references resolving.
`gen_decisions_register.py --check`: the register matches its data across all 21 files. A **new
field-shape guard** rides this pass, from the standing self-check on its own first write: an empty
`ratified_by` rendered as a dangling "ratified by " with nothing after it instead of failing, so the
register could carry a silently-empty field; `--verify` now rejects an empty `ratified_by` or `date`.

---

**Dated note — 2026-08-02 (Cowork): THE LIVING SURFACE IS LANDED, and the row closes.** The user
adopted the living-surface edit (presented with alternatives rated on the principles and the
objective): `DECISIONS.md` is now in `CLAUDE.md`'s mandatory session-start reads, and the
decisions-register section there makes the rules standing — the index read at session start, a
dispatch citing the entry it touches, **a new ratification/shelving/falsification landing in the
register in the commit that records it** (registered as D-230), the register generated-only, and
the owning-specification-first rule restated. The same-commit rule was honored on its first day:
this very commit carries the OI-234 ruling (D-025/D-026 moved), the OI-241 ruling (D-229), and
D-230 itself. Shape (2026-07-28) + content (2026-08-02) + living surface (2026-08-02) — the row's
purpose is achieved.

---

**Dated note — 2026-08-02 (CC, the phase-1d enumeration wave): the register is 281 entries, and
the same-commit rule was honored twice in one session.** The six working-protocol rules were homed
and their register data moved in the commit that records the homing (`19fbe9e271`, rule (c)); the
27 decisions found by the wave were entered as data in the commit that records the finding
(`042f8dde79`). Full method and figures: the [[OI-207]] dated note of this date.

**Ruling 2 is now under a question this row's own vocabulary cannot answer, and that is the
finding.** The homing acts took *recorded only on a tracking surface* from **6 back to 0** — the
count [[OI-266]] had pushed off zero. But the wave's 27 new entries put **22 into
`nonspec_kind: "gap"`**, and inspection shows the four-case vocabulary does not fit them: they are
recorded in documents carrying explicit user ratifications, and in at least two cases
`ARCHITECTURE.md` **delegates to that document by name**. Register entry **D-092** — a
cross-cutting contract is stated once and never redefined in a layer document — makes a single
non-layer home for such a contract *conformant*. So either ruling 2's "wherever possible" reaches
them and twenty-two homing acts are owed, or the register needs a **fifth** non-specification case
("a cross-cutting contract, correctly homed outside any layer") beside the four it has. Rowed as
[[OI-268]]; the pass did not decide it, because deciding it is a ruling.

**Ruling 1 still honoured.** No conformance field: the three findings of this wave are ordinary
`OPEN_ITEMS.md` rows ([[OI-268]], [[OI-269]], [[OI-270]]), each pointing back at what it bears on.

**Ruling 3 unchanged.** Consolidation was again done by reading — 21 design documents in full —
not by consolidating the harvest.

**Establishment (#19), at 281 entries.** Every verbatim extracted from its home by line range,
never retyped. `gen_cluster_dispositions.py --verify`: 281/281 quotes found at their cited home,
276/276 line anchors correct, all `D-…`/`OI-…` cross-references resolving.
`gen_decisions_register.py --check`: the register matches its data across all 21 files.

**A note on the register's own conventions, from the wave's reading.** Register entry **D-256** (a
design document opens with one of four status banners) is **not universally applied** — 101 of the
143 design documents in the reading list carry no banner at all. The wave used the banner as a
mechanical filter for where binding decisions live, and it worked well on the 42 documents that
have one; the finding is that the convention's own coverage is a fact nobody had measured. Not
rowed separately: it is the [[OI-268]] and [[OI-269]] surface seen from another angle, and both
rows record it.
