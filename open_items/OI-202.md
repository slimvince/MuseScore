# OI-202 — Register split-check false-fails on every post-split addition

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-202 | Cowork-found (2026-07-27) at the seams-2 P4 verification (provenance HEAD `599eebd45e`; quoted in the dispatch `cc_instruction_notation_noteseam.md` Task 1): `tools/open_items_split_check.py` compared the tree ONLY against the frozen split baseline (`cb246a7580`, the 200-item pre-split file), so every LEGITIMATELY-added post-split item (OI-201, and every future one) false-failed the reconciliation — reported as `added_in_index` / `extra_detail_file` with `counts_equal=false` and OVERALL FAIL — even when the addition was correct and complete. A second, coupled hazard: the failing reconciliation was the DEFAULT run and it WROTE `split_reconciliation.json`, so a bare invocation on a grown tree also clobbered the frozen split-event artifact (the committed 200/200/200 PASS record). The instrument had no standing register-HEALTH mode valid on a tree that grows after the split (the split reconciliation is a one-time event record, only re-runnable against the tree AS IT WAS at the split). | source: Cowork P4 verification (`599eebd45e`); the P4 report's finding; the OI-153/register-lint tooling family (register mechanical guards) | ✅ RESOLVED 2026-07-27 (this dispatch, Task 1 commit) — the instrument gains a default LIVING mode (`register_check.json`) that checks the CURRENT tree, valid no matter how many items are added post-split: (a) INDEX rows ↔ detail files are a bijection (no dupes, no orphans either way); (b) no detail file carries a status of its own (the header sentinel must be present, and no `\| OPEN`/`\| ✅ RESOLVED`-style status cell may appear outside the single verbatim historical row block — the two-place-status drift killer, a prose resolution note is exempt); (c) for the ORIGINAL split items (present at `cb246a7580`) the detail row stays BYTE-VERBATIM against the baseline (the historical guarantee UNWEAKENED), while post-baseline items are checked structurally (a)+(b) only, never against a non-existent baseline row. The historical split reconciliation stays INVOCABLE under `--split-baseline` (writes `split_reconciliation.json`, unchanged behavior), and the committed `split_reconciliation.json` is left UNTOUCHED (it documents the split event). Living mode OVERALL PASS on the current tree: index=201 / detail=201 / baseline=200 / post-baseline={OI-201} (+ OI-202 in this commit). Making living the default also closes the clobber hazard — the default no longer writes the frozen artifact |
