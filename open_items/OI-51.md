# OI-51 — Foreign uncommitted SHELVED-banner edit

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — Doc-sync debt (#10) — annotate, don't rewrite history**

| OI-51 | Foreign uncommitted `cowork_joint_key_chord_design.md` SHELVED-banner edit sitting in the working tree since session 35 | cc_eg2 §0 | OPEN — fold or discard |

---

**★ Dated RESOLUTION note — 2026-08-03 (CC, phase 1p, Task 3). The rider that sent this session here
carried a premise, and the premise is REFUTED: the banner edit is IN the record, not in the working
tree. Nothing was committed, because there was nothing to commit.**

## What the rider said, and where it came from

The phase-1p dispatch §4 carries a rider: *"`open_items/OI-281.md` records that OI-51 tracks an
uncommitted SHELVED-banner edit sitting in the working tree for `cowork_joint_key_chord_design.md`.
That is the OI-285 class — a status change on disk and not in the record. Commit it, unedited, and
note it on OI-51."*

`open_items/OI-281.md`'s first measurement note does say that, and this row's own text has said it
since session 35. **Neither is a primary source for what is in the working tree today** — the first
repeats this row, and this row records a state observed at session 35. Under **D-431**'s premise
clause a claim of fact about the record is cited to the primary source it can be checked at, and here
that is the file and the commit.

## What the check found

Checked this session, both halves:

- **The working tree**, read with the file tools: `cowork_joint_key_chord_design.md:3-12` carries the
  banner in full — *"★★ SHELVED — MEASURED NOT TO PAY (user-ratified 2026-07-07, arc #12). NOT
  BUILT."* with the probe figures and the ponder-point pointer.
- **The record**, read as a git object by explicit hash: `git show
  ccc3086ab3:cowork_joint_key_chord_design.md` returns the same banner, **byte-identical over the
  block**, including the trailing *"Status: DESIGN (CC, 2026-07-07)"* paragraph beneath it.

Corroborating and independent: the session-start working-tree snapshot for this session lists
untracked files only and **no modified tracked file at all**, so no part of this document differs
from the record.

**Stated as a limit, because the check should not be read wider than it is:** the byte comparison
covers the banner block this row is about, not the whole document; the whole-document claim rests on
the snapshot, which is the harness's own report rather than a comparison this session made.

## Disposition

**The exposure this row names does not exist.** The SHELVED banner is committed and durable; a
`git clean` or a working-tree reset would not lose it. The row's "fold or discard" question was
answered by the fold, at some point between session 35 and now, and never came back to close the row.

**Nothing was committed by this session on this row's account** — committing an unmodified file is
not an act, and inventing one to satisfy a rider would have been the worse outcome.

**What this leaves standing, and it is elsewhere:** the arc plan's delegation to this document is a
parenthetical naming inside a bulleted list, which the delegation bar (**D-432**) does not admit, so
the four register entries homed here stay `gap` — on clause (b), not on shelving. That is
[[OI-291]]'s business, not this row's.

*Provenance: rowed at session 35 (`cc_eg2` §0). Premise refuted and row resolved 2026-08-03 (CC,
phase 1p, Task 3), by the check the dispatch's own figures rule requires before a carried-across
premise is acted on. Cross-ref [[OI-281]] (which repeats the stale premise inside a dated note, left
unedited as the record of what was believed then), [[OI-285]] (the uncommitted-surfaces class this
was assumed to belong to), [[OI-291]].*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
