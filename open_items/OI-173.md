# OI-173 — Four inequivalent definitions of diatonicToKey

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-173 | **FOUR inequivalent definitions of `diatonicToKey`, TWO of `degree`, and THREE copies of the diatonic-parent table (#6/#12; CC-discovered 2026-07-14 by the OI-170 mechanical sweep).** The OI-170 row calls the consumer sites "five re-derivations of one published fact". **They are not: they compute three different predicates, and mostly for a different key.** (1) **`diatonicToKey`** — **D1** the producer (`chordanalyzer.cpp` `buildChordResult`): degree via the mode's **diatonic PARENT** scale AND every pc with `pcWeight > 0.2` in that same parent-scale set; **D2** (`sectionanalyzer.cpp:140-157/275-293`, `notationcomposingbridge.cpp:97-115`): degree via the **mode's OWN** scale AND every **tone** in the mode's own scale; **D3** (`functionromannumeral.cpp:36`, `functionrelationallabel.cpp:49`, `sectioncadencedetection.cpp:241`, `notationcomposingbridge.cpp:1144`): **`degree >= 0` only — no membership loop at all**; plus the CORRECT question (signature-collection membership) which no site asks today. (2) **`degree`** has two definitions: the producer's parent-scale numbering vs `diatonicDegreeForRootPc`'s mode's-own-scale numbering — they differ for **all 14 non-diatonic modes** (e.g. under HarmonicMinor a root on the raised 7th is `-1` to the first and `6` to the second), and the parent-scale numbering is **structurally wrong** for `Altered`/`AlteredDomBB7` (their tonic is not a member of any parent collection). (3) The 21→7 parent table exists **three times**: `DIATONIC_PARENT_INDEX` (`chordanalyzer.cpp:1397`), `csfTonicizationParent` (`chordsymbolformatter.cpp:799`), `CHR_DIATONIC_PARENT` (`chordsymbolformatter.cpp:840`). **Consequence for the OI-170 fix design:** "single-source `diatonicToKey` — the consumers read the published fact" is **not implementable as stated**, because most consumers re-derive for a **DIFFERENT key** (a pivot key, an applied key, a re-stabilized section key) where the published value is not the answer. What IS available is **one shared, key-parameterized predicate**. **No corpus effect today** — none of the re-derivations runs on the default `batch_analyze` path (`analyzeSection` is behind `--section-level`; the bridges are UI-side), so `.ours.json` always carries D1. **Also: the published fact has ONE reader in the whole tree** (`notationimplodebridge.cpp:1205`, the borrowed-chord UI marker), and **`BaseRomanNumeral::diatonicToKey` (`functionromannumeral.h:84`) is written and read by NOBODY** — declared dormancy or waste under the fact-publication corollary. | `cc_oi170_measure_report.md` §2/§4 + the artifact's SWEEP 3/4/5; `chordanalyzer.cpp`; `sparsechordrefinement.cpp:109-120`; `sectionanalyzer.cpp`; `functionromannumeral.{h,cpp}`; `functionrelationallabel.cpp:49`; `sectioncadencedetection.cpp:241`; `notationcomposingbridge.cpp:97/1144`; `chordsymbolformatter.cpp:799/840`; OI-170 | OPEN — CC-discovered 2026-07-14. The **collection** question unifies onto `pcInMask(diatonicMaskFromFifths(...))` (OI-170's fix); the **degree** question unifies onto `diatonicDegreeForRootPc` (the mode's own scale) — a SEPARATE change that moves `degree`, hence Roman numerals, hence possibly Gate G-E / OI-172, hence committed chords: **it must not be folded into the collection fix** |
