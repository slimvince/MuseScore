# OI-191 — The direct-metric search is rugged and under-determined

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-191 | ★ **The direct-metric search objective is RUGGED and UNDER-DETERMINED, so "best of 21 restarts" is itself an unbudgeted fitting act** (CC-measured 2026-07-20 at the direct-metric fit event). The 21 declared restarts per fold converge to 21 materially different optima: training-R spread **0.019–0.058** per fold (0.077 on the all-326 fit), with only **1–2 of 21** starts landing within 1e-3 of the best. The named starts are not reliably good — the identity start ranks **5th / 13th / 19th / 5th / 14th** of 21 and won **zero** folds; the likelihood-fit start ranks 20th/1st/10th/3rd/11th and won one. Selection came from a seeded-random start in **4 of 5** folds. The consequence for the headline: **held-out key-local varies by 3.38–5.42 pp across the 21 converged optima of a single fold**, so taking the training-minimum of 21 draws is a selection over ~21 effective degrees of freedom stacked on top of the 13 parameters — a capacity cost the OI-177 budget never accounted for, because that budget was written for a CONVEX fit with one optimum ("total effective free parameters ≤ training tokens / 10" counts weights, not restarts). Corroborating under-determination: the selected weight vectors are NOT consistent across folds — fold2's optimum is qualitatively different (declared_mode 0.370 vs ~3.1–3.9 elsewhere; spelling 2.639 vs ~0.6–1.3; key_trans 2.649 vs ~0.8–1.2) yet grades similarly, i.e. quite different weight vectors buy the same performance | `tools/joint_estimator/weight_search.json` (per-start R_train + traces); `search_grading.json` stability block (per-fold spread, id.rank, per-start held-out columns); dispatch `cc_instruction_direct_metric_weight_fit.md` Task 1 ("a wide spread is a prominently-reported finding") | ★ OPEN — a METHODOLOGY item for Cowork/the user, not a value tweak. It does not invalidate the held-out figures (each fold's held-out set was touched once, by an optimum selected on training alone), but it bounds how much of the +2.9/+3.3/+1.5/+1.7 pp gain should be attributed to the weights rather than to restart selection. Options: (a) amend the OI-177 capacity budget to count restart selection explicitly and re-declare the constant; (b) select by inner-validation within the training folds instead of by training minimum (the OI-176 machinery already supports it); (c) report the MEDIAN converged optimum beside the minimum as the honest central estimate. Not actioned — no value was changed in response |
