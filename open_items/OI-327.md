# OI-327 — Five drafted delegations await the user: the documents the doc-governance clause reaches only by glob or by prose reference

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Rowed 2026-08-04 (CC, READ WAVE 4, dispatch `cc_instruction_reads_4.md`), on the user's ruling R2
at [[OI-326]].**

## Why this is a row of its own rather than a re-opening of OI-293

The user's ruling R2 says the residue of R1 *"is decided through the OI-293 write list: a delegation
the user writes settles that document without touching the bar."* The write LIST is the mechanism,
and the four documents were added to it where it lives —
`tools/audit/decisions/backbone_decisions.json` → `section_home_criterion.write_list`, the same block
the six 2026-08-03 rows sit in.

[[OI-293]] itself is **RESOLVED**: its own population was the six documents the phase-1q pass
reported losing entries, and the user wrote all six delegations on 2026-08-03. A resolved row that
silently acquires new open work is a status contradiction, and the open-items register exists
because eleven of those were once found scattered across twelve surfaces. So the mechanism stays at
OI-293 and the outstanding act is tracked here.

## The four, and what each is waiting for

Each has a drafted delegation in the write list, in the wording `ARCHITECTURE.md` already uses for
its delegated layers. Nothing is written into `ARCHITECTURE.md` or `CLAUDE.md` by this session —
rule (g)'s guard is that only the user writes a delegation.

- **`cowork_layer1_note_model_design.md`** — the glob `cowork_layer*_design.md` matches it and,
  under **D-546**, confers nothing. It is named in **no** user-ratified surface, so clause (a) fails
  before the bar is reached. The measured asymmetry the draft would close: the Layer-3, Layer-4 and
  Layer-5 sections each carry an explicit delegation pointer and the Layer-1 section carries none.
- **`cowork_layer2_slicing_design.md`** — same glob, same ruling. Its only naming is a bare appended
  citation inside a *"See …"* list of three documents, two of which are session reports; that form
  is what **D-432** excludes.
- **`cowork_layer6_grouping_design.md`** — same glob, same ruling; its naming is the *"Full spec:"*
  two-line shape that is the bar's own worked example of the excluded form. ★ **This one carries a
  question the draft does not answer**, and it is stated rather than decided: the section heads
  itself *Design-only — v1 spec*, and **D-266** PROHIBITS Layer 6 until the bounded-context design
  is coded and regression-tested for Layers 1–5. Whether the record means to delegate a contract for
  a prohibited layer is the user's to say. Nothing about the layer's state settles it mechanically —
  **D-433** holds that a shelved section can be a home, shelving being a status and not a kind.
- **`cowork_layer1_tone_collection_design.md`** — same glob, same ruling, and named in no
  user-ratified surface at all. ★ **This one arrived inside the ruling's own wave**: read wave 4
  made it a home in the same commit that applied R1, so the R5 measurement run *before* the ruling
  did not carry it and the count of glob-matched home documents moved by one during the wave. That
  movement is this wave's own doing and is recorded rather than absorbed. ★ **Its siting question
  is unlike the others'**: the document is a SIGN-OFF surface with its checkboxes unticked, and the
  role it designs was absorbed by the note model — so whether the record means it to be a contract
  home at all is one of the things a delegation would answer.
- **`cowork_phrase_boundary_design.md`** — the PROSE-REFERENCE case, and the one R1's own words do
  not reach in either direction: R1 names the glob and the trailing ellipsis as conferring nothing
  and admits the members named EXPLICITLY, and a prose description is neither. ★ **Two things the
  draft leaves open.** WHICH document the phrase *"the phrase-boundary design"* means — the
  candidate set derived from the tree has more than one member, and
  `cowork_phrase_boundary_methods.md` is itself still owed a read. And WHERE the clause would go —
  `ARCHITECTURE.md` names the primitive only as *"the L1.5 phrase-boundary primitive"* inside the
  Layer-6 section and carries no section that owns it, so siting the clause is an architectural act
  rather than a wording repair. **If the user writes the clause naming the document by filename,
  both questions close at once and the bar is untouched.**

## What is NOT in doubt

The ruling itself, and that nothing moved on it: **D-546** is homed, **D-432** and **D-430** are
unchanged and carry their confirmations, no entry's home class moved, and
`gen_home_classification.py`'s apply mode stays UNRUN under [[OI-305]] / [[OI-319]]. The measurement
the user made a condition of applying R1 came back showing the split moves nothing —
`tools/audit/decisions/reads4_oi326_application.json`.

## Why this is an apparatus row

**D-438**'s test: does the row's subject bear on the analysis, its inputs, or an instrument a
measurement depends on? It does not — the subject is where a decision is recorded and which surface
delegates to which document, which is the register's own filing apparatus, the side that
declaration's own line names. It gates nothing and is worked in leftover capacity. It carries no
establishment obligation, so the starred clause that makes those gate whatever their subject does
not reach it either.

*Provenance: CC, READ WAVE 4, 2026-08-04, dispatch `cc_instruction_reads_4.md` §0a ruling R2.
Register **D-546** (the ruling), **D-432** (the delegation bar), **D-430** (the section-level unit),
**D-433** (a shelved section can be a home), **D-266** (the Layer-6 prohibition), **D-438** (the
gating test). Cross-ref [[OI-326]] (the ruling's own row), [[OI-293]] (the write list, resolved for
its own population), [[OI-291]], [[OI-290]].*

---

## Note of 2026-08-04 (CC, READ WAVE 5, dispatch `cc_instruction_reads_5.md` §0a ruling R4) — THREE WRITTEN, TWO WITHHELD, and the two questions go back to the user

**The user ruled on all five.** Three delegations are **written into `ARCHITECTURE.md`**; two are
**deliberately not written**, because each carries a question a delegation would answer by fiat and
that answer is the user's to give. Withholding two is the ruling's intended outcome, not an
incomplete execution of it.

### The three written, each one line and revertible

Each was written from the drafted wording in the write list
(`tools/audit/decisions/backbone_decisions.json` → `section_home_criterion.write_list`), with the
entry range and the banner state filled from each document's own line 3, in the form
`ARCHITECTURE.md` already uses for its delegated layers. Each row now carries its
`disposition_2026_08_04`.

- **`cowork_layer1_note_model_design.md`** — written into the **Layer-1** section. The measured
  asymmetry the row named is closed: that section now carries a delegation pointer as its three
  delegated siblings do.
- **`cowork_layer2_slicing_design.md`** — written into the **Layer-2** section, immediately after
  the paragraph ending in the *"See …"* citation, which it **supplements and does not replace** —
  the shape the Layer-5 function case took on 2026-08-03.
- **`cowork_phrase_boundary_design.md`** — written into the **Layer-1** section. **Both questions
  this row carried are closed by the act, exactly as the row predicted.** *Which document:* the
  clause names the file by **filename**, so the doc-governance clause's prose reference is no longer
  what points at it, the multi-candidate set stops deciding anything, and the bar is untouched.
  *Where:* the Layer-1 section, and the choice is **stated inside the clause** rather than left to be
  inferred — `ARCHITECTURE.md` defines L1.5 as *"the thin derived-view layer over the L1 note model"*
  and carries no section of its own for it, so the layer the primitive is a view OVER is the nearest
  owning section; the only other place the primitive is named is the **Layer-6** section, which
  CONSUMES it rather than owning it and which **D-266** prohibits, so siting a contract pointer there
  would have attached it to the very question ruling R4 left open. **This is the one siting judgment
  the write required**, and it is a single line.

### The two withheld, with the question each carries — stated, not decided

- **`cowork_layer6_grouping_design.md`** — **does the record mean to delegate a contract home for a
  layer D-266 PROHIBITS** until the bounded-context design is coded and regression-tested for Layers
  1–5? **D-433** makes shelving irrelevant to the question — a shelved or deferred section can be a
  home, shelving being a status and not a kind — so nothing about the layer's state settles it
  mechanically. The nine entries stay `gap`.
- **`cowork_layer1_tone_collection_design.md`** — the document is a **sign-off surface with its
  checkboxes unticked**, and the role it designs was **absorbed by the note model**. So **whether it
  should be a contract home at all** is one of the things a delegation would decide, and a delegation
  cannot be the instrument that decides it. The two entries stay `gap`.

### What this note does NOT do

No entry's home class moved by this act: `gen_home_classification.py`'s apply mode stays **UNRUN**
under [[OI-305]] / [[OI-319]], exactly as on 2026-08-04 wave 4, so the three written delegations
change what a future re-classification pass would READ and change nothing it has already recorded.
**D-432**, **D-430** and **D-546** are untouched — a delegation the user writes settles a document
without touching the bar, which is ruling R2's own sentence.

*This row stays OPEN on the two withheld documents. It closes when the user writes or rejects each.*

---

## Note of 2026-08-04 (CC, READ WAVE 6, dispatch `cc_instruction_reads_6.md` §0a rulings R5 and R6, Task 5) — BOTH withheld documents are ruled, and the two answers are different KINDS

The note above ended by saying this row closes when the user writes or rejects each of the two.
**Both happened, in one act, and they went opposite ways — which is why the row closes rather than
narrowing a second time.**

### `cowork_layer6_grouping_design.md` — WRITTEN (R5)

The clause is in `ARCHITECTURE.md`'s **Layer-6** section, beside the *"Full spec:"* line it
**supplements and does not replace**, with the entry range **D-454…D-462** and the **AS-BUILT
(2026-07-02)** banner state filled from the document's own line 3 — the same shape the Layer-1,
Layer-2 and phrase-boundary writes took on the previous wave.

**The question this row carried is answered, and the answer's ground is stated inside the clause
rather than left in a ruling nobody reading `ARCHITECTURE.md` would find.** **D-611** establishes
that the dormant path and its staged scaffolding are **deferred-engagement, not dead code** — their
wire-or-remove verdict belongs to a named later step, and removing them is a STOP. So deferred work
is a state the record already keeps decisions for, and a contract home for deferred work is the same
posture rather than a new one; **that is why D-266's prohibition does not forbid the pointer.**
**D-266 is untouched and the clause says so in terms:** Layer 6 may not be BUILT until the
bounded-context design is coded and regression-tested for Layers 1–5. The clause records where the
layer's design decisions live and authorizes no build, no wiring, and no change to what the analysis
computes.

### `cowork_layer1_tone_collection_design.md` — NOT DELEGATED, and RULED rather than deferred again (R6)

**The concern this document designs was ABSORBED BY THE NOTE MODEL**, so a delegation would create a
**second home for a concern that already has one**, which **#6** forbids. The question is therefore
settled by the standing principles rather than left open — which is **D-599** working: apply the
principles first, and only where they do not decide is there a genuine user choice.

What was recorded instead of a delegation, in both places the record needs it:

- **On the document's own status**, as a starred banner: the concern is absorbed by the note model,
  the responsibility §1 states is now the lossless note model's — specified at `ARCHITECTURE.md`'s
  Layer-1 section and contracted at `cowork_layer1_note_model_design.md`, which that section
  delegates to by name — and the document is **HISTORICAL**, to be read as the provenance of how
  collection came to be separated from weighting, bass selection and filtering, never as a live
  contract.
- **In the write list**, as this row's `disposition_2026_08_04_wave6`.

Its two entries — **D-569** and **D-570** — stay `gap`, which is the correct class for a decision
whose concern is homed elsewhere and whose recording surface is historical.

### The delegation-bar grades moved with the acts, and are recorded rather than absorbed

`gen_phase1p_delegation_bar.py`'s FORMS table carries both movements with the former grade preserved
(#12): `cowork_layer6_grouping_design.md` moves **BARE_CITATION → CLAUSE**, anchored on the
**delegation's own sentence** rather than on the bare filename — the [[OI-330]] lesson applied *at*
the write instead of after it, since the filename is no longer unique in that file and under rule
**(k1)** the strongest naming governs; and the tone-collection row now records **NOT_NAMED as a
RULING**, with R6's ground, rather than as an unanswered question.

### What this note does NOT do

No entry's home class moved: `gen_home_classification.py`'s apply mode stays **UNRUN** under
[[OI-305]] / [[OI-319]], so the written delegation changes what a future re-classification pass would
READ and changes nothing it has already recorded. **D-432**, **D-430** and **D-546** are untouched —
a delegation the user writes settles a document without touching the bar, which is ruling R2's own
sentence, and a delegation the user DECLINES settles one the same way.

*This row is RESOLVED: all five drafted delegations are disposed — four written, one ruled not a
delegation target.*

---

*Resolution belongs in the INDEX row; dated notes may be appended here.*
