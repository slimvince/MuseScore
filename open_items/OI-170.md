# OI-170 — The OI-168 defect survives at three more sites — L4 is NOT tonic-independent

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-170 | ★★ **THE OI-168 DEFECT SURVIVES AT THREE MORE SITES — L4 IS *NOT* TONIC-INDEPENDENT (CC-discovered 2026-07-14 at the OI-168 fix; Class-A #18).** The OI-168 fix corrects the two key-consuming **scoring terms** (`dim7CharacteristicBonus`, `diatonicRootContribution`), and that is all it corrects. **Three further sites in L4 answer a pure COLLECTION-membership question through the TONIC** — the identical `(keyTonicPc + scale[i]) % 12` construction, with the identical δ≠0 corruption on `Altered`/`AlteredDomBB7`, all on the LIVE region path (the decoder runs none of them — it passes `gateCtxOut=nullptr` and never reaches the degree block): **(1)** `buildChordResult`'s `diatonicToKey` flag (`chordanalyzer.cpp`, the "Diatonic check: every sounding pc must be in the scale" loop) — a PUBLISHED fact (`r.function.diatonicToKey`) read by `notationimplodebridge.cpp:1205`, and re-derived independently in `sectionanalyzer.cpp:145/280`, `functionromannumeral.cpp:36`, `functionrelationallabel.cpp:49`, `notationcomposingbridge.cpp:97` (a #6 duplication in its own right — five re-derivations of one published fact); **(2)** **Gate I**'s `invRootIsDiatonic` (`postscoringgates.cpp:475-479`) and **(3)** **Gate L**'s `invRootIsDiatonic` (`:520-524`) — both *swap the committed winner* when they fire, so they are decision-bearing, not cosmetic. **⇒ The OI-168 dispatch's closure statement — "L4 is structurally tonic-independent" — is REFUTED, and the `cowork_evidence_inventory.md` §8 circle-3 collection/tonic split still DOES NOT HOLD for the live L4.** After the fix, L4 is also internally INCONSISTENT under a δ≠0 mode: `diatonicRootContribution` now says a root IS in the key (signature collection) while Gate L can simultaneously say it is NOT (mode-transposed set) — two answers to one question inside one analysis (#6/#12). **This does NOT invalidate the OI-168 measurement or its adoption:** the A/B held these three sites constant, so the measured result (Baroque/Default byte-identical; Jazz −480 class-(b), removal-only, OVERALL PASS) is exactly what the adopted fix delivers — a strict improvement, just a partial one. **Declared, NOT fixed** (#8 — inference-affecting, outside the fix dispatch's scope; a further correctness re-baseline with its own robust-stop diff). The population is the same 24 Jazz `Altered` regions; Baroque/Default should again be byte-identical (δ=0). **★ MEASURED 2026-07-14 (`cc_oi170_measure_report.md`; dispatch `cc_instruction_oi170_measure.md`; default-OFF counters + a default-OFF signature-mask A/B over all three sites, the OI-110 pattern; OFF path byte-identical 352×3, all three suites green, no golden refreshed, `tools/robust_stop`/`tools/corpus` untouched). THE THREE SITES ARE CONFIRMED — AND THE FIX IS METRIC-NEUTRAL.** Under the variant: **Baroque and Default are BYTE-IDENTICAL (352/352 each — the δ=0 derivation verified at runtime, every `…Differs` counter 0)**; **Jazz: 9 files change and the ONLY JSON key that changes anywhere in the corpus is `diatonicToKey`** — **22 flags flip, every one `false → true`, and NOT ONE committed chord moves (0 flips on all three presets)**. Per-site attribution at the counters: **Gate I**'s membership verdict differs on exactly **1** candidate corpus-wide and its SWAP decision differs **0** times; **Gate L**'s verdict never differs. **The hard stop is EXACTLY unmoved** — `robust_stop_diff.py` **OVERALL PASS** with run-diff **(+0/−0)** on every preset, class-(b) root-disagree duration **δ=0**, class-(a) **δ=0**, every key column identical to the digit ⇒ **no robust-stop re-baseline is needed; only `tools/corpus/jazz` (9 files) regenerates + a manifest re-stamp.** The pipeline-snapshot goldens carry **no `diatonicToKey` field and no Altered-family key** (verified) ⇒ **no golden refresh at the fix.** **Toward-correct: all 22 are right** — the 22 regions' chords are plain diatonic triads/sevenths of the local signature's own collection, which the current code scores against that collection **transposed up a semitone**. **THE ENUMERATION ALSO REFUTES THIS ROW'S SCOPE:** a *mechanical* whole-tree sweep (118 hits, every one read) found **3 class-(a) sites — exactly the three named — but SEVEN class-(b) genuine-tonic sites, three of them LIVE and DECISION-BEARING**: Gate G-E (**58 winner swaps on Baroque**), **`applyTonicPriorToSparseChord` (172/183/172 committed-QUALITY overwrites on the commit path — named by no register row before now, see OI-172)**, and the published `degree` itself. **⇒ L4 CANNOT be made fully tonic-independent** — the collection question can be made tonic-free, the DEGREE question cannot, and the degree question is currently wired into the identity decision. The "five re-derivations of `diatonicToKey`" framing is also wrong at the code (four inequivalent definitions, most for a *different* key — see OI-173). | `cc_oi170_measure_report.md` (+ the generated artifact `cc_oi170_measurements.txt`); `cc_oi168_fix_report.md` §5; `chordanalyzer.cpp` (`buildChordResult` degree + diatonic-check loops); `postscoringgates.cpp` (Gate I / Gate L / Gate G-E); `sparsechordrefinement.cpp:182-215`; `notationimplodebridge.cpp:1205`; OI-167; OI-168; OI-172; OI-173 | OPEN — **the STOP stands, but its ground has moved: no doc may call L4 tonic-independent, because it structurally CANNOT be (class-(b) sites b1/b2/b3 are live).** The three class-(a) sites are now MEASURED and ready to fix as a **metric-neutral, user-ratified correctness commit** (0 chord flips; 9 Jazz corpus files + manifest re-stamp; no golden). Awaiting the Cowork/user disposition on (i) promoting that fix and (ii) the design question it exposes — does DEGREE-based decision-making (Gate G-E, `applyTonicPriorToSparseChord`) belong in L4 at all? |
