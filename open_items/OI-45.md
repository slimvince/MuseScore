# OI-45 — scoring_model §4/§6 stale anchors

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — Doc-sync debt (#10) — annotate, don't rewrite history**

| OI-45 | scoring_model §4/§6 stale anchors + kHalfDimFirstInversionBonus missing from §6 | premise-debt T3-4 | OPEN — fix at next scoring_model touch |

## ★ DATED NOTE — 2026-08-14 (CC, `cc_instruction_scoring_model_pass.md` Task 2; document pass `b366d44947`)

*Status of record is the INDEX row, which this note does not carry.*

**Both halves of this row were wrong about their own subject, and enumerating at the document is
what found it.** The dispatch required the population be enumerated at `docs/scoring_model.md`
itself rather than from this row's list, on the previous batch's method result — a row's list can
be wrong about its own subject. It was, twice.

**The anchor half named the wrong sections.** The raw line-number anchors stood in **§2, §3, §4, §5
and §7**. Not §6 — §6's Location column had already been converted to named code regions, on
exactly the ground this row was raised for; and not only §4. *(★ CORRECTED 2026-08-14, later the
same day, by this session's own self-check. The clause first ended "…on exactly the ground this row
was raised for, **before this row was written**"; that ordering is **NOT established** — the document
records the conversion without dating it — so the claim is WITHDRAWN rather than restated in weaker
words, its former wording preserved here (#12). What the finding rests on is the section list at
HEAD, established at the document.)* All of
them are now re-aimed to named code regions under **D-307**, and every former wording is preserved
verbatim in a single "Code locators" note in the document, each with the section it stood in (#12).
**Four had drifted across a FILE boundary**, not merely down a file: refactor #1 moved three §7
subjects out of `chordanalyzer.cpp` into `postscoringgates.cpp`, and the `ChordAnalyzerPreferences`
declaration is in `analysis/types/analysistypes.h`.

**Two further stale locators this row does not name**, found the same way and corrected in the same
pass: §4's `ScoringPhase` citation, which named `chordanalyzer.h` for the enum AND the prefs field
— both live in `analysis/types/analysistypes.h` — and placed the `ScoringSnapshot` forward
declaration beside the enum, where it no longer is; and §4's `chordanalyzer.h:411` for the prefs
declaration, wrong in the file as well as the line.

**`kHalfDimFirstInversionBonus` is not missing from §6.** The entry exists, and §1's override list
names the constant as well. **What was false is where the entry said the bonus fires.** Read at the
code, the bonus is applied inside the **bias-correction** block — after the winner's bass-root
deduction, before the bias re-sort, inside the `!didEnharmonicFlip` branch that by construction runs
only when the enharmonic flip did NOT fire. The former wording placed it "inside the enharmonic-flip
block", in the "Gate-A / G-family region". `paramoverride.h`'s rule enum is the second witness: the
`BiasCorrection` rule owns this bonus, so `disable_rule BiasCorrection` suppresses it along with the
deduction. Corrected in place, former wording preserved (#12).

**One naming observation, declared and NOT acted on:** the constant's name says *first inversion*
while the code admits the alternative's third, fifth OR seventh in the bass. The code's own comment
carries the same wording, so the name is inherited rather than a defect this pass could re-specify.

**No `src/` edit. No scoring value, threshold, gate or template added, changed or removed** — this
was a documentation pass over a document that describes them, and the sync rule runs the other way.
