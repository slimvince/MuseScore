# OI-281 — Does an `ARCHITECTURE.md` delegation pointer confer contract-home status on a document that self-declares "v1 draft"?

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section E — user adjudication. Rowed 2026-08-03 on the user's ruling of the same date (R5(b) of
`cc_instruction_phase1k_apply_rulings.md`), which directed that the question be recorded rather than
answered. It is a question about the fifth home case's criterion, so it is the user's to settle, not
a homing act.**

## The question

The fifth home case — a ratified contract document the owning `ARCHITECTURE.md` section points at is
a proper home — was user-ratified 2026-08-02 at [[OI-268]] with a **two-part criterion**: the document
is ratified or signed **and** carries a status banner, **and** the owning section delegates to it by
pointer. The transitive refinement of the same date adds that the ratification may be the user's
**directly OR TRANSITIVELY** — a user-ratified surface explicitly delegating to the document carries
the user's authority to it — with the guard that **an assistant's stamp alone never confers
contract-home status** (`CLAUDE.md`, decisions-register rule (g)).

`cowork_progression_schema_dictionary.md` satisfies the **delegation** half and fails the
**ratification** half in a way none of the other candidates showed:

- **Delegation, satisfied.** `ARCHITECTURE.md:4589-4591` (§7, The Knowledge Base) names the document
  as the component's own specification: "formalised as an **independent knowledge-base component**
  with its own spec (`cowork_progression_schema_dictionary.md`)". §6.7 points at it a second time,
  for the style taxonomy (`:4579-4580`).
- **Ratification, not satisfied as written.** The document's own banner reads "**Status: component
  spec, v1 draft (2026-06-29)**" (`:3`). A draft names no ratifier — neither the user's directly, nor
  a delegating surface's transitively.

Nine register entries (**D-406…D-414**) are homed there and carry the `⚠gap` flag for exactly this
reason. They were ratified as *decisions* by the user on 2026-08-03; that ratification is of each
rule, and it does not decide the document's home class, which is what this row asks.

## The two readings, stated without choosing

**(i) Transitive authority reaches it.** The delegating surface — `ARCHITECTURE.md` §7 — is
user-ratified, and it delegates the component's specification to this document by name. On the
transitive clause that is the user's authority carried across, and the document's own banner wording
is then a stale self-description rather than a statement of authority. *Consequence:* nine gap flags
close at their cause rather than being tracked one by one, which is what #6 asks of a defect with a
single source.

**(ii) It does not.** Rule (g)'s transitive clause was ratified together with the guard that an
assistant's stamp alone never confers contract-home status. A self-declared draft names **no**
ratifier at all — not the user's, not an assistant's — so there is nothing for the delegation to
carry across; a pointer establishes *where the detail lives*, not *that the detail was decided*.
*Consequence:* the nine stay flagged until the document is ratified or re-bannered, which is itself a
user act.

Phase 1i's own precedent cuts both ways and is recorded here so the ruling does not have to rediscover
it: `cowork_structural_integrity_audit.md` was excluded from contract-home status even though its
transitive ratification was judged genuine, because the second half of the criterion failed — the
same shape as (ii). Against that, `cowork_layer5_engagement_design.md` was admitted on transitive
authority alone, its banner reading only "DESIGN (CC, 2026-07-07)" — an assistant's own status word,
admitted because the user-ratified arc plan delegated to it by name three times. Whether "DESIGN (CC)"
and "v1 draft" are the same case is precisely what is unsettled.

## The scope — why this is not one document's problem

**The answer applies to every draft-bannered document `ARCHITECTURE.md` points at, not to this one.**
That is why it is a user ruling and not a homing act: settling it for the dictionary alone would leave
the criterion ambiguous for the next document that arrives in the same state, and the register would
then carry two documents in the same condition with different home classes.

It is the sibling of [[OI-268]]'s other still-open question — **whether a COWORK ratification counts**,
which decides **twenty-one** entries (`docs/decoder_design.md` 4 + `cowork_score_census.md` 17). Both
ask what kind of authority the fifth home case requires; they should be ruled together, and the two
answers must be consistent with each other.

## What is NOT in doubt

The document's content, read in full at phase 1j and yielding the nine register entries; the nine
rules themselves, user-ratified 2026-08-03; and the delegation pointer, verified at
`ARCHITECTURE.md:4589-4591` in this session.

## Disposition

**Deliberately NOT resolved by editing the dictionary's banner.** The phase-1k dispatch says so in
terms: re-bannering the document would decide the question by fiat and would decide it for one
document only. If the ruling goes to (i), the nine flags clear by re-classification and the banner is
corrected as a consequence; if it goes to (ii), the flags stand until the document is ratified on its
own. Any banner edit here also wants to land with the [[OI-280]] label-collision fix, so the file is
touched once.

*Provenance: the underlying finding is CC's, 2026-08-02, at `cc_instruction_phase1j_homing_and_reads.md`,
Task 4 — recorded then only inside D-406…D-414's provenance fields. Rowed 2026-08-03 by the user's
direction at phase 1k. Cross-ref [[OI-268]] (the fifth home case and its open sibling question),
[[OI-280]] (the same document's label collision), [[OI-207]].*

---

**★ Dated MEASUREMENT note — 2026-08-03 (CC, phase 1l, Task 7). The user ruled the criterion; the
user did NOT authorize re-classifying against it before the cost was known. This note is the cost.
NO entry's home class was changed.**

## The criterion as ruled

*A document is a contract home when a user-ratified surface delegates to it **BY NAME**, **FOR A
STATED CONCERN**, and the document is **stable enough to be cited**.* The banner's self-description
is not the test; the delegation's specificity is. Rule (g)'s guard survives intact — an assistant's
stamp confers nothing, because the delegation does the conferring and only the user writes a
delegation into `ARCHITECTURE.md`.

## Method

The three surfaces whose user ratification is not in question were searched for every by-name
mention of every home document: **`ARCHITECTURE.md`** (the canonical document, **D-091**),
**`CLAUDE.md`** (the standing instructions), and **`cowork_engage_arc_plan.md`** (RATIFIED by the
user, 2026-07-07 — the one user-ratified PLAN the phase-1i precedent turned on). Each mention was
then read in place to judge clauses (b) and (c).

**The population is 131 entries across 21 documents** — the 85 currently classified `contract-home`
plus the 46 currently classified `gap`. It is not the ~112 the dispatch names: that was phase 1i's
affected set, and the register has grown by 23 entries and three waves since. The `process` (45),
`project-convention` (35) and `unhomed` (11) classes are outside the criterion's reach — it decides
whether a *design document* is a home, not whether `CLAUDE.md` is.

## 1. What moves, in which direction, and which ones

### Clean ADMIT — stays or becomes `contract-home` (75 entries)

| document | entries | the delegation |
|---|---|---|
| `cowork_layer3_keymode_design.md` | 16 · D-343…D-358 | `ARCHITECTURE.md:1329` "The ratified contract for this layer is …" |
| `cowork_score_census.md` | **17 · D-359…D-375 — MOVES IN** | `ARCHITECTURE.md:317` "The constraint's own detailed block, with the per-licence-class pool table, is `cowork_score_census.md` §8c" — inside a passage `:315-316` marks user-ratified 2026-07-04 and reaffirmed 2026-08-02; **and** `CLAUDE.md:388` "the research-tier-on-entry rule is stated on the corpus side at `cowork_score_census.md`'s decision-tier block" |
| `cowork_progression_schema_dictionary.md` | **9 · D-406…D-414 — MOVES IN** | `ARCHITECTURE.md:4589-4591` "formalised as an **independent knowledge-base component with its own spec** (`…`)"; `:4539` declines to restate it in terms — "it is not restated here (a cross-cutting definition is stated once…)" |
| `cowork_layer5_engagement_design.md` | 8 · D-380…D-387 | `ARCHITECTURE.md:1483`, **and independently** `cowork_engage_arc_plan.md:41`, `:46`, `:53-55` |
| `cowork_bounded_context_design.md` | 7 · D-260…D-266 | `ARCHITECTURE.md:909-913` "The ONE detailed cross-layer spec for this contract is **`…`**" |
| `cowork_layer4_chordsymbol_design.md` | 6 · D-329…D-334 | `ARCHITECTURE.md:1405` "The ratified contract for this layer is …" |
| `docs/scoring_model.md` | 4 · D-321…D-324 | `CLAUDE.md:651-691` — a whole standing section naming it, making it a mandatory read, and binding a same-commit sync rule to it |
| `cowork_stage5_fitter_design.md` | 3 · D-312…D-314 | `ARCHITECTURE.md:283` "the fitting event's own design contract is …" |
| `cowork_confidence_contract.md` | 3 · D-267…D-269 | `ARCHITECTURE.md:948-949` "…are stated in full in `cowork_confidence_contract.md`" |
| `cowork_notation_output_contract.md` | 2 · D-275/D-276 | `ARCHITECTURE.md:73` "…contract `cowork_notation_output_contract.md`" |

### Clean EXCLUDE — stays or becomes `gap` (23 entries)

| document | entries | why it fails |
|---|---|---|
| `cowork_architecture_reassessment.md` | **4 · D-282…D-285 — MOVES OUT** | **not named at all** in any of the three surfaces. It became `contract-home` at phase 1k purely on the user's ratification of its status banner — precisely the test the new criterion replaces |
| `docs/decoder_design.md` | 4 · D-325…D-328 | **not named at all** |
| `docs/implementation_roadmap.md` | 4 · D-419, D-421, D-422, D-423 | **not named at all** — though it calls itself "the single tracker" |
| `docs/redesign_plan.md` | **4 · D-317…D-320 — MOVES OUT** | named three times, but each is a citation ("Design reference: …", "adopted Phase E direction: …"), and **two of the three sit inside blocks `ARCHITECTURE.md` itself banners SUPERSEDED** (`:783-786`, "retained only as history — do not build to it"). A citation from superseded text is not a live delegation |
| `docs/iteration_path1_summary.md` | **2 · D-280/D-281 — MOVES OUT** | **not named at all** |
| `cowork_structural_integrity_audit.md` | 5 · D-401…D-405 | excluded — but see §2, it fails on clause (c) ALONE |

### GENUINELY AMBIGUOUS — the criterion does not decide them (33 entries)

| document | entries | the delegation, and why it is not decisive |
|---|---|---|
| `cowork_voiceleading_axis_design.md` | 13 · D-388…D-400 | `ARCHITECTURE.md:899-900` "Criterion + **build home**: `…` §0/§5.3 (AS-BUILT)" and `:928` "VL-D/E/F/G/H named + **design-gated** (`…`)". Both name a concern and read as delegations; neither is the "the ratified contract for X is Y" form. Does "build home" delegate, or cite? |
| `cowork_layer5_function_design.md` | 8 · D-335…D-342 | `ARCHITECTURE.md:1480-1481` "**Full spec:** `cowork_layer5_function_design.md`." Names the concern in two words and nothing more — a citation appended to a paragraph, not a delegation clause. Compare `:1483` immediately beneath it, which IS one |
| `cowork_prefit_gates.md` | 5 · D-270…D-274 | `ARCHITECTURE.md:48-49` "pre-fit gates `cowork_prefit_gates.md`" in a list of citations, and `:279-281` "Protocols ratified 2026-07-19 (`…`)" — attributions of where something was ratified, not instructions to read it instead of the section |
| `cowork_joint_key_chord_design.md` | 4 · D-376…D-379 | `cowork_engage_arc_plan.md:44` delegates arc #10 to it by name for a stated concern — clauses (a) and (b) PASS. Clause (c) is the problem: the design is **shelved** (OI-43 settled and OI-44 declared, both 2026-07-12), and [[OI-51]] records an uncommitted SHELVED-banner edit still sitting in the working tree. Is a shelved design "stable enough to be cited"? |
| `cowork_engage_arc_plan.md` itself | 3 · D-278, D-279, D-311 | `CLAUDE.md:130` names it for a stated concern (the MEASURE-BEFORE-BUILD gate, "now the middle stage of the #17 funnel"). Phase 1i excluded it on the ground that **"it is a plan, not a contract for a layer"** — a document-TYPE test the new criterion does not contain. On the criterion as ruled it would be admitted |

### The arithmetic

The three buckets partition the population; the fourth row is a SUBSET of the second, not a fourth
bucket. 75 + 23 + 33 = 131.

| | entries |
|---|---|
| population measured (`contract-home` 85 + `gap` 46) | **131** |
| clean ADMIT | 75 — of which **26 MOVE IN** (score census 17, dictionary 9) |
| clean EXCLUDE | 23 — of which **10 MOVE OUT** (reassessment 4, redesign plan 4, iteration summary 2) |
| — of that 23, decided only by the JUDGMENT clause | 5 (`cowork_structural_integrity_audit.md`) |
| genuinely ambiguous | **33** |

*Scope of the measurement:* it was taken at the register's 423-entry state, before this wave's read
added D-424…D-426. Those three are homed in a twenty-second document
(`cowork_notation_adoption_increment.md`) which is named in none of the three surfaces and so would
be excluded under the criterion — consistent with the class they were entered in, and not counted
above.

**Net, on the clean cases alone: +16 into `contract-home`** (26 in, 10 out). If every ambiguous
document were admitted the net would be +49; if every one were excluded, −17. **The criterion
therefore does not determine the direction of its own effect** — the ambiguous 33 are larger than
the clean net movement of 16.

## 2. Do the two precedents come out right?

**Both land where the precedent has them, so this is NOT a #13 STOP.** But how they land matters:

- **`cowork_layer5_engagement_design.md` — ADMITTED. ✅ And on its own original ground.** The arc
  plan's three by-name delegations (`:41`, `:46`, `:53-55`) carry it independently of
  `ARCHITECTURE.md:1483`, which is important because `:1483` was itself WRITTEN at phase 1i/1j as
  the remedy for the missing delegation. Had the pointer been its only support, the criterion would
  have been admitting a document on a pointer the classification itself produced.
- **`cowork_structural_integrity_audit.md` — EXCLUDED. ✅ But on clause (c) ALONE, and for a
  DIFFERENT reason than phase 1i used.** On clauses (a) and (b) it **passes**: `cowork_engage_arc_plan.md:4`
  (a user-ratified surface) says "It does not re-derive the fix details — **those live in
  `cowork_structural_integrity_audit.md`** (§3 fix-queue, §4 sequencing)" — by name, for a stated
  concern, as specific a delegation as any in the ADMIT table. What excludes it is "stable enough to
  be cited": its **§5 is headed "UNCLEAR rows for user adjudication"** and lists seven open
  questions, and `:343-346` states its own sequencing call "is brought to the user" — which the
  record does not show happening. Phase 1i excluded it on the OLD criterion's second half (no
  ratified-or-signed banner), a half the new criterion drops.

**The finding: the precedent survives only because clause (c) is a judgment, and clause (c) is doing
all the work in the hardest case.** The criterion moved the test from the document's
self-description to the delegation's specificity — and then the one case it was built to preserve is
decided not by the delegation at all, but by a reading of how settled a document is. That is not a
refutation; it is the shape of the residue, and it is what the ambiguous 33 have in common.

## 3. Do the two sibling populations answer consistently?

They answer under **one criterion applied uniformly**, and they come out **differently** — which is
consistency of method, not of outcome:

- **`cowork_progression_schema_dictionary.md` (9, D-406…D-414) — ADMITTED.** The delegation at
  `ARCHITECTURE.md:4589-4591` names it as the component's own spec, and `:4539` explicitly declines
  to restate its content. The "v1 draft" banner never enters the test.
- **The Cowork-ratification set (21) SPLITS.** `cowork_score_census.md` (17) is **ADMITTED** on two
  independent delegations from two user-ratified surfaces. `docs/decoder_design.md` (4) is
  **EXCLUDED** — it is not named in `ARCHITECTURE.md`, `CLAUDE.md` or the arc plan at all.

**The material finding here is what the criterion does to [[OI-268]]'s sibling question.** That
question — *does a COWORK ratification count?* — is not answered by this criterion. It is **made
moot**: the criterion never asks who ratified the document, only who delegates to it. Ruling the
criterion therefore **dissolves** the sibling question rather than deciding it, and the 21 entries
split 17/4 on grounds that have nothing to do with Cowork ratification. Whether dissolution is the
answer the user intends for OI-268 is not something this measurement can settle, and it is stated
here rather than assumed.

## 4. What a re-classification would cost, if ordered

Two acts, and they are not the same size:

1. **The clean cases — 36 entries move** (26 in, 10 out), each with the delegation citation recorded
   in its provenance and the former class preserved (#12). Mechanical, one regeneration, the three
   guards. Comparable in size to the phase-1i wave.
2. **The ambiguous 33 need a second ruling, not a session's judgment.** They divide into two
   questions the user would have to answer, and both are general rather than per-document: **does a
   bare citation of the form "Full spec: `X`" delegate, or only cite?** (that decides
   `cowork_layer5_function_design.md`, `cowork_voiceleading_axis_design.md` and
   `cowork_prefit_gates.md` — 26 entries); and **is a SHELVED or explicitly-unsettled document
   "stable enough to be cited"?** (that decides `cowork_joint_key_chord_design.md` and, with it, the
   `cowork_structural_integrity_audit.md` precedent — 9 entries).

Applying the criterion to the clean cases while the ambiguous 33 wait would leave the register in a
state where two documents in materially the same condition carry different home classes — the exact
outcome the scope paragraph above gives as the reason this is a user ruling and not a homing act. So
the honest sequencing is: rule the two follow-on questions first, then re-classify once.

*Nothing in this note was applied. The 131 entries carry the classes they carried before it.*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

---

**★ Dated MEASUREMENT note — 2026-08-03 (CC, phase 1m, Task 5). The user replaced this criterion's
third clause with a KIND test. The test is MEASURED here and deliberately NOT APPLIED — no entry's
home class was changed, exactly as at phase 1l. Every figure below is generated:
`tools/audit/decisions/phase1m_measurements.json`, from
`tools/audit/decisions/gen_phase1m_measurements.py` (#17f).**

## The criterion as ruled

*A document is a contract home when a user-ratified surface delegates to it **BY NAME**, **FOR A
STATED CONCERN**, and the document is of a **kind that states rules** — specification, contract,
design, component spec, binding plan — rather than a kind that **records findings** — audit, report,
census, inventory, measurement artifact, probe, dossier of findings.* The operative question is the
document's **purpose**: does it state what shall be, or record what was found? The banner's
self-description is not the test. Rule (g)'s guard survives intact — an assistant's stamp confers
nothing, because the delegation does the conferring and only the user writes a delegation into
`ARCHITECTURE.md`.

**What changed from the criterion measured at phase 1l is the third clause only.** Clauses (a) and
(b) — by name, for a stated concern, from a user-ratified surface — are unchanged, and every
delegation verdict below was re-derived mechanically this session and then read in place.

**Precedence, stated because it does work.** The criterion requires BOTH halves, so either half
failing is decisive and the other need not be judged: a document no user-ratified surface delegates
to is excluded whatever kind it is, and a findings-kind document is excluded however specific the
delegation. Only where nothing is decisive is an entry ambiguous.

## 1. The population, and what moves

**134 entries across 22 documents** (85 currently `contract-home` + 49 currently `gap`) — three more
than phase 1l's 131, the register having grown by D-424…D-426.

| | entries |
|---|---|
| ADMIT | **65** |
| EXCLUDE | **40** |
| AMBIGUOUS | **29** |

**MOVES IN (16):** `cowork_progression_schema_dictionary.md` 9 · `cowork_joint_key_chord_design.md`
4 · `cowork_engage_arc_plan.md` 3.

**MOVES OUT (10):** `cowork_architecture_reassessment.md` 4 · `docs/redesign_plan.md` 4 ·
`docs/iteration_path1_summary.md` 2.

**Net on the clean cases: +6 into `contract-home`** (against phase 1l's +16 under the old clause).

## 2. Cowork's advance prediction, and the answer

**The prediction was recorded in the dispatch before the run (#17b): `cowork_score_census.md` is a
census — a findings kind — so its 17 entries should move OUT, against phase 1l's measurement which
moved them in. CONFIRMED.** The census is EXCLUDED on kind, though it passes (a) and (b) twice over
(`ARCHITECTURE.md:317`, inside a passage the section marks user-ratified, and `CLAUDE.md:388`).
Relative to the REGISTER's current state nothing moves — those 17 are already `gap` — so the
confirmation is against phase 1l's measured outcome, which is the comparison the prediction was
about. The two reference frames are kept distinct here rather than merged into one "movement"
number.

## 3. The two precedents — both land right, so there is NO #13 STOP

- **`cowork_layer5_engagement_design.md` — ADMITTED.** Delegation passes (`ARCHITECTURE.md:1483`,
  and independently `cowork_engage_arc_plan.md:41`/`:46`/`:53-55`); kind is a design. ✅
- **`cowork_structural_integrity_audit.md` — EXCLUDED.** Delegation PASSES
  (`cowork_engage_arc_plan.md:4`, by name for a stated concern); it is excluded on **KIND**, and the
  document says what kind it is in its own words: *"read-only grounded catalogue"*, and *"Every fix
  named here is a later, separate, user-ratified refactor — this document queues them, it does not
  act."* ✅

**This is the material improvement, and it is real.** Under the old clause the hardest case was
decided by "stable enough to be cited" — a judgment about how settled a document is, doing all the
work in the one case the criterion was built to preserve. It is now decided by what the document IS,
from its own opening sentence. **Clause (c) as a judgment is gone from the test entirely.**

## 4. Does ambiguity shrink? By four entries — which is not materially

**33 → 29.** The accounting matters more than the number, because the reduction is not where the
dispatch expected it:

- **The kind test DISSOLVES the whole of the old clause-(c) residue — 9 entries.**
  `cowork_joint_key_chord_design.md` (4) was ambiguous only because it is *shelved*; the kind test
  never asks, and it is a design, so it is ADMITTED. `cowork_structural_integrity_audit.md` (5) was
  decided by clause (c) alone; it is now decided by kind. The follow-on question phase 1l left for
  the user in this class — *is a shelved or explicitly-unsettled document stable enough to be
  cited?* — is **moot**.
- **It also resolves `cowork_engage_arc_plan.md` (3)**, ambiguous at phase 1l because phase 1i had
  excluded it on a document-TYPE test the criterion then lacked. The kind test contains one: a
  ratified plan that *"fixes the **order and the principle behind each step**"* is a binding plan,
  the kind the ruling names.
- **But the dominant source of ambiguity is untouched, because it lives in clause (b), which the
  ruling did not change: 26 entries turning on ONE question — does a bare citation of the form
  "Full spec: `X`", "build home: `X`", or "(`X`)" inside a list DELEGATE, or only CITE?** That
  decides `cowork_voiceleading_axis_design.md` (13), `cowork_layer5_function_design.md` (8) and
  `cowork_prefit_gates.md` (5).
- **And one document joins the ambiguous set on a correction of record** — §5 — bringing it to 29.

**The verdict, stated plainly because Cowork proposed the criterion and the honest answer is the
useful one: the kind test does what it was designed to do, and not what the dispatch's §6.5 hoped.**
It makes the third clause mechanical, and the hardest precedent now turns on a citable sentence
rather than on a judgment about settledness. It does not make the TEST mechanical, because the
residual ambiguity was never in clause (c). **The criterion still does not determine the direction of
its own effect:** admit all 29 ambiguous and the net is +16; exclude all 29 and the net is −20. The
sign is undetermined, as it was at phase 1l.

## 5. ★ A correction of record, found by re-deriving rather than trusting

Phase 1l recorded `cowork_notation_adoption_increment.md` as *"named in NONE of `ARCHITECTURE.md`,
`CLAUDE.md` or `cowork_engage_arc_plan.md` — established at Task 7 this session"*, and the
[[OI-207]] note of the same date repeats it. **It is named in two of them:**
`ARCHITECTURE.md:4660` (*"the pedal-point ruling of the notation-adoption increment (`…` §7 +
§10)"*) and `CLAUDE.md:128` (*"analysis in `…` §2"*). Both name a concern; both are attributions of
where a ruling's analysis lives — the same shape as `cowork_prefit_gates.md` — so the verdict is
**UNCLEAR, not FAIL**, and its 3 entries join the ambiguous set rather than the excluded one.
Nothing downstream of phase 1l depended on the error, since no class was changed. The guard against
its recurrence is the one that caught it: the mention set is derived mechanically by the generator
and cross-checked against the authored table, which raises an error if a document recorded as
unnamed is named.

## 6. Genuinely ambiguous KINDS, reported rather than forced into the binary

The dispatch asked for these rather than a forced call. Three documents, and one structural finding:

- **`docs/implementation_roadmap.md` — both at once.** It is *"the single tracker ensuring every
  review conclusion is addressed"* (`:5-6`) AND it states rules: its own standing rule (`:10-11`)
  and the user-ratified engage criteria and retirement map. Excluded here by clause (b) regardless,
  so the kind question does not bite — but it would if anything ever delegated to it.
- **`docs/redesign_plan.md` — kind and status disagree.** A plan by kind, converted to a record by a
  **user-ratified banner** (*"SUPERSEDED AS A PLAN — RETAINED AS THE RECORD OF WHAT WAS TRIED AND
  CLOSED"*). "The banner is not the test" was written against SELF-descriptions; this banner is a
  ruling. Excluded by clause (b) regardless.
- **`cowork_notation_adoption_increment.md` — a decision surface**, a shape the kind list has no
  entry for: its options are findings, its ratified recommendations are rules.
- **★ The structural one: kind is a property of the DOCUMENT, but a delegation points at a
  SECTION.** `ARCHITECTURE.md:317` delegates to `cowork_score_census.md` **§8c** — a block that
  states a binding constraint — inside a document whose kind is a census. The kind test excludes the
  document and with it the block. Whether the unit of the test should be the document or the
  delegated section is a question the ruling does not answer, and it decides the largest single
  population in this measurement (17 entries).

## 7. Does the test still make [[OI-268]]'s Cowork-ratification question moot?

**Yes, and now uniformly.** Neither clause asks who ratified the document. The 21-entry
Cowork-ratification set is now **excluded entire, for two unrelated reasons**:
`cowork_score_census.md` (17) on KIND, `docs/decoder_design.md` (4) on DELEGATION — it is named in
none of the three surfaces. At phase 1l the same set split 17/4 the other way. Either way the ground
has nothing to do with Cowork ratification, so the sibling question is **dissolved, not decided** —
and whether dissolution is the answer the user intends remains, as phase 1l said, not something a
measurement can settle.

*Nothing in this note was applied. The 134 entries carry the classes they carried before it.*

---

**★ Dated note — 2026-08-03 (CC, phase 1n, Task 1). THE USER RULED A THIRD CRITERION, AND THIS TIME
IT IS APPLIED — staged, to 46 entries. The tooling cost was measured FIRST, against a stop criterion
the dispatch declared before the diff existed (#22). Every figure below is a field of a generated
artifact or of the register's own data (register entry D-431, ruled the same day):
`tools/audit/decisions/backbone_decisions.json` → `section_home_criterion`, and the summary
`gen_section_homes.py` prints.**

## The criterion as ruled, and what it does to the two before it

*A home is a **SECTION** of a document. It is admitted when a user-ratified surface delegates a
stated concern to that section **by name**, and that section **states rules** rather than recording
findings.* Homed at `CLAUDE.md`, the decisions-register section, as rule **(h)**; register entry
**D-430**.

**It SUBSUMES the two earlier criteria rather than falsifying them, and both are recorded that way**
— the delegation-specificity criterion of this row's first measurement note, and the document-kind
test of its second. Each was a proxy for this test; each produced the evidence that located its own
error. The specificity criterion's residue turned out to be its own third clause used as a
judgment, which the kind test then removed; the kind test's residue turned out to be the structural
finding §6 of that note reported and could not answer — **kind is a property of the DOCUMENT, and a
delegation points at a SECTION.** A reader now meets one derivation instead of three rival tests.
Rule (g)'s guard is untouched: the delegation confers, and only the user writes a delegation into
`ARCHITECTURE.md`.

## 1. The tooling cost, measured before anything changed — and the verdict is PROCEED

The dispatch declared the stop before the diff: PROCEED if the change stays inside the home field's
shape plus the generator and checker reading it and **no existing anchor needs re-aiming**; STOP if
it reaches the anchor or drift machinery, or if the quote-verification path must change. What was
established, by reading all four surfaces:

| surface | what it assumes about the home field | does section granularity reach it? |
|---|---|---|
| the backbone entry shape | `home` is `"<path>:<line>"` or `"<path>:<line>-<line>"` | **No** — the section is an ADDITIVE field; `home` is untouched |
| `gen_decisions_register.py`, index render | splits `home` on `:` and prints part 0 | No — it gains a rendered field |
| `gen_decisions_register.py`, entry render + `--check` | prints `home` whole; `--check` byte-diffs the emitted files | No — a regeneration, not a re-anchoring |
| `gen_cluster_dispositions.py --verify` | splits `home` on `:`, resolves part 0 as a path and part 1 as the line the verbatim must START at | **No** — this is the quote-verification and drift path, and it never sees the new field |
| the drift report | the same parse, printing `LINE DRIFT` per entry | No — byte-unchanged |

**The embedded form would have failed the stop and was not built.** Writing the section into `home`
itself (`cowork_score_census.md §8c:187`) makes `parts[0]` stop being a path, so
`verify_backbone` reports "home file does not exist" for every changed entry — it reaches the
quote-verification path exactly as the stop describes. The additive form was chosen for that
reason and for no other.

## 2. What was applied, and the scope note that goes with it

**46 entries across 5 documents**, the staged population the ruling names: the ambiguous population
of the kind test, plus the `cowork_score_census.md` entries. Everything else keeps its
document-level home and migrates as its document is next touched — recorded in the register's own
scope note (`header.scope.home_granularity`) so a reader knows the field is mixed and why, and
knows that a missing section line is **not** a claim that the whole document is the home.

**Nothing is transcribed.** Each entry's section is DERIVED from its home document's own headings
and its own cited line by `tools/audit/decisions/gen_section_homes.py`, which also refuses to run if
re-serializing the untouched backbone would reformat it, and whose `--check` re-derives every one —
so a heading moving inside a home document cannot leave a stale section behind. The only authored
input is the `section_home_criterion` block: per document, whether clauses (a)+(b) hold, which
sections the delegations name, and whether each names a rule-stating section — each judgment
carrying the citation it was made from.

## 3. ★ What the ruling decides, and what it does NOT — the honest split

**It decides the census, which is the case it was ruled on.** `cowork_score_census.md`'s clauses
(a)+(b) are settled twice over (`ARCHITECTURE.md:319` inside a user-ratified passage, and
`CLAUDE.md:388`), so the section unit is decisive there: **11 of the 17 entries are homed in §8c —
the section `ARCHITECTURE.md:319` delegates to by name — and move to `contract-home`; the other 6
sit in §1, §3, §4, §8 and §8b, which no delegation names, and stay `gap`.** Under the document-kind
test all 17 were excluded together, which is exactly the error the census case was raised to show.

**It decides none of the 29 ambiguous entries, and the reason is worth stating plainly: their
ambiguity was never in the clause this ruling changed.** Phase 1m's own §4 said so —
26 of the 29 turn on clause (b), *does a bare citation of the form "Full spec: `X`" delegate, or
only cite?* — and this ruling changed the criterion's UNIT, not clause (b). Concretely:

- `cowork_layer5_function_design.md` (8) and `cowork_prefit_gates.md` (5): the delegations name **no
  section at all**, so the section unit has nothing to test. Unchanged.
- `cowork_voiceleading_axis_design.md` (13) and `cowork_notation_adoption_increment.md` (3): the
  delegations **do** name sections (`§0`/`§5.3`; `§7`+`§10` and `§2`), and those sections are
  recorded in the criterion block for the ruling that settles clause (b) — but whether "build home"
  or "analysis in" delegates or merely cites is still open, so nothing is decided today. Their
  section homes are recorded and their classes are untouched.

**This is a confirmed advance prediction, not a new discovery**: phase 1m registered that the
dominant ambiguity lives in clause (b) and would survive any change to clause (c). It did.

## 4. One observation, reported rather than smoothed over

**§8c is not homogeneous, and the criterion's unit cannot see that.** The section states three
binding rules — the Stage-5 fitting-pool licence constraint the delegation actually points at, the
intake rule, and the supersession decision protocol — and it also holds the needs-vector TABLE,
whose rows record findings; 9 of the 11 entries now admitted are rows of that table. The criterion
admits the section, so it admits them.

The alternative reading is stated so it is not lost: *"delegates a **stated concern** to that
section"* could be read as scoping the admission to the delegated CONCERN, in which case a decision
sitting in a delegated section but on a different concern would not be admitted. That reading is
**not** the one applied, and the ground is precedent rather than preference: through three waves,
"for a stated concern" has been used as a test of DELEGATION QUALITY — naming a target versus a
bare mention — and never as a per-decision scope limit; `ARCHITECTURE.md:1329` delegates "this
layer" and admits all 16 of that document's entries whatever sub-concern each covers. Applying the
narrow reading here would silently change what the clause has meant since it was written. **It is
recorded, not chosen, and it is the user's if it matters.**

*Nothing else in the register moved. The 88 entries outside the staged population carry the classes
and the document-level homes they carried before this note.*

---

**★ Dated note — 2026-08-03 (CC, phase 1p, Tasks 2 and 3). THE USER RULED THE REMAINING CLAUSE — what
counts as a DELEGATION — and the check ordered before applying it came back a STOP. The ruling is
HOMED (`CLAUDE.md` rule (i), register entry D-432); NOTHING IS APPLIED. Every figure is a field of
`tools/audit/decisions/phase1p_delegation_bar.json`, generated by
`tools/audit/decisions/gen_phase1p_delegation_bar.py` (D-431).**

## 1. The bar, and what it settles

The bar grades a delegation by its FORM. **ADMITTED:** an explicit delegation clause, or a named home
with sections. **NOT ADMITTED:** a bare appended citation, or a provenance attribution — a naming
inside a list of citations, or a parenthetical recording where something was ratified. Its defense is
evidence and not judgment, and both halves are quoted from the file rather than transcribed
(`the_defense`): `ARCHITECTURE.md`'s *"Full spec:"* line and, immediately beneath it, a delegation
clause naming its target AND its sections. The canonical document distinguishes the two acts in
adjacent lines.

**This is the clause the three previous criteria never touched**, and the one phase 1m's own §4
identified as the dominant remaining source of ambiguity. It decides the 26 entries that turned on it.

## 2. The check the dispatch ordered — and it is a STOP

The dispatch predicted that the bar would change no currently-admitted verdict, "the admitted set
having been reached on explicit delegation clauses", and named a different answer a #13 surprise.
**The prediction is refuted.** `pre_apply_check` carries the verdict and the three distinct movements,
kept apart because they have three different causes: documents the BAR excludes; documents named in
NO user-ratified surface at all, which fail the earlier clause and have since phase 1l; and the
section-level consequence the already-ratified D-430 produces once clause (b) is settled.

**The diagnosis, which is the material finding: the `contract-home` class at HEAD was not produced by
any criterion now in force.** It is phase 1i's, under the original fifth-home-case test. All three
criteria ruled since were measured and deliberately not applied, except phase 1n's staged 46 — the
three notes above this one are that record. So the dispatch's premise was a claim about the register
the register does not support. Full disposition, and the three shapes the user is being asked to
choose between, at [[OI-291]].

## 3. W3 — a shelved section can be a home, and its one application does not follow

**Recorded as ruled** (`CLAUDE.md` rule (h), register entry **D-433**): shelving is a STATUS, not a
KIND; a section whose rules are shelved still states rules, and the register records shelvings with
their evidence, so a shelved decision needs a home exactly as a live one does (#12).

**Its one candidate does not move, for a different reason.** `cowork_joint_key_chord_design.md` was
ambiguous at phase 1l only because it is shelved; that obstacle is gone. But its delegation —
`cowork_engage_arc_plan.md:44`, a parenthetical naming inside a bulleted list of arcs, naming no
section — is a form the bar does not admit, while arc #9 and arc #11 in the same list DO name their
target's sections. So the four entries stay `gap` on clause (b), not on shelving.

**The phrase *"stable enough to be cited"* is RETIRED.** Its clause is superseded by D-430 and now by
D-433. Its surviving occurrences — in the first measurement note above, in the OI-281 index row's
recitation of that measurement, in `STATUS.md`'s phase-1l entry, in `gen_phase1m_measurements.py`'s
docstring, and inside D-430's own `supersedes` text — are **quotations of a superseded clause inside
dated historical records**, and they stay: rewriting a dated note to remove a phrase it correctly
reports would falsify the record (#12). What is retired is its use as a live test, and that is stated
where the live rule is.

## 4. W4 — the arc plan's three entries, and a collision reported rather than resolved

The dispatch directs that `cowork_engage_arc_plan.md`'s three entries be ADMITTED, the document-type
test that excluded them at phase 1i no longer existing, and instructs: *"Verify the delegation at
`CLAUDE.md` names it by name for a stated concern before admitting; if it does not, report that
instead of admitting."*

**It does name it by name for a stated concern.** `CLAUDE.md:129-130` names the document for the
MEASURE-BEFORE-BUILD gate. On the dispatch's own stated test, it admits.

**And the bar excludes it.** The naming is inside a list — *"Companion standing rules elsewhere: the
⛔ TOTAL UNIFICATION rule (`cowork_handoff.md`), the MEASURE-BEFORE-BUILD gate
(`cowork_engage_arc_plan.md`, now the middle stage of the #17 funnel), and the doc-sync, layer, and
gate policies below"* — and a parenthetical. That is the bar's second excluded form, in both its
halves.

**The two rulings of the same wave disagree, and neither is a session's to overrule.** The entries are
NOT moved. The collision carries a second question with it, stated because it is not obvious: the arc
plan is one of the three surfaces the criterion USES as a source of delegations, and the bar would
exclude it as a delegation TARGET. Whether that is intended is the user's.

*Nothing in this note was applied. The 134 entries carry the classes they carried before it.*
