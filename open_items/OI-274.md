# OI-274 — `docs/scoring_model.md` describes the legacy scorer as WHAT RUNS, and CLAUDE.md makes it a mandatory read

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10) / DT-12 (stale which-code-is-running statement). Found by the
phase-1g triage wave, 2026-08-02, reading `docs/scoring_model.md` IN FULL. This is the
[[OI-232]] / [[OI-265]] class, one document over. Both sides are quoted; nothing is fixed here.**

## The finding

`docs/scoring_model.md` §1 opens, verbatim (`:32-34`):

> `RuleBasedChordAnalyzer::analyzeChord` is a **bottom-up** vertical-sonority
> scorer: pitch-class evidence → chord identity. It performs no harmonic-function
> reasoning (that lives in Phase E — secondary dominants, tonicization, etc.).

The whole document is written in that present tense — the pipeline "is", the gates "run", the
oracle "computes". Nowhere does it say that the scorer it describes is **dormant on both
production surfaces**: the joint estimator is the production inference layer on the batch and
corpus surface since 2026-07-26 (`CLAUDE.md` gate block (A), register entry **D-005**) and on the
notation surface since 2026-07-27 (**D-010**), with the legacy path compiled and selected only by
an explicit `useJointNotationRecord = false`.

That is exactly the defect [[OI-232]] found eight times in `ARCHITECTURE.md` and [[OI-265]] found
a ninth time in its Layer-4 body. Both were resolved by adding a **scoping sentence** — the
Layer-3 form, "the description below remains accurate for the dormant decoder … it is not a
description of what runs". `docs/scoring_model.md` has no such sentence.

## Why it matters more here than in an ordinary stale document

`CLAUDE.md` makes this document a **mandatory session-start read** for a whole class of session:

> **Read `docs/scoring_model.md` at the start of any session that touches scoring
> logic in `chordanalyzer.cpp`** — this includes adding or modifying templates,
> bonuses, guards, gates, score matrices, or post-scoring passes.

So a session directed here is directed to a document that presents a dormant scorer as the
system's scoring pipeline, and no other mandatory read corrects it: `CLAUDE.md`'s own scoring
sections describe the same scorer in the same tense. The one place the truth is stated —
`ARCHITECTURE.md`'s corrected Layer-3 and Layer-4 blocks — is not part of that session's
mandated reading.

## Two smaller instances of the same class, found in the same read

- **The footer date is five weeks stale.** `docs/scoring_model.md` closes with *"Last updated:
  2026-06-12 — Stage 3.1"*, while the body carries the Stage-3.3 oracle migration (§11), the
  Stage-5 gate retirements of 2026-07-05 (§6), the OI-168 fix of 2026-07-14 and the OI-170
  measurement of 2026-07-16 (§4). A reader who trusts the footer under-reads the document by
  five weeks of change.
- **Two design documents carry an UNCOMMITTED banner while being tracked files.**
  `docs/key_path_design.md:3-6` and `docs/precision_metric_design.md:3-5` both open
  *"DRAFT — UNCOMMITTED"*, and `docs/key_path_design.md` adds *"this document is **HELD**
  (`git add` permitted, `git commit` NOT) until a ratification addendum lands"*. Both are
  committed files at HEAD. Recorded together because the fix is the same act — a banner that
  states the document's true standing.

## What is NOT in doubt

The scoring content itself. Nothing in this row says a term, a gate or a constant is described
wrongly; §8's twelve constraints are all registered (**D-214**…**D-224**, **D-061**) and the
phase-1g read added no correction to any of them. What is wrong is the frame: the document does
not say which of the two chord paths it is describing.

## Disposition

For a later doc-sync act, at the document's next touch: the [[OI-232]]/[[OI-265]] scoping
sentence at the top of §1, the footer re-stamped, and the two UNCOMMITTED banners corrected.
Whether `CLAUDE.md`'s mandatory-read instruction should also name the joint estimator's own
specification is a **governing-document** question and therefore the user's. **No edit is made
here** — this pass is read-only on every specification and governing document.

*Provenance: CC-found 2026-08-02 at `cc_instruction_phase1g_triage.md`, Task 2, reading
`docs/scoring_model.md` in full. Cross-ref [[OI-232]] (the eight `ARCHITECTURE.md` instances,
resolved), [[OI-265]] (the ninth, resolved), [[OI-183]] (the scoring-model symbol-coverage gap,
the other open row on this document), [[OI-207]] (the audit this serves).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

## ★ DATED NOTE — 2026-08-14 (CC, `cc_instruction_scoring_model_pass.md` Tasks 2–3; document pass `b366d44947`)

*Status of record is the INDEX row, which this note does not carry. **The row does NOT flip** — see
"what remains" below.*

**The finding's main half is done.** `docs/scoring_model.md` §1 now opens with the scoping sentence
this row's Disposition asked for, in the [[OI-232]]/[[OI-265]] form: everything from §1 on remains
accurate for the legacy vertical scorer it describes and is **not** a description of the code that
produces a committed chord today, the production inference layer being the joint estimator
(**D-005** batch/corpus, **D-010** notation), with this path compiled and selected only by an
explicit `useJointNotationRecord = false`. The sentence also says that **§8's constraints bind
regardless of that dormancy**, which is the one thing a scoping sentence could otherwise be misread
as weakening.

**The document's top banner carried a sentence this act made false**, and it was corrected in the
same commit: it said the body's present tense is NOT corrected by the banner and that this row stays
open for it. Former wording preserved in place (#12); the banner now says the present tense is
scoped at §1 and that this row stays open on its second half only.

**Both smaller instances are done.** The **footer** is re-stamped, and it names — at the dates the
body itself gives them — every act the stale "2026-06-12 — Stage 3.1" stamp omitted. The **two
UNCOMMITTED banners** are corrected: `docs/key_path_design.md` (its banner AND its closing line,
which repeated the claim — correcting only the banner would have left the falsity in the document)
and `docs/precision_metric_design.md`. **Trackedness was established at the git object by explicit
hash, not asserted**, which is what this row's own claim rested on.

**The banner correction is deliberately MINIMAL, and this is the reason.** [[OI-317]] is a separate
open row whose subject is exactly `docs/key_path_design.md`'s banner, and its status cell reserves
what the banner should BECOME as a **filing decision** — retained as a superseded design record, or
re-bannered. That decision was not taken here. Only the false statements about each file's own
standing are struck; the DRAFT markings stand, and nothing in either banner now claims that a
ratification gate has or has not been passed. **[[OI-317]] therefore does not close**, and its own
cell records what was struck and what remains.

**What remains, and why this row stays open.** Its second half: whether `CLAUDE.md`'s mandatory-read
instruction should also name the joint estimator's own specification. That is a governing-document
question and therefore the user's, and it was explicitly not performed. *A pointer to that
specification was added inside `docs/scoring_model.md` §1 itself, marked in the document as a
pointer that does not amend `CLAUDE.md` — so a reader of the scoring document is not left without
the primary source while the question stands.*

**One observation for whoever takes that decision, declared and not acted on.** `CLAUDE.md`'s
scoring-model section already carries a clause of the same FORM for the production layer — the
user's Ruling 64 of 2026-08-11, directing that the joint estimator's section of `ARCHITECTURE.md` be
read at the start of any session touching the joint estimator's behaviour. **Whether that discharges
this row's second half is not decided here**: it adds a parallel conditional read rather than
amending the scoring-model instruction this row is about, and reading it either way is the user's
act, not a session's.
