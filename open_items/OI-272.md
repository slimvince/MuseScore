# OI-272 — Fourteen standing decisions are recorded ONLY in the session-handoff ARCHIVE — a surface explicitly outside the session-start read

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10) / documentation gap. Found by the phase-1e second-partition wave,
2026-08-02. [[OI-266]]'s class one level out: that row found six standing rules on the LIVE session
handoff; these are on the handoff's ARCHIVE, which `STATUS.md:5-6` and `CLAUDE.md` both declare
reference-only and NOT part of the session-start read.**

## The finding

Reading `cowork_handoff_archive.md` in full entered **fourteen** decisions the register did not
carry (**D-286…D-299**). Every one carries `nonspec_kind: "unhomed"` — their only recorded home is
the archive. That raises the register's tracking-surface-only count from 0 to 14, which is the same
shape [[OI-266]] reported when it raised it 0 → 6: the count was true of the entries the homing
passes knew about, not of the repository.

The fourteen, by what they are:

| kind | entries |
|---|---|
| a SHELVING with evidence | **D-286** whole-score interactive analysis · **D-287** key-as-distribution · **D-288** beam widening |
| a FALSIFICATION | **D-290** the key-agnostic local cadence approach at its precision ceiling |
| a standing PROHIBITION | **D-291** do not wire the tonicization labeller and do not credit it in the measurement · **D-299** no negative-margin guard |
| a binding CONSTRAINT | **D-292** the fitting-pool licence constraint ([[OI-271]]) · **D-293** fit per idiom, never per preset · **D-294** the only ground truth is the human annotation · **D-295** zero information loss to the end user |
| a scoping CORRECTION of another decision | **D-296** read engraving yes, edit engraving no (bears on D-229) · **D-297** never-computed is not information loss (scopes D-099 / principle #12) |
| a meta-principle | **D-289** precision lives in the evidence and the labelling, not the search |
| a standing METHOD | **D-298** the layer-by-layer audit |

## Why the archive is worse than the live handoff

[[OI-240]] ruled that a handoff block tracks a session and is not a home for a standing decision.
The archive is that, plus a second remove: the live handoff is at least opened by the planning side
each session, while the archive is declared reference-only by two governing documents. A decision
recorded there is not merely mis-homed; it is placed where the reading protocol says not to look.
**This is the exact mechanism [[OI-207]] was opened to investigate**, and D-286 — the Stage-3.1b
shelving of whole-score interactive analysis — is that audit's founding case, now entered.

## What is NOT in doubt, and what is

Not in doubt: the fourteen were invisible to the register until this wave, and several bind
something live (D-292 is [[OI-271]]; D-296 sits beside the ruled D-229; D-297 scopes principle #12;
D-294's two further clauses — never call a measurement ground-truth agreement, and no
self-annotation in any measurement — are recorded nowhere else).

In doubt, and the user's: whether homing them is the right act for each. Three plainly want a
specification home (D-294 the measurement conventions, beside `CLAUDE.md` gate block (A) where
D-210/D-211/D-212 now live; D-296 beside D-229 in `ARCHITECTURE.md` §3.3; D-297 beside D-099 in
§2.15). Three are shelvings and falsifications whose value is precisely that they are *history with
evidence* — a homing act must not turn a shelving into a live specification statement. The rest are
process or product decisions whose homes are the same candidates [[OI-240]] used.

## Disposition

For the user: rule per kind, not per entry. Then the homing acts follow [[OI-237]]'s and
[[OI-240]]'s pattern — the rule in the specification's own voice, with its defense, citing the
ratifying event, former home preserved in provenance (#12). **No fix, no design, no specification
edit is made here.** This row is the sibling of [[OI-268]] and should be ruled with it.

*Provenance: CC-found 2026-08-02 at `cc_instruction_phase1e_second_partition.md`, Task 1; the
fourteen entries land in the register in the same commit that records the finding (the register's
rule (c)). Cross-ref [[OI-237]] (sixteen with no layer-specification home, resolved), [[OI-240]]
(twelve on a tracking surface, resolved), [[OI-266]] (six on the live handoff, resolved),
[[OI-268]] (twenty-two on ratified design surfaces, open), [[OI-208]] ruling 2.*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

---

**★ Dated note — 2026-08-02 (CC, the phase-1f final-partition wave): the archive population is now
COMPLETE, and it is larger than fourteen — the second archive adds THIRTEEN more, one of which is
a live edit to MuseScore's own code.** `STATUS_ARCHIVE.md` is now read in full (lines 1–118 and
301–929 by phase 1e; lines 119–300 and 930–3,861 by this wave), so both archives are read and this
row's population no longer grows. Thirteen of the sixteen entries this wave made are homed in
`STATUS_ARCHIVE.md` and carry `nonspec_kind: "unhomed"` — **D-300**…**D-311** and **D-315** —
raising the register's tracking-surface-only count from 14 to 27.

**The kinds, because the ruling wanted is per KIND and this archive's mix differs from the
handoff's.** Three are standing PROHIBITIONS with measured evidence (**D-300**, **D-301** — two
correction rules deferred with an explicit *do not retry*; **D-302** — no further local scoring fix
for inversions). Two are standing POLICIES about what the analyzer emits and what it may read
(**D-304**, **D-305**). One is a DEFERRAL that also constrains the eventual shape (**D-303** —
non-chord-tone detection must be chord identification that knows about non-chord tones, never
stripping afterwards). One is a user RULING on a shipped default (**D-306**). Four are decisions
about how the project is run (**D-307**, **D-308**, **D-309**, **D-311**). One is a measured
conclusion that bounds a whole style's claims (**D-310**). **And one is not a statement at all but
an ACT: D-315, a one-line edit to `src/engraving/dom/chordlist.cpp` that is live at HEAD, absent
from `CLAUDE.md`'s local-patches section, and carries no distribution disposition — rowed
separately at [[OI-273]].**

**Two things this makes concrete for the ruling.** First, the archive class is not marginal:
`docs/scoring_model.md` §8 is the standing home for scoring dead ends and mentions none of the
three prohibitions, so a reader looking for "what has been tried and closed" in the right place
would not find them. Second, a homing act here is *not* uniform — **D-315** cannot be homed by
writing a sentence into a specification, because what it needs is a governing-document entry plus
a distribution disposition plus a ratification, three acts with three different owners. The ruling
per kind is what makes that separable.

*Cross-ref [[OI-207]]'s phase-1f note (the wave's measured scope), [[OI-268]] (the design-surface
siblings, to be ruled together with this row), [[OI-273]] (the MuseScore-core edit).*

---

**★ Dated note — 2026-08-02 (CC, the phase-1g triage wave): A CORRECTION OF THIS ROW'S OWN
PREMISE — at least THREE of the fourteen are NOT archive-only. Their fuller record lives in
design documents, and for one of them the archive entry SAYS SO.**

This row states that of D-286…D-299 "every one carries `nonspec_kind: "unhomed"` — their only
recorded home is the archive." Reading five design documents in full shows that is not true of
three of them:

- **D-286** — whole-score interactive analysis SHELVED WITH EVIDENCE, **the founding case of
  [[OI-207]] itself**. Its full record, with the measurement and a standing prohibition, is
  `docs/decoder_design.md`'s dated **§8 amendment** ("Q1 RE-DECIDED → bounded-window cache";
  whole-score "changed the displayed P3 chord on 32–40 % of ticks … combined root verdict 59/41
  in the WINDOW path's favour, Mozart 35/65 against whole-score"; and, closing the section, "Do
  not re-attempt whole-score P3 before that decision"). The evidence itself is a second document,
  `docs/p3_granularity_ab_3_1b.md`, committed as Stage-5 input.
- **D-287** — key-as-distribution SHELVED. Its full record is `docs/redesign_plan.md` "Step 3 —
  Key-as-distribution ⛔ SHELVED (premise obsolete)", with the investigation, the two additional
  findings and the named cleanup. **The archive entry itself points there** — its own verbatim
  ends "See `docs/redesign_plan.md` §Step 3."
- **D-288** — beam widening SHELVED. `docs/beam_widening_design.md` was banner-shelved and
  retained for its derivation, and `docs/decoder_design.md` §11 carries the dated erratum that
  applies it.

**What the correction changes, and what it does not.** It does **not** weaken the row: the three
were still invisible to the register until the archives were read, and the other eleven are
unaffected by this note. What it changes is the **diagnosis**. The failure was not "the decision
was buried in an archive"; for the audit's own founding case the decision was recorded three
times, twice in design documents, one of them the RATIFIED decoder design. What none of the three
homes is, is **a session-start read** — and that is the property the remedy has to attack. A
homing act that moves a shelving out of an archive and into a design document would, for these
three, move it to where it already is.

**Consequence for the ruling this row wants.** The per-kind ruling should distinguish "recorded
nowhere a reader is directed" from "recorded in several places, none of them read at session
start", because the second is the case for the founding instance and the remedy differs. It also
bears on [[OI-268]]: the two rows' populations overlap more than either row assumed, which is a
further reason to rule them together.

*Cross-ref [[OI-207]]'s phase-1g note, [[OI-268]]'s phase-1g note (the design-surface count 25 →
51), [[OI-210]] (the extent question D-286's evidence still serves, OPEN).*

---

**Dated note — 2026-08-02 (CC, the phase-1h continuation wave): TWO more archive-homed decisions
are found ALSO living in a design document — and one of the archive entries says so in its own
verbatim.** The phase-1g note corrected this row's premise by finding at least three of the
archive-homed decisions recorded a second time in a design document. Reading five more LIVE-SPEC
documents in full adds two instances, collected as evidence only — no home is moved, because
moving one would change the tracking-surface-only count this row's diagnosis rests on, and that
would disturb the premise of a ruling the user has not yet made.

- **D-292 — the fitting-pool licence constraint.** Registered home `cowork_handoff_archive.md:2478`,
  marked tracking-surface-only. Its full statement, in far more detail than the archive carries, is
  at `cowork_score_census.md:239-253` — the per-licence-class pool declaration, the
  objective-versus-validation split the fitter's design must state, the measurement-is-not-shipped-
  parameters carve-out, and the roadmap ride. **The archive entry itself names that home:** its
  verbatim opens "census §8c block". So this is not a decision that lived only in an archive; it is
  a decision whose PRIMARY record is a design document, indexed from an archive, with the register
  pointing at the index rather than at the record.
- **D-308 — a newly acquired corpus enters as research material.** Registered home
  `STATUS_ARCHIVE.md:250`, marked tracking-surface-only. The same rule is stated at
  `cowork_score_census.md:81-83`, in the decision-tier block: research-tier on entry, and promotion
  to any gate is its own ratified re-baseline event.
- **A near-instance worth recording, because it splits differently.** **D-306** (the tonality
  layer's backward re-reading stays switched off) is homed at `STATUS_ARCHIVE.md:232`, and
  `cowork_layer3_keymode_design.md:174-176` states the SAME FACT as as-built — the parameter, its
  default, and that the production path therefore never reaches back. What the specification does
  NOT carry is the RULING: that the user decided on 2026-07-02 to keep it off, and that turning it
  on reopens only on a named evidence follow-up. Here the archive holds something the design
  document genuinely lacks, which is the opposite of the two cases above and is worth the user's
  attention when ruling per KIND: a state fact and a ruling about that state can live apart.

**Running tally of the overlap this row's premise did not anticipate: five instances** — the three
the phase-1g note found (D-286 among them, the audit's founding case) plus D-292 and D-308 here,
with D-306 as the partial case. **This pass did not rule and moved no home.** *Cross-ref
[[OI-207]]'s phase-1h note, [[OI-268]]'s phase-1h note (58 more decisions on the design-document
surface), [[OI-276]] (the staleness of some of those design documents, which bears on the same
choice).*


---

**Dated note — 2026-08-02 (Cowork): RULED by the user — the per-kind scheme adopted as presented.** Live constraints/policies are homed into the owning specification (an `ARCHITECTURE.md` section or an OI-268 contract home); shelvings, falsifications and dead ends NEVER become live specification statements — their home is the register plus the evidence document, with at most a one-line tried-and-closed pointer in the owning specification; process rules go to `CLAUDE.md`/the audit protocol; measurement conventions to the gate block. The discoverability remedy is the pointer FROM the surface where a reader works (attacking the recorded-three-times-none-a-session-start-read property), never more copies. The row stays OPEN only for the delivery: the homing/pointer pass in the next dispatch.

---

**Dated note — 2026-08-02 (CC, the phase-1i delivery acts): THE POINTER HALF IS DELIVERED; THE
HOMING HALF IS NOT, AND THIS DISPATCH COULD NOT DELIVER IT.**

The ruling below has two halves. The **pointer half** is done, as four one-line insertions in
`ARCHITECTURE.md` — never a copy, never a new ruling:

| where | what it says | serves |
|---|---|---|
| Layer 3 (`ARCHITECTURE.md:1275`) | the ratified contract for this layer is `cowork_layer3_keymode_design.md` (SIGNED, user, 2026-06-22) — D-343…D-358 | the missing delegation ([[OI-268]] rule (g)) |
| Layer 3, beside it (`ARCHITECTURE.md:1277`) | tried and closed on this layer: **D-287** (key-as-distribution, shelved), **D-290** (the key-agnostic local cadence approach, falsified) | this row's dead-end class |
| Layer 4 (`ARCHITECTURE.md:1342`) | the ratified contract for this layer is `cowork_layer4_chordsymbol_design.md` (SIGNED, user, 2026-06-24) — D-329…D-334 | the missing delegation |
| Layer 4, beside it (`ARCHITECTURE.md:1344`) | tried and closed on the chord layer: **D-215, D-299, D-300, D-301, D-302, D-317, D-318, D-319, D-320, D-328** | this row's dead-end class |
| the joint estimator's standing rules, after (b) (`ARCHITECTURE.md:282`) | the fitting event's own design contract is `cowork_stage5_fitter_design.md` (SIGNED, user, 2026-07-04) — D-312…D-314 | the missing delegation |
| the joint estimator's standing rules, after (c) (`ARCHITECTURE.md:296`) | tried and closed on the search: **D-288** (beam widening, shelved), **D-328** (a wider search over the same scoring, refuted), **D-278** (the joint key-and-chord step over the two legacy decoders, shelved) | this row's dead-end class |

Three of this row's eight shelvings/falsifications/dead ends are deliberately **not** pointed at,
each for a stated reason. **D-286** (whole-score interactive analysis, shelved with evidence) is
NOT written as "tried and closed" anywhere, because [[OI-210]] — the analysis-extent question — is
OPEN and reads that shelving as live evidence; a tried-and-closed line would misdescribe an
undecided question. **D-291** (the tonicization labeller is not wired AND the measurement is not
changed to credit it) is not a dead end at all in its second half: the measurement prohibition is a
live convention, and this pass had no licence to write a convention into the gate block. **D-302's
sibling class in `docs/scoring_model.md` §8** — which the phase-1f note above names as the place a
reader looks for "what has been tried and closed" on scoring — was left alone: that document is a
mandatory session-start read for scoring sessions with its own sync rule in `CLAUDE.md`, and
editing it was outside this dispatch's stated scope.

**Why the homing half could not be delivered here, stated plainly rather than worked around.** The
ruling's first clause requires live constraints and policies to be *homed into the owning
specification* — that is, their rule text written into a section of `ARCHITECTURE.md` with its
defense. The dispatch that executes this ruling carries a hard stop permitting `ARCHITECTURE.md`
edits **only** as the pointer class, "never content copies, never new rulings". Those two
instructions cannot both be satisfied, so the pointer half was done and the homing half was left
undone and declared, rather than a homing act being smuggled in as a pointer. What remains owed:
the ~12 live constraints and policies (**D-292**, **D-293**, **D-294**, **D-295**, **D-296**,
**D-297**, **D-303**, **D-304**, **D-305**, **D-306**, **D-309**, **D-310**) and the four process
rules (**D-298**, **D-307**, **D-308**, **D-311**) — several of which this row's own text already
names a home for (D-294 beside the gate block's measurement conventions; D-296 beside D-229 in
§3.3; D-297 beside D-099 in §2.15). **D-315** needs no homing act here: it was resolved through
[[OI-273]] and is recorded in `CLAUDE.md`'s local-patches section as **D-316**.

*Cross-ref [[OI-268]] (the fifth home case, whose pointer criterion these delegation lines satisfy),
[[OI-210]] (why D-286 is not pointed at), [[OI-207]].*

---

**★ Dated note — 2026-08-02 (CC, the phase-1j homing pass, `88fd87e9d1`): THE HOMING HALF IS
DELIVERED. THE ROW CLOSES.**

The phase-1i note above declared this half undone and said why: writing a live constraint's rule
text into a specification is a content edit, and that dispatch's hard stop permitted
`ARCHITECTURE.md` edits only as the pointer class. Phase 1j licenses the homing class explicitly, so
the sixteen owed acts are made. None is a new decision; each is an already-ratified rule written
where a reader of the owning specification will meet it, in that specification's own voice, with its
defense and its ratifying event, and with the former home preserved in the register's provenance
(#12).

**The twelve live constraints and policies.**

| entry | written into | as |
|---|---|---|
| **D-292** the fitting-pool licence constraint | `ARCHITECTURE.md:308-321` | new standing rule **(e)** of the joint estimator's rules |
| **D-293** fit per idiom, never per preset | `ARCHITECTURE.md:323-330` | new standing rule **(f)** |
| **D-294** only the human annotation is ground truth | `CLAUDE.md:395-409` | the FIRST of gate block (A)'s grading conventions |
| **D-295** zero information loss to the end user | `ARCHITECTURE.md:6266-6273` | the governing requirement at the head of §12 |
| **D-296** read engraving yes, edit engraving no | `ARCHITECTURE.md:1162-1172` | clause 4 of the MuseScore-Dependency Rule, §3.3 |
| **D-297** never-computed is not information loss | `ARCHITECTURE.md:963-972` | the boundary clause of §2.15's negative-evidence contract |
| **D-303** non-chord-tone detection deferred, shape constrained | `ARCHITECTURE.md:1409-1416` | a deferred capability in the Layer-4 section |
| **D-304** the analysis emits its fullest reading | `ARCHITECTURE.md:983-993` | a new §2.15 cross-cutting contract bullet |
| **D-305** the harmony-annotation ban is content-based | `ARCHITECTURE.md:2635-2644` | beside D-066 in §4.2 |
| **D-306** the backward re-reading stays off | `ARCHITECTURE.md:1333-1341` | the Layer-3 section |
| **D-309** a badly-handled corpus stays on the roadmap | `docs/score_inventory.md:357-367` | hard rule 7 |
| **D-310** jazz is not measurable on the corpora held | `ARCHITECTURE.md:2649-2662` | §4.2's jazz-validation block, beneath the measurement that is its defense |

**The four process rules.** **D-298** (the layer-by-layer audit) → `cowork_audit_protocol.md:117-127`
as **P10**, beside P8 and P9, which are the method this rule says WHEN to apply. **D-307** (cite code
by function or section anchor) → `cowork_design_doc_template.md:93-101`, beside the
implementation/test locator rule it constrains — homed THERE rather than in `CLAUDE.md`, because
`CLAUDE.md`'s own Conventions entry names that file the ONE home for the writing standards (#6), and
the phase-1f entry recorded the gap precisely as the locator rule stating no constraint on the
locator's form. **D-308** (a new corpus enters as research material) → `CLAUDE.md:381-388`, beside the
re-baseline discipline it bounds. **D-311** (the file split is last) → **home moved with NO text
written**: the rule already stands verbatim in `cowork_engage_arc_plan.md:126` (RATIFIED by the user,
2026-07-07 — "the standing reference for the order of work"), whose Stage 4 states it and why. Writing
it a second time would have been the copy the ruling forbids. The arc plan is not a contract home (a
plan, not a contract for a layer — the phase-1i criterion), so the classification stays `gap`; what
changes is that the decision is no longer recorded ONLY on a tracking surface.

**Two judgments made in delivering this, both stated rather than absorbed.**

1. **D-292 got a homed restatement, not a pointer.** This row's own ruling permits a pointer where the
   proper home is a contract document meeting the fifth-home-case criteria. `cowork_score_census.md`
   §8c holds the constraint's detailed block, but the census FAILS the criteria — its banner reads
   "v1 DELIVERED (Cowork, 2026-07-02); for user disposition", and the phase-1j transitive-authority
   search found no user-ratified surface delegating to it (its only mention from a user-signed surface
   is a terms-table gloss at `cowork_voiceleading_axis_design.md:81-82`, which defines a phrase and
   delegates nothing). So the rule was written out, and the homed text cites the census block for the
   per-licence-class detail rather than standing on it.
2. **The homed D-292 carries the OI-271 verification's OPEN state, not a resolution.** The rule as
   written says in terms that which licence class the 326-chorale ground truth falls in is NOT
   settled — the NC reading refuted, CC-BY-SA not established — because homing a constraint must not
   quietly convert an open question into a specification statement.

**What this note does NOT claim.** It does not touch the shelvings, falsifications and dead ends: per
the ruling those never become live specification statements, and their pointer half landed in phase
1i. D-315 needed no homing act here — it was resolved through [[OI-273]] and is recorded in
`CLAUDE.md`'s local-patches section as D-316.

*Cross-ref [[OI-268]] (the fifth home case and its transitive-authority refinement, whose Task-3
verification rode this same commit), [[OI-271]] (the licence-class question D-292's homed text
carries as open), [[OI-207]].*
