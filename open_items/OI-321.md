# OI-321 — `docs/unified_analysis_pipeline.md` states three things as current that are false at HEAD

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10). Found 2026-08-04 (CC, READ WAVE 2,
`cc_instruction_reads_2.md`) reading `docs/unified_analysis_pipeline.md` in full — the wave's reading
order 7.**

The document's own header says *"Status: **Phases 1b–5b complete and merged.**"* and *"The design
content below is retained as the spine of the refactor"*, so a reader takes its architecture and
divergence sections as a description of what was built.

## (1) A phase the header calls deferred, which the code says was executed

The header: *"Phase 4c (`analyzeSection` move to composing module) deferred — gated on consumer
need."*

At HEAD, two comments say the opposite, from both sides of the move:

- `src/notation/internal/notationcomposingbridgehelpers.cpp:29-33` — *"Stage 2.1 (Phase 4c) moved the
  section-level ANALYSIS layer out of this file and into the composing module — analyzeSection(),
  key/mode stabilization, and cadence / pivot detection now live in
  composing/analysis/section/sectionanalyzer.{h,cpp}"*.
- `src/composing/analysis/section/sectionanalyzer.h:26-30` — *"Moved here from
  notation/internal/notationcomposingbridgehelpers.cpp in Stage 2.1 (Phase 4c) so the analysis layer
  lives in the composing module rather than the notation bridge"*.

## (2) A directory that does not exist

The architecture section: *"Location: `src/composing/analysis/pipeline/` (new directory)."* There is
no `src/composing/analysis/pipeline/` at HEAD; the entry point is
`src/composing/analysis/section/sectionanalyzer.{h,cpp}`.

## (3) A divergence described in the present tense that the code has closed

The problem section: *"**Divergence C** — annotation (P2) applies `minimumDisplayDurationBeats = 0.5`
after shared analysis; implode (P1) and tick-regional (P3) do not. Sub-beat regions appear in the
chord track and status bar but are silently dropped from Roman numeral annotation."*

At HEAD the implode path applies it: `src/notation/internal/notationimplodebridge.cpp:847-849` reads
the preference (defaulting to 0.5) and `:869` computes `passesMinimumDisplayDuration`, which gates the
chord symbol (`:1167`), the function notation (`:1185`) and the non-diatonic marker (`:1231`). So
sub-beat regions do NOT appear in the chord track.

## What this row does not settle, and deliberately

Item (3) raises a question a session may not answer. The header records divergence C as **PARKED**,
and the document's own decision rule for it — entered by this wave as register entry **D-471** — is
undischarged: keep the gate only if a measured observation run shows it reduces clutter without
suppressing correct annotations. Whether the implode path applying the gate IS that rule discharged,
or a divergence that closed by drift with no observation run, is not answerable from the document or
from the code comments, and this row does not guess. It is recorded so that whoever rules on C knows
the state changed under the parked rule.

## ★ 2026-08-09 — this row was ABSENT from every derivation until now, and it stays OPEN

**Dated note, not a resolution.** From the day it was written until 2026-08-09 this row was in **no
population at all** — neither open nor resolved but skipped. Its INDEX row carried **seven** cells:
its layer/gate column was written TWICE, the second copy holding the gating clause. The ONE index
parser splits a row on the cell separator and, where the result was not six cells, **skipped it with
no report of any kind** — so the open-row count, the TRUE-half cuts, the finish line's populations
and the apparatus declaration's candidate cut all behaved exactly as though this row did not exist,
and nothing anywhere reported a difference. The bijection check uses a looser row pattern and
passed, so the row was accounted for on one surface while invisible on another. That defect is
[[OI-362]].

**What was done, under the user's Ruling 33** of `cowork_rulings_2026_08_09_fifth_stop.md` (CC,
`cc_instruction_return_continuation_5.md` Task 1): the duplicate column was **merged into the status
cell with its whole text preserved** (#12), and the parser now **STOPS** on a row that does not
split rather than skipping it. **The row's own state did not move** — it was open by its own words
before and after; what changed is that it is now counted. It reached the apparatus declaration's
first cut for the first time, and a **GATES** verdict was authored for it on the ground this row's
own text already carried in words.

**Nothing about the three findings above is touched.** They are unchanged, uncorrected, and still
owed; the document still states three things as current that are false at HEAD.

*Provenance: CC, READ WAVE 2, 2026-08-04, dispatch `cc_instruction_reads_2.md`, Task 1.2. Cross-ref
[[OI-232]], [[OI-276]], [[OI-362]], and register entries **D-469**, **D-470**, **D-471**, **D-472**,
entered from the same read.*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
