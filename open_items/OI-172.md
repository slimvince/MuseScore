# OI-172 — applyTonicPriorToSparseChord decides committed quality from a scale degree

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-172 | ★ **`applyTonicPriorToSparseChord` DECIDES THE COMMITTED CHORD QUALITY FROM A SCALE DEGREE — a live, tonic-dependent identity write that no register row named (CC-discovered 2026-07-14 by the OI-170 mechanical sweep).** `sparsechordrefinement.cpp:182-215`, called on the region analyzer's **commit path** at `regionanalyzer.cpp:1017` (immediately before `decoder.commit(...)` at `:1026`): when the committed chord is thin (`Power`/`Suspended2`/`Suspended4`) and the region has ≤2 distinct pitch classes, it computes the root's **degree** (`diatonicDegreeForRootPc` — the tonic + the mode's own scale) and **overwrites the committed quality** with `diatonicTriadShapeForDegree`'s diatonic triad quality. **LIVE at real volume: 172 / 183 / 172 quality overwrites** (Baroque/Jazz/Default; entered 1802/2197/1813 times) — measured at a default-OFF counter over 352 scores × 3 presets (`cc_oi170_measure_report.md` §3). This is a **class-(b) genuine-tonic site the signature-mask primitive cannot replace** (a degree is tonic-relative by definition), and it is exactly the shape OI-167/OI-171 discuss for the *sibling* function `refineSparseChordQualityFromKeyContext` — except that sibling is measured **DEAD** (0 fires) while **this one is very much alive**. So the OI-167/OI-171 discussion of "the one tonic-dependent site outside the chord scorer" was aimed at the wrong function. **Declared, NOT fixed** (#8). Two questions for the design pass: (a) does a **degree**-driven quality decision belong in Layer 4 at all (the same question OI-170 raises for Gate G-E), and (b) `applyTonicPriorToSparseChord` shares OI-171's structural problem — it is on the commit path, so any "re-home to presentation" removes it from the analysis and is a behavior change, not a move. | `cc_oi170_measure_report.md` §3 (class-(b) table) + §7; `sparsechordrefinement.cpp:182-215`; `regionanalyzer.cpp:1017/1026`; the generated artifact `cc_oi170_measurements.txt` (counters `tonicPriorEntries` / `tonicPriorApplied`) | OPEN — CC-discovered 2026-07-14; needs a design-pass disposition alongside OI-170's Gate G-E question and OI-171 |
