# OI-182 — Notation-bridge decision constants on no audited surface

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-182 | Notation-bridge consumption-surface decision constants on NO audited surface (CC-discovered 2026-07-18 by the term-inventory DT-26 tree-wide sweep): `notationimplodebridge.cpp:79-80` — `kTentativeKeyExposureThreshold = 0.5` / `kAssertiveKeyExposureThreshold = 0.8`, hand-set confidence buckets deciding key exposure in the imploded output (`keyExposureBucket`, `:87-96`). In no `tools/param_manifest.json` row, in no `tools/audit/*/file_table.csv`, on no register row until now. Consumption-side (the bridge consumes analysis facts, it does not infer), so NOT an inference-correctness STOP — but it is a hand-set decision constant family outside every audit scope, the OI-87/OI-91/OI-103/OI-106 manifest-gap class extended to the bridge surface. Same file also carries `kSameChordReannotationGap` (`:661`, verified present at OI-60c) | `cc_term_inventory_report.md` §1c; `tools/term_inventory/term_inventory_summary.json` (dt26_sweep); `notationimplodebridge.cpp:79-96` | OPEN — CC-discovered 2026-07-18; disposition with the Stage-5/EG-5 manifest-extension family (or declare the bridge surface out of fitting scope explicitly) |

---

**Execution note (2026-07-27, seams-2 P4 — the notation output-surface contract §4.1 disposition).**
This family is dispositioned by the contract's presentation-gate rule (§4.1): the exposure/annotation
gates keep their FUNCTION but their INPUT becomes the record's §3.3 published gap on the record arm, and
their VALUES are declared PRESENTATION constants at their emitting layers, each cited, none pretending
to be an inference value. Every member's new declared site:

- **`kAssertiveKeyExposureGap = 1.055757`** — the record-arm assertive gate (the 0.8-family upper bound),
  declared in `src/composing/analysis/section/sectionrecordadapter.cpp` at P2b (commit `d25cf40170`),
  cited to `tools/notation_seams/exposure_constants.json`: legacy dur-rate 0.273902 / record 0.272871 /
  residual 0.001031. Sets `hasAssertiveExposure` and the bucket's upper bound on the record arm.
- **`kTentativeKeyExposureGap = 0.975911`** — the record-arm tentative gate (the 0.5-family lower bound),
  declared beside it in `sectionrecordadapter.cpp` at P4 Task 1 (commit `0e0ca379b3`), cited to the same
  artifact: legacy dur-rate 0.338501 / record 0.340837 / residual 0.002336. Sets the key-exposure
  bucket's lower bound on the record arm.
- **The 0.35 mode-suffix gate (`modeNameConfidenceThreshold` / `fallbackModeSuffix`)** — RETIRED on the
  record path under the ratified two-mode display rule (C1): the record-arm region mode is always
  Ionian/Aeolian, so `keyModeSuffix(mode) == fallbackModeSuffix(mode)` and the gate is inert (the exotic
  suffix does not exist on the record path). Its measured constant g₀.₃₅=0.649691 stays recorded in the
  artifact as record, UNWIRED by design; the published modal reading (§3.4) is the successor fact.
- **`kSameChordReannotationGap = 960` (2 quarter notes)** — RE-HOMED unchanged from block scope to a
  declared namespace-scope presentation-TIMING constant in `notationimplodebridge.cpp` (P4 Task 2, commit
  `bb9e3c9811`); same value, documented role (annotation timing, not inference); a re-home, not a re-tune.
- **The legacy `kTentativeKeyExposureThreshold = 0.5` / `kAssertiveKeyExposureThreshold = 0.8` literals** —
  now LEGACY-ARM-ONLY, re-homed to `sectionanalyzer.cpp::legacyKeyExposureBucket` (the one legacy-arm set
  site for the stored `AnalyzedRegion::keyExposureBucket`, P4 Task 1); the implode-local copies + the
  `keyExposureBucket()` / `supportsTentativeKeyExposure()` re-thresholding functions removed as dead code.
  They retire fully with the legacy analysis path at the OI-180 retirement map.

Status flipped to EXECUTED AT THE RECORD SURFACE in the INDEX (the authoritative status surface). The
family retires fully when the legacy path retires (OI-180). Provenance: dispatch
`cc_instruction_notation_p4.md`.
