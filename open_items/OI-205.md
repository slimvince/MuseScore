# OI-205 — STATUS.md and ARCHITECTURE.md have grown too large (render + human/LLM context capacity)

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync / documentation structure (#10)**

User-directed 2026-07-27 (mid-seams-2, while P7 ran): both files have re-grown past comfortable
human and LLM working size — STATUS.md through the ~10 dated entries accumulated since the
2026-07-18 doc split, ARCHITECTURE.md through the per-increment as-built blocks (the P7
consolidation helps but does not solve the trajectory).

**Half (a) — STATUS.md: mechanical.** The archive pattern exists (`STATUS_ARCHIVE.md`, the
2026-07-18 split, verbatim moves, byte-reconciled): superseded dated entries move; the lean
file keeps the current entries, the active iteration/next action, and the baseline pointers
(CLAUDE.md gate block (A) remains the ratified-baselines home). No design needed. Timing: ride
the switch dispatch's doc-sync, or the first commit after it.

**Half (b) — ARCHITECTURE.md: a design question.** The requirement is completeness AND
comprehensibility: the whole architecture reachable, no doc-sync loss (#10/#12), but the
top-level file small enough that a human or an LLM session can hold it whole. Candidate shape:
the register split's proven pattern — a lean top-level map (layers, responsibilities,
surfaces, invariants, pointers) + per-layer/per-surface satellite documents, each
independently readable, with the sync duty (#10) binding index and satellites together, and a
mechanical coherence check (the `open_items_split_check.py` living-mode precedent) if the
split shape warrants one. The decision (shape, what stays top-level, what satellites exist,
how the session-start read is defined afterward) is a Cowork-drafted decision surface for the
user — alternatives with pros/cons per the standing rule, not a sweep.

**Timing for (b), derived:** the ratified Stage-3.5 file-split lesson (CLAUDE.md's deferred
structural refactors: "deferred until the layer boundaries stabilize so we split once")
applies verbatim — the OI-180 retirement map will delete the legacy path and churn
ARCHITECTURE.md's content substantially; restructuring before it means restructuring twice.
So: AFTER the map's deletions settle, BEFORE the OI-198/OI-199/OI-200 reviews (which consume
ARCHITECTURE.md and benefit from the readable shape).

**Dated note — HALF (a) DONE (CC, 2026-07-27, at the notation switch):** the STATUS.md archive
sweep is executed. The 29 superseded dated entries (the seams P0–P6 and the whole joint-estimator
arc, 2026-07-27 back to 2026-07-16) moved VERBATIM to `STATUS_ARCHIVE.md`; the lean STATUS.md keeps
the switch entry, the P7 partition-close entry, and the older-entries pointer (2 dated entries).
The move is byte-reconciled by the NEW committed instrument `tools/status_archive_sweep_check.py`
(reads the pre-sweep STATUS.md from git, proves every moved entry byte-identical in the post
archive, none dropped/duplicated, the pre-archive preserved) — VERDICT PASS (30→2 STATUS entries,
46→75 archive entries, delta 29 == moved 29). Half (b) — the ARCHITECTURE.md restructure decision
surface — remains OPEN (post-map, pre-reviews).

*Resolution belongs in the INDEX row; dated notes may be appended here.*

## ★ LAPSE RECORD — 2026-08-12 (CC, `cc_instruction_resume_lapse_records.md` Task 3)

**What has happened to this row.** Under the user's **Ruling 66** of 2026-08-11
(`cowork_rulings_2026_08_11_fifteenth_stop.md`; register entry **D-676**, homed at `CLAUDE.md`'s
non-gating declaration) this row **STAYS OPEN, STOPS GATING and STOPS BEING OWED** — it no longer
draws leftover capacity. **This is not a resolution.** No status cell moves, the row does not close,
the open-row count is unchanged, and nothing about the issue recorded above has been done. What
lapses is the obligation, not the row.

**The derivation that graded it — which is what makes this record challengeable rather than final.**

- `tools/audit/nongating_apparatus_rows.json` — the apparatus cut, which classes this row
  NON-GATING on **D-438**'s criterion on the ground *tracking/documentation apparatus, the user's
  own phrase*, with its per-row reason recorded there.
- `tools/audit/decisions/true_half_reach_rows.json` — **D-639**'s test, which put this row OUTSIDE
  the doc-sync half, so it is owed nothing further on phase 1's TRUE half either.

**How to re-open it.** Challenge a named derivation rather than rediscovering the issue: if the
apparatus cut's verdict for this row is wrong, or D-639's test reaches it after all, the row returns
inside the gate and back into what is owed. **An establishment obligation (#19) never lapses,
whatever its subject** — that clause of the non-gating declaration is untouched by Ruling 66, and the
cut cannot place a row outside the gate whose recorded ground names it, the carve-out being encoded
in the cut rather than remembered.

**Where the population lives.** `tools/audit/phase1_finish_line.json` →
`★_the_bearing_cut.the_lapse_population`, derived on every run from the apparatus declaration's
NON-GATING set and never hand-listed; no count is restated here (**D-431**). A row with no named
grading does not lapse — it halts that derivation instead.
