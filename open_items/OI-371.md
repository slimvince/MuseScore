# OI-371 — A set of comment blocks was HELD pending a call-graph answer; the answer is now established and the row that recorded the hold is RESOLVED

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10), in the code. Found 2026-08-11 (CC,
`cc_instruction_row_landing_and_oi141_arm_check.md` Task 2) while performing the licensed
read-only establishment of which code path performs key inference on the shipping arm.**

## How it was found

Not by looking for it. Task 2 ordered one thing: establish, AT THE CALL GRAPH, whether the joint
estimator's decode consults `keymodesequence` / `keymodeanalyzer` on the production arm, or carries
its own key path. That trace is exactly the input a set of committed verdicts was waiting on.

## What was established, and at which object

**The hold, read at the artifact.** `tools/audit/arm_comment_sweep.json` grades a group of comment
blocks **HELD**, each with the same recorded reason — for `jointdecoder.h`: *"Same shape as the
adapter: 'DORMANT (no production consumer)', with no outside src/ reference to refute it literally.
HELD."* The blocks name the joint adapter, the joint decoder, the tables loader, the class value
type and the fact adapter, plus the test headers that close on the same claim. The population and
its size are at that artifact and are not restated here (**D-431**). The governing clause is the
user's Ruling 16 as the sweep records it: *"a sibling whose falsity is not mechanical is HELD."*

**The answer, read at the code.** The trace is in the close of this dispatch and is not restated
here (#6). Its load-bearing step: `jointnotationproducer.cpp`'s `produceNotationRecord` calls
`decodePiece` directly, and `composingconfiguration.cpp` sets `USE_JOINT_NOTATION_RECORD`'s default
value to `true`, so every in-app notation seam reaches that call. On the batch surface
`batch_analyze.cpp`'s `runJointInference` calls `decodePiece` and returns before `analyzeScore`.
**So the decoder has a production consumer on both surfaces**, and `jointdecoder.h`'s header
sentence *"DORMANT (no production consumer)"* is false at HEAD. The adapter's header carries the
same sentence and is reached by the same call.

**The tracking, read at the INDEX.** [[OI-353]] is the row that recorded the hold, and it is
RESOLVED — its status cell names the HELD blocks as *deliberately not edited* under the ruling's own
clause. A resolved row is not a place a later reader looks for owed work, so at HEAD **no open row
carries them**.

## Why it is a row rather than a remark

**The hold was correct and is now spent.** Nothing here says Ruling 16 was applied wrongly: it
declined to edit a block whose falsity was not mechanical, which is what it was ruled to do. What
has changed is that the fact it declined to guess at has been established.

**And the shape is one the record already names twice.** A finding stated once and never marked
discharged is the [[OI-283]] shape; a claim about which arm ships, left standing in the code where
a session reads it, is [[OI-353]]'s own subject. This is both at once, and the reason it is worth a
row is that the artifact's HELD verdicts read as *undecided* rather than as *owed*, so nothing
would bring them back.

## What is NOT claimed

**No gating verdict.** Whether this row gates is derived from a cut and is never hand-added — the
lesson [[OI-336]] records and the change **D-436** reserves. The question is reported and no verdict
is proposed. *Stated so it is not lost:* the subject is a statement about the analysis's BUILD
STATE — which decoder ships — which is the second half of D-438's line, and [[OI-353]] carried
exactly that reasoning in its own former status.

**No count, in either direction.** How many blocks are HELD is the artifact's own figure and is read
there; how many of them the trace makes FALSE is a per-block reading against each block's own words,
and re-authoring another act's verdict table is not this task's act. Two are named above because
they were read; the remainder are not graded here.

**No correction, and no `src/` edit.** The dispatch is read-only on the analysis and admits no
`src/` change. The correcting act needs its own authorization, exactly as [[OI-368]]'s act (a)
records for the neighbouring case, and the sweep's own STOP fires on a verdict whose block the scan
no longer finds — so a correction must go through the sweep rather than around it.

**Nothing about the analysis.** No behaviour moved, no measured value, no golden, no corpus of
scores, nothing in `tools/robust_stop/`. Every subject here is a statement ABOUT which arm ships
and the apparatus that grades such statements. A comment is not executable.

## The closing acts, neither of them this session's

**(a) Re-grade every HELD block against the established call graph**, through
`gen_arm_comment_sweep.py` so the verdicts stay derived and each keeps its reason, and correct
whichever the re-grade makes FALSE-AT-HEAD in one comment-only commit — the shape Ruling 16 already
used for the first pass.

**(b) Decide where the re-graded set is tracked.** [[OI-353]] is resolved and re-opening a resolved
row is not this session's call; whether the act hangs on this row or on a re-opened one is a
question for whoever authorizes (a).

*Provenance: CC, 2026-08-11, dispatch `cc_instruction_row_landing_and_oi141_arm_check.md` Task 2,
while establishing the shipping arm's key path at the call graph under the licence chain that
dispatch's §0b sets out. Cross-ref [[OI-353]] (the sweep that recorded the hold), [[OI-368]] (the
neighbouring surviving site and its two acts), [[OI-303]] (the enumerated claim family),
[[OI-141]] (the sitting whose fourth input the trace was performed for), [[OI-357]] (whose own
establishment located the same production-path anchors).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
