# OI-134 — Debris directory tools/tools/corpus/

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-134 | Debris directory `tools/tools/corpus/` discovered on disk (Cowork, 2026-07-12, while verifying the grading+fitting instruments session): **23,613+ untracked files** (git-tracked count 0) with mangled absolute-path filenames (`C__Users_vince_AppData_Local_Python_..._music21_corpus_bach_bwv*.mxl.xml`) — physical evidence that the OI-130 destructive-default-output-path hazard (DT-24) has ALREADY FIRED historically: a batch run executed from inside `tools/` with a relative default output path nested a second `tools/corpus` tree. Not referenced by any register row or report before this one. No repo damage (untracked); disk waste + a contamination trap for path-globbing tools. Cleanup = deleting the directory — a user-authorized deletion, not done unilaterally; the underlying default-path fix is OI-130. | Cowork verification session 2026-07-12; OI-130; DT-24 | OPEN — deletion HELD (user question 2026-07-12: debris, or scores we should have been using?): the files are converted copies of the music21 package's own Bach corpus (canonical source = the Python package, so deletion loses nothing unique), BUT before deleting, the harness session runs the mechanical check — diff the debris stem set against the corpus registry + `docs/score_inventory.md`'s documented exclusions; report any stems present in the debris but absent from the 352 and whether their exclusion is documented or accidental; deletion + any corpus-onboarding implication (relates OI-38/OI-39) decided by the user on that comparison. **★ COMPARISON DONE 2026-07-12 (harness session, `cc_l5_audit_pass1_harness_report.md` §9, READ-ONLY, nothing deleted):** the debris `tools/tools/corpus/` is a strict SUPERSET of the project corpus — **all 352 committed stems present**, plus **53 extra Bach chorale stems** + 22 Beethoven string-quartet movements + `bwv846` (WTC prelude). The mangled filenames literally encode the music21 package path (`…_music21_corpus_bach_bwv<stem>`), so nothing unique is lost by deletion (canonical source = the package). **Every exclusion is documented at the RULE level** in `docs/score_inventory.md` §"Chorale-selection provenance (audit C3)": the `_is_bach_chorale` / `410 → 353` filter (has bwv, **not a variant suffix**, **not a non-chorale BWV**, exactly 4 SATB parts) — spot-checks confirm the 53 extras are variant-suffix (`bwv248.12-2`, `bwv36.4-2`, …) or non-chorale (`bwv846`) exclusions; the Beethoven set is a different repertoire. **DEBRIS, not scores we should have been using** — the extras are deliberately filtered out, not accidentally omitted; expansion is a Stage-5 decision (§C3 + OI-38/OI-39). **Deletion + onboarding remain the user's call; nothing deleted.** → **✅ USER DECIDED 2026-07-12: DELETE the debris.** **✅ CLOSED 2026-07-12** — deleted at the L5 pass-2 Task-0 (`rm -rf tools/tools`; untracked = 0 git-tracked files, so a filesystem deletion with no commit of its own; `tools/tools/` confirmed gone afterward, `git status --short` unchanged by the deletion — `cc_l5_audit_pass2_report.md` §0). **The onboarding half became a standing directive at OI-38** (use as many scores/corpora as possible at the corpus-onboarding event — the 53 extras, the Beethoven set, `bwv846`, and anything else unused). |
