# OI-286 — The record says the `chordanalyzer.cpp` file split is OWED and parked; it was delivered on 2026-06-17, and one of the units it produced is live production code

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10) / decision-conformance. The [[OI-232]] / [[OI-274]] / [[OI-276]] /
[[OI-279]] / [[OI-284]] class, over a PLANNING statement rather than a descriptive one. CC-found
2026-08-03 at phase 1l, Task 2, running the code check the dispatch required BEFORE writing
[[OI-207]]'s D-416 disposition.**

## Why the check was run at all

The phase-1l dispatch ordered a disposition for **D-416** — the 2026-06-14 user mandate recording
two deferred structural refactors — on a stated premise: that half (1), *the physical split of
`chordanalyzer.cpp` along the layer seams + iteration-API renames*, is owed against code the
retirement map (**D-418**) deletes, so the disposition would be *superseded-with-transfer, the
subject deleted rather than refactored*. The dispatch made the check precede the ruling and named a
different result a STOP. It is one, on two counts.

## What the check found

**(a) The split is DELIVERED, and it is the same named act as D-311's R9.** Commit `41f7c65f63`
(2026-06-17, verified at the object by explicit hash) — *"split chordanalyzer.cpp into
single-responsibility layer TUs (byte-identical; oracle residual +
formatter/voicing/diagnose/post-passes/gates)"* — moved 2,178 lines out of `chordanalyzer.cpp` into
five sibling translation units and touched nothing else but `CMakeLists.txt` and
`docs/scoring_model.md`. That is **three days after** the 2026-06-14 mandate that deferred it.
`docs/scoring_model.md:26-37` records the resulting layout as current. It is the same act R9 names:
`docs/implementation_roadmap.md` writes *"R9 `chordanalyzer.cpp` file-split (**OWED refactor #1**)"*
in so many words, and the execution artifacts are titled "Refactor #1 —
`chordanalyzer.cpp` layer-split" (`cc_refactor1_split_design_dossier.md:1`,
`cc_refactor1_split_build_report.md:1`).

**(b) Five later records still call it owed and parked.** None of them notes the delivery:

| where | what it says |
|---|---|
| `docs/implementation_roadmap.md` (the retirement map, RATIFIED user 2026-07-02, **D-418**) | "R9 `chordanalyzer.cpp` file-split (OWED refactor #1) AFTER E4 removals — split once" |
| `STATUS_ARCHIVE.md:166` (session 22i, the Stage-5 planning checkpoint) | "the `chordanalyzer.cpp` file split (refactor #1) **stays parked** BY the ratified engage map … pulling it now would violate, not honor, the ratified order" |
| `cowork_stage5_fitter_design.md:103` (2026-07-04, scope-out list) | "the `chordanalyzer.cpp` file split (OWED refactor #1 — **parked by ratified R9**)" |
| `cowork_structural_integrity_audit.md:313-314` (2026-07-07) | "**Stage 4 — R9** (after the E4 removals; OWED refactor #1): the `chordanalyzer.cpp` file split — 'split once' after the removals, **exactly as parked**" |
| register entry **D-311** (status DEFERRED, ratified 2026-08-02) | "the chord-analyzer file split happens once, after the retirements have settled — not before" |

Against them, the delivery is equally on the record: `cc_instruction_module_layering_assessment.md:3`
("**Refactor #1 done** … committed `41f7c65f63`, Cowork-verified"), and
`cc_module_layering_assessment_dossier.md:51-56, :198, :294` assessing all six resulting units as
single-responsibility and the chord layer as "Refactor #1 landed clean; confirmed".

**(c) Part of half (1)'s subject is LIVE production code, not code the retirement map deletes.**
`chordsymbolformatter.cpp`, one of the five units the split produced, runs on the record arm — the
production in-app notation path since 2026-07-27 (**D-010**):

- `notationimplodebridge.cpp:1159-1170` — "the DISPLAY form via the shared presentation formatter
  (`ChordSymbolFormatter::formatSymbol` … **the record arm's chordResult** carries the fine class
  quality's seventh-ness)";
- `:1189-1191` — "Nashville is a PRESENTATION derivation on **BOTH arms** …
  `formatNashvilleNumber`";
- `sectionrecordadapter.h:69-71` names `formatSymbol` / `formatNashvilleNumber` as the record path's
  presentation derivations, the ratified D2 / §3.3-amendment ones.

And `STATUS_ARCHIVE.md:114` (the 2026-07-11 Layer-4 certification audit) records the residual itself
as one that "**SURVIVES the engagement; R9 *splits* `chordanalyzer.cpp`, does not delete it**".

**(d) What IS still owed of half (1) is the iteration-API renames, and they were not done.**
`applyIter8691Pedal` remains the declared name in `chordanalyzer.h`, defined in
`chordpostpasses.cpp`, and called from `sectionanalyzer.cpp:450` (inside the legacy `analyzeSection`,
which begins at `:197`) and `regionanalyzer.cpp:1011`, plus the test suites. Roadmap item **3.5**
names exactly this second half — "rename iteration-vocabulary APIs (`applyIter8691Pedal` →
descriptive names)" (`docs/implementation_roadmap.md`, the Stage-3 table) — and the row carries no
done mark.

## Why this is a row and not a ruling

Three of the five "parked" statements sit inside user-ratified surfaces (the retirement map D-418,
and D-311 as ratified 2026-08-02). Correcting what a ratified surface says about the state of the
work is a change to the record, not a doc-sync tidy — the same shape [[OI-279]] took, and the same
reason it was rowed rather than executed. Nor can a session decide on its own judgment whether the
delivered split *discharged* the mandate: half (1) has two halves of its own and only one landed.

## What is NOT in doubt

That the split happened, at that commit, byte-identically (the build report's five per-step gates,
`.ours.json` 0-diff on all three presets, suites and pipeline-snapshot goldens untouched); that the
renames did not; and that D-416's own text and D-416's ratification as a correct record of the
2026-06-14 mandate are unaffected either way — the mandate said what it said.

## Disposition

For the user, in three parts:

1. **Is half (1) discharged, partly discharged, or still owed?** The delivered split plus the
   undone renames is a *partly discharged* item on its face; the record has no answer.
2. **What happens to R9 and to D-311?** If half (1)'s split is delivered, R9's "AFTER E4 removals —
   split once" describes an act already performed, and D-311's DEFERRED status is stale. Both sit in
   ratified surfaces, so the correction is the user's.
3. **Then, and only then, D-416's disposition** — which is what phase 1l was asked for and stopped
   on. Any disposition must survive (c): the subject is not wholly legacy, so the
   "superseded-by-D-418, the subject is deleted rather than refactored" shape does not fit half (1)
   as it stands.

Half (2) — the dissolution of the post-hoc gate-correction layer into fitted weights — is untouched
by this row and is not in question: it is undone, its subject is the legacy gate layer, and the
retirement map's R1 covers it.

*Provenance: CC-found 2026-08-03 at `cc_instruction_phase1l_rulings_and_reads.md`, Task 2 §3.1, the
check the dispatch ordered to precede the ruling. Commit `41f7c65f63` read at the object by explicit
hash; every other citation read with the file tools this session. Cross-ref [[OI-284]] (the same
document's other stale statements), [[OI-180]] (the retirement map), [[OI-207]], [[OI-208]].*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
