# OI-186 — compare_rn.extract_quality coarse on WiR slash notation

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-186 | `compare_rn.extract_quality` is COARSE on WiR slash-spelled notation (CC-found 2026-07-19 building the OI-177 count inventory; quantified in `tools/joint_estimator/count_inventory.json` normalization_gaps): the half-dim sigil `/o` reads as non-half-dim (545 labels, e.g. `ii/o6/5`→Min) and slashed seventh figures read as a triad quality (1,999 labels, e.g. `V6/5`→Maj, `V4/3`→Maj — the first `/` is a figured-bass slash in WiR, not a tonicization). The count inventory is NOT corrupted (the full suffix is preserved verbatim in the figure field; classes never merge), but (a) the FIT-TIME class normalization must not read quality through `extract_quality`, and (b) whether the pinned rn_agree GRADING axis is coarsened by the same parse is an open instrument question — assess read-only before any RN-axis claim rests on fine quality distinctions. The pinned instrument was NOT modified (correctly — the dispatch's hard stop) | CC report 2026-07-19 (`cc_instruction_prefit_instruments.md` dispatch); `tools/joint_estimator/count_inventory.json` | OPEN — CC-found 2026-07-19; (a) binds the fit-event normalization design; (b) a read-only instrument assessment, separate |
