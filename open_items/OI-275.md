# OI-275 — CONFLICT: the SIGNED Layer-4 specification makes COMPLETE candidate listing the governing design decision; the production decoder's candidate admission prunes, and empties the analysis outright on most orchestral scores

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section E — user adjudication / decision-conformance. Found by the phase-1g triage wave,
2026-08-02, reading `cowork_layer4_chordsymbol_design.md` IN FULL. Both sides are quoted;
nothing is fixed, and no design is proposed here.**

## The two sides

`cowork_layer4_chordsymbol_design.md` (**SIGNED, user, 2026-06-24**), §4 step 1, verbatim
(`:208-212`, entered this wave as register entry **D-329**):

> 1. **List the possible chords.** From the slice's pitches, generate **every** tertian chord the pitches could spell —
>    each basic type at each root — and score each by how well the pitches fit it. **Completeness is the priority:** a
>    chord never listed can never be chosen, and the measured dominant error is "the right chord was never on the list,"
>    not "the wrong one was picked among good options."

The same document states it twice more, as a weighed architecture decision (§9, *"List
completely, then select — completeness is the lever"*, with a re-scorer over a partial list the
**rejected** alternative) and as a risk (§11, *"Complete listing is the priority risk. A chord
never listed can never be chosen"*).

Against that, the production joint decoder's **candidate admission**: three gates plus a top-6
key prune, already rowed at [[OI-226]] as having **no ratified basis** in the ratified decode
plan. Measured consequences already on the register:

- [[OI-215]]: an event whose every covering window carries fewer than two distinct onset pitch
  classes admits **nothing**, so the whole decode returns **0 segments** — on **13 of the 23
  committed large scores**, every symphony in the set.
- [[OI-227]]: a **second** gate (the fit / non-chord-tone budget) empties the decode at the
  opposite end of the density spectrum — 21 of the 312 uncoverable events over those scores.
- [[OI-188]]: the ground-truth state is reachable on only **~72 %** of spans; it was
  force-added on 14,257 spans at the fit event.

## Why this is a decision-conformance question and not a restatement of OI-226

[[OI-226]]'s finding is an **absence**: the admission rule has no ratified basis. This row's
finding is an **opposition**: there IS a ratified statement about candidate listing, in a signed
layer specification, and it says the opposite of what the production rule does. That changes what
the family design owes — not merely "give admission a basis", but "reconcile the basis you give
it with D-329, or record that D-329 does not reach this estimator".

## The question the record does not answer, and which is the user's

**Does a SIGNED layer specification bind the joint estimator?** The joint estimator replaced the
layered pipeline for inference (**D-001**, user-ratified 2026-07-14/17), and
`cowork_layer4_chordsymbol_design.md` describes a decoder that is built but dormant. Three
readings are possible and the record does not say which holds:

1. **It binds.** The signed specification states a design principle about candidate listing, not
   an implementation detail of one decoder; nothing in the joint-estimator ratification withdraws
   it, so the admission rule is non-conformant and the family design must satisfy it.
2. **It is superseded in fact.** The joint estimator's own factorization is the governing
   specification for its candidate space, and D-329 lapsed with the path it was written for —
   in which case the register needs D-329's status corrected and the joint estimator's
   specification needs its own listing decision, which [[OI-226]] says it does not have.
3. **It is scoped narrower than it reads** — a statement about the vertical scorer's template
   enumeration rather than about the decode's state space — in which case the scoping was never
   written down.

Deciding between them is a ruling, so this pass did not make one.

## What is NOT in doubt

D-329's own defense is a measurement, not a preference: the record states the dominant remaining
error is that the right chord was never listed, and rejects a strong re-ranker over a cheap
partial list on exactly that ground. And the admission rule's cost is measured, not suspected:
zero segments on 13 of 23 large scores.

## Disposition

For the user: rule which reading holds. Then the consequence follows the standing rule that one
fix is designed once over the whole enumerated family (**D-204**) — this row joins
[[OI-215]] / [[OI-226]] / [[OI-227]] / [[OI-228]] / [[OI-243]] / [[OI-244]] as an input to that
single design, and is **not** a separate fix. **No fix, no design, no specification edit is made
here**, and none may be: the standing instruction forbids inference-problem-driven coding while
the scaffolding is still being built.

*Provenance: CC-found 2026-08-02 at `cc_instruction_phase1g_triage.md`, Task 2, reading
`cowork_layer4_chordsymbol_design.md` in full (SIGNED, user, 2026-06-24); the decision it rests
on is entered as **D-329** in the same commit that records this row (the register's rule (c)).
Cross-ref [[OI-226]] (admission has no ratified basis), [[OI-215]] / [[OI-227]] (the two
empty-decode gates), [[OI-188]] (the prune cannot reach the ground truth), [[OI-207]] (the audit
this serves).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

---

**Dated note — 2026-08-02 (CC, the phase-1h continuation wave): a SECOND instance of this row's
governing question, on a different layer and a different axis — does a SIGNED layer specification
bind the joint estimator?**

Reading `cowork_layer3_keymode_design.md` in full (SIGNED, user, 2026-06-22) entered **D-345**: the
style preset first enters the analysis at the tonality layer, as a deliberately weak prior over
which of the twenty-one modes are likely in that style, which the note evidence overrides.

That sits against **D-003** — *inference is preset-independent; presets are presentation concerns*
— which is LIVE, homed in `ARCHITECTURE.md`, and measured: `CLAUDE.md` gate block (A) records the
three preset directories as identical at the inference fields, which is why every baseline column
there is one value rather than three.

**The shape is exactly this row's:** a signed layer specification states one thing, the production
estimator does another, and **the record contains no ruling that names either statement against the
other**. The differences from the founding instance are worth stating precisely, because they bound
what this adds:

- **It is weaker in consequence.** D-329's completeness statement is contradicted by an admission
  rule with a measured cost — zero segments on 13 of 23 large scores. D-345 has no measured cost at
  all: the tonality decoder it governs is dormant, so nothing in production reads a preset prior.
- **It is stronger in one respect.** The two statements are not merely in tension about a mechanism;
  they answer the SAME question — does the user's style setting influence inference — in opposite
  directions, and both are recorded as standing.
- **The three readings are the same three.** It binds (and the estimator owes a mode prior per
  idiom, which is what **D-293** separately says fitted values are calibrated against). Or it is
  superseded in fact by D-001 with the supersession never written down. Or it is scoped narrower
  than it reads — to the dormant path only — also never written down.

**No ruling is made here, and D-345 is entered with the record's own status (LIVE), not statused as
superseded** — inferring the supersession is exactly the move the never-work-from-memory rule
forbids, and the register's `superseded-in-fact` value is reserved for cases the record supports.
The entry's own `status_source` carries this flag so the tension is visible at the register, not
only here.

*Cross-ref [[OI-207]]'s phase-1h note, [[OI-268]] (this row's question in its general form),
[[OI-226]] (the absence-of-basis sibling), [[OI-276]] (the same Layer-3 document's stale
current-state banner, found in the same read).*
