# OI-285 — The eight ratification-surface documents are UNCOMMITTED, and the register now cites two of them as the provenance of ratified decisions

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10) / #12, no information loss. CC-found 2026-08-03 at phase 1k, on
staging the commit.**

## The finding

Every one of the documents that carried a ratification queue to the user exists **only in the
working tree**. Verified at the objects this session, against commit `b6be14097e`:

| document | state at `b6be14097e` |
|---|---|
| `cowork_pending_ratifications_next_session.md` | **absent** — `git cat-file` reports "exists on disk, but not in" the commit |
| `cowork_decisions_pending_ratification_2.md` … `_8.md` (seven files) | **untracked** |

These are not scratch notes. They are the surfaces on which the user was asked to rule, and the
project's own rule makes the surface part of the ruling: a decision is only readable against the
text it answered (`CLAUDE.md`, *the whole decision surface is delivered as user-visible text before
any choice question*, D-249). Nine register entries — **D-406…D-414** — now record their
ratification as "the phase-1j queue presented at `cowork_pending_ratifications_next_session.md` §2",
and that file in turn names `cowork_decisions_pending_ratification_8.md` as where the nine complete
entries were read. Uncommitted, the citation chain resolves to nothing a future reader can open.

The exposure is ordinary and total: an uncommitted file is one `git clean`, one machine change or
one working-tree reset away from gone, and there is no snapshot of it anywhere else.

## What was done at phase 1k, and what was left

**One file was committed:** `cowork_pending_ratifications_next_session.md`, because the phase-1k
dispatch directed that its status block be flipped to RULED and that it not be deleted (#12), and
because nine register entries were given it as their provenance in the same wave. Editing a document
into the record and then leaving it outside the record would have been the worse of the two
inconsistencies.

**Seven were not.** `cowork_decisions_pending_ratification_2.md` … `_8.md` are another session's
working files; adding them was outside anything this dispatch licenses, and the judgment about
whether a per-event reading aid belongs in the repository at all is not a session's to make
unilaterally. They are named here so the decision is a decision rather than an omission.

## What is NOT in doubt

The ratifications themselves. The user's rulings of 2026-08-02 and 2026-08-03 are recorded per entry
in `backbone_decisions.json` and rendered into the register, which is committed; this row is about
the SURFACE the user read, not about whether the ruling happened.

## Disposition

A choice for the user or for Cowork, not a doc-sync act, because it decides what class of document
the repository keeps:

- **(a) Commit all seven** — they become the durable record of what was put to the user at each of
  the eight ratification events, matching how every other decision surface in this project is kept.
- **(b) Commit none and stop citing them** — the register's provenance would then name the ruling
  and its date without naming a file it cannot resolve, which is honest but loses the wording the
  user actually read.
- **(c) Fold each into the register entries it served** and delete the files, which is the #6 answer
  (one home) but is a substantial rewrite of provenance fields already ratified.

Cowork owns these files and should rule; nothing here should be done by a session on its own
judgment.

*Provenance: CC-found 2026-08-03 at `cc_instruction_phase1k_apply_rulings.md`, Task 6, on staging —
`git diff --cached --numstat` showed 82 insertions and 0 deletions for a file that had been edited,
not created, which is the signature of an untracked file. Verified at the objects by explicit hash.
Cross-ref [[OI-207]], [[OI-208]] (the register's shape), [[OI-283]] (the register's other
provenance-hygiene row).*

---

**★ Dated correction note — 2026-08-03 (CC, phase 1l, Task 1): both phase-1k commits cite a dispatch
filename that does not exist.**

The two phase-1k commits — **`7454abe5db`** ("phase 1k tasks 1-4 — the five 2026-08-03 rulings
APPLIED …") and **`16f1f1775e`** ("phase 1k tasks 5-6 — the single tracker read in full …") — each
name their authority as `cc_instruction_phase1k_apply_rulings.md`. **No file of that name exists on
disk or in the tree at any commit.** The dispatch those commits actually executed is
**`cc_instruction_phase1k_ratification_application_and_reads.md`**, which is present on disk and,
until this session, untracked — the eighth instance of exactly the exposure this row records, and
one this row's own enumeration missed (it counted the seven pending-ratification surfaces and the
one already-committed queue document, not the dispatch).

The executed content matches the true file: the tasks the two commit messages describe — applying
the five 2026-08-03 rulings, the nine ratifications, the §6.7 taxonomy correction, D-131/D-132, the
single-tracker full read — are that document's tasks.

**The repair is this note, not a rewrite.** Commit messages are immutable and pushed history is not
rewritten (the standing rule this project has already applied once, at the phase-1h commit-sequence
correction). The correction lives where a reader tracing the citation will meet it: on the row that
owns the uncommitted-surface problem, and the true file is committed at phase 1l so the corrected
name resolves.

The same mistake is guarded against going forward only by care: a dispatch's own name is copied from
the file, never retyped from memory (`CLAUDE.md` Conventions, *never work from memory instead of
documented facts*).

---

**★ Dated resolution note — 2026-08-03 (CC, phase 1l, Task 5): the COMMIT half is executed; the
directory half is carried on [[OI-287]].**

The user ruled option **(a)** — commit them — together with a directory convention for the class.
What phase 1l did, and the one refinement it applied with its reason stated:

- **All seven committed IN PLACE, at their current paths, content unchanged** —
  `cowork_decisions_pending_ratification_2.md` … `_8.md`. Not edited, not reformatted, not
  re-bannered: they are the text the user read, and a ratification surface that has been tidied is
  no longer the surface that was ratified (**D-249**).
- **The eighth instance committed too** —
  `cc_instruction_phase1k_ratification_application_and_reads.md`, the file both phase-1k commits
  cite as their authority under a name that does not exist (see the correction note above). This
  row's own enumeration missed it; it is added here.
- **The directory convention was ROWED, not executed** — [[OI-287]]. The refinement, and its reason:
  moving the files would re-point the paths nine committed register entries (D-406…D-414) cite as
  the provenance of a ratified decision, and re-aiming ratified provenance is a larger act than a
  filing decision. It also owes a per-citation re-aim from the verify report's own output, never an
  assumed path rewrite.

The exposure this row found — that the surfaces on which the user was asked to rule existed only in
the working tree, one `git clean` from gone — is **closed**.

---

**★ Dated location note — 2026-08-03 (CC, phase 1u, Task 4): these files have MOVED, and the text
above is deliberately not re-aimed.**

The directory half, carried on [[OI-287]], was executed on the user's ruling of 2026-08-03. **Every
file this row names now lives in `ratification_surfaces/`** — `git mv`, so history follows. The one
exception is `cowork_pending_rulings_2026_08_02.md`, which is untracked and was left where it is,
because moving it would commit it and that is this row's own question, unasked for that file.

**Why the paths above were left alone.** Everything this row says about these files is a statement
about a PAST state: what commit `b6be14097e` did and did not contain, which files were untracked
when the finding was made, and that phase 1l committed seven of them *"IN PLACE, at their current
paths"*. All of that is true of the paths as they then were. Rewriting those paths would make the
record assert that `ratification_surfaces/` existed at a commit where it did not — the precise
failure OI-287's per-citation constraint exists to prevent, and the reason the re-aim classified
each citation instead of sweeping them.

So the row reads as it was written, and this note is where a reader learns where to look now. The
per-citation classification, with the reason for every leave, is at
`tools/audit/ratification_surface_reaim.json`.

*Resolution belongs in the INDEX row; dated notes may be appended here.*
