# OI-224 — OI-199 pass 1 under-delivered two arms against the L4 standard (P4 corpus routes; P3's second side)

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — review (user-directed) / method finding on [[OI-199]]**

OI-199 pass 1 (`cc_oi199_pass1_report.md`, 2026-07-28) dispositioned the joint module's 1,069 rows
correctly, but **two of the four protocol arms under-delivered against the certified L4 precedent**
(`cc_l4_audit_pass1_decoder_report.md` §5 — real corpus routes with per-branch fire counts, and a
two-sided P3):

1. **P4 (behavioral characterization) ran a passing test suite + a fire-structure read off the code, not
   real corpus routes with per-branch fire counts.** At L4 the decoder was run over the corpus (29,080
   slices; commit 34.4 % / inherit 3.0 % / abstain 62.6 %; per-site fire counts under the OI-110
   counter). Pass 1's joint P4 was "78/78 tests pass + a structural read." The corpus-fire-rate arm was
   the one that would have measured the member-overlap gate's rejection behaviour on out-of-envelope
   texture.
2. **P3's second side (code with no ratified expectation) reported only the stale dormancy headers
   ([[OI-220]]) and missed that candidate admission has NO ratified basis.** The root-present /
   member-overlap / NCT-budget admission gates entered production via the `probe_decoder` port with only
   inline comments; the ratified decode plan (`cowork_joint_estimator_factorization.md` §5) specifies the
   full 24-key × vocabulary state space and reserves only a *different* prune. Pass 1's P3-second-side
   should have surfaced this (now [[OI-226]]).

**This dispatch (`cc_instruction_oi199_joint_p3_p4.md`) is the remedy** — it completed both arms to the
L4 standard: the DEFAULT-OFF fire-count instrument (OI-110 disposition, reverted at close) over BOTH the
fit corpus AND the 23 committed large scores, with per-filter admission counts, the per-event coverage
decomposition, and the dead-branch sets per population; and the exhaustive P3-second-side check against
the ratified documents. Report: `cc_oi199_joint_p3_p4_report.md`; artifacts `tools/audit/oi199/joint_firecount_{fit,large}.json`,
`task1_predictions.md`, `pass1_dispositions_joint_annotations.csv`.

*Provenance: CC-found 2026-07-28 at this dispatch, at Cowork's ruling (amended alternative 2) that
"dispositions are NOT redone; the two arms are COMPLETED." The pass-1 dispositions STAND ([[OI-225]]).*
