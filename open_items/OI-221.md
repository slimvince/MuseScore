# OI-221 — Two published joint-record facts have no consumer (§3.4 modal reading + §3.2 augSixthSubType; declared dormancy)

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — the joint-module review (OI-199 pass-1 finding, area a; fact-publication corollary)**

**The finding (CC, OI-199 pass 1, grep-verified).** The §3 notation record is the module's published
output surface, and most of its derived facts ARE consumed outside `joint/` (grep counts, non-test:
`diatonicToKey` 12, `chordSymbol` 10, `romanNumeral` 6, `.slices`/posterior 2, `.provenance` 1,
`bassPerEvent`/`rootSpellingLof` via `sectionrecordadapter`). **Two published families are consumed by
NOBODY outside `joint/`:**
1. **The §3.4 un-rounded modal reading** — `NotationRecord::modalReading` and its
   `ModalKeyRun`/`ModalDegree`/`ModalInflection` (10 fields), built by `computeModalReading`. **0
   external consumers.**
2. **The §3.2 `RecordSegment::augSixthSubType`** — the Italian/German/French aug-sixth display sub-type
   from the sounding content. **0 external consumers.**

**Disposition: declared dormancy, not waste.** The fact-publication corollary permits a published fact
with no consumer IF a future consumer is named. Both are named: the notation output-surface contract
(§3.4 "the presentation layer formats later"; §3.2 the aug-sixth DISPLAY sub-type) names the presentation
layer as the consumer. So neither is a #12/waste violation. **But flagged** so (a) a reader does not
mistake these live-computed fields for consumed signals, and (b) the named future consumer is tracked to
resolution — the aug-sixth one relates to the aug-sixth display-completeness work, and the modal reading
is the C1 modal-counter's publication awaiting its presentation formatter.

**NOT fixed here** (OI-199 read-only; nothing to fix — the disposition is declared-dormancy; this row is
the tracking so the future consumer lands). Related: [[OI-199]] (the review), [[OI-201]] (aug-sixth
display symbol completeness, if that row exists — the augSixthSubType's likely consumer), [[OI-194]] (the
ornament/label publication siblings). DT-5 (declared-dormancy face).

*Provenance: CC-found at OI-199 pass 1 (2026-07-28, `cc_oi199_pass1_report.md` §3/§6 F-OI199-2; the
publication grep). Rowed at the pass-1 unblind commit.*
