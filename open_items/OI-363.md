# OI-363 — The partial-signature correction is recorded resolved on one anchor case, while over the derived population it leaves a large minority of the tonics unread and locked to the notated signature

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — the analysis (what the legacy key arm does with a Baroque notation practice it has a
dedicated correction for). Measured 2026-08-09 (CC, `cc_instruction_return_continuation_4.md`,
Task 3).**

## What was done, and the bounds it was done inside

The user's **Ruling 26** ordered an EARLY, BOUNDED, READ-ONLY establishment for [[OI-357]]: the
committed corpus outputs for the partial-signature stems compared against ground truth, with **no
new measurement machinery, no corpus regeneration, no fix, no design and no inference change.** All
of those bounds held. Nothing was run over any score; the comparison reads files already committed.

## The population is DERIVED, and that is why it is larger than the record's own table

`docs/key_detection_baroque_partial_signature.md` §3 states its own method — the notated key
signature against the published annotated key, taken from the corpus metadata. **That METHOD was
applied mechanically to every piece in that file**, rather than the document's six-row table being
transcribed. The population is therefore whatever the record's own rule makes it, and no piece is
selected by judgment. It is many times the size of the table.

The rule needs no direction clause: a piece is in the population when its notated signature differs
from the natural signature of its annotated key, which is exactly *notated one accidental short*.
The direction per piece is reported rather than assumed.

## What was measured

At the most recent committed run for this repertoire — dated after the correction landed, so it
reflects it — **a large minority of the population is NOT read with the annotated tonic.**

> **★ THE WORDING ABOVE WAS CORRECTED 2026-08-09** by `cc_instruction_return_continuation_7.md`
> Task 2. **THE FORMER WORDING, PRESERVED (#12):** *"fewer than half of the population is read with
> the annotated tonic."* **It was inverted against the artifact it cites** — at the column it names,
> the agreeing pieces OUTNUMBER the disagreeing ones, by a narrow margin. The former wording also
> stood in this file's title, in [[OI-357]]'s row and in `cowork_away_returns.md` §2.10, and is
> corrected at each with the same note. **What is NOT withdrawn:** the two characterizations below,
> which are true as written; the finding itself, which is that a correction recorded RESOLVED leaves
> a large minority of its own derived population unread; and the user's **Ruling 35(c)**, which
> ruled the FORM of a direction-without-value statement acceptable and could not make a direction
> true. The full account is at `cowork_away_returns.md` **§2.11**.

**The large majority of the disagreements land on a home of the NOTATED signature.** That is the
evidence document's own diagnostic for the signature lock the correction exists to escape: when the
resolver cannot leave the notated family it settles on that family's major or minor home. Both homes
are computed here from the notated signature alone, so the column is derived and not a judgment.

**The split by the annotated key's mode is the sharpest cut.** The minor-key half of the population
fares markedly better than the major-key half. The correction's own recorded detector keys on a
single MINOR-key signal — the flattened sixth degree pervasive across the sounding weight and
dominating its natural form.

**THE CUT IS REPORTED AND THE CAUSAL CLAIM IS NOT MADE.** This pass counts; it does not explain. No
mechanism is diagnosed, no fix is proposed, and none is authorized (**D-231**; #8's three-clause gate
stands).

Every value lives at `tools/audit/oi357_partial_signature_establishment.json` and none is restated
here (**D-431**).

## ★ The record's own account reads wider than the measurement supports

The evidence document's banner declares the weakness **RESOLVED**, and what it verifies is **one
anchor case**. Both halves of that are true as written. But a reader meets *resolved* and would not
learn that a large minority of the population the anchor case belongs to is not — which is a
document's account of itself changing how its analysis content is read. *(The clause formerly read
*"the population the anchor case belongs to is mostly not"*, preserved here under #12; it is
corrected by the same 2026-08-09 act as the sentence above, and for the same reason.)*

## What is owed FIRST, and it is not a fix

**A reading, per case.** Each disagreement is one of three things and this pass distinguishes none of
them:

- a genuine defect of the reading;
- a **defensible modal reading the major/minor ground truth cannot represent**, which `CLAUDE.md`
  gate block (A) makes a GROUND-TRUTH LIMITATION and explicitly not a defect to optimize away — the
  count of non-major/minor emissions among the disagreements is reported for exactly this reason, so
  the disagreement total is not read as a defect total;
- an artifact of comparing a global reading against a global annotation on a piece that modulates.

## What bounds the size of this

**Its subject is the LEGACY arm, which the production path does not run.** Nothing here is a claim
about what ships. [[OI-357]] — not this row — carries the production question, and that question is
still unanswered because no committed output for this repertoire was produced by the production arm.

**And this repertoire is not the gate corpus**, so no published measurement moves and none is
questioned.

## What is NOT in doubt

- **The anchor case**, which the committed output reads with the annotated tonic exactly as the
  record says.
- **The correction itself**, which landed and does what its own description says it does.
- **The ground truth**, which is the published human annotation and nothing else.
- **Every published measurement of the gate corpus**, none of which this touches.

## The doc-sync half

One edit for a session whose authority reaches `docs/`: the banner should say what was verified
rather than that the weakness is resolved. It is not taken here.

*Cross-ref [[OI-357]] (the row whose bounded establishment measured this), [[OI-315]] (the sibling
legacy key-path staleness naming the same source document).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
