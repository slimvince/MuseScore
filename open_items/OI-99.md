# OI-99 — L3 pass-2 dangling doc-reference

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-99 | L3 PASS-2 dangling doc-reference (DT-12, #10, low): `regionanalyzer.cpp:534` and `:815` comments cite `cowork_phase5c_step4_report.md` (the J-key-iii §6 byte-identity proof), which **does not exist** in the repo (not tracked, not gitignored). Two occurrences in production source comments. Doc-sync only, no code effect. | `cc_l3_audit_pass2_report.md` §4; `tools/audit/l3/sweep_results.json` (DT-12); DT-12 | OPEN — re-point to the extant provenance doc or drop the citation at the next `regionanalyzer.cpp` touch (comment-only, #8) |

## ★ LAPSE RECORD — 2026-08-12 (CC, `cc_instruction_resume_lapse_records.md` Task 3)

**What has happened to this row.** Under the user's **Ruling 66** of 2026-08-11
(`cowork_rulings_2026_08_11_fifteenth_stop.md`; register entry **D-676**, homed at `CLAUDE.md`'s
non-gating declaration) this row **STAYS OPEN, STOPS GATING and STOPS BEING OWED** — it no longer
draws leftover capacity. **This is not a resolution.** The status of record above is untouched, the
row does not close, the open-row count is unchanged, and nothing about the issue has been done. What
lapses is the obligation, not the row.

**The derivation that graded it — which is what makes this record challengeable rather than final.**

- `tools/audit/nongating_apparatus_rows.json` — the apparatus cut, which classes this row
  NON-GATING on **D-438**'s criterion on the ground *tracking/documentation apparatus, the user's
  own phrase*, with its per-row reason recorded there. Its recorded reason is that the citation is a
  dangling POINTER: nothing false is stated about the analysis by it.
- `tools/audit/decisions/true_half_reach_rows.json` — **D-639**'s test, which put this row OUTSIDE
  the doc-sync half, so it is owed nothing further on phase 1's TRUE half either.

**How to re-open it.** Challenge a named derivation rather than rediscovering the issue — in
particular the reading that the two production comments state nothing false about the analysis, which
is what puts the row on the apparatus side rather than inside D-438's build-state clause. **An
establishment obligation (#19) never lapses, whatever its subject.**

**Where the population lives.** `tools/audit/phase1_finish_line.json` →
`★_the_bearing_cut.the_lapse_population`, derived on every run from the apparatus declaration's
NON-GATING set and never hand-listed; no count is restated here (**D-431**). A row with no named
grading does not lapse — it halts that derivation instead.
