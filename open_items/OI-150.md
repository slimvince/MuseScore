# OI-150 — BUILD_AND_TEST.md stale test baselines

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — Doc-sync debt (#10) — annotate, don't rewrite history**

| OI-150 | `BUILD_AND_TEST.md` stale test baselines (backlog triage, 2026-07-13; DT-12/#10 — found by RUNNING the suites at HEAD): `:103` states composing "**Current baseline: 974/974** passing, 2 disabled" — measured at HEAD **1101/1101 passing, 2 disabled**; `:114` states notation "**Current baseline: 53/53** passing" — measured at HEAD **53 passing and 4 SKIPPED**. The notation line is the harmful one: it reports a clean suite and **hides the four key-emission xfails** (OI-148), so a reader checking "do the tests pass" is told yes and never learns four user-visible DCML-checked cases are failing by design. Both lines carry a "(verify with CC — count changes as tests are added)" caveat that was never exercised. | `cc_backlog_triage_report.md` §6; `BUILD_AND_TEST.md:103/114`; measured suite output at HEAD `3966502265`; OI-148; DT-12 | OPEN — re-stamp both baselines AND make the notation line name the 4 xfails (a passing count that conceals xfails is worse than no count) at the next `BUILD_AND_TEST.md` touch (doc-only, #10) |

---

## Dated note — 2026-08-03 (CC, phase 1x): the trigger condition FIRED and was NOT discharged, and the row's own citations drifted

**The trigger fired.** This row's remedy is due *"at the next `BUILD_AND_TEST.md` touch"*, and phase 1x
touched that file (Task 4, the `--joint-inference` correction). **The remedy was not taken**, for a
stated reason rather than by oversight: re-stamping a baseline means MEASURING it — building at HEAD
and running both suites — and this wave neither built nor ran a suite. Re-stamping from the numbers
already written in the row above would be transcribing a 2026-07-13 measurement as if it were current,
which is what the row exists to complain about.

**The row's own line citations drifted, by this wave's edit.** The verbatim row above cites `:103`
(composing) and `:114` (notation). Those lines are now **124** and **135** — the Task-4 correction
inserted text above them. The row is byte-verbatim-protected by the split reconciliation
(`tools/open_items_split_check.py`), so it is **not rewritten**; the current numbers are recorded here
instead. The quoted phrases — *"Current baseline: 974/974"* and *"Current baseline: 53/53"* — are
unchanged and remain the reliable way to find both lines.

*Provenance: CC, phase 1x, 2026-08-03, dispatch `cc_instruction_phase1x_guard_visibility_and_commit.md`,
Task 4 and the standing self-check. Cross-ref [[OI-148]] (the four key-emission xfails the notation
line conceals).*

---

## Dated note — 2026-08-13 (CC): the trigger fired again and WAS discharged — both baselines measured, the notation line made to name the four

**What was done, and the one thing that made it possible.** The suites were **built and run at HEAD**
before anything was written — `setup_and_build.bat`, then `./composing_tests.exe` and
`./notation_tests.exe` — and both `BUILD_AND_TEST.md` baselines were rewritten **from those two runs
and from nothing else**. That is the whole difference from 2026-08-03: the remedy was never a wording
change, it was a measurement, and the wave that declined it declined correctly because it had not
taken one.

**The four xfails were established at the run, not read off this row.** The notation suite reported
exactly four skipped cases, and their identities are exactly the four [[OI-148]] names. That is the
first time the row's *"4 key-emission cases fail by design"* assertion has been checked by anyone
rather than repeated; the count and the identities agree, so nothing had to be reconciled. Had either
differed, the batch's own stop rule required it to halt and report rather than smooth the difference.

**Both suites have GROWN since the row was written, and the four have not.** The row's own
at-HEAD-2026-07-13 measurement was composing 1101 passing and notation 53 passing beside 4 skipped;
this batch measures both higher, and the exact values are in `BUILD_AND_TEST.md` rather than repeated
here. What did **not** move is the skipped set: still four, still the same four names. That is the
part worth recording — a growing passing count is ordinary test-adding, whereas a skipped set holding
at four across five weeks is the row's own point standing unchanged.

**Two facts about this file's citations, recorded here rather than in the byte-verbatim row above.**
The row's `:103`/`:114` citations, already recorded as having drifted to 124/135 on 2026-08-03, have
moved again with this re-stamp, and no line number is given for them here — the record's standing
rule is that a coordinate quoted in prose goes stale on the next insertion above it. **The quoted
phrases remain the reliable way to find both lines**: *"Current baseline: 974/974"* and *"Current
baseline: 53/53"* are both still in the file, now inside the preserved-former-wording blocks that sit
beside their replacements (#12).

**What this note does NOT say.** It records no status — the INDEX row is the status of record — and
it discharges nothing belonging to [[OI-148]]: the four xfails are still failing by design, still
owned by the key-layer build, and naming them in a baseline line neither fixes nor re-blesses them.

*Provenance: CC, 2026-08-13, dispatch `cc_instruction_oi150_baselines.md`, Tasks 1 and 2.*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
