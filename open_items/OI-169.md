# OI-169 — structuralPenalties silently ignores its extThreshold parameter

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-169 | **`structuralPenalties` silently IGNORES its `extThreshold` parameter — a preset knob that appears wired to a scoring term but never reaches it.** `chordanalyzer.cpp`: the signature is `structuralPenalties(tpl, rootPc, pcWeight, tpcForPc, distinctPcs, double extThreshold = kExtensionThreshold)` and the single call site passes `prefs.extensionThreshold`, but **the body never reads the parameter** — its four penalty branches use `kSus4StructuralFourthThreshold` and two hardcoded `0.05` literals instead (confirmed at HEAD: `git show HEAD:…` shows exactly one occurrence of the name, the parameter declaration). So a preset that tunes `extensionThreshold` moves the other four scoring terms that DO honor it and silently does **not** move the structural penalties — a live trap for the Stage-5 fitter and for anyone reading the call site (which reads as if the threshold is honored). **Discovered incidentally** at the OI-168 measurement build: pre-existing, surfaced only as a `C4100 unreferenced formal parameter` warning when the edit forced the TU to recompile. **Declared, NOT fixed** — it is inference-affecting (removing the parameter is behavior-neutral; making the branches honor it is not), so it is not touched under a measurement dispatch (#8). The two dispositions to choose between: (a) delete the dead parameter (documentation-truth fix, provably byte-identical), or (b) make the branches read it (a scoring change ⇒ robust-stop diff + ratification). | `cc_oi168_magnitude_report.md` §9; `chordanalyzer.cpp` (`structuralPenalties` decl + body + its call in the scoring loop); `docs/scoring_model.md` §4 (the structural-penalty entries) | OPEN — **CC-discovered 2026-07-14; needs a disposition (a) or (b).** Cheap and low-risk under (a); under (b) it is a Stage-5 fitting question. **★ A SECOND INSTANCE of the same defect type, found at the OI-168 fix build (2026-07-14): `formatNashvilleNumber(const ChordAnalysisResult&, int keySignatureFifths)` (`chordsymbolformatter.cpp`) never reads `keySignatureFifths` either** — same `C4100 unreferenced formal parameter`, same trap (a call site that reads as if the signature reaches the formatter). Pre-existing at HEAD, in a file this session did not touch. Fold into the same disposition |
