# OI-304 — Two dated annotations still assert the opposite of D-428 as corrected one wave later

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10). Rowed 2026-08-03 (CC, phase 1w), found by the [[OI-289]]
verification's citation scan.** Both are located and quoted at
`tools/audit/decisions/phase1w_legacy_verification.json` →
`findings_rowed_elsewhere.two_annotations_contradict_the_corrected_D_428` (D-431).

## The contradiction

**D-428** is the owed half of the two-deferred-refactors mandate — the iteration-vocabulary API
renames. Two dated annotations, **both written 2026-08-03 at phase 1m**, state that the renames have
a subject that *"includes live Layer-1.5 code"*, one of them adding that they therefore *"do not
retire with the legacy path"*:

- `cowork_stage5_fitter_design.md:116`
- `cowork_structural_integrity_audit.md:323`

**D-428 was corrected later the same day, at phase 1n**, against the [[OI-165]] premise and at the
call sites: every use of the named function sits on the older stage-by-stage path, on one of its
orchestrators, in its diagnostic view or in its tests — so the renames are naming debt on code
awaiting deletion, and deleting that path discharges them. The entry says so in terms, and records
that it said the opposite until that date.

So the correction reached the register entry and not the two documents that carry the premise it
refuted. This is the shape [[OI-288]] recorded from the other side, where a correction reached a
detail file and its INDEX row went on carrying a false route for five waves: **a correction lands
where it is written, and the record has no mechanism that carries it to the other statements of the
same fact.**

## Why it was not corrected in the wave that found it

Both sit inside dated annotation blocks, and an annotation block is the record of what was believed
when it was written — so the correct act is a further dated annotation beside them, not an edit of
the original (#12). That is a document act on two ratified-surface neighbours, and the phase-1w
dispatch is a gathering wave whose only licence to act is over a mark it establishes wrong.

## What is not claimed

That either annotation was careless. Both were written before the check that refuted their premise
had been run — the phase-1n reading of the two retirement maps at their own text. What is owed is the
propagation, not blame.

*Provenance: CC, phase 1w, 2026-08-03, dispatch `cc_instruction_phase1w_legacy_mark_verification.md`.
Cross-ref [[OI-289]] (the verification), [[OI-286]] (the D-416 disposition that produced D-428),
[[OI-165]] (the premise), [[OI-288]] (the same one-surface-corrected shape), [[OI-180]].*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
