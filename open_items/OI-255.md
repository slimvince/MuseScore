# OI-255 — `addAnalyzedHarmonyToSelection` is unreachable from the UI: implemented, interfaced, mocked, behavior-snapshot-tested — registered to no action, menu, or shortcut

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — notation / interaction seam (DT-5-adjacent at the seam level). RE-SCOPES
[[OI-213]].**

The per-note command exists at every level except the one users can reach: implemented
(`notationinteraction.cpp:8256`), declared on the interface (`inotationinteraction.h:258`),
mocked (`mocks/notationinteractionmock.h:262`), behavior-snapshot-tested by replication
(`notationinteraction_harmony_pinning_tests.cpp`) — and a repository-wide search finds no action
registration, no menu item, no shortcut, and no QML caller. The three annotate action codes all
route to `addHarmonicAnnotationsToSelection` instead (`notationactioncontroller.cpp:378-386`).

Two readings, needing one disposition: dead code at the interaction seam (#6/DT-5 — remove with
its interface row and mocks), or an unshipped feature awaiting its action registration (then a
declared dormancy with the consumer named). **Consequence for [[OI-213]]:** its N-produce-per-
command multiplier is real as a code fact but concerns a command no user can currently invoke —
the cost fact stands, the user exposure does not; OI-213's detail file gains a dated note. Not
fixed here.

*Provenance: Cowork-found 2026-08-02, the requirement-side review
(`cowork_candidate_open_items_2026_08_02.md` C-2); user reviewed 2026-08-02.*
