# OI-107 — L4 oracle ARCHITECTURE.md doc drifts

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-107 | L4 PASS-1 (L4-2b oracle) `ARCHITECTURE.md` doc drifts vs the code (DT-12/#10, non-correctness; `docs/scoring_model.md`, the authoritative oracle contract per CLAUDE.md, IS in sync — the drift is confined to ARCHITECTURE.md): (a) **`bassNoteRootBonus` documented 0.65** (§4.1 line 1289, §4.1b line 1373) but the code default is **0.70** (`analysistypes.h:177`); (b) the **`Extension` enum bit order** (§4.1 lines 1227-1244) has bits 4/5, 9/10, 14/15 *swapped* vs `chordanalyzer.h:213-230` (in-memory only, no functional impact); (c) **§4.4 mis-locates AnalysisUtils** (`analysis/analysisutils.h` vs actual `analysis/chord/analysisutils.h`) and under-lists it (omits `diatonicMaskFromFifths`/`collectionMask`/`pcInMask`); (d) **§4.1 broadly predates the refactor-1 split** (Gates A–L described as in `chordanalyzer.cpp`; Gate K/M + iteration BIR baselines 21/128 + test counts 407/407 stale). | `cc_l4_audit_pass1_oracle_report.md` §1/§2; ARCHITECTURE.md §4.1/§4.1b/§4.4; DT-12 | OPEN — re-sync ARCHITECTURE.md §4.1/§4.4 to the code at the next doc touch (comment/doc-only, #8/#10) |

---

**Dated note — 2026-08-02 (CC, the phase-1 truth-sync, commit `9df7f3ee9d`): PARTIALLY DISCHARGED,
and one NEW instance found outside this row's stated scope.**

**Done:** (a) `bassNoteRootBonus` corrected 0.65 → 0.70 at both sites (§4.1's preferences listing and
§4.1b's problem statement), against `analysis/types/analysistypes.h:196`. (b) the `Extension` bit
order corrected — bits 4/5, 9/10 and 14/15 were listed swapped against `chordanalyzer.h:213-230` —
and, in the same listing, `ChordIdentity`'s four missing fields added (`naturalFifthPresent`,
`tiePriority`, `isPedalPoint`, `pedalBassPc`). (c) §4.4's path corrected to
`src/composing/analysis/chord/analysisutils.h` and its three missing functions listed
(`diatonicMaskFromFifths`, `collectionMask`, `pcInMask`). The first half of (d): the gates are
recorded as living in `analysis/chord/postscoringgates.cpp`, not `chordanalyzer.cpp`, and **Gate K is
recorded RETIRED** (Stage 5, 2026-07-05, design item D-7) — the section had been listing a retired
threshold as a live Baroque-calibrated value.

**Still owed:** the rest of (d). §4.1h's iteration-era figures (the BIR baselines and the 407/407 test
count) are presented as current state and are not; correcting them means either re-measuring or
re-labelling the block as a dated historical snapshot, and neither was in this pass's scope.

**★ NEWLY FOUND, and owed:** this row states that "the drift is confined to ARCHITECTURE.md". It is
not. `CLAUDE.md`'s own gate-threshold policy still lists **Gate K: 0.20** among the live
Baroque-calibrated thresholds, five weeks after the gate was retired. Correcting a governing document
was outside the truth-sync's scope, so it is recorded here rather than silently fixed.
