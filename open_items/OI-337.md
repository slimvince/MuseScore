# OI-337 — The uncommitted backlog is SIX waves, not the three ruling R4 names, and the waves cannot be committed separately

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10) / #12, no information loss. CC-found 2026-08-04 at the
census-delegation wave, Task 3, by the STOP that task instructs.**

## The finding

Ruling **R4** authorized committing *"the phase-1 completion inventory and the delegations-and-corrections
wave … together with this one"* — three waves — and Task 3 instructed: *"If anything is modified outside
the three waves named in R4, STOP and report."*

There is. Verified through `tools/audit/changed_paths.py`, the sanctioned reader of the working-tree
change list (**D-253**):

**READ WAVES 4, 5 and 6 are also uncommitted.** Their yield artifacts and the generators that write
them are untracked — `reads4_yield.json`, `reads5_repack.json`, `reads5_yield.json`,
`reads6_yield.json`, `reads4_oi326_application.json`, and `gen_reads4_yield.py`,
`gen_reads4_oi326_application.py`, `gen_reads5_repack.py`, `gen_reads5_yield.py`,
`gen_reads6_yield.py`. HEAD is **`0787ebf0ff`**, the READ WAVE 3 commit, which `STATUS.md`'s own
wave-4 entry records in terms: *"READ WAVE 3 IS COMMITTED — `0787ebf0ff`"*.

So the true backlog is **six** waves — read waves 4, 5 and 6, the phase-1 completion inventory, the
delegations-and-corrections wave, and this one — against the three R4 has in view.

## Why "commit only the three named waves" is not an available act

The six waves edit the **same files**, each layering on the last: `STATUS.md`, `DECISIONS.md`,
`OPEN_ITEMS.md`, `tools/audit/decisions/backbone_decisions.json`, the twenty rendered group files
under `decisions/`, and the guard artifacts. There is no per-wave partition of the working tree to
stage. Committing the three R4 names necessarily commits read waves 4–6 with them; the only choices
actually on the table are **one commit covering all six**, or **none**.

This is stated as a fact about the tree, not as a recommendation. Which of the two is taken is the
user's, because a commit is the act **#14** makes user-ratified and because R4's premise — that two
waves were outstanding — is not the state on disk.

## What is uncommitted and is NOT wave work

Flagged separately so a commit decision is made with the whole picture. Each is untracked and
predates this arc; none was produced by any of the six waves:

| path | note |
|---|---|
| `tools/robust_stop/baroque_variant_a_root_fail_cells.txt` and five siblings (`*_variant_{a,b}_root_fail_cells.txt`, three presets) | **The one worth a second look.** These sit inside `tools/robust_stop/`, which gate block (A) names as the committed reference for the hard regression stop. They are untracked, so they are not part of that reference; nothing reads them as one. Whether they are intermediate dumps or something the reference should carry is not settled here and was not investigated — this wave is read-only on the measurement surface |
| `tools/joint_estimator/joint_posterior_slice_parity.json`, `joint_spelling_parity_cpp.json` | parity artifacts from earlier joint-estimator work |
| `cowork_jazz_realization_qa_instrument.md` | a `cowork_*` document never committed |
| `cowork_pending_rulings_2026_08_02.md` | **deliberately left** — [[OI-285]]'s dated location note records that moving or committing it would decide a question that row never asked for that file |
| `idiom_discovery/vl_discovery_out.txt`, `idiom_discovery/vl_orthogonality_out.txt` | tool output from the voice-leading idiom discovery runs |
| `scratch_artifacts/` | a directory of working files |

## What is NOT in doubt

The work itself. Every one of the six waves is on the record in `STATUS.md` with its dispatch named,
its artifacts on disk and its guards run; the register, the open-items register and the generated
artifacts all re-derive at HEAD. This row is about the work existing **only in the working tree**,
which is the exposure [[OI-285]] recorded for a different population and closed for that one: an
uncommitted file is one `git clean`, one machine change or one working-tree reset away from gone.

The exposure is larger here than it was there, because it now includes **instruments** — the guard
tools, the yield generators and the derivation this wave's own conclusions rest on — and an
instrument that exists only in a working tree cannot support a reproducibility claim (**#16**).

## Disposition

The user's, and it is one question with two answers:

- **(a) One commit covering all six waves**, provenance-stamped, naming each wave and its dispatch.
  This is what R4's intent — get the outstanding waves committed — actually requires on this tree,
  and it is what the inseparability above forces.
- **(b) None**, leaving the backlog as it stands until a dispatch scopes the commit against the real
  population rather than against three waves.

The non-wave paths above are a **separate** decision and are not swept into either: none of them is
this arc's work, and committing them would be exactly the unilateral filing act [[OI-285]] declined
for the seven ratification surfaces.

*Provenance: CC-found 2026-08-04 at `cc_instruction_census_delegation_and_commit.md`, Task 3, by the
STOP the task itself instructs — the check worked. Cross-ref [[OI-285]] (the same exposure, a
different population, closed), [[OI-287]] (the directory half).*
