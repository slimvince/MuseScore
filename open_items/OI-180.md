# OI-180 — Sanctioned dual path for building the joint estimator

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-180 | Sanctioned dual path for building the joint estimator: A built beside the certified stack temporarily violates #6 (one path per concern) — declare the parallel decode, the side-by-side grading (#15), and the retirement map (the R1–R9 pattern) in the structure-design step, so the transition is pre-ratified rather than surfacing mid-build as a principles dispute | `cowork_joint_estimator_architecture.md` §7 item 5 | PROTOCOL RATIFIED 2026-07-19 (`cowork_prefit_gates.md`) — sanction in force from the build dispatch on: A isolated in its own module reading ONLY the L1/L1.5 fact surface (shared code limited to the enumerated dependency-free pc/mask primitives), production byte-identical behind a default-OFF driver for the whole arc, side-by-side grading on the full output surface (#15), six-item retirement map (incl. scheduling the deferred Gates A–L dissolution) executing only after the OI-178 adoption, declared reverse map if not adopted, DT-13 handoff-visibility guard |

**Dated close-out note (CC, 2026-07-27, seams-2 P7 — the partition close-out; INDEX status UNCHANGED, this file is not a status of record):** the sanctioned dual path's FORWARD EXIT is now ASSEMBLED and completeness-verified, pending the user's switch ratification. The joint estimator's A-native notation record and every re-plumbed notation consumer are built beside the certified legacy stack and DORMANT behind the default-OFF `useJointNotationRecord` (the seams partition P0-P7; production byte-identical with the flag off, proven per unit). The read-only completeness verification found no gaps (`tools/notation_seams/partition_completeness.json` — every audited consumer's record branch cited, every ratified ruling implemented or carried, the flag off everywhere, composing 1186 / notation 61 / pipeline-snapshot 13 green at HEAD 89412b48b2). The side-by-side full-surface grading (#15) is delivered as the P6 dual-arm classified comparison (`tools/notation_seams/dualarm_classified_report.json`: identical 2053 / inference-driven 6029 / presentation-rule 220 / input-scoping 0 / unexplained 0). This row stays open and its INDEX status is unchanged: the six-item retirement map executes only AFTER the switch (§8.4) — a separate, user-ratified commit; the retirement map, and this row's close, follow it. The declared reverse map (if not adopted) and the DT-13 handoff-visibility guard remain in force until then.

**Dated note — THE SWITCH EXECUTED (CC, 2026-07-27; INDEX status carries the change, this file is not a status of record):** the forward exit is now EXECUTED on the notation surface. The notation switch (dispatch `cc_instruction_notation_switch.md`, user-ratified 2026-07-27) flipped `useJointNotationRecord`'s default ON, so the in-app notation analysis is produced by A's record path (all four seams). With the batch/corpus surface adopted at OI-178 and the notation surface adopted now, the sanctioned dual path's forward exit is complete on BOTH surfaces, and the **six-item retirement map is FULLY LIVE**: the legacy `analyzeHarmonicRhythm`/`analyzeChord`/`ChordAnalysisResult`/`NoteHarmonicContext`/`HarmonicRegion` path and the per-arm legacy branches now remain compiled ONLY as the flag-false dormant arm, awaiting the map's deletions (their own later dispatches). The switch established the record arm at the objects (the pipeline-snapshot goldens refreshed against it and every diff reconciled to the P6 classified evidence — 0 unexplained, `tools/notation_seams/switch_golden_reconciliation.json`); the batch surface and `tools/robust_stop/` did not move. The declared reverse map (an explicit flag-false restores the legacy arm — the switch is ONE revertible commit) and the DT-13 handoff-visibility guard remain in force until the map completes. The sanction stays in force (the dual path still physically exists) until the last map deletion lands.
