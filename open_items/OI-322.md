# OI-322 — `docs/symbol_input_audit.md` describes deleted code in the present tense throughout, and corrects itself only in its last line

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section F — doc-sync debt (#10). Found 2026-08-04 (CC, READ WAVE 2,
`cc_instruction_reads_2.md`) reading `docs/symbol_input_audit.md` in full — the wave's reading order
14.**

## The shape

The audit runs 346 lines. Its §B, §C, §1, §3 and §4 describe five things as live code, with file and
line citations and with tables headed "Write sites" and "Read sites":

- `analyzeScoreJazz` (the chord-symbol-boundary analysis path in the batch tool),
- `injectWrittenRootTone` and the `--inject-written-root` flag,
- `scoreHasValidChordSymbols` (the gateway gate that routes to the jazz path),
- the `jazzMode` field on the chord temporal context,
- the `fromChordSymbol` / `writtenRootPc` / `writtenQuality` comparison fields.

Two of them are put to the user as open questions — *"Q1: Accept or retire `analyzeScoreJazz`
(C.1)?"*, *"Q2: Accept or retire `--inject-written-root` (C.2)?"*, *"Q3: Should `jazzMode` be deleted
…?"* — each with an options list marked *"(do not choose for user)"*.

Its final line reads: *"**Status:** All Category-C findings resolved in 69716deead
(`analyzeScoreJazz`, `--inject-written-root`, `jazzMode` field deleted). Category A remained at zero
throughout. Audit actionable items complete."*

## Established at HEAD

A search of every `.cpp` and `.h` file in the tree for `analyzeScoreJazz`, `inject-written-root`,
`injectWrittenRootTone`, `jazzMode` and `scoreHasValidChordSymbols` returns **no match**. The closing
line is the true one; the body is stale.

## Why it is worth a row rather than a shrug

Three reasons, and the third is why it is filed as a defect rather than as an artefact of any
completed audit.

1. The stale body carries **line citations into `tools/batch_analyze.cpp`** (lines 783–800,
   1653–1675, 1680–1812, 1824–1826, 2223–2225 and others). A reader following one of them lands
   somewhere unrelated, which is [[OI-133]]'s dangling-anchor shape.
2. It puts **three questions to the user that were answered by deletion**. A reader who reaches the
   questions and not the closing line is invited to decide something already decided — and the
   register records the answer as **D-067** (*"Jazz mode (chord-symbol-driven boundaries) is
   retired"*).
3. The correcting sentence sits **after the 340 lines it refutes**. That is the defect [[OI-315]]
   recorded in `docs/key_path_design.md` — a document that contains its own correction, placed where
   a reader meets it last — and this is a second instance, which makes it a shape rather than an
   accident.

## Why it is not corrected here

The wave reads and rows, and the remedy is a filing decision no session should take alone: an audit
is a dated report of a state at a moment, so the question is whether such a document is re-bannered
as a historical record of the pre-deletion state (the treatment `CLAUDE.md` gate block (C) uses for
the superseded gate) or rewritten to the state at HEAD. The answer applies to every completed audit
in the tree, not to this one, which is why it is not settled inside a row about one document.

*Provenance: CC, READ WAVE 2, 2026-08-04, dispatch `cc_instruction_reads_2.md`, Task 1.2. Cross-ref
[[OI-232]], [[OI-276]], [[OI-315]], [[OI-133]], and register entry **D-501**, entered from the same
read (the operating principle's tool clause, which the audit's own categories are graded against).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*

---

**Dated note — 2026-08-11 (CC, `cc_instruction_return_continuation_14.md` Task 2), on the user's
Ruling 62 of `cowork_rulings_2026_08_11_fourteenth_stop.md`. The status of record is the INDEX row.**

**The filing question this row posed for the whole class is ANSWERED: BRANCH ONE.** A completed audit
is re-bannered as a historical record and its body is never rewritten. The banner states what the
document is a record OF — its date, the state it audited, the fate of its subject, and the deleting
commit its own closing line names, together with the at-HEAD search that confirms no match survives.
**Not one line of the body is touched (#12).**

*Why the banner rather than the rewrite, in the convention's own terms:* a completed audit is
evidence about an act — what was known, when, and what was concluded — so rewriting it destroys the
thing it exists to be, and a reader who cannot see what the document said cannot tell a correction
from a revision. **And the banner fixes BY CONSTRUCTION the defect this row identified**: a
correcting sentence sitting three hundred lines below what it refutes. A reader who stops before the
last line was told the opposite of HEAD; a reader who meets the banner first cannot be.

**The row's wider half — *the same question applies to every completed audit in the tree* — is
answered by a DERIVED enumeration rather than by this document's treatment being generalized by
hand.** `tools/audit/filing_convention_application.json` runs over a named surface set, looking for
signatures of OVERTAKING carried in a document's own banner or closing line rather than for
signatures of being a report, and carries an authored verdict per candidate. **It states its own
bound** — its reach against the text it scans has never been measured, so its silence is not evidence
— under D-673's test, which asks whether an ANALYSIS DECISION consumes the enumeration; none does.
**And it is declared NOT SOUND against its seeds**, because it misses one the record already holds;
that is reported with its diagnosis and the population stays advisory. No count is restated here
(**D-431**).
