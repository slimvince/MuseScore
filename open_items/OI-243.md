# OI-243 — The decode is not transposition-equivariant: the spelling factor is anchored to a canonical per-tonic spelling, so a respelled edition moves content scores and segmentation

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — joint decoder / emission spelling factor (declared to Cowork). The representation
face of the [[OI-215]]/[[OI-226]]/[[OI-227]]/[[OI-228]] family.**

## The invariant, and why it was probed

Music theory: a piece transposed by k semitones has the transposed analysis — every key tonic and
chord root shifts by k (mod 12), the degree structure and segment boundaries are identical. This
is the first invariant check of the perspective-inventory program
(`cowork_oi200_perspective_inventory.md` channel 3), run 2026-08-01/02 as an EXPLORATORY,
read-only probe (surprises are the stage's product, per the scope-of-surprise rule) while the
register-completion dispatch held the register surface.

## The protocol and the measured result

Apparatus: the pure-Python reference decoder (`tools/joint_estimator/probe_decoder.py`, decode
bit-parity-established against production). Established first: 12 deterministically sampled
pieces reproduce the committed `decode_parity_ref.json` exactly, 12/12. Predictions registered
before measuring: ≥99 % segment equivalence under +2, −3, +6 semitone transposition (each applied
as a uniform respelling — pitch +k, line-of-fifths shifted by the matching fifths delta,
signature shifted the same way).

**Measured: 811 of 1224 segments equivalent (66.26 %). Only 6 of 36 piece×shift conditions fully
equivariant; segment boundaries moved in 27 of 36; per shift +2 = 83.3 %, −3 = 65.2 %,
+6 = 50.2 %, with three near-total collapses at +6 (worst 0/32 segments).** The six bit-exact
conditions (total-score delta 0.0) are the apparatus's own soundness proof.

## The mechanism (Cowork-verified at the source)

- `probe_decoder.py:743` — the spelling factor anchors each key at the CANONICAL tonic spelling,
  `key_lof = _PC_TO_FIFTHS[tonic % 12]`; the fitted note table bins each note's line-of-fifths
  position relative to that anchor (`gen_note_tables.py:218-233` per the probe's diagnosis).
  Under a uniform respell, the canonical anchor moves by the fifths delta ±12 for wrap-set
  tonics, re-binning a whole tonality's diatonic tones as pooled chromatic (or a competitor's
  chromatic tones as diatonic). Content scores drive the semi-Markov segmentation, so boundaries
  move — 396 of the 413 violations are this class, in both directions (committed-tonic wraps
  collapse; competitor wraps produce strictly better-scoring transposed paths).
- Exonerated: the signature prior — its fold of `collection_fifths − sig_fifths` into (−6,6]
  (`probe_decoder.py:406-412`) is invariant.
- The remaining label-only violations are the prune tie-break ([[OI-244]]) and propagation from
  adjacent flipped segments (byte-identical factor vectors; the competitor moved).

## Establishment status, carried honestly

The probe was run by a sub-agent with an agent-written measurement script — established only by
its P1 self-check (12/12 reference reproduction) and the six bit-exact conditions. **The row's
FIRST action is a repository-side re-run of the probe before this finding carries any load
(#19).** Artifacts committed for that purpose:
`tools/joint_estimator/transposition_probe_2026_08_02/` (report.md with the predictions as
registered and all 413 violations enumerated; transpose_state.json; establish_state.json;
run_probe.py). Caveat: transposition-as-uniform-respelling models an engraver's spelling choice;
+6 semitones is genuinely enharmonically ambiguous, so SOME +6 divergence is defensible — moved
boundaries and 0/32 collapses are not an enharmonic-frame difference.

## Relation and disposition

Compounds [[OI-246]] (the adapter feeds the spelling factor written-pitch spelling on transposing
staves — representation sensitivity meets representation inconsistency) and joins the
struck-vs-sounding family's governing question — WHAT the model reads, now extended by "and in
which representation." Any fix is a Premise-Gate design at its #8-correct stage over the whole
family (#6/#7), never a per-symptom patch; NOT fixed here (read-only exploration).

*Provenance: Cowork-found 2026-08-02, the four-perspective review pass
(`cowork_candidate_open_items_2026_08_02.md` A-1); mechanism sites re-read by Cowork at the
source; user reviewed the candidate file 2026-08-02.*

---

**★ Dated cross-reference — 2026-08-03 (CC, phase 1m, Task 1, on the user's D-416 ruling of the
same date). A PHASE-3 DESIGN CONSTRAINT this row's family must satisfy.**

Register entry **D-429** — component (2) of the 2026-06-14 two-deferred-refactors mandate — carries
a principle the user ruled binding on the family design that will eventually answer this row:

> **A correction belongs in a factor's fitted value, never in a layer of after-the-fact corrections
> laid over the decode.**

The mandate's own subject is the legacy Gates A–L layer, which retires at the retirement map's R1
(**D-418**); the principle is not legacy and does not retire with it. It is stated once, at D-429,
and pointed at — never restated — from the joint estimator's standing rules in `ARCHITECTURE.md`
(#6, one path per concern). Read it before proposing a remedy for this row that patches an answer
after the decode has produced it. Cross-ref [[OI-286]] (the finding that produced the split),
[[OI-288]], [[OI-289]].
