# OI-367 — A fourth row whose status cell states a closure in words reads OPEN to every derivation, and the pass that enumerated that family missed it

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — instrument / measurement layer (the open-items index's own status apparatus). Found
2026-08-11 (CC, `cc_instruction_return_continuation_10.md`, Task 0) while DERIVING that task's own
population at the artifact that owns it, which is the act that surfaced it.**

## The finding, read at the index and at the two artifacts

[[OI-298]]'s status cell reads **`OPEN — ★ RESOLVED 2026-08-03 (CC, phase 1v) — …`**. Its own text
then records the closure in full, in six numbered acts with their provenance, and `CLAUDE.md`'s
phase-2 clause says of the same subject that *"the gap it closed was tracked at `OPEN_ITEMS.md`
OI-298."* The row's STATE, which is carried by the first token of its status cell and by nothing
else (the canonical status discipline, **D-662**), is **OPEN**.

**How it came to read that way is recorded in the two artifacts and is not a guess.**

- `tools/audit/oi356_parser_correction.json` enumerated the family [[OI-361]] records — cells that
  state a closure in words with no resolved mark. It found **three** and OI-298 is not among them;
  the two the pass called plain defects open with the word after markdown emphasis, and OI-298's
  cell opens with a decorative marker before the word.
- `tools/audit/index_status_normalization.json` then carries OI-298 with `canonical_token_before`
  null, `the_mark_occurs_anywhere_in_the_cell` false, and `canonical_token_after` **OPEN**. The
  normalization is PREPEND-ONLY outside the corrections it names in advance, and it preserved the
  state the row already held — which is the pass working exactly as ruled, over a population that
  did not contain this row.

## Why it is worth a row rather than a remark

**[[OI-361]] is closed.** It was flipped on the user's Ruling 33, in the act that made the resolved
mark required at the head of a resolved cell — and its own account says both plain defects it found
were corrected and that those were *the only state movements the normalization made*. A fourth
member found afterwards, left unrecorded because the row that named the family is closed, is the
[[OI-283]] shape this open-items register exists against.

**And the miss is about a derivation rather than about one cell.** The family was enumerated by a
pattern over cell openings, and the pattern's reach was never measured against the openings the
index actually uses. That is the same question the reserved-word scanner's outcome answers for a
different derivation (**D-661**): *complete* means complete relative to a NAMED derivation, with
that derivation's measured miss rate against a seed as a portion of its name. Here the miss rate was
not measured, and this row is one member it missed.

## What this row does NOT do, and why

**No state is moved.** Flipping [[OI-298]] would move the open-row count, the TRUE-half cuts, the
finish line's populations and the apparatus declaration's candidate cut — and it would move the
population of the very derivation that found it, which was derived at that task's start. The
record's own precedent governs: at [[OI-362]] a one-character repair was available and deliberately
deferred, because *a population movement belongs to an act that accounts for it, not to a task that
would slip it in unremarked.*

**Nothing is proposed about the parser.** How the index is read is a mechanism **D-436** reserves to
the user, and the canonical status discipline it already carries is not in question — under that
discipline this row's cell is being read CORRECTLY. What is wrong is the cell, not the reader.

## The two acts that would close it, stated so neither has to be rediscovered

1. **If [[OI-298]] is closed**, its status cell gains the canonical resolved opening, with the
   provenance it already carries, in an act that accounts for the population movement — the
   [[OI-362]] shape.
2. **If something remains owed on it**, the cell says what, and the open-state token is then correct
   and stays.

**Which of the two is right is a reading of that row and is not taken here.** Its own text and
`CLAUDE.md`'s clause both point one way; this row records the discrepancy and leaves the bit where
it stands.

**A third act rides with either and is the general half:** the family's enumerating pattern owes a
measured miss rate over the openings the index actually uses, in the shape **D-436** requires of
every mechanism, before *three members* may be read as *the family*.

## What is NOT in doubt

- **[[OI-361]]'s own resolved status**, which is true of the population it covered, and the
  canonical status discipline, which is unchanged and which this row does not propose amending.
- **[[OI-298]]'s content**, whose closure is recorded with its provenance whichever way its single
  bit falls.
- Nothing about the analysis, any measurement of it, any golden, any corpus of scores or anything in
  `tools/robust_stop/`.

*Provenance: CC, 2026-08-11, `cc_instruction_return_continuation_10.md` Task 0 — found by deriving
that task's population from `tools/audit/phase1_completion_inventory.json` and reading each row of it
at its own detail file, which is the step that put the cell in front of a reader. Cross-ref
[[OI-361]] (the family this belongs to), [[OI-356]] and [[OI-362]] (its two siblings, remedied in the
same act), [[OI-298]] (the row itself), [[OI-283]] (a finding never marked discharged).*

*Resolution belongs in the INDEX row; dated notes may be appended here.*
