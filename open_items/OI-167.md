# OI-167 — The collection/tonic split holds — conditionally

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section B — Owned by Stage-3 / E4 (build + design decisions at the engage step)**

| OI-167 | ★ **THE COLLECTION/TONIC SPLIT HOLDS — CONDITIONALLY.** `cowork_evidence_inventory.md` §8 circle 3's break (the chord layer needs the COLLECTION, not the TONIC) is **upgraded THEORY→FACT for the REBUILT L4**: `ChordSliceDecoder::decideSlice` takes **no key at all** (`chordslicedecoder.h:599-604`); the decoder's only key use is one `analyzeChord` call with `gateCtxOut=nullptr` (`chordslicedecoder.cpp:452-454`), so it **never runs the post-scoring gates**; and its two key-consuming terms — `dim7CharacteristicBonus` (`chordanalyzer.cpp:574-578`) and `diatonicRootContribution` (`:896-907`) — are **pure collection-membership tests** (the constructed pc-set is provably identical for all 7 diatonic modes of one signature, so the oracle cannot distinguish relative major from relative minor). Corroborated by corpus measurement (chord-flip-under-GT fires on 7/8/6 regions = 0.30–0.37 %, `cc_mode_key_chord_probe_report.md`). **BUT two TONIC-dependent sites are LIVE and are NOT in the decoder:** (a) **Gate G-E** (`postscoringgates.cpp:379-385` — the ii/iii/vii *degree* test; firing swaps the winner to a HalfDiminished root, i.e. changes the committed chord; preset-gated `preferMinorOverMajorAdd6`, ON for Baroque) — rides **R1** ("Gates A–L"); G-E is a Gate-G sub-rule so the letter range covers it, but R1's text names letters, not sub-rules — **confirm at E4**; (b) the **Aeolian lone-tonic/dominant guard** (`sparsechordrefinement.cpp:154-159`: a single sounding A under A-Aeolian stays `Unknown`; the same A under C-Ionian — identical collection — hardens to A-minor) — and **`sparsechordrefinement`'s retirement is UNDECIDED** (OI-102 carries "the `sparsechordrefinement` L4/L5 boundary + retire question"; OI-90 re-tagged the file L4). **If that guard survives the engagement, L4 is NOT tonic-independent and every design resting on the collection/tonic split loses its premise (#18).** **★ SITE (b) MEASURED AT A REAL COUNTER 2026-07-14 (`cc_oi168_magnitude_report.md` §4; default-OFF branch counter at the guard itself, 352 scores × 3 presets): the guard fires 0 / 0 / 0 — and it is DEADER than the output-surface inference claimed. The guard's shape preconditions match 0 times under ANY mode, and `refineSparseChordQualityFromKeyContext` is reached with an `Unknown`-quality chord 0 times across all four call sites: `analyzeChord` never hands the region path an unqualified chord, so the early return takes every call and the whole function body is unexercised on this corpus.** The guard therefore cannot change any committed chord today, and its tonic dependence is unexercised — evidence FOR retirement at OI-102's `sparsechordrefinement` disposition, not a substitute for that decision. | `cc_fact_dependency_audit_report.md` §3.6/§5.3/§6; `chordslicedecoder.h:599-604`; `chordslicedecoder.cpp:452-454`; `postscoringgates.cpp:379-385`; `sparsechordrefinement.cpp:154-159`; `cc_mode_key_chord_probe_report.md`; OI-90/OI-102 | OPEN — **the row's CONCLUSION is now REFUTED, not merely conditional.** Site (b) is measured DEAD (0/0/0, body unexercised) and site (a) is decoder-unreachable, so the two sites this row NAMES are harmless — but the split it guards **does not hold** all the same: OI-168 broke it in the scorer (now fixed) and **OI-170** shows it still broken at three more L4 sites. The proposed **RE-HOME of site (b) was attempted 2026-07-14 and STOPPED — its premise ("already runs post-commit, zero behavior change") is FALSE at the code (see OI-171).** **★ AND THE ROW'S CENTRAL QUESTION IS NOW ANSWERED, NEGATIVELY (OI-170's mechanical whole-tree sweep, 2026-07-14, `cc_oi170_measure_report.md`): L4 CANNOT be made tonic-independent AT ALL.** Beyond the three collection-membership sites (which the signature mask does fix, metric-neutrally), **seven class-(b) sites genuinely need the tonic and three are LIVE and decision-bearing** — Gate G-E (58 winner swaps on Baroque), `applyTonicPriorToSparseChord` (172/183/172 committed-quality overwrites — OI-172), and the published `degree`. A degree is tonic-relative by definition; no collection can answer it. **So `cowork_evidence_inventory.md` §8 circle 3's break is FALSE as stated** — what is true is the narrower claim that the *collection question* can be made tonic-free. This row cannot close until OI-170 (the collection fix), OI-171 (the re-home disposition) and OI-172 (the degree-decides-identity question) are resolved |
