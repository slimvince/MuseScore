# OI-35 — O-18 stale fs_* manifests process fix

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-35 | O-18 stale fs_* manifests process fix (validate or re-manifest E0 dirs; EG-2 used fresh stamped dumps — generalize) | fitter O-18 | ✅ RESOLVED (read-site validation half) 2026-07-12 (OI-145 wave-1, `cc_measurement_chain_hardening_report.md`): the generalized "validate-or-re-manifest at the read site" rule is applied at the km/fs dump-substrate read (`calibration_fit`) — the substrate's per-carrier stem COVERAGE is checked against the validated corpus and a partial/stale/missing substrate is SURFACED loudly (silent on the full substrate today, so byte-identical). Written down + mechanically enforced at the read site. RESIDUAL: a full manifest-based validate (fingerprints) needs the km/fs PRODUCER to stamp a manifest — the same producer-side change as OI-129's km/fs half; tracked there. |
