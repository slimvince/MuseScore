# OI-137 — Harness output-discipline + exit-path robustness (#16)

> STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record

**Section D — Instrument / measurement layer**

| OI-137 | L5 PASS-1 (partition-3 harness) **output-discipline + exit-path robustness (establishment/reproducibility #16, both LATENT — not defects today)**: (a) the standard output file is opened `QIODevice::Text` (`batch_analyze.cpp:4593`) → on Windows the committed `.ours.json` corpus is written **CRLF**, while every diagnostic path writes `std::ofstream(std::ios::binary)` (**LF**) — two line-ending disciplines in one instrument; corpus byte-identity (reproduce-check PASS) holds only while regeneration stays on Windows with Text mode (a foreign-OS regen would silently differ; compounds the O-12/OI-34 corpus git-tracking decision); (b) the standard path FORCE-EXITS (`::TerminateProcess`/`std::_Exit`, `:4615-4621`, bypassing static destructors to dodge a Qt-TLS hang) while the ~10 diagnostic early-return paths return NORMALLY through that same destructor sequence — an exit-discipline asymmetry; whether the diagnostic returns are exposed to the "some runs" hang is UNESTABLISHED (NOT observed — `--validate-slices`/`--decode-chords` exited 0 cleanly this session, but `--validate-slices` returns a MEANINGFUL 0/2 a crash could mask). | `cc_l5_audit_pass1_harness_report.md` §3.4/§3.5/§6; `tools/audit/l5/pass1_dispositions_harness.csv` (standard-output-crlf-vs-diagnostic-lf + exit-path-asymmetry); #16; OI-34 | ✅ ESTABLISHED-AND-DOCUMENTED 2026-07-13, NO CODE CHANGE (the harness group, `cc_harness_group_report.md`) — both halves were measured, and the measurement says do not touch either. **(a) line endings — the prediction was confirmed at the data, then declined:** all **1056** committed `.ours.json` (352 stems × 3 presets) are strictly CRLF (no bare LF, no bare CR), and **all 1056 would change sha256** if the standard path were opened binary — so dropping `QIODevice::Text` is a full-corpus RE-BASELINE, not a cleanup, and is out of scope for a byte-identical dispatch. The diagnostic writers were NOT "aligned" to it either: they already use `std::ofstream(std::ios::binary)`, which is the OS-INDEPENDENT discipline, so aligning them to the standard path would SPREAD the platform dependence rather than remove it (the correct alignment direction is the other one, and that is the re-baseline). Both facts are now in the code at the open site. **The live hazard, stated:** corpus byte-identity holds only while regeneration stays on Windows — the same code on Linux writes LF and every `corpus_manifest.json` fingerprint moves (#16; compounds OI-34). A byte-identical fix EXISTS and is offered to the user, not taken unilaterally: open binary and write the CRLF explicitly, which reproduces the committed bytes exactly (provable by the regen gate) while removing the OS dependence. **(b) exit-path asymmetry — established as LOAD-BEARING in both directions, not an oversight:** the standard path may force-exit because it has already flushed and closed everything by hand (`cout.flush()`+`fflush(stdout)`, or `outFile.flush()`+`close()`), so skipping the destructors cannot truncate it, and every error return happens BEFORE it (verified at the binary: unreadable score → exit 1, so the force-exit masks no failure code). The ~10 diagnostic paths may NOT adopt it: they rely on `std::ofstream`'s destructor at scope exit to write their output, which `_Exit`/`TerminateProcess` would SKIP — truncating it — and `--validate-slices` returns a MEANINGFUL 0/2 that a force-exit(0) would swallow. So unification is unsafe in either direction; the reason is now recorded at the force-exit. Verified side by side at the built binary on one score: standard → exit 0, 1704 CRLF / 0 LF; `--validate-slices` → exit 0, 0 CRLF / 17 LF, output complete. Whether the diagnostic returns are exposed to the same "some runs" Qt-TLS hang stays UNESTABLISHED (never observed); the code now states that if one is ever seen to hang, the fix is an explicit flush + a force-exit carrying that path's OWN exit code — never a bare force-exit(0). **The row closes as a #16 establishment item; the deferred corpus line-ending normalization is the named future obligation above (user's call).** |
