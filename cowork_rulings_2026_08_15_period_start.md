# User rulings — 2026-08-15: the restructuring period opens EXCLUSIVE at `b006dc15b5` (Alternative B), WITH the two attached checks

> **STATUS: RULING RECORD, an interim carrier (D-230).** Taken by the user in conversation on
> 2026-08-15, on the surface `ratification_surfaces/cowork_restructuring_period_start_decision_surface.md`,
> which was delivered in full as visible text before the question was put. Two answers, verbatim:
> **"B"**, and — asked whether the recommendation's two attached checks were ruled with it —
> **"as per your recommendation"**. **The classification — decision or exercise — is OWED and not
> made here**, and no register entry is written: the decisions register is under the eighteenth
> stop's filtering ruling and the usual queue route may not survive it.

## The first ruling — the period

**The restructuring period opens EXCLUSIVE at commit
`b006dc15b5f696f2fc86ad72b97fae58d2119cd7`** — D-231's ratification, authored 2026-08-02
00:00:30 +0200, read at the git object by explicit hash on 2026-08-15. Everything after that
commit is inside the period; the commit itself and everything before it is outside. The first
three in-period commits are `200a4e1087` (LF-normalization chore), `f833a2d2a9` (the homing
acts) and `ab336f43b5` (the truth-sync), a direct parent-child chain.

## The second ruling — the two checks are ruled with it, as the recommendation attached them

Quoting the surface's recommendation, which the ruling adopts: *"(1) A1's split re-derived by a
checked tool; (2) the July screen — the pre-S4 specification-bearing flagged hunks read
individually, with the falsification rule stated now: if any shows a code-influenced correction,
the period question RE-OPENS."* **Both checks run BEFORE Ruling 13's marking runs.** The
falsification rule is LIVE: a positively code-influenced correction found among the pre-S4
specification-bearing candidates re-opens the period question, and that re-opening is the user's
act on the screen's report.

## What follows mechanically, adopted from the surface the rulings were taken on

- **Ruling 10's pre-pollution baseline is the tree at `b006dc15b5`.**
- **Ruling 13's marking of open-items rows reads this period** when that act runs; it has not
  run, and it does not run before the two checks have.
- **Ruling 12's sizing reads this period.**
- **The published enumeration is NOT re-run and keeps its S1 floor**: the in-period candidate set
  is the S4 stratum filter over `tools/audit/doc_change_candidates.json`, which that artifact
  states of itself requires no re-derivation.

## What is NOT ruled or done, and must not be assumed

- **The checks are ruled but NOT RUN.** Until they have run, no act treats the July strata as
  screened, and Cowork's ad-hoc per-stratum numbers carry no authority — the checked tool's
  output supersedes them wherever they differ. The executing dispatch is
  `cc_instruction_period_checks.md`.
- **No repair is authorized** — the reconciliation still waits on the phase re-ruling and the
  pilot. **No row is marked.** **No register entry is written.** D-231's rephrasing, the new
  phases, the pilot's file, the register filter and the `src/` comment marking remain owed
  exactly as the eighteenth stop leaves them.
- **The handoff's entry block is not yet updated** to carry these rulings; until it is, this file
  and the eighteenth-stop record together are the on-disk carriers.

*Provenance: Cowork, 2026-08-15, recorded in the turns the rulings were given. The commit
identities and their chain were read at the git objects by explicit hash on the same date; the
surface's own premise ledger (F1/F2, A1/A2, carried caveat A3) governs every count referred to
here.*
