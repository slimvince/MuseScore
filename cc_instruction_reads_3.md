# CC dispatch — READ WAVE 3: ratify wave 2's entries with four riders, then read

> **Status: ACTIVE DISPATCH, written 2026-08-04 (Cowork).** Read IN FULL.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_reads_3.md`.
>
> **★ WHY THIS WAVE CARRIES NON-READ WORK, AND WHY IT IS NOT A BREACH OF THE FREEZE.** D-230 puts a
> ruling in the commit that records it, so read wave 2's ratification belongs here. The four riders
> are **recording what a ratified fact means, at the surface that owns it** — phase 1's own truth
> work. **No tool is built, no tool is fixed, no guard is repaired.** That freeze is untouched, and a
> tool defect found here is rowed and left.
>
> **★ NO FIGURES (D-431) AND NO STATE NOT READ (#17a).** No `src/`, no goldens, no corpus, no
> behaviour change, no fix to inference, no design. Phase 1 under D-231.

## 0a. THE RULING LEDGER

- **R1 — RULED by the user, 2026-08-04.** The read-wave-2 entries **D-469…D-501 are ratified as
  drafted**, each keeping the status the record states, several of which read *"not stated"*.
  Ratification confirms the register records the decision correctly; it is not an endorsement of the
  decision and not a conformance finding.
- **R2 — RULED, same act.** D-474's consequence is **recorded where principle #21 lives**: the
  ground-truth ceiling cannot be cited from the literature and would have to be measured here.
- **R3 — RULED, same act.** D-475's **establishment path is recorded as an owed act**, naming the
  part only the user can perform.
- **R4 — RULED, same act.** D-492's recommendation is **marked as a phase-3 fix-plan input**, and the
  override's arm is **checked** rather than assumed.
- **R5 — RULED, same act.** The D-501 conformance question is **rowed as gating**, and not run here.

**None of R2–R5 authorizes a fix, a design, or an inference change.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — read at the object by Cowork this session:**

- **F1.** D-474 is a FACT-of-absence: no published study reports per-axis inter-annotator agreement
  for Roman-numeral or key annotation of this repertoire; TAVERN published no number, ABC split
  pieces with no overlap by design, the Mozart corpus is consensus-built, *When in Rome* states the
  variance is unmeasured, Dilemmadata computes nothing yet — `decisions/group_C.md:900-908`.
- **F2.** D-475 names its own establishment path: the **JEP:HPP Method section** and the **dataset
  zip's headers** are the two places that would settle annotator identity and validation; the zip is
  **fetch-blocked in the working environment and downloadable on the user's machine** —
  `decisions/group_C.md:918-926`.
- **F3.** D-490's finding: the override's harm rate is **flat** against both quantities its threshold
  is built from, so no threshold separates corrections from harms, and the corr−harm-maximizing
  measurable bar **drops fires to zero** — `decisions/group_H.md:1119-1127`.

**ASSUMPTION — each checked before the act it licenses:**

- **A1. Cowork has NOT read D-469.** Its index row fell outside the query Cowork ran, so the entry is
  unexamined. → **Task 1.1**, before the ratification includes it.
- **A2.** That the fine-grain function override is on the **legacy** path rather than reachable on a
  production surface. **This is Cowork's inference from the joint estimator being production, not a
  read fact.** → **Task 4.2**.
- **A3.** That principle **#21** is where the ground-truth-ceiling rule lives and is numbered as
  Cowork believes. `CLAUDE.md` has been edited repeatedly since. → **Task 2.1**.

## 1. Task 1 — Ratify (R1)

**1.1 Read D-469 first** and report what it is. If it is anything other than a routine registration
of a decision at the record's own status, **report it before ratifying** — Cowork did not put it to
the user because Cowork did not read it.

**1.2** Ratify **D-469…D-501** as drafted, each keeping the status the record states. Record per the
register's existing entry-ratification convention; if that convention wants a form Cowork has not
anticipated, follow the convention and report the difference.

**1.3 Do not re-ratify what was already ratified.** D-494…D-500 register **amendments the user
ratified at the architecture review**. Ratifying the entry records that the register transcribes them
correctly; it does not make them fresh decisions. Say so in the ratification record so nothing is
double-counted.

## 2. Task 2 — Record D-474's consequence (R2)

**2.1** Check A3 — locate the ground-truth-ceiling principle at its own text and confirm its number.

**2.2** Record, at that principle: **the ceiling cannot be cited from the published literature (F1)
and would have to be measured here.** The principle's own words are that without the ceiling,
structural error and annotator disagreement are indistinguishable in the residual — so this fact
changes what may be claimed about any residual, and that is why it belongs at the principle rather
than only in a row.

**2.3** Update the ground-truth-ceiling row accordingly: it is not "a measurement not yet built" but
**the only available route**, because citation is unavailable. Cite D-474.

## 3. Task 3 — Record D-475's establishment path (R3)

Record, on the row that owns the second annotation set, the **owed establishment act** and what each
part requires (F2): the JEP:HPP Method section, and the dataset zip's headers — **the latter
downloadable only on the user's machine.** Name it as the user's act, plainly, so it is not waiting
on a session that cannot perform it.

Record also the two defects that survive establishment and must be handled in any measurement design:
the annotations sit on a **homorhythmic reduction** (a unit mismatch with full-texture grading), and
they arrived through a **machine translation** whose noise would appear inside any measured
disagreement.

## 4. Task 4 — Mark D-492 as a phase-3 input, and check the arm (R4)

**4.1** Mark D-492's recommendation — demote the override to an annotation carrying the earlier
reading unchanged, floored by disabling it — as an **input to the phase-3 fix plan**, cross-referenced
from where that plan will be assembled. **Do not act on it.** D-231 forbids the fix and D-490's
falsification does not change the sequencing.

**4.2 Check A2 at the code**: is the fine-grain function override on the legacy path, or reachable on
a production surface? **Say which, with the evidence**, and record it on the entry. Cowork's guess
that it is legacy is a guess, and the difference decides whether a measured-harmful mechanism is
running today.

**If it is reachable on production, STOP and report** before continuing — that would be a measured
harmful mechanism on the live path, which is a different situation from a falsified legacy pass.

## 5. Task 5 — Row the D-501 conformance question (R5)

Row it as **gating**: does anything read a written chord symbol as input that influences what the
analyzer computes? Under D-438's test the subject bears on the analysis's inputs, so it gates rather
than sitting with the apparatus rows.

**Row it; do not run it.** The check belongs to phase 2's program.

## 6. Task 6 — Read

Read documents **in the regime's order**, in full, for as much capacity as the ratification leaves.
Per document, exactly as read waves 1 and 2: entries at the record's own status, *"not stated"*
expected; actual yield against the registered band recorded **in this wave's own artifact**; OI-232 /
OI-274 / OI-276 / OI-279 / OI-315 class findings **rowed, not corrected**; decisions homed in
findings-recording sections noted for OI-296.

**Do not write yields into the regime artifact** — OI-316, and it is now three waves deep.

**On the bands:** read wave 2 measured that all eight of its documents fell in one length tercile, so
the proxy made one prediction for yields differing by an order of magnitude. **Report band and point
both ways, and do not describe a band pass as validating the proxy.**

## 7. Task 7 — Close

Guards at the committed tree with the list derived by `gen_guard_state.py`; report the pre-existing
failures as moved or unmoved and **fix none**. Where this wave's own edits drift an anchor, re-aim per
citation — completing the edit, not fixing a rowed defect. Verify the commit through
`tools/audit/changed_paths.py`. Run `tools/audit/process_check.py` over this dispatch.

Update the OI-207 note with the new read count and the measured yield. **Do not revise the remaining
estimate without a measured basis.**

## 8. Accepted outcomes

**A1 turning up something other than a routine registration is a success** — it means the user is
asked before it is ratified rather than after. **A2 coming back "production-reachable" is a STOP** and
the most valuable thing this wave could find. **A short read half is acceptable**; the ratification and
the riders are the wave's first duty and the reads take what is left.

## 9. Self-check (D-434) — run by Cowork before release

- **Ruling ledger.** Five rulings stated, with R2–R5 explicitly not authorizing any fix.
- **#17(a).** Three facts read at the entries themselves. Three assumptions — A1 is an admission that
  Cowork put an entry to the user without reading it, A2 is labelled a guess about a live-versus-
  legacy question of exactly the kind that has been wrong before, A3 because `CLAUDE.md`'s numbering
  may have moved.
- **Principles.** #21 — Task 2 puts a fact about the ceiling where the principle that needs it lives.
  #19 — Task 3 records an establishment path rather than treating the corpus as usable. #231/#13 —
  Task 4 marks a falsified mechanism for the fix plan and stops if it turns out live. D-438 — Task 5
  classifies the D-501 check as gating, not apparatus.
- **Freeze.** No tool built or fixed; the riders are recording acts at owning surfaces.
