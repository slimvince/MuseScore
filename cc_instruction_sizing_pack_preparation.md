# CC dispatch — land the method-ruling and sizing-pilot records; render the `scoring-model` boot pack with an EMPTY withheld family; deliver its leak list as a reading file; boot NO session

> **Dispatch (Cowork, 2026-08-24, the forty-fifth Cowork session).** Executes Ruling 1 and §4(1)
> of `cowork_rulings_2026_08_24_sizing_pilot_sitting.md`, and lands
> `cowork_rulings_2026_08_24_method_ruling_sitting.md` (the pilot's closing ruling — landed, not
> acted on: it orders nothing you execute). **Written at a verified stop: the comparison batch's
> four commits are landed and verified, the method is ruled ESTABLISHED, and nothing is
> running.** The tip was read by the writing side with `git for-each-ref` at the explicit hash
> `9264b968b7` and at nothing else.
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** Task 0 lands TWO sitting records, the modified
> handoff and this dispatch, and PUSHES. Task 1 authors the `scoring-model` subject into the
> boot-pack generator's AUTHORED tables with an EMPTY withheld family, renders the pack, and
> writes the leak-list reading file — ONE commit. Task 2 closes. **THIS BATCH BOOTS NO SESSION,
> DERIVES NOTHING, COMPARES NOTHING, AND TOUCHES NOTHING OF THE HARMONY-BOUNDARY SUBJECT** — its
> pack, family, reading file and tabulation are byte-unchanged or the batch STOPs.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The sizing pilot* — the ruled second
> subject of the pilot (Ruling 3 of the successor-plan sitting): `docs/scoring_model.md`, the
> hardest implementation-blind case, derived blind by a later session to produce the sizing
> record every later unit's budget is set from. *The boot pack* — the rendered directory such a
> session reads; this batch renders the `scoring-model` subject's. *The withheld family* — the
> per-subject authored cuts; **for this subject it is EMPTY by ruling** (no oracle exists for
> it). *The leak check* — the standing per-entry test that keeps an entry whose rendered fields
> carry a `docs/` or `src/` path or the string `ARCHITECTURE.md` OUT of the generated member,
> listed instead. *The authored tables* — the per-subject structures inside
> `tools/audit/gen_derivation_boot_pack.py`, corrected by hand under a ruling record.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md`
> — CONDITIONAL, and this batch MEETS the condition: read it in full; rule (a)'s `gating_ids`
> only. **(2)** `cowork_handoff.md`, ITS CURRENT (FIFTY-SECOND) ENTRY. **(3)**
> `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)** Both landed sitting
> records, whole: `cowork_rulings_2026_08_24_sizing_pilot_sitting.md` (this batch's authority)
> and `cowork_rulings_2026_08_24_method_ruling_sitting.md` (context; nothing in it is yours to
> execute). **(5)** The whole of `tools/audit/gen_derivation_boot_pack.py` — you edit its
> authored tables and, under the two licenses below, possibly two bounded rendering spots.
> **(6)** `ratification_surfaces/cowork_withheld_family_harmony_boundary_reading.md` LIST FOUR
> (the shape your leak-list reading file takes). **You do NOT read:** `docs/scoring_model.md`
> beyond what the leak check's own mechanics touch (it is the sizing subject's untrusted source
> and no act of this batch needs its content); the blind output beyond nothing at all; any
> oracle document.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS
> built, designed, scoped or run, no design, no repair, no derivation of any specification
> statement, NO SESSION BOOTED, no document archived, moved or deleted AS A FILE, **no
> open-items row created, flipped or discarded**, **no edit to any governing document, any
> register entry or any register source**, **no change to the generator OTHER THAN (i) the
> authored `scoring-model` subject entry and (ii) the two LICENSED accommodations named in Task
> 1(b) — the criterion, the leak check's tests, `withhold_passage()`, the marker and every STOP
> are otherwise untouched**, **no edit to any `harmony-boundary` authored entry, rendered file
> or manifest record** (its manifest subject block must be byte-identical in the re-derived
> manifest; a difference is a STOP-and-report).
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (CHECK mode) and
> again at the end (write mode, committed as the artifact of a real run). Verify every commit at
> the object by explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and
> `git diff` over the working tree time out on this mount and are denied by the armed guard —
> use the per-path explicit-hash forms named in A1. If staging is refused by a stale zero-length
> `index.lock` with no git process running, establish the cause (D-669), remove it, report the
> act. Plain single-quoted `git commit -m '…'` subjects. The reserved-word and vocabulary rules
> bind every line this batch writes.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1 of the sizing-pilot sitting:** *"a second subject, `scoring-model`: no withheld
  identities, no withheld documents, no withheld passages … The standing leak check does the
  whole of the cutting … The leak list goes to the user as a reading file in the shape List Four
  took. Members (1)–(4) and (6) render whole; member (2) carries NO withheld passage for this
  subject."*
- **The two licenses, §4(1) of the same record, quoted:** *"(i) if the tool's authored shape
  cannot express an empty family or an empty candidate criterion without a minimal change, that
  minimal change is licensed and declared — it is this ruling's own execution; (ii) the read-me's
  what-was-cut section renders truthfully for a subject with no withheld entries and no withheld
  passages."*
- **Ruling 3 of the successor-plan sitting (standing):** `docs/scoring_model.md` is the sizing
  subject, its dormant-content banner travelling with it as a property. Nothing in this batch
  opens it.

## Premise ledger

- **FACT** — the branch tip is `9264b968b7` (parent `b4b713d0c1`), subject opening *"end state:
  the guard set run at the tree the close left"*; both refs at it. Read by the writing side at
  the explicit hash on 2026-08-24. **Nothing is unpushed.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 75, passing 74, failing 1
  ([[OI-372]]'s tool), not_run 4, historical 16. **Re-read it yourself at the object.**
- **FACT** — at the tip, `derivation_boot_pack.json` carries ONE subject, `harmony-boundary`,
  whose `counted` reads `design_intent_class 244 … withheld_identities_total 33,
  withheld_passages 2, leaks 3, design_intent_entries_rendered 208, files_in_the_pack 7`.
  **Its whole subject block is predicted BYTE-UNCHANGED in the re-derived manifest.**
- **FACT** — at the tip, `evidence_pin_membership.json` → `ruling_records_read` is 54.
- **FACT, measured by the writing side:** both records to land are on disk and absent from the
  tip; NEITHER carries the word `pinned` on any line (`grep -c`: 0 and 0).

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications: **(i)**
  `cowork_handoff.md` — ONE entry (the fifty-second) inserted above the committed content, the
  fifty-first heading marked superseded-as-entry-point, otherwise additions only. **A
  modification at any OTHER tracked path is a STOP-and-report.** Untracked and to be landed by
  Task 0: `cowork_rulings_2026_08_24_method_ruling_sitting.md`,
  `cowork_rulings_2026_08_24_sizing_pilot_sitting.md`, and this dispatch
  (`cc_instruction_sizing_pack_preparation.md`) — **three untracked paths.** The rest of the
  untracked population is NOT landed and is outside A1's STOP. **Check ordered (Task 0, first
  act):** the per-path explicit-hash forms as every predecessor took them, and the whole tracked
  population enumerated with the sanctioned enumeration tool.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  The reds this batch causes and must clear: **the membership check, cleared by Task 0 step 2**;
  **the boot-pack generator's own `--check`, red between the table edit and the render, inside
  Task 1's SAME commit**; **`gen_session_start_read_size.py --check` at the close**. Any other
  red is classed by its measured cause; one outside this batch's own acts' subjects is a
  STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration.** **Route A** — the ruling-record count rises by
  exactly TWO (54 → 56) and exactly two names are added (the method-ruling and sizing-pilot
  records). **Route B** — neither record carries `pinned` on any line. **Route C** — no tool
  added. Measure the diff against the committed blob at `9264b968b7` BEFORE accepting; an
  additive derived cross-reference from the records' own naming of other documents is reported
  rather than absorbed; movement beyond is a STOP-and-report.

- **ASSUMPTION A4 — the guard registry.** No tool added (the generator gains a subject, not an
  invocation — **if its registered invocation is per-subject and the registry therefore moves,
  that is a STOP-and-report, not an edit to make**). `guard_state.json`: population UNCHANGED at
  run 75; failing set unchanged. The three other registry artifacts unchanged except run stamps.

- **ASSUMPTION A5 — the manifest and the pack after Task 1.** The manifest gains ONE subject
  block, `scoring-model`; the `harmony-boundary` block is BYTE-UNCHANGED. The new block's
  `counted`, predicted where the ruling fixes it and measured where it does not:
  `design_intent_class` 244; `withheld_identities_authored` 0; `withheld_identities_derived` 0;
  `withheld_identities_total` 0; `withheld_documents` 0; `withheld_passages` 0; `candidates` and
  `verdicts` — the empty shape the licensed accommodation admits (all zero); **`leaks` MEASURED
  — the leak check runs over the whole class of 244; report the figure at the artifact; the
  three harmony-boundary leaks (D-270, D-296, D-440) are predicted to recur in it, and every
  further member is measured, not predicted**; `design_intent_entries_rendered` = 244 − leaks,
  computed at the artifact; `files_in_the_pack` 7. The new directory
  `tools/audit/derivation_boot_pack/scoring-model/` holds the read-me and six members; members
  (1)–(4) and (6) byte-identical to the harmony-boundary pack's ONLY where their sources and
  spans are subject-independent — **measure, do not assume**; member (2) carries ZERO withheld
  markers; the read-me's unit sentence names the sizing subject (the wording is the brief's to
  refine later; render the subject name the authored entry carries) and its what-was-cut section
  states truthfully that for this subject no register entry and no passage was withheld and that
  only leak-listed entries are absent — the license (ii) edit, derived from the counts, true at
  any future subject. **The harmony-boundary pack directory is byte-unchanged, every file.**

## The declared start state — at the tree you will meet

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing
   red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`** STALE — caused by this dispatch's own
   TWO untracked ruling records.

**`gen_guard_state.py --check` opens "STALE vs the run" for the same cause (F83); not a third
failing check.** A THIRD failing verdict is a STOP-and-report.

## Findings in this batch

**No finding number is allocated; the series stands at F88.** Ruling 9's routing binds. A
paraphrase-shaped leak you happen to MEET in spans you touch is SURFACED with its verbatim and
location — not cut, not ruled. **A tool shape the licenses cannot minimally accommodate is a
STOP-and-report with the shape described — never a wider rewrite.**

## Task 0 — land the two records, clear the membership check, and PUSH

1. **A1's check first, entirely at content-addressed objects.**
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob BEFORE
   accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_24_method_ruling_sitting.md`,
   `cowork_rulings_2026_08_24_sizing_pilot_sitting.md`, and
   `cc_instruction_sizing_pack_preparation.md` (new) — **five paths.** Subject, exactly:
   `record: the pilot closes ESTABLISHED and the sizing pilot is designed — the 2026-08-24 method-ruling and sizing-pilot rulings landed; evidence-pin membership regenerated`.
4. **PUSH**; verify at the object.
5. **`gen_evidence_pin_membership.py --check` must PASS.**

**E0:** the five paths; `ruling_records_read` 56; the check passing; `origin/master` at the
commit.

## Task 1 — the `scoring-model` subject authored, the pack rendered, the leak list delivered — ONE commit

**(a)** Author the `scoring-model` entry in the generator's AUTHORED tables: subject name
`scoring-model`; its plain-words subject line naming `docs/scoring_model.md` as the unit whose
specification a later blind session derives; an EMPTY withheld family (no identities, no
documents, no passages); the empty candidate criterion and verdict set in whatever minimal shape
the tool's STOPs accept.

**(b)** The two LICENSED accommodations, each only if actually needed and each declared in your
report with its whole diff hunk quoted: (i) the minimal change letting the authored shape express
an empty family/criterion; (ii) the read-me's what-was-cut rendering made truthful at zero (and
still truthful at the harmony-boundary subject's two-kinds state — re-render BOTH subjects and
verify the harmony-boundary read-me byte-unchanged, which is the check that (ii) is derived and
not hardcoded).

**(c)** Run the generator for the new subject; run `--check` over both subjects — a halt is a
STOP-and-report.

**(d)** A5, measured at the manifest and at both pack directories, every item.

**(e)** Write `ratification_surfaces/cowork_sizing_pack_leak_list_reading.md` in LIST FOUR's
shape: what the leak check is, its scope (the two generated members), the full leak list with
each entry's id, title, field and matched string, the statement that none of this withholds
anything (the family is empty by ruling) and that the user is asked only whether the check keeps
excluding them for this subject; and what the file does not do.

**(f)** Run the FULL guard set. A2, A4, A5 must hold. Commit the generator, the manifest, the
new directory, the reading file, and the guard artifacts the run rewrote, subject:
`the scoring-model boot pack rendered with an EMPTY withheld family — the leak check alone cuts; the leak list delivered for ruling; no session booted`.
Push; verify at the object.

**E1:** the new subject at A5; both `--check`s green; the harmony-boundary subject block and pack
directory byte-unchanged; the reading file whole in the ruled shape; the licensed accommodations
(if taken) declared with quoted hunks; no session booted.

## Task 2 — the close

1. One `STATUS.md` pointer entry per task that did work; the previous batch's entries move
   through `gen_status_batch_bound.py --apply` (D-648, licensed in terms);
   `gen_session_start_read_size.py` regenerated (A2).
2. The FULL close appended to `cowork_away_returns.md` as a **THE PILOT CLOSED ESTABLISHED AND
   THE SIZING PACK RENDERED** section — both guard states, every SHA, E0–E2 graded, A3–A5 graded,
   declared departures, quarantined questions (or none new), the plan lines (**the user rules the
   leak list; the sizing brief is written and validated by the writing side; the fresh Cowork
   session derives; the tests (a)+(b) batch; the ledger before the framework phase**), and the
   plan's tell in one sentence. The report is `cc_report_sizing_pack_preparation.md`; hashes a
   commit cannot contain point at the git log.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **The ordered
   structure yields FOUR commits.**

**E2:** at the tree carrying the close, a fresh full guard run — every run check passing except
[[OI-372]]'s tool, zero STOPs, the population at run 75 — committed only after the run that
produced it.

## What this batch does NOT do

- **No session booted. No derivation. No comparison. No oracle involved. Nothing of the
  harmony-boundary subject touched.**
- **No edit to the generator beyond the authored entry and the two licensed accommodations; no
  edit to any governing document or register source; `docs/scoring_model.md` not opened.**
- **No finding number.** [[OI-179]] OPEN and GATES; [[OI-372]], [[OI-374]] stand.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule
  (c). No `src/` change, no golden, no test, nothing under `tools/corpus/` or
  `tools/robust_stop/`.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #19 — nothing is withheld without a ruled reason, and the empty family
   is itself ruled; #6 — one generator, one authored home, both subjects derived from it, and
   the license (ii) check (the harmony-boundary read-me byte-unchanged) is what proves the
   rendering derived rather than duplicated; #12 — the harmony-boundary subject byte-unchanged
   everywhere, the leak list delivered rather than absorbed; #17(f)/D-431 — the leak count
   measured at the artifact, never predicted into prose; #13 — a tool shape the licenses cannot
   minimally reach is a STOP surfaced, never a rewrite. Conforms.
2. *Conventions:* American English; no self-invented labels; music-theory words in their musical
   sense; towards, never against.
3. *Figures and premises:* the tip, guard summary, membership count and the harmony-boundary
   `counted` block read at the objects by the writing side on 2026-08-24 and ordered re-read;
   the leak figure explicitly measured-not-predicted.
4. *File-tools rule:* the writing side's own departures are in the fifty-second handover entry.
5. *Uncertainty:* the measured-not-predicted items are the leak count, the member byte-identity
   across subjects, and any licensed accommodation's necessity — each declared as such.
6. *Re-read from disk before release:* performed over the delivered file, hash-verified after
   re-staging (F74), recorded in the fifty-second handover entry.

---

*Provenance: Cowork, 2026-08-24, the forty-fifth session, written at branch tip `9264b968b7`
under Rulings 1–3 and §4(1) of `cowork_rulings_2026_08_24_sizing_pilot_sitting.md`. TOWARDS the
ultimate objective and TOWARDS the guiding principles.*
