## COWORK SESSION CLOSE (NINETIETH ENTRY, 2026-09-02 — THE CONTINUING LINE) — ★★ **THE COMPARISON INSTRUCTION EXISTS.** `cc_instruction_comparison_l0_l1_2026_09_02.md` is on disk at the repository root, UNTRACKED, written under **Ruling 32** (§3am of `cowork_rulings_2026_08_31_decision_surface_sitting.md`), which fixed the outgoing L0/L1 population. **The user opens it with Claude Code; this side does not.** ★ **THE BRIEF IS BACK AT THE ROOT** — the user had a copy and restored it; verified at the file, 41,093 bytes, carrying every amendment the eighty-ninth entry described. ★ **NOTHING IS IN FLIGHT until the user opens the dispatch.** THE CURRENT ENTRY POINT.

You start clueless. Read this block first, then perform the ordinary session-start read: `CLAUDE.md`
whole, `DECISIONS.md` whole, `STATUS.md`, and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`). **A
single-file opening instruction is not an exemption from that read** (ratified 2026-08-29, P-1). **The
eighty-second through eighty-ninth entries remain binding except where this entry names a change**, and
every standing bar of the earlier blocks remains binding.

**On form.** Short. In conversation with the user, plain ordinary English — he said so again this
session, in the words of Ruling 21: *"decisions should be fact-based/checked, weighed against ultimate
objective and guiding principles, in plain language etc."*

### What this session was

The session that booted on `cowork_handoff_entry_eighty_nine.md`. It performed the ordinary
session-start read in full, read the derivation's §5, §6 and §7 in that order and **not its
statements**, settled the missing brief (the user restored it), took **Ruling 32**, wrote it to the
sitting record in the turn it was given, wrote the comparison dispatch, and verified both files on disk
by hash against what it wrote. **It derived nothing, compared nothing, dispositioned nothing, and landed
nothing in git.** It did not compact.

### ★ THE STATE YOU INHERIT

- **The plan stands at:** phase OPEN → the first derivation DELIVERED and untracked → the comparison
  instruction WRITTEN and untracked → **the comparison (the user's to start)** → the disposition
  surface, put by the writing side over the tabulation, one decision per turn → L2, sequenced by
  Ruling 10.
- **The dispatch's Task 0 lands:** the derivation, the amended brief, the sitting record (modified,
  tracked), the running order, the eighty-ninth entry and the dispatch itself, and PUSHES — **the local
  tip `4c9b7af5…` is THREE commits ahead of `origin/master` `f54995c0…`**, established at the refs and
  by `merge-base --is-ancestor` at the explicit hashes. **This entry file is NOT in that list** and is
  landed by a later dispatch, as every entry is.
- **The dispatch runs in stoppable batches, one commit per outgoing document** (D-672). If it returns
  stopped at a document boundary, the next dispatch resumes there; **the reading file's header and the
  report say which documents are done.**
- **Ruling 32's population, in one line:** the old Layer 1 and Layer 2 territory (two `ARCHITECTURE.md`
  sections, five root design documents), the four verified places where the current text describes an
  L1 output outside that territory (the Layer 5 cadence detector and its design document; the
  phrase-boundary design; the factorization's beat-strength, fermata and cadence factors), and every
  file a two-tier term search admits over the three prose classes of the ruled artifact inventory —
  with a **forty-file STOP** on the search's yield, which is a batch threshold and not a corpus figure.
- **The pack's withheld family for `l0-l1` is EMPTY BY RULING** (`tools/audit/derivation_boot_pack.json`
  → `subjects` → `l0-l1`), so the pilot's D-450/D-575 caveat has no counterpart here. Established at
  the artifact.
- **Guard state at the tip:** run 75, failing 8 — read at the object `4c9b7af5…:tools/audit/guard_state.json`;
  the on-disk copy is older than the tip. Not otherwise measured by this session.
- **Nothing landed in git.** No code opened; no measurement, golden, build or test; nothing under
  `tools/corpus/` or `tools/robust_stop/`; **no open-items row created, flipped or discarded; no
  decisions-register entry and no `D-NNN` allocated.** The workbook was never opened.

### What the record settled and this session applied without a question

Rulings 1–4 and §5 of the comparison-design sitting of 2026-08-24, whose grounds are general; the five
dispositions of the phase definition §0 and §3.4; the derivation's §7 and the pack's cut record as
inputs. **Deliberately not put: the L0/L1 split** — the derivation's §1 says they separate cleanly; the
comparison keys derived statements by number and does not need it; it returns at ratification. All of
this is recorded at §3am, not restated here (#6).

### The user-owed list

**New from this session:** none.

**Standing, from the eighty-ninth entry and unchanged:** the **§3 leak repair in the brief** (name the
class, not the content — this side's own defect) and whether **§8's instance** is repaired, which is
ruled text; the **rename of `00_READ_THIS_FIRST.md`**; the plainness repair; `.gitattributes` for
committed `.mscx` exemplars; the merged D-674 question; the three routed `FRAMEWORK.md` findings; the
corrections-batch scope; the commit-trailer form; the two candidate bounds; the two research-paper
binaries outside the ignore rule's reach; the shell surface's answer; marker enrolment of P-2..P-5.
**The eleven quarantined audit questions stay reserved to the AUDIT.**

**Owed as work, none of it blocking:** the four second independent extractions; the manifest's
`rendered_from` line for pack member (7); OI-229's terminology pass; Ruling 15's cadence item; **the
running order's own rewrite** (its header has demanded it since act D; the dispatch lands it as it
stands, as a record — rewrite it, do not patch it); and **the pack-composition finding of §3al.4,
which goes into L2's pack**, not into the comparison.

### Errors

**ONE COUNTED, caught by the user.** The first writing of the Ruling 32 surface carried two claims read
off the decisions-register index rather than checked at the files (where the cadence detector and the
fermata reasoning live in the current text) and one admitted guess ("there may be others"). The user
restated the form; the claims were then checked at `ARCHITECTURE.md`, `cowork_layer5_function_design.md`,
`cowork_phrase_boundary_design.md`, `cowork_joint_estimator_factorization.md` and the inventory's class
table, and the surface was put again with locations. **Same shape as the eighty-ninth entry's four: a
claim about an object this side had not opened.** Recorded at §3am too.

**What went right and is worth copying:** every claim in the dispatch's premise ledger was read at an
object or a staged file before it was written, and both delivered files were re-staged and hash-compared
against the written copies before this entry was written.

### The cadence for your session

0. **Boot as above, in full.** This entry file is UNTRACKED and you never land it yourself (the
   2026-08-26 role ruling).
1. **If the dispatch has NOT run:** it is ready at its path; the user starts it. Do not rewrite it
   unless he asks; if he asks for a change, edit it on disk and record the amendment at its foot.
2. **If the dispatch HAS run:** read Claude Code's FULL close in `cowork_away_returns.md` (never a
   summary), verify every commit at the git objects by explicit hash, verify the derivation's blob is
   byte-identical to the file this side read (its hash is in the report), and only then read the
   reading file. **Then put the disposition surface, one decision per turn, in the ruled form (D-249,
   Ruling 1, Ruling 21)** — start with the rows the report gathers under PROPOSALS, and write what he
   rules to disk in the turn he rules it.
3. **Verify at the object, not at a report** — including this entry.
4. **Close at a member boundary with nothing half-done**, and write the next entry before you close.
