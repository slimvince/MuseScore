# CC report — the 2026-08-22 step-zero return rulings EXECUTED, and the per-entry pass CONTINUED

> **Dispatch:** `cc_instruction_step_zero_exclusion_and_pass_continuation.md` (Cowork, 2026-08-22,
> the forty-first session), executing Rulings 1(a), 1(c) and 2(b) of
> `cowork_rulings_2026_08_22_step_zero_return_sitting.md` and Ruling 1 of
> `cowork_rulings_2026_08_22_dispatch_order_sitting.md`.
>
> **ALL FOUR TASKS RAN. TASK 2 IS A PER-ENTRY PASS AND IS STOPPED AT A RECORDED MEMBER BOUNDARY** —
> the standing clause admits that, and the stop is recorded below in the form that clause fixes:
> which members were completed, that the remainder is UNTOUCHED rather than partly worked, and that
> nothing is left half-edited.
>
> **NO STOP-AND-REPORT RULE FIRED.** Every red the batch met was classed by its MEASURED cause and
> every one of those causes is this batch's own act.
>
> **The bars held whole.** No `src/` edit. No golden. No test changed, moved or run. Nothing under
> `tools/corpus/` or `tools/robust_stop/`. No measurement of the ANALYSIS built, designed, scoped or
> run. No design, no repair, no derivation of any specification, no frame, no pilot act, no
> fact-gate admission, no ledger, no mining read. No document archived, moved or deleted AS A FILE.
> **No open-items row created, flipped or discarded.** **No screened document edited** —
> `ARCHITECTURE.md`, `docs/scoring_model.md` and every document-set member were read and never
> written; `STATUS.md`'s content was touched only by Task 3's own pointer entries and by the ruled
> forward-bound move. No edit to the delegation at `ARCHITECTURE.md:4265`, none to the
> document-governance clause, and no re-grading of any authored grade. No caller flag or candidacy
> acted on. **No finding number allocated; the series stands at F88.**
>
> **Figures are not transcribed here (D-431).** Every population, count, verdict and share is named
> as an artifact and a field, or stated as a DIRECTION with its artifact named (D-663). The only
> bare quantities below are commit identities and the counts of this batch's own acts.

---

## 1. The chain of commits, named as far as a sentence can name it

Eight commits, each pushed to `origin/master` and each verified at the object by explicit hash:

| # | commit | what it carries |
|---|---|---|
| 1 | `4f55fe802b` | Task 0 — the 2026-08-22 record landed; five paths; the push performed |
| 2 | `beccfa14f5` | Task 1 — the exclusion as an authored input; the three artifacts re-derived; the declared third pollution value |
| 3 | `810ff863c9` | Task 2 — verdicts authored, members 62–82 of `ARCHITECTURE.md` |
| 4 | `f33441940f` | Task 2 — verdicts authored, members 83–96 of `ARCHITECTURE.md` |
| 5 | `dc340d84fb` | Task 2 — members 97–107, and six of this batch's own earlier verdicts corrected |
| 6 | `979436429d` | Task 2 — members 108–125; `ARCHITECTURE.md` read whole |
| 7 | `74abd1b905` | Task 2 — eight further document-set members read whole |
| 8 | `311dee8d11` | Task 2 — `cowork_evidence_inventory.md` read whole |

**THE NINTH COMMIT IS THIS CLOSE, AND A SENTENCE CANNOT NAME ITS OWN HASH.** It carries this report,
the `STATUS.md` pointer entries, the ruled forward-bound move and the close section. **AND A TENTH
FOLLOWS IT**, carrying the end-state guard run — because the end-state run is taken *after* the
commit that carries the close exists, so its values cannot be written into that commit. That is the
E-ordering rule at both grains, and it is why this section names eight commits and declares two.

---

## 2. Task by task

### Task 0 — the record landed and the push performed

The branch rule was taken at the tip with `git show -s` and `git for-each-ref` at the explicit hash
and at nothing else: `refs/heads/master` and `refs/remotes/origin/master` both at `64e7d2fd7f`, the
dispatch **not started**, so it was mine to run.

**A1 was checked FIRST and entirely at content-addressed objects**, by the per-path commands A1
names, because the bare working-tree forms are denied by the armed guard. It **HELD**:
`cowork_handoff.md` carries the three inserted handover blocks against the tip's blob, with exactly
one deletion, which is the thirty-eighth heading marked superseded — the only one of the three
preceding headings that exists at the tip; and all three named untracked paths are present on disk
and absent from the tip.

**One fact about A1(ii) is reported because it is not what the dispatch predicted, and it is
narrower rather than wider.** A1(ii) declares `DECISIONS.md` modified by carriage returns only. At
the OBJECT LEVEL git reports **no modification at all** for that path: the repository normalises
line endings on the way in, so the carriage returns the writing side measured on disk do not reach
the index. The file was neither staged nor repaired, exactly as ordered; what changes is that the
possible third red A2 names for it could not arise, and none did.

**The membership regeneration was measured against BOTH halves of the prediction before it was
accepted, and the prediction HELD on both.** Route A: the ruling-record count rose by exactly two
and the two predicted record names were added. Route B: nothing — neither record carries the word
`pinned` on any line, checked with the file tools before the run, exactly as predicted. No member,
route, document, pin constant, state or count moved beyond those two additions.

Five paths were staged **by explicit path**, never by `git add -A` or `git add .`, and committed
with the subject the dispatch prescribes verbatim. `origin/master` moved to that commit, verified at
the object. `gen_evidence_pin_membership.py --check` **passes** at the resulting tree.

### Task 1 — the exclusion as an input, the re-derivation, the declared third value

**(a) The exclusion is an AUTHORED INPUT beside the grades, in the D-677 shape**, carrying its
finding, its date and its reason, with a STOP on an exclusion naming an ungraded or a non-admitted
target and a STOP on one lacking any of the three fields. It is applied to the MEMBER LIST and to
nothing else: the seed reconciliation runs over the unfiltered admitted set exactly as before, the
target keeps its grade, and its governing naming is still located and quoted at `ARCHITECTURE.md` on
every run. `--check` re-derives the set with the exclusion applied, which is what makes it an input
rather than a hand edit to an output. The derivation's own NAME states the exclusion (D-661), and
the former finding about the status surface stands in place marked OVERTAKEN (#12).

**(b) The widened population re-derived with NO edit to its generator**, from the new document set.

**(c) The declared third value is DERIVED and nothing in it is typed.** The period's bounds are read
from the candidate enumeration's own artifact; the authoring commit and its date come from `git log`
at those bounds. Two STOPs ride with it: a commit strictly inside the period that touches a
coverage-gap member contradicts the reason the enumeration gives for itself, and the two readings
disagreeing halts the tool rather than declaring anything; and a gap member the enumeration DOES
carry — enumerated with every changed passage PURE — halts it too, because the ruling declares
nothing for that case. Neither fired.

**A3's four routes were graded at the artifacts, and §3 below carries the grading.**

### Task 2 — the per-entry pass, continued and stopped at a member boundary

**The remainder was DERIVED FRESH from `july_screen.json` → `★_the_widened_screen.the_hunks` where
the verdict is the tool's unread default**, after the exclusion, and never from any report's account
of it. The order is the artifact's own — by document, then by commit, then by changed passage.

**Each passage was retrieved from its git object by explicit hash**, read at its own removed and
added text together with its commit's own account, and classified by the inherited method: the four
classes in their DECLARED ORDER, with POSITIVELY CODE-INFLUENCED applied FIRST so that a ratified act
cannot launder a correction made under it. Where that class was tested and declined, the verdict
says so and gives the reason; several are close calls and each records what was tested and what
decided it.

**Two members are section-scoped, and their passages were placed rather than judged.** For each such
passage the document's own heading structure was read at that commit's own blob and the passage's
post-image line range placed against it; a passage outside the sections the delegation names is
recorded `OUTSIDE NAMED SECTIONS` and NOT graded, which is the declared value for exactly that case.

### Task 3 — the close

One `STATUS.md` pointer entry per task that did work, and **in the same act** the step-zero batch's
entries moved verbatim to `STATUS_ARCHIVE.md` through `gen_status_batch_bound.py --apply`, **both
directions proven** by the tool: byte-present in the archive exactly once, absent from the must-read.
The declared `Last updated:` prefix adjustment is imported from its one home and not re-decided (#6).
No entry text was retyped; the move is byte-faithful by construction.

---

## 3. A3's four routes, each graded at the artifact

| route | verdict | what was measured |
|---|---|---|
| **1 — the document set** | **HELD** | The member count falls by exactly one and the removed member is `STATUS.md`; `members_by_limb` loses one limb-2 member; **`targets_ADMITTED` is UNCHANGED** and the seed reconciliation's miss rate is UNCHANGED, which is what proves membership moved and grading did not; every other member and every grade is byte-unchanged. The three further per-member tallies that moved — by delegation scope, by live-or-dormant, by declared establishment status — are the SAME ONE member counted in three more tables, and each moved by exactly one. |
| **2 — the widened population** | **HELD** | `size` falls by exactly the number of changed passages the previous artifact's `by_document` recorded for that key; `already_screened` is UNCHANGED; `NEW` falls by the same number; `by_document` loses the one key; the coverage gap is unchanged in membership and in every reason; **every field ABOVE the widened block is byte-identical**, measured by the first diff hunk falling strictly inside that block. The stratum, role, period and distinct-commit tallies moved, and each is the same removed passages counted another way — each of those tables sums to the new size. |
| **3 — the July screen** | **HELD** | The existing verdicts' digest is IDENTICAL before and after, checked at the tip's own object and at the regenerated artifact; the widened authored block is unchanged; the per-document table loses the one row; the unread total falls by the same number; the read share's denominator falls and the share rises; the failure signal stays `INCONCLUSIVE-AT-THIS-COVERAGE`. **Nine rows carry the third value, each with a commit and a date — not a tenth and not an eighth.** |
| **4 — the guard registry** | **HELD, with one declared departure** | No tool was added or removed, so the guard population is unchanged. `epoch_write_path.json` is UNMOVED. `recognizer_establishment_sort.json` went red and its cause was ESTABLISHED AT THE OBJECT BEFORE IT WAS TOUCHED (D-669) — §5, departure 1. |

**No movement outside these four routes was found at any point in the batch.**

---

## 4. The size reported BEFORE Task 2 opened

Stated as a direction with its artifact named, never as a transcribed value (D-663):

**The unread remainder after the exclusion is smaller than it was before it and still manifestly
beyond one working session** — larger than everything the step-zero batch authored in its two bounded
runs put together. Its exact size is at `tools/audit/july_screen.json` →
`★_the_widened_screen.counted.unread`, derived fresh at the artifact after Task 1 and before the
first passage was read.

Per the dispatch that is **not a STOP**: Task 2 is a per-entry pass and stops at a member boundary
with its stop recorded. It was said before the first passage was read, and it is what happened.

---

## 5. THE STOP, recorded in the standing form

**What was completed.** Every changed passage of **ten document-set members** now carries a verdict:
`ARCHITECTURE.md`, `cowork_bounded_context_design.md`, `cowork_confidence_contract.md`,
`cowork_evidence_inventory.md`, `cowork_layer3_keymode_design.md`,
`cowork_layer5_function_design.md`, `cowork_layer6_grouping_design.md`,
`cowork_phrase_boundary_design.md`, `cowork_stage5_fitter_design.md` and
`cowork_voiceleading_axis_design.md`. The members are named individually rather than counted,
because a subset published under a scope that names its members cannot be taken for the whole.

**What was NOT done, and its state.** Every remaining changed passage is **UNTOUCHED**, not partly
worked. Each carries the tool's unread default, which no hand set and which the tool forbids being
authored. **Nothing is left half-edited:** `--check` re-derives byte-identically at every commit of
this batch, the artifact is never behind the tool, and the report lists the unread remainder per
document. The seven members still carrying unread passages are
`cowork_joint_estimator_architecture.md`, `cowork_joint_estimator_factorization.md`,
`cowork_notation_adoption_increment.md`, `cowork_notation_output_contract.md`,
`cowork_prefit_gates.md`, `cowork_score_census.md` and `docs/scoring_model.md`.

**How a continuing session derives the remainder.** Freshly, from the artifact — never from this
report's account of it. The order is the artifact's own, and the unread set is exactly the members
whose verdict is the unread default at `july_screen.json` → `★_the_widened_screen.the_hunks`,
summarised per document in `july_screen_report.md`.

**Why it stopped here rather than one member further.** Task 3 binds harder: the close, the ruled
forward-bound move and the end-state run are not stoppable partway, and the boundary reached is a
clean one — ten members whole, none part-read.

---

## 6. The correction this batch made to its own work, reported rather than quietly applied

**What happened.** The first two bounded runs graded six re-homing passages in the not-cleared class.
The four classes are applied in the DECLARED ORDER, so that class is reached only where the first
three all fail; the second fires where a change records what a named user act ruled, ratified or
DIRECTED, and a homing act performed under a named user ruling is such a change.

**How it was established rather than argued.** The step-zero batch's own authored block was read at
a git object by explicit hash: over its sixty-one authored passages it used the not-cleared class
**zero times**, and its recorded near-miss states the test in terms — a code citation offered as the
DESIGN REASON for a decision does not make the first class fire, because the change's source is the
ratification the text names.

**What was done.** The six were re-graded, each keeping its near-miss reasoning in full and now
ending in the class the inherited method reaches. The one class-4 verdict of those runs that STANDS
is the self-check word correction, where the second class genuinely fails — no user act is recorded,
the change being the standing self-check's own product — and the third fails because the change
replaces a standing statement rather than relocating, splitting, re-heading or growing text.

**One line of the method was exercised for the first time in this batch and is stated once, at the
block it was forced by**, rather than at each verdict: a user direction to PERFORM a survey is not a
user act whose CONTENT the change records, so a catalog of what our own layers publish, silo, trap
and leave dormant — made at the user's direction — is not a ratified-act edit, while a homing act,
whose content is a decision the register already holds, is.

---

## 7. What the screen found so far, stated as directions

**The ruled failure signal did NOT fire, and it is not yet able to.** It is evaluated mechanically at
the artifact and reports `INCONCLUSIVE-AT-THIS-COVERAGE`, with the read share published beside it —
the ruling's own treatment of a majority reached on a part-read population. On the members read so
far the not-cleared class is **a small minority**, and it fell further as the pass ran; but that is a
direction over a part-read population and is not a verdict.

**On the members read so far the largest class is the ratified-act edit, and the code-influenced
class is concentrated in a small number of commits** whose own accounts state the direction of the
correction in terms. **This is the pollution the successor plan's founding premise names, measured
rather than assumed.** The exclusion did not move that reading: it removed a member whose passages
were all unread, so no verdict changed with it.

**★ A CODE-INFLUENCED PASSAGE HERE IS NOT THE PERIOD QUESTION'S FALSIFICATION**, and the artifact says
so in its own field. That rule belongs to the ORIGINAL screen, whose population is out-of-period by
construction. This population is deliberately wider because the ruling asks it to MEASURE the
pollution rather than to test the period, so an in-period code-influenced passage is the
measurement's own subject and is EXPECTED.

---

## 8. Quarantined audit questions

*Under Ruling 9 a finding that bears on the analysis, its inputs or a measurement tool is SURFACED
here — not rowed, not numbered, not acted on.*

1. **A SPECIFICATION STATES AN OPEN QUESTION ABOUT WHAT THE LIVE PATH ACTUALLY GROUPS OVER, AND THE
   TWO ARMS ARE RECORDED AS BEHAVIOURALLY NON-EQUIVALENT AND UNMEASURED.** Read while grading the
   key-area grouping passages: `ARCHITECTURE.md` first carried the grouping rule with a marked block
   saying the stabilization pass it names as a PRECONDITION has its only call site inside the legacy
   arm, and the question was later answered by a read-only probe — the record arm meets the
   requirement by a different design. **What rides forward is the correction's own clause:** the two
   designs do not produce identical per-region key sequences — the legacy step ERASES a one-region
   key island unconditionally while the decoder's key-transition term only makes one EXPENSIVE, so an
   island SURVIVES where the content evidence outweighs two key changes — **and that difference is
   stated UNMEASURED, with no corpus comparison taken.** This bears on the analysis and on what a
   later design may assume about the key sequence the grouping runs over; it is surfaced, not acted
   on.
2. **A SPECIFICATION RECORDS A CONFORMANCE GAP IN WHAT THE EMISSION READS, DECLARED AND NOT FIXED.**
   The evidential-priority section written for the production arm names, in its own
   what-this-does-not-do clause, that the pitch and bass emissions read the STRUCK set where the
   design says SOUNDING. The remedy is left to the one design over the whole family at its
   #8-correct stage. It is surfaced here because it bears on the analysis and because the passage
   that names it is now graded rather than unread.
3. **A SPECIFICATION RECORDS THAT THE IMPLEMENTATION AT HEAD CONTRADICTS A SHELVING WITH EVIDENCE,
   WITH NO RULING OVERTURNING IT.** The whole-score-versus-bounded-window shelving was homed with a
   marked block stating that the record producer takes no tick range and every record-arm seam
   analyzes the whole score. The homing act deliberately takes no verdict, which is the ruled form;
   the contradiction itself bears on the analysis and is surfaced.
4. **THE COVERAGE GAP'S NINE MEMBERS NOW CARRY A DECLARED VALUE THAT IS NOT A MEASUREMENT, AND A
   READER MUST NOT TAKE IT FOR ONE.** Ruling 2(b)'s third value says what it is not, in the
   generator's own words, and the report carries that sentence under the coverage-gap heading. It is
   surfaced rather than left in the artifact because the reading depth of every later unit is
   declared from this distribution, and a third of the document set contributes a declaration rather
   than a measurement to it.

**No finding here is rowed, numbered or acted on**, and no establishment obligation was met or
discharged by anything in this batch.

---

## 9. Findings DISCARDED under the worth test (#10), with finding, date and reason

1. **The report's per-document table gains a column whose cell text is long enough to make that
   table wide** (2026-08-22). *Reason:* a rendering property of a generated report, with no
   consequence for what is built or for whether code can be compared against a correct
   specification. The column is required by the ruling and its content is derived; the width is what
   carrying a declared text per row costs.
2. **Five commit hashes were written into the screen's authored block from their abbreviations
   before being resolved** (2026-08-22). *Reason:* caught and corrected before anything was run, and
   the tool's own STOP on a verdict naming a passage the population does not carry would have caught
   it otherwise. It is recorded as a departure in §5's own list below rather than carried as an
   issue, and it left nothing in the record.

---

## 10. Declared departures

1. **One artifact the dispatch does not name was regenerated and committed:**
   `tools/audit/recognizer_establishment_sort.json`. It went red between the start-state run and the
   Task-1 run. **The cause was ESTABLISHED AT THE OBJECT BEFORE THE ARTIFACT WAS TOUCHED** (D-669):
   that sort derives, per tool, the paths a tool READS AND DOES NOT WRITE, and Task 1 step (c) is
   ordered to add exactly such a path constant to the July screen's generator. The measured
   difference is **one added record naming that constant and nothing else** — no member entered or
   left the classification and no verdict moved. `epoch_write_path.json` was predicted unmoved and
   is unmoved.
2. **Five commit hashes were first written from their abbreviations and replaced with the resolved
   objects before the tool was run.** Reported rather than left in the diff; nothing was run against
   the placeholder values and nothing entered any artifact.
3. **The full guard set was run in `--check` mode rather than write mode until the end-state run.**
   `--check` runs every guard and does not rewrite the registry, which keeps the tree quiet between
   commits; the registries are written by the end-state run, which is what the tenth commit exists
   to carry.
4. **Task 2 is stopped at a recorded member boundary**, as §5 states.
5. **A1(ii)'s predicted difference does not exist at the object level**, as §2 records. Nothing was
   staged or repaired; the departure is that the dispatch's expected third red could not arise.

---

## 11. The registered expectations, graded

| | verdict | why |
|---|---|---|
| **E0** | **MET** | Exactly the five named paths; one content modification matching A1(i) and one bounded regeneration; three additions; no staging override; the membership check passing at the resulting tree; `origin/master` at the Task 0 commit. The guard set stands at one failing from Task 0 onward. A1(ii) caused nothing, as §2 records. |
| **E1** | **MET** | The exclusion applied to membership and to nothing else, with its finding, date and reason published in the artifact and the derivation's name stating it; every grade, every quoted naming and the seed reconciliation byte-unchanged; the widened population re-derived with the existing fields byte-identical and the coverage gap unchanged; the July screen re-derived with the existing digest identical and the widened authored block unchanged; the nine third values derived from git with the in-period check passing; all three `--check`s passing at the commit; `ARCHITECTURE.md`, `STATUS.md` and every member byte-unchanged. |
| **E2** | **MET FOR THE MEMBERS READ, AND THE REMAINDER IS DECLARED** | Every read passage carries exactly one verdict in the inherited vocabulary; the unread remainder carries the tool's default and no hand; the existing sixty-eight are byte-identical by published digest at every commit; the per-document distribution is published by the generator; the failure signal is evaluated at the artifact and stated either way; the stop is recorded in the standing form. **It is not met for the unread remainder, and that is the stop, not a shortfall.** |
| **E3** | **NOT YET ASSERTABLE HERE — BY DESIGN** | The end-state run is taken after the commit carrying this close exists. Its values land in ONE FURTHER commit. **No row, cell or sentence asserting the end state is written into the close commit.** |

---

## 12. The plan's tell, in one sentence

**Did this batch produce anything other than the exclusion input, the third-value derivation, the
re-derived artifacts, the authored verdicts and the report?** Yes, and it is named: one regenerated
guard artifact, forced by the registry's own derivation over a path constant the dispatch itself
orders added, declared in §10 rather than left for a reader to find.

---

## 13. The standing self-check (D-434) over this batch's own work

1. **The guiding principles.** #12 — nothing deleted: the grade, the quoted naming, the candidate
   enumeration's changed passages, the former finding text and the unused property block all stay,
   and the moved `STATUS.md` entries are byte-faithful in both directions. #6 — the exclusion has
   one home and the third value one home; the existing verdicts are not re-graded; the prefix
   adjustment is imported rather than restated. #17(f)/D-431 — no count transcribed: every figure is
   an artifact and a field, or a direction with its artifact named. #19 — the candidate generator's
   unestablished status is carried unchanged; the third value's in-period check re-measures the
   coverage-gap reason at the objects rather than inheriting it. #13 — the one thing that came out
   different from the dispatch's prediction (A1(ii) at the object level) is reported before anything
   rested on it. #24 — no comparison between measured quantities is asserted anywhere above.
   **Conforms.**
2. **The conventions.** American English. No self-invented labels: the new table, constant and field
   names follow the tools' own `AUTHORED` / `the_*` shapes, and every term of the tools' own is
   explained in plain words at its first use. "Hunk" appears only where it names a field or quotes a
   tool; elsewhere the plain phrase *changed passage* is used. Music-theory words in their musical
   sense only — *score* is never used numerically, *measurement tool* and never the reserved word,
   *measure* only as the verb or as *measurement*, *key* only as tonality, *register* only as *the
   open-items register* or *the decisions register* in full.
3. **The figures-and-premises rule.** Every quantity is named as an artifact and a field. Every
   premise is cited to the primary source it can be checked at — the classes and their order from
   the tool's own docstring, the calibration for the class order from the step-zero batch's authored
   block read at a git object, the period's bounds from the candidate enumeration's own artifact,
   each ruling from its own record.
4. **The file-tools rule.** Every read of working-tree content went through Read/Grep/Glob. The only
   shell reads of repository content were git OBJECT queries by explicit hash, and every tool output
   was written to an absolute scratchpad path outside the repository and read back from there.
   **The armed guard denied three commands and was obeyed rather than worked around** — a `python
   -c` carrying a repository path, a `sed` over a tool's source, and a `grep` over a generated
   report; each was re-taken through the file tools or through a git object.
5. **Uncertainty on any comparison.** No difference between two measured quantities is asserted. The
   one comparative statement — that the not-cleared class is a small minority on the members read so
   far — is stated as a direction over a named read share and explicitly not as a verdict.
6. **Re-read from disk before reporting.** Every generator's `--check` was run after its final edit
   and re-derives byte-identically; the diffs of the artifacts whose byte-identity is claimed were
   read, not assumed.

---

*Provenance: CC, 2026-08-22, executing `cc_instruction_step_zero_exclusion_and_pass_continuation.md`
under Rulings 1(a), 1(c) and 2(b) of `cowork_rulings_2026_08_22_step_zero_return_sitting.md` and
Ruling 1 of `cowork_rulings_2026_08_22_dispatch_order_sitting.md`. The reserved-word conventions and
the vocabulary rule of 2026-08-17 bind every line of this report — TOWARDS the ultimate objective and
TOWARDS the guiding principles.*
