# CC report — the preparation phase's TENTH batch

> **Dispatch `cc_instruction_preparation_tenth.md` (Cowork, 2026-08-17), executed by CC on
> 2026-08-18.** Every act of this batch was performed on 2026-08-18, the day after the rulings it
> executes were taken; every record it writes is dated accordingly and no earlier dated record is
> re-stamped. **The batch is COMPLETE.** Six task commits and one further commit carrying the
> end-state guard run, which the E-ordering rule requires.
>
> **Nothing in this report is a claim about the analysis.** No `src/` change, no golden, no test
> changed, moved or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of
> the analysis built, designed, scoped or run. **No open-items row was created, flipped or
> discarded.** Every figure below is cited to a generated artifact and none is transcribed
> (**D-431**).

---

## 1. The reading, the start state, and Task 0a

### 1.a The session-start reads, under the regime that moved at the ninth batch

`CLAUDE.md` and `STATUS.md` **as they now stand, in full**; `DECISIONS.md` in full, unconditional
and explicitly not demoted; `BUILD_AND_TEST.md` in full, because this batch MEETS its condition —
it runs the guard set, whose commands live there. **The whole open-items INDEX was NOT read**: rule
(a)'s derived gating answer was read at `tools/audit/nongating_apparatus_rows.json` →
`★_the_live_gating_answer`, and no archive companion was opened, because no decision was re-opened.

**And the file Ruling 2(c) adds to every read-first block from this dispatch onward was read:
`cowork_handoff.md`, its TWENTY-EIGHTH block.** That is what told this session the findings series
stands at **F59** — the fact whose absence caused F58.

Then, as the dispatch orders: `cowork_rulings_2026_08_17_ninth_return.md` whole;
`cowork_rulings_2026_08_17_session_start_read_sitting.md` **Ruling 1**;
`cowork_rulings_2026_08_16_preparation_return.md` **§6** and **§5(D)**; and
`cc_report_preparation_ninth.md` **§§2.d, 3.e and 10**. The three artifacts the tasks bind at were
read at their own text: `tools/audit/guard_classification.json`,
`tools/audit/nongating_apparatus_rows.json`, `tools/audit/guard_state.json`.

### 1.b The start-state guard run — exactly what the dispatch declares

Run **before the first edit**, at a quiet tree:

```
69 guard(s) run, 1 failing, 4 not run, 16 historical record(s)
  [FAIL] tools/audit/gen_filing_convention_application.py --check
```

and `gen_guard_classification.py --check` printed **"the guard classification re-derives"** — 68
passing, **zero STOPs**. That is the dispatch's declared expectation to the tool and to every count,
so no STOP-and-report was owed. The runner **rewrote its own artifact byte-identically**, proven by
re-running the path enumeration afterwards: the tree was unmoved by the measurement.

### 1.c Assumption A1, checked first and entirely at content-addressed objects

`tools/audit/changed_paths.py` reported **exactly ONE tracked modification in the whole working
tree** — `cowork_handoff.md` — with the ruling record and the dispatch untracked among the
already-known untracked population.

The difference was taken **blob against blob by explicit hash**: the committed blob at
`1760d9a4a8` (`5414ef4fdb`) against the working blob (`97c127a3f0`). It is **ONE contiguous changed
passage**, 163 insertions and 1 deletion, carrying both parts A1's own sentence names — the
TWENTY-EIGHTH session-close block inserted, and the TWENTY-SEVENTH heading gaining its entry-point
demotion marker. **A1 held at the shape its own sentence describes**, and no count of changed
passages was asserted in advance (the F25 lesson).

**The F57 caveat did not arise for that file and was not assumed away**: the premise ledger records
it measured LF, and git's own check-in normalisation warning on this platform confirms the working
copy is LF, so the two blob hashes are directly comparable without normalisation.

### 1.d Task 0a

Commit **`6bd9285006`**, parent `1760d9a4a8`, pushed, **3 paths**, verified at the object.
**E0a — MET.** Exactly 3 paths at the enumeration; one modification whose content matches A1's
stated shape, two additions; no staging override.

---

## 2. Task 0b — the two recorded corrections of Ruling 2(a) and 2(b)

Commit **`59191d7e65`**, parent `6bd9285006`, pushed, **2 paths**, verified at the object.

**(1) The F57 re-issue, at the site where the collision was created.** `cowork_handoff.md`'s
TWENTY-SEVENTH block gains a dated correction appended immediately beneath its own findings
paragraph, quoting the heading sentence it corrects **verbatim** and stating four things: F52 as
used there is **RE-ISSUED AS F57** with the former number preserved beside it (#12); **F52 belongs
to CC's finding** of the ninth batch; **F53 is ONE finding surfaced independently by both sides**,
not two, and the marking of it as *new* is corrected in the same act; and **the cause is F58**, with
Ruling 2(c) as its remedy. **The block's own text is not rewritten** — a handover block is corrected
by appending, on the pattern the ninth batch's own Task 0b used.

**(2) The note at the site a future reader meets the numbers.** `cowork_away_returns.md`, THE
PREPARATION NINTH BATCH section's §8, gains a dated correction appended after its last entry: F53
was independently surfaced by the writing side before that batch ran and is one finding, not a new
one; and F57 is the writing side's re-issued line-ending finding, which fired inside that batch at
its own §4.e. **Nothing already written in that section is edited** — it was true when written on
the information its author had, which is exactly what F58 records.

**E0b — MET.** Both sites carry a dated correction with the former number preserved beside it; no
finding's content, weight or population moves; no row is created, flipped or discarded.

---

## 3. Task 1 — Ruling 1: the pinned kind extended to its class

Commit **`b0596b288b`**, parent `59191d7e65`, pushed, **9 paths**, verified at the object.

### 3.a The class rule, verbatim, at the classification's home

`tools/audit/gen_guard_classification.py` carries each measurement tool's verdict and its ground,
and it now carries Ruling 1's sentence **word for word** as the class rule, with the general form
and the bound beside it. Three things are stated with it so a later session classifying a new
generator meets the whole rule rather than a slogan: it **adds no fourth verdict and moves none of
the three** — a pinned generator is still `re-derives-a-live-invariant` where what it asserts is a
demand about the tree, because the pin fixes WHAT IT READS and not WHETHER IT RE-DERIVES; it
**EXTENDS** §6 kind 1 of the 2026-08-16 record rather than replacing it; and the membership lives at
its own derived artifact and is not hand-listed at the tool.

### 3.b The membership, DERIVED and published

New tool `tools/audit/gen_evidence_pin_membership.py`; artifact
`tools/audit/evidence_pin_membership.json`. It reads the ruling records themselves. **Every
ratification document a tool WRITES** — and naming one is not writing it: the constant must also
reach a write call, which is what keeps a hand-written surface a tool merely CONSUMES out of the
map, a distinction the first run got wrong and the second measured. **Every root-level
`cowork_rulings_*.md`.** And what each record says **in its own two STRUCTURAL blocks** — its
leading blockquote and its provenance paragraph — that it was taken from; a structural test rather
than a vocabulary of phrases, so it cannot be widened by taste.

Two routes, both derived: **A**, a generated document the record names; **B**, a tool the record
itself names as PINNED, which is how the §6 kind-1 ruling pinned two tools that name no ratification
document at all. Route B resolves a naming against the tool population and resolves nothing where
the naming is ambiguous — it may not guess which tool a record meant any more than the commit
resolution may guess a commit.

### 3.c ★ The measured answer, and it is mostly a STOP-and-report

**Seven members. THREE already pinned, FOUR UNRESOLVED.** No pin was applied by this act.

- **`gen_claude_md_finer_surface.py` — PINNED at the commit a ruling record states**
  (`cfb69a7ecb`). **RECORDED under the class rather than re-taken**, exactly as the dispatch orders:
  it is already the commit the seventh-return record names, and re-taking it would move a value for
  no reason.
- **`gen_decisions_filter.py` and `gen_deciding_act_recovery.py` — PINNED by the route their own
  pin constants record.** They are §6 kind 1's own two tools and enter by route B. Recorded, not
  re-taken.
- **`gen_artifact_inventory_surface.py`, `gen_ratified_document_check.py`,
  `gen_governing_surface_readers.py` and `gen_rulings_sort.py` — UNRESOLVED.** Their ruling records
  name the document each was taken from and **NO COMMIT**. **ASSUMPTION A3 IS FALSIFIED FOR ALL
  FOUR**, and A3's own ordered check is a STOP-and-report, never a guessed pin.

**THE TWO RESOLUTION ROUTES CONSIDERED AND DECLINED, recorded because an excluded alternative is
evidence about the choice.** *The last commit that touched the document* — declined outright: it is
demonstrably wrong for at least one member, the rulings-sort surface having been hand-corrected
AFTER its sitting, so that route would pin the post-ruling rendering and record it as the evidence
of what was PUT, which is the precise defect Ruling 1 exists against. *The artifact's own
`derived_at_commit`, where one exists* — declined because it is the commit the DERIVATION read at,
not the commit the ruling was taken at; the two are different quantities, and Ruling 1's sentence
names the second.

The whole pinned population is published beside the members so no pin is invisible: **six tools
carry a pin constant, three of them outside this class**, each with its own constant's description
of what the commit IS. **No verdict is taken on those three** — whether a pin taken for another
recorded reason also belongs to this class is a question for the user, not a derivation.

### 3.d What the pin does NOT freeze, written at each pinned generator

At all three pinned generators, so it is not mistaken for the accepted ossification cost: the
UNDERLYING DATA FILE is **not** pinned and continues to re-derive — `gen_claude_md_finer_spans.py`
re-cuts `CLAUDE.md` on every run; the decisions register's data file is live at its own surfaces —
and what is fixed is ONE thing, the rendering of the document the user ruled from. **A later ruling
can unpin any member in one edit.**

### 3.e Nothing moved that must not move

No verdict, fate, population or row. **No document was regenerated to a new content**, and the
restore-and-report limb had nothing to act on: no path under `ratification_surfaces/` is touched by
this commit and none differs from its committed bytes, measured at the path enumeration.

**The new-tool rule was discharged in the same commit** — the authored run-instruction in
`gen_guard_state.py`, the authored classification verdict in `gen_guard_classification.py`. The
guard population grew by exactly that one invocation, **69 → 70**.

**E1 — PARTLY MET, and the unmet half is the dispatch's own provided outcome.** The class rule
stands verbatim at the classification's home; the membership is derived and published with each
member's ruling record and resolved commit named; **every derived member is pinned OR STOPped on**,
which is E1's own disjunction; no document's committed content moves; the guard classification
re-derives.

---

## 4. Task 2 — Ruling 3: rule (a)'s pointer narrowed to the answer

Commit **`594074e1e1`**, parent `b0596b288b`, pushed, **24 paths**, verified at the object.

### 4.a The clause

Rule (a) named the whole live-gating-answer section. It now names the **list of gating row
identities** inside it — that section's `gating_ids` — and states in the same clause that **the
grounds stand in the same file at `the_gating_rows` and are OPENED WHEN A VERDICT IS CHALLENGED,
exactly as the INDEX is opened when a row is needed.** Nothing was moved, deleted, split or
regenerated in the artifact itself; ONE clause changed. The sentence already at the site — the INDEX
remains the AUTHORITATIVE STATUS SURFACE, the derived answer a route to ONE question it answers and
never a second home for status (#6) — is **not rewritten**.

### 4.b The #19 distinction, written at the clause

What establishes the answer is what the GENERATOR proves on every run: reconciliation in BOTH
directions against the INDEX, every gating row carrying a ground, and a HALT on any row it cannot
place. **Narrowing what a SESSION reads does not weaken what the GENERATOR establishes.** The
grounds are not removed from the artifact, not made optional, and not downgraded as evidence. The
site states why the distinction is written rather than inferred: a pointer that names less than it
used to reads, at a glance, like a claim that less is proven — and the opposite is the case, the
establishment being what made the narrower pointer admissible at all.

### 4.c The archive move, both directions proven

The superseded wording moved **verbatim** to `CLAUDE_ARCHIVE.md` in the same act under the
continuous-pruning rule, with a compact dated pointer left at the site naming its companion. **Both
directions proven, the second being the load-bearing one:** the superseded pointer clause is
byte-present in the companion **exactly once** and **absent** from `CLAUDE.md`. Its closing half —
*"OPEN THE INDEX `OPEN_ITEMS.md` WHEN YOU NEED A ROW (and its detail file as needed)"* — survives at
the site because it is the **RETAINED** half of rule (a) and was never superseded. That is stated
rather than claimed away: a both-directions proof that reported total absence here would be false.

### 4.d The F15 anchored-quote class, cleared by its ruled treatment

**48 anchors re-aimed PER CITATION** from the drift report, then **six derivations regenerated**.
The drift authority `gen_cluster_dispositions.py --verify` now reports **474/474 verbatim quotes at
their cited home and 468/468 cited line numbers correct**, against 420/468 before.
`reaim_home_anchors.py --check` was NOT treated as the authority — F3's reading rule applied rather
than rediscovered.

**Every regenerated surface's own difference was MEASURED before it was accepted**, blob against
blob by explicit hash, on the ninth batch's own §3.e pattern: the rulings-sort surface moved 3 lines
and the decisions backbone 52, and **in both files every changed line carries a `CLAUDE.md` line
coordinate and nothing else**. `DECISIONS.md` itself did not move at all.

### 4.e ★ The measured effect, published and generated

`tools/audit/session_start_read_size.json`, new tool `gen_session_start_read_size.py`. **The
ordinary session-start read falls from 367,121 characters to 296,383** — **−70,738, −19.27 %** —
each value read from the files at explicit git objects and not estimated. The reading immediately
after the narrowing act itself is carried as its own baseline, so the act's effect and the record's
later growth read as two movements rather than one.

Three properties of the measurement are worth stating because they are what make it a guard rather
than a note. The **pointer is PARSED FROM RULE (a)'s OWN CLAUSE** rather than listed in the tool, so
a later narrowing or widening moves the measurement without anybody editing it, and **a pointer that
stops resolving in the artifact it names STOPS the run**. A **CONDITIONAL read is not a member** —
counting `BUILD_AND_TEST.md` would report a read no ordinary session takes. And the **comparison
says where its bound is**: before the ninth batch the regime named a different member set and rule
(a) named no artifact pointer at all, so this shape does not reach it and forcing it would compare
two different questions.

**The new-tool rule was discharged in the same commit** for the second time this batch. The guard
population grew **70 → 71**.

**E2 — MET.** Rule (a) names the answer and not the whole section; the grounds are reachable from
the clause; the #19 distinction stands at the site; the superseded wording is byte-present in the
companion exactly once and the pointer clause absent from `CLAUDE.md`, both directions proven; every
re-aimed anchor is verified at its new coordinate by the drift authority. **No row is created,
flipped or discarded and [[OI-179]] stays OPEN and GATES.**

---

## 5. Task 3 — Ruling 2(c): the read-first clause at its derived home

Commit **`b6134f41ff`**, parent `594074e1e1`, pushed, **4 paths**, verified at the object.

### 5.a ★ The home was DERIVED, and it is neither candidate the dispatch named — a DECLARED DEPARTURE

Both named candidates were **read** and both were excluded **on their own text**.
`cowork_design_doc_template.md` scopes itself in its opening line to *"every architecture/design
document in this project"*, and its standards are for specifications, design documents and decision
surfaces; a dispatch is none of those. `CLAUDE.md`'s Conventions carries rules binding both sides,
but rule (e) of the decisions register prefers the OWNING document wherever one exists — and one
does.

**The record makes `cowork_audit_protocol.md` the home, and says so in its own words at the very
section that receives the clause:** *"Their scope is wider than this document's audits: they govern
every dispatch, and this document is their home because it is where the project's
dispatch-construction rules already live."* Thirty-odd register entries are homed there and classed
`process` — *correctly homed* in the register's own vocabulary — **D-250, D-251, D-252, D-431 and
D-434 among them, every one of them a rule about how a dispatch is written.**

**So no home was invented**, which is what the STOP condition forbids; writing the clause there is
following the record. The departure is from the dispatch's *enumeration of candidates*, not from its
instruction, whose first words are *"Derive the home; do not assume it."* It is declared here, in
the commit message, and in the `STATUS.md` entry.

### 5.b The clause and its ground

**Every dispatch's read-first block names the current handover block of `cowork_handoff.md`.** It
stands beside the self-sufficiency rule rather than inside it: an instruction can be self-sufficient
about the work it orders and still send the executing session in without the one thing both sides
share. **F58 is recorded as its ground**, with the measured instance (F52, double-booked) and the
sharper half — the dispatch named `cowork_handoff.md` three times and never as something to READ —
and the lesson in its general form: **a dispatch that orders a write into a document must also order
the read of it.**

This dispatch's own read-first block already discharges the clause, so the act makes it standing
rather than changing anything already read.

### 5.c The anchored-quote consequence, again

**27 anchors re-aimed per citation**, then two derivations regenerated; the drift authority reports
474/474 and 468/468, against 441/468 before. Measured before accepted: the decisions backbone moved
27 lines, **every changed line a `cowork_audit_protocol.md` coordinate and nothing else**, and the
rulings-sort surface is **BYTE-UNCHANGED**, its blob identical before and after.

**E3 — MET.** The clause stands at a home the record makes a home, with the ground for that
placement recorded and F58 named; no other authoring rule moves. **No decisions-register entry is
written**: the four 2026-08-17 sitting records each state that the classification of every ruling as
decision or exercise is OWED and not made there, and **D-652** is the route by which that debt is
discharged — which is not this batch's act and which the dispatch does not order.

---

## 6. Task 4 — the census re-pinned at the moved read regime

Commit **`30d44165cf`**, parent `b6134f41ff`, pushed, **4 paths**, verified at the object.

The census was regenerated at **Task 3's commit** with the callers sitting's ruled kinds unchanged,
and `gen_retirement_caller_check.py --check` **passes at the new pin**.

**Eight movements, ZERO unclassed**, all of them HOLDING NAMINGs and all placed in class **(iii) the
tree's ordinary growth** by the content-level relation. **No CANDIDACY VERDICT unit moved at all**,
so no verdict flipped and no candidacy was conferred or withdrawn.

**★ The read-regime class placed NOTHING, for the second re-pin running, and that is the finding.**
The class is **IMPORTED and used as it stands, not re-derived** (#6), and it is **tested BEFORE the
growth relation**, which is F47's lesson applied rather than rediscovered. It is published as a
MEASUREMENT so the absence of a cause cannot be mistaken for nobody having looked. The corroborating
scalar is in the artifact: the holder-kind tally for *a mandatory-read or boot listing* reads the
same before and after — **narrowing what a session READS changed nothing about what a mandatory-read
listing HOLDS.**

**The outgoing commit moved and the previous value is recorded rather than overwritten (#12):** the
ninth batch's own outgoing commit joins the tool's aiming history with the reading it carried.

**One account inside the tool was brought up to date and the RELATION was not touched.** The fifth
class's narrative named the ninth batch's two read-regime acts; it now also names this batch's one.
The relation is unchanged and imported; only the tool's account of its own subject moved, because a
tool that misdescribes its own subject is the doc-sync defect at the smallest grain.

**The KIND-UNDERIVABLE population returns to the user as published data and stands at ONE**, the
same caller as at the ninth batch's re-pin. It is never guessed.

**E4 — MET.** The check passes at the new pin; every difference is classed with zero unclassed;
every set-aside naming stays published; no verdict flipped, so no candidacy was conferred.

---

## 7. Task 5 — the close, and Ruling 4's forward bound

Seven `STATUS.md` pointer entries, one per task. **In the same act, Ruling 4's forward bound moved
the NINTH batch's seven entries verbatim to `STATUS_ARCHIVE.md`** — 9,640 characters, **byte-present
in the archive exactly once and absent from the must-read, both directions proven**. The declared
`Last updated: ` prefix adjustment was **imported, not re-decided** (#6), and no entry needed a
second adjustment.

**The batch-level summary sentence is AVOIDED rather than declared.** The dispatch offers both
routes for the smaller grain of the E-ordering rule; the `STATUS.md` close entry states that the
end-state guard run is not asserted there and lands in ONE FURTHER commit, so there is no prediction
to confirm or refute.

---

## 8. The guard set, and assumption A2

| when | run | failing | not run | historical | STOPs |
|---|---:|---:|---:|---:|---:|
| before the first edit | 69 | 1 | 4 | 16 | 0 |
| at the Task 0b tree | 69 | 1 | 4 | 16 | 0 |
| at the Task 1 tree | 70 | 1 | 4 | 16 | 0 |
| at the Task 2 tree | 71 | 1 | 4 | 16 | 0 |
| at the Task 3 tree | 71 | 1 | 4 | 16 | 0 |
| at the Task 4 tree | 71 | 1 | 4 | 16 | 0 |
| **the end state**, after the Task 5 commit existed | see §12 | | | | |

**★ ASSUMPTION A2 HELD.** Its check is that any red other than [[OI-372]]'s is classed by its
measured cause, and one outside this batch's own acts' subjects is a STOP-and-report. **Eight reds
appeared across the batch and every one is inside this batch's own acts' subjects** — six from the
ordered `CLAUDE.md` edit and two from the ordered `cowork_audit_protocol.md` edit, all of them the
F15 anchored-quote class. **Each was cleared by its ruled treatment, none by an adjustment made to
turn a check green.**

**★ EVERY RUN WAS TAKEN AT A QUIET TREE.** No guard run overlapped an edit of this session's own.

---

## 9. The new-tool rule, discharged twice

Two tools landed, each with **its authored run-instruction** in `gen_guard_state.py` and **its
authored classification verdict** in `gen_guard_classification.py`, in the same commit that created
it: `gen_evidence_pin_membership.py` (Task 1) and `gen_session_start_read_size.py` (Task 2). The
guard population grows by exactly those two invocations, **69 → 71**.

**★ THE SECOND TOOL IS A DECLARED JUDGMENT AND IS STATED AS ONE.** Ruling 3's own declined
alternative records that *the standing mechanism freeze bars tool work that does not block the
work*. Building a measurement tool is tool work. It was built because **Task 2 step 6 ORDERS the
effect published**, and **D-431 forbids a transcribed figure**, so a published measurement needs a
generator: the work was blocked without it. The alternative — folding the measurement into an
existing artifact whose subject is something else — was declined under #6.

---

## 10. Surfaced findings (D-641, #13, #19) — surfaced, not rowed

The dispatch bars creating an open-items row. **The series stood at F59** when this batch began,
which this session knew because Ruling 2(c) put the current handover block in its read-first block.
Each new finding is stated here and in the close. **None bears on the analysis, its inputs, or a
measurement tool the analysis depends on** — every subject below is the project's own record and the
apparatus that reads it.

- **F60 (new, the largest) — RULING 1's CLASS RULE PRESUMES A RULING RECORD NAMES A COMMIT, AND
  ONLY ONE OF FIVE DOES.** The rule's operative half is *"its generator thereafter reads its inputs
  at the git objects of the commit the ruling names"*. Measured over the derived membership: of the
  five members reached by a named document, **one** record names a commit and **four** name none.
  The class is therefore ruled and, for four fifths of its route-A membership, **not applicable as
  written**. **The general form worth carrying: a rule whose operative clause names a field the
  records do not carry is a rule that cannot be applied without either amending the rule or
  amending the records.** §3.c.
- **F61 (new) — THREE OF THE FOUR GENERATED RULING SURFACES STILL SAY OF THEMSELVES THAT NOTHING ON
  THEM IS RULED, AND EACH HAS BEEN RULED.** The residue surface, the governing-surface split surface
  and the artifact-inventory surface all open *"STATUS: RULING SURFACE, awaiting the user. NOTHING
  HERE IS RULED"*; each was ruled at a recorded sitting. Only the rulings-sort surface was corrected,
  by hand, at the eighth batch — which is the instance Ruling 1's own ground already names. **The
  general form: a generated document whose banner asserts its own status goes stale the moment the
  status changes, and nothing regenerates a banner the generator hard-codes.** Directly adjacent to
  F60: these are three of the four UNRESOLVED members.
- **F62 (new) — ONE TOOL NOW SITS IN TWO RULED CLASSES AT ONCE, AND THE RECORD DOES NOT SAY WHICH
  GOVERNS.** §6 kind 3 of the 2026-08-16 record rules `gen_artifact_inventory_surface.py` a derived
  census that **REGENERATES** under a widened bound; Ruling 1's class rule would place the same tool
  in the **PINNED** kind, its document having been ruled from. The two are incompatible for one tool
  writing one document. **No verdict is taken here** — it is returned to the user as data, and it is
  a second, independent reason no pin was applied to that member.
- **F63 (new, small) — THE CENSUS COUNTS ITS OWN MOVEMENT ARTIFACT AS A HOLDER, so every re-pin
  generates movements whose only cause is the previous re-pin's own record.** All eight movements
  this batch have that one subject. They are correctly classed and nothing is wrong with the
  placement; the finding is that a measurement which reads the tree includes its own output in the
  tree, so a share of every future re-pin's movement is self-referential churn. **The general form:
  a census over the record counts the record OF the census.**
- **F64 (new, small) — A TOOL'S OWN `Run:` BLOCK NAMES A FLAG ITS COMMAND LINE DOES NOT ACCEPT.**
  `gen_retirement_census_movement.py` documents `--outgoing <commit>`; the outgoing commit is an
  authored constant and the flag does not exist. Found by running the documented invocation, which
  failed. **The general form: an interface documented in prose beside an interface implemented in
  code is two homes for one concern (#6), and the prose is the one that goes stale.**
- **F1–F59 (carried, unchanged)**, including **F3**, now **twelve times surfaced** —
  `reaim_home_anchors.py --check` exits 0 while printing drifted anchors, and
  `gen_cluster_dispositions.py --verify` is the drift authority, which this batch used as the
  authority throughout. **Still unfixed and unrowed: the dispatch bars both.** **F25 did not
  repeat**: no changed-passage count was asserted in advance. **F47's lesson was applied rather than
  rediscovered** at Task 4. **F57 was applied rather than assumed** at Task 0a.
- **The E3 ordering defect and the A1 premise error of the earlier batches** ride to the phase's
  retrospective as the dispatch orders.
- **★ THE TWO DEPARTURES FROM THE DISPATCH'S WORDS ARE DECLARED** — the derived home that is neither
  candidate the dispatch named (§5.a), and the second new tool built because a published measurement
  needs a generator (§9) — each in its commit message, in the `STATUS.md` entry where it bears on a
  task's result, here, and in the close.

---

## 11. What this batch did NOT do

**No curated boot list** — it stays at its ruled definition and awaits its own drafting and sitting.
**No fate on any `CLAUDE.md` span ruled at the eighth-return sitting**: both stay at site, that
question is CLOSED, and `gen_claude_md_finer_archive.py --apply` was not run. **No archiving pass** —
nothing re-opens Ruling 2 of the session-start-read sitting and none of its nineteen spans is
re-proposed. **No candidacy acted on**, and none flipped. **No document archived, moved or deleted AS
A FILE.** **No mining, no empirical findings ledger, no fact-gate admission; no completion claim
about the superseded phase-1 program; no derivation, design, repair or pilot act.** **No `src/`
change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
`tools/robust_stop/`, no measurement of the analysis.** **No open-items row was created, flipped or
discarded** — [[OI-372]] and [[OI-374]] stand exactly as found, **[[OI-179]] stays OPEN and GATES**,
and F3 stays surfaced, unfixed and unrowed. **`gen_gating_row_sizing.py`'s frozen population was left
alone.**

---

## 12. The end state, under the E-ordering rule

The end-state guard run is taken **AFTER the Task 5 commit exists**, and its output with the final
SHAs lands in **ONE FURTHER commit**. Its values are therefore not in this section as written; the
further commit carries them, and **E5** is graded there on the run and on nothing else.

---

## 13. The self-check over this batch's whole diff (`CLAUDE.md`, the standing self-check)

The diff of every touched file was re-read against the guiding principles, the conventions, the gate
and threshold policies and `DEFECT_TYPES.md` before this report was written.

- **#6, one path per concern.** The read-regime movement class, the pin's shape, the archive-pointer
  form, the `Last updated: ` prefix adjustment and the anchored-quote treatment were all **imported**
  rather than re-invented. The two new tools each own one concern and neither restates a figure
  another artifact carries.
- **#12, no information loss.** Every superseded wording is preserved: the rule (a) clause verbatim
  in the companion, the outgoing census commit in the tool's aiming history, the former finding
  number beside its re-issue, and the narrowing act's own reading kept live as a baseline rather than
  left to be recovered from a blob.
- **#17f / D-431.** Every figure in this report is cited to a generated artifact. The one measurement
  this batch publishes was built as a generator for that reason and is declared as a judgment in §9.
- **#19.** No unestablished thing was put under load: the four UNRESOLVED members are published as
  questions rather than pinned, and the two new tools each carry the STOPs that make their claims
  demands about the tree rather than dated readings.
- **D-253, the file-tools rule.** Working-tree content was read with the file tools throughout. Shell
  access was used only for read-only git OBJECT queries by explicit hash, for the sanctioned path
  enumeration, and for running the project's own committed tools. **One mechanism is declared:**
  `git hash-object -w` was used to bring a working blob into the object store so that a
  content-addressed `git diff <shaA> <shaB>` could be taken — it emits a hash and no content, and the
  diff that follows is the enumerated sanctioned form. The guard denied three attempts during this
  session and each was re-taken through the file tools, which is the control working.
- **The reserved-word conventions** bind every line written this batch; no new collision was
  introduced, and the vocabulary rule of 2026-08-17 was observed — no alternative is rated *against*
  the objective or the principles anywhere in this batch's prose.
- **DT-2, per-case tuning; DT-26, scope-assumed enumeration.** Neither applies: nothing was fitted,
  and every enumeration this batch publishes states its own bound — the membership derivation names
  the two structural blocks it reads and the one resolution form it accepts, and the size measurement
  names where its comparison starts and why.

*Provenance: CC, 2026-08-18, dispatch `cc_instruction_preparation_tenth.md`. Task 0a `6bd9285006`
(parent `1760d9a4a8`), 3 paths. Task 0b `59191d7e65`, 2 paths. Task 1 `b0596b288b`, 9 paths. Task 2
`594074e1e1`, 24 paths. Task 3 `b6134f41ff`, 4 paths. Task 4 `30d44165cf`, 4 paths. Task 5's close
and this report are the commit carrying this sentence, whose own identity it cannot contain — that is
the E-ordering terminus, not an omission — and the end-state guard run lands in ONE FURTHER commit.
Every commit is pushed and verified at the object by explicit hash.*
