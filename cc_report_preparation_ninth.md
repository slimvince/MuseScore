# CC report — the preparation phase's NINTH batch: the session-start read cut at its ruled route, the live gating answer established, and an archiving pass whose largest finding is a refusal

> **Dispatch:** `cc_instruction_preparation_ninth.md`, executing Rulings 1–3 of
> `cowork_rulings_2026_08_17_session_start_read_sitting.md`, the two acts
> `cowork_rulings_2026_08_17_eighth_return.md` left for its successor, and the F51 correction at its
> three remaining sites.
> **Commits:** Task 0a `ff99a71a22` (parent `f27c3ded70`) · Task 0b `514b0dbc19` · Task 1
> `4667816255` · Task 2 `b8f5aaf268` · Task 3 `15dfb0e172` · Task 4 `e36168ec33` · Task 5 the commit
> carrying this file · then the ONE FURTHER commit the E-ordering rule requires. Every one verified
> at the object and pushed.
> **The full close is THE PREPARATION NINTH BATCH section of `cowork_away_returns.md`.**
> Every verdict, classed movement, population and count lives in the generated artifacts and is
> not restated here (**D-431**).

---

## 1. The reading, the start state, and Task 0a

### 1.a The session-start reads

`CLAUDE.md`, `STATUS.md` and `BUILD_AND_TEST.md` were read **as they now stand, in full** —
`BUILD_AND_TEST.md` unconditionally, as the dispatch's read-first block orders for THIS batch — and
no archive companion was opened, because no decision was re-opened. Then, as the dispatch orders:
`cowork_rulings_2026_08_17_session_start_read_sitting.md` **whole**;
`cowork_rulings_2026_08_17_eighth_return.md` **whole, its §6 above all**;
`cowork_rulings_2026_08_16_preparation_return.md` §§5–6;
`cowork_rulings_2026_08_17_callers_sitting.md` §4; `cc_report_preparation_eighth.md` §§2–4 and §9;
and the five artifacts the tasks bind at.

### 1.b The start-state guard run — exactly what the dispatch declares

Run **before the first edit**, at a quiet tree:

```
68 guard(s) run, 1 failing, 4 not run, 16 historical record(s)
  [FAIL] tools/audit/gen_filing_convention_application.py --check
```

and `gen_guard_classification.py --check` printed **"the guard classification re-derives"** — 67
passing, **zero STOPs**. That is the dispatch's declared expectation to the tool and to every
count, so no STOP-and-report was owed. The runner **rewrote its own artifact byte-identically**, so
the tree was unmoved by the measurement.

### 1.c Assumption A1, checked first and entirely at content-addressed objects

`tools/audit/changed_paths.py` reported **exactly ONE tracked modification in the whole working
tree** — `cowork_handoff.md` — with the two ruling records and the dispatch untracked.

The difference was taken **blob against blob by explicit hash**, under A1's declared line-ending
caveat: the committed blob resolved from the explicit commit `f27c3ded70` (`a87846b199`), the
working blob `ec4ccf405c`. It is **ONE contiguous changed passage**, 368 insertions and 1 deletion,
carrying every part A1's own sentence names — the TWENTY-FIFTH, TWENTY-SIXTH and TWENTY-SEVENTH
session-close blocks inserted, and the TWENTY-SIXTH, TWENTY-FIFTH and TWENTY-FOURTH headings each
gaining an entry-point demotion marker. **A1 held at the shape its own sentence describes**, and no
count of changed passages was asserted in advance (the F25 lesson).

### 1.d Task 0a

Commit **`ff99a71a22`**, parent `f27c3ded70`, pushed, **4 paths**, verified at the object.
**E0a — MET.** Exactly 4 paths at the enumeration; one modification whose content matches A1's
stated shape, three additions; no staging override.

---

## 2. Task 0b — the three recorded corrections

Commit **`514b0dbc19`**, parent `ff99a71a22`, pushed, **10 paths**, verified at the object.
**`CLAUDE.md` is BYTE-UNCHANGED by this task, proven by hash** — blob `8230aa91b6`.

### 2.a F49 — the retired block's opening sentence

The sentence attributed the whole block to the 2026-08-16 ruling while the block also holds the 38
entries the 2026-08-17 residue sitting retired. **ONE qualifying sentence is APPENDED at the
generator** (`tools/audit/decisions/apply_soft_discard.py`) **and rendered into the data file in
the same act**, because that tool's `--apply` path cannot run again by construction. The former
wording is **unchanged and stands as the opening of the string** — nothing is deleted, which is how
#12 is met here. The generator's literal and the data file's value were compared and **agree**; the
generator's own `--check` passes, and so do `gen_decisions_register.py --check` and
`gen_cluster_dispositions.py --verify`. No verdict, population, arithmetic or entry identity moves.

### 2.b F51 — the widened sentence at its three remaining sites

`cowork_rulings_2026_08_17_seventh_return.md` gains a new **§5 CORRECTION** appended after its
provenance, quoting the sentence it corrects: **a ruling record is corrected by appending, never by
rewriting the ruled text**, so §3 stands exactly as written. The **TWENTY-FOURTH** and
**TWENTY-FIFTH** handover blocks each gain a dated correction note immediately beneath the widened
parenthesis, quoting it. Each names F51, quotes the ruled definition from the surface the user
ratified 2026-08-15, and states that **Rulings 1–3 of the session-start-read sitting are the route
for ordinary sessions**.

### 2.c ★ The standing archive-pointer constraint, written at the recognizer AND answered by it

`tools/audit/gen_claude_md_finer_spans.py` carries Ruling 1's constraint verbatim and now locates
**every archive-pointer LINE in a span wherever it sits**: a recognizer marker — or, for a
preserved former wording, its accompanying quotation — lying inside one **may not place the span**.
A marker of the same class found OUTSIDE every pointer still places it, so the constraint is about
where the classifying text sits and not about a span merely containing a pointer.

Re-derived, the widening moves **exactly ONE span**: the F44 span at pinned lines 53–60, 845
characters, leaves `defense-and-declined-alternatives` and falls to the ruled **doubt default**,
its two refused markers (`DECLINED`, `THE COSTS THE USER ACCEPTED`) and the constraint published
beside it. The doubt-defaulted share rises 135,341 → 136,186 characters (89.6 % → 90.2 %).
**The recognizer independently reproduces the verdict Ruling 1 gave that span.** It is a recognizer
change and it moves no fate: `--apply` is not run and `CLAUDE.md` is byte-unchanged.

### 2.d ★ Two departures from the dispatch's words, both caused by 2.c and both declared

**(a) A DRIFT STOP NARROWED, on the eighth batch's own precedent.**
`gen_claude_md_finer_archive.py` asserted *"two archive, four refused"* — the seventh-return
Ruling 1's own shape, TRUE while that was the last word and FALSE the moment a later ruling removed
one of the two from the archive classes **by ordering a recognizer change**. Re-derived, it
reported a disagreement **that is not one**. It now asserts something narrower that stays true
across the later ruling: the archive-class spans **plus** the spans the standing constraint removed
must still be the ruling's two, and the refused set must still be its four. A span that simply
VANISHED still halts it, and a constraint-removed span carrying a MOVE verdict now halts it too.
Both spans remain READ and both STAY; every reconciliation direction is true.

**(b) A RULED RATIFICATION SURFACE PINNED — MEASURED FIRST, THEN DECIDED.**
`gen_claude_md_finer_surface.py --check` went red. Regenerating the surface was **measured before
anything was decided**: it would have rewritten the proposal the user actually ruled on — the
53–60 row would read STAYS AT SITE, and the surface's own answer sentence would fall from *"1 span,
845 characters … proposed on ground no reading has yet refused"* to zero. **That destroys the
evidence of what was PUT**, which #12 and the ruled Kind-1 treatment of
`cowork_rulings_2026_08_16_preparation_return.md` §6 both exist to prevent, and which E0b's own
words forbid. The surface was restored to its committed bytes and the tool's **span artifact is
PINNED** to the commit the ruling record names as where the surface was ruled (`cfb69a7ecb`; the
blob is identical there and at this batch's base). The surface and the readers artifact are
byte-unchanged. **The pin's SHAPE is a ruled treatment; applying it to a third tool is a
classification the user has not made, and it is declared here for the user's ruling.** A later
ruling can unpin it in one edit.

**E0b — MET.** The register's own check passes with generator and data file agreeing; each of the
three F51 sites carries a dated correction with its former wording preserved beside it; the widened
recognizer re-derives its artifact and the one span it now declines is marked doubt-defaulted
rather than dropped; no verdict, no fate, no population and no row moves.

---

## 3. Task 1 — the live gating answer, DERIVED and ESTABLISHED

Commit **`4667816255`**, parent `514b0dbc19`, pushed, **20 paths**, verified at the object.

### 3.a A3 re-established at the objects, and the dispatch's known shape confirmed

Read at `f27c3ded70`'s `tools/audit/guard_classification.json` by explicit hash, the split is
exactly as the premise ledger states: `gen_phase1_finish_line.py` and
`gen_phase1_completion_inventory.py` both `records-a-point-in-time-measurement`, both OUT of the
live guard list, both HISTORICAL; `gen_nongating_apparatus_rows.py` and `gen_gating_row_sizing.py`
both `re-derives-a-live-invariant`, both live, both PASS.

**The known shape is CONFIRMED, at the tools and not at their headers.**
`gen_nongating_apparatus_rows.py` parses `OPEN_ITEMS.md` on every run through THE ONE INDEX PARSER
and published the non-gating side **by identity**, the gating side only **by count and by
ground** — and that per-ground listing reaches only rows inside its own over-inclusive apparatus
first cut, so **the open rows outside that cut were never enumerated at all**.
`gen_gating_row_sizing.py`, live as a check, draws its population from
`tools/audit/phase1_completion_inventory.json → the_gating_split.gates.ids`, whose generator is
frozen HISTORICAL. **So the one artifact enumerating gating rows by identity drew that enumeration
from a superseded record — F22's ossification reaching the answer this ruling needs.**

### 3.b The bounded act, and why it is a publication rather than a judgment

The gating set is the **COMPLEMENT** of the file's non-gating verdicts over the open rows the tool
already parses. **No new verdict is authored**: the apparatus first cut is deliberately
OVER-INCLUSIVE and every candidate in it must carry an authored verdict or the tool STOPs, so an
open row outside the cut is not an apparatus candidate and **the ruled default already gates it**.
Nothing is imported from any frozen artifact to reach the answer, and no ruled criterion, exemption
or default is widened. **None of the three STOP conditions the dispatch names was met.**

### 3.c Established under #19, positively and on every run

Reconciled in **both directions against the INDEX itself**: every gating identity is an OPEN row of
the parsed population; every open row is accounted for as gating or non-gating, **none in both and
none in neither**; every gating row carries a ground; and **a row the derivation cannot place HALTS
the run**. What would falsify it is recorded with it, so a later session can challenge the
derivation rather than rediscover the question.

**The answer: 216 gating + 25 non-gating = 241 open rows.** [[OI-179]] is present in the gating set
with its ground — OPEN and GATING, as the ruling requires.

### 3.d ★ The frozen enumeration measured against the live one, acted on in no direction

The frozen record enumerates **30**; the live derivation determines **216**. Of the frozen 30, 29
still gate live; **ONE — OI-370 — does not, because it RESOLVED after the freeze**. **187 rows gate
live and are absent from the frozen record, [[OI-179]] among them.** The two populations differ BY
CONSTRUCTION and that is the finding: the frozen split is applied over `the wide cut`, an authored
keyword selection whose own over-inclusion that artifact declares unmeasured, while this
derivation's population is EVERY OPEN ROW. **Evidence for the retrospective, not an act**: no row is
flipped, created or discarded, and `gen_gating_row_sizing.py`'s frozen population is left exactly
as it stands. The live answer does not depend on the frozen artifact and must not — the comparison
read is separate and non-fatal, and reports itself unreadable rather than empty if it cannot be
taken.

### 3.e The read-regime change, taken only because 3.c established

`CLAUDE.md` rule (a) now reads the derived gating answer at session start and opens the INDEX when
it needs a row. **Rule (a)'s authority is untouched and says so at the site**: the INDEX remains the
AUTHORITATIVE STATUS SURFACE, and the derived answer is a route to ONE question it answers, never a
second home for status (#6). The superseded wording moved **verbatim** to `CLAUDE_ARCHIVE.md` in
the same act under the continuous-pruning rule, with a compact dated pointer at the site.

**The anchored-quote consequence is the F15 class and was cleared by its ruled treatment** — 48
anchors re-aimed PER CITATION from the drift report, then six derivations regenerated. The drift
authority `gen_cluster_dispositions.py --verify` reports 474/474 verbatim quotes at their cited home
and 468/468 cited line numbers correct. **The rulings-sort surface's own diff was MEASURED before it
was accepted**: every changed line is a `CLAUDE.md:` home coordinate and nothing else.

**E1 — MET.** The split re-derives as stated; the answer is derived at the INDEX as it stands today
and reconciles both ways with a halt on any unplaceable row; the frozen-against-live difference is
published and acted on in no direction; rule (a) is amended because the establishment succeeded;
no row is created, flipped or discarded and [[OI-179]] stays OPEN and GATES.

---

## 4. Task 2 — the archiving pass, and a refusal larger than its yield

Commit **`b8f5aaf268`**, parent `4667816255`, pushed, **9 paths**, verified at the object.

### 4.a No new mechanism

Everything the pass decides with is **IMPORTED** (#6): the span cut, the six classes and the
recognizers from `gen_governing_surface_spans`; the ONE compact dated pointer shape and the
per-file companions from `gen_governing_surface_split`; the standing archive-pointer constraint
from `gen_claude_md_finer_spans`; the six settled `CLAUDE.md` spans from
`gen_claude_md_finer_archive`'s own ruled population at its own pin. **What is new is only WHEN the
measurement is taken** — at this batch's Task 1 commit — which is what the split's own pinning
comment says a later pruning wave does. The three ruled exclusions are each DERIVED, and the
settled-span exclusion tests containment in **both** directions, because the two acts cut at
different grains.

### 4.b The yield is one span, and the ruling predicted and accepted that

**Nineteen spans were placed by the test and every one was READ WHOLE before anything moved. ONE
moved:** [[OI-370]]'s resolved row, 3,542 characters — the single genuine member of the ruling's own
named subject. The site keeps the row itself, so the register's bijection, its canonical status
opening, its gate cell and its detail link all survive. **The one hesitation is published rather
than suppressed:** the cell's closing clause records a proposed-and-untaken closing act, which is a
record of that resolution's own limits and is carried by the row's detail file.

### 4.c ★ The largest finding is a refusal, measured on the register's own rows

Applying the standing coarse recognizers to `OPEN_ITEMS.md` outside the `resolved-row` class
proposed **SEVEN LIVE OPEN ROWS** for archiving — **[[OI-179]] among them**, which is OPEN and
GATES under #19 and which every ruling this batch executes names as untouched. Each was placed by a
**#12 preservation phrase or a supersession phrase sitting INSIDE its own status cell**, describing
one of that cell's own sentences rather than the row. **This is finding F33's mis-class shape
reappearing on the register's rows**, and the read-before-move safeguard refused every one.

The other refusals are the same family: `CLAUDE.md` 457–616 is the **entire decisions-register
section** — rules (a) through (n) — placed by the two words `former wording` inside rule (i)'s and
rule (j)'s defenses, which is the F33 case the finer pass measured **by name**; `CLAUDE.md` 1100 is
a **bare heading**, refused on the structural ground the finer pass already places positively; and
the three `DECISIONS.md` spans are live rule text and live scope bounds placed by phrases that
POINT AT former wordings preserved elsewhere.

### 4.d ★ A third verdict value, declared rather than folded into the second

The six `STATUS.md` entries the test placed are the **EIGHTH batch's** dated entries, which the
standing forward bound of Ruling 4 of `cowork_rulings_2026_08_17_governing_surface_split.md`
already owns and which **this dispatch's own Task 5 moves**. They did not FAIL the reading;
recording them as flagged failures would say something false about it, and moving them here would
put a second path on one concern (#6) and race that mechanism inside one batch. They carry
**OWNED-BY-A-STANDING-MECHANISM** with the owner named.

### 4.e ★ The declared line-ending caveat was measured, not assumed

The pass's first run reported `DECISIONS.md`'s spans **absent from a file that carries them**: the
file is stored on disk with carriage-return line endings where its committed blob has none. Every
comparison and every replacement now goes through **one function** that normalises the form first,
and the parent is edited **in its own line-ending form** rather than rewritten from the pinned blob
— rewriting would re-line-end every line of a governing surface to move one span.

### 4.f ★ One red this act caused, cleared by the #12 ground the archiving rule itself rests on

`gen_gating_row_sizing.py` STOPPED because [[OI-370]]'s anchor quote left the INDEX with the row's
bulk. What that STOP exists for is that **the quote be REAL and locatable**, so a sizing cannot rest
on a reading of a row nobody opened — and **archive-with-record keeps every citation resolvable**,
the same ground the continuous-pruning ruling and the callers sitting both rest on. The quote is now
sought in the INDEX first and in the companion the INDEX's own pointer names second; a quote in
**neither** still halts the run. Its **population is untouched**, no sizing is re-authored, and no
status is read from the companion.

### 4.g ★ And the OWNED verdict's own reconciliation was corrected at Task 5, by the mechanism it named

The six OWNED spans stayed **for** Task 5's forward bound, and at Task 5 that bound moved them.
`gen_post_split_archive.py --check` then went red on
`every_span_the_reading_flagged_is_still_present_at_site_exactly_once` — **asserting of an OWNED
span that it is still at site is asserting that the owning mechanism had NOT run**, which is the
opposite of what the pass found. The at-site directions now apply to the spans the READING flagged;
an OWNED span carries what this pass is actually responsible for — **that this pass moved none of
them** — with where it stands now published as DATA and proven by the owning mechanism's own check,
named per span in the verdict. Declared with the other departures.

**E2 — MET.** Both directions proven per file, the second being the load-bearing one at a pass that
moved one of nineteen; every doubt-defaulted span is marked; no anchor needed re-aiming, because
the moved row's site keeps its identity, its gate cell and its detail link; no verdict, gate, cut or
population moves, and no document is archived AS A FILE. **The new-tool rule is discharged in the
same commit.**

---

## 5. Task 3 — `BUILD_AND_TEST.md` demoted, `DECISIONS.md` explicitly not

Commit **`15dfb0e172`**, parent `b8f5aaf268`, pushed, **19 paths**, verified at the object.

`CLAUDE.md`'s build-and-test section now names **two** unconditional session-start reads —
`STATUS.md` and `DECISIONS.md` — and `BUILD_AND_TEST.md` is a **CONDITIONAL** read, stated in the
ruled words and no others: **mandatory for a session that builds, tests, or runs a measurement tool
whose command lives there**. It is the twice-ruled conditional-read pattern applied **for the first
time to DEMOTE** rather than to add. The superseded wording moved verbatim to `CLAUDE_ARCHIVE.md`
in the same act with a dated pointer at the site.

**`DECISIONS.md` is explicitly NOT demoted and the ground is recorded at the site**, because a later
ruling must have something to answer: its rule (a) rests on the stated premise that rulings bind
mechanically only if every session reads them, and a condition would replace a mechanical bind with
a judgment each session makes about its own work BEFORE reading the thing that would tell it
whether the judgment is right. Nothing about its read is edited.

**This batch's own reading is unaffected** — the dispatch orders `BUILD_AND_TEST.md` read
unconditionally for this batch, it was so read, and this task changes nothing already read.

The anchored-quote consequence was cleared exactly as in Task 1: **35 anchors re-aimed per
citation**, six derivations regenerated, `gen_cluster_dispositions.py --verify` reporting 474/474
and 468/468.

**E3 — MET.**

---

## 6. Task 4 — the census re-pinned at the moved read regime

Commit **`e36168ec33`**, parent `15dfb0e172`, pushed, **4 paths**, verified at the object.

The census is re-pinned at **Task 3's commit**, the content-addressed pin the dispatch names, with
the callers sitting's ruled kinds unchanged: nine candidacies, one PASSES-THE-CHECK and eight
HELD-BY-CALLERS.

### 6.a ★ A fifth movement class is declared and it placed NOTHING, which is the finding

The dispatch orders the read-regime change carried as its own named cause. The class is **DERIVED**
on the same shape as the other four — `CLAUDE.md` names the file at BOTH measured commits and the
SET of its lines naming it DIFFERS between them, while the file was neither added to nor removed
from the tree — and it is tested **BEFORE the growth relation**, which is F47's lesson applied
rather than rediscovered. **It held for no movement.** The read regime moved twice in this batch and
moved no candidacy, because the files it re-classed are **governing documents rather than retirement
candidates**. **An empty class is published as a MEASUREMENT**: the artifact now carries every class
the tool TESTS beside the ones that placed something, so the absence of a cause cannot be mistaken
for nobody having looked.

### 6.b The outgoing commit, corrected before anything was committed

A first run left `OUTGOING_COMMIT` unmoved and re-derived the **eighth** batch's own 687
ruled-input movements a second time. It is now Task 3's commit, at which the census still holds the
reading measured at `d499027a8c`, and **the eighth batch's own aiming joins the recorded history
beside the seventh's (#12)**.

### 6.c The movement

**294 movements at four units of identity, ZERO fitting none.** All are HOLDING NAMINGs placed by
the tree's ordinary growth: this batch's own new tracked files name flagged candidates, and the
eighth batch's own movement artifact entered the tree between the two measured readings.
**No candidacy verdict flipped** — there is no CANDIDACY VERDICT movement at all — and a crossing
would confer CANDIDACY ONLY in any case. **The KIND-UNDERIVABLE population returns to the user as
published data: it stands at ONE**, and its naming HOLDS in the meantime, which is the conservative
direction.

**E4 — MET.**

---

## 7. Task 5 — the close, and Ruling 4's forward bound

Six `STATUS.md` pointer entries, one per task. **In the same act, Ruling 4's forward bound moved the
EIGHTH batch's six entries verbatim to `STATUS_ARCHIVE.md`** — 8,292 characters, **byte-present in
the archive exactly once and absent from the must-read, both directions proven**. The declared
`Last updated: ` prefix adjustment F48 names was **imported, not re-decided** (#6), and no entry
needed a second adjustment. `gen_governing_surface_split.py --check --pair STATUS.md` re-derives.

---

## 8. The new-tool rule, discharged once

`tools/audit/gen_post_split_archive.py` landed with **its authored run-instruction** in
`gen_guard_state.py` and **its authored classification verdict** in `gen_guard_classification.py`,
in the same commit that created it. The guard population grows by exactly that one invocation,
68 → 69.

---

## 9. The guard set

| when | run | failing | not run | historical | STOPs |
|---|---:|---:|---:|---:|---:|
| before the first edit | 68 | 1 | 4 | 16 | 0 |
| at the Task 0b tree | 68 | 1 | 4 | 16 | 0 |
| at the Task 1 tree | 68 | 1 | 4 | 16 | 0 |
| at the Task 2 tree | 69 | 1 | 4 | 16 | 0 |
| at the Task 3 tree | 69 | 1 | 4 | 16 | 0 |
| at the Task 4 tree | 69 | 1 | 4 | 16 | 0 |
| **the end state**, after the Task 5 commit existed | **69** | **1** | 4 | 16 | **0** |

**★ E5 — MET, on the run and on nothing else.** The end-state run, taken **AFTER commit
`ccb3131725` existed**, printed:

```
69 guard(s) run, 1 failing, 4 not run, 16 historical record(s)
  [FAIL] tools/audit/gen_filing_convention_application.py --check
```

and `gen_guard_classification.py --check` printed **"the guard classification re-derives"** —
**every run check passing except [[OI-372]]'s tool, zero STOPs**, the shape the dispatch predicted,
reached for the third batch running. Run and read, never inferred; the values are committed only
after the run that produced them. **The Task-5 `STATUS.md` entry's prediction is CONFIRMED exactly,
and it was declared as a prediction before this run existed.**

**★ ASSUMPTION A2 HELD.** Its check is that any red other than [[OI-372]]'s is classed by its
measured cause, and one outside this batch's own acts' subjects is a STOP-and-report. **Seven reds
appeared across the batch and every one is inside this batch's own acts' subjects** — one from the
ordered recognizer widening (the finer surface), four from the ordered `CLAUDE.md` edits (the F15
anchored-quote class), one from the ordered archiving move (the sizing pass's anchor quote), and
one from Ruling 4's forward bound moving the very spans the archiving pass had marked OWNED for it.
Each cleared by a ruled treatment, **none by an adjustment made to turn a check green**.

**★ AND EVERY RUN WAS TAKEN AT A QUIET TREE.** No guard run overlapped an edit of this session's
own; where a correction was owed mid-task, the run in flight was allowed to finish before anything
was touched, and the set was re-run after.

---

## 10. Surfaced findings (D-641, #13, #19) — surfaced, not rowed

The dispatch bars creating an open-items row. Each is stated here and in the close.

- **F52 (new, the largest) — THE STANDING RECOGNIZERS PROPOSE LIVE OPEN REGISTER ROWS FOR
  ARCHIVING, AND ONE OF THEM IS THE ROW THE RECORD NAMES MOST OFTEN AS UNTOUCHABLE.** Seven open
  rows, [[OI-179]] among them, were placed in an archive class by a `#12` preservation phrase or a
  supersession phrase sitting inside their own status cells and describing one of the cell's own
  sentences. F33's mis-class shape reappearing on the register's rows; the read-before-move
  safeguard refused every one, which is the safeguard being the only thing between a recognizer and
  a live gating obligation. §4.c.
- **F53 (new) — THE ONE ARTIFACT ENUMERATING GATING ROWS BY IDENTITY DREW THAT ENUMERATION FROM A
  FROZEN RECORD, AND THE GAP IS LARGE.** 30 against 216, over populations that differ by
  construction. Measured and published; acted on in no direction. §3.a, §3.d.
- **F54 (new) — A RULED RATIFICATION SURFACE WOULD HAVE BEEN SILENTLY REWRITTEN BY AN ORDERED
  RECOGNIZER CHANGE.** The regeneration was measured before it was decided: it would have replaced
  the proposal the user actually ruled on with its own later answer. The general form worth
  carrying is that **a surface generated from a live artifact is not evidence of what was PUT
  unless its generation is pinned.** §2.d(b).
- **F55 (new, small) — A CHECK THAT LOCATES A QUOTE IN ONE FILE BREAKS WHEN THE RULED ARCHIVING
  PASS MOVES THE TEXT.** Found by the guard on the day it happened. The general form: **a
  locate-in-a-named-file check needs the archive-with-record route written into it, or every
  archiving act creates a red.** §4.f.
- **F1–F51 (carried, unchanged)**, including **F3**, now **eleven times surfaced** —
  `reaim_home_anchors.py --check` exits 0 while printing drifted anchors, and
  `gen_cluster_dispositions.py --verify` is the drift authority. **Still unfixed and unrowed: the
  dispatch bars both.** **F25 did not repeat**: no changed-passage count was asserted in advance.
  **F35 recurred and was answered by its own machinery** rather than by a fresh reading.
- **The E3 ordering defect and the A1 premise error of the earlier batches** ride to the phase's
  retrospective as the dispatch orders.
- **F56 (new, small) — A CHECK CAN ASSERT THAT A STANDING MECHANISM HAS NOT RUN.** The archiving
  pass's at-site direction, applied to a span it had marked OWNED by another mechanism, asserted
  exactly that — and went red the moment the owning mechanism did its job. The general form: **an
  at-site assertion belongs only to spans the ASSERTING act is responsible for.** §4.g.
- **★ THE DEPARTURES FROM THE DISPATCH'S WORDS ARE DECLARED** — the narrowed drift STOP, the pinned
  ruling surface, the fifth movement class that placed nothing, the third verdict value, and the
  OWNED reconciliation correction — each in its commit message, in the `STATUS.md` entry where it
  bears on a task's result, here, and in the close.
- **No finding bears on the analysis, its inputs, or a measurement tool the analysis depends on.**
  Every subject of this batch is the project's own record and the apparatus that reads it.

---

## 11. What this batch did NOT do

**No curated boot list** — it stays at its ruled definition and Ruling 4 orders no act. **No fate on
any `CLAUDE.md` span ruled at the eighth-return sitting**: both stay at site, that question is
CLOSED, and `gen_claude_md_finer_archive.py --apply` was not run. **No candidacy acted on**: the
census's verdicts confer candidacy only, and none flipped. **No document was archived, moved or
deleted AS A FILE.** **No mining, no empirical findings ledger, no fact-gate admission; no
completion claim about the superseded phase-1 program; no derivation, design, repair or pilot act;
no `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
`tools/robust_stop/`, no measurement of the analysis.** **No open-items row was created, flipped or
discarded** — [[OI-372]] and [[OI-374]] stand exactly as found, [[OI-179]] stays OPEN and GATES,
[[OI-370]] keeps the status it already had, and F3 stays surfaced, unfixed and unrowed.
**`gen_gating_row_sizing.py`'s frozen population was measured and left alone.**

---

## 12. The self-check over this batch's whole diff

Run over the diff actually on disk, not the memory of writing it.

- **Reserved-word conventions.** The new prose uses *measurement tool*, *check*, *script* and
  *generator* for the gauging sense; *entry*, *record* and *row* rather than a bare note;
  *measurement* for the gauging noun. `score`, `key`, `mode` and `register` appear only in their
  musical sense or in the qualified compound the convention fixes (*the open-items register*, *the
  decisions register*). **The 2026-08-17 vocabulary ruling binds every line written here**: nothing
  is rated *against* the objective or the principles.
- **#17f / D-431.** Every figure in the report, the close and the `STATUS.md` entries is read from a
  generated artifact or from a captured run, and each says where. No figure is hand-transcribed
  into a governing surface.
- **#12.** Five preservations: the retired block's own opening sentence, unchanged beneath its
  qualification; the seventh-return record's §3, corrected by appending and never rewritten; both
  handover blocks' widened parentheses, standing above their correction notes; `CLAUDE.md`'s two
  superseded read-regime wordings, moved verbatim to the companion with dated pointers at their
  sites; and the census-movement tool's previous aiming, recorded rather than overwritten.
- **#6.** The span cut, the classes, the recognizers, the pointer shape, the companions, the
  standing pointer constraint, the ONE index parser, the declared prefix adjustment, the census's
  own base-name test and the live-prohibition marker are all **imported** rather than restated or
  re-decided. Rule (a) points at the derived answer and states that the INDEX remains the
  authoritative status surface.
- **The departures from a dispatch's words are declared** — the narrowed drift STOP, the pinned
  ruling surface, the fifth movement class, and the third verdict value — each in the commit
  message, the close, this report and, where it bears on a task's result, the `STATUS.md` entry.
- **Nothing was adjusted to make a check green.** Every red that cleared did so because the ruled
  treatment for its class removed its cause; the one check that was narrowed was narrowed because
  its former assertion had become false under a later ruling, with the narrower assertion still
  catching what it exists to catch; and the one check that was widened was widened to the route the
  archiving rule's own #12 ground already provides, with a quote in neither file still halting it.
