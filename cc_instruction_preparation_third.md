# CC dispatch — the preparation phase's THIRD batch: the sole-carrier subclass DERIVED, the ratified-document subject check RUN over the 62, and the ruled soft-discard EXECUTED over the 194 minus its sole-carriers — the first register mutation of the phase, under the ruling's own bounds

> **Dispatch (Cowork, 2026-08-16).** Executes §3 of `cowork_rulings_2026_08_16_preparation_return.md`
> — the soft-discard sitting's rulings (A with the sole-carrier guard, B1, B2). **This batch
> performs the phase's FIRST decisions-register mutation**: the ruled soft-discard, executed
> through the register's own mechanism (data edit + regeneration, rule (d)), behind the derived
> sole-carrier exclusion. Written at a verified STOP; nothing else is running. **The archiving
> wave, the empirical findings ledger, the fact-gate, the curated boot list, the mining, the
> landing of the newly visible files, the eight KIND-UNDERIVABLE callers and the prose-citation
> question are NOT in this batch.**
> **Read first (every session):** `C:\s\MS\CLAUDE.md`, `C:\s\MS\STATUS.md` (header only),
> `C:\s\MS\BUILD_AND_TEST.md`. **Then read, in full:**
> `cowork_rulings_2026_08_16_preparation_return.md` (§3 is this batch's whole authority; §1 and
> §2 are context); your own `cc_report_preparation_second.md` §4 (the recovery pass this batch
> consumes); the decisions-register section of `CLAUDE.md` (rules (c), (d) — the register is a
> generated surface, never hand-edit the rendered files); and the retired-block precedent:
> `cowork_rulings_2026_08_15_batch_return.md` §2 (the [[OI-373]] clearing act — a retired block
> carries its own STOPs so it cannot become a hole).
> The standing bars bind this batch whole: **no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no open-items row created, flipped or
> discarded, no archiving, no file moved or deleted, no mining.** The ONE register mutation
> permitted is Task 3's, exactly as bounded there.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end;
> record both states.** The expected start state: **52 run, 51 passing, ONE failing**
> ([[OI-372]]'s tool, `gen_filing_convention_application.py --check`), zero STOPs, no stale
> report. **A different start state is a STOP-and-report.** Verify every commit at the object and
> record the SHAs in the close. The reserved-word conventions and the ruled vocabulary
> replacements bind all new prose. **The new-tool rule stands** (authored run-instruction and
> classification verdict in the same commit that adds a check). **The E3-ordering rule stands**
> (no graded value committed before the run that produced it). **F3's reading rule stands**
> (`gen_cluster_dispositions.py --verify` is the drift authority).

## Ruling ledger (quoted at the record, not paraphrased)

- **§3 (A):** the 194 NOTHING-FOUND entries *"are SOFT-DISCARDED — behind the sole-carrier
  guard"*; the subclass is *"an entry whose status is DEFERRED, or whose home cannot be located,
  or whose content is found nowhere outside the register family"*; sole-carrier members *"do NOT
  ride the discard — they return to the user as a list"*. Every executed discard record carries
  finding, date, reason AND the clause verbatim: *"a provenance verdict, not a judgment on
  soundness or usefulness; the statement stands at its home and is met by the derivation."*
  *"Nothing destroyed (#12); every discard individually revivable."*
- **§3 (B1):** the ten clear recoveries KEPT — D-004, D-008, D-009, D-021, D-123, D-248, D-315,
  D-403, D-410, D-411 — *"each with its recovered act recorded as the entry's provenance at the
  discharge (the act quoted, its document and location named, from the recovery artifact)."*
- **§3 (B2):** the 62 remaining ACT-FOUND entries NOT ruled — *"for each entry whose recovered
  act is a document-ratification passage, the RATIFIED DOCUMENT ITSELF is checked for the
  entry's subject recognizers (the register's own, again)"* — ruled at the return.

## Premise ledger

- **FACT** — the second batch is complete and pushed through `8ee16dca1a` (verified by the
  writing side at the objects: the seven-commit chain, path sets, the register's byte-identity by
  hashing, all three artifacts' populations and distributions recounted at the artifacts).
- **FACT** — the recovery artifact's distribution is 72 ACT-FOUND / 194 NOTHING-FOUND / 0
  CITATIONS-UNRESOLVED, and the filter artifact's non-keep population is 266
  (both read by the writing side at the staged snapshots; the artifacts are the authority).
- **ASSUMPTION A1** — the working tree carries NO tracked modification, with
  `cowork_rulings_2026_08_16_preparation_return.md` differing from its committed blob ONLY by the
  appended §3 (one contiguous changed passage before the provenance block, which §3 rewrites in
  place) and `cc_instruction_preparation_third.md` untracked. **Check ordered (Task 0, first
  act, at content-addressed objects):** any difference outside that shape is a STOP-and-report.
- **ASSUMPTION A2** — the register's data file can carry a RETIRED-ENTRIES block without breaking
  its own reconciliations, on the [[OI-373]] retired-block pattern (a block with its own STOPs so
  it cannot become a hole). **Check ordered (Task 3):** after the mutation, EVERY
  register-consuming check re-derives clean, and live entries + retired entries account for the
  whole former population BY ARITHMETIC — a mismatch of one is a STOP.
- **ASSUMPTION A3** — the mutation's reach is the register family and its derived views ONLY.
  **Check ordered (Task 3): movement in any verdict, gate, cut or population field of any
  artifact OUTSIDE the register family** (the phase-1 completion inventory and finish line, the
  phase-3 gate partition, the guard mechanism's own records excepted for the ordinary
  registration of this batch's new checks) **is a STOP-and-report** — the ruling authorizes the
  discard's own movement and nothing else.

## Task 0 — land the ruling record's §3 and this dispatch

1. **A1's check first, entirely at content-addressed objects.**
2. Commit exactly these TWO paths and no third:
   `cowork_rulings_2026_08_16_preparation_return.md` (modified — §3 lands) and
   `cc_instruction_preparation_third.md` (new, staged plainly). Push. Verify at the object.

**Registered expectation E0:** exactly 2 paths at `git diff-tree`; no staging override.

## Task 1 — the SOLE-CARRIER subclass, derived over the whole non-keep population

1. Build `tools/audit/gen_sole_carrier_subclass.py` over ALL 266 non-keep entries (the
   population imported from the committed filter artifact, never restated — #6; the whole
   population, D-671 — computed over all 266 so the 62's later ruling reuses it, applied in this
   batch only to the 194). Per entry, THREE derived signals, each published with its evidence:
   (i) the entry's recorded status is DEFERRED (read from the register's data);
   (ii) the HOME cannot be located — the home field names a document or section that does not
   exist in the tracked tree at the measured commit;
   (iii) the CONTENT is found nowhere outside the register family — the entry's verbatim
   quotation is not found in any tracked file outside `DECISIONS.md`, `decisions/` and the
   register's data and audit artifacts.
   **SOLE-CARRIER iff (i) or (ii) or (iii).** No threshold, no hand-picked member.
2. Publish `tools/audit/sole_carrier_subclass.json` (with `--check`; the new-tool rule applies).

**Registered expectation E1:** all 266 covered, each with the three signals and a verdict, every
signal carrying its evidence; no register file touched in this task's commit.

## Task 2 — the ratified-document subject check over the 62 (B2's ordered derivation)

1. Build `tools/audit/gen_ratified_document_check.py` over EXACTLY the 62 (the ACT-FOUND entries
   minus the ten B1 keeps — a subset published under a scope that NAMES its members, D-671,
   imported from the recovery artifact and the ruling record's B1 list). For each entry, for
   each of its recovered acts: establish whether the act's passage is a DOCUMENT-RATIFICATION
   (it ratifies, rules on, or adopts a named document or section) — and where it is, locate the
   RATIFIED DOCUMENT in the tracked tree and search IT for the entry's own subject recognizers
   (the register's `patterns`, the same test the recovery pass used). Publish per entry:
   **SUBJECT-IN-RATIFIED-DOCUMENT** (the ratified document named, the matching passage quoted
   with its span) / **SUBJECT-NOT-FOUND-THERE** / **NO-DOCUMENT-RATIFICATION-ACT** (none of the
   entry's acts ratifies a document). An act whose ratified document cannot be located is
   recorded per entry with the dangling name quoted — a result, not a halt.
2. Publish `tools/audit/ratified_document_check.json` (with `--check`; the new-tool rule
   applies), and generate the ruling surface
   `ratification_surfaces/cowork_discard_residue_surface_2026_08_16.md` covering BOTH returns of
   this batch — the sole-carrier list (Task 1's members among the 194, each with signals and
   restatement) and the 62's check results — self-contained per the presentation rule, its
   banner stating NOTHING HERE IS RULED, and §3's clause quoted verbatim.

**Registered expectation E2:** exactly the 62 covered, each with a per-act establishment and one
per-entry result; the scope block names its population by citation; no register file touched in
this task's commit.

## Task 3 — the ruled soft-discard EXECUTED — ONE commit, the register's own mechanism, behind the guard

1. **The discard population is DERIVED, in the artifact before the act:** the 194 NOTHING-FOUND
   entries MINUS Task 1's sole-carrier members among them. Record the arithmetic in the close:
   194 = executed + withheld-as-sole-carrier, to the digit.
2. **In the register's DATA file** (`tools/audit/decisions/backbone_decisions.json`), through
   the register's own mechanism and never a hand-edit of any rendered file (rule (d)):
   - move each discard-population entry WHOLE into a **RETIRED-ENTRIES block** (added on the
     [[OI-373]] pattern if the data file does not carry one, WITH its own STOPs so it cannot
     become a hole: a retired entry re-appearing live, or an entry in neither block, halts the
     renderer and the reconciliations);
   - each retired record carries: the entry UNCHANGED (#12), its finding (NO-DECIDING-ACT-FOUND
     at the ratified filter classification; NOTHING-FOUND at the recovery pass), the date, the
     retiring authority (`cowork_rulings_2026_08_16_preparation_return.md` §3) **and the ruled
     clause verbatim** (the provenance-not-soundness sentence);
   - stamp the TEN B1 entries' provenance with their recovered acts (quoted, document and
     location, from the recovery artifact), leaving every other field untouched.
3. Regenerate `DECISIONS.md` and every `decisions/group_*.md` BY THE GENERATOR
   (`gen_decisions_register.py`). Regenerate every register-derived artifact BY ITS OWN
   GENERATOR, **diffing each against its committed blob at an explicit hash before the commit —
   A3's bound applies: movement outside the register family's own derived views is a STOP.**
4. Run the full guard set at the edited tree. Expected: reds ONLY in register-consuming checks,
   each clearing after its regeneration; the end state clean per E4. **Any red outside the
   register family is a STOP-and-report.**
5. Commit as ONE commit naming its own act and citing §3. Push. Verify at the object.

**Registered expectation E3:** the data-file diff touches exactly the retired-block addition,
the moved entries and the ten provenance stamps; every rendered file regenerated, none
hand-edited; **live + retired = the former population BY ARITHMETIC, stated in the close to the
digit; nothing destroyed** — every retired entry's text present, byte-preserved, in the retired
block; `gen_decisions_register.py --check` and `gen_cluster_dispositions.py --verify` re-derive
clean at the end state.

## Task 4 — the close

One `STATUS.md` pointer entry per task and nothing else in that file. The FULL close appended to
`cowork_away_returns.md` (a PREPARATION THIRD BATCH section) — both guard states, every SHA,
every expectation graded, the discard arithmetic to the digit, every problem declared, the
carried findings restated for the retrospective (F1–F12, the E3 ordering defect, the A1 premise
error — surfaced, none rowed; rows stay barred). The report `cc_report_preparation_third.md`, an
ordinary tracked file. Commit, push, verify at the object. **The E3-ordering rule:** the
end-state guard run demonstrated across the commit boundary is taken AFTER the Task 4 commit
exists, its output and the final SHAs landing in ONE FURTHER commit.

**Registered expectation E4:** the end guard state shows every check passing except
`gen_filing_convention_application.py --check` ([[OI-372]]), zero STOPs, this batch's new checks
registered under authored verdicts. **Any other failing check is a STOP-and-report.**

**Registered expectation E5:** at the tree carrying the close, a fresh full guard run re-derives
with the same state — run and read, not inferred, its values committed only after the run.

## What this batch does NOT do

- **No discard beyond the derived population of Task 3; no sole-carrier member discarded; the 62
  not ruled and not touched in the register.** The residue surface awaits the user.
- **No archiving, no file moved or deleted, no mining, no landing; the eight KIND-UNDERIVABLE
  callers and the prose-citation question stay open; every retirement flag stays a candidacy.**
- **No empirical findings ledger, no fact-gate admission, no curated boot list, no rulings-sort
  execution** (the sort's surface stands proposed, awaiting its own sitting).
- **No derivation of any specification, no design, no repair, no pilot act, no `src/` edit, no
  golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the
  analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly as
  found; [[OI-179]] stays OPEN and GATES; `reaim_home_anchors.py`'s F3 defect stays surfaced,
  unfixed and unrowed (both bars stand).
