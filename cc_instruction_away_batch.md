# The away-batch PLAN — the ruled queue for the user's absence. NOT A DISPATCH.

> **Status: RATIFIED WORK PLAN, written 2026-08-08 (Cowork); RE-BANNERED the same day on the
> user's correction.** This file was first written as a queued dispatch while
> `cc_instruction_document_routes_and_d472.md` was still in flight — a violation of the
> one-dispatch rule, whose point the user restated: **every dispatch is written from ALL
> present knowledge at a full STOP, with nothing knowledge-generating running in parallel.**
> A queued dispatch authored mid-wave fails that by construction, whatever its Task 0 says.
> **The correction:** this file carries the RULED SUBSTANCE (the queue, the authorizations
> R1–R4 below, the returns-file and STOP-note mechanics) as the plan of record. **The actual
> dispatch is written by Cowork AFTER the document-routes wave returns and is verified at the
> objects — from full knowledge at that STOP — reading this plan as its ratified input.**
> Nothing below is executable until then; the former dispatch framing is preserved in place
> because rewriting it would lose the ratified content (#12).
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_away_batch.md`.
>
> **★ THE USER IS AWAY.** The standing defaults are HOLD-DON'T-GUESS and CONTINUE-PAST-HOLDS:
> anything needing a ruling is written to the returns file (below) with its reason and the
> batch moves on to work that does not depend on it. **A finding whose subject bears on the
> analysis, its inputs, or a measurement tool a measurement depends on is SURFACED in the
> returns file and pauses every task that depends on it; independent tasks continue** (D-641,
> #13). An establishment obligation (#19) is always surfaced there.
>
> **★ THE RETURNS FILE is `cowork_away_returns.md`** — created by Task 0, appended per task:
> holds with reasons, surfacings, and each task's completion note. It is the user's first read
> on return and Cowork's verification index.
>
> **★ THE FREEZE IS NOT RELAXED except by ruling R1 (Task 1's guard family, corpus first).**
> **★ NO FIGURES (D-431), NO STATE NOT READ (#17a), D-253 IN EVERY DIALECT.** No `src/` change
> except none (read-only on the analysis throughout; the probe in Task 7 is read-only by
> construction), no goldens, no corpus, no `tools/robust_stop/` movement, no behaviour change,
> no fix to inference, no design. Phase 1 under D-231. Same-commit rule (D-230) throughout.
>
> **★ Item 2 of the finish line is not started by this batch. Phase 1's completion statement is
> not written, drafted, or partially written — even if every item closes, the statement is
> commissioned by the user.**

## 0a. THE RULING LEDGER (all user, 2026-08-08)

- **R1 — the guard family design**: `cowork_ruling_guard_family_2026_08_08.md`, read WHOLE
  (D-643). Licenses Task 1's mechanism change (D-436); corpus first.
- **R2 — the batch itself**: the user directed maximal unattended progress on already-ruled or
  ruling-free work ("it would be nice to have BOTH you and CC churning away as much as
  possible"), with the queue as presented and ratified on Cowork's surface of 2026-08-08.
- **R3 — COMMIT AND PUSH PER TASK BOUNDARY**: one provenance-stamped commit per completed task,
  then `git push origin master`. **The hard stop, restated:** `origin` = `slimvince/MuseScore`
  ONLY; NEVER `upstream` (`musescore/MuseScore`) or any route toward it; any push/PR/merge that
  would carry `cfc7eb5e39` or its content toward `musescore/MuseScore` is a HARD STOP. A push
  failure is reported in the returns file and worked around by NOTHING — no force, no rebase,
  no pull-merge; commits continue locally.
- **R4 — the D-472 conformance probe is AUTHORIZED, read-only** (Task 7): whether the record
  arm satisfies the stated stabilization precondition. Its result is SURFACED, never acted on.

**None of R1–R4 authorizes a fix to the analysis, a design, or an inference change.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT:** the ruling records on disk (`cowork_ruling_guard_family_2026_08_08.md`,
`cowork_document_route_rulings_2026_08_08.md`, `cowork_owner_rulings_2026_08_07.md` and its
amendment); the finish line and its populations as regenerable artifacts; the conformance row
created by the routes dispatch.

**ASSUMPTION — checked before the act each licenses, STOP into the returns file if refuted:**
**A1** the routes wave completed and its guards ran (checked at its report, its STATUS entry
and a fresh guard run — Task 0). **A2** per-entry owner determinacy, per the standing homing
discipline. **A3** the probe changes nothing — no flag flipped, no golden touched, no output
written inside measured directories; enforced by scope and verified at the task's own diff
(empty on the tree except the probe's named artifact).

## The tasks, in order — after each: self-check (D-434), guards, anchors per citation, worktree enumeration, STATUS pointer entry, ONE commit, push (R3)

**★ BEFORE STARTING EACH TASK, read `cowork_away_returns.md` in full.** Cowork may verify
completed tasks between CC sessions and append notes there. **A note whose heading carries
STOP halts the batch at that point** — the remaining tasks are not started, and the halt is
recorded in the returns file and `STATUS.md`. A note without STOP is context, not instruction.
This is the one channel by which a verification failure can stop work from being built upon
while the user is away; it costs one file read per task.

**Task 0 — reconcile.** Verify A1. Regenerate the finish line and every population this batch
reads. Create the returns file with the batch's start state.

**Task 1 — the guard family (R1).** Exactly per the ruling record: corpus first, blindness
measured at the unwidened guard, the five design parts, both rates on the extended corpus,
revert condition governing. OI-348 flips on success; OI-300 retires with every residual named,
or reports what survives.

**Task 2 — the archive-unhomed eleven.** Per entry into the owning specification; archives
untouched (#12); the item's own closing act.

**Task 3 — the defense gaps.** Per entry: SEARCH the record; record the defense it holds at the
entry's home, or "derivation not recorded". Writing a defense from memory is forbidden. An
entry whose home lies outside this batch's edit authority is HELD with that reason.

**Task 4 — the reach-verdict derivation.** ONE generated derivation applying D-639's test over
the apparatus rows, the way its first application was generated; verdicts derived with reasons;
rows move where the derivation puts them.

**Task 5 — the section-unreached four and the findings-not-rules thirteen.** Per-entry re-homes
under the standing discipline; rule (e)'s preference; soft cases held.

**Task 6 — the session-executable gating rows.** Enumerate the TRUE-half gating set at HEAD;
for each row whose correction is session-performable inside this batch's edit authority,
perform it and flip the row with provenance; hold the rest with reasons.

**Task 7 — the D-472 conformance probe (R4).** Read-only: establish at the code and, if
needed, at a diagnostic run whether the record arm's input to the key-area grouping satisfies
the stabilization precondition D-472's specification states, or by what other means, or not at
all. Verify A3 at the diff. The finding — whichever way — goes in the returns file with its
citations; a NOT-SATISFIED finding is a #13 surface and pauses nothing else (no queued task
depends on it).

**Task 8 — OI-346's mark application (leftover capacity).** The §6 marks with validating
corpora named, per the homed rule; the row flips if complete, else the remainder is stated.

**Edit authority for this batch, complete list:** `ARCHITECTURE.md`; `docs/scoring_model.md`,
`CLAUDE.md`, `BUILD_AND_TEST.md` (D-645, homing acts only — and Task 3's defense recording at
homes inside these files counts as a homing act's defense half); the register data and
generated files; `OPEN_ITEMS.md` and detail files; the guard tooling under R1 only; the returns
file and `STATUS.md`. **Anything else: hold.**

## Accepted outcomes

Holds are results. A task completing with an empty yield is a result. The batch ending early on
an analysis-bearing surface is the mechanism working. Item 1 or the TRUE half not emptying is
expected; the artifacts say what remains.

## Self-check (D-434) — run by Cowork before release

Ruling ledger complete with the push hard stop quoted; populations derived at execution time,
none carried; #12/#6/#7 discipline restated at the tasks that need them; the probe read-only by
scope AND verified at its diff; commit cadence bounded per task with the failure path stated.
