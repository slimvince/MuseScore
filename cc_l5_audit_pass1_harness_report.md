# L5 audit — PASS 1 (blind), session 3 of 3: the shared harness (`tools/batch_analyze.cpp`) — EG-7 / OI-84 / OI-116

> **Status: FINAL.** §0–§7 were frozen at the Task-3 blinding boundary (commit
> `708d0c3708`) before any withheld file was opened; §8–§10 (reconciliation, the OI-134
> comparison, the register rows) were added in Task 4 after the freeze. This is the LAST
> first-pass session of the whole certification plan — **the first pass of the final
> audit is now COMPLETE across all populations**; the whole-scope SECOND pass decides the
> certification proposal. **Certification is NOT decided here.**

## 0. Scope and row-count reconciliation

Population: **INSTRUMENT-HARNESS** — the single file `tools/batch_analyze.cpp` (4622
lines), tagged in the committed partition `tools/audit/l5/pass1_partition.json` as the
shared score→analysis driver every corpus run and diagnostic path uses. Scope is taken
from the committed artifact, not memory.

Deep rows for the harness in the committed inventory (`tools/audit/l5/l5_*.csv`,
filtered to `tools/batch_analyze.cpp`):

| kind | rows |
|---|---|
| functions (`l5_functions.csv`) | 51 |
| numeric literals (`l5_literals.csv`) | 188 |
| branches (`l5_branches.csv`) | 562 |
| cross-layer includes (`l5_crosslayer.csv`) | 69 |
| **total** | **870** |

51 + 188 + 562 + 69 = **870**, matching the partition's `INSTRUMENT-HARNESS` deep-row
count exactly. Every-row rigor fits one session (comparable to partition-2a's 954 rows /
7 files in one session), so no further split was needed.

**Scope note (`l5_io.csv`, `l5_fields.csv`, `l5_decls.csv` carry ZERO harness rows).**
The harness's reads (score) / writes (`.ours.json`) surface is *not* represented in the
`l5_io.csv` table — that table is populated only for the Python instruments (the C++
inventory extractor emits functions / literals / branches / cross-layer but not io /
fields / decls, per `manifest.json`'s stated extraction method). The harness's IO is
therefore audited from the branch/literal rows and the source directly, not from an io
table. Recorded so the reader does not mistake an empty io table for "no IO."

## 1. Protocol and the closed verdict set (P1–P4)

Every one of the 870 rows carries a closed-set verdict — no silent skip; "no issue" is
itself a recorded verdict with a stated reason. The harness is BOTH a program with
production-shaped behavior AND an instrument, so it gets both question forms.

- **functions → SURVIVES** (live harness code; nothing retires). Sub-role recorded:
  output-surface / preset-builder / pipeline-driver / plumbing.
- **literals → ESTABLISHED / UNFIT / DEAD.** Every harness literal is **ESTABLISHED**
  (each has a stated basis: a formatting precision, a music-theory constant, a structural
  cap/index, an init value, an inference-affecting constant, or a diagnostic-only
  threshold on a default-off path). Inference-affecting literals carry a manifest-presence
  note.
- **branches → SURVIVES** (arg-parse/dispatch, or serialization/guard).
- **cross-layer includes → SURVIVES** (the top-level driver legitimately consumes every
  layer's published surface — dependency-appropriate).

Machine-readable dispositions: `tools/audit/l5/pass1_dispositions_harness.csv` (all 870
rows) and `.json` (per-class counts + the flagged rows), generated by
`tools/audit/l5/gen_harness_dispositions.py` (this session's verdict-embodying script).

## 2. Disposition summary per verdict class

| verdict | rows |
|---|---|
| SURVIVES (functions 51 + branches 562 + crosslayer 69) | 682 |
| ESTABLISHED (literals) | 188 |
| **total** | **870** |

Flagged rows: **29** across **6** row-anchored findings (below). Everything else is a
recorded "no issue" with its stated reason in the CSV `note` column.

## 3. Findings (each with file:line and one plain-language sentence)

### Row-anchored (in the disposition CSV, flagged)

1. **default-preset-mode-priors-hand-copied** — `tools/batch_analyze.cpp:196-216`
   (21 rows). `applyPreset()`'s "Default" branch hand-copies the 21 app mode-prior
   defaults from `composingconfiguration.cpp`; sync is enforced only by a code comment
   (no mechanical check) and the values are not recorded in `tools/param_manifest.json`
   (which is chord-scoring scoped) — a value-copied inference-affecting constant
   (guiding #6 / #17f). *(The five NAMED presets instead read the single-source
   `modePriorPresets()` table — published once, consumed, correct.)*

2. **onset-boundary-threshold-hardcoded** — `tools/batch_analyze.cpp:597, 2555, 3718`
   (3 rows). `onsetBoundaryThreshold` is hard-coded to `0.25`, duplicating the
   `IComposingAnalysisConfiguration` default the user-facing bridge reads; a documented
   latent divergence (roadmap 0.6) with no mechanical sync — the two coincide today, but
   a config-default change would make batch stop measuring the user pipeline.

3. **flags-absent-from-help** — `tools/batch_analyze.cpp:3982, 4093` (2 rows).
   `--reachback-ab` and the five `--key-in-*` emission-weight flags are parsed by `main()`
   but appear in neither `printHelp()` nor `BUILD_AND_TEST.md` (documented only in in-code
   comments) — undocumented measurement modes on the shared harness (contract code→doc).

4. **standard-output-crlf-vs-diagnostic-lf** — `tools/batch_analyze.cpp:4593` (1 row).
   The standard analysis output file is opened `QIODevice::Text`, so on Windows the
   committed `.ours.json` corpus is written with CRLF; every diagnostic path writes with
   `std::ofstream(std::ios::binary)` (LF). Confirmed behaviorally this session (standard
   output CRLF, diagnostic output LF). Corpus byte-identity holds only while regeneration
   stays on Windows with Text mode.

5. **exit-path-asymmetry** — `tools/batch_analyze.cpp:4615-4621` (anchored at the `main()`
   row, 1 row). The standard path force-exits (`TerminateProcess` / `std::_Exit`,
   bypassing static destructors to dodge a Qt-TLS shutdown hang), while the ~10 diagnostic
   early-return paths return normally and DO run that destructor sequence; the two exit
   disciplines are inconsistent. Whether the diagnostic returns are ever exposed to the
   same "some runs" hang is unestablished (NOT observed on `--validate-slices` /
   `--decode-chords`, which exited cleanly with their meaningful codes this session).

6. **inventory-overcapture-call-as-function** — `l5_functions.csv` row
   `locateMeasureByTick 748-749` (1 row). Not a second definition — a CALL site inside
   `convertNotationRegion`; the over-capture-biased C++ inventory scanner (bias disclosed
   in `manifest.json`) emitted a spurious function row. The real definition is at 438-470.
   A finding about the inventory extraction, not the harness code; the 870 count includes
   this spurious row.

### Discovered at the harness, not a harness-row verdict (report + register)

7. **param-manifest-line-refs-stale** — `tools/param_manifest.json`. Its
   `batch_analyze.cpp` preset-builder line references (`~3843-3898`) are stale vs HEAD
   (the builder is at `~4220-4257`); the manifest is stamped to an older head
   (`459c92c46d`). The *values* it records for the harness's per-preset constants are
   correct; only the site line numbers drift. (Doc-sync; the manifest is a Python-instrument
   surface, discovered here.)

8. **buildtest-lowercase-preset-example** — `BUILD_AND_TEST.md:24, 342`. Both use a
   lowercase `--preset jazz`; the harness matches preset names case-sensitively, so that
   exact command errors ("unknown preset 'jazz'"). Confirmed behaviorally this session
   (exit 1). A broken documented example (contract doc→code).

## 4. The flag inventory (documented vs code; exercised vs unexercised)

The harness parses these flags in `main()`. "Doc'd in help" = listed in `printHelp()`.
"Exercised" = actually run this session (reproduce corpus run + targeted probes).

| flag | doc'd in help | alters standard corpus? | exercised this session |
|---|---|---|---|
| `--preset <name>` / `-p` | yes | yes (the measured config) | yes (Baroque/Jazz/Default full corpus) |
| `--param-override <file>` | yes | yes (when present; strict) | no |
| `--dump-regions batch\|notation\|notation-premerge` | yes | yes (which pipeline) | batch only (default) |
| `--section-level` | yes | yes (batch path) | no |
| `--validate-slices` | yes | no (returns before analysis) | yes (exit 0 clean) |
| `--decode-keymode` (+ `--seq-*`) | yes / yes | no (returns early) | no |
| `--decode-chords` (+ `--chord-*`) | yes / yes | no (returns early) | yes (`--decode-chords`, exit 0) |
| `--ignore-declared-mode` | yes | yes (key inference floor) | no |
| `--diagnose-measures` | yes | replaces output | no |
| `--dump-key-candidates` | yes | replaces output | no |
| `--dump-cadence-anchor` | yes | append-only (regions unchanged) | yes (append-only confirmed) |
| `--dump-modulation` | yes | append-only | no |
| `--dump-tonicization` | yes | append-only | no |
| `--dump-l5` | yes | append-only | no |
| `--dump-joint-key` | yes | append-only | no |
| `--joint-key-wiring` | yes (marked HELD) | **yes** (intentional, default OFF) | no |
| `--dump-fullspine` (+ `--fullspine-no-override`, `--dump-l6`, `--dump-progressions`) | yes | no (returns early, own side file) | no |
| `--dump-region-keymargin` | yes | no (returns early) | no |
| `--dump-fanout` | yes | no (returns early) | no |
| `--dump-joint-probe` | yes | no (returns early) | no |
| `--dump-vl` | yes | no (returns early) | no |
| `--reachback-ab` | **NO** | no (returns early; HELD) | no |
| `--key-in-neither / -keysig-only / -candidate-only / -both / -leading-tone` | **NO** | no (decode-only local copy) | no |

Establishment note: **no flag silently changes the serialized standard corpus.** Every
output-altering flag is either documented as such or default-off; the append-flags leave
`regions[]` byte-identical (verified: `--dump-cadence-anchor` shared a 38283-char prefix
with the no-flag output — the whole file minus its closing bracket). The undocumented
flags (`--reachback-ab`, `--key-in-*`) are the only code→doc contract gaps and are
default-off diagnostic-only paths. Most diagnostic flags were **not exercised** this
session; an unexercised mode is unestablished-by-this-session (some are exercised by the
prior-session Python instruments and by `test_batch_analyze_regressions.py`).

## 5. Contract check (P3, both directions)

- **doc → code (BUILD_AND_TEST.md / printHelp / `docs/scoring_model.md` presets):** every
  documented command and flag is delivered by the code — `--help`, `--preset`,
  `--section-level`, `--dump-regions`, and all flags listed in `printHelp()` resolve to a
  parsed branch. The harness's own regression contract (`test_batch_analyze_regressions.py`,
  BUILD_AND_TEST.md §2) PASSES at HEAD. **One broken example:**
  `buildtest-lowercase-preset-example` (§3.8).
- **code → doc:** six flags the code parses have no help entry
  (`flags-absent-from-help`, §3.3). Everything else the code does is documented.

## 6. Behavioral characterization (P4)

- **Reproduce-check (the harness end-to-end establishment): PASS, byte-identical.** A
  scratch regeneration of all three presets (`run_bach_preset.py` → scratch dir, corpus
  input read-only from `tools/corpus`) produced `.ours.json` whose per-score SHA-256
  matched the committed `tools/corpus/{baroque,jazz,default}` manifests exactly —
  **352/352 each preset, 0 mismatches**. The current binary (rebuilt at HEAD, size
  50006016 — the committed corpus was made with a 49971712 binary) reproduces the corpus
  bit-for-bit, so the harness is established end-to-end despite the binary rebuild.
- **Harness regression test PASS** (`test_batch_analyze_regressions.py`, exit 0,
  "batch_analyze regressions passed").
- **Probes on `bwv253`:** standard run exit 0 (via `_Exit`); `--preset jazz` (lowercase)
  exit 1 with the correct error; unknown `--preset Foo` exit 1 (strict validation);
  `--validate-slices` and `--decode-chords` exit 0 (diagnostic returns did not crash);
  `--dump-cadence-anchor` append-only confirmed; standard output CRLF vs diagnostic LF
  confirmed.

## 7. Withheld-file open log

Through the Task-3 freeze (`708d0c3708`): **NO withheld file was opened.** The blind pass
used only the allowed safe reads (`CLAUDE.md`, `cowork_audit_protocol.md`,
`BUILD_AND_TEST.md`, `tools/param_manifest.json`, the committed corpus manifests, the raw
inventory tables + `manifest.json` + `pass1_partition.json` under `tools/audit/l5/`, the
two parent audit instructions `cc_instruction_l5_audit_pass1{,_instruments}.md`, and the
source itself).

**First opened AFTER the freeze (Task 4, in this order):** `DEFECT_TYPES.md`,
`OPEN_ITEMS.md` (full — the deferred mandatory read), `STATUS.md` (prepend area); the
three earlier layer-5 session reports' substance was absorbed via the register rows
(OI-116, OI-118…OI-133) and the STATUS prepend blocks that summarize
`cc_l5_audit_pass1_resolver_report.md` / `cc_l5_audit_pass1_instruments_report.md` /
`cc_l5_audit_pass1_grading_fitting_report.md`; `docs/score_inventory.md` §C3 and
`tools/corpus_registry.json` were read for the OI-134 comparison. The working-tree carry
`cowork_joint_key_chord_design.md` was neither read nor touched.

## 8. Reconciliation with the existing register (post-freeze)

No blind verdict changed on unblinding. The six row-anchored + two doc-adjacent findings
map onto existing DEFECT-TYPES and sit as new siblings of tracked rows — none is a
correctness bug, consistent with every prior layer/instrument pass:

- **default-preset-mode-priors-hand-copied** + **onset-boundary-threshold-hardcoded** →
  **DT-3** (value-copied constants), siblings of OI-63 (mode-prior single-sourcing),
  OI-92/OI-97/OI-111/OI-126/OI-132. New rows: **OI-135**.
- **flags-absent-from-help** → **new type DT-25** (undocumented capability on a shared
  instrument — the reverse of DT-17). New row: **OI-136**. (Adjacent to OI-105(b), a
  param_manifest doc-drift about batch_analyze, but distinct — that is a stale claim, this
  is a total help omission.)
- **standard-output-crlf-vs-diagnostic-lf** + **exit-path-asymmetry** → establishment /
  reproducibility observations (#16), latent (byte-identity holds, exit codes clean this
  session). New row: **OI-137**.
- **param-manifest-line-refs-stale** + **buildtest-lowercase-preset-example** +
  **inventory-overcapture** → **DT-12** doc-sync + audit-tooling fidelity, siblings of
  OI-105/OI-107/OI-112 and OI-95/OI-127(e). New row: **OI-138**.

The **reproduce-check byte-identity** completes the establishment picture the
regression-stop instruments began: partition-2a proved the regression-stop pair
reproduces byte-identically, partition-2b proved the fitting harness green and the
calibration maps byte-identical; this session proves the **producer** (the harness that
writes every `.ours.json` those instruments grade) reproduces the committed corpus
bit-for-bit. The corpus-generation chain is now established end-to-end at HEAD.

## 9. OI-134 — the `tools/tools/corpus/` debris stem-set comparison (READ-ONLY; nothing deleted)

Per Task 4.2. The untracked debris directory `tools/tools/corpus/` (gitignored via
`.gitignore:31 /tools/tools/`) holds converted copies of the music21 package's own Bach
corpus — the mangled filenames literally encode the source path
(`C__Users_vince_AppData_Local_Python_…_music21_corpus_bach_bwv<stem>[.mxl].xml`),
physical evidence of the DT-24 / OI-130 destructive-default hazard firing historically (a
batch run from inside `tools/` with a relative default output path nested a second corpus
tree).

Mechanical diff (real stems parsed out of the mangled names) vs the project's committed
`tools/corpus/` (352 stems) and the documented exclusion filter:

| finding | result |
|---|---|
| project committed Bach stems | 352 |
| debris distinct Bach stems | 405 |
| **all 352 project stems present in the debris?** | **YES** (debris is a strict superset) |
| Bach stems in debris but ABSENT from the 352 | **53** |
| non-Bach debris stems | 22 Beethoven string-quartet movements + `bwv846` (WTC prelude, not a chorale) |

**Is each exclusion documented?** YES — at the RULE level, in
`docs/score_inventory.md` §"Chorale-selection provenance (audit C3)": the corpus is
"music21's bach corpus filtered by `_is_bach_chorale` (has `bwv`, not a variant suffix,
not a non-chorale BWV, exactly 4 SATB parts) — the `410 → 353` filter." Spot-checks of the
53 extras confirm the rule accounts for them: the variant-suffix extras
(`bwv248.12-2`, `bwv36.4-2`, `bwv248.23-2`, `bwv248.42-4`, `bwv248.59-6`, `bwv248.64-6`,
`bwv248.9-1`) are excluded by "not a variant suffix"; `bwv846` by "not a non-chorale BWV."
The 22 Beethoven movements are a different repertoire entirely (not Bach-chorale-corpus
candidates). No stem is excluded WITHOUT a documenting filter rule; the exclusion is
documented as the filter, not per-stem. *(A pre-existing documented count wobble — §C3
says the filter yields 353, `corpus_registry.json` says 352, `tools/corpus/` currently
holds 352 — is noted in §C3 itself, "the +1 is not separately logged"; not a harness
finding.)*

**Conclusion for the user's OI-134 question ("debris, or scores we should have been
using?"): DEBRIS.** (a) Nothing unique is lost by deletion — the canonical source is the
music21 package itself, encoded in the filenames. (b) The 53 extra chorales are
DELIBERATELY excluded by the documented `_is_bach_chorale` filter (variant suffix /
non-chorale / SATB-part-count), not accidentally omitted. (c) Any corpus expansion is a
Stage-5 decision (§C3 "Corpus-expansion / cross-validation is a Stage-5 decision; do not
silently treat one as a superset of the other"; relates OI-38/OI-39). **The deletion and
any onboarding implication remain the user's decision on these facts; nothing was
deleted.** OI-134 updated with this result.

## 10. Register, status, and push

- **New register rows (this commit): OI-135, OI-136, OI-137, OI-138.** **DT-25** promoted
  into `DEFECT_TYPES.md`. **OI-116** updated (partition 3 DONE → **the L5 pass-1 is
  complete across all populations**; pass 2 + certification owed). **OI-134** updated with
  the §9 comparison. **OI-84** carries the "L5 pass-1 complete" note.
- **The reproduce-check is the highest-value establishment result** and is clean; there is
  **no highest-rank stop-and-report finding** on the harness.
- **Plainly stated:** the FIRST PASS of the final (L5 + instruments) audit is COMPLETE.
  The whole-scope SECOND pass (the catalog signature sweep + the P6 error rate) decides
  the certification proposal. Certification is NOT proposed here.
- Push: fork (`origin`) only; `upstream` push confirmed still disabled; the pushed hash is
  recorded in STATUS.md's prepend.
