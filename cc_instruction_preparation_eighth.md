# CC dispatch — the preparation phase's EIGHTH batch: the four sittings of 2026-08-17 EXECUTED — the two-span archive, the residue's discards and keeps, the census under the ruled kinds, and the sort recorded

> **Dispatch (Cowork, 2026-08-17).** Executes the rulings of the four sitting records of
> 2026-08-17: `cowork_rulings_2026_08_17_seventh_return.md` (§1 only — §§2–3 rule standing
> states and order no act), `cowork_rulings_2026_08_17_residue_sitting.md`,
> `cowork_rulings_2026_08_17_callers_sitting.md` and
> `cowork_rulings_2026_08_17_rulings_sort_sitting.md`. **Written at a verified stop; nothing
> else is running.** The archiving wave for whole documents, the empirical findings ledger,
> the fact-gate, the curated boot list, the mining, the landing of the newly visible files,
> and every act on any candidacy the regenerated census may flip are NOT in this batch.
> **Read first (every session):** `C:\s\MS\CLAUDE.md`, `C:\s\MS\STATUS.md` and
> `C:\s\MS\BUILD_AND_TEST.md` as they now stand; an archive companion is reached only when a
> decision is re-opened. **Then read, in full:** the FOUR sitting records named above (the
> whole authority for this batch); `cowork_rulings_2026_08_17_sixth_return.md` and
> `cowork_rulings_2026_08_16_preparation_return.md` §§3–6 (the discard mechanism, the
> sole-carrier guard, the STANDING-check treatment and the ruled archivability test this
> batch executes under); your own `cc_report_preparation_seventh.md` §§2–4 and §7 (the
> residue-move pattern, the census-pin precedent, F40–F43);
> `tools/audit/claude_md_finer_spans.json` and
> `ratification_surfaces/cowork_claude_md_finer_split_2026_08_17.md` (Task 1's ruled spans);
> `tools/audit/ratified_document_check.json` and `tools/audit/sole_carrier_subclass.json`
> (Task 2's populations); `tools/audit/rulings_sort_classification.json` (Task 4's subject).
> The standing bars bind this batch whole: **no `src/` edit, no golden, no test changed,
> moved or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the
> analysis, no design, no repair, no derivation of any specification, no mining, no document
> archived or deleted AS A FILE, no open-items row created, flipped or discarded.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the
> end; record both states.** The expected start state: **65 run, 64 passing, ONE failing —
> `gen_filing_convention_application.py --check` ([[OI-372]]) — 4 not run, 16 historical,
> zero STOPs.** **A different start state is a STOP-and-report.** Verify every commit at the
> object and record the SHAs in the close. The reserved-word conventions bind all new prose.
> **The new-tool rule stands. The E-ordering rule stands. F3's reading rule stands**
> (`gen_cluster_dispositions.py --verify` the drift authority). **Every guard run is taken
> at a quiet tree** (the seventh batch's discipline).

## Ruling ledger (quoted at the records, not paraphrased)

- **Seventh-return §1:** *"Two spans ARCHIVE, with a dated pointer at each site; the four
  contested proposals are REFUSED and their spans STAY AT SITE ... the executing act derives
  them from `tools/audit/claude_md_finer_spans.json` at the surface's own commit
  `cfb69a7ecb`, never retyped ... A span that does not read at execution time as this ruling
  assumes is a STOP-and-report, never a forced move."*
- **Residue §§1–4:** *"All 29 ride the soft-discard, and each is carried into the framework
  phase's candidate enumeration as an UNTRUSTED CANDIDATE"* ... *"All 47 join the keep side,
  each with its deciding act recorded as the document-ratification act the check located"*
  ... *"the nine ... join the soft-discard under the same provisions as the 194 ... D-069 is
  carried ... exactly as Ruling 1 carries the 29"* ... *"The six stay on the keep side on
  the recovery pass's original quoted acts."*
- **Residue §5:** *"keep side 411 + 10 + 47 + 6 = 474; discarded 165 + 29 + 9 = 203; 474 +
  203 = 677 ... the live register falls 512 → 474. A derivation that does not reconcile to
  these sums is a STOP-and-report, not an adjustment."*
- **Callers §§1–4:** *"All nine are ruled ENUMERATOR-class on the published evidence ...
  Their namings are published as data and hold nothing"* ... *"a prose citation does NOT
  hold a candidate"* ... *"The 1,213 namings outside the ruling's three kinds ... hold
  nothing"* ... *"A tool reading the file by name, and a mandatory-read or boot listing,
  HOLD under every part of this sitting."*
- **Sort §§1–3:** *"The 220 proposed DESIGN-INTENT entries are the framework phase's seed
  list as proposed"* ... the 60 placements of §2 (24 DESIGN-INTENT / 36
  IMPLEMENTATION-MANAGEMENT, the identities listed there) recorded *"as USER-RULED
  placements, distinguished from the rule's own routes"* ... *"No repair is ordered"* for
  the `nonspec_kind` header disagreement.

## Premise ledger

*(Every FACT pins content at content-addressed objects; no premise names a terminus commit
or asserts a changed-passage count.)*

- **FACT** — the seventh batch is complete, pushed and verified at the objects and at the
  values by the writing side; its end-state guard shape is the start state declared above.
- **FACT** — the four sitting records exist on disk untracked, written by the writing side
  on 2026-08-17; the rulings they carry are quoted above at the records.
- **ASSUMPTION A1** — the working tree carries exactly ONE tracked modification:
  `cowork_handoff.md` (content: the twenty-fourth session-close block inserted above the
  twenty-third, and the twenty-third heading carrying its demotion marker — two parts of one
  act). Untracked: the FOUR sitting records and this dispatch. **Check ordered (Task 0,
  first act, blob against blob by explicit hash):** any tracked difference whose CONTENT is
  outside that shape is a STOP-and-report.
- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero
  STOPs. **Check ordered (each task's last step):** any other red is classed by its measured
  cause; one outside the acts' own subjects is a STOP-and-report. The known in-flight
  reds this batch itself causes and must clear by its own ruled treatments: censuses and
  inventories whose subjects this batch's acts move (the F35 class), and any kind-2 authored
  table whose judgment subjects this discard retires (treated by the RETIRED-SUBJECTS
  pattern of `cowork_rulings_2026_08_16_preparation_return.md` §6, its second application —
  membership DERIVED and published, a judgment whose membership cannot be derived STOPS).
- **ASSUMPTION A3** — each of the two ruled `CLAUDE.md` spans still reads, whole, as the
  ruling assumes (a declined-alternatives note; principle #21's preserved former wording).
  **Check ordered (Task 1, read-before-move):** a span that does not stays at site, is
  flagged, and is a STOP-and-report (the ruling's own words).

## Task 0 — land the four sitting records, the handover block and this dispatch

1. **A1's check first, entirely at content-addressed objects.**
2. Commit exactly these SIX paths and no seventh: `cowork_handoff.md` (modified),
   `cowork_rulings_2026_08_17_seventh_return.md`, `cowork_rulings_2026_08_17_residue_sitting.md`,
   `cowork_rulings_2026_08_17_callers_sitting.md`,
   `cowork_rulings_2026_08_17_rulings_sort_sitting.md`,
   `cc_instruction_preparation_eighth.md` (all new). Staged plainly; push; verify at the
   object.

**Registered expectation E0:** exactly 6 paths at the enumeration; one modification whose
content matches A1's stated shape, five additions; no staging override.

## Task 1 — the two-span `CLAUDE.md` archive, in ONE commit

1. The two ruled spans — the ones `tools/audit/claude_md_finer_spans.json` at commit
   `cfb69a7ecb` lists at lines 53–60 (845 characters, `defense-and-declined-alternatives`)
   and lines 194–213 (1,905 characters, `preserved-former-wording`) — derived from the
   artifact, located in the CURRENT file by content (the pinned line numbers are the
   artifact's coordinates, not an instruction to count lines), READ WHOLE (A3), and moved
   verbatim to `CLAUDE_ARCHIVE.md` on the established pattern, a compact dated pointer at
   each site.
2. Both-directions reconciliation proven on the established pattern (each moved span
   byte-present in the companion exactly once and absent from the parent; moved + kept
   accounts for the pre-act committed blob to the character). **The four REFUSED spans are
   not touched and nothing else in `CLAUDE.md` moves.**
3. Anchors re-aimed per citation from the drift report (`gen_cluster_dispositions.py
   --verify` the authority); affected artifacts regenerated under the ordinary bound
   (movement only in line coordinates or populations whose subject is a moved span);
   commit, push, verify at the object.

**Registered expectation E1:** the reconciliation proves both directions; the archived
total is exactly the two ruled spans' characters; every re-aimed anchor verified at its new
coordinate; no verdict, gate, cut or population moves.

## Task 2 — the residue's discards, carries and keeps, in ONE commit

1. **The discards:** the 29 sole-carrier withholds (membership derived from
   `tools/audit/sole_carrier_subclass.json`) and the 9 SUBJECT-NOT-FOUND-THERE entries
   (membership derived from `tools/audit/ratified_document_check.json`) retire into the
   register data file's retired block by the register's own mechanism, byte-preserved,
   each discard record carrying its finding, date and reason AND the ruled clause verbatim:
   *"a provenance verdict, not a judgment on soundness or usefulness; the statement stands
   at its home and is met by the derivation."*
2. **The carry:** a NEW generated artifact publishes the framework phase's untrusted-candidate
   list — the 29 plus D-069, exactly 30, each with its entry's title, restatement, recorded
   home and the sitting that carried it — under the new-tool rule with a `--check` form
   (the list re-derives from the register data and the two population artifacts; a
   hand-edit fails it).
3. **The keeps:** the 47 SUBJECT-IN-RATIFIED-DOCUMENT entries gain their deciding act —
   the document-ratification act the check located, recorded per entry from the check
   artifact's own quoted match; the 6 NO-DOCUMENT-RATIFICATION-ACT entries gain theirs from
   `tools/audit/deciding_act_recovery.json`'s original quoted acts. The filter
   classification's rendered views regenerate accordingly.
4. **The arithmetic, reconciled as a STOP:** keep 474 / retired 203 / total 677; the live
   register 512 → 474. Kind-2 authored tables whose judgment subjects retire are treated by
   the ruled RETIRED-SUBJECTS pattern; affected artifacts regenerate under the ordinary
   bound; commit, push, verify at the object.

**Registered expectation E2:** the sums reconcile exactly; every retired entry
byte-present in the retired block; the candidate artifact lists exactly 30 and its check
passes; every keep's recorded act quotes and locates a real passage, spot-checkable at the
artifact.

## Task 3 — the census regenerated under the ruled kinds, in ONE commit

1. `gen_retirement_caller_check.py` taught the four rulings AS USER-RULED inputs,
   distinguished in the artifact from derived classifications: the nine named callers
   ENUMERATOR-class (the callers record §1's identities, membership derived from the record,
   never retyped); a prose citation holds nothing; a naming outside the three kinds (a data
   record) holds nothing; a tool-read and a mandatory-read or boot listing hold. A future
   caller whose kind cannot be derived still STOPS to the user.
2. Regenerated at **Task 2's commit** (the content-addressed pin, the seventh batch's
   precedent now specified rather than departed to); every movement against the previous
   committed reading enumerated and classed in the ruled classes — the split's effect, the
   discard's residue (THIS batch's Task 2 among its causes, separated and published), the
   tree's ordinary growth. **A movement fitting none is a STOP-and-report.**
3. The check run and passing at the new pin; verdicts re-derive with holds resting on
   tool-reads and boot listings alone; commit, push, verify at the object.

**Registered expectation E3:** the check PASSES at the new pin; the movement artifact
classes every difference with zero unclassed; every set-aside naming stays published;
verdict flips (if any) are enumerated and confer CANDIDACY only.

## Task 4 — the sort's ruled placements recorded, in ONE commit

1. The 60 USER-RULED placements (the sort record §2's two identity lists: 24 DESIGN-INTENT,
   36 IMPLEMENTATION-MANAGEMENT) recorded into
   `tools/audit/rulings_sort_classification.json` as a distinct route — *"USER-RULED at the
   2026-08-17 sitting"* — beside the rule's own routes, membership derived from the sitting
   record, never retyped; the ratification of the rule-placed 351 recorded in the artifact's
   own header.
2. The rendered surface regenerated; totals 244 / 167 / 0 reconciled against 411 in both
   directions as a STOP; the `nonspec_kind` header disagreement left exactly as found.
3. Commit, push, verify at the object.

**Registered expectation E4:** the totals reconcile; every USER-RULED placement carries its
route marker; the surface re-derives from the artifact byte-identically under its own check.

## Task 5 — the close

One `STATUS.md` pointer entry per task — and, per the standing Ruling-4 bound, the same act
moves the seventh batch's entries to `STATUS_ARCHIVE.md` (`gen_status_batch_bound.py`). The
FULL close appended to `cowork_away_returns.md` (a PREPARATION EIGHTH BATCH section) — both
guard states, every SHA, every expectation graded, every A3 flag, every problem declared,
the carried findings restated for the retrospective (F1–F43, the E3 ordering defect, the A1
premise error — surfaced, none rowed; the row bar stands whole this batch). The report
`cc_report_preparation_eighth.md`, an ordinary tracked file. Commit, push, verify at the
object. **The E-ordering rule:** the end-state guard run is taken AFTER the Task 5 commit
exists, its output and the final SHAs landing in ONE FURTHER commit.

**Registered expectation E5:** at the tree carrying the close, a fresh full guard run
re-derives — **every run check passing except [[OI-372]]'s tool**, zero STOPs — run and
read, not inferred, its values committed only after the run.

## What this batch does NOT do

- **No fate beyond the two ruled spans** — the four refused spans stay; no further
  `CLAUDE.md` movement of any kind.
- **No candidacy acted on** — verdict flips at the regenerated census confer candidacy
  only; the archiving wave waits for its own dispatch behind its ruled conditions
  (mined-first; members-seen-by-the-user-first for the stray root files).
- **No mining, no curated boot list, no empirical findings ledger, no fact-gate admission;
  no completion claim about the superseded phase-1 program; no derivation, design, repair
  or pilot act; no `src/` change, golden, test, corpus or robust-stop movement; no
  measurement of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay
  exactly as found; [[OI-179]] stays OPEN and GATES; F3 stays surfaced, unfixed and
  unrowed.
