# CC dispatch — land the sizing-brief rulings and the RULED sizing brief; touch nothing else; boot NO session

> **Dispatch (Cowork, 2026-08-24, the forty-fifth Cowork session).** Executes §5 of
> `cowork_rulings_2026_08_24_sizing_brief_sitting.md`. **Written at a verified stop: the
> manifest-prose batch's four commits are landed and verified, and nothing is running.** The tip
> was read by the writing side with `git for-each-ref` at the explicit hash `3fbbcb5b5d` and at
> nothing else.
>
> **★ WHAT THIS BATCH IS — AND IT IS THE SMALLEST OF THE ARC.** Task 0 lands FOUR paths and the
> regenerated membership artifact, and PUSHES. Task 1 closes. **NO GENERATOR EDIT. NO PACK
> RE-RENDER. NO SESSION BOOTED. NOTHING DERIVED, NOTHING COMPARED.** The ordered structure yields
> **THREE commits**: Task 0, the close, the end state.
>
> **★ THE BRIEF IS THE WRITING SIDE'S TEXT AND YOU DO NOT EDIT IT.** It arrives REFRESHED to the
> ruled state — banner RULED, its §3 carrying the ruled annotated-score file list, its §7 carrying
> the fixed output name, its §8 all-ruled, its §9 carrying a dated amendment note. **You land it as
> delivered. A defect you find in it is a STOP-and-report, never a fix.** Read it at its status
> banner only; nothing in it is yours to execute.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md`
> — CONDITIONAL, and this batch MEETS the condition (it runs the guard set): read it in full; rule
> (a)'s `gating_ids` only. **(2)** `cowork_handoff.md`, ITS CURRENT (FIFTY-FOURTH) ENTRY. **(3)**
> `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)**
> `cowork_rulings_2026_08_24_sizing_brief_sitting.md`, whole — this batch's authority; **its Ruling
> 4 is the reason this batch does NOT open the generator, and you do not open it.** **You do NOT
> read:** `docs/scoring_model.md`; the brief beyond its status banner; either blind output; any
> rendered pack member; any oracle document; `tools/audit/gen_derivation_boot_pack.py`.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS built,
> designed, scoped or run, no design, no repair, no derivation of any specification statement, NO
> SESSION BOOTED, no document archived, moved or deleted AS A FILE, **no open-items row created,
> flipped or discarded**, **no edit to any governing document, to the generator, to either pack, to
> the manifest, to any register entry or to any register source**, **no edit to the brief's
> content**, **no correction of the three unlisted ruling records or of the module docstring** —
> Ruling 4 leaves both as reported facts until the generator is next touched for a substantive
> reason, and this batch is not that.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (CHECK mode) and again
> at the end (write mode, committed as the artifact of a real run). Verify every commit at the
> object by explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and
> `git diff` over the working tree time out on this mount and are denied by the armed guard — use
> the per-path explicit-hash forms named in A1. If staging is refused by a stale zero-length
> `index.lock` with no git process running, establish the cause (D-669), remove it, report the act.
> Plain single-quoted `git commit -m '…'` subjects, written to need no apostrophe. The reserved-word
> and vocabulary rules bind every line this batch writes — including the standing bar that no NEW
> instance of a known collision is introduced (*figure* reserved for figuration; the numeric sense
> is *number* or *value*).**

## Ruling ledger (quoted at the record, not paraphrased)

- **§5 of the sizing-brief sitting:** *"One dispatch — Task 0 lands this record, the REFRESHED
  brief … the modified handoff and the dispatch itself, and regenerates the evidence-pin
  membership; Task 1 closes. No generator edit, no pack re-render, no session booted. After it
  lands and its return is verified, the fresh Cowork session derives the sizing subject under the
  ruled brief."*
- **Ruling 2 of the same record (the staged pairs, for your report's context only — you stage
  nothing):** the three pairs are staged BY NAME to the DERIVING session, the third being the BWV
  301 score with *When in Rome* folder **134**'s analysis, **paired by content and never by
  number**; they are exemplars, not a corpus, and no measurement is built or run over them.
- **Ruling 4 of the same record:** the two known generator-prose defects are **left as reported
  facts**, discharged the next time the generator is touched for a substantive reason.

## Premise ledger

- **FACT** — the branch tip is `3fbbcb5b5d` (parent `41b77c9f92`), subject opening *"end state: the
  guard set run at the tree the close left"*; both refs at it. Read by the writing side at the
  explicit hash on 2026-08-24. **Nothing is unpushed.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 75, passing 74, failing 1
  ([[OI-372]]'s tool), not_run 4, historical 16. **Re-read it yourself at the object.**
- **FACT** — at the tip, `evidence_pin_membership.json` → `ruling_records_read` is 57.
- **FACT** — at the tip, `derivation_boot_pack.json` carries TWO subjects. **This batch predicts it
  BYTE-UNCHANGED**, along with both pack directories, the generator, and every governing document.
- **FACT, measured by the writing side:** `cowork_rulings_2026_08_24_sizing_brief_sitting.md` is on
  disk and absent from the tip, and carries the word `pinned` on no line.
  `cowork_blind_session_brief_scoring_model.md` is TRACKED and MODIFIED against the tip — it was
  landed DRAFT by the previous batch and is refreshed here; its difference is confined to the
  banner, §3's annotated-score paragraph, §7's one name sentence, §8, and an appended note at the
  foot of §9. **§§0–2, §4, §5, §6 and the rest of §9 are byte-unchanged**; check that at the diff
  rather than taking it from here.

- **ASSUMPTION A1 — the working tree, stated by CONTENT, and TAKEN FROM THE TREE.** Tracked
  modifications: **(i)** `cowork_handoff.md` — inserted entries above the committed content, the
  outgoing entry-point heading marked superseded, additions otherwise; **the COUNT of inserted
  entries is NOT asserted here — MEASURE it and report what you find; any count is admitted.**
  **(ii)** `cowork_blind_session_brief_scoring_model.md` — the refresh described in the premise
  ledger. **A modification at any OTHER tracked path is a STOP-and-report** — in particular at the
  generator, the manifest, either pack directory, or any governing document. Untracked and to be
  landed by Task 0: `cowork_rulings_2026_08_24_sizing_brief_sitting.md` and this dispatch
  (`cc_instruction_sizing_brief_ruled.md`) — **two untracked paths.** **Check ordered (Task 0,
  first act):** the per-path explicit-hash forms, and the whole tracked population enumerated with
  the sanctioned enumeration tool.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs. The
  reds this batch causes and must clear: the membership check (Task 0 step 2);
  `gen_session_start_read_size.py --check` at the close. Any other red is classed by its measured
  cause; one outside this batch's own acts' subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration.** **Route A** — the ruling-record count rises by
  exactly ONE (57 → 58), the added name being
  `cowork_rulings_2026_08_24_sizing_brief_sitting.md`. **The brief is NOT a ruling record and is
  already tracked; it must NOT enter route A — if it does, STOP-and-report.** **Route B** — the
  record carries `pinned` on no line. **Route C** — no tool added. Measure against the committed
  blob at `3fbbcb5b5d` before accepting; an additive derived cross-reference caused by the landed
  record's own naming of a ratification surface or of the brief is REPORTED rather than absorbed;
  movement beyond is a STOP-and-report.

- **ASSUMPTION A4 — the guard registry.** No tool added; population UNCHANGED at run 75; failing
  set unchanged; the three other registry artifacts unchanged except run stamps. **`guard_state.json`
  is predicted to carry, in this batch's write-mode run, at most the stdout lines this batch's own
  Task 0 moves** — your predecessor recorded that a check-mode verification run leaves an earlier
  batch's stdout uncommitted, so a stdout line older than this batch is not this batch's doing and
  is reported rather than absorbed.

- **ASSUMPTION A5 — the packs, the manifest and the generator are BYTE-UNCHANGED** by this batch,
  each verified at the object after the last commit. **Any difference at any of them is a
  STOP-and-report**, not a thing to explain.

## The declared start state — at the tree you will meet

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`** STALE — caused by this dispatch's own
   untracked ruling record.

**`gen_guard_state.py --check` opens "STALE vs the run" for the same cause (F83); not a third
failing check.** A THIRD failing verdict is a STOP-and-report.

## Findings in this batch

**No finding number is allocated; the series stands at F88.** Ruling 9's routing binds. **A defect
you meet in the brief is a STOP-and-report. A further instance of the generator-prose class is
NOT to be looked for and NOT to be corrected — Ruling 4 governs, and this batch does not open the
generator.**

## Task 0 — land the record and the RULED brief, clear the membership check, and PUSH

1. **A1's check first, entirely at content-addressed objects**, with the handoff's inserted-entry
   count MEASURED and reported, and the brief's difference read at the diff and confirmed confined
   to the five regions the premise ledger names.
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob BEFORE
   accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `cowork_blind_session_brief_scoring_model.md` (modified — the writing side's refresh),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_24_sizing_brief_sitting.md` (new), and
   `cc_instruction_sizing_brief_ruled.md` (new) — **five paths.** Subject, exactly:
   `record: the 2026-08-24 sizing-brief rulings landed and the sizing brief is RULED; evidence-pin membership regenerated`.
4. **PUSH**; verify at the object.
5. **`gen_evidence_pin_membership.py --check` must PASS.**

**E0:** the five paths; `ruling_records_read` 58 with the brief absent from route A; the check
passing; `origin/master` at the Task 0 commit; **the generator, the manifest and both pack
directories absent from the commit's own path set.**

## Task 1 — the close

1. One `STATUS.md` pointer entry for Task 0; the previous batch's entries move through
   `gen_status_batch_bound.py --apply` at its three declared authored inputs (authored-input
   maintenance, D-648 — licensed here in terms); `gen_session_start_read_size.py` regenerated (A2).
2. The FULL close appended to `cowork_away_returns.md` as a **THE SIZING BRIEF RULED** section —
   both guard states, every SHA, E0 and E2 graded, A1 and A3–A5 graded, declared departures,
   quarantined questions (or none new), the plan lines (**the FRESH COWORK SESSION derives the
   sizing subject under the ruled brief — the user opens it, and nothing stands before it; its
   output lands uncompared by the next ordinary dispatch; then the tests (a)+(b) batch; then the
   sizing record and the framework phase's opening surface, with the ledger owed before that
   phase**), and **the plan's tell in one sentence: did this batch produce anything other than the
   five landed paths and the report? If yes, name it.** The report is
   `cc_report_sizing_brief_ruled.md`; hashes a commit cannot contain point at the git log — **no
   backfill commits.**
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **THREE commits, and
   a correction commit beyond them is declared in BOTH surfaces if one proves necessary.**

**E2:** at the tree carrying the close, a fresh full guard run — every run check passing except
[[OI-372]]'s tool, zero STOPs, the population at run 75 — committed only after the run that
produced it.

## What this batch does NOT do

- **No session booted. No derivation. No comparison. No staging of any score or analysis file** —
  the staging of the ruled pairs is the DERIVING session's own act under its brief, not this
  batch's.
- **`docs/scoring_model.md` not opened. Neither blind output opened. The generator not opened.**
- **No edit to the brief's content; no generator or manifest or pack change of any kind.**
- **No finding number.** [[OI-179]] OPEN and GATES; [[OI-372]], [[OI-374]] stand.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule (c).
  No `src/` change, no golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* **#12** — the brief's unruled regions are preserved byte-for-byte and the
   amendment is recorded at its foot rather than replacing history; **#19** — the brief tells its
   session that the staged pairs are exemplars and not a corpus, so nothing measured is claimed
   from three chorales; **#6** — the annotated pairs have one home, the brief's §3, and the pack is
   untouched by them; **the just-in-time rule and the standing sharpening** — this batch does the
   ruled landing and nothing else, and Ruling 4's deferral is carried into its bars so that no
   generator edit rides along. Conforms.
2. *Conventions:* American English; no self-invented labels; music-theory words in their musical
   sense; towards, never against.
3. *Figures and premises:* the tip, the guard summary and the membership count read at the objects
   by the writing side on 2026-08-24 and ordered re-read; **A1's handoff-entry count deliberately
   left to measurement**, the remedy that worked last batch.
4. *File-tools rule:* the writing side's own departures are in the fifty-fourth handover entry.
5. *Uncertainty:* the one measure-and-report item is A3's possible additive cross-reference;
   everything else is predicted, and a deviation is a STOP.
6. *Re-read from disk before release:* performed over the delivered files, hash-verified after
   re-staging (F74), recorded in the fifty-fourth handover entry.

---

*Provenance: Cowork, 2026-08-24, the forty-fifth session, written at branch tip `3fbbcb5b5d` under
§5 of `cowork_rulings_2026_08_24_sizing_brief_sitting.md`. TOWARDS the ultimate objective and
TOWARDS the guiding principles.*
