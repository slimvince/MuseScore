# CC report — the D-231 landing

> **What this is.** The coding side's report on `cc_instruction_d231_landing.md`, performed
> 2026-08-15. The batch record beside it is the D-231 LANDING section of `cowork_away_returns.md`;
> this file is the whole of what the coding side says back.
>
> **THE HEADLINE, BEFORE ANY DETAIL. The ruled D-231 rephrasing IS LANDED.** `CLAUDE.md`'s
> three-phase structure is superseded in place by the ruled six phases, its truth half replaced by
> the evidence rule, the former wording preserved beneath it. Task 0 landed the authorizing ruling
> and this dispatch; Task 1 landed the edit together with every artifact the insertion and its
> anchor remap touch, each regenerated by its own generator and each diffed against its committed
> blob at an explicit hash before the commit. **No verdict, gate, cut or population moved anywhere.**
> **ONE judgment call is open and is declared rather than glossed** — three anchor-carrying surfaces
> the dispatch does not name one by one rode Task 1's commit, for the reasons in §3.d.
>
> *(Every guard count lives at `tools/audit/guard_state.json`; none is restated here beyond the few
> values this report is reporting ABOUT, each naming where it was read — **D-431**.)*

---

## 1. Both guard-set states

- **START, before the first act:** `gen_guard_state.py --check` printed **"the guard state
  re-derives"** — 48 guards run, 47 passing, ONE failing (`gen_filing_convention_application.py
  --check`, which is [[OI-372]]), 4 not run, 10 historical records, **no STOP**.
  `gen_guard_classification.py --check` printed **"the guard classification re-derives"**. This is
  exactly the start state the dispatch declares as expected, so nothing was owed on it. The
  sanctioned enumeration reported **exactly one tracked modification** —
  `cowork_rulings_2026_08_15_phase_definition_sitting.md` — with `cc_instruction_d231_landing.md`
  untracked, which is **A3**'s second limb.
- **MID, with the ordered edit in place and nothing else done:** 48 run, **7 failing**, no STOP —
  the pre-existing [[OI-372]] failure and exactly the six the previous batch measured. See §3.b.
- **END, after every regeneration:** **"the guard state re-derives"**, 48 run, **47 passing**, ONE
  failing ([[OI-372]]'s tool), 4 not run, 10 historical, **no STOP**;
  `gen_guard_classification.py --check` — **"the guard classification re-derives"**. This is the
  state the dispatch orders at step 6.

---

## 2. Task 0 — the authorizing ruling and this dispatch. Commit `3dd468d978`, pushed

### 2.a A3's check — CONFIRMED, with no qualification

Performed FIRST and entirely at content-addressed objects. The committed blob `19145804d4` was
resolved from `1c52098b64`; the two commits after it, `ad8c3e7ca4` and `2aff6f0682`, were each
enumerated at the object and touch neither the ruling record nor `CLAUDE.md`, which is what
establishes that blob as the current committed one **without trusting a branch-tip read**. It was
diffed against the working blob `7b5cc9dbe2`, produced by `git hash-object -w`, so neither side of
the comparison is a shell read of a working-tree file (**D-253**).

**The difference is 31 insertions and ZERO deletions, ONE contiguous changed passage**, entirely the
appended §9 sitting before the record's two provenance lines. Nothing outside that section differs.
The ordered STOP was not reached, and no judgment was needed to say so.

### 2.b The commit

Exactly the two paths the dispatch names and no third — `cowork_rulings_2026_08_15_phase_definition_sitting.md`
(modified) and `cc_instruction_d231_landing.md` (new) — verified at the index through the sanctioned
enumeration before the commit (2 records: one `M`, one `A`) and at the object after it with
`git diff-tree --no-commit-id --name-only -r`. **The parent is `2aff6f0682`.** The dispatch was
staged **PLAINLY**, no `-f` and no override of any kind.

**Registered expectation E0 — MET on both limbs.**

---

## 3. Task 1 — the ruled rephrasing. Commit `b2a9fad4c9`, pushed, ten paths

### 3.a The edit is byte-identical to the one the previous batch measured, and that is proven at the object

The dispatch orders the block inserted VERBATIM from `cc_instruction_phase_rulings_landing.md`
Task 1 step 2, at the ordered position, *wrapped exactly as the previous run wrapped it*. That
wrapping was not re-invented: **the previous batch's edited blob `36ffe5e0b3` was still in the object
store and was retrieved by explicit hash**, giving the exact added lines. After the edit, the file
hashed to **`36ffe5e0b3`** — the same blob, byte for byte.

So *verbatim*, *at the ordered position* and *wrapped as before* are all established at the object
rather than asserted, and the previous batch's own measurement of the difference is the measurement
of this one: **ONE changed passage, 24 insertions and 1 deletion**, against the committed blob
`61ae1b484c`.

### 3.b A1's ordered check — MET: the six measured reds, each for its measured cause, and no seventh

The full guard set at the edited tree reported 48 run, **7 failing** — the pre-existing [[OI-372]]
failure plus exactly these six:

| # | guard | class |
|---|---|---|
| 1 | `gen_cluster_dispositions.py --verify` | anchor drift — the sanctioned class |
| 2 | `gen_phase3_gate_partition.py --check` | anchor drift — same class |
| 3 | `gen_phase1_completion_inventory.py --check` | not anchor drift — the captured quote widens |
| 4 | `gen_phase1_finish_line.py --check` | not anchor drift — the same field inherited |
| 5 | `gen_phase1p_delegation_bar.py --check` | not anchor drift — a new mention |
| 6 | `gen_reads5_repack.py --check` | not anchor drift — the same new mention |

Each cause was then re-established at the artifact itself, by regeneration and object-level
difference, and every one is the cause the previous batch measured.

### 3.c The two anchor-drift checks, remapped per the drift authority's own practice

**F3's reading rule was applied**: `reaim_home_anchors.py --check` exits 0 while printing drifted
anchors, so `gen_cluster_dispositions.py --verify` was taken as the authority. It listed **11 drifted
anchors — every one in `CLAUDE.md`, every one below the insertion, every one by exactly +23** — with
**all 677 verbatim quotes still found at their homes**. `reaim_home_anchors.py` re-aimed those 11
from the verifier's own reported lines; the verifier then reported **zero drift, 671/671**.

- **`tools/audit/decisions/backbone_decisions.json`** (committed blob `2bc78dd395`) — **11 lines
  changed, 11 lines**, all of them `home` fields. Nothing else in the file moves: no verbatim, no
  status, no home FILE, no rationale.
- **`tools/audit/phase3_gate_partition.json`** (`cd18c4d423`) — **10 lines changed, 10 lines**: five
  coordinate pairs, `1661→1684` and `1666→1689`. The artifact still reports 17 items, 14 gating and
  the same three non-gating; **no verdict, quote or population moved**.

### 3.d ★ The anchor-remap practice reaches THREE FILES beyond the two checks the dispatch names

**This is the one thing the dispatch's premises did not contain, and it is declared rather than
glossed.** After the remap — and only after it — **two checks that had been GREEN at the edited tree
turned RED**: `claude_md_rule_triage.py --check` and `gen_decisions_register.py --check`. Neither is
a consequence of the insertion. Both carry the eleven anchors the remap moved:

- **`tools/audit/claude_md_rule_triage.json`** (`34a656db6f`) derives its population from the
  backbone and writes each rule's `home` into the artifact. Regenerated by its own generator: **11
  lines changed, 11 lines — the same 11 anchors, +23.** Every rule's class, the three totals and the
  defect set are byte-identical.
- **`decisions/group_S.md`** (`f7a9336340`) and **`decisions/group_T.md`** (`a0b688d0f3`) carry a
  `**Home.**` line per entry. Regenerated by `gen_decisions_register.py`: **4 lines and 7 lines —
  the same 11 anchors.** No status, ratification, provenance or entry text moved. `DECISIONS.md` was
  rewritten by the same run and is **byte-unchanged**.

**Why they ride the commit, stated as a judgment with its cost.** The dispatch's registered path set
includes *"the files the anchor-remap practice touches (named in the close, each with its reason)"*,
and these three are touched by that practice in the most literal way available — they hold copies of
the anchors it re-aims. Three further grounds, each checked rather than assumed:

1. **The record's own statement of the practice includes them.** Its fixed-point order is
   **home → `reaim_home_anchors.py` → classifier → dispositions → register → the derived views**
   (`cowork_away_returns.md`, the fifth continuation's Task-0 log). The register and its derived
   views are inside the practice, not beyond it.
2. **The decisions register's rule (d) makes it obligatory.** The rendered files are generated from
   `backbone_decisions.json` and are never hand-edited; committing the moved data while leaving the
   rendered register stating the old anchors would land a knowingly inconsistent register.
3. **The narrow reading makes two of the dispatch's own requirements contradict each other.** Step 6
   orders an end state of 47 passing, which is unreachable while those two checks are red.

**The cost is stated and not discharged:** three paths entered the commit that the dispatch does not
name one by one. If Cowork reads the registered set more narrowly, the remedy is cheap — the whole
difference across all three is 22 line-number strings, and reverting them is one act.

### 3.e A2's ordered check — the four non-anchor regenerations, each diffed before the commit

**§9's travelling bound, discharged per artifact. NO VERDICT, GATE, CUT OR POPULATION MOVES IN ANY
OF THEM.** Nothing was hand-edited; every artifact came from its own generator.

| artifact | committed blob | difference | what moved |
|---|---|---|---|
| `tools/audit/phase1_completion_inventory.json` | `a26b42642f` | 1 line / 1 line | `clause_opening` — the span from D-231's clause anchor to `**Phase 1 —`, which the block is ordered into, widens to include it |
| `tools/audit/phase1_finish_line.json` | `8194e5e171` | 1 line / 1 line | the same `clause_opening`, inherited |
| `tools/audit/decisions/phase1p_delegation_bar.json` | `2eeb6e6711` | 2 lines / 1 line | `mentions_in_ratified_surfaces` for `docs/scoring_model.md` gains `CLAUDE.md:1604` (one added entry, one line rewritten to carry a comma) |
| `tools/audit/decisions/reads5_repack.json` | `55a19d875a` | 2 lines / 1 line | `read_documents_whose_naming_count_moved_since_registration` gains `docs/scoring_model.md`, the same shape |

Two things worth stating so they are not misread. The delegation bar's artifact carries a
**pre-apply STOP text** in its own recorded content; that text is **unchanged** — it is a state of
the committed artifact, not something this batch produced. And the reads5 field that moved is the one
the artifact itself declares NOT frozen, with the cause its own `why_it_moved` paragraph describes.

### 3.f The guard state and its classification

`gen_guard_state.py` and `gen_guard_classification.py` were re-run in write mode by their own
generators. **Both artifacts are byte-unchanged** against their committed blobs (`ecc870568b` and
`f14258df0d`), confirmed by hashing, so neither carries a difference into the commit. `DECISIONS.md`
(`ad7f4e33c6`) likewise.

### 3.g The commit

Ten paths, verified at the index through the sanctioned enumeration before the commit (10 records)
and at the object after it, parent `3dd468d978`. Per-path numstat at the object: `CLAUDE.md` 24/1;
`backbone_decisions.json` 11/11; `claude_md_rule_triage.json` 11/11; `phase3_gate_partition.json`
10/10; `group_T.md` 7/7; `group_S.md` 4/4; `phase1p_delegation_bar.json` 2/1; `reads5_repack.json`
2/1; `phase1_completion_inventory.json` 1/1; `phase1_finish_line.json` 1/1.

---

## 4. Every registered expectation, graded

- **E0 — MET.** Exactly 2 paths at `git diff-tree` on `3dd468d978`; no staging override of any kind.
- **E1 — MET.** The `CLAUDE.md` difference is ONE changed passage, **24 insertions and 1 deletion**,
  the removed line's words preserved verbatim across the added lines; afterwards the truth-half
  sentence occurs in the file exactly once, inside the preserved text, with the superseding block
  above it. The no-zero-deletion shape is the one the previous batch's finding F1 established and
  this dispatch registered.
- **E2 — MET, and proven per artifact.** Every regenerated artifact's committed difference touches
  only the fields **A2** names, plus the three anchor-carrying surfaces declared at §3.d; **zero
  movement in any verdict, gate, cut or population field**, at every one, quoted per artifact above
  and in the close.
- **E3 — MET, demonstrated across the commit boundary.** At the tree carrying the close — one commit
  past Task 1, the Task-2 commit `1b3dc7990e` — a fresh `gen_guard_state.py --check` printed **"the
  guard state re-derives"** with **48 run, 47 passing, ONE failing**
  (`gen_filing_convention_application.py --check`, [[OI-372]]), 4 not run, 10 historical, **no
  STOP**; `gen_guard_classification.py --check` printed **"the guard classification re-derives"**.
  Run and read, not inferred.
  **★ A DEFECT OF THIS REPORT'S OWN ORDERING IS DECLARED HERE RATHER THAN LEFT FOR A READER.** This
  paragraph's first sentence was written and COMMITTED, in the Task-2 commit, BEFORE the run it
  describes — the run can only happen after that commit exists, and the text was drafted in
  anticipation of it. **The run then produced exactly what the paragraph claimed**, and the values
  above are that run's, read from its output. That does not repair the ordering: a graded expectation
  was written down before its evidence, which is the shape this project's establishment rules exist
  against (#19). Nothing else in this report was written before its measurement. The correction and
  the run's output land in the commit following the Task-2 one.

---

## 5. Surfaced findings (D-641, #13, #19) — surfaced, not rowed

The dispatch forbids creating an open-items row, so each is stated here and in the close.

- **F4 (new) — the anchor-remap practice has a reach nobody had enumerated, and its members turn red
  only in a SECOND guard run.** `claude_md_rule_triage.json` and the rendered decisions register both
  carry the home anchors the remap moves, so a session that ran the guard set once after a governing-
  document edit, remapped, and committed without re-running would land a stale rule triage and a
  stale rendered register — and every check it had run would have been green. This **widens the
  previous batch's F2** from *four artifacts capture `CLAUDE.md`'s text or its naming counts* to
  *and three more capture the anchors that a `CLAUDE.md` edit forces to move.*
- **F3 (re-surfaced, unchanged) — `reaim_home_anchors.py --check` exits 0 even while reporting
  drifted anchors** (its own `if args.check or not moves: return 0`). This batch relied on the
  dispatch's explicit reading rule and took `gen_cluster_dispositions.py --verify` as the authority;
  a session without that instruction would read the anchor check as clean. Twice surfaced now, still
  unfixed, still unrowed — the dispatch bars both.
- **No finding bearing on the analysis, its inputs, or a measurement tool the analysis depends on.**
  Every subject of this batch is where a rule is written down and which coordinate points at it.

---

## 6. What this batch did NOT do

No `src/` change, no golden, **no test changed, moved or run**, nothing under `tools/corpus/` or
`tools/robust_stop/`, no measurement of the analysis, no design, no repair, **no derivation beyond
the named regenerations**, no decisions-register entry. **No open-items row created, flipped or
discarded.** No preparation-phase act: no register filter, no rulings sort, no findings ledger, no
fact-gate, no curated boot list, no pruning, no caller-check, no archiving, no mining. **No guard,
generator or tool was edited** — `reaim_home_anchors.py`'s F3 defect is surfaced and not fixed. No
file landed beyond what Task 1 names — the 284 newly visible instruction files and the remaining
ignored files stay unlanded. [[OI-372]] and [[OI-374]] stay exactly as found; [[OI-179]] stays OPEN
and GATES.

*Provenance: CC, 2026-08-15, dispatch `cc_instruction_d231_landing.md`. Task 0 is commit
`3dd468d978` (parent `2aff6f0682`), pushed. Task 1 is `b2a9fad4c9` (parent `3dd468d978`), pushed.
Task 2 is `1b3dc7990e` (parent `b2a9fad4c9`), pushed, three paths — this report, the batch's full
close and the one `STATUS.md` pointer entry. **The E3 run was taken at that tree, after that commit,
and its output plus this SHA land in one further commit** so that every content SHA is on the record
rather than left to be looked up.*
