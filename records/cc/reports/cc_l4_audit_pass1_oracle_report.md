# CC REPORT — Layer-4 (chord) audit, PASS 1, session 2 of 3: the live scoring oracle

> **EG-7 / OI-84 / OI-102.** Blind enumerative first pass over the three LIVE scoring-oracle
> translation units, per `cowork_audit_protocol.md` (P1–P4) and the oracle-session instruction
> (`cc_instruction_l4_audit_pass1_oracle.md`). Freeze commit: `1a11cf7210`. Read-only
> fact-finding — no production behavior changed, no constant tuned, no golden refreshed, no
> reference artifact under `tools/robust_stop/` or `tools/corpus/` touched. Findings are
> register rows, never patches (guiding principle 8).

## 0. Scope, inventory, and the pinned instruments

Three files, from the committed layer-4 inventory (`tools/audit/l4/`, generated at inventory
head `7f57aad4b5`, corpus hash `c50002fee1`):

| File | Population (re-verified at the code) | rows |
|---|---|---|
| `chord/chordanalyzer.cpp` | **LIVE vertical scoring oracle** — `analyzeChord`, templates, score matrices, per-candidate helpers, `buildChordResult`, `deriveChordExtensions`, factory. SURVIVES the engagement; R9 *splits* this file, does not delete it. | 545 |
| `chord/chordanalyzer.h` | **L4 scorer CONTRACT surface** — `kTemplateCount`/`kTemplateIntervals`, `ChordIdentity`/`ChordAnalysisResult`/`RawCandidate`, `IChordAnalyzer`/`RuleBasedChordAnalyzer`/factory; plus declarations that serve the RETIRING post-scoring gates (retirement-coupling noted per row). | 243 |
| `chord/analysisutils.h` | **Cross-cutting pitch/key helpers** — `normalizePc`/`diatonicMaskFromFifths`/`collectionMask`/`ionianTonicPcFromFifths`/`pcInMask`/`endsWith`. SURVIVES (also serves L3/L5). | 67 |
| **Total** | | **855** |

Row count keyed on the inventory `file` column (855). An earlier string-match tally (858)
over-counted three `crosslayer` rows where a chord header appears only as the *target* of an
include from another file (`chordpathdecoder.h`, `sparsechordrefinement.{cpp,h}`) — those are
inbound consumer edges, dispositioned in the consuming files' sessions, and are listed here as
context, not judged. 855 ≈ the planned ~855, so every-row rigor fit this one session; no split
needed (Task 0 feasibility check passed).

Disposition artifact (protocol P1/P2 — a verdict for EVERY row, "no issue" a recorded claim):
`tools/audit/l4/pass1_dispositions_oracle.csv` / `.json`, generated by
`tools/audit/l4/pass1_oracle_dispositions.py` (a total function over the inventory + a curated
override table carrying the specific judgments).

**Disposition summary (855 rows):**

| verdict | count | | verdict_class | count |
|---|---|---|---|---|
| ESTABLISHED (constant) | 490 | | constant | 489 |
| SURVIVES (code) | 268 | | code | 277 |
| PUBLISHED (derived fact) | 83 | | derived-fact | 83 |
| THEORY (premise) | 9 | | premise | 6 |
| FACT (premise) | 5 | | | |

`in_param_manifest` over the 489 constant rows: **25 present**, **14 absent** (3 override-registered
constants + 11 inline hand-set thresholds — see findings), 450 not-applicable (structural /
interval / index literals). Flagged rows: **24**.

## 1. Findings (flagged rows → register rows)

All findings are recorded, not fixed. Plain-language, one sentence each; the code location is
clickable.

### Doc↔code drifts (contract-direction, protocol P3)

- **`bassNoteRootBonus` documented 0.65, code is 0.70.** The struct default is
  `0.70` ([analysistypes.h:177](src/composing/analysis/types/analysistypes.h#L177), applied by
  [appliedBassRootBonus](src/composing/analysis/chord/chordanalyzer.cpp#L867)); `ARCHITECTURE.md`
  §4.1 (line 1289) and §4.1b (line 1373) both say `0.65`. `docs/scoring_model.md` §4 is correct
  (0.70). ARCHITECTURE.md is the stale surface.
- **`ARCHITECTURE.md` `Extension` enum bit order does not match the code.** The doc
  (lines 1227–1244) assigns bits 4/5, 9/10, 14/15 to NaturalNinth/FlatNinth,
  NaturalThirteenth/FlatThirteenth, SixNine/OmitsThird; the code
  ([chordanalyzer.h:213-230](src/composing/analysis/chord/chordanalyzer.h#L213)) has each of
  those three pairs *swapped*. In-memory only (the bitmask is serialized nowhere as a stable
  contract), so no functional impact — a documentation drift.
- **`ARCHITECTURE.md` §4.4 mis-locates and under-lists AnalysisUtils.** It gives the file
  as `analysis/analysisutils.h` (actual: `analysis/chord/analysisutils.h`) and lists only
  `normalizePc`/`ionianTonicPcFromFifths`/`endsWith`, omitting `diatonicMaskFromFifths`,
  `collectionMask`, and `pcInMask` that the file actually exports.
- **`ARCHITECTURE.md` §4.1 broadly predates the refactor-1 split.** It describes Gates
  A–L and the inversion bonuses as living "in `chordanalyzer.cpp`" and references Gate K / Gate M
  and Path-1/2 iteration BIR baselines (21/128) and test counts (407/407); the gates now live in
  `postscoringgates.cpp`, Gate K is retired, and the counts are stale. `docs/scoring_model.md`
  (the authoritative oracle contract per CLAUDE.md) IS in sync; the drift is confined to the
  ARCHITECTURE.md narrative. Recorded as one doc-sync item (not per stale line).

### Parameter-manifest / constant-publication gaps

- **three override-registered oracle constants are absent from
  `tools/param_manifest.json`.** `kWCompletePresenceThreshold`
  ([:142](src/composing/analysis/chord/chordanalyzer.cpp#L142)), `kComplexityEvidenceFloor`
  ([:143](src/composing/analysis/chord/chordanalyzer.cpp#L143)), and `kAugThinEvidenceFactor`
  ([:144](src/composing/analysis/chord/chordanalyzer.cpp#L144)) are registered for the Stage-5
  `--param-override` mechanism (registration block
  [:178-181](src/composing/analysis/chord/chordanalyzer.cpp#L178)) yet do not appear as manifest
  rows, whereas the sibling `kExtensionThreshold` (also frozen) IS enumerated. Two of the three
  (`kComplexityEvidenceFloor`, `kAugThinEvidenceFactor`) multiplicatively shape the score, so a
  Stage-5 fitter reading the manifest is blind to a real part of the override surface — a
  manifest↔code coverage gap under the fact-publication corollary and CLAUDE.md #17(f).
- **a cluster of inline extension/presence thresholds is hand-set, unregistered, and
  partly duplicative.** Bare literals `0.3` ([:355](src/composing/analysis/chord/chordanalyzer.cpp#L355),
  [:375](src/composing/analysis/chord/chordanalyzer.cpp#L375)), `0.2`
  ([:376](src/composing/analysis/chord/chordanalyzer.cpp#L376),
  [:1003](src/composing/analysis/chord/chordanalyzer.cpp#L1003)), `0.1`
  ([:1081](src/composing/analysis/chord/chordanalyzer.cpp#L1081)) and repeated `0.05`
  ([:692](src/composing/analysis/chord/chordanalyzer.cpp#L692),
  [:704](src/composing/analysis/chord/chordanalyzer.cpp#L704),
  [:726](src/composing/analysis/chord/chordanalyzer.cpp#L726),
  [:944](src/composing/analysis/chord/chordanalyzer.cpp#L944),
  [:1190](src/composing/analysis/chord/chordanalyzer.cpp#L1190),
  [:1248](src/composing/analysis/chord/chordanalyzer.cpp#L1248)) sit inside `detectExtensions`,
  `structuralPenalties`, `buildChordResult`, and `analyzeChord`; each has an in-comment
  rationale but none is a named/registered constant, and the `0.05` presence bar duplicates the
  named `kBassSupportPresenceThreshold`/`kWCompletePresenceThreshold` (both = 0.05). These
  thresholds are also **not documented in `docs/scoring_model.md` §4** (which enumerates the
  scoring constants but not the extension-detector's internal bars) — a P3 negative-space gap.

### In-code markers / layering notes (existing-and-known, recorded for completeness)

- **a `// BUG-10` marker sits in `categorizeExtraNote`.** The Diminished branch
  `if (rel == 7) return Contradiction; // P5 → not diminished (requires ♭5) // BUG-10`
  ([:216](src/composing/analysis/chord/chordanalyzer.cpp#L216)) carries a tracked in-code bug
  label. Whether an `OPEN_ITEMS.md`/`DEFECT_TYPES.md` row already covers "BUG-10" is checked at
  unblind (Task 4); recorded here so the marker is not lost.
- **display/voicing declarations live in the analysis contract header.** The
  `ChordSymbolFormatter` namespace (`formatSymbol`/`formatRomanNumeral`/`formatNashvilleNumber`,
  [:797-835](src/composing/analysis/chord/chordanalyzer.h#L797)) and the voicing helpers
  (`ClosePositionVoicing`/`closePositionVoicing`/`chordTonePitchClasses`,
  [:839-862](src/composing/analysis/chord/chordanalyzer.h#L839)) are declared in `chordanalyzer.h`,
  a display-agnostic analysis header (`ARCHITECTURE.md` §2.3). `ARCHITECTURE.md` §4.1i already
  names this as backlog tech-debt; recorded so the audit's total-function coverage reflects it.

### Retirement-coupling and dormancy notes (verdict + coupling, not defects)

- **(note) `inferNextRootPc`, `PostScoringGateContext`, `PromotionTarget`,
  `promoteToWinner`, `applyPostScoringGates`, `applyIter8691Pedal`** are declared on the L4
  contract surface but serve the **retiring** post-scoring gates (roadmap R1) and the
  `chordpostpasses.cpp` tail (L4-RETIRES). They SURVIVE as declarations only until R1; each row
  carries the retirement-coupling note in the disposition CSV. Not a finding — a boundary
  recorded, not crossed.
- **(note) `computeRawFanoutSummary` / `RawFanoutSummary`** compute the pre-cap
  above-threshold ranked-set size that Layer 5 will select over (Engage arc #8 measurement),
  carried in-memory and **never serialized**. This is a declared-dormancy derived fact whose
  future consumer (the L5 carry-selection) is named — published once, consumed by no production
  path yet. Recorded so it is not mistaken for waste.

### Behavioral (protocol P4)

- **the augmented-root correction never fires on the pinned corpus.** In
  `buildChordResult` ([:960](src/composing/analysis/chord/chordanalyzer.cpp#L960)) the
  augmented-root bass-fallback fires only when the winner is Augmented AND its root has **no TPC
  data** (`ctx.tpcForPc[rootPc] < 0`). Measured over 122,047 `analyzeChord` invocations on the
  Baroque corpus its fire count is **0**: the corpus scores are fully spelled (TPC always
  present), so the TPC-absent population is empty. This is not dead code — the branch is a
  fallback for TPC-absent inputs (status-bar / synthetic / catalog paths) — but on the pinned
  corpus its designed population does not occur. Recorded per P4 ("a mechanism that never fires
  is surfaced"), with the benign cause named; the TPC-absent paths that DO exercise it were not
  separately fire-measured this pass.

## 2. Contract-direction check (protocol P3) — results

Direction: from `docs/scoring_model.md` (the authoritative oracle contract), `ARCHITECTURE.md`
§4.1/§4.4, and the roadmap → located in code, or flagged.

- **Template set — IN SYNC.** `kTemplateCount = 17`
  ([chordanalyzer.h:63](src/composing/analysis/chord/chordanalyzer.h#L63)) matches the 17-row
  `templates` array (`static_assert` enforces it, [:1338](src/composing/analysis/chord/chordanalyzer.cpp#L1324))
  and the 17-entry `kTemplateIntervals` table; all 17 rows' intervals match `scoring_model.md` §2
  exactly, and the §2 "(currently 17)" count matches the constant. The size-sync invariant
  (one `kTemplateCount` → templates array + three score matrices + derived `kMasks`) holds, and
  Gate R's masks are DERIVED from `kTemplateIntervals` via `makeTemplateMasks()` — the audit-Q1.3
  drift hazard is closed by construction. **No drift.**
- **Documented terms present in code — all located.** `dim7CharacteristicBonus`, `w_complete`,
  the B2 aug7 dual guard, `complexityFactor`, `augFactor`, `nonBassAdjustment`, the structural
  penalties (`kSus4MissingFourth`/`kSus4VariantMissing7th`/`kSus4Maj7MissingP5`/`kDom7FlatFive*`/
  `kPowerChord3PcPenalty`), `tpcConsistencyBonus`, `diatonicRootContribution`, the tone/extension
  factors, `kContradictionPenalty`/`kForeignPenalty`, and `ScoringPhase` all appear where the
  document says. The oracle is genuinely vertical (Stage 3.3): `rootContinuityBonus`, the four
  §4.1b inversion bonuses, `resolutionBonus`, `w_seq`/`w_dim`/step bonuses, and Gate R live in
  the competition pipeline (`harmonicfunctionlayer.cpp`, L5) and the post-scoring gates
  (`postscoringgates.cpp`, retiring) — not in these three files — consistent with §11.
- **Negative space — the extension detector's internal thresholds are undocumented (the inline-threshold cluster above).**
  `docs/scoring_model.md` §4 enumerates the scoring constants but not the `0.3`/`0.2`/`0.1`/`0.05`
  bars inside `detectExtensions`/`structuralPenalties`. This is the one contract absence.
- **the `bassNoteRootBonus` value drift** is the one load-bearing numeric doc↔code drift; the
  Extension-enum and AnalysisUtils drifts and the §4.1 narrative staleness are
  documentation-only.

## 3. Behavioral characterization (protocol P4) — fire rates on the pinned corpus

The oracle is the LIVE production hot path. Two routes, least-invasive first.

### 3a. Output-observable behavior — committed corpus (zero rebuild)

Route: aggregate the **committed** `tools/corpus/baroque/*.ours.json` (352 pieces, pinned
`git_hash c50002fee1`) — the oracle's winner+alternatives per region.
(`tools/audit/l4/pass1_oracle_dispositions.py` is the disposition generator; the corpus
aggregation is `pass1_oracle_corpus_agg` in the report workpapers.) Over **11,222 analyzed
regions**:

- **Quality-win distribution:** Major 64.8 % · Minor 28.9 % · HalfDiminished 3.7 % · Diminished
  1.0 % · Suspended2 0.8 % · Suspended4 0.7 % · Augmented 0.1 % · Power 0.1 %. **All eight
  non-Unknown qualities win at least once — no dead quality/template.** Augmented (14 regions)
  and Power (8) sit at the floor, exactly as the `augFactor` thin-evidence halving and the
  `kPowerChord3PcPenalty` are designed to produce — the suppression mechanisms bite without
  eliminating the qualities.
- **Sonority-size population (edge cases):** noteCount 1 → 4 regions, 2 → 121 (1.1 % sparse), 3
  → 53.2 %, 4 → 35.9 %, ≥5 → 9.8 %. Single- and two-note regions reach the oracle via the
  greedy-expand relaxation of `minDistinctPcsForCandidate` (1 instead of 3) — the sparse-admission
  edge is genuinely exercised.
- **Inversion / slash:** 36.7 % of regions (33.5 % by duration) are `bassIsRoot = false` — the
  inversion machinery is heavily live.
- **Diatonic:** 93.3 % `diatonicToKey` — the dominant population for `diatonicRootContribution`.
- **Fan-out to L5:** 99.8 % of regions carry ≥1 alternative (61.7 % carry 2, 36.2 % carry the
  cap of 3) — the "up to 3 candidates" contract is populated on essentially every region.

### 3b. Internal-branch fire counts — default-OFF instrumentation

Route: a minimal **default-OFF** fire counter added to `chordanalyzer.cpp` (its own revertible
`feat(tools):` commit), gated on the `MU_ORACLE_FIRECOUNT` env var so the live scorer pays
nothing and touches no shared state when it is unset. Because `batch_analyze` exits its success
path via `::TerminateProcess`/`std::_Exit` (which bypasses `atexit`), the tool calls an explicit
gated flush before terminating; the counters are harvested by a native serial driver
(`tools/audit/l4/pass1_oracle_firecount_run.py`) so the Windows-path env value is not mangled by
the `run_bach_preset` git-bash wrapper. Byte-identity re-proven: the Baroque corpus regenerated
with the instrumented binary is **byte-identical to the committed reference on all 352 pieces**,
and all three test suites stay green (composing 1101, notation 53, pipeline snapshot 11). No golden
refreshed. Aggregated over the full Baroque corpus (`tools/audit/l4/pass1_oracle_firecount.json`),
**122,047 analyzeChord invocations**:

*Per-call mechanisms (evaluated once per `analyzeChord`):*

| mechanism | fires / call | reading |
|---|---|---|
| `jointEnabled` | 99.7 % | regional accumulation is essentially always on (the single-tick path is rare) |
| `legacySingleBass` | 71.6 % | most calls fall back to single-bass; the complement is `bassEnumerated` |
| `bassEnumerated` | 28.4 % | multi-bass enumeration fires when the bass-moves evidence is present — matches its gate |
| `insufficientData` | 0.6 % | returns `{}` (distinctPcs < min) — rare (greedy-expand relaxes the gate) |
| `sparseUpperAmbiguous` | 0.2 % | the Corelli-style sparse-upper fallback — rare by design (a specific edge) |
| `structuralBassFalse` | 0.2 % | `hasStructuralBass == false` (sparse upper "bass") — rare, gates the inversion bonuses |
| `sus2ToSus4` | 1.2 % | the Sus2→Sus4 upgrade in `buildChordResult` — fires but uncommon |
| `susToMajorOmitsThird` | 1.0 % | the Sus→Major(omitsThird) re-qualification — fires but uncommon |
| `augRootCorrection` | **0.0 %** | **never fires on this corpus** — see the never-fires note in §1 |

*Per-cell mechanisms (evaluated per candidate cell — 12 roots × templates × bass candidates,
so counts are naturally large; the reading is whether the rate tracks the intended population):*

| mechanism | fires / call | reading |
|---|---|---|
| `nonBassPenaltyApplied` | 84.3 | per-cell non-bass penalty on Min7/Sus4-4/HalfDim; heavy, as expected for the inner loop |
| `nonBassPenaltyWaived` | 8.0 | the TPC-spelling waiver fires ~8.6 % of the penalty's population — the composer's spelling overrides |
| `sus4MissingFourth` | 33.7 | sus4 templates rarely have their defining P4 → penalised — consistent with sus4 winning only 0.7 % |
| `sus4VariantMissing7th` | 17.3 | sus4♭5/♯5 without m7 → penalised |
| `sus4Maj7MissingP5` / `dom7b5Missing7th` | 8.7 each | the two "missing-defining-tone" penalties on their families |
| `dom7b5TpcPenalty` | 11.7 | dom7♭5 without a confirmed G♭ spelling → penalised (nearly always in Baroque) |
| `power3pcPenalty` | 11.6 | power template with ≥3 PCs → penalised on ~every root — consistent with Power winning 0.1 % |
| `aug7GuardSkip` | 11.6 | the B2 dual guard skips the aug7 template on ~every root (M3∧aug5 rarely both present) — consistent with Aug 0.1 % |
| `augFactorHalved` | 6.0 | the augmented thin-evidence halving fires per augmented cell |
| `wCompleteFired` | 1.2 | root-position complete-triad bonus — fires ~once per call (a 3-PC region has one such root) |
| `dim7BonusFired` | 0.017 (2 080 total) | the dim7 rotation selector fires on diminished cells with a non-diatonic ♭♭7 — tracks Dim winning 1.0 % |

**Every documented mechanism except `augRootCorrection` fires at a rate consistent with its
stated intent** (the suppression penalties fire heavily → Aug/Power/sus4 stay at the win-floor;
`bassEnumerated`+`legacySingleBass` ≈ 100 %; the TPC waiver at ~8.6 % of its population).

## 4. Withheld-file open log (the DT-20 blind discipline)

At the time of the Task-3 freeze commit `1a11cf7210`, **no withheld file had been opened** —
the entire blind pass (Tasks 0–3) ran on the safe-read set only (`CLAUDE.md`,
`cowork_audit_protocol.md`, `BUILD_AND_TEST.md`, `ARCHITECTURE.md`, the roadmap,
`docs/scoring_model.md`, the `tools/audit/l4/` inventory tables + `manifest.json`, the parent
instruction, and the three source files). The withheld set (`OPEN_ITEMS.md`, `DEFECT_TYPES.md`,
`STATUS.md`, the `cc_*_report.md` including the decoder report, the `cowork_*` design/audit docs,
and the `tools/audit/*dispositions*`/`*blind*`/… artifacts) was first opened only AFTER the
freeze, during Task 4 — logged in §5.

## 5. Unblind reconciliation (Task 4)

**Withheld-file open order (all AFTER the freeze commit `1a11cf7210`):** `OPEN_ITEMS.md`
(full — the deferred mandatory session-start read), then `DEFECT_TYPES.md`, then `STATUS.md`,
then `cc_l4_audit_pass1_report.md` (parent) and `cc_l4_audit_pass1_decoder_report.md` (session 1).
No withheld file was opened during Tasks 0–3.

**Reconciliation with the register and the defect-type catalog:**

- **No finding is a correctness bug** — consistent with the parent and decoder sessions
  ("the L3/L4 spine reproduces its documented design"). The oracle behaves as
  `docs/scoring_model.md` describes.
- **No new DEFECT_TYPE.** Every finding maps to an existing catalog row: the manifest/inline
  constant gaps are **DT-2** (+ the `0.05` duplication is **DT-3**); the ARCHITECTURE.md drifts
  and the orphaned `// BUG-10` marker are **DT-12** (with the register-completeness #10 aspect);
  the augmented-root never-fires is **DT-7**; the display-in-header layering is **DT-19**.
- **Register rows added (this commit):** **OI-106** (constant/manifest-publication gaps — the
  DT-2 oracle twin of the decoder's **OI-103**, and of **OI-87/OI-91**; feeds **OI-6/EG-5**,
  relates the hand-set-constant mass **OI-23**), **OI-107** (ARCHITECTURE.md oracle doc drifts,
  DT-12), **OI-108** (augmented-root never-fires + display-in-header, DT-7/DT-19; the never-fires
  is the oracle sibling of the decoder's **OI-104**), **OI-109** (orphaned `// BUG-10` marker +
  the P5-over-diminished scoring question declared to Cowork, DT-12). **OI-102** updated to record
  this L4-2b session done (L4-2c + the whole-layer pass-2 sweep still owed).
- **Neither the ARCHITECTURE.md drifts nor the `// BUG-10` marker were previously tracked**
  anywhere in `OPEN_ITEMS.md`/`DEFECT_TYPES.md` (grep-verified before the freeze) — genuinely new
  rows, not duplicates.
- **Existing rows confirmed, not duplicated:** the oracle-is-vertical / Gate-R-basisDep story
  (**OI-30**) is confirmed by the P3 contract check (the five progression signals have migrated
  out of `analyzeChord` into the competition pipeline per §11); the hand-set-constant mass
  (**OI-23**) is the home for the pre-2026-06-13 tuning history that OI-106(b) points at; the
  template-count sync invariant (17 = templates array + `static_assert` + `kTemplateIntervals` +
  `scoring_model.md` §2) holds from the oracle side, matching the decoder session's confirmation
  from the decoder side.
- **The P6 audit-error-rate and the whole-layer P8 signature sweep are NOT run here** — they
  belong to the layer-4 second pass after all three pass-1 sessions (L4-2c still owed).

## 6. Self-check (guiding principles / conventions / gate policy)

Re-read of every touched diff on disk before reporting done: the instrumentation is default-OFF
and output-neutral (byte-identical corpus ×2, all suites green); no scoring constant was tuned,
no golden refreshed, no `tools/robust_stop`/`tools/corpus` reference touched; findings are
register rows, never patches (principle 8); the self-invented "OF-N" finding labels in an earlier
draft were removed per the no-self-invented-labels convention (findings carry real OI-NNN numbers
assigned at unblind); the known working-tree carry `cowork_joint_key_chord_design.md` was left
untouched; commits stage only named files (no `git add -A`); the fork-only push rule was honored
(`origin` only; `upstream` push disabled and untouched).
