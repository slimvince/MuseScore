# CC report — the COMPARISON: the blind derivation graded at the withheld oracle and at the current text, with NO recommendation on the method

> **Dispatch:** `cc_instruction_comparison_harmony_boundary.md`, performed 2026-08-24, executing
> Rulings 1–4 and §5 of `cowork_rulings_2026_08_24_comparison_design_sitting.md` under Ruling 4(c) of
> `cowork_rulings_2026_08_21_successor_plan_sitting.md` and Ruling 1 of
> `cowork_rulings_2026_08_22_pilot_order_sitting.md`.
>
> **This report carries NO recommendation on the derivation method** (Ruling 4), and no verdict on
> the blind session's independence. The deliverable is
> `ratification_surfaces/cowork_comparison_harmony_boundary_reading.md`; the full close is the
> **THE COMPARISON TABULATED, THE METHOD UNRULED** section of `cowork_away_returns.md`. Where this
> report would name hashes it cannot contain, it points at the git log — no backfill commit was
> written, and the declined resolution stays declined.

---

## 1. What was done, in one paragraph

The blind derivation's file was read whole as this session's very first act, before the session-start
read, by Ruling 2. Its population was then re-counted at its own structure. Each statement was graded
on two axes in the closed vocabulary — against the withheld oracle at both its arms, and against the
current specifications and code as evidence about the present text — and the ten open questions were
listed ungraded with one sentence each on what would settle them. The blind session's independence
record was relayed as facts with no verdict word attached. The result is one reading file. Nothing
was recommended, nothing was booted, nothing about the analysis was measured, and nothing the
comparison grades was edited.

## 2. Task 0 — the landing, the membership, the push

**A1's check was taken as the first act after the ordered first read, entirely at content-addressed
objects,** because the bare working-tree forms are denied by the armed guard and time out on this
mount.

1. **Exactly ONE tracked modification, and it is the one A1 names.** The population was ENUMERATED
   with the sanctioned enumeration tool rather than sampled: `cowork_handoff.md`, and no other
   tracked path anywhere in the tree.
2. **`cowork_handoff.md` — one hunk at the file's fourth line, additions only** but for the outgoing
   entry-point heading, which returns verbatim as the last inserted line with the
   superseded-as-entry-point clause appended. The shape A1 states holds in full.
3. **Both named untracked paths present and both absent from the tip**, each checked at the object.
4. **The premise ledger re-checked at the objects:** the tip and both refs; the guard summary at its
   own artifact; the committed membership artifact's ruling-record count; and the blind output's
   blob, recorded size and piped content hash, all matching the ledger.

**The membership was regenerated and measured against the committed blob BEFORE it was accepted, from
every route.** Route A moved by exactly the predicted amount with exactly the predicted name; route B
added nothing, the landed record carrying on no line the word that route matches on; route C is
unmoved, this batch adding no measurement tool. The measured difference is two hunks and nothing
else.

The four ordered paths were staged, enumerated as staged, committed under the exact ordered subject,
pushed, and verified at the object; the enumeration was re-taken at the commit. **`gen_evidence_pin_
membership.py --check` passes at the resulting tree.**

## 3. Task 1 — the comparison

**(a) The population, established at the graded file's own structure.** The statement count and the
open-question count were taken at the file and are stated in the reading file's manifest header;
they are not restated here (**D-431**). The deriving session had relayed the same two counts; this
session re-counted rather than carrying them, as ordered.

**(b) The grading.** Every statement carries, in the output's own order: its own words compressed to
statement, defense class and source class; the oracle text it is graded against, or which arm is
engaged and why, or that neither speaks; exactly one verdict on the oracle axis; and exactly one
verdict on the current-text axis with its specification or code location cited. **No row was
delivered UNGRADED.** The second arm's engagement is stated per row. The distribution of verdicts is
counted at the reading file's own rows and lives only there.

**How the oracle axis was applied** — stated in the reading file so every row is re-gradeable: a
verdict other than SILENT is recorded only where the oracle's own text makes a claim the statement
reproduces, departs from, or contradicts; where the oracle is engaged by the subject without making
a claim about it, the row records SILENT and says what the arm names.

**(c) The caveats.** The D-450/D-575 caveat was applied and **its trigger was met on no row** — the
one statement citing the notated key signature denies rather than asserts that the signature is a
boundary cue — and that is recorded as considered rather than passed over. Salvage-classed rows
carry their cited identifiers and are graded like any other. The three ruled annotated pairs were
available to be opened and cited; **no row required opening one, and nothing was run over them.**

**(d) The open questions** are listed ungraded, each with one sentence on what would settle it and
whether the oracle or the current text already does; the sentence is information, not a grade.

**(e) The independence record** is relayed in its own section — the stop-on-meeting record's positive
statement, the three disclosures, the files consulted, the input finding, the self-check the output
records, and a pointer to its sizing record's values — **with no verdict word attached**, and with
the judging left to the user at the record.

## 4. What was established at the untrusted sources — recorded, NOT graded

Opening the current specifications and code per statement established five things about the present
text. **No finding number is allocated — the series stands at F88 — and no open-items row is created;
both are barred by this dispatch. None is graded as a defect, and the reading file states in terms
that a DIFFERS row is not a defect finding.** They are named here so the writing side can see them
without re-reading the tabulation, and each is carried in its own row:

1. The production event lattice is built from the TIE-UNRESOLVED notated atoms, so a notated tie
   point is a lattice boundary and an admissible committed chord-span boundary; Layer 2's slicer,
   reading the tie-RESOLVED surface, has no such boundary. (Row 1.)
2. A committed segment's published extent can bridge a stretch the lattice omitted for carrying no
   sounding note. (Row 26.)
3. Two adjacent committed segments carrying the same key and class are representable, priced by the
   self-transition cell; the declared total order breaks only exact ties toward fewer segments.
   (Row 2.)
4. The ratified pitch-emission covariate list names chromatic-neighbor motion; the shipped emission
   call does not pass it as an argument of its own. (Row 20.)
5. Candidate admission reads the ONSET pitch-class union of a segment while the missing-tone term
   reads the SOUNDING union. (Rows 11 and 22.)

## 5. The guard states

- **Before the first edit, the FULL set:** 75 run, TWO failing — [[OI-372]]'s tool and the
  evidence-pin membership check, both declared by the dispatch — zero STOPs; the guard-state
  comparison opening "STALE vs the run" for the same cause, which the dispatch declares is not a
  third failing check. `gen_guard_classification.py --check` run separately and PASSING.
- **At Task 1, the FULL set again, as a check:** 75 run, **ONE failing** — [[OI-372]]'s tool alone,
  the membership red having been cleared by Task 0 — zero STOPs, four not run and sixteen historical
  records unchanged. `gen_guard_classification.py --check` PASSING, run separately.
- **The end state** is the one further commit's, and this report does not assert it; see the close's
  §9 and the git log.

## 6. Departures, declared

1. **The ordered first read preceded the session-start read**, by Ruling 2.
2. **The pre-first-edit guard run was performed in write mode rather than in the ordered check
   invocation**, because the generator writes its artifact unless `--check` is passed and the
   invocation used to learn its interface passed a flag it does not accept. The difference against
   the committed blob was measured — exactly the declared second red and the summary counts that
   follow from it, nothing else — and the artifact was then **restored from the index before any
   edit**, with the tracked population re-enumerated to confirm the tree Task 0 committed was the
   tree A1 describes. The restore is a working-tree write and is declared as one. No guard artifact
   was committed before the run that produced it.
3. **Shell use** was limited to read-only git object queries by explicit hash, the per-path
   `git diff` forms the dispatch orders (with the changed passages written to scratch paths outside
   the repository and read there with the file tools), the sanctioned enumeration tool, the project's
   own generators, and `head`/`tail`/`sha256sum` over scratch files and git-object output. **No
   working-tree `git status`, no bare working-tree `git diff`.**
4. **An attempted shell heredoc append of the close failed to parse and was abandoned** rather than
   retried in another dialect; the close was written with the file tools.
5. **No stale index lock was met**, so the **D-669** remedy was neither needed nor taken.

## 7. What this batch did NOT do

No `src/` change, no golden, **no test changed, moved or run**, nothing under `tools/corpus/` or
`tools/robust_stop/`; no behaviour change to the analysis, no fix to inference, no design, no
repair, no derivation of any specification statement; **no measurement of the analysis built,
designed, scoped or run**; **no session booted**; no document archived, moved or deleted AS A FILE
except the previous batch's `STATUS.md` entry under the ruled forward bound; **no open-items row
created, flipped or discarded**; no edit to the blind output, the boot pack or its manifest, the
brief, the withheld-family reading file, any governing document, any register entry or any register
source; **no verdict word on the blind session's independence record**; and **no recommendation on
the method anywhere**. [[OI-179]] stays OPEN and GATES; [[OI-372]] and [[OI-374]] stand as found.

## 8. The standing self-check (D-434) over this batch's own diff

1. *Principles touched.* **#19** — the tabulation establishes nothing and says so. **#12** — the
   graded file was never edited; SILENT verdicts and the open questions are carried rather than
   dropped; the moved `STATUS.md` entry travelled verbatim. **#13** — both departures are surfaced.
   **#17(f)/D-431** — the counts of record live at the reading file's own rows and manifest, and the
   one transcribed total the reading file had carried was removed by the self-check before delivery.
   **#24** — the two axes are kept apart; no comparison between measured quantities is asserted.
   **#6** — the vocabulary has one home and the oracle was located by its own text. Conforms.
2. *Conventions.* American English; no self-invented labels; music-theory words in their musical
   sense, the self-check catching three uses of *beat* as a verb for "outperformed" and two numeric
   uses of *figure* in the delivered file.
3. *Figures and premises.* Every premise re-checked at its object; the two population counts re-taken
   at the graded file's own structure rather than carried.
4. *File-tools rule.* §6.3 above.
5. *Uncertainty.* No difference between measured quantities is asserted.
6. *Re-read from disk before release.* The reading file was re-read whole after assembly. **That read
   is what found this batch's own error:** three rows had been written on the premise that the
   production lattice reads the tie-RESOLVED note surface, and two more on the premise that the
   missing-tone term reads the same onset union as candidate admission. Both premises are false at
   the code. Row 1's current-text verdict changed, the reasons at Rows 3, 11, 12 and 22 were
   corrected, and **the verdict distribution was recounted after the corrections.**

---

*Provenance: CC, 2026-08-24. The tip, the guard summary, the membership count and the blind output's
blob, size and hash were read at content-addressed git objects; the two population counts were taken
at the graded file's own structure; the oracle's two arms were located by their own text and quoted
from the files; the untrusted sources were opened per statement. TOWARDS the ultimate objective and
TOWARDS the guiding principles.*
