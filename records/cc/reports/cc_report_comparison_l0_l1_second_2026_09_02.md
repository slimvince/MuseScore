# CC report — the L0/L1 comparison, second writing: the population cut, the guard STOP cleared, and the comparison OPENED

> **STATUS: SESSION REPORT. It decides nothing, establishes nothing, and recommends nothing.**
> Prepared by Claude Code, 2026-09-02, under `cc_instruction_comparison_l0_l1_second_2026_09_02.md`,
> executing **Ruling 33** (§3an of `cowork_rulings_2026_08_31_decision_surface_sitting.md`) together
> with **Ruling 32** (§3am), and through them Rulings 1–4 and §5 of
> `cowork_rulings_2026_08_24_comparison_design_sitting.md` and the disposition discipline of
> `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §0 and §3.4.
>
> **What the user is asked to rule here: NOTHING.** The disposition surface comes separately, framed
> by the writing side over the reading file's rows. Nothing in this report is a recommendation about
> any disposition, any difference, the derivation, the method, or the L0/L1 split.
>
> **Where this report would name hashes it cannot contain — the close commit and the end-state
> commit — it points at the git log**, as the dispatch directs.

---

## 1. The headline

**All four tasks ran. Task 2 OPENED and is INCOMPLETE by a recorded stop at a member boundary.**

- **Task 0** landed four paths and pushed; `origin/master` verified equal at its ref file.
- **Task 1** amended the population tool to cut the term search to the ruled specification document
  set, published the residue whole, re-derived and re-ordered the population, and **enrolled the tool
  in the guard runner's authored list — clearing the one STOP the first writing created.**
- **The ruled size stop does NOT fire under the cut**, so Task 2 opened, where the previous batch's
  did not.
- **Task 2 tabulated seven of the population's twenty-nine documents** — positions 1 to 7, which is
  the whole of Ruling 32's items 1 and 2 — and **stopped at the boundary between positions 7 and 8**
  on capacity, as **D-672** provides for.
- **Task 3** wrote the two `STATUS.md` pointer entries, moved the previous batch's entries through the
  forward bound, regenerated the read size, appended the close, and produced this report.

**The reading file exists**: `ratification_surfaces/cowork_comparison_l0_l1_reading.md`. **Every
disposition in it is a proposal; nothing is applied anywhere.**

## 2. What the cut did, and what the artifact now states

`tools/audit/gen_l0_l1_outgoing_population.py` gained four things, each stated on the artifact's own
face rather than only in the tool:

1. **The specification-set cut.** It reads `tools/audit/specification_document_set.json` →
   `the_document_set` → each record's `member`, and admits a hit file **only** on exact path equality.
   The set's member count as read, the members themselves, and the named members that are and are not
   in the set are all recorded. **The set is READ, never re-derived here** (#6), so a document leaving
   or entering it moves this population without anyone editing this tool.
2. **The residue, published whole.** Every hit file outside the set is listed under
   `the_residue_for_the_mining_map` with its path, its inventory class, its admitting-hit count and the
   ruled one-line statement; **its per-file hit records stay intact** at `the_term_search.per_file`,
   exactly as an in-population file's do. The artifact states what is **not** claimed: that the cut is
   by document KIND and not by content, so a residue file may hold design content and the mining map is
   how it reaches L1.
3. **Both orderings.** The cut population is `the_population_in_comparison_order`; the first writing's
   pre-cut list is preserved beside it as `the_population_before_ruling_33` (#12), because what a cut
   removed is evidence about the cut.
4. **The size stop re-evaluated on the IN-set count**, with the pre-cut figure kept beside it so the
   effect of the cut on the stop is readable rather than argued. **Its outcome: NOT reached.**

**No figure of that artifact is restated in this report, in the close, or in either `STATUS.md`
entry** (#17f, **D-431**). The writing side's predicted intersection was declared a prediction; the
tool's own derivation is the figure of record, and it is at the artifact.

## 3. The guard STOP, and how it was cleared

The first writing ordered a `--check`-carrying script written and forbade enrolling it, which halts the
runner by construction. **This writing enrolled it**, in the act that amends it — the standing new-tool
rule the runner's own authored comments state — with the reason string the dispatch gives verbatim and
the authored comment above it naming this dispatch and Ruling 33. **Nothing else in
`gen_guard_state.py` changed.**

**Measured after the enrolment, over the full set:** population **76**, `unclassified_candidates`
**empty**, the new tool **passing its own `--check`**, and the failing set **exactly the ten inherited,
none added**. **A4 holds in every limb.**

## 4. What the comparison found, in four sentences and no more

This report does not restate the reading file. Four things are worth naming here because the close and
the file both carry them and a reader of this report should know they exist.

**The two texts agree closely on the construction itself** — the change-point set, the slices, slice
identity as the event set, the release clause, the refusal of a grid, the refusal of a pitch-class
fold — and they reach it from the same published source, which the rows record because it bears on the
independence question the user judges separately.

**They disagree flatly on grace notes**, in five independent places across three documents: the
derivation states that a grace note opens no change point and belongs to no sounding set; the outgoing
text states that a grace *"genuinely opens/closes a boundary by its span"*, records excluding graces as
a **heuristic that was dropped**, and carries the question as still open in its own risk register.

**They disagree on what the published domain is, on what a slice carries, and on the form of the metric
strength class** — each stated three or more times, and each a difference between two considered
positions rather than an oversight: the outgoing text names its clip semantic *"a deliberate semantic
… stated so on purpose"*.

**And the derivation has no enlargement contract at all**, where the outgoing record has a full one —
append-only, idempotent, one step per call, clamp-and-report, build-then-extend equivalence,
re-slice equivalence, determinism independent of how finely the span was enlarged. **Whether L0 should
carry one is the user's to rule; what the comparison establishes is that the two records differ.**

**Two rows run towards the derivation rather than against it**, and are recorded as such: the outgoing
text holds a fact the derivation lacked — that an imported score cannot distinguish a cue note from a
muted one, which bears on one of the derivation's open questions — and it supplies an eight-case
completeness list of which the derivation names five.

## 5. The stop, stated as the rule requires

**Positions 1 to 7 are DONE. Positions 8 to 29 are UNTOUCHED — not partly worked.** Nothing in them has
been read for tabulation, quoted, counted or dispositioned, and no row for any of them exists anywhere.
**No document is half-tabulated.** The reading file's §0 carries the same table and names where the next
dispatch resumes: **position 8**, `ARCHITECTURE.md`'s Layer 5 section.

**The boundary is a principled one as well as a capacity one:** positions 1 to 7 are exactly Ruling 32's
items 1 and 2 — the two `ARCHITECTURE.md` sections and all five root design documents, the whole of the
old Layer-1 and Layer-2 territory. What remains is item 3's four places and the eighteen files the cut
admits.

**Three sections of the reading file are marked NOT YET WRITTEN in place**, because each can only be
written once every document is done and writing one over seven would state a completeness this
comparison does not have: the derived-side rows S-1 to S-54 with their arithmetic check; the seventeen
open questions; and the derivation's §7. **The independence record IS written**, whole, because it is a
relay of the derivation's own §6 and does not depend on the population.

## 6. Declared departures

1. **The dispatch was read from the working tree before it was pinned** — standing clause **P-2**'s
   fallback route, the user's opening line having named only the dispatch file. Pinned at Task 0 to
   blob `d9fb1db0c492677afa9db8790b157a0aadf0be02`; re-hashed before staging and proven unmoved.
2. **The ordered first read preceded the session-start read** — the derivation's §5, then §6, then §7,
   then the whole file, before `CLAUDE.md`. **The read SET is unchanged; only its ORDER**, as Ruling 2
   of the comparison-design sitting requires and Ruling 32 applies.
3. **An unordered full guard run was taken in write mode.** The runner was invoked with `--help`, which
   it does not handle, so it ran the whole set in write mode. **It was not killed** — the standing rule
   against stopping a long-running process without asking. Its verdicts are the same set a check run
   measures, so the start state is established from a real full run; **the artifact it wrote was
   restored from the tip object before Task 0's commit**, and the tracked population was re-enumerated
   afterwards to prove the tree back at exactly the two A1 modifications. No commit of this batch
   carries that run's artifact.
4. **`tools/audit/gen_status_batch_bound.py` was re-aimed** — its own per-batch carve-out, licensed by
   the dispatch in terms; the previous aiming is recorded beside the new one (#12).
5. **A scratch directory outside the repository** carried every diff, every extracted object and every
   captured tool output, each read back with the file tools.

## 7. One finding of this batch, about the apparatus

**The previous batch's re-aiming of the forward-bound tool was incomplete.** Four fields are authored;
two were moved. `DISPATCH` and `TASK` were left naming the then-previous batch, so the header that move
wrote into `STATUS_ARCHIVE.md` says the entries were moved by the batch **whose entries were being
moved**. The date is right; the attribution is wrong.

**This batch re-aimed all four fields and recorded the defect in the tool's own preserved-aimings
list.** It did **not** edit the already-written archive block: that is a previous batch's record, the
dispatch bars rewriting an archived document, and recording the defect is what a later reader needs.
**Sorted by D-438's test it is apparatus**, so under R3 it is reported and left, and **no open-items row
is created.**

## 8. What this batch did NOT do

**No decision on any disposition, any difference, the derivation, the method or the L0/L1 split. No
verdict on the deriving session's independence — its record is relayed with no verdict word attached,
and the words that would grade it appear nowhere in the reading file. No session booted. No measurement
of the analysis built, designed, scoped or run — the comparison is textual throughout. No specification
statement derived. No disposition applied anywhere.** No edit to the derivation, the brief, the pack,
its generator, its artifact, any outgoing text, or any register source. No governing document was edited
other than `STATUS.md` and `STATUS_ARCHIVE.md`, which Task 3 orders. **No `src/` change, no golden, no
test changed, moved or run, no build, nothing under `tools/corpus/` or `tools/robust_stop/`. No
open-items row created, flipped or discarded. No decisions-register entry and no `D-NNN` allocated** —
that register cannot accept one, and `cowork_register_rule_c_suspension_2026_08_28.md` is the route.
**No finding number allocated. No residue file dispositioned.** **One guard enrolment and no other.**
No recommendation anywhere.

## 9. The self-check over this batch's own diff, as the standing rule requires

Read at the diff actually on disk, not at the intention.

1. **Principles.** **#19** — the reading file establishes nothing and says so; every verdict is this
   session's authored reading and every row is re-placeable at the two quoted texts; the population's
   reach stays a declared lower bound. **#12** — the residue is published whole with its hit records
   intact, the pre-cut ordering is preserved beside the cut one, the previous aiming of the re-aimed
   tool is kept, and UNPLACED and SILENT are carried beside ADOPTED rather than collapsed into it.
   **#13** — the one finding of this batch is surfaced in the close and here rather than absorbed.
   **#17(f) / D-431** — no count of the population artifact appears in this report, in the close, or in
   either `STATUS.md` entry; the reading file's own counts are taken at its own rows and stated as
   sums. **#6** — the five dispositions have one home and are not restated; the specification set has
   one home and is read from it. **#24** — no comparison of two measured quantities is asserted
   anywhere. Conforms.
2. **Conventions.** American English throughout. No self-invented label: the disposition words are the
   phase definition's, the verdict words the comparison-design sitting's, and *residue* is the plain
   word Ruling 33 itself uses. Music-theory words in their musical sense — *score* is the music, *key*
   is tonality, *bar* is the metric unit with the prohibiting sense written *exclude*, *note* is a
   pitch event, *tie* is the notated tie, *release* is a note's ending, *slice* is the stretch between
   change points, and *measurement tool* or *script* is written where the collided word would be
   wrong. **No numeric grade appears anywhere.**
3. **Figures and premises.** Every figure here is a commit or blob identifier read at the object by
   explicit hash, a guard tally from this batch's own runs, or a count taken at the reading file's own
   rows. **The population's counts are cited to the artifact and never transcribed.** Every premise the
   dispatch supplied was re-checked at its own source before use, and all of them held — which is
   itself worth recording, the first writing having had three fail.
4. **The file-tools rule.** Working-tree content was read with Read, Grep and Glob throughout; the
   shell was used for git object queries by explicit hash, for the sanctioned enumeration and generator
   scripts, and for writes into scratch outside the repository. **The guard refused two commands and
   both were re-taken through the file tools** — one aimed at a repository path, one carrying an
   unexpanded shell variable, which is the deny-on-indeterminate policy working; the refusals are
   recorded here rather than passed over.
5. **Uncertainty.** The one uncertainty that matters is stated where it lives: the term search's reach
   is **UNMEASURED**, and the artifact says so rather than implying a coverage it cannot claim. The
   reading file's own counts describe seven documents and are labelled a running total, never a
   description of the outgoing text as a whole.

**One correction made during the work and recorded rather than hidden:** the per-document summary
tables were first written with integers that disagreed with their own statement lists. The lists were
right and the integers wrong. **They were corrected by counting at the rows**, and each table now
states its own sum so the arithmetic is checkable rather than asserted.

## 10. Where the hashes this report cannot contain are found

The close commit and the end-state commit cannot name themselves. **Both are in the git log**, and the
close's own table names every commit that precedes them by explicit hash. **The end state is not
asserted in the close**; the one further commit carries it, per the dispatch's item 3.

---

*Provenance: CC, 2026-09-02, executing `cc_instruction_comparison_l0_l1_second_2026_09_02.md` at local
tip `8f1e4e9deffd816f0e54c07c0912055a144f404f` with `origin/master` equal to it. Every commit and blob
identifier was read at the object; `origin/master` was read at its ref file after each push; the guard
verdicts are this batch's own runs. TOWARDS the ultimate objective and TOWARDS the guiding principles.*
