# CC report — the ROW 4.3 PLACEMENT, eighth writing: the one row Ruling 62 item 4 left unplaced, QUARANTINED under Ruling 78, with the §13b paragraph corrected and its former wording preserved

> **This report decides nothing.** It records what was placed, what was read at each of Ruling 62's
> other six rows, what was measured at the objects, and what was deliberately not touched. **The
> placement is a ruling APPLIED, not a judgment made**: its disposition word and its ground are Ruling
> 78's own, transcribed to the row. No ruling was taken, no position was weighed, no other row was
> touched, no proposed disposition was edited or deleted, no verdict was attached to the derivation or
> to the deriving session's independence, and no decisions register and no suspension file was written.
> Where this report would name a hash it cannot contain — its own commit, and the end-state commit that
> follows — it points at the git log. **No count of the reading file is restated here** (**D-431**,
> #17f).

**Dispatch:** `cc_instruction_comparison_l0_l1_eighth_2026_09_04.md`, pinned at Task 0 to blob
`ea6feeef7353de015f28050865b07b1a680c518f` and proven unmoved immediately before staging.
**Full close:** `cowork_away_returns.md`, the section **THE ROW 4.3 PLACEMENT (EIGHTH WRITING) …**,
which carries the commit table, both guard states, E0–E2, A1–A5, the departures and the plan lines.
This report does not restate them; it carries the placed row, the six sibling rows as they were read,
and the object-level check the dispatch orders here rather than in the close.

---

## 1. What was done

**One row was placed and one paragraph was corrected.**

- **Task 0** landed `cowork_rulings_2026_08_31_decision_surface_sitting.md` (Ruling 78, additions
  only), `cowork_handoff_entry_ninety_eight.md` and this dispatch — **three paths**, because A3 was
  measured first and the membership artifact had not moved.
- **Task 1** added, at the foot of Row 4.3's block, **one `*RULED DISPOSITION` line**; replaced the one
  §13b paragraph the placement falsifies, its former wording carried verbatim inside the replacement;
  and added one foot note at §13a. Three edits, one commit.
- **Task 2** closed: the three `STATUS.md` pointer entries, the forward-bound move, the regenerated
  read-size artifact, the close section and this report.

**Nothing else was touched.** The derivation is byte-unchanged, banner and body; the derived
specification is byte-unchanged; no outgoing text was opened; no decisions register, no suspension
file, no open-items row.

## 2. The row placed — its kind, its disposition word and its ruling

| Row | Kind | Disposition placed | Ruling | Under |
|---|---|---|---|---|
| 4.3 | a ruling applied, its words transcribed | QUARANTINED | 78 (§3cg) | Ruling 62 (§3bq), item 4 |

**The ground, in the ruling's own words**, which is what the line carries: *"the outgoing eleven- and
twelve-field lists are descriptions of builds and targets, placed by S-3 — carried where they match,
QUARANTINED where they describe code."* Ruling 78 places Row 4.3 under that rule **on the ground its
four sibling rows were placed on**, and the line says so on its face.

**The audit question the line carries, in one sentence as §5 requires:** does the in-region duration —
a note's duration clipped to the queried region — belong to L0's supplied facts at all, being a
property of the query rather than of the note?

**The line is NOT a family placement**, and §13b now says so at its foot: it is a ruled line taken on
its own surface, so §13b's three tables and their totals are unchanged, and §13a's foot note records
that one further ruled line exists and where it is counted.

## 3. What was read at Ruling 62's other six rows

Each was read at its row with the file tools **before anything was written**, so the ground is checked
at the file rather than taken from the dispatch's face. **Not one of the six was edited.**

| Row | What its ruled line reads | Notes |
|---|---|---|
| 1.3 | **QUARANTINED** — unchanged | The eleven per-note fields; the row already carried QUARANTINED as its proposed disposition, and Ruling 62 left it there. |
| 3.31 | **ADOPTED — carried** | The *carried where they match* branch: its subject is the layer's input list, which matches S-3 to S-7 in kind. |
| 3.41 | **QUARANTINED** | Had carried ADOPTED — proposed, *travelling with Row 3.42*. |
| 3.42 | **QUARANTINED** | Had carried ADOPTED — proposed; the row enumerates the as-built eleven fields, so it *"describes code"*. |
| 3.48 | **QUARANTINED** | Had carried ADOPTED — proposed; its ruled line reads ***"travelling with Row 3.42"*** — the same words Row 4.3's proposed disposition carries. |
| 14.9 | **QUARANTINED** | Had carried ADOPTED — proposed; **its proposed disposition names Row 4.3** among the rows it travels with, and it has Row 4.3's own two-part shape (one derived statement differing over the field list, one agreeing about something else) yet was quarantined whole. |

**Row 4.3 was the only member of that set standing at ADOPTED — proposed**, and it is the row Ruling
62's item 4 cited by name and did not place. With this line, every row of the set carries a placement.

## 4. The object-level check of Task 1, measured rather than asserted

The edited reading file was written into the object store with `git hash-object -w` (blob
`72f9ad22be310e45aa593bc09f13bf1ef10d5c6b`) and diffed **between two explicit blob hashes** against the
tip blob `f83b30d3f15959e186777558d14407d0335b055f`.

- **Exactly three changed passages**, and they are the three Task 1 orders: the placement line at Row
  4.3, the §13a foot note, and the §13b paragraph replacement. A fourth would have been a STOP and
  there was none.
- **20 insertions and exactly three deletions.** The three deleted lines are the whole of the §13b
  paragraph, and the replacement carries them **verbatim inside itself** under its *Former wording,
  preserved (#12)* clause. **No other deletion anywhere in the file.**
- **Exactly one added `*RULED DISPOSITION` opening line**, at Row 4.3: the count of that opening across
  the two blobs rose by one and by no more.
- **No `*PROPOSED DISPOSITION` line was touched** — the same count on both sides, none added, none
  deleted, and Row 4.3's own stands beside its placement.
- **No `**Row` heading was added or removed, proven rather than eyeballed.** The set of lines matching
  the strict row-heading form is **identical on both sides** and the two sets diff empty. **One line of
  the LOOSE shape is new and it is not a heading:** the §13b paragraph the dispatch orders verbatim
  opens `**Row 4.3 is PLACED, …`, which is prose inside §13b, carries no em dash and stands in no row
  block. It is reported because a mechanical test on the loose shape would flag it; the seventh writing
  met the same shape and reworded around it, whereas here the wording is the dispatch's own ordered
  text and was written as ordered.
- **§13's tables, §13a's tables and figures, and §13b's three tables are byte-identical**, and the
  proof is over the whole file rather than over a line range: every table row was extracted from both
  blobs and the two sets compared identical.
- **§11 and §12 are untouched**, which the diff's own hunk positions establish — no changed passage
  falls inside either section.

## 5. What was deliberately NOT done, and the act that stays owed

**§11 (the audit questions) and §12 (the proposals) were not edited and not re-derived.** Ruling 78
records that both were derived from the PROPOSED dispositions, that the ruled dispositions since have
not been folded into them, that Rows 3.41, 3.42, 3.48 and 14.9 already stand quarantined outside §11
and Row 4.3's placement makes a fifth, and that **repairing one of five would leave the file
inconsistent in a new way** — so the closing act is ONE re-derivation of both sections against the
ruled dispositions, its own scoped act. The dispatch makes attempting either half **a STOP-and-report**;
neither was attempted. **The act stays owed, and the ruling records why it is not pure apparatus:** the
audit reads §11, so a quarantined row whose question is not in §11 is a question the audit does not
meet.

**Ruling 62's closing phrase was not corrected.** Ruling 78 reports it and leaves it standing, and so
does this batch.

## 6. What was measured, not assumed

- **The declared start state held exactly.** A full guard run before the first edit: **76 run, 10
  failing, 4 not run, 16 historical, zero STOPs** — the ten failing being the ten the dispatch's own
  FACT names, member for member, with no eleventh red. **That this run IS the tip's own measurement was
  established rather than assumed**: the artifact it wrote hashed to `5ede24692d2c901e51b283f08ee017bb6d0e26e5`,
  byte-identical to the committed blob at the tip, so `guard_state.json` was left unmodified. **The run
  was taken in write mode rather than `--check`, and that is declared at the close as departure 3.**
- **A1 was checked over the whole tracked population** with the sanctioned enumeration tool: **exactly
  one** tracked modification at boot — the sitting record — which is what A1 declares, and both named
  untracked paths confirmed present. `cowork_handoff_entry_ninety_nine.md` stands untracked at the root
  and was correctly not landed.
- **The sitting record's additions-only property was proven at the object**, between two explicit blob
  hashes: one changed passage, 108 insertions, **zero deletions**, every added line inside the new
  `## 3cg.` section.
- **A3 was measured before being accepted:** the membership artifact regenerated and its blob hash
  compared against the committed one — **byte-identical** (`fb3a00be…` both sides) — so it was **not**
  committed and Task 0 carried **three** paths rather than four. The dispatch's prediction of zero
  movement rested on a reading of that tool's three routes at its own source, and the measurement
  agrees with it.
- **The forward bound was measured, not assumed:** re-aimed at all five authored inputs with
  `PREVIOUS_AIMINGS` appended to rather than replaced, run, and the dispatch's prediction held exactly
  — **the seventh writing's four entries moved, the second writing's two stayed, no STOP** — with the
  tool's own reconciliation green in both limbs and `--check` passing after the act.
- **A2's watched exceptions did not fire.** `gen_l0_l1_outgoing_population.py --check` still re-derives
  after the reading file and `STATUS.md` moved, and `gen_session_start_read_size.py --check` is green,
  regenerated after the last edit to `STATUS.md`.
- **Every commit was verified at the object** by `git show --stat` at its explicit hash, and
  `origin/master` read at its ref file with the file tools after each push, never from a push's own
  output.

## 7. What this batch did not do

No ruling taken, no position weighed, no verdict on any open question, on the derivation, or on the
deriving session's independence. No session booted. No measurement of the analysis built or run. No
derivation of any statement. No edit to the derivation (banner or body), the derived specification, the
brief, the pack, its generator, its artifact, the population tool, any outgoing text, the suspension
file, any register source, or any governing document other than the two Task 2 orders. In the reading
file: no row other than 4.3 touched, and Row 4.3's own heading, statement, derived line, axis line and
`*PROPOSED DISPOSITION` paragraph unedited; §13's tables, §13a's figures and §13b's three tables
untouched; §11 and §12 untouched. No `src/` change, no golden, no test changed, moved or run, no build,
nothing under `tools/corpus/` or `tools/robust_stop/`. No open-items row created, flipped or discarded;
no register entry and no identifier allocated; no guard enrolment; no tool added. **No recommendation
appears anywhere** — not in the reading file, not in the close, not in this report, not in a commit
message.

The one `tools/audit/*.py` edit is the re-aiming of `gen_status_batch_bound.py`'s five authored
constants, its `PREVIOUS_AIMINGS` list and its head comments, which Task 2 orders in terms and which
**D-648** classes as authored-input maintenance.

## 8. Declared departures

1. **The dispatch was read from the working tree before it was pinned** — P-2's own fallback route.
   Pinned at Task 0, blob proven unmoved before staging.
2. **Diff and guard output was written to scratch paths outside the repository and read with the file
   tools.** Every repository read went through the file tools; the only shell reads were git object
   queries by explicit hash, some piped to `grep`, `sed`, `sort`, `diff` and `wc` to count and compare
   object-level line sets.
3. **★ The start-state guard run was taken in WRITE mode rather than `--check`.** `gen_guard_state.py`
   was invoked with `--help`, which it does not recognise as a flag and which therefore selected its
   default write mode. The artifact it wrote was then hashed and compared against the committed blob at
   the tip and came back **byte-identical**, so nothing moved and no tracked path was modified by it.
   **Declared because the dispatch orders that run in `--check` mode and this run was not that**, and
   because the reason it cost nothing is a measurement rather than an argument.
4. **★ One command was denied by the armed shell-read guard and re-taken through the file tools.** A
   `grep` over a scratch-path file named through a shell variable was refused on the deny-on-
   indeterminate policy (**D-647**): the guard cannot expand a variable, so it cannot decide whether
   the path is inside the repository. The read was re-taken with the Grep file tool. **Recorded because
   the guard firing is the mechanism working.**
5. **★ `git ls-files --others --exclude-standard -- <name>`, which the dispatch orders, is not among
   D-253's named exceptions.** It returns names from the untracked list and no file content, and it is
   the form this project's own dispatches order for exactly this check; declared on the ninety-eighth
   entry's own precedent rather than left unmentioned.

## 9. Where the hashes are

The commit table — the tree met, Task 0, Task 1 and the close — is in the close section of
`cowork_away_returns.md`, each by explicit hash, each pushed and each verified equal at
`.git/refs/remotes/origin/master` **with the file tools**. **This report's own commit and the end-state
commit that follows it are in the git log**, which is where a document cannot name its own identifier.
**The close does not assert the end state**; the one further commit carries it.
