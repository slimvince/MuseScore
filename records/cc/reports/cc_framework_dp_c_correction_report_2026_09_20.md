# `FRAMEWORK.md` DP-C — STOPPED AT TASK 1, NOTHING WRITTEN (report, 2026-09-20)

> **STATUS: STOPPED AT TASK 1, ON THE RULED STOP FORM.** Written by the CC run of
> `records/cc/instructions/cc_instruction_framework_dp_c_correction_2026_09_20.md`, which executes the
> user's ruling of 2026-09-20 — his words, *"I agree: A"* — recorded at
> `records/cowork/handoff/cowork_handoff_entry_two_hundred_and_seven.md` §1 item 4.
>
> **★ THE HEADLINE. The batch did not begin. FOUR conditions were met at Task 1, three of them this
> dispatch's own STOPs, and NO FILE OF THE REPOSITORY WAS EDITED** — proven at the objects, not
> asserted: every one of the twenty-two pins re-hashes to the value it was pinned at, and the changed-path
> enumeration at the close is byte-identical to the one taken before any act.
>
> **★ THE ONE THAT DECIDES IT, AND IT IS NOT THE GUARD SET. `l0-l1` IS FROZEN.** §3 of the dispatch
> states *"`l0-l1` is a **live** subject, not frozen, so it re-renders by the ordinary path."* **That is
> false at the object.** The pack was frozen on **2026-09-04** under **Ruling 17(a)** and **D-646**, its
> deriving session having run on 2026-09-02, and the generator writes **nothing** into a frozen subject's
> directory. So Task 3 could not have produced the difference it predicts, and forcing member (7) to
> change would breach the freeze, **D-646** and **#12** — back-dating into a completed derivation's record
> a wording that did not exist when its session ran.
>
> **★ AND THE VERIFICATION §2(b) MAKES A PRECONDITION OF THE WRITE CANNOT BE RUN HERE.** The paper's page
> images are unreadable in this environment — `pdftoppm` is not installed — and reading the file's content
> through a shell utility is forbidden by **D-253**. So the write was barred on its own terms, independently
> of everything above.
>
> **★ WHAT IS *NOT* WRONG, reported so the next run does not re-establish it.** The two sites are exactly
> two, as the writing side read them — **no third site**. The passage §2(a) quotes stands at the file
> **word for word**. The `l0-l1` freeze is **intact**, all ten files at their recorded blobs.
>
> **★ NOTHING IS SETTLED HERE.** This report takes no decision, proposes no wording and edits no
> governing surface. Every question it raises stands with the user.

## 0. The boot

The ordinary session-start read was performed in full before the named file was acted on — a single-file
opening instruction is not an exemption (ratified 2026-08-29, P-1; **D-230**):

1. `CLAUDE.md` at its ruled membership; `DECISIONS.md` **whole** (four calls); `STATUS.md` whole; and the
   derived gating answer at `tools/audit/nongating_apparatus_rows.json` →
   `★_the_live_gating_answer` → `gating_ids`, read at its live section through the identity list.
2. `BUILD_AND_TEST.md` — **the condition was ESTABLISHED and is NOT MET, so the file was not read.** The
   dispatch's §0 item 2 ordered exactly this establishment and recorded that the writing side had not made
   it. `CLAUDE.md`'s condition is *a session that builds, tests, or runs a measurement tool **whose command
   lives there***. This batch builds nothing and tests nothing, and the three tools it runs are absent
   from that file: searches for `boot_pack` / `boot pack` (case-insensitive), for `gen_status_batch_bound`
   and `gen_derivation`, and for `gen_guard_state` / `guard_state` each returned **no match**. *(A
   divergence worth one remark: the previous batch of this same lineage,
   `records/cc/reports/cc_framework_9_0_correction_report.md` §0, judged the same conditional **MET** on
   the looser ground *"this batch runs a Python tool"*. The two readings differ; this run applied the
   rule's own words. Reported, not resolved.)*
3. `records/cowork/handoff/cowork_handoff_entry_two_hundred_and_seven.md` **whole**.
4. `records/cowork/rulings/cowork_rulings_2026_08_31_decision_surface_sitting.md` **§3r and §3t** —
   Ruling 18 and the correction of record that follows it.
5. `reading_pass/extracts_second_pass/pardo-birmingham-2002-algorithms-for-chordal-analysis.md`
   **§9.4 item 2**.
6. This batch's dispatch, whole.

## 1. Task 0 — the pins, and their state at the close

Twenty-two files pinned with `git hash-object -w` before any other act, and **re-hashed at the close.
Every one reproduces exactly. NO READ DISAGREED WITH ITS PIN.**

| File | Pin | At the close |
|---|---|---|
| `CLAUDE.md` | `00e2f93239115a0803b53b9d510f3abd91a6b10b` | unchanged |
| `DECISIONS.md` | `a825f7b7caaca4b49afc7b637986e3404d3ae3ee` | unchanged |
| `STATUS.md` | `634fcd80aa127c700e88083fbbb2537e93d6b0aa` | unchanged |
| `BUILD_AND_TEST.md` | `8c9cf5cb7d4ea7a83adfd804a725e2f7b388d738` | unchanged |
| **`FRAMEWORK.md`** | **`a6a6ca335292a1cb00463234012ba19c4af55bfb`** | **unchanged** |
| `tools/audit/nongating_apparatus_rows.json` | `1cfc1ff9eecc1b74ba3d7084dc2a53e77b6721df` | unchanged |
| `tools/audit/derivation_boot_pack.json` | `a73eb4545df640028778dfd9c615c9fd98453433` | unchanged |
| `tools/audit/guard_state.json` | `a225c51d183120a0b132c5db5e9b520dff1cbb80` | unchanged |
| this batch's dispatch | `8fd94fb401e487233cc37a1906311785b94c4974` | unchanged |
| handoff entry 207 | `2addffe93d2fa86c34e13fead9f730ee4be3ac60` | unchanged |
| the 2026-08-31 rulings record | `99e75e8ed147635a94bc793d64472a5b4778d8f0` | unchanged |
| the second-pass extract | `8916fd035521945f2391a79932729e56f2556843` | unchanged |

**The `l0-l1` pack, all ten files — and the same ten values are what the generator's own freeze records.**
The left column is this run's pin; the right is the digest authored in `FROZEN["l0-l1"]`. **They agree on
every file, so the freeze is intact in both directions.**

| Pack file | Pin, this run | Recorded frozen digest |
|---|---|---|
| `00_READ_THIS_FIRST.md` | `7e7339cf3250ac87337317ca109cf9645dd31b8d` | same |
| `01_the_phase_definitions.md` | `518b1e50d60af2b4e2ddcd8978623832eb071899` | same |
| `02_the_guiding_principles_and_the_conventions.md` | `00ffb0bede471f15d70de6cb7435a617b09caa58` | same |
| `03_the_writing_standards.md` | `518048459da6a865285a0f7c66c5d8f8045f0fc2` | same |
| `04_the_dispatch_protocol.md` | `02107d1ab37af197821edf3ca98ccf6f7ae5c0d3` | same |
| `05_the_ratified_design_intent.md` | `60563ab26e5c5c8827e32645b12eceaeb355933b` | same |
| `06_the_defect_type_catalog.md` | `1dec7621dc48d89242cacaf79b3048cd965d6a19` | same |
| **`07_the_charter_the_layers_and_the_decisions.md`** | **`a0513707886414a1c193a884e3f5f15ffd3f12f5`** | **same** |
| `08_the_five_research_extracts.md` | `54d0892b022107a8bd4cedc9e4bba54679a1ed41` | same |
| `09_the_empirical_findings_ledger.md` | `2bf845db798e91382236387e7f35fabf48b2ec07` | same |

## 2. Task 1 — the start state, established item by item

### (a) The guard set — **RED. This is the dispatch's first-listed STOP.**

`python tools/audit/gen_guard_state.py --check` → **exit 1**. Its own closing line: **78 guard(s) run, 16
failing, 4 not run, 19 historical record(s)**, under the heading `STALE vs the run: guard_state.json does
not re-derive`. The complete line-by-line output is at
`<scratchpad>/guard_run.txt`; the sixteen failing invocations are:

| # | Failing invocation | In the committed failing set? |
|---|---|---|
| 1 | `tools/audit/gen_phase3_gate_partition.py --check` | yes |
| 2 | `tools/audit/gen_filing_convention_application.py --check` | yes |
| 3 | `tools/audit/gen_l0_l1_outgoing_population.py --check` | yes |
| 4 | `tools/audit/gen_artifact_inventory.py --check` | yes |
| 5 | `tools/audit/gen_artifact_inventory_surface.py --check` | yes |
| 6 | `tools/audit/gen_test_construction_evidence.py --check` | yes |
| 7 | `tools/audit/gen_retirement_caller_check.py --check` | yes |
| 8 | `tools/audit/decisions/apply_soft_discard.py --check` | yes |
| 9 | `tools/audit/decisions/apply_residue_discard.py --check` | yes |
| 10 | `tools/audit/gen_evidence_pin_membership.py --check` | yes |
| 11 | `tools/audit/gen_epoch_write_path.py --check` | yes |
| **12** | **`tools/audit/gen_derivation_boot_pack.py --check`** | **NO — ADDED** |
| 13 | `tools/audit/gen_recognizer_establishment_sort.py --check` | yes |
| 14 | `tools/audit/decisions/gen_cluster_dispositions.py --verify` | yes |
| 15 | `tools/audit/decisions/gen_home_classification.py --check` | yes |
| 16 | `tools/audit/decisions/gen_phase1p_delegation_bar.py --check` | yes |

**The comparison is against the committed record, not against memory.** `tools/audit/guard_state.json`'s
own `summary` records **15 failing** and names them; fifteen of the sixteen above are exactly that set,
and the sixteenth is not in it. **The added failure is the boot-pack generator — the one tool Task 3
orders this batch to run.** It is also new relative to the previous batch of this lineage, whose report
states of the same check *"`--check` is GREEN at the object"*
(`records/cc/reports/cc_framework_9_0_correction_report.md`, headline).

**What that check actually says, run on its own:** `STALE: the derivation boot pack does not re-derive` →
`- derivation_boot_pack.json does not re-derive`. **One drift line, and it names the manifest only — no
pack member of any subject is reported stale**, and the frozen subjects' hash verification raised nothing.
Read at the generator's `check_all`, that means every rendered member of every subject re-renders
byte-identically today and only the manifest differs. **The cause is NOT established by this run** and is
not asserted: the generator's own `frozen_block` records that a frozen subject's member records inside the
manifest are built *"from the sources AS THEY STAND TODAY"*, which is an area where the manifest can move
while no directory does — named as the place to look, **not** as a finding. The manifest was last committed
at `54804de49a`, one commit after the freeze at `8664a85f15`.

**Both readings of "red" give the same verdict, so no interpretation was chosen.** Read literally — any
failure is red — the set is red. Read narrowly, as *red beyond the inherited failing set* — the set is
**still** red, because one failure is outside it, and it is the generator Task 3 depends on. **This is
reported rather than resolved: fixing the guard set is outside §7, and "any act outside §7" is itself a
STOP.**

### (b) The current blobs

Established and tabulated at §1 above, for `FRAMEWORK.md` and for all ten files of the `l0-l1` pack.

### (c) How many sites in `FRAMEWORK.md` carry the target sentence — **exactly two. NO STOP.**

| Search string | Lines returned |
|---|---|
| `26%` | **700**, **1600** |
| `tie-breaking` | **699**, **1600** |
| `tie breaking` | *(no match)* |

**Two sites and no third**, exactly as the writing side read them: the main text at DP-C (the sentence
spanning **699–701**) and Appendix B's candidate decomposition **(d)** (spanning **1599–1601**). The
dispatch's third-site STOP does not fire.

### (d) Does §2(a)'s quotation stand in the file exactly as quoted — **YES. NO STOP.**

Read at `FRAMEWORK.md` lines 690–711 and matched line for line, including the line breaks the dispatch's
quotation block preserves:

- 699 — `reported by the work named.] One further measurement is worth carrying: with perfect tie-breaking`
- 700 — `between equally-scoring labels, one published segment-then-label system would still remove only 26% of`
- 701 — `its errors — the rest needs tonal context and voice leading its decomposition does not admit. [FACT.]`

**The scope statement §2(a) makes is also confirmed at the object:** the label
`[FACT — each reported by the work named.]` closes the preceding measurement list across lines 698–699 and
stands immediately before *"One further measurement"*, so it belongs to that list and not to the target
sentence, exactly as §2(a) states.

### (e) The tracked-modification shape — **ONE MODIFICATION OUTSIDE THE TWO NAMED DIRECTORIES. STOP (§7).**

Enumerated with the sanctioned tool, `python tools/audit/changed_paths.py` (**D-253**'s permitted route;
the file tools were used for every content read). **588 changed-path records.** Ten are tracked
modifications:

| Tracked modification | Inside the two directories §7 names? |
|---|---|
| `reading_pass/extracts/…burgoyne-pugin-kereliuk-fujinaga-2007…md` | yes |
| `reading_pass/extracts/…declercq-2015…md` | yes |
| `reading_pass/extracts/…harasim-rohrmeier-odonnell-2018…md` | yes |
| `reading_pass/extracts/…ju-conditschultz-arthur-fujinaga-2017…md` | yes |
| `reading_pass/extracts/…ju-howes-mckay-conditschultz-calvozaragoza-fujinaga-2019…md` | yes |
| `reading_pass/extracts/…pardo-birmingham-2002…md` | yes |
| `reading_pass/extracts/…rohrmeier-2006…md` | yes |
| `reading_pass/extracts/…rohrmeier-2011…md` | yes |
| `reading_pass/extracts_second_pass/…burgoyne-pugin-kereliuk-fujinaga-2007…md` | yes |
| **`tools/audit/claude_md_finer_archive.json`** | **NO** |

**§7's rule:** *"A tracked modification OUTSIDE those two directories that this batch did not make IS a
STOP, and the batch reports it rather than working around it."* **This batch did not make it** — it stood
in the working tree at session start, before any act of this run, and the closing enumeration is
byte-identical to the opening one. **Reported, not worked around, and not touched.**

**And one half of §7's stated expectation did not hold, which is reported rather than passed over.** §7
expects tracked-and-modified files under **both** `records/cowork/handoff/` and `reading_pass/`. There are
**none** under `records/cowork/handoff/`: every handoff entry there, entry 207 included, is **untracked**
(`??`), not modified. The expectation holds for `reading_pass/` alone.

## 3. Task 2 — NOT PERFORMED. The two reasons, and both are independent of the guard set

**Nothing was written to `FRAMEWORK.md`. Its blob at the close is the blob it was pinned at.**

### (a) §2(b)'s verification at the paper CANNOT BE RUN IN THIS ENVIRONMENT — so the write was barred on
its own terms

§2(b) is unambiguous: *"★ VERIFY IT AT THE PAPER BEFORE WRITING IT"*, at **PDF pages 5 to 9**, *"at the
page images"*, and it names the per-class check that depends on them — *"the labels alternate above and
below the axis, so read each label's horizontal position, not its row"*. **That verification is not
available here, and three facts establish it:**

1. **The page images cannot be produced.** Reading
   `docs/research_papers/pardo_birmingham_2002_cmj_algorithms_chordal_analysis.pdf` at pages 5–7 returned
   `pdftoppm is not installed. Install poppler-utils … to enable PDF page rendering.`
2. **No other renderer is present.** `which pdftoppm` → not found; `which mutool` → not found; `which gs`
   → not found.
3. **The one available route is forbidden, and was therefore not taken.** `pdftotext` **is** present
   (`/mingw64/bin/pdftotext`). Using it on a repository file is a read of working-tree content through a
   shell, which **D-253** forbids and which the 2026-08-08 guard-family widening covers in terms — *"The
   restriction is on WHAT is read … not on which utility spells the read."* **It was not attempted.** It
   would in any case not deliver what §2(b) asks for: linear extracted text carries no horizontal
   positions, and the per-class values are small numerals inside the paper's Figure 8.

**This cannot be discharged by relaying the writing side's own read.** The dispatch's provenance records
that every value in §2(b) was read at the page images by the writing side this sitting — and Task 1's own
rationale states the governing principle for exactly this situation: *"a quotation is a claim about a
file's current state and this side's read is not the batch's."* The same holds of a claim about a paper's
printed pages. **So the result §5 requires — the verification run and reported item by item — is NOT
reported, because it was not run. No item of it is asserted, and no percentage of §2(b) is confirmed or
denied by this run.**

### (b) The guard-set STOP precedes Task 2

§1 is explicit: *"If it is red, STOP — this batch must not begin from an unestablished start state."*
It is red (§2(a) above).

**What was NOT done, so the boundary is unmistakable.** No text was written in place of the quoted
sentence; the correction note of §2(c) was not written; no paragraph was re-wrapped; and **Appendix B was
not touched**, which §2(d) bars and which its own pin confirms — `FRAMEWORK.md` is byte-identical.

## 4. Task 3 — NOT PERFORMED, AND MUST NOT BE: `l0-l1` IS FROZEN

**This is the finding that outlives the other three, because it is not a start-state condition to be
cleared — it is a false premise in the dispatch.**

§3 opens: *"`l0-l1` is a **live** subject, not frozen, so it re-renders by the ordinary path."*

**Established at the object — `tools/audit/gen_derivation_boot_pack.py`, the authored `FROZEN` table,
which carries `l0-l1` as a member.** Its entry records:

- **finding** — *"Its deriving session has RUN: the detail-specification phase's first derivation was
  delivered 2026-09-02 and is `cowork_blind_derivation_l0_l1_2026_08_31.md` (the sitting record, §3al).
  Unlike the two subjects frozen on 2026-08-31, this pack was NOT stale when it was frozen — `--check`
  PASSED at the tip's own guard run."*
- **date** — **2026-09-04**.
- **reason** — *"A spent subject's pack is the record of what its session was given rather than an input
  to be kept current (Ruling 17(a); D-646). Left live it would re-render on the first growth of
  `CLAUDE.md` or `cowork_audit_protocol.md` … and would then either turn `--check` red or, if refreshed,
  back-date into a completed derivation's record rules that did not exist when its session ran (#12).
  Frozen at the blobs it carries, with the hash STOP."*

**The table even records the supersession of the very sentence §3 relies on**, preserved in place under
**#12**: *"★ SUPERSEDED 2026-09-04, THE FORMER WORDING PRESERVED IN PLACE (#12): \"`l0-l1` IS ABSENT
DELIBERATELY, and its absence is what makes it LIVE: its deriving session has not run …\" True when
written, and made false by that session's run."* **§3's premise is that superseded wording.** The commit
that performed the freeze is `8664a85f15`, *"freeze the l0-l1 boot pack at its rendered blobs, its
deriving session having run."*

**Four consequences, each read at the generator's own code rather than inferred:**

1. **The re-render cannot produce the predicted difference.** `write_all` short-circuits a frozen
   subject — *"Nothing is written into a spent subject's directory — not the whole pack, not one file of
   it."* So member (7) **would not change**, and §3's expected difference — *"only member (7) … changes"* —
   is unreachable.
2. **§3's verification at the rendered object could never be satisfied.** It requires the corrected
   paragraph and the correction note to be **present** in member (7). Nothing can write them there.
3. **Forcing the change would breach the freeze.** `verify_frozen` raises a STOP — *"the record of a
   completed derivation has been altered"* — and the substantive rule behind it is **D-646** with **#12**:
   putting today's corrected wording into that member would make the pack claim its deriving session read
   a sentence that did not exist when it ran. **This is the same defect, in the same file, that froze the
   other two packs.**
4. **Running the generator in write mode would have had exactly one effect** — overwriting
   `derivation_boot_pack.json`, whose drift is inherited (§2(a)) — **and would have masked that inherited
   drift under this batch's name.** It was not run in write mode.

**The uncorrected sentence therefore stands in the frozen member permanently, and that is correct rather
than a defect.** It is at `07_the_charter_the_layers_and_the_decisions.md` lines **285–286** — one site in
that member, not two, as §3's ground states. Under **D-646** the pack records what that session was
actually given. **Whether anything is owed on account of a completed derivation having been given a
sentence now ruled too wide is a question for the user; nothing here settles it, and this is the same
shape as the question §3t of the 2026-08-31 record leaves OWED, NOT BLOCKING and NOT SETTLED.**

**Neither frozen pack directory was written to. `harmony-boundary` and `scoring-model` were not touched at
all**, and the two `--check` runs wrote nothing anywhere (§6).

## 5. Task 4 — the close, and what of it was deliberately not done

1. **`STATUS.md` — NOT updated, and the forward bound NOT re-aimed.** Declared rather than omitted
   silently. §5's Done conditions are not met and the batch performed none of its work, so there is no
   batch close to record; and writing the entry would require re-aiming
   `tools/audit/gen_status_batch_bound.py --apply`, which moves the previous batch's entries to
   `STATUS_ARCHIVE.md` — a substantive act on two governing surfaces on behalf of a batch that changed
   nothing. **`STATUS.md` is byte-identical to its pin.** Whether a stopped batch owes a `STATUS.md` entry
   is not settled by anything this run read, and it stands with the user.
2. **Push — nothing to push. No commit was made.** §4 item 2's own condition is *"every branch that
   received a commit this session"*; no branch received one, so no push was attempted and none failed.
   **This report is written to disk and is NOT committed** — the standing convention is to commit only when
   explicitly asked, and the close that would have carried a commit was not reached.
3. **This report** — the one thing §7 authorizes creating.

### The standing self-check

Run before this report was written, at the objects on disk and not from the memory of the session.
**The batch's diff is EMPTY, and that was measured twice over:**

- **Every one of the twenty-two pins re-hashes to its pinned value** (§1) — including `FRAMEWORK.md`, all
  ten `l0-l1` pack files, `tools/audit/derivation_boot_pack.json` and `tools/audit/guard_state.json`.
- **The changed-path enumeration at the close is byte-identical to the one taken before any act** — 588
  records both times, `diff` exit 0. So the two `--check` runs wrote nothing, and this run introduced no
  modification of its own.

Checked against the guiding principles, the conventions, the gate and threshold policies and
`DEFECT_TYPES.md`: **no violation found in this run's own conduct.** Three points worth stating
positively, because each is a rule this batch could have broken and did not:

- **#13 / #3** — every one of the four conditions was surfaced as a STOP **before** anything was built
  around it. None was worked around, and no dispatch text was corrected by this side.
- **D-253** — every content read went through the file tools. The one shell attempt aimed at repository
  content was **denied by the guard and not retried in another dialect**; the permitted routes used were
  `git hash-object`, `git log`, and `tools/audit/changed_paths.py`.
- **#17f / D-431** — no value of this project's own measurement is restated here. The guard counts, the
  blob hashes and the line numbers above are this run's own establishments, each named with the act that
  produced it.

**One departure, declared:** §2(b)'s verification at the paper was **attempted** and could not be
performed (§3(a)). The attempt is reported as an attempt, and no value of that paper is carried forward
from it.

## 6. The footprint, stated as what it is — empty

**Edited: NOTHING.** No `FRAMEWORK.md` edit; no pack member; no manifest; no `STATUS.md`; no governing
document; no tool source. **Created:** this report — and, outside the repository in the session
scratchpad, seven working files (`pins_task1.txt`, `pins_close.txt`, `guard_run.txt`,
`bootpack_check.txt`, `changed.txt`, `changed_close.txt`, `changed_final.txt`).

**The footprint claim was MEASURED, not asserted.** A third changed-path enumeration, taken after this
report was written, differs from the opening one by **exactly one record** — this report's own path, as an
untracked addition — and in nothing else.

**Not done at all:** no build, no test, no golden, no measurement of the analysis; nothing under
`tools/corpus/`, `tools/robust_stop/` or `src/`; **no paper read** — the one §2(b) names could not be
rendered, and no other was opened; **no extract edited**; **no open-items row created, flipped or
discarded**; **no decisions-register entry and no `D-NNN` allocated**; no score staged, no brief written,
no session booted. **Written into NO pack directory, frozen or live.** The commands run were read-only:
`gen_guard_state.py --check`, `gen_derivation_boot_pack.py --check`, `changed_paths.py` (twice),
`git hash-object`, `git log`, and `which`.

## 7. Done, on the ruled stop form

**DONE.** The boot, in full, with the `BUILD_AND_TEST.md` condition established and found not met. Task 0:
twenty-two files pinned, and every pin re-hashed at the close with no disagreement. Task 1's
establishments, all five: the guard set's line-by-line output; the current blobs; the site count in
`FRAMEWORK.md`; the §2(a) quotation match; and the tracked-modification enumeration. Two further
establishments the STOPs made necessary: which guard failure is new, measured against the committed
failing set rather than against memory; and the `l0-l1` subject's frozen status, read at the generator's
authored table. The self-check. This report.

**NOT DONE, AND WHY, each naming the condition that barred it.** **Task 2** — the one correction — on the
guard-set STOP (§1) and, independently, because §2(b) makes a verification at the paper's page images a
precondition of the write and that verification cannot be performed in this environment. **§2(b)'s
verification itself** — not run, so **no item of it is reported**, and none of its values is confirmed or
denied. **Task 3** — the re-render — because `l0-l1` is FROZEN, so the act is both impossible and, if
forced, forbidden under Ruling 17(a), **D-646** and **#12**. **Task 4's `STATUS.md` entry and forward
bound** — deliberately, the reason declared at §5. **No push** — no commit was made.

**THE REMAINDER IS UNTOUCHED.** `FRAMEWORK.md` stands at the blob it was pinned at, at both of its two
sites, with Appendix B byte-identical. All ten `l0-l1` pack files stand at their recorded frozen blobs, and
neither other pack directory was written to. `tools/audit/derivation_boot_pack.json` carries its inherited
drift, unmasked. `tools/audit/claude_md_finer_archive.json` stands modified as this run found it.
`STATUS.md`, `DECISIONS.md`, `OPEN_ITEMS.md`, `CLAUDE.md` and every tool source are unchanged. The
untracked Cowork material is untouched.

**FOUR THINGS STAND WITH THE USER AND NOTHING HERE DECIDES ANY OF THEM:** whether the guard set's
inherited failing set bars this dispatch or whether *red* was meant narrowly; what the boot-pack
manifest's inherited drift is and who cures it; **how the ruled narrowing reaches `FRAMEWORK.md` now that
`l0-l1` is frozen — the dispatch's §3 needing rewriting, not reinterpreting**; and how §2(b)'s
verification is to be discharged by a session that cannot render the paper's pages.

---

*Provenance: written by the CC run of
`records/cc/instructions/cc_instruction_framework_dp_c_correction_2026_09_20.md`, 2026-09-20. Every claim
above was established at the object named beside it in this session: the guard set's output at its own run;
the committed failing set at `tools/audit/guard_state.json`'s `summary`; the boot-pack check at its own
run; the `FROZEN` table, `write_all`, `check_all` and `verify_frozen` at
`tools/audit/gen_derivation_boot_pack.py`; the two sentence sites, the §2(a) quotation and the `[FACT — each
reported by the work named.]` label at `FRAMEWORK.md`; the sentence in the frozen member at that member;
the tracked and untracked population at `tools/audit/changed_paths.py`; the freeze and manifest commits at
`git log` by path; the absent renderer at the failed read and at `which`. The previous batch's GREEN
report was read at `records/cc/reports/cc_framework_9_0_correction_report.md`. No value of this project's
own measurement is restated (#17f, **D-431**). No decisions-register identity is allocated, and no
open-items row was created or flipped.*
