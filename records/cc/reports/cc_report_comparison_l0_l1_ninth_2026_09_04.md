# CC report — the ADDITIVE REPAIR OF §11 AND §12, ninth writing: each section gains a subsection at its foot and its opening claim is corrected, no numbered item edited or renumbered

> **This report decides nothing.** It records what was derived at the rows, what was added, what was
> measured at the objects, and what was deliberately not touched. **The repair is a ruling APPLIED, not
> a judgment made**: Ruling 79 (§3ch) fixes the shape and this batch executed it. No ruling was taken,
> no position was weighed, no row block was touched, no proposed disposition was edited or deleted
> anywhere, no numbered item of §11 or §12 was edited, deleted, renumbered or reordered, §10 was not
> touched, **no audit question was composed**, no verdict was attached to the derivation or to the
> deriving session's independence, and no decisions register and no suspension file was written. Where
> this report would name a hash it cannot contain — its own commit, and the end-state commit that
> follows — it points at the git log. **No count of the reading file is restated here** (**D-431**,
> #17f); the counts below are this batch's own measurements at the objects, each named with how it was
> taken.

**Dispatch:** `cc_instruction_comparison_l0_l1_ninth_2026_09_04.md`, pinned at Task 0 to blob
`21623a5fb32f3a58f1de4d17a100f1e2a6aa1eac` and proven unmoved immediately before staging.
**Full close:** `cowork_away_returns.md`, the section **THE ADDITIVE REPAIR OF 11 AND 12 (NINTH
WRITING) …**, which carries the commit table, both guard states, E0–E2, A1–A5, the departures and the
plan lines. This report does not restate them; it carries the four derived sets, the audit-question gap
at its four rows, and the object-level check of Task 1.

---

## 1. What was done

**Two subsections were added and two opening claims were corrected. Nothing was removed.**

- **Task 0** landed `cowork_rulings_2026_08_31_decision_surface_sitting.md` (Ruling 79, additions
  only), `cowork_handoff_entry_ninety_nine.md` and this dispatch — **three paths**, because A3 was
  measured first and the membership artifact had not moved.
- **Task 1** derived the four sets at the rows of §6 **before any edit**, then made the four ordered
  edits in one commit: one subsection at the foot of §11, one at the foot of §12, and a correcting
  sentence appended to each section's opening paragraph.
- **Task 2** closed: the three `STATUS.md` pointer entries, the forward-bound move, the regenerated
  read-size artifact, the close section and this report.

**Nothing else was touched.** The derivation is byte-unchanged, banner and body; the derived
specification is byte-unchanged; no outgoing text was opened; §10 is byte-unchanged; no decisions
register, no suspension file, no open-items row.

## 2. How the sets were derived

Every `*PROPOSED DISPOSITION` line, every `*RULED DISPOSITION` line and every `*PLACED — FAMILY` line of
§6 was read at the file **with the file tools**, together with the physical line following each, so
that a disposition word wrapping onto the next line is read there rather than missed. Each ruled or
placed line was mapped to its row by the nearest preceding row heading and cross-checked against that
row's own `*PROPOSED DISPOSITION` paragraph. **The fourteen rows whose proposed word wraps were read
directly at the row**, one by one, rather than inferred: Rows 10.44, 11.3, 15.18, 18.31, 18.32, 18.33,
18.34, 18.37, 19.6, 19.7, 19.14, 21.5, 21.6 and 24.19. **Both ruled lines were taken for each of the
two rows that carry two** — Rows 1.7 and 10.21.

## 3. SET Q — ruled QUARANTINED, proposed something else

**NINE rows. Exactly the dispatch's prediction, member for member.**

| Row | Proposed | Ruled | By | Audit question at the row? |
|---|---|---|---|---|
| 2.16 | ADOPTED — proposed | QUARANTINED | Ruling 34, §3ao | yes |
| 2.17 | ADOPTED — proposed | QUARANTINED | Ruling 34, §3ao | yes, by reference to 2.16's |
| 3.5 | ADOPTED — proposed | QUARANTINED | Ruling 35, §3ap | yes |
| 3.41 | ADOPTED — proposed | QUARANTINED | Ruling 62, §3bq, item 4 | **no** |
| 3.42 | ADOPTED — proposed | QUARANTINED | Ruling 62, §3bq, item 4 | **no** |
| 3.48 | ADOPTED — proposed | QUARANTINED | Ruling 62, §3bq, item 4 | **no** |
| 4.3 | ADOPTED — proposed | QUARANTINED | Ruling 78, §3cg | yes |
| 14.9 | ADOPTED — proposed | QUARANTINED | Ruling 62, §3bq, item 4 | **no** |
| 17.13 | ADOPTED — proposed | QUARANTINED | Ruling 55, §3bj | yes |

**The four candidates the dispatch's declared bound left open were checked at their rows and are NOT
members.** Rows **10.51**, **18.35**, **18.39** and **19.22** each carry a family placement line reading
QUARANTINED — and each was **already proposed QUARANTINED**, so none of the four moved.

## 4. SET Q-OUT and SET P-IN — both EMPTY, as predicted

**Set Q-out** — proposed QUARANTINED, ruled anything else — is **empty**. Every row proposed
QUARANTINED that carries a ruled line keeps QUARANTINED. **Row 1.7 is the one that needed care**: it
carries two ruled lines, one QUARANTINED *"unchanged"* for its audit question and one ADOPTED — carried
in substance for its content, so it was **not** ruled out of QUARANTINED and its numbered question in
§11 stands. No further bullet group was added to §11.

**Set P-in** — ruled ADOPTED — proposed, proposed something else — is **empty**. Every row ruled
ADOPTED — proposed was proposed ADOPTED — proposed. No further bullet group was added to §12.

## 5. SET P — proposed ADOPTED — proposed, ruled anything else

**SEVENTY-FIVE rows, where the dispatch predicted fifty-eight. The derivation governs, as the dispatch
orders, and the difference reconciles member by member.**

The dispatch declared its own bound in terms: its prediction rested on the ruled lines whose
disposition word stands on the marker's own physical line, plus Row 4.3, and did **not** cover the
family placement lines, whose word wraps onto the following line. It stated that a larger set is the
expected outcome and not a STOP.

- **The fifty-eight the prediction names are all present, and their composition matches its own
  breakdown exactly** — nine QUARANTINED, nineteen RELOCATED, and thirty to ADOPTED — carried,
  ADOPTED — carried in substance or HISTORICAL. Not one is missing and not one is differently classed.
- **Sixteen further rows come from the family placement lines the bound excluded** — ten of the
  `*PLACED — FAMILY` kind (Rows 10.5, 14.14, 15.8, 18.3(i), 18.4, 18.5, 18.24, 18.30, 18.40, 18.41) and
  six of the `— FAMILY PLACEMENT` kind (Rows 18.10, 18.11, 18.13, 18.14(i), 18.20, 19.10). **Every one
  of the sixteen is ADOPTED — carried in substance.** They reconcile against §13b's own published
  figure for that disposition without restating it: that population is the sixteen plus exactly one
  further row, **Row 10.11**, whose proposed disposition was ADOPTED — carried rather than
  ADOPTED — proposed and which is therefore correctly outside Set P.
- **One further row, Row 10.21, is a judgment this side took and states rather than hides.** It carries
  two ruled lines — Ruling 49 (§3bd) rules it ADOPTED — proposed for its three raw relations, and
  Ruling 48 (§3bc) RELOCATES its computed half to L3. **Only the second takes it out of the proposals
  half.** It is listed, with both lines named on its own bullet and the two-line fact stated there, so
  a reader can see it is a partial move; excluding it would have hidden a relocation the rulings made.

**58 + 16 + 1 = 75.** The full membership, each row with its ruled word and the ruling that gave it,
stands in §12's own added subsection and is not restated here (#6).

## 6. The four rows for which NO AUDIT QUESTION IS WRITTEN — the gap STATED, nothing composed

**Rows 3.41, 3.42, 3.48 and 14.9**, exactly the four the dispatch predicts, each established by reading
that row's ruled line. Each was quarantined by **Ruling 62 (§3bq, item 4)**, whose line at each row
states the GROUND and writes no question. §5 requires a quarantined row to write its audit question in
one sentence, so these four rows are a GAP in the file.

**NOTHING WAS COMPOSED FOR THEM.** Each of the four entries in §11's added subsection says in terms that
no audit question is written at the row, quotes the ground **verbatim** from that row's own ruled line,
and points at question 2 of the numbered list — Row 1.3's — which Ruling 78 (§3cg) records as the
standing question about the same field list. **Composing a question would have authored what the audit
asks, which no batch may do.**

**REPORTED TO THE USER AND LEFT OPEN:** whether those four rows are owed an audit question at all, and
who writes it. Ruling 79's own sentence — *"§11's entries carry the audit question in one sentence as
§5 requires"* — assumes every quarantined row writes one, and these four do not; the ruling does not
say what to do there, and neither the dispatch nor this batch decided it.

## 7. The object-level check of Task 1, measured rather than asserted

The edited reading file was written into the object store with `git hash-object -w` (blob
`a025c686cb652ad871bf12b7cec140023ce92609`) and diffed **between two explicit blob hashes** against the
tip blob `72f9ad22be310e45aa593bc09f13bf1ef10d5c6b`.

- **139 insertions and ZERO deletions anywhere.** A **pure insertion diff** — every line of the tip
  blob survives byte-identical, so no numbered item's leading number changed, no row block moved, and
  no table row could have moved.
- **★ THREE changed passages where the ordered check names FOUR, reported rather than smoothed over,
  and its cause measured.** All four ordered edits are present; two of them — the §11 addition and the
  §12 opening correction — sit **seven lines apart** in the file, so a diff tool merges them into one
  hunk under the dispatch's own definition of a changed passage. The three hunks are: §11's opening
  correction; the §11 addition together with §12's opening correction; and the §12 addition. **The
  difference is accounted for by the ordered edits themselves and by nothing else.**
- **Exactly two `###` headings added, both of them the two ordered subsections** (34 → 36), with the
  `##` heading count identical on both sides at 19.
- **Every `*PROPOSED DISPOSITION` line unmoved** and **every `*RULED DISPOSITION` line unmoved** — the
  same count on each side, none added, none deleted.
- **No `**Row` heading added or removed, proven rather than eyeballed**: the strict row-heading line
  set was extracted from both blobs, sorted and diffed — **empty both ways**. The added bullets open
  `- Row `, the shape §10's own added subsection already uses, and no added line opens `**Row`.
- **§13's tables, §13a's tables and figures, and §13b's three tables byte-identical**, proven over the
  whole file rather than a line range: every table row in both blobs extracted, sorted and compared —
  **901 on each side, the two sets diff empty**.
- **One bullet per member, exactly**: the count of lines opening `- Row ` rose by the size of Set Q plus
  the size of Set P and by no more.

## 8. What was measured, not assumed

- **The declared start state held exactly.** A full guard run **in `--check` mode, which the dispatch
  orders**, before the first edit: **76 run, 10 failing, 4 not run, 16 historical, zero STOPs** — the
  ten failing being the ten the dispatch's own FACT names, member for member, with no eleventh red.
  **The mode was named at the tool rather than probed for**, on the eighth batch's declared departure
  3, and the artifact was hashed afterwards rather than assumed unmoved: `guard_state.json` came back
  at `df81a61d5d5812a5995f2ef35ca698c19deaa6cf`, the very blob the dispatch names.
- **A1 was checked over the whole tracked population** with the sanctioned enumeration tool: **exactly
  one** tracked modification at boot — the sitting record — which is what A1 declares, and both named
  untracked paths present. The large standing untracked root population was met exactly as A1 names it
  in advance and none of it was landed.
- **The sitting record's additions-only property was proven at the object**, between two explicit blob
  hashes: one changed passage, **120 insertions, ZERO deletions**, every added line inside the new
  `## 3ch.` section, the trailing context the `## 4.` heading.
- **A3 was measured before being accepted:** the membership artifact regenerated and its blob hash
  compared against the committed one — **byte-identical** (`fb3a00be…` both sides) — so it was **not**
  committed and Task 0 carried **three** paths rather than four.
- **The forward bound was measured, not assumed:** re-aimed at all five authored inputs with
  `PREVIOUS_AIMINGS` appended to rather than replaced, run, and the dispatch's prediction held exactly
  — **the eighth writing's three entries moved, the second writing's two stayed, no STOP** — with the
  tool's own reconciliation green in both limbs and `--check` passing after the act.
- **A2's watched exceptions did not fire.** `gen_l0_l1_outgoing_population.py --check` still re-derives
  after the reading file and `STATUS.md` moved — no member of the population moved and the artifact
  needed no regeneration — and `gen_session_start_read_size.py --check` is green, regenerated after the
  last edit to `STATUS.md`.
- **Every commit was verified at the object** by `git show --stat` at its explicit hash, and
  `origin/master` read at its ref file with the file tools after each push, never from a push's own
  output. The Task 1 commit's own blob for the reading file was read back at
  `git rev-parse <commit>:<path>` and is the blob the check above was taken over.

## 9. What this batch did not do

No ruling taken, no position weighed, no verdict on any open question, on the derivation, or on the
deriving session's independence. No session booted. No measurement of the analysis built or run. No
derivation of any statement about the analysis. No edit to the derivation (banner or body), the derived
specification, the brief, the pack, its generator, its artifact, the population tool, any outgoing
text, the suspension file, any register source, or any governing document other than the two Task 2
orders. In the reading file: **no row block touched**; **§10 not edited at all**; no numbered item of
§11 or §12 edited, deleted, renumbered or reordered; no proposed disposition deleted anywhere; §13's
tables, §13a's figures and §13b's three tables untouched. **No audit question composed, paraphrased,
merged, or attached to an existing numbered question.** No `src/` change, no golden, no test changed,
moved or run, no build, nothing under `tools/corpus/` or `tools/robust_stop/`. No open-items row
created, flipped or discarded; no register entry and no identifier allocated; no guard enrolment; no
tool added. **No recommendation appears anywhere** — not in the reading file, not in the close, not in
this report, not in a commit message.

The one `tools/audit/*.py` edit is the re-aiming of `gen_status_batch_bound.py`'s five authored
constants, its `PREVIOUS_AIMINGS` list and its head comments, which Task 2 orders in terms and which
**D-648** classes as authored-input maintenance.

## 10. Declared departures

1. **The dispatch was read from the working tree before it was pinned** — P-2's own fallback route.
   Pinned at Task 0, blob proven unmoved before staging.
2. **Diff, guard and derivation output was written to scratch paths outside the repository and read
   with the file tools.** Every repository read went through the file tools; the only shell reads were
   git object queries by explicit hash, some piped to `grep`, `sed`, `sort` and `diff` to count and
   compare object-level line sets.
3. **★ The two blobs were materialised to scratch with `git cat-file blob <hash>` and compared there.**
   Both sides of every structural comparison — the row-heading set, the table-row set, the
   disposition-line counts and the heading counts — were taken over those two scratch copies. It is a
   content-addressed read by explicit hash on both sides, which is what makes the comparison
   self-verifying; declared because the reads that followed were ordinary text utilities over those
   copies.
4. **★ Three commands were denied by the armed shell-read guard and re-taken.** Each named its file
   through a shell variable or a relative path after a `cd`, and the guard refused on its
   deny-on-indeterminate policy (**D-647**). Two were re-taken with absolute scratch paths and one with
   the Grep file tool. **Recorded because the guard firing is the mechanism working.**
5. **★ `python tools/audit/changed_paths.py --tracked-only` was attempted once and rejected by the tool
   itself** as an unrecognized argument, before the correct flagless invocation was taken. It read
   nothing and wrote nothing; declared because it is an invocation this batch made.

## 11. Where the hashes are

The commit table — the tree met, Task 0, Task 1 and the close — is in the close section of
`cowork_away_returns.md`, each by explicit hash, each pushed and each verified equal at
`.git/refs/remotes/origin/master` **with the file tools**. **This report's own commit and the end-state
commit that follows it are in the git log**, which is where a document cannot name its own identifier.
**The close does not assert the end state**; the one further commit carries it.
