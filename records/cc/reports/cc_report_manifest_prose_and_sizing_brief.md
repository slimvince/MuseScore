# CC report — the manifest prose corrected in two bounded places, and the sizing brief landed

> **Dispatch:** `cc_instruction_manifest_prose_and_sizing_brief.md`, performed 2026-08-24, executing
> Ruling 2 and §3 of `cowork_rulings_2026_08_24_sizing_leak_list_sitting.md`, and landing that record
> together with the DRAFT sizing brief `cowork_blind_session_brief_scoring_model.md` — **landed, not
> written, and not edited.**
>
> **NO SESSION WAS BOOTED. Nothing was derived, nothing compared, no oracle opened,
> `docs/scoring_model.md` was NOT opened, and neither blind output was opened.** The full close is
> the **THE MANIFEST PROSE CORRECTED AND THE SIZING BRIEF LANDED** section of
> `cowork_away_returns.md`. Where this report would name a hash a commit cannot contain, it points at
> the git log — **no backfill commit was written.**

---

## 1. What was done, in one paragraph

Task 0 landed the leak-list ruling record, the DRAFT sizing brief, the modified handoff and the
dispatch, and regenerated the evidence-pin membership after measuring its difference against the
committed blob from every route. Task 1 took exactly the two licensed corrections to the boot-pack
generator's own prose — the candidate-criterion block made true of an empty-by-ruling criterion, and
the top-level rulings list completed with the sizing-pilot ruling — and re-rendered both subjects.
Task 2 is the close. **Both pack directories are byte-unchanged, every file, and the
harmony-boundary subject's manifest block is byte-unchanged too** — which is the stronger of the two
outcomes the dispatch admitted, and it is proven at the objects rather than asserted.

## 2. Task 0 — the landing, the membership, the push

**A1's check was taken as the first act after the ordered session-start read, entirely at
content-addressed objects,** the bare working-tree forms being denied by the armed guard and timing
out on this mount.

1. **The branch rule at the tip and nothing else.** Both refs named
   `24d7f0be931940ef7eb0c32127f84cf702c1c059`; its parent and its subject are the ones the premise
   ledger states.
2. **Exactly ONE tracked modification, and it is the one A1 names** — `cowork_handoff.md`, and no
   other tracked path anywhere in the tree. The population was ENUMERATED with the sanctioned
   enumeration tool rather than sampled.
3. **The three named untracked paths are present, and all three are absent from the tip**, each
   checked at the object.
4. **THE INSERTED-ENTRY COUNT, MEASURED RATHER THAN ASSERTED — see §6.1.** It came back at **ONE**,
   which is what A1 declined to assert and ordered measured.
5. **The premise ledger re-checked at the objects:** the guard summary at its own artifact (run 75,
   passing 74, failing 1, not_run 4, historical 16); **BOTH subjects' whole `counted` blocks**, field
   by field, matching the ledger exactly; the committed membership artifact's ruling-record count;
   and neither landed file carrying the word route B matches on, on any line.

**The membership was regenerated and MEASURED against the committed blob BEFORE it was accepted,
from every route (§4, A3).** The five ordered paths were staged, enumerated as staged, committed
under the exact ordered subject, pushed, and verified at the object; the enumeration was re-taken at
the commit. **`gen_evidence_pin_membership.py --check` passes at the resulting tree.**

## 3. Task 1 — the two licensed corrections, and the re-render

### (a) The candidate-criterion block, made true of an empty-by-ruling criterion

The correction is **DERIVED from the authored entry and never switched on a subject name**, which is
the licence's own stated condition. Two functions were added:

- `criterion_is_empty(spec)` — true where the authored criterion carries **no term at all**
  (`groups`, `home_documents`, `architecture_spans`, `keywords`, `always` all empty). It recognizes
  the STATE, so a later subject in the same state renders the same way without a line being touched.
- `criterion_block(subject)` — returns the bound paragraph and the criterion block together. The
  **non-empty branch is byte-for-byte what the tool published before the correction**, which is what
  makes the harmony-boundary block's byte-identity a proof rather than a coincidence.

**What the empty branch now says**, in place of five conditional bullets describing a pattern match
that never ran and a paragraph disclaiming that match's unmeasured reach: that **no candidate search
was run for this subject and no criterion is authored for it, its withheld family being empty by
ruling; and that the empty candidate list and the empty verdict table are a CONSEQUENCE OF THAT
RULING and not the result of a match that returned nothing.** The five term lists are still
rendered, all empty, under a statement that their emptiness says nothing about what the record holds
— so nothing is lost (#12) and no reach may be read into them.

### (b) The top-level `the_rulings_it_executes` list

One entry appended, in the shape the list's four existing entries use, naming
`cowork_rulings_2026_08_24_sizing_pilot_sitting.md` and what its Ruling 1 rules: the second subject,
`scoring-model`, rendered with an EMPTY withheld family, the standing leak check doing the whole of
the cutting. **The wording was taken from that record read whole at its own text**, not from memory
of it and not from the dispatch's account of it.

### (c) The checks, in the ordered sequence

- **BETWEEN the edit and the re-render**, the generator's own `--check` reported drift at
  `derivation_boot_pack.json` **and at NO file of either subject's pack**. That is the proof the
  dispatch's §A5 asks for, and it was taken **before any file was written**: neither correction is
  rendered into anything a deriving session opens.
- **AFTER the re-render**, `--check` is **GREEN over both subjects**. No STOP fired at any run.

### (d) A5, measured — every item

- **Every count of BOTH subjects is UNCHANGED**, field by field. The render's own summary reproduces
  the harmony-boundary and `scoring-model` blocks exactly as the premise ledger states them, and the
  manifest's difference (below) contains **no `counted` hunk at all**.
- **BOTH pack directories are BYTE-UNCHANGED, every file — established at the content-addressed
  blob identifiers, by enumeration and not by sampling.** All fourteen files (seven per subject)
  were listed from the Task 0 commit's tree and each compared against the working-tree file's own
  content hash: **fourteen of fourteen IDENTICAL.** The independent enumeration agrees — after the
  render the whole tracked-modification population is the manifest and the generator, and no pack
  file appears in it.
- **THE MANIFEST'S WHOLE DIFFERENCE IS TWO HUNKS**, quoted here in full because the dispatch orders
  it quoted:

  **Hunk 1 — the top-level list, purely additive.** One member is appended, and the member above it
  changes only by the comma the appending requires:
  ```
  -    "Ruling 4(c) of `cowork_rulings_2026_08_21_successor_plan_sitting.md` — the held-out test and its oracle."
  +    "Ruling 4(c) of `cowork_rulings_2026_08_21_successor_plan_sitting.md` — the held-out test and its oracle.",
  +    "Ruling 1 of `cowork_rulings_2026_08_24_sizing_pilot_sitting.md` — the second subject, `scoring-model`, rendered with an EMPTY withheld family, the standing leak check doing the whole of the cutting."
  ```
  No other member of that list moved, and the three above it are absent from the difference.

  **Hunk 2 — inside the `scoring-model` subject and nowhere else**: the bound paragraph replaced,
  and the five conditional bullets replaced by one statement. The removed text is the pattern-match
  paragraph and the bullets; the added text is the empty-by-ruling statement quoted at §3(a).

- **THE HARMONY-BOUNDARY SUBJECT'S MANIFEST BLOCK IS BYTE-UNCHANGED, AND THIS IS THE STRONGER
  RESULT THE DISPATCH NAMED AS THE ONE TO AIM AT.** It is established at the objects rather than
  from the hunks' positions: the block was extracted from **both committed manifest blobs** — the
  Task 0 commit's and the Task 1 commit's — from `"harmony-boundary": {` to the line before
  `"scoring-model": {`, and the two extracts carry the **same sha256**,
  `307af0046207cab6515e8a2ade4683872d9f9a4144b283dc00e02d79e3da1628`.
- **No pack file changed, so the STOP A5 reserves for one did not arise.**

### (e) The guard run and the commit

The FULL set was run in write mode. **75 guards run, ONE failing** — [[OI-372]]'s tool alone, the
standing red; 4 not run, 16 historical records; **ZERO STOP verdicts**; the population UNMOVED.
`gen_guard_classification.py --check` was run separately, as its own STOP requires, and re-derives.
The generator, the manifest and the one guard artifact the run rewrote were committed under the
exact ordered subject, pushed, and verified at the object.

**One difference inside `guard_state.json` is NOT this batch's doing and is reported rather than
absorbed — see §6.4.**

## 4. A3, graded at the membership regeneration

**Route A — MET exactly.** `ruling_records_read` moved **56 → 57**, the single added name being
`cowork_rulings_2026_08_24_sizing_leak_list_sitting.md`. **The brief did NOT enter route A**, and the
STOP A3 declares for that did not fire: neither `cowork_blind_session_brief_scoring_model.md` nor the
dispatch appears anywhere in the regenerated artifact.

**Route B — MET.** Nothing was added: neither landed file carries the word that route matches on, on
any line, checked at both files before the regeneration.

**Route C — MET.** Unmoved: this batch adds no measurement tool, and the pin-constant population, the
member set and the pinned count are all absent from the difference.

**THE MEASURED DIFFERENCE IS THREE HUNKS** — the count, the one added name, and **ONE additive
derived cross-reference**: the leak-list record's own text names
`ratification_surfaces/cowork_sizing_pack_leak_list_reading.md`, which no tool writes, so that
document's list of namings gains the record. **That is exactly the addition A3's text anticipates and
orders reported rather than absorbed, and its cause is the ordered act itself.** No existing value
moved anywhere.

## 5. What this batch did NOT do

**No session booted. No derivation, no comparison, no oracle opened. `docs/scoring_model.md` not
opened. Neither blind output opened.** The brief was read at its **status banner only** — the twelve
lines the dispatch admits — and its content is untouched: it lands DRAFT and unruled at its §8, as
delivered.

**No change to the generator beyond the two corrections.** The authored tables, the criterion, the
leak check, `withhold_passage()`, the withheld marker, the read-me's rendering and every STOP are
untouched — established by re-reading the generator's whole diff from the git objects before the
commit.

**No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
`tools/robust_stop/`.** No open-items row created, flipped or discarded. **No edit to any governing
document, to any entry of the decisions register, or to any of that register's sources** — proven by
the tracked-modification enumeration at each stage, which never carried one. No document archived,
moved or deleted as a file **except** the previous batch's two `STATUS.md` entries under the ruled
forward bound. **No finding number: the series stands at F88.**

## 6. Surfaced for the writing side

**Nothing below is graded as a defect anywhere, and no row is created — both are barred by the
dispatch.**

1. **A1'S CONTENT DESCRIPTION IS EXACT THIS TIME, AND THE MEASURED COUNT IS ONE.** The committed
   handoff's newest entry is the **FIFTY-SECOND**; the tree carried **ONE** inserted entry, the
   fifty-third; the **ONE** replaced line is the fifty-second's own entry-point heading, which gains
   its superseded-as-entry-point clause. The arithmetic closes over the whole measured difference —
   115 added lines and 1 deleted, one inserted entry block plus one replaced heading. **A count other
   than one was admitted and did not arise. A1's declared STOP did not fire.** The remedy the
   dispatch applied — ordering the count measured instead of asserting it — is what made this
   checkable rather than merely right.

2. **A THIRD PROSE DEFECT OF THE SAME CLASS WAS MET AND IS NOT CORRECTED: THE TOP-LEVEL
   `the_rulings_it_executes` LIST IS STILL INCOMPLETE AFTER CORRECTION (b).** Ruling 2(b)'s own words
   are that the addition makes the list *"complete rather than incomplete"*. Measured at the
   generator's own text, **two further ruling records the generator executes are named nowhere in
   that list**, and both are cited inside the authored `WITHHELD` table where they do their work:

   - **`cowork_rulings_2026_08_22_withheld_family_sitting.md`** — cited twice. Verbatim at the first
     withheld passage's `finding` field: *"Ruling 1 of
     `cowork_rulings_2026_08_22_member_two_leak_sitting.md`, widened by Ruling 5 of
     `cowork_rulings_2026_08_22_withheld_family_sitting.md`"*; and verbatim at the D-569 verdict:
     *"Ruling 3 of `cowork_rulings_2026_08_22_withheld_family_sitting.md`, ruled by the user over an
     UNPLACED verdict"*. Location: `tools/audit/gen_derivation_boot_pack.py`, the `WITHHELD` table's
     `harmony-boundary` entry and the `VERDICTS` table's `D-569` entry.
   - **`cowork_rulings_2026_08_23_member_two_second_leak_sitting.md`** — cited once, verbatim at the
     second withheld passage's `finding` field: *"Ruling 1 of
     `cowork_rulings_2026_08_23_member_two_second_leak_sitting.md`: the sentence names, from its own
     words, a boundary-membership convention"*. Location: the same `WITHHELD` table entry.

   **Both are rulings the generator EXECUTES in exactly the sense the list's third existing member
   is** — that member names the 2026-08-22 member-two leak ruling as an authored withheld input, and
   these two authored the widening of that same passage, the three verdicts the user ruled, and a
   second withheld passage. **The licence reaches two corrections and the narrow-letter default
   governs the remainder**, so this is surfaced with its verbatim and its location, and nothing was
   written for it.

3. **A FOURTH INSTANCE OF THE SAME CLASS, IN THE GENERATOR'S OWN DOCSTRING, LIKEWISE NOT CORRECTED.**
   The module docstring's *WHAT THIS DOES NOT ASSERT* block still reads, verbatim:

   > `* That the CANDIDATE CRITERION is complete.  Its reach is that of a pattern match over the`
   > `  register's own text and it is UNMEASURED (#19, D-673); the bound is stated on the artifact at`
   > `  \`★_the_bound_on_the_candidate_criterion\` and an empty match is evidence of nothing.  No`
   > `  analysis decision consumes this enumeration -- the user does -- which is the test D-673 fixes.`

   Location: `tools/audit/gen_derivation_boot_pack.py`, the module docstring, the *WHAT THIS DOES NOT
   ASSERT* block. It is written for a subject that HAS a criterion, and after correction (a) the
   field it names carries, for the empty subject, the empty-by-ruling statement rather than that
   bound. It was already in this state before this batch — the empty criterion existed at HEAD — and
   **the licence names the MANIFEST's block, not the docstring**, so it is reported and left.

4. **`guard_state.json` CARRIES ONE RECORDED DIFFERENCE THIS BATCH DID NOT CAUSE, AND ITS CAUSE IS
   MEASURED RATHER THAN GUESSED.** The write-mode run's artifact differs from the committed one in
   **two** places, both inside a `stdout` array of a check whose verdict is **PASS** in both: the
   membership line (`ruling records read 56` → `57`, this batch's own Task 0), and the session-start
   read measurement's `STATUS.md` size and totals, lower by one character. **The second is the
   PREVIOUS batch's fifth commit** — its one-character reserved-word correction in `STATUS.md` —
   **whose verification run was performed in CHECK mode, which writes nothing**, so the committed
   `guard_state.json` still carried the pre-correction stdout. The write-mode run this batch was
   ordered to perform is the first to record it. **No verdict moved, no tool was added, the
   population is unmoved, and the failing set is unchanged** — the summary block is absent from the
   difference entirely. Reported because a difference in a committed artifact that a batch did not
   cause is exactly what a later reader would otherwise attribute to it.

5. **THE ONE FORM QUESTION THE CORRECTION RAISED, ANSWERED BY THE FILE'S OWN CONVENTION.** The new
   `criterion_block` docstring quotes Ruling 2(a) with ASCII double hyphens where the ruling record
   carries em dashes. That is the convention the module's own top docstring already uses for every
   ruling it quotes; the wording is verbatim. Stated so the choice is visible rather than assumed.

## ★ THE STANDING SELF-CHECK (D-434) OVER THIS BATCH'S OWN WORK

1. *Principles touched.* **#19** — a generated artifact stops asserting a search that never ran, so
   what it claims is what was done; and the correction is derived from a state, so it cannot be
   right for one subject and wrong for the next. **#6** — one derivation of the prose from the
   authored entry, no per-subject branch, and the non-empty branch byte-for-byte what it was. **#12**
   — every count, every pack file, the harmony-boundary manifest block and the brief's own text
   preserved untouched; the five empty term lists still rendered rather than dropped. **#15** — the
   byte-identity claims are verified at content-addressed objects and by whole-population
   enumeration, never at an assertion. **#17(f)/D-431** — the manifest's difference is quoted from
   the measured diff, and no count is restated in the `STATUS.md` pointer entries beyond the
   measured handoff arithmetic the dispatch itself ordered reported. **#13** — three further
   instances of the corrected class are surfaced with their verbatim and location rather than swept
   in. **#10** — the narrow-letter default governs everything the licence does not reach. Conforms.
2. *Conventions.* American English (one British spelling in the new docstring was found by this
   check and corrected before the commit); no self-invented labels — *subject*, *member*, *pack*,
   *leak*, *criterion* and *bound* are the generator's and the rulings' own; music-theory words in
   their musical sense with every non-musical use qualified — this check replaced *figures* with
   *counts* in a `STATUS.md` entry and *the rest* with *the remainder*, and it wrote *any entry of
   the decisions register* rather than a bare *register entry*.
3. *Figures and premises.* The tip, both refs, the guard summary, both subjects' `counted` blocks
   and the membership count were re-read at the objects rather than carried from the dispatch's
   ledger; the sizing-pilot ruling's content for correction (b) was read at that record's own text;
   the manifest and generator differences were read from `git diff` against an explicit hash, written
   to scratch paths outside the repository and read back there.
4. *File-tools rule.* Declared in the close. Every working-tree read went through Read/Grep/Glob;
   the shell was used for read-only git object queries by explicit hash, the sanctioned enumeration
   tool, the project's own committed tools, the staging and commit acts, and `grep`/`head`/`tail`/
   `sed`/`sha256sum`/`wc` over scratch files and git-object output only. **Three shell reads were
   DENIED by the armed guard** — one `python -c` naming a repository path at the session's start, and
   two later commands whose scratch paths were carried in shell variables the guard could not expand
   (deny-on-indeterminate, working as ruled). Each was retaken with the file tools or with the
   absolute scratch path spelled out; **none was retried in another dialect.** No working-tree
   `git status`, no bare working-tree `git diff`.
5. *Uncertainty.* No difference between measured quantities is asserted in this batch.
6. *Re-read from disk before release.* The generator's whole diff was re-read from the git objects
   after the edits and before the commit; the manifest's whole difference was read the same way; the
   `STATUS.md` entries were re-read at the file after the forward bound was applied over them.

---

*Provenance: CC, 2026-08-24, under `cc_instruction_manifest_prose_and_sizing_brief.md`, executing
Ruling 2 and §3 of `cowork_rulings_2026_08_24_sizing_leak_list_sitting.md`. Every value above was
read at a content-addressed git object or at the artifact the run itself wrote; none was carried
forward from an earlier run or inferred from a summary. TOWARDS the ultimate objective and TOWARDS
the guiding principles.*
