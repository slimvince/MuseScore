# CC report — the prune-at-amendment rule applied to `CLAUDE.md`: the backlog derived and read, and the rule given something that watches it

> **STATUS: BATCH REPORT.** Executing dispatch:
> `cc_instruction_claude_md_prune_at_amendment_2026_09_07.md`, on the user's order of 2026-09-07:
> *"1. tidy the mess that was caused by this rule not being followed. 2. make sure this rule is
> being followed from now on."*
>
> **THE HEADLINE, STATED FIRST BECAUSE IT REFUTES THE BATCH'S OWN PREMISE.** The rule was
> **followed** at every amendment of `CLAUDE.md` since it was ruled — measured twice, by two
> independent routes. The material the order calls a mess is **older than the rule**, it was lawful
> when written, and at the ruled span unit **none of it can move**: every remaining candidate is a
> LIVE statement that happens to carry the OPENING of a preserved wording. So **half one of the
> order yielded no move**, and why is the finding. **Half two is delivered**: a lint now watches the
> pattern, its reach measured over all five governing documents before it was enrolled.

---

## 0. What this batch did, in one table

| Task | Act | Commit |
|---|---|---|
| 0 | The dispatch landed; A1 and A2 measured | `d125281a43192f23a65711199b3ccc27f7f07356` |
| 1 | The growth attributed at the git objects | `87b830889d5471ea067be395de72a2ec28b5dad3` |
| 2 | The fresh decomposition and the backlog tool; every candidate read | `ecf989cc3a1b3661f134dabf8354bf600d63d98e` |
| 3 | The lint, its reach measured before enrolment | `1a93a1eac6bd58b8616549d892f451d1214c21bb` |
| 4 | `OPEN_ITEMS.md` OI-380 and the regenerated gating answer | `ff6ba36fddd4bdabdfda88c1fac6fd3b5e63fb03` |
| 4 | The close — this report, the `STATUS.md` entries, the forward-bound move | *below* |

Every commit was verified at its own object after pushing. **`CLAUDE.md` was not edited by any task
of this batch**, and neither was `CLAUDE_ARCHIVE.md`.

---

## 1. The ordinary session-start read, performed before the named file was acted on

`CLAUDE.md` (1,863 lines), `DECISIONS.md`, `STATUS.md` and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`), each
read in full — **D-230** and the standing convention of 2026-08-29 (P-1) that a single-file opening
instruction is not an exemption. Then the dispatch's own named reads, each at its own object
(**D-643**): §5(D) and §5(E) of `cowork_rulings_2026_08_16_preparation_return.md`;
`cowork_rulings_2026_08_17_governing_surface_split.md` whole;
`tools/audit/gen_governing_surface_split.py` and `tools/audit/gen_status_residue_move.py` whole;
`CLAUDE_ARCHIVE.md` whole (it is 152 lines, so head and tail is the file).

### Departure (i) — the dispatch's read-first block names the wrong record for its quoted Ruling 2

The ruling ledger quotes *"Ruling 2 of 2026-08-17"* as ordering the archivability test applied to
*"any span in the five that the test places"*. **Those words are Ruling 2 of
`cowork_rulings_2026_08_17_session_start_read_sitting.md`, not of
`cowork_rulings_2026_08_17_governing_surface_split.md`**, which the read-first block names. Ruling 2
of the record the block names is a different ruling — that the doubt-defaulted share is accepted as
it stands. **Both were read at their own objects**, and both support this act, so nothing turns on
it; it is reported rather than absorbed because a mis-cited ruling is the shape **D-643** exists
against.

### Departure (ii) — two guard-set members the record already knew about, and one the batch found

`tools/audit/gen_guard_classification.py` **STOPs**, and did so before this batch touched anything:
`gen_l0_l1_outgoing_population.py` and `gen_withheld_family_reading.py` were enrolled in the guard
runner by earlier batches without being classified. **This batch added a classification for each of
its own three new tools so it did not make the STOP worse, and repaired neither of the two**, which
are outside its scope. Reported, not acted on.

---

## 2. Task 0 — the start state, measured and not relayed

**A1**, over the whole tracked population with the sanctioned enumeration tool
(`tools/audit/changed_paths.py`): **919 changed-path records, every one untracked, ZERO tracked
modifications**, the dispatch present among them. **A1 HOLDS as declared.**

**A2**, the full guard set run and read: **77 guards run, 13 failing, 4 not run, 16 historical
records** — the twelve inherited plus `gen_phase3_gate_partition.py --check`, which is exactly what
A2 declares. **Nothing was adjusted to reach a number.**

**The handoff entry needed no landing:** `cowork_away_returns.md` carries no tracked modification.

**Registered expectation E0 — GRADED PASS.**

---

## 3. Task 1 — the sixty-line gap, closed at the objects

### How the range was walked, and why it matters

**D-253** permits four git forms, all by explicit hash, and names a branch-tip or log read as never
trusted for what is current. So the chain from the pruning measurement's pin to this batch's Task 0
commit was **walked one PARENT link at a time out of the commit objects themselves**, with
`git cat-file`. **No `git log` and no `git rev-list` was used** — the dispatch declares such an
invocation a breach, and none was made.

### The attribution, and the STOP that makes it complete

**332 commits** in the chain; **SIX touched `CLAUDE.md`**. Their net line changes **sum to exactly
the difference between the two ends' line counts**, which is the tool's load-bearing STOP: a
touching commit left out of the enumeration would break the sum. Every figure is computed;
`tools/audit/claude_md_growth_2026_09_07.json` carries them and no figure is restated here
(**D-431**).

### What it confirms, and what it refutes

**CONFIRMED — the growth since the split is dominated by amendment apparatus.** One commit removes
text on balance (the executed split). Three are amendments; two add new operative rule text.

**REFUTED — and the refutation is the finding.** The dispatch's premise is that the growth was
caused by rule (D) not being followed. **In every one of the three amending commits the superseded
wording is ABSENT FROM THE DIFF.** It went to `CLAUDE_ARCHIVE.md` in the same act and a compact
dated pointer was left at the site — which is rule (D)'s own prescribed shape, performed. What
those amendments ADDED is the pointer and its ground, which the rule *requires*. **Every
preserve-in-place block `CLAUDE.md` still carries is older than the range**, and is Task 2's
separate subject.

Every statement of what a commit added was read at that commit's own diff and **never from a commit
subject**, which the dispatch excludes by name.

**Registered expectation E1 — GRADED PASS.** The touching commits are enumerated whole; their nets
sum to the difference between the two ends' line counts; **the sum closes.**

---

## 4. Task 2 — the backlog derived, every candidate read, and nothing moved

### The fresh decomposition, and the frozen one left alone

`tools/audit/gen_governing_surface_spans.py` gained `--commit`, `--commit-is`, `--dispatch` and
`--out`, **all defaulting to the first wave's values**, so its committed artifact re-derives
byte-identically and its `--check` is green. The fresh five-file measurement at the Task 0 commit is
`tools/audit/governing_surface_spans_2026_09_07.json`. **The frozen artifact was not regenerated,
edited or moved**, and the split tool's STOP on the first wave's own pin is untouched — so the two
`STATUS.md` archive pointers that cite that tool's `--check` by name go on proving what they say.
Verified after the change: all five prior archive-act checks re-derive green
(`gen_governing_surface_split`, `gen_status_residue_move`, `gen_post_split_archive`,
`gen_claude_md_finer_spans`, `gen_claude_md_finer_archive`).

### Departure (iii) — the act runs at `CLAUDE.md`'s own finer unit, and the coarse unit is reconciled against it

The dispatch orders the candidate population taken from the fresh COARSE decomposition. **The
record itself measured that unit wrong for this file** — finding F33, where 15,395 characters of
live governing rule text were placed in an archive class by two sentences that merely point at
former wordings held elsewhere — and Ruling 4 of `cowork_rulings_2026_08_17_sixth_return.md`
commissioned the finer unit as the answer. So the act runs at the finer unit, **and the fresh coarse
unit is reconciled against it span by span, with a STOP on any span the reconciliation cannot
place.** All eleven of its archive-class spans are accounted for: six settled at earlier sittings,
three archive pointers, one the F33 case, one a bare section heading. Neither unit is quietly
preferred.

### The recognizer this act adds, and why it was needed

**No existing recognizer looks for the pattern rule (D) supersedes.** Measured, not assumed: run
against the live file, the coarse classifier places eleven spans and the finer one five, and
**neither reaches the largest preserve-in-place block in the file**. The phrasings the record
actually uses — *preserved in place*, *stands in place*, *preserved under #12*, *Former text,
preserved* — were derived by searching the five governing files, and are published on the tool's
face. The recognizer **deliberately does not match a supersession recorded as a pointer**: that is
rule (D) working, not a breach of it.

### The reading, and its outcome

The population is DERIVED and nothing is hand-listed. After the three ruled exclusions — spans
already archived, the six settled at the seventh- and eighth-return sittings, and the standing
archive-pointer constraint — **two candidates remain, both reached only by the new recognizer, and
both were READ WHOLE.**

| Span | What it is, read whole | Verdict |
|---|---|---|
| 1603–1631 | The six-phase supersession block. Lines 1603–1624 state the governing structure, the rule that replaced the truth half — in words saying it *"must bind even a session that reads nothing else"* — the per-phase retrospective, the standing of every embedded sub-ruling, and how HEAD is read below. Lines 1625–1631 open the preserved former text. | **STAYS** |
| 1720–1749 | The phase-2 ratification note. Lines 1720–1733 are live, and their operative half is **what the ratification did NOT do**. Lines 1734–1749 are the preserved former text of the same note. | **STAYS** |

Both **fail test (1)**: read whole, neither IS a preserved former wording — each is a live statement
carrying the opening of one. The ruled doubt default keeps both. `--apply` **STOPS and reports
rather than forcing a move**, which is the ruled behaviour when a reading keeps its whole
population.

### What would be needed to clear the backlog, stated rather than attempted

**A span unit that separates a preserved wording from the live statement announcing it.** The record
carries two units and **each was commissioned by a ruling with measured evidence behind it**.
Inventing a third to reach a named span is the stretched judgment the ruled doubt default exists to
prevent, and §5(E)'s own ground is that a wrongly archived operative span fails silently while
wrongly kept noise fails visibly and cheaply. **That question stands with the user.**

**Registered expectation E2 — GRADED PASS on every limb, with its move limb vacuous by the
reading.** The fresh decomposition exists at its own path with the frozen one byte-unchanged; the
byte-presence limbs hold trivially because nothing moved; **moved plus kept accounts for the base
blob to the character**; every left-at-site flag is enumerated (two); and the new tool's own guard
PASSES.

---

## 5. Task 3 — the rule given something that watches it

`tools/audit/prune_at_amendment_lint.py` fails when a governing document carries a span saying its
OWN former wording is preserved IN PLACE, and passes when a supersession is recorded as a pointer
naming where the former text now lives. **Its recognizer is IMPORTED from Task 2's tool, never
copied (#6)**, so a phrasing added there is watched here the same day.

**Its one cut is derived from the rule's own ruling date, not authored.** A statement dated at or
before it was the lawful pattern when written and is reported as PRE-RULE BACKLOG; one dated after
it is a BREACH; **one carrying no date at all is a BREACH too**, so nothing escapes by carrying
none. Without that cut the check would be red forever on text older than the rule — and a check red
at every tree teaches a reader to ignore it, which the record names as the failure the guard set
exists against.

### The reach, measured over all five before enrolment, in both directions

| File | Hits | of which pre-rule | of which BREACH | Near misses | Pointer-form records |
|---|---|---|---|---|---|
| `CLAUDE.md` | 5 | 5 | **0** | 1 | 9 |
| `OPEN_ITEMS.md` | 2 | 2 | **0** | 4 | 134 |
| `DECISIONS.md` | 0 | 0 | **0** | 2 | 0 |
| `STATUS.md` | 0 | 0 | **0** | 1 | 2 |
| `BUILD_AND_TEST.md` | 0 | 0 | **0** | 0 | 8 |

*(The table restates the tool's own published measurement, which is the artifact
`tools/audit/prune_at_amendment_lint.json`; it is reproduced here because the dispatch orders every
hit and every near miss listed in the report.)*

**ZERO BREACHES IN ANY OF THE FIVE.** That is an independent confirmation of Task 1's refutation,
taken by a different route: rule (D) has not been breached in a governing document since it was
ruled.

**Enrolled scoped to `CLAUDE.md`**, which is a scope decision and not a measurement — the user's
order names that file. A hit in the other four is REPORTED and not acted on; **widening the scope is
the user's.**

**Registered expectation E3 — GRADED PASS, with its stated reason corrected.** The lint PASSES on
`CLAUDE.md` at the tree Task 2 leaves. E3 predicts that pass *"because Task 2 has just cleared what
it looks for"*. **Task 2 cleared nothing.** It passes because every hit carries a pre-rule date. The
prediction's verdict holds and its reason does not, and that is stated rather than absorbed.

---

## 6. Task 4 — the row, the gating answer, and the close

### The row

**`OPEN_ITEMS.md` OI-380**, with its detail file in the same commit (register rule (c)). The
identity was measured across the INDEX and `open_items/` before it was written.

### Departure (iv) — the row is written to what was measured, not to the dispatch's draft

The dispatch's draft row states *"The backlog was cleared by … Task 2"*. **It was not**, and writing
that would have put a false statement into the register (#10) which three separate derivations read.
The row records the three measured findings instead — the refutation, the lint's zero breaches, and
the reading that kept every candidate — asserts no cause beyond those objects, proposes no remedy,
carries no apparatus declaration and no gating verdict, and allocates no finding number. **It is the
only row act performed.**

### The gating answer

**Regenerated, never hand-edited.** Its check went **FAIL then PASS**, the new row falling in the
**RULED DEFAULT** cut — outside the over-inclusive apparatus first cut, so the non-gating
declaration does not reach it. The index status lint **PASSES** and the split reconciliation
**PASSES** across it (bijection 380/380).

### The close, and the forward-bound move performed on this batch itself

The five `STATUS.md` entries were written **FIRST** and `--apply` run **second** — the order is
mechanical, not a preference, because the declared prefix adjustment strips `Last updated: ` from
the text read at the base commit and the stripped form exists in the live file only once the prefix
has moved up. `gen_status_batch_bound.py` was re-aimed at **all six authored fields together**, with
`MOVE_KIND = "ordinary"`, and its own aiming appended to `PREVIOUS_AIMINGS` (#12). **Four entries
moved**, both reconciliation limbs green.

### ★ Departure (v) — the forward-bound tool STOPPED on a real defect, the cause was established at the object, and the mechanism was repaired narrowly

**What happened.** The first `--apply` STOPPED: *"the entry at base line 12 occurs 0 time(s) in the
live STATUS.md"*.

**The cause, established at the object before anything was touched (D-669).** The declared prefix
adjustment tested `if PREFIX_ADJUSTMENT in text` — **a substring search over the whole entry**. The
forward-bound batch's Task 1 entry *explains* that adjustment and therefore **quotes the literal
`Last updated: ` inside its own prose**. The unanchored test stripped that occurrence, produced a
string present nowhere, and the occurrence check STOPPED the move. **The tool's own STOP is what
caught it — that STOP working, not failing.** Nothing was moved wrongly.

**The repair, and why it is a correction rather than a widening.** The test now fires only when the
entry OPENS with the prefix, which is what the constant's own comment already says it means
(*"The newest entry of a batch carries the `Last updated:` prefix"* — and a prefix is at the start).
An entry needing a second adjustment still reaches the occurrence check and still STOPS. The former
behaviour is recorded at the site (#12).

**Why the repair was taken rather than the move omitted.** The dispatch's exclusion names *the
forward-bound trigger* (OI-379) and this is a different defect; **D-669** permits a maintenance act
once the cause is established, which it was; **D-436** forbids removing a failing mechanism
automatically, and nothing was removed; and omitting the move would have reproduced exactly the
defect the previous batch was written to clear.

**★ WHAT IS OWED AND WAS NOT DONE, stated plainly rather than left implicit.** This defect deserves
its own register row under rule (c). **No row was created for it**, because the dispatch permits
exactly one row act and that act is OI-380. So it is recorded here and in the close, and **whether
it is rowed stands with the user** — beside OI-379, whose repair he has already reserved to himself.
Recording it in prose alone is a doc-sync exposure and is declared as one.

---

## 7. The guard set at the closing tree

*(Run and read at the tree carrying this close, never inferred; the values are recorded in
`tools/audit/guard_state.json` by the run itself, and were committed only after the run.)*

| | Start (Task 0) | End (this close) |
|---|---|---|
| Guards run | 77 | **80** |
| Failing | 13 | **13** |
| Not run | 4 | 4 |
| Historical records | 16 | 16 |

**The failing set is UNCHANGED, member for member: none added, none cleared.** The three guards
added are this batch's own three tools, each enrolled in the act that created it on the standing
new-tool rule, and **all three PASS**.

### One red appeared mid-batch, its cause established at the enumeration, and it was regenerated rather than left

The first closing run reported **fourteen** failing. The addition was
`gen_session_start_read_size.py --check`, and its cause was established rather than assumed:
`STATUS.md` is a member of the session-start read and this batch's close writes to it, so the
measurement was stale. **Regeneration is that check's own sanctioned remedy** — its guard-set entry
and its classification both say it goes red when a governing surface changes and that the remedy is
the same regeneration the other governing-surface-derived checks take. The dispatch orders exactly
that for the same cause, naming `CLAUDE.md`; the cause here is `STATUS.md`, the only member of the
read this batch touched, and it is regenerated and reported on that ground. **No measured value was
adjusted to reach a number** — the artifact was re-derived, and the guard set re-run afterwards to
take the end state above.

**Registered expectation E4 — GRADED PASS.** The end state was re-derived, run and read rather than
inferred, and every addition beyond A2's recorded start state is traced to this batch's own ordered
acts.

---

## 8. What this batch did NOT do

- **It changed not one rule `CLAUDE.md` states**, and did not edit `CLAUDE.md` or `CLAUDE_ARCHIVE.md`
  at all.
- **It did not touch the pinned first-wave decomposition or the first wave's tool.**
- **It pruned no governing document other than `CLAUDE.md`** — and in the event moved nothing there
  either.
- **It did not repair the forward-bound trigger** (OI-379), which stands with the user. The separate
  prefix-adjustment defect it did repair is declared at §6 above.
- No `src/` edit, no golden, no test changed, moved or run, no build, no measurement of the analysis,
  nothing under `tools/corpus/` or `tools/robust_stop/`, no design of the analysis, no derivation of
  any specification, no document archived or deleted as a file, no decisions-register entry and no
  `D-NNN` allocated, and no edit to `DECISIONS.md`, `ARCHITECTURE.md`, `FRAMEWORK.md`,
  `STATUS_ARCHIVE.md` (beyond the ruled forward-bound move) or any ruling record.
- **It opened no paper, no extract and no companion; it ran no sweep; it moved no verdict; and it
  lifted no gate.** The row-by-row extraction and the general pruning pass stay paused.

---

## 9. The two questions standing with the user

1. **Should a finer span unit be commissioned** — one separating a preserved wording from the live
   statement that announces it — so the pre-rule backlog becomes reachable at all? Without it, the
   material the order calls a mess cannot lawfully move, and this batch declined to invent one.
2. **Should the lint's enrolled scope widen beyond `CLAUDE.md`?** Its reach over the other four is
   published and shows zero breaches there today.

*(And, smaller: whether the prefix-adjustment defect of §6 should be rowed, and whether the two
unclassified guard-set members of §1 should be classified.)*
