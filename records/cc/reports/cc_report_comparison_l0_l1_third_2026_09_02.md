# CC report — the L0/L1 comparison, THIRD WRITING: the population completed, nothing decided

> **This report decides nothing.** It records what was done, what was measured, where the writing
> stopped and why. Every disposition it refers to is a proposal living in
> `ratification_surfaces/cowork_comparison_l0_l1_reading.md`; none is applied anywhere. Where this
> report would name a hash it cannot contain — its own commit, and the end-state commit that follows —
> it points at the git log instead.

**Dispatch:** `cc_instruction_comparison_l0_l1_third_2026_09_02.md`, pinned at Task 0 to blob
`6457ca79d6c7aed962d8408a6dbd1f2c1a4cd5ed` and proven unmoved before staging.
**Full close:** `cowork_away_returns.md`, the section **THE L0/L1 COMPARISON TABULATED (THIRD WRITING),
NOTHING DECIDED**, which carries the commit table, both guard states, E0–E2, A1–A5, the departures, the
finding and the plan lines. This report does not restate them; it says what the batch actually did.

---

## 1. What was done

**The population is complete.** The twenty-two documents at positions 8 to 29 were each **read whole**
and tabulated, **one commit per document**, in the artifact's own order. With the seven the previous
batch placed, **all 29 members of the ruled population are now tabulated** and none is partly worked.

Each document's section carries, as the second writing's rules require and unchanged from them: a
manifest header saying how many outgoing statements it holds and how many units are counted under *not
a statement*; the rows themselves, each with the outgoing statement quoted with its locator, the derived
statements that speak to it, the current-text verdict per named derived statement, and exactly one
proposed disposition; a *not a statement* table so the arithmetic closes at the document; the
distribution and the verdict tally; and a closing paragraph naming what that document puts in front of
the user.

**Four of the whole-population sections were closed** over all 29 documents — the transfer list, the
audit questions, the proposals-and-differences, and the distribution — each stating at its head that it
is complete.

**Three were not, and that is where the writing stopped.**

## 2. Where the writing stopped, and why

**Task 1(j)** orders §7 (one row per derived statement S-1 to S-54, naming every outgoing row that
speaks to it, with the two-way arithmetic check), §8 (the seventeen open questions) and §9 (the
derivation's own §7). **None is written**, each is marked NOT YET WRITTEN whole, and the reading file's
§0 records both the state and the reason.

**The precondition is met** — the population is complete, so the section is writable. **What stopped it
is §7's attribution.** The mechanical decomposition was established and is recorded in §0 so the next
writing does not repeat the derivation: §6 carries **1,305 verdicts** — **885 AGREES, 240 DIFFERS, 180
SILENT** — of which **89 sit on rows that name no derived statement at all**, leaving **1,216** to be
placed under an S-number. Placing them is read off §6's own axis lines, and **most attribute
mechanically**. A minority do not: an axis line continuing past a line break can carry the next
statement's verdict on the following line, and a few rows state **two verdict words for one named
statement** — Row 3.27's *"S-32: AGREES on the span half; THE DERIVATION IS SILENT on cost"* is the
worked case. Those need a per-row reading.

**No count was asserted.** §7's whole point is a check that the two sides reconcile; publishing one
built on an unresolved attribution would put a false arithmetic into a file whose only claim is that its
arithmetic closes. **The stop is at the population's own end** — the cleanest boundary this writing has,
since no document is partly worked and what is owed is three named sections, whole (**D-672**).

## 3. What was measured, not assumed

- **The declared start state held exactly.** A full guard run before the first edit: **76 run, 10
  failing, 4 not run, 16 historical, zero STOPs**, the ten failing being the ten the dispatch's own FACT
  names, member for member. Both A2-predicted-stale checks passed at boot.
- **A1 was checked over the whole tracked population** with the sanctioned enumeration tool: **exactly
  the one named exception**, the sitting record modified on disk additions-only below §3an by the
  writing side, not landed and not read beyond §3an.
- **A3 was measured before being accepted:** the membership artifact regenerated and compared against
  the committed blob — byte-identical, so it was **not** committed and Task 0 carried **two** paths.
- **The forward bound was measured, not assumed** — see §5.

## 4. What the tabulation put in front of the user

Named here in one line each, because they are the reason the tabulation exists; every one lives at its
row and none is decided.

- **A ratified amendment says the tie-resolved note model is lossy for a real consumer**, and requires
  the unresolved notated notes and the tie flags published additively — the third statement of that need
  in the outgoing record and the first that is ratified (Row 23.4).
- **A spelling destroyed by iteration order**, measured: two spellings of one pitch class collapsed to
  one (Row 29.7).
- **Three positions on capping a carried alternative list** — one caps it, one refuses to, one measures
  what a cap costs — and a ratified contract that rules **no truncation constant exists anywhere in the
  publication** (Rows 22.16, 24.1, 27.8).
- **A boundary failure a weight fit could only relocate**, measured at one named case where the analysis
  had the chord right and the span wrong (Row 29.10).
- **A spelling manufactured from a decision**, with the notated spelling named as what establishes it
  (Rows 25.13, 27.6).
- **The declared-mode tag consumed as evidence in a ratified architecture**, against the admission
  criterion's own worked counter-example — and, in another ratified contract, the same tag published as
  an input echo, labelled as read (Rows 21.14, 27.3).
- **Two published surfaces with opposite completeness contracts**, each stating its own (Row 28.12).
- **A third meeting with a passage the deriving session's own record says it SAW** — the pedal-point
  definition and its design-intent entry — named at its row and relayed to the independence section, so
  the user reads that agreement as the descended one it is (Row 25.14).

## 5. The one finding of this batch, about the apparatus

**Ruling 4's forward bound could not be maintained over the previous batch's `STATUS.md` entries.**
`gen_status_batch_bound.py` derives which entries to move from the dispatch name their own text carries;
**the previous batch's two entries name no dispatch**, established by searching the file for the string,
which does not occur in it. The ordered re-aiming was performed, the tool run, and it **STOPped with its
own message**; the aiming was then **reverted and the tool re-run to prove it green**, so no guard red
was introduced and the tool is byte-unchanged.

**The entries were not moved by hand** — that would build around a surprise rather than surface it
(#13) and would trade the tool's byte-proof for a hand copy — **and the previous batch's entry text was
not edited** to insert the missing name, which would rewrite another batch's record. **This batch's own
two entries name this dispatch**, restoring the convention so the next batch's move works.

**Consequence, declared:** `STATUS.md` carries two batches' entries, and the second writing's two have
no mechanism that can retire them. **Sorted by D-438's test it is apparatus**, so under R3 it is
reported and left; no open-items row was created.

## 6. What this batch did not do

No decision on any disposition, any difference, the derivation, the method, or the L0/L1 split. No
verdict on the deriving session's independence. No session booted. No measurement of the analysis built,
designed, scoped or run. No `src/` change, no golden, no test changed, moved or run, no build, nothing
under `tools/corpus/` or `tools/robust_stop/`. No open-items row created, flipped or discarded; no
register entry and no identifier allocated; no guard enrolment. No edit to the derivation, the brief,
the pack, its artifact, the population tool, the population artifact, any outgoing text, or any
governing document other than `STATUS.md`, which Task 2 orders. **`STATUS_ARCHIVE.md` is untouched**
because the move it would have received did not run.

**One correction was made inside an already-placed row and is declared:** Row 17.2's parenthetical
claimed to be the outgoing record's only worked use of the pedal-point sense; document 22 falsified it,
so it was corrected in place with the former wording quoted at the site and the falsifying row named
(#12). No verdict, disposition or count of that row changed.

## 7. Where the hashes are

The commit table — the tree met, Task 0, the twenty-two document commits, the population-complete commit
— is in the close section of `cowork_away_returns.md`, each by explicit hash, each pushed and each
verified equal at `.git/refs/remotes/origin/master` with the file tools. **This report's own commit and
the end-state commit that follows it are in the git log**, which is where a document cannot name its own
identifier.

*CC, 2026-09-02. This report recommends nothing and rules on nothing; the user is asked to rule nothing
in the reading file, which says so in its own closing section.*
