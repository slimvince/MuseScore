# CC dispatch — the L0/L1 COMPARISON, FOURTH WRITING: land the sitting record and the three entries, then write §7, §8 and §9 of the reading file over the complete population — proposals only, NO decision taken, NO session booted, the application act NOT in this batch

> **Dispatch (Cowork writing side, 2026-09-03, the session that booted on
> `cowork_handoff_entry_ninety_four.md`, its first dispatch).** Executes **Task 1(j) of
> `cc_instruction_comparison_l0_l1_third_2026_09_02.md`**, which the third writing did not reach, and
> through it **Rulings 32 and 33** (§3am and §3an of
> `cowork_rulings_2026_08_31_decision_surface_sitting.md`), Rulings 1 to 4 and §5 of
> `cowork_rulings_2026_08_24_comparison_design_sitting.md`, and the disposition discipline of
> `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §0 and §3.4. **Written at a
> verified stop: the third writing's batch returned with all twenty-nine documents tabulated and a
> recorded stop at the population's own end; its twenty-six commits are verified at the objects and
> pushed; nothing is running.**
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** It is the COMPLETION of the reading file the second and
> third writings built: the three whole-population sections that file marks NOT YET WRITTEN — **§7**
> (the derived side, one row per S-1 to S-54 with the two-way arithmetic), **§8** (the seventeen open
> questions) and **§9** (the derivation's own §7) — written from the mechanical decomposition the
> file's own §0 records, after the per-row reading §0 says a minority of axis lines needs. Task 0 lands
> the sitting record as it stands on disk, the ninety-second, ninety-third and ninety-fourth entries and
> this dispatch, and PUSHES. Task 1 writes the three sections. Task 2 closes. **THIS BATCH DECIDES
> NOTHING: every disposition in the reading file remains a PROPOSAL; nothing in §7, §8 or §9 is a
> verdict on the derivation. It BOOTS NO SESSION, BUILDS AND RUNS NO MEASUREMENT OF THE ANALYSIS,
> DERIVES NO SPECIFICATION STATEMENT, APPLIES NO RULING, and EDITS NOTHING BUT ITS OWN DELIVERABLES.**
> Your framing is NEUTRAL throughout: you tabulate; you neither refute nor confirm.
>
> **★ THE APPLICATION ACT IS NOT IN THIS BATCH, AND THE REASON IS SIZING, RULED BY THE ENTRY.** The
> ninety-fourth entry owes an application act — a dispatch that re-marks the ruled rows and writes the
> amendments of Rulings 34 to 62 into the derived specification — and states that it *"cannot run
> before §7–§9 of the reading file are written, because §7's two-way check is what the application act
> re-marks against"*, and that it follows in the same dispatch *"only if the sizing is safe, otherwise
> in the next."* §7 alone places, by §0's own figures, well over a thousand verdicts after a per-row
> reading; the application act reads twenty-nine rulings and re-marks every row they touch. Putting both
> in one batch is the shape D-672's stop rule exists for. **So this batch writes §7–§9 and stops; the
> application act is the next dispatch, written by the writing side after this batch's close is
> verified.** You do not begin it, do not prepare for it, and do not re-mark any row.
>
> **★ THE SITTING RECORD IS LANDED BY TASK 0 AS IT STANDS ON DISK AT THAT MOMENT, AND MAY BE MODIFIED
> AGAIN DURING YOUR RUN.** `cowork_rulings_2026_08_31_decision_surface_sitting.md` carries §3ao to
> §3bq — Rulings 34 to 62 — on disk and not at the tip; Task 0 lands it. The writing side may add to it
> again at any point during your run, below §3bq, additions only; **a modification AFTER Task 0's commit
> is NOT landed by this batch, is NOT read, and is NOT a STOP** — A1 below names it. **You do not read
> the sitting record beyond §3am and §3an for this batch's authority; you do not read Rulings 34 to 62
> at all.** They are the application act's inputs, not this batch's: nothing in them changes a row you
> write for §7, §8 or §9, because those sections are read off §6's rows as they stand, and a ruling on a
> family of rows is applied later by the separate act.
>
> **★ THE ORDERED FIRST READ COMES BEFORE YOUR SESSION-START READ, BY RULING, AND THE DEPARTURE IS OF
> RECORD (Ruling 2 of the comparison-design sitting, applied by Ruling 32 and unchanged here).** Your
> VERY FIRST read this session is `cowork_blind_derivation_l0_l1_2026_08_31.md` — **§5 first, then §6,
> then §7, then the whole file** — before `CLAUDE.md`, before anything. *Why:* §7, §8 and §9 are written
> FROM that file's own structure — its 54 statements, its 17 open questions and the five points of its
> §7 — and the ruling orders that you understand what the blind session claimed before the current
> text's frame exists to map it onto. **Your SECOND read is the reading file as it stands,
> `ratification_surfaces/cowork_comparison_l0_l1_reading.md`, WHOLE — §0 to §16, every row of every one
> of the 29 documents** — because §7 is §6 read from the other side and can be written only from every
> row of it, and because the vocabulary and the row shape are that file's own. Then: **(1)** `CLAUDE.md`,
> `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md` — CONDITIONAL, and this batch MEETS the
> condition (it runs the guard set): read it in full; rule (a)'s `gating_ids` at
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` (DO NOT read the whole
> open-items index). **(2)** `cowork_handoff.md`, ITS TOPMOST ENTRY, AND `cowork_handoff_entry_ninety_four.md`
> whole — the current entry point, untracked at the root — and `cowork_handoff_entry_ninety_two.md` and
> `cowork_handoff_entry_ninety_three.md` whole, which Task 0 lands. **(3)** `cowork_audit_protocol.md`'s
> dispatch-protocol section IN FULL — every `###` section carrying the standing-clause marker binds this
> batch; the marker is the population, and no number is written here. **(4)**
> `cowork_rulings_2026_08_31_decision_surface_sitting.md` §3am and §3an ONLY — this batch's authority.
> **(5)** `cowork_rulings_2026_08_24_comparison_design_sitting.md` whole. **(6)**
> `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §0 (the referents) and §3.4.
> **(7)** `cowork_blind_session_brief_l0_l1.md` §2 (the seven faces), §4 (the six-field form) and §7.
> **(8)** `cc_instruction_comparison_l0_l1_third_2026_09_02.md` Task 1 items (c), (d), (e), (f) and (j)
> — the orders this batch executes, repeated below so this instruction is self-sufficient; where the
> two texts differ, THIS dispatch governs and the difference is named in your report. **(9)**
> `cc_report_comparison_l0_l1_third_2026_09_02.md` whole and the close it points at in
> `cowork_away_returns.md` (*THE L0/L1 COMPARISON TABULATED (THIRD WRITING), NOTHING DECIDED* and its
> end-state subsection). **(10)** No outgoing text is opened by this batch: §7, §8 and §9 are written
> from the reading file's rows and the derivation, and an outgoing passage is cited only as a row of §6
> already cites it. **If writing §8 or §9 seems to need a passage of an outgoing text that no row of §6
> quotes, that is a STOP-and-report, not a reason to open the text** — the population was tabulated
> whole, and a passage missing from it is a finding about the tabulation.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The derivation* — the blind output: 54
> statements (S-1 to S-54, in the six-field form) and 17 open questions (OQ-1 to OQ-17), written by an
> implementation-blind session that read the brief, the ten-member boot pack, four staged scores and
> fetched research, and nothing else of this project — **relayed from its own §5; the reading file
> re-counted them at the file, and you re-count them yourself; your counts are the figures of record for
> this batch.** *The outgoing text* — the current specifications describing what the new L1 charter now
> covers; under the phase definition it is a WITNESS read after the derivation, evidence about the
> present text and never authority over a derived statement. *The reading file* — your deliverable,
> `ratification_surfaces/cowork_comparison_l0_l1_reading.md`, which EXISTS and whose §6 is complete over
> 29 documents. *A row of §6* — one outgoing statement with its location, the derived statement or
> statements it names, its current-text axis (AGREES / DIFFERS / THE DERIVATION IS SILENT, one per named
> derived statement) and its proposed disposition. *An axis line* — the line or lines of a §6 row that
> carry its current-text verdicts; most read `S-<n>: **VERDICT**` and attribute mechanically; a minority
> do not. *The two-way check* — the arithmetic that every AGREES and every DIFFERS recorded in §6 appears
> in §7 against the derived statement it names, and the reverse. *The application act* — the dispatch
> the ninety-fourth entry owes and this batch does NOT perform.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, **no measurement of the ANALYSIS built,
> designed, scoped or run** (the comparison is TEXTUAL), no design, no repair, **no derivation of any
> specification statement**, NO SESSION BOOTED, no document archived, moved or deleted AS A FILE, **no
> open-items row created, flipped or discarded**, **no edit to any governing document, to the
> derivation, to the brief, to the pack or its generator, to any register entry or register source, to
> any outgoing text, to the sitting record**, **no disposition APPLIED anywhere, no ruling of 34 to 62
> applied anywhere, no row of §6 revised** — §7, §8 and §9 are ADDED; a placed row is never touched —
> **no verdict word attached to the derivation's independence record** (§14 stands as written),
> **no recommendation about the derivation, the method or the split of L0 from L1 anywhere** — not in
> the reading file, not in the report, not in a commit message.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (CHECK mode) and again at
> the end (write mode, committed as the artifact of a real run). Verify every commit at the object by
> explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and `git diff` over the
> working tree time out on this mount and are denied by the armed guard — use the per-path
> explicit-hash forms named in A1. If staging is refused by a stale zero-length `index.lock` with no
> git process running, establish the cause (D-669), remove it, report the act. Plain single-quoted
> `git commit -m '…'` subjects. The reserved-word and vocabulary rules bind every line — *score* only
> the musical score, *key* only tonality, *bar* only the metric unit (the prohibiting sense is
> *exclude*), no numeric grade anywhere: the vocabulary of the reading file's §5 is the whole of the
> verdict language.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 33, §3an (the population):** *"a term-search hit enters the outgoing population only if its
  file is a member of the specification document set. The population is therefore Ruling 32's eleven
  named members plus every hit file in that set."* The reading file's §0 records all 29 tabulated.
- **Ruling 3 of the comparison-design sitting (the vocabulary's form):** closed, two axes, words not
  numbers, fixed before grading; every row carries both texts' own words; *"a verdict the comparer
  cannot defend in one sentence at the texts is delivered UNGRADED, with what was read."*
- **Ruling 4 of the same sitting:** *"The comparison tabulates; the method ruling comes to the user
  afterwards as a Cowork-written decision surface over the tabulation."*
- **The phase definition §0, the disposition discipline:** *"every statement of an outgoing text reaches
  ONE recorded disposition — adopted / relocated (to the cross-layer transfer list) / quarantined (as an
  audit question) / discarded (under the worth test, with finding, date, reason) / historical — with
  completeness checked by arithmetic."* §3.4: *"a silent rewrite where a proposal per difference is
  owed; a disposition class invented beyond the five"* — both NOT ALLOWED.
- **D-672 (the standing clause the second and third writings stopped under):** *"A per-entry pass may
  be stopped at any member boundary, and the stop is RECORDED — what was done, what was not, and that
  the remainder is untouched rather than partly worked."* This batch resumes at the recorded boundary:
  three sections owed, whole.
- **The ninety-fourth entry, on the application act:** *"It cannot run before §7–§9 of the reading file
  are written (Task 1(j)), because §7's two-way check is what the application act re-marks against — so
  the next dispatch's order is: land the records → Task 1(j) → the application act, or Task 1(j) in one
  dispatch and the application act in the next."* This dispatch takes the second form.
- **The ninety-second entry's landing rule, restated by the ninety-fourth:** the sitting record and the
  ninety-second, ninety-third and ninety-fourth entries *"are landed by the next dispatch."* This is
  that dispatch.

## Premise ledger

- **FACT** — the local branch tip is `93ab4e340387baebbff310d1fc4449b1f6762e55` (parent
  `24f6d0e09f33560b56a207a71454c78c730c01bb`, the third writing's close), the third writing's end-state
  commit; `origin/master` is the SAME hash. Read by the writing side at the two ref files with the file
  tools, 2026-09-03. **Nothing is unpushed. Re-read the refs yourself before Task 0.**
- **FACT** — at the tip, `tools/audit/guard_state.json` → `summary` records **run 76**, passing 66,
  **failing 10**, not_run 4, historical 16, and `unclassified_candidates` EMPTY — and it was WRITTEN BY
  THAT COMMIT (its path list is `cowork_away_returns.md` and `tools/audit/guard_state.json`, two paths).
  The ten failing tools: `gen_filing_convention_application.py --check`,
  `gen_artifact_inventory.py --check`, `gen_artifact_inventory_surface.py --check`,
  `gen_test_construction_evidence.py --check`, `gen_retirement_caller_check.py --check`,
  `decisions/apply_soft_discard.py --check`, `decisions/apply_residue_discard.py --check`,
  `gen_epoch_write_path.py --check`, `gen_recognizer_establishment_sort.py --check`,
  `decisions/gen_cluster_dispositions.py --verify`. Read by the writing side at the object
  `93ab4e34…:tools/audit/guard_state.json` by explicit hash. **Re-read the artifact yourself at that
  object.**
- **FACT** — the reading file `ratification_surfaces/cowork_comparison_l0_l1_reading.md` is TRACKED at
  the tip (blob `ef9c002f41cc294b4602c7bdf71b0fb91e9afc0f`) and IDENTICAL on disk to that blob (sha1 of
  the staged copy, 2026-09-03). Its §0 records all 29 documents DONE; §10 to §13 complete over the
  population; §7, §8 and §9 marked NOT YET WRITTEN, whole. **Its §0 records, as the population's
  mechanical facts, the figures the next writing starts from — the number of verdicts §6 carries, their
  split by verdict word, the number of rows naming no derived statement, and the number of verdicts
  attributable to a named derived statement — and names Row 3.27 as the worked case of an axis line
  carrying two verdict words for one named statement. Those figures are the file's own; none is restated
  here (D-431). Read them at §0 and at §7's own marker.**
- **FACT** — the derivation `cowork_blind_derivation_l0_l1_2026_08_31.md` is TRACKED at the tip (blob
  `9b102c575643d672e4747b4cfc23e377cd344c27`) and IDENTICAL in size on disk; the population artifact
  `tools/audit/l0_l1_outgoing_population.json` is at blob `72fc2937…`; `cowork_handoff.md` (blob
  `e6457cf3…`) and `STATUS.md` (blob `fe6de276…`) are IDENTICAL on disk to their blobs (sha1 of the
  staged copies). `tools/audit/gen_status_batch_bound.py` is at blob `a57d562e…`, byte-unchanged by the
  third writing as its close records.
- **FACT** — at the tip: `cowork_rulings_2026_08_31_decision_surface_sitting.md` is TRACKED at blob
  `31163be6a51525c9973e0142f7c21e3be2e6bd5c` (249,226 bytes) and is MODIFIED on disk (424,754 bytes at
  the writing side's last read, 2026-09-03), carrying §3ao to §3bq — Rulings 34 to 62. **The shape of
  the modification was MEASURED by the writing side at
  `git diff --numstat 93ab4e34… -- <path>`: 1,911 lines inserted, ZERO deleted, in exactly TWO hunks
  — one opening at the blob's line 3039, immediately after §3an's last line and before the old §4,
  carrying the §3ao–§3bq block; and one appended after the blob's line 3146, carrying the per-ruling
  provenance paragraphs at the file's tail.** The headings §3ao through §3bq were confirmed present at
  the staged copy. The file is NOT a byte-prefix append of the blob (the insertions sit inside it), so
  a prefix test is the wrong check; the numstat and the hunk headers are the check. `cowork_handoff_entry_ninety_two.md`, `cowork_handoff_entry_ninety_three.md`,
  `cowork_handoff_entry_ninety_four.md` and this dispatch
  (`cc_instruction_comparison_l0_l1_fourth_2026_09_03.md`) are ABSENT from the tip (checked at
  `git ls-tree` of the tip by explicit hash) and PRESENT on disk at the root.
- **FACT** — the third writing's close records that `STATUS.md` carries TWO batches' entries: the third
  writing's two, which NAME their dispatch, and the second writing's two, which name none and which
  `gen_status_batch_bound.py` cannot identify (the finding is at that close, *RULING 4's FORWARD BOUND
  CANNOT BE MAINTAINED OVER THE PREVIOUS BATCH'S ENTRIES*, classed apparatus, no row). This batch's
  Task 2 moves the THIRD writing's entries by the ordered mechanism, and leaves the second writing's
  where they are — see Task 2.

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications at your boot:
  **exactly ONE** — `cowork_rulings_2026_08_31_decision_surface_sitting.md`, additions only below §3an,
  which Task 0 LANDS. **After Task 0's commit**, that file MAY be modified again on disk at any time by
  the writing side, additions only, below §3bq — **not landed by this batch, not read, its modification
  reported in the close and graded as the cadence working, not a STOP.** A modification at any OTHER
  tracked path, at boot or during the run, is a STOP-and-report. Untracked and to be landed by Task 0:
  `cowork_handoff_entry_ninety_two.md`, `cowork_handoff_entry_ninety_three.md`,
  `cowork_handoff_entry_ninety_four.md` and this dispatch — **four untracked paths.** A further
  untracked `cowork_handoff_entry_*.md` or `cowork_rulings_*.md` appearing at the root during the run
  is the writing side's and is NOT landed and NOT a STOP. The remainder of the untracked population is
  outside A1's STOP. **Check ordered (Task 0, first act — AFTER the ordered first reads):** per named
  tracked path `git diff --stat 93ab4e340387baebbff310d1fc4449b1f6762e55 -- <path>; echo "exit:$?"`
  with any changed passages written to a scratch path outside the repository and read with the file
  tools — **for the sitting record, run `git diff --numstat 93ab4e34… -- <path>` and read the hunk
  headers: ZERO deletions is required, and every hunk must open either at the blob's line 3039 (the
  §3ao–§3bq block, which may have grown if the writing side added a ruling before your boot) or at or
  after the blob's line 3146 (the provenance tail). A deletion, or an insertion anywhere else, is a
  STOP-and-report, and the file is then NOT landed**; per named untracked path
  `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`; and the whole tracked
  population enumerated with the sanctioned enumeration tool, `tools/audit/changed_paths.py`.

- **ASSUMPTION A2 — the reds at the CHECK-mode run before your first edit.** The ten failing tools
  named above, exactly, and zero STOPs. The reds this batch's own inputs may cause:
  `gen_evidence_pin_membership.py --check` — predict PASS at boot (the third writing's end state
  measured it passing with the modified sitting record already on disk); if the writing side modifies
  the record again mid-run and the check goes STALE, that is the named exception's own consequence —
  report it, do not repair it, not a STOP. `gen_l0_l1_outgoing_population.py --check` STALE if
  `STATUS.md`'s recorded-tier hits move under Task 2's forward-bound move — the second writing's end
  state recorded exactly this, a recorded-tier `tie` hit inside the word *TIERS* leaving the file with
  the moved entry — **if it goes STALE at the end state, regenerate it, measure the difference as the
  second writing did, commit it with the end state, and report that no member of the population moved;
  a member moving is a STOP-and-report.** `gen_status_batch_bound.py --check` — see Task 2 for the two
  outcomes it may have and what each orders. A red outside that set is classed by its measured cause;
  one outside this batch's own acts' subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration.** `ruling_records_read` moves by ZERO at Task 0 —
  landing the sitting record changes nothing the derivation reads that was not already on disk when
  the third writing's end state measured it passing. Measure before accepting; movement beyond is a
  STOP-and-report; a mid-run movement caused by the writing side's further record edits is reported
  under A1's exception.

- **ASSUMPTION A4 — the guard registry.** No tool added; population unchanged at **76**; failing set
  the ten named, less any a Task-2 ordered act clears, plus none; zero STOPs throughout.

- **ASSUMPTION A5 — byte-unchanged by this batch, verified at the objects after the last commit:** the
  derivation (blob `9b102c57…`), the brief, the pack directory and `derivation_boot_pack.json`, every
  outgoing text in the population, `tools/audit/gen_l0_l1_outgoing_population.py`, the population
  artifact EXCEPT the A2 regeneration case, the sitting record AFTER Task 0 lands it (the blob Task 0
  commits is the blob at the end state, whatever the disk then holds), and every governing document
  EXCEPT `STATUS.md` and `STATUS_ARCHIVE.md` (Task 2's own orders). **Every row of §6, every section
  §0 to §6 and §10 to §16 of the reading file is byte-unchanged by this batch EXCEPT the four places
  Task 1 names** — the banner's incompleteness notice, §0's stop statement, and the three NOT YET
  WRITTEN markers, each replaced by the section it stands for. Any other difference is a
  STOP-and-report.

## The declared start state — at the tree you will meet

1. The ten failing tools named in the FACT above — standing reds, none of them this batch's.
2. Zero STOPs; `gen_evidence_pin_membership.py --check`, `gen_l0_l1_outgoing_population.py --check`
   and `gen_status_batch_bound.py --check` PASS predicted, **measured** (A2).

A failing verdict outside 1–2 is a STOP-and-report, save the A2 exceptions named.

## Findings in this batch

**No finding number is allocated by this dispatch; the series stands where the topmost entry of
`cowork_handoff.md` and the ninety-fourth entry file say it stands — read both.** Ruling 9's routing
binds. **What you find IN the derivation or IN the reading file's rows is not a finding of this batch —
it is the tabulation's content, and §7's per-row resolution table (Task 1) is where an axis line that
resists mechanical attribution is recorded.** A defect in the comparison APPARATUS (a §6 row whose axis
line cannot be read as any verdict at all; a derived statement the derivation numbers that no row and
no silence can be stated for; a row of §6 whose vocabulary is outside §5's closed set) is a
STOP-and-report, not a thing to fix — **the row is left as it stands and named in §7's resolution table
as UNRESOLVED with what was read, and the arithmetic is stated WITH that row excluded and the exclusion
named.**

## Task 0 — land the sitting record, the three entries and this dispatch, and PUSH

1. **A1's check** (after the ordered first reads), including the additions-only confirmation on the
   sitting record.
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob at the tip BEFORE
   accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_rulings_2026_08_31_decision_surface_sitting.md`
   (modified), `cowork_handoff_entry_ninety_two.md` (new), `cowork_handoff_entry_ninety_three.md` (new),
   `cowork_handoff_entry_ninety_four.md` (new), `cc_instruction_comparison_l0_l1_fourth_2026_09_03.md`
   (new), and `tools/audit/evidence_pin_membership.json` (regenerated, only if it moved) — **five or six
   paths.** Subject, exactly:
   `record: the sitting record with Rulings 34-62, the ninety-second, ninety-third and ninety-fourth entries and the fourth comparison dispatch landed; the whole-population sections are next`.
4. **PUSH**; verify at the ref file that `origin/master` equals the new tip.

**Under standing clause P-2 this dispatch is PINNED at Task 0:** record its blob, and prove the blob
unmoved immediately before staging it. *(P-2 and P-3 are the ratified proposals of the framework phase
retrospective — `cowork_rulings_2026_08_29_ratification_sitting.md`, the ruling that ratified
`cowork_framework_phase_retrospective_2026_08_29.md`: P-2 *pin-to-blob as the standing Task 0 form, the
pin ordered in the user's opening line, the declared-departure fallback kept*; P-3 *footprint
assumptions as act-lists, third-party movement graded*. Their enrolment with the standing-clause marker
in `cowork_audit_protocol.md` is on the user's owed list and has not happened; they bind by the
ratification, and this dispatch cites them by that home so that their absence from the marker
population is not read as their absence from the record.)*

**E0:** the named paths in one commit; `origin/master` at the commit; A3 measured; A1's whole-tree
check reported with its enumeration and the additions-only confirmation stated.

## Task 1 — §8 and §9, then §7, of the reading file: proposals only

**The unit and the vocabulary are the reading file's own and are not restated here** (#6): the rows of
§6 are the population §7, §8 and §9 are written from; the verdict words are §5's closed set; a derived
statement is named by its S-number and quoted by its first field; an open question by its OQ-number.

**(a) The order, and why.** **§8 first, then §9, each in ITS OWN commit; then §7 in ONE commit.** §8
and §9 are small and each is one member; §7 is the large one and is the section the application act
re-marks against. Landing §8 and §9 first means a stop before §7 leaves two whole sections done rather
than none. **A section is written WHOLE or not at all** — §7 with 54 rows or with none; a partial
section is never committed, and D-672's stop is at the section boundary.

**(b) §8 — the open questions OQ-1 to OQ-17.** Listed in the derivation's order, each ungraded, each
with **one sentence on what the outgoing text says about it, with location** — the location being a
ROW of §6 (document and row number), which carries the outgoing passage's own quotation and heading —
**or that the outgoing text says nothing**. Where a §6 row's disposition or audit question already
speaks to the open question, cite that row. **No answer to any open question is given or implied**;
where the derivation's own text or the reading file's §14 records the deriving session having later
answered one, say so as a fact with its location, and nothing more. Subject:
`comparison L0/L1: section 8 written — the seventeen open questions against the outgoing text, ungraded`.
Push; verify at the ref file.

**(c) §9 — the derivation's §7, its five points.** Each with one sentence on what the outgoing text
says about it, with location as a §6 row, or that it says nothing. **The two structural points —
notated versus unfolded order as its own face, and marks that fall where no change point is — are
named as such, as §3al.4 of the sitting record records them** (read §3al.4 for those two phrases and
nothing else of that section; the authority window (4) is widened by exactly that subsection). **No verdict on whether §7 is right.** Subject:
`comparison L0/L1: section 9 written — the derivation's own five points against the outgoing text, no verdict`.
Push; verify at the ref file.

**(d) §7 — the derived side, one row per S-1 to S-54, with the two-way check.** Three components, in this
order, all in the one section and the one commit.

**(d1) The per-row resolution table, BEFORE any row of §7 is written.** §0 states that most axis lines
attribute mechanically and that a minority do not — an axis line continuing past a line break onto a
second statement's verdict, and a row stating two verdict words for ONE named statement (Row 3.27,
*"S-32: **AGREES** on the span half; **THE DERIVATION IS SILENT** on cost"*). **Enumerate that
minority mechanically first** — every axis line that is not exactly of the form one S-number, one
verdict word — **and resolve each at its row, one at a time, recording in a table at the head of §7:
the row; the axis line quoted; what was read; and how it is carried into §7.** The carrying rule is
fixed here so the batch takes no decision: **a verdict word is carried as itself, with the row's own
qualifier attached** — Row 3.27 places one AGREES and one THE DERIVATION IS SILENT against S-32, each
carrying its qualifier (*on the span half*; *on cost*) — **nothing is merged, nothing is dropped, no
qualifier is stripped (#12)**, and a line that continues onto a second statement's verdict is read as
two attributions, one per statement. A row whose axis line cannot be read as any verdict at all is
recorded UNRESOLVED with what was read and is EXCLUDED from the arithmetic by name — the Findings
clause above. **The table states, at its foot, how many rows it holds and how many verdict
attributions the minority yields**, derived at the rows.

**(d2) The rows.** One row per derived statement, S-1 to S-54 in the derivation's order: the
statement's own words (its first field, quoted), its source class (its own field), and **every
outgoing statement of every document that names it** (by document and row number) with the AGREES /
DIFFERS / THE DERIVATION IS SILENT verdict that row carries against it — the qualifier attached where
(d1) attached one — or **THE OUTGOING TEXT IS SILENT** where no row names it. A row of §7 states its own
count of attributions by verdict word, at the row.

**(d3) The two-way check, stated as arithmetic, at the foot of §7.** **Direction one:** every AGREES
and every DIFFERS in §6 appears in §7 against the statement it names, and every SILENT that names an
S-number likewise; the count of attributions summed over the 54 rows of §7 equals the count of
attributions summed over the rows of §6, per verdict word, **both sums derived at the rows by this
batch and stated as sums**. **Direction two:** every attribution in §7 is found at the §6 row it cites.
**Beside the derived sums, §0's recorded mechanical figures are quoted as what they were, and any
difference between §0's figure and the derived sum is stated with its cause in one sentence** — a
per-row resolution moving a count is the expected cause, and is not a defect; a difference with no
cause found is a STOP-and-report, the section still committed with the discrepancy stated, never
silently reconciled. **The count of §6 rows naming no derived statement is derived again and stated,
so a reader can see that every row of §6 is either attributed in §7 or listed as naming no statement.**
Subject:
`comparison L0/L1: section 7 written — the derived side, one row per S-1 to S-54, the minority of axis lines resolved at the row, the two-way check stated as arithmetic, proposals only`.
Push; verify at the ref file.

**(e) The banner and §0.** In the §7 commit (or, if §7 is not reached, in the last section commit
taken): replace the banner's incompleteness notice and §0's stop statement so the file states its own
state exactly — **COMPLETE over the whole population** if §7, §8 and §9 are all written; otherwise
which sections are written and which are not, whole, and that the next dispatch resumes at the
unwritten one. **The §0 mechanical figures are NOT deleted** — they are marked as the figures the
writing started from, with §7's derived sums now the figures of record (#12).

**(f) What §7, §8 and §9 do NOT contain.** No recommendation. No verdict on the derivation, on any
open question, on §7's five points, or on the deriving session's independence. No reference to Rulings
34 to 62 — they are not read. No count carried from §0 or from any earlier close as a figure of record:
every figure in these sections is derived at the rows by this batch.

**(g) The stop rule (D-672).** **If your context is degrading — the tells: citing a summary instead of
the source, a count never derived, a defined term glossed — STOP AT THE NEXT SECTION BOUNDARY**, commit
what is whole, update the banner and §0 under (e), and record in your report which sections are
written, which are not, and that the remainder is UNTOUCHED. **A partly written section is never
committed.**

**E1:** §8 with seventeen entries each carrying a location as a §6 row or *says nothing*; §9 with five
entries, the two structural points named; §7 with its resolution table at the head (every non-mechanical
axis line enumerated and resolved or recorded UNRESOLVED), 54 rows, and the two-way check stated as
derived sums in both directions with §0's figures quoted beside and every difference explained; the
banner and §0 stating the file's own state; every row of §6 byte-unchanged; no recommendation anywhere.
**If a section is not reached, its NOT YET WRITTEN marker stands as it was.**

## Task 2 — the close

1. **The `STATUS.md` entries and the forward-bound move, IN THIS ORDER — the order is the tool's own
   mechanism, read by the writing side at the object `93ab4e34…:tools/audit/gen_status_batch_bound.py`.**
   **(a) FIRST write this batch's own entries** at the top of `STATUS.md`: one pointer entry per task that
   did work (no distribution restated — the pointer names the reading file), **the close entry FIRST and
   NAMING THIS DISPATCH BY ITS FILE NAME, and each earlier task's entry below it saying `Same dispatch`**
   — the convention the tool derives membership from (its `moved_entries()`: the entry naming the
   then-previous dispatch, plus the run of dated entries immediately below it whose text says `Same
   dispatch`, stopping at the first that says neither). **The `Last updated: ` prefix moves from the
   third writing's close entry to your newest entry** — the tool's one declared textual adjustment
   (`PREFIX_ADJUSTMENT`, imported from `gen_governing_surface_split.py`) expects exactly that: it strips
   the prefix from the base commit's text and requires the stripped text to occur exactly once in the
   live file, so **if the prefix is still on the third writing's entry when the tool runs, the occurrence
   test fails and the tool STOPs** — that STOP would be this batch's own ordering error, not a finding.
   **(b) THEN re-aim `gen_status_batch_bound.py` — ALL of its authored constants, which are FIVE, named
   here so none is missed as the first writing missed two:** `BASE_COMMIT` (the last Task-1 commit of
   this batch — the tool's docstring: *the last task commit of the executing dispatch*),
   `PREVIOUS_BATCH_DISPATCH` (`cc_instruction_comparison_l0_l1_third_2026_09_02.md`), `ACT_DATE` (the
   day the move RUNS, per the tool's own comment — not this dispatch's date if they differ),
   `DISPATCH` (this dispatch's file name) and `TASK` (`Task 2`); **and append to `PREVIOUS_AIMINGS`, kept
   rather than replaced (#12), the aiming being superseded** — the second writing's
   (`cc_instruction_comparison_l0_l1_second_2026_09_02.md, Task 3`, base `2acef005…`, then-previous the
   first dispatch) — **and one further row recording that the third writing performed NO move**, with the
   tool's own STOP message quoted from the third close and the row marked as the backfilled 2026-08-26
   row is marked, so the record of aimings does not silently omit a batch (#10, #12). **(c) THEN run
   `gen_status_batch_bound.py --apply`** (D-648, licensed in terms). **The outcome is PREDICTED from the
   tool's derivation read at the object, and is measured:** the third writing's close entry names the
   third dispatch and the entry below it says `Same dispatch`; the second writing's first entry below
   those names no dispatch and does not say `Same dispatch`, so the run STOPS THERE by the tool's own
   loop — **prediction: exactly the third writing's TWO entries move, the second writing's two stay in
   `STATUS.md`, no STOP.** If the tool nonetheless STOPs, restore it to its passing aim, re-run it to
   prove it green (the third writing's own act), report the STOP with the tool's message under the
   standing finding, **and move nothing by hand and edit no earlier entry.** The second writing's two
   entries remain in `STATUS.md` in either outcome, as the third close's finding records; **that is a
   declared state, not a STOP, and no row is created for it.** **(d)** `gen_session_start_read_size.py`
   regenerated. The re-aimed tool, `tools/audit/status_batch_bound.json`, `STATUS.md` and
   `STATUS_ARCHIVE.md` are committed together in the close.
2. The FULL close appended to `cowork_away_returns.md` as a **THE L0/L1 COMPARISON TABULATED (FOURTH
   WRITING), THE WHOLE-POPULATION SECTIONS WRITTEN, NOTHING DECIDED** section (or, if a section was not
   reached, *… §8 AND §9 WRITTEN, §7 NOT REACHED, NOTHING DECIDED* — the heading states what was done)
   — both guard states, every SHA, E0–E2 graded, A1–A5 graded, declared departures (the sitting record's
   post-Task-0 modification, if any, reported here under A1's exception), the per-row resolution
   table's foot restated as a POINTER to §7 (no count restated), the plan lines (**the application act
   next, written by the writing side over §7's two-way check and the sitting record's §3ao–§3bq; the
   L0/L1 split at ratification; the pack finding of §3al.4 to L2's pack; then L2, sequenced by Ruling
   10**), and **the plan's tell in one sentence: did this batch produce anything other than the landed
   records, the reading file's three sections and the report? If yes, name it.** The report is
   `cc_report_comparison_l0_l1_fourth_2026_09_03.md` — which likewise decides nothing; where the report
   would name hashes it cannot contain, point at the git log.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **The ordered structure
   yields: Task 0, the §8 commit, the §9 commit, the §7 commit (each if reached), the close, the end
   state.**

**E2 — DERIVED from the declared start state plus this batch's footprint (P-3):** at the tree carrying
the close, a fresh full guard run — **population 76**; **zero STOPs**; **the failing set exactly the
ten named, LESS any this batch's ordered acts clear, PLUS NONE**, the A2 exceptions handled as A2 says
— committed only after the run that produced it.

## What this batch does NOT do

- **No decision on any disposition, any difference, any open question, the derivation, the method, or
  the L0/L1 split. No verdict on the deriving session's independence. No session booted. No measurement
  built or run. No row of §6 revised. No application of any ruling of 34 to 62; none of them read.**
- **No edit to the derivation, the brief, the pack, its generator, its artifact (save A2's
  regeneration), the population tool, any outgoing text, any governing document other than the two
  Task 2 orders, any register source, or the sitting record beyond LANDING it as it stands at Task 0.**
  The reading file's three sections, its banner and §0 statement, and the report are the whole
  authored write set.
- **No finding number. No open-items row created, flipped or discarded EXCEPT an apparatus-finding row
  under rule (c). No `src/` change, no golden, no test, nothing under `tools/corpus/` or
  `tools/robust_stop/`. No guard enrolment. No `tools/audit/*.py` written or edited except the
  re-aiming of `gen_status_batch_bound.py`'s five authored constants and its `PREVIOUS_AIMINGS` list,
  which Task 2 licenses and D-648 classes as authored-input maintenance.**
- **No disposition of any residue file.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #19 — the reading file establishes nothing; every figure in §7, §8 and §9 is
   derived at the rows by the batch and none carried from §0 or a close; #6 — the unit, the vocabulary
   and the row shape are the reading file's own and are pointed at, not restated; the carrying rule of
   (d1) is fixed here so one comparison has one attribution rule; #12 — no verdict word is merged,
   dropped or stripped of its qualifier at the resolution table, §0's figures are kept and marked
   rather than deleted, and every row of §6 is untouched; #13 — the two outcomes of the forward-bound
   move are named in advance so a STOP there is not a surprise, and a passage §8 or §9 seems to need
   that no §6 row quotes is a STOP rather than a reason to open a text; #17(f)/D-431 — no count of the
   reading file is restated in this dispatch; #24 — the two axes are kept apart, and a difference
   between a derived sum and §0's figure is stated with its cause rather than reconciled. Conforms.
2. *Conventions:* American English; no self-invented labels — *axis line*, *the two-way check* and
   *the per-row resolution table* are defined at the head or at (d1) before use, and *family* keeps the
   reading file's meaning; music-theory words in their musical sense; *exclude* for the prohibiting
   sense; towards, never against.
3. *Figures and premises:* the tip and its parent read at the ref files; the guard summary read at the
   object the end-state commit itself wrote and its path list checked by `git show --stat` at the
   explicit hash; the reading file's, `STATUS.md`'s and `cowork_handoff.md`'s on-disk identity with
   their blobs established by sha1 of the bridge-staged copies against `git rev-parse <tip>:<path>`;
   the sitting record's blob size and on-disk size read at the object and at the folder listing, its
   §3ao–§3bq headings confirmed at the staged copy, and the shape of its modification measured at
   `git diff --numstat <tip> -- <path>` with the hunk headers read from the same per-path
   explicit-hash diff (the A1 form the third dispatch ordered, run by the writing side on the user's
   machine); `gen_status_batch_bound.py`'s five authored constants, its membership derivation and its
   prefix adjustment read at the object, and the imported `ENTRY` pattern and `PREFIX_ADJUSTMENT`
   constant read at their own objects; P-2 and P-3 read at the ratification sitting's object; every
   tool and document this dispatch names checked present at the tip by `git cat-file -e`; the
   derivation's 54 statements and 17 open questions re-counted at the staged copy; Row 3.27's axis
   line and §3al.4's two phrases read at the files; the three entries and this dispatch confirmed absent
   from the tip at `git ls-tree` by explicit hash; the reading file's §0 and its three markers read at
   the file. **One declared departure of this writing side:** the guard artifact, the forward-bound
   tool and the 2026-08-29 ratification sitting were dumped at the object by `git show <hash>:path` to
   a scratch path OUTSIDE the repository and read there with `sed`, `grep` and an interpreter — a
   D-253-permitted object query followed by shell reads of non-working-tree copies — declared rather
   than assumed covered, as the ninety-fourth entry declared its range query. No working-tree file was
   read by any shell; the per-path `git diff --numstat` and its hunk headers are the A1 form the third
   dispatch itself orders.
4. *File-tools rule:* every repository read through the file tools on bridge-staged snapshots; git
   object queries by explicit hash only; no `git status`, no working-tree diff, no `git log`.
5. *Uncertainty:* whether §7 is reached is unknown and the stop rule covers it; whether the forward-bound
   tool STOPs on the second writing's nameless entries is unknown and both outcomes are ordered; the A2
   exceptions are named with their measured precedent. The sizing judgment that puts the application
   act in the next dispatch is the writing side's, taken under the ninety-fourth entry's own
   *"otherwise in the next"* clause, and is stated here so the user can overrule it before Task 0.
6. *Re-read from disk before release:* to be performed over the committed file after it is written to
   the root, hash-compared with the written copy, recorded in the ninety-fifth entry.

---

*Provenance: Cowork writing side, 2026-09-03, the session that booted on
`cowork_handoff_entry_ninety_four.md`, at tip `93ab4e34…` with `origin/master` equal, under Rulings 32
and 33 of `cowork_rulings_2026_08_31_decision_surface_sitting.md` and Task 1(j) of the third comparison
dispatch, applying Rulings 1–4 and §5 of `cowork_rulings_2026_08_24_comparison_design_sitting.md` and
the disposition discipline of the phase definition. TOWARDS the ultimate objective and TOWARDS the
guiding principles.*
