# CC dispatch — the preparation phase's ELEVENTH batch: the evidence pin's unresolved members closed at the records, the stale ruling-surface banners corrected at their generators, and the session-start-read figure corrected of record

> **Dispatch (Cowork, 2026-08-18).** Executes the three rulings and the one sub-ruling of
> `cowork_rulings_2026_08_18_tenth_return.md`. **Written at a verified stop; nothing else is running
> and no dispatch was active when this was written.** The curated boot list, the empirical findings
> ledger, the fact-gate, the mining, the landing of the newly visible instruction files, the
> whole-document archiving wave, and every act on any candidacy are NOT in this batch.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional, and explicitly NOT demoted. **(3)** `BUILD_AND_TEST.md` is a
> CONDITIONAL read — **and this batch MEETS the condition**, because it runs the guard set, whose
> commands live there, so read it in full. **(4) DO NOT READ THE WHOLE OPEN-ITEMS INDEX.** Rule (a)
> now reads the DERIVED GATING ANSWER narrowed to its identity list —
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids` — and the
> grounds stand at `the_gating_rows` in the same file and are opened when a verdict is CHALLENGED.
> The INDEX remains the AUTHORITATIVE STATUS SURFACE; you open it when you need a row.
>
> **★ AND THE STANDING CLAUSE YOUR OWN LAST BATCH LANDED BINDS THIS ONE: read
> `cowork_handoff.md`, ITS CURRENT (TWENTY-NINTH) BLOCK.** The clause lives at
> `cowork_audit_protocol.md`'s dispatch-protocol section, where Task 3 of the tenth batch derived
> its home. **The findings series stands at F66 when this batch begins** — you allocated F60–F64;
> the writing side allocated F65 and F66 at the tenth batch's return sitting, and the twenty-ninth
> block is where it announced them. Number from **F67**.
>
> **Then read, in full:** `cowork_rulings_2026_08_18_tenth_return.md` (this batch's whole
> authority); `cowork_rulings_2026_08_17_ninth_return.md` **§1** (Ruling 1, the class this batch
> completes); `cowork_rulings_2026_08_16_preparation_return.md` **§6** (the Kind-1 and Kind-3
> treatments Ruling 1b reconciles) and **§5(D)** (the continuous-pruning rule every superseded
> wording moves under); your own `cc_report_preparation_tenth.md` **§§3.b, 3.c and 10**.
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/evidence_pin_membership.json` (Task 1's subject — the derived membership and the four
> unresolved members); `tools/audit/session_start_read_size.json` (Task 3's authority);
> `tools/audit/guard_classification.json` and `tools/audit/guard_state.json` (the start and end
> states).
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no mining, no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, and **no caller flag or
> candidacy acted on**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end;
> record both states.** The expected start state, which is the tenth batch's proven end state:
> **71 run, 70 passing, ONE failing — `tools/audit/gen_filing_convention_application.py --check`
> ([[OI-372]]) — 4 not run, 16 historical, zero STOPs.** **A different start state is a
> STOP-and-report.** Verify every commit at the object by explicit hash and record the SHAs in the
> close. **The reserved-word conventions bind all new prose. The new-tool rule stands. The
> E-ordering rule stands, at both grains. F3's reading rule stands**
> (`gen_cluster_dispositions.py --verify` is the drift authority; `reaim_home_anchors.py --check`
> exits 0 while printing drifted anchors and is not the authority). **Every guard run is taken at a
> quiet tree.**
>
> **★ THE VOCABULARY RULE OF 2026-08-17 BINDS EVERY LINE THIS BATCH WRITES.** A surface rates an
> alternative **TOWARDS the ultimate objective** and **TOWARDS the guiding principles** — never
> *against* either. A term carrying opposite senses in adjacent context is fixed AT THE WORD.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1 — the four unresolved members, closed at the records:** *"THE FACT THE RULE NEEDS GOES
  WHERE THE RULE ALREADY LOOKS FOR IT. Each of the four ruling records whose generated surface could
  not be pinned gains a dated correction naming the commit its surface was ruled at, established per
  member; Ruling 1 of `cowork_rulings_2026_08_17_ninth_return.md` then applies exactly as written
  and is not amended."* — the derivation ruled with it: *"The commit is the last commit touching
  that surface which is dated at or before its sitting."* — its bound: *"A member whose derivation
  cannot be established STAYS UNRESOLVED and is never pinned."* — and the forward clause: *"A RULING
  RECORD TAKEN FROM A GENERATED DOCUMENT NAMES THE COMMIT IT WAS RULED AT."*
- **Ruling 1b — F62's collision, split at the output:** *"`gen_artifact_inventory_surface.py`'s
  CENSUS keeps re-deriving; the RATIFICATION DOCUMENT it wrote has its rendering pinned … Both are
  true at once because they are about two different OUTPUTS of one tool, not two verdicts about one
  output."* — with its bound: *"Neither is superseded here, and the executing act writes that at the
  tool so a later reader meets both treatments where they apply."*
- **Ruling 2 — the three stale banners:** *"one sentence written AT EACH GENERATOR, qualifying the
  banner with the sitting, its date and the ruling record that carries it, rendered into the
  document in the same act, with the former banner text preserved beside it (#12)."* — and its
  sequencing: *"This act runs AFTER Ruling 1's, in the same dispatch … each document's own
  difference is MEASURED and reported, and must be confined to the banner."*
- **Ruling 3 — the figure corrected of record:** *"A DATED CORRECTION IS APPENDED AT BOTH SITES …
  quoting the figures it corrects and preserving them (#12), and naming
  `tools/audit/session_start_read_size.json` as the authority from here."* — with the refusal that
  is part of it: *"the ninth batch's reduction percentage is WITHDRAWN rather than recomputed"* —
  and the standing clause: *"A PUBLISHED CHARACTER FIGURE NAMES THE TOOL THAT PRODUCED IT."*

## Premise ledger

*(Every FACT pins content at content-addressed objects. No premise names a terminus commit as a
premise about content, and no premise asserts a changed-passage count — the F25 lesson.)*

- **FACT** — the tenth batch is complete, pushed, and verified at the objects by the writing side:
  the chain `1760d9a4a8` → `6bd9285006` → `59191d7e65` → `b0596b288b` → `594074e1e1` →
  `b6134f41ff` → `30d44165cf` → `8f88e1a61e` → `9390e2ca2c`, every parent confirmed and every path
  count to the digit (3, 2, 9, 24, 4, 4, 9, 2). Its proven end-state guard shape is the start state
  declared above.
- **FACT** — the membership this batch acts on is derived and published at
  `tools/audit/evidence_pin_membership.json`: seven members, three pinned, four UNRESOLVED. **No
  member identity is retyped in this dispatch as a population** (D-431); the four are named in the
  task below only as the subjects of an act, and the artifact governs.
- **FACT, measured by the writing side at the object `9390e2ca2c` and stated because Task 2's shape
  depends on it** — `tools/audit/gen_rulings_sort.py` carries the RULED-status banner text **at the
  generator**, and `ratification_surfaces/cowork_rulings_sort_surface_2026_08_16.md` renders it,
  opening *"STATUS: RULED, 2026-08-17. NOTHING IS EXECUTED BY IT."* and stating that the banner it
  replaces *"said the surface was awaiting the user and that nothing here was ruled — true when it
  was written, and made untrue by the sitting; the former rendering stands in git at the commits
  that carried it (#12)."* **So Task 2's shape is IMPORTED from a treatment already in the tree
  (#6), not invented, and no new mechanism is needed.**
- **FACT** — `OPEN_ITEMS.md` and `tools/audit/nongating_apparatus_rows.json` are **byte-identical**
  at `1760d9a4a8` and `9390e2ca2c` (blobs `6ae67d8603` and `5bb43d0b3a` at both). Nothing this batch
  does may move either; a difference in either at any task is a STOP-and-report.
- **ASSUMPTION A1** — the working tree carries exactly ONE tracked modification,
  `cowork_handoff.md`, whose content is: **the TWENTY-NINTH session-close block inserted, and the
  TWENTY-EIGHTH heading carrying an entry-point demotion marker — two parts of one act.** Untracked
  and to be landed by Task 0: `cowork_rulings_2026_08_18_tenth_return.md`, this dispatch, and
  `cowork_verification_plan_preparation_tenth.md`. Every other untracked path is the known,
  declared, unlanded population. **Check ordered (Task 0, first act, blob against blob by explicit
  hash):** any tracked difference whose CONTENT falls outside that shape is a STOP-and-report.
  **★ THE F57 CAVEAT ON THAT CHECK.** Compare through git, or normalise line endings before any
  byte comparison decides anything; **a file differing from its blob by EXACTLY its own line count
  is NOT a modification and is NOT a STOP.** For the three files the writing side wrote this session
  the caveat does not arise — each was measured LF with zero carriage returns and round-tripped back
  through the device bridge byte-identical — but it binds every other file the check enumerates.
  **★ WHY THE BLOCK AND THIS ASSUMPTION WERE WRITTEN IN THE SAME ACT:** a handover block written
  after a dispatch has already stated its premise about the working tree is how a false premise
  reaches the executing side, which is the A1 premise error's own shape.
- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  **Check ordered (each task's last step):** any other red is classed by its measured cause, and one
  outside this batch's own acts' subjects is a STOP-and-report. The reds this batch itself may cause
  and must clear by their ruled treatments: **the F15 anchored-quote class**, because Task 1 and
  Task 3 edit `cowork_audit_protocol.md` and the decisions register anchors quotes into it; **any
  check whose subject is a generator Task 1 pins**; and **any check that reads one of the three
  ratification surfaces Task 2 re-renders.**
- **ASSUMPTION A3** — for each of the four UNRESOLVED members, at least one commit touching its
  surface is dated at or before its sitting. **Check ordered (Task 1, first act):** a member with
  none is a **STOP-and-report**, never a guessed pin, and it stays UNRESOLVED.
  **★ THIS IS AN ASSUMPTION AND NOT A FACT, AND THE REASON IS DECLARED RATHER THAN LEFT AS A GAP:**
  the writing side ATTEMPTED to establish it and the per-path history walk **TIMED OUT on this
  mount** — the same class of timeout the record already carries for `git status` and
  `changed_paths.py` — and was not retried. It is stated as an assumption with an ordered check so
  that the executing side, which has the working mount, establishes it rather than inheriting a
  claim the writing side could not check.

## Task 0 — land the ruling record, the handover block, this dispatch and the verification plan

1. **A1's check first, entirely at content-addressed objects**, under the F57 caveat above.
2. Commit exactly these FOUR paths and no fifth: `cowork_handoff.md` (modified),
   `cowork_rulings_2026_08_18_tenth_return.md` (new), `cc_instruction_preparation_eleventh.md`
   (new), `cowork_verification_plan_preparation_tenth.md` (new). Staged plainly; push; verify at the
   object.

**★ WHAT THE FOURTH PATH IS, because it is new in kind and a reader must not mistake it for a
dispatch or a ruling.** `cowork_verification_plan_preparation_tenth.md` is the writing side's
**pre-registered expectations for the tenth batch, written BEFORE that batch's report existed** and
carrying a dated correction of its own. It rules nothing and orders nothing. It lands because
pre-registration is only evidence if the record can show it preceded the result, and git is the only
thing that can show that.

**Registered expectation E0:** exactly 4 paths at the enumeration; one modification whose content
matches A1's stated shape, three additions; no staging override.

## Task 1 — Ruling 1 and Ruling 1b: the four members closed at the records, the pins taken, the forward clause and the collision written, in ONE commit

1. **A3's check first, per member, and it is a derivation and not a lookup.** For each UNRESOLVED
   member the artifact names: establish **the last commit touching its ratification document that is
   dated at or before its sitting**. **Publish, per member: the document, its sitting, the sitting's
   date, the ruling record that carries the sitting, every candidate commit considered with its
   date, the one selected, and the evidence that no later commit at or before that date touched the
   document.** A member with no such commit is a **STOP-and-report** and stays UNRESOLVED.
2. **★ MEASURE THE RESIDUAL RISK PER MEMBER AND PUBLISH IT — do not carry it as a class caveat.**
   The ruling states the risk in terms: where a surface was regenerated but not committed between
   its last pre-sitting commit and the sitting itself, this derivation names a rendering the user did
   not read. **Report, per member, the interval between the selected commit and the sitting, and any
   evidence bearing on whether the document moved inside it.** A member whose interval the evidence
   cannot bound is reported as such; it is still pinned, and the bound is published with the pin, so
   a later reader can challenge it. **The one thing forbidden is silence about the interval.**
3. **Write the established commit as a DATED CORRECTION APPENDED to each of the four ruling
   records**, quoting nothing that is rewritten: **a ruling record is corrected by appending, never
   by rewriting the ruled text**, on the pattern the ninth batch's own Task 0b and the tenth batch's
   Task 0b both used. Each correction names the commit, the derivation that produced it, the
   interval from step 2, and Ruling 1 of `cowork_rulings_2026_08_18_tenth_return.md` as its
   authority.
4. **Apply the pin to each member whose commit is established**, on the shape already in the tree
   and **IMPORTED rather than re-invented (#6)**: the generator reads its inputs from the git
   objects at the pinned commit, and its check compares against the committed document.
   **`gen_claude_md_finer_surface.py`'s existing pin is not re-taken**, and neither are the two
   route-B pins the tenth batch recorded.
5. **What the pin does NOT freeze is written at each newly pinned generator**, exactly as the tenth
   batch wrote it at the three it recorded: the UNDERLYING DATA FILE is not pinned and continues to
   re-derive; only the rendering of the document the user ruled from is fixed; **a later ruling can
   unpin any member in one edit.**
6. **★ RULING 1b, written at `gen_artifact_inventory_surface.py` itself.** That tool has TWO
   outputs and TWO ruled treatments, and both stand: **its CENSUS re-derives** under §6 kind 3 of
   the 2026-08-16 record; **the RATIFICATION DOCUMENT it wrote has its rendering pinned** under
   Ruling 1. Write both at the tool, each naming the ruling that governs it, and state in terms that
   **neither ruling is superseded** and that the pin fixes what the rendering reads without freezing
   the census. **If the tool's construction cannot express the split — if one code path writes both
   outputs from the same read — that is a STOP-and-report**, not an improvisation: report the
   measured shape and stop.
7. **The forward clause, at the home the record already makes one.** In
   `cowork_audit_protocol.md`'s dispatch-protocol section, beside the read-first clause your last
   batch landed there: **a ruling record taken from a generated document names the commit it was
   ruled at.** Record **F60** as its ground, with the measured instance — one of five route-A
   members carried the field — and the general form: *a rule whose operative clause names a field
   the records do not carry cannot be applied without amending either the rule or the records.*
8. **The anchored-quote consequence is the F15 class and is cleared by its ruled treatment** —
   anchors re-aimed PER CITATION from the drift report, affected derivations regenerated under the
   ordinary enumerated-movement bound, `gen_cluster_dispositions.py --verify` the drift authority.
   **Any regenerated surface's own difference is MEASURED before it is accepted.**
9. **No verdict, fate, population or row moves**, and **no document is regenerated to a new
   content** — a member whose current rendering already differs from its committed one is
   **restored to its committed bytes and the difference reported**, never adopted.
10. Commit, push, verify at the object.

**Registered expectation E1:** per member, the derivation is published with its candidates, its
selection, its evidence and its interval; every member whose commit is established is pinned and
every member whose commit is not is STOPped on and left UNRESOLVED; the four ruling records each
carry a dated correction with nothing rewritten; the forward clause stands at the derived home with
F60 named; the collision is written at its tool with neither ruling superseded, or STOPped on with
the measured shape reported; no verdict, population or row moves and `OPEN_ITEMS.md` is
byte-unchanged.

## Task 2 — Ruling 2: the three stale banners corrected at their generators, in ONE commit

**This task runs AFTER Task 1 and the order is ruled, not preferred.** For any member Task 1 pins, a
change to its rendering afterwards is a deliberate, recorded act; run the other way round, this
change would land inside the rendering the pin is being taken over.

1. **The shape is IMPORTED from `gen_rulings_sort.py` (#6), which already carries it**, and the
   import is stated at each site so a later reader meets the precedent rather than three
   independently authored treatments. Write, at each of the three generators, ONE sentence
   qualifying the banner with **the sitting, its date, and the ruling record that carries it**, and
   **render it into the document in the same act**.
2. **The former banner is preserved (#12)** in the form the precedent uses: the rendered banner
   states what it replaces and that the former rendering stands in git at the commits that carried
   it. **Nothing is deleted.**
3. **Each document's own difference is MEASURED before it is accepted**, blob against blob by
   explicit hash, and **must be confined to the banner**. A difference reaching any verdict,
   population, entry or count in any of the three documents is a **STOP-and-report**.
4. **The mechanism freeze is answered at the site, not assumed away.** State why this is admissible:
   a governing surface stating something false about itself is #10's own doc-truth subject, and the
   act creates no new mechanism because the treatment is imported.
5. **The rulings-sort surface is NOT re-corrected** — it already carries the treatment. **But
   REPORT, as data, where its correction actually lives and whether it survives regeneration**, and
   correct of record, in this batch's own findings, the tenth batch's characterisation of it as made
   *"by hand"*: the writing side established at the object that the text lives at the generator.
   **The former characterisation is preserved beside its correction (#12).**
6. Commit, push, verify at the object.

**Registered expectation E2:** three generators carry the qualification and three documents render
it; each preserves its former banner in the imported form; each document's difference is measured and
confined to the banner; the rulings-sort surface's own treatment is located and reported as data with
the tenth batch's characterisation corrected; no verdict, population, entry or row moves.

## Task 3 — Ruling 3: the correction of record, and the standing figure clause, in ONE commit

1. **A dated correction appended at BOTH sites** — §3 of
   `cowork_rulings_2026_08_17_ninth_return.md`, and the TWENTY-EIGHTH block of `cowork_handoff.md`.
   Each **quotes the figures it corrects and preserves them (#12)**, names
   `tools/audit/session_start_read_size.json` as the authority, and states the corrected values **by
   citation to that artifact and never by transcription** (D-431). **Neither site's own text is
   rewritten**; a ruling record and a handover block are both corrected by appending.
2. **The figures corrected are the section's and those derived from it**: the total, the section,
   the identity list, the grounds and the frozen comparison. **State the arithmetic that closes
   it** — the whole gap is the one term, and every other member of the read agrees to the digit.
3. **★ THE PERCENTAGE IS WITHDRAWN AND NOT RECOMPUTED, and this is the half that binds hardest.**
   The live tool declares its shape does not reach the pre-ninth-batch regime, so a corrected
   percentage would be a hand computation across two different questions. **Correcting an
   unreproducible hand figure by writing a second unreproducible hand figure is the defect
   repeating itself.** State that the percentage is withdrawn and why. **If the tool CAN be shown to
   reach both regimes, that is a finding to report and not a licence to compute** — the withdrawal
   stands for this act either way.
4. **Ruling 3 of the ninth-return sitting is NOT re-opened and the correction says so**: the saving
   is larger than that ruling claimed, not smaller, and its prediction holds against the measured
   value.
5. **The standing clause, beside D-431 in `cowork_audit_protocol.md`: a published character figure
   names the tool that produced it.** Record **F66** as its ground — a figure that becomes a
   headline is checked at a generator before it becomes one — and state that this is D-431's own
   missing half: D-431 says where a figure comes from, and this says that a reader must be able to
   see it.
6. **The anchored-quote consequence is cleared exactly as in Task 1.**
7. Commit, push, verify at the object.

**Registered expectation E3:** both sites carry a dated correction with the former figures quoted and
preserved and nothing rewritten; every corrected value enters by citation to the artifact; the
percentage is withdrawn with its reason stated and no percentage is asserted that the tool cannot
derive; the standing clause stands beside D-431 with F66 named.

## Task 4 — the close

One `STATUS.md` pointer entry per task — and, per the standing Ruling-4 bound of the
governing-surface split record, **the same act moves the TENTH batch's entries verbatim to
`STATUS_ARCHIVE.md`** (`gen_status_batch_bound.py`), both directions proven. **The declared
`Last updated: ` prefix adjustment is imported, not re-decided (#6); an entry needing a second
adjustment still halts.**

The FULL close is appended to `cowork_away_returns.md` as a PREPARATION ELEVENTH BATCH section: both
guard states, every SHA, every registered expectation graded MET or NOT MET with its reason, every
declared departure, and the carried findings restated for the phase's retrospective — **F1–F66,
surfaced and none rowed; the row bar stands whole this batch.** The report is
`cc_report_preparation_eleventh.md`, an ordinary tracked file. Commit, push, verify at the object.

**★ THE E-ORDERING RULE, AT BOTH GRAINS.** The end-state guard run is taken AFTER the Task 4 commit
exists, and its output with the final SHAs lands in ONE FURTHER commit. **A batch-level summary
sentence written in the same commit as the work it summarizes is the same defect at a smaller
grain** — your last batch AVOIDED the sentence rather than declaring it, which is the cleaner of the
two routes the tenth dispatch offered; do the same.

**Registered expectation E4:** at the tree carrying the close, a fresh full guard run re-derives —
**every run check passing except [[OI-372]]'s tool, zero STOPs** — run and read, never inferred, its
values committed only after the run that produced them.

## What this batch does NOT do

- **No curated boot list.** It stays at its ruled definition — the implementation-free read list an
  implementation-blind session boots from, for those sessions only — and remains a live, unblocked
  preparation output awaiting its own drafting and sitting.
- **No fate on any `CLAUDE.md` span ruled at the eighth-return sitting.** Both stay at site, that
  question is CLOSED, and `gen_claude_md_finer_archive.py --apply` is not run.
- **No archiving pass**, and no re-opening of Ruling 2 of the session-start-read sitting.
- **No change to rule (a).** Its pointer stands exactly as your last batch narrowed it, and
  `tools/audit/nongating_apparatus_rows.json` must be byte-unchanged at every task.
- **No candidacy acted on, no census re-pin.** No mandatory-read or boot listing moves in this
  batch, so the caller census's inputs are untouched; if any act would move one, that is a
  STOP-and-report and not a task to add.
- **No mining, no empirical findings ledger, no fact-gate admission**, no completion claim about the
  superseded phase-1 program, no derivation, design, repair or pilot act.
- **No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
  `tools/robust_stop/`, no measurement of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly as
  found; **[[OI-179]] stays OPEN and GATES**; F3 stays surfaced, unfixed and unrowed at twelve times
  surfaced.
- **`gen_gating_row_sizing.py`'s frozen population is left alone.**
