# CC dispatch — the preparation phase's FOURTH batch: the reach ruling landed, the derived split and reader enumeration run, and the ruled soft-discard EXECUTED under the ruled reach — the superseded phase-1 apparatus reclassified historical, the standing apparatus regenerated under the enumerated-movement bound

> **Dispatch (Cowork, 2026-08-16).** Executes §4 of `cowork_rulings_2026_08_16_preparation_return.md`
> — the reach ruling (Alternative A as recommended) — and thereby completes §3's soft-discard,
> which the third batch correctly STOP-reported (F13) and reverted. **Written at a verified STOP;
> nothing else is running.** The archiving wave, the empirical findings ledger, the fact-gate, the
> curated boot list, the mining, the landing of the newly visible files, the eight
> KIND-UNDERIVABLE callers, the prose-citation question, the residue surface's sitting and the
> rulings-sort sitting are NOT in this batch.
> **Read first (every session):** `C:\s\MS\CLAUDE.md`, `C:\s\MS\STATUS.md` (header only),
> `C:\s\MS\BUILD_AND_TEST.md`. **Then read, in full:**
> `cowork_rulings_2026_08_16_preparation_return.md` (§3 and §4 are this batch's whole authority);
> `ratification_surfaces/cowork_discard_reach_surface_2026_08_16.md` (the ruled surface — §6 is
> the act's shape, §4 its premise ledger); your own `cc_report_preparation_third.md` (§4 above
> all — the measured reach this batch executes past); and the retired-block precedent,
> `cowork_rulings_2026_08_15_batch_return.md` §2.
> The standing bars bind this batch whole: **no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no open-items row created, flipped or
> discarded, no archiving, no file moved or deleted, no mining.** The register mutations permitted
> are Task 2's, exactly as bounded there.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end;
> record both states.** The expected start state: **55 run, 54 passing, ONE failing**
> ([[OI-372]]'s tool, `gen_filing_convention_application.py --check`), 4 not run, 10 historical,
> zero STOPs, no stale report. **A different start state is a STOP-and-report.** Verify every
> commit at the object and record the SHAs in the close. The reserved-word conventions and the
> ruled vocabulary replacements bind all new prose. **The new-tool rule stands** (authored
> run-instruction and classification verdict in the same commit that adds a check). **The
> E-ordering rule stands** (no graded value committed before the run that produced it). **F3's
> reading rule stands** (`gen_cluster_dispositions.py --verify` is the drift authority).

## Ruling ledger (quoted at the record, not paraphrased)

- **§4 (the split):** the classification of every red check is derived and published — *"SUPERSEDED
  (serves only the old phase-1 gate) or STANDING (serves a rule of the current record)"* — *"a
  member the derivation cannot place, and any STANDING member whose red the enumerated-movement
  bound cannot explain, STOPS to the user"*; the reader enumeration *"runs BEFORE anything
  freezes; a live consumer outside the superseded program is a STOP-and-report."*
- **§4 (historical):** the SUPERSEDED members move to the guard set's historical class *"by an
  authored classification change committed with the act, their committed artifacts frozen in
  place as record (#12), never regenerated again"*; historical status *"asserts NOTHING about
  whether that program's obligations were discharged"*; a reclassification the guard mechanism's
  own records cannot express *"is a STOP, not an improvisation."*
- **§4 (the bound):** the STANDING members are regenerated alongside the discard in the same
  commit — *"movement ONLY in values whose subject is a discard-population entry, or a document
  whose class or home standing those entries alone carried; every moved value enumerated in the
  close and diffed at explicit hashes; any other movement is a STOP-and-report."*
- **§3 (A):** the 194 NOTHING-FOUND entries *"are SOFT-DISCARDED — behind the sole-carrier
  guard"*; sole-carrier members *"do NOT ride the discard"*. Every executed discard record
  carries finding, date, reason AND the clause verbatim: *"a provenance verdict, not a judgment
  on soundness or usefulness; the statement stands at its home and is met by the derivation."*
  *"Nothing destroyed (#12); every discard individually revivable."*
- **§3 (B1):** the ten clear recoveries KEPT — D-004, D-008, D-009, D-021, D-123, D-248, D-315,
  D-403, D-410, D-411 — *"each with its recovered act recorded as the entry's provenance at the
  discharge."*

## Premise ledger

*(Per the recorded F12/F17 lesson, no premise names a terminus commit; every FACT pins content,
checked at content-addressed objects.)*

- **FACT** — the third batch is complete, pushed and verified at the objects; its committed plan
  is `tools/audit/soft_discard_application.json` (the derived discard population: 194 = 29
  withheld + 165 to retire; live 677 → 512), its guard artifacts
  `tools/audit/sole_carrier_subclass.json` and `tools/audit/ratified_document_check.json`, and
  its tool writes no decisions-register file without `--apply` (`cc_report_preparation_third.md`
  §4.d; the artifacts are the authority, no value restated here — D-431).
- **FACT** — the fourteen-red measurement and its three causes are recorded at
  `cc_report_preparation_third.md` §4.c, quoted from the run.
- **ASSUMPTION A1** — the working tree carries exactly TWO tracked modifications and no third:
  `cowork_handoff.md` (the twentieth session-close block inserted AND the nineteenth heading's
  demotion marker — two parts of one act, reported by the version history as ONE changed passage
  because they are adjacent) and `cowork_rulings_2026_08_16_preparation_return.md` (ONE
  contiguous changed passage: §4 inserted before the provenance block, which the same act
  rewrites in place). `cc_instruction_preparation_fourth.md` and
  `ratification_surfaces/cowork_discard_reach_surface_2026_08_16.md` are untracked, among the
  standing untracked records which this premise does not enumerate. **Check ordered (Task 0,
  first act, at content-addressed objects, blob against blob by explicit hash):** any tracked
  difference outside that shape is a STOP-and-report.
- **ASSUMPTION A2** — the guard mechanism's own records can express a historical
  reclassification of a currently-run check (the guard state already reports ten historical
  records). **Check ordered (Task 2):** the reclassification is performed through the guard
  mechanism's own recorded form; if that form cannot express it, STOP-and-report — no
  improvised side-channel.
- **ASSUMPTION A3** — with the ruled reach applied (historical reclassification + bounded
  regeneration + the mechanism work), NO red survives at the end state except [[OI-372]]'s
  tool. **Check ordered (Task 2, step 6):** any surviving red outside that expectation is a
  STOP-and-report — the ruling authorizes the enumerated movement and nothing else.

## Task 0 — land the ruling, the surface, the handoff block and this dispatch

1. **A1's check first, entirely at content-addressed objects.**
2. Commit exactly these FOUR paths and no fifth: `cowork_handoff.md` (modified),
   `cowork_rulings_2026_08_16_preparation_return.md` (modified — §4 lands),
   `ratification_surfaces/cowork_discard_reach_surface_2026_08_16.md` (new),
   `cc_instruction_preparation_fourth.md` (new). All staged plainly; push; verify at the object.

**Registered expectation E0:** exactly 4 paths at `git diff-tree`; two modifications matching
A1's stated shapes, two additions; no staging override of any kind.

## Task 1 — the reader enumeration (R1) and the derived split (R2), before any mutation

1. **R1.** Build `tools/audit/gen_phase1_gate_readers.py`: enumerate every reader, at the
   current commit, of the old phase-1 gate artifacts and their derivation family's outputs —
   `tools/audit/phase1_completion_inventory.json`, `tools/audit/phase1_finish_line.json`, and
   the committed outputs of `gen_outstanding_delegations.py`, `gen_finish_line_item1_routes.py`,
   `gen_item1_rehome_blocker.py`, `gen_r1_superseded_reach.py` — every tracked file that names
   any of them, each classified with evidence: the superseded program's own apparatus, a
   historical record, or a LIVE consumer. **A LIVE consumer outside the superseded program is a
   STOP-and-report.** Publish `tools/audit/phase1_gate_readers.json` (with `--check`; the
   new-tool rule applies).
2. **R2.** Build `tools/audit/gen_discard_reach_split.py` over exactly the checks the discard's
   application turns red (imported from the committed report's §4.c list, never retyped — read
   the fourteen from the artifact side by re-deriving: apply the committed plan to a SCRATCH
   working state or derive membership statically from each check's imports; state in the
   artifact which route was taken and why). Per member, one verdict with evidence — its imports
   and its own stated purpose: **SUPERSEDED** (serves only the old phase-1 gate derivation
   family) or **STANDING** (serves a rule of the current record). The two register-derived
   checks the third dispatch anticipated (`gen_decisions_filter.py`,
   `gen_deciding_act_recovery.py`) and the pre-existing [[OI-372]] red are classed STANDING
   with their ordinary-regeneration / pre-existing standing noted. **A member the derivation
   cannot place STOPS to the user.** Publish `tools/audit/discard_reach_split.json` (with
   `--check`; the new-tool rule applies).

**Registered expectation E1:** every red check covered with one verdict and its evidence; R1's
enumeration published with zero LIVE consumers outside the superseded program (else the batch
has already STOPped); no decisions-register file and no guard-classification record touched in
this task's commits beyond the ordinary registration of the two new checks.

## Task 2 — the ruled soft-discard EXECUTED under the ruled reach — ONE commit

All of the following in ONE commit, citing §3 and §4:

1. **The discard, through the register's own mechanism** (rule (d), never a hand-edit of a
   rendered file): the derived population (the committed plan re-derived and re-verified — 194 =
   withheld + executed to the digit, a both-blocks or neither-block member halting) moved whole
   into the RETIRED-ENTRIES block on the [[OI-373]] pattern, WITH the block's own STOPs (a
   retired entry re-appearing live, or an entry in neither block, halts the renderer and the
   reconciliations); each retired record carrying the entry UNCHANGED (#12), its finding, date,
   the retiring authority (`cowork_rulings_2026_08_16_preparation_return.md` §3) and the ruled
   clause verbatim. The TEN B1 entries' provenance stamped with their recovered acts (quoted,
   document and location, from the recovery artifact), every other field untouched.
2. **The mechanism work:** the establishment pass consults the retired block beside the live
   entries (the third batch measured this working — `--verify` at 512/512 with 33 surviving
   cross-references; re-derive, do not assume); the renderer carries the retired block's STOPs;
   **F15's anchored-quote remap** performed per-citation from the `--verify` drift report
   (`gen_cluster_dispositions.py --verify` the authority — F3: `reaim_home_anchors.py --check`'s
   exit code establishes nothing).
3. **`DECISIONS.md` and every `decisions/group_*.md` regenerated BY THE GENERATOR.**
4. **The SUPERSEDED members (Task 1's split) reclassified HISTORICAL** through the guard
   mechanism's own records (A2's check), authored classification change in this same commit;
   their committed artifacts left byte-untouched in place, frozen as record; **no SUPERSEDED
   member's generator is run again.**
5. **The STANDING members regenerated by their own generators**, each diffed against its
   committed blob at an explicit hash before the commit; **the ruled bound applies: movement
   ONLY in values whose subject is a discard-population entry, or a document whose class or home
   standing those entries alone carried; every moved value enumerated in the close; any other
   movement is a STOP-and-report.**
6. **The full guard set at the edited tree (A3's check):** expected — every run check passing
   except [[OI-372]]'s tool, the SUPERSEDED members reported historical, zero STOPs. **Any
   other red is a STOP-and-report.** Then commit as ONE commit, push, verify at the object.

**Registered expectation E2:** the data-file diff touches exactly the retired-block addition,
the moved entries and the ten provenance stamps; every rendered file regenerated, none
hand-edited; **live + retired = the former population BY ARITHMETIC, stated in the close to the
digit; nothing destroyed** — every retired entry's text present, byte-preserved, in the retired
block; `gen_decisions_register.py --check` and `gen_cluster_dispositions.py --verify` re-derive
clean at the end state; the frozen artifacts byte-identical to their pre-batch committed blobs,
proven by hashing.

## Task 3 — the close

One `STATUS.md` pointer entry per task and nothing else in that file. The FULL close appended to
`cowork_away_returns.md` (a PREPARATION FOURTH BATCH section) — both guard states, every SHA,
every expectation graded, the discard arithmetic to the digit, the historical members named, the
enumerated movement listed value by value, every problem declared, the carried findings restated
for the retrospective (F1–F18, the E3 ordering defect, the A1 premise error — surfaced, none
rowed; rows stay barred). The report `cc_report_preparation_fourth.md`, an ordinary tracked
file. Commit, push, verify at the object. **The E-ordering rule:** the end-state guard run
demonstrated across the commit boundary is taken AFTER the Task 3 commit exists, its output and
the final SHAs landing in ONE FURTHER commit.

**Registered expectation E3:** at the tree carrying the close, a fresh full guard run re-derives
with the end state E2 describes — run and read, not inferred, its values committed only after
the run.

## What this batch does NOT do

- **No sole-carrier member discarded; the 62 not ruled and not touched in the register; the
  residue surface and the rulings-sort surface stand awaiting their own sittings.**
- **No archiving, no file moved or deleted, no mining, no landing; the eight KIND-UNDERIVABLE
  callers and the prose-citation question stay open; every retirement flag stays a candidacy.**
- **No empirical findings ledger, no fact-gate admission, no curated boot list.**
- **No completion claim of any kind about the superseded phase-1 program** — historical status
  records that its apparatus graded a superseded program, nothing more.
- **No derivation of any specification, no design, no repair, no pilot act, no `src/` edit, no
  golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the
  analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly as
  found; [[OI-179]] stays OPEN and GATES; `reaim_home_anchors.py`'s F3 defect stays surfaced,
  unfixed and unrowed (both bars stand).
