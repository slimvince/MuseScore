# ⏸ REFERENCE ONLY (2026-07-26) — NO LONGER A DISPATCH VEHICLE. The remaining partition units are dispatched as their own fresh instruction files (P-strings: `cc_instruction_notation_pstrings.md`; P4 / note-seam / P6 / P7 follow the same pattern). The ratified partition and amendments 1–6 below remain the governing record they cite.

# CC instruction — seams part 2: the consumer re-plumb (default-OFF) + the §8.3(d) dual-arm comparison (the LAST pre-switch increment)

> **★ DATED AMENDMENT — THE PARTITION IS RATIFIED (Cowork, 2026-07-26, at CC's correct
> feasibility STOP; user informed):** the P0–P7 partition from CC's report governs, **including
> the reordering: P1 (the §4.1 gap-scale measurement, original Task 2.3) runs BEFORE the
> section/bridge confidence passes it feeds** — the dependency is real and the original
> Task-1→Task-2 numbering inverted it. P0 (the flag, `b090f6e4ab`) is DONE. Resume at P1 with
> expected HEAD `685bde37d6`; each partition unit is a complete, committed, byte-identical
> delivery per the standing rules; sessions may end after any unit with a resume note.
>
> **One sharpening, binding on P2 (the record→AnalyzedSection adapter):** the legacy
> confidence FIELDS on the record path carry the **RAW §3.3 key-axis content-score gap (nats)**
> — documented as such at the adapter — and the P1 constants are declared on that same scale.
> **No mapping of the gap into a [0,1] pseudo-confidence is permitted** (no sigmoid, no
> normalization, no rescale): that would be an invented calibration, the exact #19/DT-2 defect
> the contract's §3.3 establishment-status rule forbids. A consumer field whose SEMANTICS
> cannot tolerate a nats-valued number (if any exists) is a FINDING, not a conversion.
> Additionally per the report's Gap-1 note: the record path's ranked-alternatives ordering (by
> §3.3 content score) differing from the legacy template-score ordering is an EXPECTED
> inference-driven difference — P6 classifies it; it is not to be "fixed" toward legacy.
>
> **★ DATED AMENDMENT 2 (Cowork, 2026-07-26, at the P1 verification — two P2/P4 dispositions):**
> (1) **The gate unification CC proposed at the P1 report is RATIFIED for P2:** the shared
> detectors (key-area grouping, `detectCadences`/`detectPivotChords`, the implode buckets) read
> the STORED per-arm boolean(s)/bucket the section layer sets ONCE (legacy: `conf >= threshold`;
> record: `gap >= g` from the committed `exposure_constants.json`) instead of re-thresholding
> the confidence field themselves — ONE thresholding site per gate (#6; the confidence-axis
> analogue of the OI-173 lesson). CONDITION: the legacy arm's byte-identity is PROVEN per
> commit as always (the boolean must be set from the same value, same threshold, same stage the
> detectors read today — any legacy output change is a STOP); the single-site constants are
> declared with the P1 measurement citation (value, legacy rate, record rate, residual).
> (2) **The 0.35 mode-suffix gate gets NO record-path constant:** under the ratified C1 rule
> the exotic mode suffix does not exist on the record path (two-mode label always; the modal
> reading published beside), so `modeNameConfidenceThreshold`/`fallbackModeSuffix` RETIRE there
> (the audited Task-3 disposition) rather than re-declare. The measured g₀.₃₅ stays in the
> committed artifact as the record of what was measured (#12) — do not wire it anywhere.
>
> **★ DATED AMENDMENT 3 (Cowork, 2026-07-26, at the P2a verification):** CC's routing of the
> implode tentative/assertive BUCKET unification (`keyExposureBucket`, 0.5/0.8) to **P4** is
> RATIFIED — its only consumer is the implode bridge, re-plumbed at P4; unifying it earlier
> would add a stored field with no live consumer (an undeclared dormancy) and cross partition
> scopes. P2a (`e3dac74b04`) is verified delivered. Resume at **P2b** (the record→
> `AnalyzedSection` adapter) from HEAD `c19aec5369`, under the amendment-1 raw-gap rule and the
> P2b design in the STATUS resume note — with one layering reminder made explicit: the
> per-segment tones re-collection is COMPOSING-side (the adapter's layer), reading the L1
> published note surface; the notation-side collector is not reached into (#7).
>
> **★ DATED AMENDMENT 4 (Cowork, 2026-07-26, at the P2b verification):** P2b
> (`d25cf40170` + STATUS `3acc8e141c`) is verified delivered. Resume at **P3** (the span-path
> composing bridge) from expected HEAD **`3acc8e141c`** (the origin tip — not `d25cf40170`).
> Carried notes: (a) **P4's OI-182 row-update records `kAssertiveKeyExposureGap = 1.055757`
> alongside the P4-declared constants** (it landed at P2b as a necessary input, fully cited);
> (b) P3's chord-symbol/RN strings come from the RECORD's derived facts
> (`seg.chordSymbol`/`seg.romanNumeral` — §5.6 formatter continuity, ONE formatter path, #6),
> never re-formatted from `ChordIdentity`; the bridge reading the record beside the
> `AnalyzedSection` is the contract-conformant shape (A2 — the record IS the surface), P3's to
> plumb; (c) the two expected inference-driven differences (alternatives ordering; the
> class-native `diatonicToKey`) stay carried to P6's classification, as reported.
>
> **★ DATED AMENDMENT 5 (Cowork, 2026-07-26, at the P3a verification):** P3a (`e2c59715a5` +
> STATUS `cb246a7580`) is verified delivered (the new 12th snapshot test confirmed golden-less
> — structural pins of the record arm against its own published facts; no output blessed).
> **CC's P3 re-partition is RATIFIED:** the status-bar/note-context path MERGES with the old
> P5 into ONE note-seam unit (status bar + `notationinteraction` + `notationcontextmenumodel`,
> all on `noteView`, the `NoteHarmonicContext`↔record string-carriage resolved ONCE — deciding
> the carriage twice would be the #6 defect). **The remaining order:** the register split runs
> as its OWN dispatch first (`cc_instruction_open_items_split.md`, user-scheduled — after it,
> OPEN_ITEMS is an INDEX + per-item `open_items/OI-xxx.md` detail files; ALL later row edits
> — OI-182 at P4 included — target index row + detail file); then **P-strings** (the two P3a
> findings' derived-string publications — PENDING the user's rulings, arriving as amendment 6);
> then P4 (implode + tuning, bucket + OI-182); then the merged note-seam unit; then P6; P7.
> The P3a findings themselves (Nashville gap; grading-vs-display chord symbol) are with the
> user — do NOT improvise either at P4/note-seam; the record arm's Nashville stays unwritten
> and the emitters keep the current form until amendment 6 lands.
>
> **★ DATED AMENDMENT 6 (Cowork, 2026-07-26 — the user's rulings on both P3a findings):
> the P-strings unit is PRESENTATION-SIDE, not module publications.** Rulings: Nashville is a
> presentation concern (its every constituent — degree + accidental via the published root
> spelling, class quality incl. seventh-ness, bass role/inversion, key — is a published record
> fact, verified, no exception class); the display chord symbol is D2 — the grading-form
> string stays on the record (batch continuity §5.6), the DISPLAY symbol is a presentation
> derivation (preset styling is presentation territory; the record stays preset-independent).
> **The P-strings unit therefore delivers:**
> (1) ONE shared presentation-side display-symbol path — reuse the existing
> `ChordSymbolFormatter` component where faithful (carried establishment, #19); **prove
> faithfulness explicitly:** the full class facts reach the formatter (seventh-ness/extensions
> not coarsened by the adapter's quality map — a named hazard; fix the CARRIAGE, never the
> formatter's inference-side inputs), the root spelling comes from the record's published tpc,
> flat-key cases tested (e.g. A♭ vs G♯).
> (2) ONE shared presentation-side Nashville formatter from record facts; establishment =
> derivation documented + tests + continuity vs the legacy Nashville formatter on coinciding
> readings.
> (3) **The permanent boundary guard** (the user's directive at the D2 ruling): a mechanical
> dependency-direction test — the joint module references NO presentation
> formatter/styling/preset symbol; the presentation formatters consume ONLY the published
> record/adapter surface, never inference internals (the include-closure pattern; a failing
> test, not a convention).
> (4) The record-arm emitters (P3a's, then P4/note-seam's) wire both formatters; the contract
> gains the dated note (Cowork's riding edit).
> Flag-OFF byte-identity per commit as always. Order unchanged: register split → P-strings →
> P4 → merged note-seam → P6 → P7.

> **Read first (every session):** `C:\s\MS\CLAUDE.md`, `C:\s\MS\STATUS.md` (header + newest
> entries), `C:\s\MS\BUILD_AND_TEST.md`, `OPEN_ITEMS.md`, the ratified
> `C:\s\MS\cowork_notation_output_contract.md` IN FULL (§1 seams, §3 record, §4 consumer
> mapping + §4.1 presentation gates, §5 establishment), and the audit's field-level map
> `tools/audit/notation_surface/consumption_fields.csv` — **the CSV is your per-field
> re-plumb checklist**; every `A-SOURCED`/`DERIVABLE` row must end up reading its named
> contract source on the record path.
>
> **Current state:** branch `master`; expected HEAD `6e71b3ceff` (seams part 1, pushed) —
> verify; mismatch = STOP. Riding Cowork edit: `cowork_handoff.md` only. This dispatch file
> stays untracked.
>
> **Hard stops, always:** origin only; ANY behavior change with the flag OFF (byte-identity is
> proven per commit — both suites + pipeline snapshots untouched, no golden refresh in this
> dispatch); no legacy-analysis call on the record path (the record path never invokes
> `analyzeRegions`/`analyzeChord`/the legacy key layer — if a consumer "needs" something only
> the legacy analysis computes, that is a FINDING returned to Cowork, never a port, #13/#12);
> no inference edit anywhere (the standing rule: no inference-problem fixing — if a record-path
> output looks musically wrong, it is DIAGNOSED in the comparison report, never patched);
> files outside the touchable set. VS Code bash rules on every command.
>
> **Feasibility:** if the full re-plumb does not fit one session, STOP after any COMPLETE,
> committed, byte-identical task with a partition proposal (the L4/L5 audit precedent) — never
> a half-plumbed consumer.
>
> **No mid-flight steering:** self-sufficient; anything uncovered waits for the report.

**Dispatch author:** Cowork, 2026-07-26, at the seams-part-1 verification. **Purpose:** every
audited notation consumer gains its RECORD-path implementation behind ONE internal default-OFF
flag; the dual-arm comparison instrument captures both arms' consumer outputs and classifies
the differences — the input the user's switch ratification reads. **The flag stays OFF at the
end of this dispatch; the flip is the separate, user-ratified switch commit.**

**Touchable set:** `src/notation/internal/**` (the audited consumers + bridge),
`src/composing/analysis/section/**` (the section layer's record-path derivation),
`src/composing/analysis/joint/**` (only if a view/accessor gap surfaces — additive only),
`src/composing/icomposinganalysisconfiguration.h` + its configuration implementation (the ONE
internal flag; NO UI/QML), the relevant test dirs + CMake lists, `tools/` for the comparison
driver + artifact, `ARCHITECTURE.md`, `STATUS.md`, `OPEN_ITEMS.md` (OI-182 row execution
note), the riding Cowork file.

---

## Task 0 — commit the riding `cowork_handoff.md` (with your Task-1 flag commit; no separate commit needed)

## Task 1 — the flag + the section layer + the span-path composing bridge (commit per coherent step; flag OFF byte-identical each)

1. **The flag:** one internal boolean in the composing analysis configuration (descriptive
   name; default **false**; no settings UI — an internal migration switch, documented as
   retiring WITH the legacy path at the map).
2. **The section layer, record path:** `analyzeSection`'s record-path variant derives
   `AnalyzedSection` from the record — regions from segments (spans, committed reading,
   derived facts), key areas by collapsing the record's key sequence (the §4 mapping), cadence
   and pivot labels from the record's fields (degree/quality/key + the §3.3 slice where the
   audited gate read confidence). The legacy gap-fill/trim/stabilization machinery is NOT
   ported (A's decode is contiguous by construction — state this in the code comment); the
   legacy path stays untouched beside it.
3. **The span-path composing bridge** (annotations, bracket markers, status-bar strings):
   record-path implementations per the CSV rows — chord symbol/RN strings from the record's
   derived facts (the module formatter, #6), key strings from the two-mode key (+ the modal
   reading is AVAILABLE; §4.1's C1 display rule: no exotic suffix anywhere), the candidate
   score suffix from the §3.3 slice. **The pedal "X ped." emission: on the record path it does
   NOT fire** — the P1 ruling; leave the legacy-path code untouched, and at the record-path
   site a comment naming OI-194 (the ornament-class successor).
4. Tests: record-path unit coverage for the new derivations (section derivation on a corpus
   piece via the producer; the C1 display rule; the pedal non-emission); flag-OFF byte-identity
   (all three suites green, snapshots untouched) proven at EVERY commit.

## Task 2 — implode + tuning, record path (+ the §4.1 presentation constants; OI-182 executes)

1. **Tuning bridge:** root/quality/key per record segment (direct fields), the note-seam
   single-note path via `noteView`.
2. **Implode bridge:** key runs, voicing (tones re-collected over the segment span from the L1
   note surface, per the audited derivation), chord-symbol/roman/nashville from the derived
   facts, coalescing per its audited semantics.
3. **The §4.1 presentation gates:** every legacy gate fed by `normalizedConfidence` (the
   exposure thresholds 0.5/0.8, the section-layer 0.8 annotate gate, the exposure bucket)
   re-declares on the record's §3.3 group (i) KEY-AXIS GAP scale (nats). **Value selection is
   a documented, measured correspondence — NOT a fit and NOT a guess:** run the legacy arm
   over the pipeline-snapshot corpus, record each gate's firing set; choose the gap-scale
   constant whose firing set on the record arm most nearly preserves the legacy EXPOSURE
   RATE (not the per-site identity — the committed readings differ); document per constant:
   the chosen value, the legacy rate, the record-arm rate, the residual difference. These are
   PRESENTATION constants (outside inference, re-tunable as presentation policy later) —
   declare each in code with this rationale + the measurement, and **update the OI-182 row**:
   the exposure-constant family is now declared-on-the-record-surface (the confidence-mapping
   disposition executed; `kSameChordReannotationGap` re-homed unchanged as the audited
   presentation-timing constant).
4. Tests + flag-OFF byte-identity per commit, as Task 1.

## Task 3 — the note-seam consumers (interaction + context menu), record path

`notationinteraction`'s harmony-write and `notationcontextmenumodel`'s menu read `noteView`:
the committed reading + the full §3.3 chord-axis list (display order: committed first, then
by descending content score — a VIEW ordering, not a truncation; the menu shows what it showed
before structurally — items + score suffixes — from record facts). The status-bar/accessibility
chain follows the composing-bridge formatter automatically (audited: string-only downstream).
Tests + flag-OFF byte-identity.

## Task 4 — the §8.3(d) dual-arm comparison instrument + the classified report (the switch-ratification input)

1. **The instrument:** a test-harness driver (extend the pipeline-snapshot capture machinery —
   it already serializes the notation path's outputs over the 10-score snapshot corpus) that
   runs the FULL notation output surface twice — flag OFF (legacy) and flag ON (record) — and
   serializes both arms' consumer outputs: regions/annotations (chord symbols, RN, key
   strings, cadence/pivot labels, bracket markers, pedal texts), implode chord-track output,
   tuning offsets, note-seam answers on a declared tick sample. Deterministic, run-to-run
   stable.
2. **The classified diff (generated artifact, #17f):** per output class, per score: identical
   / inference-driven difference (the record's committed reading differs from legacy — cite
   the segment and both readings) / presentation-rule difference (C1 mode display, §4.1 gate
   re-declaration, pedal suspension — cite the rule) / UNEXPLAINED. **UNEXPLAINED entries are
   the report's headline and each is investigated to a mechanism before delivery — an
   unexplained output difference at the pre-switch gate is exactly what this instrument
   exists to catch (#13/#15).** Commit the instrument + the classified report artifact
   (`tools/` home, stable filename); the report is the evidence base the user's switch
   ratification reads.
3. Byte-identity discipline unchanged: the flag is OFF by default everywhere; the dual-arm
   driver sets it only inside its own harness scope.

## Task 5 — doc sync + closing

`ARCHITECTURE.md` (the record path as-built behind the flag; the §4.1 declared constants;
the comparison instrument), `STATUS.md` closing entry (figures from the artifacts), the
OI-182 row execution note. Commits per change-class throughout; push origin.

## Report

Hashes per task; the per-consumer re-plumb checklist result (every CSV row's record-path
source — any row you could NOT satisfy is a FINDING with the row quoted, #12); the §4.1
constants table (value, legacy rate, record rate, residual, rationale); the pedal suspension
site; the dual-arm report's classification counts per output class + EVERY unexplained entry's
investigated mechanism; suite totals per commit; reuse-vs-new / what-retires (expected: the
record path reuses the module surface exclusively; the legacy path retires at the map, not
here); anomalies. Standing self-check before reporting.
