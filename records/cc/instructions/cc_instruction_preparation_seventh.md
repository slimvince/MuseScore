# CC dispatch — the preparation phase's SEVENTH batch: the split's consequences EXECUTED — the taught citation, the residue moved, the census re-pinned, and the finer `CLAUDE.md` measurement

> **Dispatch (Cowork, 2026-08-17).** Executes the four rulings of
> `cowork_rulings_2026_08_17_sixth_return.md`, taken at the sixth batch's verified return.
> **Written at a verified stop; nothing else is running.** The archiving wave for whole
> documents, the empirical findings ledger, the fact-gate, the curated boot list, the mining,
> the landing of the newly visible files, the eight-or-nine KIND-UNDERIVABLE callers'
> sitting, the prose-citation question, the residue surface's sitting, the rulings-sort
> sitting, and EVERY fate on the finer surface Task 3 delivers are NOT in this batch.
> **Read first (every session): the interim reading scope has EXPIRED** (the sixth batch's
> close records it) — read `C:\s\MS\CLAUDE.md`, `C:\s\MS\STATUS.md` and
> `C:\s\MS\BUILD_AND_TEST.md` as they now stand; an archive companion is reached only when a
> decision is re-opened. **Then read, in full:**
> `cowork_rulings_2026_08_17_sixth_return.md` (the whole authority);
> `cowork_rulings_2026_08_17_governing_surface_split.md` (the split rulings this completes);
> your own `cc_report_preparation_sixth.md` §2.b, §2.e, §6 and §7 (F33–F39 — the evidence
> Tasks 1–3 rest on); `tools/audit/governing_surface_split_application.json` (the 17
> left-at-site verdicts and the `STATUS.md` residue).
> The standing bars bind this batch whole: **no `src/` edit, no golden, no test changed,
> moved or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the
> analysis, no design, no repair, no derivation of any specification, no mining, no document
> archived or deleted as a file, no decisions-register entry retired or revived, NO EDIT to
> `CLAUDE.md` by Task 3, no open-items row created, flipped or discarded.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the
> end; record both states.** The expected start state: **61 run, 58 passing, THREE failing**
> — `gen_filing_convention_application.py --check` ([[OI-372]]),
> `gen_retirement_caller_check.py --check` (F37), and
> `gen_phase1w_legacy_verification.py --check` (F36) — 4 not run, 16 historical, zero STOPs.
> **A different start state is a STOP-and-report.** Verify every commit at the object and
> record the SHAs in the close. The reserved-word conventions bind all new prose. **The
> new-tool rule stands. The E-ordering rule stands. F3's reading rule stands**
> (`gen_cluster_dispositions.py --verify` the drift authority).

## Ruling ledger (quoted at the record, not paraphrased)

- **§1:** *"the tool's citation for that figure points at the companion
  (`OPEN_ITEMS_ARCHIVE.md`) ... The quote itself, and every verdict and population in the
  tool, are untouched."*
- **§2:** *"the next dispatch regenerates the census fresh at its own commit (`--at` that
  commit), every movement published and classed — the split's effect (the F35 namings), the
  discard's residue, and the tree's ordinary growth separated — and the artifact re-pinned
  there ... A census crossing confers CANDIDACY only."*
- **§3:** *"moves the 17 dated entries to `STATUS_ARCHIVE.md` under Ruling 4's own line, each
  entry READ before it moves ... each move byte-faithful."*
- **§4:** *"the span unit cuts inside blocks at the record's own ★ amendment markers and
  recognizes the amendment-record shape ... every doubt still defaults to STAYS AT SITE ...
  nothing moves until the user rules that surface."*

## Premise ledger

*(Every FACT pins content at content-addressed objects; no premise names a terminus commit or
asserts a changed-passage count — the F17 and F25 lessons.)*

- **FACT** — the sixth batch is complete, pushed and verified at the objects and at the values
  by the writing side; the 17 left-at-site verdicts and the per-file arithmetic are
  `tools/audit/governing_surface_split_application.json`; the `STATUS.md` residue (17
  pre-convention dated entries) is enumerated in the report §2.e and the close (c).
- **FACT** — F36's halt text and F37's measured-and-reverted regeneration:
  `cc_report_preparation_sixth.md` §6. The [[OI-289]] quote is byte-present in
  `OPEN_ITEMS_ARCHIVE.md`.
- **ASSUMPTION A1** — the working tree carries exactly ONE tracked modification:
  `cowork_handoff.md` (content: the twenty-third session-close block inserted above the
  twenty-second, and the twenty-second heading carrying its demotion marker — two parts of
  one act). `cowork_rulings_2026_08_17_sixth_return.md` and
  `cc_instruction_preparation_seventh.md` are untracked. **Check ordered (Task 0, first act,
  blob against blob by explicit hash):** any tracked difference whose CONTENT is outside that
  shape is a STOP-and-report.
- **ASSUMPTION A2** — after Tasks 1 and 2, exactly ONE red remains ([[OI-372]]'s tool), zero
  STOPs. **Check ordered (each task's last step):** any other red is classed by its measured
  cause; one outside the acts' own subjects is a STOP-and-report.
- **ASSUMPTION A3** — each of the 17 residue entries READS as a completed batch's dated
  record. **An entry that does not stays at site, is flagged, and is enumerated in the close**
  — the doubt default, not a batch STOP.

## Task 0 — land the ruling record, the handover block and this dispatch

1. **A1's check first, entirely at content-addressed objects.**
2. Commit exactly these THREE paths and no fourth: `cowork_handoff.md` (modified),
   `cowork_rulings_2026_08_17_sixth_return.md` (new),
   `cc_instruction_preparation_seventh.md` (new). Staged plainly; push; verify at the object.

**Registered expectation E0:** exactly 3 paths at `git diff-tree`; one modification whose
content matches A1's stated shape, two additions; no staging override.

## Task 1 — the taught citation and the residue, in ONE commit

1. **§1's one-line act:** `gen_phase1w_legacy_verification.py`'s citation for the [[OI-289]]
   figure points at `OPEN_ITEMS_ARCHIVE.md`; nothing else in the tool changes; its `--check`
   run and passing.
2. **§3's residue act:** the 17 pre-convention dated entries move verbatim from `STATUS.md`
   to `STATUS_ARCHIVE.md` (A3's read first, per entry), one compact dated pointer noting the
   act beside the existing clearing pointer; membership derived from the enumeration in the
   split-application artifact, never retyped; both-directions reconciliation proven on the
   established pattern (each moved entry byte-present in the archive exactly once and absent
   from the parent; the moved set accounts for the enumerated residue).
3. Affected artifacts regenerated under the ordinary bound (movement only in line coordinates
   or populations whose subject is a moved entry); commit, push, verify at the object.

**Registered expectation E1:** `gen_phase1w_legacy_verification.py --check` PASSES; the tool
diff touches its one citation and nothing else, proven blob against blob; every residue move
byte-faithful; A3 flags enumerated (zero is a valid count).

## Task 2 — the census regenerated and re-pinned, in ONE commit

1. `gen_retirement_caller_check.py` regenerated **at this task's own commit** (`--at` it, the
   tool's own interface), the artifact re-pinned there.
2. **Every movement against the previous committed reading enumerated and CLASSED in three
   named classes, separated:** (i) the split's effect — namings that left the governing
   record with archived spans (the F35 nine among them); (ii) the fifth batch's discard
   residue; (iii) the tree's ordinary growth (new tools, new records — the ENUMERATOR and
   parsed-source growth). **A movement fitting none of the three is a STOP-and-report.**
3. The classed movement published as a generated artifact beside the census; the check run
   and passing at the new pin; commit, push, verify at the object.

**Registered expectation E2:** the check PASSES at the new pin; the movement artifact classes
every difference with zero unclassed; the KIND-UNDERIVABLE population's new value is
published as data (candidacy only — no fate, no archiving, no flag acted on).

## Task 3 — the finer `CLAUDE.md` measurement, read-only

1. The span decomposition extended (or a successor tool written) per §4: cuts inside blocks
   at ★ amendment markers; recognizes the amendment-record shape (ruled-date header,
   former-wording quote, declined-alternatives paragraph, accepted-costs paragraph); every
   recognizer's marker published beside its verdict; the per-class byte accounting to the
   character (the existing STOP).
2. The reader/anchor inventory refreshed for `CLAUDE.md` at this tree (the split moved
   anchors; the finer surface must carry current reach).
3. **The ratification surface generated**, not hand-typed:
   `ratification_surfaces/cowork_claude_md_finer_split_2026_08_17.md` — per-span proposed
   fates under §5(E)'s ruled test, the doubt default STAYS AT SITE written in, every
   doubt-defaulted span marked, the banner stating NOTHING HERE IS RULED. **No edit to
   `CLAUDE.md`.**

**Registered expectation E3:** every span classed with evidence and accounted to the
character; the surface generated from the artifacts; `CLAUDE.md` byte-unchanged, proven by
hash; new tools registered under the new-tool rule with `--check` forms.

## Task 4 — the close

One `STATUS.md` pointer entry per task — and, per the standing Ruling-4 bound, the same act
moves the sixth batch's entries to `STATUS_ARCHIVE.md` (`gen_status_batch_bound.py`). The FULL
close appended to `cowork_away_returns.md` (a PREPARATION SEVENTH BATCH section) — both guard
states, every SHA, every expectation graded, every A3 flag, every problem declared, the
carried findings restated for the retrospective (F1–F39, the E3 ordering defect, the A1
premise error — surfaced, none rowed; the row bar stands whole this batch). The report
`cc_report_preparation_seventh.md`, an ordinary tracked file. Commit, push, verify at the
object. **The E-ordering rule:** the end-state guard run is taken AFTER the Task 4 commit
exists, its output and the final SHAs landing in ONE FURTHER commit.

**Registered expectation E4:** at the tree carrying the close, a fresh full guard run
re-derives — **every run check passing except [[OI-372]]'s tool**, zero STOPs — run and read,
not inferred, its values committed only after the run.

## What this batch does NOT do

- **No fate on the finer surface executed** — Task 3 proposes; the user rules; a LATER
  dispatch executes. **No edit to `CLAUDE.md`.**
- **No caller flag acted on** — the census's crossings confer candidacy only; the callers
  sitting reads the current population when the user holds it.
- **No sole-carrier member discarded; the 62 not ruled; the residue and rulings-sort
  surfaces stand awaiting their sittings; the prose-citation question stays open.**
- **No empirical findings ledger, no fact-gate admission, no curated boot list; no
  completion claim about the superseded phase-1 program; no derivation, design, repair or
  pilot act; no `src/` change, golden, test, corpus or robust-stop movement; no measurement
  of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly
  as found; [[OI-179]] stays OPEN and GATES; F3 stays surfaced, unfixed and unrowed.
