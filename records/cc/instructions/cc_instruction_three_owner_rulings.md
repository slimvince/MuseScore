# CC dispatch — the three owner rulings of 2026-08-07 (third batch)

> **Status: ACTIVE DISPATCH, written 2026-08-07 (Cowork).** Read IN FULL.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_three_owner_rulings.md`.
>
> **★ THE FREEZE IS NOT RELAXED.** No mechanism changes this wave. Everything found is rowed
> and left, except a finding whose subject bears on the analysis, its inputs, or a measurement
> tool a measurement depends on — SURFACED, not acted on (D-641). An establishment obligation
> (#19) always gates and is always surfaced.
>
> **★ NO FIGURES (D-431) AND NO STATE NOT READ (#17a).** No `src/` change, no goldens, no
> corpus, no `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to
> inference, no design. Phase 1 under D-231. **Never bash to read working-tree files (D-253).**
>
> **★ Same-commit rule (D-230)** for every register movement. **Commit only when the user
> asks** — leave the tree uncommitted at close and record the worktree enumeration.
>
> **★ Item 2 of the finish line is not started. Phase 1's completion statement is not written,
> drafted, or partially written.**

## 0a. THE RULING LEDGER — three rulings, RULED by the user 2026-08-07 (third batch)

Ruled on Cowork's decision surface of 2026-08-07 ("I agree with your recommendations", after
the full surface with alternatives was delivered as user-visible text). Restated in full
(D-643).

- **R1 — D-281 is homed in `BUILD_AND_TEST.md`.** The batch measurement tool's output-schema
  floor (root/bass/quality/bass-is-root emitted on every alternative, below which the corpus
  figures silently revert) is a contract of the measurement tools, and `BUILD_AND_TEST.md` is
  the authoritative document for those tools — the proper layer (#7). The excluded alternative,
  recorded with the ruling: gate block (A) in `CLAUDE.md` governs how figures are READ and
  already delegates commands and tooling to `BUILD_AND_TEST.md`, so homing the schema there
  would blur the layer and restate what is pointed at. One home only (#6); a pointer from the
  gate block is permitted, a copy is not. The D-645 licence covers `BUILD_AND_TEST.md` for
  homing acts, so no new authorization is needed.
- **R2 — D-538 is RATIFIED AND RULED SUPERSEDED in the same act.** The entry (LEGACY subject:
  the dormant pipeline's four-step staged landing with a corpus check per step) is ratified as
  correctly recorded, and its content is ruled superseded by the general discipline the project
  later stated: principle #14 (every behavior change one revertible, provenance-stamped,
  user-ratified commit) and gate block (A)'s measured non-increase / re-baseline discipline.
  Under D-642 the C1 obligation moves to those successors, which are homed in `CLAUDE.md`; the
  entry's early concrete form stays in the register verbatim (#12), and the ⚠ LEGACY mark
  stays. The excluded alternative, recorded with the ruling: homing it beside the gate block
  would put a second copy of in-force discipline into a live specification (#6) under a LEGACY
  subject a reader could misapply.
- **R3 — D-539 is homed in `cowork_engage_arc_plan.md`, beside the MEASURE-BEFORE-BUILD
  gate.** The structural/fitted/ceiling decomposition method is an order-of-work rule; the arc
  plan is the ratified, delegated contract for the order of work (the delegation written into
  `CLAUDE.md`'s principles section points at it and does not restate it, #6), and D-277 — the
  method's recorded descendant — lives there. The excluded alternative, recorded with the
  ruling: homing it in `CLAUDE.md` beside the #17 funnel would begin restating what the
  governing file delegates. **The user's ruling is the authorization for this one edit of
  `cowork_engage_arc_plan.md`** — a ratified contract document otherwise outside every licence;
  the edit is the homing act and nothing else.

**None of R1–R3 authorizes a fix to the analysis, a design, or an inference change.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork this session (2026-08-07):**

- **F1.** The three entries' full register records, read at `decisions/group_U.md` (D-281),
  `decisions/group_G.md` (D-538), `decisions/group_C.md` (D-539), including D-538's provenance
  naming it an early form of #14 + the hard stop, and D-539's naming it D-277's ancestor.
- **F2.** The D-645 licence scope (homing acts on the three named files, `BUILD_AND_TEST.md`
  among them): `cowork_audit_protocol.md`, the D-645 section.
- **F3.** The three entries re-derive as the licensing class's residue:
  `tools/audit/decisions/item1_rehome_blocker.json` at HEAD.

**ASSUMPTION — each checked BEFORE the act it licenses, with a STOP if refuted:**

- **A1.** That `BUILD_AND_TEST.md` has a determinate owning section for the batch measurement
  tools' contract. → check before writing; if the document has no section that owns the
  concern, STOP and report what section structure the homing would need — do not invent one
  silently.
- **A2.** That D-538's successors are locatable as homed register entries (the entry recording
  principle #14, and the entry recording the gate-block hard-stop discipline), so the
  supersession's `superseded_by` can name identifiers. → search the register first; if either
  has no entry, the supersession names the specification clause by its home (`CLAUDE.md`,
  principle #14 / gate block (A)) and reports that shape — a STOP only if neither an entry nor
  a homed clause exists.
- **A3.** That the arc plan's MEASURE-BEFORE-BUILD section STATES RULES (D-430's kind half) so
  the homing lands in a rule-stating section. → read the section before writing; if it records
  findings rather than stating rules, STOP and report — the remedy would be a different
  section of the same document, and that is a reading question, not a licence one.

## 1. Task 1 — Apply R1 (D-281 → `BUILD_AND_TEST.md`)

Discharge A1. Home the entry: the schema floor stated in that document's own voice, with its
defense (the recorded three-week silent loss and #19 applied to a measurement tool), former
home/class/verbatim preserved (#12), provenance in the register's fields only. Adds only — no
existing line of `BUILD_AND_TEST.md` modified or deleted; checked at the diff.

## 2. Task 2 — Apply R2 (D-538 ratified + superseded)

Discharge A2. Record the ratification (2026-08-07, user) and the supersession in the register
data in the same commit (D-230); status moves to SUPERSEDED BY with the successors as A2
resolved them; the LEGACY mark and the verbatim stay. No specification is edited for this
entry — that is the ruling's point.

## 3. Task 3 — Apply R3 (D-539 → `cowork_engage_arc_plan.md`)

Discharge A3. Home the entry beside the MEASURE-BEFORE-BUILD gate, in the contract's own
voice, with its defense (the thrice-paid lesson its record states), former home/class/verbatim
preserved (#12). Adds only — no existing line modified or deleted; checked at the diff.

## 4. Task 4 — Close

Regenerate what follows the register — the blocker artifact, the routes artifact, the finish
line (its STOP must account for every entry that leaves item 1: two homed, one discharged by
supersession). Re-aim any drifted anchors per citation. Guards at the tree with the derived
list; **fix nothing**. Record the worktree enumeration to a named artifact. Run
`tools/audit/process_check.py` over this dispatch. `STATUS.md` gains one POINTER entry. Leave
the tree uncommitted.

## 5. Accepted outcomes

**A1 or A3 finding no rule-stating owning section is a STOP with the section question
reported.** **A2 resolving to specification clauses rather than register entries is a result,
reported, not a STOP.** **The finish line's STOP firing on an unaccounted entry is a STOP,
reported with the entry.**

## 6. Self-check (D-434) — run by Cowork before release

- **Ruling ledger:** three rulings in full, each with its excluded alternative; R3 carries its
  one-edit authorization explicitly.
- **#17(a):** three facts read at the objects this session; three assumptions with ordered
  checks and STOPs.
- **Principles:** #6 at both excluded alternatives and the one-home rule; #7 at both homing
  targets; #12 at every preservation; D-642 at the supersession's discharge; D-431 — no figure
  in this dispatch; D-253 restated in the header.
- **Scope:** two homing acts and one register status change; no other edit of any file.
