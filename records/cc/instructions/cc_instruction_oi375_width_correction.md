# CC dispatch — OI-375 closed: principle #21's consequence sentence restated at its own width, saying what is actually absent

> **Status: ACTIVE DISPATCH, written 2026-08-11 (Cowork), at a verified STOP** — the previous
> batch completed, four commits pushed; Cowork verified A1's and A3's findings at the objects and
> confirms OI-375 is a real defect in text Cowork itself drafted. Nothing is running.
>
> **Read IN FULL, and read FIRST:** `CLAUDE.md` principle #21 in full, including the D-474 block
> and the annotation this dispatch corrects; `open_items/OI-375.md`;
> `cowork_scratch_2026_08_08/draft_oi179_ceiling_measurement_surface_2026_08_08.md` lines 24–28
> and 60–63; `open_items/OI-179.md`'s dated notes including the proxy-premise note.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_oi375_width_correction.md`. Acts dated
> from the clock; commit messages via `-F`; identifiers verified at their own indexes before use.
>
> **★ All standing rules as adopted.** D-253 in every dialect. NO TRANSCRIBED VALUES (D-431).
> Hold-don't-guess. **Read-only on the analysis: no `src/` edit, no behaviour change, no golden,
> no corpus of scores, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement built
> or run, no design.** D-231 and #8 stand. **Phase 1's completion statement is not written, not
> drafted and not partially written.** Commit and push per task boundary; `origin` only.

## 0a. THE RULING THIS EXECUTES

**User, 2026-08-11, on a surface with three alternatives, their principled pros and cons and their
ratings against the ultimate objective: KEEP THE WIDTH AND RESTATE WHAT IS ACTUALLY ABSENT.** The
two declined options are recorded because an excluded alternative is evidence about the choice:
*narrowing the sentence to our gate repertoire* was declined because it would place a narrow
reading of *this repertoire* nine lines below the same phrase at a wide width, which is the
ambiguity that produced the defect; *striking the consequence sentence* was declined because a
reader of #21 would then still infer the ceiling might come from citation, which is what D-474
exists to prevent.

**The defect being corrected is Cowork's**, in text Cowork drafted and the user ruled: the
consequence sentence asserted the ceiling is not obtainable from inside any single corpus in this
repertoire, while the D-474 block nine lines above records two corpora whose duplicate or
dual annotations exist and were never computed.

## 0b. THE PREMISE LEDGER (#17a)

**FACT — verified by Cowork at the objects.** `CLAUDE.md:169` — *"TAVERN released duplicate
annotations but published no such number"* — and `:172` — Dilemmadata *"identifies dual-annotated
pieces and computes nothing"*. The design surface at lines 26–28 — both *"computable BY US as
additional legs, each with a declared domain caveat"* — and at 60–62 — TAVERN's duplicates
*"within-corpus, true inter-annotator, off-repertoire"*, Dilemmadata's *"domain to be checked at
the data"*, and *"Neither substitutes for the primary leg; each brackets it."*

**ASSUMPTION — checked before the act resting on it; a refutation is a STOP.**

- **A1.** The design surface is a **Cowork reading surface, NOT ratified**, so its classification
  of TAVERN is attributed rather than asserted, and neither computable route is claimed
  established (#19). *Check: the replacement text attributes it and claims no establishment.*
- **A2.** The corrected text **points at D-474's own per-corpus list rather than re-enumerating
  it** (#6). *Check: no corpus's standing is restated that the block above already carries.*
- **A3.** Whether the annotation carries a register identity of its own. *Check at the register.
  If it does, its verbatim is re-taken the way D-174's was; if it does not, none is created.*

## 0c. THE TASKS

**Task 1 — the correction. FIRST, atomic.** Under D-674 `CLAUDE.md` is a live governing surface,
so the **BODY is corrected** and the **former wording is preserved in place (#12)**. Replace the
consequence sentence and its "NOT claimed" clause with the ruled text below, written in the
principle's own voice. Everything else in the annotation — the reply's facts, BCMH joining the
consensus-built class, the errata limb outstanding — **stands unchanged**.

> **The consequence for this principle, stated at the width the D-474 block above uses.** What is
> absent is a **published** per-axis agreement value, and that absence is unchanged. What is **not**
> absent is computability: the block above already records two corpora whose duplicate or
> dual annotations exist and were never computed, and no corpus's standing is restated here (#6).
> **So the ceiling is not obtainable from inside any corpus of OUR GATE REPERTOIRE** — BCMH and the
> Mozart sonatas are consensus-built and can never yield it, and ABC has no overlap by design —
> **while within the wider enumerated set it is computable by us, off-repertoire or of unchecked
> domain.** OI-179's design surface — a Cowork reading surface, **not ratified** — classes TAVERN's
> duplicates as within-corpus, true inter-annotator and off-repertoire, and Dilemmadata's domain as
> still to be checked at the data; **neither computable route is established (#19)**, and neither
> has been checked at its data. **That is why OI-179's on-repertoire leg is a comparison BETWEEN
> annotation traditions and therefore a PROXY, and why the off-repertoire legs bracket it rather
> than replace it.** The choice this leaves — the right quantity on the wrong music against a proxy
> quantity on the right music — is a **phase-2 design decision and is not settled here.**
> *NOT claimed:* that no corpus anywhere could supply it; that either computable route will yield
> a usable value; or that the domain caveats have been discharged.

Then flip **OI-375** with provenance (rule (d)), its detail file gaining a dated note. Commit and
push.

**Task 2 — the rider on the proxy premise.** One line added to OI-179's proxy-premise note: **a
non-proxy route exists off-repertoire and BRACKETS the proxy rather than replacing it**, so the
premise's validation question is how tightly an off-repertoire true value bounds an on-repertoire
proxy — an open question, not an answered one. Nothing else in the note changes. Commit and push.

**Task 3 — the close.** One `STATUS.md` pointer entry per completed task, nothing else in that
file. Append the close to `cowork_away_returns.md`. Report at the objects: commits and push state,
A3's finding, and the former wording's preserved location.

## 0d. WHAT IS DELIBERATELY NOT DONE

No measurement is built, designed, scoped or run and no ceiling value exists. The two surviving
defects on BCMH are untouched. The third option on #21 — qualifying what the principle DEMANDS —
still waits on the desk simulation. The errata re-ask remains the user's and is not performed.
**OI-179 stays OPEN and GATES** under #19.

## 0e. STOP RULES

Halt with a STOP in `cowork_away_returns.md` if: the design surface does not say what §0b quotes;
the annotation's register identity is ambiguous; the former wording cannot be preserved in place
without disturbing the D-474 block; or a guard goes red for a cause that is neither this
dispatch's own edits nor already recorded.

---

*Provenance: Cowork, 2026-08-11, at the verified STOP following the previous batch. The defect
corrected here originated in Cowork's own drafting; the finding is CC's, made while applying the
text. The standing writing-side self-check was run against this file before release (D-434).*
