# CC dispatch — LAND the two 2026-08-28 Cowork sessions' work and prepend the eightieth handoff entry. No repair, no design, no fill-in. Boot NO session.

> **★ THIS IS THE ACTIVE DISPATCH. Dated dispatch note, 2026-08-28: written and released the same
> day. It carries no `⏸ PARKED` banner and none is to be added.** It may go straight to a session.
>
> **★ WHO WROTE IT, DECLARED SO YOU CAN WEIGH IT.** The writing side that wrote this is **at its
> handover threshold with five counted errors**, three of them in dispatch writing: it edited a live
> dispatch, it attributed a quotation to a file it had not opened, and it wrote a footprint assumption
> its own tasks broke. **All three are answered structurally below rather than promised against.**
> **Read this dispatch's own bars sceptically and STOP on a contradiction rather than resolving it in
> this dispatch's favour.**
>
> **★ WHAT THIS BATCH IS.** Task 0 pins and establishes. Task 1 prepends the eightieth handoff entry
> and lands the untracked documents of the two 2026-08-28 Cowork sessions. Task 2 closes. **That is
> the whole of it. It repairs nothing, designs nothing, derives nothing and binds nothing.**
>
> **★ WHY IT EXISTS, IN ONE SENTENCE.** Two sessions' worth of ruling records, decision surfaces, the
> framework document draft, the cross-layer transfer list and **three unlanded handoff entries**
> currently exist only as untracked files on one machine — **in git nowhere and pushed nowhere** —
> and the standing end-of-session cadence is that work is pushed for backup.
>
> **★ THE HANDOFF CARRIES THREE UNLANDED ENTRIES AFTER THIS PREPEND, NOT ONE.** The previous batch
> established at the objects that `cowork_handoff.md`'s working-tree copy already carries the
> **seventy-eighth AND seventy-ninth** entries unlanded. **This batch adds the eightieth. That is
> expected, is not a defect, and is not a STOP.**
>
> **★ WORDS USED HERE.** *The staging file* — `cowork_handoff_entry_eighty.md`, a root-level file
> holding the eightieth entry's text and nothing else, written that way because the writing side has
> no shell and cannot prepend to a ~930 KB file through the file tools. **It is deleted by this batch
> after the prepend so the entry has one home (#6).**
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md` —
> CONDITIONAL, and **this batch does NOT meet the condition** (it builds nothing, runs no test, runs
> no measurement tool): rule (a)'s `gating_ids` only. **(2)** `cowork_handoff.md`, **ITS CURRENT ENTRY
> (the seventy-ninth) and the entry below it**, AND **`cowork_handoff_entry_eighty.md` WHOLE** — it is
> the entry this batch lands and it names everything this batch touches. **(3)**
> `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL — every `###` section carrying the
> standing-clause marker. **You do NOT open:** any of the three sealed placement-sample files; either
> pack directory's contents; `ARCHITECTURE.md`.
>
> **The standing bars bind this batch whole:** **no `src/` edit**, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, **no measurement of the ANALYSIS**,
> no design, no repair, no derivation of any specification statement, **NO SESSION BOOTED**, no
> document archived or moved AS A FILE, **no open-items row created, flipped or discarded**, **no edit
> to any governing document, any register entry or any register source**, and **no edit to the CONTENT
> of any file this batch lands — every landed path is committed exactly as it stands on disk.**
> **The forward-bound re-aiming of `gen_status_batch_bound.py --apply` in Task 2 is EXCEPTED BY NAME**
> under Ruling 5 of `cowork_rulings_2026_08_26_amendment_landing_sitting.md`.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first act (CHECK mode) and again at
> the end (write mode, committed as the artifact of a real run). Verify every commit at the object by
> explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and a working-tree
> `git diff` are denied by the armed guard — use the per-path explicit-hash forms and
> `tools/audit/changed_paths.py`. Plain single-quoted `git commit -m '…'` subjects; where a subject
> contains an apostrophe, use the standard escape and verify the subject at the object.**

## Premise ledger

**★ THE WRITING SIDE HAS NO SHELL AND ASSERTS NO GIT-OBJECT VALUE.** Everything below was read with
the file tools on bridge-staged snapshots on 2026-08-28. **No commit, blob or tree value is stated
anywhere in this dispatch and none may be taken from it.** **The tip HAS MOVED** — the arm-and-site
fill-in batch landed four commits and pushed — **and this dispatch deliberately states no value for
it. Establish it at the object.**

- **FACT — the staging file exists** at `cowork_handoff_entry_eighty.md`, root level, untracked, and
  **begins with its own `---` and ends with a blank line**, so it is inserted whole and unchanged.
- **FACT — `cowork_handoff.md` begins** with its `# Cowork Session Handoff …` title line, then a blank
  line, then the `---` opening the seventy-ninth entry.
- **FACT — the guard set's three known failing checks** are `gen_filing_convention_application.py
  --check`, `decisions/apply_soft_discard.py --check` and `decisions/apply_residue_discard.py --check`.
  **The register repair that would clear the last two is PARKED and is not this batch's business.**

- **ASSUMPTION A1 — the working tree, by SHAPE and never by count.** *(The writing side has no shell
  and the untracked population is in the hundreds; a count here would be an assertion it cannot
  make.)*
  1. **`cowork_informed_session_brief_framework.md` IS modified** — the previous session's revision,
     still unlanded. **This batch lands it as it stands and edits nothing in it.** *(The §8 move is
     NOT performed here; it belongs to the parked register dispatch.)*
  2. **`cowork_handoff.md` IS modified** — carrying the seventy-eighth and seventy-ninth entries
     unlanded. **This batch prepends the eightieth to it.**
  3. **A third TRACKED modification outside `STATUS.md` and the artifacts Task 2 regenerates is a
     STOP-and-report.**
  **Enumerate with `tools/audit/changed_paths.py` and REPORT what you find before acting.**
- **ASSUMPTION A2 — the guard state at the start.** The three known failing checks, **plus
  `gen_evidence_pin_membership.py --check` RED** — caused by this batch's own untracked inputs, that
  derivation's population being the file system rather than the git index. **A fifth failing verdict
  is a STOP-and-report.** *(Measure it. The last two batches measured this check on opposite sides of
  green and each said so; carry neither statement.)*
- **ASSUMPTION A3 — this batch's footprint is exactly what its own orders move, and nothing else.**
  **Stated as a list of the acts, not as a blanket** — *(the writing side's fifth counted error was a
  footprint assumption its own tasks broke, and this is that lesson applied)*:
  the paths Task 1 lands; `cowork_handoff.md` as prepended; the staging file deleted;
  `tools/audit/evidence_pin_membership.json` regenerated; and in Task 2 `STATUS.md`,
  `STATUS_ARCHIVE.md`, `tools/audit/status_batch_bound.json`, `tools/audit/gen_status_batch_bound.py`
  (**the named carve-out**), `tools/audit/session_start_read_size.json`, the report, and
  `tools/audit/guard_state.json`. **Movement at any path outside that list is a STOP-and-report.**

## The declared start state

**Four failing guard checks** — the three known, each for its own recorded cause, **plus
`gen_evidence_pin_membership.py --check` RED, caused by this dispatch's own untracked inputs.** A
fifth is a STOP-and-report.

## Task 0 — pin, then establish. Nothing is written in this task.

1. **PIN THIS INSTRUCTION AND THE STAGING FILE FIRST, BEFORE READING EITHER FOR CONTENT.**
   `git hash-object -w` each, report both blob identities, and **take every subsequent read of both
   from `git cat-file blob <hash>` into a scratch file outside the repository.** *(The writing side
   declares it does not touch either while this batch runs.)*
2. **Establish the tip** at the object, and `origin/master` beside it.
3. **Run the FULL guard set in CHECK mode** and grade A2 from that run.
4. **Enumerate with `tools/audit/changed_paths.py`** and grade A1.

**E0:** both blob identities reported; the tip and `origin/master` at the object; A1 and A2 graded
from measurement.

## Task 1 — prepend the entry, land the documents, PUSH

1. **THE PREPEND, AND IT IS ADDITIONS-ONLY OR IT DOES NOT HAPPEN.** Insert the staging file's content
   **whole and unchanged, immediately after the blank line following `cowork_handoff.md`'s title
   line** — so the eightieth entry sits exactly where the seventy-ninth sat and the seventy-ninth
   follows it. **PROVE ZERO deletion lines at the blob-to-blob diff. Any deletion is a
   STOP-and-report and nothing is committed.**
2. **THEN DELETE the staging file from the working tree.** It is untracked and has never been
   committed, so **this is a plain file removal and NOT a `git rm`** — and it is the only file this
   batch deletes.
3. **ESTABLISH the handoff's entry count at the object** with the loose pattern
   `^## .*COWORK SESSION CLOSE`, and **if you also report a strict-pattern figure, say which pattern
   produced which number.** **This dispatch asserts no count.**
4. **LAND THESE UNTRACKED ROOT-LEVEL PATHS, each exactly as it stands on disk.** Confirm each is
   present and untracked at Task 0's enumeration; **a path in this list that is absent, or already
   tracked, is a STOP-and-report:**
   `cowork_framework_document_draft_2026_08_28.md`,
   `cowork_register_blocker_surface_2026_08_28.md`,
   `cowork_section8_breach_surface_2026_08_28.md`,
   `cowork_informed_brief_provenance.md`,
   `cowork_section8_bar_record_2026_08_28.md`,
   `cowork_register_rule_c_suspension_2026_08_28.md`,
   `cowork_rulings_2026_08_28_framework_delta_sitting.md`,
   `cowork_cross_layer_transfer_list.md`,
   `cc_instruction_register_baseline_repair.md` (**parked; landed as the record of what was written,
   NOT run or revalidated**),
   `cc_instruction_landing_2026_08_28.md` (this dispatch).
   **Plus the tracked modification `cowork_informed_session_brief_framework.md`, landed as it stands.**
5. **★ REPORT, AND DO NOT LAND, any OTHER untracked root-level file matching `cowork_*.md`,
   `cc_instruction_*.md` or `cc_report_*.md` that item 4 does not name.** The arm-and-site batch's own
   dispatch, report and fill-in may or may not already be tracked — **establish which, report it, and
   land any that is untracked, naming it in the report as an addition this dispatch did not
   anticipate.** **Nothing outside those three patterns is landed by this batch at all.**
6. **REGENERATE `tools/audit/evidence_pin_membership.json`** and **MEASURE its diff against the
   committed blob before accepting it.** Movement beyond this batch's own landed paths is reported
   rather than absorbed.
7. **Commit; the subject, exactly:**
   `record: land the two 2026-08-28 Cowork sessions — the framework-delta rulings, the register blocker and §8 surfaces, the rule-(c) suspension, the cross-layer transfer list, and the eightieth handoff entry`
8. **PUSH**; verify at the object. **`gen_evidence_pin_membership.py --check` must PASS.**

**E1:** the prepend proven additions-only with ZERO deletions; the staging file gone; the entry count
established with its pattern named; every path of item 4 committed and no path outside items 4 and 5
committed; the membership check passing; `origin/master` at the commit.

## Task 2 — the close

1. One `STATUS.md` pointer entry per task, per the OI-222 pointer convention — **no count, no identity
   and no rendered value restated** (**D-431**). The previous batch's entries move through
   **`gen_status_batch_bound.py --apply`** — **the named carve-out** — and
   `gen_session_start_read_size.py` regenerates.
2. The report is **`cc_report_landing_2026_08_28.md`**: both guard states, every SHA, E0 and E1
   graded, A1–A3 graded, declared departures, the established entry count with its pattern named, the
   measured membership movement, **the full list of what item 5 found**, and the plan lines.
3. **★ THE PLAN LINES, AND THE FIRST ONE IS THE POINT OF SAYING THEM.** **This batch closes nothing
   and moves the framework phase not at all** — it is a backup act. The phase's postcondition is a
   **ratified** framework, held until the user's external research list arrives. Then, owed and NOT
   done: **§9.0's decision surface** — the grain of a unit, the phase's first ratified finding, which
   no surface exists for and which is the largest owed item that moves the plan; **the placement
   test**, carrying the record's twice-stated condition that a session with a shell precede reliance
   on its results; **the phase's retrospective** (§3.9 of the phase-definition surface), which must
   land before the next phase opens and does not exist; and **everything the eightieth entry's
   backlog carries**, including the eleven quarantined audit questions that belong to the AUDIT and
   must not be worked before it.
4. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; one further commit carries it.
5. Commit, push, verify; then the one further commit with the end-state guard run.

**E2:** at the tree carrying the close, a fresh full guard run — **the three known failing checks and
no others, zero STOPs** — committed only after the run that produced it.

## What this batch does NOT do

- **It edits no landed file's content.** Every path is committed exactly as it stands.
- **It performs no repair.** The register blocker stands; its dispatch is landed as a parked record
  and is neither run nor revalidated. **The §8 move is not performed.**
- **It writes no decisions-register entry and allocates no `D-NNN`.** The register cannot accept one;
  `cowork_register_rule_c_suspension_2026_08_28.md` is the route and **its list is still underived.**
- **It creates no open-items row and allocates no finding number**, though it lands documents that owe
  several.
- **It does not touch the eleven quarantined audit questions**, which are reserved to the AUDIT.
- **No `src/` change, no test, no golden, no build, no measurement of the analysis, no session booted,
  no pack rendered, no design, no framework text.** None of the three sealed placement-sample files is
  opened; `ARCHITECTURE.md` is not opened.

## The writing side's self-check over this dispatch

1. *Principles touched:* **#12** — the prepend is additions-only and proven so before commit; nothing
   is rewritten and the only deletion is the staging file, which is ordered by name and whose content
   survives inside the handoff. **#6** — the staging file goes so the eightieth entry has one home.
   **#13** — the author's own error count is stated at the head, because a reader weighing this
   dispatch should know it. **#17f / D-431** — no count and no git-object value asserted anywhere;
   the entry count and the tree state are ordered measured.
2. *Conventions:* American English; no self-invented label; no music-theory word arises in this
   batch's subject matter.
3. *Figures and premises:* every premise was read at the files by the writing side on 2026-08-28 and
   is ordered re-established at the objects.
4. *File-tools rule:* the writing side read only through the file tools and ran **no shell command of
   any kind** — the user barred it for this session — **so it verified no git object and this dispatch
   relays none.**
5. *Uncertainty:* the measured-not-predicted items are the tree state, the entry count, the membership
   movement, and **which of the arm-and-site batch's own three files are already tracked** — item 5
   exists because the writing side could not establish that without a shell and **declined to guess.**
6. *The three failures this dispatch is written against, each answered structurally:* the pin at
   Task 0 and the writing side's declared restraint answer the live-edit failure; **A3 stated as a
   list of its own ordered acts** answers the footprint failure; and **no sentence in this dispatch is
   quoted from a file the writing side did not open** answers the mis-attribution failure.
