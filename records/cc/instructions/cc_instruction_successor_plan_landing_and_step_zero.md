# CC dispatch — land the 2026-08-21 record, then STEP ZERO of the ratified successor plan: the document set derived, the July screen widened over it, the pollution distribution published

> **Dispatch (Cowork, 2026-08-21, the thirty-eighth Cowork session).** Executes Ruling 6 and Ruling 7
> of `cowork_rulings_2026_08_21_successor_plan_sitting.md`, and the landing every 2026-08-21 record
> has been waiting for. **Written at a verified stop; nothing else is running and no dispatch was
> active when this was written.** The tip was read by the writing side with `git show --stat` at the
> explicit hash `3cfb220b1d` and at nothing else.
>
> **★ WHAT THIS BATCH IS.** Task 0 lands the whole 2026-08-21 record and PUSHES — the push the
> evaluation dispatch withheld until both evaluations had returned; both have. Tasks 1–3 are **step
> zero of the ratified successor plan** (`cowork_specification_reconstruction_plan_successor_2026_08_21.md`
> §4, §5; ruled at the sitting record above, Rulings 6 and 7): the specification document set is
> DERIVED from `ARCHITECTURE.md`'s admitted delegations, the July screen's population is WIDENED over
> that set with its method untouched, and the per-document pollution distribution is published.
> **Step zero is a PREPARATION-PHASE act and the ONE mechanism the plan widens** (the plan's §4, and
> guardrail 3 declared rather than breached). **No pilot act, no derivation of any specification, no
> frame, no ledger.**
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional, and explicitly NOT demoted. **(3)** `BUILD_AND_TEST.md` is a
> CONDITIONAL read — **and this batch MEETS the condition**, because it runs the guard set and the
> audit generators, so read it in full. **(4) DO NOT READ THE WHOLE OPEN-ITEMS INDEX.** Rule (a)
> reads the DERIVED GATING ANSWER narrowed to its identity list —
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`. The INDEX
> remains the AUTHORITATIVE STATUS SURFACE.
>
> **★ AND THE STANDING CLAUSE BINDS THIS ONE: read `cowork_handoff.md`, ITS CURRENT (THIRTY-EIGHTH)
> BLOCK.** **The findings series stands at F88 when this batch begins** — no number was allocated by
> the thirty-fifth, thirty-sixth, thirty-seventh or thirty-eighth sessions, and none by the two
> evaluations. **This batch allocates NO finding number either** — see *Findings in this batch*
> below, which is the plan's repaired guardrail 2 (Ruling 9) applied.
>
> **★ AND READ THE STANDING CLAUSES AT THE FILE, NOT AT A COUNT.** `cowork_audit_protocol.md`'s
> dispatch-protocol section is to be read **IN FULL** — every `###` section carrying the membership
> marker binds this dispatch and your report — before Task 1 writes anything. No number is stated
> here and none is to be written anywhere in this batch.
>
> **Then read, in full and in this order:** `cowork_rulings_2026_08_21_successor_plan_sitting.md`
> (this batch's authority — **Rulings 6, 7, 8 and 9 above all**);
> `cowork_specification_reconstruction_plan_successor_2026_08_21.md` (the ratified plan, read WITH
> the sitting record — its §0 terms, §4, §5, §8 and §12 decisions 6 and 7 above all); the thirty-sixth
> and thirty-seventh handover blocks of `cowork_handoff.md` (below the thirty-eighth; they name every
> file Task 0 lands and the state each was left in); and the docstrings of
> `tools/audit/gen_doc_change_candidates.py`, `tools/audit/gen_period_stratum_split.py` and
> `tools/audit/gen_july_screen.py` — **the three tools whose population and method step zero inherits
> and may not re-invent.**
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/doc_change_candidates.json` and `tools/audit/doc_change_candidates_hunks.jsonl` (the
> candidate enumeration — **read and never regenerated**); `tools/audit/period_stratum_split.json`
> (the existing screen population — **extended by Task 2, its existing fields byte-unchanged**);
> `tools/audit/july_screen.json` and `tools/audit/july_screen_report.md` (the existing screen —
> **extended by Task 3, its existing sixty-eight verdicts byte-unchanged**);
> `tools/audit/decisions/phase1p_delegation_bar.json` and `tools/audit/decisions/home_classification.json`
> (**read as SEED only** for Task 1, never as the population); `tools/audit/evidence_pin_membership.json`
> (Task 0's subject); `tools/audit/nongating_apparatus_rows.json` (**read and never written**);
> `tools/audit/guard_classification.json` and `tools/audit/guard_state.json`.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS built,
> designed, scoped or run (step zero measures the DOCUMENTS' provenance and nothing about the
> analysis), no design, no repair, **no derivation of any specification, no frame, no pilot act, no
> fact-gate admission, no ledger**, no document archived, moved or deleted AS A FILE, **no open-items
> row created, flipped or discarded**, **no edit to any screened document** (`ARCHITECTURE.md`,
> `docs/scoring_model.md` and every document-set member are READ and never written, however plainly
> wrong a passage is found — the plan's §13), and **no caller flag or candidacy acted on**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end.**
> The declared start state is below and is stated **at the tree you will actually meet**. **Verify
> every commit at the object by explicit hash.** **The reserved-word conventions bind all new prose.
> The E-ordering rule stands, at both grains: state at the close that the end state is NOT asserted
> there, and let the further commit carry it.** **F3's reading rule stands** (`gen_cluster_dispositions.py
> --verify` is the drift authority; `reaim_home_anchors.py --check` exits 0 while printing drifted
> anchors and is not the authority). **Every guard run is taken at a quiet tree.** **Every shell
> command carries `; echo "exit:$?"` and no command produces large output to the terminal**
> (`CLAUDE.md`, the VS Code extension rules).
>
> **★ THE VOCABULARY RULE OF 2026-08-17 BINDS EVERY LINE THIS BATCH WRITES** — TOWARDS the ultimate
> objective and TOWARDS the guiding principles, never *against* either. **Music-theory words in their
> musical sense only**: a *screen* here is a measurement over documents; *score* is never used in the
> numerical sense; *measurement tool* and never the reserved word.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 6 — the document set (Alternative A):** *"Three limbs, as plan §5 states them:
  `ARCHITECTURE.md` (the analysis layers' sections, the joint estimator's standing-rules section,
  document governance); every document `ARCHITECTURE.md` delegates to in a form the delegation-form
  rule admits (`CLAUDE.md` decisions-register rule (i)), each delegating line quoted; and
  `docs/scoring_model.md`. The decisions register's home population is NOT the source. Whether
  `ARCHITECTURE.md`'s delegations are complete is recorded as a finding, not assumed. Three
  properties travel with every member: its pollution distribution (Ruling 7), LIVE or DORMANT, and
  its declared establishment status."*
- **Ruling 7 — step zero (Alternative A):** *"A preparation-phase act and the one mechanism this
  plan runs, declared so guardrail 3 is not breached silently. Its per-document distribution feeds
  the reading depth (Ruling 12) and the ordering of units. Failure signal, ruled with it: if most
  passages land UNDETERMINED the premise is not measurable, and that is a STOP to the user, not a
  licence to proceed."*
- **Ruling 8 — the ledger (Alternative B), binding here only as a bar:** *"The ledger (a ruled
  preparation output) is not built before the pilot."* — so this batch builds no ledger and admits
  no fact.
- **Ruling 9 — guardrail 2, the repaired form (Alternative B):** *"A finding that bears on the
  analysis goes to the quarantined audit questions; a finding about the apparatus is rowed under the
  open-items register's rules (c) and (e) and lapses under the ruled lapse rule (D-676); everything
  else is discarded under the worth test (#10) with finding, date and reason. The predecessors' 'no
  numbers, no rows' is not adopted. No findings series is opened."*

## Premise ledger

- **FACT** — the branch tip is `3cfb220b1d`, parent `7d7a0e76f7`, carrying `cc_report_plan_evaluation.md`
  alone (867 lines), subject line *"evaluation: the specification-reconstruction plan evaluated under
  the neutral brief; report at cc_report_plan_evaluation.md"*; `origin/master` stands at `891bacc5d2`.
  Read by the writing side with `git show --stat` at the explicit hash on 2026-08-21. **`7d7a0e76f7`
  and `3cfb220b1d` are NOT PUSHED.**
- **FACT** — the fourteenth batch's invariants: `OPEN_ITEMS.md` at blob `6ae67d8603` and
  `tools/audit/nongating_apparatus_rows.json` at blob `5bb43d0b3a`, byte-identical at every commit
  of that batch; the two commits since (`7d7a0e76f7`, `3cfb220b1d`) touch neither path (each carries
  one file, read at the object). **Nothing this batch does may move either; a difference in either at
  any task is a STOP-and-report.**
- **FACT** — [[OI-179]] is in `gating_ids` on 2026-08-21; read at the staged artifact by the writing
  side (the list opens at the artifact's `gating_ids` key; `OI-179` is a member).
- **FACT** — the July screen's method is AUTHORED per hunk and DERIVED in its population: its
  docstring states the four classes, the order they are applied in (POSITIVELY CODE-INFLUENCED
  first), the six reported shapes, and the five STOPs (`tools/audit/gen_july_screen.py`, lines 1–78
  at the tip). Its population is `tools/audit/period_stratum_split.json` → the screen population:
  the OUT-OF-PERIOD specification-bearing FLAG hunks, selected by the two document ROLES `governing`
  and `specification-or-docs` (`tools/audit/gen_period_stratum_split.py`, `SPEC_BEARING_ROLES`). **Its
  counts are at the artifact and are not restated here (D-431).**
- **FACT** — the candidate enumeration assigns every `cowork_*.md` document the role
  `cowork-planning-or-ruling` (`tools/audit/gen_doc_change_candidates.py`, `document_role`). **So a
  document-set member `ARCHITECTURE.md` delegates to under that name is OUTSIDE the existing screen
  population by role, and the widening of Task 2 selects by MEMBERSHIP and not by role** — that is
  the whole of what "widened over the whole document set" means mechanically.
- **FACT** — the candidate enumeration's commit population is the restructuring period (its
  docstring, and `tools/audit/doc_change_candidates.json` → `commits`); the in-period stratum is
  `S4-D231-phase-1`. **A document-set member with no hunk in either candidate artifact is a COVERAGE
  GAP of step zero and is published as one (Task 2 step 5), never silently absent.**
- **FACT** — the delegation-form rule is `CLAUDE.md` decisions-register rule (i): ADMITTED — an
  explicit delegation clause or a named home with sections; NOT ADMITTED — a bare appended citation
  or a provenance attribution; with (k): a glob pattern and a trailing ellipsis confer nothing, and
  the strongest naming governs (k1). Read at `CLAUDE.md` by the writing side; **quote it at the file
  in your report, never from here.**
- **FACT, measured by the writing side immediately before `cowork_handoff.md` was edited** — the
  file stood at sha256 `16dcf35a0388…`, **466,380 bytes with ZERO carriage returns** (measured over a
  bridge-staged copy with `sha256sum` and `wc -c`), carrying the thirty-seventh block as its entry
  point. The thirty-eighth block was then inserted above it and the thirty-seventh heading marked
  superseded-as-entry-point. **So the F57 caveat does not arise for that file, and this is measured
  rather than assumed.**

- **ASSUMPTION A1 — the working tree, stated by CONTENT and never by a changed-passage count.** The
  tree carries exactly **ONE tracked modification**: `cowork_handoff.md`, whose content is **five
  blocks inserted by five sessions** — the THIRTY-FOURTH, THIRTY-FIFTH, THIRTY-SIXTH, THIRTY-SEVENTH
  and THIRTY-EIGHTH, each with the preceding heading's entry-point demotion marker. **The committed
  blob's entry point is the THIRTY-THIRD block — read by the writing side with
  `git show 3cfb220b1d:cowork_handoff.md` at the object — so the fourteenth batch's own handover
  block and everything after it have never reached git; Task 0 lands all five.**
  **★ ONE PATH IS NAMED AS EXPECTED-UNMODIFIED BECAUSE TASK 0 WILL MOVE IT.**
  `tools/audit/evidence_pin_membership.json` is expected **UNMODIFIED**. **If it IS modified, that is
  accounted for by Task 0 step 2 and is NOT a STOP. A modification at any OTHER tracked path is a
  STOP-and-report.**
  **Untracked and to be landed by Task 0, named one by one (F82 applied):**
  `cowork_rulings_2026_08_21_evaluation_brief_sitting.md`, `cc_instruction_plan_evaluation.md`,
  `cowork_plan_evaluation_brief_2026_08_21.md`, `cowork_evaluation_boot_list_2026_08_21.md`,
  `cowork_curated_boot_list_draft_2026_08_19.md`, `cowork_review_findings_prediction_2026_08_21.md`,
  `cowork_report_plan_evaluation_2026_08_21.md`, `cowork_prediction_tabulation_2026_08_21.md`,
  `cowork_specification_reconstruction_plan_successor_2026_08_21.md`,
  `cowork_rulings_2026_08_21_successor_plan_sitting.md`, and this dispatch. **ALSO landed, because
  they never reached git and the successor plan §1 names them as the withdrawn-and-preserved
  predecessors (#12):** `cowork_specification_reconstruction_plan_2026_08_19.md`,
  `cowork_specification_reconstruction_plan_v2_2026_08_19.md`,
  `cowork_specification_reconstruction_plan_v3_2026_08_19.md`,
  `cowork_specification_reconstruction_plan_v4_2026_08_19.md`, and `cc_instruction_plan_challenge.md`.
  **`cc_report_plan_challenge.md` is ALREADY TRACKED at the tip** (the writing side checked each of
  these names with `git cat-file -e 3cfb220b1d:<path>`; every other name above is absent from the tip
  and present on disk). **Sixteen untracked paths in all; a named path the tree does not carry is a
  STOP-and-report.**
  **★ AND THE REST OF THE UNTRACKED POPULATION IS NAMED AS THE TREE CARRIES IT, NOT AS A CLASS.** The
  tree also carries newly visible `cc_instruction_*.md`, `cc_*_report.md` and `cc_*_dossier.md` files
  from before 2026-08-19 and a scratch directory. **This batch lands none of those**, and A1's STOP
  condition is scoped to TRACKED paths. **Check ordered (Task 0, first act, blob against blob by
  explicit hash)**, under the F57 caveat; **`git status` is measured to time out on this mount — use
  `git diff --stat 3cfb220b1d -- <path>` per named tracked path and `git ls-files --others
  --exclude-standard -- <name>` per named untracked path, each with `; echo "exit:$?"`.**

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  **Check ordered (each task's last step):** any other red is classed by its measured cause, and one
  outside this batch's own acts' subjects is a STOP-and-report. The reds this batch causes and must
  clear: **the membership check, cleared by Task 0 step 2**; **`gen_period_stratum_split.py --check`
  and `gen_july_screen.py --check`, each failing between the moment its tool is extended and the
  moment its artifact is regenerated inside the SAME task**, never left red across a commit; and
  **`gen_session_start_read_size.py --check` at the close**, because Task 4 moves `STATUS.md` —
  **F79's own instance, expected, cleared by regenerating the measurement and by nothing else.**

- **ASSUMPTION A3 — the delegation seed is a SEED and not the population.** The generated
  delegation grades at `tools/audit/decisions/phase1p_delegation_bar.json` and
  `home_classification.json` were produced to answer *where does a register entry live*, over the
  entries' home documents, and **not** to enumerate every delegation `ARCHITECTURE.md` writes. **Check
  ordered (Task 1, step 3):** Task 1's own read of `ARCHITECTURE.md` is measured against the seed
  both ways — every seed-graded admitted delegation re-found at the text, or its absence explained at
  the text; every text-found delegation absent from the seed named. **A seed-admitted delegation the
  text does not carry is a STOP-and-report** (the seed is then wrong about the tree, which is a
  finding bearing on the record and not this batch's to repair).

## The declared start state — at the tree you will meet, with the RECORD of the run named beside the VERDICTS

**The committed guard registry records the fourteenth batch's END STATE** — `guard_state.json` at
the tip, one failing check ([[OI-372]]'s tool), zero STOPs — and the two commits since touched no
guard input. **The tree you meet differs from that record by this dispatch's OWN INPUTS, and the
difference is declared rather than left (F67 applied):**

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — **caused
   by this dispatch's own inputs**: TWO untracked root-level `cowork_rulings_*.md` records now sit
   there, `cowork_rulings_2026_08_21_evaluation_brief_sitting.md` (on disk since the thirty-sixth
   session; the evaluation dispatch ran no guard and declared the set unmoved because it touched
   nothing, which was true of the batch and is not true of the tree) and
   `cowork_rulings_2026_08_21_successor_plan_sitting.md`.

**★ AND THE GUARD REGISTRY'S OWN ARTIFACT DOES NOT RE-DERIVE EITHER, NAMED HERE RATHER THAN LEFT (F83
applied).** `gen_guard_state.py --check` will open *"STALE vs the run"*, because the committed
registry records one failing while the tree has two, and **the same input causes both**. **It is not
a third failing check**: the registry's own `not_a_subject_of_this_run` field records why the runner
is not a subject of its own run. **The declared start state is MATCHED when the verdicts match, and no
STOP is owed for the registry's drift.**

**★ THE PREDICTION ABOUT THE MEMBERSHIP DIFFERENCE IS TAKEN FROM BOTH ROUTES (the standing clause).**
**Route A** — the count rises by TWO from its committed value and two record names are added. **Route
B** — the writing side probed both new ruling records at their own text before this dispatch was
written: **NEITHER carries the word `pinned` on any line**, so route B is predicted to add NOTHING and
no third hunk is expected. **Both halves are predictions and not citations — check them at the
artifact.**

**A THIRD failing check among the verdicts, or a difference in the membership artifact reaching any
member, route, document, pin constant, state or count beyond the two additions, is a STOP-and-report.**

## Findings in this batch — the plan's repaired guardrail 2, applied here because step zero is the plan's own act

**No finding number is allocated in this batch; the series stands at F88 and stays there.** A finding
you make is sorted by D-438's test as the standing clause *"A finding that bears on the analysis is
SURFACED whatever its size; an apparatus finding is ROWED AND LEFT"* orders, with Ruling 9's routing:
**(a)** a finding that bears on the analysis, its inputs or a measurement tool is SURFACED in the
report under its own heading **"Quarantined audit questions from step zero"** — the list the plan's
§6.3 step three and §6.6 hand to the audit; **it is not rowed, not numbered, not acted on**; **(b)** a
finding about the apparatus gets its open-items row and detail file in the commit that records it
(rule (c)), the row states in its own text that it lapses under D-676, and nothing further is owed;
**(c)** a finding bearing on neither is recorded DISCARDED in the report with finding, date and reason
(#10). **An establishment obligation is never discarded and always surfaced (#19).** The two owed
dispositions the plan's §2 names — three source files self-declared DORMANT against a live default,
and `CLAUDE.md` #21's stale phase numbering — **are NOT this batch's to row or act on; if you meet
either, cite the plan's §2 and move on.**

## Task 0 — land the 2026-08-21 record, clear the membership check in the same act, and PUSH

1. **A1's check first, entirely at content-addressed objects**, under the F57 caveat, by the
   per-path commands A1 names.
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE its difference against the
   committed blob BEFORE accepting it**, against **both** halves of the prediction:
   `python tools/audit/gen_evidence_pin_membership.py; echo "exit:$?"` then
   `git diff --stat -- tools/audit/evidence_pin_membership.json; echo "exit:$?"` and read the hunks.
   **If any member, route, document, pin constant, state or count MOVES beyond the two predicted
   additions, that is a STOP-and-report.** An ADDED derived cross-reference whose cause is this
   batch's own landed records is not such a movement — the sharpened bar standing at
   `cowork_audit_protocol.md` — but it is reported with its cause established at the record's own
   text.
3. **Commit exactly the paths A1 names and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated), and the sixteen named untracked files
   (new) — eighteen paths. **The commit subject carries no verdict on the
   plan and no verdict on the evaluations**; use exactly:
   `record: the 2026-08-21 evaluation arc and the successor plan's ratification landed; evidence-pin membership regenerated`.
   Staged by explicit path, never by `git add -A` or `git add .`.
4. **PUSH** — `git push origin master; echo "exit:$?"` — this is the push the evaluation dispatch
   withheld. **Verify at the object** that `origin/master` now names your Task 0 commit.
5. **Re-run `gen_evidence_pin_membership.py --check` at the tree this commit carries: it must PASS.**

**Registered expectation E0:** exactly the named paths; two modifications matching A1's shape and
step 2's bounded regeneration; every other path an addition; no staging override; the membership
check passing at the resulting tree, so the guard set stands at **one failing** ([[OI-372]]) from
Task 0 onward; `origin/master` at the Task 0 commit.

## Task 1 — Ruling 6: the specification document set, DERIVED and published whole

**This task is a derivation over a population and is published WHOLE or not at all (the standing
clause); it is placed before the per-entry pass of Task 3 by the ordering rule, and that placement is
imported from the ruling, not preferred.**

1. **A3's check is step 3; the reading is steps 1–2.** Read `ARCHITECTURE.md` **in full** at the tip
   — the writing side has NOT read its body and makes no claim about its contents beyond what the
   evaluations cite. Identify, by heading and line range, the three limb-1 regions the ruling names:
   the analysis layers' sections (filed as children of §3.3), the joint estimator's standing-rules
   section above the table of contents, and document governance. **Record each region's heading text
   and line range; a region you cannot locate by the ruling's description is a STOP-and-report, never
   a guess.**
2. **Enumerate EVERY delegation `ARCHITECTURE.md` writes**, reading the whole file and not searching
   it (P1: enumerate-then-classify). For each candidate delegating passage record: the line, the
   passage QUOTED VERBATIM, the target document and — where the passage names them — the sections, and
   the grade under rule (i)/(k) with the clause quoted from `CLAUDE.md` that decides it: **ADMITTED**
   (explicit delegation clause; named home with sections) or **NOT ADMITTED** (bare appended citation;
   provenance attribution; glob; trailing ellipsis), and under (k1) the strongest naming per target
   governs. **A passage the clauses cannot place is a STOP-and-report, never defaulted.**
3. **A3's check: reconcile against the seed both ways.** Read `phase1p_delegation_bar.json` and
   `home_classification.json` for every delegation they grade in `ARCHITECTURE.md`; every seed-admitted
   delegation must be re-found at the text (else STOP); every text-found admitted delegation absent
   from the seed is named as such. **Publish the miss rate against the seed as part of the
   derivation's name** (D-661), and state that the seed answers a different question (A3).
4. **Derive the member list**: limb 1 (`ARCHITECTURE.md`, the three regions); limb 2 (every ADMITTED
   target, whole document where the delegation names the document, the named sections where it names
   sections — the granularity clause of rule (h)); limb 3 (`docs/scoring_model.md`). **A target the
   tree does not carry at the named path is a member-with-no-file, published as such, never dropped.**
5. **Attach the two declared properties per member** (the third, pollution, is Task 3's): **LIVE or
   DORMANT** — quoted from the member's own status banner or from the joint estimator's standing rules
   where they name it, with the quote and its coordinate; **UNDECLARED** where no banner or rule says
   (never inferred); and **the declared establishment status** — quoted where the document declares
   one (`docs/scoring_model.md` §8's declaration is the plan's own example), **UNDECLARED** otherwise.
6. **Write ONE new generator, `tools/audit/gen_specification_document_set.py`, on the project's
   established shape**: AUTHORED — the per-passage grades and quotes of step 2, the per-member
   property quotes of step 5, each carrying its coordinate; DERIVED — every count, the member list from
   the grades, and a quote-fidelity check that every authored quote exists at its cited line of the
   file at HEAD (a mismatch STOPS the tool); `--check` re-derives and exits 1 on drift. Output
   `tools/audit/specification_document_set.json`. **Its docstring states what is authored and what is
   derived, the seed's miss rate, the regions, and the members-with-no-file.** No recognizer over
   prose decides a grade — the grades are authored (F42, F84).
7. Run the guard set; commit (`the specification document set derived — Ruling 6`); push; verify at
   the object.

**Registered expectation E1:** every delegation in `ARCHITECTURE.md` enumerated and graded with its
deciding clause quoted; the seed reconciled both ways with its miss rate published; the member list
published whole with both declared properties per member and every UNDECLARED stated as such; the
generator's `--check` passing at the commit; **`ARCHITECTURE.md` and every member byte-unchanged.**

## Task 2 — Ruling 7, first half: the July screen's POPULATION widened over the document set, its method untouched

**A derivation over a population; published WHOLE or not at all; placed before Task 3 by the ordering
rule.**

1. **Extend `tools/audit/gen_period_stratum_split.py`** to publish, BESIDE its existing fields and
   leaving every existing field byte-identical, a second population **`the_widened_screen_population`**:
   every FLAG hunk in `doc_change_candidates_hunks.jsonl` whose file path is a member of
   `specification_document_set.json` (read from that artifact; a member named by sections is matched
   by path, and the section restriction is recorded on the hunk for Task 3), **across EVERY stratum,
   in-period and out-of-period alike**. Selection is by MEMBERSHIP; the role field is reported per
   hunk and excludes nothing (the candidate generator's own ruling 4, inherited).
2. **Publish the relation to the existing population**: which of the widened members are ALREADY
   screened (the existing out-of-period members that are document-set members), which are NEW, and
   which existing screened members are NOT document-set members (a `docs/` document outside the set,
   for instance) — both ways, none in both, none in neither, a STOP on any hunk the two readings
   cannot reconcile.
3. **Publish the per-document and per-stratum counts of the widened population.** Every count
   derived; none transcribed.
4. **The inherited caveat travels**: the candidate generator has never been positively established
   (`gen_period_stratum_split.py`, `INHERITED_CAVEAT`); carry it verbatim into the new field and the
   report. **Nothing here establishes it.**
5. **Publish the COVERAGE GAP**: every document-set member with zero hunks in the candidate
   enumeration, with the reason the enumeration gives for itself (outside its commit population,
   never changed in the period, or a member-with-no-file). **This is the plan's own finding about the
   premise's measurability and is reported whole.**
6. Regenerate the artifact; `--check` must pass; the existing fields' byte-identity measured
   (`git diff` over the artifact must show additions only, no changed line inside an existing field);
   run the guard set; commit (`the screen population widened over the document set — Ruling 7`);
   push; verify at the object.

**Registered expectation E2:** the widened population published whole with both-ways reconciliation
against the existing one; counts per document and per stratum derived; the coverage gap published;
the inherited caveat carried; every existing field of the artifact byte-identical; `--check` passing.

**★ BEFORE TASK 3 OPENS, REPORT THE SIZE YOU ARE ABOUT TO READ**, as a direction with its artifact
named — not a transcribed value — against the plan's §11 budget for step zero: **one working session,
carried as a stop-and-report threshold and not an estimate.** If the widened NEW population is
manifestly beyond one session, that is not a STOP: Task 3 is a per-entry pass and stops at a member
boundary with its stop recorded (the standing clause). Say so in the report before reading the first
hunk.

## Task 3 — Ruling 7, second half: the authored verdict per NEW hunk, and the per-document distribution

**A per-entry pass: it may be stopped at any member boundary, and the stop is RECORDED — which members
were completed, that the remainder is UNTOUCHED rather than partly worked, and that nothing is left
half-edited. The order of members is the artifact's own order (by document, then by commit, then by
hunk) so a continuing session derives the remainder fresh.**

1. **Extend `tools/audit/gen_july_screen.py`** to read BOTH populations from
   `period_stratum_split.json` and to hold authored verdicts for the widened members in a second
   authored block, **the existing sixty-eight verdicts byte-unchanged** — measured: the existing
   authored block's text is hashed before and after the edit and the two hashes are published in the
   report. **The four classes, their ORDER, the six shapes and the five STOPs are inherited whole and
   unchanged — "its method untouched" is Ruling 7's own clause.** The STOP that a population member
   with no authored verdict halts the tool is RETAINED for the widened population **with one declared
   exception for the per-entry stop**: a widened member carrying the verdict `NOT YET READ` is admitted
   to the artifact, counted in its own class, and reported as unread — never silently absent and never
   counted as UNDETERMINED.
2. **Author one verdict per NEW hunk, by the inherited method**: retrieve the hunk from the git object
   by explicit hash (the tool already does this); read it; apply the four classes in their declared
   order; name the shape; write the ground citing what it was made from; for a RATIFIED-ACT EDIT name
   the act AND where its ratification is recorded, or do not admit the class. **A section-restricted
   member's hunks outside the named sections are recorded `OUTSIDE NAMED SECTIONS` and not graded.**
3. **Regenerate after each bounded run of members** so that the tree is never left with the artifact
   behind the tool; `--check` passes at every commit. Commit in bounded runs (`the widened screen:
   verdicts authored, members <first>–<last> of <document>`), each pushed and verified.
4. **The per-document POLLUTION DISTRIBUTION** is published in the report by the generator: per
   document-set member, the hunks by class, with `NOT YET READ` and `OUTSIDE NAMED SECTIONS` as their
   own columns and the coverage gap beside them. **Every count derived.**
5. **★ THE RULED FAILURE SIGNAL, checked at the artifact and not at an impression:** if, over the
   widened population's READ members, the UNDETERMINED class is the majority, **that is a STOP to the
   user** — the premise is not measurable by this screen — and it is reported as the batch's headline,
   with the plan's §4 quoted. **It is not a licence to proceed, and it is not a licence to argue the
   class down.** A majority reached only because few members were read is reported as
   INCONCLUSIVE-AT-THIS-COVERAGE with the read share named, not as the signal firing.
6. **State, honestly, what the widened screen IS**: an authored verdict per hunk over a derived
   population, positive evidence of influence only, bounded by the invisibility the original screen
   declares at its own `:13`; a clean class is not a certificate. Inherit that text; do not re-argue
   it.

**Registered expectation E3:** every NEW member carries exactly one verdict in the inherited
vocabulary or the declared `NOT YET READ` / `OUTSIDE NAMED SECTIONS`; the existing sixty-eight verdicts
byte-identical by published hash; the per-document distribution published by the generator; the
failure signal evaluated at the artifact and stated either way; a stop, if taken, recorded in the
standing form.

## Task 4 — the close

1. One `STATUS.md` pointer entry per task, and **in the same act the FOURTEENTH batch's entries move
   verbatim to `STATUS_ARCHIVE.md`** (`gen_status_batch_bound.py`), both directions proven. **The
   declared `Last updated:` prefix adjustment is imported, not re-decided (#6).**
2. The FULL close is appended to `cowork_away_returns.md` as a **STEP ZERO OF THE SUCCESSOR PLAN**
   section: both guard states, every SHA, every registered expectation graded MET or NOT MET with its
   reason, every declared departure, the independence of every authored verdict from any verdict the
   record already carries on the same hunk (there is none for NEW members by construction — say so),
   the quarantined audit questions (or that there are none), and **the plan's tell for this act, in
   one sentence: did this batch produce anything other than what the plan's §4 declares — the widened
   generator, the document-set generator, the artifacts and the report? If yes, name it.** The report
   is `cc_report_step_zero.md`.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE.** State at its head that the end-state run is taken
   after the commit carrying it exists and that its values land in ONE FURTHER commit. **Do not write a
   row, a cell or a sentence asserting the end state into the close commit.**
4. **★ NAME THE CHAIN AS FAR AS A SENTENCE CAN NAME IT, DECLARING THE ONE COMMIT IT CANNOT** — the
   shape the last batches established. Import it; do not re-invent it.
5. Commit, push, verify at the object; then the one further commit carrying the end-state run.

**Registered expectation E4:** at the tree carrying the close, a fresh full guard run re-derives —
**every run check passing except [[OI-372]]'s tool, zero STOPs** — run and read, never inferred, its
values committed only after the run that produced them, **and no sentence asserting them written
before that run existed**; the tell stated in one sentence.

## What this batch does NOT do

- **No pilot act, no derivation of any specification statement, no frame, no placement test, no
  format test, no held-out test** — those open after this batch returns, under Rulings 3, 4 and 8.
- **No empirical findings ledger and no fact-gate admission** (Ruling 8 bars the ledger before the
  pilot; nothing here is a pilot).
- **No edit to any screened document.** `ARCHITECTURE.md`, `docs/scoring_model.md` and every
  document-set member are read and never written.
- **No regeneration of `doc_change_candidates.json` or its hunk file** — the population is inherited
  and the generator's unestablished status is carried, not discharged.
- **No change to the July screen's classes, order, shapes or STOPs; no re-reading or re-grading of
  any of the existing sixty-eight verdicts.**
- **No finding number allocated; no findings series opened.** [[OI-372]] and [[OI-374]] stay as
  found; **[[OI-179]] stays OPEN and GATES**; F3 stays surfaced, unfixed and unrowed.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule (c)
  as *Findings in this batch* states** — and no row for either of the plan's §2 owed dispositions.
- **No candidacy acted on, no census re-pin, no caller flag moved, no pin taken or re-taken, no
  archiving pass, no `CLAUDE.md` span fate, no change to rule (a)**;
  `tools/audit/nongating_apparatus_rows.json` and `gen_nongating_apparatus_rows.py` byte-unchanged.
- **No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
  `tools/robust_stop/`, no measurement of the analysis.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #18 — step zero exists to check the load-bearing premise; #19 — the
   candidate generator's unestablished status is carried and nothing here claims to establish it;
   #12 — nothing deleted, the existing verdicts and fields byte-unchanged and measured; #6 — one
   population selection, one screen method, the existing sixty-eight not re-graded; #17(f)/D-431 — no
   count transcribed, the expected membership movement stated as a prediction; #24 — no comparison
   between measured quantities asserted. Conforms.
2. *Conventions:* American English; no invented labels — the new field and tool names follow the
   existing `gen_*` / `the_*` shapes; music-theory words in their musical sense, *score* never
   numerical, *measurement tool* never the reserved word; the 2026-08-17 vocabulary rule observed.
3. *Figures and premises:* every quantity named to its artifact or stated as a prediction; the one
   number in the premise ledger beyond hashes is the handoff's measured size with its producing tools
   named (the character-figure clause); the delegation-form rule cited to `CLAUDE.md` and ordered to be
   quoted there, not from here; the screen's method cited to the tool's own docstring lines.
4. *File-tools rule:* the writing side's own departures are declared in the thirty-eighth handover
   block, not here; the executing side is bound as `CLAUDE.md` Conventions state, with the per-path
   git commands named in A1 because `git status` is measured to time out.
5. *Uncertainty:* no comparison between measured quantities is asserted in this dispatch; the
   failure-signal test of Task 3 step 5 is a majority test over a derived population and is stated
   with its coverage caveat.
6. *Re-read from disk before release:* performed by the writing side over the delivered file,
   hash-verified against the intended content (F74), and recorded in the thirty-eighth handover block.

---

*Provenance: Cowork, 2026-08-21, the thirty-eighth session, written at branch tip `3cfb220b1d` under
Rulings 6, 7, 8 and 9 of `cowork_rulings_2026_08_21_successor_plan_sitting.md`. The reserved-word
conventions bind this dispatch, and the vocabulary rule of 2026-08-17 binds every line of the report
it orders — TOWARDS the ultimate objective and TOWARDS the guiding principles.*
