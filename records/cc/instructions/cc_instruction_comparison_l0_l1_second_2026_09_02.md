# CC dispatch — the L0/L1 COMPARISON, SECOND WRITING: cut the outgoing population to the ruled specification set, enrol the population tool, and disposition every statement of the outgoing text against the 54 derived statements — proposals only, NO decision taken, NO session booted

> **Dispatch (Cowork writing side, 2026-09-02, the session that booted on
> `cowork_handoff_entry_eighty_nine.md`, its second dispatch).** Executes **Ruling 33** (§3an of
> `cowork_rulings_2026_08_31_decision_surface_sitting.md`), which cuts Ruling 32's term search to the
> ruled specification document set, together with **Ruling 32** (§3am) and — as both record — Rulings
> 1 to 4 and §5 of `cowork_rulings_2026_08_24_comparison_design_sitting.md` and the disposition
> discipline of `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §0 and §3.4.
> **Written at a verified stop: the first writing's batch returned with its ruled size stop fired at
> Task 1, its four commits are verified at the objects and pushed, and nothing is running.**
>
> **★ WHY THERE IS A SECOND WRITING, IN ONE PARAGRAPH.** The first writing
> (`cc_instruction_comparison_l0_l1_2026_09_02.md`) ran Task 0 and Task 1 and then stopped, as it was
> ruled to, because the term search admitted more than forty files outside the named members (the
> count is at the population artifact, not here). The user ruled the cut (Ruling 33). The first
> writing also carried three defects of the writing side's, which that batch found and this writing
> repairs: its start state was read from a guard artifact three commits stale; its assumption A1
> missed a modified tracked file; and its assumption A4 ordered a `--check` script written and
> forbade enrolling it, which halts the guard runner by construction. **Task 0 and Task 1 of the
> first writing are DONE and are not repeated; Task 1 of this writing AMENDS their product.**
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** Task 0 lands this sitting's new records and PUSHES.
> Task 1 amends the population tool so that a search hit enters the population only if its file is a
> member of the ruled specification document set, publishes every other hit file as a listed residue,
> re-runs it, and ENROLS the tool in the guard runner's authored list — clearing the one STOP the
> first writing created. Task 2 is the COMPARISON: every statement of the outgoing population receives
> exactly one PROPOSED disposition out of the five the phase definition names, and every one of the
> derivation's 54 statements receives exactly one verdict about the current text; delivered as a
> reading file, one outgoing document per commit. Task 3 closes. **THIS BATCH DECIDES NOTHING: every
> disposition is a PROPOSAL the user ratifies later; every difference is put as a proposal, never
> applied. It BOOTS NO SESSION, BUILDS AND RUNS NO MEASUREMENT OF THE ANALYSIS, DERIVES NO
> SPECIFICATION STATEMENT, and EDITS NOTHING BUT ITS OWN DELIVERABLES.** Your framing is NEUTRAL
> throughout: you tabulate; you neither refute nor confirm.
>
> **★ THE ORDERED FIRST READ COMES BEFORE YOUR SESSION-START READ, BY RULING, AND THE DEPARTURE IS
> OF RECORD (Ruling 2 of the comparison-design sitting, applied by Ruling 32).** Your VERY FIRST
> read this session is `cowork_blind_derivation_l0_l1_2026_08_31.md` — **§5 first, then §6, then
> §7, then the whole file** — before `CLAUDE.md`, before anything. *Why:* the ordinary
> session-start read includes `CLAUDE.md`, whose Conventions section states in words how slice
> identity currently works (the derivation's own §6.2 records those lines as passages it met), and
> the ruling orders that you understand what the blind session claimed before the current text's
> frame exists to map it onto. The read SET of the session-start bar is unchanged; its ORDER is
> changed by the ruling. Then: **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full;
> `BUILD_AND_TEST.md` — CONDITIONAL, and this batch MEETS the condition (it runs the guard set):
> read it in full; rule (a)'s `gating_ids` at `tools/audit/nongating_apparatus_rows.json` →
> `★_the_live_gating_answer` (DO NOT read the whole open-items index). **(2)** `cowork_handoff.md`,
> ITS TOPMOST ENTRY (the eighty-eighth, as amended on disk — the amendment is landed by Task 0),
> AND `cowork_handoff_entry_ninety.md` whole — the entry point of this dispatch's writer, untracked
> at the root; `cowork_handoff_entry_eighty_nine.md` is in git and may be read for the state it
> describes. **(3)** `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL — every `###`
> section carrying the standing-clause marker binds this batch; the marker is the population, and
> no number is written here. **(4)** `cowork_rulings_2026_08_31_decision_surface_sitting.md` §3an
> (Ruling 33) and §3am (Ruling 32) — this batch's authority — and §3al. **(5)**
> `cowork_rulings_2026_08_24_comparison_design_sitting.md` whole, and
> `ratification_surfaces/cowork_comparison_harmony_boundary_reading.md` §1–§4, the first two rows
> of §5.1 (for the row shape), and §6 — the pilot's comparison, whose SHAPE this batch's reading
> file follows. **(6)** `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §0
> (the referents — the disposition discipline, the mining map and the worth test) and §3.4.
> **(7)** `cowork_blind_session_brief_l0_l1.md` §2 (the seven faces), §4 (the six-field form) and
> §7. **(8)** `tools/audit/derivation_boot_pack.json` → `subjects` → `l0-l1` →
> `the_members_as_rendered` (members 7 and 8: what was removed and cut from the charter member and
> the research extracts) and → `LEAKS`. Note at the same object that `THE_WITHHELD_FAMILY` for this
> subject is EMPTY BY RULING — so the pilot's D-450/D-575 caveat has NO counterpart here, and no
> row applies one. **(9)** `cc_report_comparison_l0_l1_2026_09_02.md` whole and the close it points
> at in `cowork_away_returns.md` (the section *THE L0/L1 COMPARISON TABULATED, NOTHING DECIDED* and
> its end-state subsection) — what the first writing's batch did and found. **(10)**
> `tools/audit/l0_l1_outgoing_population.json` as it stands, and
> `tools/audit/specification_document_set.json` → `the_document_set` (the ruled set Task 1 cuts
> to). **(11)** `tools/audit/gen_guard_state.py`, the authored invocation list (the tuples of tool,
> arguments and reason — locate the list by the entry for `gen_specification_document_set.py`),
> because Task 1 adds one entry to it. **(12)** The outgoing texts are opened AS TASK 2 ORDERS, not
> read whole up front.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The derivation* — the blind output: 54
> statements (S-1 to S-54, in the six-field form) and 17 open questions (OQ-1 to OQ-17), written by
> an implementation-blind session that read the brief, the ten-member boot pack, four staged scores
> and fetched research, and nothing else of this project — **relayed from its own §5; you count
> them yourself and your counts are the figures of record.** *The outgoing text* — the current
> specifications describing what the new L1 charter now covers; under the phase definition it is a
> WITNESS read after the derivation, evidence about the present text and never authority over a
> derived statement. *The outgoing population* — the set of passages of the outgoing text this
> comparison covers, fixed by Rulings 32 and 33 and derived by Task 1. *The residue* — the hit
> files outside the ruled specification document set: published on the artifact, dispositioned by
> nobody here, left for the mining map. *An outgoing statement* — one atomic
> claim of the outgoing text about what the analysis does, must do or may assume for L0's or L1's
> subject: one sentence or one bullet, never a paragraph. *A disposition* — one of the five fates
> the phase definition names for an outgoing statement: **adopted** (its content is carried by a
> derived statement, or is proposed for adoption), **relocated** (its content belongs to another
> charter — L2, L3, the voice-leading axis, or the measurement design — and goes to the
> cross-layer transfer list), **quarantined** (it states how the implementation currently works, or
> a measured property of it, and becomes an audit question), **discarded** (under principle #10's
> worth test, with finding, date and reason), **historical** (it records an event, a build state,
> a superseded plan or a status, not a rule). *The transfer list* — the parking list for content
> found in the wrong charter; it exists so nothing is silently dropped. *The reading file* — your
> deliverable, `ratification_surfaces/cowork_comparison_l0_l1_reading.md`.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, **no measurement of the ANALYSIS
> built, designed, scoped or run** (the comparison is TEXTUAL), no design, no repair, **no
> derivation of any specification statement**, NO SESSION BOOTED, no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit to any
> governing document, to the derivation, to the brief, to the pack or its generator, to any register
> entry or register source, to any outgoing text**, **no edit to `tools/audit/gen_guard_state.py`
> beyond the ONE enrolment tuple Task 1 orders**, **no disposition APPLIED anywhere** — the transfer
> list is written inside the reading file and nowhere else — **no verdict word attached to the
> derivation's independence record** (relay its contents as facts), **no recommendation about the
> derivation, the method or the split of L0 from L1 anywhere** — not in the reading file, not in the
> report, not in a commit message.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (CHECK mode) and again
> at the end (write mode, committed as the artifact of a real run). Verify every commit at the
> object by explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and
> `git diff` over the working tree time out on this mount and are denied by the armed guard — use
> the per-path explicit-hash forms named in A1. If staging is refused by a stale zero-length
> `index.lock` with no git process running, establish the cause (D-669), remove it, report the act.
> Plain single-quoted `git commit -m '…'` subjects. The reserved-word and vocabulary rules bind
> every line — *score* only the musical score, *key* only tonality, *bar* only the metric unit (the
> prohibiting sense is *exclude*), no numeric grade anywhere: the vocabulary in §T2 is the whole of
> the verdict language.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 33, §3an of the sitting record (the cut):** *"a term-search hit enters the outgoing
  population only if its file is a member of the specification document set. The population is
  therefore Ruling 32's eleven named members plus every hit file in that set. Every other hit file is
  published on the population artifact as a listed RESIDUE — hit, outside the specification set, not
  dispositioned by this comparison … The residue is published, never dropped (#12)."*
- **Ruling 32, §3am (the population's named members):** items 1–3 of *What was decided* — the two
  `ARCHITECTURE.md` sections, the five root design documents, the four verified places — stand
  unchanged; item 4 is narrowed by Ruling 33.
- **Ruling 3 of the comparison-design sitting (the vocabulary's form):** *"closed, two axes, words
  not numbers, fixed before grading starts … Every row carries the statement's own words beside the
  oracle's, so the user can re-grade any row at the texts. Default nothing: a verdict the comparer
  cannot defend in one sentence at the texts is delivered UNGRADED, with what was read."* Here the
  oracle axis does not exist (the charter was GIVEN to the deriving session, not withheld); the
  current-text axis and the disposition axis are what remain, and both are closed.
- **Ruling 4 of the same sitting:** *"The comparison tabulates; the method ruling comes to the user
  afterwards as a Cowork-written decision surface over the tabulation."*
- **The phase definition §0, the disposition discipline:** *"every statement of an outgoing text
  reaches ONE recorded disposition — adopted / relocated (to the cross-layer transfer list) /
  quarantined (as an audit question) / discarded (under the worth test, with finding, date, reason)
  / historical — with completeness checked by arithmetic."* And §3.4's constraint: *"a silent rewrite
  where a proposal per difference is owed; a disposition class invented beyond the five"* — both NOT
  ALLOWED.
- **The guard runner's own rule, at `tools/audit/gen_guard_state.py`'s authored list:** *"a derived
  candidate with no authored invocation is this runner's own STOP, and a new guard may not be
  silently left unrun"* — the standing new-tool rule, which Task 1 obeys.

## Premise ledger

- **FACT** — the local branch tip is `8f1e4e9deffd816f0e54c07c0912055a144f404f` (parent
  `a4d431a86911da3c539444cf3f241126d3369bb9`), the first writing's end-state commit; `origin/master`
  is the SAME hash. Read by the writing side from `.git/refs/heads/master` and
  `.git/refs/remotes/origin/master` with the file tools, 2026-09-02, after the batch returned. **Nothing
  is unpushed. Re-read the refs yourself before Task 0.**
- **FACT** — at the tip, `tools/audit/guard_state.json` → `summary` records run 75, passing 65,
  **failing 10**, not_run 4, historical 16, **and it was WRITTEN BY THAT COMMIT** (the end-state
  commit's own path list is `cowork_away_returns.md` and `tools/audit/guard_state.json`), so unlike
  the first writing's FACT this one is read from the object the tree's last run wrote. The ten failing
  tools: `gen_filing_convention_application.py --check`, `gen_artifact_inventory.py --check`,
  `gen_artifact_inventory_surface.py --check`, `gen_test_construction_evidence.py --check`,
  `gen_retirement_caller_check.py --check`, `decisions/apply_soft_discard.py --check`,
  `decisions/apply_residue_discard.py --check`, `gen_epoch_write_path.py --check`,
  `gen_recognizer_establishment_sort.py --check`, `decisions/gen_cluster_dispositions.py --verify`.
  **And ONE STOP:** `unclassified_candidates` carries `tools/audit/gen_l0_l1_outgoing_population.py`
  (the end-state section of the close states it; the summary object read by the writing side carries
  no `unclassified` key at its top level — locate the STOP's own field at the object and quote it).
  **Re-read the artifact yourself at the object `8f1e4e9d…:tools/audit/guard_state.json`.**
- **FACT** — the derivation `cowork_blind_derivation_l0_l1_2026_08_31.md` is in git at blob
  `9b102c575643d672e4747b4cfc23e377cd344c27`, 112,954 bytes, and the writing side verified that blob's
  sha256 equal to the copy it read before the first writing. **The writing side has read its §0, §1,
  §5, §6 and §7 and NOT its statements.**
- **FACT** — at the tip: `cowork_rulings_2026_08_31_decision_surface_sitting.md` is TRACKED (blob
  `a5daf80f…`) and is MODIFIED on disk (§3an and one provenance paragraph added, additions only);
  `cowork_handoff.md` is TRACKED (blob `0be97f02…`) and is MODIFIED on disk — the on-disk file is
  1,699 bytes larger than the blob (1,123,182 against 1,121,483, read at the staged file and at the
  object), modified 2026-09-01 by the eighty-ninth session, with a *SUPERSEDED IN PART — READ THIS
  FIRST* block at its line 19 (the first writing's batch read the diff and reported it as the amended
  eighty-eighth entry); `cowork_handoff_entry_ninety.md` and this dispatch
  (`cc_instruction_comparison_l0_l1_second_2026_09_02.md`) are ABSENT from the tip.
- **FACT** — at `tools/audit/l0_l1_outgoing_population.json` → `the_term_search.counts`,
  `hit_files_outside_the_named_members` exceeds the first writing's stop of forty (the value is at the
  artifact). At `tools/audit/specification_document_set.json` → `the_document_set`, 26 members, each
  with a `member` path. **The writing side's own intersection of the two — hit files outside the named
  members whose path is a `member` of the set — counted EIGHTEEN; that figure is a prediction for
  Task 1's re-derivation and is superseded by whatever the tool publishes.** Three of Ruling 32's
  named members (`cowork_layer1_tone_collection_design.md`, `cowork_layer1_extend_design.md`,
  `cowork_layer2_reslice_design.md`) are not members of the set and stay in the population by name.
- **FACT** — at `tools/audit/derivation_boot_pack.json` → `subjects` → `l0-l1`, the withheld family
  is EMPTY (`counted.withheld_identities_total`, `withheld_documents`, `withheld_passages` all zero).

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications, exactly two:
  **(i)** `cowork_rulings_2026_08_31_decision_surface_sitting.md` — §3an inserted before §4 and one
  provenance paragraph appended; additions only; **(ii)** `cowork_handoff.md` — the eighty-eighth
  entry amended by the eighty-ninth session (the *SUPERSEDED IN PART* block and the §3ag-to-§3aj
  correction), additions and in-place corrections inside that one entry only. **A modification at any
  OTHER tracked path is a STOP-and-report; a modification of `cowork_handoff.md` OUTSIDE its topmost
  entry is a STOP-and-report.** Untracked and to be landed by Task 0: `cowork_handoff_entry_ninety.md`
  and this dispatch — **two untracked paths.** The rest of the untracked population is NOT landed and
  is outside A1's STOP. **Check ordered (Task 0, first act — AFTER the ordered first read):** per named
  tracked path `git diff --stat 8f1e4e9deffd816f0e54c07c0912055a144f404f -- <path>; echo "exit:$?"`
  with the changed passages written to a scratch path outside the repository and read with the file
  tools; per named untracked path `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`;
  and the whole tracked population enumerated with the sanctioned enumeration tool. **The check is
  over the whole tracked population, so a third modification this ledger has missed is FOUND by it
  and is the STOP — this is the repair of the first writing's A1.**

- **ASSUMPTION A2 — the reds at the CHECK-mode run before your first edit.** The ten failing tools
  named in the FACT above, and the one STOP (the unclassified candidate), **exactly** — this ledger is
  read from the artifact the last run wrote, so no red of the tree's own is missing from it. The
  reds this batch's own inputs may cause: `gen_evidence_pin_membership.py --check` STALE if its
  derivation counts the modified sitting record's on-disk state (predict PASS — the first writing's
  batch measured PASS with the same record modified on disk — but MEASURE and report), and
  `gen_guard_state.py --check` STALE vs the run by the same route. **A red outside that set is classed
  by its measured cause; one outside this batch's own acts' subjects is a STOP-and-report.**

- **ASSUMPTION A3 — the membership regeneration.** `evidence_pin_membership.json` →
  `ruling_records_read` moves by ZERO (Task 0 lands no new ruling record; the sitting record is
  already tracked). **Route A** 0 new records; **Route B** no new `pinned`; **Route C** no tool added
  to ITS population. Measure before accepting; movement beyond is a STOP-and-report. **Your Task 2
  reading file lands under `ratification_surfaces/` — if the membership derivation reacts to it,
  measure, report the movement and its route, and class it by its measured cause; a movement you
  cannot class is a STOP.**

- **ASSUMPTION A4 — the guard registry, REPAIRED.** Task 1 ENROLS `tools/audit/gen_l0_l1_outgoing_population.py`
  with `["--check"]` in the runner's authored list (this is the one-script instance of the
  marker-enrolment act on the user-owed list, taken on Ruling 33's record). After enrolment: the
  population is **76** (one more than 75), the STOP is CLEARED (`unclassified_candidates` empty),
  and the new tool PASSES its own `--check` at the tree Task 1 leaves. The failing set is the ten
  named, less any this batch's ordered acts clear, plus none. **Measured, not assumed.**

- **ASSUMPTION A5 — byte-unchanged by this batch, verified at the objects after the last commit:**
  the derivation (blob `9b102c57…`), the brief (blob at the tip), the pack directory and
  `derivation_boot_pack.json`, every outgoing text in the population, and every governing document
  EXCEPT the two Task 3 orders written (`STATUS.md` and `STATUS_ARCHIVE.md`) — the first writing's
  blanket collided with its own Task 3, and this writing names the exception. Any other difference is
  a STOP-and-report.

## The declared start state — at the tree you will meet

1. The ten failing tools named in the FACT above — standing reds, none of them this batch's.
2. ONE STOP — `unclassified_candidates: tools/audit/gen_l0_l1_outgoing_population.py` — created by the
   first writing's own order and CLEARED by this writing's Task 1.
3. `gen_evidence_pin_membership.py --check` and `gen_guard_state.py --check` — PASS predicted,
   **measured** (A2).

A failing verdict outside 1–3 is a STOP-and-report.

## Findings in this batch

**No finding number is allocated by this dispatch; the series stands where the topmost entry of
`cowork_handoff.md` and the ninetieth entry file say it stands — read both.** Ruling 9's routing binds.
**What you find IN the derivation or IN the outgoing text is not a finding of this batch — it is the
tabulation's content.** A defect in the comparison APPARATUS (a document named by Ruling 32 that does
not exist at its path, a member of the specification set that does not exist at its path, an outgoing
text you cannot parse into statements, a derived statement without its six fields) is a
STOP-and-report, not a thing to fix.

## Task 0 — land the records, and PUSH

1. **A1's check** (after the ordered first read). For `cowork_handoff.md`, read the changed passages
   at the scratch diff and confirm every one lies inside the topmost (eighty-eighth) entry — the
   region from the title line down to the heading of the eighty-seventh entry; a change below that
   heading is a STOP.
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob at the tip BEFORE
   accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_rulings_2026_08_31_decision_surface_sitting.md`
   (modified), `cowork_handoff.md` (modified), `cowork_handoff_entry_ninety.md` (new),
   `cc_instruction_comparison_l0_l1_second_2026_09_02.md` (new), and
   `tools/audit/evidence_pin_membership.json` (regenerated, only if it moved) — **four or five
   paths.** Subject, exactly:
   `record: Ruling 33 (the population cut to the specification set), the amended eighty-eighth entry, the ninetieth entry and the second comparison dispatch landed`.
4. **PUSH**; verify at the ref file that `origin/master` equals the new tip.

**E0:** the named paths in one commit; `origin/master` at the commit; A3 measured; A1's whole-tree
check reported with its enumeration.

## Task 1 — the population cut to the specification set, the residue published, the tool enrolled — ONE commit

**(a) Amend `tools/audit/gen_l0_l1_outgoing_population.py`.** It keeps everything it does — the eleven
named members proven present and located by heading text, the two-tier term search over the three
inventory classes read from `artifact_inventory.json`, the reach statement, the counts — and gains
three things, each stated on the artifact:

- **The specification-set cut (Ruling 33).** It reads `tools/audit/specification_document_set.json` →
  `the_document_set` → each record's `member`, and a search hit ADMITS its file to the population only
  if that file's path is one of those members (path equality, exact). The artifact records the set's
  member count as read, and for each hit file whether it is IN the set or OUT.
- **The RESIDUE.** Every hit file that is OUT of the set is listed under a field named
  `the_residue_for_the_mining_map`: path, inventory class, admitting-hit count, and the one-line
  statement that it is *hit, outside the specification document set, not dispositioned by this
  comparison; reachable by the mining map*. **Nothing is dropped**; the per-file hit records stay
  whole for residue files too.
- **The ordered population, re-derived**: the eleven named members first in Ruling 32's order, then
  the IN-set hit files by descending admitting-hit count with ties broken by path. Written to
  `the_population_in_comparison_order`, replacing the first writing's list, **and the first writing's
  list is preserved beside it** under `the_population_before_ruling_33` (#12).
- **The size stop, re-evaluated** on the IN-set count outside the named members, threshold forty,
  outcome stated on the artifact. **The writing side predicts eighteen; the tool's figure is the one of
  record.** If the stop is reached, STOP after committing and report — that would refute Ruling 33's
  own factual ground and is the user's.
- Its `--check` mode re-derives and exits non-zero on any drift of the artifact from what it would
  now write, as every enrolled tool's does. **It reads the tree at the current commit and nothing
  else** (the artifact inventory and the specification set as they stand), so a later change to
  either input turns it STALE, which is what a check is for.

**(b) Enrol it.** In `tools/audit/gen_guard_state.py`, add ONE tuple to the authored invocation list,
directly after the entry for `gen_specification_document_set.py` and in that list's own form
(tool path, `["--check"]`, a reason string), with the authored comment line above it naming this
dispatch and Ruling 33. The reason, verbatim:
`the L0/L1 outgoing population re-derives from Ruling 32's named members, the term search over the three prose classes of the artifact inventory, and the cut to the ruled specification document set (Ruling 33). What it guards is not the term list, which is authored and says so, but that every named member is present at its path, that the searched population is the inventory's and not the file system's, that the cut reads the specification set as it stands, and that the residue is published whole — so a document moving under it, or a member leaving the specification set, halts the run rather than moving the population silently`.
**Nothing else in that file changes.**

**(c) Run the tool in write mode; read the artifact; then run the FULL guard set in CHECK mode.**
A4 must hold: population 76, `unclassified_candidates` empty, the new tool passing, the failing set the
ten named (or fewer, never more). A new red is classed by its measured cause; one you cannot class is
a STOP.

**(d) Commit** the tool, its artifact and `gen_guard_state.py` (plus any guard artifact the run
rewrote), subject:
`the L0/L1 outgoing population cut to the ruled specification set under Ruling 33, the residue published for the mining map, and the population tool enrolled in the guard set`.
Push; verify at the ref file.

**E1:** the artifact whole with the cut, the residue and both ordered lists; the enrolment tuple
present and the STOP cleared at a measured run; the size stop evaluated on the IN-set count and its
outcome stated.

## Task 2 — the comparison — ONE commit PER OUTGOING DOCUMENT, in the population's order

**The unit, fixed before you begin (Ruling 8: the grain of a unit is the decision).** An OUTGOING
STATEMENT is one sentence or one bullet of the outgoing text that states what the analysis does,
must do, may assume, or must not do, about L0's subject (what the notated record supplies, what
may be assumed of it, what happens when it does not supply it) or L1's subject (the brief's seven
faces (a)–(g): note eligibility, ties, the change-point set and slices, metric strength, notated
boundary evidence, the local cadence cues, the form of what is published). A sentence that is
none of those — narrative, provenance, a test count, a commit hash, a pointer to another document —
is NOT an outgoing statement and is listed once per document under *not a statement*, so that
completeness can be checked by arithmetic at the document. **A paragraph is never a unit.** Where
one sentence carries two claims, it is two statements, each quoted with the sentence and marked
(i) and (ii).

**(a) Per outgoing document, in the population's order, establish the population at the document
itself:** count its outgoing statements; the count goes in that document's manifest header in the
reading file and nowhere else.

**(b) Per outgoing statement:** quote it verbatim with its location (document and heading; a line
number only as a locator beside the heading, never as the anchor); name the derived statement or
statements (by S-number) that speak to it, or state that none does; on the **CURRENT-TEXT AXIS**
say, for each named derived statement, exactly one of **AGREES / DIFFERS / THE DERIVATION IS
SILENT** — evidence about the present text, never a verdict on the derivation; then the
**PROPOSED DISPOSITION**, exactly one of:

- **ADOPTED — carried**: a derived statement carries its content in substance (name it).
- **ADOPTED — proposed**: no derived statement carries it, it is not implementation-describing,
  not another charter's, not historical, and the worth test does not discard it. **This is a
  PROPOSAL for the user: the outgoing content would be added to the derived specification.** Write
  the proposal in one sentence beside the row. Never write it into the derivation.
- **RELOCATED**: its content belongs to another charter. Name the charter (L2, L3, the
  voice-leading axis, the measurement design) by `FRAMEWORK.md` §5's own words, and append the
  statement to the TRANSFER LIST section of the reading file.
- **QUARANTINED**: it states how the implementation currently works, or a measured property of it.
  Write the audit question in one sentence beside the row. (The derivation's §6.2 lists such
  passages it met inside the pack; where an outgoing statement is one of those, say so.)
- **DISCARDED**: under principle #10's worth test, with the finding, the date (2026-09-02) and the
  reason in the row — all three, or the disposition does not reach the row (D-677's shape).
- **HISTORICAL**: an event, a build state, a superseded plan, a status.
- **UNPLACED**: you cannot defend one disposition in one sentence at the two texts. Say what you
  read. **This is the honest default and is not a sixth disposition** — it is a row the user
  places.

**Where a derived statement DIFFERS from an outgoing statement**, the row carries, beside the
disposition, the DIFFERENCE stated in one sentence in both texts' own words, and NOTHING that
chooses between them: that choice is the user's, put later by the writing side as a surface over
your rows.

**(c) The derived side, once, after every document is done:** one row per derived statement S-1 to
S-54, in the derivation's order — the statement's own words (its first field), its source class,
and the outgoing statements that speak to it (by document and row) with the AGREES / DIFFERS /
SILENT verdict each carried, or **THE OUTGOING TEXT IS SILENT** where none does. **This row set is
the same matrix as (b) read from the other side; every AGREES or DIFFERS in (b) appears here and
vice versa — check it by arithmetic and state the check's result.**

**(d) The open questions OQ-1 to OQ-17:** listed, ungraded, each with one sentence on what the
outgoing text says about it, with location, or that it says nothing.

**(e) The derivation's §7, its five points:** each with one sentence on what the outgoing text
says about it (with location) or that it says nothing — the two structural points (notated versus
unfolded order as its own face; marks that fall where no change point is) named as such, as
§3al.4 records them. **No verdict on whether §7 is right.**

**(f) The independence record (§6 of the derivation):** relayed in its own section — what arrived
and how much was read, the stop-on-meeting record by file and location, the two elements not read,
the fetched sources, the primaries not re-read — **with no verdict word attached.** Where an
outgoing statement of this comparison is one of the passages §6.2 records the deriving session
having SEEN (in `02_the_guiding_principles…` lines 323–335 and 426–428 as the pack renders them,
or in `05_the_ratified_design_intent` entries D-023, D-024, D-033, D-057, D-207, D-221, D-224,
D-229), the row says so — that is a fact the user reads the AGREES verdict against.

**(g) The reading file** — `ratification_surfaces/cowork_comparison_l0_l1_reading.md`, in the
standing ratification-surface shape and following the pilot's file section for section: the
subject re-explained from scratch for a reader who knows music and not this project; the words
used, explained first; the population, named to the Task-1 artifact; the unit, stated; the
vocabulary and that it is closed; then the tabulation per outgoing document in the population's
order, each document opening with its manifest header; then the derived-side rows; then the open
questions; then §7; then the TRANSFER LIST (every RELOCATED row gathered, by target charter); then
the AUDIT QUESTIONS (every QUARANTINED row gathered); then the PROPOSALS (every ADOPTED — proposed
row and every DIFFERS row gathered, each in one sentence, with nothing chosen); then the
distribution of dispositions and of current-text verdicts BY COUNT AT THE FILE'S OWN ROWS, per
document and in total, with the arithmetic check that every outgoing statement carries exactly
one disposition and every derived statement exactly one row; then *what the user is asked to rule*
— NOTHING in this file; the surface follows from the writing side — and *what this file does not
do* (it decides nothing; it establishes nothing; it applies no disposition; every row is
re-placeable at the quoted texts). **Neutral framing throughout.**

**(h) Batching, ruled (D-672 — a per-entry pass may be stopped at any member boundary, and the
stop is recorded).** The reading file is built one outgoing document at a time, in the population's
order, and **committed after each document** with the subject
`comparison L0/L1: <document name> tabulated — <n> outgoing statements placed, proposals only`
where `<n>` is that document's count from its manifest header. Push after each commit. **If your
context is degrading — the tells: citing a summary instead of the source, a count never derived,
a defined term glossed — STOP AT THE NEXT DOCUMENT BOUNDARY**, commit what is done, and record in
the reading file's header and in your report: which documents are done, which are not, and that
the remainder is UNTOUCHED rather than partly worked. **A partly worked document is never
committed.** The next dispatch resumes at the recorded boundary.

**E2:** for every document done: its manifest count; every outgoing statement with exactly one
disposition or UNPLACED with what was read; every DIFFERS with its one-sentence difference and
nothing chosen; the transfer list, the audit questions and the proposals gathered; the derived-side
rows and the arithmetic check (only once all documents are done); the independence record relayed;
no recommendation anywhere; A5 intact.

## Task 3 — the close

1. One `STATUS.md` pointer entry per task that did work (no distribution restated — the pointer
   names the reading file and the population artifact); the previous batch's entries move through
   `gen_status_batch_bound.py --apply` (D-648, licensed in terms); `gen_session_start_read_size.py`
   regenerated.
2. The FULL close appended to `cowork_away_returns.md` as a **THE L0/L1 COMPARISON TABULATED (SECOND
   WRITING), NOTHING DECIDED** section — both guard states, every SHA, E0–E3 graded, A1–A5 graded, declared
   departures, quarantined questions (the reading file's audit-question section is pointed at, not
   restated), the plan lines (**the writing side puts the DISPOSITION surface to the user over
   your rows, one decision per turn — the next act; the L0/L1 split returns at ratification; the
   pack finding of §3al.4 goes to L2's pack; then L2, sequenced by Ruling 10**), and **the plan's
   tell in one sentence: did this batch produce anything other than the landed records, the
   population artifact, the reading file and the report? If yes, name it.** The report is
   `cc_report_comparison_l0_l1_second_2026_09_02.md` — which likewise decides nothing; where the report
   would name hashes it cannot contain, point at the git log.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **The ordered
   structure yields: Task 0, Task 1, one commit per outgoing document, the close, the end state.**

**E3 — DERIVED from the declared start state plus this batch's footprint, as standing clause P-3
requires:** at the tree carrying the close, a fresh full guard run — **population 76** (75 plus the one
enrolled tool); **zero STOPs** (the one at the start cleared by Task 1's enrolment); **the failing set
exactly the ten named in the start state, LESS any that this batch's own ordered acts clear** (Task 3's
regeneration of `session_start_read_size.json` may clear nothing or one — report which), **and PLUS
NONE** — committed only after the run that produced it. A red this batch causes that is not an ordered
act's own is the STOP-and-report of A2.

## What this batch does NOT do

- **No decision on any disposition, any difference, the derivation, the method, or the L0/L1
  split. No verdict on the deriving session's independence. No session booted. No measurement
  built or run.**
- **No edit to the derivation, the brief, the pack, its generator, its artifact, any outgoing
  text, any governing document other than the two Task 3 orders, any register source.** The
  amended population tool and artifact, the one enrolment tuple, the reading file and the report
  are the whole authored write set.
- **No finding number. No open-items row created, flipped or discarded EXCEPT an apparatus-finding
  row under rule (c). No `src/` change, no golden, no test, nothing under `tools/corpus/` or
  `tools/robust_stop/`.**
- **One guard enrolment and no other:** the population tool, by Task 1, on Ruling 33's record.
- **No disposition of any residue file.** The residue is published and left for the mining map.

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #19 — the reading file is authored evidence for the user's ruling and
   establishes nothing; every row re-placeable at quoted texts; the population's reach stays a
   stated lower bound; the cut reads a ruled, derived set rather than a session's judgment of
   kind; #6 — the five dispositions have one home; the specification set has one home and is read
   from it; #12 — the residue is published whole, the pre-cut population list is preserved beside
   the cut one, UNPLACED and SILENT are kept beside ADOPTED; #13 — the three defects of the first
   writing are named at the head and each has its repair in the ledger; the enrolment is stated as
   the act it is; #17(f)/D-431 — counts live at the artifacts' and the reading file's own rows, the
   writing side's eighteen declared a prediction; #24 — the two axes are kept apart. Conforms.
2. *Conventions:* American English; no self-invented labels — the disposition words are the phase
   definition's, the verdict words the comparison-design sitting's, *residue* is the plain word
   for what is left and is defined where it is first used; music-theory words in their musical
   sense; *exclude* for the prohibiting sense; towards, never against.
3. *Figures and premises:* the tip and its parent read at the ref files after the batch; the
   guard summary read at the object the end-state commit itself wrote, and its path list checked
   at `git show --stat` by explicit hash; the sitting record's and the handoff's tracked status and
   blob identifiers by `git rev-parse` at the explicit hash, the handoff's on-disk size against
   the blob's; the population counts and the specification set at their artifacts; the runner's
   authored list read at the file. The eighteen is this side's own intersection and is labelled a
   prediction.
4. *File-tools rule:* every repository read through the file tools on bridge-staged snapshots; git
   object queries by explicit hash only; no `git status`, no working-tree diff; one listing of
   `tools/audit/` read to find the specification-set artifact by name.
5. *Uncertainty:* A2's membership reaction and A3's reaction to the new ratification-surface file
   are measure-and-report items; the size of the IN-set population is a prediction until Task 1
   publishes it; whether Task 3's regeneration clears a standing red is reported, not predicted.
6. *Re-read from disk before release:* performed over the committed file after it is written to
   the root, hash-compared with the written copy, recorded in the ninety-first entry.

---

*Provenance: Cowork writing side, 2026-09-02, the session that booted on
`cowork_handoff_entry_eighty_nine.md`, at tip `8f1e4e9d…` with `origin/master` equal, under Ruling 33
and Ruling 32 of `cowork_rulings_2026_08_31_decision_surface_sitting.md`, applying Rulings 1–4 and §5 of
`cowork_rulings_2026_08_24_comparison_design_sitting.md` and the disposition discipline of the phase
definition. TOWARDS the ultimate objective and TOWARDS the guiding principles.*
