# CC dispatch — the document routes applied, the three delegations written, D-472 homed

> **Status: ACTIVE DISPATCH, written 2026-08-08 (Cowork).** Read IN FULL — and read FIRST, in
> this order: **`cowork_document_route_rulings_2026_08_08.md`** (the ruling record this dispatch
> applies) and the **dated amendment of 2026-08-08 in `cowork_owner_rulings_2026_08_07.md`**
> (D-472's ruled form). Neither is restated here (#6; D-643 satisfied by reading them whole).
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_document_routes_and_d472.md`.
>
> **★ THE FREEZE IS NOT RELAXED.** No mechanism changes this wave. Everything found is rowed and
> left, except a finding whose subject bears on the analysis, its inputs, or a measurement tool
> a measurement depends on — SURFACED, not acted on (D-641). An establishment obligation (#19)
> always gates and is always surfaced. **OI-348 is NOT worked here** — its family remedy awaits
> the user's ruling.
>
> **★ NO FIGURES (D-431) AND NO STATE NOT READ (#17a).** No `src/` change, no goldens, no
> corpus, no `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to
> inference, no design. Phase 1 under D-231. **Never bash to read working-tree files (D-253) —
> any dialect, any mechanism; see the rule's 2026-08-08 clause.**
>
> **★ Same-commit rule (D-230)** for every register movement and for the new conformance row
> (index row + detail file in the recording commit). **Commit only when the user asks** — leave
> the tree uncommitted at close and record the worktree enumeration.
>
> **★ Item 2 of the finish line is not started. Phase 1's completion statement is not written,
> drafted, or partially written.**

## 0a. THE RULING LEDGER

- **R1 — the three delegations (route i), ruled by the user 2026-08-08.** The ruling record
  carries the draft clauses and placements. The delegation is the user's act executed by this
  session under the ruling (the OI-293 precedent); each clause must be an explicit delegation
  clause in a form **D-432** admits, and each placement is verified against the owning section
  before writing — **a placement whose owning section is other than the record names is a STOP,
  reported before anything is written.**
- **R2 — the thirteen re-home documents (route ii), same ruling.** Per the record's table, with
  the standard per-entry discipline and STOPs.
- **R3 — D-472 homed in its ruled form and the conformance question ROWED**, per the dated
  amendment: the Layer-6 home stating the arm split (grouping rule live at its citation; Pass-4
  stabilization precondition ⚠ LEGACY at its citation), and a NEW open-items row for whether the
  record arm satisfies the stated stabilization precondition at all — index row + detail file in
  the same commit, the row's subject stated as analysis-bearing, its probe timing left to the
  user.

**None of R1–R3 authorizes a fix to the analysis, a design, or an inference change.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork this session:**

- **F1.** The ruling record and the D-472 amendment, both written by Cowork from the ratified
  surfaces and committed at `d1891db158`'s successor tree (the amendment) / this worktree.
- **F2.** The bar-excluded population at HEAD (sixteen documents, their entry lists):
  `tools/audit/phase1_finish_line.json`, the class-2 item.
- **F3.** D-472's mixed-arm evidence at its two citations, per the homing wave's report and its
  held-entry record in `tools/audit/decisions/item1_rehome_blocker.json`.

**ASSUMPTION — each checked BEFORE the act it licenses, with a STOP if refuted:**

- **A1 (per delegation).** The named placement is the owning section for the stated concern,
  and the clause as drafted passes the delegation bar (D-432's admitted forms). → verified per
  clause; a failure is a STOP with the finding, never a reworded clause written silently.
- **A2 (per re-home entry).** The destination owner is determinate and its section states
  rules. → per entry; soft owners and findings-not-rules sections are held with reasons, the
  rest homed.
- **A3.** The three delegations move their documents' entries out of item 1's class 2 by the
  DERIVED route (the delegation partition re-derives them as contract-homes) — nothing is
  hand-reclassified. → regenerate and confirm the movement is derived; a hand edit of any
  class is forbidden.
- **A4 (the superseded checks).** `cowork_target_architecture.md` and each
  `docs/architecture_joint_inference.md` entry: whether the content is live or superseded by
  the ratified architecture is READ, cited, and reported — reported findings, never assumed;
  an entry found superseded is recorded superseded with its evidence, not copied into a
  specification.

## 1. Task 1 — Write the three delegations (R1)

Discharge A1 per clause; write each at its verified placement, adds-only; regenerate the
delegation-derived artifacts; discharge A3.

## 2. Task 2 — Apply route (ii) (R2)

Per the record's table, with A2 per entry and A4 where named. LEGACY / shelved / superseded
markings ride along; nothing superseded is copied.

## 3. Task 3 — D-472 and the conformance row (R3)

Home per the amendment; create the row (index + detail, same commit) stating the question, both
citations, and that its subject bears on the analysis — the timing of its read-only probe is
the user's.

## 4. Task 4 — Close

Regenerate everything that follows the register — the finish line's STOP must account for every
entry that leaves item 1 by either route. Re-aim drifted anchors per citation (expect many).
Guards at the tree; **fix nothing**. Worktree enumeration to a named artifact;
`tools/audit/process_check.py` over this dispatch; one `STATUS.md` POINTER entry. Tree left
uncommitted.

## 5. Accepted outcomes

**A1 or A2 STOPs are results** — held clauses and held entries come back with reasons. **A4
finding content superseded is a result and its recording is the correct act.** **The finish
line's STOP firing on an unaccounted entry is a STOP.** **Item 1 NOT reaching empty is
expected** — the archive-unhomed eleven and any held entries remain, and the artifacts say
exactly what is left.

## 6. Self-check (D-434) — run by Cowork before release

- **Ruling ledger:** three rulings pointing at their records read whole; the delegation act's
  authority chain stated (user's ruling, session execution, OI-293 precedent).
- **#17(a):** three facts; four assumptions with ordered checks and STOPs.
- **Principles:** #6 at never-copying and the delegation-over-duplication choice; #7 at every
  destination; #12 at every preservation and marking; D-432 at the clause forms; D-431 — no
  figure here; D-253's widened clause restated in the header.
- **Scope:** no OI-348 work; no mechanism change; the conformance row created but its probe not
  run.
