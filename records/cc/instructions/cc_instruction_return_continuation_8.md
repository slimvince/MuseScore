# CC dispatch — the eighth return continuation: Rulings 40–43 applied, the homing pass closed, and the tail through the batch close

> **Status: ACTIVE DISPATCH, written 2026-08-09 (Cowork), at a verified STOP** — the
> seventh continuation closed clean after its Tasks 0–2; all three commits verified
> ancestors of `origin/master`; guards at zero failing; the inversion verified by Cowork AT
> THE ARTIFACT this time (`tonic_agreeing` exceeds `tonic_disagreeing` at the named
> column), and the A1 refutation verified at the returns file and the queue's §9.4. Read IN
> FULL, and read FIRST, in this order:
> **`cowork_rulings_2026_08_09_eighth_stop.md`** (Rulings 40–43, each read WHOLE, D-643),
> the seven earlier 2026-08-09 ruling records with their correction notes,
> **`cowork_away_returns.md`** END TO END (§1.12's per-entry route rows are Task 1's
> input; §1.13 is Task 2's), and **`cc_instruction_return_continuation_7.md`** (whose
> unstarted Tasks 3–5 this dispatch resumes; its adopted frames govern except where
> Rulings 40–43 and the task list below differ).
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_return_continuation_8.md`.
>
> **★ All standing rules as adopted: hold-don't-guess into the returns file; D-641
> surfacing; #19 always surfaced; D-253 in every dialect and mechanism; NO TRANSCRIBED
> VALUES (D-431); read-only on the analysis — no behaviour change, no golden, no corpus of
> scores, no `tools/corpus/` or `tools/robust_stop/` movement, no fix to inference, no
> design. The licensed acts outside the read-only default, each naming its ruling:
> Ruling 16's ONE comment-only `src/` commit; Ruling 40's census edits (rule-stating
> sections, these nine entries only); Ruling 41's one `docs/` sentence; Ruling 42's one
> `CLAUDE.md` entry. Homings in the owning section's own voice (#7, rule (e)); D-230
> same-commit; commit and push per task boundary; `origin` only, NEVER `upstream`. Phase
> 1's completion statement is not written, drafted, or partially written by this batch.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork at this STOP, each at the object named:** the three
seventh-continuation commits and their push state; guards at zero failing; the nine held
entries with both routes named per row (`finish_line_item1_routes.json`; returns §1.12);
the inversion's corrected state at every surface in the batch's authority and its one held
instance (§1.13); the queue's §9 as ratified by Ruling 42; the OI-357 answer's two
comparison artifacts re-deriving byte-identically.

**ASSUMPTION — checked before the act it licenses, STOP into the returns file if refuted:**

- **A1.** Ruling 40's procedure, per entry and in order: the census's candidate section is
  READ before any step-1 pointer move (the rule must actually be stated there, not
  paraphrased); the kind half is judged per section before any step-2 write; a step-3 case
  is HELD with its row named. An entry fitting none of the three steps is a STOP, not a
  judgment.
- **A2.** The remaining homing population (beyond the nine) is derived fresh at task
  start; owner-indeterminate entries HELD with candidates named.
- **A3.** Ruling 16's family is derived by the sweep; the commit verified comment-only at
  its own diff.
- **A4.** Any INDEX row flip runs under the canonical-status lint and the both-ways
  discipline where a parser-visible state moves.
- **A5.** Every population derived at task start from the artifact that owns it.

## The tasks, in order

**Task 0 — the rulings recorded and the small licensed edits.** Ruling 42's one register
entry (the Ruling 38 decision at `CLAUDE.md` beside rule (g), licensed wording in the
section's own voice); the queue re-bannered with §9's closing state (upgrades declined,
ground recorded); Ruling 40 recorded on the nine route rows; Ruling 41's one-sentence
correction in `docs/key_detection_baroque_partial_signature.md` (former preserved);
Ruling 43 recorded — the widening accepted on the D-660 ground at the run artifact's own
closing state, and OI-357's answer recorded on its row and OI-363's (flip OI-357 if its
question now reads answered in both directions at the row's own terms; otherwise state
what remains).

**Task 1 — Ruling 40 executed over the nine** (A1; partial-stop at entry boundary
accepted and recorded; every step-3 hold named for the user).

**Task 2 — the remaining homing population** (A2; same discipline; partial-stop
accepted).

**Task 3 — the reach-verdict derivation, then the session-executable gating rows**
(unchanged; the derivation is one generated act and is not partially published).

**Task 4 — Ruling 16's sweep and its ONE comment-only `src/` commit** (A3; OI-353 flips
on it).

**Task 5 — OI-346's marks and the per-row sizing pass (leftover capacity), then final
close:** batch summary appended to the returns file — tasks completed, holds, surfacings,
the finish line's end state derived fresh — last commit and push.

**Edit authority:** the seventh continuation's list, PLUS `cowork_score_census.md`
(Ruling 40, rule-stating sections, the nine only), `docs/key_detection_baroque_partial_signature.md`
(Ruling 41's one sentence), and `CLAUDE.md`'s decisions-register section (Ruling 42's one
entry). **Anything else: hold.**

## Accepted outcomes

Holds are results; every step-3 census case held is a result the user rules on next; a
STOP on any assumption returns with evidence; a capacity stop at any clean task boundary
is accepted and recorded as every previous continuation recorded theirs.

## Self-check (D-434) — run by Cowork before release

Written at a verified STOP with nothing running; every ruling applied is on disk in a
ratified record and ordered read whole (D-643); populations derived at task start; the
licensed acts are the only exceptions to the read-only default, each naming its ruling and
its scope; the census licence is entry-scoped and section-kind-gated; the push hard stop
carried; the completion statement barred; no value restated (D-431); reserved-word
discipline checked over this file's own text.
