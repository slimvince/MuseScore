# CC dispatch — the away execution: written at the 2026-08-08 full STOP, from complete knowledge

> **Status: ACTIVE DISPATCH, written 2026-08-08 (Cowork), at a genuine STOP** — the
> document-routes wave returned and was verified at the objects; nothing is running; every
> ruling this dispatch applies is recorded on disk. Read IN FULL, and read FIRST, in this
> order: **`cowork_rulings_2026_08_08_pre_away.md`** (the three pre-away rulings),
> **`cowork_ruling_guard_family_2026_08_08.md`** (the family design), and
> **`cc_instruction_away_batch.md`** (the RE-BANNERED PLAN — its ruling ledger R2/R3/R4, its
> returns-file and STOP-note mechanics, and its edit-authority frame are ratified substance
> this dispatch adopts; where this dispatch and the plan differ, THIS dispatch governs, and the
> differences are the pre-away rulings and the task order below).
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_away_execution.md`.
>
> **★ THE USER IS AWAY. HOLD-DON'T-GUESS; CONTINUE-PAST-HOLDS.** Everything needing a ruling
> goes to **`cowork_away_returns.md`** (created in Task 0) with its reason; the batch moves on
> to work not depending on it. A finding bearing on the analysis, its inputs, or a measurement
> tool a measurement depends on is SURFACED there and pauses every task depending on it;
> independent tasks continue (D-641, #13). An establishment obligation (#19) is always
> surfaced. **Before each task, read the returns file; a note headed STOP halts the batch.**
>
> **★ THE FREEZE is relaxed ONLY by the two ruled mechanism changes:** Task 0's classifier
> apply under pre-away Ruling 1, and Task 1's guard family under the family ruling (with
> OI-351 folded in by pre-away Ruling 3). Both D-436-licensed by their records.
>
> **★ NO FIGURES (D-431), NO STATE NOT READ (#17a), D-253 IN EVERY DIALECT AND MECHANISM.**
> Read-only on the analysis throughout: no `src/` change, no goldens, no corpus, no
> `tools/robust_stop/` movement, no behaviour change, no fix to inference, no design. Phase 1
> under D-231. Same-commit rule (D-230) throughout.
>
> **★ COMMIT AND PUSH PER TASK BOUNDARY** per the plan's R3, hard stop included: `origin` =
> `slimvince/MuseScore` ONLY, NEVER `upstream`; push failure reported in the returns file and
> worked around by nothing.
>
> **★ Phase 1's completion statement is not written, drafted, or partially written by this
> batch — even if every item closes, the user commissions it.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork at this STOP (2026-08-08):** the register is current (645
verbatims at zero drift, `--check` green, run live); the three delegations stand in
`ARCHITECTURE.md` (the joint-estimator section, §11.5, §19); the four stale artifacts are
exactly those `open_items/OI-350.md` names; D-601's owner and licence are as pre-away Ruling 2
records; OI-349 gates with its probe authorized (plan R4); the phase-1q snapshot exists and is
established (OI-305/OI-319).

**ASSUMPTION — checked before the act it licenses, STOP into the returns file if refuted:**

- **A1.** The epoch treatment leaves every field outside its intended writes byte-stable —
  field-diff of the classifier's data before/after the apply; movement anywhere else is a STOP.
- **A2.** After the apply, `gen_home_classification.py --check` is green and the six blocked
  generators run, their movement accounted for by derivation (route (i) closed, the re-homed
  twenty-seven at their new homes, D-472 at Layer 6). Any hand-assignment is forbidden.
- **A3.** Per-entry owner determinacy, per the standing homing discipline (Tasks 2, 5).
- **A4.** The probe changes nothing — verified at its task's diff (empty but the probe's named
  artifact).
- **A5.** The guard family's changes move no verdict on any pre-existing corpus row except
  those the family design names — both rates on the extended corpus, revert condition
  governing.

## The tasks, in order — after each: self-check (D-434), guards, anchors per citation, worktree enumeration, STATUS pointer entry, ONE commit, push

**Task 0 — OI-305/OI-350 resolution FIRST (pre-away Ruling 1), then reconcile.** Epoch-declare
the phase-1q artifact historical against its established snapshot; run the apply; discharge A1
and A2; regenerate the four stale artifacts and the finish line; flip OI-305 and OI-350 with
provenance; report OI-319's remaining scope on its row. Create the returns file with the
batch's start state. *This runs first because every later task's populations come from the
generators this unblocks.*

**Task 1 — the guard family (family ruling + pre-away Ruling 3).** Corpus first, including
OI-351's observed command and path-form variants; blindness measured at the unwidened guard AND
the live hook; the five design parts; both rates; A5; revert condition. OI-348 flips on
success; OI-300 retires or residuals named; OI-351 flips or is held with its established cause.

**Task 2 — the archive-unhomed eleven, plus D-601** (pre-away Ruling 2's one licensed edit of
`cowork_confidence_contract.md`). Per entry into the owning specification; archives untouched
(#12).

**Task 3 — the defense gaps.** Per entry: SEARCH the record; record the defense it holds at the
entry's home, or "derivation not recorded"; memory forbidden; homes outside edit authority
held.

**Task 4 — the reach-verdict derivation.** ONE generated derivation applying D-639's test, as
its first application was generated; verdicts derived with reasons.

**Task 5 — the section-unreached four and the findings-not-rules thirteen.** Per-entry
re-homes; rule (e)'s preference; soft cases held.

**Task 6 — the session-executable gating rows.** Enumerate the TRUE-half gating set at HEAD;
perform what is session-performable inside this batch's authority, flip with provenance; hold
the rest with reasons.

**Task 7 — the OI-349 conformance probe (plan R4).** Read-only; A4 at the diff; the finding —
whichever way — into the returns file with citations; a NOT-SATISFIED finding is a #13 surface
and pauses nothing after it (no later task depends on it).

**Task 8 — OI-346's mark application** (leftover capacity), then final close: a batch summary
appended to the returns file — tasks completed, holds, surfacings, the finish line's end
state — and the last commit and push.

**Edit authority, complete list:** `ARCHITECTURE.md`; `docs/scoring_model.md`, `CLAUDE.md`,
`BUILD_AND_TEST.md` (D-645, homing acts); `cowork_confidence_contract.md` (ONE edit, Ruling 2);
the register data and generated files; `OPEN_ITEMS.md` + detail files; the guard tooling and
classifier under the two ruled changes only; the returns file and `STATUS.md`. **Anything
else: hold.**

## Accepted outcomes

Holds are results; empty yields are results; the batch halting early on an analysis-bearing
surface is the mechanism working. A2 failing (the generators still blocked after the apply) is
a STOP for every task needing them — Tasks 1 and 7 are independent and proceed; the rest hold.

## Self-check (D-434) — run by Cowork before release

Written at a verified STOP with nothing running; every ruling applied is on disk and read
whole; populations derived at task start, none carried; the two mechanism changes are the only
freeze relaxations and both are user-ruled; the push hard stop carried; the completion
statement barred; hold-don't-guess at every judgment point.
