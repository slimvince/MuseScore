# CC DISPATCH — BUILD THE EMPIRICAL FINDINGS LEDGER

*Written by the Cowork writing side, 2026-08-26, against tip
`673d0eec4e66fc62ceb9eb7d33faf7aef98d4c7f`. Executes Ruling 1 of
`cowork_rulings_2026_08_26_ledger_form_sitting.md`. This batch performs NO ratification, orders NO
register entry, and MAKES NO ADMISSION — admission is never a session's act.*

---

## 0. What this batch is

The empirical findings ledger is the record of what this project has measured about the analysis, as
opposed to what it has decided. It is the last input the framework phase waits on and it does not
exist. Its gate is ruled, its five-field entry shape is ruled, and its seed — thirty-one admissions the
user dispositioned on 2026-08-26 — exists. **This batch builds it, as a single hand-written record
whose entries CITE the five fields where they already stand rather than restating them.**

**The prohibition that shapes everything below: you do not admit anything.** The thirty-one are the
user's admissions. Your job is to re-check each at the gate as Ruling 8 requires, to write the record,
and to report — never to decide what enters.

---

## Task 0 — start state and landings

**(a)** Read `.git/refs/heads/master` with the file tool. It must read
`673d0eec4e66fc62ceb9eb7d33faf7aef98d4c7f`. **If it does not, STOP and report.** Name the side
measured wherever you state a hash.

**(b) Do NOT run `git status`** (D-253). Run `python tools/audit/changed_paths.py`; record the
population; commit none of the standing untracked population.

**(c) Land, in one commit:** `cowork_rulings_2026_08_26_ledger_form_sitting.md` (untracked, the record
this dispatch executes); this dispatch; and **`cowork_rulings_2026_08_26_sizing_tests_sitting.md`,
which stands TRACKED-MODIFIED** — a §8 *Corrections of record* was added to it after the previous batch
returned, **additions only, no sentence above it reworded and no ruling amended**. Establish that
before committing, as the previous batch did for the handoff: compare the working tree against the blob
at the tip and report what differs.

Then `python tools/audit/gen_evidence_pin_membership.py`.

---

## Task 1 — the third ruled seed: establish whether it was mined

The ledger's ruled definition names **three** seeds (`cowork_rulings_2026_08_15_method_directions.md:46–54`,
restated at `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md:217–219`):
`DEFECT_TYPES.md`, `docs/scoring_model.md` §8, and **the refuted-repair register entries**.

The first two are accounted for. The third is not, and the writing side could not settle it:
`cc_report_ledger_harvest.md` §9.1 says *"Task 3 names the second and the third and not the first"*,
while §8's source table lists `OPEN_ITEMS.md` and `open_items/` — a different register from the
decisions register — and **no candidate in either harvest names the refuted-repair register entries as
its source.**

**(a) Establish what the phrase denotes at the objects.** Which register, and which entries qualify as
*refuted-repair*. Quote the text you rely on.

**(b) Establish whether that population was mined**, by searching both harvests' candidates for any
entry whose provenance is such a register entry, and by reading `cc_report_ledger_harvest.md` §8 and §9
at the object rather than through this dispatch's account of them.

**(c) If it was NOT mined, mine it now — as a THIRD HARVEST, appended to
`cowork_empirical_findings_candidates.md`, numbered from the highest existing candidate identifier + 1,
each naming its source.** Do not edit, renumber or re-verdict any existing candidate; the second
harvest's own banner establishes that discipline and you follow it.

> **★ AND THIS IS A DEPARTURE FROM WHAT THE WRITING SIDE TOLD THE USER, DECLARED HERE RATHER THAN MADE
> SILENTLY.** The surface said this task would carry a STOP condition so the ledger is not built on a
> seed missing a ruled member. **It does not stop, and the reason is that the stop would have been
> wrong:** the ledger is built from ADMISSIONS, not from seeds, and a newly harvested candidate cannot
> enter it until the user has dispositioned it. Stopping would gate the framework phase behind a
> harvest nobody has ruled on. **So the ledger is built on the thirty-one regardless, and the third
> harvest's yield goes to the user as candidates.**
>
> **What replaces the stop:** the ledger's own banner records **which of the three ruled seeds are
> represented in it and which are not**, so it is never read as complete when it is not. If Task 1
> yields candidates, the banner says so and says they are undispositioned.

---

## Task 2 — re-check all thirty-one admissions at the gate, per Ruling 8

Ruling 8 of `cowork_rulings_2026_08_21_successor_plan_sitting.md` requires **every hand admission to be
re-checked at the ledger's gate when the ledger is built.** This is that re-check, and it is the reason
the gate exists.

**The gate, in its ruled words:** *does the fact survive the implementation being thrown away?* — and
the entry must be **approach-level**. A gate can refuse entry; that is its job.

**Report the verdict for each of the thirty-one, one by one**, from
`cowork_fact_gate_admissions_2026_08_26.md`, each with the fact's identifier and a one-sentence ground.

**Where a re-check REFUSES an admission the user made:**

- **Do not enter it in the ledger's body.**
- **Do not drop it either.** List it in the ledger's banner under **REFUSED AT RE-CHECK**, with its
  identifier and your ground, and carry it to the report.
- **Propose nothing about it.** The admission is the user's; the re-check is Ruling 8's; reconciling
  the two is the user's act and not this batch's.

**If the re-check refuses none, say so in terms** — a re-check that never refuses anything is worth
being able to see.

---

## Task 3 — write the ledger

**Path: `EMPIRICAL_FINDINGS_LEDGER.md` at the repository root.** Chosen by the writing side on the
existing convention for governing documents at the root (`CLAUDE.md`, `STATUS.md`, `DECISIONS.md`,
`DEFECT_TYPES.md`, `OPEN_ITEMS.md`); it is not ruled and the user may rename it in one line.

**Each entry carries:** the fact in one sentence; its identifier; its gate verdict from Task 2; and
**the citation to where its five ruled fields already stand** — file, section and line span. **It does
NOT restate the five fields.** That is Ruling 1, and it is the whole point of the form chosen: no
second copy of a fact that already exists in the shape.

**The banner carries:** that this IS the ledger; the gate in its ruled words; the five-field shape and
its two attached rules, quoted from the ruling that states them; **which of the three ruled seeds are
represented and which are not**; the REFUSED AT RE-CHECK list; and, if Task 1 harvested, that its
candidates are undispositioned and not in the ledger.

**★ One consequence of the chosen form, stated so you do not act on a sentence it has overtaken.**
`cowork_empirical_findings_candidates.md` says of itself that it is *"a working list superseded by the
ledger itself"*. **Under this form the ledger CITES INTO it, so it is load-bearing and must NOT be
retired, pruned, superseded or re-bannered by this batch.** The contradiction is the writing side's to
resolve; report it, act on none of it.

---

## Task 4 — the root-population hazard, checked BEFORE the ledger is written

`gen_filing_convention_application.py` derives its candidate population over the repository root's
`*.md` files, and it is the guard behind the one standing DECISION red, `[[OI-372]]` — whose candidate
list is currently exactly three. **A new root-level `.md` can in principle enter that population and
widen a red this batch is forbidden to cure.**

**Read that tool's candidate derivation and report, before writing the ledger, whether
`EMPIRICAL_FINDINGS_LEDGER.md` will enter it.** Then run the sweep with the ledger on disk and report
the candidate list.

**If the list widens: report it prominently, do NOT classify it, do NOT cure it, and do NOT regenerate
the guard.** The batch still completes — the ledger is the deliverable — but the widening is a
consequence the writing side must see, and it was anticipated rather than discovered.

---

## Task 5 — `STATUS.md`, the forward bound, the sweep, the report, the commit

**(a)** One **POINTER** entry in `STATUS.md` (OI-222 remedy; **D-431**: no count, no identity, no
rendered value). **Write it BEFORE running the forward-bound tool** — the reverse order makes the
tool's occurrence test find zero and STOP.

**(b)** Re-aim `tools/audit/gen_status_batch_bound.py` — the **five** aiming constants, and **append**
the outgoing aiming to `PREVIOUS_AIMINGS` rather than overwriting it (#12); both are inside the named
carve-out. Read the tool's own parser for the flag; report the exact command line and the values set.
**`TASK` is a choice — declare which task number you used and why.**

**(c) The sweep**, as ruled: `gen_guard_state.py`, then `gen_guard_classification.py`, in that order.
Three reds are standing and are **not yours to cure**: `[[OI-372]]`'s guard,
`apply_soft_discard.py --check`, `apply_residue_discard.py --check`. A staleness red caused by this
batch's own writes is cured under the standing sweep rule. **For any other red: if you cannot tell
whether it is a decision red or a regeneration red, treat it as a DECISION red and STOP.**

**(d)** Write `cc_report_ledger_build.md` at the root, then commit. State separately: the Task 0(c)
establishment of the modified ruling record; Task 1's finding and any third harvest; **all thirty-one
re-check verdicts**; whether the root population widened; every path written; and **every departure and
every instruction you could not obey.**

---

## §6 THE FENCE

Writes permitted at **exactly** these paths:

- `EMPIRICAL_FINDINGS_LEDGER.md` — new
- `cowork_empirical_findings_candidates.md` — **APPEND ONLY**, and only if Task 1(c) fires
- `cc_report_ledger_build.md` — new
- `STATUS.md` — one pointer entry
- `tools/audit/gen_status_batch_bound.py` — the five aiming constants and the appended row, carve-out
- the three Task 0(c) landings and `tools/audit/evidence_pin_membership.json`
- **any file a tool this dispatch orders you to run writes as its own output.** Name each in the report.

**Explicitly forbidden.** No `CLAUDE.md`, `ARCHITECTURE.md` or `DECISIONS.md` edit. **No register
entry** — this batch performs no ratification, so rule (c) is not engaged and the two mutually
unsatisfiable discard-act checks stay out of its path, as the last two batches were shaped to do.
**No admission made, changed or refused as an act** — the Task 2 re-check REPORTS, it does not decide.
No existing candidate edited, renumbered or re-verdicted. `cowork_empirical_findings_candidates.md` not
retired, pruned, superseded or re-bannered. No `src/` change, no test changed, moved or run, no golden.
Nothing under `tools/corpus/` or `tools/robust_stop/`. No open-items row created, flipped or discarded.
No finding number allocated. Neither blind output opened. Neither pack, the generator, the manifest or
any withheld family touched. No other `.py` source edited. Do not cure the two discard-act checks; do
not regenerate `[[OI-372]]`.

**★ THE STANDING CLAUSE.** **If obeying any instruction here would require a write outside this fence,
STOP and report the conflict. Do not choose a route, do not widen the fence, and do not substitute a
weaker form of the instruction to stay inside it.** Stopping and reporting is the correct outcome.
