# CC dispatch — the return continuation: the ten 2026-08-09 rulings applied, then the away program's Tasks 3–8

> **Status: ACTIVE DISPATCH, written 2026-08-09 (Cowork), at a verified STOP** — the away
> batch stopped clean after its Tasks 0–2; all three commits were verified at the objects
> (each shown by explicit hash, each an ancestor of `origin/master`, archives untouched,
> the T1 establishment artifact read against every claim). Nothing is running. Read IN FULL,
> and read FIRST, in this order: **`cowork_rulings_2026_08_09_return.md`** (the ten rulings
> this dispatch applies — every one read WHOLE, D-643), **`cowork_away_returns.md`** END TO
> END (the live returns file; this dispatch CONTINUES that program and appends there per
> task), and **`cc_instruction_away_execution.md`** (the away dispatch whose Tasks 3–8 this
> continues; its returns-file mechanics, its edit-authority frame and the plan's R3
> commit-and-push-per-task rule are adopted here; where the two differ, THIS dispatch
> governs, and the differences are the ten rulings and the task order below).
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_return_continuation.md`.
>
> **★ HOLD-DON'T-GUESS.** Anything needing a judgment this dispatch does not settle goes to
> `cowork_away_returns.md` §1 with its reason; independent tasks continue. A finding bearing
> on the analysis, its inputs, or a measurement tool a measurement depends on is SURFACED
> there (D-641, #13); an establishment obligation is always surfaced (#19). A STOP-headed
> entry in the returns file halts the batch. Read the returns file before each task.
>
> **★ THE FREEZE is relaxed ONLY by the ruled acts:** Rulings 1–4's named edits and removals,
> Rulings 5–7's homing/supersession/split acts with their STOP conditions, Ruling 8's
> authored triage entries, Ruling 9's read-only probe. Nothing else is fixed, however broken
> it looks — rowed and left (D-641).
>
> **★ NO TRANSCRIBED VALUES (D-431), NO STATE NOT READ (#17a), D-253 IN EVERY DIALECT AND MECHANISM**
> (the guard now watches the wrappers, the interpreters and the lowercase-drive spellings —
> its remaining stated ceiling is not a licence). Read-only on the analysis throughout: no
> `src/` change, no goldens, no corpus of scores, no `tools/robust_stop/` movement, no
> behaviour change, no fix to inference, no design. Phase 1 under D-231; #8's full
> three-clause gate stands. Every homing lands in the OWNING layer's specification section
> (#7, register rule (e)), with its defense (C2), provenance in register fields and never in
> specification text. Same-commit rule (D-230) throughout.
>
> **★ COMMIT AND PUSH PER TASK BOUNDARY** (the plan's R3, adopted): after each task —
> self-check (D-434), guards, anchors per citation, worktree enumeration, `STATUS.md` pointer
> entry, ONE commit, push. `origin` = `slimvince/MuseScore` ONLY, NEVER `upstream`; a push
> failure is reported in the returns file and worked around by nothing.
>
> **★ Phase 1's completion statement is not written, drafted, or partially written by this
> batch — even if every item closes, the user commissions it.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork at this STOP (2026-08-09), each at the object named:** the
away batch's three commits are `82ebfd68d9`, `d286aaf32e`, `dfbf3ab824`, pushed (each
verified an ancestor of `origin/master`); the guard summary's failing set is the three
standing tools (`tools/audit/guard_state.json` → `summary`); the T2 held entries are exactly
the completion inventory's `class_2_recorded_only_on_a_tracking_surface` population; Task 3's
population is the inventory's `entries_with_no_rationale_at_all` list; the register's own
check passed at the batch's last boundary; the landed Cowork reading surfaces are
`cowork_scratch_2026_08_08/` (re-bannered 2026-08-09, ⟲ cells re-derived); the returns
file's §1 holds 1.1–1.3 are answered by Rulings 1, 2 and 3.

**ASSUMPTION — checked before the act it licenses, STOP into the returns file if refuted:**

- **A1.** Every committed state of `phase1q_reclassification.json` is reachable as a git
  object at the commits the divergence artifact names — checked before the removal.
- **A2.** D-289's named successors carry ALL of its live content, checked at their homes —
  a residue no successor carries is a STOP (Ruling 6's own condition).
- **A3.** D-291's measurement half and gate block (A)'s same-date convention are the same
  binding statement, compared verbatim — any binding difference is a STOP (Ruling 7's own
  condition).
- **A4.** The rule-triage tool's own contract admits session authorship of triage entries —
  read before any entry is authored; a criterion demanding an author the session cannot be
  is a STOP (Ruling 8's own condition).
- **A5.** The OI-349 probe changes nothing — its task's diff is empty but the probe's named
  artifact (Ruling 9; the away dispatch's A4, carried).
- **A6.** D-286's target section — the analysis-extent / effort-control home — STATES RULES
  (the register's rule (h) kind half), read before any home text is written; a
  findings-recording section is a STOP (Ruling 5's own condition).
- **A7.** Task 3's eight pre-searched entries are as the returns file's close records them —
  re-read at the entries' own records before reuse, not carried from the summary.

## The tasks, in order

**Task 0 — the filing, status and licence rulings (Rulings 1, 2, 3, 4, 10).** Remove the
phase-1q file (A1 first; removal recorded in the D-644 shape where the file was named); flip
OI-319 with provenance and create the fragment's row in the same commit (index row AND detail
file, rule (c)); the `CLAUDE.md` D-253 narrowing correction (Ruling 3's exact scope — the
#19 sandbox point survives verbatim); the delegation-bar generator's pointer re-aim and
retired-block maintenance (Ruling 4 — judgments moved WHOLE with reasons, #12); record
Ruling 10 on OI-179's row — including that the PeARL contact was PERFORMED by the user
2026-08-09 as a public GitHub issue on the BCMH repository (see the ruling record's own
Ruling 10 text for what it asked and how silence is later recorded); the measurement opens
WITH phase 2, desk simulation first. Regenerate what these acts stale; the delegation-bar guard
is expected to clear — say so by derivation, not assertion.

**Task 1 — the OI-349 conformance probe (Ruling 9).** Read-only; A5 at the diff. Establish
what the record arm supplies to the key-area grouping pass and compare it against the stated
stabilization precondition, at the code, with citations. The finding — whichever way — goes
to the returns file §2; NOT-SATISFIED is a #13 surface and nothing is proposed for it beyond
the row; SATISFIED-BY-OTHER-MEANS names the means and the specification wording that then
needs correcting, without correcting it here (that correction is its own later act on OI-349's
row).

**Task 2 — the three held entries (Rulings 5, 6, 7).** D-286 homed by the D-472 pattern at
the extent/effort-control owner (A6 first; both facts visible; conformance and extent
questions pointed at OI-206/OI-210, no verdict taken). D-289 ruled superseded (A2 first;
status recorded in the decisions register with successors named; nothing written into any
specification for it; the excluded doctrine-homing alternative recorded). D-291 split (A3
first; build half homed at its owning layer with its defense; measurement half recorded
recorded-twice with gate block (A) standing as the one home). Decisions-register status changes land
in their recording commit (rule (c)); every former field preserved (#12).

**Task 3 — the rule-triage entries (Ruling 8).** A4 first. Author the owed entries per the
tool's own criteria, each verdict with its reason; deliver the set as a ratification-queue
reading file under `ratification_surfaces/`; the guard is expected to clear. The user
reviews at the queue — nothing here is self-ratifying.

**Task 4 — the defense gaps (the away program's Task 3).** Population: the completion
inventory's `entries_with_no_rationale_at_all` list, derived at task start. Per entry:
SEARCH the record; record at the entry's home the defense the record holds WITH its
citation, or "derivation not recorded" where it holds none. Memory is FORBIDDEN as a source
— a defense written after the fact without a source is invention. The eight already-searched
entries are reused per A7, not re-searched. Homes outside this dispatch's edit authority:
HELD with the entry and its found defense stated, so the later act is one edit.

**Task 5 — the reach-verdict derivation (the away program's Task 4).** ONE generated
derivation applying D-639's test over the apparatus-classed documentation rows, generated
the way its first application was (`tools/audit/decisions/true_half_reach.json` is the
pattern), verdicts derived with per-row reasons, the fallback (option 1A) applied and said
where the test does not decide mechanically. Rows move where the derivation puts them;
no hand verdicts.

**Task 6 — the section-unreached four and the findings-not-rules thirteen (the away
program's Task 5).** Per-entry re-homes under rule (e)'s preference — the rule written into
a section that STATES it, in the owning specification; soft cases HELD with candidates
named. The finish-line item populations are derived at task start, never carried from this
file.

**Task 7 — the session-executable gating rows (the away program's Task 6).** Enumerate the
TRUE-half gating set at HEAD; perform what is session-performable inside this batch's
authority; flip with provenance; hold the remainder with reasons, each hold naming the act it
needs and whose it is.

**Task 8 — OI-346's mark application (leftover capacity), then final close.** The batch
summary appended to the returns file — tasks completed, holds, surfacings, the finish
line's end state derived fresh — and the last commit and push.

**Edit authority, complete list:** `ARCHITECTURE.md`; `CLAUDE.md` (Ruling 3's one
correction + homing acts and triage entries per D-645 and Ruling 8); `docs/scoring_model.md`,
`BUILD_AND_TEST.md` (homing acts); the register data and generated files;
`OPEN_ITEMS.md` + detail files; `tools/audit/decisions/gen_phase1p_delegation_bar.py`
(Ruling 4's two acts only); the removal of
`tools/audit/decisions/phase1q_reclassification.json` (Ruling 1); `ratification_surfaces/`
(Task 3's reading file); the returns file and `STATUS.md`. **The first commit also stages
the provenance surfaces this STOP produced:** `cowork_rulings_2026_08_09_return.md`, this
dispatch, `cowork_instruction_return_session.md`, and the landed
`cowork_scratch_2026_08_08/` reading surfaces. **Anything else: hold.**

## Accepted outcomes

Holds are results; empty yields are results; a STOP on A2/A3/A4/A6 is the mechanism working
and returns that ruling to the user with the evidence. The probe finding NOT-SATISFIED is an
accepted outcome and pauses nothing after it (no later task depends on it).

## Self-check (D-434) — run by Cowork before release

Written at a verified STOP with nothing running; every ruling applied is on disk in a
ratified record and ordered read whole (D-643); populations derived at task start, none
carried from this file; the ruled acts are the only freeze relaxations and each names its
ruling; the push hard stop carried; the completion statement barred; hold-don't-guess at
every judgment point; no value restated (D-431); reserved-word discipline checked over this
file's own text.
