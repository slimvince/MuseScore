# CC dispatch — the first homing under the licence, the adoption recorded, OI-344 repaired

> **Status: ACTIVE DISPATCH, written 2026-08-07 (Cowork).** Read IN FULL.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_licensed_homing_and_oi344.md`.
>
> **★ THE FREEZE IS NOT RELAXED except where a ruling below relaxes it explicitly.** Task 3
> changes a mechanism (`gen_reads5_repack.py`) — licensed by ruling R3 below (D-436). Everything
> else found this wave is rowed and left, except a finding whose subject bears on the analysis,
> its inputs, or a measurement tool a measurement depends on — SURFACED, not acted on (D-641).
> An establishment obligation (#19) always gates and is always surfaced.
>
> **★ NO FIGURES (D-431) AND NO STATE NOT READ (#17a).** No `src/` change, no goldens, no
> corpus, no `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to
> inference, no design. Phase 1 under D-231. **Never bash to read working-tree files (D-253).**
>
> **★ Same-commit rule (D-230)** for every register movement. **Commit only when the user
> asks** — leave the tree uncommitted at close and record the worktree enumeration.
>
> **★ Item 2 of the finish line is not started. Phase 1's completion statement is not written,
> drafted, or partially written.**

## 0a. THE RULING LEDGER — three rulings, RULED by the user 2026-08-07 (second batch)

Each was ruled on Cowork's decision surface of 2026-08-07 ("agree with recommendations", after
the full surface with alternatives was delivered as user-visible text). Restated in full
(D-643).

- **R1 — THE REGENERATED DISPOSITION LAYER SUPERSEDES THE COMMITTED ONE.** The A3 STOP of
  `cc_instruction_five_rulings.md` fired and its report was accepted: every moved cluster's
  cause is derived and unanimous (a decision entered after the snapshot's derivation now
  matches it), no cluster lost a naming in either direction, the escape itself accounts for a
  small named subset, and the prediction's shape error is located in the disposition pass's own
  rule order (the backbone rules run before the sweep rules and split on where the matching
  entry is homed). The ruling adopts the regenerated layer as the layer of record; the snapshot
  at `tools/audit/decisions/snapshot_2026-08-07_pre_oi333_repair/` stays, as the O-12 pattern
  requires (#16). With adoption, OI-333's bound lifts: the unresolved residual may again be
  published as current, sourced to the regenerated layer's own artifacts (D-431). **What the
  ruling does not do:** it does not declare the layer's dispositions individually reviewed, and
  it authorizes no fix, design or inference change.
- **R2 — THE HOMING WAVE IS COMMISSIONED: the licensing class of finish-line item 1's re-home
  set is homed under the D-645 licence.** The class is DERIVED at HEAD from
  `tools/audit/decisions/item1_rehome_blocker.json` (the entries whose recorded blocker is
  edit-surface licensing and nothing else), never hand-listed here. Each entry is written into
  its owning specification section — in `docs/scoring_model.md`, `CLAUDE.md` or
  `BUILD_AND_TEST.md` per the entry's own recorded owner — in that section's own voice, WITH
  ITS DEFENSE where the record holds one and "derivation not recorded" where it does not
  (writing a defense from memory is forbidden); the entry's former home, class and verbatim are
  preserved (#12) and provenance lives in the register's own field, never in the specification
  text (the OI-330/OI-328 lesson: a filename written into a governing document reads as a new
  naming and moves measured populations). **The owner-not-determinate class and the
  no-blocker-stated class are NOT touched** — they return to the user per entry on Cowork's
  surfaces.
- **R3 — OI-344 IS REPAIRED BY FREEZE-AND-READ-BACK.** The dated proxy figure inside
  `reads5_repack.json` (`recomputed_2026_08_04` and its companion `moved_by`) is FROZEN — stored
  once, read back by `--check` rather than recomputed on every run — the shape
  `gen_phase1q_snapshot_establishment.py` already uses for its own moment-in-time checks (#6).
  Every other field keeps its current semantics. The excluded alternative is recorded with the
  ruling: regenerating the artifact would write a 2026-08-07 figure under a key dating it
  2026-08-04 — the OI-330/OI-301 refusal — and leaving the guard red normalizes a red guard,
  which is the #19 cost the repair removes. D-436 licenses the mechanism change by this ruling.

**None of R1–R3 authorizes a fix to the analysis, a design, or an inference change.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — established at the objects by Cowork this session (2026-08-07):**

- **F1.** The blocker partition, its licensing class and each entry's per-file owner list:
  `tools/audit/decisions/item1_rehome_blocker.json`, read in full.
- **F2.** The A3 STOP's full report: `tools/audit/decisions/oi333_repair.json` — the unanimous
  cause attribution, the no-naming-lost check, the escape-attributable subset, the rule-order
  explanation, and the artifact's own statement that supersession is the user's (now given as
  R1).
- **F3.** OI-344's cause block at `open_items/OI-344.md`: the failure is confined to one proxy
  entry's recomputed correlation and companion field; the re-pack, the registered bands and
  every wave's yield are untouched; and the row itself names freeze-and-read-back as the shape
  that works, with its precedent.
- **F4.** The licence's recorded scope: `cowork_audit_protocol.md`, the D-645 section — homing
  acts only, no other edit of the three files.

**ASSUMPTION — each checked BEFORE the act it licenses, with a STOP if refuted:**

- **A1.** That every licensing-class entry's recorded owner names a DETERMINATE section of its
  file. → Task 2.1, per entry, before writing anything: an entry whose owner is soft does not
  belong to this class — STOP for that entry, report it, and home the rest; it returns to the
  user with the owner question, never a guessed section.
- **A2.** That homing adds prose ONLY — no pre-existing line of `CLAUDE.md`,
  `docs/scoring_model.md` or `BUILD_AND_TEST.md` is modified or deleted. → checked at the diff
  of each file after writing (the standing self-check reads the diff, not the memory of it); any
  modification of existing text is reverted before close — the licence does not cover it.
- **A3.** That the freeze-and-read-back change leaves every OTHER field of `reads5_repack.json`
  re-deriving exactly as before. → Task 3.2: field-by-field comparison of the artifact before
  and after; movement anywhere but the frozen entry is a STOP.
- **A4.** That `docs/scoring_model.md`'s staleness check (the §2 template count against
  `chordanalyzer.cpp`) is untouched by homing adds. → run the check after the edit; a trip is a
  STOP (it would mean the homing touched what it must not).

## 1. Task 1 — Record ruling R1 (the adoption)

**1.1** Dated note on `open_items/OI-333.md` (the row stays resolved): the regenerated layer is
ADOPTED as the layer of record by the user's 2026-08-07 ruling, the STOP's report accepted; the
snapshot location named; the bound on publishing a current residual LIFTED, with the residual
readable only off the regenerated layer's artifacts (D-431). **1.2** Where the register's own
scope block qualifies the residual as the committed layer's figure at a smaller backbone,
update that qualification to state the layer of record and its backbone — the generator owns
that text, so the change goes through the generator's data, never a hand-edit of a rendered
file.

## 2. Task 2 — Apply ruling R2 (the homing wave)

**2.1** Derive the licensing class at HEAD (F1's artifact regenerated if the register moved);
discharge A1 per entry. **2.2** Home each entry: the decision written into its owning section
in that section's own voice, defense from the record or "derivation not recorded", former
home/class/verbatim preserved (#12), register data + regenerated files in the same commit
(D-230). **2.3** Discharge A2 (the per-file diff) and A4 (the scoring-model staleness check).
**2.4** Regenerate what follows the register — the blocker artifact, the routes artifact, the
finish line (its own STOP must account for every entry that leaves item 1). **2.5** Re-aim any
drifted anchors per citation. **2.6** Run `tools/audit/claude_md_rule_triage.py`; report its
movement and fix nothing in it — its pre-existing failure is on record and is not this wave's.

## 3. Task 3 — Apply ruling R3 (OI-344)

**3.1** Change `gen_reads5_repack.py`: the dated proxy figure and its companion frozen in the
artifact and read back by `--check`; the docstring states the freeze and why (the general
lesson from OI-344's own row: a live guard whose artifact contains a dated recomputation cannot
stay green across a legitimate refresh of what it recomputes against). Former structure
preserved in the row's dated note (#12). **3.2** Discharge A3 (the field diff). **3.3** The
guard runs green at the result; the guard classification's entry for the tool is updated if its
recorded reason no longer describes the tool. **3.4** Flip OI-344's INDEX row with provenance;
dated note on the detail file.

## 4. Task 4 — Close

Guards at the tree with the list `gen_guard_state.py` derives; **fix nothing beyond Tasks 1–3**.
Record the worktree enumeration via `tools/audit/changed_paths.py` to a named artifact. Run
`tools/audit/process_check.py` over this dispatch. `STATUS.md` gains one POINTER entry. Leave
the tree uncommitted.

## 5. Accepted outcomes

**A1 finding one or more entries with a soft owner is a result, not a failure** — those entries
return to the user; the class was cut by phrase and the cut can be wrong per entry. **A3 or A4
tripping is a revert-and-report.** **The finish line's STOP firing on an unaccounted entry is a
STOP, reported with the entry.** **`claude_md_rule_triage.py` moving on the homing adds is
reported movement, not a defect to fix here.**

## 6. Self-check (D-434) — run by Cowork before release

- **Ruling ledger:** three rulings in full, each with its excluded alternative or explicit
  non-authorization; the adoption states what it does NOT declare.
- **D-643:** R1 restates the STOP's findings including the half that went against the
  prediction; R3 records the rejected repair beside the taken one.
- **#17(a):** four facts read at the objects this session; four assumptions each with an
  ordered check and a STOP.
- **Principles:** #12 at every preservation; #16 at the retained snapshot; #6 at the
  freeze-precedent reuse; #19 at the red-guard cost; D-431 — no figure in this dispatch;
  D-436 cited for the one mechanism change; the OI-330/OI-328 lesson carried into R2's
  provenance rule.
- **Scope:** no homing outside the licensing class; the licence's own limits quoted at F4; the
  user-serial classes explicitly untouched.
