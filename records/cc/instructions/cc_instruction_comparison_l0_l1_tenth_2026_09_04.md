# CC dispatch — the L0/L1 COMPARISON, TENTH WRITING, THE AUDIT QUESTIONS FOR THE FOUR PER-NOTE FIELD-LIST ROWS: land the hundredth entry and Ruling 80, replace five sentences inside §11's added subsection so that each of Rows 3.41, 3.42, 3.48 and 14.9 carries a question, close — NO ruling taken, NO numbered question edited, NO renumbering, NO row block touched, NO outgoing text opened, NO session booted

> **Dispatch (Cowork writing side, 2026-09-04, the session that booted on
> `cowork_handoff_entry_one_hundred.md`, its first dispatch).** Executes **Ruling 80 (§3ci)**, which
> settles the residue the ninth batch reported and left open: whether the four rows quarantined without
> an audit question are owed one, and who writes it. **Written at a verified stop: the ninth writing's
> batch is FINISHED and was verified at the objects by this side** — the chain
> `28c465e9c3… → ff3b47b519… → 778e70c6dd… → 122d5f1091… → a70fcf6d51…`, each commit read at
> `git show --stat` by explicit hash; the close section present in `cowork_away_returns.md` at
> `122d5f1091…` and one further commit `a70fcf6d51…` carrying the end-state guard artifact, which is
> the finished test the hundredth entry states. **The tip was read at both ref files with the file
> tools. Nothing is running.**
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** It APPLIES one ruling the user took on 2026-09-04,
> **Ruling 80 (§3ci)** of `cowork_rulings_2026_08_31_decision_surface_sitting.md`. **THIS BATCH DECIDES
> NOTHING**: it takes no ruling, weighs no position, places no row, edits no row block, edits no
> proposed disposition, edits no ruled disposition, edits no outgoing text, derives no statement about
> the analysis, and touches no register. **It does not compose an audit question either** — the one new
> question is Ruling 80's own, quoted from the ruling, and the three placements are the ruling's own
> placements, likewise quoted. Its whole authored write set is **five replaced sentences inside one
> subsection of §11**, plus the close files. Where an anchor this face quotes is not found verbatim at
> the file, that edit is a STOP-and-report and no part of it is improvised.
>
> **★ THE ORDERED FIRST READ.** Your VERY FIRST read this session is the sitting record's **§3ci
> (Ruling 80) WHOLE** — the authority for this batch — then **§3ch (Ruling 79) WHOLE** and **§3bq
> (Ruling 62) item 4**, the ruling that quarantined the four rows and wrote no question. Then:
> **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md` — CONDITIONAL, and this
> batch MEETS the condition (it runs the guard set): read it in full; rule (a)'s `gating_ids` at
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` (DO NOT read the whole
> open-items index). **(2)** `cowork_handoff.md`, ITS TOPMOST ENTRY, and
> `cowork_handoff_entry_one_hundred.md` whole — the entry point this batch lands; a
> `cowork_handoff_entry_one_hundred_and_one.md` at the root, if present, is the writing side's and is
> NOT landed. **(3)** `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)** the ninth
> dispatch `cc_instruction_comparison_l0_l1_ninth_2026_09_04.md` whole, its report whole, and the ninth
> close in `cowork_away_returns.md` (*THE ADDITIVE REPAIR OF 11 AND 12 (NINTH WRITING) …*). **(5)** the
> reading file `ratification_surfaces/cowork_comparison_l0_l1_reading.md` — its **§0, §5, §11 WHOLE
> INCLUDING ITS ADDED SUBSECTION, and §6's blocks for Rows 1.3, 3.41, 3.42, 3.48 and 14.9**; §6's five
> row blocks are read to check this face's quotations against the rows and **are not edited**.
> **(6) No outgoing text, no part of the derived specification, and no part of the derivation is opened
> by this batch. §10, §12, §13, §13a and §13b are not opened for editing at all.**
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The reading file* —
> `ratification_surfaces/cowork_comparison_l0_l1_reading.md`. *The added subsection* — the one
> subsection at the foot of §11 headed `### ★ ADDED BY THE ADDITIVE REPAIR — the rows the rulings
> QUARANTINED that this list did not carry`, written by the ninth batch under Ruling 79; it is the only
> part of the reading file this batch edits. *An entry* — one bullet of that subsection, from its `- Row
> N.M` line to the line before the next `- Row` line or the blank line before the next heading. *The
> numbered list* — §11's questions 1 to 82, which this batch does not touch at all. *A row's block* —
> the `**Row N.M —` heading and every line under it up to the blank line before the next `**Row`
> heading; **no row block is edited by this batch.** *A changed passage* — one contiguous run of changed
> lines in a diff (what a diff tool calls a hunk). *The current commit* — the commit named by hash,
> never a bare branch tip.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, **no measurement of the ANALYSIS**, no
> design, no repair of the analysis, **no derivation of any statement**, NO SESSION BOOTED, no document
> archived, moved or deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit
> to any governing document (the two `STATUS.md`/`STATUS_ARCHIVE.md` acts of Task 2 excepted), to the
> derivation (body OR banner), to the derived specification, to the brief, to the pack or its
> generator, to any register entry or register source, to the suspension file, or to any outgoing text;
> in the reading file, only the five sentence replacements Task 1 orders — no `**Row` heading,
> `*Statement.`, `*Derived.`, `*Current-text axis.` line, `*PROPOSED DISPOSITION` paragraph or
> `*RULED DISPOSITION` line anywhere in §6 is edited or deleted; §10 and §12 are not edited at all; no
> numbered item of §11 is edited, deleted, renumbered or reordered; §13's tables, §13a's figures and
> §13b's three tables are not touched** — **no ruling taken, no position weighed, no recommendation
> anywhere, and NO AUDIT QUESTION AUTHORED BY YOU.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (`--check`) and again at the
> end (write mode, committed as the artifact of a real run). Verify every commit at the object by
> explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and any `git diff` over
> the whole working tree time out on this mount and are denied by the armed guard — use the per-path
> explicit-hash forms named in A1; a `git diff` with NO explicit hash is a D-253 breach. `git diff
> --no-index` against a working-tree path is denied by the guard: take every check between two explicit
> blob hashes, writing the working copy into the object store with `git hash-object -w` first, as the
> fifth to ninth batches did. **`gen_guard_state.py` DOES NOT RECOGNISE `--help` and silently selects
> its default WRITE mode** — established by the eighth batch, confirmed by the ninth; do not probe it
> with a flag, name the mode you intend, and hash the artifact afterwards rather than assuming nothing
> moved. **Do not order or accept a hunk count as a check** — the ninth batch's own close records that
> two edits seven lines apart merge into one changed passage, so the property that carries the weight
> here is ZERO DELETED LINES OUTSIDE THE FIVE REPLACED SENTENCES, never a count of passages. If staging
> is refused by a stale zero-length `index.lock` with no git process running, establish the cause
> (D-669), remove it, report the act. **Commit subjects are given below without an apostrophe
> anywhere**, so a plain single-quoted `git commit -m '…'` carries each verbatim. The reserved-word and
> vocabulary rules bind every line — *score* only the musical score, *key* only tonality, *bar* only the
> metric unit (the prohibiting sense is *exclude*), *note* only a pitch event, *field* only a record's
> field, no numeric grade anywhere.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 80, §3ci — its decision, verbatim:** *"Rows 3.41, 3.42 and 3.48 are recorded as carried by
  §11's standing question 2, in the form §11's own convention uses — the row says so. Row 14.9 gains
  ONE FURTHER AUDIT QUESTION of its own, asking whether the shipped code carries the tie facts the
  target list has named all along, and if it does not, whether anything downstream depends on their
  absence. The writing is ADDITIVE and touches only the four entries in the subsection at the foot of
  §11 that the ninth batch wrote, each of which currently reads that no audit question is written at
  the row: those four sentences are replaced by the placement this ruling makes, and no numbered
  question of the standing list 1 to 82 is edited, renumbered or reordered, and no row block anywhere
  in the file is touched."*
- **Ruling 80's ordered consequence, verbatim:** *"The subsection's own preamble, written by the ninth
  batch, ends 'Where no audit question is written at the row, the entry says so and composes none.'
  That sentence becomes false at the moment Row 14.9's entry carries a question, so it is corrected in
  the SAME act, its former wording preserved at the site (#12). … The corrected sentence records BOTH
  limbs of what this ruling does, because either limb alone leaves it false of some entry: where the
  ruling which quarantined a row wrote no question, the entry carries EITHER the question the PLACING
  ruling writes, naming that ruling, OR the standing numbered question above that the placing ruling
  records as carrying the row. No other sentence of the preamble is touched, and the count of entries
  in the subsection does not move."*
  *(That sentence of the ruling was itself corrected before the ruling landed, on this side's
  pre-release fact-check of this dispatch — as first drafted it named only the first limb, which is
  false of the three entries the ruling points at question 2. The correction is recorded at the site
  with its former wording preserved (#12), and **nothing the user ratified moves**: both limbs stand in
  the decided text quoted above, which is unchanged.)*
- **Ruling 80's stated exclusions, verbatim:** *"It takes no position on any row's disposition — all
  four stay QUARANTINED, as Ruling 62 ruled. It does not reopen what the note stage is required to
  supply, which S-3 settles. It edits no proposed disposition, no ruled line, no row block, and no
  figure of §13, §13a or §13b. It does not touch §10 or §12."*
- **Why the numbering is left alone, Ruling 80's own words:** *"Ruling 79 recorded that whether
  anything elsewhere in the record cites a question by its number is UNMEASURED, so a shape that leaves
  every number where it is cannot break a citation silently (D-307)."*
- **The reading file's §5, the closed vocabulary:** **QUARANTINED** — *"it states how the
  implementation currently works, or a measured property of it. The row writes the audit question in
  one sentence."*
- **§11's own many-rows-per-question convention, at its opening:** *"82 numbered questions — fewer
  numbers than rows, because several questions carry more than one row where the same question is met a
  second, third or fourth time and the row says so."*
- **§11's numbered question 2, Row 1.3's, quoted here because three entries point at it:** *"do the
  five per-note facts S-3 requires and the eleven fields do not carry reach any consumer, and does
  `staffEligible` belong to L0's supplied facts or to an eligibility decision above it?"*
- **Ruling 62, §3bq, item 4 — the ruling that quarantined the four and wrote no question, verbatim:**
  *"The per-note list is S-3's as written, plus Ruling 44's stem direction and Ruling 50's attached
  marks; the outgoing eleven- and twelve-field lists are descriptions of builds and targets, placed by
  S-3 — carried where they match, QUARANTINED where they describe code, as the reading file already has
  1.3 and 4.3."*
- **D-672 (the stop rule):** *"A per-entry pass may be stopped at any member boundary, and the stop is
  RECORDED."* The member boundaries of this batch are the tasks; **within Task 1 the five sentence
  replacements are ONE member and stand or fall together**, since a subsection whose preamble says no
  question is composed while one of its entries carries one states something false about itself (#10).

## Premise ledger

- **FACT — the local branch tip is `a70fcf6d510c2e2e8d0fde895cd7817f8872ae47`** (the ninth writing's
  end state; parent `122d5f1091eb7b786ce442ed174f9ac350dfe07c`, the ninth close); **`origin/master` is
  the SAME hash.** Read by this side at the two ref files with the file tools, 2026-09-04, and the
  chain `28c465e9c3… → ff3b47b519… → 778e70c6dd… → 122d5f1091… → a70fcf6d51…` verified at
  `git show --stat` per explicit hash by this side, each commit's parent confirmed. **Nothing is
  unpushed. Re-read the refs yourself before Task 0.**
- **FACT — the ninth batch is FINISHED by the record's own test.** Its close section stands in
  `cowork_away_returns.md` at `122d5f1091…` (that commit adding 348 lines to it) and ONE FURTHER commit
  `a70fcf6d51…` carries the end-state guard artifact. Both read at `git show --stat` by explicit hash.
- **FACT — the guard state at the tip IS the tip's own measurement**, read by this side at the object
  `a70fcf6d51…:tools/audit/guard_state.json` (blob `497e802bac1bcc1715e7f87a80e5854d625eb098`, 111,689
  bytes) → `summary`: **run 76**, passing 66, **failing 10**, not_run 4, historical_records 16. The ten
  failing tools, read at that object: `gen_filing_convention_application.py --check`,
  `gen_artifact_inventory.py --check`, `gen_artifact_inventory_surface.py --check`,
  `gen_test_construction_evidence.py --check`, `gen_retirement_caller_check.py --check`,
  `decisions/apply_soft_discard.py --check`, `decisions/apply_residue_discard.py --check`,
  `gen_epoch_write_path.py --check`, `gen_recognizer_establishment_sort.py --check`,
  `decisions/gen_cluster_dispositions.py --verify` — the same ten the fifth to ninth batches inherited
  and measured at both their ends. Nothing has landed since.
- **FACT — the blobs at the tip**, read at `git rev-parse a70fcf6d51…:<path>` with their sizes at
  `git cat-file -s` by this side: the reading file `a025c686cb652ad871bf12b7cec140023ce92609`
  (1,101,199 bytes); the sitting record `1cc0f48de30c6e5949ef5624e43384cdcae81fd5` (522,435);
  `STATUS.md` `253856ce232c4bd49c653f53439abf436aba7de4` (11,234); `tools/audit/guard_state.json`
  `497e802bac1bcc1715e7f87a80e5854d625eb098` (111,689); `tools/audit/gen_status_batch_bound.py`
  `3d50e7eb1ee5d801ad9b02aa26909d26aafec65f` (39,156); `tools/audit/evidence_pin_membership.json`
  `fb3a00be268401e5386954cc3c410c9c827539df` (17,641).
- **FACT — the sitting record IS MODIFIED ON DISK by this writing side, additions only.** This session
  took Ruling 80 and wrote its section into
  `cowork_rulings_2026_08_31_decision_surface_sitting.md`: **one new `## 3ci. Ruling 80 — …` section of
  165 lines**, placed immediately before the line `## 4. What this ruling does NOT do` — which occurs
  **exactly once** in the file, established before the insertion. The file on disk is **536,518
  bytes**; at the tip it is **522,435**. **No other byte of that file changed**; the insertion was a
  splice, not a rewrite, and the result was read back through the file tools, with `## 3ch.` proven
  unmoved at its own line and `## 4.` proven displaced by exactly the inserted count. **A1 declares
  this modification and Task 0 lands it; you prove additions-only at the object.**
- **FACT — WHAT IS AT THE FOUR ENTRIES NOW, read at the file by this side.** Each of the four entries
  for Rows 3.41, 3.42, 3.48 and 14.9 in §11's added subsection opens `- Row N.M — **no audit question
  is written at the row.**`, gives the ground quoted from Ruling 62's line, points at question 2 as the
  standing question about the same field list on Ruling 78's authority, and closes `**this entry states
  the gap and composes nothing.**` **The other five entries of that subsection — Rows 2.16, 2.17, 3.5,
  4.3 and 17.13 — each already carry a question and are NOT touched by this batch.**
- **FACT — THE ENTRY COUNT DOES NOT MOVE.** The subsection carries **nine** entries before this batch
  and **nine** after it. No entry is added, removed or reordered; five sentences inside four of them,
  and one sentence of the preamble, are replaced.
- **FACT — the five anchors of Task 1 exist verbatim at the file**, each read by this side at the
  staged copy of blob `a025c686cb…` and quoted below **with the two-space continuation indentation and
  the line breaks they carry at the file**. Each of the four entry anchors is unique in the file by its
  leading `- Row N.M — **no audit question` fragment; the preamble anchor is unique by its closing
  `none.*` on its own line inside that subsection's italic note. **Match each on its whole quoted block,
  never on a fragment.**
- **FACT — the substantive requirement is ALREADY SETTLED and is not in this batch.** S-3 at
  `cowork_derived_specification_l0_l1_2026_09_03.md` §2.2 carries the per-note list as the rulings
  amended it, ties included. **This batch reaches only what the audit is asked, never what the
  specification requires**, and it opens the derived specification not at all.
- **FACT — `tools/audit/gen_status_batch_bound.py` at the tip** has its five authored constants at
  `BASE_COMMIT = "778e70c6dd9f74853fcaeaf77643809ecd07e9e6"` (the ninth's Task 1 commit),
  `PREVIOUS_BATCH_DISPATCH = "cc_instruction_comparison_l0_l1_eighth_2026_09_04.md"`,
  `ACT_DATE = "2026-09-04"`, `DISPATCH = "cc_instruction_comparison_l0_l1_ninth_2026_09_04.md"`,
  `TASK = "Task 2"`, read at the object by this side; `PREVIOUS_AIMINGS` carries the eighth writing's
  aiming as its last row.
- **FACT — `STATUS.md` at the tip carries, top to bottom:** the ninth writing's close entry (naming the
  ninth dispatch, carrying the `Last updated: ` prefix), then that batch's two `Same dispatch` entries
  for Tasks 1 and 0, then the second writing's two 2026-09-02 entries, the first of which names no
  dispatch and does not say `Same dispatch`, then the archive pointers. Read at the file by this side,
  whose working copy is 11,234 bytes and therefore equal in size to the tip blob.
- **FACT — WHAT `gen_evidence_pin_membership.py` READS OF A RULING RECORD**, established at the tool's
  own source by the eighth batch and NOT restated from any dispatch's summary of it: route A reads only
  the record's LEADING BLOCKQUOTE and every paragraph opening `*Provenance:`; **route B reads EVERY
  LINE**, and for each line containing `PINNED` in any case it resolves any tool name on that line, a
  resolved tool becoming a member. **The Ruling 80 section moves none of the three:** the leading
  blockquote is untouched, the section contains no paragraph opening `*Provenance:`, and it contains no
  line carrying the word *pinned* in any case — established by this side at the file after the
  insertion. **A3 therefore predicts ZERO movement.** Measure before accepting.

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications at your boot:
  **EXACTLY ONE** — `cowork_rulings_2026_08_31_decision_surface_sitting.md`, additions only, the one
  new section named in the FACT above. **The bound on that declaration, stated rather than implied
  (#24):** this writing side could not run the whole-population enumeration from its own side,
  `tools/audit/changed_paths.py` exceeding the bridge's command window, so the declaration rests on the
  ninth close's own whole-population enumeration, on this side's own act-list since, and on a per-path
  comparison this side DID take — **and the comparison is narrower than the path list, which is stated
  rather than glossed.** For **three** paths the bridge-staged working copy was compared BY SIZE
  against `git cat-file -s` at the tip and agreed exactly: the reading file (1,101,199), `STATUS.md`
  (11,234), and the sitting record before this side's own insertion (522,435). The other paths this
  batch depends on — `tools/audit/guard_state.json`, `tools/audit/gen_status_batch_bound.py`,
  `tools/audit/evidence_pin_membership.json`, the derived specification, `cowork_away_returns.md` and
  `cowork_handoff.md` — were read **at their committed objects only**, so this side has no measurement
  of their working copies at all. **Your Task 0 enumeration is what ESTABLISHES A1; this declaration is
  what it is measured against.** Untracked and to be landed by Task 0:
  `cowork_handoff_entry_one_hundred.md` and this dispatch — **two untracked paths.** **★ THE ROOT ALSO
  CARRIES A LARGE STANDING POPULATION OF UNTRACKED HISTORICAL `cc_instruction_*` AND `cc_*_report`
  FILES**, which is pre-existing, is not this line's business, and which no dispatch of this line
  lands: **an untracked-file check at the root will return hundreds of names, and that is not a
  defect.** Any further `cowork_handoff_entry_*.md` or `cowork_rulings_*.md` at the root is the writing
  side's, is NOT landed and is NOT a STOP. A modification at any other tracked path, at boot or during
  the run other than by this batch's own ordered acts, is a STOP-and-report. **Check ordered (Task 0,
  first act, AFTER the ordered first reads):** the whole tracked population enumerated with the
  sanctioned tool `tools/audit/changed_paths.py`; per named untracked path
  `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`. **The sitting record's
  additions-only property is proven at the object**: write the working copy into the object store with
  `git hash-object -w` and diff it against blob `1cc0f48de30c6e5949ef5624e43384cdcae81fd5` between the
  two explicit hashes — **zero deleted lines, and every added line inside the new `## 3ci.` section.**
  Any deletion is a STOP-and-report and nothing is committed.

- **ASSUMPTION A2 — the reds at the `--check` run before your first edit.** The ten failing tools named
  above, exactly, and zero STOPs. The reds this batch's own acts may cause, each classed by its cause
  and NOT a STOP: `gen_l0_l1_outgoing_population.py --check` STALE at the end state if the reading
  file's or `STATUS.md`'s recorded-tier hits move under Tasks 1 and 2 — regenerate, measure as the
  fifth to ninth did, commit with the end state, report that no MEMBER of the population moved; a
  member moving IS a STOP-and-report. `gen_session_start_read_size.py --check` STALE if regenerated
  before the LAST edit to `STATUS.md` — regenerate LAST. `gen_evidence_pin_membership.py --check` STALE
  if the sitting record's leading blockquote, a paragraph opening `*Provenance:`, or a line carrying
  the word *pinned* in any case moved — none of the three did, and a STALE verdict there is a
  STOP-and-report. `gen_artifact_inventory.py --check` and `gen_artifact_inventory_surface.py --check`
  are standing reds — report any difference, repair nothing. A red outside that set is classed by its
  measured cause; one outside this batch's own acts' subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration.** `evidence_pin_membership.json` moves by ZERO at
  Task 0. Measure against blob `fb3a00be268401e5386954cc3c410c9c827539df` before accepting; movement is
  a STOP-and-report.

- **ASSUMPTION A4 — the guard registry.** No tool added, none edited except Task 2's re-aiming of
  `gen_status_batch_bound.py`'s authored constants and its `PREVIOUS_AIMINGS` list (D-648); population
  unchanged at **76**; failing set the ten named, plus none; zero STOPs throughout.

- **ASSUMPTION A5 — byte-unchanged by this batch, verified at the objects after the last commit (P-3,
  footprint as an act-list):** the derivation `cowork_blind_derivation_l0_l1_2026_08_31.md` whole,
  banner and body; the derived specification `cowork_derived_specification_l0_l1_2026_09_03.md` whole;
  the sitting record beyond the one section Task 0 lands; the brief; the pack directory and
  `derivation_boot_pack.json`; every outgoing text; `tools/audit/gen_l0_l1_outgoing_population.py`; the
  population artifact except A2's case; every governing document except `STATUS.md` and
  `STATUS_ARCHIVE.md`; the suspension file; every register source. **In the reading file, the diff
  against blob `a025c686cb652ad871bf12b7cec140023ce92609` is exactly the five sentence replacements
  Task 1 orders and nothing else, all five inside the one added subsection of §11.** Any other
  difference is a STOP-and-report.

## The declared start state — at the tree you will meet

1. The ten failing tools named in the FACT above — standing reds, none of them this batch's.
2. Zero STOPs; `gen_evidence_pin_membership.py --check`, `gen_l0_l1_outgoing_population.py --check`,
   `gen_status_batch_bound.py --check` and `gen_session_start_read_size.py --check` PASS predicted,
   **measured** (A2).
3. The artifact at the tip being the tip's own run, your start-state run is expected to reproduce it
   exactly; a difference is reported with its cause. **Name the mode you run it in** — the tool does
   not recognise `--help` and silently writes.

A failing verdict outside 1–3 is a STOP-and-report, save the A2 exceptions named.

## Findings in this batch

**No finding number is allocated by this dispatch.** A finding met during the run is written up on its
own and surfaced, never absorbed: **Ruling 9 (§3i), part (iii)** states that surfacing obligation as
*"UNCONDITIONAL and IMMEDIATE"* and makes only the HALT conditional. **An anchor this face quotes that
is not found verbatim at the file is a STOP-and-report for THAT edit**, and because the five
replacements stand or fall together, **if any one of the five cannot be made, Task 1 is not committed
at all and the batch reports and stops.**

## Task 0 — land the hundredth entry, Ruling 80 and this dispatch, and PUSH

1. **A1's check** (after the ordered first reads), including the sitting record's additions-only proof
   at the object.
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob
   `fb3a00be268401e5386954cc3c410c9c827539df` BEFORE accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_handoff_entry_one_hundred.md` (new),
   `cc_instruction_comparison_l0_l1_tenth_2026_09_04.md` (new),
   `cowork_rulings_2026_08_31_decision_surface_sitting.md` (modified, additions only), and
   `tools/audit/evidence_pin_membership.json` (regenerated, only if it moved) — **three or four
   paths.** Subject, exactly:
   `record: Ruling 80 and the hundredth entry landed with the tenth comparison dispatch, the audit questions for the four per-note field list rows`
4. **PUSH**; verify at the ref file that `origin/master` equals the new tip.

**Under standing clause P-2 this dispatch is PINNED at Task 0:** record its blob, and prove the blob
unmoved immediately before staging it.

**E0:** the named paths in one commit; `origin/master` at the commit; A3 measured; A1's whole-tree
check reported with its enumeration; the sitting record's additions-only property proven at the object.

## Task 1 — the five sentence replacements inside §11's added subsection, in ONE commit

**All five edits are inside the subsection headed `### ★ ADDED BY THE ADDITIVE REPAIR — the rows the
rulings QUARANTINED that this list did not carry`, at the foot of §11. Nothing outside that subsection
is edited.**

**(a) The preamble's last clause, corrected with its former wording preserved (#12).** Anchor,
verbatim, the last two lines of that subsection's italic note:

```
the row writes it in. Where no audit question is written at the row, the entry says so and composes
none.*
```

Replace those two lines with:

```
the row writes it in. **★ THE LAST CLAUSE OF THIS NOTE IS AMENDED BY RULING 80 (§3ci), AND ITS FORMER
WORDING IS PRESERVED HERE (#12): "Where no audit question is written at the row, the entry says so and
composes none."** Where the ruling that quarantined a row wrote no question, the entry carries either
the question the PLACING ruling writes, naming that ruling, or the standing numbered question above
that the placing ruling records as carrying the row.*
```

**(b) Row 3.41's entry.** Anchor, verbatim, five lines:

```
- Row 3.41 — **no audit question is written at the row.** It was quarantined by Ruling 62 (§3bq, item
  4) on the ground *"the outgoing eleven- and twelve-field lists are descriptions of builds and
  targets, placed by S-3 — carried where they match, QUARANTINED where they describe code."* Ruling 78
  (§3cg) records that question 2 above, Row 1.3's, is the standing question about the same field list;
  **this entry states the gap and composes nothing.**
```

Replace with:

```
- Row 3.41 — quarantined by Ruling 62 (§3bq, item 4) on the ground *"the outgoing eleven- and
  twelve-field lists are descriptions of builds and targets, placed by S-3 — carried where they match,
  QUARANTINED where they describe code."* That ruling wrote no question. **Its audit question is
  question 2 above, Row 1.3's, met a second time** (Ruling 80, §3ci): this row names the same
  eleven-field as-built record that question 2 asks about, and its own axis line reads *"the same
  difference as Row 1.3, stated there."*
```

**(c) Row 3.42's entry.** Anchor, verbatim, four lines:

```
- Row 3.42 — **no audit question is written at the row.** It was quarantined by Ruling 62 (§3bq, item
  4) on the ground *"The per-note list is S-3's as written, plus Ruling 44's stem direction and Ruling
  50's attached marks"*. Ruling 78 (§3cg) records that question 2 above, Row 1.3's, is the standing
  question about the same field list; **this entry states the gap and composes nothing.**
```

Replace with:

```
- Row 3.42 — quarantined by Ruling 62 (§3bq, item 4) on the ground *"The per-note list is S-3's as
  written, plus Ruling 44's stem direction and Ruling 50's attached marks"*. That ruling wrote no
  question. **Its audit question is question 2 above, Row 1.3's, met a third time** (Ruling 80, §3ci):
  this row is the enumeration of the very eleven fields question 2 asks about, and the facts it records
  as absent are question 2's own five.
```

**(d) Row 3.48's entry.** Anchor, verbatim, four lines:

```
- Row 3.48 — **no audit question is written at the row.** It was quarantined by Ruling 62 (§3bq, item
  4) on the ground *"carried where they match, QUARANTINED where they describe code."* Ruling 78
  (§3cg) records that question 2 above, Row 1.3's, is the standing question about the same field list;
  **this entry states the gap and composes nothing.**
```

Replace with:

```
- Row 3.48 — quarantined by Ruling 62 (§3bq, item 4) on the ground *"carried where they match,
  QUARANTINED where they describe code."* That ruling wrote no question. **Its audit question is
  question 2 above, Row 1.3's, met a fourth time** (Ruling 80, §3ci): this row is the time clause of
  that same eleven-field list, and the bar-relative metric position it lacks is one of question 2's own
  five facts.
```

**(e) Row 14.9's entry — the one that gains a question of its own.** Anchor, verbatim, six lines:

```
- Row 14.9 — **no audit question is written at the row.** It was quarantined by Ruling 62 (§3bq, item
  4) on the ground *"The per-note list is S-3's as written, plus Ruling 44's stem direction and Ruling
  50's attached marks; the outgoing eleven- and twelve-field lists are descriptions of builds and
  targets, placed by S-3 — carried where they match, QUARANTINED where they describe code."* Ruling 78
  (§3cg) records that question 2 above, Row 1.3's, is the standing question about the same field list;
  **this entry states the gap and composes nothing.**
```

Replace with:

```
- Row 14.9 — quarantined by Ruling 62 (§3bq, item 4) on the ground *"The per-note list is S-3's as
  written, plus Ruling 44's stem direction and Ruling 50's attached marks; the outgoing eleven- and
  twelve-field lists are descriptions of builds and targets, placed by S-3 — carried where they match,
  QUARANTINED where they describe code."* That ruling wrote no question, and question 2 above does not
  reach this row, whose subject is the TARGET list rather than the as-built one. **Its audit question,
  written by Ruling 80 (§3ci): does the arm that ships carry the tie facts the target list of
  `cowork_target_architecture.md` §2 has named all along, and if it does not, does any consumer depend
  on their absence?** *(Distinct from question 2, which asks whether the five facts S-3 requires reach
  any consumer in the code; this asks after a fact the architecture itself already required and the
  build does not carry.)*
```

**(f) What Task 1 does NOT touch, stated so it is not inferred.** **§10, §12, §13, §13a and §13b are
not edited at all.** No row block in §6 is edited — the five row blocks you read are read to check this
face's quotations, and reading is not editing. **No numbered item of §11's list 1 to 82 is edited,
renumbered, reordered or deleted**, and question 2 in particular is left exactly as it stands: it gains
no cross-reference and no appended clause. **The five entries of the subsection for Rows 2.16, 2.17,
3.5, 4.3 and 17.13 are untouched.** No proposed disposition anywhere is deleted. **No entry is added or
removed: the subsection carries nine entries before and nine after.**

**(g) The object-level check, before the commit.** Write the working copy into the object store
(`git hash-object -w`) and diff it against the tip blob `a025c686cb652ad871bf12b7cec140023ce92609`
between the two explicit hashes, to a scratch path outside the repository, read with the file tools.
**The properties to prove, none of them a count of changed passages:**

1. **Every deleted line is one of the five anchor blocks quoted above, and no other line is deleted
   anywhere in the file.** Enumerate the deleted lines and match them, in full, against the five
   anchors on this face. A deleted line outside them is a STOP-and-report.
2. **No `###` heading is added or removed**, the subsection's own heading being already present.
3. **No `**Row` heading is added or removed**, and no `*PROPOSED DISPOSITION` or `*RULED DISPOSITION`
   line is touched.
4. **No numbered item's leading number changes**, and the strict set of numbered-item opening lines in
   §11 is identical on both sides.
5. **§13's tables, §13a's tables and figures, and §13b's three tables are byte-identical.**
6. **The subsection's bullet count is nine on both sides.**

A difference none of those accounts for is a STOP-and-report and the file is not committed until it is
removed. Subject:
`comparison L0/L1: the four quarantined rows of the per-note field list gain their audit questions under Ruling 80, three carried by question 2 and one written for Row 14.9`
Push; verify at the ref file.

**E1:** the five replacements present and each matching this face's replacement text verbatim; every
deleted line accounted for by an anchor; nine entries before and after; no numbered item touched; §10
and §12 untouched; the object-level properties 1 to 6 proven and reported as measured.

## Task 2 — the close

1. **The `STATUS.md` entries and the forward-bound move, IN THIS ORDER.** **(a) FIRST write this
   batch's own entries** at the top of `STATUS.md`: one pointer entry per task that did work (Tasks 0
   and 1; no figure restated, D-431), **the close entry FIRST and NAMING THIS DISPATCH BY ITS FILE
   NAME, the earlier task's entry below it saying `Same dispatch`**, and **the `Last updated: ` prefix
   moved from the ninth writing's close entry to your newest entry.** The close entry states in one
   sentence that the four quarantined rows of the per-note field list now carry their audit questions
   under Ruling 80, that three are recorded as carried by the standing question 2 and one carries the
   question the ruling wrote for Row 14.9, and that no numbered question was edited or renumbered and
   no row block touched. **(b) THEN re-aim `gen_status_batch_bound.py` — ALL FIVE authored
   constants:** `BASE_COMMIT` (this batch's Task 1 commit), `PREVIOUS_BATCH_DISPATCH`
   (`cc_instruction_comparison_l0_l1_ninth_2026_09_04.md`), `ACT_DATE` (`2026-09-04`), `DISPATCH` (this
   dispatch's file name), `TASK` (`Task 2`); **and append to `PREVIOUS_AIMINGS`, kept rather than
   replaced (#12), the ninth writing's aiming now superseded** (base `778e70c6dd…`, then-previous the
   eighth dispatch, executing act the ninth dispatch Task 2, which RAN and moved the eighth writing's
   three entries). Amend the head comments as the ninth did. **(c) THEN run
   `gen_status_batch_bound.py --apply`.** **Prediction, from the tool's mechanism as the eighth and
   ninth batches established it:** at `BASE_COMMIT`'s `STATUS.md` the ninth writing's close entry names
   the ninth dispatch and the two entries below it say `Same dispatch`; the second writing's first
   entry below those names no dispatch and does not say `Same dispatch`, so the run stops there —
   **exactly the NINTH writing's THREE entries move, the second writing's two stay in `STATUS.md`, no
   STOP.** If the tool nonetheless STOPs, restore it to its passing aim, re-run to prove it green,
   report the STOP with the tool's message, and move nothing by hand. **(d) LAST**, after the final
   edit to `STATUS.md`, `gen_session_start_read_size.py` regenerated and its `--check` confirmed green
   before the commit. The re-aimed tool, `tools/audit/status_batch_bound.json`, `STATUS.md`,
   `STATUS_ARCHIVE.md` and the read-size artifact are committed together in the close.
2. The FULL close appended to `cowork_away_returns.md` as a **THE AUDIT QUESTIONS FOR THE FOUR
   PER-NOTE FIELD-LIST ROWS (TENTH WRITING): THREE CARRIED BY QUESTION 2, ONE WRITTEN FOR ROW 14.9, NO
   NUMBERED QUESTION EDITED** section — both guard states, every SHA, E0–E2 graded, A1–A5 graded,
   declared departures, **the object-level check of Task 1 stated as measured, property by property,
   with the deleted-line enumeration published**, the nine entries counted before and after, and the
   plan lines (**the pack finding of §3al.4 to L2's pack; then L2, sequenced by Ruling 10**), and **the
   plan's tell in one sentence: did this batch produce anything other than the landed records, the five
   replaced sentences, the four Task-2 files and the report? If yes, name it.** The report is
   `cc_report_comparison_l0_l1_tenth_2026_09_04.md` — which likewise decides nothing; it carries the
   five replacements with their anchors, the deleted-line enumeration, and the object check of Task 1;
   where it would name hashes it cannot contain, point at the git log.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **The ordered structure
   yields: Task 0, the reading-file commit, the close, the end state.**

**E2 — DERIVED from the declared start state plus this batch's footprint (P-3):** at the tree carrying
the close, a fresh full guard run — **population 76**; **zero STOPs**; **the failing set exactly the
ten named, PLUS NONE**, the A2 exceptions handled as A2 says — committed only after the run that
produced it.

## What this batch does NOT do

- **No ruling taken, no position weighed, no verdict on any open question, on the derivation or on the
  deriving session's independence. No session booted. No measurement of the analysis built or run. No
  derivation of any statement about the analysis.**
- **NO AUDIT QUESTION IS AUTHORED BY YOU.** The one new question is Ruling 80's, quoted from the
  ruling; the three placements are the ruling's placements, likewise quoted. Composing, paraphrasing,
  extending or merging a question is a STOP-and-report.
- **No row block is touched. No numbered item of §11 is edited, deleted, renumbered or reordered. No
  proposed disposition is deleted anywhere. §10 and §12 are not edited.**
- **§13's tables, §13a's figures and tables, and §13b's three tables are untouched**, and no figure of
  any of them is restated on this face or in the replacements (#17f, D-431).
- **No edit to the derivation (banner or body), to the derived specification, to the brief, to the
  pack, its generator, its artifact, the population tool, any outgoing text, any governing document
  other than the two Task 2 orders, the suspension file, or any register source.**
- **No finding number. No open-items row created, flipped or discarded EXCEPT an apparatus-finding row
  under rule (c). No `src/` change, no golden, no test, nothing under `tools/corpus/` or
  `tools/robust_stop/`. No guard enrolment. No `tools/audit/*.py` written or edited except the
  re-aiming of `gen_status_batch_bound.py`'s five authored constants and its `PREVIOUS_AIMINGS` list
  (D-648).**
- **No move of the second writing's two `STATUS.md` entries. Nothing written into the suspension file
  or any register.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* **#12** — nothing deleted but the five sentences the ruling names, the
   preamble's former wording preserved verbatim at its own site, every numbered question and every
   proposed disposition standing, `PREVIOUS_AIMINGS` appended to. **#10** — the subsection ceases to
   state something false about itself in the same act that gives Row 14.9 its question, which is the
   whole of what Ruling 80's consequence clause requires. **#6** — one question for one concern:
   question 2 is pointed at rather than copied, and the one new question is written once, at the row
   it belongs to. **#19** — nothing is established by this batch; the five anchors are quoted from the
   file and the executing side proves them at the object. **#13** — an anchor not found verbatim is a
   STOP, and the five replacements stand or fall together. **#24** — the bound on this side's A1
   declaration is stated and the direction of its gap named. **#17(f)/D-431** — no count of the reading
   file is restated in any replacement, and this face's counts of entries are properties for the
   executing side to prove rather than figures of record.
2. *Conventions:* American English; no self-invented label — *the added subsection*, *an entry*, *the
   numbered list*, *a row's block* and *a changed passage* are all defined at the head, the last in the
   ruled words for what a diff tool calls a hunk; music-theory words in their musical sense, *exclude*
   for the prohibiting sense; *the current commit* for a commit named by hash; no numeric grade
   anywhere. **A hunk count is explicitly refused as a check**, the ninth batch having established that
   two nearby edits merge into one passage.
3. *Figures and premises:* the tip at the two ref files and its chain at `git show --stat` per explicit
   hash, taken by this side this session with every parent confirmed; the ninth batch's finished state
   proven at the close commit and the end-state commit; the guard summary and its ten failing tools at
   the tip's own artifact, read at the object by this side; every blob and every blob size at
   `git rev-parse` and `git cat-file -s` by explicit hash; the tool's five constants at the object; the
   five anchors read at the file with the file tools on a bridge-staged snapshot; question 2, §5's
   vocabulary, §11's opening convention and the five row blocks read at the file; S-3 read at the
   derived specification. **Declared departures of this writing side:** (i) `git show <hash>:path`,
   `git rev-parse`, `git cat-file -s` and `git show --stat` by explicit hash, their output filtered
   through `grep` and a python parser — git object queries, the ninety-fifth entry's declared form;
   (ii) the Ruling 80 section written into the sitting record by editing a bridge-staged copy and
   committing it back under a modification-time guard, the anchor proven unique before the edit and the
   result read back through the file tools — **a write, not a read for a fact** — performed THREE
   times: the section itself, then the consequence clause the ruling owed, then the correction of that
   clause found by the pre-release fact-check; (iii) three `cp` of that staged copy inside the sandbox,
   to deliver it. **No working-tree file was read for its CONTENT by any shell.**

   **★ THE PRE-RELEASE FACT-CHECK OF THIS FACE, RUN AT THE OBJECTS, AND WHAT IT FOUND — ONE DEFECT,
   CORRECTED BEFORE RELEASE, DECLARED AND NOT COUNTED.** The five anchors were re-matched at the staged
   file and all five stand verbatim, the four entry openings proven to occur exactly four times between
   them and the preamble's two lines exactly once. The three quotations of Ruling 80 were compared
   against the section as written. **The defect was in the RULING, not in the anchors:** its consequence
   clause named only the limb in which the placing ruling writes a question, which is false of the three
   entries the same ruling points at question 2, so a preamble replacement built on it would have
   widened the ruling's words. The clause was corrected at the sitting record before this dispatch was
   released, its former wording preserved at the site, and the correction is recorded in the ruling
   ledger above.
4. *File-tools rule:* every repository read through the file tools on bridge-staged snapshots, save the
   declared departure (i) above; no `git status`, no working-tree diff, no `git log`.
5. *Uncertainty:* this side read §11's numbered list whole — all 82 — and found exactly two questions
   about this field list, question 2 and question 69, the latter stating in its own text that it is the
   same subject as question 2 met a second time. **The rest of the reading file, 1,101,199 bytes, was
   not read**, so a row outside the seven read whole that bears on these placements was not seen. That
   the four entries needing replacement are exactly Rows 3.41, 3.42, 3.48 and 14.9 is this side's
   reading of the subsection at the staged copy; a fifth such entry found at the file is a
   STOP-and-report, not a sixth edit.
6. *Re-read from disk before release:* performed over this file after it was written to the root, and
   over the sitting record's inserted section, both read back through the file tools.

---

*Provenance: Cowork writing side, 2026-09-04, the session that booted on
`cowork_handoff_entry_one_hundred.md`, at tip `a70fcf6d51…` with `origin/master` equal, under Ruling 80
(§3ci) with Rulings 79 (§3ch) and 62 (§3bq) of
`cowork_rulings_2026_08_31_decision_surface_sitting.md`, the reading file's §5, §11 and the five row
blocks, and S-3 at the derived specification. TOWARDS the ultimate objective and TOWARDS the guiding
principles.*
