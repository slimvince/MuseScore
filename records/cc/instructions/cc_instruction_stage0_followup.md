# CC Instruction: Stage 0 follow-up — Commit 2 + tracked junk removal

## Context

Stage 0 work is verified and approved (Cowork re-checked Commit 1 contents, the
`kTemplateCount` sites incl. your sixth bounds-check site, the dead-field removal, and the
tie-policy doc section — all good). Two actions remain. Base: `7bc1609159`.

**Never stage the `muse` submodule. Explicit per-file `git add` only.**

---

## Task 1 — Commit 2 (code/doc hygiene) — APPROVED, commit now

Stage exactly:
```
cd C:\s\MS && git add \
  src/composing/analysis/chord/chordanalyzer.h \
  src/composing/analysis/chord/chordanalyzer.cpp \
  src/composing/analysis/function/harmonicfunctionlayer.h \
  src/composing/analysis/function/harmonicfunctionlayer.cpp \
  src/composing/analysis/region/regionanalyzer.cpp \
  tools/batch_analyze.cpp \
  docs/scoring_model.md ; echo "exit:$?"
cd C:\s\MS && git diff --cached --name-only ; echo "exit:$?"
```
(If your Commit-2 set differs from this list, stop and say so before committing.)
Verify the staged list is exactly those files, then commit with the instruction's original
message **amended in one place**: say the extents derive from `kTemplateCount` across
**six** literal-17 sites (the five scoring_model §9 sync sites plus the
`bassIsTemplateChordTone` tiePriority bounds check). Do NOT include the junk-file
deletions in this commit (Task 2 handles them separately).

## Task 2 — Tracked junk files: investigate, then remove in a separate commit

Cowork's independent check shows the two junk paths **present and modified ( M), not
deleted** — strong indication they were REGENERATED by the Task-7 build/test cycle after
your Task-2 deletion (Task 2 ran before Task 7). Their names look like build-tooling
accidents: a mangled PowerShell invocation written as a filename, and a `C:\tmp\build_out.txt`
redirect with the path separators eaten (the tracked names contain U+F03A, the Windows `:`
substitute).

1. Establish current ground truth (use `-z` to see exact bytes; redirect, output may render
   oddly):
   ```
   cd C:\s\MS && git status --porcelain | grep -E "ExecutionPolicy|tmpbuild" ; echo "exit:$?"
   cd C:\s\MS && git log --oneline -3 -- ':(literal)'"$(git ls-files | grep tmpbuild)" ; echo "exit:$?"
   ```
   Confirm: (a) both paths are tracked (appear in `git ls-files`), (b) whether they exist
   on disk right now.
2. **If they are back on disk** (expected): identify the generator before removing.
   Suspects: `setup_and_build.bat` or any wrapper it calls — grep for `ExecutionPolicy`,
   `Activate.ps1`, `tmpbuild`, `build_out` in the repo's .bat/.ps1/.sh files:
   ```
   cd C:\s\MS && grep -rn -l -E "ExecutionPolicy|Activate\.ps1|build_out" --include=*.bat --include=*.ps1 --include=*.sh . 2>/dev/null | grep -v node_modules | head ; echo "exit:$?"
   ```
   Report what you find. Do NOT fix the generator script in this instruction unless the fix
   is trivial AND the file is inside the pre-authorized set — generator scripts are outside
   `src/composing/`, so by default: document, don't touch, ask first.
3. Remove from tracking and prevent re-entry:
   - `git rm --cached -- '<exact name 1>' '<exact name 2>'` (use `--cached` if the
     generator will recreate them; plain `git rm` if not), delete from disk,
   - append both exact names to `.gitignore` (so regenerated copies stay untracked),
   - one commit containing exactly: the two deletions + the `.gitignore` change.
   Proposed message:
   ```
   chore: remove tracked build-artifact junk files + gitignore them

   Two accidentally-committed build artifacts (a mangled PowerShell invocation
   written as a filename, and a mis-redirected build log with U+F03A in place
   of ':'). Generated by <generator, per investigation — fill in>; gitignored
   by exact name so regenerated copies stay untracked. Generator script fix
   tracked separately.
   ```
4. Run the build once more after the removal and confirm `git status --porcelain` stays
   clean of both names (gitignore proof):
   ```
   powershell.exe -Command "Start-Process 'C:\s\MS\setup_and_build.bat' -Wait -NoNewWindow"
   cd C:\s\MS && git status --porcelain | grep -E "ExecutionPolicy|tmpbuild" ; echo "exit:$?"
   ```
   (Empty grep output + exit:1 is the success case here.)
5. **If they are NOT on disk** and the deletions are real: skip the generator hunt, do the
   simple removal commit (deletions + .gitignore entries anyway, as insurance), still run
   step 4's post-build check.

## Task 3 — Quick post-commit sanity

```
cd C:\s\MS && git log --oneline -4 ; echo "exit:$?"
cd C:\s\MS && git status --porcelain | grep -v "^??" ; echo "exit:$?"
```
Expected: Commit 2 + junk commit on top of `7bc1609159`; remaining modified files limited
to `muse` (never commit) and any CRLF-rendering noise; nothing unexplained.

Out of scope (tracked by Cowork for a later doc pass): reconciling CLAUDE.md's "4-site
atomic update" section with the new compiler-enforced `kTemplateCount` model.

## Report

Reply inline (no separate report file): Commit 2 hash; junk investigation result (tracked?
regenerated? generator identified — name the script and line if found); junk commit hash;
post-build gitignore proof; any deviations. Stop and ask if the staged set differs from
Task 1's list, if the generator fix would be needed to stop constant regeneration, or if
anything else doesn't match this instruction's description.
