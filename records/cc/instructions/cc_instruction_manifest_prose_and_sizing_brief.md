# CC dispatch — land the leak-list ruling and the sizing brief; take the TWO licensed manifest-prose corrections; re-render both subjects; boot NO session

> **Dispatch (Cowork, 2026-08-24, the forty-fifth Cowork session).** Executes Ruling 2 and §3 of
> `cowork_rulings_2026_08_24_sizing_leak_list_sitting.md`, and lands that record together with the
> DRAFT sizing brief (written by the writing side — **you land it, you do not write it, and a
> defect you find in it is a STOP-and-report, never a fix**). **Written at a verified stop: the
> sizing-pack batch's six commits are landed and verified, and nothing is running.** The tip was
> read by the writing side with `git for-each-ref` at the explicit hash `24d7f0be93` and at
> nothing else.
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** Task 0 lands the record, the brief, the modified
> handoff and this dispatch, and PUSHES. Task 1 takes exactly TWO bounded corrections to the
> boot-pack generator's own prose and re-renders BOTH subjects — ONE commit. Task 2 closes.
> **THIS BATCH BOOTS NO SESSION, DERIVES NOTHING, COMPARES NOTHING, AND OPENS
> `docs/scoring_model.md` NOT AT ALL.**
>
> **★ THE ONE THING TO GET RIGHT.** Both corrections are to text the generator WRITES about
> itself. Neither may change what any pack file says to a deriving session, and neither may move
> a single verdict, identity, document, passage, leak or count. **The proof required of both is
> the same one the last batch used and it is ordered below: the harmony-boundary subject's
> manifest block and BOTH pack directories re-render BYTE-UNCHANGED.**
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md`
> — CONDITIONAL, and this batch MEETS the condition: read it in full; rule (a)'s `gating_ids`
> only. **(2)** `cowork_handoff.md`, ITS CURRENT (FIFTY-THIRD) ENTRY. **(3)**
> `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)**
> `cowork_rulings_2026_08_24_sizing_leak_list_sitting.md`, whole — this batch's authority. **(5)**
> `cc_report_sizing_pack_preparation.md` §6, whole — its §6.2 and §6.3 are the two defects this
> batch corrects, in your predecessor's own words. **(6)** The whole of
> `tools/audit/gen_derivation_boot_pack.py`. **You do NOT read:** `docs/scoring_model.md`; the
> sizing brief beyond its status banner (it is the deriving session's, and carries nothing you
> execute); either blind output; any oracle document.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS
> built, designed, scoped or run, no design, no repair, no derivation of any specification
> statement, NO SESSION BOOTED, no document archived, moved or deleted AS A FILE, **no open-items
> row created, flipped or discarded**, **no edit to any governing document, any register entry or
> any register source**, **no change to the generator beyond the two corrections of Task 1** (the
> authored tables, the criterion, the leak check, `withhold_passage()`, the marker, the read-me's
> rendering and every STOP are untouched), **no edit to the brief's content**, **no re-grading of
> anything and no movement of any withheld identity, document or passage**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (CHECK mode) and again
> at the end (write mode, committed as the artifact of a real run). Verify every commit at the
> object by explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and
> `git diff` over the working tree time out on this mount and are denied by the armed guard — use
> the per-path explicit-hash forms named in A1. If staging is refused by a stale zero-length
> `index.lock` with no git process running, establish the cause (D-669), remove it, report the
> act. Plain single-quoted `git commit -m '…'` subjects — and note your predecessor's declared
> finding that such a subject cannot carry an apostrophe: write subjects that need none. The
> reserved-word and vocabulary rules bind every line this batch writes — including the standing
> bar that no NEW instance of a known collision is introduced (*figure* reserved for figuration;
> the numeric sense is *number* or *value*).**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 2 of the sizing leak-list sitting:** *"Two bounded generator corrections, each
  maintenance of a delivered artifact's truthfulness … are licensed in the next dispatch and no
  further: (a) the manifest's candidate-criterion block renders truthfully for a subject whose
  criterion is EMPTY BY RULING — stating that no search was run because the ruling empties the
  family, rather than describing a pattern match that never ran; derived from the authored entry,
  not switched on a subject name, with the harmony-boundary subject block required to re-render
  BYTE-UNCHANGED as the proof; (b) the generator's top-level `the_rulings_it_executes` list gains
  the sizing-pilot ruling's record name, making the list complete rather than incomplete."*
- **Ruling 1 of the same record:** the leak check keeps excluding D-270, D-296 and D-440 from the
  `scoring-model` pack. **Nothing in this batch acts on it** — it is landed inside the record; the
  check is already what it rules.

## Premise ledger

- **FACT** — the branch tip is `24d7f0be93`, subject opening *"measurement: the correction commit
  claims about the guard state were written before they were taken"*; both refs at it. Read by the
  writing side at the explicit hash on 2026-08-24. **Nothing is unpushed.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 75, passing 74, failing 1
  ([[OI-372]]'s tool), not_run 4, historical 16. **Re-read it yourself at the object.**
- **FACT** — at the tip, `derivation_boot_pack.json` carries TWO subjects. `harmony-boundary`'s
  `counted` reads `… withheld_identities_total 33, withheld_passages 2, leaks 3,
  design_intent_entries_rendered 208 …`; `scoring-model`'s reads `design_intent_class 244,
  candidates 0, verdicts IN 0 / OUT 0 / UNPLACED 0, withheld_identities_total 0,
  withheld_documents 0, withheld_passages 0, leaks 3, design_intent_entries_rendered 241,
  defect_type_rows_rendered 25, files_in_the_pack 7`. **BOTH blocks are predicted unchanged in
  every count by this batch; only prose moves.**
- **FACT** — at the tip, `evidence_pin_membership.json` → `ruling_records_read` is 56.
- **FACT, measured by the writing side:** `cowork_rulings_2026_08_24_sizing_leak_list_sitting.md`
  and `cowork_blind_session_brief_scoring_model.md` are on disk and absent from the tip; **NEITHER
  carries the word `pinned` on any line.** The brief is a DRAFT whose §8 carries points the user
  has not yet ruled; **it is landed in that state deliberately**, as the first unit's brief was.

- **ASSUMPTION A1 — the working tree, stated by CONTENT, and TAKEN FROM THE TREE.** Tracked
  modifications: **(i)** `cowork_handoff.md` — inserted entries above the committed content and
  the outgoing entry-point heading marked superseded, additions otherwise. **The COUNT of inserted
  entries is deliberately not asserted here:** the writing side has twice stated it from what it
  delivered rather than from the tree, and both times the tree carried one more. **Measure it, and
  report the count you find; a count other than one is NOT a STOP.** **A modification at any OTHER
  tracked path IS a STOP-and-report.** Untracked and to be landed by Task 0:
  `cowork_rulings_2026_08_24_sizing_leak_list_sitting.md` and
  `cowork_blind_session_brief_scoring_model.md` and this dispatch
  (`cc_instruction_manifest_prose_and_sizing_brief.md`) — **three untracked paths.** The rest of
  the untracked population is NOT landed and is outside A1's STOP. **Check ordered (Task 0, first
  act):** the per-path explicit-hash forms, and the whole tracked population enumerated with the
  sanctioned enumeration tool.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  The reds this batch causes and must clear: the membership check (Task 0 step 2); the boot-pack
  generator's own `--check` between the edit and the re-render, inside Task 1's SAME commit;
  `gen_session_start_read_size.py --check` at the close. Any other red is classed by its measured
  cause; one outside this batch's own acts' subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration.** **Route A** — the ruling-record count rises by
  exactly ONE (56 → 57), the added name being the leak-list record; **the brief is NOT a ruling
  record and must NOT enter route A — if it does, STOP-and-report.** **Route B** — neither landed
  file carries `pinned`. **Route C** — no tool added. Measure against the committed blob at
  `24d7f0be93` before accepting; an additive derived cross-reference caused by either landed
  document's own naming of a ratification surface is REPORTED rather than absorbed (your
  predecessor met exactly one); movement beyond is a STOP-and-report.

- **ASSUMPTION A4 — the guard registry.** No tool added; population UNCHANGED at run 75; failing
  set unchanged; the three other registry artifacts unchanged except run stamps.

- **ASSUMPTION A5 — the manifest and both packs after Task 1.** **Every count of BOTH subjects
  UNCHANGED**, field by field, against `24d7f0be93`. **BOTH pack directories BYTE-UNCHANGED, every
  file** — the corrections are to the manifest's own prose and to a top-level manifest list, and
  neither is rendered into any pack file; **a single changed pack file is a STOP-and-report.** The
  harmony-boundary subject's manifest block changes ONLY where correction (a) rewrites prose that
  block also carries, if it does; **if (a) is written so that the harmony-boundary block is
  byte-unchanged, say so and show it — that is the stronger result and the one to aim at.** The
  manifest's whole difference is to be measured and quoted in your report.

## The declared start state — at the tree you will meet

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing
   red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`** STALE — caused by this dispatch's own
   untracked ruling record.

**`gen_guard_state.py --check` opens "STALE vs the run" for the same cause (F83); not a third
failing check.** A THIRD failing verdict is a STOP-and-report.

## Findings in this batch

**No finding number is allocated; the series stands at F88.** Ruling 9's routing binds. **A third
prose defect of the same class that you MEET while making these two corrections is SURFACED in
your report with its verbatim and location — not corrected: the licence reaches two and the
narrow-letter default governs the rest.**

## Task 0 — land the record and the brief, clear the membership check, and PUSH

1. **A1's check first, entirely at content-addressed objects**, with the handoff's inserted-entry
   count MEASURED and reported.
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob BEFORE
   accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_24_sizing_leak_list_sitting.md`,
   `cowork_blind_session_brief_scoring_model.md`, and
   `cc_instruction_manifest_prose_and_sizing_brief.md` (new) — **five paths.** Subject, exactly:
   `record: the 2026-08-24 sizing leak-list ruling landed and the sizing subject DRAFT brief landed; evidence-pin membership regenerated`.
4. **PUSH**; verify at the object.
5. **`gen_evidence_pin_membership.py --check` must PASS.**

**E0:** the five paths; `ruling_records_read` 57 with the brief absent from route A; the check
passing; `origin/master` at the Task 0 commit.

## Task 1 — the two licensed corrections, and the re-render — ONE commit

**(a) The manifest's candidate-criterion prose, made true of an empty-by-ruling criterion.** The
block's conditional bullets and its `★_the_bound_on_the_candidate_criterion` paragraph are written
for a criterion that runs a pattern match over the register's own text. **DERIVED from the
authored entry — never switched on a subject name** — the block must, where a subject's criterion
is empty, say instead what is true: that **no candidate search was run for this subject, because
its withheld family is empty by ruling, and the empty candidate and verdict lists below are a
consequence of that ruling rather than of a search that returned nothing.** Where a subject's
criterion is non-empty, the block renders exactly as it does today. **The bound paragraph's own
disclaimer about an unmeasured pattern reach belongs only to the non-empty case.**

**(b) The top-level `the_rulings_it_executes` list gains
`cowork_rulings_2026_08_24_sizing_pilot_sitting.md`** with a one-line statement of what it rules
(the second subject and its empty family), in the shape the list's existing entries use.

**(c)** Run the generator; run `--check` over both subjects — a halt is a STOP-and-report.

**(d) A5, measured, every item**, with the manifest's whole difference quoted in your report and
both pack directories' byte-identity established by enumeration at the objects.

**(e)** Run the FULL guard set. A2 and A4 must hold. Commit the generator, the manifest, and the
guard artifacts the run rewrote, subject:
`the manifest prose corrected: an empty-by-ruling criterion no longer reads as a search that returned nothing, and the top-level rulings list is complete; both packs byte-unchanged; no session booted`.
Push; verify at the object.

**E1:** both corrections present and bounded; every count of both subjects unchanged; both pack
directories byte-unchanged; `--check` green over both subjects; **`CLAUDE.md`, `ARCHITECTURE.md`,
`DEFECT_TYPES.md`, `cowork_design_doc_template.md`, `cowork_audit_protocol.md`, the
phase-definition surface, `DECISIONS.md`, `docs/scoring_model.md` and every register source
byte-unchanged**; no session booted.

## Task 2 — the close

1. One `STATUS.md` pointer entry per task that did work; the previous batch's entries move through
   `gen_status_batch_bound.py --apply` (D-648, licensed in terms);
   `gen_session_start_read_size.py` regenerated (A2).
2. The FULL close appended to `cowork_away_returns.md` as a **THE MANIFEST PROSE CORRECTED AND THE
   SIZING BRIEF LANDED** section — both guard states, every SHA, E0–E2 graded, A3–A5 graded,
   declared departures, quarantined questions (or none new), the plan lines (**the user rules the
   brief's §8 points; the brief is then refreshed by the writing side and re-landed; the fresh
   Cowork session derives the sizing subject; the tests (a)+(b) batch; then the sizing record and
   the framework phase's opening surface, with the ledger owed before that phase**), and the
   plan's tell in one sentence. The report is `cc_report_manifest_prose_and_sizing_brief.md`;
   hashes a commit cannot contain point at the git log — **no backfill commits.**
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **The ordered
   structure yields FOUR commits; a correction commit beyond them is declared in BOTH surfaces, as
   your predecessors' were.**

**E2:** at the tree carrying the close, a fresh full guard run — every run check passing except
[[OI-372]]'s tool, zero STOPs, the population at run 75 — committed only after the run that
produced it.

## What this batch does NOT do

- **No session booted. No derivation. No comparison. `docs/scoring_model.md` not opened. Neither
  blind output opened.**
- **No change to the generator beyond the two corrections; no pack file changed; no count moved.**
- **No edit to the brief's content** — it lands as delivered, DRAFT and unruled at §8.
- **No finding number.** [[OI-179]] OPEN and GATES; [[OI-372]], [[OI-374]] stand.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule
  (c). No `src/` change, no golden, no test, nothing under `tools/corpus/` or
  `tools/robust_stop/`.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* **#19** — a generated artifact stops asserting a search that never ran,
   so what it claims is what was done; **#6** — one derivation of the prose from the authored
   entry, no per-subject branch; **#12** — every count, every pack file and the brief's own text
   preserved untouched; **#17(f)/D-431** — the manifest difference measured and quoted, no count
   restated in prose; **#13** — a third defect of the same class is surfaced, not silently
   swept in. **The just-in-time rule:** the brief lands DRAFT because the record's §3 orders the
   §8 points ruled after it lands, exactly as the first unit's brief was handled — no new
   obligation is created. Conforms.
2. *Conventions:* American English; no self-invented labels; music-theory words in their musical
   sense; towards, never against.
3. *Figures and premises:* the tip, the guard summary, both `counted` blocks and the membership
   count read at the objects by the writing side on 2026-08-24 and ordered re-read. **A1's
   handoff-entry count is deliberately left to measurement** — the writing side's twice-repeated
   error, corrected here by not asserting it.
4. *File-tools rule:* the writing side's own departures are in the fifty-third handover entry.
5. *Uncertainty:* the one measure-and-report item is whether correction (a) leaves the
   harmony-boundary manifest block byte-unchanged; both outcomes are admitted and the stronger is
   named.
6. *Re-read from disk before release:* performed over the delivered files, hash-verified after
   re-staging (F74), recorded in the fifty-third handover entry.

---

*Provenance: Cowork, 2026-08-24, the forty-fifth session, written at branch tip `24d7f0be93` under
Ruling 2 and §3 of `cowork_rulings_2026_08_24_sizing_leak_list_sitting.md`. TOWARDS the ultimate
objective and TOWARDS the guiding principles.*
