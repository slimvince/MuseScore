# CC dispatch — the STOP recorded where it belongs, and the ten available acts enumerated at the artifact

> **Status: ACTIVE DISPATCH, written 2026-08-11 (Cowork), at the STOP the previous dispatch
> declared.** That STOP is ACCEPTED: A1 and A2 refuted Cowork's premise, which Cowork had marked at
> §0b as *reported by CC and not independently verified* precisely so it would be checked. **No act
> on OI-274 follows from this dispatch** — the narrow question CC isolated belongs to a user sitting,
> not to a dispatch.
>
> **★ THIS DISPATCH IS PLAN-SHAPED, NOT REACTIVE, and the distinction is the reason it is short.**
> The plan is D-231's order; phase 1 completes by the acts available now and by the user's rulings
> on the user-blocked set, and by nothing else. Twelve dispatches today moved neither. This one
> produces the enumeration that lets the user choose among the available acts, and preserves a
> record that currently exists nowhere on disk.
>
> **Read IN FULL, and read FIRST:** `tools/audit/gating_row_sizing.json` **as a structure, not
> through match context** — its entries nest a `the_second_half` object inside main entries, and
> reading it through grep windows has produced two wrong claims on Cowork's side today.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_stop_record_and_enumeration.md`. Acts
> dated from the clock; commit messages via `-F`; **no positional count anywhere** (D-307, D-432).
>
> **★ All standing rules as adopted.** D-253 in every dialect. NO TRANSCRIBED VALUES (D-431) —
> the enumeration below is an exception in form only: it publishes row identities and authored
> field values read at the artifact, each cited to it, and derives nothing. Hold-don't-guess.
> **Read-only on the analysis: no `src/` edit, no behaviour change, no golden, no corpus of scores,
> nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement, no design.** D-231 and #8
> stand. **Phase 1's completion statement is not written, not drafted and not partially written.**
> `origin` only.

## 0a. WHY TASK 1 EXISTS — a record that is nowhere on disk

The previous batch reported *"`STATUS.md` and `cowork_away_returns.md` are untouched — Task 3 is
the close of acts that did not happen."* **So the STOP and its three findings exist only in the
executing session's report to Cowork, and nothing on disk carries them.** The dispatch protocol
puts a STOP in the returns file, and the findings are load-bearing for a user sitting: without them
the A1/A2/A3 verdicts and the narrow question must be re-derived from scratch.

## 0b. THE PREMISE LEDGER (#17a)

**FACT — verified by Cowork at the objects.** That the previous batch produced no commit: the
report states it and no new commit is claimed. The nesting of `the_second_half` inside main entries
in the sizing artifact, which Cowork read at the file and misread twice through match context.

**FACT — reported by CC and NOT independently verified by Cowork:** the A1, A2 and A3 verdicts and
their cited locations; that the sizing generator carries an authored staleness flag asserting
OI-274's second half is answered at HEAD. **Task 1 records these AS CC's findings with their cited
objects, not as Cowork's established facts** — recording a finding is not adopting it, and the
distinction is written into the record so a later reader can tell.

**ASSUMPTION — checked before the act resting on it; a refutation is a STOP.**

- **A1.** The enumeration is read from the artifact **as a structure** — each main entry taken
  whole, its nested second half distinguished from it. *Check: every listed act names which entry
  it came from and whether it is a main entry or a second half. If any act cannot be attributed
  that way, STOP.*
- **A2.** *Available now* means the artifact's own blocker value for **nothing beyond capacity**,
  read at the entry rather than inferred from its text. *Check: the value is quoted per act.*
- **A3.** The enumeration **derives nothing and changes nothing** — no regeneration, no verdict, no
  count computed by this dispatch. *Check: the artifact is not written to.*

## 0c. THE TASKS, IN ORDER

**Task 1 — record the STOP where it belongs. FIRST, atomic and tiny.** Append to
`cowork_away_returns.md`: the STOP, the A1, A2 and A3 verdicts each with the object cited,
**the narrow question isolated** — *should the `chordanalyzer.cpp`-scoring trigger also name the
joint estimator's specification, over and above the parallel trigger Ruling 64 created* — and the
finding that the sizing generator carries an authored flag asserting the refuted premise.
**Record all of it as the executing session's findings, not as established fact** (§0b). **The flag
is NOT corrected** — an act on a committed instrument under a STOP, and whether it is corrected at
all is a separate ruling. Commit and push.

**Task 2 — enumerate the acts available now (A1, A2, A3).** From the sizing artifact, read as a
structure: every act whose blocker is **nothing beyond capacity**, with its row, whether it is a
main entry or a second half, the act owed in the artifact's own words, and its size. Append to the
close. **Nothing is regenerated, nothing is judged, and no ordering or recommendation is offered** —
the choice among them is the user's and this dispatch supplies only the facts under it. Commit and
push.

**Task 3 — the close.** One `STATUS.md` pointer entry per task, nothing else in that file. Report at
the objects.

## 0d. WHAT IS DELIBERATELY NOT DONE

**No act on OI-274, in either branch.** No row written, corrected, flipped or discarded. No sweep for
siblings of any defect named here — if one is suspected it is reported, not acted on. No instrument
corrected. No sizing regeneration. **Phase 1 does not become nearer completion by this dispatch; it
becomes choosable.** OI-179 stays OPEN and GATES.

## 0e. STOP RULES

Halt with a STOP in `cowork_away_returns.md` if: an act cannot be attributed to a main entry or a
second half; the blocker value cannot be read at an entry; the artifact would have to be regenerated
to answer the question; or a guard goes red for a cause that is neither this dispatch's own edits nor
already recorded — the two standing reds are recorded and are not that.

---

*Provenance: Cowork, 2026-08-11, at the STOP the previous dispatch declared, written to the plan
rather than to the last finding, on the user's direction. Self-check run before release (D-434).*
