# CC dispatch — land the pilot-order ruling; build the boot-pack generator of boot-list amendment (a1); derive the candidates and AUTHOR the withheld family for the harmony-boundary subject; render the cut boot pack as a READING FILE for the user — and boot NO session from it

> **★★ AMENDED 2026-08-22 (the forty-fourth Cowork session) AFTER AN INTERRUPTED START — READ THIS
> BLOCK BEFORE THE REST, AND WHERE IT CONTRADICTS A CLAUSE BELOW, THIS BLOCK GOVERNS.** A Claude
> Code session began this dispatch and crashed before Task 0's commit. **The tree you meet is
> therefore NOT the one the original A1 describes**, and every difference was measured by the
> writing side at content-addressed objects and at the index on 2026-08-22, at tip `dcbfa5fe32`
> (both refs still at it; NO commit was added):
>
> 1. **The index is populated.** `git diff --cached --stat` shows exactly the four Task 0 paths
>    staged and uncommitted: `cc_instruction_pilot_preparation_withheld_family.md` (new, 451 lines
>    as staged), `cowork_handoff.md` (134 lines changed), `cowork_rulings_2026_08_22_pilot_order_sitting.md`
>    (new, 90 lines), `tools/audit/evidence_pin_membership.json` (6 lines). **Nothing else is staged.**
> 2. **`tools/audit/evidence_pin_membership.json` is MODIFIED against the tip** — the crashed session
>    performed Task 0 step 2 — and the difference is exactly A3 route A's prediction at the time:
>    `ruling_records_read` 46 → 47, the one added name `cowork_rulings_2026_08_22_pilot_order_sitting.md`,
>    and that record's appearance under the un-generated-document listing beside the phase-definition
>    surface. **That regeneration is now STALE**, because two further ruling records exist (item 4).
> 3. **Every other tracked path the writing side checked is UNMODIFIED against the tip:**
>    `tools/audit/gen_guard_state.py`, `tools/audit/gen_guard_classification.py`, `STATUS.md`,
>    `tools/audit/guard_state.json`, `tools/audit/guard_classification.json`, `cowork_away_returns.md`.
>    No generator was built; `tools/audit/derivation_boot_pack/` does not exist; no report exists.
> 4. **Three further untracked files exist at the root, written by the forty-fourth Cowork session,
>    and Task 0 now LANDS THEM TOO:** `cowork_rulings_2026_08_22_member_two_leak_sitting.md`,
>    `cowork_rulings_2026_08_22_deriving_side_sitting.md` and
>    `cowork_blind_session_brief_harmony_boundary.md`. Read the two ruling records IN FULL before
>    Task 1 — **the first of them adds an authored input to Task 1 (item 6)**; the brief is landed and
>    NOT read by you beyond its status banner (it is the blind session's, and carries nothing you
>    execute).
> 5. **`cowork_handoff.md` on disk carries TWO inserted blocks above the committed content** — the
>    forty-third AND a forty-fourth, the latter written after the interrupted start — with the
>    forty-second and forty-third headings marked superseded. The read-first clause below that names
>    the FORTY-THIRD block is read as the FORTY-FOURTH; the forty-third is read beneath it as the
>    forty-second was before.
> 6. **Task 1 gains ONE authored input, ruled by the user on 2026-08-22 (Ruling 1 of
>    `cowork_rulings_2026_08_22_member_two_leak_sitting.md`): a WITHHELD PASSAGE in member (2).**
>    The `WITHHELD` table of Task 1(a)2 carries, for the `harmony-boundary` subject and beside the
>    withheld identities and the withheld document, a `withheld_passages` list with ONE entry: the
>    founding-instance sentence of the never-work-from-memory rule in `CLAUDE.md`'s Conventions span
>    — **located by its own text and never by line number** (the sentence opening *"the
>    specification states it explicitly and twice"* and closing *"ranked the STRONGEST evidence)"*,
>    one contiguous span inside the bullet that opens `**NEVER WORK FROM MEMORY INSTEAD OF DOCUMENTED
>    FACTS`), carrying `finding`, `date` (2026-08-22) and `reason` quoted from that ruling record.
>    The rendered member (2) OMITS exactly that span and marks the omission in place with one line
>    stating that a passage is withheld for this subject (no content, no reason — the reason lives in
>    the manifest); the manifest publishes the omission with the text matched; **STOP if the anchor
>    text is not found exactly once**; the reading file of Task 2 lists the withheld passage as a
>    FIFTH list beside IN, OUT, UNPLACED and LEAKS. `CLAUDE.md` itself is untouched. The leak-check
>    scoping of Task 1(a)1 (members (5) and (6) only) is otherwise unchanged.
> 7. **The second ruling record (`…deriving_side_sitting.md`) changes NOTHING you execute** — it
>    rules the deriving side (a fresh Cowork session), the annotated-score route, and records a
>    correction to the pilot-order record's fact (i): the pollution distribution is complete over the
>    PASSAGE population and silent on nine of the twenty-six members (cite
>    `period_stratum_split.json` → `the_coverage_gap`; do not carry the figure). It is landed, not
>    acted on.
>
> **Task 0, AS AMENDED — replaces steps 1–3 below in these particulars and keeps the rest:**
> **(1)** A1's check is taken against THIS block's items 1–5, not the original A1: the index
> populated as item 1 states; `evidence_pin_membership.json` modified exactly as item 2 states
> (`git diff dcbfa5fe32 -- tools/audit/evidence_pin_membership.json` written to a scratch path and
> read); the six paths of item 3 unmodified; the five untracked paths (the two originals and item
> 4's three) present and absent from the tip. **Any other difference is a STOP-and-report.**
> **(2)** RE-RUN `gen_evidence_pin_membership.py` over the tree as it now stands and MEASURE the
> result against the committed blob at `dcbfa5fe32`: **route A — the ruling-record count rises by
> exactly THREE (46 → 49) and exactly three names are added** (the pilot-order, member-two-leak and
> deriving-side records); **route B — none of the three carries the word `pinned` on any line**
> (checked by the writing side with `grep -c` over the container copies: 0, 0, 0), so route B adds
> nothing; **the brief is NOT a ruling record and must NOT enter** — if it does, STOP-and-report.
> **(3)** Commit exactly these SEVEN paths and no other, re-staged by explicit path (the crashed
> session's staging is not trusted; `git reset` nothing — simply `git add` each path again):
> `cowork_handoff.md`, `tools/audit/evidence_pin_membership.json`,
> `cowork_rulings_2026_08_22_pilot_order_sitting.md`,
> `cc_instruction_pilot_preparation_withheld_family.md`,
> `cowork_rulings_2026_08_22_member_two_leak_sitting.md`,
> `cowork_rulings_2026_08_22_deriving_side_sitting.md`,
> `cowork_blind_session_brief_harmony_boundary.md`. Subject, exactly:
> `record: the 2026-08-22 pilot-order, member-two-leak and deriving-side rulings and the blind session's brief landed after an interrupted start; evidence-pin membership regenerated`.
> **(4)** and **(5)** as below. **E0, as amended:** seven paths; `ruling_records_read` 49 at the
> committed artifact; the membership check passing; `origin/master` at the Task 0 commit.
>
> **The declared start state, as amended:** the same two reds as below (OI-372's tool; the
> membership check STALE — now by THREE untracked records plus the stale regeneration), zero STOPs.
> **A4, as amended:** unchanged in every prediction. **The read-first list gains the two new ruling
> records, in full, after the pilot-order record.** **Everything else below stands as written.** The
> former text is preserved in place beneath this block (#12); where a clause below names "four paths"
> or "ONE block" or "exactly ONE", this block's figure governs.

> **Dispatch (Cowork, 2026-08-22, the forty-second Cowork session, its second dispatch).** Executes
> the FIRST act of Ruling 1 of `cowork_rulings_2026_08_22_pilot_order_sitting.md` — the preparation
> of the held-out test — under Ruling 1(a1) of `cowork_rulings_2026_08_22_boot_list_sitting.md`.
> **Written at a verified stop; nothing else is running and no dispatch was active when this was
> written.** The tip was read by the writing side with `git show -s` and `git for-each-ref` at the
> explicit hash `dcbfa5fe32` and at nothing else.
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** Task 0 lands the pilot-order sitting record, the
> modified handoff and this dispatch, and PUSHES. Task 1 builds ONE generator — the boot-pack
> generator the boot-list ruling's amendment (a1) names — registers it in the guard set, derives the
> CANDIDATE identities for the withheld family of the harmony-boundary subject at the objects,
> AUTHORS the family in the D-677 shape (finding, date, reason per identity), and renders the cut
> boot pack. Task 2 delivers the candidates-and-verdicts as a reading file and closes. **THIS BATCH
> BOOTS NO SESSION FROM THE PACK, DERIVES NOTHING, AND OPENS NO PILOT ACT.** The blind derivation is a
> LATER act, opened only after the user has ruled the authored family at the reading file (the
> standing clause: a session may author an establishment; its verdicts clear no guard until the
> reviewed set is applied).
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The pilot* — the phase that proves the
> derivation method before it is trusted. *A deriving session* — an implementation-blind session
> that writes what the analysis SHOULD do for one unit without reading what the code or the
> specifications say it DOES. *The curated boot list* — the implementation-free read list such a
> session boots from; its standing membership was ruled today (six members, eight exclusions, three
> amendments). *The boot pack* — the rendered, self-contained set of files a deriving session reads
> at boot, generated from the list; a deriving session opens the pack and nothing else. *The held-out
> test* — Ruling 4(c) of 2026-08-21: one decision derived blind with its user-ratified ruling
> WITHHELD; the withheld ruling is the ORACLE the derivation is later compared against. *The withheld
> family* — the set of register identities (and one document) withheld from the pack for ONE
> subject; an AUTHORED input, ruled by the user before any session boots. *The harmony-boundary
> subject* — how the analysis decides where one chord ends and the next begins; Ruling 3's held-out
> unit. *The design-intent class* — the `DESIGN-INTENT` class of
> `tools/audit/rulings_sort_classification.json`, the decisions-register entries sorted as ruled
> design intent rather than management of the implementation; boot-list member (5).
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional, and explicitly NOT demoted. **(3)** `BUILD_AND_TEST.md` is a
> CONDITIONAL read — **and this batch MEETS the condition**, because it runs the guard set and the
> audit generators, so read it in full. **(4) DO NOT READ THE WHOLE OPEN-ITEMS INDEX.** Rule (a)
> reads the DERIVED GATING ANSWER narrowed to its identity list —
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`. The INDEX
> remains the AUTHORITATIVE STATUS SURFACE.
>
> **★ AND THE STANDING CLAUSE BINDS THIS ONE: read `cowork_handoff.md`, ITS CURRENT (FORTY-THIRD)
> BLOCK.** **The findings series stands at F88 when this batch begins.** **This batch allocates NO
> finding number either** — see *Findings in this batch* below.
>
> **★ AND READ THE STANDING CLAUSES AT THE FILE, NOT AT A COUNT.** `cowork_audit_protocol.md`'s
> dispatch-protocol section is to be read **IN FULL** — every `###` section carrying the membership
> marker binds this dispatch and your report — before Task 1 writes anything. No number is stated
> here and none is to be written anywhere in this batch.
>
> **Then read, in full and in this order:** `cowork_rulings_2026_08_22_boot_list_sitting.md` (the
> list, its amendments, and the D-057 fact that forced (a1)); `cowork_curated_boot_list_draft_2026_08_19.md`
> (the six members and eight exclusions, by their own anchors); `cowork_rulings_2026_08_22_pilot_order_sitting.md`
> (this batch's authority, and the three facts its §1 carries); Rulings 2, 3, 4, 8 and 12 of
> `cowork_rulings_2026_08_21_successor_plan_sitting.md`; §3.2 and §3.8 of
> `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md`; plan §6.1, §6.4 and §7 of
> `cowork_specification_reconstruction_plan_successor_2026_08_21.md`; the forty-second handover
> block of `cowork_handoff.md` (below the forty-third); and the docstrings of
> `tools/audit/gen_guard_state.py`, `tools/audit/gen_guard_classification.py`,
> `tools/audit/gen_specification_document_set.py` (the `AUTHORED_EXCLUSIONS` table is the SHAPE the
> withheld family copies) and `tools/audit/gen_evidence_pin_membership.py`.
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/rulings_sort_classification.json` (**read and never regenerated**; its `entries`
> carry `id`, `group`, `title`, `home`, `proposed_class`);
> `tools/audit/decisions/backbone_decisions.json` (**read and never written**; its `decisions` carry
> `id`, `group`, `title`, `verbatim`, `plain`, `home`, `status`, `rationale`, `status_source`,
> `patterns`); `tools/audit/evidence_pin_membership.json` (Task 0's subject);
> `tools/audit/guard_classification.json`, `tools/audit/guard_state.json`,
> `tools/audit/epoch_write_path.json`, `tools/audit/recognizer_establishment_sort.json` (A4's
> subjects); `tools/audit/nongating_apparatus_rows.json` (**read and never written**).
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS built,
> designed, scoped or run, no design, no repair, **no derivation of any specification statement, no
> frame, no placement test, no format test, no held-out derivation, no fact-gate admission, no
> ledger, no mining read, NO SESSION BOOTED FROM THE PACK**, no document archived, moved or deleted
> AS A FILE, **no open-items row created, flipped or discarded**, **no edit to `ARCHITECTURE.md`,
> `docs/scoring_model.md`, `DEFECT_TYPES.md`, `cowork_design_doc_template.md`,
> `cowork_audit_protocol.md`, the phase-definition surface or any document-set member** (every
> member of the boot list is READ and RENDERED INTO THE PACK, never edited at its home), **no edit
> to any register entry or to the sort artifact**, **no edit to `DECISIONS.md` or its source data**,
> and **no caller flag or candidacy acted on**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end.**
> The declared start state is below and is stated **at the tree you will actually meet**. **Verify
> every commit at the object by explicit hash.** **The reserved-word conventions bind all new prose.
> The E-ordering rule stands, at both grains.** **Every guard run is taken at a quiet tree.** **Every
> shell command carries `; echo "exit:$?"` and no command produces large output to the terminal.**
> **`git status` and `git diff` over the working tree are measured to time out on this mount and
> are denied by the armed guard — use the per-path explicit-hash forms named in A1.** **Resolve
> every commit hash at the object before writing it anywhere; a padded abbreviation is the shape the
> previous batch's STOP caught.**
>
> **★ THE VOCABULARY RULES BIND EVERY LINE THIS BATCH WRITES** — TOWARDS the ultimate objective and
> TOWARDS the guiding principles, never *against* either; no self-invented or abbreviated term;
> every term of the tools' own explained in plain words at its first use in your report; **music-
> theory words in their musical sense only** — *key* is tonality, *measure* only as the verb or as
> *measurement*, *score* never numerical, *measurement tool* and never the reserved word, *register*
> only as *the decisions register* in full.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1(a1), 2026-08-22 boot-list sitting:** *"A per-session WITHHELD LIST, generated, never
  hand-cut. A dispatch for a session that runs the held-out test (Ruling 4 of 2026-08-21) names the
  withheld register identities as an AUTHORED INPUT carrying finding, date and reason — the same
  shape as an authored exclusion (D-677; the `STATUS.md` exclusion of
  `gen_specification_document_set.py`). A generator cuts member (5) to the `DESIGN-INTENT` class
  LESS those identities, derives and adds every entry of the class that quotes or cross-references
  the withheld oracle's home lines, STOPs on a named identity that is not in the class, and
  publishes the cut artifact; the session boots from that artifact and never from the whole sort.
  For the ruled oracle of the held-out test the first withheld identity is D-057 … The ten-factor
  model `cowork_joint_estimator_factorization.md` is recorded as ORACLE material for the pilot
  (Ruling 4's second arm), not a boot member."*
- **Ruling 1(a3), same record:** *"`DEFECT_TYPES.md` is admitted at its TYPE and DEFINITION columns
  only; its founding-instance and signature columns are excluded."*
- **Ruling 1, 2026-08-22 pilot-order sitting:** *"A first dispatch (ordinary boot) builds the (a1)
  generator, authors the WITHHELD FAMILY for the harmony-boundary subject as an input in the D-677
  shape — finding, date and reason per identity — and cuts the curated boot list into a rendered
  boot artifact; that artifact is delivered as a reading file and the user rules the family before
  any session boots from it."* And: *"The dispatch derives the candidates at the objects and the
  authored family is the user's to rule."*
- **Ruling 4 of 2026-08-21, the oracle:** *"(c) the held-out test — one decision derived blind with
  its user-ratified design-intent ruling withheld, the evidence-ranking ruling of 2026-08-11
  (`ARCHITECTURE.md:394-402`) as the oracle, and Claude Code's five recorded corpus traces of the
  ratified factorization (`cowork_joint_estimator_factorization.md:196-203`) as the oracle's second
  arm where the pilot unit is a factor of the factorization."*
- **§3.2 of the phase-definition surface, the constraint:** *"NOT ALLOWED: reading
  implementation-derived material inside the deriving session; treating the current text as
  authority rather than as an untrusted source."*
- **The standing clause on authoring:** *"A session is licensed to perform an owed establishment and
  to author its verdicts … and those verdicts CLEAR NO GUARD when they are written. They are
  delivered as a ratification-surface reading file … Authoring and clearing are two acts by two
  parties, and a session performs only the first."*

## Premise ledger

- **FACT** — the branch tip is `dcbfa5fe32` (parent `473f287372`), subject line *"end state: the
  guard set run at the tree the close left — every check passing except OI-372's tool, ZERO STOPs,
  the guard population unchanged, and no red appeared mid-batch"*; both refs at it. Read by the
  writing side with `git show -s` and `git for-each-ref` at the explicit hash on 2026-08-22.
  **Nothing is unpushed.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 74, passing 73, failing 1,
  not_run 4, historical 16; verdicts 73 `PASS`, 1 `FAIL`
  (`tools/audit/gen_filing_convention_application.py --check`, [[OI-372]]'s tool). Read at the
  object by the writing side; **read it again yourself at `git show dcbfa5fe32:tools/audit/guard_state.json`.**
- **FACT** — [[OI-179]] is in `gating_ids` on 2026-08-22 (`tools/audit/nongating_apparatus_rows.json`,
  the list opening at its line 186, the identity at line 244; the file hashes to the tip's blob).
- **FACT** — at `git show dcbfa5fe32:tools/audit/rulings_sort_classification.json`, the entry
  `D-057` carries `proposed_class: DESIGN-INTENT`, `decided_by: the decisions register's own
  home_is_layer_spec`, `home: ARCHITECTURE.md:4361`; the artifact's `the_distribution` reads
  `DESIGN-INTENT 244, IMPLEMENTATION-MANAGEMENT 167, NEEDS-THE-USER 0`; and the only
  `DESIGN-INTENT` entries whose `home` names `cowork_joint_estimator_factorization.md` or
  `cowork_factorization_desk_simulation.md` are `D-565` and `D-453`. **Re-derive all of it; cite
  none of it from here.**
- **FACT** — at `git show dcbfa5fe32:ARCHITECTURE.md` lines 394–402 state the four-rank evidence
  ordering and say in terms *"The legacy statement of the same ranking is `§5.2`'s
  priority-of-evidence table (register entry D-057)"*; lines 4361–4364 are that table. **Locate
  both by their own text (the heading *THE RANKING, and it binds THIS ARM* and the table's
  *Strongest | Actual sounding notes* row), never by line number, in the generator.**
- **FACT** — `DEFECT_TYPES.md` at the tip carries its catalog as a table whose header row is
  `| ID | type (plain) | founding instance | detection signature | mechanical? |` (read at the
  object). Ruling 1(a3)'s *"TYPE and DEFINITION columns"* are the `ID` and `type (plain)` columns;
  the other three are excluded.
- **FACT** — `tools/audit/gen_guard_state.py` derives the guard population from its own `AUTHORED`
  invocation table, and `gen_guard_classification.py` STOPs on *"a tool in the guard-state
  population with NO authored verdict"* (both docstrings, read at the objects). **A new `--check`
  tool is therefore registered in BOTH, in the same commit that adds it, or the registry STOPs.**
- **FACT, measured by the writing side on 2026-08-22:** `cowork_rulings_2026_08_22_pilot_order_sitting.md`
  is on disk and absent from the tip; **it does not carry the word `pinned` on any line** (Grep by
  the writing side); its sha256 and size are in the forty-third handover block (`sha256sum`,
  `wc -c`). `cowork_handoff.md` on disk carries the forty-third block above the committed content
  of `4547a2809f`'s successor tree — checked by A1, not asserted here.

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications: **(i)**
  `cowork_handoff.md` — ONE block (the forty-third) inserted above the committed content, the
  forty-second heading marked superseded-as-entry-point, otherwise additions only. **A modification
  at any OTHER tracked path is a STOP-and-report.** Untracked and to be landed by Task 0:
  `cowork_rulings_2026_08_22_pilot_order_sitting.md` and this dispatch
  (`cc_instruction_pilot_preparation_withheld_family.md`) — **two untracked paths; a named path the
  tree does not carry is a STOP-and-report.** The rest of the untracked population is NOT landed
  and is outside A1's STOP. **Check ordered (Task 0, first act):** per named tracked path
  `git diff --stat dcbfa5fe32 -- <path>; echo "exit:$?"`; per named untracked path
  `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  The reds this batch causes and must clear: **the membership check, cleared by Task 0 step 2**;
  **`gen_guard_state.py --check` and `gen_guard_classification.py --check`, red between the moment
  the new tool is added and the moment both registries carry it, inside the SAME commit**; **the new
  generator's own `--check`, red until its artifact is first written, inside the same commit**;
  **`gen_session_start_read_size.py --check` at the close** (Task 2 moves `STATUS.md` entries —
  F79's instance, cleared by regenerating). Any other red is classed by its measured cause, and one
  outside this batch's own acts' subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration, from EVERY route.** **Route A** — the
  ruling-record count rises by exactly ONE and ONE name is added
  (`cowork_rulings_2026_08_22_pilot_order_sitting.md`). **Route B** — the record carries the word
  `pinned` on no line, so route B adds NOTHING. **Route C — the new generator.** The membership
  derivation classes *"every ratification document a tool WRITES"*; the boot pack is NOT a
  ratification document and the reading file of Task 2 is written by YOU, not by a tool — so the
  new tool is predicted NOT to enter the membership artifact by that route. **Check it at the
  artifact after Task 1 as well as after Task 0; an entry for the new tool is a movement beyond
  the prediction and a STOP-and-report.**

- **ASSUMPTION A4 — the guard registry, from EVERY route, because this batch ADDS A TOOL.**
  **`guard_state.json`:** the population rises by exactly ONE (run 75), the addition being the new
  generator's `--check`, PASSING; the failing set unchanged at [[OI-372]]'s tool. **`guard_classification.json`:**
  one added member with its authored verdict. **`recognizer_establishment_sort.json`:** predicted
  to gain exactly the records its derivation writes for ONE new tool (the paths that tool reads
  and does not write); **no existing member's classification moves.** **`epoch_write_path.json`:**
  `tools walked` rises by one; `members` UNCHANGED — the new tool reads the TREE, is not
  epoch-pinned, and is predicted to fall outside that enumeration's members. **Any movement
  outside these predictions is a STOP-and-report; a movement inside them that is not ADDITIVE is a
  STOP-and-report; for any red whose cause you have not established at the object, D-669 binds —
  establish, then touch, or STOP.**

## The declared start state — at the tree you will meet

**The committed guard registry records the previous batch's END STATE** — one failing check
([[OI-372]]'s tool), zero STOPs. **The tree you meet differs from that record by this dispatch's OWN
INPUTS (F67 applied):**

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — caused by
   this dispatch's own input, the ONE untracked root-level `cowork_rulings_*.md` record.

**★ `gen_guard_state.py --check` will open *"STALE vs the run"* for the same cause (F83 applied);
it is not a third failing check.** The declared start state is MATCHED when the verdicts match.
**A THIRD failing verdict, or a membership difference beyond the one addition, is a STOP-and-report.**

## Findings in this batch

**No finding number is allocated; the series stands at F88 and stays there.** Ruling 9's routing
(2026-08-21): **(a)** a finding bearing on the analysis, its inputs or a measurement tool is SURFACED
under **"Quarantined audit questions"** — not rowed, not numbered, not acted on; the five already
surfaced (four at `cc_report_step_zero_return_executed.md` §8, one at
`cc_report_pass_continuation_second.md` §8) are NOT restated; **(b)** an apparatus finding gets its
open-items row and detail file in the commit that records it (rule (c)), lapsing under D-676;
**(c)** a finding bearing on neither is DISCARDED with finding, date and reason (#10). **An
establishment obligation is never discarded and always surfaced (#19).** **A candidate of Task 1(c)
that you cannot place IN or OUT of the withheld family is NOT a finding and NOT a STOP of this batch:
it is recorded UNPLACED in the reading file, with what was read, and the user rules it.**

## Task 0 — land the pilot-order record, clear the membership check in the same act, and PUSH

1. **A1's check first, entirely at content-addressed objects.**
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE its difference against the
   committed blob BEFORE accepting it**, against A3 routes A and B:
   `python tools/audit/gen_evidence_pin_membership.py; echo "exit:$?"` then
   `git diff --stat dcbfa5fe32 -- tools/audit/evidence_pin_membership.json; echo "exit:$?"` and the
   changed passages written to an absolute scratch path outside the repository and read with the
   file tools. Movement beyond the one addition is a STOP-and-report.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_22_pilot_order_sitting.md`, and
   `cc_instruction_pilot_preparation_withheld_family.md` (new) — **four paths.** Subject, exactly:
   `record: the 2026-08-22 pilot-order ruling landed; evidence-pin membership regenerated`.
   Staged by explicit path, never by `git add -A` or `git add .`.
4. **PUSH**; verify at the object that `origin/master` names your Task 0 commit.
5. **Re-run `gen_evidence_pin_membership.py --check`: it must PASS.**

**Registered expectation E0:** exactly the four named paths; one content modification matching
A1(i) and one bounded regeneration; two additions; the membership check passing at the resulting
tree, so the guard set stands at one failing from Task 0 onward; `origin/master` at the Task 0
commit.

## Task 1 — the boot-pack generator, the candidate derivation, the authored withheld family, the rendered pack — ONE commit

**This task is a derivation over a population — the candidate identities — and is published WHOLE
or not at all (the standing clause). It is the only task of its kind in this batch and nothing
large stands in front of it (D-670). Its steps land as ONE commit: a tree with the generator and
no artifact, or an artifact and no guard registration, is never committed.**

**(a) The generator — `tools/audit/gen_derivation_boot_pack.py`, new.** Its docstring states, in
the shape the other generators use: what it is (Ruling 1(a1)'s generator), what is AUTHORED (the
withheld family per subject; the member spans by anchor — imported from the ruled draft, not
re-decided) and what is DERIVED (the candidate list, the cross-reference additions, the rendered
pack, the checks), its STOPs, and the ruling records it executes. It takes `--subject <name>`,
`--check`, and writes `tools/audit/derivation_boot_pack.json` (the manifest, every subject) and a
rendered directory `tools/audit/derivation_boot_pack/<subject>/` holding SIX files, one per ruled
member, plus `00_READ_THIS_FIRST.md` naming the six in order and stating that a deriving session
opens nothing outside this directory. **The subject for this batch is `harmony-boundary`.**

1. **The six members, rendered by ANCHOR and never by line number**, each from the file at the tree
   (the generator reads working-tree content as the other generators do; `--check` re-renders and
   compares bytes):
   **(1)** `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` — the span from the
   heading `## 3. DECISION 1 — the proposed phase definitions` to the heading `## 4.`, whole;
   **(2)** `CLAUDE.md` — two spans: from `## Guiding principles` to the end of the paragraph opening
   `**Delegation pointer`, and from `## Conventions` to the end of the file's last section, `## The
   self-check after every coding exercise` (the draft's own anchors; STOP if an anchor is not found
   exactly once);
   **(3)** `cowork_design_doc_template.md`, whole;
   **(4)** `cowork_audit_protocol.md` — from the heading `## The dispatch protocol these audits are
   commissioned and run under` to the end of the file;
   **(5)** the design-intent class CUT — see (c) and (d); rendered as ONE markdown file, one entry
   per admitted identity carrying ONLY `id`, `title`, `verbatim` and `plain` from
   `backbone_decisions.json` → `decisions` (NOT `home`, NOT `rationale`, NOT `status_source`, NOT
   `patterns` — each of those names a place or a fact in the implementation's own documents);
   **(6)** `DEFECT_TYPES.md` — the catalog table's `ID` and `type (plain)` columns only, header
   row included, every other column dropped (Ruling 1(a3)).
   **The leak check, scoped to members (5) and (6):** an entry whose rendered fields contain a
   withheld identity string (`D-0NN` of the withheld set), the string
   `cowork_joint_estimator_factorization.md`, a `docs/` or `src/` path, or the string
   `ARCHITECTURE.md` is NOT rendered into the pack; it is listed in the manifest under `LEAKS` with
   the matching field and string, and goes to the user in the reading file as a fourth list. **A
   leak in members (1)–(4) is not checked** — member (2)'s Conventions span names `ARCHITECTURE.md`
   in the never-work-from-memory rule by design, and those members are ruled whole. State the
   scoping in the docstring.

2. **The authored table, `WITHHELD`**, keyed by subject, each identity carrying `finding`, `date`
   and `reason` — the D-677 shape — and, beside the identities, `withheld_documents` (for this
   subject: `cowork_joint_estimator_factorization.md`, reason: Ruling 4's second arm). **STOP on an
   identity not in the `DESIGN-INTENT` class; STOP on a missing field.**

3. **The derived additions**: every `DESIGN-INTENT` entry whose `title`, `verbatim`, `plain`,
   `rationale` or `status_source` contains a withheld identity string, or names a withheld document,
   is added to the withheld set with `derived_because` stating which field matched which identity.
   Published whole in the manifest.

4. **Register the tool**: an `AUTHORED` invocation in `gen_guard_state.py` and a verdict with its
   evidence in `gen_guard_classification.py`, in the shape every other entry there uses. **No other
   line of either registry changes.**

**(b) A session-boot guard the pack carries for itself.** `00_READ_THIS_FIRST.md` states: the
ordinary session-start read is REPLACED for the deriving session by this directory (the ruled
departure the evaluations' dispatch declared); `cowork_handoff.md`, `STATUS.md`, `DECISIONS.md`,
`ARCHITECTURE.md`, `docs/scoring_model.md`, the open-items register and every `cc_*` and `cowork_*`
file outside this directory are NOT opened; no branch rule is taken; **if the session nonetheless
meets any statement about how THIS project's analysis currently works, it stops reading that file
and records where and how much it saw** (the evaluations' own clause, reused verbatim in sense).

**(c) The CANDIDATE derivation for the `harmony-boundary` subject — published WHOLE, at the
objects.** A candidate is every `DESIGN-INTENT` entry meeting ANY of:
   - its `group` is `E` (Layer 2 — the slicer);
   - its `home` document is `cowork_joint_estimator_factorization.md` or
     `cowork_factorization_desk_simulation.md`;
   - its `home` is `ARCHITECTURE.md` at a line inside the span whose heading text is
     `THE RANKING, and it binds THIS ARM` (located by text at the tree) or inside §5.2's
     priority-of-evidence table (located by the row `| Strongest | Actual sounding notes |`);
   - any of `title`, `verbatim`, `plain`, `patterns` contains, case-insensitively, any of:
     `slice`, `slicing`, `segment`, `segmentation`, `boundary`, `boundaries`, `change-point`,
     `onset`, `release`, `harmonic rhythm`, `where one chord ends`, `finest grain`, `grain`,
     `atomic`, `sounding`, `struck`, `priority of evidence`, `evidence ranking`;
   - it is `D-057`.
   **The candidate list is published in the manifest with the matching criterion per candidate.
   Its reach is that of a pattern match and is UNMEASURED; state the bound on the artifact (the
   standing clause on enumerating patterns) — no analysis decision consumes it, the user does.**

**(d) AUTHOR the family over the whole candidate list — IN, OUT, or UNPLACED, one verdict per
candidate, with finding, date (2026-08-22) and reason.** The test, stated so it is checkable per
verdict: **IN** — a deriving session that read this entry would know, in whole or in part, what
the ruled answer to *where one chord ends and the next begins, and what evidence decides it* is;
**OUT** — the entry bears on another unit and reading it tells the session nothing about that
answer (state what it bears on instead); **UNPLACED** — you cannot decide from the entry's own text,
and you say what you read. **Default NOTHING: a verdict you cannot defend in one sentence at the
entry's own verbatim is UNPLACED.** Write the IN verdicts into `WITHHELD["harmony-boundary"]`;
write OUT and UNPLACED into the manifest beside them. **The user rules all three lists at the
reading file before any session boots; an UNPLACED candidate blocks nothing in this batch.**

**(e) Run the generator; run `--check`; run the FULL guard set. A4 must hold.** Commit the
generator, the two registries, the manifest and the rendered directory, plus the guard artifacts
the run rewrote, with the subject
`the boot-pack generator of boot-list amendment (a1); the withheld family for the harmony-boundary subject derived at the objects and AUTHORED — IN, OUT and UNPLACED published whole; the cut pack rendered; no session booted`.
Push; verify at the object.

**Registered expectation E1:** the generator registered in both registries and its `--check`
passing; the six members rendered by anchor with the leak check run on members (5) and (6) and
every leak listed rather than rendered; the
candidate list published whole with its criterion per candidate and its bound stated; every
candidate carrying exactly one of IN / OUT / UNPLACED with finding, date and reason; the derived
additions published with their cause; the manifest re-derived byte-identically by `--check`;
**`ARCHITECTURE.md`, `DEFECT_TYPES.md`, `CLAUDE.md`, `cowork_design_doc_template.md`,
`cowork_audit_protocol.md`, the phase-definition surface, `DECISIONS.md` and every register source
byte-unchanged**; no session booted.

## Task 2 — the reading file, and the close

1. **Write `ratification_surfaces/cowork_withheld_family_harmony_boundary_reading.md`** — a
   READING FILE for the user, in the standing ratification-surface shape: the subject re-explained
   from scratch; the oracle as Ruling 4 states it; the candidate criterion; then FOUR lists — IN,
   OUT, UNPLACED, LEAKS — each candidate with its `id`, `title`, the one-sentence reason, for
   UNPLACED what was read, and for LEAKS the field and string that matched; then what the user is
   asked to rule (the family, as a whole or per entry) and
   what the ruling does NOT do (it boots no session; the deriving side's identity is the user's,
   Claude Code by default under D-252). **No recommendation on any UNPLACED candidate** (the
   standing clause: where the record does not settle the question, the surface gathers facts and
   makes no recommendation).
2. One `STATUS.md` pointer entry per task that did work; the previous batch's entries move verbatim
   to `STATUS_ARCHIVE.md` through `gen_status_batch_bound.py --apply` at its three declared authored
   inputs (authored-input maintenance, D-648 — **licensed here in terms, so it is not a
   departure**); `gen_session_start_read_size.py` regenerated (A2).
3. The FULL close appended to `cowork_away_returns.md` as a **THE PILOT PREPARED: THE BOOT-PACK
   GENERATOR AND THE WITHHELD FAMILY** section — both guard states, every SHA, every registered
   expectation graded, A3 and A4 graded at the artifacts route by route, the declared departures,
   the quarantined questions (or none new), the plan lines (the blind derivation, opened only after
   the user rules the family; the comparison after it; the sizing pilot after the method is ruled;
   the two fact-gate whole reads), and **the plan's tell in one sentence: did this batch produce
   anything other than the generator, its registrations, the manifest, the rendered pack, the
   reading file and the report? If yes, name it.** The report is `cc_report_pilot_preparation.md`.
4. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
5. Commit, push, verify; then the one further commit with the end-state run.

**Registered expectation E2:** at the tree carrying the close, a fresh full guard run — **every
run check passing except [[OI-372]]'s tool, zero STOPs, the guard population at run 75** — run and
read, committed only after the run that produced them; the tell stated in one sentence.

## What this batch does NOT do

- **No session is booted from the pack. No derivation. No comparison. No oracle opened for the
  purpose of the test. No pilot act beyond the preparation the ruling names.**
- **No edit to any boot-list member at its home; no edit to any register entry, to the sort
  artifact or to `backbone_decisions.json`.**
- **No re-derivation of the document set, the widened population or the July screen; no edit to
  `gen_july_screen.py`.**
- **No finding number allocated.** [[OI-372]] and [[OI-374]] stay as found; **[[OI-179]] stays OPEN
  and GATES**; the five quarantined questions stand.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule (c).**
- **No `src/` change, no golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #19 — the family is AUTHORED and clears nothing until the user rules it;
   the candidate pattern's reach is declared unmeasured and bounded on its artifact; #6 — the
   withheld family has one home (the generator's authored table), the pack is derived from the
   ruled members and never a second copy kept by hand; #12 — OUT and UNPLACED verdicts are kept
   beside IN, nothing is dropped; #17(f)/D-431 — no count transcribed, the sort's distribution and
   the guard summary cited at the objects and ordered re-read; #24 — no comparison asserted.
   Conforms.
2. *Conventions:* American English; no self-invented labels — `WITHHELD`, `derived_because`,
   `00_READ_THIS_FIRST.md` follow the tools' own authored/derived shapes and are explained at the
   head; music-theory words in their musical sense; towards, never against.
3. *Figures and premises:* every quantity named to its artifact; every ruling quoted at its record;
   the D-057 fact and the sort's fields read at `dcbfa5fe32` objects by the writing side and
   ordered re-derived; the candidate list the surface carried is explicitly NOT a premise — the
   dispatch derives it.
4. *File-tools rule:* the writing side's own departures are in the forty-third handover block; the
   executing side is bound as `CLAUDE.md` Conventions state.
5. *Uncertainty:* no comparison between measured quantities asserted.
6. *Re-read from disk before release:* performed over the delivered file, hash-verified after
   re-staging (F74), recorded in the forty-third handover block.

---

*Provenance: Cowork, 2026-08-22, the forty-second session, written at branch tip `dcbfa5fe32` under
Ruling 1 of `cowork_rulings_2026_08_22_pilot_order_sitting.md` and Ruling 1(a1) and (a3) of
`cowork_rulings_2026_08_22_boot_list_sitting.md`. TOWARDS the ultimate objective and TOWARDS the
guiding principles.*
