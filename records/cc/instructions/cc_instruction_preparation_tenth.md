# CC dispatch — the preparation phase's TENTH batch: the evidence pin ruled as a class and applied to its derived membership, the findings series given its allocation point, and rule (a)'s pointer narrowed to its answer

> **Dispatch (Cowork, 2026-08-17).** Executes the three rulings of
> `cowork_rulings_2026_08_17_ninth_return.md`. **Written at a verified stop; nothing else is
> running and no dispatch was active when this was written.** The curated boot list, the empirical
> findings ledger, the fact-gate, the mining, the landing of the newly visible instruction files,
> the whole-document archiving wave, and every act on any candidacy a regenerated census may flip
> are NOT in this batch.
>
> **★ READ FIRST — AND THE READ REGIME HAS MOVED SINCE YOUR LAST BATCH. READ THIS PARAGRAPH BEFORE
> YOU READ ANYTHING ELSE.** The ninth batch changed what a session reads at session start, and the
> change is live. **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional, and explicitly NOT demoted. **(3)** `BUILD_AND_TEST.md` is now a
> CONDITIONAL read — **and this batch MEETS the condition**, because it runs the guard set, whose
> commands live there, so read it in full. **(4) DO NOT READ THE WHOLE OPEN-ITEMS INDEX.** Rule (a)
> of the open-items register now reads the DERIVED GATING ANSWER —
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` — and you open
> `OPEN_ITEMS.md` when you need a row. The INDEX remains the AUTHORITATIVE STATUS SURFACE; the
> derived answer is a route to one question it answers, never a second home for status (#6). An
> archive companion is reached only when a decision is re-opened.
>
> **★ AND ONE FILE IS ADDED TO EVERY DISPATCH'S READ-FIRST BLOCK FROM THIS ONE ONWARD, ON RULING
> 2(c): `cowork_handoff.md`, ITS CURRENT (TWENTY-EIGHTH) BLOCK.** Read it. The reason is finding
> **F58**, which this batch corrects: the ninth dispatch ordered you to WRITE INTO that file without
> ever ordering you to READ it, and the numbers the writing side had allocated three blocks above
> your edits were therefore invisible to you. **The findings series is shared between both sides and
> the current handover block is where the writing side's own additions to it are announced.**
>
> **Then read, in full:** `cowork_rulings_2026_08_17_ninth_return.md` (this batch's whole
> authority); `cowork_rulings_2026_08_17_session_start_read_sitting.md` **Ruling 1** (the ground
> Task 2 amends); `cowork_rulings_2026_08_16_preparation_return.md` **§6** (the Kind-1 treatment
> Task 1 extends) and **§5(D)** (the continuous-pruning rule every superseded wording moves under);
> your own `cc_report_preparation_ninth.md` **§§2.d, 3.e and 10**.
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/guard_classification.json` (Task 1's subject — the live-or-frozen split and the
> pinned kind); `tools/audit/nongating_apparatus_rows.json` (Task 2's subject);
> `tools/audit/guard_state.json` (the start and end states).
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no mining, no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, and **no caller flag or
> candidacy acted on** — a census crossing confers CANDIDACY only.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end;
> record both states.** The expected start state, which is the ninth batch's proven end state:
> **69 run, 68 passing, ONE failing — `tools/audit/gen_filing_convention_application.py --check`
> ([[OI-372]]) — 4 not run, 16 historical, zero STOPs.** **A different start state is a
> STOP-and-report.** Verify every commit at the object by explicit hash and record the SHAs in the
> close. **The reserved-word conventions bind all new prose. The new-tool rule stands. The
> E-ordering rule stands. F3's reading rule stands** (`gen_cluster_dispositions.py --verify` is the
> drift authority; `reaim_home_anchors.py --check` exits 0 while printing drifted anchors and is not
> the authority). **Every guard run is taken at a quiet tree.**
>
> **★ THE VOCABULARY RULE OF 2026-08-17 BINDS EVERY LINE THIS BATCH WRITES.** A surface rates an
> alternative **TOWARDS the ultimate objective** and **TOWARDS the guiding principles** — never
> *against* either. The general form binds beyond that word: a term carrying opposite senses in
> adjacent context is fixed AT THE WORD, never left to the reader.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1 — the evidence pin, as a CLASS:** *"A GENERATED DOCUMENT PUT TO THE USER FOR A RULING
  JOINS THE PINNED KIND AT THE MOMENT IT IS RULED FROM: its generator thereafter reads its inputs at
  the git objects of the commit the ruling names."* — and the general form it rests on: *"a document
  generated from a live data file is not evidence of what was PUT in front of the user unless its
  generation is PINNED."* — and its bound: *"A ratification document's job ends at the ruling.
  Pinning it afterwards removes nothing live — the current classification stays available in the
  underlying data file, which is NOT pinned and continues to re-derive."*
- **Ruling 2 — the findings series, in three limbs:** *"(a) CC's numbers STAND."* — *"(b) THE
  WRITING SIDE'S LINE-ENDING FINDING IS RE-ISSUED AS F57, its former number preserved beside it
  (#12) … F53 is recorded as ONE finding surfaced independently by both sides, not as two, and CC's
  marking of it as new is corrected in the same act."* — *"(c) EVERY FUTURE DISPATCH'S READ-FIRST
  BLOCK NAMES THE CURRENT HANDOVER BLOCK."*
- **Ruling 3 — rule (a)'s pointer:** *"RULE (a) OF THE OPEN-ITEMS REGISTER NAMES THE LIST OF GATING
  ROW IDENTITIES, NOT THE WHOLE SECTION. The grounds stay in the same data file one line below, and
  a session opens them when it wants to challenge a verdict — exactly as it opens the INDEX when it
  needs a row."* — and the clause that bounds it: *"Narrowing what a SESSION reads does not weaken
  what the generator establishes, and the executing act writes that distinction at the clause so a
  later reader cannot mistake one for the other."*

## Premise ledger

*(Every FACT pins content at content-addressed objects. No premise names a terminus commit, and no
premise asserts a changed-passage count — the F25 lesson.)*

- **FACT** — the ninth batch is complete, pushed, and verified at the objects by the writing side:
  the chain `f27c3ded70` → `ff99a71a22` → `514b0dbc19` → `4667816255` → `b8f5aaf268` →
  `15dfb0e172` → `e36168ec33` → `ccb3131725` → `1760d9a4a8`, every parent confirmed and every path
  count to the digit (4, 10, 20, 9, 19, 4, 9, 2). Its proven end-state guard shape is the start
  state declared above.
- **FACT, measured by the writing side and stated so Task 0a's check cannot mis-read it** —
  `cowork_handoff.md` is stored with **LF** line endings (zero carriage returns), and its working
  copy hashed **equal to its committed blob** at `1760d9a4a8` (content sha256
  `f14f9944c95e27382980b56892aafe3493a9896e21186daa39a6b4a3ed4eba80`, 317,754 bytes) immediately
  before the TWENTY-EIGHTH block was written into it. **So the only difference this file carries is
  the one A1 names.**
- **FACT** — the ruling record this batch executes exists on disk untracked, written by the writing
  side on 2026-08-17 and round-tripped back through the device bridge byte-identical.
- **ASSUMPTION A1** — the working tree carries exactly ONE tracked modification,
  `cowork_handoff.md`, whose content is: **the TWENTY-EIGHTH session-close block inserted, and the
  TWENTY-SEVENTH heading carrying an entry-point demotion marker — two parts of one act.**
  Untracked: `cowork_rulings_2026_08_17_ninth_return.md` and this dispatch. **Check ordered (Task
  0a, first act, blob against blob by explicit hash):** any tracked difference whose CONTENT falls
  outside that shape is a STOP-and-report.
  **★ THE F57 CAVEAT ON THAT CHECK, CARRIED FORWARD AND NARROWED BY MEASUREMENT.** The tree stores
  its text files in BOTH line-ending conventions; the repository marks them `text=auto` and git
  normalises on check-in, so git reports no modification while a raw byte copy does not hash equal
  to its blob. **A file differing from its blob by EXACTLY its own line count is NOT a modification
  and is NOT a STOP.** **Compare through git, never through a raw byte copy**, and where a
  byte-level comparison is used at all, normalise line endings before it decides anything. **For the
  one file A1 names this caveat does not arise** — it is measured LF above — but it binds every
  other file the check enumerates.
  **★ WHY THE BLOCK AND THIS ASSUMPTION WERE WRITTEN IN THE SAME ACT:** a handover block written
  after a dispatch has already stated its premise about the working tree is how a false premise
  reaches the executing side, which is the A1 premise error's own shape. The twenty-seventh block
  recorded that lesson and this dispatch applies it.
- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  **Check ordered (each task's last step):** any other red is classed by its measured cause, and one
  outside this batch's own acts' subjects is a STOP-and-report. The reds this batch itself may cause
  and must clear by their ruled treatments: **the F15 anchored-quote class**, because Tasks 2 and 3
  edit `CLAUDE.md` and the decisions register anchors quotes into it; **any check whose subject is a
  generator Task 1 pins**; and **the retirement caller census**, whose holds move when a
  mandatory-read listing changes (Task 4).
- **ASSUMPTION A3** — every generated document that has been put to the user for a ruling can have
  its ruling commit DERIVED from a ruling record that names it. **Check ordered (Task 1, first
  act):** a member whose ruling commit cannot be derived from a record is a **STOP-and-report**,
  never a guessed pin. **The membership is DERIVED and published, never hand-listed.**

## Task 0a — land the ruling record, the handover block and this dispatch

The record must be tracked before anything corrects the sites it rules on, which is why Task 0's
subjects land in two commits rather than one. This is the first.

1. **A1's check first, entirely at content-addressed objects**, under the F57 caveat above.
2. Commit exactly these THREE paths and no fourth: `cowork_handoff.md` (modified),
   `cowork_rulings_2026_08_17_ninth_return.md` (new), `cc_instruction_preparation_tenth.md` (new).
   Staged plainly; push; verify at the object.

**Registered expectation E0a:** exactly 3 paths at the enumeration; one modification whose content
matches A1's stated shape, two additions; no staging override.

## Task 0b — the two recorded corrections of Ruling 2(a) and 2(b), in ONE commit

Each is a correction of record: the corrected wording and the former number are PRESERVED (#12),
never deleted, and each carries its date and the finding it answers.

1. **The F57 re-issue, at the site where the collision was created.** In `cowork_handoff.md`, the
   **TWENTY-SEVENTH** block states: *"TWO WRITING-SIDE FINDINGS ADDED — **F52** (THE RULED
   SNAPSHOT-HASH TEST IS NOT LINE-ENDING-SAFE) AND **F53** (THE ONE ARTIFACT ENUMERATING GATING ROWS
   BY IDENTITY DRAWS THAT ENUMERATION FROM A FROZEN INVENTORY)."* Append a dated correction note
   immediately beneath that block's own findings paragraph, quoting the sentence it corrects, and
   stating: that **F52 as used there is RE-ISSUED AS F57** on Ruling 2(b), the former number
   preserved; that **F52 belongs to CC's finding** of the ninth batch; that **F53 is ONE finding
   surfaced independently by both sides**, not two; and that the cause is **F58**, with Ruling 2(c)
   as its remedy. **The block's own text is not rewritten** — a handover block is corrected by
   appending, on the pattern the ninth batch's own Task 0b used.
2. **The note at the site a future reader meets the numbers.** In `cowork_away_returns.md`, the
   **PREPARATION NINTH BATCH** section's **§8**, append one dated correction note stating that F53
   was independently surfaced by the writing side before that batch ran and is **one finding, not a
   new one**, and that **F57** is the writing side's re-issued line-ending finding, which **fired
   inside that batch at its own §4.e**. **Nothing already written in that section is edited** — it
   was true when written on the information its author had, and that is exactly what F58 records.
3. **No finding's CONTENT changes; no finding is dropped, merged or graded; none is rowed.**
4. Commit, push, verify at the object.

**Registered expectation E0b:** both sites carry a dated correction with the former number preserved
beside it; no finding's content, weight or population moves; no row is created, flipped or
discarded.

## Task 1 — Ruling 1: the pinned kind extended to its class, and the class's membership DERIVED and pinned, in ONE commit

1. **Write the class where a future pass meets it.** The Kind-1 treatment's operative home is
   `tools/audit/gen_guard_classification.py`, which carries each measurement tool's verdict and its
   ground. Write Ruling 1's sentence there **verbatim** as the class rule, with the general form
   beside it, so a later session classifying a new generator meets the rule at the tool rather than
   having to recall it.
2. **DERIVE the membership; do not hand-list it.** A member is a generator whose rendered document
   was PUT TO THE USER FOR A RULING. Derive the set by reading the ruling records themselves — each
   names the document it was taken from and, where the record states it, the commit it was ruled at.
   **Publish the derived membership with, per member: the document, its generator, the ruling record
   that names it, and the commit the pin resolves to.** **A member whose ruling commit cannot be
   derived from a record is a STOP-and-report** (A3), never a guessed pin, and never a pin taken at
   a branch tip.
3. **Apply the pin to each derived member**, on the shape the ninth batch already used for
   `gen_claude_md_finer_surface.py` and which is IMPORTED rather than re-invented (#6): the
   generator reads its inputs from the git objects at the pinned commit, and its check compares
   against the committed document. **`gen_claude_md_finer_surface.py`'s existing pin is RECORDED
   under the class rather than re-taken** — it is already correct and re-taking it would move a
   value for no reason.
4. **What the pin does NOT freeze, written at each pinned generator so it is not mistaken for F22's
   ossification:** the UNDERLYING DATA FILE is not pinned and continues to re-derive; only the
   rendering of the document the user ruled from is fixed. **A later ruling can unpin any member in
   one edit.**
5. **No verdict, fate, population or row moves by this task**, and no document is regenerated to a
   new content — a member whose current rendering already differs from its committed one is
   **restored to its committed bytes and the difference reported**, never adopted.
6. Commit, push, verify at the object.

**Registered expectation E1:** the class rule stands verbatim at the classification's home; the
membership is derived and published with each member's ruling record and resolved commit named; every
derived member is pinned or STOPped on; no document's committed content moves; the guard
classification re-derives.

## Task 2 — Ruling 3: rule (a)'s pointer narrowed to the answer, in ONE commit

1. **The clause, written where a session meets it.** In `CLAUDE.md`'s open-items register section,
   rule (a) currently names `tools/audit/nongating_apparatus_rows.json` →
   `★_the_live_gating_answer`. **Narrow it to that section's list of gating row identities**, and
   state in the same clause that **the grounds stand one line below in the same file and are opened
   when a verdict is challenged, exactly as the INDEX is opened when a row is needed.**
2. **Rule (a)'s authority is untouched and the clause says so** — the INDEX remains the
   AUTHORITATIVE STATUS SURFACE, and the derived answer is a route to one question it answers, never
   a second home for status (#6). That sentence is already at the site and is not rewritten.
3. **★ THE #19 DISTINCTION IS WRITTEN AT THE CLAUSE, and this is the half that binds hardest.** The
   establishment is what the GENERATOR proves on every run — reconciliation in both directions
   against the INDEX, every gating row carrying a ground, and a halt on any row it cannot place.
   **Narrowing what a SESSION reads does not weaken what the generator establishes.** Write that
   distinction at the clause so a later reader cannot mistake a narrower read for a weaker
   establishment.
4. **The superseded wording moves verbatim to `CLAUDE_ARCHIVE.md`** in the same act under the
   continuous-pruning rule (§5(D) of the 2026-08-16 record), a compact dated pointer left at the
   site naming its companion.
5. **The anchored-quote consequence is the F15 class and is cleared by its ruled treatment** —
   anchors re-aimed PER CITATION from the drift report, affected derivations regenerated under the
   ordinary enumerated-movement bound, `gen_cluster_dispositions.py --verify` the drift authority.
   **Any regenerated surface's own difference is MEASURED before it is accepted**, on the ninth
   batch's own §3.e pattern.
6. **Publish the measured effect**, because it is the point of the act: the session-start read's
   character total before and after, each value read from the files at explicit objects and not
   estimated.
7. Commit, push, verify at the object.

**Registered expectation E2:** rule (a) names the answer and not the whole section; the grounds are
reachable from the clause; the #19 distinction stands at the site; the superseded wording is
byte-present in the companion exactly once and absent from `CLAUDE.md`, with a dated pointer at the
site — **both directions proven, the second being the load-bearing one**; every re-aimed anchor
verified at its new coordinate; **no row is created, flipped or discarded and [[OI-179]] stays OPEN
and GATES.**

## Task 3 — Ruling 2(c): the read-first standing clause written at its DERIVED home, in ONE commit

1. **Derive the home; do not assume it.** The clause is a standing rule binding the WRITING side's
   authoring of dispatches. Establish where the writing side's document-authoring standards live by
   reading the candidates the record names — `cowork_design_doc_template.md`, which `CLAUDE.md`
   states is *"the ONE home for writing standards"*, and `CLAUDE.md`'s own Conventions section,
   which carries the rules that bind Cowork and CC equally. **Write the clause at whichever the
   record makes the home, with the ground for that placement recorded. If neither is the home for a
   dispatch-authoring rule, that is a STOP-and-report** — inventing a home is forbidden (#6).
2. **The clause:** *every dispatch's read-first block names the current handover block of
   `cowork_handoff.md`.* Record **F58** as its ground: the findings series is shared, has no other
   allocation point, and a dispatch that orders a write into a document must also order the read of
   it.
3. **This dispatch's own read-first block already discharges the clause**, so the act is to make it
   standing rather than to change anything already read.
4. Commit, push, verify at the object.

**Registered expectation E3:** the clause stands at a home the record makes a home, with the ground
recorded and F58 named; or a STOP with the candidates read and the reason stated. No other authoring
rule moves.

## Task 4 — the retirement caller census re-pinned at the moved read regime, in ONE commit

Ruling 4 of `cowork_rulings_2026_08_17_governing_surface_split.md` and §4 of the callers sitting:
**a mandatory-read or boot listing HOLDS a retirement candidate**, so Task 2 has moved what holds
candidates by changing what rule (a) names.

1. Regenerate `gen_retirement_caller_check.py` at **Task 3's commit** (`--at` that commit), with the
   callers sitting's ruled kinds unchanged.
2. **Every movement against the previous committed reading is enumerated and classed**, with the
   read-regime class the ninth batch derived used as it stands — **it is IMPORTED, not re-derived**
   (#6) — and tested BEFORE the growth relation, which is F47's lesson already applied. **A movement
   fitting no class is a STOP-and-report.** **An empty class is published as a MEASUREMENT**, on the
   ninth batch's own pattern, so the absence of a cause cannot be mistaken for nobody having looked.
3. **A verdict that flips toward PASSES-THE-CHECK confers CANDIDACY ONLY.** No candidacy is acted
   on, no file is archived, and every ruled condition on the archiving wave stands untouched.
4. **The KIND-UNDERIVABLE population returns to the user as published data** — it stood at ONE at
   the ninth batch's re-pin; a caller whose kind the derivation cannot place still STOPS to the user
   and is never guessed.
5. Commit, push, verify at the object.

**Registered expectation E4:** the check PASSES at the new pin; every difference is classed with zero
unclassed; every set-aside naming stays published; any verdict flip is enumerated and confers
candidacy only.

## Task 5 — the close

One `STATUS.md` pointer entry per task — and, per the standing Ruling-4 bound of the
governing-surface split record, **the same act moves the NINTH batch's entries verbatim to
`STATUS_ARCHIVE.md`** (`gen_status_batch_bound.py`), both directions proven. **The declared
`Last updated: ` prefix adjustment is imported, not re-decided (#6); an entry needing a second
adjustment still halts.**

The FULL close is appended to `cowork_away_returns.md` as a PREPARATION TENTH BATCH section: both
guard states, every SHA, every registered expectation graded MET or NOT MET with its reason, every
declared departure, and the carried findings restated for the phase's retrospective — **F1–F59,
surfaced and none rowed; the row bar stands whole this batch.** The report is
`cc_report_preparation_tenth.md`, an ordinary tracked file. Commit, push, verify at the object.

**★ THE E-ORDERING RULE, AT BOTH GRAINS.** The end-state guard run is taken AFTER the Task 5 commit
exists, and its output with the final SHAs lands in ONE FURTHER commit. **A batch-level summary
sentence written in the same commit as the work it summarizes is the same defect at a smaller
grain.** The ninth batch DECLARED its instance before it could be noticed, which is the remedy while
the rule does not yet reach that grain; do the same, or avoid the sentence.

**Registered expectation E5:** at the tree carrying the close, a fresh full guard run re-derives —
**every run check passing except [[OI-372]]'s tool, zero STOPs** — run and read, never inferred, its
values committed only after the run that produced them.

## What this batch does NOT do

- **No curated boot list.** It stays at its ruled definition — the implementation-free read list an
  implementation-blind session boots from, for those sessions only — and remains a live, unblocked
  preparation output awaiting its own drafting and sitting.
- **No fate on any `CLAUDE.md` span ruled at the eighth-return sitting.** Both stay at site, that
  question is CLOSED, and `gen_claude_md_finer_archive.py --apply` is not run.
- **No archiving pass.** Ruling 2 of the session-start-read sitting was discharged by the ninth
  batch; nothing here re-opens it, and none of the nineteen spans it read is re-proposed.
- **No candidacy acted on, no document archived, moved or deleted AS A FILE, no mining, no empirical
  findings ledger, no fact-gate admission**, no completion claim about the superseded phase-1
  program, no derivation, design, repair or pilot act.
- **No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
  `tools/robust_stop/`, no measurement of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly as
  found; **[[OI-179]] stays OPEN and GATES**; F3 stays surfaced, unfixed and unrowed at eleven times
  surfaced.
- **`gen_gating_row_sizing.py`'s frozen population is left alone.** Changing it is a separate act
  over a frozen artifact and is not ruled here.
