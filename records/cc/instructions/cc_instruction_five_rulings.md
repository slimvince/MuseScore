# CC dispatch — record and apply the five rulings of 2026-08-07

> **Status: ACTIVE DISPATCH, written 2026-08-07 (Cowork).** Read IN FULL.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_five_rulings.md`.
>
> **★ THE FREEZE IS NOT RELAXED except where a ruling below relaxes it explicitly.** Task 4
> changes a mechanism (the apparatus-rows verdict table) and Task 5 changes a mechanism (the
> disposition tool) and regenerates a measured surface — each is licensed by the user's ruling
> named at that task (D-436: changing a mechanism that fails a measured condition is the user's
> ruling). Everything else found this wave is rowed and left, except a finding whose subject
> bears on the analysis, its inputs, or a measurement tool a measurement depends on — that is
> SURFACED, not acted on (D-641).
>
> **★ NO FIGURES (D-431) AND NO STATE NOT READ (#17a).** No `src/` change, no goldens, no
> corpus, no `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to
> inference, no design. Phase 1 under D-231. **Never bash to read working-tree files (D-253)** —
> the guard is fixed but the rule is the rule.
>
> **★ Same-commit rule (D-230):** each ruling's register entry (data + regenerated files) lands
> in the commit that records it. **Commit only when the user asks** — leave the tree uncommitted
> at close and record the worktree enumeration, as the three preceding waves did.
>
> **★ Item 2 of the finish line is not started. Phase 1's completion statement is not written,
> drafted, or partially written.**

## 0a. THE RULING LEDGER — five rulings, RULED by the user 2026-08-07

Each was ruled on Cowork's decision surface of 2026-08-07 ("I agree with recommendations",
after the full surface with alternatives was delivered as user-visible text). The ruling is the
recommendation as presented; each is restated here in full (D-643 — the whole ruling, not the
branch that helps).

- **R1 — the edit surface for finish-line item 1's re-home class is WIDENED.**
  `docs/scoring_model.md`, `CLAUDE.md` and `BUILD_AND_TEST.md` are licensed for HOMING
  dispatches, **scoped to homing acts only**: writing a register entry's decision into its
  owning specification in that section's own voice with its defense, former home/class/verbatim
  preserved (#12). The licence does not extend to any other edit of those files. Context the
  ruling was taken on: the blocker partition at
  `tools/audit/decisions/item1_rehome_blocker.json` — the licensing class is the minority, the
  owner-not-determinate class is untouched by this ruling and comes back to the user per entry.
  **This dispatch RECORDS the licence; it performs NO homing under it** — the homing wave is the
  next dispatch, written when this one returns.
- **R2 — D-426 is NAMED as the successor carrying "the OI-194 increment" in D-285's
  supersession.** D-285's `superseded_by` names "the ratified factorization emission design
  (D-004 and the OI-194 increment)"; the second naming is by an open-items row, not a register
  identifier, and the user now names **D-426** ("the modal reading lands inside the notation
  increment; the ornament labels get their own increment, with the tracking row created at
  ruling time", home `cowork_notation_adoption_increment.md` §7) as the recorded decision that
  carries it. Consequence, stated with the ruling: D-426 is itself in item 1's second class (its
  home document's strongest naming is a form the delegation bar excludes), so the obligation
  moves to homing D-426 — which is where D-642 puts it, and item 1 already carries it.
- **R3 — OI-331 is resolved in the strike-and-record form (the D-644 shape, third instance).**
  The proxy clause and its worked example are STRUCK from `cowork_bounded_context_design.md`
  (D-261's home, a signed user-ratified surface — the user's authorization is this ruling): the
  clause *"In practice a layer uses a domain proxy that implies convergence … The proxy is
  validated once, in design, to imply convergence"* and the Layer-3 reach-back example inside
  it. The contract states the current behaviour (the headline criterion used directly — the
  as-built tracks the leading-edge key across iterations and stops when it stops changing) and
  records the struck clause as TRIED AND CLOSED with its evidence: the Phase-3 measurement
  disproved the proxy (D-622), and the one exercise of "validated once, in design" passed a
  proxy that measurement then refuted, which is the #19 conflict. **D-622 is recorded as
  superseding the proxy clause. D-261's headline rule — a layer never guesses how much context
  it needs; the amount is discovered by convergence — STANDS UNCHANGED**, keeps its ratified
  status, and its verbatim is re-taken from the edited home with the former verbatim preserved
  (#12). OI-331 flips.
- **R4 — OI-333 is resolved by one bounded repair wave (this one, Task 5).** Snapshot the
  committed disposition layer first (the O-12 pattern, #16); escape the six patterns; regenerate
  the disposition layer at the register's current backbone; diff against the snapshot and
  explain every cluster that moves — **expected movement is `unresolved` → `restates` for
  entries entered since the last successful derivation; ANY other movement is a STOP (#13)**;
  and add a narrow PRODUCIBILITY mode to `gen_cluster_dispositions.py` — compile every pattern
  and dry-run the derivation without writing — so this failure class fails a guard in future.
  `--check`'s existing re-read semantics are NOT changed (coverage proof and producibility are
  distinct concerns, #6 — two narrow checks, not one broad one). OI-333 flips at completion.
- **R5 — OI-332 is RE-CLASSED GATING, and OI-336 closes.** The row's apparatus classification
  was cut from the first half of D-438's line; the second half — *"a correction to a statement
  about the analysis or its build state, or the completion of a specification, GATES"* — reaches
  two of its three documents (the "no code" banner over built operations is a build-state
  statement; the falsified design with no supersession note is a statement about the analysis).
  The authored verdict for OI-332 in `tools/audit/gen_nongating_apparatus_rows.py` is corrected
  to GATES with this ruling as its provenance, the derived set regenerated, and the finish
  line's derived populations follow. OI-336 flips.

**None of R1–R5 authorizes a fix to the analysis, a design, or an inference change.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — established at the objects by Cowork this session (2026-08-07):**

- **F1.** The blocker partition and its file list: `tools/audit/decisions/item1_rehome_blocker.json`
  (read in full; the licensing class, the not-determinate class, the per-file entry lists).
- **F2.** D-285's successor block: `tools/audit/decisions/r1_superseded_reach.json`, the D-285
  row — the second successor is named "the OI-194 increment", `register_id: null`, and the row's
  own note records that a register search finds ONE entry on that subject.
- **F3.** D-426's index row and home: `DECISIONS.md` (the index row naming
  `cowork_notation_adoption_increment.md` §7, class gap).
- **F4.** OI-331's full row at `open_items/OI-331.md`, including its report that the code
  implements the criterion, not the proxy (`regionanalyzer.cpp`, the convergence note and the
  leading-edge comparison). That code claim is the ROW's, verified by the wave that wrote it.
- **F5.** OI-333's full row at `open_items/OI-333.md`, including the six-entry pattern table and
  the crash location, and its note that A1 was settled against the row blocking the completion
  inventory.
- **F6.** D-438's line, both halves, at `CLAUDE.md` (the open-items register section), and
  OI-336's full row at `open_items/OI-336.md` with `tools/audit/decisions/true_half_reach.json`.

**ASSUMPTION — each checked BEFORE the act it licenses, with a STOP if refuted:**

- **A1.** That the six `patterns` strings in
  `tools/audit/decisions/backbone_decisions.json` (entries D-555, D-570, D-576, D-577, D-578,
  D-581) are exactly as OI-333's table lists them, and are the ONLY patterns that fail to
  compile. → Task 5.1: compile EVERY pattern in the backbone before editing any; if the failing
  set differs from the row's six, STOP and report — fix the set found, not the set assumed.
- **A2.** That D-426 is the only register entry whose subject is the ornament-label increment.
  → Task 2.1: re-run the search before writing the naming; if more than one candidate exists,
  STOP — the naming is the user's and a second candidate re-opens it.
- **A3.** That the disposition regeneration, after the escape fix, completes and its movement is
  confined to `unresolved` → `restates`. → Task 5.4: the diff is the check; any other movement
  is a #13 STOP, reported with the clusters named.
- **A4.** That the proxy clause and its example are confined to the passage OI-331 quotes from
  `cowork_bounded_context_design.md` and are not restated elsewhere in that contract. → Task
  3.1: read the whole document before editing; if the clause is restated, the strike covers
  every instance or STOPs and reports.

## 1. Task 1 — Record ruling R1 (the licence)

**1.1** Record R1 in `cowork_audit_protocol.md`'s dispatch-protocol section, beside D-642,
D-643 and D-644, in that section's own voice: the three files, the homing-acts-only scope, the
provenance (user, 2026-08-07, this dispatch §0a). **1.2** Enter it in the register (data +
regenerated files, same commit) as a process decision at the next free identifier — do not
hard-code an identifier anywhere prose could go stale. **1.3** Perform NO homing under it.

## 2. Task 2 — Apply ruling R2 (the naming)

**2.1** Discharge A2 (the search, re-run). **2.2** Record the naming: D-285's supersession now
names D-426 as the carrier of the increment half, with the user's 2026-08-07 ruling as
provenance and the former `superseded_by` wording preserved (#12). **2.3** Regenerate what
follows the register; D-285's route in the derived artifacts moves from "a naming is owed
first" to "home the successor (D-426)". The finish line's own STOP must account for the
movement.

## 3. Task 3 — Apply ruling R3 (OI-331, the D-644 shape)

**3.1** Discharge A4 (read the whole contract document first). **3.2** Edit
`cowork_bounded_context_design.md`: the current-behaviour statement in the section's own voice
with its defense; the proxy clause and example struck; the tried-and-closed line naming D-622
and the measurement that decided it; former text preserved in place or in the register's
provenance per that document's own convention (#12). **3.3** Register acts in the same commit:
D-622 recorded as superseding the proxy clause; D-261's verbatim re-taken from the edited home,
former verbatim preserved, headline rule and ratified status unchanged; provenance on both
naming the user's 2026-08-07 ruling. **3.4** Flip OI-331's INDEX row with provenance; dated
note on the detail file. **3.5** Re-aim any drifted anchors per citation.

## 4. Task 4 — Apply ruling R5 (OI-332 re-classed; OI-336 closes)

**4.1** Correct the authored verdict for OI-332 in `tools/audit/gen_nongating_apparatus_rows.py`
to GATES, provenance the user's 2026-08-07 ruling, former verdict preserved (#12). **4.2**
Regenerate the derived set and every artifact that follows it (the finish line's populations
move: OI-332 leaves the reach-verdict item and joins the gating item). **4.3** Flip OI-336's
INDEX row with provenance; dated note on the detail file.

## 5. Task 5 — Apply ruling R4 (OI-333, the repair wave) — LAST, after Tasks 2–4, so the regeneration derives at the final backbone

**5.1** Discharge A1 (compile every pattern; report the failing set found). **5.2** Snapshot the
committed disposition layer (the O-12 pattern; record where). **5.3** Escape the failing
patterns in the backbone — the matcher must match what the author meant literally; former
strings preserved (#12). **5.4** Regenerate the disposition layer at the current backbone; diff
against the snapshot; every moved cluster explained; A3's STOP governs. **5.5** Add the
producibility mode to `gen_cluster_dispositions.py` (compile + dry-run, no write; `--check`
unchanged); add its invocation to the guard list — the guard list's own per-tool condition will
refuse it until its invocation and classification verdict are authored, so author both. **5.6**
Flip OI-333's INDEX row with provenance; dated note recording that the residual figure may now
be re-derived and the committed-layer bound is lifted. **5.7** The register's own
measured-remainder template refills from the regenerated layer — verify the generator fills it
and its guard passes.

## 6. Task 6 — Close

Guards at the tree with the list `gen_guard_state.py` derives; **fix nothing beyond the tasks
above**. Re-aim any anchor this wave's edits drift, per citation. Record the worktree
enumeration via `tools/audit/changed_paths.py` to a named artifact. Run
`tools/audit/process_check.py` over this dispatch. `STATUS.md` gains one POINTER entry — it must
also record, for the previous waves, that their work was COMMITTED on the user's instruction at
`bd3a608fecf82c446f959432b13e0a5944093cd2` (the commit-only act could not record itself; this is
the record the handoff's rule owes). Leave this wave's own work uncommitted.

## 7. Accepted outcomes

**A1 finding a different failing-pattern set is expected and is why it is compiled first.**
**A2 finding a second candidate is a STOP, not a failure** — the naming re-opens with the user.
**A3's diff showing movement outside `unresolved` → `restates` is a STOP with the clusters
named.** **A4 finding the clause restated elsewhere widens the strike or STOPs** — either is a
result. **Task 5.7 finding the template does not refill is a finding about the generator,
surfaced not fixed.**

## 8. Self-check (D-434) — run by Cowork before release

- **Ruling ledger:** five rulings, each in full with its context and its explicit non-authorization;
  none dressed as derivable (D-599 — the user chose among genuine alternatives presented in full
  prose).
- **D-643:** R5 quotes both halves of D-438's line; R3 quotes the clause it strikes.
- **#17(a):** six facts, each read by Cowork at the object this session; four assumptions, each
  with an ordered check and a STOP.
- **Principles:** #12 everywhere text moves; #16 at the snapshot; #13 at the diff; #6 at the
  two-narrow-checks choice and the no-second-shape-rule naming; #19 is what R3 and R4 exist to
  serve; D-436 licenses both mechanism changes by user ruling; D-431 — no figure appears in
  this dispatch, populations are named by artifact and identity only.
- **Sequencing:** Task 5 runs last so the disposition layer derives at the post-Tasks-2–4
  backbone; no task performs homing under R1.
