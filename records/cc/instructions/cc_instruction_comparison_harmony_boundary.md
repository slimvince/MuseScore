# CC dispatch — the COMPARISON: grade the blind derivation's 26 statements against the withheld oracle and the untrusted sources, deliver the tabulation as a reading file, and recommend NOTHING on the method

> **Dispatch (Cowork, 2026-08-24, the forty-fifth Cowork session).** Executes Rulings 1–4 and the
> §5 bounds of `cowork_rulings_2026_08_24_comparison_design_sitting.md`, under Ruling 4(c) of
> `cowork_rulings_2026_08_21_successor_plan_sitting.md` (the held-out test) and Ruling 1 of
> `cowork_rulings_2026_08_22_pilot_order_sitting.md`. **Written at a verified stop: the
> blind-output landing batch's four commits are landed and verified, and nothing is running.**
> The tip was read by the writing side with `git for-each-ref` at the explicit hash `2eeacf6b7a`
> and at nothing else.
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** Task 0 lands the sitting record, the modified
> handoff and this dispatch, and PUSHES. Task 1 is the COMPARISON — a textual grading of the
> blind output's statements against the oracle's two arms and the untrusted sources, delivered as
> `ratification_surfaces/cowork_comparison_harmony_boundary_reading.md` — ONE commit. Task 2
> closes. **THIS BATCH RECOMMENDS NOTHING ON THE METHOD (Ruling 4): the method ruling is the
> user's, framed by the writing side over your tabulation, later. THIS BATCH BOOTS NO SESSION,
> BUILDS AND RUNS NO MEASUREMENT, AND EDITS NOTHING BUT ITS OWN DELIVERABLES.** Your framing is
> NEUTRAL throughout: you grade; you neither refute nor confirm.
>
> **★ THE ORDERED FIRST READ COMES BEFORE YOUR SESSION-START READ, BY RULING, AND THE DEPARTURE
> IS OF RECORD (Ruling 2).** Your VERY FIRST read this session is
> `cowork_blind_derivation_harmony_boundary_2026_08_23.md`, WHOLE, before `CLAUDE.md`, before
> anything — because the ordinary session-start read carries the oracle's founding-instance
> statement in words, and the ruling orders that you understand what the blind session claimed
> before the oracle's frame exists to map it onto. The read SET of the session-start bar is
> unchanged; its ORDER is changed by the ruling. Then: **(1)** `CLAUDE.md`, `STATUS.md`,
> `DECISIONS.md` in full; `BUILD_AND_TEST.md` — CONDITIONAL, and this batch MEETS the condition
> (it runs the guard set): read it in full; rule (a)'s `gating_ids` at
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` (DO NOT read the whole
> open-items index). **(2)** `cowork_handoff.md`, ITS CURRENT (FIFTIETH) ENTRY. **(3)**
> `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)**
> `cowork_rulings_2026_08_24_comparison_design_sitting.md`, whole — this batch's authority.
> **(5)** THE ORACLE, at the objects and only then: `ARCHITECTURE.md`, the span whose heading
> text is `THE RANKING, and it binds THIS ARM` and §5.2's priority-of-evidence table (locate both
> by their own text — the row `| Strongest | Actual sounding notes |` — never by line number;
> D-057's register entry at `tools/audit/decisions/backbone_decisions.json` is its legacy
> statement); and `cowork_joint_estimator_factorization.md`, the five recorded corpus traces
> (locate the section by its own text), read for the SECOND ARM — a statement is graded against
> this arm only where it touches a factor of the ratified factorization. **(6)** The reading file
> `ratification_surfaces/cowork_withheld_family_harmony_boundary_reading.md` (what was withheld
> and why — you need it to apply the caveats). **(7)** `cowork_rulings_2026_08_24_blind_return_sitting.md`
> Ruling 1 (the annotated-pair correction). **(8)** The untrusted sources — the current
> specifications and code — are opened PER STATEMENT as §T1 orders, not read whole up front.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The blind output* — the 26 statements and
> 10 open questions the implementation-blind session wrote; in git, byte-identical, at the root.
> *The oracle* — the user-ratified answer withheld from that session: the evidence-ranking ruling
> (first arm) and the factorization's five corpus traces (second arm, engaged only where a
> statement touches a factor of the factorization). *The untrusted sources* — the current
> specifications and code; under the phase definitions they are EVIDENCE about the present text,
> never authority over a statement. *Salvage* — the blind output's own source class for a
> statement taken from a ruled design-intent entry in its pack, cited by identifier. *The
> tabulation* — your deliverable: one row per statement, in the closed vocabulary below.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, **no measurement of the ANALYSIS
> built, designed, scoped or run** (the comparison is TEXTUAL), no design, no repair, no
> derivation of any specification statement, NO SESSION BOOTED, no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit to any
> governing document, the generator, the pack, the brief, the blind output, the withheld-family
> reading file, any register entry or any register source**, **no verdict word attached to the
> blind session's independence record** (relay its contents as facts), **no recommendation on the
> method anywhere** — not in the reading file, not in the report, not in a commit message.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (CHECK mode) and again
> at the end (write mode, committed as the artifact of a real run). Verify every commit at the
> object by explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and
> `git diff` over the working tree time out on this mount and are denied by the armed guard — use
> the per-path explicit-hash forms named in A1. If staging is refused by a stale zero-length
> `index.lock` with no git process running, establish the cause (D-669), remove it, report the
> act. Plain single-quoted `git commit -m '…'` subjects. The reserved-word and vocabulary rules
> bind every line — *score* only the musical score, *key* only tonality, no numeric grade
> anywhere: the vocabulary below is the whole of the verdict language.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 4(c) of 2026-08-21 (the oracle):** *"the held-out test — one decision derived blind
  with its user-ratified design-intent ruling withheld, the evidence-ranking ruling of 2026-08-11
  (`ARCHITECTURE.md:394-402`) as the oracle, and Claude Code's five recorded corpus traces of the
  ratified factorization (`cowork_joint_estimator_factorization.md:196-203`) as the oracle's
  second arm where the pilot unit is a factor of the factorization."* (Locate both by their own
  text at the current tree; the line numbers are the ruling's own record, not your route.)
- **Ruling 3 of the comparison-design sitting (the vocabulary):** quoted in full in §T1(c) below,
  and it is CLOSED — no other verdict word exists.
- **Ruling 4 of the same sitting:** *"The comparison tabulates; the method ruling comes to the
  user afterwards as a Cowork-written decision surface over the tabulation."*
- **§5 of the same sitting (the bounds):** the D-450/D-575 caveat; salvage visibility; the
  independence record relayed without a verdict word; the three annotated pairs may be OPENED and
  CITED where a statement's own text names them, and nothing is run over them; neutral framing.
- **Ruling 1 of the blind-return sitting:** the third pair is the BWV 301 score with *When in
  Rome* folder `134`'s `analysis.txt` — by content, never by number.

## Premise ledger

- **FACT** — the branch tip is `2eeacf6b7a` (parent `2a799c2444`), subject opening *"correction:
  the report and the close both stated TWO guard denials"*; both refs at it. Read by the writing
  side at the explicit hash on 2026-08-24. **Nothing is unpushed.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 75, passing 74, failing 1
  ([[OI-372]]'s tool), not_run 4, historical 16. **Re-read it yourself at the object.**
- **FACT** — at the tip, the blind output is IN GIT at
  `cowork_blind_derivation_harmony_boundary_2026_08_23.md`, blob
  `49d92ccb14614ba71ee755d4917cdfc14e370222`, 89,497 bytes, sha256
  `86b194280c9ab24fdb330cdf6fb34ab5a7cc70fe19b5ea25db7d7e0875ab0407`, banner *DRAFT — BLIND
  DERIVATION, NOT COMPARED, NOT RATIFIED*. **No party on the writing side has read it beyond its
  first three lines; you are the FIRST reader of its content since the session that wrote it.**
  The deriving session reported it carries 26 statements and 10 open questions — **relayed, not
  verified; you count them yourself and your counts are the figures of record.**
- **FACT** — at the tip, `evidence_pin_membership.json` → `ruling_records_read` is 53.
- **FACT, measured by the writing side:** `cowork_rulings_2026_08_24_comparison_design_sitting.md`
  is on disk and absent from the tip; it does not carry the word `pinned` on any line.

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications: **(i)**
  `cowork_handoff.md` — ONE entry (the fiftieth) inserted above the committed content, the
  forty-ninth heading marked superseded-as-entry-point, otherwise additions only. **A
  modification at any OTHER tracked path is a STOP-and-report.** Untracked and to be landed by
  Task 0: `cowork_rulings_2026_08_24_comparison_design_sitting.md` and this dispatch
  (`cc_instruction_comparison_harmony_boundary.md`) — **two untracked paths.** The rest of the
  untracked population is NOT landed and is outside A1's STOP. **Check ordered (Task 0, first
  act — AFTER the ordered first read):** per named tracked path
  `git diff --stat 2eeacf6b7a -- <path>; echo "exit:$?"` with the changed passages written to a
  scratch path outside the repository and read with the file tools; per named untracked path
  `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`; and the whole tracked
  population enumerated with the sanctioned enumeration tool.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  The reds this batch causes and must clear: **the membership check, cleared by Task 0 step 2**;
  **`gen_session_start_read_size.py --check` at the close**. Any other red is classed by its
  measured cause; one outside this batch's own acts' subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration.** **Route A** — 53 → 54, the ONE added name
  `cowork_rulings_2026_08_24_comparison_design_sitting.md`. **Route B** — the record carries
  `pinned` on no line. **Route C** — no tool added. Measure before accepting; an additive derived
  cross-reference is reported rather than absorbed; movement beyond is a STOP-and-report.
  **Note: your Task 2 reading file lands under `ratification_surfaces/` — if the membership
  derivation reacts to it, measure, report the movement and its route, and class it by its
  measured cause; a movement you cannot class is a STOP.**

- **ASSUMPTION A4 — the guard registry.** No tool added; population unchanged at run 75; failing
  set unchanged; the three other registry artifacts unchanged except run stamps.

- **ASSUMPTION A5 — byte-unchanged by this batch, verified at the objects after the last commit:**
  the blind output, the pack directory and its manifest, the brief, the withheld-family reading
  file, every governing document and register source. Any difference is a STOP-and-report.

## The declared start state — at the tree you will meet

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing
   red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`** STALE — this dispatch's own untracked
   record.

**`gen_guard_state.py --check` opens "STALE vs the run" for the same cause (F83); not a third
failing check.** A THIRD failing verdict is a STOP-and-report.

## Findings in this batch

**No finding number is allocated; the series stands at F88.** Ruling 9's routing binds. **What
you find IN the blind output is not a finding of this batch — it is the tabulation's content.** A
defect in the comparison APPARATUS (a missing oracle span, an unlocatable anchor, a statement
count that does not match the output's own structure) is a STOP-and-report, not a thing to fix.

## Task 0 — land the record, clear the membership check, and PUSH

1. **A1's check** (after the ordered first read).
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob at
   `2eeacf6b7a` BEFORE accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_24_comparison_design_sitting.md`, and
   `cc_instruction_comparison_harmony_boundary.md` (new) — **four paths.** Subject, exactly:
   `record: the 2026-08-24 comparison-design rulings landed; evidence-pin membership regenerated`.
4. **PUSH**; verify at the object.
5. **`gen_evidence_pin_membership.py --check` must PASS.**

**E0:** the four paths; `ruling_records_read` 54; the check passing; `origin/master` at the
commit.

## Task 1 — the comparison — ONE commit

**(a) Establish the population at the output's own structure.** Count its statements and its open
questions at the file; your counts are the figures of record and go in the manifest header of the
reading file (named to the file, not restated elsewhere — D-431). A structure you cannot parse
into statements is a STOP.

**(b) Per statement, in the output's own order:** quote the statement (its six fields compressed
to statement + defense-class + source class); quote the oracle text it is graded against (or
state which arm is engaged and why, or that neither speaks); grade on the ORACLE AXIS with
exactly one of **MATCHES / DEFENDED ALTERNATIVE / SILENT / CONTRADICTS, UNDEFENDED / UNGRADED**
(UNGRADED = a verdict you cannot defend in one sentence at the two texts; say what you read); then
on the CURRENT-TEXT AXIS, from the untrusted sources opened per statement, exactly one of
**AGREES / DIFFERS / THE TEXT IS SILENT**, each with the specification or code location cited —
evidence about the present text, never a verdict on the statement. **Second arm engaged only
where the statement touches a factor of the ratified factorization, and the row says when it is.**
**The D-450/D-575 caveat:** where a statement cites the notated key signature as boundary
evidence, the row applies the caveat in terms and is not graded CONTRADICTS for that citation.
**Salvage:** a salvage-classed statement's row carries its cited identifier and grades the
statement's content normally — the source class is visible, not penalized.

**(c) The open questions:** listed, ungraded, each with one sentence on what would settle it and
whether the oracle or the current text already does (that sentence is information, not a grade).

**(d) The independence record:** relayed in its own section — the stop-on-meeting record or its
positive absence, the disclosures, the files consulted, the sizing record's own headline figures
named to the output file — **with no verdict word attached.**

**(e) The reading file** — `ratification_surfaces/cowork_comparison_harmony_boundary_reading.md`,
in the standing ratification-surface shape: the subject re-explained from scratch; the oracle
quoted at its arms; the vocabulary and its closed-ness stated; the caveats stated; then the
tabulation; then the distribution of verdicts BY COUNT AT THE FILE'S OWN ROWS (no interpretation);
then *what the user is asked to rule* — NOTHING here; the method surface follows from the writing
side — and *what this file does not do* (it recommends nothing; it establishes nothing; the
verdicts are yours to re-grade at the quoted texts). **Neutral framing throughout.**

**(f) Run the FULL guard set. A2, A4, A5 must hold.** Commit the reading file (plus any guard
artifact the run rewrote), subject:
`the comparison tabulated: the blind derivation's statements graded at the oracle and the current text in the closed vocabulary — NO recommendation on the method; no session booted`.
Push; verify at the object.

**E1:** the reading file whole in the ruled shape; every statement carrying exactly one verdict
per axis in the closed vocabulary or UNGRADED with what was read; the oracle quoted beside every
graded row; A5 byte-unchanged set intact; no recommendation anywhere.

## Task 2 — the close

1. One `STATUS.md` pointer entry per task that did work (no verdict distribution restated — the
   pointer names the reading file); the previous batch's entries move through
   `gen_status_batch_bound.py --apply` (D-648, licensed in terms);
   `gen_session_start_read_size.py` regenerated (A2).
2. The FULL close appended to `cowork_away_returns.md` as a **THE COMPARISON TABULATED, THE
   METHOD UNRULED** section — both guard states, every SHA, E0–E2 graded, A3–A5 graded, declared
   departures, quarantined questions (or none new), the plan lines (**the METHOD RULING surface,
   written by the writing side over the tabulation — the next act; then the sizing pilot if the
   method is ruled usable; the ledger before the framework phase**), and **the plan's tell in one
   sentence: did this batch produce anything other than the landed record, the reading file and
   the report? If yes, name it.** The report is `cc_report_comparison_harmony_boundary.md` —
   which likewise carries NO method recommendation; where the report would name hashes it cannot
   contain, point at the git log (no backfill commits — the declined resolution stays declined).
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **The ordered
   structure yields FOUR commits: Task 0, Task 1, the close, the end state.**

**E2:** at the tree carrying the close, a fresh full guard run — every run check passing except
[[OI-372]]'s tool, zero STOPs, population at run 75 — committed only after the run that produced
it.

## What this batch does NOT do

- **No method recommendation, anywhere. No verdict on the blind session's independence. No
  session booted. No measurement built or run. No pilot act beyond the comparison the rulings
  name.**
- **No edit to the blind output, the pack, the brief, any governing document, any register
  source.** The reading file and the report are the whole authored write set.
- **No finding number.** [[OI-179]] stays OPEN and GATES; [[OI-372]], [[OI-374]] stand.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule
  (c). No `src/` change, no golden, no test, nothing under `tools/corpus/` or
  `tools/robust_stop/`.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #19 — the tabulation is authored evidence for the user's ruling and
   establishes nothing itself; every verdict re-gradeable at quoted texts; #6 — the vocabulary
   has one home (Ruling 3, quoted in terms) and the oracle is located by its own text; #12 — the
   blind output is graded, never edited; UNGRADED and SILENT are kept beside MATCHES; the open
   questions are carried, not dropped; #13 — the first-read-before-boot departure is ruled and
   stated rather than smuggled; #17(f)/D-431 — counts live at the reading file's own rows; #24 —
   the two axes are kept apart so no comparison between measured quantities is implied. Conforms.
2. *Conventions:* American English; no self-invented labels — the verdict vocabulary is the
   ruling's own; music-theory words in their musical sense; towards, never against.
3. *Figures and premises:* the tip, guard summary, membership count, and the blind output's blob,
   size and hash all read by the writing side at the objects on 2026-08-24 and ordered
   re-checked; the 26/10 counts explicitly relayed-not-verified and ordered re-counted.
4. *File-tools rule:* the writing side's own departures are in the fiftieth handover entry.
5. *Uncertainty:* the one measure-and-report item is A3's possible reaction to the new
   ratification-surface file, declared as such.
6. *Re-read from disk before release:* performed over the delivered file, hash-verified after
   re-staging (F74), recorded in the fiftieth handover entry.

---

*Provenance: Cowork, 2026-08-24, the forty-fifth session, written at branch tip `2eeacf6b7a`
under Rulings 1–4 and §5 of `cowork_rulings_2026_08_24_comparison_design_sitting.md`. TOWARDS
the ultimate objective and TOWARDS the guiding principles.*
