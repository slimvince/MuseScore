# CC dispatch — land the withheld-family sitting; CORRECT the authored table to the ruled lists (D-569 IN; D-457, D-526 OUT; the withheld passage WIDENED); re-render the pack; correct the reading file; boot NO session

> **Dispatch (Cowork, 2026-08-22, the forty-fifth Cowork session).** Executes Rulings 3 and 5 of
> `cowork_rulings_2026_08_22_withheld_family_sitting.md` and records Rulings 1, 2 and 4 of the same
> sitting as applied. **Written at a verified stop: the pilot-preparation batch returned complete
> (`c42f8141f4` → `a12cc03503` → `a223fe8358` → `0dcbacce2d`), was verified at the objects by the
> writing side, and nothing is running.** The tip was read by the writing side with `git show -s`
> and `git for-each-ref` at the explicit hash `0dcbacce2d` and at nothing else.
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** Task 0 lands the sitting record, the modified
> handoff and this dispatch, and PUSHES. Task 1 corrects the AUTHORED table of the boot-pack
> generator to the five rulings, re-renders the pack, and corrects the reading file to the ruled
> lists — one commit. Task 2 closes. **THIS BATCH BOOTS NO SESSION FROM THE PACK, DERIVES NOTHING,
> AND OPENS NO PILOT ACT.** When it lands, the pack at
> `tools/audit/derivation_boot_pack/harmony-boundary/` is the one the blind session boots from;
> that session is a FRESH COWORK SESSION (Ruling 1 of
> `cowork_rulings_2026_08_22_deriving_side_sitting.md`), not you.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The pilot* — the phase that proves the
> derivation method before it is trusted. *A deriving session* — an implementation-blind session
> that writes what the analysis SHOULD do for one unit without reading what the code or the
> specifications say it DOES. *The boot pack* — the rendered, self-contained directory such a
> session reads at boot. *The withheld family* — the register identities, one document and one
> passage withheld from the pack for one subject; an AUTHORED input the user rules. *The authored
> table* — the `WITHHELD` and verdict tables inside `tools/audit/gen_derivation_boot_pack.py`,
> inputs to the generator and corrected by hand under a ruling record. *The reading file* —
> `ratification_surfaces/cowork_withheld_family_harmony_boundary_reading.md`, written by Claude Code
> for the user, not generated.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional. **(3)** `BUILD_AND_TEST.md` — CONDITIONAL, and this batch MEETS
> the condition (it runs the guard set and a generator): read it in full. **(4) DO NOT READ THE
> WHOLE OPEN-ITEMS INDEX.** Rule (a) reads `tools/audit/nongating_apparatus_rows.json` →
> `★_the_live_gating_answer` → `gating_ids`.
>
> **★ AND THE STANDING CLAUSE BINDS THIS ONE: read `cowork_handoff.md`, ITS CURRENT (FORTY-FIFTH)
> BLOCK.** The findings series stands at F88 and this batch allocates NO finding number.
>
> **★ AND READ THE STANDING CLAUSES AT THE FILE, NOT AT A COUNT.** `cowork_audit_protocol.md`'s
> dispatch-protocol section IN FULL before Task 1 writes anything.
>
> **Then read, in full and in this order:** `cowork_rulings_2026_08_22_withheld_family_sitting.md`
> (this batch's authority — all seven sections); `cowork_rulings_2026_08_22_member_two_leak_sitting.md`
> (the passage ruling that Ruling 5 widens); `cc_report_pilot_preparation.md` (your predecessor's
> report, whole); the reading file at the tip, whole; the module docstring and the `AUTHORED`
> tables of `tools/audit/gen_derivation_boot_pack.py` (read the whole file — you edit it).
>
> **Bind at these artifacts, never at a recollection of them:** `tools/audit/derivation_boot_pack.json`
> (the manifest; rewritten by the generator); `tools/audit/decisions/backbone_decisions.json`
> (**read and never written**); `tools/audit/rulings_sort_classification.json` (**read and never
> regenerated**); `tools/audit/evidence_pin_membership.json` (Task 0's subject);
> `tools/audit/guard_classification.json`, `tools/audit/guard_state.json`,
> `tools/audit/epoch_write_path.json`, `tools/audit/recognizer_establishment_sort.json` (A4's
> subjects).
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS built,
> designed, scoped or run, no design, no repair, **no derivation of any specification statement, no
> held-out derivation, NO SESSION BOOTED FROM THE PACK**, no document archived, moved or deleted AS
> A FILE, **no open-items row created, flipped or discarded**, **no edit to `ARCHITECTURE.md`,
> `CLAUDE.md`, `DEFECT_TYPES.md`, `cowork_design_doc_template.md`, `cowork_audit_protocol.md`, the
> phase-definition surface, `DECISIONS.md` or any register source**, **no change to the generator
> OTHER THAN its authored tables** (no new mechanism, no change to the criterion, the leak check,
> the marking or the STOPs — Rulings 4 and 5 decline exactly those), **no re-grading of any verdict
> the sitting did not move** (the fifteen IN and fifty-seven OUT stand at their recorded finding,
> date and reason, byte for byte).
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end.
> Verify every commit at the object by explicit hash. Every shell command carries
> `; echo "exit:$?"`. `git status` and `git diff` over the working tree time out on this mount and
> are denied by the armed guard — use the per-path explicit-hash forms named in A1. Resolve every
> commit hash at the object before writing it anywhere. The reserved-word and vocabulary rules bind
> every line this batch writes** (music-theory words in their musical sense; *measurement tool*,
> never the reserved word; *the decisions register* in full; TOWARDS, never *against*).

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 3 of the withheld-family sitting:** *"D-569 moves to IN: its leak is the oracle's own
  vocabulary, and the comparison could not distinguish recovery from recall. D-457 and D-526 move
  to OUT, with this session's readings above as the 'what it bears on instead'."*
- **Ruling 5 of the same sitting:** *"The withheld passage for this subject is widened to the
  whole founding-instance clause: inside the same bullet (scope anchor `**NEVER WORK FROM MEMORY
  INSTEAD OF DOCUMENTED FACTS`), the span opening '**Founding instance:** on 2026-07-28 Cowork
  reasoned' and closing 'which is the general case, not the exception.' … The widened span CONTAINS
  the span ruled at the member-two-leak sitting; the authored table carries ONE passage with the
  widened anchors, replacing the narrower anchors, and the finding names both rulings."*
- **Rulings 1, 2 and 4 of the same sitting:** List One, List Two and the leak check stand as
  authored — *no edit follows from them*.
- **Ruling 1 of the pilot-order sitting (standing):** *"the user rules the family before any
  session boots from it."* The family is ruled by this record; the session that boots is not you.

## Premise ledger

- **FACT** — the branch tip is `0dcbacce2d` (parent `a223fe8358`), subject opening *"end state:
  the guard set run at the tree the close left — every check passing except OI-372's tool, ZERO
  STOPs, the guard population moved by exactly the one tool this batch added"*; both refs at it.
  Read by the writing side at the explicit hash on 2026-08-22. **Nothing is unpushed.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 75, passing 74, failing 1
  (`tools/audit/gen_filing_convention_application.py --check`, [[OI-372]]'s tool), not_run 4,
  historical 16. Read at the object by the writing side; **read it again yourself at
  `git show 0dcbacce2d:tools/audit/guard_state.json`.**
- **FACT** — at the tip, `derivation_boot_pack.json` → `subjects.harmony-boundary.counted` reads
  `design_intent_class 244, candidates 75, verdicts IN 15 / OUT 57 / UNPLACED 3,
  withheld_identities_authored 15, withheld_identities_derived 17, withheld_identities_total 32,
  withheld_documents 1, withheld_passages 1, leaks 3, design_intent_entries_rendered 209,
  defect_type_rows_rendered 25, files_in_the_pack 7`. Read at the object by the writing side.
  **Re-read it yourself; these are the BEFORE figures A5 is measured against.**
- **FACT** — at `git show 0dcbacce2d:tools/audit/gen_derivation_boot_pack.py`, the authored
  `withheld_passages` list carries ONE entry with `opens: "the specification states it explicitly
  and twice"` and `closes: "ranked the STRONGEST evidence)"`; the verdict table carries `D-457`,
  `D-526`, `D-569` as `VERDICT_UNPLACED`; `withhold_passage()` cuts ONE span per passage between
  its anchors within the scope bullet and replaces it with the marker.
- **FACT** — at `git show 0dcbacce2d:CLAUDE.md`, the text `**Founding instance:** on 2026-07-28
  Cowork reasoned` occurs exactly once (line 1486) and `which is the general case, not the
  exception.` occurs exactly once (line 1493), both inside the bullet opening at line 1475 with
  `- **NEVER WORK FROM MEMORY INSTEAD OF DOCUMENTED FACTS`. **Locate by text; the line numbers
  are for your corroboration only and are never written into the tool.**
- **FACT, measured by the writing side:** `cowork_rulings_2026_08_22_withheld_family_sitting.md`
  is on disk and absent from the tip; it does not carry the word `pinned` on any line (Grep by the
  writing side over the container copy).

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications: **(i)**
  `cowork_handoff.md` — ONE block (the forty-fifth) inserted above the committed content, the
  forty-fourth heading marked superseded-as-entry-point, otherwise additions only. **A
  modification at any OTHER tracked path is a STOP-and-report.** Untracked and to be landed by
  Task 0: `cowork_rulings_2026_08_22_withheld_family_sitting.md` and this dispatch
  (`cc_instruction_withheld_family_correction.md`) — **two untracked paths; a named path the tree
  does not carry is a STOP-and-report.** The rest of the untracked population is NOT landed and is
  outside A1's STOP. **Check ordered (Task 0, first act):** per named tracked path
  `git diff --stat 0dcbacce2d -- <path>; echo "exit:$?"`; per named untracked path
  `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`; and the whole tracked
  population enumerated with the sanctioned enumeration tool as your predecessor did.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  The reds this batch causes and must clear: **the membership check, cleared by Task 0 step 2**;
  **the boot-pack generator's own `--check`, red between the table edit and the re-render, inside
  the SAME commit**; **`gen_session_start_read_size.py --check` at the close** (Task 2 moves
  `STATUS.md` entries, cleared by regenerating). Any other red is classed by its measured cause,
  and one outside this batch's own acts' subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration, from EVERY route.** **Route A** — the
  ruling-record count rises by exactly ONE (49 → 50) and ONE name is added
  (`cowork_rulings_2026_08_22_withheld_family_sitting.md`). **Route B** — the record carries the
  word `pinned` on no line, so route B adds NOTHING. **Route C** — no tool is added, so the
  tool-written set is unchanged. Measure the diff against the committed blob at `0dcbacce2d`
  BEFORE accepting it; movement beyond these is a STOP-and-report.

- **ASSUMPTION A4 — the guard registry.** This batch ADDS NO TOOL. `guard_state.json`: the
  population UNCHANGED at run 75; the failing set unchanged at [[OI-372]]'s tool.
  `guard_classification.json`, `epoch_write_path.json`, `recognizer_establishment_sort.json`:
  **UNCHANGED except** for any run-stamp field those artifacts carry. A member entering or leaving
  any of them is a STOP-and-report.

- **ASSUMPTION A5 — the manifest after Task 1, from EVERY field of `counted`.**
  `design_intent_class` 244 unchanged; `candidates` 75 unchanged; `verdicts` **IN 16, OUT 59,
  UNPLACED 0**; `withheld_identities_authored` **16**; `withheld_identities_derived` — PREDICTED
  to move by the derived cross-references D-569 adds (one pass, not transitive): **measure, report
  the figure at the artifact, and class any change as the derivation's own**; `withheld_documents`
  1; `withheld_passages` **1** (widened, not two); `leaks` 3 unchanged;
  `design_intent_entries_rendered` = 244 − `withheld_identities_total` − 3, **computed at the
  artifact, not carried**; `files_in_the_pack` 7. **The rendered member (2) carries the marker
  exactly once and carries NEITHER anchor of the widened span NOR the phrase `documented decision
  the implementation contradicts`. The rendered member (5) carries no `## D-569` entry and DOES
  carry `## D-457` and `## D-526`.** Any movement outside this is a STOP-and-report.

## The declared start state — at the tree you will meet

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — caused
   by this dispatch's own input, the ONE untracked root-level `cowork_rulings_*.md` record.

**`gen_guard_state.py --check` will open "STALE vs the run" for the same cause (F83); it is not a
third failing check.** A THIRD failing verdict is a STOP-and-report.

## Findings in this batch

**No finding number is allocated; the series stands at F88.** Ruling 9's routing (2026-08-21)
binds as in the previous dispatch: a finding bearing on the analysis is SURFACED under
"Quarantined audit questions"; an apparatus finding gets its row under rule (c); a finding bearing
on neither is DISCARDED with finding, date and reason. **An anchor of Ruling 5 that does not
resolve exactly once is a STOP-and-report of this batch, not a finding — report the count the
tool reports and stop.**

## Task 0 — land the sitting record, clear the membership check in the same act, and PUSH

1. **A1's check first, entirely at content-addressed objects.**
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE its difference against the
   committed blob BEFORE accepting it**, against A3 routes A and B:
   `python tools/audit/gen_evidence_pin_membership.py; echo "exit:$?"` then
   `git diff --stat 0dcbacce2d -- tools/audit/evidence_pin_membership.json; echo "exit:$?"`, the
   changed passages written to an absolute scratch path outside the repository and read with the
   file tools.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_22_withheld_family_sitting.md`, and
   `cc_instruction_withheld_family_correction.md` (new) — **four paths.** Subject, exactly:
   `record: the 2026-08-22 withheld-family rulings landed; evidence-pin membership regenerated`.
   Staged by explicit path, never by `git add -A` or `git add .`.
4. **PUSH**; verify at the object that `origin/master` names your Task 0 commit.
5. **Re-run `gen_evidence_pin_membership.py --check`: it must PASS.**

**Registered expectation E0:** exactly the four named paths; `ruling_records_read` 50 at the
committed artifact; the membership check passing; `origin/master` at the Task 0 commit.

## Task 1 — correct the authored table, re-render, correct the reading file — ONE commit

**(a) The authored table of `tools/audit/gen_derivation_boot_pack.py` — three edits and no other
line of the file:**

1. **`D-569`** → `VERDICT_IN`, in the shape the other IN rows use, with finding
   (*"Ruling 3 of `cowork_rulings_2026_08_22_withheld_family_sitting.md`, ruled by the user over an
   UNPLACED verdict: its verbatim supplies the vocabulary of the withheld passage — 'every
   sounding note in a region', 'eligible for harmonic analysis', onset and offset as the note's
   own edges — without stating the rule."*), date `2026-08-22`, and reason (*"A deriving session
   that read it would have the oracle's own terms, and the comparison could not distinguish
   recovery from recall."*). **If the IN rows live in a separate structure from the verdict rows
   (the `identities` table of `WITHHELD`), add D-569 there too in the same shape as the other
   fifteen — the generator STOPs if an IN verdict and the withheld identities disagree, and that
   STOP is the check that you did it.**
2. **`D-457`** → `VERDICT_OUT`, what it bears on instead: *"The grouping layer's marking of a group
   or key area truncated by the selection edge; it discloses that the slicer distinguishes an
   artificial clip boundary from a musical one — the existence of a distinction, not its rule
   (Ruling 3 of the withheld-family sitting)."* **`D-526`** → `VERDICT_OUT`, what it bears on
   instead: *"The chord axis's representation as a scale degree; it names a segmenter prior at the
   head of a gap in order to say it dissolves — the existence of a prior, not a rule (Ruling 3 of
   the withheld-family sitting)."*
3. **The ONE entry of `withheld_passages`** keeps `file`, `member` and `scope_anchor`, and its
   anchors become `opens: "**Founding instance:** on 2026-07-28 Cowork reasoned"` and
   `closes: "which is the general case, not the exception."`; its `finding` names BOTH rulings
   (*"Ruling 1 of `cowork_rulings_2026_08_22_member_two_leak_sitting.md`, widened by Ruling 5 of
   `cowork_rulings_2026_08_22_withheld_family_sitting.md`: the founding-instance clause of the
   never-work-from-memory rule states both halves of the held-out test's withheld answer for this
   subject, and its remainder tells a deriving session that a documented decision on the subject
   exists and is contradicted by the implementation."*); `date` `2026-08-22`; `reason` (*"The
   narrower cut left a residue that discloses the existence of the ruled answer and the
   implementation's disagreement with it; one authored passage with wider anchors removes the
   clause whole, and the statement of the rule itself, above it, stays in the pack."*).
   **Do NOT add a second passage. Do NOT change `withhold_passage()`, the marker, the criterion,
   the leak check or any STOP.**

**(b) Run the generator; run `--check`.** The generator's own STOPs are the check that the table
is consistent: an IN verdict without its identity, an UNPLACED row left behind, or an anchor not
found exactly once halts it — **a halt is a STOP-and-report, not a thing to fix around.**

**(c) A5, measured at the manifest and at the pack files, every item.**

**(d) Correct the reading file** `ratification_surfaces/cowork_withheld_family_harmony_boundary_reading.md`
**to the ruled lists, and no further:** its status banner becomes *RULED 2026-08-22 — applied by
this commit; the lists below are the ruled family* and names the sitting record; LIST ONE gains the
D-569 row (its reason as in (a)1) — sixteen rows; LIST TWO gains D-457 and D-526 (their "bears on
instead" as in (a)2) — fifty-nine rows; LIST THREE keeps its heading and says that the three were
ruled (D-569 IN, D-457 and D-526 OUT) and the list is now empty; LIST FOUR states Ruling 4 (the
check stands); LIST FIVE states the widened anchors and that the widened span contains the earlier
one; §4's sentence *"Two of the entries below are exactly that"* is corrected to the figure you
count at the tables after also annotating D-224 (`onset` inside `onsetAtRegionStart`) and D-313
(`boundary` inside `combinedBoundary`) as in-word matches in their LIST TWO rows (§6(a) of the
sitting record); "What you are asked to rule" becomes "What was ruled", one line per list with the
user's letter. **Nothing else in the file changes; no verdict text of the seventy-two unmoved
entries is edited.**

**(e) Run the FULL guard set. A2 and A4 must hold.** Commit the generator, the manifest, the
rendered directory, the reading file, and the guard artifacts the run rewrote, with the subject
`the withheld family CORRECTED to the ruled lists — D-569 withheld, D-457 and D-526 admitted, the withheld passage widened to the founding-instance clause; the pack re-rendered; the reading file ruled; no session booted`.
Push; verify at the object.

**Registered expectation E1:** the authored table carries exactly the three edits; the manifest's
`counted` at A5; `--check` green; member (2) carries the marker once and neither anchor; member (5)
without D-569 and with D-457 and D-526; the reading file at the ruled lists; **`CLAUDE.md`,
`ARCHITECTURE.md`, `DEFECT_TYPES.md`, `cowork_design_doc_template.md`, `cowork_audit_protocol.md`,
the phase-definition surface, `DECISIONS.md` and every register source byte-unchanged**; the
seventy-two unmoved verdicts byte-unchanged at the manifest (compare each row's `finding`, `date`,
`reason` against `git show 0dcbacce2d:tools/audit/derivation_boot_pack.json`); no session booted.

## Task 2 — the close

1. One `STATUS.md` pointer entry per task that did work; the previous batch's entries move
   verbatim to `STATUS_ARCHIVE.md` through `gen_status_batch_bound.py --apply` at its three
   declared authored inputs (authored-input maintenance, D-648 — licensed here in terms);
   `gen_session_start_read_size.py` regenerated (A2).
2. The FULL close appended to `cowork_away_returns.md` as a **THE WITHHELD FAMILY RULED AND THE
   PACK CORRECTED** section — both guard states, every SHA, E0–E2 graded, A3–A5 graded at the
   artifacts, the declared departures, the quarantined questions (or none new), the plan lines
   (**the blind derivation by a FRESH COWORK SESSION, now unblocked**; the comparison after it; the
   sizing pilot after the method is ruled; the ledger before the framework phase), and **the plan's
   tell in one sentence: did this batch produce anything other than the three table edits, the
   re-rendered pack and manifest, the corrected reading file and the report? If yes, name it.** The
   report is `cc_report_withheld_family_correction.md`.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run.

**Registered expectation E2:** at the tree carrying the close, a fresh full guard run — **every
run check passing except [[OI-372]]'s tool, zero STOPs, the guard population at run 75** — run and
read, committed only after the run that produced them.

## What this batch does NOT do

- **No session booted. No derivation. No comparison. No oracle opened for the purpose of the test.**
- **No change to the generator's mechanism**; no re-grading of any verdict the sitting did not move.
- **No edit to any boot-list member at its home; no register entry, sort artifact or register
  source touched.**
- **No finding number allocated.** [[OI-179]] stays OPEN and GATES; [[OI-372]], [[OI-374]] stand.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule (c).**
- **No `src/` change, no golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #19 — the family is now RULED and the correction applies the ruled set,
   nothing more; the criterion's reach stays declared unmeasured; #6 — one passage with wider
   anchors rather than two overlapping ones, one home for the family; #12 — the three moved
   verdicts are re-written at their own rows with finding, date and reason, the seventy-two
   unmoved ones byte-unchanged, the report's own miscount left as a fact about the report;
   #17(f)/D-431 — every BEFORE figure cited at the artifact and ordered re-read, every AFTER figure
   predicted and ordered measured; #24 — no comparison asserted. Conforms.
2. *Conventions:* American English; no self-invented labels; music-theory words in their musical
   sense; towards, never against.
3. *Figures and premises:* every quantity named to its artifact; every ruling quoted at its
   record; the anchor counts read at `0dcbacce2d:CLAUDE.md` by the writing side and ordered
   re-located by the tool.
4. *File-tools rule:* the writing side's own departures are in the forty-fifth handover block.
5. *Uncertainty:* `withheld_identities_derived` after Task 1 is declared NOT predicted and ordered
   measured; no other figure is left open.
6. *Re-read from disk before release:* performed over the delivered file, hash-verified after
   re-staging (F74), recorded in the forty-fifth handover block.

---

*Provenance: Cowork, 2026-08-22, the forty-fifth session, written at branch tip `0dcbacce2d`
under Rulings 3 and 5 of `cowork_rulings_2026_08_22_withheld_family_sitting.md`. TOWARDS the
ultimate objective and TOWARDS the guiding principles.*
