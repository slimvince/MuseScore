# CC instruction — Phase 1j: the OI-272 homing half · the banner proposals · the transitive-authority verification · continued full reads

> **Read first (every session):** `C:\s\MS\CLAUDE.md` IN FULL — the register rules now carry the
> TRANSITIVE-AUTHORITY refinement of (g): a user-ratified surface explicitly delegating to a
> document carries the user's authority to it; an assistant's stamp alone never confers
> contract-home status. `C:\s\MS\DECISIONS.md` (405 entries, ALL user-ratified through eight
> events). `C:\s\MS\STATUS.md`. `C:\s\MS\OPEN_ITEMS.md` (INDEX) with **OI-272** (THIS dispatch
> delivers its homing half and flips it), **OI-207**, **OI-268/OI-275/OI-278** (ruled — context).
> `C:\s\MS\cowork_design_doc_template.md` (the writing standards BIND every specification
> sentence — predicates qualified, plain vocabulary, the bare word musical).
>
> **Current state:** ONE local commit ahead of origin (`0fde58786c`). **FIRST act: `git push
> origin master`; verify HEAD `0fde58786c`; mismatch = STOP.** Last act: push.
>
> **Hard stops:** origin only; no `src/` change; no golden/`tools/corpus/`/`tools/robust_stop/`
> movement; no fix, no design, no inference change. Specification edits are permitted in TWO
> classes only: (i) the pointer class (delegation + tried-and-closed one-liners), and (ii) THIS
> dispatch's licensed HOMING class — writing an already-ratified rule into its owning
> specification in that specification's own voice, with its defense, citing the ratifying event;
> NEVER resolving anything open by drafting (a tension is stated and pointed, not settled). NO
> status banner is edited (Task 2 PROPOSES; the user ratifies). New decisions from the reads are
> NEVER self-ratified. Anchor discipline: per-citation from the `--verify` drift report. A
> surprise is a STOP (#13). VS Code bash rules. Feasibility stops with measured partitions are
> accepted; work the tasks in order so each session boundary is a complete, guarded increment.

**Dispatch author:** Cowork, 2026-08-02.

## Task 1 — the OI-272 homing half (the edit class the 1i hard stop correctly refused)

Home the live constraints and process rules the ruling names, per the ratified per-kind scheme:
- **Live constraints → the owning specification.** The list from the 1i report: D-292 (the
  fitting-pool licence constraint — home per its own record: the census §8c statement is present;
  the owed act is the delegation pointer from the owning `ARCHITECTURE.md` section OR a homed
  restatement per OI-268's criteria — judge which per the fifth-home-case rules), D-293, D-294,
  D-295, D-296, D-297, D-303, D-304, D-305, D-306, D-309, D-310. For each: the rule in the
  specification's own voice + its defense + the ratifying event cited; register home updated
  (former home to provenance, #12); where the proper home is a contract document that meets the
  criteria, the pointer suffices (never a copy).
- **Process rules → `CLAUDE.md` / `cowork_audit_protocol.md`:** D-298, D-307, D-308, D-311 —
  same discipline (note `CLAUDE.md` insertions shift anchors; drift loop after).
- **Dead ends stay dead ends:** any shelving encountered gets at most a tried-and-closed pointer
  (the ruled scheme; D-317-class entries are LEGACY-marked and get nothing).
OI-272 flips RESOLVED when this task completes, with the homing inventory in the report.

## Task 2 — the banner proposals (drafted, never applied)

For the four unbannered documents (`docs/redesign_plan.md`, `docs/scoring_model.md`,
`docs/iteration_path1_summary.md`, `cowork_architecture_reassessment.md`): draft ONE status
banner each, derived strictly from the document's established standing in the register (what its
decisions' statuses collectively say the document IS — e.g. a live mandatory reference with
LEGACY-scoped mechanism content; an iteration-era record whose rulings are registered). Present
the four drafts in the report's ASK section for the user's ratification. NOTHING is edited.

## Task 3 — the transitive-authority verification (the 8+ affected entries)

For each document the 1i report listed as failing the ratification criterion with a Cowork/CC
banner (`docs/decoder_design.md`, `cowork_layer5_engagement_design.md`, and any sibling): search
the record for a USER-RATIFIED surface that explicitly delegates to it (the
`cowork_engage_arc_plan.md` → structural-audit §3/§4 delegation is the worked example, D-401's
observation). Where a genuine delegation exists: cite it, add the missing `ARCHITECTURE.md`
delegation pointer if absent (pointer class), and re-classify the document's entries
contract-home with the transitive citation in provenance. Where none exists: the document stays
non-contract-home and is listed in the report — the user can sign it off directly later if
wanted. Nothing is inferred; a delegation is explicit or it is absent.

## Task 4 — continued full reads (the remaining ≈69 LIVE-SPEC documents)

Highest unresolved-cluster count first (`cowork_progression_schema_dictionary.md` 19 ·
`docs/implementation_roadmap.md` 18 · the three at 17 …). Same discipline as every wave; the
RATIFICATION QUEUE; the flag duties (D-282…D-285; measurement tools fenced; OI-268-relevant
conduct evidence now applied via Task 3 rather than collected).

## Task 5 — close

Dispositions; the manifest; all guards at the final tree; rows for gaps/conflicts; dated notes
(OI-272 flip, OI-207 coverage); `STATUS.md` pointer at the TOP; commits per change-class; push.

## Report

Task 1: the homing inventory (entry → new home, file:line) and the OI-272 flip. Task 2: the four
banner drafts in an ASK section. Task 3: per-document delegation verdicts with citations; the
re-classification count; the no-delegation list. Task 4: documents read with yields + the queue;
the cumulative read-in-full count against 143. Guards; anomalies each diagnosed; the measured
remainder. Standing self-check before reporting.
