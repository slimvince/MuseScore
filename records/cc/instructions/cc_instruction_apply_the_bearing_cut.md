# CC dispatch — Rulings 65 and 66 applied: the finish line cut by bearing, the apparatus rows' obligation lapsed, and the eight red guards cleared as a consequence rather than as a target

> **Status: ACTIVE DISPATCH, written 2026-08-11 (Cowork), at a verified STOP** — the previous
> dispatch `cc_instruction_status_touch_and_oi141_premise_repin.md` completed, four commits,
> tip `5a463bfedb` verified on `origin/master`, no `src/` movement in any of them. Cowork
> verified its report at the objects: D-674 at the INDEX, Ruling 62's own text naming its home,
> D-137's actual subject, the guard summary, and the mandatory read re-attempted and still
> failing. Nothing is running.
>
> **Read IN FULL, and read FIRST, in this order:** `CLAUDE.md`, with particular attention to
> the open-items register's non-gating declaration and its rule (f);
> **`cowork_rulings_2026_08_11_fifteenth_stop.md`** (Rulings 65 and 66 — read WHOLE, D-643);
> `DECISIONS.md` (INDEX); `OPEN_ITEMS.md` (INDEX); `tools/audit/phase1_finish_line.json` and
> `tools/audit/guard_state.json` at HEAD; and `tools/audit/decisions/true_half_reach_rows.json`,
> which is the derivation Ruling 66's lapse records point at.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_apply_the_bearing_cut.md`. Acts dated
> from the clock; commit messages via `-F`; an identifier belonging to the open-items register
> or to the decisions register is verified at that register's own index immediately before it is
> written anywhere, and the same for the two ruling numbers.
>
> **★ All standing rules as adopted.** D-253 in every dialect and mechanism. NO TRANSCRIBED
> VALUES (D-431). Hold-don't-guess. **Read-only on the analysis: no behaviour change, no `src/`
> edit, no golden refreshed, no corpus of scores touched, nothing under `tools/corpus/` or
> `tools/robust_stop/` moved, no fix to inference, no design.** D-231 stands and phase 1 is
> open; #8's three-clause gate stands. **Phase 1's completion statement is not written, not
> drafted and not partially written by this dispatch.** Commit and push per task boundary;
> `origin` only, NEVER `upstream`.

## 0a. WHAT THIS DISPATCH IS FOR

Rulings 65 and 66 are taken and recorded. This applies them. **The eight red guards clear as a
CONSEQUENCE of the cut running, and clearing them is not the goal** — a cut arranged to make
guards green would be the defect the falsification test below exists against.

## 0b. THE RULING LEDGER

**Rulings 65 and 66 are carried in full at `cowork_rulings_2026_08_11_fifteenth_stop.md` and
are not restated here (#6).** Read that file whole before acting (D-643). The two clauses this
dispatch turns on, quoted because it applies them: **the cut is DERIVED and regenerated, never
hand-classified — a gating verdict comes from a cut and is never hand-added**; and **if the cut
moves into the apparatus class any item the record elsewhere calls inference-bearing, the cut is
wrong and halts.**

**D-670** places a task that cannot be stopped partway first with nothing large in front of it;
**D-672** permits a per-entry pass to stop at a member boundary with the three things it names
recorded. Both are quoted in full in the previous dispatch and are unchanged; this dispatch
applies the ordering they produce.

## 0c. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork at this STOP, each at the object named.** The previous
dispatch's four commits and their file lists, read as git objects by explicit hash. D-674 at the
INDEX, homed at the design-document template. Ruling 62's own text naming that home in terms.
D-137's actual subject, read at the INDEX, which is the harmony maps and intellectual-property
claims and not any mandatory read. The guard summary — forty run, thirty-two passing, eight
failing, the failing set opening with the apparatus-cut and finish-line generators. The
mandatory read of `STATUS.md`, re-attempted by Cowork after the archive pass and still refused.
Finish-line item 8's seventeen rows and the fact that its grading derivation has already run and
placed exactly one of the eighteen graded inside the doc-sync half.

**ASSUMPTION — each checked BEFORE the act resting on it; a refutation is a STOP written into
`cowork_away_returns.md`, and the task is not completed around it.**

- **A1.** The ruling numbers 65 and 66 are correct — the fourteenth stop carried 60–64. *Check:
  at the ruling records. If wrong, correct the record file and say so; do not propagate.*
- **A2.** No exercise reading exists for either ruling, which is why the entry is written
  directly rather than through the queue. *Check: search the register for an entry stating
  either rule. If one exists, STOP — the classification is then not obvious and the queue is the
  route.*
- **A3.** **The `owed` state may not need a canonical status token.** Gating is already a derived
  field rather than a token. *Check, before writing anything: establish at rule (f)'s home
  (`tools/audit/index_status_lint.py`) and at the one index parser whether a row's not-owed
  state must be carried by the leading token or may be a derived field of the cut. **If the
  record does not settle it, STOP.** Do not invent a token and do not assume one is
  unnecessary.*
- **A4.** The cut's carve-out for #19 is encoded mechanically, not remembered. *Check: an
  establishment obligation is shown surviving the cut by construction, demonstrated on a member,
  not asserted in prose.*
- **A5.** Each lapse record names the derivation that graded its row, and points at
  `tools/audit/decisions/true_half_reach_rows.json` where that is what graded it. *Check: no
  row's obligation lapses without a named grading; a row with none STOPS rather than lapsing by
  default.*
- **A6.** **OI-370 and OI-371 are NOT assumed apparatus.** OI-371's subject is a statement in the
  code about which decoder ships, and the non-gating declaration puts a correction to a statement
  about the analysis's build state on the GATING side. *Check: both rows go through the cut like
  any other and take whatever verdict it gives them. A verdict of GATES for either is a correct
  outcome, not a failure of this dispatch.*

## 0d. THE TASKS, IN ORDER

**Task 1 — the ruling record and its register entry (A1, A2). FIRST, with nothing large in front
of it.** It cannot be stopped partway: the decisions register's rule (c) puts the entry and its
regenerated files in the commit that records the ruling. Commit
`cowork_rulings_2026_08_11_fifteenth_stop.md` together with the entry, written through the
backbone data and the generator, never by hand-editing a rendered file (rule (d)). The home is
derived under rules (g)–(k) and **verified, not assumed** — these are rules about how a derived
scope is cut and what an open row is owed, so the audit protocol's dispatch-protocol block and
the open-items register's own section in `CLAUDE.md` are both candidates; **if two are equally
supported, STOP and report both rather than choosing.** Then
`gen_decisions_register.py --check` and `gen_cluster_dispositions.py --verify`, re-aiming any
drifted citation per citation. Commit and push.

**Task 2 — the cut built and run (A3, A4, A5, A6).** Change the generators so the finish line's
gate is derived from D-438's test, with #19's carve-out encoded, and regenerate. **Preserve the
pre-cut population beside the post-cut one (#12)** — the movement must be visible rather than
inferred. Report the movement BOTH WAYS: which items and rows changed class, and, recomputed
with the cut off, that nothing else moves. **Then run the falsification test and report it
explicitly:** for every item or row the cut places in the apparatus class, check whether the
record elsewhere calls it inference-bearing, and **halt if any does.** The eight failing guards
are expected to clear once the two ungraded rows have verdicts; **report what the guard set does
after the cut, and if a guard is still red, report why rather than adjusting anything to suit
it.**

**Task 3 — the lapse records (A5).** A per-row lapse record for each row whose obligation lapses,
naming the derivation that graded it. This is a per-entry pass and may stop at a member boundary
under D-672, recording the three things that rule names. **A row without a named grading does not
lapse.**

**Task 4 — the close.** One `STATUS.md` pointer entry per completed task — and nothing else in
that file, which remains unreadable and whose row OI-370 stays open. Append the close to
`cowork_away_returns.md`. Report at the objects: commits with hashes and push state, A3's finding
about the token question, the cut's movement both ways, the falsification test's outcome, the
guard set after the cut, and the lapse records written.

## 0e. WHAT IS DELIBERATELY NOT DONE

**§21's false D-137 cross-reference is NOT corrected here.** Under D-674 the kind call between
re-bannering and correcting the body is a hard case for a ratification surface that records a
classification as it was made, and Ruling 62 says a hard case STOPs to the user. It is reported
and left.

**The OI-141 sitting pack's §7 is NOT rewritten here.** It is a Cowork surface and Cowork wrote
the error; the rewrite is Cowork's act from the seven-decision re-pin in `cowork_away_returns.md`.

**Nothing joins the finish line, and nothing is closed that the cut does not place.** The size
guard stays proposed and untaken; the mechanism set is frozen except for the generator change
Ruling 65 licenses.

## 0f. STOP RULES

Write a STOP into `cowork_away_returns.md`, halt, and do not work around it, if: any assumption
above is refuted at its check; the ruling numbers are wrong; an existing entry already states
either rule; the token question in A3 is not settled by the record; two homes are equally
supported for the entry; the falsification test finds an inference-bearing item placed in the
apparatus class; a row would lapse with no named grading; or a guard remains red for a reason
the cut does not explain. A capacity stop at a member or task boundary is NOT a STOP — it is
recorded under D-672.

---

*Provenance: Cowork, 2026-08-11, at the verified STOP following
`cc_instruction_status_touch_and_oi141_premise_repin.md`, written from all present knowledge
with nothing knowledge-generating running. The standing writing-side self-check was run against
this file's own text before release (D-434).*
