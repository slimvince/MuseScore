# CC dispatch — the ninth extension closed, the one consolidated `STATUS.md` touch, and OI-141's premise re-pin over all seven decisions

> **Status: ACTIVE DISPATCH, written 2026-08-11 (Cowork), at a verified STOP** — the previous
> dispatch `cc_instruction_row_landing_and_oi141_arm_check.md` completed (commits `f916ee7c56`
> and `f4339ab43b`, both verified on `origin/master`, no `src/` movement), and Cowork verified
> its report at the objects: every code citation re-read at the code, OI-353's resolved state
> read at the INDEX, and the OI-357 artifacts read at their own columns. Nothing is running.
>
> **Read IN FULL, and read FIRST, in this order:** `CLAUDE.md`; `DECISIONS.md` (INDEX);
> `OPEN_ITEMS.md` (INDEX); `tools/audit/phase1_finish_line.json` at HEAD;
> `ratification_surfaces/cowork_ruling_registration_queue_2026_08_09.md` **§21 and §20**;
> `cowork_rulings_2026_08_11_fourteenth_stop.md` (Rulings 60–64, each read WHOLE, D-643);
> `cowork_scratch_2026_08_11/cowork_oi141_sitting_pack.md` **including its new §7**;
> `cowork_key_layer_design_opening.md`; and `cc_instruction_doc_split.md`, which is the
> precedent act Task 2's archive pass follows.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_status_touch_and_oi141_premise_repin.md`.
> Acts dated from the clock; commit messages via `-F`; an identifier belonging to the
> open-items register or to the decisions register is verified at that register's own index
> immediately before it is written anywhere.
>
> **★ All standing rules as adopted.** D-253 in every dialect and mechanism. NO TRANSCRIBED
> VALUES (D-431). Hold-don't-guess. **Read-only on the analysis: no behaviour change, no
> `src/` edit, no golden refreshed, no corpus of scores touched, nothing under
> `tools/corpus/` or `tools/robust_stop/` moved, no fix to inference, no design.** D-231
> stands and phase 1 is open; #8's three-clause gate stands. **Phase 1's completion statement
> is not written, not drafted and not partially written by this dispatch.** Commit and push
> per task boundary; `origin` only, NEVER `upstream`.

## 0a. THE USER'S RULINGS THIS DISPATCH EXECUTES, taken 2026-08-11 in conversation

Five verdicts, recorded here because this dispatch is what applies them and a dispatch that
applies a ruling must carry it.

1. **Queue §21, Ruling 62 — DECISION.** The filing convention gets its register entry. The
   downgrade reading was put and declined; the ground the user was given, and did not
   contradict, is that the two open rows this ruling closes each recorded the composition as
   ambiguous, which is evidence the principles did not decide it.
2. **Queue §21, Ruling 61 — EXERCISE.** No entry. The contrary reading was put and declined.
3. **Queue §21, Rulings 60, 63 and 64 — ratified as proposed**, no contrary reading offered
   for any of them and none found.
4. **The `STATUS.md` archive act JOINS THE FINISH LINE, BOUNDED** — it rides ONE dispatch as
   a per-entry pass under D-672, and **nothing else joins the finish line by this ruling.**
5. **OI-141's repair is a PREMISE RE-PIN OVER ALL SEVEN DECISIONS, not a mechanism report**,
   and it is in scope on the same bounded footing.
6. **OI-357's shared-surface stand-in stays BOUNDED, not closed** — confirmed deliberate. No
   act follows; see §0e for why no register entry is written for it.

## 0b. THE RULING LEDGER — quoted in full, never by the branch that supports the claim (D-643)

**D-670, `cowork_audit_protocol.md`, "A task that CANNOT BE STOPPED PARTWAY is dispatched
FIRST, with nothing large in front of it — and the ordering is RULED, never left to a
preference" — ruled by the user, 2026-08-11** (`cowork_rulings_2026_08_11_eleventh_stop.md`,
Ruling 52, taking Ruling 51 of `cowork_rulings_2026_08_11_tenth_stop.md`). The two rules that
collide: one kind of task must be published over its WHOLE derived population or not at all,
because a derivation covering some of its population reads as covering the class, which is the
cap nobody sees; the other kind, a per-entry pass, may be stopped at any member boundary,
because each completed member is whole in itself — **so the second kind can always absorb
whatever capacity is left and the first kind never can.** **THE RULE.** Where a dispatch
carries a task that cannot be stopped partway, **that task is placed FIRST, with nothing large
in front of it**, and the placement is recorded as a RULING rather than as the dispatch-writer's
preference. *Why a ruling rather than a preference:* a preference does not survive one more
capacity squeeze; such a task is small in COUNT and large in READING, so every honest estimate
of it looks cheap while every attempt at it loses to work that can stop at a boundary — and
each individual refusal is CORRECT on its own terms, which is what makes the pattern invisible
from inside any one dispatch. *The evidence, measured rather than argued:* one such task was
declined by seven consecutive dispatches, every refusal right on its own terms and for the same
reason each time; dispatched first under this rule it closed WHOLE in one act, and turned up a
finding nobody was looking for while deriving its own population. The ordering was the whole
difference, and nothing about the task had changed.

**D-672, `cowork_audit_protocol.md`, "A PER-ENTRY PASS may be stopped at any member boundary,
and the stop is RECORDED — what was done, what was not, and that the remainder is untouched
rather than partly worked" — ruled by the user, 2026-08-11**
(`cowork_rulings_2026_08_11_twelfth_stop.md`, Ruling 55). **THE RULE.** A task whose
deliverable is a per-entry, per-row or per-site PASS may be stopped at any **member boundary** —
on capacity, or because a later task binds harder — and the stop is not a failure. **Three
things are recorded when it happens:** which members were completed, that the remainder is
UNTOUCHED rather than partly worked, and that nothing is left half-edited. A stop recorded that
way is a result; a stop that is silent is the defect the ordering rule names. *Why the
allowance is safe here and nowhere else:* each completed member is **whole in itself** — a
homed entry, a corrected row, a re-aimed anchor each stands alone and is not made wrong by the
next one never happening, so a stopped pass publishes nothing that reads as more than it is.
That is exactly the property a derivation over a population does not have, which is why the two
rules are different rules rather than one. *Why the recording clause is the load-bearing half:*
an unrecorded stop turns a per-entry pass into the very thing the ordering rule forbids — a
reader meets a list of completed members and cannot tell a finished pass from an interrupted
one. **The remainder is therefore derived fresh by the continuing session and never carried
from the stopping session's account of it.** **What it does NOT license:** leaving a member
half-edited; a stop inside a task the ordering rule governs; and a stop presented as completion.

**The open-items register's rules (c), (d) and (f), `CLAUDE.md`.** (c) *"every newly discovered
issue gets an **index row AND its detail file** **in the same commit** that records the
discovery"*; (d) *"every resolution **flips the INDEX row** with provenance (the detail file
gains a dated resolution note, never a status of its own)"*; (f) *"A row's STATE is carried by
the first token of its status cell"*.

**The decisions register's rule (c), `CLAUDE.md`:** *"a new ratification, shelving or
falsification gets its register entry (data + regenerated files) IN the commit that records
it"*; and rule (d), that the register is a GENERATED surface — change the backbone data and
regenerate, never hand-edit the rendered files.

## 0c. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork at this STOP, each at the object named.** The previous
dispatch's two commits, their file lists and their presence on `origin/master`, read as git
objects by explicit hash. OI-370 and OI-371 at the INDEX, both opening with the canonical
token. OI-353's `✅ RESOLVED` state at the INDEX, and its row's own clause recording that ten
blocks were HELD pending a call-graph answer. `jointdecoder.h`'s dormancy comment,
`composingconfiguration.cpp`'s default of `true`, `jointdecoder.cpp`'s per-segment key prune
and its cadence-approach window, and `jointfactadapter.cpp`'s single signature read — each
read at the code. The OI-357 production-arm and legacy-control artifacts at their own columns.
`STATUS.md`'s own banner text stating the archive remedy.

**ASSUMPTION — each checked BEFORE the act resting on it; a refutation is a STOP written into
`cowork_away_returns.md`, and the task is not completed around it.**

- **A1.** §21 proposes NO home for Ruling 62's entry, exactly as §19 proposed none — the
  departure §1.18 already recorded. The home is therefore DERIVED, and the derivation is
  stated rather than assumed: Ruling 62's convention was already written into
  `cowork_design_doc_template.md` beside the kind list by
  `cc_instruction_return_continuation_14.md` Task 0, so the entry's home is that section, and
  the entry points at text that already exists rather than creating a second statement of it
  (#6). *Check: read that section and confirm the convention stands there. If it does not, or
  if a second candidate home is equally supported, STOP — do not choose.* **Report the
  departure from Ruling 60's clause explicitly, as §1.18 did.**
- **A2.** The archive pass moves entries by a DERIVED rule and never by judgment. The rule:
  an entry moves when it is a per-task or per-batch POINTER entry whose dispatch has completed
  and whose close is recorded in `cowork_away_returns.md`, or when its own text already marks
  it superseded or historical. **An entry the rule does not decide STAYS WHERE IT IS.** *Check:
  the moved set is enumerated with, per entry, which clause of the rule moved it.*
- **A3.** The pass follows `cc_instruction_doc_split.md`'s pattern for how a moved entry is
  written into `STATUS_ARCHIVE.md`. *Check: where that precedent does not decide a question,
  STOP rather than invent a convention.*
- **A4.** Nothing is deleted, and the movement is lossless (#12). *Check: every moved entry is
  byte-identical in the archive to what left the live file, verified mechanically, not by eye.*
- **A5.** OI-370 flips ONLY on evidence. After the pass, attempt the mandatory session-start
  read of `STATUS.md`. **Flip the row only if the read succeeds**; if it does not, record what
  moved, leave the row OPEN, and say so. *A flip on the assumption that enough moved is exactly
  the assertion #19 forbids.*
- **A6.** Task 3 reports, per decision, what the shipping arm's corresponding mechanism IS and
  whether the decision has an object — and **refuses to rule an analogous-but-different
  mechanism same-or-different.** The prune is the live example: the shipping arm HAS a prune,
  and it is not the pinned one. *Check: every one of the seven carries either a code citation
  or an explicit "no object on the shipping arm", and any analogy is REPORTED as an analogy.
  Ruling it is a design judgment inside a fact-gathering act, which #8 forbids — STOP instead.*

## 0d. THE TASKS, IN ORDER — and the order is D-670's, not a preference

**Task 1 — the ninth extension closed and Ruling 62's entry written (A1). FIRST, with nothing
large in front of it.** It cannot be stopped partway: the decisions register's rule (c) puts
the entry and its regenerated files in one commit, and a closing state that records four of
five verdicts is worse than none. Write the queue's **§22 closing state** over Rulings 60–64 in
the shape §6, §8, §10, §12, §14, §16, §18 and §20 use, recording each verdict as the user
ruled it and recording that the 62 downgrade reading and the 61 contrary reading were both put
and declined. Write **one** register entry, for Ruling 62 only, through the backbone data and
the generator — never by hand-editing a rendered file (rule (d)). Then run
`gen_decisions_register.py --check` and `gen_cluster_dispositions.py --verify`, re-aiming any
drifted citation per citation. Commit and push.

**Task 2 — the ONE consolidated `STATUS.md` touch (A2, A3, A4, A5).** A per-entry pass under
D-672, in this internal order, because the order removes ambiguity rather than merely being
tidy:

1. Land the fourteenth continuation's `STATUS.md` entry, which is drafted complete.
2. Discharge **OI-47's banner half** — mark the four submission-era sections historical. This
   comes BEFORE the archive pass so those sections are unambiguous when the pass reaches them.
3. Append the correction note for the gating-row miscount recorded in `cowork_handoff.md`'s
   entry-point block. **A correction note, never a rewrite** — the commit message it corrects
   is immutable and stays as written (#12).
4. Run the archive pass under A2/A3/A4.
5. Attempt the mandatory read and treat OI-370 under A5.

Then the two open-items guards and the disposition verifier at the committed tree. **If
capacity runs out, stop at a member boundary and record the three things D-672 names.** Commit
and push.

**Task 3 — OI-141's premise re-pin over ALL SEVEN decisions (A6). READ-ONLY.** For each of the
seven decisions `cowork_key_layer_design_opening.md` carries, establish **at the shipping
code** what the corresponding mechanism is and whether the decision has an object. This is a
premise ledger, **not** a mechanism report: one question per decision, each answer a code
citation or an explicit absence. It is a per-entry pass and may stop at a decision boundary
under D-672 — **but the sitting is unblocked only when all seven are covered**, so a partial
pass names the members it covered and states that the sitting stays blocked. Nothing is
changed: no `src/` edit, no document corrected on the strength of the finding, no design
decision re-opened or proposed, and the sitting pack is left as written with its §7 standing.

**Task 4 — the close.** One `STATUS.md` pointer entry per completed task. Append the close to
`cowork_away_returns.md`, including any STOP and any recorded per-entry stop. Report at the
objects: commits with hashes and push state, guard runs and outcomes, the derived home for
Ruling 62's entry with the departure reported, the archive pass's moved set with the clause
that moved each, OI-370's evidence-based treatment, and Task 3's seven answers with their
citations.

## 0e. WHAT IS DELIBERATELY NOT DONE

**No register entry is written for the user's confirmation on OI-357's stand-in.** Applying
D-599: the artifacts already declare the bound in their own text, and the confirmation changes
nothing about the standing of the measurement — so it is an EXERCISE of #19 as already
recorded, not a new establishment verdict. **If the user reads it instead as a ratification,
rule (n) makes the decisions register its home and an entry is owed — that is surfaced here and
not decided.**

**The size guard stays PROPOSED and untaken.** The mechanism set is frozen and the condition it
would detect does not block this dispatch.

**Nothing else joins the finish line.** The user's ruling admits the archive act and nothing
more; an item discovered while working this dispatch is governed by D-641, not by that ruling.

## 0f. STOP RULES

Write a STOP into `cowork_away_returns.md`, halt at that point, and do not work around it, if:
any assumption above is refuted at its check; Ruling 62's home cannot be derived unambiguously
or has two equally supported candidates; the archive rule in A2 leaves a large share of the
live file undecided, which would mean the rule is wrong rather than the file unusual; the
precedent act does not decide how a moved entry is written; a guard fails at the committed tree
for a reason this dispatch's own edits do not explain; or Task 3 reaches an
analogous-but-different mechanism whose sameness the code does not settle. A capacity stop at a
member or task boundary is NOT a STOP — it is recorded under D-672.

---

*Provenance: Cowork, 2026-08-11, at the verified STOP following
`cc_instruction_row_landing_and_oi141_arm_check.md`, written from all present knowledge with
nothing knowledge-generating running. The user's five verdicts of 2026-08-11 are recorded at
§0a and are what this dispatch applies. The standing writing-side self-check was run against
this file's own text before release (D-434).*
