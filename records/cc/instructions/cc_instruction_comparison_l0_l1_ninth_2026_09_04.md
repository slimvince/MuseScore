# CC dispatch — the L0/L1 COMPARISON, NINTH WRITING, THE ADDITIVE REPAIR OF §11 AND §12: land the ninety-ninth entry and Ruling 79, derive at the rows which rows the rulings moved, add one subsection to each of §11 and §12 and correct each section's opening claim, close — NO ruling taken, NO standing row edited, NO renumbering, NO outgoing text opened, NO session booted

> **Dispatch (Cowork writing side, 2026-09-04, the session that booted on
> `cowork_handoff_entry_ninety_nine.md`, its first dispatch).** Executes **Ruling 79 (§3ch)**, which
> fixes the SHAPE of the repair Ruling 78 (§3cg) surfaced and left owed. **Written at a verified stop:
> the eighth writing's batch returned and its close was verified at the objects by this session** — the
> chain `7a5f307563… → e10acd807d… → 0e6649fd95… → cf268270cb… → 28c465e9c3…`, each commit read at
> `git show --stat` by explicit hash by THIS side; the tip read at both ref files with the file tools;
> the end-state guard artifact read at the object at the tip. **Nothing is running.**
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** It APPLIES one ruling the user took on 2026-09-04,
> **Ruling 79 (§3ch)** of `cowork_rulings_2026_08_31_decision_surface_sitting.md`. **THIS BATCH DECIDES
> NOTHING**: it takes no ruling, weighs no position, places no row, edits no row block, edits no
> proposed disposition, edits no ruled disposition, edits no outgoing text, derives no statement about
> the analysis, touches no register, and **authors no audit question**. Its whole authored write set is
> two corrected opening paragraphs and two new subsections, plus the close files. Where an anchor this
> face quotes is not found verbatim at the file, that edit is a STOP-and-report and no part of it is
> improvised.
>
> **★ THE ORDERED FIRST READ.** Your VERY FIRST read this session is the sitting record's **§3ch
> (Ruling 79) WHOLE** — the authority for this batch — then **§3cg (Ruling 78) WHOLE**, the ruling that
> surfaced the owed act. Then: **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full;
> `BUILD_AND_TEST.md` — CONDITIONAL, and this batch MEETS the condition (it runs the guard set): read
> it in full; rule (a)'s `gating_ids` at `tools/audit/nongating_apparatus_rows.json` →
> `★_the_live_gating_answer` (DO NOT read the whole open-items index). **(2)** `cowork_handoff.md`, ITS
> TOPMOST ENTRY, and `cowork_handoff_entry_ninety_nine.md` whole — the entry point this batch lands; a
> `cowork_handoff_entry_one_hundred.md` at the root, if present, is the writing side's and is NOT
> landed. **(3)** `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)** the eighth
> dispatch `cc_instruction_comparison_l0_l1_eighth_2026_09_04.md` whole, its report whole, and the
> eighth close in `cowork_away_returns.md` (*THE ROW 4.3 PLACEMENT (EIGHTH WRITING) …*). **(5)** the
> reading file `ratification_surfaces/cowork_comparison_l0_l1_reading.md` — its **§0, §5, §10 WHOLE
> INCLUDING BOTH OF ITS ADDED SUBSECTIONS, §11 WHOLE, §12 WHOLE, §13, §13a, §13b, §15 and §16**; §10 is
> read not to be edited but because **its added subsection is the FORM this batch's two additions
> copy**. **(6) No outgoing text, no part of the derived specification, and no part of the derivation is
> opened by this batch.**
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The reading file* —
> `ratification_surfaces/cowork_comparison_l0_l1_reading.md`. *A row's block* — the `**Row N.M —`
> heading and every line under it up to the blank line before the next `**Row` heading, `####` heading,
> `---` rule or `**★` paragraph. *A row's proposed word* — the disposition word in bold immediately
> after `*PROPOSED DISPOSITION.*` in that block. *A row's ruled word* — the disposition word in bold
> that follows `*RULED DISPOSITION (…).*` or `*PLACED — FAMILY, …*` in that block; **on some lines that
> word stands on the NEXT physical line because the marker wraps, and it must be read there.** *A moved
> row* — a row whose ruled word differs in class from its proposed word. *An added subsection* — the
> one new subsection this batch appends at the foot of a section, in the form §10 already carries. *A
> changed passage* — one contiguous run of changed lines in a diff (what a diff tool calls a hunk).
> *The current commit* — the commit named by hash, never a bare branch tip.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, **no measurement of the ANALYSIS**, no
> design, no repair of the analysis, **no derivation of any statement**, NO SESSION BOOTED, no document
> archived, moved or deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit
> to any governing document (the two `STATUS.md`/`STATUS_ARCHIVE.md` acts of Task 2 excepted), to the
> derivation (body OR banner), to the derived specification, to the brief, to the pack or its
> generator, to any register entry or register source, to the suspension file, or to any outgoing
> text; in the reading file, only the four edits Task 1 orders — no `**Row` heading, `*Statement.`,
> `*Derived.`, `*Current-text axis.` line, `*PROPOSED DISPOSITION` paragraph or `*RULED DISPOSITION`
> line anywhere in §6 is edited or deleted; §10 is not edited at all; no numbered item of §11 or §12 is
> edited, deleted, renumbered or reordered; §13's tables, §13a's figures and §13b's three tables are
> not touched** — **no ruling taken, no position weighed, no recommendation anywhere, and NO AUDIT
> QUESTION COMPOSED.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (`--check`) and again at the
> end (write mode, committed as the artifact of a real run). Verify every commit at the object by
> explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and any `git diff` over
> the whole working tree time out on this mount and are denied by the armed guard — use the per-path
> explicit-hash forms named in A1; a `git diff` with NO explicit hash is a D-253 breach. `git diff
> --no-index` against a working-tree path is denied by the guard: take every check between two explicit
> blob hashes, writing the working copy into the object store with `git hash-object -w` first, as the
> fifth to eighth batches did. **`gen_guard_state.py` DOES NOT RECOGNISE `--help` and silently selects
> its default WRITE mode** — established by the eighth batch and declared at its close as departure 3;
> do not probe it with a flag, name the mode you intend, and hash the artifact afterwards rather than
> assuming nothing moved. If staging is refused by a stale zero-length `index.lock` with no git process
> running, establish the cause (D-669), remove it, report the act. **Commit subjects are given below
> without an apostrophe anywhere**, so a plain single-quoted `git commit -m '…'` carries each verbatim.
> The reserved-word and vocabulary rules bind every line — *score* only the musical score, *key* only
> tonality, *bar* only the metric unit (the prohibiting sense is *exclude*), *bass* only the chord's
> bass note L3 decides except inside a quotation, no numeric grade anywhere.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 79, §3ch — its decision, verbatim:** *"The repair of §11 and §12 is ADDITIVE. Each section
  keeps its standing text unedited and gains one subsection at its foot, in the form §10's own added
  subsection uses, each entry naming the ruling that moved its row; §11's entries carry the audit
  question in one sentence as §5 requires. Each section's opening sentence is corrected in the SAME
  act, so that neither goes on claiming completeness while the complete answer stands in two parts —
  without that correction the shape leaves standing the very defect it repairs. The enumeration is
  DERIVED at the rows by the executing side and checked against this side's predicted sizes; where the
  two disagree, the executing side's derivation governs and the difference is reported."*
- **Ruling 79's stated exclusions, verbatim:** *"It does not re-derive, rebuild or renumber either
  section. It edits no standing row of either section, no proposed disposition anywhere, and no figure
  of §13, §13a or §13b. It does not touch §10, which is current."*
- **Ruling 78, §3cg — the owed act this discharges, verbatim:** *"The reading file's §11 (every
  QUARANTINED row's audit question) and §12 (every ADOPTED — proposed row) both open '★ COMPLETE OVER
  THE POPULATION' and were derived from the PROPOSED dispositions. The ruled dispositions since have
  not been folded into them … the act that closes it is one re-derivation of both sections against the
  ruled dispositions, which is its own scoped act."*
- **The reading file's §5, the closed vocabulary:** **QUARANTINED** — *"it states how the
  implementation currently works, or a measured property of it. The row writes the audit question in
  one sentence."*
- **The form both additions copy, at §10's own added subsection:** the heading *"### ★ ADDED BY THE
  APPLICATION ACT — the relocations Rulings 34 to 62 name that this list did not carry"*, a one-line
  italic provenance note reading *"Additions only; no row above is edited. Each entry names the ruling
  that relocated it."*, then bullets of the form `- Row N.M — <what it is> (Ruling <n>).`
- **The gap rule (`CLAUDE.md`, Conventions, every design decision carries its defense at its home):**
  *"the gap is stated, never filled in retroactively from memory"*.
- **★ WHERE RULING 79's OWN WORDS DO NOT REACH, STATED RATHER THAN RESOLVED BY STRETCH.** The ruling
  says *"§11's entries carry the audit question in one sentence as §5 requires"*. **That sentence
  assumes every quarantined row writes one, and four rows do not** — 3.41, 3.42, 3.48 and 14.9, each
  ruled by Ruling 62 item 4, whose line gives the ground and no question. The ruling does not say what
  to do there. **This dispatch does not decide it either:** composing a question would author what the
  audit asks, which no batch may do, so Task 1(b) orders the gap STATED at each of the four, in the
  shape the gap rule above requires, and the question of whether those rows are owed a question and who
  writes it is REPORTED to the user and left open.
- **D-672 (the stop rule):** *"A per-entry pass may be stopped at any member boundary, and the stop is
  RECORDED."* The member boundaries of this batch are the tasks; **within Task 1 the four edits are one
  member and stand or fall together.**

## Premise ledger

- **FACT — the local branch tip is `28c465e9c3467f4120bc658fb3ab10bc7489e17d`** (the eighth writing's
  end state; parent `cf268270cb…`, the eighth close); **`origin/master` is the SAME hash.** Read by the
  writing side at the two ref files with the file tools, 2026-09-04, and the chain
  `7a5f307563… → e10acd807d… → 0e6649fd95… → cf268270cb… → 28c465e9c3…` verified at `git show --stat`
  per explicit hash by this side. **Nothing is unpushed. Re-read the refs yourself before Task 0.**
- **FACT — the guard state at the tip IS the tip's own measurement**, read by this side at the object
  `28c465e9c3…:tools/audit/guard_state.json` (blob `df81a61d5d5812a5995f2ef35ca698c19deaa6cf`) →
  `summary`: **run 76**, passing 66, **failing 10**, not_run 4, historical_records 16. The ten failing
  tools, read at that object: `gen_filing_convention_application.py --check`,
  `gen_artifact_inventory.py --check`, `gen_artifact_inventory_surface.py --check`,
  `gen_test_construction_evidence.py --check`, `gen_retirement_caller_check.py --check`,
  `decisions/apply_soft_discard.py --check`, `decisions/apply_residue_discard.py --check`,
  `gen_epoch_write_path.py --check`, `gen_recognizer_establishment_sort.py --check`,
  `decisions/gen_cluster_dispositions.py --verify` — the same ten the fifth to eighth batches inherited
  and measured at both their ends. Nothing has landed since.
- **FACT — the blobs at the tip**, read at `git rev-parse <tip>:<path>` with their sizes at
  `git cat-file -s`: the reading file `72f9ad22be310e45aa593bc09f13bf1ef10d5c6b` (1,090,547 bytes); the
  sitting record `ecd241d1d2a59e5d7b010c1fd9e738a993a3d0cc` (512,553); `STATUS.md`
  `e3a333b17ee743811a17e890223ad2214a8e4171` (10,849); `tools/audit/gen_status_batch_bound.py`
  `649cb20dc5fc365b2c488297e651365040c5e917`; `tools/audit/evidence_pin_membership.json`
  `fb3a00be268401e5386954cc3c410c9c827539df`; the derived specification
  `61ca4bb7b0590ad65c33d52b9969501eb4ce4219`; `cowork_away_returns.md`
  `4d481565c3bbe62db8696044ada3e7e637d28478`; `cowork_handoff.md`
  `e6457cf3ef6d56bb1dd4369d286f16c1905d5c9d`.
- **FACT — the sitting record IS MODIFIED ON DISK by this writing side, additions only.** This session
  took Ruling 79 and wrote its section into
  `cowork_rulings_2026_08_31_decision_surface_sitting.md`: **one insertion of 9,882 bytes**, the whole
  of a new `## 3ch. Ruling 79 — …` section, placed immediately before the line
  `## 4. What this ruling does NOT do` — which occurs **exactly once** in the file, established before
  the insertion. The file on disk is **522,435 bytes**; at the tip it is **512,553 bytes**. **No other
  byte of that file changed**; the insertion was a splice, not a rewrite, and the result was read back
  through the file tools. **A1 declares this modification and Task 0 lands it; you prove additions-only
  at the object.**
- **FACT — §10 IS CURRENT AND IS NOT IN THIS REPAIR.** The fifth writing added to it, on 2026-09-03
  under `cc_instruction_comparison_l0_l1_fifth_2026_09_03.md` Task 3(e), the subsection headed *"★
  ADDED BY THE APPLICATION ACT — the relocations Rulings 34 to 62 name that this list did not carry"*,
  and every row this side measured as newly RELOCATED stands in it. Read at the file by this side. **Do
  not edit §10. Read it for its form.**
- **FACT — WHAT THIS SIDE MEASURED, AND HOW THE MEASUREMENT WAS ESTABLISHED.** Every
  `*PROPOSED DISPOSITION` line and every ruled or placed line of the reading file was extracted
  mechanically from the staged copy at blob `72f9ad22…`. The extraction reconciles against the file's
  own published arithmetic in three independent places: **757** proposed lines against §13's 757
  statements; **249** `*RULED DISPOSITION` lines against §13a's 210 plus §13b's 38 of kind (A) plus the
  one line the eighth batch added; and **13** `*PLACED — FAMILY` lines against §13b's 13 of kind (B).
  Nothing was left over in either direction. **This is a PREDICTION for you to check, not a figure of
  record: no artifact in the tree carries it (D-431), and where your own derivation disagrees, YOURS
  GOVERNS and the difference is reported.**
- **FACT — the predicted §11 set: NINE rows, each proposed as something else and ruled QUARANTINED.**
  **2.16** and **2.17** (Ruling 34, §3ao); **3.5** (Ruling 35, §3ap); **3.41**, **3.42**, **3.48** and
  **14.9** (Ruling 62, §3bq, item 4); **17.13** (Ruling 55, §3bj); **4.3** (Ruling 78, §3cg). **No row
  §11 already carries has been ruled OUT of QUARANTINED** — the predicted movement is one-way.
- **FACT — FIVE OF THE NINE CARRY AN AUDIT QUESTION AT THE ROW AND FOUR DO NOT, read at each row by
  this side.** Carrying one, inside the ruling's own quoted words: **2.16** (*"does the slicer open a
  boundary at a grace note, and with what span, against the rule that it opens none"*), **2.17**
  (*"the same question"*, pointing at 2.16's), **3.5** (*"which specifications and code currently
  define the eligibility combination, since after ratification only L1 does"*), **17.13** (*"does the
  arm that ships still place any harmonic boundary sequentially after a chord decision, or only inside
  the joint decode?"*), and **4.3**, whose question the eighth batch wrote at the row. **Carrying
  none:** **3.41**, **3.42**, **3.48** and **14.9** — each ruled by Ruling 62 item 4, whose line states
  the GROUND for the quarantine and writes no question. **§5 requires a quarantined row to write its
  question in one sentence, so these four rows are a GAP in the file.** The treatment is fixed at Task
  1(b) and **it is to STATE the gap, never to fill it**: composing a question here would author what
  the audit asks, which no batch may do.
- **FACT — the predicted §12 set: FIFTY-EIGHT rows leave the proposals half**, each having carried
  **ADOPTED — proposed** and having been ruled otherwise: **thirty** to ADOPTED — carried, ADOPTED —
  carried in substance, or HISTORICAL; **nineteen** to RELOCATED; and the **nine** ruled QUARANTINED
  above. **No ruling has produced a fresh proposal**, so the predicted movement is again one-way.
  **§12's DIFFERS half is NOT in this repair**: a DIFFERS verdict is a statement about the current
  text, which a disposition ruling does not move.
- **★ FACT — THE BOUND ON THIS SIDE'S MEASUREMENT, DECLARED (#24).** The two predicted sets rest on
  the **210** ruled lines whose disposition word stands on the marker's own physical line, **plus Row
  4.3, whose word wraps and which this side established by reading that row directly** — 211 rows in
  all. They do **not** cover the **51** family placement lines, whose word likewise wraps onto the
  following line and which this side's extraction could not reach; **seventeen** of those state in
  their own words that *"the proposed disposition stands"* and the remaining **thirty-four** state
  neither. **The arithmetic of the three groups closes: 210 + 51 + 1 = 262, against the 249 plus 13
  lines counted above.** **So both predicted
  sizes are LOWER bounds and never upper ones. YOUR derivation must read the wrapped word on the
  following line**, and a set larger than predicted is the expected outcome, not a STOP; it is
  reported with the members the prediction lacked.
- **FACT — the four anchors of Task 1 exist verbatim at the file**, each read by this side at blob
  `72f9ad22…`: §11's opening paragraph beginning `**★ COMPLETE OVER THE POPULATION.** All 29 documents
  are tabulated, and the QUARANTINED rows`; §11's closing line `**These are questions for the AUDIT
  phase. None is answered here, and none is an open-items row.**`, which occurs **exactly once** in the
  file; §12's opening paragraph beginning `**★ COMPLETE OVER THE POPULATION.** All 29 documents are
  tabulated, and every ADOPTED — proposed row`; and §12's last numbered item, ending
  `**These are the only DIFFERS in the file backed by a measurement of the current surface rather than
  by a reading of a specification.**`. **The phrase `★ COMPLETE OVER THE POPULATION` occurs five times
  in the file** — at §8, §9, §10, §11 and §12 — so each opening anchor must be matched on its whole
  quoted first sentence and never on that phrase alone.
- **FACT — `tools/audit/gen_status_batch_bound.py` at the tip** has its five authored constants at
  `BASE_COMMIT = "0e6649fd95665d76f3b3702ceaed886bd7b1e7e3"` (the eighth's Task 1 commit),
  `PREVIOUS_BATCH_DISPATCH = "cc_instruction_comparison_l0_l1_seventh_2026_09_03.md"`,
  `ACT_DATE = "2026-09-04"`, `DISPATCH = "cc_instruction_comparison_l0_l1_eighth_2026_09_04.md"`,
  `TASK = "Task 2"`, read at the object by this side; `PREVIOUS_AIMINGS` carries the seventh writing's
  aiming as its last row, marked as a move that RAN.
- **FACT — `STATUS.md` at the tip carries, top to bottom:** the eighth writing's close entry (naming
  the eighth dispatch, carrying the `Last updated: ` prefix), then that batch's two `Same dispatch`
  entries for Tasks 1 and 0, then the second writing's two 2026-09-02 entries, the first of which names
  no dispatch and does not say `Same dispatch`, then the archive pointers. Read at the file.
- **FACT — WHAT `gen_evidence_pin_membership.py` READS OF A RULING RECORD**, established at the tool's
  own source by the eighth batch and NOT restated from any dispatch's summary of it: route A reads only
  the record's LEADING BLOCKQUOTE and every paragraph opening `*Provenance:`; **route B reads EVERY
  LINE**, and for each line containing `PINNED` in any case it resolves any tool name on that line, a
  resolved tool becoming a member. **The Ruling 79 section moves none of the three:** the leading
  blockquote is untouched, the section contains no paragraph opening `*Provenance:`, and it contains no
  line carrying the word *pinned* in any case — established by this side at the file after the
  insertion. **A3 therefore predicts ZERO movement.** Measure before accepting.

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications at your boot:
  **EXACTLY ONE** — `cowork_rulings_2026_08_31_decision_surface_sitting.md`, additions only, the one
  new section named in the FACT above. **The bound on that declaration, stated rather than implied
  (#24):** the writing side could not run the whole-population enumeration from its own side, the tool
  exceeding the bridge's command window, so the declaration rests on the eighth close's own
  whole-population enumeration, on this side's own act-list since, and on a per-path comparison this
  side DID take — **and the comparison is narrower than the path list, which is stated rather than
  glossed.** For **three** paths the bridge-staged working copy was compared BY SIZE against
  `git cat-file -s` at the tip and agreed exactly: the reading file (1,090,547), the sitting record
  before this side's own insertion (512,553) and `STATUS.md` (10,849). The other paths this batch
  depends on — `tools/audit/guard_state.json`, `tools/audit/gen_status_batch_bound.py`,
  `tools/audit/evidence_pin_membership.json`, the derived specification, `cowork_away_returns.md` and
  `cowork_handoff.md` — were read **at their committed objects only**, so this side has no measurement
  of their working copies at all. **Your Task 0 enumeration is what closes that gap.** **Your Task 0 enumeration is what ESTABLISHES A1; this declaration is what it is
  measured against.** Untracked and to be landed by Task 0: `cowork_handoff_entry_ninety_nine.md` and
  this dispatch — **two untracked paths.** **★ THE ROOT ALSO CARRIES A LARGE STANDING POPULATION OF
  UNTRACKED HISTORICAL `cc_instruction_*` AND `cc_*_report` FILES**, which is pre-existing, is not this
  line's business, and which no dispatch of this line lands: **an untracked-file check at the root will
  return hundreds of names, and that is not a defect.** Any further `cowork_handoff_entry_*.md` or
  `cowork_rulings_*.md` at the root is the writing side's, is NOT landed and is NOT a STOP. A
  modification at any other tracked path, at boot or during the run other than by this batch's own
  ordered acts, is a STOP-and-report. **Check ordered (Task 0, first act, AFTER the ordered first
  reads):** the whole tracked population enumerated with the sanctioned tool
  `tools/audit/changed_paths.py`; per named untracked path
  `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`. **The sitting record's
  additions-only property is proven at the object**: write the working copy into the object store with
  `git hash-object -w` and diff it against blob `ecd241d1d2a59e5d7b010c1fd9e738a993a3d0cc` between the
  two explicit hashes — **zero deleted lines, one changed passage, and every added line inside the new
  `## 3ch.` section.** Any deletion is a STOP-and-report and nothing is committed.

- **ASSUMPTION A2 — the reds at the `--check` run before your first edit.** The ten failing tools named
  above, exactly, and zero STOPs. The reds this batch's own acts may cause, each classed by its cause
  and NOT a STOP: `gen_l0_l1_outgoing_population.py --check` STALE at the end state if the reading
  file's or `STATUS.md`'s recorded-tier hits move under Tasks 1 and 2 — regenerate, measure as the
  fifth, seventh and eighth did, commit with the end state, report that no MEMBER of the population
  moved; a member moving IS a STOP-and-report. `gen_session_start_read_size.py --check` STALE if
  regenerated before the LAST edit to `STATUS.md` — regenerate LAST.
  `gen_evidence_pin_membership.py --check` STALE if the sitting record's leading blockquote, a
  paragraph opening `*Provenance:`, or a line carrying the word *pinned* in any case moved — none of
  the three did, and a STALE verdict there is a STOP-and-report. `gen_artifact_inventory.py --check`
  and `gen_artifact_inventory_surface.py --check` are standing reds — report any difference, repair
  nothing. A red outside that set is classed by its measured cause; one outside this batch's own acts'
  subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration.** `evidence_pin_membership.json` moves by ZERO at
  Task 0. Measure before accepting; movement is a STOP-and-report.

- **ASSUMPTION A4 — the guard registry.** No tool added, none edited except Task 2's re-aiming of
  `gen_status_batch_bound.py`'s authored constants and its `PREVIOUS_AIMINGS` list (D-648); population
  unchanged at **76**; failing set the ten named, plus none; zero STOPs throughout.

- **ASSUMPTION A5 — byte-unchanged by this batch, verified at the objects after the last commit (P-3,
  footprint as an act-list):** the derivation `cowork_blind_derivation_l0_l1_2026_08_31.md` whole,
  banner and body; the derived specification `cowork_derived_specification_l0_l1_2026_09_03.md` whole;
  the sitting record beyond the one section Task 0 lands; the brief; the pack directory and
  `derivation_boot_pack.json`; every outgoing text; `tools/audit/gen_l0_l1_outgoing_population.py`; the
  population artifact except A2's case; every governing document except `STATUS.md` and
  `STATUS_ARCHIVE.md`; the suspension file; every register source. **In the reading file, the diff
  against blob `72f9ad22be310e45aa593bc09f13bf1ef10d5c6b` is exactly the four edits Task 1 orders and
  nothing else — two paragraphs replaced and two subsections inserted, each replaced paragraph's former
  wording carried verbatim inside its replacement.** Any other difference is a STOP-and-report.

## The declared start state — at the tree you will meet

1. The ten failing tools named in the FACT above — standing reds, none of them this batch's.
2. Zero STOPs; `gen_evidence_pin_membership.py --check`, `gen_l0_l1_outgoing_population.py --check`,
   `gen_status_batch_bound.py --check` and `gen_session_start_read_size.py --check` PASS predicted,
   **measured** (A2).
3. The artifact at the tip being the tip's own run, your start-state run is expected to reproduce it
   exactly; a difference is reported with its cause. **Name the mode you run it in** — the tool does
   not recognise `--help` and silently writes.

A failing verdict outside 1–3 is a STOP-and-report, save the A2 exceptions named.

## Findings in this batch

**No finding number is allocated by this dispatch.** A finding met during the run is written up on its
own and surfaced, never absorbed: **Ruling 9 (§3i), part (iii)** states that surfacing obligation as
*"UNCONDITIONAL and IMMEDIATE"* and makes only the HALT conditional. *(This citation was checked at
Ruling 9's own text. The eighth dispatch's face carried the shorter phrase "Ruling 9's routing binds",
which that ruling's text does not support as written; it is corrected here rather than repeated.)*
**An anchor this face
quotes that is not found verbatim at the file is a STOP-and-report for THAT edit.** The four edits of
Task 1 are NOT independent of one another — an added subsection without its section's corrected opening
leaves the section claiming completeness while the complete answer stands in two parts, which is the
defect Ruling 79 orders repaired — so **if any one of the four cannot be made, Task 1 is not committed
at all and the batch reports and stops.**

## Task 0 — land the ninety-ninth entry, Ruling 79 and this dispatch, and PUSH

1. **A1's check** (after the ordered first reads), including the sitting record's additions-only proof
   at the object.
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob
   `fb3a00be268401e5386954cc3c410c9c827539df` BEFORE accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_handoff_entry_ninety_nine.md` (new),
   `cc_instruction_comparison_l0_l1_ninth_2026_09_04.md` (new),
   `cowork_rulings_2026_08_31_decision_surface_sitting.md` (modified, additions only), and
   `tools/audit/evidence_pin_membership.json` (regenerated, only if it moved) — **three or four
   paths.** Subject, exactly:
   `record: Ruling 79 and the ninety-ninth entry landed with the ninth comparison dispatch, the additive repair of 11 and 12`
4. **PUSH**; verify at the ref file that `origin/master` equals the new tip.

**Under standing clause P-2 this dispatch is PINNED at Task 0:** record its blob, and prove the blob
unmoved immediately before staging it.

**E0:** the named paths in one commit; `origin/master` at the commit; A3 measured; A1's whole-tree
check reported with its enumeration; the sitting record's additions-only property proven at the object.

## Task 1 — the derivation at the rows, then the four edits, in ONE commit

**(a) DERIVE THE TWO SETS AT THE ROWS, BEFORE ANY EDIT.** Read the reading file's §6 with the file
tools. For every row block: take the row's **proposed word** and the row's **ruled word** as those
terms are defined at the head of this dispatch, **reading a wrapped ruled word on the following
physical line**; where a block carries two ruled lines, both are taken and the row is recorded as
carrying two. Then form:

- **Set Q** — every row whose ruled word is QUARANTINED and whose proposed word is not. Predicted:
  the nine named in the FACT above.
- **Set P** — every row whose proposed word is ADOPTED — proposed and whose ruled word is anything
  else. Predicted: fifty-eight.
- **Set Q-out** — every row whose proposed word is QUARANTINED and whose ruled word is anything else.
  Predicted: empty.
- **Set P-in** — every row whose ruled word is ADOPTED — proposed and whose proposed word is not.
  Predicted: empty.

**Publish all four sets in the report, by row identity, with the ruling each row's ruled line names.**
Where a set differs from the prediction, **your derivation governs**; state the difference and its
cause. **A non-empty Set Q-out or Set P-in is NOT a STOP** — it is carried into the corresponding
subsection as its own bullet group and reported, since removal from a list is not a deletion of any
standing text.

**(b) The §11 addition.** Its anchor is §11's closing line, which occurs **exactly once**:

```
**These are questions for the AUDIT phase. None is answered here, and none is an open-items row.**
```

**After that line, and before the heading of §12, add:**

```
### ★ ADDED BY THE ADDITIVE REPAIR — the rows the rulings QUARANTINED that this list did not carry

*Written 2026-09-04 under `cc_instruction_comparison_l0_l1_ninth_2026_09_04.md` Task 1, on Ruling 79
(§3ch). Additions only; no numbered question above is edited, renumbered or reordered. Each entry names
the ruling that quarantined its row, and carries that row's audit question in the words the ruling or
the row writes it in. Where no audit question is written at the row, the entry says so and composes
none.*
```

then one bullet per member of **Set Q**, in the file's own row order, each in this shape:

```
- Row N.M — <the audit question, quoted verbatim from the row's ruled line where the row carries one>
  (Ruling <n>, §<x>).
```

and, for a row whose ruled line carries **no** audit question, this shape instead:

```
- Row N.M — **no audit question is written at the row.** It was quarantined by Ruling <n> (§<x>) on the
  ground <the ground, quoted verbatim from the row's ruled line>. Ruling 78 (§3cg) records that
  question 2 above, Row 1.3's, is the standing question about the same field list; **this entry states
  the gap and composes nothing.**
```

Then, after the bullets, add — **only if Set Q-out is non-empty** — one further short bullet group
under the sentence `**And these rows were ruled OUT of QUARANTINED; their numbered questions above
stand unedited and no longer have a quarantined row behind them:**`, one bullet per member.

**★ THE HARD BAR ON THIS EDIT: NO AUDIT QUESTION IS COMPOSED, PARAPHRASED, MERGED WITH ANOTHER, OR
ATTACHED TO AN EXISTING NUMBERED QUESTION.** A question is quoted verbatim or its absence is stated.
Attempting to write one is a STOP-and-report.

**(c) The §11 opening correction, the former wording preserved.** Its anchor is §11's opening
paragraph, matched on its whole first sentence and never on the phrase `★ COMPLETE OVER THE
POPULATION` alone, which occurs five times in the file. **Append to that paragraph, editing not one
word of it, this sentence:**

```
**★ AND IT IS COMPLETE OVER THE PROPOSED DISPOSITIONS, NOT OVER THE RULED ONES.** The rulings taken
since have quarantined further rows, whose questions are NOT in the numbered list above; they stand in
the subsection at the foot of this section, added 2026-09-04 under Ruling 79 (§3ch). **The complete set
of audit questions is this section's numbered list TOGETHER WITH that subsection.**
```

**(d) The §12 addition and opening correction.** Its addition's anchor is the last two lines of §12's
last numbered item, **quoted here with the four-space indentation and the line break they carry at the
file**:

```
    measured discard of an above-threshold reading on most slices. **These are the only DIFFERS in the
    file backed by a measurement of the current surface rather than by a reading of a specification.**
```

**After it, and before the heading `## 13. The distribution so far, counted at this file's own rows`,
add:**

```
### ★ ADDED BY THE ADDITIVE REPAIR — the rows the rulings placed elsewhere, whose proposals above stand unedited

*Written 2026-09-04 under `cc_instruction_comparison_l0_l1_ninth_2026_09_04.md` Task 1, on Ruling 79
(§3ch). Additions only; **no numbered item above is edited, renumbered, reordered or deleted, and no
proposed disposition anywhere is deleted (#12)** — a numbered item above may therefore name a row that
has since been placed elsewhere, and this list is where that is recorded. Each entry names the
disposition the ruling gave the row and the ruling that gave it. This list concerns the ADDITIONS half
of this section only: a DIFFERS verdict is a statement about the current text, which a disposition
ruling does not move.*
```

then one bullet per member of **Set P**, in the file's own row order, each in this shape:

```
- Row N.M — ruled **<the ruled word>** (Ruling <n>, §<x>).
```

and — **only if Set P-in is non-empty** — one further bullet group under the sentence `**And these rows
were ruled ADOPTED — proposed and are not among the numbered items above:**`.

**Then append to §12's opening paragraph, editing not one word of it, this sentence:**

```
**★ AND IT IS COMPLETE OVER THE PROPOSED DISPOSITIONS, NOT OVER THE RULED ONES.** The rulings taken
since have placed rows of the additions half elsewhere; the numbered items naming them stand unedited,
and the rows are listed in the subsection at the foot of this section, added 2026-09-04 under Ruling 79
(§3ch). **What is still a live proposal is this section's additions half MINUS that subsection.**
```

**(e) What Task 1 does NOT touch, stated so it is not inferred.** **§10 is not edited.** No row block
in §6 is edited. No numbered item in §11 or §12 is edited, renumbered, reordered or deleted. §13's
tables, §13a's figures and tables, and §13b's three tables are untouched. **No proposed disposition is
deleted anywhere.** No audit question is composed.

**(f) The object-level check, before the commit.** Write the working copy into the object store
(`git hash-object -w`) and diff it against the tip blob `72f9ad22be310e45aa593bc09f13bf1ef10d5c6b`
between the two explicit hashes, to a scratch path outside the repository, read with the file tools.
**Exactly four changed passages. Exactly two `###` headings added, both of them the two named above.
Zero deleted lines anywhere** — both opening corrections are appends, and both subsections are
insertions, so **any deletion at all is a STOP-and-report.** No `**Row` heading added or removed. No
`*PROPOSED DISPOSITION` or `*RULED DISPOSITION` line touched. No numbered item's leading number
changed. §13's tables, §13a's tables and figures, and §13b's three tables byte-identical. A difference
none of those accounts for is a STOP-and-report and the file is not committed until it is removed.
Subject:
`comparison L0/L1: the additive repair of 11 and 12 under Ruling 79, each section gaining a subsection and its opening claim corrected, no numbered item edited`
Push; verify at the ref file.

**E1:** the four sets derived at the rows and published in the report; both subsections present with
one bullet per member; both opening paragraphs carrying their appended sentence and otherwise
unedited; zero deleted lines proven at the object; no audit question composed; no numbered item
touched; §10 untouched.

## Task 2 — the close

1. **The `STATUS.md` entries and the forward-bound move, IN THIS ORDER.** **(a) FIRST write this
   batch's own entries** at the top of `STATUS.md`: one pointer entry per task that did work (Tasks 0
   and 1; no figure restated, D-431), **the close entry FIRST and NAMING THIS DISPATCH BY ITS FILE
   NAME, the earlier task's entry below it saying `Same dispatch`**, and **the `Last updated: ` prefix
   moved from the eighth writing's close entry to your newest entry.** The close entry states in one
   sentence that §11 and §12 are repaired ADDITIVELY under Ruling 79, that no numbered item was edited
   or renumbered, and that §10 was not touched because it is already current. **(b) THEN re-aim
   `gen_status_batch_bound.py` — ALL FIVE authored constants:** `BASE_COMMIT` (this batch's Task 1
   commit), `PREVIOUS_BATCH_DISPATCH` (`cc_instruction_comparison_l0_l1_eighth_2026_09_04.md`),
   `ACT_DATE` (`2026-09-04`), `DISPATCH` (this dispatch's file name), `TASK` (`Task 2`); **and append
   to `PREVIOUS_AIMINGS`, kept rather than replaced (#12), the eighth writing's aiming now superseded**
   (base `0e6649fd95…`, then-previous the seventh dispatch, executing act the eighth dispatch Task 2,
   which RAN and moved the seventh writing's four entries). Amend the two head comments as the eighth
   did. **(c) THEN run `gen_status_batch_bound.py --apply`.** **Prediction, from the tool's mechanism
   as the eighth batch established it:** at `BASE_COMMIT`'s `STATUS.md` the eighth writing's close
   entry names the eighth dispatch and the two entries below it say `Same dispatch`; the second
   writing's first entry below those names no dispatch and does not say `Same dispatch`, so the run
   stops there — **exactly the EIGHTH writing's THREE entries move, the second writing's two stay in
   `STATUS.md`, no STOP.** If the tool nonetheless STOPs, restore it to its passing aim, re-run to
   prove it green, report the STOP with the tool's message, and move nothing by hand. **(d) LAST**,
   after the final edit to `STATUS.md`, `gen_session_start_read_size.py` regenerated and its `--check`
   confirmed green before the commit. The re-aimed tool, `tools/audit/status_batch_bound.json`,
   `STATUS.md`, `STATUS_ARCHIVE.md` and the read-size artifact are committed together in the close.
2. The FULL close appended to `cowork_away_returns.md` as a **THE ADDITIVE REPAIR OF 11 AND 12 (NINTH
   WRITING): EACH SECTION GAINS A SUBSECTION AND ITS OPENING CLAIM IS CORRECTED, NO NUMBERED ITEM
   EDITED OR RENUMBERED** section — both guard states, every SHA, E0–E2 graded, A1–A5 graded, declared
   departures, the object-level check of Task 1 stated as measured, **the four derived sets published
   whole with their differences from this dispatch's prediction named**, **the four rows for which no
   audit question is written at the row, named, with the statement that none was composed**, and the
   plan lines (**the pack finding of §3al.4 to L2's pack; then L2, sequenced by Ruling 10**), and
   **the plan's tell in one sentence: did this batch produce anything other than the landed records,
   the two subsections, the two appended sentences, the four Task-2 files and the report? If yes, name
   it.** The report is `cc_report_comparison_l0_l1_ninth_2026_09_04.md` — which likewise decides
   nothing; it carries the four sets by row identity with the ruling each names, the audit-question gap
   at its four rows, and the object check of Task 1; where it would name hashes it cannot contain,
   point at the git log.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **The ordered structure
   yields: Task 0, the reading-file commit, the close, the end state.**

**E2 — DERIVED from the declared start state plus this batch's footprint (P-3):** at the tree carrying
the close, a fresh full guard run — **population 76**; **zero STOPs**; **the failing set exactly the
ten named, PLUS NONE**, the A2 exceptions handled as A2 says — committed only after the run that
produced it.

## What this batch does NOT do

- **No ruling taken, no position weighed, no verdict on any open question, on the derivation or on the
  deriving session's independence. No session booted. No measurement of the analysis built or run. No
  derivation of any statement about the analysis.**
- **NO AUDIT QUESTION IS COMPOSED, paraphrased, merged, or attached to an existing numbered question.**
  Where a quarantined row writes none, the gap is stated.
- **No row block is touched. No numbered item of §11 or §12 is edited, deleted, renumbered or
  reordered. No proposed disposition is deleted anywhere. §10 is not edited.**
- **§13's tables, §13a's figures and tables, and §13b's three tables are untouched**, and no figure of
  any of them is restated on this face or in the additions (#17f, D-431).
- **No edit to the derivation (banner or body), to the derived specification, to the brief, to the
  pack, its generator, its artifact, the population tool, any outgoing text, any governing document
  other than the two Task 2 orders, the suspension file, or any register source.**
- **No finding number. No open-items row created, flipped or discarded EXCEPT an apparatus-finding row
  under rule (c). No `src/` change, no golden, no test, nothing under `tools/corpus/` or
  `tools/robust_stop/`. No guard enrolment. No `tools/audit/*.py` written or edited except the
  re-aiming of `gen_status_batch_bound.py`'s five authored constants and its `PREVIOUS_AIMINGS` list
  (D-648).**
- **No move of the second writing's two `STATUS.md` entries. Nothing written into the suspension file
  or any register.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* **#12** — nothing deleted anywhere, both opening paragraphs appended to rather
   than replaced, every numbered item and every proposed disposition standing, `PREVIOUS_AIMINGS`
   appended to. **#10** — both sections cease to state something false about themselves in the same act
   that makes the addition, which is the whole of what Ruling 79 requires. **#6** — one shape for one
   job, the shape §10 already carries, and one home for each addition. **#19** — nothing is established
   by this batch; the sets are derived and published, and this side's own figures are marked as a
   prediction to be checked rather than as a measurement of record. **#13** — an anchor not found
   verbatim is a STOP, and the four edits stand or fall together. **#24** — the bound on this side's
   measurement is declared, and the direction of its error is named. **#17(f)/D-431** — no count of the
   reading file is restated in either addition, and this face's counts are predictions with their
   artifact named as absent. Conforms.
2. *Conventions:* American English; no self-invented label — *a row's proposed word*, *a row's ruled
   word*, *a moved row*, *an added subsection* and *a changed passage* are all defined at the head, the
   last in the ruled words for what a diff tool calls a hunk; music-theory words in their musical sense,
   *exclude* for the prohibiting sense; *bass* nowhere outside a quotation; *the current commit* for a
   commit named by hash; no numeric grade anywhere.
3. *Figures and premises:* the tip at the two ref files and its chain at `git show --stat` per explicit
   hash, taken by this side this session; the guard summary and its ten failing tools at the tip's own
   artifact, read at the object by this side; every blob and every blob size at `git rev-parse` and
   `git cat-file -s` by explicit hash; the tool's five constants at the object; the nine rows of Set Q
   and their audit questions read at the rows with the file tools; the four anchors read at the file;
   §10's added subsection read at the file; the sitting record's byte counts taken from this side's own
   insertion, which spliced and did not rewrite. **Declared departures of this writing side:**
   (i) `git show <hash>:path`, `git rev-parse`, `git cat-file -s` and `git show --stat` by explicit
   hash, their output filtered through `grep` and a python parser — git object queries, the
   ninety-fifth entry's declared form; (ii) the Ruling 79 section written into the sitting record by
   editing a bridge-staged copy and committing it back under a modification-time guard, the anchor
   proven unique before the edit and the result read back through the file tools — **a write, not a
   read for a fact**; (iii) one `cp` of that staged copy inside the sandbox, to deliver it; (iv) the
   drift measurement taken by pattern extraction over the bridge-staged copy of the reading file, and
   two line-range checks over the bridge-staged copy of the sitting record, both **in the sandbox over
   snapshots and never through a shell on the user's machine**, the first reconciled against the file's
   own published arithmetic before use. **No working-tree file was read for its CONTENT by any shell.**

   **★ THE PRE-RELEASE FACT-CHECK OF THIS FACE, RUN AT THE OBJECTS ON THE USER'S OWN INSTRUCTION, AND
   WHAT IT FOUND — FIVE DEFECTS IN THE UNRELEASED DRAFT, ALL CORRECTED ABOVE, DECLARED AND NOT
   COUNTED.** **(1)** The §12 anchor was quoted with the wrong line break and without the four-space
   indentation it carries at the file, so the anchor this face's own rule requires to be found verbatim
   could not have been found; it is now quoted as the file carries it. **(2)** *"Ruling 9's routing
   binds"* was carried over from the eighth dispatch's face; read at Ruling 9's own text, that ruling
   is about the row-5 departure and the reconciliation of two stop widths, and the phrase is not
   supported as written — **a fact taken from another dispatch's statement of it, the exact shape the
   eighth batch counted as its first error** — now replaced by the clause of Ruling 9 that does support
   it, quoted. **(3)** The tension between Ruling 79's sentence about audit questions and the four rows
   that carry none was in the premise ledger but not named as a place the ruling's words do not reach;
   it is now named in the ruling ledger and left open rather than resolved. **(4)** The bound said
   *"211 ruled lines whose word stands on the marker's own line"*; the extraction reached **210**, and
   Row 4.3's word wraps and was established by reading its row — restated, with the arithmetic of the
   three groups closed. **(5)** A1 claimed a per-path size-and-hash comparison over eight paths; three
   were compared by size and the rest were read at their committed objects only — the claim is narrowed
   to what was done, and the gap is handed to your enumeration.
4. *File-tools rule:* every repository read through the file tools on bridge-staged snapshots, save the
   declared departure (i) above; no `git status`, no working-tree diff, no `git log`.
5. *Uncertainty:* this side's two predicted set sizes exclude the fifty-one family placement lines,
   whose disposition word wraps; the sets you derive may therefore be LARGER, which is expected and not
   a STOP. That the four rows without an audit question are exactly 3.41, 3.42, 3.48 and 14.9 is this
   side's reading of those four rows at the staged copy; a fifth such row found in derivation is
   carried under the same treatment and reported. **Whether those four rows are owed an audit question
   at all, and who writes it, is NOT settled by this dispatch and is not attempted in it.**
6. *Re-read from disk before release:* performed over this file after it was written to the root, and
   over the sitting record's inserted section, both read back through the file tools.

---

*Provenance: Cowork writing side, 2026-09-04, the session that booted on
`cowork_handoff_entry_ninety_nine.md`, at tip `28c465e9c3…` with `origin/master` equal, under Ruling 79
(§3ch) and Ruling 78 (§3cg) of `cowork_rulings_2026_08_31_decision_surface_sitting.md`, the reading
file's §5 and §10's own added subsection, and the phase definition §3.4. TOWARDS the ultimate objective
and TOWARDS the guiding principles.*
