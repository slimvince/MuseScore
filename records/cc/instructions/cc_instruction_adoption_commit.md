# CC instruction — THE ADOPTION COMMIT (user-ratified option 1, 2026-07-26; the OI-178 record's §7 executed)

> **★ RE-SCOPED BY USER RATIFICATION 2026-07-26, at CC's two-gap STOP (correct stop, both gaps
> verified):** this commit adopts on **THE MEASURED/COMMITTED SURFACE ONLY** — the batch/corpus
> path and the regression reference. **The in-app notation layer STAYS ON THE LEGACY ANALYSIS**,
> as a DECLARED migration state (the lawful-transition principle), whose named successor is the
> notation-layer increment (the output-surface contract from A's posterior, the table-packaging
> ruling, the ratified presentation publications) — Cowork drafts it next. Consequences for the
> items below: item 1's wiring = the batch_analyze STANDARD corpus path decodes via the joint
> module (tables read from `tools/joint_estimator/`, which batch can already reach — no in-app
> packaging needed at this scope); NO notation-layer file changes; item 4 inverts — the pipeline
> snapshots run the UNTOUCHED notation path, so **NO golden refresh is expected and any snapshot
> change is a STOP**; item 5's doc sync additionally records the migration state (CLAUDE.md gate
> block (A) + the OI-178 row: "EXECUTED — staged scope; the notation increment is the named
> successor"). Blockers 1 and 2 of CC's STOP are thereby deferred BY DESIGN to the successor
> increment, not resolved silently.

> **Read first (every session):** `C:\s\MS\CLAUDE.md` (gate block (A) — you will be updating it
> in this very commit; read it as the outgoing state), `C:\s\MS\STATUS.md`,
> `C:\s\MS\BUILD_AND_TEST.md`, **the adoption record (`tools/joint_estimator/adoption_record*`,
> `cc_adoption_measurement_report.md`) — the ratified basis for everything below**, and the
> dual-path sanction + amended adoption protocol in `cowork_prefit_gates.md`.
>
> **Current state:** branch `master` at `d615152c51`, in sync with origin (verify). Cowork-
> authored uncommitted doc edits ride this commit: `cowork_handoff.md` (the session-close
> entry-point block + the adoption-ruling record) and `OPEN_ITEMS.md` (the NEW OI-192 row — the
> genuine-new fifth-substitution subset, rowed at session close). Verify they are the only
> non-yours diffs. **THE USER HAS RATIFIED ADOPTION (2026-07-26): this
> dispatch performs the behavior change — ONE revertible, provenance-stamped adoption commit.**
> The product is unshipped; end-user behavior-change considerations are explicitly moot (user
> ruling) — correctness conditions below are what govern.
>
> **STOP conditions:** the regenerated corpus decode diverging from the committed
> `adoption_decode.json` on any covered piece; the re-baselined reference figures differing from
> the adoption record's; any test failure not accounted for by the sanctioned golden refresh;
> any need to modify a file class not listed below. Machine power-state actions remain at the
> user's instruction, as always.

**Dispatch author:** Cowork, 2026-07-26, at the user's ratification. **One commit, one
change-event: the joint estimator becomes the production inference layer.** The retirement map
does NOT execute here — its increments come as their own later verified dispatches; today the
legacy path merely stops being the output producer.

## The commit's contents (all of it, together, revertible as one)

1. **The wiring.** The production analysis output surface (the region/chord/key results the
   batch driver and the notation layer consume) is produced by the joint module's decode — the
   fact adapter → the §5 decoder at the committed all-326 tables + selected weights → the
   published result stream (chord symbol derived from (key, degree) as the ratified derived
   fact; the un-rounded modal reading and the post-decode ornament labels are LATER retirement-
   map/presentation increments, not this commit). The legacy analysis code remains in place and
   compiled (dormant on the output path) — its removal is the retirement map's business.
   Inference is **preset-independent** (the ratified mode decision): presets remain presentation
   concerns; document the wiring point in the commit body.
2. **The committed corpus regenerated** (all 352 stems × the three preset dirs — now carrying
   A's output; the per-preset dirs converge at the inference fields by construction), manifests
   re-stamped by the standing corpus tooling. **Establishment: the regenerated decode must match
   `adoption_decode.json` exactly on the 326 covered pieces** — the corpus IS the measured
   record, or STOP.
3. **The `tools/robust_stop/` re-baseline** (the generalized re-baseline discipline): a8 re-run
   over the new corpus; the per-preset enumerations, `summary.json`, and the manifest re-stamped
   via `robust_stop_restamp.py`; **the figures must equal the adoption record's** (root 77.03,
   RN 64.12, key-local 78.42, key-home 56.14, class-(b) durations 1,817,280 per preset) — any
   difference is a STOP. The outgoing reference's O-12 snapshot already exists
   (`snapshot_2026-07-26_pre_oi178_adoption/`) — verify, don't re-copy.
4. **The golden refresh:** `pipeline_snapshot_tests.exe --update-goldens`, then re-run to green —
   the "verified correct" precondition is the ratified adoption record, cited in the commit
   body. Both suites green (existing unit tests of the still-present legacy units keep passing
   unmodified; a legacy test that pins the OUTPUT surface and now fails is disposed per its
   kind: snapshot-class → refreshed, unit-class → STOP and report, never edited to pass).
5. **The documentation, in the same commit (#10):** CLAUDE.md gate block (A) — the new ratified
   baselines (the adoption record's columns and class-(b) durations, marked "re-baselined at the
   OI-178 adoption, 2026-07-26, user-ratified; measurement provenance `d615152c51`"), the
   prior baselines noted as superseded with the snapshot path; the preset-independence note;
   STATUS.md's new entry; ARCHITECTURE.md's joint-estimator section (the as-built inference
   layer: inputs, factors, tables, weights, the §5 decode — concise, pointing at the ratified
   documents); the OI-178 row flipped EXECUTED with provenance; the OI-175 row noted
   superseded-in-fact (the head-gap prior no longer decides committed output; its code retires
   at the map).
6. **The dual-path status:** the sanction's forward exit has EXECUTED; the handoff's status line
   (Cowork's riding edit) records it; the retirement map is now LIVE work for later dispatches.

## Report

Commit hash; the wiring point; the corpus-regeneration establishment result; the re-baseline
figures verified against the record; the golden refresh scope; the test totals; every doc
touched; anomalies (a surprise during an adoption commit is a STOP, not a workaround). Push
**origin only** (the standing hard stop on the upstream remote).
