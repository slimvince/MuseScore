# CC dispatch — the preparation phase's ELEVENTH batch, AMENDED: the four members closed at the landing-commit bound, Ruling 1b written at the two tools it is actually about, four standing clauses landed at one home, the stale banners corrected at their generators, and the session-start-read values derived rather than withdrawn

> **Dispatch (Cowork, 2026-08-18). THIS SUPERSEDES `cc_instruction_preparation_eleventh.md`,
> WHICH STOPPED AT ITS DECLARED PRECONDITION BEFORE TASK 0.** The superseded dispatch is NOT
> deleted and NOT rewritten: it lands in git at this dispatch's Task 0 beside your own STOP report,
> because the STOP is only evidence if the record can show what it stopped against (#12).
>
> **Written at a verified stop; nothing else is running and no dispatch was active when this was
> written.** The curated boot list, the empirical findings ledger, the fact-gate, the mining, the
> landing of the newly visible instruction files, the whole-document archiving wave, and every act
> on any candidacy are NOT in this batch.
>
> **★ WHAT CHANGED FROM THE SUPERSEDED DISPATCH, so you meet the amendment rather than infer it.**
> Four of the five findings your STOP returned were defects in what the writing side wrote. Each was
> put to the user on a decision surface and each was ruled; the record is
> `cowork_rulings_2026_08_18_eleventh_stop.md`. **The three rulings and the one sub-ruling of
> `cowork_rulings_2026_08_18_tenth_return.md` are NOT re-opened by any of it** — what changed is how
> they are operationalized. Concretely: the declared start state now names the reds this dispatch's
> own inputs cause (F67); §6 kind 3's tool identification is corrected and Ruling 1b's split is
> written at the two tools that actually hold the two outputs (F68); the pin derivation reads the
> **landing-commit bound** rather than the sitting's date (F69); and the two values with no derived
> home are **derived by extending the generator** rather than withdrawn (F70). Task structure has
> changed with them.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional, and explicitly NOT demoted. **(3)** `BUILD_AND_TEST.md` is a
> CONDITIONAL read — **and this batch MEETS the condition**, because it runs the guard set, whose
> commands live there, so read it in full. **(4) DO NOT READ THE WHOLE OPEN-ITEMS INDEX.** Rule (a)
> reads the DERIVED GATING ANSWER narrowed to its identity list —
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids` — and the
> grounds stand at `the_gating_rows` in the same file and are opened when a verdict is CHALLENGED.
> The INDEX remains the AUTHORITATIVE STATUS SURFACE; you open it when you need a row.
>
> **★ AND THE STANDING CLAUSE YOUR OWN LAST BATCH LANDED BINDS THIS ONE: read `cowork_handoff.md`,
> ITS CURRENT (THIRTIETH) BLOCK.** The clause lives at `cowork_audit_protocol.md`'s
> dispatch-protocol section. **The findings series stands at F74 when this batch begins** — you
> allocated F67–F71 at the STOP; the writing side allocated **F72** and **F73** at the eleventh
> STOP sitting and **F74** after it at its own executing act, and the thirtieth block is where it
> announced all three. **Number from F75.**
>
> **Then read, in full:** `cowork_rulings_2026_08_18_eleventh_stop.md` (this batch's whole
> authority); `cowork_rulings_2026_08_18_tenth_return.md` (the rulings this batch completes, not
> re-opened); **your own `cc_report_preparation_eleventh.md` IN FULL, its §5.b and §6 above all**;
> `cc_instruction_preparation_eleventh.md` (the superseded dispatch, so that you meet what was
> dispatched rather than a description of it); `cowork_rulings_2026_08_17_ninth_return.md` **§1**
> (Ruling 1, the class this batch completes) and **§3** (Task 4's first correction site);
> `cowork_rulings_2026_08_16_preparation_return.md` **§6** (the Kind-1 and Kind-3 treatments,
> **whose kind-3 tool identification Task 1 corrects**) and **§5(D)** (the continuous-pruning rule
> every superseded wording moves under); the **four ruling records Task 1 corrects**; and your own
> `cc_report_preparation_tenth.md` **§§3.b, 3.c and 10**.
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/evidence_pin_membership.json` (Task 1's subject — the derived membership and the four
> unresolved members); `tools/audit/session_start_read_size.json` and
> `tools/audit/nongating_apparatus_rows.json` (Task 4's authority and its measured subject);
> `tools/audit/guard_classification.json` and `tools/audit/guard_state.json` (the start and end
> states).
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no mining, no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, and **no caller flag or
> candidacy acted on**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end;
> record both states.** The declared start state is at §"The declared start state" below and is
> stated **at the tree you will actually meet**, not at the previous batch's end state — which is
> Ruling 1 of the eleventh STOP sitting applied to the very dispatch that ruling was taken over.
> **A start state differing from the one declared there, in any way its own paragraph does not
> account for, is a STOP-and-report.** Verify every commit at the object by explicit hash and record
> the SHAs in the close. **The reserved-word conventions bind all new prose. The new-tool rule
> stands. The E-ordering rule stands, at both grains. F3's reading rule stands**
> (`gen_cluster_dispositions.py --verify` is the drift authority; `reaim_home_anchors.py --check`
> exits 0 while printing drifted anchors and is not the authority). **Every guard run is taken at a
> quiet tree.**
>
> **★ THE VOCABULARY RULE OF 2026-08-17 BINDS EVERY LINE THIS BATCH WRITES.** A surface rates an
> alternative **TOWARDS the ultimate objective** and **TOWARDS the guiding principles** — never
> *against* either. A term carrying opposite senses in adjacent context is fixed AT THE WORD.

## Ruling ledger (quoted at the records, not paraphrased)

**From `cowork_rulings_2026_08_18_eleventh_stop.md`:**

- **Ruling 1 — the start state at the tree:** *"THE DEFECT IS THE DECLARATION'S, NOT THE MEASUREMENT
  TOOL'S. … The amended dispatch therefore declares the tenth batch's proven end state PLUS the reds
  its own inputs cause, each named with its cause, and Task 0 commits the landed paths together with
  the regenerated membership artifact, so the check returns green in the same act."* — with its
  standing clause: *"A DISPATCH'S DECLARED START STATE IS STATED AT THE TREE THE DISPATCH WILL MEET,
  INCLUDING THE REDS THE DISPATCH'S OWN INPUTS CAUSE, EACH NAMED WITH ITS CAUSE."*
- **Ruling 2 — §6 kind 3 and Ruling 1b:** *"A dated correction is APPENDED at §6 of the 2026-08-16
  record naming `gen_artifact_inventory.py` as the census's writer, the corrected wording preserved
  beside it (#12), the ruled text never rewritten. … Ruling 1b's OUTCOME stands and is reached with
  no split inside any tool."* — with its rider: *"A pin that fixes what it reads fixes BOTH routes."*
- **Ruling 3 — the landing-commit bound:** *"THE DERIVATION AS SHARPENED: the last commit touching
  that surface which is dated at or before THE COMMIT THAT LANDED THAT SITTING'S RECORD, the landing
  commit derived per member at the git objects."* — with the half that binds hardest: *"The executing
  act cites its own per-member derivation at the objects and does NOT cite D-230 for the bound."* —
  and its forward clause: *"A SITTING RECORD IS AN INTERIM CARRIER, WRITTEN IN THE TURN ITS RULING IS
  GIVEN AND LANDED IN GIT AT THE NEXT DISPATCH'S TASK 0 — SO THE COMMIT THAT LANDS IT BOUNDS WHEN THE
  SITTING WAS HELD."*
- **Ruling 4 — the generator extended:** *"THE EXTENSION IS TWO KEY CHAINS, NOT A NEW CAPABILITY, AND
  THAT IS WHAT DECIDES IT. … The work is DECLARED AS A JUDGMENT at the site, in the commit message
  and in the close — never slipped in."* — with its bound: *"The pre-ninth-batch total and the
  −45.1 % derived from it stay WITHDRAWN."*

**From `cowork_rulings_2026_08_18_tenth_return.md`, which this batch completes and does not
re-open** — Ruling 1's act (*"Each of the four ruling records whose generated surface could not be
pinned gains a dated correction naming the commit its surface was ruled at"*), its bound (*"A member
whose derivation cannot be established STAYS UNRESOLVED and is never pinned"*), its forward clause
(*"A RULING RECORD TAKEN FROM A GENERATED DOCUMENT NAMES THE COMMIT IT WAS RULED AT"*); Ruling 2's
act (*"one sentence written AT EACH GENERATOR, qualifying the banner with the sitting, its date and
the ruling record that carries it, rendered into the document in the same act, with the former banner
text preserved beside it (#12)"*) and its sequencing (*"This act runs AFTER Ruling 1's, in the same
dispatch … each document's own difference is MEASURED and reported, and must be confined to the
banner"*); and Ruling 3's correction of record and its standing clause (*"A PUBLISHED CHARACTER
FIGURE NAMES THE TOOL THAT PRODUCED IT"*).

## Premise ledger

*(Every FACT pins content at content-addressed objects or at a per-path git history query taken at
the objects. No premise names a terminus commit as a premise about content, and no premise asserts a
changed-passage count — the F25 lesson.)*

- **FACT** — the tenth batch is complete, pushed, and verified at the objects: the chain
  `1760d9a4a8` → `6bd9285006` → `59191d7e65` → `b0596b288b` → `594074e1e1` → `b6134f41ff` →
  `30d44165cf` → `8f88e1a61e` → `9390e2ca2c`, every parent confirmed and every path count to the
  digit (3, 2, 9, 24, 4, 4, 9, 2).
- **FACT** — the superseded dispatch STOPPED before Task 0 and committed nothing. `HEAD` and
  `origin/master` are both `9390e2ca2c` with **zero commits on it**; `STATUS.md` carries the TENTH
  batch's entries alone; `cowork_away_returns.md`'s last close heading is the TENTH batch's.
  Established by the writing side at the branch tip and at the staged files.
- **FACT, established by the writing side at the object `9390e2ca2c`, and the cause of the STOP** —
  `tools/audit/gen_evidence_pin_membership.py` builds its ruling-record population by listing the
  repository root **on the file system**, `os.listdir(ROOT)` filtered by `^cowork_rulings_.*\.md$`,
  and reaches the generated-document side by walking `ratification_surfaces/` the same way. **It is
  therefore moved by an UNTRACKED file at root**, which is F67 and is why the declared start state
  below names the reds this dispatch's own inputs cause.
- **FACT, established by the writing side at the object `9390e2ca2c`, at the two tools' own source,
  and Ruling 2's whole ground** — `tools/audit/gen_artifact_inventory.py` writes **one** output,
  `tools/audit/artifact_inventory.json`, the census. `tools/audit/gen_artifact_inventory_surface.py`
  writes **one** output, `ratification_surfaces/cowork_artifact_inventory_ruling_surface.md`, and
  reads **two** input routes — the census file, and a citation scan it performs over the tree. **So
  the tree holds one output each from two tools, not two outputs of one tool.**
- **FACT, established by the writing side at the git objects by per-path history query, and Ruling
  3's whole ground** — four commits touched
  `ratification_surfaces/cowork_rulings_sort_surface_2026_08_16.md` on the rulings-sort sitting's own
  day: `53e552296f` (2026-08-17T10:00:25), `a21a55fc12` (18:14:23), `4667816255` (22:28:33),
  `15dfb0e172` (23:13:00). The sitting record `cowork_rulings_2026_08_17_rulings_sort_sitting.md` was
  **added** to git at `570f2b63b1` (16:34:06). `tools/audit/gen_rulings_sort.py`'s next commit after
  `53e552296f` is `a21a55fc12`, after the bound.
- **FACT, established by the writing side at the object `9390e2ca2c`, and Ruling 4's whole ground** —
  `tools/audit/gen_session_start_read_size.py` carries `key_span_characters(text, keys)`, a function
  general over a key chain, and its `measure()` already calls it twice with different chains. Both
  values Ruling 3 of the tenth-return sitting could not correct are **named key spans in the artifact
  that tool already reads**: `★_the_live_gating_answer` → `the_gating_rows`, and
  `★_the_live_gating_answer` → `★_the_frozen_enumeration_measured_against_this_one`.
- **FACT, quoted from that tool's own source at the same object** — *"Before the ninth batch the read
  regime named a DIFFERENT MEMBER SET — `OPEN_ITEMS.md` whole, `BUILD_AND_TEST.md` unconditionally —
  and rule (a) named no artifact-and-key pointer at all, so this measurement's shape does not reach
  it and forcing it would compare two different questions."* This is why Task 4's withdrawal stands.
- **FACT** — `OPEN_ITEMS.md` and `tools/audit/nongating_apparatus_rows.json` are **byte-identical**
  at `1760d9a4a8` and `9390e2ca2c` (blobs `6ae67d8603` and `5bb43d0b3a` at both). **Nothing this
  batch does may move either; a difference in either at any task is a STOP-and-report.** Task 4
  READS the second and never writes it.

- **ASSUMPTION A1 — the working tree, stated by CONTENT and never by a changed-passage count.** The
  tree carries exactly **ONE tracked modification**: `cowork_handoff.md`, whose content is **five
  parts of one act** — the TWENTY-NINTH session-close block inserted; the TWENTY-EIGHTH heading's
  entry-point demotion marker; the ★ POINTER appended at the head of the twenty-ninth block; the
  dated CORRECTION appended at its foot; and the THIRTIETH session-close block inserted with the
  TWENTY-NINTH heading's own entry-point demotion marker.
  **★ AND ONE PATH IS NAMED AS EXPECTED-UNMODIFIED RATHER THAN LEFT OUT, because Task 0 will move
  it.** `tools/audit/evidence_pin_membership.json` is expected **UNMODIFIED**: the writing side did
  not run its generator, and your own STOP restored the path to its committed blob
  `80e97da1f79f8047069d39837e4cd454d7644b73`. **If it IS modified, that is accounted for by Task 0
  step 2 and is NOT a STOP** — the regeneration that step orders supersedes whatever a stray run
  left. **A modification at any OTHER tracked path is a STOP-and-report.**
  **Untracked and to be landed by Task 0:** `cowork_rulings_2026_08_18_tenth_return.md`,
  `cowork_verification_plan_preparation_tenth.md`, `cc_instruction_preparation_eleventh.md`,
  `cc_report_preparation_eleventh.md`, `cowork_rulings_2026_08_18_eleventh_stop.md`, and this
  dispatch. Every other untracked path is the known, declared, unlanded population.
  **Check ordered (Task 0, first act, blob against blob by explicit hash):** any tracked difference
  whose CONTENT falls outside that shape is a STOP-and-report.
  **★ THE F57 CAVEAT ON THAT CHECK.** Compare through git, or normalise line endings before any byte
  comparison decides anything; **a file differing from its blob by EXACTLY its own line count is NOT
  a modification and is NOT a STOP.** For `cowork_handoff.md` the caveat does not arise and this is
  MEASURED rather than assumed: immediately before it was edited it stood at **357,983 bytes with
  ZERO carriage returns**, sha256 `4690eab63f1750dd650d0db7afb5bd3495e791923969ef003e0fe42cfc4324e1`.
  The two files the writing side wrote this session were each delivered through the device bridge and
  **round-tripped back byte-identical**, and each is stored LF with zero carriage returns.
  **★ WHY THE THIRTIETH BLOCK AND THIS ASSUMPTION WERE WRITTEN IN THE SAME ACT:** a handover block
  written after a dispatch has already stated its premise about the working tree is how a false
  premise reaches the executing side, which is the A1 premise error's own shape.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  **Check ordered (each task's last step):** any other red is classed by its measured cause, and one
  outside this batch's own acts' subjects is a STOP-and-report. The reds this batch itself causes and
  must clear by their ruled treatments: **the membership check, cleared by Task 0 step 2**; **the F15
  anchored-quote class**, because Tasks 1, 2 and 4 edit `cowork_audit_protocol.md`, four ruling
  records and the decisions register's anchored quotes; **any check whose subject is a generator Task
  1 pins**; and **any check that reads one of the three ratification surfaces Task 3 re-renders**.

- **ASSUMPTION A3 — for each of the four UNRESOLVED members, at least one commit touching its
  ratification document is dated at or before THE COMMIT THAT LANDED that member's sitting record.**
  **Check ordered (Task 1, first act):** a member with none is a **STOP-and-report**, never a guessed
  pin, and it stays UNRESOLVED.
  **★ ITS STATUS IS DECLARED PER MEMBER RATHER THAN AS A CLASS, AND THE GAP IS NAMED RATHER THAN
  LEFT.** Your own STOP report establishes A3 for all four at its §5.b. The writing side
  **independently re-verified only the rulings-sort member** at the git objects; **its per-path
  history walk over the other three members' surfaces TIMED OUT on this mount and was not retried** —
  the same class of timeout the record already carries for `git status` and `changed_paths.py`.
  **You therefore RE-DERIVE ALL FOUR at the objects and inherit none of §5.b**, which is the only
  route by which a pin rests on an establishment rather than on a claim neither side checked.

## The declared start state — stated at the tree you will meet, per Ruling 1

**71 guards run, 69 passing, TWO failing, 4 not run, 16 historical, ZERO STOPs**, and *"the guard
classification re-derives"*.

The two failures, each named with its cause:

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing red,
   untouched by this batch and expected.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — **caused
   by this dispatch's own inputs**, per the FACT above: the tool derives its ruling-record population
   from the file system, and **TWO untracked root-level `cowork_rulings_*.md` files** now sit there,
   `cowork_rulings_2026_08_18_tenth_return.md` and `cowork_rulings_2026_08_18_eleventh_stop.md`.
   **The expected difference is confined to the ruling-record count and the two added record names.**
   Your own STOP measured that difference at ONE added record, moving the count from 36 to 37
   (`cc_report_preparation_eleventh.md` §3.a, taken blob against blob at explicit hashes); with the
   second record present the derivation should read **two** added names and a count two higher than
   the committed one. **That is a prediction, not a citation — check it at the artifact.**

**A THIRD failing check, or a difference in the membership artifact reaching any member, route,
document, pin constant, state or count, is a STOP-and-report.**

## Task 0 — land the eight paths, and clear the membership check in the same act

1. **A1's check first, entirely at content-addressed objects**, under the F57 caveat above.
2. **★ REGENERATE `tools/audit/evidence_pin_membership.json` at this tree, and MEASURE its difference
   against the committed blob `80e97da1f79f8047069d39837e4cd454d7644b73` BEFORE accepting it**, blob
   against blob by explicit hash. **The difference must be confined to the ruling-record count and
   the added record names. If ANY member, route, document, ruling record, pin constant, state or
   count moves, that is a STOP-and-report** — the regeneration is a bookkeeping act and may not move
   a verdict.
3. **Commit exactly these EIGHT paths and no ninth:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_18_tenth_return.md` (new),
   `cowork_verification_plan_preparation_tenth.md` (new),
   `cc_instruction_preparation_eleventh.md` (new — the superseded dispatch),
   `cc_report_preparation_eleventh.md` (new — your STOP report),
   `cowork_rulings_2026_08_18_eleventh_stop.md` (new — the sitting record),
   `cc_instruction_preparation_eleventh_amended.md` (new — this dispatch). Staged plainly; push;
   verify at the object.
4. **Re-run `gen_evidence_pin_membership.py --check` at the tree this commit carries: it must PASS.**
   If it does not, the cause is measured and reported and the batch STOPS.

**★ WHY THE SUPERSEDED DISPATCH AND YOUR STOP REPORT LAND, because they are new in kind and a reader
must not mistake either for a live instruction.** `cc_instruction_preparation_eleventh.md` is the
dispatch that was issued and stopped; `cc_report_preparation_eleventh.md` is the report of that stop.
Neither governs anything now. They land because **a STOP is only evidence if the record can show what
it stopped against**, and git is the only thing that can show that — the same ground on which the
tenth batch's pre-registered verification plan lands beside them.

**Registered expectation E0:** exactly 8 paths at the enumeration; two modifications whose content
matches A1's stated shape and the bounded regeneration of step 2, six additions; no staging override;
`gen_evidence_pin_membership.py --check` passing at the resulting tree, so the guard set stands at
**71 run, 70 passing, ONE failing** from Task 0 onward.

## Task 1 — Ruling 1 and Ruling 1b of the tenth-return sitting, executed under Rulings 2 and 3 of the STOP sitting, in ONE commit

1. **★ A3's check first, per member, at the LANDING-COMMIT BOUND — and it is a derivation, not a
   lookup, and not an inheritance.** For each UNRESOLVED member the artifact names, establish at the
   git objects: **(a)** the sitting the member's ruling record carries, read from that record's own
   text and never from its file name; **(b)** **the commit that ADDED that ruling record to git**,
   which is the bound; **(c)** the last commit touching that member's ratification document dated at
   or before that bound. **Publish, per member: the document, its sitting, the ruling record, the
   landing commit with its timestamp, EVERY candidate commit considered with its timestamp, the one
   selected, and the evidence that no later commit at or before that bound touched the document.** A
   member with no such commit is a **STOP-and-report** and stays UNRESOLVED.
   **★ RE-DERIVE ALL FOUR. Do not inherit your own §5.b** — the writing side re-verified one member
   only and says so, and a pin is worth nothing if the commit it names rests on a claim neither side
   checked at the moment it was applied.
2. **★ MEASURE THE RESIDUAL RISK PER MEMBER AND PUBLISH IT — do not carry it as a class caveat.**
   Report, per member, **the interval between the selected commit and the landing-commit bound**, and
   any evidence bearing on whether the document moved inside it — **including whether the member's
   generator and its inputs moved inside it**, which is what bounds an uncommitted regeneration. A
   member whose interval the evidence cannot bound is reported as such; it is still pinned, and the
   bound is published with the pin so a later reader can challenge it. **The one thing forbidden is
   silence about the interval.**
3. **Write the established commit as a DATED CORRECTION APPENDED to each of the four ruling
   records** — **a ruling record is corrected by appending, never by rewriting the ruled text**, on
   the pattern the ninth and tenth batches' own Task 0b acts used. Each correction names the commit,
   **the derivation that produced it**, the interval from step 2, and its authority: Ruling 1 of
   `cowork_rulings_2026_08_18_tenth_return.md` as amended by **Ruling 3 of
   `cowork_rulings_2026_08_18_eleventh_stop.md`**.
   **★ EACH CORRECTION CITES ITS OWN DERIVATION AND DOES NOT CITE D-230.** D-230's text is the
   decisions register's rule (c) and does not carry the interim-carrier bound; citing it would be the
   defect **F72** records, and **D-643** requires a claim invoking a ruling to be checkable at that
   ruling's own text. The clause that DOES carry it is landed by Task 2(c) — cite that once it
   stands, or cite the derivation alone if this task runs first.
4. **Apply the pin to each member whose commit is established**, on the shape already in the tree and
   **IMPORTED rather than re-invented (#6)**: the generator reads its inputs from the git objects at
   the pinned commit, and its check compares against the committed document.
   **`gen_claude_md_finer_surface.py`'s existing pin is not re-taken**, and neither are the two
   route-B pins the tenth batch recorded.
5. **What the pin does NOT freeze is written at each newly pinned generator**, exactly as the tenth
   batch wrote it at the three it recorded: the UNDERLYING DATA FILE is not pinned and continues to
   re-derive; only the rendering of the document the user ruled from is fixed; **a later ruling can
   unpin any member in one edit.**
6. **★ RULING 2 OF THE STOP SITTING — §6 kind 3's tool identification, corrected of record.** Append
   a **dated correction** at §6 of `cowork_rulings_2026_08_16_preparation_return.md` naming
   **`tools/audit/gen_artifact_inventory.py`** as the writer of the census that kind 3 rules a
   re-deriving derived census, **the former wording preserved beside it (#12) and the ruled text
   never rewritten**. State the establishment: the two tools' outputs, read at the code.
7. **★ RULING 1b, WRITTEN AT THE TWO TOOLS IT IS ACTUALLY ABOUT — and this replaces step 6 of the
   superseded dispatch, which would have written both treatments at one tool.** At
   `gen_artifact_inventory.py`: it writes the census, which **keeps re-deriving** under §6 kind 3 of
   the 2026-08-16 record. At `gen_artifact_inventory_surface.py`: it writes the ratification document,
   whose **rendering is pinned** under Ruling 1. **State in terms at both sites that NEITHER RULING IS
   SUPERSEDED**, and that the pin fixes what the rendering reads without freezing the census.
8. **★ AND THE SURFACE GENERATOR'S TWO INPUT ROUTES ARE BOTH PINNED — the census file it reads AND
   the citation scan it performs over the tree.** A pin that fixes one route leaves the rendering free
   to move when the tree moves, which is the thing the pin exists to prevent. **If the tool's
   construction cannot pin both routes, that is a STOP-and-report** — report the measured shape and
   stop; do not improvise.
9. **The anchored-quote consequence is the F15 class and is cleared by its ruled treatment** —
   anchors re-aimed PER CITATION from the drift report, affected derivations regenerated under the
   ordinary enumerated-movement bound, `gen_cluster_dispositions.py --verify` the drift authority.
   **Any regenerated surface's own difference is MEASURED before it is accepted.**
10. **No verdict, fate, population or row moves**, and **no document is regenerated to a new
    content** — a member whose current rendering already differs from its committed one is
    **restored to its committed bytes and the difference reported**, never adopted.
11. Commit, push, verify at the object.

**Registered expectation E1:** per member, the derivation is published with its landing commit, its
candidates, its selection, its evidence and its interval, and every one of the four is re-derived at
the objects rather than inherited; every member whose commit is established is pinned and every
member whose commit is not is STOPped on and left UNRESOLVED; the four ruling records each carry a
dated correction with nothing rewritten and none of them cites D-230 for the bound; §6 kind 3 carries
its dated correction with the former wording preserved; Ruling 1b's two treatments stand at the two
tools that hold the two outputs, with neither ruling superseded; the surface generator's two input
routes are both pinned, or the task STOPs with the measured shape reported; no verdict, population or
row moves and `OPEN_ITEMS.md` is byte-unchanged.

## Task 2 — the four standing clauses, at ONE home, in ONE commit

**★ THE CONSOLIDATION IS THE DISPATCH'S CHOICE AND ITS GROUND IS STATED RATHER THAN LEFT TO BE
INFERRED.** All four clauses below land in `cowork_audit_protocol.md`'s dispatch-protocol section,
where D-250, D-251, D-252, D-431 and D-434 already live and which the tenth batch DERIVED as the
record's home for dispatch-authoring rules. Landing them in one edit clears the **F15 anchored-quote
class once instead of four times**, which is the whole reason for the shape.

1. **Ruling 1's forward clause of the tenth-return sitting:** *a ruling record taken from a generated
   document names the commit it was ruled at.* Record **F60** as its ground, with the measured
   instance — one of five route-A members carried the field — and the general form: *a rule whose
   operative clause names a field the records do not carry cannot be applied without amending either
   the rule or the records.*
2. **Ruling 1 of the STOP sitting:** *a dispatch's declared start state is stated at the tree the
   dispatch will meet, including the reds the dispatch's own inputs cause, each named with its
   cause.* Record **F67** as its ground, with the general form: *where a derivation's population is
   the file system rather than the index, an untracked file is already inside it.*
3. **Ruling 3 of the STOP sitting:** *a sitting record is an interim carrier, written in the turn its
   ruling is given and landed in git at the next dispatch's Task 0 — so the commit that lands it
   bounds when the sitting was held.* Record **F72** as its ground, with the general form: *a
   practice every session follows and every record cites is not thereby a rule, and a claim invoking
   a ruling as its authority must be checkable at that ruling's own text (D-643).* **This clause is
   what Task 1 step 3's corrections may cite once it stands.**
4. **Ruling 3 of the tenth-return sitting, beside D-431:** *a published character figure names the
   tool that produced it.* Record **F66** as its ground, and state that this is D-431's own missing
   half — D-431 says where a figure comes from, and this says that a reader must be able to see it.
5. **The anchored-quote consequence is cleared exactly as in Task 1**, once for all four clauses.
6. Commit, push, verify at the object.

**Registered expectation E2:** four clauses stand at the dispatch-protocol section, each with its
named ground and its general form; no other authoring rule moves; the drift authority reports every
re-aimed anchor correct at its new coordinate.

## Task 3 — Ruling 2 of the tenth-return sitting: the three stale banners corrected at their generators, in ONE commit

**This task runs AFTER Task 1 and the order is ruled, not preferred.** For any member Task 1 pins, a
change to its rendering afterwards is a deliberate, recorded act; run the other way round, this change
would land inside the rendering the pin is being taken over.

1. **The shape is IMPORTED from `gen_rulings_sort.py` (#6), which already carries it**, and the
   import is stated at each site so a later reader meets the precedent rather than three
   independently authored treatments. Write, at each of the three generators, ONE sentence qualifying
   the banner with **the sitting, its date, and the ruling record that carries it**, and **render it
   into the document in the same act**.
2. **The former banner is preserved (#12)** in the form the precedent uses: the rendered banner states
   what it replaces and that the former rendering stands in git at the commits that carried it.
   **Nothing is deleted.**
3. **Each document's own difference is MEASURED before it is accepted**, blob against blob by explicit
   hash, and **must be confined to the banner**. A difference reaching any verdict, population, entry
   or count in any of the three documents is a **STOP-and-report**.
4. **The mechanism freeze is answered at the site, not assumed away.** State why this is admissible: a
   governing surface stating something false about itself is #10's own doc-truth subject, and the act
   creates no new mechanism because the treatment is imported.
5. **The rulings-sort surface is NOT re-corrected** — it already carries the treatment. **Correct of
   record, in this batch's own findings, the tenth batch's characterisation of it as made *"by
   hand"***: your own STOP established at §6.e that the RULED banner text lives **at the generator**
   and was already there at `a21a55fc12`, so it survives regeneration by construction. **The former
   characterisation is preserved beside its correction (#12).**
6. Commit, push, verify at the object.

**Registered expectation E3:** three generators carry the qualification and three documents render it;
each preserves its former banner in the imported form; each document's difference is measured and
confined to the banner; the rulings-sort surface's treatment is reported as data with the tenth
batch's characterisation corrected; no verdict, population, entry or row moves; and for any of the
three surfaces Task 1 pinned, the difference this task makes is visible as a deliberate act rather
than as a silent regeneration.

## Task 4 — Ruling 4 of the STOP sitting, executing Ruling 3 of the tenth-return sitting, in ONE commit

1. **★ EXTEND `tools/audit/gen_session_start_read_size.py` BY TWO KEY CHAINS**, using the
   `key_span_characters` function it already carries, over the artifact it already reads:
   `★_the_live_gating_answer` → `the_gating_rows`, and `★_the_live_gating_answer` →
   `★_the_frozen_enumeration_measured_against_this_one`.
   **★ THE TOOL WORK IS DECLARED AS A JUDGMENT — at the site, in the commit message, and in the
   close — and never slipped in.** Its ground, stated at each: the standing mechanism freeze bars tool
   work *that does not block the work*, and Ruling 3 of the tenth-return sitting's ordered act is
   blocked without this, D-431 forbidding a transcribed value. This is the same admission the tenth
   batch declared at its own §9.
2. **The extension creates no tool and adds no check invocation**, so the new-tool rule does not
   arise and **the guard population must stand where Task 3 left it**. A run count that moves with no
   cause named is a **STOP-and-report**; a count that moves because a pin Task 1 applied added a check
   invocation discharges the new-tool rule in that task's own commit and is named there.
3. **★ THE RIDER RULING 4 ORDERS SETTLED, and it is settled at the artifact rather than by taste.**
   State whether *"the grounds"* as Ruling 3 of the tenth-return sitting names them is exactly the
   `the_gating_rows` span, or the 216 rows' recorded grounds within it. **If the two differ, the value
   published is the span the tool measures, with the key chain named beside it** — which is the
   standing figure clause working rather than an exception to it.
4. **A dated correction appended at BOTH sites** — §3 of `cowork_rulings_2026_08_17_ninth_return.md`,
   and the TWENTY-EIGHTH block of `cowork_handoff.md`. Each **quotes the values it corrects and
   preserves them (#12)**, names `tools/audit/session_start_read_size.json` as the authority, and
   states the corrected values **by citation to that artifact and never by transcription** (D-431).
   **Neither site's own text is rewritten**; a ruling record and a handover block are both corrected
   by appending.
5. **The values corrected are the section's and those derived from it**: the total, the section, the
   identity list, **the grounds and the frozen comparison** — all five, the last two now reachable by
   step 1. **State the arithmetic that closes it** — the whole gap in the total is one term, and every
   other member of the read agrees to the digit.
6. **★ THE PERCENTAGE IS WITHDRAWN AND NOT RECOMPUTED, and this half is unchanged from the superseded
   dispatch.** The live tool declares its shape does not reach the pre-ninth-batch regime, so a
   corrected percentage would be a hand computation across two different questions. State that the
   percentage is withdrawn and why, quoting the tool's own declaration. **The pre-ninth-batch total is
   withdrawn with it, on the same ground.** **The extension of step 1 does NOT reach that regime and
   no attempt is made on it.**
7. **Ruling 3 of the ninth-return sitting is NOT re-opened and the correction says so**: the saving is
   larger than that ruling claimed, not smaller, and its prediction holds against the measured value.
8. **`tools/audit/nongating_apparatus_rows.json` must be BYTE-UNCHANGED.** This task READS it and
   never writes it; a difference is a STOP-and-report.
9. **The anchored-quote consequence is cleared exactly as in Task 1.**
10. Commit, push, verify at the object.

**Registered expectation E4:** the generator derives all five values Ruling 3 names, by two key chains
added to the function it already carries, the tool work declared as a judgment at three sites; both
correction sites carry a dated correction with the former values quoted and preserved and nothing
rewritten; every corrected value enters by citation to the artifact; the percentage and the
pre-ninth-batch total are withdrawn with their reason stated and no value the tool cannot derive is
asserted; the guard count is unmoved or its movement is caused and named;
`tools/audit/nongating_apparatus_rows.json` is byte-unchanged.

## Task 5 — the close

One `STATUS.md` pointer entry per task — and, per the standing Ruling-4 bound of the
governing-surface split record, **the same act moves the TENTH batch's entries verbatim to
`STATUS_ARCHIVE.md`** (`gen_status_batch_bound.py`), both directions proven. **The declared
`Last updated: ` prefix adjustment is imported, not re-decided (#6); an entry needing a second
adjustment still halts.**

The FULL close is appended to `cowork_away_returns.md` as a **PREPARATION ELEVENTH BATCH** section:
**both guard states, every SHA, every registered expectation graded MET or NOT MET with its reason,
every declared departure, the tool-work judgment of Task 4 restated, and the carried findings
restated for the phase's retrospective — F1–F74, surfaced and none rowed; the row bar stands whole
this batch.** The report is `cc_report_preparation_eleventh_amended.md`, an ordinary tracked file —
**named so that it does not overwrite the STOP report Task 0 lands.** Commit, push, verify at the
object.

**★ THE E-ORDERING RULE, AT BOTH GRAINS.** The end-state guard run is taken AFTER the Task 5 commit
exists, and its output with the final SHAs lands in ONE FURTHER commit. **A batch-level summary
sentence written in the same commit as the work it summarizes is the same defect at a smaller
grain** — your last two batches AVOIDED the sentence rather than declaring it; do the same.

**Registered expectation E5:** at the tree carrying the close, a fresh full guard run re-derives —
**every run check passing except [[OI-372]]'s tool, zero STOPs** — run and read, never inferred, its
values committed only after the run that produced them.

## What this batch does NOT do

- **No curated boot list.** It stays at its ruled definition — the implementation-free read list an
  implementation-blind session boots from, for those sessions only — and remains a live, unblocked
  preparation output awaiting its own drafting and sitting.
- **No fate on any `CLAUDE.md` span ruled at the eighth-return sitting.** Both stay at site, that
  question is CLOSED, and `gen_claude_md_finer_archive.py --apply` is not run.
- **No archiving pass**, and no re-opening of Ruling 2 of the session-start-read sitting.
- **No change to rule (a).** Its pointer stands exactly as the tenth batch narrowed it, and
  `tools/audit/nongating_apparatus_rows.json` must be byte-unchanged at every task.
- **No candidacy acted on, no census re-pin.** No mandatory-read or boot listing moves in this batch,
  so the caller census's inputs are untouched; if any act would move one, that is a STOP-and-report
  and not a task to add.
- **No sweep of the audit generators for the F71 argument-parser class.** The class is recorded and
  unowned; it is not this batch's act.
- **No mining, no empirical findings ledger, no fact-gate admission**, no completion claim about the
  superseded phase-1 program, no derivation, design, repair or pilot act.
- **No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
  `tools/robust_stop/`, no measurement of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly as
  found; **[[OI-179]] stays OPEN and GATES**; F3 stays surfaced, unfixed and unrowed at thirteen
  times surfaced.
- **`gen_gating_row_sizing.py`'s frozen population is left alone.**
