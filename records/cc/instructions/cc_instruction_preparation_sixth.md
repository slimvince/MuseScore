# CC dispatch — the preparation phase's SIXTH batch: the governing-surface split EXECUTED under the four rulings of 2026-08-17

> **Dispatch (Cowork, 2026-08-17).** Executes the four rulings of
> `cowork_rulings_2026_08_17_governing_surface_split.md` — the per-class fates, the accepted
> doubt-defaulted share, the per-file archive companions, and the `STATUS.md` superseded-entry
> rule — over the surface `ratification_surfaces/cowork_governing_surface_split_2026_08_16.md`
> (committed at `c4f15a7b32`) and its two pinned measurement artifacts. **Written at a verified
> stop; nothing else is running.** The archiving wave for retirement-candidate DOCUMENTS, the
> empirical findings ledger, the fact-gate, the curated boot list, the mining, the landing of
> the newly visible files, the eight KIND-UNDERIVABLE callers, the prose-citation question, the
> residue surface's sitting and the rulings-sort sitting are NOT in this batch.
> **Read first (every session), UNDER THE RULED INTERIM READING SCOPE (§5(B) of
> `cowork_rulings_2026_08_16_preparation_return.md`):** `C:\s\MS\CLAUDE.md` — SKIPPING the
> blocks that state of themselves that they are historical or superseded; `C:\s\MS\STATUS.md`
> (header only); `C:\s\MS\BUILD_AND_TEST.md`. **NOTE: the scope EXPIRES when this batch's
> Task 1 lands** — the executed split makes the boundary physical; the close states that it
> has expired. **Then read, in full:**
> `cowork_rulings_2026_08_17_governing_surface_split.md` (the whole authority for Task 1);
> `ratification_surfaces/cowork_governing_surface_split_2026_08_16.md`;
> `cowork_rulings_2026_08_16_preparation_return.md` §5 (the pruning rulings this executes,
> including §5(D) continuous pruning and §5(E)'s test); your own
> `cc_report_preparation_fifth.md` §4 and §7 (the measurement, the pins, F29–F32); the
> `STATUS.md` archive-rule precedent (its own header rule and `STATUS_ARCHIVE.md`'s shape);
> `tools/open_items_split_check.py` (the reconciliation-check pattern Task 1 clause 7 orders).
> The standing bars bind this batch whole: **no `src/` edit, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the
> analysis, no design, no repair, no derivation of any specification, no mining, no document
> archived or deleted as a file, no decisions-register entry retired or revived.** The edits
> permitted are Task 1's, exactly as bounded. **Open-items rows: [[OI-370]]'s flip is
> permitted and ordered (clause 8); NO OTHER row is created, flipped or discarded.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the
> end; record both states.** The expected start state: **55 run, 54 passing, ONE failing**
> ([[OI-372]]'s tool, `gen_filing_convention_application.py --check`), 4 not run, 16
> historical, zero STOPs, no stale report. **A different start state is a STOP-and-report.**
> Verify every commit at the object and record the SHAs in the close. The reserved-word
> conventions and the ruled vocabulary replacements bind all new prose. **The new-tool rule
> stands.** **The E-ordering rule stands** (no graded value committed before the run that
> produced it — the rule that caught F32). **F3's reading rule stands**
> (`gen_cluster_dispositions.py --verify` is the drift authority; `reaim_home_anchors.py`'s
> exit code is never trusted).

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1 (per-class fates):** operative-rule-text and every doubt-defaulted span *"STAYS
  AT SITE"*; resolved-row, self-declared-historical-or-superseded, preserved-former-wording
  and defense-and-declined-alternatives *"ARCHIVE, each span moved VERBATIM to its archive
  home with a dated pointer left at the site; #12 satisfied by preservation
  elsewhere-with-record"*; the defense class *"NARROW ... a rule's PURPOSE is operative and
  stays at site"*; and *"the executing dispatch READS every span it archives rather than
  trusting its class"* (the F29 safeguard).
- **Ruling 2:** *"The wave executes on today's measurement"* — no finer pass; every
  doubt-defaulted span stays.
- **Ruling 3:** *"One archive companion per governing file: `CLAUDE_ARCHIVE.md`,
  `OPEN_ITEMS_ARCHIVE.md`, `DECISIONS_ARCHIVE.md` and `BUILD_AND_TEST_ARCHIVE.md`, each
  CREATED by the executing dispatch on the established pattern"*; *"`STATUS_ARCHIVE.md`
  continues as `STATUS.md`'s home"*; every pointer *"names its own companion file"*.
- **Ruling 4:** *"An entry is SUPERSEDED the moment a later batch's close exists. The site
  keeps only the latest batch's entries"*; the backlog clears at this dispatch; the bound is
  maintained forward at every close as an instance of §5(D).
- **§5(A) of the 2026-08-16 record, the executing shape:** *"verbatim byte-reconciled moves on
  the register-split precedent (#12 satisfied by preservation elsewhere-with-record),
  per-citation anchor remaps, every affected artifact regenerated under an
  enumerated-movement bound, and a permanent reconciliation check on the split-check
  pattern."*

## Premise ledger

*(No premise names a terminus commit — every FACT pins content, checked at content-addressed
objects. No premise asserts a count of changed passages; each states the CONTENT of a
difference — the F25 lesson.)*

- **FACT** — the fifth batch is complete, pushed and verified at the objects and at the values
  by the writing side. The span decomposition and the reader inventory are PINNED at
  `c4f15a7b32` (the fifth batch's F32 remedy):
  `tools/audit/governing_surface_spans.json` (1,502 spans over the five files, per-span class
  and evidence) and `tools/audit/governing_surface_readers.json` (per file: every naming,
  anchored citation, parsing tool, homed register entry). The artifacts are the authority
  (D-431); no count in this dispatch is typed from anywhere else.
- **FACT** — `STATUS.md` carries its own archive rule (a superseded entry moves to
  `STATUS_ARCHIVE.md`), applied twice before on the established both-directions
  byte-reconciling pass; `STATUS_ARCHIVE.md` exists; no other governing file has a companion.
- **FACT** — `DECISIONS.md` is a GENERATED surface (register rule (d)): it is never
  hand-edited; every change goes through `tools/audit/decisions/backbone_decisions.json` and
  `gen_decisions_register.py`.
- **ASSUMPTION A1** — the working tree carries exactly ONE tracked modification:
  `cowork_handoff.md` (content: the twenty-second session-close block inserted above the
  twenty-first, and the twenty-first heading carrying its demotion marker — two parts of one
  act). `cowork_rulings_2026_08_17_governing_surface_split.md` and
  `cc_instruction_preparation_sixth.md` are untracked, among the standing untracked records
  this premise does not enumerate. **Check ordered (Task 0, first act, blob against blob by
  explicit hash):** any tracked difference whose CONTENT is outside that shape is a
  STOP-and-report.
- **ASSUMPTION A2** — with the moves applied, the anchors remapped per citation and the
  affected artifacts regenerated under the bound, every red clears: the end state shows every
  run check passing except [[OI-372]]'s tool, zero STOPs. Every red the edits turn is classed
  against the PINNED reader inventory before it is cleared; **a red in a check the inventory
  does not name for the edited file is a STOP-and-report.**
- **ASSUMPTION A3** — the `DECISIONS.md` moves are expressible through the register's own
  mechanism (data file and/or generator change, rendered by the generator, its `--check`
  passing). **Check ordered:** a move the mechanism cannot express is a STOP, not a hand-edit
  of a rendered file.
- **ASSUMPTION A4** — every span the artifact classes into an archive class READS, on
  inspection, as its class says (the F29 safeguard, ordered by Ruling 1). **A span that does
  not is LEFT AT SITE, flagged doubt-defaulted-at-execution, and enumerated in the close** —
  the doubt default, not a batch STOP; the wave proceeds over the remainder.

## Task 0 — land the ruling record, the handover block and this dispatch

1. **A1's check first, entirely at content-addressed objects.**
2. Commit exactly these THREE paths and no fourth: `cowork_handoff.md` (modified),
   `cowork_rulings_2026_08_17_governing_surface_split.md` (new),
   `cc_instruction_preparation_sixth.md` (new). All staged plainly; push; verify at the
   object.

**Registered expectation E0:** exactly 3 paths at `git diff-tree`; one modification whose
content matches A1's stated shape, two additions; no staging override.

## Task 1 — the split EXECUTED, complete, in ONE commit

All of the following in ONE commit, citing the four rulings:

1. **The four companions created** — `CLAUDE_ARCHIVE.md`, `OPEN_ITEMS_ARCHIVE.md`,
   `DECISIONS_ARCHIVE.md`, `BUILD_AND_TEST_ARCHIVE.md` — each opening with a short header
   naming its parent, the creating act and the rulings, on the `STATUS_ARCHIVE.md` shape.
2. **The moves, derived from the pinned spans artifact and READ before moving (A4):** every
   span classed resolved-row / self-declared-historical-or-superseded /
   preserved-former-wording / defense-and-declined-alternatives moves VERBATIM to its
   parent's companion, in parent order, each move leaving at the site a compact dated
   supersession pointer naming the companion (the §5(D) pointer shape). Nothing else in the
   parent changes on the same lines. The `CLAUDE.md`, `OPEN_ITEMS.md`, `STATUS.md` (its two
   former-wording spans) and `BUILD_AND_TEST.md` moves are direct edits; **the `DECISIONS.md`
   moves go through the register's own mechanism (A3)**, the companion receiving the same
   verbatim text.
3. **`STATUS.md` under Ruling 4:** every dated batch entry EXCEPT the fifth batch's moves
   verbatim to `STATUS_ARCHIVE.md` under the file's own rule, one dated pointer left in the
   body noting the clearing act and the new standing bound (not one pointer per entry — the
   file's own rule already names the archive; state this deviation from clause 2's per-span
   pointer shape in the close).
4. **Per-citation anchor remap after the edits:** `gen_cluster_dispositions.py --verify` is
   the drift authority; every drifted anchored verbatim re-aimed per citation; end state
   `cited line numbers correct` at the full population. The register's 87 `CLAUDE.md` homes
   and 1 each in `OPEN_ITEMS.md` / `BUILD_AND_TEST.md` are the expected drift surface; a home
   whose SPAN was archived (not merely shifted) is a STOP — no operative span may have moved.
5. **Every affected artifact regenerated under the enumerated-movement bound:** movement ONLY
   in line coordinates, per-class counts, or populations whose subject is a moved span or an
   archived resolved row; every moved value enumerated in the close and classed; **any other
   movement is a STOP-and-report** (A2). The parsing tools the pinned inventory names (4 for
   `CLAUDE.md`, 9 for `OPEN_ITEMS.md`, 2 for `DECISIONS.md`, 1 for `STATUS.md`) all run at
   the edited tree; a parser halting on the new shape is a STOP, not an improvised tool edit.
6. **The interim reading scope's expiry recorded:** the close states that §5(B)'s scope
   expired when this commit landed, per its own ruled terms.
7. **The permanent reconciliation checks, on the `tools/open_items_split_check.py` pattern:**
   one check per parent/companion pair proving both directions — every archived span
   byte-present in the companion exactly once, and parent + companion reconstructing the
   parent's pre-act committed blob modulo the dated pointers and (for `STATUS.md`) later
   appends under its own rule. Registered under the new-tool rule with `--check` forms, in
   the same commit.
8. **[[OI-370]]'s open half discharged through the register's ordinary mechanism:** the INDEX
   row flipped with provenance naming this act, the detail file gaining its dated resolution
   note. **This is the one permitted row act.**
9. **The full guard set at the edited tree (A2's check)**, then ONE commit, push, verify at
   the object.

**Registered expectation E1:** per file, moved + stayed accounts for the pre-act committed
blob TO THE CHARACTER against the pinned spans artifact's per-class byte counts (the pointers
and companion headers enumerated separately as the act's own additions); every archived span
byte-preserved, proven by the clause-7 checks; `gen_decisions_register.py --check`,
`gen_cluster_dispositions.py --verify`, `tools/open_items_split_check.py` and all four new
checks pass; every moved artifact value enumerated and classed inside the bound; A4's
left-at-site flags enumerated (zero is a valid count); no operative or doubt-defaulted span
moved.

## Task 2 — the close

One `STATUS.md` pointer entry per task — **and, per Ruling 4's standing bound, the same act
moves the fifth batch's entries to `STATUS_ARCHIVE.md`**, so the file ends holding this
batch's entries only. The FULL close appended to `cowork_away_returns.md` (a PREPARATION
SIXTH BATCH section) — both guard states, every SHA, every expectation graded, the per-file
arithmetic to the character, every A4 flag, every problem declared, the carried findings
restated for the retrospective (F1–F32, the E3 ordering defect, the A1 premise error —
surfaced, none rowed; the row bar stands except clause 8's ordered flip). The report
`cc_report_preparation_sixth.md`, an ordinary tracked file. Commit, push, verify at the
object. **The E-ordering rule:** the end-state guard run is taken AFTER the Task 2 commit
exists, its output and the final SHAs landing in ONE FURTHER commit. *(The pruning-measurement
tools and the census classification are PINNED at their commits — the F32 remedy — so this
close does not invalidate them; a red in either is a real finding, not the F32 shape.)*

**Registered expectation E2:** at the tree carrying the close, a fresh full guard run
re-derives — every run check passing except [[OI-372]]'s tool, zero STOPs, the four new
reconciliation checks among the passing set — run and read, not inferred, its values
committed only after the run.

## What this batch does NOT do

- **No document is archived, moved or deleted AS A FILE** — this batch moves SPANS between
  files the record keeps; the retirement/archiving wave for whole documents stays behind its
  own ruled gates, untouched.
- **No sole-carrier member discarded; the 62 not ruled; the residue and rulings-sort surfaces
  stand awaiting their sittings; the eight KIND-UNDERIVABLE callers and the prose-citation
  question stay open.**
- **No finer pruning pass** (Ruling 2 declined it); no span moved that the pinned artifact
  does not class into an archive class; every doubt-defaulted span stays.
- **No empirical findings ledger, no fact-gate admission, no curated boot list. No
  completion claim of any kind about the superseded phase-1 program.**
- **No derivation of any specification, no design, no repair, no pilot act, no `src/` edit,
  no golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement
  of the analysis.**
- **No open-items row created, flipped or discarded except [[OI-370]]'s ordered flip.**
  [[OI-372]] and [[OI-374]] stay exactly as found; [[OI-179]] stays OPEN and GATES;
  `reaim_home_anchors.py`'s F3 defect stays surfaced, unfixed and unrowed.
