# CC INSTRUCTION — THE CLOSE OF THE DECISION-RULES BATCH (2026-09-21)

**What this batch is.** It is **the completion of the batch
`records/cc/instructions/cc_instruction_decision_rules_consolidation_2026_09_21.md` started and did
not finish**, not a new batch. That batch ran its Tasks 0 to 5, left its work on disk, and stopped at
its own §7(d) closing guard condition before its third commit and its push. *(The Cowork side
re-checked four of its landings at the objects — D-658's corrected home, the register row, the
`CLAUDE.md` insertion and the two ref files — and each was as reported; the rest is relayed from that
batch's report and is proved again at §3 and §5 here.)* **This batch
re-derives the three artifacts that batch's own ordered acts staled, proves nothing else moved,
commits, and pushes.** It edits no governing document, writes no new `STATUS.md` entry, and re-aims
no forward bound — that work is already done and committed to disk by the batch this one closes.

**THE WRITING SIDE'S RESTRAINT, declared.** This dispatch is final at hand-over. The writing side
will not touch it, nor any file it names, while this batch runs.

---

## 0. The three states this dispatch is written knowing

**(a) THE REFS DISAGREE, AND `master` HAS MOVED SINCE THE LAST DISPATCH.** `.git/refs/heads/master`
reads **`6b5bdfc38145befd865feab9436e6ff0dc32cea0`** — the second of the two Task 0 commits the
previous batch made — and `.git/refs/remotes/origin/master` reads
**`ef4fad940d806edf8f84eb9a895f88d70a9bbcf2`**, unmoved. Both were read at the ref files with the
file tools by the Cowork side at this sitting. **The push at §4 is what closes that gap.**

**(b) THREE GUARDS ARE RED, AND THEY HAVE ONE CAUSE — NOT TWO.** `claude_md_rule_triage.py --check`,
`gen_rulings_sort.py --check` and `decisions/gen_reads5_repack.py --check` each moved PASS → FAIL at
the previous batch's closing capture. **All three read `tools/audit/decisions/backbone_decisions.json`
and nothing else that batch touched**, so all three were staled by its Task 4 backbone edit — the
D-658 field changes and the 41 re-aimed home anchors.

> **★ THIS CORRECTS THE PREVIOUS REPORT, AT THE OBJECT.**
> `records/cc/reports/cc_report_decision_rules_consolidation_2026_09_21.md` §6.4 attributes
> `claude_md_rule_triage.py`'s move to Task 2's `CLAUDE.md` insertion. **That tool never reads
> `CLAUDE.md`.** It opens exactly two files: `BACKBONE = tools/audit/decisions/backbone_decisions.json`
> (declared line 42, loaded line 646) and its own artifact `OUT = tools/audit/claude_md_rule_triage.json`
> (lines 753 and 759). Its population is the register entries whose home is `CLAUDE.md`, taken from
> the backbone; Task 2 allocated no register entry, so it cannot have staled that artifact. **Read at
> the tool source by the Cowork side at this sitting. Confirm it at 1(a) and report what you find.**

**(c) THE RATIFICATION SURFACE IS NOT A BAR, AND WHY IS SETTLED AT THE RECORD RATHER THAN ASSUMED.**
`gen_rulings_sort.py` writes both `tools/audit/rulings_sort_classification.json` (line 114) and
`ratification_surfaces/cowork_rulings_sort_surface_2026_08_16.md` (lines 115–116), and its `main()`
writes both on a bare run with no mode that writes one without the other. The previous batch stopped
partly on that. **The record answers it**, on three grounds the Cowork side read at the objects:

1. **All three tools are enrolled in the guard set with `--check`** — `tools/audit/gen_guard_state.py`
   lines 104, 368 and 903. The project has already decided their artifacts must re-derive at all
   times, so restoring them after a legitimate input change is the mechanism working.
2. **The surface's own banner** reads *"STATUS: RULED, 2026-08-17. NOTHING IS EXECUTED BY IT"*, says
   the user ruled it at `cowork_rulings_2026_08_17_rulings_sort_sitting.md` and that *"this rendering
   carries the ruled state rather than the proposal"*, and names itself *"Generated by
   `tools/audit/gen_rulings_sort.py` from `tools/audit/rulings_sort_classification.json`, which it
   writes in the same run."* **It is a generated rendering of a ruled state, not a proposal awaiting
   him.**
3. Its modification time stands about three minutes after the last backbone write and the last
   register regeneration, so **an earlier batch already regenerated it as ordinary maintenance after
   a backbone change.** *(That last is a timing inference by the Cowork side, offered as
   corroboration and not as the ground; grounds 1 and 2 are read at the objects.)*

**WHAT IS NOT SETTLED AND IS NOT DECIDED HERE: whether any VERDICT in the sort may move.** It may
not. **Task 2 proves it, and a moved verdict is a STOP.**

---

## 1. Bars

**B1 — NO TOOL SOURCE IS EDITED BY THIS BATCH, AT ALL.** Not one. Every tool named below is RUN,
never modified. If a task seems to need a tool edited, that is a bar contradicting a task — **STOP
and report it; do not resolve it.**

**B2 — NO GOVERNING DOCUMENT IS AMENDED.** Not `CLAUDE.md`, not `cowork_audit_protocol.md`, not
`STATUS.md`, not `STATUS_ARCHIVE.md`, not `DECISIONS.md`, not `ARCHITECTURE.md`, not `FRAMEWORK.md`,
not `OPEN_ITEMS.md`. **The previous batch's edits to them are already on disk and are committed here
UNCHANGED** — §3 orders each one's unchangedness proved.

**B3 — `tools/audit/decisions/backbone_decisions.json` IS NOT EDITED.** It is final. Its content is
the input the three generators re-derive from, and §2(a) proves it has not moved since the previous
batch left it.

**B4 — NO REGISTER REGENERATION.** `gen_decisions_register.py` and `gen_cluster_dispositions.py` are
run in their check modes only, and only where the guard set runs them. **A bare run of either is a
STOP.** The register is already regenerated and verified clean by the previous batch.

**B5 — `tools/audit/gen_derivation_boot_pack.py` IS NOT EDITED AND IS NEVER RUN BARE.** It runs only
inside the guard set, in `--check`. Nothing is authored into `WITHHELD`, `EXTRAS`, `VERDICTS`,
`CRITERION` or `FROZEN`. **Nothing under `tools/audit/derivation_boot_pack/` is read for content,
written, deleted, renamed or moved.**

**B6 — `tools/audit/gen_withheld_family_reading.py` IS NOT EDITED AND IS NOT RUN.** The previous
batch found that its lines 146–147 assert the superseded no-recommendation clause by D-658's own
identity into a reading surface it generates. **That is a live finding standing with the user and it
is NOT repaired here.** Touching it would edit a tool source (B1) and rewrite a generated reading
surface this batch has no business in.

**B7 — `tools/audit/claude_md_finer_archive.json` is held back**: not staged, not reverted, not
investigated.

**B8 — NO OPEN-ITEMS ROW is created, flipped or discarded. NO `D-NNN` is allocated.** Register rule
(c) is still suspended at `cowork_register_rule_c_suspension_2026_08_28.md` and its owed-entries list
is left exactly as it stands.

**B9 — no `src/` file, no build, no test, no golden, no score corpus, nothing under `tools/corpus/`
or `tools/robust_stop/`, no measurement of the analysis, no paper, no reading-pass extract, no
score.**

**B10 — NO FIGURE IS TRANSCRIBED INTO ANY ARTIFACT FROM THIS DISPATCH (D-431).** The figures this
dispatch states are exactly these, each a START-STATE BAR and none of them written anywhere: the two
ref hashes at 0(a), and the two commit hashes at 2(a).

**THE FOOTPRINT ASSUMPTION, written from the paths this batch's own tasks touch.** This batch's own
orders **modify** exactly four existing files — `tools/audit/claude_md_rule_triage.json`,
`tools/audit/rulings_sort_classification.json`,
`ratification_surfaces/cowork_rulings_sort_surface_2026_08_16.md` and
`tools/audit/decisions/reads5_repack.json` — plus `tools/audit/guard_state.json` as the ordinary
consequence of running the guard set. It **creates** exactly one new file: your report. **Every
other path §3 commits is already on disk from the previous batch and is committed unchanged.**

---

## 2. Task 0 — the start state

**0(a) — pin this dispatch:**

```
git hash-object -w records/cc/instructions/cc_instruction_decision_rules_close_2026_09_21.md
```

Record the hash and report it.

**0(b) — the refs, expected to DISAGREE.** Read both ref files **with the file tools** (`D-253`;
never `git rev-parse`). **`master` must read `6b5bdfc38145befd865feab9436e6ff0dc32cea0` and
`origin/master` must read `ef4fad940d806edf8f84eb9a895f88d70a9bbcf2`.** If either has moved, **STOP**:
something ran that this dispatch does not know about. Report both as found.

**0(c) — the working tree.** Take the enumeration with `python tools/audit/changed_paths.py` and
report it whole. **Nothing may be staged**; if anything is, report exactly what and STOP.

**The previous batch's report §7 lists the tracked paths it left modified, by name, plus its own two
untracked files.** *(RELAYED from that report, which the Cowork side read whole; this side took no
enumeration, and it puts no count on that list — the report's own prose and its own block disagree by
one, `claude_md_finer_archive.json` being in the block and outside the count.)* **Compare what you
find against that list member by member and report any difference.** A path that batch named and you
do not find, or one you find that it did not name, is a **STOP** — it would mean something touched
the tree between the two batches.

**0(d) — the corruption check**, on the text files this batch will commit and no others. A trailing
NUL byte or a final line broken off mid-word is a **STOP**. **Check no binary and nothing in the
held-back untracked population.**

**0(e) — the opening guard capture.** Run the guard set once, save the capture **outside** the
working tree, name it in your report, and record every verdict. **Expected, and reported as found
rather than assumed:** the same three guards red that the previous batch's closing capture named, and
`gen_cluster_dispositions.py --verify` green. *(RELAYED from that report §6.4.)* **If what you find
differs, report it and carry on** — it is not a STOP, but it changes what §2's comparison means and
must be said plainly.

---

## 3. Task 1 — establish the cause at the tool sources before re-deriving

**This task edits nothing and runs no generator. It is the check that the repair is aimed right.**

**1(a) — the three tools' inputs, read at their own sources.** Report, for each, the lines you read:

1. **`tools/audit/claude_md_rule_triage.py`** — report every file path it opens. **The Cowork side
   found exactly two: the backbone and its own artifact, and NO read of `CLAUDE.md`.** If you find a
   read of `CLAUDE.md`, the correction at 0(b) of this dispatch's head is wrong — **report it and
   carry on**; it changes the diagnosis, not the repair.
2. **`tools/audit/gen_rulings_sort.py`** — confirm `BACKBONE` at line 110, `OUT` at 114 and `SURFACE`
   at 115–116, and that `main()` writes both `OUT` and `SURFACE`.
3. **`tools/audit/decisions/gen_reads5_repack.py`** — confirm `BACKBONE` at line 94 and `OUT` at 95.

**1(b) — the backbone is final and has not moved.** Take
`tools/audit/decisions/backbone_decisions.json`'s identity by the content-addressed route
(`git hash-object -w --no-filters`, then `git cat-file -s`) and report both. **Then run
`python tools/audit/decisions/reaim_home_anchors.py --check` and confirm it reports `anchors drifted: 0`
and does NOT print `REFUSED`.** Either result otherwise is a **STOP**: the backbone is not in the
state the previous batch left it in, and nothing here may be re-derived from it.

**1(c) — the rulings sort's committed verdicts, captured BEFORE anything is re-derived.** Read
`tools/audit/rulings_sort_classification.json` as it now stands and record **its per-entry classes
and its totals**. Name the field you read the class from. **This is the baseline Task 2 compares
against, and it is taken now because after the run it is gone.**

---

## 4. Task 2 — re-derive the three artifacts, and prove no verdict moved

**Run these three bare, in this order, and report all output and all exit codes verbatim:**

```
python tools/audit/claude_md_rule_triage.py
python tools/audit/decisions/gen_reads5_repack.py
python tools/audit/gen_rulings_sort.py
```

**2(a) — ★ THE VERDICT PROOF, WHICH IS THE CONDITION ON THIS WHOLE BATCH.** Compare the regenerated
`tools/audit/rulings_sort_classification.json` against 1(c)'s baseline, **entry by entry**.

> **NO ENTRY'S CLASS MAY CHANGE, AND THE TOTALS MAY NOT MOVE.** The classes are `DESIGN-INTENT`,
> `IMPLEMENTATION-MANAGEMENT` and `NEEDS-THE-USER`. **Any entry whose class differs, or any movement
> in the totals, is a STOP** — report which entry, its old and new class, and stop there. **Do not
> revert and do not resolve it.**

**Why this is the condition.** The user ruled that sort on 2026-08-17 and the surface renders the
ruled state. The previous batch changed D-658's `title`, `verbatim`, `plain`, `rationale` and
`status_source`, and re-aimed 41 `home` anchors — **it changed no entry's `home_is_layer_spec` and no
entry's `nonspec_kind`, which are the two fields that tool's rule leans on first.** So the expected
diff is rendered text only, D-658's among it, with every verdict standing. **A moved verdict would
mean a regeneration had altered something the user ruled, which is the one outcome this batch may not
produce.**

**2(b) — what the surface's diff actually contains.** Report the diff of
`ratification_surfaces/cowork_rulings_sort_surface_2026_08_16.md` at the level of what changed —
which entries' rendered text moved, and the confirmation that its STATUS banner, its three ruling
paragraphs and its totals are unchanged. **If the banner or a ruling paragraph moved, STOP.**

**2(c) — the three checks, green.** Then:

```
python tools/audit/claude_md_rule_triage.py --check
python tools/audit/decisions/gen_reads5_repack.py --check
python tools/audit/gen_rulings_sort.py --check
```

All three must exit 0. **Any one that does not is a STOP**, reported whole.

**2(d) — the closing guard capture**, taken after 2(c), saved outside the working tree and named in
your report.

> **THE CONDITION, and it is a comparison in both directions.** Against **0(e)'s** capture: **the
> three named guards must move FAIL → PASS**, and **no guard that was PASS at 0(e) may carry any
> other verdict.** A guard that was red at 0(e) for an unrelated reason and stays red is **carried
> and reported**, not chased. **No condition is written on any guard's printed output or on any count
> inside it.**

**`tools/audit/gen_guard_classification.py` is EXPECTED to STOP** on the same three tools with no
authored verdict, two of them older debt. **Run it in its own order, report it whole, and CARRY ON.
It is not a STOP for this batch and it is not repaired here.**

---

## 5. Task 3 — the commit and the push

**3(a) — ONE COMMIT, by explicit path, never a directory pathspec.** The candidate set is exactly the
nineteen paths the previous batch left modified, plus this batch's four re-derived artifacts, plus
the two dispatches and two reports:

1. `CLAUDE.md`
2. `cowork_audit_protocol.md`
3. `tools/audit/decisions/backbone_decisions.json`
4. `DECISIONS.md`
5. every `decisions/group_*.md` the enumeration reports as modified — **name each in your report
   rather than assuming which**
6. `STATUS.md`
7. `STATUS_ARCHIVE.md`
8. `tools/audit/gen_status_batch_bound.py`
9. `tools/audit/status_batch_bound.json`
10. `tools/audit/session_start_read_size.json`
11. `tools/audit/defense_share.json`
12. `tools/audit/guard_state.json`
13. `tools/audit/claude_md_rule_triage.json`
14. `tools/audit/rulings_sort_classification.json`
15. `ratification_surfaces/cowork_rulings_sort_surface_2026_08_16.md`
16. `tools/audit/decisions/reads5_repack.json`
17. `records/cc/instructions/cc_instruction_decision_rules_consolidation_2026_09_21.md`
18. `records/cc/reports/cc_report_decision_rules_consolidation_2026_09_21.md`
19. `records/cc/instructions/cc_instruction_decision_rules_close_2026_09_21.md`
20. `records/cc/reports/cc_report_decision_rules_close_2026_09_21.md`
21. any further guard artifact the enumeration reports as modified — **name each**.

**★ ITEMS 1 TO 11 ARE COMMITTED UNCHANGED BY THIS BATCH, AND THAT IS PROVED RATHER THAN ASSERTED.**
For each, confirm its content is what the previous batch left — this batch amends no governing
document (B2) and no backbone (B3). **Report any that differ; a difference is a STOP.**

**Item 12, `tools/audit/guard_state.json`, IS moved by this batch** — the two guard captures at 0(e)
and 2(d) write it, which the footprint assumption names. It is committed as this batch changed it,
not as unchanged.

**Prove the staged set is EXACTLY the candidate set and nothing else before committing.**

**HELD BACK, AND NAMED SO NOTHING SWEEPS THEM IN:** `tools/audit/claude_md_finer_archive.json`; and
the standing untracked population — the `scratch_artifacts/` tree, the two PDFs, `Claude outputs/`,
`Codex research inventory/`, `docs/research_papers/polyph9-release/` and the untracked `.mscx` under
`tools/audit/derivation_exemplars/l0-l1/`. **Confirm each is absent from the staged set.**

**3(b) — push.** Push every branch that received a commit. `origin` is the fork; `upstream`'s push is
disabled and is not to be used. **This push closes the gap the refs have carried since the
L2-withheld-documents batch: after it, read `.git/refs/remotes/origin/master` with the file tools and
confirm it equals `master`.** *(If the loose ref file is absent, git may have written the value into
`.git/packed-refs`; report where you read it from — that is not a failure.)* **If the push fails for
any reason, REPORT IT; never pass `--force`.**

---

## 6. The report

`records/cc/reports/cc_report_decision_rules_close_2026_09_21.md`, carrying:

1. Task 0's pin; both refs as found; the enumeration compared against the previous batch's
   nineteen-path list with any difference named; the corruption check; and the opening capture with
   every verdict.
2. **Task 1's three source readings**, and in particular **what `claude_md_rule_triage.py` actually
   opens** — the correction this dispatch makes to the previous report, confirmed or refuted at the
   object.
3. Task 1(b)'s backbone identity and the zero-drift, no-`REFUSED` proof.
4. **Task 2's verdict proof in full** — the baseline totals, the regenerated totals, and the
   statement that no entry's class moved, with the field you compared on named.
5. Task 2(b)'s account of what the surface's diff contains, and the confirmation that its banner,
   ruling paragraphs and totals are unchanged.
6. The three `--check` runs; the closing capture compared verdict by verdict in both directions; and
   the guard classification's result, reported whatever it is.
7. The commit's hash, the proof that items 1–12 went in unchanged, the pushed branches, and
   `origin/master` after the push.
8. **What you did NOT do**, named rather than counted — no tool source edited, no governing document
   amended, no backbone edit, no register regeneration, the boot pack untouched and never run bare,
   `gen_withheld_family_reading.py` neither edited nor run, no open-items row, no `D-NNN`, and the
   rule (c) suspension left as found.

**WHAT GOES TO THE USER, named rather than counted:** whether any verdict moved in the ruled sort
(expected: none); whether `claude_md_rule_triage.py` reads `CLAUDE.md` (expected: no, which corrects
the previous report); and the confirmation that the two refs finally agree.

**If any expected result does not appear, stop at that task and report it. Do not continue to the
next one, and do not resolve a bar that contradicts a task.**
