# CC dispatch — one licensed `src/` comment edit so OI-315 can close, and two gating rows brought to truth

> **Status: ACTIVE DISPATCH, written 2026-08-13 (Cowork), at a verified STOP** — the OI-150 batch
> completed, both commits verified at the objects by explicit hash, no forbidden path. **OI-150 is
> flipped and absent from the finish line: the first gating row closed by work in this arc.**
> Nothing is running.
>
> **★ A COWORK D-253 LAPSE IS DECLARED AND BELONGS IN THIS BATCH'S CLOSE.** Cowork used
> `git merge-base --is-ancestor <hash> origin/master` in nearly every verification this arc and
> reported *tip on origin/master* as established. That is a **branch-tip read**, which D-253's own
> words say is never trusted for what is current, and Cowork never declared it. No working-tree
> file was read through a shell. **Record it in the close as Cowork's**, on the same shape CC used
> for its own `git log -1`; it is not rowed — a past behavioural lapse with no live consumer
> discards under amended #10. **Push state is CC's to establish from here on.**
>
> **Read IN FULL, and read FIRST:** `open_items/OI-315.md` and `open_items/OI-321.md` END TO END,
> and both INDEX rows; the false-statements pass's own close in `cowork_away_returns.md`.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_oi315_licence_and_cells.md`. Acts dated
> from the clock; **no positional count anywhere**; cite rulings by number, not by date.
>
> **★ Standing rules as adopted, with ONE narrow exception stated at §0a.** D-253 in every dialect.
> NO TRANSCRIBED VALUES (D-431). Hold-don't-guess. **No golden, no corpus of scores, nothing under
> `tools/corpus/` or `tools/robust_stop/`, no measurement, no design, no behaviour change.** D-231
> and #8 stand. **Phase 1's completion statement is not written, not drafted and not partially
> written.** Commit and push per task boundary; `origin` only.

## 0a. THE LICENCE — its exact reach, and it reaches nothing else

**The user ratified ONE `src/` comment edit**, on a surface with three options, their principles and
their objective ratings. **The ground is the freeze's own exception**: a code defect is rowed and
left *unless it blocks the work*, and this one blocks a gating row from closing, which is phase 1's
completion. **Precedent, not a new shape:** the record carries exactly two prior licensed
code-comment acts, each on its own user ruling.

**THE LICENCE REACHES:** the single `src/` comment OI-315 names as its third further surface —
the one still stating the removed piece-start shortcut as live — **and nothing else in `src/`,
ever, under this dispatch.**

**IT DOES NOT REACH:** any other comment, any code line, any file OI-315 does not name, and any
statement whose falsity is a **homing** question rather than a falsity in place (#7) — that STOPs.

## 0b. THE PREMISE LEDGER (#17a)

**FACT — verified by Cowork at the objects.** The OI-150 batch's two commits and their changed
paths, by explicit hash. That the piece-start shortcut is removed at HEAD was established at the
code by the false-statements pass and is not re-established here.

**FACT — reported by CC and NOT verified by Cowork:** that OI-315's third surface is a `src/`
comment the freeze blocks; that OI-321's cell opens *NOT corrected* while two of three items are
corrected; that neither detail file records the acts. **All three are checked below.**

**ASSUMPTION — each checked BEFORE the act resting on it; a refutation is a STOP.**

- **A1.** The site is **located at the code from OI-315's own text**, not assumed. *Check: the file
  and the comment are identified and quoted before editing. If OI-315 names no such site, or names
  one that no longer carries the statement, STOP.*
- **A2.** **The diff is comment-only, verified MECHANICALLY** — every changed line under `src/`
  begins with a comment marker — not by eye and not by intention (#15). *Check: the mechanical
  verification is run and its result reported. **If the correction cannot be made comment-only,
  STOP.***
- **A3.** The two cells are brought to what is **true at HEAD**, established by reading each row and
  the acts' own commits, **former wording preserved in place (#12)**. *Check: neither cell asserts
  more than the commits support.*
- **A4.** A flip happens **only** where a row's own closing conditions are met. *Check: state which
  condition each half met; if any is unmet, the row stays open and the report says which.*

## 0c. THE TASKS, IN ORDER

**Task 1 — the licensed comment edit (A1, A2). FIRST**, so the cells are written once against a
settled state rather than twice. One comment, mechanically verified, former wording preserved.
Commit and push.

**Task 2 — the two rows brought to truth (A3, A4).** Correct OI-315's and OI-321's INDEX cells to
what is true at HEAD, and **record the acts in both detail files** — they currently live only in
commits, and a session choosing what to work next reads the cell. **Flip OI-315 if and only if its
closing conditions are now met**; report OI-321's remaining item and leave it open. Commit and push.

**Task 3 — the close.** One `STATUS.md` pointer entry per task, nothing else in that file. Append
the close to `cowork_away_returns.md`, **including Cowork's declared D-253 lapse** per the banner.
Report at the objects, **with commit hashes**.

## 0d. WHAT IS DELIBERATELY NOT DONE

**No second `src/` edit under any justification.** No sweep for siblings of either row's defect — a
suspected sibling is reported. No golden, no test change, no measurement. OI-274's banner half and
the `docs/scoring_model.md` pass are ratified for later and are not started; OI-207 is last and
alone. CC's two standing findings — the stale triage clauses and the two discard-record routes —
remain held. OI-179 stays OPEN and GATES.

## 0e. STOP RULES

Halt with a STOP in `cowork_away_returns.md` if: OI-315 names no such `src/` site, or the site no
longer carries the statement; the correction cannot be made comment-only; the mechanical
verification does not pass; the falsity turns out to be a homing question; a cell cannot be brought
to truth without asserting more than the commits support; or a guard goes red for a cause that is
neither this dispatch's own edits nor already recorded.

---

*Provenance: Cowork, 2026-08-13. The licence was ratified on a surface carrying three options, each
pro and con naming its principle and each rated against the ultimate objective. Self-check run
before release (D-434).*
