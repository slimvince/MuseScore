# CC dispatch — CLOSE THE FRAMEWORK PHASE, SECOND WRITING: clear the guard reds, rename the ratified framework document, execute the six ratified retrospective edits, prepend the eighty-first entry, land everything. Boot NO session.

> **★ THIS IS THE ACTIVE DISPATCH, THE REVALIDATED SECOND WRITING. Dated dispatch note, 2026-08-30:
> written and released the same day. It carries no `⏸ PARKED` banner and none is to be added.** It
> may go straight to a session.
>
> **★ IT SUPERSEDES `cc_instruction_phase_close_2026_08_30.md`, which STOPPED at Task 0 on its own
> declared-start-state clause** — six failing guard verdicts measured against five declared
> (`cc_report_phase_close_2026_08_30.md`, read whole by the writing side). **The stop was correct.
> The first writing is not re-sent; it is landed by this batch as the record of what was written.
> Three things change here and nothing else:** A2 declares SIX failing with the sixth's cause
> quoted; E-final is derived from six; and the interim guard runs of Tasks 3 and 4 declare in
> advance the one red they will see. Every ruling, task, bar and proof of the first writing is
> otherwise carried unchanged.
>
> **★ THE PIN ORDER.** Suggested opening instruction to the executing session: *"pin
> `cc_instruction_phase_close_second_2026_08_30.md` to a blob with `git hash-object -w`, then read
> it from that object, and execute it."* Where the session read this file from the working tree
> first, declare it, pin at Task 0, and prove before staging that the blob has not moved.
>
> **★ WHO WROTE IT.** The Cowork writing side of the fifty-ninth session, 2026-08-30, with ONE
> counted error this session — **an earlier dispatch's acceptance criterion contradicted its own
> assumptions and footprint** (owned in the eighty-first entry). The first writing's five-figure
> start state is NOT counted a second error: it was internally consistent, measured as ordered, and
> its own clause stopped the batch — the declared-start-state mechanism working, per the record's
> precedent. **This dispatch's acceptance criteria are DERIVED from its declared start state plus
> its footprint, task by task.** Read the bars sceptically and STOP on a contradiction rather than
> resolving it in this dispatch's favour. The writing side declares it touches no file this batch
> lands or moves while the batch runs.
>
> **★ WHAT THIS BATCH IS.** The framework phase's closing acts, every one resting on a user ruling
> quoted in the premise ledger: Task 1 adds the USER-RULED signature row and clears the
> artifact-inventory STOP; Task 2 regenerates the evidence-pin membership artifact — **named in the
> footprint this time** — and clears the fourth red; Task 3 performs the RULED RENAME to
> `FRAMEWORK.md` with a ratification stamp; Task 4 executes the six RATIFIED retrospective edits
> (P-1…P-6); Task 5 prepends the eighty-first handoff entry; Task 6 lands everything and pushes;
> Task 7 closes. **It designs nothing, derives nothing, measures nothing of the analysis, and
> begins nothing of the next phase.**
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md` —
> CONDITIONAL, and this batch does NOT meet the condition: rule (a)'s `gating_ids` only.
> **(2)** `cowork_handoff.md`'s CURRENT ENTRY (the eightieth) and **the staging file
> `cowork_handoff_entry_eighty_one.md` WHOLE** — it is the entry this batch prepends and it names
> everything this batch touches. **(3)** `cc_report_third_landing_2026_08_28.md` in full — its
> §3.4 and §12 are two of the three reds this batch clears — AND
> `cc_report_phase_close_2026_08_30.md` in full — the first writing's stop report, whose §4 is the
> third red's established cause. **(4)**
> `cowork_rulings_2026_08_29_ratification_sitting.md` whole — the rulings this batch executes.
> **(5)** `cowork_framework_phase_retrospective_2026_08_29.md` whole — P-1…P-6, whose exact edit
> texts Task 4 carries. **(6)** `cowork_audit_protocol.md`'s dispatch-protocol section in full.
> **You do NOT open:** any sealed placement-sample file; the workbook
> (`external resarch summary/external research.xlsx`), in any portion; `ARCHITECTURE.md` beyond
> what the ordered edits below name (none); `cowork_framework_document_draft_2026_08_28.md`'s BODY
> beyond the banner block Task 3 stamps and the byte-identity proofs.
>
> **The standing bars bind this batch whole, with the ordered carve-outs EXCEPTED BY NAME** (the
> seventy-eighth entry's lesson — a bar must except on its face what its own tasks order):
> no `src/` change; no test changed, moved or run; no golden; no build; no measurement of the
> ANALYSIS; no design; no derivation of any specification statement; **NO SESSION BOOTED**; no
> open-items row created, flipped or discarded; no decisions-register entry written and no `D-NNN`
> allocated. **Tool sources:** none edited EXCEPT `tools/audit/gen_artifact_inventory.py` (Task 1,
> the user-ruled signature row, that row and nothing else) and
> `tools/audit/gen_status_batch_bound.py` (Task 7, the per-batch re-aiming, the named standing
> carve-out under Ruling 5 of `cowork_rulings_2026_08_26_amendment_landing_sitting.md`).
> **Governing documents:** none edited EXCEPT the exact ordered edits of Task 3 (the `FRAMEWORK.md`
> banner stamp), Task 4 (`CLAUDE.md` Conventions — one addition; `cowork_audit_protocol.md` — four
> additions; `DEFECT_TYPES.md` — two instance additions at DT-20) and Task 5 (`cowork_handoff.md`,
> the additions-only prepend). **Every edit is ADDITIONS-ONLY except the two named regenerations
> and the rename; no former wording is deleted anywhere (#12).**
>
> **Commit-and-push per task-group as ordered below. Run the FULL guard set BEFORE the first act
> (CHECK mode) and again at the end (write mode, committed as the artifact of a real run). Verify
> every commit at the object by explicit hash. Every shell command carries `; echo "exit:$?"`.
> `git status` and a working-tree `git diff` are denied by the armed guard — use the per-path
> explicit-hash forms and `tools/audit/changed_paths.py`.**

## Premise ledger — every ruling this batch executes, quoted

**★ THE WRITING SIDE HAS NO SHELL AND ASSERTS NO GIT-OBJECT VALUE.** Establish the tip at the
object; this dispatch states no value for it.

- **RULING — the signature row (the user, 2026-08-30).** Put to the user with the remedy CC's
  report names — *"one authored signature row naming the shape this file has, decided over the
  tool's whole population both ways, plus a regeneration of `tools/audit/artifact_inventory.json`"*
  (`cc_report_third_landing_2026_08_28.md` §12.1) — and with the writing side's recommendation to
  rule it. **The user's word, verbatim: "Yes, write the closing dispatch."** That is the mechanism
  ruling D-436/D-657 reserve to him, and Task 1 is its execution.
- **RULING — the rename (the user, 2026-08-29, Ruling 5 of
  `cowork_rulings_2026_08_29_ratification_sitting.md`).** The framework document is renamed at
  ratification; **the target name, his word verbatim: "FRAMEWORK.md"**; a git move, history
  preserved, dated records citing the old name never rewritten.
- **RULING — the retrospective (the user, 2026-08-29, Ruling 5).** His word verbatim: "ratified".
  P-1…P-6 move to their named homes by executing acts; **P-2 and P-3 subsume backlog items 9 and
  10.**
- **RULING — the ratification itself (Ruling 2)** — the ground for Task 3's banner stamp.
- **FACT — the guard set's DECLARED START STATE: SIX failing** — the three known
  (`gen_filing_convention_application.py --check`, `decisions/apply_soft_discard.py --check`,
  `decisions/apply_residue_discard.py --check`); `gen_evidence_pin_membership.py --check` (red for
  the mechanism the third report's §3.4 establishes: the 2026-08-29 sitting record entered the
  file-system population after the artifact was last regenerated);
  `gen_artifact_inventory.py --check` (the STOP of the third report's §12.1: the landed workbook
  matches no signature rule); **and `gen_session_start_read_size.py --check`, whose cause the stop
  report's §4 establishes at the git objects: the third landing batch's further commit moved
  `STATUS.md` — a measured term of that artifact — AFTER the artifact was last regenerated, and no
  measurement was re-run.** **Measure it; a SEVENTH failing verdict at the start is a
  STOP-and-report.**
- **FACT — six untracked root-level documents this batch lands or consumes exist**, read at the
  files by the writing side: `cowork_rulings_2026_08_29_ratification_sitting.md`,
  `cowork_research_list_disposition_surface_2026_08_29.md`,
  `cowork_framework_phase_retrospective_2026_08_29.md`,
  `cc_instruction_phase_close_2026_08_30.md` (the superseded first writing, landed as record),
  `cc_report_phase_close_2026_08_30.md` (its stop report, landed as record), and the staging file
  `cowork_handoff_entry_eighty_one.md` (which begins with its own `---` and ends with a blank
  line, inserted whole and unchanged).

- **ASSUMPTION A1 — the working tree, by SHAPE and never by count.** No tracked modification is
  expected at Task 0; **any found is a STOP-and-report.** The untracked population includes the
  four documents above, this dispatch, and the historical population earlier reports enumerated.
  Enumerate with `tools/audit/changed_paths.py` and report before acting.
- **ASSUMPTION A2 — the guard state at the start** is the six-failing FACT above, measured.
- **ASSUMPTION A3 — this batch's footprint is exactly what its own orders move:**
  `tools/audit/gen_artifact_inventory.py` and `tools/audit/artifact_inventory.json` (Task 1);
  `tools/audit/evidence_pin_membership.json` (Task 2); `FRAMEWORK.md` created and
  `cowork_framework_document_draft_2026_08_28.md` removed by the move, with the one banner stamp
  (Task 3); `CLAUDE.md`, `cowork_audit_protocol.md`, `DEFECT_TYPES.md` (Task 4);
  `cowork_handoff.md` prepended and the staging file deleted (Task 5); the three 2026-08-29
  documents and this dispatch landed (Task 6); and in Task 7 `STATUS.md`, `STATUS_ARCHIVE.md`,
  `tools/audit/status_batch_bound.json`, `tools/audit/gen_status_batch_bound.py` (the named
  carve-out), `tools/audit/session_start_read_size.json`, the report, and
  `tools/audit/guard_state.json` at the end-state commit. **Movement caused by this batch's own
  orders at any other path is a STOP-and-report; a movement no order of this batch caused is
  reported and graded, never absorbed.** ***One declared uncertainty:*** the writing side cannot
  establish whether the Task 3 rename or the Task 4 insertions shift a line-anchored citation some
  register check verifies. **A check that reddens at the Task 4 guard run BECAUSE of an ordered
  edit is a STOP-AND-REPORT — repair nothing unordered — and E3 grades it as such.**

## Task 0 — pin, then establish. Nothing is written in this task.

1. **PIN THIS INSTRUCTION and the staging file** (`git hash-object -w` each), report both blobs,
   take later reads of both from the objects. Declare a pre-pin working-tree read if one happened;
   prove both blobs unmoved before staging.
2. **Establish the tip** at the object, and `origin/master` beside it.
3. **Run the FULL guard set in CHECK mode**; grade A2 (six failing, no seventh).
4. **Enumerate with `tools/audit/changed_paths.py`**; grade A1.

**E0:** both blobs reported; the tip and `origin/master` at the object; A1 and A2 graded from
measurement.

## Task 1 — the USER-RULED signature row; the artifact-inventory STOP clears

1. Read `tools/audit/gen_artifact_inventory.py`'s signature table and its class vocabulary.
2. **Add ONE authored row** classifying `external resarch summary/external research.xlsx` — scoped
   to that folder's contents and no wider, **using the table's EXISTING class vocabulary; if no
   existing class fits the file's nature (an external research-input document), that is a
   STOP-and-report, never a new class.** Report the row verbatim.
3. **The D-657 derivation, both ways, before the row is accepted:** regenerate
   `tools/audit/artifact_inventory.json` and prove at the regenerated artifact that (a) the
   workbook is now classified by the new row, and (b) **no other file's classification moved** —
   the artifact's difference against the committed object carries the workbook's entry and nothing
   else. A second movement is a STOP-and-report.
4. `python tools/audit/gen_artifact_inventory.py --check` — **must PASS, exit 0.**

**E1:** the row verbatim in the report; the both-ways proof; the check passing.

## Task 2 — the membership artifact, footprint-named, regenerated; the fourth red clears

1. Regenerate `tools/audit/evidence_pin_membership.json`; **MEASURE its difference against the
   committed object before accepting it.** Expected movement: the 2026-08-29 sitting record
   entering the membership (and its read-count field), checked at both routes the derivation
   reaches its inputs by; movement beyond that is reported, not absorbed.
2. `python tools/audit/gen_evidence_pin_membership.py --check` — **must PASS, exit 0.**

**E2:** the measured difference as predicted; the check passing.

## Task 3 — the RULED RENAME, with the ratification stamp

1. **`git mv cowork_framework_document_draft_2026_08_28.md FRAMEWORK.md`** — prove the blob at the
   new path equals the blob at the old path at the parent commit (a pure move, zero content
   change).
2. **THEN one ADDITIONS-ONLY banner stamp at the head of `FRAMEWORK.md`**, inserted ABOVE the
   existing banner block, deleting nothing (#12):
   > **★ RATIFIED by the user 2026-08-29 — Ruling 2 of
   > `cowork_rulings_2026_08_29_ratification_sitting.md`; the decomposition, charters and boundary
   > contracts govern. Renamed from `cowork_framework_document_draft_2026_08_28.md` to
   > `FRAMEWORK.md` at ratification per Ruling 5. The banner below is preserved as written at
   > drafting; its "DRAFT / NOT RATIFIED" words describe the drafting moment, not the present.**
   Prove the edit additions-only at the blob-to-blob difference: insertions only, ZERO deletion
   lines. **No other character of the document is touched — this batch does not author the
   framework document.**
3. **Run the FULL guard set in CHECK mode. The EXPECTED result at this run, derived in advance:
   the three known failing checks plus `gen_session_start_read_size.py --check`** — the latter red
   from the start state and REMAINING red by design until Task 7's ordered regeneration (its
   measured inputs move again under this batch's own ordered edits). **That one red is declared
   here and is NOT a STOP.** A check newly failing BECAUSE of the rename (a tool referencing the
   old file name) is a **STOP-and-report** — the reference is not edited.

**E3 (this task's limb):** the move proven pure; the stamp proven additions-only; the guard run's
result graded against the declared expectation, any rename-caused red stop-reported.

## Task 4 — the six RATIFIED retrospective edits, each ADDITIONS-ONLY

1. **P-1 → `CLAUDE.md`, Conventions section**, appended as a new bullet at the END of the
   Conventions bullet list:
   > - **THE ORDINARY SESSION-START READ BINDS EVEN WHEN THE OPENING INSTRUCTION NAMES A SINGLE
   >   FILE (ratified 2026-08-29, Ruling 5 of `cowork_rulings_2026_08_29_ratification_sitting.md`;
   >   the framework phase retrospective, P-1).** A single-file opening instruction is not an
   >   exemption from the standing conventions: the reads rule (a) and the register sections
   >   mandate are performed before the named file is acted on. *Evidence:* the fifty-eighth
   >   session skipped the read on that ground and counted five errors of one shape; the
   >   fifty-ninth performed it and counted one.
2. **P-2, P-3, P-4, P-5 → `cowork_audit_protocol.md`**, appended as four new standing clauses at
   the end of the dispatch-protocol section, each headed `**STANDING CLAUSE (ratified 2026-08-29,
   the framework phase retrospective, P-n):**` with these contents, verbatim in substance:
   - **P-2:** every dispatch is pinned to a blob at Task 0 as the standing form; the pin is
     ordered in the user's opening line where possible; where the session read the file first,
     the declared-departure route applies — declare it, pin, prove the blob unmoved before
     staging. *(Subsumes backlog item 9.)*
   - **P-3:** a footprint assumption is stated as the list of the batch's own ordered acts, never
     as a blanket; movement no order of the batch caused is reported and graded, never absorbed
     and never auto-stopped; **and an acceptance criterion is DERIVED from the declared start
     state plus the footprint, never written independently of them** — the fifty-ninth session's
     counted error is the founding instance of that last clause. *(Subsumes backlog item 10.)*
   - **P-4:** a disposition given in conversation does not exist for the record's mechanisms until
     a ruling record lands on disk; a sitting's record is written in the same session, at the
     first ruling, and extended per ruling while the sitting runs.
   - **P-5:** before the writing side commits any file to the tree, it establishes whether a batch
     is running — the expected report's absence and the ref's freshness are the cheap checks.
3. **P-6 → `DEFECT_TYPES.md`**, at the existing DT-20 entry, an ADDITIONS-ONLY note naming the two
   2026-08-28 instances: the session brief whose §8 bar could only be obeyed by a reader who had
   already breached it, and the pin order performable only from outside the file that states it —
   each with a one-line pointer to its record.
4. **Run the FULL guard set in CHECK mode. The EXPECTED result, derived in advance: the three
   known plus `gen_session_start_read_size.py --check`** — the latter still red, and moved AGAIN by
   this task's own ordered `CLAUDE.md` insertion (a measured term of that artifact), cleared by
   Task 7's ordered regeneration. **That one red is declared and is NOT a STOP.** A check newly
   failing BECAUSE of an ordered insertion (an anchor shifted under a cited line) is a
   **STOP-and-report** — nothing is repaired unordered.

**E4:** each edit proven additions-only at the blob-to-blob difference, zero deletion lines
everywhere; the guard run's result graded against the declared expectation, any insertion-caused
red stop-reported.

## Task 5 — prepend the eighty-first entry

1. Insert `cowork_handoff_entry_eighty_one.md`'s content **whole and unchanged, immediately after
   the blank line following `cowork_handoff.md`'s title line** — the eighty-first entry sits where
   the eightieth sat, and the eightieth follows it. **PROVE ZERO deletion lines at the blob-to-blob
   difference. Any deletion is a STOP-and-report and nothing is committed.**
2. **THEN DELETE the staging file** — a plain removal of a never-committed file, the only deletion
   this batch performs; its content survives in the handoff and at its Task 0 blob.
3. Establish the handoff's entry count at the object with the loose pattern
   `^## .*COWORK SESSION CLOSE`, the pattern named beside the figure; this dispatch asserts no
   count.

**E5:** additions-only proven; the staging file gone; the count established with its pattern.

## Task 6 — land everything, PUSH

1. **LAND, each exactly as it stands** (pinned before staging, blob re-established at the commit
   object): `cowork_rulings_2026_08_29_ratification_sitting.md`,
   `cowork_research_list_disposition_surface_2026_08_29.md`,
   `cowork_framework_phase_retrospective_2026_08_29.md`,
   `cc_instruction_phase_close_2026_08_30.md` (**the superseded first writing, as record — NOT
   run**), `cc_report_phase_close_2026_08_30.md` (**its stop report, as record**), and
   `cc_instruction_phase_close_second_2026_08_30.md` (this dispatch) — **plus every tracked
   modification Tasks 1–5 produced**, all in ONE commit.
2. **REPORT, AND DO NOT LAND, any other untracked root-level file** matching `cowork_*.md`,
   `cc_instruction_*.md` or `cc_report_*.md` — with the carve-out that
   `cc_report_phase_close_2026_08_30.md` does not exist yet and is committed by Task 7.
3. **Commit; the subject, exactly:**
   `record: close the framework phase — FRAMEWORK.md ratified and renamed, the ratification sitting and retrospective landed, the two guard reds cleared, the six ratified protocol and convention clauses homed, and the eighty-first handoff entry prepended`
4. **PUSH**; verify at the object, and at `git ls-remote` as a second route.

**E6:** every named path at the commit blob-identical to its pin; the prepend and edits carried;
`origin/master` at the commit by two routes.

## Task 7 — the close

1. One `STATUS.md` pointer entry per task-group, per the OI-222 pointer convention (**D-431**);
   the forward bound applied through `gen_status_batch_bound.py --apply` (the named carve-out);
   `gen_session_start_read_size.py` regenerated; dates carry the day the batch RAN.
2. The report is **`cc_report_phase_close_2026_08_30.md`**: both guard states, every SHA, E0–E6
   graded, A1–A3 graded, declared departures, and the plan lines — the first of which is: **the
   framework phase is CLOSED; the detail-specification phase is next and is the user's sitting to
   open; this batch begins nothing of it.**
3. The close does not assert the end state; one further commit carries it, and where the report's
   SHA table cannot carry its own committing identities, the further commit fills exactly those
   cells, as the previous batches did and declared. **★ THE STALENESS LESSON, APPLIED TO THIS
   BATCH'S OWN FURTHER COMMITS (the stop report's §4, its general form): any further commit that
   moves `STATUS.md`, `CLAUDE.md` or `DECISIONS.md` regenerates
   `tools/audit/session_start_read_size.json` IN THE SAME COMMIT and declares it** — a
   measured-input edit is never left stale for the next batch's start state.
4. Commit, push, verify; then the further commit(s) with the end-state guard run.

**★ E-FINAL, DERIVED as P-3 requires — from the declared start state (SIX failing) plus this
batch's ordered acts (Task 1 clears the artifact-inventory STOP; Task 2 clears the membership red;
Task 7's ordered `gen_session_start_read_size.py` regeneration, run after every measured-input
edit of Tasks 3–5, clears the read-size red; nothing else this batch orders moves a guard's
subject):** at the end state, a fresh full guard run in write mode, committed only after the run
that produced it, reporting **the three known failing checks and no others, zero STOPs** —
**UNLESS a Task 3 or Task 4 guard run stop-reported an unexpected ordered-edit red (a rename
reference or an anchor shift), in which case E-final is graded with exactly that red named and
attributed, and nothing else new.**

## What this batch does NOT do

- It does not open the workbook, in any portion, and renames no folder.
- It does not author the framework document: Task 3's stamp is the one ordered banner insertion,
  proven additions-only, and the body is untouched.
- No repair beyond the two ordered guard clearances; the parked register dispatch untouched; the
  §8 move not performed; no open-items row, no finding number, no register entry (the suspension
  route stands); the quarantined audit questions untouched; the historical untracked population
  untouched (a user question); nothing of the detail-specification phase begun.

## The writing side's self-check over this dispatch

1. *Principles:* **#12** — every edit additions-only, zero deletions except the ordered
   staging-file removal whose content survives twice; the rename is a pure move. **#14** — every
   consequential act executes a quoted user ruling. **#6** — each ruling lands at its one ruled
   home. **#17f / D-431** — no count or git-object value asserted; every figure ordered measured
   with its producer named.
2. *Conventions:* American English; no self-invented label; music-theory words appear only in the
   record's own qualified compounds.
3. *The counted error answered structurally:* E-final is DERIVED from the start state plus the
   footprint, with the conditional limb for stop-reported ordered-edit reds — the criterion cannot
   contradict A2/A3 by construction.
4. *File-tools rule:* the writing side read only through the file tools and ran no shell command
   of any kind; this dispatch relays no git-object value.
5. *Uncertainty:* the anchor-shift question is declared in A3 and handled by STOP-and-report, not
   assumed away; the signature row's class fit is handled the same way.
