# CC dispatch — the sizing artifact regenerated so the user-blocked population is current, and one discard overruled

> **Status: ACTIVE DISPATCH, written 2026-08-11 (Cowork), at a verified STOP** — the previous batch
> completed, three commits pushed, verified by Cowork at the objects: no forbidden paths and
> `guard_state.json` absent from the changed paths as reported. Nothing is running.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_sizing_regen_and_readme.md`. Acts dated
> from the clock; commit messages via `-F`; **no positional count anywhere** (D-307, D-432).
>
> **★ All standing rules as adopted.** D-253 in every dialect, and the previous batch's own lesson:
> a shell path and a file-tool path may name different places, so a surprising read is stale until
> proven otherwise. NO TRANSCRIBED VALUES (D-431). Hold-don't-guess. **Read-only on the analysis: no
> `src/` edit, no behaviour change, no golden, no corpus of scores, nothing under `tools/corpus/` or
> `tools/robust_stop/`, no measurement built or run, no design.** D-231 and #8 stand. **Phase 1's
> completion statement is not written, not drafted and not partially written.** Commit and push per
> task boundary; `origin` only.

## 0a. THE USER'S RULINGS THIS EXECUTES, taken 2026-08-11

1. **The size guard stays PROPOSED and untaken.** Ruled, on a surface with both options, their
   principles and their objective ratings; the ground was the freeze as written, capacity to the
   analysis, and OI-370 already recording the condition. **Nothing is owed by this ruling** — it
   confirms the status quo, and the row already says the guard is proposed and untaken. It is
   recorded here so a later session does not re-open it as an unanswered question.
2. **The sizing artifact is regenerated** so the user-blocked population is current.
3. **CC's discard of the scratch README's false statements is OVERRULED and the README is
   corrected instead.**

## 0b. WHY THE DISCARD IS OVERRULED — the ground CC could not have

CC's ground was that no derivation reads `cowork_scratch_2026_08_11/`, verified at `tools/`. That
establishes there is no **machine** consumer, and it is correct as far as it goes. **The consumer is
a session.** `cowork_handoff.md`'s entry-point block names that README in its prescribed
session-start read, so a session performing the read it is instructed to perform would learn that
the §3 question is unresolved and the settling act was not performed — **both false at HEAD** — and
could carry that into the design conversation the pack exists for. That is amended #10's limb (a),
so the finding is worth fixing rather than discarded. **CC's reasoning was sound and its evidence
incomplete; the correction is to the verdict, not to the method.**

## 0c. THE PREMISE LEDGER (#17a)

**FACT — verified by Cowork at the objects.** The previous batch's three commits, their paths and
push state. The handoff's entry-point block naming the README in its reading order. The sizing
artifact's `by_blocker` field carrying its user-ruling population, and its per-row `what_blocks_it`
entries, read at the artifact.

**ASSUMPTION — checked before the act resting on it; a refutation is a STOP.**

- **A1.** The sizing artifact is **regenerated by its own generator**, never hand-edited, and its
  population is derived from the record at HEAD. *Check: it re-derives; if the generator cannot run
  or its inputs are stale, STOP rather than patching.*
- **A2.** Rulings 61, 62, 63 and 64 and the OI-141 sitting **may** have discharged several of the
  user-blocked acts. **This is Cowork's reading and is NOT asserted** — the regeneration establishes
  it. *Check: report which user-blocked acts the regenerated artifact now carries and which it no
  longer does; a count unchanged from before is a correct outcome and not a failure.*
- **A3.** The README correction preserves the former wording in place (#12) and changes nothing
  else in the file. *Check: the former sentences are present and marked superseded.*

## 0d. THE TASKS, IN ORDER

**Task 1 — regenerate the sizing artifact (A1, A2). FIRST.** It is D-671's class — a derivation over
a derived population, published whole or not at all — so it cannot absorb a capacity shortfall.
Regenerate; report the user-blocked population before and after, and **per act that left it, what
discharged it.** Adjust nothing to produce a movement. Commit and push.

**Task 2 — correct the scratch README (A3).** Its two statements — that the §3 question is surfaced
and unresolved, and that the settling act was named and not performed — were true when written and
are false at HEAD; the pack's rewritten §7 settles the question. Correct them, **former wording
preserved in place (#12)**, and note that the pack's §7 is where the answer lives. Nothing else in
the file changes; the other drafts are untouched. Commit and push.

**Task 3 — the close.** One `STATUS.md` pointer entry per task, nothing else in that file. Append
the close to `cowork_away_returns.md`, recording the overruled discard with the ground at §0b so the
reasoning is retrievable. Report at the objects.

## 0e. WHAT IS DELIBERATELY NOT DONE

No row is written, no gating verdict moves, no route out of the abandoned derivation is taken. The
size guard is not built. **Phase 1 does not become nearer completion by this dispatch** — the
user-blocked population becomes accurately described, which is a different thing. OI-179 stays OPEN
and GATES.

## 0f. STOP RULES

Halt with a STOP if: the sizing generator cannot run or its inputs are stale; a member of the
population moves for a reason the regeneration does not explain; the README's former wording cannot
be preserved in place; or a guard goes red for a cause that is neither this dispatch's own edits nor
already recorded — the two standing reds are recorded and are not that.

---

*Provenance: Cowork, 2026-08-11, at the verified STOP following the previous batch. Self-check run
before release (D-434).*
