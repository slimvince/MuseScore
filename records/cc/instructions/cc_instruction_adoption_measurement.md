# CC instruction — the adoption measurement (the OI-178 protocol executes; measurement + the adoption record, NO adoption act)

> **The ruling is IN (user, 2026-07-20): ★R = A1 — the gate amendment is ratified and recorded
> on `cowork_prefit_gates.md` (read it there; the amended condition governs this measurement).
> This file is DISPATCHED.**
>
> **Read first (every session):** `C:\s\MS\CLAUDE.md` (gate block (A) — the robust stop and its
> re-baseline discipline), `C:\s\MS\STATUS.md` (header + newest entries),
> `C:\s\MS\BUILD_AND_TEST.md`, **the OI-178 row and the adoption protocol in
> `cowork_prefit_gates.md`**, and the Task-C record (`8416b2c84c`, `020baca347`).
>
> **Current state:** branch `master` at `020baca347`, in sync with origin (verify). Cowork-
> authored uncommitted doc edits ride your commit: `OPEN_ITEMS.md` (the option-1 ruling bracket
> on the OI-184 row), `cowork_handoff.md`, `cowork_prefit_gates.md` (the ★R amendment once
> ruled). **MEASUREMENT ONLY: no production wiring, no golden refresh, no `tools/robust_stop/`
> re-baseline, NO adoption commit — this dispatch produces THE ADOPTION RECORD for the user's
> ratification.** The one permitted copy: the O-12 snapshot (a plain copy of the outgoing
> `tools/robust_stop/` reference into a dated snapshot subdirectory, per the protocol's
> sequence). Pinned instruments import-only. Suites + snapshots green and untouched.

**Dispatch author:** Cowork, 2026-07-20, at the user's direction, after the option-1 ruling on
the reader-skew finding. **What this is:** the pre-ratified architecture-adoption protocol's
measurement phase — A's decode, from its own fact adapter (the production path), measured on the
full covered corpus against the committed reference, with every PASS condition evaluated and the
whole record assembled for the user's adoption ruling.

## ★R — the key-HOME pass-condition conflict (user ruling required; surfaced BEFORE the measurement, per the gate-governance principle)

The ratified PASS condition (i) requires A's key-agreement to exceed the baseline beyond the
confidence interval on BOTH key columns — local AND home. The evidence in hand says the home
half cannot pass and, as now understood, should not be asked to: a decoder that correctly
follows modulations disagrees with the HOME key wherever the music has left it, by construction
(A: local 77.60 / home 56.46 at CV; the current system: home 71.42/67.83/70.65 — it is rewarded
on that column for under-following modulations, the very defect A exists to fix). The
never-modulate failure the home column would guard against is guarded directly by the
modulation-rate check (A: 5.52 changes/piece vs ground truth 5.28). Options: **A1** — amend
condition (i): key-LOCAL must exceed the baseline beyond the interval on every preset; key-HOME
is TRACKED with a mandatory explained decomposition (including the computed GT self-agreement
ceiling — the fraction of graded duration where the ground truth's own local key equals its home
key, the honest maximum a local-follower can score on the home column), and the modulation-rate
guard becomes a PASS condition: A's key changes per piece within a declared band around the
ground truth's (0.75×–1.25× of 5.28). **A2** — keep the condition as ratified: the measurement
will fail on home regardless of every other result, forcing either A's rejection on a
metric-construction artifact or a renegotiation on the live diff — the exact outcome the
gate-governance principle exists to prevent, which is why this is surfaced now.
**RULING: A1 (user, 2026-07-20; a third option — a ceiling-margin home condition — was also
presented and declined as planting an unestablished constant in a hard gate). The amended
condition text lives on the gates document; evaluate the PASS table against IT.**

## Task 1 — the written adoption predictions (recorded in this file BEFORE measuring; Cowork, per the prediction discipline)

Against the committed reference (per preset, duration-weighted): **class-(b) root-disagree
duration: NET DECREASE on every preset, predicted magnitude 10–30 %** (A's CV root-agree 72.97 vs
the baselines' ~66); **the run-level set-diff is LARGE in both directions** (A segments
differently, so run boundaries move wholesale — the aggregate criterion exists for exactly
this); **key-local: +8 to +13 points over every preset baseline; key-home: 12–18 points BELOW
the baselines, with the decomposition explaining it against the GT self-agreement ceiling; RN:
+12 to +16; modulation rate within the ★R band.** The root improvement is large by the
architecture's own asymmetric expectation — it appeared at the probe stage, was reviewed, and is
carried as known rather than as a fresh surprise. Any result outside these bands: the record
still assembles, the miss is flagged prominently, and the user's ruling weighs it.

## Task 2 — the measurement

1. **O-12 snapshot** of the outgoing `tools/robust_stop/` reference (copy only).
2. **A's full-corpus decode** from its own fact adapter (the production path, selected weights,
   §5 tie-break), all 326 covered pieces, rendered to the a8-comparable region stream through
   the retained measurement chain (the probe grading path — reuse, don't rebuild).
3. **The robust-unit comparison, per preset:** A's root-failing runs against the committed
   per-preset enumerations — net class-(b) duration delta; the complete run-level set-diff with
   EVERY added run classified (a)/(b) per the unchanged two-tier policy and every added
   class-(b) run individually diagnosed (which factor mis-carried; an undiagnosable one is a
   STOP); class-(a) tracked with the standing investigate threshold. Key columns (home AND
   local), RN, and the abstention flag (must read 0) beside. Piece-bootstrap intervals on every
   headline delta.
4. **The ★R items:** the GT self-agreement ceiling computed and reported; the modulation-rate
   band evaluated.
5. **Timing** at production scale (the tractability record for the wiring decision).

## Task 3 — the adoption record (the deliverable)

One generated dossier (`tools/joint_estimator/adoption_record.*` + a readable summary): every
PASS condition with its verdict; the predictions beside the measured values; the set-diff with
classifications and diagnoses; the ceiling decomposition; the snapshot provenance; what the
adoption commit WOULD contain (the wiring point, the re-baselined reference, the preset note:
the analysis becomes preset-independent at the inference layer per the ratified mode decision —
presets remain presentation concerns; the retirement map's first steps) — assembled so the
user's ratification is of a complete, verifiable record. **No adoption act in this dispatch.**

## Commit

**One commit** (measurement + record + snapshot + the riding Cowork doc edits; this file
force-added). Push **origin only** (the standing hard stop on the upstream remote).

## Self-check before reporting (standing rule)

No production file, golden, or reference artifact modified (the snapshot is additive); the
firewall grep-proven (nothing adjusted against any measured number — this dispatch fits
nothing); every figure generated; fold/provenance stamps throughout. **Report:** the PASS-
condition table with verdicts; the prediction-versus-measured table; the set-diff summary with
the added-class-(b) diagnoses; the ceiling decomposition; timings; anomalies — surprises
reported, never built around. The funnel then returns for the user's adoption ruling.
