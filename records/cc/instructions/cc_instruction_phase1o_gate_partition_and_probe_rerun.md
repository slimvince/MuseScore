# CC dispatch — phase 1o: the phase-3 gate partition, the non-gating declaration, and the transposition probe re-run

> **Status: ACTIVE DISPATCH. Re-issued 2026-08-03 (Cowork) with a premise ledger**, under the user's
> ruling of the same date: *gathering facts or acting on facts — that is what we do, only.* The gate
> at `CLAUDE.md` #17(a) has no outside; its "inference-affecting design" scope clause was the concern
> live when it was written, never a decision that other work may rest on unchecked claims.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_phase1o_gate_partition_and_probe_rerun.md`.
>
> **★ THIS DISPATCH ASSERTS NO FIGURES** (D-431) and, from this issue on, **asserts no STATE it has
> not read.** Every load-bearing claim below is either **FACT** with a citation to the object the
> claim is about, or **ASSUMPTION** with an ordered check that must run before the act it licenses.
> There is no third form. If you find an unlabelled claim, it is a defect — report it.
>
> **★★ NARROWING THE PHASE-3 GATE DOES NOT OPEN IT.** Task 2 partitions what the family design waits
> on. It does **not** authorize the family design to begin, and licenses no fix, no inference change,
> no `src/` edit. If you find yourself designing a remedy for anything, stop.

## 0. THE PREMISE LEDGER (#17a)

**FACT — read at the object by Cowork in the session that wrote this dispatch:**

- **F1.** The struck-versus-sounding family is rowed at **OI-215, OI-226, OI-227, OI-228, OI-243,
  OI-244, OI-246, OI-277** — `OPEN_ITEMS.md`, those rows.
- **F2.** D-231 makes phase 3 wait on phase 2's exhaustion **in full** — `CLAUDE.md`, Conventions,
  the three-phase rule.
- **F3.** *One fix is designed once over the whole enumerated family, never per symptom* — `CLAUDE.md`,
  Conventions, user-ruled 2026-07-28.
- **F4.** The open-items register's rule **(b)**: *a stage may not open while a register item gating it
  is open* — `CLAUDE.md`, the open-items register section.
- **F5.** OI-243's probe was run by a sub-agent with an agent-written script, is established only by
  its own self-check and the bit-exact conditions, and **the row's FIRST action is a repository-side
  re-run before the finding carries load (#19)** — `open_items/OI-243.md:46-56`.
- **F6.** The committed apparatus exists: `report.md`, `transpose_state.json`, `establish_state.json`
  and `run_probe.py` under `tools/joint_estimator/transposition_probe_2026_08_02/` — enumerated at
  the paths themselves.
- **F7.** The enharmonic caveat: transposition-as-uniform-respelling models an engraver's spelling
  choice, so **some** divergence at the tritone shift is defensible, while moved boundaries and total
  collapses are not — `open_items/OI-243.md:54-56`.
- **F8.** OI-289 is a **#19 verification-owed** row over the LEGACY-marked set; its size is
  **re-derived from the data**, and the row states in terms that **no figure in the record matches
  it** — `OPEN_ITEMS.md`, the OI-289 row. **Re-derive; do not quote any recorded figure.**
- **F9.** OI-283 is a **#17(f)/#19** item — the register's own coverage claim, hand-typed and stale —
  `OPEN_ITEMS.md`, the OI-283 row.
- **F10.** OI-287 carries the ratification-surface **directory** question and states that naming the
  directory and its membership rule is the user's or Cowork's, not a session's —
  `OPEN_ITEMS.md`, the OI-287 row.

**ASSUMPTION — NOT read at the object by Cowork; each has an ordered check below, and no act depends
on it until the check runs:**

- **A1.** That phase 2's channel program is the measurement-tools partition of OI-199, the
  record-seams partition, OI-199 pass 2, the remaining perspective-inventory channels, and OI-179.
  **Cowork has this from a session handoff block, which is a secondary surface.** → checked at
  **Task 2.2**.
- **A2.** That the OI-288 **INDEX row** still states the retirement map's R1/R5/R6 delete the
  Layer-1.5 primitive's call sites — a claim the phase-1n wave reported as false at the map's own
  text. Cowork read the index row and believes the claim is still there; whether the phase-1n
  correction reached the detail file only is **not established**. → checked at **Task 4.2**.
- **A3.** That OI-280, OI-282, OI-287 and OI-274's body-tense half are the **complete** apparatus-row
  set. Cowork has read each of those rows, but not that the set is exhaustive. → derived at
  **Task 3.1**, not assumed.

## 1. Standing constraints

1. **Every amendment lands in the PROPER LAYER** (#7).
2. **NO inference-problem fixing.** Phase 1 under D-231. No `src/` change, no golden refresh, no
   `tools/corpus/` or `tools/robust_stop/` movement, no behaviour change, no fix, no design. Task 4 is
   a **read-only measurement** writing only into its own artifact directory.
3. **Never use the shell to read working-tree files.** Path enumeration goes through
   `tools/audit/changed_paths.py`; content goes through the file tools.
4. **Never work from memory.** For a claim about the code, the code is primary; a row or a register
   entry is secondary.
5. **A surprise is a STOP** (#13).
6. Bare words carry the musical meaning. Bash: append `; echo "exit:$?"`; no large single outputs.

## 2. Task 1 and 2 — Partition the phase-3 gate

### 2.1 The ruling

The user has ruled that the family design waits only on the phase-2 channels **that could plausibly
find another member of the family named at F1**. The rule this preserves is F3; the gate's purpose is
that the family be **known** before it is designed for, not that every unrelated measurement finish
first. **The gate is narrowed, not opened.**

### 2.2 Enumerate the channels from the record — this discharges A1

**Do not take Cowork's list.** Enumerate phase 2's channels **from the record itself**, citing each
channel's defining source. If the enumeration differs from A1 — in membership or in scope — **report
the difference**; that is a finding about a handoff block, and it is the kind this wave exists to stop
propagating.

### 2.3 Classify, on one stated criterion

For each channel: *could this channel's search space contain a statement, measurement or code fact
about (a) what the decoder or the emission READS — struck versus sounding tones, note counting, pitch
representation — or (b) how candidates are ADMITTED?*

**GATING** or **NON-GATING**, each with its reason drawn from the channel's own scope. **Where the
channel's scope does not settle it, classify GATING** — the default on doubt is to keep waiting.

### 2.4 Register it as a prediction, not a fact

This classification is a prediction about where surprises live, and #3 says that is what we are worst
at. Record it in a generated artifact **before** any classified channel runs, stating: the criterion
verbatim; the per-channel verdict and reason; **the check** — as each channel runs, whether it
produced a family member is recorded against its classification; and **the stop** — a NON-GATING
channel that yields a family member is a **#13 STOP**, the partition was wrong, the gate widens.

### 2.5 Home it

At `CLAUDE.md`'s D-231 entry, as a qualification of the existing rule with its defense: the gate was
ratified to prevent designing over an unknown family, and a channel that cannot touch what the model
reads or how candidates are admitted cannot change what the family is. Register entry in the same
commit (D-230). Cross-reference the F1 rows.

## 3. Task 3 — The non-gating declaration

### 3.1 Derive the set — this discharges A3

The user has ruled that rows whose subject is this project's own tracking and documentation apparatus
are **declared non-gating**, worked in leftover capacity, gating nothing.

**Derive that set from the rows themselves**, on the stated criterion: *does the row's subject bear on
the analysis, its inputs, or an instrument a measurement depends on?* If yes it gates. Report the set
you derive; if it differs from A3's four, the difference is the finding.

### 3.2 The exemption is not discretionary

**OI-283 (F9) and OI-289 (F8) remain in phase 2's program and continue to gate.** Both are #19
establishment obligations, and backgrounding an establishment obligation is how it never happens —
state that reason in the rule text.

### 3.3 Home it

At `CLAUDE.md`'s open-items register section, as a qualification of rule (b) (F4), with the criterion
stated so a future row is classifiable without a fresh ruling, and with the clause that **an
establishment obligation (#19) always gates, whatever its subject.** Register entry in the same commit.

## 4. Task 4 — The transposition probe, repository-side

### 4.1 Why, and the scope

F5 is the whole warrant: two family members cannot bear weight until this runs. **Read-only.** Write
nothing outside a new artifact directory for this run. No `src/`, no goldens, no `tools/corpus/`, no
`tools/robust_stop/`. The apparatus is F6; the reference decoder is `probe_decoder.py`.

### 4.2 Check A2 first

Read the OI-288 **index row and its detail file** and establish whether the R1/R5/R6 claim still
stands in either. Correct whichever carries it, citing the retirement map's own text. **This is
ordered before the probe** because OI-288 is the row that frames what the probe's sibling question
even is.

### 4.3 Establish the tool before trusting the finding (#19)

1. **Reproduce the establishment step first** — the sampled pieces must reproduce the committed decode
   parity reference exactly. **If they do not, STOP**: the apparatus is not established and the finding
   cannot be re-measured with it.
2. **Register predictions BEFORE measuring** (#17b). The original run's predictions and their
   refutation are in the committed `report.md` — read them, and say explicitly whether you predict
   reproduction or divergence, and why.
3. **Re-run** repository-side over the same pieces and shifts.
4. **Report reproduction or divergence per condition.** A divergence is a finding about the
   apparatus, not the decoder, and must be reported as such.

### 4.4 Separate the defensible component

Per F7: report how much of the measured non-equivariance is **defensible enharmonic ambiguity** and
how much is **boundary movement and collapse**. **This is the number the family design needs**, and
the original run did not separate it.

### 4.5 Then the rows

OI-243 and OI-244 record the outcome; their establishment status becomes established, refuted, or
established-with-a-narrower-claim. **Design no remedy** — the disposition is the family design's, at
its own stage.

## 5. Task 5 — Guards, notes, close

Run every guard at the committed tree; **derive the list from what exists** and report rather than
substitute if one this dispatch names is absent. Read each output separately. Verify what is being
committed through `tools/audit/changed_paths.py`. Run `tools/audit/process_check.py` over **this
dispatch** and report what it finds against Cowork.

Anchor drift from the `CLAUDE.md` insertions is **re-aimed per citation from the drift report's own
line numbers** — the phase-1s wave measured three different offsets in one file, so no single shift is
correct anywhere.

`STATUS.md` gains one POINTER entry stating that the gate is **narrowed and not opened** and that the
family design remains unstarted. Commits by git plumbing; guards run explicitly at the committed tree.

**Still owed and NOT in this dispatch:** OI-280, OI-282, OI-283's own remedy, OI-274's body-tense
half, OI-287 (the directory, F10 — the user's or Cowork's to name), OI-288's fire-rate half, OI-289,
OI-290's document-side remedy, OI-296's sweep, the owed reads, and the queued phase-1t dispatch.

## 6. Accepted outcomes

**A2 or A3 coming back different from Cowork's assumption is a success** — that is what the ledger is
for, and reporting the difference is the deliverable. **A1's enumeration differing from the handoff's
list is a finding about the handoff**, not an error to reconcile silently. **Task 4 failing at §4.3
step 1 is a success.** **Task 2 classifying most channels GATING is acceptable** — the criterion
defaults that way on doubt, and a partition that narrows little is an honest one.

## 7. Self-check (D-434) — run by Cowork on this dispatch before release

- **#17(a).** Every load-bearing claim is labelled FACT with a citation to the object the claim is
  about, or ASSUMPTION with an ordered check. Three assumptions are declared, and no task acts on one
  before its check.
- **Tier condition.** Each FACT cites the thing itself — the row, the file, the paths — not a document
  that mentions it. A1 is an ASSUMPTION precisely because its source is a handoff block.
- **Principles.** #12 nothing deleted; #13 four STOP conditions; #19 the probe establishes before
  measuring and the exemptions are named; #7 each ruling homed at the surface that owns it.
- **D-431.** No bare quantity anywhere; F8 explicitly forbids quoting a recorded figure and orders
  re-derivation.
- **Consistency between rulings.** Checked: Task 2's partition and Task 3's declaration both narrow
  what gates, and Task 3.2 exempts the two #19 rows so neither narrowing reaches an establishment
  obligation. Nothing here contradicts anything else here.
