# CC instruction — Phase 1i: the delivery acts (probe rowing · contract-home re-classification · LEGACY sweep · pointer pass) + continued full reads

> **Read first (every session):** `C:\s\MS\CLAUDE.md` IN FULL — the decisions-register section
> now carries rules **(f)** (LEGACY marking) and **(g)** (the contract-home fifth case), both
> user-ratified 2026-08-02 and both EXECUTED by this dispatch. `C:\s\MS\DECISIONS.md` (the INDEX —
> 400 entries, ALL user-ratified through seven events). `C:\s\MS\STATUS.md`. `C:\s\MS\OPEN_ITEMS.md`
> (INDEX) with **OI-272** (open for THIS dispatch's delivery), **OI-268/OI-275/OI-260** (ruled,
> closed — the rulings this dispatch executes), **OI-267/OI-243/OI-244** (the probe-rowing
> inputs), **OI-207**. `C:\s\MS\cowork_candidate_findings_2026_08_02b.md` (the probe findings —
> Task 1's input). `C:\s\MS\cowork_design_doc_template.md`.
>
> **Current state:** the local branch is TWO commits ahead of origin (`adee1867a7`,
> `1d07954137`). **FIRST act: `git push origin master`; verify HEAD `1d07954137`; mismatch =
> STOP.** Last act: push.
>
> **Hard stops:** origin only; no `src/` change; no golden/`tools/corpus/`/`tools/robust_stop/`
> movement; no fix, no design, no inference change. `ARCHITECTURE.md` edits are permitted ONLY
> as the ratified POINTER class (Task 3): a one-line delegation pointer or a one-line
> tried-and-closed pointer — never content copies, never new rulings. New decisions found in the
> full reads are NEVER self-ratified (RATIFICATION QUEUE). Anchor discipline: per-citation fixes
> from the `--verify` drift report, never a single assumed threshold. A surprise is a STOP
> (#13). VS Code bash rules. Feasibility stops with measured partitions are accepted — this
> dispatch is deliberately larger than one session; work the tasks IN ORDER so every session
> boundary leaves a complete, guarded increment.

**Dispatch author:** Cowork, 2026-08-02.

## Task 1 — row the probe findings (the user-reviewed candidate file is the input)

`cowork_candidate_findings_2026_08_02b.md`, applied exactly:
1. **A-1 → a NEW ROW** (next free OI number): the emission is note-count-weighted and octave
   doubling moves committed readings (13.2 % of segments; boundaries re-cut on 9/12) — a model
   property by trained design (fit and decode consistent — NOT the OI-228 class), rowed as a
   family-design input (the what-does-the-emission-read family, with OI-228/243/246); the row
   carries the establishment caveat (agent-run probe, repo-side re-run owed with OI-243's) and
   cites the committed artifacts `tools/joint_estimator/invariance_probes_2026_08_02/` — ADD
   that directory and `tools/joint_estimator/applied_chord_stake_2026_08_02/` to git in this
   task's commit.
2. **A-2 → an establishment note**, one line where the decode-parity establishment is
   documented: note-order permutation leaves the committed surface identical 36/36; total scores
   wobble at ~1e-14 (floating-point summation order), so no future gate may compare scores at
   exact equality (the 1e-6 tolerance is safe).
3. **B-1 → the D-248 scope correction**: its plain restatement and provenance corrected to name
   the LEGACY ChordFunction structure as its true scope, because the production joint renderer
   DOES emit applied-chord labels (`jointrender.h:62-63`; 8.62 % of corpus duration; exact
   matches against applied ground truth) — cite the stake artifacts; dated note on OI-267
   marking its OI-53-tension question ANSWERED (the joint surface emits; the legacy structure
   does not carry).
4. **B-2 → OI-267's measured stake recorded** in a dated note: the +1.62-point hard ceiling,
   the +0.38-point pure-name slice, the refuted OI-192 kinship — the revisit enters the phase-3
   plan at LOW priority after the scope correction.

## Task 2 — the register-data delivery passes (rules (f) and (g) executed)

1. **The LEGACY classification sweep:** every one of the 400 entries judged — does its SUBJECT
   belong to the legacy surface (the dormant pipeline awaiting deletion)? Set `legacy_subject`
   accordingly (D-278/317/318/342/345 already carry it). Judgment standard: the entry's OWN
   subject, not its era — a decision that governs the live solution stays unmarked however old.
   Output: the marked set listed in the report for the user's confirmation.
2. **The contract-home re-classification:** every entry whose home is a ratified/signed
   `cowork_*`/`docs/` design document re-classified `nonspec_kind: "contract-home"` WHERE the
   OI-268 criteria hold (ratified or signed + a status banner + the `ARCHITECTURE.md` delegation
   pointer exists). Where the pointer is MISSING, leave the entry as-is and list the missing
   pointer for Task 3. Where the banner is missing, list it for the banner report (no banner is
   edited without the user's acceptance — a proposed banner list goes in the report).
3. The register regenerates; all guards pass at every commit.

## Task 3 — the pointer pass (the ratified ARCHITECTURE.md edit class)

For each missing delegation pointer Task 2 lists: ONE line in the owning `ARCHITECTURE.md`
section — "the ratified contract for X is `<doc>` (D-nnn…)" — never a copy. For the ruled
per-kind scheme's dead-end class (OI-272): where a future designer would look, ONE line —
"tried and closed — D-nnn". Every insertion shifts anchors: run the drift loop per citation
after each wave. OI-272 flips RESOLVED when this task completes, with the pointer inventory in
the report.

## Task 4 — continued full reads (the remaining ≈70 LIVE-SPEC documents)

As capacity allows after Tasks 1–3, highest unresolved-cluster count first
(`cowork_structural_integrity_audit.md` 20 · `cowork_progression_schema_dictionary.md` 19 ·
`docs/implementation_roadmap.md` 18 · then the three at 17 …). Same discipline as every wave;
the queue for the user; the flag duties (D-282…D-285; measurement tools fenced).

## Task 5 — close

Dispositions for what was read; the manifest; all guards; dated notes (OI-272 flip, OI-267,
OI-207 coverage count); `STATUS.md` pointer at the TOP; commits per change-class; push.

## Report

Task 1: the new row's number, the corrections applied, the artifacts committed. Task 2: the
LEGACY-marked set (for the user's confirmation) and the re-classification counts; the
missing-pointer and missing-banner lists. Task 3: the pointer inventory, per file:line. Task 4:
documents read with yields + the RATIFICATION QUEUE; the cumulative read-in-full count. Guards;
anomalies each diagnosed; the measured remainder. Standing self-check before reporting.
