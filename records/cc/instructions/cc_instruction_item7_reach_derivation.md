# CC dispatch — D-639's test applied per row inside the finish line's item 7, and the untracked scratch landing completed

> **Status: ACTIVE DISPATCH, written 2026-08-11 (Cowork), at a verified STOP** — the previous
> batch completed, three commits pushed, verified at the objects: no forbidden paths, the errata
> correction `2/2` on `CLAUDE.md` so no register anchor moved, and CC's three findings answered
> (finding 2's *unaltered* limb is establishable by construction — Cowork made that edit and the
> former §7's body was never inside the replaced string). Nothing is running.
>
> **Read IN FULL, and read FIRST:** `CLAUDE.md`'s **D-639 block** inside the D-231 three-phase
> rule, including its three worked examples and its fallback; `tools/audit/phase1_finish_line.json`
> item 7 and its population; `tools/audit/decisions/gen_true_half_reach.py` and
> `true_half_reach_rows.json`, which are the machinery this extends; Ruling 65 at
> `cowork_rulings_2026_08_11_fifteenth_stop.md`.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_item7_reach_derivation.md`. Acts dated
> from the clock; commit messages via `-F`; **no positional count anywhere — a location is
> described, never counted** (D-307, D-432).
>
> **★ All standing rules as adopted.** D-253 in every dialect. NO TRANSCRIBED VALUES (D-431).
> Hold-don't-guess. **Read-only on the analysis: no `src/` edit, no behaviour change, no golden,
> no corpus of scores, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement, no
> design, no fix to inference.** D-231 and #8 stand. **Phase 1's completion statement is not
> written, not drafted and not partially written.** Commit and push per task boundary; `origin`
> only.

## 0a. WHY THIS RUNS, AND WHAT IT IS NOT

**The finding that prompted it.** Ruling 65 cut the finish line by D-438's test, and the outcome
was that the **owed** dimension moved — the lapse population — while **no item's gate moved** and
the gating row count did not fall. So every row in item 7 still gates on the classification
*a specification states something false at HEAD*, and **D-639's test has never been applied per
row inside that population** — although D-639 is the rule the record already made for exactly this
half.

**This is not a re-litigation of Ruling 65 and it changes no ruling.** It applies a test the record
already carries to a population it was never run over. **The outcome may be that every row in item
7 genuinely gates — that is a CORRECT outcome and is named in advance so the derivation is not read
as having failed if it yields nothing.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — verified by Cowork at the objects.** The finish line's counted block at HEAD: nine items,
eight gating, and item 7's gating row population, which rose when OI-370 and OI-371 entered it. The
previous batch's three commits and their paths. That `cowork_scratch_2026_08_11/` carries exactly
one tracked file at HEAD while `cowork_scratch_2026_08_08/` is fully tracked.

**ASSUMPTION — each checked BEFORE the act resting on it; a refutation is a STOP.**

- **A1.** **The existing reach machinery is IMPORTED, never re-implemented (#6).**
  `gen_true_half_reach.py` already encodes D-639's test and already runs a both-ways
  reconciliation with its own STOP. *Check: this pass extends its population; if its test cannot be
  applied to item 7's rows without change, STOP and report what change would be needed rather than
  making it.*
- **A2.** **D-639's three worked examples ARE the test, applied in the order the block states** —
  an as-built banner over a dormant mechanism is IN; a missing supersession note on a superseded
  plan is IN; a stale anchor or formatting artifact is OUT. *Check: each verdict cites which worked
  example decided it.*
- **A3.** **D-639's FALLBACK is honoured and never stretched around.** If the test needs judgment
  on a row, the block's own fallback applies — the doc-sync half reaches only the account of the
  ANALYSIS — and the session **says it applied the fallback** rather than arguing the row to a
  verdict. *Check: every fallback application is declared per row.*
- **A4.** **No gating verdict is hand-added.** The verdicts are authored inputs to a derivation
  whose both-ways reconciliation must agree, exactly as the existing machinery does it; the gate is
  then derived. *Check: the reconciliation runs and agrees in both directions, or STOP.*
- **A5.** **Ruling 65's falsification test runs again over the enlarged reach population.** *Check:
  if any row the record elsewhere calls inference-bearing is placed outside the doc-sync half's
  reach, the derivation is wrong and HALTS.*

## 0c. THE TASKS, IN ORDER — D-670's ordering

**Task 1 — the reach derivation over item 7's population. FIRST, with nothing large in front of
it.** It is D-671's class: a derivation over a derived population, published **whole or not at
all**, so it cannot absorb a capacity shortfall and must not be started behind anything large.
Extend the existing reach machinery to item 7's rows (A1), apply D-639's test per row with its
worked example cited (A2) and its fallback declared where used (A3), run the both-ways
reconciliation (A4) and Ruling 65's falsification test (A5), then regenerate the finish line.
**Report the movement both ways** — which rows leave the gating class, and, recomputed with the
new reach off, that nothing else moves. **Adjust nothing to produce a movement.** Commit and push.

**Task 2 — complete the untracked scratch landing.** `cowork_scratch_2026_08_11/` carries its
README, the continuation-14 verification plan and the `STATUS.md` draft row as untracked
working-tree files; a previous session recorded them as *landed* meaning written to disk. **This is
an incomplete act finished, not a defect fixed** — no row, no discard record. Commit them as they
stand; alter none of them. Commit and push.

**Task 3 — the close.** One `STATUS.md` pointer entry per completed task, nothing else in that
file. Append the close to `cowork_away_returns.md`. Report at the objects: commits and push state,
the derivation's movement both ways, the falsification test's outcome, how many verdicts used
D-639's fallback and which, and the finish line's gating population after the run.

## 0d. WHAT IS DELIBERATELY NOT DONE

**No completion statement**, and this dispatch does not bring one nearer by declaring anything —
only by measuring. **No row is closed by this derivation**; a row leaving the gating class is still
open. **No ruling is amended.** **OI-179 stays OPEN and GATES**, whatever this returns, because
#19's carve-out is not a doc-sync question. The OI-141 sitting is the user's and is not prepared,
scheduled or pre-empted here.

## 0e. STOP RULES

Halt with a STOP in `cowork_away_returns.md` if: the existing machinery cannot take item 7's rows
without modification; the both-ways reconciliation disagrees; the falsification test places an
inference-bearing row outside the reach; a verdict cannot be reached without judgment and the
fallback does not settle it either; or a guard goes red for a cause that is neither this dispatch's
own edits nor already recorded.

---

*Provenance: Cowork, 2026-08-11, at the verified STOP following the previous batch. The user ruled
this derivation, bounded to one pass, on a surface carrying three alternatives with their
principled costs and their ratings against the ultimate objective. The standing writing-side
self-check was run against this file before release (D-434).*
