# CC dispatch — the mandatory-read row landed, and OI-141's fourth input checked against the arm that ships

> **Status: ACTIVE DISPATCH, written 2026-08-11 (Cowork), at a verified STOP** — the
> fourteenth continuation completed (tip `e263aa9174`, pushed to `origin/master`) and was
> verified at the objects against expectations registered in writing before its report
> existed; nothing is running; the read-only hold is released.
>
> **Read IN FULL, and read FIRST, in this order:** `CLAUDE.md`; `DECISIONS.md` (INDEX);
> `OPEN_ITEMS.md` (INDEX); `tools/audit/phase1_finish_line.json` at HEAD — THE SCOPE;
> `cowork_scratch_2026_08_11/README.md` and the two draft files it lands
> (`draft_row_status_md_unreadable.md`, `cowork_oi141_sitting_pack.md`); and
> `cowork_handoff.md`'s entry-point block.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_row_landing_and_oi141_arm_check.md`.
> Acts dated from the clock; commit messages via `-F`; an identifier belonging to the
> open-items register or to the decisions register is verified at that register's own
> index immediately before it is written anywhere.
>
> **★ All standing rules as adopted.** D-253 in every dialect and mechanism — the file
> tools read working-tree files, the shell is for git object queries by explicit hash
> only. NO TRANSCRIBED VALUES (D-431). D-641 surfacing; #19 with its order half;
> hold-don't-guess. **Read-only on the analysis: no behaviour change, no `src/` edit, no
> golden refreshed, no corpus of scores touched, nothing under `tools/corpus/` or
> `tools/robust_stop/` moved, no fix to inference, no design.** D-231 stands and phase 1
> is open; #8's three-clause gate stands. **Phase 1's completion statement is not written,
> not drafted and not partially written by this dispatch.** Commit and push per task
> boundary; `origin` only, NEVER `upstream`.

## 0a. WHAT THIS DISPATCH IS FOR, IN ONE PARAGRAPH

Two acts, both of which were already owed before this dispatch existed and neither of
which needs a new ruling to become permitted. The first lands an open-items row for a
finding made while performing the prescribed session-start read: a mandatory read cannot
be performed. The second answers a question that has been surfaced and left unresolved —
whether the mechanism OI-141's fourth input pins at the code still describes the arm that
ships. Everything else touching `STATUS.md` is deliberately held; §0e says which acts and
why.

## 0b. THE RULING LEDGER — quoted in full, never by the branch that supports the claim (D-643)

**D-641 / R3, `cowork_audit_protocol.md`, "A finding that bears on the analysis is
SURFACED whatever its size; an apparatus finding is ROWED AND LEFT" — ruled by the user,
2026-08-04, dispatch `cc_instruction_commit_and_finish_line.md`, R3.** Every finding a
session makes is sorted by D-438's own test, and by nothing else: *does the finding's
subject bear on the analysis, on the analysis's inputs, or on an instrument a measurement
depends on?* **If YES — it is SURFACED to the user for decision, WHATEVER ITS SIZE.** Not
held for a later wave, not absorbed into the middle of a report, and never left as a row
on the ground that it looked small. Size is not one of the test's terms. **If NO — it is
ROWED AND LEFT: no wave, no dispatch, no surface.** The row is written, and the row is the
whole of what is owed. The exception, stated plainly: **an establishment obligation (#19)
always gates, and is therefore always surfaced, whatever its subject** — including one
whose subject is this project's own apparatus. What it is NOT: *"It is not permission to
leave an apparatus defect undocumented — the row is mandatory, and the open-items
register's rule (c) still requires the row and its detail file in the commit that records
the discovery."* And, on how it composes with the finish line: *"Adding an item to the
finish line is a user ruling."*

**The two clauses of that entry this dispatch actually rests on, named so the application
is checkable.** The *what it is NOT* clause is what makes Task 1 mandatory rather than
optional, whichever way the surfaced/rowed test falls for its subject. The *adding an item
to the finish line is a user ruling* clause is what keeps the archive application out of
this dispatch — see §0e.

**D-670, `cowork_audit_protocol.md`, "A task that CANNOT BE STOPPED PARTWAY is dispatched
FIRST, with nothing large in front of it — and the ordering is RULED, never left to a
preference" — ruled by the user, 2026-08-11** (`cowork_rulings_2026_08_11_eleventh_stop.md`,
Ruling 52, taking Ruling 51 of `cowork_rulings_2026_08_11_tenth_stop.md`). The two rules
that collide: one kind of task must be published over its WHOLE derived population or not
at all, because a derivation covering some of its population reads as covering the class,
which is the cap nobody sees; the other kind, a per-entry pass, may be stopped at any
member boundary, because each completed member is whole in itself — **so the second kind
can always absorb whatever capacity is left and the first kind never can.** **THE RULE.**
Where a dispatch carries a task that cannot be stopped partway, **that task is placed
FIRST, with nothing large in front of it**, and the placement is recorded as a RULING
rather than as the dispatch-writer's preference. *Why a ruling rather than a preference:*
a preference does not survive one more capacity squeeze; such a task is small in COUNT and
large in READING, so every honest estimate of it looks cheap while every attempt at it
loses to work that can stop at a boundary — and each individual refusal is CORRECT on its
own terms, which is what makes the pattern invisible from inside any one dispatch. *The
evidence, measured rather than argued:* one such task was declined by seven consecutive
dispatches, every refusal right on its own terms and for the same reason each time;
dispatched first under this rule it closed WHOLE in one act, and turned up a finding
nobody was looking for while deriving its own population. The ordering was the whole
difference, and nothing about the task had changed.

**The open-items register's rules (c), (e) and (f), `CLAUDE.md`.** (c) *"every newly
discovered issue gets an **index row AND its detail file** **in the same commit** that
records the discovery"*; (e) *"tracking an owed/deferred/TODO item in prose only, without
a register row, is a doc-sync violation (#10)"*; (f) *"A row's STATE is carried by the
first token of its status cell"*, with the lint and the single index parser reading
exactly that leading token.

**The licence for Task 2, stated as a chain rather than asserted.** The arm-staleness
question's subject is which code path performs key inference on the shipping arm, which is
the analysis itself, so D-641's test returns YES and the finding was SURFACED — it stands
in `cowork_handoff.md`'s entry-point block and in the sitting pack's §3, and the pack
records that the settling act was named and not performed. The user's decision on a
surfaced finding is what authorizes the response to it, and the user gave it in
conversation on 2026-08-11 against a plan that named this act in these terms. This is a
read-only establishment, which `CLAUDE.md`'s investigate-by-default convention independently
directs. **If the executing session judges that chain broken, Task 2 is a STOP, not a
guess.**

## 0c. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork at this STOP, each at the object named.** The fourteenth
continuation's completion and push state, and its verification outcome, at
`cowork_handoff.md`'s entry-point block. The drafted row and detail file, complete and
unlanded, at `cowork_scratch_2026_08_11/draft_row_status_md_unreadable.md`, whose landing
note records that no index row exists, no detail file exists under `open_items/`, and no
identifier is assigned. The blocking reason for that landing — the single-writer hold while
the batch ran — recorded in the same file, and the batch is finished. The named settling
act and the dates it rests on, at `cowork_scratch_2026_08_11/cowork_oi141_sitting_pack.md`
§3, which states that dates only were checked and no code was read. The scope rule and its
operational clauses at `tools/audit/phase1_finish_line.json` → `the_scope_rule`.

**ASSUMPTION — each checked BEFORE the act resting on it; a refutation is a STOP written
into `cowork_away_returns.md` and the task is not completed around it.**

- **A1.** The next identifier is read at the INDEX at landing time and never inferred from
  any prose, this dispatch included. Three citation slips on this arc's Cowork side are
  already recorded as correction notes; an identifier guessed against a moving index would
  be a fourth. *Check: read the INDEX immediately before writing.*
- **A2.** The drafted row and detail file are landed AS DRAFTED in substance. The
  identifier is filled in and nothing else is re-argued — in particular the two things the
  draft withholds stay withheld: **no gating verdict is assigned by hand**, and **no count
  of what should move is written**. *Check: diff the landed detail file against the draft's
  fenced block before committing; any divergence beyond the identifier is reported.*
- **A3.** The row's status cell opens with the canonical token the lint reads, and the
  index-to-detail bijection still holds after the insertion. *Check: the two guard runs
  named in Task 1, at the committed tree.*
- **A4.** An insertion into `OPEN_ITEMS.md` shifts line anchors. Any cited coordinate that
  moves is re-aimed **per citation from the drift report**, never by an assumed uniform
  shift. *Check: `gen_cluster_dispositions.py --verify` after the insertion.*
- **A5.** Task 2 is an establishment about a code path and is therefore performed AT THE
  CODE, at the call graph, not at any document's description of the code. A document is a
  secondary source for a claim about the code and may not carry the verdict. *Check: every
  claim in the Task 2 finding cites the code it is about.*
- **A6.** Task 2's three outcomes are all permitted and none is preferred: the pinned
  mechanism describes the shipping path; it describes it partly; it describes the dormant
  legacy path only. **A verdict that the evidence does not support is HELD, with the
  evidence, in both directions (#19).** *Check: the finding states which of the three it
  reaches and what would have been needed for the others.*

## 0d. THE TASKS, IN ORDER — and the order is D-670's, not a preference

**Task 1 — land the row (A1, A2, A3, A4). FIRST, with nothing large in front of it.** The
open-items register's rule (c) makes this atomic: the index row and its detail file go in
ONE commit, the commit message recording the discovery. It therefore cannot be stopped
partway, which is exactly the class D-670 places first. It is mandatory under D-641's
*what it is NOT* clause whichever way the surfaced-or-rowed test falls for its subject, so
no ruling is awaited for it. Perform the draft's own six-step landing checklist, which is
not restated here (#6). Then run, at the committed tree: `tools/audit/index_status_lint.py
--check`, `tools/open_items_split_check.py`, and `gen_cluster_dispositions.py --verify`
with per-citation re-aiming if it reports drift. Commit and push at the task boundary.

**Task 2 — settle OI-141's fourth input against the arm that ships (A5, A6). READ-ONLY.**
The act is the one the sitting pack names and is quoted here so it is not widened:
**establish, at the call graph, whether the joint estimator's decode consults
`keymodesequence` / `keymodeanalyzer` on the production arm, or carries its own key path.**
Report, for each of the four mechanism elements the pack names — the top-8 emission-union
lattice, the three hand-set change costs, the four-beat emission window, the single
start-tick anchoring — whether it is on the shipping path, and cite the code for each. The
question is what the shipping arm DOES, so the production flag's default belongs to the
call graph and is read, not assumed. **Nothing is changed.** No behaviour moves, no
document is corrected on the strength of the finding, and no design decision is re-opened
— the seven design decisions this bears on are the sitting's business and the sitting is
the user's. If the finding refutes anything the sitting pack states, that is reported and
the pack is left as written (#12); Cowork annotates it at the next STOP.

**Task 3 — the close.** One `STATUS.md` pointer entry per completed task, per the standing
protocol, appended and nothing else in that file touched — §0e is the reason. Append this
dispatch's close to `cowork_away_returns.md`, including any STOP raised. Report at the
objects: the commits with their hashes and push state, the guard runs and their outcomes,
the landed identifier, and Task 2's finding with its citations.

## 0e. WHAT IS DELIBERATELY HELD, AND WHY — so the next reader does not take it for an omission

**Four acts that all want the same `STATUS.md` touch are held for one consolidated later
act.** They are: applying the archive rule the document's own banner states; the
fourteenth continuation's `STATUS.md` entry, which is drafted complete elsewhere; OI-47's
banner half, which is assigned *at the next `STATUS.md` touch*; and the correction note
owed for the gating-row miscount recorded in `cowork_handoff.md`'s entry-point block,
whose ruled remedy is a correction note and never a rewrite. Doing them in one touch costs one anchor re-aim and one guard run instead
of four, on a file that is already unreadable end to end.

**The archive application additionally cannot be dispatched yet, and that is a scope
matter rather than a caution.** It is a wave; it is not among the items
`tools/audit/phase1_finish_line.json` carries; and the scope rule states that nothing
outside those items gets a wave — *"Not a dispatch, not a session, not a sub-task of
one"* — while *"An addition to this list requires a USER RULING."* Whether the subject
reaches D-641's YES branch is the open question the drafted row deliberately refuses to
answer, because a gating verdict is derived from a cut and never hand-added. It is with the
user.

**The size guard stays PROPOSED and untaken.** The mechanism set is frozen, and the
condition it would detect does not block this dispatch's work.

## 0f. STOP RULES

Write a STOP into `cowork_away_returns.md`, halt at that point, and do not work around it,
if: any assumption above is refuted at its check; the next identifier cannot be established
at the INDEX; the landed detail file diverges from the draft beyond the identifier; a guard
fails at the committed tree and the failure is not fully explained by this dispatch's own
insertion; Task 2's licence chain in §0b appears broken; or Task 2's evidence supports none
of its three outcomes. A capacity stop at a task boundary is not a STOP — it is recorded as
what was done, what was not, and that the remainder is untouched.

---

*Provenance: Cowork, 2026-08-11, at the verified STOP following
`cc_instruction_return_continuation_14.md`. Written from all present knowledge at a full
stop with nothing knowledge-generating running, per the one-dispatch-at-a-time rule. The
standing writing-side self-check was run against this file's own text before release
(D-434).*
