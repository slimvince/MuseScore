# CC dispatch — the preparation phase OPENS: the nineteenth handoff block landed, the stale twelfth heading demoted, the decisions-register filter DERIVED onto a ruling surface, and the caller-check at the objects RUN — derivations and record only; nothing discarded, nothing archived, nothing landed beyond what the tasks name

> **Dispatch (Cowork, 2026-08-15).** The first dispatch of the PREPARATION phase, under the ruled
> definition — `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §3.1 and §8
> (wave 1), ruled whole at `cowork_rulings_2026_08_15_phase_definition_sitting.md` §2 and §8. The
> phase's remaining acts (the rulings sort, the empirical findings ledger, the curated boot list,
> the archiving wave, the mining and the landing of the newly visible files) are NOT in this batch
> — the nineteenth handoff block bounds the first dispatch, and this is that bound. Written at a
> verified STOP; nothing else is running.
> **Read first (every session):** `C:\s\MS\CLAUDE.md`, `C:\s\MS\STATUS.md` (header only),
> `C:\s\MS\BUILD_AND_TEST.md`. **Then read, in full:**
> `cowork_rulings_2026_08_15_phase_definition_sitting.md`;
> `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §0, §3.1 and §8;
> `cowork_rulings_2026_08_13_eighteenth_stop.md` §2 Ruling 8;
> `cowork_rulings_2026_08_15_inventory_sitting.md` §3 (the ruled flags and their conditions); and
> your own `cc_report_d231_landing.md` (its F3 and F4 reading rules bind here).
> The standing bars bind this batch whole: **no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no decisions-register entry written or
> edited** (the filtering ruling stands until the user rules on the surface Task 2 generates),
> **no open-items row created, flipped or discarded, no soft-discard executed, no archiving, no
> file moved or deleted, no mining, no landing of the 284 newly visible instruction files or the
> remaining ignored files.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end;
> record both states.** The expected start state: 48 run, 47 passing, ONE failing ([[OI-372]]'s
> tool, `gen_filing_convention_application.py --check`), zero STOPs, no stale report. **A
> different start state is a STOP-and-report.** Verify every commit after the fact at the object
> and record the SHAs in the close. The reserved-word conventions and the ruled vocabulary
> replacements (a changed passage / the current commit / untrusted source; implementation-derived
> material / independent sources / fact-gate / implementation-blind redesign) bind all new prose.
> **Reading rule from your own finding F3: `reaim_home_anchors.py --check`'s exit code is not
> evidence — the drift authority is `gen_cluster_dispositions.py --verify`'s report.**
> **The new-tool rule, from [[OI-373]]'s lesson:** each tool this batch adds under `tools/audit/`
> with a `--check` mode joins the derived guard-candidate population the moment it exists, so
> **each lands WITH its authored run-instruction and its authored classification verdict in the
> SAME commit that adds it** — the runner and `gen_guard_classification.py` must exit clean at
> the end state with the new checks inside the classified population.

## Ruling ledger (quoted at the records, not paraphrased)

- **Eighteenth stop, Ruling 8:** *"THE DECISIONS REGISTER IS FILTERED, NOT WHOLESALE RETIRED.
  Soft-discard — retired from the live record, not destroyed — only those entries where no
  deciding act can be named. A decision ABOUT the code is legitimate; what is not a decision is
  an observation of what the code does, recorded as though it had been decided."* And the
  register's origin: `cc_instruction_decision_harvest.md` lists production code comments as a
  harvest source, so the class exists by construction.
- **The ruled preparation definition (surface §3.1), its stop rule:** *"a filter verdict that
  cannot name the deciding act STOPS to the user rather than guessing; a fact-gate refusal is
  recorded, never argued around."* This batch operationalizes the first half as a PROPOSED
  classification: nothing is discarded by the batch; the user rules on the generated surface.
- **The pruning plan (surface §8, wave 1):** *"no archiving executes on the citation-scan signal
  alone — the caller-check at the objects precedes every wave."* This batch RUNS the check and
  publishes it; the archiving of passing candidacies is a LATER dispatch's act, after the check's
  artifact is verified.
- **The presentation rule (`cowork_rulings_2026_08_15_batch_return.md` §5):** every decision
  surface is self-contained — the surface Task 2 generates re-explains every identifier's
  referent from scratch before any question rests on it.
- **D-671 / D-672** (the dispatch-protocol block, `cowork_audit_protocol.md`): a derivation over
  a derived population is published WHOLE or not at all; a per-entry pass may stop at a member
  boundary only with the stop RECORDED — what was done, what was not, the remainder untouched.

## Premise ledger

- **FACT** — the D-231 landing is complete and pushed: commits `3dd468d978`, `b2a9fad4c9`,
  `1b3dc7990e`, `02636987b0`, all ancestors of `origin/master`; `CLAUDE.md`'s blob at
  `b2a9fad4c9` and at `02636987b0` is `36ffe5e0b3`, and the working file hashes identical to it
  (the writing side verified all of this at the objects on 2026-08-15, before this dispatch was
  written).
- **FACT** — measured by the writing side at content-addressed objects before this dispatch was
  written: the working `cowork_handoff.md` hashes `1bb96d838e`; against the committed blob
  `f64837573c` (resolved from `02636987b0:cowork_handoff.md`) the difference is **101 insertions
  and 1 deletion — TWO PARTS OF ONE ACT** (the A1 lesson of your phase-rulings report applied:
  both parts are named in this premise): the NINETEENTH entry block inserted above the
  eighteenth, AND the eighteenth block's heading demoted — its closing sentence
  `THE NEXT WRITING-SIDE ACT IS THE PHASE-DEFINITION SURFACE.` now followed by
  `(SUPERSEDED as the entry point by the nineteenth block above.)`, the file's own convention.
- **ASSUMPTION A1** — `cowork_handoff.md` is unchanged at your start, and the working tree
  carries exactly ONE tracked modification (that file), with `cc_instruction_preparation_opening.md`
  untracked. **Check ordered (Task 0, first act):** enumerate the working tree through the
  sanctioned tool; resolve the committed blob by explicit hash and diff it against the working
  blob produced by `git hash-object` (D-253 — neither side a shell read of a working-tree file).
  Any difference beyond the two named parts, or any other tracked modification, is a
  STOP-and-report.
- **ASSUMPTION A2** — the decisions register's whole population is enumerable at
  `tools/audit/decisions/backbone_decisions.json`, and the rendered INDEX (`DECISIONS.md`)
  carries exactly that population. **Check ordered (Task 2):** the filter tool derives its
  population from the backbone and cross-checks the rendered INDEX's entry identities both ways;
  a mismatch in either direction is a STOP-and-report (D-671 — the derivation covers the whole
  population or is not begun).
- **ASSUMPTION A3** — the ruled retirement-candidacy population is derivable from the committed
  inventory artifact (`tools/audit/artifact_inventory.json`) joined with the ruled flags of
  `cowork_rulings_2026_08_15_inventory_sitting.md` §3 (the six whole-class candidacies, the three
  partial uncited-side candidacies, and each candidacy's ruled condition — mined-first, or
  members-seen-by-the-user-first, or none). **Check ordered (Task 3):** the caller-check tool
  derives its population from the artifact and the record; a flagged class it cannot locate at
  the artifact, or a member list it cannot derive, is a STOP-and-report — nothing is hand-listed.

## Task 0 — land the nineteenth handoff block and this dispatch

1. **A1's check first, entirely at content-addressed objects.**
2. Commit, in ONE commit whose message names its own act (the nineteenth block and the
   eighteenth heading's demotion, two parts of one act), exactly these TWO paths and no third:
   `cowork_handoff.md` (modified) and `cc_instruction_preparation_opening.md` (new, staged
   plainly). Push. Verify at the object.

**Registered expectation E0:** `git diff-tree` on the Task 0 commit lists exactly 2 paths; the
`cowork_handoff.md` numstat is 101 insertions, 1 deletion; no staging override of any kind.

## Task 1 — the stale twelfth heading, demoted (upkeep of the handoff file's own convention)

The TWELFTH block's heading still closes with `THE CURRENT ENTRY POINT.` — pre-existing, noticed
in passing by your phase-rulings report, carried by the nineteenth block as upkeep. Replace that
closing sentence with `(SUPERSEDED as the entry point by the thirteenth block above.)` — the
demotion marker every other superseded heading in the file carries. Nothing else in the file
moves. Commit as ONE commit naming its own act. Push. Verify at the object.

**Registered expectation E1:** ONE path, ONE changed passage, 1 insertion and 1 deletion,
confined to the twelfth block's heading line; after the edit the string
`THE CURRENT ENTRY POINT.` occurs exactly TWICE in the file — in the NINETEENTH block's heading,
and inside the nineteenth block's carried-findings sentence, which QUOTES the stale heading this
task removes (a quotation is inert and stays).

## Task 2 — the decisions-register filter DERIVED, onto a ruling surface — NOTHING discarded

1. Build `tools/audit/gen_decisions_filter.py`. It walks EVERY entry of
   `backbone_decisions.json` (A2's check applies) and, per entry, extracts the evidence that a
   deciding act exists — the recorded ratifier and date, the ruling record named, a
   user-ruled/user-directed/user-ratified marker, a *"the record states no ratifier"* clause,
   harvest-source provenance — quoting each evidence field from the entry's own recorded text,
   never inferring one. It classifies every entry into exactly ONE proposed class:
   - **DECIDING-ACT-NAMED** — the entry's own record names the act (who ruled, when, at which
     record or event): proposed KEEP.
   - **NO-DECIDING-ACT-FOUND** — the record carries no nameable deciding act, or is
     observation-shaped (what the code does, recorded as though decided — Ruling 8's own test):
     proposed SOFT-DISCARD candidate.
   - **EVIDENCE-AMBIGUOUS** — the evidence supports neither confidently: proposed NEEDS-THE-USER
     (the §3.1 stop rule in artifact form — never guessed by the tool or by you).
   An entry the tool cannot classify AT ALL is a STOP — establish the STOP able to fire, the
   inventory tool's own pattern.
2. The artifact is `tools/audit/decisions_filter_classification.json`, with a `--check` mode
   that re-derives it. **The new-tool rule applies: the authored run-instruction and
   classification verdict land in the same commit.**
3. Generate the ruling surface
   `ratification_surfaces/cowork_decisions_filter_surface_2026_08_15.md` FROM the artifact:
   what the filter is and why (Ruling 8 quoted; referents re-explained from scratch, the §5
   presentation rule); the class populations by citation to the artifact (no count transcribed
   into prose as a judgment); EVERY proposed SOFT-DISCARD and NEEDS-THE-USER member listed with
   its quoted evidence. The surface's banner states: **NOTHING HERE IS RULED AND NOTHING HAS
   BEEN DISCARDED; the register stays under the filtering ruling until the user rules.**
4. `DECISIONS.md`, every `decisions/group_*.md` file and `backbone_decisions.json` are
   **byte-unchanged** — confirm each by hashing against its committed blob.

**Registered expectation E2:** the artifact classifies the whole backbone population (its own
counted fields carry the numbers; none is restated here, D-431), every entry in exactly one
class with quoted evidence; the register's rendered and data files byte-identical to their
committed blobs; `--check` re-derives on a second run.

## Task 3 — the caller-check at the objects, over EVERY ruled retirement candidacy — check only, NO archiving

1. Build `tools/audit/gen_retirement_caller_check.py`. It derives the candidacy population per
   A3, and for each candidate file establishes — at the TREE OF AN EXPLICIT COMMIT (the most
   recent commit of this batch at the time of the run, expected the Task 2 commit; record its
   hash inside the artifact; content-addressed queries only, D-253) — every reference to it from
   tracked content
   outside the candidate's own class: imports, path naming, tool reads. Per candidacy it
   publishes: the caller list (or NONE FOUND), the ruled condition still governing its archiving
   (mined-first / members-seen-by-the-user-first / none), and ONE verdict —
   **PASSES-THE-CHECK** (no caller, no standing condition), **HELD-BY-CALLERS** (each named), or
   **HELD-BY-CONDITION** (the condition named). The artifact records the commit hash whose tree
   it was measured at, so its staleness bound is explicit.
2. The artifact is `tools/audit/retirement_caller_check.json`, with a `--check` mode. **The
   new-tool rule applies.**
3. **NO file is moved, archived or deleted by this task.** The archiving of PASSES-THE-CHECK
   candidacies is a later dispatch's act, after this artifact is verified at the objects.

**Registered expectation E3:** every ruled candidacy appears in the artifact with exactly one
verdict and its evidence; the commit whose tree was measured is named inside the artifact; the
task's commit carries the tool, the artifact and the guard-mechanism records, and no other path.

## Task 4 — the close

One `STATUS.md` pointer entry per task and nothing else in that file. The FULL close appended to
`cowork_away_returns.md` (a THE PREPARATION OPENING section) — both guard-set states, every SHA,
every expectation graded, every problem declared, and the carried findings restated for the
phase's retrospective: F1–F4, the E3 ordering defect of your previous report, and the A1 premise
error (all surfaced, none rowed — rows stay barred). The report file
`cc_report_preparation_opening.md`, an ordinary tracked file. Commit, push, verify at the object.

**The E3-ordering lesson of your own report is a RULE here: no graded value is committed before
the run that produced it.** The end-state guard run demonstrated across the commit boundary is
taken AFTER the Task 4 commit exists, and its output plus the final SHAs land in ONE FURTHER
commit — exactly the shape your previous batch used.

**Registered expectation E4:** the end guard state shows every check passing except
`gen_filing_convention_application.py --check` ([[OI-372]]), zero STOPs, with this batch's new
checks inside the classified population under authored verdicts, the runner and
`gen_guard_classification.py` both printing their re-derives lines. **Any other failing check is
a STOP-and-report** — no sanctioned red class exists in this batch.

**Registered expectation E5:** at the tree carrying the close, a fresh full guard run re-derives
with the same state — run and read, not inferred, its values committed only after the run.

## What this batch does NOT do

- **No soft-discard executed, no register entry written, edited or retired** — the filtering
  ruling stands; Task 2's surface awaits the user.
- **No archiving, no file moved or deleted, no mining, no landing** — the 284 newly visible
  instruction files and the remaining ignored files stay unlanded; every retirement flag stays a
  candidacy; the caller-check's PASSES verdicts confer nothing until a later dispatch executes
  them.
- **No rulings sort, no empirical findings ledger, no fact-gate admission, no curated boot
  list** — they follow this batch: the sort and the ledger ride the next dispatch once the
  filter surface is ruled; the boot list is the writing side's draft for the user's ruling.
- **No derivation of any specification, no design, no repair, no pilot act, no `src/` edit, no
  golden, no test changed, moved or run, nothing under `tools/corpus/` or `tools/robust_stop/`,
  no measurement of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly as
  found; [[OI-179]] stays OPEN and GATES; `reaim_home_anchors.py`'s F3 defect stays surfaced,
  unfixed and unrowed (both bars stand).
