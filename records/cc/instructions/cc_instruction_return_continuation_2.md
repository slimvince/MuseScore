# CC dispatch — the second return continuation: Rulings 11–16 applied, then the remaining program (Task 4's remainder, Tasks 5–8)

> **Status: ACTIVE DISPATCH, written 2026-08-09 (Cowork), at a verified STOP** — the first
> continuation stopped clean after its Tasks 0–3 and a partial Task 4; both commits verified
> at the objects (`935efcf993`, `f3f2a07ba6`, each an ancestor of `origin/master`); the
> OI-349 probe's decisive citations independently re-read at the code. Read IN FULL, and
> read FIRST, in this order: **`cowork_rulings_2026_08_09_second_stop.md`** (Rulings 11–16,
> each read WHOLE, D-643), **`cowork_rulings_2026_08_09_return.md`** (Rulings 1–10, still
> the governing record for the program this continues), **`cowork_away_returns.md`** END TO
> END (the live returns file — §1.4–§1.7 are the holds Rulings 11–16 answer; the Task-4
> close note carries the duplicate-key trap you WILL meet), and
> **`cc_instruction_return_continuation.md`** (whose remaining Tasks 4–8 this dispatch
> resumes; its premise ledger, edit-authority frame, returns-file mechanics and
> commit-and-push-per-task rule are adopted; where the two differ, THIS dispatch governs,
> and the differences are Rulings 11–16 and the task list below).
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_return_continuation_2.md`.
>
> **★ HOLD-DON'T-GUESS; the returns file is the channel; a STOP-headed entry halts the
> batch; D-641 surfacing and the #19 always-surface rule as before. D-253 IN EVERY DIALECT
> AND MECHANISM. NO TRANSCRIBED VALUES (D-431). Read-only on the analysis: no behaviour
> change, no golden, no corpus of scores, no `tools/robust_stop/` movement, no fix to
> inference, no design. The ONE licensed `src/` act is Ruling 16's comment-only commit,
> exactly as ruled. Phase 1 under D-231; #8's full gate stands; every homing in the OWNING
> layer's section (#7, rule (e)) with its defense; provenance in register fields, never in
> specification text. Same-commit rule (D-230). Commit and push per task boundary; `origin`
> only, NEVER `upstream`.**
>
> **★ Phase 1's completion statement is not written, drafted, or partially written by this
> batch.**

## 0b. THE PREMISE LEDGER (#17a)

**FACT — established by Cowork at this STOP, each at the object named:** the two
continuation commits and their push state (verified by explicit hash against
`origin/master`); the guard failing set is the legacy-mark verification alone
(`tools/audit/guard_state.json` → `summary`); the probe artifact
`tools/audit/oi349_record_arm_precondition_probe.json` with its located citations, two
re-read at the code by Cowork (`jointdecoder.cpp` stay/change branches;
`composingconfiguration.cpp` default-true); OI-352 and OI-353 exist as rows; the eight
Task-4 entries are marked by fields opening "SEARCHED 2026-08-09"; the returns file's
§1.4–§1.7 are the four holds; the triage queue awaits the user's review and is NOT applied.

**ASSUMPTION — checked before the act it licenses, STOP into the returns file if refuted:**

- **A1.** Gate block (A) is a rule-stating home for Ruling 11's side-by-side entry (the rule
  (h) kind half) — read before writing; and the measurement half's text is taken VERBATIM
  from D-291's record, not paraphrased.
- **A2.** The D-644 replacement text at the register data's
  `section_home_criterion.scope_of_application` says what Ruling 13's docstring replacement
  needs — read before the edit; transcribe, never re-judge.
- **A3.** The legacy-mark tool's own output names the line its citation must re-aim at —
  run it, read its output, re-aim per its own numbers (Ruling 14); the failing set then
  empties by derivation, stated as the run's result.
- **A4.** The Layer-6 section's current D-472 text is read WHOLE before Ruling 15's
  correction; the corrected wording states the by-other-means precondition AND the
  island non-equivalence visibly; former wording preserved (#12); register verbatim
  re-taken from the corrected home.
- **A5.** Ruling 16's sweep enumeration is DERIVED (a search over `src/` and build files
  for arm/flag-default/build-state comment claims, each checked against the configuration
  facts), its artifact generated; the commit is verified comment-only at its own diff
  before push; a candidate whose falsity needs analysis judgment is HELD.
- **A6.** Task 4's fill REPLACES each entry's trailing null `rationale` — the recorded
  duplicate-key trap: a second earlier key is silently discarded and breaks the
  classifier's round-trip; the derived count is read after each batch of fills, which is
  how the first failure was caught.
- **A7.** Every population is derived at task start from the artifact that owns it, never
  carried from this file or from the returns file's prose.

## The tasks, in order

**Task 0 — Rulings 11, 13, 14, 15 applied.** The D-291 measurement half homed side by side
in gate block (A) with cross-references both ways (A1); the classifier's dead constant and
stale docstring corrected (A2); the legacy-mark anchor re-aimed (A3); D-472's wording
corrected at the Layer-6 section with the non-equivalence stated, and OI-349 then flipped
with provenance — the probe answered its question and the specification now matches the
shipping arm; OI-353 stays open for Task 5. Regenerate what these acts stale; expected and
stated by derivation: the guard failing set empties.

**Task 1 — Ruling 12's queue drafted.** The per-ruling classification of all TWENTY rulings
now carried outside the register (the four of 2026-08-08, Rulings 1–10, Rulings 11–16) —
decision the register carries versus exercise of one it already holds, each with its ground —
delivered as a ratification-surface reading file. **NO register entry is written**; the user
rules first. The queue names, for each proposed DECISION, its proposed home under rule (e).

**Task 2 — Task 4's remainder: the defense gaps.** Population derived fresh from the
completion inventory's `entries_with_no_rationale_at_all` (A7); the eight done are exactly
those whose fields open "SEARCHED 2026-08-09". Per entry: read the home text and provenance;
write into the trailing `rationale` field (A6) the defense the record holds WITH citation,
or the statement that a search was performed and the record holds none. Memory forbidden.
Homes outside edit authority: held with the found defense stated.

**Task 3 — the reach-verdict derivation** (the away program's Task 4, unchanged): ONE
generated derivation applying D-639's test over the apparatus-classed documentation rows,
per-row reasons, fallback (1A) applied and said where the test does not decide; rows move
where it puts them; no hand verdicts.

**Task 4 — the section-unreached and findings-not-rules re-homes** (the away program's
Task 5, unchanged): per entry under rule (e); soft cases held with candidates named.

**Task 5 — the session-executable gating rows + Ruling 16's comment-sync.** Enumerate the
TRUE-half gating set at HEAD; perform what is session-performable; flip with provenance;
hold the remainder with reasons. Then Ruling 16 as its own act inside this task: the sibling
SWEEP first (A5, generated enumeration), then ONE comment-only `src/` commit over the
enumerated family, diff verified comment-only; OI-353 flips on it, and any held candidate is
rowed or noted with its reason.

**Task 6 — OI-346's marks (leftover capacity), then final close:** batch summary appended
to the returns file — tasks completed, holds, surfacings, the finish line's end state
derived fresh — last commit and push.

**Edit authority, complete list:** the first continuation's list, PLUS
`tools/audit/decisions/gen_home_classification.py` (Ruling 13's two strikes only),
`tools/audit/decisions/gen_phase1w_legacy_verification.py` (Ruling 14's re-aim only),
`CLAUDE.md` gate block (A) (Ruling 11's side-by-side entry only, beside the existing
homing/triage licences), `ARCHITECTURE.md` Layer 6 (Ruling 15), `src/` and build files
COMMENT-ONLY per Ruling 16's enumerated family, `ratification_surfaces/` (Task 1's queue).
**The first commit stages `cowork_rulings_2026_08_09_second_stop.md` and this dispatch.**
**Anything else: hold.**

## Accepted outcomes

Holds are results; a STOP on any assumption returns to the user with evidence; an empty
sweep yield in Ruling 16 is a result (the family is then the six known sites); Task 2
stopping partway on capacity at a clean boundary is accepted, recorded as the first
continuation recorded it.

## Self-check (D-434) — run by Cowork before release

Written at a verified STOP with nothing running; every ruling applied is on disk in a
ratified record and ordered read whole (D-643); populations derived at task start; the
ruled acts are the only freeze relaxations, each naming its ruling; the one `src/` licence
is comment-only with its verification step stated; the push hard stop carried; the
completion statement barred; no value restated (D-431); reserved-word discipline checked
over this file's own text.
