# CC dispatch — land the brief-validation rulings and the RULED brief; scope the pack read-me's boundary wording so the brief's ruled inputs are no longer forbidden by its letter; boot NO session

> **Dispatch (Cowork, 2026-08-23, the forty-fifth Cowork session).** Executes Ruling 1 of
> `cowork_rulings_2026_08_23_brief_validation_sitting.md` and records Rulings 2 and 3 of the same
> sitting as applied (they are applied inside the refreshed brief this dispatch lands). **Written
> at a verified stop: the second-passage batch returned complete (`1ff83f569a` → `cf00b6af7b` →
> `59b3c0ff33` → `d2a517c8db`), was verified at the objects by the writing side, and nothing is
> running.** The tip was read by the writing side with `git for-each-ref` and `git show -s` at the
> explicit hash `d2a517c8db` and at nothing else.
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** Task 0 lands the sitting record, the REFRESHED
> brief (authored by the writing side — you land it, you do not write it), the modified handoff
> and this dispatch, and PUSHES. Task 1 makes ONE rendering edit to the boot-pack generator's
> read-me template — the boundary wording, scoped per the ruling — re-renders, and verifies the
> pack otherwise byte-unchanged. Task 2 closes. **THIS BATCH BOOTS NO SESSION FROM THE PACK,
> DERIVES NOTHING, AND OPENS NO PILOT ACT. When it lands, NOTHING stands before the blind
> session** — a FRESH COWORK SESSION (Ruling 1 of
> `cowork_rulings_2026_08_22_deriving_side_sitting.md`), which the user opens; not you.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The pilot*, *the boot pack*, *the withheld
> family*, *the authored table* — as the previous two dispatches explain them, unchanged. *The
> brief* — `cowork_blind_session_brief_harmony_boundary.md`, the document the blind deriving
> session reads FIRST, before the pack; written and refreshed by the writing side; tracked. *The
> read-me* — `tools/audit/derivation_boot_pack/harmony-boundary/00_READ_THIS_FIRST.md`, rendered
> by the generator from a template inside it; the subject of Task 1's one edit.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional. **(3)** `BUILD_AND_TEST.md` — CONDITIONAL, and this batch MEETS
> the condition (it runs the guard set and a generator): read it in full. **(4) DO NOT READ THE
> WHOLE OPEN-ITEMS INDEX.** Rule (a) reads `tools/audit/nongating_apparatus_rows.json` →
> `★_the_live_gating_answer` → `gating_ids`.
>
> **★ AND THE STANDING CLAUSE BINDS THIS ONE: read `cowork_handoff.md`, ITS CURRENT
> (FORTY-SEVENTH) BLOCK.** The findings series stands at F88 and this batch allocates NO finding
> number.
>
> **★ AND READ THE STANDING CLAUSES AT THE FILE, NOT AT A COUNT.** `cowork_audit_protocol.md`'s
> dispatch-protocol section IN FULL before Task 1 writes anything.
>
> **Then read, in full and in this order:**
> `cowork_rulings_2026_08_23_brief_validation_sitting.md` (this batch's authority — all five
> sections); the refreshed brief as landed by your own Task 0, WHOLE — you verify its statements
> about the pack against the pack you re-render; `cc_report_second_passage_withheld.md` (your
> predecessor's report, whole); the read-me at the tip object; the module docstring and the
> read-me template of `tools/audit/gen_derivation_boot_pack.py` (read the whole file — you edit
> its template).
>
> **★ ONE READ IS FORBIDDEN THAT EARLIER DISPATCHES ALSO FORBADE, AND IT MATTERS MORE NOW:** you
> do NOT open `cowork_joint_estimator_factorization.md`, and you do not open the two
> `ARCHITECTURE.md` oracle spans beyond what the generator's own run locates. The brief you land
> is the blind session's first read; nothing you write in this batch may add oracle content
> anywhere.
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/derivation_boot_pack.json` (the manifest; rewritten by the generator);
> `tools/audit/evidence_pin_membership.json` (Task 0's subject);
> `tools/audit/guard_classification.json`, `tools/audit/guard_state.json`,
> `tools/audit/epoch_write_path.json`, `tools/audit/recognizer_establishment_sort.json` (A4's
> subjects).
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS
> built, designed, scoped or run, no design, no repair, **no derivation of any specification
> statement, no held-out derivation, NO SESSION BOOTED FROM THE PACK**, no document archived,
> moved or deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit to
> `ARCHITECTURE.md`, `CLAUDE.md`, `DEFECT_TYPES.md`, `cowork_design_doc_template.md`,
> `cowork_audit_protocol.md`, the phase-definition surface, `DECISIONS.md` or any register
> source**, **no change to the generator OTHER THAN the one read-me-template wording edit** (no
> change to the authored tables, the criterion, the leak check, `withhold_passage()`, the marker
> or any STOP), **no edit to the brief beyond landing the refreshed file the writing side
> delivered** (its content is the writing side's; a defect you find in it is a STOP-and-report,
> not a fix), **no re-grading of any verdict and no movement of any withheld identity, document
> or passage**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end —
> the pre-edit run in CHECK mode; Task 1's run in write mode so the artifact it commits is the
> artifact of a real run (your predecessor's resolution, adopted here in terms). Verify every
> commit at the object by explicit hash. Every shell command carries `; echo "exit:$?"`.
> `git status` and `git diff` over the working tree time out on this mount and are denied by the
> armed guard — use the per-path explicit-hash forms named in A1. If staging is refused by a
> stale zero-length `index.lock` with no git process running, establish the cause (D-669), remove
> it, and report the act. Resolve every commit hash at the object before writing it anywhere. The
> reserved-word and vocabulary rules bind every line this batch writes.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1 of the brief-validation sitting:** *"One dispatch does both: (a) the read-me's
  rendered wording is scoped so it stays true — the whole-of-your-read sentence scoped to the
  repository with the brief excepted, and the boundary paragraph closing with the statement that
  what the session may read beyond the pack — its brief, files the brief stages to it by name,
  published research — is stated by the brief and by nothing in this directory; generic wording,
  true for every future subject; (b) the brief is refreshed to the ruled state … authored by the
  writing side and landed by the dispatch, not written by Claude Code."*
- **Ruling 2:** (P2) confirmed — fetched published research allowed, as the brief's §3 is written.
- **Ruling 3:** (P4) fixed — `cowork_blind_derivation_harmony_boundary_2026_08_23.md`.
- **The deriving-side ruling (standing):** the deriving side is a fresh Cowork session; the output
  file is committed by the next ordinary dispatch; the session itself commits nothing.

## Premise ledger

- **FACT** — the branch tip is `d2a517c8db` (parent `59b3c0ff33`), subject opening *"end state:
  the guard set run at the tree the close left"*; both refs at it. Read by the writing side at the
  explicit hash on 2026-08-23. **Nothing is unpushed.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 75, passing 74, failing 1
  ([[OI-372]]'s tool), not_run 4, historical 16. **Re-read it yourself at the object.**
- **FACT** — at the tip, `derivation_boot_pack.json` → `subjects.harmony-boundary.counted` reads
  `design_intent_class 244, candidates 75, verdicts IN 16 / OUT 59 / UNPLACED 0,
  withheld_identities_authored 16, withheld_identities_derived 17, withheld_identities_total 33,
  withheld_documents 1, withheld_passages 2, leaks 3, design_intent_entries_rendered 208,
  defect_type_rows_rendered 25, files_in_the_pack 7`. **Re-read it; A5 predicts it UNMOVED.**
- **FACT** — at `git show d2a517c8db:tools/audit/derivation_boot_pack/harmony-boundary/00_READ_THIS_FIRST.md`,
  the boundary section carries the sentences: *"Together with this file they are **the whole of
  your read**."*; *"…and every `cc_*` and `cowork_*` file outside this directory are **NOT
  opened**. No branch rule is taken. Nothing outside this directory is opened at all."* — the
  sentences Task 1 rewords, rendered from the template inside
  `tools/audit/gen_derivation_boot_pack.py` (the template also carries the hardcoded lead-in
  *"Two kinds:"* and the derived passage-count sentence; neither is this batch's subject).
- **FACT, measured by the writing side:** `cowork_rulings_2026_08_23_brief_validation_sitting.md`
  is on disk and absent from the tip; it does not carry the word `pinned` on any line. The
  refreshed `cowork_blind_session_brief_harmony_boundary.md` is on disk, modified against the tip
  by exactly the writing side's refresh (banner, §7's fixed name, §8's three ruled points, §9's
  dated amendment note; §§0–6 untouched); its sha256 is in the closing message.

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications: **(i)**
  `cowork_handoff.md` — ONE block (the forty-seventh) inserted above the committed content, the
  forty-sixth heading marked superseded-as-entry-point, otherwise additions only; **(ii)**
  `cowork_blind_session_brief_harmony_boundary.md` — the writing side's refresh, additions and
  replacements confined to the banner, §7's name sentence, §8 and §9's tail; **§§0–6 and every
  other line byte-unchanged** (check the diff at the object: no hunk outside those regions).
  **A modification at any OTHER tracked path is a STOP-and-report.** Untracked and to be landed
  by Task 0: `cowork_rulings_2026_08_23_brief_validation_sitting.md` and this dispatch
  (`cc_instruction_brief_ratification_and_readme_boundary.md`) — **two untracked paths.** The rest
  of the untracked population is NOT landed and is outside A1's STOP. **Check ordered (Task 0,
  first act):** per named tracked path `git diff --stat d2a517c8db -- <path>; echo "exit:$?"` with
  the changed passages written to a scratch path outside the repository and read with the file
  tools; per named untracked path `git ls-files --others --exclude-standard -- <name>;
  echo "exit:$?"`; and the whole tracked population enumerated with the sanctioned enumeration
  tool.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  The reds this batch causes and must clear: **the membership check, cleared by Task 0 step 2**;
  **the boot-pack generator's own `--check`, red between the template edit and the re-render,
  inside the SAME commit**; **`gen_session_start_read_size.py --check` at the close**. Any other
  red is classed by its measured cause, and one outside this batch's own acts' subjects is a
  STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration, from EVERY route.** **Route A** — the
  ruling-record count rises by exactly ONE (51 → 52) and ONE name is added
  (`cowork_rulings_2026_08_23_brief_validation_sitting.md`). **Route B** — the record carries the
  word `pinned` on no line. **Route C** — no tool is added. Measure the diff against the committed
  blob at `d2a517c8db` BEFORE accepting it; an additive derived cross-reference caused by the
  landed record's own naming of other documents is reported rather than absorbed; movement beyond
  these is a STOP-and-report.

- **ASSUMPTION A4 — the guard registry.** This batch ADDS NO TOOL. `guard_state.json`: the
  population UNCHANGED at run 75; the failing set unchanged at [[OI-372]]'s tool. The three other
  registry artifacts UNCHANGED except any run-stamp field. A member entering or leaving any of
  them is a STOP-and-report.

- **ASSUMPTION A5 — the manifest and the pack after Task 1.** **Every field of `counted`
  UNMOVED** (the FACT above). The manifest's whole difference against `d2a517c8db` is confined to
  what derives from the read-me's own content (its rendered bytes or record, if the manifest
  carries one) — measure it whole and report it; a difference touching any verdict row, identity,
  document, passage, leak or member record is a STOP-and-report. At the pack: **only the read-me
  changes**; members (1)–(6) byte-unchanged against `d2a517c8db`, each checked at the object. The
  reworded read-me: **(i)** keeps its title, its unit sentence, its six-file list and order, its
  stop-on-meeting clause, its what-was-cut section and its output section; **(ii)** the
  whole-of-your-read sentence reads that together with this file the six files are the whole of
  the session's read **within this repository, apart from the brief that dispatched it**; **(iii)**
  the NOT-opened list stands, with the brief excepted in its own words; **(iv)** the paragraph
  closes: what the session may read beyond that — its brief, score and analysis files the brief
  stages to it BY NAME, published research — **is stated by the brief, and by nothing in this
  directory**; **(v)** it does NOT name the brief's filename, any staged file's name, any ruling
  record, or anything about what was withheld beyond what it already says. Wording is yours;
  content is fixed by (i)–(v); **the read-me template is the ONLY generator lines that change.**

## The declared start state — at the tree you will meet

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing
   red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — caused
   by this dispatch's own input, the ONE untracked root-level `cowork_rulings_*.md` record.

**`gen_guard_state.py --check` will open "STALE vs the run" for the same cause (F83); it is not a
third failing check.** A THIRD failing verdict is a STOP-and-report.

## Findings in this batch

**No finding number is allocated; the series stands at F88.** Ruling 9's routing binds as before.
**A defect you find in the refreshed brief is a STOP-and-report — the brief is the writing side's
text and you do not fix it. A further paraphrase-shaped leak you happen to MEET in the spans you
touch is SURFACED in your report with its verbatim and its location — not cut, not ruled.**

## Task 0 — land the record and the refreshed brief, clear the membership check, and PUSH

1. **A1's check first, entirely at content-addressed objects.**
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE its difference against the
   committed blob BEFORE accepting it**, against A3.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `cowork_blind_session_brief_harmony_boundary.md` (modified — the writing side's refresh),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_23_brief_validation_sitting.md`, and
   `cc_instruction_brief_ratification_and_readme_boundary.md` (new) — **five paths.** Subject,
   exactly:
   `record: the 2026-08-23 brief-validation rulings landed and the blind session's brief is RULED; evidence-pin membership regenerated`.
   Staged by explicit path, never by `git add -A` or `git add .`.
4. **PUSH**; verify at the object that `origin/master` names your Task 0 commit.
5. **Re-run `gen_evidence_pin_membership.py --check`: it must PASS.**

**Registered expectation E0:** exactly the five named paths; `ruling_records_read` 52 at the
committed artifact; the membership check passing; `origin/master` at the Task 0 commit.

## Task 1 — the read-me boundary wording, the re-render, the byte-unchanged pack — ONE commit

1. **Edit the read-me template in `tools/audit/gen_derivation_boot_pack.py`** to A5's (i)–(v),
   and nothing else in the file.
2. **Run the generator; run `--check`** — a halt is a STOP-and-report.
3. **A5, measured at the manifest and at the pack files, every item** — including reading the
   rendered read-me whole and quoting its reworded boundary paragraph in your report.
4. **Run the FULL guard set. A2 and A4 must hold.** Commit the generator, the manifest, the
   read-me, and the guard artifacts the run rewrote, with the subject
   `the pack read-me's boundary scoped to the repository with the brief excepted — what the blind session reads beyond the pack is the brief's to state; no session booted`.
   Push; verify at the object.

**Registered expectation E1:** the read-me at A5 (i)–(v) and quoted in the report; every other
pack file byte-unchanged at the object; `counted` unmoved field by field; `--check` green;
**`CLAUDE.md`, `ARCHITECTURE.md`, `DEFECT_TYPES.md`, `cowork_design_doc_template.md`,
`cowork_audit_protocol.md`, the phase-definition surface, `DECISIONS.md` and every register
source byte-unchanged**; the seventy-five verdict rows and both passages byte-unchanged at the
manifest; no session booted.

## Task 2 — the close

1. One `STATUS.md` pointer entry per task that did work; the previous batch's entries move
   verbatim to `STATUS_ARCHIVE.md` through `gen_status_batch_bound.py --apply` at its three
   declared authored inputs (authored-input maintenance, D-648 — licensed here in terms);
   `gen_session_start_read_size.py` regenerated (A2).
2. The FULL close appended to `cowork_away_returns.md` as a **THE BRIEF RULED AND THE PACK'S
   BOUNDARY TRUE: NOTHING STANDS BEFORE THE BLIND SESSION** section — both guard states, every
   SHA, E0–E2 graded, A3–A5 graded at the artifacts, the declared departures, the quarantined
   questions (or none new), the plan lines (**the blind derivation by a FRESH COWORK SESSION —
   the user opens it; the output lands under the ruled name and is committed by the next ordinary
   dispatch**; the comparison after it; the sizing pilot after the method is ruled; the ledger
   before the framework phase), and **the plan's tell in one sentence: did this batch produce
   anything other than the landed record and refreshed brief, the one reworded read-me, the
   re-rendered manifest and the report? If yes, name it.** The report is
   `cc_report_brief_ratification_and_readme_boundary.md`.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run.

**Registered expectation E2:** at the tree carrying the close, a fresh full guard run — **every
run check passing except [[OI-372]]'s tool, zero STOPs, the guard population at run 75** — run and
read, committed only after the run that produced them.

## What this batch does NOT do

- **No session booted. No derivation. No comparison. No oracle opened. No pilot act at all.**
- **No change to the generator beyond the read-me template; no edit to the brief's content; no
  movement of any verdict, identity, document or passage; no paraphrase sweep.**
- **No edit to any boot-list member at its home; no register entry, sort artifact or register
  source touched.**
- **No finding number allocated.** [[OI-179]] stays OPEN and GATES; [[OI-372]], [[OI-374]] stand.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule (c).**
- **No `src/` change, no golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #19 — the pack's boundary claim is made TRUE rather than overridden, and
   the blind session's inputs stay the ruled ones; #6 — what the session may read beyond the pack
   has ONE home, the brief, and the read-me points there instead of keeping a second list; #12 —
   the read-me's other sections are kept, the brief's §§0–6 are untouched, and the brief's §9
   records the amendment rather than replacing history; #17(f)/D-431 — every BEFORE figure cited
   at the artifact and ordered re-read; #24 — no comparison asserted. Conforms.
2. *Conventions:* American English; no self-invented labels; music-theory words in their musical
   sense; towards, never against.
3. *Figures and premises:* every quantity named to its artifact; the rulings quoted at their
   record; the read-me sentences quoted at the tip object; the brief's refresh enumerated by
   region and checkable at the diff.
4. *File-tools rule:* the writing side's own departures are in the forty-seventh handover block.
5. *Uncertainty:* the manifest's read-me-derived difference is declared measured-not-predicted;
   every other A5 item is predicted, and any deviation is a STOP rather than a figure to absorb.
6. *Re-read from disk before release:* performed over the delivered file, hash-verified after
   re-staging (F74), recorded in the forty-seventh handover block.

---

*Provenance: Cowork, 2026-08-23, the forty-fifth session, written at branch tip `d2a517c8db`
under Rulings 1–3 of `cowork_rulings_2026_08_23_brief_validation_sitting.md`. TOWARDS the
ultimate objective and TOWARDS the guiding principles.*
