# CC dispatch — the cheap look on a false header contract, then the `docs/scoring_model.md` pass over three rows

> **Status: ACTIVE DISPATCH, written 2026-08-13 (Cowork), at a verified STOP** — the class-licence
> batch completed and Cowork verified it at the objects by explicit hash: the licensed commit
> touched **two paths**, forty changed lines, and an independent filter over the commit object found
> **no non-comment changed line under `src/`**. **OI-315 is flipped** and out of the derived
> populations. Nothing is running.
>
> **★ THE PREVIOUS BATCH'S METHOD RESULT IS CARRIED FORWARD AS AN INSTRUCTION, NOT A COMPLIMENT.**
> Every account of that row placed its residue in a frozen test file; **one member was in a
> production header.** Enumerating at the code rather than from the row's list is what found it.
> **Task 2 below enumerates at the document, not from the rows' lists, for the same reason.**
>
> **Read IN FULL, and read FIRST:** `open_items/OI-45.md`, `open_items/OI-183.md` and
> `open_items/OI-274.md` END TO END; `docs/scoring_model.md`; `CLAUDE.md`'s scoring-model section.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_scoring_model_pass.md`. Acts dated from
> the clock; **no positional count anywhere**; cite rulings by number, not by date.
>
> **★ All standing rules as adopted.** D-253 in every dialect; a comment-only check, if one were
> ever needed, runs at the commit object. NO TRANSCRIBED VALUES (D-431). Hold-don't-guess.
> **NO `src/` EDIT — no licence is granted by this dispatch, and none is implied.** No golden, no
> test changed, no corpus of scores, nothing under `tools/corpus/` or `tools/robust_stop/`, no
> measurement of the analysis, no design. D-231 and #8 stand. **Phase 1's completion statement is
> not written, not drafted and not partially written.** Commit and push per task boundary; `origin`
> only.

## 0a. THE PREDICTION, REGISTERED BEFORE THE LOOK RUNS (#17b)

Task 1 answers: **does anything consume `KeyResolveDump::candidates`' emptiness?**

**PREDICTION — moderate confidence: NOTHING DOES.** Ground: it is a diagnostic dump field, and the
record's pattern is that dumps are read by people and by ad-hoc triage rather than by production
code; and if a reader treated emptiness as *anchor or fallback*, the insufficient-pitch-classes
fallback with notes present would already have produced visible wrong behaviour rather than a stale
comment.

**What would refute it:** any reader of that field that branches on its being empty, or any
document or tool that states the emptiness as a contract it relies on.

**What the look cannot settle**, stated in advance so the result is not over-read: whether the
comment should be corrected. That is the worth test's call once the consumer question is answered,
and it is not made here.

## 0b. THE PREMISE LEDGER (#17a)

**FACT — verified by Cowork at the objects.** The class-licence batch's three commits, their changed
paths and the comment-only property of the licensed one, by explicit hash. OI-315's flip and its
departure from the derived views.

**FACT — established by CC at the code and NOT re-established here:** that
`"(empty on anchor/fallback)"` is false at HEAD in both halves.

**ASSUMPTION — each checked BEFORE the act resting on it; a refutation is a STOP.**

- **A1.** The consumer question is answered **at the code**, over readers of the field enumerated
  at the call graph — not from any document's account of what reads it. *Check: every reader named
  with its site; state whether it branches on emptiness.*
- **A2.** Task 2's population is enumerated **at `docs/scoring_model.md` itself**, not from the
  three rows' lists. *Check: report any item a row names that the document does not carry, and any
  the document carries that no row names. **The previous batch's finding is that a row's list can be
  wrong about its own subject.***
- **A3.** OI-274's **banner half is performed** — it came back WORTH FIXING on limb (b), measured
  rather than imagined: a previous batch had to commit a file over a banner forbidding that commit.
  *Check: the banners corrected are on tracked files and say something false about those files.*
- **A4.** A row flips **only** where its own closing conditions are met across all halves, since
  rule (f) gives a row one state. **OI-274's second half is a user ruling and is NOT performed
  here, so OI-274 cannot flip.** *Check: state per row which conditions are met and which are not.*

## 0c. THE TASKS, IN ORDER

**Task 1 — the cheap look (A1). FIRST, read-only.** It is D-671's class — an enumeration over a
population, published whole or not at all — so it cannot absorb a capacity shortfall and goes first.
Enumerate readers of `KeyResolveDump::candidates` at the call graph; report whether any branches on
emptiness, and grade the prediction at §0a as met or refuted. **No fix, no licence, no row, no
discard** — the worth test's verdict is the user's once the consumer question is answered.

**Task 2 — the `docs/scoring_model.md` pass (A2, A3).** In one pass over the one document, since
that economy is the reason these three rows were grouped: re-aim OI-45's stale anchors and give
`kHalfDimFirstInversionBonus` its entry; give OI-183's unmentioned constants a by-name mention **or
state per constant which table cell already covers it**, which is the row's own alternative and not
a lesser outcome; give OI-274 its scoping sentence and re-stamp its footer. **Former wording
preserved in place (#12)** wherever a body is corrected. A per-item pass: it may stop at an item
boundary under D-672 with the three things that rule names recorded. Commit and push.

**Task 3 — OI-274's banner half, and the flips (A3, A4).** Correct the two draft banners standing on
tracked files. Then flip each of the three rows **only** where its conditions are met; **OI-274
cannot flip** and its cell must say what remains. Commit and push.

**Task 4 — the close.** One `STATUS.md` pointer entry per task, nothing else in that file. Append
the close to `cowork_away_returns.md`. Report at the objects, with commit hashes.

## 0d. WHAT IS DELIBERATELY NOT DONE

**No `src/` edit and no third licence** — the header comment stays as it is until the worth test has
run on Task 1's answer. **No scoring value, threshold, gate or template is added, changed or
removed**; this is a documentation pass over a document that describes them, and the sync rule runs
the other way. No sweep beyond the three rows' own subjects. **OI-274's second half — whether the
scoring trigger should also name the joint estimator's specification — is a user ruling and is
untouched.** OI-207 is last and alone. OI-179 stays OPEN and GATES.

## 0e. STOP RULES

Halt with a STOP in `cowork_away_returns.md` if: a reader of the field branches on emptiness in a
way that makes the comment load-bearing; Task 2's enumeration at the document disagrees with the
rows in a way A2 cannot resolve by reporting; correcting an item would require touching `src/` or a
scoring value; a banner's falsity turns out not to be about the file it stands on; or a guard goes
red for a cause that is neither this dispatch's own edits nor already recorded — the two standing
reds are recorded and are not that.

---

*Provenance: Cowork, 2026-08-13. Third of the five ratified acts, with the cheap look folded in on
the user's ruling after a surface measuring four options against the ultimate objective, the guiding
principles, the guardrails and the standing rules. Self-check run before release (D-434).*
