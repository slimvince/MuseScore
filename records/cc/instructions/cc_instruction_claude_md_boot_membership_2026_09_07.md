# CC dispatch — the ordinary session-start read of `CLAUDE.md` becomes a NAMED MEMBERSHIP, and the remainder becomes conditional

> **Dispatch (Cowork, 2026-09-07), on the user's ruling of this date.** He was put a self-contained
> surface naming six spans read at boot and eight read only when a session's work touches them, with
> its costs and its risk stated, and the choice question in a later turn. **His word was "yes".**
>
> **Written at a verified stop.** The tip at writing is
> `f7d6d7c391cdbe0881f2013faa260626866af721`, read at `.git/refs/heads/master`. The prune-at-amendment
> batch closed before this was written; nothing is running.
>
> **NOT in this batch:** the row-by-row extraction (PAUSED); the pruning pass (PAUSED); any move of
> any span of `CLAUDE.md` to `CLAUDE_ARCHIVE.md`; any change to what any rule of `CLAUDE.md` SAYS;
> any change to the session-start read of `DECISIONS.md`, `STATUS.md` or the derived gating answer;
> any decisions-register write; the repair of the forward-bound trigger (OI-379); and the restructure
> of `CLAUDE.md` into satellite files, which is a separate question standing with the user.
>
> **Read first, in full — the ordinary session-start read (`D-230`, P-1):** `CLAUDE.md`,
> `DECISIONS.md`, `STATUS.md`, and the derived gating answer. **This batch is the last one that reads
> `CLAUDE.md` whole under the old practice, and it must, because it edits it.** **Then read, in
> full:** `cowork_rulings_2026_08_17_session_start_read_sitting.md` at its own object (`D-643`) —
> Rulings 1 and 3 are the precedent this act repeats; and §5(D) of
> `cowork_rulings_2026_08_16_preparation_return.md`, for the reason stated at Task 1 clause 5.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, no build, no measurement of the analysis, nothing under `tools/corpus/` or
> `tools/robust_stop/`, no design, no derivation of any specification, no document archived or
> deleted as a file, no decisions-register entry and no `D-NNN` allocated, and no edit to
> `DECISIONS.md`, `ARCHITECTURE.md`, `FRAMEWORK.md`, `OPEN_ITEMS.md`, `CLAUDE_ARCHIVE.md`, any ruling
> record, or any governing document other than `CLAUDE.md` and `STATUS.md`. **No open-items row is
> created, flipped or discarded — see Task 2.**
>
> **Run the FULL guard set BEFORE the first edit and again at the end; record both states.** Verify
> every commit at the object and record the SHAs in the close. **Commit and push per task.**

## Ruling ledger

- **The user's ruling, 2026-09-07.** Put as a named question — *"Do you ratify that list as the
  ordinary session-start read of `CLAUDE.md` — six spans read at boot, eight read only when a
  session's work touches them?"* — after a surface delivered in its own earlier turn. **His word:
  "yes".** The question stated that a yes carries three things: the reinterpretation of the editor
  bash-rules section, the one split that does not fall on a heading, and a new block naming what is
  conditional. **All three are ordered below and none is added beyond them.**
- **The precedent, recorded in `CLAUDE.md` itself.** Ruling 1 of
  `cowork_rulings_2026_08_17_session_start_read_sitting.md` amended the open-items register's rule
  (a), and Ruling 3 demoted `BUILD_AND_TEST.md` from an unconditional session-start read to a
  conditional one. **Both were executed by `cc_instruction_preparation_ninth.md`, Tasks 1 and 3.**
  So a user ruling changing what a session reads, executed as an edit to `CLAUDE.md`, is an
  established act with an executed precedent. **No new licence is claimed and none is needed.**

## Premise ledger

*(Each FACT established at the object named beside it by the writing side on 2026-09-07.)*

- **★ FACT — NOTHING IN `CLAUDE.md` MANDATES READING `CLAUDE.md`, so this act WRITES a rule rather
  than amending one.** Established by searching the file for every session-start mandate it carries:
  the open-items register section's rule (a) mandates the derived gating answer; the decisions
  register section's rule (a) mandates that INDEX; the build-and-test section's block mandates
  `STATUS.md` and `DECISIONS.md` and demotes `BUILD_AND_TEST.md` to a condition; and the conventions
  section's 2026-08-29 entry binds "the reads rule (a) and the register sections mandate" against a
  single-file opening instruction. **No clause names `CLAUDE.md` itself.** The whole-file read has
  been PRACTICE, not rule. *(The consequence, stated so it is not mistaken: this act does not loosen
  a rule, it replaces an unwritten habit with a stated membership, and the membership is narrower
  than the habit.)*
- **FACT — the fourteen spans are located by THIRTEEN headings and ONE block anchor.** Twelve of the
  spans are whole `##` sections; the gate-policy span additionally carries its `###` blocks (A) to
  (D); and the session-start read block has no heading of its own, so it is located by the text it
  opens with and the text it ends with, both quoted at Task 1. The membership is named by heading or
  by quoted anchor and never by line number (`D-307`); Task 1 derives the coordinates at the tree and
  STOPs on any anchor it does not find exactly once.
- **ASSUMPTION A1** — the working tree carries no tracked modification and this dispatch is among the
  untracked root files. **Check ordered (Task 0, first act, with the sanctioned enumeration tool):**
  any tracked difference is a STOP-and-report.
- **ASSUMPTION A2 — the guard set's start state is 80 run, 13 failing.** **RELAYED from the previous
  batch's report and NOT re-measured by the writing side.** **Check ordered (Task 0, before any
  edit).** A state matching neither that nor the previous batch's recorded end state is a
  STOP-and-report. **Nothing is adjusted to reach a number.**

## Task 0 — land the dispatch, and measure the start state

1. **A1's check**, over the whole tracked population with the sanctioned enumeration tool.
2. **A2's check:** the full guard set, run and read. Record it; do not act on it.
3. Commit this dispatch and this line's current handoff entry if it is not yet landed. Push; verify
   at the object. **Record the SHA.**

**Registered expectation E0:** A1 holds; the guard state is recorded as measured.

## Task 1 — the membership written into `CLAUDE.md`

### Where it goes

**Into the session-start read block of the build-and-test section** — the block opening *"Always read
these two files at the start of every session:"* — immediately after the existing conditional-read
sentence for `BUILD_AND_TEST.md` and its 2026-08-17 provenance note. That block is the ONE place the
file states what is read at start and what is conditional, so the new text sits beside its own kind
and no second home is created (#6).

### What it says

Two lists and one sentence of provenance. **Every span is named by its HEADING, never by a line
number.**

**READ AT SESSION START — six spans:**

1. **Guiding principles**, from its heading through the *Delegation pointer* paragraph that closes it.
2. **The open-items register** section, whole.
3. **The decisions register** section, whole.
4. **The session-start read block itself** — the block this text sits in, from *"Always read these
   two files…"* through the paragraph ending *"…NOT part of the session-start read."*
5. **Conventions**, whole.
6. **The self-check after every coding exercise**, whole.

**READ CONDITIONALLY — eight spans, each with its condition:**

| Span, by heading | Read by |
|---|---|
| **Project context** | a session that touches the composing module or needs the full project-context pointer |
| **Autonomous operation — composing module** | a session that edits code under the composing module or runs the build-fix-test loop |
| **Build and test commands** — the command blocks below the session-start read block | a session that builds, tests, or runs a measurement tool whose command lives there |
| **Gate threshold and preset policy**, including blocks (A) to (D) | a session that measures the analysis, changes a gate or threshold, re-baselines, or needs a ratified baseline |
| **Scoring model** | unchanged — a session that touches scoring logic (the section's own standing condition) |
| **Score corpora** | any task involving scores |
| **Local patches — do not revert** | a session that edits MuseScore's own code, or that runs or reviews a dependency update |
| **VS Code extension — bash command rules** | any session that runs bash commands |

**The provenance sentence:** that this membership is the user's ruling of 2026-09-07, taken on a
surface delivered in its own turn with the choice question in a later one, and that until that date
the whole-file read was practice and was mandated by no clause of this file.

### The three things the ruling carried, each ordered explicitly

1. **The editor bash-rules section moves to the conditional list**, although its own heading reads
   *"MANDATORY, every session"*. **Its heading is amended to name its real condition** — any session
   that runs bash commands — which is what its body governs. **This is the one place where the act
   changes what a heading says**, it was named in the question the user answered, and it is recorded
   in the report as such.
2. **The one split that does not fall on a heading** is the build-and-test section: its
   session-start read block stays at boot and its command blocks become conditional. **No other
   section is split.**
3. **The conditional list is announced in the kept half**, which is what the table above does, so a
   session that never reads a section still learns it exists and when to read it.

### The constraints on the edit

4. **DERIVE the coordinates at the tree; do not take them from this dispatch.** Locate each of the
   fourteen headings in `CLAUDE.md` at the Task 0 commit. **A heading not found exactly once is a
   STOP-and-report**, never a guess.
5. **Rule (D) — prune at amendment — is TESTED here, not assumed either way, and the test is stated
   rather than the conclusion.** That rule moves text a change SUPERSEDES, in the same act, with a
   dated pointer left behind. **The membership block supersedes nothing** — it adds a statement where
   the file previously carried none. **The bash-rules heading is the one wording this act changes,
   and its treatment is the executing side's to decide at the object, between exactly two readings,
   with the one it takes and its ground recorded in the report:** either the former heading is
   SUPERSEDED text, in which case rule (D) fires and it moves with a dated pointer; or the heading
   states something FALSE about its own scope — it says every session, its body governs bash commands
   — in which case it is a doc-sync correction and #12 is satisfied by the former wording being
   quoted in this batch's own record. **Neither reading may be taken silently, and a third is a
   STOP.** **If any other wording this act touches turns out to be superseded, rule (D) fires on it
   and the move is made in the same act.**
6. **NOT ONE RULE'S TEXT CHANGES.** No principle, no register rule, no convention, no gate value, no
   patch record. The act adds a membership block and amends one heading. **Any other diff is a STOP.**
7. **The self-check runs on the diff before the work is reported**, as `CLAUDE.md`'s own self-check
   section requires.
8. ONE commit, push, verify at the object.

**Registered expectation E1:** every one of the thirteen headings and the one quoted block anchor
found exactly once; the diff touches the session-start read block and the bash-rules heading and
nothing else; no rule text changes; the membership names every one of the fourteen spans and nothing
beyond them; and clause 5's test is answered with its ground rather than assumed.

## Task 2 — what is deliberately NOT done, and why

1. **NO open-items row is created.** This is a ruled change carried out, not a defect found. Creating
   a row would put a non-issue in the register.
2. **NO decisions-register entry is written and no `D-NNN` is allocated**, although rule (c) asks for
   one in the commit that records a ratification. **The reason, and it is the writing side's reading
   rather than a ruling:** `CLAUDE.md`'s own clause on discharging rule (c) once it has been missed
   says an accumulated run of rulings is CLASSIFIED first, put to the user as a reading file, and
   landed in ONE commit, and that no entry is written before the user rules on the classification.
   That debt exists and is unclassified. Writing this one entry alone would leave a register partly
   current and partly not, with nothing to tell a reader which. **So this ruling joins the debt, and
   the close says so in terms.** The user can overrule this reading in one word.

## Task 3 — the close

One `STATUS.md` pointer entry per task, **and the forward-bound move performed on this batch itself**
— `gen_status_batch_bound.py` re-aimed at all six authored fields with `MOVE_KIND = "ordinary"`, the
then-previous batch being the prune-at-amendment batch, **this batch's own entries written FIRST and
`--apply` run second**, for the mechanical reason the prefix adjustment gives. Omitting it reproduces
the defect two batches were written to clear.

The report `cc_report_claude_md_boot_membership_2026_09_07.md`: both guard states, every SHA, every
expectation graded, the fourteen headings with the coordinates derived for each, the whole diff of
`CLAUDE.md` characterised line by line, the self-check's output, and every declared departure
reported rather than absorbed. **If `CLAUDE.md`'s or `STATUS.md`'s change stales the read-size
measurement, regenerate it in the end-state commit** and say so.

**Registered expectation E2:** at the tree carrying the close, a fresh full guard run re-derives, run
and read rather than inferred; every addition beyond A2's recorded start state is traced to this
batch's own ordered acts.

## What this batch does NOT do

- **It changes no rule of `CLAUDE.md`** — only which spans a session must read before starting, plus
  one heading amended to state its real condition.
- **It moves nothing to `CLAUDE_ARCHIVE.md`** and touches no archiving tool.
- **It does not restructure `CLAUDE.md`** into satellite files; that stands with the user.
- **It does not repair the forward-bound trigger** (OI-379), which stands with the user.
- **It opens no paper, no extract and no companion; it runs no sweep; it moves no verdict; and it
  lifts no gate.** The row-by-row extraction and the pruning pass stay paused.
