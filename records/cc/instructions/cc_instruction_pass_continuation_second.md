# CC dispatch — land the boot-list sitting record; CONTINUE the per-entry pass of the widened July screen from the artifact's own `NOT YET READ` set, a second time

> **Dispatch (Cowork, 2026-08-22, the forty-second Cowork session).** Executes Ruling 1 of
> `cowork_rulings_2026_08_22_dispatch_order_sitting.md` a second time — the pass it orders was
> stopped at a recorded member boundary by the previous batch and is continued here — and lands
> Ruling 1 of `cowork_rulings_2026_08_22_boot_list_sitting.md`, which orders NO act in this batch.
> **Written at a verified stop; nothing else is running and no dispatch was active when this was
> written.** The tip was read by the writing side with `git show -s` and `git for-each-ref` at the
> explicit hash `40fb613060` and at nothing else.
>
> **★ WHAT THIS BATCH IS.** Task 0 lands the boot-list sitting record, the modified handoff and this
> dispatch, and PUSHES. Task 1 CONTINUES the per-entry pass over the widened screen's `NOT YET READ`
> set, re-derived at the artifact, by the inherited method, in the artifact's own order, stoppable
> at a member boundary. Task 2 closes. **No pilot act** — the pilot's opening is the next decision
> surface, and the boot-list ruling this batch lands is its prerequisite, not its opening.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *A changed passage* — one contiguous run of
> changed lines inside one commit's edit of one document; the tools' own artifacts and field names
> say "hunk", and that word is used below ONLY where it names a field or quotes a tool. *The document
> set* — the members `tools/audit/specification_document_set.json` lists. *The widened screen* — the
> July screen's second population, every flagged changed passage of the restructuring period whose
> file is a document-set member, with an authored verdict per passage. *The inherited method* — the
> four verdict classes of `tools/audit/gen_july_screen.py`, applied in their DECLARED ORDER, with
> POSITIVELY CODE-INFLUENCED applied first, as that tool's docstring states them. *The curated boot
> list* — the implementation-free read list a derivation session boots from; ruled today, landed
> here, used by nothing in this batch.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional, and explicitly NOT demoted. **(3)** `BUILD_AND_TEST.md` is a
> CONDITIONAL read — **and this batch MEETS the condition**, because it runs the guard set and the
> audit generators, so read it in full. **(4) DO NOT READ THE WHOLE OPEN-ITEMS INDEX.** Rule (a)
> reads the DERIVED GATING ANSWER narrowed to its identity list —
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`. The INDEX
> remains the AUTHORITATIVE STATUS SURFACE.
>
> **★ AND THE STANDING CLAUSE BINDS THIS ONE: read `cowork_handoff.md`, ITS CURRENT (FORTY-SECOND)
> BLOCK.** **The findings series stands at F88 when this batch begins** — no number was allocated by
> the thirty-ninth to forty-second sessions or by the batch this one continues. **This batch
> allocates NO finding number either** — see *Findings in this batch* below.
>
> **★ AND READ THE STANDING CLAUSES AT THE FILE, NOT AT A COUNT.** `cowork_audit_protocol.md`'s
> dispatch-protocol section is to be read **IN FULL** — every `###` section carrying the membership
> marker binds this dispatch and your report — before Task 1 writes anything. No number is stated
> here and none is to be written anywhere in this batch.
>
> **Then read, in full and in this order:** `cowork_rulings_2026_08_22_dispatch_order_sitting.md`
> (Task 1's authority); `cc_report_step_zero_return_executed.md` whole (the batch this one continues
> — its §5 is the standing form of the stop, its §6 is the calibration of the not-cleared class
> that batch established at a git object, and it binds your verdicts); `cc_report_step_zero.md` §4
> and §6; `cowork_rulings_2026_08_22_boot_list_sitting.md` (what Task 0 lands; it orders nothing
> here); the forty-first handover block of `cowork_handoff.md` (below the forty-second); and the
> docstring of `tools/audit/gen_july_screen.py` — **the one tool Task 1 edits and may not
> re-invent.**
>
> **Bind at these artifacts, never at a recollection of them:** `tools/audit/july_screen.json` and
> `tools/audit/july_screen_report.md` (extended by Task 1, **the existing sixty-eight verdicts
> byte-unchanged by published digest, and every verdict the two previous batches authored
> byte-unchanged**); `tools/audit/specification_document_set.json` and
> `tools/audit/period_stratum_split.json` (**read and never regenerated** — their generators are not
> edited by this batch); `tools/audit/doc_change_candidates.json` and
> `tools/audit/doc_change_candidates_hunks.jsonl` (**read and never regenerated**);
> `tools/audit/evidence_pin_membership.json` (Task 0's subject);
> `tools/audit/nongating_apparatus_rows.json` (**read and never written**);
> `tools/audit/guard_classification.json` and `tools/audit/guard_state.json`.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS built,
> designed, scoped or run, no design, no repair, **no derivation of any specification, no frame, no
> pilot act, no fact-gate admission, no ledger, no mining read, no session booted from the curated
> boot list and no generator for its per-session cut built** (Ruling 1(a1) of the boot-list sitting
> dispatches that generator WITH the pilot's opening, not before), no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit to any screened
> document** (`ARCHITECTURE.md`, `docs/scoring_model.md`, `STATUS.md`'s content beyond Task 2's
> pointer entries, and every document-set member are READ and never written, however plainly wrong
> a passage is found), **no edit to any generator other than `gen_july_screen.py`'s widened authored
> block**, **no re-grading of any authored grade or of any verdict already authored**, and **no
> caller flag or candidacy acted on**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end.**
> The declared start state is below and is stated **at the tree you will actually meet**. **Verify
> every commit at the object by explicit hash.** **The reserved-word conventions bind all new prose.
> The E-ordering rule stands, at both grains: state at the close that the end state is NOT asserted
> there, and let the further commit carry it.** **Every guard run is taken at a quiet tree.** **Every
> shell command carries `; echo "exit:$?"` and no command produces large output to the terminal**
> (`CLAUDE.md`, the VS Code extension rules). **`git status` and `git diff` over the working tree are
> measured to time out on this mount and are in any case denied by the armed guard — use the
> per-path explicit-hash forms named in A1.**
>
> **★ THE VOCABULARY RULES BIND EVERY LINE THIS BATCH WRITES** — TOWARDS the ultimate objective and
> TOWARDS the guiding principles, never *against* either; **no self-invented or abbreviated term**:
> write *a changed passage*, *the current commit*, *untrusted source*, *measurement tool* (the
> 2026-08-17 vocabulary ruling), and explain any term of the tools' own in plain words at its first
> use in your report. **Music-theory words in their musical sense only**: a *screen* here is a
> measurement over documents; *score* is never used numerically; *measurement tool* and never the
> reserved word.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1, 2026-08-22 dispatch order — this batch's authority for Task 1:** *"The dispatch
  continues the per-entry pass from the artifact's own `NOT YET READ` set — re-derived at the
  artifact after the exclusion of `STATUS.md`, never carried from any report's account of it — by
  the inherited method, in the artifact's own order, stoppable at a member boundary with the stop
  recorded in the standing form. The pilot is NOT opened by this dispatch."* The previous batch
  executed it and stopped at a recorded member boundary; the ruling is not spent by a stop, and
  this batch continues under it.
- **Ruling 7 of 2026-08-21, still the screen's authority:** *"… Its per-document distribution feeds
  the reading depth (Ruling 12) and the ordering of units. Failure signal, ruled with it: if most
  passages land UNDETERMINED the premise is not measurable, and that is a STOP to the user, not a
  licence to proceed."*
- **Ruling 1, 2026-08-22 boot-list sitting — LANDED by Task 0, ORDERING NOTHING HERE:** *"The
  draft's six members … and its eight exclusions … are the STANDING boot list for derivation
  sessions, with these three amendments: (a1) A per-session WITHHELD LIST, generated, never
  hand-cut. … (a2) The ledger hole is carried exactly as Ruling 8 of 2026-08-21 declares it … (a3)
  `DEFECT_TYPES.md` is admitted at its TYPE and DEFINITION columns only …"* and, in the same
  record: *"The generator of (a1) is dispatched with the pilot's opening, not before, and is the
  one mechanism this ruling adds."* **So this batch builds no generator and boots no session from
  the list.**
- **The calibration the previous batch established at a git object, binding your verdicts
  (`cc_report_step_zero_return_executed.md` §6):** *"the four classes are applied in the DECLARED
  ORDER, so that class is reached only where the first three all fail; the second fires where a
  change records what a named user act ruled, ratified or DIRECTED, and a homing act performed
  under a named user ruling is such a change."* And its one exercised line: *"a user direction to
  PERFORM a survey is not a user act whose CONTENT the change records … while a homing act, whose
  content is a decision the register already holds, is."*

## Premise ledger

- **FACT** — the branch tip is `40fb613060` (parent `d276e8afb2`), subject line *"end state: the
  guard set run at the tree the close left — every check passing except OI-372's tool, zero STOPs,
  the guard population unchanged"*; `refs/heads/master` and `refs/remotes/origin/master` both at
  it. Read by the writing side with `git show -s` and `git for-each-ref` at the explicit hash on
  2026-08-22. **Nothing is unpushed.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 74, passing 73, failing 1,
  not_run 4, historical 16; the verdict set is 73 `PASS` and 1 `FAIL`; the one failing check is
  `tools/audit/gen_filing_convention_application.py --check` ([[OI-372]]'s tool). Read at the
  object by the writing side; **read it again at `git show 40fb613060:tools/audit/guard_state.json`
  yourself, not from here.**
- **FACT** — [[OI-179]] is in `gating_ids` on 2026-08-22; read at the staged artifact by the
  writing side (`tools/audit/nongating_apparatus_rows.json`, the `gating_ids` list opening at its
  line 186, the identity at line 244).
- **FACT** — the existing verdicts' digest is
  `32366793f298ff1c1d17422b61aed4660b0fdefe7aa201bc677ead0e0435320c`, published at
  `tools/audit/july_screen.json` → `★_the_widened_screen.the_existing_verdicts_digest` and in the
  report; identical at `64e7d2fd7f`, `beccfa14f5` and `40fb613060` (read by the writing side at all
  three objects). **It must be identical across every run of THIS batch; a different value is a
  STOP-and-report.**
- **FACT** — at `git show 40fb613060:tools/audit/july_screen_report.md`, the section *"What remains
  UNREAD, per document"* lists seven members, and the failure-signal section reads
  `INCONCLUSIVE-AT-THIS-COVERAGE`. **The names and the per-document counts are read at the
  artifact, never from here or from the previous report's §5** — the remainder is DERIVED fresh
  (Task 1 step 1).
- **FACT** — the widened authored block of `gen_july_screen.py` is where every new verdict goes
  (`W`), and the tool STOPs on a widened verdict naming a passage the new population does not
  carry; the previous batch's own hashes-from-abbreviations slip was caught by that STOP's shape
  and by its own re-read (its report §10, departure 2). **Resolve every commit hash at the object
  before writing it into a verdict; a verdict keyed on an abbreviation is the shape that STOP
  exists against.**
- **FACT, measured by the writing side on 2026-08-22:** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md`,
  `cc_report_step_zero_return_executed.md`, `cowork_away_returns.md` and
  `tools/audit/nongating_apparatus_rows.json` on disk hash to the tip's blobs (`git hash-object`
  on the user's machine against `git ls-tree 40fb613060`). `cowork_handoff.md` hashed to the tip's
  blob BEFORE the forty-second block was inserted; after it, the file carries ONE block above the
  committed content. **The previous batch established at the object level that `DECISIONS.md`
  reports no modification — the repository normalises line endings on the way in — and nothing
  about that path is predicted or expected to differ here.**
- **FACT** — `cowork_rulings_2026_08_22_boot_list_sitting.md` is on disk and ABSENT from the tip
  (it did not exist when `40fb613060` was made). **It does not carry the word `pinned` on any
  line** (Grep over it by the writing side; its sha256 at delivery is
  `af0f8a12f79433987b9b82674f17ce9152ac527bdad40706fc110c4e203c541d`, 7,965 bytes, zero carriage
  returns — `sha256sum`, `wc -c`, over the container copy and again over the re-staged disk copy).

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications: **(i)**
  `cowork_handoff.md` — ONE block (the forty-second) inserted above the committed content, the
  forty-first heading marked superseded-as-entry-point; **exactly one deletion-and-reinsertion of
  that heading line and otherwise additions only.** **A modification at any OTHER tracked path is a
  STOP-and-report.** Untracked and to be landed by Task 0, named one by one:
  `cowork_rulings_2026_08_22_boot_list_sitting.md` and this dispatch
  (`cc_instruction_pass_continuation_second.md`) — **two untracked paths; a named path the tree does
  not carry is a STOP-and-report.** The rest of the untracked population (older `cc_*` files, a
  scratch directory) is NOT landed and is outside A1's STOP. **Check ordered (Task 0, first act):**
  per named tracked path `git diff --stat 40fb613060 -- <path>; echo "exit:$?"` (the explicit-hash
  form the guard admits — the bare working-tree form is denied and is not to be attempted); per
  named untracked path `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  **Check ordered (each task's last step):** any other red is classed by its measured cause, and
  one outside this batch's own acts' subjects is a STOP-and-report. The reds this batch causes and
  must clear: **the membership check, cleared by Task 0 step 2**; **`gen_july_screen.py --check`,
  failing between the moment its authored block is edited and the moment its artifact is
  regenerated inside the SAME bounded run**, never left red across a commit; and
  **`gen_session_start_read_size.py --check` at the close**, because Task 2 moves `STATUS.md`
  entries — **F79's own instance, expected, cleared by regenerating the measurement and by nothing
  else.**

- **ASSUMPTION A3 — the prediction about Task 0's membership regeneration, taken from EVERY route
  the input reaches the artifact by (the standing clause).** **Route A** — the ruling-record count
  rises by exactly ONE from its committed value and ONE record name is added
  (`cowork_rulings_2026_08_22_boot_list_sitting.md`). **Route B** — the record carries the word
  `pinned` on no line, so route B is predicted to add NOTHING. **Both are predictions and not
  citations — check them at the artifact.** **No member, route, document, pin constant, state or
  count moves beyond that one addition; any such movement is a STOP-and-report.** An ADDED derived
  cross-reference whose cause is the landed record is not such a movement (the sharpened bar in
  `cowork_audit_protocol.md`), but it is reported with its cause established at the record's own
  text.

- **ASSUMPTION A4 — the guard registry.** No tool is added or removed by this batch, so the guard
  population is UNCHANGED; `epoch_write_path.json` and `recognizer_establishment_sort.json` are
  predicted UNMOVED (this batch adds no path constant to any tool — the previous batch's one
  departure had exactly that cause and cannot recur here). **If either goes red, establish the
  cause at the object before touching it (D-669) and report it; a cause that is not this batch's
  own edit is a STOP-and-report.**

## The declared start state — at the tree you will meet

**The committed guard registry records the previous batch's END STATE** — one failing check
([[OI-372]]'s tool), zero STOPs. **The tree you meet differs from that record by this dispatch's OWN
INPUTS, and the difference is declared (F67 applied):**

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing
   red. Its STOP text, as the thirty-ninth block records, is *"derived candidates with no authored
   verdict: BUILD_AND_TEST_ARCHIVE.md, OPEN_ITEMS_ARCHIVE.md, cc_report_preparation_fourteenth.md"*
   (read by the writing side at `guard_state.json` at the tip, the failing run's `stderr`); it is
   still [[OI-372]]'s red and not a second one.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — **caused
   by this dispatch's own input**: ONE untracked root-level `cowork_rulings_*.md` record now sits
   there, `cowork_rulings_2026_08_22_boot_list_sitting.md`.

**★ THE GUARD REGISTRY'S OWN ARTIFACT DOES NOT RE-DERIVE EITHER (F83 applied).** `gen_guard_state.py
--check` will open *"STALE vs the run"*, because the committed registry records one failing while
the tree has two, and the same input causes both. **It is not a third failing check.** The declared
start state is MATCHED when the verdicts match.

**A THIRD failing check among the verdicts, or a difference in the membership artifact reaching
any member, route, document, pin constant, state or count beyond the one addition, is a
STOP-and-report.**

## Findings in this batch

**No finding number is allocated; the series stands at F88 and stays there.** A finding you make is
sorted by D-438's test as the standing clause orders, with Ruling 9's routing (2026-08-21): **(a)** a
finding that bears on the analysis, its inputs or a measurement tool is SURFACED in the report under
the heading **"Quarantined audit questions"** — not rowed, not numbered, not acted on; the four the
previous batch surfaced at its report's §8 are NOT restated, re-argued or acted on — a new one is
added beside them only if it is new; **(b)** a finding about the apparatus gets its open-items row
and detail file in the commit that records it (rule (c)), the row stating in its own text that it
lapses under D-676; **(c)** a finding bearing on neither is recorded DISCARDED with finding, date
and reason (#10). **An establishment obligation is never discarded and always surfaced (#19).** The
two owed dispositions the plan's §2 names are NOT this batch's to row or act on.

## Task 0 — land the boot-list sitting record, clear the membership check in the same act, and PUSH

1. **A1's check first, entirely at content-addressed objects**, by the per-path commands A1 names.
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE its difference against the
   committed blob BEFORE accepting it**, against both halves of A3:
   `python tools/audit/gen_evidence_pin_membership.py; echo "exit:$?"` then
   `git diff --stat 40fb613060 -- tools/audit/evidence_pin_membership.json; echo "exit:$?"` and read
   the changed passages with `git diff 40fb613060 -- tools/audit/evidence_pin_membership.json >
   <absolute scratch path outside the repository>; echo "exit:$?"` and the file tools. **If any
   member, route, document, pin constant, state or count MOVES beyond the one predicted addition,
   that is a STOP-and-report.**
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_22_boot_list_sitting.md`, and
   `cc_instruction_pass_continuation_second.md` (new) — **four paths.** Subject, exactly:
   `record: the 2026-08-22 boot-list ruling for derivation sessions landed; evidence-pin membership regenerated`.
   Staged by explicit path, never by `git add -A` or `git add .`.
4. **PUSH** — `git push origin master; echo "exit:$?"`. **Verify at the object** that
   `origin/master` names your Task 0 commit.
5. **Re-run `gen_evidence_pin_membership.py --check` at the tree this commit carries: it must PASS.**

**Registered expectation E0:** exactly the four named paths; one content modification matching
A1(i) and one bounded regeneration; two additions; no staging override; the membership check
passing at the resulting tree, so the guard set stands at **one failing** ([[OI-372]]) from Task 0
onward; `origin/master` at the Task 0 commit.

**★ BEFORE TASK 1 OPENS, REPORT THE SIZE YOU ARE ABOUT TO READ**, as a direction with its artifact
named — the `NOT YET READ` total at `july_screen.json` → `★_the_widened_screen.counted.unread`,
never a number carried from any report. **The writing side's reading, stated as a reading: the
remainder is smaller than the previous batch read in one session, so the pass is EXPECTED to
complete whole; if it does not, that is not a STOP — Task 1 is a per-entry pass and stops at a
member boundary with its stop recorded.** Say which it is before reading the first passage.

## Task 1 — Ruling 1 (dispatch order): the per-entry pass CONTINUED, a second time, from the artifact's own `NOT YET READ` set

**A per-entry pass (the standing clause): it may be stopped at any member boundary, and the stop is
RECORDED — which members were completed, that the remainder is UNTOUCHED rather than partly worked,
and that nothing is left half-edited. The order is the artifact's own (by document, then by commit,
then by changed passage). The remainder is DERIVED fresh from `july_screen.json` →
`★_the_widened_screen.the_hunks` where `verdict == "NOT YET READ"` — never from
`cc_report_step_zero_return_executed.md` §5 or from the report's unread list.**

1. **Author one verdict per unread passage, by the inherited method and no other**: retrieve the
   passage from the git object by explicit hash (the tool already does this); read it at its own
   removed and added text together with its commit's own account; apply the four classes in their
   declared order (POSITIVELY CODE-INFLUENCED first); name the shape; write the ground citing what
   it was made from; for a RATIFIED-ACT EDIT name the act AND where its ratification is recorded,
   or do not admit the class; **apply the calibration the previous batch established (its report
   §6): a homing act performed under a named user ruling is a ratified-act edit, and the not-cleared
   class is reached only where the first three all fail — state, for every not-cleared verdict, which
   of the three was tested and why each failed.** A section-restricted member's passages outside the
   named sections are recorded `OUTSIDE NAMED SECTIONS` and not graded. The verdicts go into the
   widened authored block (`W`), never into the existing block (`V`), and **no verdict already in
   `W` is re-read or re-graded.**
2. **Regenerate after each bounded run** so the tree is never left with the artifact behind the
   tool; `--check` passes at every commit; **the existing digest identical at every commit**. Commit
   in bounded runs (`the widened screen: verdicts authored, members <first>–<last> of <document>`),
   each pushed and verified.
3. **The ruled failure signal, checked at the artifact and not at an impression.** If the pass
   completes whole, the signal is evaluated over the WHOLE widened population for the first time and
   the report states it either way as the batch's headline, with Ruling 7 quoted: **a majority of
   `UNDETERMINED` over the read members is a STOP to the user — not a licence to proceed and not a
   licence to argue the class down.** If the pass stops at a boundary, a majority reached on a
   part-read population is reported as `INCONCLUSIVE-AT-THIS-COVERAGE` with the read share named.
4. **Stop at a member boundary** when capacity ends or when Task 2 binds harder; record the stop in
   the standing form (`cc_report_step_zero_return_executed.md` §5 is the template — import its
   shape).

**Registered expectation E1:** every read passage carries exactly one verdict in the inherited
vocabulary; the unread remainder, if any, carries `NOT YET READ` by the tool's default and no hand;
the existing sixty-eight byte-identical by published digest at every commit; every verdict the two
previous batches authored byte-identical; the per-document distribution published by the generator;
the failure signal evaluated at the artifact and stated either way; if the pass completed, the
report says so in the same standing form (every member named, the unread set EMPTY at the
artifact); if not, the stop recorded. **`ARCHITECTURE.md`, `docs/scoring_model.md` and every member
byte-unchanged.**

## Task 2 — the close

1. One `STATUS.md` pointer entry per task that did work, and **in the same act the previous batch's
   entries move verbatim to `STATUS_ARCHIVE.md`** (`gen_status_batch_bound.py --apply`), both
   directions proven. **The declared `Last updated:` prefix adjustment is imported, not re-decided
   (#6).** `gen_session_start_read_size.py` regenerated (A2).
2. The FULL close is appended to `cowork_away_returns.md` as a **THE PER-ENTRY PASS CONTINUED, THE
   SECOND TIME** section: both guard states, every SHA, every registered expectation graded MET or
   NOT MET with its reason, every declared departure, A3 and A4 graded at the artifact, the
   independence of every new verdict from any verdict the record already carries on the same
   passage (none by construction — say so), the quarantined audit questions (or that there are
   none new), **the plan lines restated as plan lines and not tasks** — the owed whole read of
   `STATUS.md` + `STATUS_ARCHIVE.md` (Ruling 1(d) of the step-zero return sitting), of the five
   glob-only documents (Ruling 3(b)), and **the pilot's opening, now with its boot-list
   prerequisite RULED and landed, and with the (a1) generator owed at that opening** — and **the
   plan's tell in one sentence: did this batch produce anything other than the authored verdicts,
   the regenerated screen artifacts and the report? If yes, name it.** The report is
   `cc_report_pass_continuation_second.md`.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE.** State at its head that the end-state run is taken
   after the commit carrying it exists and that its values land in ONE FURTHER commit.
4. **★ NAME THE CHAIN AS FAR AS A SENTENCE CAN NAME IT, DECLARING THE ONE COMMIT IT CANNOT.**
5. Commit, push, verify at the object; then the one further commit carrying the end-state run.

**Registered expectation E2:** at the tree carrying the close, a fresh full guard run re-derives —
**every run check passing except [[OI-372]]'s tool, zero STOPs, the guard population unchanged** —
run and read, never inferred, its values committed only after the run that produced them; the tell
stated in one sentence.

## What this batch does NOT do

- **No pilot act, no derivation of any specification statement, no frame, no placement test, no
  format test, no held-out test, no session booted from the curated boot list, no generator for the
  per-session withheld list** — the pilot's opening is the next decision surface; the boot-list
  ruling is landed here and exercised nowhere.
- **No mining read of `STATUS.md`, `STATUS_ARCHIVE.md` or the five glob-only documents** — plan
  lines, after the pilot establishes the method.
- **No edit to `ARCHITECTURE.md`, to `docs/scoring_model.md` or to any document-set member.**
- **No edit to `gen_specification_document_set.py` or `gen_period_stratum_split.py`; no
  regeneration of their artifacts; no regeneration of `doc_change_candidates.json` or its
  changed-passage file.**
- **No change to the July screen's classes, order, shapes or STOPs; no re-reading or re-grading of
  any verdict already authored, whether existing or widened.**
- **No finding number allocated; no findings series opened.** [[OI-372]] and [[OI-374]] stay as
  found; **[[OI-179]] stays OPEN and GATES**; F3 stays surfaced, unfixed and unrowed; the four
  quarantined questions of the previous report's §8 stay surfaced and unacted on.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule (c)
  as *Findings in this batch* states.**
- **No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
  `tools/robust_stop/`, no measurement of the analysis.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #12 — nothing deleted: every existing and widened verdict stays, the
   previous batch's `STATUS.md` entries move whole to the archive and are proven both ways; #6 —
   one authority quoted per act, the calibration pointed at the report that established it rather
   than restated; #17(f)/D-431 — no count transcribed: the remainder is named to the artifact and
   ordered re-derived, and the one reading about its size is stated as a reading; #19 — the
   candidate generator's unestablished status is carried; the failure signal is evaluated at the
   artifact; #24 — no comparison between measured quantities asserted. Conforms.
2. *Conventions:* American English; no self-invented labels — the file names follow the
   `cc_instruction_*` / `cc_report_*` / `cowork_rulings_<date>_<sitting>` shapes the record uses,
   and every term of the tools' is explained at the head; "hunk" used only where it names a field or
   quotes a tool; music-theory words in their musical sense; the 2026-08-17 vocabulary ruling
   observed (towards, never against).
3. *Figures and premises:* every quantity named to its artifact or stated as a prediction; the
   digest and the guard summary quoted from the objects at `40fb613060`; the rulings quoted at
   their records; the record's hash and size named to the tools that produced them (F66).
4. *File-tools rule:* the writing side's own departures are declared in the forty-second handover
   block, not here; the executing side is bound as `CLAUDE.md` Conventions state, with the
   explicit-hash diff forms named because the bare forms are denied.
5. *Uncertainty:* no comparison between measured quantities is asserted in this dispatch.
6. *Re-read from disk before release:* performed by the writing side over the delivered file,
   hash-verified against the intended content after re-staging (F74), and recorded in the
   forty-second handover block.

---

*Provenance: Cowork, 2026-08-22, the forty-second session, written at branch tip `40fb613060` under
Ruling 1 of `cowork_rulings_2026_08_22_dispatch_order_sitting.md`, landing Ruling 1 of
`cowork_rulings_2026_08_22_boot_list_sitting.md`. The reserved-word conventions bind this dispatch,
and the vocabulary rules bind every line of the report it orders — TOWARDS the ultimate objective
and TOWARDS the guiding principles.*
