## COWORK SESSION CLOSE (HUNDRED AND TWENTY-FIFTH ENTRY, 2026-09-06 — THE CONTINUING LINE) — ★★ **THE SESSION THAT BOOTED ON THE HUNDRED-AND-TWENTY-FOURTH ENTRY READ ROW 20 WHOLE AT THE OBJECT. THIRTEEN OF 36 ARE NOW READ — rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19 and 20 — GROUP 1 IS CLOSED AND GROUP 2 IS OPEN WITH FOUR READ. The extract `reading_pass/extracts/korzeniowski-widmer-2018-improved-chord-recognition-by-combining-duration-and-harmonic-language-models.md` (46,899 bytes) and the rewritten progress record `reading_pass/l2_slice_reading_progress.md` (58,000 bytes) are on disk, both proved at a listing against a `stat` taken immediately before the commit, BOTH ON THE FIRST COMMIT — the progress record on a STAGED path, guarded by the staged copy's `expectedMtimeMs`.** ★ **ROW 20 IS CENTRAL ON A NARROW GROUND AND OWES A SECOND EXTRACTION. NO identity finding — title, authors, venue, year and CC BY 4.0 licence match the bibliography's row 34 at page 1. THE INPUT IS AUDIO (row 19's carried question (b), answered at page 1). NO [FACT] rests on the paper — confirmed by `Grep` of the staged tree, not inherited. Row 11's carried question (a) is ANSWERED: the paper's duration model classes OUTSIDE both families the §2.3 test distinguishes, and its two baselines on the no-segmental-decode side. No falsifier; no STOP.** ★ **THE NEXT ACT IS THE NEXT BOUNDED READING SESSION — row 18 next (Sheh & Ellis), then row 47, which closes group 2.** ★ **NOTHING RENDERS: Ruling 1's gate stands — 23 of 36 not opened.** ★ **THE HUNDRED-AND-TWENTY-FOURTH ENTRY'S FOURTH LANDING — the one it made from a FRESH container path and left to this session to prove — LANDED RIGHT: 35,427 at the root listing, the size the user gave, a whole read. Both of this session's reading files also went from a fresh path and landed right first time. That is the first evidence for the cheap hypothesis that entry states; it is not a cause, and the operating rule is unchanged.** ★ **NO BATCH IS OUT. THE TIP IS UNMOVED AT `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, read at both ref files.** ★ **THE GUARD SET STANDS AT TWELVE FAILING (relayed) AND `STATUS.md`'s TOPMOST ENTRY SAYS ELEVEN (read at the object this session) — both known, both ruled (Rulings 8 and 9, relayed as the hundred-and-twenty-fourth entry relays them), untouched; nothing ran.** THE CURRENT ENTRY POINT; the hundred-and-twenty-fourth entry is superseded as entry point and stands otherwise — including its bridge-fault section, its running total and its declared departures, none of which is restated here except where this session adds to it.

You start clueless. Read this block first, then perform the ordinary session-start read: `CLAUDE.md`
whole, `DECISIONS.md` whole, `STATUS.md`, and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`). **A
single-file opening instruction is not an exemption from that read** (P-1). **The eighty-second
through hundred-and-twenty-fourth entries remain binding except where this entry names a change**, and
every standing bar of the earlier blocks remains binding.

**★ THE PRACTICAL FACTS of the hundred-and-sixteenth, hundred-and-eighteenth, hundred-and-twentieth,
hundred-and-twenty-second, hundred-and-twenty-third and hundred-and-twenty-fourth entries are unchanged
and are not restated here (#6); read them there.** This session confirms them by doing them: the file
tools do not read a device path directly (the first `Read` of the entry failed on both the mount path
and the device path), so every repository file was staged through the bridge and read at its staged
copy; the root listing exceeded the tool-result cap and was saved by the harness, and **this session
consulted the saved listings with `Grep` only** (three root listings, each consulted once: to prove
the previous entry's landing at 35,427 bytes, and to prove this entry's first and second landings);
`reading_pass/`, `reading_pass/extracts/` and `docs/research_papers/` were listed directly as
subdirectories. **`candidacy_upgrades.md` lives in `reading_pass/`, not at the root.**

**On form.** Plain ordinary English with the user. **NEVER use the multiple-choice widget with him.**
A decision surface is fact-based and checked at the objects, every position weighed towards the
ultimate objective and towards the guiding principles, the recommendation with the reason and the
strongest argument against it; when the facts collapse the alternatives to one, say so; before putting
a question, check whether the record already answers it. **He asks that a dispatch be sourced and
fact-checked at the objects before he runs it, and that a deliverable he is about to rule on be
fact-checked as a turn of its own before any question** — do both unasked. **A read put to him by name
is shown, not ratified; if he says nothing it stands and the next act proceeds.** Every self-invented
word is explained in plain words the first time or not used.

### ★★ THE BRIDGE FAULT — READ THE HUNDRED-AND-EIGHTEENTH ENTRY'S SECTION BEFORE YOU WRITE ANYTHING

It is not restated here (#6). **This session saw NEITHER shape.** Four landings were made or proved
this session and all four carried the right bytes (the fourth, this entry's own first landing, is
recorded in the paragraph after the next): (1) the hundred-and-twenty-fourth entry's fourth
landing, made by its own session from a FRESH container path (`outputs/relanding/…`) on the by-then
STAGED root path, guarded by `expectedMtimeMs` 1788680498505, and left to this session to prove —
proved at 35,427 bytes at the root listing (read by `Grep` on the saved listing), the size the user gave
as its proof, and by a whole read; (2) the row 20 extract, a never-staged path, 46,899 at the listing;
(3) the progress record, a staged path guarded by the staged copy's `expectedMtimeMs` 1788680136221,
58,000 at the listing — (2) and (3) in ONE `device_commit_files` call, both from a FRESH container path
(`outputs/rp125/…`), both equal to a `stat` taken in the same shell call that copied them, immediately
before the commit. **The observation the hundred-and-twenty-fourth entry recorded — that across three
commits from ONE unchanged container path each landing carried the bytes that path held at the commit
before it — now has four data points on the other side: every landing KNOWN TO THIS SESSION to have
been made from a fresh container path has landed the current bytes** (that entry's fourth; this
session's two reading files; and this entry's own first landing, 31,825 at the root listing against a
`stat` of 31,825 — whether any earlier session's right landings were also from fresh paths was not
checked, those entries not having been read). **Still no cause is asserted (D-641, D-658).** What it
changes in practice, and only this: **this session used a fresh outputs path for every commit, and the
cadence below says to do the same** — a rule that costs one `mkdir` and removes the condition under
which the hundred-and-twenty-fourth entry's two proven stale landings occurred (whether the earlier
three stale landings shared that condition was not checked by this session). Whatever the
path, the operating rule is unchanged: prove every write at the listing against the immediately
preceding `stat`, and read the landed copy back when the sizes differ.

**The running total, in the hundred-and-twenty-fourth entry's two-reading form:** that entry's totals
were *forty-four across eleven sessions, thirty-three never-staged and eleven staged*, or *forty-three,
thirty-two and eleven*. This session adds two proven first-commit landings (the extract, never-staged;
the progress record, staged), and proves the hundred-and-twenty-fourth entry's fourth landing (staged,
guarded, from a fresh path — that entry's own first three landings are already counted there as one
right and two stale), and proves its own FIRST landing (never-staged, 31,825 at the root listing
against a `stat` of 31,825): **forty-eight across twelve sessions, thirty-five never-staged and
thirteen staged; or forty-seven, thirty-four and thirteen.** **Beside either total stand FIVE proven
STALE landings — four on staged paths and one on a never-staged path** — unchanged by this session.
This entry's SECOND landing, corrected and guarded, is proved by the next session by a whole read and
by its size at the root listing.

### What this session was

The session that booted on `cowork_handoff_entry_one_hundred_and_twenty_four.md`, on the user's
opening instruction to read that entry with the file tools, take its proof of landing as 35,427 bytes
at the root listing, and read what row 20 IS at `candidacy_upgrades.md`'s row 20 before staging
anything. It did both first and reported them to the user in plain text, then on his "go ahead" the
full session-start read (the tip at both ref files; `CLAUDE.md` whole, loaded by the harness;
`DECISIONS.md` whole, 862 lines; `STATUS.md` whole; the gating answer — 244 open, 219 gating, 25
non-gating, and the `gating_ids` list whole; the hundred-and-eighteenth entry's bridge-fault section),
then the reading set the cadence names (the progress record whole; `candidacy_upgrades.md` whole; both
commissions whole; the row 19 extract whole, for the form and for the two carried items; the slice
derivation's row 20 line; the bibliography's row 20 line; `FRAMEWORK.md` at the L2 and L3 charters and
DP-B to DP-H; the findings surface's DP-C block; `population.md` §3's verification table; and a `Grep`
of the staged tree for "Korzeniowski" and "Widmer"), then one thing:

**Read row 20 whole at the object and extracted it.** Korzeniowski & Widmer, "Improved Chord
Recognition by Combining Duration and Harmonic Language Models", the held file
`docs/research_papers/korzeniowski_widmer_2018_ismir_duration_harmonic_language_models.pdf` (384,606
bytes), eight printed pages (10–17), staged through the bridge by the one matching file name in the
`docs/research_papers/` listing and read as page images with the file tools; the printed title, authors,
venue, year and licence were checked against the bibliography's row (line 34) on page 1 before anything
was extracted — **all match; NO identity finding; the input is AUDIO.** The record's own statements about
the paper were read at their lines first: `candidacy_upgrades.md` row 20 (line 82); the slice
derivation's row 20 (line 67); the bibliography's row. **The `Grep` found the paper cited NOWHERE in
`FRAMEWORK.md`, the findings surface or `population.md`** (that file's two "Widmer" matches at lines 107
and 109 are Karystinaios & Widmer, V6 and V8) — so no [FACT] rests on it, the hundred-and-twenty-fourth
entry's report that `population.md` §3 names no Korzeniowski primary is CONFIRMED and widened to the two
surfaces that entry had not searched, and the read re-verified nothing. **The extract is the record; what
it produced, in one line each, is in the progress record and is not restated here (#6). No falsifier; no
STOP.** The paper is **CENTRAL on a narrow ground** by the extract's own reasoning — the only held
primary in the slice whose SUBJECT is the segment-length term, with a measured comparison of three
shapes for it — and **a second independent extraction is OWED**; the extract states the ground on which
NOT CENTRAL could be argued, and the verdict is challengeable at the progress record.

**Two things stated here because the next reader meets row 18 through them.** (a) The slice now holds
THREE worked shapes for a segment-length term under row 11's §2.3 test — linear (row 19's
self-transition term and row 20's exponential baseline: no segmental decode), Gaussian (row 11's own
example: segmental decode) and unbounded-history (row 20's chosen recurrent model: neither, an
approximate decode) — plus the negative binomial as a bounded-state frame-level shape; by row 20's own
statement an HMM implies the exponential shape (one state per chord) or the negative-binomial shape
(several states per chord), so row 18's HMM has one of THOSE two by construction, and the reader
records which. *(Corrected at the user's fact-check instruction: the first landed copy said "one of the
first two", which named the linear and Gaussian shapes — wrong; the former wording is preserved here.)* (b) Row 18 is V10's
68.8-against-23.3 primary, already VERIFIED by the pass; the reader re-verifies it at the object as rows
10, 11 and 19 did for their V10 figures.

**Why the session stopped after one paper.** The full boot read plus one eight-page paper with a
forty-seven-kilobyte extract, plus the two opening reads and their report, is at the bound the previous
sessions found; the stop is at a member boundary, by choice, under the commissions' cadence rule, with
everything landed and proved.

### ★ THE STATE YOU INHERIT

**What in this section was read at an object this session and what was relayed, stated once:** the
tip (both ref files), `CLAUDE.md`, `DECISIONS.md`, `STATUS.md`, the gating answer, the progress record,
`candidacy_upgrades.md`, both commissions, the row 19 extract whole, the slice derivation's and the
bibliography's row 20 lines, `FRAMEWORK.md` at the sections named above, the findings surface's DP-C
block, and `population.md` §3 were READ. The verdict counts, the guard-set figure, the untracked-file
list, the continuous-pruning remark and the "waiting on him" list are RELAYED from the
hundred-and-twenty-fourth entry, which relays them from earlier entries; this session did not open
those earlier entries beyond the hundred-and-eighteenth's opening and bridge-fault section.

- **The tip** is `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, `origin/master` equal, read at both ref
  files with the file tools (`.git/HEAD` → `refs/heads/master`); nothing ran since. **No batch is out.**
- **`VERDICTS["l2"]`** 111 IN / 133 OUT / 0 UNPLACED (relayed; not re-verified). **`WITHHELD`, `EXTRAS`,
  `FROZEN`** carry no `l2` key (relayed; `STATUS.md`'s topmost entry, read this session, says the same
  of the writeback batch).
- **The reading file** `ratification_surfaces/cowork_withheld_family_l2_reading.md` stands RULED
  (`STATUS.md`'s topmost entry, read this session).
- **Task B's L2 slice: 13 of 36 read** (rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19 and 20);
  **group 1 closed, group 2 open with rows 10, 11, 19 and 20 read; rows 18 and 47 close it.** The
  progress record is `reading_pass/l2_slice_reading_progress.md` (58,000 bytes) — read it whole before
  opening any paper; it names the next member, carries THREE things to row 18, and states that its row
  numbers are `candidacy_upgrades.md`'s and that `object_reads/` uses `population.md`'s numbering.
- **Untracked files of this line at writing, none landed by this side:** this entry; the
  hundred-and-twenty-fourth through hundred-and-ninth entries; the surface
  `cowork_l2_boot_list_surface_2026_09_05.md`; the sitting record
  `cowork_rulings_2026_09_05_l2_boot_list_sitting.md`; the two derivations of the hundred-and-eleventh
  entry; the read file `cowork_l2_score_set_read_2026_09_05.md`; the thirteen extracts of rows 27, 28, 1,
  8, 3, 26, 29, 30, 5, 10, 11, 19 and 20 and the progress record. The standing untracked historical
  `cc_*` root population is pre-existing. This side ran no enumeration.
- **The gating answer:** 244 open, 219 gating, 25 non-gating at the session-start read; nothing ran.
- **The guard set:** 77 run, 12 failing (relayed; not re-run). **`STATUS.md`'s topmost entry says
  "ELEVEN FAILING"** (read at the object this session; Ruling 9: corrected in place by the next dispatch
  that touches `STATUS.md`, former wording preserved). **The continuous-pruning bound is unmaintained** —
  standing since the hundred-and-sixth entry (relayed).
- **The plan stands at:** … → L2's boot list RULED (nine rulings) → reads 1, 2 and 3 of §11 DONE →
  **Task B's L2 slice (36 papers) read whole at the object — 13 DONE, 23 TO GO, group 1 closed, group 2
  open with two to go** — in bounded sessions closing at member boundaries, under the remedial
  commission §3 and the original commission's §4 form, central papers owing a second extraction, in the
  verdict table's proposed order ("stated, not ruled") → **the pack-build dispatch** when next (D-250) →
  the brief → the blind deriving session → L2's comparison reading. **Beside it, the L0/L1 comparison
  reading stands at 7 of 29 documents tabulated** (relayed).

### ★★ THE GATE, RESTATED

No derivation may begin before L2's slice of Task B is in (eighty-seventh entry; Ruling 10 of the
2026-08-31 sitting; Ruling 1 of the 2026-09-05 sitting). **The slice is 36 papers, all held. Thirteen
are read.** The gate is not lifted and cannot be until the other 23 are read, flagged unreadable, or
stop-reported.

### ★ WHAT COMES NEXT

**The next act is the next bounded reading session** — open `reading_pass/l2_slice_reading_progress.md`,
the remedial commission's §3 and the original commission's §4 form before reading anything; take the
next row's paper from `candidacy_upgrades.md`'s own row (never from a file name elsewhere), find its
held file in the `docs/research_papers/` listing, stage it by name, check its printed title, authors and
version against the bibliography's row on page 1, read it as page images, extract in the §4 form,
rewrite the progress record whole (a file this line wrote, 58,000 bytes — read it whole first), close at
a member boundary. **Row 18 is next** — this session read its table line in `candidacy_upgrades.md`
(line 80: Sheh & Ellis, ISMIR 2003, chord segmentation and recognition using EM-trained HMMs, held,
"DP-C's 68.8-against-23.3 primary, and its method is the boundaries-given-versus-found design that
measurement is about") and its bibliography row (line 32: "Chord Segmentation and Recognition using
EM-Trained HMMs," ISMIR 2003, tier "LINK (author copy)" — so the identity check must establish which
version the held file is); the next session reads both rows again and the slice derivation's row 18
line, and finds the held file in the listing (the one whose name begins `sheh_ellis_2003`, 109,996
bytes at this session's listing). **`population.md` §3's V10 row (line 111) cites the paper's Table 3
for the 68.8/23.3 figure; `FRAMEWORK.md` DP-C (lines 693–694) carries the figure without the name; the
findings surface was NOT searched for "Sheh" this session** — the reader confirms the record's citations
with a `Grep` of the staged tree for the authors' names before extracting, as this session did for row
20. **The three things carried to row 18 are in the progress record's "Next" section.** The pack-build
dispatch is NOT next (D-250).

**Routed in the row 20 extract and the USER's to act on or to leave:** the audio-domain caveat and the
CC tier confirmation, for the bibliography reconciliation (nothing owed); the factorisation of the
temporal score into a chord-level transition term and a frame-level length term, beside D-532 and
D-533; the three-shape classification of the length term under row 11's test, beside row 11's finding
(4), row 19's finding (5) and D-004; the label-independent length term as an open question; the
harmonic-rhythm term inside the decode as a rival placement for a fact the framework homes at L3, for
the L2 and L3 detail specifications and the findings surface's DP-C block; the approximate decode as an
instance of what the charter forbids, beside D-098 and D-004; the modest, significant, untabulated
gain, for the DP-C block; the frame-lattice "with" as a precision to DP-C's candidate-list parenthesis;
key-shift augmentation beside D-526; the Tonnetz-shaped embedding for the style system; the fit
findings and the duration-weighted metric for measurement design; the absent posterior beside D-006,
D-008 and DP-K. Together with the earlier extracts' routed findings, none is a STOP and none needs a
ruling before row 18 is read. **Shown, not ratified.**

### The user-owed list

**Closed this session by this side, and not owed to him:** the thirteenth reading session (row 20).

**Owed to him by this side:** nothing. Every finding of the extract is ROUTED in the extract itself and
applied nowhere.

**Waiting on him, CARRIED from the hundred-and-tenth entry as later entries relay it (this session read
only the hundred-and-twenty-fourth and the hundred-and-eighteenth's opening and bridge-fault section),
still open, unverified this session:** **(a)** whether the pilot's derived specification still carries
its independence claim after Ruling 85; **(b)** which branch of the filing convention (D-674) governs a
ruling record; **(c)** the close test's wording (close section and end-state artifact in one commit
versus one apart); **(d)** re-aiming the `expected_line` values in `gen_phase3_gate_partition.py`;
**(e)** whether the report template should carry the close-tip marker by design; **(f)** the fifth,
sixth and seventh guard observations not yet in OI-378's detail file.

**Shown to him and standing unless challenged:** the score-set read of §11 (hundred-and-twelfth
entry); the row 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19 and 20 reads. His opening instruction this
session named row 20 as the next act and his "go ahead" released the session-start read and the paper,
which is the next act proceeding, not a ratification of any read.

**Standing, unchanged and still the user's:** the list the hundred-and-ninth entry carries under this
heading, as later entries relay it — neither the hundred-and-ninth nor the hundred-and-tenth entry was
read this session. **The eleven quarantined audit questions stay reserved to the AUDIT.** **Not acted
on:** the guard-runner parallelisation suggestion; the rendering tool's "1 entries" pluralisation
defect. The hundred-and-twenty-second entry's observation about `FRAMEWORK.md`'s sealed first-stage
text at lines 1685–1686 (the former V4 wording) stands as that entry states it; this session did not
read that passage.

### Errors

**Counted on this side this session, of the family the degradation rule names: ONE, in this entry's
FIRST landed copy and not in the extract or the progress record, found at the user's instruction to
fact-check the handover and corrected here before the re-landing.** It was a wrong derivation asserted
as fact: the paragraph carrying row 18's two items said row 18's HMM has "one of the first two" of the
listed length-term shapes, which named the linear and Gaussian shapes, where row 20's own statement
names the exponential and negative-binomial ones; the former wording is preserved at the correction.
The same fact-check also narrowed two over-wide claims in the bridge-fault section (a statement about
"every landing this line has made from a fresh path" and about "every proven stale landing", each
asserting something about sessions this one had not read) — the shape the rule names as a hedged or
unhedged assertion about unexamined things, corrected in place. One counted error is below the rule's
two; this close IS the handover, at a member boundary with nothing half-done, and the error is in the
entry's account of itself, not in the reading files — checked by re-reading both at their landed copies'
sizes against the pre-commit `stat` and by the extract's own two fact-check passes. **Two
near-misses, recorded because the rule counts the tell and not the landing:** (i) the progress record's
first draft of the "Next" section named row 18's paper by a title this reader had expanded from the
candidacy row's abbreviation ("… Hidden Markov Models") and asserted that the bibliography's row "names
it audio by its title" — two claims about an unexamined thing, of exactly the family the rule names;
caught by a `Grep` of the bibliography before landing, the title replaced by the row's own words ("using
EM-Trained HMMs") with its tier, and the audio claim replaced by the statement that the reader
establishes it at page 1; (ii) the extract's first draft presented the three-way classification of the
length term under row 11's test without saying whose classification it was — the paper states the HMM
form of each baseline and the all-predecessors dependence of its chosen model, and the classification is
this reader's application of the test; caught at the first fact-check pass and labelled. Both were
caught at the object before landing, so neither is counted; both are the shape the previous two entries'
near-misses had (an assertion ahead of its derivation), which is why they are recorded. The extract's
fact check was run twice against the page images before landing — every cell of Tables 1 to 3 compared
with the extract's transcription, every quotation of §1–§5 compared at the page on the re-read (pages
10–15), the references cited by number compared at pages 16–17 on the first read, the section, figure,
table, equation and footnote counts confirmed at the pages, and the corrected text searched for on the
second pass. One Figure 6 axis tick that could not be read with certainty against Table 2 was FLAGGED in
the extract rather than transcribed.

**One departure from the user's opening instruction, reported to him in the first turn:** the
instruction said to read row 20 "before staging anything", and the file tools cannot read a device
path, so the entry and `candidacy_upgrades.md` were STAGED in order to be read at all. The
session-start set was staged only after both reads had been reported and he had said "go ahead"; the
paper was not staged until after the reading set was read.

**Errors of previous sessions found this session:** none. The hundred-and-twenty-fourth entry's report
of the row 20 held file's size (384,606 bytes) and name prefix is CONFIRMED at this session's listing;
its statement that `candidacy_upgrades.md` row 20 is at line 82 and the bibliography's at line 34 is
CONFIRMED at the files; its statement that `population.md` §3 names no Korzeniowski primary is
CONFIRMED and widened (no citation in `FRAMEWORK.md` or the findings surface either); its running total
is carried forward as it states it.

**Declared departures of this side, this session.** (i) **A shell `cp` of the progress record's STAGED
copy into the work directory before its rewrite** — the hundred-and-twentieth through
hundred-and-twenty-fourth entries' declared departure, repeated here for the same reason (no
work-directory copy existed and re-typing 50,725 bytes from tool output is the transcription risk the
record forbids); the copy was byte-exact (50,725 at `stat`), then edited with the `Edit` tool in eight
places (the session note; the table row and counts; the "what row 20 produced" paragraph and the "Next"
section; two corrections to the "Next" section's row 18 clause; the not-done paragraph; the provenance's
session count; the provenance's ninth-session sentence). (ii) **Four `stat` commands (one on the
progress record's copy in the work directory; one on both reading files in the outputs directory
immediately before their commit; one on this entry immediately before its first commit; one on the
corrected entry immediately before its second), three `mkdir` (all fresh outputs paths), and four `cp`
of this side's own output files into the outputs directory (the two reading files, this entry, and this
entry corrected)** — all in the container on this side's OWN output files, declared because they are
shell commands in the sandbox; none touched repository content beyond the staged copy named in (i).
(iii) **Three `device_commit_files` calls:** the first carrying two files — the extract (never-staged)
and the progress record (staged, guarded) — both landed right, both proved at listings; the second
carrying this entry as first written on a never-staged path from a fresh container path, which landed
RIGHT (31,825 at the root listing, equal to the `stat` taken immediately before it); the third carrying
this entry corrected, RE-LANDED on the by-then-staged path from a FRESH container path, guarded by the
landed copy's `expectedMtimeMs` 1788681692036, proved by this session at the root listing against the
`stat` taken immediately before it and by the next session by a whole read. (iiia) **This entry was
re-staged and read back WHOLE after its first landing, fact-checked against the sources in this
session's context on the user's instruction, amended in eight places, and re-landed.** The amendments:
the corrected row 18 clause; the two narrowed bridge-fault claims; the bridge-fault section's opening
count and its running total, each gaining this entry's proven first landing; the practical-facts
sentence on the saved listings; this Errors count (none → one); and this departures list ((ii), (iii),
(iiia) and (xi)). (iv) **`Grep` through the file tools** on: the saved root
listing (once, as stated in the practical facts above); the gating answer for its keys; the
hundred-and-eighteenth entry for its heading list; the staged tree for "Korzeniowski" and "Widmer";
`FRAMEWORK.md` for "segment length", "harmonic rhythm", "duration model" and "language model"; the
findings surface for "duration" and "language model"; the bibliography for "Sheh" and "Chen & Su"; and
the extract and the rewritten progress record for stale or corrected phrases before landing. (v) No
subagent was used. (vi) One paper was opened, whole, eight pages; pages 2–6 and page 1 were re-read once
each for the fact check. (vii) The handoff entries before the hundred-and-twenty-fourth were NOT read
except the hundred-and-eighteenth entry's opening and bridge-fault section; the sitting record, the
hundred-and-eleventh entry's derivations, the extracts of rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10 and 11,
the V4 surface, and `FRAMEWORK.md` outside the sections named above were NOT read. (viii)
`reading_pass/continuation.md` was staged in the session-start batch on a guess that it was the slice
derivation, found not to be, and NOT read; the slice derivation was then staged by its own name from the
progress record's banner. (ix) The user connected no new folder. (x) `SendUserMessage` was used once to
put the row 20 read to the user in plain text; the two opening reads were reported as the first turn's
final response; no multiple-choice widget was used. (xi) Seven directory listings were requested: the
repository root at the start (exceeded the cap, saved, consulted by `Grep` once); `reading_pass/` and
`docs/research_papers/` before staging; `reading_pass/` and `reading_pass/extracts/` after the reading
files' commit for their proof; the root after this entry's first landing (saved, `Grep` once — 31,825);
and the root after its second landing (saved, `Grep` once), the seventh. (xii) The task-list widget was used to track the session's six steps; it asked
the user nothing.

**What went right and is worth copying:** the two opening reads were done and reported before the boot
so the user saw them at once, and the departure from "before staging anything" was stated in the same
turn; the `Grep` for the authors' names was run BEFORE extracting and found the paper uncited, so the
extract opens with that fact rather than inheriting the previous entry's narrower statement; the
paper's page 1 was checked against the bibliography's row before extracting and settled the carried
domain question at once; the row 19 extract was read WHOLE for the form, so both carried items were
met at their source; the fact check caught an unlabelled reader's inference and two assertions ahead of
their derivation before landing; every commit went from a FRESH container path and every landing was
proved at a listing against a `stat` taken in the same shell call as the copy; and the handover was
written at a member boundary with nothing half-done.

### The cadence for your session

0. **Boot as above, in full.** This entry file is UNTRACKED and you never land it yourself; a later
   dispatch's Task 0 does. **If your first file read fails, stage the file through the bridge and read
   the staged copy; only if staging fails, ask the user to add the folder.** **List `docs/research_papers/`
   and `reading_pass/` as subdirectories; if you must consult the saved root listing, `Grep` it.
   `candidacy_upgrades.md` is in `reading_pass/`; the slice derivation is
   `cowork_l2_task_b_slice_derivation_2026_09_05.md` at the root.**
1. **Re-read the tip at the ref files with the file tools.** It should be `911f5f7cda…` at both. A
   moved tip means something ran that this entry does not know of — read `cowork_away_returns.md`'s
   last section before anything else.
2. **BEFORE YOU WRITE ANYTHING, READ THE HUNDRED-AND-EIGHTEENTH ENTRY'S "THE BRIDGE FAULT" SECTION.**
   Prove this entry landed: a successful whole read of it at the object is the proof; record its size
   from the root listing in your own entry.
3. **Read `reading_pass/l2_slice_reading_progress.md` whole**, then the remedial commission's §3 and
   the original commission's §4 form, then one of the thirteen extracts for the form (the row 10 extract
   is the largest; the row 20 extract is the most recent). **The next act is row 18 — read what row 18
   IS at `candidacy_upgrades.md`'s row 18 before anything else, and read that file whole while you are
   there; `Grep` the staged tree for the authors' names, because the record cites this paper at V10 and
   the reader confirms where rather than inherits it.**
4. **Verify at the object, not at a report; sweep the whole document when you correct a claim; run the
   fact check twice and search for the corrected text on the second pass. Locate a section reference at
   the heading list, never from where the passage was found; locate a ROW at its table, never from a
   file name. Check the paper's printed title, authors and version against the bibliography's row
   before extracting. Do not transcribe a table cell you cannot read with certainty at the image. Do
   not cite another extract's content from a summary of it — read the extract or cite nothing. Derive
   every range, count and difference you state from the cells, state the sign convention with the
   numbers, and check it once more at the fact check. Label a classification or inference as the
   reader's where the paper does not state it. Take the `stat` of the container source IMMEDIATELY
   BEFORE every `device_commit_files`, in the same shell call as the copy where possible, COPY FROM A
   FRESH OUTPUTS PATH for every commit, and after it LIST the directory and compare the listing's size
   against THAT stat; a size that matches an older stat, or a new modification time at the old size,
   is the stale shape — it happens on never-staged paths too — stage the landed copy back, read it,
   re-commit guarded by its `expectedMtimeMs`.**
5. **A read put to him is shown, not ratified. Do not ask him to confirm it; do not gate the next act
   on his word.**
6. **Close at a member boundary with nothing half-done**, and write the next entry before you close.
