## COWORK SESSION CLOSE (HUNDRED AND TWENTY-SEVENTH ENTRY, 2026-09-06 — THE CONTINUING LINE) — ★★ **THE SESSION THAT BOOTED ON THE HUNDRED-AND-TWENTY-SIXTH ENTRY READ ROW 47 WHOLE AT THE OBJECT — BOTH OF ITS PAPERS. FIFTEEN OF 36 ARE NOW READ — rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19, 20, 18 and 47 — AND **GROUP 2 IS CLOSED**, as group 1 already was. The two extracts `reading_pass/extracts/chen-su-2019-harmony-transformer-incorporating-chord-segmentation-into-harmony-recognition.md` (45,884 bytes) and `reading_pass/extracts/chen-su-2021-attend-to-chords-improving-harmonic-analysis-of-symbolic-music.md` (45,474 bytes), and the rewritten progress record `reading_pass/l2_slice_reading_progress.md` (78,565 bytes), are on disk — all three in ONE `device_commit_files` call, ALL THREE ON THE FIRST COMMIT, all three from a FRESH container path, each proved at a directory listing against a `stat` taken in the same shell call that copied them; the progress record on a STAGED path, guarded by the staged copy's `expectedMtimeMs`.** ★★ **ROW 47 IS THE FIRST ROW OF THE SLICE WHOSE REGISTER ENTRY CARRIES TWO PAPERS. THE DECISION WAS TWO EXTRACTS, ONE PER PAPER, ON THE COMMISSION'S OWN §4 GROUND (*"Every population paper is read WHOLE. Per paper, one extraction file"*) — THE ROW STAYS ONE ROW, WITH ONE CENTRALITY VERDICT STATED ONCE.** ★★ **IT PRODUCED TWO CORRECTED STRUCTURAL CLAIMS, BOTH WITH THE USER AND BOTH APPLIED NOWHERE — AND NEITHER IS OF ROW 18's KIND, WHICH IS THE FIRST THING TO SAY ABOUT THEM.** *(a)* **The candidacy row's own reason is wrong about its paper: the Harmony Transformer does NOT decide segmentation and labelling together.** The encoder's chord-change decision is separately supervised and **committed by a hard threshold at 0.5**; the labelling is then conditioned on it through region-averaged embeddings; the training is joint and the deciding is sequential — **and the same two authors say so in terms** in the 2021 paper (*"estimated chord transitions … **and then** recognized chords"*). So on DP-C's three-way question the row is a **"before"** shape, its group 2 placement is imprecise in the way row 8's group 1 placement was, **and its ADMITTED verdict stands MORE squarely, not less**, because a rival is exactly what a design point must argue against. *(b)* **`FRAMEWORK.md` §S4(a)'s sealed first-stage sentence *"Every system from Chen and Su 2018 onward predicts key, primary degree, secondary degree, quality and inversion as separate heads"* is FALSE of Chen and Su's own 2021 system**, which publishes two outputs (a 42-way key and a 5040-way composite Roman numeral) for the reason DP-A itself gives, in the authors' words. **Correcting it would STRENGTHEN DP-A, whose live text carries no such universal.** **NEITHER (a) NOR (b) TOUCHES A CHOSEN DESIGN POINT'S LIVE RECORDED GROUND** — (a) corrects a derivation file that calls itself challengeable at its own row, (b) corrects sealed first-stage text — **so neither is reported as a STOP of the remedial commission §5 class, and the grading is stated so it can be overruled.** ★ **ROW 47 IS **NOT CENTRAL** AND OWES NO SECOND EXTRACTION**, with the ground on which CENTRAL could be argued stated in full: no ratified text names either paper and no figure DP-A's or DP-C's ground cites is theirs. ★ **THE ONE MEASUREMENT WORTH CARRYING: the 2021 paper ABLATES the mechanism by which the committed segmentation reaches the labelling (`HT-noReg`), and ALL TEN of the resulting differences are smaller in absolute value than the standard deviation that paper reports on the HT cell each is taken from — two of them running the other way — where the paper reads the same comparison as *"validating the employment of the regionalization unit"*. Derived at the transcribed cells with the sign convention stated, and stated as a BOUND and not a result, because a standard deviation is not a significance test.** ★ **THE NEXT ACT IS THE NEXT BOUNDED READING SESSION — row 45 next (Micchi, Gotham & Giraud 2020, "Not All Roads Lead to Rome"), the first member of group 3, and the FIRST row since row 18 on which a ratified `[FACT]` rests.** ★ **NOTHING RENDERS: Ruling 1's gate stands — 21 of 36 not opened.** ★ **THE HUNDRED-AND-TWENTY-SIXTH ENTRY'S RE-LANDING LANDED RIGHT: 39,435 at the root listing, the size the user gave, and a whole read.** ★ **NO BATCH IS OUT. THE TIP IS UNMOVED AT `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, read at both ref files.** ★ **THE GUARD SET STANDS AT TWELVE FAILING (relayed) AND `STATUS.md`'s TOPMOST ENTRY SAYS ELEVEN (read at the object this session) — both known, both ruled (Rulings 8 and 9), untouched; nothing ran.** ★★ **AND SEVEN ERRORS OF THE DEGRADATION RULE'S OWN FAMILY WERE COUNTED ON THIS SIDE. FOUR WERE CAUGHT BEFORE ANYTHING LANDED AND NEVER REACHED DISK; THREE WERE IN THIS ENTRY'S OWN FIRST LANDED COPY AND WERE FOUND ONLY AT THE USER'S INSTRUCTION TO FACT-CHECK THE HANDOVER, THEN CORRECTED BEFORE THE RE-LANDING, WITH THE FORMER WORDING PRESERVED AT EACH (#12). SIX OF THE SEVEN ARE A COUNT OR A PROPORTION ASSERTED AND NEVER DERIVED, AND EVERY ONE OF THE THREE THAT LANDED IS IN THIS ENTRY'S ACCOUNT OF ITS OWN CHECKING — NOT IN THE EXTRACTS, NOT IN THE PROGRESS RECORD, AND NOT IN ANY CLAIM ABOUT EITHER PAPER. SEVEN IS FAR ABOVE THE RULE'S THRESHOLD OF TWO. ONE OF THEM REPRODUCES, ALMOST WORD FOR WORD, AN ERROR THE HUNDRED-AND-TWENTY-SIXTH ENTRY HAD ALREADY COUNTED AND CORRECTED IN ITSELF, WHICH IS THE PLAINEST DEGRADATION SIGNAL IN THIS SESSION. THE NEXT SESSION MUST BE A FRESH ONE.** THE CURRENT ENTRY POINT; the hundred-and-twenty-sixth entry is superseded as entry point and stands otherwise — including its bridge-fault section, its running total and its declared departures, none of which is restated here except where this session adds to it.

You start clueless. Read this block first, then perform the ordinary session-start read: `CLAUDE.md`
whole, `DECISIONS.md` whole, `STATUS.md`, and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`). **A
single-file opening instruction is not an exemption from that read** (P-1). **The eighty-second
through hundred-and-twenty-sixth entries remain binding except where this entry names a change**, and
every standing bar of the earlier blocks remains binding.

**★ THE PRACTICAL FACTS of the hundred-and-sixteenth, hundred-and-eighteenth, hundred-and-twentieth,
hundred-and-twenty-second through hundred-and-twenty-sixth entries are unchanged and are not restated
here (#6); read them there.** This session confirms the staging route by doing it: every repository
file was staged through the bridge and read at its staged copy, and **it inherits without re-testing
the earlier entries' finding that the file tools cannot read a device path — it made no attempt at
one.** **The root listing exceeded the tool-result cap and was saved by the harness, and this session
consulted the saved listing with `Grep` only** — TWO saved root listings and **THREE** `Grep`s over
them: the first listing **TWICE** (once for the handoff-entry file names and sizes, which is how the
hundred-and-eighteenth entry was located to stage it; once for the previous entries' landing sizes,
39,435 and 35,116), the second once (to prove this entry's own landing). *(Former wording, preserved
under #12 because it was one of this session's counted errors: "TWO saved root listings and TWO
`Grep`s over them: the first listing once … the second once".)* `reading_pass/`, `reading_pass/extracts/` and
`docs/research_papers/` were listed directly as subdirectories. **`candidacy_upgrades.md` lives in
`reading_pass/`, not at the root.**

**★ ONE NEW PRACTICAL FACT, AND IT NEARLY COST A CHECK.** **A `Grep` for a two-word phrase can miss a
real occurrence, because the phrase may be split across a line break in the file.** The findings
surface names the Harmony Transformer at lines 186–187 with *"Harmony"* ending one line and
*"Transformer v2"* beginning the next; a search for the phrase returned nothing there, and only the
bare-word search for *"Transformer"* found it. **When you confirm where the record cites a paper,
search BARE WORDS as well as phrases, and say in the extract which searches you ran.** This is carried
into the progress record's items for row 45.

**On form.** Plain ordinary English with the user. **NEVER use the multiple-choice widget with him.**
A decision surface is fact-based and checked at the objects, every position weighed towards the
ultimate objective and towards the guiding principles, the recommendation with the reason and the
strongest argument against it; when the facts collapse the alternatives to one, say so; before putting
a question, check whether the record already answers it. **He asks that a dispatch be sourced and
fact-checked at the objects before he runs it, and that a deliverable he is about to rule on be
fact-checked as a turn of its own before any question** — do both unasked. **A read put to him by name
is shown, not ratified; if he says nothing it stands and the next act proceeds.** Every self-invented
word is explained in plain words the first time or not used.

### ★★ THE BRIDGE FAULT — READ THE HUNDRED-AND-EIGHTEENTH ENTRY'S SECTION BEFORE YOU WRITE ANYTHING

It is not restated here (#6). **This session saw NEITHER shape.** Five landings were made or proved
this session and all five carried the right bytes: (1) the hundred-and-twenty-sixth entry's
RE-LANDING — its corrected, guarded one, left to this session to prove — proved at **39,435** bytes at
the saved root listing (read by `Grep`), the size the user gave as its proof, and by a whole read;
(2), (3) and (4) the two row 47 extracts (never-staged paths, **45,884** and **45,474** at the
listing) and the progress record (staged path, guarded by the staged copy's `expectedMtimeMs`
1788682829728, **78,565** at the listing) — **all three in ONE `device_commit_files` call**, all three
from a FRESH container path (`outputs/rp127a/…`), all three equal to a `stat` taken in the same shell
call that copied them, immediately before the commit; and (5) this entry's own FIRST landing, from a
second fresh path, proved at the root listing against its own immediately preceding `stat`. **A sixth,
this entry's RE-LANDING (declared at (xvi)), is necessarily outside this count and outside every count
below that is stated "up to this entry's landing" — an entry cannot contain the proof of its own final
landing, the same bound the record already names for a commit that cannot carry its own hash, and the
clause is written so that it bounds itself rather than recurring.**

**The observation the hundred-and-twenty-fourth entry recorded — that across three commits from ONE
unchanged container path each landing carried the bytes that path held at the commit before it — now
has thirteen data points on the other side: every landing KNOWN TO THIS SESSION to have been made from
a fresh container path has landed the current bytes** — the eight the hundred-and-twenty-sixth entry
records, plus the five above, of which four were made and proved here and one was proved here.
**Still no cause is asserted (D-641, D-658).** What it changes in practice, and only this: **use a
fresh outputs path for every commit** — a rule that costs one `mkdir` and removes the condition under
which the two proven stale landings of the hundred-and-twenty-fourth entry occurred. Whatever the
path, the operating rule is unchanged: prove every write at the listing against the immediately
preceding `stat`, and read the landed copy back when the sizes differ.

**The running total, in the hundred-and-twenty-fourth entry's two-reading form:** the
hundred-and-twenty-sixth entry's totals were *fifty-two across thirteen sessions, thirty-seven
never-staged and fifteen staged*, or *fifty-one, thirty-six and fifteen* — a count that names *"this
entry's own landing"* in the singular and declares its re-landing apart, at its own (xiii). This
session proves that re-landing (staged, guarded) and adds four of its own — the two extracts
(never-staged), the progress record (staged, guarded) and this entry's own FIRST landing
(never-staged): **fifty-seven across fourteen sessions, forty never-staged and seventeen staged; or
fifty-six, thirty-nine and seventeen.** **This entry's RE-LANDING is outside that count, by the
self-bounding clause above, and the next session adds it (staged, guarded) when it proves it.** **Beside either total stand FIVE proven STALE
landings — four on staged paths and one on a never-staged path** — unchanged by this session.

### What this session was

The session that booted on `cowork_handoff_entry_one_hundred_and_twenty_six.md`, on the user's opening
instruction to read that entry with the file tools, take its proof of landing as 39,435 bytes at the
root listing, and read what row 47 IS at `candidacy_upgrades.md`'s row 47 before staging anything. It
did both first and reported them to the user in plain text — **declaring in the same turn that reading
those two files required staging them, which is the departure from "before staging anything"** — then
on his "go ahead" the full session-start read (the tip at both ref files; `CLAUDE.md` whole, loaded by
the harness; `DECISIONS.md` whole, 862 lines; `STATUS.md` whole; the gating answer — 244 open, 219
gating, 25 non-gating, and the `gating_ids` list whole; the hundred-and-eighteenth entry whole for its
bridge-fault section), then the reading set the cadence names (the progress record whole; both
commissions whole; the row 18 extract whole for the form; `candidacy_upgrades.md` whole; the L2 slice
derivation whole; `docs/research_papers/BIBLIOGRAPHY.md` whole; `FRAMEWORK.md` at the L2 charter, DP-A
to DP-K, and §S4's opening with §S4(a); the findings surface at its DP-A and DP-C blocks), then one
thing:

**Read row 47 whole at the object — BOTH its papers — and extracted them.** Chen & Su, *"Harmony
Transformer: Incorporating Chord Segmentation into Harmony Recognition"*, ISMIR 2019, the held file
`docs/research_papers/chen_su_2019_ismir_harmony_transformer.pdf` (744,532 bytes), eight pages,
proceedings pp. 259–266; and Chen & Su, *"Attend to Chords: Improving Harmonic Analysis of Symbolic
Music Using Transformer-Based Models"*, TISMIR 4(1) 2021, the held file
`docs/research_papers/chen_su_2021_tismir_attend_to_chords.pdf` (2,915,635 bytes), thirteen pages,
pp. 1–13. Both staged through the bridge by their matching names in the `docs/research_papers/`
listing and read as page images with the file tools; the printed title, authors, venue and year of
each were checked against the bibliography's row on page 1 before anything was extracted. **The two
extracts are the record; what they produced, in one line each, is in the progress record and is not
restated here (#6).**

**Where the record cites this row was established by `Grep` BEFORE extracting, not inherited:** five
places and only five — `candidacy_upgrades.md` line 119 (the row), the slice derivation line 82 (the
placement), `BIBLIOGRAPHY.md` line 66 (**ONE row carrying BOTH papers**), `FRAMEWORK.md` line 1553
(§S4(a)'s provenance sentence, which names *"Chen and Su 2018"* — **row 46, not row 47**), and the
findings surface lines 186–187 (*"Harmony Transformer v2 62.1"*). **`FRAMEWORK.md`'s live DP-C does not
cite this row at all**, and no `[FACT]` in the record names either paper — a claim bounded in the
extract by a figure-by-figure comparison against DP-A's and DP-C's cited figures, with what was NOT
checked stated.

### ★★ THE TWO FINDINGS THE NEXT READER MUST MEET BEFORE ANYTHING ELSE

**Both are with the user, both are applied nowhere, and NEITHER supersedes row 18's finding (3), which
is also still open.** Stated once here because a reader who meets §S4(a) or the candidacy row before
meeting them will build on a sentence the primaries contradict.

**(a) The candidacy row's reason, and the slice derivation's line 82.** Both say the Harmony
Transformer *"decides segmentation and labelling together"*. At the object: §3.3 of the 2019 paper
binarizes the encoder's chord-change probability with **deterministic binary neurons and hard
thresholding at 0.5**; §3.4 then **average-pools the decoder's embeddings inside the regions that
binary sequence defines** and feeds the result to the recognition decoder; equation (14) trains both
with one total loss and §4.3 carries the gradient across the threshold with a straight-through
estimator. **The training is joint; the deciding is sequential.** The 2021 paper's §2.2 says it in the
authors' own words: *"the HT estimated chord transitions (or chord boundaries), **and then** recognized
chords via attending to the segmentation-informed sequence."* **What follows and what does not:** the
row stays **ADMITTED** (a rival is what consequence (i) admits); its **group 2 placement is imprecise**,
the same shape as row 8's finding (1); DP-C's verdict and ground are untouched; and the row becomes
the slice's closest published analogue to the findings surface's rival-shaped BACHI item, which is a
fact the DP-C block would want beside that item.

**(b) `FRAMEWORK.md` §S4(a), line 1553, the sealed first-stage text.** *"Every system from Chen and Su
2018 onward predicts key, primary degree, secondary degree, quality and inversion as separate heads."*
**True of the 2019 paper** — its §4.2.2 and Table 2 are exactly that, five heads with class counts 24,
21, 21, 10, 4. **False of the 2021 paper by the same authors**, whose Table 3 gives two outputs: a
42-way key and a 5040-way composite Roman numeral. The authors' stated reason is DP-A's own argument:
the per-component form *"makes the components somewhat independent of each other"*. **And §S4(a)'s next
sentence — *"Every later system adds machinery to undo the separation"* — names AugmentedNet,
ChordGNN, AnalysisGNN and RNBert but not this paper, which is an instance of it and predates three of
the four.** *(The three, derived by year at the bibliography, which this session read whole: ChordGNN
2023, RNBert 2024, AnalysisGNN 2025. AugmentedNet is also 2021 — this paper prints "Published: 24
February 2021" and AugmentedNet is ISMIR 2021 — and the order within that year is NOT established from
anything read here, so it is left undetermined rather than counted either way.)* So the passage **overstates its universal while understating its own evidence, and
correcting it would strengthen DP-A**, whose live text (lines 671–683) carries no universal at all.
**NOTHING IS AMENDED.** It is the class of row 27's precision that Appendix B's sealed draft names
Catteau where the live charter says "another".

**Why neither is reported as a STOP, stated so the grading is challengeable.** The remedial commission
§5's STOP is *"a CORRECTED structural claim that moves a design-point verdict"*, and row 18's finding
(3) qualified because it corrected **DP-C's own live recorded ground**. (a) corrects a derivation file
whose banner says every verdict in it *"is challengeable at its own row"*, and moves that verdict not
at all; (b) corrects sealed first-stage text in a direction that supports the live design point.
**The user may grade either otherwise; the facts are above and nothing rests on the grading.**

### ★ THE STATE YOU INHERIT

**What in this section was read at an object this session and what was relayed, stated once:** the tip
(both ref files), `CLAUDE.md`, `DECISIONS.md`, `STATUS.md`, the gating answer, the progress record,
`candidacy_upgrades.md`, both commissions, the hundred-and-eighteenth entry whole, the slice derivation
whole, the bibliography whole, `FRAMEWORK.md` at the sections named above, and the findings surface's
DP-A and DP-C blocks were READ. The verdict counts, the guard-set figure, the untracked-file list, the
continuous-pruning remark and the "waiting on him" list are RELAYED from the hundred-and-twenty-sixth
entry, which relays them from earlier entries; this session did not open those earlier entries.

- **The tip** is `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, read at both ref files with the file tools
  (`.git/HEAD` → `refs/heads/master`); nothing ran since. **No batch is out.**
- **`VERDICTS["l2"]`** 111 IN / 133 OUT / 0 UNPLACED (relayed; not re-verified). **`WITHHELD`, `EXTRAS`,
  `FROZEN`** carry no `l2` key (relayed; `STATUS.md`'s topmost entry, read this session, says the same of
  the writeback batch).
- **The reading file** `ratification_surfaces/cowork_withheld_family_l2_reading.md` stands RULED
  (`STATUS.md`'s topmost entry, read this session).
- **Task B's L2 slice: 15 of 36 read** (rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19, 20, 18 and 47);
  **groups 1 AND 2 are both CLOSED; group 3 is open with none of its thirteen members read.** The
  progress record is `reading_pass/l2_slice_reading_progress.md` (78,565 bytes) — read it whole before
  opening any paper; it names the next member, carries FIVE things to row 45, and states that its row
  numbers are `candidacy_upgrades.md`'s and that `object_reads/` uses `population.md`'s numbering.
- **Untracked files of this line at writing, none landed by this side:** this entry; the
  hundred-and-twenty-sixth through hundred-and-ninth entries; the surface
  `cowork_l2_boot_list_surface_2026_09_05.md`; the sitting record
  `cowork_rulings_2026_09_05_l2_boot_list_sitting.md`; the two derivations of the hundred-and-eleventh
  entry; the read file `cowork_l2_score_set_read_2026_09_05.md`; the **sixteen** extracts of rows 27, 28,
  1, 8, 3, 26, 29, 30, 5, 10, 11, 19, 20, 18 and 47 — **row 47 having two** — and the progress record.
  The standing untracked historical `cc_*` root population is pre-existing. This side ran no enumeration.
- **The gating answer:** 244 open, 219 gating, 25 non-gating at the session-start read; nothing ran.
- **The guard set:** 77 run, 12 failing (relayed; not re-run). **`STATUS.md`'s topmost entry says
  "ELEVEN FAILING"** (read at the object this session; Ruling 9: corrected in place by the next dispatch
  that touches `STATUS.md`, former wording preserved). **The continuous-pruning bound is unmaintained** —
  standing since the hundred-and-sixth entry (relayed).
- **The plan stands at:** … → L2's boot list RULED (nine rulings) → reads 1, 2 and 3 of §11 DONE →
  **Task B's L2 slice (36 papers) read whole at the object — 15 DONE, 21 TO GO, groups 1 and 2 closed,
  group 3 open with all thirteen to go** — in bounded sessions closing at member boundaries, under the
  remedial commission §3 and the original commission's §4 form, central papers owing a second
  extraction, in the verdict table's proposed order ("stated, not ruled") → **the pack-build dispatch**
  when next (D-250) → the brief → the blind deriving session → L2's comparison reading. **Beside it, the
  L0/L1 comparison reading stands at 7 of 29 documents tabulated** (relayed).

### ★★ THE GATE, RESTATED

No derivation may begin before L2's slice of Task B is in (eighty-seventh entry; Ruling 10 of the
2026-08-31 sitting; Ruling 1 of the 2026-09-05 sitting). **The slice is 36 papers, all held. Fifteen are
read.** The gate is not lifted and cannot be until the other 21 are read, flagged unreadable, or
stop-reported.

### ★ WHAT COMES NEXT

**The next act is the next bounded reading session** — open `reading_pass/l2_slice_reading_progress.md`,
the remedial commission's §3 and the original commission's §4 form before reading anything; take the next
row's paper from `candidacy_upgrades.md`'s own row (never from a file name elsewhere), find its held file
in the `docs/research_papers/` listing, stage it by name, check its printed title, authors and version
against the bibliography's row on page 1, read it as page images, extract in the §4 form, rewrite the
progress record whole (a file this line wrote, 78,565 bytes — read it whole first), close at a member
boundary. **Row 45 is next and it OPENS group 3** — this session read its table line in
`candidacy_upgrades.md` (line 117: Micchi, Gotham & Giraud, TISMIR 3(1) 2020, "Not All Roads Lead to
Rome", held, admitted because *"The lineage's baseline architecture and the source of DP-A's
self-contradiction quotation"*) and its slice-derivation line (line 80: *"The lineage's baseline
architecture; DP-A's evidence, read as a candidate scoring architecture for L2"*); **the bibliography's
row for it (line 64) was NOT read this session and the next reader reads it.** The held file is
`docs/research_papers/micchi_gotham_giraud_2020_tismir_not_all_roads_lead_to_rome.pdf` (1,906,114 bytes)
at this session's listing. **Row 45 differs from every row read since row 18 in one respect: a ratified
`[FACT]` rests on it** — the *"potential for self-contradictory outputs in which the six sub-labels have
different ideas about the chord"* quotation, carried at DP-A (lines 671–683) and at §S4(a) — **so it
carries a verification target, and that quotation is verified at its own page before anything is built
on it.** **The five things carried to row 45 are in the progress record's "Next" section.** The
pack-build dispatch is NOT next (D-250).

**Routed in the two row 47 extracts and the USER's to act on or to leave:** the two corrected structural
claims above; two mild bibliography-reconciliation points (one register row carries two distinct
publications, so it cannot be resolved to a single held file; and the 2019 held file's printed reference
list ends at [41] where its own §4.3 cites [42]); the three precisions on the findings surface's naming
of *"Harmony Transformer v2"*; the `HT-noReg` ablation and its bound, for the DP-C block beside the
rival-shaped BACHI item; the answered length-term question (no length term of any kind, a fourth shape
beside the slice's three); the 2021 paper being the first in the slice to print an uncertainty on every
figure, for measurement design as the pattern to want; its figures being model-selected on the sets they
are reported over, beside D-097, D-574 and principle #20; the two corpora's functional figures being far
apart with no experiment separating repertoire from data quantity, a datum beside D-500; the 2019
model's 'others' and 'no-chord' classes for the L2 charter's candidate-admission clause; the 2021 model's
42 SPELLED keys decided from an unspelled piano roll, beside DP-F and D-450; the authors' naming of
boundary detection and chord recognition as *"intertwined problems"*, ENRICHES-shaped for DP-C and kept
**unmerged** with row 18's finding (3); and the mapping decision that grades a dominant seventh and an
augmented sixth as MAJOR while excluding every diminished sonority from evaluation, for measurement
design. **Of these, only the two corrected structural claims are of the shape that could be graded a
STOP, and neither needs a ruling before row 45 is read.** **Shown, not ratified.**

### The user-owed list

**Closed this session by this side, and not owed to him:** the eleventh reading session (row 47), which
closes group 2.

**Owed to him by this side:** nothing beyond the two corrected structural claims, which are PUT to him
with this entry and the extracts, and applied nowhere. Every other finding is ROUTED in the extracts
themselves and applied nowhere.

**Waiting on him, CARRIED from the hundred-and-tenth entry as later entries relay it (this session read
only the hundred-and-twenty-sixth and the hundred-and-eighteenth), still open, unverified this session:**
**(a)** whether the pilot's derived specification still carries its independence claim after Ruling 85;
**(b)** which branch of the filing convention (D-674) governs a ruling record; **(c)** the close test's
wording (close section and end-state artifact in one commit versus one apart); **(d)** re-aiming the
`expected_line` values in `gen_phase3_gate_partition.py`; **(e)** whether the report template should carry
the close-tip marker by design; **(f)** the fifth, sixth and seventh guard observations not yet in
OI-378's detail file. **And, from the hundred-and-twenty-sixth entry, row 18's finding (3)**, which is
open and is not applied, restated as settled, or built on by this session's work.

**Shown to him and standing unless challenged:** the score-set read of §11 (hundred-and-twelfth entry);
the row 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19, 20, 18 and 47 reads. His opening instruction this
session named row 47 as the next act and his "go ahead" released the session-start read and the papers,
which is the next act proceeding, not a ratification of any read.

**Standing, unchanged and still the user's:** the list the hundred-and-ninth entry carries under this
heading, as later entries relay it — neither the hundred-and-ninth nor the hundred-and-tenth entry was
read this session. **The eleven quarantined audit questions stay reserved to the AUDIT.** **Not acted
on:** the guard-runner parallelisation suggestion; the rendering tool's "1 entries" pluralisation defect.

### Errors

**★ COUNTED ON THIS SIDE THIS SESSION, OF THE FAMILY THE DEGRADATION RULE NAMES: SEVEN.** **FOUR — (1)
to (4) below — were caught at this side's own fact check and corrected before anything landed, so they
never reached disk.** **THREE — (5) to (7) — were in THIS ENTRY'S OWN FIRST LANDED COPY**, were found
only when the user instructed that the handover be fact-checked, and are corrected here before the
re-landing, with the former wording preserved at each (#12). **All seven are of one shape — an
assertion made ahead of its derivation — and six of the seven are a count or a proportion.** **Every
one of the three that landed is in this entry's account of its OWN checking**; none is in the two
extracts, none is in the progress record, and none is a claim about either paper.

**(1)** A `Grep` pattern was written with an HTML-escaped ampersand, so the search this side then
described as covering *"Chen & Su"* **never ran as written**. Found by re-checking the searches
themselves rather than their results; the bare-word search for *"Chen"* was then run and returned
nothing beyond what was already found, so **no conclusion moved** — but the extract had already been
drafted asserting a search that had not happened. Corrected: the extract now lists the searches
actually run, in order, and says which ones found what.
**(2)** *"6 References, 42 entries"* in the 2019 paper's structure list — **a count asserted and never
derived.** The re-read that checked it established that the printed list **ends at [41]** while §4.3
cites [42], which became a recorded finding of its own. Corrected to carry no count.
**(3)** *"Third instance in the slice after rows 19 and 18"* for the fixed-grid shape — **a count
asserted and never derived, and wrong**: row 20 carries the same shape on a 0.1 s frame lattice.
Replaced with the three rows named and no count at all.
**(4)** *"the first of the slice to close on a proceedings text with venue, pages, licence and year all
printed"* — **an assertion about unexamined things**: the progress record's own rows record that 19, 20
and 30 also closed with no identity finding. Replaced with those rows named.

**★ THE THREE THAT REACHED DISK, found at the user's instruction to fact-check the handover.**

**(5) THE WORST OF THE SEVEN, AND THE REASON THE CAPACITY JUDGMENT IS NOT A FORMALITY.** This entry's
first landed copy said, in its practical facts and again at its departure (v), *"TWO saved root
listings and TWO `Grep`s over them: the first listing once … the second once"*, and its departure
(xiv) said the root listing was *"consulted by `Grep` once"*. **There were THREE `Grep`s over two
listings, the first listing TWICE.** Corrected in all three places, former wording preserved at the
first. **This is, almost word for word, the error the hundred-and-twenty-sixth entry counted as its
own (2) and corrected in itself** — *"two root listings, each consulted once"*, where the truth was
three `Grep`s over two listings. **Reproducing a predecessor's already-corrected error, in an entry
that had read that predecessor whole, is a stronger degradation signal than any of the other six.**

**(6)** This entry's first landed copy said the second reading of the 2021 paper's Table 4
*"confirmed all eighty cells against three of the paper's own counting sentences"*. **It did not, and
could not**: those three sentences count how many measures one model beats another on, so they
cross-check the ORDERINGS among named model pairs, not the digits of eighty cells. What the extract
itself says — and says correctly — is that the three counting sentences close against the transcribed
cells and that every cell was read twice at the image and agreed cell for cell. **The entry
overstated its own extract.** Corrected below.

**(7)** The same sentence said the 2019 paper's Table 1 had *"all six of Table 1's"* cells confirmed
by arithmetic the prose states. **Table 1 has nine numeric cells and the prose arithmetic reaches
five of them** (BLSTM, FK and HT on Billboard; BLSTM and HT on BPS-FH); the HT* column and both
segmentation cells are not cross-checked by any prose. Corrected below.

**(2) and (4) are the same error twice over in different clothes; (5) is one error stated in three
places.** **(1) is the most serious of the four that did not land**, because a check that did not run
is worse than a count that is wrong: it would have left a claim about the record resting on nothing.
It is why this entry states the searches rather than summarising them.

**SEVEN IS FAR ABOVE THE DEGRADATION RULE'S THRESHOLD OF TWO, AND IT IS REPORTED AS THE RULE ASKS
RATHER THAN ABSORBED. THE NEXT SESSION MUST BE A FRESH ONE.** What the seven are NOT: **none is a
claim about either paper's content.** **Every quotation, every table cell and every figure in the two
extracts was read at the page image; the fact check that found (1) to (4) re-checked those and moved
none of them; and the fact check that found (5) to (7) re-checked this entry's every figure, size,
line number and quotation against the objects and moved none of those either.** The pattern is
unmistakable and is stated rather than left to be inferred: **the analysis held and the bookkeeping
about the analysis did not.** That is precisely the shape the user's standing rule names — a count or
proportion asserted and never derived, and a summary cited in place of the source it summarises.

**What the fact check DID confirm, recorded because it is what the check is for, and now stated at its
true width:** the second reading of the 2021 paper's Table 4 read every cell a second time and the two
readings agreed cell for cell, and **three of the paper's own counting sentences close against the
transcription in both corpora** — including the one whose "four out of five" is right only because one
cell is exactly equal rather than greater; the second reading of the 2019 paper's Tables 1 and 2
confirmed **eight of Table 2's ten cells and five of Table 1's nine numeric cells** by arithmetic the
prose states, and **the cells with no prose cross-check are recorded with that stated** rather than
glossed.

**★ NO BREACH OF THE WORKING-TREE READ RULE (D-253) BY THIS SIDE THIS SESSION.** **Two `Edit` calls
failed to match, and BOTH were recovered with `Grep -n` and `Read` at an offset — never with a shell.**
That is exactly the remedy the hundred-and-twenty-sixth entry prescribed after its own breach, and it is
recorded here as the remedy working.

**Errors of previous sessions found this session: one, and it is small.** The hundred-and-twenty-sixth
entry's instruction to *"read the bibliography's rows for BOTH of that row's papers … one per held
file"* is **not performable as written**: `BIBLIOGRAPHY.md` carries **ONE row (line 66) for both
papers**, with two URLs. Everything else it reported is CONFIRMED at the objects: the two held file
names and their sizes (744,532 and 2,915,635) at this session's listing; that `candidacy_upgrades.md`
row 47 is at line 119 and the slice derivation's at line 82, both quoted verbatim; that the row is two
papers in one register entry; and its own landing at 39,435. Its running total is carried forward as it
states it.

**Declared departures of this side, this session.** (i) **The two opening reads required STAGING the two
files they were reads of**, which is a departure from the user's *"before staging anything"*; it was
stated to him in the same turn as the reads and nothing else was staged until his "go ahead". (ii) **A
shell `cp` of the progress record's STAGED copy into the work directory before its rewrite** — the
hundred-and-twentieth through hundred-and-twenty-sixth entries' declared departure, repeated here for the
same reason (no work-directory copy existed and re-typing 66,000 bytes from tool output is the
transcription risk the record forbids); the copy was byte-exact (66,422 at `stat`), then changed with
**NINE `Edit` calls covering TEN places**, one call carrying two of them (the session banner; the row 47
table line and the counts paragraph, together in one call; the "what row 47 produced" paragraph; the
"Next" section's opening; its carried items; its "shown to the user" paragraph; the not-done paragraph;
the provenance's session count; the provenance's eleventh-session sentence and exceptions list).
**Every one of the nine matched on the first attempt; the two `Edit` failures this session were on the
2019 extract, not on this file, and are declared at (xv).** (iii) **One `mkdir` and one `cp`+`stat` in the same call for the three reading files'
fresh outputs path; one `mkdir` and one `cp`+`stat` for this entry's** — all in the container on this
side's own output files, declared because they are shell commands in the sandbox. **No shell read
repository content or any staged copy of it.** (iv) **Two `device_commit_files` calls:** the first
carrying three files — the two extracts (never-staged) and the progress record (staged, guarded by
`expectedMtimeMs` 1788682829728) — all three landed right on the first attempt and all three proved at
listings; the second carrying this entry, from a fresh container path on a never-staged path, proved at
the root listing against the `stat` taken immediately before it. (v) **`Grep` through the file tools**
on: the two saved root listings — **THREE `Grep`s, the first listing TWICE and the second once** — for
handoff-entry file names and sizes *(former wording, preserved under #12: "two `Grep`s, one over
each")*; the staged tree, **four searches** (the phrase set; the bare word "Transformer"; "BACHI"; the
bare word "Chen"); and this side's own draft files, **two searches** — one sweeping the drafts for
unqualified superlatives and instance counts, one confirming the corrected text on the second pass.
(vi) No subagent was used. (vii) **TWO papers were opened, both whole** — eight pages and thirteen
pages; the 2019 paper's pages 5 and 8 and the 2021 paper's page 8 were re-read for the fact check.
(viii) The handoff entries before the hundred-and-twenty-sixth were NOT read except the
hundred-and-eighteenth, read whole; the sitting record, the hundred-and-eleventh entry's derivations,
the extracts of rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19 and 20, the V4 STOP memo, `population.md`
and `FRAMEWORK.md` outside the sections named above were NOT read. (ix) `reading_pass/continuation.md`
was NOT staged this session. (x) The user connected no new folder. (xi) The two opening reads were
reported as the first turn's final response and the close is reported as this turn's; **no
multiple-choice widget was used.** (xii) The task-list widget was used to track the session's six steps;
it asked the user nothing. (xiii) **The bridge to the user's computer dropped and reconnected several
times during the session.** No call was lost to it, nothing was re-run because of it, and no landing was
affected; it is recorded because it is a fact about the environment the next session may meet. (xiv)
**SIX directory listings were requested UP TO THIS ENTRY'S FIRST LANDING**, and the count is the
enumeration's: (1) the repository root at the start (exceeded the cap, saved, consulted by `Grep`
**TWICE**); (2) `reading_pass/` before staging; (3) `docs/research_papers/` before staging the two
papers; (4) `reading_pass/` and (5) `reading_pass/extracts/` after the reading files' commit, for their
proof; and (6) the root after this entry's first landing (saved, `Grep` once). **The listing that
proves the RE-LANDING declared at (xvi) is necessarily outside this count, by the self-bounding clause
in the bridge-fault section.** *(Former wording, preserved under #12: "(1) the repository root at the
start (exceeded the cap, saved, consulted by `Grep` once)" and "The listing that proves THIS entry's
landing is inside that count because this entry is landed once".)* (xv) **TWO `Edit` calls FAILED to match, both on the 2019 extract — one on its
structure list, one on its identity paragraph — and BOTH were recovered with `Grep -n` and then `Read`
at an offset, never with a shell**, which is the remedy the previous entry prescribed; each then matched
and changed what it was meant to. (xvi) **AFTER ITS FIRST LANDING THIS ENTRY WAS RE-STAGED AND READ BACK
WHOLE AT THE OBJECT** — the landed copy read identically to what was written, which proves the landing a
second way — **fact-checked on the user's instruction, amended in ten places, and RE-LANDED on the
by-then-staged path, guarded by the landed copy's `expectedMtimeMs` 1788694160084, from a fresh
container path.** The amendments: the three counted errors that had landed (the `Grep`-count in three
places, the "all eighty cells" overstatement, the "all six of Table 1's" count); the banner's error
count and its account of what landed; the Errors section rebuilt around seven rather than four; the
self-bounding re-landing clause in the bridge-fault section and in the running total; the derivation
behind *"predates three of the four"*; the draft-file `Grep` count at (v); and this clause. **The
2021 extract was amended in the same act, for the same derivation, and re-landed guarded beside it.**

**What went right and is worth copying:** the two opening reads were done and reported before the boot
so the user saw them at once, and the departure from "before staging anything" was stated in the same
turn; **the `Grep` for the record's citations was run BEFORE extracting and, when the phrase search came
back empty at the findings surface, the check was widened to bare words rather than concluded from —
which is what found the citation and is the practical fact this entry carries forward**; the record's
sentence about the paper was tested against the paper's own words, which is what found both corrected
claims; **the second of them was found by reading the COMPANION paper against the record's universal**,
which is an argument only a whole-row read could produce and is the case for reading two-paper rows
whole; the ablation arithmetic was derived at the cells with its sign convention stated and reported as
a bound rather than a result; the fact check caught four assertions ahead of their derivation before
landing, and one of them turned into a recorded finding about the held file — **and the user's
instruction to fact-check the handover caught three more that had landed, which is the case for that
instruction being standing rather than occasional**; every commit went from a
FRESH container path and every landing was proved at a listing against a `stat` taken in the same shell
call as the copy; and the handover was written at a member boundary — **a group boundary, in fact** —
with nothing half-done.

### The cadence for your session

0. **Boot as above, in full.** This entry file is UNTRACKED and you never land it yourself; a later
   dispatch's Task 0 does. **If your first file read fails, stage the file through the bridge and read the
   staged copy; only if staging fails, ask the user to add the folder.** **List `docs/research_papers/` and
   `reading_pass/` as subdirectories; if you must consult the saved root listing, `Grep` it.
   `candidacy_upgrades.md` is in `reading_pass/`; the slice derivation is
   `cowork_l2_task_b_slice_derivation_2026_09_05.md` at the root.**
1. **Re-read the tip at the ref files with the file tools.** It should be `911f5f7cda…` at both. A moved
   tip means something ran that this entry does not know of — read `cowork_away_returns.md`'s last section
   before anything else.
2. **BEFORE YOU WRITE ANYTHING, READ THE HUNDRED-AND-EIGHTEENTH ENTRY'S "THE BRIDGE FAULT" SECTION.**
   Prove this entry landed: a successful whole read of it at the object is the proof; record its size from
   the root listing in your own entry.
3. **Read `reading_pass/l2_slice_reading_progress.md` whole**, then the remedial commission's §3 and the
   original commission's §4 form, then one of the sixteen extracts for the form (the 2021 row 47 extract is
   the most recent; the row 10 extract is the largest). **The next act is row 45 — read what row 45 IS at
   `candidacy_upgrades.md`'s row 45 before anything else, and read that file whole while you are there;
   read the bibliography's row 64; and confirm where the record cites the paper with `Grep`, searching
   BARE WORDS as well as phrases, because a phrase can be split across a line break — which is how this
   session nearly missed a real citation.**
4. **Verify at the object, not at a report; sweep the whole document when you correct a claim; run the fact
   check twice and search for the corrected text on the second pass. Locate a section reference at the
   heading list, never from where the passage was found; locate a ROW at its table, never from a file name.
   Check the paper's printed title, authors and version against the bibliography's row before extracting.
   Do not transcribe a table cell you cannot read with certainty at the image, and say when a cell has no
   cross-check. Do not reconcile two statements the paper itself leaves different — record both as printed.
   Do not cite another extract's content from a summary of it — read the extract or cite nothing. **DERIVE
   EVERY COUNT YOU STATE, INCLUDING "the first", "the only", "the nth instance" AND EVERY COUNT OF YOUR
   OWN TOOL CALLS — six of this session's seven counted errors were counts of exactly that kind, and
   three of them landed**; where a count is not worth deriving, name the rows instead and say no count is
   asserted. **When you summarise a check you ran, do not state it more strongly than the file it is
   recorded in states it** — that is how two of the three that landed happened. State the sign convention
   with every difference. **Do not read a difference as a result where the paper's own reported spread is as large as
   the difference.** Label a classification or inference as the reader's where the paper does not state it.
   WHEN AN `Edit` FAILS TO MATCH, RE-READ THE PASSAGE WITH `Grep -n` AND `Read` AT AN OFFSET — never with a
   shell; it worked twice this session. Take the `stat` of the container source IMMEDIATELY BEFORE every
   `device_commit_files`, in the same shell call as the copy where possible, COPY FROM A FRESH OUTPUTS PATH
   for every commit, and after it LIST the directory and compare the listing's size against THAT stat; a
   size that matches an older stat, or a new modification time at the old size, is the stale shape — it
   happens on never-staged paths too — stage the landed copy back, read it, re-commit guarded by its
   `expectedMtimeMs`.**
5. **A read put to him is shown, not ratified. Do not ask him to confirm it; do not gate the next act on
   his word.** Row 18's finding (3) and this session's two corrected structural claims are the exceptions
   in kind, and even they are SHOWN: they are written up and put to him, and the next act does not wait on
   his answer — but they are not applied, and nothing is built on them, until he rules.
6. **Close at a member boundary with nothing half-done**, and write the next entry before you close.
