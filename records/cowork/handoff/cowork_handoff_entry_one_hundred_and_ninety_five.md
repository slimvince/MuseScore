# Cowork handoff entry 195 — 2026-09-18

**The current entry point.** Entry 194 is superseded as entry point and stands otherwise. Grades: **[checked]** =
opened or measured at the file by this sitting; **[relayed]** = taken from CC's report or chat reply, or from
another record, and not checked here.

## 0. State at close

- `.git/refs/heads/master` and `.git/refs/remotes/origin/master` both read
  `5c95032de157aff105c2c2abefce2c87cbb95cab` **[checked]**, the value entry 194 records at its close. No commit and
  no push were made this sitting. `STATUS.md` is unchanged (12,068 bytes at staging **[checked]**).
- **Nothing is running. No dispatch is out.** This entry is landed uncommitted, like entries 189 to 194 before
  their batches; the next batch that commits record files carries it.
- The open point of entry 193 §1 — whether the reclassification of the three passage checks owes a
  decisions-register entry — is still open and was not put this sitting.

## 1. The user's rulings, 2026-09-18

**Ruling 1 — the 173 `cowork_*.md` files at the root: Alternative D.** His words: "I agree with D", given on the
surface turn itself (the surface was complete and in front of him; not re-asked). Ruled: the files stay at the root
until the fourteen owed second extractions (entry 186's count) are done. When the question is taken up again, the
first act is a read-only measurement by CC, per target file — register homes, `ARCHITECTURE.md` and `CLAUDE.md`
mentions, specification-set membership, frozen-pack mentions, tool code naming it, citing-line count — and any move
is of the dated records only, with a class rule for the files CC classed UNDECIDED decided on the measurement rather
than file by file. The alternatives put: A leave all; B move the dated subset, later; C move everything, including
the documents `ARCHITECTURE.md` delegates to; D defer, then B with the measurement first.

**Ruling 2 — the rest of the root clean-up list is deferred to the same point.** His words: "I agree with your
recommendations". Ruled: entry 194 §2 items (b) and (c) — the loose leftovers at the root (old test JSON, logs, echo
files, the file named `--output-json`, `.bat` scripts, an `.mp3`, scratch folders); the 24 files the map never
classified; CC's five self-check rows; entry 189 decision 4 (`Claude outputs/`, `Codex research inventory/`, the
`.mscx` byte-stability question); the 35 `LINE DRIFT` lines in `CLAUDE.md`; the stray `…_eighty_two-1.md` — are
all deferred with the 173. **Two members are named to be looked at first when the list is taken up:** whether any root `.bat`
script is a build command `BUILD_AND_TEST.md` names (not checked; moving or deleting one would break that file), and
the `.mscx` byte-stability question, because a score file's bytes changing under a measurement is an establishment
matter and an establishment obligation gates whatever its subject (`CLAUDE.md`, the open-items register section).
The ground for both rulings, stated on the surface: apart from the two members named above, none of these items
bears on the analysis, its inputs or a measurement tool, so none of the others gates anything; each would cost a CC
batch or a sitting for no gain towards the objective. *(★ Corrected at the user-ordered check after landing. Former
wording, preserved (#12): "none of these items bears on the analysis, its inputs or a measurement tool, so none
gates anything" — which contradicted the `.mscx` sentence two lines above it.)*

## 2. Facts established this sitting, read-only

- **Thirty-nine distinct documents among the 173 are decisions-register homes** **[checked: a search of the
  staged `DECISIONS.md` index for `cowork_*.md` names, unique names counted by hand]**. Of them, by CC's
  classification **[relayed, map report §2(c).2]**, eleven are DATED (among them `cowork_architecture_reassessment.md`,
  `cowork_adjudication_dossier.md`, `cowork_l1l4_completion_ledger.md`), five UNDECIDED, twenty-three LIVE.
- **The classification tallies** stated on the surface — roughly seventy DATED, thirty LIVE, forty-five UNDECIDED,
  twenty-odd never classified — are this side's approximate count over CC's relayed list, not derived by a tool.
- **The 173 count** is CC's, recomputed by its check tool **[relayed; check report §7 table, section 1.5]**; the
  root was not listed to count them.
- **Precedent for editing a delegation's path:** the last move replaced eleven moved names inside existing
  `ARCHITECTURE.md` sentences under the user's "fix all references", and CC reports "No delegation was added, removed
  or reworded" **[relayed; move report line 325]**. `CLAUDE.md` register rule (h) says only the user writes a
  delegation into `ARCHITECTURE.md` **[checked, line 527]**. So a move of the LIVE documents needs the user's ruling
  to cover the path replacements expressly.
- **The `CLAUDE.md` line drift is not caused by a move:** entry 192 §1 item 2 records two drifting quotes that name
  no moved file **[relayed through entry 192; checked there at `CLAUDE.md`]**.
- **The move's edit footprint for the 173 is unmeasured:** the map's expression-6 count is per citing file, not per
  target **[checked at the check report §6 table]**.

## 3. What comes next

1. **A fresh session starts the second extractions.** Entry 186 head (5): establish the next member at the progress
   record (`reading_pass/l2_slice_reading_progress.md`, "Next in the proposed order") BEFORE choosing; apply
   entry 186's Ruling 2 (name-only look-up of the first extract's file name, declared in §0) when naming the file;
   follow entry 169 §3 step by step; judge capacity before opening a paper (entry 169 cadence 3). Entry 186's
   watch line carries the owed progress update (its §4, three items) and row 46's first-extract residue.
2. **The root clean-up list waits** (§1). Nothing on it is to be acted on before the extractions are done.
3. The open point of entry 193 §1 stands.

## 4. Boot order for the next session

1. The ordinary session-start read (entry 186's Boot block: `CLAUDE.md` at its six spans, `DECISIONS.md` whole,
   `STATUS.md`, the gating answer), the 118th's bridge-fault section, the 152nd's (v)–(x), the 169th whole. Then this
   entry; entry 186 whole for the extraction work; entry 194 only where pointed.
2. Verify both refs read `5c95032de1…`. If either has moved, find out what ran before anything else.

## 5. Declared departures of this side

- **THREE SHELL COMMANDS ON THE DEVICE, AGAINST THE USER'S OPENING INSTRUCTION ("NOT bash")**, all before any
  repository content had been read through the file tools: a `find` for this entry point's file name with a
  listing of the repository root, and a second `find` for `*handoff*`. All read-only; no file changed; **listing the
  root breaks cadence 0 of entry 169**. No claim in this entry rests on their output; every fact graded [checked] was
  read afterwards through Read, Grep or a staging call. Declared to the user in the same turn. No shell command after
  that, in the container or on the device.
- One folder-access grant (`C:\s\MS`); one bridge listing of `records/cc/reports/`.
- No memory tool called; no popup, subagent, web access or task list. This entry was written in the container's
  outputs folder, which shows it in the conversation, and committed from there; no other file was sent into the
  conversation. *(★ Corrected at the user-ordered check after landing. Former wording, preserved (#12): "No file sent
  into the conversation; this entry was written in the container's outputs folder and committed from there.")*
- **Read this sitting, named:** entries 194, 193, 192, 191, 190, 189, 186 whole; the 118th at its bridge-fault
  section; the 152nd at (v)–(x); the 169th whole; `CLAUDE.md` at its six spans; `DECISIONS.md` whole (861 lines);
  `STATUS.md` whole; the gating answer (247 open, 222 gating, 25 non-gating, read at its own fields); both ref
  files; the map dispatch at lines 1–40; the check report at §7 and §8.1, with §8.2 at its heading and opening lines only *(★ corrected at the
  user-ordered check after landing; former wording, preserved (#12): "at §7 and §8")*; the map report at §1.5 and §2(c).0–§2(c).2;
  the move report at lines 176–245 and 298–337. **Not read:** every paper, every extract, the progress record,
  `FRAMEWORK.md`, `OPEN_ITEMS.md`, `ARCHITECTURE.md`, every rulings record.
- **Landing:** this entry was committed twice at its first landing (unguarded, then forced with nothing edited
  between), staged back and read whole (8,210 bytes); then, after each user-ordered check, committed twice again
  (guarded by the previous landing's modification time, then forced), staged back and proved at the corrected
  wording and at its last line. The closing size is at the closing staging result and in the conversation's closing
  report. *(★ Corrected at the user-ordered check after landing. Former wording, preserved (#12): "this entry
  committed twice (unguarded, then forced with nothing edited between), staged back, proved at its last line read
  and at size" — the first landing only.)*
- **Degradation:** the three shell commands are one lapse of attention at boot, not a listed tell. No listed tell
  counted; that is a statement about what was looked for. Handover is at a verified stop: two rulings recorded,
  nothing running, this entry landed.
