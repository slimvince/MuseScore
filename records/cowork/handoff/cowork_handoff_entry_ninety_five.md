## COWORK SESSION CLOSE (NINETY-FIFTH ENTRY, 2026-09-03 — THE CONTINUING LINE) — ★★ **THE FOURTH COMPARISON BATCH RAN AND ITS CLOSE WAS VERIFIED AT THE OBJECTS: THE SITTING RECORD (RULINGS 34–62) AND THE THREE ENTRIES ARE LANDED, THE READING FILE IS COMPLETE (§7, §8, §9 WRITTEN, THE TWO-WAY CHECK CLOSING), AND THE APPLICATION ACT IS THE FIFTH DISPATCH — OWED NOW.** ★ **THE TIP IS `b00fb69455372396ff10696524f3d4683fc38614`; THE END-STATE GUARD RUN IS ONE COMMIT BEHIND IT.** THE CURRENT ENTRY POINT; the ninety-fourth entry is superseded as entry point and stands otherwise.

You start clueless. Read this block first, then perform the ordinary session-start read: `CLAUDE.md`
whole, `DECISIONS.md` whole, `STATUS.md`, and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`). **A
single-file opening instruction is not an exemption from that read** (ratified 2026-08-29, P-1). **The
eighty-second through ninety-fourth entries remain binding except where this entry names a change**,
and every standing bar of the earlier blocks remains binding.

**On form.** Short. In conversation with the user, plain ordinary English. A decision surface is
fact-based and checked at the objects, in plain uncompressed language, every position weighed towards
the ultimate objective and towards the guiding principles, one decision per turn, the question in a
later turn (D-249, Ruling 1, Ruling 21), the recommendation and its two-axis reasoning on the surface
itself. An outgoing form that loses notated information is EXCLUDED AT THE THRESHOLD before any
weighing (Ruling 47); facts (L0 and L1) are never altered, hidden or added to (Ruling 52). **A "go on"
or "continue" is never read as a ratification.** **Check at the object before you write a fact — and
SOURCE-CHECK A DISPATCH AT THE OBJECTS BEFORE RELEASING IT, not after** (this session's counted error).

### What this session was

The session that booted on `cowork_handoff_entry_ninety_four.md`, performed the full session-start read
(`CLAUDE.md` all 1,864 lines, `DECISIONS.md` all 862, `STATUS.md`, the gating answer's 218 ids),
re-verified the tip at both ref files with the file tools — `93ab4e340387baebbff310d1fc4449b1f6762e55`,
`origin/master` equal, nothing landed since the ninety-fourth entry — and then wrote **the fourth
comparison dispatch, `cc_instruction_comparison_l0_l1_fourth_2026_09_03.md`**, to the root. Before
writing it: the guard start state was read at the object the tip commit itself wrote (population 76,
passing 66, failing 10 — the same ten — `unclassified_candidates` empty); every tracked file the
dispatch orders read was checked against the tip (the reading file, `STATUS.md` and `cowork_handoff.md`
hash-identical to their blobs by sha1 of the bridge-staged copies; the derivation and the population
artifact at their blobs; the sitting record the one modified path); the three entries and the fourth
dispatch confirmed absent from the tip at `git ls-tree` by explicit hash. No `tools/audit/*.py` is
ordered written, so the guard-runner precondition did not arise. **On the user's instruction the
dispatch was then fact- and source-checked at the objects and revised — see Errors.** The revised file
was re-read from disk through the bridge and is hash-identical to the written copy (sha256
`d5050e037f09a0f91ab949f205095aa0e9aa9f9b4c89aa89350e7f0ce9e3ed28`, 45,829 bytes). **The user handed it
to Claude Code; the first form of this entry was written while that batch ran, and the entry was then
REVISED after the batch returned**, on the user's relay of CC's close, with the close, the end-state
section and `cc_report_comparison_l0_l1_fourth_2026_09_03.md` read in FULL through the file tools and
**the whole chain verified at the objects by explicit hash** (below). This session derived nothing,
dispositioned nothing, read no ruling of 34 to 62 for content, and landed nothing in git. It did not
compact.

### ★ THE STATE YOU INHERIT

- **The tip is `b00fb69455372396ff10696524f3d4683fc38614`**, read at both ref files with the file
  tools after the batch returned; `origin/master` equal. **Re-verify at the ref files before relying on
  it.** **The chain, verified by this session at `git show --stat` per explicit hash, each parent
  read:** `93ab4e34…` (the tree met) → `25b275e5fe65f73d624cc8feb30906b23a0282f5` (Task 0 — FIVE paths:
  the sitting record +1,911 lines, the three entries, the dispatch; the membership artifact did not
  move) → `07ab5a83ce…` (§8) → `8afac6c0f4…` (§9) → `b460ea2983…` (§7, +1,093/−49 in the reading file
  only) → `0466807e96…` (the close: `STATUS.md`, `STATUS_ARCHIVE.md`, the re-aimed
  `gen_status_batch_bound.py`, `status_batch_bound.json`, `session_start_read_size.json`, the close
  section, the report) → `e6adf74d2e…` (the end state: `guard_state.json`, the read-size artifact
  regenerated, the report and close appended) → `b00fb69455…` (a SEVENTH commit, the tell correction,
  `cowork_away_returns.md` only). **The reading file's §6 is untouched, measured at the objects by this
  session**: the diff from the tree met to the §7 commit has six hunks, at the banner, §0 (three), the
  §7–§9 markers (one merged hunk at old line 12687) and §16's last bullet — none inside §6's range; and
  the stat diff from the §7 commit to the tip over the derivation, the sitting record, `CLAUDE.md`,
  `DECISIONS.md`, `ARCHITECTURE.md` and `OPEN_ITEMS.md` is EMPTY.
- **The reading file `ratification_surfaces/cowork_comparison_l0_l1_reading.md` is COMPLETE** over
  its whole population: §7 (7a the per-row resolution table — five classes, 125 distinct rows, no row
  UNRESOLVED; 7b one row per S-1…S-54; 7c the two-way check), §8 and §9 written; §10–§16 as before.
  **§7c's arithmetic was re-read by this session and is internally consistent**: 884 + 240 + 180 =
  1,304 verdicts in §6; 89 unplaceable (bare SILENT, no S-number); 1,215 placed = 884 + 240 + 91;
  757 statement-rows = 89 + 668. The one difference from §0's recorded figures (−1 on verdicts, AGREES
  and attributable) is caused at **Row 12.26** (names S-14 and S-3; grades S-14 only). **Eleven derived
  statements are named by no outgoing statement in the population** — S-10, S-11, S-12, S-17, S-21,
  S-22, S-26, S-27, S-37, S-38, S-42 — gathered at the foot of §7c; five of them are subjects the
  derivation itself marks open. **Every disposition remains a proposal; nothing is applied; Rulings
  34–62 were not read by the batch.**
- **The sitting record is LANDED** at Task 0, blob as it stood on disk (Rulings 34–62 at §3ao–§3bq
  plus the provenance tail); CC re-measured it immediately before staging and it has not been modified
  since. **The ninety-second, ninety-third and ninety-fourth entries are LANDED.** **Untracked at the
  root: THIS entry only**, which the fifth dispatch's Task 0 lands (the ninety-second entry's rule).
  **Tracked and modified on disk at this entry's close: NONE** (the fifth's A1 predicts none).
- **`STATUS.md` carries this batch's two entries at the top** (the close naming the fourth dispatch,
  then `Same dispatch, Task 0`) **and, below them, the second writing's two nameless entries**, which
  no mechanism can move — the third close's finding, now also rowed inside
  `gen_status_batch_bound.py`'s `PREVIOUS_AIMINGS`. The third writing's two entries moved to
  `STATUS_ARCHIVE.md` by the tool, the prediction holding exactly.
- **The plan stands at:** phase OPEN → derivation LANDED → comparison COMPLETE (29/29, §7–§9 written)
  → dispositions over documents 1–29: Rulings 34–62 taken; NO family left open → **the application
  act — OWED NOW, the FIFTH dispatch** → ratification (the L0/L1 split returns there) → L2, sequenced
  by Ruling 10.
- **Guard state:** measured by the batch at `e6adf74d2e…` — population 76, passing 66, failing 10 (the
  same ten), zero STOPs — after a first end-state run of ELEVEN whose eleventh (the read-size artifact,
  two characters stale from a later `STATUS.md` edit) was caused, regenerated and re-run. **That
  artifact is ONE COMMIT BEHIND THE TIP**: `b00fb694…` changed only the close prose in
  `cowork_away_returns.md` after it. Declare the fifth dispatch's start state from
  `e6adf74d2e…:tools/audit/guard_state.json` and say so; **the fifth's start-state run is what
  measures the tip**, and a red caused by the seventh commit (if any tool reads
  `cowork_away_returns.md`) is classed by its cause, not a STOP.
- **Nothing landed in git by this session.** No code opened; no measurement, golden, build or test;
  nothing under `tools/corpus/` or `tools/robust_stop/`; **no open-items row created, flipped or
  discarded; no decisions-register entry and no `D-NNN` allocated.**

### Facts established this session that the fifth dispatch needs (each checked at its object)

- **`gen_status_batch_bound.py` has FIVE authored constants** — `BASE_COMMIT`,
  `PREVIOUS_BATCH_DISPATCH`, `ACT_DATE`, `DISPATCH`, `TASK` — plus `PREVIOUS_AIMINGS`, kept rather than
  replaced. Its `moved_entries()` takes the entry naming `PREVIOUS_BATCH_DISPATCH` at `BASE_COMMIT`'s
  `STATUS.md` plus the run of dated entries immediately below saying `Same dispatch`, stopping at the
  first that says neither; `PREFIX_ADJUSTMENT = "Last updated: "` (imported from
  `gen_governing_surface_split.py`) is stripped from the base text, which must then occur exactly once
  in the live file — so **a batch writes its own entries, taking the prefix, BEFORE `--apply`**. Its
  docstring says four fields; the constant `ACT_DATE` is the fifth. Read at `93ab4e34…:tools/audit/gen_status_batch_bound.py`.
- **P-2 and P-3 are homed at `cowork_rulings_2026_08_29_ratification_sitting.md`** (the ruling that
  ratified `cowork_framework_phase_retrospective_2026_08_29.md`): P-2 *pin-to-blob as the standing
  Task 0 form, the pin ordered in the user's opening line, the declared-departure fallback kept*; P-3
  *footprint assumptions as act-lists, third-party movement graded*. **They are NOT in
  `cowork_audit_protocol.md`'s standing-clause marker population** (enrolment is on the user's owed
  list); cite them by that home.
- **The sitting record's on-disk modification is not a byte-append of the blob**: 1,911 insertions, 0
  deletions, two hunks (after the blob's line 3039, before the old §4; and after line 3146, the
  provenance tail). A prefix-hash test fails on it; numstat plus hunk headers is the check.
- The reading file's sections: §5 the closed vocabulary, §7/§8/§9 the three markers (lines 12688,
  12700, 12707 at blob `ef9c002f…`), §14 the independence record, §16 the last. Row 3.27's axis line
  reads exactly *"S-32: **AGREES** on the span half; **THE DERIVATION IS SILENT** on cost"*. §3al.4's
  two structural phrases: *"notated-versus-unfolded order is not a tie question but its own face"*;
  *"face (e) has no home for marks falling where no change point is."* The derivation re-counts to 54
  S-numbers and 17 OQ-numbers at the staged copy.

### The user-owed list

**The application act is the FIFTH dispatch, and its preconditions are now ALL MET** — a dispatch that
re-marks the ruled rows and writes the amendments of Rulings 34–62 into the derived specification,
together with the pointer-form items each ruling names (the register's route for the two
principle-level rulings, Ruling 40's #12 rule and Ruling 52's facts rule; the L2 flags of Rulings 48
(21.5), 57 (D-528/§8.6) and 60 (22.16)). Its inputs are the sitting record's §3ao–§3bq (landed) and the
reading file with §7's two-way check written (landed). **It is the writing side's to write, not to
run.** Two questions the fifth dispatch must settle on its face before Task 0, and the fourth close
leaves open: **(a)** whether the eleven derived statements no outgoing statement names (§7c's foot)
are inside the act's scope at all — they carry no ruled row, so the act as the ninety-fourth entry
frames it does not reach them, and that should be stated rather than discovered; **(b)** what the act
does with the second writing's two nameless `STATUS.md` entries — nothing, per the third and fourth
closes, and the dispatch should say so.

**Two apparatus findings of the fourth batch, reported and NOT rowed by it, put here for the user:**
**(1)** the shell-read guard ADMITTED one working-tree `git diff` without an explicit hash (the next,
naming `STATUS.md`, was denied) — a D-253 breach by CC, declared at the close's departure 7 and the
report's §6.7, and a coverage gap in the guard's own population; **(2)** the end-state guard artifact
stands one commit behind the tip (above). Whether either earns an open-items row under R3 as amended
by #10's worth test is the user's; the third close's precedent was *reported and left, no row*.

**Standing, from the ninety-second through ninety-fourth entries and unchanged:** the §3 leak repair in
the brief and §8's instance; the rename of `00_READ_THIS_FIRST.md`; the plainness repair;
`.gitattributes` for committed `.mscx` exemplars; the merged D-674 question; the three routed
`FRAMEWORK.md` findings; the corrections-batch scope; the commit-trailer form; the two candidate
bounds; the two research-paper binaries; the shell surface's answer; marker enrolment of P-2..P-5; the
artifact-inventory signature-table amendment (the standing STOP). **The eleven quarantined audit
questions stay reserved to the AUDIT**, with the reading file's §11 (complete over 29) and the rows the
rulings quarantined (1.7, 4.11, 12.12(ii), 17.13, 15.15, 29.7 and the reading file's own).

**Owed as work, none of it blocking:** unchanged from the ninety-fourth entry (CC's declared British
spellings in the reading file's own prose; the second writing's two `STATUS.md` entries that name no
dispatch and cannot be moved by the mechanism — the fourth close will report them still in place).

### Errors

**THREE COUNTED — one caught by the user, one found by CC at the object, one self-declared.**

0. **★ THE DISPATCH'S A1 LOCATOR FOR THE SITTING RECORD'S FIRST HUNK WAS THE HUNK HEADER'S START, NOT
   THE INSERTION LINE.** The dispatch said the §3ao–§3bq hunk "opens at the blob's line 3039"; that
   number is what `git diff` prints as the hunk header with its three lines of leading context, and
   the insertion itself follows §3an's last line two lines later. CC met the two-line disagreement,
   did not STOP, and settled the test at the blob's own content (§3an's closing sentence, a blank line,
   the old `## 4` heading) — the right act. **The lesson: a locator taken from a diff header is
   context-dependent; state the check by what stands at the position, or by `-U0`.** Counted because a
   stricter reading of the A1 clause as written would have STOPped Task 0 on the writing side's own
   number.

1. **★ THE DISPATCH WAS RELEASED TO DISK BEFORE IT WAS SOURCE-CHECKED AT THE OBJECTS, AND THE CHECK THE
   USER THEN ASKED FOR FOUND THREE THINGS THAT WOULD HAVE STOPPED THE BATCH.** (a) A1 said the sitting
   record was *"additions only … nothing above §3ao moved"* in a form that read as a byte-append; the
   file is an insertion inside the blob plus a tail, so the check as written would have failed at Task
   0. (b) Task 2 said *"re-aim ALL FOUR of its authored fields"*, carried from the third dispatch; the
   tool has five constants and a list, and its `--apply` STOPs unless the batch's own entries are
   written first with the prefix moved — none of which the first release stated. (c) P-2 and P-3 were
   cited by name with no home, and they are not in the protocol's marker population. All three were
   corrected in the revised file before the batch was handed over. **The lesson, stated as the rule
   it already is: the writing side's self-check item 3 — figures and premises read at their objects —
   is performed BEFORE release, over every mechanism the dispatch orders run, by reading the tool at
   the object; a rule inherited verbatim from the previous dispatch is not thereby checked.** The
   third dispatch's "four fields" was itself inherited from the second writing's finding and was
   already one short.
2. **Declared departure, not a breach:** the guard artifact, `gen_status_batch_bound.py` and the
   2026-08-29 ratification sitting were dumped at the object by `git show <hash>:path` to a scratch
   path outside the repository on the user's machine and read there with `sed`, `grep` and an
   interpreter; `git diff --numstat <tip> -- <path>` and its hunk headers were run on the user's
   machine for the sitting record (the A1 form the third dispatch itself orders); the bridge's
   directory listing was parsed with an interpreter in the container (tool output, not a repository
   read). No working-tree file was read by any shell. Declared here as the ninety-fourth entry declared
   its range query; the form is not among D-253's four named forms.

**What went right and is worth copying:** the tip, the guard state and every tracked input were
verified at the objects before a line of the dispatch was written; the sizing judgment on the
application act was taken under the entry's own *"otherwise in the next"* clause and stated on the
dispatch's face so the user could overrule it; the forward-bound tool's outcome was PREDICTED from its
own derivation and HELD exactly at the run; the user's source-check instruction was executed as a
per-claim read at the objects, each correction written into the dispatch with its source; and the
close was verified at the objects — chain, path lists, §6's untouched range, the arithmetic — before
this entry stated a single fact from it. **On CC's side, worth copying:** the §7 attribution was
resolved by enumerating the minority mechanically into classes with the carrying rule fixed first, and
the written section was parsed back out of its own git object and compared to the extraction, twice.

### The cadence for your session

0. **Boot as above, in full.** This entry file is UNTRACKED and you never land it yourself (the
   2026-08-26 role ruling).
1. **Re-verify the tip at the ref files with the file tools**; expect `b00fb69455…`. If a later
   dispatch has landed since, read its FULL close in `cowork_away_returns.md` first and verify its
   commits at the objects by explicit hash. **If a batch is running, do not write anything into the
   tree and do not write a dispatch (D-251, P-5): tell the user, and wait.**
2. **Then write the fifth dispatch — the application act.** Before writing it: declare its start state
   from `e6adf74d2e…:tools/audit/guard_state.json` and state that it is one commit behind the tip;
   check every tracked file it orders read for modification against the tip; read Rulings 34–62 at
   §3ao–§3bq **in full** for their amendments and pointer-form items (this is the first session that
   reads them for content since they were taken — read them at the landed object, not from the
   ninety-fourth entry's one-line summaries); read the reading file's §7 and §12 as the surfaces the
   act re-marks against, and §7c's foot for the eleven unnamed statements; settle the two open questions
   under *The user-owed list* on the dispatch's face; open the guard runner
   (`tools/audit/gen_guard_state.py`) before ordering any `tools/audit/*.py` written; **and
   source-check the dispatch at the objects BEFORE releasing it** (Error 1), stating every locator by
   what stands at the position (Error 0). Under P-2 the dispatch is pinned at Task 0; its Task 0 lands
   this entry.
3. **Verify at the object, not at a report** — including this entry.
4. **Close at a member boundary with nothing half-done**, and write the next entry before you close.
