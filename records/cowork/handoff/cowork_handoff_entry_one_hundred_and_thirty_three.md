## COWORK SESSION CLOSE (HUNDRED AND THIRTY-THIRD ENTRY, 2026-09-06 — THE CONTINUING LINE) — ★★ **THE SESSION THAT BOOTED ON THE HUNDRED-AND-THIRTY-SECOND ENTRY READ ROW 46 WHOLE AT THE OBJECT. TWENTY-ONE OF 36 ARE NOW READ — rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19, 20, 18, 47, 45, 48, 49, 50, 52 and 46 — AND GROUP 3 IS OPEN AT ITS SEVENTH MEMBER**, groups 1 and 2 having been closed before this session. The extract `reading_pass/extracts/chen-su-2018-functional-harmony-recognition-of-symbolic-music-data-with-multi-task-rnn.md` (**57,123 bytes**) and the rewritten progress record `reading_pass/l2_slice_reading_progress.md` (**164,854 bytes**) are on disk, each landed from a fresh container path on the first attempt and each proved at a directory listing against a `stat` taken in the same shell call that copied it; the progress record's landing was guarded by its staged copy's own `expectedMtimeMs`. ★★ **FOUR PLACES OF THE RECORD BEAR ON ROW 46 AND EVERY ONE OF THEM WAS NAMED IN ADVANCE** — the candidacy row (line 118), the slice derivation (line 81), the bibliography (line 65) and `FRAMEWORK.md` §S4(a) (lines 1553–1554) — **so this read found no place no carried item named, which is the opposite of row 52's shape and is a statement about those two rows only.** ★★ **THIS READ PRODUCES NO CORRECTED STRUCTURAL CLAIM AND NO STOP OF EITHER CLASS. WHAT IT PRODUCES IS A CONFIRMATION WHERE ROW 47's READ LEFT A CORRECTION: §S4(a)'s universal — *"Every system from Chen and Su 2018 onward predicts key, primary degree, secondary degree, quality and inversion as separate heads"* — IS TRUE AT ITS STARTING POINT.** Table 1 (page 93) and §1 (page 90) give exactly those five with 24, 21, 21, 10 and 4 classes, and §4 states the 80-D output vector they form (24 + 21 + 21 + 10 + 4 = 80, derived as a transcription check against the paper's own sentence). **Row 47's finding (3), that the same universal is FALSE of the same authors' 2021 paper, stays open with the user, is applied nowhere, and is neither restated as settled nor built on. Both ends of that sentence are now read at their own objects.** ★ **AND ONE FINDING FOR AN UNDERIVED, OPEN DESIGN POINT THAT THE RECORD DOES NOT CARRY: BPS-FH's LABEL SPACE FORECLOSES TWO OF DP-N's THREE CANDIDATE READINGS OF THE CADENTIAL SIX-FOUR BY CONSTRUCTION** — a chord is a (key, primary degree, secondary degree, quality, inversion) tuple with ten qualities and four inversions, so `Cad64` as its own category cannot be written at all and a dominant with a double suspension is not distinguishable from a plain V. **What the annotator actually wrote for such bars is NOT established here.** It is routed and applied nowhere. ★ **NOTHING RENDERS: Ruling 1's gate stands — 21 of 36 read, 15 not opened.** ★ **THE HUNDRED-AND-THIRTY-SECOND ENTRY'S LANDING LANDED RIGHT: 63,768 bytes THREE WAYS — at the harness-saved root listing, at the staging call, and by a whole read — the size the user gave.** ★ **NO BATCH IS OUT. THE TIP IS UNMOVED AT `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, read at both ref files.** ★ **THE GUARD SET STANDS AT TWELVE FAILING (relayed) AND `STATUS.md`'s TOPMOST ENTRY SAYS ELEVEN (read at the object this session) — both known, both ruled (Rulings 8 and 9), untouched; nothing ran.** ★★ **TEN ERRORS OF THE DEGRADATION RULE'S OWN FAMILY WERE COUNTED ON THIS SIDE, AND FOUR OF THEM REACHED DISK IN THIS ENTRY'S OWN FIRST LANDING. FIVE WERE MADE IN THIS SIDE'S OWN WORKING COPY OF THE EXTRACT AND CAUGHT AT ITS FACT-CHECK BEFORE ANYTHING WAS COMMITTED; ONE WAS MADE IN THIS ENTRY'S DRAFT AND CAUGHT BEFORE IT LANDED; TWO WERE FOUND WHEN THIS SIDE READ THE LANDED ENTRY BACK UNASKED; AND ★★★ TWO MORE SURVIVED THAT READ-BACK AND FELL ONLY TO A SECOND FACT-CHECK THE USER ORDERED. SO THE BOAST THE FIRST LANDING MADE, THAT NOT ONE ERROR REACHED DISK, WAS FALSE OF ITS OWN TEXT — THE HUNDRED-AND-THIRTY-SECOND ENTRY'S OWN SHAPE REPEATED — AND THIS SIDE'S OWN READ-BACK DID NOT CLEAR IT EITHER. ALL FOUR ARE CLAIMS ABOUT THIS SIDE'S OWN ACTS OR ITS OWN CHECKS: that every landing was proved at a directory listing (this entry's was proved by staging the landed copy back); that no `Edit` had failed to match (one did); that the figure sweep found nothing (it returned two hits on a numeric coincidence the extract records correctly); and a count put on this side's own landings, which the standing instruction bars outright. All four are corrected in guarded re-landings.** They are enumerated below. ★★ **THE RULE'S THRESHOLD IS TWO, SO IT FIRES: this side reports it unprompted and RECOMMENDS HANDOVER AT THIS VERIFIED STOP** — a member boundary with both reading files landed and proved, this entry landed, read back, corrected, re-landed, fact-checked again on the user's instruction, corrected again and re-landed again, and nothing half-done. **One of the ten was this side first writing that the rule did NOT fire, on a misreading of its own threshold; the reason for the recommendation and the argument against it are in the errors section.** THE CURRENT ENTRY POINT; the hundred-and-thirty-second entry is superseded as entry point and stands otherwise — including its bridge-fault section, its running total and its declared departures, none of which is restated here except where this session adds to it.

You start clueless. Read this block first, then perform the ordinary session-start read: `CLAUDE.md`
whole, `DECISIONS.md` whole, `STATUS.md`, and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`). **A
single-file opening instruction is not an exemption from that read** (P-1). **The eighty-second
through hundred-and-thirty-second entries remain binding except where this entry names a change**, and
every standing bar of the earlier blocks remains binding.

**★ THE PRACTICAL FACTS of the hundred-and-sixteenth, hundred-and-eighteenth, hundred-and-twentieth,
hundred-and-twenty-second through hundred-and-thirty-second entries are unchanged and are not restated
here (#6); read them there.** This session confirms the staging route by doing it: **every repository
file was staged through the bridge and read at its staged copy, and it made NO attempt at a device path
at all, so it tested nothing about the inherited finding that the file tools cannot read one.** **A
root listing exceeds the tool-result cap and is saved by the harness; this session read the saved
listing with `Grep` rather than opening it** — for the hundred-and-thirty-second entry's size, and for
the two commissions' names and sizes. **`reading_pass/`, `reading_pass/extracts/` and
`docs/research_papers/` were listed directly as subdirectories.** **`candidacy_upgrades.md` lives in
`reading_pass/`, not at the root.**

**★ WHAT THE THREE-PART RULE PRODUCED AT THIS ROW, STATED PER LEG RATHER THAN AS A RATIO — AND IT IS
NOT AN ARGUMENT FOR SKIPPING ANY LEG.** The IDENTITY sweep reached all four places. The FIGURE sweep,
run after the paper was read, reached **no citation of this paper at all** — **no figure of this paper
is reported anywhere in the staged tree as this paper's**, which is itself the finding that no `[FACT]`
rests on it. **It is stated that way, and not as "no figure of this paper occurs anywhere", because the
sweep DID return two hits and the first landing of this entry overstated the result — that is error
(i).** Both hits are the string *"66.65"*, both in the row 47 (2019) extract, where it is that paper's
chord-segmentation F1 on BPS-FH; it is also this paper's Table 3 Key cell for the two-task-specific-layer
model. **Two different quantities in two different papers' tables, recorded at the extract so no later
reader merges them.** The BARE-WORD sweeps reached two
extra lines in `FRAMEWORK.md` and **both are traps rather than places**: line 1737's *"The 2018
expectation study"* is candidacy row 39, and §S10 line 1968 names *"Chen, Su and Yang"*, the electric-
guitar paper the R-9 mis-filed file actually contains — **each read at its own line rather than
assumed, and neither is this paper.** The WHOLE READING of DP-A to DP-Q and DP1 to DP17 added no place.
**So on this row the legs that paid at rows 48, 49, 50 and 52 added nothing; that is a statement about
this row, it is only knowable after running them, and the next session runs all three again.**

**On form.** Plain ordinary English with the user. **NEVER use the multiple-choice widget with him.**
A decision surface is fact-based and checked at the objects, every position weighed towards the
ultimate objective and towards the guiding principles, the recommendation with the reason and the
strongest argument against it; when the facts collapse the alternatives to one, say so; before putting
a question, check whether the record already answers it. **He asks that a dispatch be sourced and
fact-checked at the objects before he runs it, and that a deliverable he is about to rule on be
fact-checked as a turn of its own before any question** — do both unasked. **A read put to him by name
is shown, not ratified; if he says nothing it stands and the next act proceeds.** Every self-invented
word is explained in plain words the first time or not used.

### ★★ THE BRIDGE FAULT — READ THE HUNDRED-AND-EIGHTEENTH ENTRY'S SECTION BEFORE YOU WRITE ANYTHING

It is not restated here (#6). **This session saw NEITHER shape. Every landing it made carried the right
bytes on the first attempt, and it made no re-landing of either reading file.** *(The first landing of
this entry said "BOTH landings it made", which is a number on this side's own acts — barred outright by
the standing instruction — and which this side's own later act, a third landing and then a guarded
re-landing, made ambiguous besides. That is error (j).)* Named rather than counted, they are: the row 46
extract, landed from a fresh outputs path and proved at **57,123** bytes
at the `reading_pass/extracts/` listing, equal to the `stat` taken in the same shell call that copied
it; and the rewritten progress record, landed from a second fresh outputs path, guarded by the staged
copy's own `expectedMtimeMs`, and proved at **164,854** bytes at the `reading_pass/` listing, equal to
the `stat` taken in the same shell call that copied it. **This entry's own landings are outside that
enumeration by the self-bounding clause below, and are not named or counted anywhere in this entry.**

**★ THIS ENTRY'S OWN LANDING IS OUTSIDE THAT ENUMERATION, ON THE CONVENTION THE HUNDRED-AND-THIRTIETH
THROUGH HUNDRED-AND-THIRTY-SECOND ENTRIES STATE AND RELAY FROM THE THREE BEFORE THEM.** An entry cannot
contain the proof of its own final landing — the same bound the record already names for a commit that
cannot carry its own hash — so **the next session proves it, by its size at the root listing and a
whole read**, which is what the hundred-and-eighteenth entry's cadence step 2 already prescribes.

**★ THE RUNNING TOTAL IS CARRIED FORWARD UNCHANGED AND IS DELIBERATELY NOT ADVANCED, ON THE
PREDECESSORS' OWN REASONING.** The hundred-and-thirtieth entry's totals stand exactly as it states them
and as the two entries since carry them forward: *seventy-one across seventeen sessions, forty-five
never-staged and twenty-six staged; or seventy, forty-four and twenty-six*, with those sessions' own
landings deliberately left out. **This session does not produce a new total**, for the reason the
hundred-and-thirty-first entry gives: a running total of landings is a number on this side's own acts,
which is the family that reached disk in two consecutive entries before it. **Nothing is lost by
declining (#12):** this session's landings are named above and each predecessor names its own, so
anyone can derive the total from the named landings whenever it is wanted, and will derive it rather
than inherit it. **Beside either reading of the total stand FIVE proven STALE landings — four on
staged paths and one on a never-staged path** — unchanged by this session.

**The fresh-outputs-path observation is unchanged and gains nothing this session that it could not have
gained anyway:** every landing went from a fresh path and each carried the current bytes on the first
attempt. **Still no cause is asserted (D-641, D-658).** The operating rule is unchanged: use a fresh
outputs path for every commit, prove every write at the listing against the immediately preceding
`stat`, and read the landed copy back when the sizes differ.

### What this session was

The session that booted on `cowork_handoff_entry_one_hundred_and_thirty_two.md`, on the user's opening
instruction to read that entry with the file tools, take its proof of landing as 63,768 bytes at the
root listing, and read what row 46 IS at `candidacy_upgrades.md`'s row 46 before staging anything. It
did both first and reported them to the user in plain text — **declaring in the same turn that reading
those two files required staging them, which is the departure from "before staging anything"** — then
on his "go ahead" the full session-start read (`CLAUDE.md` whole, 1,864 lines; `DECISIONS.md` whole,
862 lines; `STATUS.md` whole; the gating answer — 244 open, 219 gating, 25 non-gating, and the
`gating_ids` list whole; the tip at both ref files; the hundred-and-eighteenth entry whole for its
bridge-fault section), then the reading set the cadence names (the progress record whole; both
commissions whole; the row 52 extract at its banner, its two verification targets, its findings (1) and
(2), its coupling facts, its adopt-or-argue section and its heading list, for the form; the row 47
(2019) extract whole, which the progress record's carried item (d) asked for as the nearest neighbour;
`candidacy_upgrades.md` whole; the L2 slice derivation whole;
`docs/research_papers/BIBLIOGRAPHY.md` whole; `FRAMEWORK.md` at §4.2, at the L2 and L3 charters, at the
design-point opening with DP-A to DP-Q, at §14.1, at A.3, at §S4 whole, at the entanglement argument,
at §S5's L3 passage and at §S6's DP1 to DP17; `reading_pass/population.md` at its verification rows V1
to V13), then one thing:

**Read row 46 whole at the object and extracted it.** Chen & Su, *"Functional Harmony Recognition of
Symbolic Music Data with Multi-task Recurrent Neural Networks"*, the held file
`docs/research_papers/chen_su_2018_ismir_bpsfh_functional_harmony.pdf` (671,916 bytes), **eight pages,
printed 90–97**. Staged through the bridge by its matching name in the `docs/research_papers/` listing
and read as page images with the file tools; **page 93, which carries Tables 1 and 2 and Figure 2, and
page 95, which carries Table 3, were each read a second time on their own and every transcribed cell
re-checked.** The page count was **established rather than assumed**: a request for page 20 was refused
by the tool with *"PDF has 8 pages"*. The printed title, the two authors, the venue, the year and the
CC BY 4.0 licence were checked against the bibliography's row 65 on page 1 before anything was
extracted. **The extract is the record; what it produced, in one line each, is in the progress record
and is not restated here (#6).**

**Where the record bears on this row was established by `Grep` and by reading BEFORE extracting, not
inherited:** four places, enumerated in the extract, **and every one of them was named in advance** —
three by the progress record's own row-46 paragraph and the fourth by its carried item (a). The
searches actually run are listed in the extract's banner and in the progress record's provenance.

### ★★ WHAT THE NEXT READER MUST MEET BEFORE ANYTHING ELSE

**NO STOP FIRES ON THIS READ.** What fires instead is the other half of a sentence the record has been
carrying, and one datum for a design point that is open.

**(1) §S4(a)'s UNIVERSAL IS TRUE AT ITS STARTING POINT.** The sentence is *"Every system from Chen and
Su 2018 onward predicts key, primary degree, secondary degree, quality and inversion as separate
heads"* (`FRAMEWORK.md` lines 1553–1554, Appendix B, the sealed first-stage draft). **At the object:
Table 1 on page 93 lists exactly Key (24 classes), Pri. deg. (21), Sec. deg. (21), Quality (10) and
Inversion (4); §1 on page 90 names the same five in the same order in the authors' own words; and §4 on
page 93 states that the output layer is an 80-D vector carrying the classes for the five tasks.**
*(Derived as a transcription check: 24 + 21 + 21 + 10 + 4 = 80, the dimension the paper itself
states.)* **Row 47's read established the same sentence FALSE of the same two authors' 2021 paper —
that is row 47's finding (3), open with the user, applied nowhere, and NOT restated as settled or built
on here.** **So both ends of the universal are now read at their own objects: true at its first term,
false at a later one.**

**Three precisions on how that reads, none of them a correction.** *(i)* The paper carries **two**
architectures — one with a single fully-connected layer between the shared encoder and all five
outputs, and one with five task-specific layers of 512/512/512/256/128 units — so the word *heads* is
exact for the second and loose for the first, and the first is the configuration the paper's own prose
prefers. *(ii)* The paper **also** carries the separation taken one step further, as its single-task
baseline of **five wholly separate networks**, and it measures the three against each other. *(iii)*
The later-system half of §S4(a)'s sentence is correct about this paper **by omission**: it predicts no
root, no bass, no harmonic rhythm and no pitch-class set.

**(2) THE PAPER MEASURES SHARING, NOT DIVIDING, AND A DETAIL SPECIFICATION MUST KNOW THE DIFFERENCE.**
Table 3's shared-encoder model beats the five separate networks on **all seven** of its columns
(+1.42 key, +2.18 degree, +1.58 secondary, +0.44 quality, +2.09 inversion, +1.96 overall, +0.86
translation — derived, sign convention *shared minus separate*), while deepening the per-task branch
moves three of seven the other way. **Nothing in the paper measures the cost of splitting the answer
into five decisions against not splitting it.** So what an argument for DP-A's chosen *"no"* gets from
the term §S4(a)'s universal starts from is **evidence about a common representation** — which is not
the same claim.

**(3) THE COMPOSITE IS MEASURED AND THE INCOHERENCE IS NOT.** §5.2 defines the overall accuracy as
*"the segments in which the five chord function detections are all correct"* — a conjunction, reading
23.57 / 25.53 / 25.69 where key, quality and inversion individually read 57.95 to 68.48. **A
conjunction cannot separate a wrong head from heads contradicting each other**, and the paper
decomposes that residual nowhere. **Recorded BESIDE row 45's finding (1), which is a correction to what
DP-A says of a different paper, and MERGED WITH IT NOWHERE.** **No figure of this paper is among the
figures DP-A's ground cites**, established by the figure sweep, **so nothing is owed to DP-A from here
and no correction is made.**

**(4) ★ THE DP-N DATUM, WHICH IS THE THING IN THIS READ THE RECORD MOST LACKS.** DP-N — *which theory
of the cadential six-four does the label vocabulary take?* — is **NONE CHOSEN, underived and open**,
with three candidates: the tonic chord in second inversion, its own category, and a dominant carrying a
double suspension. **BPS-FH's label space decides two of the three by construction.** A chord is a
(key, primary degree, secondary degree, quality, inversion) tuple; the ten qualities are M, m, a, d,
M7, m7, D7, d7, h7 and a6; the four inversions are 0th, 1st, 2nd and 3rd. **There is no `Cad64`
category, so DP-N's second candidate cannot be written at all; and a dominant with a double suspension
is not distinguishable from a plain V, so its third cannot be marked.** What the space CAN express is
the first. **What the annotator actually wrote for such bars is NOT established here** — the paper
prints no cadential-six-four example, and the label space says only which choices it forecloses.
**This is a fact about the corpus every later member of this lineage measures on, and it bears on an
OPEN design point.** Routed to DP-N and to measurement design; **applied nowhere, and no verdict is
proposed.**

**(5) AND FOUR THINGS ABOUT THE CORPUS AND THE FITTING, EACH RECORDED AS PRINTED.** BPS-FH here is
**23** of the 32 first movements with the annotation *"an ongoing work"*, where row 47's 2019 paper
describes it as containing all 32 — **so "BPS-FH" is not one fixed set across this lineage, and a
figure reported on it carries its version and its protocol or it carries nothing.** Table 2's three
rows carry 12, 6 and 6 piece numbers against footnote 4's 23 pieces, so exactly one piece appears twice;
read at the image, it is **No. 23, in both the training and the validation set** — the testing set is
untouched, so it is not a test-set leak. The dataset carries *"29 different keys"* against a
**24-class** key label space and the paper reconciles the two nowhere. **No table in the paper prints
uncertainty of any kind and no significance test appears anywhere (#24).**

**Row 18's finding (3), row 47's two corrected structural claims, row 45's finding (1) and row 52's
corrected structural claim all stay open with the user, all applied nowhere, none touched, restated as
settled or built on by this session's work. Row 46 adds none to that population.**

### ★ THE STATE YOU INHERIT

**What in this section was read at an object this session and what was relayed, stated once:** the tip
(both ref files), `CLAUDE.md`, `DECISIONS.md`, `STATUS.md`, the gating answer, the progress record,
`candidacy_upgrades.md`, both commissions, the hundred-and-eighteenth entry whole, the slice derivation
whole, the bibliography whole, the row 47 (2019) extract whole, the row 52 extract at the sections named
above, `FRAMEWORK.md` at the sections named above, and `population.md` at V1 to V13 were READ. The
verdict counts, the guard-set figure, the untracked-file list, the continuous-pruning remark and the
"waiting on him" list are RELAYED from the hundred-and-thirty-second entry, which relays them from
earlier entries; this session did not open those earlier entries.

- **The tip** is `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`, read at both ref files with the file tools
  (`.git/HEAD` → `refs/heads/master`); nothing ran since. **No batch is out.**
- **`VERDICTS["l2"]`** 111 IN / 133 OUT / 0 UNPLACED (relayed; not re-verified). **`WITHHELD`, `EXTRAS`,
  `FROZEN`** carry no `l2` key (relayed; `STATUS.md`'s topmost entry, read this session, says the same of
  the writeback batch).
- **The reading file** `ratification_surfaces/cowork_withheld_family_l2_reading.md` stands RULED
  (`STATUS.md`'s topmost entry, read this session).
- **Task B's L2 slice: 21 of 36 read** (rows 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19, 20, 18, 47, 45,
  48, 49, 50, 52 and 46); **groups 1 and 2 are CLOSED; group 3 is OPEN with rows 45, 48, 49, 50, 52 and
  46 read and its other seven members not.** The progress record is
  `reading_pass/l2_slice_reading_progress.md` (**164,854 bytes**) — read it whole before opening any
  paper; it names the next member, carries its lettered items to row 51, and states that its row numbers
  are `candidacy_upgrades.md`'s and that `object_reads/` uses `population.md`'s numbering.
- **Untracked files of this line at writing, none landed by this side:** this entry; the
  hundred-and-thirty-second through hundred-and-ninth entries; the surface
  `cowork_l2_boot_list_surface_2026_09_05.md`; the sitting record
  `cowork_rulings_2026_09_05_l2_boot_list_sitting.md`; the two derivations of the hundred-and-eleventh
  entry; the read file `cowork_l2_score_set_read_2026_09_05.md`; the extracts of the twenty-one rows 27,
  28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19, 20, 18, 47, 45, 48, 49, 50, 52 and 46 — **row 47 having two, so
  the rows are named and no file count is asserted** — and the progress record. The standing untracked
  historical `cc_*` root population is pre-existing. This side ran no enumeration.
- **The gating answer:** 244 open, 219 gating, 25 non-gating at the session-start read; nothing ran.
- **The guard set:** 77 run, 12 failing (relayed; not re-run). **`STATUS.md`'s topmost entry says
  "ELEVEN FAILING"** (read at the object this session; Ruling 9: corrected in place by the next dispatch
  that touches `STATUS.md`, former wording preserved). **The continuous-pruning bound is unmaintained** —
  standing since the hundred-and-sixth entry (relayed).
- **The plan stands at:** … → L2's boot list RULED (nine rulings) → reads 1, 2 and 3 of §11 DONE →
  **Task B's L2 slice (36 papers) read whole at the object — 21 DONE, 15 TO GO, groups 1 and 2 closed,
  group 3 open at its seventh member** — in bounded sessions closing at member boundaries, under the
  remedial commission §3 and the original commission's §4 form, central papers owing a second
  extraction, in the verdict table's proposed order ("stated, not ruled") → **the pack-build dispatch**
  when next (D-250) → the brief → the blind deriving session → L2's comparison reading. **Beside it, the
  L0/L1 comparison reading stands at 7 of 29 documents tabulated** (relayed).

### ★★ THE GATE, RESTATED

No derivation may begin before L2's slice of Task B is in (eighty-seventh entry; Ruling 10 of the
2026-08-31 sitting; Ruling 1 of the 2026-09-05 sitting). **The slice is 36 papers, all held.
Twenty-one are read.** The gate is not lifted and cannot be until the other 15 are read, flagged
unreadable, or stop-reported.

### ★ WHAT COMES NEXT

**The next act is the next bounded reading session** — open `reading_pass/l2_slice_reading_progress.md`,
the remedial commission's §3 and the original commission's §4 form before reading anything; take the next
row's paper from `candidacy_upgrades.md`'s own row (never from a file name elsewhere), find its held file
in the `docs/research_papers/` listing, stage it by name, check its printed title, authors and version
against the bibliography's row on page 1, read it as page images, extract in the §4 form, rewrite the
progress record whole (a file this line wrote, **164,854 bytes** — read it whole first), close at a member
boundary. **Row 51 is next** — this session read its table line in `candidacy_upgrades.md` (line 123: Wu,
Nakamura & Yoshii, APSIPA 2020, held, **ADMITTED ON THE DOUBT DEFAULT** for *"The register names no
subject beyond the venue; nothing in the record settles what its method does. The default is to read."*)
and its slice-derivation line (line 86), and the bibliography's row for it is **line 70**, read whole this
session: *Wu, Nakamura & Yoshii, APSIPA 2020*, an `apsipa.org` URL, held ✓, tier **LINK**. The held file
is `docs/research_papers/wu_nakamura_yoshii_2020_apsipa.pdf` (552,527 bytes) at this session's listing.
**★ ROW 51 IS UNLIKE EVERY ROW OF GROUP 3 READ SO FAR IN TWO WAYS, AND BOTH CHANGE WHAT THE READ IS
FOR.** First, **it stands on the DOUBT DEFAULT**: no `[FACT]`, no figure, no characterisation, and no
subject even in its own register row — **so the read is not a verification, it is the derivation's own
question being answered**, and the precedent for what that can produce is row 39 in the L1 slice, where
the answer was that the method does NOT bear on the layer its row was admitted for. Second, **the
bibliography's row carries NO TITLE AT ALL** — authors, venue and year and nothing else — **so a title
match is impossible by construction and that must be said rather than reported as an identity check that
closed.** **The items carried to row 51 are in the progress record's "Next" section, lettered rather
than counted, and one of them names a trap: Nakamura and Yoshii are also two of the four authors of
candidacy row 25, which is ADMITTED and NOT READ, so a bare-word sweep on either name will reach row
25's places — distinguish them at the line, never by the name.** The pack-build dispatch is NOT next
(D-250).

**Routed in the row 46 extract and the USER's to act on or to leave:** the DP-N datum at (4) above; the
sharing-not-dividing finding and the translation column's 15.80-point gap, with the authors' own reading
attached as its bound; the composite-measured-incoherence-not finding, recorded beside row 45's finding
(1) and merged with none; the annotation protocol §3.1 prints, which is the framework's own four
entangled questions in an expert annotator's own words and which the record cites for none of them; the
authors' own named remedy for their model's discontinuities being candidacy row 10's 2017 conference
version, which the bibliography does not hold; BPS-FH growing from 23 to 32 first movements between this
paper and row 47's, so the cross-primary check is recorded as NOT COMPARABLE rather than merged; Table
2's one piece in both the training and the validation set; the *"29 different keys"* against a 24-class
key label space; the secondary-degree column carrying Table 3's largest movement and running both ways;
the explicit *'other'* class beside row 18's `X` and row 47's *'others'* and *'no-chord'*; and reference
[28] being candidacy row 2, which row 52's extract records as having been cited by three held primaries
— **the rows are named and no total is asserted.** **None of these is of the shape that is graded a
STOP, and none needs a ruling before row 51 is read.** **Shown, not ratified.**

### The user-owed list

**Closed this session by this side, and not owed to him:** the seventeenth reading session (row 46),
which is group 3's sixth member.

**Owed to him by this side: NO FINDING.** Every finding of this read is ROUTED in the extract and
applied nowhere. **The DP-N datum is the one worth his eye first if he reads only one thing**, because
it bears on a design point that is open rather than on one already chosen — but it is a datum, not a
STOP, and nothing waits on it.

**What IS put to him, and it is not a finding but a report about this side:** **the degradation rule
fires — ten errors of its named family against a threshold of two, four of which reached disk in this
entry's own first landing, two caught by this side's own read-back and two only by the second
fact-check he ordered — so this side reports it unprompted and recommends HANDOVER AT THIS VERIFIED
STOP.** The reason, the count's derivation and the strongest
argument for continuing instead are in the errors section. **Nothing waits on his answer: row 51 is the
next act either way, and this entry is written so a fresh session can take it.**

**Waiting on him, CARRIED from the hundred-and-tenth entry as later entries relay it (this session read
only the hundred-and-thirty-second and the hundred-and-eighteenth), still open, unverified this
session:** **(a)** whether the pilot's derived specification still carries its independence claim after
Ruling 85; **(b)** which branch of the filing convention (D-674) governs a ruling record; **(c)** the
close test's wording (close section and end-state artifact in one commit versus one apart); **(d)**
re-aiming the `expected_line` values in `gen_phase3_gate_partition.py`; **(e)** whether the report
template should carry the close-tip marker by design; **(f)** the fifth, sixth and seventh guard
observations not yet in OI-378's detail file. **And, from the hundred-and-twenty-fifth,
hundred-and-twenty-seventh, hundred-and-twenty-eighth and hundred-and-thirty-second entries, row 18's
finding (3), row 47's two corrected structural claims, row 45's finding (1) and row 52's corrected
structural claim** — all open, none applied, restated as settled, or built on by this session's work.
**Row 46 adds none.**

**Shown to him and standing unless challenged:** the score-set read of §11 (hundred-and-twelfth entry);
the row 27, 28, 1, 8, 3, 26, 29, 30, 5, 10, 11, 19, 20, 18, 47, 45, 48, 49, 50, 52 and 46 reads. His
opening instruction this session named row 46 as the next act and his "go ahead" released the
session-start read and the paper, which is the next act proceeding, not a ratification of any read.

**Standing, unchanged and still the user's:** the list the hundred-and-ninth entry carries under this
heading, as later entries relay it — neither the hundred-and-ninth nor the hundred-and-tenth entry was
read this session. **The eleven quarantined audit questions stay reserved to the AUDIT.** **Not acted
on:** the guard-runner parallelisation suggestion; the rendering tool's "1 entries" pluralisation defect.

### Errors

**★ COUNTED ON THIS SIDE THIS SESSION, OF THE FAMILY THE DEGRADATION RULE NAMES: TEN — (a) to (j).
FIVE — (a) to (e) — WERE MADE IN THIS SIDE'S OWN WORKING COPY OF THE EXTRACT AND CAUGHT AT THIS SIDE'S
OWN FACT-CHECK OF THAT COPY BEFORE ANYTHING WAS COMMITTED, SO NOT ONE OF THOSE REACHED DISK. ONE — (f)
— WAS MADE IN THIS ENTRY'S OWN DRAFT AND CAUGHT AT THE FACT-CHECK OF THE ENTRY BEFORE IT WAS LANDED.
★★ TWO — (g) AND (h) — REACHED DISK IN THIS ENTRY'S OWN FIRST LANDING AND WERE FOUND WHEN THIS SIDE
READ THAT LANDED COPY BACK AND FACT-CHECKED IT AT THE OBJECTS UNASKED. ★★★ AND TWO MORE — (i) AND (j) —
REACHED DISK IN THAT SAME FIRST LANDING, **SURVIVED THIS SIDE'S OWN READ-BACK**, AND WERE FOUND ONLY AT
A SECOND FACT-CHECK THE USER ORDERED. ALL FOUR ARE CORRECTED IN GUARDED RE-LANDINGS OF THIS ENTRY.**
**Two are a superlative or a comparison asserted before it was derived; eight are claims about this
side's own acts, about this side's own document, about where a passage sits, about what a governing
document says, or about a search result stated more strongly than the object supports, asserted before
they were checked.**
*(Derived: (a) + (b) + (c) + (d) + (e) + (f) + (g) + (h) + (i) + (j) = 10, and 2 + 8 = 10 — the first
kind being (a) and (b), the second (c), (d), (e), (f), (g), (h), (i) and (j); and 5 + 1 + 2 + 2 = 10 by
where each was caught. The enumeration below is what carries the meaning; the sum is of the items named
in it and of nothing else. If a later read-back of this entry or of the two reading files finds another,
this entry is re-landed with it added and the sum re-derived at this sentence.)*

**★★ AND THE BOAST THE FIRST LANDING OF THIS ENTRY MADE — that not one error reached disk — WAS FALSE OF
ITS OWN TEXT, which is exactly the shape the hundred-and-thirty-second entry counted at its own errors
(f) and (i) to (n).** The first landing said the read-back was still owed and asked the reader to treat
*"not one reached disk"* as untested of this entry. **The read-back was then performed by this side
rather than left to the next session, and it found two — both about this side's own acts, in the file
written last.**

**★★★ AND THAT READ-BACK WAS NOT EXHAUSTIVE EITHER, WHICH IS THE HARDER FACT AND IS STATED PLAINLY
BECAUSE IT BEARS ON HOW MUCH THIS SIDE'S OWN CHECKING IS WORTH.** The user then instructed that the
handover be read and fact-checked again. **That second pass found two more — (i) and (j) — both of which
had been in the first landing and had passed through this side's own read-back untouched.** So the
sequence on this entry is: written, fact-checked, landed, read back by this side and corrected, and then
corrected again only because the user ordered another pass. **Two independent checks by this side left
four errors on disk between them; the user's instruction is what removed the last two.** **The
predecessor's warning was exact, this side walked into it, and then walked into it a second time at one
remove: check the entry's claims about your OWN acts hardest of all, and do not treat one read-back as
having discharged that.**

**(a)** The extract's draft said the two-task-specific-layer model's *"Overall column is the largest in
the table"*. **FALSE at the object: Table 3's Overall column also carries the chord-symbol task's
72.71**, which is larger. Corrected to *"the largest of the three chord-function rows"*, with the reason
the chord-symbol cell is not comparable stated beside it. **A superlative over a table asserted before
the table was re-read for it.**

**(b)** The extract's draft said *"Row 46 predates all of them"* of rows 18, 19, 20 and 47 together with
rows 45, 48, 49, 50 and 52. **FALSE: row 18 is 2003 and row 20 is 2018.** Rewritten to a comparison over
the printed years of §S4(a)'s lineage members only — rows 45, 47, 48, 49, 50 and 52 — with the excluded
rows named and their years given. **A comparison asserted before it was derived.**

**(c)** The extract's draft twice called this paper *"the lineage's founding paper"* / *"the paper that
founds the lineage"*. **That is this side's own framing, not the record's**: what the record says is
that §S4(a)'s universal starts from *"Chen and Su 2018"*. Replaced at both sites with *"the term
§S4(a)'s universal starts from"*. **Counted because compact self-made framing standing in for what a
governing document actually says is the shape the rule names**; the judgment is stated so it can be
overruled.

**(d)** The extract's draft cross-referenced the annotation-protocol quotation to *"the finding at (5)
below"* when the annotation-protocol finding is **(4)**. **A claim about this side's own document
asserted before it was checked.** Corrected.

**(e)** The extract's draft carried three page references that are wrong at the object: §3.1 given as
*page 91* where its bullets run 91–92; §3.2's label definitions given as *pages 92–93* where they sit on
92; and §5.1's training regime given as *page 94* where the passage begins on 93. **Locator claims
asserted from where a passage was found rather than checked against the printed page.** All three
corrected in one pass. **Counted as ONE**, being one shape corrected in one act in one file; the
judgment is stated so it can be overruled.

**(f)** This entry's own draft said that five was *"not seven times the rule's threshold"* and that
this side therefore did **not** report the degradation rule as firing. **FALSE at the governing text,
which was then read rather than recalled: the rule's threshold is TWO** — *"When two or more appear,
report 'I have already made two of these' and recommend handover at a verified stop"*. The predecessor
entry's *"seven times the threshold"* was fourteen against two, and this side had taken the MULTIPLE for
the threshold. **A claim about what a governing rule says, asserted before it was read.** Corrected at
its site, the count re-derived to include it, and the recommendation reversed to what the rule
prescribes. **This is the sixth, it is in this entry rather than in a reading file, and it is the reason
the paragraph it sits in now says what it says.**

**(g)** This entry's first landing said, at its declared departure (iv), that **each** landing *"was
proved at a directory listing against the `stat` taken in the same shell call as the copy"*, and its
declared departure (xiii) spoke of *"listings taken to prove THIS entry's own landing"*. **FALSE of this
side's own acts: no directory listing was taken for this entry at all.** Its landing was proved by
STAGING THE LANDED COPY BACK through the bridge and reading it whole, at 55,589 bytes — which is a
proof, and a stronger one, but it is not the act the sentence named. **A claim about this side's own
acts, asserted from the shape used for the two reading files rather than checked against what was done
for this one.** Corrected at both sites.

**(h)** This entry's first landing said **"`Edit` CALLS THAT FAILED TO MATCH: NONE"**. **FALSE: one
edit to this entry's own working copy failed to match**, at the cadence step 4 passage, because the old
text was written out from memory of having written it minutes earlier and omitted the three-space
indentation the passage carries as a numbered list item. The passage was re-located with `Grep` with
context and the edit re-made against the text on disk; no content was lost. **A claim about this side's
own acts, asserted before it was checked — and made in the same entry that tells the next session a
file you wrote yourself is a file you must read back like any other.** Corrected at its site, where the
failure is now named rather than counted.

**(i)** This entry's first landing said the figure sweep *"reached none — no figure of this paper occurs
anywhere in the staged tree"*. **STATED MORE STRONGLY THAN THE OBJECT SUPPORTS, and the extract this
entry summarises already states it correctly**: the sweep returned two hits, both the string *"66.65"*
in the row 47 (2019) extract, where it is that paper's chord-segmentation F1 on BPS-FH and where this
paper's Table 3 carries the same digits as its Key cell for the two-task-specific-layer model. **What is
true is that no figure of this paper is reported anywhere in the staged tree AS THIS PAPER'S**, which is
what carries the *no `[FACT]` rests on it* finding. **A summary of a check stated more strongly than the
file it is recorded in states it — the exact thing this entry's own cadence step 4 tells the next
session not to do.** Corrected at its site. **It survived this side's own read-back and was found only
at the user-ordered second fact-check.**

**(j)** This entry's first landing said in its bridge-fault section that **"BOTH landings it made"**
carried the right bytes, and again that *"both landings went from fresh paths and both carried the
current bytes"*, and it called this entry's own landing singular. **A NUMBER ON THIS SIDE'S OWN ACTS,
which the standing instruction bars outright — "not bounded, not qualified, not 'one', not 'twice'" —
and which this side's own later acts made ambiguous besides**, the session having landed three files and
then re-landed one of them twice. Rewritten to *"every landing it made"* and *"every landing went from a
fresh path"*, with this entry's own landings made plural. **It survived this side's own read-back and
was found only at the user-ordered second fact-check.** *(Two inherited forms in the departures list
below carry numbers of the same kind — "ONE paper was opened, whole" and "three further kinds of act" —
and they are LEFT UNTOUCHED because they are this line's own convention, used by every predecessor
entry. The tension between that convention and the standing instruction is declared here rather than
resolved by this side, and it is the user's to rule if he wants it resolved.)*

**★ ONE FURTHER REWRITE THAT IS NOT COUNTED, WITH THE JUDGMENT STATED SO IT CAN BE OVERRULED.** A
sentence in the progress record's carried item (e) first read that row 51 would be *"the first sight
this slice has of that author group"*. **It is true at the object** — no paper by Nakamura or Yoshii
appears among the twenty-one rows this file's own table records as read — but it was rewritten to state
that derivation in the sentence rather than assert the result. **Not counted, because the claim was true
and the check had been made; recorded because a reader who disagrees with that line should be able to.**

**★ THE PATTERN, STATED RATHER THAN LEFT TO BE INFERRED.** It is the same family the three preceding
entries name of themselves — **the analysis holds and the bookkeeping about the analysis does not** —
and it holds again here: **not one of the ten is a claim about the paper.** Every quotation, every
Table 1, 2 and 3 cell and every derived difference was read at the page image, the two pages carrying
the tables were each read a second time on their own, and the page count was established by a refused
request rather than assumed. **What changed from the previous session, on its own recorded figures:
fourteen became ten, and six reaching disk became four — and ALL FOUR of this session's four are claims
about this side's own acts or about its own checks, where four of the predecessor's five were. The file
that carried every one of them was the handover entry, in both sessions.** **What is worse than the
predecessor's shape, and is stated rather than softened: two of this session's four survived a read-back
this side performed on its own initiative, and fell only to a second pass the user ordered.**

**★ THE RECOMMENDATION, WITH ITS REASON AND THE STRONGEST ARGUMENT AGAINST IT — AND THE FIRST DRAFT OF
THIS PARAGRAPH GOT IT WRONG, WHICH IS ERROR (f).** **The rule's threshold is TWO** — its own words are
*"When two or more appear, report 'I have already made two of these' and recommend handover at a
verified stop"*. **TEN IS ABOVE IT. SO THE DEGRADATION RULE FIRES, this side reports it unprompted,
and the recommendation is HANDOVER AT THIS VERIFIED STOP** — a member boundary with both reading files
landed and proved, this entry landed, read back, corrected, re-landed, fact-checked a second time on the
user's instruction, corrected again and re-landed again, and nothing half-done.
**The argument the other way, stated so the user can overrule the recommendation rather than only accept
it:** six of the ten were caught by this side's own checks before anything they touched reached disk;
not one of the ten is a claim about the paper; and the ratio the rule tells this side to watch — *"I
checked at this object"* against *"I recall"* — held for the analysis, every figure, every quotation and
every locator in the extract having been read at its own page or its own line. **The argument for
stopping, which this side finds much the stronger and which the second fact-check made stronger still:**
all four that reached disk are claims about this side's own acts or its own checks, made in the entry
written last; one of them was a flat statement that no edit had failed, written in the same file that
tells the next session to read back a file it wrote itself; **and two of the four survived a read-back
this side had performed unasked, so this side's own checking of its own bookkeeping cannot be relied on
to converge.** That is the tell the rule exists to catch, it fired in the same place it fired last
session, and it took a third pass to clear.

**★ ONE ACT IN THE BARRED SHAPE, BEGUN AND ABANDONED, DECLARED RATHER THAN DEFENDED.** At the
fact-check of the extract, a shell command was started in the container that changed directory into
this side's own work directory and began a Python one-liner over this side's WORKING COPY of a
repository file. **It assigned a file name to a variable and did nothing else: it opened no file, read
no byte and changed nothing**, and the edits were then made with `Edit` calls, which is the route the
record prescribes. **It is nevertheless the shape the standing bar names — DO NOT `grep` YOUR OWN
WORKING COPY OF A REPOSITORY FILE — and it is declared here, relied on for nothing.** **Being right is
not the defence**, and neither is stopping in time.

**Otherwise no shell read repository content or any staged copy of it. The shell was used for three
further kinds of act only, named rather than counted:** `mkdir` for the work directory and for each
fresh outputs path; `cp` of the progress record's staged copy into the work directory before the
rewrite and of this side's own outputs into each fresh outputs path; and `stat` of this side's own
files in the same call as each copy. **Nothing else was run in a shell.**

**★ `Edit` CALLS THAT FAILED TO MATCH, NAMED RATHER THAN COUNTED — AND THE FIRST LANDING OF THIS ENTRY
SAID THERE WERE NONE, WHICH IS ERROR (h).** Every edit to the extract and to the progress record
matched on the first attempt, because the old text in each call came from a `Read` or a `Grep` of the
file rather than from memory of it — including the two long replacements in the progress record's
"Next" section, whose old text was re-read out of the working copy after this session's earlier edits
had moved the line numbers. **One edit to THIS ENTRY's own working copy failed to match**, at the
cadence step 4 passage carrying the *"kept five of this session's counted errors"* clause: **the old
text was written out from memory of having written it minutes earlier, and it omitted the three-space
indentation the passage carries as part of a numbered list item.** **The recovery route the
hundred-and-twenty-sixth entry prescribes was followed** — the passage was re-located with `Grep` with
context and the edit re-made against the text actually on disk, never with a shell. No content was
lost. **The lesson is the predecessor's, in the file this side had written minutes earlier: a file you
wrote yourself is a file you must read back like any other.**

**Errors of previous sessions found this session: none of substance, and the inherited inconsistency is
RECORDED RATHER THAN CORRECTED, unchanged.** Everything the hundred-and-thirty-second entry reported and
this session could check is CONFIRMED at the objects: its own landing at 63,768 in three places; row
46's table line at `candidacy_upgrades.md` line 118 and the slice derivation's at line 81, both quoted
verbatim; the bibliography's row 65; the held file name and its 671,916 bytes; the row 52 extract at
67,495 and the progress record at 146,424 before the rewrite; the row 45 extract at 58,313 and the row
50 extract at 65,160; the tip at both ref files; the gating answer's 244 / 219 / 25; `STATUS.md`'s
topmost entry saying "ELEVEN FAILING"; and its statement that no verification row of `population.md`
names Chen & Su 2018, checked at V1 to V13 rather than taken from the sentence. **The inconsistency:**
the progress record states the open corrected-structural-claim population as **three** at its carried
item (d) while naming **two** for row 47 elsewhere in the same file. **It is recorded and NOT
corrected**, because which reading is right is a question about row 47's own two findings and is the
user's.

**Declared departures of this side, this session.** (i) **The two opening reads required STAGING the two
files they were reads of**, which is a departure from the user's *"before staging anything"*; it was
stated to him in the same turn as the reads, and nothing else was staged until his "go ahead". (ii) **A
shell `cp` of the progress record's STAGED copy into the work directory before its rewrite** — the
hundred-and-twentieth through hundred-and-thirty-second entries' declared departure, repeated here for
the same reason (no work-directory copy existed and re-typing 146,424 bytes from tool output is the
transcription risk the record forbids); the copy was byte-exact (146,424 at `stat`), and the rewrite was
made with `Edit` calls against that copy, **named rather than counted**: the banner's seventeenth-session
paragraph; the row 46 table row together with the counts paragraph; the "what row 46's read produced"
paragraph; the "Next in the proposed order" heading paragraph with its row-51 replacement; the lettered
items carried to row 51, replacing those carried to row 46; the shown-to-the-user block's row 46
addition; the closing group-placement sentence; the not-done paragraph's row lists; the unfetched-works
paragraph for row 46; and the provenance's session count, its seventeenth-session sentence and its
shell-exception list. (iii) **A `mkdir` and a `cp`-with-`stat` for the work directory, and a `mkdir` and
a `cp`-with-`stat` for each fresh outputs path**, all in the container, declared because they are shell
commands in the sandbox. **No shell read any staged copy and no shell read any repository object; the
one act begun in the barred shape and abandoned is declared above and is relied on for nothing.** (iv)
**`device_commit_files` was used for the extract and for the progress record, the second guarded by the
staged copy's `expectedMtimeMs`; and separately for this entry — as first written, then re-landed
guarded by the landed copy's own `expectedMtimeMs` to carry the corrections at errors (g) and (h), then
re-landed guarded again to carry the corrections at errors (i) and (j) found at the second fact-check
the user ordered.**
**The two reading files' landings were each proved at a directory listing against the `stat` taken in
the same shell call as the copy; THIS ENTRY's first landing was proved differently and the first landing
of this entry described it wrongly — that is error (g): it was proved by STAGING THE LANDED COPY BACK
THROUGH THE BRIDGE and reading it whole, at 55,589 bytes, and no directory listing was taken for it.** (v) **`Grep` through the file tools,
and NO COUNT OF THOSE CALLS IS ASSERTED ANYWHERE IN THIS ENTRY — what is stated is WHAT was searched and
WHERE, which no later act of this side can falsify.** **The searches were:** over the **harness-saved
root listing**, for the hundred-and-thirty-second entry's size and for the two commissions' names and
sizes; over the **derived gating answer**, to locate its `★_the_live_gating_answer` block; over the
**staged tree**, an IDENTITY sweep for "Chen and Su", "Chen & Su", "chen_su" and "BPS-FH"; over
**`FRAMEWORK.md`**, the bare words "Chen", "2018", "multi-task" and "multitask", and its heading list;
over **`reading_pass/population.md`** and **`cowork_reading_pass_findings_2026_08_31.md`**, "Chen", "BPS"
and "2018"; over the **staged tree** again after the paper was read, a FIGURE sweep for "72.71",
"25.69", "25.53", "68.48", "50.49", "60.04", "48.31", "57.95", "23.57", "56.91", "BLSTM" and "piano
roll", and then for every cell of Table 3 together with "86,950", "7,394" and "531 key"; and over **this
side's own working copies**, to sweep both files for superlatives, ordinals and stale numbers before
landing. **Every one of these read through the file tools, none through a shell.** (vi) No subagent was
used. (vii) **ONE paper was opened, whole** — eight pages; pages 93 and 95 were each read a second time
on their own, and the page count was established by a refused request for page 20. (viii) The handoff
entries before the hundred-and-thirty-second were NOT read except the hundred-and-eighteenth, read
whole; the sitting record, the hundred-and-eleventh entry's derivations, **the extracts of this slice
other than the row 47 (2019) one, which WAS read whole, and the row 52 one, which was read at the
sections named above for the form**, the V4 STOP memo, `population.md` outside its verification-row
table, the findings surface (searched but not read), and `FRAMEWORK.md` outside the sections named above
were NOT read. (ix) `reading_pass/continuation.md` was NOT staged this session. (x) The user connected no
new folder. (xi) The two opening reads were reported as the first turn's final response and the close is
reported as this turn's; **no multiple-choice widget was used.** (xii) **The task-list widget was NOT
used this session**, the work being one paper at a member boundary. (xiii) **The directory listings
requested are named rather than counted**: the repository root, at session start (it exceeded the
tool-result cap and was saved by the harness); `reading_pass/` and `reading_pass/extracts/` before
staging anything, to establish the progress record's and the extracts' sizes; `docs/research_papers/`
before staging the paper; `reading_pass/extracts/` again after the extract's commit; and `reading_pass/`
again after the progress record's commit. **No directory listing was taken to prove THIS entry's own
landing** — it was proved by staging the landed copy back and reading it whole — **and that staging, the
whole read that followed it, and every act taken to prove this entry's own landings are outside every
enumeration in this entry, by the self-bounding clause in the bridge-fault section.** (xiv) The bridge to the user's computer did not drop during this session. (xv)
**The running total was deliberately not advanced**, following the two preceding entries; the reason is
stated at the bridge-fault section and the landings are named there so the total stays derivable. (xvi)
**This entry was written after both reading files were landed and proved**, so it carries their landed
state. **(xvii) This entry was LANDED, READ BACK WHOLE AT THE OBJECT BY THIS SIDE WITHOUT BEING ASKED,
FACT-CHECKED AT THE OBJECTS, AND RE-LANDED guarded by the landed copy's own `expectedMtimeMs` to carry
the corrections at errors (g) and (h); THEN, ON THE USER'S INSTRUCTION TO READ AND FACT-CHECK THE
HANDOVER AGAIN, IT WAS READ AT THE OBJECT A SECOND TIME, TWO FURTHER ERRORS WERE FOUND — (i) AND (j),
BOTH OF WHICH HAD SURVIVED THE FIRST READ-BACK — AND IT WAS RE-LANDED GUARDED A SECOND TIME.** **Each
read-back was itself a proof of the landing before it: a whole read matching what was written, at the
size the staging call gives.** **The stagings of this entry's own landed copies back through the bridge,
the whole reads that followed them, and every act taken to prove this entry's own landings are outside
every enumeration in this entry by the self-bounding clause in the bridge-fault section.**

**What went right and is worth copying:** the two opening reads were done and reported before the boot
so the user saw them at once, and the staging departure was stated in the same turn; **the verification
target was established at the paper's own Table 1 and §1 before anything else was extracted, and the
80-D arithmetic was derived as a transcription check rather than trusted**; the two pages carrying
tables were each read a second time on their own and every transcribed cell re-checked; the page count
was established by a refused request rather than assumed; **the FIGURE sweep was run after the paper was
read as the carried item prescribes, and its NEGATIVE result is what establishes that no `[FACT]` rests
on this paper — a sweep that finds nothing is evidence, not a wasted act**; **the bare-word sweeps
reached two lines that look like this paper and are not, and both were read at their own lines rather
than counted as places**; **`reading_pass/extracts/` and `docs/research_papers/` were listed before
anything was called missing**, on the method warning row 48's read paid for; **the cross-primary check
was run and came back NOT COMPARABLE, which was recorded as the result rather than glossed over**; the
fact-check of this side's own working copies before landing kept five counted errors off both reading
files; **the read-back of this entry's own landed copy was performed by this side WITHOUT BEING ASKED
and it caught two more that the first landing had carried to disk** — each corrected at its site in a
guarded re-landing rather than left to a bound — **and the second fact-check the user ordered caught two
further ones that read-back had missed, which is recorded as a limit on this side's own checking rather
than as a success**; every landing went from a FRESH container path and each
was proved, the two reading files at a directory listing against a `stat` taken in the same shell call
as the copy and this entry by staging the landed copy back and reading it whole; and the handover was
written at a member boundary with nothing half-done.

### The cadence for your session

0. **Boot as above, in full.** This entry file is UNTRACKED and you never land it yourself; a later
   dispatch's Task 0 does. **If your first file read fails, stage the file through the bridge and read the
   staged copy; only if staging fails, ask the user to add the folder.** **List `docs/research_papers/` and
   `reading_pass/` as subdirectories; if you must consult the saved root listing, `Grep` it.
   `candidacy_upgrades.md` is in `reading_pass/`; the slice derivation is
   `cowork_l2_task_b_slice_derivation_2026_09_05.md` at the root.**
1. **Re-read the tip at the ref files with the file tools.** It should be `911f5f7cda…` at both. A moved
   tip means something ran that this entry does not know of — read `cowork_away_returns.md`'s last section
   before anything else.
2. **BEFORE YOU WRITE ANYTHING, READ THE HUNDRED-AND-EIGHTEENTH ENTRY'S "THE BRIDGE FAULT" SECTION.**
   Prove this entry landed: a successful whole read of it at the object is the proof; record its size from
   the root listing in your own entry. **The running total is NOT advanced by this entry and the reason is
   stated there; if you advance it, derive it from the named landings rather than adding to a number.**
3. **Read `reading_pass/l2_slice_reading_progress.md` whole**, then the remedial commission's §3 and the
   original commission's §4 form, then one of the extracts for the form (the row 46 extract is the most
   recent, at **57,123** bytes; the row 52 extract is 67,495, the row 50 extract 65,160 and the row 45
   extract 58,313 — the four are named with their sizes and no superlative is asserted). **The next act is
   row 51 — read what row 51 IS at `candidacy_upgrades.md`'s row 51 before anything else, and read that
   file whole while you are there; read the bibliography's row 70; and confirm where the record cites the
   paper with `Grep`, running the IDENTITY sweep, a FIGURE sweep once you know what figures it reports, AND
   the BARE-WORD sweeps — AND READ DP-A, §4.2, DP-C, DP-D, A.3, §S4(a), the entanglement argument and
   DP1–DP17 WHOLE, because the carried-item list is a starting point and not the population.** **★ AND
   REMEMBER WHAT ROW 51 IS: a DOUBT-DEFAULT row with no subject in its register entry and no title in its
   bibliography row. The read is the derivation's own question being answered, not a verification, and a
   placement finding of row 39's shape is a legitimate outcome.**
4. **Verify at the object, not at a report; sweep the whole document when you correct a claim; run the fact
   check twice and search for the corrected text on the second pass. Locate a section reference at the
   heading list, never from where the passage was found; LOCATE A PAGE THE SAME WAY, at the printed page
   number on the image and not at where you happened to be reading — this session counted an error of
   exactly that shape. Locate a ROW at its table, never from a file name. Check the paper's printed title,
   authors and version against the bibliography's row before extracting. Do not transcribe a table cell you
   cannot read with certainty at the image, and do not make a claim about a table's EMPHASIS unless you read
   it as carefully as you read its values. Do not reconcile two statements the paper itself leaves
   different — record both as printed. Do not cite another extract's content from a summary of it, and do
   not ATTRIBUTE a fact to a file you met only through a summary — read the file or cite where you actually
   read it.**
   **★ AND THE ONES THIS SESSION PAID FOR. FIRST: FACT-CHECK EVERY FILE BEFORE YOU COMMIT IT AND READ IT
   BACK AFTER, AND SWEEP BOTH TIMES FOR "THE FIRST", "THE ONLY", "THE WIDEST", "THE LAST", "THE LARGEST",
   "THE Nth" AND EVERY RATIO — the pre-landing sweep kept five of this session's counted errors off the two
   reading files, and two of those were a superlative and a comparison over a population the sentence
   had not been checked against. **AND THE READ-BACK AFTER LANDING IS NOT OPTIONAL AND NOT THE NEXT
   SESSION'S JOB: do it yourself, in the same sitting, on every file you land — this session's read-back
   of its own handover entry caught two more that the first landing had already carried to disk, and
   both were claims about this side's own acts.** **THE FILE THAT ESCAPES THE FIRST CHECK IS THE ENTRY
   ITSELF: check its claims about your OWN acts, and about what a governing rule SAYS, hardest of all —
   five of this session's ten were in this entry: a misreading of the degradation rule's own threshold, a
   false account of how a landing was proved, a flat "no edit failed" when one had, a search result
   stated more strongly than the extract states it, and a count put on this side's own landings.
   RE-READ A RULE BEFORE YOU REPORT ON IT, AND RE-READ YOUR OWN LOG BEFORE YOU REPORT ON YOURSELF.**
   **★ AND THE HARDEST LESSON OF THIS SITTING: ONE READ-BACK IS NOT ENOUGH. This side read its own
   landed entry back unasked, corrected two, and still left two — which fell only to a second pass the
   user ordered. Read it back TWICE, and on the second pass read the sentences you did not change on the
   first.** SECOND: WHEN A SUPERLATIVE IS OVER A TABLE, RE-READ THE
   TABLE FOR IT — this session's own error (a) was a "largest in the table" that was false because the
   table's own column carried a larger cell from a different task. THIRD: DO NOT LET YOUR OWN FRAMING STAND
   IN FOR WHAT A GOVERNING DOCUMENT SAYS — quote the sentence and name its terms, as error (c) had to be
   corrected to do.**
   **DERIVE EVERY COUNT YOU STATE, AND PUT NO NUMBER ON YOUR OWN ACTS AT ALL** — not bounded, not
   qualified, not "one", not "twice"; name what you did and where, and let the count go. **AND BEFORE YOU
   WRITE THAT THE RECORD LACKS, MISSES OR HAS NOT IDENTIFIED SOMETHING, GO AND LOOK — list
   `reading_pass/extracts/` and the directory the thing would live in.** **When you summarise a check you
   ran, do not state it more strongly than the file it is recorded in states it.** State the sign
   convention with every difference. **Do not read a difference as a result where the paper's own reported
   spread is as large as the difference**, and **check what an ablated or baseline variant actually removes
   or compares, not only that its figures are printed** — row 46's own reading turned on the difference
   between measuring SHARING and measuring DIVIDING. Label a classification or inference as the reader's
   where the paper does not state it. WHEN AN `Edit` FAILS TO MATCH, RE-READ THE PASSAGE WITH `Grep -n` AND
   `Read` AT AN OFFSET — never with a shell — **and remember that your own earlier edit may have moved the
   line breaks around it.** **AND DO NOT `grep` YOUR OWN WORKING COPY OF A REPOSITORY FILE — use `Grep`;
   this session began one such command and abandoned it before it opened anything, and even that is
   declared.** Take the `stat` of the container source IMMEDIATELY BEFORE every `device_commit_files`, in
   the same shell call as the copy where possible, COPY FROM A FRESH OUTPUTS PATH for every commit, and
   after it LIST the directory and compare the listing's size against THAT stat; a size that matches an
   older stat, or a new modification time at the old size, is the stale shape — it happens on never-staged
   paths too — stage the landed copy back, read it, re-commit guarded by its `expectedMtimeMs`.
5. **A read put to him is shown, not ratified. Do not ask him to confirm it; do not gate the next act on
   his word.** Row 18's finding (3), row 47's two corrected structural claims, row 45's finding (1) and row
   52's corrected structural claim are the exceptions in kind, and even they are SHOWN: they are written up
   and put to him, and the next act does not wait on his answer — but they are not applied, and nothing is
   built on them, until he rules. **Row 46 adds none to that population.**
6. **Close at a member boundary with nothing half-done**, and write the next entry before you close.
