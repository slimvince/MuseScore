## COWORK SESSION CLOSE (HUNDRED AND SIXTH ENTRY, 2026-09-04 — THE CONTINUING LINE) — ★★ **ONE RULING TAKEN AND RECORDED. RULING 86: L2's KEYWORD LIST IS THE FOUR-LIMB WIDENING AT FORTY-TWO TERMS — THE PILOT'S EIGHTEEN UNCHANGED PLUS THE TWENTY-FOUR OF THE OTHER THREE CHARTER LIMBS — AND THE SIX BARE WORDS `mode`, `root`, `quality`, `figure`, `applied`, `passing` ARE EXCLUDED.** ★ **THE MEASUREMENT RULING 83 OWED IS TAKEN, RETURNED AND VERIFIED AT THE ARTIFACT BY THIS SIDE — NOT ACCEPTED AT THE EXECUTING SIDE'S REPORT — SO THE SMALLEST OWED ACT OF THE HUNDRED-AND-FIFTH ENTRY IS DISCHARGED AND THE OBSTACLE THAT ENTRY RECORDED IS CLOSED.** ★ **THE TIP IS `e03fae855d1cf54fee8103dcef3e7d97adbedf6e`; `origin/master` EQUAL, BOTH READ AT THE REF FILES BY THIS SIDE THREE TIMES — AT BOOT (where they read `425c9a12b3…`, mid-flight), on the executing side's return, and again at close — AND NOTHING IS RUNNING AND NO DISPATCH IS OUT.** ★ **ONE TRACKED PATH IS MODIFIED ON DISK AND IT CARRIES EXACTLY ONE ADDED SECTION — the sitting record `cowork_rulings_2026_08_31_decision_surface_sitting.md`, ADDITIONS ONLY: §3co (Ruling 86, this session's). ONE UNTRACKED FILE OF THIS LINE EXISTS — this entry. NEITHER IS LANDED BY YOU; the next dispatch's Task 0 lands both. The root also carries a large standing population of untracked historical `cc_*` files, pre-existing, which no dispatch of this line lands.** ★★ **NOTHING IS RUNNING AND NOTHING IS HALF-DONE. TWO ERRORS ARE COUNTED, BOTH IN THE FIRST WRITING OF THIS ENTRY ITSELF, BOTH FOUND BY THE FACT CHECK THE USER ORDERED AND BOTH CORRECTED BEFORE THIS FILE WAS LANDED — AND THEY ARE TWO DIFFERENT DEGRADATION TELLS, SO THE STANDING RULE FIRED. IT FIRED AT A VERIFIED STOP, WITH THE HANDOVER BEING WRITTEN, WHICH IS THE ONE PLACE IT COSTS NOTHING. SEE "ERRORS".** THE CURRENT ENTRY POINT; the hundred-and-fifth entry is superseded as entry point and stands otherwise.

You start clueless. Read this block first, then perform the ordinary session-start read: `CLAUDE.md`
whole, `DECISIONS.md` whole, `STATUS.md`, and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`). **A
single-file opening instruction is not an exemption from that read** (P-1). **The eighty-second
through hundred-and-fifth entries remain binding except where this entry names a change**, and every
standing bar of the earlier blocks remains binding.

**★ ONE PRACTICAL FACT BEFORE ANYTHING ELSE, MEASURED THIS SESSION.** The repository folder must be
connected to the session before any file tool can reach it, and **a folder-access request made by
this side was REFUSED by the desktop application, in both path spellings.** The user connected
`C:\s\MS` himself with the folder picker, after which every read worked normally. **If your first
file read fails, ask him to add the folder rather than retrying the request.**

**On form.** Short. In conversation with the user, plain ordinary English — he ruled on 2026-08-28
that the record style stays in the records. **NEVER use the multiple-choice question widget with him**
(his ruling of 2026-09-04); a decision surface and its choice question go as plain text in the
message. A decision surface is fact-based and checked at the objects, every position weighed towards
the ultimate objective and towards the guiding principles, one decision per turn, the choice question
in a LATER turn (D-249, Ruling 1, Ruling 21); **when the facts have collapsed the alternatives to one,
say so plainly rather than presenting a menu**, and **before putting any question to him, check whether
the record already answers it.** **Check at the object before you write a fact.**

**★ AND THE FORM THIS SESSION ADDS, BECAUSE HE STATED IT IN HIS OWN WORDS.** When the choice question
is put, **it carries a weighed, fact-based RECOMMENDATION** — his instruction opening that turn was
*"go on, remember the weighed fact based recommendation."* The no-recommendation bar that a DISPATCH
places on the executing side binds CC and not this side; recommending is what this side is for, and
the recommendation is weighed towards the ultimate objective and towards the guiding principles like
every other position on the surface.

**★ THE BAR THE LAST FOUR ENTRIES HAVE EACH PAID FOR, AND WHAT THIS SESSION DID WITH IT.** It is not
enough to open the object that settles a claim you are about to write. **Ask that object the question
the decision actually turns on, and take the measurement it can give, BEFORE the surface is written.
A COUNT IS DERIVED AT THE OBJECT OR IT IS NOT WRITTEN.** This session's application of it: the
executing side reported three counts and a five-word table; **this side opened the artifact and closed
the arithmetic a second way, then opened every record of both marginal populations and read each match
in its own surrounding text** — which is what turned *"the six bare words add twenty-four entries"*
into *"twenty of those twenty-four are matched on a sense of the word that has nothing to do with
music"*, and that is the fact the ruling actually rests on.

### What this session was

The session that booted on `cowork_handoff_entry_one_hundred_and_five.md`, performed the full
session-start read with the file tools on bridge-staged snapshots, and did four things.

**It found the tip MOVED at boot and correctly did not act.** The entry it booted on declared
`5922a1f5bc…`; both ref files read `425c9a12b3…`. It read `cowork_away_returns.md`, found **no close
section for that writing**, reported the batch mid-flight, wrote nothing into the tree and waited —
which the user confirmed.

**It verified the executing side's return at the objects rather than at the report.** The report and
the dispatch were read WHOLE; the artifact was read at its population, group-term, count, per-word and
both marginal blocks; the close section and the end-state commit were confirmed, so the batch reads
FINISHED by the standing test.

**It delivered the keyword-list surface as its own turn, then put the choice question ALONE in the
following turn with a weighed recommendation.** The user ruled the recommendation: **"yes"**.

**It recorded Ruling 86 at §3co** and proved the write additions-only at the object after committing
it back.

### ★ THE STATE YOU INHERIT

- **The tip is `e03fae855d1cf54fee8103dcef3e7d97adbedf6e`**, `origin/master` equal, both read at the
  ref files with the file tools by this side **three times — at boot, on the executing side's return,
  and again at close.** **Re-verify before relying on it.** **NOTHING IS RUNNING AND NO DISPATCH IS
  OUT.**
- **★ ONE TRACKED PATH IS MODIFIED ON DISK AND IT CARRIES ONE ADDED SECTION.**
  `cowork_rulings_2026_08_31_decision_surface_sitting.md` carries **§3co (Ruling 86, written by this
  session)**. **Additions only, proven at the object AFTER the write and after re-staging the file
  from the user's machine:** §3ci stands unmoved at line 5964, §3cj at 6129, §3ck at 6226, §3cl at
  6378, §3cm at 6577 and §3cn at 6740; **§3co opens at 6902 — the line §4 formerly occupied — and §4
  now stands at 7071.** **The file is 611,698 bytes on disk**, from **598,937** before this section,
  which is the figure the hundred-and-fifth entry declared and which this side measured at the object
  before editing. **The next dispatch's Task 0 lands it, and the executing side proves additions-only
  at the object — give it the eight anchor positions above and the byte size.**
- **★ THE SPLICE ANCHOR OCCURS FOUR TIMES AND EXACTLY ONE IS A HEADING LINE**, unchanged and
  re-confirmed by use: a plain-substring edit on the bare string `## 4. What this ruling does NOT do`
  is refused. **Match the heading line TOGETHER WITH the first words of the section it opens**, which
  is unique; that is how §3co was spliced, as §3cn was.
- **★ ONE UNTRACKED FILE OF THIS LINE EXISTS — this entry.** The four earlier entries
  (`..._two.md`, `..._three.md`, `..._four.md`, `..._five.md`) and the dispatch
  `cc_instruction_l2_keyword_count_2026_09_04.md` were **LANDED by the executing side's Task 0**, and
  the report `cc_report_l2_keyword_count_2026_09_04.md` and the `STATUS.md` entry by its close commit.
  **This side did not re-derive that landing**; it is carried from the executing side's own
  enumeration and its close section — see the bound below.
- **★ THE BOUND ON WHAT THIS SIDE MEASURED OF THE TREE (#24), WIDE AND STATED.** This side ran **no**
  whole-population enumeration — `tools/audit/changed_paths.py` exceeds the bridge's command window
  from this side, measured by the hundred-and-first entry's own attempt (`rc=124` at 170 seconds) and
  **carried here rather than re-measured** — and compared **no** working copy against a committed
  object by size. What stands behind the shape above is the executing side's own post-run enumeration
  as its report and close section state it (**848 records, every one untracked, therefore zero tracked
  modifications**), plus this side's own act list: **one write, to one file, adding one section.**
  **The next Task 0 enumeration is what establishes it.**
- **HOW TO TELL A RUNNING BATCH FROM A FINISHED ONE**, unchanged: a batch commits and pushes per task,
  so a moved tip is not by itself a finished batch. **The batch is FINISHED only when
  `cowork_away_returns.md` carries its close section AND one further commit after it carries the
  end-state guard artifact.** A moved tip with no close section for that writing means the batch is
  **mid-flight**: write nothing into the tree, write no dispatch (D-251, P-5), tell the user, and wait.
  **THIS SESSION EXERCISED BOTH HALVES OF THAT TEST** — mid-flight at boot, finished on the return —
  and it works as written.
- **The plan stands at:** phase OPEN → derivation LANDED → comparison COMPLETE → dispositions: Rulings
  34–63 → application act RAN → ratification act TAKEN: Rulings 64–77 → ratification edits APPLIED, the
  specification RATIFIED → the item (d) family placements MADE → Row 4.3 RULED AND PLACED → §11 and §12
  REPAIRED ADDITIVELY → the four rows' AUDIT QUESTIONS PLACED → the `l0-l1` pack FROZEN → Ruling 81:
  L2's pack CARRIES a withheld family → Ruling 82: the criterion's GROUP TERM is A, C, D, E, F, G →
  Ruling 83: the criterion's REMAINING REACH is both non-group terms at their widest → Ruling 84: a
  derived pass may add but may not overturn → Ruling 85: the pilot's five reversed entries are ADMITTED
  → **Ruling 86: the KEYWORD LIST is RULED at forty-two terms** → **the HOME-DOCUMENT list, derived
  then put** → **the `ARCHITECTURE.md` PASSAGE list, derived then put** → the criterion COMPLETE → then
  L2's pack, and L2 sequenced by Ruling 10.

### ★★ WHAT COMES NEXT — THE HOME-DOCUMENT LIST, THEN THE PASSAGE LIST

**No dispatch is out and nothing is waiting to return. The next act is a derivation by this side.**

**The home-document list.** Ruling 83 cut it at its widest and its members are not ruled. It names
which DOCUMENTS' entries the criterion picks.

**★ AND MUCH OF ITS DERIVATION IS ALREADY DONE AT §3cl — READ THAT SECTION WHOLE BEFORE YOU DERIVE
ANYTHING. This side read it at the object at the close of this session and states its contents here as
verified, not carried.** §3cl already holds: **(a)** the distribution of the pilot's 75 candidates by
register group, summing to 75, 44 inside the six swept groups and 31 outside; **(b)** where those 31
are written down, by home document, summing to 31 — `cowork_phrase_boundary_design.md` 8,
`ARCHITECTURE.md` 5, `cowork_layer6_grouping_design.md` 4, `docs/scoring_model.md` 3,
`cowork_layer5_function_design.md` 3, `cowork_layer5_engagement_design.md` 3,
`cowork_voiceleading_axis_design.md` 2, and three documents at 1 each; **(c)** the same across all 290
unswept register rows, summing to 290, whose two largest homes are `CLAUDE.md` at 68 and
`ARCHITECTURE.md` at 55 — **neither of which is an analysis document, which is why a home-document
term reaches far less L2 material than its raw size suggests**; and **(d)** the measured price of the
one member Ruling 83 named — `cowork_layer5_engagement_design.md`, the home of **D-383**, the one
withheld entry with no honest keyword route — which is the home of 8 register rows of which 4 are
group-G entries the group term already sweeps, **so naming it adds 4 or fewer.**

**★ THE BOUND §3cl PUTS ON ITS OWN FIGURES, AND IT IS THE FIRST THING YOUR DERIVATION OWES (#24).**
Every one of those figures counts REGISTER rows. The criterion sweeps the **design-intent class, 244
of 477**, and **§3cl states in terms that it did NOT derive which of the 290 unswept rows are
design-intent**, so each figure is an UPPER bound in a ratio nobody has derived. **Deriving that ratio
at the objects is what turns §3cl's distribution into a home-document surface**; it is the measurement
this decision turns on and it is not taken.

Derive it at the objects first, then put the list to the user as its own self-contained list with its
numbers beside it, the choice question in the turn after, with a weighed recommendation.

**Then the `ARCHITECTURE.md` passage list**, the same way, one decision per turn — which passages are
named, and located by their own text on every run. §3cl's stated reason for a passage term rather than
a document term is at the object: `ARCHITECTURE.md` is the home of **131** register rows, against 35
for `docs/scoring_model.md`, so naming it as a document would sweep it wholesale.

**★ ONE QUESTION THE KEYWORD RULING DELIBERATELY LEFT TO THESE TWO LISTS, AND IT IS THE FIRST THING TO
CHECK WHEN YOU DERIVE THEM.** Ruling 86 excluded the six bare words at the price of four entries that
DO match a real musical sense — **D-206, D-222, D-406 and D-509** — and the surface's third supporting
fact was that all four remain reachable by these two terms. **Their homes, read at the `DECISIONS.md`
INDEX this session:** D-206 → `ARCHITECTURE.md`; D-222 → `docs/scoring_model.md`; D-406 →
`cowork_progression_schema_dictionary.md` §1; D-509 → `cowork_progression_schema_design.md` §4.3.
**Whether the ruled home-document and passage lists actually reach them was NOT decided and NOT
checked.** If your derived lists do not reach them, **say so to the user in terms** — the ground of
Ruling 86 would then be weaker than it read, and he is owed that.

**Do not author a verdict, do not withhold anything, and do not touch the generator** until the user
has ruled the remaining membership. **`tools/audit/gen_derivation_boot_pack.py`'s committed `KEYWORDS`
tuple and `CRITERION` table are UNCHANGED on disk** and stay so until a dispatch changes them under
Ruling 86; the measurement injected its lists into the imported module in memory and wrote nothing to
that file.

### Facts established this session that the next session needs (each checked at its object)

- **★ THE MEASUREMENT, AT THE ARTIFACT `tools/audit/l2_keyword_count_measurement.json`, WHICH IS THE
  ONE HOME OF EVERY FIGURE (D-431).** Over the **244** DESIGN-INTENT entries of the sort artifact's
  411: the ruled group term alone picks **130**; the pilot's eighteen pick **161**, which is **31**
  beyond the group term; the four-limb widening picks **177**, **47** beyond; that widening plus the
  six bare words picks **201**, **71** beyond. **As steps: the widening adds 16 the pilot's eighteen
  do not reach, and the six bare words add a further 24.**
- **★ THE ARITHMETIC WAS CLOSED A SECOND WAY BY THIS SIDE.** The union of the identities the pilot's
  eighteen reach outside the six groups, taken from the artifact's own per-word table, is exactly
  **31** — the figure the three-list block independently reports. The two marginal populations carry
  **47** and **24** identities, and the 24 are exactly the identities the executing side's report
  names, one for one.
- **★ WHAT THE COST IS MADE OF — THIS SIDE'S OWN READING, MARKED AS A READING AND NOT A MEASUREMENT.**
  Of the **16** the widening adds, **14** match a real musical use and **2** are accidents (`tonic`
  inside *Diatonic-functional* at D-131, inside *diatonic* at D-398). Of the **24** the six bare words
  add, **4** match a real musical sense and **20** do not — **11** matched inside a longer word
  (`mode` inside *model* at D-229, D-389, D-440, D-441, D-442, D-443, D-444, D-447, D-448, D-503, and
  inside *modest* at D-492) and **9** in the ordinary non-musical sense (`applied` as *"was applied"*
  at D-223, D-393, D-421, D-470, D-471, D-542; `figure` as a reported number at D-180, D-182; `mode`
  as *"failure mode"* at D-544). **It is a judgment about which sense of a word fired, taken by
  reading each match in its own surrounding text; it is not a mechanical count.**
- **★ SIX OF THE FORTY-TWO RULED TERMS MATCH NOTHING IN THIS POPULATION** — `change-point`,
  `harmonic rhythm`, `evidence ranking`, `chord-tone assignment`, `elaboration relation`,
  `figured bass`. Inert as things stand, ruled in nonetheless.
- **★ THE MEASUREMENT'S OWN BOUND, BINDING ON EVERY FIGURE ABOVE (#19, #24).** It counts CANDIDATES
  and not withheld entries — a candidate carries an authored verdict and a candidate ruled OUT is
  still rendered into the pack. **The home-document and passage terms are EMPTY in every criterion
  measured**, so every count is a count for the criterion **as far as it is ruled**.
- **★ THE MATCHER MATCHES INSIDE LONGER WORDS AND THIS IS DOCUMENTED BEHAVIOUR, NOT A DEFECT
  FINDING.** The keyword branch of `candidates()` carries a comment saying so in terms and emits
  `in_context` for exactly that reason, so every such match is visible and challengeable. **Ruling 86
  takes no position on it and asks for no change to it.**
- **★ §3cl WAS READ WHOLE AT THE OBJECT AT THE CLOSE OF THIS SESSION, AND TWO THINGS THIS ENTRY'S
  FIRST WRITING HAD CARRIED FROM A SUMMARY ARE NOW VERIFIED THERE.** The clause Ruling 86 discharges
  stands verbatim at §3cl: *"IT DOES NOT MEASURE WHAT THE WIDENED KEYWORD LIST COSTS, AND THAT IS OWED
  BEFORE THE KEYWORD LIST IS RULED."* And §3cl's own *What was decided* names
  `cowork_layer5_engagement_design.md` as a member of the home-document term, *"as the home of the one
  withheld entry with no honest keyword route"* — that entry being **D-383**, homed there at §3.2.
- **★ RULING 86's WEIGHING IS THE DIRECT APPLICATION OF §3cl's OWN CORRECTED READING, AND THAT READING
  IS NOW VERIFIED AT §3cl RATHER THAN INFERRED.** §3cl records, as the correction that decided Ruling
  83: **being made a candidate is not being withheld** — a candidate ruled OUT is rendered into the
  pack — so **the cost of a wider term is the labour of authoring more verdicts and the risk that an
  authored verdict is wrong**, in its own words *"a hundred verdicts written carelessly protect the
  objective worse than seventy-five written carefully. A pack is not over-stripped by a wide criterion;
  it is over-stripped by a wrong verdict."* **Ruling 86 excludes the six bare words on exactly that
  ground**, twenty of the twenty-four they buy being matched on a non-musical sense, and this side
  reached it independently from the artifact's own stated bound before verifying it here.
- **★ THE EXECUTING SIDE'S THREE DECLARED DEPARTURES, READ AT ITS REPORT AND ITS CLOSE SECTION, NONE
  OF WHICH MOVES A FIGURE.** **(i)** The dispatch's own Task 0(a) command was a `python -c` carrying a
  literal repository path — a form the shell-read guard DENIES under D-253 as widened 2026-08-08 — so
  the anchors were taken with the file tools and the byte size at the git object, and additions-only
  was proven a second way (773 added, 0 deleted between two explicit blob hashes). **A dispatch of
  this line must never again write that command form.** **(ii)** Task 0's heading said *five paths*
  where its own enumeration named six; the six enumerated paths were committed. **A dispatch's heading
  and its enumeration must agree.** **(iii)** An eleventh guard red appeared at the end state —
  `gen_session_start_read_size.py --check`, stale because the batch's own ordered `STATUS.md` entry
  grew the session-start read, the same mechanism the preceding batch established for the same tool.
  Cause established at the tool, artifact regenerated, guard set re-run: **end state 76 run, 10
  failing, 4 not run, 16 historical, zero STOPs — exactly the ten inherited.**

### The user-owed list

**CLOSED this session:** the **keyword list** — settled by Ruling 86 at forty-two terms, the six bare
words excluded. **The measurement Ruling 83 declared owed before the list is ruled is TAKEN**, so the
hundred-and-fifth entry's recorded obstacle is closed and its dispatch route is spent.

**Waiting on him, CARRIED and still open:** **(i)** whether the pilot's derived specification still
carries the independence claim it was meant to carry, given that its deriving session was short five
entries he had ruled admitted — **Ruling 85 sharpened this rather than answering it, and neither that
session nor this one has examined it**; **(ii)** **which branch of the filing convention (D-674)
governs a RULING RECORD** — the convention was read at its home by an earlier session and does not
decide it, its own rule for that case being that it STOPS to the user. **It was moot again this
session**, §3co being a pure addition and no landed record's body needing correction. **It will not be
moot the first time a landed ruling record's body actually needs correcting.**

**Then in the plan:** the home-document list; then the `ARCHITECTURE.md` passage list; then the
criterion complete; then L2's pack, and L2 sequenced by Ruling 10, with the three L2 flags (Rulings
48, 57, 60) waiting at the reading file's §10 and OQ-1's remainder at L2's surface (Ruling 70).
**That last sentence is CARRIED VERBATIM from the hundred-and-fifth entry, which carried it from the
hundred-and-fourth and that from the hundred-and-third, and no part of it was verified at its object
by this side.**

**Standing, unchanged and still the user's:** the two apparatus findings of the fourth batch; the
register repair's STOP leaving the suspension list empty since 2026-08-28 while every ruling since
names it as the route (**Ruling 86 among them**); the §3 leak repair; the rename of
`00_READ_THIS_FIRST.md`; the plainness repair; `.gitattributes` for committed `.mscx` exemplars; the
merged D-674 question; the three routed `FRAMEWORK.md` findings; the corrections-batch scope; the
commit-trailer form; the two candidate bounds; the two research-paper binaries; the shell surface's
answer; marker enrolment of P-2..P-5; the artifact-inventory signature-table amendment (the standing
STOP). **The eleven quarantined audit questions stay reserved to the AUDIT.**
**★ THE BOUND ON THAT STANDING LIST (#24).** It is CARRIED from the hundred-and-fifth entry, which
carried it from the hundred-and-fourth and that from the hundred-and-third. **This session verified
NOT ONE of its members at its object.** Every member is carried unverified.

**★ ONE STANDING ITEM GAINS A SECOND MEMBER, RECORDED HERE RATHER THAN LEFT.** `STATUS.md`'s
continuous-pruning bound — that the file keeps only the latest batch's entries — is **unmaintained**:
the executing side declared that the file already carried two batches' entries on arrival and that its
own dispatch's scope clause forbade moving them out. **Counted at the file by this side at close: it
now carries FIVE dated entries from THREE batches** — one from the keyword-count batch, two from the
`l0-l1` freeze batch, and two from the 2026-09-02 comparison batch. It is apparatus and gates nothing;
it is named so it is not rediscovered.

### Errors

**TWO COUNTED, BOTH IN THE FIRST WRITING OF THIS ENTRY, BOTH FOUND BY THE FACT CHECK THE USER ORDERED
AT THE CLOSE, AND BOTH CORRECTED BEFORE THIS FILE WAS LANDED.** **Nothing in the decision surface, in
the choice question or in Ruling 86 itself was contradicted by any object** — every figure in those
was read at the artifact this session. Both errors are in this handover entry's own account of the
state, which is the document a session with no memory has to trust.

**COUNTED ERROR 1 — a count asserted from memory of a file, never derived, and wrong even against the
version this side had read.** The first writing said `STATUS.md` carried *"three dated batch
entries"*. **It carries five, from three batches** — counted at the file at close. At boot this side
had read it whole and it carried four from two batches; the executing side then added one. So the
figure was not the boot figure, not the close figure, and derived from nothing. **It is the tell the
standing rule names first: asserting a count never derived.**

**COUNTED ERROR 2 — a claim carried from a summary when the source was open in the same file this side
had just edited.** The first writing said of the two remaining lists *"neither list is derived yet and
this side did not derive either"*, carried verbatim from the hundred-and-fifth entry. **§3cl already
holds a substantial derivation of the home-document term** — three distributions that each close, one
named member and that member's measured price — and §3cl sits at line 6378 of the very file into which
this session spliced §3co. **It is the second tell: citing a summary instead of the source.** The
correction is not cosmetic: it changes what the next session's first act actually is, from deriving
from nothing to extending a derivation that exists and deriving the design-intent ratio §3cl declares
it did not take.

**★ THE STANDING DEGRADATION RULE FIRED, AND IT IS REPORTED IN THE FORM THE RULE ASKS FOR.** Two tells
appeared, and they are two DIFFERENT tells rather than one shape twice — which is the condition, and
it is why this does not read like §3cn's single counted error. **I have already made two of these.**
The rule's own remedy is a handover at a verified stop, and **that is exactly where it fired**: the
work was finished, the ruling recorded and proven additions-only at a fresh snapshot, nothing running,
and this entry being written. **It cost the record nothing because the user ordered the fact check
before the file was landed.** The bearing on what follows: **both errors are in the state description
and neither touches Ruling 86 or its ground**, so the ruling stands as recorded — but a next session
should treat this entry's *state* and *user-owed* sections with the same suspicion it would give any
report, and re-verify at the objects rather than at this file.

**Declared departures of this side.** **(i)** No shell was used for any git object query and **no
working-tree file was read for its CONTENT by any shell**; every read of every file went through the
file tools on bridge-staged snapshots. **(ii)** ONE write to
`cowork_rulings_2026_08_31_decision_surface_sitting.md`, performed by editing a bridge-staged copy and
committing it back under a modification-time guard, **accepted**, so the file had not changed
underneath this side between read and write; **the file was then re-staged from the user's machine and
the anchor positions and byte size taken at that fresh snapshot**, which is a stronger proof than the
previous session took. **(iii)** ONE sandbox shell command in the container, copying the edited file
into the folder the bridge delivers from and reporting its size and line count — **the only shell use
of this session, and it read no working-tree file.** **(iv)** The choice question carried a weighed
recommendation, at the user's own instruction in the turn that asked for it. **(v)** This file is
written by this side and is UNTRACKED; the next dispatch's Task 0 lands it.

**What went right and is worth copying:** the moved tip at boot was met by reading
`cowork_away_returns.md` before anything else and by writing nothing, which is exactly what the
inherited cadence ordered; the executing side's report was verified at the artifact rather than
accepted, and the arithmetic closed a second way; the reading that the ruling actually turns on — what
the twenty-four are made of — was taken at the objects BEFORE the surface was written; the surface said
in terms that the pilot's eighteen were already closed by Ruling 83 rather than presenting them as a
live option; the question was put alone, in its own turn, with the recommendation the user asked for;
and additions-only was proven at a fresh snapshot taken from the user's machine after the write.
**And the fact check the user ordered on this entry was run against the objects rather than against
the entry's own plausibility, which is what found both counted errors** — one of them a correction
that materially changes the next session's first act.

### The cadence for your session

0. **Boot as above, in full.** This entry file is UNTRACKED and you never land it yourself. The
   sitting record's §3co is a TRACKED modification and you never land that yourself either. **If your
   first file read fails, the repository folder is not connected — ask the user to add it.**
1. **Re-verify the tip at the ref files with the file tools**; expect `e03fae855d…` at both.
   **NOTHING IS RUNNING AND NO DISPATCH IS OUT**, so a MOVED tip means something happened this side
   does not know about: read `cowork_away_returns.md` before anything else, and if there is no close
   section for that writing, write nothing into the tree, write no dispatch (D-251, P-5), tell the
   user, and wait.
2. **READ §3cl WHOLE FIRST** — it already holds three closing distributions, one named member and
   that member's measured price, and it declares the one measurement it did not take: which of the 290
   unswept register rows are design-intent. **Take that measurement**, then put the HOME-DOCUMENT list
   to the user as its own self-contained list with its numbers beside it, the choice question in the
   turn after, carrying a weighed recommendation. **Check whether the list reaches D-206, D-222, D-406
   and D-509, and say so either way** — Ruling 86's third supporting fact was that it might.
3. **Then the `ARCHITECTURE.md` PASSAGE list**, the same way, one decision per turn.
4. **Verify at the object, not at a report** — including this entry, and including anything the
   executing side writes. **Ask the object the question the decision turns on, DERIVE every count at
   the object, and OPEN THE NEIGHBOUR before you generalise from one section's wording.**
5. **Close at a member boundary with nothing half-done**, and write the next entry before you close.
