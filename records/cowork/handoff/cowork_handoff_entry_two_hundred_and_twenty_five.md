# Cowork handoff entry 225 — 2026-09-21

**The current entry point.** Entry 224 is superseded as entry point and stands otherwise. Grades as
in entries 210 and 219 to 224: **[checked]** = opened or measured at the object by this sitting;
**[relayed]** = not.

## 0. State at close — THE BATCH RAN AND STOPPED; THE TWO REFS DISAGREE; TWO RULINGS WERE TAKEN

- **THE REFS DISAGREE, AND THAT IS THE FIRST THING A SUCCESSOR MUST CARRY.** `.git/refs/heads/master`
  reads **`7d7291f401d05052240d76078db175ce160e8b91`** and `.git/refs/remotes/origin/master` reads
  **`ef4fad940d806edf8f84eb9a895f88d70a9bbcf2`**, both read with the file tools at this sitting
  **[checked]**. The commit is **unpushed**: `records/cc/instructions/cc_instruction_l2_withheld_documents_2026_09_21.md`
  puts its push at §6(b), and the batch stopped before reaching it. **A dispatch whose Task 0 is
  written in the usual *"both refs must read `<hash>`"* shape will STOP on this.** **No commit to git
  was made by this side.**
- **THE DISPATCH RAN.** It stopped at its own 5(e), the hazard it named: `tools/audit/gen_guard_classification.py`
  STOPs. §6 was not reached, so its second commit was not made and nothing was pushed. Its report is
  `records/cc/reports/cc_report_l2_withheld_documents_2026_09_21.md`, **39,332 bytes at this sitting's
  staging result and NOT at a directory listing** — `records/cc/reports/` WAS listed by this sitting,
  but **before that report existed**, so its listing does not carry this figure.
  *(★ CORRECTED AT THE CHECK, §11. FORMER: "that directory's listing does not overrun the tool and was
  taken, but the size was read at the staging call" — true of the tool and of the listing, and silent
  about the listing predating the file, which is the only reason the figure is not a listing figure.)*
- **TASK 0's COMMIT CARRIED ALL SIX INTERIM CARRIERS, ENTRY 224 INCLUDED** — the two ruling records
  of 2026-09-20, entries 222, 223 and 224, and CC's boot-pack close report. **[relayed from CC's §1(f)
  and §6; this side ran no git command and took no enumeration — it has no shell.]** So the
  uncommitted population entry 224 §0 names is **changed**, and a successor must not carry that list
  forward unchecked.
  *(★ CORRECTED AT THE CHECK, §11. FORMER: "CARRIED FIVE OF THE SIX INTERIM CARRIERS PLUS ENTRY 224"
  — entry 224 IS the sixth member of 0(f), so the heading contradicted its own list of six.)*
- **Uncommitted on disk at the close**, named rather than counted, **[all relayed from CC's own
  enumeration at its §6; this side enumerated nothing]**: `STATUS.md`, `STATUS_ARCHIVE.md`,
  `tools/audit/claude_md_finer_archive.json` (still held back, its modification's cause still
  established by nobody), `tools/audit/defense_share.json`, `tools/audit/gen_guard_state.py`,
  `tools/audit/gen_status_batch_bound.py`, `tools/audit/session_start_read_size.json`,
  `tools/audit/status_batch_bound.json`, the new `tools/audit/gen_l2_withheld_documents.py` and
  `tools/audit/l2_withheld_documents.json`, the dispatch itself, and the standing untracked
  population entry 224 §0 names. **Beside them, CC's own report**, written by that batch and
  uncommitted because §6 was not reached; **and this sitting's own three:**
  `records/cowork/rulings/cowork_rulings_2026_09_21_decision_surface_form_sitting.md` (15,410),
  `records/cowork/rulings/cowork_rulings_2026_09_21_l2_withheld_documents_sitting.md` (14,581) —
  both figures at this sitting's staging results **[checked]** — and this entry.
  *(★ CORRECTED AT THE CHECK, §11. FORMER: "**Plus three this sitting is responsible for:** CC's
  report, and this sitting's two ruling records … and this entry." — a count put on this side's own
  acts, which entry 169's cadence 6 forbids; it named four members under the word three; and it made
  CC's report this sitting's responsibility, which it is not.)*
- **WHAT STANDS WITH THE USER.** *(a)* **The one question that stood — L2's withheld documents — was
  ruled** (§3), and this sitting put no other question to him. *(b)* **One act is DIRECTED and not yet
  begun**: the consolidation of the decision rules (§7 item 1). *(c)* Carried and still undecided:
  whether row 8's second extract is renamed (entry 215 §3 item 3), **whose question this is still not
  established**, entry 215 not having been opened by this sitting either — **so whether anything
  awaits him is not settled by this entry.**
  *(★ CORRECTED AT THE CHECK, §11. FORMER: "**Nothing awaits an answer.**" — wider than anything
  established and refuted by (c) three lines below it, which is the very defect entry 223 §11 caught
  in its own §0 and corrected there.)*

## 1. What this sitting did

1. **Boot in full**, per entry 224 §6, **with no departure from it** (§8).
2. **Established that a guard-set run had just happened**, before any report existed — at a single
   artifact's modification time and at the guard table that writes it (§2). **That the runner was the
   dispatch is an inference and was not established**, which §2 states and this line formerly did not.
   *(★ CORRECTED AT THE CHECK, §11. FORMER: "**Established that the dispatch was running**" — a claim
   §2's own closing sentence refutes.)*
3. **Read CC's report whole and verified it at the objects**, finding one false claim in it (§2).
4. **Put L2's withheld documents to the user and took his ruling** (§3).
5. **Took a second ruling, on the form of a decision surface itself**, given unprompted after this
   side misapplied D-658 (§3, §5).
6. **Wrote both ruling records and landed them; the bridge fault fired on one** (§4).

## 2. What was established at the objects, and the one thing CC got wrong

**THE BATCH WAS FOUND RUNNING BEFORE IT REPORTED, AND THE ROUTE IS WORTH CARRYING.** Entry 224 §6
item 3 says to establish whether the dispatch has run, at CC's report and at the objects, and not at
the tip alone. At that point there was no report and the tip had not moved. What settled it was
**`tools/audit/changed_paths_establishment.json` carrying a modification time about a minute before a
device-info call** — and then, at the objects: `tools/audit/changed_paths.py` writes that artifact
**only** under its `--establish` mode, and `tools/audit/gen_guard_state.py`'s authored list carries
`tools/audit/changed_paths.py` with `["--establish"]` and says in its own words that it *"has no
verify-only mode, and writes its establishment artifact on every run"* **[both checked]**. Its two
neighbours in that list run `--establish --check`, which compares without writing — and their
artifacts' modification times had not moved **[checked]**. **One artifact moving and those two not is
the fingerprint of a guard-set run**, which is what the dispatch's 0(e) orders. **Who ran it was not
established** and is an inference from the timing.

**WHAT WAS VERIFIED OF THE REPORT, and the bounds of each:**

- **The ruled count, by TWO independent routes.** The artifact `tools/audit/l2_withheld_documents.json`
  was opened at the file; its identity list, per-document lists and per-document counts were
  re-derived by this side rather than taken from its own `counted` block, and they close
  **[checked]**. Then, at `tools/audit/gen_derivation_boot_pack.py`, the `VERDICTS["l2"]` block was
  bounded by reading **both** its ends — it opens at line 1364 and closes at 2813 — so no other
  subject's rows fall inside it, and the `VERDICT_IN` markers within it were counted at the file
  **[checked]**. The two routes agree.
- **The ruled figure at its own record.** §4 of `records/cowork/rulings/cowork_rulings_2026_09_05_l2_withheld_family_sitting.md`
  records the family as 111 IN, 133 OUT, 0 UNPLACED **[checked]**. Ruling 3 of that record is executed
  at the table: `D-453` reads `VERDICT_IN` and `D-535` reads `VERDICT_OUT`, read at their own lines
  **[checked]**; the artifact's list carries D-453 and not D-535.
- **The homes, against the register.** For each of the twenty-one documents, at least one identity
  assigned to it was looked up in `DECISIONS.md`, read whole at boot. Every one matched. **This is a
  sample of one per document and not a sweep of all 111**, and the bound is carried into the ruling
  record.
- **The mechanism of withholding, read at the tool** rather than taken from the report: `leak_strings`
  assembles the withheld identities, the withheld document names and the literal `ARCHITECTURE.md`;
  `leaks_in` additionally flags any `docs/` or `src/` path through `PATH_LIKE`; and
  `cross_reference_additions` adds to the withheld set every DESIGN-INTENT entry whose title,
  verbatim, plain, rationale or status_source names a withheld identity **or a withheld document**,
  one pass and not transitive **[all checked]**.
- **Two tools carry the SAME modification times after the batch as in this sitting's own pre-batch
  listing** — `gen_derivation_boot_pack.py` and `gen_guard_classification.py` **[checked]**. That is
  what supports CC's statement that neither was edited; **an unmoved modification time is strong
  evidence and not a proof**, and no stronger check was run.
- **Task 2's enrolment landed without disturbing what is above it**: the new entry sits at line 845 of
  `gen_guard_state.py`, and the two pre-existing tools remain at lines 228 and 240 — the same line
  numbers as in this session's pre-batch copy **[checked]**.

**★ THE ONE FALSE CLAIM IN CC's REPORT, AND IT WAS LOAD-BEARING FOR THE DECISION.** Its §2 states that
`ARCHITECTURE.md` and `docs/scoring_model.md` *"are governing surfaces that the boot pack renders as
MEMBERS, so what the user rules here bears on what a deriving session can be given at all."*
**`MEMBERS` opens at line 254 of `gen_derivation_boot_pack.py` and carries six members** whose sources
are the phase-definition surface, `CLAUDE.md`, `cowork_design_doc_template.md`,
`cowork_audit_protocol.md`, the generated design-intent cut, and `DEFECT_TYPES.md` **[checked]**.
**Neither named document is among them.** The false clause was put to the user **as false** on the
surface, before he ruled.

**★ THE GUARD-CLASSIFICATION STOP WAS ALREADY FIRING, and that is established rather than inferred.**
Its population comes from `gen_guard_state.AUTHORED` and not from the committed artifact — line 1404
reads it from that table and line 1408 is the STOP **[checked]**. **This session still held its own
staged copy of `gen_guard_state.py` taken BEFORE the batch ran**, and that copy already carries
`gen_l0_l1_outgoing_population.py` and `gen_withheld_family_reading.py` at lines 228 and 240
**[checked]**. A search of `gen_guard_classification.py` for all three tool names returns **no
occurrence** **[checked]**, so none carries an authored verdict. **So the enrolment added a third name
to a list already non-empty; clearing the STOP needs verdicts for three tools, two of them older debt.**

**WHAT COULD NOT BE CHECKED AND IS CARRIED AS A RELAY.** The two guard captures sit at a path
**outside the connected folder** and cannot be reached from here, so CC's verdict-by-verdict
comparison, its fifteen failing guards and its 78→79 count are **[relayed]** and uncheckable by this
side. The new tool `tools/audit/gen_l2_withheld_documents.py`'s **source was never opened**; what
stands about the derivation rests on its output checked two ways and on the pack generator's own
tables, not on its code. `tools/audit/decisions/backbone_decisions.json` was not opened; the homes
were checked at the generated INDEX.

## 3. The two rulings, each with its own record

**Both records are the record and neither is reproduced here (#6);** what follows is the minimum a
next side needs.

**(a) `records/cowork/rulings/cowork_rulings_2026_09_21_l2_withheld_documents_sitting.md`.** L2's
derived withheld-document set is **CONFIRMED whole** — the user's word, verbatim: *"confirmed."* The
set is the `documents` field of `tools/audit/l2_withheld_documents.json` and is not transcribed
(**D-431**, **#17f**). The reading stated back to him: the set whole, no subset, **and this does NOT
authorize the second half of the pack-build dispatch**, which is a separate act not yet written. The
widening the confirmation causes — entries ruled OUT pulled back in because their own text names a
withheld document — **is unmeasured** and returns to him as the leak list, which the record already
provides for.

**(b) `records/cowork/rulings/cowork_rulings_2026_09_21_decision_surface_form_sitting.md`.** The user
gave, unprompted, a four-clause standing form for decision surfaces. **His words are quoted whole in
that record and are not paraphrased here.** What a successor must carry:

- **It supersedes D-658's no-recommendation clause, and that clause only.** D-658's other three
  requirements stand — every claim cited at its source, the records concerned read whole, anything
  unsettled marked unsettled rather than filled.
- **It closes a question the record had twice recorded as open** — `cowork_rulings_2026_08_31_decision_surface_sitting.md`
  states in terms that it does not settle whether D-249's recommendation clause or D-658's
  no-recommendation rule governs a surface of that kind. It is settled for D-249.
- **The meta level of the suggested act becomes a THIRD standing ground**, beside the ultimate
  objective and the guiding principles.
- **★ ENTRY 224 §7 ITEM 2 STATES THE OLD TEST BACKWARDS AND IS NOT CORRECTED AT ITS OWN OBJECT.** It
  reads *"No recommendation unless the record fails to settle the question (D-658)"*, which permitted
  a recommendation in exactly the case D-658 forbade one. **This entry is that correction**, on the
  shape entry 152 (x) sets. The line is now moot as a rule — clause 4 supersedes what it misstates —
  but a reader of entry 224 meets a false statement of a rule, which is why it is named here.

## 4. The bridge fault fired, in its stale shape

`device_commit_files` returned success and wrote a copy **one revision behind** on the second landing
of the decision-surface form record: **two of three amendments present, the third absent**, read at
the device's own copy staged back **[checked]**. It was caught because the landed copy was probed at
**all three** amendment sites rather than at one — which is entry 152 (vi)'s own rule: on a file
changed by several edits, proving the first edit landed proves nothing about the last. Re-committed
under the modification-time guard and re-proved.

**Entry 224 §8 records the fault not firing on any of its landings. That was not evidence it had
gone.**

## 5. Degradation, reported in this sitting and acted on

**Two of the user's named tells appeared in this sitting's own work, both inside one episode.** After
establishing at the objects that the derived set is arithmetic over the user's own ruling of
2026-09-05 and carries no judgment — which is the fact that decides which rule applies — this side
applied **D-658**, which governs only where the record does **not** settle the question. That is *a
refuted shortcut*. It then built a refusal on top of the inversion it had just found in entry 224,
and had to revise it when challenged — *a claim revised across messages, each resting on the
previous*.

**This side reported the two tells unprompted and recommended handover at a verified stop; the user
directed the handover.** Against that: the two checks it could have leaned on — CC's account of the
members table and of the STOP's cause — it opened for itself instead, and one of them came back false
(§2). **A THIRD TELL APPEARED IN THIS ENTRY'S OWN FIRST WRITING and was caught by its own check**: a
home stated from memory of a grouping rather than from the register read whole at boot (§7 item 1, and
§11).
*(★ CORRECTED AT THE CHECK, §11. FORMER: "every claim in this entry not marked [relayed] was taken at
an object this sitting opened" — refuted by §9's trend figures, which are entry 224's and were not
measured here, and by the false home claim the check then found.)*

## 6. Boot order for the next session

1. **THE DEVICE-INFO CALL FIRST**, before any folder-access request. File tools only; no shell in the
   container or on the device; **list no repository root.** Listing `C:\s` — the PARENT — returns a
   names-only skeleton and is enough to confirm the repository exists before requesting access. **This
   sitting took that route.**
2. The ordinary session-start read (entry 186's Boot block: `CLAUDE.md` at its **six** spans, the
   membership read at that file's own "Build and test commands" block; `DECISIONS.md` whole;
   `STATUS.md`; the gating answer's identity list at `tools/audit/nongating_apparatus_rows.json` →
   `★_the_live_gating_answer` → `gating_ids`), entry 118's bridge-fault section, entry 152's (v)–(x),
   entry 169 whole. **Then this entry whole, then BOTH ruling records of 2026-09-21 whole** — they are
   short and the next act rests on them. **Then entry 224 whole**, reading its §7 item 2 as corrected
   at §3(b) above. The two ruling records of 2026-09-20 need not be re-read whole unless the next act
   touches them. Earlier entries only where this entry points into them.
3. **READ BOTH REF FILES, AND EXPECT THEM TO DISAGREE** — `master` at
   `7d7291f401d05052240d76078db175ce160e8b91`, `origin/master` at
   `ef4fad940d806edf8f84eb9a895f88d70a9bbcf2`. **If they now agree, something pushed; find out what
   before anything else.** If `master` has moved further, something else ran.
4. Verify at listings: this entry at `records/cowork/handoff/` (the size the opening instruction
   gives), and both 2026-09-21 ruling records at `records/cowork/rulings/`. **For CC's report the
   figure is 39,332, taken at this sitting's staging result** and reported as such.
   **`records/cc/instructions/`'s listing exceeds the tool's inline limit**: the tool saves it to a
   file and it is searched with `Grep`, not read whole **[relayed from entry 222 through entries 223
   and 224; this sitting did not list that directory either]**.
5. Then §7's list.

## 7. What comes next, in this order

1. **THE CONSOLIDATION OF THE DECISION RULES — DIRECTED BY THE USER**, in the turn that confirmed the
   withheld documents: *"since you have had difficulties understanding the decision rules why not
   fix…"*. **It is a decision with alternatives, so his clause 2 applies and a SURFACE is owed** —
   all alternatives, each weighed towards the ultimate objective, towards the guiding principles and
   marked for its meta level, **with a recommendation** (clause 4).
   **★ THE SCATTER, WITH THE HOMES READ AT THE REGISTER AND NOT RECALLED [checked at `DECISIONS.md`,
   read whole at this sitting's boot, and at D-658's full entry in `decisions/group_T.md`].** The four
   register entries that bear on decision surfaces are homed in **FOUR DIFFERENT DOCUMENTS**:
   **D-249** in `CLAUDE.md`; **D-424** in `cowork_notation_adoption_increment.md`, the opening block
   above its first section heading; **D-599** in `cowork_adjudication_dossier.md`; **D-658** in
   `cowork_audit_protocol.md:1306-1317`. **Beside them, and carried by no register entry at all**,
   stand Ruling 1 of `cowork_rulings_2026_08_31_decision_surface_sitting.md` and the per-sitting
   rulings of 2026-08-15, 2026-08-21, 2026-08-28 and 2026-09-04, which live only in handoff entries
   and ruling records — and now the four-clause ruling of 2026-09-21.
   *(★ CORRECTED AT THE CHECK, §11. FORMER: "`cowork_audit_protocol.md`, where three of the four
   register entries are homed" and "**Open before writing:** `cowork_audit_protocol.md` (where D-424,
   D-599 and D-658 are homed)". **Only D-658 is homed there.** The claim was written from memory of a
   grouping rather than from the register this sitting had read whole, and it was carried into the
   decision-surface form record's §3 item 2 as well, where it is corrected in the same act.)*
   **THE TWO LIVE CHOICES, as far as this sitting located them and not as an enumeration:** *(i)*
   **which document becomes the one home** — `CLAUDE.md` Conventions is read at every session start
   and the other three homes are not, so a rule that must bind every session has to live where every
   session reads; against that, **`CLAUDE.md` appears to be under an active growth regime**, which is
   **INFERRED** from its own 2026-09-07 membership ruling (read at its six spans this sitting) and
   from the names of `tools/audit/claude_md_prune_backlog.json` and
   `tools/audit/claude_md_growth_2026_09_07.json` — **neither artifact was opened**, so the size of
   that cost is unestablished. *(ii)* **whether the rule text MOVES or stays put under a pointer index
   over it** — #6 argues one way and the preserved-former-wording convention bears on the other.
   **★ THE READING IT NEEDS FIRST, AND WHY THE SURFACE WAS NOT WRITTEN IN THIS SITTING:** writing it
   would have meant asserting things about objects this side never opened. **Open before writing:**
   the three non-`CLAUDE.md` home documents named above; `cowork_register_rule_c_suspension_2026_08_28.md`
   (whether these register entries can be re-homed at all); the two `claude_md_*` artifacts above;
   and Ruling 1 of `cowork_rulings_2026_08_31_decision_surface_sitting.md` at its own passage.
   **AND THE ACT ITSELF IS PROBABLY A CC DISPATCH**, not a Cowork edit: an edit to `CLAUDE.md` moves
   `tools/audit/session_start_read_size.json` and `tools/audit/defense_share.json`, whose generators
   have to be re-run — the same ordering rule the last two batches were written on.
2. **THE SECOND HALF OF THE PACK-BUILD DISPATCH.** `WITHHELD["l2"]` authored — the two prose fields
   `the_subject_in_plain_words` and `the_oracle_this_family_protects` (both read by direct index, so a
   missing one is an unnamed error rather than a STOP), the six passages of Ruling 5, and **the
   confirmed documents**; `EXTRAS["l2"]` authored — the charter member, the two extract populations
   and the ledger (Rulings 2 to 4); the `DATE` mechanism at its four sites; Ruling 8's two named
   regenerations; the render; the cross-reference additions and the leak check run, **the leak list to
   the user**. Entry 224 §2 is what that dispatch needs and did not have. Source-check it at the
   objects before release (**D-250**, and the user's standing rule of 2026-09-05).
   **★ TWO STATES IT MUST BE WRITTEN KNOWING:** the refs disagree (§0), and
   `gen_guard_classification.py` STOPs and was already stopping (§2).
3. **Item 1(c) — the staged score set with each claim beside it** (Ruling 7 of 2026-09-05). **Not
   started: nothing this sitting or entries 221 to 224 read shows it begun.** A session of its own; it
   cannot be half-done.
4. Carried unchanged from entry 224 §7: the one-word *figure* → *value* fix in `FRAMEWORK.md`;
   §14.1's uncorrected restatement; the unknown cause of
   `tools/audit/claude_md_finer_archive.json`'s modification.
5. Open, not decided: whether row 8's second extract is renamed (entry 215 §3 item 3).
6. **Ruling 9's lost object** (entry 224 §4) — stated, not decided, and not to be repaired by a
   dispatch on its own initiative.

**★ WHAT THE ORDER OF ITEMS 1 AND 2 IS NOT.** The user directed item 1 and said nothing about whether
it precedes item 2, which is the plan's main line. **Nothing this sitting read settles that**, and it
is marked unsettled rather than filled.

## 8. Declared departures, and this side's own state

- **NO SHELL COMMAND WAS RUN**, in the container or on the device. **No repository path was read or
  written through a shell on either machine.** No WebSearch, no WebFetch, no subagent, no
  multiple-choice popup, no task list. **No commit to git by this side.**
- **NO DEPARTURE FROM THE BOOT ORDER.** The device-info call was first; the one listing outside a
  connected folder was of `C:\s`, the parent, which returned a names-only skeleton. **No repository
  root was listed.**
- **The user's Cowork memory store was NOT read and NOT written to** by any memory tool call.
- **Directories listed:** `C:\s` (skeleton, before access); `records/cowork/handoff/`;
  `records/cc/reports/`; `tools/audit/`; `records/cowork/rulings/`.
- **Read whole:** entry 224; entry 223; entry 169; `DECISIONS.md` (862 lines, three calls);
  `STATUS.md` (twice — before the batch and after it); the dispatch
  `cc_instruction_l2_withheld_documents_2026_09_21.md`; CC's report
  `cc_report_l2_withheld_documents_2026_09_21.md`; `tools/audit/l2_withheld_documents.json`;
  `tools/audit/changed_paths_establishment.json`; both ruling records of 2026-09-20; both ref files
  (twice).
- **Read at sections or by search:** `CLAUDE.md` at its six ruled spans, located by heading search,
  the membership read at that file's own "Build and test commands" block; the gating answer at
  `★_the_live_gating_answer` and through its identity list, **whose 222 entries this side counted at
  the file rather than taking the artifact's own `gating_rows` field** (247 open, 222 gating, 25
  non-gating at its own fields); entry 118 at its bridge-fault section; entry 152 at (v)–(x); entry
  186 at its Boot block; `tools/audit/gen_derivation_boot_pack.py` at `MEMBERS`, `WITHHELD`'s head
  and its harmony-boundary record, `leak_strings`, `leaks_in`, `PATH_LIKE`,
  `cross_reference_additions`, the `VERDICTS["l2"]` boundaries at 1364 and 2813, its `VERDICT_IN`
  occurrences by search, the `withheld_documents` sites and three verdict identities;
  `tools/audit/gen_guard_state.py` at the three tool names, in **both** its pre-batch and post-batch
  copies; `tools/audit/gen_guard_classification.py` by search for those names and at its import,
  population and STOP lines; `tools/audit/changed_paths.py` at its establish mode;
  `tools/audit/shell_read_guard.py` at its `main()` establish branch; `decisions/group_T.md` at
  D-658's full entry; `cowork_rulings_2026_09_05_l2_withheld_family_sitting.md` at its §4 by search;
  **`cowork_rulings_2026_08_31_decision_surface_sitting.md` by search for every occurrence of
  *recommendation* with its context — that record was NOT read whole; it is 644,942 bytes**, and what
  this entry and the form record cite from it is cited at those occurrences and nothing wider.
- **NOT read at all:** `OPEN_ITEMS.md`; `ARCHITECTURE.md`; `FRAMEWORK.md` — **at all**;
  `cowork_audit_protocol.md`; `STATUS_ARCHIVE.md`; `tools/audit/decisions/backbone_decisions.json`;
  the new tool `tools/audit/gen_l2_withheld_documents.py` — **its source was never opened**; the two
  guard captures, which sit outside the connected folder and are unreachable from here; the slice
  derivation; every extract, first-pass or second; every paper; entries 187 to 222; **every rulings
  record but the four this entry names — the two of 2026-09-20 read whole, the 2026-09-05
  withheld-family record read at its §4, and the 2026-08-31 record searched and NOT read whole.**
  **And `gen_derivation_boot_pack.py` was NOT read whole** — it is 279,959 bytes, and it was read at
  the sites listed above and nowhere else.
- **Landings.** The decision-surface form record **three times** — as first written (**13,719**,
  proved by probing the device's own copy staged back at its head, its two stated readings, its
  defect section and its foot); after three amendments (**14,681** — **the bridge fault's stale
  shape**, two amendments present and the third absent, §4); and re-committed under the
  modification-time guard (**15,410**, proved with all three amendments and the foot present). The
  withheld-documents record **once** (**14,581**, proved the same way at four places spanning it).
  Then this entry. **All are new-path or own-path landings.**
- **DEGRADATION: two tells, named at §5**, both inside one episode, reported unprompted; the user
  directed this handover.

## 9. Landing

Written in the container and committed to
`C:\s\MS\records\cowork\handoff\cowork_handoff_entry_two_hundred_and_twenty_five.md` through the
bridge. **It is uncommitted to git.** No dispatch is out to carry it; the next dispatch's Task 0
commits it together with this sitting's two ruling records, CC's report, and whatever else the
interim-carrier clause then reaches.

**No size for this entry is predicted here** — entry 221 §9 records that predicting an entry's own
size is the defect its own landing then proves. The closing figure is measured at the folder listing
after the last edit and is carried in the opening instruction for the next session. For the trend a
next side may want: entry 221 landed at 30,334, entry 222 at 20,861, entry 223 at 25,144, entry 224
at 29,655 — **the first three [relayed from entry 224 §9]; entry 224's own 29,655 [checked at the
folder listing at this sitting's boot]**.

## 10. ★ The user-ordered fact- and source-check, written in the act that ran it

This entry was re-read **whole** as first landed, **at the device's own copy staged back** rather than
at this side's container copy, and each claim checked against the object it rests on, on the four axes
of the user's rule of 2026-09-12 — completeness, coherence, correctness, and misuse of absolutes.
**What it found is recorded at §11 below rather than promised here**, because a check that reports its
own result before running is the shape this record has been caught in.

## 11. What the check found, named rather than counted

**It did not come back empty.** Every defect below is corrected at its own site with its former
wording preserved (#12).

- **★ A HOME STATED FROM MEMORY OF A GROUPING, IN THE SECTION THAT TELLS A SUCCESSOR WHAT TO READ.**
  §7 item 1 said `cowork_audit_protocol.md` is *"where three of the four register entries are homed"*
  and sent a successor there for D-424, D-599 and D-658. **Only D-658 is homed there.** At
  `DECISIONS.md`, read whole at this sitting's own boot: **D-424** is homed in
  `cowork_notation_adoption_increment.md`, **D-599** in `cowork_adjudication_dossier.md`, **D-249** in
  `CLAUDE.md`. **This is the worst defect the check found**, because it would have sent the next
  sitting to one file for three rules that are not in it — and it makes the scatter *wider* than the
  entry claimed, not narrower. **The same false claim stands in the decision-surface form record's §3
  item 2 and is corrected there in this same act.**
- **A COUNT PUT ON THIS SIDE'S OWN ACTS, AND IT NAMED FOUR UNDER THE WORD THREE.** §0 said *"Plus
  three this sitting is responsible for"* and then listed four things, one of which — CC's report —
  is not this sitting's. Entry 169's cadence 6 says to put no number on your own acts and name the
  members.
- **A CLAIM WIDER THAN ANYTHING ESTABLISHED, REFUTED THREE LINES BELOW ITSELF.** §0 said *"Nothing
  awaits an answer"* while its own (c) records a question whose owner this sitting never established.
  **This is the defect entry 223 §11 caught in its own §0**, met again here.
- **A SENTENCE REFUTED BY ITS OWN SECTION.** §1 item 2 said the sitting *"Established that the
  dispatch was running"*; §2 closes by saying who ran the guard set was not established. **This is the
  same shape as the bullet above and the second instance in one entry**, which is why both are named
  rather than fixed quietly.
- **A SELF-CONTRADICTING CLAIM ABOUT THIS SITTING'S OWN SOURCING.** §5 said *"every claim in this
  entry not marked [relayed] was taken at an object this sitting opened"* — refuted by §9's trend
  figures, which came from entry 224, and by the false home claim above.
- **A FIGURE'S PROVENANCE STATED INCOMPLETELY.** §0 said CC's report's size is not a listing figure
  because the listing *"does not overrun the tool and was taken"* — the load-bearing fact, left out,
  is that the listing was taken **before that report existed**.
- **A HEADING CONTRADICTING ITS OWN LIST.** §0 said Task 0's commit carried *"five of the six interim
  carriers plus entry 224"* and then named six members of which entry 224 is one.
- **AN INFERENCE PRESENTED AS A STANDING FACT.** §7 item 1 called `CLAUDE.md` *"under an active growth
  regime"*. Neither `claude_md_prune_backlog.json` nor `claude_md_growth_2026_09_07.json` was opened;
  the reading now says what it rests on and that the cost's size is unestablished.
- **A CONCLUSION DRAWN HARDER THAN ITS MEASUREMENT.** §2 said two tools *"were not edited,
  established by"* unmoved modification times. It now states the measurement and calls it strong
  evidence rather than proof.
- **A VAGUE POINTER.** §8's *"every rulings record but the four named"* did not say which four or at
  what depth each was read. Named now.

**What the check CONFIRMED at its objects and did not strike:** both ref values, read again at their
own files; the sizes of the two ruling records and of CC's report at this sitting's staging results;
that `MEMBERS` carries six members and neither named document among them; that the `VERDICTS["l2"]`
block opens at 1364 and closes at 2813; that D-453 reads `VERDICT_IN` and D-535 `VERDICT_OUT`; that
§4 of the 2026-09-05 withheld-family record states 111 IN, 133 OUT, 0 UNPLACED; that
`gen_guard_classification.py` carries no occurrence of any of the three tool names; that the
pre-batch copy of `gen_guard_state.py` carries two of them at lines 228 and 240; and the user's two
quoted phrases, checked word for word against what he wrote.

**What it did NOT do.** It opened no object this sitting had not already opened. The home defect was
settled by re-reading the four rows at `DECISIONS.md` itself rather than at this sitting's memory of
its boot read — **D-424 at line 625, D-249 at 768, D-599 at 803, D-658 at 823**, each row's home read
in place. It did not re-read CC's report, the dispatch, or either ruling record whole. **Nothing here
says a further pass would come back empty.**
