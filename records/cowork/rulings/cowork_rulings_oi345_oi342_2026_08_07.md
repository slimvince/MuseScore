# Two rulings of 2026-08-07 (fourth batch) — OI-345 and OI-342

> **STATUS: RATIFIED RULING RECORD, written 2026-08-07 (Cowork), awaiting application.** Ruled by
> the user on Cowork's decision surface of 2026-08-07 ("I agree with your two recommendations",
> after the full surface with alternatives was delivered as user-visible text). Interim carrier
> until the applying dispatch records them (the same-commit rule, D-230, binds at that
> recording). Applied by the dispatch AFTER `cc_instruction_owner_rulings_homing.md` returns —
> one dispatch at a time.

## Ruling 1 — OI-345: the shell-read guard's PowerShell blindness is FIXED, CORPUS FIRST

The guard's utility set is POSIX-only while the guard is armed on the PowerShell tool by design:
measured at its own decision function, `Get-Content`, `Select-String`, `Get-ChildItem` and
`Test-Path` aimed at repository paths are allowed where `cat` is denied, and the establishment
corpus contains no member of that family — so the published rates re-derive exactly while blind
to the whole dialect (the #19 condition; OI-343's shape one shell dialect over).

**The ruling: OI-343's ruled order is applied again, as one remedy per defect class (#6).**
Extend the establishment corpus FIRST with the PowerShell family and its aliases (`gc`, `sls`,
`dir`, and the rest), sanctioned and forbidden forms both; then widen the utility set; publish
both rates against the extended corpus; the revert condition — a material rise in false
denials — governs as before. The mechanism change is licensed by this ruling (D-436). *Why
corpus-first is not optional:* re-establishing against the blind corpus would repeat the error
being fixed — the current `--establish --check` passes precisely because the corpus omits the
family (OI-343's own reasoning, which this ruling adopts).

**The excluded alternative, recorded:** leaving the row open — rejected because a gating
establishment row would stay open indefinitely and the guard's green would go on over-claiming.

## Ruling 2 — OI-342 CLOSES

Its subject — the routes artifact's owner-determinacy column carrying a different question's
answer for ten rows — is discharged at HEAD: no open row carries the conflated reason, the fact
is recorded in the row's dated block, and the lesson is operationalized in the blocker
derivation, which reads each row's own recorded reason rather than the column. **The ruling: the
row closes**, with this ruling as its provenance (the INDEX row flips; the detail file gains the
dated resolution note). A future artifact repeating the conflation is a new instance and gets
its own row.

**Neither ruling authorizes a fix to the analysis, a design, or an inference change.**
