# The document-route rulings of 2026-08-08 — finish-line item 1's bar-excluded class, ruled per document

> **STATUS: RATIFIED RULING RECORD, written 2026-08-08 (Cowork), awaiting application.** Ruled by
> the user on Cowork's decision surface of 2026-08-08 ("raatified", after the full surface with
> the two routes, their pros and cons naming their principles, and a per-document recommendation
> was delivered as user-visible text; every recommendation ratified unchanged). Interim carrier
> until the applying dispatch records them (D-230). Scope: the sixteen documents of the
> finish-line item "Register entries whose home document is named only in a form the delegation
> bar excludes" (population at `tools/audit/phase1_finish_line.json`, that item's
> `entry_ids_by_document`).
>
> **None of these rulings authorizes a fix to the analysis, a design, or an inference change.**

## Route (i) — THE USER WRITES A DELEGATION (three documents)

Per rule (g) the delegation is the user's act; per the OI-293 precedent it is executed by a
session under the user's ruling, which this record carries. Each clause below is DRAFT WORDING
prepared by Cowork under that ruling, in a form D-432 admits (an explicit delegation clause);
the applying dispatch writes it at the named placement and STOPS if the placement's owning
section is other than named, reporting before writing.

- **`cowork_joint_estimator_factorization.md`** (discharges D-565's class question). Placement:
  the joint-estimator section of `ARCHITECTURE.md`. Draft clause: *"The ratified contract for
  the estimator's factor structure — the factor set, the per-factor granularity, the emission
  design and the §5 decode plan — is `cowork_joint_estimator_factorization.md`, which this
  section points at and does not restate."*
- **`cowork_notation_adoption_increment.md`** (discharges D-424, D-425, D-426). Placement: the
  `ARCHITECTURE.md` section that owns the notation record surface (§11.5 as named on the
  surface; the dispatch verifies the owning section and STOPs if it is another). Draft clause:
  *"The ratified contract for the notation-surface adoption increment — its scope, its
  increments and their tracking — is `cowork_notation_adoption_increment.md`, which this
  section points at and does not restate."*
- **`docs/llm_integration.md`** (discharges D-442…D-448). Placement: `ARCHITECTURE.md` §19
  (LLM Integration). Draft clause: *"The ONE detailed design for this section's scope is
  `docs/llm_integration.md`, which this section points at and does not restate."* The user's
  ratification took the delegation route with the stated caveat resolved in its favour (the
  document is authoritative, not stale).

## Route (ii) — RE-HOME THE ENTRIES (thirteen documents, session work per entry)

Standard homing discipline throughout: per-entry owner check before writing (a soft owner is a
STOP for that entry and returns to the user), the destination section must state rules, the
entry written in the section's own voice with its defense or "derivation not recorded", former
home/class/verbatim preserved (#12), provenance in register fields only, LEGACY / DEFERRED /
shelved markings ride along.

| Document | Entries | Target, as ratified |
|---|---|---|
| `cowork_style_taxonomy_proposal.md` | D-587…D-591 | §6 / §6.7 (the style-system family, per the batch rulings) |
| `cowork_style_clustering_plan.md` | D-598 | §6 / §6.7 |
| `cowork_idiom_discovery_findings.md` | D-554, D-555, D-556 | §6.7 |
| `cowork_adjudication_dossier.md` | D-600, D-601 | per entry, the subject's owning section |
| `cowork_l1_l5_premise_debt_audit.md` | D-615 | per entry, the subject's owning section |
| `cowork_uncertain_resolver_investigation.md` | D-629 | per entry, the subject's owning section |
| `cowork_layer2_reslice_design.md` | D-631, D-632 | the Layer-2 section |
| `docs/architecture_joint_inference.md` | D-618…D-621 | the joint-estimator section for live content; content superseded by the ratified architecture is recorded superseded, never copied |
| `docs/nct_detection_design.md` | D-560 | the layer that reads the notation (structural notational metadata) |
| `docs/iter90_bass_as_root_promotion_shelved.md` | D-594, D-595 | `docs/scoring_model.md` §8, shelved evidence intact |
| `docs/key_detection_baroque_partial_signature.md` | D-575 | its legacy family's home, superseded-in-fact status and ⚠ LEGACY mark intact |
| `docs/redesign_plan.md` | D-317…D-320 | per entry with ⚠ LEGACY marks intact |
| `cowork_target_architecture.md` | D-467 | per-entry re-home; if the document proves superseded by `ARCHITECTURE.md` itself, REPORT — never assume |

## Also carried by the same application

**D-472 (Option A, user 2026-08-08)** — per the dated amendment in
`cowork_owner_rulings_2026_08_07.md`: Layer-6 home with the arm split stated (grouping rule
live, Pass-4 stabilization precondition ⚠ LEGACY, both citations), and the conformance question
rowed as its own open item (index row + detail file in the same commit).
