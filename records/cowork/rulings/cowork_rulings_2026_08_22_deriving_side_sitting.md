# Rulings — the deriving-side sitting, 2026-08-22

> **STATUS: RULING RECORD.** Cowork, 2026-08-22 (the forty-fourth Cowork session). An interim
> carrier under the standing clause that a sitting record is written in the turn its ruling is
> given and lands in git at the next dispatch's Task 0.
>
> **Taken at branch tip `dcbfa5fe32`** (parent `473f287372`; both refs at the tip, read with
> `git show -s` and `git for-each-ref` at the explicit hash on the user's machine), while the
> pilot-preparation dispatch was running and had landed no commit. The objects ruled on are
> Ruling 1 §1 fact (iii) of `cowork_rulings_2026_08_22_pilot_order_sitting.md` (the deriving
> side's identity, unsettled), register entry D-252, and §8 point (P1) of
> `cowork_blind_session_brief_harmony_boundary.md`. No document ruled here is generated.

---

## 0. What was put, and in what form

One decision, put self-contained as a turn's final response with no question in it — the two
sides re-explained from scratch, the precedent of the 2026-08-21 evaluations, the facts bearing on
the choice each at its source (one marked unmeasured), three alternatives each with its case for
and against and rated towards the ultimate objective and towards the guiding principles, and this
session's recommendation. Before ruling, the user asked and was answered, in prose, three
questions about the plan itself: whether the derive-blind step redoes the thinking work (yes, inside
the ratified design intent); whether there are two comparisons (yes — derived statements against
the existing text per unit, then the ratified replacement against the code at the audit); and what
of the prior work a blind session reads (the boot pack only; the prior work re-enters at the grading
step and as the framework's placement-test population; the empirical findings ledger, the ruled
third channel, is not built — the hole the user accepted for the pilot only). The user then ruled
with the alternative's letter and three conditions, verbatim below.

## 1. Ruling 1 — the blind deriving session is a FRESH COWORK SESSION, booted from the brief and the boot pack only (Alternative B; the user's word: "B")

**Ruled.** The held-out test's deriving session is a fresh Cowork session, not Claude Code. Its
reads are the staged files — the brief and the boot-pack directory — and nothing else, so its
independence is measured by the staging record rather than asserted (#19). Its output is delivered
to the repository root through the device bridge and committed by the next ordinary dispatch (the
brief's §7, the Cowork branch). D-252 is untouched: a deriving session writes no instruction file
and executes none. This session, having read everything, is not that session.

**The ground stated, recorded as this session's reading.** A Claude Code session's boot hands it
`CLAUDE.md` whole before any brief is opened — the one file the pack cuts to two spans — and
whether that automatic read can be suppressed for one session is a fact nobody has measured at the
tool. Independence resting on an unmeasured tool behaviour is the shape #19 forbids, in the one
place the test cannot afford it.

**The alternatives declined.** A — Claude Code, the D-252 default: declined on the unmeasured boot
read. C — Claude Code after measuring whether the boot read can be suppressed: declined as a
measurement session spent on a question B answers by construction, and as resting on a tool
setting rather than a ruled mechanism.

## 2. The three conditions the user attached, verbatim, and how each stands against the record

The user's words:

> *"whenever specs are done - we have scoured all existing knowledge for bad and good things - to
> make sure any previously thought of good idea is not lost."*

> *"Scouring/mining for "good ideas" to put in v1 of spec - includes allowing us to look in
> implementation?"*

> *"before the audit specs vs code specs should be in as good shape as possible (=correct and
> complete)."*

**Condition 1 — every previously thought-of good idea is scoured before a specification is done.**
Recorded as a STANDING CONDITION on the detail-specification phase's completion, in the user's
words. Against the record it is already carried in three places and this condition binds them as a
requirement rather than a description: the grading step (plan §6.3 step three — every covered
passage of every document-set member, its pre-restructuring version and its deletions, opened after
derivation, with *adds* salvaged and nothing dropped); the disposal step (§6.3 step four — every
statement of the outgoing text reaches exactly one of five fates, completeness checked by
arithmetic, #12); and the framework's placement test (§6.2 — ruling records, decision surfaces,
dossiers, deferred register entries, the evidence inventory, declared dormancies and every current
and deleted heading placed, every unplaceable one a finding). The empirical findings ledger
(preparation output (c)) is the fourth channel and is NOT built; the user accepted that hole **for
the pilot only** (this sitting, in prose: "holes/blind spots in pilot only - that is ok"). **So the
ledger is owed before the framework and detail-specification phases rely on it**, and this record
states that consequence without ruling when it is built.

**Condition 2 — is the implementation a source of good ideas for the first derived text? — a
QUESTION, answered from the record and NOT ruled here.** The record's answer is no, in two
clauses: §3.2 of the phase-definition surface (*"NOT ALLOWED: reading implementation-derived
material inside the deriving session"*) and plan §6.3 step one (*"Code sites and failing runs are
NOT sources of a deriving pass"*). Under the ruled plan an idea that lives only in the code reaches
the specification through the audit — a divergence between the ratified replacement and the code is
evidence, and the fix plan decides which side moves — and through the fact-gate where a fact
observed in the code's behaviour survives the implementation being thrown away (the ledger's
admission test). Whether the user wants the implementation mined for ideas into the first derived
text is a ruling that would amend both clauses, and it is put as its own decision surface in the
turn that records this sitting. Nothing is changed by this record.

**Condition 3 — before the audit, the specifications are as correct and complete as can be.**
Recorded as the user's restatement of the ruled order: the detail-specification phase (born-complete
statements, each with its defense) and the measurement-design stage both precede the audit, and no
residual is interpreted before the audit. Conforms to the record; no amendment.

## 2a. Ruling 2 — the implementation is NOT mined for ideas into the first derived text; ideas that live only in the code surface at the audit and are then considered for the specification (Alternative A; the user's words: "We can do A")

**What was put.** Condition 2 above was put as its own decision surface in the turn that recorded
Ruling 1 — the referents from scratch, the record's present answer and its ground, three
alternatives (keep the exclusion; admit the code as a mining input to the ledger behind the
fact-gate; let the grading step open the code as an untrusted source), each rated, and the
recommendation A with B as the route if the code were judged to hold ideas the documents do not.
The question was put in the following turn.

**Ruled.** The exclusion stands as the record states it: §3.2 of the phase-definition surface and
plan §6.3 step one are unamended. The user's own statement of the consequence, verbatim: *"Any good
ideas in the code will then appear as a result of the audit spec <> code, and then be considered
as something that should be put in spec or not."* So a mechanism that exists only in the code
reaches the specification through the audit's divergence evidence and the fix plan's disposition of
each divergence — never through a deriving or grading session. Condition 2 of §2 is thereby
answered and closed; conditions 1 and 3 stand as recorded.

**The alternatives declined.** B — code as a mining input to the ledger behind the fact-gate:
declined; the user did not take the route. C — the code opened at the grading step: declined as
collapsing the audit's first direction into the grading step.

**What it does NOT do.** It does not build the ledger and does not move the date at which it is
owed (§2, condition 1). It adds nothing to the audit's definition; the audit surfaces every
divergence by its own ruled design, and this ruling only names what the user expects to do with one
class of them.

## 2b. Ruling 3 — the blind session reads the repository's own HUMAN-ANNOTATION files, STAGED to it by name through the bridge, after those files are enumerated at the objects (the variant put after the user's question; the user's word: "Agree")

**What was put.** The brief's §8 point (P3) — whether the blind session may open the repository's
annotated scores — was put self-contained with three alternatives: A, no repository scores and the
public annotation sources fetched under the research rule (recommended as put); B, a named subset
rendered into the pack as a seventh member by the generator; C, a repository path named in the
brief as an exception. The user asked whether A meant the downloaded corpora were not trusted. The
writing side answered that trust was not the ground — the held corpora are the established gate
data, their sources anchored by hash to their public origin (D-489) — and that A's two grounds (a recorded read route; no `.ours.json` beside the
annotation) are met equally by a variant it had not put: the writing side STAGES the human-annotation
files, by individual file name and never a directory, into the blind Cowork session exactly as it
stages the brief and the pack. The writing side recommended the variant over A on the condition
stated below. The user agreed in the following turn.

**Ruled.** The blind session's independent musical-score source is the repository's own human
annotation files — the same bytes as their public origin — staged to it by name through the device
bridge, beside the brief and the boot pack. **Condition, ruled with it:** before the brief is
dispatched, the writing side enumerates at the tip objects (the corpus registry, the
reproducibility document, the corpus directories' manifests) which files are human annotation and
nothing else, and writes the staged list into the brief's §3 by file name; no directory is staged,
and no file holding the analysis's own output is staged. The raw annotation file is what the session
reads — the transposed-edition offsets the project applies at load time live in the parser, not in
the files, so the session sees the annotators' own reading. A file the enumeration cannot class is
NOT staged and is reported.

**The alternatives declined.** A — fetch from the public source: declined as adding a network
dependency to data already held and established; its two grounds are kept by the variant. B —
a seventh pack member: declined as a boot-list amendment and a generator change for one pilot. C —
a repository path named in the brief: declined as contradicting the pack's read-me and as resting on
the session's restraint.

**What it does NOT do.** It names no file yet — the enumeration is owed, and the brief's §3 is
amended when it is done, with the change recorded in the brief's §9. It amends no ruled boot-list
member; the pack is unchanged. It does not settle how later phases read scores (Ruling 12 of
2026-08-21 governs those, per unit).

## 2c. A correction of record to the pilot-order sitting's §1 fact (i), made at the user's direction after the step-zero return was read whole

At the user's direction this session read `cc_report_step_zero.md` whole (1–401) and the STEP ZERO
close section of `cowork_away_returns.md` whole, and verified its chain and figures at the objects
(`553dd5f405`, `64e7d2fd7f`, `dcbfa5fe32`). One statement of this side's own record is corrected:
`cowork_rulings_2026_08_22_pilot_order_sitting.md` §1 fact (i) says the pollution distribution
*"is complete over the whole document set (`unread: 0` …)"*. **That is true of the passage
population and false of the members.** At `git show dcbfa5fe32:tools/audit/period_stratum_split.json`
→ `★_the_widened_screen_population.the_coverage_gap`, nine of the twenty-six members carry no
flagged passage at all, so the distribution is silent on them by construction — CC's first
quarantined question of the step-zero report, standing unacted on. The nine are named at the
artifact; one of them is `cowork_layer2_slicing_design.md`, the design document of the layer whose
decision is the held-out subject. **Consequences, stated and not acted on:** the reading depth
Ruling 12 declares per unit from that distribution has no input for those members; the comparison
session for the harmony-boundary unit will meet that document with no pollution verdict and must
not read absence as cleanliness. The pilot-order record is not rewritten (#12); this section is its
correction and is cited beside it.

## 3. What this sitting does NOT do

It opens no session and dispatches nothing; the brief remains a draft until its remaining §8 points
are ruled. It builds no ledger and rules no date for it. It amends no phase definition. No finding
number is allocated; the series stands at F88. No open-items row is created, flipped or discarded;
[[OI-179]] stays OPEN and GATES.

---

*Provenance: Cowork, 2026-08-22, the forty-fourth session, at tip `dcbfa5fe32`. Every governing
document read from a bridge-staged snapshot with the file tools. The user's words: "B", with three
conditions quoted verbatim above; then "We can do A" on condition 2's surface; then "Agree" on the staged-annotation variant of the
annotated-scores surface.*
