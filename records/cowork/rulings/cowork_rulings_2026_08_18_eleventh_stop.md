# Rulings — the eleventh batch's STOP sitting (2026-08-18): the start state declared at the tree, Ruling 1b's premise corrected at the objects, the pin derivation sharpened to the landing-commit bound, and the two homeless values derived rather than withdrawn

> **Sitting record (Cowork, 2026-08-18).** The user's rulings on the four questions the eleventh
> batch's STOP-and-report returned, taken in conversation with the user present. **This record is
> an interim carrier and lands in git at the next dispatch's Task 0.** Nothing is executed by
> anything here — no file is edited, no pin is taken, no banner moves, no value is corrected and no
> generator is touched until an executing dispatch performs it. Rule first, execute later, in that
> order and no other.
>
> **★ THE CITATION THIS PREAMBLE DOES NOT MAKE, AND WHY.** Every earlier sitting record of this arc
> opens by citing **D-230** for the interim-carrier sentence. Finding **F72** of this sitting
> establishes at D-230's own full entry that its text does not carry that proposition. The sentence
> above therefore states the practice and cites nothing, and **Ruling 3 below writes the practice
> down as a rule** so that the next record may cite something that says it. This is F72 applied to
> the first document that could apply it.

## 0. The sitting's ground, verified at the objects before anything was put

`cc_instruction_preparation_eleventh.md` was dispatched and **STOPPED at its declared precondition,
before Task 0. Nothing was committed and nothing was pushed.** The writing side established the
state at the objects before this sitting opened, and the establishment is recorded here because it
is the premise everything below rests on.

**The branch rule reads NOT STARTED on all three of its own tests.** `HEAD` and `origin/master` are
both `9390e2ca2c` — the tenth batch's terminus — with **zero commits on it**; `STATUS.md` carries
the TENTH batch's entries alone and no eleventh-batch pointer entry; `cowork_away_returns.md`'s last
close heading is `# ★ CLOSE — THE PREPARATION TENTH BATCH (2026-08-18)`.

**What fired.** The dispatch declares the tenth batch's proven end state as its start state — 71
run, 70 passing, ONE failing, zero STOPs — and its own words make a different start state a
STOP-and-report. The measured start state, taken before any edit at a quiet tree, is **71 run, 69
passing, TWO failing**: `gen_filing_convention_application.py --check` ([[OI-372]], expected) and
`gen_evidence_pin_membership.py --check`, which reports its artifact STALE.

**★ FOUR ESTABLISHMENTS THE WRITING SIDE TOOK AT THE OBJECTS RATHER THAN ACCEPTING FROM THE REPORT,
each of which decides one of the four rulings below.** (a) `gen_evidence_pin_membership.py` builds
its ruling-record population by listing the repository root **on the file system** —
`os.listdir(ROOT)` filtered by `^cowork_rulings_.*\.md$` — and walks `ratification_surfaces/` the
same way; it has no argument parser, `if "--check" in argv` deciding read from write. (b)
`gen_artifact_inventory.py` writes **one** output, `tools/audit/artifact_inventory.json`, the
census; `gen_artifact_inventory_surface.py` writes **one** output,
`ratification_surfaces/cowork_artifact_inventory_ruling_surface.md`, and reads **two** input routes,
the census file and a citation scan it performs over the tree. (c) **Four** commits touched the
rulings-sort surface on the sitting's own day — `53e552296f` 10:00:25, `a21a55fc12` 18:14:23,
`4667816255` 22:28:33, `15dfb0e172` 23:13:00 — and the sitting record was added to git at
`570f2b63b1`, **16:34:06**. (d) `gen_session_start_read_size.py` carries
`key_span_characters(text, keys)`, a function general over a key chain, already called twice with
different chains; and both values Ruling 3 of the tenth-return sitting could not correct are **named
key spans in the artifact that tool already reads** — `★_the_live_gating_answer` →
`the_gating_rows`, and `★_the_live_gating_answer` →
`★_the_frozen_enumeration_measured_against_this_one`.

**★ ONE ESTABLISHMENT THE WRITING SIDE ATTEMPTED AND COULD NOT TAKE, DECLARED RATHER THAN LEFT AS A
GAP.** The per-path history walk over the other three unresolved members' ratification surfaces
**TIMED OUT on this mount and was not retried** — the same class of timeout the record already
carries for `git status` and for `changed_paths.py`. Those three rows of the report's §5.b stand as
the executing side derived them, unverified by the writing side, and the executing dispatch
re-derives them at the objects rather than inheriting them.

**The presentation form held, and the count is recorded so it is checkable rather than asserted.**
The full self-contained surface — background from scratch for each decision, every alternative with
its costs named to the principle or rule it rests on, each rated TOWARDS the ultimate objective and
TOWARDS the guiding principles, a recommendation with its reason — was delivered as the turn's final
response carrying **no question**, and the user **ruled in a LATER turn**. **ZERO presentation-form
tells fired.** The user's ruling, verbatim: ***"I agree with your recommendations."*** — given
against recommendations stated as **Alternative A** on Decision 1, **Alternative A** on Decision 2,
**Alternative B** on Decision 3, and **Alternative A** on Decision 4.

**What is NOT re-opened by anything here.** The three rulings and the one sub-ruling of
`cowork_rulings_2026_08_18_tenth_return.md` stand whole. Every defect below is in how the eleventh
dispatch operationalized them, not in what was ruled. Ruling 3 of the ninth-return sitting is
likewise untouched.

## 1. Ruling 1 — a dispatch declares its start state AT THE TREE IT WILL MEET, and the regeneration is an ordered act of Task 0 (Alternative A)

**THE DEFECT IS THE DECLARATION'S, NOT THE MEASUREMENT TOOL'S.** The tool correctly describes a tree
that holds the record; what was wrong is a dispatch declaring a start state it could not have had.
The amended dispatch therefore **declares the tenth batch's proven end state PLUS the reds its own
inputs cause, each named with its cause**, and **Task 0 commits the landed paths together with the
regenerated membership artifact**, so the check returns green in the same act.

**THE STANDING CLAUSE RIDING THE RULING, which is what stops the class rather than this instance: A
DISPATCH'S DECLARED START STATE IS STATED AT THE TREE THE DISPATCH WILL MEET, INCLUDING THE REDS THE
DISPATCH'S OWN INPUTS CAUSE, EACH NAMED WITH ITS CAUSE.** Its home is `cowork_audit_protocol.md`'s
dispatch-protocol section, beside the read-first clause the tenth batch landed there, with **F67**
recorded as its ground.

**THE GROUND FOR CORRECTING THE DECLARATION RATHER THAN THE DERIVATION.** This is Ruling 1 of the
tenth-return sitting's own shape at one remove — the four unresolved members were closed by
**correcting the records rather than the rule**, and this closes a false declaration rather than a
truthful tool. **Alternative B, deriving the population from the git index, was DECLINED and the
exclusion is recorded because an excluded alternative is evidence about the choice:** under it the
artifact would report 36 ruling records while 37 sat on disk, so a measurement whose subject is the
evidence a ruling was taken from would stop counting a ruling record that exists and was ruled
from — **#12**'s own direction of loss, and **#10**'s doc-truth half inverted. **Alternative C,
exempting the check, was DECLINED** as **#19**'s silent-failure direction: a standing exemption
teaches sessions that a red on this tool means nothing, and it would remove the one mechanism that
caught the false premise this time.

**★ THE RULING FIRES ON THIS RECORD IMMEDIATELY, AND THAT IS STATED RATHER THAN LEFT TO BE
DISCOVERED.** This record is a root-level `cowork_rulings_*.md` and is therefore inside the derived
population from the moment it reaches disk. **The amended dispatch's declared start state must
account for TWO untracked ruling records** — `cowork_rulings_2026_08_18_tenth_return.md` and this
one — and its assumption A1 must state the tree it will actually meet.

**WHAT THIS RULING DOES NOT DO.** It moves no verdict, no member, no route, no pin, no population
and no row. It changes no measurement tool. It does not touch the four unresolved members'
resolution, which is Ruling 2 and Ruling 3 below.

## 1b. Recorded with Ruling 1, not ruled by it — F71's class is unowned

`gen_evidence_pin_membership.py` has no argument parser, so every unrecognised flag performs the
write; it fired once inside the STOP and was restored to the committed blob in the same turn. **The
class — audit generators whose command line silently accepts an undocumented flag as the write —
is worth a sweep rather than a fix at this one tool, and no sweep is ordered by anything on the
record or by this sitting.** Recorded so it is not lost.

## 2. Ruling 2 — §6 kind 3 meant the CENSUS'S ACTUAL WRITER; that is corrected of record, and Ruling 1b's outcome is reached at the true shape (Alternative A)

**THE TREE HOLDS ONE OUTPUT EACH FROM TWO TOOLS, NOT TWO OUTPUTS OF ONE TOOL.** §6 kind 3 of
`cowork_rulings_2026_08_16_preparation_return.md` names `gen_artifact_inventory_surface.py` as one of
*"the two derived censuses"*. **Read at the code, the census is `gen_artifact_inventory.py`'s
output**, and the surface generator writes only the ratification document.

**THE ACT.** A **dated correction is APPENDED** at §6 of the 2026-08-16 record naming
`gen_artifact_inventory.py` as the census's writer, **the corrected wording preserved beside it
(#12), the ruled text never rewritten**. Ruling 1b's **premise sentence is corrected of record in
the same shape**, and **Ruling 1b's OUTCOME stands and is reached with no split inside any tool**:
the census keeps re-deriving under §6 kind 3 at `gen_artifact_inventory.py`, and the ratification
document's rendering is pinned under Ruling 1 at `gen_artifact_inventory_surface.py`. **Neither
ruling is superseded**, and the executing act writes each treatment at the tool it is actually
about.

**★ THE RIDER THAT BINDS THE EXECUTING ACT, AND IT IS NOT OPTIONAL.**
`gen_artifact_inventory_surface.py` has **TWO input routes** — the census file it reads, and a
citation scan it performs over the tree. **A pin that fixes what it reads fixes BOTH routes**, or
the rendering can still move when the tree moves and the pin will have frozen half its inputs. The
executing act states this at the tool rather than leaving it to be discovered.

**THE GROUND.** Writing both treatments at the surface generator, as Task 1 step 6 orders, would put
at that tool a sentence about a census it does not write — **#10**'s own doc-truth subject, and the
very defect Ruling 2 of the tenth-return sitting is being run to remove from three other surfaces in
the same dispatch. The outcome the user ruled is reachable and cleaner at the shape the tree has.

**THE COST ACCEPTED, RECORDED BECAUSE AN ACCEPTED COST IS NOT A DISCHARGED ONE.** This ruling reads
an intention into an earlier ruling of the user's. The reading is checkable at the objects — kind 3's
own words describe a census that regenerates, and exactly one of the two tools writes a census — but
reading intent is inference, and **D-436** reserves to the user what a derived cut carries. **That is
why it was put on a decision surface and ruled rather than derived.** **Alternative C, taking no
reading and leaving the member UNRESOLVED, was DECLINED**: staying is the recoverable direction, but
the objects answer the question unambiguously, and Ruling 1's own hazard runs while a member waits —
an unpinned ratification surface keeps regenerating, and one of the four did so inside the tenth
batch itself.

## 3. Ruling 3 — the pin derivation reads its sitting at the LANDING-COMMIT BOUND, derived per member at the objects and cited as a derivation (Alternative B)

**THE DERIVATION AS SHARPENED: the last commit touching that surface which is dated at or before THE
COMMIT THAT LANDED THAT SITTING'S RECORD**, the landing commit **derived per member at the git
objects**. Ruling 1 of the tenth-return sitting is not amended in its purpose — this is the same
rule read at the granularity the evidence supports.

**THE GROUND, MEASURED ON THE MEMBER THAT FORCED IT.** Four commits touched
`ratification_surfaces/cowork_rulings_sort_surface_2026_08_16.md` on the sitting's own day. A
date-granularity reading selects `15dfb0e172` at 23:13:00 — **after** the sitting record landed at
16:34:06 and **after** the sitting's own executing act at 18:14:23. **It pins a POST-RULING
rendering and records it as the evidence of what was PUT in front of the user — the precise defect
Ruling 1 exists against, arriving on the exact member Ruling 1's own ground names as its reason for
adding the date bound. The bound fails on its founding case.** Under the landing-commit bound the
selection is `53e552296f`, interval ≤ 6 h 34 min, and `gen_rulings_sort.py`'s next commit is
`a21a55fc12` at 18:14:23 — after the bound — so neither the document nor its generator could have
moved inside the interval.

**FOR THE OTHER THREE MEMBERS THE TWO READINGS SELECT THE SAME COMMIT**, so this ruling changes the
outcome for exactly one member. The executing act **re-derives all four at the objects** rather than
inheriting the three the writing side could not verify.

**★ THE CITATION IS CORRECTED WITH THE RULING, AND THIS HALF BINDS HARDEST.** The executing act
**cites its own per-member derivation at the objects and does NOT cite D-230** for the bound — D-230's
text is the decisions register's rule (c) and does not carry it (**F72**). Under **D-643**, a claim
invoking a ruling as an application of it quotes that ruling in full.

**THE FORWARD CLAUSE RIDING THE RULING: A SITTING RECORD IS AN INTERIM CARRIER, WRITTEN IN THE TURN
ITS RULING IS GIVEN AND LANDED IN GIT AT THE NEXT DISPATCH'S TASK 0 — SO THE COMMIT THAT LANDS IT
BOUNDS WHEN THE SITTING WAS HELD.** Its home is `cowork_audit_protocol.md`'s dispatch-protocol
section, beside Ruling 1 of this sitting's clause and the tenth batch's read-first clause, with
**F72** recorded as its ground. **This writes down a practice the record has followed throughout and
never stated.**

**THE RESIDUAL RISK IS RESTATED, NOT REMOVED.** The published interval still does not exclude a
regeneration that was never committed inside it. That is the risk Ruling 1 of the tenth-return
sitting states in terms, measured per member and published with each pin, and it is unchanged here.

**THE TWO ALTERNATIVES DECLINED.** Keeping the date reading was declined because it defeats Ruling 1
on its founding case. Leaving the member UNRESOLVED was declined because the derivation **can** be
established — what fails is one reading of the rule, not the evidence — and **#19** used to avoid a
decision rather than to guard one leaves a derivable fact recorded as underivable.

## 4. Ruling 4 — the generator is EXTENDED by two key chains; four values are corrected by citation and only the pre-ninth-batch total and its percentage are withdrawn (Alternative A)

**THE EXTENSION IS TWO KEY CHAINS, NOT A NEW CAPABILITY, AND THAT IS WHAT DECIDES IT.**
`gen_session_start_read_size.py` already reads the artifact the two values live in and already
carries `key_span_characters(text, keys)`, general over a key chain and already called twice with
different chains. The 216 rows' grounds and the frozen comparison are **named key spans in that same
artifact** — `★_the_live_gating_answer` → `the_gating_rows`, and `★_the_live_gating_answer` →
`★_the_frozen_enumeration_measured_against_this_one`.

**THE MECHANISM FREEZE IS ANSWERED RATHER THAN IGNORED.** The freeze bars tool work **that does not
block the work**, and Ruling 3 of the tenth-return sitting's ordered act is blocked without this.
The admission is the same shape the tenth batch declared at its own §9, where a whole new
measurement tool was built for exactly this reason. **The work is DECLARED AS A JUDGMENT at the
site, in the commit message and in the close** — never slipped in.

**WHAT IS CORRECTED AND WHAT IS WITHDRAWN.** Four of Ruling 3's five values are corrected **by
citation to `tools/audit/session_start_read_size.json`** (D-431) — the total, the section, the
identity list, and the grounds — together with the frozen comparison, which the extension also
reaches. **The pre-ninth-batch total and the −45.1 % derived from it stay WITHDRAWN**, refused by
the tool's own source in its own words: the read regime before the ninth batch named a different
member set and rule (a) named no artifact pointer at all, so forcing it would compare two different
questions. **Ruling 3 of the tenth-return sitting's refusal is not weakened; its reach is
measured.**

**★ THE RIDER THE EXECUTING ACT MUST SETTLE.** Whether *"the grounds"* as Ruling 3 names them is
exactly the `the_gating_rows` span, or the 216 rows' recorded grounds within it, is stated at the
executing act. **If the two differ, the value published is the span the tool measures, with the key
chain named beside it** — which is the standing figure clause working rather than an exception to
it.

**THE TWO ALTERNATIVES DECLINED.** Withdrawing all three unreachable values was declined because two
of them are derivable at a cost the record has already accepted once, and withdrawing a value whose
derivation exists and was not taken is a different act from withdrawing one that cannot be derived
at all; it would also execute Ruling 3 more narrowly than it was ruled, without a ruling narrowing
it. Extending the tool to reach the pre-ninth-batch regime as well was declined at the tool's own
declaration and at the dispatch's own branch — *"a finding to report and not a licence to
compute."*

## 5. Writing-side findings recorded at this sitting

Numbered under Ruling 2's allocation point of the ninth-return sitting. The executing side allocated
**F67–F71** at the eleventh batch; the writing side allocates from **F72**. Both below are the
writing side's; the row bar stands and neither is rowed.

- **F72 (new) — A DERIVATION'S BOUND WAS GROUNDED ON A RULING WHOSE TEXT DOES NOT STATE IT.** The
  landing-commit bound proposed as F69's remedy is attributed to **D-230**; D-230's verbatim text is
  the decisions register's rule (c) — *"a new ratification, shelving or falsification gets its
  register entry (data + regenerated files) IN the commit that records it"* — and it says nothing
  about a sitting record, an interim carrier or a dispatch's Task 0. No entry in the decisions
  register's INDEX states the practice either. **The practice is real, uniform across this arc and
  derivable per member at the objects; what is missing is a rule the record carries.** **The general
  form: a practice every session follows and every record cites is not thereby a rule, and a claim
  invoking a ruling as its authority must be checkable at that ruling's own text (D-643).** Remedied
  by Ruling 3's forward clause; it rides to the retrospective because the general form outlives it.
- **F73 (new, small) — AN ALTERNATIVE'S COST WAS CHARACTERISED WITHOUT BEING MEASURED.** F70 names
  *extend the generator* as one of two lawful exits and characterises it as tool work the standing
  mechanism freeze bars, without establishing the size of the extension — and the size is exactly
  what the freeze's own test turns on, the freeze barring tool work *that does not block the work*.
  **The general form: where a rule's test is a threshold on cost, an alternative is declined or
  admitted on a MEASURED cost, and a characterisation is not a measurement (#19).** Remedied by
  Ruling 4.

**F1–F71 ride to the preparation phase's retrospective unchanged**, with the E3 ordering defect and
the A1 premise error. **F3 is thirteen times surfaced, unfixed and unrowed** —
`reaim_home_anchors.py --check` exits 0 while printing drifted anchors, and
`gen_cluster_dispositions.py --verify` is the drift authority.

## 6. What follows

**The amended dispatch's work from this sitting**, in the order the rulings bind. Its **Task 0**
lands the tenth-return record, this record, the amended dispatch, the pre-registered verification
plan, the executing side's report `cc_report_preparation_eleventh.md`, the handover block carrying
this close, **and the regenerated `tools/audit/evidence_pin_membership.json`** (Ruling 1); its
assumption A1 states the tree it will actually meet and its declared start state names the reds its
own inputs cause. Then: the four members' commits **re-derived at the objects under the
landing-commit bound** and written as dated corrections into their four ruling records, the pins
applied and any unestablished member left UNRESOLVED, the forward clauses written at
`cowork_audit_protocol.md`'s dispatch-protocol section, and §6 kind 3's correction and Ruling 1b's
split written at the two tools they are actually about, with the surface generator's **two** input
routes both pinned (Rulings 1, 2, 3); the three banner qualifications written at their generators and
rendered, **after** the pin acts, each document's difference measured and confined (Ruling 2 of the
tenth-return sitting, unchanged); the generator extended by two key chains and the correction of
record appended at both sites with the percentage withdrawn (Ruling 4).

**The sittings and outputs still awaiting the user, in the standing recommended order:** the ONE
remaining caller whose kind the derivation cannot place, published as data; the **curated boot list
DRAFTED for ruling AT ITS RULED DEFINITION** — the implementation-free read list an
implementation-blind session boots from, for those sessions only; the empirical findings ledger
behind its fact-gate; the archiving wave read at the REGENERATED census, never at a stale one; and
the mining and landing of the newly visible instruction files.

**The bars stand:** no open-items row created, flipped or discarded; [[OI-179]] OPEN and GATING;
[[OI-372]] the one standing red; [[OI-374]] untouched; no candidacy acted on; no document archived,
moved or deleted AS A FILE; no mining, no fact-gate admission, no findings ledger; nothing under
`src/`, `tools/corpus/` or `tools/robust_stop/`; no golden, no test, no measurement of the analysis.

*Provenance: Cowork, 2026-08-18, the eleventh batch's STOP sitting, the user present and ruling in
conversation. The user's words are quoted verbatim in §0. The batch's state was verified at the
objects by the writing side before the sitting opened, and the four establishments each ruling rests
on are recorded in §0. This record is written in the remote Cowork environment, where the repository
is reached through the device bridge: every establishment in §0 was taken by git OBJECT read at the
explicit hash `9390e2ca2c` or by a per-path git history query, and no working-tree file was read
through a shell — every file read was read from a snapshot staged through the bridge and opened with
the file tools. `git status` was NOT run — the recorded index-refresh hazard on this mount, and the
standing rule bars it. **Three departures are declared:** the branch tip was read (`git rev-parse`,
`git log`) to establish the batch's state, there being no close to supply an explicit hash; a
per-path git history query was used for the rulings-sort establishment at §0(c); and the follow-up
walk over the other three members' surfaces **TIMED OUT on this mount and was not retried**, which
is why Ruling 3 orders all four re-derived at the objects rather than inherited. This record was
written by delivering the file through the bridge and round-tripping it back byte-identical, never
by shell redirection (the recorded 2026-08-02 slip).*
