# Rulings — the cascade sitting, 2026-08-25 — THE BLAST RADIUS IS PAID ONCE, BY SWEEP TO A FIXPOINT

> **STATUS: RULING RECORD.** Cowork, 2026-08-25 (the forty-fifth Cowork session, its fifteenth
> sitting). An interim carrier under the standing clause that a sitting record is written in the
> turn its ruling is given and lands in git at the next dispatch's Task 0.
>
> **Taken at branch tip `9b1b0a02943fd047ab0c92ef817e8b81e52cf5a3`** — the regeneration-and-citation
> batch's second commit — **after that batch's return was verified at the objects by this session.**
>
> **★ THIS RECORD ORDERS ONE DISPATCH.** It decides nothing about the derivation method, the re-run,
> or the plan's phases.

---

## 0. The return, verified at the objects — and THREE CORRECTIONS OF RECORD

**Verified, not relayed.** Both commits exist as reported — **`744ed4a708d3a3cf1c6764ccf2bf6ab33fa5aa2b`**
with **exactly five** paths, then **`9b1b0a02943fd047ab0c92ef817e8b81e52cf5a3`** with **exactly
three**, the second's parent the first. **Both writing-side files are byte-identical at the committed
objects.** The `OI-376` row now reads `D-438` with the inline correction note, and the row-level diff
shows **it is the only row that moved**. The guard summary at the tip reads **`{run 75, passing 73,
failing 2}`** with the two tools named. The regenerated artifact's `gating_ids` carries **217**
identities and contains `OI-376`, **placed on the gating side by the outside-the-cut ground** — so
this side's published expectation is no longer a reading of the source but a measurement.

**★ AND THE BATCH FALSIFIED A3 BY EXPERIMENT RATHER THAN BY ARGUMENT**, which is the better order: it
regenerated with the correction, reverted the row to its exact pre-correction bytes, regenerated
again, and got a **byte-identical** artifact. **Recorded with approval.** Its STOP at Task 5 was
correct and nothing was fixed.

**Correction 1 — the stale `216` stands at THREE sites, not one.** The report names `FURTHER_SPANS`.
At the object, `tools/audit/gen_session_start_read_size.py` also carries it in its **module
docstring** (line 34) and inside the published **`★_the_further_spans_and_why_they_are_here`** block
(line 298). **Two of the three are published INTO the artifact**, so a reader is told `216` twice by
a file whose own run derives `217`.

**Correction 2 — `core.autocrlf` is NOT set in this clone.** `git config --show-origin --get-all
core.autocrlf` returns nothing, exit 1. **The mechanism is `.gitattributes`**, which marks these
paths `text: auto` (`git check-attr`), normalising to CRLF in a Windows worktree. **The EFFECT the
report warns of is real and verified** — that artifact's blob is **173,748** bytes against a
**176,351**-byte worktree copy — so the warning about comparing a `git cat-file blob` hash against a
worktree hash stands and is adopted. **Only the cause was misattributed, and it matters because it
changes where a later dispatch would look.**

**Correction 3 — A1 reconciles, and the report was right to flag it.** Under this side's pattern the
entry-heading counts run **44 → 48 → 49** across the three commits; under the report's pattern, **62
→ 63**. **Both say ONE entry this cycle**, and the populations differ by matching pattern, not by
fact.

## 1. What the return establishes that is bigger than the batch

**Three consecutive batches have each turned one further derived artifact red, and each link cost a
full dispatch.** The chain is now visible and measured: **opening one register row moves
`gating_ids`** → **which moves `nongating_apparatus_rows.json`** → **which is an INPUT SPAN to
`session_start_read_size.json`.**

**★ THIS IS THE MECHANISM D-641's OWN GROUND NAMES — the apparatus generating its own defect stream —
except that here it is generated by CORRECTLY OBEYING register rule (c).** Rowing a finding, which is
the ruled routing, costs an unknown number of downstream regenerations, discovered one per batch.
**That is the thing this sitting fixes, and it is not fixed by reasoning about the graph.**

**Measured at the objects as a LEAD, not as an answer.** Only two generators read the answer key
`★_the_live_gating_answer` — the producer and `gen_session_start_read_size.py`. **But at least eight
further generators read the artifact by NAME** (`gen_discard_records.py`, `gen_guard_classification.py`,
`gen_guard_state.py`, `gen_phase1_completion_inventory.py`, `gen_phase1_finish_line.py`,
`gen_recognizer_establishment_sort.py`, `index_status_lint.py`, `claude_md_rule_triage.py`, and the
two under `tools/audit/decisions/`), and **whether any of them consumes a key that moves is exactly
what reasoning cannot settle.**

## 2. Ruling 1 — the closure is paid ONCE, and it is discovered by SWEEP TO A FIXPOINT, not by static analysis (Alternative A)

**Ruled.** One dispatch does the following, in this order:

1. **The `216` finding is ROWED** at the next free identity, **measured** across both the INDEX and
   `open_items/`, under register rules (c) and (e) — the same routing as `OI-376`, **no finding
   number, no apparatus declaration, no gating verdict, no remedy.**
2. **THEN the sweep runs.** Run the guard set; **regenerate every tool whose failure is a STALENESS
   — the artifact does not re-derive — and ONLY those**; run the guard set again; repeat **until the
   failing set is the standing red alone, or a round makes no progress.**
3. **Commit the record, the handoff, the new row and its detail file, and every artifact the sweep
   regenerated, in ONE commit.** Then the final guard run lands in a second.

**★ THE ORDER IS THE POINT AND IS PART OF THE RULING: THE ROW IS CREATED BEFORE THE SWEEP**, so the
new row's own cascade is absorbed by the same sweep instead of starting a fourth batch. **A row added
after the sweep would re-open everything the sweep had just closed.**

**★ AND THE ONE BAR THAT MAKES THIS SAFE — IT IS ABSOLUTE.** **A red is regenerated ONLY when its
failure is that the artifact no longer re-derives.** **A red that STOPs demanding an authored
decision is NEVER regenerated** — the standing red [[OI-372]] is exactly that shape (*"derived
candidates with no authored verdict … an unclassified candidate is a STOP, never a silent pass
(D-661)"*), **and regenerating such a tool would convert an unanswered question into a silent pass.**
**If any red of that shape appears that is not the standing one, the sweep STOPS and reports.**

**The alternatives declined, by their ratings.** **B** — regenerate `session_start_read_size.json`
alone and stop again if it cascades: declined, it is the third batch buying one link at full price
when the shape is already known to recur. **C** — leave the red standing, recorded as caused and
understood: declined, at two reds the guard set stops being a signal, and the standing red is the
live proof of how a known red becomes furniture. **D** — row the `216` and regenerate without the
sweep: declined, it guarantees a fourth cascade batch if anything else consumes that span.

## 3. Ruling 2 — the `216` is ROWED AS A FINDING ONLY, with NO remedy ordered (limb (i))

**Ruled.** `gen_session_start_read_size.py` carries a **hand-transcribed count of a figure the same
run derives** — `216` at three sites, two of them published into its artifact, against a derived
`217`. **That is the shape #17f and D-431 forbid: a measured figure restated by hand.**

**It is rowed and nothing else is done to it.** **No remedy is ordered, and the transcription is NOT
updated to `217`.** *Why the obvious repair is declined:* **the standing mechanism freeze bars tool
work that does not block the work**, and **updating the number would re-commit the very defect** — a
hand-count that goes stale on the next row. **The right repair is to remove the transcription and
let the figure enter by citation (D-431), which is a DESIGN question with real alternatives, not
hygiene** — and it gets its own surface if and when the user calls for it. **The row records the
defect; it does not pre-decide its cure.**

**Recorded with it, because it is the sitting's own irony:** rowing this finding will itself move the
open population and so move the artifacts the sweep is about to settle. **That is precisely why
Ruling 1 orders the row FIRST.**

## 4. What is HELD, and what this sitting does NOT do

**HELD, unchanged:** the method ruling stands **VOIDED**; the framework phase and the
detail-specification phase do not open; the tests batch (a)+(b) stays held, and whether **(b)**
survives on its own ground is **still not decided** and remains the one surface genuinely owed. **The
empirical findings ledger remains owed before the framework phase** and is the standing alternative
act.

**★ THE RE-RUN IS UNBLOCKED AND IS THE USER'S TO OPEN AT ANY TIME.** It does not wait on this
dispatch and this dispatch does not wait on it.

**Not done:** no session is booted by this side; no derivation, no comparison, no oracle opened; no
blind output read beyond its bounded receipt; **no pack, generator, manifest, withheld family, brief
or governing document is touched — `CLAUDE.md` above all**; **no tool source is edited**, the `216`
included; **[[OI-372]] and its tool are not touched and are never regenerated**; `OI-376` is not
re-opened, re-worded or given a verdict; no finding number is allocated (**Ruling 9 opens no
series**); [[OI-179]] stays OPEN and GATES; [[OI-374]] stands as found. **The three deferred
apparatus items stay deferred.** **The large pre-existing untracked population is not re-litigated
and nothing of it is committed.**

---

*Provenance: Cowork, 2026-08-25, the forty-fifth session's fifteenth sitting, at tip `9b1b0a02`.
The return was verified at content-addressed git objects — both commits and their path lists, both
landing blobs' hashes, the corrected row's text and the row-level diff, the guard summary, and the
regenerated artifact's `gating_ids` length and membership. The three corrections of §0 were each
measured this turn: the `216` sites at the tool's source, the attribute state at `git check-attr`
and `git config --show-origin`, the blob-versus-worktree sizes, and the entry-heading counts under
both patterns. §1's consumer list was measured at the objects and is carried into the dispatch as a
LEAD, never as the closure. The user's word: "Agree with your recommendations" — against this side's
stated choices A, limb (i).*
