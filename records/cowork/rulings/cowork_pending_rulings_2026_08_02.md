# Pending rulings — ratified in conversation 2026-08-02, to be APPLIED the moment phase 1h lands

> **STATUS: RULED BY THE USER, APPLICATION HELD** — phase 1h is mid-flight on the register
> surface, so these are carried here (never conversation-only) and land in ONE commit right
> after 1h's report is verified. This file is superseded by that commit and then removable.

## 1. The LEGACY-marking convention (user, 2026-08-02) — a standing rule

Every register entry — and any statement presented to the user — whose SUBJECT is the legacy
surface (the dormant pipeline awaiting deletion at the retirement map) is EXPLICITLY MARKED
LEGACY, so a ruling about soon-deleted code can never be mistaken for one about the live
solution. Application, in order:
(a) one clause appended to `CLAUDE.md`'s decisions-register section: *"(f) an entry whose
subject is the LEGACY surface is explicitly marked LEGACY — a reader must never mistake a ruling
about soon-deleted code for one about the live solution (user, 2026-08-02)"*;
(b) the register generator renders a visible LEGACY badge for entries carrying a new
`legacy_subject: true` field;
(c) the classification sweep over all 400+ entries to set that field (a CC act in the next
dispatch, the marked set presented for the user's confirmation);
(d) immediately at application time: D-317, D-318, D-342 and the phase-1e/1f legacy-subject
entries already known (D-278's subject among them) get the field set.

## 2. D-317 and D-318 — RATIFIED, with rephrased plain restatements (verbatims untouched)

- **D-317 plain:** "LEGACY (the analyzer awaiting deletion): a one-tick boundary fix was tried
  and closed — counting notes that stop exactly where a stretch begins as belonging to that
  stretch, in the hope of recovering a missing chord root. Measured: the boundary-touching notes
  are other chord tones (the root attacks later), and five display paths depend on the current
  convention. A boundary-membership dead end ONLY — it says nothing about extending the temporal
  context the analysis reads, which is a decided live capability (the extensible working span,
  D-030)."
- **D-318 plain:** "LEGACY (the segmenter awaiting deletion): a proposed after-the-fact pass
  merging very short neighbouring stretches was tried and closed — measured, its trigger never
  fires, because the earlier inline same-root merge has already combined them. A prohibition on
  re-adding one redundant merger pass — nothing about collecting notes over time or extending
  context."
- Both `status_source` gain: "★ RATIFIED (user, 2026-08-02) as LEGACY-scoped dead ends with no
  effect on the going solution; plain restatement rephrased at the user's direction to preclude
  the temporal-context misreading."

## 3. D-342 — RATIFIED, with the clarifying addition

Plain gains: "THIS IS NOT THE INFERENCE-ENGINE SWITCH — that happened (D-010): the joint
estimator is production and the legacy pipeline is dormant. This entry concerns the separate
Layer-5 function-annotation module, built and validated but never in production in either era.
Its concerns are handled by the LIVE implementation natively or by schedule: degree-in-key
Roman numerals are the estimator's own state; key changes are decided inside the decode;
applied-chord labels are emitted by the live renderer (the D-248/OI-267 revisit covers the
remainder); cadence is a fitted factor inside the model and a marker on the presentation
surface; carried-abstention resolution is obviated by the full-posterior publication (D-006);
the ornament labels are the ratified OI-194 increment; and the complete concern-by-concern
mapping of the legacy layer's remaining scope is the OI-259 phase-3 re-disposition."
`status_source` gains: "★ RATIFIED (user, 2026-08-02) after the live-handling question was
answered (the concern mapping above); LEGACY-subject marked."

## 4. The sixth ratification event (register preamble)

"Sixth ratification event, 2026-08-02: the 26 phase-1g entries (D-317…D-342) — D-319…D-341
directly; D-317/D-318 with rephrased plain restatements under the new LEGACY-marking convention;
D-342 with the live-handling clarification. The ratification is of each RULE itself; homes and
provenance are bookkeeping."

## 5. OI-268 — RATIFIED (user, 2026-08-02): the fifth home case exists

A **ratified contract document** is a proper home for a decision, under three criteria, all
required: the document is user-ratified or signed; it carries a status banner; and the owning
`ARCHITECTURE.md` section POINTS to it. Where the pointer is missing, the POINTER is the
documentation gap — never a copy of the content (one home per concern, #6; the two-clock drift
of copies is the rejected alternative). The register's home marker gains the fifth case; the
51 design-surface entries re-classify under it; the banner discipline over those documents joins
the remedy (the 1g triage's 101 unbannered files). Row OI-268 closes with this application.

## 6. OI-272 — RATIFIED (user, 2026-08-02): the per-kind homing scheme

Per kind, for the tracking-surface and design-surface recorded decisions: **live constraints and
policies** → homed into the owning specification (an `ARCHITECTURE.md` section or an OI-268
contract document); **shelvings, falsifications and dead ends** → NEVER become live
specification statements — home is the register plus the evidence document, with at most a
one-line pointer in the owning specification where a future designer would look ("tried and
closed — D-nnn"); **process rules** → `CLAUDE.md`/the audit protocol; **measurement
conventions** → the gate block. The remedy requirement: the discoverability fix is the POINTER
from the surface where a reader actually works (attacking the "recorded three times, none a
session-start read" property), never more copies. The homing/pointer pass is a CC act in the
next dispatch; row OI-272 closes with its delivery.

## 7. OI-275 — RATIFIED (user, 2026-08-02): reading (1)-with-transfer (the D-266 pattern)

**The PRINCIPLE of D-329 BINDS the family design:** candidate admission is complete by default;
any prune must be derived from the model, measured for established loss, and ratified — the
factorization's own reserve clause, so the record converges. The document's LETTER stays home to
the legacy scorer and is LEGACY-marked under ruling 1 of this file. Application: D-329 gains the
transfer annotation and stands LIVE as the family design's admission premise; OI-275 closes;
the family rows (OI-215/226/227/228/243/244) gain a dated note naming D-329 as the premise their
one design must satisfy.

## 8. OI-260 — RATIFIED (user, 2026-08-02): remove the retired Gate K line

The one-line `CLAUDE.md` gate-threshold-policy correction rides the post-1h `CLAUDE.md` commit
with rulings 1(a) and the session-start additions. Row OI-260 closes with it.

## 9. Also queued behind 1h (unchanged)

The probe findings' rowing (`cowork_candidate_findings_2026_08_02b.md`), the D-248/OI-267 scope
reconciliation, the triage generator's acceptance banner, and 1h's own ratification queue
(D-343 onward) for the user.
