# Rulings — the sizing leak-list sitting, 2026-08-24

> **STATUS: RULING RECORD.** Cowork, 2026-08-24 (the forty-fifth Cowork session, its eighth
> sitting). An interim carrier under the standing clause that a sitting record is written in the
> turn its ruling is given and lands in git at the next dispatch's Task 0.
>
> **Taken at branch tip `24d7f0be93`** (both refs at it), **after the sizing-pack batch returned
> complete and was verified at the objects by this session** (six commits `ac81167005` →
> `7c1119dcdb` → `2fa17e669d` → `ed1c1962f8` → `19081fb7e7` → `24d7f0be93`, the last two declared
> corrections; the landed records byte-identical to the delivered ones; membership 54→56; guard
> 75/74/1; the scoring-model pack at A5 on every re-measured axis — leaks 3, 241 rendered, zero
> markers in member (2), the harmony-boundary subject byte-unchanged everywhere; license (i)
> measured unnecessary; license (ii) taken as a derived rendering with the byte-identity proof).
> The objects ruled on are
> `ratification_surfaces/cowork_sizing_pack_leak_list_reading.md` at the tip, read whole, and the
> two manifest-prose defects the report's §6.2–6.3 surfaced.

---

## 0. What was put, and in what form

The verification was delivered whole, including this side's own repeated error owned of record —
the dispatch's A1 again stated the handoff from what this session had delivered rather than from
the tree (the fifty-first entry had no dispatch to land it; the committed newest was the
fiftieth); Claude Code measured the difference, closed the arithmetic, and the declared STOP
rightly did not fire. The leak-list surface was then delivered with its alternatives rated
against the ultimate objective and the guiding principles, plus one limb for the two surfaced
manifest-prose defects. The user ruled in their next turn: **"A i - ok"**.

## 1. Ruling 1 — the leak check keeps excluding D-270, D-296 and D-440 from the `scoring-model` pack (Alternative A)

**Ruled.** The check applies to this subject as to every subject: each of the three entries
carries, in its own quoted words, a live repository path (`docs/score_inventory.md`;
`src/notation` and `src/engraving`; `src/llm/`), and the check exists to close routes from the
pack into the implementation — the blindness the sizing depends on. The 2026-08-22 class ruling
(LIST FOUR, "A") stands unreopened, its two declined alternatives declined still.

**The alternative declined.** B — admit the three for this subject: hands the deriving session
live paths into `src/` and `docs/` for no content it needs; contradicts the ruled class without
new grounds.

## 2. Ruling 2 — limb (i): the two manifest-prose corrections are LICENSED in the next dispatch (the user's word: "i")

**Ruled.** Two bounded generator corrections, each maintenance of a delivered artifact's
truthfulness (the same class as the read-me's license (ii)), are licensed in the next dispatch
and no further: **(a)** the manifest's candidate-criterion block renders truthfully for a subject
whose criterion is EMPTY BY RULING — stating that no search was run because the ruling empties
the family, rather than describing a pattern match that never ran; derived from the authored
entry, not switched on a subject name, with the harmony-boundary subject block required to
re-render BYTE-UNCHANGED as the proof; **(b)** the generator's top-level
`the_rulings_it_executes` list gains the sizing-pilot ruling's record name, making the list
complete rather than incomplete.

**The alternative declined.** (ii) — leave both as reported facts: costs nothing today, but a
generated artifact then reads as though a search ran that never did — the shape #19 dislikes —
and the incomplete rulings list misstates the tool's own authority.

## 3. What this sitting orders, and what it does NOT do

**Ordered:** one dispatch — Task 0 lands this record, the SIZING BRIEF
(`cowork_blind_session_brief_scoring_model.md`, written by the writing side; landed, not written,
by Claude Code), the modified handoff and the dispatch, and regenerates the evidence-pin
membership; Task 1 takes the two licensed corrections of Ruling 2 and re-renders, with the
harmony-boundary manifest block and BOTH pack directories byte-unchanged; Task 2 closes. **After
it lands and its return is verified, the brief's open §8 points come to the user, and the fresh
Cowork session derives the sizing subject.**

**Not done:** no session is booted; no derivation; nothing of the harmony-boundary subject
touched; no finding number (F88); no open-items row; [[OI-179]] OPEN and GATES; [[OI-372]],
[[OI-374]] as found. The three excluded entries' standing in the decisions register is untouched.

---

*Provenance: Cowork, 2026-08-24, the forty-fifth session's eighth sitting, at tip `24d7f0be93`.
The reading file and the report read whole from hash-verified staged snapshots; the pack
measurements re-taken at the tip objects. The user's words: "A i - ok".*
