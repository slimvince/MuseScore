# Rulings — the L2 boot-list sitting, 2026-09-05

> **STATUS: RULING RECORD.** Cowork, 2026-09-05, the session that booted on
> `cowork_handoff_entry_one_hundred_and_nine.md`. An interim carrier under the standing clause that a
> sitting record is written in the turn its ruling is given and lands in git at the next dispatch's
> Task 0.
>
> **Taken at branch tip `911f5f7cdaa3fb53b9b5a2bdefb82e793c65eafb`** (`refs/heads/master` and
> `refs/remotes/origin/master` both at the tip, read with the file tools). The object ruled on is
> `cowork_l2_boot_list_surface_2026_09_05.md` (untracked at the repository root; 41,230 bytes at the
> directory listing), delivered to the user as a file and then, on his instruction, re-delivered as
> plain text in the message: each decision with its checked facts, its weighing towards the ultimate
> objective and towards the guiding principles, and its recommendation with the reason. No document
> ruled here is generated.

---

## 0. What was put, and in what form

Nine decisions on one surface, self-contained, the referents explained from scratch, every fact read at
the object named beside it, every position weighed on the two axes, a recommendation on each with the
strongest argument against it. The surface's own fact check was delivered as a turn of its own before the
plain-text delivery (six corrections, named there). **The user's words, verbatim: "Agree on all 9".**

**The form, recorded exactly.** The choice questions were to come one decision per turn after the surface
was read (the standing form). **The user disposed directly upon the surface, pre-empting the question
turns** — the same declared shape as Rulings 11, 12, 13 and 81 of the 2026-08-31 record. What the form
exists to secure — that the whole surface is read before it is answered — holds, the answer having come
after the surface was delivered twice; what did not happen is the asking. **No choice question is owed
and none was put.** Each ruling below takes the surface's recommended position; where the surface stated
a condition inside a recommendation, the condition is part of the ruling.

## 1. Ruling 1 — L2's boot-list members are ruled now; the research-extract member is ruled as a CLASS; the pack is rendered only after L2's slice of Task B is read

The standing gate — no derivation before the subject's slice of Task B is in (the eighty-seventh handoff
entry; Ruling 10 of `cowork_rulings_2026_08_31_decision_surface_sitting.md`, "what it does not do") —
**stands and is not lifted.** L2's slice is not read: at `reading_pass/candidacy_upgrades.md`, 47 rows
admitted, 5 read (the L1 slice: rows 4, 7, 37, 38, 39), 5 unreadable from here (rows 2, 6, 9, 22, 44),
leaving 37 admitted, readable, unread rows, an upper bound on L2's slice. Which of the 37 are L2's rather
than L3's is a derivation this side makes and puts to the user before any reading is dispatched.

**Ruled.** The members of L2's pack are ruled by this sitting (Rulings 2 to 6). The extracts member is
the L2 slice's extracts as they stand when the pack is rendered, each with its conclusion section cut,
**the file names published to the user before rendering.** The pack-build dispatch is written when it is
next (D-250), which is after the slice is in.

**Declined:** deferring the members decision until the extracts exist by name (a second surface for the
same outcome); rendering now with no extracts member (not available without a ruling lifting the gate).

## 2. Ruling 2 — the extracts member carries TWO populations, each with its conclusion section cut

**Ruled.** *(i)* Task B's L2-slice extracts under `reading_pass/extracts/`, with the section headed
*"…detail specification could adopt, adapt, or must argue against"* cut — the `l0-l1` mechanism and cut
string unchanged. *(ii)* The first reading pass's extracts whose paper bears on L2, with the section
headed *"Bearing on the framework…"* cut — a second cut string in the generator's `cuts` filter, verified
both ways on every render as the first is. **The L2-bearing subset of the first-pass extracts is derived
by this side and put to the user by name before rendering**; it is not ruled here by name.

**Facts the ruling rests on, at the objects:** twenty first-pass extract files and five Task B files under
`reading_pass/extracts/` (the directory listing); seven at-the-object re-reads under
`reading_pass/object_reads/`; the two heading forms read at
`reading_pass/extracts/mcleod-rohrmeier-2021-modular-harmonic-analysis.md` and
`reading_pass/extracts/pardo-birmingham-2002-algorithms-for-chordal-analysis.md`; P2 of Ruling 13
(2026-08-31) leaves the session free to read the primaries.

**Declined:** Task B's extracts only (leaves the closest published relatives of L2's charter out although
read); no extracts (excluded with Ruling 1's third position).

## 3. Ruling 3 — the charter member is `l0-l1`'s member (7), unchanged

`FRAMEWORK.md` §5 and §9, whole, with exactly the three authored removals the generator's `EXTRAS["l0-l1"]`
carries (§5's second-axis provenance parenthetical; DP-N's and DP-Q's stage-two parentheticals). The
alternatives had collapsed to one: this side searched §5 (lines 288–493) and §9 (lines 620–817) for every
other mention of this project's own material and found only DP-K's two research grounds and L1's
release-clause note about the draft's own history, neither implementation content. **The user did not
take the option of keeping the DP-N and DP-Q parentheticals for L2.**

## 4. Ruling 4 — the empirical findings ledger enters whole and unfiltered

By the phase definition's own naming (§3.4 of the phase definition surface, as Ruling 12's correction of
record cites it), not by any decision of this sitting. The `l0-l1` member (9) shape, unchanged.

## 5. Ruling 5 — SIX passages of `CLAUDE.md` are withheld from L2's member (2)

**Ruled.** `WITHHELD["l2"]["withheld_passages"]` carries, each in the D-677 shape (scope anchor, opening
and closing anchors by text, finding, date, reason; a drifted anchor STOPs the tool):

- **(a)** the pilot's first passage — the founding-instance clause of the never-work-from-memory rule
  (scope `**NEVER WORK FROM MEMORY INSTEAD OF DOCUMENTED FACTS`, opening `**Founding instance:** on
  2026-07-28 Cowork reasoned`, closing `which is the general case, not the exception.`), with L2's own
  finding: it states the ruled evidence ranking, the first limb's answer (D-057, IN);
- **(b)** the pilot's second passage — the design-defense founding instances (scope `**EVERY DESIGN
  DECISION CARRIES ITS DEFENSE AT ITS HOME`, opening `Founding instances of the gap:`, closing `each
  recorded with no derivation.`), with L2's finding: it names the decode segment cap's value, an L2 value
  no verdict reaches because D-004 is not a candidate of the family;
- **(c)** in principle #17, the block opening `**★ EVERY DESK-SIMULATION TRACE RUNS AT IDENTITY WEIGHTS`
  — states the ruled score shape (counted tables, fitted weights, an equal-weights ablation arm; D-525's
  content, IN);
- **(d)** in the conventions, the bullet opening `**CANDIDATE ADMISSION IS COMPLETION, NOT REFINEMENT` —
  states what this project's current admission rule is (unspecified, threshold-based) and names the joint
  decoder;
- **(e)** in the conventions, the bullet opening `**ONE FIX IS DESIGNED ONCE OVER THE WHOLE ENUMERATED
  FAMILY` — states current failure behaviour of the decoder and its emission;
- **(f)** in the conventions' issue-exhaustion block, the D-437 qualification's family sentence (the
  sentence beginning `could this item's search space contain a fact about (a) what the decoder or the
  emission READS`) — names the decoder's and emission's inputs, the third limb's subject.

**The exact opening and closing anchors of (c) to (f), and the scope of each cut (a whole bullet or a
sentence inside one), are written by the pack-build dispatch and verified at the file before it is
released**; this record names the passages by their opening text so the dispatch cannot pick different
ones. The pilot's two frozen packs are untouched. `CLAUDE.md` is not amended by this ruling.

**Declined:** the pilot's two only (leaves four found passages in, #19's merely-unfalsified shape with the
falsification in hand); the pilot's two plus (c), (d), (e) without (f).

## 6. Ruling 6 — withheld documents for L2 are every home document of an IN entry, DERIVED and confirmed before authored

**Ruled.** The pack-build dispatch derives, from the register's data file
(`tools/audit/decisions/backbone_decisions.json`) and the ruled verdict table, the set of home documents of
the 111 IN entries; prints it; and **STOPs for the user's confirmation before the set is authored into
`WITHHELD["l2"]["withheld_documents"]`**, each with its finding, date and reason. The pilot's one withheld
document is not restated for L2 — it enters, if at all, as a member of the derived set.

**Facts the ruling rests on, at the generator:** the leak check over members (5) and (6) strikes an
entry whose rendered fields carry a withheld document's name; the read-me's what-was-cut section
(`render_what_was_cut`) discloses withheld entries, passages and extras' filters, NOT documents, so a
withheld document is invisible to the session.

**Declined:** the two joint-estimator documents by hand; none.

## 7. Ruling 7 — the brief stages scores WITH their published analyses, selected for named L2 phenomena, plus the `l0-l1` plain-score set

**Ruled.** A small set of scores with their *When in Rome*/DCML analyses (the only annotation family
established as ground truth under the ruled grading conventions; BCMH is not, D-475), chosen to exhibit
named phenomena of L2's four limbs — a modulation with a tonicization beside it; a cadential six-four; a
passage where the annotation records a rival reading in the same stream; a suspension or passing-tone
texture where the chord-tone assignment is non-trivial — **each claim checked at the file before it is
staged, the named set put to the user with each claim beside it before anything is staged** (Ruling 15's
form). Plus the `l0-l1` plain-score set unchanged. The standing bars carry over: files staged BY NAME and
never a directory; EXEMPLARS, NOT A CORPUS; the session builds, designs, scopes and runs no measurement.

**Declined:** plain scores only (withholds the primary evidence for L2's own question); analyses without
the plain-score set.

## 8. Ruling 8 — the two guard reds are regenerated in the pack-build dispatch

`gen_evidence_pin_membership.py` and `gen_l0_l1_outgoing_population.py` are regenerated in the
pack-build dispatch, both declared, the first publishing the 2026-09-05 sitting record's surface generator
as a pinned-evidence member UNRESOLVED for the user to resolve at that artifact. Cause and declined
alternatives at `cc_report_l2_ruling_writeback_2026_09_05.md` §5.

## 9. Ruling 9 — the `STATUS.md` "ELEVEN FAILING" entry is left, with a condition

Left as it stands; the pack-build batch's own close supersedes it (Ruling 4 of the 2026-08-17
governing-surface split). **Condition, part of the ruling:** if the pack-build batch is far off, the word
is corrected in place by the next dispatch that touches `STATUS.md`, the former wording preserved beside
it (D-674's branch two).

---

## 10. What this sitting does NOT do

It renders no pack, writes no dispatch, authors nothing into `EXTRAS` or `WITHHELD`, boots no session,
lifts no gate and reads no paper. It selects no score and names none. It amends no governing document;
`CLAUDE.md` and `FRAMEWORK.md` are untouched. No open-items row is created, flipped or discarded. No
decisions-register identity is allocated — that register cannot accept one and
`cowork_register_rule_c_suspension_2026_08_28.md` is the route. The brief's P1, P2 and P4 are expected to
carry over from `l0-l1` unchanged and were not re-decided.

## 11. What follows, in order, none of it executed here

1. This side derives and puts to the user by name: which of the 37 unread admitted rows are L2's slice
   (Ruling 1); which first-pass extracts bear on L2 (Ruling 2); the staged score set with each claim
   beside it (Ruling 7). Reads, not rulings.
2. Task B's L2 slice is read whole at the object, in bounded sessions closing at member boundaries, under
   the remedial commission's own rules; central papers owe a second extraction.
3. The pack-build dispatch, when next: `WITHHELD["l2"]` authored from the ruled LIST ONE with the six
   passages (Ruling 5) and the derived-and-confirmed documents (Ruling 6); `EXTRAS["l2"]` with the
   charter member, the two extract populations and the ledger (Rulings 2 to 4); the `DATE` mechanism at
   its four sites; the two regenerations (Ruling 8); the cross-reference additions and the leak check
   run, the leak list to the user.
4. The brief, and the blind deriving session in a fresh Cowork session.

---

*Provenance: Cowork, 2026-09-05, at tip `911f5f7cda…`. The surface and this record were written from
this session's own reads at the objects, listed in the surface's provenance. The user's words: "Agree on
all 9".*
