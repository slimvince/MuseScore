# Rulings — the withheld-family sitting, 2026-08-22

> **STATUS: RULING RECORD.** Cowork, 2026-08-22 (the forty-fifth Cowork session). An interim
> carrier under the standing clause that a sitting record is written in the turn its ruling is
> given and lands in git at the next dispatch's Task 0.
>
> **Taken at branch tip `0dcbacce2d`** (parent `a223fe8358`; `refs/heads/master` and
> `refs/remotes/origin/master` both at the tip), read by this session with `git show -s` and
> `git for-each-ref` at the explicit hash on the user's machine, **after the pilot-preparation
> batch (`cc_instruction_pilot_preparation_withheld_family.md`, as amended) returned complete and
> was verified at the objects by this session; nothing was running.** The object ruled on is the
> reading file `ratification_surfaces/cowork_withheld_family_harmony_boundary_reading.md` at the
> tip (blob `4f4f0da58e…`), its five lists, and the authored table of
> `tools/audit/gen_derivation_boot_pack.py` that the reading file publishes. The reading file is
> written by Claude Code and is not generated; the authored table is an input to a generator and
> is corrected by hand under this record.

---

## 0. What was put, and in what form

Five decisions, one list per turn, in the order the reading file lists them. Each was put
self-contained as a turn's final response with no question in it — the list re-explained, every
entry of it named with Claude Code's reason and this session's own reading of the entry's
verbatim and plain at the manifest (`tools/audit/derivation_boot_pack.json`, read at the tip
object), the alternatives each with its case for and against and rated towards the ultimate
objective and towards the guiding principles, and this session's choice when the user asked for
it. The choice question was put in the following turn (List One through the question widget; Lists
Two to Five answered by the user with the alternative's letter in conversation). Before the first
list was put, this session's verification of the batch's return was delivered whole as a turn of
its own, including three presentation-form discrepancies found between the report, the close and
the reading file (recorded in §6 below).

## 1. Ruling 1 — LIST ONE stands as authored: all fifteen IN entries are withheld (Alternative A; the user's word: "A")

**The fact ruled on, at the object.** At the tip, `derivation_boot_pack.json` →
`subjects.harmony-boundary.THE_WITHHELD_FAMILY.identities` carries exactly the fifteen identities
the reading file's LIST ONE carries — D-001, D-022, D-023, D-024, D-057, D-317, D-318, D-383,
D-449, D-450, D-453, D-491, D-545, D-565, D-575 — and each carries one `IN` verdict with finding,
date and reason in the candidate list. This session read every reason against the entry's own
verbatim and plain at the manifest.

**Ruled.** All fifteen are withheld from the pack. **Recorded beside the ruling, at the user's
acceptance of this session's stated caveat:** D-450 and D-575 are withheld on a reading of the
test wider than its words — both rank notated against sounding evidence for the KEY axis, not the
chord boundary. If the blind session's specification later cites the notated key signature as
evidence for a chord boundary, the comparison is to read that against the pack's silence on these
two entries and not score it as a method failure.

**The alternatives declined.** B — thirteen withheld, D-450 and D-575 admitted: declined because
both state the oracle's ranking (sounding over notated) for the neighbouring axis, and a derivation
that then ranks sounding notes first could not be told apart from generalisation; the held-out
test's negative case would be less believable for no gain the pilot can measure. C — additions to
IN beyond the candidate list: not available from the record; the criterion's reach is unmeasured
and an addition would be this session's, not derived.

## 2. Ruling 2 — LIST TWO stands as authored: all fifty-seven OUT entries are admitted (Alternative A; the user's word: "A")

**The fact ruled on, at the object.** The fifty-seven `OUT` verdicts of the candidate list, each
with what the entry bears on instead. This session read all fifty-seven reasons against the
entries' text and named six as one step from the subject — D-285, D-329, D-330, D-339, D-490,
D-623 — and one, D-339 (*a confident earlier decision can be overturned by decisive later
evidence*), as the only OUT entry on the same dimension as the oracle's second half.

**Ruled.** All fifty-seven are admitted. D-339 is admitted as a principle the derivation is meant
to apply across every axis, not an answer it is meant to find.

**The alternative declined.** B — D-339 to IN: declined because it is architecture-wide and states
no ranking of channels for a boundary; withholding it would remove a ratified principle the
session needs for any unit, on a leak that is inferential rather than stated.

## 3. Ruling 3 — LIST THREE: D-569 is IN (withheld); D-457 and D-526 are OUT (admitted) (Alternative B; the user's word: "B")

**The fact ruled on, at the object.** The three `UNPLACED` verdicts, each with what was read, and
Claude Code's recorded abstention from any recommendation on them. This session's own readings:
D-457 discloses that the slicer distinguishes an artificial clip boundary from a musical one —
the existence of a distinction, not its rule; D-526 discloses that the segmenter once applied a
tonic prior at the head of a gap, named in order to say it dissolves — the existence of a prior,
not a rule; D-569 supplies the vocabulary of the withheld passage — "every sounding note",
"eligible for harmonic analysis", onset and offset as the note's own edges — without stating the
rule.

**Ruled.** D-569 moves to IN: its leak is the oracle's own vocabulary, and the comparison could
not distinguish recovery from recall. D-457 and D-526 move to OUT, with this session's readings
above as the "what it bears on instead". **This changes the authored table and is executed by the
next dispatch** (§7).

**The alternatives declined.** A — all three OUT: declined for D-569 on the ground just stated.
C — all three IN: declined because D-457 and D-526 disclose existence only, against the
minimal-withholding standard Ruling 2 applied.

## 4. Ruling 4 — LIST FOUR: the leak check stays as it is; D-270, D-296 and D-440 remain listed and not rendered (Alternative A; the user's word: "A")

**The fact ruled on, at the object.** `subjects.harmony-boundary.LEAKS.entries`: D-270 matched
`docs/score_inventory.md`, D-296 matched `src/notation` and `src/engraving`, D-440 matched
`src/llm/`, all in the verbatim field. None bears on the subject.

**Ruled.** The check is unchanged; the three stay out of member (5).

**The alternatives declined.** B — render with the matched path redacted: declined as a hand edit
to a ratified entry's verbatim inside a generated member, a second copy differing from the
register (#6), and new mechanism. C — drop the `docs/`/`src/` path test: declined because that
test is the one that catches routes into code, which the phase definition's constraint is
precisely about, and none of the three is needed for this unit.

## 5. Ruling 5 — LIST FIVE: the passage was applied as meant, AND the withheld passage is WIDENED to the whole founding-instance clause (Alternative B; the user's word: "B")

**The fact ruled on, at the object.** At the tip, the rendered member (2)
(`tools/audit/derivation_boot_pack/harmony-boundary/02_the_guiding_principles_and_the_conventions.md`)
carries the marker `[A PASSAGE IS WITHHELD FROM THIS PACK FOR THIS SUBJECT.]` exactly once, at the
ruled anchors; the manifest's `passages[0].the_text_matched` is the ruled span (229 characters);
`CLAUDE.md` is byte-unchanged (same blob at `dcbfa5fe32` and the tip); none of the oracle's own
phrases occurs in any of the seven pack files. **And the residue around the cut reads, in the
rendered member:** *"…reported the position as ambiguous; [marker] , which turned an 'ambiguous
spec, narrowed in implementation' reading into a documented decision the implementation
contradicts. The primary source was more specific than the memory of it, which is the general
case, not the exception."* — telling a deriving session that a documented Layer-2 decision on note
collection exists, is more specific than a summary of it, and is contradicted by the
implementation. No content of the rule is disclosed; the member-two-leak ruling's stated bound
(the oracle's phrases, not every paraphrase) is respected.

**Ruled.** (i) The application is confirmed as meant. (ii) The withheld passage for this subject is
**widened to the whole founding-instance clause**: inside the same bullet (scope anchor
`**NEVER WORK FROM MEMORY INSTEAD OF DOCUMENTED FACTS`), the span opening
*"**Founding instance:** on 2026-07-28 Cowork reasoned"* and closing *"which is the general case,
not the exception."* — located by its own text and never by line number; at
`git show 0dcbacce2d:CLAUDE.md` each anchor occurs exactly once in the file (lines 1486 and 1493;
the bullet opens at line 1475). **The widened span CONTAINS the span ruled at the member-two-leak
sitting; the authored table carries ONE passage with the widened anchors, replacing the narrower
anchors, and the finding names both rulings.** The statement of the never-work-from-memory rule
itself, above the clause, stays whole in the pack. `CLAUDE.md` is untouched.

**The ground this session stated, recorded as its reading.** The correction commit is owed by
Ruling 3 anyway; the mechanism is the one already ruled (an authored passage in the D-677 shape);
and "the implementation contradicts a documented decision here" is the kind of statement the pack's
read-me tells the session to stop reading at.

**The alternatives declined.** A — confirm and nothing further: declined for the residue named
above. C — change the generator's marking to replace the whole sentence: declined as new mechanism
applying to every future passage, where an authored input suffices.

## 6. Recorded of the return, not ruled: three presentation-form discrepancies in the batch's own text

Found by this session at verification and reported to the user before List One; none changes a
verdict. (a) The count of candidates reaching the list only through an in-word keyword match is
stated as *three* in the report (§3.2) and the close (§6), as *two* in the reading file §4, and
FOUR entries are annotated in the reading file's tables (D-383, D-341, D-474, D-613); and by the
manifest's own `in_context` fields two further entries reach the list only through a keyword inside
a code identifier and are not annotated — D-224 (`onset` in `onsetAtRegionStart`) and D-313
(`boundary` in `combinedBoundary`). (b) The report §7.4 says the armed guard denied "four attempts"
and lists five; the close §8 says five. (c) The residue of §5 above. **The next dispatch corrects
(a) in the reading file at the annotation and at §4's count, and leaves (b) as a fact about the
report (#12; a report is not edited after its commit).**

## 7. What this sitting orders, and what it does NOT do

**Ordered:** one correction dispatch — the authored table's D-569 verdict to IN (finding, date
2026-08-22, reason from Ruling 3), D-457 and D-526 to OUT with this session's readings as what
they bear on instead; the withheld passage's anchors widened per Ruling 5; the pack re-rendered
and `--check` green; the reading file corrected to the ruled lists (LIST ONE gains D-569, LIST
TWO gains D-457 and D-526, LIST THREE is empty and says so, LIST FIVE states the widened span) and
at discrepancy (a); the guard set run; one commit per task, pushed. **After it lands, the pack is
the one the blind Cowork session boots from, under Ruling 1 of the deriving-side sitting.**

**Not done:** no session is booted; no derivation; no oracle opened for the test; no edit to any
boot-list member at its home; no register entry, sort artifact or register source touched; no
finding number allocated (the series stands at F88); no open-items row created, flipped or
discarded — [[OI-179]] stays OPEN and GATES, [[OI-372]] and [[OI-374]] stand as found. The
criterion's reach stays unmeasured and the family does not claim completeness.

---

*Provenance: Cowork, 2026-08-22, the forty-fifth session, at tip `0dcbacce2d`. The report, the
dispatch, the reading file and `STATUS.md` were read whole from bridge-staged snapshots
hash-verified against the tip blobs; the manifest, the pack files, `guard_state.json`,
`epoch_write_path.json`, `evidence_pin_membership.json`, the two registries' diffs and `CLAUDE.md`
were read at `git show 0dcbacce2d:<path>` on the user's machine. The user's words: "A", "A", "B",
"A", "B".*
