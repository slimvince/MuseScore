# CC dispatch — the L0/L1 COMPARISON, EIGHTH WRITING, THE ROW 4.3 PLACEMENT: land the ninety-eighth entry and Ruling 78, place Row 4.3 QUARANTINED at the reading file, correct the one §13b paragraph the placement falsifies with its former wording preserved, note it at §13a's foot, close — NO ruling taken, NO outgoing text edited, NO other row touched, NO session booted

> **Dispatch (Cowork writing side, 2026-09-04, the session that booted on
> `cowork_handoff_entry_ninety_eight.md`, its first dispatch).** Discharges the one row the fifth,
> sixth and seventh writings each left standing: **Row 4.3**, the row Ruling 62's item 4 cites and does
> not place. **Written at a verified stop: the seventh writing's batch returned and its close was
> verified at the objects by this session** — the chain `c09f3fa9ea… → 3b30aadb3e… → 507e2fbb10… →
> cbb059bc19… → 56dcec1434… → 7a5f307563…`, each commit read at `git show --stat` by explicit hash;
> the reading file's Task 1 commit stat read as **285 insertions and zero deletions**; the successor's
> Task 2 diff read whole as exactly two changed passages; §13b's three tables checked against the
> placement lines counted at the file by this side; the end-state guard artifact read at the object —
> **nothing is running.**
>
> **★ WHAT THIS BATCH IS, AND WHAT IT IS NOT.** It APPLIES one ruling the user took on 2026-09-04,
> **Ruling 78 (§3cg)** of `cowork_rulings_2026_08_31_decision_surface_sitting.md`, which places Row 4.3
> QUARANTINED under Ruling 62's own item 4. **THIS BATCH DECIDES NOTHING**: it takes no ruling, weighs
> no position, edits no other row, edits no proposed disposition, edits no outgoing text, derives no
> statement, touches no register, and re-derives neither §11 nor §12. Where an anchor this face quotes
> is not found verbatim at the file, that edit is a STOP-and-report and no part of it is improvised.
>
> **★ THE ORDERED FIRST READ.** Your VERY FIRST read this session is the sitting record's **§3cg
> (Ruling 78) WHOLE** — the authority for this batch — then **§3bq (Ruling 62) WHOLE**, the ruling it
> applies. Then: **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md` —
> CONDITIONAL, and this batch MEETS the condition (it runs the guard set): read it in full; rule (a)'s
> `gating_ids` at `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` (DO NOT read
> the whole open-items index). **(2)** `cowork_handoff.md`, ITS TOPMOST ENTRY, and
> `cowork_handoff_entry_ninety_eight.md` whole — the entry point this batch lands; a
> `cowork_handoff_entry_ninety_nine.md` at the root, if present, is the writing side's and is NOT
> landed. **(3)** `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)** the seventh
> dispatch `cc_instruction_comparison_l0_l1_seventh_2026_09_03.md` whole, its report whole, and the
> seventh close in `cowork_away_returns.md` (*THE L0/L1 FAMILY PLACEMENTS (SEVENTH WRITING) …*).
> **(5)** the reading file `ratification_surfaces/cowork_comparison_l0_l1_reading.md` — its §0, §5,
> §13, §13a, §13b, §15 and §16 whole, and these rows at the row and no others: **4.3** (the row this
> batch places) and **1.3, 3.31, 3.41, 3.42, 3.48, 14.9** (Ruling 62's own row set, read so the ground
> is checked and not taken from this face). **(6)** `tools/audit/gen_status_batch_bound.py` whole at
> the object `7a5f307563…:tools/audit/gen_status_batch_bound.py` before Task 2. **(7) No outgoing text
> and no part of the derived specification is opened by this batch.**
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The reading file* —
> `ratification_surfaces/cowork_comparison_l0_l1_reading.md`: the comparison's tabulation, one row per
> outgoing statement, each row carrying a `*PROPOSED DISPOSITION.*` paragraph and, where a ruling has
> placed it, a `*RULED DISPOSITION (…).*` line beneath it. *A row's block* — the `**Row N.M —` heading
> and every line under it up to the blank line before the next `**Row` heading, `####` heading, `---`
> rule or `**★` paragraph. *A placement line* — the one line this batch adds at the foot of Row 4.3's
> block, in the form fixed at Task 1(a). *A changed passage* — one contiguous run of changed lines in a
> diff (what a diff tool calls a hunk). *The current commit* — the commit named by hash, never a bare
> branch tip.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, **no measurement of the ANALYSIS**, no
> design, no repair, **no derivation of any statement**, NO SESSION BOOTED, no document archived, moved
> or deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit to any governing
> document (the two `STATUS.md`/`STATUS_ARCHIVE.md` acts of Task 2 excepted), to the derivation (body
> OR banner), to the derived specification, to the brief, to the pack or its generator, to any register
> entry or register source, to the suspension file, or to any outgoing text; in the reading file, only
> the three edits Task 1 orders — no other `**Row` heading, `*Statement.`, `*Derived.`,
> `*Current-text axis.` line, `*PROPOSED DISPOSITION` paragraph or existing `*RULED DISPOSITION` line
> is edited or deleted, §13's tables are not touched, §13a's figures are not touched, and §13b's three
> tables are not touched** — **no ruling taken, no position weighed, no recommendation anywhere.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (`--check`) and again at
> the end (write mode, committed as the artifact of a real run). Verify every commit at the object by
> explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and any `git diff` over
> the whole working tree time out on this mount and are denied by the armed guard — use the per-path
> explicit-hash forms named in A1; a `git diff` with NO explicit hash is a D-253 breach. `git diff
> --no-index` against a working-tree path is denied by the guard: take every check between two explicit
> blob hashes, writing the working copy into the object store with `git hash-object -w` first, as the
> fifth, sixth and seventh batches did. If staging is refused by a stale zero-length `index.lock` with
> no git process running, establish the cause (D-669), remove it, report the act. **Commit subjects
> are given below without an apostrophe anywhere, so that a plain single-quoted `git commit -m '…'`
> carries each one verbatim** — the seventh writing's declared departure 4, prevented rather than
> declared again. The reserved-word and vocabulary rules bind every line — *score* only the musical
> score, *key* only tonality, *bar* only the metric unit (the prohibiting sense is *exclude*), *bass*
> only the chord's bass note L3 decides except inside a quotation, no numeric grade anywhere.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 78, §3cg — its decision:** *"Row 4.3 is QUARANTINED, placed under Ruling 62's item 4 by this
  ruling, on the ground its four sibling rows were placed on."* Its ordered consequences, in its own
  words: the placement line *"is written by the next dispatch, at the foot of the row, in the shape the
  file's other ruled lines use, and it carries the audit question in one sentence as §5 requires"*;
  *"§13b's paragraph reading 'No line here places Row 4.3' becomes false the moment the line is written
  and is corrected in the same act, its former wording preserved at the site (#12)"*; *"§13a and §13b
  each gain a foot note recording that one further ruled line exists and where it is counted, their own
  figures untouched."* Its stated exclusions: it does **not** correct Ruling 62's closing phrase, and it
  does **not** re-derive §11 or §12.
- **Ruling 62, §3bq, item 4 — the rule this batch's line applies:** *"The per-note list is S-3's as
  written, plus Ruling 44's stem direction and Ruling 50's attached marks; the outgoing eleven- and
  twelve-field lists are descriptions of builds and targets, placed by S-3 — carried where they match,
  QUARANTINED where they describe code, as the reading file already has 1.3 and 4.3."*
- **The reading file's §5, the closed vocabulary:** **QUARANTINED** — *"it states how the
  implementation currently works, or a measured property of it. The row writes the audit question in
  one sentence."*
- **D-672 (the stop rule):** *"A per-entry pass may be stopped at any member boundary, and the stop is
  RECORDED."* The member boundaries of this batch are the tasks.

## Premise ledger

- **FACT** — the local branch tip is `7a5f3075639bba91c79dc6ee634d66fde40c42b0` (the seventh writing's
  end state; parent `56dcec143406a8f7928d8d04e862d5d66756351c`, the seventh close); `origin/master` is
  the SAME hash. Read by the writing side at the two ref files with the file tools, 2026-09-04; the
  chain `c09f3fa9ea… → 3b30aadb3e… → 507e2fbb10… → cbb059bc19… → 56dcec1434… → 7a5f307563…` verified at
  `git show --stat` per explicit hash. **Nothing is unpushed. Re-read the refs yourself before Task 0.**
- **FACT — the guard state at the tip IS the tip's own measurement.** At the object
  `7a5f307563…:tools/audit/guard_state.json` (blob `5ede2469…`) → `summary`: **run 76**, passing 66,
  **failing 10**, not_run 4, historical_records 16; the ten failing tools, read at that object:
  `gen_filing_convention_application.py --check`, `gen_artifact_inventory.py --check`,
  `gen_artifact_inventory_surface.py --check`, `gen_test_construction_evidence.py --check`,
  `gen_retirement_caller_check.py --check`, `decisions/apply_soft_discard.py --check`,
  `decisions/apply_residue_discard.py --check`, `gen_epoch_write_path.py --check`,
  `gen_recognizer_establishment_sort.py --check`, `decisions/gen_cluster_dispositions.py --verify` —
  the same ten the fifth, sixth and seventh batches inherited and measured at both their ends. Nothing
  has landed since.
- **FACT — the blobs at the tip**, read at `git rev-parse <tip>:<path>`: the reading file
  `f83b30d3f15959e186777558d14407d0335b055f`; the sitting record
  `fc421cee769fbffc8daea6b8d65318f301664f5e`; `STATUS.md` `cf83df31ef9aae01e373fc52895cd6675f48fd17`;
  `tools/audit/gen_status_batch_bound.py` `0828747b92ae1d53618f18307251028454f82682`;
  `tools/audit/evidence_pin_membership.json` `fb3a00be268401e5386954cc3c410c9c827539df`; the derived
  specification `61ca4bb7b0590ad65c33d52b9969501eb4ce4219`.
- **FACT — the sitting record IS MODIFIED ON DISK by this writing side, additions only.** This session
  took Ruling 78 and wrote its section into
  `cowork_rulings_2026_08_31_decision_surface_sitting.md`: **one insertion of 9,125 bytes**, the whole
  of a new `## 3cg. Ruling 78 — …` section, placed immediately before the line
  `## 4. What this ruling does NOT do` — which occurs **exactly once** in the file, established before
  the insertion. The file on disk is **512,553 bytes**; at the tip it is **503,428 bytes**. **No other
  byte of that file changed**; the insertion was a splice, not a rewrite. **A1 declares this
  modification and Task 0 lands it; you prove additions-only at the object.**
- **FACT — Row 4.3's block at the reading file**, read with the file tools this session at blob
  `f83b30d3…`. Its heading is `**Row 4.3 — one factual question, answered with the annotated note
  set.**`; the block carries a `*Statement.*` paragraph, a `*Derived.*` line reading `*Derived.* S-3,
  S-29.`, a `*Current-text axis.*` line, a `*The difference.*` paragraph, and — as its **last**
  paragraph — the `*PROPOSED DISPOSITION.*` paragraph quoted at Task 1(a). The block carries **no
  `*RULED DISPOSITION` line**. It is followed by a blank line and then the heading `**Row 4.4 — the
  four things it must not do.**`.
- **FACT — Ruling 62's other six rows, as the fifth writing placed them**, each read at its row by this
  writing side: **1.3** QUARANTINED, unchanged; **3.31 ADOPTED — carried**; **3.41**, **3.42**,
  **3.48** and **14.9** all QUARANTINED, each having carried ADOPTED — proposed before. **Row 3.48's
  ruled line reads *"travelling with Row 3.42"***, the same words Row 4.3's proposed disposition
  carries; **Row 14.9's proposed disposition names Row 4.3** among the rows it travels with.
- **FACT — `tools/audit/gen_status_batch_bound.py` at the tip** has its five authored constants at
  `BASE_COMMIT = "cbb059bc1924f6fa04afcda959938e4ae610df0e"` (the seventh's Task 2 commit),
  `PREVIOUS_BATCH_DISPATCH = "cc_instruction_comparison_l0_l1_sixth_2026_09_03.md"`,
  `ACT_DATE = "2026-09-03"`, `DISPATCH = "cc_instruction_comparison_l0_l1_seventh_2026_09_03.md"`,
  `TASK = "Task 3"`, read at the object; `PREVIOUS_AIMINGS` carries the sixth writing's aiming as its
  last row, marked as a move that RAN.
- **FACT — `STATUS.md` at the tip carries, top to bottom:** the seventh writing's close entry (naming
  the seventh dispatch, carrying the `Last updated: ` prefix), then that batch's three `Same dispatch`
  entries for Tasks 2, 1 and 0, then the second writing's two 2026-09-02 entries naming no dispatch,
  then the archive pointers. Read at the file.
- **FACT — WHAT `gen_evidence_pin_membership.py` ACTUALLY READS OF A RULING RECORD, established at the
  tool's own source at the object `7a5f307563…:tools/audit/gen_evidence_pin_membership.py` and NOT
  taken from the seventh dispatch, whose statement of it is INCOMPLETE and is corrected here.** Its
  route A reads only the two structural blocks `own_statement_blocks` returns — the LEADING BLOCKQUOTE
  (the contiguous `>` block standing before the record's first `##` heading) and every paragraph
  opening `*Provenance:` — and matches its surface-naming and commit patterns against those blocks
  alone. **Its route B reads EVERY LINE of the record:** for each line containing `PINNED` in any case
  it resolves any tool name on that line, and a resolved tool becomes a member. **The Ruling 78 section
  moves none of the three.** The sitting record's leading blockquote is untouched; the section contains
  no paragraph opening `*Provenance:`; and it contains no line carrying the word *pinned* in any case —
  the file's five such lines all stand far above the insertion point and are unchanged. **A3 therefore
  predicts ZERO movement** of `evidence_pin_membership.json` at Task 0. **Measure before accepting.**

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications at your boot:
  **EXACTLY ONE** — `cowork_rulings_2026_08_31_decision_surface_sitting.md`, additions only, the one
  new section named in the FACT above. **The bound on that declaration, stated rather than implied
  (#24):** the writing side could not run the whole-population enumeration from its own side, the tool
  exceeding the bridge's command window, so the declaration rests on the seventh close's own
  whole-population enumeration (zero tracked modifications, the files its Task 3 ordered named exactly),
  on this side's own act-list since, and on a per-path hash comparison this side DID take against the
  tip for the eight paths this batch depends on — `ratification_surfaces/cowork_comparison_l0_l1_reading.md`,
  `STATUS.md`, `tools/audit/guard_state.json`, `tools/audit/gen_status_batch_bound.py`,
  `tools/audit/evidence_pin_membership.json`, `cowork_derived_specification_l0_l1_2026_09_03.md`,
  `cowork_away_returns.md` and `cowork_handoff.md` — **all eight byte-identical to their blobs at the
  tip.** **Your Task 0 enumeration is what ESTABLISHES A1; this declaration is what it is measured
  against.** Untracked and to be landed by Task 0:
  `cowork_handoff_entry_ninety_eight.md` and this dispatch — **two untracked paths.** Any further
  `cowork_handoff_entry_*.md` or `cowork_rulings_*.md` at the root is the writing side's and is NOT
  landed and NOT a STOP. A modification at any other tracked path, at boot or during the run other than
  by this batch's own ordered acts, is a STOP-and-report. **Check ordered (Task 0, first act, AFTER the
  ordered first reads):** the whole tracked population enumerated with the sanctioned tool
  `tools/audit/changed_paths.py`; per named untracked path
  `git ls-files --others --exclude-standard -- <name>; echo "exit:$?"`. **The sitting record's
  additions-only property is proven at the object**: write the working copy into the object store with
  `git hash-object -w` and diff it against blob `fc421cee769fbffc8daea6b8d65318f301664f5e` between the
  two explicit hashes — **zero deleted lines, one changed passage, and every added line inside the new
  `## 3cg.` section.** Any deletion is a STOP-and-report and nothing is committed.

- **ASSUMPTION A2 — the reds at the `--check` run before your first edit.** The ten failing tools named
  above, exactly, and zero STOPs. The reds this batch's own acts may cause, each classed by its cause
  and NOT a STOP: `gen_l0_l1_outgoing_population.py --check` STALE at the end state if the reading
  file's or `STATUS.md`'s recorded-tier hits move under Tasks 1 and 2 — regenerate, measure as the
  fifth and seventh did, commit with the end state, report that no MEMBER of the population moved; a
  member moving IS a STOP-and-report. `gen_session_start_read_size.py --check` STALE if regenerated
  before the LAST edit to `STATUS.md` — regenerate LAST. `gen_evidence_pin_membership.py --check` STALE
  if the sitting record's leading blockquote, a paragraph opening `*Provenance:`, or a line carrying
  the word *pinned* in any case moved — none of the three did, and a STALE verdict there is a
  STOP-and-report. `gen_artifact_inventory.py --check` and
  `gen_artifact_inventory_surface.py --check` are standing reds — report any difference, repair
  nothing. A red outside that set is classed by its measured cause; one outside this batch's own acts'
  subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration.** `evidence_pin_membership.json` moves by ZERO at
  Task 0. Measure before accepting; movement is a STOP-and-report.

- **ASSUMPTION A4 — the guard registry.** No tool added, none edited except Task 2's re-aiming of
  `gen_status_batch_bound.py`'s authored constants and its `PREVIOUS_AIMINGS` list (D-648); population
  unchanged at **76**; failing set the ten named, plus none; zero STOPs throughout.

- **ASSUMPTION A5 — byte-unchanged by this batch, verified at the objects after the last commit
  (P-3, footprint as an act-list):** the derivation `cowork_blind_derivation_l0_l1_2026_08_31.md`
  whole, banner and body; the derived specification
  `cowork_derived_specification_l0_l1_2026_09_03.md` whole; the sitting record beyond the one section
  Task 0 lands; the brief; the pack directory and `derivation_boot_pack.json`; every outgoing text;
  `tools/audit/gen_l0_l1_outgoing_population.py`; the population artifact except A2's case; every
  governing document except `STATUS.md` and `STATUS_ARCHIVE.md`; the suspension file; every register
  source. **In the reading file, the diff against blob
  `f83b30d3f15959e186777558d14407d0335b055f` is exactly the three edits Task 1 orders and nothing
  else — two insertions and one paragraph replaced, the replaced paragraph's former wording carried
  verbatim inside its replacement.** Any other difference is a STOP-and-report.

## The declared start state — at the tree you will meet

1. The ten failing tools named in the FACT above — standing reds, none of them this batch's.
2. Zero STOPs; `gen_evidence_pin_membership.py --check`, `gen_l0_l1_outgoing_population.py --check`,
   `gen_status_batch_bound.py --check` and `gen_session_start_read_size.py --check` PASS predicted,
   **measured** (A2).
3. The artifact at the tip being the tip's own run, your start-state run is expected to reproduce it
   exactly; a difference is reported with its cause.

A failing verdict outside 1–3 is a STOP-and-report, save the A2 exceptions named.

## Findings in this batch

**No finding number is allocated by this dispatch.** Ruling 9's routing binds. **An anchor this face
quotes that is not found verbatim at the file is a STOP-and-report for THAT edit: nothing is written
for it, it is reported with what was read, and the other edits are made only if they are independent of
it.** The three edits of Task 1 are NOT independent of one another — the §13b correction and the §13a
note are true only if the placement line was added — so **if the placement line cannot be added, Task 1
is not committed at all and the batch reports and stops.**

## Task 0 — land the ninety-eighth entry, Ruling 78 and this dispatch, and PUSH

1. **A1's check** (after the ordered first reads), including the sitting record's additions-only proof
   at the object.
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob
   `fb3a00be268401e5386954cc3c410c9c827539df` BEFORE accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_handoff_entry_ninety_eight.md` (new),
   `cc_instruction_comparison_l0_l1_eighth_2026_09_04.md` (new),
   `cowork_rulings_2026_08_31_decision_surface_sitting.md` (modified, additions only), and
   `tools/audit/evidence_pin_membership.json` (regenerated, only if it moved) — **three or four
   paths.** Subject, exactly:
   `record: Ruling 78 and the ninety-eighth entry landed with the eighth comparison dispatch, the Row 4.3 placement`
4. **PUSH**; verify at the ref file that `origin/master` equals the new tip.

**Under standing clause P-2 this dispatch is PINNED at Task 0:** record its blob, and prove the blob
unmoved immediately before staging it.

**E0:** the named paths in one commit; `origin/master` at the commit; A3 measured; A1's whole-tree
check reported with its enumeration; the sitting record's additions-only property proven at the object.

## Task 1 — the Row 4.3 placement, the §13b correction and the §13a note, in the reading file

By targeted edits only, on the working copy, never retyped; **one commit for the whole of Task 1.**

**(a) The placement line at Row 4.3.** Its anchor is the last paragraph of Row 4.3's block, which reads
**exactly**:

```
*PROPOSED DISPOSITION.* **ADOPTED — proposed**, travelling with Row 3.42, and adding one question of
its own: whether a clipped in-region duration belongs in L0 at all, being a function of the caller's
region.
```

Find the row by its heading `**Row 4.3 — one factual question, answered with the annotated note
set.**` and read the whole block at the file WITH THE FILE TOOLS before editing; no shell reads a
working-tree file (D-253). **Immediately after that paragraph's last line
(`region.`), and before the blank line that precedes the heading `**Row 4.4 — the four things it must
not do.**`, add these lines and nothing else:**

```
*RULED DISPOSITION (Ruling 78, §3cg — cc_instruction_comparison_l0_l1_eighth_2026_09_04.md Task 1).*
**QUARANTINED** — placed under Ruling 62 (§3bq, item 4) on the ground its four sibling rows were placed
on, travelling with Row 3.42 as Rows 3.41, 3.48 and 14.9 do: *"the outgoing eleven- and twelve-field
lists are descriptions of builds and targets, placed by S-3 — carried where they match, QUARANTINED
where they describe code."* The per-note list is S-3's as written. *Audit question:* does the in-region
duration — a note's duration clipped to the queried region — belong to L0's supplied facts at all,
being a property of the query rather than of the note?
```

**The `*PROPOSED DISPOSITION.*` paragraph is not edited.**

**(b) The §13b correction, the former wording preserved.** Its anchor is the paragraph of §13b reading
**exactly**:

```
**No line here places Row 4.3.** It stands at its proposed disposition — the fifth writing
reported that Ruling 62 item 4's citation of it and the row's own content do not meet — and it goes to
the user on its own surface.
```

**Replace that paragraph, and nothing else in §13b, with:**

```
**Row 4.3 is PLACED, and this section's tables do not count it.** On 2026-09-04 the user ruled it
QUARANTINED under Ruling 78 (§3cg), which places it under Ruling 62's item 4 on the ground its four
sibling rows were placed on; its line was added by
`cc_instruction_comparison_l0_l1_eighth_2026_09_04.md` Task 1. It is **not a family placement**, so
this section's own totals and all three of its tables are unchanged, and it is the one further ruled
line §13a's foot note names.
*Former wording, preserved (#12): "**No line here places Row 4.3.** It stands at its proposed
disposition — the fifth writing reported that Ruling 62 item 4's citation of it and the row's own
content do not meet — and it goes to the user on its own surface."*
```

**§13b's three tables and every other paragraph of §13b are untouched.**

**(c) The §13a foot note.** Its anchor is the last paragraph of §13a, reading **exactly**:

```
*★ THE FAMILY PLACEMENTS WERE MADE on 2026-09-03 under
`cc_instruction_comparison_l0_l1_seventh_2026_09_03.md` Task 1 and are counted APART, at §13b; the 210
lines above and their arithmetic are unchanged.*
```

**After it, and before the heading `## 13b. The FAMILY placements — derived at the placement lines`,
add one paragraph, the former text untouched:**

```
*★ AND ONE FURTHER RULED LINE WAS ADDED on 2026-09-04, at Row 4.3, under Ruling 78 (§3cg) by
`cc_instruction_comparison_l0_l1_eighth_2026_09_04.md` Task 1; it is recorded at §13b's foot and is not
a family placement; the 210 lines above and their arithmetic are unchanged.*
```

**§13a's figures and tables are untouched.**

**(d) What Task 1 does NOT touch, stated so it is not inferred.** **§11 (the audit questions) and §12
(the proposals) are NOT edited and NOT re-derived.** Ruling 78 states in terms that they are owed a
single re-derivation against the ruled dispositions as their own scoped act, that four rows already
stand outside them, and that repairing one of five would leave the file inconsistent in a new way.
Adding Row 4.3's audit question to §11, or removing its proposal from §12, is therefore **a
STOP-and-report if attempted, not an improvement.**

**(e) The object-level check, before the commit.** Write the working copy into the object store
(`git hash-object -w`) and diff it against the tip blob `f83b30d3f15959e186777558d14407d0335b055f`
between the two explicit hashes, to a scratch path outside the repository, read with the file tools.
**Exactly three changed passages. Exactly one added `*RULED DISPOSITION` line, at Row 4.3. Exactly
three deleted lines, all of them the §13b paragraph the replacement carries verbatim inside itself; no
other deletion anywhere. No `**Row` heading added or removed. No `*PROPOSED DISPOSITION` line touched.
§13's tables, §13a's tables and figures, and §13b's three tables byte-identical.** A difference none of
those accounts for is a STOP-and-report and the file is not committed until it is removed. Subject:
`comparison L0/L1: Row 4.3 placed QUARANTINED under Ruling 78, the one row Ruling 62 item 4 left unplaced, with the 13b paragraph corrected and its former wording preserved`
Push; verify at the ref file.

**E1:** the placement line present at Row 4.3 and nowhere else; the §13b paragraph replaced with its
former wording carried inside the replacement; the §13a note added; exactly three changed passages and
exactly three deleted lines proven at the object; §11 and §12 untouched; no recommendation anywhere.

## Task 2 — the close

1. **The `STATUS.md` entries and the forward-bound move, IN THIS ORDER.** **(a) FIRST write this
   batch's own entries** at the top of `STATUS.md`: one pointer entry per task that did work (Tasks 0
   and 1; no figure restated, D-431), **the close entry FIRST and NAMING THIS DISPATCH BY ITS FILE
   NAME, the earlier task's entry below it saying `Same dispatch`**, and **the `Last updated: ` prefix
   moved from the seventh writing's close entry to your newest entry.** The close entry states in one
   sentence that Row 4.3 is PLACED QUARANTINED under Ruling 78, that it is the row Ruling 62's item 4
   cited and left unplaced, and that §11 and §12 are deliberately untouched and owed one re-derivation.
   **(b) THEN re-aim `gen_status_batch_bound.py` — ALL FIVE authored constants:** `BASE_COMMIT` (this
   batch's Task 1 commit), `PREVIOUS_BATCH_DISPATCH`
   (`cc_instruction_comparison_l0_l1_seventh_2026_09_03.md`), `ACT_DATE` (`2026-09-04`), `DISPATCH`
   (this dispatch's file name), `TASK` (`Task 2`); **and append to `PREVIOUS_AIMINGS`, kept rather than
   replaced (#12), the seventh writing's aiming now superseded** (base `cbb059bc19…`, then-previous the
   sixth dispatch, executing act the seventh dispatch Task 3, which RAN and moved the sixth writing's
   three entries). Amend the two head comments as the seventh did. **(c) THEN run
   `gen_status_batch_bound.py --apply`.** **Prediction, from the tool's mechanism read at the object:**
   at `BASE_COMMIT`'s `STATUS.md` the seventh writing's close entry names the seventh dispatch and the
   three entries below it say `Same dispatch`; the second writing's first entry below those names no
   dispatch and does not say `Same dispatch`, so the run stops there — **exactly the SEVENTH writing's
   FOUR entries move, the second writing's two stay in `STATUS.md`, no STOP.** If the tool nonetheless
   STOPs, restore it to its passing aim, re-run to prove it green, report the STOP with the tool's
   message, and move nothing by hand. **(d) LAST**, after the final edit to `STATUS.md`,
   `gen_session_start_read_size.py` regenerated and its `--check` confirmed green before the commit.
   The re-aimed tool, `tools/audit/status_batch_bound.json`, `STATUS.md`, `STATUS_ARCHIVE.md` and the
   read-size artifact are committed together in the close.
2. The FULL close appended to `cowork_away_returns.md` as a **THE ROW 4.3 PLACEMENT (EIGHTH WRITING):
   THE ONE ROW RULING 62 ITEM 4 LEFT UNPLACED, QUARANTINED UNDER RULING 78, THE 13b PARAGRAPH
   CORRECTED WITH ITS FORMER WORDING PRESERVED** section — both guard states, every SHA, E0–E2 graded,
   A1–A5 graded, declared departures, the object-level check of Task 1 stated as measured, **the fact
   that §11 and §12 are untouched and why**, and the plan lines (**the pack finding of §3al.4 to L2's
   pack; then L2, sequenced by Ruling 10**), and **the plan's tell in one sentence: did this batch
   produce anything other than the landed records, the placement line, the §13b correction, the §13a
   note, the four Task-2 files and the report? If yes, name it.** The report is
   `cc_report_comparison_l0_l1_eighth_2026_09_04.md` — which likewise decides nothing; it carries the
   one placed row with its kind, disposition word and ruling, what was read at each of Ruling 62's
   other six rows, and the object check of Task 1; where it would name hashes it cannot contain, point
   at the git log.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **The ordered structure
   yields: Task 0, the reading-file commit, the close, the end state.**

**E2 — DERIVED from the declared start state plus this batch's footprint (P-3):** at the tree carrying
the close, a fresh full guard run — **population 76**; **zero STOPs**; **the failing set exactly the
ten named, PLUS NONE**, the A2 exceptions handled as A2 says — committed only after the run that
produced it.

## What this batch does NOT do

- **No ruling taken, no position weighed, no verdict on any open question, on the derivation or on the
  deriving session's independence. No session booted. No measurement built or run. No derivation of
  any statement.**
- **No row other than Row 4.3 is touched**, and Row 4.3's own heading, statement, derived line, axis
  line and `*PROPOSED DISPOSITION` paragraph are not edited.
- **§11 and §12 are not edited and not re-derived**; §13's tables, §13a's figures and tables, and
  §13b's three tables are untouched.
- **No edit to the derivation (banner or body), to the derived specification, to the brief, to the
  pack, its generator, its artifact, the population tool, any outgoing text, any governing document
  other than the two Task 2 orders, the suspension file, or any register source.** The reading file's
  three edits, the four Task-2 files and the report are the whole authored write set; the sitting
  record's one section was written by the writing side and is landed, not authored, here.
- **No correction to Ruling 62's closing phrase.** Ruling 78 reports it and leaves it standing.
- **No finding number. No open-items row created, flipped or discarded EXCEPT an apparatus-finding row
  under rule (c). No `src/` change, no golden, no test, nothing under `tools/corpus/` or
  `tools/robust_stop/`. No guard enrolment. No `tools/audit/*.py` written or edited except the
  re-aiming of `gen_status_batch_bound.py`'s five authored constants and its `PREVIOUS_AIMINGS` list
  (D-648).**
- **No move of the second writing's two `STATUS.md` entries. Nothing written into the suspension file
  or any register.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* **#12** — the §13b paragraph's former wording carried verbatim inside its
   replacement, `PREVIOUS_AIMINGS` appended to rather than replaced, Row 4.3's proposed disposition
   left standing beside its placement. **#10** — §13a and §13b cease to state something false about the
   file the moment the line exists, by a note beside the one and a corrected paragraph in the other.
   **#6** — one placement line, one home for its record (§13b's foot), the close and the report
   pointing at it. **#19** — nothing is established by this batch; the placement is a ruling applied,
   and the ground is quoted from the ruling rather than argued here. **#13** — an anchor not found
   verbatim is a STOP, not a reason to improvise, and the three edits stand or fall together.
   **#17(f)/D-431** — no count of the reading file is restated on this face. **#24** — no comparison
   asserted. Conforms.
2. *Conventions:* American English; no self-invented label — *a row's block*, *a placement line* and
   *a changed passage* are defined at the head, the last of them in the ruled words for what a diff
   tool calls a hunk; music-theory words in their musical sense, *exclude* for the prohibiting sense;
   *bass* nowhere outside a quotation; *the current commit* for a commit named by hash; no numeric
   grade anywhere.
3. *Figures and premises:* the tip and its chain at the two ref files and at `git show --stat` per
   explicit hash; the guard summary and its ten failing tools at the tip's own artifact; every blob at
   `git rev-parse` by explicit hash; the tool's five constants at the object; Row 4.3's block and
   Ruling 62's other six rows read at the reading file with the file tools this session; Ruling 62's
   and Ruling 78's words read at the sitting record; the sitting record's byte counts taken from the
   writing side's own insertion, which spliced and did not rewrite; and the reading rule of
   `gen_evidence_pin_membership.py` at the TOOL's own source rather than from the seventh dispatch's
   statement of it, which is incomplete and is corrected on this face. **Declared departures of this
   writing side:** (i) `git show <hash>:path` piped to `grep`, `sed` and a python parser on the user's
   machine for the tool constants, the guard summary and the tool's own source — git object queries by
   explicit hash, their output filtered, the ninety-fifth entry's declared form; (ii) a python
   read-modify-write on the user's machine to splice the Ruling 78 section into the sitting record and
   to apply this face's own pre-release corrections, each anchor proven unique before the write and
   each result read back through the file tools afterwards — **a write, not a read for a fact**;
   (iii) `git hash-object` on eight working-tree paths, compared against their blobs at the tip — it
   returns a hash and no content, and it is the form this project's own dispatches order for an
   object-level check; (iv) one `cat` of the two ref files on the user's machine, AFTER both had
   already been read with the file tools and agreeing with them — a departure, and recorded as one.
   **No working-tree file was read for its CONTENT by any shell.**
4. *File-tools rule:* every repository read through the file tools on bridge-staged snapshots, save
   the declared departure (i) above; no `git status`, no working-tree diff, no `git log`.
5. *Uncertainty:* that Row 4.3's block ends at its `*PROPOSED DISPOSITION.*` paragraph is this side's
   reading of the staged copy at blob `f83b30d3…`; a block whose shape differs is a STOP for that edit
   and therefore for Task 1. Whether §11 and §12's re-derivation is owed as one act or as part of a
   later one is not settled by this dispatch and is not attempted in it.
6. *Re-read from disk before release:* performed over this file after it was written to the root, and
   over the sitting record's inserted section, both read back through the file tools.

---

*Provenance: Cowork writing side, 2026-09-04, the session that booted on
`cowork_handoff_entry_ninety_eight.md`, at tip `7a5f307563…` with `origin/master` equal, under Ruling
78 (§3cg) and Ruling 62 (§3bq, item 4) of
`cowork_rulings_2026_08_31_decision_surface_sitting.md`, the reading file's §5, and the phase
definition §3.4. TOWARDS the ultimate objective and TOWARDS the guiding principles.*
