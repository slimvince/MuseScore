# CC dispatch — the decisions-register baseline repair (Alternative B, three parts); land the §8 move and the session's records; sweep the bibliography's `Local ✓` rows; boot NO session

> # ⏸ PARKED
>
> **Parked 2026-08-28 by the Cowork writing side, under the standing rule that an instruction file
> which exists but is not the active dispatch carries this banner.** It is written and complete; it is
> not the active dispatch and **must be revalidated against the then-current `STATUS.md` and HEAD
> immediately before dispatch, with a dated dispatch note added here.**
>
> **★ WHY IT IS PARKED RATHER THAN SENT.** It was written when the seventy-ninth handoff entry's
> cadence made it next. **The user's standing bar of 2026-08-28 — keep the progress of the plan in
> focus, never act on the latest impulse — changes the order.** Every subject in this dispatch is
> apparatus: under `CLAUDE.md`'s non-gating declaration none of it bears on the analysis, its inputs,
> or a measurement tool something depends on, so it gates nothing. **The framework document is derived
> and declares its own outstanding gap; that gap, not this, is what moves the phase.**
>
> **Nothing here is withdrawn and nothing needs rewriting** — the register blocker is still real, the
> repair is still the user's disposition, and this file is ready the moment it is next.
>
> **★ ONE PREMISE WILL HAVE GONE STALE BY THEN AND IT IS THE ONE THAT STOPS A BATCH: A1.** Every
> further act on this tree changes the untracked population, and A1's own STOP fires on a path it does
> not name. **Revalidate A1 before dispatch, not during.**

> **Dispatch (Cowork, 2026-08-28).** Executes the user's disposition of the decisions-register
> blocker, taken in conversation on `cowork_register_blocker_surface_2026_08_28.md`. **Nothing is
> running and nothing else is dispatched.**
>
> **★ WHAT THIS BATCH IS.** Task 0 establishes two things read-only and carries one BRANCH. Task 1
> lands this session's documents, including a byte-preserving move of the brief's §8 out into its own
> file. Task 2 performs the three-part register repair. Task 3 sweeps the bibliography's `Local ✓`
> rows and reports. Task 4 closes. **That is the whole of it.**
>
> **★ TWO OF THE USER'S DISPOSITIONS OF 2026-08-28 RIDE INSIDE TASK 1 AND ARE NOT PART OF THE
> REPAIR.** The **§8 bar is DROPPED** (step 3) and **Alternative C is PERFORMED as a written
> suspension with a DERIVED list** (step 5). **Both survive limb (b)** — neither depends on Task 0's
> branch, and neither is skipped if Task 2 is.
>
> **★ WHAT THIS BATCH IS NOT.** No `src/` edit, no golden, no test, no measurement of the ANALYSIS, no
> design, no derivation of any specification statement, **NO SESSION BOOTED**, no framework text
> authored, no document archived, moved or deleted AS A FILE, no open-items row created, flipped or
> discarded, **no edit to any governing document.**
>
> **★ THIS BATCH DOES EDIT TOOL SOURCE, AND THAT IS THE POINT OF IT. READ THIS BEFORE ANYTHING
> ELSE.** Every dispatch before this one barred tool-source edits. **This one orders them, at exactly
> three sites named in Task 2 and nowhere else.** A tool-source edit at any other site is a
> STOP-and-report. **And, BY NAME, the forward-bound re-aiming of `gen_status_batch_bound.py --apply`
> in Task 4 is EXCEPTED from every bar in this dispatch** — it is a named carve-out under Ruling 5 of
> `cowork_rulings_2026_08_26_amendment_landing_sitting.md`, and it is written here because the two
> previous dispatches wrote a no-tool-source-edit bar without it and put their batches between two
> instructions.
>
> **★ THIS BATCH ALSO EDITS THE REGISTER'S DATA FILE, AT ONE FIELD.** `tools/audit/decisions/backbone_decisions.json`
> → `retired_entries` → `the_population_before_this_retirement`, **one integer, 680 → 677.** **No
> register ENTRY is created, moved, retired, revived or edited; nothing moves between the live and
> retired blocks; no register file is regenerated by hand.** An edit to any other field of that file
> is a STOP-and-report.
>
> **★ THE ASSUMPTION THAT HAS CHANGED SINCE EVERY PREVIOUS DISPATCH.** The last dispatch declared
> four tracked modifications. **This one declares TWO, and FIVE untracked additions.** A1 states them
> by path. **A modification at any path A1 does not name is a STOP-and-report.**
>
> **★ THE TRIP-WIRE THIS BATCH MUST NOT TOUCH.** `apply_residue_discard.py` verifies on every run
> that seven sentences of `cowork_rulings_2026_08_17_residue_sitting.md` are still present in that
> record, and STOPs if one is gone. **This batch does not edit that ruling record, or any ruling
> record, in any way.** The repair changes what a check reconciles OVER; it changes no ruled sum and
> no ruled sentence.
>
> **★ WORDS USED HERE, EXPLAINED FIRST.** *The block* — the `retired_entries` object in
> `tools/audit/decisions/backbone_decisions.json`, holding BOTH retirements. *The former population*
> — that block's `the_population_before_this_retirement` field, which names the size of the register
> before the 2026-08-16 retirement. *The sitting's population* — the 677 entries that existed at that
> retirement. *A later entry* — a live register entry created AFTER the second retirement
> (2026-08-17), and therefore never a member of the sitting's population.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md` —
> CONDITIONAL, and this batch does NOT meet the condition: rule (a)'s `gating_ids` only. **(2)**
> `cowork_handoff.md`, **ITS CURRENT ENTRY (the seventy-ninth) and the entry below it**, AND the
> untracked `cowork_handoff_entry_eighty.md` at the root, which is the eightieth entry awaiting the
> prepend Task 1 orders. **(3)** `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL —
> every `###` section of it carrying the standing-clause marker. **(4)**
> `cowork_register_blocker_surface_2026_08_28.md` whole: it is the surface the user disposed of and
> it states the alternatives and their grounds. **You do NOT open:** `ARCHITECTURE.md`; any of the
> three sealed placement-sample files; `cowork_informed_session_brief_framework.md` beyond the two
> sites Task 1 names; either pack directory's contents.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first act (CHECK mode) and again at
> the end (write mode, committed as the artifact of a real run). Verify every commit at the object by
> explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and a working-tree
> `git diff` are denied by the armed guard — use the per-path explicit-hash forms and
> `tools/audit/changed_paths.py`. Plain single-quoted `git commit -m '…'` subjects.**

## Ruling ledger

- **The user's disposition of the register blocker, 2026-08-28. His word was "ok", given in
  conversation and NOT in a single-limbed ruling record.** It is recorded as that and is not dressed
  up as more. **Alternative B, in three parts:** restore the former population to 677; re-shape the
  block-level assertion to account for the population as it stood at the retirement, with later
  entries outside it; re-shape `apply_residue_discard.py`'s `the_arithmetic` the same way. **He also
  took the sub-fork on the writing side's recommendation and not by a separate ruling: the field is
  HISTORICAL, not a running total.**
- **Alternative A is WITHDRAWN** on the record's own words, not on preference:
  `apply_soft_discard.py`'s source states the purpose of its plan comparison — *"A plan whose
  arithmetic was rewritten still fails, which is what this check exists for."*
- **Alternative C — a written suspension of register rule (c) with the owed entries listed — was
  recommended alongside B and is NOT RULED.** This batch does not perform it and does not write it.
- **Ruling 5 of `cowork_rulings_2026_08_26_amendment_landing_sitting.md`**, read at the record by the
  writing side: the forward-bound re-aiming is *"a **named carve-out** from the no-tool-source-edit
  bar in this and future dispatches"*, on the ground that the bar *"was this side's own, written
  over-broad … without checking what it would block."*
- **The user's disposition of the §8 breach, 2026-08-28,** taken as settled from his question *"why
  do you need a decision?"*: **the draft STANDS**; **§8 moves OUT of the brief into its own file**;
  and the author's continuing past an explicit stop is **recorded as a note, not ruled.**

## Premise ledger

**★ THE WRITING SIDE HAS NO SHELL AND ASSERTS NO GIT-OBJECT VALUE.** Everything below was read with
the file tools on bridge-staged snapshots of the working tree on 2026-08-28. **No commit, blob or
tree value is stated anywhere in this dispatch and none may be taken from it.** The tip was read as
a file at `.git/refs/heads/master` and is `6005daecaf9f1a6692e61521911ef8b99ed73b55`; **establish it,
and everything else, at the objects yourself.**

- **FACT — the false figure, read at the data file.** `tools/audit/decisions/backbone_decisions.json`
  → `retired_entries` → `the_population_before_this_retirement` reads **680**.
- **FACT — what it should read, by two independent committed routes.** The committed plan
  `tools/audit/soft_discard_application.json` records `the_live_record_before: 677`,
  `retired_by_this_act: 165`, `the_live_record_after: 512`, and its performed-state block records the
  same three; and `cowork_rulings_2026_08_17_residue_sitting.md` §5 states *"474 + 203 = 677, the
  register's whole non-trivial population"*. **Both say 677. The field is the only thing that says
  680.**
- **FACT — the two red checks and their exact causes, quoted from `tools/audit/guard_state.json`.**
  `decisions/apply_soft_discard.py --check`: *"the committed plan's recorded arithmetic disagrees
  with the data file's: the plan records {'the_live_record_before': 677, 'retired_by_this_act': 165,
  'the_live_record_after': 512}, while the block's former population is 680 …"*.
  `decisions/apply_residue_discard.py --check`: *"the sitting's arithmetic does not reconcile at
  ['the_whole_population', 'the_live_record']"* — `the_whole_population` with `keep_plus_retired`
  **677**, `the_sum_the_sitting_states` **677** and the data file's before-figure **680**; and
  `the_live_record` with `after_this_act` **477** against the sitting's **474**.
- **FACT — the guard summary.** `tools/audit/guard_state.json` → `summary`: run **75**, passing
  **72**, failing **3** — the two above and `gen_filing_convention_application.py --check` —
  `not_run` **4**, both `ratification_surfaces/` tools among them with authored reasons.
- **FACT — the three later entries, read at the data file.** D-678, D-679 and D-680 are live, each
  carries `"date": "2026-08-25"`, and each `status_source` names **`cc_instruction_amendment_landing.md`
  Task 6** as the act that entered it. **NOT ESTABLISHED: that the same act bumped the former
  population field.** Task 0 establishes that at the objects; it is the strong candidate and it is
  not asserted here.
- **FACT — where the two assertions to be re-shaped live.**
  `tools/audit/decisions/apply_soft_discard.py`, the block-level assertion `len(live) + len(retired)
  != before` in `check_applied`, immediately above the `required` tuple; and
  `tools/audit/decisions/apply_residue_discard.py`, the function `the_arithmetic`, its
  `the_live_record` sub-dict. **The prose twin of the first lives in the data file**, in the block's
  `★_the_stops_this_block_carries_so_it_cannot_become_a_hole` list: *"live entries plus retired
  entries must equal `the_population_before_this_retirement` exactly …"*.
- **FACT — `apply_soft_discard.py`'s check has ALREADY been re-aimed once**, for a closely related
  reason, with the justification written into the source as a comment above the plan comparison.
  **B's parts 2 and 3 are the second application of a liberty already taken at this same site, not a
  new one.**

- **ASSUMPTION A1 — the working tree, by PATH and by SHAPE. ★ THIS IS THE ONE PLACE THIS DISPATCH IS
  UNSURE, AND IT IS DECLARED AS A RANGE RATHER THAN GUESSED AS A FIGURE.** The seventy-ninth handoff
  entry states two tracked modifications — itself and the brief — **and states in the same breath
  that its own figure must not be assumed. The writing side has no shell and could not enumerate the
  index to settle it.** So A1 asserts a shape, not a count:
  1. **`cowork_informed_session_brief_framework.md` IS modified** — the previous session's revision,
     unlanded. **Task 1 adds its own two edits to this path**, so it is the one path where deletions
     are expected.
  2. **`cowork_handoff.md` MAY be modified or clean.** The writing side did not touch it this
     session; the eightieth entry sits untracked at `cowork_handoff_entry_eighty.md` and Task 1
     prepends it. **Either state is expected. Report which you find.**
  3. **NO OTHER tracked path is modified. A third modified path is a STOP-and-report.**

  **Enumerate with `tools/audit/changed_paths.py` and report what you find before acting.**

  **Untracked and to be landed by Task 1 — eight paths:**
  `cowork_framework_document_draft_2026_08_28.md` (the framework author's),
  `cowork_register_blocker_surface_2026_08_28.md`, `cowork_section8_breach_surface_2026_08_28.md`,
  `cowork_informed_brief_provenance.md`, `cowork_section8_bar_record_2026_08_28.md`,
  `cowork_register_rule_c_suspension_2026_08_28.md`,
  `cowork_rulings_2026_08_28_framework_delta_sitting.md`, and this dispatch.
  **`cowork_handoff_entry_eighty.md` is untracked and is NOT landed as a file** — Task 1 prepends its
  content into `cowork_handoff.md` and then removes it.
- **ASSUMPTION A2 — the guard state.** **75 / 72 / 3** at the start, the three named above.
  `gen_evidence_pin_membership.py --check` is red between this dispatch's untracked inputs and its
  regeneration inside Task 1 — **caused by this batch's own inputs, and named here for that reason.**
  **Any other red before Task 2 is a STOP-and-report.**
- **ASSUMPTION A3 — the end state of the two repaired checks.** After Task 2, **both
  `decisions/apply_soft_discard.py --check` and `decisions/apply_residue_discard.py --check` PASS**,
  and `gen_filing_convention_application.py --check` remains the sole failing member. **This is a
  PREDICTION and is registered as one.** If either stays red, **MEASURE the cause and report it; do
  NOT loosen an assertion further to force it green.** A check made to pass by weakening it past what
  Task 2 orders is the exact defect the record treats as worst.
- **ASSUMPTION A4 — the later-entry population, by TWO routes.** Task 2 derives the set of later
  entries and **never types it**. **The prediction: both routes return the same set, and it has
  exactly three members.** **If the two routes disagree, that is a STOP-and-report and the repair is
  not performed** — the standing clause requiring a prediction to be taken from every route is
  exactly why two are ordered.
- **ASSUMPTION A5 — the boot pack is untouched.** `tools/audit/derivation_boot_pack.json` and its
  pack files are byte-identical to their committed blobs throughout. Prove the manifest by hash; do
  **not** run the generator.

## The declared start state

Three failing guard checks — `gen_filing_convention_application.py --check` and the two register
checks, each for the cause quoted in the premise ledger — plus `gen_evidence_pin_membership.py
--check` STALE, **caused by this dispatch's own untracked inputs and by the four other untracked
files listed at A1**, the membership derivation's population being the file system rather than the
index. **A further failing verdict is a STOP-and-report.**

## Findings in this batch

**No finding number is allocated.** [[OI-179]] OPEN and GATES.

**One item is carried and NOT acted on:** the writing side's reading of
`cowork_rulings_2026_08_17_residue_sitting.md` §5, at Task 0. It is carried because Task 0's own
read is the thing that grades it.

## Task 0 — establish two things, read-only, and take the BRANCH

**Nothing is written in this task.**

1. **★ THE BRANCH, AND IT GOVERNS WHETHER TASK 2 RUNS AT ALL.** Read
   `cowork_rulings_2026_08_17_residue_sitting.md` **whole**. Its §5 ends: *"A derivation that does not
   reconcile to these sums is a STOP-and-report, not an adjustment."* **Decide, at the record and not
   from this dispatch, which of these two the sentence says**, and report the decision with the words
   it rests on:

   **(a)** it bars a derivation from ADJUSTING THE RULED SUMS when it fails to reconcile — in which
   case **Task 2 is permitted**, because B changes none of 474, 203, 677 or the 512 → 474 movement;
   it changes only which population those sums are reconciled OVER; **or**

   **(b)** it bars any later change to what "reconciling" means for these sums at all — in which case
   **TASK 2 IS NOT PERFORMED.** Tasks 1, 3 and 4 still run; the close reports the bar, quotes it, and
   the blocker returns to the user unrepaired.

   **The writing side's own reading, declared so that it can be graded and not so that it can be
   followed:** it reads as **(a)**. The grounds it offers, each checkable at the record — the
   sentence's subject is *"a derivation"*, in a paragraph that opens *"stated as a registered
   expectation for the executing batch"*, so its addressee is the 2026-08-17 executing act; the four
   sums it protects are all preserved unchanged by B; and the failure being repaired was caused by
   three entries created eight days AFTER the sitting, which the sitting's expectation cannot have
   been about. **AGAINST that reading, and stated because an unopposed reading is not an established
   one:** `apply_residue_discard.py`'s own docstring asserts the sentence governs the live re-check —
   *"THE SITTING'S OWN ARITHMETIC IS RE-RECONCILED against the record as it stands, and the ruling
   makes that a STOP rather than an adjustment in its own words."* **That is the tool author's
   reading of the sentence, not the sentence.** Grade both at the record. **If you land on (b), or
   cannot decide between them, take (b)** — the conservative limb, because B is not urgent and a
   wrongly-permitted re-aiming is not recoverable by a later reading.

2. **Establish which act bumped the former population from 677 to 680**, at the git objects:
   `git log -L` or equivalent over the `the_population_before_this_retirement` field in
   `tools/audit/decisions/backbone_decisions.json`, back to the commit that wrote 680. **Report the
   commit, its date, its subject and the dispatch named in it.** **The candidate the writing side
   read at the data file — `cc_instruction_amendment_landing.md` Task 6, the act that entered D-678,
   D-679 and D-680 — is a CANDIDATE and is not the answer;** report what the objects say.
   **Report additionally whether that act's own dispatch or report mentions the field at all.** This
   is the handoff's owed item *"a change to the register's data file outside a ruled discard act is a
   separate question"* — **it is REPORTED here and NOTHING is done about it.**

**E0:** the branch limb taken, with the words it rests on quoted at the record; the bumping commit
identified at the object with its subject; and a statement of whether the bumping act's dispatch
mentions the field.

## Task 1 — land this session's documents, including the §8 move, and PUSH

1. **A1's check first**, entirely at content-addressed objects. Enumerate with
   `tools/audit/changed_paths.py`; report every tracked modification and every untracked addition you
   find; **a third tracked modification is a STOP-and-report.**

2. **THE §8 MOVE, AND IT IS BYTE-PRESERVING OR IT DOES NOT HAPPEN.** In
   `cowork_informed_session_brief_framework.md`, the final section begins at the heading line
   `## 8. Provenance — for the user and the record; NOT opened by the deriving session` and runs to
   the end of the file.
   **The two regions to compare, stated so neither is guessed.** In the brief: the `## 8. Provenance …`
   heading line through **the last non-empty line of the file**. In the already-written
   `cowork_informed_brief_provenance.md`: its identical `## 8. Provenance …` heading line through the
   last non-empty line **before** the `---` separator that follows it.
   - **PROVE FIRST, before deleting anything:** the two regions are **byte-identical**. Compare by
     hash of the two extractions. **A difference of any kind is a STOP-and-report and nothing is
     deleted.**
   - **THEN delete** that region from the brief, together with the `---` separator line and blank
     line immediately preceding the heading if present, leaving the brief ending cleanly at §7.
   - **Report the brief's `--numstat` for this edit and the deleted line count.**

3. **THE ONE OTHER EDIT TO THE BRIEF, and it is a single bullet REPLACED — a bar dropped and a
   different bar put in its place.** In §3's closed list, the bullet reading
   `- **§8 of this brief**, its provenance section.` becomes
   `- **\`cowork_section8_bar_record_2026_08_28.md\`** — the record of how the §8 bar was governed. *(The former §8 itself is at \`cowork_informed_brief_provenance.md\` and is NOT closed: the user dropped that bar on 2026-08-28.)*`

   **★ THESE ARE TWO DIFFERENT ACTS AND A REPORT MUST NOT COLLAPSE THEM.** The §8 bar is **dropped
   on the user's disposition**. The new item is **the writing side's own act**, adding a bar on a
   file that did not exist when the list was written, and it is flagged for the user at the close.
   **No other line of §3 changes.**

   **Verify after the edit that the brief contains no remaining reference to "§8" that would send a
   reader looking for a section that is no longer there;** report any you find and **do not act on
   them** — the writing side settles the wording.

4. **THE HANDOFF PREPEND.** `cowork_handoff.md` begins with its `# Cowork Session Handoff …` title
   line, then a blank line, then the `---` that opens the seventy-ninth entry.
   `cowork_handoff_entry_eighty.md` **begins with its own `---` and ends with a blank line**, so it
   is inserted **whole and unchanged immediately after that blank line following the title** — the
   eightieth entry then sits exactly where the seventy-ninth sat, and the seventy-ninth follows it.
   **Additions-only: prove ZERO deletion lines at the blob-to-blob diff.**
   Then **delete `cowork_handoff_entry_eighty.md` from the working tree** so it does not survive as
   a second home for the entry (**#6**). **It is untracked and has never been committed, so this is a
   plain file removal and NOT a `git rm`** — and it is the one file this batch deletes, ordered by
   name, with the standing bar on deleting a document as a file untouched for every other path. **The entry count is not asserted by this dispatch:** establish it at the
   object with the pattern `^## .*COWORK SESSION CLOSE`, the loose form, and **if you also report a
   strict-pattern figure, say which pattern produced which number.**

5. **FILL IN THE SUSPENSION LIST — Alternative C, ordered by the user on 2026-08-28.**
   `cowork_register_rule_c_suspension_2026_08_28.md` carries an empty list under
   `## The owed entries` with an HTML-comment placeholder. **DERIVE the list and write it in there,
   replacing the placeholder.** The population: **every ratification, shelving or falsification
   recorded in a `cowork_rulings_*.md` record on disk that has NO decisions-register entry**, taken
   over the records dated from the last register entry's date forward. **Enumerate it at the files
   and at the register's data file; never type it from this dispatch, which lists none, and never
   from a session's recollection.** Each row names **the ruling record, the ruling within it, and its
   date — and nothing else.** **Allocate no `D-NNN` identity**, and **write no register entry**: the
   register does not accept them until Task 2 lands, which is the whole point of the suspension.
   **Report the population size; do not restate it in `STATUS.md` (D-431).** **If the derivation
   cannot bound the population — if you cannot establish where the un-entered run begins — say so and
   leave the placeholder, with the reason written in its place. An empty list left silently is a
   defect; an empty list with its reason is a finding.**

6. **REGENERATE `tools/audit/evidence_pin_membership.json`** and MEASURE its diff against the
   committed blob before accepting it. Movement beyond the untracked files this task lands is
   reported rather than absorbed.

7. **Commit exactly these paths and no others:** `cowork_handoff.md` (M),
   `cowork_informed_session_brief_framework.md` (M), `cowork_informed_brief_provenance.md` (A),
   `cowork_section8_bar_record_2026_08_28.md` (A),
   `cowork_register_rule_c_suspension_2026_08_28.md` (A),
   `cowork_rulings_2026_08_28_framework_delta_sitting.md` (A),
   `cowork_framework_document_draft_2026_08_28.md` (A),
   `cowork_register_blocker_surface_2026_08_28.md` (A),
   `cowork_section8_breach_surface_2026_08_28.md` (A),
   `cc_instruction_register_baseline_repair.md` (A),
   `tools/audit/evidence_pin_membership.json` (regenerated). Subject, exactly:
   `record: the register blocker and the §8 breach are disposed — the brief's provenance section moves out to its own file, and the framework draft and both decision surfaces land`

8. **PUSH**; verify at the object. **`gen_evidence_pin_membership.py --check` must PASS.**

**E1:** the eleven committed paths and no others; the §8 region proven byte-identical BEFORE deletion;
the §3 bullet replaced, with the dropped bar and the added bar reported as two separate acts; the
suspension list derived and written in, or its absence explained; the handoff prepend proven
additions-only with zero deletions; the entry count established with its pattern named; the
membership check passing; `origin/master` at the commit.

## Task 2 — the three-part register repair — SKIPPED ENTIRELY IF TASK 0 TOOK LIMB (b)

**★ THE ORDER MATTERS AND IS NOT A PREFERENCE.** Part 1 alone leaves BOTH checks red for NEW reasons;
parts 2 and 3 are what make them green. **Do all three, then run the checks; do not run them between
parts and conclude anything from it.**

1. **Part 1 — the false figure.** In `tools/audit/decisions/backbone_decisions.json`, set
   `retired_entries.the_population_before_this_retirement` from **680** to **677**. **One integer.
   Prove at the blob-to-blob diff that exactly one line changed and that it is that line.**

2. **Derive the later-entry set, by BOTH routes, and NEVER type it.**
   - **Route 1 — by date.** Every live entry in `decisions` whose `date` is strictly later than the
     block's latest `acts` entry's retirement date (2026-08-17, read at the block, not typed from
     here).
   - **Route 2 — by identity ordinal.** Every live entry whose `D-NNN` ordinal exceeds the restored
     former population (677, read at the field after part 1). **This route is only valid if identity
     allocation is dense and monotonic across both blocks — ESTABLISH that: the ordinals of the live
     entries and the retired entries together must be exactly the integers 1..680, each once. If
     they are not, Route 2 is VOID; say so, and STOP** rather than proceeding on one route.
   - **The two routes must return the SAME SET. A disagreement is a STOP-and-report and no assertion
     is re-shaped.** Report the set's members and its size.

3. **Part 2 — the block-level assertion, in the code AND in its prose twin.**
   - In `tools/audit/decisions/apply_soft_discard.py`'s `check_applied`, re-shape
     `len(live) + len(retired) != before` so that it accounts for **the population as it stood at the
     retirement**: the live entries that are NOT later entries, plus the retired entries, must equal
     the former population exactly. **The later-entry set is derived inside the tool by Route 1** —
     the tool must not depend on identity density, which Route 2 only checks and does not guarantee
     forward. **The STOP message must still name what it caught** — an entry in NEITHER block — and
     must additionally report how many later entries it set aside, so the number is published rather
     than absorbed.
   - **Write the reason into the source at the site, in the shape the existing re-aiming comment at
     that file uses**, naming: the user's disposition of 2026-08-28; that the field is HISTORICAL and
     not a running total; and that the assertion formerly required a historical quantity to grow with
     every new entry, which is what put a false figure into the data file.
   - **In `tools/audit/decisions/backbone_decisions.json`**, re-shape the matching prose stop in
     `retired_entries.★_the_stops_this_block_carries_so_it_cannot_become_a_hole` — the item reading
     *"live entries plus retired entries must equal `the_population_before_this_retirement`
     exactly …"* — so it states the same thing the code now asserts. **The code and its prose twin
     disagreeing is the condition this repair exists to remove; leaving the prose is not an option.**

4. **Part 3 — `the_arithmetic`.** In `tools/audit/decisions/apply_residue_discard.py`, re-shape the
   `the_live_record` sub-dict's `it_reconciles` so the sitting's ruled keep-side is reconciled over
   **the sitting's own population** — the live record less the later entries — rather than over the
   live record as it stands today. **`RULED_KEEP_SIDE`, `RULED_RETIRED` and `RULED_TOTAL` are NOT
   CHANGED and NOT MOVED; if you find yourself editing one of those three, STOP.** Publish the
   later-entry count in the returned dict beside the figures, so a reader of the check's output sees
   what was set aside. **Write the reason at the site**, citing the user's disposition and Task 0's
   branch finding.

5. **Run both checks.** Then run the FULL guard set. **A3 predicts both green and
   `gen_filing_convention_application.py --check` the only remaining red.** Grade A3. **If a check is
   still red, report the cause; do not weaken anything further.**

6. **Commit exactly the four changed paths and no others:**
   `tools/audit/decisions/backbone_decisions.json`, `tools/audit/decisions/apply_soft_discard.py`,
   `tools/audit/decisions/apply_residue_discard.py`, and `tools/audit/guard_state.json` if the guard
   run rewrote it. **Regenerate any register-derived artifact by its own generator if a generator's
   `--check` goes red as a result, and report which** — but **do not hand-edit `DECISIONS.md` or any
   `decisions/group_<X>.md`.** Subject, exactly:
   `fix: the register's former-population field carried a false historical figure — restore 677 and re-aim both discard checks over the population as it stood at the retirement`

7. **PUSH**; verify at the object.

**E2:** the four sums 474 / 203 / 677 / 512→474 unchanged at both tools, proven by showing the three
constants and the plan's figures untouched at the blobs; the later-entry set derived by two agreeing
routes and its members reported; both register checks green; a full guard run in which
`gen_filing_convention_application.py --check` is the ONLY failing member — **the summary's figures
measured and reported from that run, never restated from this dispatch, which asserts none**;
`origin/master` at the commit.

**★ IF TASK 0 TOOK LIMB (b):** this task is not performed, nothing above is edited, and Task 4's
report says so with the ruling's words. **Do not perform a partial repair.**

## Task 3 — the bibliography sweep, REPORT ONLY

**Nothing is corrected in this task.** `docs/research_papers/BIBLIOGRAPHY.md` carries a `Local`
column, and a `✓` in it asserts that the paper is present in `docs/research_papers/`. **One such row
is already known false at the object:** line 86, Humphrey & Bello 2015 — the file at
`docs/research_papers/humphrey_bello_2015_ismir_four_timely_insights_ace.pdf` is page 1 of *"Electric
Guitar Playing Technique Detection in Real-World Recordings Based on F0 Sequence Pattern
Recognition"*, Chen, Su & Yang, ISMIR 2015, p. 708 — **a different paper from the same proceedings.**

1. **For every row carrying `Local ✓`:** confirm the named file exists, and **open it far enough to
   establish that its title and authors are the row's** — a file's existence at a plausible name is
   what already failed here. **Report every row as CONFIRMED, FALSE, or UNCHECKABLE with the reason.**
2. **Report the population size and the method**, so the sweep's reach is measured rather than
   claimed. **A row you cannot open — an image-only scan with no extractable text — is UNCHECKABLE
   and is reported as such, never as confirmed.**
3. **Correct nothing**, in the bibliography or on disk. **No file is renamed, re-fetched, moved or
   deleted.** The disposition of a false row is the user's.

**E3:** every `Local ✓` row graded, the population size stated, and the method named.

## Task 4 — the close

1. One `STATUS.md` pointer entry per task, per the OI-222 pointer convention — no count, no identity
   and no rendered value restated (**D-431**). The previous batch's entries move through
   **`gen_status_batch_bound.py --apply`** — **the named carve-out; it re-aims its own forward bound
   and that is expected, declared and permitted** — and `gen_session_start_read_size.py` regenerates.
2. The report is **`cc_report_register_baseline_repair.md`**: both guard states, every SHA, E0–E3
   graded, A1–A5 graded, declared departures, **Task 0's branch limb with the words it rests on**,
   **the bumping commit**, the later-entry set and its two routes, the §8 move's byte-identity proof,
   the handoff entry count with its pattern named, the bibliography sweep's table, and the plan
   lines.
3. **★ ONE ITEM IS PUT TO THE USER AND IS THE WRITING SIDE'S OWN ACT:** the §3 closed-list item
   naming `cowork_section8_bar_record_2026_08_28.md`. **The user dropped the §8 bar; he did not add
   this one.** State it as a bar added by the writing side over a file that did not exist when the
   list was written, and say it can be struck in one line.
4. **The plan lines, stated as owed and NOT as done:** **the framework document's placement test,
   which is the plan's live act and the only item here that moves the phase forward** — by a side
   that did not author, and noting that it now measures **coverage, not independence**; the
   disposition of any FALSE bibliography row; the independent challenge run; the three sealed
   samples; the first-stage draft's home; the **#18** exposure; the correction of the §3.3
   explanation in `cc_report_framework_arrangement_landing.md`; and **the dispatch-protocol note that
   a no-tool-source-edit bar must EXCEPT the forward-bound re-aiming BY NAME, which is OWED at
   `cowork_audit_protocol.md` and which no session writes on its own authority.**
5. **★ SAY PLAINLY, AT THE HEAD OF THE PLAN LINES, THAT EVERYTHING THIS BATCH DID IS APPARATUS.**
   Under `CLAUDE.md`'s non-gating declaration, no part of this batch's subject bears on the analysis,
   its inputs, or a measurement tool the analysis depends on. **It gates nothing and it moved the
   framework phase not at all.** A successor reading the report should meet that before it meets the
   detail.
6. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; one further commit carries it.
7. Commit, push, verify; then the one further commit with the end-state guard run.

**E4:** at the tree carrying the close, a fresh full guard run — **on limb (a),
`gen_filing_convention_application.py --check` failing and no other, zero STOPs; on limb (b), the
three known failing and no others** — committed only after the run that produced it.

## What this batch does NOT do

- **It writes no ruling record and rules nothing.** The user's word on the register was "ok" in
  conversation; the §8 disposition was taken from a question. **Both are recorded as that.**
- **It enters NO decisions-register entry and allocates no `D-NNN` identity.** Alternative C is
  performed as a written suspension with a derived list, which is the opposite of routing around the
  blocker silently — **the eighth consecutive act unable to enter an entry, and the first to say in
  one place what is owed.**
- **It does not edit `CLAUDE.md`.** The suspension stands beside rule (c) as a dated record; a
  governing document is not edited by a session.
- **It does not correct `cc_report_framework_arrangement_landing.md` §3.3.** Amending a landed report
  is the user's act.
- **It edits no ruling record, no governing document, and no register ENTRY.**
- **It boots no session, renders no pack, runs no placement test and authors no framework text.**
- **It does not act on the bumping commit Task 0 identifies.** That is reported and left.

## The writing side's self-check over this dispatch

1. *Principles touched:* **#12** — the §8 move is byte-preserving and is proven so BEFORE the
   deletion, and the framework draft, both surfaces and the superseded brief are all kept. **#6** —
   the eightieth handoff entry gets one home, so its staging file is removed in the same task that
   prepends it; the register's prose stop and its code assertion are re-shaped together rather than
   left to disagree. **#13** — the changed A1 shape and the tool-source edits are stated at the head
   as the things that will stop a batch carrying the old assumption. **#17f / D-431** — no count and
   no git-object value asserted; the handoff entry count is ordered measured with its pattern named.
   **#19** — A3 and A4 are registered as predictions, and A4 is ordered taken from **both** routes,
   which is the standing clause's own requirement.
2. *Conventions:* American English; no self-invented labels; no music-theory word arises in this
   batch's subject matter.
3. *Figures and premises:* every figure above was read at the files by the writing side on
   2026-08-28 on bridge-staged snapshots and is ordered re-established at the objects.
4. *File-tools rule:* the writing side read only through the file tools and ran **no shell command of
   any kind** — the user barred it for this session — **so it verified no git object and this dispatch
   relays none.**
5. *Uncertainty, and this dispatch has more of it than its predecessors:* **A1's tracked-modification
   count could not be enumerated without an index read, and is declared as a range with a STOP rather
   than as a figure.** The measured-not-predicted items are the branch limb, the bumping commit, the
   later-entry set, the entry count, the §8 byte-identity, the membership movement, and every
   bibliography row. **A3 and A4 are predictions and are registered as such.**
6. *The bar this dispatch itself nearly repeated:* an earlier draft of §3's closed-list edit was
   going to be made by the writing side directly. **It is ordered here instead, with a byte-identity
   proof, because the writing side has no shell and a hand-retyped 425-line file is exactly the
   truncation risk the record has already paid for once.**
