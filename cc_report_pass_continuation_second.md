# CC report — the boot-list ruling landed, and the per-entry pass of the widened July screen COMPLETED

> **Dispatch:** `cc_instruction_pass_continuation_second.md` (Cowork, 2026-08-22, the forty-second
> session), continuing Ruling 1 of `cowork_rulings_2026_08_22_dispatch_order_sitting.md` a second
> time and landing Ruling 1 of `cowork_rulings_2026_08_22_boot_list_sitting.md`.
>
> **ALL THREE TASKS RAN. TASK 1 IS A PER-ENTRY PASS AND IT COMPLETED WHOLE** — the unread set is
> EMPTY at the artifact, so no stop is recorded and the ruled failure signal is evaluated over the
> whole widened population for the first time.
>
> **NO STOP-AND-REPORT RULE FIRED.** Every red the batch met was classed by its MEASURED cause and
> every one of those causes is this batch's own act or the declared start state.
>
> **The bars held whole.** No `src/` edit. No golden. No test changed, moved or run. Nothing under
> `tools/corpus/` or `tools/robust_stop/`. No measurement of the ANALYSIS built, designed, scoped or
> run. No design, no repair, no derivation of any specification, no frame, **no pilot act, no
> fact-gate admission, no ledger, no mining read, no session booted from the curated boot list and
> no generator for its per-session withheld list built.** No document archived, moved or deleted AS
> A FILE. **No open-items row created, flipped or discarded.** **No screened document edited** —
> `ARCHITECTURE.md`, `docs/scoring_model.md` and every document-set member were read and never
> written; `STATUS.md`'s content was touched only by Task 2's own pointer entries and by the ruled
> forward-bound move. No re-grading of any authored grade or of any verdict already authored. No
> caller flag or candidacy acted on. **No finding number allocated; the series stands at F88.**
>
> **Figures are not transcribed here (D-431).** Every population, count, verdict and share is named
> as an artifact and a field, or stated as a DIRECTION with its artifact named (D-663). The only
> bare quantities below are commit identities and the counts of this batch's own acts.

---

## 1. The chain of commits, named as far as a sentence can name it

Five commits, each pushed to `origin/master` and each verified at the object by explicit hash:

| # | commit | what it carries |
|---|---|---|
| 1 | `4547a2809f` | Task 0 — the 2026-08-22 boot-list ruling landed; four paths; the push performed |
| 2 | `ed51ddd49c` | Task 1 — verdicts authored, the two joint-estimator members read whole |
| 3 | `8b0a83c71d` | Task 1 — verdicts authored, the two notation members read whole |
| 4 | `3e47297a03` | Task 1 — verdicts authored, the pre-fit gates and the score census read whole |
| 5 | `3e9410bfd7` | Task 1 — verdicts authored, `docs/scoring_model.md` read whole; **the pass completes** |

**THE SIXTH COMMIT IS THIS CLOSE, AND A SENTENCE CANNOT NAME ITS OWN HASH.** It carries this report,
the `STATUS.md` pointer entries, the ruled forward-bound move, the regenerated session-start read
size and the close section. **AND A SEVENTH FOLLOWS IT**, carrying the end-state guard run — because
the end-state run is taken *after* the commit that carries the close exists, so its values cannot be
written into that commit. That is the E-ordering rule at both grains, and it is why this section
names five commits and declares two.

---

## 2. Task by task

### Task 0 — the boot-list ruling landed and the push performed

The branch rule was taken at the tip with `git show -s` and `git for-each-ref` at the explicit hash
and at nothing else: `refs/heads/master` and `refs/remotes/origin/master` both at `40fb613060`, no
`cc_report_pass_continuation_second.md` on disk and no commit on the tip, so the dispatch was **not
started** and was mine to run after re-validating it against the tree.

**A1 was checked FIRST and entirely at content-addressed objects**, by the per-path commands A1
names, because the bare working-tree forms are denied by the armed guard and are measured to time
out on this mount. It **HELD**: `cowork_handoff.md` carries one inserted block against the tip's
blob with exactly one deletion — the forty-first heading marked superseded — and otherwise additions
only; both named untracked paths are present on disk and absent from the tip. `DECISIONS.md` reports
no modification at the object level, exactly as the dispatch's own FACT predicted.

**The parked-instruction clause was discharged before anything ran:** the branch rule was re-taken
at the tip, A1 was re-checked by content, and the sitting record was re-checked for the word
`pinned` through the file tools — it carries none on any line, case-insensitively.

**The declared start state MATCHED EXACTLY.** Two failing verdicts and zero STOPs at the full guard
run taken before the first edit: `gen_filing_convention_application.py --check`, which is
[[OI-372]]'s standing red; and `gen_evidence_pin_membership.py --check`, caused by this dispatch's
own untracked input. The guard registry's own artifact opened *"STALE vs the run"* for the same
cause, which the dispatch declares in advance is not a third failing check, and it is not counted as
one here.

**The membership regeneration was measured against BOTH halves of A3 before it was accepted, and
the prediction HELD on both.** Route A: the ruling-record count rose by exactly one and the one
predicted record name was added. Route B: nothing — the record carries the word `pinned` on no line,
checked before the run, exactly as predicted. No member, route, document, pin constant, state or
count moved beyond that one addition; the whole difference is two lines added and one removed.

Four paths were staged **by explicit path**, never by `git add -A` or `git add .`, and committed
with the subject the dispatch prescribes verbatim. `origin/master` moved to that commit, verified at
the object. `gen_evidence_pin_membership.py --check` **passes** at the resulting tree.

### Task 1 — the per-entry pass, continued and COMPLETED

**The remainder was DERIVED FRESH from `july_screen.json` → `★_the_widened_screen.the_hunks` where
the verdict is the tool's unread default**, before the first passage was read, and never from
`cc_report_step_zero_return_executed.md` §5 or from any report's account of it. It was then
reconciled BOTH WAYS against the artifact's own published summary: the per-document enumeration I
derived matches the report's own per-document unread table member by member, and their sum matches
the artifact's own unread total. The order worked is the artifact's own — by document, then by
commit, then by changed passage.

**Each passage was retrieved from its git object by explicit hash**, read at its own removed and
added text together with its commit's own account, and classified by the inherited method: the four
classes in their DECLARED ORDER, with POSITIVELY CODE-INFLUENCED applied FIRST so that a ratified
act cannot launder a correction made under it. **Every verdict that reaches a later class states
which earlier ones were tested and why each failed**, and every close call is recorded as one with
what was tested and what decided it. The calibration the previous batch established at a git object
was applied throughout: a homing act performed under a named user ruling IS a ratified-act edit,
while a user direction to PERFORM a survey is not a user act whose CONTENT the change records.

**One member is section-scoped and its passages were placed rather than judged.** For each, the
document's own heading structure was read at that commit's own blob and the passage's post-image
line range placed against it; all three fall inside the sections the delegation names, so none was
recorded outside them.

**The pass COMPLETED WHOLE.** Every member of the widened population now carries a verdict, the
unread set is EMPTY at the artifact, and no member is part-read.

### Task 2 — the close

Two `STATUS.md` pointer entries — one for Task 0, one covering Task 1 and this close — and **in the
same act** the previous batch's entries moved verbatim to `STATUS_ARCHIVE.md` through
`gen_status_batch_bound.py --apply`, **both directions proven** by the tool: byte-present in the
archive exactly once, absent from the must-read. The declared `Last updated:` prefix adjustment is
imported from its one home and not re-decided (#6). No entry text was retyped; the move is
byte-faithful by construction. `gen_session_start_read_size.py` was regenerated, which is A2's own
named instance and is cleared by regenerating the measurement and by nothing else.

---

## 3. A3 and A4, each graded at the artifact

| assumption | verdict | what was measured |
|---|---|---|
| **A3 route A — the ruling-record population** | **HELD** | The count rises by exactly one from its committed value and exactly one record name is added, `cowork_rulings_2026_08_22_boot_list_sitting.md`. Read at the regenerated artifact against the committed blob by explicit hash. |
| **A3 route B — the `pinned` scan** | **HELD** | The landed record carries the word on no line, checked through the file tools BEFORE the run; route B adds nothing, exactly as predicted. |
| **A3, the movement bar** | **HELD** | `generated_ratification_documents`, `members`, `members_pinned` and `members_unresolved` are all unchanged; no member, route, document, pin constant, state or count moved beyond the one addition, and no derived cross-reference was added either. |
| **A4 — the guard registry** | **HELD** | No tool was added or removed, so the guard population is unchanged. `epoch_write_path.json` and `recognizer_establishment_sort.json` were both checked after the first bounded run and both PASS — the previous batch's one departure had a path constant added to a generator as its cause, and this batch adds none, so it could not recur and did not. |

---

## 4. The size reported BEFORE Task 1 opened

Stated as a direction with its artifact named, never as a transcribed value (D-663):

**The unread remainder was a small fraction of what the previous batch authored in one session**, so
the pass was expected to complete whole. Its exact size is at `tools/audit/july_screen.json` →
`★_the_widened_screen.counted.unread`, derived fresh at the artifact before the first passage was
read. It was said before reading began, and it is what happened.

---

## 5. THE PASS IS COMPLETE — stated in the standing form, with the unread set EMPTY at the artifact

The standing form records which members were completed, that the remainder is untouched rather than
partly worked, and that nothing is left half-edited. Here the first clause exhausts the population,
so the other two are vacuous and are stated as such rather than omitted.

**What was completed.** Every changed passage of **all seventeen members the widened population
carries** now has a verdict. This batch read the last seven whole:
`cowork_joint_estimator_architecture.md`, `cowork_joint_estimator_factorization.md`,
`cowork_notation_adoption_increment.md`, `cowork_notation_output_contract.md`,
`cowork_prefit_gates.md`, `cowork_score_census.md` and `docs/scoring_model.md`. The members are
named individually rather than counted, as a published subset must be — and here the subset plus the
ten the previous batch completed is the whole.

**What was NOT done.** Nothing. **The unread set is EMPTY at the artifact** — `july_screen.json` →
`★_the_widened_screen.counted.unread` reads zero, and every member's own per-document unread cell
reads zero. No member is part-read and nothing is left half-edited: `--check` re-derives
byte-identically at every commit of this batch, so the artifact was never behind the tool.

**The three passages recorded `OUTSIDE NAMED SECTIONS` are NOT unread.** They are a section-scoped
member's passages that fall outside the sections its delegation names; the tool records them and
does not grade them, which is the declared value for exactly that case, and they are carried in the
artifact's own count separately from the four classes.

---

## 6. The ruled failure signal, evaluated over the WHOLE population for the first time

Ruling 7 of 2026-08-21, quoted as the dispatch requires: *"Its per-document distribution feeds the
reading depth (Ruling 12) and the ordering of units. Failure signal, ruled with it: if most passages
land UNDETERMINED the premise is not measurable, and that is a STOP to the user, not a licence to
proceed."*

**THE SIGNAL DOES NOT FIRE.** It is evaluated mechanically at the artifact —
`july_screen.json` → `★_the_widened_screen.★_the_ruled_failure_signal` — which now carries the
verdict `DOES NOT FIRE` together with the read share, the read-member count and the not-cleared
share of the read. Because every widened member has been read, this is the first time the signal has
been evaluated over the whole population rather than reported
`INCONCLUSIVE-AT-THIS-COVERAGE`; no figure of it is restated here (D-431).

**What that does and does not settle.** It settles that the premise the successor plan rests on is
MEASURABLE on this population — the not-cleared class is a minority of the read members, not a
majority. It settles nothing about what the measured pollution means for the reading depth or the
ordering of units, which is Ruling 12's question and not this batch's.

---

## 7. What the screen found, stated as directions

**On the whole widened population the largest class is the ratified-act edit**, and the
code-influenced class is the second largest — larger, in share, than it was on the part-read
population the previous batch reported, because this batch's last member is the one documentation
pass in the period that re-aimed stale code locators AT THE CODE. **This is the pollution the
successor plan's founding premise names, measured rather than assumed**, and every figure is at
`tools/audit/july_screen.json` → `★_the_widened_screen.counted` and at the report's own per-document
table.

**★ A CODE-INFLUENCED PASSAGE HERE IS NOT THE PERIOD QUESTION'S FALSIFICATION**, and the artifact
says so in its own field. That rule belongs to the ORIGINAL screen, whose population is
out-of-period by construction. This population is deliberately wider because the ruling asks it to
MEASURE the pollution rather than to test the period, so an in-period code-influenced passage is the
measurement's own subject and is EXPECTED.

**Where this batch's code-influenced verdicts concentrate, and what qualifies them.** Almost all of
them sit in one commit's one-document pass, and in that pass every former wording is preserved
verbatim in the same commit's own "Code locators" note (#12), so nothing the document stated is
lost. D-639's own worked examples place a stale anchor OUTSIDE the doc-sync half phase 1 owes. **The
class fires on the test as the tool states it, not on a judgment that a discrepancy signal was
destroyed** — the counter-consideration is recorded once at the shared account in the tool rather
than asserted per verdict, which is the form the original screen's own single code-influenced
verdict already uses. **Two of that pass's corrections are substantive rather than coordinates** and
are named at their verdicts: a stale FILE citation for an enum and a preferences field, and the
place where one bonus fires.

---

## 8. Quarantined audit questions

*Under Ruling 9 a finding that bears on the analysis, its inputs or a measurement tool is SURFACED
here — not rowed, not numbered, not acted on. The four the previous batch surfaced at its report's
§8 stand as found and are NOT restated, re-argued or acted on.*

**ONE NEW QUESTION IS ADDED BESIDE THEM.**

1. **A SPECIFICATION'S OWN ENTRY FOR A SCORING CONSTANT WAS WRONG ABOUT WHERE THE CONSTANT ACTS, AND
   THE CORRECTION RECORDS A NAMING IT DELIBERATELY DID NOT TOUCH.** Read while grading the
   one-document pass: `docs/scoring_model.md`'s §6 entry for `kHalfDimFirstInversionBonus` placed
   the bonus in the enharmonic-flip block, fired from the Gate-A / G-family region on a
   first-inversion alternative. Corrected at the code, it sits in the bias-correction block, after
   the winner's bass-root deduction and before the bias re-sort, and fires on an alternative whose
   third, **fifth or seventh** is the winner's bass. **The correcting note states, and expressly
   does not act on, that the constant's NAME says *first inversion* while the code admits three
   chord factors in the bass** — the code's own comment carrying the same wording. It also records
   that the provenance, the preset gating and the dissolution-target status were carried over from
   the former wording and **were not re-verified by that pass**. This bears on a specification a
   session is required to read before touching scoring logic, and on what a later reader may assume
   a named constant does; it is surfaced, not acted on. *(⚠ The subject is the legacy vertical
   scorer, dormant on both production surfaces — which bounds the exposure and does not remove it,
   because §8's constraints bind a future change regardless of that dormancy.)*

**No finding here is rowed, numbered or acted on**, and no establishment obligation was met or
discharged by anything in this batch.

---

## 9. Findings DISCARDED under the worth test (#10), with finding, date and reason

1. **One base-commit hash was written into a measurement tool's authored input by padding an
   abbreviation instead of resolving it at the object** (2026-08-22). *Reason:* it is exactly the
   shape the dispatch warns against, and it was caught immediately and by the mechanism that exists
   for it — the tool STOPPED with `git show` reporting the path absent from a tree that does not
   exist, before anything was moved, written or committed. It was corrected by resolving the hash at
   the object with `git rev-parse`, nothing ran against the wrong value, and it left nothing in the
   record. It is recorded as a declared departure at §11 rather than carried as an issue: leaving it
   unfixed risks neither something being built that does not serve maximum-precision inference nor
   code ceasing to be comparable against a correct specification.

---

## 10. Declared departures

1. **A measurement tool other than `gen_july_screen.py` was edited — `gen_status_batch_bound.py`,
   and only at its three declared authored inputs.** The standing bars forbid an edit to any
   generator other than the July screen's widened authored block; Task 2 nonetheless orders that
   tool's `--apply`, which cannot run without re-aiming it. **The tool's own docstring declares the
   arrangement:** *"THE FORWARD BOUND IS APPLIED ONCE PER BATCH, AND EXACTLY THREE INPUTS MOVE WITH
   IT — the base commit, the then-previous batch and the executing act. Nothing else about this tool
   changes, and every previous aiming is recorded rather than overwritten (#12)."* That is what was
   done: the three inputs moved, the previous aiming was appended to the kept list rather than
   replacing anything, and no line of the tool's mechanism was touched. It is authored-input
   maintenance in the shape D-648 names, ordered by the dispatch — and it is declared here rather
   than left for a reader to notice, because the bar's letter does not carve it out.
2. **One base-commit hash was padded from an abbreviation before being resolved**, and the tool's
   own STOP caught it before anything moved. Reported rather than left in the diff; the resolved
   object was read with `git rev-parse` and nothing ran against the padded value. §9 records why it
   is discarded rather than rowed.
3. **The full guard set was run in `--check` mode rather than write mode until the end-state run.**
   `--check` runs every guard and does not rewrite the registry, which keeps the tree quiet between
   commits; the registries are written by the end-state run, which is what the seventh commit exists
   to carry.
4. **The armed guard denied two commands and was obeyed rather than worked around** — a `grep` over
   a tool's source inside the repository, and a `tail` over a scratch path the guard could not
   resolve because the path sat in an unexpanded shell variable (its declared deny-on-indeterminate
   policy). Each was re-taken through the file tools or with the path written out in full.

---

## 11. The registered expectations, graded

| | verdict | why |
|---|---|---|
| **E0** | **MET** | Exactly the four named paths; one content modification matching A1(i) and one bounded regeneration; two additions; no staging override; the membership check passing at the resulting tree, so the guard set stands at one failing ([[OI-372]]) from Task 0 onward; `origin/master` at the Task 0 commit. |
| **E1** | **MET, AND MET FOR THE WHOLE POPULATION** | Every read passage carries exactly one verdict in the inherited vocabulary; the unread remainder is EMPTY, so the clause about the tool's default has no subject and nothing was authored into it; the existing sixty-eight are byte-identical by published digest at every commit of this batch; every verdict the two previous batches authored is byte-identical; the per-document distribution is published by the generator; the failure signal is evaluated at the artifact and stated. **The pass completed, so the report says so in the standing form: every member named, the unread set EMPTY at the artifact.** `ARCHITECTURE.md`, `docs/scoring_model.md` and every member are byte-unchanged. |
| **E2** | **NOT YET ASSERTABLE HERE — BY DESIGN** | The end-state run is taken after the commit carrying this close exists. Its values land in ONE FURTHER commit. **No row, cell or sentence asserting the end state is written into the close commit.** |

---

## 12. The plan's tell, in one sentence

**Did this batch produce anything other than the authored verdicts, the regenerated screen artifacts
and the report?** Yes, and it is named: the regenerated evidence-pin membership artifact that Task 0
orders, the `STATUS.md` pointer entries and the ruled forward-bound move that Task 2 orders, the
regenerated session-start read size that A2 names, and the three re-aimed authored inputs of the
forward-bound tool — declared at §10 rather than left for a reader to find.

---

## 13. The standing self-check (D-434) over this batch's own work

1. **The guiding principles.** #12 — nothing deleted: every existing and widened verdict stays, the
   previous batch's `STATUS.md` entries move whole and are proven both ways, and the forward-bound
   tool's previous aiming is appended rather than replaced. #6 — one authority quoted per act; the
   calibration is pointed at the report that established it rather than restated; the prefix
   adjustment is imported from its one home. #17(f)/D-431 — no count transcribed: every figure is an
   artifact and a field, or a direction with its artifact named. #19 — the candidate generator's
   unestablished status is carried unchanged; the failure signal is evaluated at the artifact rather
   than at an impression. #13 — the one thing that came out different from what I intended (a padded
   hash) was surfaced by a STOP and is reported before anything rested on it. #24 — one comparative
   statement is made at §7, that the code-influenced share is larger than on the part-read
   population; it is stated as a direction over two named artifact states and NOT as a difference
   between two measured quantities with an asserted magnitude. **Conforms.**
2. **The conventions.** American English. No self-invented labels: every new constant and field name
   follows the tool's own `_`-prefixed shapes, and every term of the tools' own is explained in
   plain words at its first use. "Hunk" appears only where it names a field or quotes a tool;
   elsewhere the plain phrase *changed passage* is used. Music-theory words in their musical sense
   only — *score* is never used numerically, *measurement tool* and never the reserved word,
   *measure* only as the verb or as *measurement*, *key* only as tonality except where a quoted
   artifact field name carries it, *register* only as *the open-items register* or *the decisions
   register* in full, *note* only as a pitch event, *part* only as a musical part.
3. **The figures-and-premises rule.** Every quantity is named as an artifact and a field. Every
   premise is cited to the primary source it can be checked at — the four classes and their order
   from the tool's own docstring, the calibration from the previous batch's report §6, each ruling
   from its own record, each commit's account from its own git object by explicit hash, and every
   register entry from `DECISIONS.md`'s INDEX read this session.
4. **The file-tools rule.** Every read of working-tree content went through Read/Grep/Glob. The only
   shell reads of repository content were git OBJECT queries by explicit hash, and every tool output
   was written to an absolute scratchpad path outside the repository and read back from there with
   the file tools. **The armed guard denied two commands and was obeyed rather than worked around**
   — §10 names both.
5. **Uncertainty on any comparison.** No difference between two measured quantities is asserted. The
   one comparative statement is stated as a direction over named artifact states and explicitly not
   as a verdict.
6. **Re-read from disk before reporting.** The generator's `--check` was run after every edit and
   re-derives byte-identically; the diffs of the artifacts whose byte-identity is claimed were read,
   not assumed; and the published existing-verdicts digest was read at the artifact at every commit
   and matches the value the dispatch names at all three objects it cites.

---

*Provenance: CC, 2026-08-22, executing `cc_instruction_pass_continuation_second.md` under Ruling 1
of `cowork_rulings_2026_08_22_dispatch_order_sitting.md` and landing Ruling 1 of
`cowork_rulings_2026_08_22_boot_list_sitting.md`. The reserved-word conventions and the vocabulary
rule of 2026-08-17 bind every line of this report — TOWARDS the ultimate objective and TOWARDS the
guiding principles.*
