# CC dispatch — the preparation phase's FIFTH batch: the ruled soft-discard EXECUTED under the completed treatment ruling, and the governing-surface pruning MEASURED — the first batch under the ruled interim reading scope

> **Dispatch (Cowork, 2026-08-16).** Executes §3, §4 and §6 of
> `cowork_rulings_2026_08_16_preparation_return.md` together — the soft-discard, complete, under
> the reach ruling and the treatment ruling — and §5(A)'s ordered READ-ONLY measurement of the
> governing surfaces. **Written at a verified STOP; nothing else is running.** The archiving
> wave, the empirical findings ledger, the fact-gate, the curated boot list, the mining, the
> landing of the newly visible files, the eight KIND-UNDERIVABLE callers, the prose-citation
> question, the residue surface's sitting, the rulings-sort sitting, and EVERY pruning fate
> (Task 2 proposes only) are NOT in this batch.
> **Read first (every session), UNDER THE RULED INTERIM READING SCOPE (§5(B) of the ruling
> record, quoted in the ruling ledger below):** `C:\s\MS\CLAUDE.md` — SKIPPING the blocks that
> state of themselves that they are historical or superseded (the frozen historical case
> enumerations, the superseded baseline narratives, the preserved former wordings of amended
> rules); `C:\s\MS\STATUS.md` (header only); `C:\s\MS\BUILD_AND_TEST.md`. **Then read, in
> full:** `cowork_rulings_2026_08_16_preparation_return.md` §§3–7 (the whole authority — §6 is
> Task 1's treatment, §5 is Task 2's); `ratification_surfaces/cowork_standing_treatment_surface_2026_08_16.md`
> (§4 is Task 1's ordered shape); `ratification_surfaces/cowork_discard_reach_surface_2026_08_16.md`
> §6; your own `cc_report_preparation_fourth.md` §4 (what is measured working, and the three
> grounds Task 1 resolves); the retired-block precedent, `cowork_rulings_2026_08_15_batch_return.md` §2.
> The standing bars bind this batch whole: **no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no open-items row created, flipped or
> discarded, no archiving, no file moved or deleted, no mining, and NO EDIT to any of the five
> governing files by Task 2.** The register mutations permitted are Task 1's, exactly as bounded.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end;
> record both states.** The expected start state: **57 run, 56 passing, ONE failing**
> ([[OI-372]]'s tool, `gen_filing_convention_application.py --check`), 4 not run, 10 historical,
> zero STOPs, no stale report. **A different start state is a STOP-and-report.** Verify every
> commit at the object and record the SHAs in the close. The reserved-word conventions and the
> ruled vocabulary replacements bind all new prose. **The new-tool rule stands.** **The
> E-ordering rule stands** (no graded value committed before the run that produced it). **F3's
> reading rule stands** (`gen_cluster_dispositions.py --verify` is the drift authority).

## Ruling ledger (quoted at the record, not paraphrased)

- **§6 (kind 1):** the filter and recovery checks *"read inputs from the git objects at the
  commit each committed artifact records; the artifacts stand byte-unchanged as the rulings'
  permanent evidence."*
- **§6 (kind 2):** the four authored tables *"gain RETIRED-SUBJECTS sections on the
  retired-block pattern: every judgment whose subject the discard retires moves verbatim WITH
  its subject reference (#12 ...); the checks keep watching live subjects; a judgment in neither
  section halts; membership is DERIVED and published, and a judgment whose membership cannot be
  derived STOPS to the user."*
- **§6 (kind 3):** the two censuses *"regenerate under the bound WIDENED BY ONE NAMED CATEGORY —
  citation-carried standing ... beside the ruled class standing and home standing; every moved
  value enumerated and classed; movement outside all three a STOP."*
- **§6:** *"[[OI-372]]'s tool is untouched ... F22's ossification is ACCEPTED AS A NAMED COST"*;
  everything else of §4 *"stands unchanged and executes with this ruling"* — the six SUPERSEDED
  checks historical with artifacts frozen, the retired block's STOPs, the establishment pass
  consulting the retired block, F15's remap per-citation, 677 = 512 + 165 to the digit.
- **§3:** the discard records carry finding, date, reason AND the clause verbatim: *"a
  provenance verdict, not a judgment on soundness or usefulness; the statement stands at its
  home and is met by the derivation"*; the TEN B1 keeps stamped with their recovered acts.
- **§5(B), the interim scope this dispatch reads under:** *"the blocks that state of themselves
  that they are historical or superseded ... are skipped at session start; ... Every dispatch
  states this scope in its read-first block, citing this ruling."*
- **§5(A), Task 2's authority:** *"The next batch after the fourth is READ-ONLY: a per-file
  decomposition by span ... and the reader/anchor/quote inventory over all five files,
  delivering a ratification surface with per-span fates."* **§5(E)** is the ruled test the
  proposal applies: stays at site / archives with dated pointer / moves to its proper home, with
  the doubt default STAYS AT SITE.

## Premise ledger

*(No premise names a terminus commit — every FACT pins content, checked at content-addressed
objects. Per the F25 lesson, no premise asserts a count of changed passages; each states the
CONTENT of a difference, and the check verifies content, whatever passage count the version
history reports.)*

- **FACT** — the fourth batch is complete, pushed and verified at the objects and at the values
  by the writing side; the split (6 SUPERSEDED / 9 STANDING) is
  `tools/audit/discard_reach_split.json`; the reader enumeration (zero live consumers) is
  `tools/audit/phase1_gate_readers.json`; the discard plan (194 = 29 withheld + 165 to retire;
  677 → 512) is `tools/audit/soft_discard_application.json`. The artifacts are the authority
  (D-431).
- **FACT** — measured working at the fourth batch (`cc_report_preparation_fourth.md` §4.b): the
  discard applies (165 retired, 512 live, 10 stamps); the renderer regenerates and its `--check`
  passes; the establishment pass with the retired block consulted prints
  `cross-references resolving: ALL` at 512/512; all six SUPERSEDED members halt for one reason.
- **ASSUMPTION A1** — the working tree carries exactly TWO tracked modifications and no third:
  `cowork_handoff.md` (content: the twenty-first session-close block inserted above the
  twentieth, and the twentieth heading carrying its demotion marker — two parts of one act) and
  `cowork_rulings_2026_08_16_preparation_return.md` (content: §5, §6 and §7 appended before the
  provenance block, which is rewritten in place). `cc_instruction_preparation_fifth.md` and
  `ratification_surfaces/cowork_standing_treatment_surface_2026_08_16.md` are untracked, among
  the standing untracked records this premise does not enumerate. **Check ordered (Task 0, first
  act, blob against blob by explicit hash):** any tracked difference whose CONTENT is outside
  that shape is a STOP-and-report.
- **ASSUMPTION A2** — with §6's three treatments applied beside the §4 acts, every red clears:
  the end state shows every run check passing except [[OI-372]]'s tool, the six SUPERSEDED
  members historical, zero STOPs. **Check ordered (Task 1, last step):** any other red is a
  STOP-and-report.
- **ASSUMPTION A3** — the guard mechanism's own records can express the historical
  reclassification and the two pinned-reading conversions. **Check ordered:** performed through
  the mechanism's recorded form; a form that cannot express it is a STOP, not an improvisation.

## Task 0 — land the rulings, the surface, the handover block and this dispatch

1. **A1's check first, entirely at content-addressed objects.**
2. Commit exactly these FOUR paths and no fifth: `cowork_handoff.md` (modified),
   `cowork_rulings_2026_08_16_preparation_return.md` (modified — §5–§7 land),
   `ratification_surfaces/cowork_standing_treatment_surface_2026_08_16.md` (new),
   `cc_instruction_preparation_fifth.md` (new). All staged plainly; push; verify at the object.

**Registered expectation E0:** exactly 4 paths at `git diff-tree`; two modifications whose
content matches A1's stated shapes, two additions; no staging override.

## Task 1 — the ruled soft-discard EXECUTED, complete, in ONE commit

All of the following in ONE commit, citing §3, §4 and §6:

1. **The discard through the register's own mechanism** (rule (d)): the derived population
   re-derived and re-verified (194 = withheld + executed to the digit; a member in both blocks
   or neither halts); the 165 moved WHOLE into the RETIRED-ENTRIES block WITH its own STOPs;
   each retired record carrying the entry unchanged, its finding, date, the retiring authority
   (`cowork_rulings_2026_08_16_preparation_return.md` §3) and the ruled clause verbatim; the TEN
   B1 entries' provenance stamped from the recovery artifact; `DECISIONS.md` and every group
   file regenerated BY THE GENERATOR; the establishment pass consulting the retired block; the
   renderer's retired-block STOPs in place; **F15's anchored-quote remap per-citation from the
   `--verify` drift report.**
2. **Kind 1 (§6):** `gen_decisions_filter.py` and `gen_deciding_act_recovery.py` converted to
   PINNED reading — inputs from the git objects at the commit each committed artifact records;
   both artifacts BYTE-UNCHANGED, proven by hash; both `--check` runs passing against the pinned
   state.
3. **Kind 2 (§6):** the four authored tables gain RETIRED-SUBJECTS sections with their own
   STOPs (an entry in both sections, or a judgment in neither, halts); **the move list per table
   DERIVED** (a judgment moves iff its subject is a discard-population entry, or a document
   whose class or home standing those entries alone carried) **and published**; every moved
   judgment verbatim with its subject reference; **a judgment whose membership cannot be derived
   is a STOP to the user.** The thirty legacy-marking verdicts (F24) are inside this treatment.
4. **Kind 3 (§6):** `gen_artifact_inventory_surface.py` regenerated and
   `gen_retirement_caller_check.py` run with the commit its own interface requires; **every
   moved value enumerated and classed against the THREE categories** (class standing, home
   standing, citation-carried standing); **any movement outside all three is a
   STOP-and-report.**
5. **The six SUPERSEDED checks reclassified HISTORICAL** through the guard mechanism's own
   records (A3), their committed artifacts left byte-untouched, proven by hash; no SUPERSEDED
   member's generator run again.
6. **The full guard set at the edited tree (A2's check)**, then ONE commit, push, verify at the
   object.

**Registered expectation E1:** the register's data-file diff touches exactly the retired-block
addition, the moved entries and the ten stamps; live + retired = 677 BY ARITHMETIC, stated in
the close to the digit; nothing destroyed — every retired entry and every moved judgment
byte-preserved in its retired section; `gen_decisions_register.py --check` and
`gen_cluster_dispositions.py --verify` re-derive clean; the pinned artifacts and the frozen
artifacts byte-identical to their pre-batch blobs, proven by hash; every kind-2 move and every
kind-3 movement enumerated in the close.

## Task 2 — the governing-surface pruning MEASURED (read-only; §5(A)'s ordered batch)

Over the five files — `CLAUDE.md`, `OPEN_ITEMS.md`, `DECISIONS.md` (post-Task-1), `STATUS.md`,
`BUILD_AND_TEST.md` — and touching NONE of them:

1. **The per-span decomposition**, published as a generated artifact: every span classed with
   evidence — operative rule text / preserved former wording / defense-and-declined-alternatives
   record / self-declared historical-or-superseded block / resolved row / pointer entry of a
   completed batch (`STATUS.md`) — with byte counts per class per file, no count typed by hand.
2. **The reader, anchor and quote inventory**: every committed artifact that quotes, anchors
   into, or parses each of the five files (the F2/F4 class, the index parser and lint, the
   anchor machinery, the register's homes and anchored verbatims), published per file — the
   split's full measured reach, BEFORE any act (the F13 lesson, prospectively).
3. **The ratification surface**, GENERATED from the two artifacts:
   `ratification_surfaces/cowork_governing_surface_split_2026_08_16.md` — per-span proposed
   fates under §5(E)'s ruled test (STAYS AT SITE / ARCHIVES with dated pointer / MOVES TO ITS
   PROPER HOME), the doubt default STAYS AT SITE written in and every doubt-defaulted span
   marked as such; self-contained per the presentation rule, plain language, its banner stating
   NOTHING HERE IS RULED.

**Registered expectation E2:** every span of all five files classed with evidence; the surface
generated, not hand-typed; no edit to any of the five files; both new tools registered under
the new-tool rule with `--check` forms.

## Task 3 — the close

One `STATUS.md` pointer entry per task and nothing else in that file. The FULL close appended to
`cowork_away_returns.md` (a PREPARATION FIFTH BATCH section) — both guard states, every SHA,
every expectation graded, the discard arithmetic to the digit, every kind-2 move and kind-3
movement listed, every problem declared, the carried findings restated for the retrospective
(F1–F26, the E3 ordering defect, the A1 premise error — surfaced, none rowed; rows stay
barred). The report `cc_report_preparation_fifth.md`, an ordinary tracked file. Commit, push,
verify at the object. **The E-ordering rule:** the end-state guard run demonstrated across the
commit boundary is taken AFTER the Task 3 commit exists, its output and the final SHAs landing
in ONE FURTHER commit.

**Registered expectation E3:** at the tree carrying the close, a fresh full guard run
re-derives — every run check passing except [[OI-372]]'s tool, the six SUPERSEDED members
historical, zero STOPs — run and read, not inferred, its values committed only after the run.

## What this batch does NOT do

- **No pruning fate executed** — Task 2 proposes; the user rules its surface; a LATER dispatch
  executes. **No edit to any governing file by Task 2.**
- **No sole-carrier member discarded; the 62 not ruled; the residue and rulings-sort surfaces
  stand awaiting their sittings.**
- **No archiving, no file moved or deleted, no mining, no landing; the eight KIND-UNDERIVABLE
  callers and the prose-citation question stay open; every retirement flag stays a candidacy —
  a census crossing confers candidacy only.**
- **No empirical findings ledger, no fact-gate admission, no curated boot list. No completion
  claim of any kind about the superseded phase-1 program.**
- **No derivation of any specification, no design, no repair, no pilot act, no `src/` edit, no
  golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the
  analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly
  as found; [[OI-179]] stays OPEN and GATES; `reaim_home_anchors.py`'s F3 defect stays
  surfaced, unfixed and unrowed (both bars stand).
