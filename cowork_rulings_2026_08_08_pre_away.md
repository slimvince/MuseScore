# Three rulings of 2026-08-08, taken at the pre-away STOP

> **STATUS: RATIFIED RULING RECORD, written 2026-08-08 (Cowork).** Ruled by the user ("All three
> ratified") after the full surface — the situation from OI-305 and OI-350 read whole, the
> alternatives and why the record itself refutes them, and the recommendations — was delivered
> as user-visible text. Interim carrier until the applying dispatch records them (D-230).
>
> **None of these rulings authorizes a fix to the analysis, a design, or an inference change.**

## Ruling 1 — OI-305 is resolved by the EPOCH TREATMENT, and the classifier's apply runs again

`phase1q_reclassification.json` is declared HISTORICAL: the record of what the phase-1q pass
found, frozen at its established snapshot (OI-305/OI-319), never regenerated over — the OI-301
hazard discharged by freezing rather than by holding the writer forever. The apply mode of
`gen_home_classification.py` then RUNS at HEAD, writing current `home_section` fields with every
entry's former values preserved in the classifier's own before-fields (#12); the four
`docs/scoring_model.md` section fields re-derive in the same apply; `--check` must come back
green. The four artifacts OI-350 names stale are then regenerated and must account for the
route-(i) and re-homing movement by derivation. OI-305 and OI-350 flip on success. **The
recurrence ends:** a delegation the user writes lands as CLOSED in the derived view, not as
"newly broken". The mechanism change is licensed by this ruling (D-436). *Excluded alternatives,
recorded:* authoring the settled-list entry (its own truthful STOP fires — "the widening did not
take"); leaving the hold (the finish line permanently unable to describe the tree, defeating C4
and the completion statement).

## Ruling 2 — D-601's homing is authorized by ONE edit of `cowork_confidence_contract.md`

The owner is determinate (the confidence contract's frame table, which D-269 names as the one
home of the override arithmetic); the file lay outside every standing authorization. This ruling
is the authorization, scoped to that one homing act — the precedent being the arc-plan one-edit
authorizations for D-539 and D-568.

## Ruling 3 — OI-351 joins the ruled guard-family design

The live guard's admission of `ls -la` at a repository directory — one day after the widening
whose record says that shape is denied — is worked INSIDE the one family design
(`cowork_ruling_guard_family_2026_08_08.md`), never as its own patch: the observed command and
its path-form variants (drive-letter forms included) enter the establishment corpus FIRST, the
cause is established at the LIVE HOOK and not only at `decide()` (the gap between the two being
one of the candidate causes), and the fix lands with the family's, under the same revert
condition. OI-351 flips on success or is held with its established cause named.
