## COWORK SESSION CLOSE (NINETY-SIXTH ENTRY, 2026-09-03 — THE CONTINUING LINE) — ★★ **RULING 63 IS TAKEN AND ON DISK (§3br: THE AMENDMENTS OF RULINGS 34–62 GO INTO A NEW FILE BESIDE THE DERIVATION, WHOSE BODY IS NEVER REWRITTEN), AND THE FIFTH COMPARISON DISPATCH — THE APPLICATION ACT — IS WRITTEN, SOURCE-CHECKED AT THE OBJECTS BEFORE RELEASE, AND ON DISK, UNTRACKED, READY FOR THE USER TO HAND TO CLAUDE CODE.** ★ **THE TIP IS STILL `b00fb69455372396ff10696524f3d4683fc38614`; NOTHING IS IN FLIGHT; THE SITTING RECORD IS MODIFIED ON DISK (ADDITIONS ONLY) AND THE FIFTH'S TASK 0 LANDS IT WITH THE NINETY-FIFTH ENTRY.** THE CURRENT ENTRY POINT; the ninety-fifth entry is superseded as entry point and stands otherwise.

You start clueless. Read this block first, then perform the ordinary session-start read: `CLAUDE.md`
whole, `DECISIONS.md` whole, `STATUS.md`, and the derived gating answer
(`tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`). **A
single-file opening instruction is not an exemption from that read** (ratified 2026-08-29, P-1). **The
eighty-second through ninety-fifth entries remain binding except where this entry names a change**,
and every standing bar of the earlier blocks remains binding.

**On form.** Short. In conversation with the user, plain ordinary English. A decision surface is
fact-based and checked at the objects, in plain uncompressed language, every position weighed towards
the ultimate objective and towards the guiding principles, one decision per turn, the question in a
later turn (D-249, Ruling 1, Ruling 21), the recommendation and its two-axis reasoning on the surface
itself. An outgoing form that loses notated information is EXCLUDED AT THE THRESHOLD (Ruling 47);
facts are never altered, hidden or added to (Ruling 52). **A "go on" or "continue" is never read as a
ratification.** **Check at the object before you write a fact, and SOURCE-CHECK A DISPATCH AT THE
OBJECTS BEFORE RELEASING IT** — this session did, and the check found two things (below). **Read a
staged repository file with the file tools, never with an interpreter in the sandbox** (this session's
counted error).

### What this session was

The session that booted on `cowork_handoff_entry_ninety_five.md`, performed the full session-start
read (`CLAUDE.md` all 1,864 lines, `DECISIONS.md` all 862, `STATUS.md`, the gating answer's field read
at the file), re-verified the tip at both ref files — `b00fb69455…`, `origin/master` equal, nothing
landed since the ninety-fifth entry — and verified the fourth batch's chain again at the objects
(`git cat-file -p` and `git show --stat` per explicit hash from `93ab4e34…` to the tip). It then
performed the pre-dispatch work the ninety-fifth entry ordered: declared the guard start state from
`e6adf74d2e9dedf65b708571b2284eaf1dfc62c1:tools/audit/guard_state.json` (run 76, passing 66, failing
10, the same ten, not_run 4) and stated it one commit behind the tip; checked every tracked input
against its tip blob by git blob sha1 of the bridge-staged copies (all identical; `DECISIONS.md` and
`nongating_apparatus_rows.json` CRLF on disk under `text=auto`, identical with CR removed); **read
Rulings 34–62 (§3ao–§3bq) in full at the landed object** — the first session to read them for content;
read the reading file's §0, §7 (7a, the head of 7b, 7c whole), §12's head, §13, §15, §16 and the shape
of its §6 rows in both forms; read the fourth dispatch and report whole; opened `gen_guard_state.py`
and `gen_status_batch_bound.py` at the objects; read D-674 at `decisions/group_K.md`, the suspension
file and the register repair's STOP report, the phase definition §3.3–§3.5, and the dispatch protocol's
standing clauses. **One question the record did not settle — where the amended text is written — was
put to the user as a surface with the facts, three positions weighed on both axes and a recommendation
(at his instruction); he ruled "position two", written to the sitting record as Ruling 63 (§3br) with
its provenance paragraph, committed under the bridge's change guard and read back from a fresh copy.**
The fifth dispatch was then written, source-checked at the objects before release, revised, written to
the root and re-read from disk (sha256 `808bcf2feb179127050b4a3937b34811d9d32e9ad53f72242df813d576d87eff`,
65,243 bytes, hash-identical to the written copy). This session derived nothing, dispositioned nothing
itself, applied nothing, and landed nothing in git. It did not compact.

### ★ THE STATE YOU INHERIT

- **The tip is `b00fb69455372396ff10696524f3d4683fc38614`**, `origin/master` equal, unchanged by this
  session. **Re-verify at the ref files before relying on it.** If the fifth batch has run, read its
  FULL close in `cowork_away_returns.md` (*THE L0/L1 APPLICATION ACT (FIFTH WRITING) …*) first and
  verify every commit at the objects by explicit hash.
- **Tracked and modified on disk, not landed:** `cowork_rulings_2026_08_31_decision_surface_sitting.md`
  — §3br (Ruling 63) inserted between the line closing §3bq and `## 4. What this ruling does NOT do`,
  and one provenance paragraph opening `*★ AND WITH **Ruling 63** (§3br)` appended at the tail;
  additions only. **Untracked at the root:** `cowork_handoff_entry_ninety_five.md`,
  `cc_instruction_comparison_l0_l1_fifth_2026_09_03.md` (both landed by the fifth's Task 0), and THIS
  entry (landed by the dispatch after the fifth, the ninety-second entry's rule). **Nothing else
  tracked is modified** (every input hashed to its blob this session).
- **Ruling 63, one line (full text at §3br):** the application act writes the amendments of Rulings
  34–62 into a NEW file, `cowork_derived_specification_l0_l1_2026_09_03.md` (the name and its banner
  authored in the dispatch, overrulable before Task 0), carrying S-1–S-54 and OQ-1–OQ-19 whole in the
  six-field form with every amendment in place citing its ruling, NOT RATIFIED; the derivation's body
  is never rewritten and gains D-674's top banner only. Position one (edit the derivation in place)
  was contrary to D-674 as ruled; position three (re-mark only, amendments at ratification) left the
  phase's output unwritten. Both axes agreed; Ruling 1's precedence clause not exercised.
- **The fifth dispatch, what it orders (read it whole; nothing restated here):** Task 0 lands the
  sitting record, the ninety-fifth entry and the dispatch, pinned under P-2, A3 predicting zero
  movement of the pin membership; Task 1 the successor from the git object, §0–§4 whole, §5–§7 replaced
  by a pointer, the amendments in a fixed form per ruling with a per-ruling locator table the rulings'
  own *What was decided* paragraphs govern, and an object-level diff check attributing every hunk;
  Task 2 the derivation's D-674 banner, one hunk at the head; Task 3 the reading file's re-marks (one
  added `*RULED DISPOSITION` line per named row, proposed lines never edited), Row 4.37's correction
  under Ruling 38, the family rows of documents 8–29 found by a mechanical term search with an unplaced
  class, §10 gaining the rulings' relocations and the three L2 flags, a derived §13a ruled distribution,
  the four state sentences amended with former wordings kept; Task 4 the close, the forward-bound tool
  re-aimed at all five constants plus `PREVIOUS_AIMINGS`, the prediction that exactly the fourth's two
  `STATUS.md` entries move and the second writing's two stay, the read-size artifact regenerated LAST.
  **The three questions the fourth close left open are settled on its face**: the eleven unnamed
  derived statements are not reached and are carried as derived, named in the successor's banner; the
  nameless `STATUS.md` entries stay; the register route is the suspension file, whose list is derived
  and never typed and whose deriving repair was STOPped and never ran — nothing is written there.
- **The plan stands at:** phase OPEN → derivation LANDED → comparison COMPLETE → dispositions:
  Rulings 34–63 taken → **the application act — the fifth dispatch, ON DISK, READY** → ratification (a
  decision surface over the successor, where the L0/L1 split returns) → L2, sequenced by Ruling 10.
- **Guard state:** unchanged by this session (nothing landed, no tool run); declared in the dispatch
  from `e6adf74d2e…`'s artifact, one commit behind the tip; the fifth's start-state run measures the
  tip.
- **Nothing landed in git by this session.** No code opened; no measurement, golden, build or test;
  nothing under `tools/corpus/` or `tools/robust_stop/`; **no open-items row created, flipped or
  discarded; no decisions-register entry and no `D-NNN` allocated.**

### Facts established this session that the next dispatch needs (each checked at its object)

- **`gen_evidence_pin_membership.py`** reads of a ruling record only its leading blockquote and its
  paragraphs opening `*Provenance:`, and names a tool a member only where a record says it is PINNED
  (route B). A `*★ AND WITH` tail paragraph is neither — so an addition to the sitting record in that
  shape moves the membership by zero.
- **The readers among `tools/` of the reading file, the derivation, the sitting record and
  `STATUS.md`** were enumerated at the tip by `git grep -l` (declared below): the derivation and the
  reading file are read by no `--check` tool; `STATUS.md` is read by the population tool (recorded
  tier), the read-size generator, the forward-bound tool and the governing-surface tools, among
  others. A new root-level `cowork_*.md` enters no population unless the artifact-inventory pair,
  already red, counts it.
- **`cowork_register_rule_c_suspension_2026_08_28.md`** still carries the placeholder comment where its
  owed list goes; `cc_report_register_baseline_repair.md` is a STOP report — the repair never ran.
- **D-674's two branches** (`decisions/group_K.md`, home `cowork_design_doc_template.md:120-141`): a
  dated report re-bannered, body never rewritten; a live governing surface corrected in place; a hard
  case stops to the user.
- **The reading file's §6 rows come in two forms:** documents 1–7 (`*Outgoing statement.*`, `*Derived
  statements that speak to it.*`, `*Current-text axis.*`, `*PROPOSED DISPOSITION.*`) and documents 8–29
  (`*Statement.*`, `*Derived.*`, `*Current-text axis.*`, `*PROPOSED DISPOSITION.*`); the proposed line
  is uniform across both.
- **Row 21.5** is already RELOCATED to L2 with Row 11.3 at the reading file; Ruling 48's flag is a line
  beside it, not a re-placement.

### The user-owed list

**The fifth dispatch is ready at its path; the user starts it.** Before it is started the user may
overrule three authored things named on its face: the successor's file name, the successor's banner,
and the derivation's D-674 banner wording.

**Next after the fifth's close is verified: the ratification act** — a Cowork-written decision surface
over the successor, one decision per turn, where the L0/L1 split returns (Ruling 32's *"deliberately
not put"*). It is the writing side's to write.

**Two apparatus findings of the fourth batch, unchanged, still the user's:** the shell-read guard
admitted one hash-less working-tree `git diff` (its report §6.7); the end-state guard artifact stands
one commit behind the tip. **One of this session's, for the user:** the register repair's STOP has
left the suspension list empty since 2026-08-28, and every ruling since then (34–63 among them) says
*"the suspension file is the route"* — the route exists and its list does not; whether that earns a
row under R3 as amended by #10's worth test is the user's.

**Standing, from the ninety-second through ninety-fifth entries and unchanged:** the §3 leak repair in
the brief and §8's instance; the rename of `00_READ_THIS_FIRST.md`; the plainness repair;
`.gitattributes` for committed `.mscx` exemplars; the merged D-674 question; the three routed
`FRAMEWORK.md` findings; the corrections-batch scope; the commit-trailer form; the two candidate
bounds; the two research-paper binaries; the shell surface's answer; marker enrolment of P-2..P-5; the
artifact-inventory signature-table amendment (the standing STOP). **The eleven quarantined audit
questions stay reserved to the AUDIT**, with the reading file's §11 and the rows the rulings
quarantined.

**Owed as work, none of it blocking:** unchanged from the ninety-fifth entry.

### Errors

**ONE COUNTED, self-declared; three declared departures; one departure of form declared and not
counted.**

1. **★ The gating answer's `gating_ids` field was first read with a `python -c` one-liner over the
   staged copy in the container** — the D-253 Cowork-sandbox instance `CLAUDE.md` names as the same
   violation as `cat` — and only then re-read through Grep and Read at the file, where it reproduced.
   Counted, not declared: the rule names this form. **Use Grep and Read on a staged JSON; parse only
   tool output.**

Declared departures, the ninety-fourth and ninety-fifth entries' shape: **(i)** `git grep -l` over
the tip's tree by explicit hash, to enumerate the tools that read this batch's inputs — an object
query by explicit hash, not one of D-253's four named forms; **(ii)** the guard artifact and six tool
sources dumped by `git show <hash>:path` and read with `sed` and `grep` on the user's machine, the
ninety-fifth entry's declared form; **(iii)** the bridge's directory listing parsed with an interpreter
in the container — tool output, not a repository read. No working-tree file was read by any shell on
the user's machine.

**Declared, not counted — the form of the Ruling 63 surface.** The standing clause D-658 says a surface
returning an unsettled question gathers facts and makes NO recommendation. The first return did
exactly that (facts, UNSETTLED, no recommendation). The user then instructed *"do so fact based/checked
and visavi utlimate objective and guiding principles"*, and the surface was re-put in the decision-
surface form the entries prescribe — three positions weighed on both axes, a recommendation — and
ruled on. Recorded so the two forms are not confused: D-658's form is the default for an unsettled
question; the weighed form was the user's instruction for this one.

**What went right and is worth copying:** every ruling of 34–62 was read whole at the landed object
before a line of the dispatch was written; the per-ruling locator tables were transcribed from the
rulings' *What was decided* paragraphs and the dispatch says the paragraph governs; the tools the
batch's inputs touch were enumerated at the tip rather than assumed; the register route was settled at
its object (the placeholder comment, the STOP report) instead of from the rulings' phrase; the
source-check before release found two defects — the derivation banner's position was stated loosely,
and Task 1's diff check would have STOPped on the dispatch's own title, banner and pointer hunks — and
both were fixed before release; and the sitting record was extended by a targeted edit under the change
guard and read back from a fresh copy.

### The cadence for your session

0. **Boot as above, in full.** This entry file is UNTRACKED and you never land it yourself (the
   2026-08-26 role ruling).
1. **Re-verify the tip at the ref files with the file tools**; expect `b00fb69455…` if the fifth has
   not run. **If a batch is running, do not write anything into the tree and do not write a dispatch
   (D-251, P-5): tell the user, and wait.**
2. **If the fifth has NOT run:** it is ready at its path; the user starts it. Meanwhile nothing is
   owed on the writing side except answering the user's questions about the dispatch's three authored
   items.
3. **If it HAS run:** read the FULL close in `cowork_away_returns.md` (never a summary) and the report,
   verify every commit at the objects by explicit hash (Task 0 with its three or four paths; the
   successor commit; the banner commit — ONE hunk at the head; the reading-file commit; the close; the
   end state), check the successor's per-hunk attribution table against the rulings, check the
   unplaced rows and family hits by identity, and only then write the ratification act's first surface
   over the successor — one decision per turn, the L0/L1 split first as Ruling 32 left it — with the
   next dispatch landing this entry.
4. **Verify at the object, not at a report** — including this entry.
5. **Close at a member boundary with nothing half-done**, and write the next entry before you close.
