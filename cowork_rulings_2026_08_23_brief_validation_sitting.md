# Rulings — the brief-validation sitting, 2026-08-23

> **STATUS: RULING RECORD.** Cowork, 2026-08-23 (the forty-fifth Cowork session, its third
> sitting). An interim carrier under the standing clause that a sitting record is written in the
> turn its ruling is given and lands in git at the next dispatch's Task 0.
>
> **Taken at branch tip `d2a517c8db`** (parent `59b3c0ff33`; both refs at the tip), read by this
> session with `git for-each-ref` and `git show -s` at the explicit hash on the user's machine,
> **after the second-passage batch (`cc_instruction_second_passage_withheld.md`) returned complete
> and was verified at the objects by this session; nothing was running.** The objects ruled on are
> `cowork_blind_session_brief_harmony_boundary.md` at the tip (working-tree copy verified
> hash-identical to the tip blob, then read whole from a bridge-staged snapshot), the landed pack's
> `00_READ_THIS_FIRST.md` at the tip object, and the staged-file enumeration of the brief's §3
> (all eight files verified present on disk under their exact names; chorale 137's BCMH absence
> confirmed).

---

## 0. What was put, and in what form

The brief's validation against the landed pack was delivered whole as a turn's final response —
what validated clean, the one conflict with the read-me quoted at both objects, three alternatives
each with its case for and against, and this session's choice — with the question in the following
turn. The user ruled the shape with the alternative's letter; the two open §8 points were then put
as two questions through the question widget and ruled.

## 1. Ruling 1 — the read-me's boundary is REWORDED and the brief REFRESHED, in one dispatch (Alternative C; the user's word: "C")

**The fact ruled on, at the objects.** The pack's `00_READ_THIS_FIRST.md` states: *"Together with
this file they are **the whole of your read**"*; *"every `cc_*` and `cowork_*` file outside this
directory are **NOT opened**"*; *"Nothing outside this directory is opened at all."* The brief's
§3 grants three things those sentences forbid, each under a ruling taken AFTER the read-me's
wording was fixed by the pilot-preparation dispatch: the brief itself (a `cowork_*` file the
session reads FIRST), the eight annotation and score files staged by name (Ruling 3 of the
deriving-side sitting), and fetched published research ((P2), confirmed by Ruling 2 below). A
session obeying the read-me refuses its ruled inputs; one obeying the brief overrides its own
pack's boundary on its own judgment.

**Ruled.** One dispatch does both: **(a)** the read-me's rendered wording is scoped so it stays
true — the whole-of-your-read sentence scoped to the repository with the brief excepted, and the
boundary paragraph closing with the statement that what the session may read beyond the pack — its
brief, files the brief stages to it by name, published research — is stated by the brief and by
nothing in this directory; generic wording, true for every future subject; **(b)** the brief is
refreshed to the ruled state — its banner from draft to ruled, §7's output name fixed (Ruling 3),
§8 brought to the rulings' current state — with the refresh authored by the writing side and
landed by the dispatch, not written by Claude Code.

**The alternatives declined.** A — the read-me fix alone: declined only as incomplete; the brief's
banner and §8 were stale regardless. B — a brief-side override note with the read-me left false:
declined because the false absolute stays in the pack and every deriving session meets it
knowingly, documenting a session trained to override its own boundary rule.

## 2. Ruling 2 — (P2) is CONFIRMED: the blind session may fetch and read published research, as the brief's §3 is written (the user's choice: "Yes, as §3 is written")

Papers, published algorithms, public datasets' documentation; every load-bearing claim labelled
FACT / THEORY / CONJECTURE; a source that could not be fetched yields no statement. The ground the
brief states stands: the pilot's ruled inputs are independent sources and fetched literature, and
a session forbidden research would test a different method from the one under test.

**The alternative declined.** Theory-and-pack-only: declined for the reason above.

## 3. Ruling 3 — (P4) the output file's name is FIXED: `cowork_blind_derivation_harmony_boundary_2026_08_23.md` (the user's choice)

The draft name with the date fixed to the day of this ruling; if the blind session runs later, the
name identifies the derivation, not the run day. The commit route is unchanged (Ruling 1 of the
deriving-side sitting: the next ordinary dispatch commits it; the session itself commits nothing).

**The alternative declined.** Leaving the date to the session: declined; a name not fully fixed
before dispatch re-opens a ruled point at boot time.

## 4. What this sitting orders, and what it does NOT do

**Ordered:** one dispatch — Task 0 lands this record, the refreshed brief, the modified handoff
and the dispatch, and regenerates the evidence-pin membership; Task 1 makes the read-me rendering
edit (the ONE generator change), re-renders, and verifies the pack otherwise byte-unchanged;
Task 2 closes. **After it lands, the blind session is the user's to open: a fresh Cowork session,
its desktop connecting the repository folder and the chorale folder, reading the brief first and
the pack, staging the eight named files, deriving, and writing the one output file under the
ruled name. NOTHING else stands before it.**

**Not done:** no session is booted; no derivation; no oracle opened; no verdict, identity,
document, passage, anchor or scope of the withheld family moves; no edit to any boot-list member
at its home; no register entry, sort artifact or register source touched; no finding number
allocated (the series stands at F88); no open-items row created, flipped or discarded — [[OI-179]]
stays OPEN and GATES, [[OI-372]] and [[OI-374]] stand as found.

---

*Provenance: Cowork, 2026-08-23, the forty-fifth session's third sitting, at tip `d2a517c8db`.
The brief read whole from a hash-verified staged snapshot; the read-me and the manifest read at
`git show d2a517c8db:<path>` on the user's machine; the eight staged-file names verified at the
directory listings. The user's words: "C"; "Yes, as §3 is written"; the fixed name of Ruling 3.*
