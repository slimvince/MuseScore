# The L0+L1 boot pack — freeze the spent packs, render (dispatch, second writing, 2026-08-31)

> **STATUS: WRITTEN, NOT STARTED.** Written by the Cowork writing side on 2026-08-31, executing
> **Ruling 17** of `cowork_rulings_2026_08_31_decision_surface_sitting.md` §3q, which settles the four
> questions the first writing's STOP left open. **The user opens it with the CC run of his choice.**
>
> **★ THE WRITING SIDE'S RESTRAINT.** From hand-over, the writing side does not touch this file or any
> file named in §0's read-first block while the batch runs.
>
> **★ AND THE STANDING FIX THIS DISPATCH EXISTS TO APPLY.** The first writing ordered `--check` GREEN
> and both existing packs byte-identical, which could not both hold from the actual start state; the
> information to know that was on the record and this side did not use it. **This dispatch therefore
> ESTABLISHES THE START STATE OF EVERY CHECK IT ASSERTS ON, BEFORE IT ASSERTS ON IT** (Task 1), and its
> Done conditions are written against what Task 1 finds.
>
> **This batch renders a boot pack. It stages no score, writes no brief, boots no session, derives
> nothing.**

## 0. Boot — read before any other act

You start clueless; a single-file opening instruction is not an exemption (ratified 2026-08-29, P-1).

1. The ordinary session-start read: `CLAUDE.md` whole, `DECISIONS.md` whole, `STATUS.md`, the derived
   gating answer.
2. `BUILD_AND_TEST.md` — **conditionally mandatory, condition MET** (this batch runs a Python tool).
3. `cowork_rulings_2026_08_31_decision_surface_sitting.md` **§3k, §3l, §3o, §3p and §3q** — Rulings 11,
   12, 15, 16 and 17 — **whole.**
4. `cc_l0l1_boot_pack_report.md` — the first writing's report, **whole**. Its claims about the generator
   are **RELAYED to you** and Task 1 orders the load-bearing ones re-established.
5. This document, whole.

The bash rules of `CLAUDE.md`'s VS Code section bind every command (#6).

## 1. Task 0 and Task 1 — pin, then establish the start state

**Pin** this file and every file §0 names with `git hash-object -w`; later reads from
`git cat-file blob`. A read disagreeing with its pin is a **STOP**.

**Then establish, and report, BEFORE any assertion rests on it:**

- **`--check`'s current output, line by line** — run it and record every line it prints. The first
  writing reports six: the manifest, members (2) and (4) of both existing packs, and the missing
  `l0-l1` directory. **Confirm or correct that list at the object.**
- **The current blob of every file under `tools/audit/derivation_boot_pack/` and of
  `tools/audit/derivation_boot_pack.json`**, recorded per file. These become the freeze's own baseline.
- **That the previous batch's extension is present and as reported**: the per-subject `EXTRAS` table
  rendering after the ruled six; the `removals` and `cuts` filters with both-ways verification; the
  derived read-me count; the STOP on the ruled six not standing intact and in order; and `l0-l1` in all
  four authored tables with an empty withheld family. **Absent or materially different is a STOP** —
  this dispatch builds on that work and may not rebuild it silently.

**Every Done condition of §7 is written against this list. If Task 1 finds a red line NOT on it, STOP
and report rather than proceeding.**

## 2. Task 2 — freeze the two spent packs (Ruling 17(a))

**`harmony-boundary` and `scoring-model` are SPENT: their sessions have run, and their packs are the
record of what those sessions were given.** Under **D-646** — *a generated record that must outlive its
own writer is frozen at an established snapshot, and the freeze is enforced by a hash STOP* — they are
**pinned at the blobs Task 1 recorded** and **NEVER RE-RENDERED**.

**Build it as a mechanism, not a convention:**

- a **`FROZEN`** authored table, subject → per-file digest, filled from Task 1's recorded blobs;
- **`write_all` writes nothing into a frozen subject's directory**;
- **`check_all` verifies a frozen subject against its recorded digests** instead of against a re-render,
  and **STOPs on any mismatch** — so the freeze is enforced rather than trusted;
- the manifest continues to carry both subjects' entries unchanged.

**Why this and not a refresh, recorded so a later reader meets the ground:** the drift is addition-only
and its first added lines are the **P-1** and **P-2** clauses **ratified 2026-08-29 — after both packs
were rendered and after both their sessions ran.** Re-rendering would make those packs claim their
sessions read rules that did not yet exist (**#12**).

## 3. Task 3 — the one further removal (Ruling 17(b))

Add **one** removal to the `l0-l1` charter extra, by anchor text, on the same both-ways verification as
the two already there:

- **anchor:** `This is adopted from this project's material`

**Exactly once or STOP.** The removal runs from the opening of the parenthetical it sits in to its close.

**Nothing else is added to the filter.** The sweep's other candidates — DP-N's two disagreeing analyses,
DP-Q's three exemplar analyses, and the two references to the ledger — **stay in place by ruling**: the
first two describe corpus material rather than what this project's system does and are load-bearing
evidence for their design points; the ledger references are moot, the pack carrying that document whole.
**Do not widen the filter beyond this one anchor, and report any new candidate rather than removing it.**

## 4. Task 4 — the four residuals (Ruling 17(c)), under the bar the ruling relaxes

**Ruling 17(c) relaxes the first writing's requirement that the manifest change only by the added
subject — for this purpose and no other.** Repair:

1. **`the_pack_files_in_order`** — derived per subject rather than global, so it names each subject's
   actual files including its extras.
2. **`the_rulings_it_executes`, `the_STOPS` and `what_is_AUTHORED`** — brought to the extended tool's
   actual state.
3. **The read-me's stop-and-record clause** — *"including one of these six"* becomes derived, so it
   never states a count that contradicts the pack it introduces. **The clause's rule is unchanged**; only
   the count is derived. **The boundary clause is untouched.**

**A subject whose extras are empty must still render byte-identically** — the frozen subjects are checked
against their digests, so any drift there is caught by Task 2's own STOP.

## 5. Task 5 — render

Render `l0-l1`. Its members: the ruled six, then the three extras — the filtered `FRAMEWORK.md`
§5-and-§9 charter member, the five reading-pass extracts with their one named section cut, and
`EMPIRICAL_FINDINGS_LEDGER.md` whole. **Both filters verified in both directions on the render, as the
extension already requires.**

## 6. Task 6 — CC's declared change is ratified and stands (Ruling 17(d))

The what-was-cut section derives its text from the counts, so a subject with an empty withheld family
but filtered extras does not tell its session *"Nothing has been withheld"*. **Ruling 17(d) ratifies
this; it stays as built and needs no further act.** Stated here so a later reader does not take it for
an unreviewed accommodation.

## 7. Done — written against Task 1's findings

Done when **`--check` is GREEN**, and the report **accounts for each line Task 1 recorded as red**:

- the **manifest** line — resolved by regeneration, which Ruling 17(c) permits;
- the **four member lines** of the two existing packs — resolved **not by re-rendering** but by those
  subjects being **frozen and checked against their digests**;
- the **missing `l0-l1` directory** — resolved by Task 5's render.

**If `--check` is green for any other reason, or red for a reason not on Task 1's list, STOP and
report.** Also done when: the ruled six stand intact and in order for every subject; the new anchor
matched exactly once and verified both ways; the two frozen subjects' files are **byte-identical to the
blobs Task 1 recorded**, proven by re-hash; and every STOP met is written up.

**The standing self-check runs before the report is written** — the actual diff on disk of every touched
file, against the principles, the conventions and `DEFECT_TYPES.md`.

## 8. STOPs

The extension absent or materially different from Task 1's expectation; a frozen file's blob moving; the
new anchor matching zero or more than once; any ruled member removed, reordered or renamed; a red
`--check` line not on Task 1's list; a read disagreeing with its pin; any act outside §9.

**This batch repairs nothing it finds beyond what §§2–5 order, and widens no filter.**

## 9. The footprint — enumerated from this batch's own orders

**Created:** `tools/audit/derivation_boot_pack/l0-l1/` and its ten files; this batch's report and its
artifact.

**Edited:** `tools/audit/gen_derivation_boot_pack.py` — the `FROZEN` table and its two behaviours, the
one added removal, and the four residual repairs. **Regenerated:** `tools/audit/derivation_boot_pack.json`.
**No other existing tool source is edited except the forward bound's own per-batch re-aiming,
`gen_status_batch_bound.py --apply`, EXCEPTED BY NAME** (Ruling 5 of
`cowork_rulings_2026_08_26_amendment_landing_sitting.md`).

**Written into NEITHER existing pack directory** — that is Task 2's whole point.

**Read and never written:** `FRAMEWORK.md`, the five extracts, `EMPIRICAL_FINDINGS_LEDGER.md`.
**`FRAMEWORK.md` IS NOT AMENDED.**

**Not done at all:** no build, no test, no golden, no measurement of the analysis, nothing under
`tools/corpus/`, `tools/robust_stop/`, `src/` or `docs/`; no governing document amended; **no open-items
row created, flipped or discarded**; **no decisions-register entry and no `D-NNN`** — that register
cannot accept one and `cowork_register_rule_c_suspension_2026_08_28.md` is the route; the workbook not
opened; **no score staged, copied or moved**; no brief written; no session booted.

**The tracked-modification assumption, as a SHAPE with a STOP and with BOTH known exceptions named, so
this bar does not break on acts already taken.** This batch expects the working tree to carry untracked
Cowork material, **and expects TWO tracked-and-modified files: `cowork_rulings_2026_08_31_decision_surface_sitting.md`**
— the writing side's landing of the rulings this dispatch executes — **and
`tools/audit/gen_derivation_boot_pack.py`**, carrying the previous batch's extension. **Neither is a
STOP. Any OTHER tracked modification this batch did not make IS a STOP.**

---

*Provenance: written by the Cowork writing side, 2026-08-31, executing Ruling 17. Read at the files by
this side before writing: `cc_l0l1_boot_pack_report.md` whole and `tools/audit/l0l1_boot_pack_extension.json`
at `the_start_state_stop` and the prediction block — which is where the four stale files and their
addition-only drift were verified; `tools/dcml/ABC/metadata.tsv` and two `.mscx` files at the object, for
Ruling 17(e)'s ground. **Every claim this dispatch makes about the generator's current state is RELAYED
from the previous batch's report and is ordered RE-ESTABLISHED at Task 1 rather than trusted.** No shell
command was run on the repository by the writing side. No figure of this project's own measurement is
restated (#17f, D-431).*
