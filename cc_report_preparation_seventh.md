# CC report — the preparation phase's SEVENTH batch: the split's consequences executed, and the commissioned finer pass answering in the negative

> **Dispatch:** `cc_instruction_preparation_seventh.md`, executing the four rulings of
> `cowork_rulings_2026_08_17_sixth_return.md`.
> **Commits:** Task 0 `ec953b4cef` (parent `6d11d3f8e6`) · Task 1 `5c38b41166` · Task 2
> `e94f765c25` · Task 3 `cfb69a7ecb` · Task 4 the commit carrying this file · then the ONE
> FURTHER commit the E-ordering rule requires. Every one verified at the object and pushed.
> **The full close is THE PREPARATION SEVENTH BATCH section of `cowork_away_returns.md`.**
> Every per-span verdict, every classed movement and every count lives in the generated
> artifacts and is not restated here (**D-431**).

---

## 1. The reading, the start state, and Task 0

### 1.a The session-start reads

The interim reading scope had **expired** before this batch began, by its own ruled terms and as the
sixth batch's close records. `CLAUDE.md`, `STATUS.md` and `BUILD_AND_TEST.md` were read **as they now
stand, in full**; no archive companion was opened, because no decision was re-opened. Then, as the
dispatch orders: `cowork_rulings_2026_08_17_sixth_return.md` whole,
`cowork_rulings_2026_08_17_governing_surface_split.md` whole, §2.b / §2.e / §6 / §7 of
`cc_report_preparation_sixth.md`, that batch's full close, and
`tools/audit/governing_surface_split_application.json`.

### 1.b The start-state guard run — exactly what the dispatch declares

Run **before the first edit**:

```
61 guard(s) run, 3 failing, 4 not run, 16 historical record(s)
  [FAIL] tools/audit/gen_filing_convention_application.py --check
  [FAIL] tools/audit/gen_retirement_caller_check.py --check
  [FAIL] tools/audit/decisions/gen_phase1w_legacy_verification.py --check
```

and `gen_guard_classification.py --check` printed **"the guard classification re-derives"** — 58
passing, **zero STOPs**. This is the dispatch's declared expectation to the tool and to the count, so
no STOP-and-report was owed. The runner **rewrote its own artifact byte-identically**: the sanctioned
enumeration reported no tracked change from the run, so the tree was unmoved by the measurement.

### 1.c Assumption A1, checked first and entirely at content-addressed objects

`tools/audit/changed_paths.py` reported **exactly ONE tracked modification in the whole working
tree** — `cowork_handoff.md` — with the ruling record and the dispatch untracked.

The difference was taken **blob against blob by explicit hash**: the committed blob resolved from the
explicit commit `6d11d3f8e6` (`06cffd9484`), the working blob from `git hash-object`
(`5c46cd44e3`). It is **ONE contiguous changed passage**, 93 insertions and 1 deletion, carrying both
parts A1's own sentence names: the twenty-third session-close block inserted above the twenty-second,
and the twenty-second heading gaining this file's own entry-point demotion marker. **A1 held at the
shape its own sentence describes**, and no count of changed passages was asserted in advance (the F25
lesson).

### 1.d Task 0

Commit **`ec953b4cef`**, parent `6d11d3f8e6`, pushed, **3 paths**, verified at the object.
**E0 — MET.** Exactly 3 paths at the enumeration; one modification whose content matches A1's stated
shape, two additions; no staging override.

---

## 2. Task 1 — the taught citation and the residue

### 2.a Ruling 1's one-line act, and the second citation the same act had broken

`decisions/gen_phase1w_legacy_verification.py` locates five recorded figures by quoting them in the
file that carries each. Ruling 1 orders one of those citations re-aimed at
`OPEN_ITEMS_ARCHIVE.md`, where the split moved [[OI-289]]'s resolved row. That was done: the `where`
and the `line`, and **nothing else in the tool** — proven at the objects, the diff falling in exactly
the two `RECORDED_FIGURES` records and nowhere else.

**★ AND THE SAME ACT HAD BROKEN A SECOND CITATION IN THE SAME WAY, WHICH THE RULING DOES NOT NAME.**
The phase-1i `STATUS.md` entry carrying the 75-entry figure was archived by the split too; its quote
is byte-present in `STATUS_ARCHIVE.md` and nowhere in `STATUS.md`. The check STOPs on the first
failure, so this one was invisible until the first was fixed — and **`--check` cannot pass while a
second citation points at a file its quote has left**, which is what E1 requires. The same remedy was
applied under the ruling's own stated discipline (*the pointer follows the move*), and a third edit
was made with it: that citation's note asserted the entry was *"Still in the live STATUS entries, not
archived"*, which the split had made false. The note is corrected **with its former wording preserved
in place (#12)**.

**This is an extension beyond the ruling's letter and is declared rather than taken silently.** The
alternative — apply the ruling's one edit, leave the check red, and report — was available and was
rejected on the ground that it would have left a red the ruling's own words say should clear (*"the
check returns to guarding the live legacy markings"*) while the cause is identical and already ruled.

**The artifact moves by exactly five lines**, measured against the commit by explicit hash: two
`located_at` records and one note. `--check` PASSES.

### 2.b Ruling 3's residue — and the count is one too many

**The population is DERIVED, never listed.** The candidates are the spans of `STATUS.md` that the
PINNED decomposition classes `operative-rule-text` and that begin **at or after the file's first
dated entry** — a cut that removes the title and the banner without naming either. The derivation is
cross-checked in BOTH directions against the split-application artifact: no candidate is in that
act's moved set, and the moved set plus the candidates plus the file's head account for **every span
the decomposition carries**. A mismatch in either direction halts the tool.

**★ THE RESIDUE IS 17 CANDIDATES, OF WHICH 16 ARE DATED ENTRIES.** The dispatch, the ruling record
and the sixth batch's close all say *17 dated entries*; the seventeenth is not one. It is the live
navigational paragraph at the foot of `STATUS.md` — *"All older dated entries … are in
`STATUS_ARCHIVE.md` … The ratified robust-unit baselines, the hard stop, and the gate policy live in
`CLAUDE.md` … Build/test commands: `BUILD_AND_TEST.md`."* A working session acts on it today.
**It fails the reading's first clause, so it STAYS AT SITE and is flagged** — assumption A3's own
instruction, exercised rather than recorded.

### 2.c The reading each of the sixteen passed, stated once

> A candidate MOVES only when **(1)** it IS, read whole, a dated record of a COMPLETED batch or
> session — the dispatch it names has finished and the work it points forward to has been overtaken
> — and **(2)** it carries no rule, STOP, prohibition or live caveat whose **ONLY** home is this
> entry. A rule the entry states AND names a home for is not a sole carrier: the move is
> archive-with-record and the pointer resolves (#12). Anything else STAYS: the ruled doubt default,
> flagged.

Clause (2)'s narrowness is the half worth reading, and it is meant narrowly: **every candidate here
predates the OI-222 pointer remedy or paraphrases it**, so unlike a modern entry it restates
substance rather than pointing at it — which is exactly why the recognizer left it behind and exactly
why each had to be read rather than classed. Two carried standing rules and **both name their own
homes in their own sentences**: the withheld-findings remedy (*"rowed by CC as OI-222"*, and that row
is OPEN in the register at this tree), and the user's large-score additions (homed in
`ARCHITECTURE.md`, in the decisions register and on [[OI-206]] / [[OI-209]]'s detail files, verified
at those files rather than recalled).

### 2.d The moves, proven in three directions

**46,105 characters** moved verbatim. The reconciliation proves, at the files as they stand and at the
git objects by explicit hash: every moved entry **byte-present in `STATUS_ARCHIVE.md` exactly once**;
every moved entry **absent from `STATUS.md`**; and **the flagged candidate still present exactly
once at site**. The third is the one that matters most — a check that only proved the move would stay
green if a later act quietly archived the flagged span too.

`STATUS.md` falls from **157 lines to 19**: five entries (written at Task 4), two dated pointers, and
the paragraph the reading kept.

### 2.e The one other artifact that moved — F35 recurring

`gen_artifact_inventory_surface.py --check` went red at the edited tree and was regenerated under the
ordinary bound. The whole movement, enumerated: **NAMED in the governing record 82 → 81** and NAMED
NOWHERE 31 → 32, `cowork_spec_code_audit_adjudication_method.md` crossing because its only naming was
inside a moved entry; and **the named ignored files 109 → 107**, `cc_instruction_notation_p7.md` and
`cc_instruction_oi207_adjudication.md` crossing for the same reason. Every subject is a moved entry,
so it is inside the ruled bound, and **it moves no verdict**: a crossing confers candidacy only.

### 2.f Task 1

Commit **`5c38b41166`**, parent `ec953b4cef`, pushed, **11 paths**, verified at the object.
**E1 — MET.** `gen_phase1w_legacy_verification.py --check` PASSES; the tool diff touches its citation
records and nothing else, proven blob against blob; every residue move byte-faithful; **one A3 flag
enumerated** (zero was a valid count and is not what was found).

---

## 3. Task 2 — the census regenerated, re-pinned, and its movement classed

### 3.a Where the pin is, and why it is not where the dispatch's words put it

Ruling 2 orders the census regenerated *"at this task's own commit"* and the dispatch orders Task 2
**in ONE commit**. **These cannot both hold**: a commit cannot name its own hash, so pinning at Task
2's commit takes a second commit to land the artifact. The pin is **Task 1's commit `5c38b41166`** —
the commit this task is performed on top of, which is this batch's own state and is content-addressed
so the reading re-derives forever.

**The departure has an independent merit rather than being merely forced.** A census cannot measure
its own output, and Task 2's commit adds a tool and two artifacts that name candidates. Excluding
them is the correct bound — the same self-reference hazard `gen_governing_surface_readers.py`
excludes its own three outputs for, measured there rather than anticipated.

### 3.b The unit of a movement, and why it is not a diff line

A byte diff of the two readings is **19,896 insertions against 4,373 deletions**, almost all of it one
naming per line. That number says nothing about what changed. The comparison is taken at **four units
of identity**: a HOLDING NAMING (a flagged file and the caller that holds it, with the line it was
found on), a CALLER KIND, a FLAGGED MEMBER with its member verdict, and a CANDIDACY VERDICT.

**41 movements, and ZERO fit none of the three ruled classes:**

| class | holding namings | flagged members | caller kinds |
|---|---:|---:|---:|
| (i) the split's effect | 13 | 1 | 2 |
| (ii) the discard's residue | 2 | | 2 |
| (iii) the tree's ordinary growth | 8 | | 13 |

Each class is a derived **RELATION** tested against the two trees at the commits the two readings were
MEASURED at — a path that appeared or vanished; a naming's own line now sitting in an archive
companion; a subject still named inside the register data's `retired_entries` block, whose own words
are that nothing in it is rendered into the INDEX or the group files. **A relation is evidence about a
movement's origin, not proof of its cause**, so the relations are tested in a fixed order and the
first that holds is recorded WITH its evidence — a placement can be checked rather than trusted.

**Three properties of the derivation are worth naming because each was forced by a failure the tool's
own STOP surfaced, not designed in advance.** The naming line the census publishes is a WINDOW with an
ellipsis at each cut end, so a bare substring test fails on exactly the longest lines — the governing
record's own. Both sides had at first been read at the commit that CARRIES an artifact rather than at
the commit its reading was MEASURED at, which is a statement about a third tree neither reading is
about. And a caller's kind does not move on its own: it appears or vanishes with the namings that put
it in the table, so it is placed LAST and by inheritance, and a caller whose namings hold nothing
inherits instead from the flagged members its own text names, **by the census's own base-name
reference test** rather than by a stricter one this tool would have invented.

**★ CLASS (i) SEPARATES THE TWO ACTS THAT ARCHIVE A SPAN OF THE SAME PARENT.** The ruled split itself,
and Ruling 3's residue move which completes it under Ruling 4's own line, both put spans into
`STATUS_ARCHIVE.md`. Which one did it is derived — was the line already in the companion at the
outgoing reading's own commit? — and published per movement rather than blurred.

**★ AND A MIXED INHERITANCE IS RECORDED, NEVER RESOLVED BY PREFERENCE.** One caller entered the table
for two reasons at once. The placement takes the class first in the ruling's own order — split,
discard, growth, which runs from the specific cause to the general — and **every class it inherits is
published beside the verdict**, so the mixture stays visible while the tally stays a partition.

### 3.c The scalars, published and deliberately not classed

Every scalar the census publishes is a SUM over the units above, so classing it would count the same
cause twice. They are published with their before and after — among them ENUMERATOR 7 → 15, tracked
Python sources parsed 328 → 341, and the flagged population 413 → 414.

**★ THE KIND-UNDERIVABLE POPULATION IS 9 AT THE NEW PIN, PUBLISHED AS DATA.** It confers CANDIDACY
only. No fate moved, no flag was acted on, every ruled condition on candidacies stands untouched, and
the callers sitting reads this population when the user holds it.

### 3.d Task 2

Commit **`e94f765c25`**, parent `5c38b41166`, pushed, **7 paths**, verified at the object.
**E2 — MET.** `gen_retirement_caller_check.py --check` PASSES at the new pin; the movement artifact
classes every difference with **zero unclassed**; the KIND-UNDERIVABLE population's new value is
published as data.

---

## 4. Task 3 — the finer `CLAUDE.md` measurement, read-only

### 4.a What changed, and the measured ground of each change

Two changes, each answering one of F33's two measured cases.

- **A cut at every numbered-principle opener.** F33's second case: an amendment record's span
  OVERRAN into principles 22, 23 and 24, which carry no blank line above them, so the coarse pass
  placed three standing principles in an archive class. The numbering is the record's own convention,
  exactly as the ★ marker is.
- **A former-wording marker no longer places a span on its own.** A QUOTATION must accompany it,
  beginning within a published window. F33's largest mis-class: 15,395 characters of live governing
  rule text — the decisions register's rules (a) through (n) — classed on two sentences that POINT AT
  former wordings held in the register's own provenance, the span containing none.

**What the floor costs is stated so it is chosen rather than discovered:** a genuinely preserved
wording shorter than the floor, or beginning further from every one of its markers than the window,
is NOT placed and stays at site; a preservation marked by italics alone, with no quotation marks, is
likewise not placed. Both errors run in the recoverable direction.

Two smaller properties were forced by the measurement itself rather than designed: **every**
former-wording marker in a span is tested, not merely the first, because a span often announces the
preservation in its heading and carries the wording several sentences later; and the window bounds
where a quotation may START, never where it may END, because a preserved wording routinely runs past
any window one could pick — principle #21's runs some four hundred characters past this one.

### 4.b ★ Two structural spans the pass's own self-check caught before they shipped

Reading the surface it had just generated, this pass found **two spans it was proposing to archive
that must never be classed by the words they contain**, and both would have been wrong in the
direction the ruled doubt default exists to prevent.

- **An archive pointer.** The executed split leaves a compact dated pointer at every site it
  archives, and that pointer **quotes its target's class and the opening of what moved**. A
  recognizer reading the pointer's words therefore files it under the class of the thing it points
  at. Archiving a pointer removes the one breadcrumb a reader has back to the moved span — the
  opposite of what the pointer discipline is for. Three were proposed.
- **A bare heading.** A markdown heading is a span of its own and its section body is another, so a
  heading classed by its own words — *"RETROSPECTIVE … superseded … historical reference"* — would
  move while everything under it stayed, leaving a headless section here and a heading filed alone in
  the companion. One was proposed.

Both are now placed at site **POSITIVELY rather than by the doubt default**: the doubt default records
an absence of evidence, while these are decisions with a reason, and publishing the reason is what
lets the user overrule either in one edit rather than rediscover it.

### 4.c ★ The cross-check that makes the surface decidable — and the pass's own negative answer

The measurement: **151,045 characters, 185 spans, 89.6% left at site by the doubt default**, and
**13,542 characters in six spans placeable as archive material by evidence — 9.0%**, against the
coarse pass's 23% prediction and the split's actual 3.2%.

**But the number that decides the question is smaller, and it comes from a cross-check this pass
adds.** The executed split's A4 safeguard READ 17 spans and deliberately KEPT them. **Five of this
pass's six proposals are among them.** Each conflict is named on the surface with the safeguard's own
reason quoted, under a stated rule for reading it: where the reason was that a span OVERRAN into live
rule text, the finer cut answers it; where the reason was that the span ITSELF states a rule, no cut
answers it and the proposal should be refused.

**Exactly one conflict is answered** — the principle-#21 overrun. The other four were kept because
the span states a rule: the apparatus-row lapse rule, the OI-170 carry-forward with its live
prohibition, the A-8 dual-track's live baselines, and the shell-read rule's reach. **One span, 845
characters, is proposed on ground no reading has yet refused.**

So the surface states the answer to the commission in terms: **if the goal is a materially smaller
`CLAUDE.md`, the recognizers are not the route.** What remains is rule text and amendment records that
no pattern over prose separates from the rules they amend. That is a finding about the file, not a
failure of the pass, and it is written on the surface rather than left for the user to infer from a
small number.

### 4.d The reach, refreshed — and the one refactor it needed

The reach for `CLAUDE.md` at this pass's own commit: **797 files naming it, 11,362 namings, 2,723
anchored across 65 files, 4 tools parsing it, 87 register entries homed in it.**

The scan is **IMPORTED** from `gen_governing_surface_readers.measure` rather than re-implemented
(#6). That module was pinned to the coarse pass's commit, so it gained a `commit` parameter
defaulting to its own pin — and **its artifact and its ruling surface re-derive BYTE-IDENTICALLY**,
which its `--check` proves rather than asserts. **Nothing is excluded from this scan**, unlike the
coarse pass's: that pass excludes its own three outputs because each names all five governing files;
this reading is taken at a pinned commit at which its own outputs do not exist, so the figure is
wider and is not comparable to the coarse one — stated on the surface itself.

### 4.e Task 3

Commit **`cfb69a7ecb`**, parent `e94f765c25`, pushed, **10 paths**, verified at the object.
**E3 — MET.** Every span classed with evidence and **accounted to the character** (the STOP in the
tool, not a reported figure); the surface generated from both artifacts; **`CLAUDE.md` byte-unchanged,
proven by hash** — blob `8230aa91b6` at the pin and in the working tree; both new tools registered
under the new-tool rule with `--check` forms.

---

## 5. The new-tool rule, discharged three times

This batch adds **three** tools, and each landed **with its authored run-instruction and its authored
classification verdict in the SAME commit that adds it** — four invocations between them, all four
passing.

- **`tools/audit/gen_status_residue_move.py`** (Task 1) — proves three directions, and the third is
  what makes it more than a move record: the ONE candidate the reading flagged is still at site, so a
  later act that archived it too fails on the day it happens.
- **`tools/audit/gen_retirement_census_movement.py`** (Task 2) — its STOP is the guard: a movement
  the derivation cannot place halts it and **prints the unplaced records WHOLE**, because a halt that
  names a count and five identities sends the next session hunting for what it already knew.
- **`tools/audit/gen_claude_md_finer_spans.py`** and **`tools/audit/gen_claude_md_finer_surface.py`**
  (Task 3) — the second's load-bearing claim is that the ruling surface still re-derives
  byte-identically from the two artifacts, so a hand-edited proposal cannot reach a ruling unnoticed.

*(Three tools, four guard invocations: the finer pass is two tools, on the same split of concerns the
coarse pass uses — one owns the decomposition, the other the reach and the rendered surface.)*

---

## 6. The guard set

| when | run | failing | not run | historical | STOPs |
|---|---:|---:|---:|---:|---:|
| before the first edit | 61 | 3 | 4 | 16 | 0 |
| at the Task 1 tree | 62 | 2 | 4 | 16 | 0 |
| at the Task 2 tree | 63 | 1 | 4 | 16 | 0 |
| at the Task 3 tree | 65 | 1 | 4 | 16 | 0 |
| **the end state**, after the Task 4 commit existed | **65** | **1** | 4 | 16 | **0** |

**★ E4 — MET, on the run and on nothing else.** The end-state run ordered at the tree carrying the
close, taken **AFTER commit `97b3792521` existed**, printed:

```
65 guard(s) run, 1 failing, 4 not run, 16 historical record(s)
  [FAIL] tools/audit/gen_filing_convention_application.py --check
```

and `gen_guard_classification.py --check` printed **"the guard classification re-derives"**. **Every
run check passes except [[OI-372]]'s tool, zero STOPs** — the shape the dispatch predicted, reached
for the first time in three batches. Run and read, never inferred; the values are committed only
after the run that produced them.

The population grows by exactly this batch's four new invocations. **Two of the three opening reds
CLEARED by the acts that were ruled to clear them** — the legacy-mark verification at Task 1 (F36
discharged) and the retirement caller-check at Task 2 (F37 discharged). **The one that remains is
[[OI-372]]'s tool and no other**, which is what assumption A2 predicts. `gen_guard_classification.py
--check` printed **"the guard classification re-derives"** after every regeneration.

**★ ASSUMPTION A2 HELD.** Its check is that any red other than [[OI-372]]'s is classed by its measured
cause, and one outside the acts' own subjects is a STOP-and-report. No such red appeared: the only
reds after the opening run were the two the batch was dispatched to clear, and both cleared.

**★ AND EVERY RUN WAS TAKEN AT A QUIET TREE.** No guard run overlapped an edit of this session's own —
the defect the sixth batch's close records at its §2.3. Where a correction was owed mid-task, the run
in flight was allowed to finish before anything was touched, and the set was re-run after.

---

## 7. Surfaced findings (D-641, #13, #19) — surfaced, not rowed

The dispatch bars creating an open-items row. Each is stated here and in the close.

- **F40 (new) — THE RESIDUE'S SEVENTEENTH CANDIDATE IS NOT A DATED ENTRY, AND THREE SURFACES SAY IT
  IS.** The dispatch, Ruling 3 and the sixth batch's close all say *17 dated entries*. Derived, the
  residue is 17 CANDIDATES of which 16 are dated entries; the seventeenth is the live navigational
  paragraph at the foot of `STATUS.md`. **The read-before-move safeguard caught it**, which is A3
  working. §2.b.
- **F41 (new) — A RECOGNIZER OVER PROSE FILES A POINTER UNDER THE CLASS OF THE THING IT POINTS AT.**
  The split's own dated pointers quote their target's class and opening, so the finer pass proposed
  archiving three of them; a bare section heading was proposed for the same reason. **Found by this
  pass's own self-check reading the surface it had just generated**, before anything shipped. The
  general form is worth carrying: **an archiving act that leaves pointers creates, in the same act,
  a population its own recognizers will mis-class on the next pass.** §4.b.
- **F42 (new, the largest) — THE COMMISSIONED FINER PASS ANSWERS IN THE NEGATIVE, AND THE CONFLICT
  CROSS-CHECK IS WHAT ESTABLISHES IT.** 9.0% of `CLAUDE.md` is placeable as archive material by
  evidence, but five of the six proposals were already read at the file and kept by the A4 safeguard,
  and only one of those conflicts is answered by the finer cut. **One span, 845 characters, is
  proposed on ground no reading has refused.** The recognizers are not the route to a materially
  smaller `CLAUDE.md`. §4.c.
- **F43 (new, small) — A NEW FALSE DENY MEASURED LIVE AT THE SHELL-READ GUARD, of the [[OI-300]]
  family.** A shell command reading a **scratchpad** path held in a shell VARIABLE was denied as
  aimed at a repository path: the guard cannot resolve the variable and classifies the token by its
  shape. The same command with the literal path was admitted. Measured here rather than reasoned
  about, and **not tuned away**: the guard's deny-on-indeterminate policy is what produced it.
- **F36 and F37 are DISCHARGED by this batch's own acts**, and are recorded as discharged rather than
  dropped: the standing check broken by an archived row now passes, and the artifact that could not
  be regenerated inside the act that moved it is re-pinned with its movement classed.
- **F1–F35, F38, F39 (carried, unchanged)**, including **F3**, now **nine times surfaced** —
  `reaim_home_anchors.py --check` exits 0 while printing drifted anchors, and
  `gen_cluster_dispositions.py --verify` is the drift authority. **Still unfixed and unrowed: the
  dispatch bars both.** **F35 recurred twice more**, in both directions, and is reported at §2.e.
  **F25 did not repeat**: no changed-passage count was asserted in advance.
- **The E3 ordering defect and the A1 premise error of the earlier batches** ride to the phase's
  retrospective as the dispatch orders.
- **No finding bears on the analysis, its inputs, or a measurement tool the analysis depends on.**
  Every subject of this batch is the project's own record and the apparatus that reads it.

---

## 8. What this batch did NOT do

**No fate on the finer surface was executed** — Task 3 proposes, the user rules, a LATER dispatch
executes. **`CLAUDE.md` is byte-unchanged, proven by hash.** **No caller flag was acted on**: the
census's crossings confer candidacy only. **No sole-carrier member discarded; the 62 not ruled; the
residue and rulings-sort surfaces stand awaiting their sittings; the prose-citation question stays
open.** No empirical findings ledger, no fact-gate admission, no curated boot list; no completion
claim about the superseded phase-1 program; no derivation, design, repair or pilot act; **no `src/`
change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
`tools/robust_stop/`, no measurement of the analysis.** **No open-items row was created, flipped or
discarded** — [[OI-372]] and [[OI-374]] stand exactly as found, [[OI-179]] stays OPEN and GATES, and
F3 stays surfaced, unfixed and unrowed. **No document was archived, moved or deleted AS A FILE**: this
batch moved SPANS between files the record keeps.

---

## 9. The self-check over this batch's whole diff

Run over the diff actually on disk, not the memory of writing it.

- **Reserved-word conventions.** The new prose uses *measurement tool*, *check* and *script* for the
  gauging sense; *entry*, *record* and *row* rather than a bare note; *measurement* for the gauging
  noun. `score`, `key`, `mode` and `register` appear only in their musical sense or in the qualified
  compound the convention fixes (*the open-items register*, *the decisions register*).
- **#17f / D-431.** Every figure in the report, the close and the `STATUS.md` entries is read from a
  generated artifact or from a captured run, and each says where. No figure is hand-transcribed into
  a governing surface.
- **#12.** Two former wordings are preserved in place — the phase-1i citation note in the tool, and
  the forward bound's two re-aimed inputs, whose previous values remain readable at the objects.
- **#6.** The reach scan is imported rather than re-implemented; the dated-entry pattern is imported
  from the pass that owns it; the census's own base-name reference test is used rather than a
  stricter one invented here. The finer decomposition's cut is a **different unit** from the coarse
  pass's, not a second implementation of the same one, and everything the two units share is
  imported.
- **The one departure from a dispatch's words** — the census pin — is declared in the commit message,
  the close, the report and the `STATUS.md` entry, with its ground.
- **Nothing was adjusted to make a check green.** Every red that cleared did so because the act the
  ruling ordered removed its cause.
