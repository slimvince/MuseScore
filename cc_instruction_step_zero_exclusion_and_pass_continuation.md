# CC dispatch — land the 2026-08-22 record; execute the step-zero return rulings (the `STATUS.md` exclusion, the re-derivation, the declared third pollution value); then CONTINUE the per-entry pass of the widened July screen

> **Dispatch (Cowork, 2026-08-22, the forty-first Cowork session).** Executes Rulings 1(a), 1(c) and
> 2(b) of `cowork_rulings_2026_08_22_step_zero_return_sitting.md` and Ruling 1 of
> `cowork_rulings_2026_08_22_dispatch_order_sitting.md`. **Written at a verified stop; nothing else
> is running and no dispatch was active when this was written.** The tip was read by the writing
> side with `git show -s` and `git for-each-ref` at the explicit hash `64e7d2fd7f` and at nothing
> else.
>
> **★ WHAT THIS BATCH IS.** Task 0 lands the two 2026-08-22 sitting records, the modified handoff
> and this dispatch, and PUSHES. Task 1 executes the three rulings that order an act: `STATUS.md`
> LEAVES the specification document set by an AUTHORED EXCLUSION fed to the generator (1(a)); the
> widened screen population and the July screen are RE-DERIVED over the set as it then stands
> (1(c)); and the nine members the screen cannot see get a DECLARED THIRD VALUE for their pollution
> input, derived from the candidate enumeration and from git (2(b)). Task 2 CONTINUES the per-entry
> pass over the widened screen's `NOT YET READ` set, re-derived at the artifact. Task 3 closes.
> **No pilot act** — Ruling 1 of the dispatch-order sitting declines it for this batch.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *A changed passage* — one contiguous run of
> changed lines inside one commit's edit of one document; the tools' own artifacts and field names
> say "hunk", and that word is used below ONLY where it names a field or quotes a tool. *The document
> set* — the members `tools/audit/specification_document_set.json` lists, derived by
> `tools/audit/gen_specification_document_set.py` from `ARCHITECTURE.md`'s admitted delegations plus
> `docs/scoring_model.md`. *The widened screen* — the July screen's second population, every flagged
> changed passage of the restructuring period whose file is a document-set member, with an authored
> verdict per passage. *The coverage gap* — the document-set members with no flagged changed passage
> in the candidate enumeration. *A fact-gate mining input* — a document that is NOT a member but is
> read once, whole, behind the admission test (*does the fact survive the implementation being
> thrown away?*); a plan line, not a task of this batch.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional, and explicitly NOT demoted. **(3)** `BUILD_AND_TEST.md` is a
> CONDITIONAL read — **and this batch MEETS the condition**, because it runs the guard set and the
> audit generators, so read it in full. **(4) DO NOT READ THE WHOLE OPEN-ITEMS INDEX.** Rule (a)
> reads the DERIVED GATING ANSWER narrowed to its identity list —
> `tools/audit/nongating_apparatus_rows.json` → `★_the_live_gating_answer` → `gating_ids`. The INDEX
> remains the AUTHORITATIVE STATUS SURFACE.
>
> **★ AND THE STANDING CLAUSE BINDS THIS ONE: read `cowork_handoff.md`, ITS CURRENT (FORTY-FIRST)
> BLOCK.** **The findings series stands at F88 when this batch begins** — no number was allocated by
> the thirty-ninth, fortieth or forty-first sessions. **This batch allocates NO finding number
> either** — see *Findings in this batch* below.
>
> **★ AND READ THE STANDING CLAUSES AT THE FILE, NOT AT A COUNT.** `cowork_audit_protocol.md`'s
> dispatch-protocol section is to be read **IN FULL** — every `###` section carrying the membership
> marker binds this dispatch and your report — before Task 1 writes anything. No number is stated
> here and none is to be written anywhere in this batch.
>
> **Then read, in full and in this order:** `cowork_rulings_2026_08_22_step_zero_return_sitting.md`
> (this batch's authority for Task 1 — Rulings 1 and 2 above all; §5 and §6 whole);
> `cowork_rulings_2026_08_22_dispatch_order_sitting.md` (the authority for Task 2's place and for the
> pilot's absence); `cc_report_step_zero.md` whole (the batch this one continues; its §4 is the
> standing form of the stop you will take again); the fortieth and thirty-ninth handover blocks of
> `cowork_handoff.md` (below the forty-first); and the docstrings of
> `tools/audit/gen_specification_document_set.py`, `tools/audit/gen_period_stratum_split.py` and
> `tools/audit/gen_july_screen.py` — **the three tools Task 1 edits or re-runs and may not
> re-invent.**
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/doc_change_candidates.json` and `tools/audit/doc_change_candidates_hunks.jsonl` (the
> candidate enumeration — **read and never regenerated**; its `range` is the period Task 1 step (c)
> reads); `tools/audit/specification_document_set.json` (re-derived by Task 1);
> `tools/audit/period_stratum_split.json` (re-derived by Task 1, its existing fields ABOVE the
> widened block byte-unchanged); `tools/audit/july_screen.json` and
> `tools/audit/july_screen_report.md` (re-derived by Task 1, extended by Task 2, **the existing
> sixty-eight verdicts byte-unchanged by published digest**);
> `tools/audit/evidence_pin_membership.json` (Task 0's subject);
> `tools/audit/nongating_apparatus_rows.json` (**read and never written**);
> `tools/audit/guard_classification.json` and `tools/audit/guard_state.json`.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS built,
> designed, scoped or run, no design, no repair, **no derivation of any specification, no frame, no
> pilot act, no fact-gate admission, no ledger, no mining read**, no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, **no edit to any screened
> document** (`ARCHITECTURE.md`, `docs/scoring_model.md`, `STATUS.md`'s content beyond Task 3's
> pointer entries, and every document-set member are READ and never written, however plainly wrong
> a passage is found), **no edit to the delegation at `ARCHITECTURE.md:4265` (Ruling 1(b))**, **no
> edit to the document-governance clause (Ruling 3(d))**, **no re-grading of any authored grade in
> `gen_specification_document_set.py` — the exclusion is an INPUT beside the grades, never a change
> to one**, and **no caller flag or candidacy acted on**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end.**
> The declared start state is below and is stated **at the tree you will actually meet**. **Verify
> every commit at the object by explicit hash.** **The reserved-word conventions bind all new prose.
> The E-ordering rule stands, at both grains: state at the close that the end state is NOT asserted
> there, and let the further commit carry it.** **Every guard run is taken at a quiet tree.** **Every
> shell command carries `; echo "exit:$?"` and no command produces large output to the terminal**
> (`CLAUDE.md`, the VS Code extension rules). **`git status` and `git diff` over the working tree are
> measured to time out on this mount and are in any case denied by the armed guard — use the
> per-path explicit-hash forms named in A1.**
>
> **★ THE VOCABULARY RULES BIND EVERY LINE THIS BATCH WRITES** — TOWARDS the ultimate objective and
> TOWARDS the guiding principles, never *against* either; **no self-invented or abbreviated term**:
> write *a changed passage*, *the current commit*, *untrusted source*, *measurement tool* (the
> 2026-08-17 vocabulary ruling), and explain any term of the tools' own in plain words at its first
> use in your report. **Music-theory words in their musical sense only**: a *screen* here is a
> measurement over documents; *score* is never used numerically; *measurement tool* and never the
> reserved word.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1(a), 2026-08-22 step-zero return:** *"`STATUS.md` is EXCLUDED from the specification
  document set. The exclusion is an AUTHORED INPUT to `tools/audit/gen_specification_document_set.py`,
  carrying the finding, the date (2026-08-22) and the reason — the D-677 shape: an input to a
  derivation, never a hand edit to its output — so that `--check` still re-derives the set. The
  derivation's name states the exclusion (D-661: "derived from the admitted delegations, less one
  authored exclusion"). The ground is the plan's own §5 exclusion ground: a process record is not a
  specification of the analysis."*
- **Ruling 1(b):** *"The delegation at `ARCHITECTURE.md:4265` is UNTOUCHED."*
- **Ruling 1(c):** *"The 200 changed passages of `STATUS.md` STAY in the candidate enumeration
  (`tools/audit/doc_change_candidates.json`) untouched (#12). They leave the widened screen
  population and its read-share denominator; they are not read by the screen and carry no
  verdict."*
- **Ruling 1(d), binding here only as a plan line and a bar:** *"`STATUS.md` and
  `STATUS_ARCHIVE.md` are RECLASSIFIED as FACT-GATE MINING INPUTS … ONE whole read of both is OWED,
  in its own run, AFTER the pilot has established the method … This is a PLAN LINE, not a
  dispatch."* — so this batch reads neither for mining.
- **Ruling 2(a):** *"The nine REMAIN members of the specification document set … NOTHING is
  excluded from the derivation, and no file is archived, moved or deleted."*
- **Ruling 2(b):** *"ONLY the pollution input of Ruling 12 is affected. For each of the nine, the
  July screen's per-document value is recorded as a DECLARED THIRD VALUE — "NOT EDITED IN THE
  RESTRUCTURING PERIOD; last authored before it, at <commit, date>" — derived by the generator from
  the candidate enumeration and from git, never hand-typed (#17f, D-431). It is distinct from a
  measured distribution and from "clean" …"*
- **Rulings 3 and 4 of the same record order no act in this batch** (the record's §5 says so in
  terms); Ruling 3(b)'s five documents and Ruling 1(d)'s two are plan lines carried in Task 3's
  close, not read here.
- **Ruling 1, 2026-08-22 dispatch order:** *"The dispatch continues the per-entry pass from the
  artifact's own `NOT YET READ` set — re-derived at the artifact after the exclusion of `STATUS.md`,
  never carried from any report's account of it — by the inherited method, in the artifact's own
  order, stoppable at a member boundary with the stop recorded in the standing form. The pilot is
  NOT opened by this dispatch."*
- **Ruling 7 of 2026-08-21, still the screen's authority:** *"… Its per-document distribution feeds
  the reading depth (Ruling 12) and the ordering of units. Failure signal, ruled with it: if most
  passages land UNDETERMINED the premise is not measurable, and that is a STOP to the user, not a
  licence to proceed."*

## Premise ledger

- **FACT** — the branch tip is `64e7d2fd7f` (parent `a1d7d95ecc`), subject line *"end state: the
  guard set run at the tree the close left — every check passing except OI-372's tool, zero
  STOPs"*; `refs/heads/master` and `refs/remotes/origin/master` both at it. Read by the writing side
  with `git show -s` and `git for-each-ref` at the explicit hash on 2026-08-22. **Nothing is
  unpushed.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 74, passing 73, failing 1,
  not_run 4, historical 16, no `"STOP"` string in the object; the one failing check is
  `tools/audit/gen_filing_convention_application.py --check` ([[OI-372]]'s tool). Read at the
  object by the fortieth session; **read it again at `git show 64e7d2fd7f:tools/audit/guard_state.json`
  yourself, not from here.**
- **FACT** — [[OI-179]] is in `gating_ids` on 2026-08-22; read at the staged artifact by the
  writing side (`tools/audit/nongating_apparatus_rows.json`, the `gating_ids` list opening at its
  line 186, the identity at line 244).
- **FACT** — the existing verdicts' digest is
  `32366793f298ff1c1d17422b61aed4660b0fdefe7aa201bc677ead0e0435320c`, published at
  `tools/audit/july_screen.json` → `★_the_widened_screen.the_existing_verdicts_digest` and in the
  report, identical across every run of the step-zero batch. **It must be identical across every
  run of THIS batch; a different value is a STOP-and-report.**
- **FACT** — `STATUS.md` is a member of the document set by the authored grade at
  `gen_specification_document_set.py` `GRADES["STATUS.md"]` (form `explicit-delegation-clause`,
  anchor `implemented/planned state lives in STATUS.md`), and it is NOT a member of the seed
  (`the_seed_reconciliation` names it absent). **The grade stays; only membership changes (Ruling
  1(a)).** The tool's STOP 2 — an authored grade for a target the scan does not find — is why the
  grade may not be deleted: `ARCHITECTURE.md` still names `STATUS.md`.
- **FACT** — at `tools/audit/july_screen_report.md` (the per-document table and the unread list),
  `STATUS.md`'s changed passages are ALL `NOT YET READ`: none carries an authored verdict. The
  writing side also searched `gen_july_screen.py` for the string `STATUS.md` and found it only
  inside one EXISTING (`CLAUDE.md`) verdict's ground at its line 339 — **no entry of the widened
  authored block names a `STATUS.md` passage.** So removing `STATUS.md` from the population removes
  no authored verdict, and neither authored block changes in Task 1. **Check it at the tool, not
  here (Task 1 step (b)).**
- **FACT** — the nine members of the coverage gap, every one with the reason *"NO hunk of this
  file is in the candidate enumeration … The file does not appear in the enumeration's `files`
  table at all"*, are listed at `tools/audit/period_stratum_split.json` →
  `★_the_widened_screen_population.the_coverage_gap.members`. No member of the gap is there for the
  OTHER reason the tool can give (enumerated, every passage PURE, zero flagged). **A gap member of
  that other shape, if Task 1's re-derivation produces one, is a STOP-and-report: Ruling 2(b)'s
  third value is ruled for the no-commit case only.**
- **FACT** — `gen_period_stratum_split.py` declares in its docstring *"Nothing is read from git
  here"*; `gen_july_screen.py` already reads git objects by explicit hash through its `git()`
  helper (its line 1035) and already derives the per-document distribution (its lines 1227–1247).
  **So the git-derived third value is written in `gen_july_screen.py` and NOT in
  `gen_period_stratum_split.py`, which keeps the latter's declared property true.**
- **FACT, measured by the writing side over bridge-staged copies on 2026-08-22** (`sha256sum`;
  `wc -c` for the one size): `CLAUDE.md` and `STATUS.md` on disk are byte-identical to the tip's
  blobs (`git show 64e7d2fd7f:<path> | sha256sum` on the user's machine, compared to the staged
  copies' hashes); `DECISIONS.md` on disk differs from the tip's blob ONLY by carriage returns
  (stripping `\r` reproduces the tip's hash `e7c5b75078f3…`); `cowork_handoff.md` on disk is NOT the
  tip's blob (`0ef8e62d11db…`) — it carries the thirty-ninth, fortieth and forty-first blocks above
  the committed content, ZERO carriage returns, 502,168 bytes before the forty-first block was
  inserted (`wc -c`).
- **FACT** — `cowork_rulings_2026_08_22_step_zero_return_sitting.md` and
  `cowork_rulings_2026_08_22_dispatch_order_sitting.md` are on disk and ABSENT from the tip
  (`git cat-file -e 64e7d2fd7f:<path>` fails for both). **Neither record carries the word
  `pinned` on any line** (Grep over both by the writing side).

- **ASSUMPTION A1 — the working tree, stated by CONTENT.** Tracked modifications: **(i)**
  `cowork_handoff.md` — three blocks (thirty-ninth, fortieth, forty-first) inserted above the
  committed content, each preceding heading marked superseded-as-entry-point; **(ii)**
  `DECISIONS.md` — carriage returns only, content identical (the F52 shape). **(ii) is EXPECTED,
  is NOT staged by this batch, and is NOT a STOP**; a guard check that fails on it is classed by its
  measured cause and reported, not repaired — the file is generated and a regeneration is NOT
  ordered here. **A modification at any OTHER tracked path is a STOP-and-report.** Untracked and to
  be landed by Task 0, named one by one: `cowork_rulings_2026_08_22_step_zero_return_sitting.md`,
  `cowork_rulings_2026_08_22_dispatch_order_sitting.md`, and this dispatch
  (`cc_instruction_step_zero_exclusion_and_pass_continuation.md`) — **three untracked paths; a
  named path the tree does not carry is a STOP-and-report.** The rest of the untracked population
  (older `cc_*` files, a scratch directory) is NOT landed and is outside A1's STOP. **Check ordered
  (Task 0, first act):** per named tracked path `git diff --stat 64e7d2fd7f -- <path>; echo
  "exit:$?"` (the explicit-hash form the guard admits — the bare working-tree form is denied and is
  not to be attempted); per named untracked path `git ls-files --others --exclude-standard --
  <name>; echo "exit:$?"`.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  **Check ordered (each task's last step):** any other red is classed by its measured cause, and one
  outside this batch's own acts' subjects is a STOP-and-report. The reds this batch causes and must
  clear: **the membership check, cleared by Task 0 step 2**; **`gen_specification_document_set.py
  --check`, `gen_period_stratum_split.py --check` and `gen_july_screen.py --check`, each failing
  between the moment its tool or its input is changed and the moment its artifact is regenerated
  inside the SAME task**, never left red across a commit; and **`gen_session_start_read_size.py
  --check` at the close**, because Task 3 moves `STATUS.md` entries — **F79's own instance,
  expected, cleared by regenerating the measurement and by nothing else.** **Named as a possible
  third shape, not predicted:** a check whose cause is `DECISIONS.md`'s carriage returns (A1(ii))
  — classed and reported, not cleared.

- **ASSUMPTION A3 — the predictions about Task 1's re-derivation, taken from EVERY route the
  inputs reach the artifacts by (the standing clause).** **Route 1, the document set:** the member
  count falls by exactly one (`STATUS.md`), `members_by_limb` loses one limb-2 member, every other
  member and every grade is unchanged; `targets_ADMITTED` is UNCHANGED (the grade stays) and the
  seed reconciliation's `miss_rate_against_the_seed` is UNCHANGED (`STATUS.md` was already absent
  from the seed, and the reconciliation is over admitted TARGETS, not members — **if your change
  moves the miss rate, you have changed grading and not membership: STOP and re-read 1(a)**).
  **Route 2, the widened population:** its `size` falls by exactly the number of `STATUS.md`
  changed passages the previous artifact carried (read that number at the committed artifact's
  `by_document`, never from a report); `already_screened` is UNCHANGED; `NEW` falls by the same
  number; `by_document` loses the one key; the coverage gap is UNCHANGED in membership and reasons;
  the fields ABOVE the widened block are byte-identical. **Route 3, the July screen:** the existing
  digest UNCHANGED; the widened authored block UNCHANGED; the per-document table loses the
  `STATUS.md` row; the `NOT YET READ` total falls by the same number; the read share's DENOMINATOR
  falls and the share rises (a direction — read the value at the artifact); the failure signal stays
  `INCONCLUSIVE-AT-THIS-COVERAGE`. **Route 4, the guard registry:** no tool is added or removed, so
  the guard population is UNCHANGED; `epoch_write_path.json` and `recognizer_establishment_sort.json`
  are predicted UNMOVED — **if either goes red, establish the cause at the object before touching it
  (D-669) and report it; a cause that is not this batch's own edit is a STOP-and-report.** **Any
  movement outside these four routes is a STOP-and-report.**

## The declared start state — at the tree you will meet

**The committed guard registry records the step-zero batch's END STATE** — one failing check
([[OI-372]]'s tool), zero STOPs. **The tree you meet differs from that record by this dispatch's OWN
INPUTS, and the difference is declared (F67 applied):**

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing
   red. Its STOP text has moved under the row since the row was written (the thirty-ninth block
   records it: *"derived candidates with no authored verdict: BUILD_AND_TEST_ARCHIVE.md,
   OPEN_ITEMS_ARCHIVE.md, cc_report_preparation_fourteenth.md"*); it is still [[OI-372]]'s red and
   not a second one.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — **caused
   by this dispatch's own inputs**: TWO untracked root-level `cowork_rulings_*.md` records now sit
   there, `cowork_rulings_2026_08_22_step_zero_return_sitting.md` and
   `cowork_rulings_2026_08_22_dispatch_order_sitting.md`.

**★ THE GUARD REGISTRY'S OWN ARTIFACT DOES NOT RE-DERIVE EITHER (F83 applied).** `gen_guard_state.py
--check` will open *"STALE vs the run"*, because the committed registry records one failing while
the tree has two, and the same input causes both. **It is not a third failing check.** The declared
start state is MATCHED when the verdicts match.

**★ THE PREDICTION ABOUT THE MEMBERSHIP DIFFERENCE IS TAKEN FROM BOTH ROUTES.** **Route A** — the
count rises by TWO from its committed value and two record names are added. **Route B** — neither
record carries the word `pinned` on any line, so route B is predicted to add NOTHING. **Both are
predictions and not citations — check them at the artifact.**

**A THIRD failing check among the verdicts — OTHER than one whose measured cause is A1(ii)'s
carriage returns, which is reported and not cleared — or a difference in the membership artifact
reaching any member, route, document, pin constant, state or count beyond the two additions, is a
STOP-and-report.**

## Findings in this batch

**No finding number is allocated; the series stands at F88 and stays there.** A finding you make is
sorted by D-438's test as the standing clause orders, with Ruling 9's routing (2026-08-21): **(a)** a
finding that bears on the analysis, its inputs or a measurement tool is SURFACED in the report under
the heading **"Quarantined audit questions"** — not rowed, not numbered, not acted on; **(b)** a
finding about the apparatus gets its open-items row and detail file in the commit that records it
(rule (c)), the row states in its own text that it lapses under D-676; **(c)** a finding bearing on
neither is recorded DISCARDED with finding, date and reason (#10). **An establishment obligation is
never discarded and always surfaced (#19).** The two owed dispositions the plan's §2 names are NOT
this batch's to row or act on.

## Task 0 — land the 2026-08-22 record, clear the membership check in the same act, and PUSH

1. **A1's check first, entirely at content-addressed objects**, by the per-path commands A1 names.
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE its difference against the
   committed blob BEFORE accepting it**, against both halves of the prediction:
   `python tools/audit/gen_evidence_pin_membership.py; echo "exit:$?"` then
   `git diff --stat 64e7d2fd7f -- tools/audit/evidence_pin_membership.json; echo "exit:$?"` and read
   the changed passages with `git diff 64e7d2fd7f -- tools/audit/evidence_pin_membership.json >
   <absolute scratch path outside the repository>; echo "exit:$?"` and the file tools. **If any
   member, route, document, pin constant, state or count MOVES beyond the two predicted additions,
   that is a STOP-and-report.** An ADDED derived cross-reference whose cause is this batch's own
   landed records is not such a movement (the sharpened bar in `cowork_audit_protocol.md`), but it
   is reported with its cause established at the record's own text.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_22_step_zero_return_sitting.md`,
   `cowork_rulings_2026_08_22_dispatch_order_sitting.md`, and
   `cc_instruction_step_zero_exclusion_and_pass_continuation.md` (new) — **five paths. `DECISIONS.md`
   is NOT staged.** Subject, exactly:
   `record: the 2026-08-22 step-zero return rulings and the dispatch-order ruling landed; evidence-pin membership regenerated`.
   Staged by explicit path, never by `git add -A` or `git add .`.
4. **PUSH** — `git push origin master; echo "exit:$?"`. **Verify at the object** that
   `origin/master` names your Task 0 commit.
5. **Re-run `gen_evidence_pin_membership.py --check` at the tree this commit carries: it must PASS.**

**Registered expectation E0:** exactly the five named paths; one content modification matching
A1(i) and one bounded regeneration; three additions; no staging override; the membership check
passing at the resulting tree, so the guard set stands at **one failing** ([[OI-372]]) from Task 0
onward, **plus whatever A1(ii) causes, reported**; `origin/master` at the Task 0 commit.

## Task 1 — Rulings 1(a), 1(c) and 2(b): the exclusion as an input, the re-derivation, the declared third value

**This task is a derivation over a population and is published WHOLE or not at all (the standing
clause); it is placed before the per-entry pass of Task 2 by the ordering rule (D-670), and that
placement is imported from the ruling, not preferred. Its three steps land as ONE commit: the three
artifacts must re-derive together, and a tree with one re-derived and another stale is never
committed.**

**(a) The authored exclusion — `tools/audit/gen_specification_document_set.py`.**

1. Directly below the `GRADES` dict (it closes before the `PROPERTIES` block; locate with
   `grep -n "^PROPERTIES" tools/audit/gen_specification_document_set.py; echo "exit:$?"` and insert
   above that line), add a NEW authored table. Use these names; the content is the ruling's and is
   not to be paraphrased:

   ```python
   # ── AUTHORED — the EXCLUSIONS, fed to the derivation beside the grades (the D-677 shape) ────
   # An exclusion removes an ADMITTED target from the MEMBER LIST and changes NO grade: the
   # target stays graded, its naming stays quoted, the seed reconciliation is untouched. It is an
   # INPUT to the derivation and never a hand edit to its output, so `--check` re-derives the set
   # with the exclusion applied. Every exclusion carries its finding, its date and its reason, and
   # one lacking any of the three STOPS the tool — amended #10's own demand of a discard record.
   AUTHORED_EXCLUSIONS: dict[str, dict] = {
       "STATUS.md": {
           "finding": ("`cc_report_step_zero.md` §6 finding 2: `STATUS.md` is a member by the "
                       "ruled mechanism and contributes about half of the widened screen "
                       "population, while its subject is implementation status, not a "
                       "specification of the analysis."),
           "date": "2026-08-22",
           "reason": ("The plan's own §5 exclusion ground: a process record is not a "
                      "specification of the analysis. The user's stated ground, verbatim: "
                      "\"Best case 'status.md' is a history of what has been done, not anything "
                      "explaining why we do inference in a certain way\"."),
           "ruled_at": ("Ruling 1(a) of `cowork_rulings_2026_08_22_step_zero_return_sitting.md` "
                        "(Alternative A as amended by the user; the user's word: \"approved\")."),
           "what_the_document_becomes": (
               "A FACT-GATE MINING INPUT under the plan's §5 clause for process records, together "
               "with `STATUS_ARCHIVE.md`: ONE whole read of both is owed, in its own run, AFTER the "
               "pilot has established the method (Ruling 1(d)). A plan line, not a dispatch."),
           "what_is_untouched": (
               "The delegation at `ARCHITECTURE.md` (Ruling 1(b)); this tool's grade of the "
               "target; the candidate enumeration's changed passages of the document (Ruling "
               "1(c))."),
       },
   }
   ```

2. In `build()`, immediately after `admitted = [r for r in graded if r["admitted"]]` (the tool's
   line 819 at the tip), add the STOPs and the split:

   ```python
   # ── the authored exclusions, applied to MEMBERSHIP and to nothing else ──────────────────
   for target, ex in AUTHORED_EXCLUSIONS.items():
       if target not in GRADES:
           raise Stop(f"exclusion names {target}, which carries no authored grade")
       if GRADES[target]["form"] not in ADMITTING:
           raise Stop(f"exclusion names {target}, whose grade is not ADMITTED — an exclusion "
                      f"removes a member and there is none to remove")
       for field in ("finding", "date", "reason"):
           if not ex.get(field):
               raise Stop(f"exclusion of {target} lacks its {field} and does not reach the "
                          f"derivation (amended #10)")
   excluded = [r for r in admitted if r["target"] in AUTHORED_EXCLUSIONS]
   admitted_less_exclusions = [r for r in admitted if r["target"] not in AUTHORED_EXCLUSIONS]
   ```

   Then change the ONE member loop `for r in admitted:` (the tool's line 888) to
   `for r in admitted_less_exclusions:`. **Change nothing in the seed reconciliation** (lines
   821–842), which stays over `admitted`.

3. In the returned dict: **(i)** change `"what_this_is"` so the derivation's NAME states the
   exclusion (D-661) — replace *"derived from `ARCHITECTURE.md`'s admitted delegations under Ruling
   6"* with *"derived from `ARCHITECTURE.md`'s admitted delegations under Ruling 6 of
   `cowork_rulings_2026_08_21_successor_plan_sitting.md`, LESS ONE AUTHORED EXCLUSION under Ruling
   1(a) of `cowork_rulings_2026_08_22_step_zero_return_sitting.md`"*; **(ii)** add to `"what_is_AUTHORED"`
   the line *"the exclusions, each with its finding, date and reason, applied to membership and
   never to a grade"*; **(iii)** add a top-level key `"the_authored_exclusions"` directly after
   `"the_grades"`, carrying, per exclusion: `target`, the whole authored record, the target's grade
   record as graded (so a reader sees the delegation still stands), and
   `"★_applied_to": "membership only — the grade, the quoted naming and the seed reconciliation are untouched"`;
   **(iv)** add to `"counted"`: `"targets_ADMITTED_less_exclusions": len(admitted_less_exclusions)`
   and `"authored_exclusions": len(AUTHORED_EXCLUSIONS)`; **(v)** in
   `"★_the_findings_this_derivation_records_rather_than_acts_on"`, the entry opening *"THE SET
   CONTAINS A STATUS SURFACE"* is OVERTAKEN — replace it with an entry that opens *"★ OVERTAKEN
   2026-08-22 — `STATUS.md` LEFT THE SET by the authored exclusion above (Ruling 1(a)); the former
   finding stands in place (#12):"* followed by the former text verbatim.

4. Update the docstring's *WHAT IS DERIVED AND WHAT IS AUTHORED* block with one AUTHORED line for
   the exclusions, and add one sentence after the STOP list: *"9. an exclusion naming an ungraded
   or non-admitted target, or lacking its finding, date or reason, STOPS it."* **Keep
   `PROPERTIES["STATUS.md"]` in place (#12) — it is unused by the member loop and harms nothing;
   deleting it is not ordered.**

5. `python tools/audit/gen_specification_document_set.py; echo "exit:$?"` then `--check`. Measure
   the artifact's difference against the committed blob with
   `git diff 64e7d2fd7f -- tools/audit/specification_document_set.json > <scratch>; echo "exit:$?"`
   and read it: **A3 route 1 must hold exactly.**

**(b) The re-derivation — `gen_period_stratum_split.py` and `gen_july_screen.py` run unchanged in
method.** `python tools/audit/gen_period_stratum_split.py; echo "exit:$?"` — **no edit to this tool
is ordered**; its widened block re-derives from the new document set. Measure the difference against
the committed blob (explicit-hash diff to a scratch file, read with the file tools): **A3 route 2
must hold, and every field ABOVE `★_the_widened_screen_population` must be byte-identical.** Then,
BEFORE regenerating the July screen, do step (c) below, because the July screen's artifact lands
once with both changes.

**(c) The declared third value — `tools/audit/gen_july_screen.py`.** The per-document distribution
is built at the tool's lines 1227–1247 (`distribution.append({...})`); the coverage gap is read from
`widened_block["the_coverage_gap"]["members"]` there; `git()` exists at line 1035.

1. Add, beside the other declared values near `WIDENED_KEY` (line 127), the authored text of the
   third value and its ruling:

   ```python
   # ── AUTHORED — the DECLARED THIRD VALUE of the pollution input (Ruling 2(b), 2026-08-22) ────
   NOT_EDITED_IN_PERIOD = "NOT EDITED IN THE RESTRUCTURING PERIOD"
   THIRD_VALUE_RULING = (
       "Ruling 2(b) of `cowork_rulings_2026_08_22_step_zero_return_sitting.md`: \"ONLY the "
       "pollution input of Ruling 12 is affected. For each of the nine, the July screen's "
       "per-document value is recorded as a DECLARED THIRD VALUE — 'NOT EDITED IN THE "
       "RESTRUCTURING PERIOD; last authored before it, at <commit, date>' — derived by the "
       "generator from the candidate enumeration and from git, never hand-typed (#17f, D-431). "
       "It is distinct from a measured distribution and from 'clean': the screen measures "
       "corrections made toward the code DURING the period and has never measured "
       "authoring-time influence for any member; the fact-gate tests that, per statement, for "
       "every source.\"")
   ```

2. Read the period's bounds from the candidate enumeration's own artifact — load
   `tools/audit/doc_change_candidates.json` (add an `IN_MAIN` path constant beside `IN_SPLIT` if the
   tool does not already load it) and take `range["start_commit_EXCLUSIVE"]` and
   `range["end_commit"]`. **Never type a hash.**

3. Add a DERIVED helper, placed after `git()`:

   ```python
   def last_authored_before_the_period(path: str, start_exclusive: str, end: str) -> dict:
       """Ruling 2(b)'s third value, derived from git and from nothing typed: the commit that last
       touched `path` at or before the period's end, with the check — a STOP, not a warning — that
       no commit strictly inside the period touched it (which is what the candidate enumeration's
       own coverage-gap reason asserts, re-measured here at the objects rather than inherited)."""
       inside = git("log", "--format=%H", f"{start_exclusive}..{end}", "--", path).split()
       if inside:
           raise Stop(f"{path}: the coverage gap says no period commit touched it, but git "
                      f"names {len(inside)} — the two readings disagree and nothing is declared")
       last = git("log", "-1", "--format=%H %cs", end, "--", path).strip()
       if not last:
           raise Stop(f"{path}: no commit at or before {end} touches it — nothing to declare")
       commit, date = last.split()
       return {"value": NOT_EDITED_IN_PERIOD,
               "last_authored_before_it_at": {"commit": commit, "date": date},
               "declared_text": f"{NOT_EDITED_IN_PERIOD}; last authored before it, at {commit[:10]}, {date}",
               "★_what_it_is_not": ("Not a measured distribution and not 'clean' — the screen has "
                                    "never measured authoring-time influence for any member.")}
   ```

4. In the distribution loop, add ONE field per row, `"pollution_input"`: for a member NOT in the
   coverage gap, `{"value": "MEASURED", "see": "by_class"}`; for a gap member whose gap record has
   `hunks_in_the_candidate_enumeration == 0`, the helper's result; **for a gap member with
   `hunks_in_the_candidate_enumeration > 0` (enumerated, all PURE), raise `Stop` naming the member
   — Ruling 2(b) is ruled for the no-commit case and this batch declares nothing for the other.**
   Add `"★_the_declared_third_value"` carrying `THIRD_VALUE_RULING` beside
   `"the_per_document_pollution_distribution"` in the widened block.

5. In `render_report`, add one column `pollution input` at the END of the per-document table
   (header, separator and cell: the `declared_text` for the third value, `MEASURED` otherwise), and
   ONE paragraph under the coverage-gap heading quoting `THIRD_VALUE_RULING`. **No other line of the
   report's rendering changes.**

6. Update the docstring: one paragraph stating the third value, that it is derived from the
   enumeration's range and from git, and its STOP. **The four classes, their order, the six shapes
   and the five STOPs are untouched; `NOT YET READ` and `OUTSIDE NAMED SECTIONS` are untouched.**

7. `python tools/audit/gen_july_screen.py; echo "exit:$?"` then `--check`. Measure the artifact's
   and the report's differences against the committed blobs: **A3 route 3 must hold; the existing
   digest must be identical; the widened authored block must be unchanged — publish both hashes
   (before and after) in the report.** Nine rows carry the third value, each with a commit and a
   date; **a tenth, or an eighth, is a STOP-and-report.**

**(d)** Run the guard set. **A3 route 4 must hold.** Commit the three tools and the three artifacts
(`tools/audit/gen_specification_document_set.py`, `tools/audit/specification_document_set.json`,
`tools/audit/period_stratum_split.json`, `tools/audit/gen_july_screen.py`,
`tools/audit/july_screen.json`, `tools/audit/july_screen_report.md`) — plus the two guard
registries if the run rewrote them — with the subject
`the STATUS.md exclusion as an authored input; the document set, the widened population and the July screen re-derived; the declared third pollution value for the coverage gap — Rulings 1(a), 1(c), 2(b) of 2026-08-22`.
Push; verify at the object.

**Registered expectation E1:** the exclusion applied to membership and to nothing else, with its
finding, date and reason published in the artifact and the derivation's name stating it; every
grade, every quoted naming and the seed reconciliation byte-unchanged; the widened population
re-derived with the existing fields byte-identical and the coverage gap unchanged; the July screen
re-derived with the existing digest identical and the widened authored block unchanged; the nine
third values derived from git with the in-period check passing; all three `--check`s passing at the
commit; **`ARCHITECTURE.md`, `STATUS.md` and every member byte-unchanged.**

**★ BEFORE TASK 2 OPENS, REPORT THE SIZE YOU ARE ABOUT TO READ**, as a direction with its artifact
named — the `NOT YET READ` total at `july_screen.json` → `★_the_widened_screen` after Task 1, never a
number carried from any report. **It is expected to be beyond one session; that is not a STOP — Task
2 is a per-entry pass and stops at a member boundary with its stop recorded.** Say so before reading
the first passage.

## Task 2 — Ruling 1 (dispatch order): the per-entry pass CONTINUED from the artifact's own `NOT YET READ` set

**A per-entry pass (the standing clause): it may be stopped at any member boundary, and the stop is
RECORDED — which members were completed, that the remainder is UNTOUCHED rather than partly worked,
and that nothing is left half-edited. The order is the artifact's own (by document, then by commit,
then by changed passage). The remainder is DERIVED fresh from `july_screen.json` →
`★_the_widened_screen.the_hunks` where `verdict == "NOT YET READ"` — never from
`cc_report_step_zero.md` §4 or from the report's unread list.**

1. **Author one verdict per unread passage, by the inherited method and no other**: retrieve the
   passage from the git object by explicit hash (the tool already does this); read it; apply the
   four classes in their declared order (POSITIVELY CODE-INFLUENCED first); name the shape; write
   the ground citing what it was made from; for a RATIFIED-ACT EDIT name the act AND where its
   ratification is recorded, or do not admit the class. **A section-restricted member's passages
   outside the named sections are recorded `OUTSIDE NAMED SECTIONS` and not graded.** The verdicts
   go into the widened authored block (`W`), never into the existing block (`V`).
2. **Regenerate after each bounded run** so the tree is never left with the artifact behind the
   tool; `--check` passes at every commit; **the existing digest identical at every commit**. Commit
   in bounded runs (`the widened screen: verdicts authored, members <first>–<last> of <document>`),
   each pushed and verified.
3. **The ruled failure signal, checked at the artifact and not at an impression:** if, over the
   widened population's READ members, `UNDETERMINED` is the majority, **that is a STOP to the user**
   — reported as the batch's headline with Ruling 7 quoted; **not a licence to proceed and not a
   licence to argue the class down.** A majority reached on a part-read population is reported as
   `INCONCLUSIVE-AT-THIS-COVERAGE` with the read share named.
4. **Stop at a member boundary** when capacity ends or when Task 3 binds harder; record the stop in
   the standing form (`cc_report_step_zero.md` §4 is the template — import its shape).

**Registered expectation E2:** every read passage carries exactly one verdict in the inherited
vocabulary; the unread remainder carries `NOT YET READ` by the tool's default and no hand; the
existing sixty-eight byte-identical by published digest at every commit; the per-document
distribution published by the generator; the failure signal evaluated at the artifact and stated
either way; the stop recorded in the standing form. **It is not met for the unread remainder, and
that is the stop, not a shortfall.**

## Task 3 — the close

1. One `STATUS.md` pointer entry per task that did work, and **in the same act the step-zero
   batch's entries move verbatim to `STATUS_ARCHIVE.md`** (`gen_status_batch_bound.py --apply`),
   both directions proven. **The declared `Last updated:` prefix adjustment is imported, not
   re-decided (#6).** `gen_session_start_read_size.py` regenerated (A2).
2. The FULL close is appended to `cowork_away_returns.md` as a **THE STEP-ZERO RETURN RULINGS
   EXECUTED, AND THE PASS CONTINUED** section: both guard states, every SHA, every registered
   expectation graded MET or NOT MET with its reason, every declared departure, A3's four routes each
   graded at the artifact, the independence of every new verdict from any verdict the record already
   carries on the same passage (none by construction — say so), the quarantined audit questions (or
   that there are none), **the three PLAN LINES restated as plan lines and not tasks** — the owed
   whole read of `STATUS.md` + `STATUS_ARCHIVE.md` (Ruling 1(d)), of the five glob-only documents
   (Ruling 3(b)), and the pilot's opening with the boot-list ruling for derivation sessions owed
   first (dispatch-order Ruling 1) — and **the plan's tell in one sentence: did this batch produce
   anything other than the exclusion input, the third-value derivation, the re-derived artifacts,
   the authored verdicts and the report? If yes, name it.** The report is
   `cc_report_step_zero_return_executed.md`.
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE.** State at its head that the end-state run is taken
   after the commit carrying it exists and that its values land in ONE FURTHER commit.
4. **★ NAME THE CHAIN AS FAR AS A SENTENCE CAN NAME IT, DECLARING THE ONE COMMIT IT CANNOT.**
5. Commit, push, verify at the object; then the one further commit carrying the end-state run.

**Registered expectation E3:** at the tree carrying the close, a fresh full guard run re-derives —
**every run check passing except [[OI-372]]'s tool, zero STOPs, plus whatever A1(ii) causes,
reported** — run and read, never inferred, its values committed only after the run that produced
them; the tell stated in one sentence.

## What this batch does NOT do

- **No pilot act, no derivation of any specification statement, no frame, no placement test, no
  format test, no held-out test** — the pilot's opening is the next decision, owed WITH the curated
  boot list's ruling for derivation sessions.
- **No mining read of `STATUS.md`, `STATUS_ARCHIVE.md` or the five glob-only documents** — plan
  lines, after the pilot establishes the method.
- **No edit to `ARCHITECTURE.md`** — neither the delegation at `:4265` nor the governance clause at
  `:558-560`; **no write-list delegation written by a session** (Ruling 3(c), 4).
- **No change to any authored grade; no re-grading of the near-tie `cowork_idiom_entry_mapping.md`
  (Ruling 4).**
- **No regeneration of `doc_change_candidates.json` or its changed-passage file** — the 200
  `STATUS.md` passages STAY there (Ruling 1(c)).
- **No change to the July screen's classes, order, shapes or STOPs; no re-reading or re-grading of
  any of the existing sixty-eight verdicts or of any verdict the step-zero batch authored.**
- **No edit to `gen_period_stratum_split.py`** — it re-runs unchanged.
- **No regeneration of `DECISIONS.md`** — its carriage-return difference is reported, not repaired.
- **No finding number allocated; no findings series opened.** [[OI-372]] and [[OI-374]] stay as
  found; **[[OI-179]] stays OPEN and GATES**; F3 stays surfaced, unfixed and unrowed.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule (c)
  as *Findings in this batch* states.**
- **No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
  `tools/robust_stop/`, no measurement of the analysis.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* #12 — nothing deleted: the grade, the naming, the 200 passages, the former
   finding text and `PROPERTIES["STATUS.md"]` all stay; #6 — the exclusion has one home (the
   generator's authored table) and the third value one home (the July screen's distribution);
   #17(f)/D-431 — no count transcribed: every movement is a prediction checked at the artifact, the
   one number the writing side used (the unread remainder) is named to the artifact and ordered
   re-derived; #19 — the candidate generator's unestablished status is carried; the in-period check
   of the third value re-measures the gap reason at the objects rather than inheriting it; #24 — no
   comparison between measured quantities asserted (the read share's rise is stated as a direction).
   Conforms.
2. *Conventions:* American English; no self-invented labels — `AUTHORED_EXCLUSIONS`,
   `NOT_EDITED_IN_PERIOD`, `pollution_input` follow the tools' own `AUTHORED`/`the_*` shapes, and
   every term of the tools' is explained at the head; "hunk" used only where it names a field or
   quotes a tool; music-theory words in their musical sense; the 2026-08-17 vocabulary ruling
   observed (towards, never against).
3. *Figures and premises:* every quantity named to its artifact or stated as a prediction; the
   digest quoted from the artifact; the rulings quoted at their records; the tool line numbers cited
   at the tip's blobs (re-locate with `grep -n` if an edit above has moved them — a line number is a
   locator, not a premise).
4. *File-tools rule:* the writing side's own departures are declared in the forty-first handover
   block, not here; the executing side is bound as `CLAUDE.md` Conventions state, with the
   explicit-hash diff forms named because the bare forms are denied.
5. *Uncertainty:* no comparison between measured quantities is asserted in this dispatch.
6. *Re-read from disk before release:* performed by the writing side over the delivered file,
   hash-verified against the intended content after re-staging (F74), and recorded in the
   forty-first handover block.

---

*Provenance: Cowork, 2026-08-22, the forty-first session, written at branch tip `64e7d2fd7f` under
Rulings 1(a), 1(c) and 2(b) of `cowork_rulings_2026_08_22_step_zero_return_sitting.md` and Ruling 1
of `cowork_rulings_2026_08_22_dispatch_order_sitting.md`. The reserved-word conventions bind this
dispatch, and the vocabulary rules bind every line of the report it orders — TOWARDS the ultimate
objective and TOWARDS the guiding principles.*
