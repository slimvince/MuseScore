# Cowork self-dispatch — the return-session program (written 2026-08-08, executed by the NEXT Cowork session at the user's return)

> **Status: HANDED BY THE USER to a fresh Cowork session. You start clueless; this file plus
> the tenth `cowork_handoff.md` block are your boot. Written by the outgoing Cowork session at
> the last moment it could write — while CC's away batch was in flight — so every population
> and status below is named by artifact, never carried as a figure (D-431), and everything
> marked ⟲ is re-derived by YOU at HEAD before it carries load.**
>
> **★ FIRST, THE BRANCH RULE.** Establish whether the away batch (`cc_instruction_away_execution.md`)
> is COMPLETE: its final task appends a batch summary to `cowork_away_returns.md`, and `STATUS.md`
> carries one pointer entry per completed task. **If the batch is still running: you are
> READ-ONLY on the repository** — boot, read, verify completed tasks only, prepare surfaces in
> your scratch, and write nothing into the tree until CC's stream stops. The user's parallelism
> doctrine (2026-08-08, tenth handoff block) governs: fact-gathering in parallel is fine;
> repo-writing work only at a full STOP with nothing running.
>
> **★ CONDUCT, before anything.** D-253 in EVERY dialect and mechanism — the file tools for all
> repo content; no `python -c "open(...)"`, no PowerShell reads, no shell text utilities; git
> object queries by explicit hash and running the sanctioned measurement tools are the only
> shell uses. The outgoing session broke this and recorded it in `CLAUDE.md`'s D-253 clause —
> read it and do not add an instance. Long-line files (`STATUS.md`, `OPEN_ITEMS.md` rows):
> Grep for the line number, Read that single line with offset/limit — Read on the whole file
> errors and is not an excuse. Verify every CC claim at the objects. Choices to the user in
> FULL PROSE with pros and cons naming principles, rated against the ultimate objective; never
> the option widget. The whole decision surface as user-visible text BEFORE any question. One
> dispatch at a time, written only at a full STOP from ALL present knowledge. The writing-side
> self-check before releasing anything.

## Task 1 — Boot (in this order)

(1) `cowork_handoff.md`, the TENTH block only (the ninth and below are provenance).
(2) `CLAUDE.md` IN FULL. (3) `DECISIONS.md` (the INDEX; note the D-642…D-645 process-rule
entries and the register-count row — do not carry counts, read them). (4) `OPEN_ITEMS.md`
(the INDEX; statuses authoritative there only). (5) `tools/audit/phase1_finish_line.json` at
HEAD ⟲ — THE SCOPE; regenerated by the batch's Task 0, so read it fresh and carry nothing
from this file about its populations. (6) `STATUS.md`'s top entries (one per batch task).
(7) `cowork_away_returns.md` END TO END — the holds, surfacings, STOP notes and per-task
completion notes are your work queue. (8) The five ruling records this arc produced, read
whole when a task touches their subject: `cowork_owner_rulings_2026_08_07.md` (+ its D-472
amendment), `cowork_document_route_rulings_2026_08_08.md`, `cowork_rulings_oi345_oi342_2026_08_07.md`,
`cowork_ruling_guard_family_2026_08_08.md`, `cowork_rulings_2026_08_08_pre_away.md`.
(9) `cowork_scratch_2026_08_08/README.md` and the eight drafts it indexes.

## Task 2 — Verify the away batch at the objects

Execute `cowork_scratch_2026_08_08/draft_return_verification_checklist_2026_08_08.md` in
full: the batch-wide checks (returns file end-to-end; STATUS pointers; the per-task commit
chain by explicit hash; push state to `origin` only; the live guard battery re-run yourself;
self-check sections present; the D-431 sweep) and the per-task checks T0–T8 as the checklist
states them. **Anything that fails verification is surfaced to the user before anything is
built on it — the batch's later tasks may then also be suspect (they build on the committed
state of earlier ones); say so plainly if it is so.**

## Task 3 — Re-derive every ⟲ marker

The checklist's ⟲ section: the finish-line counted block; the disposition residual (now
readable as current only if Task 0's adoption held); the phase-3 gate partition verdicts (a
widening is a #13 STOP by the partition's own terms); OI-349's state after the probe; the
family rows' dated notes; the census-row states the channel-7 table cites; OI-207's channel-9
residual face. Update the eight drafts' ⟲ cells from what you find — in your own scratch
first if the batch still runs, in place if at the STOP.

## Task 4 — Land the drafts at the STOP

Re-banner the eight drafts from scratch-draft status to Cowork reading surfaces (they remain
non-ratified inputs, never specifications), leaving the directory README's status statement
true. This is a repo write: only at the full STOP.

## Task 5 — The consolidated return surface, delivered to the user as visible text

One surface, in this order: (a) the batch's outcome task by task — completed / held /
surfaced, with your verification verdict beside CC's claim wherever they differ; (b) **the
OI-349 probe finding** — analysis-bearing whichever way it landed; if the record arm runs the
grouping on unstabilized input, that is a #13 item and nothing is proposed for it beyond the
row; (c) every hold needing a ruling, each with its full-prose alternatives; (d) the finish
line's remaining distance ⟲ — what still stands between HEAD and the completion statement the
user must commission; (e) **the phase-2 program** (the drafted surface, its five decisions,
with the channel-3/7 preparations and the 1/2/6 scaffolds attached); (f) **the OI-179
commissioning** (the drafted design surface, including the PeARL contact only the user can
make); (g) the guard-family and OI-351 outcomes; (h) commit/push state.

## Task 6 — Then, and only then, the next CC dispatch

Written at the full STOP, from the user's fresh rulings plus everything above, under the
one-dispatch rule and the audit protocol's dispatch rules (ruling ledger with full quotes;
premise ledger with facts at the objects and assumptions with ordered checks and STOPs; no
figures; the writing-side self-check recorded). Likely content — but DERIVE it then, do not
inherit it from this sentence: whatever the batch held, the phase-2 opening if the user rules
it, or the completion-statement commissioning if the finish line supports it.

## What this dispatch does NOT authorize

No fix to the analysis, no design, no inference change; phase 1's completion statement is
written only on the user's commission; no addition to the finish line (a user ruling by its
own scope rule); the freeze holds except where a recorded ruling relaxes it. **The ultimate
objective — maximum-precision inference (#4) — is served here by exactly one thing: closing
the knowledge phases correctly so the family design can finally open on complete ground.**
