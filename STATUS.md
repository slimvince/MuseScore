# MuseScore Arranger — Implementation Status

> **Living document.** Claude Code reads this at the start of every session. Update this as the
> last act when anything changes. For stable architectural decisions, see ARCHITECTURE.md.
> Historical entries live in `STATUS_ARCHIVE.md` (reference-only, NOT part of the session-start
> read) — a superseded entry moves there instead of accumulating here.

*Last updated: 2026-08-22 (CC — **★ THE PER-ENTRY PASS OF THE WIDENED JULY SCREEN IS CONTINUED AND STOPS AT A RECORDED MEMBER BOUNDARY; THE CLOSE.** No `src/` change, no golden, **no test changed, moved or run**, nothing under `tools/corpus/` or `tools/robust_stop/`, no behaviour change to the analysis, no fix to inference, no design; **no measurement of the analysis built, designed, scoped or run; no document archived, moved or deleted AS A FILE; NO open-items row created, flipped or discarded; NO screened document edited.** Dispatch **`cc_instruction_step_zero_exclusion_and_pass_continuation.md`**, Task 2 and Task 3, under Ruling 1 of `cowork_rulings_2026_08_22_dispatch_order_sitting.md`. Per the OI-222 remedy this entry is a **POINTER** — the report is `cc_report_step_zero_return_executed.md`, the full close is THE STEP-ZERO RETURN RULINGS EXECUTED, AND THE PASS CONTINUED section of `cowork_away_returns.md`, and nothing is restated here (**D-431**). **(1) THE REMAINDER WAS DERIVED FRESH AT THE ARTIFACT** after the exclusion, never carried from any report's account of it, and worked in the artifact's own order. **(2) THE METHOD IS THE INHERITED ONE AND NO OTHER** — the four classes in their DECLARED ORDER with POSITIVELY CODE-INFLUENCED applied FIRST, so that a ratified act cannot launder a correction made under it; where that class was tested and declined, every verdict says so and gives the reason. **(3) SIX VERDICTS OF THIS BATCH'S OWN EARLIER RUNS WERE CORRECTED AGAINST THE INHERITED CALIBRATION**, measured at the step-zero batch's authored block rather than argued, and the correction is reported rather than quietly applied. **(4) THE STOP IS RECORDED IN THE STANDING FORM** — which members were completed, that the remainder is UNTOUCHED rather than partly worked, and that nothing is left half-edited.)*

*2026-08-22 (CC — **★ `STATUS.md` LEAVES THE SPECIFICATION DOCUMENT SET BY AN AUTHORED EXCLUSION; THE THREE ARTIFACTS ARE RE-DERIVED; THE COVERAGE GAP GAINS ITS DECLARED THIRD POLLUTION VALUE.** No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no behaviour change to the analysis, no design. Same dispatch, Task 1, executing Rulings 1(a), 1(c) and 2(b) of `cowork_rulings_2026_08_22_step_zero_return_sitting.md`. Per the OI-222 remedy this entry is a **POINTER** — the artifacts are their own record and nothing of them is restated here (**D-431**). **(1) THE EXCLUSION IS AN INPUT TO THE DERIVATION, NEVER A HAND EDIT TO ITS OUTPUT** (the D-677 shape), carrying its finding, its date and its reason, with a STOP on an exclusion naming an ungraded or non-admitted target or lacking any of the three — so `--check` re-derives the set with the exclusion applied. **(2) IT IS APPLIED TO MEMBERSHIP AND TO NOTHING ELSE**: the grade stays, the quoted naming stays, the seed reconciliation is untouched, and the derivation's own NAME states the exclusion (**D-661**). **(3) THE DECLARED THIRD VALUE IS DERIVED, NEVER TYPED** — the period's bounds from the candidate enumeration's own artifact and the authoring commit and date from git at those bounds — with a STOP where a period commit touches the file and a STOP where a gap member is one the enumeration DOES carry, for which the ruling declares nothing. **(4) THE EXISTING VERDICTS' DIGEST IS IDENTICAL BEFORE AND AFTER**, and every field above the widened block is byte-identical.)*

*2026-08-22 (CC — **★ THE 2026-08-22 RECORD LANDS IN GIT AND IS PUSHED.** No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no behaviour change to the analysis, no design. Same dispatch, Task 0. Per the OI-222 remedy this entry is a **POINTER** — the landed files are their own record and nothing of them is restated here (**D-431**). **(1) THE DECLARED START STATE MATCHED EXACTLY**, both reds named with their causes and the guard registry's own drift declared rather than left. **(2) A1 HELD**, checked FIRST and entirely at content-addressed objects, because the bare working-tree forms are denied by the armed guard. **(3) THE PREDICTION HELD ON BOTH ROUTES** — the ruling-record count rose by the predicted amount with the two predicted names, and the second route added nothing, exactly as predicted before the run. **(4) NO MEMBER, ROUTE, DOCUMENT, PIN CONSTANT, STATE OR COUNT MOVED** beyond those two additions, and the membership check passes at the resulting tree.)*

*★ ARCHIVED 2026-08-17 — every dated batch entry EXCEPT the latest batch's was moved verbatim to `STATUS_ARCHIVE.md` by `cc_instruction_preparation_sixth.md` Task 1, under Ruling 4 of `cowork_rulings_2026_08_17_governing_surface_split.md`: **an entry is SUPERSEDED the moment a later batch's close exists, and this file keeps only the latest batch's entries.** The bound is maintained forward at every batch close, in the same act that writes its own entries — an instance of the continuous-pruning rule (§5(D) of `cowork_rulings_2026_08_16_preparation_return.md`). ONE pointer stands for the whole cleared block rather than one per entry, because this file's own banner already names the archive; the per-entry reconciliation is re-derived by `tools/audit/gen_governing_surface_split.py --check --pair STATUS.md`.*

*★ ARCHIVED 2026-08-17, THE SECOND ACT — the dated entries that PREDATE (or paraphrase) the OI-222 pointer convention fell to `operative-rule-text` in the pinned measurement and stayed when the block above was cleared. They were READ one by one and moved verbatim to `STATUS_ARCHIVE.md` by `cc_instruction_preparation_seventh.md` Task 1, under Ruling 3 of `cowork_rulings_2026_08_17_sixth_return.md`, which brings them under Ruling 4's own line. **One candidate did NOT move and is flagged:** the navigational paragraph at the foot of this file is not a completed batch's dated record but a live pointer to the archive and to the gate policy, so the read-before-move safeguard left it at site. The per-entry reconciliation is re-derived by `tools/audit/gen_status_residue_move.py --check`.*

*(All older dated entries — the seams P0–P6 and the whole joint-estimator arc, 2026-07-27 back to 2026-04 — and every historical section (old current-state summaries, roadmaps, iteration and deduplication records) are in `STATUS_ARCHIVE.md`, moved verbatim in two sweeps (the 2026-07-18 doc split + the 2026-07-27 OI-205(a) sweep, byte-reconciled by `tools/status_archive_sweep_check.py`). The ratified robust-unit baselines, the hard stop, and the gate policy live in `CLAUDE.md` §"Gate threshold and preset policy" block (A). Build/test commands: `BUILD_AND_TEST.md`.)*
