# MuseScore Arranger — Implementation Status

> **Living document.** Claude Code reads this at the start of every session. Update this as the
> last act when anything changes. For stable architectural decisions, see ARCHITECTURE.md.
> Historical entries live in `STATUS_ARCHIVE.md` (reference-only, NOT part of the session-start
> read) — a superseded entry moves there instead of accumulating here.

*Last updated: 2026-09-02 (CC — **★★ THE L0/L1 OUTGOING POPULATION IS DERIVED AND PUBLISHED, AND THE COMPARISON DID NOT OPEN: THE DISPATCH'S OWN RULED SIZE STOP FIRED AT TASK 1 AND TASK 2 WAS NOT ENTERED.** The population is `tools/audit/l0_l1_outgoing_population.json`, written by `tools/audit/gen_l0_l1_outgoing_population.py` under Ruling 32 — the eleven members the ruling names, each **proven present at its path** and the three `ARCHITECTURE.md` sections **located by heading text and never by line number** (**D-307**), plus every file admitted by the term search over the three ruled inventory classes, whose membership is read from the inventory's own `every_member` and never from a directory listing. ★ **THE TWO SEARCH TIERS ARE DECLARED AUTHORED ON THE ARTIFACT ITSELF**, not merely in the tool, and **the reach is STATED AS A LOWER BOUND WITH NO INDEPENDENTLY-KNOWN POPULATION TO RECONCILE AGAINST — declared UNMEASURED, never a census** (**D-673** and the recognizer clause). ★ **THE SIZE STOP IS THE BATCH'S OWN THRESHOLD, NOT A FIGURE ABOUT THE CORPUS**, and whether the term list narrows or the comparison splits across dispatches is the user's ruling, not this batch's. **NO DISPOSITION WAS PROPOSED OR APPLIED, NO STATEMENT WAS GRADED, NO OUTGOING TEXT WAS EDITED, AND NO READING FILE EXISTS** — `ratification_surfaces/cowork_comparison_l0_l1_reading.md` was NOT created, because a partly worked population is exactly what the ruled stop exists to prevent. ★ **THREE THINGS THE DISPATCH PREDICTED ARE REFUTED AND ALL THREE ARE REPORTED RATHER THAN ABSORBED:** its declared start state was read from a `guard_state.json` object written **three commits before the tip**, so the tree carries more standing reds than the eight it names, each cause established at its own tool; assumption A1 enumerated two tracked modifications where the tree carries three, the third being the writing side's own amendment of the eighty-eighth handoff entry; and **assumption A4 is refuted by measurement — the new script carries a `--check` mode, so leaving it unenrolled HALTS the guard runner rather than leaving the population unmoved.** **The dispatch forbids enrolment twice, so it was not enrolled and the halt stands, unrepaired and attributed.** Dispatch **`cc_instruction_comparison_l0_l1_2026_09_02.md`**. Per the OI-222 pointer convention this entry is a **POINTER** — every count, every term, every hit and the whole ordered population are the artifact, and the report carries the causes — and no figure is restated here (**D-431**).)*

*2026-09-02 (CC — Same dispatch, Task 0. **★★ THE FIRST BLIND DERIVATION IS IN GIT, AND THAT IT LANDED UNTOUCHED IS PROVEN RATHER THAN ASSERTED:** the committed blob was read back at the object by explicit hash and compared byte-for-byte against the file on disk, and its blob hash and byte count are recorded in the report. `cowork_blind_derivation_l0_l1_2026_08_31.md` had existed in exactly one place, untracked, on one machine; the detail-specification phase has been open with **zero** derived specifications since it opened, and that is no longer true. Landed with it, in one commit of six paths and no others: the twice-amended blind brief, the sitting record carrying Rulings 26–32, the running order, the eighty-ninth handoff entry and this batch's own dispatch. ★ **THE PUSH CARRIED THE THREE COMMITS THAT WERE SITTING UNPUSHED**, and `origin/master` was verified equal to the new tip at the object rather than from the push's own output. ★ **THE EVIDENCE-PIN MEMBERSHIP WAS REGENERATED AND MEASURED BEFORE BEING ACCEPTED**, against the committed blob at the tip and against all three declared routes: it came back **byte-identical**, so it was **NOT committed** — the ordered path list says *only if it moved*, and it did not. **No `src/` change, no golden, no test changed, moved or run, no build, no measurement of the analysis, nothing under `tools/corpus/` or `tools/robust_stop/`, no open-items row created, flipped or discarded, no decisions-register entry and no `D-NNN` allocated, and no edit to the derivation, the brief, the pack, any governing document or any outgoing text.** The dispatch was pinned to a blob at Task 0 under standing clause P-2 and proven unmoved before staging; the working-tree-first read is declared as that clause's fallback route. Per the OI-222 pointer convention this entry is a **POINTER** and no figure is restated here (**D-431**).)*

*★ ARCHIVED 2026-08-17 — every dated batch entry EXCEPT the latest batch's was moved verbatim to `STATUS_ARCHIVE.md` by `cc_instruction_preparation_sixth.md` Task 1, under Ruling 4 of `cowork_rulings_2026_08_17_governing_surface_split.md`: **an entry is SUPERSEDED the moment a later batch's close exists, and this file keeps only the latest batch's entries.** The bound is maintained forward at every batch close, in the same act that writes its own entries — an instance of the continuous-pruning rule (§5(D) of `cowork_rulings_2026_08_16_preparation_return.md`). ONE pointer stands for the whole cleared block rather than one per entry, because this file's own banner already names the archive; the per-entry reconciliation is re-derived by `tools/audit/gen_governing_surface_split.py --check --pair STATUS.md`.*

*★ ARCHIVED 2026-08-17, THE SECOND ACT — the dated entries that PREDATE (or paraphrase) the OI-222 pointer convention fell to `operative-rule-text` in the pinned measurement and stayed when the block above was cleared. They were READ one by one and moved verbatim to `STATUS_ARCHIVE.md` by `cc_instruction_preparation_seventh.md` Task 1, under Ruling 3 of `cowork_rulings_2026_08_17_sixth_return.md`, which brings them under Ruling 4's own line. **One candidate did NOT move and is flagged:** the navigational paragraph at the foot of this file is not a completed batch's dated record but a live pointer to the archive and to the gate policy, so the read-before-move safeguard left it at site. The per-entry reconciliation is re-derived by `tools/audit/gen_status_residue_move.py --check`.*

*(All older dated entries — the seams P0–P6 and the whole joint-estimator arc, 2026-07-27 back to 2026-04 — and every historical section (old current-state summaries, roadmaps, iteration and deduplication records) are in `STATUS_ARCHIVE.md`, moved verbatim in two sweeps (the 2026-07-18 doc split + the 2026-07-27 OI-205(a) sweep, byte-reconciled by `tools/status_archive_sweep_check.py`). The ratified robust-unit baselines, the hard stop, and the gate policy live in `CLAUDE.md` §"Gate threshold and preset policy" block (A). Build/test commands: `BUILD_AND_TEST.md`.)*
