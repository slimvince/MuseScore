# MuseScore Arranger — Implementation Status

> **Living document.** Claude Code reads this at the start of every session. Update this as the
> last act when anything changes. For stable architectural decisions, see ARCHITECTURE.md.
> Historical entries live in `STATUS_ARCHIVE.md` (reference-only, NOT part of the session-start
> read) — a superseded entry moves there instead of accumulating here.

*Last updated: 2026-08-17 (CC — **★ THE PREPARATION PHASE'S SIXTH BATCH CLOSES WITH THE RULED SPLIT EXECUTED — AND WITH THE SAFEGUARD'S VERDICT AS ITS LARGEST FINDING.** No `src/` change, no golden, **no test changed, moved or run**, nothing under `tools/corpus/` or `tools/robust_stop/`, no behaviour change to the analysis, no fix to inference, no design; **no measurement of the analysis built, designed, scoped or run; no document archived, moved or deleted AS A FILE; no decisions-register entry retired or revived; one open-items row acted on — [[OI-370]]'s ordered flip and no other**. Dispatch **`cc_instruction_preparation_sixth.md`**, Task 2, closing the batch. Per the OI-222 remedy this entry is a **POINTER** — the report is `cc_report_preparation_sixth.md`, the full close is THE PREPARATION SIXTH BATCH section of `cowork_away_returns.md`, and nothing is restated here (**D-431**). **(1) BOTH TASKS ARE PERFORMED AND THE RULED ACT IS COMPLETE** — 281 spans archived verbatim, the five mandatory reads down by half. **(2) ★ THE A4 SAFEGUARD FIRED FOR REAL AND IS THE FINDING** — 17 archive-class spans do not read as their class says and were left at site, among them the decisions register's rules (a)–(n), three standing principles, seven OPEN index rows including the gating [[OI-179]], and all three of `DECISIONS.md`'s; the measurement's error runs entirely in the ARCHIVE direction. **(3) TWO REDS DO NOT CLEAR AND NEITHER WAS ADJUSTED AWAY** — one is a standing check whose quoted figure lives in a row this act archived, whose remedy is a tool edit clause 5 forbids; the other is an artifact that cannot be regenerated inside the act that moves it. **(4) ASSUMPTION A2 IS FALSIFIED IN TWO WAYS, BOTH DECLARED.** **(5) SEVEN NEW FINDINGS RIDE TO THE PHASE'S RETROSPECTIVE**, none rowed — the bars stand. **(6) THE RULED INTERIM READING SCOPE HAS EXPIRED** by its own terms, the executed split having made the boundary physical.)*

*2026-08-17 (CC — **★ THE RULED GOVERNING-SURFACE SPLIT IS EXECUTED, COMPLETE: 281 SPANS ARCHIVED VERBATIM, 17 LEFT AT SITE BECAUSE THE SAFEGUARD READ THEM.** No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or `tools/robust_stop/`, no behaviour change to the analysis, no design; **no document archived, moved or deleted AS A FILE**. Same dispatch, Task 1, executing all four rulings of `cowork_rulings_2026_08_17_governing_surface_split.md` together. Per the OI-222 remedy this entry is a **POINTER** — every per-span verdict, the per-file arithmetic and the reconciliation are at `tools/audit/governing_surface_split_application.json`, the reasoning at `cc_report_preparation_sixth.md` §2, and nothing is restated here (**D-431**). **(1) NOTHING IS LOST AND IT IS PROVEN RATHER THAN ASSERTED** — moved + kept accounts for each parent's pre-act committed blob TO THE CHARACTER, every archived span byte-present in its companion exactly once and absent from its parent. **(2) THE FOUR COMPANIONS ARE CREATED ON THE ESTABLISHED PATTERN** and `STATUS_ARCHIVE.md` continues as this file's home. **(3) ★ THE PINNED MEASUREMENT OVER-CLASSES, ENTIRELY IN THE ARCHIVE DIRECTION**, which is what the ruled doubt default exists to prevent and what Ruling 1's read-before-move caught. **(4) THE 73 DRIFTED HOME ANCHORS ARE RE-AIMED PER CITATION** — verbatim quotes found at their cited home 512/512, cited line numbers correct 506/506. **(5) EVERY REGENERATED ARTIFACT'S DIFFERENCE IS ENUMERATED AND CLASSED INSIDE THE RULED BOUND**, none outside it — and one of them is a finding: archiving a span removes namings from the governing record, so retirement candidacy moves with them. **(6) [[OI-370]] FLIPS AND ITS CONDITION WAS TESTED** — after the act the file tools read this file without refusing.)*

*2026-08-17 (CC — **★ THE GOVERNING-SURFACE SPLIT SITTING'S FOUR RULINGS, THE TWENTY-SECOND HANDOFF BLOCK AND THE DISPATCH THAT EXECUTES THEM ENTER GIT.** No `src/` change, no golden, no test changed, nothing under `tools/corpus/` or `tools/robust_stop/`, no behaviour change to the analysis, no design; **no measurement built, designed, scoped or run**. Same dispatch, Task 0. Per the OI-222 remedy this entry is a **POINTER** — the landed files are their own record and nothing of them is restated here (**D-431**). **(1) UNTIL THIS COMMIT THE WHOLE OF THE 2026-08-17 SITTING LIVED ON DISK UNTRACKED** — the per-class fates, the doubt-defaulted share accepted as it stands, the per-file archive companions, and this file's superseded-entry rule, all four ruled Alternative A. **(2) ASSUMPTION A1 HELD AT THE SHAPE ITS OWN SENTENCE DESCRIBES** — exactly ONE tracked modification in the whole working tree, ONE contiguous changed passage carrying both named parts, taken blob against blob by explicit hash. **(3) EXACTLY THE THREE PATHS NAMED AND NO FOURTH, STAGED PLAINLY**, verified at the index before the commit and at the object after it.)*

*★ ARCHIVED 2026-08-17 — every dated batch entry EXCEPT the latest batch's was moved verbatim to `STATUS_ARCHIVE.md` by `cc_instruction_preparation_sixth.md` Task 1, under Ruling 4 of `cowork_rulings_2026_08_17_governing_surface_split.md`: **an entry is SUPERSEDED the moment a later batch's close exists, and this file keeps only the latest batch's entries.** The bound is maintained forward at every batch close, in the same act that writes its own entries — an instance of the continuous-pruning rule (§5(D) of `cowork_rulings_2026_08_16_preparation_return.md`). ONE pointer stands for the whole cleared block rather than one per entry, because this file's own banner already names the archive; the per-entry reconciliation is re-derived by `tools/audit/gen_governing_surface_split.py --check --pair STATUS.md`.*

*★ ARCHIVED 2026-08-17, THE SECOND ACT — the dated entries that PREDATE (or paraphrase) the OI-222 pointer convention fell to `operative-rule-text` in the pinned measurement and stayed when the block above was cleared. They were READ one by one and moved verbatim to `STATUS_ARCHIVE.md` by `cc_instruction_preparation_seventh.md` Task 1, under Ruling 3 of `cowork_rulings_2026_08_17_sixth_return.md`, which brings them under Ruling 4's own line. **One candidate did NOT move and is flagged:** the navigational paragraph at the foot of this file is not a completed batch's dated record but a live pointer to the archive and to the gate policy, so the read-before-move safeguard left it at site. The per-entry reconciliation is re-derived by `tools/audit/gen_status_residue_move.py --check`.*

*(All older dated entries — the seams P0–P6 and the whole joint-estimator arc, 2026-07-27 back to 2026-04 — and every historical section (old current-state summaries, roadmaps, iteration and deduplication records) are in `STATUS_ARCHIVE.md`, moved verbatim in two sweeps (the 2026-07-18 doc split + the 2026-07-27 OI-205(a) sweep, byte-reconciled by `tools/status_archive_sweep_check.py`). The ratified robust-unit baselines, the hard stop, and the gate policy live in `CLAUDE.md` §"Gate threshold and preset policy" block (A). Build/test commands: `BUILD_AND_TEST.md`.)*
