# MuseScore Arranger — Implementation Status

> **Living document.** Claude Code reads this at the start of every session. Update this as the
> last act when anything changes. For stable architectural decisions, see ARCHITECTURE.md.
> Historical entries live in `STATUS_ARCHIVE.md` (reference-only, NOT part of the session-start
> read) — a superseded entry moves there instead of accumulating here.

*Last updated: 2026-08-26 (CC — **★ THE THREE RULED CLAUSE AMENDMENTS ARE LANDED AT THEIR HOME FILE, `CLAUDE.md`, AND THE FOUR ORDERED REGISTER ENTRIES ARE WRITTEN — at the principles, the Class A scope clause, the Class B object fence and the extension of the uncertainty principle from figures to results; and in the decisions register, the unregistered residue of the sharpened decision-surface rule together with the v1-sufficiency sitting's two verdicts and its phase-status change.** ★ **`CLAUDE.md` DIFFERS AT EXACTLY THREE SITES AND EVERY CHANGE IS AN ADDITION** — no existing sentence is reworded, no principle renumbered, and every register anchor the additions displaced was re-aimed by the tool that exists for it rather than by hand. ★ **THE FORWARD-BOUND TOOL IS RE-AIMED AND ITS MISSING ROW BACKFILLED**, so the previous batch's hand-performed move is no longer absent from the tool's own record of aimings, and the row says in its own field that no tool performed it. **TWO DEPARTURES ARE DECLARED AND CARRIED BY THE REPORT RATHER THAN RESOLVED HERE:** the decision-surface residue entry is homed at the ruling record that states it and not at `CLAUDE.md`, because the establishment check requires an entry's quoted text to be found in its home file and the dispatch's fence forbade the write that a `CLAUDE.md` home would need — the homing is OWED as a separate act; and NO register-level supersession was entered for either superseded item, because neither has a register entry to carry the mark, which is left as a question for the user. No `src/` change, no golden, **no test changed, moved or run**, nothing under `tools/corpus/` or `tools/robust_stop/`, no behaviour change to the analysis, no fix to inference, no design; **no measurement of the analysis built, designed, scoped or run; no derivation and NO COMPARISON; the blind derivation outputs NOT opened at all; no edit to `ARCHITECTURE.md`, to the boot-pack generator, to either pack directory or to the manifest; NO open-items row created, flipped or discarded; NO finding number allocated; and NO ADMISSION TO THE EMPIRICAL FINDINGS LEDGER, WHICH THIS BATCH NEITHER BUILT NOR TOUCHED.** ONE tool source was edited — the forward-bound tool, under the carve-out ruled for it by name, and nothing else under `tools/` ending `.py`. Dispatch **`cc_instruction_amendment_landing.md`**, executing the five rulings of `cowork_rulings_2026_08_26_amendment_landing_sitting.md`. Per the OI-222 remedy this entry is a **POINTER** — the report is `cc_report_amendment_landing.md` — and no count, no identity and no rendered value is restated here (**D-431**).)*

*★ ARCHIVED 2026-08-17 — every dated batch entry EXCEPT the latest batch's was moved verbatim to `STATUS_ARCHIVE.md` by `cc_instruction_preparation_sixth.md` Task 1, under Ruling 4 of `cowork_rulings_2026_08_17_governing_surface_split.md`: **an entry is SUPERSEDED the moment a later batch's close exists, and this file keeps only the latest batch's entries.** The bound is maintained forward at every batch close, in the same act that writes its own entries — an instance of the continuous-pruning rule (§5(D) of `cowork_rulings_2026_08_16_preparation_return.md`). ONE pointer stands for the whole cleared block rather than one per entry, because this file's own banner already names the archive; the per-entry reconciliation is re-derived by `tools/audit/gen_governing_surface_split.py --check --pair STATUS.md`.*

*★ ARCHIVED 2026-08-17, THE SECOND ACT — the dated entries that PREDATE (or paraphrase) the OI-222 pointer convention fell to `operative-rule-text` in the pinned measurement and stayed when the block above was cleared. They were READ one by one and moved verbatim to `STATUS_ARCHIVE.md` by `cc_instruction_preparation_seventh.md` Task 1, under Ruling 3 of `cowork_rulings_2026_08_17_sixth_return.md`, which brings them under Ruling 4's own line. **One candidate did NOT move and is flagged:** the navigational paragraph at the foot of this file is not a completed batch's dated record but a live pointer to the archive and to the gate policy, so the read-before-move safeguard left it at site. The per-entry reconciliation is re-derived by `tools/audit/gen_status_residue_move.py --check`.*

*(All older dated entries — the seams P0–P6 and the whole joint-estimator arc, 2026-07-27 back to 2026-04 — and every historical section (old current-state summaries, roadmaps, iteration and deduplication records) are in `STATUS_ARCHIVE.md`, moved verbatim in two sweeps (the 2026-07-18 doc split + the 2026-07-27 OI-205(a) sweep, byte-reconciled by `tools/status_archive_sweep_check.py`). The ratified robust-unit baselines, the hard stop, and the gate policy live in `CLAUDE.md` §"Gate threshold and preset policy" block (A). Build/test commands: `BUILD_AND_TEST.md`.)*
