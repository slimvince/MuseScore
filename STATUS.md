# MuseScore Arranger — Implementation Status

> **Living document.** Claude Code reads this at the start of every session. Update this as the
> last act when anything changes. For stable architectural decisions, see ARCHITECTURE.md.
> Historical entries live in `STATUS_ARCHIVE.md` (reference-only, NOT part of the session-start
> read) — a superseded entry moves there instead of accumulating here.

*Last updated: 2026-09-07 (CC — `cc_instruction_read_size_repair_and_rows_2026_09_07.md`, Task 4, the close. **★★ THE FORWARD CLAUSE IS PERFORMED ON THIS BATCH ITSELF, AS AN ORDINARY MOVE** — `tools/audit/gen_status_batch_bound.py` re-aimed at all six authored fields, the then-previous batch being the boot-membership batch, **this batch's own entries written FIRST and `--apply` run second**, which is the order the declared prefix adjustment makes mechanical rather than preferred; every previous aiming is appended to rather than replaced (#12). ★ **THE GUARD SET AT THE CLOSING TREE WAS RUN AND READ RATHER THAN INFERRED**, and every departure from the measured start state is traced to this batch's own ordered acts. ★ **THE READ-SIZE MEASUREMENT WAS REGENERATED IN THE END-STATE COMMIT** because `STATUS.md` is a member of the read it measures and this batch moves it; no measured value was adjusted to reach a number. ★ **THE TWO DECISIONS-SIDE CHECKS THE PRECEDING BATCH REDDENED STAY RED AND THAT IS NOW ROWED RATHER THAN ONLY SURFACED** — [[OI-383]] — and they were NOT regenerated here, on the same ground the preceding close gave: one of them writes the decisions register's source of record, which this batch may not touch. ★ **THREE DECLARED READINGS ARE REPORTED RATHER THAN ABSORBED** — the resolved token on [[OI-382]], the two generated artifacts that ride with a new row, and one bound left standing on the falsification test. **This batch decided nothing, ruled nothing, opened no paper, ran no sweep, moved no verdict, lifted no gate, edited no governing document other than `OPEN_ITEMS.md` and `STATUS.md`, and did not touch `CLAUDE.md`.** Per the OI-222 pointer convention this entry is a **POINTER** — the whole arithmetic is `cc_report_read_size_repair_and_rows_2026_09_07.md` — and no figure is restated here (**D-431**).)*

*2026-09-07 (CC — Same dispatch, Task 3. **★★ THE THREE OWED ROWS ARE OPEN: [[OI-381]], [[OI-382]], [[OI-383]], each with its index row AND its detail file in the same commit (register rule (c)) and each opening with one canonical token (rule (f)).** ★ **THE ROW IDENTITIES WERE DERIVED AT THE INDEX AND NOT TAKEN FROM THE DISPATCH** — the highest identity `OPEN_ITEMS.md` carries is OI-380 and `open_items/` holds nothing above it, which is what the dispatch said to expect, so no STOP. ★ **[[OI-381]] IS CREATED AND FLIPPED RESOLVED IN THE SAME BATCH**, because rule (c) requires the discovery rowed and rule (d) requires the resolution to flip its row, the discovery being the preceding batch's and the repair this batch's Task 1. ★ **BOTH RELAYED PREMISES WERE ESTABLISHED AT THE OBJECTS BEFORE ANY ROW WAS WRITTEN, AS ORDERED — relayed is not established.** The forward-bound prefix defect reproduces in every clause at four objects, the former unanchored test read at the `-` side of the diff that repaired it; and the `CLAUDE.md`-edit coupling was established at BOTH generators' own sources and its firing measured at this tree, the moved anchor still resolving uniquely, which is what distinguishes it from [[OI-330]]. ★ **A3's PRECONDITION WAS RUN AND ITS ANSWER IS RECORDED EITHER WAY**: all six rows mentioning either generator were read and none covers the finding, the per-row ground standing in the detail file. **No apparatus declaration and no gating verdict is hand-added to any of the three** (**D-438**), no remedy is proposed for [[OI-382]] or [[OI-383]], and no finding number is allocated. Per the OI-222 pointer convention this entry is a **POINTER** and no figure is restated here (**D-431**).)*

*2026-09-07 (CC — Same dispatch, Task 2. **★★ THE REPAIRED MEASUREMENT IS POSITIVELY ESTABLISHED (#19) ON FOUR CHECKS, NOT MERELY RUN.** The overstatement is MEASURED rather than asserted — both values computed at the SAME tree, so their difference is the overstatement itself and not an estimate; a reproduce-check ran the derivation three times over, the artifact written once and two further independent derivations each compared byte-for-byte against it by `--check`, both passing; the reconciliation against the membership block runs in BOTH directions on every run, none in both and none in neither, and a mismatch in either direction is a STOP rather than a reported field; and a falsification test runs on every run, stopping the tool if the six spans ever cost at least as much as the whole file. ★ **ONE BOUND IS DECLARED RATHER THAN LEFT UNSTATED (#24)**: the falsification test RUNS and did not fire, and its FIRING was not exercised on a synthetic input, because doing so needs either an edit to `CLAUDE.md` or a new probe file and this batch's bars admit neither. What is established is that it runs and passes, not that it has been seen to fire. **No measured value was adjusted to reach a number.** Per the OI-222 pointer convention this entry is a **POINTER** and no figure is restated here (**D-431**).)*

*2026-09-07 (CC — Same dispatch, Task 1. **★★ THE SESSION-START READ MEASUREMENT STOPS COUNTING THE EIGHT CONDITIONAL SPANS OF `CLAUDE.md`.** It measured `len(CLAUDE.md)` — the whole file — as an ordinary session's read, which after the 2026-09-07 ruling counts eight CONDITIONAL spans into a read no ordinary session takes, and its own `--check` passed while it did, the fault sitting in the DEFINITION of what is measured rather than in a drift (#19). ★ **NOTHING IS AUTHORED: every span name, every heading and every anchor is PARSED out of `CLAUDE.md`'s own membership block**, on the idiom `rule_a_pointer()` already used and for the reason that function's own comment gives — a later amendment of the membership moves the measurement without anybody editing the tool. **Every span is named by its HEADING and never by a line number** (**D-307**), each found exactly once in heading form or the run STOPs; the two that are not headings are located by the anchors the block itself quotes, sought OUTSIDE the block, because the block quotes the anchors it names spans by and the naming is not the thing named. ★ **THE EIGHT CONDITIONAL SPANS ARE PUBLISHED BESIDE THE READ AND NEVER SUMMED INTO IT**, each with its own condition, and where one overlaps a session-start span the shared characters are published so an overlap is visible rather than silent. ★ **THE MEMBER ROW'S CLAUSE IS NOW THE REAL ONE**, quoted out of the block rather than retyped — a retyped sentence would be one more transcription site in a file that already carries [[OI-377]]'s three. ★ **THE TWO REGIMES ARE STATED RATHER THAN INFERRED**, every movement row naming the regime on BOTH sides, and **both earlier commits were MEASURED into the whole-file regime rather than inferred from how the tool describes them** — the check the dispatch ordered because the writing side had declared the point inferred. ★ **[[OI-377]] IS NOT REPAIRED AND ITS THREE SITES WERE VERIFIED UNTOUCHED** — that row reserves its own repair for its own surface. ★ **THE SELF-CHECK FOUND AND CORRECTED A RESERVED-WORD BREACH IN THIS BATCH'S OWN NEW TEXT** before the work was reported. Per the OI-222 pointer convention this entry is a **POINTER** and no figure is restated here (**D-431**).)*

*2026-09-07 (CC — Same dispatch, Task 0. **★ A1 WAS CHECKED OVER THE WHOLE TRACKED POPULATION** with the sanctioned enumeration tool and holds exactly as declared: **every changed-path record untracked, ZERO tracked modifications**, with the dispatch present among them. ★ **A2 WAS MEASURED AND NOT RELAYED**, and nothing was adjusted to reach a number: the full guard set matching the relayed start state at every count, and the failing set matching **member for member** — established not by a second comparison but by the run's own artifact re-deriving BYTE-IDENTICALLY to the committed one, which the enumeration after the run confirms by reporting no tracked modification. ★ **NO ENCODING DEPARTURE THIS TIME**: every run of this batch was made with the interpreter's output encoding aligned, which is the preceding batch's own established remedy. ★ **THE HANDOFF ENTRY NEEDED LANDING AND GOT IT** — this line's hundred-and-forty-seventh entry appeared untracked DURING Task 0, between two enumerations, and was committed with the dispatch as ordered; a second file from the same sitting was left untracked, being neither ordered nor within this batch's file list. **Registered expectation E0 GRADED PASS.**)*

*2026-09-02 (CC — **★★ THE L0/L1 COMPARISON IS OPEN AND SEVEN OF ITS TWENTY-NINE DOCUMENTS ARE TABULATED; THE READING FILE EXISTS.** `ratification_surfaces/cowork_comparison_l0_l1_reading.md` places **every statement of positions 1 to 7** — the two `ARCHITECTURE.md` sections and all five root design documents, which is the whole of Ruling 32's items 1 and 2 — under exactly one of the phase definition's five dispositions, with the current-text axis carried beside it. ★ **EVERY DISPOSITION IS A PROPOSAL AND NOTHING IS APPLIED**: no outgoing text is edited, the derivation is untouched, and the file states in terms that the user is asked to rule nothing in it. ★ **THE STOP IS AT A MEMBER BOUNDARY AND THE REMAINDER IS UNTOUCHED RATHER THAN PARTLY WORKED** (**D-672**): positions 8 to 29 have not been read for tabulation, quoted, counted or dispositioned, the file's own §0 says which are done and where the next dispatch resumes, and the three sections that can only be written once the population is complete are marked NOT YET WRITTEN rather than left to look empty. ★ **THE INDEPENDENCE RECORD IS RELAYED WHOLE, WITH NO VERDICT WORD ATTACHED** — and the two places where an outgoing statement of this comparison meets a passage the deriving session's own stop-on-meeting record says it SAW are named at the rows, which is a fact the user reads the AGREES verdicts against. ★ **THE ARITHMETIC CLOSES AT EVERY DOCUMENT** and is stated as a sum rather than asserted. Per the OI-222 pointer convention this entry is a **POINTER** — every row, every count and every proposal is the reading file — and no figure is restated here (**D-431**).)*

*2026-09-02 (CC — Same dispatch, Tasks 0 and 1. **★★ RULING 33's CUT IS APPLIED AND THE SIZE STOP THAT ENDED THE PREVIOUS BATCH DOES NOT FIRE.** `tools/audit/gen_l0_l1_outgoing_population.py` now admits a term-search hit only where its file is a member of the ruled specification document set, read at `tools/audit/specification_document_set.json` by exact path equality and never re-derived here (#6); every other hit file is published as a listed **RESIDUE** for the mining map with its per-file hit records kept whole (#12), and the first writing's pre-cut ordering is preserved beside the cut one. ★ **THE GUARD-RUNNER STOP THE PREVIOUS BATCH LEFT STANDING IS CLEARED**: the population tool is enrolled in the authored invocation list in the act that amends it — the standing new-tool rule — and the full guard set measured **population 76, zero STOPs, and the failing set exactly the ten it inherited, none added**. ★ **THE DECLARED START STATE WAS MEASURED AND HELD EXACTLY**, this dispatch having read it from the object the tree's own last run wrote rather than from a stale one. ★ **A1 WAS CHECKED OVER THE WHOLE TRACKED POPULATION** with the sanctioned enumeration tool and holds: exactly the two named modifications, the handoff's changes all inside its topmost entry, the sitting record additions-only. ★ **A3 WAS MEASURED BEFORE BEING ACCEPTED** and came back byte-identical against the committed blob, so the membership artifact was **NOT** committed and Task 0 carries four paths, not five. **No `src/` change, no golden, no test changed, moved or run, no build, no measurement of the analysis, nothing under `tools/corpus/` or `tools/robust_stop/`, no open-items row created, flipped or discarded, no decisions-register entry and no `D-NNN` allocated, and no edit to the derivation, the brief, the pack, any outgoing text or any governing document other than the two this dispatch's own Task 3 orders.** Per the OI-222 pointer convention this entry is a **POINTER** and no figure is restated here (**D-431**).)*

*★ ARCHIVED 2026-08-17 — every dated batch entry EXCEPT the latest batch's was moved verbatim to `STATUS_ARCHIVE.md` by `cc_instruction_preparation_sixth.md` Task 1, under Ruling 4 of `cowork_rulings_2026_08_17_governing_surface_split.md`: **an entry is SUPERSEDED the moment a later batch's close exists, and this file keeps only the latest batch's entries.** The bound is maintained forward at every batch close, in the same act that writes its own entries — an instance of the continuous-pruning rule (§5(D) of `cowork_rulings_2026_08_16_preparation_return.md`). ONE pointer stands for the whole cleared block rather than one per entry, because this file's own banner already names the archive; the per-entry reconciliation is re-derived by `tools/audit/gen_governing_surface_split.py --check --pair STATUS.md`.*

*★ ARCHIVED 2026-08-17, THE SECOND ACT — the dated entries that PREDATE (or paraphrase) the OI-222 pointer convention fell to `operative-rule-text` in the pinned measurement and stayed when the block above was cleared. They were READ one by one and moved verbatim to `STATUS_ARCHIVE.md` by `cc_instruction_preparation_seventh.md` Task 1, under Ruling 3 of `cowork_rulings_2026_08_17_sixth_return.md`, which brings them under Ruling 4's own line. **One candidate did NOT move and is flagged:** the navigational paragraph at the foot of this file is not a completed batch's dated record but a live pointer to the archive and to the gate policy, so the read-before-move safeguard left it at site. The per-entry reconciliation is re-derived by `tools/audit/gen_status_residue_move.py --check`.*

*★ CLEARED 2026-09-07, THE FORWARD BOUND'S SIX LATE MOVES — a backlog of SEVEN entries, left by six consecutive batch closes that omitted their own move, was cleared by `cc_instruction_status_forward_bound_second_2026_09_07.md` Task 1 **BY THE MECHANISM AND NOT BY HAND**: six re-aimings of `tools/audit/gen_status_batch_bound.py`, oldest omission first so the archive reads in the order the moves would have happened had each close performed its own, each `--apply` reading its entries from the base commit's own git object and matching them verbatim in this file — **so no entry was retyped and none was hand-copied**. The omitting closes were those of `cc_instruction_l2_keyword_count_2026_09_04.md`, `cc_instruction_l2_criterion_write_2026_09_04.md`, `cc_instruction_l2_candidate_list_2026_09_05.md`, `cc_instruction_l2_verdict_pass_2026_09_05.md`, `cc_instruction_l2_reading_file_2026_09_05.md` and `cc_instruction_l2_ruling_writeback_2026_09_05.md`. ONE pointer stands for the whole catch-up rather than one per move, on the shape the two pointers above set. **THE STANDING BOUND IS UNCHANGED** — Ruling 4 of `cowork_rulings_2026_08_17_governing_surface_split.md`: an entry is SUPERSEDED the moment a later batch's close exists, this file keeps only the latest batch's entries, and every future close moves the then-previous batch's entries in the same act that writes its own. **Each of the six archive headers says in terms that its move was LATE**, the header choosing between two sentences on a field added to that tool in this same batch under the user's ruling of 2026-09-07, so that no header claims to have been written in the act that wrote its batch's own entries (#10); the ordinary sentence was PROVEN to render byte-identically to the constant it replaces before any move ran. **THE TWO 2026-09-02 ENTRIES ABOVE DID NOT MOVE, AND THEY WERE NOT MOVED BY HAND:** they name no dispatch, so no aiming of that tool can identify them, and neither `tools/audit/gen_status_residue_move.py` nor `tools/audit/gen_governing_surface_split.py` reaches them either — a declared standing state recorded in that tool's own list of previous aimings, and whether they ever move is a question standing with the user. **THE MECHANISM'S MISSING TRIGGER IS ROWED, NOT REPAIRED HERE** — `OPEN_ITEMS.md` OI-379: the tool is re-aimed by hand at every close and its `--check` reconciles only the aiming currently authored, so a close that never re-aims it leaves the guard green and the bound unmet. The per-move reconciliation is re-derived by `tools/audit/gen_status_batch_bound.py --check`, which reconciles the LAST aiming alone and is therefore not by itself the proof that the backlog cleared.*

*(All older dated entries — the seams P0–P6 and the whole joint-estimator arc, 2026-07-27 back to 2026-04 — and every historical section (old current-state summaries, roadmaps, iteration and deduplication records) are in `STATUS_ARCHIVE.md`, moved verbatim in two sweeps (the 2026-07-18 doc split + the 2026-07-27 OI-205(a) sweep, byte-reconciled by `tools/status_archive_sweep_check.py`). The ratified robust-unit baselines, the hard stop, and the gate policy live in `CLAUDE.md` §"Gate threshold and preset policy" block (A). Build/test commands: `BUILD_AND_TEST.md`.)*
