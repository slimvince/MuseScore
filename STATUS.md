# MuseScore Arranger — Implementation Status

> **Living document.** Claude Code reads this at the start of every session. Update this as the
> last act when anything changes. For stable architectural decisions, see ARCHITECTURE.md.
> Historical entries live in `STATUS_ARCHIVE.md` (reference-only, NOT part of the session-start
> read) — a superseded entry moves there instead of accumulating here.

*Last updated: 2026-07-27, twelfth entry (CC — **★ SEAMS PART 2 — P7: THE PARTITION CLOSE-OUT — completeness verified, docs consolidated (pre-switch state).** Dispatch `cc_instruction_notation_p7.md`. The LAST unit of the seams-2 partition — NO new behavior, NO new code path: a read-only completeness verification + a documentation consolidation, closing the partition so the switch ratification assembles on a verified-complete record. **TASK 1 — THE COMPLETENESS VERIFICATION (read-only; generated artifact `tools/notation_seams/partition_completeness.json`) — OVERALL COMPLETE / PASS, ZERO FINDINGS:** (1) **every audited consumer has its record-arm branch behind the default-OFF `useJointNotationRecord`** — the span-annotation emitter (`notationcomposingbridge.cpp:1491`), the note-seam funnel `analyzeHarmonicContextAtTick` (`:729`), the implode `populateChordTrack` (`notationimplodebridge.cpp:1416`), the tuning `applyRegionTuning` region path (`notationtuningbridge.cpp:774`); the section layer + cadence/pivot enter via `analyzeSectionFromRecord` (key-areas via the shared `groupKeyAreas`, cadence/pivot via the shared `detectCadences`/`detectPivotChords` reading the P2a-unified stored `hasAssertiveExposure`); interaction / context-menu / accessibility route through `analyzeNoteHarmonicContext[Details]` → the note-seam funnel (**NO consumer code change**) — each cited to file:line. (2) **every ratified ruling implemented/carried** — the raw-gap rule (grep-verified: `normalizedConfidence = gap.value_or(0.0)` RAW, no [0,1] remap/sigmoid/normalize anywhere on the record arm), C1 two-mode (`twoModeOf`), the D2 grading/display split + the reused `ChordSymbolFormatter`, the shared `formatNashvilleNumber`, the pedal suspension (`isPedalPoint` false + OI-194 comment), the §4.1 constants at their declared sites (`kAssertiveKeyExposureGap=1.055757` / `kTentativeKeyExposureGap=0.975911`, cited to `exposure_constants.json`; the 0.35 mode-suffix gate RETIRED/unwired; `kSameChordReannotationGap=960` re-homed), and the permanent both-ways boundary guard `inference_presentation_boundary_tests`. (3) **every seams-era register row in state** (the INDEX is the authority): OI-182 EXECUTED; OI-195/197/202/204 RESOLVED; OI-196/201/203 OPEN with carried dispositions; OI-193/194 OPEN (post-switch/increment); the living split-check PASS (index=204 / detail=204 / baseline=200 at verification — OI-205, a concurrent user-directed doc-structure row (NOT seams-era; STATUS/ARCHITECTURE size), rides this commit via the handoff → committed register 205/205, living-check re-run PASS). (4) **the flag default is OFF everywhere** (`Val(false)`, no settings UI — the switch has NOT happened) and **the three suites are GREEN at HEAD `89412b48b2`: composing 1186 / notation 61 / pipeline-snapshot 13** (build "no work to do" — this unit changes no code). **TASK 2 — DOC CONSOLIDATION (#10):** ARCHITECTURE.md — the FIVE accumulated per-unit record-path blocks consolidated into ONE coherent as-built section "THE RECORD PATH" (producer + input-scoping → section adapter → the four consumers → the presentation formatters + boundary guard → the dual-arm instrument → the #23 dual-path posture + the named switch scope), nothing historical removed; STATUS this entry; the OI-180 detail file gains a dated close-out note (the dual path's forward exit is ASSEMBLED, pending ratification — the INDEX row UNTOUCHED, the map executes post-switch). **THE P0…P7 DELIVERY LIST (hashes):** P0 `b090f6e4ab` → P1 `ad0c4069bc` → P2a `e3dac74b04` → P2b `d25cf40170` → P3a `e2c59715a5` → P-strings `58ac88079b`/`55ae163ef5`/`b2c71fb6e3` → P4 `0e0ca379b3`/`bb9e3c9811`/`599eebd45e` → note-seam `874bda0ab6`/`2d16899734`/`903125a5dc` → P6 `f4a8bfdf1d`/`c7eafb777d`/`89412b48b2` (seams part 1 producer `6e71b3ceff`; OI-197 `c8c5bb6a25`). **THE PRE-SWITCH STATE:** the partition is closed out and completeness-verified; the record path is DORMANT behind the OFF flag; batch/corpus output is A's, the in-app notation analysis is LEGACY — the declared, bounded #23 migration, awaiting the user's switch ratification. **THE SWITCH EVIDENCE is `tools/notation_seams/dualarm_classified_report.json` (P6): identical 2053 / inference-driven 6029 / presentation-rule 220 / input-scoping 0 / UNEXPLAINED 0.** The switch (§8.4) is a separate, Cowork-written, user-ratified commit: flag default flips ON; goldens refresh against the established record with the P6 classified report + the OI-178 adoption record as cited preconditions; CLAUDE.md staged-scope block + STATUS + ARCHITECTURE + the register rows + the handoff dual-path line move together. **NAMED POST-SWITCH AGENDA:** the OI-180 retirement map (the legacy `ChordAnalysisResult`/`NoteHarmonicContext`/`HarmonicRegion` path + the per-arm legacy branches retire); OI-193 (the marginal-posterior completion); OI-194 (the ornament-label / voice-independent pedal-class publication); OI-203 (the record-cache increment); OI-201 (the aug-sixth display-symbol completeness); then the OI-198 / OI-199 / OI-200 review trio. NO code / golden / `tools/robust_stop/` change — docs + the completeness artifact + the riding `cowork_handoff.md` only. ONE commit, pushed origin. NEXT (per the handoff): Cowork assembles the SWITCH decision surface (§8.4) for the user.)*

*Last updated: 2026-07-27, eleventh entry (CC — **★ SEAMS PART 2 — P6: the OI-204 producer input-scoping fix + the DUAL-ARM CLASSIFIED COMPARISON over the FULL notation output surface — the switch-ratification evidence.** Dispatch `cc_instruction_notation_p6.md`. This is the second-to-last unit of the notation-layer migration: it closes OI-204 (a pre-switch gate), then builds/runs the dual-arm classified comparison the user's switch ratification reads (§8.4). **Task 1 (commit `f4a8bfdf1d`) — OI-204 RESOLVED (a pre-switch gate):** `buildAdapterFacts`/`produceNotationRecord` gain an `excludeStaves` input-scoping parameter — an excluded staff's notated notes never enter the L1 fact surface the decode reads (INPUT selection at the fact adapter, the layer that owns its input surface #7; NOT a consumer post-filter, NOT an inference change; the empty default skips nothing → byte-identical extraction). All four record-arm seams (annotation emitter, implode, tuning, the note-seam funnel) thread the SAME chord-track exclude set their legacy arm passes (arm-for-arm input parity), so a populated chord track's own imploded notes are never fed back into a re-analysis (the self-feedback hazard). Established both ways: empty-set byte-identity (the 3 existing producer tests unchanged) + NEW `ExcludeStavesScopesTheFactSurfaceAndDecode` (nm_two_staff staff-1 notes removed from the facts; pb_chorale bass excluded changes the decode; empty-set==default); end-to-end, the note-seam record-arm tests confirm the exclusion fires on the two populated-chord-track fixtures (their reference record updated to the same exclusion via a shared helper). **Task 2 (commit `c7eafb777d`) — the dual-arm CAPTURE:** `pipeline_snapshot_tests DISABLED_DualArmClassifiedCapture` (opt-in) runs the FULL notation output surface TWICE per snapshot-corpus score over the 16-measure window — legacy (flag OFF) + record (flag ON) — serializing four surfaces: annotation (display symbols/Roman/key brackets/Nashville/pedal+cadence StaffText), implode (treble symbols/bass Roman/Nashville/voicing pitches/key+cadence text), tuning (per-note cents, fixed JI tonic-anchored, no split), noteSeam (committed reading + rendered symbol/roman/nashville + §3.3 alternatives at every measure downbeat). Deterministic — **TWO RUNS BYTE-IDENTICAL (sha256 verified)**; the flag is restored OFF by RAII per surface; default suite 13 GREEN, NO golden refresh. **Task 3+4 (this commit) — the classified diff + docs:** `tools/notation_seams/classify_dualarm.py` (#17f) aligns the two arms per surface and classifies EVERY non-identical item, emitting `dualarm_capture.json` + `dualarm_classified_report.json` + `dualarm_classified_summary.txt`. **★ THE RESULT: identical 2053 / inference-driven 6029 / presentation-rule 220 / input-scoping 0 / UNEXPLAINED 0.** Every presentation-rule entry cites its ratified rule — C1 two-mode display 86, §3.3 alternatives ordering 74, applied-chord Nashville "?" 52, §4.1 exposure gate 5, OI-194 pedal 3. **Input-scoping is 0** — the corpus carries no chord tracks, so the OI-204 fix is confirmed inert here exactly as predicted. **UNEXPLAINED is 0** — every one-sided cadence/key-run derivation resolved to mechanism (its local region sequence agrees between arms → the §4.1 exposure gate, which P2a made these derivations gate on, fired differently = presentation; the local sequence differs → inference-driven). The inference-driven majority IS the adoption's expected differences reaching the notation surface (the joint estimator commits different readings than the legacy analysis on ~23% of roots + segmentation/key/cadence differences; **0 true enharmonic-spelling-only misclassifications verified** — every differing display symbol reflects a genuine root/quality/inversion/extension/key move). Notable cited examples for the user's reading: bach_chorale_003@0 legacy E/V vs record Am/i; bach_chorale_001@16320 legacy Bm7/iii7 vs record G/B/#VII6. **SUITES: composing 1186 / notation 61 / pipeline-snapshot 13 GREEN** (Task 1); NO inference change, no legacy-analysis call on the record path, `tools/robust_stop/` untouched. Doc sync: ARCHITECTURE.md (the input-scoping parameter + the dual-arm instrument as-built), `tools/notation_seams/README.md` (the P6 section). Reuse-vs-new: the capture reuses the existing snapshot machinery + `chordTrackExcludeStaves`; the classifier is stdlib-only new. NO SURPRISE. Commits pushed origin. NEXT (per the handoff): P7 (doc-sync/close of the partition) → the user's SWITCH ratification, with this classified report as the §8.4 evidence.)*

*Last updated: 2026-07-27, tenth entry (CC — **★ SEAMS PART 2 — THE MERGED NOTE-SEAM RE-PLUMB (status bar + harmony write + right-click menu on `noteView`) + the split-check LIVING mode (OI-202).** Dispatch `cc_instruction_notation_noteseam.md`. The ratified merged note-seam unit (the P3 re-partition: the status-bar/note-context producer + the old P5 consumers as ONE unit, resolving the `NoteHarmonicContext`↔record carriage ONCE). **Task 1 (commit `874bda0ab6`) — the split-check LIVING mode + OI-202:** `tools/open_items_split_check.py` gains a default LIVING mode (`open_items/register_check.json`) valid on a tree that grows after the split — (a) INDEX↔detail-file bijection (no dupes/orphans), (b) NO detail file carries a status of its own (header sentinel present + no `| OPEN`/`| ✅ RESOLVED`-style status cell outside the one verbatim historical row; the two-place-status drift killer), (c) the ORIGINAL 200 baseline items stay BYTE-VERBATIM vs `cb246a7580` (historical guarantee unweakened), post-baseline items structural-only. The historical split reconciliation stays invocable under `--split-baseline` (writes `split_reconciliation.json`, unchanged); the committed `split_reconciliation.json` left UNTOUCHED. Making living the default also fixes the coupled hazard the P4-verification finding named (a bare run false-failed on OI-201 AND clobbered the frozen artifact). Console output made encoding-safe (OI-137 class: a ✅ in a violation string no longer crashes cp1252 stdout). Established both ways: living OVERALL PASS (index=202/detail=202/baseline=200/post-baseline={OI-201,OI-202}); a negative control (orphan + stray status cell) FAILs reporting both. **OI-202** created AND flipped ✅ RESOLVED in the same commit (the OI-195 created-and-resolved pattern; Cowork-found at the P4 verification, provenance `599eebd45e`). **Task 2 (commit `2d168997341`) — the record-arm carriage:** `analyzeNoteHarmonicContext[Details]` (both funnelling through `analyzeHarmonicContextAtTick`) gain the record arm behind default-OFF `useJointNotationRecord`: flag ON → `produceNotationRecord` (whole-score, the P3a/P4 pattern) → `noteView(rec, tick)` → ONE builder `buildNoteContextFromRecord` filling `NoteHarmonicContext` from the record's published facts (contract §1 seam 2 — a VIEW, not a second computation, #6). `chordResults[0]` = committed via `chordResultFromRecordSegment`; `chordResults[1..]` = the §3.3 chord-axis alternatives (committed-first then descending content score, each `.identity.score` = its §3.3 content score); `keyFifths`/`keyMode` = C1 two-mode key; `keyConfidence` = the RAW §3.3 key-axis gap in nats (NO [0,1] remap); pedal false/-1 (OI-194); `temporalExtensions` default; `enclosingKeyArea` nullopt. Unification (#6): the per-segment record→`AnalyzedRegion` mapping EXTRACTED to the shared composing-side `regionFromRecordSegment` (`sectionrecordadapter`), so span seam + note seam derive committed reading + two-mode key + exposure bucket + §3.3 alternatives in ONE place — a pure code movement, `analyzeSectionFromRecord` byte-identical. **Task 3+4 (this commit) — the three consumers on the carriage + tests + doc:** the three audited consumers — status bar (`harmonicAnnotation` → the accessibility chain), harmony write (`notationinteraction`), right-click menu (`notationcontextmenumodel`) — all route through `analyzeNoteHarmonicContext[Details]`, so the ONE builder carries all three; each already renders via the SHARED P-strings formatters (`formatChordResultForStatusBar`/`ChordSymbolFormatter`) reading the carriage — **NO consumer code change**. New golden-less structural pins `notationnoteseam_record_tests` (6, on 2 fixtures, the P3a/P4 precedent): the carriage == the record's committed reading at each note's tick; the ordering rule (committed first, descending §3.3 score); the status bar renders the committed symbol; the harmony write uses the carriage; the context-menu score suffix is the §3.3 committed content score; the edges (out-of-span tick + produce failure → nothing, no partial, #13). ARCHITECTURE.md note-seam as-built block added. **Cache disposition:** the bounded-window decode cache is BYPASSED on the record arm (a whole-score produce per invocation, the P3a/P4 pattern; a record cache is a later measured concern — the interactive-frequency cost NOTED, not a structural incompatibility, not a FINDING). Carriage field-by-field source: rootPc/rootTpc/quality/extensions(seventh)/bass/degree/diatonicToKey/keyTonicPc/keyMode all from the record segment via `chordResultFromRecordSegment`; keyFifths=`keySignatureFifths`; keyConfidence=raw §3.3 key-axis nats gap; alternatives=§3.3 chord-axis; pedal suspended; temporalExtensions default (no in-tree reader); enclosingKeyArea nullopt. **NO inference change, no legacy-analysis call on the record path, `tools/robust_stop/` untouched; byte-identical flag OFF** (NO golden refresh). **SUITES: composing 1185 / notation 61 (+6 over 55) / pipeline-snapshot 13 GREEN.** Reuse-vs-new: `chordResultFromRecordSegment` + `recordAlternatives` + the P-strings formatters REUSED; `regionFromRecordSegment` newly extracted-and-shared; `NoteHarmonicContext` itself is transitional — it RETIRES at the OI-180 map (the carriage exists only to feed the audited consumers until the switch). NO SURPRISE. Commits pushed origin. NEXT (per the handoff): P6 (dual-arm classified comparison over the full notation output surface — the switch evidence; catalogue includes OI-201 + the applied-chord Nashville "?" convention) → P7 (doc-sync/close) → the user's switch ratification.)*

*Last updated: 2026-07-27, ninth entry (CC — **★ SEAMS PART 2 — P4: the IMPLODE + TUNING span-seam record paths + the EXPOSURE-BUCKET unification + OI-182 EXECUTED.** Dispatch `cc_instruction_notation_p4.md`. The last two span-seam consumers are re-plumbed onto the record path behind the default-OFF `useJointNotationRecord`. **Task 1 (commit `0e0ca379b3`) — the key-exposure BUCKET unification (the P2a pattern completed):** the tentative/assertive bucket the implode re-thresholded at 0.5/0.8 is now a STORED per-arm `AnalyzedRegion::keyExposureBucket` (0/1/2), set ONCE at the section-layer set site beside `hasAssertiveExposure` — legacy from `normalizedConfidence >= 0.5/0.8` (`sectionanalyzer.cpp::legacyKeyExposureBucket`, legacy-arm-only), record from the raw §3.3 gap at `kTentativeKeyExposureGap=0.975911` (declared from `exposure_constants.json` at full precision: legacy dur-rate 0.338501 / record 0.340837 / residual 0.002336) + the P2b `kAssertiveKeyExposureGap=1.055757`. The implode reads the stored bucket (`sameUserFacingInference`, `collectStableKeyAnnotationCandidates`); the dead implode-local `keyExposureBucket()`/`supportsTentativeKeyExposure()` + the 0.5/0.8 constants removed. **Task 2 (commit `bb9e3c9811`) — the implode chord-track record path:** `populateChordTrack` flag ON → `produceNotationRecord` → `analyzeSectionFromRecord` → the SAME `emitImplodedChordTrack` with `&rec.record`; a produce failure writes NOTHING (A2/#13, no fallback). Record-arm specifics (`record`-guarded, legacy inert): the Roman is the record's PUBLISHED `romanNumeral` (a fact via `noteView`, NOT re-formatted); the display symbol + Nashville are presentation derivations (`formatSymbol`/`formatNashvilleNumber`; a rootless class → no symbol); exposure via the stored bucket; the borrowed-key source-key search restricted to the C1 two modes (exotic enumeration + the 0.35 mode-suffix gate legacy-arm-only, inert on the record arm by two-mode construction); `kSameChordReannotationGap` (960) re-homed to a declared namespace-scope presentation-timing constant. **Task 3+4 (this commit) — the tuning region record path + OI-182 + doc sync:** `applyRegionTuning` flag ON derives the tuning regions from the record and maps each to the `(span, chordResult, keyModeResult)` HarmonicRegion the loop reads — the inputs are FACTS the record carries (`rootPc`, `quality`, `keyModeResult.tonicPc`, the only field any `TuningSystem` reads — `JustIntonation::rootOffset`); NO FINDING (no consumed fact the record lacks). **OI-182 EXECUTED at the record surface** (§4.1 presentation-gate disposition; INDEX row flipped + detail-file execution note: every constant's declared site — the two gap constants cited, the 0.35 mode-suffix gate retired-on-record/unwired, `kSameChordReannotationGap` re-homed, the legacy 0.5/0.8 now legacy-arm-only). ARCHITECTURE.md as-built block added. **NO inference change, no legacy-analysis call on the record path, `tools/robust_stop/` untouched; byte-identical flag OFF** (the pipeline-snapshot implode goldens UNCHANGED across all three commits — NO golden refresh). **New golden-less structural tests (the P3a precedent):** `KeyExposureBucketMirrorsLegacyThresholdsAtSetSite` (legacy bucket == 0.5/0.8 thresholding of its confidence, notation), the record-arm bucket in `section_record_adapter_tests` (composing), `ImplodeRecordArm.RecordArmEmitsRecordDerivedChordTrack` (pipeline — record Romans == `noteView` published, display symbols no grading token), `RecordArmAppliesTuningFromRecordFacts` (notation). **SUITES: composing 1185 / notation 55 (+2 over 53) / pipeline-snapshot 13 (+1) GREEN.** Reuse-vs-new: the implode/tuning machinery REUSED over the derived section; what retires at the OI-180 map — the implode confidence-literal + exotic-suffix reads (legacy-arm-only). Commits pushed origin. NEXT (per the handoff): the merged note-seam re-plumb (status bar + `notationinteraction` + `notationcontextmenumodel` on `noteView`) → P6 dual-arm → P7 doc-sync.)*

*Last updated: 2026-07-27, eighth entry (CC — **★ SEAMS PART 2 — P-STRINGS: the presentation string formatters (record-arm DISPLAY chord symbol + Nashville) + the PERMANENT inference↔presentation boundary guard; the two P3a findings CLOSED.** Dispatch `cc_instruction_notation_pstrings.md` (the ratified D2 ruling + `cowork_notation_output_contract.md` §3.3 amendment — display renderings are PRESENTATION derivations from published facts, the record stays preset-independent). **Task 1 (commit `58ac88079b`):** the record→`ChordAnalysisResult` carriage COMPLETED — `seventhExtensionFromJointQuality` carries the FINE class quality's seventh-ness into `ChordIdentity.extensions` (the coarse `ChordQuality` dropped it, the named hazard); the ONE record-segment→`ChordAnalysisResult` converter exposed as `chordResultFromRecordSegment` (#6; `analyzeSectionFromRecord` + the presentation layer read it). The emitter record arm renders the DISPLAY symbol ("G7"/"Am7"/"Bdim7"/"C7#5") via the REUSED `ChordSymbolFormatter::formatSymbol` (through the shared `formatChordResultForStatusBar`) instead of the record's grading-form `chordSymbol` ("GDom7", kept on the record for batch/a8 continuity); a rootless chromatic class → "" (matching the batch `jointChordSymbol`); the Roman stays a PUBLISHED FACT. Establishment (#19): the vocabulary families + FLAT-KEY spellings (Ab vs G# from the record tpc) + inversion/slash + applied/chromatic classes. **Task 2 (commit `55ae163ef5`):** the P3a Nashville GAP CLOSED — the record arm renders Nashville via the SAME shared `formatNashvilleNumber` (a presentation derivation; NO `jointRenderNashville`, NO second formatter #6); continuity established over 7 readings (record-arm == legacy-arm == the standard Nashville oracle); the applied/chromatic "?" convention is the legacy formatter's own. **Task 3 (this commit):** the PERMANENT boundary guard `inference_presentation_boundary_tests` — a mechanical include-closure test failing on violation BOTH ways: the joint estimator's inference module (glob `analysis/joint/`) includes NO presentation formatter (`chordanalyzer.h`/`chordsymbolformatter`; the shared pitch leaf `analysisutils.h` exempt), and `chordsymbolformatter.cpp` includes NO joint inference internal (consumes only the published output surface) — with a NEGATIVE CONTROL (the guard fires on a perturbed include, both directions). `COMPOSING_SRC_DIR` compile-def added so the guard scans the real sources (a glob → new module files auto-covered). **Task 4:** ARCHITECTURE.md boundary as-built; this entry. **OI-201 NEW** (user-directed): the aug-sixth DISPLAY chord symbol renders as a plain major triad (the fitted vocabulary collapsed the family to Italian content; the It/Ger/Fr distinction is on the ROMAN only) — a presentation-completeness gap declared to Cowork, catalogue at P6, never a knob-turn. **NO OI-182 presentation-constant site moved** (the exposure constants stay for P4 — row untouched). Reuse-vs-new: `ChordSymbolFormatter` REUSED, nothing retires. **NO inference change, NO legacy-analysis call on the record path, `tools/robust_stop/` untouched; byte-identical flag OFF** (the record path is never taken with the flag off; the converter refactor is a pure code movement). **SUITES: composing 1185 (Task-1 +4 display, Task-2 +1 Nashville, Task-3 +3 boundary = +8 over the 1177 baseline) / notation 53 / pipeline-snapshot 12 (11 goldens + 1) GREEN, NO golden refresh.** Riding Cowork edits (the presentation-derivations amendment + handoff) committed with Task 1. Commits pushed origin. NEXT (per the handoff): P4 implode+tuning (the exposure-bucket unification + OI-182 execute) → the merged note-seam unit (P3b+P5) → P6 dual-arm → P7 doc-sync.)*

*Last updated: 2026-07-27, seventh entry (CC — **★ OPEN_ITEMS REGISTER SPLIT — the register is now a lean INDEX + per-item detail files.** Dispatch `cc_instruction_open_items_split.md` (user-ratified option 1, 2026-07-26). `OPEN_ITEMS.md` (was 323 KB, too large to render) is now the complete lean **INDEX** — 200 rows, ~76 KB (ID / name / 1–2-sentence description / owning layer-gate / status / detail link; the two governing header blocks, the A–I section structure and the section-I prose all preserved; the INDEX is the **authoritative status surface**) — plus one **verbatim detail file** per item under `open_items/OI-<n>.md` (a fixed three-line header — ID+name, the status-authoritative disclaimer, the section — then the item's FULL original row byte-for-byte, table pipes included; narrative/provenance only, NEVER a status of record). **Byte-reconciled (#12 / #17f):** NEW `tools/open_items_split_check.py` reads the pre-split file from git (`cb246a7580:OPEN_ITEMS.md`) and the post-split tree and proves every ID present in the INDEX AND with a detail file, every detail row byte-identical to its pre-split row (modulo the declared header), none lost / none added → `open_items/split_reconciliation.json` **200 / 200 / 200 OVERALL PASS**. CLAUDE.md's "Open-items register" section is amended (index+detail rules, user-ratified 2026-07-26); `cowork_handoff.md` carries a dated pointer note. A pure documentation-structure move — NO code / corpus / golden / `tools/robust_stop/` touch, NO item's substance altered. ONE commit, pushed origin. NEXT (per the handoff): the seams-2 note-seam re-plumb continues, now in the new register shape.)*

*Last updated: 2026-07-27, sixth entry (CC — **★ SEAMS PART 2 — P3a (the annotation-emitter record path) DELIVERED + a P3 RE-PARTITION STOP.** Dispatch `cc_instruction_notation_seams_2.md` (partition P3, the span-path composing bridge). **P3a (commit `e2c59715a5`, pushed):** the span-path bridge's WRITE emitter (`addHarmonicAnnotationsToSelection` / `emitHarmonicAnnotations`, `notationcomposingbridge.cpp`) gains its record-path implementation behind the default-OFF `useJointNotationRecord`. Flag ON → `produceNotationRecord` (whole-score) → `analyzeSectionFromRecord` (windowed to selection+lookahead) → `emitHarmonicAnnotations(…, &rec.record)`; a produce failure writes NOTHING (no legacy fallback — the record IS the surface, A2; #13). **Strings from the record's DERIVED FACTS** (`seg.chordSymbol`/`seg.romanNumeral`, jointRender form — §5.6, #6; NEVER re-formatted from `ChordIdentity`), looked up per region by start tick via `noteView`. The Roman is the segment's **committed-key** numeral — no legacy area re-contextualization (A commits a coherent per-segment key; an EXPECTED inference-driven diff, P6). Shared/unchanged on the record arm: two-mode key-area **bracket markers** (mode∈{Ionian,Aeolian} → no exotic suffix by construction), the min-duration gate, **cadence/pivot** (the section layer's derivation over the record), the per-staff write. **Pedal SUSPENDED** (`isPedalPoint`=false from the adapter → the "X ped." block never fires; OI-194, commented). **Legacy preserved VERBATIM** in the else-arm (area recontext + `ChordSymbolFormatter` + Unknown-Roman refinement + Nashville) → byte-identical (11 goldens untouched). **SUITES: composing 1177 / notation 53 / pipeline-snapshot 11 goldens + 1 new (`SectionRecordAdapterAnnotation`) = 12 GREEN, NO golden refresh**; no inference change, no legacy-analysis call on the record path, `tools/robust_stop/` untouched. **★ TWO FINDINGS (to Cowork):** (1) **NASHVILLE is a record-path GAP** — the record/jointRender publish no Nashville string and §5.6 has no batch-render Nashville form to reproduce, so the record arm writes nothing for Nashville (declared gap, #12/#13 — not ported; a second formatter would violate #6). It recurs at P4 (implode nashville) + P5 (context-menu nashville) — a CONTRACT question: publish a Nashville derived fact / add `jointRenderNashville`, or rule Nashville a presentation derivation from degree+quality. (2) **The record's `chordSymbol` is the GRADING form** (`jointChordSymbol` = pc-name + class quality, e.g. `"GMaj"`/`"GDom7"`/`"AMin7"`, NOT idiomatic `"G"`/`"G7"`/`"Am7"`); written verbatim as a STANDARD Harmony it is MuseScore-re-parsed on read-back — a display-form question (is the notation chord symbol the grading form, or does the display layer transform it?). **★ P3 RE-PARTITION STOP (feasibility; the dispatch's L4/L5-audit precedent):** the span-path bridge does NOT fit one session. The **status-bar / note-context producer** half (`analyzeHarmonicContextAtTick` → `buildWindowSection` (memoized window cache) → `buildRegionalContextFromSection` → `harmonicAnnotation`) is entangled with (a) the bounded-window memoization cache and (b) the **note-seam string-carriage** — `NoteHarmonicContext` carries no strings, and the SAME carriage is needed by the P5 note-seam consumers (`notationinteraction` harmony-write, `notationcontextmenumodel`). **Proposed: merge the status-bar/note-context path with the old P5 into ONE "note-seam re-plumb" unit (P3b+P5)** — it resolves the `NoteHarmonicContext`↔record string carriage once and re-plumbs the status bar + interaction + context menu together on `noteView`. **★ RESUME at the note-seam re-plumb** (HEAD `e2c59715a5`) after Cowork's disposition of the re-partition + the two findings. Then P4 implode+tuning (implode 0.5/0.8 bucket + OI-182 execute) → P6 dual-arm → P7 doc-sync. Riding: `cowork_handoff.md` + `OPEN_ITEMS.md` (the new user-directed rows OI-198/199/200).)*

*Last updated: 2026-07-27, fifth entry (CC — **★ SEAMS PART 2 — P2b (the record→AnalyzedSection adapter `analyzeSectionFromRecord`) DELIVERED; resume at P3, the span-path composing bridge.** Dispatch `cc_instruction_notation_seams_2.md` (partition P2b). **P2b (commit `d25cf40170`, pushed):** the section layer's record path — NEW `section/sectionrecordadapter.{h,cpp}` `analyzeSectionFromRecord(sc, from, to, excludeStaves, rec)` derives the shared `AnalyzedSection` from `joint::NotationRecord` instead of the legacy Pass-0 `HarmonicRegion` stream. **DEAD CODE** (declared dormancy — no `src/` caller; named consumer = the span/implode bridge re-plumb behind the default-OFF `useJointNotationRecord`), unit-tested via `produceNotationRecord`. **Mapping** (one record segment overlapping `[from,to)` → one `AnalyzedRegion`, 1:1; the legacy gap-fill/trim/measure-layout/key-stabilization machinery is NOT ported — A's decode is contiguous): identity from the record's published facts (`rootPc`=`seg.rootPc` value_or −1; `rootTpc`=`rootSpellingLof`+`Tpc::TPC_C`; bass from `bassPerEvent.front()`; quality=`jointOursQuality(seg.quality)`→`ChordQuality`, the ONE coarse map reused #6); function (**degree resolved** — `recordFunctionDegree` maps an UNALTERED diatonic Roman I..VII with empty `target`→0..6, else −1: reads A's native `degreeBase`, never recomputes from root+key, the OI-173 lesson; `diatonicToKey`=`seg.diatonicToKey`; `keyMode`=C1 two-mode Ionian/Aeolian); `keyModeResult.normalizedConfidence` = the **RAW §3.3 key-axis gap in nats** (`keyAxis.scores[committed]−max(others)`, NO [0,1] remap, amendment 1), `hasAssertiveExposure`=`gap >= kAssertiveKeyExposureGap`=**1.055757** (declared in-adapter with the `exposure_constants.json` citation: legacy dur-rate 0.273902 / record 0.272871 / residual 0.001031; the 0.5/0.35 constants + implode bucket stay for P4/OI-182); **tones re-collected** over each span via `ebr::weightedPcView` (composing-side, #7/amendment 3 — NOT the notation collector); **alternatives** from the chord-axis §3.3 slice ranked by content score, committed dropped (the content-vs-template ordering diff is EXPECTED inference-driven, Gap-1/P6); `isPedalPoint`=false (pedal suspends, OI-194). The confidence-gated **key-area grouping EXTRACTED verbatim** to the shared `groupKeyAreas` helper (`sectionanalyzer.h/.cpp`), legacy + record share ONE definition (#6); `KeyArea::confidence` seeds from the per-region `normalizedConfidence` (a [0,1] value legacy, a nats gap record — no production consumer reads it as a probability; only the snapshot serializer + P6 dual-arm, both non-production). **BYTE-IDENTICAL legacy** (the extraction is a pure code movement; the adapter has no caller): composing 1174→**1177** (+3: whole-span faithful map, `[from,to)` overlap window unclipped, null/degenerate guards) / notation **53** / pipeline-snapshot **11** GREEN, **NO golden refresh**. No inference change, no legacy-analysis call on the record path, no committed-output change; `tools/robust_stop/` untouched. **★ RESUME AT P3** (HEAD `d25cf40170`): the span-path composing bridge (`notationcomposingbridge.cpp` annotations/bracket markers/status-bar strings) — record-path implementations per the CSV rows: chord-symbol/RN strings from the record's DERIVED FACTS (`seg.chordSymbol`/`seg.romanNumeral`, the module formatter #6 — NOT re-formatted from `ChordIdentity`, the §5.6 formatter-continuity concern to work out at P3), key strings from the two-mode key (+ modal reading AVAILABLE; §4.1 C1 no-exotic-suffix rule), candidate score suffix from the §3.3 slice; the pedal "X ped." does NOT fire on the record path (comment naming OI-194). Then P4 implode+tuning (implode 0.5/0.8 bucket + OI-182 execute) → P5 note-seam → P6 dual-arm → P7 doc-sync (ARCHITECTURE.md record-path as-built deferred to P7 per the partition).)*

*Last updated: 2026-07-27, fourth entry (CC — **★ SEAMS PART 2 — P2a (the assertive-exposure gate unification) DELIVERED; resume at P2b, the record→AnalyzedSection adapter.** Dispatch `cc_instruction_notation_seams_2.md` (amendment 2(1) ratified the unification). **P2a (commit `e3dac74b04`, pushed):** the cadence/pivot detectors (`sectioncadencedetection.cpp`) and the section key-area grouping (`sectionanalyzer.cpp`) now read the STORED `AnalyzedRegion::hasAssertiveExposure` boolean instead of re-thresholding `normalizedConfidence` — ONE thresholding site per gate (#6; the confidence-axis analogue of OI-173). The assertive (0.8) decision is made ONCE in `analyzeSection` (via `hasAssertiveKeyConfidence` at the set site), so the record path can set the boolean from `gap >= g₀.₈` (the P1 constant) without any consumer comparing the nats gap to a [0,1] literal. `notationannotate_tests.cpp`'s `region()` helper now sets the boolean to match. **BYTE-IDENTICAL legacy** (proven analytically — every detector-input region comes from `analyzeSection` where `hasAssertiveExposure == hasAssertiveKeyConfidence(keyModeResult)`; bridge passes `section.regions`, implode passes whole-region copies + endTick-only merge — and empirically): composing 1174 / notation 53 / pipeline-snapshot 11 GREEN, NO golden refresh. The implode 0.5/0.8 BUCKET unification rides P4 (its consumer is re-plumbed there); the 0.35 mode-suffix gate retires under C1 at P4 (amendment 2(2)). **★ RESUME AT P2b** (HEAD `e3dac74b04`): the record variant `analyzeSectionFromRecord` (composing section layer) mapping `NotationRecord` → `AnalyzedSection` — DEAD CODE (nothing calls it; byte-identical), unit-tested via `produceNotationRecord`. Design points worked out: per RecordSegment→AnalyzedRegion — identity (rootPc, quality string→`ChordQuality`, bass from `bassPerEvent`, root/bass tpc from the record's lof spellings), function (degree string→int, `diatonicToKey` verbatim, keyMode Ionian/Aeolian only per C1), `keyModeResult.normalizedConfidence` = the RAW key-axis gap (nats), `hasAssertiveExposure` = `gap >= g₀.₈`=1.0558, `isPedalPoint`=false (pedal suspends), `alternatives` from the chord-axis §3.3 slice ranked by content score with `.identity.score`=slice score; key-area grouping EXTRACTED to a shared helper (reads `hasAssertiveExposure`, so legacy + record share it, #6); `tones` re-collected from the score's L1 note surface over each segment span (the record carries no per-segment raw tones — so the variant takes the score, or the bridge fills tones at P3 — the collector must be composing-side (#7, amendment 3), not the notation `collectRegionTones`). **★ CONVERSIONS DE-RISKED (2026-07-27, at the P2b start — the two feared unknowns pinned): (a) quality — `jointOursQuality(seg.quality)` (in `jointrender`, the existing single-source coarse map: Maj/Dom7/Maj7/AugSixth/Neapolitan→"Major", Min/Min7/MinMaj7→"Minor", Dim/Dim7→"Diminished", HalfDim/HalfDim7→"HalfDiminished", Aug/Aug7/AugMaj7→"Augmented") → a trivial string→`ChordQuality` switch — REUSE, do not re-invent (#6); (b) tpc = `rootSpellingLof + Tpc::TPC_C`** (the record's lof is `tpc - TPC_C` per `jointprimitives.h`). `ChordQuality` enum = {Unknown,Major,Minor,Diminished,Augmented,HalfDiminished,Suspended2,Suspended4,Power} (`analysistypes.h`). STILL TO RESOLVE at P2b: the degree string→int mapping (from `SegmentSummary.degree` / `LabelClass.degreeBase`), the composing-side per-span tone collector (`ebr::weightedPcView`-equivalent), and `rootPc`/`bassPc` sourcing (`SegmentSummary.rootPc` + `bassPerEvent`). Then P3 span bridge → P4 implode+tuning (implode bucket + OI-182 execute) → P5 note-seam → P6 dual-arm → P7 doc-sync.)*

*Last updated: 2026-07-27, third entry (CC — **★ SEAMS PART 2 — P1 (the §4.1 gap-scale measurement) DELIVERED; the ratified partition is in progress.** Dispatch `cc_instruction_notation_seams_2.md` (partition ratified by Cowork 2026-07-26; the P2 sharpening: the record path carries the RAW §3.3 key-axis gap in nats in the confidence fields, NO [0,1] remap). **P1 (commit `ad0c4069bc`, pushed):** the measured correspondence that lets the record-path exposure gates preserve the legacy exposure behavior without inventing a calibration. NEW opt-in measurement-only `DISABLED_SeamsGapMeasurement` (in `pipeline_snapshot_tests`; the `DISABLED_P3PerfBaseline` precedent) dumps, over the snapshot corpus in the 16-measure golden window, per legacy region the `normalizedConfidence` the gates read today and per record committed segment the §3.3 key-axis content-score gap; `tools/notation_seams/choose_exposure_constants.py` picks, per legacy threshold (0.35 mode-suffix / 0.50 tentative / 0.80 assertive), the gap constant whose DURATION-WEIGHTED record firing rate matches the legacy rate (segmentation-invariant — the robust-unit principle; count rate reported, not selected on). **Result (authoritative in the generated `tools/notation_seams/exposure_constants.json`, #17f): pool 327 legacy regions / 448 record segments (0 null-gap; ALL 11 scores produced a record with NO error); the three chosen gap constants are monotone in the threshold and preserve each legacy duration-rate within ~0.5% of duration.** BYTE-IDENTICAL: production untouched (only the test file + `tools/notation_seams/` added; the measurement test is DISABLED_, separate from the golden comparison); **pipeline-snapshot 11 goldens GREEN, NO golden refresh**; composing 1174 / notation 53 binaries unchanged by the diff. **★ RESUME AT P2** (the record→`AnalyzedSection` adapter) from HEAD `ad0c4069bc`. **P2 design note surfaced for the next session:** every legacy gate reading `normalizedConfidence` (key-area grouping, cadence/pivot `hasAssertiveKeyConfidence`, implode buckets/mode-suffix) compares against the [0,1] thresholds — on the record path the field carries nats, so each gate must use the P1 gap constant, NOT the 0.8/0.5/0.35 literal. Clean unification candidate (byte-identical on legacy, correct on record): make the shared `detectCadences`/`detectPivotChords` read the already-stored `AnalyzedRegion.hasAssertiveExposure` boolean (which the section layer sets per-arm — `conf>=0.8` legacy, `gap>=g₀.₈` record) instead of re-thresholding `normalizedConfidence` themselves. Then P3 span bridge → P4 implode+tuning (OI-182 executes) → P5 note-seam → P6 dual-arm → P7 doc-sync.)*

*Last updated: 2026-07-27, second entry (CC — **★ SEAMS PART 2 — Task 1.1 (the default-OFF internal migration flag) DELIVERED + a FEASIBILITY STOP with a partition proposal.** Dispatch `cc_instruction_notation_seams_2.md`. **Task 0 + Task 1 flag (commit `b090f6e4ab`, pushed):** the ONE internal boolean `useJointNotationRecord` (default FALSE; NO settings UI; documented as retiring WITH the legacy branch at the OI-180 map) added to `IComposingAnalysisConfiguration` + `ComposingConfiguration` (the sole implementer — verified no mock/stub breaks), mirroring the existing boolean pattern exactly; the riding `cowork_handoff.md` seams-part-1 verification rides with it (Task 0). **Nothing reads the flag yet → BYTE-IDENTICAL: composing 1174 / notation 53 / pipeline-snapshot 11 GREEN, NO golden refresh** (`composing_tests` correctly unaffected — it does not link `composingconfiguration.cpp`; ninja "no work to do"). **★ FEASIBILITY STOP after this COMPLETE, committed, byte-identical unit (the dispatch's explicit authorization + the L4/L5 audit precedent):** the full consumer re-plumb (6 consumer files + a shared record→`AnalyzedSection` adapter + the §4.1 measurement + the dual-arm instrument, each requiring its own build + 3-suite byte-identity cycle) does not fit one session, and the mapping study surfaced a CROSS-TASK DEPENDENCY the dispatch's Task-1→Task-2 ordering inverts: **the section-layer record path (Task 1.2) and the span/implode bridges (Tasks 1.3/2) consume `normalizedConfidence`/`hasAssertiveExposure` — the 0.8 key-area/cadence gate, the 0.5/0.8 implode exposure buckets, the 0.35 mode-suffix threshold — whose record-path inputs ARE the §4.1 gap-scale constants the dispatch schedules for MEASUREMENT in Task 2.3.** The report to Cowork carries the full partition proposal (P0 flag DONE → P1 §4.1 measurement pulled early → P2 the shared adapter → P3 span bridge → P4 implode+tuning → P5 note-seam → P6 dual-arm instrument → P7 doc-sync) + the confirmed consumer map + the open design points. NO inference change, NO committed-output change, `tools/robust_stop/` untouched. NEXT: Cowork's disposition on the partition, then the re-plumb units under it.)*

*Last updated: 2026-07-27 (CC — **★ SEAMS PART 1 — the notation output-surface contract §1 seams' PRODUCER side is DELIVERED (dormant), and OI-197 (the C++ spelling establishment, a switch precondition) is DISCHARGED.** Dispatch `cc_instruction_notation_seams_1.md`. **Task 0 (rows commit `77cbb72b8d`):** OI-196 (interim fifths-mapping duplicate, unifies at map item 2) + OI-197 rows + the handoff record-assembly verification, pushed. **Task 1 — OI-197 RESOLVED (commit `c8c5bb6a25`):** the record's §5.2 root/bass tonal-spelling establishment was measured PYTHON-side; the C++ mapping (`jointprimitives::rootSpellingLof`/`factorSpellingLof` — the one the switch will PUBLISH) needed the same-strength check. NEW default-OFF `tools/batch_analyze.cpp --joint-spelling-parity` dumps the C++ record's root + all-factor line-of-fifths spellings per committed segment (production config — the compiled-in EMBEDDED tables + the SELECTED weight vector, §5 seg_cap 4; 326 pieces / 13,063 segments); `gen_spelling_establishment.py --cpp-parity` emits the Python-side lof per segment (the same `root_lof`/`factor_lofs` the notated establishment uses) and compares lof-EXACT. **ZERO divergences** — 13,063/13,063 decode-aligned (0 class-mismatch / 0 missing / 0 extra), 0 root / 0 bass / 0 factor divergences; the ROOT field on all 13,063 cells and the BASS field on 11,182 lowest-note-factor cells both reconcile to the committed establishment, plus a 39,954-factor superset. So the C++ mapping is lof-IDENTICAL to the Python mapping on every committed segment and INHERITS the Python-side corpus establishment (root 13061/13063 = 99.985 %, bass 11180/11182 = 99.982 % derived-vs-notated, the 4 enumerated enharmonic-convention divergences). The comparator itself is established by a NEGATIVE CONTROL (a perturbed lof is caught → FAIL/exit-nonzero). Generated `cpp_parity` block in `spelling_establishment.json` (#17f); the notated establishment values byte-UNCHANGED; the default batch output byte-identical (`test_batch_analyze_regressions` passes). **Task 2 — the record-producing entry + the two views (this commit):** NEW `jointnotationproducer` — the ONE-call `produceNotationRecord(score, stem)` (buildAdapterFacts → the embedded tables/adapter + SELECTED weights → decodePiece §5 seg_cap 4 → assembleNotationRecord; whole-score decode ONCE, deterministic, NO caching; returns an UNAMBIGUOUS failure state when the adapter cannot extract the score — never a partial record, #13) + its facts→record core, the SPAN view (segment indices overlapping [startTick, endTick); empty span selects nothing) and the NOTE view (the segment containing a tick, `startTick <= t < endTick` — a boundary tick belongs to the segment it STARTS; `found=false` outside the analyzed span). Pure views, no recompute (#6); the score is read ONLY via the L1 adapter (no raw-DOM walk). DORMANT — named consumer: seams part 2 (the re-plumbed notation consumers behind the default-OFF switch). Coverage `joint_producer_tests` (5): the producer core == the parts-assembled record + a `decode_parity_ref` spot-check (bwv324/bwv362); the score wrapper == buildAdapterFacts + core on `pb_chorale.mscx` (non-empty record); the null-score failure path; the span-view overlap incl. span-splitting + the empty-span edge; the note-view boundary rule + the out-of-span edge. Doc sync (#10): ARCHITECTURE.md joint as-built (the OI-197 inheritance note + the producer/views paragraph). **SUITES: composing 1174 (+5 producer) / notation 53 / pipeline-snapshot 11 — GREEN, NO golden refresh** (the notation path is byte-identical — the joint module stays dormant). No `src/` inference change, no committed-output change; `tools/robust_stop/` untouched. Reuse-vs-new: the producer composes exclusively delivered, established parts (buildAdapterFacts/embedded loaders/decodePiece/assembleNotationRecord); nothing retires (the legacy seam retires at the switch). Commits pushed origin. NEXT: seams part 2 (the consumer re-plumb behind the default-OFF flag + the §8.3(d) consumer-output comparison instruments), then the switch (§8.4).)*

*Last updated: 2026-07-26, sixth entry (CC — **★ THE NOTATION OUTPUT-SURFACE RECORD (contract §3.1–§3.4) IS ASSEMBLED — the joint module publishes the A-native notation record, DORMANT.** Dispatch `cc_instruction_notation_record_assembly.md` (Task 0 ratification-record commit `04fb57ab08` + four build commits). **Task 1 (`c06298c983`):** the chord-symbol/Roman/grading-quality render primitives moved from `tools/batch_analyze.cpp` into the joint module as `jointrender` (a derived-published-fact unit; the pc→spelling map single-sourced to `jointprimitives::pcKeyName`, retiring the decoder's + batch's copies, #6); the batch `--joint-inference` render re-pointed; **full-corpus regen BYTE-IDENTICAL on all three presets (352/352 each, 0 diffs)** — the render moved, values did not. **Task 2 (`e064f08298`):** `NotationRecord` + ONE `assembleNotationRecord` (score-decode-INDEPENDENT — never re-decodes, never reads the score): §3.1 piece block (span, sig/declared INPUT ECHO, the §2 provenance from the D1 embedded constants — **discharges the D1 declared dormancy**; the adapter exposes NO mid-piece re-anchor points, so that §3.1 list is empty, recorded not invented); §3.2 committed fields verbatim + derived facts (module-local `keySignatureFifths` duplicating the legacy primitive under OI-180 isolation, member/factor roles reused, chord-symbol/RN from Task 1, class-native `diatonicToKey` per OI-173's lesson, per-event bass role, aug-sixth It/Ger/Fr from the SOUNDING content); §3.3 the delivered `computePosteriorSlice` attached; §3.5 ornament fields RESERVED-absent (OI-194); §3.6 excluded fields absent. **Task 3 (`acea6057c4`):** the root/bass tonal-SPELLING derivation (line-of-fifths; `jointprimitives::rootSpellingLof`/`factorSpellingLof`, keySignatureFifths RELOCATED there as a key primitive) with the §5.2 establishment `tools/joint_estimator/gen_spelling_establishment.py` → `spelling_establishment.json`: **root 13061/13063 (99.985 %) agree with the notated tpc where the root sounds, 0 unmappable/not-sounding/pc-mismatch; ALL FOUR divergences are ENHARMONIC re-spellings (same pc — the OI-168 D#/Eb convention class, a FINDING not a wrong row)**; ONE bug caught+fixed (#3: applied-target LOF parsed lowercase targets wrong). **Task 4 (this commit):** the §3.4 un-rounded modal reading (`computeModalReading`) per key run — per scale degree 1..7, the sounding duration + onset count of every chromatic inflection (degree from the notated spelling, inflection by pc offset; no label/threshold/rounding, C1); established by a synthetic hand-counted run + the **bwv254 hand-check** (D-minor: degree-6 all ♭6 B♭ ×4/1920t, no B♮; degree-7 leading-tone C♯ ×5/2160t + subtonic C♮ ×1/240t — verified against an independent pc count of the notated notes AND cross-checked vs decode_parity_ref). **DORMANT throughout — no production consumer (named consumer: the later seams dispatch); no inference change, no committed-output change; batch corpus BYTE-IDENTICAL (spot-checks); `tools/robust_stop/` untouched; the notation path untouched.** **SUITES: composing 1169 (+14: 6 record + 6 spelling + 2 modal) / notation 53 / pipeline-snapshot 11 — GREEN, NO golden refresh.** Doc sync (#10): ARCHITECTURE.md joint as-built (the record §3.1–§3.4, the spelling instrument + artifact, the modal counter, ornament reserved, seams pending). Commits pushed origin. NEXT: the seams dispatch (§1 span/note seams reading this record), then the switch (§8.4).)*

*Last updated: 2026-07-26, fifth entry (CC — **★ THE POSTERIOR SLICE — notation output-surface contract §3.3 GROUP (i) — is DELIVERED, Python reference + C++ module surface, bit-identical.** Dispatch `cc_instruction_posterior_slice.md` (three commits). **Task 1 — OI-195** (commit `4565830c82`): the D1 embedded artifacts were the CRLF working-tree form, not the git-canonical LF blobs, so hashes/regen/drift-guard were checkout-dependent (the OI-34 class in a `src/` generated file); `gen_embedded_tables.py` now LF-normalizes each input before embedding AND hashing (values byte-equal CRLF-vs-LF, verified), the header states the canonical form, and the drift guard normalizes the file side — checkout-independent (#16/#19). `tables_all.json` embedded 118,460→114,787 bytes = the committed blob; drift guard 6/6, composing 1152, 4 stems byte-identical. **Task 2 — the Python reference instrument** (own commit): NEW `tools/joint_estimator/gen_posterior_slice.py` (imports `probe_decoder` IMPORT-ONLY) decodes all 326 covered pieces at the SELECTED weights and publishes, per committed segment, the two group-(i) FULL candidate lists (no truncation) — KEY axis (committed class under every scoreable key, KEYS_24 order) + CHORD axis (every scoreable class under the committed key, sorted) — as `posterior_slice_ref.json` (**37.3 MB, 13,063 segments**, full-precision floats; **shared-label form** — the scoreable sets are span-INDEPENDENT: exactly one 24-key list + one 104-class list across all segments, so labels are top-level and each segment stores scores + committed index; lossless, uniformity asserted). **Establishment (#19), the ratified two-half form (user-ratified option A at CC's correct Task-2 STOP — the original oracle `probe_corpus_decode.json` is the IDENTITY-weight, pre-§5 slice, incompatible with a SELECTED-weights deliverable; the arms differ on 287/326 pieces): (a) mechanism** — the identity-arm key-axis runner-up/gap reproduces the committed `probe_corpus_decode.json` EXACTLY on the **325 §5-unaffected pieces**, with **bwv362 the sole §5 exception** (equal total score −1770.143, enumerated, not a tolerance); **(b) decode** — the selected-arm committed segments equal `decode_parity_ref.json` on **326/326**. **Task 3 — the C++ module surface** (own commit, with doc sync): additive `jointdecoder::computePosteriorSlice` (built on `segmentContentScore` → inherits the established Neumaier bit-parity; the decode is UNCHANGED, the batch `.ours.json` render UNCHANGED — a8 keeps `"alternatives": []`); the default-OFF `batch_analyze --joint-posterior-slice <dir>` parity driver verifies the C++ slice **BIT-IDENTICALLY** against the reference — **326 pieces × 13,063 segments × 1,672,064 candidates, both axes, 0 divergences, PASS**. NEW `joint_slice_tests.cpp` (3 tests: structure/committed-flags/primitive-consistency, scoreability filter, empty/degenerate). **SUITES: composing 1155 (+3 slice) / notation 53 / pipeline-snapshot 11 — GREEN, NO golden refresh**; batch corpus BYTE-IDENTICAL (4-stem spot-check). NO inference value change, NO committed-output change, NO `tools/robust_stop/` change; the notation path untouched. GROUP (ii) forward-backward marginals remain OI-193's later step. Commits pushed origin.)*

*Last updated: 2026-07-26, fourth entry (CC — **★ DECISION D1 EXECUTED — the joint estimator's fitted tables + selected weight vector are now DELIVERED AS COMPILED-IN EMBEDDED SOURCE, provenance-locked at build time; a value-neutral DELIVERY change (NO inference, NO committed output moves).** Dispatch `cc_instruction_joint_table_codegen.md`. **Task 0** (the second ratification-record commit `5f3362f359`): the P1 pedal-point ruling (voice-independent ornament class, OI-194) + the user-ratified notation output-surface contract (`cowork_notation_output_contract.md`) + the OI-194/§10 riding edits; pushed origin. **The codegen (this commit):** new generator `tools/joint_estimator/gen_embedded_tables.py` compiles the five committed table artifacts (`tables_all`/`note_tables_all`/`factor_presence_all`/`fermata_boundary_addendum`/`mode_marginal`, 118460/9651/6878/15069/8665 bytes) + the direct-metric SELECTED weight vector (extracted from `decode_parity_ref.json`, identity `random07`) **VERBATIM** — JSON bytes as C-escaped runtime-concatenated chunks, NOT a parsed-structure codegen — into the generated `src/composing/analysis/joint/jointembeddedartifacts.{h,cpp}`, each with its sha256 + the §2 output-surface-contract provenance constants (artifact hashes, weight-vector identity, decoder version `1.0`) published as DECLARED DORMANCY (consumer: the notation record build). **Loading (#6, one parse path):** `JointTables::loadEmbedded` / `FittedAdapter::loadEmbedded` / `selectedWeights()` parse the embedded bytes through the SAME established parser as the filesystem loaders (the table-parse `buildTables` + `parseRoot` are now shared between the file and embedded paths). `tools/batch_analyze.cpp --joint-inference` — the PRODUCTION batch/corpus decode — switched to the embedded source; the `<artifact-dir>` stays on the CLI (trigger) but is **no longer read for fitted values**. Filesystem table-loading remains ONLY in the tests/diagnostics that establish it. **ESTABLISHMENT (#19) ALL PASS — nothing moved:** the standing DRIFT GUARD (`joint_embedded_tests`, 6 new tests) proves embedded bytes == committed files (byte-equality + sha256, per artifact) and the embedded weight vector value-exact vs `decode_parity_ref.json`; the full-corpus regen through the embedded build is **BYTE-IDENTICAL to the committed corpus on all three presets (352/352 each, 0 content diffs)**; the pinned a8 + `robust_stop_diff` sandwich is **OVERALL PASS, run-diff (+0/−0) every preset** (class-(b) hard-stop 1,817,280 = 1,817,280 δ+0; class-(a) 89040 δ+0; key-home 56.1427 / key-local 78.4161 % identical; abstain 0). **SUITES: composing 1152 (+6 drift-guard) / notation 53 / pipeline-snapshot 11 — GREEN, NO golden refresh** (the notation path is untouched — any snapshot change would have been a STOP). **What retires:** the production path's filesystem table/weight dependence (batch `--joint-inference` no longer reads `tables_*.json`/`decode_parity_ref.json` from disk); file-loading stays only as the tests/diagnostics establishment comparator. NO re-baseline, NO `tools/robust_stop/` change, NO corpus commit (the graded surface is unmoved). Commit: ONE revertible, provenance-stamped codegen commit; pushed origin only. NEXT: the record + posterior-slice build dispatch (the notation output-surface contract §3), then the seams, then the switch.)*

*Last updated: 2026-07-26, third entry (Cowork — **★ THE NOTATION-LAYER ADOPTION INCREMENT'S DECISION SURFACE IS USER-RATIFIED (`cowork_notation_adoption_increment.md`)** — the principles gain the decision-neutrality corollary (reuse/obsolescence secondary, counting only as carried-forward establishment #19; consumer-change impact and end-user behavior-change impact carry no weight; #14/#15/#19 gates unchanged — CLAUDE.md updated this commit), and the five design rulings: (A2) the joint-native record IS the notation surface, no compatibility view, `HarmonicRegion` retires with the legacy path; (B-full) the uncertainty contract is the FULL posterior (forward-backward marginals, status-marked model probabilities), delivered established-slice-first, completion rowed OI-193; (C1) two-mode key + the published un-rounded modal reading, no 21-value mode inferred again; (D1) fitted tables embedded as provenance-stamped generated source; (E) modal reading inside the increment, ornament labels their own increment, rowed OI-194. NEXT: the read-only notation consumption-surface audit (`cc_instruction_notation_consumption_audit.md`), then the contract drafting, then the build dispatches under the OI-180 sanction pattern.)*

*Last updated: 2026-07-26, second entry (CC — **★ THE OI-178 ADOPTION COMMIT — the joint estimator IS the production inference layer on the batch/corpus surface (user-ratified option 1, STAGED SCOPE).** Dispatch `cc_instruction_adoption_commit.md`. **THE WIRING:** `tools/batch_analyze.cpp` gains a default-OFF `--joint-inference <dir>` — the standard `.ours.json` is produced by the joint module's decode (L1 fact adapter `buildAdapterFacts` → the ratified §5 `decodePiece` at the committed all-326 tables + the direct-metric SELECTED weight vector read from `decode_parity_ref.json`, seg_cap 4, leftover 2a) rendered to the a8 region schema — a faithful C++ port of `probe_run.decode_to_regions` + `adoption_measure.region_to_dict` (the exact chain the adoption record was measured through) — instead of the legacy `analyzeScore` pipeline; `tools/run_bach_preset.py` passes the flag through. **Inference is PRESET-INDEPENDENT** (the three preset dirs are identical at the inference fields). **STAGED SCOPE (declared migration state, #23):** the in-app NOTATION layer STAYS on the legacy analysis — its output-surface contract from A's posterior + the fitted-table packaging (the two gaps CC's two-gap STOP surfaced: the joint tables have no in-app delivery, `JOINT_ARTIFACT_DIR` is test-only, and the joint decode does not yet produce the notation surface) are the NAMED SUCCESSOR increment; the OI-180 retirement map is now the live agenda. **ESTABLISHMENT (#19) PASS:** the regenerated corpus decode is field-exact vs `tools/joint_estimator/adoption_decode.json` on all 326 covered pieces (all 3 preset dirs; 8-stem subset de-risk then 326/326 full, via a scratch verifier), and a8 over the new corpus reproduces the ratified adoption record EXACTLY — **root 77.03 / RN 64.12 / key-home 56.14 / key-local 78.42 %, class-(b) hard-stop 1,817,280 ticks per preset, key-abstain 0** (`robust_stop_diff` OVERALL PASS: class-(b) −33.0/−34.7/−33.1 %, coverage 326=326, no class-(a) INVESTIGATE). **`tools/robust_stop/` RE-BASELINED** via the pinned `robust_stop_restamp.py` (figures derived not hand-typed, #17f; O-12 snapshot `snapshot_2026-07-26_pre_oi178_adoption/` verified pre-existing; superseded OI-168 columns preserved in `reproduce_status.superseded_oi168`, #12; self-diff +0/−0). **CLAUDE.md gate block (A)** (both the ratified-baselines and A-8 dual-track paragraphs) updated to the OI-178 baselines + preset-independence + staged-scope; the OI-168 lineage marked HISTORICAL. **SUITES: composing 1146 / notation 53 / pipeline-snapshot 11 — all GREEN and UNCHANGED, NO golden refresh** (the notation path is byte-identical — any snapshot change would have been a STOP); `test_batch_analyze_regressions` passes (default batch path byte-identical → the flag-gating holds). Register: **OI-178 → EXECUTED (staged scope)**; **OI-175 → superseded-in-fact on the batch/corpus surface** (remains live on the legacy notation path, retires at the map); OI-192 (the genuine-new fifth-substitution cost side) is Cowork's riding row. Commit: ONE revertible, provenance-stamped adoption commit; pushed origin only. NEXT: the OI-180 retirement-map increments as their own verified dispatches — first the notation-layer increment (output-surface contract + table packaging) that closes the staged scope.)*

*Last updated: 2026-07-26 (CC — **★ THE OI-178 ROBUST-STOP ADOPTION MEASUREMENT: RUN, AND EVERY PASS CONDITION HOLDS — the adoption RECORD is assembled for the user's ruling (NO adoption act).** Dispatch `cc_instruction_adoption_measurement.md` (Cowork 2026-07-20; user ruling ★R=A1). MEASUREMENT ONLY — no production wiring, no golden refresh, no `tools/robust_stop/` re-baseline, no adoption commit; pinned instruments import-only, the firewall clean (the only decoder input was the frozen selected weight vector). A decodes the full covered corpus **from its own fact adapter (the production path**: `adapter_facts.json`, the direct-metric SELECTED all-326 vector `random07`, the §5 tie-break, seg_cap 4), rendered to the a8 region stream through the RETAINED chain (`probe_run.decode_to_regions`/`grade_regions` → `a8.build_piece_grid` vs WiR), and diffed against the committed reference via the pinned **a8 + robust_stop_diff R10 sandwich**. **ESTABLISHMENT (#19) PASS:** the Python-from-adapter decode reproduces the C++ production decode EXACTLY — the divergent-vs-oracle set is byte-identical to `joint_endtoend_parity`'s C++-from-adapter ten stems (the OI-184 reader-skew class); the current-system per-piece grading reproduces the committed manifest ROOT to the digit (66.04/64.98/65.93); a8's grid self-validation passed on every piece. **★ ALL PASS (OI-178 amended ★R=A1):** (i) key-LOCAL exceeds baseline beyond the piece-bootstrap CI on every preset — A−current **+12.37 [+10.57,+14.19] B / +15.37 J / +12.63 D**, every interval excluding 0; root non-degrading (**+11.0/+12.1/+11.1**, improves), RN non-degrading (**+17.8/+20.0/+17.9**); (i-b) modulation-rate band [3.96,6.60] — A **6.07** changes/piece (GT measured 5.28, closest of any arm); (ii) class-(b) root-disagree DURATION **NET DECREASE −33.0% B / −34.7% J / −33.1% D** (`robust_stop_diff` OVERALL PASS, rc 0), and A grades slightly MORE duration (8 300 640 vs 8 293 680) so it is not a coverage artifact; (iii) class-(a) tracked, all decrease; (—) **key-abstain reads zero** (A commits MAP — the OI-33 flag as required). **Columns (A all-326 vs current B/J/D): key-LOCAL 78.42 vs 65.99/62.98/65.71; key-HOME 56.14 vs 71.42/67.83/70.65; root 77.03 vs 66.04/64.98/65.93; RN 64.12 vs 46.33/44.10/46.23.** **★R key-HOME decomposition:** the GT self-agreement ceiling (fraction of graded duration where GT local==home) is **59.27 %**; A's home 56.14 sits just below it (a good local-follower), while the current system's home 71.42/67.83/70.65 sits 12–15 pts ABOVE the ceiling — it achieves home-agreement by UNDER-following modulation, the defect A exists to fix (the ★R=A1 rationale, borne out). **PREDICTIONS (Cowork #17b): every band deviation FAVORABLE** — class-(b) decrease −33% exceeds the predicted 30%, RN +18 exceeds +16, key-local jazz +15.4 above +13; key-home/root/modulation as predicted; NO inverted mechanism (contrast OI-187). **SET-DIFF (LARGE both directions, as predicted):** runs ref 6506→cand 4547 (+3688/−5647 baroque); added class-(b) 3474 (dur 1.44M), removed class-(b) dur 2.32M → net −0.90M. **Every added class-(b) run is classified by mechanical signature (none undiagnosable);** the GENUINE-NEW subset (A wrong where current was right) is 1474 runs / 671 880 ticks (~8% of the current class-(b) failure), **dominated by fifth-apart functional root substitutions with the key correct (fifth 506 + fourth 176 = 46%)** — the chord-transition/bass factor signature, a FINDING declared to Cowork (a candidate for the OI-180 retirement-map fitted-transition refinement), the honest cost side of a strongly favorable trade (A removes 2.32M, adds 0.67M). Decode timing mean 3.285 s / max 18.0 s / total 1071 s (326 pieces, production scale). O-12 snapshot of the outgoing reference at `tools/robust_stop/snapshot_2026-07-26_pre_oi178_adoption/` (byte-identical copy; NO re-baseline). Artifacts: `tools/joint_estimator/adoption_record.json` + `adoption_record_summary.txt` + `adoption_setdiff.json` + `adoption_measure.py`/`_b.py` + `adoption_decode.json`; report `cc_adoption_measurement_report.md`. **NEXT: the user's adoption ruling — the funnel does not advance to the production wiring/re-baseline without it.**)*

*Last updated: 2026-07-25, second entry (CC — **★ TASK B — THE L1 FACT-SURFACE ADDITIVE EXTENSION: `notatedNotes()` PUBLISHED.** Dispatch `cc_instruction_joint_input_parity.md` Task B (the OI-180-sanctioned ADDITIVE extension — a module-private raw score walk stays forbidden). The L1 note model (`composing/analysis/notemodel/note_model.{h,cpp}`) now republishes, ADDITIVELY, the tie-UNRESOLVED notated atoms that the tie-resolved `NoteEvent` surface discards but the joint module's event lattice + emission covariates need: `notatedNotes()` returns EVERY notated note — tie STARTS and CONTINUATIONS alike — each with its OWN notated span (`Chord::actualTicks`, NOT tie-resolved), a `tieContinuation` flag (`Note::tieBack`), a `hasFermata` flag (a Fermata segment-annotation at the chord's track — music21's per-chord fermata), and `resolvedIndex` linking to its tie-resolved `NoteEvent` (the tie-start is the DOM's own `Note::firstTiedNote()`, resolved via a tie-start→event map). Built in the SAME single walk as the resolved events. **★ ONE SURPRISE caught + fixed (#3):** a first hand-rolled `tieBack()->startNote()` chain-walk INFINITE-LOOPED on the tupleted-Dvorak notation fixture (a degenerate/partial tie structure — consistent with a laissez-vibrer / partial tie whose raw chain does not terminate); replaced with the DOM's cycle-safe `firstTiedNote()` (#6: reuse the DOM tie API, never re-implement a DOM traversal). Lesson recorded. **Two proofs (the amended sanction):** (1) BYTE-IDENTITY for existing consumers — `notes()` and its emission order UNCHANGED (the resolved-event push is the same `makeEvent` + overlap test, only re-expressed to record the tie-start index); `composing_tests` **1146** (T1–T8 note-model + all others unchanged), `notation_tests` **53**, `pipeline_snapshot_tests` **11 UNTOUCHED** (no golden refresh). (2) FULL COVERAGE of the new paths — NEW `note_notated_tests.cpp` (3 tests): the tie chain (`nm_tie_chain`) publishes 4 atoms with correct spans / `tieContinuation` / `resolvedIndex` while the resolved surface stays 2 notes; the fermata flag (`pb_chorale`) fires per fermata-bearing chord and is false elsewhere; grace notes published + flagged. ARCHITECTURE.md L1 row synced (#10/OI-146). NEXT: Task C (the joint module's fact adapter consuming `notatedNotes()` + input parity vs `note_events.json` by divergence class + end-to-end parity).)*

*Last updated: 2026-07-25 (CC — **★ TASK A — THE §5 TIE-BREAK IN BOTH DECODERS + THE BIT-IDENTITY FIX → FULL-CORPUS DECODE PARITY 326/326 PATH-EXACT ON BOTH ARMS.** Dispatch `cc_instruction_joint_input_parity.md` Task A. The ratified §5 total order (fewer segments → earliest boundary-tick sequence → canonical class-key order; EXACT-score ties only, no epsilon) is implemented identically in `probe_decoder.decode_piece` (Python) and `jointdecoder.decodePiece` (C++) at every DP decision point (predecessor branches / kchg change-source / per-key representative / state replacement / terminal), a fast raw-score path with a lazy backpointer sig-reconstruction consulted ONLY on an exact bit-equal tie. §5 preserves the optimal score EXACTLY (0 score changes) and canonicalises the Python decode on **6 pieces** (identity `bwv362`; selected `bwv245.40`/`bwv362`/`bwv39.7`/`bwv437`/`bwv48.3` — all EQUAL-SCORE one-boundary shifts on repeated-chord runs). **★ THE 322/326 PATH RESIDUAL WAS A CROSS-LANGUAGE SUM DRIFT, NOT an epsilon tie — DIAGNOSED AND FIXED (the earlier "libm log" attribution was WRONG):** `std::log`==Python `math.log` bit-for-bit (80,961 values, `/fp:precise`), picojson==Python JSON double-parse bit-for-bit (every committed table double), every emission/spelling `distSum` order-invariant, `/fp:strict` makes NO difference. The ~1 ULP drift is **CPython 3.12+ `sum()` NEUMAIER (Kahan-Babuska) compensated summation** vs the C++ naive accumulation, in exactly the three decode-path float sums Python computes with `sum()`: `weighted_content` (5 weighted factors), the emission-floor `distSum`, and `cadence_at`. A naive C++ sum drifts ~1 ULP, flipping exact-tie-adjacent boundary decisions in the semi-Markov Viterbi. Fixed by mirroring Neumaier in C++ (`jointprimitives.neumaierSum`, used by `weightedContent`/`distSum`/`cadenceAt`), verified bit-identical to Python's `sum()` on every drifting segment. **RESULT (`joint_decode_parity.json`): 326/326 SEGMENT-EXACT, `max_rel_score = 0` (score BIT-IDENTICAL, not merely within 1e-6), on BOTH weight arms** (identity + the direct-metric selected vector). Re-grade of the 6 §5-changed pieces on the robust unit: **0.00 pp on every axis** (root / key-local / key-home / RN) — the shifted boundary lies within a repeated-chord run, so the graded cells are unmoved (well below the <0.05 pp expectation). `decode_parity_ref.json` regenerated under §5 (provenance re-stamped, deterministic — the authoritative §5 decode reference); the committed CV headline UNEDITED (`weight_search.json`/`search_grading.json` untouched — the Neumaier fix is C++-side only, Python already used `sum()`). Deferred/noted: `probe_corpus_decode.json`'s one `bwv362` identity entry is stale-under-§5 (it carries non-deterministic per-piece timings, so a full refresh would churn 326 spurious diffs; not consumed by the unit test). The cached-lattice `maxplus_decode` (diagnostic-only, off the CV headline and the production/parity path) retains its pre-§5 branch-order tie-break — a deferred item surfaced to Cowork: §5-ify it (or relax `establish_decoder` to accept equal-score §5-equivalent paths) before the stability diagnostic is re-run. Suites green — `composing_tests` 1143, `notation_tests` 53, `pipeline_snapshot_tests` 11 UNTOUCHED (joint module dormant → production byte-identical, no golden refresh). NEXT: Task B (the L1 fact-surface additive extension), Task C (the fact adapter + input parity by divergence class + end-to-end parity).)*

*Last updated: 2026-07-20 (CC — **★ ALGORITHM COMPLETION STEP 2, THE WEIGHT FIT: RUN, ESTABLISHED, AND A STOP-FOR-REVIEW.** Dispatch `cc_instruction_weight_fit.md`; both user rulings executed (★R1 = W1 four-beat tritone window; ★R2 = C1 cap ≤ 14, the vector enumerated at **13 weights**). PYTHON-ONLY, instrument layer only — no `src/`, no build, no golden, no corpus regen, no re-baseline; pinned instruments import-only; the firewall grep-proven (`fit_weights.py` imports NO grading module; grading is confined to `fit_run.py` and feeds nothing back). **THE HEADLINE (pooled 5-fold CV, each piece decoded once by the model — tables AND weights — fitted on the complement of its own fold; piece-bootstrap 95 % CI, paired across arms): key-LOCAL 70.22 [68.27,72.26] vs identity 74.68 → paired −4.45 [−5.47,−3.46]; key-HOME 56.34 [53.04,59.65] vs 53.20 → +3.16 [+1.75,+4.52]; root 73.04 [70.92,75.26] vs 72.70 → +0.34 [−0.08,+0.79] (inside the CI, NOT a finding, #24); RN 57.99 vs 60.08 → −2.09 [−2.70,−1.45].** Publishable all-326 model beside it: 70.47 / 56.64 / 73.07 / 57.54. **THREE OF FOUR WRITTEN BANDS MISSED — and the load-bearing key-local prediction is INVERTED (predicted 77–85 UP; measured DOWN below the generative baseline).** Per the dispatch this is a STOP: nothing is built on the fitted weights. **The fit itself is sound and established (#19):** the lattice max-plus recursion reproduces `decode_piece`'s Viterbi **exactly** (max\|δ\| 0.0 — so Z is over the decoded lattice), forward logZ == backward logZ (0.0), the GT path lies inside Z, the analytic gradient matches central finite differences to **2.3e-10**, and every fold converged (monotone iterate objective, \|grad\| 2.3e-06–1.6e-05); the objective improved 2.550 → 1.264 nats/token (fold 3). Weights are stable across folds (emission 0.316±0.007, boundary 0.774±0.012, chord_trans 0.276±0.008). **MECHANISM ESTABLISHED, named-piece (`fit_mechanism_report.json`):** only RELATIVE weights decide a decode; the fit up-weights key_trans **1.57×** and boundary **2.38×** vs emission, the key table's stay cell dominates, and **key changes fall −70 % (90 → 27) on the 14 named largest-movement pieces, 5 collapsing to ONE key for the whole piece** (bwv331, bwv9.7, bwv326, bwv57.8) — a never-modulating decode agrees with the global key by construction and disagrees with every local-key region, which is exactly the +home/−local split. **Structural diagnosis (#3/#13): maximizing the conditional likelihood of the GT PATH is not the same objective as maximizing duration-weighted per-cell agreement; that proxy→target link (#17d) was never a ledger premise and is now refuted on two of four axes.** NEW REGISTER ROWS: **OI-187** (the band miss + mechanism + structural options — STOP-for-review), **OI-188** (the prune reaches only ~72 % of GT spans; GT key widened on 3 135 spans, state force-added on 14 257; prune cost AT FITTED WEIGHTS +7.04 pp key-local / +9.31 pp key-home vs +4.8 at identity — the fitted model is hurt ~1.5× more), **OI-189** (42 % of GT chord transitions route through the ratified leftover rule — 6 356/15 050), **OI-190** (cad_leading_tone 0.007±0.029, sign-unstable, vs the desk-sim provisional +0.9 — premise P8 doing what it predicts; only cad_tritone_pair 0.314±0.046 carries load). **★R1 measured in isolation over the step-1 committed path (read from `c6ae08bd45`): tritone-pair 589 → 9084 fires / 13 556 boundaries, the other three features +0**; the desk-sim mechanism-fires establishment goes **11/12 with one exemption → 12/12 exact, exemption used 0×**, and the S4/S5 non-firing guards survive the widening. **Algorithm completion is metric-neutral at identity**: the fermata cells crossed with beat class (the ratified §3 items 7+8 boundary form; the crossed table shows the fermata effect is concentrated at sub-tactus 0.494 vs 0.273 and nil at downbeat — the step-1 marginal 0.809/0.629 was largely a beat-class confound) move the identity arm by ≤0.09 pp on every axis (74.42/53.69/72.88/59.49) — METRIC-neutral but NOT decode-neutral: the committed path changes on **90 of 326 pieces**, and the aggregate is unmoved, so the cells redistribute rather than improve. Sensitive cases: S5 deceptive-cadence verdict PRESERVED under fitted weights (Cmaj, margin +4.93 → +3.67); bwv88.7 — the probe's named dominant error — improves under fitted weights on the all-326 tables (key-local 22.9 → 31.4, root 60.0 → 67.1, RN 28.6 → 41.4); bwv10.7 degrades (key-home 22.7 → 9.1). Chromatic classes remain 0 segments decoded at identity AND unwoken by the fit. GT-path construction: 319 stems, 516 units (197 resuming after an unlabeled gap), 17 264 GT segments of which 179 (1.04 %) from the seg-cap split, 2 classes outside the vocabulary, 2 units dropped unscorable — all reported, none silent. Decode timings mean 2.79 s (fitted) / 2.72 s (identity) / 2.98 s (all-326), max 15.3 s. **NEXT: the user/Cowork ruling on OI-187 — the funnel does NOT advance to the production build on these weights.** Artifacts: `tools/joint_estimator/weight_fit.json`, `fit_grading.json`, `fit_grading_summary.txt`, `fit_mechanism_report.json`, `cadence_window_report.json`.)*

*Last updated: 2026-07-20, second entry (CC — **★ THE DIRECT-METRIC WEIGHT SEARCH (the ratified fallback): RUN, ESTABLISHED, AND A NARROW STOP-FOR-REVIEW.** Dispatch `cc_instruction_direct_metric_weight_fit.md`; user ruling **★R = M2** executed (the objective is the equal-weighted sum of duration-weighted ROOT and KEY-vs-LOCAL disagreement on training folds; RN and key-home tracked beside, never inside it). PYTHON-ONLY, instrument layer only — no `src/`, no build, no golden, no corpus regen, no re-baseline; pinned instruments import-only; the firewall grep-proven (`grade_stems` is the one grading site, `training_stems` the one stem source returning `fold_of[s] != fold`; `search_direct.py` never constructs a held-out stem list; held-out evaluation is the separate `search_run.py`, once per fold, feeding nothing back; selection is `select_optimum` = best TRAINING R). **THE HEADLINE (pooled 5-fold CV, each piece decoded once by its own fold's complement model; piece-bootstrap 95 % CI, paired): key-LOCAL 77.60 [75.72,79.46] vs identity 74.68 → +2.92 [+2.06,+3.77]; key-HOME 56.46 [53.76,59.14] vs 53.20 → +3.27 [+2.40,+4.17]; root 74.19 [72.11,76.43] vs 72.70 → +1.50 [+0.82,+2.17]; RN 61.77 [59.73,64.02] vs 60.08 → +1.68 [+0.93,+2.46] — ALL FOUR AXES IMPROVE OVER THE GENERATIVE BASELINE, every interval excluding zero.** Against the likelihood arm (OI-187): key-local **+7.37** [+6.16,+8.65], root +1.15, RN +3.77, key-home +0.11 (inside the CI, not a finding). Publishable all-326 beside: 77.41 / 56.16 / 74.86 / 62.25. **THREE OF FOUR WRITTEN BANDS HOLD** (key-local 74–82 ✓ 77.60; root 72–77 ✓ 74.19; RN 59–66 ✓ 61.77); **key-HOME 57–68 is MISSED at 56.46 — a STOP-for-review per the dispatch, though the miss is NOT statistically resolvable (the CI spans the band edge, #24) and the axis still improved +3.27.** **★ THE OI-187 MECHANISM DOES NOT REAPPEAR:** key changes/piece **5.52** against the ground truth's **5.28** with 9 zero-modulation pieces, where the likelihood arm sat at 4.05 with **43** collapsed pieces and identity over-modulates at 7.28 — the direct arm is the closest of any arm to the ground-truth modulation rate. **THE WEIGHT READING:** the metric strengthens the NOTATED evidence — declared_mode **2.94 ± 1.32**, spelling 1.41, signature prior 1.39, key_trans 1.36 — and discounts the learned sequential/positional machinery — bass **0.136** (÷7), boundary **0.210** (÷5), chord_trans 0.357, entry 0.405, emission 0.79. **CADENCE (OI-190 SHARPENED — a REVERSAL): cad_tritone_pair is pinned at exactly 0.000 in all five folds AND the all-326 model, as is cad_fermata_location** (the metric rejects both outright), while cad_leading_tone 0.202±0.149 and cad_dominant_tonic_bass 0.610±0.626 carry what load there is — the exact reverse of the likelihood fit, where tritone_pair was the only feature carrying load; two objectives disagree completely, so the four features look mutually substitutable and individually unidentified (stronger than premise P8 predicted). **★ NEW REGISTER ROW OI-191 — THE SURFACE IS RUGGED AND THE SEARCH UNDER-DETERMINED:** 21 restarts → 21 materially different optima, training-R spread 0.019–0.058 per fold (0.077 all-326), only **1–2 of 21** within 1e-3 of best; the identity start won **ZERO** folds (ranks 5/13/19/5/14) and selection came from a seeded-random start in 4 of 5; **held-out key-local varies 3.38–5.42 pp across a single fold's 21 optima**, so best-of-21 is a selection over ~21 effective degrees of freedom stacked on the 13 parameters — a capacity cost the OI-177 budget never covered (it was written for a CONVEX fit with one optimum). Corroboration: fold2's selected vector is qualitatively different (declared_mode 0.370 vs ~3.1–3.9; spelling 2.639; key_trans 2.649) yet grades similarly. Training gain reproduces tightly: identity R 0.515–0.537 → best 0.463–0.485, gain 0.0499–0.0543 across five independent complements. **SENSITIVE CASES:** `bwv352` byte-unchanged (39 segs, keyL 92.7, the C2 probe-tick reading identical under both arms — the honest near-tie undisturbed); `bwv10.7` MERGES 49→38 segments (the direction the discounted boundary weight predicts) with keyL 61.4→**70.5**, root −2.2, RN −2.3; `bwv88.7` keyL 22.9→**70.0**, root 60.0→71.4, RN 28.6→55.7, the opening re-read from D major/VI to **B minor/I** (same root pc 11, correctly re-anchored); **S5 deceptive cadence verdict PRESERVED** (Cmaj, margin +4.93→+3.35) even with two cadence features at zero. **OI-188 UPDATED:** prune cost at the selected weights key-local +2.15 / key-home +3.22 / RN +1.19 pp — materially LESS prune-sensitive than the likelihood weights (+7.04/+9.31) and than identity on key-local (+4.8); **root −0.58 pp is unexplained and carried as a small anomaly**. **ESTABLISHMENT ALL PASS (#19/#16):** the cached-lattice decode reproduces the pinned `decode_piece` exactly — score AND segmentation — at identity + 4 random vectors (20 checks) and at the SELECTED weights (20 checks), 0 mismatches, at 17 ms vs 2.9 s per decode (167×, which is what made 46,597 objective evaluations affordable); scale invariance 60 checks 0 path changes — **so the declared [0,5] box constrains exactly NON-NEGATIVITY, its upper bound cannot bind, and no clip ever fired**; the identity and likelihood arms both reproduce the committed `fit_grading.json` per-piece counters exactly on 326 pieces; **byte-reproducibility PASS** — fold0/start0 re-run alone reproduced the weight vector, R, evaluation count and all 26 accepted trace moves identically. Method: coordinate line search on a declared multiplicative grid with 21 seeded restarts — the published minimum-error-rate protocol (Och 2003, ACL) — with the grid declared in the artifact as the ONE approximation to Och's exact upper-envelope line search. Cost: 46,597 evaluations, 140 core-hours, 8.0 h wall over 19 processes; evaluation 5,416 s. Three post-launch instrument edits declared in full (unused import removed; a docstring corrected; the dispatch filename corrected in the provenance label) — none touching arithmetic, constants or control flow, and the byte-reproducibility check re-ran under the committed code. **NOTHING IS ADOPTED: the OI-178 robust-stop protocol has NOT been run** — no full-corpus a8 measurement, no `tools/robust_stop/` re-baseline, no class-(b) duration diff, no snapshot; these CV columns are on the joint estimator's own decode, not on the committed pipeline. **NEXT: the user/Cowork ruling on the §10 agenda — the key-home band miss (OI-187's successor STOP), OI-191's selection-effect capacity question, and OI-190's cadence form question.** Artifacts: `tools/joint_estimator/weight_search.json`, `search_grading.json`, `search_grading_summary.txt`; report `cc_direct_metric_search_report.md`.)*

*Last updated: 2026-07-20 (Cowork — **the joint estimator's full pre-production arc is COMPLETE through the C++ module's decode parity, and the fit story is closed:** the likelihood weight fit STOPPED itself (OI-187 — the path-likelihood objective rewards never-modulating; refuted premise recorded), the user ruled the ratified direct-metric fallback with objective M2 (root + key-local equal-weighted), and the fallback DELIVERED (`3ff9017f4d`): pooled 5-fold CV — key-local 77.60, key-home 56.46, root 74.19, RN 61.77 — beating the generative baseline on all four axes with every paired interval excluding zero, modulation rate 5.52/piece vs ground truth 5.28 (the OI-187 mechanism did not reappear). The four review rulings (2026-07-20): key-home band miss accepted-and-recorded (not statistically resolvable, axis improved +3.27); OI-191 figures stand but individual weight values are UNIDENTIFIED (standing caution against reading any single weight as musical fact); the two zero-pinned cadence features stay wired (form question deferred); the production build OPENED. The C++ module landed in three local commits (`803ef9e2ae`…`e0efd495f1`, push riding the next dispatch): `src/composing/analysis/joint/`, score parity 326/326 at machine precision on both arms, path parity 322/326 with the residual PROVEN equal-score floating-point ties, include closure clean of all legacy analysis. Three further ratifications: the mode-marginal + parity-reference artifacts; the §5 TIE-BREAK rule (platform-stable decode, both decoders); Task 1 = the L1 fact-surface EXTENSION (additive publication; sanction amended with two proofs per commit) — module-private raw score reading forbidden. **ACTIVE: `cc_instruction_joint_input_parity.md`** (tie-break → 326/326, L1 extension, input parity by divergence class, end-to-end parity, push). THEN: the OI-178 adoption measurement on the full pipeline — the only remaining gate between the dual path and production.)*

*Last updated: 2026-07-19, third entry (Cowork — **the four pre-fit gates are DRAFTED and USER-RATIFIED (`cowork_prefit_gates.md`)** — the OI-176/OI-177/OI-178/OI-180 protocols, each row flipped to "protocol ratified — pending execution". OI-176: 5-fold CV grouped by shared WiR analysis file (326 pieces → 324 files), committed seed/assignment artifact, every fitted object train-fold-only (incl. the degree-vocabulary threshold, smoothing, L2), headline = pooled CV figure + piece-bootstrap 95 % CI, identity-weight ablation through the same folds, BCMH off-CV; boundary/metric counts gated on OI-184. OI-177: pre-fit parameter-inventory artifact, cell own-MLE iff count ≥ 20 else declared-parent pooling, free params ≤ tokens/10, ≤ 12 weights, the desk-sim §4.3 sensitive cells get named treatment records + post-fit prediction-first re-check of C2/C3/S5. OI-178 (written before any live diff, #22): A commits MAP (no abstention; the OI-33 flag must read 0); PASS = key columns beat baseline beyond CI on every preset + root/RN non-degrading + class-(b) duration NET DECREASE per preset with every added run enumerated/classified and every added class-(b) diagnosed (undiagnosable ⟹ STOP) + O-12 snapshot + one revertible user-ratified adoption commit. OI-180: sanction in force from the build dispatch — A isolated on the L1/L1.5 fact surface, production byte-identical behind a default-OFF driver, side-by-side full-surface grading, six-item retirement map (incl. the deferred Gates A–L dissolution) post-adoption only, declared reverse map, DT-13 visibility guard. Ratified [prov-ratify] constants: 5 folds / count ≥ 20 / params ≤ tokens/10 / 95 % CI. **NEXT: the funnel's probe/build arc may open under the OI-180 sanction — dispatches written just-in-time; the first concrete steps are the OI-176 fold-assignment artifact + the OI-177 count-inventory instrument (read-only), and OI-184's alignment establishment.** Commit dispatch: `cc_instruction_prefit_gates_commit.md`.)*

*Last updated: 2026-07-19, second entry (Cowork — **the desk simulation of the ratified factorization is RUN and USER-RATIFIED (`cowork_factorization_desk_simulation.md`) — the #17c stage of the joint-estimator funnel is passed; OI-181 discharged.** All corpus facts verified at committed sources (incl. the OI-142 +2 offset on bwv145.5); identity weights (= the mandated generative-baseline form); all table values declared provisional. Outcome: **9 of 10 traces pass as specified** — S1 cadence commits (margin 6.8 nats); S2 relative-pair holds a 0.07-nat tie until the leading tone then flips 6.2 retroactively (#12); S3 Dorian-notated content beats the weak prior by 8; S4 tonicization resolves by transition economics (6.7); S5 deceptive cadence stays in key (4.9); C1 `bwv145.5@12960` yields (E major, V6) with NO signature-mask special form — the emission alone rejects the old Ebm by 2.0 nats even inside the wrong key; C4 `bwv110.7@2880` flips to b minor by 5.1 via degree-economics + A♯ spelling + cadence; C2 `bwv352@1440` is an honest near-tie (correct for the pc-identical pair); C5 `bwv254` reproduces the GT analyst's dual d/F reading within 1 nat, declared-mode input +0.59 in the right direction. **The tenth (C3 `bwv10.7@36000`) surfaced the stage's one SURPRISE (#13): the score form's factor granularity (per-segment vs per-event) was under-specified and decides merge-vs-split by ~6.6 nats against GT — AMENDED with ratification** (factorization doc §2: bass per event per Ni's form, missing-tone penalty per event of length; residual gap 1.3 nats rides two fittable values). **Also settled with ratification: the signature/declared-mode prior is INITIAL-STATE-ONLY** (re-anchored at a notated signature change; persistent-pull rejected — S3/C5). Register: OI-181 ✅; NEW OI-184 (WiR anacrusis alignment convention — establish before fit counts), OI-185 (bwv352 b4 bass one-note check); the sensitive-cell record (`§4.3`) feeds OI-177. Erratum: `cc_uncertain_resolver_measurement_report.md`'s "C-D-E-F-G" for bwv10.7@36000 is E♭ at the data (mask 173) — the TRACKED record is the desk-sim doc §4.6 (in this commit); a dated courtesy note was added on disk to the report itself, which is and stays UNTRACKED per the `/cc_*.md` ignore policy (CC-surfaced 2026-07-19: the report was never committed; force-tracking it for one line was declined). **NEXT: the pre-fit gates — OI-176 (held-out split), OI-177 (capacity budget), OI-178 (robust-stop adoption protocol), OI-180 (dual-path sanction + retirement map) — Cowork-drafted for user ratification. The funnel stays SHUT — no build until they pass.** Commit dispatch: `cc_instruction_desk_sim_commit.md`.)*

*Last updated: 2026-07-18 (CC — **the term inventory (`cc_instruction_term_inventory.md`): the code-enumeration half of the term-level theory-grounding audit is DELIVERED.** Report `cc_term_inventory_report.md`; generated artifact `tools/term_inventory/` (`gen_term_inventory.py` → `term_inventory.csv` 95 hand-verified term rows + `term_inventory_summary.json`, every figure mechanical, #17f). **80 live / 15 dormant terms across L1.5–L5 + the bridge**, each with file:line, form, constants+provenance, consumers, and the §2 roster-factor mapping. Cross-checks: template staleness **PASS** (17==17==17); **OI-23 measured** — 57 live chord-surface manifest rows (G1+G7 core = 31 ≈ the "~30"); **DT-26 tree-wide sweep** 0 undispositioned hit files (generator fails otherwise) — surfaced the un-audited implode-bridge exposure constants (**OI-182 NEW**) and the scoring_model.md symbol-coverage gap (**OI-183 NEW**). Gap map: only the fermatas/phrase-facts factor has zero live term, but the cadence factor has no key-axis term (OI-166), the spelling factor is chord-level only (the L3 emission is spelling-blind), the progression-grammaticality factor is three narrow fragments (the licensing grammar is dormant), the segmentation factor has no duration model, and the pitch-content emission has no live NCT-cleaning; 26 terms map to NO roster factor (tonic/degree priors, rotation gates, carry/abstention policy, key transition costs). **OI-179 census: 87/326 covered stems carry a second analysis (`analysis_BCMH.txt`)**; DCML bach_chorales repo has 0 harmony labels; counts only, grading not built. NO STOP: nothing new implicates live inference correctness beyond OI-170/172/173/174/175 (each re-verified at its site). No behavior change anywhere — no `src/` edit, no golden, no corpus, no re-baseline. NEXT: Cowork's theory-derivation half against this inventory; then keep/fix/drop triage + the design-pass decisions (mode vocabulary OI-174/OI-132/OI-147 — the USER's) with both halves on the table.)*

*Last updated: 2026-07-19 (Cowork — **the term-level theory-grounding audit is COMPLETE (both halves) and the FIVE design-pass decisions are USER-RATIFIED.** The audit: code half `cc_term_inventory_report.md` + `tools/term_inventory/` (95 terms; commit `abca1ff2c3`); theory half `cowork_term_theory_grounding.md` (five deep-research passes, primary sources fetched; per-factor forms F1–F10, the ground-truth-ceiling findings, the fitting evidence). Research library: `docs/research_papers/` (README index + BIBLIOGRAPHY register; binaries git-homed in the PRIVATE repo `slimvince/research-papers` only — nothing redistributed; fork guarded by `.gitignore`, commit `4f2c5ddfdb`). OI-179: literature half answered (NO published inter-annotator agreement figure exists for classical symbolic Roman-numeral analysis anywhere); BCMH inspected at the files (independent origin, single reading, no annotator record — the 87-stem agreement measurement is feasible and would be novel). **The five ratified decisions (full record: `cowork_joint_estimator_architecture.md` §5a):** (1) mode axis = {major, minor}, modal color in the emission, un-rounded modal reading PUBLISHED for the presentation layer; (2) staged fitting — generative tables from counts (frozen) + convex conditional-likelihood fit of the few combination weights, identity-weight generative baseline as mandatory ablation; (3) chord state is SCALE-DEGREE-VALUED (Roman numeral relative to tonic and mode; chord symbol a derived published fact; applied-degree classes for tonicization) — the tonic/degree coupling terms dissolve structurally, transposition tying pools all keys' counts; (4) non-chord tones as emission categories with chord-independent melodic/metric covariates, NO live cleaning stage, ornament labels derived post-decode; (5) the signature/declared-mode prior is a weak FITTED soft prior with no conditional gate — influence confined to ambiguous regions by the probability calculus, signature-influence rate published at every fit, the declared-mode wall retired. **AND: the factorization specification is USER-RATIFIED same day — `cowork_joint_estimator_factorization.md`** (semi-Markov segmentation over the event slices; state = 24 keys × a ground-truth-derived Roman-numeral vocabulary; the ten-factor log-linear score whose identity-weight setting IS the generative baseline; the premise ledger P1–P8 with labels and false-negative paths; exact block-factorized semi-Markov Viterbi with full posterior retained; the desk-simulation forms discharging OI-181 — five synthetic cases + five named corpus traces incl. `bwv145.5@12960`, `bwv352@1440`, `bwv10.7@36000`). **NEXT:** run the §6 desk simulations on paper (a session, no code), then draft/ratify the pre-fit gates (OI-176 held-out split, OI-177 capacity budget, OI-178 robust-stop adoption protocol, OI-180 dual-path sanction). The estimator funnel stays SHUT — no build until those pass.)*

*Last updated: 2026-07-18 (CC — **the doc split (`cc_instruction_doc_split.md`)**: `STATUS.md` and `cowork_handoff.md` split into lean active must-reads + verbatim reference-only archives (`STATUS_ARCHIVE.md`, `cowork_handoff_archive.md`); every older dated entry and historical section moved verbatim, nothing lost — the partition was reconciled byte-exact against the originals before commit; `CLAUDE.md` session-start references updated in the same commit. The 2026-07-18 principles #20–#24 / OI-176…OI-181 ratification was committed separately first per the dispatch's Task 0, commit `06d4318bd1`.)*

*Last updated: 2026-07-17 (Cowork — **★★ THE ARCHITECTURE DECISION: THE KEY/MODE/CHORD ESTIMATOR IS JOINT (option A), USER-RATIFIED.** Governing document `cowork_joint_estimator_architecture.md`; cited theory basis `cowork_key_chord_joint_inference_grounding.md` (5-stream primary-source literature review). Key, mode, and chord are inferred by **ONE joint probabilistic estimate** — a single decode over `(tonic, mode, chord)` with segmentation as a modeled (semi-Markov) variable and every enumerated clue entering as a theory-grounded factor — **NOT** a feed-forward pipeline and **NOT** a separable "key layer." This **supersedes the incremental framing of `cowork_key_layer_design_opening.md`**. **Why:** OI-175 established that the committed chord depends on the *provisionally inferred key* (the L2 segmenter's head-gap prior uses `resolveKeyAndModeRanked().front().mode`), so **L4 cannot be made tonic-independent** — the current architecture is an ad-hoc provisional-then-refine bootstrap of the coupling the problem demands; and the research shows the key/mode/chord mutual dependence is the *established* structure of tonal analysis, with the best methods (Raphael-Stoddard composite `(tonic,mode,chord)` Viterbi; Ni et al. key-conditioned chord transition; the multi-task lineage) inferring them **jointly**. Judged on long-term inference accuracy under our own methodology, no argument survives for feed-forward (loses information early, #12) or a learned black box (un-establishable/undiagnosable, #1/#3/#18/#19); B and C survive only *inside* A (approximate inference for tractability; a learned factor only where theory supplies no form). **REFRAME (architecture doc §3):** the open OI items become A-factors — OI-166 → the L1.5 cadence factor; OI-168/OI-170 → the signature-mask collection emission term (right form); OI-174/OI-147 → A's mode-vocabulary/mode-emission question; OI-175 → superseded by A's joint decode; OI-23 (~30 hand-set chord constants) + OI-91/OI-97 (hand-set key change-costs) → retire into A's fit-once factors. **OI-145 wave-1 (the measurement chain) is RETAINED in full** — it is how A is graded. **★ IMMEDIATE NEXT ACTION — the term-level theory-grounding audit (READ-ONLY; the #17 funnel's first stage; NO building):** enumerate every factor (roster in the architecture doc §2), derive each *form* from established theory/research (#1/#2), triage the current terms keep/fix/drop, name the few forms where theory runs out (#5). It answers "do we have the right terms with the right forms" (current answer: no — several are known-wrong and it was never audited). **The mode-vocabulary decision (OI-174/OI-132/OI-147) is the USER's.** **DISCIPLINE:** build A *right*, not now — forms from theory, values fit ONCE globally against GT (#19) and frozen, NEVER per-case tuned (#8, DT-2 firewall); the joint model is the antidote to information loss (#12). **PENDING (drafted, not run): `cc_instruction_doc_split.md`** — split this file + `cowork_handoff.md` into a lean active must-read + a reference-only archive (both are huge). Full arc in `cowork_handoff.md`'s 2026-07-17 entry. The key/mode/chord estimator funnel stays SHUT until the forms are pinned and ratified.)*

*Last updated: 2026-07-16 (CC — **OI-170 — THE WHOLE-LAYER TONIC-USE MAP OF L4: ENUMERATED, CLASSIFIED, MEASURED. DEFAULT-OFF; NO FIX PROMOTED.** Report `cc_oi170_measure_report.md`; every figure cited from the generated artifact `cc_oi170_measurements.txt` (#17f). Dispatch `cc_instruction_oi170_measure.md` (Cowork 2026-07-13). **THE DEFAULT PATH IS UNCHANGED**: with both flags unset the instrumented binary regenerates the committed corpus **352/352 byte-identical on all three presets**; `composing_tests` **1103**, `notation_tests` **53**, `pipeline_snapshot_tests` **11** — green, **no golden refreshed**, `tools/robust_stop` and `tools/corpus` untouched (every arm wrote to a scratch dir). **★ THE ANSWER TO THE DISPATCH'S QUESTION: NO — L4 CANNOT BE MADE FULLY TONIC-INDEPENDENT.** The mechanical sweep found the **three** class-(a) collection-membership-through-the-tonic sites OI-170 named (`buildChordResult`'s `diatonicToKey`; Gate I's and Gate L's `invRootIsDiatonic`) — **and seven class-(b) sites where the tonic is genuinely needed, three of them LIVE and DECISION-BEARING**: Gate G-E (**58** winner swaps, Baroque), `applyTonicPriorToSparseChord` (**172/183/172** committed-QUALITY overwrites on the commit path — **OI-172**, named by no row until now), and the published `degree`. A degree is tonic-relative by definition; the signature mask cannot replace it. **THE COLLECTION QUESTION can be made tonic-free; THE DEGREE QUESTION cannot — and the degree question is currently wired into the IDENTITY decision.** **★ THE MAGNITUDE OF FIXING THE THREE class-(a) SITES: METRIC-NEUTRAL, AND CORRECT.** Under the default-OFF signature-mask variant: **ZERO committed-chord flips on every preset** (Gate I's verdict differed on exactly **1** candidate corpus-wide and `gateISwapDiffers`=**0** — another conjunct blocked it either way; Gate L's never differed). **Baroque + Default byte-identical 352/352**; **Jazz 9 files, and the ONLY JSON key that changes anywhere in the corpus is `diatonicToKey`** — **22 flags flip, every one `false→true`, every one toward-correct** (all 22 sit in `Altered` regions whose chords are plain diatonic triads/sevenths of their own signature). Hard stop on the variant corpus: run-diff **(+0/−0)** every preset, class-(b) root-disagree duration **δ=0**, class-(a) **δ=0**, every key column identical to the digit, `robust_stop_diff.py` **OVERALL PASS**. **So the recommended fix needs NO robust-stop re-baseline** — only a `tools/corpus/jazz` regen (9 files) + a manifest re-stamp whose figures do not move; goldens carry no `diatonicToKey` and no Altered key (verified). **★ THE `diatonicToKey` SINGLE-SOURCING IS NOT IMPLEMENTABLE AS SPECIFIED — and that is a finding (OI-173):** the tree holds **FOUR inequivalent definitions** of the flag and **TWO of `degree`** (parent scale vs the mode's own — they differ for all 14 non-diatonic modes), and most 're-derivations' answer **for a different key** than the one L4 committed under, so 'read the published fact' is not merely non-identical, it is **not well-defined**. What unifies is the **predicate**, not the value. The published flag has **ONE** reader tree-wide (`notationimplodebridge.cpp:1205`); `BaseRomanNumeral::diatonicToKey` has **none** (dormancy or waste). Also: **three byte-identical copies of the 21→7 parent table** (#6). **★★ TWO PREDICTIONS MISSED, BOTH DIAGNOSTIC (#3, #17b — predictions written before measuring).** (1) Flag flips **22 > predicted ≤18**: I assumed an `Altered` local key implies chromatic material. It does not — **22 of the 23 Jazz `Altered` regions contain NO pitch class outside their own signature's collection** (**OI-174**: the key layer emits an 'altered' mode over material with no altered tone — an INFERENCE problem, declared not fixed). (2) Site count **5 predicted, 10 found**. **★ AND THE SAME LESSON REPEATED INSIDE THIS SESSION — OI-175 (NEW).** The enumeration as first written swept only the dispatch's named L4 files and would have shipped 'nothing was left unclassified' while a live, chord-deciding tonic site sat two directories away. Running the identical pattern **tree-wide** (140 hits vs 118) and reading the 22-hit out-of-scope remainder file by file caught it: **`harmonicsegmenter.cpp:849-852` — the Iter-74 Fix-B head-gap tonic prior inside `greedyExpandSegmentation`, on the live region path — builds the mode tonic exactly as every δ-bug site does and OVERWRITES a synthesized head region's committed `rootPitchClass`, `bassPitchClass` and `quality` when the winner is not tonic-rooted and the margin is thin (<0.4).** It is **class-(b)** (it asks 'is this root THE TONIC?' — no collection can answer that), so it is **not** a δ-bug and the primitive cannot replace it; **its fire count is NOT measured** (instrumenting Layer 2 was out of scope). **It means fixing L4 would not make the COMMITTED CHORD tonic-independent — the THIRD live chord-deciding genuine-tonic site lives in the SEGMENTER (Layer 2), the first one outside L4** (the two inside are Gate G-E and `applyTonicPriorToSparseChord`), and it composes with OI-174 (a wrong `Altered` mode ⇒ a tonic a semitone off). Declared, NOT fixed, NOT built around. **A scoped sweep proves completeness of the SCOPE, never of the QUESTION** — the scope check is now part of the generated artifact (§SWEEP 1b/1c) so a future sweep cannot drop it silently. **★ PROVENANCE — THE FIRST MEASUREMENT WAS NOT ESTABLISHED, AND WAS RE-RUN (#16/#19).** The artifact's first generation came from a binary built at 12:05, **before** the last source edit to `chordanalyzer.cpp` at 12:22 — the stamp to the committed source was broken, so no figure it carried was established. The tree was rebuilt from the exact source being committed (the incremental build recompiled exactly **one** translation unit — confirming that edit was the only one outstanding) and **all nine corpus arms, the three suites and the hard stop were re-run. Every figure reproduced EXACTLY**, which *establishes* rather than assumes that the edit was non-behavioral. **Stamp the arm to the BUILD, not to the working tree** — a corpus run does not rebuild. **Register: OI-172, OI-173, OI-174, OI-175 NEW (all declared, none fixed); OI-170 measured — its 'five re-derivations of one published fact' premise is corrected by OI-173.** **NEXT: the user/Cowork disposition on the recommended fix** (one shared signature-mask primitive for the collection question + `diatonicDegreeForRootPc` for the degree question + retire the parent table; 0 flips ⇒ a 9-file Jazz corpus regen, no robust-stop re-baseline) — **the degree-basis correction is a SEPARATE change with its own measurement, not folded in.** The key-layer funnel stays SHUT.)*

*(All older dated entries — 2026-07-14 back to 2026-04 — and every historical section (old current-state summaries, roadmaps, iteration and deduplication records) are in `STATUS_ARCHIVE.md`, moved verbatim 2026-07-18. The ratified robust-unit baselines, the hard stop, and the gate policy live in `CLAUDE.md` §"Gate threshold and preset policy" block (A). Build/test commands: `BUILD_AND_TEST.md`.)*
