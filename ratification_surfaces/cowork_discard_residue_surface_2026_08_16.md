# The soft-discard's residue — what the guard withheld, and what the check over the 62 found

> **STATUS: RULED, 2026-08-17. NOTHING IS EXECUTED BY IT.** This surface was a ruling
> surface awaiting the user; **the user ruled it at the residue sitting of 2026-08-17,
> whose record is `cowork_rulings_2026_08_17_residue_sitting.md`** — the authority for what
> was ruled and what was not, none of which is restated here (#6). **The banner it replaces
> read "STATUS: RULING SURFACE, awaiting the user. NOTHING HERE IS RULED." — true when it
> was written, and made untrue by the sitting; the former rendering stands in git at the
> commits that carried it (#12).** No entry on this
> surface has been discarded, retired, edited, moved or marked BY THIS SURFACE, and the two
> derivations it reports performed no decisions-register mutation of any kind. Generated by
> `tools/audit/gen_ratified_document_check.py` from
> `tools/audit/ratified_document_check.json` and `tools/audit/sole_carrier_subclass.json`,
> so no count and no member list below is typed by hand (#17f). Per the standing
> presentation rule (`cowork_rulings_2026_08_15_batch_return.md` §5) every identifier used
> below is re-explained from scratch in §0 before any question rests on it.
>
> **The user's own clause, which binds every discard record and is quoted verbatim:** a
> soft-discard record is *"a provenance verdict, not a judgment on soundness or usefulness; the statement stands at its home and is met by the derivation"*.

## 0. The referents, re-explained from scratch (read this section first)

- **The decisions register** — the project's record of WHAT WAS DECIDED about how this
  system works and whether each decision still stands. It has three surfaces: a data file
  (`tools/audit/decisions/backbone_decisions.json`), which is the only one ever edited;
  a rendered INDEX (`DECISIONS.md`); and rendered group files (`decisions/group_*.md`).
- **An entry** — one row of that record: a decision's title, the verbatim words it was
  recorded in at its home document, a plain restatement, where it is recorded, its status,
  the date and ratifier its original record states, and its recorded reasoning.
- **A deciding act** — the event in which somebody decided: a user ruling, a ratification,
  a shelving, a falsification. Naming one means naming who decided, or when, or at which
  record or event it is written down.
- **The filter** — the first of three passes. It read each entry's OWN FIELDS and proposed
  one class per entry: a deciding act is named (the KEEP side, which the user has since
  RATIFIED), no deciding act was found, or the evidence is ambiguous. The last two are the
  **non-keep population**.
- **The deciding-act recovery pass** — the second. For every non-keep entry it followed
  the entry's OWN cited sources through the record and searched them for a user act naming
  that entry's subject, returning ACT-FOUND (quoted and located) or NOTHING-FOUND.
- **Soft-discard** — retired from the live record, **not destroyed** (#12), and
  individually revivable the moment a deciding act is named. It is a PROVENANCE verdict
  and never a judgment on whether the decision is sound or useful.
- **The sole-carrier guard** — the third, and the subject of §2. A derived subclass of
  entries that may be the ONLY place their content is carried. Its members do NOT ride the
  discard: they come back to the user as the list in §2.
- **A subject recogniser** — a search pattern the decisions register carries FOR AN ENTRY,
  authored when that entry was written. Wherever this surface says an entry's subject was
  looked for, these are what was looked for, never this side's reading of what the entry
  is about.
- **The preparation phase** — the first of the six phases the user ruled on 2026-08-15
  (`cowork_rulings_2026_08_15_phase_definition_sitting.md`). Turning the project's own
  record into usable inputs for the later derivation; the filtered decisions register is
  the first of its named outputs.

## 1. What the user ruled, and what comes back here

The ruling is `cowork_rulings_2026_08_16_preparation_return.md` §3, and it has three
limbs. **(A)** the entries the recovery pass returned NOTHING-FOUND for are
SOFT-DISCARDED — *behind the sole-carrier guard*. **(B1)** ten clear recoveries are KEPT.
**(B2)** the remaining ACT-FOUND entries are NOT ruled until one further bounded
derivation has run. This surface carries the two things that ruling sends back to the
user, and nothing else:

- **§2 — the 29 entries the sole-carrier guard WITHHELD from the discard.**
  They are not discarded, and this surface does not propose that they should be.
- **§3 — the 62 entries of (B2), each with the
  result of the check the ruling ordered.**

**The discard arithmetic, to the digit, so the two halves can be checked against each
other:**

| | entries |
|---|---|
| the recovery pass returned NOTHING-FOUND for | 194 |
| of which the sole-carrier guard WITHHELD | 29 |
| leaving the executed discard population | 165 |

## 2. The sole-carrier guard — what it is, and every entry it withheld

**Why the guard exists, in the user's own question.** The user asked whether the discard
risks losing a genuinely good idea that should have been used as input when designing or
building the analysis. Three standing nets catch almost all of that — the disposition
discipline over every specification statement, the audit's something-missing verdict, and
the fact-gate — and ONE residual class escapes all three: an idea whose only carrier is
the decisions-register entry itself, typically a proposal that was deferred and never
built. The guard is that class, derived.

**The rule, which has no threshold and no hand-picked member.** An entry is a
SOLE-CARRIER if ANY of three signals fires:

- **(i)** its recorded status is `deferred` — the value the decisions register's own
  header defines as *"decided to be built later; the decision itself stands"*;
- **(ii)** its home cannot be located — the document is not in the tracked tree, or the
  cited line is one that document does not reach, or the recorded section's heading is
  not in it;
- **(iii)** its content is found nowhere outside the decisions-register family — the
  entry's verbatim quotation, normalized by the decisions register's OWN normalization,
  is not in any searched tracked file outside `DECISIONS.md`, `decisions/` and
  `tools/audit/`.

**What each signal found over the whole non-keep population (266 entries):**

| signal | entries it fired for |
|---|---|
| (i) the recorded status is DEFERRED | 39 |
| (ii) the home cannot be located | 0 |
| (iii) the content is found nowhere outside the decisions-register family | 0 |

**★ TWO OF THE THREE SIGNALS CAME BACK EMPTY, AND THAT IS A RESULT RATHER THAN A
MALFUNCTION.** Each of the three is shown, in the artifact, BOTH to fire on a case that
should fire it and to stay quiet on one that should not — so an empty signal cannot be
mistaken for one that cannot fire at all (#19). What the two empty ones say is that every
non-keep entry's home resolves at the measured commit and that every one of their
verbatim quotations is carried outside the decisions-register family. That agrees with
two standing checks that assert the same thing by different routes — the decisions
register's own establishment pass, which finds every verbatim at its cited home, and the
home classification, which re-derives every home section from the documents' own
headings — so it is corroboration and not an assumption.

### The 29 entries WITHHELD from the discard

Each is an entry the recovery pass returned NOTHING-FOUND for — so it would otherwise
have ridden the discard — and which at least one signal flagged. **They return to the
user as a list; nothing here proposes what should happen to them.** The ruling's own
provision is that the keepers are carried into the framework phase's candidate
enumeration as UNTRUSTED CANDIDATES, never as decisions.

- **D-054** — All 21 modes are scored against all 12 tonics; the harmonic major family is deferred
  - *What the entry says the decision is:* The key finder considers 21 scale types on each of the 12 possible tonics. The harmonic major family was left out because it is rare and we have no annotated music to calibrate it against.
  - *Recorded at* `ARCHITECTURE.md:3323-3325`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-136** — The inference demo view is a developer tool and is not shipped
  - *What the entry says the decision is:* The step-by-step view of the analysis making its decisions exists so a developer can watch and judge it by eye and ear. It is not part of what a user gets.
  - *Recorded at* `ARCHITECTURE.md:6021`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-137** — The harmony maps are our own visual design, and are chosen partly to avoid intellectual-property claims
  - *What the entry says the decision is:* The planned map of harmonic function draws on published chord-scale theory but is laid out our own way, not copied from the commercial product that inspired it.
  - *Recorded at* `ARCHITECTURE.md:6180`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-138** — Chord preview uses MuseScore's note-input pathway, not the playback pipeline
  - *What the entry says the decision is:* Clicking a chord on a harmony map plays it through the same quick path MuseScore uses when you hear a note as you enter it, not through full score playback.
  - *Recorded at* `ARCHITECTURE.md:6211`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-139** — The language model holds no object references - every tool call carries its own musical address
  - *What the entry says the decision is:* When a language model asks the program to do something, it names the place in the music each time. It never holds a handle to an object in the score.
  - *Recorded at* `ARCHITECTURE.md:8118`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-140** — The language model is a search agent and is never given the whole score
  - *What the entry says the decision is:* Rather than being handed the entire score, the language model is given tools to find what it needs and fetches it piece by piece - the way a person reads a large document by searching it.
  - *Recorded at* `ARCHITECTURE.md:8123`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-141** — The language model sees what the user set, not what the engraving engine derived
  - *What the entry says the decision is:* The model is shown the composer's own choices - pitches, dynamics, articulation, colour, lyrics, visibility - and not the results of laying the music out, such as positions, beam geometry or stem lengths.
  - *Recorded at* `ARCHITECTURE.md:8129`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-142** — The composing module is the language model's context provider; the model never re-derives harmony
  - *What the entry says the decision is:* Every stretch of music sent to a language model arrives with our harmonic analysis already attached - chord symbols, Roman numerals, key, harmonic rhythm. The model reads that; it does not work the harmony out from the notes itself.
  - *Recorded at* `ARCHITECTURE.md:8142`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-143** — The language-model bridge is built as a module but confined to the core access layer, so it can become a plugin
  - *What the entry says the decision is:* It is written inside the program for speed of development, but restricted to the same narrow interface a plugin would have, so that moving it out to a plugin later is straightforward.
  - *Recorded at* `ARCHITECTURE.md:8163`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-154** — New panels use MuseScore's own panel and interface infrastructure
  - *What the entry says the decision is:* Any new panel is built with the same window-docking and interface technology MuseScore already uses, after reading how MuseScore's existing panels are built. No parallel machinery is created.
  - *Recorded at* `ARCHITECTURE.md:7485`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-158** — Our data lives in separate files inside the score archive; the score file is never touched
  - *What the entry says the decision is:* Constraints, branches, cached analysis and preferences travel with the score as extra files inside its archive, beside the standard MuseScore score file, which our code never modifies.
  - *Recorded at* `ARCHITECTURE.md:7603`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-159** — Every custom file carries a format version, and the score file is never rewritten by our persistence
  - *What the entry says the decision is:* Each of our own files records which version of its format it is, so older files can be migrated when the format changes; the standard MuseScore score file inside the archive is never rewritten by us.
  - *Recorded at* `ARCHITECTURE.md:7623`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-160** — Arranger interactions are logged from the start, with consent, as future training data
  - *What the entry says the decision is:* Every suggestion a user accepts, changes or rejects is recorded - with their consent - as a labelled example for future machine learning.
  - *Recorded at* `ARCHITECTURE.md:7666`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-161** — Chord symbols already in a score are a second analyst's opinion, not ground truth
  - *What the entry says the decision is:* When the automated review meets a score that already carries chord symbols, it treats them as another analyst's reading. Disagreements are discussed, not scored as our errors.
  - *Recorded at* `ARCHITECTURE.md:7698`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-236** — Chord-symbol trust is per symbol, not a per-score preference
  - *What the entry says the decision is:* If written chord symbols are ever treated as authoritative input, the authority is carried by each symbol. A single switch for a whole score is rejected.
  - *Recorded at* `ARCHITECTURE.md:3495-3497`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-237** — Only a symbol marked trusted becomes analyzer input; an untrusted symbol is never read
  - *What the entry says the decision is:* Under the planned authoritative-symbol mode, a written chord symbol opens a region and names its chord only when it is marked trusted. An untrusted symbol stays comparison metadata and the analysis never reads it.
  - *Recorded at* `ARCHITECTURE.md:3506-3510`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-238** — Two pitch classes may nominate a chord but may not finalize one; one pitch class may not
  - *What the entry says the decision is:* In the monophonic fallback, a slice with only two distinct pitch classes can propose candidates but cannot settle the chord without context; a single pitch class cannot settle one at all and may only keep an existing reading alive.
  - *Recorded at* `ARCHITECTURE.md:3651-3655`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-239** — Chord identity stays local; expansion is by one neighbouring region and is bounded
  - *What the entry says the decision is:* When a group of notes is too weak to resolve on its own, the analyzer may take in one neighbouring region and score again. It stops as soon as confidence or the margin crosses its threshold, the winner repeats, or the expansion cap is reached.
  - *Recorded at* `ARCHITECTURE.md:3660-3667`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-240** — The monophonic smoothing terms are tunable parameters, not prose-only rules
  - *What the entry says the decision is:* The margins and thresholds that govern the monophonic fallback's smoothing are implemented as named settings, so they can be changed and measured rather than being buried in prose.
  - *Recorded at* `ARCHITECTURE.md:3677-3678`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-241** — The monophonic local-grouping problem is deferred to Phase 2
  - *What the entry says the decision is:* Deciding how to group a single melodic line into harmonic units is left to the later, full monophonic engine rather than attempted in the minimal fallback.
  - *Recorded at* `ARCHITECTURE.md:3707-3708`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-242** — Vertical and monophonic raw scores are never compared directly
  - *What the entry says the decision is:* The layer that combines the two chord engines may not put their raw numbers side by side. The two engines weigh different evidence, so their confidences must be calibrated onto a common footing first.
  - *Recorded at* `ARCHITECTURE.md:3735-3737`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-244** — Choosing an interval family for an ambiguous sonority is deferred; fixed tables are used
  - *What the entry says the decision is:* When more than one pure interval could be targeted - a 5-limit minor seventh against a septimal one, and the same choice for tritones, minor and altered sonorities - the tuning systems keep their fixed lookup tables. A style-aware choice is left for later.
  - *Recorded at* `ARCHITECTURE.md:6354-6363`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-245** — Voice role comes from staff position or explicit assignment; automatic melody detection is deferred
  - *What the entry says the decision is:* Which voice counts as the melody is taken from where it sits in the score or from what the user says. Working it out automatically is left for later, as is a per-staff override.
  - *Recorded at* `ARCHITECTURE.md:6477-6479`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-246** — Fixed-pitch instruments are deferred, and will never receive tuning offsets
  - *What the entry says the decision is:* Piano, organ and fretted guitar are not handled yet. When they are, they will be the fixed reference other instruments tune to, and will not be retuned themselves.
  - *Recorded at* `ARCHITECTURE.md:6578-6580`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-303** — Non-chord-tone detection is deferred, and if built it must be chord identification that knows about non-chord tones, never stripping after the fact
  - *What the entry says the decision is:* Deciding which sounding notes do not belong to the chord is postponed. When it is built, the knowledge must enter the chord decision itself; removing notes from an answer after the chord has been named is ruled out.
  - *Recorded at* `ARCHITECTURE.md:1963-1965`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-342** — Putting the function layer into production is DEFERRED INDEFINITELY — the posture is a dormant build with ground-truth validation
  - *What the entry says the decision is:* Switching the function layer on in the product is not scheduled. It is built and checked against published human analyses, and stays inactive; the conditions written for a switch apply if one is ever made, and are not outstanding work. THIS IS NOT THE INFERENCE-ENGINE SWITCH - that happened (D-010): the joint estimator is production and the legacy pipeline is dormant. This entry concerns the separate Layer-5 function-annotation module, built and validated but never in production in either era. Its concerns are handled by the LIVE implementation natively or by schedule: degree-in-key Roman numerals are the estimator's own state; key changes are decided inside the decode; applied-chord labels are emitted by the live renderer (the D-248/OI-267 revisit covers the remainder); cadence is a fitted factor inside the model and a marker on the presentation surface; carried-abstention resolution is obviated by the full-posterior publication (D-006); the ornament labels are the ratified OI-194 increment; and the complete concern-by-concern mapping of the legacy layer's remaining scope is the OI-259 phase-3 re-disposition.
  - *Recorded at* `cowork_layer5_function_design.md:705-707`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-358** — A sonority shaped like a dominant is note-level evidence for the tonality it implies, and belongs in the key/mode emission — deferred, design-first
  - *What the entry says the decision is:* As built, the per-stretch tonality score asks only how well the sounding notes fit each candidate's scale, and carries nothing about the shape of the sonority. But a chord shaped like a dominant seventh or a leading-note seventh points at one major and one minor tonic, and that is readable from the notes alone, before any chord is named — so it belongs in the tonality stage without breaking the evidence split. It is deferred, to be designed and measured before it is wired, and the measurement must include what it costs on passages of constant tonality, not only what it gains on passages that change.
  - *Recorded at* `cowork_layer3_keymode_design.md:523-536`; *status* `deferred`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-520** — Widening the loaded span was built DECOUPLED from the whole-score-read fix — superseding the recorded framing that the two were one coupled change
  - *What the entry says the decision is:* The note model still reads the whole score even when only part of it is asked about. That is interim. The operation that widens the covered music was expected to have to wait for it; it was built first instead, re-walking the whole score and filtering to the enlarged span, which produces exactly what a fresh build over that span would. The span-scoped walk is what remains.
  - *Recorded at* `cowork_layer1_note_model_design.md:230-238`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Signals that fired:* (i) the recorded status is DEFERRED
- **D-523** — If the algorithmic second opinion's LOCAL key is ever adopted it enters UNVALIDATED, and adopting it is a corroborator re-baseline under user ratification — not a refresh
  - *What the entry says the decision is:* Whether to consult the second-opinion library for a local, moving key rather than only a single key for the whole piece is left open. If it is ever taken up, the field arrives explicitly unvalidated and no consumer may lean on it until it is established. Turning it on changes the committed second-opinion files, so it is a deliberate re-baseline the user ratifies.
  - *Recorded at* `cowork_evidence_inventory.md:241-248`; *status* `deferred`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Signals that fired:* (i) the recorded status is DEFERRED

## 3. The check over the 62 — every entry, with what the ratified document said

**What was ordered, quoted:**

> for each entry whose recovered act is a document-ratification passage, the RATIFIED DOCUMENT ITSELF is checked for the entry's subject recognizers (the register's own, again); subject found and quoted, or not found

**Why the question is worth asking.** The recovery pass found, for these entries, a
passage in a document the entry itself cites that carries a user-act marker AND matches
the entry's own subject recognizers. Among them a recognizable shape appears: the
passage is one in which the user ratified a WHOLE DOCUMENT, and it matched the entry's
recognizers only because those words happen to occur in the ratifying sentence. Such a
passage is evidence about THIS entry only if the document that was ratified actually
carries the entry's subject. That is what was checked.

**How the population was derived, and why it is not typed anywhere.** It is the recovery
pass's ACT-FOUND entries minus the ten the (B1) limb KEPT. The ten are PARSED from the
ruling record's own bullet on every run; a bullet naming any number other than ten, or
naming an entry the recovery pass did not return ACT-FOUND for, halts the derivation
rather than being corrected.

- ACT-FOUND entries: **72**
- The ten KEPT by (B1): **D-004**, **D-008**, **D-009**, **D-021**, **D-123**, **D-248**, **D-315**, **D-403**, **D-410**, **D-411**
- Checked here: **62**

| result | entries |
|---|---|
| SUBJECT-IN-RATIFIED-DOCUMENT | 47 |
| SUBJECT-NOT-FOUND-THERE | 9 |
| NO-DOCUMENT-RATIFICATION-ACT | 6 |

**What each result does and does not claim:**

- **a SUBJECT IN RATIFIED DOCUMENT is not a verdict.** It is evidence that a document-level ratification reaches this entry's subject. Whether that makes the act a DECIDING act for this entry is the user's reading.
- **a SUBJECT NOT FOUND THERE is not a finding that the entry was never decided.** It says the ratified document does not carry the entry's own recognizers. An act recorded somewhere neither the entry nor this pass reaches is outside it by construction.
- **a NO DOCUMENT RATIFICATION ACT is not a weakening.** It says the shape this derivation was ordered to test is not present. The recovery pass's evidence for such an entry stands exactly as it stood.
- **nothing here grades a decision.** Not its content, not its conformance, not whether the code obeys it — and nothing here is a judgment on soundness or usefulness.

**★ WHERE THE RECOGNIZER IS WIDE AND WHERE IT IS NARROW, stated before the results.** A document is RATIFIED BY THE ACT when it is NAMED IN A SENTENCE OF THE PASSAGE THAT ALSO CARRIES A RATIFICATION WORD. The sentence, and not the whole passage, is the unit — and the narrowing is the point of this derivation rather than a refinement of it. The ruling asks what THE RATIFIED DOCUMENT says; a passage often names several documents, most of them mentioned rather than ratified, so admitting every name in the passage would send the search to documents the act never ratified and report their contents as if the act had ratified them. ★ WHAT THE WIDER READING WOULD HAVE ADMITTED IS NOT DISCARDED: every document named elsewhere in the passage is published per act, by name and counted, so the difference between the two readings is visible and a reader can ask for either. ★ WHERE THE NARROW READING IS STILL WIDE: any ratification word beside any document name inside one sentence admits it, which will call some sentences ratifications that a reader would not. That direction is the harmless one — a wrongly admitted document is then SEARCHED and reports either a subject match, which is evidence a reader can check at the quoted line, or SUBJECT-NOT-FOUND-THERE, which claims nothing. ★ WHERE IT IS DELIBERATELY NARROW AND THE LIMIT IS NAMED RATHER THAN PATCHED: a sentence ratifying a SECTION without naming its document is NOT admitted, because the document it would send the search to is the one the act already sits in, and that document was searched by the recovery pass. Such an act is counted and published below rather than silently dropped.

**★ COORDINATES THAT NO LONGER RESOLVE: 0 entries.** The recovery pass recorded a document, a line and a span. A document can move under those coordinates, and reading whatever now sits at a line would silently grade the wrong passage. Every re-read block is therefore compared with the quote that pass published; where they disagree the published quote is used and the entry is named here.

### SUBJECT-IN-RATIFIED-DOCUMENT — 47 entries

- **D-006** — The published uncertainty surface is two full candidate lists, with no truncation
  - *What the entry says the decision is:* For every chord it commits to, the estimator also publishes how every other tonality and every other chord would have scored - the complete lists, not a top-few.
  - *Recorded at* `ARCHITECTURE.md:58-60`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `cowork_handoff.md` line 1527 — matched by the decisions register's own recogniser `posterior slice` (`posterior slice`)
    > posterior slice (bit-parity); the record assembly (spelling establishment 99.985 %, modal counter); the seams (producer + views; consumer re-plumb P0–P7 with the §4.1 gap-scale constants, the P2a/P4 gate unifications, P-strings presentation formatters + the permanent inference↔presentation boundary guard, OI-204 input-scoping); P6's dual-arm classified evidence (identical 2053 / inference-driven 6029 / presentation-rule 220 / unexplained 0);
- **D-011** — The producer decodes the WHOLE score once, and does not cache
  - *What the entry says the decision is:* Every request analyses the entire score from the beginning, every time, and nothing is remembered between requests.
  - *Recorded at* `ARCHITECTURE.md:126-127`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 3, of which 3 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 205 — matched by the entry's own identity (`D-011`)
    > | OI-238 | §5.13's analyze-at-tick path table predates the record arm: it asserts "no path-selection flag" while a flag selects between two arms, and its entry-point list describes the arm that is not running | `ARCHITECTURE.md:3688-3690` states "no path-selection flag"; `notationcomposingbridge.cpp:728-738` branches on `useJointNotationRecord` (default ON, `composingconfiguration.cpp:178`) and returns from the record arm before any legacy code. Sharper consequence: the table's `analyzeHarmonicRhythm(score, start, end, …)` row is the only place a reader is told an analyze-at-tick path takes a TIME RANGE — on the record arm no such narrowing exists (D-011), which is the [[OI-212]] input-scope question arriving on the note seam through a stale table. Third item the table should record: D-063's revisit trigger was never discharged AND the P4 fallback is now unreachable from the production f
  - *Subject found in* `STATUS.md` line 744 — matched by the decisions register's own recogniser `WHOLE-score decode` (`whole-score decode`)
    > whole-score decode per single-note selection; mechanism field-established + measured ~5–27 s per query vs sub-microsecond memoized), the 3.1b contradiction owned (whole-score interactive was SHELVED-with-evidence in the legacy era; Cowork's seams dispatches specified it anyway — OI-207 decision-conformance audit + OI-208 decisions-register proposal rowed), and the marginals Tasks 1–2 delivered (the OI-193 oracle PASSES on all 326; C++ half queued with the
  - *Subject found in* `cowork_handoff.md` line 1540 — matched by the decisions register's own recogniser `WHOLE-score decode` (`whole-score decode`)
    > measured (the note-seam funnel runs one SYNCHRONOUS whole-score decode per single-note selection; measured ~5 s chorale → ~27 s sonata per query vs sub-microsecond memoized lookup). (2) **The 3.1b contradiction, a Cowork error owned:** whole-score interactive analysis had been A/B-FALSIFIED and SHELVED WITH EVIDENCE in the legacy era (`docs/p3_granularity_ab_3_1b.md`; archive :2957–2979; the Tristan review §7) — Cowork's
  - *Subject found in* `ARCHITECTURE.md` line 126 — matched by the decisions register's own recogniser `WHOLE-score decode` (`WHOLE-score decode`)
    > `assembleNotationRecord` (which attaches the §3.3 slice). WHOLE-score decode ONCE; deterministic; NO caching (a later, measured concern — #17's funnel, not built speculatively). It returns a `NotationRecordResult` — either the full record or an UNAMBIGUOUS failure (`ok=false`, `error` set, empty record) when the fact adapter cannot extract the score (`AdapterFacts.ok == false`, e.g. a null score): never a partial record, never a silent fallback (#13). **Input-scoping (OI-204):** `produceNotationRecord(score, stem, excludeStaves = {})` forwards `excludeStaves` to
- **D-013** — Which staves feed the analysis is decided at the fact adapter, not by a later filter
  - *What the entry says the decision is:* When a staff is excluded from analysis - for instance the chord staff the program itself writes to - its notes are dropped before the analysis starts, not filtered out of the answer afterwards.
  - *Recorded at* `ARCHITECTURE.md:131-133`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 2, of which 2 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 168 — matched by the decisions register's own recogniser `excludeStaves` (`excludeStaves`)
    > | OI-212 | The record producer analyzes the WHOLE SCORE regardless of the requested span | `produceNotationRecord(score, stem, excludeStaves)` takes NO tick range; all four record-arm seams (annotation emit `notationcomposingbridge.cpp:1495`, implode `:1420`, tuning `:777`, note-seam `:732`) analyze the entire score and narrow by view AFTERWARD, while every legacy arm passed the actual span into `analyzeHarmonicRhythm` (+~8-bar RN lookahead). Both a cost multiplier on every seam AND a silent analysis-INPUT-SCOPE change made without a ruling; the contract's span seam specifies the opposite. The layer-correct fix (if the extent ruling calls for it) is a span input at the producer/adapter (input-scoping, NOT inference — the OI-204 pattern for the temporal axis). | composing / the record producer's input surface | OPEN — the cost side quantified by the analysis-cost phase profile; the scope-
  - *Subject found in* `STATUS.md` line 758 — matched by the decisions register's own recogniser `input-scoping` (`input-scoping`)
    > *Last updated: 2026-07-27, thirteenth entry (CC — **★ THE NOTATION SWITCH — the joint estimator's record path is now the PRODUCTION in-app notation analysis (user-ratified 2026-07-27); ONE revertible commit.** Dispatch `cc_instruction_notation_switch.md`. **THE FLIP:** `useJointNotationRecord` default OFF→ON (`composingconfiguration.cpp` + the interface doc comment; the flag now selects the LEGACY arm only when explicitly false, and it + the legacy branch retire together at the OI-180 map). So the in-app notation analysis — span-annotation emit, implode chord-track, tuning region path, and the note-seam (status-bar / harmony-write / right-click-menu) — is produced by A's record path (the seams P0–P7), not the legacy `analyzeHarmonicRhythm`/`analyzeChord`. **THE GOLDEN REFRESH + RECONCILIATION (the switch's establishment, #15/#19):** the pipeline-snapshot goldens were refreshed against th
  - *Subject found in* `cowork_handoff.md` line 1401 — matched by the decisions register's own recogniser `excludeStaves` (`excludeStaves`)
    > stem, excludeStaves)` **takes no tick range at all.** All four record-arm seams — annotation emit (`notationcomposingbridge.cpp:1495`), implode (`notationimplodebridge.cpp:1420`), tuning, and the note seam (`:732`) — analyze the ENTIRE score and narrow the result by view afterwards, while every legacy arm passed the actual span into `analyzeHarmonicRhythm` (`:1509`, `:1434`) with a deliberate ~8-bar Roman-numeral lookahead. So annotating four bars costs a full-score analysis,
  - *Subject found in* `cc_instruction_notation_switch.md` line 22 — matched by the decisions register's own recogniser `input-scoping` (`input-scoping`)
    > (identical / inference-driven / presentation-rule / input-scoping-zero are the explained classes — anything else is a STOP); any change to the committed corpus, `tools/robust_stop/`, or any non-pipeline-snapshot golden (the flag is notation-side; the batch surface must not move — verify explicitly); any need to touch a file class not listed. A surprise during an adoption-class commit is a STOP, not a workaround (#13).
- **D-014** — The two seams read the record as pure views - no recomputation
  - *What the entry says the decision is:* The two ways of asking the record a question - 'what is in this stretch of music' and 'what is at this moment' - only look things up. Neither works anything out for itself.
  - *Recorded at* `ARCHITECTURE.md:137`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `cowork_handoff.md` line 2033 — matched by the decisions register's own recogniser `noteView` (`noteView`)
    > chorale → ~27 s sonata); memoized `noteView` sub-microsecond — the ~10⁷–10⁸× gap that is the cache design's bound. The marginals oracle PASSES on all 326 (fwd==bwd 8.5e-14; max-plus == decoder Δ=0; fit-arc logZ tie Δ=0; normalization 5.6e-16; synthetic hand-trace 3.3e-16; MAP-consistency positive) — OI-193's publication gate is established; Tasks 3–4 (C++ + record fields) partitioned to a follow-up. **Cowork's logsumexp ruling (dated amendment on
- **D-019** — The record arm publishes the raw key-axis gap, with no remapping to 0..1
  - *What the entry says the decision is:* The confidence value carried on the record arm is the estimator's own raw score gap, on its own scale - deliberately not converted into a 0-to-1 number.
  - *Recorded at* `ARCHITECTURE.md:212-213`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 2, of which 2 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 190 — matched by the entry's own identity (`D-019`)
    > | OI-231 | ★ NON-CONFORMANCE: the production notation path publishes an UNBOUNDED score gap (nats) in a field whose declared contract is [0,1] — against the ratified cross-layer confidence contract | The confidence contract is RATIFIED (user 2026-07-02, `cowork_confidence_contract.md:3`) and its rule U2 (`:39-40`) requires every confidence crossing a layer boundary to be `[0,1]`, class-declared, decision-named; U4 forbids silent re-interpretation downstream. The record arm writes the raw §3.3 key-axis gap straight into `KeyModeAnalysisResult::normalizedConfidence` (`sectionrecordadapter.cpp:293`), a field whose own type header (`keymodeanalyzer.h:111`) and the canonical architecture document (`ARCHITECTURE.md:2224`) both declare `[0,1]`, and carries it across the module boundary as `NoteHarmonicContext::keyConfidence`. NOT a live defect — the one in-tree consumer reads the STORED `hasAss
  - *Subject found in* `ARCHITECTURE.md` line 182 — matched by the decisions register's own recogniser `RAW §3.3` (`raw §3.3`)
    > legacy-arm-only), record arm from the raw §3.3 key-axis gap (nats) at `kTentativeKeyExposureGap=0.975911` / `kAssertiveKeyExposureGap=1.055757` (`sectionrecordadapter.cpp`, the P1 constants from `tools/notation_seams/exposure_constants.json`; the record's confidence field carries nats, never compared to the [0,1] literals). The implode reads the stored bucket (#6 — one thresholding site per gate). **The implode chord-track record path** (`notationimplodebridge.cpp`, `populateChordTrack` behind the flag → `produceNotationRecord` →
  - *Subject found in* `DECISIONS.md` line 339 — matched by the entry's own identity (`D-019`)
    > | D-019 | The record arm publishes the raw key-axis gap, with no remapping to 0..1 | LIVE | — | `ARCHITECTURE.md` | | D-020 | The interactive path bypasses the old window cache and has none of its own | LIVE | — | `ARCHITECTURE.md` | | D-021 | The pedal-point fields are suspended on the record arm | DEFERRED | — | `ARCHITECTURE.md` | | D-275 | Every published record carries its own instrument provenance; a provenance-less analysis cannot exist | LIVE | 2026-08-02 · user | `cowork_notation_output_contract.md` §2 | | D-276 | Modal colour is published as un-rounded per-degree counts; no mode label is inferred or published anywhere | LIVE | 2026-08-02 · user | `cowork_notation_output_contract.md` §3.4 |
  - *Subject found in* `cowork_joint_estimator_factorization.md` line 71 — matched by the decisions register's own recogniser `nats` (`nats`)
    > against ground truth by ~6.6 nats on the named case. ## 3. The factors — form, table, provenance (values all unfit) 1. **Pitch emission** `P_emit(tone | k, c; covariates)` — each tone classified into categories:
  - *Subject found in* `open_items/OI-231.md` line 47 — matched by the entry's own identity (`D-019`)
    > key-axis gap in nats (a model-internal quantity, NO [0,1] remap)"** (register entry D-019). ## Why this is a conformance item and not a defect report The **one in-tree consumer that would have been broken is guarded**: `detectCadences` /
  - *Subject found in* `STATUS.md` line 760 — matched by the decisions register's own recogniser `NO \[0,1\] remap` (`no [0,1] remap`)
    > *Last updated: 2026-07-27, twelfth entry (CC — **★ SEAMS PART 2 — P7: THE PARTITION CLOSE-OUT — completeness verified, docs consolidated (pre-switch state).** Dispatch `cc_instruction_notation_p7.md`. The LAST unit of the seams-2 partition — NO new behavior, NO new code path: a read-only completeness verification + a documentation consolidation, closing the partition so the switch ratification assembles on a verified-complete record. **TASK 1 — THE COMPLETENESS VERIFICATION (read-only; generated artifact `tools/notation_seams/partition_completeness.json`) — OVERALL COMPLETE / PASS, ZERO FINDINGS:** (1) **every audited consumer has its record-arm branch behind the default-OFF `useJointNotationRecord`** — the span-annotation emitter (`notationcomposingbridge.cpp:1491`), the note-seam funnel `analyzeHarmonicContextAtTick` (`:729`), the implode `populateChordTrack` (`notationimplodebridge.cp
  - *Subject found in* `STATUS_ARCHIVE.md` line 142 — matched by the decisions register's own recogniser `RAW §3.3` (`RAW §3.3`)
    > *Last updated: 2026-07-27, tenth entry (CC — **★ SEAMS PART 2 — THE MERGED NOTE-SEAM RE-PLUMB (status bar + harmony write + right-click menu on `noteView`) + the split-check LIVING mode (OI-202).** Dispatch `cc_instruction_notation_noteseam.md`. The ratified merged note-seam unit (the P3 re-partition: the status-bar/note-context producer + the old P5 consumers as ONE unit, resolving the `NoteHarmonicContext`↔record carriage ONCE). **Task 1 (commit `874bda0ab6`) — the split-check LIVING mode + OI-202:** `tools/open_items_split_check.py` gains a default LIVING mode (`open_items/register_check.json`) valid on a tree that grows after the split — (a) INDEX↔detail-file bijection (no dupes/orphans), (b) NO detail file carries a status of its own (header sentinel present + no `| OPEN`/`| ✅ RESOLVED`-style status cell outside the one verbatim historical row; the two-place-status drift killer), (c
  - *Subject found in* `open_items/OI-187.md` line 7 — matched by the decisions register's own recogniser `nats` (`nats`)
    > | OI-187 | ★ **THE WEIGHT FIT'S BAND MISS — the training objective is not a validated proxy for the grading metric (#17d).** CC-measured 2026-07-20 at the ratified fit event: the convex conditional-likelihood fit CONVERGED cleanly on every fold (monotone iterate objective, \|grad\| 2.3e-06–1.6e-05, weights stable across folds: emission 0.316±0.007, boundary 0.774±0.012, chord_trans 0.276±0.008) and IMPROVED its own objective substantially (fold 3: 2.550 → 1.264 nats/token) — yet the held-out CV decode moved key-LOCAL **DOWN** 74.68 → **70.22** (paired delta −4.45 [−5.47,−3.46], outside the CI ⟹ real) and Roman numeral DOWN 60.08 → **57.99** (−2.09 [−2.70,−1.45]); only key-HOME rose (53.20 → 56.34, +3.16 [+1.75,+4.52]) and root was flat (+0.34 [−0.08,+0.79] — inside the CI, NOT a finding, #24). **Three of Cowork's four written bands are missed** (key-local predicted 77–85, key-home 62–75,
- **D-020** — The interactive path bypasses the old window cache and has none of its own
  - *What the entry says the decision is:* Clicking a note re-analyses the whole score. The old shortcut that reused a small window's work is not used on the new path, and no replacement has been built yet.
  - *Recorded at* `ARCHITECTURE.md:220-222`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 2, of which 2 ratify a document other than their own
  - *Subject found in* `cc_instruction_notation_switch.md` line 81 — matched by the decisions register's own recogniser `OI-203` (`OI-203`)
    > (this commit), half (b) open; `open_items/OI-203.md` dated note — the latency is now on the DEFAULT path, the cache increment's priority rises. INDEX rows only where status actually changes. 6. **Commit + push origin only.** Message: `THE NOTATION SWITCH: the record path is the production notation analysis (user-ratified 2026-07-27) — flag default ON; goldens
  - *Subject found in* `cc_instruction_notation_switch.md` line 81 — matched by the decisions register's own recogniser `OI-203` (`OI-203`)
    > (this commit), half (b) open; `open_items/OI-203.md` dated note — the latency is now on the DEFAULT path, the cache increment's priority rises. INDEX rows only where status actually changes. 6. **Commit + push origin only.** Message: `THE NOTATION SWITCH: the record path is the production notation analysis (user-ratified 2026-07-27) — flag default ON; goldens
- **D-037** — The note model is the single source of truth for what sounds, and reads the score once
  - *What the entry says the decision is:* One component reads the score and works out which notes are sounding when. Everything else asks it, and nothing else reads the score.
  - *Recorded at* `ARCHITECTURE.md:1575`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 3, of which 2 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 208 — matched by the decisions register's own recogniser `note model` (`note model`)
    > | OI-241 | ★ A MISSING RULING (the user's to make): no GENERAL rule states which existing MuseScore code our code may depend on — only scoped forms exist | Checked rather than assumed, at the dispatch's direction. What EXISTS: D-227 (§2.8 — follow MuseScore's patterns, never invent parallel infrastructure: a rule about imitation), D-072 (§3.3 — the dependency ORDER is enforced, analysis depends on no engraving type), D-228 (§3.3 — the bridge PATTERN: shape and location of the functions that touch engraving types), D-073 (§2.10 — shared logic has one implementation), D-120 (§17.1 — coding style), D-143 (§19.4 — the future language-model module confined to the Core Access Layer, the ONE permitted-interface rule stated anywhere, scoped to a module that does not exist yet). What does NOT exist: any statement of which MuseScore interfaces the BRIDGE layer may call — whether it must go through
  - *Subject found in* `cowork_prefit_gates.md` line 159 — matched by the decisions register's own recogniser `note model` (`note model`)
    > published note model is tie-resolved and lossy for A's needs, and a module-private raw score walk would be the raw-source-outside-the-fact-layer defect class the certification audits condemned. The sanctioned touchable set therefore ALSO includes ADDITIVE extension of the L1/L1.5 fact surface — publishing the missing notated-note facts (tie flags, unresolved notated notes and continuations, and kin) once, on the fact layer's output surface — under two proofs per commit:
  - *Subject found in* `STATUS.md` line 186 — matched by the decisions register's own recogniser `note model` (`note model`)
    > *Last updated: 2026-08-04, LATEST+++++++++++++++++++++++++++++++ (CC — **★ READ WAVE 6 — THE LAST OWED READING WAVE RAN AND THE OWED SET IS EMPTY. Four rulings applied: D-584…D-611 RATIFIED; principle #8 now POINTS at what FINISHED means and at the line inside the firewall; the duplicate entry for #8 is RESOLVED; the guard population is CLASSIFIED PER TOOL and four tools leave the live list as historical records — and the assumption that classification was given is REFUTED IN PART.** READ-ONLY on the system: no `src/` change, no golden / `tools/corpus/` / `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to inference, no design. Dispatch **`cc_instruction_reads_6.md`** (cite it by THAT name). Per the OI-222 remedy this entry is a **POINTER** — every figure lives in the generated artifacts and nothing is restated here (**D-431**). **Read, in this order:** `tools/a
  - *Subject found in* `cowork_handoff.md` line 2647 — matched by the decisions register's own recogniser `note model` (`note model`)
    > **Caught 2026-06-21:** layer 1 (note model, `e30bb45a4f`) shipped with **zero** canonical-doc updates — verified: 0 mentions of the note model across the entire `docs/` tree or `ARCHITECTURE.md`, which still describe the old segment-first pipeline. This rule exists so that gap is closed now and never recurs. ---
  - *Subject found in* `OPEN_ITEMS.md` line 208 — matched by the decisions register's own recogniser `note model` (`note model`)
    > | OI-241 | ★ A MISSING RULING (the user's to make): no GENERAL rule states which existing MuseScore code our code may depend on — only scoped forms exist | Checked rather than assumed, at the dispatch's direction. What EXISTS: D-227 (§2.8 — follow MuseScore's patterns, never invent parallel infrastructure: a rule about imitation), D-072 (§3.3 — the dependency ORDER is enforced, analysis depends on no engraving type), D-228 (§3.3 — the bridge PATTERN: shape and location of the functions that touch engraving types), D-073 (§2.10 — shared logic has one implementation), D-120 (§17.1 — coding style), D-143 (§19.4 — the future language-model module confined to the Core Access Layer, the ONE permitted-interface rule stated anywhere, scoped to a module that does not exist yet). What does NOT exist: any statement of which MuseScore interfaces the BRIDGE layer may call — whether it must go through
  - *Subject found in* `cowork_evidence_inventory.md` line 30 — matched by the decisions register's own recogniser `note model` (`note model`)
    > alignment fix. UNDISCUSSED as key evidence; not in the note model's 11 documented facts. Cheap to read; enormous leverage for transition costs and cadence location. - **Rests/silences as phrase ends** (user-raised 2026-07-12, pairing with the fermata fact above): a sufficiently long rest signals a phrase end just as a fermata does. Status: HALF-EMBODIED — the dormant phrase-boundary view is already
- **D-040** — The tie-unresolved atoms are republished additively for the joint estimator
  - *What the entry says the decision is:* As well as merging tied notes, the note reader also publishes them separately, each with a marker saying it is a continuation. The joint estimator needs both views.
  - *Recorded at* `ARCHITECTURE.md:1575`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `cowork_handoff.md` line 2407 — matched by the decisions register's own recogniser `notatedNotes` (`notatedNotes`)
    > `notatedNotes()` additive publication landed under both proofs, catching and fixing a latent infinite-loop on partial ties. Task C DELIVERED and verified (`8416b2c84c`, `020baca347`, pushed): the fact adapter reads only the published surface; input parity 300/326 byte-perfect with ONE mechanically-unmappable class (metric position — music21 read the xml's EDITORIAL measure bookkeeping, the production engraving model normalizes it; ~26 pathological-measure pieces; end-to-end 320/326 identity, 316/326
- **D-055** — The 21 mode priors are independent and user-configurable
  - *What the entry says the decision is:* How likely each scale type is considered to be is a separate adjustable number per scale type, exposed in the preferences.
  - *Recorded at* `ARCHITECTURE.md:3271-3272`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 2, of which 2 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 3271 — matched by the decisions register's own recogniser `modePrior` (`ModePrior`)
    > - `scoreModePrior` — **21 independent additive priors**, one per mode, user-configurable via `IComposingAnalysisConfiguration::modePrior{ModeName}()`; defaults reflect Western tonal frequency (Ionian=+1.20, Aeolian=+1.00, down to Altered=−3.50) - `applyRelativePairDisambiguation` — post-hoc score mutations for the relative major/minor pair sharing a key signature (four documented cases; see implementation)
  - *Subject found in* `DECISIONS.md` line 476 — matched by the entry's own identity (`D-055`)
    > | D-055 | The 21 mode priors are independent and user-configurable | LIVE ⚠LEGACY | — | `ARCHITECTURE.md` | | D-056 | Notes always win - the notated key signature is a weak hint, not a bypass | LIVE | — | `ARCHITECTURE.md` | | D-057 | The priority of evidence - actual sounding notes are the strongest evidence | LIVE | — | `ARCHITECTURE.md` | | D-058 | The piece-start shortcut | SUPERSEDED IN FACT ⚠LEGACY | — | `ARCHITECTURE.md` | | D-059 | The temporal window - 16 beats back, 8 beats forward, decayed | LIVE ⚠LEGACY | — | `ARCHITECTURE.md` |
  - *Subject found in* `cowork_structural_integrity_audit.md` line 184 — matched by the decisions register's own recogniser `modePrior` (`modeprior`)
    > | S7 | `key/modepriorpresets.h:45-69` + `.cpp:34-55` — `ModePriorPreset` "Standard" | (c) | **VIOLATION** | LOW | The 21 "Standard" mode-prior magnitudes exist as three literal copies (struct initializers, explicit `standard.*=` assignments, and the `KeyModeAnalyzerPreferences` defaults), kept in sync by a **test** (`modepriorpresets_tests.cpp:106`) rather than a single source. | | S8 | `key/keymodesequence.h:117-137` — `KeyModeSequencePreferences` cost/window constants | (c) | UNCLEAR | MED | The live decoder's `changeBaseCost/changePerFifthStep/relativePairExtraCost/decayRate/lookaheadWeight` are **copied by value** from the resolver's `hysteresisMargin`/`keySignatureDistancePenalty`/scoreharvest decay — no shared symbol, so a Stage-5 fit of either drifts them apart. | | S9 | `region/regionanalyzer.cpp:585` & `:611` — full ranked key resolve as a segmentation seed | (d)/(b) | UNCLEAR |
  - *Subject found in* `cowork_handoff_archive.md` line 1852 — matched by the decisions register's own recogniser `modePrior` (`modeprior`)
    > minimal `modepriorpresets.h`→`analysistypes.h`, a dependency-profile decision)] · **FQ-7/S8 `56b06462db`** [key-decoder cost/window constants sourced from `kDefaultKeyModeAnalyzerPreferences.*` + `scoreharvest::DECAY_RATE`/`LOOKAHEAD_WEIGHT`; **S9 adjudicated KEPT — load-bearing NOT dead**: `resolveKeyAndModeRanked@585` feeds `greedyExpandSegmentation@851` + `findTemporalContext@900` = the grid] · **FQ-6 `5420e6e543`** [`appendCappedAlternatives` shared projection in `analyzed_section.h`; batch cap=3,
  - *Subject found in* `STATUS.md` line 186 — matched by the entry's own identity (`D-055`)
    > *Last updated: 2026-08-04, LATEST+++++++++++++++++++++++++++++++ (CC — **★ READ WAVE 6 — THE LAST OWED READING WAVE RAN AND THE OWED SET IS EMPTY. Four rulings applied: D-584…D-611 RATIFIED; principle #8 now POINTS at what FINISHED means and at the line inside the firewall; the duplicate entry for #8 is RESOLVED; the guard population is CLASSIFIED PER TOOL and four tools leave the live list as historical records — and the assumption that classification was given is REFUTED IN PART.** READ-ONLY on the system: no `src/` change, no golden / `tools/corpus/` / `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to inference, no design. Dispatch **`cc_instruction_reads_6.md`** (cite it by THAT name). Per the OI-222 remedy this entry is a **POINTER** — every figure lives in the generated artifacts and nothing is restated here (**D-431**). **Read, in this order:** `tools/a
  - *Subject found in* `STATUS_ARCHIVE.md` line 214 — matched by the decisions register's own recogniser `modePrior` (`modePrior`)
    > *Last updated: 2026-07-13 (CC — **THE HARNESS GROUP — the OI-145 wave-1 remainder is CLOSED** (`cc_harness_group_report.md`). Commits `0922e2bfdc` (Cowork's register/design edits) → `a62c67e423` (OI-153) → `70f64e4bcb` (OI-135) → `92d5092f33` (OI-136) → `f4d1878dcf` (OI-137) → `375c984366` (OI-52) → `3f839e0e24` (OI-155). **BYTE-IDENTICAL, as the dispatch required: no grading digit moved, no constant tuned, no gate threshold touched, no golden refreshed — and the committed corpus regenerates bit-for-bit (`regen` gate: 0 of 1056 `.ours.json` differ across all three presets), which is the proof that recompiling `batch_analyze.cpp` changed nothing.** Establishment battery PASS on the committed tree (register 155 IDs no collision · a8_diff **+0/−0 all presets**, class-(b) Δ+0 · calib **4/4 byte-identical** · validate 3/3 · **regen 0/0/0 differ**); suites green (composing **1103/1103** = 1101
  - *Subject found in* `ARCHITECTURE.md` line 3271 — matched by the decisions register's own recogniser `modePrior` (`ModePrior`)
    > - `scoreModePrior` — **21 independent additive priors**, one per mode, user-configurable via `IComposingAnalysisConfiguration::modePrior{ModeName}()`; defaults reflect Western tonal frequency (Ionian=+1.20, Aeolian=+1.00, down to Altered=−3.50) - `applyRelativePairDisambiguation` — post-hoc score mutations for the relative major/minor pair sharing a key signature (four documented cases; see implementation)
  - *Subject found in* `STATUS.md` line 186 — matched by the entry's own identity (`D-055`)
    > *Last updated: 2026-08-04, LATEST+++++++++++++++++++++++++++++++ (CC — **★ READ WAVE 6 — THE LAST OWED READING WAVE RAN AND THE OWED SET IS EMPTY. Four rulings applied: D-584…D-611 RATIFIED; principle #8 now POINTS at what FINISHED means and at the line inside the firewall; the duplicate entry for #8 is RESOLVED; the guard population is CLASSIFIED PER TOOL and four tools leave the live list as historical records — and the assumption that classification was given is REFUTED IN PART.** READ-ONLY on the system: no `src/` change, no golden / `tools/corpus/` / `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to inference, no design. Dispatch **`cc_instruction_reads_6.md`** (cite it by THAT name). Per the OI-222 remedy this entry is a **POINTER** — every figure lives in the generated artifacts and nothing is restated here (**D-431**). **Read, in this order:** `tools/a
  - *Subject found in* `cowork_structural_integrity_audit.md` line 184 — matched by the decisions register's own recogniser `modePrior` (`modeprior`)
    > | S7 | `key/modepriorpresets.h:45-69` + `.cpp:34-55` — `ModePriorPreset` "Standard" | (c) | **VIOLATION** | LOW | The 21 "Standard" mode-prior magnitudes exist as three literal copies (struct initializers, explicit `standard.*=` assignments, and the `KeyModeAnalyzerPreferences` defaults), kept in sync by a **test** (`modepriorpresets_tests.cpp:106`) rather than a single source. | | S8 | `key/keymodesequence.h:117-137` — `KeyModeSequencePreferences` cost/window constants | (c) | UNCLEAR | MED | The live decoder's `changeBaseCost/changePerFifthStep/relativePairExtraCost/decayRate/lookaheadWeight` are **copied by value** from the resolver's `hysteresisMargin`/`keySignatureDistancePenalty`/scoreharvest decay — no shared symbol, so a Stage-5 fit of either drifts them apart. | | S9 | `region/regionanalyzer.cpp:585` & `:611` — full ranked key resolve as a segmentation seed | (d)/(b) | UNCLEAR |
  - *Subject found in* `STATUS_ARCHIVE.md` line 214 — matched by the decisions register's own recogniser `modePrior` (`modePrior`)
    > *Last updated: 2026-07-13 (CC — **THE HARNESS GROUP — the OI-145 wave-1 remainder is CLOSED** (`cc_harness_group_report.md`). Commits `0922e2bfdc` (Cowork's register/design edits) → `a62c67e423` (OI-153) → `70f64e4bcb` (OI-135) → `92d5092f33` (OI-136) → `f4d1878dcf` (OI-137) → `375c984366` (OI-52) → `3f839e0e24` (OI-155). **BYTE-IDENTICAL, as the dispatch required: no grading digit moved, no constant tuned, no gate threshold touched, no golden refreshed — and the committed corpus regenerates bit-for-bit (`regen` gate: 0 of 1056 `.ours.json` differ across all three presets), which is the proof that recompiling `batch_analyze.cpp` changed nothing.** Establishment battery PASS on the committed tree (register 155 IDs no collision · a8_diff **+0/−0 all presets**, class-(b) Δ+0 · calib **4/4 byte-identical** · validate 3/3 · **regen 0/0/0 differ**); suites green (composing **1103/1103** = 1101
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-056** — Notes always win - the notated key signature is a weak hint, not a bypass
  - *What the entry says the decision is:* The key printed at the start of the score does not settle the question. It only nudges the answer; what the notes actually do decides.
  - *Recorded at* `ARCHITECTURE.md:4283-4285`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 422 — matched by the decisions register's own recogniser `declaredMode` (`declaredMode`)
    > | OI-60 | Blocking/needs-fix trio | Chord-symbols-as-input, declaredMode soft-boost, implode chord-track gaps — all three fixed (verified at code). | submission backlog | ✅ SUPERSEDED 2026-07-13 — all three fixed; residue (missing fixtures) rides OI-38 | [detail](open_items/OI-60.md) | | OI-63 | S7 full mode-prior single-sourcing | A Stage-1 leftover (a dependency-profile call). | substrate hygiene | OPEN — not a triage row; already ASSIGNED (OI-145 wave 2); the harness half rides OI-135 | [detail](open_items/OI-63.md) | ## H. Long-horizon roadmap (held deliberately; listed so they are never "forgotten")
- **D-058** — The piece-start shortcut
  - *What the entry says the decision is:* At the very start of a piece there is not yet enough music to judge the key, so if the score declares major or minor the program simply believes it, marked as a middling-confidence answer.
  - *Recorded at* `ARCHITECTURE.md:4321-4322`; *status* `superseded-in-fact`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 3, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 4316 — matched by the entry's own identity (`D-058`)
    > **★ CORRECTED 2026-08-04 (phase 1z; `OPEN_ITEMS.md` OI-315, register entry D-058). The paragraph this replaces specified a piece-start shortcut in the present tense. The code removed that short-circuit in Stage 4b-i on 2026-06-14 and this document went on asserting it.** What the opening does at HEAD, read at the code:
  - *Subject found in* `cowork_audit_protocol.md` line 781 — matched by the entry's own identity (`D-058`)
    > declared-mode piece-start shortcut (`OPEN_ITEMS.md` OI-315, register entry **D-058**), and the ruling names that act as its own source. The precedent is quoted **in full**, located by its own anchors and re-read from `ARCHITECTURE.md` on every run, at `tools/audit/phase1_completion_inventory.json` → `the_requirement.criteria` → C1 → the removal block; it is deliberately not restated here (#6), and a rewording of it STOPS the derivation rather than leaving a stale account of it standing.
  - *Subject found in* `STATUS.md` line 170 — matched by the entry's own identity (`D-058`)
    > *Last updated: 2026-08-04, LATEST+++++++++++++++++++++++++++++++++++++++ (CC — **★ THE GUARD'S MISS IS FIXED WITH ITS CORPUS EXTENDED FIRST AND THE OLD RATES RECORDED AS NOT BOUNDING WHAT THEY APPEARED TO; THE REMOVAL SHAPE R1 WAS SILENT ON IS RULED AND APPLIED AT THE ONE ENTRY IT REACHES; THE FINISH LINE IS RE-CUT AT ENTRY GRANULARITY; AND THE ASSUMPTION THAT ITEM 1's RE-HOME CLASS IS BLOCKED BY LICENSING CAME BACK REFUTED — HALF THE CLASS NAMES ONLY A FILE THAT IS ALREADY LICENSED AND IS STILL NOT EXECUTABLE.** No `src/` change, no golden / `tools/corpus/` / `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to inference, no design. **Two things DID change and are named rather than folded into a read-only claim:** a MECHANISM (the shell-read guard) under **D-436**, which makes that the user's ruling and it was given; and a SPECIFICATION (`ARCHITECTURE.md` §5.2) 
  - *Subject found in* `STATUS_ARCHIVE.md` line 2551 — matched by the entry's own identity (`D-058`)
    > intentional, was REMOVED from the code on 2026-06-14 — register entry **D-058**, superseded in fact, with the specification corrected and the removal recorded tried-and-closed at [[OI-315]]. - **`ChordAnalysisTone::weight` not populated** — currently always 1.0; duration and beat position are collected in `notationcomposingbridge.cpp` for `PitchContext` (used by
  - *Subject found in* `docs/key_path_design.md` line 46 — matched by the decisions register's own recogniser `piece-start shortcut` (`piece-start shortcut`)
    > | **Reuse** | `analyzeKeyMode` (the 252-candidate scorer) = emission, REUSED. `resolveKeyAndModeRanked`'s per-region hysteresis + `promoteWinnerInPlace` = REPLACED by the decode. Partial-signature correction = KEPT (folds into emission input). **The piece-start shortcut is not in this list — it no longer exists; see the correction block below.** | [code] | | **Caveat (not a falsification)** | The resolver currently surfaces only the **top-3** candidates with post-family-selection scores, not the raw 252. The HMM emission needs the **top-N (or full 252) raw scores** exposed — a small refactor of `analyzeKeyMode`'s output, not a rebuild. | [code] | **★ CORRECTED 2026-08-13 — THE PIECE-START SHORTCUT DOES NOT EXIST AT HEAD, AND FOUR STATEMENTS IN THIS DOCUMENT RESTED ON IT (`OPEN_ITEMS.md` OI-315).** §1's item 2 already recorded the removal.
  - *Subject found in* `open_items/OI-324.md` line 10 — matched by the entry's own identity (`D-058`)
    > Read wave 1 established that **D-058** — one of three decisions the 2026-08-01 adjudication names as provably invisible to the harvest's signature net — had been entered with unusual care and never checked against the code afterwards ([[OI-315]]). The dispatch's Task 2 was to identify the other two from the adjudication's own record and check each against the code.
  - *Subject found in* `cowork_away_returns.md` line 356 — matched by the entry's own identity (`D-058`)
    > gloss that exists); **D-058** likewise, with a stated pragmatic ground but no derivation; and **two of the eight turn out to HAVE a defense the empty field misrepresents** — **D-052**, whose home text states the reason as a consequence (*"so no signature/partial-correction logic is duplicated"*, which is #6), and **D-003**, whose home states the constraint that forced it (*"presets are presentation concerns"*). **D-019** was searched and the record holds no reason — what it holds
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-068** — The chord identifier needs at least three distinct pitch classes
  - *What the entry says the decision is:* With fewer than three different pitch names sounding, the chord identifier declines to answer rather than guessing.
  - *Recorded at* `ARCHITECTURE.md:2691`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 2691 — matched by the decisions register's own recogniser `Minimum 3 distinct pitch classes` (`Minimum 3 distinct pitch classes`)
    > Minimum 3 distinct pitch classes required. Returns empty vector if insufficient data. Callers instantiate `RuleBasedChordAnalyzer{}` or hold an `IChordAnalyzer*` for dependency injection (e.g. future ML-based analyzer). #### Factory — `ChordAnalyzerFactory` (P4b)
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-073** — Single implementation for shared logic; mirroring is a last resort
  - *What the entry says the decision is:* If the program and the measurement tool must agree, the code that makes them agree lives in one shared place. Copying it into both is a last resort that must be flagged as debt.
  - *Recorded at* `ARCHITECTURE.md:803-805`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 3, of which 2 ratify a document other than their own
  - *Subject found in* `cowork_l1l3_stabilization_plan.md` line 9 — matched by the decisions register's own recogniser `total unification` (`total unification`)
    > design precedes each non-trivial step); total unification (one path per concern); byte-identity guards where a step must not move output; the two-tier BIR gate + both test suites + pinned snapshots as the standing safety net. Each step below becomes its **own** gated Claude-Code instruction when we reach it — this document is the **sequence and the gates**, not the build instructions.
  - *Subject found in* `OPEN_ITEMS.md` line 208 — matched by the entry's own identity (`D-073`)
    > | OI-241 | ★ A MISSING RULING (the user's to make): no GENERAL rule states which existing MuseScore code our code may depend on — only scoped forms exist | Checked rather than assumed, at the dispatch's direction. What EXISTS: D-227 (§2.8 — follow MuseScore's patterns, never invent parallel infrastructure: a rule about imitation), D-072 (§3.3 — the dependency ORDER is enforced, analysis depends on no engraving type), D-228 (§3.3 — the bridge PATTERN: shape and location of the functions that touch engraving types), D-073 (§2.10 — shared logic has one implementation), D-120 (§17.1 — coding style), D-143 (§19.4 — the future language-model module confined to the Core Access Layer, the ONE permitted-interface rule stated anywhere, scoped to a module that does not exist yet). What does NOT exist: any statement of which MuseScore interfaces the BRIDGE layer may call — whether it must go through
- **D-081** — The cadence detector is key-agnostic
  - *What the entry says the decision is:* The part that spots cadences must not be told what key it is in - it is one of the things that decides the key, so reading the answer first would be circular.
  - *Recorded at* `ARCHITECTURE.md:2082-2083`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 112 — matched by the decisions register's own recogniser `key-agnostic` (`key-agnostic`)
    > | OI-166 | The key-agnostic cadence pre-scan is not built | functioncadence is key-agnostic but chord-derived (reads the L4 root/quality), not the bass-driven tonic-enumerating pre-scan the dossier specified; a forward L1.5 break is realizable. | L5 / L1.5 (key-layer) | OPEN — a STOP before any cadence→key build; the layer placement is the design pass's to ratify | [detail](open_items/OI-166.md) | | OI-167 | The collection/tonic split holds — conditionally | Two named tonic-dependent L4 sites (Gate G-E, the Aeolian guard); site (b) is measured DEAD (0/0/0, body unexercised). | L4 | OPEN — the row's conclusion is now REFUTED (the split does not hold); cannot close until OI-170/OI-171/OI-172 resolve | [detail](open_items/OI-167.md) | | OI-168 | The collection/tonic split breaks inside the engaged decoder | analyzeChord's two key-consuming terms test a tonic-transposed set that is wrong for
  - *Subject found in* `cowork_layer6_grouping_design.md` line 320 — matched by the decisions register's own recogniser `cadence detector` (`cadence detector`)
    > cadence detector, or key segmenter. It **retires** the scattered `detectCadences`/`detectPivotChords`/`KeyArea` paths into itself at engagement. - **The firewall.** The alignment window, the codetta closeness/margin, and the key-area confidence combiner are precision-phase constants; this document fixes the rules and their direction, not the numbers. - **Proportionality.** The SOTA reaches competitive Roman-numeral accuracy with **no** explicit grouping layer (grouping
  - *Subject found in* `cowork_evidence_inventory.md` line 129 — matched by the decisions register's own recogniser `key-agnostic` (`key-agnostic`)
    > resolving up at an arrival — key-agnostic, voice-aware). - Non-chord-tone classifications per note (passing/neighbor/suspension) — the anchor-redesign topic; also cleans the key layer's emission input (§8). - Chromatic-alteration events (a sounding pc outside the prevailing collection = a tonicization signal at its tick).
- **D-082** — The grouping layer is additive, read-only, with no feedback
  - *What the entry says the decision is:* The stage that assembles phrases and key areas only organises what earlier stages decided. It never changes their answers.
  - *Recorded at* `ARCHITECTURE.md:2197`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 2, of which 1 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 356 — matched by the decisions register's own recogniser `grouping layer` (`grouping layer`)
    > | OI-318 | Two label defects in the canonical architecture document: the Layer-6 section still calls the grouping unit a “phrase”, and §19.4 is used twice | Found checking two read documents against the specification that summarises them (CC, READ WAVE 1, 2026-08-04). **(1)** `ARCHITECTURE.md:1502-1504` describes the grouping layer as assembling *“**phrases** … and the **alignment of cadences to phrase ends**”*, while `:892-894` in the same document records the ratified rename that forbids exactly this — the span is the **punctuation-span**, *“renamed from ‘phrase’ 2026-07-01 so that ‘phrase [MT]’ denotes only the accepted melodic phrase”* — and points at `cowork_layer6_grouping_design.md` §0, whose whole terminology section exists to separate the two. A reader of the Layer-6 paragraph alone is told the grouping layer segments melodic phrases, the one thing that §0 denies. This is [[OI-2
  - *Subject found in* `cowork_layer6_grouping_design.md` line 324 — matched by the decisions register's own recogniser `grouping layer` (`grouping layer`)
    > - **Proportionality.** The SOTA reaches competitive Roman-numeral accuracy with **no** explicit grouping layer (grouping falls out of stable key runs — `contrapunctus_findings.md`). L6 is a deliberate **explainability** layer, not an accuracy requirement; it stays the thin assembly layer specified here and does not grow detection of its own. ## 8. The Layer-5-override ↔ Layer-6-merge division (the standing joint item, L5 §15-6)
- **D-084** — The progression-schema recognizer is a consumer of the function layer, not a new layer
  - *What the entry says the decision is:* Recognising well-known chord patterns is something that reads the finished analysis and annotates it. It is not another stage in the chain.
  - *Recorded at* `ARCHITECTURE.md:2259`; *status* `deferred`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `cowork_confidence_contract.md` line 84 — matched by the decisions register's own recogniser `recognition consumer` (`recognition consumer`)
    > the recognition consumer's schema-contradiction override, `cowork_progression_schema_design.md` §2) must add its frame row to this section before build — an undeclared cross-layer comparison is a contract violation. **★ BEFORE ANY CONVERSION CONSTANT IS FITTED, THE PREMISE THAT A FITTED CONSTANT CAN MAKE TWO DIFFERENTLY-SCALED CONFIDENCES COMMENSURABLE MUST ITSELF PASS A PREMISE LEDGER AND A DESK SIMULATION (user-ratified 2026-07-10;
  - *Subject found in* `OPEN_ITEMS.md` line 431 — matched by the decisions register's own recogniser `recognition consumer` (`Recognition consumer`)
    > | OI-66 | Recognition consumer (encyclopedia → L5 prior) | Designed-not-built. | long-horizon roadmap | OPEN — deferred; hold confirmed 2026-07-13 | [detail](open_items/OI-66.md) | | OI-67 | Style-clustering / idiom auto-detection | The cross-tradition harmonic-idiom study has landed in idiom_discovery/; the auto-detection BUILD stays deferred. | long-horizon roadmap | OPEN — deferred; the BUILD hold confirmed 2026-07-13, but the idiom study has landed (the row's source was understated) | [detail](open_items/OI-67.md) | | OI-68 | Capability tracks A-3/A-4/A-5 + NCT lever + VL axis | A-3 (dominant-implication key evidence) partly promoted to the key-layer build; A-4/A-5 + the voice-leading axis stay held; the NCT-filter lever now has a consumer (OI-55). | long-horizon roadmap / key-layer | OPEN — ★ PARTLY PROMOTED 2026-07-13 — A-3 ASSIGNED to the key-layer build; the rest stay HELD | [det
  - *Subject found in* `cowork_layer6_grouping_design.md` line 30 — matched by the decisions register's own recogniser `progression-schema` (`progression-schema`)
    > progression-schema-span, §0) (§2 / §3 / §5.5, the L6↔encyclopedia annotation home), the **§2.15 span-typology term scrub** (`region` unqualified → slice / key-span), and the §15-6 / §15-1 status updates. Research foundation: `cowork_layer6_grouping_research.md`. Specified by **rule and direction**; numeric calibration is the later precision phase (the firewall). Build **dormant + byte-identical**, validate against the oracles in §10, engagement deferred indefinitely (production out of scope).
- **D-085** — The voice-leading axis is a separate axis with its own layers
  - *What the entry says the decision is:* How the individual voices move is a second, independent line of analysis alongside the harmonic one, with its own stages.
  - *Recorded at* `ARCHITECTURE.md:1190-1191`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 3, of which 3 ratify a document other than their own
  - *Subject found in* `cowork_confidence_contract.md` line 61 — matched by the decisions register's own recogniser `voiceleading_axis` (`voiceleading_axis`)
    > | **VL-C texture (axis 2)** | **texture-of-span** | the best-vs-second-best **fit** margin, where fit = `exp(−distance/fitScale)` of the z-space (ABz) euclidean distance to a class centroid — already ∈ [0,1) | M | The **axis-2** first judgment component (`cowork_voiceleading_axis_design.md` §5.3; `textureclassifier.h`, DORMANT). The published confidence is the exp-fit margin; squash per **R5** below. The output also carries the **full ranked list of ALL class fits with weights** (zero information loss). Three declared floors — **evidential** (min motion samples), **margin**, **fit** — are precision-phase; abstention = margin < margin-floor OR best fit < fit-floor (contract U5), single-voice → *no-pair* abstention. No new §4 comparison frame (nothing compares a VL confidence against a harmonic one). | ## 4. The comparison frames (the §8 override arithmetic, stated once) A **frame** is a d
  - *Subject found in* `OPEN_ITEMS.md` line 433 — matched by the decisions register's own recogniser `voice-leading axis` (`voice-leading axis`)
    > | OI-68 | Capability tracks A-3/A-4/A-5 + NCT lever + VL axis | A-3 (dominant-implication key evidence) partly promoted to the key-layer build; A-4/A-5 + the voice-leading axis stay held; the NCT-filter lever now has a consumer (OI-55). | long-horizon roadmap / key-layer | OPEN — ★ PARTLY PROMOTED 2026-07-13 — A-3 ASSIGNED to the key-layer build; the rest stay HELD | [detail](open_items/OI-68.md) | | OI-69 | Joint segmentation (past Stage 5) | Deferred. | long-horizon roadmap | OPEN — deferred; hold confirmed 2026-07-13 | [detail](open_items/OI-69.md) | | OI-70 | Standing cautions §8 + the Chopin cascade | The B3 dim7-template + rootContinuity sparse-gate dead ends + the Gate-A enharmonic constraint; + re-attempting the key-confidence-gated dominant-quality fix re-triggers the Chopin op30-2 segmentation cascade (latent). | scoring model / standing | STANDING constraints | [detail](open_i
  - *Subject found in* `cowork_layer6_grouping_design.md` line 424 — matched by the decisions register's own recogniser `voice-leading axis` (`voice-leading axis`)
    > the future voice-leading axis), and absorbs the counterpoint / implied-harmony difficulty via an explicit **non-chord-tone filter** (AnalysisGNN's non-chord-tone module; Contrapunctus) — a future **Layer-4 (emission)** lever, **not** L6's. No located system models concurrent overlapping per-voice phrases for harmonic analysis, which is why L6's grouping is flat. (In that literature "phrase/cadence detection" is the field's own term for the texture-wide task; it maps to our **punctuation-span** + cadence-alignment work, not to the accepted melodic phrase [MT].)
  - *Subject found in* `cowork_evidence_inventory.md` line 74 — matched by the decisions register's own recogniser `voice-leading axis` (`voice-leading axis`)
    > - Texture classification (homophonic/polyphonic; the voice-leading axis): DORMANT. The comparable product routes its key detectors BY texture — never discussed here. - UNDISCUSSED from this layer: **bass MOTION intervals** (a bass falling a fifth into a strong beat is the dominant→tonic skeleton — computable voice-aware from the model without any chord knowledge); **soprano scale-degree at phrase ends**
  - *Subject found in* `cowork_idiom_discovery_findings.md` line 183 — matched by the decisions register's own recogniser `voice-leading axis` (`voice-leading axis`)
    > voice-leading axis is a **notated-music** axis — different coverage from the cross-tradition harmonic axis. **Verdict:** the axis question is answered — voice-leading is a real, strong, orthogonal organizing axis; the spec's **voice-leading layer is warranted.** The fuller follow-on (not the axis question, which is settled) would discover the voice-leading *idioms* (beyond chorale-vs-piano — contrapuntal/fugal, pianistic, jazz-comping…) and run a formal
  - *Subject found in* `cc_vl_idiom_discovery_report.md` line 198 — matched by the decisions register's own recogniser `VL-C` (`VL-c`)
    > | set | n | VL-cluster spread | step | leap | contrary | oblique | |---|---|---|---|---|---|---| | steely_dan | 22 | {c0:10, c1:9, c2:3} | 25% | 44% | 7% | 79% | | piazzolla | 6 | {c1:5, c2:1} | 39% | 31% | 5% | 82% | | hiromi | 19 | {c2:16, c1:2, c0:1} | 36% | 48% | 5% | 86% |
- **D-092** — A cross-cutting contract is stated once and never redefined in a layer document
  - *What the entry says the decision is:* Rules that apply to every stage are written down in one place. A stage's own document may use such a rule but may not restate it in its own words.
  - *Recorded at* `ARCHITECTURE.md:562-563`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 2, of which 1 ratify a document other than their own
  - *Subject found in* `cowork_confidence_contract.md` line 63 — matched by the decisions register's own recogniser `stated once` (`stated once`)
    > ## 4. The comparison frames (the §8 override arithmetic, stated once) A **frame** is a declared triple *(incumbent confidence, contradiction-strength measure, conversion)* under which one comparison is defined. The general §8 rule, stated once for all instances:
  - *Subject found in* `OPEN_ITEMS.md` line 204 — matched by the entry's own identity (`D-092`)
    > | OI-237 | ★ DOCUMENTATION GAP ([[OI-208]] ruling 2): sixteen load-bearing decisions have no layer-specification home; SEVEN of them govern a layer's behaviour and are unfindable from that layer's section | Of the 115 decisions in `DECISIONS.md`, 16 are recorded outside any layer specification. Seven are genuine gaps — D-032 (the [0,1] boundary-confidence contract, departed from at [[OI-231]]), D-096 (forms from theory, values fit ONCE), D-097 (held-out evaluation + capacity budget), D-098 (the exact-decode reserve; what the decoder actually prunes has NO specified form — the [[OI-226]] question), D-099 (negative evidence is information), D-100 (publish once, consumers read never re-derive — created BECAUSE facts were being siloed, and the specification of every layer it governs is silent on it), D-114 (no key abstention, in tension with D-090 which IS in a layer spec). The other nine ar
  - *Subject found in* `CLAUDE.md` line 360 — matched by the decisions register's own recogniser `stated once` (`stated once`)
    > IT GATES.** The line inside the documentation rows, stated once so the test is mechanical rather than a matter of taste: **what is owed decides it** — a pointer, an anchor, a label, a banner, a filing decision or a section boundary is apparatus; a correction to a statement about the analysis or its build state, or the completion of a specification, GATES, because the phase-1 rule in Conventions makes specifications COMPLETE and TRUE the thing that precedes everything else. **A row
- **D-097** — Held-out evaluation and a capacity budget are declared before any fit
  - *What the entry says the decision is:* Before the estimator's numbers are learned, we say in advance which music will be held back to test them on, and how many numbers we are allowed to learn at all. The headline number is always the one measured on the held-back music.
  - *Recorded at* `ARCHITECTURE.md:289-290`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 3, of which 2 ratify a document other than their own
  - *Subject found in* `CLAUDE.md` line 157 — matched by the decisions register's own recogniser `held-out` (`held-out`)
    > event declares its held-out data (split or k-fold) and its capacity budget (parameter count, regularization, justified against corpus size) BEFORE fitting; the headline claim is the held-out figure. A fitted-and-self-measured number is not established (#19). 21. **Ground truth is an instrument too.** The accuracy of ground truth is itself a measured quantity — per-axis annotator agreement, not an assumed binary (sharpens #9's "accurate").
  - *Subject found in* `cowork_joint_estimator_factorization.md` line 60 — matched by the decisions register's own recogniser `held-out` (`held-out`)
    > convex conditional likelihood under the held-out gate. **Factor granularity (amendment, user-ratified 2026-07-19 at the desk simulation — `cowork_factorization_desk_simulation.md` §4.1, the `bwv10.7@36000` length-bias finding):** the per-segment sums above are evaluated at these granularities — the pitch and spelling emissions **per
  - *Subject found in* `OPEN_ITEMS.md` line 204 — matched by the entry's own identity (`D-097`)
    > | OI-237 | ★ DOCUMENTATION GAP ([[OI-208]] ruling 2): sixteen load-bearing decisions have no layer-specification home; SEVEN of them govern a layer's behaviour and are unfindable from that layer's section | Of the 115 decisions in `DECISIONS.md`, 16 are recorded outside any layer specification. Seven are genuine gaps — D-032 (the [0,1] boundary-confidence contract, departed from at [[OI-231]]), D-096 (forms from theory, values fit ONCE), D-097 (held-out evaluation + capacity budget), D-098 (the exact-decode reserve; what the decoder actually prunes has NO specified form — the [[OI-226]] question), D-099 (negative evidence is information), D-100 (publish once, consumers read never re-derive — created BECAUSE facts were being siloed, and the specification of every layer it governs is silent on it), D-114 (no key abstention, in tension with D-090 which IS in a layer spec). The other nine ar
  - *Subject found in* `cowork_prefit_gates.md` line 23 — matched by the decisions register's own recogniser `held-out` (`held-out`)
    > ## The held-out evaluation protocol (OI-176; gates the fit event; #20/#19/#16) **What is being prevented:** a headline figure graded on data that helped fit it — including the subtle forms: a degree VOCABULARY derived from all-corpus counts, a smoothing constant chosen on the grading data, a threshold "checked" against the final metric.
  - *Subject found in* `open_items/OI-176.md` line 1 — matched by the decisions register's own recogniser `held-out` (`Held-out`)
    > # OI-176 — Held-out evaluation for the joint-estimator fit event STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record **Section C — Owned by Stage-5 (precision phase — do NOT fix earlier, #8)**
  - *Subject found in* `open_items/OI-177.md` line 1 — matched by the decisions register's own recogniser `OI-177` (`OI-177`)
    > # OI-177 — Capacity budget for the joint-estimator fit STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record **Section C — Owned by Stage-5 (precision phase — do NOT fix earlier, #8)**
  - *Subject found in* `cowork_stage5_fitter_design.md` line 38 — matched by the decisions register's own recogniser `held-out` (`held-out`)
    > | **validation pool** | Sources usable only for held-out validation, quality assurance, and statistics — never to shape a shipped parameter: all NC-class sources (the 40 DLC corpora, MCMA, Essen, …) and all no-license sources. Census §8c. | | **fitting split / held-out split** | The division *within* the fitting pool: parameters are optimized on the fitting split only; the held-out split is never optimized against and is scored only at declared checkpoints (§4.2 declares the one deliberate exception). The split discipline is the ratified OQ-C1 rule (roadmap E2 line: dev/held-out, demotion only by recorded decision, registry `split` field). | | **the batch stop** | The current hard regression stop: the batch-region BIR=false **case-identity sets** Baroque 53 / Jazz 24 / Default 53, with the two-tier class-(a)/(b) policy (CLAUDE.md). "Stop" is used, not "gate", to keep this word distinct f
  - *Subject found in* `cowork_score_census.md` line 263 — matched by the decisions register's own recogniser `held-out` (`held-out`)
    > | N5 | key/modulation GT | L3, S1/S2 | *(Wave-3 MEASURED, corrects the audit)* **KMT is NOT present as analyses at the WiR pin** `aa7539f1` (Corpus/Textbooks = 201 scores / **0** analysis.txt — verified by CC AND by Cowork glob); KMT acquisition = the DDMAL `key_modulation_dataset` upstream (direct-acquisition candidate, next corpus increment). Sears pivots: no public deposit. SWD score-aligned local keys unchanged (ChoCo). WiR analyses still carry local keys generally (N5 partial). *(Wave-3 ADDENDUM, `cc_wave3_addendum_report.md`)* **★ KMT ACQUIRED:** `DDMAL/key_modulation_dataset` @ `6602ae6a` cloned+pinned+walked — **201 annotated Humdrum .krn** (aldwell 7 / kostka-payne 15 / reger 117 / rimsky-korsakov 37 / tchaikovsky 25), key/modulation as `*C:` key-designation tokens + inline `NEWKEY=>:RN` markers in the **text spine; CC-BY-SA scores / MIT code; held-out. The direct-acquisition ca
  - *Subject found in* `OPEN_ITEMS.md` line 204 — matched by the entry's own identity (`D-097`)
    > | OI-237 | ★ DOCUMENTATION GAP ([[OI-208]] ruling 2): sixteen load-bearing decisions have no layer-specification home; SEVEN of them govern a layer's behaviour and are unfindable from that layer's section | Of the 115 decisions in `DECISIONS.md`, 16 are recorded outside any layer specification. Seven are genuine gaps — D-032 (the [0,1] boundary-confidence contract, departed from at [[OI-231]]), D-096 (forms from theory, values fit ONCE), D-097 (held-out evaluation + capacity budget), D-098 (the exact-decode reserve; what the decoder actually prunes has NO specified form — the [[OI-226]] question), D-099 (negative evidence is information), D-100 (publish once, consumers read never re-derive — created BECAUSE facts were being siloed, and the specification of every layer it governs is silent on it), D-114 (no key abstention, in tension with D-090 which IS in a layer spec). The other nine ar
- **D-098** — The exact-decode reserve - the declared prune was never adopted
  - *What the entry says the decision is:* The estimator was meant to be allowed to narrow its search when that gets too slow. The narrowing rule that was specified turned out to cost more than it saved, so the estimator still searches exactly - and how it actually narrows in practice was never specified.
  - *Recorded at* `ARCHITECTURE.md:303-304`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 3, of which 2 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 204 — matched by the entry's own identity (`D-098`)
    > | OI-237 | ★ DOCUMENTATION GAP ([[OI-208]] ruling 2): sixteen load-bearing decisions have no layer-specification home; SEVEN of them govern a layer's behaviour and are unfindable from that layer's section | Of the 115 decisions in `DECISIONS.md`, 16 are recorded outside any layer specification. Seven are genuine gaps — D-032 (the [0,1] boundary-confidence contract, departed from at [[OI-231]]), D-096 (forms from theory, values fit ONCE), D-097 (held-out evaluation + capacity budget), D-098 (the exact-decode reserve; what the decoder actually prunes has NO specified form — the [[OI-226]] question), D-099 (negative evidence is information), D-100 (publish once, consumers read never re-derive — created BECAUSE facts were being siloed, and the specification of every layer it governs is silent on it), D-114 (no key abstention, in tension with D-090 which IS in a layer spec). The other nine ar
  - *Subject found in* `cowork_stage5_fitter_design.md` line 187 — matched by the decisions register's own recogniser `prune` (`prune`)
    > 8. **Zero information loss (E-14, user principle):** calibration re-scales confidences; it never prunes the ranked alternatives a layer publishes. A reliability map is a monotone re-labeling, not a filter. 9. **Documentation in lockstep:** any commit touching a scoring term updates `docs/scoring_model.md` in the same commit (CLAUDE.md sync rule); the confidence contract's §3/§5 rows update when squashes or θ gain fitted values; this document flips to AS-BUILT status at landing.
  - *Subject found in* `ARCHITECTURE.md` line 311 — matched by the decisions register's own recogniser `OI-188` (`OI-188`)
    > architecture and no recorded basis. Both facts are open and are tracked at `OPEN_ITEMS.md` OI-188 (the reachability bound on every ceiling claim) and OI-226 (admission has no ratified basis); neither is settled here. *Tried and closed on the search — do not retry; the register carries each with its measurement: D-288 (beam widening, shelved), D-328 (a wider search over the same scoring, refuted), D-278 (the joint key-and-chord step over the two legacy decoders, shelved).*
  - *Subject found in* `ratification_surfaces/cowork_decisions_pending_ratification.md` line 153 — matched by the decisions register's own recogniser `circle-of-fifths` (`circle-of-fifths`)
    > **Provenance.** ARCHITECTURE.md:6301-6313 (§17.3), which gives a worked example from existing code (the circle-of-fifths interval deltas). No date or ratifier stated. ### D-125 — Every test documents the musical situation, the expected result, and what a failure means Every test must document:
  - *Subject found in* `cowork_stage5_fitter_design.md` line 187 — matched by the decisions register's own recogniser `prune` (`prune`)
    > 8. **Zero information loss (E-14, user principle):** calibration re-scales confidences; it never prunes the ranked alternatives a layer publishes. A reliability map is a monotone re-labeling, not a filter. 9. **Documentation in lockstep:** any commit touching a scoring term updates `docs/scoring_model.md` in the same commit (CLAUDE.md sync rule); the confidence contract's §3/§5 rows update when squashes or θ gain fitted values; this document flips to AS-BUILT status at landing.
  - *Subject found in* `open_items/OI-207.md` line 151 — matched by the entry's own identity (`D-098`)
    > Also corrected: two stale line anchors in [[OI-237]] (D-096 `:26`→`:25`, D-098 `:185`→`:194`→`:197`) plus its OI-208 consequence quote (`:59-62`→`:59-61`); three plain restatements the review named (D-033 is about OWNERSHIP of a contribution, not a restriction on evidence; D-050's never-reach-outside rule is the SLICER's, and re-slice equivalence is what makes extending the span lawful; D-100 carries its 2026-07-12 amendment); D-103's ratified successor pointer (now SUPERSEDED
  - *Subject found in* `DECISIONS.md` line 295 — matched by the entry's own identity (`D-098`)
    > | D-098 | The exact-decode reserve - the declared prune was never adopted | LIVE | — | `ARCHITECTURE.md` | | D-114 | The decoder commits its best path; there is no abstention on the key axis | LIVE | — | `ARCHITECTURE.md` | | D-270 | The held-out evaluation protocol - five-fold cross-validation grouped by ground-truth analysis file | LIVE | 2026-08-02 · user | `cowork_prefit_gates.md` “The held-out evaluation protocol” | | D-271 | The capacity budget - a cell keeps its own estimate only above a stated count, and free parameters are bounded against the training tokens | LIVE | 2026-08-02 · user | `cowork_prefit_gates.md` “The capacity budget” | | D-272 | The protocol constants are protocol, not tuning - changing one is an amendment, never a fitting act | LIVE | 2026-08-02 · user | `cowork_prefit_gates.md` the opening block (above the first section heading) |
  - *Subject found in* `open_items/OI-227.md` line 43 — matched by the decisions register's own recogniser `prune` (`prune`)
    > key prune ([[OI-226]]); whether the full 24-key set would recover some `fitBlocked` events (isolating the key prune as a third contributor) is a named follow-up probe not run here — it does not change the answer (these 21 empty the decode as shipped). *Provenance: CC-found 2026-07-28 at `cc_instruction_oi199_joint_p3_p4.md` Task 1 (population B), report
  - *Subject found in* `cowork_structural_integrity_audit.md` line 122 — matched by the decisions register's own recogniser `prune` (`prune`)
    > - alternatives are **never pruned** on COMMIT/INHERIT (`decode_chord_tests.cpp:1371-1397`). So the clean-target is: one governed carry (a single principled limit or uncapped), the different-root alternative as a natural consequence, **pedal detection decoupled as a reader over the carry** (the decoder has **no** pedal detection yet — a real gap the engage design must fill), and serialization + display as
  - *Subject found in* `cowork_handoff_archive.md` line 796 — matched by the decisions register's own recogniser `prune` (`prune`)
    > top-8 lattice prune exists but is secondary); `cc_key_mode_inference_diagnosis_report.md` (the failure taxonomy); `contrapunctus_findings.md` (the comparable product — mine the repository's OWN research holdings FIRST, a lesson learned twice). **WORKING NOTES FOR YOU (lessons this arc taught, so you do not relearn them):** the user
  - *Subject found in* `open_items/OI-244.md` line 1 — matched by the decisions register's own recogniser `prune` (`prune`)
    > # OI-244 — The key-prune tie-break ranks by absolute tonic pitch class — not transposition-covariant; the expected shifted key can fall out of the kept candidates STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record **Section D — joint decoder / candidate admission (declared to Cowork). Compounds [[OI-226]].**
  - *Subject found in* `STATUS.md` line 144 — matched by the decisions register's own recogniser `prune` (`prune`)
    > *2026-08-11, LATEST+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ (CC — **★ THE STATUS TOUCH AND THE OI-141 PREMISE RE-PIN — CLOSE: ALL FOUR TASKS REACHED, NO STOP, AND THE RE-PIN COMES BACK DIFFERENT FROM THE PACK'S OWN COUNT IN BOTH DIRECTIONS.** No `src/` change, no golden, nothing promoted, no `tools/corpus/` or `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to inference, no design. Dispatch **`cc_instruction_status_touch_and_oi141_premise_repin.md`**, Task 4 and the close. Per the OI-222 remedy this entry is a **POINTER** — every citation and every population lives in `cowork_away_returns.md` and in the generated artifacts, and none is restated here (**D-431**). **Read:** the returns file's **§2.22** first, then **§1.20**, then the queue's new **§22**. **★ THE SIX THINGS THE USER SHOULD SEE.** (1) **★
  - *Subject found in* `STATUS_ARCHIVE.md` line 178 — matched by the decisions register's own recogniser `OI-188` (`OI-188`)
    > *Last updated: 2026-07-20 (CC — **★ ALGORITHM COMPLETION STEP 2, THE WEIGHT FIT: RUN, ESTABLISHED, AND A STOP-FOR-REVIEW.** Dispatch `cc_instruction_weight_fit.md`; both user rulings executed (★R1 = W1 four-beat tritone window; ★R2 = C1 cap ≤ 14, the vector enumerated at **13 weights**). PYTHON-ONLY, instrument layer only — no `src/`, no build, no golden, no corpus regen, no re-baseline; pinned instruments import-only; the firewall grep-proven (`fit_weights.py` imports NO grading module; grading is confined to `fit_run.py` and feeds nothing back). **THE HEADLINE (pooled 5-fold CV, each piece decoded once by the model — tables AND weights — fitted on the complement of its own fold; piece-bootstrap 95 % CI, paired across arms): key-LOCAL 70.22 [68.27,72.26] vs identity 74.68 → paired −4.45 [−5.47,−3.46]; key-HOME 56.34 [53.04,59.65] vs 53.20 → +3.16 [+1.75,+4.52]; root 73.04 [70.92,75.26] 
  - *Subject found in* `open_items/OI-188.md` line 1 — matched by the decisions register's own recogniser `OI-188` (`OI-188`)
    > # OI-188 — The decode prune cannot reach the ground truth STATUS IS AUTHORITATIVE IN THE INDEX (OPEN_ITEMS.md) — this file carries narrative and provenance only and is NEVER the status of record **Section D — Instrument / measurement layer**
- **D-105** — The spelling written in the score is read through ONE shared interpreter
  - *What the entry says the decision is:* How a note is spelt on the page - F sharp versus G flat - is interpreted in one shared place, not re-implemented by each stage that needs it.
  - *Recorded at* `ARCHITECTURE.md:2007-2008`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 2, of which 1 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 385 — matched by the decisions register's own recogniser `lineOfFifths` (`lineOfFifths`)
    > | OI-347 | **The two Layer-1.5 primitives are sited in different sections of the canonical architecture document**, and only one of the two sitings states its reason | Found 2026-08-07 (CC, `cc_instruction_owner_rulings_homing.md`) discharging the STOP the homing of **D-625** carries — *STOP if the primitive is specified elsewhere*. `ARCHITECTURE.md` §3.3's terminology note names exactly two L1.5 primitives, *"the shared `lineOfFifths` spelling interpreter, the phrase-boundary primitive."* The **phrase-boundary** primitive's contract pointer sits at **Layer 1** and states its reason in full — the document defines L1.5 as the thin derived-view layer over the L1 note model and carries no section of its own for it, so the layer the primitive is a view OVER is the nearest owning section. The **spelling** primitive is named in three places and **specified in none**: the §3.3 terminology note 
  - *Subject found in* `STATUS.md` line 162 — matched by the decisions register's own recogniser `line-of-fifths` (`line-of-fifths`)
    > *Last updated: 2026-08-07, LATEST+++++++++++++++++++++++++++++++++++++++++++ (CC — **★ THE FORTY-EIGHT OWNER RULINGS ARE APPLIED: THE JOINT ESTIMATOR GAINS A SECTION A DECISION CAN BE SITED INSIDE — BUILT BY ADDING A HEADING AND MOVING NOTHING — AND EVERY ENTRY BUT ONE IS HOMED. THE ONE HELD BACK IS HELD BY A CHECK THE RULING ITSELF ORDERED, NOT BY A REFUSAL: ITS DECISION HAS ONE END LIVE AND ONE END LEGACY.** No `src/` change, no golden / `tools/corpus/` / `tools/robust_stop/` movement, no behaviour change to the analysis, no fix to inference, no design. Dispatch **`cc_instruction_owner_rulings_homing.md`** (cite it by THAT name); the ruling record it applies is **`cowork_owner_rulings_2026_08_07.md`**, read whole (**D-643**). Per the OI-222 remedy this entry is a **POINTER** — every count lives in the generated artifacts and none is restated here (**D-431**). **Read, in this order:** `
- **D-106** — The augmented-sixth labels are gated to the Standard and Baroque presets
  - *What the entry says the decision is:* The specific Italian, French and German augmented-sixth labels are shown only under the classical presets.
  - *Recorded at* `ARCHITECTURE.md:4862-4863`; *status* `superseded-in-fact`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 3, of which 3 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 3886 — matched by the decisions register's own recogniser `augmented sixth` (`augmented sixth`)
    > Berklee/jazz ("Δ7", "ø7"), classical (augmented sixth notation) - *Symbol vocabulary:* half-diminished ø vs "m7b5" vs "ø7"; major seventh Δ vs "maj7" vs "M7"; augmented "+" vs "aug" When `IChordSymbolFormatter` is introduced, `ChordSymbolFormatter::Options` becomes
  - *Subject found in* `ARCHITECTURE.md` line 3886 — matched by the decisions register's own recogniser `augmented sixth` (`augmented sixth`)
    > Berklee/jazz ("Δ7", "ø7"), classical (augmented sixth notation) - *Symbol vocabulary:* half-diminished ø vs "m7b5" vs "ø7"; major seventh Δ vs "maj7" vs "M7"; augmented "+" vs "aug" When `IChordSymbolFormatter` is introduced, `ChordSymbolFormatter::Options` becomes
  - *Subject found in* `DECISIONS.md` line 699 — matched by the entry's own identity (`D-106`)
    > | D-106 | The augmented-sixth labels are gated to the Standard and Baroque presets | SUPERSEDED IN FACT | — | `ARCHITECTURE.md` | | D-234 | A chord symbol string must be valid under chords_std.xml; chords.xml is not relied on | LIVE | 2026-08-02 · user | `ARCHITECTURE.md` | | D-295 | Zero information loss to the end user - every inferred object must be displayable | LIVE | 2026-08-02 · user | `ARCHITECTURE.md` | | D-304 | The analyzer always emits its fullest reading; simplifying it happens only when comparing against a corpus, never in the product | LIVE | 2026-08-02 · user | `ARCHITECTURE.md` | | D-471 | The sub-beat annotation duration gate is not retired on argument — it is kept or dropped on a measured observation run, with the verdict stated in advance | LIVE | 2026-08-04 · user | `ARCHITECTURE.md` |
  - *Subject found in* `cowork_handoff_archive.md` line 5108 — matched by the decisions register's own recogniser `augmented sixth` (`Augmented sixth`)
    > Augmented sixth chords (It+6, Fr+6, Ger+6 — structurally distinct from dom7♭5 despite PC overlap), Neapolitan (♭II / N6), borrowed chords / modal mixture (♭VII in major, iv in major), extended tonicization chains beyond V/x and vii°/x. ---
  - *Subject found in* `STATUS.md` line 760 — matched by the decisions register's own recogniser `aug-sixth` (`aug-sixth`)
    > *Last updated: 2026-07-27, twelfth entry (CC — **★ SEAMS PART 2 — P7: THE PARTITION CLOSE-OUT — completeness verified, docs consolidated (pre-switch state).** Dispatch `cc_instruction_notation_p7.md`. The LAST unit of the seams-2 partition — NO new behavior, NO new code path: a read-only completeness verification + a documentation consolidation, closing the partition so the switch ratification assembles on a verified-complete record. **TASK 1 — THE COMPLETENESS VERIFICATION (read-only; generated artifact `tools/notation_seams/partition_completeness.json`) — OVERALL COMPLETE / PASS, ZERO FINDINGS:** (1) **every audited consumer has its record-arm branch behind the default-OFF `useJointNotationRecord`** — the span-annotation emitter (`notationcomposingbridge.cpp:1491`), the note-seam funnel `analyzeHarmonicContextAtTick` (`:729`), the implode `populateChordTrack` (`notationimplodebridge.cp
  - *Subject found in* `STATUS_ARCHIVE.md` line 2163 — matched by the decisions register's own recogniser `augmented sixth` (`augmented sixth`)
    > - **Step 6 (augmented sixth It+6/Fr+6/Ger+6):** Not implemented anywhere. Per stop condition, must be implemented as standalone composing unit first. Deferred. - **Nashville mode confirmed clean.** `writeRomanNumerals=false` (Nashville-only call) skips the entire cadence/pivot annotation block. No pivot or cadence labels in Nashville output.
  - *A ratified document that could not be located:* `cowork_phase5c_step4_report.md` (named at OPEN_ITEMS.md line 51)
- **D-107** — American English throughout
  - *What the entry says the decision is:* Analyzer, not analyser; color, not colour.
  - *Recorded at* `ARCHITECTURE.md:761`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 3, of which 2 ratify a document other than their own
  - *Subject found in* `cowork_rulings_2026_08_11_fourteenth_stop.md` line 30 — matched by the decisions register's own recogniser `behaviour` (`behaviour`)
    > behaviours across five siblings, and a permanently unclosable entry misread as population. ## Ruling 62 — the FILING CONVENTION: two branches by document kind **Branch one — dated reports** (completed audits, probe reports, falsified or superseded
  - *Subject found in* `ARCHITECTURE.md` line 759 — matched by the decisions register's own recogniser `American English` (`American English`)
    > ### 2.5 American English Throughout All identifiers, comments, and documentation use American English spelling. ```cpp
  - *Subject found in* `ARCHITECTURE.md` line 759 — matched by the decisions register's own recogniser `American English` (`American English`)
    > ### 2.5 American English Throughout All identifiers, comments, and documentation use American English spelling. ```cpp
  - *Subject found in* `STATUS.md` line 8 — matched by the decisions register's own recogniser `behaviour` (`behaviour`)
    > *Last updated: 2026-08-16 (CC — **★ THE PREPARATION PHASE'S SECOND BATCH CLOSES: BOTH RULED ACTS ARE PERFORMED, AND WHAT GOES BACK TO THE USER IS THREE SURFACES OF EVIDENCE RATHER THAN ONE VERDICT.** No `src/` change, no golden, **no test changed, moved or run**, nothing under `tools/corpus/` or `tools/robust_stop/`, no behaviour change to the analysis, no fix to inference, no design; **no measurement of the analysis built, designed, scoped or run; no open-items row created, flipped or discarded; no decisions-register entry written, edited or retired; no soft-discard executed; no file moved, archived or deleted; no mining, no landing**. Dispatch **`cc_instruction_preparation_second.md`**, Task 4, closing the batch. Per the OI-222 remedy this entry is a **POINTER** — the report is `cc_report_preparation_second.md`, the full close is THE PREPARATION SECOND BATCH section of `cowork_away_ret
  - *Subject found in* `OPEN_ITEMS.md` line 185 — matched by the decisions register's own recogniser `behaviour` (`behaviour`)
    > | OI-365 | ★ **Whether the live competition design owes the two carved-out gates' CONCERNS is a phase-3 fix-plan input — recorded, not decided** | Created 2026-08-09 (CC, `cc_instruction_return_continuation_5.md` Task 0) by the register rule (c) check the user's **Ruling 34** of `cowork_rulings_2026_08_09_fifth_stop.md` requires. That ruling settles the conflict the Ruling-27 fact-gathering pass located between **D-580** and the retirement map's first entry, and settles it as **reading (ii): different acts, both records true.** D-580's carve-out is about a DISSOLUTION into the competition that never ran; the map's R1 performs a DELETION of the legacy path; **both stand, R1 is NOT qualified, and the carve-out is recorded as scoped to the unrun dissolution.** The excluded reading is recorded (#12) — that R1 overlooked the carve-out, which would qualify a ratified map on an assumption about
- **D-118** — GPL v3, and every external library must be GPL v3 compatible
  - *What the entry says the decision is:* All the code is released under the GPL v3 licence, the same licence MuseScore Studio uses, and no outside library may be used unless its licence is compatible with that.
  - *Recorded at* `ARCHITECTURE.md:636`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 200 — matched by the entry's own identity (`D-118`)
    > | OI-271 | ★ CONFLICT: the ratified fitting-pool licence constraint says a shipped fitted value may be fitted only on freely-licensed music; the joint estimator's tables and weights are fitted on the DCML annotations that same constraint classifies as validation-only | `cowork_handoff_archive.md:2478-2481` records, **user-ratified 2026-07-04 and marked BINDING on the fitter design** (register entry **D-292**): "ship-intended weights fit only on the PD/CC0/CC-BY pool; NC-class (all DCML, MCMA, Essen…) + no-license sources = validation/QA only". `CLAUDE.md` gate block (A) records that production inference on both surfaces decodes at the committed all-326 tables + the direct-metric SELECTED weight vector — fitted against the *When in Rome* / DCML analyses of the 326 Bach chorales, the corpus the constraint names in its own non-commercial class. Nothing this pass read amends, scopes or withd
- **D-119** — The MuseScore contributor licence agreement is signed before any pull request
  - *What the entry says the decision is:* Before any of this work is offered back to MuseScore as a pull request, the contributor agreement with MuseScore must be signed.
  - *Recorded at* `ARCHITECTURE.md:639`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 3, of which 3 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 221 — matched by the entry's own identity (`D-119`)
    > | OI-254 | NON-CONFORMANCE: the status-bar harmonic annotation is UNLOCALIZED and outside the accessibility tree — against the ratified §12.1 localization + accessibility statements (D-118/D-119) | Raw string concatenation with no translation call (`notationcomposingbridge.cpp:835-844`); plain-text append to accessibility info (`notationaccessibility.cpp:203-207`), never in `screenReaderInfo`. `ARCHITECTURE.md:5854-5858` mandates MuseScore Qt localization (English + Swedish for ALL new strings) and MuseScore accessibility patterns; the user re-stated the requirement 2026-08-01 | notation / presentation / decision-conformance | OPEN — Cowork-found 2026-08-02 (requirement-side review); a presentation-layer completion at its #8-correct stage (no inference change anywhere) | [detail](open_items/OI-254.md) | | OI-255 | `addAnalyzedHarmonyToSelection` is UNREACHABLE from the UI — implemented, 
  - *Subject found in* `cowork_joint_estimator_architecture.md` line 44 — matched by the decisions register's own recogniser `CLA` (`cla`)
    > - **notated signature + declared mode** → prior on `(tonic, mode)` (fixed input); - **notated spelling + accidentals** → spelling-conditioned emission + mode disambiguation; - **cadence votes + leading-tone events** → emission/transition factor on `(tonic, mode)` at cadence points (the OI-166 channel, as a *factor*); - **progression grammaticality** → the chord transition term (licensed motions weighted up); - **harmonic rhythm + boundary strength** → the segmentation (semi-Markov segment-duration) model;
  - *Subject found in* `cowork_joint_estimator_factorization.md` line 14 — matched by the decisions register's own recogniser `CLA` (`cla`)
    > estimator funnel stays shut. Every load-bearing conditional-independence claim is an explicit premise (P1–P8 below), labeled THEORY or ASSUMPTION per #17a, with its false-negative path named per #17e. **Ratifying this document ratifies the structure and the premise ledger; all VALUES remain unfit** and enter only through the ratified fitting protocol (§5a) under the OI-176/OI-177 gates.
  - *Subject found in* `cowork_prefit_gates.md` line 29 — matched by the decisions register's own recogniser `CLA` (`CLA`)
    > 1. **Unit and axes.** Evaluation is on the robust unit (CLAUDE.md block (A)): duration-weighted union-of-boundaries cells, root governs, RN + key(home, local) tracked beside, DCML-only GT through `dcml_parser.load_wir_regions` (the OI-142-corrected substrate), all three presets. 2. **The split: 5-fold cross-validation [prov-ratify] over the 326 WiR-covered pieces, grouped by WiR analysis file.** The 326 pieces resolve to 324 distinct analysis files (`docs/score_inventory.md`
  - *Subject found in* `cc_adoption_measurement_report.md` line 25 — matched by the decisions register's own recogniser `CLA` (`cla`)
    > The per-preset run enumeration + class-(a)/(b) split + set-diff is the pinned **a8 + robust_stop_diff R10 sandwich**, run over A's decode rendered as a candidate corpus (the ratified pattern). Pooled columns + piece-bootstrap = `fit_run`; modulation = `search_run`. - **The firewall.** The only decoder input is the frozen selected weight vector; nothing here fits, tunes, or feeds a value back. The instruments are import-only; no `src/`, no build, no golden, no
  - *Subject found in* `STATUS.md` line 3 — matched by the decisions register's own recogniser `CLA` (`Cla`)
    > **Living document.** Claude Code reads this at the start of every session. Update this as the last act when anything changes. For stable architectural decisions, see ARCHITECTURE.md. Historical entries live in `STATUS_ARCHIVE.md` (reference-only, NOT part of the session-start read) — a superseded entry moves there instead of accumulating here.
  - *Subject found in* `cowork_handoff.md` line 4 — matched by the decisions register's own recogniser `CLA` (`CLA`)
    > ## ★★★★★ COWORK SESSION CLOSE (NINETEENTH, 2026-08-15) — THE PHASE-DEFINITION SITTING IS HELD AND ALL FIVE DECISIONS ARE RULED; THE SIX PHASES GOVERN; THE D-231 REPHRASING IS LANDED IN `CLAUDE.md` AND VERIFIED AT THE OBJECTS; EIGHTEENTH-STOP RULINGS 6 AND 7 ARE DISCHARGED. THE NEXT WRITING-SIDE ACT IS THE PREPARATION PHASE'S OPENING DISPATCH. THE CURRENT ENTRY POINT. **You start clueless. Read this block first. The standing bars of the earlier blocks REMAIN BINDING — with ONE standing caveat now DISCHARGED: `CLAUDE.md`'s D-231 entry no longer needs a warning from this file, because the entry itself now opens with the ruled supersession block
  - *Subject found in* `cc_instruction_notation_switch.md` line 3 — matched by the decisions register's own recogniser `CLA` (`CLA`)
    > **Read first (every session):** `C:\s\MS\CLAUDE.md` (gate block (A) — you will update its STAGED SCOPE text in this very commit; read it as the outgoing state), `C:\s\MS\STATUS.md`, `C:\s\MS\BUILD_AND_TEST.md`, `C:\s\MS\OPEN_ITEMS.md` (INDEX), the P7 completeness artifact (`tools/notation_seams/partition_completeness.json`) and the P6 classified report (`tools/notation_seams/dualarm_classified_report.json`) — **the report is this commit's
  - *Subject found in* `CLAUDE.md` line 1 — matched by the decisions register's own recogniser `CLA` (`Cla`)
    > # Claude Code — Standing Instructions for This Repository ## Guiding principles The standing decision guides for all work in this repository. Every design, build, and
  - *Subject found in* `cowork_joint_estimator_factorization.md` line 14 — matched by the decisions register's own recogniser `CLA` (`cla`)
    > estimator funnel stays shut. Every load-bearing conditional-independence claim is an explicit premise (P1–P8 below), labeled THEORY or ASSUMPTION per #17a, with its false-negative path named per #17e. **Ratifying this document ratifies the structure and the premise ledger; all VALUES remain unfit** and enter only through the ratified fitting protocol (§5a) under the OI-176/OI-177 gates.
  - *Subject found in* `OPEN_ITEMS.md` line 221 — matched by the entry's own identity (`D-119`)
    > | OI-254 | NON-CONFORMANCE: the status-bar harmonic annotation is UNLOCALIZED and outside the accessibility tree — against the ratified §12.1 localization + accessibility statements (D-118/D-119) | Raw string concatenation with no translation call (`notationcomposingbridge.cpp:835-844`); plain-text append to accessibility info (`notationaccessibility.cpp:203-207`), never in `screenReaderInfo`. `ARCHITECTURE.md:5854-5858` mandates MuseScore Qt localization (English + Swedish for ALL new strings) and MuseScore accessibility patterns; the user re-stated the requirement 2026-08-01 | notation / presentation / decision-conformance | OPEN — Cowork-found 2026-08-02 (requirement-side review); a presentation-layer completion at its #8-correct stage (no inference change anywhere) | [detail](open_items/OI-254.md) | | OI-255 | `addAnalyzedHarmonyToSelection` is UNREACHABLE from the UI — implemented, 
  - *Subject found in* `cowork_prefit_gates.md` line 29 — matched by the decisions register's own recogniser `CLA` (`CLA`)
    > 1. **Unit and axes.** Evaluation is on the robust unit (CLAUDE.md block (A)): duration-weighted union-of-boundaries cells, root governs, RN + key(home, local) tracked beside, DCML-only GT through `dcml_parser.load_wir_regions` (the OI-142-corrected substrate), all three presets. 2. **The split: 5-fold cross-validation [prov-ratify] over the 326 WiR-covered pieces, grouped by WiR analysis file.** The 326 pieces resolve to 324 distinct analysis files (`docs/score_inventory.md`
  - *Subject found in* `open_items/OI-176.md` line 7 — matched by the decisions register's own recogniser `CLA` (`cla`)
    > | OI-176 | ★ Held-out evaluation for the joint-estimator fit event: A's values are to be fit once against DCML GT and graded on the same GT through the robust unit — a figure graded on the data that fit it is not established (#19/#16). Declare the fit/held-out split (or k-fold over the 326 covered pieces) BEFORE any value is fit; the headline claim is the held-out figure | `cowork_joint_estimator_architecture.md` §7 item 1 | PROTOCOL RATIFIED 2026-07-19 (`cowork_prefit_gates.md`): 5-fold CV grouped by shared WiR analysis file, committed seed/assignment artifact, all fitted objects (incl. vocabulary threshold, smoothing, L2) train-fold-only, headline = pooled CV figure + piece-bootstrap 95 % CI, identity-weight ablation through the same folds, BCMH reserved off-CV; boundary/metric counts gated on OI-184 — pending the fit event |
  - *Subject found in* `open_items/OI-177.md` line 7 — matched by the decisions register's own recogniser `CLA` (`cla`)
    > | OI-177 | Capacity budget for the joint-estimator fit: parameter count and any regularization declared and justified against corpus size (352 stems, one composer) before fitting — "fit once globally" can still overfit in one shot; fitted values are Bach-chorale values, generalization claims stay de-scoped (the OI-7 pattern) | `cowork_joint_estimator_architecture.md` §7 item 2 | PROTOCOL RATIFIED 2026-07-19 (`cowork_prefit_gates.md`): pre-fit parameter-inventory artifact (#17f), cell own-MLE iff count ≥ 20 else declared-parent pooling, free params ≤ tokens/10, ≤ 12 weights, desk-sim §4.3 sensitive cells get named treatment records + post-fit re-check of C2/C3/S5 prediction-first, fit-scope declaration restated (Noland lesson) — pending the fit event |
  - *Subject found in* `cowork_stage5_fitter_design.md` line 28 — matched by the decisions register's own recogniser `CLA` (`cla`)
    > | **the fitter** | The Stage-5 component this document designs: the machinery that replaces hand-chosen numeric constants in the harmonic-analysis scoring pipeline with values optimized against the declared objective, under the declared constraints. | | **constant / parameter** | A numeric value in the scoring pipeline that was chosen by hand (a bonus, penalty, threshold, weight, squash constant, or override factor θ). "Parameter" is used once the constant is under the fitter's management. | | **objective** | The single quantity the fitter maximizes: duration-weighted **root agreement** on the robust unit under variant (b), measured on the fitting split of the licensed pool. Declared fully in §4.2, including the scope of every constraint attached to it. | | **robust unit** | The granularity-robust **union-of-boundaries cell**: overlay our region boundaries with the human-annotation row b
  - *Subject found in* `cowork_score_census.md` line 18 — matched by the decisions register's own recogniser `CLA` (`cla`)
    > | Container class | Closure state (v1) | |---|---| | DCMLab GitHub org — **127 repos**, incl. `distant_listening_corpus` (**40 submodules** — the census's original "41" was an overcount, corrected at Wave-1 onboarding from the live `.gitmodules`, 2026-07-02), `dcml_corpora` (12), `romantic_piano_corpus` (9); also `schema_annotation_data` (voice-leading-schema GT over Mozart sonatas) | **Fully enumerated** [verified; Wave-1 ONBOARDED — all 40 present, hash-pinned, registry v2 `tools/score_census_registry.json`] · [Wave-2 ONBOARDED the DCMLab-org `schema_annotation_data` **annotation bed** — see `cc_corpus_wave2_report.md`] | | When-in-Rome meta-corpus — full component list | **Fully enumerated** | | ChoCo — all 18 partner datasets | **Fully enumerated** |
- **D-215** — Gating the root-continuity bonus on a sparse predecessor is a dead end
  - *What the entry says the decision is:* Making the bonus that rewards keeping the same root depend on how much evidence the previous chord had was tried in two forms and abandoned.
  - *Recorded at* `docs/scoring_model.md:1137`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1948 — matched by the entry's own identity (`D-215`)
    > **Tried and closed on the chord layer — do not retry; the register carries each with its measurement: D-215, D-299, D-300, D-301, D-302, D-317, D-318, D-319, D-320, D-328.** **★ A REBUILT OR RE-TUNED CHORD SCORING MUST NOT RELY ON THE HELD-NOTE REPETITION BONUS THE FAITHFUL NOTE MODEL REMOVED (re-homed into this specification 2026-08-08 on the user's ruling).** Before the note reader was rebuilt, a note held across a tie was counted more than once, and that spurious extra
  - *Subject found in* `cowork_away_returns.md` line 1231 — matched by the entry's own identity (`D-215`)
    > is named at the entry** — D-302 for a procedure with no code, D-215 for an obligation never executed, D-284 and D-243 for a finding about a legacy surface, D-325 for a principle adjacent to one already transferred. **A2 HOLDS.** The population is eleven, derived at task start from the tool's own output and not
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-256** — Every design document opens with one of four declared status banners
  - *What the entry says the decision is:* A design document states its status in one line at the top: draft for sign-off, signed with a date, as-built with a date and commits, or superseded with a pointer to what replaced it. When the code or a decision moves, the document moves with it.
  - *Recorded at* `cowork_design_doc_template.md:161-164`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 189 — matched by the decisions register's own recogniser `Status-banner convention` (`status-banner convention`)
    > | OI-230 | The writing-standards conformance question — DEFERRED by the user to the Friday discussion | Recorded so it does not live only in conversation (rule (e)). `cowork_design_doc_template.md` is the ratified home of the project's writing standards: predicate-qualification (user 2026-06-24), defined-terms/plain-vocabulary/no-shorthand (user 2026-07-02, rule 5's own examples key/bar/measure), the fourteen-section document structure (arc42 + IEEE 1016), the status-banner convention, the implementation/test locator rule (+ two worked examples not yet read: `cowork_spec_language_sweep.md`, `cowork_layer3_spec_language_sweep.md`). Open FOR FRIDAY, not this dispatch: (a) does the current documentation tree conform (several decision-surface docs plainly do not follow the fourteen-section structure — gap or deliberate exemption unjudged); (b) are DOCUMENTS in or out of the conformance enume
- **D-287** — Key-as-distribution is SHELVED - its motivating case was already fixed and no live target was found
  - *What the entry says the decision is:* Carrying a ranked distribution of key candidates forward, instead of one committed key, was withdrawn: the one failure it was designed to fix had already been fixed another way, no other case in the corpus needed it, and the confidence number it would have been weighted by is not trustworthy.
  - *Recorded at* `ARCHITECTURE.md:1751-1755`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1744 — matched by the entry's own identity (`D-287`)
    > **Tried and closed on this layer — do not retry; the register carries each with its evidence: D-287 (key-as-distribution, shelved), D-290 (the key-agnostic local cadence approach, falsified).** **★ WHAT THOSE TWO ARE, re-homed into this specification 2026-08-08; both ⚠ LEGACY-SCOPED (their subject is this layer's dormant pipeline).** Until now each lived ONLY in a session-handoff archive — a surface that is not among the session-start reads and is not a home for a standing decision —
  - *Subject found in* `STATUS.md` line 369 — matched by the entry's own identity (`D-287`)
    > premise: at least THREE of the fourteen archive-homed decisions, D-286 included — the audit's FOUNDING CASE — also live in design documents, and D-287's archive entry says so; the diagnosis changes from "buried in an archive" to "recorded three times, none of them a session-start read"**); the [[OI-268]] note (the design-surface count 25 → **51**); then [[OI-274]] and [[OI-275]], the two new rows. **★ [[OI-275]] IS THE SHARP ONE:** the SIGNED Layer-4 specification makes COMPLETE candidate listing the governing design decision (**D-329**, three times over, with a measured defense), while the production decoder's admission prunes and returns **0 segments on 13 of 23 large scores** — which turns [[OI-226]]'s *absence* of a basis into an *opposition* to a
  - *Subject found in* `cowork_away_returns.md` line 694 — matched by the entry's own identity (`D-287`)
    > at D-288, the not-the-key-path half at D-287 (with D-283's positive counterpart at D-001/D-096), the positive half at D-284 — which this entry's own provenance already calls the same insight independently derived, and which the user ruled superseded into D-036/D-001/D-010 on 2026-08-02 — and what-delivers-instead at D-472 and D-001. The excluded alternative is recorded: homing it as doctrine in the search block would state a rule that section already carries.
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-290** — The key-agnostic local cadence approach is FALSIFIED at its precision ceiling
  - *What the entry says the decision is:* Deciding the key from cadences found without knowing the key, one cadence at a time, was tested to its limit and cannot be made accurate enough to use. The remaining errors need either a long-range key decision or a different kind of model - not a better local cadence rule.
  - *Recorded at* `ARCHITECTURE.md:1762-1766`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1744 — matched by the entry's own identity (`D-290`)
    > **Tried and closed on this layer — do not retry; the register carries each with its evidence: D-287 (key-as-distribution, shelved), D-290 (the key-agnostic local cadence approach, falsified).** **★ WHAT THOSE TWO ARE, re-homed into this specification 2026-08-08; both ⚠ LEGACY-SCOPED (their subject is this layer's dormant pipeline).** Until now each lived ONLY in a session-handoff archive — a surface that is not among the session-start reads and is not a home for a standing decision —
  - *Subject found in* `docs/stage4c_cadence_key_design.md` line 12 — matched by the entry's own identity (`D-290`)
    > day later — register entry **D-290**, homed in `ARCHITECTURE.md`'s key layer, whose own words are that deciding the key from cadences found without knowing the key, one cadence at a time, was tested to its limit and cannot be made accurate enough to use; the remaining errors need either a long-range key decision or a different kind of model, not a better local cadence rule. **What survives the falsification is entered separately as D-616 and D-617 and is NOT falsified.**
  - *Subject found in* `cowork_away_returns.md` line 5735 — matched by the entry's own identity (`D-290`)
    > the document. The entry that records this falsification is **D-290**, and **its own status is `live`** — the FINDING stands; what was falsified is the approach the document designs. **So the signature encoded a wrong premise: a falsified approach does not show up as a falsified-status entry.** The two are different things and the derivation conflated them.
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-297** — Correction of record: never computing a possibility is not information loss; only discarding a computed one is
  - *What the entry says the decision is:* The no-information-loss principle forbids throwing away something the analysis has worked out. It does not require working out everything that could be worked out. Deciding, on measured evidence, not to compute a possibility is an ordinary design decision - you cannot lose what you never had.
  - *Recorded at* `ARCHITECTURE.md:1245-1247`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 312 — matched by the entry's own identity (`D-297`)
    > | OI-272 | Fourteen standing decisions are recorded ONLY in the session-handoff ARCHIVE — a surface two governing documents declare outside the session-start read | [[OI-266]]'s class one level out (and [[OI-268]]'s sibling): reading `cowork_handoff_archive.md` IN FULL entered **D-286…D-299**, every one `nonspec_kind: "unhomed"`, raising the register's tracking-surface-only count 0 → 14. Three are SHELVINGS WITH EVIDENCE (D-286 whole-score interactive analysis — **the founding case of [[OI-207]] itself**; D-287 key-as-distribution; D-288 beam widening), one a FALSIFICATION (D-290), two standing PROHIBITIONS (D-291, D-299), four binding CONSTRAINTS (D-292 → [[OI-271]]; D-293; D-294; D-295), two scoping CORRECTIONS of other decisions (D-296 bears on the ruled D-229; D-297 scopes D-099 / principle #12), one meta-principle (D-289) and one standing method (D-298). The archive is worse than th
- **D-299** — No negative-margin guard may be added - it would break every intentional backward-swap gate
  - *What the entry says the decision is:* A rule that refuses to let a later correction step overturn the leading reading when the margin against it is negative must not be added. Several correction steps exist precisely to overturn a leading reading, and such a rule would disable all of them.
  - *Recorded at* `docs/scoring_model.md:1413-1417`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1948 — matched by the entry's own identity (`D-299`)
    > **Tried and closed on the chord layer — do not retry; the register carries each with its measurement: D-215, D-299, D-300, D-301, D-302, D-317, D-318, D-319, D-320, D-328.** **★ A REBUILT OR RE-TUNED CHORD SCORING MUST NOT RELY ON THE HELD-NOTE REPETITION BONUS THE FAITHFUL NOTE MODEL REMOVED (re-homed into this specification 2026-08-08 on the user's ruling).** Before the note reader was rebuilt, a note held across a tie was counted more than once, and that spurious extra
  - *Subject found in* `STATUS.md` line 422 — matched by the entry's own identity (`D-299`)
    > open-items living check 272/272). **NEXT: the user ratifies (or corrects) D-286…D-299 and rules on OI-271, and on OI-272 TOGETHER WITH OI-268 (one ruling session — the answer to "does ruling 2 reach a delegated contract document?" bounds what is owed for an archive); then the wave's third partition — the 1,084 design-document clusters, untouched by this wave, and the 99 remaining `STATUS_ARCHIVE.md` clusters — before phase 2 opens.**)*
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-300** — Gate M (minor read as diminished) is DEFERRED and must not be retried without a new runtime signal
  - *What the entry says the decision is:* A proposed correction rule that would have re-read a minor chord as a diminished one on the same root is abandoned. It is not to be attempted again unless some new evidence becomes available while the music is being analysed.
  - *Recorded at* `docs/scoring_model.md:1418-1424`; *status* `deferred`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1948 — matched by the entry's own identity (`D-300`)
    > **Tried and closed on the chord layer — do not retry; the register carries each with its measurement: D-215, D-299, D-300, D-301, D-302, D-317, D-318, D-319, D-320, D-328.** **★ A REBUILT OR RE-TUNED CHORD SCORING MUST NOT RELY ON THE HELD-NOTE REPETITION BONUS THE FAITHFUL NOTE MODEL REMOVED (re-homed into this specification 2026-08-08 on the user's ruling).** Before the note reader was rebuilt, a note held across a tie was counted more than once, and that spurious extra
  - *Subject found in* `STATUS.md` line 395 — matched by the entry's own identity (`D-300`)
    > prohibitions absent from `docs/scoring_model.md` §8 (D-300/D-301/D-302) and **D-315 — a THIRD live edit to MuseScore's own code, verified at the objects, absent from `CLAUDE.md`'s local-patches section and carrying no distribution disposition ([[OI-273]]; [[OI-241]]'s count was one short)**. **★ ONE new bulk rule, BR-18**, over `STATUS_ARCHIVE.md`'s non-ruling prose (69 clusters) — licensed by the full read and by nothing else, mirroring BR-17 one file over. All guards PASS (`--verify` 315/315 quotes, 310/310 anchors, references ALL; `--check` 14,460/14,460 clusters, 15,224/15,224 occurrences; register `--check` across 21 files; open-items living check 273/273). **NEXT: the user ratifies (or corrects) D-300…D-315 and rules on [[OI-273]], and on
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-301** — Gate N (major read as an inverted minor) is DEFERRED and must not be retried without a multi-region model
  - *What the entry says the decision is:* A proposed correction rule that would have re-read a root-position major chord as the first inversion of a minor chord is abandoned. Six real cases were found against roughly two hundred and seventy wrong firings, so it is not to be attempted again without a model that reads several chords together.
  - *Recorded at* `docs/scoring_model.md:1425-1430`; *status* `deferred`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1948 — matched by the entry's own identity (`D-301`)
    > **Tried and closed on the chord layer — do not retry; the register carries each with its measurement: D-215, D-299, D-300, D-301, D-302, D-317, D-318, D-319, D-320, D-328.** **★ A REBUILT OR RE-TUNED CHORD SCORING MUST NOT RELY ON THE HELD-NOTE REPETITION BONUS THE FAITHFUL NOTE MODEL REMOVED (re-homed into this specification 2026-08-08 on the user's ruling).** Before the note reader was rebuilt, a note held across a tie was counted more than once, and that spurious extra
  - *Subject found in* `STATUS.md` line 395 — matched by the entry's own identity (`D-301`)
    > prohibitions absent from `docs/scoring_model.md` §8 (D-300/D-301/D-302) and **D-315 — a THIRD live edit to MuseScore's own code, verified at the objects, absent from `CLAUDE.md`'s local-patches section and carrying no distribution disposition ([[OI-273]]; [[OI-241]]'s count was one short)**. **★ ONE new bulk rule, BR-18**, over `STATUS_ARCHIVE.md`'s non-ruling prose (69 clusters) — licensed by the full read and by nothing else, mirroring BR-17 one file over. All guards PASS (`--verify` 315/315 quotes, 310/310 anchors, references ALL; `--check` 14,460/14,460 clusters, 15,224/15,224 occurrences; register `--check` across 21 files; open-items living check 273/273). **NEXT: the user ratifies (or corrects) D-300…D-315 and rules on [[OI-273]], and on
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-302** — No further local scoring fix for inversions may be attempted — the remaining divergence is not an analyzer defect
  - *What the entry says the decision is:* Trying to correct which note of a chord is treated as its root by adjusting the numbers a single sonority earns in isolation is closed as a line of work. What remains is a genuine difference between reading a sonority on its own and reading it by its role in the music.
  - *Recorded at* `docs/scoring_model.md:1431-1439`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1948 — matched by the entry's own identity (`D-302`)
    > **Tried and closed on the chord layer — do not retry; the register carries each with its measurement: D-215, D-299, D-300, D-301, D-302, D-317, D-318, D-319, D-320, D-328.** **★ A REBUILT OR RE-TUNED CHORD SCORING MUST NOT RELY ON THE HELD-NOTE REPETITION BONUS THE FAITHFUL NOTE MODEL REMOVED (re-homed into this specification 2026-08-08 on the user's ruling).** Before the note reader was rebuilt, a note held across a tie was counted more than once, and that spurious extra
  - *Subject found in* `STATUS.md` line 395 — matched by the entry's own identity (`D-302`)
    > prohibitions absent from `docs/scoring_model.md` §8 (D-300/D-301/D-302) and **D-315 — a THIRD live edit to MuseScore's own code, verified at the objects, absent from `CLAUDE.md`'s local-patches section and carrying no distribution disposition ([[OI-273]]; [[OI-241]]'s count was one short)**. **★ ONE new bulk rule, BR-18**, over `STATUS_ARCHIVE.md`'s non-ruling prose (69 clusters) — licensed by the full read and by nothing else, mirroring BR-17 one file over. All guards PASS (`--verify` 315/315 quotes, 310/310 anchors, references ALL; `--check` 14,460/14,460 clusters, 15,224/15,224 occurrences; register `--check` across 21 files; open-items living check 273/273). **NEXT: the user ratifies (or corrects) D-300…D-315 and rules on [[OI-273]], and on
  - *Subject found in* `cowork_away_returns.md` line 1231 — matched by the entry's own identity (`D-302`)
    > is named at the entry** — D-302 for a procedure with no code, D-215 for an obligation never executed, D-284 and D-243 for a finding about a legacy surface, D-325 for a principle adjacent to one already transferred. **A2 HOLDS.** The population is eleven, derived at task start from the tool's own output and not
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-328** — A wider search cannot fix the arpeggio root failure — the wrong reading IS the global optimum, so only re-weighting or joint segmentation can
  - *What the entry says the decision is:* On the arpeggio failures the locally wrong reading is not a weak transient that a broader search would discard: it is the best-scoring reading, so a broader search finds exactly what the narrow one found. Fixing it needs different weights or a different segmentation, not a wider search.
  - *Recorded at* `docs/scoring_model.md:1207-1216`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 2, of which 2 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 312 — matched by the entry's own identity (`D-328`)
    > | OI-272 | Fourteen standing decisions are recorded ONLY in the session-handoff ARCHIVE — a surface two governing documents declare outside the session-start read | [[OI-266]]'s class one level out (and [[OI-268]]'s sibling): reading `cowork_handoff_archive.md` IN FULL entered **D-286…D-299**, every one `nonspec_kind: "unhomed"`, raising the register's tracking-surface-only count 0 → 14. Three are SHELVINGS WITH EVIDENCE (D-286 whole-score interactive analysis — **the founding case of [[OI-207]] itself**; D-287 key-as-distribution; D-288 beam widening), one a FALSIFICATION (D-290), two standing PROHIBITIONS (D-291, D-299), four binding CONSTRAINTS (D-292 → [[OI-271]]; D-293; D-294; D-295), two scoping CORRECTIONS of other decisions (D-296 bears on the ruled D-229; D-297 scopes D-099 / principle #12), one meta-principle (D-289) and one standing method (D-298). The archive is worse than th
  - *Subject found in* `ARCHITECTURE.md` line 315 — matched by the entry's own identity (`D-328`)
    > *Tried and closed on the search — do not retry; the register carries each with its measurement: D-288 (beam widening, shelved), D-328 (a wider search over the same scoring, refuted), D-278 (the joint key-and-chord step over the two legacy decoders, shelved).* **★ WHAT D-288 IS, re-homed into this specification 2026-08-08; ⚠ LEGACY-SCOPED (its subject is the segmenter and scorer awaiting deletion).** Until now it lived ONLY in a session-handoff archive, a surface that is not among the session-start reads, so the naming above could not be resolved by a
  - *Subject found in* `open_items/OI-281.md` line 141 — matched by the entry's own identity (`D-328`)
    > | `docs/decoder_design.md` | 4 · D-325…D-328 | **not named at all** | | `docs/implementation_roadmap.md` | 4 · D-419, D-421, D-422, D-423 | **not named at all** — though it calls itself "the single tracker" | | `docs/redesign_plan.md` | **4 · D-317…D-320 — MOVES OUT** | named three times, but each is a citation ("Design reference: …", "adopted Phase E direction: …"), and **two of the three sit inside blocks `ARCHITECTURE.md` itself banners SUPERSEDED** (`:783-786`, "retained only as history — do not build to it"). A citation from superseded text is not a live delegation | | `docs/iteration_path1_summary.md` | **2 · D-280/D-281 — MOVES OUT** | **not named at all** | | `cowork_structural_integrity_audit.md` | 5 · D-401…D-405 | excluded — but see §2, it fails on clause (c) ALONE |
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-404** — Relocating the neighbour-chord temporal-context computation out of the derived-view layer is DEFERRED to the decoder engagement, which owns regional temporal context
  - *What the entry says the decision is:* A view-only helper in the derived-view layer runs the whole chord-decision pipeline twice to work out the identity of the neighbouring chords — a decision-layer computation living in a layer that is supposed only to expose views of the notes. Moving it was verified to be possible without changing any output, but it was deferred: the replacement decoder is the decided owner of regional temporal context, so an interim home would be built and then undone.
  - *Recorded at* `cowork_structural_integrity_audit.md:270-275`; *status* `deferred`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `cowork_structural_integrity_audit.md` line 270 — matched by the decisions register's own recogniser `STOP-and-deferred to E4` (`STOP-and-deferred to E4`)
    > - **FQ-3 — ⛔ STOP-and-deferred to E4 (UNCLEAR-7 resolved → fold into E4).** Byte-identically relocatable and decoder-independent, BUT E4-entangled: the decoder (already seeded by `findTemporalContext` at `regionanalyzer.cpp:899-902`, `decoder.commit()≡advanceTemporalContext`) is the E4-decided owner of regional temporal context (ARCHITECTURE.md D-P4/D-BRIDGE/1068: the cold walk is superseded). Relocating to an interim L4 home now is the "redone at E4" case; most-invasive item (new region unit + notation
  - *Subject found in* `open_items/OI-293.md` line 70 — matched by the entry's own identity (`D-404`)
    > and D-403/D-404/D-405 in a BUILD-STATUS RECORD (`cowork_structural_integrity_audit.md` §3.1). Both fail the section unit's KIND half, and no delegation can repair that: the remedy is at the document, in the [[OI-290]] shape. ## What this row is not
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-408** — Voice-leading is a DIFFERENT DIMENSION and is not held in the harmonic vocabulary — a schema defined by its voice-leading is present only by its harmonic pattern
  - *What the entry says the decision is:* This catalog holds chords and functions. How individual voices move is a separate dimension kept elsewhere. Patterns that musicians define partly by their melodic lines — the galant schemata among them — appear here by their chord pattern alone, and are only fully recognised when the voice-leading side is brought in from where it lives.
  - *Recorded at* `cowork_progression_schema_dictionary.md:55-59`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `cowork_progression_schema_dictionary.md` line 55 — matched by the decisions register's own recogniser `Hold voice-leading` (`Hold voice-leading`)
    > - **Hold voice-leading.** It is the **harmonic** vocabulary — chords and functions. Voice-leading (the motion of individual voices — for example the melodic lines that, together with the harmony, complete a galant schema) is a **different dimension**, held elsewhere (a future voice-leading layer, or a separate voice-leading vocabulary). A schema conventionally defined by voice-leading is present here **only by its harmonic pattern**; its voice-leading is held elsewhere and combined when the complete schema is recognised.
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-414** — The catalog is GENERATIVE where it can be and enumerated only where it must be
  - *What the entry says the decision is:* The systematic part of harmony — the applied and substitute chords that exist for every degree of the scale — is stored once as a pattern with the degree left open, and produced on demand. Only the patterns that have individual names and cannot be generated are written out one by one.
  - *Recorded at* `cowork_progression_schema_dictionary.md:255-257`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 3, of which 3 ratify a document other than their own
  - *Subject found in* `STATUS.md` line 257 — matched by the entry's own identity (`D-414`)
    > self-declares "v1 draft"? Nine entries (D-406…D-414) carry a gap flag for exactly this, and their ratification as RULES does not decide it. **Both readings are stated and neither is chosen**, with phase 1i's own precedent recorded cutting both ways; the scope is stated honestly — the answer applies to every draft the canonical document points at — and it is the sibling of [[OI-268]]'s still-open Cowork-ratification question over 21 entries, which must answer consistently. It was deliberately NOT resolved by editing the dictionary's banner, which would have decided it by fiat and for one document only. (2) **[[OI-284]]: the SINGLE TRACKER is stale in three places** — `docs/implementation_roadmap.md` records L4/L5 as dormant and the production switch as "deferred indefinitely" (false at HEAD, D-005/D-010); its
  - *Subject found in* `cowork_progression_schema_dictionary.md` line 255 — matched by the decisions register's own recogniser `Generative where possible` (`Generative where possible`)
    > - **D3 — Generative where possible, enumerated where necessary.** The secondary/substitute apparatus is parameterised by target degree; only the named recurring patterns are enumerated. *Rejected:* a fully enumerated flat list (combinatorial, and it hides the systematic structure). - **D4 — One style taxonomy, shared with the presets; style behavior at the consumer.** *Rejected:* a tag set private to this component (it would drift from the presets); per-style silos (duplicate shared entries); a hard style filter
  - *Subject found in* `open_items/OI-281.md` line 31 — matched by the entry's own identity (`D-414`)
    > Nine register entries (**D-406…D-414**) are homed there and carry the `⚠gap` flag for exactly this reason. They were ratified as *decisions* by the user on 2026-08-03; that ratification is of each rule, and it does not decide the document's home class, which is what this row asks. ## The two readings, stated without choosing
  - *Subject found in* `cowork_progression_schema_dictionary.md` line 255 — matched by the decisions register's own recogniser `Generative where possible` (`Generative where possible`)
    > - **D3 — Generative where possible, enumerated where necessary.** The secondary/substitute apparatus is parameterised by target degree; only the named recurring patterns are enumerated. *Rejected:* a fully enumerated flat list (combinatorial, and it hides the systematic structure). - **D4 — One style taxonomy, shared with the presets; style behavior at the consumer.** *Rejected:* a tag set private to this component (it would drift from the presets); per-style silos (duplicate shared entries); a hard style filter
  - *A ratified document that could not be located:* `cowork_pending_ratifications_next_session.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_2.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `cowork_decisions_pending_ratification_8.md` (named at OPEN_ITEMS.md line 296)
  - *A ratified document that could not be located:* `_design.md` (named at OPEN_ITEMS.md line 296)
- **D-570** — An upstream layer's correctness oracle is the SCORE, not the Roman-numeral ground truth
  - *What the entry says the decision is:* Because this layer sits before any decision about tonality or chords, whether it is right is judged against the notation itself: does it collect exactly the notes a musician reading the score would say are sounding in that stretch, from the right staves, with the right sustains. The published harmonic analyses cannot judge it, and the per-event agreement measurement does not reach it.
  - *Recorded at* `cowork_layer1_tone_collection_design.md:198-204`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `OPEN_ITEMS.md` line 371 — matched by the entry's own identity (`D-570`)
    > | OI-333 | **The cluster-disposition layer CANNOT BE REGENERATED at HEAD** — six register patterns are invalid regular expressions, and the guard that watches that tool is structurally incapable of seeing it | Found by ATTEMPTING the regeneration (CC, READ WAVE 6, 2026-08-04), not by reading. The write mode of `tools/audit/decisions/gen_cluster_dispositions.py` terminates with an uncaught `re.PatternError: multiple repeat at position 3`, raised at `:407` where each entry's `patterns` are compiled. **The cause, enumerated at the data:** six entries carry a pattern containing UNESCAPED markdown emphasis — a literal `**`, which the compiler reads as a repeat applied to a repeat — and all six were authored by READ WAVE 5 on 2026-08-04: **D-555**, **D-570**, **D-576**, **D-577**, **D-578**, **D-581**. Every other pattern in the register escapes its emphasis or avoids it, which is what makes t
- **D-585** — The bass-scale-degree / Rule-of-the-Octave prior is admitted as a SOFT prior and tie-breaker only, never a gate
  - *What the entry says the decision is:* Which harmony a bass note usually carries — the partimento Rule of the Octave — is allowed to break a tie between readings that are otherwise equally good, and nothing more. It may never rule a reading out, because one bass note maps to several harmonies, the mapping depends on which way the bass is moving, and the surrounding progression overrides it.
  - *Recorded at* `ARCHITECTURE.md:2120-2125`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 2121 — matched by the entry's own identity (`D-585`)
    > only, never a gate (D-585).** Which harmony a bass degree usually carries may break a tie between otherwise equally good readings and may never rule a reading out. *Why:* two reasons, both load-bearing — the mapping is theoretically authoritative (the partimento tradition and functional-bass theory) but **largely unexplored as an explicit computational prior**, so it is not established (#19); and it is structurally many-to-one and direction-dependent, with the surrounding
- **D-586** — "Function" in the machine-learning literature names the Roman-numeral components, not Riemann's function theory — and this project's own function layer is misnamed for the same confusion
  - *What the entry says the decision is:* When published research says a system predicts "function", it means the Roman numeral's parts — the degree, the quality, the applied relation — and not the three-role tonic/subdominant/dominant scheme of German function theory. The two senses are routinely confused. This project's own component named after the function layer does neither: it compares candidate chords.
  - *Recorded at* `ARCHITECTURE.md:2127-2137`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 2128 — matched by the entry's own identity (`D-586`)
    > function theory — and this project's own component named after this layer does neither (D-586).** When published research says a system predicts "function" it means the Roman numeral's parts (the degree, the quality, the applied relation), not the three-role tonic/subdominant/dominant scheme; the two senses are routinely confused, and the legacy component carrying the name compares candidate chords instead. *Why:* established by survey — every published autonomous Roman-numeral
- **D-607** — Polyphonic phrase-boundary detection has NO validated deterministic rule set in the literature — the extension is engineering on monophonic cues, validated against our own corpus, and is not settled science
  - *What the entry says the decision is:* Almost all published work on finding phrase endings is about single melodic lines. Our music has several lines at once, and nothing comparable has been established for that case. So carrying the single-line cues over to several voices is our own engineering and has to be checked against our own annotated music; it may not be presented as established method.
  - *Recorded at* `ARCHITECTURE.md:1612-1624`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1613 — matched by the entry's own identity (`D-607`)
    > the L1.5 primitive is our own engineering and may not be presented as established method (D-607; re-homed into this specification 2026-08-04 from the methods document that formerly carried it).** Almost all published work on locating phrase endings addresses a single melodic line; for several simultaneous voices nothing comparable has been established. Carrying the monophonic cues over to polyphony is therefore engineering of ours, and it is validated against our own annotated corpus
- **D-608** — The symmetric-root spelling pin's entry premise was measured FALSE — it is unreachable because the scorer rarely chooses the diminished quality; the remedy is enumerated and NOT decided
  - *What the entry says the decision is:* The mechanism that picks the correctly-spelled rotation of a symmetric diminished-seventh chord only runs if the scorer has already called the chord diminished. It usually has not — on the great majority of such sonorities the scorer either declines to commit or calls the chord something else — so the mechanism almost never runs. The assumption that it would was never written down as one.
  - *Recorded at* `ARCHITECTURE.md:2030-2038`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 2031 — matched by the entry's own identity (`D-608`)
    > and the remedy is enumerated and NOT decided (D-608).** The pin only runs once the scorer has already called the sonority diminished. On the great majority of diminished-seventh sonorities it has not: the scorer either declines to commit or names the chord something else, so the mechanism almost never fires. That it would fire was an assumption and was never written down as one. *Why:* measured at the probe and traced at the code — of the diminished-seventh sonorities in the
- **D-609** — The abstention rate rides on an arbitrary, never-fitted seed constant — a whole probe's metric-moving behaviour sat downstream of an unestablished value
  - *What the entry says the decision is:* How often the chord stage declines to commit is governed by one number that was set by hand as a starting value and never fitted to anything. Everything the probe measured therefore depends on a value nobody has established.
  - *Recorded at* `ARCHITECTURE.md:2040-2046`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 2040 — matched by the entry's own identity (`D-609`)
    > - **The abstention rate rides on an arbitrary, never-fitted SEED CONSTANT (D-609).** How often G1 declines to commit is governed by one number set by hand as a starting value and never fitted, so every quantity measured downstream of the ladder depends on an unestablished value (#19). *Why:* established at the code — the constant is a seed in the decoder's own header, and the control flow was traced to show that everything not committed and not inherited abstains, including a case that
- **D-624** — The hard bound and the score start are SAFETY CAPS for a loop that never settles — never the amount of context a layer needs
  - *What the entry says the decision is:* The loop that reads backwards for context has two stops that are not answers: a maximum distance, and the beginning of the piece. Neither of them says how much context the analysis needed — they only stop a loop that would otherwise not stop.
  - *Recorded at* `ARCHITECTURE.md:1780-1787`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1781 — matched by the entry's own identity (`D-624`)
    > amount of context this layer needed (D-624; re-homed into this specification 2026-08-04 from the design document that formerly carried it — the register records which).** The backward loop has two stops that are not answers: a maximum distance, and the beginning of the piece. Neither reports how much context the analysis required; each only terminates a loop that would otherwise not terminate, and a cap that fired may never be read as the discovered amount. *Why:* the amount of context a layer needs is **discovered
- **D-628** — The finest meaningful extension step is the change-point — within a slice the sounding set is constant, so a finer request loads no note and changes no answer
  - *What the entry says the decision is:* Between two points where the sounding notes change, nothing about the music changes, so asking to load a little more music that stops inside such a stretch brings in no new notes and can move no answer. The smallest request worth making therefore reaches the next point of change.
  - *Recorded at* `ARCHITECTURE.md:1602-1610`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1603 — matched by the entry's own identity (`D-628`)
    > move no answer (D-628; re-homed into this specification 2026-08-04 from the design document that formerly carried it — the register records which).** When a consumer asks this model to reach further into the score, the smallest request worth making is one that reaches the next change-point. *Why:* it follows from what a Layer-2 slice **is** — the stretch over which the eligible sounding-note set is constant — so a request that ends inside a slice is *provably* a no-op rather than merely a small
- **D-635** — Reach-back is a REAL product requirement, currently MASKED by the whole-score load — it must land WITH selection-based loading or selection-edge key inference breaks
  - *What the entry says the decision is:* The shipped program analyses the stretch of music a user has selected, not the whole piece; the whole-piece path exists only for offline measurement. Reading backwards before the selection begins is therefore genuinely needed, and the only reason its absence causes no trouble today is that the note reader still loads everything anyway. Making the note reader load only the selection without also building the backwards read would break key inference at the start of every selection.
  - *Recorded at* `ARCHITECTURE.md:1789-1799`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *Subject found in* `ARCHITECTURE.md` line 1790 — matched by the entry's own identity (`D-635`)
    > WITH selection-based loading (D-635; re-homed into this specification 2026-08-04 from the disposition record that formerly carried it).** The shipped program analyses the stretch a user has selected; the whole-score path exists for offline measurement. Reading backwards before the selection begins is therefore genuinely needed, and the only reason its absence costs nothing today is that the note model still loads the whole score anyway. **Narrowing Layer 1 to load only the

### SUBJECT-NOT-FOUND-THERE — 9 entries

- **D-007** — The published scores are log-scores, not probabilities
  - *What the entry says the decision is:* The numbers beside each alternative are not chances of being right. They are model scores, and the difference between two of them is a score gap, not a percentage.
  - *Recorded at* `ARCHITECTURE.md:65-66`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
- **D-012** — Failure is unambiguous - never a partial record, never a silent fallback
  - *What the entry says the decision is:* If the analysis cannot be produced, the program says so and returns nothing. It never returns half an answer or quietly falls back to the old method.
  - *Recorded at* `ARCHITECTURE.md:129`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
- **D-015** — A boundary tick belongs to the segment it starts
  - *What the entry says the decision is:* When a moment is exactly where one chord ends and the next begins, it counts as belonging to the new chord.
  - *Recorded at* `ARCHITECTURE.md:140-141`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
- **D-039** — Ineligible notes are kept and flagged, never dropped
  - *What the entry says the decision is:* Notes that should not drive the analysis - grace notes, hidden notes, notes on a non-musical staff - are still recorded, marked as such. Nothing is thrown away.
  - *Recorded at* `ARCHITECTURE.md:1575`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 2, of which 2 ratify a document other than their own
- **D-053** — The tick-local path keeps the older resolver (the ratified P4-defer)
  - *What the entry says the decision is:* One narrow fallback - answering about a single moment when no surrounding stretch is available - still uses the older method. That was a deliberate deferral.
  - *Recorded at* `ARCHITECTURE.md:1846-1847`; *status* `superseded-in-fact`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
- **D-069** — Two identity modes for merged stretches - harmonic summary and as-written
  - *What the entry says the decision is:* When neighbouring stretches are merged, they count as the same chord if the root and the major/minor character match. A second mode that would also require the exact voicing to match is designed but not built.
  - *Recorded at* `ARCHITECTURE.md:2829-2830`; *status* `deferred`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
- **D-070** — Style behaviour is fully data-driven - no conditional logic on style identity
  - *What the entry says the decision is:* Nowhere does the code ask 'is this jazz?'. Differences between styles are numbers in a settings file.
  - *Recorded at* `ARCHITECTURE.md:690-692`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
- **D-399** — The texture feature space was decided BY MEASUREMENT among three named candidates — the standardized combination of both views won; the unstandardized combination was rejected before testing for a measured dilution
  - *What the entry says the decision is:* Which numbers the texture decision is made from was not chosen by argument. Three candidates were tested against one criterion: reproduce the classes the earlier study established. Putting both kinds of statistic together after standardizing them reproduced those classes best. Putting them together without standardizing was ruled out in advance, because the sixteen interval numbers would simply outvote the four motion numbers.
  - *Recorded at* `cowork_voiceleading_axis_design.md:394-397`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
- **D-617** — A perfect-detection CEILING is never banked — the realized fraction is measured before the mechanism is wired into production scoring
  - *What the entry says the decision is:* A figure saying how much of a problem a mechanism could address if it worked perfectly is not a figure for how much it will address. The share it actually reaches depends on how reliably it fires, and that is a separate measurement, taken before the mechanism is connected to anything that decides an answer.
  - *Recorded at* `docs/stage4c_cadence_key_design.md:46-51`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 1 ratify a document other than their own
  - *A ratified document that could not be located:* `back_half_design.md` (named at docs/stage4c_cadence_key_design.md line 3)
  - *A ratified document that could not be located:* `cc_stage4b_ii_report.md` (named at docs/stage4c_cadence_key_design.md line 3)

### NO-DOCUMENT-RATIFICATION-ACT — 6 entries

- **D-091** — ARCHITECTURE.md is the canonical architecture document and wins every disagreement
  - *What the entry says the decision is:* Where two documents disagree about the architecture, this one is right, and a new ruling must be written into it before anywhere else.
  - *Recorded at* `ARCHITECTURE.md:565-566`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 0 ratify a document other than their own
- **D-103** — Pedal-point detection is a second pass, accepted only on two conditions
  - *What the entry says the decision is:* When the lowest note does not belong to the chord the upper voices spell, the program re-analyses without it. It accepts that reading only if the upper voices give at least two different pitch names and the answer is clearly better than the next different-rooted one.
  - *Recorded at* `ARCHITECTURE.md:4893-4894`; *status* `superseded-by`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 0 ratify a document other than their own
- **D-250** — Dispatches are written only when they are next; a parked instruction is revalidated first
  - *What the entry says the decision is:* One instruction is dispatched at a time, and the next is written only once its predecessor's report is ratified. Upcoming work lives as plan lines, not as pre-written instruction files. An instruction file that is not the active one carries a parked banner and must be revalidated against the current state before it is dispatched.
  - *Recorded at* `cowork_audit_protocol.md:203-211`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 0 ratify a document other than their own
- **D-252** — One side writes the instruction files and the other executes them, never the reverse
  - *What the entry says the decision is:* The planning side writes instruction files and may read sources and update the summary documents; it does not edit anything under src/, run builds, or spawn agents that do. The executing side runs the instruction.
  - *Recorded at* `cowork_audit_protocol.md:193-199`; *status* `live`; *the filter proposed* NO-DECIDING-ACT-FOUND
  - *Recovered acts:* 1, of which 0 ratify a document other than their own
- **D-333** — The membership tie-break's direction is an idiom-calibrated number, never a branch on style — the three-tier structure is fixed
  - *What the entry says the decision is:* How a note that steps on one side only is judged depends on the style: in chorale writing an accented foreign note is usually a real chord note, in late-romantic writing it usually is not. That difference is carried as a number set per idiom, never as a separate code path per style; the three-tier rule itself does not vary.
  - *Recorded at* `cowork_layer4_chordsymbol_design.md:606-607`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 0 ratify a document other than their own
- **D-334** — The bare-fifth chord type stays in the catalogue structurally; whether it wins is an idiom-calibrated number
  - *What the entry says the decision is:* Whether a root-and-fifth with no third counts as a chord is a question music theory itself answers differently by style. The pattern therefore stays available to every style, and how strongly it competes is set per idiom rather than switched on and off by style.
  - *Recorded at* `cowork_layer4_chordsymbol_design.md:617-620`; *status* `live`; *the filter proposed* EVIDENCE-AMBIGUOUS
  - *Recovered acts:* 1, of which 0 ratify a document other than their own

## 4. What this surface does NOT do, and must not be read as doing

- **It rules nothing and discards nothing.** Both halves are evidence for the user's
  ruling.
- **It re-classifies no entry.** The filter's proposed class and the recovery pass's
  result ride beside every row and are unchanged by anything here.
- **It edits no decisions-register file.** The two derivations it reports performed no
  mutation of the data file, the rendered INDEX or the group files.
- **It grades no decision's content**, its conformance, or whether the code obeys it —
  and nothing here is a judgment on soundness or usefulness.
- **It performs no archiving, no mining and no fact-gate admission.**

*Generated by `tools/audit/gen_ratified_document_check.py` from
`tools/audit/ratified_document_check.json` and `tools/audit/sole_carrier_subclass.json`.
Reproduce: `python tools/audit/gen_ratified_document_check.py --check`.*
