# The rulings sort — design intent, or management of the implementation, RULED for every confirmed decision

> **STATUS: RULED, 2026-08-17. NOTHING IS EXECUTED BY IT.** This surface was a ruling
> surface awaiting the user; the user ruled it at the rulings-sort sitting
> (`cowork_rulings_2026_08_17_rulings_sort_sitting.md`), and this rendering carries the
> ruled state rather than the proposal. **The banner it replaces said the surface was
> awaiting the user and that nothing here was ruled — true when it was written, and made
> untrue by the sitting; the former rendering stands in git at the commits that carried
> it (#12).** What the sitting did NOT do is unchanged: no entry is retired, edited, moved
> or marked; the decisions register's data file and every file rendered from it are
> byte-unchanged by the sort; and a class here grades no decision's worth. Generated by
> `tools/audit/gen_rulings_sort.py` from `tools/audit/rulings_sort_classification.json`,
> which it writes in the same run. Per the standing presentation rule
> (`cowork_rulings_2026_08_15_batch_return.md` §5) every identifier used below is
> re-explained from scratch in §0 before any question rests on it.

> **THE SITTING'S THREE RULINGS.** *(1)* The proposed DESIGN-INTENT entries are the framework phase's seed list as proposed; the proposed IMPLEMENTATION-MANAGEMENT entries are not design input. The ratification moves no verdict — the verdicts it ratifies are the ones this rule already produces — so it is recorded here rather than applied. Its ground, from the sitting: most of the rule-placed side rests on the decisions register's own recorded judgments, the recognizer-placed remainder is correctable at consumption, and the seed list stays amendable when the framework phase consumes it, an amendment there being its own recorded act.
>
> *(2)* The entries the rule RETURNED rather than guessed are placed by the ruled
> criterion, entering as a distinct route — **USER-RULED at the 2026-08-17 sitting** — beside the
> rule's own, so a later reader sees where the judgment came from. Their membership is
> parsed from the sitting record itself and never retyped here; the totals the sitting
> states are reconciled against the population in both directions as a STOP.
>
> *(3)* "No repair is ordered." The mismatch rides to the phase's retrospective as evidence — a disagreement between a record and what it describes — exactly as this pass already treated it: values the header does not define were never mapped by name. It is published unchanged below.
>
> **★ AND A MANAGEMENT PLACEMENT REMOVES NOTHING.** A management placement of a standing principle removes none of its force — the principles govern every session through the standing rules regardless of the sort. The sort classifies; it retires nothing, edits nothing and grades no decision's worth.

## 0. The referents, re-explained from scratch (read this section first)

- **The decisions register** — the project's record of WHAT WAS DECIDED about how this
  system works and whether each decision still stands. Its data file
  (`tools/audit/decisions/backbone_decisions.json`) is the only surface ever edited; the
  INDEX `DECISIONS.md` and the group files are rendered from it.
- **An entry** — one row of that decisions register: a decision's title, the verbatim
  words it was recorded in at its home, a plain restatement, where it is recorded, and
  its status.
- **The filter** — the pass that read each entry's own fields and asked whether a DECIDING
  ACT could be named for it. Its confirmed side — the entries where one could — is what
  the user ratified on 2026-08-16, and is exactly the population sorted here.
- **The framework phase** — the third of the six phases the user ruled on 2026-08-15: the
  architecture decided before the details. Its SEED MATERIAL is the design-intent side of
  this sort.
- **`home_is_layer_spec`** — a field the decisions register carries: the entry is recorded
  in a LAYER'S OWN SPECIFICATION rather than somewhere else.
- **`nonspec_kind`** — a second field the decisions register carries, for an entry that is
  NOT in a layer specification: which kind of home it has instead. The data file's own
  header defines the values, and this surface quotes those definitions rather than
  interpreting them.

## 1. What was asked for, quoted

**The ruling that permits the sort** — `cowork_rulings_2026_08_16_preparation_return.md`:

> The RULINGS SORT may begin over the ratified 411 (design intent versus management of the implementation — the preparation phase's output (b)), as a PROPOSED classification onto a ruling surface, nothing executed by it.

**The ruled definition of the two classes** — `cowork_phase_definition_surface_2026_08_15.md`:

> the RULINGS SORT — surviving rulings sorted into design intent (seed material for the framework) versus management of the implementation (not design input)

**What the confirmed side is** — `cowork_rulings_2026_08_16_preparation_return.md`:

> The 411 DECIDING-ACT-NAMED entries are RATIFIED as the live record's confirmed side.

**The ratification of the rule-placed entries** — `cowork_rulings_2026_08_17_rulings_sort_sitting.md`:

> The 220 proposed DESIGN-INTENT entries are the framework phase's seed list as proposed; the 131 proposed IMPLEMENTATION-MANAGEMENT entries are not design input.

**The criterion the 60 returned entries were placed by** — `cowork_rulings_2026_08_17_rulings_sort_sitting.md`:

> an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT.

**How the ruled placements are to be recorded** — `cowork_rulings_2026_08_17_rulings_sort_sitting.md`:

> The executing act records these placements into the sort's classification artifact as USER-RULED placements, distinguished from the rule's own routes, so a later reader sees where the judgment came from.

**What a management placement does not do** — `cowork_rulings_2026_08_17_rulings_sort_sitting.md`:

> A management placement of a standing principle removes none of its force — the principles govern every session through the standing rules regardless of the sort.

**The totals the sitting states** — `cowork_rulings_2026_08_17_rulings_sort_sitting.md`:

> the sort's totals become 244 DESIGN-INTENT / 167 IMPLEMENTATION-MANAGEMENT / 0 unplaced, 244 + 167 = 411.

**★ WHY THE DESIGN-INTENT SIDE IS THE PAYLOAD.** The framework phase's SEED LIST — the decisions a derivation may start from. A decision wrongly placed there seeds the architecture with something nobody meant to seed it with; one wrongly placed on the other side is simply not consulted. That asymmetry is why every design-intent member is listed on the surface with its plain restatement.

## 2. The rule, published beside the verdicts it produced

**The two ruled classes, and the third outcome the phase's stop rule requires:**

- **DESIGN-INTENT** — a decision about what the analysis is or does — seed material for the framework phase
- **IMPLEMENTATION-MANAGEMENT** — a decision about process, apparatus, workflow, record-keeping or sequencing — not design input
- **NEEDS-THE-USER** — the rule cannot place it, and it is returned rather than guessed

**DERIVED** — nothing below was hand-listed:
- the population, imported from the committed filter artifact and never hand-listed
- every entry's fields, read from the decisions register's own data file
- the two home-classification facts the rule leans on — `home_is_layer_spec` and `nonspec_kind` — which are the REGISTER'S OWN recorded judgments
- every count and every distribution

**AUTHORED** — and marked as such, so a reader sees where the judgment starts:
- the mapping from a `nonspec_kind` value to a proposed class, each carrying the definition the data file's own header gives that value, verbatim
- the word recognizers, reached only where the decisions register's own home classification leaves an entry undecided
- the order the rule applies its steps in

**The rule, applied in order:**

1. `home_is_layer_spec` is true -> DESIGN-INTENT. A decision about what the analysis is or does is what a layer specification holds.
2. otherwise the decisions register's own `nonspec_kind`, where this pass maps it: gap -> DESIGN-INTENT; process -> IMPLEMENTATION-MANAGEMENT
3. otherwise the authored word recognizers over the entry's own title and plain restatement: exactly one side firing decides; both or neither does not.
4. otherwise NEEDS-THE-USER — returned, never guessed.

**The definitions the mapping rests on, quoted from the data file's own header:**

- `process` — *'process' (a decision about how the work is done, not about the system)*
- `gap` — *'gap' (a decision that governs a layer and is not findable from that layer's section*

**★ A DISAGREEMENT FOUND AND NOT REPAIRED.** The data file's header says `nonspec_kind` says which of THREE cases an entry is, and names three. The entries use more than three.

- *The values the entries actually use:* `contract-home`, `gap`, `process`, `project-convention`, `unhomed`
- *The values the header defines and this pass maps by name:* `gap`, `process`
- *What was done:* NOTHING was repaired. The values the header does not define are not mapped by their name; they fall through to the recognizers, which is the only honest treatment of a value the record does not define.
- *Why it was not repaired:* This batch edits no decisions-register file, and a disagreement between a record and what it describes is evidence rather than a defect to smooth away.

## 3. What this sort is NOT — read before the proposals

- **not executed.** Nothing moves, nothing is retired, nothing is written into the decisions register. The sort is a proposal awaiting the user.
- **not a judgment on a decision's worth.** A decision placed in IMPLEMENTATION-MANAGEMENT is not lesser, not wrong and not discarded — it is simply not design input to the framework phase.
- **not a re reading of the filter.** The confirmed side is taken as the filter left it and the user ratified it. Nothing here re-opens whether a deciding act can be named.
- **the recognizers reach is UNMEASURED.** The word recognizers decide only the entries the decisions register's own home classification leaves undecided, and neither their reach nor their error rate is measured (#19). Where they leave an entry, it returns to the user rather than being placed.

## 4. The population and the proposed distribution

- Entries sorted (the whole confirmed side, imported from the filter's artifact): **411**
- The membership is the committed filter artifact's own, read on every run (#6), and reconciled against the decisions register's data file in BOTH directions as a STOP (D-671).

| proposed class | entries |
|---|---|
| DESIGN-INTENT | 244 |
| IMPLEMENTATION-MANAGEMENT | 167 |
| NEEDS-THE-USER | 0 |

**By the route that decided it**, so a reader can see how much of the sort is the
decisions register's own recorded judgment and how much is this pass's words:

| decided by | DESIGN-INTENT | IMPLEMENTATION-MANAGEMENT | NEEDS-THE-USER |
|---|---|---|---|
| the decisions register's own `home_is_layer_spec` | 112 | 0 | 0 |
| the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value | 16 | 120 | 0 |
| USER-RULED at the 2026-08-17 sitting | 24 | 36 | 0 |
| the authored word recognizers, over the decisions register's own restatement | 92 | 11 | 0 |

## 5. Every DESIGN-INTENT entry — the seed list, as ruled

These are the decisions the sitting ruled to be seed material for the framework phase.
**This is the payload of the sort**: a decision listed here is one a derivation is allowed
to start from, so it is listed with what the decisions register says the decision is. The
list remains amendable when the framework phase consumes it; an amendment there is its own
recorded act.

### D-001 — Key, mode and chord are inferred by ONE joint decode

- **Recorded at:** `ARCHITECTURE.md:4-6` · **status:** `live` · **group:** A
- **What the entry says the decision is:** The tonality, the major/minor character and the chord are not worked out one after another. They are worked out together, in a single pass that also decides where one chord ends and the next begins.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-002 — The fitted tables and weights are compiled into the binary verbatim

- **Recorded at:** `ARCHITECTURE.md:21-22` · **status:** `live` · **group:** A
- **What the entry says the decision is:** The numbers the estimator was trained on are built into the program at compile time rather than read from disk at run time, so a running copy cannot quietly disagree with the numbers we published.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-003 — Inference is preset-independent; presets are presentation concerns

- **Recorded at:** `ARCHITECTURE.md:33-34` · **status:** `live` · **group:** A
- **What the entry says the decision is:** Choosing the Baroque, Jazz or Default preset changes nothing about what the estimator concludes; it changes only how the result is shown.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-005 — The joint estimator is the production inference layer on the batch and corpus surface

- **Recorded at:** `ARCHITECTURE.md:11-12` · **status:** `live` · **group:** A
- **What the entry says the decision is:** Everything the measurement corpus is graded on now comes from the joint estimator, not from the older chord-by-chord pipeline.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-010 — The switch - the record path is the production in-app notation analysis

- **Recorded at:** `ARCHITECTURE.md:250` · **status:** `live` · **group:** B
- **What the entry says the decision is:** Since 27 July 2026 the harmony you see inside the program is produced by the joint estimator. The old path is still compiled in but is only reachable by explicitly turning the new one off.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-022 — The founding principle - analyse at the finest grain, coarser views are derived

- **Recorded at:** `ARCHITECTURE.md:1077-1078` · **status:** `live` · **group:** C
- **What the entry says the decision is:** The analysis works on the smallest stretch over which the sounding harmony does not change. Phrases, key areas and sections are then read off that, never analysed directly.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-023 — The atomic analysis unit is the constant-sonority slice, never the metric beat

- **Recorded at:** `ARCHITECTURE.md:1078` · **status:** `live` · **group:** C
- **What the entry says the decision is:** The smallest thing analysed is a stretch during which exactly the same notes are sounding - not a beat of the bar.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-024 — The fact layers are style-agnostic; style lives only in calibration

- **Recorded at:** `ARCHITECTURE.md:1084-1087` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Reading the notes and cutting the music into constant-sound stretches works the same for every kind of music. Whether a piece is Baroque or jazz can change only the numbers the judging layers use, never the shape of the code.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-025 — Forward-only, with two scoped escapes

- **Recorded at:** `ARCHITECTURE.md:1057-1058` · **status:** `superseded-by` · **group:** C
- **What the entry says the decision is:** Each stage was to pass its answer forward and never reach back. A confident earlier answer could be overturned only by re-running that one stretch forwards, and the one genuinely tangled key-versus-chord case got a narrow, gated exception.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-026 — The global joint-lattice decode was measured inert (2026-06-29)

- **Recorded at:** `ARCHITECTURE.md:1054-1055` · **status:** `live` · **group:** C
- **What the entry says the decision is:** An earlier plan to search all the possibilities at once was tested and found to add nothing, so the effort was redirected into better evidence flowing forwards.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-027 — Every layer emits ranked candidates plus a confidence, never a forced point estimate

- **Recorded at:** `ARCHITECTURE.md:1059` · **status:** `live` · **group:** C
- **What the entry says the decision is:** No stage is allowed to report only its single best answer. It reports the runners-up too, with a measure of how clear-cut the choice was.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-028 — The span typology - every layer names the span it operates on; bare 'region' is banned

- **Recorded at:** `ARCHITECTURE.md:1141-1142` · **status:** `live` · **group:** C
- **What the entry says the decision is:** The word 'region' on its own is forbidden, because it hides which kind of stretch is meant. Each stretch has its own name: the chord-span, the key-span, the punctuation-span and so on.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-029 — The verifiability contract

- **Recorded at:** `ARCHITECTURE.md:1167-1169` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Prefer what we can check against annotated music. Where the theory is sound but we have nothing to check it against, build it anyway - but mark it as unchecked and give it its own confidence path.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-030 — Bounded context - cost scales with the working span, not the whole score

- **Recorded at:** `ARCHITECTURE.md:1173-1175` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Analysis runs on what the user has selected. The work must grow with the size of that selection, not with the size of the piece; re-analysis after an edit must only redo the changed part; and a layer that needs more music asks for it rather than reading everything.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-031 — Whole-score analysis is the degenerate case, not the design

- **Recorded at:** `ARCHITECTURE.md:1175` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Analysing the whole piece is what happens when the user has selected the whole piece. It is not the normal mode of operation.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-032 — Every confidence crossing a layer boundary is in 0..1, class-declared, with its decision named

- **Recorded at:** `ARCHITECTURE.md:1221-1223` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Inside a stage, a confidence can be on any scale. The moment another stage can read it, it must be a 0-to-1 number, labelled with what kind of confidence it is and what decision it belongs to.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-033 — Each layer owns one evidence-source-times-question contribution and uses all of L1's information

- **Recorded at:** `ARCHITECTURE.md:1180-1183` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Each stage owns one contribution and says plainly what it does not own, handing unresolved cases forward as ranked options. Owning one contribution does not narrow what it may look at: within its scope it uses all the information the note reader carries - how the note is spelt, where it falls in the bar, and which voice it is in.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-034 — A new layer or axis is admitted only through three co-equal gates

- **Recorded at:** `ARCHITECTURE.md:1213-1214` · **status:** `live` · **group:** C
- **What the entry says the decision is:** A new stage is added only if it carries one distinct responsibility, can be validated somehow, and buys something we can actually check. Carrying a distinct responsibility is enough on its own, even with no immediate accuracy gain.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-035 — The effort setting - every cost-driving choice is a setting, never a hardcoded constant

- **Recorded at:** `ARCHITECTURE.md:1070-1072` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Anything that makes the analysis slower must be something the user or the caller can turn down, not a number baked into the code; and any expensive extra step must be separable so it can be switched off.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-057 — The priority of evidence - actual sounding notes are the strongest evidence

- **Recorded at:** `ARCHITECTURE.md:4361` · **status:** `live` · **group:** F
- **What the entry says the decision is:** In deciding the key, what is actually sounding right now outranks the surrounding bars, which outrank the printed key signature, which outranks the major/minor tag on it.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-072 — The dependency rule - the analysis library knows nothing about the score format

- **Recorded at:** `ARCHITECTURE.md:1453` · **status:** `live` · **group:** I
- **What the entry says the decision is:** The music-theory library must not know how MuseScore stores a score. Anything that needs both lives in a thin bridge layer in between.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-095 — The dual path during the joint-estimator build is a declared, bounded, pre-ratified migration state

- **Recorded at:** `ARCHITECTURE.md:43-44` · **status:** `superseded-in-fact` · **group:** A
- **What the entry says the decision is:** Building the new estimator beside the old one temporarily breaks the rule that there is one way to do each thing. That was declared in advance, bounded, and given a retirement plan.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-096 — Fitted values are fit once against ground truth, never per-case tuned

- **Recorded at:** `ARCHITECTURE.md:280-281` · **status:** `live` · **group:** A
- **What the entry says the decision is:** The shape of each piece of evidence comes from music theory. Its numerical strength is learned once from annotated music, and never adjusted to make a particular passage come out right.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-099 — Negative evidence is information - a ruled-out possibility is carried, not dropped

- **Recorded at:** `ARCHITECTURE.md:1237-1239` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Knowing that something is not the case is itself useful. A reading that has been ruled out is kept at low confidence rather than thrown away, unless we could work out the exclusion again from what we did keep.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-100 — Every derived fact is published exactly once, on the producing layer's output surface

- **Recorded at:** `ARCHITECTURE.md:1254-1255` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Whatever a stage works out, it publishes on its own output surface; every later stage reads that instead of working it out again. Facts that are hints a later stage might one day use are published broadly even when nothing reads them yet, each carrying whether it has been established, because a consumer may not rely on an unestablished fact. What to do with a fact nobody reads is decided case by case: keep it with a named future reader stated, or remove it - and a reader outside the analysis counts.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-113 — Music-theory words are reserved for their music-theory meaning

- **Recorded at:** `CLAUDE.md` · **status:** `live` · **group:** K
- **What the entry says the decision is:** In this project a score is a piece of music, a key is a tonality, and a measure is a bar. Where a word is needed in its everyday computing sense, it must be qualified - candidate score, map key, measurement.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-114 — The decoder commits its best path; there is no abstention on the key axis

- **Recorded at:** `ARCHITECTURE.md:331-332` · **status:** `live` · **group:** A
- **What the entry says the decision is:** The joint estimator always names a key. It never declines to answer on the key axis, so the abstention counter is always zero.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-131 — One shared style taxonomy, not two parallel vocabularies

- **Recorded at:** `ARCHITECTURE.md:5459` · **status:** `live` · **group:** M
- **What the entry says the decision is:** The list of style categories the presets choose from is the SAME list the harmonic vocabulary tags its entries with — one shared set, not two that can drift apart. That set is the five idioms (Diatonic-functional, Chromatic-functional, Seventh-functional, Triadic-modal, Chromatic-coloristic), with major/minor and diatonic/chromatic carried separately beside them; an entry may carry more than one idiom.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-132 — The remaining empirical grounding is the per-preset WEIGHTS alone; the clusters half is delivered by the ratified five-idiom set

- **Recorded at:** `ARCHITECTURE.md:5494` · **status:** `deferred` · **group:** M
- **What the entry says the decision is:** Grounding the style system in data was recorded as two pieces of committed work: discovering the categories, and measuring how strongly each one weighs in each preset. The first is done — the five idioms were discovered from corpora, ratified and encoded. What is still owed is the second: a per-preset weighting over those five, derived by clustering corpora rather than asserted.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-168 — #4 - the long-term goal is maximum-precision inference

- **Recorded at:** `CLAUDE.md:17` · **status:** `live` · **group:** S
- **What the entry says the decision is:** The objective the whole project is measured against is getting the analysis as accurate as it can be made.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-170 — #6 - total unification: one path per concern

- **Recorded at:** `CLAUDE.md:20` · **status:** `live` · **group:** S
- **What the entry says the decision is:** There is exactly one implementation of any given concern. No duplicated code, no second place the same question is answered.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-171 — #7 - a layer is enhanced only with what belongs to it

- **Recorded at:** `CLAUDE.md:21` · **status:** `live` · **group:** S
- **What the entry says the decision is:** A stage of the analysis gets only the methods that are properly its own. If the right method does not belong there, the layers are redesigned rather than the method smuggled across.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-172 — #8 - no inference-problem-driven coding until the refactoring, the architectural design and the algorithmic completion are done

- **Recorded at:** `CLAUDE.md:23-26` · **status:** `live` · **group:** S
- **What the entry says the decision is:** Work is not steered by whichever analysis error is currently visible. Until the system is built right - the refactoring carried out, the architecture designed, and every method and algorithm finished in its correct layer - no fix is made because an analysis result is wrong. All three must be done, not the last alone.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-180 — #17 - the Premise Gate

- **Recorded at:** `CLAUDE.md:86` · **status:** `live` · **group:** S
- **What the entry says the decision is:** Before anything that affects the analysis is built or even probed: every load-bearing causal claim is written down and labelled as an established fact, a published theory, or an assumption; every assumption gets a written numerical prediction BEFORE anything is measured; the mechanism is traced by hand through three to five real failing cases, asking first whether it fires at all and only then what it changes; any stand-in quantity must itself be justified; any claim that one thing cannot affect another must name how it could; and no number enters a document by being typed in by hand.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-182 — #19 - an unestablished measurement tool is forbidden (Class B)

- **Recorded at:** `CLAUDE.md:118` · **status:** `live` · **group:** S
- **What the entry says the decision is:** A measuring script, a corpus, a gate or a recorded figure is trusted only once it has been positively shown to be right - checked against an independent oracle, with a derivation of what its unit actually measures, and a reproduce-check. Never merely because nothing has contradicted it.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-185 — #22 - every hard gate declares in advance how it handles the largest change it will meet

- **Recorded at:** `CLAUDE.md:214` · **status:** `live` · **group:** S
- **What the entry says the decision is:** A rule that decides whether a change may ship must say, before the fact, what it does when the change is far bigger than the incremental ones it was written for. It must never be rewritten while such a change is sitting in front of it.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-190 — The decision-neutrality corollary - what exists carries no weight in choosing a design

- **Recorded at:** `CLAUDE.md:254` · **status:** `live` · **group:** S
- **What the entry says the decision is:** A design is chosen on the principles and the goal of the best possible analysis, and on nothing else. What it would cost to make existing code obsolete is a secondary consideration that can only break a tie between designs already equal; how many places downstream would have to change counts for nothing; and a change in what the user sees counts for nothing either - though every such change still needs ratifying and verifying exactly as before. The best design is chosen first, and what exists then either serves it or is retired.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-201 — Very large scores must be handled, and are expected to be more common than our corpora

- **Recorded at:** `ARCHITECTURE.md:1282-1283` · **status:** `live` · **group:** S
- **What the entry says the decision is:** A Wagner act or a symphony must work. The user expects such scores to be a more common use than the chorales the system was fitted on. This is a standing requirement every later design is judged against, not a defect report.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-202 — The effort control is one setting with several dials, and it must bound the time taken

- **Recorded at:** `ARCHITECTURE.md:1290-1291` · **status:** `deferred` · **group:** S
- **What the entry says the decision is:** How hard the analysis works is a single setting the user turns, not several. Behind it sit several dials, and among the things it must be able to bound is how long the analysis takes. It is too early to build: which pieces of the analysis have to be switchable is not yet known.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-205 — A human acts as ground truth where no formal ground truth exists

- **Recorded at:** `ARCHITECTURE.md:7923-7925` · **status:** `live` · **group:** S
- **What the entry says the decision is:** For music nobody has published an analysis of, the reference answer is a person's judgment. They may reach it however they like, including by letting an automated judge point them at the passages most likely to be wrong. That judge is guidance for the human, never a grader and never a number we report.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-206 — Intonation is held as a future feature, and is a declared future consumer of the analysis

- **Recorded at:** `ARCHITECTURE.md:6220-6222` · **status:** `deferred` · **group:** S
- **What the entry says the decision is:** The six unbuilt pieces of the tuning design stay on the books as a deliberate long-horizon hold, revisited at a natural pause in the analysis work. The reason the hold is strategic rather than neglect: tuning will read the analysis - knowing the mode, the chord, its function and the progression is what lets a just-intonation decision be made, particularly the decision about staying in tune over time versus letting the pitch drift.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-207 — The pedal-point class is defined voice-independently, superseding the bass-only fact

- **Recorded at:** `ARCHITECTURE.md:5774-5777` · **status:** `deferred` · **group:** G
- **What the entry says the decision is:** A pedal point is a note held - or struck again and again - while the harmony changes around it, in ANY voice, not only the bass. It is labelled by where it sits: in the bass, inside the texture, or above it. This replaces the older fact, which could only see a pedal in the lowest voice.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-220 — The augmented-seventh guard requires both the major third and the augmented fifth

- **Recorded at:** `docs/scoring_model.md:1169-1170` · **status:** `live` · **group:** U
- **What the entry says the decision is:** The guard fires only when both intervals are present, not when either one is. Requiring only the third was tried and reverted.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-221 — A sparse upper-register lowest note does not earn inversion bonuses

- **Recorded at:** `docs/scoring_model.md:1177-1178` · **status:** `live` · **group:** U
- **What the entry says the decision is:** A low note that is thin and high in the texture is not treated as a structural bass, so the bonuses that reward a recognisable inversion do not fire for it.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-222 — If the diminished bonus rotates the winner to a non-diminished chord, the result without it is used

- **Recorded at:** `docs/scoring_model.md:1180-1182` · **status:** `live` · **group:** U
- **What the entry says the decision is:** The bonus that favours diminished readings can, in the course of comparing bass notes, end up electing a winner that is not diminished at all. When that happens the analysis falls back to the answer it had before the bonus was applied.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-223 — A gate that judges the pre-correction winner reads a snapshot, not the live result

- **Recorded at:** `docs/scoring_model.md:1184-1186` · **status:** `live` · **group:** U
- **What the entry says the decision is:** Where a gate has to compare against whatever the analysis thought before a correction was applied, it reads a copy taken beforehand rather than the current top result, which the correction may already have changed.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-224 — Joint bass-and-chord scoring requires accumulated regional evidence

- **Recorded at:** `docs/scoring_model.md:1188-1191` · **status:** `live` · **group:** U
- **What the entry says the decision is:** The scoring that considers the bass note and the chord together only switches on when the notes came from accumulating a whole stretch of music. The single-moment paths - the status bar, a unit test - use the simpler single-bass scoring.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-229 — The MuseScore-dependency rule - one general rule for what our code may depend on

- **Recorded at:** `ARCHITECTURE.md:1460-1473` · **status:** `live` · **group:** I
- **What the entry says the decision is:** Three parts. The music-theory library uses no MuseScore code at all. The bridge code that connects analysis to the score reads the score only through the established bridge functions, and never uses layout results (positions, spacing) as analysis input - the note reader is the one sanctioned reading surface. And changing MuseScore's own code is allowed only to fix a defect blocking our feature, each change recorded, with its distribution (upstreamable or fork-only) decided and ratified case by case.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-260 — Analysis output covers exactly the selection; everything loaded beyond it is evidence, never a result

- **Recorded at:** `cowork_bounded_context_design.md:43-44` · **status:** `live` · **group:** C
- **What the entry says the decision is:** The user's selection is the output span: labels are emitted only for it. Music loaded from outside the selection is pulled in as evidence for judging the selection's edges and is never itself labelled.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-261 — A layer never guesses how much context it needs - the amount is discovered by convergence

- **Recorded at:** `cowork_bounded_context_design.md:57-71` · **status:** `live` · **group:** C
- **What the entry says the decision is:** A layer knows what evidence it needs but not how far away it is, so it never picks an amount. It extends the loaded span incrementally and stops on a principled condition: convergence, meaning its in-selection output stops changing as more context arrives. The layer applies that test directly, on the quantity it asked for more context about, and stops when that quantity repeats.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-262 — The extension increment is chosen by the requesting layer, not by the layer that supplies the notes

- **Recorded at:** `cowork_bounded_context_design.md:94-102` · **status:** `live` · **group:** C
- **What the entry says the decision is:** How much music to load per extension step is set by the layer asking for it, in its own natural inference scale, because the note supplier is domain-blind and no single step size fits every layer. The increment is an efficiency knob only - a larger step means fewer round trips, never a different answer, because convergence fixes the result.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-264 — Extension is an optimisation of load-more-then-rerun: any sequence of extensions equals one fresh run

- **Recorded at:** `cowork_bounded_context_design.md:142-147` · **status:** `live` · **group:** C
- **What the entry says the decision is:** The result after any sequence of extensions must equal a single fresh run over the final loaded span. Extension exists to avoid recomputing from scratch; it is never allowed to be a different computation, and the analysis must not depend on how many steps reached a given span.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-265 — Asking a lower layer for more notes is a data-supply call, not a backward inference edge

- **Recorded at:** `cowork_bounded_context_design.md:136-141` · **status:** `live` · **group:** C
- **What the entry says the decision is:** An extension request travels down the stack to the note supplier, and the new notes and every re-inference then flow forward through the layers exactly as on a first run. Inference never flows backward: a later layer re-inferring cannot alter an earlier layer's result. So extension is consistent with the forward-only contract rather than an exception to it.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-267 — There are exactly two admissible confidence classes, and no layer may claim a calibrated probability until one is fitted

- **Recorded at:** `cowork_confidence_contract.md:25-34` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Every published confidence declares one of two classes. A decision margin says how much better the chosen reading is than the best different one under that layer's own scoring - a rank statement, not a probability, published only after being squashed into the zero-to-one range. A calibrated probability says with what measured frequency a decision at this confidence is correct; it is the later target, and until its reliability map is fitted no layer may claim it.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-268 — A confidence attaches to a named decision, is compared only within its class and a declared frame, and keeps its identity downstream

- **Recorded at:** `cowork_confidence_contract.md:36-48` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Five rules of use. A confidence belongs to a named decision, never to a layer in general. At a layer boundary it is zero-to-one, class-declared and decision-named. A consumer may compare two confidences only within one class and one declared comparison frame. A carried-forward confidence keeps its source layer, decision and class, with no silent reinterpretation. An abstention means the decision's confidence is below that layer's declared bar - the same meaning everywhere, not a separate ad-hoc judgment.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-270 — The held-out evaluation protocol - five-fold cross-validation grouped by ground-truth analysis file

- **Recorded at:** `cowork_prefit_gates.md:32-42` · **status:** `live` · **group:** A
- **What the entry says the decision is:** Evaluation splits the 326 ground-truth-covered pieces into five folds, grouped so that pieces sharing one ground-truth analysis file share a fold. Fold assignment is generated once from a fixed committed seed and never changes. Everything fitted - the tables, the weights, and the fitted structure choices such as the vocabulary threshold, the smoothing constants and the penalty - is fitted inside the training folds only; the held-out fold is touched exactly once, by that fold's final model.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-271 — The capacity budget - a cell keeps its own estimate only above a stated count, and free parameters are bounded against the training tokens

- **Recorded at:** `cowork_prefit_gates.md:68-81` · **status:** `live` · **group:** A
- **What the entry says the decision is:** Before any fit, the parameter inventory is published as a generated artifact: every table, its dimensions, its raw cell-count histogram and its resulting free-parameter count. A table cell keeps its own maximum-likelihood estimate only if its training count reaches twenty; below that it is pooled into its declared parent class under smoothing. Total effective free parameters stay at or below one tenth of the training tokens, and the combination-weight vector stays at or below fourteen weights with a penalty.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-275 — Every published record carries its own instrument provenance; a provenance-less analysis cannot exist

- **Recorded at:** `cowork_notation_output_contract.md:54-57` · **status:** `live` · **group:** B
- **What the entry says the decision is:** Each record published for the notation path carries the source-artifact hashes of the fitted table set, the identity of the selected weight vector, and the decoder's version. A consumer, or any later measurement, can always answer which fitted values produced a given analysis from the analysis itself.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-276 — Modal colour is published as un-rounded per-degree counts; no mode label is inferred or published anywhere

- **Recorded at:** `cowork_notation_output_contract.md:139-147` · **status:** `live` · **group:** B
- **What the entry says the decision is:** For each key run and each scale degree, the record publishes the sounding duration and onset count of every chromatic inflection of that degree actually observed. That is the whole publication - counted, un-rounded, nothing hand-set - so minor's variable sixth and seventh, major's lowered seventh or raised fourth, and every borrowing appear as their actual counts. A presentation layer may format a reading from it; no twenty-one-value mode label is inferred or published.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-278 — The joint key-and-chord step is SHELVED - measured not to pay

- **Recorded at:** `cowork_engage_arc_plan.md:108-117` · **status:** `shelved-with-evidence` · **group:** C
- **What the entry says the decision is:** The separate joint key-and-chord decision was measured before being built and does not pay: about a tenth of a percentage point net over roughly 6200 stretches, with harm at three quarters to nine tenths of the correction, an oracle ceiling under a percentage point, and a firing rate of 1.4 per cent. The cause is that the carried alternative keys are siblings within one collection, so the chord is almost always stable across them. It drops off the build inventory. DEPRECATION MADE EXPLICIT (user, 2026-08-02): the shelved step's subject is deprecated legacy-era machinery, to be entirely discarded with the legacy path at the retirement map; the shelving binds that class only and does not bear on the joint estimator (D-001), a different mechanism class.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-279 — The Stage-3 entry gate - seven conditions before any engagement wiring reaches production

- **Recorded at:** `cowork_engage_arc_plan.md:69-72` · **status:** `live` · **group:** T
- **What the entry says the decision is:** Before the rebuilt path's wiring can reach production, seven conditions hold: the two measured-harmful mechanisms are defused or provably bypassed; the go/no-go measurement runs under the full Premise Gate with its measurement tool established first; the pedal reader waits on its underpowered premise being settled; the confidence-commensurability premise owes a ledger and a desk simulation before any threshold is fitted; the fit surface is completed; the Jazz preset's validation status is declared honestly; and no step opens until every layer it depends on has passed its audit.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-280 — Gates read structured fields only - never a chord symbol string and never a Roman numeral

- **Recorded at:** `ARCHITECTURE.md:1500-1505` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Any gate or scoring rule reads structured analysis fields. It never parses a chord-symbol string and never infers from a Roman numeral. Signals derived from symbols or Roman numerals are too lossy and too entangled with the formatter to be reliable inputs to chord classification.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-282 — Meta-finding: the oracle/tier metric, never a bare proxy - superseded by the robust-unit stop and the two-tier policy

- **Recorded at:** `cowork_architecture_reassessment.md:106` · **status:** `superseded-by` · **group:** C
- **What the entry says the decision is:** Never grade the analysis on the bare bass-is-root number, which rewards a wrong chord root that happens to be the bass; use the oracle-checked, tiered measurement. Its content became standing through the robust-unit regression stop and the two-tier class policy.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-283 — Meta-finding: never learn keys, the lever is keychain structure - superseded by the joint estimator and the forms-from-theory rule

- **Recorded at:** `cowork_architecture_reassessment.md:107` · **status:** `superseded-by` · **group:** A
- **What the entry says the decision is:** A rejection of a learned key detector in favour of structural levers, cadence precision above all. Whichever reading was intended, the later explicit ratifications govern: the joint estimator infers the key inside a theory-declared generative form whose factor values are fitted once against ground truth, and its cadence factor carries the structural insight forward. This finding binds nothing the current design does.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-284 — Meta-finding: selection/competition is saturated, stop adding re-ranking gates - superseded by the gates doctrine and the adoption

- **Recorded at:** `cowork_architecture_reassessment.md:108-109` · **status:** `superseded-by` · **group:** G
- **What the entry says the decision is:** Stop investing in the legacy scorer’s gate and re-ranking surface; the remaining error lives in candidate generation and key quality. The doctrine lives on generalized in the ratified accumulating-gates rule, and the mechanism it warned about was retired wholesale when the joint estimator replaced the legacy selection surface.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-285 — Meta-finding: embellishment is chord-first, never a richer vocabulary - absorbed by the emission design and the ornament-label increment

- **Recorded at:** `cowork_architecture_reassessment.md:110` · **status:** `superseded-by` · **group:** A
- **What the entry says the decision is:** Ornamental tones are handled by classifying them against the committed chord - segmentation first, then a non-chord-tone post-process - never by widening the chord vocabulary until every embellishment is a chord. The ratified factorization’s emission carries exactly this shape (chord-member and non-chord-tone categories), and the ornament-label publication is its own ratified increment.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-286 — Whole-score interactive analysis was SHELVED WITH EVIDENCE; the bounded window is the ratified reading

- **Recorded at:** `ARCHITECTURE.md:1301-1308` · **status:** `live` · **group:** C
- **What the entry says the decision is:** At Stage 3.1b a measured A/B put a whole-score interactive analysis against a bounded-window one and the window won against the published annotations; the whole-score variant was withdrawn against that measurement and the bounded window adopted. The question of whether a per-note answer must match the whole-piece answer was parked, not settled.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-288 — Beam widening is SHELVED - a wider search cannot fix the failure class it was proposed for

- **Recorded at:** `ARCHITECTURE.md:320-324` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Searching more candidate readings in parallel was withdrawn. The failure it was meant to fix is not a search failure: the wrong reading is the highest-scoring one, so looking at more readings finds the same wrong answer. Only changing how readings are scored, or cutting the music differently, can fix it.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-289 — Meta-principle: precision lives in the evidence and the functional labelling, not in the search

- **Recorded at:** `cowork_handoff_archive.md:3082` · **status:** `superseded-by` · **group:** C
- **What the entry says the decision is:** Three independent investigations converged on one rule: accuracy is gained by improving what evidence each reading is judged on and by labelling harmonic function better - not by searching harder over the readings already on the table. The rule is SUPERSEDED as an entry: every part of it is stated by a later decision that is written into a specification, so it is recorded here and homed nowhere, which is what keeps one rule in one place.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-291 — The tonicization labeller is NOT wired - wiring it would raise the reported agreement while hiding a real key error

- **Recorded at:** `ARCHITECTURE.md:2164-2168` · **status:** `live` · **group:** H
- **What the entry says the decision is:** A working labeller for applied chords was deliberately left unwired, and the proposal to make the accuracy measurement treat its labels as equivalent to the annotator's was rejected. Both would have raised the reported Roman-numeral agreement while the underlying reading stayed wrong: the annotator has changed key, and labelling the chord relative to the old key hides that.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-292 — The fitting-pool licence constraint - values that ship are fitted only on freely-licensed music

- **Recorded at:** `ARCHITECTURE.md:340-342` · **status:** `live` · **group:** L
- **What the entry says the decision is:** Any number that is fitted and then shipped may be fitted only on public-domain or permissively-licensed music. Music under a non-commercial or unstated licence may be used to check and validate, never to fit a shipped value.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-293 — Fitted values are fitted per IDIOM, never for a user preset; presets are regression surfaces and delivery carriers

- **Recorded at:** `ARCHITECTURE.md:376-378` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Numbers are fitted once per musical idiom - a body of repertoire that shares a practice - and never tuned to match one of the program's named presets. A preset is a way of delivering a set of values and a surface to check for regressions; which presets a user should see is a separate product question, decided later.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-295 — Zero information loss to the end user - every inferred object must be displayable

- **Recorded at:** `ARCHITECTURE.md:7475-7477` · **status:** `live` · **group:** J
- **What the entry says the decision is:** Anything the analysis works out must be capable of being shown to the user. Showing it gradually, so the display is not overwhelming, is fine; leaving something permanently unreachable because the interface has no place for it is not.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-296 — READING MuseScore's engraving code is allowed from anywhere we may edit; only EDITING the notation and engraving code is off limits

- **Recorded at:** `ARCHITECTURE.md:1474-1476` · **status:** `live` · **group:** I
- **What the entry says the decision is:** Our code may call into and read from MuseScore's own score and engraving code wherever we are allowed to write. What is out of bounds is changing MuseScore's notation and engraving source itself.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-306 — The key layer's backward re-reading stays switched off in the shipped configuration

- **Recorded at:** `ARCHITECTURE.md:1771-1773` · **status:** `live` · **group:** F
- **What the entry says the decision is:** The key analysis has a facility for going back and re-reading an earlier stretch once later evidence arrives. It is built but switched off, and turning it on is reopened only when a specific piece of evidence has been gathered.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-313 — A confidence map is monotone or it is not fitted — a non-monotone curve is an upstream finding, not a mapping target

- **Recorded at:** `cowork_stage5_fitter_design.md:695` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Turning a layer's internal confidence number into a statement about how often it is right is only done when a higher number really does mean more often right. Where the measured curve goes the wrong way in places, that is reported as a fault in the layer, not smoothed over by the map.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-317 — The backward-walk boundary change is a dead end — do not retry it

- **Recorded at:** `docs/scoring_model.md:1375-1381` · **status:** `live` · **group:** G
- **What the entry says the decision is:** LEGACY (the analyzer awaiting deletion): a one-tick boundary fix was tried and closed - counting notes that stop exactly where a stretch begins as belonging to that stretch, in the hope of recovering a missing chord root. Measured: the boundary-touching notes are other chord tones (the root attacks later), and five display paths depend on the current convention. A boundary-membership dead end ONLY - it says nothing about extending the temporal context the analysis reads, which is a decided live capability (the extensible working span, D-030).
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-318 — A short-region external merger is a dead end — do not retry it

- **Recorded at:** `docs/scoring_model.md:1382-1385` · **status:** `live` · **group:** G
- **What the entry says the decision is:** LEGACY (the segmenter awaiting deletion): a proposed after-the-fact pass merging very short neighbouring stretches was tried and closed - measured, its trigger never fires, because the earlier inline same-root merge has already combined them. A prohibition on re-adding one redundant merger pass - nothing about collecting notes over time or extending context.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-319 — Re-analysing the merged aggregate is a dead end — no tone-aggregation approach fixes the arpeggio root failure

- **Recorded at:** `docs/scoring_model.md:1386-1392` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Pooling an arpeggio's notes and re-reading the chord from the pool was implemented, measured, and reverted: pooling makes the answer worse, because the wrong note sounds for longer than the right one. The evidence was never the problem.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-320 — The absent-root guard is REVERTED and must not be retried — 'absent root means wrong reading' is false corpus-wide

- **Recorded at:** `docs/scoring_model.md:1393-1399` · **status:** `live` · **group:** G
- **What the entry says the decision is:** A rule that rejected any chord whose own root is not sounding was built, measured, and removed entirely. It fixed two cases and broke four, and the premise behind it is false across the corpus: sometimes the published human analysis names a chord whose root is not sounding.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-321 — Winner selection compares candidate scores exactly, with no epsilon anywhere in the ranking

- **Recorded at:** `docs/scoring_model.md:265-278` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Two candidate readings are ordered by comparing their numbers exactly, with no tolerance band; exact ties are broken by a declared order. This is deliberate.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-322 — Any change to optimization flags or to the order of the scoring arithmetic requires a full corpus A/B on both presets

- **Recorded at:** `docs/scoring_model.md:286-291` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Because candidate scores are compared exactly, re-ordering the arithmetic or changing compiler optimization settings can flip a reading that was decided by a hair. Such a change is not trusted to leave the output unchanged until it has been checked against the whole corpus on both tuning presets.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-323 — Asking whether a pitch belongs to the key is a question about the collection, never about the tonic — the tonic-anchored form must not return

- **Recorded at:** `docs/scoring_model.md:356-360` · **status:** `live` · **group:** F
- **What the entry says the decision is:** A test of the form 'is this note in the key' must read the key signature's own collection of notes, never a scale laid out from a tonic. Asking about a scale degree is a different question and may legitimately use the tonic.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-324 — Retirement of a post-scoring rule is global — a rule still doing work on any one preset is retained for all

- **Recorded at:** `docs/scoring_model.md:1026-1027` · **status:** `live` · **group:** G
- **What the entry says the decision is:** A correction rule is either removed everywhere or kept everywhere. If it still changes an answer under any one of the tuning presets, it stays.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-325 — A correction rule that changes a committed chord's identity is retired or folded in BEFORE the search is widened past it

- **Recorded at:** `docs/scoring_model.md:1198-1205` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Where a later correction can change which chord was committed, that correction is removed or absorbed into the scoring before the search is allowed to consider more alternatives — otherwise the search would be reading a predecessor that a later step is still going to change.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-326 — The chord-path search emits the whole path with every stretch's alternatives and margins, not the committed reading alone

- **Recorded at:** `ARCHITECTURE.md:2052-2062` · **status:** `live` · **group:** G
- **What the entry says the decision is:** The search hands forward, for each stretch, the chosen reading together with the readings it beat and by how much — because the layer above chooses among them.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-327 — The root-continuity guard reads the reconstructed inversion credit, superseding the designed sounding-third test

- **Recorded at:** `docs/scoring_model.md:478-490` · **status:** `live` · **group:** G
- **What the entry says the decision is:** The guard that withholds the continue-the-same-root reward asks whether the candidate earned any inversion credit at all, rather than testing directly whether its third is sounding. The two agree everywhere except on diminished chords, where the direct test would be wrong.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-329 — Completeness of the candidate list is the priority — a chord never listed can never be chosen

- **Recorded at:** `cowork_layer4_chordsymbol_design.md:208-212` · **status:** `live` · **group:** G
- **What the entry says the decision is:** LEGACY (the per-slice chord decoder awaiting deletion) IN ITS LETTER, LIVE IN ITS PRINCIPLE: for each stretch the analysis first generates every chord the sounding notes could spell, and only then chooses among them. Leaving a chord off the list is the error that matters most, because nothing downstream can recover it. The letter — this listing step of the dormant Layer-4 decoder — goes with that decoder. The PRINCIPLE was transferred to the live joint estimator by the user's OI-275 ruling (2026-08-02, reading 1-with-transfer): candidate admission complete by default, and any prune derived from the model, measured for established loss, and ratified. So this entry is LEGACY-marked for where its text lives, and it is at the same time the family design's ratified admission premise — the marker below must not be read as retiring the principle.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-330 — Never a pooled recompute — the chord is never re-derived from several stretches' notes thrown together

- **Recorded at:** `cowork_layer4_chordsymbol_design.md:394-398` · **status:** `live` · **group:** G
- **What the entry says the decision is:** The analysis never gathers the notes of several consecutive stretches into one bag and reads a chord off the bag. Each note's membership is judged in its own stretch against the prevailing chord.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-331 — Every chord decision carries its ranked alternatives and its confidence — committed, inherited, and abstained alike, never pruned

- **Recorded at:** `cowork_layer4_chordsymbol_design.md:576-579` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Whatever the chord layer decides for a stretch, it carries the readings it did not choose and how sure it was. That carry is what lets the layer above correct a decision by choosing among readings rather than working the notes out again.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-335 — The function layer outputs the Roman numeral; the tonic/subdominant/dominant summary is a derived read-out, never a stored output

- **Recorded at:** `cowork_layer5_function_design.md:621-626` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The layer's answer is the Roman numeral — the complete, precise reading. The coarse three-role label can be worked out from it whenever a display needs it, so it is never stored or used to drive the analysis.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-336 — Cadence detection is key-agnostic and votes for the key rather than reading one

- **Recorded at:** `cowork_layer5_function_design.md:627-631` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Points of harmonic closure are found without being told the key, and each one casts a vote for what the key is. Reading a key that a cadence is supposed to help decide would be circular.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-337 — A lean toward another degree is a tonicization by default; a key change needs a confirming cadence AND persistence, expressed as a change-cost

- **Recorded at:** `cowork_layer5_function_design.md:632-634` · **status:** `live` · **group:** H
- **What the entry says the decision is:** When the music leans toward a note other than the home tonic, the home key holds and the chord is written as an applied chord. The key changes only when a cadence confirms the new key and the music stays in it; how long it must stay is a cost that falls as the candidate area grows, not a fixed number of bars.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-338 — The function layer selects among the chord layer's carried readings and never re-derives a chord from the notes

- **Recorded at:** `cowork_layer5_function_design.md:636-639` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Where the chord layer left a stretch open, this layer picks one of the readings that layer carried. It never goes back to the notes and works out a chord of its own.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-339 — A confident earlier decision can be overturned by decisive later evidence, through ONE confidence-weighted forward-recompute mechanism — architecture-wide

- **Recorded at:** `cowork_layer5_function_design.md:646-657` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Every later stage brings its own evidence to bear on every earlier decision. Agreement strengthens it; disagreement overturns it only when the contradicting evidence is strong enough, and how strong depends on how sure the earlier stage was. When that happens the affected passage is re-read forward once, and the overturned decision is then closed for the rest of the pass.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-341 — The licensed root-motion set is completed by theory — the ascending fifth, the descending second and the diatonic diminished fifth are added

- **Recorded at:** `cowork_layer5_function_design.md:203-216` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The list of chord-to-chord root motions the analysis treats as real functional progressions was inherited from an older scoring mechanism and left out three motions that standard theory licenses and the project's own catalogue uses. They are added. This is completing an algorithm against theory, not tuning it.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-343 — The key/mode layer owns the candidate space and the note-evidence model outright; the residual is SELECTED from its carried alternatives, never re-scored

- **Recorded at:** `cowork_layer3_keymode_design.md:76-88` · **status:** `live` · **group:** F
- **What the entry says the decision is:** Working out the tonality from the notes happens in exactly one place: that stage owns the list of possible tonalities and the model of how well each fits the notes, and no other stage infers a tonality from the notes or generates or re-scores a tonality candidate. What the notes cannot settle — relative major against relative minor, and where one tonality gives way to the next — is handed on with the ranked runners-up, and the later function stage settles it by choosing one of those runners-up, never by inventing a candidate or scoring the notes again.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-344 — A scale outside the twenty-one recognized modes is reported as the best-fitting recognized mode, never as the unrecognized scale

- **Recorded at:** `cowork_layer3_keymode_design.md:145-149` · **status:** `live` · **group:** F
- **What the entry says the decision is:** Music written in a scale the analysis does not know — pentatonic, blues, whole-tone, octatonic, or any non-Western or microtonal scale — is reported as whichever of the twenty-one recognized modes fits the notes best, chosen by the ordinary whole-run decision rather than by any separate similarity measurement. The unrecognized scale is never named as such.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-345 — The style preset first enters the analysis at the key/mode layer, as a deliberately weak prior over the modes that the note evidence overrides

- **Recorded at:** `cowork_layer3_keymode_design.md:169-175` · **status:** `live` · **group:** F
- **What the entry says the decision is:** The user's style setting has no effect on reading the notes or cutting the music into stretches; the first place it acts is the tonality decision, where it nudges which of the twenty-one modes are expected in that style. The nudge is deliberately small: the notes decide, and the setting only tips cases the notes leave genuinely open.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-347 — The cost of changing tonality is cheap-to-stay plus a term growing with tonal distance plus a large extra penalty on the relative major/minor switch

- **Recorded at:** `cowork_layer3_keymode_design.md:323-326` · **status:** `live` · **group:** F
- **What the entry says the decision is:** Staying in the current tonality costs nothing; changing costs a base amount, plus more the further away the new tonality is, plus a large extra amount for the specific switch between a major key and its relative minor. A single flat 'do not flip too easily' margin was considered and rejected.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-348 — Tonal distance in the change cost is circle-of-fifths distance — not semitone distance, not differing scale tones — and brief-versus-sustained has no duration threshold at all

- **Recorded at:** `cowork_layer3_keymode_design.md:228-236` · **status:** `live` · **group:** F
- **What the entry says the decision is:** How far apart two tonalities are, for the purpose of the change cost, is counted in steps around the circle of fifths — the number of key-signature steps between them — so C to F sharp and C to G flat are both six. It is not the distance in semitones and not a count of how many scale notes differ. And nothing anywhere counts how long an excursion lasts: whether a passage reads as a passing tonicization or as a real change of tonality falls out of the fit-against-cost arithmetic alone.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-349 — The key/mode confidence compares whole readings — the winning run against the best run forced to a different tonality there — not the top two candidates at that stretch

- **Recorded at:** `cowork_layer3_keymode_design.md:327-329` · **status:** `live` · **group:** F
- **What the entry says the decision is:** How sure the analysis is about the tonality at one stretch is measured by re-running the whole passage with that stretch forced to a different tonality and seeing how much worse the best such reading is. It is not the gap between the two best-scoring candidates at that stretch on its own.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-351 — The key/mode search is its own decoder; the chord decoder is not reused for it

- **Recorded at:** `cowork_layer3_keymode_design.md:330-331` · **status:** `live` · **group:** F
- **What the entry says the decision is:** Finding the best run of tonalities uses a decoder written for that job. Reusing the existing chord decoder was considered and rejected, because that one is specific to chords.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-352 — The key/mode grading bar splits the cases first: agreement where the published analyses are unanimous, any recorded reading (or an uncertain mark) where they are not

- **Recorded at:** `cowork_layer3_keymode_design.md:337-343` · **status:** `live` · **group:** F
- **What the entry says the decision is:** A case counts as unambiguous when the published human analysis gives one tonality there, records no alternative, and — where more than one published analysis covers the piece — the analyses agree. Everything else counts as genuinely ambiguous: a recorded alternative, disagreeing analyses, or a modal passage the major/minor-only human analysis cannot express. On the unambiguous cases the analysis must match the single reading; on the ambiguous ones it must match one of the recorded readings or declare itself unsure.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-353 — The key/mode layer is graded on two goals kept apart — agreement where the notes decide, and whether its own uncertainty lands on the genuinely ambiguous cases

- **Recorded at:** `cowork_layer3_keymode_design.md:353-358` · **status:** `live` · **group:** F
- **What the entry says the decision is:** Two things are measured, and neither is folded into the other. First, does the tonality agree with the published human analysis where the notes settle it. Second, is the layer's own declared uncertainty honest — whether the unsure mark and the confidence actually fall on the genuinely ambiguous stretches, and whether the true tonality is among the runners-up it carried.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-365 — A corpus search driven by the SUM of all needs is worth running, but it is step 3 of 3 — the needs list and the re-scoring of what is already enumerated come first

- **Recorded at:** `cowork_score_census.md:226-230` · **status:** `live` · **group:** Q
- **What the entry says the decision is:** Searching against everything the project needs at once is useful, but only after two cheaper steps. First the full list of needs has to exist as a written artifact. Then every collection already enumerated is re-scored against that list, without searching at all. Only what is still uncovered afterwards is searched for.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-376 — The joint key-and-chord step was designed as a BOUNDED COUPLING over the two existing decoders, and the unified single-state alternative was REJECTED — the option later adopted as the production architecture

- **Recorded at:** `cowork_joint_key_chord_design.md:87-106` · **status:** `shelved-with-evidence` · **group:** A
- **What the entry says the decision is:** When the coupling of tonality and chord was designed, two shapes were on the table: one decision over a single combined state holding tonic, mode and chord together, or the two existing stages kept apart with a bounded coupling between them. The bounded coupling was chosen, for three stated reasons: the two stages are already built as separate decoders and the combined state would discard both and re-lay the pipeline; the gain is concentrated on a small hard minority, so re-laying the whole pipeline to serve it is disproportionate; and the coupling can be built forward-only, respecting the rule against a later stage reaching back into an earlier one. The step was afterwards shelved against measurement, and the option rejected here is the shape the production engine now has.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-380 — The carry's meaningful axis is DISTINCT ROOTS, and every above-threshold root is carried at graded confidence — a carry of winner-plus-one discards the third root on about a quarter of slices

- **Recorded at:** `cowork_layer5_engagement_design.md:136-142` · **status:** `live` · **group:** G
- **What the entry says the decision is:** What one stretch of music offers is many candidate spellings of very few chord roots: measured, about five candidate readings but only about two distinct roots. So what is handed forward is a distribution over distinct roots, each with its best voicing, its variant set, and its own confidence. A third distinct root passes the bar on roughly a quarter of stretches, and those ruled-out and low-confidence roots are information, not noise — they are where the later selection and any tonality-chord coupling earn their keep. A carry that offers only the winner and one alternative throws the third root away on a quarter of stretches.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-381 — The carry must cap on DISTINCT ROOTS, not on voicings — the existing voicing-keyed cap gives no structural guarantee that a third root survives

- **Recorded at:** `cowork_layer5_engagement_design.md:164-172` · **status:** `deferred` · **group:** G
- **What the entry says the decision is:** The limit on how many alternatives are kept counts spellings, not roots, so the allowance can be used up entirely by inversions and template variants of the top two roots before a third root is reached. Keeping a third root is therefore a by-product rather than a guarantee. The shape owed is the other way round: for each distinct root above the bar, carry its best voicing, its variants and its confidence, and set the limit on the number of roots, with each root's variant depth bounded separately.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-382 — The function layer selects by JOINT CONSISTENCY across tonality, root, inversion and bass — not by maximizing any one score — and every ambiguity kind reasons over the full carried distribution

- **Recorded at:** `cowork_layer5_engagement_design.md:187-198` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Choosing among the readings handed forward is done by picking the one that agrees best across all the evidence at once — the tonality, the root, the inversion and the bass — rather than the one that scores highest on any one kind of evidence alone. Everything else is carried on at graded confidence, and where nothing dominates the stretch is marked open. This generalizes what was built: only the symmetric-rotation case already reasoned over the whole set of alternatives, while every other case decided between just two readings.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-383 — Bass, spelling and tonality-consistency DECIDE; a licensed progression is only a tie-break among already-consistent readings and may never override a committed root

- **Recorded at:** `cowork_layer5_engagement_design.md:212-217` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The built resolver leads with whether one chord progresses plausibly into the next, and that is the wrong lead. The evidence that actually carries root correctness is the bass and inversion, the written spelling, and how well a root fits the tonality of the passage. Whether the progression is a licensed one is a tidy signal that turns out to be uncorrelated with getting the root right, so it is demoted: it may separate readings that are already equally consistent, and it may never overturn a root the vertical evidence committed to. Only the ordering is fixed here; the weights are left to the fitting phase.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-384 — Re-ranking the tonality under chord evidence is a SEPARATE step, never part of the function layer's selection — the function layer reasons inside a tonality already fixed

- **Recorded at:** `cowork_layer5_engagement_design.md:263-266` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The function stage chooses among readings within a tonality that has already been settled; it never re-opens which tonality that is. Anything that re-ranks the tonality in the light of the chords, or the chords in the light of the tonality, is a distinct piece of machinery upstream of it, and that piece owns the small population of places where the two genuinely depend on each other.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-385 — Pedal-point detection's home is DECIDED: a reader over the chord layer's carry that annotates a carried reading — never a second analysis that overwrites the winner

- **Recorded at:** `cowork_layer5_engagement_design.md:410-414` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Deciding that a SUSTAINED NOTE is a pedal - that the real harmony is the chord moving against it - is a chord-identity question, so the detector sits on the chord layer's output side, reading what that layer already carried. THE PEDALED NOTE CAN BE IN ANY VOICE: the bass pedal is the classic case, but the ratified pedal-point class is voice-independent (D-207), and this entry's home decision - an ADDITIVE reader that marks one carried reading as the pedal reading - applies unchanged whichever voice holds the note. It never takes ownership of the winning reading, never writes back into the scoring, and never replaces the set of alternatives; the original reading survives at its own confidence. (The source document's own wording says 'bass' - the legacy-era default; the voice-independent scope is the ruled one, user 2026-08-02 with D-207.)
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-386 — No fourth hand-rolled scan for the best different-root alternative — the pedal reader consumes the carry's own ranking, or the one unified primitive

- **Recorded at:** `cowork_layer5_engagement_design.md:445-452` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Finding the strongest alternative with a different root is a decision the code already makes in four separate places. The pedal reader adds no fifth: it reads the margin straight off the ranked distinct roots the chord layer already carried, or through the single shared routine that unification replaces the four with. The second-strongest root's carried confidence is the pedal confirmation signal. RULING (user, 2026-08-02, OI-278): the SECOND alternative — 'the one unified primitive' — is STRUCK: measured at the code not to exist (the four scans it presumed one were never one decision, D-403). The FIRST alternative stands: the pedal reader takes its margin from the ranked distinct roots the chord layer already carries.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-387 — A contradiction between the function context and a committed chord is surfaced on the ONE open mark, enriched with a reason — not on a second parallel flag, and not by overloading the plain undecided mark

- **Recorded at:** `cowork_layer5_engagement_design.md:490-505` · **status:** `live` · **group:** H
- **What the entry says the decision is:** When the functional context disagrees with a chord the earlier stage committed to confidently, that disagreement is recorded as a reason on the single existing open mark, alongside the genuinely-undecided reason it already carries. Two shapes were rejected. Setting the plain undecided mark would be wrong in meaning: the chord stage was not undecided, it committed, and the reading is carried unchanged. Adding a second flag beside the open mark would be two fields on the same object meaning the same thing, threaded through the same three places.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-388 — Texture is read primarily from HOW VOICES MOVE TOGETHER, not from how far each line leaps — the interval-led alternative was measured weaker and partly an encoding artifact

- **Recorded at:** `cowork_voiceleading_axis_design.md:545-548` · **status:** `live` · **group:** H
- **What the entry says the decision is:** What separates one texture from another is the pattern of parallel, similar, contrary and oblique motion between pairs of lines. The rates of those four motion types alone recover the texture structure; the statistics of how far each single line moves do not, and are used only as a secondary description of melodic complexity.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-389 — A notated voice is a FACT and an inferred perceptual line is a JUDGMENT — the two are separate types and are never conflated

- **Recorded at:** `cowork_voiceleading_axis_design.md:549-551` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The line the score actually writes and the line a listener hears are different things and are kept apart, in the words used and in the types the code carries. The written one is a fact taken from the score; the heard one is always called a stream, is always marked inferred, and carries its own confidence. Merging them into one idea with a quality flag was considered and rejected.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-390 — The first version classifies the WHOLE selection as one texture — classifying within a piece is deferred behind a measurement, because the evidence is per-piece

- **Recorded at:** `cowork_voiceleading_axis_design.md:552-555` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The study that established the texture classes measured whole pieces. Whether the same statistics, computed over a moving window, would find the places where the texture changes inside a piece has not been measured. So the first version gives the whole selection one texture, and finding several within it waits on that measurement. Shipping the windowed version now was considered and rejected as building on an assumption.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-391 — Reads between the two analysis dimensions are admissible only where the combined dependency graph stays acyclic — harmonic layers may take voice-leading FACTS freely; a voice-leading component may take a committed harmonic result only if nothing that result depends on consumes it back

- **Recorded at:** `cowork_voiceleading_axis_design.md:559-567` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The harmonic analysis and the voice-leading analysis may read each other, under one rule checked at every wiring. A harmonic stage may freely use the voice-leading facts derived straight from the notes, because those depend on no harmonic decision. A voice-leading component may use a harmonic result that has already been committed — recognizing a stock pattern needs scale degrees, and scale degrees need the tonality — but only if nothing that harmonic stage depends on, directly or through others, reads that component back. A blanket ban on reading anything harmonic was considered and rejected.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-392 — The later voice-leading components are CLAIMS WITH OWNERS, not builds — each clears its own design document and its own evidence before any instruction exists

- **Recorded at:** `cowork_voiceleading_axis_design.md:556-558` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Stream separation, phrase segmentation, pattern recognition, voicing analysis and part-writing advice are all named and assigned, but none is built. Each first needs its own design document and the evidence to stand on. Building the whole dimension in one go was considered and rejected.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-393 — Every voice-leading inference publishes the committed answer AND the FULL ranked list of all alternatives with their weights — nothing below the top is discarded

- **Recorded at:** `cowork_voiceleading_axis_design.md:372-377` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The texture stage does not publish only the class it chose. It publishes every class it considered, ranked, each with the weight it earned, so that anything reading it later — including the calibration step — sees exactly what the stage saw. Nothing below the winner is thrown away.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-394 — Reducing a chord-bearing voice to one line is a DECLARED parameter of the request, uniform across sources — never silent, never chosen per source; the first version offers exactly one rule

- **Recorded at:** `cowork_voiceleading_axis_design.md:330-334` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Where a written voice carries chords rather than single notes, anything needing one line from it must name the rule that picks that line, and the rule travels with the answer as provenance. There is one rule in the first version: take the highest sounding pitch. It is applied the same way everywhere, which is what removes the uneven treatment the exploratory study had between its sources.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-395 — Three named floors govern abstention, and the FIT floor is the one that lets a passage resembling NO known texture decline rather than be forced to its nearest

- **Recorded at:** `cowork_voiceleading_axis_design.md:402-407` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The texture stage declines to answer under three named conditions: too few motion samples to support any decision, too small a lead of the best class over the second, or too poor an absolute fit of the best class. The third is the one that matters for music the taxonomy does not cover: without it, a passage unlike every known class would still be assigned to whichever class it least resembled, because a lead over the second-best says nothing about whether either fits.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-396 — The voice-leading dimension covers NOTATED music only, and its style coordinate is UNDEFINED — not zero — for sources that carry no voices

- **Recorded at:** `cowork_voiceleading_axis_design.md:528-530` · **status:** `live` · **group:** H
- **What the entry says the decision is:** This dimension reads the lines a score writes, so a source that carries no lines at all, such as a lead sheet, has no voice-leading character to read. Every consumer must treat that coordinate as undefined rather than as zero, because a missing measurement is not a measurement of nothing.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-397 — The homeless analysis objects are ASSIGNED to named owners on the voice-leading dimension — the stock patterns, the melodic phrase, chord voicing, and part-writing advice — as claims, discharged only at each owner's own ratified design

- **Recorded at:** `ARCHITECTURE.md:1197-1200` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Four kinds of analysis object that previously had no owner are assigned here: the stock eighteenth-century patterns and the chromatic line cliché, which the chord dictionary already flags as belonging to this dimension; the melodic phrase; chord voicing and arrangement, which the dictionary explicitly excludes from its own scope; and checking and advising on part-writing. Each is recorded as a claim with an owner, not as work started, and the claim is settled only when that owner's own design is ratified.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-398 — Parallel motion is judged SEMITONE-EXACT, not by generic diatonic size — a same-direction move whose semitone interval changes counts as similar motion

- **Recorded at:** `cowork_voiceleading_axis_design.md:109-112` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Two lines count as moving in parallel only when they move the same way and the distance between them in semitones is unchanged. A pair moving the same way from a major third to a minor third is therefore similar motion, not parallel, even though both are thirds. The alternative — counting by the size of the interval as written on the staff, so that any third to any third is parallel — was the open question, and this is the answer.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-400 — A PER-VOICE span kind is admitted to the span typology — melodic phrases overlap across voices by construction and tile only within one voice

- **Recorded at:** `ARCHITECTURE.md:1153-1158` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Until now every kind of span the analysis produces cuts across all the music at once. The melodic phrase does not: in contrapuntal writing the voices' phrases run concurrently and out of step with one another, as a fugue's staggered entries do. So a per-voice kind of span is admitted to the catalogue of span kinds, which is what a phrase-segmentation design can then be written against.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-406 — The catalog owns the NAMED progressions and substitutions; the pairwise licensing grammar is owned by the function layer — the two are never derived from each other

- **Recorded at:** `cowork_progression_schema_dictionary.md:35-41` · **status:** `live` · **group:** M
- **What the entry says the decision is:** Two places in the system know about which chords may follow which. This reference catalog holds the named progressions and substitutions; the function layer holds the separate rule about which root motions are allowed at all. Neither is computed from the other. The single link between them runs one way: every adjacent pair in a new catalog entry must be allowed by the grammar, and a pair that is not is either a mis-encoded entry or a genuine gap in the grammar.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-419 — Until the recognition consumer is built, the function layer does not touch the harmonic vocabulary

- **Recorded at:** `ARCHITECTURE.md:5705-5715` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The reference catalog of named progressions and the function layer are connected by a separate piece of work that has not been built. Until it is, the function layer makes no use of the catalog at all — it is not a partial or optional connection, it is absent. That piece is also where the five idioms first do any work, by weighting which catalog entries count.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-421 — Idiom re-discovery rides every corpus wave, on research material only, and a changed cluster set is its own ratification event

- **Recorded at:** `ARCHITECTURE.md:5564-5577` · **status:** `live` · **group:** M
- **What the entry says the decision is:** Whenever the body of music the project holds changes materially, the study that discovered the five idioms is re-run under the same protocol, to ask whether the five reproduce. It is run only on the development set and outside research corpora, never on the music held back for evaluation, because what the study produces becomes a shipped parameter. If the clusters come out different, that is a taxonomy revision and needs its own ratification — it changes the tags on every catalog entry, so after the tags were encoded it is a migration, not a relabel.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-423 — The gate-retirement stage is the only sanctioned way the post-scoring gates change, and three do-not rules hold through every stage

- **Recorded at:** `docs/scoring_model.md:1218-1225` · **status:** `live` · **group:** G
- **What the entry says the decision is:** LEGACY (the chord analyzer awaiting deletion): three prohibitions hold for the whole programme — no new after-the-fact correction rules, no widening of a threshold, and no gating of the root-continuity bonus. The only sanctioned way any of those correction rules changes is the deliberate per-rule retirement stage, where a rule is removed only once the replacement reproduces the fixes it was pinned to.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-425 — The uncertainty surface's contract IS the full posterior; the local slice is the first delivered step, and the completion is a named step, never an indefinite upgrade

- **Recorded at:** `cowork_notation_adoption_increment.md:282` · **status:** `live` · **group:** B
- **What the entry says the decision is:** What the analysis publishes about its own uncertainty is, by contract, the full spread of probability over every reading it considered — not just a note about the runner-up. The narrower runner-up form ships first, because it is the part already checked and it is a subset of the full form; finishing the rest is a named, tracked step of the same piece of work, not something that waits for someone to ask for it. If checking the full form turns up a problem, that stops the work and goes back to the user; it never quietly becomes a decision to keep the narrow form forever.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-440 — The language-model integration is purpose-built and does not wait for the plugin-API reform

- **Recorded at:** `ARCHITECTURE.md:8113-8116` · **status:** `deferred` · **group:** N
- **What the entry says the decision is:** The language-model bridge is built as its own focused module against the interfaces that already exist, rather than waiting for the general plugin API to be redesigned. The two are separate projects on separate timetables and may converge later.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-441 — Analysis and modification are phases of ONE conversation; a follow-up instruction re-uses the reasoning rather than re-analysing

- **Recorded at:** `ARCHITECTURE.md:8135-8138` · **status:** `deferred` · **group:** N
- **What the entry says the decision is:** Asking about the music and then changing it happen in a single conversation. When the model has already worked out what is wrong, an instruction to fix it is carried out from what it already reasoned through, not by analysing the music again.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-442 — A validation failure goes back to the language model as a tool-call error and is never shown to the user

- **Recorded at:** `docs/llm_integration.md:304-305` · **status:** `not stated` · **group:** N
- **What the entry says the decision is:** When the checks reject something the model proposed — a note outside an instrument's range, parallel fifths, a malformed bar — the rejection is returned to the model, which corrects itself and tries again. The user never sees the rejected attempt; only output that passed the checks reaches the music.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-443 — Tool use is the only capability the provider abstraction requires; a provider without it is read-only

- **Recorded at:** `docs/llm_integration.md:601-604` · **status:** `not stated` · **group:** N
- **What the entry says the decision is:** The user picks which language-model provider to use. The only thing the system demands of a provider is that it can call tools. One that cannot may still be used to answer questions about the music, but it may not be used to change the music.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-444 — The core access layer is a facade over interfaces that already exist, not a redesign

- **Recorded at:** `docs/llm_integration.md:367-369` · **status:** `not stated` · **group:** N
- **What the entry says the decision is:** The shared foundation the language-model bridge and any future plugin interface both sit on is not new machinery. An audit of the existing internal interfaces found they already cover almost everything it needs, so the layer is a clean face over what is there.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-445 — A musical address does not identify a single note, so the note entity carries its own identifier

- **Recorded at:** `docs/llm_integration.md:418-421` · **status:** `not stated` · **group:** N
- **What the entry says the decision is:** Several notes of one chord sit at exactly the same address — same part, staff, bar, beat and voice — so an address cannot name one note. The note therefore carries an identifier of its own, and that identifier is what a change is aimed at.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-447 — The model's tool definitions are generated from the operation set, never maintained by hand

- **Recorded at:** `docs/llm_integration.md:613-615` · **status:** `not stated` · **group:** N
- **What the entry says the decision is:** What the model is told it can do is derived from the operations themselves. Adding an operation makes it available to the model with no second list to keep in step.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-448 — The operation set is curated from observed use, not an exposure of every editing method

- **Recorded at:** `docs/llm_integration.md:286-288` · **status:** `not stated` · **group:** N
- **What the entry says the decision is:** The model gets a chosen set of about forty editing operations covering the changes that matter, rather than everything the editor can do. Which ones are chosen is decided by watching what the read-only phases actually reach for.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-449 — Factor granularity is fixed: the bass factor is evaluated per event, the missing-tone penalty per event of segment length, the emission per tone, and the boundary-family factors per boundary

- **Recorded at:** `ARCHITECTURE.md:434-445` · **status:** `live` · **group:** A
- **What the entry says the decision is:** The scoring form is written as a term per segment, which left open whether each term is counted once for the whole segment or once for each event inside it. That is now fixed: the bass evidence is judged at every event against the segment's chord, a missing chord tone is charged in proportion to how long it fails to sound, each sounding pitch is judged on its own, and the terms that belong to a boundary stay at the boundary.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-450 — The key-signature and declared-mode prior conditions the INITIAL key state only, re-entering only at a notated signature change

- **Recorded at:** `ARCHITECTURE.md:447-456` · **status:** `live` · **group:** A
- **What the entry says the decision is:** What the written key signature and any declared major/minor say about the key is used once, to set the starting key, and then not again — unless the score itself changes signature part way through, which is new written evidence and re-anchors it. The alternative, a pull toward the signature at every point, is rejected.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-451 — A desk simulation's table values are provisional, enter no fit, and a verdict that would flip inside a provisional value's plausible range is reported as a near-tie, never as a win

- **Recorded at:** `CLAUDE.md:100-102` · **status:** `live` · **group:** A
- **What the entry says the decision is:** When a mechanism is traced by hand, the numbers used are stand-ins declared up front whose only job is to let the mechanism be followed. None of them may become a fitted value later. And if a trace's answer would change had a stand-in been chosen differently within its believable range, the trace reports a near-tie and names the deciding cell rather than claiming a winner.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-452 — Every desk-simulation trace runs at identity weights — the ratified ablation baseline — so the trace tests the structure and the tables, not the weighting

- **Recorded at:** `CLAUDE.md:110-112` · **status:** `live` · **group:** A
- **What the entry says the decision is:** Each hand trace is run with every weight set to one, which is the baseline the design already requires be measured. That way what the trace checks is whether the shape of the model and its tables behave, and not whether a weighting was chosen well.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-453 — The desk simulation's verdict: the ratified factorization passes nine of ten traces and no finding reopens the structure

- **Recorded at:** `cowork_factorization_desk_simulation.md:590-591` · **status:** `live` · **group:** A
- **What the entry says the decision is:** Ten cases were traced by hand against the agreed model. Nine behaved as the model says they should. The tenth exposed one thing the model had not settled — how finely each term is counted — and that was fixed by sharpening the model rather than by changing what the model is made of. So the variables, the factors and the decoding stay as ratified.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-454 — The grouping layer detects nothing — it assembles what earlier layers decided, and pressure to add detection means the work belongs elsewhere

- **Recorded at:** `cowork_layer6_grouping_design.md:307-311` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The grouping stage adds no detector of its own. It puts together the boundaries, cadences, keys and unresolved marks the earlier stages produced. If it starts to feel as though grouping needs to detect something, that is a sign the work belongs to an earlier stage or is out of scope — not that grouping needs a new mechanism.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-455 — A cadence away from a grouping boundary is surfaced as internal, never snapped to the nearest boundary and never discarded

- **Recorded at:** `cowork_layer6_grouping_design.md:355-357` · **status:** `live` · **group:** H
- **What the entry says the decision is:** A cadence usually lands where a grouping span ends, but a span may end with no cadence at all, so the relation runs one way only. A cadence that lands nowhere near a boundary is marked as falling inside a span and shown as such. It is not dragged to the nearest boundary and it is not thrown away.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-456 — Sections, periods and sentences are out of the grouping layer's core for PROPORTIONALITY — not disqualified for lacking an oracle

- **Recorded at:** `cowork_layer6_grouping_design.md:348-354` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Larger formal structures — sections, periods, sentences — are left out of the grouping stage's core because that stage assembles the flat grouping and formal structure is a bigger thing belonging higher up. They are NOT rejected for being uncheckable against our annotated music: the standing contract says that alone never disqualifies sound theory. They may be built when a need arises, with a chosen way of gaining confidence in them and an explicit mark that they are empirically unchecked.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-457 — A group truncated by the selection edge is marked as truncated, and a group that runs off the edge unclosed carries an extension cue the grouping layer only surfaces

- **Recorded at:** `cowork_layer6_grouping_design.md:214-222` · **status:** `live` · **group:** H
- **What the entry says the decision is:** When a group begins or ends only because the user's selection stops there, it is marked as clipped by the selection edge, so a cut-off group is never presented as a complete one; the same mark applies to a key area at the edge. When a group reaches the edge with neither a closing boundary nor a cadence, it carries a cue saying that widening the selection would complete it. The grouping stage only shows the cue — deciding to act on it, by asking for more music and re-running, belongs to whatever drives the pipeline.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-459 — The key-area confidence is a declared margin-class boundary confidence, and its input is the declared key confidence — never the grading diagnostics' sigmoid

- **Recorded at:** `cowork_layer6_grouping_design.md:254-261` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The confidence the grouping stage publishes for a key area is a quantity crossing a stage boundary, so it obeys the cross-layer confidence rules: it sits between zero and one, it is declared as a margin rather than a calibrated probability, and it names how it was combined and from what. What it is combined FROM is the declared key confidence, not the squashed number the grading diagnostics use.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-460 — A group counts as fully resolved exactly when no unit in it carries an unresolved mark — no confidence threshold enters the test

- **Recorded at:** `cowork_layer6_grouping_design.md:292-296` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Where an earlier stage left a reading unresolved, that mark is shown on the group and the key area containing it. The grouping stage never resolves it — it has no evidence the earlier stage lacked. A group is reported as fully resolved when, and only when, none of its units carries such a mark; no confidence number is consulted.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-461 — The grouping layer is an explainability layer, not an accuracy requirement, and is deliberately kept thin

- **Recorded at:** `cowork_layer6_grouping_design.md:324-326` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The best published systems reach competitive Roman-numeral accuracy with no grouping stage at all — grouping falls out of stable key runs. Ours exists to make the analysis explainable, not to make it more accurate, and it is held to the thin assembly job on that basis.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-462 — Cadence validation is scoped to LOCATION; cadence TYPE is only partially attributable and is never a clean gate

- **Recorded at:** `cowork_layer6_grouping_design.md:369-371` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Cadences are checked against the annotated corpus for WHERE they fall, because that check survives a wrong Roman numeral. WHAT KIND of cadence it is depends on the harmony being right, so on the harder repertoire that can only partly be attributed — it is measured and reported with that caveat, and it never becomes a pass-or-fail gate.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-463 — The temporal signals sitting in the vertical scorer are left where they are, and the gate that depends on them must move with them

- **Recorded at:** `docs/scoring_model.md:1227-1235` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Five signals that look backward or forward in time are computed inside the part of the scorer that is supposed to judge only what sounds at one moment. They are known debt and are not to be moved yet. When they are eventually moved, the gate whose test depends on one of them has to be changed at the same time.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-464 — No further progression-level signal may be added to the single-step look-around structure; it goes in the progression context instead

- **Recorded at:** `ARCHITECTURE.md:4462-4471` · **status:** `live` · **group:** G
- **What the entry says the decision is:** The structure that carries a chord's immediate neighbours was designed as a one-step look-around, and four fields describing the previous winner's competition outcome were added to it that belong to a planned progression-level structure instead. Nothing further of that kind goes in, and the migration of the four is to be planned explicitly when the progression analyzer's design begins.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-465 — The policy for judging a proposed post-scoring gate: another bias correction gets the bias fixed first, a structural condition is sound, and a cascade means the missing thing is functional context

- **Recorded at:** `docs/scoring_model.md:1238-1250` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Three tests decide whether a proposed gate should be added. If it is one more variant of correcting the scorer's pull toward reading the bass as the root, first ask whether the pull itself can be reduced or whether functional context would remove the ambiguity — add the gate only if the fix really is local. If it turns on pitch arithmetic and what is present rather than on what came before or after, it is likely sound. And if it needs the whole three-step cascade shape, that is a strong sign the real problem is missing functional context.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-466 — Forward-only is a strong DEFAULT, not dogma — a backward edge is admissible only as a deliberate, surfaced, measured, documented exception

- **Recorded at:** `ARCHITECTURE.md:1090-1092` · **status:** `live` · **group:** C
- **What the entry says the decision is:** The rule that each stage passes its work forward and never reaches back may be relaxed if it genuinely gets in the way of being right. But only deliberately and in the open: the case must be justified by evidence that the forward-only path has stopped improving, confined to the cases that need it, gated so it does not fire on the ordinary majority, bounded so an iterative one cannot run away, and recorded as an architecture decision. A silent cycle is never admissible.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-467 — A rebuilt or re-tuned chord scoring must not rely on the held-note repetition bonus the faithful note model removed

- **Recorded at:** `ARCHITECTURE.md:1950-1961` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Before the note reader was rebuilt, a note held across a tie was counted more than once, and that spurious extra weight happened to push a handful of ambiguous sonorities toward the right root. The faithful note model removed it. Whatever replaces or re-tunes the chord scoring must not lean on that boost to get those cases right.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-469 — The tick-local path is left OUTSIDE the unified pipeline by design — its point-in-time semantics would be distorted by one shared interface

- **Recorded at:** `ARCHITECTURE.md:7356-7367` · **status:** `live` · **group:** I
- **What the entry says the decision is:** Three of the four ways the program produces harmony were merged into one shared pipeline. The fourth — the one that answers 'what chord is under this note, right here' — was deliberately left separate, because forcing it through a pipeline built around stretches of music would bend what it means.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-470 — The temporal-context extension fields are recorded during the pipeline's own analysis pass; no consumer re-runs the chord analysis to rebuild them

- **Recorded at:** `ARCHITECTURE.md:4472-4481` · **status:** `live` · **group:** I
- **What the entry says the decision is:** What the chord analysis saw around each stretch of music is written down while the analysis runs. A consumer that needs it reads what was recorded, instead of analysing the passage a second time with a freshly built context — which is how the two paths used to disagree.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-471 — The sub-beat annotation duration gate is not retired on argument — it is kept or dropped on a measured observation run, with the verdict stated in advance

- **Recorded at:** `ARCHITECTURE.md:7369-7384` · **status:** `live` · **group:** J
- **What the entry says the decision is:** A rule hides very short chords from the Roman-numeral annotation. Whether to keep it was not settled by opinion: the decision was written down in advance as a comparison — run the annotation with and without it on real scores, and keep it only if it removes clutter without also removing correct labels.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-472 — Key areas are grouped by a smoothing pass over regions whose key sequence has already been smoothed, and a region that disagrees without clearing the confidence test keeps its own key while being grouped into the enclosing area

- **Recorded at:** `ARCHITECTURE.md:2208-2220` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Neighbouring stretches in the same key are collected into one key area. A stretch that reads a different key but is not confident enough to open a new area keeps its own reading for display, yet is counted inside the surrounding area — so the Roman numerals are written against the key that actually governs the passage rather than against a momentary wobble.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-474 — No published study reports per-axis inter-annotator agreement for Roman-numeral analysis of Baroque/classical symbolic music — the ground-truth ceiling principle #21 demands is unmeasured by the entire field

- **Recorded at:** `CLAUDE.md:131-141` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Principle #21 says the accuracy of the human annotation is itself something to measure, so that an error we cannot fix is told apart from two experts simply disagreeing. Searching the literature found that nobody has published such a figure for this repertoire — so the ceiling cannot be cited from anywhere and would have to be measured here.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-475 — The BCMH chorale annotations are NOT established as an instrument: one named annotator with no independent second annotation, the annotations sit on a reduction, and they reached the repository through a machine translation

- **Recorded at:** `cowork_term_theory_grounding.md:274-279` · **status:** `live` · **group:** C
- **What the entry says the decision is:** A second set of human chorale analyses is held, and it would be the natural way to measure how far two annotators disagree. It still cannot be trusted, and since 2026-08-11 one of the three grounds has changed rather than gone away: the annotating laboratory has now named its single annotator and stated that nothing was annotated independently, so there is no second reading inside this collection at all. The other two grounds are untouched — the analyses describe a simplified version of the music rather than the full texture, and they were converted automatically into our format, so the conversion's own errors would show up as disagreement.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-476 — The phrase-boundary primitive is owned by the notation-derived view layer — not by the note model, and not by the function layer that consumes it

- **Recorded at:** `cowork_phrase_boundary_design.md:250-253` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Working out where a musical phrase ends is done by the same kind of component that reads the bass line or the written spelling off the page. It is not part of the plain record of the notes, and it is not part of the stage that detects cadences — because that stage uses phrase ends as input and cannot also produce them.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-477 — Phrase boundaries are read from the written surface alone — never from a resolved key, chord or cadence — and the boundaries this misses are accepted, not recovered here

- **Recorded at:** `cowork_phrase_boundary_design.md:64-69` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Phrase ends are found from what is printed: rests, note lengths, leaps, metric position, marks and barlines. Nothing about the key or the chords may enter, because the stage that detects cadences uses phrase ends, so a phrase end that depended on a cadence would be circular. The cost is accepted and stated: a phrase that is marked only by its harmony, with no gap in the surface, is missed here and picked up later.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-478 — A phrase boundary is a peak in a continuous boundary-strength profile, not the OR of a few binary signals

- **Recorded at:** `cowork_phrase_boundary_design.md:260-265` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Rather than declaring a phrase end wherever any one signal fires, the program computes how strongly each moment is marked as an ending and then picks the peaks. The all-or-nothing version is the special case that cannot express 'a bigger gap than its neighbours', and it finds too many endings.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-479 — The boundary cues run per eligible voice and aggregate to the texture, and BOTH the per-voice and the texture boundaries are published

- **Recorded at:** `cowork_phrase_boundary_design.md:266-272` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The signals that mark a phrase end are properties of a single melodic line, so they are computed for every voice separately and then added up across the voices. Where many voices phrase together the total is high; where one inner voice alone pauses it is low. Both answers are published: each voice's own phrasing and the whole texture's.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-480 — The phrase-boundary primitive is NOT an accuracy requirement — a competitive reference engine does no phrase segmentation at all — so it is built right but kept proportionate

- **Recorded at:** `cowork_phrase_boundary_design.md:303-309` · **status:** `live` · **group:** H
- **What the entry says the decision is:** A comparable system that performs as well as ours at Roman-numeral analysis has no phrase detection whatsoever; it picks up phrase structure indirectly. So this component is not what accuracy depends on. It is a deliberate bet on an explainable, decomposed design — worth building properly, not worth letting grow without limit, and there is a proven fallback if the explicit route proves hard.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-481 — The notated markers are emitted as boundaries unconditionally; only the surface-cue strength is peak-picked

- **Recorded at:** `cowork_phrase_boundary_design.md:188-193` · **status:** `live` · **group:** H
- **What the entry says the decision is:** A fermata, a breath mark, a structural barline and the other written signs are facts, not evidence to be weighed — so each one is reported as a phrase end directly. Only the computed strength has to clear a local-maximum test and a threshold.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-482 — The two hand-synchronised copies of the fermata scan retire into one owned primitive, and that retirement changes no output

- **Recorded at:** `cowork_phrase_boundary_design.md:254-256` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The same fermata-finding code existed twice, kept in step by hand. Both copies are replaced by the single owned component and every consumer re-pointed at it. Because the marker-only behaviour is unchanged, the swap produces identical results — the new behaviour is a separate, measured step.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-484 — The phrase-boundary primitive is a derived view: it inherits the loaded span, requests no extension of its own, and publishes a per-profile max-normalised boundary confidence

- **Recorded at:** `cowork_phrase_boundary_design.md:83-94` · **status:** `live` · **group:** H
- **What the entry says the decision is:** When only part of a score is loaded, this component does not ask for more music. Its profile simply ends where the loaded stretch ends; a consumer that wants boundary evidence further out asks for the extension itself and this component recomputes. Its published strength is comparable only within one score's own profile — it never overrides another layer's answer.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-485 — Each picked boundary should carry which cue fired and at what scope; the picked set is scope-blind today and the refinement waits for the inference phase

- **Recorded at:** `cowork_phrase_boundary_design.md:205-218` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The markers that produce a phrase end are of two kinds: some apply to the whole ensemble by notation (a structural barline), and some are written on one instrument (a breath mark). Today both are treated as whole-texture endings, so a local breath is promoted to a global boundary and the fact that it was local is lost. A boundary should record which signal produced it and at what scope — recorded as owed, and deliberately not built yet.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-490 — FALSIFIED: no threshold can make the fine-grain function override net-positive — the harm rate is flat against both quantities the threshold is built from

- **Recorded at:** `docs/scoring_model.md:1276-1284` · **status:** `live` · **group:** H
- **What the entry says the decision is:** A late correction pass overturns a committed chord when the surrounding progression argues against it. Whether it helps or hurts turns out to be unrelated to either quantity its trigger is made of — how confident the earlier reading was, and how strongly the progression contradicts it. So no setting of the trigger separates the cases it fixes from the cases it breaks, and the best available setting simply switches the pass off.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-491 — REFUTED: making the override's comparison vertically fair does not repair it — even where the alternative fits the notes at least as well, it is still about 71 % harmful

- **Recorded at:** `docs/scoring_model.md:1285-1292` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The obvious repair was to let the pass overturn a chord only when the replacement fits the sounding notes at least as well as the reading it displaces. Measured, that band is still wrong about seven times in ten. The problem is not that the comparison was unfair; it is that the progression argument does not predict which root is correct at these moments.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-492 — The recommended redesign is to demote the override to an annotation — carrying the earlier reading unchanged and surfacing the contradiction — floored by simply disabling it

- **Recorded at:** `docs/scoring_model.md:1302-1312` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Instead of overturning the committed chord, the pass should record that the surrounding progression disagrees and leave the chord alone. That matches simply switching the pass off on accuracy, while keeping the disagreement as information a later stage can use. Tightening the trigger and repairing the comparison are both rejected: they were measured and both lose.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-493 — Restricting the override to the genuinely-coupled key-and-chord minority is UN-COMPUTABLE, not merely unmeasured: its trigger is not computed anywhere and building it is the still-owed joint step

- **Recorded at:** `docs/scoring_model.md:1293-1301` · **status:** `live` · **group:** H
- **What the entry says the decision is:** The principled home for this correction is the small set of moments where the key and the chord genuinely depend on each other. That set cannot be measured today, and not for want of a dump: half of the trigger requires re-deciding the chord under a different candidate key, which is precisely the joint step the project has not built. So this option is a long-run successor, not a near-term choice.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-494 — RATIFIED AMENDMENT A-4: the function layer must gain key-confirmation channels that do not require a cadence, plus an enharmonic-identity rule for key spans

- **Recorded at:** `ARCHITECTURE.md:2154-2162` · **status:** `live` · **group:** F
- **What the entry says the decision is:** The program only accepts a change of key when a cadence confirms it. Music that deliberately avoids cadences — a sustained dominant that never resolves, a sequence that transposes step by step — therefore keeps being read in the old key. The amendment requires channels that confirm a key without a cadence, and a rule for deciding whether a span is written in one spelling of a key or its enharmonic twin.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-495 — RATIFIED AMENDMENT A-5: when the phrase-boundary profile is flat, cadence admission relaxes with vote-weight scaling instead of starving

- **Recorded at:** `ARCHITECTURE.md:2143-2153` · **status:** `live` · **group:** H
- **What the entry says the decision is:** Cadences are only looked for at phrase ends. In music with almost no surface punctuation the phrase-end signal goes flat, and everything that depends on it gets nothing to work with. The amendment requires a specified fallback: admit cadences more freely there but weight their votes down, using the graded strength that is already computed.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-496 — RATIFIED AMENDMENT A-6: whether the pairwise progression grammar lives inside the harmonic vocabulary or stays a separate store is decided at the recognition-consumer build, explicitly

- **Recorded at:** `ARCHITECTURE.md:5716-5725` · **status:** `live` · **group:** M
- **What the entry says the decision is:** Knowledge about which chord may follow which is held in two places: a pairwise rule set inside the function layer, and a catalog of longer patterns. Whether these become one store or stay two is not to be settled by drift — the amendment requires the choice to be made, and made when the component that queries the catalog is built.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-497 — RATIFIED AMENDMENT A-7: the empirically-unvalidated mark must be APPLIED to the Jazz preset constants and the unvalidated idioms, with the validation path named

- **Recorded at:** `ARCHITECTURE.md:5423-5436` · **status:** `live` · **group:** C
- **What the entry says the decision is:** The rule that says an unvalidated value must be marked as such already exists. The review found it was not actually applied to the constants only Baroque data has ever calibrated. The amendment requires the mark to be put on them, and the corpus that would validate each to be named alongside.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-498 — RATIFIED AMENDMENT A-9: a product stance is owed for output that is mostly uncertain, and for music outside the tonal vocabulary altogether

- **Recorded at:** `cowork_architecture_review_2026_07.md:333-335` · **status:** `live` · **group:** J
- **What the entry says the decision is:** The design deliberately says 'uncertain' rather than guessing. Nobody has decided what the user should see when most of a passage comes back uncertain, or what the program should say about music that is not tonal at all — where the right answer is to state that plainly rather than to produce a confident reading. The amendment requires that stance to be written.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-499 — RATIFIED AMENDMENT A-10: four documentation riders — a consolidated ownership page for the notation-derived views, the membership tie-breaker recorded as idiom-calibrated, and the producer-agnostic seam pinned as a design property

- **Recorded at:** `cowork_architecture_review_2026_07.md:336-338` · **status:** `live` · **group:** K
- **What the entry says the decision is:** Four small documentation debts, ratified together: the notation-derived view layer owns several things and has no one page saying so; the rule that breaks a tie about whether a note belongs to the chord is calibrated to one style and is not recorded as such; the property that a learned component could be dropped in where the hand-built one sits is currently true but written down nowhere; and the status file's entry format is hard to read.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-500 — The user ratified CORPUS EXPANSION at the architecture review: gate-grade jazz ground truth, chromatic material of the Wagner class, and more non-Bach, non-Baroque annotation generally

- **Recorded at:** `cowork_score_census.md:112-120` · **status:** `live` · **group:** C
- **What the entry says the decision is:** At the same review the user approved widening the material the program is measured against: real ground truth for jazz, hard chromatic repertoire, and in general more annotated music that is neither Bach nor Baroque.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-501 — A tool may read a written chord symbol ONLY as a comparison or ground-truth label — never as input that influences what the analyzer computes

- **Recorded at:** `ARCHITECTURE.md:1507-1514` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Chord symbols printed in a score are instructions the user wrote, not results. Production analysis may not read them at all. A measurement tool may put them beside the program's own answer to see how far the two agree, but it may not let them change what the program computes.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-502 — The span a recognised named progression covers is called the progression-schema-span — the bare word 'sequence' is reserved for the harmonic sequence and 'progression' for the whole committed chord stream

- **Recorded at:** `cowork_progression_schema_design.md:245-247` · **status:** `live` · **group:** M
- **What the entry says the decision is:** The stretch of music covered by a recognised named progression needed a name. It is called the progression-schema-span. The two shorter names were rejected because each already means something else here: a *sequence* is a progression repeated at rising or falling transpositions, and *the progression* is the entire analysed chord stream.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-503 — The idiom mixture is DISCOVERED from the score and merely SEEDED by the user's preset, in three forward-only phases

- **Recorded at:** `cowork_progression_schema_design.md:135-136` · **status:** `live` · **group:** M
- **What the entry says the decision is:** How much weight each harmonic idiom carries is worked out from the music itself. The user's chosen preset only supplies the starting point, and the estimate moves away from it as recognised evidence accumulates. It runs in three passes that only ever feed forward, so nothing loops.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-504 — A recognised harmonic sequence is ALWAYS emitted as key evidence — the earlier gate that emitted it only where no cadence existed threw corroboration away

- **Recorded at:** `cowork_progression_schema_design.md:161-178` · **status:** `live` · **group:** M
- **What the entry says the decision is:** When the same progression is recognised at successive transpositions, that is evidence about where the tonality is going. It is now always published. It corroborates a cadence that agrees with it and tempers one that disagrees, and it stands in as the confirming channel only where no authentic cadence confirms the candidate tonality — always at a weight below the cadence's.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-505 — A harmonic sequence requires at least two transposed statements of the SAME recognised entry; a single internally-sequential entry emits none

- **Recorded at:** `cowork_progression_schema_design.md:162-166` · **status:** `live` · **group:** M
- **What the entry says the decision is:** A recognised progression that is itself built out of transpositions — a circle of fifths, a Monte, a Fonte — does not by itself count as a sequence. Two or more transposed statements of the same catalog entry do. A single internally-sequential recognition publishes its own span instead, and its transposition structure stays where it belongs, in the catalog.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-506 — Progression recognition is ADDITIVE: the literal Roman numeral is never rewritten, and a substitution is recorded only in the annotation

- **Recorded at:** `cowork_progression_schema_design.md:193-194` · **status:** `live` · **group:** M
- **What the entry says the decision is:** When the recogniser sees that a chord is standing in for another — a tritone substitute doing a dominant's job — it says so in the annotation and leaves the Roman numeral exactly as the analysis committed it.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-507 — A catalog entry defined by its melodic or bass lines is recognised by its chord skeleton alone and carries a 'chords-only' mark, with its prior strength reduced

- **Recorded at:** `cowork_progression_schema_design.md:272-273` · **status:** `live` · **group:** M
- **What the entry says the decision is:** Some named patterns are defined by their melody and bass lines as much as by their chords. This consumer can only see the chords, so it recognises such a pattern by its chord skeleton, marks the recognition as chords-only, and trusts it less. The mark comes off, per entry, when the voice-leading work supplies the other half.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-508 — The catalog/grammar consistency test ships scoped to the MEASURED containment — an explicit known-gap list — and tightens to a clean assertion when the grammar amendment lands

- **Recorded at:** `cowork_progression_schema_design.md:242-244` · **status:** `live` · **group:** M
- **What the entry says the decision is:** The premise that every adjacent chord pair inside every catalog entry is licensed by the analysis's own grammar was checked and turned out to be false: a handful of entries exercise musically correct motions the grammar did not license. The test therefore ships allowing exactly those, and any further failure is an error. When the grammar is completed the allowance list empties and the test becomes the plain assertion it was meant to be.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-509 — Where the analysis already committed a chord, a recognised progression corrects it through the EXISTING override frame and may only SELECT an already-carried reading — no new comparison frame, and never a reading built from the notes

- **Recorded at:** `cowork_progression_schema_design.md:121-128` · **status:** `live` · **group:** M
- **What the entry says the decision is:** If a recognised progression demands a different chord than the one already committed, the recogniser does not invent a chord from the notes. It puts its evidence into the comparison the correction mechanism already makes, under the same threshold, the same tie rule and the same once-per-pass rule, and it can only pick a reading that was already on the table.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-510 — The correct carry is the one that keeps the distinct alternative reading, not the one that appends a near-duplicate of the winner — chosen on the carry's purpose, not on which code is at HEAD

- **Recorded at:** `docs/scoring_model.md:936-946` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Two ways of promoting a chord to winner were in use. One swaps an alternative already on the list to the front and leaves the displaced reading in place; the other builds a fresh copy and appends it. Measured, the second injects a near-copy of the winner into the alternatives and pushes out the genuinely different reading. The first is therefore the correct behaviour to unify on.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-511 — One promotion primitive with a present-first dedup guard replaces the two ad-hoc promotion idioms; the append branch fires only when the target is genuinely absent

- **Recorded at:** `docs/scoring_model.md:947-953` · **status:** `live` · **group:** G
- **What the entry says the decision is:** Promoting a chord to winner becomes a single shared operation. If the reading is already among the alternatives it is swapped to the front; only if it is genuinely absent is a fresh one built and appended. That ordering is the whole fix, and it reproduces the existing behaviour exactly where the reading is already present.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-512 — Gate A becomes removable only once the unified promotion reproduces its carry byte-for-byte — that reproduction IS the retirement condition, not the winner-inertness that preceded it

- **Recorded at:** `docs/scoring_model.md:954-965` · **status:** `live` · **group:** G
- **What the entry says the decision is:** The rule could not simply be deleted: deleting it left the winner unchanged but changed the alternatives on a number of scores. It is removable once the shared promotion produces exactly the same alternatives, at which point exactly one rule name survives for the flip.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-514 — A newly acquired annotation set whose works OVERLAP the regression corpus is RECORD-ONLY: it may not be wired to, compared against, or bulk-diffed with the gate corpus without a user ruling

- **Recorded at:** `cowork_score_census.md:88-96` · **status:** `live` · **group:** Q
- **What the entry says the decision is:** One acquired collection of chorale analyses covers the same works the accuracy gate is measured on. It is recorded and left alone: it may not be connected to the analysis, compared against the gate corpus, or diffed against it in bulk. Using it over those pieces at all is a decision for the user. Whatever portion of it covers other repertoire is outside the gate and unaffected.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-516 — Two ground-truth classes with named consumers but no needs row were ADOPTED at the first full-needs audit — contrapuntal/imitative structure, and marked part-writing errors

- **Recorded at:** `cowork_census_full_needs_audit.md:223-225` · **status:** `live` · **group:** Q
- **What the entry says the decision is:** Scanning the list of things the project intends to build against the list of ground truth it tracks turned up two kinds of annotation that a named future tool needs and nothing was tracking: analyses of fugal and imitative structure, and graded exercises with their mistakes marked.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-521 — The general law of the circularity map: an abstract circle becomes acyclic in the concrete by one of four named conditions — and every alleged circle in this system fell to one of them

- **Recorded at:** `cowork_evidence_inventory.md:196-204` · **status:** `live` · **group:** C
- **What the entry says the decision is:** The worry that key, chord, cadence and non-chord tones each need one another turns out not to block anything once the cases are named. A circle dissolves when the score already contains one side of it, when a form of the evidence exists that does not need the other side, when the real dependency is on a coarser fact that is already settled, or when the rare remainder is covered by the ratified forward-recompute pattern.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-522 — Explaining an inference to the end user is a late-bound DISPLAY consumer of facts that already exist — not a new analysis

- **Recorded at:** `cowork_evidence_inventory.md:215-224` · **status:** `live` · **group:** C
- **What the entry says the decision is:** If the evidence behind each inference is published — which pitch classes drove the key, which cadence confirmed the change, how far ahead the winner was, why the analyzer declined to decide — then answering 'show me why' is a matter of displaying what is already there, not of analysing anything again.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-524 — The joint state's mode axis is TWO modes — major and composite minor; modal and chromatic colour lives in the pitch emission, and the un-rounded reading is published

- **Recorded at:** `cowork_joint_estimator_architecture.md:89-103` · **status:** `live` · **group:** A
- **What the entry says the decision is:** The estimator's tonality has only two characters, major and minor, with minor meaning the ordinary minor practice whose sixth and seventh degrees vary. Everything more colourful — Dorian, Mixolydian, the altered scales — is handled as evidence about which notes are likely, not as a separate tonality to decide between. The finer reading is not thrown away: it is published, so the display can tell the user that a passage read as D minor would, unrounded, be called D Dorian.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-525 — The fit is STAGED: the factor tables are counted from ground truth and frozen, and only a small vector of combination weights is fit discriminatively — with an all-weights-equal ablation arm that must be beaten

- **Recorded at:** `cowork_joint_estimator_architecture.md:105-123` · **status:** `live` · **group:** A
- **What the entry says the decision is:** Each table of probabilities is counted from the annotated corpus and then frozen. On top of them sits a short list of weights saying how much each kind of evidence counts, and only those are trained. Because setting every weight to one is exactly the untrained model, the trained weights have to beat that on held-out music before they are adopted at all.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-526 — The joint state's chord axis is SCALE-DEGREE-VALUED — a Roman numeral relative to the state's own tonic and mode — and the chord symbol is a DERIVED fact published once

- **Recorded at:** `cowork_joint_estimator_architecture.md:125-144` · **status:** `live` · **group:** A
- **What the entry says the decision is:** The estimator decides chords as scale degrees within the tonality it is considering, not as absolute chord roots. The ordinary chord name is then worked out from the degree and published once. Two things follow by construction: the terms that used to couple a chord to a key dissolve, because a degree is key-relative already; and evidence from every key pools into the same table, which is what makes counting on a corpus of this size possible.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-527 — There is NO live non-chord-tone cleaning stage: each tone is emitted by category inside the one decode, conditioned on chord-independent melodic and metric covariates, and ornament labels are derived AFTER it

- **Recorded at:** `cowork_joint_estimator_architecture.md:146-167` · **status:** `live` · **group:** A
- **What the entry says the decision is:** The estimator does not first decide which notes are decoration and then read the chord from what is left. Every sounding note is scored by what kind of tone it would be under the chord being considered, using only facts computable without knowing the chord — how it is approached and left, how weak its metrical position is, whether it is tied over. Chord and tone status are settled together, and the ornament names are worked out afterwards from the committed chord.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-528 — The key signature and declared mode enter as a WEAK FITTED SOFT PRIOR with no conditional gate anywhere — the probability calculus delivers 'consult it only when unsure', and the hard declared-mode wall is formally retired

- **Recorded at:** `cowork_joint_estimator_architecture.md:169-182` · **status:** `live` · **group:** A
- **What the entry says the decision is:** The written key signature is used as a gentle nudge whose strength is counted from the corpus, not as a rule and not behind an 'if the analysis is unsure' branch. A weak prior is negligible where the notes are decisive and tips the balance only where they are not, which is exactly the intended behaviour without any threshold. The old hard penalty for contradicting the declared mode is retired.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-531 — The hand-built emission is CONFIRMED and the learned replacement is NOT triggered — retained as an explicit fallback with a concrete trigger, and scoped to one repertoire with a named re-check gate

- **Recorded at:** `ARCHITECTURE.md:731-746` · **status:** `live` · **group:** C
- **What the entry says the decision is:** The open question was whether to keep improving the hand-written scorer or replace it with a trained model. The measured answer is to keep the hand-written one: the error mass decomposes into causes that are fixable within it, and the bucket that would need a learned model came back empty on the sample. The learned option is kept as a stated fallback, to be reconsidered for any slice later shown to be a genuine ceiling.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-532 — The chord-transition table gains one pooling level that groups a secondary dominant's continuations by their RELATION to its target — restoring from counts the one behaviour that defines the chord class

- **Recorded at:** `ARCHITECTURE.md:458-472` · **status:** `live` · **group:** A
- **What the entry says the decision is:** As counted, every secondary dominant was too rare on its own to keep its own row, so all its continuations were merged into the general chord-frequency list. The consequence is that 'the dominant of X moving to X' and 'the dominant of X moving anywhere else' read the same probability — the table is blind to what makes the chord a secondary dominant at all. One extra grouping level, pooled across all targets, restores the distinction from real counts.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-533 — A continuation too rare to have its own stored probability is scored by dividing the row's leftover in PROPORTION to each chord's overall frequency — never evenly, and never as impossible

- **Recorded at:** `ARCHITECTURE.md:474-484` · **status:** `live` · **group:** A
- **What the entry says the decision is:** Each row of the transition table ends with one pooled probability covering everything too rare to store on its own. When the decoder meets one specific rare continuation it must turn that pooled value into a number for that continuation. It does so in proportion to how common the chord is generally.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-534 — The penalty for a chord tone that never sounds is COUNTED per chord factor — root, third, fifth, seventh — replacing one invented blanket number; the per-factor asymmetry then comes free

- **Recorded at:** `ARCHITECTURE.md:486-499` · **status:** `live` · **group:** A
- **What the entry says the decision is:** Judging a candidate chord means weighing notes that sound but do not belong to it AND chord notes that never sound at all. The second direction was answered by a number invented for a paper walkthrough. It is replaced by counting, for every humanly labelled chord segment in the corpus, how often each of the chord's own factors actually sounds.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-535 — The checking stage's verdict: the real counted tables overturn no desk-simulation verdict, but margins moved in both directions and one margin expectation was plainly wrong

- **Recorded at:** `cowork_sensitive_cell_probe.md:246-248` · **status:** `live` · **group:** A
- **What the entry says the decision is:** The three passages whose paper outcomes depended most on placeholder numbers were recomputed with the real counted ones. Every verdict held. The margins did not: they moved appreciably in both directions, and one prediction about a margin was simply wrong.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-536 — The bass note and the chord are chosen TOGETHER — the winner is the (bass, root, template) triple — replacing the sequential commit-the-bass-then-score pipeline

- **Recorded at:** `docs/scoring_model.md:843-856` · **status:** `live` · **group:** G
- **What the entry says the decision is:** The analyzer used to pick the bass note first and only then score chords against it. Two confirmed misreadings both came from that order. It now enumerates the plausible bass notes and the chord candidates together and takes the best combination.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-537 — The completeness bonus fires ONLY for a root-position reading whose three triad tones are all present — the guard that stops it from demoting genuine slash chords

- **Recorded at:** `docs/scoring_model.md:549-559` · **status:** `live` · **group:** G
- **What the entry says the decision is:** The bonus that rewards a chord for having all of its notes present applies only when the candidate bass IS the chord's root and all three tones are above threshold. A genuine slash chord therefore neither gains it nor is beaten by a reading that gains it wrongly.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-538 — A multi-signal scoring change lands one signal at a time, with the corpus check re-run after each step and any increase in errors a hard stop before the next

- **Recorded at:** `docs/iter92_joint_bass_chord_scoring.md:190-193` · **status:** `superseded-by` · **group:** G
- **What the entry says the decision is:** The change was not landed as a whole. Each new signal was added on its own, the corpus was re-measured, and the next signal was only added if the error count had not risen.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-542 — Idiom discovery runs DISCOVER-THEN-NAME: structure is learned on a low-level encoding carrying no theory or genre labels, and theory features and genre labels are interpretation lenses applied afterwards, never clustering input

- **Recorded at:** `ARCHITECTURE.md:5518-5525` · **status:** `not stated` · **group:** M
- **What the entry says the decision is:** The grouping of music into harmonic idioms is learned from a plain, label-free encoding of the notes and chords. Only afterwards is the result held up against theory terms and against genre labels to see what the emergent groups correspond to. Neither is ever fed in.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-543 — The encoding is key-normalised tonal-pitch-class TRANSITIONS — spelled where spelling is reliable, mod-12 only where it is genuinely absent — run as two complementary views

- **Recorded at:** `ARCHITECTURE.md:5526-5536` · **status:** `not stated` · **group:** M
- **What the entry says the decision is:** Pieces are encoded as sequences of chord-to-chord moves with every piece transposed to a common tonic, using the written note names wherever the source spells them. Where no spelling exists at all, plain pitch classes are used. A second, order-free view of the same material runs alongside as a cross-check.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-544 — Confound control is a FIRST-CLASS GATE, and the source-leakage test decides validity: if the clusters are explained by which corpus a piece came from, the result is bookkeeping and not idiom

- **Recorded at:** `ARCHITECTURE.md:5537-5549` · **status:** `not stated` · **group:** M
- **What the entry says the decision is:** The dominant way this kind of study fails is by discovering which collection a piece came from, or what key it is in, or how long it is, and calling that a style. So the source label is held out and the clusters are tested against it. If they are explained by it, the encoding goes back to the drawing board. A discovered structure earns the word idiom only after surviving this.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-545 — The uniform mechanical extractor for idiom discovery is the external library, stopping at the note-and-slice front — OUR OWN key/chord/function inference must NEVER touch the extraction

- **Recorded at:** `ARCHITECTURE.md:5550-5563` · **status:** `not stated` · **group:** M
- **What the entry says the decision is:** Turning every corpus into chords for this study is done by one external library applied identically to every source, and only as far as reading notes and cutting them into simultaneities. Our own analyzer is deliberately not used for it.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-565 — Exact score ties in the decode are real and are broken by a declared TOTAL ORDER on paths, implemented identically in every decoder — no epsilon, no platform dependence

- **Recorded at:** `cowork_joint_estimator_factorization.md:149-157` · **status:** `live` · **group:** A
- **What the entry says the decision is:** Two different readings of a piece can come out exactly equal, and this happens in real music. Left unresolved, which one the program commits to would depend on the machine's floating-point library. So ties are settled by a fixed rule applied in the same way everywhere: prefer fewer segments; if still tied, prefer the earlier sequence of boundary positions; if still tied, prefer the canonical ordering of the states.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-569 — Collecting, filtering and weighting are THREE separate responsibilities; the collection layer collects and annotates, and does nothing else

- **Recorded at:** `cowork_layer1_tone_collection_design.md:33-44` · **status:** `live` · **group:** D
- **What the entry says the decision is:** Finding out which notes sound in a stretch of music, deciding which of them the harmonic analysis should consider, and turning them into weighted evidence are three different jobs. The first is a matter of fact, the second a decision, the third an interpretation. The collection layer answers only the factual question and hands the notes on annotated with everything a later step could need.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-571 — The declared-mode influence becomes a small additive hint, and SMALLNESS IS THE GATE — no separate confidence test is added

- **Recorded at:** `docs/stage4b_design.md:69-73` · **status:** `superseded-in-fact` · **group:** F
- **What the entry says the decision is:** The written major/minor declaration used to override note evidence outright. It becomes a small bonus instead. Because it is small, it can only decide a case where the evidence was already almost balanced, and it cannot overturn a clear reading — so no extra rule is needed to say when it may apply. Its smallness is that rule.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-572 — The hard post-hoc declared-mode promotion is REMOVED OUTRIGHT rather than kept in a gated form

- **Recorded at:** `ARCHITECTURE.md:4339-4348` · **status:** `superseded-in-fact` · **group:** F
- **What the entry says the decision is:** A step that took the best reading agreeing with the written major/minor declaration and pushed it to the front regardless of how badly it had scored was deleted, not softened. Keeping any version of it would have made the demotion of the declaration pointless wherever the note evidence had already won.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-575 — The Baroque partial-signature convention is handled by DETECTING it and reinterpreting the signature one step, not by widening the candidate family for every score

- **Recorded at:** `ARCHITECTURE.md:1917-1933` · **status:** `superseded-in-fact` · **group:** F
- **What the entry says the decision is:** Baroque scores are often written with one accidental fewer than the modern convention, so the true key sits one step to the sharp side of anything the analysis could name. The fix adopted detects that situation — the flattened sixth degree being pervasive and dominating its natural form — and reinterprets the written signature accordingly. The alternative considered and not taken was to let every score choose from two signature families, which would have added a competitor to correctly-written music as well.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-576 — The corpus root-agreement measurement UNDERSTATES the real-world quality impact of a wrong key, because root and bass are largely key-independent

- **Recorded at:** `CLAUDE.md:793-797` · **status:** `live` · **group:** C
- **What the entry says the decision is:** A chord's root and its lowest note can both be named correctly while the key is wrong. So a measurement built on root agreement barely moves when the tonality is misread — but the quality of the chord, its Roman numeral and some of its inversions are all corrupted. The measurement therefore reports less damage than a listener or reader would see.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-580 — Two of the twelve post-scoring gates are purely-local vertical refinements and MUST survive the dissolution; the other ten dissolve into the competition

- **Recorded at:** `docs/scoring_model.md:1252-1265` · **status:** `deferred` · **group:** G
- **What the entry says the decision is:** Most of the after-the-fact repair steps in the old chord path exist only because the decision that preceded them could not see enough context, and they disappear once that decision can. Two do not: they refine the reading from the notes alone and are not compensation for anything. Those two must be carried across, not deleted alongside the others.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-584 — The perfect/imperfect cadence call is made on the BASS-DERIVED inversion; the soprano arrival degree is demoted to a soft optional nudge and the tool never attempts melody identification

- **Recorded at:** `ARCHITECTURE.md:2111-2118` · **status:** `live` · **group:** J
- **What the entry says the decision is:** Whether a cadence is a full close or a weaker one is decided from the bass and the chord's inversion, not from which note the melody lands on. Standard theory uses the melody note, but the program cannot reliably tell which line is the melody: the highest sounding voice is often a doubling, and in some music the lead sits below the top. The top voice may nudge the confidence in a chordal texture; it never decides.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-587 — A user-facing preset presents as a familiar genre-era label plus exemplars the user knows — never as an idiom name or an obscure exemplar; genre names are LABELS over mixtures, never axes

- **Recorded at:** `ARCHITECTURE.md:5628-5635` · **status:** `live` · **group:** F
- **What the entry says the decision is:** What a user picks is named after a period and style they recognise, anchored by musicians they know. It is never named after one of the five structural idioms, and never after an exemplar most people have not heard of. The genre name is only a label for a blend of idioms — genre is not one of the things the analysis is organised by.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-588 — Preset coverage beyond the analysed corpora is three tiers with NO bare guessing — measured, editorially declared with a stated theory rationale, or self-correcting by detection

- **Recorded at:** `ARCHITECTURE.md:5636-5643` · **status:** `live` · **group:** F
- **What the entry says the decision is:** A style we hold annotated music for gets its blend measured from that music. A style we hold none for gets a blend written down deliberately, with the theory reason for it stated, and checked when data arrives. Either way the analysis moves away from the starting blend as it reads the actual score, so a badly chosen preset degrades gently rather than being wrong throughout.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-589 — Every idiom mixture is selectable and the discovered cloud is the EVIDENCE MAP, not the boundary — each chosen point carries its evidence status

- **Recorded at:** `ARCHITECTURE.md:5644-5652` · **status:** `live` · **group:** F
- **What the entry says the decision is:** A user may set any blend of the five idioms, not only the named ones. Where the chosen blend sits relative to the music we actually measured decides what may be claimed about it: inside a measured cluster it is validated, between clusters it is an interpolation, and outside everything measured it is still selectable but is marked as never having been checked against real music.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-590 — The score's own metadata is the PRIMARY home of that score's idiom mixture, and a user-set mixture is never silently overwritten by re-detection

- **Recorded at:** `ARCHITECTURE.md:5653-5668` · **status:** `live` · **group:** F
- **What the entry says the decision is:** A piece's blend of idioms is stored inside the piece's own file, using the score properties MuseScore already saves beside title and composer. So it travels with the file and a later analysis starts warm instead of cold. The stored value records whether a person set it or the program detected it: a person's setting is never quietly replaced, a detected one may be refreshed, and editing the score marks it as due for refresh.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-591 — The licence split for the style system: the ANCHORS are the shipped licence-constrained fitted parameters, and the mixture weights are free user configuration

- **Recorded at:** `ARCHITECTURE.md:5669-5677` · **status:** `live` · **group:** F
- **What the entry says the decision is:** The licensing rule that limits which music our shipped numbers may be fitted on applies to the per-idiom reference values, not to the blend a user chooses over them. A user's own blend carries no constraint at all; only the blends we ship as named defaults must come from freely-licensed music or be declared editorially.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-598 — The style taxonomy and the per-style weights are ONE data-derived object; VALIDATION is a separate third thing that needs annotated scores and is not delivered by the clustering

- **Recorded at:** `ARCHITECTURE.md:5586-5594` · **status:** `live` · **group:** F
- **What the entry says the decision is:** Discovering which styles exist and measuring how strongly each one weighs are not two jobs — they are the clusters and their distributions, one result. Checking whether the analysis actually gets better when it uses a style is a third, separate job, and it needs music with both the notes and a published human analysis, which the clustering does not supply.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-600 — The quality-overwrite information-loss violation is TOLERATED until the gate-dissolution step and stays VISIBLE in the open-items register — tolerated is not forgotten

- **Recorded at:** `docs/scoring_model.md:1355-1365` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Two later passes change the chord quality the chord stage committed and keep no record of what they replaced, which is information loss. It is left in place for now rather than ripped out, because there is no other component yet that owns deciding quality from the key, and removing it now would be a patch across stages. The violation is kept on the open-items list so that leaving it is a decision and not an oversight.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-601 — Before any constant that would make two differently-scaled confidences comparable is fitted, the premise that a fitted constant CAN do so must itself pass a premise ledger and a desk simulation

- **Recorded at:** `cowork_confidence_contract.md:89-101` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Two confidence numbers in the program are on different scales — one runs from zero to one, the other is an unbounded total — and a comparison between them treats them as the same kind of quantity. Fitting a conversion factor is not allowed to be the first move: the assumption that any single factor could make the two comparable has to be written down as a premise and traced by hand first, because the one attempt at such a calibration did not behave monotonically.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-605 — The local-key hypothesis derives from key-agnostic signals ONLY and never from the key-area grouping, which is a post-grouping of the resolved key — a hard design rule, not a preference

- **Recorded at:** `ARCHITECTURE.md:1866-1878` · **status:** `live` · **group:** E
- **What the entry says the decision is:** Deciding that a passage has moved to a new key may only use evidence that does not already assume a key: the closure detector, which works without being told the key, and the plain shape of the music. It may not read the key-area grouping, because that grouping is built FROM the key already decided — using it would mean the detector confirming its own input.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-613 — Ground truth for IMPLIED polyphony is confirmed ABSENT — do not re-search it

- **Recorded at:** `cowork_union_search_record.md:32-34` · **status:** `live` · **group:** Q
- **What the entry says the decision is:** For music where several lines are implied by a single melodic instrument, no published collection of correct line assignments exists — every candidate was either never released or simply reuses the voices the engraver wrote. The absence is the finding, and the record says so rather than leaving the search open.
- **Decided by:** the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value — the decisions register records this entry as 'gap', which its own header defines as "'gap' (a decision that governs a layer and is not findable from that layer's section"

### D-614 — Every real difficulty-grade label source is research-only or proprietary at origin — a commercial grading feature needs a licence path or its own labels

- **Recorded at:** `cowork_score_census.md:307-316` · **status:** `live` · **group:** L
- **What the entry says the decision is:** Every collection that says how hard a piece is to play is either restricted to research use or belongs to somebody who sells it. So a difficulty feature in a shipped product would need either a licence agreement or labels of our own; the held material is enough to check the idea works and not enough to ship it.
- **Decided by:** USER-RULED at the 2026-08-17 sitting — the rule returned this entry rather than guessing, and the user placed it at the 2026-08-17 rulings-sort sitting by the ruled criterion: an entry whose statement binds the SYSTEM (what the analysis is, does, or must satisfy) is DESIGN-INTENT; one whose statement binds the WORK (how we decide, verify, record, sequence) is IMPLEMENTATION-MANAGEMENT

### D-616 — A global tonic anchor enters key scoring at RESOLVER/SECTION scope — never as one more local term inside the window scorer, which is what re-enters the coupling that defeated the local levers

- **Recorded at:** `ARCHITECTURE.md:1879-1893` · **status:** `live` · **group:** F
- **What the entry says the decision is:** Evidence about which key a whole passage is in is applied where the passage is decided, not inside the per-window scoring that ranks candidate keys note by note. Adding it as one more local term is what failed before: any local term strong enough to settle a near-tie when the mode is unknown also overrides the correct answer when the mode IS known.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-622 — The reach-back convergence PROXY was measured FALSE and dropped — the as-built tracks the leading-edge key itself and stops when it stops changing

- **Recorded at:** `ARCHITECTURE.md:1894-1905` · **status:** `live` · **group:** F
- **What the entry says the decision is:** When the analysis has to read backwards for context, it needs a rule for when it has read far enough. The cheap rule proposed in design — stop once a settled key appears anywhere in the material read back — was measured and found to stop too early: one settled measure does not fix the key at the edge of the selection, which only settles once a confident earlier key is established over a stretch. So the built code checks the thing that actually matters and stops when that stops moving.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-623 — A selection-aware capability is a PARAMETER on the one orchestrator, never a sibling — the capability must not duplicate the orchestration

- **Recorded at:** `ARCHITECTURE.md:1801-1808` · **status:** `live` · **group:** I
- **What the entry says the decision is:** A new way of driving the analysis over part of a score was built as an option on the existing driver rather than as a second driver beside it, so there is still one path that builds the note model, cuts it into slices and decodes them. The option is off by default, so the shipped behaviour and every measurement are unchanged.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-625 — Spelling presence is tested with the validity predicate, never with a non-negative test — the flat side of the line of fifths is negative and a non-negative guard silently drops it

- **Recorded at:** `ARCHITECTURE.md:2063-2073` · **status:** `live` · **group:** D
- **What the entry says the decision is:** How a note is spelt is stored as a position on the line of fifths, and that position is negative for the flattest spellings. Code that checks whether a spelling is present by testing for a non-negative number therefore throws away every heavily-flattened spelling — including one that happens to share its number with the field's empty value. The validity test is the correct check.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-629 — The resolver of carried uncertain readings IS the function layer itself — there is no distinct gated box between the note layers and it

- **Recorded at:** `ARCHITECTURE.md:2088-2104` · **status:** `live` · **group:** H
- **What the entry says the decision is:** When the earlier stages cannot decide between two readings they hand both forward with a mark. The thing that then picks one is not a separate component: it is the function stage, choosing as part of naming the harmony. The wording elsewhere about a gated step describes the point at which that stage begins, not another stage.
- **Decided by:** the decisions register's own `home_is_layer_spec` — the entry is recorded in a LAYER'S OWN SPECIFICATION, which is where a decision about what the analysis is or does lives

### D-656 — The crediting rule is NOT amended to count a tonicization label as agreeing with the annotator's modulated numeral; only a diagnostic partial-sub-split is defensible

- **Recorded at:** `CLAUDE.md:1011-1020` · **status:** `live` · **group:** C
- **What the entry says the decision is:** Where our analysis names an applied chord relative to the home key and the human annotator has changed key, the accuracy comparison is not to be altered so that our label counts as agreeing. Only a diagnostic that splits such cases out and exposes the masking is defensible. This is the measurement half of a decision whose build half — leaving the labeller unwired — belongs to the function layer's specification.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-660 — A research-tied name is not renamed but is governed by a two-tier rule, and the terminology cleanup runs in a fixed order with no tree-wide rename

- **Recorded at:** `CLAUDE.md:1528-1539` · **status:** `live` · **group:** K
- **What the entry says the decision is:** A term borrowed from the published research that collides with this project's vocabulary is not renamed. Instead: where the research is actually discussed, the collision is explained and our own synonym stated; and every later use of the borrowed term outside our vocabulary carries a short inline note pointing at the research, so an unannotated repeat use is a flag. The wider terminology cleanup runs in a fixed order — the derived inventory first, then per-word batches the user rules, governing surfaces first — and there is no tree-wide rename.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

### D-665 — What a voice/stream label set actually MEASURES is said at intake — the labels obtainable today come from engraved notation, not from a listener's judgment

- **Recorded at:** `cowork_score_census.md:328-330` · **status:** `live` · **group:** Q
- **What the entry says the decision is:** When a collection of per-note voice labels is taken in, the record states where those labels came from: they are read off the way the music was written down, not off what a listener hears as separate lines. For keyboard music the two are close enough that the field works with the engraved version, and that acceptance is recorded too rather than left unsaid.
- **Decided by:** the authored word recognizers, over the decisions register's own restatement — the entry's own title and plain restatement carry the subject of the analysis and nothing of the working process

## 6. The entries the rule RETURNED, and where the sitting placed them

The rule reached no verdict for these: the decisions register's own home classification
does not place them, and their own restatement carries both subjects or neither. They were
returned rather than guessed, and the user placed them at the 2026-08-17 sitting by the
ruled criterion — an entry whose statement binds the SYSTEM is DESIGN-INTENT; one whose
statement binds the WORK is IMPLEMENTATION-MANAGEMENT. Each appears in §5 or §7 with its
route recorded as *USER-RULED at the 2026-08-17 sitting*, and the rule's own recognizer evidence is kept
beside the placement so it can be read against what the rule saw.

**Placed DESIGN-INTENT (24):** `D-170`, `D-171`, `D-260`, `D-261`, `D-264`, `D-268`, `D-275`, `D-313`, `D-321`, `D-383`, `D-390`, `D-394`, `D-395`, `D-443`, `D-444`, `D-447`, `D-448`, `D-456`, `D-478`, `D-481`, `D-482`, `D-484`, `D-485`, `D-614`

**Placed IMPLEMENTATION-MANAGEMENT (36):** `D-112`, `D-165`, `D-166`, `D-167`, `D-169`, `D-173`, `D-174`, `D-176`, `D-178`, `D-179`, `D-181`, `D-183`, `D-184`, `D-186`, `D-187`, `D-188`, `D-189`, `D-255`, `D-266`, `D-272`, `D-273`, `D-274`, `D-311`, `D-312`, `D-314`, `D-366`, `D-367`, `D-368`, `D-401`, `D-422`, `D-424`, `D-426`, `D-473`, `D-483`, `D-513`, `D-639`

**Left unplaced by the sitting: 0.**

*None.*

## 7. The IMPLEMENTATION-MANAGEMENT entries

Listed by identity: decisions about process, apparatus, workflow, record-keeping or
sequencing. **This is not a judgment on their worth** — they are simply not design input
to the framework phase, and nothing happens to them. A management placement of a standing
principle removes none of its force: the principles govern every session through the
standing rules regardless of the sort.

<details><summary>The IMPLEMENTATION-MANAGEMENT entries</summary>

- **D-109** — The open-items register is the one home for every unresolved issue, and the index is the status of record *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-110** — The decisions register records what was decided and its status - nothing else *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-111** — A decision belongs in the owning layer's specification; the register is an index *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-112** — Never work from memory instead of documented facts *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-115** — The regression stop is the granularity-robust unit; root governs, key and Roman numeral ride beside *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-165** — #1 - build only on established fact and theory *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-166** — #2 - target the specific open question, not the general topic *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-167** — #3 - an unexpected finding is a failure to diagnose, not a curiosity *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-169** — #5 - when facts may be scarce, investigate *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-173** — #9 - measure only on corpora known to be non-stale and accurate *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-174** — #10 - documentation is kept in sync with code SO THAT code can always be compared against its specification, and that purpose is the test of what is worth fixing *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-175** — #11 - regression tests in sync with code, and run between iterations *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-176** — #13 - surface a surprise as a stop before building around it *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-177** — #14 - every behavior change is one user-ratified, revertible, provenance-stamped commit *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-178** — #15 - verify at the objects on the full output surface, never at an assertion *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-179** — #16 - every measurement is stamped to its corpus and its tooling, and the outgoing reference is snapshotted *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-181** — #18 - an unverified causal premise is forbidden (Class A) *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-183** — #20 - fit and evaluation are separated *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-184** — #21 - ground truth is a measurement tool too, and its accuracy is measured *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-186** — #23 - an end-state principle needs a lawful transition *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-187** — #24 - every reported figure carries its uncertainty *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-188** — The constrained-optimum ledger corollary *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-189** — The scope of surprise, and the three-stage funnel *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-191** — The two-tier regression class policy - functional regression stops, rotation churn is tracked *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-193** — The writing standards live in one place, and predicates must be qualified *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-194** — No self-invented labels, abbreviations, numbering schemes or jargon *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-195** — Every design decision carries its defense at its home *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-196** — The self-check: re-read the diff against the principles before reporting *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-197** — The distribution constraint - the import-fix patch is fork-local and never goes upstream *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-198** — The Windows snap fix in the muse submodule is intentional and must not be reverted *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-199** — The MusicXML declared-mode import fix is intentional and must not be reverted *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-200** — Make it work first; compromise on performance only if performance proves a problem *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-203** — Candidate admission is completion, not refinement - so #8 permits fixing it now *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-204** — One fix is designed once over the whole enumerated family, never per symptom *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-208** — A withheld finding never enters a mandatory session-start read *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-209** — Code that is about to be deleted gets no audit - only the no-information-loss check at deletion *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-210** — An exotic mode is graded against its parent collection's minor key, not its own tonic triad *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-211** — Key agreement is reported against both the global home key and the local key *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-212** — The regression stop is abstain-aware: an abstention counts as disagreement on root *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-213** — The defect-type catalog is the living list of every problem type, and it is added to at discovery *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-230** — The decisions register is a mandatory session-start read, and a new ruling lands in the register in the commit that records it *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-231** — Issue-exhaustion and specification completion before any fix design - the three-phase sequencing rule *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-249** — The whole decision surface is delivered as user-visible text before any choice question *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-251** — A running dispatch is never interrupted or steered mid-flight; every instruction is self-sufficient *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-253** — Working-tree files are read with the file tools; bash is limited to git object queries by explicit hash *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-254** — Investigate by default; never ask the user whether to investigate or proceed *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-255** — Every design document follows one fourteen-section structure, synthesized from three published standards *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-257** — A specification carries a locator to its code and tests; code mechanics never do the explaining *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-258** — A prune and tidy pass runs before any publish of the fork, and nothing on its list is acted on before it *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-263** — A refused or truncated extension is marked on the output, never silently absorbed *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-266** — Layer 6 is prohibited until the bounded-context design is coded and regression-tested for Layers 1 to 5 *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-269** — The frame table is the one home of the override arithmetic; a new override site declares its frame before it is built *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-272** — The protocol constants are protocol, not tuning - changing one is an amendment, never a fitting act *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-273** — The architecture-adoption variant of the hard regression stop, written before any diff existed *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-274** — The reverse map - if the new estimator is not adopted it is removed whole, and the retirement map is void *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-277** — Measure before build - and a byte-identical structural refactor is exempt, because byte-identity is its prediction *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-281** — The batch measurement tool must emit the structured fields on every alternative, or the corpus figures silently revert *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-294** — The only ground truth is the human annotation; the algorithmic analysis is a filter, and no self-annotation ever enters a measurement *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-298** — The layer-by-layer audit - each layer is audited once its pieces are in place *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-307** — A specification cites code by function or section anchor, never by raw line number *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-308** — A newly acquired corpus enters as research material; the frozen regression corpus stays the gate until a deliberate re-baseline *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-311** — The chord-analyzer file split happens once — the SEQUENCING is spent (the split happened first, not last); the once-only LESSON is what carries forward *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-312** — The carried alternative readings are inside the byte-identity acceptance contract — same winner with different alternatives is a behavior change *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-314** — A correction rule kept for structural reasons must keep producing evidence that it still fires *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-316** — The chord-symbol parser sussus fix is a recorded local patch with an UPSTREAMABLE distribution disposition *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-366** — Recorded-performance intonation material is OUT of corpus scope — the intonation features are validated by theory and by listening *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-367** — A corpus found FOR one need is scored against the WHOLE needs list at intake, and every annotation layer it carries is inventoried — never tagged to the purpose that found it *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-368** — When new material bears on an already-settled conclusion, the rework question is settled by a recorded protocol — record, measure cheaply, then fork on whether it CONTRADICTS or merely enriches *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-401** — The refactor sequencing call — the portable unification wins run before Layer 5, the legacy-path tangles fold into the decoder engagement, and the file split is last *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-415** — An item on the roadmap may be marked done only with the evidence its own verify column names *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-416** — Two structural refactors are DEFERRED and OWED, and must be surfaced at every planning checkpoint until done *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-417** — The engage criteria — six gates that must all hold, a staged plan, and the user ratification event *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-418** — The retirement map — nothing retires by silence; ten named retirements, each with its trigger and its order *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-420** — One cross-layer extension specification, and the duplicate written the same day is killed into it *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-422** — The jazz fit is deferred to the jazz ground-truth conversion; only the classical common-practice idiom is fitted now *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-424** — A decision surface names the principle behind every pro and con, and rates every option on two axes *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-426** — The modal reading lands inside the notation increment; the ornament labels get their own increment, with the tracking row created at ruling time *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-427** — Component (1a) of the two-deferred-refactors mandate — the physical `chordanalyzer.cpp` file split: DELIVERED 2026-06-17 *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-428** — Component (1b) of the two-deferred-refactors mandate — the iteration-vocabulary API renames: STILL OWED, and the subject is the LEGACY arm *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-429** — Component (2) of the two-deferred-refactors mandate — dissolving the post-hoc gate-correction layer into fitted weights: STILL OWED, and its PRINCIPLE binds the live design *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-430** — The contract-home criterion's unit is a SECTION of a document, not the document *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-431** — A figure enters a dispatch or a report by citation to a generated artifact, never by transcription — and so does a premise *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-432** — What counts as a delegation, graded by form — the clause the section-level criterion did not touch *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-433** — A shelved section can be a home — shelving is a status, not a kind *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-434** — The writing side runs the standing self-check before a dispatch is released, and records its output *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-435** — Delegating to a document and being a home are different tests with different subjects *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-436** — A mechanism is judged on three measured conditions — automatic, detection rate, false-positive rate — and a failing one is REPORTED, not automatically removed *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-437** — Phase 3 waits on the phase-2 items that could find another member of the family being designed for, not on all of phase 2 *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-438** — Open-items register rows whose subject is this project's own tracking and documentation apparatus gate nothing — but an establishment obligation always gates *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-439** — The perspective inventory's §4 is the one home for the enumerated discovery channels, and CLAUDE.md's phase-2 clause points at it instead of listing its own subjects *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-446** — The language model resolves how the user names a passage; no index is built and the kinds of reference are not enumerated *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-458** — The codetta refinement is read as the as-built tiling: keep the strong cut, drop the weak one, and record the codetta end as an annexe *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-468** — The pinned block-(A) instrument declares which inference arm its baselines were measured on, and refuses a corpus whose stamp disagrees *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-473** — A theory-grounding pass labels every load-bearing claim FACT / THEORY / CONJECTURE, cross-checks its central sources independently, and carries no equation out of a text it could not fetch *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-483** — The picked boundaries are validated against the analysts' own phrase marks; a fermata-derived phrase list is inadmissible as ground truth because the primitive reads fermatas *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-486** — A measurement publishes its coverage denominator and its per-corpus breakdown; a single aggregate number that hides which corpus moved is not reported *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-487** — The eleven snapshot source scores are frozen and hash-pinned; changing the set or bumping a pin is a deliberate golden and gate re-baseline *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-488** — The two Bach chorale collections are independent selections, not sub- and superset — and the diff between them is not recoverable in-repo *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-489** — The snapshot sources are hash-pinned rather than copied in-tree, because their licences make an in-tree copy incompatible with this project's licence *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-513** — A corpus registry's content summary is enumeration provenance, not evidence that an annotation layer is present — per-slice presence must be measured *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-515** — A ground-truth class with a named consumer gets its OWN row in the needs vector, never a remark under a neighbouring row — ruled generally after being decided for pedal-point ground truth *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-539** — The standing method for every error slice: decompose it into structural, fitted and ceiling BEFORE building anything — derive, never assert *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-546** — A delegation reaches only the members it names explicitly — a glob and a trailing ellipsis confer nothing; with the two confirmations ruled beside it *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-547** — An audit's scope is generated mechanically from the code and every row gets a disposition — the auditor never chooses what to look at *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-548** — The audit's verdict set is closed with no silent escape, and 'no issue' is itself a recorded claim with a stated reason *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-549** — An audit runs in BOTH directions — from the contract to the code as well as from the code to the reader's judgment — because an absence is findable only from the contract side *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-550** — Every mechanism in the audit inventory carries a MEASURED fire rate beside what it claims — reading answers what it claims, counting answers what it does *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-551** — The audit is itself established with a MEASURED residual-error rate; an audit whose error rate is unknown certifies nothing *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-552** — An audit is TWO runs in a fixed order — blind enumeration first, then the known-problem signature sweep — and certification requires both *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-557** — Build-it-right comes BEFORE tune-precision, strictly — no inference-problem fixing anywhere until the refactoring, the architectural design and the algorithmic completion are done *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-564** — Correction of record: the 'function-only' share of the legacy residual was overstated, because over-grabbed segmentation corrupts the BASS and not only the pitch-class window *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-566** — STRUCTURAL fixes come before correctness fixes: never tune an inference decision while a responsibility is still in the wrong place *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-567** — An audit document labels each claim TRUTH or PROVISIONAL — a claim read from the source this session stands; any quantity carried from memory of an earlier investigation does not *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-568** — The two-track remedy: the chord axis is completed by hand-built rules, the key axis by soft-evidence quality and calibration — and NOT by a wider search on either *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-573** — A floor measurement that collapses is a FINDING, not a failure — it is reported, not papered over *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-574** — The pass-bar for a measured change is set AFTER the baseline measurement, so the threshold is data-grounded rather than guessed *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-581** — Information not yet consumed is NOT automatically a defect: every site is classified preserved-awaiting-consumer, lost, should-already, or unclear — and unclear is recorded for adjudication, never guessed *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-582** — A collapse that is RECOMPUTABLE from what is kept is not information loss — not every collapse is a defect *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-583** — A known deferred loss is KEPT only while it stays characterized EXACTLY, and is re-adjudicated the moment its form changes *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-592** — What FINISHED means for a layer, as the user's standing bar: restructured, built, dead code resolved, legacy retired, regression- and reachable-branch-tested, specs synced — nothing left *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-593** — The line inside the build-it-right firewall: plain correctness bugs are allowed now, accuracy on hard cases is not — and a KNOWN WRONG ANSWER on the key axis is on the forbidden side *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-596** — The north star: BEST means CORRECT — every obligation is judged against the ground-truth oracle and never against whether it passes a proxy gate *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-599** — Adjudication method: apply the standing principles FIRST, and only where they do not decide is there a genuine user choice — what remains for the user is ratifying the derivation, not picking an option *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-602** — A layer's measurement is judged on coverage-matched accuracy AND correct abstention, never on raw coverage — abstaining on a genuinely undecidable case is a RIGHT outcome *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-603** — While the pipeline is being rebuilt, a behaviour-changing increment is graded DIRECTIONALLY and not against a fixed bar — meaningful comparison happens only against the fully reconstructed pipeline *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-604** — A defensible modal reading the major/minor ground truth cannot represent is a GROUND-TRUTH LIMITATION, not a defect to optimise away *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-606** — The binding metric for the modulation detector is modulation CORRECTNESS — explicitly not the agreement percentage, which the change under test can game *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-611** — The tidy guardrails, in three tiers — and the dormant path and its staged scaffolding are DEFERRED-ENGAGEMENT, not dead code: removing them is a STOP *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-626** — A scoring term lands together with its tuning — a dormant weight-zero term is code with no effect and no test that exercises its effect *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-630** — Minimality sets the default and the burden — do not add a component; a new one is earned only by exhibiting a residual class a separable criterion resolves *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-638** — The symmetric-rotation churn is a chord-layer root ambiguity SURFACED, not caused, by a key change — the proper fix is spelling-aware root selection, never a key-layer change *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-639** — Phase 1's doc-sync half reaches a document's account of ITSELF only where that account changes how its analysis content is read — with three worked examples that are the test, and a fallback ruled with it *(decided by USER-RULED at the 2026-08-17 sitting)*
- **D-640** — A count of outstanding work is derived from state at HEAD, never taken from the membership of a list of asks or from an authored disposition beside a row *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-641** — A finding that bears on the analysis is surfaced to the user whatever its size; a finding about this project's own apparatus is rowed and left, and gets no wave *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-642** — Criterion C1 reaches every decision whose content is LIVE — a superseded entry's obligation moves to its successor, and is discharged only where that successor is homed *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-643** — A claim that invokes a ruling as an application of it quotes that ruling in full, never the branch of it that supports the claim *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-644** — Where a superseded decision's content is a REMOVAL, the owning specification states the current behaviour and records the removal as a tried-and-closed line *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-645** — A homing dispatch may edit docs/scoring_model.md, CLAUDE.md and BUILD_AND_TEST.md, and the license is scoped to homing acts alone *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-646** — A generated record that must outlive its own writer is frozen at an established snapshot, and the freeze is enforced by a hash STOP *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-647** — The shell-read guard denies on indeterminate as standing policy, and the ceiling it cannot see is published in the measured rate *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-648** — Moving an authored judgment whole into a retired block is authored-input MAINTENANCE, not a mechanism change *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-649** — Where the implementation contradicts the decision being homed, the decision is written in as what it is with the contradiction stated beside it and no verdict taken *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-650** — Two same-dated texts are compared verbatim before either is retired into the other, and where they bind different acts they are homed side by side *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-651** — The ground-truth ceiling measurement is COMMISSIONED in two halves, and silence on the laboratory contact route is recorded as the route EXHAUSTED *(decided by the authored word recognizers, over the decisions register's own restatement)*
- **D-652** — How the decisions register's same-commit rule is discharged once it has already been missed *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-653** — A correction reconciling a specification to the arm that ships must carry the behavioural non-equivalence visibly, as unmeasured *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-654** — Where a licence's letter leaves a known falsity standing in the file it licensed, the session corrects it and REPORTS the widening in the same act *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-655** — A session may author an owed establishment; its verdicts clear no guard until the reviewed set is applied *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-657** — A mechanism change is decided over its whole population both ways before it is applied, and only the members the defect's own shape names may move *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-658** — Where the record does not settle the question, the surface that returns it to the user gathers facts and makes no recommendation *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-659** — Document-structure conformance is kind-scoped, and the kind list is enumerated with a STOP on an unlisted kind *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-661** — Complete means complete relative to a named derivation, whose measured miss rate against the record is part of its name *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-662** — Every open-items index status cell begins with one canonical token, with a lint and a parser STOP behind it *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-663** — A direction with its artifact named is not a transcribed value *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-664** — Re-homing is the default closing route where no delegation admits an entry's home, and only the user may except a document — before its entries are re-homed, never after *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-666** — An event a mechanism exists to produce is not a rule needing a home — the entry closes as the event, its evidence pointed at the surface the mechanism wrote to *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-667** — A per-corpus establishment verdict is a STATUS, so the decisions register is its home — and writing one into a rule-stating section is the mirror of the findings-table error *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-668** — A homing act tests a section in a FIXED ORDER — pointer move first, kind half before any write — and a findings-recording owner means HELD, never written by stretch *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-669** — A maintenance act establishes the cause BEFORE it touches the mechanism — and a cause that resists establishment is a STOP, with no fix taken on a named-but-unasserted candidate *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-670** — A task that cannot be stopped partway is dispatched FIRST with nothing large in front of it, and the ordering is ruled rather than preferred *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-671** — A derivation, a measurement or a sizing over a derived population is published WHOLE or not at all, and a subset only under a scope that NAMES its members *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-672** — A per-entry pass may be stopped at any member boundary, and the stop is RECORDED — what was done, what was not, and that the remainder is untouched *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-673** — An enumerating pattern whose reach is unmeasured may STATE its bound on its own artifact, and the test is whether an analysis decision consumes it *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-674** — The filing convention for a document the record has OVERTAKEN — a dated report is re-bannered and never rewritten, a live governing surface has its body corrected *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-675** — Phase 1 completes when the INFERENCE-BEARING obligations are discharged — the finish line is cut by D-438's test, the apparatus residue does not gate the completion, and the cut carries a falsification test *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-676** — An apparatus row STAYS OPEN, STOPS GATING and STOPS BEING OWED — with a per-row lapse record naming the derivation that graded it, and no row lapses without one *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*
- **D-677** — A DISCARD verdict on an already-rowed item is an INPUT to the derivation that decides gating, never an edit to a gating verdict — the row stays open, draws no capacity, and a record lacking finding, date or reason does not reach the cut *(decided by the decisions register's own `nonspec_kind`, by the definition its data file's header gives that value)*

</details>

## 8. What this surface does NOT do, and must not be read as doing

- **It executes nothing.** The sort classifies; it retires nothing and moves nothing.
- **It edits no decisions-register file.** The data file, the rendered INDEX and the
  rendered group files are byte-unchanged by the run that produced this surface.
- **It re-opens no filter verdict.** The confirmed side is taken as ratified.
- **It grades no decision's worth.** A management placing is not a demotion, and it
  removes none of a standing principle's force.
- **It seeds nothing yet.** The framework phase has not begun; the seed list is ruled and
  waits for the phase that consumes it, where it remains amendable.

*Generated by `tools/audit/gen_rulings_sort.py` from
`tools/audit/rulings_sort_classification.json`. Reproduce:
`python tools/audit/gen_rulings_sort.py --check`.*
