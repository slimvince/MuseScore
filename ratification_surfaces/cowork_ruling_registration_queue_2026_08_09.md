# The twenty unregistered rulings — which the decisions register should CARRY, and which are exercises of decisions it already holds

> **STATUS: AWAITING THE USER. NO REGISTER ENTRY IS WRITTEN BY THIS FILE**, and nothing here
> ratifies, shelves, supersedes or reworks any decision. Written 2026-08-09 (CC,
> `cc_instruction_return_continuation_2.md` Task 1) on the user's **Ruling 12 of 2026-08-09**
> (`cowork_rulings_2026_08_09_second_stop.md`): CC drafts the per-ruling classification as a
> ratification-surface reading file; **the user rules on the set; the entries then land in ONE
> commit** (D-230 rule (c), discharged late but by the pattern every prior register event used).
>
> **Why this exists.** Three ruling records carry the same banner sentence — *"interim carrier
> until the applying dispatch records them (D-230)"* — and **no register entry exists for any of
> them.** Every ruling's CONTENT is on disk in a ratified record, and every ruling applied is
> recorded in the provenance of the act it licensed. What is missing is the register ENTRY. What is
> at risk is not the rulings; it is the register's claim to be the ONE place a session learns what
> was decided.

---

## 1. What the user is being asked, and what the two verdicts mean

**One verdict per ruling**, and for every ruling proposed as a DECISION, one proposed home.

- **DECISION** — the ruling establishes something that BINDS A FUTURE SESSION beyond the act it
  licensed: a standing rule, a method, a status change, or a commissioning. The register carries
  it, and rule (e) puts its text in the owning specification with the register pointing.
- **EXERCISE** — the ruling APPLIES a decision the register already holds to one occasion: a
  scoped licence to perform an act the record already requires, a timing choice a row reserved to
  the user, a status flip performed under the register's own rules. Nothing new binds; the ruling
  is fully recorded in the provenance of the act it licensed, which is where every one of them
  already is.

**The distinction is not about importance.** Several EXERCISE rulings below unblocked more work
than several DECISION ones. It is about whether a session that reads only the register would be
missing a rule.

**A verdict the user disagrees with costs one line here** — no entry has been written, so nothing
has to be unwritten.

**Where a DECISION's proposed home is a section of a document rather than a layer specification**,
that is rule (e)'s *"wherever possible"* doing its work: a rule about how the record itself is kept
has no owning LAYER, and every comparable entry the register already holds is homed in a section of
`cowork_audit_protocol.md`'s dispatch-protocol block or in `CLAUDE.md`'s own governing text. Where
the proposed home is `CLAUDE.md`, **only the user writes it** — that is flagged on each such row.

---

## 2. The verdicts

### 2.1 The four rulings of 2026-08-08

| # | The ruling, in one line | Verdict | Ground |
|---|---|---|---|
| **A** | **The EPOCH TREATMENT** (`cowork_rulings_2026_08_08_pre_away.md` Ruling 1) — the phase-1q record is declared HISTORICAL and frozen at its established snapshot, the classifier's apply then runs at HEAD, and the freeze is enforced by a hash STOP on every run | **DECISION** | It states a REUSABLE METHOD, and the ruling's own words say so: the hazard of a generated record being regenerated over is discharged **by freezing the record, not by holding its writer forever** — *"the epoch treatment this tool already applies to its own class fields, applied one level up — to the artifact."* A future wave meeting the same hazard needs that method, and nothing in the register carries it. The apply itself, and the flips that followed, are exercises riding on it |
| **B** | **D-601's homing authorized by ONE edit** of `cowork_confidence_contract.md` (Ruling 2) | **EXERCISE** | A scoped licence to perform a homing act phase 1 already requires, over a file that lay outside standing authorization. The ruling names its own precedent — the arc-plan one-edit authorizations — and the standing rule it exercises is already homed (`cowork_audit_protocol.md`, *"A homing dispatch may edit three further files, and the license is scoped to homing acts alone"*). D-601's home and its status are already register content |
| **C** | **OI-351 joins the ruled guard-family design** (Ruling 3) | **EXERCISE** | It applies the standing one-fix-per-family rule (user, 2026-07-28) to one row, and says so in terms. The rule it exercises is already in the register and already in `CLAUDE.md` |
| **D** | **The guard-family design** (`cowork_ruling_guard_family_2026_08_08.md`) — five clauses over OI-300's and OI-348's whole family | **DECISION** | Two of its clauses bind beyond the act. **Clause 4 is closed BY RULING and says so:** *"deny-on-indeterminate is adopted as standing policy and recorded as such"*, with the asymmetry that decides it — a false deny costs a retry through the file tools, a false admit costs an unverified read through the very mount whose measured failure created **D-253**. **Clause 2 carries a stated CEILING** — interpreter code whose path is COMPUTED carries no literal for any policy to see and is admitted, published in the rate rather than left silent (#19, #17d). A session that does not know the ceiling will read the published rate as covering more than it does. Clause 5's corpus-first order is NOT part of this: the ruling itself attributes it to the OI-343 and OI-345 rulings, so that half is an exercise |

### 2.2 Rulings 1–10 of 2026-08-09 (`cowork_rulings_2026_08_09_return.md`)

| # | The ruling, in one line | Verdict | Ground |
|---|---|---|---|
| **1** | `phase1q_reclassification.json` is REMOVED, the removal recorded where the file was named | **EXERCISE** | A filing decision over one committed artifact, performed in the **D-644** shape — a register decision the record already holds — and grounded in #6 and #12's recomputable-collapse clause, both already carried. The two excluded alternatives are recorded at the act's own home |
| **2** | OI-319 FLIPS; its remaining fragment gets its own row | **EXERCISE** | The open-items register's own rules (c) and (d), applied. The OI-283 never-marked-discharged shape it ends is already recorded |
| **3** | The `CLAUDE.md` **D-253** doc-sync correction is LICENSED, as a NARROWING | **EXERCISE** | A scoped licence for one doc-sync correction. Its content is **D-253's own text**, whose verbatim the register re-takes from the corrected home — an update to an entry the register holds, not a new one |
| **4** | The delegation-bar generator: (a) pointer re-aim, (b) retired-block maintenance, both LICENSED | **DECISION**, on (b) only | **(b) legitimises a CLASS and draws a line the record did not have:** moving an authored judgment WHOLE into a retired block, with its reason, is **authored-input MAINTENANCE** and *"not a mechanism change under D-436"*. That line has since decided at least five acts across three batches, and it is the line that says when a session may proceed and when it must return to the user. **(a) is an exercise** — a pointer follows the file it names |
| **5** | **D-286** is homed by the **D-472 PATTERN**; no conformance verdict is taken | **DECISION**, on the pattern | The **pattern** is now a named, twice-ruled homing FORM: write the decision in AS what it is, state the later build's contradiction BESIDE it in a marked block, POINT the questions that would need a judgment at their own rows, and take **no verdict**. It is what lets phase 1 home a decision the implementation contradicts without deciding which is right — exactly the case that would otherwise stall. The homing of D-286 itself is an exercise |
| **6** | **D-289** is ruled SUPERSEDED (the D-642 route), conditional on successor coverage | **EXERCISE** | The D-642 route is already a register decision, and a supersession is a STATUS the register records under rule (c) — which it now does, at D-289's own entry, with the successors named and the condition's piece-by-piece check recorded |
| **7** | **D-291** is SPLIT; the measurement half ruled recorded-twice **on a verbatim-comparison condition** | **DECISION**, on the condition | The condition is a METHOD and it fired: *"the two texts are the same binding statement, compared verbatim before the act; any binding difference is a STOP."* Applied, it stopped the collapse and preserved the more specific prohibition (#12). Without it in the register, the next session meeting two same-dated texts has only the temptation to merge them. **Propose ONE entry covering this and Ruling 11**, which supplies the other half of the same method — see 2.3. The split of D-291 itself is an exercise |
| **8** | The owed `CLAUDE.md` rule-triage entries are AUTHORED BY THE SESSION, reviewed at the queue | **EXERCISE** | The ruling names the decision it exercises — *"the pattern ruled for the non-gating generator's authored verdicts"* — and its pre-act check is that tool's own contract |
| **9** | The OI-349 probe RUNS NOW | **EXERCISE** | A timing choice the row itself reserved to the user. The probe's finding is register and row content; the timing binds nothing further |
| **10** | **OI-179** commissioned in two halves; the ceiling measurement opens WITH phase 2 | **DECISION** | It is a **COMMISSIONING** of the measurement principle #21 demands and D-474 established cannot be satisfied by citation — the only route to that quantity. It also fixes **how the contact route ENDS**: a reply is recorded on the row, and *"silence after a reasonable wait is recorded as the route EXHAUSTED, making D-475's declared status final rather than provisional (#19 closed by exhaustion)."* That exhaustion rule binds a future session directly, and no register entry carries it |

### 2.3 Rulings 11–16 of 2026-08-09 (`cowork_rulings_2026_08_09_second_stop.md`)

| # | The ruling, in one line | Verdict | Ground |
|---|---|---|---|
| **11** | **D-291's measurement half homed SIDE BY SIDE** in gate block (A), each text cross-referencing the other | **DECISION**, paired with Ruling 7 | It supplies the REMEDY the comparison condition leaves open: where two texts share a date and an argument but bind different acts, they are **two decisions homed side by side with cross-references, not one merged text** — because *"#6 forbids two homes for ONE rule and these are demonstrably two"*, and merging would edit a user-homed text and risk paraphrasing away the more specific prohibition (#12, #14). **Propose ONE entry with Ruling 7**: the condition and its remedy are one method, and splitting them across two entries would put the test in one place and what to do about it in another |
| **12** | The propose-then-ratify queue for the unregistered rulings — this file | **DECISION**, narrow — *or* EXERCISE, and the user may reasonably say so | As a procedure it is Ruling 8's pattern again, which would make it an exercise. What is arguably new is one narrow thing: **how rule (c)'s same-commit requirement is discharged once it has already been MISSED** — *"the entries then land in ONE commit … discharged late but by the pattern every prior register event used"* — rather than being retro-fitted ruling by ruling or quietly abandoned. If the user reads that as already implied by rule (c), the verdict is EXERCISE and nothing is lost |
| **13** | The classifier's stale self-account: one edit LICENSED | **EXERCISE** | A scoped licence, and the shape it applies — **D-644** — is already a register decision. *(One thing about its APPLICATION is reported at `cowork_away_returns.md` §1.8 and is not a register question: the same stale sentence stood twice in that file and both were corrected.)* |
| **14** | The legacy-mark verification's moved anchor: one edit LICENSED | **EXERCISE** | It exercises Ruling 4(b)'s maintenance class — itself proposed as a DECISION above, which is the clearest evidence that (b) is the load-bearing one and this is its application. *(Its predicted consequence was refuted; that is a finding, at `cowork_away_returns.md` §2.6 and [[OI-354]], not a register question.)* |
| **15** | **D-472's** wording correction LICENSED, **with the non-equivalence stated** | **DECISION**, narrow — on the non-equivalence requirement only | The correction itself is phase-1 TRUE-half work and an exercise. What binds beyond it is the clause the ruling puts in capitals: a correction that reconciles a specification to the arm that SHIPS **must carry the recorded behavioural difference between the arms visibly, as unmeasured, and must not word it away as equivalence** (#12, #24). That is the failure mode a doc-sync correction invites — the correction reads as *the two are the same*, and a real, unmeasured difference disappears into a tidy sentence |
| **16** | **OI-353 is BUNDLED**: sweep first, then ONE comment-only `src/` commit | **EXERCISE** | It applies the standing one-fix-per-family rule (user, 2026-07-28) and says so in terms — *"never per-symptom; OI-353's six sites are the found members, not the family"* — and its `src/` licence is scoped to that act. Both the rule and the freeze it narrows are already register content |

---

## 3. The tally, and the one register-content question that rides with it

**Proposed DECISIONS — seven entries** (Rulings 7 and 11 proposed as ONE): the epoch treatment
(**A**); the guard family's deny-on-indeterminate policy and its stated interpreter ceiling (**D**);
authored-input maintenance versus a mechanism change (**4b**); the D-472 homing pattern (**5**); the
verbatim-comparison condition and its side-by-side remedy (**7 + 11**); OI-179's commissioning and
its exhaustion rule (**10**); the non-equivalence requirement on an arm-reconciling correction
(**15**). **Plus one the user may downgrade: 12.**

**Proposed EXERCISES — twelve** (thirteen if 12 is downgraded): **B**, **C**, and Rulings 1, 2, 3,
4a, 6, 8, 9, 13, 14, 16.

**★ ONE FURTHER QUESTION, PUT HERE BECAUSE IT IS REGISTER CONTENT AND NOT A FILING ACT.**
Ruling 11 established that **D-291 is two decisions**, and its measurement half is now homed in gate
block (A) while the entry's `home` field still names the build half's home at the Layer-5 function
section — which is correct, because that is where the entry's `verbatim` is located and verified.
**Should the measurement half have its own register identifier?** A session did not decide it: an
entry is the register's unit, and splitting one into two is register content. It is recorded on
D-291's own entry that the question is here.

---

## 4. Proposed homes, for the seven (rule (e))

| Proposed entry | Proposed home | Why there |
|---|---|---|
| **A** — the epoch treatment | `cowork_audit_protocol.md`, a new subsection of the dispatch-protocol block | It is a rule about how the record's own generated artifacts are kept, and that block is where every comparable rule is homed. The applying tool's docstring already states it in full and would become the register's pointer target if the user prefers |
| **D** — deny-on-indeterminate + the interpreter ceiling | `cowork_audit_protocol.md`, dispatch-protocol block, beside *"The three measured conditions a mechanism is judged on"* | The ceiling half is an #19 statement about a published rate, so it belongs beside the conditions a mechanism is judged on rather than in the tool alone |
| **4b** — maintenance versus mechanism change | `cowork_audit_protocol.md`, beside the same subsection | It is precisely the line that says when the mechanism-change reservation (**D-436**) is engaged, so it belongs against it |
| **5** — the D-472 homing pattern | `cowork_audit_protocol.md`, beside *"Where a superseded decision's content is a REMOVAL, the specification states the current behaviour and records the removal as a tried-and-closed line"* | Same family: both are forms for writing a decision into a specification when the plain form would state something false |
| **7 + 11** — the comparison condition and its side-by-side remedy | `cowork_audit_protocol.md`, beside **5** | Same family again, and the three read as one method for homing under difficulty |
| **10** — OI-179's commissioning and its exhaustion rule | **`CLAUDE.md`**, principle **#21**'s ★ block, beside **D-474** | That block already carries the FACT-of-absence and names OI-179 as *"the only available route to the quantity this principle demands"*. **USER-ONLY TEXT** — a session may not write it. If the user prefers not to grow that block, the alternative home is the landed design surface `cowork_scratch_2026_08_08/draft_oi179_ceiling_measurement_surface_2026_08_08.md`, which the ruling already names |
| **15** — the non-equivalence requirement | `cowork_audit_protocol.md`, beside **5** and **7 + 11** | It is the doc-sync case of the same family. The alternative is **`CLAUDE.md`**'s phase-1 clause, at its doc-sync half — **USER-ONLY TEXT** |

*(**12**, if the user keeps it as a DECISION, homes in **`CLAUDE.md`**'s decisions-register section
beside rule (c) — **USER-ONLY TEXT**, and the smallest of the seven-or-eight.)*

---

## 5. What this file does NOT do

It writes no register entry and assigns no identifier. It changes no ruling's text and re-reads no
ruling's merits — every verdict above is about **where a ruling's content must be findable**, never
about whether the ruling was right. It moves no status, homes nothing, and authorizes nothing. It
touches no `src/`, no golden, no corpus of scores and nothing in `tools/robust_stop/`.

**Phase 1's completion statement is not written, not drafted and not partially written here.**

*Provenance: the user's Ruling 12 of 2026-08-09 (`cowork_rulings_2026_08_09_second_stop.md`). The
gap this discharges is `cowork_away_returns.md` §1.4. The three interim carriers whose banners name
the debt are `cowork_rulings_2026_08_08_pre_away.md`, `cowork_ruling_guard_family_2026_08_08.md`,
`cowork_rulings_2026_08_09_return.md` and `cowork_rulings_2026_08_09_second_stop.md` — read whole
(D-643) before this file was written, and each ruling classified from its own text rather than from
any summary of it.*
