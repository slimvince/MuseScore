# CC dispatch — the false-statements pass: two documents corrected where they state something the code refutes, and two acts put to the worth test

> **Status: ACTIVE DISPATCH, written 2026-08-13 (Cowork), at a verified STOP** — the previous batch
> completed, four commits verified at the objects, only `cowork_away_returns.md` and `STATUS.md`
> touched, tip on `origin/master`. Nothing is running.
>
> **★ THIS IS THE FIRST OF FIVE RATIFIED ACTS AND IT IS PLAN-SHAPED.** The user ratified an
> ordering over the acts phase 1 has available: this pass first, OI-150 second, the
> `docs/scoring_model.md` pass third, OI-207 alone and last, the apparatus pair split. **Its ground
> is that this is the only one whose value compounds into phase 2** — the specification-versus-code
> audit is the next event, and a specification describing removed code as live manufactures
> instances of the class that audit's own method names as producing the most confident wrong
> conclusions.
>
> **Read IN FULL, and read FIRST:** `open_items/OI-315.md` and `open_items/OI-321.md` END TO END;
> `CLAUDE.md`'s principle #10 as amended and its worth test; **D-674** at
> `cowork_design_doc_template.md` beside the kind list.
>
> **★ CITE THIS FILE BY ITS EXACT NAME** — `cc_instruction_false_statements_pass.md`. Acts dated
> from the clock; **no positional count anywhere** (D-307, D-432).
>
> **★ All standing rules as adopted.** D-253 in every dialect, including the path-mapping hazard.
> NO TRANSCRIBED VALUES (D-431). Hold-don't-guess. **Read-only on the analysis: no `src/` edit, no
> behaviour change, no golden, no corpus of scores, nothing under `tools/corpus/` or
> `tools/robust_stop/`, no measurement, no design, no fix to inference.** D-231 and #8 stand.
> **Phase 1's completion statement is not written, not drafted and not partially written.** Commit
> and push per task boundary; `origin` only.
>
> **★ A DATE QUESTION IS OPEN AND IS NOT THIS DISPATCH'S TO SETTLE.** This arc's rulings are
> recorded as 2026-08-11 while the clock now reads later. **Date your own acts from the clock**;
> do not correct any existing date, and do not cite a ruling by date where its number will do.

## 0a. THE PREMISE LEDGER (#17a)

**FACT — verified by Cowork at the objects.** The previous batch's four commits, their changed
paths and push state. The enumeration of available acts, read as a structure at
`cowork_away_returns.md`, from which both rows' acts are quoted in their artifact's own words.

**FACT — asserted by the ROWS and NOT verified by anyone yet:** that the piece-start shortcut is
removed at HEAD; that the phase OI-321's header calls deferred was executed. **These are claims
about the CODE made by a row, and a row is a secondary source for a claim about code.** Both are
checked below before any correction rests on them.

**ASSUMPTION — each checked BEFORE the act resting on it; a refutation is a STOP.**

- **A1.** The piece-start shortcut **is removed at HEAD**. *Check AT THE CODE, reading the
  enclosing function and not a matching line. **If it is present, STOP** — correcting the documents
  would then make them false in the other direction, which is worse than leaving them.*
- **A2.** The phase OI-321's header calls deferred **was executed**, established at the code the
  row points at. *Check at the code. If the code does not settle it, STOP.*
- **A3.** **The surfaces are located at the files, not taken from the row's list.** A row can be
  stale about which documents carry a statement. *Check: enumerate the surfaces that actually carry
  each false statement at HEAD; report any the row does not name, and any it names that no longer
  carries it. Correct only what is there.*
- **A4.** **D-674 decides the remedy per document** — a dated report of a state is re-bannered with
  its body untouched; a live governing surface has its body corrected. *Check: state the kind call
  per document with its ground. **A hard case STOPs to the user**, which is Ruling 62's own clause.*
- **A5.** A correction of falsity is made **where the false statement stands**. *Check: if a
  statement is false because it belongs in another layer's document, that is a HOMING question and
  it STOPs — homing is not this dispatch's licence (#7).*

## 0b. THE TASKS, IN ORDER

**D-670 does not order these two** — neither is a derivation over a population, so both may stop at
a site boundary under D-672. They are ordered so that **the check most likely to halt runs first**,
which keeps a halt from arriving after completed work.

**Task 1 — OI-315 (A1, A3, A4, A5).** Establish A1 at the code. Then correct
`docs/key_path_design.md`'s §2.1/§5 self-contradiction and every further `docs/` surface that
states the removed piece-start shortcut as live, per A3's enumeration and A4's kind call. **Former
wording preserved in place (#12)** wherever a body is corrected. A per-site pass: it may stop at a
site boundary under D-672 with the three things that rule names recorded. Commit and push.

**Task 2 — OI-321 (A2, A3, A4, A5).** Establish A2 at the code. Then correct the two false
statements — the header calling a phase deferred, and the named directory that does not exist —
per A4's kind call, former wording preserved. Commit and push.

**Task 3 — two acts put to the worth test, and NOT performed.** Apply amended #10 to **OI-90**
(reason strings under `tools/audit/l1l2/` made stale by the L3 pass) and to **OI-274's banner half**
(two draft banners standing on tracked files). For each: the #19 carve-out first, then name the
analysis decision that consumes it or state that none does, then the verdict. **A verdict of WORTH
FIXING for either is a correct outcome, not a failure** — the test is being applied, not confirmed.
Where the verdict is DISCARDED, record finding, date and reason by the route this batch's
predecessors used; **no row is flipped and no status cell moves.** Commit and push.

**Task 4 — the close.** One `STATUS.md` pointer entry per task, nothing else in that file — it
remains unreadable and its row OI-370 open and gating. Append the close to
`cowork_away_returns.md`. Report at the objects: A1's and A2's findings each with the code read,
A3's enumeration against the rows' own lists, A4's kind call per document with its ground, and the
two worth-test verdicts.

## 0c. WHAT IS DELIBERATELY NOT DONE

**OI-107 does not ride this dispatch unless one of these acts touches `ARCHITECTURE.md`** — the
user ratified that it rides whichever act next touches that file, and if neither does, it waits.
**No other row is corrected and no sweep is run for siblings** — a suspected sibling is reported,
not acted on. OI-150, the `docs/scoring_model.md` pass and OI-207 are ratified for later and are
not started here. No row leaves the gating population by the worth test alone. OI-179 stays OPEN
and GATES.

## 0d. STOP RULES

Halt with a STOP in `cowork_away_returns.md` if: the piece-start shortcut is present at HEAD; the
code does not settle whether the phase was executed; a document's kind under D-674 is a hard case;
a correction would require homing rather than correcting in place; a row's named surfaces and the
surfaces that actually carry the statement disagree in a way A3 cannot resolve by enumeration; or a
guard goes red for a cause that is neither this dispatch's own edits nor already recorded — the two
standing reds are recorded and are not that.

---

*Provenance: Cowork, 2026-08-13. The ordering this dispatch opens was ratified by the user on a
surface carrying five mutually exclusive first-acts, each pro and con naming its principle and each
rated against the ultimate objective. Self-check run before release (D-434).*
