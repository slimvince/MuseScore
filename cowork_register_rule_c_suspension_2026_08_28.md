# SUSPENSION of decisions-register rule (c), with the owed entries listed — Alternative C

> **Written by the Cowork writing side, 2026-08-28, on the user's disposition of the same day.**
> Alternative C of `cowork_register_blocker_surface_2026_08_28.md`, recommended there alongside the
> repair and disposed after the surface was delivered in plain language. **His word was "ok", given
> in conversation and not in a single-limbed ruling record. It is recorded as that.**
>
> **★ THE LIST BELOW IS EMPTY AT AUTHORING AND IS FILLED BY DERIVATION, NEVER BY TYPING.** The
> writing side has no shell and could not enumerate it. `cc_instruction_register_baseline_repair.md`
> Task 1 orders the enumeration and orders it written in here. **A version of this file whose list is
> still empty after that task has run is a defect, not a clean state.**

---

## What is suspended, and what is not

**Suspended:** decisions-register rule (c) of `CLAUDE.md` — *"a new ratification, shelving or
falsification gets its register entry (data + regenerated files) IN the commit that records it"* —
**for ratifications taken while the register's two discard checks are red, and for those only.**

**NOT suspended, and none of these is touched by this file:** rules (a), (b), (d), (e), (f) and
(g)–(n); the requirement that every suspended entry be made; the register's own stops; and any
obligation under **#19**, which the record already places outside every lapse and every
declaration.

**`CLAUDE.md` IS NOT EDITED.** It is a governing document and no session edits one on its own
authority. This file is a dated written suspension standing beside the rule, not a change to it.

## Why a suspension rather than continuing silently

Seven consecutive batches were shaped around the register blocker without it ever being put to the
user, and the eighth is the dispatch that repairs it. **Each of those batches recorded that it wrote
no register entry, which is honest per batch and, across eight, is a drift with no single place a
reader could see it.** The suspension gives the drift one home and one list, so that when the
register accepts entries again the debt is discharged in one act rather than rediscovered.

**The alternative was doing nothing**, which costs nothing today and leaves exactly that condition.
It was declined on the ground above and on no other.

## The bound — when this suspension ends

**It ends when both `tools/audit/decisions/apply_soft_discard.py --check` and
`tools/audit/decisions/apply_residue_discard.py --check` are green**, which is what the repair is
for. **At that point every entry on the list below is owed in one act, and this file is superseded
rather than deleted (#12).** A ratification taken after the checks are green is governed by rule (c)
as normal and never joins this list.

## The owed entries

*To be written in by `cc_instruction_register_baseline_repair.md` Task 1, DERIVED from the ruling
records on disk and never typed from a session's recollection. Each row names the ruling record, the
ruling within it, and the date — and nothing else, no register identity being allocated here.*

<!-- THE DERIVED LIST GOES HERE. An empty list after Task 1 has run is a defect. -->

*Provenance: Cowork writing side, 2026-08-28, at tip `6005daecaf9f1a6692e61521911ef8b99ed73b55` read
as a file at `.git/refs/heads/master`. **No shell command of any kind was run by this side and no git
object was resolved; every git-object value here is RELAYED.** The blocker's diagnosis and the
alternatives are at `cowork_register_blocker_surface_2026_08_28.md` and are not restated here (#6).*
