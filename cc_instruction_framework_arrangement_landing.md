# CC dispatch — land the informed-framework ruling and the three writing-side files; RETIRE the `framework` subject from the boot-pack generator; turn the fourth failing check GREEN; boot NO session

> **Dispatch (Cowork, 2026-08-28).** Executes the consequences of
> `cowork_rulings_2026_08_28_informed_framework_sitting.md`, in which the user ruled that the
> framework phase's deriving session is **not implementation-blind**. **Nothing is running and
> nothing else is dispatched.**
>
> **★ WHAT THIS BATCH IS.** Task 0 lands the untracked writing-side files and the modified handoff,
> and PUSHES. Task 1 **removes the `framework` subject from the boot-pack generator's authored
> tables** — the subject exists there only to serve a pack that the ruling has cancelled — and the
> fourth failing guard check clears in that same act. Task 2 puts a superseded banner on the blind
> brief. Task 3 closes.
>
> **★ WHAT THIS BATCH IS NOT.** **It renders no pack, withholds nothing, boots no session, derives
> nothing, compares nothing and places nothing.** It touches nothing of the `harmony-boundary` or
> `scoring-model` subjects: their manifest blocks, pack directories, families and reading files are
> byte-unchanged or the batch STOPs. **It authors no framework text and opens none of the three
> sealed placement-sample files.**
>
> **★ ONE BAR THAT IS NOT A CONVENIENCE AND MUST NOT BE READ AS ONE: THIS BATCH EDITS NOTHING UNDER
> `ratification_surfaces/` AND NOTHING IN THE PHASE-DEFINITION SURFACE.** Two banner edits there are
> arguably owed — a pointer at
> `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md` §3.3 saying its first NOT
> ALLOWED limb is set aside for the framework phase, and a superseded banner at
> `ratification_surfaces/cowork_withheld_family_framework_reading.md` saying the verdicts it asks for
> will not be taken. **Editing a ratified surface is not a batch's call and is not ordered here.**
> Both are put to the user by the writing side. **If you meet either file, you read it and you do not
> write it.**
>
> **★ THE ONE TIMING CONSTRAINT.** Task 0 lands `cowork_handoff.md`. **Whether the writing side's
> current entry is in that file is NOT a precondition** — this dispatch asserts no number of handoff
> entries and orders the count ESTABLISHED AT THE OBJECT by a blob-to-blob diff, whose result is
> reported. **What IS binding: `cowork_handoff.md` must not be edited between A1's check and Task 0's
> commit** — the writing side does not touch it while this batch runs.
>
> **★ WORDS USED IN THIS DISPATCH, EXPLAINED FIRST.** *The framework document* — the product of the
> framework phase: the layer decomposition, each layer's charter, and the boundary contracts between
> layers. *A deriving session* — one that writes what the analysis SHOULD do. *An informed deriving
> session* — one that may also read what this project's code and specifications say it DOES; that is
> what the ruling made this phase's session. *The boot pack* — the rendered directory a blind session
> reads. *The withheld family* — the per-subject authored cuts inside the generator. *The authored
> tables* — the per-subject structures inside `tools/audit/gen_derivation_boot_pack.py`.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md` —
> CONDITIONAL, and this batch does NOT meet the condition: rule (a)'s `gating_ids` only. **(2)**
> `cowork_handoff.md`, ITS CURRENT ENTRY, and the entry below it. **(3)**
> `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)** The three records this
> batch lands, whole: `cowork_rulings_2026_08_28_informed_framework_sitting.md` (this batch's
> occasion), `cowork_rulings_2026_08_28_ledger_precondition_sitting.md` (still unlanded from the
> previous session) and `PHASE_CONSTRAINTS_AND_STOP_RULES.md`. **(5)** The whole of
> `tools/audit/gen_derivation_boot_pack.py` — you delete from its authored tables and edit nothing
> else. **You do NOT open:** `ARCHITECTURE.md`; any of the three sealed placement-sample files;
> either existing pack directory except to prove it byte-unchanged.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS built,
> designed, scoped or run, no design, no repair, no derivation of any specification statement, NO
> SESSION BOOTED, no document archived, moved or deleted AS A FILE, **no open-items row created,
> flipped or discarded**, **no edit to any governing document, any register entry or any register
> source**, **no change to the generator other than the removal of the authored `framework` entries —
> the criterion machinery, the leak check's tests, `withhold_passage()`, the marker and every STOP are
> untouched**, **no edit to any `harmony-boundary` or `scoring-model` authored entry, rendered file or
> manifest record.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (CHECK mode) and again at
> the end (write mode, committed as the artifact of a real run). Verify every commit at the object by
> explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and `git diff` over the
> working tree are denied by the armed guard — use the per-path explicit-hash forms and
> `tools/audit/changed_paths.py`. Plain single-quoted `git commit -m '…'` subjects. The reserved-word
> and vocabulary rules bind every line this batch writes.**

## Ruling ledger (quoted at the record, not paraphrased)

- **`cowork_rulings_2026_08_28_informed_framework_sitting.md`, Ruling 1:** the framework phase's
  deriving session **is not implementation-blind**; *"no pack is rendered, no candidate verdicts are
  taken, no leak check is run."* The user's words: *"In my opinion 3 still stands, yours too it
  seems."*
- **The same record §2:** the two candidate lists *"are not ruled and are not owed"*, and
  `ratification_surfaces/cowork_withheld_family_framework_reading.md` is **KEPT, not deleted** (#12).
- **The same record §2:** the independent challenge run is **NOT commissioned** and the three sealed
  placement samples are **NOT disposed of**. **Neither is ordered here and neither may be treated as
  settled.**
- **Ruling 3 of `cowork_rulings_2026_08_15_phase_definition_sitting.md`:** *"an amendment is the
  user's ruling, never silently applied — the retrospective PROPOSES; the phase definitions … move
  only by ratification."* This is why `PHASE_CONSTRAINTS_AND_STOP_RULES.md` exists and why its shape
  is marked unruled on its own face.

## Premise ledger

**★ THE WRITING SIDE HAS NO SHELL AND ASSERTS NO GIT-OBJECT VALUE.** Every FACT below was read with
the file tools at the working tree on 2026-08-28; **no commit, blob or tree value is stated anywhere
in this dispatch, and none may be taken from it.** The tip was read as a file at
`.git/refs/heads/master` and is `bf3249e73d9eb91d0f2513bc2c16aa626b53e464`; **establish it, and
everything else, at the objects yourself.**

- **FACT** — `tools/audit/guard_state.json` → `summary` records run **75**, passing **71**, failing
  **4**, not_run 4. The four failing are `tools/audit/gen_filing_convention_application.py --check`,
  `tools/audit/decisions/apply_soft_discard.py --check`,
  `tools/audit/decisions/apply_residue_discard.py --check` and
  **`tools/audit/gen_derivation_boot_pack.py --check`**. **Re-read it yourself at the object.**
- **FACT** — `tools/audit/derivation_boot_pack.json` carries exactly **two** subject blocks,
  `harmony-boundary` and `scoring-model`. **The string `framework` does not occur anywhere in that
  file.** Both blocks are predicted BYTE-UNCHANGED after this batch.
- **FACT** — `tools/audit/gen_derivation_boot_pack.py` enumerates its subjects with
  **`for subject in sorted(WITHHELD)`**, and the string `framework` occurs in it at exactly **four**
  places: two authored dictionary entries — one in the `WITHHELD` table, one in the candidate-criterion
  table — and **two comments naming the dispatch those entries came from.** No other reference.
- **FACT** — the fourth STOP is *"a derived CANDIDATE with no authored verdict STOPS it"*, and it
  fires inside `build()` for the `framework` subject only.
- **FACT** — these files are on disk and **untracked** at the tree the writing side read:
  `cowork_rulings_2026_08_28_ledger_precondition_sitting.md`,
  `cowork_rulings_2026_08_28_informed_framework_sitting.md`,
  `cowork_informed_session_brief_framework.md`, `PHASE_CONSTRAINTS_AND_STOP_RULES.md`, and this
  dispatch.
- **FACT** — `cowork_blind_session_brief_framework.md` and
  `ratification_surfaces/cowork_withheld_family_framework_reading.md` are **tracked**.

- **ASSUMPTION A1 — the working tree, stated by CONTENT and not by count.** Exactly one tracked
  modification: `cowork_handoff.md`, additions-only and prepended above the committed content. **THIS
  DISPATCH ASSERTS NO NUMBER OF NEW HANDOFF ENTRIES** and orders the count established at the object
  by a blob-to-blob diff, reported. **A modification at any OTHER tracked path is a STOP-and-report.**
  Untracked and to be landed by Task 0: the five files named in the FACT above. The rest of the
  untracked population is NOT landed and is outside A1's STOP.
- **ASSUMPTION A2 — the guard state, and this is the batch's headline.** After Task 1 the full guard
  set reads **75 run, 72 passing, 3 failing** — the three known checks, each for its own recorded
  cause. **The fourth, `gen_derivation_boot_pack.py --check`, PASSES**, its cause having been removed.
  The membership check is red between Task 0's untracked landings and its own regeneration inside
  Task 0. **Any other red is classed by its measured cause; one outside this batch's own acts'
  subjects is a STOP-and-report.**
- **ASSUMPTION A3 — the manifest and the packs after Task 1.** `tools/audit/derivation_boot_pack.json`
  is **byte-identical** to its committed blob, and all fourteen files of the two pack directories are
  byte-identical to theirs, **compared one by one by hash.** **No `framework/` directory comes into
  existence at any point.**
- **ASSUMPTION A4 — no other derived artifact moves.** The previous batch measured that a
  module-level constant in this generator moves `tools/audit/recognizer_establishment_sort.json`; the
  framework keywords were inlined for that reason, so their removal is predicted to move nothing.
  **MEASURE every derived artifact before accepting it. A movement is reported with its measured
  cause, never absorbed**, and one whose cause is not this batch's own act is a STOP-and-report.

## The declared start state — at the tree you will meet

1. **Four** failing guard checks, named in the premise ledger. The fourth is the previous batch's own
   declared STOP and is what Task 1 removes.
2. `tools/audit/gen_evidence_pin_membership.py --check` STALE — caused by this dispatch's own
   untracked ruling records.

**A further failing verdict is a STOP-and-report.**

## Findings in this batch

**No finding number is allocated.** A tool shape this dispatch's instruction cannot reach without a
wider change is a STOP-and-report with the shape described, never a wider rewrite.

## Task 0 — land the five files, clear the membership check, and PUSH

1. **A1's check first, entirely at content-addressed objects**, including the handoff's
   additions-only property proven at a blob-to-blob diff and **the new-entry count established there
   and reported.**
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE the diff against the committed
   blob BEFORE accepting it.** Movement beyond the two landing ruling records' own additions is
   reported rather than absorbed.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (M),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_28_ledger_precondition_sitting.md` (A),
   `cowork_rulings_2026_08_28_informed_framework_sitting.md` (A),
   `cowork_informed_session_brief_framework.md` (A), `PHASE_CONSTRAINTS_AND_STOP_RULES.md` (A) and
   `cc_instruction_framework_arrangement_landing.md` (A). Subject, exactly:
   `record: the framework phase is authored informed — the 2026-08-28 rulings, the informed brief and the phase constraints-and-stop-rules home landed`.
4. **PUSH**; verify at the object.
5. **`gen_evidence_pin_membership.py --check` must PASS.**

**E0:** the committed paths; the established new-entry count; the check passing; `origin/master` at
the commit.

## Task 1 — RETIRE the `framework` subject from the generator — ONE commit

**(a) Delete the two authored `framework` entries** from `tools/audit/gen_derivation_boot_pack.py` —
the `WITHHELD` table entry and the candidate-criterion table entry — together with the two comments
that name only them. **Delete nothing else. Edit nothing else.** The criterion machinery, the leak
check, `withhold_passage()`, the marker, every STOP and both other subjects' entries are untouched, or
the batch STOPs.

**(b) State in the code, at the point of deletion, WHY the subject is not there** — one short comment
naming `cowork_rulings_2026_08_28_informed_framework_sitting.md` — so that a later reader does not
re-add it as an oversight. **This is a comment, not a restatement of the ruling** (#6, D-431): it
names the record and quotes nothing.

**(c) Run `python tools/audit/gen_derivation_boot_pack.py` in write mode and MEASURE the result before
accepting it.** A3 must hold: the manifest byte-identical to its committed blob, all fourteen pack
files byte-identical to theirs, no `framework/` directory. **Any byte that moves is a
STOP-and-report.**

**(d) `python tools/audit/gen_derivation_boot_pack.py --check` must exit 0.** If it does not, **STOP
and report what it says** — do not author a verdict, do not re-add the subject, do not touch the
criterion machinery.

**(e) Run the FULL guard set. A2, A3, A4 must hold.** Commit the generator and any derived artifact
whose movement (d) or the guard run caused and which you have measured and declared, subject:
`the framework subject is retired from the boot-pack generator — the ruling cancels its pack, and the fourth failing check goes green`.
Push; verify at the object.

**E1:** `gen_derivation_boot_pack.py --check` exits 0; the guard set reads 75 run, 72 passing, 3
failing with the three known; the manifest and all fourteen pack files byte-identical by hash; no
`framework/` directory in existence; the generator's diff carrying only the deletions of (a) and the
comment of (b).

## Task 2 — the superseded banner on the blind brief — ONE commit

**Add a banner to the head of `cowork_blind_session_brief_framework.md`** stating that it is
SUPERSEDED by `cowork_informed_session_brief_framework.md` under
`cowork_rulings_2026_08_28_informed_framework_sitting.md`, that it is **kept and not deleted** (#12)
as the record of what was designed under the previous arrangement and as the starting point for any
later blind run over this subject, and that **nothing else in the file is edited.** **Change no other
line of it.**

**Nothing under `ratification_surfaces/` is touched** — see the banner bar at the head of this
dispatch.

Commit, subject:
`the blind framework brief is marked superseded by the informed brief, and is kept`.
Push; verify at the object.

**E2:** the file's diff carries the banner and nothing else; the two `ratification_surfaces/` files
byte-unchanged.

## Task 3 — the close

1. One `STATUS.md` pointer entry per task that did work, per the OI-222 pointer convention — no
   count, no identity and no rendered value restated (**D-431**); the previous batch's entries moved
   through `gen_status_batch_bound.py --apply`; `gen_session_start_read_size.py` regenerated.
2. The report is `cc_report_framework_arrangement_landing.md`: both guard states, every SHA, E0–E2
   graded, A1–A4 graded, declared departures, the established handoff-entry count, and the plan lines
   — **the user rules the informed brief's §7 points (P1, P4, P5, P6, P7); then a session that has
   read neither the ruling record nor the current handoff entry authors the framework document; the
   independent challenge run and the sealed samples' disposition remain unruled.**
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state guard run.

**E3:** at the tree carrying the close, a fresh full guard run — **the three known failing checks and
no others, zero STOPs** — committed only after the run that produced it. **This gate is meetable as
written, unlike the previous batch's, and if it is not met the reason is reported rather than the gate
reinterpreted.**

## What this batch does NOT do

- **No pack rendered. Nothing withheld. No session booted. No derivation, no comparison, no placement
  test. No framework text authored.**
- **Nothing of the `harmony-boundary` or `scoring-model` subjects touched** — manifest blocks, pack
  directories, families and reading files byte-unchanged, or STOP.
- **Nothing under `ratification_surfaces/` edited**, and **no edit to
  `ratification_surfaces/cowork_phase_definition_surface_2026_08_15.md`**, whose §3.3 will still carry
  the set-aside clause on its face when this batch ends. **That is deliberate and is declared in the
  report**, the pointer being a user question and not a batch's call.
- **No edit to any governing document, register entry or register source**; `ARCHITECTURE.md` not
  opened; none of the three sealed placement-sample files opened, not in any portion.
- **No finding number.** [[OI-179]] OPEN and GATES.
- **No open-items row created, flipped or discarded** except an apparatus-finding row under rule (c).
  **No `src/` change, no golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`.**
- **It does not write the register entries owed for the two 2026-08-28 rulings.** The register
  blocker stands; the route is **D-652** and it is not this batch's business.

## The writing side's self-check over this dispatch

1. *Principles touched:* **#12** — the blind brief and the withheld-family reading file are KEPT and
   marked rather than deleted, and the retirement comment of Task 1(b) says why the subject is absent
   rather than leaving a silent hole; **#13** — a check that will not go green is a STOP surfaced, not
   a verdict authored to clear it; **#6** — the ruling has one home, the record, and every other file
   points at it rather than restating it; **#17f / D-431** — no count and no git-object value is
   asserted anywhere, and every predicted state is ordered measured; **#19** — nothing here claims the
   arrangement is established, and the #18 exposure the ruling carries is named in the record rather
   than quietly dropped. Conforms.
2. *Conventions:* American English; no self-invented labels; music-theory words in their musical
   sense — none arises in this batch.
3. *Figures and premises:* the guard summary, the manifest's two subject blocks, the generator's four
   `framework` occurrences and its subject loop were read at the files by the writing side on
   2026-08-28 and are ordered re-read at the objects. **The handoff's new-entry count is deliberately
   not asserted.**
4. *File-tools rule:* the writing side read only through the file tools and ran **no shell command of
   any kind** — the user barred it for that session in his opening instruction — **so it verified no
   git object and this dispatch relays none.**
5. *Uncertainty:* the measured-not-predicted items are the handoff-entry count, the byte-identity of
   the manifest and the fourteen pack files, any derived-artifact movement, and the post-Task-1 guard
   summary. **A2's prediction that the fourth check goes green is a PREDICTION and is registered as
   one**; if it fails, the shape is reported and nothing is authored to force it.
