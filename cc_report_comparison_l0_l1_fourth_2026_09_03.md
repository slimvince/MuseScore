# CC report — the L0/L1 comparison, FOURTH WRITING: the three whole-population sections written, nothing decided

> **This report decides nothing.** It records what was done, what was measured, and what was declared
> rather than asserted. Every disposition it refers to is a proposal living in
> `ratification_surfaces/cowork_comparison_l0_l1_reading.md`; none is applied anywhere. Where this
> report would name a hash it cannot contain — its own commit, and the end-state commit that follows —
> it points at the git log instead. **No count of the reading file is restated here** (**D-431**,
> #17f): the reading file is the one home for every figure, and this report points at it.

**Dispatch:** `cc_instruction_comparison_l0_l1_fourth_2026_09_03.md`, pinned at Task 0 to blob
`d6bd44db1b1b07e7dc6e0544df50d0801f794428` and proven unmoved immediately before staging.
**Full close:** `cowork_away_returns.md`, the section **THE L0/L1 COMPARISON TABULATED (FOURTH
WRITING), THE WHOLE-POPULATION SECTIONS WRITTEN, NOTHING DECIDED**, which carries the commit table,
both guard states, E0–E2, A1–A5, the departures and the plan lines. This report does not restate them;
it says what the batch actually did.

---

## 1. What was done

**The reading file is complete.** The three whole-population sections the third writing left marked
NOT YET WRITTEN were written whole, in the ordered order, **one commit each**:

- **§8 — the seventeen open questions.** Each open question of the derivation is carried with its own
  text, **ungraded**, and located: either at the row or rows of §6 where the outgoing record speaks to
  what it asks, or with the statement that the outgoing text says nothing to it. Two facts of the
  derivation's own record are relayed once and not repeated per entry — which open questions the
  derivation's §6.5 itself names, and that neither the derivation nor the reading file's independence
  section records any open question as later answered. **No open question is answered, ranked or
  recommended for answering.**
- **§9 — the derivation's own §7, five points.** Each is stated in the derivation's terms with what
  the tabulated rows show beside it, and **the two the sitting record's §3al.4 calls structural are
  named as structural in the words that record uses**. **No verdict is given on whether any point is
  right**, and none is graded against the outgoing record as a disposition.
- **§7 — the derived side.** A **per-row resolution table** at its head, then **one row per derived
  statement S-1 to S-54**, then the **two-way check stated as arithmetic** in both directions with
  §0's recorded figures quoted beside the derived sums.

**The banner, §0's stop statement, the member-boundary paragraph and the paragraph naming which
sections were owed were each brought up to date** so the file states its own state truthfully, with
every superseded wording preserved at its site (#12).

**Nothing was applied.** No row of §6 was revised, no outgoing text edited, the derivation untouched,
and **none of Rulings 34 to 62 was read or applied** — the dispatch places the application act in a
separate batch on sizing grounds, and this batch did not begin it or prepare for it.

## 2. How §7's attribution was resolved — the thing that stopped the third writing

The third writing stopped at §7 because the attribution of §6's axis lines to derived statements is
mechanical for most lines and not for a minority, and it declined to publish an arithmetic built on an
unresolved attribution. **This batch resolved the minority mechanically rather than by reading care.**

**The minority was enumerated, not sampled.** §6 was parsed out of the reading file's own git object,
read by explicit hash into a scratch copy outside the repository, and every axis line classified. The
non-mechanical remainder fell into **five classes**, each named at §7a: two verdict words for one named
statement; a statement named in a row's *Derived* line that the row's axis line does not grade; a
statement graded that the *Derived* line does not name; a row naming no derived statement at all; and
an axis block continuing past a line break. **Each class was given a carrying rule fixed before it was
applied**, and **every member was resolved at its own row — no row is recorded UNRESOLVED**. One class
returned empty in both directions. The counts are at §7a and are not restated here.

**The written section was then verified against the extraction it was generated from, twice** — once
when it was written and again at the file's final state — each time by parsing §7b back out of a
scratch copy taken at the file's own git object and comparing every row, every attribution and every
per-row count against the extraction. **Both runs returned an exact match, zero transcription errors.**
This was done because a section whose entire claim is that its arithmetic closes cannot rest on
careful copying; transcription is the one risk its construction actually carries, and it was closed
mechanically.

## 3. The two-way check, and the one difference

**The check closes**, and it is stated as arithmetic in both directions rather than asserted: the sum
over the 54 derived rows against the sum over the 29 documents' rows, with §0's recorded figures quoted
beside them.

**One difference from §0's recorded figures stands, and its cause was FOUND and is named at its row.**
It is one unit on each of three figures, and it was located rather than reconciled away: the derived
sums were compared **per document**, twenty-eight of the twenty-nine match their own verdict table
exactly, and the twenty-ninth over-counts by one at a single named row — a row whose *Derived* line
names two statements and whose axis line grades one. **That is the expected shape of such a difference
under the resolution table's own rules** — a per-row resolution moved a count — **not a defect**, and
§7c says so with the row named. **A difference with no cause found would have been a STOP** under the
dispatch's own terms; there is none. §0's figures are kept and marked as what they were (#12), with
§7c's derived sums now the figures of record.

## 4. What was measured, not assumed

- **The declared start state held exactly.** A full guard run before the first edit: **76 run, 10
  failing, 4 not run, 16 historical, zero STOPs** — the ten failing being the ten the dispatch's own
  FACT names, member for member, with no eleventh red. Both A2-predicted-stale checks passed at boot,
  so neither exception fired.
- **A1 was checked over the whole tracked population** with the sanctioned enumeration tool: exactly
  **one** tracked modification, the sitting record, and no second. **Its additions-only shape was
  proven at the object rather than accepted from the dispatch** — `git diff --numstat` at the explicit
  tip hash returned insertions and **zero** deletions in two hunks. The first hunk's git-reported
  position differs by two lines from the locator the dispatch quotes, so **the substantive test was
  settled at the blob's own content**: the lines standing at that position were read and are the
  closing sentence of the section the dispatch names, a blank line, and the heading that followed it.
  The four named untracked paths were confirmed present and confirmed absent from the tip.
- **A3 was measured before being accepted:** the membership artifact regenerated and compared against
  the committed blob — **byte-identical**, so it was **not** committed and Task 0 carried **five**
  paths rather than six.
- **The forward bound was measured, not assumed** — see §5.

## 5. Ruling 4's forward bound — re-aimed, run, and the prediction held

The third writing recorded that the bound could not be maintained over the second writing's entries,
because those entries name no dispatch and the tool derives what to move from that name.

**This batch re-aimed the tool at all five of its authored constants** — the base commit, the
then-previous dispatch, the act date, this dispatch and the task — **and appended to
`PREVIOUS_AIMINGS` rather than replacing it** (#12) with two rows: the second writing's aiming, now
superseded, **and a row recording that the third writing performed NO move**, carrying the tool's own
STOP message quoted from the third close and marked in the shape the backfilled 2026-08-26 row uses.
**The record of aimings therefore does not silently omit a batch** (#10, #12), and a later reader meets
the third writing's finding at the mechanism rather than only in a close.

**The move then ran and the dispatch's prediction held exactly: the third writing's two entries moved,
the second writing's two stayed, and there was no STOP.** The reconciliation the tool derives is green
in both limbs — every moved entry byte-present in the archive exactly once and absent from the
must-read — and `--check` passes at the end of the act. **No entry was moved by hand and no earlier
entry's text was edited.**

**The second writing's two entries remain in `STATUS.md`**, as the third close's finding records. That
is a declared state, not a STOP, and **no open-items row was created for it**.

## 6. Declared departures

1. **The dispatch was read from the working tree before it was pinned** — standing clause P-2's own
   fallback route, the user's opening line having named only the dispatch file. It was pinned at Task 0
   and its blob proven unmoved before staging.
2. **The start-state guard set ran in WRITE mode rather than CHECK mode**, and the cause was
   established at the tool's source rather than guessed: the runner tests only for `--check` in its
   argument list and otherwise takes its write branch, so an invocation meant to read its usage ran the
   full set and rewrote the artifact. **The artifact was then measured against the tip's blob and came
   back byte-identical** — which is precisely the comparison `--check` performs — so the ordered
   measurement was in fact taken and the tree was not moved by it. Declared, not presented as the
   ordered form.
3. **A correction was made to the reading file's §16 last bullet and is declared here rather than made
   quietly.** It said the file is not complete over its population and that §0 says which documents are
   done and which untouched — true of every earlier writing and made false by this one. It was
   corrected in place **with the former wording preserved at the site** (#12). It is a statement about
   the file's own state, which the dispatch's own item orders brought up to date; **no verdict,
   disposition or count changed.**
4. **A python extractor was run over scratch copies taken from git objects by explicit hash**, outside
   the repository, to read §6's axis lines mechanically and to verify §7 after it was written. **No
   working-tree file was read by any shell:** the copies were produced by content-addressed reads
   (`git show <hash>:path`, and `git hash-object -w` followed by `git cat-file -p <hash>`), which
   **D-253** permits, and every repository read went through the file tools. Declared on the precedent
   the ninety-fourth handover entry sets rather than assumed covered.
5. **A missing-object error was met and surfaced rather than worked around.** A `git diff` returned
   *"Could not access"*, and the cause was established before anything was touched: the shell's working
   directory had persisted into the scratch directory from an earlier invocation, so git was running
   outside the repository. Every later invocation runs from the repository root or in a subshell. **No
   fix was taken on a guess** (**D-669**).
6. **The shell-read guard refused one command on an unexpanded variable**, and the file tools were used
   instead — deny-on-indeterminate (**D-647**) working exactly as the record describes it.
7. **One working-tree `git diff` was run without an explicit hash, and the guard admitted it.** D-253
   permits git for read-only object queries named by an explicit hash; a diff against the working tree
   is not one. It named `tools/audit/gen_status_batch_bound.py` at the self-check. The next such
   command, naming `STATUS.md` among its paths, **was denied with the rule quoted** — which is how the
   departure was noticed. **No conclusion rests on it:** the forward-bound act is established by the
   tool's own reconciliation and its `--check`, and every reading-file diff in this batch was taken
   between two explicit commit hashes.

## 7. What this batch did not do

No decision on any disposition, any difference, any open question, §9's five points, the derivation,
the method, or the L0/L1 split. **No verdict word attached to the deriving session's independence
record.** No session booted. No measurement of the analysis built, designed, scoped or run. No `src/`
change, no golden, no test changed, moved or run, no build, nothing under `tools/corpus/` or
`tools/robust_stop/`. No open-items row created, flipped or discarded; no register entry and no
identifier allocated; no guard enrolment; no tool added. **No row of §6 revised, and that was measured
at the objects rather than asserted** — the diff from the tree met to the §7 commit is **eight hunks**:
one at the banner, three in §0, one at each of §7's, §8's and §9's own NOT YET WRITTEN markers, and one
at §16's last bullet. **Not one falls inside §6.** **No recommendation appears anywhere**, in the
reading file or in this report.

The one `tools/audit/*.py` edit is the re-aiming of `gen_status_batch_bound.py`'s five authored
constants and its `PREVIOUS_AIMINGS` list, which Task 2 orders in terms and which **D-648** classes as
authored-input maintenance.

## 8. Where the hashes are

The commit table — the tree met, Task 0, the §8 commit, the §9 commit, the §7 commit and the close — is
in the close section of `cowork_away_returns.md`, each by explicit hash, each pushed and each verified
equal at `.git/refs/remotes/origin/master` **with the file tools, never from a push's own output**.
**This report's own commit and the end-state commit that follows it are in the git log**, which is
where a document cannot name its own identifier. **The close does not assert the end state**; the one
further commit carries it.

*CC, 2026-09-03. This report recommends nothing and rules on nothing; the user is asked to rule nothing
in the reading file, which says so in its own closing section.*
