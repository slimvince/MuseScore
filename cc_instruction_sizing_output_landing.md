# CC dispatch — land the sizing derivation's output UNCOMPARED and UNREAD, with the blinding-failure ruling that SUSPENDS the method ruling; touch nothing else

> **Dispatch (Cowork, 2026-08-24, the forty-fifth Cowork session).** Executes Ruling 2 and §3 of
> `cowork_rulings_2026_08_24_blinding_failure_sitting.md`, and lands that record — whose Ruling 1
> **SUSPENDS a ruling already given** and which you land, not act on. **Written at a verified stop:
> the sizing-brief batch's four commits are landed and verified, the sizing session has delivered
> its output and ended, and nothing is running.** The tip was read by the writing side with
> `git for-each-ref` at the explicit hash `4f57ce5133` and at nothing else.
>
> **★ WHAT THIS BATCH IS.** Task 0 lands FOUR paths and the regenerated membership artifact, and
> PUSHES. Task 1 closes. **NO GENERATOR EDIT. NO PACK RE-RENDER. NO SESSION BOOTED. NOTHING
> DERIVED, NOTHING COMPARED, NO VERDICT ON ANYTHING.** The ordered structure yields **THREE
> commits**: Task 0, the close, the end state.
>
> **★ THE ONE RULE THAT MAKES THIS BATCH UNUSUAL — THE SAME ONE THE FIRST UNIT'S LANDING CARRIED.
> YOU DO NOT READ THE BLIND OUTPUT.** You are an ordinarily-booted session: your session-start read
> carries `CLAUDE.md`, which is exactly the material that unit's blinding failed against, so you
> can neither judge that output nor compare it — both belong to a later act under its own dispatch,
> and that act is HELD by Ruling 1. **You verify the file at its NAME, its SIZE (125,529 bytes),
> its sha256
> (`4887a9ab4dd16494cd7799b18babbfede83e51a40e11205920f1137a84a9861b`), its FIRST LINE and its
> BANNER line (*DRAFT — BLIND DERIVATION, NOT COMPARED, NOT RATIFIED*) — a bounded read of the
> first three lines with the file tools — and you open nothing further of it.** A hash or banner
> mismatch is a STOP-and-report. **A defect anywhere in it is a STOP, never a fix: it lands
> byte-identical or not at all.**
>
> **★ AND A SECOND BOUND, NEW TO THIS BATCH AND STATED BECAUSE IT IS COUNTER-INTUITIVE.** The
> record you land reports that a deriving session's boot carried the standing instruction file
> automatically. **That is a fact about the tooling, and this batch neither investigates it,
> reproduces it, nor proposes any mechanism for a boot that would exclude that file.** Do not
> edit, move, split or annotate `CLAUDE.md`; do not add a setting, an ignore rule or a wrapper
> anywhere. **If you find yourself designing a fix, STOP and report — the determination that
> governs this is the user's and is not a repository act.**
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md`
> — CONDITIONAL, and this batch MEETS the condition (it runs the guard set): read it in full; rule
> (a)'s `gating_ids` only. **(2)** `cowork_handoff.md`, ITS CURRENT (FIFTY-SIXTH) ENTRY. **(3)**
> `cowork_audit_protocol.md`'s dispatch-protocol section IN FULL. **(4)**
> `cowork_rulings_2026_08_24_blinding_failure_sitting.md`, whole — this batch's authority; **its
> Ruling 1 orders nothing you execute, and its §4 names what is HELD.** **You do NOT read:** the
> blind output beyond its first three lines; the first unit's blind output at all;
> `docs/scoring_model.md`; the sizing brief beyond its status banner; any rendered pack member;
> the boot-pack generator; any oracle document.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the ANALYSIS built,
> designed, scoped or run, no design, no repair, no derivation of any specification statement, NO
> SESSION BOOTED, no document archived, moved or deleted AS A FILE, **no open-items row created,
> flipped or discarded**, **no edit to any governing document — `CLAUDE.md` above all — to the
> generator, to either pack, to the manifest, to any register entry or to any register source**,
> **no edit to the blind output**, **no correction of the three deferred apparatus items**, and
> **no verdict of any kind on either blind output, on the method, or on the blinding failure.**
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit (CHECK mode) and again
> at the end (write mode, committed as the artifact of a real run). Verify every commit at the
> object by explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and
> `git diff` over the working tree time out on this mount and are denied by the armed guard — use
> the per-path explicit-hash forms named in A1. If staging is refused by a stale zero-length
> `index.lock` with no git process running, establish the cause (D-669), remove it, report the act.
> Plain single-quoted `git commit -m '…'` subjects, written to need no apostrophe. The reserved-word
> and vocabulary rules bind every line this batch writes.**

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 2 of the blinding-failure sitting:** *"`cowork_blind_derivation_scoring_model_2026_08_24.md`
  is committed by the next ordinary dispatch, byte-identical, uncompared, and unread by the
  landing batch — the first unit's precedent. Landing is record-keeping and not use. The landing
  record carries both exposures of §1, including the corrected salvage count, so that no later
  reader meets the output without them."*
- **Ruling 1 of the same record (landed, NOT executed):** the method ruling of 2026-08-24 is
  **SUSPENDED** pending a determination the user makes from evidence that already exists; no phase
  opens on it meanwhile; nothing landed is unlanded; **no mechanism for an instruction-file-free
  boot is proposed by anyone.**
- **§4 of the same record:** the framework phase, the detail-specification phase and the tests
  batch are **HELD**. **This batch opens none of them and prepares none of them.**

## Premise ledger

- **FACT** — the branch tip is `4f57ce5133` (parent `4f6a6d8dbb`), subject opening *"declared
  fourth commit: the A5 verification re-taken AFTER the last commit"*; both refs at it. Read by the
  writing side at the explicit hash on 2026-08-24. **Nothing is unpushed, and the sizing session
  committed nothing.**
- **FACT** — at the tip, `guard_state.json` → `summary` records run 75, passing 74, failing 1
  ([[OI-372]]'s tool), not_run 4, historical 16. **Re-read it yourself at the object.**
- **FACT** — at the tip, `evidence_pin_membership.json` → `ruling_records_read` is 58.
- **FACT, measured by the writing side on 2026-08-24 at the working-tree file:** the blind output
  is on disk at the repository root, untracked, **125,529 bytes**, sha256 as quoted above, its
  first line `# Blind derivation — how a harmonic analysis should score a candidate chord reading
  against the evidence` and its banner as quoted. **It carries the word `pinned` on no line**
  (counted without content entering this side's session).
- **FACT, measured by the writing side:** `cowork_rulings_2026_08_24_blinding_failure_sitting.md`
  is on disk and absent from the tip, and carries the word `pinned` on no line.
- **FACT** — at the tip, `derivation_boot_pack.json`, both pack directories, the generator and
  every governing document are what the previous batch left. **This batch predicts every one of
  them BYTE-UNCHANGED.**

- **ASSUMPTION A1 — the working tree, stated by CONTENT, and TAKEN FROM THE TREE.** Tracked
  modifications: **(i)** `cowork_handoff.md` — inserted entries above the committed content, the
  outgoing entry-point heading marked superseded, additions otherwise; **the COUNT of inserted
  entries is NOT asserted here — MEASURE it and report what you find; any count is admitted.**
  **A modification at any OTHER tracked path is a STOP-and-report** — in particular at `CLAUDE.md`,
  the generator, the manifest, either pack directory, or the sizing brief. Untracked and to be
  landed by Task 0: `cowork_blind_derivation_scoring_model_2026_08_24.md`,
  `cowork_rulings_2026_08_24_blinding_failure_sitting.md`, and this dispatch
  (`cc_instruction_sizing_output_landing.md`) — **three untracked paths.** **Check ordered (Task 0,
  first act):** the per-path explicit-hash forms, the bounded three-line receipt check on the blind
  output, and the whole tracked population enumerated with the sanctioned enumeration tool.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs. The
  reds this batch causes and must clear: the membership check (Task 0 step 2);
  `gen_session_start_read_size.py --check` at the close. Any other red is classed by its measured
  cause; one outside this batch's own acts' subjects is a STOP-and-report.

- **ASSUMPTION A3 — the membership regeneration.** **Route A** — the ruling-record count rises by
  exactly ONE (58 → 59), the added name being
  `cowork_rulings_2026_08_24_blinding_failure_sitting.md`. **The blind output is NOT a ruling
  record and must NOT enter route A — if it does, STOP-and-report.** **Route B** — neither landed
  file carries `pinned` on any line. **Route C** — no tool added. Measure against the committed
  blob at `4f57ce5133` before accepting; an additive derived cross-reference caused by the landed
  record's own naming of another document is REPORTED rather than absorbed; movement beyond is a
  STOP-and-report.

- **ASSUMPTION A4 — the guard registry.** No tool added; population UNCHANGED at run 75; failing
  set unchanged; the three other registry artifacts unchanged except run stamps. A stdout line
  older than this batch is not this batch's doing and is reported rather than absorbed.

- **ASSUMPTION A5 — BYTE-UNCHANGED by this batch, verified at the objects AFTER THE LAST COMMIT**
  (the term, as your predecessor established): `CLAUDE.md` and every other governing document, the
  generator, the manifest, both pack directories in full, the sizing brief, and both blind outputs'
  content. **Any difference at any of them is a STOP-and-report**, not a thing to explain.

## The declared start state — at the tree you will meet

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`** STALE — caused by this dispatch's own
   untracked ruling record.

**`gen_guard_state.py --check` opens "STALE vs the run" for the same cause (F83); not a third
failing check.** A THIRD failing verdict is a STOP-and-report.

## Findings in this batch

**No finding number is allocated; the series stands at F88.** Ruling 9's routing binds. **Anything
you notice ABOUT either blind output beyond the five receipt terms is outside this batch's read and
must not exist in your report. Anything you notice about the boot mechanism is outside this batch's
scope entirely — do not investigate it and do not report a design for it.**

## Task 0 — land the three files, clear the membership check, and PUSH

1. **A1's check first, entirely at content-addressed objects**, with the handoff's inserted-entry
   count MEASURED and reported, and the blind output's bounded three-line receipt taken.
2. **REGENERATE `evidence_pin_membership.json`, MEASURE against the committed blob BEFORE
   accepting**, against A3.
3. **Commit exactly these paths and no other:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_blind_derivation_scoring_model_2026_08_24.md` (new, byte-identical to the delivered
   file), `cowork_rulings_2026_08_24_blinding_failure_sitting.md` (new), and
   `cc_instruction_sizing_output_landing.md` (new) — **five paths.** Subject, exactly:
   `record: the sizing derivation output landed UNCOMPARED and UNREAD by the landing batch; the 2026-08-24 blinding-failure ruling landed and the method ruling stands SUSPENDED; evidence-pin membership regenerated`.
4. **PUSH**; verify at the object, and **re-verify that the landed blind output's blob and the
   working-tree file's own content hash are the same object id**, so the file landed
   byte-identical.
5. **`gen_evidence_pin_membership.py --check` must PASS.**

**E0:** the five paths; `ruling_records_read` 59 with the blind output absent from route A; the
check passing; `origin/master` at the Task 0 commit; the landed output byte-identical; **`CLAUDE.md`,
the generator, the manifest and both pack directories absent from the commit's own path set.**

## Task 1 — the close

1. One `STATUS.md` pointer entry for Task 0 — **it states no verdict, names no comparison, and
   does not restate the finding**: the output landed, uncompared, unread by the landing batch, and
   the method ruling stands suspended by the landed record. The previous batch's entries move
   through `gen_status_batch_bound.py --apply` at its three declared authored inputs (D-648,
   licensed here in terms); `gen_session_start_read_size.py` regenerated (A2).
2. The FULL close appended to `cowork_away_returns.md` as a **THE SIZING OUTPUT LANDED, THE METHOD
   RULING SUSPENDED** section — both guard states, every SHA, E0 and E2 graded, A1 and A3–A5
   graded, declared departures, quarantined questions (or none new), the plan lines (**the
   determination is the user's and nothing proceeds on the method ruling until it returns; the
   framework phase, the detail-specification phase and the tests batch are HELD; the ledger stays
   owed before the framework phase**), and **the plan's tell in one sentence: did this batch
   produce anything other than the five landed paths and the report? If yes, name it.** The report
   is `cc_report_sizing_output_landing.md`; hashes a commit cannot contain point at the git log —
   **no backfill commits.**
3. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; the one further commit carries it.
4. Commit, push, verify; then the one further commit with the end-state run. **THREE commits, and
   a correction commit beyond them is declared in BOTH surfaces if one proves necessary.**

**E2:** at the tree carrying the close, a fresh full guard run — every run check passing except
[[OI-372]]'s tool, zero STOPs, the population at run 75 — committed only after the run that
produced it.

## What this batch does NOT do

- **No comparison, no verdict, no reading of either blind output beyond the bounded receipt, no
  session booted, no oracle opened.**
- **No investigation of the boot mechanism and no fix designed for it.** No edit to `CLAUDE.md` of
  any kind — not a split, not a move, not an ignore rule, not a wrapper.
- **No opening or preparing of the framework phase, the detail-specification phase or the tests
  batch** — all three are HELD by the landed record.
- **No finding number.** [[OI-179]] OPEN and GATES; [[OI-372]], [[OI-374]] stand.
- **No open-items row created, flipped or discarded EXCEPT an apparatus-finding row under rule (c).
  No `src/` change, no golden, no test, nothing under `tools/corpus/` or `tools/robust_stop/`.**

## The writing side's self-check over this dispatch (recorded, per the standing clause)

1. *Principles touched:* **#19** — the output lands as an unverified authored deliverable whose
   blinding is known to have failed, and the landing record says so, so nothing is claimed
   established; **#12** — the suspended ruling's own text stands unamended and the correction is
   recorded beside it, and the output lands byte-identical; **#13** — both exposures are carried
   into the record rather than absorbed, with the sizing session's own salvage count corrected at
   the object; **#15** — every receipt term is measured, and the landing batch is barred from the
   judgments its own boot disqualifies it from; **the just-in-time rule and the standing
   sharpening** — nothing is prepared for the held acts and no fix is designed for a mechanism
   whose determination is the user's. Conforms.
2. *Conventions:* American English; no self-invented labels; music-theory words in their musical
   sense; towards, never against.
3. *Figures and premises:* the tip, the guard summary, the membership count and the blind output's
   size, hash, first line and banner read at the objects by the writing side on 2026-08-24 and
   ordered re-checked; **A1's handoff-entry count deliberately left to measurement.**
4. *File-tools rule:* the writing side's own departures are in the fifty-sixth handover entry.
5. *Uncertainty:* the one measure-and-report item is A3's possible additive cross-reference.
6. *Re-read from disk before release:* performed over the delivered files, hash-verified after
   re-staging (F74), recorded in the fifty-sixth handover entry.

---

*Provenance: Cowork, 2026-08-24, the forty-fifth session, written at branch tip `4f57ce5133` under
Ruling 2 and §3 of `cowork_rulings_2026_08_24_blinding_failure_sitting.md`. TOWARDS the ultimate
objective and TOWARDS the guiding principles.*
