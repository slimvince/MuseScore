# The owner rulings of 2026-08-07 — finish-line item 1's re-home class, ruled entry by entry

> **STATUS: RATIFIED RULING RECORD, written 2026-08-07 (Cowork), awaiting application.** The user
> ruled these on four decision surfaces delivered in this session's conversation ("I agree with
> recommendations" / "all ratified"), each surface presenting every entry with its candidates, the
> pros and cons naming their principles, and a recommendation; every recommendation was ratified
> unchanged. This file is the interim carrier of those rulings until the homing dispatch records
> and applies them (the same-commit rule, D-230, binds at that recording). It follows the
> precedent of `cowork_pending_rulings_2026_08_02.md`.
>
> **Scope.** The forty owner-not-determinate entries and the eight no-blocker entries of
> finish-line item 1's re-home class — the whole population `tools/audit/decisions/
> item1_rehome_blocker.json` holds outside the licensing class. (The licensing class's three
> refuted entries D-281, D-538, D-539 were ruled separately the same day and are carried by the
> active dispatch `cc_instruction_three_owner_rulings.md`, not by this file.)
>
> **None of these rulings authorizes a fix to the analysis, a design, or an inference change.**
> Every act below is a homing act: the decision written into the named section in that section's
> own voice, with its defense from the record or "derivation not recorded", the entry's former
> home, class and verbatim preserved (#12), provenance in the register's fields only.

## The family rules the user ratified, stated once (#6)

- **The scoring-surface rule (batches 1–3).** Rules, policies, falsifications, dead ends,
  promotion carries and retirement conditions of the legacy scoring surface home in
  `docs/scoring_model.md` — the governing document for the scoring pipeline, whose §8 exists to
  collect constraints and dead ends and which is a mandatory read for scoring sessions (C4 at
  the reader's actual location). LEGACY and DEFERRED markings ride along unchanged.
- **The joint-estimator section (batch 1).** `ARCHITECTURE.md` gains a dedicated joint-estimator
  SECTION (or the existing standing-rules block is formally converted into a headed section) —
  the production inference layer's rules must be a section a decision can be sited inside, not a
  banner. Authorized by the batch-1 ruling; `ARCHITECTURE.md` is inside the standing edit
  authorization.
- **The maintained-object rule (batches 1 and 3).** A maintained object and its maintenance or
  discovery method belong together: §6.7 owns both the canonical style taxonomy and the
  protocols and method that produce and re-produce it.
- **The producer-side publication rule (batch 2).** A published-fact invariant homes with the
  PRODUCER's output-surface specification (the fact-publication corollary); consumer sections
  point.
- **The boundary rule (batches 1 and 3).** A boundary invariant that binds every reader homes at
  the boundary's own section (§3.3); other documents point, never copy.

## The ruled destinations, entry by entry

| Entry | Destination | Note |
|---|---|---|
| D-280 | `ARCHITECTURE.md` §3.3 | boundary invariant (gates read structured fields only); `docs/scoring_model.md` may point |
| D-325 | `docs/scoring_model.md` | scoring-surface rule (correction rule retired/folded before search widening) |
| D-326 | `ARCHITECTURE.md` Layer 4 | the search's output-surface contract; dormancy stated beside it (a shelved section can be a home) |
| D-327 | `docs/scoring_model.md` | scoring-surface rule (root-continuity guard reads reconstructed inversion credit) |
| D-328 | `docs/scoring_model.md` §8 | recorded dead end (arpeggio root failure is the global optimum) |
| D-419 | `ARCHITECTURE.md` §7 | declared-dormancy constraint, future consumer named (the fact-publication corollary's shape) |
| D-421 | `ARCHITECTURE.md` §6.7 | maintained-object rule (re-discovery protocol; a changed cluster set is its own ratification event) |
| D-423 | `docs/scoring_model.md` | scoring-surface rule (three gate prohibitions + the retirement stage); LEGACY rides along |
| D-449 | the joint-estimator section | factor granularity, per factor |
| D-450 | the joint-estimator section | the key-signature/declared-mode prior conditions the initial state only |
| D-463 | `docs/scoring_model.md` | scoring-surface rule (temporal signals stay in the vertical scorer; the dependent gate moves with them) |
| D-464 | `ARCHITECTURE.md` §5.3 | the prohibition binds the structure §5.3 specifies; the redirect half states where new signals go instead |
| D-465 | `docs/scoring_model.md` | scoring-surface rule (the three-test policy for judging a proposed post-scoring gate) |
| D-469 | `ARCHITECTURE.md` §11.5 | the pipeline's scope owns the exclusion; §5.13 points; STOP if §11.5 does not state the scope in rule form |
| D-470 | `ARCHITECTURE.md` §5.3 | producer-side publication rule (extension fields recorded in the pass; consumers never rebuild) |
| D-471 | `ARCHITECTURE.md` §11.5 | the gate's pre-declared keep-or-drop protocol (#22 gives it its kind) |
| D-472 | `ARCHITECTURE.md` Layer 6 | the owning layer in the target architecture; §11.5 points; CC checks the arm — LEGACY mark if the smoothing pass is legacy-only |
| D-490 | `docs/scoring_model.md` | the override falsification; the family (490/491/492/493) stays in one place |
| D-491 | `docs/scoring_model.md` | the refuted vertically-fair repair |
| D-492 | `docs/scoring_model.md` | the recommended, NOT-ADOPTED demotion redesign — its not-adopted status stated so no reader takes a proposal for a specification; phase-3 fix-plan input per OI-2 |
| D-493 | `docs/scoring_model.md` | the restriction is un-computable today — completes the override evidence record |
| D-494 | SPLIT: `ARCHITECTURE.md` Layer 5 + Layer 3 | A-4's two separable obligations each at its layer, cross-pointing; the register entry records both homes |
| D-495 | `ARCHITECTURE.md` Layer 5, whole | the obligation is cadence admission's; the flat phrase-boundary profile is a published L1.5 fact it reads |
| D-501 | `ARCHITECTURE.md` §3.3 | one boundary invariant binding every reader INCLUDING measurement tools; `BUILD_AND_TEST.md` gains a pointer beside the tool contracts |
| D-510 | `docs/scoring_model.md` | the correct promotion carry (Gate A unification set) |
| D-511 | `docs/scoring_model.md` | one promotion primitive with the present-first dedup guard |
| D-512 | `docs/scoring_model.md` | Gate A's byte-for-byte retirement condition |
| D-532 | the joint-estimator section | the transition-table pooling level for secondary-dominant continuations |
| D-533 | the joint-estimator section | the leftover-probability back-off, proportional, never even, never impossible |
| D-534 | the joint-estimator section | the missing-tone penalty counted per chord factor |
| D-536 | `docs/scoring_model.md` | the joint (bass, root, template) selection; legacy-scorer change |
| D-537 | `docs/scoring_model.md` §4 | the completeness bonus and its guard |
| D-542 | `ARCHITECTURE.md` §6.7 | discover-then-name (maintained-object rule) |
| D-543 | `ARCHITECTURE.md` §6.7 | the study's encoding |
| D-544 | `ARCHITECTURE.md` §6.7 | confound control as a first-class validity gate |
| D-545 | `ARCHITECTURE.md` §6.7 | the external extractor and the prohibition on our own inference touching it |
| D-568 | `cowork_engage_arc_plan.md` | a work-programme rule; the arc plan owns the order of work (the D-539 ruling's rule); the user's ruling authorizes this one edit of that contract, as it did for D-539 |
| D-580 | `docs/scoring_model.md` | which gates survive the dissolution; DEFERRED rides along |
| D-605 | `ARCHITECTURE.md` Layer 3 | the input rule binds the hypothesis's derivation; the grouping section points |
| D-616 | `ARCHITECTURE.md` Layer 3 | CC checks the arm — LEGACY mark if both named mechanisms are legacy-scoped (the D-472 treatment) |
| D-625 | the section specifying the shared line-of-fifths primitive (Layer 4 per the record) | §5.14 points; STOP if the primitive is specified elsewhere |
| D-474 | `CLAUDE.md` principle #21 | FIELD RE-AIM ONLY — the content is already written there and names D-474 in terms |
| D-496 | `ARCHITECTURE.md` §7 | deferred decision stated with its trigger (the recognition-consumer build) — the D-419 shape |
| D-497 | `ARCHITECTURE.md` §6 + an open-items row | the RULE (the mark and its maintenance) at §6 beside the constants; the APPLICATION is owed work — CC checks whether a row tracks it (OI-7 the candidate) and creates one if none does |
| D-531 | `ARCHITECTURE.md` §2.2 | the standing not-yet-triggered verdict and its concrete trigger are the current state of the substitutability contract |
| D-604 | `CLAUDE.md` gate block (A) | grading convention, beside the conventions homed there |
| D-606 | `CLAUDE.md` gate block (A) | grading convention (the binding metric is modulation correctness); cites the abstain-aware convention beside it |
| D-622 | `ARCHITECTURE.md` Layer 3 | the OI-331 hold is lifted by the user's ruling; the bounded-context contract's annotation points at it, each surface stating its own concern |

## ★ Dated amendment, 2026-08-08 — D-472's arm check came back MIXED, and the user ruled the form

The homing wave held D-472 by its own table row's A5 check: the grouping rule is shared and runs
on the production record arm (`sectionrecordadapter.cpp:360`), while the Pass-4 stabilization the
entry's own text names as the grouping's precondition has one call site, inside the legacy arm
(`sectionanalyzer.cpp:750`). **The user ruled Option A on 2026-08-08:** D-472 is homed in the
Layer-6 section WITH THE ARM SPLIT STATED — the grouping rule specified as live, the
stabilization precondition beside it marked ⚠ LEGACY, each end carrying its citation — one home,
both halves visible (#12). **And the conformance question is ROWED as its own open item** (the
D-641 surfacing followed through): whether the record arm satisfies the stated stabilization
precondition by other means, or the live path runs the grouping on input its own specification
says must be stabilized first. The row is analysis-bearing; whether its read-only probe runs
before phase 2 or inside it is the user's timing call, not settled by this ruling.

## What executes this

The homing dispatch written when `cc_instruction_three_owner_rulings.md` returns. It carries
these rulings in its ruling ledger by reference to this file, applies them with the standard
assumption checks (a STOP wherever a named section does not exist or does not state rules —
the D-469 and D-625 STOPs are named in the table), regenerates everything that follows the
register, and leaves the tree uncommitted.
