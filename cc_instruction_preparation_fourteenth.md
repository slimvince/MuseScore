# CC dispatch — the preparation phase's FOURTEENTH batch: the standing-clause population marked at its own sites, the count retired as a second home, and two explicit do-nothings

> **Dispatch (Cowork, 2026-08-19).** Executes the three rulings of
> `cowork_rulings_2026_08_19_thirteenth_return.md`. **Written at a verified stop; nothing else is
> running and no dispatch was active when this was written.**
>
> **★ THIS IS A SMALL BATCH AND TWO OF THE THREE RULINGS ORDER NOTHING.** Ruling 1 accepts the sort's
> member-test widenings as executed and orders no review pass. Ruling 2 leaves the ESTABLISHED-side
> check a hand-check and orders no mechanism. **Both are do-nothings and both are ordered as such
> below, so that a later reader meets a decision rather than an omission.** The whole executable
> content of this batch is Ruling 3: one authored placement over one section, and the retirement of a
> count.
>
> **★ READ FIRST.** **(1)** `CLAUDE.md` and `STATUS.md` as they now stand, in full. **(2)**
> `DECISIONS.md` — unconditional, and explicitly NOT demoted. **(3)** `BUILD_AND_TEST.md` is a
> CONDITIONAL read — **and this batch MEETS the condition**, because it runs the guard set, so read it
> in full. **(4) DO NOT READ THE WHOLE OPEN-ITEMS INDEX.** Rule (a) reads the DERIVED GATING ANSWER
> narrowed to its identity list — `tools/audit/nongating_apparatus_rows.json` →
> `★_the_live_gating_answer` → `gating_ids`. The INDEX remains the AUTHORITATIVE STATUS SURFACE.
>
> **★ AND THE STANDING CLAUSE BINDS THIS ONE: read `cowork_handoff.md`, ITS CURRENT (THIRTY-THIRD)
> BLOCK.** **The findings series stands at F88 when this batch begins** — you allocated NO number at
> the thirteenth batch; the writing side allocated **F88** at the return sitting. **Number from F89.**
>
> **★ AND READ THE STANDING CLAUSES AT THE FILE, NOT AT A COUNT — WHICH IS THIS BATCH'S OWN SUBJECT
> AND IS WHY THIS BLOCK STATES NO NUMBER.** `cowork_audit_protocol.md`'s dispatch-protocol section is
> to be read **IN FULL**, and every clause binding a dispatch met, before Task 1 writes anything.
> **F88 is that the count in this position has never been checkable; this dispatch does not add one
> more instance of it.**
>
> **Then read, in full:** `cowork_rulings_2026_08_19_thirteenth_return.md` (this batch's whole
> authority, and **its §3 above all**, which carries the ruling, the test the placement applies, the
> two members it settles, and **a dated correction of the evidence the ruling was taken on**); **your
> own `cc_report_preparation_thirteenth.md`, its §4.e, §4.f and §10 above all** — because Ruling 1
> disposes of exactly what those sections return; and
> `cowork_rulings_2026_08_19_twelfth_return.md`, whose Ruling 1 is the clause this batch's placement
> must satisfy about its own status.
>
> **Bind at these artifacts, never at a recollection of them:**
> `tools/audit/evidence_pin_membership.json` (Task 0's subject);
> `tools/audit/recognizer_establishment_sort.json` (**read and never written** — Ruling 1 bars acting
> on it); `tools/audit/nongating_apparatus_rows.json` (**read and never written**);
> `tools/audit/guard_classification.json` and `tools/audit/guard_state.json`.
>
> **The standing bars bind this batch whole:** no `src/` edit, no golden, no test changed, moved or
> run, nothing under `tools/corpus/` or `tools/robust_stop/`, no measurement of the analysis, no
> design, no repair, no derivation of any specification, no mining, no document archived, moved or
> deleted AS A FILE, **no open-items row created, flipped or discarded**, and **no caller flag or
> candidacy acted on**.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first edit and again at the end.** The
> declared start state is below and is stated **at the tree you will actually meet**. **Verify every
> commit at the object by explicit hash.** **The reserved-word conventions bind all new prose. The
> new-tool rule stands. The E-ordering rule stands, at both grains — your last batch executed it
> correctly and its proof was READ IN THE REPORT BLOBS by the writing side, so do the same: state at
> the close that the end state is NOT asserted there, and let the further commit carry it.** **F3's
> reading rule stands** (`gen_cluster_dispositions.py --verify` is the drift authority;
> `reaim_home_anchors.py --check` exits 0 while printing drifted anchors and is not the authority —
> **it fired again in your own last batch**). **Every guard run is taken at a quiet tree.**
>
> **★ THE VOCABULARY RULE OF 2026-08-17 BINDS EVERY LINE THIS BATCH WRITES** — TOWARDS the ultimate
> objective and TOWARDS the guiding principles, never *against* either.

## Ruling ledger (quoted at the record, not paraphrased)

- **Ruling 1 — the member-test widenings, ACCEPTED AS EXECUTED:** *"The three corrections the sort's
  member test took while it was being built stand as executed. No review pass is ordered, and the
  narrow own-source-only test is not restored. The general form is recorded here, and this record is
  its home; no separate site is written and no finding number is allocated for it."*
- **Ruling 2 — the ESTABLISHED-side check, LEFT AS IT STANDS:** *"No mechanism is built to guard the
  ESTABLISHED direction. The bound stands as declared at the artifact, in the dangerous direction, in
  its own words. Nothing is added, nothing is re-published, and no verdict is re-taken."* — with its
  forward trigger: *"When the ESTABLISHED population grows beyond what can be read by hand, the route
  is the MECHANISM and not the provisional status."*
- **Ruling 3 — the standing-clause population, MARKED AT ITS OWN SITES:** *"Every `###` section of
  `cowork_audit_protocol.md`'s dispatch-protocol section is placed IN or OUT of the standing-clause
  population, and every member carries a one-line membership marker AT ITS OWN SECTION. The placement
  is DERIVED against the section population, both ways, with a HALT on any section the test cannot
  place — none in both, none in neither. No generator is written and no recognizer over prose is
  built. The two disputed members are settled by this ruling and are both IN."*

## Premise ledger

- **FACT** — the thirteenth batch is complete and pushed, verified at the objects by the writing side:
  `16a172715a` → `ae55748557` (4) → `2a909b47c2` (4) → `a33a933404` (5) → `1fdce14fdd` (6) →
  `9fbe98a305` (4) → `54874000cb` (1) → **`4e93ee84c3`** (1), every parent confirmed and every path
  count read from the object. **The terminus is `4e93ee84c3` and it carries the report alone.**
- **FACT** — `OPEN_ITEMS.md` (`6ae67d8603`) and `tools/audit/nongating_apparatus_rows.json`
  (`5bb43d0b3a`) are **byte-identical at EVERY commit of the thirteenth batch**, base to terminus,
  checked at all eight. **Nothing this batch does may move either; a difference in either at any task
  is a STOP-and-report.**
- **FACT** — `tools/audit/gen_nongating_apparatus_rows.py` is **byte-identical (`43ae5e1d15`) at every
  commit of the thirteenth batch**. It is read by nothing this batch does and must stay so.
- **FACT, measured by the writing side immediately before `cowork_handoff.md` was edited** — the file
  stood **UNMODIFIED at the terminus**, reproducing the committed blob `ada40e3602` exactly, at
  **408,269 bytes with ZERO carriage returns**. **So the F57 caveat does not arise for that file, and
  this is measured rather than assumed.**
- **FACT, measured at the git object and the ground of Task 1's declared population** — the
  dispatch-protocol section of `cowork_audit_protocol.md` carries **THIRTY-NINE `###` sections**, the
  section running from its `##` heading to end of file. **Task 1 DERIVES this count at the tree it
  meets rather than inheriting it from this dispatch, and a difference is not a STOP but is reported
  with its cause.**

- **ASSUMPTION A1 — the working tree, stated by CONTENT and never by a changed-passage count.** The
  tree carries exactly **ONE tracked modification**: `cowork_handoff.md`, whose content is **two parts
  of one act** — the THIRTY-THIRD session-close block inserted, and the THIRTY-SECOND heading carrying
  its entry-point demotion marker.
  **★ ONE PATH IS NAMED AS EXPECTED-UNMODIFIED BECAUSE TASK 0 WILL MOVE IT.**
  `tools/audit/evidence_pin_membership.json` is expected **UNMODIFIED**. **If it IS modified, that is
  accounted for by Task 0 step 2 and is NOT a STOP. A modification at any OTHER tracked path is a
  STOP-and-report.**
  **Untracked and to be landed by Task 0:** `cowork_rulings_2026_08_19_thirteenth_return.md` and this
  dispatch.
  **★ AND THE REST OF THE UNTRACKED POPULATION IS NAMED AS THE TREE CARRIES IT, NOT AS A CLASS — F82
  APPLIED RATHER THAN REPEATED.** The tree also carries newly visible `cc_instruction_*.md` files,
  `cc_*_report.md` and `cc_*_dossier.md` files, and a scratch directory. **This batch touches none of
  them**, and A1's STOP condition is scoped to TRACKED paths.
  **Check ordered (Task 0, first act, blob against blob by explicit hash)**, under the F57 caveat.

- **ASSUMPTION A2** — after all tasks, exactly ONE red remains ([[OI-372]]'s tool), zero STOPs.
  **Check ordered (each task's last step):** any other red is classed by its measured cause, and one
  outside this batch's own acts' subjects is a STOP-and-report. The reds this batch causes and must
  clear: **the membership check, cleared by Task 0 step 2**; **the F15 anchored-quote class**, because
  Task 1 edits `cowork_audit_protocol.md` at many sections and will move many line coordinates; and
  **`gen_session_start_read_size.py --check` at the close**, because Task 2 moves `STATUS.md` —
  **F79's own instance, expected, cleared by regenerating the measurement and by nothing else.**

- **ASSUMPTION A3 — the section is delimited by its own headings.** The dispatch-protocol section runs
  from its `##` heading to the next `##` heading or to end of file, and its members are the `###`
  headings inside it. **Check ordered (Task 1, first act):** if the section's bounds cannot be
  established from the headings alone, that is a STOP-and-report and nothing is written.

## The declared start state — at the tree you will meet, with the RECORD of the run named beside the VERDICTS

**73 guards run, 71 passing, TWO failing, 4 not run, 16 historical, ZERO STOPs**, and *"the guard
classification re-derives"*.

1. **`tools/audit/gen_filing_convention_application.py --check`** — [[OI-372]], the one standing red.
2. **`tools/audit/gen_evidence_pin_membership.py --check`**, reporting its artifact STALE — **caused by
   this dispatch's own input**: one untracked root-level `cowork_rulings_*.md` now sits there,
   `cowork_rulings_2026_08_19_thirteenth_return.md`.

**★ AND THE GUARD REGISTRY'S OWN ARTIFACT DOES NOT RE-DERIVE EITHER, NAMED HERE RATHER THAN LEFT — F83
APPLIED RATHER THAN REPEATED.** `gen_guard_state.py --check` will open *"STALE vs the run"*, because
the committed registry records the thirteenth batch's end state — one failing — while the tree has
two, and **the same input causes both**. **It is not a third failing check**: the registry's own
`not_a_subject_of_this_run` field records why the runner is not a subject of its own run. **The
declared start state is MATCHED when the verdicts match, and no STOP is owed for the registry's
drift.**

**★ THE PREDICTION ABOUT THE MEMBERSHIP DIFFERENCE IS TAKEN FROM BOTH ROUTES.** **Route A** — the count
rises by one from its committed value and one record name is added. **Route B** — the writing side
probed the new ruling record at its own text before this dispatch was written: **exactly ONE line
carries the word `pinned`, and that line carries no backtick-quoted `*.py` name, so route B is
predicted to add NOTHING and no third hunk is expected.** **Both halves are predictions and not
citations — check them at the artifact.**

**A THIRD failing check among the verdicts, or a difference in the membership artifact reaching any
member, route, document, pin constant, state or count, is a STOP-and-report.**

## Task 0 — land the four paths, and clear the membership check in the same act

1. **A1's check first, entirely at content-addressed objects**, under the F57 caveat.
2. **REGENERATE `tools/audit/evidence_pin_membership.json` and MEASURE its difference against the
   committed blob BEFORE accepting it**, against **both** halves of the prediction. **If any member,
   route, document, pin constant, state or count MOVES, that is a STOP-and-report.** An ADDED derived
   cross-reference whose cause is this batch's own ordered act is not such a movement — that is the
   sharpened bar standing at `cowork_audit_protocol.md` — but it is reported with its cause
   established at the record's own text.
3. **Commit exactly these FOUR paths and no fifth:** `cowork_handoff.md` (modified),
   `tools/audit/evidence_pin_membership.json` (regenerated),
   `cowork_rulings_2026_08_19_thirteenth_return.md` (new), `cc_instruction_preparation_fourteenth.md`
   (new). Staged plainly; push; verify at the object.
4. **Re-run `gen_evidence_pin_membership.py --check` at the tree this commit carries: it must PASS.**

**Registered expectation E0:** exactly 4 paths; two modifications matching A1's shape and step 2's
bounded regeneration, two additions; no staging override; the membership check passing at the
resulting tree, so the guard set stands at **73 run, 72 passing, ONE failing** from Task 0 onward.

## Task 1 — Ruling 3: the membership marker at every IN section, and the count retired as a second home

1. **A3's check first.** Establish the section's bounds from the headings alone and **enumerate every
   `###` section inside it**. Derive the count at the tree; report it with its cause if it differs from
   the thirty-nine the premise ledger names.
2. **PLACE EVERY SECTION, IN OR OUT, BY THE TEST RULING 3 STATES — and the test is quoted, not
   invented:** a section is **IN** when all three hold — **(i)** it states a rule rather than recording
   a finding; **(ii)** the rule binds how a dispatch, a session report or a ruling record is WRITTEN,
   SEQUENCED or EXECUTED, that is, it binds one of the two sides in the conduct of a dispatch cycle;
   and **(iii)** this section is that rule's home rather than a pointer to it. A section failing any of
   the three is **OUT**. **★ A SECTION THE TEST CANNOT PLACE IS A STOP-AND-REPORT, never defaulted to
   either side.**
3. **RECONCILE BOTH WAYS IN THE ACT: none in both, none in neither.** Every enumerated section is
   placed exactly once, and the two sides sum to the enumeration. **A section in both or in neither
   halts the task.**
4. **★ THE TWO DISPUTED MEMBERS ARE RULED IN BY THE RECORD AND ARE NOT RE-DECIDED HERE** — the
   read-first clause naming the current handover block, and the sitting-record interim-carrier clause.
   Ruling 3 states the reason for each; import it, do not re-argue it.
5. **WRITE A ONE-LINE MEMBERSHIP MARKER AT EACH IN SECTION**, in one edit over the file. The marker is
   the same line at every member so it is greppable by a reader without a tool, and it carries nothing
   but the membership — **no count, no index, no ordinal, and no cross-reference to any other member**,
   because an ordinal is a second value that goes stale on the next insertion.
6. **★ RETIRE THE COUNT RATHER THAN RESTATE IT, WHICH IS THE HALF THAT DISCHARGES F88.** Write, at the
   dispatch-protocol section's own head, the convention that **the standing clauses are the sections
   carrying the marker, that a session obtains them by reading them, and that the number is NEVER
   restated in a dispatch, a handover block, a report or a close (#6, D-431).** **Do not write a count
   anywhere in this act.** A dispatch's read-first block from here names the marker and not a number.
7. **The anchored-quote consequence is the F15 class and is cleared by its ruled treatment** — anchors
   re-aimed PER CITATION from the drift report's own reported start line, and **never by an assumed
   uniform shift**, which matters more here than in your last batch because this edit inserts at MANY
   points rather than one and no uniform shift exists; affected derivations regenerated;
   `gen_cluster_dispositions.py --verify` the drift authority. **Any regenerated artifact's own
   difference is MEASURED before it is accepted**, and every changed line must carry a
   `cowork_audit_protocol.md` coordinate and nothing else — **counted, not sampled.**
8. **★ STATE WHAT THIS PLACEMENT IS, HONESTLY, IN THE REPORT.** The section population is externally
   enumerable, so the placement reconciles both ways against a side this act does not author. **But the
   placement itself is AUTHORED per section and is not a per-run guard** — nothing re-checks it on a
   later run, and a section added later carries no marker until someone writes one. **Say so in those
   terms.** A report that presents this as an established mechanism is a STOP-and-report.
9. Commit, push, verify at the object.

**Registered expectation E1:** every `###` section of the dispatch-protocol section is enumerated and
placed exactly once, with none in both and none in neither and a STOP on any the test cannot place;
every IN section carries the identical one-line marker and no member carries a count, an ordinal or a
cross-reference; the two disputed members are IN; **the retirement convention stands at the section's
head and NO count is written anywhere in the act**; the placement's authored, non-guard character is
stated in the report; the drift authority reports every re-aimed anchor correct at its new coordinate.

## Task 2 — the close, and the two do-nothings recorded as decisions

1. **★ RECORD THE TWO DO-NOTHINGS AS RULED DECISIONS, in the close and in the report**, so that a
   later reader meets a decision rather than an omission:
   **(a) Ruling 1** — the sort's member-test widenings stand as executed; no review pass was run, the
   narrow test was not restored, and `tools/audit/recognizer_establishment_sort.json` and its tool are
   untouched. The ground recorded is that the artifact publishes as a **LOWER BOUND with UNMEASURED
   reach**, so a wider test raises a declared floor and cannot be wrong in the damaging direction; and
   that the general form — *a static test over a single source file systematically misreads exactly
   the code that obeys a one-home rule; the blind spot is correlated with compliance* — **is homed at
   the ruling record and is not to be written to a second site.**
   **(b) Ruling 2** — no mechanism was built for the ESTABLISHED direction, no verdict was re-taken and
   no bound was re-published. The ground recorded is the population — three members, one the seed and
   two read at source against an independently authored record — and **the forward trigger is named:
   when that population grows beyond hand-reading, the route is the MECHANISM and not a provisional
   status, the latter excluded because it would put one verdict in two states (#6).**
2. One `STATUS.md` pointer entry per task, and **in the same act the THIRTEENTH batch's entries move
   verbatim to `STATUS_ARCHIVE.md`** (`gen_status_batch_bound.py`), both directions proven. **The
   declared `Last updated: ` prefix adjustment is imported, not re-decided (#6).**
3. The FULL close is appended to `cowork_away_returns.md` as a **PREPARATION FOURTEENTH BATCH**
   section: both guard states, every SHA, every registered expectation graded MET or NOT MET with its
   reason, every declared departure, and the carried findings — **F1–F88, surfaced and none rowed; the
   row bar stands whole this batch.** The report is `cc_report_preparation_fourteenth.md`.
4. **★ THE CLOSE DOES NOT ASSERT THE END STATE.** State at its head that the end-state run is taken
   after the commit carrying it exists and that its values land in ONE FURTHER commit. **Do not write a
   row, a cell or a sentence asserting the end state into the close commit** — the writing side
   verified your last batch's compliance **by reading the report BLOB at each commit and finding §13
   absent from the close commit**, so the test is on the text and not on the path-set; meet it there.
5. **★ AND NAME THE CHAIN AS FAR AS A SENTENCE CAN NAME IT, DECLARING THE ONE COMMIT IT CANNOT** — the
   shape your last two batches established. Import it; do not re-invent it.
6. Commit, push, verify at the object.

**Registered expectation E2:** at the tree carrying the close, a fresh full guard run re-derives —
**every run check passing except [[OI-372]]'s tool, zero STOPs** — run and read, never inferred, its
values committed only after the run that produced them, **and no sentence asserting them written
before that run existed**; both do-nothings recorded as decisions with their grounds and the forward
trigger named.

## What this batch does NOT do

- **No review pass over the sort's member test**, no re-derivation of the sort, and **no edit to
  `gen_recognizer_establishment_sort.py` or its artifact** — Ruling 1 accepts them as executed.
- **No mechanism for the ESTABLISHED direction**, no verdict re-taken, no bound re-published, and **no
  provisional status introduced** — Ruling 2 leaves it and excludes that route.
- **No generator is written and no recognizer over prose is built** for the standing-clause population
  — Ruling 3 refuses that route in terms, and building one would be answering F84 by doing the thing
  F84 warns against.
- **No count is written anywhere** — retiring it is the point of Task 1 step 6.
- **No pin is taken, restored, moved or re-taken.** The five pinned and two NOT PINNED members are
  untouched, and **no ratification document is opened for writing.**
- **No curated boot list**, no archiving pass, no re-opening of Ruling 2 of the session-start-read
  sitting.
- **No fate on any `CLAUDE.md` span**; `gen_claude_md_finer_archive.py --apply` is not run.
- **No change to rule (a)**; `tools/audit/nongating_apparatus_rows.json` and
  `tools/audit/gen_nongating_apparatus_rows.py` are **byte-unchanged at every task**.
- **No candidacy acted on, no census re-pin, no caller flag moved.**
- **No F71 sweep of the audit generators** — recorded and unowned.
- **No mining, no empirical findings ledger, no fact-gate admission**, no derivation, design, repair or
  pilot act.
- **No `src/` change, no golden, no test changed, moved or run, nothing under `tools/corpus/` or
  `tools/robust_stop/`, no measurement of the analysis.**
- **No open-items row created, flipped or discarded.** [[OI-372]] and [[OI-374]] stay exactly as found;
  **[[OI-179]] stays OPEN and GATES**; F3 stays surfaced, unfixed and unrowed.
- **`gen_gating_row_sizing.py`'s frozen population is left alone.**
