# CC Instruction — L5 close-out: commit the Cowork close-review docs + pull the §15-8 backlog notes (docs only)

> **Docs only. No code, no build, no measurement, no production movement.** Two tasks: **(A)** commit the Cowork doc
> edits from the L5-close synthesis as one commit; **(B)** pull the §15-8 project-memory backlog notes and check them
> against the L5 vocabulary, **report** (declare, do not act on, any vocabulary judgment call). Then L5 is formally
> closed. *(Reminder: the never-bash rule is Cowork's; you may read the project-memory file.)*

## PART A — Commit the Cowork close-review docs (local, unpushed)
Stage and commit **only** these tracked Cowork `*.md` docs (Cowork-authored this round; do NOT stage gitignored reports
or instruction files):
- `cowork_layer5_function_design.md` — the merged close-review tidies: §5.6 over-trigger note (+ D1 inertness proof, +
  D2 broader-class); §15-3 reduction-pin marked ✅ done (lock-in test ref); §11 (+ D3 two-confidence-scales, + phrase-
  boundary non-chorale validation gap, + engagement-deferred framing); §5.5 Transition clarity.
- `contrapunctus_findings.md` — the L6-grouping research addendum (DCML 4-layer target; flat-phrase finding; the
  verifiability caveat) that you left for Cowork last round.
- `cowork_target_architecture.md` — the new architecture-wide methodological note (**verifiability is a risk posture,
  not a permission gate**, user-ratified 2026-06-29), added to §2.
- Message: `docs(cowork): close L5 — merged close-review tidies + L6 research addendum + verifiability methodology note`.
- Report the sha + the exact committed file list (confirm only these three docs moved).

## PART B — Pull the §15-8 backlog notes (read + report; declare, do not act)
- Read the project-memory file (`C:\Users\vince\.claude\projects\c--s-MS\memory\project_chord_analyzer.md`) and extract
  the **two backlog notes** §15-8 names: **(i) extended harmonic functions** and **(ii) cadence internationalization**.
- Check each against the **as-built L5 function vocabulary** (the §5.6 relational-label set + the §3/§7 Roman-numeral
  completeness, and the §5.2 cadence-type set): is the vocabulary already complete with respect to the note, or does the
  note name a construct (a harmonic-function category, a label, a localization) the L5 vocabulary does not yet cover?
- **Report** per note: covered / not-covered, with the specific construct if not. **Declare** any vocabulary gap as a
  judgment call for Cowork (a vocabulary addition is a spec change — Cowork's, not yours). Do **not** edit the vocabulary.

## §Gate (docs only)
- After Part A: working tree clean but for gitignored reports/instructions; **HEAD advances by the one docs commit only**;
  **no** `src/`/`tools/` change; corpus untouched. Part B is read-only (no commit unless Cowork later rules a change).
  Any code/build/measurement/production movement → STOP. `upstream` → STOP.

## §Deliver
Report: the Part-A sha + file list; the Part-B per-note coverage finding (covered / the specific gap), declared for
Cowork. With Part A committed and Part B reported, **L5 is formally closed** — next is the L6 design (Cowork,
research-first; scope: the validatable flat-phrase/key-area/cadence core, with hierarchy/form an explicitly-weighed
extension under the new verifiability contract).
