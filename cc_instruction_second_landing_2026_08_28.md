# CC dispatch — LAND the unit-question ruling record, the §9.0 decision surface, and the register-repair stop report. No repair, no design, no prepend. Boot NO session.

> **★ THIS IS THE ACTIVE DISPATCH. Dated dispatch note, 2026-08-28: written and released the same
> day. It carries no `⏸ PARKED` banner and none is to be added.** It may go straight to a session.
>
> **★ THE PIN ORDER, MADE PERFORMABLE — the previous batch's finding 4 applied.** The previous
> landing batch established that "pin before reading" cannot be performed when the user's opening
> instruction names only this file. **So: the suggested opening instruction to the executing session
> is — *"pin `cc_instruction_second_landing_2026_08_28.md` to a blob with `git hash-object -w`, then
> read it from that object, and execute it."*** Where the session has instead read this file from the
> working tree first, that is DECLARED, not a STOP: pin it at Task 0 and prove at Task 1 that the
> blob has not moved, exactly as the previous batch did.
>
> **★ WHO WROTE IT, DECLARED SO YOU CAN WEIGH IT.** The Cowork writing side of the fifty-ninth
> session, on 2026-08-28, with no counted error so far this session — and it inherits the record's
> standing caution about dispatch writing: **read this dispatch's own bars sceptically and STOP on a
> contradiction rather than resolving it in this dispatch's favour.** The writing side declares it
> does not touch this file or any file this batch lands while the batch runs.
>
> **★ WHAT THIS BATCH IS.** Task 0 pins and establishes. Task 1 lands three untracked root-level
> documents and this dispatch. Task 2 closes. **That is the whole of it. It repairs nothing, designs
> nothing, derives nothing, binds nothing, and prepends nothing** — no handoff entry rides this
> batch; the eighty-first entry is written at the fifty-ninth session's own close, later.
>
> **★ WHY IT EXISTS, IN ONE SENTENCE.** The user ruled §9.0 of the framework document on 2026-08-28
> — a unit is a DECISION the analysis makes about the music — and the ruling record, the decision
> surface it was ruled on, and the previous CC session's stop report on the parked register dispatch
> currently exist only as untracked files on one machine, **in git nowhere and pushed nowhere**; the
> standing end-of-session cadence is that work is pushed for backup, and the previous landing report
> states in terms that a landing act for two of these three is owed (`cc_report_landing_2026_08_28.md`
> §5.3, §5.4, §10).
>
> **★ READ FIRST.** **(1)** `CLAUDE.md`, `STATUS.md`, `DECISIONS.md` in full; `BUILD_AND_TEST.md` —
> CONDITIONAL, and **this batch does NOT meet the condition** (it builds nothing, runs no test, runs
> no measurement tool of the analysis): rule (a)'s `gating_ids` only. **(2)** `cowork_handoff.md`'s
> CURRENT ENTRY — the eightieth, at the head of the file since the previous batch's prepend.
> **(3)** `cc_report_landing_2026_08_28.md` in full — the previous batch's report; its §5.3 and §5.4
> name two of the three files this batch lands. **(4)** `cowork_audit_protocol.md`'s
> dispatch-protocol section in full. **You do NOT open:** any of the three sealed placement-sample
> files; either pack directory's contents; `ARCHITECTURE.md`; `cowork_framework_document_draft_2026_08_28.md`.
>
> **The standing bars bind this batch whole:** **no `src/` edit**, no golden, no test changed, moved
> or run, nothing under `tools/corpus/` or `tools/robust_stop/`, **no measurement of the ANALYSIS**,
> no design, no repair, no derivation of any specification statement, **NO SESSION BOOTED**, no
> document archived or moved AS A FILE, **no open-items row created, flipped or discarded**, **no
> edit to any governing document, any register entry or any register source**, and **no edit to the
> CONTENT of any file this batch lands — every landed path is committed exactly as it stands on
> disk.** **The forward-bound re-aiming of `gen_status_batch_bound.py --apply` in Task 2 is EXCEPTED
> BY NAME** under Ruling 5 of `cowork_rulings_2026_08_26_amendment_landing_sitting.md`.
>
> **Commit-and-push per task. Run the FULL guard set BEFORE the first act (CHECK mode) and again at
> the end (write mode, committed as the artifact of a real run). Verify every commit at the object by
> explicit hash. Every shell command carries `; echo "exit:$?"`. `git status` and a working-tree
> `git diff` are denied by the armed guard — use the per-path explicit-hash forms and
> `tools/audit/changed_paths.py`. Plain single-quoted `git commit -m '…'` subjects.**

## Premise ledger

**★ THE WRITING SIDE HAS NO SHELL AND ASSERTS NO GIT-OBJECT VALUE.** Everything below was read with
the file tools on bridge-staged snapshots on 2026-08-28, after the previous landing batch's report
landed. **No commit, blob or tree value is stated anywhere in this dispatch and none may be taken
from it.** The tip has moved past the previous batch's named end-state commit — its report's own
declared fourth commit could not name itself — **and this dispatch deliberately states no value for
it. Establish it at the object.**

- **FACT — the three files this batch lands exist at the repository root**, read at the files by the
  writing side: `cowork_rulings_2026_08_28_unit_question_sitting.md` (the ruling record, written by
  this writing side and placed on disk after the previous batch closed),
  `cowork_unit_question_surface_2026_08_28.md` (the decision surface, which the previous batch found
  appearing mid-batch, reported and did not land), and `cc_report_register_baseline_repair.md` (the
  previous CC session's stop report on the parked register dispatch, which the previous landing
  batch was ordered not to land).
- **FACT — the guard set's three known failing checks** are
  `gen_filing_convention_application.py --check`, `decisions/apply_soft_discard.py --check` and
  `decisions/apply_residue_discard.py --check`. **The register repair that would clear the last two
  is PARKED and is not this batch's business.**

- **ASSUMPTION A1 — the working tree, by SHAPE and never by count.**
  1. **NO tracked modification is expected** outside `STATUS.md` and the artifacts Task 2
     regenerates. **Any tracked modification found at Task 0 is a STOP-and-report.**
  2. The untracked root-level population includes the three files this batch lands, this dispatch,
     and the large historical dispatch-file population the previous report enumerated (its §5.4).
     **Enumerate with `tools/audit/changed_paths.py` and REPORT what you find before acting.**
- **ASSUMPTION A2 — the guard state at the start.** The three known failing checks, **plus
  `gen_evidence_pin_membership.py --check` expected RED** — that derivation's population is every
  root-level `cowork_rulings_*.md` on the FILE SYSTEM, and this batch's own untracked input
  `cowork_rulings_2026_08_28_unit_question_sitting.md` entered that population after the artifact
  was last regenerated. **Measure it and grade it; carry neither this statement nor any earlier
  one. A fifth failing verdict is a STOP-and-report.**
- **ASSUMPTION A3 — this batch's footprint is exactly what its own orders move, and nothing else.**
  **Stated as a list of the acts, not as a blanket:** the three paths Task 1 lands plus this
  dispatch; `tools/audit/evidence_pin_membership.json` regenerated; and in Task 2 `STATUS.md`,
  `STATUS_ARCHIVE.md`, `tools/audit/status_batch_bound.json`,
  `tools/audit/gen_status_batch_bound.py` (**the named carve-out**),
  `tools/audit/session_start_read_size.json`, the report, and `tools/audit/guard_state.json` at the
  end-state commit. **Movement at any path outside that list that is caused by this batch's own
  orders is a STOP-and-report; a movement no order of this batch caused is reported and graded, as
  the previous batch's §5.3 did.**

## The declared start state

**Four failing guard checks expected** — the three known, each for its own recorded cause, plus
`gen_evidence_pin_membership.py --check` RED for the stated mechanism. **Measured, not carried:** a
fifth is a STOP-and-report; fewer is reported with the mechanism established, as the previous batch
did.

## Task 0 — pin, then establish. Nothing is written in this task.

1. **PIN THIS INSTRUCTION** with `git hash-object -w`, report the blob identity, and take every
   subsequent read of it from `git cat-file blob <hash>` into a scratch file outside the repository.
   Where this file was read from the working tree before the pin, declare it and prove at Task 1
   that the blob has not moved.
2. **Establish the tip** at the object, and `origin/master` beside it.
3. **Run the FULL guard set in CHECK mode** and grade A2 from that run.
4. **Enumerate with `tools/audit/changed_paths.py`** and grade A1.

**E0:** the dispatch blob reported; the tip and `origin/master` at the object; A1 and A2 graded from
measurement.

## Task 1 — land the three documents, PUSH

1. **LAND THESE UNTRACKED ROOT-LEVEL PATHS, each exactly as it stands on disk.** Pin each to a blob
   before staging and re-establish each blob at the commit object afterwards. Confirm each is
   present and untracked at Task 0's enumeration; **a path in this list that is absent, or already
   tracked, is a STOP-and-report:**
   `cowork_rulings_2026_08_28_unit_question_sitting.md`,
   `cowork_unit_question_surface_2026_08_28.md`,
   `cc_report_register_baseline_repair.md`,
   `cc_instruction_second_landing_2026_08_28.md` (this dispatch).
2. **★ REPORT, AND DO NOT LAND, any OTHER untracked root-level file matching `cowork_*.md`,
   `cc_instruction_*.md` or `cc_report_*.md` that item 1 does not name** — with one carve-out:
   **`cc_report_second_landing_2026_08_28.md` does not exist yet and is committed by Task 2, which
   is not a breach of this item.** The historical dispatch-file population the previous report
   enumerated is reported by pattern and count only, not re-listed. **Nothing outside those three
   patterns is landed by this batch at all.**
3. **REGENERATE `tools/audit/evidence_pin_membership.json`** and **MEASURE its diff against the
   committed blob before accepting it.** The expected movement is the one new ruling record entering
   the membership; movement beyond this batch's own landed paths is reported rather than absorbed.
4. **Commit; the subject, exactly:**
   `record: land the unit-question ruling and its decision surface, and the register-repair stop report`
5. **PUSH**; verify at the object. **`gen_evidence_pin_membership.py --check` must PASS.**

**E1:** every path of item 1 committed exactly as it stood (blob-identical before staging and at the
commit object); no path outside item 1 committed by Task 1; the membership check passing;
`origin/master` at the commit.

## Task 2 — the close

1. One `STATUS.md` pointer entry per task, per the OI-222 pointer convention — **no count, no
   identity and no rendered value restated** (**D-431**). The previous batch's entries move through
   **`gen_status_batch_bound.py --apply`** — **the named carve-out** — and
   `gen_session_start_read_size.py` regenerates.
2. The report is **`cc_report_second_landing_2026_08_28.md`**: both guard states, every SHA, E0 and
   E1 graded, A1–A3 graded, declared departures, the measured membership movement, the full item-2
   findings, and the plan lines.
3. **★ THE PLAN LINES, AND THE FIRST ONE IS THE POINT OF SAYING THEM.** **This batch closes nothing
   and moves the framework phase not at all** — it is a backup act. The phase's postcondition is a
   **ratified** framework, held until the user's external research list arrives and is
   dispositioned. Then, the state as it stands: **§9.0 is RULED** — a unit is a decision the
   analysis makes about the music; the ruling record and its surface are what Task 1 lands, and the
   layer-ownership question (Δ2) is thereby gradable and NOT decided. Owed and not done: **the
   placement test**, carrying the record's twice-stated condition that a session with a shell
   precede reliance on its results, which is still unanswered; **the phase's retrospective** (§3.9
   of the phase-definition surface), which must land before the next phase opens and does not
   exist; and **everything the eightieth entry's backlog carries**, including the eleven
   quarantined audit questions that belong to the AUDIT and must not be worked before it, and the
   mixed dispatch-file population the previous report surfaced, on which nothing in the record says
   which state is intended — a user question, not a session's.
4. **★ THE CLOSE DOES NOT ASSERT THE END STATE**; one further commit carries it. **Where the
   report's own SHA table cannot carry the close and end-state identities because they commit the
   report itself, fill them by one further commit that changes nothing else, as the previous batch
   did and declared.**
5. Commit, push, verify; then the further commit(s) with the end-state guard run.

**E2:** at the tree carrying the close, a fresh full guard run — **the three known failing checks
and no others, zero STOPs** — committed only after the run that produced it.

## What this batch does NOT do

- **It edits no landed file's content.** Every path is committed exactly as it stands.
- **It performs no repair.** The register blocker stands; the parked register dispatch is neither
  run nor revalidated; **the §8 move is not performed.**
- **It writes no decisions-register entry and allocates no `D-NNN`.** The register cannot accept
  one; `cowork_register_rule_c_suspension_2026_08_28.md` is the route and its list is still
  underived.
- **It creates no open-items row and allocates no finding number**, though it lands documents that
  owe several.
- **It does not touch the eleven quarantined audit questions**, reserved to the AUDIT.
- **It acts on the historical dispatch-file population in no way** — that is a user question.
- **No `src/` change, no test, no golden, no build, no measurement of the analysis, no session
  booted, no design, no framework text.** None of the three sealed placement-sample files is
  opened; `ARCHITECTURE.md` is not opened; the framework document is not opened.

## The writing side's self-check over this dispatch

1. *Principles touched:* **#12** — nothing is deleted, nothing rewritten; every landed path is
   committed as it stands. **#6** — the ruling record has one home and this batch adds no second
   one. **#13** — the previous batch's unperformable pin order is answered structurally at the head
   rather than repeated. **#17f / D-431** — no count and no git-object value asserted anywhere; the
   tree state, the guard state and the membership movement are ordered measured.
2. *Conventions:* American English; no self-invented label; no music-theory word arises in this
   batch's subject matter in a non-musical sense.
3. *Figures and premises:* every premise was read at the files by the writing side on 2026-08-28
   after the previous batch's report landed, and is ordered re-established at the objects.
4. *File-tools rule:* the writing side read only through the file tools and ran **no shell command
   of any kind** — the shell bar stands as the user stated it — **so it verified no git object and
   this dispatch relays none.**
5. *Uncertainty:* the measured-not-predicted items are the tree state, the membership check's start
   verdict and movement, and the tracked status of every path item 1 names.
6. *The recorded dispatch-writing failures, each answered structurally:* the pin is ordered in a
   performable form (the head); A3 is stated as a list of this batch's own ordered acts, with
   third-party movement graded rather than blanket-barred; and no sentence in this dispatch is
   quoted from a file the writing side did not open this session.
